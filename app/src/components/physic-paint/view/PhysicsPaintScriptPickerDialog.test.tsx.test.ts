// Vitest is intentionally configured for `src/**/*.test.ts` only. This import-only
// launcher keeps the plan's exact `.test.tsx` command usable without changing the
// shared test configuration or duplicating the sentinel behavior.
import './PhysicsPaintScriptPickerDialog.test.tsx';
