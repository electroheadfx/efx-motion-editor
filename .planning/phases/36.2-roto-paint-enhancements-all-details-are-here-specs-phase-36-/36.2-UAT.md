---
status: diagnosed
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
source: [36.2-01-SUMMARY.md, 36.2-02-SUMMARY.md, 36.2-03-SUMMARY.md, 36.2-04-SUMMARY.md, 36.2-05-SUMMARY.md, 36.2-06-SUMMARY.md]
started: 2026-06-17T14:41:09Z
updated: 2026-06-17T18:41:11Z
---

## Current Test

[testing complete]

## Tests

### 1. Roto Cell Cache Colors and Save Copy
expected: In standalone Physics Paint Roto mode, the workflow strip shows empty Roto frames as gray, cached-only real-key frames as green, editable-session frames as pink, and the current frame as an outline over the existing fill. Dirty frames show subtle pending/saving status text and the action copy reads Save pending or Save current without introducing another cell color.
result: pass

### 2. Roto Save on Leave and Close
expected: After painting or editing a Roto frame, leaving the frame, clicking Save pending/current, or closing/unloading the standalone saves the pending Roto frame through the normal apply path. Returning to the frame shows the newly cached Roto image as a repaintable visual reference rather than losing it or compositing it twice.
result: issue
reported: "When closing Physics Painter without changing frame or saving, the cache is lost. When painting then closing Physics Painter, reopening shows the cached paint but with the stroke attenuated like small transparency; moving through frames makes all frames pink, including frames without cache. After saving the project, quitting, reopening the project and Roto Paint, the EFX Physics cache paint is lost even though cache exists in the EFX Motion app."
severity: major

### 3. Main Editor Cached Roto Preview After Standalone Return
expected: After leaving or closing standalone Physics Paint with a saved Roto frame, the main EFX Motion editor shows the Roto cached frame preview/marker for the corresponding frame and uses that cached image for preview/export without requiring a manual refresh or replaying editable strokes.
result: pass

### 4. Generated-Only Frame Launch Redirect
expected: Opening standalone Physics Paint from a generated interpolation-only Roto frame targets the nearest real Roto key frame instead of opening the generated frame as an editable square.
result: issue
reported: "I can't reproduce because I dont know how generate interpolation roto frame from efx-physics, I dont see any tool setting for that."
severity: major

### 5. Cached Roto Playback Transport
expected: In Roto mode with cached frames available, the compact Roto Play control previews cached PNG frames only, changes to Stop while active, shows missing-frame status copy for transparent/background-only gaps, and stops when you stop playback, navigate manually, edit, or change workflow mode.
result: pass

### 6. Roto Interpolation Connector UI
expected: With at least two real cached Roto keys, enabling interpolation with one or more in-betweens shows connector lines between the real key squares. Generated interpolation frames do not appear as editable green or pink boxes in the standalone strip.
result: issue
reported: "Where is interpolation enabling? All I see is navigation, play button, the UI message 'Playing 6 cached Roto frame(s). Missing frames play transparent/background.', and Save current button. All empty frames become pink, so I am lost. In this test I can't enable interpolation."
severity: major

### 7. Generated Roto In-Betweens in Preview and Export
expected: After interpolation regenerates, the main EFX Motion preview and export use the generated cached in-between Roto frames between real keys while still treating them as render-only cache frames.
result: skipped
reason: "Cannot test because the previous checkpoint showed no discoverable way to enable/generate Roto interpolation from EFX Physics."

### 8. Roto Real-Key Utilities
expected: Duplicate key, Insert frame, Delete frame, Copy frame, and Paste frame operate on real Roto key squares only. Real keys shift, duplicate, replace, or delete as expected, generated interpolation cache updates afterward, and generated-only frames never become editable targets.
result: issue
reported: "Nothing of that is in the UI; it was not coded. The visible Physics Paint UI only shows brush tools, color/onion/motion panels, navigation, Play, status text, Save current, and the Roto strip. There are no Duplicate key, Insert frame, Delete frame, Copy frame, or Paste frame controls."
severity: major

### 9. Missing Roto Frame Background Behavior
expected: When a Roto playback/export frame is missing but the layer has active paper/background behavior, preview/export renders the background-only result virtually without creating a new green cached cell; missing transparent frames remain transparent.
result: issue
reported: "no work"
severity: major

## Summary

total: 9
passed: 3
issues: 5
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "After painting or editing a Roto frame, leaving the frame, clicking Save pending/current, or closing/unloading the standalone saves the pending Roto frame through the normal apply path. Returning to the frame shows the newly cached Roto image as a repaintable visual reference rather than losing it or compositing it twice."
  status: failed
  reason: "User reported: When closing Physics Painter without changing frame or saving, the cache is lost. When painting then closing Physics Painter, reopening shows the cached paint but with the stroke attenuated like small transparency; moving through frames makes all frames pink, including frames without cache. After saving the project, quitting, reopening the project and Roto Paint, the EFX Physics cache paint is lost even though cache exists in the EFX Motion app."
  severity: major
  test: 2
  root_cause: "Close-save starts async flush without blocking standalone close; occupiedRotoFrames is used as editableRotoFrames so visited/background-only frames become pink; cached Roto references render semi-transparent; persisted rendered cache is not restored as per-frame editable Roto state."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      issue: "flushCurrentRotoFrameBeforeClose starts flushRotoFrame without awaiting close; snapshotCurrentRotoFrame adds background-only frames to occupiedRotoFrames; workflow strip receives editableRotoFrames=occupiedRotoFrames."
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      issue: "cached Roto reference overlay uses 0.78 opacity, making reopened cached paint look attenuated."
    - path: "app/src/lib/physicPaintBridge.ts"
      issue: "launch context hydrates cachedRotoFrames but not per-frame editable Roto state."
    - path: "app/src/stores/physicPaintStore.ts"
      issue: "applyCanvas stores layer-level editable state only, not per-Roto-frame editable states."
  missing:
    - "Await/sequence close flush before closing standalone."
    - "Separate editable real-key frames from cached/visited/background-only frames."
    - "Restore cached Roto state intentionally as editable state or clearly non-editable reference without pink cell state."
  debug_session: "agent-ad9a38414bb27afe2"

- truth: "Opening standalone Physics Paint from a generated interpolation-only Roto frame targets the nearest real Roto key frame instead of opening the generated frame as an editable square."
  status: failed
  reason: "User reported: I can't reproduce because I dont know how generate interpolation roto frame from efx-physics, I dont see any tool setting for that."
  severity: major
  test: 4
  root_cause: "Generated-only redirect exists behind store/bridge code, but no user-facing interpolation control exists to generate interpolation frames, so the redirect path is not reachable from the UI."
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      issue: "setRotoInterpolationSettings and regenerateRotoInterpolationCache exist but no visible UI calls them."
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      issue: "Interpolation connectors render only from existing enabled settings; the strip exposes no enable/count/mode controls."
  missing:
    - "Add visible Roto interpolation controls that can generate interpolation frames."
  debug_session: "agent-af16c4e0ed0b52e03"

- truth: "With at least two real cached Roto keys, enabling interpolation with one or more in-betweens shows connector lines between the real key squares. Generated interpolation frames do not appear as editable green or pink boxes in the standalone strip."
  status: failed
  reason: "User reported: Where is interpolation enabling? All I see is navigation, play button, the UI message 'Playing 6 cached Roto frame(s). Missing frames play transparent/background.', and Save current button. All empty frames become pink, so I am lost. In this test I can't enable interpolation."
  severity: major
  test: 6
  root_cause: "Interpolation settings are never exposed in the standalone UI, and empty/background-only visited frames are classified as editable-session pink because occupiedRotoFrames is passed as editableRotoFrames."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      issue: "shouldPersistRotoFrame persists background-only frames; snapshotCurrentRotoFrame adds them to occupiedRotoFrames; editableRotoFrames prop receives occupiedRotoFrames."
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      issue: "No interpolation enable/count/mode controls are rendered; status text competes with the control area."
  missing:
    - "Expose interpolation enable/count/mode controls in Roto UI."
    - "Keep empty/background-only frames gray unless they are true editable real keys."
  debug_session: "agent-af16c4e0ed0b52e03"

- truth: "Duplicate key, Insert frame, Delete frame, Copy frame, and Paste frame operate on real Roto key squares only. Real keys shift, duplicate, replace, or delete as expected, generated interpolation cache updates afterward, and generated-only frames never become editable targets."
  status: failed
  reason: "User reported: Nothing of that is in the UI; it was not coded. The visible Physics Paint UI only shows brush tools, color/onion/motion panels, navigation, Play, status text, Save current, and the Roto strip. There are no Duplicate key, Insert frame, Delete frame, Copy frame, or Paste frame controls."
  severity: major
  test: 8
  root_cause: "Real-key utility helper logic and keyboard handlers exist, but there are no visible Duplicate/Insert/Delete/Copy/Paste controls in the standalone Roto UI."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintWorkflowState.ts"
      issue: "duplicateRotoKeyFrame, insertRotoKeyFrame, deleteRotoKeyFrame, and replaceRotoKeyFrame helper functions exist."
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      issue: "duplicateRotoKey, insertRotoFrame, deleteRotoFrame, copyRotoFrame, and pasteRotoFrame handlers exist but are only wired to keyboard/help paths, not visible buttons."
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      issue: "Roto strip renders navigation/play/save controls but no key utility controls."
  missing:
    - "Add visible Duplicate key, Insert frame, Delete frame, Copy frame, and Paste frame controls in the Roto UI."
  debug_session: "agent-af16c4e0ed0b52e03"

- truth: "When a Roto playback/export frame is missing but the layer has active paper/background behavior, preview/export renders the background-only result virtually without creating a new green cached cell; missing transparent frames remain transparent."
  status: failed
  reason: "User reported: no work"
  severity: major
  test: 9
  root_cause: "Preview/export exits before missing physic-paint frames can resolve virtual backgrounds; background state is read only from layer.paintBgColor, while Physics Paint paper/background settings are not persisted as layer metadata; the resolver only supports flat colors and cached playback maps gaps to null."
  artifacts:
    - path: "app/src/lib/previewRenderer.ts"
      issue: "renderFrame hasDrawable preflight can return before the missing physic-paint frame branch calls resolveMissingRotoFrameDraw; getMissingRotoBackgroundState only checks layer.paintBgColor."
    - path: "app/src/lib/rotoFrameDraw.ts"
      issue: "resolveMissingRotoFrameDraw supports only transparent or solid background color, not Physics Paint paper/canvas grain modes."
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      issue: "getRotoCachedPlaybackFrames/toggleRotoCachedPlayback maps missing frames to null rather than synthesizing transient background-only preview frames."
  missing:
    - "Persist non-cache Roto background/paper settings as layer metadata."
    - "Let preview/export draw missing physic-paint frames when virtual background metadata is non-transparent."
    - "Synthesize transient background-only playback frames without creating green cached cells."
  debug_session: "agent-a7be0e0f6f8d7bf65"
