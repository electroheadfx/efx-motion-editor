---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 04
type: execute
wave: 3
depends_on: [36.2-01]
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/lib/previewRenderer.ts
  - app/src/lib/exportRenderer.ts
  - app/src/lib/exportRenderer.test.ts
autonomous: true
requirements:
  - PH36.2-CACHE
  - PH36.2-MISSING
  - PH36.2-PLAYBACK
must_haves:
  truths:
    - "D-18/D-19/D-20: Missing non-cached Roto frames render transparent or virtual background-only according to background state without creating green cached cells."
    - "D-21: Missing one side of an interpolation span creates no generated interpolation cache."
    - "D-01/D-27: EFX Motion preview/export uses cached Roto PNG frames quickly, including generated cache, without stroke replay."
  artifacts:
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Launch context cached Roto summary hydration and generated-only redirect target data"
    - path: "app/src/lib/previewRenderer.ts"
      provides: "Existing cached-frame preview path kept as source of playback truth"
    - path: "app/src/lib/exportRenderer.ts"
      provides: "Export path continues delegating to PreviewRenderer cached-frame behavior"
  key_links:
    - from: "physicPaintBridge.ts"
      to: "PhysicsPaintStudio.tsx"
      via: "cachedRotoFrames launch-context field"
      pattern: "cachedRotoFrames"
  prohibitions:
    - statement: "Do not add a stroke replay path or batch renderFromStrokes path for Roto playback/export."
      status: "resolved"
      verification: "Preview/export tests assert cached frame lookup remains the physics paint render path."
---

<objective>
Hydrate cached Roto frame summaries on launch and lock playback/export to the rendered cache path with missing-frame semantics from D-18 through D-21.

Purpose: EFX Motion should show Roto output from cached PNG frames immediately and consistently, while missing/empty frames remain virtual behavior and never pollute the cache.
Output: Bridge launch context cache summaries, renderer/export validation, and no replay/batch path.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
@SPECS/phase-36.2-physics-paint-roto-cache.md
@app/src/lib/physicPaintBridge.ts
@app/src/lib/physicPaintBridge.test.ts
@app/src/lib/previewRenderer.ts
@app/src/lib/exportRenderer.ts
@app/src/lib/exportRenderer.test.ts
@app/src/stores/physicPaintStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Include cached Roto frame summaries in launch context</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/types/physicPaint.ts
    - app/src/stores/physicPaintStore.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
  </read_first>
  <behavior>
    - Launching Roto includes all cached Roto frames for the layer, not only the current frame.
    - Generated-only interpolation frames are identified as render-only and expose nearest real key frame metadata.
    - Opening a generated-only frame can be redirected by consumers to the nearest real Roto key per D-33.
  </behavior>
  <action>Update `createPhysicPaintLaunchContext()` to call `physicPaintStore.getRotoCacheFrames(layerId)` when launching a physics-paint layer. Populate `cachedRotoFrames` with real-key and generated-interpolation metadata from Plan 01. If the current frame is generated-only and has `nearestRealKeyFrame`, set the launch `startFrame` or an explicit selection field so the standalone opens the nearest real Roto key, not the generated frame, per D-33. Keep Play script containment behavior from Phase 36.1 intact and do not confuse Play ranges with Roto generated cache.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/lib/physicPaintBridge.test.ts src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Bridge test creates two cached Roto frames and asserts both appear in `cachedRotoFrames` on launch.
    - Bridge test creates a generated-only frame with nearest real key and asserts launch opens or points at the nearest real key.
    - Existing saved Play script launch tests still pass unchanged.
  </acceptance_criteria>
  <done>Standalone Roto opens with full cached frame awareness and generated frames do not become editable targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify cached playback/export and virtual missing-frame behavior</name>
  <files>app/src/lib/previewRenderer.ts, app/src/lib/exportRenderer.ts, app/src/lib/exportRenderer.test.ts</files>
  <read_first>
    - app/src/lib/previewRenderer.ts
    - app/src/lib/exportRenderer.ts
    - app/src/lib/exportRenderer.test.ts
    - app/src/stores/physicPaintStore.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
  </read_first>
  <behavior>
    - Preview/export draw physics-paint layers only from `physicPaintStore.getFrame(layerId, paintLookupFrame)`.
    - A missing transparent Roto frame does not create or fetch a cache entry.
    - Background-only missing-frame behavior is virtual playback/export behavior only and does not write a green cached cell.
    - No `renderFromStrokes`, headless adapter, or engine replay path is introduced.
  </behavior>
  <action>Unconditionally implement a named pure helper for D-18/D-19/D-20 missing-frame playback/export behavior, such as `resolveMissingRotoFrameDraw(layerId, frame, backgroundState)`, in the renderer layer used by preview/export. The helper must read the same paper/background state that existing preview/export rendering uses for the sequence/canvas (for example transparent vs paper/background mode) and return an explicit draw instruction: transparent/no-op when no paper/background is active, or virtual background-only draw when paper/background is active. Wire preview/export integration so frames with no real Roto key and no generated interpolation cache call this resolver after `physicPaintStore.getFrame(layerId, paintLookupFrame)` returns null; the background-only path must draw virtually in playback/export only, without calling `setFrame`, `upsertRealRotoKeyFrame`, `replaceGeneratedRotoCache`, or any store mutation, and without adding green Roto cells. Keep exportRenderer delegating through PreviewRenderer; do not add a parallel Roto renderer. Add source assertions that physics paint rendering still uses `physicPaintStore.getFrame` and `void physicPaintVersion.value` subscription.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/lib/exportRenderer.test.ts src/stores/physicPaintStore.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Tests/source assertions find `physicPaintStore.getFrame(paintLayerId, paintLookupFrame)` in `previewRenderer.ts`.
    - Unit tests prove a missing Roto frame with transparent background returns/draws transparent no-op output.
    - Unit tests prove a missing Roto frame with active paper/background returns/draws virtual background-only output.
    - Unit tests prove neither missing-frame path calls `setFrame`, `upsertRealRotoKeyFrame`, `replaceGeneratedRotoCache`, or creates cached/green Roto cell metadata.
    - Tests/source assertions verify `exportRenderer.ts` delegates normal frame drawing to `renderer.renderFrame` and does not import the physics paint engine.
    - Negative source check confirms `renderFromStrokes` does not appear in modified renderer/export files outside comments or tests that explicitly forbid it.
  </acceptance_criteria>
  <done>EFX Motion playback/export remains fast cached-PNG compositing with virtual missing-frame behavior and no replay path.</done>
</task>

</tasks>

## Artifacts this phase produces

- `cachedRotoFrames` launch hydration path in `createPhysicPaintLaunchContext`
- Generated-only nearest-real-key launch behavior
- Pure missing Roto playback/export resolver, e.g. `resolveMissingRotoFrameDraw(layerId, frame, backgroundState)`, used unconditionally for transparent vs virtual paper/background output without store mutation
- Renderer/export source-contract tests for cached physics paint frames

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor store -> standalone launch context | Cached frame metadata crosses to standalone window/browser fallback. |
| Renderer -> image decode/cache | PNG data URLs become image elements for preview/export. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-10 | Tampering | `createPhysicPaintLaunchContext` | mitigate | Use validated store metadata and typed launch-context validators for cached Roto summaries. |
| T-36.2-11 | Denial of Service | Preview/export cached frame rendering | mitigate | Continue using existing image cache path and frame lookup; avoid all-frame rerender/replay. |
| T-36.2-12 | Tampering | Missing-frame handling | mitigate | Missing/background virtual behavior must not mutate cache or create green cells. |
| T-36.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit states no install. |
</threat_model>

<verification>
Focused bridge/export/store tests; full wave gate with `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- --run` and `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
Reopening standalone sees all cached Roto frames, generated interpolation frames do not open as editable targets, and EFX Motion preview/export remains a cached PNG compositor.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-04-SUMMARY.md` when done.
</output>
