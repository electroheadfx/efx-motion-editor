---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 03
type: execute
wave: 3
depends_on: [36.2-01, 36.2-02]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements:
  - PH36.2-CACHE
must_haves:
  truths:
    - "D-02/D-03/D-04: Cached-only Roto frames display as immediate repaintable references and painting replaces, not composites over, old cache on next save."
    - "D-05/D-06: Dirty Roto frames flush once on frame leave and never auto-save repeatedly while brush movement stays on the same frame."
    - "D-07/D-08: Dirty current frame attempts one final close flush and navigation is guarded while a Roto flush is in flight."
    - "D-10: Save pending/current flushes only dirty unsaved Roto frames."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Dirty Roto tracking, cached reference preview, save-on-leave, save-on-close, guarded navigation, Save pending wiring"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Source/contract coverage for Roto autosave and cached reference behavior"
  key_links:
    - from: "PhysicsPaintStudio.tsx"
      to: "physicPaintBridge.ts"
      via: "sendPhysicPaintApplyPayload with kind apply-canvas"
      pattern: "kind: 'apply-canvas'"
  prohibitions:
    - statement: "No autosave during active brush movement."
      status: "resolved"
      verification: "Dirty mark and flush boundaries are separated; tests assert no repeated apply while current frame is unchanged."
---

<objective>
Make the standalone Roto editor cache-first during navigation and lifecycle events per D-02 through D-10.

Purpose: Users should paint normally, navigate without losing output, reopen cached-only frames as visual references, and avoid expensive or repeated rendering while drawing.
Output: Studio-level dirty tracking, cached reference display, save-on-leave/save-on-close, and Save pending/current callback wiring.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
@SPECS/phase-36.2-physics-paint-roto-cache.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/lib/physicPaintBridge.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dirty tracking and guarded save-on-leave</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
  </read_first>
  <behavior>
    - Painting or Roto key utility mutation adds current frame to `dirtyRotoFramesRef`.
    - `navigateToSyncedFrame(nextFrame)` flushes previous dirty frame once before switching.
    - A `rotoFlushInFlightRef` or equivalent prevents a second navigation while the first dirty-frame apply is pending.
    - Staying on the same frame while drawing does not call `sendPhysicPaintApplyPayload` repeatedly.
  </behavior>
  <action>Refactor `saveRotoFrame` into a concrete helper such as `flushRotoFrame(frame, options)` that can flush a specified dirty frame through the existing `apply-canvas` payload. Add `dirtyRotoFramesRef`, `rotoFlushInFlightRef`, and state for pending frames shown in the workflow strip. Wire paint/edit completion handlers to mark dirty without sending apply. Update `navigateToSyncedFrame` so it snapshots the current frame, checks D-05 dirty state, sets an applying/saving message, awaits/queues the one apply, then loads the next frame only after the guard is clear enough to avoid race conditions. Preserve existing operationId matching.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintStudio.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Test/source contract finds `dirtyRotoFramesRef` and `rotoFlushInFlightRef` or equivalently named refs in `PhysicsPaintStudio.tsx`.
    - Test/source contract finds `navigateToSyncedFrame` awaits or calls a dirty Roto flush before `sendPhysicPaintFrameSyncMessage` for changed dirty frames.
    - Test/source contract finds no path that invokes apply payload from continuous pointer movement without frame leave, close, or Save pending/current.
  </acceptance_criteria>
  <done>Dirty Roto frames are saved once at navigation boundaries with race guard protection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cached-only reference preview and Save pending/close flush</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/types/physicPaint.ts
    - app/src/stores/physicPaintStore.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
  </read_first>
  <behavior>
    - Navigating/opening a cached Roto frame with no session editable state displays the cached PNG immediately as reference/background.
    - If the user paints on a cached-only reference, the next flush replaces the cached frame instead of compositing with it.
    - Save pending/current flushes only current dirty state or the dirty set, disabled through strip props when empty.
    - Close/unload path attempts a final current-frame dirty flush with best-effort behavior in Tauri/browser fallback.
  </behavior>
  <action>Add a Roto cached reference lookup using `launchContext.cachedRotoFrames`, `rotoPreviewFramesRef`, and `physicPaintStore.getFrame(layerId, frame)` fallback, modeled on `findCachedPlayPreviewFrame` but scoped to Roto. Use a separate preview overlay or engine background path only if it is excluded from `exportCompositeCanvas()` before save; if using engine background, clear/reset it before `buildRotoOutputFrame` so D-04 replacement does not composite over old cache. Wire new strip props from Plan 02: `cachedRotoFrames`, `editableRotoFrames`, `pendingRotoFrames`, `rotoSaveInFlight`, and `onSavePendingRotoFrames`. Add a `beforeunload`/close lifecycle best-effort final flush for D-07 without starting the dev server.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Source contract finds Roto cached reference lookup using `cachedRotoFrames` and `physicPaintStore.getFrame` fallback.
    - Source contract finds Save pending/current callback passed to `PhysicsPaintWorkflowStrip` and pending frames derived from dirty Roto state.
    - Source contract finds a close/unload handler that calls or schedules the same dirty-frame flush helper for D-07.
    - Source contract ensures cached reference preview is cleared/reset before exported replacement frame generation, or uses an overlay class that cannot be exported by `engine.exportCompositeCanvas()`.
  </acceptance_criteria>
  <done>Cached-only Roto frames are fast visual references, and dirty output flushes on explicit/manual/lifecycle boundaries only.</done>
</task>

</tasks>

## Artifacts this phase produces

- `dirtyRotoFramesRef`
- `rotoFlushInFlightRef`
- `flushRotoFrame(frame, options)` or equivalent helper
- Roto cached reference lookup helper
- Roto close/unload flush handler
- Save pending/current workflow strip wiring from Studio

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> engine state | Pointer/keyboard edits mark dirty local Roto state. |
| Standalone window -> editor bridge | Apply payload crosses from standalone to editor store. |
| Cached PNG -> editable canvas reference | Existing cache is displayed while new editable state may be clean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-07 | Tampering | `sendPhysicPaintApplyPayload` | mitigate | Keep `operationId` matching and existing payload validation; send only `apply-canvas` rendered PNG plus current editable state. |
| T-36.2-08 | Denial of Service | Dirty Roto flushing | mitigate | Flush only on frame leave, close, and Save pending/current; no repeated apply during active brush movement. |
| T-36.2-09 | Tampering | cached reference export | mitigate | Ensure cached reference overlay/background is excluded or cleared before replacement export so old cache is not composited by default. |
| T-36.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit states no install. |
</threat_model>

<verification>
Focused Studio/Strip tests after each task; full wave gate with `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- --run` and `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
A user can paint frame A, navigate to B, get exactly one save for A, return to cached-only A as a reference if editable state is absent, repaint it cleanly, and close with dirty output flushed best-effort.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-03-SUMMARY.md` when done.
</output>
