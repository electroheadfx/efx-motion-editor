---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 12
type: execute
wave: 6
depends_on: [36.2-08, 36.2-10]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
gap_closure: true
requirements: [PH36.2-PLAYBACK, PH36.2-MISSING, PH36.2-INTERP]
user_setup: []
must_haves:
  truths:
    - "Roto cached playback enumerates every app frame in the intended playback range, including missing in-between gaps."
    - "Transparent missing frames return missing/null playback output while active background metadata returns transient background-only playback frames without creating green cache cells."
    - "Generated interpolation PNG creation uses real Canvas 2D image generation for blend, position, and deform semantics rather than metadata-only dataUrl mutation or no-op arithmetic."
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Real generated PNG interpolation rendering for blend/duplicate motion options"
      contains: "renderBlendedRotoInterpolationFrame"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Range-enumerated cached Roto playback gap synthesis"
      contains: "getRotoCachedPlaybackFrames"
    - path: "app/src/stores/physicPaintStore.test.ts"
      provides: "Behavior tests for interpolation output semantics"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Behavior/source tests for missing gap frame enumeration"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/stores/physicPaintStore.ts"
      via: "cached playback reads cached/generated frames and background metadata while leaving store unchanged for transient gaps"
      pattern: "getRotoCachedPlaybackFrames|resolve.*Background"
    - from: "app/src/stores/physicPaintStore.ts"
      to: "app/src/lib/previewRenderer.ts"
      via: "generated interpolation frames remain stored in existing rendered-frame cache consumed by preview/export"
      pattern: "replaceGeneratedRotoCache"
---

<objective>
Close verification gaps 2 and 3 by replacing interpolation stubs with real generated PNG behavior and making cached Roto playback synthesize missing in-between gap frames across a defined range.

Purpose: The phase goal requires cached playback/export and render-only interpolation to work at runtime, not only through visible controls or source-string assertions.
Output: Canvas-backed interpolation generation, range-enumerated cached playback, and behavioral tests that prove generated output/gap semantics.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-VERIFICATION.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-UAT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-08-SUMMARY.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-10-SUMMARY.md
@app/src/stores/physicPaintStore.ts
@app/src/stores/physicPaintStore.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/types/physicPaint.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Canvas-backed Roto interpolation PNG generation</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts
    - app/src/stores/physicPaintStore.test.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-VERIFICATION.md
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-06-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Blend mode generates a PNG data URL from a canvas draw path using complementary alpha for first and second real key images; it does not append blend metadata onto an existing data URL.
    - Test 2: Duplicate mode generates a PNG data URL from a canvas draw path using the source key image plus deterministic transform settings.
    - Test 3: `position` changes generated output semantics by translating the source draw placement or sampled pixels.
    - Test 4: `deform` changes generated output semantics through a deterministic canvas transform or pixel displacement; it is not consumed by no-op arithmetic.
  </behavior>
  <action>Replace the diagnosed interpolation stubs in `physicPaintStore.ts`. `renderBlendedRotoInterpolationFrame` must load/draw both real-key PNG images into an offscreen canvas or equivalent testable canvas factory, draw the first with alpha `1 - t`, draw the second with alpha `t`, apply `settings.position` as deterministic per-in-between translation, apply `settings.deform` as deterministic scale/skew/pixel-displacement behavior, and return a freshly encoded `data:image/png` URL for the generated frame. `renderDuplicateRotoInterpolationFrame` must draw the source key image into a canvas and apply the same position/deform transform pipeline before encoding. If tests run under a mocked canvas/Image environment, add injectable/test-local helpers inside the existing store test pattern; do not add packages or a one-off test config. Keep generated frames in `replaceGeneratedRotoCache` with `source: 'generated-interpolation'`, nearest-real-key metadata, and existing `physicPaintVersion` invalidation per D-25 through D-32.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if `renderBlendedRotoInterpolationFrame` creates output by string-concatenating metadata onto an input `dataUrl`.
    - Tests prove blend mode draws both first and second key sources with complementary alpha values.
    - Tests prove duplicate mode encodes a generated PNG through a canvas output path.
    - Tests prove non-zero `position` and non-zero `deform` alter draw instructions or generated output compared with zero settings.
    - No new dependency is added to package manifests.
  </acceptance_criteria>
  <done>Generated Roto interpolation frames are real render-only PNGs whose blend/position/deform settings affect generated output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enumerate cached Roto playback range and synthesize actual gaps</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/stores/physicPaintStore.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-VERIFICATION.md
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-10-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Cached Roto playback with cached frames at 0 and 2 enumerates frame 1 even though frame 1 is absent from known cached/generated sets.
    - Test 2: Missing frame 1 returns null/transparent playback output when Roto background metadata is transparent.
    - Test 3: Missing frame 1 returns a transient background-only playback frame when active background metadata exists.
    - Test 4: Transient missing playback frames do not call store cache mutation helpers or add green cached-cell metadata.
  </behavior>
  <action>Define the Roto cached playback range inside `PhysicsPaintStudio.tsx`: prefer the launch/session frame range if available (`launchContext.startFrame` plus `frameCount` or current workflow frame count), otherwise use min-to-max across real cached/generated Roto frames with at least the current visible strip span. Update `getRotoCachedPlaybackFrames()` so it enumerates every integer app frame in that range, not only frames found in known cached sets. For each enumerated frame: return cached/generated PNG frame when present; return null/missing for transparent gaps per D-18; return a transient background-only frame object when active background metadata exists per D-19 and Plan 10; never write transient frames to `physicPaintStore`, `savedRotoFrames`, or Roto cache metadata per D-20. Keep the existing Play/Stop UI and stop conditions from Plans 05 and 10.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests/source contract shows `getRotoCachedPlaybackFrames()` or its helper constructs an `Array.from`/loop over a min..max or start..end frame range.
    - A test fixture with cached frames 0 and 2 asserts frame 1 is represented as a missing transparent or transient background-only entry.
    - A test asserts no call to `physicPaintStore.setFrame`, `upsertRealRotoKeyFrame`, or `replaceGeneratedRotoCache` occurs for transient playback gaps.
    - Playback status still reports missing frames play transparent/background.
  </acceptance_criteria>
  <done>Cached Roto playback now covers real missing gaps across the playback range and preserves cache-free virtual gap behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run integrated interpolation and playback regression gate</name>
  <files>app/src/stores/physicPaintStore.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/lib/exportRenderer.test.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Existing visible interpolation controls still pass after real output generation changes.
    - Test 2: Existing missing background preview/export tests still pass after playback range enumeration changes.
    - Test 3: Typecheck passes without schema or package changes.
  </behavior>
  <action>After Tasks 1 and 2 pass individually, run the focused Phase 36.2 regression set that covers types, store, Studio, WorkflowStrip, and export renderer behavior. Fix only regressions caused by this plan. Do not run the dev server. Do not change test config. Keep all changes inside the files listed by this plan unless a failing type import requires a narrow update to `app/src/types/physicPaint.ts` already referenced in read_first.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/lib/exportRenderer.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Focused Vitest command passes.
    - Typecheck passes.
    - Source grep for metadata-only blend data URL mutation finds no active production-code pattern in `physicPaintStore.ts`.
    - Source grep for no-op position/deform arithmetic in interpolation rendering finds no active production-code pattern in `physicPaintStore.ts`.
  </acceptance_criteria>
  <done>Verification gaps 2 and 3 have automated behavioral coverage beyond source-string consumption.</done>
</task>

</tasks>

## Artifacts this phase produces

- Canvas-backed `renderBlendedRotoInterpolationFrame(...)` behavior using first/second key images, complementary alpha, position, and deform
- Canvas-backed `renderDuplicateRotoInterpolationFrame(...)` behavior using source key image, position, and deform
- Testable Roto playback range enumeration helper or logic in `getRotoCachedPlaybackFrames()`
- Transient missing-gap playback frame handling for active background metadata without store mutation
- Behavioral tests for generated PNG output and true missing in-between playback gaps

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 36.2 | Cached playback/export and render-only interpolation/key utilities | 36.2-12 | COVERED | Fixes runtime semantics after visible UI plans.
REQ | PH36.2-PLAYBACK | Cached playback across missing gaps | 36.2-12 | COVERED | Enumerates full range and synthesizes missing gaps.
REQ | PH36.2-MISSING | Transparent/background missing-frame behavior | 36.2-12 | COVERED | Uses Plan 10 metadata without green cells.
REQ | PH36.2-INTERP | Blend/position/deform generated PNG behavior | 36.2-12 | COVERED | Replaces metadata/no-op stubs with Canvas output.
RESEARCH | Pitfall: generated frames render-only | Generated cache stays in rendered-frame path | 36.2-12 | COVERED | No editable generated frames or replay path.
CONTEXT | D-18..D-20 | Missing frames transparent/background only without cache mutation | 36.2-12 | COVERED | Playback gap logic cites and implements these decisions.
CONTEXT | D-24..D-32 | Duplicate/blend interpolation with position/deform and cached PNG output | 36.2-12 | COVERED | Canvas-backed implementation preserves render-only semantics.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cached PNG data URL -> Canvas/Image decode | Real-key cached images are decoded and redrawn to generate interpolation PNGs. |
| Interpolation settings -> generated cache | User-controlled settings affect generated rendered output. |
| Playback synthesis -> cache store | Transient missing-gap frames must not mutate durable cache state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-12-01 | Tampering | interpolation image generation | mitigate | Decode/draw only validated `data:image/png` frames from existing store/type validators; keep generated source metadata render-only. |
| T-36.2-12-02 | Denial of Service | playback range enumeration | mitigate | Use bounded launch/session frame ranges or min/max cached-frame span; do not generate durable frames during playback. |
| T-36.2-12-03 | Integrity | transient background playback | mitigate | Tests assert transient gap playback does not call store mutation methods or create green cached cells. |
| T-36.2-12-SC | Tampering | package installs | accept | No package installs are planned. |
</threat_model>

<verification>
<automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/lib/exportRenderer.test.ts</automated>
<automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
</verification>

<success_criteria>
- Blend, duplicate, position, and deform interpolation settings produce real generated PNG output through a canvas path.
- Cached Roto playback enumerates and handles missing in-between gaps across the playback range.
- Missing transparent/background behavior remains virtual and never creates green cached cells.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-12-SUMMARY.md` when done.
</output>
