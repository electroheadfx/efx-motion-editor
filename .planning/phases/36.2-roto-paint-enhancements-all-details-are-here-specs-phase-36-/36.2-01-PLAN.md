---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
autonomous: true
requirements:
  - PH36.2-CACHE
  - PH36.2-MISSING
  - PH36.2-INTERP
must_haves:
  truths:
    - "D-01: Cached PNG frames in physicPaintStore._frames are the durable Roto playback/export truth."
    - "D-17: Removing an empty/deleted real Roto key removes its cached output instead of leaving a green cached cell."
    - "D-27/D-29: Generated interpolation frames can exist as render-only cached PNG frames but are distinguishable from editable real Roto keys."
    - "D-31: Changing global interpolation settings has a store-level path to replace generated in-between cache."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Roto cache metadata, real/generated frame source contracts, interpolation settings validation, cached Roto launch summaries"
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Store helpers for Roto real-key metadata, generated cache replacement, frame removal, and version invalidation"
  key_links:
    - from: "app/src/types/physicPaint.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "validated Roto metadata consumed by store helpers"
      pattern: "normalizePhysicPaintRoto|PhysicPaintRoto"
  prohibitions:
    - statement: "Do not add guaranteed durable per-frame editable Roto stroke persistence."
      status: "resolved"
      verification: "No new per-frame editable-state persistence map or MCE field is introduced; only rendered cache and metadata are durable per D-01."
    - statement: "Do not introduce a parallel PNG cache outside physicPaintStore._frames."
      status: "resolved"
      verification: "Generated and real Roto output use existing frame storage paths."
---

<objective>
Define the typed and store-level cache-first Roto foundation for Phase 36.2 decisions D-01, D-17 through D-21, and D-22 through D-33.

Purpose: Later UI, bridge, navigation, and interpolation work needs a tested source of truth that distinguishes real editable-session Roto keys from durable cached-only frames and generated render-only interpolation frames.
Output: Validated TypeScript contracts and store helpers that mutate non-reactive maps only through invalidating paths.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
@SPECS/phase-36.2-physics-paint-roto-cache.md
@app/src/types/physicPaint.ts
@app/src/types/physicPaint.test.ts
@app/src/stores/physicPaintStore.ts
@app/src/stores/physicPaintStore.test.ts
@.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validated Roto cache and interpolation contracts</name>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts
    - app/src/types/physicPaint.test.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
    - SPECS/phase-36.2-physics-paint-roto-cache.md
  </read_first>
  <behavior>
    - A launch context may include a cached Roto frame summary list whose entries validate `appFrame`, PNG `dataUrl`, and source metadata.
    - Roto frame source metadata accepts real key frames and generated interpolation frames as separate values.
    - Interpolation settings validate global `enabled`, `inBetweenFrameCount`, `mode`, `deform`, and `position` fields, and reject invalid frame counts above `PHYSIC_PAINT_MAX_APPLY_FRAMES`.
    - Validators continue rejecting forbidden apply fields named `engine` or `internals`.
  </behavior>
  <action>Add minimal exported contracts in `app/src/types/physicPaint.ts`: `PhysicPaintRotoFrameSource = 'real-key' | 'generated-interpolation'`, `PhysicPaintRotoCacheFrame` extending the rendered PNG shape with `source`, `nearestRealKeyFrame?`, and `backgroundOnly?`, and `PhysicPaintRotoInterpolationSettings` with global `enabled`, integer `inBetweenFrameCount`, `mode: 'duplicate' | 'blend'`, `deform`, and `position`. Extend `PhysicPaintLaunchContext` with optional `cachedRotoFrames?: PhysicPaintRotoCacheFrame[]` and optional `rotoInterpolationSettings?: PhysicPaintRotoInterpolationSettings`. Add validators/normalizers following existing Play range patterns. Preserve D-01 by not adding durable per-frame editable stroke persistence fields.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/types/physicPaint.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `isPhysicPaintLaunchContext({ operationId:'op', layerId:'layer', startFrame:0, cachedRotoFrames:[{ frameIndex:0, appFrame:0, dataUrl:'data:image/png;base64,a', source:'real-key' }] })` returns true in a test.
    - A cached Roto frame with `source:'generated-interpolation'` and `nearestRealKeyFrame:0` validates; one with non-PNG `dataUrl` rejects.
    - Interpolation settings with `mode:'blend'` and `inBetweenFrameCount:2` validate; non-integer and out-of-range counts reject.
    - No new exported type stores per-frame `SerializedProject` for durable Roto frames.
  </acceptance_criteria>
  <done>Roto cache and interpolation metadata are test-covered, fail-closed, and compatible with existing launch/apply validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add invalidating store helpers for real Roto keys and generated cache</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts
    - app/src/stores/physicPaintStore.test.ts
    - app/src/types/physicPaint.ts
    - app/src/types/physicPaint.test.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
  </read_first>
  <behavior>
    - Real Roto key frames are tracked separately from generated interpolation frames.
    - Generated interpolation cache writes update existing rendered frame storage and mark generated frames as render-only metadata.
    - Removing a real Roto key removes that frame's cached PNG and invalidates serialization/version state.
    - Replacing generated cache for a layer removes old generated frames before adding new generated frames.
  </behavior>
  <action>In `physicPaintStore.ts`, add small private maps for Roto frame metadata and interpolation settings keyed by layer ID, not editable engine state. Add public methods named concretely: `getRotoCacheFrames(layerId)`, `getRealRotoKeyFrames(layerId)`, `upsertRealRotoKeyFrame(layerId, frame, renderedFrame)`, `removeRealRotoKeyFrame(layerId, frame)`, `replaceGeneratedRotoCache(layerId, frames, settings)`, `getRotoInterpolationSettings(layerId)`, and `setRotoInterpolationSettings(layerId, settings)`. All mutations must call `_notifyVisualChange()` when they change frames/metadata. Extend `toMceOutputs()` / `loadFromMceOutputs()` only for metadata needed to distinguish real keys and generated interpolation cache; keep PNG data in existing `frames` output. Do not create a second rendered-frame store.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/stores/physicPaintStore.test.ts src/types/physicPaint.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Calling `upsertRealRotoKeyFrame('layer', 2, frame)` makes `getFrame('layer', 2)` non-null and includes frame 2 in `getRealRotoKeyFrames('layer')`.
    - Calling `removeRealRotoKeyFrame('layer', 2)` removes `getFrame('layer', 2)` and increments `physicPaintVersion.value` once for the changed removal.
    - Calling `replaceGeneratedRotoCache('layer', generatedFrames, settings)` stores PNG frames in existing `getFrame()` lookup and returns them from `getRotoCacheFrames()` with `source:'generated-interpolation'`.
    - MCE round-trip preserves real/generated metadata but does not serialize per-frame editable stroke state.
  </acceptance_criteria>
  <done>Store helpers make real/generated Roto cache state explicit, durable enough for playback/export, and reactive through `physicPaintVersion`.</done>
</task>

</tasks>

## Artifacts this phase produces

- `PhysicPaintRotoFrameSource`
- `PhysicPaintRotoCacheFrame`
- `PhysicPaintRotoInterpolationSettings`
- `cachedRotoFrames` launch-context field
- `rotoInterpolationSettings` launch-context field
- `getRotoCacheFrames(layerId)`
- `getRealRotoKeyFrames(layerId)`
- `upsertRealRotoKeyFrame(layerId, frame, renderedFrame)`
- `removeRealRotoKeyFrame(layerId, frame)`
- `replaceGeneratedRotoCache(layerId, frames, settings)`
- `getRotoInterpolationSettings(layerId)`
- `setRotoInterpolationSettings(layerId, settings)`

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase goal | Cache-first Roto workflow | 01-06 | COVERED | Plans deliver cache cells, cached preview, save-on-leave, playback/export, and tail interpolation/utilities.
REQ | PH36.2-CACHE | Cache-first Roto cells, cached preview, save-on-leave | 01-04 | COVERED | Introduced as phase-local requirement because ROADMAP was TBD.
REQ | PH36.2-MISSING | Missing/deleted/background frame semantics | 01,04 | COVERED | Empty/deleted removes cache; playback/export handles missing virtually.
REQ | PH36.2-PLAYBACK | Cached Roto playback/export without replay | 04 | COVERED | Preview/export keep cache path.
REQ | PH36.2-INTERP | Tail interpolation and key utilities | 01,06 | COVERED | Final plan after cache foundation.
CONTEXT | D-01..D-04 | Cache-first editable model | 01,03 | COVERED | Durable PNG truth; cached-only repaintable reference.
CONTEXT | D-05..D-10 | Auto-save and Save pending rules | 03 | COVERED | Save-on-leave/close; no default Save all.
CONTEXT | D-11..D-16 | Gray/green/pink cell semantics | 02 | COVERED | Exact fills and outline-only current state.
CONTEXT | D-17..D-21 | Missing/empty/background rules | 01,04 | COVERED | Store removal and virtual playback/export behavior.
CONTEXT | D-22..D-33 | Interpolation tail plan | 01,06 | COVERED | Metadata in 01; UI/generation/open behavior in 06.
CONTEXT | D-34..D-41 | Key edit utilities tail plan | 06 | COVERED | Duplicate/insert/delete/copy/paste over real keys only.
RESEARCH | rendered cache source | Reuse `apply-canvas` and `_frames` | 01,03,04,06 | COVERED | No new cache subsystem.
RESEARCH | no new packages | No installs | all | COVERED | No package manager install tasks.
RESEARCH | validation architecture | Existing pnpm/Vitest commands | all | COVERED | Each task has app-relative focused tests.
SPEC | save-on-leave/cached cells/cached play | Core spec behaviors with context overrides | 02-04 | COVERED | Save all/yellow dirty superseded by D-09/D-15.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serialized project -> store hydration | Project file metadata and data URLs become local cache state. |
| Store helpers -> preview/export | Non-reactive maps feed renderer output and serialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-01 | Tampering | `isPhysicPaintLaunchContext` / Roto metadata validators | mitigate | Reject malformed Roto cached-frame summaries, invalid PNG data URLs, invalid source values, and invalid interpolation settings before store use. |
| T-36.2-02 | Denial of Service | `replaceGeneratedRotoCache` | mitigate | Clamp interpolation frame counts with `PHYSIC_PAINT_MAX_APPLY_FRAMES` and reject non-finite/non-integer counts. |
| T-36.2-03 | Tampering | `physicPaintStore` metadata maps | mitigate | Route all mutations through methods that call `_notifyVisualChange()` and preserve MCE serialization invalidation. |
| T-36.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit states no install. |
</threat_model>

<verification>
Run focused tests after each task and the full app test/typecheck after the wave: `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- --run` and `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
Types and store tests prove real Roto keys, cached-only frames, generated interpolation frames, and missing/deleted cache removals are distinct without durable per-frame editable stroke persistence.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-01-SUMMARY.md` when done.
</output>
