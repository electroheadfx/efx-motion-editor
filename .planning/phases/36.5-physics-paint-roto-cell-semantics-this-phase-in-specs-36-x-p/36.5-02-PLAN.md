---
phase: 36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p
plan: 02
type: tdd
wave: 2
depends_on:
  - 36.5-01
files_modified:
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
requirements:
  - 36.5-STATE-01
  - 36.5-STATE-02
  - 36.5-STATE-03
  - 36.5-STATE-04
  - 36.5-STATE-05
  - 36.5-STATE-06
  - 36.5-SCOPE-01
must_haves:
  truths:
    - "D-02: The Roto strip renders current, empty/missing, cached/saved, dirty/unsaved, generated/render-only, and background-only semantics from the Plan 01 view model."
    - "D-03: Roto frame cells remain spreadsheet-like square cells in the bottom workflow strip using the referenced timeline UI structure."
    - "D-04: The selected/current frame has a visible selected state while preserving the base cell meaning."
    - "Dirty/pending work is visible without hiding the base cell meaning."
    - "Generated/render-only cells are distinguishable and do not silently become editable targets."
    - "Source contracts prove key utilities, copy/paste, interpolation controls, cached playback controls, save-on-leave behavior, Phase 36.4 close behavior, and full timeline imports are not introduced."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Roto view-model rendering, legend, status copy, aria labels, render-only generated handling"
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts"
      provides: "Source-contract tests for UI semantics and scope exclusions"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/physicsPaintWorkflowState.ts"
      via: "imports and maps `getRotoCellViewModel` over `frameCells`"
      pattern: "getRotoCellViewModel"
    - from: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/physicsPaintStudio.css"
      via: "renders semantic class names consumed by Plan 03 CSS"
      pattern: "roto-fill-(empty|cached|editable-current|generated|background-only)"
  prohibitions:
    - statement: "D-01: Phase 36.5 is not Phase 36.4 close behavior."
      status: resolved
      verification: "Source-contract tests keep close prompts and close-request wiring out of the Roto semantic strip."
    - statement: "D-05: Do not include key utilities, copy/paste, interpolation controls, cached playback, save-on-leave, or final full timeline redesign."
      status: resolved
      verification: "Source-contract tests assert these controls/imports do not appear in the Roto controls block."
---

<objective>
Wire the tested semantic view model into the Roto timeline strip and lock the scope boundary with source-contract tests.

Purpose: D-02, D-03, and D-04 require visible trustworthy semantics in the actual bottom strip, while D-01 and D-05 require strict exclusion of close behavior and timeline tool creep.
Output: `PhysicsPaintWorkflowStrip.tsx` renders semantic cell classes, labels, legend, status copy, and generated/background-only handling from the Plan 01 helper.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-CONTEXT.md
@.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-RESEARCH.md
@.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-VALIDATION.md
@.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-PATTERNS.md
@.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-UI-SPEC.md
@SPECS/36.x-phases/phase-36.5-cell-semantics/spec-36.5-cell-semantics.md
@SPECS/36.x-phases/timeline-ui/TimelineBottomWindow.tsx
@SPECS/36.x-phases/timeline-ui/timeline-bottom-window.html
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Trustworthy visible Roto timeline cell semantics | Covered by Task 1 and Task 2 |
| REQ | 36.5-STATE-01 through 36.5-STATE-06 | Covered by source-contract tests and component wiring |
| REQ | 36.5-SCOPE-01 | Covered by strict exclusion tests |
| RESEARCH | Source-contract tests for UI exclusions | Covered by Task 1 and Task 3 |
| UI-SPEC | Legend/status copy and non-color semantic classes | Covered by Task 1 and Task 2 |
| CONTEXT | D-01 through D-05 | Covered by objective, tasks, and prohibitions |
</source_audit>

<feature>
  <name>Roto semantic strip rendering</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <behavior>
    - Source contains `getRotoCellViewModel` and maps it over `frameCells` for Roto buttons.
    - Roto button classes include `vm.fillClass`, overlay classes `current`, `dirty`, and `pending`, and preserve `physics-paint-roto-cell`.
    - Roto buttons use `aria-label={vm.ariaLabel}` and `title={vm.title}` from the view model.
    - The strip renders `physics-paint-roto-cell-legend` with labels `Empty`, `Cached`, `Current`, `Generated`, `Background only`, `Unsaved`, and `Saving`.
    - The status stack renders `Frame {frame}: {state}`-style copy from the current frame view model and special generated/background/dirty/pending summaries.
  </behavior>
  <implementation>After RED source-contract tests fail, replace direct `getRotoCellFill` calls in the component with Plan 01 view-model calls. Keep the existing bottom timeline strip and Roto row; do not import or clone the main editor Timeline. Generated cache entries may appear as render-only semantic cells if metadata is present; do not add interpolation controls or editing utilities.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED source-contract tests for strip semantics and scope guards</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - app/src/components/physic-paint/physicsPaintStudio.css
    - .planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-UI-SPEC.md
    - SPECS/36.x-phases/timeline-ui/TimelineBottomWindow.tsx
  </read_first>
  <behavior>
    - Add failing source-contract tests expecting `getRotoCellViewModel`, `physics-paint-roto-cell-legend`, and the seven visible labels from the UI-SPEC.
    - Add failing tests expecting class strings `roto-fill-cached`, `roto-fill-editable-current`, `roto-fill-generated`, `roto-fill-background-only`, `dirty`, and `pending` in component/CSS source.
    - Add strict exclusion tests for D-01 and D-05 using the existing `rotoControlsBlock` pattern.
  </behavior>
  <action>Create RED tests in `PhysicsPaintWorkflowStrip.test.ts` for the component contract. Use region-scoped checks where possible: slice from `props.mode === 'roto'` to `physics-paint-play-controls` for Roto-only controls, and slice the Roto cell map block for semantic button assertions. Keep negative checks focused on controls/imports, not comments. Assert no main timeline import with the existing `/from ['"].*Timeline/` pattern. Assert the Roto controls block does not contain close-choice prompts, key utility button labels, copy/paste labels, interpolation control markup, cached playback transport, save-on-leave copy, or a full timeline import.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts -t "Roto cell"</automated>
  </verify>
  <acceptance_criteria>
    - The targeted command fails before component implementation because semantic legend/view-model strings are missing.
    - Tests cite D-01, D-02, D-03, D-04, and D-05 in test names or comments for decision traceability.
    - Negative tests do not rely on file-wide unfiltered `grep -c` gates.
  </acceptance_criteria>
  <done>RED source-contract tests define the component and scope boundaries before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN component wiring for semantic cells, legend, and status</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - .planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-PATTERNS.md
    - .planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-UI-SPEC.md
  </read_first>
  <behavior>
    - Roto frame buttons render from `getRotoCellViewModel`.
    - Visible legend includes every base meaning and dirty/pending overlay label.
    - Current frame status uses the same view model as the button classes and labels.
  </behavior>
  <action>Update `PhysicsPaintWorkflowStrip.tsx` to import `getRotoCellViewModel` and use it for Roto `frameCells.map`. Pass the unfiltered `props.cachedRotoFrames` so generated and background-only metadata can be represented when present, while preserving the existing `realRotoFrames` calculation for interpolation connectors. Replace `currentRotoFill` with `currentRotoCell`. Add a compact legend container `physics-paint-roto-cell-legend` in the Roto status area with swatches using the same fill and overlay class names. Add current-frame status text from `currentRotoCell.label` and keep existing `playPublicationSummary` and `statusMessage` rendering. For generated cells, keep click behavior routed through existing `onNavigateToSyncedFrame(frame)` only; do not add new editing controls, redirect logic, or interpolation UI. Do not add `dangerouslySetInnerHTML`.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `PhysicsPaintWorkflowStrip.test.ts` passes.
    - `PhysicsPaintWorkflowStrip.tsx` contains `getRotoCellViewModel` and `physics-paint-roto-cell-legend`.
    - Visible copy contains `Roto cell states`, `Empty`, `Cached`, `Current`, `Generated`, `Background only`, `Unsaved`, and `Saving`.
    - The Roto map block uses view-model `ariaLabel`, `title`, and `fillClass` fields.
  </acceptance_criteria>
  <done>Component renders semantic Roto cells, legend, and status copy from the pure view model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR strict exclusion contract</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - .planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-CONTEXT.md
    - SPECS/36.x-phases/phase-36.5-cell-semantics/spec-36.5-cell-semantics.md
  </read_first>
  <action>Refactor tests to keep a single Phase 36.5 strict-scope case that asserts the Roto controls/status block excludes Phase 36.4 close choices, key utilities, copy/paste, interpolation controls, cached playback controls, save-on-leave language, and main timeline imports. If existing older tests name Phase 36.3, do not delete useful coverage; adjust only comments/test titles that now describe Phase 36.5 semantics. Keep implementation unchanged unless a refactor is needed for test readability.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - One source-contract test explicitly references `36.5-SCOPE-01`.
    - Source-contract tests include D-01 and D-05 traceability.
    - No new props or callbacks are added for excluded controls.
  </acceptance_criteria>
  <done>Scope guard tests are concise, current, and green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `physics-paint-roto-cell-legend` markup in `PhysicsPaintWorkflowStrip.tsx`
- `physics-paint-roto-cell-legend-item` markup in `PhysicsPaintWorkflowStrip.tsx`
- `physics-paint-roto-cell-swatch` markup in `PhysicsPaintWorkflowStrip.tsx`
- Roto button use of `getRotoCellViewModel` in `PhysicsPaintWorkflowStrip.tsx`
- Source-contract test for semantic legend labels and view-model use
- Source-contract test for Phase 36.5 strict exclusions
- Rendered visible labels `Roto cell states`, `Empty`, `Cached`, `Current`, `Generated`, `Background only`, `Unsaved`, `Saving`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper view model→Preact DOM | Typed semantic data becomes classes, labels, titles, and visible text. |
| user click→frame navigation | User clicks a cell whose visual state may be generated/render-only or background-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.5-05 | Spoofing | `PhysicsPaintWorkflowStrip.tsx` Roto cells | mitigate | Use one helper output for class, title, aria label, and status so visuals and copy cannot drift. |
| T-36.5-06 | Tampering | generated/render-only cell clicks | mitigate | Do not add editing utilities; keep generated cells visibly render-only and source-contract tested. |
| T-36.5-07 | Repudiation | dirty/saving status | mitigate | Render `Unsaved`/`Saving` legend and current-frame status from view-model overlays. |
| T-36.5-08 | Tampering | Preact text rendering | mitigate | Use text interpolation for frame labels; do not add HTML injection APIs. |
| T-36.5-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks exist in this plan. |
</threat_model>

<verification>
Run component contract suite: `cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts`.
</verification>

<success_criteria>
- RED source-contract tests were written first and failed.
- Component tests pass after implementation.
- D-01 through D-05 are cited and enforced.
- Roto strip renders semantic cells without adding excluded controls.
</success_criteria>

<output>
Create `.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-02-SUMMARY.md` when done.
</output>
