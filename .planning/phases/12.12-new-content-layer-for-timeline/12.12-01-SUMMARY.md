---
phase: 12.12-new-content-layer-for-timeline
plan: 01
subsystem: ui
tags: [preact-signals, content-overlay, timeline, sequence, keyframes]

# Dependency graph
requires:
  - phase: 12-timeline-keyframes
    provides: "FX sequence infrastructure (createFxSequence, fxTrackLayouts, keyframeStore)"
provides:
  - "Sequence.kind 'content-overlay' type discriminator"
  - "FxTrackLayout extended with thumbnailImageId and layerType"
  - "createContentOverlaySequence() store method with undo"
  - "getOverlaySequences() returning FX + content-overlay"
  - "fxTrackLayouts including content-overlay sequences with color/thumbnail"
  - "findLayerContext returning inFrame as startFrame for content-overlay"
  - "frameMap/trackLayouts correctly skip content-overlay sequences"
  - ".mce hydration supporting content-overlay kind"
affects: [12.12-02, 12.12-03, 12.12-04]

# Tech tracking
tech-stack:
  added: []
  patterns: ["content-overlay kind discriminator for timeline-level content layers"]

key-files:
  created: []
  modified:
    - "Application/src/types/sequence.ts"
    - "Application/src/types/timeline.ts"
    - "Application/src/stores/sequenceStore.ts"
    - "Application/src/stores/keyframeStore.ts"
    - "Application/src/lib/frameMap.ts"
    - "Application/src/stores/projectStore.ts"

key-decisions:
  - "content-overlay as new Sequence.kind value (not reusing 'fx')"
  - "reorderFxSequences operates on all non-content sequences for interleaved reorder"
  - "Content-overlay color: green for static-image, blue for image-sequence, purple for video (matching sidebar dots)"
  - "Content-overlay sequences use inFrame as keyframe startFrame (not trackLayouts)"

patterns-established:
  - "content-overlay kind: new sequence discriminator for timeline-level content layers that interleave with FX"
  - "getThumbnailImageId helper: extracts first image ID from content layer source for range bar icons"

requirements-completed: [CO-TYPE, CO-STORE, CO-FMAP, CO-KF]

# Metrics
duration: 3min
completed: 2026-03-19
---

# Phase 12.12 Plan 01: Foundation Summary

**Content-overlay type system, store CRUD, fxTrackLayouts pipeline, keyframe support, and .mce serialization for timeline-level content layers**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-19T11:20:24Z
- **Completed:** 2026-03-19T11:23:15Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Extended Sequence.kind with 'content-overlay' and FxTrackLayout with thumbnailImageId/layerType fields
- Added createContentOverlaySequence() with full undo support, getOverlaySequences(), and interleaved reorder
- Updated fxTrackLayouts to include content-overlay sequences with content-type-specific colors
- Updated keyframeStore.findLayerContext to return inFrame as startFrame for content-overlay layers
- Ensured frameMap and trackLayouts correctly skip content-overlay (no frame contribution)
- Updated .mce hydration to handle 'content-overlay' kind

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend type system for content-overlay sequences** - `a11b0c7` (feat)
2. **Task 2: Add store operations and data pipeline for content overlays** - `77a7dda` (feat)

## Files Created/Modified
- `Application/src/types/sequence.ts` - Added 'content-overlay' to Sequence.kind union
- `Application/src/types/timeline.ts` - Extended FxTrackLayout with content-overlay kind, thumbnailImageId, layerType
- `Application/src/stores/sequenceStore.ts` - Added createContentOverlaySequence(), getOverlaySequences(), extended reorder and remove
- `Application/src/stores/keyframeStore.ts` - Updated findLayerContext for content-overlay startFrame handling
- `Application/src/lib/frameMap.ts` - Updated all computeds for content-overlay; added getThumbnailImageId helper
- `Application/src/stores/projectStore.ts` - Updated hydration kind cast to include 'content-overlay'

## Decisions Made
- Used 'content-overlay' as a new Sequence.kind value rather than reusing 'fx' with content layer types -- cleaner discriminator
- reorderFxSequences operates on all `kind !== 'content'` sequences, enabling interleaved FX/content-overlay drag reorder
- Content-overlay range bar colors use CSS variables matching sidebar dot colors (green/blue/purple by content type)
- Content-overlay keyframes use inFrame as startFrame (global timeline position), not trackLayouts lookup

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Extended removeLayerFromSequence for content-overlay cleanup**
- **Found during:** Task 2
- **Issue:** removeLayerFromSequence only removed the entire sequence for `kind === 'fx'` when last layer deleted, but content-overlay sequences have the same behavior (single-layer sequences that should be removed entirely when empty)
- **Fix:** Extended condition to `(ownerSeq.kind === 'fx' || ownerSeq.kind === 'content-overlay')`
- **Files modified:** Application/src/stores/sequenceStore.ts
- **Verification:** TypeScript compiles cleanly
- **Committed in:** 77a7dda (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Essential for correctness -- prevents orphaned empty content-overlay sequences.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Type system and store infrastructure complete for content-overlay sequences
- Downstream plans (02: menu/import, 03: timeline rendering/interaction, 04: preview compositing) can proceed without touching types or stores
- fxTrackLayouts signal will automatically include content-overlay sequences once they are created

## Self-Check: PASSED

All 6 modified files exist. Both task commits (a11b0c7, 77a7dda) verified in git log.

---
*Phase: 12.12-new-content-layer-for-timeline*
*Completed: 2026-03-19*
