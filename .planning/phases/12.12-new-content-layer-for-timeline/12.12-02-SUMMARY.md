---
phase: 12.12-new-content-layer-for-timeline
plan: 02
subsystem: ui
tags: [preact-signals, content-overlay, menu, import-flow, sidebar, intent-dispatch]

# Dependency graph
requires:
  - phase: 12.12-new-content-layer-for-timeline
    provides: "content-overlay Sequence.kind, createContentOverlaySequence(), getOverlaySequences()"
provides:
  - "AddLayerMenu with CONTENT section (Static Image, Image Sequence, Video)"
  - "Content overlay creation flow via ImportedView intent dispatch"
  - "Change Source flow for content overlay layers via ImportedView"
  - "Content overlay layer detection in LeftPanel with SidebarProperties"
  - "Change Source button in SidebarProperties for content overlay layers"
  - "layerStore routing for content-overlay layers via overlayLayers computed"
affects: [12.12-03, 12.12-04]

# Tech tracking
tech-stack:
  added: []
  patterns: ["content-overlay intent dispatch with target field", "changeSourceFor round-trip update pattern"]

key-files:
  created: []
  modified:
    - "Application/src/components/timeline/AddFxMenu.tsx"
    - "Application/src/components/layout/TimelinePanel.tsx"
    - "Application/src/stores/uiStore.ts"
    - "Application/src/components/views/ImportedView.tsx"
    - "Application/src/components/layout/LeftPanel.tsx"
    - "Application/src/components/sidebar/SidebarProperties.tsx"
    - "Application/src/stores/layerStore.ts"

key-decisions:
  - "AddLayerIntent extended with target and changeSourceFor fields rather than separate signals"
  - "Content overlay creation reuses same ImportedView flow with target: 'content-overlay' discriminator"
  - "Change Source round-trip: SidebarProperties sets changeSourceFor in intent, ImportedView updates layer source in place"
  - "layerStore overlayLayers computed replaces fxLayers for correct routing of both FX and content-overlay layers"

patterns-established:
  - "target field in AddLayerIntent: discriminates between adding to active content sequence vs creating content-overlay sequence"
  - "changeSourceFor pattern: intent carries layerId+sequenceId for in-place source replacement via ImportedView"

requirements-completed: [CO-MENU, CO-IMPORT, CO-SIDEBAR]

# Metrics
duration: 7min
completed: 2026-03-19
---

# Phase 12.12 Plan 02: Menu, Import Flow, and Sidebar Summary

**AddLayerMenu with Content section, ImportedView content-overlay creation and Change Source flows, LeftPanel content-overlay detection, and SidebarProperties Change Source button**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-19T11:27:14Z
- **Completed:** 2026-03-19T11:34:31Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Renamed AddFxMenu to AddLayerMenu with CONTENT section (Static Image, Image Sequence, Video) above GENERATORS/ADJUSTMENTS
- Extended ImportedView to create content-overlay sequences (not layers in active sequence) when target is 'content-overlay'
- Implemented Change Source round-trip: sidebar button opens importer, selecting new media updates existing layer source
- LeftPanel detects content-overlay layers and renders SidebarProperties with full controls in LAYERS panel
- Fixed layerStore routing to use overlayLayers computed for correct update/remove of content-overlay layers

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename AddFxMenu to AddLayerMenu and add Content section** - `014889c` (feat)
2. **Task 2: Wire ImportedView for content overlay creation and sidebar properties** - `72c55be` (feat)

## Files Created/Modified
- `Application/src/components/timeline/AddFxMenu.tsx` - Renamed export to AddLayerMenu, button text to "+ Layer", added CONTENT section and handleAddContentLayer
- `Application/src/components/layout/TimelinePanel.tsx` - Updated import from AddFxMenu to AddLayerMenu
- `Application/src/stores/uiStore.ts` - Extended AddLayerIntent with target and changeSourceFor fields
- `Application/src/components/views/ImportedView.tsx` - Content-overlay creation, Change Source handler for all three content types
- `Application/src/components/layout/LeftPanel.tsx` - Content-overlay layer detection, SidebarProperties in LAYERS panel
- `Application/src/components/sidebar/SidebarProperties.tsx` - isContentOverlay prop, Change Source button with sequence lookup
- `Application/src/stores/layerStore.ts` - overlayLayers computed for correct FX + content-overlay routing

## Decisions Made
- Extended AddLayerIntent type with `target?: 'content-overlay'` and `changeSourceFor?: { layerId, sequenceId }` rather than using separate signals -- keeps intent flow self-contained
- Change Source uses same ImportedView flow with changeSourceFor discriminator -- minimal new UI code, consistent UX
- Replaced fxLayers computed with overlayLayers (uses getOverlaySequences) to route both FX and content-overlay layer operations correctly

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Fixed layerStore routing for content-overlay layers**
- **Found during:** Task 2
- **Issue:** layerStore.updateLayer and layerStore.remove only checked fxLayers (FX sequences) for routing to sequence-aware methods. Content-overlay layers would incorrectly try to update in the active content sequence where they don't exist.
- **Fix:** Added overlayLayers computed using getOverlaySequences() (FX + content-overlay), replaced fxLayers with overlayLayers in routing checks
- **Files modified:** Application/src/stores/layerStore.ts
- **Verification:** TypeScript compiles cleanly, overlayLayers correctly includes both FX and content-overlay sequences
- **Committed in:** 72c55be (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Essential for correctness -- without this fix, sidebar property edits and layer deletion would fail for content-overlay layers.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Menu, import flow, and sidebar wiring complete for content-overlay sequences
- Downstream plans (03: timeline rendering/interaction, 04: preview compositing) can proceed
- layerStore correctly routes content-overlay operations via overlayLayers computed

## Self-Check: PASSED

All 7 modified files exist. Both task commits (014889c, 72c55be) verified in git log.

---
*Phase: 12.12-new-content-layer-for-timeline*
*Completed: 2026-03-19*
