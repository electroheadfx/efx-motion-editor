---
phase: 38.1-studio-render-path-performance
plan: 10
type: execute
wave: 6
depends_on:
  - 38.1-09
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
requirements:
  - 38.1-D-04
  - 38.1-D-07
  - 38.1-D-09
  - 38.1-D-10
  - R-D-04
  - R-D-05
  - R-D-06
  - R-D-08
  - R-D-09

must_haves:
  truths:
    - "Timeline ResizeObserver installation is mount-lifetime stable: adjacent committed navigation produces zero observer install and zero observer cleanup delta while custom-scrollbar geometry still refreshes correctly (R-D-04/R-D-06)."
    - "Static Workflow chrome has zero render delta after warm-up; only frame-sensitive live regions execute for ordinary navigation (R-D-04/R-D-06)."
    - "At most three private RotoTimelineCellButton bodies execute for one adjacent committed navigation after warm-up, covering the previous/current cells and at most one legitimately changed neighbor (R-D-04)."
    - "Workflow and action callbacks are referentially stable and read current values through existing refs/controllers; no React-style synchronization effect is added (R-D-05/R-D-06)."
    - "All tooltip, drag, selection, focus, keyboard, wheel, custom scrollbar, capsule arbitration, DOM class, aria, and timeline behavior remains exact (38.1-D-09/D-10; R-D-08)."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Install-once observers, static/live memo boundaries, and memoized private cell bodies"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Stable Workflow/action callbacks and identity-aware Workflow prop grouping"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts"
      provides: "Observer lifetime, static/live boundary, cell memo, and preserved interaction source contracts"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Stable Workflow callback and frame-sensitive prop ownership contracts"
  key_links:
    - from: "PhysicsPaintStudio stable workflow actions"
      to: "PhysicsPaintWorkflowStrip live regions and cells"
      via: "stable callbacks accepting frame/key arguments instead of per-map inline closures"
      pattern: "workflow"
    - from: "frameCells derivation cache"
      to: "memoized RotoTimelineCellButton"
      via: "stable per-cell primitive/object props; only changed cells fail shallow comparison"
      pattern: "RotoTimelineCellButton"
    - from: "timeline ResizeObserver"
      to: "custom scrollbar measurements"
      via: "mount-once stable element refs plus explicit geometry refresh after content changes"
      pattern: "ResizeObserver"
---

<objective>
Localize `PhysicsPaintWorkflowStrip` so frame navigation updates only the live frame-sensitive region, executes no more than three cell bodies, and never restarts timeline observers, while preserving every existing interaction and visual contract (R-D-04/R-D-06).

Purpose: The existing derivation cache avoids repeated expensive cell computation but the parent still executes static chrome and all child bodies, and its observer effect restarts when `frameCells` changes. This plan closes those measured gaps without changing timeline behavior.

Output: stable Studio workflow actions, install-once observer lifecycle, static/live Workflow boundaries, memoized private cells, and focused source-contract regression coverage.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-09-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (RED then GREEN): make timeline observer lifetime stable and remove per-render Workflow action identities</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — timeline refs, `updateScrollbar`, ResizeObserver effect currently dependent on `frameCells`, layout/wheel effects, scroll handlers, pointer drag listeners, and rAF cleanup.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — current fresh `workflow` object and inline action closures in `usePhysicsPaintStudioViewModel` input.
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts — existing identity memo helper and view-model pass-through contract.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — preserve existing observer, wheel, drag, and scrollbar assertions.
  </read_first>
  <behavior>
    - Timeline observer setup occurs once per mounted strip and cleanup once on unmount, not on frameCells/current-frame changes.
    - Scrollbar thumb/track geometry refreshes after frame content/current-frame changes without disconnecting observers.
    - Existing plain-wheel vertical-to-horizontal conversion, shift/horizontal bypass, passive:false listener, pointer capture, edge-scroll rAF, Escape, focus restoration, and cleanup semantics are unchanged.
    - Studio action callbacks keep identity across frame-only renders but invoke current controllers/refs and current selection/frame state.
  </behavior>
  <action>
    RED: extend WorkflowStrip source contracts under a dedicated `localized render contract` describe block to fail on the current observer dependency and to require stable element refs, mount-only observer ownership, explicit post-content geometry refresh, and unchanged wheel/drag cleanup literals. Extend Studio's existing `navigation render localization` contracts to reject inline closures in the Workflow view-model assembly and require stable named callbacks.

    GREEN per R-D-06: give the timeline content element its own stable ref and make the ResizeObserver setup effect mount-lifetime stable. Observe the stable scroll element and content element once; increment the Plan 08 install/cleanup counters at the same boundaries. Keep `updateScrollbar` current through a stable callback or ref-backed callback, and trigger measurement after relevant content/geometry changes with a focused DOM-synchronization layout effect or existing event handlers; never reconnect the observer to refresh geometry. Preserve the wheel effect exactly.

    In Studio, replace inline Workflow action closures with named `useCallback` handlers. When a handler needs changing frame/selection/controller state, read the existing current refs/controllers at invocation time rather than adding state-copying effects. Group static versus live Workflow inputs in the view-model contract only where this enables Tasks 2–3; dependency lists enumerate exact inputs. No behavior/order/copy changes (R-D-08).
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts -t "localized render contract|navigation render localization" && pnpm -C app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source contracts prove observer setup is mount-only and geometry refresh is separate from observer ownership.
    - Plan 08 observer counter names remain at the actual setup/cleanup boundaries.
    - Workflow actions are named/stable and no state-copying synchronization effect is introduced.
    - Every pre-existing wheel, drag, scrollbar, selection, tooltip, capsule, DOM, and aria assertion remains green.
  </acceptance_criteria>
  <done>Adjacent navigation no longer disconnects/reinstalls timeline observers, and Workflow actions no longer invalidate children through fresh function identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: split static Workflow chrome from live frame-sensitive regions</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — identify exact static header/action/ruler/scrollbar chrome versus current-cell, selection, status/capsule, and cell-strip live inputs.
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx — Workflow mount and props spread.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — exact markup order, labels, tooltip regions/copy, action ordering, capsule arbitration, and CSS class contracts.
  </read_first>
  <behavior>
    - Static Workflow chrome implementation executes zero times for adjacent committed navigation when its real inputs are unchanged.
    - Live frame-sensitive regions update current/selected/generated/empty state and status/capsule content exactly as before.
    - Header, action ordering, tooltip mounts/regions/copy, ruler, custom scrollbar DOM, log/tube, and accessibility attributes remain in the same observable order and structure.
  </behavior>
  <action>
    RED: extend the same `localized render contract` block with region-scoped source contracts that identify one private static-chrome implementation and one or more private live implementations, require compat memo on static chrome, and pin the existing markup/copy/action order to the correct owner. Tests must fail on the monolithic current strip.

    GREEN: split `PhysicsPaintWorkflowStrip` internally without changing its public export or StudioView mount. Keep the outer strip responsible for shared refs and orchestration. Move inputs that do not change on ordinary frame navigation into a private memoized static-chrome component receiving referentially stable primitives/objects/callbacks. Move the exact Plan 08 `render.workflowStaticChrome` counter from the monolithic baseline owner to the top of this private implementation, delete the baseline owner, and source-test that exactly one owner remains. Keep frame cursor, current cell, selection, busy/status/capsule, and frame list in narrowly scoped live components. Do not cache any Signal `.value` in parent props; Signal-subscribing children keep internal reads. Preserve exact DOM order/classes and CSS selectors, using fragments or stable wrapper ownership rather than new visible containers.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts -t "localized render contract|navigation render localization" && pnpm -C app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Static/live ownership source contracts pass and the Plan 08 static Workflow counter is attached to the private static implementation.
    - Public `PhysicsPaintWorkflowStrip` props/export and StudioView mount remain compatible.
    - Existing tooltip, action order, capsule, scrollbar, DOM class, and aria tests remain green.
    - No CSS or test-environment changes are needed.
  </acceptance_criteria>
  <done>Workflow static chrome has an independent memo boundary while live frame/status regions preserve exact behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: memoize private timeline cell bodies with stable props and enforce the three-cell ceiling</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — `RotoTimelineCellButton`, `frameCells` derivation cache invalidation for previous/current, mapped props/handlers, styled tooltip hook, and pointer/click contracts.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — cell data attributes, semantic classes, title/tooltip copy, selection/current visuals, click/pointer handlers, and focus behavior.
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md — exact `<= 3` cell-body native threshold.
  </read_first>
  <behavior>
    - Unchanged cells receive shallow-equal props and skip their private body on adjacent navigation.
    - Previous and next current cells update; at most one additional cell may update when a legitimate neighbor-derived semantic changes, yielding a maximum delta of three.
    - Tooltips retain per-cell local hook state, one-second hover, instant focus, Escape, copy, placement, and aria behavior.
    - Drag, selection, pointer capture, click, and focus route the exact frame/key identifiers through stable shared handlers.
  </behavior>
  <action>
    RED: extend the same `localized render contract` block with source-contract tests requiring a plain private cell implementation plus a memoized private cell wrapper, stable shared action callbacks receiving frame/key arguments, and no inline mapped event closures passed as props. Keep all existing cell behavior assertions.

    GREEN: retain a plain private `RotoTimelineCellButtonImpl` containing `useStyledTooltip` and the exact existing markup, then memoize it with `preact/compat`. Feed it stable primitives and structurally cached cell objects; do not rebuild tooltip/cell descriptor objects for unchanged frames. Replace per-map handler closures with stable parent handlers plus frame/key props, letting the child invoke them locally. Preserve stable `key={frame}` ownership. Keep the current cache invalidation for previous/current and allow one genuinely affected neighbor; do not broaden invalidation. Keep the Plan 08 cell counter at the implementation-body top so native delta measures actual executions.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts -t "localized render contract|navigation render localization" && (pnpm -C app exec vitest run --reporter=json --outputFile=/tmp/38.1-10-known-red.json >/tmp/38.1-10-known-red.log 2>&1; suite_exit=$?; SUITE_EXIT=$suite_exit node --input-type=module -e 'import fs from "node:fs"; const report=JSON.parse(fs.readFileSync("/tmp/38.1-10-known-red.json","utf8")); const expected=["PhysicsPaintWorkflowStrip clipping guard contract (36.15-08, UAT Gap B) places header tooltips below their anchors so no header control is masked by the canvas","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) ignores blank candidates and keeps higher-priority content over blank lines","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) returns the ambient baseline when nothing higher-priority exists"].sort(); const expectedFiles=["PhysicsPaintWorkflowStrip.test.ts","physicsPaintWorkflowPresentation.test.ts"].sort(); const actual=report.testResults.flatMap((file)=>file.assertionResults??[]).filter((item)=>item.status==="failed").map((item)=>item.fullName).sort(); const failedFiles=report.testResults.filter((file)=>file.status==="failed").map((file)=>file.name.split("/").pop()).sort(); const runtimeMessages=report.testResults.filter((file)=>String(file.message??"").trim()!=="").map((file)=>({name:file.name,message:file.message})); if(process.env.SUITE_EXIT!=="1"||report.numFailedTests!==3||JSON.stringify(actual)!==JSON.stringify(expected)||JSON.stringify(failedFiles)!==JSON.stringify(expectedFiles)||runtimeMessages.length!==0){console.error({suiteExit:process.env.SUITE_EXIT,numFailedTests:report.numFailedTests,actual,failedFiles,runtimeMessages});process.exit(1)}') && pnpm -C app exec tsc --noEmit && pnpm -C app build</automated>
  </verify>
  <acceptance_criteria>
    - Private cell implementation and memo wrapper contracts pass; mapped props contain no fresh object/function identity for unchanged cells.
    - Existing cell tooltip, drag, selection, click, focus, data attribute, class, and aria assertions remain green.
    - The JSON full-suite gate reports exactly the three named sanctioned Phase 38 failures and no others; typecheck and build pass.
    - Plan 12 native profiler gate remains the final proof that reverse and forward adjacent navigation each execute no more than three cell bodies and zero static chrome/observer restarts.
  </acceptance_criteria>
  <done>Adjacent frame navigation executes only the cells whose observable state changed, with a strict native ceiling of three and no interaction regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — local UI render and DOM-observer ownership only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-10-01 | Tampering | memoized timeline cells | high | mitigate | Stable prop contract, previous/current cache invalidation retained, one-neighbor allowance, full cell interaction source tests, and native `<=3` counter gate. |
| T-38.1-10-02 | Denial of Service | timeline ResizeObserver | medium | mitigate | Mount-only observer ownership with explicit geometry refresh; Plan 08 install/cleanup counters must stay zero during navigation. |
| T-38.1-10-03 | Tampering | static/live Workflow split | medium | mitigate | Exact DOM/copy/action/tooltip/capsule source contracts and no CSS changes; live state remains in narrow owners. |
| T-38.1-10-04 | Repudiation | frame-navigation acceptance | low | mitigate | Named profiler deltas record static chrome, cell executions, and observer events in both directions after warm-up. |
| T-38.1-10-SC | Tampering | package installs | high | mitigate | No installs or manifest changes (R-D-09). |
</threat_model>

<verification>
- `pnpm -C app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts`
- Full app JSON reporter gate: exactly the three named sanctioned Phase 38 assertions fail; every other assertion passes.
- `pnpm -C app exec tsc --noEmit`
- `pnpm -C app build`
- Plan 12 native profiler gate: static Workflow chrome delta `0`, timeline observer install/cleanup deltas `0`, and cell implementation delta `<=3` for one forward and one reverse adjacent committed navigation after warm-up.
</verification>

<success_criteria>
- Timeline observers install once and do not restart on navigation.
- Static Workflow chrome is skipped on frame-only changes.
- No more than three cell bodies execute per adjacent committed navigation.
- Every existing tooltip/drag/selection/wheel/scrollbar/capsule/accessibility behavior remains exact.
- Automated gates pass; native numerical acceptance remains owned by Plan 12.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-10-SUMMARY.md` when done. Record observer ownership, static/live component map, stable callback table, cell prop-stability/invalidation rationale, automated results, and the pending Plan 12 native counter thresholds.
</output>
