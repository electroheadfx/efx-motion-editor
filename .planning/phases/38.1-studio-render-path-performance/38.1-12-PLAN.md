---
phase: 38.1-studio-render-path-performance
plan: 12
type: execute
wave: 8
depends_on:
  - 38.1-09
  - 38.1-10
  - 38.1-11
files_modified: []
autonomous: false
requirements:
  - 38.1-D-01
  - 38.1-D-02
  - 38.1-D-03
  - 38.1-D-04
  - 38.1-D-05
  - 38.1-D-06
  - 38.1-D-07
  - 38.1-D-08
  - 38.1-D-09
  - 38.1-D-10
  - R-D-01
  - R-D-04
  - R-D-08
  - R-D-09
  - R-D-10

must_haves:
  truths:
    - "All focused profiler/static-region/Workflow/CanvasMount/reference tests pass; the JSON full-suite gate contains exactly the three named sanctioned Phase 38 failures and no others; typecheck and production build pass before native UAT begins (R-D-10)."
    - "The agent never launches the server, browser, or native app; the user owns both native UAT stages (R-D-09/R-D-10)."
    - "After warm-up, one forward and one reverse adjacent committed navigation each show zero deltas for TopBar, ToolRail implementation, entire right-panel region, RightPanel implementation, closed PlayScriptDialog, CanvasMount, app-owned Efx child request, engine-ready/pre-destroy callbacks, static Workflow chrome, and all timeline/canvas observer restarts; Workflow cell execution is at most three (R-D-04)."
    - "Studio/View orchestration may update; frame-sensitive Workflow subregions may update; CanvasStack updates only when overlay/input-lock/reference/playback semantics change. Controlled canvasKey and dimension changes still replace/reconfigure CanvasMount/Efx correctly (R-D-04/R-D-07)."
    - "Only after render-count approval, the user completes and approves the full Phase 38 `38-06` 33-step native script, confirming tooltip/drag/selection/wheel/scrollbar/capsule/scripts/save-reopen/playback/preview/export parity (38.1-D-09/D-10; R-D-08/R-D-10)."
    - "Any rejection is recorded with the smallest owning boundary and blocks final reverification; final automated commands run only after explicit approval of both native stages (R-D-10)."
  artifacts: []
  key_links:
    - from: "Plan 08 native profiler snapshot API"
      to: "R-D-04 acceptance table"
      via: "warm-up, before/after snapshot, and delta in both navigation directions"
      pattern: "__EFX_PHYSICS_PAINT_PROFILE__"
    - from: "native rejection"
      to: "smallest owning implementation plan"
      via: "static Studio regions -> 09; Workflow -> 10; CanvasMount/Efx -> 11"
      pattern: "38.1-(09|10|11)"
    - from: "explicit two-stage approval"
      to: "final automated reverification"
      via: "blocking checkpoint resume signal"
      pattern: "approved"
---

<objective>
Run the complete automated precondition gate, stop for user-owned native render-count acceptance, then stop for the full Phase 38 33-step native regression script, and only after explicit approval rerun final automated reverification (R-D-10).

Purpose: Deterministic source/tests prove structure; only the native runtime proves Preact render localization and complete visible behavior on the user's real project. This plan is the sole acceptance gate and changes no code.

Output: a recorded approval or smallest-owner rejection in `38.1-12-SUMMARY.md`, with precondition and post-approval automated evidence. No production or test artifact is modified.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-09-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-10-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-11-SUMMARY.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run all automated preconditions without modifying the repository</name>
  <files>None</files>
  <read_first>
    - .planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md — final app-owned counter dictionary and native snapshot API.
    - .planning/phases/38.1-studio-render-path-performance/38.1-09-SUMMARY.md — static region wrappers and pending zero-delta surfaces.
    - .planning/phases/38.1-studio-render-path-performance/38.1-10-SUMMARY.md — Workflow static/live/cell and observer ownership.
    - .planning/phases/38.1-studio-render-path-performance/38.1-11-SUMMARY.md — CanvasMount wrapper, callback refs, key/dimension ownership.
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md — R-D-04 thresholds and R-D-09 launch constraints.
  </read_first>
  <action>
    Confirm Plans 08–11 SUMMARYs exist and the working tree is clean before testing. Run the focused profiler, Studio, Workflow, TopBar, RightPanel, CanvasMount, and existing reference/lifecycle suites; then run the app suite through the exact named-known-red JSON gate, TypeScript no-emit check, and production build using the commands below. Do not launch any server, browser, or native app. Do not edit files to make a gate pass inside this acceptance plan: a failure routes back to the plan owning the failing test/surface.

    Ownership routing for automated failures: recorder/instrumentation -> 38.1-08; TopBar/dialog/right-panel region -> 38.1-09; Workflow/observer/cell -> 38.1-10; CanvasMount/Efx lifecycle -> 38.1-11. Stop before native UAT if any command fails.
  </action>
  <verify>
    <automated>test -f .planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-09-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-10-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-11-SUMMARY.md && test -z "$(git status --short)" && pnpm -C app exec vitest run src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/hooks/useRotoReferenceController.test.ts && (pnpm -C app exec vitest run --reporter=json --outputFile=/tmp/38.1-12-pre-known-red.json >/tmp/38.1-12-pre-known-red.log 2>&1; suite_exit=$?; SUITE_EXIT=$suite_exit node --input-type=module -e 'import fs from "node:fs"; const report=JSON.parse(fs.readFileSync("/tmp/38.1-12-pre-known-red.json","utf8")); const expected=["PhysicsPaintWorkflowStrip clipping guard contract (36.15-08, UAT Gap B) places header tooltips below their anchors so no header control is masked by the canvas","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) ignores blank candidates and keeps higher-priority content over blank lines","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) returns the ambient baseline when nothing higher-priority exists"].sort(); const expectedFiles=["PhysicsPaintWorkflowStrip.test.ts","physicsPaintWorkflowPresentation.test.ts"].sort(); const actual=report.testResults.flatMap((file)=>file.assertionResults??[]).filter((item)=>item.status==="failed").map((item)=>item.fullName).sort(); const failedFiles=report.testResults.filter((file)=>file.status==="failed").map((file)=>file.name.split("/").pop()).sort(); const runtimeMessages=report.testResults.filter((file)=>String(file.message??"").trim()!=="").map((file)=>({name:file.name,message:file.message})); if(process.env.SUITE_EXIT!=="1"||report.numFailedTests!==3||JSON.stringify(actual)!==JSON.stringify(expected)||JSON.stringify(failedFiles)!==JSON.stringify(expectedFiles)||runtimeMessages.length!==0){console.error({suiteExit:process.env.SUITE_EXIT,numFailedTests:report.numFailedTests,actual,failedFiles,runtimeMessages});process.exit(1)}') && pnpm -C app exec tsc --noEmit && pnpm -C app build</automated>
  </verify>
  <acceptance_criteria>
    - Plans 08–11 SUMMARYs exist and the working tree is clean before commands run.
    - Every focused suite passes; the full-suite JSON report matches exactly the three named sanctioned Phase 38 failures and no others; typecheck and build pass.
    - No file is changed and no runtime is launched.
    - Any failure stops this plan and is routed to 08, 09, 10, or 11 before native UAT.
  </acceptance_criteria>
  <done>All automated preconditions are green on a clean tree, so user-owned native acceptance may begin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>BLOCKING native UAT: strict render-count matrix, controlled remounts, then the complete Phase 38 33-step script</name>
  <precondition>Task 1 is green on a clean tree; Plans 08–11 SUMMARYs identify the final counter names; the user has the native app running on their side with the existing profiler gate enabled.</precondition>
  <read_first>
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md — exact warm-up/snapshot protocol and sanctioned/unsanctioned surfaces.
    - .planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md — final native API syntax and counter dictionary; use these exact shipped names if they differ from the planning labels below.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-PLAN.md:78-129 — present and execute the full user-owned Phase 38 script verbatim, all 33 numbered steps, without abbreviation.
  </read_first>
  <what-built>
    Plans 08–11 extended the existing development profiler, isolated static Studio regions, localized Workflow observers/chrome/cells, and isolated persistent CanvasMount/Efx lifecycle. This checkpoint builds nothing; it obtains native evidence that the exact counter thresholds and all Phase 38 visible behaviors pass on the user's real project.
  </what-built>
  <action>
    Present the following two-stage script to the user and WAIT. The agent must not launch the server, browser, or native app, and must not patch a failure inside this checkpoint.

    STAGE A — STRICT NATIVE RENDER-COUNT ACCEPTANCE

    Setup:
    1. Open the user's real Physics Paint project used for performance acceptance, with multiple real Roto keys and generated/empty adjacent cells.
    2. Ensure the Play Script dialog is closed, the shortcuts overlay is not changing, playback is stopped, and no brush/input mutation is active.
    3. Enable the existing development profiler gate. Call `window.__EFX_PHYSICS_PAINT_PROFILE__.clear()`.
    4. Navigate one adjacent committed frame forward and one backward, waiting for the existing rAF propagation after each, to warm subscriptions and caches.

    Forward sample:
    5. Capture `beforeForward = window.__EFX_PHYSICS_PAINT_PROFILE__.snapshot()`.
    6. Navigate exactly one adjacent committed frame forward and wait for committed rAF settlement.
    7. Capture `afterForward = window.__EFX_PHYSICS_PAINT_PROFILE__.snapshot()` and compute `forward = window.__EFX_PHYSICS_PAINT_PROFILE__.delta(beforeForward, afterForward)` using the shipped API signature from 38.1-08-SUMMARY.

    Reverse sample:
    8. Capture `beforeReverse`, navigate exactly one adjacent committed frame backward, wait for settlement, capture `afterReverse`, and compute `reverse` the same way.

    For BOTH `forward` and `reverse`, verify this matrix using the final counter names recorded by Plan 08:

    | Surface | Required delta |
    |---|---:|
    | PhysicsPaintTopBar implementation | 0 |
    | PhysicsPaintToolRail implementation | 0 |
    | entire right-panel region shell/toggle | 0 |
    | PhysicsPaintRightPanel implementation | 0 |
    | closed PhysicsPaintPlayScriptDialog | 0 |
    | PhysicsPaintCanvasStack (unchanged overlay/input semantics) | 0 |
    | PhysicsPaintCanvasMount | 0 |
    | app-owned Efx child request | 0 |
    | CanvasMount engine-ready callback | 0 |
    | CanvasMount pre-destroy callback | 0 |
    | static Workflow chrome | 0 |
    | timeline ResizeObserver install | 0 |
    | timeline ResizeObserver cleanup | 0 |
    | CanvasStack ResizeObserver install/cleanup | 0 |
    | CanvasStack MutationObserver install/cleanup | 0 |
    | CanvasMount ResizeObserver install/cleanup | 0 |
    | app engine-lifecycle setup/cleanup | 0 |
    | RotoTimelineCellButton implementation | <= 3 |

    Sanctioned checks:
    9. Studio/View orchestration and frame-sensitive Workflow counters may be positive. CanvasStack must be zero during an ordinary navigation unless the snapshot also shows a real overlay/input-lock/reference/playback semantic change.
    10. Trigger an existing overlay or input-lock change (for example enter/leave a real paint/drag/busy state) and confirm CanvasStack remains live and the overlay/lock visibly updates.
    11. Switch to another Physics Paint layer/project so `canvasKey` changes, then return; confirm one controlled old-mount cleanup and one new app-owned Efx request/engine-ready path, with the correct canvas visible.
    12. Open/reopen a Physics Paint surface with different canvas dimensions (or use the existing dimension-changing project workflow) and confirm the mount reconfigures to the new contained size with no stale canvas or observer leak.

    Memo/Signal runtime checks (source contracts are not sufficient proof):
    13. Open Play Script through its Signal-backed controller, change the frame-count input, then Cancel and reopen/Generate once. Confirm open/content/progress/close/focus-return behavior remains immediate.
    14. Create an Undoable paint mutation and use Undo/Redo so their ToolRail availability/badges change without requiring a frame-prop invalidation; confirm only the narrow buttons update and actions remain correct.
    15. Change the right-panel color/palette and one controller-backed brush option; confirm picker, swatches/recent/favorites, canvas input, and persistence behavior update while the memoized region remains functional.
    16. Exercise Scripts selection plus a busy/status transition (refresh or existing library operation); confirm rows, selection, disabled reasons, status, and toolbar actions react immediately.
    17. Collapse and expand the right panel, then repeat one palette and one Scripts update to confirm the extracted region restores state and internal Signal subscriptions.
    18. Report the forward and reverse delta objects plus PASS/FAIL for steps 9–17. A Stage A failure blocks Stage B.

    Rejection routing for Stage A:
    - TopBar/dialog/right-panel region or RightPanel nonzero -> 38.1-09.
    - Workflow static chrome, cell count, timeline observer, tooltip/drag/selection/wheel/scrollbar/capsule issue -> 38.1-10.
    - CanvasStack/CanvasMount, app-owned Efx child request, engine-ready/pre-destroy callback, canvasKey, dimensions, or canvas observer issue -> 38.1-11.
    - Missing/wrong counters or snapshot semantics -> 38.1-08.

    STAGE B — COMPLETE PHASE 38 NATIVE REGRESSION

    Only after Stage A passes, present and execute `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-PLAN.md` steps 1 through 33 verbatim, including:
    - steps 1–4 group/single Copy;
    - steps 5–9 successful group Paste and one Undo/Redo;
    - steps 10–12 collision/capacity rejects and reusable clipboard;
    - steps 13–14 unchanged single-key paths;
    - steps 15–20 capsule real/generated/empty/playback/feedback arbitration;
    - steps 21–29 tooltip placement/notch/multiline/clamp/scroll/keyboard behavior;
    - steps 30–33 save/reopen, playback, preview/export, cached reference/onion, and timeline extent parity.

    Do not summarize away or omit any numbered step; the referenced 38-06 plan is the canonical exact wording.

    Record one verdict only after both stages: `approved` with the forward/reverse deltas and explicit confirmation that all 33 Phase 38 steps passed, or a rejection listing the failing Stage A item/Stage B step number and smallest owning plan. On rejection, stop; do not run Task 3.
  </action>
  <how-to-verify>
    The user runs both native stages. Stage A must satisfy every numeric row in both directions and controlled overlay/key/dimension checks. Stage B must pass all 33 canonical Phase 38 steps. The user returns explicit approval or precise failing items/step numbers with observed deltas/screenshots where useful.
  </how-to-verify>
  <verify>
    <automated>test -z "$(git status --short)"</automated>
    <human-check>The user explicitly approves Stage A with forward/reverse deltas and controlled replacement checks, then approves every Stage B step 1–33; or reports a rejection routed to 38.1-08, 09, 10, or 11.</human-check>
  </verify>
  <acceptance_criteria>
    - Both navigation directions satisfy every zero threshold and the `<=3` cell threshold after warm-up.
    - CanvasStack remains live for real overlay/input-lock/reference/playback changes; canvasKey and dimension changes still replace/reconfigure correctly.
    - Play Script, ToolRail history, right-panel palette/controllers, Scripts reactive state, and collapse/restore behavior all remain live through memo boundaries.
    - All 33 canonical Phase 38 native steps pass after render-count acceptance.
    - The agent launched no runtime and changed no file.
    - A rejection names the smallest owner and blocks Task 3.
  </acceptance_criteria>
  <done>The user explicitly approved both native stages, or a precise rejection is recorded and routed; no final automated reverification has run before approval.</done>
  <resume-signal>Type `approved` and include the forward/reverse delta objects after both stages pass, or list the failing Stage A item / Stage B step number with what you observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: after explicit approval only, rerun final automated reverification and record closure evidence</name>
  <files>None</files>
  <precondition>The Task 2 user verdict explicitly approves Stage A render counts, controlled CanvasStack/key/dimension checks, and all Stage B steps 1–33; any rejection makes this task ineligible.</precondition>
  <read_first>
    - The explicit Task 2 user verdict in the active execution context — must contain approval, forward/reverse deltas, and confirmation of all 33 steps.
  </read_first>
  <action>
    Only after the approval precondition is true, rerun the same focused suites, existing reference/lifecycle coverage, exact named-known-red JSON app-suite gate, typecheck, and build as Task 1. Confirm the tree remains clean. Do not launch a runtime and do not modify files. Record command results and the approved native evidence in `38.1-12-SUMMARY.md`; describe the reopened phase as accepted only now, never before this task.
  </action>
  <verify>
    <automated>test -z "$(git status --short)" && pnpm -C app exec vitest run src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/hooks/useRotoReferenceController.test.ts && (pnpm -C app exec vitest run --reporter=json --outputFile=/tmp/38.1-12-post-known-red.json >/tmp/38.1-12-post-known-red.log 2>&1; suite_exit=$?; SUITE_EXIT=$suite_exit node --input-type=module -e 'import fs from "node:fs"; const report=JSON.parse(fs.readFileSync("/tmp/38.1-12-post-known-red.json","utf8")); const expected=["PhysicsPaintWorkflowStrip clipping guard contract (36.15-08, UAT Gap B) places header tooltips below their anchors so no header control is masked by the canvas","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) ignores blank candidates and keeps higher-priority content over blank lines","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) returns the ambient baseline when nothing higher-priority exists"].sort(); const expectedFiles=["PhysicsPaintWorkflowStrip.test.ts","physicsPaintWorkflowPresentation.test.ts"].sort(); const actual=report.testResults.flatMap((file)=>file.assertionResults??[]).filter((item)=>item.status==="failed").map((item)=>item.fullName).sort(); const failedFiles=report.testResults.filter((file)=>file.status==="failed").map((file)=>file.name.split("/").pop()).sort(); const runtimeMessages=report.testResults.filter((file)=>String(file.message??"").trim()!=="").map((file)=>({name:file.name,message:file.message})); if(process.env.SUITE_EXIT!=="1"||report.numFailedTests!==3||JSON.stringify(actual)!==JSON.stringify(expected)||JSON.stringify(failedFiles)!==JSON.stringify(expectedFiles)||runtimeMessages.length!==0){console.error({suiteExit:process.env.SUITE_EXIT,numFailedTests:report.numFailedTests,actual,failedFiles,runtimeMessages});process.exit(1)}') && pnpm -C app exec tsc --noEmit && pnpm -C app build && test -z "$(git status --short)"</automated>
  </verify>
  <acceptance_criteria>
    - Explicit two-stage approval existed before commands ran.
    - Every final automated command passes on a clean tree.
    - SUMMARY records forward/reverse native deltas, controlled replacement results, all-33-step approval, and final command results.
    - No production/test file is modified and no runtime is launched.
  </acceptance_criteria>
  <done>Automated evidence and explicit native evidence agree; the reopened localized-render acceptance is complete and recorded without code changes in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native runtime -> recorded acceptance | User-provided profiler deltas and visible UAT results determine phase acceptance. |
| approval checkpoint -> final automation | Final reverification must not execute unless both native stages are explicitly approved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-12-01 | Repudiation | native counter verdict | high | mitigate | Record forward/reverse delta objects and explicit row verdicts in SUMMARY, not a qualitative performance statement. |
| T-38.1-12-02 | Tampering | acceptance sequencing | high | mitigate | Blocking checkpoint, explicit precondition on Task 3, Stage A before all 33 Stage B steps, and no final reverify on rejection. |
| T-38.1-12-03 | Repudiation | regression coverage | medium | mitigate | Canonical 38-06 script is executed verbatim with numbered-step verdict; failures name exact steps and smallest owner. |
| T-38.1-12-04 | Elevation of Privilege | agent runtime ownership | low | mitigate | Agent never launches server/browser/native app; user owns the native environment per CLAUDE.md/R-D-09. |
| T-38.1-12-SC | Tampering | package installs | high | mitigate | No installs, manifests, production files, or test files change in this plan. |
</threat_model>

<verification>
- Pre-UAT and post-approval: focused app suites, existing reference/lifecycle coverage, exact named-known-red JSON full-suite gate, TypeScript no-emit, and production build.
- Native Stage A: exact warm-up/delta matrix in both directions; zero unsanctioned surfaces/observer restarts, `<=3` cells, sanctioned orchestration only, live overlay shell, controlled key/dimension replacement.
- Native Stage B: canonical Phase 38 `38-06` steps 1–33 verbatim.
- Process: clean tree throughout; agent launches no runtime; rejection blocks final commands and routes to the smallest owner.
</verification>

<success_criteria>
- All automated preconditions pass.
- User explicitly approves native render counts in both directions and controlled overlay/key/dimension behavior.
- User explicitly approves all 33 Phase 38 regression steps.
- Final automated reverification runs only after approval and passes.
- No files change in Plan 12; evidence is recorded in `38.1-12-SUMMARY.md`.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-12-SUMMARY.md` when done. Record: Task 1 command results; the exact forward/reverse profiler deltas; Stage A row verdicts and controlled overlay/key/dimension checks; Stage B approval for all 33 canonical steps or rejection with smallest-owner routing; explicit approval timestamp/order; Task 3 final command results; and confirmation that the agent launched no runtime and changed no production/test files.
</output>
