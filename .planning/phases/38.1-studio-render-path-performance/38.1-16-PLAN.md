---
phase: 38.1-studio-render-path-performance
plan: 16
type: tdd
wave: 9
depends_on:
  - 38.1-12
files_modified:
  - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
  - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts
autonomous: true
gap_closure: true
requirements:
  - 38.1-D-07
  - R-D-02
must_haves:
  truths:
    - "A throwing `window.localStorage` getter disables optional profiling without escaping into Studio rendering (38.1-D-07, R-D-02)."
    - "A throwing `localStorage.getItem()` disables optional profiling without recording samples or counters."
    - "Profiler snapshots remain deterministic and explicit-zero while storage is unavailable."
  artifacts:
    - path: app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
      provides: "Fail-closed profiling gate"
    - path: app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts
      provides: "Restricted-storage behavior coverage"
  key_links:
    - from: app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
      to: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      via: "render-path counter calls consult the optional profiling gate"
      pattern: "profilingEnabled"
---

<objective>
Close the profiler warning by making optional instrumentation fail closed when browser storage access is restricted (38.1-D-07, R-D-02).

Purpose: Development-only counters must never become a production render-path failure source.
Output: Exception-safe profiling detection and two restricted-storage regression cases.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md
@app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
@app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts
</context>

## Artifacts this phase produces

- Production source: fail-closed storage access in `physicsPaintPerformanceTrace.ts`.
- Tests: throwing-property and throwing-method behavior cases in the existing profiler suite.
- Documentation: none.
- Configuration: none.
- Dependencies: none.
- Persisted data or migrations: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disable profiling safely when localStorage access throws</name>
  <read_first>
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts:1-100 — profiler registry and unsafe enablement gate
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts:1-190 — current window stub, disabled behavior, counters, snapshots, and native profiler object
    - .planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md:103-110 — warning acceptance contract
  </read_first>
  <files>app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts, app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts</files>
  <behavior>
    - RED 1: a `window` object whose `localStorage` property getter throws does not make sample recording, counter recording, or snapshot access throw.
    - RED 2: a storage object whose `getItem()` throws produces the same fail-closed behavior.
    - RED 3: in both cases no sample or counter is recorded and the canonical snapshot registry remains populated with explicit zero counters.
    - RED 4: ordinary enabled and disabled storage behavior remains unchanged.
  </behavior>
  <action>
    RED: Add separate tests for a throwing property descriptor and a throwing `getItem()` implementation. Exercise exported recorder/counter/snapshot functions rather than the private gate; assert no exception escapes, no data is recorded, and the complete explicit-zero counter registry remains available.

    GREEN: Keep the server/non-development short-circuit, then contain both the property access and `getItem()` call in one fail-closed exception boundary that returns disabled on any storage error. Do not log from the render path, mutate the profiler registry on failure, change the storage key, or alter the native profiler API. This implements 38.1-D-07 and R-D-02.

    REFACTOR: Keep the exception boundary localized to enablement detection. Do not add component guards, hooks, Signals, or callers that duplicate the policy.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both restricted-storage cases fail before the production edit and pass afterward.
    - No storage exception can escape through any exported recorder/counter call.
    - Disabled snapshots remain deterministic with the complete zero-valued counter registry.
    - Existing enabled profiler, detached snapshot, delta, and native object tests remain green.
  </acceptance_criteria>
  <done>Optional profiling is fail-closed for both storage access failure modes (38.1-D-07, R-D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser storage capability → Studio render path | A host-controlled storage implementation is consulted by optional instrumentation during rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38.1-16-01 | Denial of Service | `profilingEnabled()` | high | mitigate | Catch both property-getter and method-call failures and prove exported render-path recorders remain non-throwing. |
| T-38.1-16-02 | Tampering | profiler state | medium | mitigate | Treat storage failure as disabled and assert no samples/counters mutate while snapshots remain deterministic. |
| T-38.1-16-03 | Information Disclosure | storage key lookup | low | accept | The existing fixed development preference key is unchanged and no stored value is exposed. |
| T-38.1-16-SC | Tampering | package supply chain | low | accept | No packages or external services are added. |
</threat_model>

<verification>
- Focused profiler tests pass with `vitest run`.
- `pnpm -C app exec tsc --noEmit` passes.
- No server, browser, or native runtime is launched.
</verification>

<success_criteria>
- Restricted storage cannot abort Studio rendering through performance counters.
- Profiling remains enabled only for the existing explicit development opt-in.
- TDD history contains RED then GREEN commits, with optional REFACTOR after green.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-16-SUMMARY.md` when done.
</output>
