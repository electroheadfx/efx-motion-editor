---
phase: 38.1-studio-render-path-performance
plan: 15
type: tdd
wave: 9
depends_on:
  - 38.1-12
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts
autonomous: true
gap_closure: true
requirements:
  - 38.1-D-09
  - R-D-08
must_haves:
  truths:
    - "Replacing physical Roto state with identical records/interpolation but a different valid capacity is a real structural mutation (38.1-D-09, R-D-08)."
    - "A capacity-only replacement installs the new capacity, clamps the cursor, replaces the projection identity, publishes `rotoPhysicalRevision`, and notifies visible subscribers."
    - "An exact records/interpolation/capacity match remains a no-op."
  artifacts:
    - path: app/src/stores/physicPaintStore.ts
      provides: "Capacity-aware replacement no-op guard"
    - path: app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts
      provides: "Capacity-only mutation and exact-no-op regression matrix"
  key_links:
    - from: app/src/stores/physicPaintStore.ts
      to: app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts
      via: "replaceRotoPhysicalRecords observable store API"
      pattern: "replaceRotoPhysicalRecords"
    - from: app/src/stores/physicPaintStore.ts
      to: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      via: "rotoPhysicalRevision and physicPaintVersion publication"
      pattern: "rotoPhysicalRevision"
---

<objective>
Close the third behavior-preservation blocker by treating capacity as a member of physical Roto structural identity (38.1-D-09, R-D-08).

Purpose: A successful capacity replacement must install every requested structural field and make the visible timeline consistent immediately.
Output: A capacity-aware no-op guard and complete observable regression coverage.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-07-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md
@app/src/stores/physicPaintStore.ts
@app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts
@app/src/stores/physicPaintStore.test.ts
</context>

## Artifacts this phase produces

- Production source: capacity-aware structural replacement in `physicPaintStore.ts`.
- Tests: expanded physical structural-cache behavior suite.
- Documentation: none.
- Configuration: none.
- Dependencies: none.
- Persisted data or migrations: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish capacity-only physical timeline replacements</name>
  <read_first>
    - app/src/stores/physicPaintStore.ts:105-123 — structural cache identity triple
    - app/src/stores/physicPaintStore.ts:1202-1225 — current replacement no-op guard and skipped publication path
    - app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts:1-260 — projection identity, recomputation, and exact-no-op harness
    - app/src/stores/physicPaintStore.test.ts:1-70 — `physicPaintVersion` and dirty-callback assertion pattern
    - .planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md:88-101 — required observable outcomes
  </read_first>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts</files>
  <behavior>
    - RED 1: after installing records/interpolation at a larger capacity and moving the cursor beyond a smaller requested capacity, replacing the same records/interpolation with the smaller capacity succeeds and installs that capacity.
    - RED 2: the cursor clamps to the new upper bound and the projected timeline is replaced with cells bounded by the new capacity.
    - RED 3: `rotoPhysicalRevision` and `physicPaintVersion` each publish the structural/visible mutation, and the registered dirty callback observes it once.
    - RED 4: repeating the replacement with the same records, interpolation, and capacity keeps projection identity and publication counters unchanged.
  </behavior>
  <action>
    RED: Extend the structural-cache suite using the public store replacement, capacity/cursor/projection getters, exported Signals, and the established dirty callback seam. Use identical record/interpolation content and only vary capacity for the failing case. Assert independent literal revision/version deltas and projection identity; confirm the current early return makes the test fail.

    GREEN: Read the previous installed capacity before the no-op decision and require records revision, interpolation revision, and capacity all to match before returning success without publication. Let a capacity difference continue through the existing cursor clamp, capacity installation, projection-cache invalidation/replacement, `rotoPhysicalRevision` increment, and `_notifyVisualChange()` path. Preserve validation, payload cleanup, exact-no-op behavior, Signal publication, and the Phase 38.1 structural cache introduced by Plan 07. This implements 38.1-D-09 and R-D-08.

    REFACTOR: Keep the comparison local to `replaceRotoPhysicalRecords`; do not add a second cache key abstraction or copied state. Re-run the focused store suites after cleanup.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Capacity-only replacement installs the requested capacity and clamps an out-of-range cursor.
    - The resulting projection has a new identity and the requested extent.
    - `rotoPhysicalRevision`, `physicPaintVersion`, and the visible dirty callback publish the change.
    - Repeating the exact triple remains a true no-op with stable projection identity and no publication.
  </acceptance_criteria>
  <done>Capacity participates in the replacement no-op identity exactly as it already participates in the structural cache identity (38.1-D-09, R-D-08).</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

This plan introduces no authentication, session, access-control, network, or new untrusted-input surface. ASVS L1 integrity and availability controls apply to validated physical-timeline replacement state, projection identity, and Signal/UI publication already mitigated by the STRIDE register below.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated replacement payload → mutable store | Records, interpolation, and capacity become authoritative timeline state. |
| Store mutation → Signal/UI subscribers | Revision and visible-version publication drives projection consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38.1-15-01 | Tampering | replacement no-op guard | high | mitigate | Compare all three structural identity members and prove capacity-only updates through public getters and Signals. |
| T-38.1-15-02 | Denial of Service | projection cache invalidation | medium | mitigate | Preserve exact-triple no-op behavior so identical replacement requests do not rebuild or publish. |
| T-38.1-15-03 | Repudiation | visible mutation publication | medium | mitigate | Assert deterministic revision/version and dirty-callback deltas for the capacity mutation. |
| T-38.1-15-SC | Tampering | package supply chain | low | accept | No package install or dependency modification is present. |
</threat_model>

<verification>
- Focused structural-cache and store suites pass with `vitest run`.
- `pnpm -C app exec tsc --noEmit` passes.
- No server, browser, or native runtime is launched.
</verification>

<success_criteria>
- Capacity-only requests cannot be acknowledged without installing and publishing the requested state.
- Exact structural matches remain allocation- and notification-free.
- TDD history contains RED then GREEN commits, with optional REFACTOR after green.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-15-SUMMARY.md` when done.
</output>
