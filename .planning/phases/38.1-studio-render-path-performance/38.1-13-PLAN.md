---
phase: 38.1-studio-render-path-performance
plan: 13
type: tdd
wave: 9
depends_on:
  - 38.1-12
files_modified:
  - app/src/lib/rotoFrameDraw.ts
  - app/src/lib/rotoFrameDraw.test.ts
  - app/src/components/physic-paint/view/rotoPlaybackBackground.ts
  - app/src/components/physic-paint/view/rotoPlaybackBackground.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
autonomous: true
gap_closure: true
requirements:
  - 38.1-D-09
  - R-D-08
must_haves:
  truths:
    - "Cached Roto playback leaves transparent projects transparent instead of inserting an opaque fallback (38.1-D-09, R-D-08)."
    - "Cached playback resolves background texture, paper grain, and grain strength as independent metadata and renders the same composite contract as preview/export (38.1-D-09, R-D-08)."
    - "Changing the playback background subscription does not change the approved render-localization ownership or add a new hook/effect layer."
  artifacts:
    - path: app/src/components/physic-paint/view/rotoPlaybackBackground.ts
      provides: "Testable cached-playback subscription/composition seam over the canonical Roto resolver and compositor"
    - path: app/src/components/physic-paint/view/rotoPlaybackBackground.test.ts
      provides: "Behavior coverage for transparency and independent texture/grain metadata"
    - path: app/src/lib/rotoFrameDraw.ts
      provides: "Shared background compositing that applies independent grain after either raster or image texture input"
  key_links:
    - from: app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
      to: app/src/components/physic-paint/view/rotoPlaybackBackground.ts
      via: "playback canvas effect delegates subscription and drawing"
      pattern: "subscribe.*Roto.*Playback.*Background"
    - from: app/src/components/physic-paint/view/rotoPlaybackBackground.ts
      to: app/src/lib/rotoFrameDraw.ts
      via: "canonical metadata resolution and drawRotoFrameComposite"
      pattern: "drawRotoFrameComposite"
---

<objective>
Close the first production behavior-preservation blocker by routing cached playback through the canonical Roto background contract (38.1-D-09, R-D-08).

Purpose: A cached playback frame must show the same transparent/color/paper/grain result as the live, preview, and export paths.
Output: A production playback composition seam, shared-compositor correction, and behavior-first regression tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md
@.planning/phases/38.1-studio-render-path-performance/38.1-REVIEW.md
@app/src/types/physicPaint.ts
@app/src/lib/projectPaperRaster.ts
@app/src/lib/rotoFrameDraw.ts
@app/src/lib/rotoFrameDraw.test.ts
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
</context>

## Artifacts this phase produces

- Production source: `app/src/components/physic-paint/view/rotoPlaybackBackground.ts`; corrected shared composition in `app/src/lib/rotoFrameDraw.ts`; playback wiring in `PhysicsPaintStudioView.tsx`.
- Tests: `rotoPlaybackBackground.test.ts` and expanded `rotoFrameDraw.test.ts`.
- Documentation: none.
- Configuration: none.
- Dependencies: none.
- Persisted data or migrations: none.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end cached playback background parity through the shared Roto compositor</name>
  <reversibility rating="costly">The shared compositor is consumed by playback, preview, and export, so undoing an incorrect semantic change requires coordinated regression work across all three pixel paths.</reversibility>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx:47-62 — current duplicated cached-playback effect
    - app/src/lib/rotoFrameDraw.ts:54-124 — canonical resolver and compositor, including the prepared-paper early-return path
    - app/src/lib/rotoFrameDraw.test.ts:1-80 — independent metadata test vocabulary and canvas-operation spies
    - app/src/lib/projectPaperRaster.ts — asynchronous paper subscription and unsubscribe contract
    - app/src/types/physicPaint.ts:401-408 — canonical background metadata fields
    - .planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md:50-69 — blocker acceptance contract
  </read_first>
  <files>app/src/lib/rotoFrameDraw.ts, app/src/lib/rotoFrameDraw.test.ts, app/src/components/physic-paint/view/rotoPlaybackBackground.ts, app/src/components/physic-paint/view/rotoPlaybackBackground.test.ts, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx</files>
  <behavior>
    - RED 1: metadata with `background: "transparent"`, `paperGrain: "canvas1"`, and `grainStrength: 0` clears the playback canvas, performs no opaque fill, and does not subscribe to a paper texture.
    - RED 2: metadata with background texture `canvas2`, paper grain `canvas3`, and strength `0.65` subscribes to `canvas2` while the canonical compositor receives and applies `canvas3` at `0.65` independently.
    - RED 3: replacing or disposing a playback background subscription invokes the raster subscription cleanup exactly once.
    - RED 4: a prepared background paper canvas does not suppress a distinct positive-strength paper-grain overlay in `drawRotoFrameComposite`.
  </behavior>
  <action>
    RED: Add behavior tests before production edits. Test observable canvas operations and subscription selection with independent literals; do not assert source strings. Confirm the transparent and differing texture/grain cases fail against the current duplicated effect/shared-compositor behavior.

    GREEN: Introduce a focused `rotoPlaybackBackground.ts` function that accepts the target 2D context, dimensions, complete `PhysicPaintRotoBackgroundMetadata`, and the existing paper subscription dependency. Resolve the metadata through a shared Roto background instruction seam rather than reinterpreting fields in the component. For transparency, clear and return a no-op cleanup without requesting a paper raster. For a drawable background, subscribe using the resolved background texture and invoke `drawRotoFrameComposite` with no paint source. Adjust `rotoFrameDraw.ts` so a supplied paper canvas and an independently selected positive-strength grain both participate in the composite; preserve existing preview/export behavior and deterministic grain generation. Replace only the cached background effect body in `PhysicsPaintStudioView.tsx` with this seam, retaining its canvas element, dimensions, class, aria contract, and effect cleanup. This implements 38.1-D-09 and R-D-08 without adding component state, Signals, or another effect.

    REFACTOR: Remove duplicated metadata interpretation from the view and keep the helper interface narrow. Re-run both focused suites after cleanup; do not alter playback timing, render counters, memo boundaries, or approved navigation behavior.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/view/rotoPlaybackBackground.test.ts src/lib/rotoFrameDraw.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The RED commit demonstrates failures for transparent playback and independent `canvas2`/`canvas3`/`0.65` metadata before production changes.
    - The GREEN result leaves transparent playback cleared with no opaque fallback or paper subscription.
    - The GREEN result selects the background paper source independently from `paperGrain` and applies the requested grain strength through the canonical compositor.
    - Existing `drawRotoFrameComposite` behavior tests remain green, including preview/export-compatible paint ordering.
    - The component retains one external-sync effect and returns the real subscription cleanup.
  </acceptance_criteria>
  <done>Cached playback uses the shared Roto background/compositing seam and has behavior proof for both required metadata cases (38.1-D-09, R-D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Project metadata → canvas compositor | Persisted project background fields determine visible pixels. |
| Async paper raster callback → mounted playback canvas | A delayed callback may arrive after replacement or teardown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38.1-13-01 | Tampering | `rotoPlaybackBackground.ts` metadata mapping | high | mitigate | Resolve the complete metadata through the canonical seam and lock transparent plus deliberately different texture/grain cases with behavior tests. |
| T-38.1-13-02 | Denial of Service | paper raster subscription lifecycle | medium | mitigate | Return and test the actual unsubscribe function so replaced/unmounted playback canvases do not retain callbacks. |
| T-38.1-13-03 | Information Disclosure | canvas background rendering | low | accept | The compositor uses existing local project metadata and bundled paper assets; no new data boundary is introduced. |
| T-38.1-13-SC | Tampering | package supply chain | low | accept | No package-manager install, dependency change, or external code is introduced. |
</threat_model>

<verification>
- Focused playback/compositor tests pass with `vitest run`.
- `pnpm -C app exec tsc --noEmit` passes.
- No server, browser, or native runtime is launched.
</verification>

<success_criteria>
- Transparent cached playback remains transparent.
- Background texture, paper grain, and grain strength are independently honored.
- Playback, preview, and export share one compositing contract rather than duplicated view logic.
- TDD history contains RED then GREEN commits, with REFACTOR only if cleanup changes are made.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-13-SUMMARY.md` when done.
</output>
