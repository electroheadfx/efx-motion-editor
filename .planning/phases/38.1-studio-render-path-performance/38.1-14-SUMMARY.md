---
phase: 38.1-studio-render-path-performance
plan: 14
subsystem: physics-engine
tags: [physics, wet-buffers, deterministic-replay, serialization, tdd]

# Dependency graph
requires:
  - phase: 38.1-12
    provides: Approved Phase 38.1 render-path baseline and focused engine verification gates
provides:
  - Completed-session physics mode ordering in stopPhysics()
  - Selective last-session wet-buffer finalization that preserves unrelated saved wet pixels
  - Recorded and serialized local/last/all/null physics-mode replay contract
affects: [38.1-15, 38.1-18, physics-paint-replay, project-serialization]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Capture completed session state before restoring ambient engine state
    - Carry recorded physics mode explicitly through deterministic replay

key-files:
  created: []
  modified:
    - packages/efx-physic-paint/src/types.ts
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts

key-decisions:
  - "Capture completedMode once at the stopPhysics() guard boundary and use it for wet synchronization, action recording, and replay metadata before restoring savedPhysicsMode."
  - "Represent recorded physics modes with one PhysicsMode alias and preserve local, last, all, and null in version-2 serialization without a legacy migration branch."

patterns-established:
  - "Completed-session ordering: teardown and finalize from captured session state, then restore prior ambient state."
  - "Recorded replay inputs are explicit arguments and never inferred from the engine's current ambient mode."

requirements-completed: [38.1-D-09, R-D-08]

coverage:
  - id: D1
    description: "Stopping a completed last physics session updates affected wet pixels while preserving unrelated saved wet pixels and restoring the prior mode."
    requirement: "38.1-D-09"
    verification:
      - kind: integration
        ref: "packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts#finalizes a completed last session before restoring the prior physics mode"
        status: pass
    human_judgment: false
  - id: D2
    description: "Completed physics modes round-trip through version-2 serialization and drive deterministic diffusion replay explicitly."
    requirement: "R-D-08"
    verification:
      - kind: integration
        ref: "packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts#round-trips every completed physics mode and replays the recorded mode"
        status: pass
    human_judgment: false
  - id: D3
    description: "Existing cooperative finalization, live alpha cache, pointer input, and package type contracts remain green."
    verification:
      - kind: integration
        ref: "pnpm -C app exec vitest run --root ../packages/efx-physic-paint src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts"
        status: pass
      - kind: other
        ref: "pnpm -C packages/efx-physic-paint check"
        status: pass
    human_judgment: false

# Metrics
duration: 7min
completed: 2026-07-29
status: complete
---

# Phase 38.1 Plan 14: Completed Physics Mode Finalization Summary

**Last-session wet-buffer preservation with explicit completed-mode recording, version-2 serialization, and deterministic replay propagation**

## Performance

- **Duration:** 7 min
- **Started:** 2026-07-29T06:22:31Z
- **Completed:** 2026-07-29T06:29:04Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments

- Reordered `stopPhysics()` so the completed session mode governs selective wet-buffer synchronization and action metadata before the prior mode is restored.
- Preserved unrelated `savedWet` pixels for `last` sessions while retaining immediate interval teardown and completed-mutation publication.
- Widened the recorded mode contract to `local | last | all | null` and threaded the recorded value through redraw, partial replay, save, and load paths.
- Added regression coverage for wet-buffer sentinels, completed tick metadata, restored public state, serialization round trips, and replay arguments.

## Task Commits

TDD gates and implementation were committed atomically:

1. **RED: Add failing completed-mode finalization coverage** - `10cd5f65` (test)
2. **GREEN: Preserve completed physics mode** - `3e528839` (fix)

**Plan metadata:** committed with this summary and sequential state update.

## Files Created/Modified

- `packages/efx-physic-paint/src/types.ts` - Defines the shared `PhysicsMode` recorded/serialized contract.
- `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts` - Finalizes and records the completed mode, restores prior state afterward, and replays explicit recorded modes.
- `packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts` - Covers last-session wet preservation, mode restoration, serialization, and deterministic replay propagation.

## Decisions Made

- Capture the completed mode exactly once before interval teardown so every finalization decision uses one stable session value.
- Restore `savedPhysicsMode` after action recording but before completed-mutation notification, preserving the listener-visible public state from the prior implementation.
- Keep project format version 2 and preserve all valid engine modes directly; invalid or absent serialized values still fail closed to `null` without compatibility migration code.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The replay assertion initially used a non-empty recorded stroke with `pointCount: 0`, which correctly represented an incomplete brush and therefore did not replay diffusion. The fixture was corrected to the production zero-point continuation shape before the GREEN verification.

## Known Stubs

None. The existing `tool: 'paint'` continuation marker is established production replay metadata, not an unimplemented UI or data stub.

## Verification

- `pnpm -C app exec vitest run --root ../packages/efx-physic-paint src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts` — 58 tests passed.
- `pnpm -C packages/efx-physic-paint check` — passed.
- TDD gate order verified: `10cd5f65` RED precedes `3e528839` GREEN.
- No server, browser, or native runtime was launched.

## TDD Gate Compliance

- RED commit present: `10cd5f65`.
- GREEN commit present after RED: `3e528839`.
- No separate REFACTOR commit was needed; the shared `PhysicsMode` alias was introduced as part of the minimal GREEN implementation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 38.1-15 can proceed against an engine whose completed-session mode is stable across finalization, history recording, serialization, and replay.
- Phase 38 Plan 38-06 remains blocked by the remaining Phase 38.1 gap plans and Plan 18 native acceptance.

## Self-Check: PASSED

- Summary file created at `.planning/phases/38.1-studio-render-path-performance/38.1-14-SUMMARY.md`.
- RED commit `10cd5f65` exists in repository history.
- GREEN commit `3e528839` exists in repository history.
- All declared modified files exist.

---
*Phase: 38.1-studio-render-path-performance*
*Completed: 2026-07-29*
