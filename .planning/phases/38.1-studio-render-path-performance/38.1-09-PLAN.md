---
phase: 38.1-studio-render-path-performance
plan: 09
type: execute
wave: 5
depends_on:
  - 38.1-08
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx
  - app/src/components/physic-paint/view/MemoizedPhysicsPaintTopBar.ts
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/view/MemoizedPhysicsPaintPlayScriptDialog.ts
  - app/src/components/physic-paint/view/PhysicsPaintRightPanelRegion.tsx
autonomous: true
requirements:
  - 38.1-D-04
  - 38.1-D-07
  - 38.1-D-09
  - R-D-04
  - R-D-05
  - R-D-08
  - R-D-09

must_haves:
  truths:
    - "After warm-up, ordinary adjacent committed frame navigation produces zero counter delta for TopBar, the entire right-panel region shell/toggle, RightPanel implementation, and a closed PlayScriptDialog (R-D-04)."
    - "TopBar and PlayScriptDialog retain plain implementation exports; Studio uses dedicated compat memo wrappers, and the existing plain RightPanel plus separate inner memo wrapper contract remains intact (R-D-05)."
    - "The open PlayScriptDialog, Scripts panel, palette/controllers, ToolRail history buttons, collapse toggle, and all other Signal-backed children remain internally reactive even when parent prop renders are skipped (R-D-05)."
    - "TopBar, dialog, and region props are identity-stable across frame-only Studio renders because dependency lists contain only their real inputs and no frame cursor (38.1-D-04/R-D-04)."
    - "No behavior, markup, CSS, package, test environment, server, browser, or native-launch change is introduced (38.1-D-09/R-D-08/R-D-09)."
  artifacts:
    - path: "app/src/components/physic-paint/view/MemoizedPhysicsPaintTopBar.ts"
      provides: "Dedicated compat memo export around the plain TopBar implementation"
    - path: "app/src/components/physic-paint/view/MemoizedPhysicsPaintPlayScriptDialog.ts"
      provides: "Dedicated compat memo export around the plain Signal-subscribing dialog implementation"
    - path: "app/src/components/physic-paint/view/PhysicsPaintRightPanelRegion.tsx"
      provides: "Memoized entire shell/rail/toggle boundary containing the existing memoized RightPanel"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Identity-stable topBar, playScriptDialog, layout/region inputs and stable collapse callback"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Plain-export, wrapper, stable-prop, Signal-reactivity, and frame-exclusion source contracts"
  key_links:
    - from: "PhysicsPaintStudio.tsx identity memo instances"
      to: "Memoized TopBar/dialog/right-panel region"
      via: "referentially stable props spread by PhysicsPaintStudioView"
      pattern: "PropsMemo.resolve"
    - from: "Signal writes inside dialog/right panel/tool rail children"
      to: "memoized component updates"
      via: "children read signal.value internally rather than caching values in parent dependencies"
      pattern: "\.value"
---

<objective>
Localize frame-only Studio renders away from static top-level regions: TopBar, closed PlayScriptDialog, and the entire right-panel shell/toggle/implementation boundary (R-D-04/R-D-05).

Purpose: Plan 08 makes the unnecessary executions visible. This plan applies the established Phase 38 `createIdentityMemo` + dedicated `preact/compat` wrapper pattern while preserving internal Preact Signal subscriptions.

Output: three dedicated static boundaries, identity-stable Studio props, source contracts, and automated proof that frame-derived inputs cannot invalidate them.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-11-SUMMARY.md
@app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
@app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (RED then GREEN): memoize plain TopBar and closed PlayScriptDialog behind identity-stable Studio props</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx, app/src/components/physic-paint/view/MemoizedPhysicsPaintTopBar.ts, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/view/MemoizedPhysicsPaintPlayScriptDialog.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — current `topBar` and `playScriptDialog` view-model assembly, existing `toolRailPropsMemo`/`rightPanelPropsMemo`, controller identities, refs, and callback definitions.
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx — direct TopBar/dialog mounts and prop types.
    - app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx — keep the plain implementation export and exact controls/handlers.
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx — internal `confirmationOpen.value` subscription, focus restoration effect, Escape/Enter/Tab behavior, and plain implementation export.
    - app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts — established dedicated-wrapper pattern required when plain implementations are directly invoked by tests.
  </read_first>
  <behavior>
    - Two Studio assemblies with identical TopBar inputs return prop values that shallow-compare equal; a frame-only input outside the dependency list cannot rebuild them.
    - Two Studio assemblies with the same playScript controller and return-focus ref return shallow-equal dialog props; changing either real input rebuilds the object.
    - TopBar and PlayScriptDialog remain exported plain functions; their dedicated wrapper modules export memoized components used only by StudioView.
    - Dialog Signal writes still open/close the dialog and execute the existing focus restoration path even when parent props are unchanged.
  </behavior>
  <action>
    RED: extend `PhysicsPaintStudio.test.ts` with source-contract assertions for dedicated wrapper modules, StudioView wrapper imports, one identity-memo resolve per region, dependency-line exclusion of frame navigation values, plain implementation exports, and the dialog's internal `confirmationOpen.value` read. Confirm the assertions fail before wrappers/stable assemblies exist.

    GREEN per R-D-05: keep `PhysicsPaintTopBar` and `PhysicsPaintPlayScriptDialog` plain. Create dedicated `.ts` wrapper modules importing `memo` from `preact/compat`, exactly following `MemoizedPhysicsPaintRightPanel.ts`. Update StudioView to mount the wrapper exports.

    In Studio, create per-instance identity memos held in refs for `topBar` and `playScriptDialog`. Build each exact existing props object inside `resolve`; dependency arrays enumerate only values read by the build. Stabilize any referenced callback with `useCallback` only when it is currently recreated per render, and have such callbacks read mutable current values from existing refs where needed rather than adding effect synchronization. Do not include startFrame/current-frame/navigation-generation values. Preserve the dialog controller Signal object and `playButtonRef` identities, never hoisting `.value` reads into Studio. Keep all TopBar control behavior, dialog focus restoration, keyboard behavior, and markup exact (R-D-08).
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts && pnpm -C app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source contracts prove dedicated wrappers are used by StudioView while plain exports remain directly callable.
    - Identity-memo dependencies enumerate exact real inputs and exclude frame navigation values.
    - Dialog still reads `confirmationOpen.value` inside its own implementation and retains its existing focus/keyboard effect.
    - Focused tests and typecheck pass; no CSS, package, config, or environment changes exist.
  </acceptance_criteria>
  <done>TopBar and a closed dialog are skipped on frame-only parent renders while all real prop and Signal changes still reach their implementations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: isolate the entire right-panel shell, toggle, rail, and implementation as one static region</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/view/PhysicsPaintRightPanelRegion.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx — current collapsed rail versus expanded shell/toggle markup and existing `MemoizedPhysicsPaintRightPanel` mount.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — layout object, `rightPanelCollapsed`, setter, and already identity-stable `rightPanel` object.
    - app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts — inner implementation boundary that must remain in use.
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx — internal Signal-backed controllers/palette/script subscribers that must keep updating without a parent render.
  </read_first>
  <behavior>
    - Expanded and collapsed region markup, classes, button title/aria-label/glyph, and collapse behavior are byte-equivalent to the current StudioView branches.
    - A frame-only StudioView render with unchanged collapsed flag, stable toggle callback, and stable rightPanel object skips the entire region body.
    - Changing the collapsed flag renders the correct branch; changing any real rightPanel input re-renders the region and inner implementation as required.
    - Internal RightPanel Signal subscriptions continue to update without a region prop change.
  </behavior>
  <action>
    RED: add source-contract assertions that the collapsed/expanded branches move intact into one dedicated `PhysicsPaintRightPanelRegion` implementation wrapped with compat memo, that StudioView contains exactly one region mount, and that the existing `MemoizedPhysicsPaintRightPanel` remains the inner child. Add a dependency exclusion assertion for frame cursor values.

    GREEN: extract the complete current rail/shell/toggle branches from StudioView into `PhysicsPaintRightPanelRegion.tsx`. Define a plain private implementation receiving `collapsed`, stable `onSetCollapsed`, and the existing identity-stable `rightPanel` props object; export a memoized region component. Move the exact Plan 08 `render.rightPanelRegion` counter from its StudioView baseline location to the top of this private implementation, delete the old owner, and source-test that exactly one owner remains. Keep the existing inner `MemoizedPhysicsPaintRightPanel`, exact DOM order/classes/copy/glyph, and no CSS changes. In Studio, make the collapse callback identity-stable and identity-memoize the region/layout inputs if needed so frame-only renders shallow-compare equal. Do not destructure Signal values in the region.

    Claude discretion from the reopen context: do not add a separate shortcuts-help boundary in this plan. The shortcuts surface is conditionally absent during the measured navigation path and is not an R-D-04 required zero surface; leaving it unchanged avoids unrelated markup movement.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts && pnpm -C app exec tsc --noEmit && pnpm -C app build</automated>
  </verify>
  <acceptance_criteria>
    - StudioView mounts one memoized right-panel region; the expanded/collapsed markup and existing inner memoized implementation are preserved.
    - Region props are stable and contain no frame-derived value; collapse and real panel-input changes still invalidate correctly.
    - RightPanel direct-call and resize-drag tests remain green, proving the plain implementation contract survived.
    - Typecheck and build pass with no CSS, package, config, or test-environment changes.
  </acceptance_criteria>
  <done>The entire right-panel region—not just its inner implementation—has a frame-independent memo boundary with exact collapse and Signal behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — render-boundary localization only; no new external or persistence boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-09-01 | Tampering | memo dependency lists | high | mitigate | Identity-memo builds enumerate exact real inputs; source contracts exclude frame values and verify rebuild on changed real input. |
| T-38.1-09-02 | Denial of Service | stale Signal-backed UI | medium | mitigate | Plain implementations retain internal `.value` reads; wrappers receive Signal/controller objects, never cached values; focused source contracts preserve this architecture. |
| T-38.1-09-03 | Tampering | right-panel collapse markup | low | mitigate | Extract branches intact, retain existing inner wrapper, run direct-call/resize tests, typecheck, build, and native counter UAT in Plan 12. |
| T-38.1-09-SC | Tampering | package installs | high | mitigate | No installs or manifest changes (R-D-09). |
</threat_model>

<verification>
- `pnpm -C app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts`
- `pnpm -C app exec tsc --noEmit`
- `pnpm -C app build`
- Plan 12 native profiler gate: after warm-up, TopBar, right-panel region, RightPanel implementation, and closed dialog deltas are all exactly zero in both adjacent-navigation directions (R-D-04).
</verification>

<success_criteria>
- Static top-level Studio regions are skipped on frame-only committed navigation.
- Plain component exports and internal Signal reactivity remain intact.
- Real prop changes, collapse toggles, and dialog open/close still render correctly.
- Automated gates pass and Plan 12 owns native zero-delta acceptance.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-09-SUMMARY.md` when done. Record wrapper modules, identity dependency tables, plain-export/Signal-reactivity evidence, focused command results, and that native zero-delta acceptance remains pending Plan 12.
</output>
