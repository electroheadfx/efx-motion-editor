---
phase: 38.1
slug: studio-render-path-performance
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-27
---

# Phase 38.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (node environment, `include: ['src/**/*.test.ts']`) |
| **Config file** | `app/vitest.config.ts` |
| **Quick run command** | `pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoTimelineModel.test.ts` (or chosen placement) |
| **Full suite command** | `pnpm --dir app vitest run` (then `pnpm --dir app typecheck`, `pnpm build` for the final gate) |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --dir app vitest run <touched test files>`
- **After every plan wave:** Run `pnpm --dir app vitest run`
- **Before `/gsd-verify-work`:** Full suite must be green (846 existing pass + 3 known-red D-15 + new D-07 test)
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

No requirement IDs are registered for 38.1 (known planning-data gap carried from Phase 38). The D-07 acceptance maps instead:

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 38.1-XX-model | TBD | TBD | D-07 (model) | — | frame change does NOT rebuild structural timeline model | unit (spy counters + signals effect, node) | `pnpm --dir app vitest run <model test>` | ❌ in-wave | ⬜ pending |
| 38.1-XX-sched | TBD | TBD | D-07 (scheduler) | — | N rapid updates coalesce to ≤1 flush per rAF | unit (stubbed rAF, flush counter) | same file or scheduler test | ❌ in-wave | ⬜ pending |
| 38.1-XX-uat | TBD | TBD | D-01..D-05, D-09, D-10 | — | canvas-first feel, playback freeze, zero behavior change, preserved mechanisms | native UAT (blocking, user-owned) | user's real project (D-06) | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None — existing test infrastructure covers the phase. The D-07 test is a new in-wave file, not a Wave 0 prerequisite. Do not add jsdom/happy-dom (project rule: no test config hacks).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Canvas-first feel at many keys | D-03/D-06 | Perceived latency is human-judged | User's real project: click through keys, confirm instant canvas feedback |
| Playback freeze + fps | D-01/D-02/D-06 | Frame-rate feel + frozen UI observation | Cached playback at full project fps; cells/capsule/tube-log frozen; missing-frame line appears once on stop |
| Zero behavior change | D-09/D-10 | Full interaction surface parity | Re-run of the 38-06 33-step script follows in phase 38 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
