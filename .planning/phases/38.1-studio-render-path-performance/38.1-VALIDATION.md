---
phase: 38.1
slug: studio-render-path-performance
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-27
---

# Phase 38.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (node environment, `include: ['src/**/*.test.ts']`) |
| **Config file** | `app/vitest.config.ts` |
| **Quick run command** | `pnpm -C app exec vitest run <focused test files>` |
| **Full suite command** | `pnpm -C app exec vitest run` (then `pnpm -C app exec tsc --noEmit`, `pnpm -C app build`) |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task:** Run `pnpm -C app exec vitest run <touched test files>`
- **After every plan wave:** Run `pnpm -C app exec vitest run`
- **Before `/gsd-verify-work`:** The full app suite must introduce no failures beyond the exact currently sanctioned Phase 38 known-red set; focused reopened-plan tests must all pass.
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

No requirement IDs are registered for 38.1 (known planning-data gap carried from Phase 38). The D-07 acceptance maps instead:

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 38.1-01-T1 | 38.1-01 | 1 | D-07 (model) | T-38.1-01-02 | frame change does NOT rebuild structural timeline model | unit (spy counters + signals effect, node) | `pnpm -C app exec vitest run src/components/physic-paint/hooks/useRotoTimelineModel.test.ts` | ✅ | ✅ green |
| 38.1-02-T1 | 38.1-02 | 1 | D-07 (scheduler) | T-38.1-02-02 | N rapid updates coalesce to ≤1 flush per rAF | unit (stubbed rAF, flush counter) | `pnpm -C app exec vitest run src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts` | ✅ | ✅ green |
| 38.1-06-T1 | 38.1-06 | 4 | D-01..D-05, D-09, D-10 | T-38.1-06-UAT | canvas-first feel, playback freeze, zero behavior change, preserved mechanisms | historical native UAT | user's real project (D-06) | n/a | ✅ historical |
| 38.1-08-T1 | 38.1-08 | 4 | D-07 / R-D-02 | T-38.1-08-01 | profiler counters, detached snapshots, explicit deltas, disabled no-op | unit (node) | `pnpm -C app exec vitest run src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts` | ✅ existing file extended | ✅ green |
| 38.1-08-T2 | 38.1-08 | 4 | D-08 / R-D-03..04 | T-38.1-08-03 | every required render/lifecycle/observer counter has one app-owned owner; no localization change | source contracts + typecheck/build | focused Studio/Workflow tests, `tsc --noEmit`, build | ✅ existing files extended | ✅ green |
| 38.1-08-UAT | 38.1-08 | 4 | R-D-03..04 | T-38.1-08-UAT | actual forward/reverse native RED deltas captured before Plan 09 | native profiler checkpoint (blocking, user-owned) | real project + profiler snapshot API | n/a | ✅ completed |
| 38.1-09-T1 | 38.1-09 | 5 | R-D-04..05 | T-38.1-09-01 | TopBar, ToolRail, closed dialog, and complete right-panel region have stable memo boundaries while Signals remain live | source/identity contracts + focused tests + native checkpoint | Studio/TopBar/RightPanel tests + typecheck/build; user-owned forward/reverse five-counter capture | ✅ wrappers/region committed | ✅ green; native-approved 2026-07-28 |
| 38.1-10-T1 | 38.1-10 | 6 | R-D-04..06 | T-38.1-10-01..03 | timeline observer install-once, static Workflow zero, cells ≤3 per adjacent navigation | source contracts + focused/full tests + native counters | Workflow/Studio/model/scheduler/playback tests + typecheck/build | ✅ existing files extended | ⬜ pending |
| 38.1-11-T1 | 38.1-11 | 7 | R-D-04..08 | T-38.1-11-01..04 | CanvasStack/CanvasMount skipped when semantics stable; callbacks/lifecycle remain current; key/dimension replacement works | source contracts + focused/full tests + native counters | CanvasMount/Studio/reference tests + typecheck/build | ❌ in-wave wrapper/test | ⬜ pending |
| 38.1-12-T1 | 38.1-12 | 8 | D-01..10 / R-D-10 | T-38.1-12-01..04 | automated preconditions, exact native zero/≤3 matrix, Signal interaction parity, and full Phase 38 33-step UAT | full automation + blocking user-owned native UAT | full app tests, typecheck/build, real project | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

None — existing test infrastructure covers the phase. The D-07 test is a new in-wave file, not a Wave 0 prerequisite. Do not add jsdom/happy-dom (project rule: no test config hacks).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Canvas-first feel at many keys | D-03/D-06 | Perceived latency is human-judged | User's real project: click through keys, confirm instant canvas feedback |
| Playback freeze + fps | D-01/D-02/D-06 | Frame-rate feel + frozen UI observation | Cached playback at full project fps; cells/capsule/tube-log frozen; missing-frame line appears once on stop |
| Zero behavior change | D-09/D-10 | Full interaction surface parity | Re-run the complete 38-06 33-step script after localized-render acceptance |
| RED baseline before localization | R-D-03/R-D-04 | Actual Preact reconciliation requires the native runtime | Plan 08: capture forward/reverse profiler deltas after warm-up; all required counter names must be present and at least one threshold RED |
| Plan 09 static-region render counts | R-D-04/R-D-05 | Node source contracts cannot prove native Preact reconciliation skips | Before Plan 10: after warm-up, capture forward/reverse deltas and require `render.topBar`, `render.toolRailImpl`, `render.rightPanelRegion`, `render.rightPanelImpl`, and closed `render.playScriptDialog` to equal zero |
| Memo/Signal runtime parity | R-D-05 | Node source contracts cannot prove mounted Signal updates through memo boundaries | Plan 12: Play Script, ToolRail history, palette/controllers, Scripts state, and collapse/restore interaction checks |
| CanvasStack/CanvasMount lifecycle | R-D-07 | Engine/canvas replacement and visible overlay behavior require native runtime | Plan 12: ordinary-navigation zeros, live overlay semantic changes, controlled canvasKey and dimension replacement |

---

## Validation Sign-Off

- [x] All reopened tasks have focused `<automated>` verification; Plans 08 and 12 retain their native-only gates, and user direction adds a blocking Plan 09 five-counter checkpoint before Plan 10.
- [x] Sampling continuity: every implementation task runs focused tests, and each plan closes with wider relevant gates before its checkpoint or successor.
- [x] Wave 0 covers all MISSING references (none — Wave 0 Requirements section: no gaps)
- [x] No watch-mode flags (every vitest invocation is `vitest run`)
- [x] Feedback latency < 10s (estimated full-suite runtime ~5s)
- [x] `nyquist_compliant: true` set after filesystem validation confirmed focused coverage for every reopened implementation task and explicit native-only checkpoints for Plans 08 and 12.

**Approval:** validated for reopened execution on 2026-07-28; Plan 09 implementation, automated gates, and native five-counter checkpoint are complete. The user confirmed all five static counters at zero in both directions while only the timeline re-rendered. Plan 10 is eligible next but has not started.
