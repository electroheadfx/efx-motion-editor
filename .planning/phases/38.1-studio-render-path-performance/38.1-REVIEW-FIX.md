---
phase: 38.1-studio-render-path-performance
fixed_at: 2026-07-28T08:25:00Z
review_path: .planning/phases/38.1-studio-render-path-performance/38.1-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 38.1: Code Review Fix Report

**Fixed at:** 2026-07-28T08:25:00Z
**Source review:** .planning/phases/38.1-studio-render-path-performance/38.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (1 Critical, 4 Warning — Info findings out of scope)
- Fixed: 5
- Skipped: 0

**Gate after all fixes:** full app suite 873 passed / exactly 3 failures — all sanctioned known-red D-15 (2 physicsPaintWorkflowPresentation + 1 PhysicsPaintWorkflowStrip clipping-guard source-grep), unchanged from the pre-fix baseline; `pnpm --dir app typecheck` clean after every fix. The 3 known-red D-15 tests were not touched.

## Fixed Issues

### CR-01: rAF flush scheduler runs the FIRST queued flush and discards the LATEST

**Files modified:** `app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts`, `app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts`
**Commit:** 39d05338
**Applied fix:** Implemented the D-05 latest-wins coalescing contract: `schedule()` now stores the incoming flush as `pendingFlush` on every call (latest wins) and the single pending rAF runs the most recently scheduled callback at fire time; `dispose()` clears both the rAF id and the pending flush. TDD order followed: added a failing test ("runs the LATEST scheduled flush when several distinct flushes are scheduled in one frame") that pinned the first-wins bug (frame-5 ran instead of frame-7), then fixed the scheduler. The pre-existing 8 scheduler tests did not assert the buggy behavior (they share one closure reading external state) and pass unmodified — 9/9 green. Studio `startFrame` no longer desyncs from the canvas on click bursts.

### WR-01: `start()` early-return on empty frame list leaves a previously running playback interval alive

**Files modified:** `app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts`, `app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts`
**Commit:** 39bd8a71
**Applied fix:** On the empty-frames path, `start()` now routes through `finishPlayback()` (the single D-02 stop funnel — clears the interval, resets `isActive`/`setIsPlaying`, clears `playbackTick`) whenever a timer is actually running (`timerRef.current !== null`), before publishing the "No cached Roto frames yet..." status. The guard keeps the plain no-prior-playback empty start from touching the external play-state callback, preserving the existing test contract (`setIsPlaying` not called) — verified: that test passes unmodified. Added a regression test that starts looping playback, clears the frame list, re-enters `start()`, and asserts the stale interval is dead (no further `onFrame` calls after advancing timers), `isActive` false, and `setIsPlaying` last called with `false` — 6/6 green.

### WR-02: Per-render fresh `interpolationSettings` object defeats the persistent signal-graph memo (38.1 D-07)

**Files modified:** `app/src/components/physic-paint/PhysicsPaintStudio.tsx`
**Commit:** b74ac80a
**Applied fix:** Added `rotoLegacyInterpolationSettings = useMemo(() => launchContext ? physicPaintStore.getRotoInterpolationSettings(launchContext.layerId) : undefined, [launchContext?.layerId, physicPaintVersion.value])` — memoized on the same structural inputs as the sibling `rotoKeyRecords`/`rotoInterpolationState` memos — and passed it into `useRotoTimelineModel` instead of the inline fresh-clone call. The D-07 structural memo now holds at the call site: settings identity is stable per (layerId, physicPaintVersion), so no structural signal-graph rebuild occurs per Studio render. The existing D-07 instrumentation test (`useRotoTimelineModel.test.ts`, 6/6) still shows zero projection rebuilds per frame write and exactly one rebuild per structural-input replacement. Other `getRotoInterpolationSettings` call sites (read-once consumers, not memo inputs) intentionally untouched.

### WR-03: WorkflowStrip per-cell derivation cache grows unboundedly as the timeline window slides

**Files modified:** `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx`
**Commit:** 24a70b9e
**Applied fix:** On a pure `currentFrame` change, entries outside the active window are now evicted: after the existing 2-cell surgical deletes (previous/new current frame — semantics preserved byte-for-byte), the branch builds `new Set(frameCells)` and deletes any cached key not in the window. `frameCells` is memoized on `props.currentFrame`, so the window only slides on this branch; structural identity changes still reset `entries` to a fresh Map. All 55 strip source-grep assertions unaffected (the one failing strip test is the sanctioned known-red D-15 clipping-guard assertion, red before and after).

### WR-04: `_rotoPhysicalStructuralCache` is never pruned on layer teardown or store reset

**Files modified:** `app/src/stores/physicPaintStore.ts`
**Commit:** d6246053
**Applied fix:** Pruned the 38.1-07 structural memo cache at all four teardown paths: `_rotoPhysicalStructuralCache.delete(layerId)` in `_clearLayerState` (derived entry, not OR-ed into `changed`) and `clearRotoPhysicalRecords`; `_rotoPhysicalStructuralCache.clear()` in `reset()` and `loadFromMceOutputs` next to the other map clears. Store suite 46/46 green; no behavioral change (reads were already self-healing — this closes the resident-memory leak only).

## Skipped Issues

None — all in-scope findings were fixed.

---

_Fixed: 2026-07-28T08:25:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
