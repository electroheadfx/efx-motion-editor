---
phase: 38.1-studio-render-path-performance
plan: 17
type: execute
wave: 10
depends_on:
  - 38.1-13
files_modified:
  - app/src/test/preactHookRuntime.ts
  - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintCanvasStack.runtime.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
autonomous: true
gap_closure: true
requirements:
  - 38.1-D-09
  - R-D-07
  - R-D-08
must_haves:
  truths:
    - "Parent rerenders with the same `canvasKey` preserve the mounted CanvasMount/Efx instance while stable forwarders invoke the latest callbacks (R-D-07, R-D-08)."
    - "Changing `canvasKey` tears down the old mounted instance exactly once and creates one replacement; changing dimensions with the same key reconfigures sizing/observer ownership without replacing the engine instance."
    - "Runtime lifecycle evidence, not source regular expressions, proves observer/rAF cleanup and callback freshness."
    - "The runtime test seam adds no DOM package, Vitest configuration, production hook, or synchronization effect."
  artifacts:
    - path: app/src/test/preactHookRuntime.ts
      provides: "Reusable Node-environment hook/effect runtime with explicit rerender and unmount semantics"
    - path: app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts
      provides: "Executed CanvasMount callback, dimension, observer, rAF, and cleanup behavior"
    - path: app/src/components/physic-paint/view/PhysicsPaintCanvasStack.runtime.test.ts
      provides: "Executed same-key persistence and keyed replacement behavior across the Studio boundary"
    - path: app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
      provides: "Plain CanvasStack implementation export used by runtime tests while retaining the existing memo boundary"
  key_links:
    - from: app/src/components/physic-paint/view/PhysicsPaintCanvasStack.runtime.test.ts
      to: app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
      via: "direct execution of the plain CanvasStack implementation and keyed mount vnode"
      pattern: "PhysicsPaintCanvasStack"
    - from: app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts
      to: app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx
      via: "mocked hooks plus mocked EfxPaintCanvas child props"
      pattern: "PhysicsPaintCanvasMount"
---

<objective>
Replace source-spelling-only lifecycle confidence with executable Node-environment component behavior coverage for the persistent CanvasMount/Studio boundary (38.1-D-09, R-D-07, R-D-08).

Purpose: The phase's central instance-persistence, callback-freshness, controlled replacement, dimension, and cleanup guarantees must survive behavior-preserving refactors.
Output: A reusable hook runtime, mounted component tests, and a narrow plain-component test seam with no environment changes.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-11-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md
@.planning/phases/38.1-studio-render-path-performance/38.1-REVIEW.md
@app/src/components/sidebar/SidebarScrollArea.test.ts
@app/src/components/physic-paint/view/PhysicsPaintPalette.test.ts
@app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx
@app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/components/physic-paint/engine/EfxPaintCanvas.lifecycle.test.ts
</context>

## Artifacts this phase produces

- Production source: one named plain CanvasStack export in `PhysicsPaintStudioView.tsx`; no lifecycle behavior change.
- Tests: runtime CanvasMount and CanvasStack suites.
- Test support: reusable `app/src/test/preactHookRuntime.ts` based on existing project harness patterns.
- Documentation: none.
- Configuration: none.
- Dependencies: none; specifically no jsdom or happy-dom.
- Persisted data or migrations: none.

<tasks>

<task type="auto">
  <name>Task 1: Execute CanvasMount rerender, callback, dimension, and cleanup behavior</name>
  <read_first>
    - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx:1-115 — hook slots, stable forwarders, dimension effect, counters, and Efx child
    - app/src/components/sidebar/SidebarScrollArea.test.ts:1-220 — established HookRuntime dependency and cleanup semantics
    - app/src/components/physic-paint/view/PhysicsPaintPalette.test.ts:1-180 — Physics Paint direct-component/VNode test pattern
    - app/src/components/physic-paint/engine/EfxPaintCanvas.lifecycle.test.ts — existing engine-ready and destroy lifecycle vocabulary
    - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts — source contracts that remain architecture checks only
  </read_first>
  <files>app/src/test/preactHookRuntime.ts, app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts</files>
  <behavior>
    - Test 1: rerendering one mounted `PhysicsPaintCanvasMount` runtime with new parent callbacks retains each child callback identity while invocations reach only the latest parent behavior.
    - Test 2: rerendering with unchanged dimensions does not disconnect/reinstall the ResizeObserver or replace the mocked Efx child instance.
    - Test 3: changing width or height in the same mounted runtime disconnects the old observer once, installs one replacement, updates contained sizing, and retains the Efx child instance.
    - Test 4: explicit unmount cancels the pending rAF and disconnects the active observer exactly once; repeated cleanup flushing does not duplicate lifecycle work.
  </behavior>
  <action>
    Build a reusable test-only hook runtime by extracting and generalizing the existing SidebarScrollArea/PhysicsPaintPalette pattern: deterministic `useState`, `useRef`, `useCallback`/`useMemo`, dependency-aware `useEffect`, effect flush, rerender cursor reset, and an idempotent `unmount()` that executes active cleanup slots once. Do not alter Vitest config or add a DOM implementation.

    In the new runtime suite, mock `preact/hooks`, `EfxPaintCanvas`, `ResizeObserver`, request/cancelAnimationFrame, and the minimum shell/parent geometry/query surface. Execute the real `PhysicsPaintCanvasMount` function across rerenders, capture the actual child VNode props, and assert the behaviors above before making any production change. The suite must prove R-D-07/R-D-08 through executed callbacks/effects; retain source tests only as architecture spelling checks and do not cite them as lifecycle proof. No production hook/effect is expected for this task.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts src/components/physic-paint/engine/EfxPaintCanvas.lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Stable child callback function identities invoke callbacks supplied by the latest rerender.
    - Same-dimension rerenders preserve observer and mocked Efx instance ownership.
    - Dimension changes perform one cleanup/reinstall cycle without keyed replacement.
    - Unmount cleanup is behaviorally exactly-once for rAF and ResizeObserver resources.
    - The test runs in the existing Node Vitest environment.
  </acceptance_criteria>
  <done>CanvasMount lifecycle and stable-forwarder guarantees have runtime behavior evidence (R-D-07, R-D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Execute Studio same-key persistence and keyed replacement behavior</name>
  <reversibility rating="reversible">The named plain export is a local testability seam around an existing implementation and can be removed without data or public integration migration.</reversibility>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx:64-145 — CanvasStack implementation, memo wrapper, overlays, and keyed mount ownership after Plan 13
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts:215-239 — current source-only persistent-boundary assertions
    - app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts — compat wrapper ownership
    - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts — mounted child runtime and instance probes from Task 1
    - .planning/phases/38.1-studio-render-path-performance/38.1-REVIEW.md:137-156 — required runtime durability matrix
  </read_first>
  <files>app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/view/PhysicsPaintCanvasStack.runtime.test.ts</files>
  <behavior>
    - Test 1: rerendering the actual CanvasStack implementation with the same `canvasKey` and navigation-only/stable semantic props preserves one mounted CanvasMount/Efx instance.
    - Test 2: updated mount callback props on that same-key instance reach the stable CanvasMount forwarders without remounting.
    - Test 3: changing `canvasKey` cleans the old child lifecycle exactly once and mounts one new instance with the new key.
    - Test 4: changing dimensions inside same-key mount props reconfigures the contained CanvasMount but does not execute keyed teardown/recreation.
  </behavior>
  <action>
    Behavior-first: Add a runtime suite that executes the real CanvasStack implementation and resolves its keyed `MemoizedPhysicsPaintCanvasMount` child through the Task 1 hook runtime. Maintain a child-runtime registry by VNode key so same-key rerenders reuse the mounted runtime and changed keys unmount the old runtime before mounting one replacement. Assertions must observe instance tokens, actual forwarded callback behavior, observer/rAF cleanup counts, and VNode keys rather than read source text.

    Implementation: Give the existing plain CanvasStack implementation a named export suitable for direct runtime execution, while retaining the private/default memoized wrapper, existing prop shape, keyed child location, render counter, overlays, observer ownership, and Studio call site. Do not add a custom memo comparator, state, Signal, hook, or effect. Because Plan 13 also touches this file, preserve its shared playback-background seam exactly. This supplies durable runtime proof for 38.1-D-09, R-D-07, and R-D-08 without reopening approved render thresholds.

    REFACTOR: Keep source-level tests only for intentional module/wrapper ownership constraints; ensure lifecycle claims live in the runtime suites. Consolidate duplicated test harness mechanics into `preactHookRuntime.ts` without moving production behavior into test utilities.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/view/PhysicsPaintCanvasStack.runtime.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Same-key parent rerenders preserve one child/engine instance and current callback behavior.
    - A key change produces exactly one old cleanup and one new mounted instance.
    - A dimension change with the same key reconfigures observer/sizing behavior without replacing the engine instance.
    - Runtime suites, not regular-expression assertions, support all lifecycle acceptance claims.
    - No package, environment, Vitest config, production hook, or effect is added.
  </acceptance_criteria>
  <done>The persistent Studio/CanvasMount boundary has executable durability coverage for reuse, freshness, replacement, reconfiguration, and cleanup (38.1-D-09, R-D-07, R-D-08).</done>
</task>

</tasks>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID/item | Coverage | Status | Notes |
|---|---|---|---|---|
| GOAL | Preserve the approved timing architecture while closing reopened correctness gaps | 38.1-13 through 38.1-17 | COVERED | Gap plans repair pixels/state and strengthen tests without changing timing ownership. |
| REQ | No formal Phase 38.1 requirement IDs are registered | decision contracts in each plan frontmatter | COVERED | Continues the phase's documented planning-data convention without inventing IDs. |
| REVIEW/VERIFICATION | Cached playback composition blocker | 38.1-13 | COVERED | Transparent and independent texture/grain behavior. |
| REVIEW/VERIFICATION | Completed physics session mode blocker | 38.1-14 | COVERED | Wet-buffer finalization, metadata, restoration, replay. |
| REVIEW/VERIFICATION | Capacity-only replacement blocker | 38.1-15 | COVERED | Capacity, cursor, projection, revisions, visible notification. |
| REVIEW/VERIFICATION | Restricted profiler storage warning | 38.1-16 | COVERED | Throwing getter and `getItem()`. |
| REVIEW/VERIFICATION | Source-only persistent-canvas warning | 38.1-17 | COVERED | Runtime rerender, callback, key, dimension, cleanup tests. |
| CONTEXT | 38.1-D-07 deterministic instrumentation | 38.1-16 | COVERED | Optional profiler remains deterministic and non-fatal. |
| CONTEXT | 38.1-D-09 timing-only exact behavior preservation | 38.1-13, 38.1-14, 38.1-15, 38.1-17 | COVERED | Every failed behavior-preservation path receives executable coverage. |
| REOPEN | R-D-02 existing profiler extension | 38.1-16 | COVERED | Existing API retained; gate fails closed. |
| REOPEN | R-D-07 persistent CanvasMount/Efx isolation | 38.1-17 | COVERED | Runtime durability replaces source-only confidence. |
| REOPEN | R-D-08 pixel and behavior invariants | 38.1-13, 38.1-14, 38.1-15, 38.1-17 | COVERED | All three blockers plus lifecycle durability. |
| RESEARCH | No packages/config changes; Node `vitest run`; Preact-native/no effect sprawl | all new plans | COVERED | Existing stack and test patterns only. |
| CONTEXT deferred | Synthetic stress fixture, timing-budget CI, cache-footprint work | excluded | EXCLUDED | Explicitly deferred/out of scope. |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Parent Studio props → persistent CanvasMount callbacks | Stable function identities must still execute current parent behavior. |
| Preact key/dimension semantics → engine lifecycle | Structural changes decide reuse, reconfiguration, or teardown. |
| Test harness → lifecycle claims | The harness must execute hooks/effects rather than infer behavior from source text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38.1-17-01 | Tampering | stable callback forwarders | high | mitigate | Rerender one mounted runtime and invoke captured child callbacks to prove latest-parent behavior. |
| T-38.1-17-02 | Denial of Service | observer/rAF/engine cleanup | high | mitigate | Test same-key reuse, dimension cleanup/reinstall, keyed teardown, and idempotent unmount counts. |
| T-38.1-17-03 | Repudiation | source-only lifecycle evidence | medium | mitigate | Base acceptance on executed component/effect tests; retain regex tests only for explicit architecture constraints. |
| T-38.1-17-SC | Tampering | package supply chain/test environment | high | mitigate | Add no packages or configuration; use the existing Node Vitest runtime and project-native mocked-hook pattern. |
</threat_model>

<verification>
- `pnpm -C app exec vitest run src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts src/components/physic-paint/view/PhysicsPaintCanvasStack.runtime.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/rotoPlaybackBackground.test.ts src/lib/rotoFrameDraw.test.ts src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts src/stores/physicPaintStore.test.ts` passes.
- `pnpm -C packages/efx-physic-paint vitest run src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts` passes.
- `pnpm -C app exec tsc --noEmit`, `pnpm -C packages/efx-physic-paint check`, and `pnpm -C app build` pass.
- The executor does not launch the server, browser, or native runtime. Phase verification decides whether any targeted user-owned native recheck is required by the material pixel changes; the already approved render-count matrix and 33-step script are not automatically reopened.
</verification>

<success_criteria>
- All five VERIFICATION/REVIEW items are covered by Plans 13-17 with no deferred mandatory item.
- Persistent-canvas guarantees are proven by executed component behavior in the existing test environment.
- No hook/effect sprawl, dependency, test configuration, launch, or environment change is introduced.
- Focused gap suites, package/app typechecks, and production build pass before re-running Phase 38.1 verification.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-17-SUMMARY.md` when done.
</output>
