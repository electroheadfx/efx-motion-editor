---
phase: 38.1-studio-render-path-performance
plan: 18
type: execute
wave: 11
depends_on:
  - 38.1-13
  - 38.1-14
  - 38.1-15
  - 38.1-16
  - 38.1-17
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - 38.1-D-06
  - 38.1-D-07
  - 38.1-D-09
  - R-D-04
  - R-D-07
  - R-D-08
  - R-D-09
  - R-D-10
must_haves:
  truths:
    - "All focused gap suites, app/package typechecks, and the app production build pass before native acceptance begins (R-D-09, R-D-10)."
    - "User-owned native proof confirms cached playback transparency and deliberately different background texture, paper grain, and grain strength produce the intended visible composite (38.1-D-09, R-D-08)."
    - "User-owned native proof confirms `last`-mode stop/finalization and replay preserve unrelated wet pixels while using the completed mode (38.1-D-09, R-D-08)."
    - "User-owned native proof confirms a capacity-only replacement clamps the cursor, publishes visibly, and projects the requested extent (38.1-D-09, R-D-08)."
    - "The real Preact runtime, not a test-owned registry, proves same-key CanvasMount/Efx persistence, latest callback behavior, dimension reconfiguration without engine replacement, changed-key replacement, and exactly-once cleanup (R-D-07, R-D-08)."
    - "The approved bidirectional render/lifecycle counter matrix remains within threshold after the gap fixes; Phase 38-06 stays blocked until explicit acceptance (R-D-04, R-D-10)."
  artifacts:
    - path: .planning/phases/38.1-studio-render-path-performance/38.1-18-SUMMARY.md
      provides: "Focused automated results, targeted native verdict, counter/lifecycle matrix evidence, and owner-routed rejection or approval"
  key_links:
    - from: "Plans 13-17 focused suites"
      to: "Plan 18 native checkpoint"
      via: "all automated preconditions must pass before user-owned runtime work"
      pattern: "vitest run"
    - from: "native pixel/state/lifecycle rejection"
      to: "smallest owning gap plan"
      via: "cached compositor -> 13; physics finalization/replay -> 14; capacity publication -> 15; Preact lifecycle -> 17"
      pattern: "38.1-(13|14|15|17)"
    - from: "explicit user approval"
      to: "Phase 38 Plan 38-06 eligibility"
      via: "blocking resume signal; no implicit or automated unblocking"
      pattern: "approved"
---

<objective>
Run the complete post-gap automated gate, then obtain targeted user-owned native acceptance for the behavior and Preact lifecycle surfaces changed or newly evidenced by Plans 13-17 (38.1-D-06/D-09, R-D-04/R-D-07/R-D-08/R-D-09/R-D-10).

Purpose: Material pixel, engine, timeline-publication, and lifecycle evidence changed after Plan 12 approval. The phase cannot unblock Phase 38-06 until those affected surfaces and the localized-render architecture are accepted again in the real native runtime.
Output: A focused automated record and a blocking native approval or smallest-owner rejection in `38.1-18-SUMMARY.md`; no production or test file changes.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md
@.planning/phases/38.1-studio-render-path-performance/38.1-13-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-14-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-15-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-16-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-17-SUMMARY.md
</context>

## Artifacts this phase produces

- Production source: none.
- Tests: none; this plan executes the focused suites created or extended by Plans 13-17.
- Test support: none.
- Documentation: `38.1-18-SUMMARY.md` records automated results and the user-owned native verdict.
- Configuration: none.
- Dependencies: none.
- Persisted data or migrations: none.
- Acceptance evidence: targeted visible behavior, actual Preact reconciliation/lifecycle, and the applicable approved bidirectional counter matrix.

<tasks>

<task type="auto">
  <name>Task 1: Run all focused post-gap automated preconditions</name>
  <files>None</files>
  <read_first>
    - .planning/phases/38.1-studio-render-path-performance/38.1-13-SUMMARY.md — cached-playback compositor implementation and focused command result
    - .planning/phases/38.1-studio-render-path-performance/38.1-14-SUMMARY.md — completed-mode finalization/replay implementation and engine command result
    - .planning/phases/38.1-studio-render-path-performance/38.1-15-SUMMARY.md — capacity-only publication implementation and focused command result
    - .planning/phases/38.1-studio-render-path-performance/38.1-16-SUMMARY.md — fail-closed profiler implementation and focused command result
    - .planning/phases/38.1-studio-render-path-performance/38.1-17-SUMMARY.md — bounded Node hook/effect evidence and explicit native-only claims
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md:62-68 — app-owned engine Vitest root, no-launch rule, and renewed acceptance sequencing
  </read_first>
  <action>
    Confirm Plans 13-17 SUMMARYs exist and the working tree is clean. Run every focused app gap suite, the repository-accurate app-owned engine suite with `--root ../packages/efx-physic-paint`, app TypeScript no-emit, package `check`, and the app production build. Do not launch the server, browser, or native app. Do not edit files inside this acceptance plan to make a gate pass.

    Route an automated failure before native work: playback/compositor -> Plan 13; engine finalization/replay -> Plan 14; capacity/store -> Plan 15; restricted profiler storage -> Plan 16; hook/effect runtime or architecture checks -> Plan 17. Stop on the first failing command and keep Phase 38-06 blocked.
  </action>
  <verify>
    <automated>test -f .planning/phases/38.1-studio-render-path-performance/38.1-13-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-14-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-15-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-16-SUMMARY.md && test -f .planning/phases/38.1-studio-render-path-performance/38.1-17-SUMMARY.md && test -z "$(git status --short)" && pnpm -C app exec vitest run src/components/physic-paint/view/rotoPlaybackBackground.test.ts src/lib/rotoFrameDraw.test.ts src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts src/stores/physicPaintStore.test.ts src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts src/components/physic-paint/engine/EfxPaintCanvas.lifecycle.test.ts src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm -C app exec vitest run --root ../packages/efx-physic-paint src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts && pnpm -C app exec tsc --noEmit && pnpm -C packages/efx-physic-paint check && pnpm -C app build && test -z "$(git status --short)"</automated>
  </verify>
  <acceptance_criteria>
    - Plans 13-17 SUMMARYs exist and identify completed owning work.
    - Every focused app and engine suite passes with `vitest run`.
    - App TypeScript, package `check`, and app production build pass.
    - The working tree is clean before and after commands.
    - No server, browser, or native runtime is launched by the agent.
    - Any failure is routed to Plan 13, 14, 15, 16, or 17 and blocks the checkpoint.
  </acceptance_criteria>
  <done>All focused gap automation, both typecheck surfaces, and the production build are green on a clean tree; native acceptance may begin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>BLOCKING user-owned native acceptance: affected pixels, physics, capacity, real Preact lifecycle, and localized counters</name>
  <precondition>Task 1 is green on a clean tree; Plans 13-17 are implemented; the user has the native app running on their side with the existing development profiler gate available.</precondition>
  <read_first>
    - .planning/phases/38.1-studio-render-path-performance/38.1-13-SUMMARY.md — exact playback background cases and any native setup notes
    - .planning/phases/38.1-studio-render-path-performance/38.1-14-SUMMARY.md — completed `last`-mode finalization/replay behavior and available user workflow
    - .planning/phases/38.1-studio-render-path-performance/38.1-15-SUMMARY.md — capacity-only replacement trigger and observable cursor/projection publication
    - .planning/phases/38.1-studio-render-path-performance/38.1-17-SUMMARY.md — behaviors proven in Node versus claims reserved for native Preact
    - .planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md — shipped profiler API and final counter names
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md:85-95 — warm-up and native snapshot protocol
  </read_first>
  <what-built>
    Plans 13-17 repaired cached playback composition, completed-mode physics finalization/replay, capacity-only physical publication, restricted-storage profiling, and bounded CanvasMount hook/effect coverage. This checkpoint changes nothing; it obtains real native evidence for the affected visible behavior and Preact-owned lifecycle.
  </what-built>
  <action>
    Present the targeted script below to the user and WAIT. The agent must not launch the server, browser, or native app and must not patch a rejection inside this checkpoint. Do not automatically reopen the unrelated Phase 38 33-step script; test only the affected native surfaces plus the counter/lifecycle matrix required by the Phase 38.1 goal.

    A — CACHED PLAYBACK PIXEL PARITY (PLAN 13)
    1. Open a cached Roto playback project whose background metadata is transparent. Start cached playback across at least one cached frame and confirm the playback background stays transparent: no white or opaque fallback appears before, during, or after stop.
    2. Use a project/frame with deliberately different metadata values for background texture, paper grain, and grain strength (for example background texture `canvas2`, paper grain `canvas3`, strength `0.65`). Confirm cached playback visibly shows the selected background texture plus the distinct grain overlay/strength, matching preview/export for the same frame.
    3. Switch between the transparent and textured cases, stop playback, and close/reopen the surface once. Confirm no stale raster callback repaints the previous background after replacement or teardown.

    B — `last` PHYSICS FINALIZATION AND REPLAY (PLAN 14)
    4. Create or load a Physics Paint state with unrelated wet pixels outside the next stroke's affected region. Run a `last`-mode physics session for the target stroke and stop it.
    5. Confirm the target stroke finalizes while the unrelated wet pixels remain visibly intact; stopping must not replace or dry the unrelated saved wet region.
    6. Exercise the existing replay/redraw path for that recorded action, then save/reopen if that is part of the normal replay workflow. Confirm replay reproduces the completed `last` behavior rather than the prior restored mode, and the engine returns to the prior mode after stop.

    C — CAPACITY-ONLY STRUCTURAL PUBLICATION (PLAN 15)
    7. In the existing timeline/project duration workflow, establish a Physics Paint physical timeline whose current cursor is beyond a smaller valid target capacity. Change only the capacity/duration while keeping the real-key records and interpolation settings unchanged.
    8. Confirm the cursor clamps to the new upper bound immediately, the visible timeline publishes the change without another edit, and no cells remain beyond the requested extent.
    9. Restore the larger capacity, then apply the exact same records/interpolation/capacity tuple again. Confirm the visible projection remains stable and no duplicate publication/flicker occurs.

    D — ACTUAL PREACT RECONCILIATION AND CANVASMOUNT/EFX OWNERSHIP (PLAN 17 NATIVE ORACLE)
    10. Enable the existing development profiler and clear it. Warm the current surface with one adjacent committed navigation in each direction.
    11. With the same `canvasKey` and dimensions, navigate forward and backward and trigger a parent callback-prop refresh through an existing Studio action. Confirm the same CanvasMount/Efx instance persists while the action uses the latest callback behavior; there is no stale callback, remount, engine-ready, pre-destroy, or observer churn.
    12. Change width or height through the existing dimension-changing project workflow while keeping the same `canvasKey`. Confirm contained sizing and observer ownership reconfigure, the current engine/canvas content persists, and there is no engine replacement.
    13. Switch to a different Physics Paint layer/project so `canvasKey` changes. Confirm exactly one old pre-destroy/cleanup path and one replacement Efx request/engine-ready path, with the correct new canvas visible.
    14. Close/unmount that replacement once. Confirm active ResizeObserver/rAF/engine cleanup occurs once; no duplicate cleanup counters, stale callback, or post-unmount repaint appears.

    E — APPROVED BIDIRECTIONAL LOCALIZED RENDER/LIFECYCLE MATRIX
    15. Return to a stable same-key/same-dimension surface: Play Script dialog closed, playback stopped, no brush/input mutation active, and no overlay/input-lock semantic change.
    16. Clear the profiler, warm one adjacent committed navigation forward and reverse, then capture before/after snapshots for exactly one forward and one reverse adjacent committed navigation after rAF settlement.
    17. For BOTH directions, verify the final Plan 08 counter names satisfy this matrix:

    | Surface | Required delta |
    |---|---:|
    | PhysicsPaintTopBar implementation | 0 |
    | PhysicsPaintToolRail implementation | 0 |
    | entire right-panel region shell/toggle | 0 |
    | PhysicsPaintRightPanel implementation | 0 |
    | closed PhysicsPaintPlayScriptDialog | 0 |
    | PhysicsPaintCanvasStack with unchanged overlay/input semantics | 0 |
    | PhysicsPaintCanvasMount | 0 |
    | app-owned Efx child request | 0 |
    | CanvasMount engine-ready callback | 0 |
    | CanvasMount pre-destroy callback | 0 |
    | static Workflow chrome | 0 |
    | timeline ResizeObserver install | 0 |
    | timeline ResizeObserver cleanup | 0 |
    | CanvasStack ResizeObserver install/cleanup | 0 |
    | CanvasStack MutationObserver install/cleanup | 0 |
    | CanvasMount ResizeObserver install/cleanup | 0 |
    | app engine-lifecycle setup/cleanup | 0 |
    | RotoTimelineCellButton implementation | <= 3 |

    18. Report PASS/FAIL for sections A-E and the forward/reverse delta objects. Approval requires every targeted behavior and both counter directions to pass.

    Rejection routing:
    - Cached transparency, texture/grain composition, or stale raster callback -> Plan 13.
    - `last` stop/finalization, unrelated wet pixels, restored mode, or replay metadata/behavior -> Plan 14.
    - Capacity-only cursor clamp, visible publication, no-op stability, or projected extent -> Plan 15.
    - Same-key persistence, latest callbacks, dimension reconfiguration without replacement, changed-key replacement, cleanup counts, or CanvasMount/Efx counter rows -> Plan 17.
    - A non-Canvas static counter regression introduced by the playback StudioView change -> Plan 13 first; keep Phase 38-06 blocked while the smallest changed owner is confirmed.

    Record `approved` only after sections A-E pass. On any rejection, record the failing section/step, observed pixels/state/counters, and owning plan; stop without unblocking Phase 38-06.
  </action>
  <how-to-verify>
    The user runs sections A-E in the native app they already launched. Visible pixels/state must match the targeted expectations; actual Preact instance ownership must match steps 11-14; both navigation directions must satisfy every counter threshold. The user returns explicit approval or precise failing steps with observed deltas/screenshots where useful.
  </how-to-verify>
  <verify>
    <automated>test -z "$(git status --short)"</automated>
    <human-check>The user explicitly approves sections A-E and supplies the forward/reverse counter verdicts, or reports a rejection routed to Plan 13, 14, 15, or 17.</human-check>
  </verify>
  <acceptance_criteria>
    - Transparent cached playback remains transparent; deliberately different background texture, paper grain, and grain strength are visibly honored.
    - `last`-mode stop/finalization and replay preserve unrelated wet pixels and use the completed mode before restoring the prior mode.
    - Capacity-only replacement clamps the cursor, publishes visibly, and projects the exact requested extent; an exact repeated tuple remains stable.
    - Real Preact reconciliation preserves same-key CanvasMount/Efx ownership, reaches latest callbacks, reconfigures dimensions without engine replacement, replaces on changed key, and cleans once.
    - Both navigation directions satisfy every applicable approved render/lifecycle threshold.
    - The agent launches no runtime and changes no file.
    - A rejection names the smallest owning Plan 13, 14, 15, or 17 surface and Phase 38-06 remains blocked.
  </acceptance_criteria>
  <done>The user explicitly approved all targeted post-gap native behavior and the bidirectional localized-render/lifecycle matrix, or a precise owner-routed rejection is recorded; Phase 38-06 is not unblocked without approval.</done>
  <resume-signal>Type `approved` with PASS confirmation for sections A-E and the forward/reverse counter verdicts, or list the failing section/step and observed result.</resume-signal>
</task>

</tasks>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID/item | Coverage | Status | Notes |
|---|---|---|---|---|
| GOAL | Renewed user-owned native proof before unblocking Phase 38-06 | 38.1-18 | COVERED | Automated preconditions precede a blocking targeted native checkpoint. |
| REQ | No formal Phase 38.1 requirement IDs are registered | decision contracts in frontmatter/tasks | COVERED | Preserves the documented spec-less fallback; no requirement record or probe predicate is fabricated. |
| VERIFICATION | Cached playback composition blocker | 38.1-13, 38.1-18 A | COVERED | Automated plus visible transparent/different-metadata proof. |
| VERIFICATION | Completed physics mode blocker | 38.1-14, 38.1-18 B | COVERED | Automated plus native stop/finalization/replay proof. |
| VERIFICATION | Capacity-only replacement blocker | 38.1-15, 38.1-18 C | COVERED | Automated plus visible cursor/publication/extent proof. |
| VERIFICATION | Restricted profiler storage warning | 38.1-16, 38.1-18 Task 1 | COVERED | Focused non-throwing tests are required before native work. |
| VERIFICATION | Persistent-canvas evidence warning | 38.1-17, 38.1-18 D | COVERED | Node claims are bounded; real Preact owns native persistence/replacement proof. |
| CONTEXT | 38.1-D-09 timing-only exact behavior | 38.1-18 A-E | COVERED | Changed behavior surfaces and timing localization are both reaccepted. |
| REOPEN | R-D-04 exact render thresholds | 38.1-18 E | COVERED | Approved matrix is sampled in both directions. |
| REOPEN | R-D-07 persistent CanvasMount/Efx isolation | 38.1-18 D-E | COVERED | Same key, latest callback, dimensions, changed key, cleanup, counters. |
| REOPEN | R-D-09 no packages/config/agent launches | 38.1-18 Tasks 1-2 | COVERED | Existing commands only; user owns native runtime. |
| REOPEN | R-D-10 renewed acceptance sequencing | 38.1-18 | COVERED | Phase 38-06 stays blocked until explicit approval. |
| CONTEXT deferred | Synthetic stress fixture, timing-budget CI, cache-footprint work | excluded | EXCLUDED | Explicitly deferred and not reopened. |
| Unrelated native scope | Full Phase 38 33-step script | excluded from this checkpoint | EXCLUDED | Already approved before the gap changes; only affected surfaces plus the required counter/lifecycle matrix are rerun. |
</source_coverage_audit>

<threat_model>
## ASVS L1 Scope

No authentication, session, network, or new untrusted-input surface is introduced. ASVS L1 integrity/availability controls apply to native acceptance sequencing, project pixel/state integrity, and lifecycle resource ownership.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Automated gap evidence -> native checkpoint | Native work must not begin while any focused suite/typecheck/build is red. |
| Persisted project metadata/engine state -> visible pixels | Background, wet buffers, completed mode, and capacity determine user-visible output. |
| Preact reconciler -> CanvasMount/Efx lifecycle | Real key/memo reconciliation decides persistence, replacement, and cleanup. |
| User verdict -> Phase 38-06 eligibility | Explicit targeted approval is the only unblock signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38.1-18-01 | Tampering | post-gap acceptance sequencing | high | mitigate | Require every focused suite, both typecheck surfaces, build, clean tree, then blocking native acceptance before any unblock. |
| T-38.1-18-02 | Repudiation | visible pixel/state verdict | high | mitigate | Record section/step verdicts for transparency, independent grain metadata, physics preservation/replay, and capacity publication. |
| T-38.1-18-03 | Repudiation | Preact reconciliation claim | high | mitigate | Use the actual user-owned native runtime for same-key persistence, dimension reconfiguration, changed-key replacement, latest callbacks, and cleanup counts. |
| T-38.1-18-04 | Denial of Service | observer/rAF/engine lifecycle | high | mitigate | Recheck bidirectional zero-churn counters plus controlled replacement and exactly-once cleanup. |
| T-38.1-18-05 | Tampering | rejection routing/unblocking | high | mitigate | Route failures to Plan 13/14/15/17, stop execution, and explicitly keep Phase 38-06 blocked. |
| T-38.1-18-SC | Tampering | package supply chain/test environment | high | mitigate | No installs or config changes; use existing app-owned Vitest commands and user-owned native runtime only. |
</threat_model>

<verification>
- Automated: all focused Plan 13-17 app suites; repository-accurate app-owned engine suite with `--root ../packages/efx-physic-paint`; app TypeScript; package `check`; app build; clean tree.
- Native pixels/state: cached transparency and deliberately different texture/grain/strength; `last` finalization/replay with unrelated wet pixels; capacity-only clamp/publication/extent.
- Native Preact lifecycle: same-key persistence, latest callbacks, same-key dimension reconfiguration without engine replacement, changed-key replacement, exactly-once cleanup.
- Native localization: approved bidirectional render/lifecycle counter matrix, with no unrelated 33-step regression script reopened.
- Process: agent launches no runtime; user owns the checkpoint; rejection routes to Plan 13, 14, 15, or 17; Phase 38-06 remains blocked until approval.
</verification>

<success_criteria>
- Every focused gap suite, app/package typecheck, and app build passes.
- The user explicitly approves targeted cached-playback, physics-finalization/replay, and capacity-publication behavior.
- The user explicitly approves real Preact CanvasMount/Efx persistence, replacement, reconfiguration, callback freshness, and cleanup.
- Both navigation directions retain the approved localized render/lifecycle thresholds.
- No packages, test config, server/browser/native agent launch, compatibility shim, or unrelated native scope is introduced.
- Only explicit Plan 18 approval unblocks Phase 38 Plan 38-06.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-18-SUMMARY.md` when done. Record Task 1 command results; section A-E verdicts; forward/reverse counter verdicts; actual same-key/dimension/key-change/cleanup observations; explicit approval or precise rejection with Plan 13/14/15/17 routing; and confirmation that the agent launched no runtime and changed no production/test files.
</output>
