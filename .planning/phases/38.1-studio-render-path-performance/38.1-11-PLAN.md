---
phase: 38.1-studio-render-path-performance
plan: 11
type: execute
wave: 7
depends_on:
  - 38.1-10
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx
  - app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts
  - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts
autonomous: true
requirements:
  - 38.1-D-03
  - 38.1-D-04
  - 38.1-D-05
  - 38.1-D-07
  - 38.1-D-09
  - R-D-04
  - R-D-05
  - R-D-07
  - R-D-08
  - R-D-09

must_haves:
  truths:
    - "After warm-up, ordinary adjacent committed frame navigation with unchanged overlay/input-lock/playback/reference semantics produces zero CanvasStack render, CanvasMount render, app-owned Efx child request, engine-ready/pre-destroy callback, CanvasStack/CanvasMount observer install/cleanup, and app engine-lifecycle setup/cleanup delta (R-D-04/R-D-07)."
    - "A memoized PhysicsPaintCanvasStack region has zero delta for ordinary navigation when overlay/input-lock/playback/reference semantics are unchanged, and renders immediately when any such semantic input changes; isolation never delays canvas-first paint or latest-wins navigation (38.1-D-03/D-05; R-D-07/R-D-08)."
    - "A changed `canvasKey` still replaces the mount/Efx engine, and changed width or height still re-renders/reconfigures the persistent boundary and refreshes its display-size observer behavior (R-D-07)."
    - "Callbacks crossing into CanvasMount/Efx are referentially stable and read current engine/controllers through existing refs, preserving save-before-leave, completed-mutation, native-pen, performance, and destroy behavior (R-D-05/R-D-08)."
    - "The CanvasMount plain implementation remains directly available; StudioView uses a dedicated compat memo wrapper; no package/environment/launch change is introduced (R-D-05/R-D-09)."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx"
      provides: "Memoized CanvasStack region that owns the stable keyed mount child without a fresh children vnode"
    - path: "app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts"
      provides: "Dedicated compat memo boundary around the plain persistent mount"
    - path: "app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx"
      provides: "Stable internal Efx callback forwarding through current refs"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Identity-stable CanvasMount prop assembly and ref-reading callbacks"
    - path: "app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts"
      provides: "Plain/wrapper, stable-callback, observer, canvasKey, and dimensions source contracts"
  key_links:
    - from: "PhysicsPaintStudio.tsx mount props memo"
      to: "MemoizedPhysicsPaintCanvasMount"
      via: "stable dimensions/callback props; frame navigation values excluded"
      pattern: "canvasMountPropsMemo.resolve"
    - from: "PhysicsPaintStudio identity-stable canvas region inputs"
      to: "memoized PhysicsPaintCanvasStack implementation"
      via: "overlay/input-lock/playback/reference props plus stable canvasKey/mount props; no children vnode prop"
      pattern: "MemoizedPhysicsPaintCanvasStack"
    - from: "memoized PhysicsPaintCanvasStack"
      to: "MemoizedPhysicsPaintCanvasMount"
      via: "region instantiates the persistent keyed child internally"
      pattern: "key={canvasKey}"
    - from: "PhysicsPaintCanvasMount callback refs"
      to: "EfxPaintCanvas optional callbacks"
      via: "stable forwarding functions read latest callback refs"
      pattern: "Ref.current"
---

<objective>
Isolate the persistent `PhysicsPaintCanvasMount` and Efx child from frame-only Studio/View orchestration while keeping CanvasStack overlays live and preserving controlled replacement on `canvasKey` or dimensions (R-D-07).

Purpose: The Efx engine effect is mount-only, but parent component execution and unstable callback props still make the persistent boundary participate in navigation. This separate gate ensures render localization cannot accidentally alter engine lifecycle.

Output: dedicated CanvasMount memo wrapper, identity-stable mount props/callback forwarding, source contracts, and automated replacement/lifecycle invariants ready for Plan 12 native counters.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-10-SUMMARY.md
@app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx
@app/src/components/physic-paint/engine/usePhysicsPaintEngineLifecycle.ts
@packages/efx-physic-paint/src/preact.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (RED then GREEN): add stable CanvasStack region and persistent CanvasMount boundaries</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts, app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — `canvas` view-model assembly, inline `mount.onEngineReady`/`onCompletedMutation`, lifecycle refs/controllers, `canvasKey`, width, height, and performance callback.
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx — CanvasStack live props and keyed `PhysicsPaintCanvasMount` child.
    - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx — plain component signature and current dimension observer/Efx mount.
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts — established per-instance identity memo helper.
    - app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts — dedicated wrapper pattern.
  </read_first>
  <behavior>
    - Frame-only Studio changes with unchanged overlay/input-lock/playback/reference semantics return shallow-equal CanvasStack and CanvasMount props, skipping both implementations.
    - Overlay/input-lock/playback/reference changes fail CanvasStack shallow equality and update its existing live DOM immediately.
    - CanvasStack receives stable `canvasKey` and mount props instead of a freshly created `children` vnode, and instantiates the memoized mount child internally.
    - `canvasKey` remains the Preact key on the memoized mount child; a new key forces replacement.
    - Width, height, or paperTextureScale changes fail shallow equality and reach CanvasMount.
  </behavior>
  <action>
    RED: create `PhysicsPaintCanvasMount.test.ts` as a node-environment source-contract suite requiring a plain CanvasMount export, a separate memo wrapper module, a memoized CanvasStack implementation, no `children` prop crossing the CanvasStack memo boundary, and the wrapper mounted as the keyed child created inside CanvasStack. Require dimensions/paper scale in mount identity dependencies, overlay/input-lock/playback/reference semantics in CanvasStack identity dependencies, and frame navigation values excluded from both. Extend Studio source contracts for `canvasStackPropsMemo.resolve`, `canvasMountPropsMemo.resolve`, and stable named callbacks. Confirm failure before implementation.

    GREEN per R-D-07: keep `PhysicsPaintCanvasMount` plain and create `MemoizedPhysicsPaintCanvasMount.ts` using the same dedicated `preact/compat` wrapper pattern as the RightPanel. Refactor the private CanvasStack API so it receives stable `canvasKey` and mount props and instantiates the keyed memoized mount internally; do not pass a freshly created child vnode. Wrap the private CanvasStack implementation with compat memo while keeping its existing overlay, keyboard/pointer lock, canvas bounds, ResizeObserver, and MutationObserver behavior/markup exact. Keep the Plan 08 `render.canvasStack` counter at the implementation-body top.

    In Studio, hold per-instance `canvasStackPropsMemo` and `canvasMountPropsMemo` instances. CanvasStack dependencies enumerate only cached-reference URL, playback Signal/object state, identity-stable playback composition, input-disabled state/message, stable input-intent callback, identity-stable onion overlay, canvasKey, and stable mount props. CanvasMount dependencies include width, height, paperTextureScale, and every real callback/service input, but no current/start frame or navigation generation primitive. Replace inline mount callbacks with named `useCallback` functions. Where behavior needs current mutable controllers/engine/context, read existing refs at call time; do not add effects that mirror state into new refs unless no current ref exists, and if one is unavoidable keep it focused with cleanup. Preserve callback behavior and ordering exactly (R-D-08).
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm -C app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Plain implementation and dedicated wrapper source contracts pass.
    - StudioView mounts a memoized CanvasStack with stable semantic props; CanvasStack owns the memoized mount child internally and retains `key={canvasKey}`.
    - Mount dependencies include dimensions/paper scale and exclude frame cursor/navigation values.
    - Focused tests and typecheck pass without package, CSS, config, or environment changes.
  </acceptance_criteria>
  <done>Frame-only orchestration skips the persistent mount while overlay/input-lock updates and controlled key/dimension changes remain reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stabilize CanvasMount-to-Efx callback forwarding and lock lifecycle/replacement behavior</name>
  <files>app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx, app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx — inline Efx `onEngineReady`, dimension observer effect, mount rAF check, callbacks and cleanup.
    - packages/efx-physic-paint/src/preact.tsx — read-only reference for existing completed-mutation/performance/before-destroy refs and the mount-only engine effect; package source remains unchanged.
    - app/src/components/physic-paint/engine/usePhysicsPaintEngineLifecycle.ts — canvasKey reset and external state cleanup that must still occur on controlled replacement/unmount.
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts — Plan 08 counter names used by Plan 12.
  </read_first>
  <behavior>
    - CanvasMount passes stable function identities to Efx across any local rerender with unchanged structural props.
    - Stable forwarding functions invoke the latest parent callbacks for engine ready, canvas mounted, native pen input, completed mutation, performance/lifecycle samples, pre-destroy, and stroke metadata.
    - Dimension changes still run the current display-size calculation/observer contract; unmount/key replacement still disconnects the observer and destroys/cleans the Efx lifecycle once.
    - Adjacent navigation produces no app-owned child request, engine-ready/pre-destroy callback, lifecycle, or observer churn; controlled key replacement produces exactly one old pre-destroy path and one new child-request/engine-ready path.
  </behavior>
  <action>
    RED: expand `PhysicsPaintCanvasMount.test.ts` with source contracts for callback refs updated during render, stable forwarding callbacks created once per mount, the existing dimension effect dependencies, and the keyed replacement path. Keep Plan 08 counter ownership assertions. The tests must fail while the Efx `onEngineReady` prop is inline.

    GREEN: mirror the package's established callback-ref pattern inside the plain CanvasMount. Store current parent callbacks in refs updated during render, and pass stable forwarding callbacks to `EfxPaintCanvas`; the engine-ready forwarder still sets the tool, clears mount error, reports mounted, and calls the current parent handler in the exact existing order. Preserve existing dimension observer dependencies `[props.height, props.width]`, initial rAF mount check, error copy, cleanup order, and package mount-only effect. Do not move paint/navigation work into this component and do not change package engine semantics.

    Run focused source contracts, existing engine/canvas lifecycle tests, the full app suite, typecheck, and build. Plan 12—not this plan—owns the native zero-delta and controlled-remount observations.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/hooks/useRotoReferenceController.test.ts && (pnpm -C app exec vitest run --reporter=json --outputFile=/tmp/38.1-11-known-red.json >/tmp/38.1-11-known-red.log 2>&1; suite_exit=$?; SUITE_EXIT=$suite_exit node --input-type=module -e 'import fs from "node:fs"; const report=JSON.parse(fs.readFileSync("/tmp/38.1-11-known-red.json","utf8")); const expected=["PhysicsPaintWorkflowStrip clipping guard contract (36.15-08, UAT Gap B) places header tooltips below their anchors so no header control is masked by the canvas","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) ignores blank candidates and keeps higher-priority content over blank lines","getRotoStatusCapsuleViewModel (36.15-05 status capsule, D-15) returns the ambient baseline when nothing higher-priority exists"].sort(); const expectedFiles=["PhysicsPaintWorkflowStrip.test.ts","physicsPaintWorkflowPresentation.test.ts"].sort(); const actual=report.testResults.flatMap((file)=>file.assertionResults??[]).filter((item)=>item.status==="failed").map((item)=>item.fullName).sort(); const failedFiles=report.testResults.filter((file)=>file.status==="failed").map((file)=>file.name.split("/").pop()).sort(); const runtimeMessages=report.testResults.filter((file)=>String(file.message??"").trim()!=="").map((file)=>({name:file.name,message:file.message})); if(process.env.SUITE_EXIT!=="1"||report.numFailedTests!==3||JSON.stringify(actual)!==JSON.stringify(expected)||JSON.stringify(failedFiles)!==JSON.stringify(expectedFiles)||runtimeMessages.length!==0){console.error({suiteExit:process.env.SUITE_EXIT,numFailedTests:report.numFailedTests,actual,failedFiles,runtimeMessages});process.exit(1)}') && pnpm -C app exec tsc --noEmit && pnpm -C app build</automated>
  </verify>
  <acceptance_criteria>
    - CanvasMount source contracts prove stable Efx callback identities with latest-ref behavior and exact engine-ready order.
    - Dimension observer and keyed lifecycle cleanup/init contracts remain present and counter-instrumented.
    - Focused lifecycle/reference coverage passes; the JSON full-suite gate reports exactly the three named sanctioned Phase 38 failures and no others; typecheck and build pass.
    - No changes exist outside listed files; no server/browser/native launch or test-environment change occurs (R-D-09).
  </acceptance_criteria>
  <done>The persistent CanvasMount/Efx boundary is identity-stable on navigation and remains fully replaceable on key/dimension changes with exact callback/lifecycle behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio -> persistent CanvasMount | Live app controllers and mutation callbacks cross a memo boundary; stale closures could save or mutate the wrong frame. |
| CanvasMount -> Efx package | Engine-ready, mutation, performance, native-pen, and destroy callbacks cross the package boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-11-01 | Tampering | ref-reading callbacks | high | mitigate | Reuse existing authoritative refs/controllers, source-test latest-ref forwarding and engine-ready order, and run full save/navigation regressions. |
| T-38.1-11-02 | Denial of Service | accidental Efx remount | high | mitigate | Stable CanvasStack/CanvasMount memo boundaries, frame-free dependency lists, keyed replacement contract, and native zero child-request/engine-ready/pre-destroy deltas. |
| T-38.1-11-03 | Tampering | controlled replacement | medium | mitigate | Keep canvasKey as the child key; dimensions remain props/effect dependencies; native UAT separately changes key/dimensions and expects one cleanup/new init path. |
| T-38.1-11-04 | Tampering | canvas-first overlay path | medium | mitigate | CanvasStack stays inside its semantic memo boundary and remains live because overlay/input-lock/reference/playback prop changes invalidate that boundary; no navigation/paint ordering code moves. |
| T-38.1-11-SC | Tampering | package installs | high | mitigate | No installs or manifest changes (R-D-09). |
</threat_model>

<verification>
- `pnpm -C app exec vitest run src/components/physic-paint/engine/PhysicsPaintCanvasMount.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts`
- `pnpm -C app exec vitest run src/components/physic-paint/hooks/useRotoReferenceController.test.ts`
- Full app JSON reporter gate: exactly the three named sanctioned Phase 38 assertions fail; every other assertion passes.
- `pnpm -C app exec tsc --noEmit`
- `pnpm -C app build`
- Plan 12 native profiler gate: ordinary adjacent navigation yields zero mount/Efx/lifecycle/observer deltas; overlay/input-lock changes can render CanvasStack; controlled canvasKey/dimension changes still trigger the expected replacement/reconfiguration.
</verification>

<success_criteria>
- Persistent CanvasMount/Efx child is absent from ordinary frame-navigation render work.
- CanvasStack overlays remain live and canvas-first/latest-wins behavior is unchanged.
- Controlled key/dimension changes still reach and replace/reconfigure the persistent boundary.
- Stable callbacks always read current authoritative values.
- Automated gates pass; native numerical and replacement acceptance remains owned by Plan 12.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-11-SUMMARY.md` when done. Record the wrapper/key ownership, mount prop dependency table, callback-ref forwarding table, lifecycle/observer counter ownership, automated results, and pending Plan 12 native gates.
</output>
