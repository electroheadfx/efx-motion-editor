---
phase: 36.10-physics-paint-roto-missing-background-preview-export
plan: 04
type: execute
wave: 4
depends_on:
  - 36.10-03
files_modified:
  - app/src/lib/previewRenderer.ts
  - app/src/lib/previewRenderer.test.ts
  - app/src/lib/exportRenderer.test.ts
  - app/src/lib/physicPaintRotoDurableCore.test.ts
autonomous: true
requirements:
  - 36.10-MISSING-TRANSPARENT
  - 36.10-MISSING-BACKGROUND
  - 36.10-PREVIEW-EXPORT-PARITY
must_haves:
  truths:
    - "D-12/D-13: Preview and export produce the same missing-frame visual rule for transparent, background-only, leading, interior, and trailing cases."
    - "D-14/D-15: Preview/export rendering never mutates, replaces, or bakes paper/background pixels into real-key paint alpha caches."
    - "D-11: Existing real cached frames continue to render normally beside missing/background-only frames."
    - "Spec acceptance: frames after the last real Roto key render dynamically as background-only when paper mode is enabled and transparent when it is not, without cache growth."
  artifacts:
    - path: app/src/lib/previewRenderer.test.ts
      provides: "Renderer behavior/source-contract tests for missing-frame draw rules if existing test harness supports it"
    - path: app/src/lib/exportRenderer.test.ts
      provides: "Preview/export parity and no engine replay contracts"
    - path: app/src/lib/physicPaintRotoDurableCore.test.ts
      provides: "Durable Roto regression coverage ensuring real cached frames remain normal beside missing frames"
  key_links:
    - from: app/src/lib/exportRenderer.ts
      to: app/src/lib/previewRenderer.ts
      via: "renderGlobalFrame uses PreviewRenderer.renderFrame, so export inherits missing-frame resolver behavior"
      pattern: "renderer\.renderFrame\("
    - from: app/src/lib/previewRenderer.ts
      to: app/src/stores/physicPaintStore.ts
      via: "Preview reads cached real frames first and only resolves missing draw instruction when no rendered frame exists"
      pattern: "getFrame\(paintLayerId, paintLookupFrame\)"
---

## Phase Goal

**As a** stop-motion animator, **I want to** render missing Roto frames as transparent or paper-background-only frames, **so that** gaps between painted keys and trailing frames preview/export consistently without accidental paint content.

<objective>
Prove the preview/export parity slice and alpha-separation backstop for Phase 36.10 after resolver, store, and UI wiring exist.

Purpose: The user-facing promise is not just a resolver; live preview and exported PNG sequences must match without changing real-key paint alpha caches.
Output: Focused renderer/source-contract regressions showing shared preview/export behavior, real cached frames unchanged beside gaps, no export-time physics replay, and no real-key alpha mutation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-CONTEXT.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-RESEARCH.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-PATTERNS.md
@SPECS/36.x-phases/phase-36.10-missing-background/spec-36.10-missing-background.md
@SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
@app/src/lib/previewRenderer.ts
@app/src/lib/exportRenderer.ts
@app/src/lib/exportRenderer.test.ts
@app/src/lib/physicPaintRotoDurableCore.test.ts
</context>

## Artifacts this phase produces

- New test cases in existing renderer test files for transparent parity, background-only parity, dynamic trailing behavior, and alpha separation.
- New test helper names, if needed: `makePhysicPaintLayer`, `renderPreviewFramePixels`, `readCanvasPixel`, `assertRealKeyDataUrlUnchanged`.
- No new production classes, decorators, CLI flags, external packages, app settings, or UI controls.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preview/export parity tests for transparent and background-only missing frames</name>
  <files>app/src/lib/exportRenderer.test.ts, app/src/lib/previewRenderer.test.ts</files>
  <read_first>
    - app/src/lib/exportRenderer.test.ts
    - app/src/lib/previewRenderer.ts
    - app/src/lib/exportRenderer.ts
    - app/src/lib/rotoFrameDraw.ts
    - .planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: D-13 transparent missing frame uses the shared resolver path and export remains delegated through PreviewRenderer.
    - Test 2: D-13 background-only missing frame uses the shared resolver path and export remains delegated through PreviewRenderer.
    - Test 3: D-12 preview source imports `resolveMissingRotoFrameDraw` and `drawMissingRotoBackground`; export source does not duplicate those imports.
    - Test 4: Export source contains no `@efxlab/efx-physic-paint`, `renderFromStrokes`, `forceDryAll`, or direct `physicPaintStore` missing-frame branch.
  </behavior>
  <action>Extend existing renderer/source-contract tests rather than building a new broad export harness. Prefer the established `readSource` pattern in `exportRenderer.test.ts` and any existing `PreviewRenderer` test helpers if present. The tests must prove D-12/D-13 source parity: preview owns the shared resolver draw path; export delegates through `renderer.renderFrame(` and does not import/store/engine-replay missing-frame logic. Use region-specific assertions where needed so negative source checks do not block legitimate preview imports.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/exportRenderer.test.ts src/lib/previewRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert preview contains `resolveMissingRotoFrameDraw` and `drawMissingRotoBackground`.
    - Tests assert export contains `renderer.renderFrame(` and does not contain `@efxlab/efx-physic-paint`, `renderFromStrokes`, `forceDryAll`, or `resolveMissingRotoFrameDraw`.
    - Focused renderer tests pass after any necessary source-contract adjustments.
  </acceptance_criteria>
  <done>Preview/export parity is guarded by focused source-contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add alpha-separation and real-key-adjacency regressions</name>
  <files>app/src/lib/physicPaintRotoDurableCore.test.ts, app/src/lib/exportRenderer.test.ts, app/src/lib/previewRenderer.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintRotoDurableCore.test.ts
    - app/src/stores/physicPaintStore.ts
    - app/src/stores/physicPaintStore.test.ts
    - app/src/lib/previewRenderer.ts
    - SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
  </read_first>
  <behavior>
    - Test 1: D-11 existing real cached frames at frames beside a missing/background-only gap keep their original `dataUrl` values after missing-frame preview/export support recomputation.
    - Test 2: D-14/D-15 background-only rendering/support does not replace a real-key cache with paper/background pixels.
    - Test 3: Trailing frame preview/export rule remains dynamic and no new serialized output appears for requested trailing frames.
    - Test 4: Existing durable Roto core save/load/reopen coverage remains green.
  </behavior>
  <action>Add focused regressions to the narrowest existing test files that already cover durable Roto and renderer contracts. Assert real-key `dataUrl` values before and after missing-frame support/parity operations. Assert `physicPaintStore.toMceOutputs()` does not gain trailing frame entries when preview/export asks for a frame after the last real key. Keep tests deterministic and avoid starting the app server or invoking browser UI.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintRotoDurableCore.test.ts src/lib/exportRenderer.test.ts src/lib/previewRenderer.test.ts src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert real-key `dataUrl` values are unchanged by background-only support/rendering operations.
    - Tests assert trailing dynamic missing frames do not add serialized frame entries.
    - Existing durable core tests pass with the new missing-frame behavior.
  </acceptance_criteria>
  <done>Alpha-only real-key cache separation and real cached frame adjacency are regression-protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run full automated phase gate without server</name>
  <files>app/src/lib/previewRenderer.ts, app/src/lib/exportRenderer.test.ts, app/src/lib/physicPaintRotoDurableCore.test.ts</files>
  <read_first>
    - app/package.json
    - app/vitest.config.ts
    - .planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-CONTEXT.md
    - .planning/STATE.md
  </read_first>
  <action>Run the Phase 36.10 automated gate using pnpm commands only. Do not run `pnpm dev`, Vite dev server, Tauri app, or any local server. If tests reveal source-contract fragility, fix the tested contract rather than weakening D-12/D-13/D-14/D-15 coverage. Preserve project instruction that the user runs the app locally.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/rotoFrameDraw.test.ts src/stores/physicPaintStore.test.ts src/types/physicPaint.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts src/components/physic-paint/physicsPaintRotoSession.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/lib/exportRenderer.test.ts src/lib/physicPaintRotoDurableCore.test.ts</automated>
    <automated>pnpm --dir app typecheck</automated>
    <automated>pnpm --dir app exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 36.10 tests pass.
    - `pnpm --dir app typecheck` passes.
    - Full app Vitest suite passes.
    - No server-start command is run.
  </acceptance_criteria>
  <done>Automated preview/export parity, alpha-separation, focused regressions, typecheck, and full app tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview renderer → exported PNG sequence | Exported files inherit live preview pixel rules through renderer delegation. |
| paper/background rendering → real-key alpha cache | Background pixels must remain draw-time/support data and not become persisted real-key paint alpha. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.10-11 | Information Disclosure | transparent missing export | medium | mitigate | Source-contract and renderer tests prevent nearest/stale cached frame fallback for transparent gaps. |
| T-36.10-12 | Tampering | real-key cache `dataUrl` | high | mitigate | Durable/core regressions assert real-key dataUrl values are unchanged by missing/background support operations. |
| T-36.10-13 | Denial of Service | export trailing frames | medium | mitigate | Tests assert trailing frame requests do not add serialized/cache entries. |
| T-36.10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if an install appears, stop and require Package Legitimacy Gate review before continuing. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/lib/rotoFrameDraw.test.ts src/stores/physicPaintStore.test.ts src/types/physicPaint.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts src/components/physic-paint/physicsPaintRotoSession.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/lib/exportRenderer.test.ts src/lib/physicPaintRotoDurableCore.test.ts`
- `pnpm --dir app typecheck`
- `pnpm --dir app exec vitest run`
</verification>

<success_criteria>
- Preview/export parity is verified by source contracts and focused regressions.
- Existing real cached frames render normally beside missing/background-only gaps.
- Real-key alpha cache separation remains protected for Phase 36.11.
- No server is started and no export-time physics replay path is introduced.
</success_criteria>

<output>
Create `.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-04-SUMMARY.md` when done.
</output>
