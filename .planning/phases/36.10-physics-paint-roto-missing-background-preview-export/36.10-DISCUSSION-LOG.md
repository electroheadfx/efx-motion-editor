# Phase 36.10: Physics Paint Roto Missing Background Preview Export - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-27
**Phase:** 36.10-physics-paint-roto-missing-background-preview-export
**Areas discussed:** Mode control, Leading frames, Cache bounds, Parity proof

---

## Mode control

### Source of truth

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse app settings | Missing frames follow existing Roto paper/background metadata or transparent layer settings, avoiding a new mode system. | ✓ |
| Add Roto toggle | Add a minimal visible Roto missing-frame mode control for UAT, separate from existing paper/background settings. | |
| You decide | Let downstream planning choose the smallest implementation that satisfies the spec and UAT. | |

**User's choice:** Reuse app settings.
**Notes:** The phase should stay MVP/TDD and avoid a new mode system.

### Missing metadata fallback

| Option | Description | Selected |
|--------|-------------|----------|
| Layer fallback | Fall back to the existing layer paint background/color rule, and otherwise transparent. | ✓ |
| Always transparent | Ignore layer background fallback unless explicit Roto paper metadata exists. | |
| Block until set | Require the user to choose/configure background mode before background-only missing frames can render. | |

**User's choice:** Layer fallback.
**Notes:** Existing app settings remain the source of truth.

### Visible UAT feedback

| Option | Description | Selected |
|--------|-------------|----------|
| Cell/status only | Use existing Roto cell state labels such as Background only plus compact status copy; no new persistent control. | ✓ |
| Tiny indicator | Add a small missing-frame mode indicator near Roto controls, but still no separate toggle. | |
| No extra UI | Rely on visual preview/export behavior and tests only; avoid adding visible feedback unless already present. | |

**User's choice:** Cell/status only.
**Notes:** Keep the phase minimal and avoid UI expansion.

---

## Leading frames

### Behavior before first real key

| Option | Description | Selected |
|--------|-------------|----------|
| Same dynamic rule | Treat leading frames like trailing frames — render dynamically from existing settings, never persist unbounded cache entries. | ✓ |
| Same as interior | Treat leading gaps as bounded interior-like cache materialization up to the first real key. | |
| Always transparent | Before the first real key, never show paper/background even if paper mode is enabled. | |

**User's choice:** Same dynamic rule.
**Notes:** Leading frames should not become a hidden bounded cache range.

### Interior definition

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, between keys | Only frames with a real key before and after are interior and eligible for bounded background-only cache materialization. | ✓ |
| Include leading span | Frames from sequence start up to the first key are also interior-like if bounded by the timeline start. | |
| You decide | Let downstream planning choose the simplest interpretation from the spec. | |

**User's choice:** Yes, between keys.
**Notes:** Interior means strictly between two real cached keys.

### Leading cache persistence

| Option | Description | Selected |
|--------|-------------|----------|
| No cache | Render leading frames on demand from current settings only, matching trailing frames and preventing hidden cache growth. | ✓ |
| Session marker | Allow temporary session-only markers for UI labels, but do not persist them in project/cache data. | |
| Persist bounded | Persist background-only cache entries from timeline start to first key. | |

**User's choice:** No cache.
**Notes:** Leading frames are dynamic and stateless.

---

## Cache bounds

### Materialization boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Interior only | Persist/cache only missing frames strictly between real keys; leading/trailing frames render dynamically with no cache entries. | ✓ |
| Visible range | Persist/cache any missing frame in the current visible timeline/export window. | |
| Never persist | Render all missing background-only frames dynamically, including interior gaps. | |

**User's choice:** Interior only.
**Notes:** This is the bounded background-only cache rule.

### Painting background-only frame

| Option | Description | Selected |
|--------|-------------|----------|
| Replace exactly | The painted frame becomes a real Roto key and replaces/removes only that frame's background-only marker/cache. | ✓ |
| Clear span | Painting one background-only frame clears all derived background-only caches in that interior span, then regenerates as needed. | |
| Keep marker | Keep the background-only marker alongside the real key as provenance metadata. | |

**User's choice:** Replace exactly.
**Notes:** Painting converts only that frame into a real key.

### Real-key layout changes

| Option | Description | Selected |
|--------|-------------|----------|
| Recompute from keys | Treat background-only caches as derived from current real-key layout; remove/regenerate only bounded interior entries as needed. | ✓ |
| Leave until paint | Keep existing background-only cache entries even if real-key layout changes, unless the animator paints them. | |
| Clear all derived | Clear every background-only cache on any real-key utility change and rely on later regeneration. | |

**User's choice:** Recompute from keys.
**Notes:** Background-only support is derived from real-key ownership, not independent authored content.

---

## Parity proof

### Preview/export automated proof

| Option | Description | Selected |
|--------|-------------|----------|
| Shared resolver | Tests prove preview and export call/use the same missing-frame resolver for transparent, background-only, leading, interior, and trailing cases. | ✓ |
| Pixel compare | Focus on image/pixel-level comparisons between preview and export outputs for representative frames. | |
| Manual UAT | Keep automated tests lighter and rely on live preview/export manual verification for the visual parity claim. | |

**User's choice:** Shared resolver.
**Notes:** TDD should prove parity through one shared rule path.

### Alpha-only separation

| Option | Description | Selected |
|--------|-------------|----------|
| Hard test gate | Add tests/contracts proving background-only rendering never mutates or bakes paper/background into real-key paint cache frames. | ✓ |
| Context note only | Document the separation clearly for Phase 36.11 but do not require a specific automated test in 36.10. | |
| UAT only | Rely on visual checks that repaint/cache behavior still looks correct, without source-level separation assertions. | |

**User's choice:** Hard test gate.
**Notes:** This protects Phase 36.11 additive alpha-only repaint.

### Human UAT scope

| Option | Description | Selected |
|--------|-------------|----------|
| Preview + export | User verifies representative transparent/background-only missing frames in both preview and exported PNG sequence. | ✓ |
| Preview only | User verifies live preview only; automated tests cover export parity. | |
| Export only | User verifies exported PNG sequence only; preview behavior is covered by automated tests. | |

**User's choice:** Preview + export.
**Notes:** Human UAT should include both surfaces.

---

## Claude's Discretion

None. The user selected concrete decisions for all discussed areas.

## Deferred Ideas

None — discussion stayed within phase scope.
