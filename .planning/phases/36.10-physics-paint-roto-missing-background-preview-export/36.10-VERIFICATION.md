---
phase: 36.10-physics-paint-roto-missing-background-preview-export
verified: 2026-06-27T17:43:39Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
---

# Phase 36.10: Physics Paint Roto Missing Background Preview Export Verification Report

**Phase Goal:** As a stop-motion animator, I want to render missing Roto frames as transparent or paper-background-only frames, so that gaps between painted keys and trailing frames preview/export consistently without accidental paint content.
**Verified:** 2026-06-27T17:43:39Z
**Status:** passed
**Re-verification:** No — initial verification

## Verdict

PASS. The phase goal is achieved in the codebase and confirmed by user-owned live UAT.

Verifier stance: SUMMARY.md claims were not treated as evidence. The implementation files and tests were inspected directly, and a critical focused Vitest command was rerun during verification.

## User Flow Coverage

User story: “As a stop-motion animator, I want to render missing Roto frames as transparent or paper-background-only frames, so that gaps between painted keys and trailing frames preview/export consistently without accidental paint content.”

| Step | Expected | Evidence in codebase | Status |
|------|----------|----------------------|--------|
| Open/use a Physics Paint Roto layer with real cached keys and missing frames | Real cached frames are preferred; missing frames are resolved only when no Roto cached frame exists | `app/src/lib/previewRenderer.ts` uses `getPhysicPaintFrameForLayer(...)` first, then resolves missing draw instructions through `resolveMissingRotoFrameDrawForLayer(...)`; `getRotoFrame(...)` requires Roto metadata | VERIFIED |
| Scrub an interior missing frame configured transparent | Missing Roto frame contributes no visible Roto pixels and does not materialize support | `app/src/lib/rotoFrameDraw.ts` returns `{ kind: 'transparent', materialize: false }`; `app/src/lib/rotoFrameDraw.test.ts` covers transparent missing frames | VERIFIED |
| Scrub/export an interior missing frame configured paper/background-only | Missing Roto frame shows paper/background only, with no stroke/physics output | `app/src/lib/rotoFrameDraw.ts` resolves paper metadata/color to `background-only`; `app/src/lib/previewRenderer.ts` calls `drawMissingRotoBackground(...)` before drawing any cached source; `app/src/lib/previewRenderer.test.ts` covers real paper texture rendering | VERIFIED |
| Scrub/export leading or trailing missing frames | Leading/trailing frames are dynamic and non-materialized; trailing export remains bounded by export range | `getMissingRotoFrameSpan(...)` classifies leading/trailing; `resolveMissingRotoFrameDraw(...)` sets `materialize` only for `interior`; store recompute requires requested strict interiors | VERIFIED |
| Export the same frames | Export uses the same preview renderer path, not a duplicate physics replay or separate missing-frame rule | `app/src/lib/exportRenderer.ts` delegates drawing through `PreviewRenderer.renderFrame(...)`; source scan found no export-side `resolveMissingRotoFrameDraw`, `physicPaintStore`, `renderFromStrokes`, or `forceDryAll` missing-frame path | VERIFIED |
| Paint over a background-only support frame | That exact support frame is removed/replaced by a real Roto key | `app/src/stores/physicPaintStore.ts` removes background-only support in `upsertRealRotoKeyFrame(...)`; replacement paths recompute support from the current real-key layout | VERIFIED |
| Final visible outcome | Gaps and trailing frames preview/export consistently without accidental paint content | User live UAT confirmed: “ok all work now” after fixes for real paper texture, scale parity, transparent painted frame output, standalone ratio/cropping, and save/cache performance | VERIFIED |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Missing Roto frames can resolve to transparent with no visible Roto pixels | VERIFIED | `resolveMissingRotoFrameDraw(...)` returns transparent for transparent state/metadata; rerun tests include `rotoFrameDraw.test.ts` transparent case |
| 2 | Missing Roto frames can resolve to paper/background-only with no paint strokes or physics output | VERIFIED | `drawMissingRotoBackground(...)` fills/draws paper/background only; it does not read stroke state or engine output |
| 3 | Roto paper/background metadata is preferred before layer paint background fallback; otherwise transparent | VERIFIED | `getMissingRotoBackgroundState(...)` reads `physicPaintStore.getRotoBackgroundMetadata(...)`, then layer `paintBgColor`, then transparent |
| 4 | Leading, interior, trailing, and no-real-key spans are classified | VERIFIED | `getMissingRotoFrameSpan(...)` implements all four span states and is covered by `rotoFrameDraw.test.ts` |
| 5 | Only strict interior background-only missing frames are eligible for bounded support materialization | VERIFIED | `materialize = span.kind === 'interior'`; store support recompute additionally requires previous and next real keys |
| 6 | Leading/trailing missing frames do not create unbounded cache entries or metadata | VERIFIED | `recomputeBackgroundOnlyRotoSupport(...)` only materializes caller-requested strict interior frames; tests cover no edge growth and dynamic trailing behavior |
| 7 | Background-only support is distinguishable from editable real-key alpha content | VERIFIED | `PhysicPaintRotoFrameSource` includes `background-only-support`; runtime guard requires `backgroundOnly === true` for that provenance; `getRealRotoKeyFrames(...)` filters only `real-key` |
| 8 | Painting/replacing a background-only support frame converts that exact frame to a real Roto key | VERIFIED | `upsertRealRotoKeyFrame(...)` removes support at `[frame]`, writes `source: 'real-key'`, and notifies visual change |
| 9 | Real-key delete/replace/paste paths clean/recompute derived support | VERIFIED | `removeRealRotoKeyFrame(...)` and `replaceRotoKeyFrames(...)` collect previous support frames, remove support, then recompute bounded support from the new layout |
| 10 | Existing real cached frames still render normally beside missing/background-only frames | VERIFIED | Preview checks cached Roto frame before missing draw; tests include real-key adjacency/alpha separation regressions |
| 11 | Preview and export use the same missing-frame visual rule | VERIFIED | `exportRenderer.ts` delegates to `renderer.renderFrame(...)`; Preview owns resolver/draw path; source-contract tests cover this boundary |
| 12 | Paper texture scale and standalone canvas ratio are wired for parity/performance | VERIFIED | `PhysicsPaintStudio.tsx` computes bounded working size preserving project ratio and passes `paperTextureScale`; package types/preact wrapper/engine pass scale to `loadPaperTexture(...)` |

**Score:** 12/12 truths verified (0 present-but-behavior-unverified)

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/lib/rotoFrameDraw.ts` | Pure span-aware missing-frame resolver and draw helper | VERIFIED | Exports `getMissingRotoFrameSpan`, `resolveMissingRotoFrameDraw`, `drawMissingRotoBackground`; no store imports |
| `app/src/lib/previewRenderer.ts` | Preview missing-frame resolver/draw wiring | VERIFIED | Imports resolver/draw helper, reads real key layout, draws background-only on missing frames, and subscribes to `physicPaintVersion` |
| `app/src/lib/exportRenderer.ts` | Export delegates to preview renderer | VERIFIED | Uses `PreviewRenderer.renderFrame(...)`; does not duplicate missing-frame or physics-engine rendering logic |
| `app/src/stores/physicPaintStore.ts` | Bounded support provenance/recompute/replacement | VERIFIED | Implements background-only support add/remove/recompute, real-key filtering, serialization and guards |
| `app/src/types/physicPaint.ts` | Explicit support provenance and runtime guards | VERIFIED | `background-only-support` source added; guard rejects invalid provenance and malformed support |
| `app/src/components/physic-paint/physicsPaintWorkflowState.ts` | Compact UI status/cell labels | VERIFIED | Maps support to `background-only` and provides transparent/background-only status labels |
| `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx` | Existing compact workflow strip surface | VERIFIED | Renders status through existing Roto strip surfaces; no new missing-frame toggle found |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | Standalone ratio/scale and support cleanup wiring | VERIFIED | Preserves project ratio through bounded working canvas and passes `paperTextureScale`; handles support cleanup effects |
| `packages/efx-physic-paint/src/preact.tsx` | Pass paper texture scale to engine | VERIFIED | Wrapper includes `paperTextureScale: props.paperTextureScale` in engine config |
| `packages/efx-physic-paint/src/types.ts` | Engine config exposes paper scale | VERIFIED | `EngineConfig.paperTextureScale?: number` exists |
| `packages/efx-physic-paint/src/core/paper.ts` | Scaled paper tiling | VERIFIED | `loadPaperTexture(..., tileScale)` scales tile width/height before tiling |
| `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts` | Engine stores and applies paper scale | VERIFIED | Constructor stores validated scale; paper loader passes it to `loadPaperTexture(...)` |

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `previewRenderer.ts` | `rotoFrameDraw.ts` | Imports and calls `resolveMissingRotoFrameDraw` and `drawMissingRotoBackground` | WIRED | Resolver called through `resolveMissingRotoFrameDrawForLayer(...)`; draw helper called in physic-paint branch |
| `previewRenderer.ts` | `physicPaintStore.ts` | Reads real Roto key layout and cached Roto frames | WIRED | `getRealRotoKeyFrames(...)`, `getRotoFrame(...)`, and metadata lookup are used |
| `exportRenderer.ts` | `previewRenderer.ts` | `renderGlobalFrame(...)` calls `renderer.renderFrame(...)` | WIRED | Export inherits preview missing-frame behavior |
| `physicPaintStore.ts` | `rotoFrameDraw.ts` | Store recompute uses resolver materialization eligibility | WIRED | `_makeBackgroundOnlySupportFrame(...)` calls `resolveMissingRotoFrameDraw(...)` and requires `interior` + `materialize` |
| `physicPaintStore.ts` | `physicPaint.ts` | Runtime guard and provenance serialization | WIRED | Store writes `source: 'background-only-support'`; type guard accepts only valid support |
| `PhysicsPaintStudio.tsx` | package `EfxPaintCanvas`/engine | Passes `paperTextureScale` and bounded dimensions | WIRED | `CanvasMountProbe` passes scale; Preact wrapper passes it to `EfxPaintEngine`; engine passes it to paper loader |

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `PreviewRenderer` | `missingDraw` | `physicPaintStore` metadata + layer fallback + real key frames | Yes | FLOWING |
| `PreviewRenderer` | paper texture/canvas | `/img/paper_1.jpg`, `/img/paper_2.jpg`, `/img/paper_3.jpg` through image cache/project paper canvas | Yes | FLOWING |
| `physicPaintStore` | background-only support frames | Roto background metadata + requested interior frames + real key layout | Yes, bounded to requested strict interiors | FLOWING |
| `PhysicsPaintStudio` | `paperTextureScale` | `canvasWidth / projectCanvasWidth`, with bounded working canvas preserving ratio | Yes | FLOWING |
| `EfxPaintEngine` | paper tiling scale | `EngineConfig.paperTextureScale` -> `loadPaperTexture(...)` | Yes | FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Critical resolver/store/preview/export/type behavior passes focused tests | `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/lib/rotoFrameDraw.test.ts src/lib/previewRenderer.test.ts src/lib/exportRenderer.test.ts src/stores/physicPaintStore.test.ts src/types/physicPaint.test.ts` | 5 test files passed; 59 passed tests, 19 todo | PASS |
| Final automated gate from main session | `pnpm --filter @efxlab/efx-physic-paint build && pnpm --dir app typecheck && pnpm --dir app exec vitest run` | User-provided gate evidence: package build passed, app typecheck passed, full app Vitest passed with 43 passed files, 3 skipped, 578 passed tests, 101 todo | PASS |

## Probe Execution

No phase probes were declared or found under `scripts/**/tests/probe-*.sh`. Probe execution skipped.

## Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| 36.10-MISSING-TRANSPARENT | Missing Roto frames can render transparent without accidental content | SATISFIED | Resolver transparent branch; non-materialized edge handling; focused tests passed; live UAT approved |
| 36.10-MISSING-BACKGROUND | Missing Roto frames can render paper/background-only without paint content and without baking paper into real alpha caches | SATISFIED | Background-only resolver/draw path, support provenance, alpha-separation tests, real paper texture scale wiring, live UAT approved |
| 36.10-PREVIEW-EXPORT-PARITY | Preview and export match for the same frame/settings | SATISFIED | Export delegates to PreviewRenderer; source-contract tests; final UAT confirmed live preview/export parity |

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| Key implementation files | n/a | No unreferenced `TBD`, `FIXME`, or `XXX`; no blocking placeholder implementations found | none | No blocker |
| `previewRenderer.ts` / `PhysicsPaintStudio.tsx` | multiple | `return null` guard/helper paths | info | Legitimate guard/loading branches, not user-visible stubs |

## Human Verification

Already completed by the user. Required visual/runtime UAT was approved with: “ok all work now”. Coverage included:

- transparent missing Roto preview/export parity;
- background-only missing Roto preview/export parity;
- real paper texture parity between EFX Motion and standalone Physics Paint;
- correct standalone canvas ratio/no height cropping;
- transparent painted Roto frame output with no baked paper/background in real alpha caches;
- acceptable save/render cache performance after bounded working canvas fix;
- no new persistent missing-frame mode control.

## Warnings / Notes

- `pnpm --filter @efxlab/efx-physic-paint check` was not used as the final gate because the package-local check has a pre-existing Vitest type-resolution issue in package tests. This is documented in `36.10-VALIDATION.md`. The package build, app typecheck, focused tests, full app tests, and live UAT all passed.
- The rerun focused Vitest command emitted existing sourcemap warnings from `@efxlab/motion-canvas-core`; tests still passed.
- ROADMAP/STATE still show Phase 36.10 as in progress/plans 4/5 in some entries, but phase artifacts and validation show Plan 05 complete and live UAT approved. This is planning metadata drift, not an implementation gap.

## Gaps Summary

No blocking gaps found. The codebase contains the resolver, preview/export wiring, bounded support model, UI status surface, paper scale/ratio fixes, and tests/live UAT evidence required for the phase goal.

---

_Verified: 2026-06-27T17:43:39Z_
_Verifier: Claude (gsd-verifier)_
