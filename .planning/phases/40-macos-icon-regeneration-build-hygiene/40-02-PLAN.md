---
phase: 40-macos-icon-regeneration-build-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/vite.config.ts
  - app/src/viteBuild.test.ts
autonomous: true
requirements: [BUILD-01, BUILD-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "app/vite.config.ts build block contains `chunkSizeWarningLimit: 1100` with the D-11 rationale comment block directly above it — packaged Tauri desktop app loading local assets; Vite's 500 kB default is a generic web threshold; 1100 is a monitored desktop entry-bundle budget, not a performance claim; must not be raised again without measurement (covered edges: BUILD-01 adjacency — the value is exactly 1100, not 1099 or 1101; comment placement is ordered directly above the key)"
    - "viteBuild.test.ts captures the resolved build.chunkSizeWarningLimit via the existing configResolved capture-plugin pattern and asserts it is exactly 1100 (covered edges: BUILD-01/BUILD-03 boundary+precision — exact integer equality against the resolved config, not the source text)"
    - "viteBuild.test.ts captures every build warning through a createLogger() wrap passed as customLogger inside the existing programmatic build() call — customLogger REPLACES logLevel:'silent'; no subprocess, no stderr parsing (D-12)"
    - "viteBuild.test.ts pins exactly two intentional chunk separations by stable prefix — src/project-*.js (Motion Canvas, spec-named, existing assertion retained) and PhysicsPaintStudio-*.js (new assertion, verified genuinely separate in the research baseline) — never content-hash filenames, never exact chunk counts (D-15, covered edge: BUILD-03 ordering — prefix identity is order- and hash-independent)"
    - "Existing BUILD-03 seam assertions are retained unchanged: HTML entry exists, referenced local assets exist and are non-empty (covered edge: BUILD-01/BUILD-03 empty — non-empty asset guard)"
    - "The production build emits no chunk-size warning once the 1100 limit is in force — the research baseline entry chunk is 969.22 kB, 130 kB under budget, so the captured warning set contains no chunk-size complaint (the 12 mixed-import warnings are expected and remain until plan 40-03)"
  artifacts:
    - app/vite.config.ts (build block gains chunkSizeWarningLimit: 1100 + rationale comment)
    - app/src/viteBuild.test.ts (captured.chunkLimit extension, warnings capture array + customLogger wrap, PhysicsPaintStudio separation pin)
  key_links:
    - "configResolved on the enforce:'post' capture plugin reads config.build.chunkSizeWarningLimit — the assertion targets the RESOLVED value, so a config typo cannot false-pass"
    - "customLogger wrap sees all warn calls; the wrap captures String(msg) unfiltered (no filtering at capture time — assertions filter, the capture never does)"
    - "The D-11 comment wording is itself a verification target (Pitfall 5: 1100 must not become a silent ratchet)"
---

<objective>
Pin the desktop build policy: set `chunkSizeWarningLimit: 1100` in `app/vite.config.ts` with the documented desktop rationale as its sole documentation (D-11), and extend the existing production-build seam in `app/src/viteBuild.test.ts` with resolved-limit assertion (D-14), warning capture via customLogger (D-12 — infrastructure the plan 40-03 non-return assertions consume), and the second intentional chunk-separation pin for PhysicsPaintStudio output (D-15).

Purpose: BUILD-01 makes the desktop chunk budget explicit and test-pinned instead of silently suppressed; BUILD-03's seam proves the policy end-to-end on the real programmatic build.
Output: One config key + rationale comment; the extended build test seam, green on the full suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-CONTEXT.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md
@app/vite.config.ts
@app/src/viteBuild.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end budget proof — resolved-limit assertion red at 500, then green at exactly 1100</name>
  <files>app/vite.config.ts, app/src/viteBuild.test.ts</files>
  <read_first>
    - app/vite.config.ts (lines 180-187 — the build block receiving the key; lines 81-94 and 175-179 — the established multi-line rationale-comment convention to copy)
    - app/src/viteBuild.test.ts (lines 52-60 — createInputCapturePlugin + captured shape; lines 62-98 — the programmatic build seam, BUILD_TIMEOUT, makeTempDir/afterAll hermetic output)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md (vite.config.ts and viteBuild.test.ts sections — exact extension points)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md (Pattern 2 — verified resolved-limit capture; Pitfall 5 — comment wording is a verification target)
  </read_first>
  <action>Prove the budget policy end-to-end per D-11/D-14, in red-then-green order. FIRST extend the test: widen the `captured` object shape in createInputCapturePlugin with a `chunkLimit` field and record `config.build.chunkSizeWarningLimit` inside the existing configResolved hook (keeping the existing `captured.input` line); add an assertion in the appropriate existing describe block — `expect(captured.chunkLimit, 'chunkSizeWarningLimit must resolve to the documented 1100 desktop budget').toBe(1100)`. Run `pnpm --dir app exec vitest run src/viteBuild.test.ts` and confirm it FAILS with the captured value 500 (research verified the pre-change resolved default is 500) — this proves the assertion reads the resolved config, not source text. THEN edit `app/vite.config.ts`: in the existing `build` block, add `chunkSizeWarningLimit: 1100` with a comment block directly above it in the file's established rationale-comment style stating all four D-11 points: (1) packaged Tauri desktop app loading local assets, (2) Vite's 500 kB default is a generic web threshold, (3) 1100 is a monitored desktop entry-bundle budget — not a performance claim, (4) it must not be raised again without measurement. Do not add any chunk-splitting configuration, do not add lazily-loaded bootstrap indirection, do not add log/warning filtering of any kind (spec-prohibited). Re-run the build test — it must go green. Keep the existing target/minify/sourcemap lines and the pre-existing motion-canvas config-hook NOTE comment untouched.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/viteBuild.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app exec vitest run src/viteBuild.test.ts` exits 0 with the chunkLimit assertion passing
    - `grep -n 'chunkSizeWarningLimit' app/vite.config.ts` shows the key inside the existing build block
    - The assertion was observed failing at resolved value 500 before the config edit (record the red run output in the SUMMARY)
    - The comment block directly above the new key contains all four phrases: `packaged Tauri desktop app`, `500 kB default`, `monitored`, `not a performance claim` (wording per D-11; `must not be raised again without measurement` also present)
    - `git diff app/vite.config.ts` touches only the build block (no plugin/config-hook architecture changes)
  </acceptance_criteria>
  <done>The resolved chunk budget is asserted at exactly 1100 on the real programmatic build, and the rationale comment sits directly above the key as the sole documentation.</done>
  <reversibility rating="reversible">Single config key + test assertion; git revert restores prior behavior with no side effects.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Warning capture (customLogger) + PhysicsPaintStudio chunk-separation pin</name>
  <files>app/src/viteBuild.test.ts</files>
  <read_first>
    - app/src/viteBuild.test.ts (full file — imports at lines 1-15, build call at lines 66-75 with `logLevel: 'silent'`, prefix-based project-* assertion at lines 122-124, collectFiles helper)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md (Pattern 1 — verified customLogger wrap; Code Examples → D-15 resolution — pin exactly two separations; Pitfall 4 — never pin hash-named chunks)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md (viteBuild.test.ts section — core patterns 2 and 3)
  </read_first>
  <action>Add the D-12 warning-capture infrastructure and the D-15 second separation pin to the existing build seam. Concretely: (1) add `createLogger` to the existing `vite` import; (2) create a module-or-describe-scoped `warnings: string[]` array and a logger from `createLogger()` whose `warn` method is wrapped to push `String(msg)` into the array unfiltered before (optionally) delegating to the original warn — capture everything, filter only in assertions; (3) in the existing programmatic `build()` call, pass `customLogger: logger` and REMOVE the `logLevel: 'silent'` line (a custom logger replaces log-level gating; the wrap sees all warn calls — verified in research); (4) add a new assertion mirroring the existing project-* pin: filter `collectFiles(outDir)` with `/PhysicsPaintStudio-[^/]*\.js$/` and expect length greater than 0 with a descriptive message ('the PhysicsPaintStudio lazy chunk must remain a separate bundle'); research verified efx-physic-paint engine code rides this chunk and it is genuinely separate; (5) add an assertion that the captured warnings contain no chunk-size complaint — filter `warnings` with a case-insensitive pattern matching chunk-size-exceeds phrasing (e.g. /chunk.*(size|larger than)/i applied defensively: assert the count is 0 with a message naming the 1100 budget) — the baseline entry chunk is 969.22 kB so this must hold; do NOT assert the total warning count (the 12 mixed-import warnings remain until plan 40-03 triage); (6) leave all existing assertions (HTML entry, local refs/assets non-empty, project-* pin) byte-identical. Keep the established conventions: explicit timeouts, descriptive second-argument messages, hermetic temp outDir. The exported `warnings` capture array will be consumed by plan 40-03's non-return assertions — keep it module/describe-scoped and named exactly `warnings`.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/viteBuild.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app exec vitest run src/viteBuild.test.ts` exits 0
    - `grep -n 'customLogger' app/src/viteBuild.test.ts` shows the logger passed into the programmatic build call, and `logLevel: 'silent'` no longer appears in that call
    - `grep -n 'PhysicsPaintStudio-\[^/\]\*\\.js' app/src/viteBuild.test.ts` shows the new prefix-based separation pin
    - The test source contains no content-hash chunk filenames (no strings like `index-DiQjlua3`) and no exact chunk-count assertions
    - The captured-warnings assertion for chunk-size complaints passes at 0 (baseline entry chunk 969.22 kB is under the 1100 budget)
  </acceptance_criteria>
  <done>All build warnings flow through the customLogger capture (ready for 40-03 non-return assertions), both intentional chunk separations are prefix-pinned, no chunk-size warning is emitted at the 1100 budget, and the full build seam stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite build → test seam | The programmatic build's warnings are process-internal evidence; suppressing or misreading them would mask regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-04 | Repudiation | chunkSizeWarningLimit budget | medium | mitigate | D-11 rationale comment is a hard acceptance target (all four phrases); D-14 resolved-limit assertion reads config via configResolved so source-text edits cannot false-pass; no warning suppression permitted (spec + D-11) |
| T-40-05 | Information disclosure (process integrity) | warning capture | low | mitigate | Capture is unfiltered at ingestion (String(msg) pushed verbatim); filtering exists only inside assertions; customLogger replaces silent logLevel so warnings are observable |
| T-40-06 | Tampering | chunk-separation pins | low | mitigate | Prefix-based regexes only (`src/project-`, `PhysicsPaintStudio-`); content-hash filenames and exact counts explicitly rejected by acceptance criteria, preventing both fragility and disguised merging |
| T-40-SC | Tampering | package installs | high | mitigate | No installs in this plan; Package Legitimacy Audit has nothing to check |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/viteBuild.test.ts` — green (real programmatic build, ~60-90s)
- Red-then-green evidence for the 1100 assertion recorded in the SUMMARY
- `git diff app/vite.config.ts` — build block only
</verification>

<success_criteria>
1. `chunkSizeWarningLimit: 1100` is set with the four-point D-11 rationale comment directly above it, and the resolved value is asserted at exactly 1100 (BUILD-01)
2. The build seam captures all warnings via customLogger (no subprocess/stderr), retains HTML-entry and non-empty-asset assertions, and pins both intentional chunk separations by stable prefix (BUILD-03 seam infrastructure)
3. No chunk-size warning is emitted at the 1100 budget; no chunk-splitting config, lazy bootstrap indirection, or warning filtering was introduced
</success_criteria>

## Artifacts this phase (plan 40-02) produces

- `app/vite.config.ts` — NEW config key `build.chunkSizeWarningLimit: 1100` + rationale comment (existing file, new key)
- `app/src/viteBuild.test.ts` — NEW test-scope symbols (existing file):
  - `captured.chunkLimit` field on the input-capture plugin's captured shape
  - `warnings: string[]` capture array + `createLogger()` customLogger wrap
  - New assertions: resolved-limit (`toBe(1100)`), `PhysicsPaintStudio-*.js` separation pin, chunk-size-warning absence

<output>
Create `.planning/phases/40-macos-icon-regeneration-build-hygiene/40-02-SUMMARY.md` when done. Record: the red-run output showing resolved 500 before the config edit, the final green run, and the exact comment wording placed above the key.
</output>
