---
phase: 40-macos-icon-regeneration-build-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src-tauri/icons/32x32.png
  - app/src-tauri/icons/128x128.png
  - app/src-tauri/icons/128x128@2x.png
  - app/src-tauri/icons/icon.icns
  - app/src-tauri/icons/icon.ico
  - .planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh
autonomous: false
requirements: [ICON-01, ICON-02, ICON-03, ICON-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 5 tracked icons under app/src-tauri/icons/ are regenerated from SPECS/efxmotioneditor-icon-2.png via `pnpm tauri icon` using the 794×794 source directly with no manual upscale (per D-01, D-03)"
    - "git status for app/src-tauri/icons/ shows exactly 5 modified files and zero untracked extras — no Square logos, StoreLogo, 64x64.png, icon.png, ios/, or android/ output enters the tracked tree (covered edge: ICON-02 adjacency/ordering — exact set, exact membership)"
    - "releaseContract.test.ts passes: bundle.icon equals the exact 5-file EXPECTED_ICONS array, every declared file exists and is non-empty, icon.icns starts with the `icns` magic bytes (covered edges: ICON-01 boundary, ICON-02 empty, ICON-03 empty)"
    - "The D-05 check script proves against the FRESH unsigned build (bundle mtime newer than the 2026-08-01 stale bundle) that Info.plist declares CFBundleIconFile, the bundled icon resource exists non-empty, and the resource starts with the icns magic bytes — reusing the exact logic lines from scripts/macos-release.sh, which remains byte-identical (covered edge: ICON-03 adjacency/ordering)"
    - statement: "No project-side resampling, scaling, or precision-sensitive image math occurs anywhere in the phase — `tauri icon` owns every size derivation from the 794×794 source; the project never hand-rolls PNG/ICNS/ICO processing (per D-02)"
      verification: backstop
  artifacts:
    - app/src-tauri/icons/32x32.png (regenerated)
    - app/src-tauri/icons/128x128.png (regenerated)
    - app/src-tauri/icons/128x128@2x.png (regenerated)
    - app/src-tauri/icons/icon.icns (regenerated)
    - app/src-tauri/icons/icon.ico (regenerated)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh (new, executable)
  key_links:
    - "Staging-dir generation with `-o` then copy of ONLY the 5 declared files — the exact-array contract (releaseContract.test.ts + tauri.conf.json bundle.icon) stays true"
    - "check-unsigned-app-icon.sh extracts the icon-check lines from scripts/macos-release.sh at runtime (sed range over the CFBundleIconFile block) — it never sources the script (unconditional `main \"$@\"` trap) and never edits it"
    - "UAT runs against the freshly built bundle at app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app, not the stale 2026-08-01 bundle (mtime guard before UAT)"
  prohibitions:
    - statement: "scripts/macos-release.sh must remain byte-identical — no signing, notarization, stapling, Gatekeeper, or preflight edits of any kind (D-06)"
      status: active
      verification: "git diff -- scripts/macos-release.sh is empty; releaseContract.test.ts grep-pins on the script stay green"
    - statement: "SPECS/efxmotioneditor-icon-2.png must never be modified, re-encoded, or regenerated — it is trusted read-only design input (D-01)"
      status: active
      verification: "flagged-unverified (SPECS/ is gitignored; honor-system + executor discipline)"
    - statement: "Zero new package dependencies in any package.json — no pngjs, no image decoders, no icon tooling beyond the installed @tauri-apps/cli (D-02)"
      status: active
      verification: "git diff on **/package.json and pnpm-lock.yaml shows no added dependencies"
---

<objective>
Replace the macOS release identity: regenerate the tracked Tauri icon set from the user-approved 794×794 alpha artwork through the existing icon pipeline (D-01..D-03), keep the generated tracked icons as release authority under the existing release-contract checks (D-04), and prove the packaged `.app` icon metadata NOW against a fresh unsigned build by reusing the release script's existing check logic (D-05/D-06).

Purpose: v0.8.0 shipped the placeholder template icon (T-jun-02 incident). Phase 40 delivers the real, legible EFX Motion Editor identity and proves the packaged-icon guard works before Phase 44 re-proves it at release time.
Output: 5 regenerated tracked icon files, an executable D-05 check script, a fresh unsigned bundle with proven icon metadata, and user visual UAT approval of the icon in Finder/Dock/Applications/Cmd-Tab/DMG.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-CONTEXT.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md
@app/src/releaseContract.test.ts
@app/src-tauri/tauri.conf.json
@scripts/macos-release.sh
</context>

<flagged_assumptions>
- **ICON-04 (unresolved, unclassified probe row):** Icon legibility at 16×16 through 512×512 across Finder, Dock, Applications, Cmd-Tab, and the mounted DMG is accepted SOLELY by the user's visual UAT on the fresh unsigned build (D-06). No automated pixel, alpha-corner, or legibility validation exists or will be built (D-01/D-02). If UAT finds the icon unreadable at small sizes, the fix is new artwork from the user, not code.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end icon regeneration — source artwork to tracked release authority, contract green</name>
  <files>app/src-tauri/icons/32x32.png, app/src-tauri/icons/128x128.png, app/src-tauri/icons/128x128@2x.png, app/src-tauri/icons/icon.icns, app/src-tauri/icons/icon.ico</files>
  <read_first>
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md (section "app/src-tauri/icons/*" — verified CLI sequence and the git-status guard)
    - app/src/releaseContract.test.ts (lines 15-21 EXPECTED_ICONS array; lines 63-72 exact-array / non-empty / icns-magic assertions)
    - app/src-tauri/tauri.conf.json (lines 14-20 bundle.icon declaration — the exact 5 paths)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md (Pattern 3 + Pitfall 1 — the ~45 extras tauri icon emits)
  </read_first>
  <action>Regenerate the tracked icon set per D-01/D-02/D-03. From the `app/` directory run `pnpm tauri icon ../SPECS/efxmotioneditor-icon-2.png -o /tmp/efx-icons-staging-40` (794×794 source used directly; no manual 1024 upscale; the installed @tauri-apps/cli 2.10.x accepts it — verified in research). Then copy ONLY these 5 files from the staging dir into `app/src-tauri/icons/`: 32x32.png, 128x128.png, 128x128@2x.png, icon.icns, icon.ico. Do NOT copy anything else — the staging dir will also contain 64x64.png, icon.png, StoreLogo.png, 10 Square*Logo.png files, ios/ and android/ trees; all of those stay out of the tracked tree. Do not modify SPECS/efxmotioneditor-icon-2.png. Do not install any package. After the copy, run `git status --porcelain -- app/src-tauri/icons/` and confirm exactly 5 lines each beginning with ` M` (modified, not untracked, not added). Then run the existing contract test — no test edits are expected (D-04: extend only if a genuine gap is found; research verdict: coverage is complete verbatim). If the contract test fails on exact-array or magic-bytes, stop and report — do not "fix" the test to match a bad generation.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/releaseContract.test.ts &amp;&amp; test "$(git status --porcelain -- app/src-tauri/icons/ | wc -l | tr -d ' ')" = "5" &amp;&amp; test -z "$(git status --porcelain -- app/src-tauri/icons/ | grep -v '^ M')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app exec vitest run src/releaseContract.test.ts` exits 0 (exact-array, non-empty, and icns-magic assertions green against the regenerated files)
    - `git status --porcelain -- app/src-tauri/icons/` outputs exactly 5 lines, every line matching `^ M ` (5 modified, 0 untracked)
    - `head -c 4 app/src-tauri/icons/icon.icns` prints `icns`
    - `git diff --stat -- '**/package.json' pnpm-lock.yaml` is empty (zero dependency changes)
  </acceptance_criteria>
  <done>The 5 tracked icons are regenerated from the approved artwork through the existing Tauri pipeline, the exact-set contract is green, and no generation extras or new dependencies entered the repo.</done>
  <reversibility rating="reversible">Tracked binary assets replaced via CLI; git revert restores the prior icon set instantly. No published contract or migration involved (pre-release).</reversibility>
</task>

<task type="auto">
  <name>Task 2: Create the D-05 packaged-icon check script and prove it against a fresh unsigned build</name>
  <files>.planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh</files>
  <read_first>
    - scripts/macos-release.sh (lines 294-328 — verify_app Info.plist/icon block; lines 21, 32-35 — $PLUTIL and die pattern; the final unconditional `main "$@"` line)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md (section "D-05 phase check script" — extract-and-eval mechanic, hard constraints, bundle path)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md (Pattern 4 — reuse constraint analysis)
  </read_first>
  <action>Create `check-unsigned-app-icon.sh` in the phase directory per D-05/D-06, using the extract-and-eval mechanic from PATTERNS (planner decision: narrow the extraction to the icon lines only, so no PRODUCT_VERSION plumbing is needed). The script must: (1) accept an optional app-bundle path argument defaulting to `app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app` (resolve relative to repo root); (2) extract at runtime — via a sed range starting at the `CFBundleIconFile` plutil extraction line and ending at the `icns magic bytes` die line — the exact icon-check lines from `scripts/macos-release.sh` and evaluate them inside a function that provides `PLUTIL=/usr/bin/plutil`, a local `die()` printing `ERROR: ...` to stderr and exiting 1, and `app_path` set to the target bundle, preceded by an explicit `Info.plist` existence check; (3) NEVER source `scripts/macos-release.sh` (its unconditional `main "$@"` would execute the full signed-release CLI); (4) NEVER run codesign/spctl/stapler — only the Info.plist/bundled-icon portion is in scope; (5) NEVER modify `scripts/macos-release.sh`; (6) print a clear PASS line naming the declared CFBundleIconFile value and bundled resource path on success. chmod +x the script. THEN produce the fresh unsigned UAT package with a direct `pnpm --dir app tauri build` (per D-06: zero changes to the release script, no signing/notarization/stapling paths). After the build, guard against the stale-bundle pitfall: verify the mtime of `app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app/Contents/Info.plist` is from today (the 2026-08-01 bundle is stale). Finally run the check script against the fresh bundle — it must exit 0. Note for the record: macOS LaunchServices may cache the old icon; that is a UAT-display concern only, not a check failure.</action>
  <verify>
    <automated>bash .planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh &amp;&amp; ! bash .planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh /nonexistent/EFX.app 2&gt;/dev/null &amp;&amp; test -z "$(git diff -- scripts/macos-release.sh)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh` exits 0 against the freshly built unsigned bundle and prints a PASS line containing `icon.icns`
    - The same script against a nonexistent bundle path exits non-zero (fail-closed, via the local `die`)
    - `git diff -- scripts/macos-release.sh` is empty (byte-identical, per D-06)
    - `stat -f '%Sm' app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app/Contents/Info.plist` shows today's date (fresh build, not the stale 2026-08-01 bundle)
    - The script source contains no `source ` or `. ` reference to macos-release.sh and no invocation of codesign, spctl, or stapler
  </acceptance_criteria>
  <done>The release script's existing packaged-icon logic is proven against the fresh unsigned `.app` (CFBundleIconFile declared, bundled resource present non-empty, icns magic) via a reusable phase check script, with the release script untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User visual UAT — icon identity on the fresh unsigned build (ICON-04, D-06)</name>
  <what-built>The 5 tracked icons were regenerated from the approved 794×794 artwork through `pnpm tauri icon` (Task 1), the release-contract test is green, and a fresh unsigned `pnpm --dir app tauri build` produced a bundle whose packaged icon metadata passed the D-05 check (Task 2).</what-built>
  <how-to-verify>
    1. Confirm you are looking at the FRESH build: `app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app` should have today's modification date (not 2026-08-01). The DMG is alongside it under `app/src-tauri/target/release/bundle/dmg/`.
    2. Open the containing folder in Finder and inspect the `.app` icon at multiple view sizes (icon view, small icons through large) — it must be the new EFX Motion Editor artwork, legible, with genuine alpha corners (no white/black box, no placeholder rocket/template icon).
    3. Launch the app and check the Dock icon while running.
    4. Press Cmd-Tab and check the application-switcher icon.
    5. Mount the DMG and check the volume window's app icon.
    6. If Finder/Dock shows the OLD cached icon anywhere, that is the known LaunchServices cache issue (research Runtime State Inventory): `touch` the .app, or log out/in, then re-check — a stale cache is not a build defect as long as Task 2's packaged-icon proof passed.
    7. Report any legibility problem at small sizes (16/32 px) — per the flagged assumption, the remedy is revised artwork from you, not code.
  </how-to-verify>
  <resume-signal>Type "approved" if the new icon is correct and legible across Finder, Dock, Cmd-Tab, and the DMG — or describe what is wrong (which surface, which size, what you see).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artwork → icon tooling | SPECS/efxmotioneditor-icon-2.png crosses into @tauri-apps/cli as parsed binary input (trusted user-owned input per D-01) |
| build machine → packaged .app | Unsigned UAT bundle is produced and inspected locally; no signing/notarization path is exercised |
| check script → release script | check-unsigned-app-icon.sh evaluates extracted lines from macos-release.sh at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | `tauri icon` image parsing | low | accept | Trusted user-owned artwork (D-01); official @tauri-apps/cli is the sole parser; no custom decoder permitted (D-02) — no new parsing surface introduced |
| T-40-02 | Tampering | scripts/macos-release.sh | medium | mitigate | Zero edits permitted (D-06); acceptance criterion asserts `git diff` empty; existing releaseContract.test.ts grep-pins detect accidental modification |
| T-40-03 | Repudiation | check-unsigned-app-icon.sh | low | mitigate | Fail-closed local `die` (non-zero exit on missing bundle/plist/icon); extraction range comes from the release script itself so the checked logic cannot silently drift from release preflight |
| T-40-SC | Tampering | package installs | high | mitigate | No package installs occur in this plan (Package Legitimacy Audit: nothing to check); acceptance criterion asserts zero package.json/lockfile changes |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/releaseContract.test.ts` — green against the regenerated set
- `git status --porcelain -- app/src-tauri/icons/` — exactly 5 modified, 0 untracked
- `bash .planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh` — exit 0 against the fresh unsigned bundle
- `git diff -- scripts/macos-release.sh` — empty
- User visual UAT approved (checkpoint)
</verification>

<success_criteria>
1. The tracked 5-icon set is regenerated from the approved artwork via the existing Tauri pipeline with no extras committed (ICON-01, ICON-02)
2. Release-contract validation is green and the packaged `.app` icon metadata is proven against the fresh unsigned build via reused release-script logic, with zero release-script/signing changes (ICON-03)
3. The user approves the icon visually in Finder, Dock, Applications/switcher, and the DMG (ICON-04)
</success_criteria>

## Artifacts this phase (plan 40-01) produces

- `app/src-tauri/icons/32x32.png` — regenerated binary (existing path, new content)
- `app/src-tauri/icons/128x128.png` — regenerated binary (existing path, new content)
- `app/src-tauri/icons/128x128@2x.png` — regenerated binary (existing path, new content)
- `app/src-tauri/icons/icon.icns` — regenerated binary (existing path, new content)
- `app/src-tauri/icons/icon.ico` — regenerated binary (existing path, new content)
- `.planning/phases/40-macos-icon-regeneration-build-hygiene/check-unsigned-app-icon.sh` — NEW executable shell script (phase-local D-05 check; symbols: `die()` local function, `PLUTIL` env, `app_path` variable; no exported API)

<output>
Create `.planning/phases/40-macos-icon-regeneration-build-hygiene/40-01-SUMMARY.md` when done. Record: the tauri icon command used, the git-status proof (5 modified / 0 untracked), the fresh-build path + mtime, the D-05 check PASS output, and the user's UAT verdict (including any LaunchServices cache note).
</output>
