---
phase: 33-enhance-current-engine
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/canvas/PaintOverlay.tsx
autonomous: true
requirements:
  - ECUR-12
gap_closure: true

must_haves:
  truths:
    - "Stroke wireframe overlay and hit testing works for both flat and FX strokes"
  artifacts:
    - path: "app/src/components/canvas/PaintOverlay.tsx"
      provides: "Wireframe rendered for all brush strokes regardless of brushStyle"
  key_links:
    - from: "app/src/components/canvas/PaintOverlay.tsx"
      to: "renderFxWireframe"
      via: "Condition removed: wireframe shown for all strokes"
      pattern: "renderFxWireframe"
---

<objective>
Enable wireframe overlay and bounding-box hit testing for flat strokes (not just FX strokes).

Purpose: User reported wireframe overlay and selection works only for FX strokes, not flat. The condition on line 807 excludes flat strokes from wireframe rendering, and hit testing is stricter for flat.

Output: All selected brush strokes show wireframe path + bounding box, and hit testing uses bounding box for all strokes.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show wireframe for all brush strokes and relax hit testing for flat</name>
  <files>app/src/components/canvas/PaintOverlay.tsx</files>
  <read_first>
    - app/src/components/canvas/PaintOverlay.tsx (lines 805-820 -- wireframe rendering condition)
    - app/src/components/canvas/PaintOverlay.tsx (lines 95-128 -- hit testing for strokes)
  </read_first>
  <action>
**1. Fix wireframe rendering condition (around line 807):**

Change the condition from:
```typescript
if ((el.tool === 'brush') && (el as PaintStroke).brushStyle && (el as PaintStroke).brushStyle !== 'flat') {
```

To include ALL brush strokes:
```typescript
if (el.tool === 'brush') {
```

This renders wireframe path + bounding box for both flat and FX selected strokes.

**2. Fix hit testing to use bounding box for flat strokes too (around lines 104-107):**

The current code on line 105 only accepts bounding-box hit for FX strokes:
```typescript
if (stroke.brushStyle && stroke.brushStyle !== 'flat') {
  return stroke.id;
}
```

Change this to accept bounding-box hit for ALL strokes. Either remove the condition entirely (making all strokes use bbox hit), or make the condition always true:

```typescript
// Accept bounding box hit for all brush strokes (flat and FX)
return stroke.id;
```

Remove (or comment out) the rest of the fine-check block below it (lines 108-127) that does precise path proximity checking, since bbox hit is sufficient and more user-friendly for all stroke types.

Actually, keep the fine-check as a fallback for strokes that MISS the bbox test but might still be near the path. The bbox check at line 103 already gates entry. The issue is the condition on lines 105-106 that only returns for FX strokes. Simply remove that condition so ALL strokes that pass bbox get returned:

Replace lines 104-127 with:
```typescript
// Accept bounding box hit for all brush strokes
return stroke.id;
```
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -n "brushStyle !== 'flat'" app/src/components/canvas/PaintOverlay.tsx | head -5 ; echo "expect 0 or only unrelated" && echo "---" && sed -n '805,810p' app/src/components/canvas/PaintOverlay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PaintOverlay.tsx wireframe rendering condition (around line 807) does NOT check `brushStyle !== 'flat'`
    - PaintOverlay.tsx wireframe renders for `el.tool === 'brush'` regardless of brushStyle
    - PaintOverlay.tsx hit testing returns stroke.id for all brush strokes inside bounding box, not just FX
  </acceptance_criteria>
  <done>Wireframe overlay appears on selected flat strokes; clicking inside bounding box selects flat strokes reliably</done>
</task>

</tasks>

<verification>
- Select a flat brush stroke -> dashed blue wireframe path and bounding box appear
- Click inside the bounding box of a flat stroke -> stroke is selected
- FX stroke selection still works identically
</verification>

<success_criteria>
- Wireframe shown for all selected brush strokes (flat and FX)
- Bounding-box hit testing works for flat strokes (not just FX)
</success_criteria>

<output>
After completion, create `.planning/phases/33-enhance-current-engine/33-20-SUMMARY.md`
</output>
