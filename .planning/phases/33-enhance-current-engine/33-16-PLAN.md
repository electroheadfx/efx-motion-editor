---
phase: 33-enhance-current-engine
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/canvas/PaintOverlay.tsx
autonomous: true
requirements:
  - ECUR-04
gap_closure: true

must_haves:
  truths:
    - "Circle cursor overlay is centered on actual cursor/paint position"
  artifacts:
    - path: "app/src/components/canvas/PaintOverlay.tsx"
      provides: "cursorPos calculated relative to overlayRef, not containerRef"
      contains: "overlayRef"
  key_links:
    - from: "app/src/components/canvas/PaintOverlay.tsx"
      to: "PaintCursor"
      via: "cursorPos coordinates match overlayRef origin"
      pattern: "setCursorPos"
---

<objective>
Fix circle cursor centering by calculating cursor position relative to overlayRef instead of containerRef.

Purpose: The circle cursor is offset from the actual paint position because cursorPos uses containerRef.getBoundingClientRect() but PaintCursor renders inside overlayRef which has a different origin.

Output: cursorPos is calculated relative to overlayRef, making the cursor circle perfectly centered on where paint actually appears.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calculate cursorPos relative to overlayRef</name>
  <files>app/src/components/canvas/PaintOverlay.tsx</files>
  <read_first>
    - app/src/components/canvas/PaintOverlay.tsx (lines 1325-1331 -- handlePointerMove cursorPos calculation)
    - app/src/components/canvas/PaintOverlay.tsx (line 540 -- overlayRef declaration)
    - app/src/components/canvas/PaintCursor.tsx (full file -- how screenX/screenY are used)
    - Also search for where PaintCursor is rendered and what div it's inside
  </read_first>
  <action>
In `handlePointerMove` (around line 1325), the cursor position is calculated using `containerRef`:

```typescript
const container = containerRef.current;
if (container) {
  const rect = container.getBoundingClientRect();
  setCursorPos({ x: e.clientX - rect.left, y: e.clientY - rect.top });
}
```

Change this to use `overlayRef` instead, since PaintCursor is rendered as a child of the overlay div:

```typescript
const overlay = overlayRef.current;
if (overlay) {
  const rect = overlay.getBoundingClientRect();
  setCursorPos({ x: e.clientX - rect.left, y: e.clientY - rect.top });
}
```

Also check if there are OTHER places where cursorPos is set (search for `setCursorPos` in the file). If found in handlePointerDown or elsewhere, apply the same fix -- always use overlayRef, not containerRef.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -n "setCursorPos" app/src/components/canvas/PaintOverlay.tsx | head -5 && echo "---" && grep -B2 "setCursorPos" app/src/components/canvas/PaintOverlay.tsx | grep -c "containerRef" ; echo "expect 0"</automated>
  </verify>
  <acceptance_criteria>
    - ALL `setCursorPos` calls in PaintOverlay.tsx use `overlayRef.current.getBoundingClientRect()`, NOT `containerRef.current.getBoundingClientRect()`
    - No `containerRef` appears within 3 lines before any `setCursorPos` call
  </acceptance_criteria>
  <done>Circle cursor overlay is centered on the actual cursor position -- no offset between cursor circle and paint position</done>
</task>

</tasks>

<verification>
- Enter paint mode -> move cursor over canvas -> circle cursor center matches exact mouse position
- Zoom in/out -> cursor still aligned with paint position
</verification>

<success_criteria>
- cursorPos uses overlayRef coordinates exclusively
- Circle cursor visually centered on cursor position at all zoom levels
</success_criteria>

<output>
After completion, create `.planning/phases/33-enhance-current-engine/33-16-SUMMARY.md`
</output>
