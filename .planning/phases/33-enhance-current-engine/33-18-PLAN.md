---
phase: 33-enhance-current-engine
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/sidebar/InlineColorPicker.tsx
autonomous: true
requirements:
  - ECUR-10
gap_closure: true

must_haves:
  truths:
    - "Inline color picker renders correctly adjacent to canvas (not offset, picker visible inside container)"
  artifacts:
    - path: "app/src/components/sidebar/InlineColorPicker.tsx"
      provides: "Renders as normal child, no createPortal, no fixed positioning"
  key_links:
    - from: "app/src/components/sidebar/InlineColorPicker.tsx"
      to: "app/src/components/layout/CanvasArea.tsx"
      via: "Normal child render inside 260px flex container"
      pattern: "flex"
---

<objective>
Fix inline color picker positioning by removing createPortal and rendering as a normal child inside the CanvasArea flex container.

Purpose: The InlineColorPicker uses createPortal to render into document.body with hardcoded fixed positioning (left:60px, top:80px), completely bypassing the 260px container that CanvasArea already provides. The container appears empty while the picker floats at wrong coordinates.

Output: InlineColorPicker renders as a normal div (no portal, no fixed position) that fills its parent container.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove createPortal and fixed positioning from InlineColorPicker</name>
  <files>app/src/components/sidebar/InlineColorPicker.tsx</files>
  <read_first>
    - app/src/components/sidebar/InlineColorPicker.tsx (full file -- especially lines 1-2 for imports, line 337 for createPortal usage, lines 338-352 for the fixed positioning styles)
    - app/src/components/layout/CanvasArea.tsx (lines 366-399 -- the container that wraps InlineColorPicker)
  </read_first>
  <action>
**In app/src/components/sidebar/InlineColorPicker.tsx:**

1. Remove the `createPortal` import from line 2:
Change `import {createPortal} from 'preact/compat';` -- remove `createPortal` from the destructure. If it's the only import from preact/compat, remove the entire line.

2. Replace the `return createPortal(` on line 337 with a plain `return (`:

Change:
```typescript
return createPortal(
  <div
    class="fixed flex flex-col gap-2 rounded-xl shadow-2xl p-3 z-40"
    style={{
      width: '260px',
      left: '60px',
      top: '80px',
      backgroundColor: 'var(--sidebar-panel-bg)',
      border: '1px solid var(--sidebar-border-unselected)',
      maxHeight: 'calc(100vh - 100px)',
      overflowY: 'auto',
    }}
```

To:
```typescript
return (
  <div
    class="flex flex-col gap-2 p-3"
    style={{
      width: '100%',
      backgroundColor: 'var(--sidebar-panel-bg)',
      overflowY: 'auto',
      height: '100%',
    }}
```

Key changes:
- Remove `createPortal(` wrapper
- Remove `fixed` from class (no fixed positioning)
- Remove `rounded-xl shadow-2xl z-40` (it's a panel child, not a floating element)
- Remove `width: '260px'` (parent container already sets width)
- Remove `left: '60px'`, `top: '80px'` (no positioning needed)
- Remove `border` (parent handles border)
- Remove `maxHeight: 'calc(100vh - 100px)'` (use `height: '100%'` to fill container)
- Set `width: '100%'` and `height: '100%'` to fill the parent container

3. At the end of the component, remove the `, document.body)` that closes the createPortal call. Change the closing from `, document.body)` to just `)`.

4. Keep all internal content (header, mode tabs, canvas, sliders, swatches, hex input) unchanged.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "createPortal" app/src/components/sidebar/InlineColorPicker.tsx ; echo "expect 0" && grep -c "fixed" app/src/components/sidebar/InlineColorPicker.tsx ; echo "expect 0 or only in unrelated context" && grep -c "document.body" app/src/components/sidebar/InlineColorPicker.tsx ; echo "expect 0"</automated>
  </verify>
  <acceptance_criteria>
    - InlineColorPicker.tsx does NOT contain `createPortal`
    - InlineColorPicker.tsx does NOT contain `document.body`
    - InlineColorPicker.tsx root div does NOT have class `fixed`
    - InlineColorPicker.tsx root div does NOT have `left: '60px'` or `top: '80px'`
    - InlineColorPicker.tsx root div has `width: '100%'` and `height: '100%'`
  </acceptance_criteria>
  <done>Inline color picker renders correctly inside the CanvasArea 260px container, adjacent to canvas</done>
</task>

</tasks>

<verification>
- Click brush color button in paint mode -> color picker panel appears to LEFT of canvas in the 260px container
- Canvas shrinks horizontally to accommodate the picker
- All picker functionality works (mode tabs, canvas, sliders, swatches)
- Closing picker restores canvas to full width
</verification>

<success_criteria>
- No createPortal or fixed positioning in InlineColorPicker
- Picker renders inside the CanvasArea flex container
- Picker content is visible and interactive
</success_criteria>

<output>
After completion, create `.planning/phases/33-enhance-current-engine/33-18-SUMMARY.md`
</output>
