---
phase: 33-enhance-current-engine
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/sidebar/PaintProperties.tsx
autonomous: true
requirements:
  - ECUR-07
gap_closure: true

must_haves:
  truths:
    - "Exit Paint Mode button pulsate animation has no scale effect (glow only)"
  artifacts:
    - path: "app/src/components/sidebar/PaintProperties.tsx"
      provides: "pulsate keyframes with glow only, no transform:scale"
      contains: "@keyframes pulsate"
  key_links: []
---

<objective>
Remove scale animation from Exit Paint Mode button pulsate keyframes -- keep only glow (box-shadow + background-color).

Purpose: User explicitly requested "remove the scale animation" -- the scale(1.05) bounce is distracting.

Output: Pulsate animation uses only background-color and box-shadow transitions, no transform:scale.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove scale transforms from pulsate keyframes</name>
  <files>app/src/components/sidebar/PaintProperties.tsx</files>
  <read_first>
    - app/src/components/sidebar/PaintProperties.tsx (lines 110-125 -- the pulsate keyframes and button style)
  </read_first>
  <action>
In PaintProperties.tsx, find the `<style>` block with `@keyframes pulsate` (around lines 117-122). Replace it with:

```css
@keyframes pulsate {
  0%, 100% { background-color: #f97316; box-shadow: 0 0 0 0 rgba(249, 115, 22, 0.4); }
  50% { background-color: #dc2626; box-shadow: 0 0 12px 4px rgba(249, 115, 22, 0.6); }
}
```

Remove ALL `transform: scale(...)` entries from both the 0%/100% and 50% keyframe rules. The animation should only pulse the background color (orange to red) and the box-shadow glow.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -A5 "@keyframes pulsate" app/src/components/sidebar/PaintProperties.tsx | grep -c "scale" ; echo "expect 0"</automated>
  </verify>
  <acceptance_criteria>
    - The string "scale" does NOT appear inside the @keyframes pulsate block
    - The @keyframes pulsate block contains `background-color: #f97316` at 0%/100%
    - The @keyframes pulsate block contains `background-color: #dc2626` at 50%
    - The @keyframes pulsate block contains `box-shadow` at both 0%/100% and 50%
  </acceptance_criteria>
  <done>Exit Paint Mode button pulses with glow effect only, no scale bounce</done>
</task>

</tasks>

<verification>
- Enter paint mode -> Exit Paint Mode button pulses with color glow, button does NOT grow/shrink
</verification>

<success_criteria>
- Pulsate keyframes contain zero transform/scale properties
- Button still animates with orange-to-red glow
</success_criteria>

<output>
After completion, create `.planning/phases/33-enhance-current-engine/33-13-SUMMARY.md`
</output>
