---
status: complete
phase: 12.6-layer-sequence-auto-selection-and-key-photo-ux-fixes
source: [12.6-01-SUMMARY.md, 12.6-02-SUMMARY.md]
started: 2026-03-18T10:00:00Z
updated: 2026-03-18T10:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Layer Auto-Selection on Sequence Switch
expected: Switch between sequences in the sidebar. The top-most layer of the newly selected sequence should be auto-selected, and transform overlay handles should appear on the canvas.
result: pass

### 2. Key Photo Click-to-Seek with Layer Selection
expected: Click a key photo in the strip. The playhead should seek to that key photo's position on the timeline, and the top-most layer should be selected (showing transform handles).
result: issue
reported: "it works but only the horizontal position on the timeline is set, the vertical position is not setup"
severity: minor

### 3. Key Photo Highlight Ring Follows Scrubbing
expected: Scrub the timeline playhead across frames. The highlight ring on the key photo strip should move to whichever key photo corresponds to the current frame position — it tracks the playhead, not a separate click selection.
result: pass

### 4. Key Photo Strip Auto-Scroll on Scrub
expected: With many key photos in the strip, scrub the timeline so the active key photo is off-screen. The strip should auto-scroll to keep the highlighted key photo visible. During playback, auto-scroll should NOT occur — only after playback stops.
result: pass

### 5. Empty Sequence Prompt
expected: Select or create a sequence that has no key photos. The strip area should show a clickable "+ Add photos" prompt instead of an empty space.
result: pass

### 6. + Add Button Creates Sequence and Opens Importer
expected: Click the "+ Add" button in the sequence sidebar. A new sequence should be created, auto-selected, and the importer should open — all in one click.
result: pass

### 7. Multi-Select Confirm Mode in Importer
expected: After clicking "+ Add" and the importer opens, you should be in multi-select mode. Clicking photos shows checkmark overlays and ring highlights. A "Confirm" button appears showing the count of selected photos. Clicking Confirm adds those photos as key photos to the new sequence.
result: pass

### 8. Cancel Deletes Empty Pending Sequence
expected: Click "+ Add" to create a new sequence and open the importer. Instead of confirming, click Cancel. The newly created empty sequence should be automatically deleted/removed — no orphan empty sequences left behind.
result: pass

### 9. Existing Single-Click Import Still Works
expected: Open the importer through the normal flow (not via "+ Add"). Single-clicking a photo should still work as before (direct key photo selection), without entering multi-select mode.
result: pass

## Summary

total: 9
passed: 8
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "Clicking key photo seeks playhead AND scrolls timeline vertically to show the relevant layer"
  status: failed
  reason: "User reported: it works but only the horizontal position on the timeline is set, the vertical position is not setup"
  severity: minor
  test: 2
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
