---
phase: 34-standalone-demo-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/efx-physic-paint/package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - RUN-01
  - RUN-02
must_haves:
  truths:
    - "RUN-01: Repo-root pnpm dev:paint delegates to the standalone @efxlab/efx-physic-paint browser demo per D-05 and D-06."
    - "RUN-02: Package demo launch scripts are separate from package build/check scripts per D-11."
  artifacts:
    - path: "package.json"
      provides: "Root dev:paint script"
      contains: "pnpm --filter @efxlab/efx-physic-paint demo:dev"
    - path: "packages/efx-physic-paint/package.json"
      provides: "Package demo:dev and demo:build scripts plus Vite demo dev dependencies"
      contains: "demo:dev"
    - path: "pnpm-lock.yaml"
      provides: "Workspace lockfile entries for any newly added demo dev dependencies"
  key_links:
    - from: "package.json scripts.dev:paint"
      to: "packages/efx-physic-paint/package.json scripts.demo:dev"
      via: "pnpm --filter @efxlab/efx-physic-paint demo:dev"
      pattern: "pnpm --filter @efxlab/efx-physic-paint demo:dev"
    - from: "packages/efx-physic-paint/package.json scripts.demo:build"
      to: "packages/efx-physic-paint/demo/vite.config.ts"
      via: "vite build --config demo/vite.config.ts"
      pattern: "vite build --config demo/vite.config.ts"
---

<objective>
Create the workspace command contract for Phase 34.

Purpose: RUN-01 and RUN-02 require a repo-root launch command for the standalone paint browser demo while keeping library build/type verification separate from demo launch. This implements D-05, D-06, and D-11 without adding editor/Tauri coupling.
Output: Root and package scripts point to a package-local Vite demo command, with approved demo-only dev dependencies available to the package.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-standalone-demo-shell/34-CONTEXT.md
@.planning/phases/34-standalone-demo-shell/34-RESEARCH.md
@.planning/phases/34-standalone-demo-shell/34-PATTERNS.md
@package.json
@packages/efx-physic-paint/package.json
@pnpm-workspace.yaml
</context>

## Artifacts this phase produces

- Root package script: `package.json#scripts.dev:paint = "pnpm --filter @efxlab/efx-physic-paint demo:dev"`.
- Package script: `packages/efx-physic-paint/package.json#scripts.demo:dev = "vite --config demo/vite.config.ts"`.
- Package script: `packages/efx-physic-paint/package.json#scripts.demo:build = "vite build --config demo/vite.config.ts"`.
- Package dev dependency: `packages/efx-physic-paint/package.json#devDependencies.vite = "5.4.21"` unless already present at an equivalent project-pinned version.
- Package dev dependency: `packages/efx-physic-paint/package.json#devDependencies.@preact/preset-vite = "^2.10.5"` unless already present at an equivalent compatible version.
- Lockfile entries in `pnpm-lock.yaml` for any newly added package dev dependencies.

<tasks>

<task type="auto">
  <name>Task 1: Retarget root standalone paint command</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - pnpm-workspace.yaml
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
    - .planning/phases/34-standalone-demo-shell/34-PATTERNS.md
  </read_first>
  <action>Update root `package.json` so `scripts.dev:paint` is exactly `pnpm --filter @efxlab/efx-physic-paint demo:dev` per D-05 and D-06. Preserve the existing pnpm-filter script style. Do not add a root alias for the old package `dev:watch` behavior per D-06. Do not create any `cd packages/efx-physic-paint` shell script. Do not change `scripts.dev`; it remains the app Vite frontend workflow that D-07 requires README documentation to distinguish later.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.scripts['dev:paint'] !== 'pnpm --filter @efxlab/efx-physic-paint demo:dev') { throw new Error('root dev:paint must delegate to package demo:dev'); }"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `package.json` contains `"dev:paint": "pnpm --filter @efxlab/efx-physic-paint demo:dev"`.
    - Source assertion: `package.json` does not contain `"dev:paint": "pnpm --filter @efxlab/efx-physic-paint dev:watch"`.
    - Source assertion: no root script added in this task contains `cd packages/efx-physic-paint`.
  </acceptance_criteria>
  <done>Repo-root `pnpm dev:paint` is a stable alias for the package-local standalone browser demo command, not the old tsup watch command.</done>
</task>

<task type="auto">
  <name>Task 2: Add package-local demo scripts and approved dev dependencies</name>
  <files>packages/efx-physic-paint/package.json, pnpm-lock.yaml</files>
  <read_first>
    - packages/efx-physic-paint/package.json
    - pnpm-lock.yaml
    - .planning/phases/34-standalone-demo-shell/34-RESEARCH.md
    - .planning/phases/34-standalone-demo-shell/34-PATTERNS.md
  </read_first>
  <action>Add `scripts.demo:dev` with value `vite --config demo/vite.config.ts` and `scripts.demo:build` with value `vite build --config demo/vite.config.ts` next to the existing `build`, `dev:watch`, and `check` scripts per D-11. Keep `scripts.build` as `tsup`, `scripts.dev:watch` as `tsup --watch`, and `scripts.check` as `tsc --noEmit`. Add approved dev dependencies with pnpm, not npm: `vite@5.4.21` and `@preact/preset-vite@^2.10.5`, unless already present at compatible values. Preserve existing `preact` optional peer dependency and dev dependency. Do not add demo paths to `exports`, `files`, or package publish configuration per D-02. The package legitimacy audit in 34-RESEARCH.md approves `vite` and `@preact/preset-vite`; `preact` is existing and intentionally used by this package.</action>
  <verify>
    <automated>node -e "const p=require('./packages/efx-physic-paint/package.json'); const s=p.scripts||{}; if (s['demo:dev'] !== 'vite --config demo/vite.config.ts') throw new Error('missing demo:dev'); if (s['demo:build'] !== 'vite build --config demo/vite.config.ts') throw new Error('missing demo:build'); if (s.build !== 'tsup' || s['dev:watch'] !== 'tsup --watch' || s.check !== 'tsc --noEmit') throw new Error('library scripts changed unexpectedly'); if (JSON.stringify(p.exports||{}).includes('demo')) throw new Error('demo must not be exported'); if (JSON.stringify(p.files||[]).includes('demo')) throw new Error('demo must not be published through files'); if (!p.devDependencies?.vite || !p.devDependencies?.['@preact/preset-vite']) throw new Error('missing demo dev dependencies');"</automated>
    <automated>pnpm --filter @efxlab/efx-physic-paint check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/efx-physic-paint/package.json` contains `"demo:dev": "vite --config demo/vite.config.ts"`.
    - Source assertion: `packages/efx-physic-paint/package.json` contains `"demo:build": "vite build --config demo/vite.config.ts"`.
    - Source assertion: `packages/efx-physic-paint/package.json` keeps `"build": "tsup"`, `"dev:watch": "tsup --watch"`, and `"check": "tsc --noEmit"`.
    - Source assertion: `packages/efx-physic-paint/package.json` `exports` has no `./demo` key and `files` remains limited to library output such as `dist`.
    - CLI assertion: `pnpm --filter @efxlab/efx-physic-paint check` exits 0.
  </acceptance_criteria>
  <done>The package has separate demo launch/build scripts and dependency metadata without changing library build/export boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer shell -> pnpm workspace scripts | Local commands execute package scripts from `package.json`; tampered script values can execute unexpected code. |
| Package manager -> npm registry | New dev dependency metadata and tarballs enter the local workspace lockfile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `package.json` scripts | mitigate | Pin `dev:paint` to `pnpm --filter @efxlab/efx-physic-paint demo:dev`; verify with a Node script assertion instead of relying on prose. |
| T-34-02 | Tampering | `packages/efx-physic-paint/package.json` scripts/exports/files | mitigate | Keep `demo:dev` and `demo:build` separate from `build`/`dev:watch`/`check`; verify no `demo` appears in `exports` or `files`. |
| T-34-SC | Tampering | pnpm installs | mitigate | Use only packages approved in `34-RESEARCH.md` Package Legitimacy Audit: `vite@5.4.21` and `@preact/preset-vite@^2.10.5`; use pnpm, not npm. |
</threat_model>

<verification>
Run the task-level automated checks. Do not run `pnpm dev`, `pnpm dev:paint`, Vite dev server, or Tauri dev server; the user runs servers per project CLAUDE.md.
</verification>

<success_criteria>
- RUN-01 command contract exists at root.
- RUN-02 script split exists in the package.
- D-02 and D-11 boundaries are preserved by static assertions.
</success_criteria>

<output>
Create `.planning/phases/34-standalone-demo-shell/34-01-SUMMARY.md` when done.
</output>
