---
phase: 44-integrated-uat-signed-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/src-tauri/tauri.conf.json
  - app/src-tauri/Cargo.toml
  - app/src-tauri/Cargo.lock
  - scripts/macos-release.sh
  - .planning/phases/44-integrated-uat-signed-release/44-GATES.md
autonomous: true
requirements:
  - REL-01
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five product version surfaces read 0.9.0 and agree with app/package.json as single source"
    - "app/src/releaseContract.test.ts passes (the single-source agreement oracle for the 5-surface bump)"
    - "All six REL-01 gates run green in the exact D-03 order AFTER the bump lands (compile-proof gates run against contract-stable 0.9.0 code, Pitfall 8)"
    - "Stale v0.8.1 bundles are archived so find_release_artifacts (exactly-one-or-fatal) will find zero stale artifacts before the credentialed run"
    - "Per-gate exit status evidence is recorded in 44-GATES.md — never assumed from a previous session"
  artifacts:
    - app/package.json (version "0.9.0")
    - app/src-tauri/tauri.conf.json (version "0.9.0")
    - app/src-tauri/Cargo.toml (version "0.9.0")
    - app/src-tauri/Cargo.lock (efx-motion-editor version "0.9.0" only — one changed line)
    - scripts/macos-release.sh (PRODUCT_VERSION="0.9.0" on line 10 — the only permitted edit to this file this phase)
    - .planning/phases/44-integrated-uat-signed-release/44-GATES.md (per-gate real exit-status record)
  key_links:
    - "app/package.json <-> tauri.conf.json / Cargo.toml / Cargo.lock / macos-release.sh via releaseContract.test.ts:53-61 (single-source agreement)"
    - "5-surface bump -> releaseContract.test.ts green -> six-gate run (Pitfall 8: compile-proof gates must run after the contract change)"
    - "stale-bundle archival -> exactly-one-or-fatal artifact discovery (Pitfall 2)"
---

<objective>
Pre-release gate plan: bump every product version surface from 0.8.1 to 0.9.0 in one atomic change, archive the stale v0.8.1 release bundles, then prove all six REL-01 automated gates green in the locked D-03 order with per-gate exit-status evidence.

Purpose: Without this plan, a `release` run would rebuild, sign, and notarize the already-published v0.8.1 (Pitfall 1). This plan makes the contract-stable v0.9.0 state the gate input and removes the stale-artifact ambiguity (Pitfall 2) that would make artifact discovery fatal.
Output: five version surfaces at 0.9.0 (one atomic commit), archived stale bundles, six gate exit statuses recorded in 44-GATES.md, and the two recorded spec-vs-implementation divergences that the signed UAT will judge against shipped values (chunk budget, truncation label).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/scripts/macos-release.sh
@/Users/lmarques/Dev/efx-motion-editor/app/src/releaseContract.test.ts
</context>

# Artifacts this phase produces

| Artifact | Change | Evidence |
|----------|--------|----------|
| `app/package.json` | `"version": "0.8.1"` → `"0.9.0"` (line 4) | `releaseContract.test.ts` single-source assertion |
| `app/src-tauri/tauri.conf.json` | `"version": "0.8.1"` → `"0.9.0"` (line 4) | same |
| `app/src-tauri/Cargo.toml` | `version = "0.8.1"` → `version = "0.9.0"` (line 3) | same |
| `app/src-tauri/Cargo.lock` | `efx-motion-editor` version `"0.8.1"` → `"0.9.0"` (lines 971-972, ONLY this entry) | `git diff --stat` exactly one changed line |
| `scripts/macos-release.sh` | `PRODUCT_VERSION="0.8.1"` → `PRODUCT_VERSION="0.9.0"` (line 10 — the ONLY permitted edit to this frozen file) | same |
| `app/src-tauri/target/release/bundle/**` | stale v0.8.1 `.app` + `_0.8.1_*.dmg` moved to `archive-v0.8.1-<date>/` (not deleted) | zero `.app` / `_0.8.1_*.dmg` remain |
| `.planning/phases/44-integrated-uat-signed-release/44-GATES.md` | NEW — per-gate exit-status evidence + divergence records | committed with the bump commit |
| `.planning/phases/44-integrated-uat-signed-release/44-COVERAGE.md` | NEW — API-coverage reasoned declaration (no external API integration) | committed |

# Flagged assumptions (from the edge probe — surfaced, not auto-resolved)

1. **REL-01 adjacency/equality** — the five version surfaces must be *exactly equal* (`expect(tauriConfig.version).toBe(expected)`); equality is strict, never fuzzy. The bump therefore edits exact string fields only.
2. **REL-01 empty input** — all six gates run against the populated repo; there is no empty-input case. `bash -n scripts/macos-release.sh` validates syntax of a non-empty file; preflight probes a full environment.
3. **REL-01 ordering** — gate order is the locked D-03 order and is significant (Pitfall 8): compile-proof gates run only after the contract-stable 0.9.0 bump. No gate is re-ordered or skipped.
4. **Chunk-budget divergence (REL-01 encoding)** — spec Phase 5 references an 1100 kB desktop budget; the implementation and `viteBuild.test.ts:138` assert the amended **1120** budget. Gate on 1120 (reverting to 1100 would fail the build for no benefit). Recorded as a known divergence in 44-GATES.md; do NOT change the code.
5. **Truncation-label divergence (carried to UAT)** — spec Phase 5 step 12 asserts the French label `Boucle raccourcie par le clip suivant`; the shipped 43-approved implementation uses the English label `Loop shortened by next clip`. This plan does not touch the label (D-09); the signed-app UAT in plan 02 judges against the shipped English label and records the divergence.

<tasks>

<task type="tracer">
  <name>Task 1: v0.9.0 version bump across all five product surfaces (atomic) + single-source contract green</name>
  <files>app/package.json, app/src-tauri/tauri.conf.json, app/src-tauri/Cargo.toml, app/src-tauri/Cargo.lock, scripts/macos-release.sh</files>
  <read_first>
    - app/package.json — the single source of truth (`"version"` at line 4)
    - app/src-tauri/tauri.conf.json — `"version"` at line 4 only; do NOT touch bundle.icon/csp/identifier/productName (each separately contract-pinned by releaseContract.test.ts:63-162)
    - app/src-tauri/Cargo.toml — `version = "0.8.1"` at line 3
    - app/src-tauri/Cargo.lock — `efx-motion-editor` package block at lines 971-972
    - scripts/macos-release.sh — `PRODUCT_VERSION="0.8.1"` at line 10; this is the ONLY permitted modification to the frozen release script
    - app/src/releaseContract.test.ts — the acceptance oracle (single-source assertion at lines 52-61; parse helpers at lines 33-45)
  </read_first>
  <action>
    Edit ONLY the version fields listed, to `0.9.0`, in ONE atomic change (per D-03/Pitfall 1 — this is the contract change that must precede every gate run):

    1. app/package.json line 4: `"version": "0.8.1"` → `"version": "0.9.0"` (single source of truth).
    2. app/src-tauri/tauri.conf.json line 4: `"version": "0.8.1"` → `"version": "0.9.0"`. Do NOT modify bundle.icon, csp, identifier, productName, beforeBuildCommand, frontendDist, or hardenedRuntime — `validate_tauri_config` (scripts/macos-release.sh:93-146) and releaseContract.test.ts pin each and fail on drift.
    3. app/src-tauri/Cargo.toml line 3: `version = "0.8.1"` → `version = "0.9.0"`.
    4. app/src-tauri/Cargo.lock lines 971-972: change ONLY the `version = "0.8.1"` inside the `[[package]] name = "efx-motion-editor"` block to `version = "0.9.0"`. NEVER global-replace the string `0.8.1` — transitive dependencies legitimately contain it. After the edit run `git diff --stat app/src-tauri/Cargo.lock` and confirm exactly one changed line.
    5. scripts/macos-release.sh line 10: `PRODUCT_VERSION="0.8.1"` → `PRODUCT_VERSION="0.9.0"`. This is the single allowed modification to this frozen file all phase; the DMG glob (`*_${PRODUCT_VERSION}_*.dmg`) and validate_tauri_config version check read this variable.

    Run `pnpm --dir app exec vitest run app/src/releaseContract.test.ts` — it asserts packageJson.version equals tauriConfig.version, Cargo.toml package version, Cargo.lock efx-motion-editor version, and PRODUCT_VERSION (lines 53-61). It must now PASS; it would have FAILED while any surface still read 0.8.1.

    COMMIT ONE atomic change staging exactly the five files: `chore(release): bump product version to 0.9.0`.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/releaseContract.test.ts 2>&1 | grep -q "Tests.*pass" && grep -q '"version": "0.9.0"' app/package.json && grep -q '"version": "0.9.0"' app/src-tauri/tauri.conf.json && grep -q '^version = "0.9.0"' app/src-tauri/Cargo.toml && grep -q -A1 'name = "efx-motion-editor"' app/src-tauri/Cargo.lock | grep -q '"0.9.0"' && grep -q '^PRODUCT_VERSION="0.9.0"' scripts/macos-release.sh && echo "5-surface bump verified"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"version"' app/package.json` → `"version": "0.9.0"`
    - `grep -n '"version"' app/src-tauri/tauri.conf.json` → `"version": "0.9.0"`
    - `grep -n '^version' app/src-tauri/Cargo.toml` → `version = "0.9.0"`
    - `grep -n -A1 'name = "efx-motion-editor"' app/src-tauri/Cargo.lock` → `version = "0.9.0"` (line 972)
    - `grep -n '^PRODUCT_VERSION' scripts/macos-release.sh` → `PRODUCT_VERSION="0.9.0"`
    - `git diff --stat app/src-tauri/Cargo.lock` → exactly one changed line
    - `pnpm --dir app exec vitest run app/src/releaseContract.test.ts` → passing (all 5 surfaces agree)
    - one commit `chore(release): bump product version to 0.9.0` containing only the five files
  </acceptance_criteria>
  <done>
    All five surfaces read 0.9.0; Cargo.lock diff is exactly one line; releaseContract.test.ts is green; committed as one atomic `chore(release)` commit with only the five surface files staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Archive stale v0.8.1 bundles + run the six REL-01 gates in exact D-03 order with per-gate evidence</name>
  <files>.planning/phases/44-integrated-uat-signed-release/44-GATES.md (NEW evidence record)</files>
  <read_first>
    .planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md — Pitfall 2 (stale-artifact ambiguity, exactly-one-or-fatal), Pitfall 8 (gate order after contract change), Environment Availability
    scripts/macos-release.sh — read-only; `find_release_artifacts` at lines ~341-357 is exactly-one-or-fatal; preflight mode at main() lines ~460-482
    app/src/viteBuild.test.ts — chunk budget assertion at line 138 (1120, the amended implementation contract)
    .planning/phases/44-integrated-uat-signed-release/44-PATTERNS.md — Shared Pattern 2 (gate-run command order)
  </read_first>
  <action>
    1. ARCHIVE stale bundles (do NOT delete — keep v0.8.1 DMG locally per Open Question 3). Create `app/src-tauri/target/release/bundle/archive-v0.8.1-<YYYYMMDD>/` and move:
       - `app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app` (stale 7-août, inner binary `Contents/MacOS/efx-motion-editor` 26 MB)
       - `app/src-tauri/target/release/bundle/dmg/EFX Motion Editor_0.8.1_aarch64.dmg` (stale 4-août)
       After moving, confirm zero `.app` under `bundle/macos/` and zero `*_0.8.1_*.dmg` under `bundle/dmg/` so the exactly-one-or-fatal discovery finds no stale artifacts. Record the archived count and destination in 44-GATES.md.
    2. Run the six REL-01 gates IN EXACT D-03 ORDER (never reorder — compile-proof gates run only after the Task 1 contract change):
       1. `pnpm --dir app exec vitest run` — expect exit 0, ~138 files / ~2675 tests
       2. `pnpm --dir app run typecheck` — expect exit 0
       3. `pnpm build` — expect exit 0 (the viteBuild hermetic build runs inside vitest too; chunk budget asserts 1120 — record the spec-1100-vs-actual-1120 divergence, do NOT change the budget)
       4. `cargo test --manifest-path app/src-tauri/Cargo.toml` — expect exit 0, ~20 tests
       5. `bash -n scripts/macos-release.sh` — expect exit 0 (syntax)
       6. `bash scripts/macos-release.sh preflight` — expect output `PREFLIGHT PASS: release configuration, Apple CLI capabilities, resources, and private-asset guards`, exit 0
       Capture the REAL exit status and a key output marker for every gate (per 44-VALIDATION sampling: full suite at wave end).
    3. Write `.planning/phases/44-integrated-uat-signed-release/44-GATES.md` recording: for each gate the exact command, real exit status, timestamp, and expected-output marker observed; the archived-bundle count; the recorded divergences (chunk budget 1100 spec vs 1120 implementation; truncation-label French vs English deferred to UAT evidence); and the note that the six gates ran green AFTER the 0.9.0 bump (Pitfall 8 satisfied).
    4. Commit with the bump commit already staged: `chore(phase-44): record 0.9.0 gate evidence + archive stale v0.8.1 bundles` (staging only 44-GATES.md; target/ is git-ignored so the archival itself is untracked).
  </action>
  <verify>
    <automated>bash scripts/macos-release.sh preflight 2>&1 | grep -q 'PREFLIGHT PASS' && echo "preflight PASS" && grep -c '^REL-01' .planning/phases/44-integrated-uat-signed-release/44-GATES.md 2>/dev/null | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `find app/src-tauri/target/release/bundle -name '*.app' -maxdepth 3` → empty; `find app/src-tauri/target/release/bundle/dmg -name '*_0.8.1_*.dmg'` → empty
    - `.planning/phases/44-integrated-uat-signed-release/44-GATES.md` exists and lists all six gate commands with their observed exit statuses and markers
    - `bash scripts/macos-release.sh preflight` prints `PREFLIGHT PASS`
    - no functional source file changed (D-09 — the only repo file edits are the five version surfaces + the evidence record)
  </acceptance_criteria>
  <done>
    Stale v0.8.1 bundles archived; all six REL-01 gates pass in exact D-03 order after the bump with each exit status recorded in 44-GATES.md; the 1100-vs-1120 chunk-budget divergence recorded; no functional code changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → release script | `scripts/macos-release.sh` is frozen except PRODUCT_VERSION; the private-asset guard fails the release if any `.p8/.p12/.key` appears in the worktree |
| stale bundles → artifact discovery | `find_release_artifacts` is exactly-one-or-fatal; stale v0.8.1 bundles under `target/` could abort or confuse the release run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Tampering | version surfaces | high | mitigate | single-source `releaseContract.test.ts` assertion (5 surfaces agree) + one atomic 5-file bump commit (Task 1) |
| T-44-02 | Tampering/Repudiation | stale bundle discovery | high | mitigate | archive (not delete) stale `.app`/`_0.8.1_*.dmg` before the credentialed run; record count (Task 2) |
| T-44-03 | Tampering | gate evidence | medium | mitigate | every gate exit status captured explicitly into 44-GATES.md — never assumed or reused from a prior session (D-03) |
| T-44-04 | Information Disclosure | credential boundary | high | mitigate | no plan task exports/echoes Apple credentials; agent never writes private-asset files; preflight private-asset guard stays untouched (D-04, RESEARCH Pitfall 7) |
| T-44-SC | Tampering | package installs | high | mitigate | RESEARCH Package Legitimacy Audit confirms ZERO packages installed this phase — the 5-surface bump is a config change, not an install; any proposed install in an execution task is a D-09 scope violation |
</threat_model>

<verification>
Run in order, all must pass (sampling after every task commit + plan end per 44-VALIDATION):
1. `pnpm --dir app exec vitest run app/src/releaseContract.test.ts` — green post-bump
2. `git diff --stat app/src-tauri/Cargo.lock` — exactly one changed line
3. `bash scripts/macos-release.sh preflight` — `PREFLIGHT PASS`
4. Full suite: `pnpm --dir app exec vitest run && pnpm --dir app run typecheck && pnpm build && cargo test --manifest-path app/src-tauri/Cargo.toml` — all green (~180s)
5. `git log --oneline -2` shows the atomic bump commit and the evidence commit

Hard boundaries: no Apple credential file accessed, no `release`/`verify-downloaded` run (user-owned in later plans), no tag created, no push, no dev server started, no Vitest watch mode, no global version replace in Cargo.lock, no modification of scripts/macos-release.sh beyond line 10.
</verification>

<success_criteria>
- All five product version surfaces read 0.9.0 with a single-source contract test green.
- Stale v0.8.1 bundles archived so exactly-one-or-fatal discovery is satisfied.
- All six REL-01 gates pass in the locked D-03 order after the bump; per-gate evidence recorded.
- The chunk-budget divergence (spec 1100 vs implementation 1120) is recorded, not "fixed".
- No functional code changed (D-09).
</success_criteria>

<output>
Create `.planning/phases/44-integrated-uat-signed-release/44-01-SUMMARY.md` when done, including: the commit hashes (atomic bump + evidence), the six gate exit-status evidence summary, the archived-bundle count, and the two recorded spec-vs-implementation divergences.
</output>
