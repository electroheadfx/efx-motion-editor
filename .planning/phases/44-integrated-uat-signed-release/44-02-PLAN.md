---
phase: 44-integrated-uat-signed-release
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - .planning/phases/44-integrated-uat-signed-release/44-RELEASE.md (NEW — user-reported release ledger)
  - .planning/phases/44-integrated-uat-signed-release/44-UAT.md (NEW — signed-app UAT evidence record)
autonomous: false
requirements:
  - REL-01
  - REL-02
user_setup:
  - service: "efx-release-efx-motion wrapper (user-owned)"
    why: "The credentialed release run (D-04) is user-run via this wrapper; credentials never enter the repo or agent context"
    env_vars:
      - name: APPLE_SIGNING_IDENTITY
        source: "Trusted Apple environment file, drag-and-dropped at the wrapper prompt"
      - name: APPLE_API_ISSUER
        source: "Trusted Apple environment file"
      - name: APPLE_API_KEY
        source: "Trusted Apple environment file"
      - name: APPLE_API_KEY_PATH
        source: "Trusted Apple environment file (resolved absolute + readable; validated by require_release_credentials)"
    dashboard_config:
      - task: "Run ~/.config/efx/scripts/efx-release-efx-motion from the repo root when the release window opens; the wrapper sources the env file, validates the four vars, runs bash scripts/macos-release.sh release, and trap-unsets them"
        location: "User-owned wrapper (mode 700), outside the repo"
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A v0.9.0 signed/notarized/stapled macOS artifact exists (RELEASE PASS ledger reported by the user via efx-release-efx-motion, D-04)"
    - "Artifact freshness is judged by app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app + inner binary Contents/MacOS/efx-motion-editor timestamps, never the bundle/dmg/ folder (D-05, Pitfall 3)"
    - "The comprehensive packaged-app UAT passes every spec step (1-17) in one pass in the packaged app (D-01)"
    - "The Phase 43 signed-artifact boundary is covered as a first-class UAT step: valid linked-loop preview/export parity AND the unresolved-loop export block (D-02 — never silently dropped)"
    - "UAT evidence records the two spec-vs-implementation divergences (truncation label French-vs-English; chunk budget 1100-vs-1120) and judges against the shipped values"
  artifacts:
    - app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app (signed, notarized, stapled, v0.9.0)
    - app/src-tauri/target/release/bundle/dmg/EFX Motion Editor_0.9.0_aarch64.dmg (signed, notarized, stapled)
    - app/src-tauri/target/dmg/notarization-evidence/* (Apple notarization evidence JSON)
    - .planning/phases/44-integrated-uat-signed-release/44-RELEASE.md (user-reported RELEASE PASS ledger)
    - .planning/phases/44-integrated-uat-signed-release/44-UAT.md (signed-app UAT step evidence)
  key_links:
    - "preflight PASS + archived stale bundles (44-01) -> user release run -> signed 0.9.0 artifact"
    - "bundle/macos/*.app + inner-binary mtime <-> UAT validity (D-05 freshness, Pitfall 3)"
    - "43-UAT.md §11/§16 approved expectations <-> signed-artifact boundary re-proof (D-02)"
---

<objective>
User-run credentialed release + comprehensive signed packaged-app UAT. The user runs the one credentialed step — `~/.config/efx/scripts/efx-release-efx-motion` — through the locked D-04 wrapper (credentials never enter the repo, project files, or agent context), and then walks the full spec UAT step list (REL-02, D-01) plus the Phase 43 signed-artifact boundary (D-02) in the packaged app.

Purpose: The signed/notarized artifact is the definition-of-done proof for v0.9.0. No gate result is trusted until the user's native packaged-app UAT passes on the signed app; the Phase 43 handoff (valid linked-loop preview/export parity + unresolved-loop export block) is verified on the signed artifact here and never silently dropped.
Output: user-reported RELEASE PASS ledger (44-RELEASE.md), signed-artifact freshness evidence, comprehensive UAT evidence (44-UAT.md) covering spec steps 1-17 + Phase 43 boundary with the two recorded divergences.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/SPECS/milestone-v0.9.0-plan.md (Phase 5 Native UAT, lines 589-609)
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md (§11 Repair/Relink unresolved, §16 Valid-loop preview/export parity)
</context>

# Artifacts this phase produces

| Artifact | Change | Evidence |
|----------|--------|----------|
| `app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app` | fresh v0.9.0 signed/notarized/stapled app (replaces the archived 0.8.1 bundle) | inner binary `Contents/MacOS/efx-motion-editor` mtime ≥ release day; user-reported RELEASE PASS |
| `app/src-tauri/target/release/bundle/dmg/EFX Motion Editor_0.9.0_aarch64.dmg` | fresh v0.9.0 signed/notarized/stapled DMG | `RELEASE PASS` + notarization evidence under `target/dmg/notarization-evidence/` |
| `.planning/phases/44-integrated-uat-signed-release/44-RELEASE.md` | NEW — user-reported release ledger (RELEASE PASS, notarization Accepted, stapler PASS) | recorded |
| `.planning/phases/44-integrated-uat-signed-release/44-UAT.md` | NEW — signed-app UAT evidence (spec steps 1-17 + Phase 43 boundary + divergence records) | user visual confirmation + summarized thresholds |

# Flagged assumptions (from the edge probe — surfaced, not auto-resolved)

1. **REL-02 truncation-label encoding (edge: encoding)** — spec step 12 asserts the French `Boucle raccourcie par le clip suivant`; the shipped 43-approved English label is `Loop shortened by next clip`. The UAT asserts the SHIPPED English label as the acceptance oracle and records the French/spec divergence — never a code change (D-09, RESEARCH Pitfall 5).
2. **REL-02 chunk-budget (edge: encoding)** — the UAT judge the shipped 1120 budget (viteBuild.test.ts) and records the spec-1100 divergence (Pitfall 6).
3. **REL-02 idempotency (edge)** — the UAT is one comprehensive pass (D-01); a failing step can be re-run in isolation (reversibility) without re-running the whole pass.
4. **REL-02 concurrency (edge)** — UAT steps run sequentially in the packaged app; no parallel app sessions. Any failure stops the pass and flags the user (D-08); no silent pass.
5. **REL-02 adjacency (edge)** — "no overlap" between loop and next clip is judged visually at the half-open interval boundary (per 43-VALIDATION coverage); the 43-approved UAT expectations are the reference, not a fresh spec reading.
6. **REL-03 unclassified (edge, manual review)** — the final publish decision is a human review recorded via the stop-condition checklist in plan 03, not an automated assert.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Credentialed signed release via efx-release-efx-motion (user-run) + freshness evidence</name>
  <read_first>
    .planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md — D-04 (user-run release via wrapper), D-05 (packaged-only proof, freshness)
    .planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md — Pattern 1 (credential-isolated handoff), Pitfall 2 (stale artifacts), Pitfall 3 (freshness)
    docs/macos-signed-release.md — credential env contract, modes table, planned sequence
    scripts/macos-release.sh — read-only; release mode + verify_app/verify_dmg
  </read_first>
  <instructions>
    AGENT side (done BEFORE presenting the checkpoint — a broken environment is never presented):
    1. Confirm `bash scripts/macos-release.sh preflight` passes (credential-free).
    2. Confirm zero stale bundles: `find app/src-tauri/target/release/bundle/macos -maxdepth 1 -name '*.app'` is empty and `find app/src-tauri/target/release/bundle/dmg -name '*_0.8.1_*.dmg'` is empty (44-01 archived them).
    3. Confirm the wrapper exists at `~/.config/efx/scripts/efx-release-efx-motion` (mode 700, executable).
    4. Record these pre-conditions in the release ledger.
    5. Present the checkpoint.

    USER action (the ONE credentialed step — D-04): run the wrapper from a user terminal:
      ~/.config/efx/scripts/efx-release-efx-motion
    The wrapper prompts to drag the trusted Apple environment file, sources it, validates the four credential env vars (APPLE_SIGNING_IDENTITY, APPLE_API_ISSUER, APPLE_API_KEY, APPLE_API_KEY_PATH), runs `bash scripts/macos-release.sh release` from the repo root, then trap-unsets every variable on exit. If the wrapper is unavailable, the documented manual 4-export flow in docs/macos-signed-release.md is the fallback (also user-run).

    USER reports back the script output (full ledger). After the report, the agent:
    6. Records the user-reported ledger in 44-RELEASE.md: RELEASE PASS line, app notarization status (Accepted), DMG notarization status (Accepted), app stapler validation (PASS), DMG stapler validation (PASS).
    7. Verifies freshness (D-05, Pitfall 3) via `stat -f "%Sm" app/src-tauri/target/release/bundle/macos/EFX Motion Editor.app/Contents/MacOS/efx-motion-editor` and the `.app` directory mtime — BOTH must postdate the release run. NEVER judge by `bundle/dmg/` timestamps (the folder can hold stale DMGs).
    8. Verify the built DMG is named `EFX Motion Editor_0.9.0_aarch64.dmg` (the `_0.9.0_` glob matches — Pitfall 1 sign).
    9. Verify notarization evidence exists under `app/src-tauri/target/dmg/notarization-evidence/`.
  </read_first>
  <action>
    The release run is USER-ONLY (D-04): credentials must never enter the repo, project files, or agent context. This checkpoint is `gate="blocking-human"` and is never auto-approved in any mode — the release cannot be automated from the agent session.
  </action>
  <verification>
    Agent re-checks after the user reports: inner binary `Contents/MacOS/efx-motion-editor` mtime is fresh (≥ release run time); `bundle/dmg/` contains exactly one `EFX Motion Editor_0.9.0_aarch64.dmg`; `RELEASE PASS` present in user output; notarization evidence files exist under `target/dmg/notarization-evidence/`.
  </verification>
  <resume-signal>Paste the wrapper output (or say "done" with the full output attached).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Comprehensive signed packaged-app UAT — spec steps 1-17 + Phase 43 signed-artifact boundary</name>
  <read_first>
    SPECS/milestone-v0.9.0-plan.md — Phase 5 Native UAT steps 1-17 (lines 589-609) and the release stop-condition list (lines 611-629)
    .planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md — D-01 (one comprehensive pass), D-02 (Phase 43 handoff folded in, never dropped), D-05
    .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md — §11 Repair/Relink unresolved (expected: `Loop source missing` marks preview; export fails before partial output), §16 Valid-loop preview/export parity (export frame count/order matches on-screen preview; no blank or unresolved placeholder in valid export)
    .planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md — Pitfall 5 (truncation label divergence), Pitfall 6 (chunk budget 1120)
  </read_first>
  <what-built>
    The signed, notarized, stapled EFX Motion Editor v0.9.0 packaged app (from Task 1). This is the definition-of-done UAT pass for REL-02.
  </what-built>
  <how-to-verify>
    Open the packaged v0.9.0 app and walk the numbered list in sequence (D-01 one comprehensive pass). Judge against shipped English labels/values (D-01 reversibility: a failing step can be re-run in isolation). Record each step's pass/fail in 44-UAT.md with the expected-outcome marker:

    1. Icon surfaces — Finder, Dock, Applications, application switcher, mounted DMG: legible, clean transparent corners, no placeholder artwork.
    2. Open a saved project containing at least one project script; open one parent Paint layer in EFX Paint.
    3. Do NOT click Refresh — existing script rows appear automatically; Save Script is enabled.
    4. Close/reopen EFX Paint — hydration occurs exactly once, no duplicate rows/listeners; Copy/Apply remain functional.
    5. Hear main-editor audio at the correct Paint frame.
    6. Seek, play, pause, loop, stop — no drift, no doubled audio.
    7. Disable local audio preview — main-editor audio metadata unchanged.
    8. Apply a PlayScript progressively.
    9. Create a five-frame static/hold source cycle with deterministic variation; set Repeat to 5.
    10. Timeline shows `Cycle 5f × 5 = 25f`, resolves 25 frames, and stores only five linked source images.
    11. Set Repeat to infinity — the loop continues to the next clip or parent end.
    12. Position the next clip to interrupt a partial cycle — the label reads the SHIPPED English `Loop shortened by next clip` (spec French `Boucle raccourcie par le clip suivant` is a recorded divergence, NOT a regression — Pitfall 5); no overlap occurs.
    13. Move and remove the next clip — the loop re-expands without regenerating its source cycle.
    14. Apply a color override — the source script remains unchanged.
    15. Manual Refresh remains available and coherent after automatic hydration.
    16. Save, close, reopen — loop references/duration intact; verify preview in the main editor; export.
    17. PHASE 43 SIGNED-ARTIFACT BOUNDARY (D-02 — MUST NOT BE DROPPED): on the signed app, confirm (a) valid linked-loop preview/export parity per 43-UAT.md §16 — export frame count/order matches the on-screen preview, Progressive/Static/Hold repeats are deterministic with authoritative cadence, Infinity stops at parent end, truncation stops at the canonical next boundary, no valid exported frame is blank or carries an unresolved placeholder, durable assets stay proportional to source cycles; AND (b) the unresolved-loop export block per 43-UAT.md §11 — an unresolved loop remains visible/selectable with `Loop source missing`, preview is marked, export fails before partial output with the carried-forward actionable copy, and unrepaired save/reopen preserves the unresolved record verbatim.
  </how-to-verify>
  <acceptance_criteria>
    All 17 steps pass in the packaged signed app. Step 12 asserts the English label; the French/spec divergence is recorded in 44-UAT.md. Step 17 covers BOTH the valid linked-loop preview/export parity AND the unresolved-loop export block — absence of either is a FAIL of the D-02 boundary. Any failing step flags the user (D-08); the agent does NOT fix functional bugs mid-phase (D-09).
  </acceptance_criteria>
  <resume-signal>Type "approved" for all 17 steps, or paste the failing step numbers with screenshots / described issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user wrapper → release script | The only credential boundary; the wrapper sources a trusted Apple env file, exports the four vars, runs `bash scripts/macos-release.sh release`, and trap-unsets them. Credentials never enter the repo or agent context. |
| signed artifact → UAT | The packaged app is the user's oracle; CSP/permission facts are only provable on the packaged build (D-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-05 | Information Disclosure | credential env vars (APPLE_*) | critical | mitigate | release runs only through the user-owned wrapper (mode 700, trap-unset on exit); agent never exports/echoes the four vars; preflight private-asset guard stays untouched (D-04) |
| T-44-06 | Spoofing | signed artifact provenance | high | mitigate | user-reported RELEASE PASS + notarization/stapler ledgers + freshness by inner-binary mtime (D-05) before any UAT |
| T-44-07 | Tampering | CSP/permission proof | high | mitigate | CSP/permission facts proven only on the packaged build; releaseContract.test.ts guards the img-src data: and connect-src efxasset: narrow grants (no dev-server proof) |
| T-44-08 | Tampering | UAT evidence | medium | mitigate | every spec step recorded pass/fail in 44-UAT.md with the shipped-label oracle; divergences recorded, never "fixed" in-phase (D-09) |
</threat_model>

<verification>
1. Preflight passes and stale bundles archived (from 44-01) BEFORE the user release run.
2. User-reported RELEASE PASS + notarization Accepted + stapler PASS recorded in 44-RELEASE.md.
3. Freshness: inner binary mtime ≥ release run date; exactly one `EFX Motion Editor_0.9.0_aarch64.dmg`.
4. 44-UAT.md records all 17 steps + the Phase 43 boundary step + the two divergences.
5. No functional source file changed (D-09).
</verification>

<success_criteria>
- Signed/notarized/stapled v0.9.0 artifact exists and is fresh by the inner binary (D-005).
- Every spec UAT step (1-17) passes in the packaged app in one pass (D-01).
- The Phase 43 signed-artifact boundary — valid linked-loop preview/export parity AND unresolved-loop export block — is verified on the signed artifact (D-02, never dropped).
- The two spec divergences are recorded, not fixed.
</success_criteria>

<output>
Create `.planning/phases/44-integrated-uat-signed-release/44-02-SUMMARY.md` when done, including: the user-reported release ledger summary, the freshness evidence, the UAT pass/fail matrix, and the two recorded divergences.
</output>
