---
phase: 36.3-physics-paint-roto-durable-core-recovery
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/physicPaintRotoDurableCore.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/stores/physicPaintStore.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/previewRenderer.ts
autonomous: true
requirements:
  - PH36.3-DURABLE-CORE
must_haves:
  truths:
    - "D-01 D-02 D-03: User sees only the strict minimal Roto surface: current frame navigation/cell, Save current, saved/reference/unsaved status."
    - "D-05 D-07 D-08: User can click Save current for the current Roto frame and remains on the same frame after success."
    - "D-09 D-10 D-16: The rendered PNG is stored as the durable frame and reopened as Cached reference, not editable stroke restore."
    - "D-11 D-12 D-13 D-14: Unsaved edits after the last save are visible as unsaved status, then discarded on close without corrupting the saved cache."
    - "Roadmap SC-03/04/05: Parent store, preview renderer path, and project save/load preserve and expose the cached PNG."
  artifacts:
    - path: "app/src/lib/physicPaintRotoDurableCore.test.ts"
      provides: "One vertical RED behavior test for the durable Roto cache skeleton"
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Roto Save current behavior, reference reopen, and discard-only close behavior"
      contains: "Save current"
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Minimal Roto workflow strip surface"
      contains: "Cached reference"
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Rendered PNG cache and MCE output durability if existing behavior needs correction"
      contains: "upsertRealRotoKeyFrame"
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Roto launch context with cached reference frames"
      contains: "cachedRotoFrames"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/stores/physicPaintStore.ts"
      via: "Save current sends an apply-canvas payload for launchContext.layerId and currentFrame"
      pattern: "kind:\s*'apply-canvas'"
    - from: "app/src/stores/physicPaintStore.ts"
      to: "app/src/lib/previewRenderer.ts"
      via: "preview renderer reads physicPaintStore.getFrame(layerId, frame) after physicPaintVersion bumps"
      pattern: "physicPaintStore\.getFrame"
    - from: "app/src/stores/physicPaintStore.ts"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "createPhysicPaintLaunchContext exposes real-key cached Roto frames for reopen reference"
      pattern: "getRotoCacheFrames"
---

<objective>
Implement the strict Phase 36.3 MVP/TDD vertical slice for one durable Physics Paint Roto cache frame.

Purpose: D-16 and D-18 require one vertical RED behavior test before implementation, then a green implementation proving the cache reaches EFX Motion, preview, project save/load, and cached-reference reopen without broad 36.2 scope.
Output: One committed RED behavior test, green implementation, minimal Roto UI, durable rendered PNG cache behavior, reference-only reopen, and discard-only unsaved close semantics.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** cache one painted Roto frame back into EFX Motion and preserve it through project save/load, **so that** Physics Paint Roto has a trustworthy durable core before adding timeline tools.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-CONTEXT.md
@.planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-UI-SPEC.md
@.planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-PATTERNS.md
@SPECS/36.2-and-more/gsd-recovery-context.en.md
@SPECS/36.2-and-more/mvp-tdd-mode.md
@app/src/lib/physicPaintBridge.test.ts
@app/src/stores/physicPaintStore.test.ts
@app/src/lib/physicPaintPersistence.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
@app/src/stores/physicPaintStore.ts
@app/src/lib/physicPaintBridge.ts
@app/src/lib/previewRenderer.ts
@app/src/components/Preview.tsx
</context>

## Artifacts this phase produces

- New test file: `app/src/lib/physicPaintRotoDurableCore.test.ts`.
- New or updated test fixture symbols in that file only: `editableState`, `makeFrame`, `physicLayer`, `mockLayers`, and one behavior test named to prove durable Roto cache save/load/reopen/discard semantics.
- Updated UI copy/state strings: `Save current`, `Saving current frame…`, `Saved current frame`, `Cached reference`, `Cached reference: repaintable, not stroke-editable.`, `Unsaved changes — click Save current to cache`, and `Could not save the current Roto frame. Keep Physics Paint open, check the selected layer and frame, then click Save current again.`
- Updated or newly introduced component-level identifiers only if needed by existing structure: `saveRotoFrame` as the current-frame save callback, `cachedRotoReferenceUrl`, `pendingRotoFrames`, `rotoSaveInFlight`, minimal workflow-strip status derivation, and close handlers that do not flush/block unsaved Roto edits.
- No new packages, no schema files, no Tauri protocol changes, no new preview channel, no generated-frame/interpolation/key-utility/playback symbols.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add the one vertical durable-cache behavior test</name>
  <files>app/src/lib/physicPaintRotoDurableCore.test.ts</files>
  <read_first>
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-CONTEXT.md, especially D-16, D-18, D-19, and deferred exclusions.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-UI-SPEC.md Interaction Contract and Copywriting Contract.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-PATTERNS.md section `app/src/lib/physicPaintRotoDurableCore.test.ts`.
    - app/src/lib/physicPaintBridge.test.ts for Vitest store/bridge fixture patterns.
    - app/src/stores/physicPaintStore.test.ts for rendered-frame/cache durability assertions.
    - app/src/lib/physicPaintPersistence.test.ts for file-I/O mock style if the vertical test uses persistence helpers.
  </read_first>
  <behavior>
    - Render or mount the Phase 36.3 Roto save surface with a physics-paint layer selected at app frame 8, an engine mock containing unsaved strokes, and a cached-reference reopen path; the test clicks the accessible `Save current` button rather than calling only store helpers.
    - The `Save current` click sends exactly one Roto `apply-canvas` payload for layer `phys-layer-1` and frame 8, keeps the current frame at 8 after success, and never invokes the save-and-advance path per D-05 and D-07.
    - The saved PNG is visible through the existing preview/cache path by checking `physicPaintStore.getFrame('phys-layer-1', 8)` and a `PreviewRenderer`-relevant rendered-frame lookup; frame 9 remains empty and no new preview channel is introduced.
    - The existing project cache path is used: `projectStore.buildMceProject()` or the equivalent save/load helper includes `physic_paint_outputs` with frame 8 and `roto_cache_metadata` source `real-key`; after close/hydrate or `loadFromMceOutputs`, frame 8 and its cache metadata survive.
    - Reopening with `createPhysicPaintLaunchContext(physicLayer(), 8, null, null, 'roto')` exposes `workflowMode: 'roto'`, `startFrame: 8`, `editableSource: 'roto'`, and a `cachedRotoFrames` item for frame 8/source `real-key`; the rendered surface labels the reopen as `Cached reference` and does not expose editable stroke restore as the durable truth per D-09 and D-10.
    - Simulate a post-save unsaved edit, then trigger close/beforeunload handling without clicking `Save current` again; the test must prove no second apply payload is sent, no blocking prompt/default-prevent occurs, and the next reopen still exposes only the last saved frame data URL, not the unsaved replacement data URL per D-11 through D-14.
    - Assert the rendered Phase 36.3 Roto accessibility surface contains `Save current`, `Cached reference`, and approved unsaved/saved status copy while excluded broad controls are not visible in that rendered surface; do not scan source text for hidden internals per D-04 and D-17.
  </behavior>
  <action>Create exactly one new vertical Vitest behavior test file per D-16 and D-18. Use existing fixture shapes from `physicPaintBridge.test.ts`, plus the smallest component/DOM harness needed to drive the Phase 36.3 `Save current` button, same-frame result, discard-only close, reference-only reopen, and rendered minimal UI assertions. Reset stores and DOM in before/after hooks; mock only what is necessary for layer selection, engine save/render, bridge delivery, close/beforeunload, and project serialization. The first commit for this plan must be a RED commit named `test(36.3-01): add failing test for durable roto cache core`. Run the test before implementation and record in the summary why it fails for the intended Phase 36.3 missing behavior such as save-and-advance still being wired, close still flushing/blocking, cached reference not being rendered as reference-only, or excluded controls still visible; it must not fail due to syntax/import/test-environment errors and must not pass by exercising only existing store/bridge/persistence helpers. Keep this as the only automated behavior test added in Phase 36.3; do not create a test matrix or source-string tests per D-17.</action>
  <verify>
    <automated>pnpm --dir app test -- app/src/lib/physicPaintRotoDurableCore.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - The new test file contains one `it(...)` behavior test covering the rendered `Save current` interaction, same-frame save, apply/store, preview-readable cache, existing project cache save/load path, reference-only reopen, discard-only close, and rendered minimal UI exclusions in one vertical scenario.
    - The RED run fails before production code changes for an intended Phase 36.3 behavior gap that existing store/bridge/persistence helper tests alone cannot satisfy.
    - The test asserts no second apply payload and no blocking/default-prevent close behavior after post-save unsaved edits.
    - Git history contains a RED commit matching `test(36.3-01): add failing test for durable roto cache core` before any `feat(36.3-01)` commit.
    - No other new test files are created for Phase 36.3.
  </acceptance_criteria>
  <done>Exactly one vertical RED behavior test exists and fails for the intended durable-cache skeleton before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — make Save current durable and preview/reopen-visible</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/stores/physicPaintStore.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/previewRenderer.ts, app/src/components/Preview.tsx</files>
  <read_first>
    - app/src/lib/physicPaintRotoDurableCore.test.ts created by Task 1, especially the Phase 36.3 component/DOM assertions that drive `Save current`, same-frame save, discard-only close, reference-only reopen, and rendered minimal UI visibility.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-CONTEXT.md decisions D-05 through D-14 and D-20.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-PATTERNS.md sections for `PhysicsPaintStudio.tsx`, `physicPaintStore.ts`, `physicPaintBridge.ts`, `Preview.tsx`, and `previewRenderer.ts`.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx existing `flushRotoFrame`, `saveRotoFrame`, `saveRotoFrameAndAdvance`, cached reference, and close/beforeunload handlers.
    - app/src/stores/physicPaintStore.ts existing `applyCanvas`, `upsertRealRotoKeyFrame`, `toMceOutputs`, `loadFromMceOutputs`, and `physicPaintVersion` invalidation.
    - app/src/lib/physicPaintBridge.ts existing `applyPhysicPaintPayload` and `createPhysicPaintLaunchContext`.
    - app/src/lib/previewRenderer.ts existing physics-paint `getFrame` draw path.
  </read_first>
  <action>Implement only the code required to make the Task 1 vertical test pass and satisfy D-05 through D-14. In `PhysicsPaintStudio.tsx`, ensure the primary Roto action snapshots the current editable state, sends one `apply-canvas` payload for `launchContext.layerId` and `currentFrame`, uses copy `Saving current frame…` and `Saved current frame`, keeps `advanceToFrame` null, and never calls `saveRotoFrameAndAdvance` for the Phase 36.3 primary action per D-07. Preserve rendered PNG as the durable truth per D-09 and D-10: reopened cached frames must set a full-strength `cachedRotoReferenceUrl` from existing store/launch data and label it as reference-only; do not implement editable per-frame restore. In store/bridge/preview files, fix only broken links the test exposes: `applyCanvas` must upsert a real-key Roto frame, bump `physicPaintVersion`, serialize/hydrate `roto_cache_metadata`, and `createPhysicPaintLaunchContext` must include real-key cached frames for the current layer/frame. Do not add schema migration, new dependencies, new preview overlays, interpolation, generated frames, key utilities, cached Roto playback, missing-background rules, Play/Roto conversion, final Pencil timeline UI, batch save, or Tauri asset protocol changes. Do not run the dev server; the user runs it per D-20.</action>
  <verify>
    <automated>pnpm --dir app test -- app/src/lib/physicPaintRotoDurableCore.test.ts --run</automated>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The Task 1 vertical behavior test passes after implementation.
    - Saving Roto uses label/copy `Save current`, `Saving current frame…`, and `Saved current frame` and stays on the same frame.
    - Parent cache for the correct source layer and app frame is available through `physicPaintStore.getFrame(layerId, appFrame)` and persists through MCE serialization/hydration.
    - Reopen launch context exposes a real-key cached Roto frame for that app frame and does not require editable stroke restore.
    - Close/beforeunload logic no longer saves, prompts, or blocks unsaved Roto edits in this MVP slice; saved cache remains intact.
    - Git history contains `feat(36.3-01): implement durable roto cache core` after the RED commit.
  </acceptance_criteria>
  <done>The vertical durable-cache test is green and the code path stores, previews, persists, and reopens one saved Roto PNG reference without auto-advance or auto-save.</done>
</task>

<task type="auto">
  <name>Task 3: Minimal Roto workflow strip and discard-status surface</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-CONTEXT.md decisions D-01 through D-06, D-08, D-13, and D-15.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-UI-SPEC.md lines for Phase UI Boundary, Copywriting Contract, Interaction Contract, and Component Inventory.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-PATTERNS.md sections `PhysicsPaintWorkflowStrip.tsx`, `physicsPaintStudio.css`, UI gating over deletion, and Close unsaved semantics.
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx existing broad Roto controls and status stack.
    - app/src/components/physic-paint/physicsPaintStudio.css existing workflow strip, button/status, Roto cell, and cached reference classes.
  </read_first>
  <action>Gate the visible Roto workflow strip to the strict minimal surface per D-01 through D-04: mode label, current frame cell/navigation already present, one real button with accessible name `Save current`, and compact visible status text. The button must be disabled when engine is not ready, save is in flight, or there are no unsaved edits. Replace broad visible Roto status/control surfaces with only these Phase 36.3 states: `Unsaved changes — click Save current to cache`, `Saving current frame…`, `Saved current frame`, `Cached reference`, and `Cached reference: repaintable, not stroke-editable.` Hide or do not render cached Roto playback transport, Roto key utilities, interpolation controls, generated-frame connector UI, missing-background controls, Play/Roto conversion, and final Pencil timeline UI in the Phase 36.3 Roto surface. Gate rendering only; do not delete broad internals unless needed to compile per D-04. Preserve existing CSS tokens and full-opacity cached reference styling; do not introduce a new design system or shadcn.</action>
  <verify>
    <automated>pnpm --dir app test -- app/src/lib/physicPaintRotoDurableCore.test.ts --run</automated>
    <automated>pnpm --dir app typecheck</automated>
    <automated>The Task 1 vertical test includes rendered DOM/accessibility assertions proving the Phase 36.3 Roto surface exposes `Save current`, `Cached reference`, and approved status copy while excluded broad controls are absent from the rendered surface.</automated>
  </verify>
  <acceptance_criteria>
    - `Save current` appears as visible button text and accessible name in the Roto workflow strip.
    - Visible Phase 36.3 Roto status copy is limited to the approved copy listed in this task.
    - Broad 36.2-era controls named in D-01 do not render in Roto mode for this phase.
    - Cached reference image remains full opacity and visible text indicates `Cached reference`.
    - No shadcn, new registry UI, new dependency, or temporary separate timeline/marker pattern is introduced.
  </acceptance_criteria>
  <done>The Roto UI is the approved strict minimal surface and communicates saved/reference/unsaved/discard behavior without broad controls.</done>
</task>

</tasks>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Cache one painted Roto frame back into EFX Motion and preserve it through project save/load | Tasks 1 and 2 |
| GOAL | Reopen cached frame as visual reference before adding timeline tools | Tasks 1, 2, and 3 |
| REQ | PH36.3-DURABLE-CORE | Tasks 1, 2, and 3 |
| CONTEXT | D-01 through D-04 strict minimal UI and gate broad controls | Task 3 |
| CONTEXT | D-05 through D-10 Save current, same-frame, cached reference only | Tasks 1, 2, and 3 |
| CONTEXT | D-11 through D-15 discard unsaved close, no prompt/save-on-close | Tasks 1, 2, and 3 |
| CONTEXT | D-16 through D-18 one RED behavior test and RED commit | Task 1 |
| CONTEXT | D-19 manual UAT | Plan 02 |
| CONTEXT | D-20 user runs server/app | Plan 02 |
| RESEARCH | Skipped by user; no RESEARCH.md/VALIDATION.md required | Plan includes manual UAT in Plan 02 |
| UI-SPEC | Copy, colors, minimal workflow strip, no new registry | Task 3 |
| PATTERNS | Existing store/bridge/preview/persistence/workflow strip analogs | Tasks 1, 2, and 3 |

Deferred ideas excluded: interpolation, generated frames, key utilities, save-on-leave, cached Roto playback, missing-background rules, Play/Roto conversion, final Pencil timeline UI, Tauri asset protocol hardening, multi-frame workflows, batch save, broad UX cleanup, and extra tests.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Standalone Physics Paint window → EFX Motion parent store | Rendered apply payload and editable state cross from a child/editor surface into trusted project state. |
| Project file/cache → runtime hydration | Saved `.mce` physics-paint outputs and PNG cache paths hydrate back into runtime stores. |
| Runtime store → preview renderer | Cached PNG data URLs are decoded/rendered by browser image APIs in the preview canvas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3-01 | Tampering | `applyPhysicPaintPayload` / `physicPaintStore.applyCanvas` | mitigate | Validate payload with existing `isPhysicPaintApplyPayload`, require known physics-paint layer/source id, and write only `payload.startFrame` for one current-frame apply. |
| T-36.3-02 | Spoofing | `createPhysicPaintLaunchContext` | mitigate | Derive `layerId` from selected physics-paint layer source and expose only store-backed `cachedRotoFrames`; do not accept arbitrary cached reference lists from UI state. |
| T-36.3-03 | Information Disclosure | `previewRenderer` image source | accept | Phase uses existing local `data:image/png` cache data already stored in project outputs; no network fetch or external URL is introduced. |
| T-36.3-04 | Denial of Service | Duplicate Save current clicks | mitigate | Disable `Save current` while `rotoSaveInFlight`/apply is in flight and preserve existing operation-id handling. |
| T-36.3-05 | Repudiation | RED/GREEN behavior proof | mitigate | D-18 requires separate RED and GREEN commits plus summary of failing/passing test output. |
| T-36.3-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; if an executor adds one, stop and require package legitimacy audit before continuing. |
</threat_model>

<verification>
- `pnpm --dir app test -- app/src/lib/physicPaintRotoDurableCore.test.ts --run` passes after GREEN.
- `pnpm --dir app typecheck` passes.
- Git log shows `test(36.3-01): add failing test for durable roto cache core` before `feat(36.3-01): implement durable roto cache core`.
- No dev server is started by the executor.
</verification>

<success_criteria>
- One vertical behavior test exists and is the only new automated Phase 36.3 test.
- User can save the current Roto frame explicitly and remain on the same frame.
- Correct layer/frame rendered PNG is in the parent store, preview-readable, project-persisted, and exposed in reopen launch context as `Cached reference`.
- Closing with unsaved edits after save does not auto-save, block, prompt, or corrupt the saved cache.
- Minimal UI excludes deferred 36.2-era controls.
</success_criteria>

<output>
Create `.planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-01-SUMMARY.md` when done.
</output>
