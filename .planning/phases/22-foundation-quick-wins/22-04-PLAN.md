---
phase: 22-foundation-quick-wins
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/canvas/MotionPath.tsx
  - Application/src/components/canvas/motionPath.test.ts
autonomous: true
requirements: [UXP-03]
gap_closure: true

must_haves:
  truths:
    - "Motion path dots appear noticeably denser on short sequences (< 30 frames) due to sub-frame sampling"
  artifacts:
    - path: "Application/src/components/canvas/MotionPath.tsx"
      provides: "Sub-frame dot rendering with unique keys and fractional frame storage"
      contains: "key={index}"
  key_links:
    - from: "sampleMotionDots"
      to: "dots.map rendering"
      via: "fractional frame values preserved through to render"
      pattern: "key=\\{index\\}"
---

<objective>
Fix motion path sub-frame dot density so short sequences (< 30 frames) actually render 4x more dots.

Purpose: Sub-frame sampling was implemented in 22-01 but two bugs (Math.round collapsing fractional frames and key={dot.frame} causing Preact deduplication) silently discard ~75% of computed dots.
Output: MotionPath.tsx rendering all sub-frame samples as distinct visible dots.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-foundation-quick-wins/22-01-SUMMARY.md
@.planning/debug/motion-path-dots-not-dense.md

<interfaces>
From Application/src/components/canvas/MotionPath.tsx:
```typescript
export function sampleMotionDots(
  keyframes: Keyframe[],
  canvasW: number,
  canvasH: number,
): {x: number; y: number; frame: number}[] // frame is currently Math.round(f) -- loses sub-frame precision

// Rendering (line 185-194):
dots.map((dot) => (
  <circle key={dot.frame} ... /> // duplicate integer keys cause Preact deduplication
))
```

Root cause (from debug session):
1. Line 63: `frame: Math.round(f)` collapses 0, 0.25, 0.5, 0.75 to same integer
2. Line 186: `key={dot.frame}` uses rounded integer as Preact key -- duplicates discarded
3. Line ~156: `dots.find((d) => d.frame === localFrame)` depends on integer frame for current-dot highlight
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix sub-frame dot storage and rendering keys</name>
  <files>Application/src/components/canvas/MotionPath.tsx, Application/src/components/canvas/motionPath.test.ts</files>
  <behavior>
    - Test: sampleMotionDots with span < 30 returns dots with DISTINCT frame values (not collapsed to integers)
    - Test: For span=5, all 21 dots have unique frame values (0, 0.25, 0.5, ... 5.0)
    - Test: Existing integer-step tests (span >= 30) still pass unchanged
    - Test: First and last dots still match keyframe positions exactly
  </behavior>
  <action>
    In sampleMotionDots (MotionPath.tsx):
    - Line 63: Remove `Math.round(f)` -- store the raw fractional frame value: `frame: f`
    - This preserves all 4 sub-frame samples per integer frame as distinct entries

    In the rendering JSX (around line 185-186):
    - Change `key={dot.frame}` to `key={index}` in the dots.map callback: `dots.map((dot, index) => <circle key={index} ...`
    - This gives each dot a unique Preact key regardless of frame value

    For the currentDot highlight lookup (around line 156):
    - The existing `dots.find((d) => d.frame === localFrame)` where localFrame is an integer will no longer find an exact match for sub-frame dots
    - Change to: `dots.find((d) => Math.round(d.frame) === localFrame)` so the highlight still works with fractional frame values
    - This finds the first dot whose rounded frame matches the playhead position

    Update tests in motionPath.test.ts:
    - The existing test "sub-frame dots have integer frame property" (line 122-128) must be updated: sub-frame dots now have fractional frame values, NOT integer. Update assertion to verify frames include fractional values like 0.25, 0.5, 0.75
    - Add a new test: verify that for span=5 (step=0.25), all 21 dots have UNIQUE frame values
    - Existing tests for span >= 30 should pass without changes (integer frames are stored as-is)
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm exec vitest run Application/src/components/canvas/motionPath.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All motionPath tests pass. Sub-frame dots (span < 30) store fractional frame values and render with unique index-based keys. Current-dot highlight uses Math.round for lookup.</done>
</task>

</tasks>

<verification>
- pnpm exec vitest run Application/src/components/canvas/motionPath.test.ts -- all tests pass
- For a 5-frame span, sampleMotionDots returns 21 dots with 21 unique frame values (0, 0.25, 0.5, ..., 5.0)
- Rendering uses key={index} so Preact renders all dots without deduplication
</verification>

<success_criteria>
- Sub-frame dots store fractional frame values (not rounded to integers)
- Rendering uses sequential index as key (not frame value)
- Current-dot highlight uses Math.round for playhead matching
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-foundation-quick-wins/22-04-SUMMARY.md`
</output>
