---
phase: 36.11-physics-paint-roto-repaint-cached-real-key
plan: 03
type: execute
wave: 3
depends_on:
  - 36.11-01
  - 36.11-02
files_modified:
  - app/src/stores/physicPaintStore.test.ts
  - app/src/lib/previewRenderer.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: false
requirements:
  - 36.11-REPAINT-CACHED-REAL-KEY
  - 36.11-ALPHA-ONLY-MERGE
  - 36.11-NO-STROKE-RESTORE
must_haves:
  truths:
    - "Preview/export continue to composite paper/background separately from the paint alpha cache."
    - "Existing key utilities, dirty save-before-action, and missing/background frame behavior remain intact."
    - "D-05: Erase remains available only for newly painted live strokes and does not erase, mask, mutate, or corrupt previous cached alpha base."
    - "D-06: Clear removes only unsaved live repaint strokes and leaves cached base/real key intact."
    - "D-07: Undo/Redo during cached-key repaint covers only new live actions since reopen."
    - "D-08: Destructive removal or replacement of old cached pixels remains out of scope."
    - "D-13: Cached-base feedback uses existing Physics Paint workflow/status surfaces only."
    - "D-14: Cached base open feedback explains the base is visible and non-editable."
    - "D-15: Successful repaint save feedback mentions merge into the frame."
    - "D-16: Failed merge keeps cached base visible, preserves live dirty edits in memory, and shows retry-oriented compact feedback."
    - "D-17: Phase 36.11 introduces no new UI surfaces."
  artifacts:
    - path: "app/src/stores/physicPaintStore.test.ts"
      provides: "Regression coverage that merged repaint output remains source='real-key', not background-only support"
    - path: "app/src/lib/previewRenderer.test.ts"
      provides: "Regression coverage that paper/background remains renderer-owned behind alpha-only real-key paint"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Final source-contract coverage for feedback, failed merge preservation, and no new UI surfaces"
  key_links:
    - from: "PhysicPaintApplyPayload.renderedFrame"
      to: "physicPaintStore.upsertRealRotoKeyFrame"
      via: "existing applyCanvas path normalizes merged output to source='real-key'"
      pattern: "upsertRealRotoKeyFrame"
    - from: "physicPaintStore real-key cache"
      to: "PreviewRenderer"
      via: "PreviewRenderer draws real-key paper/background before drawing alpha-only merged paint image"
      pattern: "realKeyBackgroundDraw"
  prohibitions:
    - statement: "Do not add generated interpolation, final Pencil timeline UI, Play/Roto conversion, XState/state-machine work, or full editable stroke persistence in this phase."
      status: "resolved"
      verification: "Source-contract/no-new-surface tests and review checklist reject new controls, XState imports, interpolation wiring, and old-stroke persistence."
    - statement: "Destructive raster erase/masking over the cached base is deferred and must not appear in Phase 36.11."
      status: "resolved"
      verification: "Studio tests assert erase/Clear operate only on live overlay and no mask/compositing erase helper is introduced."
---

<objective>
Close the vertical repaint slice with persistence/rendering regressions and user-owned live UAT: merged repaint output remains an alpha-only real key, preview/export draw paper separately, and existing Roto utilities still work.

Purpose: Plans 01-02 implement the behavior; this plan proves it did not regress Phase 36.7 key utilities, Phase 36.6 dirty save-before-action, or Phase 36.10 paper/background separation. It also captures the required live visual check without Claude starting the server.
Output: Regression tests, full automated gate, and a blocking human verification checkpoint for live cached-key repaint.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** repaint cached real Roto keys, **so that** I can add paint without losing prior work.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-RESEARCH.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-PATTERNS.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-VALIDATION.md
@SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-05-SUMMARY.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-06-SUMMARY.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-05-SUMMARY.md
@app/src/stores/physicPaintStore.test.ts
@app/src/lib/previewRenderer.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
</context>

## Artifacts this phase produces

- Additional `physicPaintStore.test.ts` regression for merged cached repaint output source metadata
- Additional `previewRenderer.test.ts` regression/source-contract for real-key paper/background separation after merged alpha repaint
- Additional `PhysicsPaintStudio.test.ts` source-contracts for D-05 through D-17 feedback/failure/no-new-surface behavior
- Phase 36.11 live UAT checklist captured in the plan summary after user approval
- No new production UI files, CSS files, dependencies, schema files, or package-manager installs

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistence and renderer regressions for merged real-key alpha</name>
  <files>app/src/stores/physicPaintStore.test.ts, app/src/lib/previewRenderer.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.test.ts
    - app/src/lib/previewRenderer.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/stores/physicPaintStore.ts
    - app/src/lib/previewRenderer.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
    - SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
  </read_first>
  <action>Add focused tests proving the merged repaint output still travels through existing `applyCanvas` / `upsertRealRotoKeyFrame`, remains `source: 'real-key'`, is not `source: 'background-only-support'`, and does not set `backgroundOnly` unless an explicit background-only path already did so outside repaint. Add or strengthen PreviewRenderer coverage so a real key with merged alpha still draws `realKeyBackgroundDraw`/paper first and the real-key alpha image second. Add Studio source-contract coverage for D-15 and D-16: successful dirty cached-key repaint save sets compact merge feedback such as `Merged new paint into frame ${frame}.`, and merge failure preserves cached-base state plus dirty live edits with retry-oriented copy such as `Could not merge frame ${frame} — edits are still open.`</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/stores/physicPaintStore.test.ts src/lib/previewRenderer.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Store test asserts merged repaint output appears in `getRotoCacheFrames(layerId)` as `expect.objectContaining({ appFrame: frame, source: 'real-key' })`.
    - Store test asserts merged repaint output is not `background-only-support` and does not grow support metadata for the same frame.
    - PreviewRenderer test asserts paper/background draw occurs before drawing the merged real-key image source.
    - Studio test asserts successful cached-key repaint feedback contains `Merged new paint into frame` or an equivalent compact merge phrase.
    - Studio test asserts failed merge feedback contains `Could not merge frame` or an equivalent retry-oriented phrase and preserves dirty state.
  </acceptance_criteria>
  <done>Persistence and renderer tests prove merged repaint output remains a real-key alpha cache with separate paper/background rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Run full automated phase gate without starting the server</name>
  <files>app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts, app/src/components/physic-paint/__tests__/physicsPaintRotoSession.test.ts, app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts, app/src/stores/physicPaintStore.test.ts, app/src/lib/previewRenderer.test.ts</files>
  <read_first>
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-VALIDATION.md
    - app/package.json
    - app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts
    - app/src/components/physic-paint/__tests__/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts
    - app/src/stores/physicPaintStore.test.ts
    - app/src/lib/previewRenderer.test.ts
  </read_first>
  <action>Run the Phase 36.11 focused validation matrix and then the app typecheck/full suite. Use pnpm commands only. Do not start the dev server. If the local workspace package declarations are missing, run the existing local package build command `pnpm --dir packages/efx-physic-paint build` before typecheck; do not install new packages. Record the exact passing commands in the summary.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/physicsPaintRotoSession.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts src/stores/physicPaintStore.test.ts src/lib/previewRenderer.test.ts && pnpm --dir app typecheck && pnpm --dir app exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 36.11 Vitest command passes.
    - `pnpm --dir app typecheck` passes.
    - `pnpm --dir app exec vitest run` passes or any unrelated pre-existing skipped/todo tests are reported without hiding a Phase 36.11 failure.
    - No command starts the dev server.
    - No package.json, pnpm-lock.yaml, or dependency manifest is modified.
  </acceptance_criteria>
  <done>Automated gate is green and ready for live visual UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify live cached real-key repaint in the running app</name>
  <files>none</files>
  <action>Pause after automated validation and ask the user to verify the live Phase 36.11 cached real-key repaint flow in their already-running app. Do not start the server. This checkpoint confirms D-01 through D-17 visually and functionally on the real canvas surface.</action>
  <what-built>Phase 36.11 additive cached real-key repaint is automated-ready: cached real Roto keys reopen with old alpha as a non-editable base, new live paint merges into the alpha-only real-key cache on save, Clear/Erase affect only live overlay, and paper/background stays separate.</what-built>
  <how-to-verify>
    1. In your already-running app, open a Physics Paint Roto real key that already has saved cached paint.
    2. Confirm the prior paint appears at full strength as normal paint with compact cached-base feedback in the existing Physics Paint status/workflow surface.
    3. Add a small new live paint mark over the existing cached paint, then save the current frame.
    4. Confirm feedback says the new paint was merged into that frame.
    5. Navigate away and back, or close/reopen the project/window through the normal workflow, and confirm both old cached paint and new paint are visible.
    6. Confirm paper/background is visible in preview/export through the normal renderer but is not baked into the alpha paint cache: the saved Roto paint should not show coarse embedded paper pixels from the standalone canvas.
    7. Reopen another cached real key and press Save current without painting; confirm the cached key remains unchanged and compact no-new-paint feedback appears.
    8. Reopen a cached real key, add temporary live paint, then Clear; confirm only the temporary live paint disappears and the old cached base/real key remains. Use Delete key utility only if you intentionally want to remove the whole real key.
  </how-to-verify>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/physicsPaintRotoSession.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts src/stores/physicPaintStore.test.ts src/lib/previewRenderer.test.ts && pnpm --dir app typecheck</automated>
    <human-check>User follows the eight live verification steps and types "approved" or reports the exact failed step.</human-check>
  </verify>
  <done>User approves live cached real-key repaint, no-change save, live-overlay-only Clear, and paper/background separation.</done>
  <resume-signal>Type "approved" if all checks pass, or describe the exact failed step and observed behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged apply-canvas payload -> physicPaintStore | Durable cache mutation replaces a real key and affects preview/export/project save. |
| renderer cached alpha -> preview/export output | Preview/export composes user-visible paper/background behind cached paint alpha. |
| automated-ready -> human live UAT | Canvas compositing can pass source tests while still being perceptually wrong in the running app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.11-07 | Tampering | physicPaintStore.applyCanvas | medium | mitigate | Store regression asserts merged repaint remains `source: 'real-key'` and does not become background-only support. |
| T-36.11-08 | Information Disclosure | PreviewRenderer/export composition | medium | mitigate | Renderer regression asserts paper/background remains renderer-owned and is drawn before alpha cache, not stored inside it. |
| T-36.11-09 | Tampering | failed merge/save flow | high | mitigate | Studio regression asserts cached base and dirty live edits remain open on merge failure with retry feedback. |
| T-36.11-10 | Repudiation | UAT feedback surfaces | low | mitigate | Existing status/workflow copy distinguishes base loaded, no-new-paint preserve, successful merge, and failed merge. |
| T-36.11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are required; Package Legitimacy Audit says no external packages. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/physicsPaintRotoSession.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts src/stores/physicPaintStore.test.ts src/lib/previewRenderer.test.ts`
- `pnpm --dir app typecheck`
- `pnpm --dir app exec vitest run`
- Human UAT checkpoint approval from the user in their running app.
</verification>

<success_criteria>
- Automated tests prove merged real-key repaint remains alpha-only and real-key sourced.
- Existing key utilities/save-before-action/missing-background contracts remain covered.
- User approves live repaint over cached real key without prior-work loss.
</success_criteria>

<output>
Create `.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-03-SUMMARY.md` when done.
</output>
