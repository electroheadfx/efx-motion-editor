---
phase: 36.11-physics-paint-roto-repaint-cached-real-key
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts
  - app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts
autonomous: true
requirements:
  - 36.11-REPAINT-CACHED-REAL-KEY
  - 36.11-ALPHA-ONLY-MERGE
must_haves:
  truths:
    - "D-01: Opening an existing real Roto key keeps its cached alpha paint visible as a full-strength base paint layer."
    - "D-02: The cached alpha base stays outside the live physics-paint engine editable stroke state while new live paint is edited."
    - "D-03: Edit-time composition is paper/background preview plus previous cached paint alpha plus live engine strokes."
    - "Saving a dirty cached-key repaint replaces the real-key cache with previous cached alpha plus newly rendered live paint alpha."
    - "The saved merged real-key cache remains alpha-only paint content and excludes paper/background pixels."
    - "Reopening the frame after a dirty repaint save shows the merged painted result."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts"
      provides: "Canvas 2D alpha-only merge helper for cached real-key repaint saves"
      exports: ["mergeCachedRotoAlphaFrame"]
    - path: "app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts"
      provides: "RED/GREEN helper coverage for cached-base-before-live draw order and alpha-only output"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Studio save path that merges cached base with live transparent stroke output for dirty cached real keys"
  key_links:
    - from: "PhysicsPaintStudio.tsx"
      to: "physicsPaintRotoAlphaMerge.ts"
      via: "dirty cached real-key save calls mergeCachedRotoAlphaFrame with the matched cached base frame and exportTransparentStrokeCanvas output"
      pattern: "mergeCachedRotoAlphaFrame"
    - from: "physicsPaintRotoAlphaMerge.ts"
      to: "PhysicPaintApplyPayload.renderedFrame"
      via: "returns a PhysicPaintRenderedFrame-compatible frameIndex/appFrame/dataUrl/width/height object"
      pattern: "frameIndex.*appFrame.*dataUrl"
  prohibitions:
    - statement: "Old cached pixels must not be restored as editable stroke/script state."
      status: "resolved"
      verification: "PhysicsPaintStudio source-contract test proves cached base is used as visual/merge input, not reconstructed engine strokes."
    - statement: "Paper/background pixels must not be baked into the saved real-key alpha cache."
      status: "resolved"
      verification: "Alpha merge test asserts only cached base image and live transparent canvas are drawn into output."
---

<objective>
Deliver the first vertical repaint slice: a user can add live paint over a reopened cached real Roto key and save without losing the prior cached alpha paint.

Purpose: Phase 36.11 depends on cached PNG output being durable visual truth, not restored stroke state. This plan creates the tested alpha-only merge path and wires it into the Studio save flow for dirty cached real-key repaint sessions.
Output: New alpha merge helper, RED/GREEN tests, and Studio save wiring that sends a merged real-key paint PNG through the existing apply-canvas path.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** repaint cached real Roto keys, **so that** I can add paint without losing prior work.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-RESEARCH.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-PATTERNS.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-VALIDATION.md
@SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-06-SUMMARY.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts
@app/src/types/physicPaint.ts
</context>

## Artifacts this phase produces

- `app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts`
- `mergeCachedRotoAlphaFrame(baseFrame, liveAlphaCanvas, appFrame, size)` exported helper
- Internal image-loading helper used only by `physicsPaintRotoAlphaMerge.ts`
- `app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts`
- Test doubles for merge-helper Canvas/Image behavior in the new helper test file
- Additional `PhysicsPaintStudio.test.ts` source-contract cases for dirty cached real-key repaint save wiring

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - specify cached-base plus live-alpha merge behavior</name>
  <files>app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts, app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-PATTERNS.md
    - SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
  </read_first>
  <behavior>
    - Test 1: Given a previous cached real-key PNG and a live transparent canvas for app frame 12, `mergeCachedRotoAlphaFrame` draws the cached image first, draws the live canvas second, returns `frameIndex: 0`, `appFrame: 12`, the requested width/height, and a `data:image/png` URL.
    - Test 2: The merge helper does not draw paper texture, paper color, background-only support frame, PreviewRenderer paper canvas, or engine background output into the merged canvas.
    - Test 3: `PhysicsPaintStudio.tsx` imports and calls `mergeCachedRotoAlphaFrame` only for a dirty cached real-key repaint save, using the matched cached real-key base and `exportTransparentStrokeCanvas(engine)` live alpha output.
    - Test 4: The Studio source contract keeps D-02 by not loading cached base pixels into editable stroke history or reconstructed stroke/script state.
  </behavior>
  <action>Create the new helper test file and extend the Studio source-contract tests before implementation. Use Vitest patterns from `previewRenderer.test.ts` and existing `PhysicsPaintStudio.test.ts` source slicing. Name the new expected export `mergeCachedRotoAlphaFrame`. The tests must cite D-01, D-02, D-03, and 36.11-ALPHA-ONLY-MERGE in test names or comments for decision traceability. Run the focused tests and confirm they fail because the helper/export and Studio merge wiring do not yet exist.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts` exists and imports `mergeCachedRotoAlphaFrame` from `../physicsPaintRotoAlphaMerge`.
    - The new helper test asserts draw order by recording cached image draw before live canvas draw.
    - The new helper test asserts output metadata contains `frameIndex: 0`, the requested `appFrame`, `width`, `height`, and a PNG data URL.
    - The Studio source-contract test contains an assertion for `mergeCachedRotoAlphaFrame(` inside the Roto flush/save block.
    - The focused Vitest command fails in RED because production support is absent or incomplete, not because of syntax/import mistakes unrelated to the intended missing behavior.
  </acceptance_criteria>
  <done>RED commit exists with failing tests that describe additive cached real-key repaint merge behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - implement alpha-only merge helper and dirty repaint save wiring</name>
  <files>app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts, app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-06-SUMMARY.md
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
  </read_first>
  <action>Implement `app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts` with exported `mergeCachedRotoAlphaFrame(baseFrame, liveAlphaCanvas, appFrame, size)` returning a `PhysicPaintRenderedFrame`. The helper must create an output canvas sized to `size.width` and `size.height`, clear it, load/draw `baseFrame.dataUrl`, then draw `liveAlphaCanvas`, then serialize with `toDataURL('image/png')`. Throw a specific `Error` if 2D context creation or base image loading fails so Studio can keep live dirty edits open per D-16 in later handling. In `PhysicsPaintStudio.tsx`, preserve the matched cached real-key base frame when `loadCachedRotoReferenceFrame` opens a cached real key, keep that base available after `markCurrentRotoFrameDirty`, and in `flushRotoFrame` build `renderedFrame` via `mergeCachedRotoAlphaFrame` when the frame has a cached repaint base and live captured alpha. Continue using `exportTransparentStrokeCanvas(engine)` for live output and the existing `PhysicPaintApplyPayload(kind: 'apply-canvas')` path; do not create a parallel store mutation path. This implements D-01, D-02, D-03, and 36.11-ALPHA-ONLY-MERGE.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `physicsPaintRotoAlphaMerge.ts` exports `mergeCachedRotoAlphaFrame` and imports the existing rendered-frame type from `app/src/types/physicPaint.ts` by relative path.
    - The merge helper draws exactly two semantic inputs for output composition: the cached real-key PNG image and the live transparent canvas.
    - `PhysicsPaintStudio.tsx` keeps a cached repaint base frame or equivalent semantic state after a dirty edit instead of clearing all cached-base visibility/state on the first edit.
    - `flushRotoFrame` uses the merge helper for dirty cached real-key repaint output and still sends the result through `apply-canvas` with `renderedFrame`.
    - The focused Vitest command passes.
  </acceptance_criteria>
  <done>Dirty repaint over a cached real key saves a merged alpha-only real-key frame without restoring old strokes or baking paper/background.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - lock draw-order and save-path source contracts</name>
  <files>app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts, app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts
    - app/src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts
    - app/src/types/physicPaint.ts
  </read_first>
  <action>Refactor only if the GREEN implementation duplicates canvas serialization or stores cached-base state with ambiguous names. Keep the helper local to `components/physic-paint`, keep image loading internal, and keep Studio as the adapter that chooses between normal replacement save and cached-base additive merge. Strengthen tests so later changes cannot reverse draw order or route cached-base repaint through `buildRotoOutputFrame` without the merge. Do not introduce new dependencies, new UI surfaces, XState, or broad `useEffect` orchestration.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Helper and Studio names distinguish cached repaint base from transient playback/onion previews.
    - Tests fail if live alpha is drawn before cached base.
    - Tests fail if dirty cached real-key save bypasses `mergeCachedRotoAlphaFrame`.
    - `pnpm --dir app typecheck` passes.
  </acceptance_criteria>
  <done>Merge helper and Studio save contract are stable, typed, and guarded by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached PNG data URL -> Canvas merge helper | Existing project/cache data is decoded as an image and drawn into a new output canvas. |
| live engine canvas -> apply-canvas payload | Live user paint output crosses from in-memory engine state into durable real-key cache payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.11-01 | Tampering | mergeCachedRotoAlphaFrame | medium | mitigate | Draw only the matched cached real-key base frame and live transparent canvas for the same appFrame; tests assert draw order and output frame metadata. |
| T-36.11-02 | Information Disclosure | merged real-key PNG | medium | mitigate | Exclude paper/background sources from helper inputs; PreviewRenderer remains responsible for paper/background composition. |
| T-36.11-03 | Denial of Service | base image loading | low | mitigate | Throw on image/context failure so Studio failure handling can preserve dirty edits rather than looping or writing corrupt output. |
| T-36.11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are required; Package Legitimacy Audit says no external packages. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/__tests__/physicsPaintRotoAlphaMerge.test.ts src/components/physic-paint/__tests__/PhysicsPaintStudio.test.ts`
- `pnpm --dir app typecheck`
</verification>

<success_criteria>
- Dirty cached real-key repaint save merges previous cached alpha and new live alpha.
- The cached base remains outside editable stroke state.
- Saved output is alpha-only and still uses existing apply-canvas persistence.
</success_criteria>

<output>
Create `.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-01-SUMMARY.md` when done.
</output>
