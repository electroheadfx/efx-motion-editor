---
phase: 25-paint-compositing-pipeline
plan: "25-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/lib/lumaKey.ts
  - Application/src/lib/lumaKey.test.ts
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Blue paint stroke on white background remains opaque (non-white areas unaffected by luma key)"
    - "Paint strokes remain editable with full color control (luma key only affects near-white pixels)"
  artifacts:
    - path: "Application/src/lib/lumaKey.ts"
      contains: "threshold"
      min_lines: 10
  key_links:
    - from: "Application/src/lib/lumaKey.ts"
      to: "Application/src/components/sidebar/PaintProperties.tsx"
      via: "lumaKeyEnabled signal"
---

<objective>
Fix luma key alpha formula to use threshold approach instead of continuous alpha mapping.

Purpose: The continuous formula `alpha = 255 - luma` makes ALL non-black colors semi-transparent (blue=237, gray=127, red=201). The correct behavior is threshold-based: near-white (luma >= 254) becomes transparent, everything else opaque.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-paint-compositing-pipeline/25-UAT.md
@.planning/phases/25-paint-compositing-pipeline/25-01-SUMMARY.md
</context>

<interfaces>
From Application/src/lib/lumaKey.ts:
```typescript
export function applyLumaKey(canvas: HTMLCanvasElement, invert: boolean): void
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Fix luma key alpha formula to threshold approach</name>
  <files>Application/src/lib/lumaKey.ts, Application/src/lib/lumaKey.test.ts</files>
  <behavior>
    - Luma Key (invert=false): luma >= 254 → alpha=0 (transparent), else alpha=255 (opaque)
    - Luma Invert (invert=true): luma < 10 → alpha=0 (transparent near-black), else alpha=255 (opaque)
    - Blue (0,0,255): luma=18 → alpha=255 (opaque, not 237)
    - Gray (128,128,128): luma=128 → alpha=255 (opaque, not 127)
    - Pure white (255,255,255): luma=255 → alpha=0 (transparent)
    - Pure black (0,0,0): luma=0 → alpha=255 (opaque)
  </behavior>
  <action>
Fix lumaKey.ts line 46 alpha formula:

For Luma Key (invert=false):
- Threshold approach: luma >= 254 → alpha=0 (transparent white BG), else alpha=255 (opaque strokes)
- This means ONLY near-white pixels become transparent, all colored strokes remain fully opaque

For Luma Invert (invert=true):
- Threshold approach: luma < 10 → alpha=0 (transparent near-black strokes), else alpha=255 (opaque white BG)
- This inverts what counts as transparent vs opaque

The key insight: luma key is NOT a gradient. It's a binary transparency gate. White background transparent, colored strokes opaque.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx vitest run src/lib/lumaKey.test.ts</automated>
  </verify>
  <done>
Luma key uses threshold: near-white (254+) is transparent, everything else is opaque.
Blue paint remains fully opaque. User can paint any color and luma key only affects white background.
  </done>
</task>

</tasks>

<verification>
- Run lumaKey tests: `npx vitest run src/lib/lumaKey.test.ts`
- All tests pass with threshold-based alpha
</verification>

<success_criteria>
1. Blue paint stroke on white: blue stays opaque (alpha=255), white becomes transparent
2. Gray paint stroke on white: gray stays opaque (alpha=255), white becomes transparent
3. Black paint stroke: stays opaque (alpha=255)
4. Pure white background: becomes transparent (alpha=0)
5. Luma invert: near-black strokes transparent, white BG opaque
</success_criteria>

<output>
After completion, create `.planning/phases/25-paint-compositing-pipeline/25-02-SUMMARY.md`
</output>
