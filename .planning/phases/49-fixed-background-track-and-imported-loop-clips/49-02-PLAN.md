---
phase: 49-fixed-background-track-and-imported-loop-clips
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/efx-paint/utils/naturalFilenameSort.ts
  - app/src/efx-paint/utils/naturalFilenameSort.test.ts
  - app/src/stores/efxPaintStore.ts
  - app/src/stores/efxPaintStore.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
autonomous: true
requirements: [BKG-02, BKG-03, BKG-04, BKG-05, BKG-07, BKG-08, BKG-09]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02 hard lock: `sortImagesByOriginalFilename` orders by ORIGINAL FILENAME basename using `Intl.Collator(undefined, { numeric: true, sensitivity: 'base' })` — `shot_1 < shot_2 < shot_10` — and never sorts by asset UUID (assets are UUID v4 per image_pool.rs:57); the comparison is stable for equal basenames and falls back to the full `original_path` when no basename separator exists (basename extraction mirrors imageStore.ts:199)"
    - "BKG-03 START-collision law (D-03/D-04, symmetric for import and drag): `addBackgroundClip` and `moveBackgroundClip` reject — returning a closed result with the fixed English reason and writing nothing — only when the landing frame is occupied by an existing clip (its startFrame ≤ landing &lt; its resolved extent); a landing frame exactly at the previous clip's exclusive end (zero-gap adjacency, touching clips) is ACCEPTED"
    - "BKG-03 downstream extent is never a rejection: a clip longer than the gap to the next clip commits with its data intact and lets the Phase 48 resolver truncation law apply (visual cut + shortened facts come from the resolver, data preserved) — no asymmetric snap, import and drag share one verdict function"
    - "BKG-04 repeat contract: `setBackgroundClipRepeat` accepts finite integers ≥ 1 (1 = play through the cycle once) and `{ mode: 'infinite' }`; 0, negative, non-integer, and empty-equivalent inputs are rejected uncommitted with the prior value preserved; a repeat change never reorders clips or moves any other clip — only the edited clip's effective extent changes"
    - "BKG-04/BKG-05 loop arithmetic is exact integer frame math (`cycleLength × count`); when the next clip starts exactly at the predecessor's natural end, the predecessor resolves untruncated (`truncated`/`partialCycle` false by resolver contract); moving or deleting a next clip re-derives every affected neighbor from the document record only, deterministically, with no residue from the prior arrangement (no stored pre-computed extents)"
    - "BKG-05 gap law: beyond the last clip's end — or with zero clips on the track (BKG-01 empty) — every frame resolves to the gap verdict and reveals the document fallback; a Background track with zero clips is valid and renders fallback across the full range"
    - "BKG-07 linked-source contract: `sourceFrameRefs` are library asset IDs stored once per clip; every repeat instance k maps to `sourceFrameRefs[k mod cycleLength]` in natural-filename order across all repetitions (adjacency + ordering probes); no runtime registry entry or durable asset is created per repetition; a clip with an EMPTY `sourceFrameRefs` list is rejected fail-closed at parse/derivation — never resolves to content (BKG-07 empty)"
    - "BKG-08 undo contract: every op (`addBackgroundClip`, `moveBackgroundClip`, `setBackgroundClipRepeat`, `deleteBackgroundClip`, `setBackgroundFallback`) emits an acceptance descriptor the unified document-wide 10-level ledger records by reference; undo restores the exact prior clip/fallback state (clip deletion is one undo step, restore-by-reference, D-08) and redo re-applies it; record → undo → redo is proven at the store+history level for all five op kinds"
    - "BKG-09 idempotency + save dedup: repeat and fallback setters are revision-stable no-ops on same-value writes (no documentRevision bump, no dirty callback, no undo record — the setRotoBackgroundMetadata idempotence lesson at physicPaintStore.ts:1552-1569); re-saving an unchanged document with clips produces no new revision and no re-staging under the Phase 45 dedup fingerprint"
    - "BKG-09 hydration: on document register/hydrate, the runtime source-byte registry receives one `registerBackgroundSourceImage(sourceRef, dataUrl)` per distinct referenced asset — bytes decoded from the asset's `efxasset://` URL following the `_compositorDecode` idiom; a referenced asset missing from the library registers nothing and resolves through the existing knownSources-miss path to transparent + missing report (D-10 fail-closed); frames requested mid-hydration resolve conservatively (null this tick, re-render on decode-complete — the proven decode-economics idiom), never a crash"
    - "Resolver facts remain the single authority: the store ops never pre-compute or cache effective extents; `range.truncated`/`partialCycle`/`effectiveEnd` come from `deriveEfxPaintBackgroundResolution` only (anti Pitfall 10/m2)"
  artifacts:
    - path: "app/src/efx-paint/utils/naturalFilenameSort.ts"
      provides: "D-02 natural original-filename ordering util"
      exports: ["sortImagesByOriginalFilename"]
    - path: "app/src/stores/efxPaintStore.ts"
      provides: "Background clip CRUD document ops + fallback setter with collision verdicts"
      exports: ["addBackgroundClip", "moveBackgroundClip", "setBackgroundClipRepeat", "deleteBackgroundClip", "setBackgroundFallback"]
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "production caller of registerBackgroundSourceImage at document hydrate"
      exports: []
  key_links:
    - "addBackgroundClip/moveBackgroundClip → shared start-collision verdict over resolver-derived existing extents → closed result with fixed English reason (the UI in 49-05 publishes it via the status capsule verbatim, D-04)"
    - "hydrate path → efxasset:// decode → registerBackgroundSourceImage → resolver knownSources — the only production writer of the registry (Pitfall 5); without it every reopened clip reports Source missing"
    - "each op's acceptance descriptor → useRotoPhysicalEditHistory unified ledger (by reference, rasters stripped at record, 10-level) — BKG-08"
  prohibitions:
    - statement: "Clip source ordering must not use asset UUID sort or selection/click order — natural original-filename sort only (D-02)"
      status: kept
      verification: flagged-unverified
    - statement: "sourceFrameRefs must not store external file paths — library asset IDs only (D-09)"
      status: kept
      verification: flagged-unverified
    - statement: "Undo records must not snapshot raster bytes — reference-only snapshots on the unified ledger (BKG-08, Phase 46 D-03)"
      status: kept
      verification: flagged-unverified
    - statement: "Collision handling must not apply asymmetric snap or clamp behavior — reject-on-occupied-landing only, identical for import and drag (D-04)"
      status: kept
      verification: flagged-unverified
    - statement: "Store ops must not pre-compute, cache, or store loop effective extents — the resolver is the single authority (Pitfall 10/m2)"
      status: kept
      verification: flagged-unverified
---

<objective>
Deliver the pure/testable core of the Background authoring surface: the D-02 natural filename sort util, the five Background clip document ops on efxPaintStore (create/move/repeat/delete/fallback with the locked collision law), undo-by-reference integration, and the source-byte hydration that makes saved Background clips resolvable after reopen (BKG-09, Pitfall 5). No UI in this plan — 49-04/49-05/49-06 consume these ops.

Purpose: every UI surface (picker confirm, rail drag, right panel, selector) funnels through these store ops; proving them with contract tests first means the UI plans only wire, never re-derive law.

Output: natural-sort util, five efxPaintStore ops with English rejection reasons, hydration registration in physicPaintStore, and the store/history-level test suite covering BKG-03/04/05/07/08/09.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md
@app/src/stores/efxPaintStore.ts
@app/src/stores/physicPaintStore.ts
@app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts
@app/src/stores/imageStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `sortImagesByOriginalFilename` util — natural original-filename ordering (D-02)</name>
  <files>app/src/efx-paint/utils/naturalFilenameSort.ts, app/src/efx-paint/utils/naturalFilenameSort.test.ts</files>
  <read_first>
    - app/src/stores/imageStore.ts (ImportedImage shape; `original_path` basename derivation at line 199 — copy this exact extraction)
    - app/src-tauri/src/services/image_pool.rs (line 57: UUID v4 allocation proving id-order is arbitrary — the anti-pattern being locked out)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md (Pattern Assignment 5: recommended util shape)
  </read_first>
  <behavior>
    - Test 1: `['shot_10.png', 'shot_2.png', 'shot_1.png']` orders as `shot_1, shot_2, shot_10` (numeric-aware, numeric: true)
    - Test 2: UUID-named assets whose original filenames differ order by filename, and two assets whose original filenames are identical keep their input relative order (stability)
    - Test 3: no basename separator (bare filename as `original_path`) falls back to the full path string; case variants (`Shot_1` vs `shot_2`) compare case-insensitively (sensitivity: 'base')
    - Test 4: the input array is not mutated; an empty input returns an empty array (empty probe)
  </behavior>
  <action>
    Create `app/src/efx-paint/utils/` with the util: module-level `Intl.Collator(undefined, { numeric: true, sensitivity: 'base' })` instance, `sortImagesByOriginalFilename(images)` returning a NEW sorted array comparing `original_path` basenames (split on '/', last segment, fallback to full path — the imageStore.ts:199 pattern). Do not accept or sort by asset `id` anywhere in the comparator. RED tests first (suite fails on missing module), then the minimal implementation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/utils/naturalFilenameSort.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>All four behaviors green; the comparator contains no reference to asset `id`; input immutability asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Background clip CRUD document ops + fallback setter — collision verdicts, idempotence, undo descriptors</name>
  <files>app/src/stores/efxPaintStore.ts, app/src/stores/efxPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/efxPaintStore.ts (addTrack/renameTrack idiom at lines 169-222 — pure spread, canonical-revision idempotence compare, single bump, single _notifyChange; the shape every new op copies)
    - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (deriveEfxPaintBackgroundResolution — the resolver authority used ONLY to derive existing clips' resolved extents for the collision verdict; lines 140/157 gap verdicts, 195-215 findOwningRange)
    - app/src/efx-paint/document/efxPaintDocument.ts (BackgroundTrack, FrameLoopClip, FrameLoopClipRepeat shapes)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts (acceptance descriptor shape the ops must emit; by-reference record contract)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (Copywriting Contract — the two locked rejection strings)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md (Pattern Assignment 6 + Shared Patterns: pure document mutation, undo ledger)
  </read_first>
  <behavior>
    - Collision truth table (BKG-03, D-04): on a document with clip A occupying frames 10..29 — landing at 10, 15, 29 rejects with reason `'start-collision'`; landing at 30 (A's exclusive end) accepts (adjacency probe); landing at 0 accepts (before-A gap); a document with zero clips accepts any landing frame ≥ 0 (empty probe)
    - Downstream extent (BKG-03/D-03): creating at frame 0 a 10-image × 3 clip when clip B starts at 12 ACCEPTS and stores repeat ×3 verbatim — no truncation of stored data (interruption is a resolver/render concern)
    - Deterministic recalculation (BKG-05): after moving or deleting B, `deriveEfxPaintBackgroundResolution` on the mutated document yields the predecessor's natural end with `truncated: false` when nothing bounds it (ordering probe — state derives from the record only)
    - Repeat validation (BKG-04): counts 1 and 5000 accepted; 0, -3, 2.5, and non-finite rejected uncommitted; `mode: 'infinite'` round-trips; a repeat edit leaves every other clip's startFrame/repeat/refs byte-identical (no cross-clip ripple)
    - Linked sources (BKG-07): a 5-ref clip stored at ×3 keeps exactly 5 sourceFrameRefs; derivation maps repeat instance k to `refs[k mod 5]` (assert via resolver mapping input, not stored duplicates)
    - Idempotence (BKG-09): setting repeat to its current value and setting fallback to its current value bump no revision and notify nothing; create always allocates a fresh clip id (repeat imports of the same refs are distinct clips)
    - Undo (BKG-08): for each of the five op kinds — apply op, record descriptor on the unified ledger, undo, assert document equals pre-op canonical revision; redo equals post-op; a deleted clip returns with its original id/refs/repeat (by-reference restore, D-08)
    - Rejection results are closed: `{ ok: false, reason }` with reason strings exactly `'start-collision'` / `'invalid-repeat'` / `'clip-not-found'` / `'invalid-source-refs'` (empty refs rejected at the store boundary too) — the UI maps these to the locked English copy
  </behavior>
  <action>
    Implement in efxPaintStore.ts following the addTrack/renameTrack shape: `addBackgroundClip(layerId, { startFrame, sourceFrameRefs, repeat })`, `moveBackgroundClip(layerId, clipId, startFrame)`, `setBackgroundClipRepeat(layerId, clipId, repeat)`, `deleteBackgroundClip(layerId, clipId)`, `setBackgroundFallback(layerId, fallback)`. Shared private verdict: derive the existing clips' resolved extents via the existing resolution projection, reject strictly-occupied landing frames; everything else (including longer-than-gap) commits. Each op: validate → build candidate by immutable spread of `background` (clips array replaced/updated, sorted by startFrame for stable document order — render order ascending) → canonical-revision idempotence compare → single `documentRevision + 1` bump → `_documents.set` → `_notifyChange()` once → return `{ ok: true, ... }` with the acceptance descriptor for the ledger. Repeat validation coerces only true integers ≥ 1 per the clip-parser precedent shape. RED: write the full behavior suite first against missing exports, then GREEN.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>The full behavior suite green including the collision truth table, repeat validation, idempotence no-ops, and record→undo→redo for all five op kinds; reason strings match the locked set; no op stores pre-computed extents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: source-byte hydration on document register/hydrate — the sole production writer of registerBackgroundSourceImage (Pitfall 5, BKG-09)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts (registry + registerBackgroundSourceImage at lines 407-433; knownSources fail-closed check at lines 1420-1430; hydrateRuntimeFromDocument seam; the `_compositorDecode`/imageCache decode-complete idiom)
    - app/src/efx-paint/compositor/efxPaintCompositeCache.ts (T-48-07: registration clears the flattened memo — batch clears during hydration are harmless)
    - app/src/stores/imageStore.ts (MceImageRef shape and assetUrl composition for efxasset://)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md (Pitfall 5 + Open Question 1: decode compositor bytes from efxasset:// images)
  </read_first>
  <behavior>
    - Test 1 (REGISTERS ALL): hydrating a document whose two clips reference assets {a,b} and {b,c} calls registerBackgroundSourceImage exactly once per DISTINCT ref (a, b, c) with decoded bytes sourced from the asset URL (dedupe across clips — no per-clip duplicate registration)
    - Test 2 (MISSING IS EXPLICIT): a clip referencing an asset absent from the library registers nothing for it; resolving one of its frames yields the missing verdict (transparent + missing report), never a throw and never placeholder content (D-10)
    - Test 3 (CONSERVATIVE DURING DECODE): a frame requested while an asset's decode is pending resolves without crashing and re-renders on decode completion (the established async-decode idiom); hydration never blocks document registration
    - Test 4 (SAVE DEDUP): two consecutive save projections of the same hydrated document produce an identical dedup fingerprint — no revision churn from hydration registration itself (registration must not bump any document revision)
  </behavior>
  <action>
    Add a hydration step on the document register/hydrate path in physicPaintStore.ts: enumerate `document.background.clips[].sourceFrameRefs`, dedupe, resolve each to its library asset (id → URL via the established assetUrl/efxasset path), decode bytes via the `_compositorDecode` idiom, and call the existing `registerBackgroundSourceImage(sourceRef, dataUrl)`. Unknown asset ids are skipped (the knownSources-miss path reports them). The step is asynchronous byte-warming after registration — document registration itself stays synchronous; pending decodes resolve conservatively and trigger the decode-complete re-render. Registration is runtime-only: it must not touch documentRevision, the undo ledger, or the dirty callback. RED tests with a fake decoder port first, then implement.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/physicPaintStore.test.ts && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from either command, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>Reopen-path hydration registers bytes for every distinct clip ref; missing assets surface via the missing verdict; registration provably touches no document revision or dirty state; typecheck green.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

- **BKG-08 / unclassified probe row — unresolved, flagged assumption:** the probe could not classify BKG-08. Manual review: undo-by-reference semantics for clip ops are covered by the unified ledger contract (record/dedupe/replay) and the behavior suite in Task 2; no additional edge class beyond the idempotency/concurrency items resolved above is assumed to apply to clip undo. Flagged for verifier confirmation.
- Probe parity for this plan: BKG-03 (5 rows), BKG-04 (5), BKG-05 (3), BKG-07 (3), BKG-09 (2) = 18 rows authored into must_haves.truths; BKG-08 (1 row) surfaced here as a flagged assumption; BKG-02's sort-order semantics authored into must_haves.truths via the D-02 lock.

## Artifacts this phase produces (this plan)

- New files: `app/src/efx-paint/utils/naturalFilenameSort.ts`, `app/src/efx-paint/utils/naturalFilenameSort.test.ts`
- New exported symbols: `sortImagesByOriginalFilename` (util); efxPaintStore: `addBackgroundClip`, `moveBackgroundClip`, `setBackgroundClipRepeat`, `deleteBackgroundClip`, `setBackgroundFallback`; rejection-reason union members `'start-collision' | 'invalid-repeat' | 'clip-not-found' | 'invalid-source-refs'`
- Modified symbols: physicPaintStore hydrate path gains the background source-byte registration step (no new export unless the hydration helper is factored out)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document JSON (disk) → clip/fallback records | persisted clip fields (repeat, refs) cross into runtime; crafted values must fail closed |
| library assets → runtime byte registry | hydrated bytes are trusted post-decode; refs are library ids only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-02-01 | Tampering | clip CRUD repeat/refs validation | high | mitigate | repeat coerced to integer ≥ 1 or rejected uncommitted; empty sourceFrameRefs rejected at the store boundary; resolver derivation also throws on malformed clips (defense in depth, ASVS V5) |
| T-49-02-02 | Tampering | sourceFrameRefs as path vector | high | mitigate | refs are opaque library asset IDs (D-09); no path string is ever parsed from a ref; `efxasset://` allowed-roots enforcement unchanged (ASVS V12) |
| T-49-02-03 | Denial of Service | infinite repeat resolution | medium | mitigate | no UI extent math exists to explode; derivation is WeakMap-identity memoized; visible-window bound lives in the rail layer (T-47-04-03 carried) |
| T-49-02-04 | Spoofing | hydration decode of non-image bytes | low | mitigate | decode goes through the existing typed image-decode idiom; a failed decode leaves the ref unregistered → missing verdict, never partial bytes |
| T-49-02-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/utils src/stores/efxPaintStore.test.ts src/stores/physicPaintStore.test.ts` green
- `pnpm --filter efx-motion-editor exec vitest run` full suite green (no regression in compositor/persistence suites)
- `pnpm --dir app run typecheck` green
</verification>

<success_criteria>
All Background clip laws (collision, interruption, repeat, linked sources, undo, persistence, hydration) are executable store behavior proven by contract tests. UI plans consume these ops and the locked reason strings without re-deriving law.
</success_criteria>

<output>
Create `.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md` when done, recording the final op signatures and reason-string union for 49-04/49-05/49-06 to consume.
</output>
