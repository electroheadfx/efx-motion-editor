---
phase: 49-fixed-background-track-and-imported-loop-clips
plan: 06
type: execute
wave: 5
depends_on: ["49-05", "49-03"]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.tsx
  - app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: false
requirements: [BKG-04, BKG-05, BKG-07, BKG-08, BKG-09]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "S5 section contract (D-07): clicking a Bg clip rail selects it and the right panel shows a `Background Clip` section (Heading 14px/600) listing `Start frame`, `Repeat` (numeric + ∞ toggle), the source cycle fact, and `Delete clip`; with NO clip selected the section is ABSENT and the panel shows the active Track section as today (UI-SPEC right-panel rows: empty, populated); all field values render from accepted document state only, as fixed-format tabular numerics"
    - "Repeat control (BKG-04, D-06): the numeric input carries `aria-label=\"Repeat\"` with `aria-describedby` at the hint and commits on blur/Enter; the ∞ toggle is `aria-label=\"Loop indefinitely\"` with `aria-pressed` reflecting state; the badge shows `× {N}` / `× ∞`; invalid input (empty, zero, negative when not indefinite) does NOT commit — the hint `Enter a positive integer.` shows in the error treatment and the prior accepted value stays visible (UI-SPEC repeat rows: error); during async commit the previous accepted value remains visible with no optimistic facts (UI-SPEC repeat rows: loading)"
    - "Source cycle fact (BKG-07 surface/D-02): the section shows `{N} image(s)` and the tooltip lists the ORIGINAL FILENAMES in natural filename sort order, exactly matching the clip's `sourceFrameRefs` order — never UUIDs, never click order"
    - "Delete (D-08, UI-SPEC destructive table): `Delete clip` is a destructive-styled 24px icon action with `aria-label=\"Delete clip\"` and NO confirmation dialog; deletion commits immediately and one Undo restores the clip by reference (id/refs/repeat intact)"
    - "Undo/redo coverage surfaced (BKG-08): repeat changes, moves, deletions, imports, and fallback changes made through this UI are all undoable/redoable by reference on the unified ledger (the 49-02 op-level contract consumed, not re-implemented)"
    - "Rejection publication (UI-SPEC status capsule rows: error, overflow, long-text): any rejection from clip edits surfaces in the status capsule with the red warning triangle and `role=\"alert\"`; capsule text ellipsizes on one line with the full reason in the styled tooltip; the idle capsule retains its existing Phase 47/48 treatment (no phase-specific idle change)"
    - "Interruption truth surfaced (BKG-05): shortening a loop by adding/moving a next clip updates this section and the rail badge from resolver facts only; removing the interrupting clip restores the full requested duration deterministically (badge returns to the requested value)"
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.tsx"
      provides: "right-panel Background Clip properties section (S5)"
      exports: ["PhysicsPaintBackgroundClipSection"]
  key_links:
    - "clip-selection signal (49-05) → PhysicsPaintBackgroundClipSection mount → setBackgroundClipRepeat/deleteBackgroundClip ops (49-02) → accepted state → badge/rail/gap re-render — the panel never holds its own truth"
    - "fallback selector (49-03) + clip ops (49-02) → unified ledger → undo/redo covers every authoring path this phase adds (BKG-08)"
  prohibitions:
    - statement: "Background clip deletion must not gain a confirmation/acknowledge dialog — plain undoable delete only (D-08)"
      status: kept
      verification: flagged-unverified
    - statement: "The repeat input must not commit invalid or empty values and must not optimistically render an unaccepted repeat (D-06 validation pattern, UI-SPEC busy rule)"
      status: kept
      verification: flagged-unverified
    - statement: "Missing-source must not gain a re-import or re-link affordance in this phase — fail-closed transparent + capsule only (D-10, deferred)"
      status: kept
      verification: flagged-unverified
    - statement: "No raw clip UUID, asset UUID, or keyId may render in the section — filenames and frame numbers only (UI-SPEC copywriting contract)"
      status: kept
      verification: flagged-unverified
---

<objective>
Complete the authoring surface with the S5 right-panel `Background Clip` section (selection-driven properties: start frame, repeat + ∞ toggle, source cycle, undoable delete), then run the phase-closing blocking native UAT — including the Background-row native UAT deferred from Phase 48.

Purpose (BKG-04/07/08): the store ops exist and the row is interactive; this plan gives the user precise per-clip control and proves the whole phase natively, including save/reopen persistence (BKG-09) of clips, source order, repeats, gaps, and fallback.

Output: the clip-properties section with contract tests, and checkpoint evidence covering the full Phase 49 authoring loop.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-05-SUMMARY.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `PhysicsPaintBackgroundClipSection` — right-panel clip properties (S5)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.tsx, app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (existing right-panel Track section build + mount point — layout, md 16px section spacing, Label/Body typography rows; the 49-05 clip-selection signal to consume)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (numeric field pattern at lines 341 and 370: commit-on-blur/Enter, invalid-input-keeps-prior-value, hint span — the D-06 idiom)
    - app/src/efx-paint/utils/naturalFilenameSort.ts and imageStore.ts (source-cycle fact: refs → original filenames in clip order)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md (setBackgroundClipRepeat / deleteBackgroundClip signatures + idempotence contracts)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (S5 geometry, copy table rows incl. `Loop indefinitely` and `Enter a positive integer.`, accessibility rows, destructive treatment)
    - efx-preact-reactivity skill (signals-only state; section reads the document via narrow signal reads)
  </read_first>
  <behavior>
    - No clip selected → the section is absent entirely; the existing Track section renders unchanged (empty row)
    - Selected clip renders `Start frame {N}`, the Repeat control reflecting the accepted repeat (`× N` input value or ∞ toggle pressed), `{N} image(s)` source fact, and `Delete clip` — all from accepted state (populated row)
    - Repeat commit: Enter/blur with a positive integer calls `setBackgroundClipRepeat` once with `{ mode: 'finite', count }`; toggling `Loop indefinitely` on calls with `{ mode: 'infinite' }`; a `'invalid-repeat'` rejection leaves the input showing the prior accepted value and the hint in the error treatment
    - A same-value commit is a store-level no-op (no revision bump — idempotence is proven at 49-02; here assert the call is not skipped but is inert, or is skipped by value-equality guard — pick one, assert it)
    - Delete: calls `deleteBackgroundClip` with no dialog; the section disappears on the resulting empty selection; Undo restores the clip (integration through the store harness)
    - Source tooltip lists original filenames in the clip's stored order (natural sort from import) — assert with a `shot_1/shot_2/shot_10` fixture
  </behavior>
  <action>
    Build the section as a signals-driven component reading the selected clip from the document via the selection port (narrow signal reads per the reactivity skill). Repeat control reuses the PlayScript numeric-input idiom verbatim in behavior (commit-on-blur/Enter + prior-value-on-invalid), plus the ∞ toggle with `aria-pressed`. Wire the mount in the right panel next to the existing Track section (mutually exclusive: clip selection shows the clip section; track-only selection shows the Track section). Delete button calls the store op directly — no dialog. All copy verbatim from the UI-SPEC table. RED the component tests first with the existing Preact harness patterns, then implement.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from any command, or any "failed" count &gt; 0 in a vitest summary</fails_when>
  </verify>
  <done>The section renders only for a selected clip; repeat commits validate and preserve prior values on invalid input; the ∞ toggle works both directions with aria state; delete is dialog-free and undo-restorable; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Native UAT — Background authoring loop, collision law, interruption, fallback, save/reopen (BKG-01..BKG-09 evidence)</name>
  <files></files>
  <read_first>
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md (D-01..D-12 the UAT is evidencing)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (copy/interaction contract the UAT checks against)
    - SPECS/milestone-v1.0.0-plan.md §Phase 5 (the locked Background rules: non-overlap, repeat 1..∞, interruption, fallback, the required 5×3 and 10-instances-from-frame-15 examples)
  </read_first>
  <action>
    Present the user with the native UAT protocol below and wait for explicit confirmation per part. Any failure halts the phase for a fix, not a workaround. Record pass/fail plus user notes per part in the summary. This protocol also discharges the Background-row native UAT deferred from Phase 48 (48 close-out note).
  </action>
  <what-built>Plans 49-01..49-05 delivered: the extended persisted fallback union (Transparent/White/Paper 1-3 + grain, 'photo' excluded), the Background clip store ops with the symmetric start-collision law, natural-filename source ordering, undo-by-reference on the unified ledger, source-byte hydration on reopen, the fond re-wire to the document fallback with the flattened-cache fallback term, the monitor-only transparency checkerboard, the scoped cross-window asset picker with native import, the Bg-row Import control with locked rejection copy, and the row-local clip rail drag. All automated gates (vitest, typecheck) are green.</what-built>
  <how-to-verify>Native UAT on the user's machine (the assistant does not run the app):
    1. Import a multi-image sequence: with the playhead on a free frame, Import images → multi-select several sequentially-named images (e.g. shot_1, shot_2, shot_10) → Confirm. Confirm the clip lands AT the playhead, the rail badge reads `Cycle {N}f × 1 = {N}f`, and the source-cycle tooltip lists names in natural order (shot_1, shot_2, shot_10) — never UUID or click order (D-02/D-03).
    2. Repeat and the spec's 5x3 example: select the clip; in the right panel set Repeat to 3 on a 5-image clip and confirm the badge reads `Cycle 5f × 3 = 15f` and the loop cycles across 15 frames; toggle `Loop indefinitely` and confirm the `× ∞` badge and the loop extending to the next clip/parent end (BKG-04/BKG-05).
    3. Collision law both directions: place the playhead inside the existing clip and Confirm a new selection — confirm NOTHING changes and the capsule shows the red triangle with `Couldn't place the clip here. The playhead is inside an existing clip. Nothing changed.` Then drag the clip so its landing overlaps its neighbor — confirm the capsule shows the drag copy and prior geometry is restored (D-04).
    4. Interruption and recalculation: place a second clip so it interrupts the first mid-cycle — confirm `Loop shortened by next clip` + the diagonal partial-cycle cut and NO overlap; then move the interrupting clip away and confirm the first loop restores its full requested duration deterministically (BKG-05).
    5. Fallback + gaps: between/around clips, switch the Background selector through Transparent / White / Paper 1-3 and confirm the gaps, the monitor fond, the main-editor preview, and a PNG export agree per surface; with Transparent and no covering clip, confirm the monitor shows the transparency checkerboard while the exported PNG keeps real transparency (D-11/D-12, BKG-06).
    6. Undo/redo: undo the repeat change, the drag, the clip creation, and a fallback change one at a time — each restores exactly; redo re-applies (BKG-08).
    7. Save/reopen (BKG-09): save the project, close and reopen it, and confirm every clip, source order, repeat, gap, and the fallback survive, AND the Background clips resolve with actual image content (no `Source missing` from a hydration gap).
    8. Missing source (D-10/CMP-05 carried): remove a referenced image from the project library, reopen, and confirm the affected frames render transparent with the `Source missing` capsule — no placeholder fill, no crash.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step with a screenshot</resume-signal>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

Probe parity for this plan: the repeat-input (loading/error), right-panel (empty/populated), and status-capsule (empty/overflow/error/long-text) UI Considerations rows are authored into must_haves.truths above; the BKG-04/BKG-05/BKG-07/BKG-08/BKG-09 interaction-surface probes are covered by 49-02 truths and evidenced natively at Task 2. No unresolved probe rows are assigned to this plan.

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.tsx`, `app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.test.ts`
- New exported symbols: `PhysicsPaintBackgroundClipSection` (component)
- Modified: `PhysicsPaintStudio.tsx` (right-panel section mount + selection exclusivity)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| right-panel text input → document mutation | user text crosses into persisted repeat values via store validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-06-01 | Tampering | repeat input validation | high | mitigate | invalid input never commits (prior value preserved); `setBackgroundClipRepeat` enforces integer ≥ 1 / infinite at the store boundary (ASVS V5); tested in Task 1 |
| T-49-06-02 | Denial of Service | infinite-repeat display | medium | mitigate | visible-window bound (T-47-04-03) + derivation memoization; UAT step 2 evidence |
| T-49-06-03 | Repudiation | silent delete | low | mitigate | delete commits immediately and is one-step undoable by reference; UAT step 6 evidence |
| T-49-06-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run` full suite green
- `pnpm --dir app run typecheck` green
- Native UAT (Task 2) approved across all 8 parts
</verification>

<success_criteria>
Every BKG requirement has executable evidence: automated contract suites for the laws (49-01..49-05) and native UAT approval for the full authoring loop including save/reopen, fallback parity across surfaces, and the monitor-only checkerboard.
</success_criteria>

<output>
Create `.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-06-SUMMARY.md` when done, recording per-part UAT results.
</output>
