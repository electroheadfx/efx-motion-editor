---
phase: 49-fixed-background-track-and-imported-loop-clips
plan: 05
type: execute
wave: 4
depends_on: ["49-04", "49-02"]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements: [BKG-01, BKG-02, BKG-03, BKG-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "S1 Import control (BKG-01 surface): the fixed Background row header carries exactly one action — an icon button with `aria-label=\"Import images\"` and the 24px hit target — alongside the existing lock indicator; the locked row keeps NO reorder grab, NO duplicate/delete hover actions (47-CONTEXT D-06 carried); exactly one Background row exists beneath all Paint rows (BKG-01 uniqueness probe — structural: one document background record, one row, no add/duplicate/reorder affordance for it)"
    - "BKG-01 ordering probe: Paint-track add/reorder never moves the Background row and the flattened composite always draws background beneath all Paint tracks — a Paint-track reorder test asserts the Bg row and the composite order are untouched"
    - "Confirm placement (BKG-02, D-03): Confirm on the picker creates the clip at the CURRENT PLAYHEAD frame with `sourceFrameRefs` in natural filename order and repeat default finite 1; a one-image selection produces the one-frame-cycle clip (`Cycle 1f × 1`); Cancel creates nothing and mutates nothing"
    - "Import collision (BKG-03, D-04): a playhead strictly inside an existing clip rejects through the status capsule with the red warning triangle and EXACTLY the copy `Couldn't place the clip here. The playhead is inside an existing clip. Nothing changed.`; the document, selection, and focus are untouched; an adjacent/overlong placement commits and shows the interruption treatment from resolver facts only"
    - "S4 rail drag (D-05): dragging a Bg clip rail previews live along its own fixed row and commits on release via `moveBackgroundClip`; a landing frame inside another clip rejects through the capsule with EXACTLY `Couldn't move the clip here. The landing frame is inside an existing clip. Nothing changed.` and restores prior geometry; the document is never mutated during the gesture (release-time commit only)"
    - "Row-fixed law: Bg clip drag never leaves the Background row and never participates in the cross-track drag machinery (which covers Paint rows only, Phase 47 D-15); the Bg row exposes no cross-track destination highlight"
    - "Rail facts come from the resolver (capsule-never-math carried): requested badge `Cycle {N}f × {R} = {T}f` / `Cycle {N}f × ∞`, the shortened visual + `Loop shortened by next clip`, the diagonal cut on partial cycle, and the tooltip line `next clip — interrupts the loop` all read `deriveEfxPaintBackgroundResolution` facts (`range.truncated`/`partialCycle`/`effectiveEnd`); infinite repeats bound to the visible frame window (T-47-04-03 carried) and fully-outside clips render nothing (overflow probe covered)"
    - "Gap display (BKG-01 empty / UI-SPEC bg-row rows: empty, populated, zero-one-many): with zero clips the row shows the fallback display (transparent checkerboard or solid swatch) across the full range; many clips render as sequential non-overlapping rails in ascending startFrame order; long ranges clip at the shared horizontal viewport and return on scroll — never wrap or stack"
    - "Loading/busy contract (UI-SPEC bg-row rows: loading, error): commit-in-flight keeps accepted geometry with mutation controls `aria-disabled=\"true\"` and rail targets `aria-busy=\"true\"`; NO optimistic rail width, badge, or repeat change — all update from accepted canonical state only; on rejection prior geometry, selection, and focus are preserved and the capsule announces with `role=\"alert\"`"
    - "Deterministic recalculation surfaced (BKG-05): moving or deleting a next clip visibly recomputes the previous loop's rail extent and shortened state on the next render from resolver facts — no stale shortened treatment or ghost extent remains"
  artifacts:
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.ts"
      provides: "Bg-row rail drag gesture hook (live preview, release commit, row-fixed)"
      exports: ["usePhysicsPaintBackgroundClipDrag"]
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Bg row Import control + clip rail interactive affordances"
      exports: []
  key_links:
    - "Bg row Import button → Studio picker swap (49-04) → onConfirm(sortedIds) → efxPaintStore.addBackgroundClip(layerId, { startFrame: playhead, sourceFrameRefs, repeat: finite 1 }) → closed result → status capsule on 'start-collision' (D-03/D-04)"
    - "usePhysicsPaintBackgroundClipDrag release → moveBackgroundClip → rejection reason 'start-collision' → capsule with the locked drag copy — the same verdict function the import path uses (D-04 symmetric law)"
    - "clip rail click → clip selection signal → right-panel section mount (consumed by 49-06)"
  prohibitions:
    - statement: "The drag gesture must not mutate any document before pointer release — live preview is paint/projection only (Phase 43 release-commit contract)"
      status: kept
      verification: flagged-unverified
    - statement: "Background clips must not participate in the cross-track drag machinery — the Bg row is fixed and drag is row-local (Phase 47 D-15 boundary)"
      status: kept
      verification: flagged-unverified
    - statement: "No loop extent, repeat product, or interruption math may be computed in the strip — badges and extents come from resolver facts only (capsule-never-math, Pitfall 10/m2)"
      status: kept
      verification: flagged-unverified
    - statement: "No raw clip/asset UUID or source keyId may render in any user-facing surface (UI-SPEC copywriting contract)"
      status: kept
      verification: flagged-unverified
    - statement: "No optimistic UI facts — rails, badges, and repeat displays update from accepted canonical state only (UI-SPEC busy/rejected contract)"
      status: kept
      verification: flagged-unverified
---

<objective>
Wire the Background row user surface: the S1 Import control on the locked row header, the Confirm→create-at-playhead flow with the locked collision rejection copy, and the S4 row-local rail drag with release-time commit. The right-panel clip properties section is 49-06; this plan delivers everything the Bg row itself does.

Purpose (BKG-02/03/05): this is where D-03/D-04's symmetric collision law meets the user — import and drag share the 49-02 verdict, surface through the established capsule, and leave the resolver as the only extent authority.

Output: Import control + confirm flow wired to the picker and store; `usePhysicsPaintBackgroundClipDrag` hook with contract tests; clip-selection routing for 49-06.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-04-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
@app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bg-row Import control (S1) + Confirm placement flow with locked collision copy (D-03/D-04)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (Bg row mount at lines 3692-3742; the setApplyStatus('error') capsule port at line 2056; the established rejection-publication path from the paste UX)
    - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx (locked Bg header at lines 447-564 — kind="background", muted, lock indicator, no reorder grab; where the Import icon button mounts)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (49-04 picker swap state — this task supplies onConfirm/onCancel)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md (addBackgroundClip signature + reason union)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (S1 surface, copy table rows: `Import images`, collision import copy; accessibility: 24px hit target, focus restore)
  </read_first>
  <behavior>
    - Click `Import images`: the picker swap opens (engine untouched); document unchanged
    - Confirm with N selected: `addBackgroundClip(layerId, { startFrame: currentPlayheadFrame, sourceFrameRefs: natural-sorted ids, repeat: { mode: 'finite', count: 1 } })` invoked exactly once; success re-renders the new rail from accepted state (badge `Cycle {N}f × 1 = {N}f` from resolver facts)
    - Confirm with playhead strictly inside an existing clip: store returns `'start-collision'`; nothing is written; the status capsule shows the red warning triangle with EXACTLY `Couldn't place the clip here. The playhead is inside an existing clip. Nothing changed.`; selection/focus preserved
    - Confirm with a clip that overlaps the NEXT clip downstream: commits successfully; the new rail renders shortened treatment from resolver facts (`Loop shortened by next clip`) — no rejection
    - Cancel: no store call at all (assert zero invocations); focus returns to the Import control
    - Playhead capture: the frame is read at Confirm time from the playback cursor signal, not cached at picker-open time
  </behavior>
  <action>
    Add the Import icon button (lucide-preact, already the strip's icon library) to the Bg row header in the strip, styled per the muted row family with the 24px hit target and `aria-label="Import images"`; clicking toggles the Studio's picker swap signal (49-04 mount). In PhysicsPaintStudio.tsx supply the wiring: `onConfirm(ids)` → capture the current playhead frame from the cursor signal → call the 49-02 store op → on `'start-collision'` publish through the existing capsule port with the exact locked copy; on success close the swap (the rail reflects accepted state via existing reactive plumbing). `onCancel()` closes with zero store interaction. Tests drive the Studio harness with a stubbed store op for both branches; a source-level assertion confirms the rejection copy string appears exactly once and matches the UI-SPEC table verbatim.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>Import opens the picker; Confirm creates the clip at playhead with finite-1 default and natural-sorted refs; collision rejects with the exact locked copy and zero mutation; Cancel provably calls nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `usePhysicsPaintBackgroundClipDrag` — row-local rail drag with release commit, collision rejection, and click-to-select routing (S4)</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts and usePhysicsPaintKeyRailDrag.test.ts (the Phase 43 machinery being adapted: live preview, release-time commit, pointer-capture handling, hook test harness)
    - app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.ts (the cross-track boundary this hook must NOT enter — row-fixed enforcement reference)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (Bg clip rail render + where preview/disabled signals plug in; capsule port at 2056)
    - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (resolver facts the live preview projects — preview reads facts, never computes loop math)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (S4 contract + drag rejection copy + busy/rejected preservation rules)
    - efx-preact-reactivity skill (hook rules: narrow signal reads, identity-stable effect deps, no render-body writes)
  </read_first>
  <behavior>
    - Drag gesture: pointerdown on a Bg clip rail starts a live preview that follows the pointer along the Bg row pitch only (18px frame grid); no document mutation occurs during the gesture (assert zero store calls until release)
    - Release on a free frame: `moveBackgroundClip(layerId, clipId, landingFrame)` called exactly once; the rail snaps to accepted state
    - Release with landing inside another clip: store `'start-collision'` → capsule `Couldn't move the clip here. The landing frame is inside an existing clip. Nothing changed.`; prior geometry restored; drag state fully reset (next drag starts clean)
    - Release after a net-zero move: no store call (idempotence at the gesture level)
    - Row-fixed: vertical pointer movement is ignored for commit purposes; the gesture never produces a destinationTrackId and never triggers the cross-track machinery (structural test: the hook exposes no cross-track signals)
    - Click (press-release same frame): routes clip selection to the right-panel selection signal WITHOUT calling moveBackgroundClip
  </behavior>
  <action>
    Create the hook adapting the usePhysicsPaintKeyRailDrag pattern to a single fixed row: resolve the drag source from Bg clip rail elements, track the preview frame as a signal, take pointer capture on the Bg row region, and on release call `moveBackgroundClip` via injected port. Map `'start-collision'` to the capsule with the locked drag copy. Route a no-movement release to the clip-selection signal (consumed by 49-06's right panel). Never read or write cross-track destination state; never mutate the document before release. Wire the hook into the strip's Bg row mount. RED the hook tests first (harness mirrors usePhysicsPaintKeyRailDrag.test.ts), then implement.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.test.ts && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from any command, or any "failed" count &gt; 0 in a vitest summary</fails_when>
  </verify>
  <done>Drag previews live, commits once on release, rejects with the locked copy on collision, never crosses rows, never mutates mid-gesture, and click-to-select routes without a move call; full suite green.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

Probe parity for this plan: BKG-01 adjacency/empty/ordering probes and BKG-03/BKG-05 interaction-surface probes are authored into must_haves.truths above (uniqueness, zero-clip fallback display, beneath-all ordering, adjacency acceptance, deterministic recalculation); the Bg-row UI Considerations rows (empty/loading/error/populated/partial/overflow/zero-one-many) are likewise authored. No unresolved probe rows are assigned to this plan.

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.ts`, `app/src/components/physic-paint/hooks/usePhysicsPaintBackgroundClipDrag.test.ts`
- New exported symbols: `usePhysicsPaintBackgroundClipDrag` (hook); a clip-selection signal/port whose final name is recorded in the summary
- Modified: `PhysicsPaintWorkflowStrip.tsx` (Import control + hook mount), `PhysicsPaintStudio.tsx` (onConfirm/onCancel wiring)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer gestures → document mutations | untrusted spatial input crosses into persisted state via store verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-05-01 | Tampering | drag commit path | medium | mitigate | release-time commit only; the 49-02 store verdict is the single mutation gate; a crafted rapid pointer sequence cannot skip validation because preview state never persists |
| T-49-05-02 | Denial of Service | infinite-repeat rail rendering | medium | mitigate | visible-frame-window bound (T-47-04-03) carried verbatim; fully-outside clips render nothing; derivation memoized per record identity |
| T-49-05-03 | Repudiation | silent rejection outcomes | low | mitigate | every rejection publishes the fixed English reason via the `aria-live`/`role="alert"` capsule; zero-mutation invariant asserted by tests |
| T-49-05-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint` green
- `pnpm --filter efx-motion-editor exec vitest run` full suite green
- `pnpm --dir app run typecheck` green
</verification>

<success_criteria>
The Bg row imports at the playhead with the locked collision law, drags within its own row with release-time commit, and renders resolver-derived loop/gap/shortened facts — all rejection paths surface the exact UI-SPEC capsule copy.
</success_criteria>

<output>
Create `.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-05-SUMMARY.md` when done, recording the clip-selection port name for 49-06.
</output>
