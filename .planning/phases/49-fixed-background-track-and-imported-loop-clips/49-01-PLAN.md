---
phase: 49-fixed-background-track-and-imported-loop-clips
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/efx-paint/document/efxPaintDocument.ts
  - app/src/efx-paint/document/efxPaintDocumentParsers.ts
  - app/src/efx-paint/document/efxPaintDocumentRevision.ts
  - app/src/efx-paint/document/efxPaintBackgroundFallback.test.ts
autonomous: true
requirements: [BKG-06, BKG-09]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The `BackgroundFallback` union carries every Phase 49 fond selector mode (D-11): `{ mode: 'transparent' }`, `{ mode: 'solid'; color: string }`, and a paper mode `{ mode: 'paper'; texture: 'canvas1' | 'canvas2' | 'canvas3'; paperGrain: boolean; grainStrength: number }`; White maps to the existing solid mode with color '#ffffff' IF that round-trips cleanly through parser + canonical encoder, otherwise a distinct `mode: 'white'` literal is added — the round-trip tests are the deciding gate (RESEARCH Open Q2)"
    - "Round-trip per mode (BKG-09 persistence half): parse(serialize(document)) deep-equals document for every fallback mode including paper with grain on/off and every texture value; the parser rejects any unknown fallback mode, any extra or missing member, and a non-finite/negative `grainStrength`, staying exact-member fail-closed in the style of `efxPaintDocumentParsers.ts:207-227`"
    - "The canonical encoder emits a stable deterministic term per fallback mode (`transparent;`, `solid:<color>`, `paper:<texture>:<paperGrain>:<grainStrength>` shape — `efxPaintDocumentRevision.ts:30-33` extension); encoder output never contains revision or volatile fields, and identical documents hash identically"
    - "The fond-selector fallback always has exactly one active mode (transparent is the Phase 45 D-08 default); the fallback union is a closed one-of with no empty or partial state"
    - "A failed or invalid fallback mutation target never parses: malformed serialized fallback data throws at parse time before any store mutation (fail-closed, ASVS V5)"
  artifacts:
    - path: "app/src/efx-paint/document/efxPaintDocument.ts"
      provides: "extended BackgroundFallback union (paper mode + optional white literal)"
      exports: ["BackgroundFallback"]
    - path: "app/src/efx-paint/document/efxPaintDocumentParsers.ts"
      provides: "extended exact-member fallback parser branch"
      exports: []
    - path: "app/src/efx-paint/document/efxPaintDocumentRevision.ts"
      provides: "extended canonical fallback encoder switch"
      exports: []
    - path: "app/src/efx-paint/document/efxPaintBackgroundFallback.test.ts"
      provides: "per-mode round-trip + rejection contract suite"
      exports: []
  key_links:
    - "efxPaintDocument.ts BackgroundFallback union ↔ efxPaintDocumentParsers.ts fallback branch ↔ efxPaintDocumentRevision.ts fallback encoder — three modules extended in ONE commit; typecheck alone cannot prove the lockstep (Pitfall 6), only the round-trip tests can"
  prohibitions:
    - statement: "The parser and canonical encoder must not accept a `mode: 'photo'` fallback — the photo slot is reserved for the Phase 50 photoReference track (D-11)"
      status: kept
      verification: flagged-unverified
    - statement: "Exact-member validation must not be loosened — unknown keys, missing members, or partial acceptance of a fallback record are forbidden (ASVS V5, Pitfall 6)"
      status: kept
      verification: flagged-unverified
---

<objective>
Extend the persisted document fallback union so the existing fond selector (Transparent / White / Paper 1-3 + grain) can become the Background track's fallback config (D-11), and prove persistence round-trips for every new mode (BKG-09). This is the model half of D-11 only — consuming the fallback in the fond draw and wiring the selector is Plan 49-03; no UI, no store ops in this plan.

Purpose: the compositor already draws a fond beneath the composite and the document already persists `background.fallback`, but today's union (`transparent | solid`) cannot carry the paper/canvas modes the selector offers. Without this extension the D-11 re-wire in 49-03 has nothing to persist.

Output: extended `BackgroundFallback` union + fail-closed parser branch + canonical encoder term, with a round-trip + rejection contract suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md
@app/src/efx-paint/document/efxPaintDocument.ts
@app/src/efx-paint/document/efxPaintDocumentParsers.ts
@app/src/efx-paint/document/efxPaintDocumentRevision.ts
@app/src/efx-paint/document/efxPaintDocument.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): paper fallback mode end-to-end — one `{ mode: 'paper'; texture: 'canvas1' }` document through type, parser, and canonical encoder</name>
  <files>app/src/efx-paint/document/efxPaintDocument.ts, app/src/efx-paint/document/efxPaintDocumentParsers.ts, app/src/efx-paint/document/efxPaintDocumentRevision.ts, app/src/efx-paint/document/efxPaintBackgroundFallback.test.ts</files>
  <read_first>
    - app/src/efx-paint/document/efxPaintDocument.ts (current BackgroundFallback union at lines 20-22; document factory at lines 104-122)
    - app/src/efx-paint/document/efxPaintDocumentParsers.ts (existing fallback branch at lines 207-227; finite-count numeric coercion precedent at lines 118-133)
    - app/src/efx-paint/document/efxPaintDocumentRevision.ts (fallback encoder switch at lines 30-33; clips/fallback terms at lines 92-107)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md (Pattern Assignments 1-4, verbatim current unions)
  </read_first>
  <behavior>
    - Test 1 (ROUND-TRIP): a factory document whose `background.fallback` is `{ mode: 'paper', texture: 'canvas2', paperGrain: true, grainStrength: 0.5 }` serializes through the canonical document serialization and parses back deep-equal (BKG-09)
    - Test 2 (ENCODER): two documents identical except fallback paper texture (`canvas1` vs `canvas3`) produce different canonical revision hashes; two identical paper fallbacks produce identical hashes
    - Test 3 (FAIL-CLOSED): a serialized fallback `{ mode: 'paper', texture: 'canvas1' }` missing `paperGrain`/`grainStrength` throws at parse; `{ mode: 'paper', texture: 'canvas9' }` throws at parse
  </behavior>
  <action>
    RED: create `efxPaintBackgroundFallback.test.ts` with the three behaviors above; run the document suite and confirm all three fail (type does not exist yet). GREEN: extend the three modules in ONE commit to keep them in lockstep (Pitfall 6): (1) add `{ readonly mode: 'paper'; readonly texture: 'canvas1' | 'canvas2' | 'canvas3'; readonly paperGrain: boolean; readonly grainStrength: number }` to `BackgroundFallback` in efxPaintDocument.ts; (2) extend the parser branch with a `paper` case that validates texture membership, `paperGrain` as boolean, and `grainStrength` as a finite number (reuse the numeric-coercion/positivity shape of the clip-count parser at lines 118-133), throwing on unknown modes or extra/missing members in the existing error-message style; (3) extend the canonical encoder switch with a deterministic paper term covering texture, paperGrain, and grainStrength. Do not touch the clips encoder term; do not introduce a `white` literal yet (Task 2 decides).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document/efxPaintBackgroundFallback.test.ts</automated>
    <fails_when>non-zero exit, or a "FAIL" / "failed" line in the vitest summary for this file</fails_when>
  </verify>
  <done>The three tracer tests pass; the paper fallback mode round-trips through type + parser + encoder; `pnpm --dir app run typecheck` still exits 0 for the touched modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: complete the union — White mapping decision, grain edge validation, and the `photo`/unknown-mode rejection contract</name>
  <files>app/src/efx-paint/document/efxPaintDocument.ts, app/src/efx-paint/document/efxPaintDocumentParsers.ts, app/src/efx-paint/document/efxPaintDocumentRevision.ts, app/src/efx-paint/document/efxPaintBackgroundFallback.test.ts</files>
  <read_first>
    - app/src/efx-paint/document/efxPaintBackgroundFallback.test.ts (Task 1 tests being extended)
    - app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx (BACKGROUND_OPTIONS at lines 20-32 — the exact 5 selector modes the union must carry: transparent, white, canvas1, canvas2, canvas3)
    - app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts (paperGrain/grainStrength semantics at lines 41-49, including the deprecated 'photo' → 'transparent' branch)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md (D-11: 'photo' excluded, reserved for Phase 50)
  </read_first>
  <behavior>
    - Test 4 (WHITE MAPPING GATE): `{ mode: 'solid', color: '#ffffff' }` round-trips to itself; IF the selector's White mode can be represented as solid '#ffffff' with a total parser/encoder (no information loss), White uses the solid arm and NO `white` literal is added; otherwise add `{ mode: 'white' }` — encode whichever wins as an explicit allow-list assertion in the test (RESEARCH Open Q2 resolved by this gate)
    - Test 5 (GRAIN EDGES): `grainStrength: 0` and `paperGrain: false` round-trip; `grainStrength: -0.1`, `NaN`, `Infinity`, and a non-number throw at parse (boundary probe BKG-04 adjacency family — fail-closed at the min boundary, no clamping)
    - Test 6 (PHOTO RESERVED): a serialized fallback `{ mode: 'photo' }` throws at parse (D-11); an unknown mode string throws; a fallback object with an extra member throws — one assertion each, distinct error text per the existing parser style
    - Test 7 (REVISION STABILITY): the canonical revision of a document with a paper fallback is unchanged by field reordering in the serialized JSON (deterministic ordering)
  </behavior>
  <action>
    Extend the Task 1 test file: first write Test 4 RED and let its result decide the White mapping (do not pre-commit to either arm). If the `white` literal is needed, extend all three modules (type, parser, encoder) with it in the same commit. Then Tests 5-7. Keep all error strings in the established parser error style (exact-member, English). The union after this task covers exactly: transparent, solid (white-as-solid when the gate passes), paper — nothing else. Never add a `photo` arm. Run the full document directory suite to catch breakage in `efxPaintDocument.test.ts` / `efxPaintMultiTrackProjection.test.ts` from the union widening.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from either command, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <done>All seven tests green plus the pre-existing document suites; the union covers exactly the 5 selector modes (white mapped per the gate result); `photo`/unknown/extra-member inputs throw fail-closed; canonical revisions are deterministic.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC `## Edge Coverage` section; the probe fallback report carried 24 items. Items authored into this plan's must_haves: BKG-06/BKG-09 persistence probes (round-trip, idempotent revision stability, fail-closed malformed input). None of the 3 `unclassified` probe rows resolve to this plan (BKG-02 unclassified → 49-04; BKG-06 unclassified → 49-03; BKG-08 unclassified → 49-02). Assumption for this plan: none — every probe item landing on BKG-06/BKG-09 persistence semantics is resolved explicit above or in 49-02/49-03.

## Artifacts this phase produces (this plan)

- Modified symbols: `BackgroundFallback` (union extended with `paper` arm, optionally a `white` literal) in `app/src/efx-paint/document/efxPaintDocument.ts`
- New files: `app/src/efx-paint/document/efxPaintBackgroundFallback.test.ts`
- New exported symbols: none beyond the widened union type members

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce disk JSON → parseEfxPaintDocument | persisted project data crosses into the runtime model; the fallback record is attacker/legacy-writable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01-01 | Tampering | fallback parser branch | high | mitigate | exact-member fail-closed parse of every new fallback arm; Tests 3/5/6 assert throws on unknown mode, extra member, non-finite grainStrength (ASVS V5) |
| T-49-01-02 | Tampering | canonical encoder | medium | mitigate | deterministic per-mode terms; revision-stability test (Test 7) proves field-reorder insensitivity so crafted key order cannot churn or collide revisions |
| T-49-01-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document` green (new + pre-existing suites)
- `pnpm --dir app run typecheck` green
- The union, parser, and encoder landed in one commit (lockstep, Pitfall 6)
</verification>

<success_criteria>
Every fond-selector mode persists through the document schema with a clean round-trip and fail-closed rejection of reserved/unknown modes. Plans 49-02 (fallback setter) and 49-03 (fond re-wire + selector) can consume the extended union without further model work.
</success_criteria>

<output>
Create `.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-01-SUMMARY.md` when done, recording the White-mapping gate outcome (solid '#ffffff' vs distinct 'white' literal) as a decision.
</output>
