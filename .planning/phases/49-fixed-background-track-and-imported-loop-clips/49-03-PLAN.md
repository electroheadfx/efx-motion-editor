---
phase: 49-fixed-background-track-and-imported-loop-clips
plan: 03
type: execute
wave: 2
depends_on: ["49-01", "49-02"]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/efx-paint/compositor/efxPaintCompositeCache.ts
  - app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts
  - app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts
  - app/src/components/physic-paint/engine/physicsPaintStudioSettings.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements: [BKG-06, BKG-09]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-11 fond authority: `_resolveDocumentFondInstruction` reads ONLY `document.background.fallback` — the per-track `_rotoBackgroundMetadata` fond walk (physicPaintStore.ts:845-852) is deleted, not shadowed; both fond derivation sites (store instruction AND the monitor-inline `fondBackground` at PhysicsPaintStudio.tsx:3073-3083) resolve the same fallback record so Studio monitor, row gap swatches, flattened parent output, main preview, and export can never disagree (BKG-06; Pitfall 1 two-sites-not-one)"
    - "Selector round-trip: the `Background` segmented swatch row (Transparent / White / Paper 1 / Paper 2 / Paper 3 + grain controls) writes through `efxPaintStore.setBackgroundFallback` and reflects the persisted document fallback; clicking a swatch bumps revision + dirty exactly once (single dirty-callback law) and a same-mode click is a revision-stable no-op; a failed mutation leaves the previous mode active with the reason in the status capsule (UI-SPEC Fallback selector rows: populated, error)"
    - "The selector option set is exactly the fixed five modes with grain controls and never offers `Photo` (D-11 reservation asserted as a test); the active mode is always unambiguously one-of (no empty/partial selector state)"
    - "Flattened-cache fallback term (BKG-09/CMP-04 carried): the derived flattened cache key gains a fallback-content term (canonical encoding of the fallback record) alongside the existing `bg:${background.revision}` term — a fallback-mode change with an unchanged background.revision still produces a different key, so a stale fond raster is never served (Pitfall 1)"
    - "Gap truth (BKG-06, empty probe): any frame resolving to the gap verdict reveals the document fallback identically on every surface — transparent mode reveals true transparency in the flattened raster (the fond is never composited INTO the flattened pixels), while the Studio monitor shows it beneath the composite"
    - "D-12 checkerboard (UI-SPEC monitor checkerboard rows: empty, populated): when and only when the effective fond is fully transparent (transparent fallback mode, no paper texture, no clip covering the frame), the Studio program monitor draws a transparency checkerboard — `repeating-conic-gradient(#777 0% 25%, #d8d8d8 0% 50%) 0 0 / 8px 8px` family — beneath the composite, clipped to canvas bounds; any active fond shows the fond as today; the checkerboard is paint-only on a monitor layer, never a document state"
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "fallback-authoritative _resolveDocumentFondInstruction"
      exports: []
    - path: "app/src/efx-paint/compositor/efxPaintCompositeCache.ts"
      provides: "flattened cache key fallback-content term"
      exports: []
    - path: "app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts"
      provides: "selector swatch ↔ document fallback mapping (write-through + reflect)"
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "monitor fondBackground re-wired to document fallback + monitor-only transparency checkerboard layer"
      exports: []
  key_links:
    - "physicsPaintStudioSettings swatch click → efxPaintStore.setBackgroundFallback (49-02 op) → document fallback → _resolveDocumentFondInstruction + monitor fondBackground — one authority, two consumers, one mutation path"
    - "document.background.fallback → efxPaintCompositeCache key term → flattened memo invalidation — a swatch change re-composites even when track revisions hold"
  prohibitions:
    - statement: "The transparency checkerboard must not appear in the flattened raster, main preview, or export — monitor-only paint (D-12)"
      status: kept
      verification: flagged-unverified
    - statement: "The fallback selector must not expose the 'photo' fond mode in Phase 49 — reserved for the Phase 50 photoReference slot (D-11)"
      status: kept
      verification: flagged-unverified
    - statement: "No second fond authority may remain: the per-track roto background metadata fond derivation must not survive alongside the document-fallback re-wire (Pitfall 1)"
      status: kept
      verification: flagged-unverified
---

<objective>
Complete D-11's consumption half and D-12: make the persisted document fallback (49-01 union, 49-02 setter) the single fond authority for both derivation sites, wire the existing Background swatch selector as the fallback config UI, add the fallback term to the flattened cache key, and add the monitor-only transparency checkerboard for the no-fond case.

Purpose: today the fond is derived from per-track roto background metadata, so the selector would write the document while the renderer reads metadata — the exact Pitfall 1 split-brain. This plan collapses to one authority and proves cache invalidation covers fallback changes (BKG-09).

Output: re-wired store instruction + monitor fond, selector write-through, cache-key term, checkerboard monitor layer; all gated by tests including the 'photo'-absence and raster-non-regression checks.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-01-SUMMARY.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md
@app/src/stores/physicPaintStore.ts
@app/src/efx-paint/compositor/efxPaintCompositeCache.ts
@app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: re-wire `_resolveDocumentFondInstruction` to the document fallback + flattened-cache fallback term</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts, app/src/efx-paint/compositor/efxPaintCompositeCache.ts, app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts (current metadata-walk fond derivation at lines 845-852; resolveMissingRotoFrameDraw the instruction is built from; getFlattenedFrame seam)
    - app/src/efx-paint/compositor/efxPaintCompositeCache.ts (key construction around line 125 — existing `bg:${background.revision}` term; encodeCanonicalString usage from 48-01)
    - app/src/efx-paint/document/efxPaintDocumentRevision.ts (canonical encoder — the fallback-content term should reuse its per-mode term shape, not a second encoder switch)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md (Pitfall 1 verbatim quote of the code being replaced)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-01-SUMMARY.md (final union shape + white-mapping outcome)
  </read_first>
  <behavior>
    - Test 1: with fallback `{ mode: 'solid', color: '#ffffff' }` the fond instruction fills solid white regardless of any per-track roto background metadata present or absent
    - Test 2: with fallback `{ mode: 'paper', texture: 'canvas2', ... }` the instruction carries the canvas2 paper draw (parity with the paper path produced today via metadata); with `{ mode: 'transparent' }` no fond instruction is produced
    - Test 3: the flattened cache key differs between two documents identical except fallback mode with EQUAL background.revision values (a same-revision fallback-content change invalidates); identical documents yield identical keys
    - Test 4: deleting a per-track `_rotoBackgroundMetadata` entry no longer changes the fond instruction (the metadata walk is gone)
  </behavior>
  <action>
    RED tests first against the current metadata-walk behavior where distinguishable. Then: replace the fond derivation in `_resolveDocumentFondInstruction` with a pure mapping from `document.background.fallback` (transparent → none; solid → solid fill; paper → paper draw with paperGrain/grainStrength), deleting the ordered-track metadata walk outright. In efxPaintCompositeCache.ts add the fallback-content term to the flattened key using the canonical per-mode term (reuse the document revision encoder's fallback term logic — do not hand-write a second switch that can drift). Keep the compositor's raster output unchanged: the fond stays BENEATH the flattened straight-alpha result and is never composited into it (the flattened raster keeps real transparency in gaps).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/physicPaintStore.test.ts src/efx-paint/compositor/efxPaintCompositeCache.test.ts src/efx-paint/compositor && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>Fond derivation consumes only the document fallback; the metadata walk is deleted; the cache key carries the fallback-content term; the full compositor suite stays green (flattened raster semantics unchanged).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fond selector → document fallback write-through (S6) with idempotent same-mode clicks</name>
  <files>app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts, app/src/components/physic-paint/engine/physicsPaintStudioSettings.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts (full file — existing settings.background ↔ metadata mapping at lines 41-49 including the deprecated 'photo' → 'transparent' branch)
    - app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx (BACKGROUND_OPTIONS at lines 20-32 — segment order and labels, unchanged)
    - app/src/stores/physicPaintStore.ts (setRotoBackgroundMetadata idempotence lesson at lines 1552-1569 — the render-loop OOM failure mode a non-idempotent write causes)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md (setBackgroundFallback signature + idempotence contract)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (S6 surface contract + Fallback selector copy rows)
  </read_first>
  <behavior>
    - Test 1 (WRITE-THROUGH MAP): each selector value maps to exactly one document fallback record — transparent → `{ mode: 'transparent' }`; white → the 49-01-gated white representation; canvas1/2/3 → `{ mode: 'paper', texture, paperGrain, grainStrength }` carrying the current grain controls
    - Test 2 (REFLECT): given a document fallback, the selector's active segment resolves unambiguously (solid non-white colors are not producible by the selector; if encountered after document edits, the selector shows the closest locked treatment per UI copy — assert the defined behavior, never a blank selector)
    - Test 3 (IDEMPOTENCE): dispatching the current mode performs a revision-stable no-op (no documentRevision bump, no dirty callback — the 1552-1569 lesson)
    - Test 4 (PHOTO ABSENT): the selector's option set contains no 'photo' value and the legacy settings `'photo'` branch no longer feeds the fallback surface (D-11; test asserts the fixed 5-option map)
  </behavior>
  <action>
    Replace the settings→metadata mapping with a settings→document-fallback mapping consumed by the swatch click handler: click → build the fallback record for the chosen mode (grain controls from current settings) → `efxPaintStore.setBackgroundFallback(layerId, fallback)`. Reflection derives the active segment from the document fallback (document is authoritative, settings mirror). Remove the `'photo'` reliance branch from the fallback path (the engine BgMode union itself is untouched — only the persisted/document surface loses 'photo'). Keep all English labels and the existing segmented-row markup unchanged; this task is wiring + mapping, no new visual UI.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/engine/physicsPaintStudioSettings.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>Every selector mode round-trips through the document fallback; same-mode clicks are provable no-ops; 'photo' is absent from the fallback surface; grain controls ride the paper mode.</done>
</task>

<task type="auto">
  <name>Task 3: monitor fondBackground re-wire + transparency checkerboard (D-12, S7)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (canvas-stack memo with the inline `fondBackground` derivation at lines 3073-3083 — being replaced; the program monitor layer ordering from Phase 48 UAT-C)
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts (existing monitor/canvas-stack test harness to extend)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (S7 + monitor checkerboard geometry row: 8px cells, two-gray repeating-conic-gradient, beneath composite, clipped to canvas bounds; state precedence rules)
    - .claude/skills or .agents/skills `efx-preact-reactivity` SKILL.md (signal-read rules for the canvas-stack memo — identity-stable deps, narrow reads)
  </read_first>
  <behavior>
    - The monitor's `fondBackground` in the canvas-stack memo resolves from the document fallback via the same store instruction as the flattened path (no inline derivation remains)
    - Checkerboard layer present IFF the resolved fond is absent/transparent for the current frame; with a solid or paper fallback active the checkerboard layer is absent and the fond shows as today (one branch, tested both ways)
    - The checkerboard uses the `repeating-conic-gradient(#777 0% 25%, #d8d8d8 0% 50%) 0 0 / 8px 8px` treatment on a layer beneath the monitor content, clipped to canvas bounds
    - Raster non-regression: the checkerboard exists only as a paint layer in the monitor stack — test asserts no checkerboard reference exists in the compositor, flattened cache, previewRenderer, or exportRenderer modules
  </behavior>
  <action>
    Rewire the canvas-stack memo to read the store's resolved fond instruction (subscribing with identity-stable deps per efx-preact-reactivity — efxPaintVersion plus the document reference, never a render-body signal write). Add a sibling layer beneath the monitor content that renders the checkerboard treatment only in the no-fond case (transparent effective fond AND no clip covering the resolved frame — consume the already-resolved background frame verdict from 48-05's monitor plumbing rather than re-resolving). Add component tests for both branches and the source-level raster non-regression assertion. No geometry changes elsewhere.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from any command, or any "failed" count &gt; 0 in a vitest summary</fails_when>
  </verify>
  <done>Monitor fond and checkerboard both derive from the documented fallback contract; full suite green (flattened path provably untouched — no checkerboard text in compositor/renderer modules).</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

- **BKG-06 / unclassified probe row — unresolved, flagged assumption:** the probe could not classify BKG-06. Manual review: the gap-reveals-fallback truth table across surfaces is covered by the truths above (identical fallback on row/monitor/flattened/preview/export, checkerboard monitor-only); no further edge class is assumed. Flagged for verifier confirmation.

## Artifacts this phase produces (this plan)

- New files: none
- New exported symbols: none (behavioral re-wiring; settings module may gain an exported `backgroundModeToFallback` mapping function if factored for testing — record the final name in the summary)
- Modified: `physicPaintStore.ts` fond instruction, `efxPaintCompositeCache.ts` key terms, `physicsPaintStudioSettings.ts` fallback mapping, `PhysicsPaintStudio.tsx` monitor layers

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selector UI → document fallback mutation | user-driven but must stay one-of and idempotent (render-loop safety) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03-01 | Denial of Service | fallback setter reactivity | high | mitigate | revision-stable no-op on same-mode writes asserted by test (a per-frame write loop was the documented OOM incident — physicPaintStore.ts:1552-1569 lesson) |
| T-49-03-02 | Tampering | cache-key fallback term | medium | mitigate | fallback term uses the canonical encoder term (single source), so crafted/corrupt fallback records cannot produce divergent keys between save and render paths |
| T-49-03-03 | Information Disclosure | checkerboard leaking into output | low | mitigate | raster non-regression test asserts the treatment exists only in the Studio monitor module; flattened/preview/export modules carry no reference |
| T-49-03-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run` full suite green
- `pnpm --dir app run typecheck` green
- Fond single-authority proven by Task 1 Test 4 (metadata deletion is inert)
</verification>

<success_criteria>
The Background swatch selector is the persisted document fallback config end-to-end; gaps reveal it identically on every surface; the monitor shows a checkerboard only in the no-fond case; a fallback change always invalidates the flattened cache.
</success_criteria>

<output>
Create `.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-03-SUMMARY.md` when done.
</output>
