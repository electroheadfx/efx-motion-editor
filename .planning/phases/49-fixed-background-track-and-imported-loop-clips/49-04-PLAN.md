---
phase: 49-fixed-background-track-and-imported-loop-clips
plan: 04
type: execute
wave: 3
depends_on: ["49-02", "49-03"]
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts
  - app/src-tauri/capabilities/physics-paint.json
  - app/src/components/physic-paint/view/BackgroundAssetPickerView.tsx
  - app/src/components/physic-paint/view/BackgroundAssetPickerView.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: false
requirements: [BKG-01, BKG-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cross-window library seam (Pitfall 2): a new operationId-correlated bridge pair `physic-paint:image-library-request` / `physic-paint:image-library-result` lets the Studio webview request `{ images: MceImageRef[], projectDir: string }` from the main webview (the authoritative imageStore realm); the pair follows the existing script-library/roto-authority idiom (physicPaintBridge.ts:85-94), rejects unknown/mismatched payloads at the bridge boundary, and answers only with the latest matching operationId"
    - "Least-privilege capability (V4): `app/src-tauri/capabilities/physics-paint.json` gains exactly `dialog:allow-open` — NO `fs:*` grant is added; library images display through the app-wide `efxasset://` protocol which needs no fs permission (CSP already allows it, tauri.conf.json:38)"
    - "S2 full-area swap (D-01): `BackgroundAssetPickerView` is a bordered panel filling the canvas region with a top bar (`Import background images` title + named `Confirm`/`Cancel` buttons) and an images-only multi-select grid; it is `role=\"region\"` with `aria-label=\"Import background images\"`, focus moves to its first actionable control on open, and Confirm/Cancel restore focus to the Import control; NO backdrop overlay and NO Tab trap (region swap, not a modal)"
    - "Engine lock (D-01): opening or closing the picker never unmounts or re-creates the engine canvas — the swap is paint/visibility only with the engine paused underneath, and the composite refreshes on return"
    - "D-01 pick-or-import: the picker's own `Import` button opens the native file dialog and imports into the project library via the existing import path (`importFiles(paths, projectDir)` with the bridge-delivered projectDir); newly imported images appear selectable without leaving the picker; selecting already-imported images reuses assets without copying (D-09/BKG-07 carried)"
    - "Picker states (UI-SPEC asset picker rows): empty library shows exactly `Drag & drop images here or use Import button`; `Confirm` stays disabled with zero selection; `Confirm` enablement depends only on selection, not click order; the grid scrolls within the full-area panel and tiles never wrap out of the region (overflow); the grid refreshes fully on open with no partial-render state (loading); an import failure leaves the picker open with prior selection preserved and the reason surfaced through the status channel (error); long filenames truncate with ellipsis on tiles with full names in hover tooltips (long-text)"
    - "Confirm ordering (BKG-02/D-02): the confirmed selection is emitted to the caller ordered by `sortImagesByOriginalFilename` — click order and asset UUID never influence the emitted reference order (zero-one-many row: one image → single-frame-cycle source; many → full natural-sorted cycle)"
    - "Grid isolation (Pitfall 4): the Studio variant surfaces no usage badges, no cascade-delete affordance, and no usage popover paths; it never reaches `sequenceStore`/`audioStore`/`getAllAssetUsages` in the Studio realm; no `useState` is introduced in new Studio code (efx-preact-reactivity)"
  artifacts:
    - path: "app/src/components/physic-paint/view/BackgroundAssetPickerView.tsx"
      provides: "scoped images-only asset picker (S2)"
      exports: ["BackgroundAssetPickerView"]
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "image-library request/result pair + projectDir delivery"
      exports: ["requestImageLibrary"]
    - path: "app/src-tauri/capabilities/physics-paint.json"
      provides: "dialog:allow-open permission for the efx-physic-paint window"
      exports: []
  key_links:
    - "BackgroundAssetPickerView open → physic-paint:image-library-request → main-webview imageStore → result payload { images, projectDir } → grid render + importFiles(paths, projectDir) — the seam that makes the Studio realm non-empty (Pitfall 2)"
    - "Confirm → sortImagesByOriginalFilename (49-02) → injected onConfirm(sortedRefs) — the only exit that emits references; Cancel emits nothing and mutates nothing (D-01)"
  prohibitions:
    - statement: "ImportedView must not be ported, mounted, or imported into the Studio window — picker composition is imageStore + ImportGrid-derived only (D-01)"
      status: kept
      verification: flagged-unverified
    - statement: "The physics-paint window capability file must not gain broad fs:* permissions — exactly dialog:allow-open is added (V4 least privilege, 45-02 precedent class)"
      status: kept
      verification: flagged-unverified
    - statement: "The engine canvas must not be unmounted or re-created when the picker opens or closes (D-01 lock)"
      status: kept
      verification: flagged-unverified
    - statement: "New Studio code must not introduce useState — signals/computed only per efx-preact-reactivity; ImportGrid's existing internal useState must not propagate into the Studio variant (Pitfall 4)"
      status: kept
      verification: flagged-unverified
    - statement: "The picker must not surface usage badges, cascade-delete, or any asset-mutation affordance beyond Import (Pitfall 4)"
      status: kept
      verification: flagged-unverified
---

<objective>
Build the scoped asset-picker surface (S2) the Bg row's Import control will open: the cross-window bridge pair that fills the Studio realm's empty image library, the one-permission capability extension that makes in-picker native import legal, and the `BackgroundAssetPickerView` full-area swap component with Confirm/Cancel semantics. The Import control itself and the clip-creation wiring land in 49-05.

Purpose (BKG-02): without the bridge seam the picker opens onto an empty realm (Pitfall 2), and without `dialog:allow-open` the in-picker Import button silently no-ops (Pitfall 3). Both are structural prerequisites proven here before the row UI depends on them.

Output: bridge pair + capability delta + picker component, plus a blocking native checkpoint proving the dialog actually opens and imported images appear in the Studio-window picker.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-CONTEXT.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-PATTERNS.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md
@.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-02-SUMMARY.md
@app/src/lib/physicPaintBridge.ts
@app/src/stores/imageStore.ts
@app/src/components/import/ImportGrid.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: image-library bridge pair + projectDir delivery + capability extension</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts, app/src-tauri/capabilities/physics-paint.json</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts (existing request/result pairs at lines 85-94 — script-library, roto-authority, thumbnail-encode; unknown-payload rejection at line 641; the main-window publisher side and the project-context push precedent at 2296-2310)
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts (existing transport test harness for request/result pairs)
    - app/src/stores/imageStore.ts (`importFiles(paths, projectDir)` signature at line 65 — projectDir is mandatory; MceImageRef production via toMceImages at line 199)
    - app/src/types/project.ts (MceImageRef shape at lines 190-198)
    - app/src/types/physicPaint.ts (PhysicPaintProjectContext at 1670-1676 — no projectDir today, confirming the gap)
    - app/src-tauri/capabilities/physics-paint.json and app/src-tauri/capabilities/default.json (the exact permission delta: default.json carries dialog:allow-open at lines 15-28; physics-paint.json does not)
  </read_first>
  <behavior>
    - Test 1 (ROUND-TRIP): a `physic-paint:image-library-request` with operationId X receives exactly the result event for X carrying `{ images: MceImageRef[], projectDir: string }` published from the main-window imageStore state at request time
    - Test 2 (CORRELATION): a late result for a superseded operationId is dropped; two overlapping requests resolve independently
    - Test 3 (VALIDATION): a malformed result payload (missing images array, missing projectDir, unknown members per the bridge's established validation posture) is rejected at the bridge boundary without surfacing to consumers
    - Test 4 (CAPABILITY DELTA): physics-paint.json parses and its permission set is exactly the prior set + `dialog:allow-open` (structural JSON assertion — no fs:* permission present)
  </behavior>
  <action>
    Add the `physic-paint:image-library-request/result` pair to physicPaintBridge.ts following the verbatim shape of the script-library pair: consumer-side `requestImageLibrary()` returning a Promise correlated by operationId; publisher-side listener in the main webview answering with `{ images: toMceImages()-shaped array, projectDir }` from imageStore/projectStore state. Validate payloads at the boundary in the existing posture. Extend physics-paint.json with the single `dialog:allow-open` permission (mirror default.json's spelling). Do NOT add any fs permission — thumbnail display rides the app-wide efxasset:// CSP grant. RED the four tests, then implement. RESEARCH assumption A2 is exercised here: custom (non-plugin) commands need no capability grant; only the dialog plugin does.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>Bridge pair correlates, validates, and delivers images+projectDir; capability file carries exactly one new permission; all bridge transport tests green.</done>
</task>

<task type="auto">
  <name>Task 2: `BackgroundAssetPickerView` — scoped full-area picker (S2) and the Studio region swap</name>
  <files>app/src/components/physic-paint/view/BackgroundAssetPickerView.tsx, app/src/components/physic-paint/view/BackgroundAssetPickerView.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/import/ImportGrid.tsx (props contract at lines 14-29; coupling imports at lines 6-12 being isolated; tile/empty-state copy being reused)
    - app/src/stores/imageStore.ts (images signal, importFiles, display-URL composition)
    - app/src/efx-paint/utils/naturalFilenameSort.ts (49-02 util — Confirm ordering)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (canvas region structure where the swap mounts; engine mount site that must keep living)
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-UI-SPEC.md (S2 geometry: bordered panel, top bar, md 16px grid gutter, heading 14px/600 title, spacing tokens; accessibility contract: role/aria labels, focus move/restore, reduced motion; empty-state copy)
    - efx-preact-reactivity skill (signal/computed-only state in new Studio code)
  </read_first>
  <behavior>
    - Open: swap appears over the canvas region, library refreshes via `requestImageLibrary()`, focus lands on the first actionable control; the engine canvas element remains mounted in the DOM (test asserts the canvas node identity before/after open)
    - Selection: multi-select via the grid's `onToggleSelect` marks; `Confirm` disabled at zero selection; `aria-pressed`/selected mark treatment carries the existing `Selected` button state (non-color cue)
    - Confirm: emits `onConfirm(sortedIds)` where sortedIds are `sortImagesByOriginalFilename`-ordered; Cancel emits nothing and leaves the document untouched
    - Import-in-picker: the `Import` button calls the dialog `open()` then `importFiles(paths, projectDir)`; imported images appear in the grid without closing the picker
    - Close: unmounts the panel, restores focus to the Import control; no slide/scale/tween animation (immediate paint change)
  </behavior>
  <action>
    Build `BackgroundAssetPickerView` as a signal-driven functional component (useSignal/useComputed only — no useState). Compose the grid per Pitfall 4 option (b): a Studio-safe adapter around ImportGrid's tile/selection contract with `assetFilter='images-only'` and the usage/context-menu affordances compiled out — if ImportGrid's internals cannot prop-disable those paths, extract a lean tile+multi-select core into the view file instead; never import sequenceStore/audioStore/cascadeRemoveAsset into Studio module graphs. Wire the Studio: a view-swap signal (engine mounted underneath, paused) that mounts the picker over the canvas region; on open, fire `requestImageLibrary()` and populate local signal state; pass `onConfirm`/`onCancel` down. Empty state and all copy verbatim from the UI-SPEC copy table. Component tests mount with the existing Preact test harness patterns and stub the bridge port.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/BackgroundAssetPickerView.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary</fails_when>
  </verify>
  <done>Picker opens with a live library, selects, imports, confirms natural-sorted, and cancels cleanly; engine identity preserved across the swap; all copy/aria contracts match the UI-SPEC table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Native checkpoint — picker dialog permission and cross-window library proof</name>
  <files></files>
  <read_first>
    - .planning/phases/49-fixed-background-track-and-imported-loop-clips/49-RESEARCH.md (Pitfall 2 realm isolation, Pitfall 3 capability delta — what this checkpoint evidences)
  </read_first>
  <action>
    Present the user with the native verification protocol below and wait for explicit confirmation. Any failure halts the phase for a fix, not a workaround (a capability denial cannot be evidenced by unit tests — this is the 45-02/10da700a failure class). Record pass/fail plus user notes in the summary.
  </action>
  <what-built>The scoped asset picker with its bridge seam: the Studio-window picker populates from the project library via the new image-library request/result pair, and the Studio window now holds the `dialog:allow-open` capability so the in-picker Import button can open the native file dialog. Unit/contract suites are green.</what-built>
    <how-to-verify>Native UAT on the user's machine (the assistant does not run the app):
    1. Open a project that already has imported images; open the EFX Physic Paint Studio; invoke the picker (temporary wiring: the plan's Studio dev hook or the 49-05 Import control if already landed). Confirm the picker shows the project's existing images — NOT the empty state (realm-isolation proof, Pitfall 2).
    2. Click the picker's `Import` button. Confirm the native OS file dialog opens (capability proof, Pitfall 3) and importing a new image adds it to the picker grid without closing the picker.
    3. Multi-select several images and click `Confirm`: confirm the emitted order follows original natural filename order (visible in 49-05 once the clip lands; here, confirm via the clip's source-cycle tooltip or dev console log if 49-05 is not yet landed).
    4. Click `Cancel` after a selection: confirm nothing changed — no clip, no dirty state, engine view resumed without recreation flicker.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step with a screenshot</resume-signal>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

- **BKG-02 / unclassified probe row — unresolved, flagged assumption:** the probe could not classify BKG-02. Manual review: import ordering (D-02), empty/one/many picker states, and cancel-safety are covered by the truths above; cross-window realm behavior is evidenced by the native checkpoint (Task 3) because no unit test can prove WKWebView realm isolation. Flagged for verifier confirmation.
- Research assumptions A2 (custom commands need no capability grant) and A3 (efxasset:// reachable from Studio) are exercised natively at Task 3: if the dialog or thumbnails fail despite code correctness, the fix is a capability/IPC grant addition reviewed at this checkpoint — never a silent fallback.

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/BackgroundAssetPickerView.tsx`, `app/src/components/physic-paint/view/BackgroundAssetPickerView.test.ts`
- New exported symbols: `BackgroundAssetPickerView` (component); `requestImageLibrary` (bridge consumer port); bridge event constants `physic-paint:image-library-request` / `physic-paint:image-library-result`
- Modified: `physics-paint.json` (+1 permission), `PhysicsPaintStudio.tsx` (swap state + picker mount)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main webview ↔ Studio webview (bridge events) | cross-window payload crossing JS realms; spoofed/malformed events must fail closed |
| Studio webview → native dialog/fs surface | capability-gated plugin APIs crossing into native code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-04-01 | Spoofing | image-library request/result pair | high | mitigate | operationId correlation + payload validation at the bridge boundary (existing posture); unknown/mismatched events rejected without consumer visibility (ASVS V5) |
| T-49-04-02 | Elevation of Privilege | physics-paint.json capability delta | high | mitigate | exactly one permission added (`dialog:allow-open`); structural test asserts no fs:* grant; efxasset:// allowed-roots + extension allow-list (`lib.rs:375-446`) unchanged (ASVS V4/V12) |
| T-49-04-03 | Tampering | importFiles path input from dialog | medium | mitigate | paths come only from the native dialog's return value (no user-typed path field exists); processing stays in `image_pool.rs` with `is_supported_format` gating |
| T-49-04-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint` green (picker + bridge + studio suites)
- `pnpm --dir app run typecheck` green
- Native checkpoint approved (dialog opens, library populates, cancel leaves no trace)
</verification>

<success_criteria>
The Studio realm can see the project library and can legally open the native import dialog; the picker confirms natural-sorted selections and cancels without side effects. 49-05 can wire the Bg row's Import control against working seams.
</success_criteria>

<output>
Create `.planning/phases/49-fixed-background-track-and-imported-loop-clips/49-04-SUMMARY.md` when done, recording the checkpoint outcome and the final bridge payload shape.
</output>
