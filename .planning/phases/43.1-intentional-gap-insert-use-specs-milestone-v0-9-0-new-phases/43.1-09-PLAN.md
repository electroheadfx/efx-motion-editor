---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 09
type: tdd
wave: 8
depends_on:
  - 43.1-08
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts
  - app/src/stores/physicPaintStore.rotoLoopClips.test.ts
  - app/src/lib/physicPaintBridge.test.ts
autonomous: true
gap_closure: true
requirements:
  - GAP-03
  - GAP-06
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GAP-03 / D-01: the exact typed intent resolved by the child is carried unchanged through action dispatch, drag/spacing preparation, coordinator execution, and the parent apply payload."
    - "GAP-03 / D-14: identity-preserving move, group move, and Force Spacing requests retain the exact stable-key authorization needed for parent recomputation."
    - "GAP-06 / D-21: existing Insert, Duplicate/Paste, delete, drag, group, spacing, Loop Clip, selection, and intentional-gap settlement behavior remains unchanged apart from stricter transport."
    - "D-18: no child/UI state is published before the parent accepts the complete intent-backed document tuple."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Required discriminated ordinary intent member on the active physical apply payload and strict guard"
      contains: "PhysicPaintRotoPhysicalEditApplyPayload"
    - path: "app/src/types/physicPaint.test.ts"
      provides: "Required-member, operation-kind coherence, and specialized-path parser fixtures"
    - path: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      provides: "Exact intent preservation from every ordinary timeline action"
    - path: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      provides: "Coordinator execution contract requiring the typed ordinary intent"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Intent-bearing complete physical apply payload"
    - path: "app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts"
      provides: "Action-to-executor intent matrix"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts"
      provides: "Coordinator-to-payload exact intent and rollback tests"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts"
      provides: "Ordinary move-key runtime-guard fixture migrated with exact move intent"
    - path: "app/src/stores/physicPaintStore.rotoLoopClips.test.ts"
      provides: "Ordinary bridge payload helper migrated with exact per-operation intent while replay variants remain specialized"
    - path: "app/src/lib/physicPaintBridge.test.ts"
      provides: "Existing ordinary apply-payload fixtures remain valid with exact intent before Plan 10 adds parent recomputation assertions"
  key_links:
    - from: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      to: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      via: "runPhysicalAction and prepared drag/spacing execution retain the resolver input intent"
      pattern: "intent"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "PhysicPaintRotoPhysicalEditApplyPayload"
      via: "the same intent is emitted alongside complete staged records, Loop Clips, breaks, interpolation, and selection"
      pattern: "operationKind|intent"
---

<objective>
Atomically activate Plan 08's strict ordinary physical-edit intent as a required discriminated apply-payload member and update every producer, action, coordinator, parser fixture, and typed harness in the same buildable plan.

Purpose: Remove the transport gap that currently drops resolver intent after child-side resolution, with no optional or backward-compatible intermediate state, while preserving D-01 exact settlement and Preact-native state boundaries.
Output: Required payload/guard discrimination, intent-aware action/coordinator contracts, complete fixture migration, and focused plus typecheck regression evidence.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-03-SUMMARY.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-04-SUMMARY.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-08-SUMMARY.md
@app/src/types/physicPaint.ts
@app/src/types/physicPaint.test.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
@app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts
@app/src/stores/physicPaintStore.rotoLoopClips.test.ts
@app/src/lib/physicPaintBridge.ts
@app/src/lib/physicPaintBridge.test.ts

<interfaces>
Plan 08 supplies the standalone shared `PhysicPaintRotoPhysicalEditIntent`, strict parser, and canonical serializer while intentionally leaving the active payload unchanged. This plan is the sole activation boundary: ordinary mapping payload variants require intent, specialized Play Script/interpolation/replay variants remain explicitly discriminated, and no optional compatibility member is permitted. `runPhysicalAction` already receives `runnerInput.intent`, calls `resolvePhysicPaintRotoPhysicalEdit`, and invokes `executePhysicalEdit`. The coordinator builds `PhysicPaintRotoPhysicalEditApplyPayload` only after launch/revision revalidation and stages records, interpolation, Loop Clips, incoming breaks, selection, and optional semantic delta. The exact-kind inventory is `physicPaintBridge.ts` (production consumer whose recomputation enforcement remains Plan 10), `useRotoPhysicalEditCoordinator.ts`, `useRotoPhysicalEditCoordinator.test.ts`, `physicsPaintRotoLoopClips.test.ts`, `physicPaintStore.rotoLoopClips.test.ts`, `physicPaint.ts`, `physicPaint.test.ts`, and `physicPaintBridge.test.ts`; this plan migrates every ordinary fixture/producer in that inventory while preserving specialized variants.
</interfaces>
</context>

## Artifacts this phase produces

- Intent-preserving ordinary timeline action dispatch, including prepared drag and Force Spacing paths.
- An intent-required coordinator execution port for non-replay mapping operations.
- Complete parent payload construction carrying the exact child-resolved request without early UI publication.
- Atomic migration of all current ordinary `replace-roto-physical-map` fixtures in the exact-kind inventory, including the Loop Clip runtime guard, store bridge helper, and bridge apply fixtures, while specialized Play Script/interpolation/Undo/Redo variants stay correctly discriminated.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Activate one required Insert Slot intent from action through parent payload</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts: active apply payload union/guard plus Plan 08 shared intent/parser
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts: `runPhysicalAction`, occupied Insert dispatch, prepared drag state, and Force Spacing execution
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts: existing contextual Insert and action execution assertions
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts: `RotoPhysicalEditExecuteInput`
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts: payload construction after launch/revision revalidation
  </read_first>
  <behavior>
    - RED: occupied Insert resolves an `insert-slot` request but the test fails until `executePhysicalEdit` receives the exact same intent and the coordinator emits it on the required ordinary payload variant.
    - GREEN: ordinary mapping payloads require intent; occupied Insert carries the exact parsed intent together with the accepted proposal and matching operation kind.
    - Specialized Play Script, interpolation, Undo, and Redo payload variants remain explicit and do not acquire an ordinary intent member.
    - Missing intent, unknown fields, and intent-kind/operation-kind mismatch reject before coordinator publication.
  </behavior>
  <action>Activate the shared Plan 08 intent in the active payload as a required member only on ordinary mapping variants, with exact `operationKind`/`intent.kind` discrimination in `isPhysicPaintRotoPhysicalEditApplyPayload`; no optional field or compatibility branch is allowed. Extend `RotoPhysicalEditExecuteInput` with the required ordinary intent, pass `runnerInput.intent` through `runPhysicalAction`, and emit that exact value from the coordinator for the occupied Insert tracer. Preserve the existing resolver call, proposal, selection, expected launch, and target-reason behavior. Do not derive intent back from the proposal and do not introduce component state, `useEffect`, or a mirrored Signal; the action event already owns the exact request. Keep the existing Insert surface and its empty-segment dispatch unchanged per D-21.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Occupied Insert sends one matching `insert-slot` intent through the existing executor port.
    - Existing disabled/preflight/rejection reason mapping is unchanged.
    - No new action, hook state, effect, or UI surface is added.
  </acceptance_criteria>
  <done>One production mapping action retains its canonical request from child resolution to coordinator entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry exact intent through every ordinary action and complete coordinator payload</name>
  <files>app/src/types/physicPaint.test.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts, app/src/stores/physicPaintStore.rotoLoopClips.test.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.test.ts: all public payload fixtures and required-member/discriminant rejection cases
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts: all calls to `executePhysicalEdit`, drag preparation/commit, and Force Spacing branches
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts: complete ordinary action-to-intent matrix
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts: execute callback, staged proposal validation, payload construction, deferred publication, and rollback
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts: every ordinary fixture plus specialized/replay discrimination
    - app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts: ordinary `move-key` runtime-guard `replace-roto-physical-map` fixture
    - app/src/stores/physicPaintStore.rotoLoopClips.test.ts: `bridgePayload` ordinary helper plus specialized Undo/Redo overrides
    - app/src/lib/physicPaintBridge.test.ts: all ordinary apply-payload fixtures plus specialized Play Script/interpolation fixtures that must retain their distinct contracts
    - app/src/lib/physicPaintBridge.ts: production exact-kind consumer inventory boundary; Plan 10 adds canonical parent recomputation rather than this activation plan changing enforcement
  </read_first>
  <behavior>
    - Delete one/group, move one/group, Force Spacing, Duplicate, Paste one/group, Insert Slot, and Insert Empty Segment carry the exact intent used to produce their proposal.
    - Prepared drag commits retain the original stable-key IDs and target frame rather than rebuilding authorization from current UI state.
    - The coordinator emits the intent unchanged with complete records, selection, Loop Clips, incoming breaks, interpolation, and semantic delta where applicable.
    - Parent rejection, settlement mismatch, or transport failure restores the complete before snapshot and does not publish selection/history.
  </behavior>
  <action>Complete the atomic activation across every current ordinary producer and fixture in the exact-kind inventory. Store the original typed intent alongside prepared single/group drag proposals, retain exact stable-key authorization through later selection/cursor changes, pass it through rail-owned Force Spacing, and include it unchanged in the coordinator payload after launch/revision revalidation. Update `physicPaint.test.ts`, every ordinary coordinator/action harness, the ordinary `move-key` runtime-guard fixture in `physicsPaintRotoLoopClips.test.ts`, the ordinary `bridgePayload` helper and its non-replay calls in `physicPaintStore.rotoLoopClips.test.ts`, and every ordinary apply payload in `physicPaintBridge.test.ts` with the exact intent matching its `operationKind`. Preserve specialized Play Script, interpolation-settings, Undo, Redo, and provenance/replay variants as separately discriminated contracts; do not attach ordinary intent where their union branch does not require it. Inspect `physicPaintBridge.ts` as the production consumer inventory boundary but leave Plan 10’s canonical recomputation enforcement to Plan 10. Use the required discriminated member directly rather than an optional compatibility value. Add table-driven parser, action, coordinator, runtime-guard, store-helper, and bridge-fixture assertions for exact deep equality and mismatch rejection. Preserve the persistent Signal/coordinator authority established in prior plans; do not create render-local Signal instances or lifecycle effects to shuttle command data. Enforce D-01/D-18 by retaining deferred publication and complete rollback.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts src/stores/physicPaintStore.rotoLoopClips.test.ts src/lib/physicPaintBridge.test.ts &amp;&amp; pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every ordinary mapping payload contains the exact strictly typed request that generated its proposal.
    - The complete exact-kind inventory is accounted for: production consumer inspected for Plan 10 handoff; coordinator/types producers migrated; ordinary fixtures migrated in coordinator, Loop Clip runtime-guard, store bridge-helper, type, and bridge suites.
    - The `physicsPaintRotoLoopClips.test.ts` ordinary `move-key` fixture carries exact move intent, and all ordinary `physicPaintStore.rotoLoopClips.test.ts` / `physicPaintBridge.test.ts` payloads carry operation-matched intent.
    - Specialized Play Script, interpolation, Undo, Redo, and replay/provenance variants remain correctly discriminated without ordinary-intent leakage.
    - Drag and spacing retain the original ordered authorization despite later render/state changes.
    - Existing intentional-gap, Duplicate/Paste, Loop Clip spacing, selection, rollback, and deferred-settlement tests pass.
  </acceptance_criteria>
  <done>The child-to-coordinator transport gap is closed for every GAP-03-B mapping operation while GAP-06 adjacent workflows remain behaviorally stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI action state → coordinator command | Render/event state becomes a stable command request. |
| Coordinator staged snapshot → parent payload | Child-owned proposal and exact intent cross into the parent authority process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.1-09-01 | Tampering | Action-to-coordinator intent | high | mitigate | Carry the exact resolver input; never reconstruct authorization from mutable selection or proposal output. |
| T-43.1-09-02 | Elevation of Privilege | Prepared group drag and Force Spacing | high | mitigate | Preserve exact ordered stable-key authorization and pair it with matching operation kind. |
| T-43.1-09-03 | Repudiation | Deferred command settlement | medium | mitigate | Retain operation ID, expected launch, exact intent, and complete accepted tuple before publication/history. |
| T-43.1-09-04 | Denial of Service | Stale render or launch state | medium | mitigate | Reuse coordinator launch/revision revalidation and full rollback; avoid new reactive synchronization paths. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts src/stores/physicPaintStore.rotoLoopClips.test.ts src/lib/physicPaintBridge.test.ts`
- `pnpm --dir app run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- The exact canonical intent reaches the parent payload for every existing ordinary mapping operation.
- No action derives authorization from a proposal result or stale render-local state.
- Parent acceptance remains the sole trigger for UI/history publication.
- Existing user-facing Insert, drag, spacing, Copy/Paste, Loop Clip, selection, and intentional-gap behavior is unchanged.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Empty key plus break remain one parent-accepted command | 43.1-09 | COVERED | Exact empty-segment intent travels with the complete staged document. |
| REQ | GAP-03 | Bridge validation and timing edits preserve break authority | 43.1-09 | COVERED | Closes the dropped-intent transport seam. |
| REQ | GAP-06 | Adjacent physical-edit workflows remain unchanged | 43.1-09 | COVERED | Existing action/coordinator suites receive regression assertions. |
| RESEARCH | — | Persistent Signals and sole acknowledged coordinator | 43.1-09 | COVERED | Event-owned data, no effect/state mirroring. |
| CONTEXT | D-01 | Atomic key/break command | 43.1-09 | COVERED | One intent and one complete payload. |
| CONTEXT | D-14 | Timing edits preserve stable-key ownership | 43.1-09 | COVERED | Exact IDs and targets are retained. |
| CONTEXT | D-18 | Failure leaves complete state unchanged | 43.1-09 | COVERED | Existing rollback and deferred publication are retained. |
| CONTEXT | D-21 | Preserve adjacent workflows | 43.1-09 | COVERED | Focused regression matrix covers existing actions. |

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-09-SUMMARY.md` when done.
</output>
