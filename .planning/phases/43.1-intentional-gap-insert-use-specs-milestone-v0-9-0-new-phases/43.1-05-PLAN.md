---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 05
type: execute
wave: 5
depends_on:
  - 43.1-04
files_modified:
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - GAP-05
  - GAP-06
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-05/D-06, a compact integrated cut appears immediately before a break-bearing real key only when that key currently has a predecessor, independent of Interpolation On/Off."
    - "Per D-07, `Starts a new interpolation segment` is appended to the existing cell tooltip and accessibility copy, with no independent target or tooltip lifecycle."
    - "Per D-08/D-09, the toolbar retains one visible `Insert` label and renders the contextual empty-frame enabled description supplied by Plan 04."
    - "The cut adds zero row/cell geometry and remains visually distinct from Phase 43's white 3px Loop Rail endpoint cuts across supported cell states."
  artifacts:
    - path: "app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts"
      provides: "Predecessor-aware `startsInterpolationSegment` cell projection and composed descriptive copy"
      contains: "startsInterpolationSegment"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Existing-cell decorative marker and contextual Insert tooltip wiring"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Compact non-white in-cell cut with no geometry change"
      contains: "starts-interpolation-segment"
  key_links:
    - from: "accepted physical document incomingInterpolationBreakKeyIds"
      to: "physicsPaintWorkflowPresentation.ts"
      via: "ordered real-key predecessor projection derives `startsInterpolationSegment`"
      pattern: "startsInterpolationSegment"
    - from: "physicsPaintWorkflowPresentation.ts"
      to: "PhysicsPaintWorkflowStrip.tsx"
      via: "existing cell button receives descriptive copy and one decorative state class/data attribute"
      pattern: "starts-interpolation-segment"
    - from: "useRotoTimelineActions insertTooltipDescription"
      to: "existing guarded Insert tooltip"
      via: "enabled description remains contextual while disabled reason remains verbatim"
      pattern: "insertTooltipDescription"
  prohibitions:
    - "Do not add a row, lane, track height, badge, separate marker element with focus/pointer handlers, native title, or independent tooltip."
    - "Do not use the canonical white Loop Rail endpoint-cut treatment or modify Loop Rail hit targets/interactions."
    - "Do not derive marker visibility from pending action state or from global interpolation enabled state."
---

<objective>
Project the accepted break into the existing physical real-key cell as descriptive, zero-geometry visual state.

Purpose: Make local segment boundaries understandable and accessible without adding an interaction surface or confusing Phase 43's Loop Rail ownership, per D-05..D-09.
Output: Pure predecessor-aware view-model state, exact tooltip/ARIA fact, contextual Insert description wiring, and compact in-cell CSS.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-04-SUMMARY.md
@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintStudio.css

<interfaces>
Add `startsInterpolationSegment: boolean` to the existing physical real-key cell view model. Add exported exact copy constant `ROTO_STARTS_INTERPOLATION_SEGMENT_COPY = 'Starts a new interpolation segment'`. Use class `starts-interpolation-segment` on the existing button when true. The marker may use a pseudo-element or one `aria-hidden` decorative child only after checking existing pseudo-element collisions; it may not be focusable, clickable, or a tooltip anchor.
</interfaces>
</context>

<planning_note>spec-less probe fallback skipped: phase had no requirement IDs at plan-phase init; goal-backward must_haves were derived from the activated SPECS prompt, CONTEXT.md, RESEARCH.md, and canonicalized GAP-01..GAP-06 instead.</planning_note>

<assumptions>
- Use a compact 2px-wide non-white accent derived from existing interpolation color tokens; exact token/layer is chosen after inspecting pseudo-element occupancy.
- A decorative child inside the existing cell button is permitted only if `::before` and `::after` are both already semantically occupied in conflicting states.
- Native visual UAT in Plan 06 is the final oracle for theme/state layering and Loop Rail distinction.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive predecessor-aware marker state and exact descriptive copy</name>
  <files>app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — cell semantic view models, tooltip/accessibility projection, and exact copy constants
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts — pure tooltip/view-model test style
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — accepted owner collection contract
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — ordered real-key/projection shapes used to determine predecessor existence
  </read_first>
  <behavior>
    1. RED→GREEN: a break-bearing real key with an ordered predecessor exposes `startsInterpolationSegment: true` and appends exactly `Starts a new interpolation segment` to existing tooltip/accessibility copy.
    2. RED→GREEN: the same owner while first is false with no appended fact; introducing a predecessor makes it true without changing ownership.
    3. RED→GREEN: toggling global interpolation leaves marker state and composed copy unchanged.
    4. RED→GREEN: non-real, generated, linked, and empty cells never receive the segment-start fact.
  </behavior>
  <action>Execute the numbered presentation behaviors as vertical public-interface micro-cycles. Add and commit the predecessor-visible projection test failing, then minimally extend the existing real-key view-model interface with `startsInterpolationSegment` and exact composed copy for GREEN. Add dormant-first, interpolation-toggle independence, and non-real exclusion one at a time; after each RED, make only the required pure projection change and rerun unchanged. Derive solely from accepted owner IDs plus ordered predecessor position, export the exact copy constant, compose it once in the existing tooltip/accessibility path, preserve base copy order, and add no UI-local state. Refactor only while all tests are green, then run the final focused command.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts -t "projects a predecessor-owned interpolation segment start into existing cell copy"</automated>
  </verify>
  <acceptance_criteria>
    - The exact fact is present once for a visible predecessor case and absent for first-owner/non-real cases.
    - Interpolation enabled state is not an input to marker visibility.
    - The existing base tooltip/accessibility copy remains present and ordered.
    - The same focused command is used for RED and GREEN.
  </acceptance_criteria>
  <done>Accepted stable-owner state projects to one predecessor-aware descriptive boolean and exact existing-cell copy.</done>
</task>

<task type="auto">
  <name>Task 2: Render the compact cut and contextual Insert description in existing geometry</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — `RotoTimelineCellButton`, class composition, tooltip host, Insert button, guarded tooltip, and Loop Rail placement
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — source/DOM contracts for one Insert label, no native disabled/title, and one cell target
    - app/src/components/physic-paint/physicsPaintStudio.css — cell dimensions, generated/dirty pseudo-elements, selection/current states, Loop Rail colors and white endpoint cuts
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — Task 1 view-model/copy contract
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — Plan 04 `insertTooltipDescription` and disabled reason signals
  </read_first>
  <action>Pass `startsInterpolationSegment` into the existing cell button and add class `starts-interpolation-segment`; keep the existing button as the sole pointer/focus/tooltip target. Wire the Insert tooltip's enabled description to `insertTooltipDescription`, preserving the visible `Insert` label and current guarded disabled-copy wrapper. Audit pseudo-element use across real/current/selected/dirty/linked states; choose a compact 2px non-white left-edge accent inside the 24px cell with no width/height/padding/border-box change, using one `aria-hidden` decorative child only if pseudo-elements conflict. Add exact source/DOM/CSS contract tests proving one Insert button, one cell button, no independent marker handlers/ARIA selection, and unchanged Loop Rail selectors.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The existing physical cell button remains the only interactive/focusable element for that frame.
    - Class `starts-interpolation-segment` is present only from accepted Task 1 projection.
    - CSS adds no row/cell height, width, padding, margin, or new timeline container.
    - The cut color is not white and does not alter `.physics-paint-loop-rail` endpoint selectors.
    - The visible toolbar text remains exactly `Insert`; the empty valid tooltip description is exact; occupied valid copy is unchanged.
    - Focused presentation/strip tests pass in one-shot mode.
  </acceptance_criteria>
  <done>The durable break is visible and described inside the existing cell and Insert tooltip without new geometry or interaction authority.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new source or test file paths. It introduces:
- View-model property: `startsInterpolationSegment`.
- Copy constant: `ROTO_STARTS_INTERPOLATION_SEGMENT_COPY` with exact value `Starts a new interpolation segment`.
- CSS selector/class: `.starts-interpolation-segment` on the existing physical cell.
- Optional non-interactive `aria-hidden` decorative child only if the CSS collision audit requires it; no new component or target.
- Presentation test cases for predecessor, dormant-first, interpolation-Off, and non-real states.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| accepted physical state → presentation projection | Only canonical owner IDs and current real-key order may drive visible segment boundaries. |
| presentation view model → interactive DOM | Descriptive state must not accidentally create a new action/focus/pointer surface. |
| controller product copy → tooltip | Disabled and enabled descriptions must preserve the one-mapper/one-action contract. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.1-18 | Spoofing | optimistic/stale marker projection | medium | mitigate | Derive marker only from accepted document owner IDs plus ordered predecessor; pure tests exclude pending action state and interpolation toggle. |
| T-43.1-19 | Elevation of Privilege | accidental marker interaction target | medium | mitigate | Existing cell remains sole button/tooltip anchor; source/DOM tests reject handlers, focusability, independent ARIA, and extra target geometry. |
| T-43.1-20 | Information Disclosure | tooltip composition | low | accept | Tooltip contains only the locked product fact and existing cell description; no internal IDs or resolver diagnostics are introduced. |
</threat_model>

<verification>
- Task 1 uses RED → GREEN with the focused pure presentation test.
- Task 2 runs the combined presentation and strip contract suite.
- `pnpm --dir app run typecheck` exits 0.
- `git diff --check` exits 0.
</verification>

<success_criteria>
- GAP-05 is covered with exact predecessor, copy, geometry, and interaction behavior.
- D-05..D-07 are directly testable in source/presentation contracts.
- D-08/D-09 preserve one Insert surface and contextual valid copy.
- Visual approval remains pending until Plan 06 native UAT.
</success_criteria>

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-05-SUMMARY.md` when done.
</output>
