---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 08
subsystem: physics-paint-roto-transport
tags: [typescript, vitest, physical-edit, strict-parser, canonical-serialization]

requires:
  - phase: 43.1-06
    provides: Parent-authoritative Roto physical edit settlement and ordinary resolver operations
provides:
  - Shared transport-safe intent union for every ordinary Roto physical mapping operation
  - Closed runtime parser with ordered identity, uniqueness, numeric-domain, and nested payload validation
  - Canonical stable JSON serialization and parsed-intent resolver reproduction proof
affects: [43.1-09, GAP-03, physics-paint-roto-bridge, physical-edit-coordinator]

actuals:
  tokens: 8607
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - Dependency-safe shared authorization contract consumed by the existing canonical resolver
    - Closed discriminated-union parsing before transport data can authorize mutation
    - Explicit canonical reconstruction before JSON serialization

key-files:
  created: []
  modified:
    - app/src/types/physicPaint.ts
    - app/src/types/physicPaint.test.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts

key-decisions:
  - "The ordinary physical-edit intent union lives in the dependency-safe transport module and is re-exported by the resolver so there is one vocabulary without changing existing caller imports."
  - "Canonical serialization reconstructs each operation-specific object and preserves authoritative array order instead of serializing caller-owned objects directly."
  - "Plan 08 leaves PhysicPaintRotoPhysicalEditApplyPayload and its active guard unchanged; Plan 09 owns the atomic producer/consumer activation."

patterns-established:
  - "Transport authorization is parsed as a closed operation-specific request, then interpreted only by resolvePhysicPaintRotoPhysicalEdit."
  - "Group and linked-spacing authorization arrays are bounded, unique, order-preserving, and checked for internal identity consistency before resolution."

requirements-completed: [GAP-03]

coverage:
  - id: D1
    description: "Every current ordinary physical mapping operation has one shared, strictly parsed transport-safe intent."
    requirement: GAP-03
    verification:
      - kind: unit
        ref: "app/src/types/physicPaint.test.ts#strictly parses and canonically serializes every ordinary physical-edit intent"
        status: pass
      - kind: unit
        ref: "app/src/types/physicPaint.test.ts#rejects malformed, duplicate, reordered, and ambiguous ordinary intent authorization"
        status: pass
    human_judgment: false
  - id: D2
    description: "Serialized and reparsed ordinary intents reproduce direct canonical resolver proposals without mutating ordered authorization inputs."
    requirement: GAP-03
    verification:
      - kind: integration
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts#reproduces every ordinary direct resolver proposal from one parsed canonical intent"
        status: pass
      - kind: integration
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts#keeps ordered authorization immutable and repeated parse-resolve cycles stable"
        status: pass
    human_judgment: false
  - id: D3
    description: "The intent contract remains independently typecheckable without activating or widening the live apply payload."
    requirement: GAP-03
    verification:
      - kind: other
        ref: "pnpm --dir app exec tsc --noEmit"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-08-10
status: complete
---

# Phase 43.1 Plan 08: Transport-Safe Ordinary Physical-Edit Intent Summary

**Closed ordinary Roto edit authorization with canonical JSON serialization and deterministic reuse of the sole physical mapping resolver**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-10T06:41:19Z
- **Completed:** 2026-08-10T06:52:50Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Promoted all ten ordinary physical-edit variants into one dependency-safe shared intent union while preserving resolver-side type imports through re-exports.
- Added strict operation-specific parsing for targets, payloads, group identities, Paste ambiguity, Force Spacing authorization, linked source-cycle order, and bounded collections.
- Added canonical serialization and adversarial tests proving parse/serialize stability, immutable authorization arrays, deterministic empty/single-key behavior, and direct-versus-parsed resolver equality.
- Preserved the Plan 09 cutover boundary: the active apply payload, active payload guard, producers, and consumers remain unchanged.

## Task Commits

Each TDD task was committed atomically:

1. **Task 1 RED: Add failing Insert Slot intent tracer** - `faf0133f` (test)
2. **Task 1 GREEN: Add transport-safe Insert Slot intent** - `d44c36b8` (feat)
3. **Task 2 RED: Add failing ordinary intent matrix** - `6d520292` (test)
4. **Task 2 GREEN: Complete ordinary intent contract** - `754c823b` (feat)

## Files Created/Modified

- `app/src/types/physicPaint.ts` - Owns the shared intent union, strict parser, nested authorization guards, and canonical serializer.
- `app/src/types/physicPaint.test.ts` - Covers every valid ordinary variant and adversarial malformed, duplicate, reordered, oversized, and ambiguous transport shape.
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` - Imports and re-exports the shared intent vocabulary while retaining all canonical mapping semantics.
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` - Proves serialized intents reproduce direct proposals and preserve deterministic resolver behavior without input mutation.

## Decisions Made

- Kept the shared contract in `app/src/types/physicPaint.ts`, which is already the bridge-facing dependency-safe module; the resolver re-exports the types to avoid churn in existing callers.
- Reused `getPhysicsPaintRotoSourceCycleId` for linked Force Spacing authorization rather than duplicating source-cycle identity semantics.
- Treated group ordering as authorization data: serialization preserves order, while parsing rejects duplicates, internally reordered linked selections, and unordered group-paste source frames.
- Deferred all live payload activation to Plan 09 exactly as planned.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected inconsistent generated STATE progress metadata**
- **Found during:** Plan closeout
- **Issue:** `state.update-progress` reported 90% but wrote `progress.percent: 57`, and the last-activity description remained on Plan 07.
- **Fix:** Reconciled the scalar progress and last-activity text with the command result and the completed Plan 08 state.
- **Files modified:** `.planning/STATE.md`
- **Verification:** STATE now reports Plan 9 of 12, 37 of 41 completed plans, and 90% consistently.
- **Committed in:** Final plan metadata commit

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** Closeout metadata only; implementation scope and runtime behavior are unchanged.

## Issues Encountered

- TypeScript initially retained one linked-spacing record property as `unknown` after guard calls. Local aliases made the narrowing explicit; focused tests and the full app typecheck then passed.
- Codebase-memory graph tools were unavailable in this executor runtime after the required skill-first discovery attempt, so focused source reads and symbol searches were used instead.

## TDD Gate Compliance

- RED gate commits: `faf0133f`, `6d520292`
- GREEN gate commits: `d44c36b8`, `754c823b`
- Final verification: 44 focused Vitest tests passed, `tsc --noEmit` passed, and `git diff --check` passed.

## User Setup Required

None - no external service configuration required.

## Self-Check: PASSED

All four modified source/test files, the summary artifact, and all four TDD task commits were verified present.

## Next Phase Readiness

- Plan 09 can atomically attach the strict intent to the active bridge payload and rerun `resolvePhysicPaintRotoPhysicalEdit` in the parent before accepting a proposed complete document.
- No UI, Play Script, interpolation, Undo/Redo, or deferred group-operation scope was changed.

---
*Phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases*
*Completed: 2026-08-10*
