---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 02
type: tdd
wave: 2
depends_on:
  - 43.1-01
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts
autonomous: true
requirements:
  - GAP-01
  - GAP-02
  - GAP-03
  - GAP-06
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02, a break suppresses only the generated strict interior entering its owning right key; interpolation from that owner to a later key remains available."
    - "Per D-03 and D-04, global Interpolation Off never deletes or rewrites break ownership, and a first-position owner is dormant until a predecessor exists."
    - "Per D-11..D-14, fresh Duplicate/Paste identities omit breaks, delete removes the owner's break, destination replacement preserves it, and identity-preserving timing edits carry it automatically."
    - "The explicit empty-segment resolver intent proposes one blank real key and one owner break together or rejects before mutation."
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "Right-key projection exclusion, insert-empty-segment intent, semantic delta, and complete break lifecycle"
      contains: "insert-empty-segment"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts"
      provides: "Canonical 0/3/6/10/13 truth table plus delete/paste/move/push/spacing lifecycle coverage"
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Break-aware projection and structural cache invalidation"
      contains: "incomingInterpolationBreakKeyIds"
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "the store invokes the single shared break-aware physical projection"
      pattern: "buildPhysicPaintRotoPhysicalTimelineProjection"
    - from: "insert-empty-segment intent"
      to: "PhysicPaintRotoPhysicalEditProposal"
      via: "one proposal carries nextRecords, nextIncomingInterpolationBreakKeyIds, and semanticDelta"
      pattern: "nextIncomingInterpolationBreakKeyIds"
  prohibitions:
    - "Do not overload `insert-slot`, infer segment creation from generic paste, or create a second interpolation implementation."
    - "Do not persist or materialize generated interiors, copy the preceding raster, or suppress interpolation on the outgoing side of the owner."
    - "Do not transfer a deleted owner's break or copy a break to a fresh Duplicate/Paste key identity."
---

<objective>
Expand the tracer into the complete resolver semantics for creating and maintaining local interpolation breaks.

Purpose: Make incoming-only interpolation behavior and every existing key-operation lifecycle deterministic before transaction/UI expansion, satisfying D-02..D-04 and D-11..D-14.
Output: A tested `insert-empty-segment` resolver feature, break-aware shared projection, and structural cache participation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-01-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
@app/src/stores/physicPaintStore.ts
@app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts

<interfaces>
Consume Plan 01's `incomingInterpolationBreakKeyIds`. Add operation/intent union member `insert-empty-segment`; proposal field `nextIncomingInterpolationBreakKeyIds: readonly string[] | null`; semantic-delta member `insert-empty-segment` carrying `insertedKeyId` and `destinationAppFrame`. Public projection accepts the complete break collection and skips an adjacent pair only when the right key ID is in that collection.
</interfaces>
</context>

<planning_note>spec-less probe fallback skipped: phase had no requirement IDs at plan-phase init; goal-backward must_haves were derived from the activated SPECS prompt, CONTEXT.md, RESEARCH.md, and canonicalized GAP-01..GAP-06 instead.</planning_note>

<assumptions>
- `insert-empty-segment` and `nextIncomingInterpolationBreakKeyIds` are the locked discretionary names for the resolver contract.
- The existing blank payload is supplied by the caller; the resolver validates and clones it but does not encode a new image.
- Identity-preserving timing operations already produce complete key-ID mappings, so unchanged owner IDs require no frame-rebase side table.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define incoming-only projection and atomic empty-segment resolution</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — intent union, paste-key fresh-key construction, proposal finalizer, fail helpers, and shared generated-cell projection
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — fixture builders and exact mapping/cell/proposal assertions
    - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — `toEmptyKeyPayload`/blank payload shape only; do not modify in this task
    - .planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md — projection and paste analog excerpts
  </read_first>
  <behavior>
    - RED: keys at 0/3/6/10 with owner break `key-10` produce generated cells 1–2 and 4–5 but none at 7–9.
    - RED: adding key 13 produces generated cells 11–12 from owner `key-10` to `key-13`, proving incoming-only suppression per D-02.
    - RED: with Interpolation Off, the projection emits no generated cells while retaining the same break collection; turning it On restores the same incoming exclusion per D-03.
    - RED: a break owner at the first ordered key is accepted and dormant; after a predecessor is introduced, only the new incoming pair is suppressed per D-04.
    - RED: `insert-empty-segment` at a genuinely empty in-capacity frame proposes exactly one supplied blank real key, adds only its new key ID to breaks, leaves interpolation and Loop Clips byte-identical, and emits the typed semantic delta.
    - RED: occupied, generated/render-only, resolved linked, unresolved linked, invalid, and out-of-capacity destinations reject with no proposal.
  </behavior>
  <action>Write the complete truth-table and intent tests first, run the focused command to prove RED, then extend the existing shared projection signature with the complete owner collection and apply the right-key adjacency predicate. Add the explicit `insert-empty-segment` intent, operation kind, proposal field, and semantic-delta member; validate exact destination kind/capacity/occupancy and the blank payload before constructing one new stable-ID real key plus the complete next break collection. Reuse the existing proposal finalizer and unrelated-state invariants. Never route this intent through generic paste or `insert-slot`. Commit RED then GREEN.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts -t "suppresses only the incoming interpolation span owned by the right key|proposes one empty real key and one incoming break atomically|rejects every non-empty-segment target without a proposal"</automated>
  </verify>
  <acceptance_criteria>
    - Frames 7–9 are absent and frames 11–12 are present in the canonical example.
    - Interpolation Off/On changes generated projection only; the break owner collection is unchanged.
    - The first-position owner remains valid and dormant rather than being discarded.
    - Successful `insert-empty-segment` changes only records, breaks, selected key/frame proposal fields, revision inputs, and its semantic delta; Loop Clips/interpolation remain unchanged.
    - The identical focused command fails before implementation and passes after implementation.
  </acceptance_criteria>
  <done>The shared physical projection and resolver express one incoming-only local break and one atomic empty-key-plus-break proposal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve break ownership across existing key operations and cache projection</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts, app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — delete candidate, paste replacement/fresh-key paths, direct/group drag, directional push, partial Key Spacing, and final proposal validation
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — existing operation regression fixtures
    - app/src/stores/physicPaintStore.ts — `RotoPhysicalStructuralCacheEntry`, cache equality predicate, projection/revision creation
    - app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts — projection/revision spy and reference-stability analog
  </read_first>
  <behavior>
    - RED: deleting a break owner removes that ID from breaks and does not transfer it to the successor, per D-12.
    - RED: paste/replace onto the same destination key preserves that key's break, while paste-to-empty and Duplicate-created fresh identities omit source breaks, per D-11/D-13.
    - RED: direct drag, rigid group drag, Push Left, Push Right, and partial Key Spacing preserve owner IDs and apply the break to each owner's newly derived predecessor, per D-14.
    - RED: unrelated Loop Clip records and interpolation settings remain byte-identical for every ordinary operation, per D-21.
    - RED: replacing the immutable break collection invalidates projection/revision exactly once; selection/cursor reads and Interpolation-Off reads do not erase or spuriously replace the collection.
  </behavior>
  <action>Add lifecycle tests before implementation. In resolver candidate/finalization paths, filter every removed key ID from the complete break collection, preserve the destination owner's collection entry on payload replacement, and leave the collection unchanged for fresh Duplicate/Paste and every identity-preserving mapping. Validate that every resulting owner still exists before returning success. Add break collection identity/content to the store structural cache key and projection/revision call. Preserve all Phase 43 Loop Clip inputs and outputs exactly. Commit RED then GREEN; refactor only shared collection helpers that reduce duplication without changing operation behavior.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Delete, replacement, fresh-key creation, drag, group drag, both directional pushes, and partial spacing each have exact break lifecycle assertions.
    - No test expects a break to move by frame number or to transfer to another identity.
    - Store cache tests prove immutable collection replacement triggers one recomputation and unchanged collection identity preserves memoization.
    - Existing Loop Clip and interpolation resolver tests remain green in the same command.
    - Git history contains `test(43.1-02):` before `feat(43.1-02):` for the resolver feature.
  </acceptance_criteria>
  <done>All existing physical key operations participate only in the explicitly locked break lifecycle, and store projection cannot become stale.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new source or test file paths. It introduces:
- Operation kind and resolver intent union member: `insert-empty-segment`.
- Proposal field: `nextIncomingInterpolationBreakKeyIds`.
- Semantic-delta member: `insert-empty-segment` with `insertedKeyId` and `destinationAppFrame`.
- Break-aware shared physical projection signature and right-key adjacency rule.
- Store structural cache participation for `incomingInterpolationBreakKeyIds`.
- Resolver test helpers/cases for the 0/3/6/10/13 truth table and lifecycle matrix; no standalone new helper file.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller target/payload → physical resolver | Current frame classification and blank payload may be stale, invalid, linked, generated, occupied, or out of capacity. |
| resolver candidate → finalized proposal | Proposed records, owner breaks, semantic delta, interpolation, and Loop Clips must remain complete and internally consistent. |
| canonical state → structural cache | Immutable break state controls projection/revision cache validity. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.1-05 | Tampering | `insert-empty-segment` resolver branch | high | mitigate | Validate target kind, occupancy, capacity, payload, stable ID uniqueness, owner referential integrity, and unrelated-state invariants before returning a proposal. |
| T-43.1-06 | Tampering | semantic delta/finalizer | high | mitigate | Recompute expected changed fields and reject incomplete mappings, orphan owners, or unrelated interpolation/Loop Clip changes. |
| T-43.1-07 | Denial of Service / Integrity | store structural cache | medium | mitigate | Include immutable break collection identity in cache equality and add exact recomputation/reference-stability tests. |
| T-43.1-08 | Tampering | existing operation lifecycle | high | mitigate | Exhaustive delete/paste/move/push/spacing tests ensure ownership cannot orphan, transfer, or copy implicitly. |
</threat_model>

<verification>
- Each task follows RED → GREEN with byte-identical focused commands.
- `pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts` exits 0.
- `pnpm --dir app run typecheck` exits 0.
- `git diff --check` exits 0.
</verification>

<success_criteria>
- GAP-02 is fully represented by the incoming-right-key projection truth table.
- D-03/D-04 durability and dormancy are independent of global interpolation rendering.
- D-11..D-14 lifecycle semantics are exact and regression-tested.
- Every invalid target fails closed before a proposal exists.
</success_criteria>

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-02-SUMMARY.md` when done.
</output>
