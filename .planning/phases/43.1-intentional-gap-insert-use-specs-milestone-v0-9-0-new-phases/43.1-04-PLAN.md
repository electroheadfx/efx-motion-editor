---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 04
type: tdd
wave: 4
depends_on:
  - 43.1-03
files_modified:
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
autonomous: true
requirements:
  - GAP-01
  - GAP-04
  - GAP-06
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-08/D-19, one visible Insert action preserves occupied selected-key `insert-slot` behavior and context-dispatches a genuinely empty cursor to `insert-empty-segment`."
    - "Per D-15..D-17, one typed target classifier and one mapper own both disabled tooltip reasons and resolver-time rejection status copy."
    - "Per D-09/D-10, the empty valid context uses the exact enabled tooltip and exact accepted status, keeps cursor at N, selects the new key, and shows an empty Paint canvas immediately."
    - "Per D-18/D-20, every invalid or racing target fails without records, breaks, Loop Clips, selection, cursor, canvas, or history mutation."
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      provides: "Signals-derived InsertTarget classifier, shared product-reason mapper, contextual description, and dispatch"
      contains: "classifyRotoInsertTarget"
    - path: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      provides: "Reusable blank payload path exposed to the Insert action without copying rendered pixels"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Immediate accepted current-frame blank-canvas reconciliation when required by the focused test"
  key_links:
    - from: "classifyRotoInsertTarget"
      to: "computed Insert eligibility/description/reason"
      via: "Preact Signals computed derivation without effect mirroring"
      pattern: "computed"
    - from: "mapRotoInsertProductReason"
      to: "disabled tooltip and rejectedCopy"
      via: "the same exact string mapper is called at preflight and resolver rejection"
      pattern: "mapRotoInsertProductReason"
    - from: "insertRotoFrame"
      to: "runPhysicalAction"
      via: "occupied-real dispatches existing insert-slot; genuinely-empty dispatches insert-empty-segment with blank payload"
      pattern: "insert-empty-segment"
  prohibitions:
    - "Do not add a second toolbar action, rename the visible Insert label, mirror computed state through `useState`/`useEffect`, or add persistent helper UI."
    - "Do not expose raw resolver/bridge codes, show a modal/alert, navigate to another frame, choose a nearest empty frame, or paint an optimistic marker."
    - "Do not alter existing occupied-key Insert tooltip/status behavior or copy the preceding rendered frame into the new key."
---

<objective>
Expose the completed authority path through the existing Insert affordance with exact context, reason, and accepted feedback semantics.

Purpose: Deliver the user-facing empty-frame insertion behavior without creating a second action or a second UI authority, while preserving occupied Insert exactly per D-08..D-10 and D-15..D-20.
Output: Signals-derived context dispatch, one product-reason mapper, exact accepted status, and immediate empty-canvas reconciliation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-03-SUMMARY.md
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts

<interfaces>
Create union `RotoInsertTarget` with variants `occupied-real`, `genuinely-empty`, `generated`, `resolved-linked`, `unresolved-linked`, `occupied-empty-intent`, `invalid-destination`, `out-of-capacity`, and `edit-in-flight`. Export pure functions `classifyRotoInsertTarget` and `mapRotoInsertProductReason`. Expose computed `insertTooltipDescription` alongside existing eligibility/reason signals.

Lock the mapper table:
- generated → `Insert is unavailable on a generated render-only frame.`
- resolved linked → `Insert is unavailable on a resolved linked frame.`
- unresolved linked → `Insert is unavailable on an unresolved linked frame.`
- occupied for the empty intent → `This frame already contains a real key.`
- invalid destination → `Choose a valid timeline frame before inserting.`
- out of capacity → `This frame is outside the physical timeline capacity.`
- edit in flight → `A Roto physical edit is already in flight.`

Locked exact valid/accepted strings remain:
- `Insert an empty key and start a new interpolation segment.`
- `Inserted empty key at frame {N}. New interpolation segment started.`
</interfaces>
</context>

<planning_note>spec-less probe fallback skipped: phase had no requirement IDs at plan-phase init; goal-backward must_haves were derived from the activated SPECS prompt, CONTEXT.md, RESEARCH.md, and canonicalized GAP-01..GAP-06 instead.</planning_note>

<assumptions>
- The exact invalid-reason sentences above are selected under Claude's Discretion and become byte-for-byte test expectations.
- The existing blank-frame builder produces the accepted transparent payload; only its routing/export may change.
- The coordinator's existing settlement may already reconcile the current canvas; Task 2 changes production code only if the RED test proves a missing branch.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context-dispatch one Insert action through one typed reason vocabulary</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — `computeInsertAvailability`, computed signals, `runPhysicalAction`, `insertRotoFrame`, rejection copy, and current occupied Insert constants
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — existing Insert/+Key availability, dispatch, and status fixtures
    - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — blank frame construction and `toEmptyKeyPayload`
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — Plan 02 intent/failure contract
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — existing guarded tooltip consumer; read only in this task
  </read_first>
  <behavior>
    - RED: occupied selected real key remains eligible and dispatches the existing `insert-slot` intent with existing success/tooltip copy unchanged, per D-19.
    - RED: genuinely empty current cursor becomes eligible and dispatches `insert-empty-segment` with a newly generated stable key ID and `toEmptyKeyPayload` output, never a rendered predecessor payload.
    - RED: empty context exposes exactly `Insert an empty key and start a new interpolation segment.` and accepted settlement publishes exactly `Inserted empty key at frame N. New interpolation segment started.`
    - RED: every invalid variant maps to the locked table above; computed preflight reason and resolver-time `rejectedCopy` are byte-identical.
    - RED: `edit-in-flight` is disabled and publishes the same mapped sentence if activation races.
  </behavior>
  <action>Write the target truth table and dispatch tests first. Implement `RotoInsertTarget`, `classifyRotoInsertTarget`, and `mapRotoInsertProductReason` as pure functions consumed by Preact `computed()` values for eligibility, contextual description, and disabled reason; do not mirror through effects. Keep the occupied-real branch byte-equivalent to current `insert-slot`. For genuinely empty input, obtain the accepted blank payload through the existing key-utility builder, generate one stable key ID, and call `runPhysicalAction` with `insert-empty-segment`, the locked success template, and the shared mapper for rejection. Invalid variants must return false before coordinator execution. Commit RED then GREEN.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts -t "context-dispatches occupied and genuinely empty Insert targets|uses identical target-specific copy for disabled and racing rejection|publishes the exact empty-segment acceptance message"</automated>
  </verify>
  <acceptance_criteria>
    - Existing occupied Insert intent, description, and success assertion remain unchanged.
    - Genuinely empty Insert payload contains the blank data URL and target appFrame, not prior rendered content.
    - Every D-16 category has one exact mapper assertion used at both feedback surfaces.
    - No new effect, local mirrored state, action button, or toolbar label is introduced.
    - The focused command fails in RED and passes unchanged after GREEN.
  </acceptance_criteria>
  <done>The single existing Insert action context-dispatches both valid behaviors and owns one exact target-specific reason vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile the accepted empty key at the retained cursor with no optimistic state</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts — accepted settlement, current-frame reference/canvas reload, selection/cursor publication, rollback, and operation-specific reconciliation
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts — accepted current-frame reconciliation and rejection snapshot fixtures
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — action-level accepted and rejected status/selection/cursor ports
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md — D-55 accepted Play Script reconciliation analog; preserve its behavior
  </read_first>
  <behavior>
    - RED: after exact parent acceptance, selectedKeyId is the new key, currentAppFrame remains N, the current reference/canvas reload port receives the accepted blank payload for N, and success status publishes once.
    - RED: before acknowledgement, selection, cursor, canvas/reference, status success, and marker-observable document state remain at the prior accepted snapshot.
    - RED: parent rejection or settlement mismatch preserves every prior surface and publishes only the mapped failure sentence; no success status or history entry appears.
    - RED: occupied `insert-slot`, play-script reconciliation, Undo, and Redo behavior remains unchanged.
  </behavior>
  <action>Add the action/coordinator acceptance and rejection tests first. Trace current settlement before editing; if ordinary accepted operations already reload the current accepted frame correctly, retain production code and close with characterization tests. If `insert-empty-segment` is excluded by an operation-kind condition, add only that operation to the existing accepted reconciliation funnel so the coordinator reloads the accepted blank frame after exact acknowledgement. Keep all pre-acceptance UI on the old snapshot and preserve D-18 rollback. Commit RED then GREEN only when production behavior was absent; if the RED test is unexpectedly green, stop and document the existing seam rather than adding duplicate logic.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Accepted action retains frame N, selects the new key, and reconciles the blank canvas without navigation.
    - Rejected action preserves records, breaks, Loop Clips, selection, cursor, canvas/reference, and history.
    - Success copy appears exactly once and only after exact acceptance.
    - Existing occupied Insert and Play Script reconciliation tests remain green.
    - Unexpected-green RED behavior is investigated rather than patched redundantly.
  </acceptance_criteria>
  <done>Accepted empty-frame Insert immediately presents the new blank key at the retained cursor, while failure remains entirely non-optimistic.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new source or test file paths. It introduces:
- Union type: `RotoInsertTarget` with occupied-real, genuinely-empty, generated, resolved-linked, unresolved-linked, occupied-empty-intent, invalid-destination, out-of-capacity, and edit-in-flight variants.
- Functions: `classifyRotoInsertTarget`, `mapRotoInsertProductReason`.
- Computed view-model/controller property: `insertTooltipDescription`.
- Exact product-reason table listed in `<interfaces>`.
- Action dispatch for `insert-empty-segment` using the existing blank payload builder.
- Coordinator operation-kind reconciliation only if the focused RED test proves it is required.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| current Signals state → action eligibility | Cursor/frame resolution and pending operation state may change between hover and activation. |
| action activation → resolver/coordinator | Preflight can become stale; resolver and parent remain authoritative. |
| internal failure → user-visible feedback | Raw resolver/bridge diagnostics must not leak through tooltip/status copy. |
| accepted acknowledgement → visible canvas/selection/status | UI must not claim acceptance before exact settlement. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.1-14 | Tampering | stale Insert activation | high | mitigate | Reclassify at activation, invoke fail-closed resolver, preserve expected revision, and assert total no-mutation on race rejection. |
| T-43.1-15 | Information Disclosure | failure feedback | medium | mitigate | One typed mapper supplies product sentences; raw resolver/bridge text remains internal and is never interpolated into UI copy. |
| T-43.1-16 | Spoofing / Repudiation | optimistic accepted UI | high | mitigate | Selection, cursor, blank canvas, marker-observable document, history, and success status update only after exact parent acknowledgement. |
| T-43.1-17 | Tampering | blank payload routing | medium | mitigate | Reuse the existing transparent payload builder and assert no predecessor rendered payload is copied. |
</threat_model>

<verification>
- Both tasks use focused one-shot Vitest RED/GREEN cycles.
- Run `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` after Task 2.
- `pnpm --dir app run typecheck` exits 0.
- `git diff --check` exits 0.
</verification>

<success_criteria>
- GAP-04 has one context-dispatched Insert action and one shared reason mapper.
- D-09/D-10 exact strings are tested byte-for-byte.
- D-15..D-18 race and no-mutation behavior is tested through action and coordinator boundaries.
- The implementation uses existing Preact Signals/computed patterns without effect mirroring.
</success_criteria>

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-04-SUMMARY.md` when done.
</output>
