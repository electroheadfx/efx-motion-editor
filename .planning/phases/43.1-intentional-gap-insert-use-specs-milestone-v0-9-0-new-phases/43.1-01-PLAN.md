---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
autonomous: false
requirements:
  - GAP-01
  - GAP-03
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01, one accepted parent-authoritative physical document can carry a stable-key-owned incoming interpolation break together with its real key as one canonical fact."
    - "Malformed, duplicate, or orphan break owners are rejected before store mutation, and a stale revision cannot install the key or break."
    - "The runtime store exposes an immutable complete break collection and replaces it only through complete-document acceptance."
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      provides: "Canonical incomingInterpolationBreakKeyIds field, empty constant, strict parser, and revision participation"
      contains: "incomingInterpolationBreakKeyIds"
    - path: "app/src/types/physicPaint.ts"
      provides: "Closed transport/result contract for complete incoming break state"
      contains: "incomingInterpolationBreakKeyIds"
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Parent-authoritative strict parse, stale-revision check, and atomic complete-document install"
      contains: "incomingInterpolationBreakKeyIds"
    - path: "app/src/lib/physicPaintBridge.test.ts"
      provides: "RED-to-GREEN tracer proving one valid break-bearing document path and fail-closed invalid inputs"
  key_links:
    - from: "app/src/types/physicPaint.ts"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "replace-roto-physical-map payload allowlist carries the complete break collection"
      pattern: "incomingInterpolationBreakKeyIds"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "one parsed PhysicPaintRotoPhysicalDocument replacement after revision and owner validation"
      pattern: "replaceRotoPhysicalDocument"
  prohibitions:
    - "Do not persist a frame-number-owned boundary, predecessor ID, generated frame, copied raster, or UI-only marker state."
    - "Do not create a second transport branch, interpolation engine, command, package dependency, or legacy migration shim."
    - "Do not mutate the break collection in place or publish any accepted state before parent acknowledgement."
---

<objective>
Establish the production stable-identity break contract through one parent-authoritative acceptance tracer before any resolver, action, or visual expansion.

Purpose: Prove the hardest architectural constraint first: a real key and its incoming break can cross the existing strict transport boundary, survive canonical parsing/revision, and install atomically without splitting document authority, per D-01 and GAP-01/GAP-03.
Output: One immutable canonical break field and one verified payload → bridge → store acceptance path with fail-closed malformed and stale cases.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md
@SPECS/milestone-v0.9.0-new-phases/phase-43.1-intentional-gap-insert-prompt.md
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/stores/physicPaintStore.ts
@app/src/lib/physicPaintBridge.ts
@app/src/lib/physicPaintBridge.test.ts

<interfaces>
Lock the discretionary vocabulary for all downstream plans: runtime/document/transport field `incomingInterpolationBreakKeyIds: readonly string[]`; immutable empty constant `PHYSIC_PAINT_ROTO_INCOMING_INTERPOLATION_BREAK_KEY_IDS_EMPTY`; parser `parsePhysicPaintRotoIncomingInterpolationBreakKeyIds(value, records)`; empty collection contributes no new canonical revision term, while a non-empty collection contributes a deterministic owner-ID term. The collection contains stable real-key IDs only.
</interfaces>
</context>

<planning_note>spec-less probe fallback skipped: phase had no requirement IDs at plan-phase init; goal-backward must_haves were derived from the activated SPECS prompt, CONTEXT.md, RESEARCH.md, and canonicalized GAP-01..GAP-06 instead.</planning_note>

<assumptions>
- The exact names above are selected under Claude's Discretion and become the phase-wide implementation vocabulary.
- Existing additive Loop Clip compatibility is the analog: absent persisted/transport input normalizes to the frozen empty runtime collection, and an empty collection does not perturb prior canonical revisions.
- No source file or package is added by this plan.
</assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Acknowledge the locked one-way stable-key ownership contract</name>
  <files>.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md</files>
  <read_first>
    - .planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md — D-01 one-way ownership rationale and D-02..D-04 lifecycle
    - SPECS/milestone-v0.9.0-new-phases/phase-43.1-intentional-gap-insert-prompt.md — activated product contract
  </read_first>
  <decision>Confirm execution of the already locked D-01 contract: incoming interpolation breaks are persisted by stable real-key identity in the canonical physical document.</decision>
  <context>D-01 is already a user decision, not an open technology choice. This checkpoint records the one-way door required by REVERSIBILITY_GATES: changing ownership after persistence/history ships would require saved-document and replay migration.</context>
  <options>
    <option id="confirm-d01">
      <name>Proceed with the locked stable-key ownership contract</name>
      <pros>Identity-preserving timing edits carry breaks automatically; delete/paste/history semantics remain derivable from canonical key identity.</pros>
      <cons>Changing to frame/adjoining-span ownership later requires migration of saved physical documents, revisions, and replay snapshots.</cons>
    </option>
    <option id="halt-phase">
      <name>Halt before implementation</name>
      <pros>No persistent contract is committed.</pros>
      <cons>Phase 43.1 cannot satisfy GAP-01 or the activated SPECS contract.</cons>
    </option>
  </options>
  <action>Pause execution before the tracer writes the persistent contract. Present the two options exactly as an acknowledgement gate; do not reinterpret or reopen D-01. Resume only on `confirm-d01`, otherwise halt the phase without source changes.</action>
  <verify>
    <automated>grep -F "**D-01:**" .planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - The user response is exactly `confirm-d01` before Task 2 starts.
    - No source file listed in this plan changes before acknowledgement.
  </acceptance_criteria>
  <done>The locked one-way decision is acknowledged for execution without changing its substance.</done>
  <resume-signal>Select: confirm-d01 or halt-phase</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Trace one break-bearing physical document through parent acceptance</name>
  <reversibility rating="one-way">Per D-01, changing persisted ownership after this contract lands requires migration of saved physical documents, canonical revisions, and history replay.</reversibility>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/stores/physicPaintStore.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — exact document allowlist, Loop Clip parser, canonical encoder, revision builder, and document parser analogs
    - app/src/types/physicPaint.ts — replace-roto-physical-map payload/result guards and operation/result unions
    - app/src/stores/physicPaintStore.ts — complete document install/getter and immutable structural state conventions
    - app/src/lib/physicPaintBridge.ts — expectedRevision check, independent parent parsing, staged revision, and one replaceRotoPhysicalDocument call
    - app/src/lib/physicPaintBridge.test.ts — complete payload fixture and atomic no-mutation assertions
    - .planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md — exact Loop Clip analog excerpts
  </read_first>
  <behavior>
    - RED: a valid payload containing records for key IDs `key-0` and `key-10` plus `incomingInterpolationBreakKeyIds: ['key-10']` is accepted and the store returns the same frozen owner collection.
    - RED: malformed non-array input, duplicate `key-10`, and orphan `missing-key` each reject before records, Loop Clips, interpolation, selection, cursor, or revision change.
    - RED: a stale expectedRevision rejects the complete proposal with no mutation.
    - RED: empty/absent break input normalizes to the frozen empty runtime collection and preserves the prior empty-state revision encoding.
  </behavior>
  <action>Start with the failing bridge integration cases above. Then add the required runtime field and immutable empty constant to `PhysicPaintRotoPhysicalDocument`; implement `parsePhysicPaintRotoIncomingInterpolationBreakKeyIds` with exact array/string validation, duplicate rejection, and referential owner validation against parsed real-key records; thread the complete collection through closed transport/result allowlists, canonical revision encoding, store document install/getter, and parent bridge staging. Use one complete `replaceRotoPhysicalDocument` only after all validation succeeds. Keep internal parser diagnostics out of product UI. Do not add resolver/action behavior in this tracer. Commit RED then GREEN, and refactor only if tests remain green. This implements D-01 and the strict portions of D-18.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts -t "accepts one stable-key-owned incoming interpolation break atomically|rejects malformed duplicate orphan or stale incoming interpolation breaks without mutation"</automated>
  </verify>
  <acceptance_criteria>
    - `PhysicPaintRotoPhysicalDocument` and the replace payload/result expose `incomingInterpolationBreakKeyIds` as immutable complete state.
    - The parser reconstructs a frozen collection and rejects malformed, duplicate, and orphan owner IDs.
    - The parent recomputes the staged revision from records, interpolation, Loop Clips, and incoming breaks before one store replacement.
    - The focused RED command fails for missing behavior before production edits and the identical command passes after GREEN.
    - Git history contains a `test(43.1-01):` commit before a `feat(43.1-01):` commit.
  </acceptance_criteria>
  <done>One production payload path carries a stable-key-owned break through strict parent acceptance into canonical runtime state, while malformed and stale paths mutate nothing.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new source or test file paths. It introduces:
- Field: `incomingInterpolationBreakKeyIds` on runtime document, apply payload/result, store state, and accepted document contracts.
- Constant: `PHYSIC_PAINT_ROTO_INCOMING_INTERPOLATION_BREAK_KEY_IDS_EMPTY`.
- Function: `parsePhysicPaintRotoIncomingInterpolationBreakKeyIds`.
- Canonical revision term for non-empty incoming-break ownership.
- Bridge/store integration for complete immutable break replacement.
- Bridge test fixture coverage for valid, malformed, duplicate, orphan, and stale break-bearing documents.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| child resolver/transport → parent bridge | Untrusted or stale complete physical-edit payload crosses into parent-authoritative mutation code. |
| persisted/runtime-shaped document → strict model parser | Unknown records and break owner IDs must be reconstructed and referentially validated before use. |
| parent bridge → store replacement | Validated staged state becomes accepted canonical runtime state in one mutation. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.1-01 | Tampering | `parsePhysicPaintRotoIncomingInterpolationBreakKeyIds` | high | mitigate | Exact array/string validation, duplicate rejection, owner referential validation, immutable reconstruction, and focused malformed-input tests before mutation. |
| T-43.1-02 | Tampering / Repudiation | `physicPaintBridge.ts` expected revision | high | mitigate | Recompute current and staged canonical revisions; reject stale proposals before store replacement and assert total no-mutation. |
| T-43.1-03 | Tampering | bridge semantic state | high | mitigate | Accept only complete allowlisted fields and install through one parsed `PhysicPaintRotoPhysicalDocument`; unrelated state cannot be omitted or partially patched. |
| T-43.1-04 | Information Disclosure | parser/bridge errors | medium | mitigate | Keep detailed parser text at the internal boundary; later product surfaces consume the typed mapper from Plan 04 rather than raw bridge diagnostics. |
</threat_model>

<verification>
- Checkpoint acknowledgement precedes all source changes.
- The focused tracer test is run RED then GREEN with the identical one-shot `vitest run` command.
- `pnpm --dir app run typecheck` exits 0 after GREEN.
- `git diff --check` exits 0.
</verification>

<success_criteria>
- D-01's stable-key ownership is an explicit canonical runtime and transport contract.
- One valid break-bearing complete document is accepted end-to-end through the parent bridge and store.
- Malformed, duplicate, orphan, and stale inputs are blocked at high severity with zero partial mutation.
- The plan is not called complete without the acknowledgement checkpoint and TDD gate evidence.
</success_criteria>

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-01-SUMMARY.md` when done.
</output>
