# Phase 36.13: Physics Paint Roto Dynamic Interpolation Spacing - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-02
**Phase:** 36.13-Physics Paint Roto Dynamic Interpolation Spacing
**Areas discussed:** Override creation, Save targeting, Key utilities, MVP feedback

---

## Override creation

| Question | Option | Description | Selected |
|----------|--------|-------------|----------|
| When should a custom segment spacing override be created? | Both sources | Infer existing distant source-key gaps on hydration/regeneration and also record new far-ahead saves while interpolation is ON. | ✓ |
| When should a custom segment spacing override be created? | New saves only | Only create overrides when the user saves a far-ahead key from the expanded interpolation timeline. | |
| When should a custom segment spacing override be created? | Claude decides | Let downstream planning choose. | |
| Which existing gaps become custom overrides? | Gap > global | Only source gaps larger than the current global in-between count become custom. | ✓ |
| Which existing gaps become custom overrides? | Any gap > 1 | Treat every non-adjacent source gap as intentional custom spacing. | |
| Which existing gaps become custom overrides? | Claude decides | Let downstream planning choose. | |
| How should a far-ahead save map when interpolation is later OFF? | Compressed source gap | Store it after the previous source key by the custom segment distance. | ✓ |
| How should a far-ahead save map when interpolation is later OFF? | Same display frame | Keep visible display frame number as source frame too. | |
| How should a far-ahead save map when interpolation is later OFF? | Claude decides | Let downstream planning infer. | |
| How should custom overrides be keyed in durable data? | Segment by source | Persist overrides by adjacent source segment, e.g. fromSourceFrame/toSourceFrame plus generated count. | ✓ |
| How should custom overrides be keyed in durable data? | Target key only | Persist the custom generated count on the destination real key. | |
| How should custom overrides be keyed in durable data? | Claude decides | Let planner choose exact data shape. | |

**User's choices:** Both sources; Gap > global; Compressed source gap; Segment by source.
**Notes:** Existing intentional distant keys must not collapse, and new far-ahead saves while interpolation is enabled must create durable source-segment spacing.

---

## Save targeting

| Question | Option | Description | Selected |
|----------|--------|-------------|----------|
| Can Save current promote a selected generated frame? | Far save only | Generated frames stay preview-only; creating a new real key happens only by navigating/saving at a far empty display position beyond the current expanded layout. | ✓ |
| Can Save current promote a selected generated frame? | Promote blue | Allow Save current on a generated frame to replace/promote it into a real key. | |
| Can Save current promote a selected generated frame? | Claude decides | Let downstream planning choose. | |
| What happens when trying to paint/save a generated frame? | Block with copy | Keep editing/save disabled and show compact render-only guidance. | ✓ |
| What happens when trying to paint/save a generated frame? | Auto jump | Automatically jump to the nearest valid real/empty target before saving. | |
| What happens when trying to paint/save a generated frame? | Claude decides | Let planner choose. | |
| Which segment gets the override for a far empty save? | Previous segment | The segment from previous source real key to new real key records the override. | ✓ |
| Which segment gets the override for a far empty save? | Nearest segment | Use whichever adjacent displayed real key is nearest. | |
| Which segment gets the override for a far empty save? | Claude decides | Let planner choose. | |
| Can far empty save append beyond the last source key? | Append with cap | Append and preserve custom spacing, clamped to existing max frame/interpolation limits. | ✓ |
| Can far empty save append beyond the last source key? | Append always | Append any non-negative display distance as custom spacing. | |
| Can far empty save append beyond the last source key? | Claude decides | Let planner choose validation boundary. | |

**User's choices:** Far save only; Block with copy; Previous segment; Append with cap.
**Notes:** Generated frames stay preview-only and cannot be converted to real keys by Save current in this phase.

---

## Key utilities

| Question | Option | Description | Selected |
|----------|--------|-------------|----------|
| Duplicate/Insert shift source keys; what happens to overrides? | Rebase with keys | Shift/rebase overrides with affected source segments where possible. | ✓ |
| Duplicate/Insert shift source keys; what happens to overrides? | Drop affected | Remove overrides for shifted segments and let global count recalculate. | |
| Duplicate/Insert shift source keys; what happens to overrides? | Claude decides | Let planner choose. | |
| Delete removes a real key in custom spacing; what happens? | Drop merged override | Remove overrides touching the deleted key; newly adjacent segment falls back to global. | ✓ |
| Delete removes a real key in custom spacing; what happens? | Merge distances | Carry total custom distance across new merged segment. | |
| Delete removes a real key in custom spacing; what happens? | Claude decides | Let planner choose. | |
| Paste targets far empty display position while interpolation is ON. | Same as save | Paste to far empty display position creates/replaces a real key and records previous-segment custom spacing. | ✓ |
| Paste targets far empty display position while interpolation is ON. | No custom paste | Paste creates a real key only at compact/global placement. | |
| Paste targets far empty display position while interpolation is ON. | Claude decides | Let planner choose. | |
| Can Copy use generated frames when custom spacing is active? | Disallow | Generated frames stay render-only; Copy requires a real green source key. | ✓ |
| Can Copy use generated frames when custom spacing is active? | Copy render | Allow copying generated rendered alpha as a new real key source. | |
| Can Copy use generated frames when custom spacing is active? | Claude decides | Let planner choose. | |

**User's choices:** Rebase with keys; Drop merged override; Same as save; Disallow.
**Notes:** Key utilities must preserve source-key authority and generated render-only semantics while keeping custom spacing coherent.

---

## MVP feedback

| Question | Option | Description | Selected |
|----------|--------|-------------|----------|
| How visibly should custom segment spacing be shown? | Subtle badge | Keep existing layout, but show a compact custom-span indicator/status on real-key connectors or selected segment. | ✓ |
| How visibly should custom segment spacing be shown? | Implicit only | Only preserved positions reveal custom spacing. | |
| How visibly should custom segment spacing be shown? | Controls now | Add reset/apply-to-all/custom controls now. | |
| What should custom-spacing copy emphasize? | Generated count | Show remembered generated-frame count, e.g. Custom span: 4 in-betweens. | ✓ |
| What should custom-spacing copy emphasize? | Source gap | Show source-key distance. | |
| What should custom-spacing copy emphasize? | Claude decides | Let planner choose exact wording. | |
| Should this phase include reset custom spacing UI? | Defer reset | No reset/apply-to-all UI now; reset can be a later explicit feature. | ✓ |
| Should this phase include reset custom spacing UI? | Single reset | Add reset-selected-segment action now. | |
| Should this phase include reset custom spacing UI? | Claude decides | Let planner choose. | |
| How should failures or invalid attempts be surfaced? | Compact status | Reuse workflow-strip status line for invalid generated-frame save, capped distance, or regeneration failure. | ✓ |
| How should failures or invalid attempts be surfaced? | Modal warn | Use modal confirmation/warnings. | |
| How should failures or invalid attempts be surfaced? | Claude decides | Let planner choose. | |
| User-added edge case: onion skinning | Real-key anchors only | Generated frames must never become onion-skin anchors; onion skin should resolve around real source keys only. | ✓ |

**User's choices:** Subtle badge; Generated count; Defer reset; Compact status; real-key-only onion anchors.
**Notes:** The user explicitly confirmed: generated frames must never become onion-skin anchors; onion skin should resolve around real source keys only.

---

## Claude's Discretion

None.

## Deferred Ideas

- Reset/apply-to-all custom spacing UI — future explicit feature, not Phase 36.13 MVP.
