---
phase: 36.13-physics-paint-roto-dynamic-interpolation-spacing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/physicsPaintWorkflowState.ts
  - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
autonomous: true
requirements:
  - 36.13-SOURCE-DISPLAY-MODEL
  - 36.13-SEGMENT-SPACING-OVERRIDES
must_haves:
  truths:
    - "D-01/D-02: existing distant source-key gaps create custom segment overrides only when the gap is larger than the current global in-between count."
    - "D-03: a far display save that creates four in-betweens after source key 2 resolves to source key 6, not display key 11."
    - "D-04: durable overrides are keyed by adjacent source endpoints and remembered generated in-between count."
    - "Normal compact case: source 0 / 1 / 2, global count 2, interpolation on displays real keys 0 / 3 / 6 and interpolation off displays 0 / 1 / 2."
    - "Custom distant case: source 0 / 1 / 2 / 6, global count 2, interpolation on displays real keys 0 / 3 / 6 / 11 and interpolation off displays 0 / 1 / 2 / 6."
    - "Changing the global interpolation count from 2 to 1 or 3 does not change the custom 2 -> 6 segment's four generated in-betweens."
  prohibitions:
    - statement: "Do not persist target-key-local custom-spacing flags in place of source endpoint overrides."
      status: resolved
      verification: "types/workflow-state tests assert fromSourceFrame/toSourceFrame keyed overrides."
    - statement: "Do not model generated frames as authoritative source keys."
      status: resolved
      verification: "helper tests assert generated entries are derived display/cache output only."
  artifacts:
    - path: app/src/components/physic-paint/physicsPaintWorkflowState.ts
      provides: "Override-aware source/display transform and far-empty display target resolver."
      exports:
        - RotoSegmentSpacingOverride
        - inferRotoSegmentSpacingOverrides
        - getExpandedRotoRealKeyFrames
        - getRotoInterpolationSpanFrames
        - getSourceRotoFrameForDisplayFrame
        - resolveRotoFarEmptyDisplaySaveTarget
    - path: app/src/types/physicPaint.ts
      provides: "Validated persistent interpolation settings including segment spacing overrides."
      exports:
        - PhysicPaintRotoSegmentSpacingOverride
        - PhysicPaintRotoInterpolationSettings.segmentSpacingOverrides
  key_links:
    - from: app/src/types/physicPaint.ts
      to: app/src/components/physic-paint/physicsPaintWorkflowState.ts
      via: "shared segmentSpacingOverrides shape consumed by pure helpers"
    - from: app/src/components/physic-paint/physicsPaintWorkflowState.ts
      to: later store/session plans
      via: "single source/display contract for regeneration, save targeting, key utility rebasing, UI feedback, preview, and export"
---

<objective>
Create the tested source/display contract for Phase 36.13 dynamic interpolation spacing.

Purpose: This plan promotes real-source segments as the durable identity and demotes generated frames to derived display/cache output. Decision: promote, because D-01 through D-04 require custom timing to be reconstructed from real source keys plus per-source-segment overrides instead of display-only metadata.
Output: Type-safe segment override settings, pure helper behavior, and RED/GREEN tests that downstream store/session/UI plans consume without reinterpreting the model.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** intentionally distant real Roto keys to create remembered per-segment interpolation spacing, **so that** custom timing survives global in-between count changes, interpolation toggles, save/load, preview, and export.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-CONTEXT.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-RESEARCH.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-PATTERNS.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-VALIDATION.md
@SPECS/36.x-phases/phase-36.13-dynamic-interpolation-spacing/spec-36.13-dynamic-interpolation-spacing.md
@.planning/phases/36.12-physics-paint-roto-generated-interpolation/36.12-06-SUMMARY.md
@.planning/phases/36.12-physics-paint-roto-generated-interpolation/36.12-11-SUMMARY.md
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
@app/src/types/physicPaint.ts
@app/src/types/physicPaint.test.ts
</context>

## Artifacts this phase produces

- Type/interface: `PhysicPaintRotoSegmentSpacingOverride`
- Field: `PhysicPaintRotoInterpolationSettings.segmentSpacingOverrides`
- Type/interface: `RotoSegmentSpacingOverride`
- Helper: `normalizeRotoSegmentSpacingOverrides`
- Helper: `inferRotoSegmentSpacingOverrides`
- Helper update: `getExpandedRotoRealKeyFrames(realKeys, settings)` uses per-segment spacing.
- Helper update: `getRotoInterpolationSpanFrames(realKeys, settings)` emits override-aware generated frames.
- Helper update: `getSourceRotoFrameForDisplayFrame(displayFrame, realKeys, settings, mode)` preserves real-source identity.
- Helper: `resolveRotoFarEmptyDisplaySaveTarget`
- Test cases in `physicsPaintWorkflowState.test.ts` and `physicPaint.test.ts` for compact/custom examples and validation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for override-aware source/display examples</name>
  <files>app/src/components/physic-paint/physicsPaintWorkflowState.test.ts, app/src/types/physicPaint.test.ts</files>
  <read_first>
    @SPECS/36.x-phases/phase-36.13-dynamic-interpolation-spacing/spec-36.13-dynamic-interpolation-spacing.md
    @.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-CONTEXT.md
    @.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-PATTERNS.md
    @app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
    @app/src/types/physicPaint.test.ts
  </read_first>
  <behavior>
    - D-01/D-02: `inferRotoSegmentSpacingOverrides([0,1,2,6], { enabled: true, inBetweenCount: 2 })` returns one adjacent source-segment override for `2 -> 6` with `inBetweenCount: 4`; compact `[0,1,2]` returns no overrides.
    - D-04: valid settings accept `segmentSpacingOverrides: [{ fromSourceFrame: 2, toSourceFrame: 6, inBetweenCount: 4 }]`; invalid endpoints, duplicate segments, non-integer counts, and counts above `PHYSIC_PAINT_MAX_APPLY_FRAMES` are rejected or normalized according to the nearby validator pattern.
    - Spec examples: compact source keys `0/1/2` with global count 2 display real-key frames `0/3/6`; custom source keys `0/1/2/6` with a `2 -> 6 = 4` override display real-key frames `0/3/6/11`.
    - Spec global count protection: changing global count to 1 or 3 changes only non-custom segments; the `2 -> 6` segment still contributes four generated in-betweens.
  </behavior>
  <action>Write failing behavior tests using literal expected arrays from the spec, not expected values recomputed with helper logic. Cover D-01 through D-04 explicitly in test names or assertions. Use existing Vitest patterns and no one-off test config.</action>
  <acceptance_criteria>
    - RED run fails because `segmentSpacingOverrides`, `inferRotoSegmentSpacingOverrides`, and override-aware display mapping are not yet implemented.
    - Test assertions include the compact `0/1/2 -> 0/3/6` and custom `0/1/2/6 -> 0/3/6/11` examples as independent literals.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts src/types/physicPaint.test.ts -x</automated>
  </verify>
  <done>RED tests exist and fail for the intended missing Phase 36.13 behavior, with no server started.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN segment override types and source/display helpers</name>
  <files>app/src/components/physic-paint/physicsPaintWorkflowState.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/physicsPaintWorkflowState.test.ts, app/src/types/physicPaint.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/physicsPaintWorkflowState.ts
    @app/src/types/physicPaint.ts
    @app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
    @app/src/types/physicPaint.test.ts
  </read_first>
  <action>Implement D-01 through D-04 by extending interpolation settings with source-segment overrides keyed by `fromSourceFrame`, `toSourceFrame`, and `inBetweenCount`. Add pure normalization/inference helpers in `physicsPaintWorkflowState.ts`; clamp counts with the existing Physics Paint frame limits and ignore malformed or non-adjacent segments rather than allowing them to drive display output. Update `getExpandedRotoRealKeyFrames` and `getRotoInterpolationSpanFrames` so each adjacent source segment uses its override count when present and otherwise uses the global `inBetweenCount`; disabling interpolation returns source real-key display positions only. Add `resolveRotoFarEmptyDisplaySaveTarget` for D-03 so downstream Studio/session code can resolve display saves to compressed source keys plus a previous-segment override without duplicating math.</action>
  <acceptance_criteria>
    - `PhysicPaintRotoInterpolationSettings` exposes a validated `segmentSpacingOverrides` array with explicit source endpoints and remembered in-between count per D-04.
    - `getExpandedRotoRealKeyFrames([0,1,2], enabled count 2)` returns real keys at display frames `0,3,6` and generated frames between them.
    - `getExpandedRotoRealKeyFrames([0,1,2,6], enabled count 2 plus `2 -> 6 = 4`) returns real keys at display frames `0,3,6,11`.
    - Global count changes preserve the custom `2 -> 6 = 4` spacing and update only non-custom segments.
    - `resolveRotoFarEmptyDisplaySaveTarget` reports source key `6` and previous segment override `2 -> 6 = 4` for the spec's far-display-save scenario.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts src/types/physicPaint.test.ts -x</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <done>Focused helper/type tests and typecheck pass; source/display identity is promotable as the phase's durable model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved project/settings -> TypeScript validators | Untrusted persisted interpolation settings may contain malformed overrides or huge counts. |
| helper model -> downstream cache/UI consumers | Incorrect helper output could make generated display frames look like source real keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.13-01 | Tampering | `PhysicPaintRotoInterpolationSettings.segmentSpacingOverrides` | high | mitigate | Validate source endpoints and counts, clamp counts with `PHYSIC_PAINT_MAX_APPLY_FRAMES`, and test invalid override rejection/normalization. |
| T-36.13-02 | Tampering | `getExpandedRotoRealKeyFrames` | high | mitigate | Keep real keys authoritative and generated entries render-only with source/display fields; tests assert source real-key arrays stay unchanged. |
| T-36.13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks exist; use existing workspace dependencies only. |
</threat_model>

<verification>
Overall checks: focused Vitest for workflow state/types; `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`; no dev server.
</verification>

<success_criteria>
Plan 01 succeeds when downstream plans can call a typed helper to reconstruct display real keys and generated spans from source real keys, global settings, and per-segment overrides, covering D-01 through D-04 and the compact/custom spec examples.
</success_criteria>

<output>
Create `.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-01-SUMMARY.md` when done.
</output>
