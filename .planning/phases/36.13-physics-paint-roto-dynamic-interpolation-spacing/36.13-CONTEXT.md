# Phase 36.13: Physics Paint Roto Dynamic Interpolation Spacing - Context

**Gathered:** 2026-07-02
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 36.13 corrects the Physics Paint Roto interpolation source/display model so intentionally distant real Roto keys create durable per-segment spacing overrides. Real Roto keys remain authoritative, generated interpolation frames remain derived/render-only, and custom timing must survive interpolation toggle, global in-between count changes, close/reopen, project save/load, preview, and export.

This phase must update the consistent source/display model across hydration, parent bridge sync, persistence, preview/export, Roto session behavior, and key utility transactions. It must not be a workflow-strip-only display patch, must not add generated-to-real conversion for blue generated frames, and must not add reset/apply-to-all custom-spacing controls or final Pencil timeline layout polish.

</domain>

<decisions>
## Implementation Decisions

### Override Creation
- **D-01:** Custom segment spacing overrides must be created from both sources: existing distant source-key gaps discovered during hydration/regeneration, and new far-ahead saves while interpolation is enabled.
- **D-02:** Existing source-key gaps become custom overrides only when the source gap is larger than the current global `inBetweenCount`. Adjacent/normal segments remain controlled by the global count.
- **D-03:** A new far-ahead real key saved while interpolation is enabled uses compressed source spacing when interpolation is later disabled. Example: a display save that creates a 4-in-between custom span after source key `2` becomes source key `6`, not source key `11`.
- **D-04:** Durable custom overrides are keyed by adjacent source segment, with explicit source endpoints such as `fromSourceFrame`/`toSourceFrame` plus the remembered generated in-between count. Do not persist only a target-key-local flag if that would make shifted/replaced segments ambiguous.

### Save Targeting
- **D-05:** Generated blue frames remain preview-only. `Save current` must not promote or replace a generated frame into a real key during this phase.
- **D-06:** Creating a new real key while interpolation is enabled happens at a far empty display position beyond the current expanded layout, not by saving a selected generated/render-only frame.
- **D-07:** If the selected frame is generated/render-only, painting and `Save current` stay disabled and the workflow strip shows compact guidance that generated frames are render-only and the user should navigate to an empty/custom position for a new key.
- **D-08:** A far empty display save attaches its custom override to the segment from the previous source real key to the new destination real key.
- **D-09:** Far empty display saves may append beyond the last source key, but generated/custom spacing must be clamped by existing safe frame/count limits such as `PHYSIC_PAINT_MAX_APPLY_FRAMES`.

### Key Utilities
- **D-10:** Duplicate and Insert must rebase custom segment overrides with shifted source real keys where possible, so intentional timing follows the real-key sequence.
- **D-11:** Delete removes any custom overrides touching the deleted key. The newly adjacent merged segment falls back to the global in-between count unless the user later creates a custom span intentionally.
- **D-12:** Paste to a far empty display position while interpolation is enabled behaves like `Save current`: it creates/replaces a real key and records previous-segment custom spacing.
- **D-13:** Copy from generated frames remains disallowed. Copy requires a real green source key even when custom spacing is active.

### MVP Feedback and Onion Skinning
- **D-14:** MVP UI should keep the existing workflow strip/timeline layout and add only subtle custom-span feedback, such as a compact badge or selected-segment/connector status. Full timeline polish belongs to Phase 36.14.
- **D-15:** Custom-spacing copy should emphasize the user-facing generated in-between count, e.g. `Custom span: 4 in-betweens`, rather than internal source-gap math.
- **D-16:** Reset/apply-to-all custom spacing UI is deferred. This phase must preserve custom spacing and never silently erase it, but it does not add controls to reset it.
- **D-17:** Invalid generated-frame save attempts, capped distances, and regeneration failures should reuse the compact workflow-strip status line. Do not add modal, toast, tutorial, or blocking overlay feedback for this MVP.
- **D-18:** Generated frames must never become onion-skin anchors. Onion skinning resolves around real source keys only.
- **D-19:** When editing a real green key, previous/next onion references must be neighboring real source keys. Custom distant real keys count as real onion-skin anchors.
- **D-20:** When previewing a generated frame, the generated preview itself may be visible, but previous/next onion references still resolve to the surrounding real source keys for that interpolation segment. Custom segment spacing must not cause onion skinning to step through generated frames as if they were real keys.

### Claude's Discretion
No decisions were delegated to Claude. Exact TypeScript field names and helper names are left to downstream planning, but the source-segment override model, generated-frame restrictions, key utility semantics, UI scope, and onion-skin behavior are locked.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 36.13 source of truth
- `.planning/ROADMAP.md` — Phase 36.13 roadmap entry, goal, requirements, dependency on Phase 36.12, and planning notes.
- `SPECS/36.x-phases/phase-36.13-dynamic-interpolation-spacing/spec-36.13-dynamic-interpolation-spacing.md` — Source of truth for dynamic per-segment interpolation spacing behavior and acceptance checklist.
- `/Users/lmarques/Desktop/quick-dynamic-interpolation-phase36.12.md` — User-provided scope-change brief that motivated Phase 36.13 and includes product examples and acceptance notes.

### Direct prior behavior contracts
- `.planning/phases/36.12-physics-paint-roto-generated-interpolation/36.12-CONTEXT.md` — Locks generated render-only interpolation, visible enabled/count/mode controls, preview-only generated selection, and source/display separation from Phase 36.12.
- `.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-CONTEXT.md` — Locks the compact Roto session/key state boundary, effect descriptors, save orchestration ownership, and regression-first approach.
- `.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-CONTEXT.md` — Locks Duplicate/Insert/Delete/Copy/Paste behavior, generated-frame eligibility, dirty save-before-action, and immediate cache/cell/canvas updates.
- `.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md` — Locks save-on-leave behavior, source-frame save truth, failed-save retention, and queued destination semantics.

### Project context
- `.planning/PROJECT.md` — Project context, Preact/Signals architecture, Canvas 2D PreviewRenderer/export pipeline, and non-replacement boundary for Basic, FX, and Physics paint modes.
- `.planning/REQUIREMENTS.md` — v0.8.0 Physics Paint milestone requirements and traceability.
- `.planning/STATE.md` — Current workflow state and recent Phase 36.12/36.13 position.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/src/components/physic-paint/physicsPaintWorkflowState.ts` — Current interpolation helper seam. `RotoInterpolationSettings`, `RotoInterpolationSpanFrame`, `getExpandedRotoRealKeyFrames`, `getRotoInterpolationSpanFrames`, and `getSourceRotoFrameForDisplayFrame` currently derive expanded display frames from source keys plus global settings, but have no per-segment override model yet.
- `app/src/stores/physicPaintStore.ts` — Store seam for Roto cache metadata, generated cache metadata, interpolation settings persistence, generated cache removal/regeneration, display-frame metadata, MCE serialization/hydration, `physicPaintVersion` invalidation, and preview/export-observed output.
- `app/src/types/physicPaint.ts` — Typed transport/persistence seam for `PhysicPaintRotoInterpolationSettings`, `PhysicPaintRotoCacheFrame`, launch context interpolation settings, apply-canvas payloads, update-interpolation payloads, and validation. The settings type currently has enabled/count/mode/deform/position but no segment override field.
- `app/src/components/physic-paint/physicsPaintRotoSession.ts` — Compact Roto session boundary already tracks real keys, generated frame numbers, dirty state, save continuations, copied key state, action availability, restore intent, and effects such as `replaceKeys`, `clearGeneratedFrames`, and navigation. Extend this boundary rather than recreating broad Roto orchestration in `PhysicsPaintStudio`.
- `app/src/components/physic-paint/physicsPaintRotoKeyController.ts` and related tests — Key utility transaction logic is the likely place to preserve, rebase, or drop segment overrides during Duplicate, Insert, Delete, Copy, and Paste.
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` and `PhysicsPaintWorkflowStrip.tsx` — Integration/UI seams for selected frame, generated-frame preview-only behavior, interpolation controls/status, save current, key utilities, and workflow-strip feedback.
- `app/src/lib/previewRenderer.ts` and `app/src/lib/exportRenderer.ts` — Preview/export paths that must observe the same regenerated generated cache and source/display result.

### Established Patterns
- This is a Preact project. Prefer Preact Signals and the existing compact Roto session boundary for shared reactive state; keep source/display transforms and transaction logic pure and directly unit-tested.
- Keep `PhysicsPaintStudio.tsx` as thin wiring for Phase 36.13. Put source/display spacing logic in focused helpers, store helpers, workflow-state/session utilities, or small dedicated hooks if needed; do not add broad new `useEffect` orchestration to the already-large Studio file.
- Real Roto keys are the only editable authoritative frames. Generated interpolation frames and background-only support frames are render support and must not become editable sources.
- Cached Roto PNG alpha output is the durable truth for real keys; paper/background composition remains separate.
- Roto cache changes must bump `physicPaintVersion` and project dirty/version state so preview, export, project save/load, and UI refresh observe current output.
- Normal Roto MVP feedback stays compact inside the Physics Paint workflow strip/status surface. Modals/toasts are avoided unless a prior explicit behavior requires them.
- Existing source/display code already stores `sourceFrame` and `displayFrame` metadata on real-key and generated cache frames; Phase 36.13 should make that model durable and override-aware instead of patching only visual cell positions.

### Integration Points
- Extend interpolation settings/types and validation to carry per-segment overrides through launch context, apply/update payloads, store persistence, project save/load, and close/reopen.
- Update expanded-display helpers so generated frames derive from source real keys, global `inBetweenCount`, and per-segment overrides; global count changes must affect only non-custom segments.
- Update store regeneration so real-key metadata, generated cache metadata, background-only support replacement, preview/export, and `getRotoFrame`/`getRotoCacheFrames` use the same override-aware display model.
- Update far empty save and paste paths so interpolation-enabled displayed positions map back to compressed source keys plus previous-segment overrides.
- Update key utility transactions so overrides rebase on Duplicate/Insert, drop around Delete, and stay compatible with Paste-to-far-empty behavior.
- Update onion-skin source resolution so previous/next anchors are real source keys only, never generated frames.
- Regression tests should cover compact source keys, custom distant source keys, global count changes, far-ahead save, far-ahead paste, Duplicate/Insert rebase, Delete drop, generated-frame save/copy blocking, onion-skin real-key anchoring, persistence/hydration, live toggle on/off, preview/export parity, and `physicPaintVersion` invalidation.

</code_context>

<specifics>
## Specific Ideas

- The user mental model is: source real keys remain green; generated frames remain blue; custom spans remember a generated in-between count per source segment.
- Example from the source brief: source keys `0 / 1 / 2 / 6` with global count `2` display real keys at `0 / 3 / 6 / 11`; the `2 -> 6` source segment has a custom spacing of 4 generated frames.
- Changing global count from `2` to `1` or `3` must not change the custom `2 -> 6 = 4 generated frames` segment.
- Generated frames are never promoted by saving, copied as source keys, or used as onion-skin anchors in this MVP.

</specifics>

<deferred>
## Deferred Ideas

- Reset/apply-to-all custom spacing UI — future explicit feature. Phase 36.13 must not silently erase custom spacing and should not add reset controls.
- Full `PhysicsPaintStudio.tsx` hooks/module split — follow-up technical debt/future phase. Phase 36.13 may add small focused helpers/hooks only when needed for safe thin wiring, but must not perform the broad Studio refactor.
- Final Pencil timeline layout and broader visual polish remain Phase 36.14.

</deferred>

---

*Phase: 36.13-Physics Paint Roto Dynamic Interpolation Spacing*
*Context gathered: 2026-07-02*
