---
phase: 12.1.1-big-ui-frontend-design-changes-with-new-sidebar-layout
plan: 05
type: execute
wave: 4
depends_on: ["12.1.1-04"]
files_modified: []
autonomous: false
requirements:
  - SIDEBAR-LAYOUT
  - SIDEBAR-CSS
  - SIDEBAR-RESIZE
  - SIDEBAR-COLLAPSE
  - KEY-PHOTOS-INLINE
  - LAYER-EYE
  - KF-NAV
  - KF-INTERP-INLINE
  - KF-POPOVER-REMOVE
  - PERSIST-PANELS
  - TYPOGRAPHY

must_haves:
  truths:
    - "Sidebar has 3 resizable sub-windows that match the .pen design"
    - "All sidebar colors use --sidebar-* CSS variables"
    - "Key photos appear inline under selected sequence"
    - "Layer rows show eye icons for visibility"
    - "Keyframe nav bar works with correct disabled states"
    - "Interpolation mode swap works when diamond selected"
    - "Old popover is completely gone"
    - "Panel heights and sidebar width persist across app restart"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint: confirm all sidebar redesign features match the .pen design spec.

Purpose: Human verifies that the redesigned sidebar looks and functions correctly against the target screenshot and .pen design.
Output: Approval or issue list for gap closure.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.1.1-big-ui-frontend-design-changes-with-new-sidebar-layout/12.1.1-UI-SPEC.md
@.planning/phases/12.1.1-big-ui-frontend-design-changes-with-new-sidebar-layout/12.1.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and test suite</name>
  <files></files>
  <read_first>
    Application/src/lib/panelResize.test.ts,
    Application/src/lib/keyframeNav.test.ts
  </read_first>
  <action>
    Run the full build and test suite to confirm everything compiles and passes:
    1. `cd Application && pnpm build` -- TypeScript + Vite build
    2. `cd Application && pnpm vitest run` -- Full test suite
    3. Verify no TypeScript errors
    4. Start the dev server: `cd Application && pnpm tauri dev` -- confirm app launches
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && pnpm build 2>&1 | tail -3 && pnpm vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 with no errors
    - `pnpm vitest run` exits 0 with all tests passing
    - No TypeScript compilation errors
  </acceptance_criteria>
  <done>Build passes, all tests green, app launches successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification against target design</name>
  <files></files>
  <action>
    Present the user with the verification checklist below. Launch the app with `cd Application && pnpm tauri dev` if not already running. Wait for user to complete visual inspection against the target screenshot at `/Users/lmarques/Desktop/screenshot-final.png`.
  </action>
  <what-built>
    Complete sidebar redesign with:
    1. Three resizable sub-windows (SEQUENCES, LAYERS, PROPERTIES) with ellipsis drag handles
    2. Sidebar-specific color palette (--sidebar-* CSS variables)
    3. Sequence cards with borders, accent bar, inline key photos with slide animation
    4. Layer rows at 44px with Eye/EyeOff visibility, GripVertical drag handle, Lock icon
    5. KeyframeNavBar (< + trash >) replacing old + Keyframe button
    6. InlineInterpolation chips replacing old double-click popover
    7. Sidebar width resize (240-480px) via right edge drag
    8. Double-line collapse handle replacing old chevron
    9. Custom thin scrollbars (4px, auto-hide)
    10. Updated typography (11px section titles, 12px field labels, 13px layer names, 14px sequence names)
  </what-built>
  <how-to-verify>
    Launch the app: `cd Application && pnpm tauri dev`

    **1. Sidebar Layout:**
    - Verify 3 panels visible: SEQUENCES, LAYERS, PROPERTIES
    - Verify ellipsis drag handles between panels
    - Drag handles to resize panels -- confirm smooth real-time resize
    - Drag a panel to very small size -- confirm it collapses to zero at ~32px threshold
    - Panel backgrounds should be `#252539` (dark purple-gray)
    - Sidebar wrapper should be `#2B2B3D` (slightly lighter)

    **2. Sequence Cards:**
    - Select a sequence -- confirm it gets border `#636382`, bg `#1f1f33`, left accent bar `#5555AA`
    - Unselected sequences should have border `#3D3D5A`
    - Key photos should appear inline under selected sequence with slide animation
    - Click + button on key photo strip -- should open full Imported view (not a small popover)
    - Select a different sequence -- confirm previous key photos collapse, new ones expand

    **3. Layer Rows:**
    - Confirm layer rows are 44px tall
    - Confirm eye icon for visibility toggle (not filled/empty circle)
    - Confirm grip-vertical icon for drag handle (not 3-bar CSS)
    - Confirm lock icon for base layer (not emoji)
    - Selected layer should have `#3D3D7A` background

    **4. Keyframe Features:**
    - Select a content layer with keyframes
    - Verify keyframe nav bar shows < + trash > buttons
    - Verify < and > navigate playhead to prev/next keyframe
    - Verify + shows Plus icon when NOT on keyframe, RefreshCw when ON keyframe
    - Verify trash is grayed unless on a keyframe
    - Click a keyframe diamond on timeline -- verify blend+opacity row swaps to interpolation chips
    - Click away from diamond -- verify blend+opacity row returns
    - Double-click a diamond -- verify no popover appears (old behavior removed)

    **5. Sidebar Resize + Collapse:**
    - Drag right edge of sidebar -- confirm width changes (min 240, max 480)
    - Click double-line at right edge -- confirm sidebar collapses
    - When collapsed, click double-line at left edge -- confirm sidebar expands
    - Close and reopen app -- confirm panel heights and sidebar width are preserved

    **6. Compare to target screenshot:**
    - Open `/Users/lmarques/Desktop/screenshot-final.png` side by side
    - Verify overall layout, colors, spacing match the target
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms sidebar matches target screenshot
    - All 6 verification areas pass visual inspection
  </acceptance_criteria>
  <done>User approves the visual design or provides issue list for gap closure.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Build passes
- All tests green
- Visual match against target screenshot
- All interactive behaviors work as specified
</verification>

<success_criteria>
- User confirms sidebar redesign matches the .pen design specification
- All 10 feature areas listed above verified working
- No visual regressions in other parts of the app
</success_criteria>

<output>
After completion, create `.planning/phases/12.1.1-big-ui-frontend-design-changes-with-new-sidebar-layout/12.1.1-05-SUMMARY.md`
</output>
