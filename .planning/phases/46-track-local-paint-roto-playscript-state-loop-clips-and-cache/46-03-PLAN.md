---
phase: 46-track-local-paint-roto-playscript-state-loop-clips-and-cache
plan: 03
type: tdd
wave: 3
depends_on: ["46-01", "46-02"]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/efxPaintStore.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
  - app/src/stores/trackIsolation.test.ts
autonomous: true
requirements: [TRK-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Copy/cut/paste/duplicate/clear operations target the exact internal track: a paste into track B never touches track A's real keys or caches (ROADMAP SC 4)"
    - "Pasted items always get fresh identities (v0.9 rail-set rule, D-05): new keyId/loopId allocations, never a link back to the source track"
    - "A Hold Loop Clip pasted across tracks is re-pointed to the destination track's own copied source frames; when re-pointing is impossible the paste is rejected explicitly — never a dangling or foreign-track reference (D-06)"
    - "Cross-track paste deep-copies the underlying source frame assets so the destination track is fully self-contained; the no-durable-asset-duplication contract applies only to linked repeats INSIDE one Loop Clip (D-07)"
    - "The store-level cross-track move operation (re-tag trackId, preserve frame timing) exists and behaves exactly as copy-paste-delete with fresh identities (D-08/D-09); the drag gesture itself is Phase 47"
    - "Undo/redo is a unified document-wide stack; each entry is tagged with the trackId it mutated and undo targets exactly that track, never another track's history (D-01)"
    - "Undo depth keeps the 10-level operation-count cap (D-02); entries store references + the prior deterministic revision hash, never raster bytes (D-03)"
    - "Undoing an entry whose target track is not active auto-activates that track via efxPaintStore.setActiveTrackId so the affected track is visible (D-04)"
    - "recordAcceptedEdit dedupes by operationId AND trackId so a cross-track edit never collides (PATTERNS line 562 extension)"
  assumptions:
    - "flagged assumption (edge TRK-04 unclassified, status unresolved): the full operation matrix (copy/cut/paste/duplicate/clear/undo/redo x same-track x cross-track) is covered by unit tests here plus native UAT; the probe could not auto-resolve it"
  artifacts:
    - app/src/stores/physicPaintStore.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
  key_links:
    - "physicsPaintRotoRailSetCopy.ts:188 buildRotoRailSetCopyPayload — the existing fresh-identity allocation (RotoRailSetFreshIdentityAllocation at :90) extended with source/target track context"
    - "useRotoPhysicalEditHistory.ts:81-86 identity + :109-118 command shape + :503-507 trimAppliedHistory (exact 10-level cap) + :562 recordAcceptedEdit dedupe + :663-718 undo replay"
    - "efxPaintStore.ts getDocument(layerId).activeTrackId — the source of the active track; setActiveTrackId is added here"
  prohibitions:
    - "Never store raster bytes in undo entries — references + prior deterministic revision hash only (D-03/Pitfall 17)"
    - "Never produce a cross-track or dangling Hold reference on paste — re-point to the destination track's copied frames or reject explicitly (D-06)"
    - "No useEffect for internal state derivation (CLAUDE.md) — the history hook keeps its existing effect-subscription shape, no new hooks"
---

<objective>
Make every edit operation track-aware: copy/cut/paste/duplicate/clear and the store-level cross-track move follow the locked semantics (fresh identities D-05, Hold re-pointing fail-closed D-06, deep-copy assets D-07, move = copy-paste-delete D-09), and the undo/redo ledger becomes a unified document-wide stack with per-entry trackId tags (D-01..D-04) so undo targets the exact track that produced the edit, capped at 10 operations.

Purpose: TRK-04's acceptance is "Undo/redo targets the exact internal track" and "Copy/cut/paste/duplicate/clear operations are track-aware". The existing rail-set copy payloads and the history ledger are already the proven machinery — this plan adds the track dimension to both, without a second history manager or a second paste engine.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-CONTEXT.md
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-RESEARCH.md
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-PATTERNS.md
@app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/stores/physicPaintStore.ts
@app/src/stores/efxPaintStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Track-scoped copy/cut/paste/duplicate/clear store ops with fresh identities</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts, app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.test.ts, app/src/stores/trackIsolation.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.ts:90-230 (RotoRailSetFreshIdentityAllocation + buildRotoRailSetCopyPayload — the fresh-identity engine to extend with track context)
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts (copy/paste clipboard surface)
    - app/src/stores/physicPaintStore.ts:834-1090 (the store accessors the ops call, now track-scoped from 46-01)
  </read_first>
  <behavior>
    - RED Test 1 (same-track copy/paste): copying rail set S from track A and pasting into track A allocates fresh keyIds/loopIds (never reuses S's ids) and produces identical payloads.
    - RED Test 2 (cross-track paste isolation): pasting A's selection into track B changes B's records only; A's records, caches, and revision values stay byte-identical.
    - RED Test 3 (Hold re-pointing): a Hold Loop Clip with sourceFrameRefs pointing at A's frames pasted into B is re-pointed to B's copied frames (new loopId, sourceFrameRefs = B's new keyIds); the reference never points back into A.
    - RED Test 4 (reject not dangle): a partial selection whose Hold source frames are NOT part of the pasted set rejects the paste explicitly (ok:false with reason) and writes nothing.
    - RED Test 5 (deep-copy assets): the destination track's pasted frames hold their own dataUrl bytes (equal to the source bytes — deep copy — but owned by B; deleting B leaves A intact).
    - RED Test 6 (clear): clearTrackFrames(layerId, trackA, frames) removes only A's frames + generated cache; B's cache paths untouched.
  </behavior>
  <action>
    Extend buildRotoRailSetCopyPayload (physicsPaintRotoRailSetCopy.ts) with sourceTrackId/targetTrackId context: the fresh-identity allocation produces new keyIds + new loopIds per D-05; for Hold Loop Clips (sourceKind 'playscript-hold') re-point sourceFrameRefs to the target track's freshly allocated keyIds for the same source frames — when a referenced source frame is not part of the pasted selection, return a closed failure result (never a cross-track or dangling reference, D-06). Add store ops on physicPaintStore: copyTrackSelection(layerId, trackId, keyIds) → clipboard payload, cutTrackSelection (copy + remove from the source track), pasteTrackSelection(layerId, targetTrackId, payload) → applies through replaceRotoPhysicalRecords(layerId, targetTrackId, …) with fresh identities, duplicateTrackFrames(layerId, trackId, frames), clearTrackFrames(layerId, trackId, frames). Every op accepts trackId explicitly and routes only through the 46-01 per-track maps. All 6 tests RED then GREEN. Commit test(46-03) then feat(46-03): track-scoped copy/cut/paste/duplicate/clear.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.test.ts src/stores/trackIsolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass; every new op signature takes an explicit trackId
    - The paste result type includes a closed failure for un-re-pointable Hold clips (ok:false + reason)
    - Deleting the pasted destination track leaves the source track's frames and caches untouched (test 5)
  </acceptance_criteria>
  <done>Copy/cut/paste/duplicate/clear route to the exact target track; Hold pastes never dangle; paste is always fresh identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store-level cross-track move (D-08/D-09) — the data primitive Phase 47 drag calls</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/trackIsolation.test.ts</files>
  <read_first>
    - 46-CONTEXT.md D-08/D-09 (cross-track move semantics: fresh identities in destination, source removed, references re-pointed under the same paste rules)
    - app/src/stores/physicPaintStore.ts:1153-1194 (replaceRotoKeyFrames — the batch application the move reuses)
  </read_first>
  <behavior>
    - RED Test 1 (move semantics): moveTrackItems(layerId, fromTrackId, toTrackId, keys) removes the items from the source and adds fresh-identity copies to the destination with the same appFrames; source cache paths and records for those frames are gone.
    - RED Test 2 (move = copy-paste-delete equivalence): the resulting destination state equals cutTrackSelection then pasteTrackSelection into the destination; the resulting source state equals cutTrackSelection's effect.
    - RED Test 3 (Hold re-pointing on move): a Hold clip moved with its source frames re-points to the destination's copied frames; a move where re-pointing is impossible fails closed (ok:false) and the source items stay in place.
  </behavior>
  <action>
    Add moveTrackItems(layerId: string, fromTrackId: string, toTrackId: string, keys: readonly string[]) on physicPaintStore implementing D-09 verbatim: cutTrackSelection from the source (fresh-identity clipboard payload), pasteTrackSelection into the destination, delete the source items; Hold Loop Clips re-point under the Task 1 rules — when re-pointing is impossible the whole move returns a closed failure and the source stays untouched. The primitive takes trackIds explicitly; Phase 47's drag gesture will call exactly this function. All 3 tests RED then GREEN. Commit test(46-03) then feat(46-03): cross-track move primitive.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/trackIsolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 tests pass; moveTrackItems(layerId, fromTrackId, toTrackId, keys) is exported
    - Failed moves leave the source untouched (test 3)
    - The destination identity is fresh — no keyId from the source survives in the destination
  </acceptance_criteria>
  <done>The store exposes a fail-closed cross-track move that Phase 47 drag will call; move is provably copy-paste-delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unified track-tagged undo/redo (D-01..D-04) with auto-activation</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts, app/src/stores/efxPaintStore.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts:81-86 (RotoPhysicalEditHistoryIdentity — gains trackId), :109-118 (RotoPhysicalEditCommand — gains trackId), :503-507 (the exact 10-cap trim), :562 (recordAcceptedEdit dedupe), :663-718 (undo() replay + snapshotReplayAuthorityEqual + coordinator.executePhysicalEdit)
    - app/src/stores/efxPaintStore.ts (the document store that owns activeTrackId — add setActiveTrackId here)
  </read_first>
  <behavior>
    - RED Test 1 (track tagging): after an edit on track B, the applied-stack top entry carries trackId B; undoing it replays B's before-state and leaves A's records untouched.
    - RED Test 2 (cap): 12 ordinary edits on mixed tracks → the applied stack holds at most 10 entries (existing trim behavior with track-tagged entries).
    - RED Test 3 (auto-activate): with track A active, undoing the top entry tagged B sets the document activeTrackId to B (efxPaintStore.setActiveTrackId effect) and bumps the document revision.
    - RED Test 4 (dedupe): two accepted edits with the same operationId on different tracks both record (the dedupe key includes trackId).
    - RED Test 5 (no-bytes): the snapshots carry records + refs + the prior revision hash only — a test asserts no snapshot field holds a dataUrl raster.
  </behavior>
  <action>
    Add trackId: string to RotoPhysicalHistoryIdentity (the hook identity now includes trackId alongside launchOperationId/layerId/projectContextId/capacity) and to RotoPhysicalEditCommand. recordAcceptedEdit dedupe keys on operationId + trackId (cross-track operation ids never collide). undo(): after the existing snapshotReplayAuthorityEqual validation on the physical records (the D-03 recompute path), check the entry's trackId — if it is not the document's active track, call the new efxPaintStore.setActiveTrackId(layerId, entry.trackId) FIRST (D-04) then replay through coordinator.executePhysicalEdit as today; redo symmetric. The 10-cap trimAllHistory (lines 503-507) is unchanged. setActiveTrackId(layerId, trackId) validates the trackId exists in getDocument(layerId).tracks (fail closed otherwise), sets activeTrackId, and bumps documentRevision via the 45-01 builders (activeTrackId is a docrev term). All 5 tests RED then GREEN. Commit test(46-03) then feat(46-03): track-tagged undo/redo with auto-activation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 tests pass; the history identity and command include trackId
    - efxPaintStore.ts exports setActiveTrackId(layerId, trackId) and getActiveTrackId(layerId)
    - No snapshot type in the history module carries raster bytes (test 5)
  </acceptance_criteria>
  <done>Undo always targets the exact mutating track, never another track's history; non-active targets auto-activate; 10-level cap holds.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New symbols (physicPaintStore.ts): `copyTrackSelection(layerId, trackId, keys)`, `cutTrackSelection`, `pasteTrackSelection(layerId, targetTrackId, payload)`, `duplicateTrackFrames(layerId, trackId, frames)`, `clearTrackFrames(layerId, trackId, frames)`, `moveTrackItems(layerId, fromTrackId, toTrackId, keys)`
- New symbols (efxPaintStore.ts): `setActiveTrackId(layerId, trackId)`, `getActiveTrackId(layerId)`
- Extended (useRotoPhysicalEditHistory.ts): `RotoPhysicalEditHistoryIdentity.trackId`, `RotoPhysicalEditCommand.trackId`, dedupe key operationId+trackId, auto-activation in undo/redo
- Extended (physicsPaintRotoRailSetCopy.ts): buildRotoRailSetCopyPayload with sourceTrackId/targetTrackId + closed Hold re-pointing failure
- Modified: `app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts`, test files

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard payload → paste | the payload is created in-process but crosses the track boundary on paste; a foreign-track reference must never survive |
| undo replay → coordinator | replay revalidates the track's current state before writing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-07 | Tampering | cross-track paste reference | high | mitigate | D-06 fail-closed re-pointing: Hold sourceFrameRefs never point at a foreign track; rejection instead of dangling refs (Task 1 tests 3/4; ASVS V4) |
| T-46-08 | Tampering | undo replay on the wrong track | high | mitigate | entries carry trackId; replay revalidates the entry's track state and auto-activates the target before write (Task 3 tests 1/3) |
| T-46-09 | Integrity | raster bytes in history | medium | mitigate | snapshots are refs + deterministic revision hashes only (Task 3 test 5) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoRailSetCopy.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/stores/trackIsolation.test.ts` — new/extended suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Copy/cut/paste/duplicate/clear and cross-track move target the exact track with fresh identities (ROADMAP SC 4)
- Undo/redo is track-tagged, capped, reference-based, and auto-activates the affected track (TRK-04)
- The cross-track move primitive the Phase 47 drag gesture calls exists and is fail-closed
</success_criteria>

<output>
Create `.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-03-SUMMARY.md` when done
</output>
</plan>
