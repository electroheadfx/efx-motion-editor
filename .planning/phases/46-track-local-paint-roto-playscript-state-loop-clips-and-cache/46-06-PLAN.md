---
phase: 46-track-local-paint-roto-playscript-state-loop-clips-and-cache
plan: 06
type: tdd
wave: 6
depends_on: ["46-01", "46-02", "46-03", "46-04", "46-05"]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/efxPaintTrackCache.test.ts
  - app/src/stores/trackIsolation.test.ts
autonomous: true
requirements: [TRK-02, TRK-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Hold Loop Clips are track-local: a clip on track A resolves exclusively against A's real-key records and A's loop clips — a clip on A at the same appFrame as a clip on B never answers B's frames (edge TRK-02 adjacency, resolved explicit)"
    - "A track with loop clips but zero real keys answers every clip cell 'linked-unresolved' — never a foreign-track frame, never a fabricated base (edge TRK-02 empty, resolved explicit)"
    - "Multiple clips on one track resolve by placementStart order with half-open effectiveEnd boundaries: the frame at the next clip's placementStart belongs to the next clip (or 'real'/'empty'), never both (edge TRK-02 ordering, resolved explicit)"
    - "Hold clips are a live single source of truth: linked cells derive from the owning track's real keys at resolution time — a source-frame edit is the ONLY cause of linked change (D-10/D-11)"
    - "Editing a Hold source frame atomically invalidates every linked occurrence of that clip within the owning track's structural memo; other tracks' memo entries stay valid — no cross-track invalidation (D-12, edge TRK-08 adjacency, resolved explicit)"
    - "A Hold clip whose source frames are missing resolves 'linked-unresolved' with missingSourceKeyIds — fail closed, never a dangling or stale answer (D-13)"
    - "A Hold clip is never created with empty sourceFrameRefs or refs pointing at a foreign track's keyIds — closed rejection at creation (edge TRK-08 empty, resolved explicit)"
    - "Linked repeats follow the deterministic modulo with stable sourceKeyIds order: repeatInstance and sourceIndex derive from (frame - placementStart) % cycleLength over the clip's own source cycle, identical for every occurrence (edge TRK-08 ordering, resolved explicit)"
  assumptions:
    - "flagged assumption (edge TRK-04 unclassified, status unresolved): the full operation matrix is 46-03 scope; the linked-clip laws proven here are the ownership + invalidation complement"
    - "flagged assumption (edge TRK-05 unclassified, status unresolved): async authority revalidation is 46-04 scope"
    - "flagged assumption (edge TRK-06 unclassified, status unresolved): stale-async laws are 46-04 scope"
    - "flagged assumption (edge TRK-07 unclassified, status unresolved): deletion laws are 46-05 scope"
  artifacts:
    - app/src/stores/physicPaintStore.ts
    - app/src/stores/efxPaintTrackCache.test.ts
  key_links:
    - "physicsPaintRotoPhysicalResolver.ts:5570 resolvePhysicPaintRotoLoopFrame — the shared scheduler REUSED verbatim (NO modification; PATTERNS do-not-fork list)"
    - "physicsPaintRotoPhysicalResolver.ts:5306-5342 PhysicPaintRotoFrameResolution — the 6-kind answer ('linked-unresolved' at 5611 IS the D-13 fail-closed state)"
    - "physicPaintStore.ts:249-303 _resolveRotoPhysicalStructural — the memo builder made track-composite-keyed in 46-01 Task 2; the seam that builds the resolution context"
    - "physicPaintStore.ts:1604-1674 getRotoPhysicalLoopClips / replaceRotoPhysicalLoopClips (46-01 track-scoped) — the clip surface the creation validation extends"
    - "app/src/efx-paint/document/efxPaintDocument.ts:30-37 FrameLoopClip (sourceFrameRefs, sourceKind 'playscript-hold', revision)"
  prohibitions:
    - "Never modify physicsPaintRotoPhysicalResolver.ts — the resolver is the do-not-fork shared primitive; track provenance lives in the store's context builder"
    - "Never persist a 'linked' or 'linked-unresolved' resolution as a cell, cache entry, or frame list — resolutions are virtual query answers (resolver contract)"
    - "No useEffect for internal state derivation and no React-style hook/effect sprawl (CLAUDE.md)"
---

<objective>
Prove and enforce the track-local Hold Loop Clip laws on top of the shared resolver: clip ownership is per-track (a clip on A never resolves against B's records), linked cells derive from the owning track's real keys as a live single source of truth with atomic per-track invalidation on source edits, source-missing answers fail closed as 'linked-unresolved', clip boundaries are half-open by placementStart, and clip creation rejects empty or foreign refs.

Purpose: TRK-02 (track-local Hold ownership) and TRK-08 (linked semantics) close the loop-clip laws. The resolver (physicsPaintRotoPhysicalResolver.ts:5570) already implements every resolution kind including the D-13 fail-closed state — this plan builds each track's resolution context from that track's own maps only, and proves the six edge truths with cache-level tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-CONTEXT.md
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-RESEARCH.md
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-PATTERNS.md
@app/src/stores/physicPaintStore.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/efx-paint/document/efxPaintDocument.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Track-local Hold resolution context (TRK-02 adjacency/empty/ordering)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/trackIsolation.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:249-303 (_resolveRotoPhysicalStructural — the memo builder that must build the context per track)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts:5570-5763 (resolvePhysicPaintRotoLoopFrame — consumed unchanged)
    - 46-PATTERNS.md 'Hold linked-source resolution' shared pattern
  </read_first>
  <behavior>
    - RED Test 1 (adjacency): two tracks each hold a Hold Loop Clip at the same appFrame over the same source keyId; resolving the appFrame on each track returns that track's own loopId and sourceKeyId — the A answer never shows B's loopId, and the two answers are byte-different when the clips are.
    - RED Test 2 (empty): a track with a Hold clip but zero real keys answers every clip cell 'linked-unresolved' (missingSourceKeyIds listed) — never a frame from the other track, never a fabricated base.
    - RED Test 3 (ordering): two clips on one track [C1@10, C2@20] with effectiveEnd boundaries resolve appFrame 19 as C1 and appFrame 20 as C2 ('real' or C2's own answer) — no overlap, no gap, deterministic by placementStart.
    - RED Test 4 (context provenance): the context the store builds for track A contains no loopId and no keyId owned by track B (the builder surfaces the clip set it used).
    - RED Test 5 (virtual-only): no 'linked'/'linked-unresolved' resolution ever appears in the store's persisted projection (records, cache entries, or document frames) — resolutions are query answers only.
  </behavior>
  <action>
    In physicPaintStore.ts, change the structural memo builder (_resolveRotoPhysicalStructural) to construct the resolution context EXCLUSIVELY from the track's own maps — `_rotoRealKeyRecords.get(layerId)?.get(trackId)`, `_rotoPhysicalLoopClips.get(layerId)?.get(trackId)`, the track's own interpolation state — and expose `getTrackRotoResolutionContext(layerId: string, trackId: string): { readonly trackId: string; readonly context: PhysicPaintRotoLoopResolutionContext } | null` that returns the provenance pair (the resolver itself stays untouched — the store never passes a context assembled from two tracks). The memo stays composite-keyed (46-01). Keep the existing "real key first, then interval, then modulo, out-of-domain 'empty'" precedence of the resolver. All 5 tests RED then GREEN. Commit test(46-06) then feat(46-06): track-local Hold resolution context.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/trackIsolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 tests pass; a clip on A never answers with B's loopId/keyId
    - The context builder carries trackId provenance; the resolver file is unmodified
    - Empty/ordering laws hold (tests 2-3) and no linked answer is persisted (test 5)
  </acceptance_criteria>
  <done>Every Hold cell answers from its owning track's live records; boundaries and empty tracks are deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live single source of truth + atomic per-track invalidation (D-10..D-12)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/efxPaintTrackCache.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:249-303 (the memo + composite-key invalidation from 46-01 Task 2)
    - app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts (the existing cache-suite harness to extend, not fork)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts:5301-5342 ('linked' resolution fields — what a cache entry may carry)
  </read_first>
  <behavior>
    - RED Test 1 (single source): a 3-repeat Hold clip resolves the same sourceKeyId for repeatInstance 0,1,2 and the modulo is (frame - placementStart) % cycleLength — editing the source frame changes EVERY repeat cell after invalidation.
    - RED Test 2 (atomic invalidation): editing a Hold source frame on track A invalidates A's structural memo entry; the next query re-resolves all linked cells of the clip from the new source — never a stale linked frame.
    - RED Test 3 (no cross-track invalidation): the A edit leaves B's memo entry valid — B's getRotoPhysicalRenderSource results for a B-linked frame are unchanged (46-01 test 3 held at the cache layer).
    - RED Test 4 (unresolved never cached): a track whose source frame was deleted answers its clip cells 'linked-unresolved' and the store's frames/cache lists contain no entry for those cells (virtual-only contract).
  </behavior>
  <action>
    No new invalidation machinery: the 46-01 composite-key memo + bumpTrackRevision is the whole mechanism. Add the exported store surface the tests use — `getTrackRotoResolutionContext` (Task 1) is already the re-resolve seam. Ensure the memo invalidation path on a track mutation deletes exactly `layerId\0trackId` (verify in the bump path from 46-01 Task 2), and add `invalidateTrackStructuralMemo(layerId, trackId)` if the composite delete is not yet reachable from the test seam — one symbol, no effect sprawl. All 4 tests RED before GREEN. Commit: test(46-06) then feat(46-06): single-source linked resolution with atomic per-track invalidation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintTrackCache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; linked cells re-resolve from the source after any source edit
    - B's memo survives A's source edit (test 3)
    - No unresolved cell is ever persisted into cache or frame lists (test 4)
  </acceptance_criteria>
  <done>Hold cells are live answers from one source cycle; invalidation is atomic within the owning track and never crosses tracks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-closed creation + linked ordering laws (D-13 + TRK-08 edges)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/efxPaintTrackCache.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts:5306-5342 ('linked' and 'linked-unresolved' result shapes — the missing-sourceKeyIds contract)
    - app/src/stores/physicPaintStore.ts:1650-1674 (replaceRotoPhysicalLoopClips — the clip surface to validate)
    - app/src/efx-paint/document/efxPaintDocument.ts:30-37 (FrameLoopClip.sourceFrameRefs/sourceKind)
  </read_first>
  <behavior>
    - RED Test 1 (empty refs rejected): creating a Hold clip with sourceFrameRefs: [] through the track-scoped clip surface fails closed with reason 'empty-source' and writes nothing.
    - RED Test 2 (foreign refs rejected): creating a Hold clip on track A whose sourceFrameRefs reference keyIds owned by track B fails closed 'foreign-track-refs' and writes nothing.
    - RED Test 3 (missing source after create): deleting a source frame of a valid clip turns the affected cells into 'linked-unresolved' with missingSourceKeyIds and never a dangling answer — re-adding a real key at that frame restores 'linked'.
    - RED Test 4 (ordering): two clips whose effectiveEnd equals the next clip's placementStart answer the boundary frame to the later clip deterministically (sourceIndex computed over the own clip's cycle) — the stable ordering is proven over 10 repeat instances.
  </behavior>
  <action>
    Add to physicPaintStore.ts `validateTrackHoldLoopClipRefs(layerId: string, trackId: string, clip: { readonly sourceFrameRefs: readonly string[]; readonly sourceKind: string }): { ok: true } | { ok: false; error: 'empty-source-refs' | 'foreign-source-refs' }` — empty list closes 'empty-source-refs'; any ref whose keyId is not in the track's own real-key map closes 'foreign-source-refs' (no cross-track lookup). Call it from the clip-replacement surface (replaceRotoPhysicalLoopClips) so a Hold clip creation carrying bad refs fails closed before parse-persist, and re-pointing from 46-03 uses it as the second gate after fresh-identity allocation. No change to the resolver's ordering math — the tests prove the existing modulo contract is track-local and stable (test 4). All 4 tests RED before GREEN. Commit: test(46-06) then feat(46-06): fail-closed Hold creation refs validation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintTrackCache.test.ts src/stores/trackIsolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; a Hold clip is never created with empty or foreign refs
    - Source-missing answers 'linked-unresolved' with missingSourceKeyIds and heal on re-adding the key (test 3)
    - Ordering proves the boundary frame belongs to exactly one clip (test 4)
  </acceptance_criteria>
  <done>Hold creation is fail-closed on empty/foreign refs; missing sources heal; boundary frames belong to exactly one clip.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/stores/efxPaintTrackCache.test.ts`
- New symbols (physicPaintStore.ts): `getTrackRotoResolutionContext(layerId, trackId)` (provenance + resolver context), `invalidateTrackStructuralMemo(layerId, trackId)` (only if the 46-01 composite delete is not already reachable), `validateTrackHoldSourceRefs(layerId, trackId, refs)` — all alongside the 46-01 per-track maps
- Unchanged (do-not-fork): `physicsPaintRotoPhysicalResolver.ts` — the shared scheduler, resolutions, and 'linked-unresolved' fail-closed state
- Modified: `app/src/stores/efxPaintTrackCache.test.ts` (new), `app/src/stores/trackIsolation.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clip creation refs → track maps | sourceFrameRefs are validated against the OWNING track's real-key map only; never cross-track |
| linked answer → cache/persistence | resolutions are virtual; only 'real' and record-owned cells may persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-16 | Spoofing | foreign-track Hold refs | high | mitigate | validateTrackHoldSourceRefs closes 'foreign-source-refs' before any clip persists; refs resolve only within the owning track (Task 3 tests 1-2; ASVS V5) |
| T-46-17 | Tampering | stale linked answer after source edit | high | mitigate | atomic composite-key memo invalidation on the owning track; no cross-track invalidation (Task 2 tests 2/3; D-12) |
| T-46-18 | Integrity | unresolved cells persisted | medium | mitigate | 'linked'/'linked-unresolved' are query-only; persistence never contains them (Task 1 test 5, Task 2 test 4) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintTrackCache.test.ts src/stores/trackIsolation.test.ts` — new/extended suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Hold Loop Clips are provably track-local: own track only, no cross-resolution (TRK-02)
- Linked cells are live single-source answers with atomic per-track invalidation and fail-closed missing-source (D-10..D-13)
- Creation rejects empty/foreign refs; boundary and repeat ordering are deterministic (TRK-08)
</success_criteria>

<output>
Create `.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-06-SUMMARY.md` when done
</output>
</plan>
