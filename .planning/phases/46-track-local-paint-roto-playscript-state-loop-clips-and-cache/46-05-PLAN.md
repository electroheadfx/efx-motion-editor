---
phase: 46-track-local-paint-roto-playscript-state-loop-clips-and-cache
plan: 05
type: tdd
wave: 5
depends_on: ["46-01", "46-02", "46-03", "46-04"]
files_modified:
  - app/src/stores/efxPaintStore.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/lib/efxPaintPersistence.ts
  - app/src/stores/projectStore.ts
  - app/src/stores/trackDeleteLaws.test.ts
autonomous: true
requirements: [TRK-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Deleting an internal track is acknowledge-and-delete: requestDeleteTrack(layerId, trackId) returns a closed preview (frame count, loop-clip count, hold-reference count to sever, isLastTrack flag) and commitDeleteTrack refuses without the acknowledgement (D-14, ASVS V4)"
    - "Deleting the LAST remaining Paint track fails closed with reason 'last-track' and writes nothing (D-17, edge TRK-07 empty, resolved explicit)"
    - "The deleted track's runtime teardown goes through removeTrackRuntime(layerId, trackId) from 46-01: frames, records, loopClips, caches, selection/cursor, leases, and the structural memo composite key are removed; every other track's maps and per-track revision values stay byte-identical (D-14/edge TRK-07 adjacency, resolved explicit)"
    - "Before teardown, every Hold Loop Clip on any surviving track whose source frames referenced the deleted track is severed (sourceFrameRefs emptied) so the resolver answers 'linked-unresolved' — never a dangling or foreign-track reference (D-16)"
    - "After commit, activeTrackId moves to the nearest adjacent surviving track by document order (index-1, else index+1); when the deletion is impossible the active track never moves (D-18, edge TRK-07 ordering, resolved explicit)"
    - "The deleted track's sidecar directory cache/efx-paint/&lt;stableSegment(layerId)&gt;/&lt;trackId&gt; is removed in the same cache transaction as the save that persists the deletion — the prepared save carries a deletions list settled at commit; a rollback keeps the sidecars (D-15)"
    - "The document revision (buildEfxPaintDocumentRevision) and the track-deletion dirty signal fire exactly once per committed deletion (docrev includes activeTrackId and tracks terms)"
  assumptions:
    - "flagged assumption (edge TRK-05 unclassified, status unresolved): async authority revalidation is 46-04 scope"
    - "flagged assumption (edge TRK-06 unclassified, status unresolved): stale-async laws are 46-04 scope"
  artifacts:
    - app/src/stores/efxPaintStore.ts
    - app/src/stores/physicPaintStore.ts
    - app/src/lib/efxPaintPersistence.ts
    - app/src/stores/trackDeleteLaws.test.ts
  key_links:
    - "physicPaintStore.ts:347-384 _clearLayerState (the teardown model) and removeTrackRuntime (46-01 Task 3 — the complete per-track teardown the delete reuses)"
    - "efxPaintStore.ts:31-49 _documents + getDocument + _notifyChange (the document authority the delete mutates)"
    - "efxPaintPersistence.ts:144-158 PreparedEfxPaintSave (gains a deletions list; removeCanonicalAfterCommit at 144-147 is the model) and settlePreparedEfxPaintSave :256-281 (commit removes, rollback keeps)"
    - "efxPaintPersistence.ts:21 the existing remove() import — the deletion primitive, already in the module"
    - "app/src/efx-paint/document/efxPaintDocumentRevision.ts:117-127 the docrev builders (activeTrackId and tracks are docrev terms — the deletion bumps the revision)"
  prohibitions:
    - "Never remove sidecar files outside the tracked two-resource cache transaction — no ad-hoc rm, no direct fs calls from the delete op (D-15, Pitfall 7)"
    - "Never delete the last Paint track and never leave a dangling activeTrackId (D-17)"
    - "No useEffect for internal state derivation and no React-style hook/effect sprawl (CLAUDE.md)"
---

<objective>
Add the acknowledged track-deletion law: `requestDeleteTrack`/`commitDeleteTrack` on efxPaintStore delete exactly one internal track — preview, explicit acknowledgement, last-track refusal (D-17), nearest-adjacent active-track re-point (D-18), severing of every Hold reference that could outlive the track (D-16), full runtime teardown through 46-01's `removeTrackRuntime`, and sidecar-directory removal in the same cache transaction as the next save (D-15).

Purpose: TRK-07 (deletion laws) closes the track lifecycle. Deletion must be acknowledge-gated (ASVS V4), fail closed on the last track, never leave a dangling activeTrackId, and never orphan sidecars outside the tracked transaction.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-CONTEXT.md
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-RESEARCH.md
@.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-PATTERNS.md
@app/src/stores/efxPaintStore.ts
@app/src/stores/physicPaintStore.ts
@app/src/lib/efxPaintPersistence.ts
@app/src/stores/projectStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acknowledge-and-delete preview + last-track refusal (D-14/D-17)</name>
  <files>app/src/stores/efxPaintStore.ts, app/src/stores/trackDeleteLaws.test.ts</files>
  <read_first>
    - app/src/stores/efxPaintStore.ts:31-49 (_documents, getDocument, _notifyChange — the document authority the delete mutates)
    - app/src/efx-paint/document/efxPaintDocument.ts:71-80 (EfxPaintDocument.tracks / activeTrackId — the model to rebuild without the track)
    - app/src/efx-paint/document/efxPaintDocumentRevision.ts:117-127 (the builders that must re-run on the rebuilt document)
  </read_first>
  <behavior>
    - RED Test 1 (preview): requestDeleteTrack(layerId, trackB) returns a TrackDeletePreview with frameCount === B's real-key count, loopClipCount === B's clip count, holdReferenceCount === the number of Hold clips (any surviving track) whose source frames reference B's keyIds, isLastTrack false; the document and runtime are untouched.
    - RED Test 2 (acknowledge gate): commitDeleteTrack(layerId, trackB, false) fails closed; commitDeleteTrack(layerId, trackB, true) succeeds and the track is gone.
    - RED Test 3 (last track): with a single-track document commitDeleteTrack(layerId, trackA, true) fails 'last-track' and the document, runtime, and activeTrackId are byte-identical after.
    - RED Test 4 (unknown track): requestDeleteTrack/commitDeleteTrack for a trackId not in document.tracks returns ok:false with no mutation.
  </behavior>
  <action>
    Add to efxPaintStore.ts: `export interface TrackDeletePreview { readonly layerId: string; readonly trackId: string; readonly frameCount: number; readonly loopClipCount: number; readonly holdReferenceCount: number; readonly isLastTrack: boolean; }` and `requestDeleteTrack(layerId: string, trackId: string): TrackDeletePreview | null` — reads the document (getDocument(layerId)), counts B's frames (physicPaintStore.getFrames(layerId, trackId)) and clip records (physicPaintStore.getRotoPhysicalLoopClips(layerId, trackId)), scans every surviving track's Hold clips for sourceFrameRefs pointing at B's keyIds (holdReferenceCount), and computes isLastTrack from document.tracks.length === 1. Add `commitDeleteTrack(layerId: string, trackId: string, acknowledged: boolean): { ok: true } | { ok: false; error: string }` — refuses when !acknowledged ('delete not acknowledged'), when the track is unknown, and when isLastTrack ('last-track'); otherwise it proceeds to Task 2's teardown (call through to the 46-01 removeTrackRuntime + the rebuilt-document write, which the next tasks provide — implement the seam now with the runtime teardown inline). All 4 tests RED then GREEN. Commit test(46-05) then feat(46-05): track delete preview and acknowledge gate.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/trackDeleteLaws.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; commitDeleteTrack never mutates without acknowledged === true
    - The last surviving track refuses deletion; the active track never moves on a failed delete
    - TrackDeletePreview is exported from efxPaintStore.ts
  </acceptance_criteria>
  <done>The delete path is acknowledged, fail-closed, and last-track-proof; the preview is complete before any mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teardown + Hold severing + nearest-adjacent activation (D-16/D-18)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/efxPaintStore.ts, app/src/stores/trackDeleteLaws.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:347-384 (_clearLayerState — the teardown model) and removeTrackRuntime (46-01 Task 3 — the exported per-track sweep primitive to reuse)
    - app/src/efx-paint/document/efxPaintDocument.ts:30-37,47-59 (FrameLoopClip.sourceFrameRefs — the refs to sever; InternalPaintTrack.rotoPhysical.loopClips)
    - 46-03-PLAN.md Task 1 (the fail-closed re-pointing rule the severing complements)
  </read_first>
  <behavior>
    - RED Test 1 (full teardown): after commitDeleteTrack(layerId, trackB, true), trackB's frames, records, loopClips, cache entries, selection/cursor, leases, and structural memo key are gone; trackA's frames, records, and per-track revision value are byte-identical (the 46-01 removeTrackRuntime contract at the delete layer).
    - RED Test 2 (hold severing): a Hold Loop Clip on surviving track A whose sourceFrameRefs reference B's keyIds is severed (refs emptied) so the resolver answers 'linked-unresolved' — never a dangling or foreign-track reference.
    - RED Test 3 (nearest-adjacent): with tracks [A,B,C] active=B, deleting B re-points activeTrackId to C (index+1); deleting A re-points to the first survivor; deleting C re-points to B.
    - RED Test 4 (revision + dirty): the committed deletion bumps buildEfxPaintDocumentRevision (activeTrackId + tracks terms) and fires the injected dirty callback exactly once.
  </behavior>
  <action>
    In physicPaintStore.ts add `severTrackHoldReferences(layerId: string, deletedTrackId: string): number` — iterates every surviving track's loop clip records; a clip whose sourceFrameRefs references the deleted track's keyIds has its refs emptied (the resolver's 'linked-unresolved' path then answers the same cells, D-13 fail-closed). In efxPaintStore.ts the commit path (Task 1 seam) calls: sever holds (severTrackHoldReferences) → removeTrackRuntime(layerId, trackId) (46-01, the complete per-track sweep) → rebuild the document via the 45-01 document builders minus the track with activeTrackId re-pointed by nearest-adjacent (deterministic index order: the previous track by document order if any, else the next; document.tracks keeps stable order) → setActiveTrackId(layerId, nextId) (46-03) → _notifyChange. The rebuild re-runs buildEfxPaintDocumentRevision on the new document. All 4 tests RED then GREEN. Commit test(46-05) then feat(46-05): track deletion teardown, hold severing, nearest-adjacent activation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/trackDeleteLaws.test.ts src/stores/trackIsolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; the teardown is the 46-01 removeTrackRuntime sweep (no second teardown implementation)
    - No Hold reference survives into a deleted track; the resolver answers 'linked-unresolved'
    - activeTrackId after deletion is the deterministic nearest adjacent survivor
  </acceptance_criteria>
  <done>Deleting a track tears down exactly that track, severs every Hold ref that could outlive it, and re-activates the nearest survivor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sidecar-directory deletion in the same cache transaction (D-15)</name>
  <files>app/src/lib/efxPaintPersistence.ts, app/src/stores/projectStore.ts, app/src/stores/trackDeleteLaws.test.ts</files>
  <read_first>
    - app/src/lib/efxPaintPersistence.ts:144-158 (PreparedEfxPaintSave — gains a deletions list; removeCanonicalAfterCommit is the model)
    - app/src/lib/efxPaintPersistence.ts:252-281 (settlePreparedEfxPaintSave — the commit removes, rollback keeps)
    - app/src/stores/projectStore.ts:107-111 (buildEfxPaintDocuments — the save-input constructor that will carry the deletions list)
  </read_first>
  <behavior>
    - RED Test 1 (deletions in the transaction): after commitDeleteTrack, the next saveEfxPaintDocumentsWithProjectWrite (with a success writeProject) removes the deleted track's sidecar directory cache/efx-paint/&lt;stableSegment(layerId)&gt;/&lt;trackId&gt; — and no surviving track's sidecar directory.
    - RED Test 2 (rollback keeps sidecars): the same save with a failing writeProject settles rollback and the deleted track's sidecar directory still exists (nothing removed outside the committed transaction).
    - RED Test 3 (deletion list cleared): after a committed save the pending deletion list for the layer is empty — a second save is a no-op deletion-wise.
  </behavior>
  <action>
    Extend EfxPaintDocumentSaveInput (efxPaintPersistence.ts:34-38) with `deletions: readonly string[]` (relative dirs under EFX_PAINT_CACHE_DIR, one per deleted track, all passing isSafeEfxPaintCachePath segment rules), extend PreparedEfxPaintSave with `deletions: readonly string[]`, and in prepareEfxPaintSave append each input's deletions. In settlePreparedEfxPaintSave's commit arm (after the canonical publication settles and BEFORE savedDocumentCache.clear) remove each deletion directory via the existing `remove(\`${projectDir}/${dir}\`, { recursive: true })` — failures recorded as non-authoritative (the transaction already committed); rollback never touches deletions. In efxPaintStore keep `pendingTrackDeletions: Map<string, string[]>` (layerId → deletion dirs), populated by the Task 2 commit path (buildEfxPaintFrameCachePath-based dir for the deleted track), exposed via `takePendingTrackDeletions(layerId): readonly string[]` that clears on read; projectStore.buildEfxPaintDocuments merges it into each layer's save input. All 3 tests RED before GREEN. Commit: test(46-05) then feat(46-05): track sidecar deletion through the cache transaction.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/trackDeleteLaws.test.ts src/lib/efxPaintPersistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 tests pass; the sidecar deletion list is settled only at commit, never at request/preview
    - A rollback keeps the deleted track's sidecars; a committed save clears the pending list
    - The deletions list paths all pass isSafeEfxPaintCachePath segment rules
  </acceptance_criteria>
  <done>Track sidecar cleanup rides the same tracked transaction as the save — commit removes, rollback keeps, no ad-hoc rm.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/stores/trackDeleteLaws.test.ts`
- New symbols (efxPaintStore.ts): `TrackDeletePreview`, `requestDeleteTrack(layerId, trackId)`, `commitDeleteTrack(layerId, trackId, acknowledged)`, `takePendingTrackDeletions(layerId)`; (physicPaintStore.ts): `removeTrackHoldReferences(layerId, deletedTrackId)` — the delete path reuses 46-01's `removeTrackRuntime` unchanged
- Extended (efxPaintPersistence.ts): `EfxPaintDocumentSaveInput.deletions`, `PreparedEfxPaintSave.deletions`, commit-arm removal in `settlePreparedEfxPaintSave`; (projectStore.ts): `buildEfxPaintDocuments` merges the per-layer deletion list
- Modified: `app/src/stores/projectStore.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delete preview → commit | the acknowledged flag is the gate; the preview must be computed before any mutation |
| commit → sidecar removal | the deletion directory is a relative path under cache/efx-paint validated by isSafeEfxPaintCachePath |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-13 | Spoofing | unacknowledged delete | high | mitigate | commit refuses without acknowledged === true; preview reports the full destruction surface (Task 1 tests 1-2; ASVS V4) |
| T-46-14 | Tampering | dangling Hold refs after delete | high | mitigate | every surviving Hold that referenced the deleted track is severed before teardown; 'linked-unresolved' is the only answer after (Task 2 test 2; D-16) |
| T-46-15 | Tampering | sidecar deletion outside the transaction | medium | mitigate | the deletion list is settled only in the commit arm of the cache transaction; rollback keeps sidecars (Task 3 tests 1-2; D-15) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/stores/trackDeleteLaws.test.ts src/stores/trackIsolation.test.ts src/lib/efxPaintPersistence.test.ts` — new/extended suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Track deletion is acknowledge-gated, last-track-proof, and fully isolated per track (TRK-07)
- Every Hold reference that could outlive a deleted track is severed; the active track re-points to the nearest survivor
- Sidecar cleanup is transactional — commit removes, rollback keeps
</success_criteria>

<output>
Create `.planning/phases/46-track-local-paint-roto-playscript-state-loop-clips-and-cache/46-05-SUMMARY.md` when done
</output>
</plan>
