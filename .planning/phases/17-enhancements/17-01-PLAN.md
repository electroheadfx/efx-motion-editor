---
phase: 17-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/sequence/SequenceList.tsx
  - Application/src/components/timeline/AddTransitionMenu.tsx
  - Application/src/components/Preview.tsx
  - Application/src/components/timeline/AddFxMenu.tsx
  - Application/src/components/AssetProtocolTest.tsx
  - Application/src/components/export/FormatSelector.tsx
  - Application/src/components/layout/TitleBar.tsx
  - Application/src/components/sidebar/SidebarFxProperties.tsx
  - Application/src/components/layout/CanvasArea.tsx
  - Application/src/components/sidebar/KeyframeNavBar.tsx
  - Application/src/components/sidebar/TransitionProperties.tsx
  - Application/src/components/export/ExportPreview.tsx
  - Application/src/components/layer/LayerList.tsx
  - Application/src/components/sidebar/SidebarProperties.tsx
  - Application/src/components/layout/Toolbar.tsx
  - Application/src/components/sidebar/AudioProperties.tsx
  - Application/src/components/layout/EditorShell.tsx
  - Application/src/components/export/ExportProgress.tsx
  - Application/src/components/layer/AddLayerMenu.tsx
  - Application/src/components/shared/ColorPickerModal.tsx
  - Application/src/components/layout/ThemeSwitcher.tsx
  - Application/src/components/layout/TimelinePanel.tsx
  - Application/src/components/timeline/AddAudioButton.tsx
  - Application/src/components/sequence/KeyPhotoStrip.tsx
  - Application/src/components/project/NewProjectDialog.tsx
  - Application/src/components/views/ExportView.tsx
  - Application/src/components/overlay/ShortcutsOverlay.tsx
  - Application/src/components/project/WelcomeScreen.tsx
  - Application/src/components/views/SettingsView.tsx
  - Application/src/components/import/ImportGrid.tsx
  - Application/src/components/views/ImportedView.tsx
  - Application/src/components/import/DropZone.tsx
  - Application/src/components/shader-browser/ShaderBrowser.tsx
autonomous: true
requirements: [ENH-01, ENH-02]

must_haves:
  truths:
    - "Second click on active sequence header collapses key photo strip"
    - "Click on different sequence auto-expands its key photos and collapses the previous"
    - "Switching sequences always auto-expands (no memory of collapsed state)"
    - "All Tailwind [var(--...)] patterns migrated to (--...) parenthetical syntax"
  artifacts:
    - path: "Application/src/components/sequence/SequenceList.tsx"
      provides: "Collapse toggle on SequenceItem"
      contains: "keyPhotoCollapsed"
    - path: "Application/src/components/layout/TimelinePanel.tsx"
      provides: "Migrated Tailwind syntax"
      contains: "bg-(--color"
  key_links:
    - from: "SequenceList.tsx SequenceItem handleSelect"
      to: "keyPhotoCollapsed state"
      via: "toggle on same-sequence second click"
      pattern: "setKeyPhotoCollapsed"
---

<objective>
Add collapsible key photo lists in the sidebar Sequences panel and migrate all Tailwind v4 syntax warnings project-wide.

Purpose: Solve sidebar scroll blocking when key photos are expanded (ENH-01/ENH-02), and clean up 332 deprecated Tailwind `[var(--...)]` patterns across 33 files (D-18/D-19).
Output: SequenceItem with collapse toggle, all Tailwind classes using parenthetical syntax.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-enhancements/17-CONTEXT.md
@.planning/phases/17-enhancements/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Application/src/components/sequence/SequenceList.tsx (lines 77-155, 379-410):
```typescript
function SequenceItem({seq, isActive}: SequenceItemProps) {
  // ... handleSelect at line 129:
  const handleSelect = useCallback(() => {
    if (!editing) {
      const wasActive = sequenceStore.activeSequenceId.peek();
      uiStore.selectSequence(seq.id);
      sequenceStore.setActive(seq.id);
      // ...
      if (seq.id !== wasActive) {
        // seek to sequence start
      }
    }
  }, [seq.id, editing]);

  // Key photo area (lines 379-407):
  // <div class="overflow-hidden transition-[max-height] duration-150 ease-out"
  //   style={{ maxHeight: isActive ? '72px' : '0px' }}>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add key photo collapse/expand toggle to SequenceItem</name>
  <files>Application/src/components/sequence/SequenceList.tsx</files>
  <read_first>
    - Application/src/components/sequence/SequenceList.tsx
    - Application/src/components/sequence/KeyPhotoStrip.tsx
  </read_first>
  <action>
In SequenceItem component (line 77), add a `useState` for collapse state:

```typescript
const [keyPhotoCollapsed, setKeyPhotoCollapsed] = useState(false);
```

Modify `handleSelect` callback (line 129) to implement the toggle behavior per D-02/D-03/D-04:

```typescript
const handleSelect = useCallback(() => {
  if (!editing) {
    const wasActive = sequenceStore.activeSequenceId.peek();

    // D-02: Second click on SAME active sequence toggles collapse
    if (seq.id === wasActive) {
      setKeyPhotoCollapsed(prev => !prev);
      return;  // Don't re-select, just toggle
    }

    // D-04: Switching sequences — always auto-expand (reset collapse)
    setKeyPhotoCollapsed(false);

    uiStore.selectSequence(seq.id);
    sequenceStore.setActive(seq.id);
    sequenceStore.clearKeyPhotoSelection();

    // Auto-select top-most layer (existing logic)
    const topLayerId = getTopLayerId(seq);
    if (topLayerId) {
      layerStore.setSelected(topLayerId);
      uiStore.selectLayer(topLayerId);
    } else {
      layerStore.setSelected(null);
      uiStore.selectLayer(null);
    }

    // Auto-seek timeline playhead to sequence start (existing logic)
    const layouts = trackLayouts.peek();
    const track = layouts.find(t => t.sequenceId === seq.id);
    if (track) {
      playbackEngine.seekToFrame(track.startFrame);
    }
    timelineStore.ensureTrackVisible(seq.id);
  }
}, [seq.id, editing]);
```

Update the maxHeight condition at line 383 to check both `isActive` AND `keyPhotoCollapsed`:

```typescript
style={{
  maxHeight: isActive && !keyPhotoCollapsed ? '72px' : '0px',
}}
```

Keep the existing `transition-[max-height] duration-150 ease-out` CSS transition class (per discretion: existing animation is fine).
  </action>
  <acceptance_criteria>
    - SequenceList.tsx contains `const [keyPhotoCollapsed, setKeyPhotoCollapsed] = useState(false)`
    - SequenceList.tsx contains `setKeyPhotoCollapsed(prev => !prev)` inside `seq.id === wasActive` branch
    - SequenceList.tsx contains `setKeyPhotoCollapsed(false)` before `uiStore.selectSequence`
    - SequenceList.tsx contains `maxHeight: isActive && !keyPhotoCollapsed`
    - `cd Application && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Second click on active sequence collapses key photos, switching sequences auto-expands, no wheel passthrough fix needed (D-05)</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all Tailwind v4 [var(--...)] to (--...) syntax project-wide</name>
  <files>
    Application/src/components/timeline/AddTransitionMenu.tsx
    Application/src/components/Preview.tsx
    Application/src/components/timeline/AddFxMenu.tsx
    Application/src/components/AssetProtocolTest.tsx
    Application/src/components/export/FormatSelector.tsx
    Application/src/components/layout/TitleBar.tsx
    Application/src/components/sidebar/SidebarFxProperties.tsx
    Application/src/components/layout/CanvasArea.tsx
    Application/src/components/sidebar/KeyframeNavBar.tsx
    Application/src/components/sidebar/TransitionProperties.tsx
    Application/src/components/export/ExportPreview.tsx
    Application/src/components/layer/LayerList.tsx
    Application/src/components/sidebar/SidebarProperties.tsx
    Application/src/components/layout/Toolbar.tsx
    Application/src/components/sidebar/AudioProperties.tsx
    Application/src/components/layout/EditorShell.tsx
    Application/src/components/export/ExportProgress.tsx
    Application/src/components/layer/AddLayerMenu.tsx
    Application/src/components/shared/ColorPickerModal.tsx
    Application/src/components/layout/ThemeSwitcher.tsx
    Application/src/components/layout/TimelinePanel.tsx
    Application/src/components/timeline/AddAudioButton.tsx
    Application/src/components/sequence/SequenceList.tsx
    Application/src/components/sequence/KeyPhotoStrip.tsx
    Application/src/components/project/NewProjectDialog.tsx
    Application/src/components/views/ExportView.tsx
    Application/src/components/overlay/ShortcutsOverlay.tsx
    Application/src/components/project/WelcomeScreen.tsx
    Application/src/components/views/SettingsView.tsx
    Application/src/components/import/ImportGrid.tsx
    Application/src/components/views/ImportedView.tsx
    Application/src/components/import/DropZone.tsx
    Application/src/components/shader-browser/ShaderBrowser.tsx
  </files>
  <read_first>
    - Application/src/components/layout/TimelinePanel.tsx (highest count at 28 occurrences — verify pattern)
    - Application/src/components/sidebar/SidebarFxProperties.tsx (25 occurrences — verify pattern)
  </read_first>
  <action>
Per D-18 and D-19, migrate all 332 occurrences of `[var(--...)]` in Tailwind utility classes to `(--...)` parenthetical syntax across all 33 files.

Migration pattern (within Tailwind class strings ONLY, not `style={{}}` objects):
- `bg-[var(--color-bg-input)]` becomes `bg-(--color-bg-input)`
- `text-[var(--color-text-secondary)]` becomes `text-(--color-text-secondary)`
- `ring-[var(--color-accent)]` becomes `ring-(--color-accent)`
- `border-[var(--color-border-subtle)]` becomes `border-(--color-border-subtle)`
- Any `{utility}-[var(--{custom-prop})]` becomes `{utility}-(--{custom-prop})`

Regex find: `(\w[\w-]*)-\[var\((--[\w-]+)\)\]`
Regex replace: `$1-($2)`

IMPORTANT: Do NOT modify `style={{}}` inline objects. Those use raw CSS `var()` which is correct. Only modify patterns inside Tailwind class strings (template literals or string concatenation inside `class=` / `className=` attributes).

IMPORTANT: Also check for patterns like `text-[var(--color-error-text)]` in SequenceList.tsx line 366 — these follow the same migration pattern.

Process each of the 33 files listed above. After all replacements, verify zero remaining `[var(--` patterns in Tailwind class contexts.
  </action>
  <acceptance_criteria>
    - `grep -r '\[var(--' Application/src/components/ Application/src/views/ | grep -v 'style={{' | grep -v node_modules | wc -l` returns 0
    - `grep -r '(--(color' Application/src/components/layout/TimelinePanel.tsx | head -3` shows parenthetical syntax
    - `cd Application && npx tsc --noEmit` exits 0
    - No `style={{` objects were modified (only Tailwind class strings)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -rn '\[var(--' Application/src/components/ | head -5; echo "---"; cd Application && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>All 332 Tailwind [var(--...)] patterns migrated to (--...) across 33 files, TypeScript compiles clean, no style={{}} objects modified</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `cd Application && npx tsc --noEmit`
2. No remaining `[var(--` in Tailwind classes: `grep -rn '\[var(--' Application/src/components/`
3. Collapse state variable exists: `grep -n 'keyPhotoCollapsed' Application/src/components/sequence/SequenceList.tsx`
</verification>

<success_criteria>
- SequenceItem toggles key photo visibility on second click of active sequence header
- Switching sequences always auto-expands the new sequence's key photos
- All 332 Tailwind v4 syntax warnings resolved across 33 component files
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-enhancements/17-01-SUMMARY.md`
</output>
