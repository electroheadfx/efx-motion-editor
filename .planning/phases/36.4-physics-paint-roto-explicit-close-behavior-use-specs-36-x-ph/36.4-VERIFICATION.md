---
phase: 36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph
verified: 2026-06-20T10:43:30Z
status: human_needed
score: 1/9 must-haves verified
behavior_unverified: 8
overrides_applied: 0
behavior_unverified_items:
  - truth: "User can close Physics Paint from a clean current Roto frame without seeing a prompt."
    test: "Open Physics Paint on a clean Roto frame and close the Physics Paint window."
    expected: "The window closes normally without rendering the unsaved Roto prompt."
    why_human: "Source and tests show the clean branch returns without preventDefault, but the native Tauri close event/window behavior is not exercised by automated runtime tests."
  - truth: "User closing a dirty current Roto frame sees exactly three choices: Close without saving, Cancel, and Close saving."
    test: "Dirty the current Roto frame, close the Physics Paint window, and inspect the prompt."
    expected: "A dialog appears with exactly Close without saving, Cancel, and Close saving."
    why_human: "The prompt JSX and source-inspection regression exist, but no component/runtime test renders the dialog from a real close gesture."
  - truth: "User can choose Close without saving and the dirty current frame is not written while any previously saved cached frame remains unchanged."
    test: "Dirty a current Roto frame that already has a saved cache, choose Close without saving, then reopen Physics Paint."
    expected: "The prior cached reference is unchanged and no dirty edit is saved."
    why_human: "Source evidence shows discard bypasses close and does not call save/apply/delete paths, but persisted reopen outcome is a live app behavior."
  - truth: "User can choose Cancel and remain in Physics Paint with the unsaved canvas state still available."
    test: "Dirty the current Roto frame, close the window, choose Cancel, then continue editing."
    expected: "The window remains open and the dirty canvas state remains visible/editable."
    why_human: "Source evidence shows cancel only clears prompt state, but canvas continuity after a real native close request is not exercised."
  - truth: "User can choose Close saving, see saving feedback, and the window closes only after the existing Save current apply path completes successfully."
    test: "Dirty the current Roto frame, choose Close saving, observe saving feedback and close timing."
    expected: "Saving current frame… appears; the existing save path runs; the window closes only after the matching successful apply result."
    why_human: "Code tracks operation ids and closes from handleApplyResult, and focused tests pass, but no runtime test drives the async Tauri/apply handshake."
  - truth: "User can reopen Physics Paint and verify the chosen saved or discarded outcome."
    test: "After discard and after save-close, reopen Physics Paint on the same frame."
    expected: "Discard shows the previous cache unchanged; save-close shows the newly saved current frame."
    why_human: "Reopen persistence relies on the running app, cache store, bridge, and window lifecycle; automated evidence is source-level only."
  - truth: "After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in a blocked close/saving state."
    test: "Exercise each close path, including a save error or timeout if possible, then attempt another close choice."
    expected: "The prompt/actions remain usable; no stale saving state blocks subsequent choices."
    why_human: "Source evidence resets refs/state on error, timeout, cleanup, and success, but no behavioral test invokes those transitions."
  - truth: "User can close Physics Paint from a clean current Roto frame without a Tauri permission rejection."
    test: "Rerun UAT Test 1 in the live Tauri app after the capability change."
    expected: "Clean close no longer reports Unhandled Promise Rejection: window.destroy not allowed."
    why_human: "The capability now grants core:window:allow-destroy and the focused test asserts it, but only the live Tauri app can prove the prior native permission rejection is gone."
human_verification:
  - test: "Clean close"
    expected: "Open Physics Paint on a clean Roto frame and close the window; it closes without a prompt and without a window.destroy permission rejection."
    why_human: "Requires native close behavior in the running Tauri/app window. Current UAT file still has Test 1 diagnosed from the previous permission failure."
  - test: "Dirty close choices"
    expected: "Dirty the current Roto frame and close the window; exactly Close without saving, Cancel, and Close saving are offered."
    why_human: "Requires real close gesture and rendered dialog confirmation."
  - test: "Discard and reopen outcome"
    expected: "Choose Close without saving; reopen Physics Paint; previous cached reference is unchanged."
    why_human: "Requires live cache/window lifecycle verification."
  - test: "Cancel preserves edit"
    expected: "Choose Cancel; the window remains open and the unsaved canvas state is still available."
    why_human: "Requires visual/runtime canvas continuity verification."
  - test: "Save-close and reopen outcome"
    expected: "Choose Close saving; Saving current frame… appears; the window closes after save; reopen shows the saved current frame."
    why_human: "Requires live bridge/apply result and reopen verification."
  - test: "No stuck state"
    expected: "After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in blocked close/saving state and choices remain usable."
    why_human: "Requires runtime transition testing, especially failure/timeout paths."
---

# Phase 36.4: Physics Paint Roto Explicit Close Behavior Verification Report

**Phase Goal:** As a stop-motion animator, I want to close Physics Paint with an unsaved Roto frame by choosing close without saving, cancel, or close saving, so that I can choose between speed and preserving the current frame.
**Verified:** 2026-06-20T10:43:30Z
**Status:** human_needed
**Re-verification:** No — previous verification existed but had no `gaps:` section, so this was treated as initial verification with the current two completed plans.

## User Flow Coverage

User story: "As a stop-motion animator, I want to close Physics Paint with an unsaved Roto frame by choosing close without saving, cancel, or close saving, so that I can choose between speed and preserving the current frame."

| Step | Expected | Evidence in Codebase | Status |
|---|---|---|---|
| Open Physics Paint on a Roto frame | Existing Physics Paint Studio remains the entry point and tracks current Roto frame state | `app/src/components/physic-paint/PhysicsPaintStudio.tsx` keeps `workflowMode`, `currentFrame`, `dirtyRotoFramesRef`, cached Roto references, and Save current wiring | Present, behavior-unverified |
| Close a clean Roto frame | Window closes normally without prompt and without the prior Tauri permission rejection | `onCloseRequested` snapshots, checks `dirtyRotoFramesRef.current.has(currentFrame)`, and returns without `preventDefault()` for clean frames; `default.json` grants `core:window:allow-destroy` to `efx-physic-paint` | Present, behavior-unverified |
| Close a dirty Roto frame | Prompt appears with exactly three choices | Dialog renders `Close without saving`, `Cancel`, and `Close saving`; focused source test asserts label presence and uniqueness | Present, behavior-unverified |
| Choose speed | `Close without saving` closes without writing/deleting cache | `closeWithoutSavingRotoFrame` sets `closeGuardBypassRef.current = true` and calls `closePhysicsPaintWindow()` without save/apply/delete calls; focused source test asserts this | Present, behavior-unverified |
| Choose keep working | `Cancel` leaves window open and dirty canvas intact | `cancelRotoClose` clears prompt state only and does not close, clear dirty refs, delete current frame, or clear engine; focused source test asserts this | Present, behavior-unverified |
| Choose preserving current frame | `Close saving` shows saving feedback, saves via existing path, and closes after matching success | `saveAndCloseRotoFrame` sets `Saving current frame…`, calls `saveRotoFrame(null)`, records operation id; `handleApplyResult` closes only on matching successful result | Present, behavior-unverified |
| Outcome | Animator can choose between speed and preserving current frame | Code paths for discard, cancel, save-close, capability permission, and recovery exist and focused tests pass; live close/reopen UAT remains required | Present, behavior-unverified |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | User can close Physics Paint from a clean current Roto frame without seeing a prompt. | Present, behavior-unverified | `app/src/components/physic-paint/PhysicsPaintStudio.tsx:1996-2004` returns without `preventDefault()` when the current Roto frame is clean; no runtime native close test proves the window closes. |
| 2 | User closing a dirty current Roto frame sees exactly three choices: Close without saving, Cancel, and Close saving. | Present, behavior-unverified | `PhysicsPaintStudio.tsx:2334-2345` renders the dialog and three buttons; `PhysicsPaintStudio.test.ts:507-523` source-checks labels and uniqueness; no render-from-close behavioral test. |
| 3 | User can choose Close without saving and the dirty current frame is not written while any previously saved cached frame remains unchanged. | Present, behavior-unverified | `PhysicsPaintStudio.tsx:1952-1958` bypasses the guard and closes; `PhysicsPaintStudio.test.ts:525-535` asserts no save/apply/delete calls in that handler; persisted reopen outcome needs live UAT. |
| 4 | User can choose Cancel and remain in Physics Paint with the unsaved canvas state still available. | Present, behavior-unverified | `PhysicsPaintStudio.tsx:1960-1965` clears only prompt/close refs; `PhysicsPaintStudio.test.ts:538-547` asserts no close, dirty clear, or engine clear; canvas continuity needs live UAT. |
| 5 | User can choose Close saving, see saving feedback, and the window closes only after the existing Save current apply path completes successfully. | Present, behavior-unverified | `PhysicsPaintStudio.tsx:1967-1986` sets saving feedback and awaits `saveRotoFrame(null)`; `PhysicsPaintStudio.tsx:1321-1385` closes only after matching successful apply result; no runtime async bridge/window test. |
| 6 | User can reopen Physics Paint and verify the chosen saved or discarded outcome. | Present, behavior-unverified | Source shows discard sends no write/delete and save-close uses existing apply-canvas path, but reopen persistence requires live cache/window lifecycle. |
| 7 | After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in a blocked close/saving state. | Present, behavior-unverified | Error/timeout/cleanup reset assertions exist in `PhysicsPaintStudio.test.ts:567-582`; transition behavior is not invoked in runtime/component tests. |
| 8 | User can close Physics Paint from a clean current Roto frame without a Tauri permission rejection. | Present, behavior-unverified | `app/src-tauri/capabilities/default.json` includes window `efx-physic-paint` and permission `core:window:allow-destroy`; `PhysicsPaintStudio.test.ts:490-505` asserts the permission and Tauri API close/destroy relationship. Live UAT must prove the prior native rejection is gone. |
| 9 | The clean-close permission gap is covered by an automated regression assertion against the Tauri capability file. | Verified | Focused test `grants the Physics Paint window permissions required by clean close` reads `default.json` and passed in `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts`. |

**Score:** 1/9 truths behavior-verified (8 present, behavior-unverified)

### Deferred Items

No failed items were deferred to later roadmap phases. Later Phase 37 covers future integration validation, not the Phase 36.4 live close-choice UAT outcomes.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` | Regression coverage for close prompt, discard, cancel, save-close, clean close, reopen outcome, recovery, and capability permission | Verified | `verify.artifacts` passed for both plans; focused test command passed with 47 tests. Coverage is mostly source-inspection rather than runtime user-flow behavior. |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | Roto-only dirty close state machine, three-choice dialog, close bypass, and apply-result-driven close-after-save | Verified | State, refs, handlers, Tauri close hook, and dialog are present and substantive at lines 1952-2005 and 2334-2345. |
| `app/src/components/physic-paint/physicsPaintStudio.css` | Minimal close dialog and saving/error feedback styling | Verified | `verify.artifacts` passed; existing `physics-paint-confirmation` styling is used by the dialog. |
| `app/src-tauri/capabilities/default.json` | Tauri permission allowing the `efx-physic-paint` window close path to destroy/close the window | Verified | JSON contains `windows: ["main", "efx-physic-paint"]`, `core:window:allow-close`, and `core:window:allow-destroy`. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `PhysicsPaintStudio.tsx` | `@tauri-apps/api/window getCurrentWindow().onCloseRequested` | Native close request is prevented only for dirty current Roto frame | Verified | `verify.key-links` found `onCloseRequested`; manual inspection confirms dirty current-frame guard at `PhysicsPaintStudio.tsx:1996-2004`. |
| `PhysicsPaintStudio.tsx` | Existing `saveRotoFrame(null)` / `flushRotoFrame(currentFrame, { force: true, advanceToFrame: null })` path | Close saving reuses Save current | Verified | `saveAndCloseRotoFrame` calls `saveRotoFrame(null)` at `PhysicsPaintStudio.tsx:1972`; regression test checks existing save path. |
| `PhysicsPaintStudio.tsx` | `handleApplyResult(detail)` | Programmatic close happens only after matching successful apply result | Verified | `handleApplyResult` compares operation ids at `PhysicsPaintStudio.tsx:1321-1324` and closes at `PhysicsPaintStudio.tsx:1382-1385`. |
| `PhysicsPaintStudio.tsx` | `app/src-tauri/capabilities/default.json` | `closePhysicsPaintWindow` awaits `getCurrentWindow().close()`, authorized by `core:window:allow-destroy` | Verified | `closePhysicsPaintWindow` awaits `appWindow.close()` at `PhysicsPaintStudio.tsx:1163-1169`; capability file contains `core:window:allow-destroy`. |
| `PhysicsPaintStudio.test.ts` | `app/src-tauri/capabilities/default.json` | Test reads capability JSON and asserts close/destroy permissions for `efx-physic-paint` | Verified | `PhysicsPaintStudio.test.ts:490-505` reads and asserts `core:window:allow-close` and `core:window:allow-destroy`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `PhysicsPaintStudio.tsx` | `rotoClosePromptState`, `rotoClosePromptMessage` | Native `onCloseRequested`, discard/cancel/save-close handlers, apply result/timeout handlers | Yes, state flows to rendered dialog/buttons/status copy | Flowing |
| `PhysicsPaintStudio.tsx` | `dirtyRotoFramesRef.current.has(currentFrame)` | `snapshotCurrentRotoFrame`, edit tracking, Save current clearing | Yes, used to decide prompt vs clean close | Flowing |
| `PhysicsPaintStudio.tsx` | `closeAfterApplyOperationIdRef` | `saveAndCloseRotoFrame` receives existing save payload operation id; `handleApplyResult` consumes matching result | Yes, gates programmatic close after save success | Flowing |
| `default.json` | `windows`, `permissions` | Tauri capability config read by runtime policy; regression test reads same file | Yes, includes `efx-physic-paint`, `core:window:allow-close`, and `core:window:allow-destroy` | Flowing |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Focused studio regression tests | `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts` | 1 test file passed, 47 tests passed | Pass |
| MVP live user flow | Not run; project instruction says not to run the dev server/Tauri app | Current UAT file remains `status: diagnosed`, Test 1 previously failed with `window.destroy not allowed`, Tests 2-6 pending until the user reruns the app | Skipped — human required |

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| None declared for this phase | N/A | No `probe-*.sh` declared in PLAN/SUMMARY for this UI phase | Skipped |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| PH36.4-CLEAN-CLOSE | 36.4-01-PLAN.md, 36.4-02-PLAN.md | Closing a clean Roto frame closes without prompting and without the Tauri close/destroy permission rejection | Present, behavior-unverified | Source branch allows clean close; capability file now grants destroy; focused test passes; live UAT rerun still required. |
| PH36.4-DIRTY-CHOICES | 36.4-01-PLAN.md, 36.4-02-PLAN.md | Dirty current Roto close presents exactly three choices | Present, behavior-unverified | Dialog labels exist exactly once in prompt block; focused source test passes; live prompt confirmation still pending. |
| PH36.4-DISCARD | 36.4-01-PLAN.md, 36.4-02-PLAN.md | Close without saving leaves previous cache unchanged | Present, behavior-unverified | Discard handler sends no save/apply/delete payloads; live reopen confirmation still pending. |
| PH36.4-CANCEL | 36.4-01-PLAN.md, 36.4-02-PLAN.md | Cancel keeps window open and dirty canvas intact | Present, behavior-unverified | Cancel handler does not close or clear dirty/canvas state; live confirmation still pending. |
| PH36.4-SAVE-CLOSE | 36.4-01-PLAN.md, 36.4-02-PLAN.md | Close saving uses existing Save current path, shows feedback, and closes after completion | Present, behavior-unverified | Save-close code uses `saveRotoFrame(null)`, `Saving current frame…`, and matching apply-result close; live bridge/apply/reopen confirmation still pending. |

Requirement IDs `PH36.4-CLEAN-CLOSE`, `PH36.4-DIRTY-CHOICES`, `PH36.4-DISCARD`, `PH36.4-CANCEL`, and `PH36.4-SAVE-CLOSE` are absent from `/Users/lmarques/Dev/efx-motion-editor/.planning/REQUIREMENTS.md`. Per the verification request, they are accounted for as roadmap-local/plan-local IDs rather than invented REQUIREMENTS.md entries.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | N/A | `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder implementation | None | No blocker debt markers found in the verified implementation file. |
| `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` | N/A | `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder implementation | None | No blocker debt markers found in the verified test file. |
| `app/src-tauri/capabilities/default.json` | N/A | Empty/static stub indicators | None | Capability is real JSON config with the required window and permissions. |

### Human Verification Required

#### 1. Clean close

**Test:** Open Physics Paint on a clean Roto frame and close the window.
**Expected:** It closes without a prompt and without `Unhandled Promise Rejection: window.destroy not allowed`.
**Why human:** Requires native close behavior in the running Tauri/app window. The current UAT file still records Test 1 as diagnosed from the previous permission failure.

#### 2. Dirty close choices

**Test:** Dirty the current Roto frame and close the window.
**Expected:** Exactly `Close without saving`, `Cancel`, and `Close saving` are offered.
**Why human:** Requires a real close gesture and rendered dialog confirmation.

#### 3. Discard and reopen outcome

**Test:** Choose `Close without saving`, reopen Physics Paint on the same frame.
**Expected:** The previous cached reference is unchanged.
**Why human:** Requires live cache/window lifecycle verification.

#### 4. Cancel preserves edit

**Test:** Choose `Cancel` after dirty-close prompt.
**Expected:** The window remains open and the unsaved canvas state is still available.
**Why human:** Requires visual/runtime canvas continuity verification.

#### 5. Save-close and reopen outcome

**Test:** Choose `Close saving` after dirtying the current Roto frame.
**Expected:** `Saving current frame…` appears, the window closes after save, and reopen shows the saved current frame.
**Why human:** Requires live bridge/apply result and reopen verification.

#### 6. No stuck state

**Test:** Exercise clean close, discard, cancel, save success, save error, and timeout paths as available.
**Expected:** The module is not stuck in a blocked close/saving state and choices remain usable.
**Why human:** Requires runtime transition testing, especially failure/timeout paths.

### Gaps Summary

No automated codebase blocker gaps were found: the planned artifacts exist, are substantive, wired, include the Tauri `core:window:allow-destroy` capability fix, and the focused regression test passes. The phase remains `human_needed` rather than `passed` because eight of nine must-haves assert native/runtime user-flow behavior. The current UAT file still has Test 1 diagnosed from the previous permission rejection and Tests 2-6 pending until the user reruns the app.

---

_Verified: 2026-06-20T10:43:30Z_
_Verifier: Claude (gsd-verifier)_
