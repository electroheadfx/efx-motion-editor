---
phase: 36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph
verified: 2026-06-20T08:02:16Z
status: human_needed
score: 0/7 must-haves verified
behavior_unverified: 7
overrides_applied: 0
behavior_unverified_items:
  - truth: "User can close Physics Paint from a clean current Roto frame without seeing a prompt."
    test: "Open Physics Paint on a clean Roto frame and close the window."
    expected: "The window closes normally without rendering the unsaved Roto prompt."
    why_human: "Code and source-inspection tests show the clean branch returns without preventDefault, but the native Tauri close event/runtime window behavior is not exercised by the automated test."
  - truth: "User closing a dirty current Roto frame sees exactly three choices: Close without saving, Cancel, and Close saving."
    test: "Dirty the current Roto frame, close the Physics Paint window, and inspect the prompt."
    expected: "A dialog appears with exactly Close without saving, Cancel, and Close saving."
    why_human: "The prompt JSX and labels exist and are source-tested, but no component/runtime test renders the dialog from a real close gesture."
  - truth: "User can choose Close without saving and the dirty current frame is not written while any previously saved cached frame remains unchanged."
    test: "Dirty a current Roto frame that already has a saved cache, choose Close without saving, reopen Physics Paint."
    expected: "The prior cached reference is unchanged and no dirty edit is saved."
    why_human: "Source evidence shows discard bypasses close and does not call save/apply/delete paths, but persisted reopen outcome is a live app behavior."
  - truth: "User can choose Cancel and remain in Physics Paint with the unsaved canvas state still available."
    test: "Dirty the current Roto frame, close the window, choose Cancel, then continue editing."
    expected: "The window remains open and the dirty canvas state remains visible/editable."
    why_human: "Source evidence shows cancel only clears prompt state, but canvas continuity after a real native close request is not exercised."
  - truth: "User can choose Close saving, see saving feedback, and the window closes only after the existing Save current apply path completes successfully."
    test: "Dirty the current Roto frame, choose Close saving, observe saving feedback and close timing."
    expected: "Saving current frame… appears; the existing save path runs; the window closes only after the matching successful apply result."
    why_human: "Code tracks operation ids and closes from handleApplyResult, and the focused source test passes, but no runtime test drives the async Tauri/apply handshake."
  - truth: "User can reopen Physics Paint and verify the chosen saved or discarded outcome."
    test: "After discard and after save-close, reopen Physics Paint on the same frame."
    expected: "Discard shows the previous cache unchanged; save-close shows the newly saved current frame."
    why_human: "Reopen persistence relies on the running app, cache store, bridge, and window lifecycle; automated evidence is source-level only."
  - truth: "After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in a blocked close/saving state."
    test: "Exercise each close path, including a save error or timeout if possible, then attempt another close choice."
    expected: "The prompt/actions remain usable; no stale saving state blocks subsequent choices."
    why_human: "Source evidence resets refs/state on error, timeout, cleanup, and success, but no behavioral test invokes those transitions."
human_verification:
  - test: "Clean close"
    expected: "Open Physics Paint on a clean Roto frame and close the window; it closes without a prompt."
    why_human: "Requires native close behavior in the running Tauri/app window."
  - test: "Dirty close choices"
    expected: "Dirty the current Roto frame and close the window; exactly Close without saving, Cancel, and Close saving are offered."
    why_human: "Requires real close gesture and rendered dialog confirmation."
  - test: "Discard and reopen outcome"
    expected: "Choose Close without saving; reopen Physics Paint; previous cached reference is unchanged."
    why_human: "Requires live cache/window lifecycle verification."
  - test: "Cancel preserves edit"
    expected: "Choose Cancel; the window remains open and the unsaved canvas state is still available."
    why_human: "Requires visual/runtime canvas continuity verification."
  - test: "Save-close and reopen outcome"
    expected: "Choose Close saving; Saving current frame… appears; the window closes after save; reopen shows the saved current frame."
    why_human: "Requires live bridge/apply result and reopen verification."
  - test: "No stuck state"
    expected: "After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in blocked close/saving state."
    why_human: "Requires runtime transition testing, especially failure/timeout paths."
---

# Phase 36.4: Physics Paint Roto Explicit Close Behavior Verification Report

**Phase Goal:** As a stop-motion animator, I want to close Physics Paint with an unsaved Roto frame by choosing close without saving, cancel, or close saving, so that I can choose between speed and preserving the current frame.
**Verified:** 2026-06-20T08:02:16Z
**Status:** human_needed
**Re-verification:** No — initial verification

## User Flow Coverage

User story: "As a stop-motion animator, I want to close Physics Paint with an unsaved Roto frame by choosing close without saving, cancel, or close saving, so that I can choose between speed and preserving the current frame."

| Step | Expected | Evidence in Codebase | Status |
|---|---|---|---|
| Open Physics Paint on a Roto frame | Existing Physics Paint Studio remains the entry point and tracks current Roto frame state | `PhysicsPaintStudio.tsx` keeps `workflowMode`, `currentFrame`, `dirtyRotoFramesRef`, cached Roto references, and Save current wiring | Present, behavior-unverified |
| Close a clean Roto frame | Window closes normally without prompt | `onCloseRequested` snapshots, checks `dirtyRotoFramesRef.current.has(currentFrame)`, and returns without `preventDefault()` for clean frames | Present, behavior-unverified |
| Close a dirty Roto frame | Prompt appears with exactly three choices | Dialog renders `Close without saving`, `Cancel`, and `Close saving`; focused test asserts label presence and uniqueness | Present, behavior-unverified |
| Choose speed | `Close without saving` closes without writing/deleting cache | `closeWithoutSavingRotoFrame` sets `closeGuardBypassRef.current = true` and calls `closePhysicsPaintWindow()` without save/apply/delete calls; focused test asserts this | Present, behavior-unverified |
| Choose keep working | `Cancel` leaves window open and dirty canvas intact | `cancelRotoClose` clears prompt state only and does not close, clear dirty refs, delete current frame, or clear engine; focused test asserts this | Present, behavior-unverified |
| Choose preserving current frame | `Close saving` shows saving feedback, saves via existing path, and closes after matching success | `saveAndCloseRotoFrame` sets `Saving current frame…`, calls `saveRotoFrame(null)`, records operation id; `handleApplyResult` closes only on matching successful result | Present, behavior-unverified |
| Outcome | Animator can choose between speed and preserving current frame | Code paths for discard, cancel, save-close, and recovery exist and focused tests pass; live close/reopen UAT remains required | Present, behavior-unverified |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | User can close Physics Paint from a clean current Roto frame without seeing a prompt. | Present, behavior-unverified | `onCloseRequested` snapshots then returns when current Roto frame is not dirty; no runtime native close test. |
| 2 | User closing a dirty current Roto frame sees exactly three choices: Close without saving, Cancel, and Close saving. | Present, behavior-unverified | Dialog JSX and focused source test verify exact labels; no runtime render-from-close test. |
| 3 | User can choose Close without saving and the dirty current frame is not written while any previously saved cached frame remains unchanged. | Present, behavior-unverified | Discard handler bypasses guard and closes without save/apply/delete calls; persisted reopen outcome needs live UAT. |
| 4 | User can choose Cancel and remain in Physics Paint with the unsaved canvas state still available. | Present, behavior-unverified | Cancel clears only prompt state; no close/window/canvas runtime test. |
| 5 | User can choose Close saving, see saving feedback, and the window closes only after the existing Save current apply path completes successfully. | Present, behavior-unverified | Save-close uses `saveRotoFrame(null)`, operation-id ref, and `handleApplyResult` close; async bridge/window behavior not exercised. |
| 6 | User can reopen Physics Paint and verify the chosen saved or discarded outcome. | Present, behavior-unverified | Source shows discard sends no write/delete and save-close reuses apply-canvas path; reopen outcome is live app behavior. |
| 7 | After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in a blocked close/saving state. | Present, behavior-unverified | Error/timeout/cleanup reset code exists and focused source tests pass; transition behavior not invoked. |

**Score:** 0/7 truths behavior-verified (7 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` | Regression coverage for close prompt, discard, cancel, save-close, clean close, reopen outcome, and recovery | Verified | `verify.artifacts` passed; focused test command passed with 46 tests. Coverage is source-inspection rather than runtime behavior. |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | Roto-only dirty close state machine, dialog, bypass, and apply-result-driven close-after-save | Verified | State, refs, handlers, Tauri close hook, and dialog are present and substantive. |
| `app/src/components/physic-paint/physicsPaintStudio.css` | Minimal close dialog and saving/error feedback styling | Verified | `.physics-paint-confirmation*`, `.physics-paint-roto-close-message`, and primary button styles exist. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `PhysicsPaintStudio.tsx` | `@tauri-apps/api/window getCurrentWindow().onCloseRequested` | Native close request is prevented only for dirty current Roto frame | Verified | `verify.key-links` found `onCloseRequested`; manual code inspection confirms dirty current-frame guard. |
| `PhysicsPaintStudio.tsx` | Existing `saveRotoFrame(null)` / `flushRotoFrame(currentFrame, { force: true, advanceToFrame: null })` path | Close saving reuses Save current | Verified | `saveAndCloseRotoFrame` calls `saveRotoFrame(null)`; `saveRotoFrame` snapshots and flushes current frame. |
| `PhysicsPaintStudio.tsx` | `handleApplyResult(detail)` | Programmatic close happens only after matching successful apply result | Verified | `handleApplyResult` compares `closeAfterApplyOperationIdRef.current === detail.operationId`; closes only in successful Roto apply branch. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `PhysicsPaintStudio.tsx` | `rotoClosePromptState`, `rotoClosePromptMessage` | Native `onCloseRequested`, discard/cancel/save-close handlers, apply result/timeout handlers | Yes, state flows to rendered dialog/buttons/status copy | Flowing |
| `PhysicsPaintStudio.tsx` | `dirtyRotoFramesRef.current.has(currentFrame)` | `snapshotCurrentRotoFrame`, `saveRotoFrame`, frame edit tracking | Yes, used to decide prompt vs clean close | Flowing |
| `PhysicsPaintStudio.tsx` | `closeAfterApplyOperationIdRef` | `saveAndCloseRotoFrame` receives existing save payload operation id; `handleApplyResult` consumes matching result | Yes, gates programmatic close after save success | Flowing |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Focused studio regression tests | `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts` | 46 tests passed | Pass |
| MVP live user flow | Not run; project instruction says not to run server/Tauri app | Requires user-owned live UAT | Skipped — human required |

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| None declared for this phase | N/A | No `probe-*.sh` declared in PLAN/SUMMARY for this UI phase | Skipped |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| PH36.4-CLEAN-CLOSE | 36.4-01-PLAN.md | Closing a clean Roto frame closes without prompting | Present, behavior-unverified | Code branch allows clean close; focused source test passes. |
| PH36.4-DIRTY-CHOICES | 36.4-01-PLAN.md | Dirty current Roto close presents exactly three choices | Present, behavior-unverified | Dialog labels exist exactly once in prompt block; focused source test passes. |
| PH36.4-DISCARD | 36.4-01-PLAN.md | Close without saving leaves previous cache unchanged | Present, behavior-unverified | Discard handler sends no save/apply/delete payloads; live reopen confirmation needed. |
| PH36.4-CANCEL | 36.4-01-PLAN.md | Cancel keeps window open and dirty canvas intact | Present, behavior-unverified | Cancel handler does not close or clear dirty/canvas state; live confirmation needed. |
| PH36.4-SAVE-CLOSE | 36.4-01-PLAN.md | Close saving uses existing Save current path, shows feedback, and closes after completion | Present, behavior-unverified | Save-close code uses `saveRotoFrame(null)`, `Saving current frame…`, and matching apply result close. |

Requirement IDs `PH36.4-CLEAN-CLOSE`, `PH36.4-DIRTY-CHOICES`, `PH36.4-DISCARD`, `PH36.4-CANCEL`, and `PH36.4-SAVE-CLOSE` are phase-local IDs and are absent from `/Users/lmarques/Dev/efx-motion-editor/.planning/REQUIREMENTS.md`. Per the verification request, this is informational only and does not fail behavior verification.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `PhysicsPaintStudio.tsx` and tests | Various | `return null` / `return []` grep matches | Info | Reviewed as normal guard/fallback returns, not phase stub behavior. |
| Phase files | N/A | `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder implementation | None | No blocker debt markers found in modified phase files. |

### Human Verification Required

#### 1. Clean close

**Test:** Open Physics Paint on a clean Roto frame and close the window.
**Expected:** It closes without a prompt.
**Why human:** Requires native close behavior in the running Tauri/app window.

#### 2. Dirty close choices

**Test:** Dirty the current Roto frame and close the window.
**Expected:** Exactly `Close without saving`, `Cancel`, and `Close saving` are offered.
**Why human:** Requires a real close gesture and rendered dialog confirmation.

#### 3. Discard and reopen outcome

**Test:** Choose `Close without saving`, reopen Physics Paint on the same frame.
**Expected:** The previous cached reference is unchanged.
**Why human:** Requires live cache/window lifecycle verification.

#### 4. Cancel preserves edit

**Test:** Choose `Cancel` after dirty-close prompt.
**Expected:** The window remains open and the unsaved canvas state is still available.
**Why human:** Requires visual/runtime canvas continuity verification.

#### 5. Save-close and reopen outcome

**Test:** Choose `Close saving` after dirtying the current Roto frame.
**Expected:** `Saving current frame…` appears, the window closes after save, and reopen shows the saved current frame.
**Why human:** Requires live bridge/apply result and reopen verification.

#### 6. No stuck state

**Test:** Exercise clean close, discard, cancel, save success, save error, and timeout paths as available.
**Expected:** The module is not stuck in a blocked close/saving state and choices remain usable.
**Why human:** Requires runtime transition testing, especially failure/timeout paths.

### Gaps Summary

No codebase blocker gaps were found: the planned artifacts exist, are substantive, wired, and the focused test passes. The phase remains `human_needed` because all seven must-haves are runtime/user-flow behaviors and current automated coverage is source-inspection rather than behavioral execution of the native close, async save-close, and reopen lifecycle.

---

_Verified: 2026-06-20T08:02:16Z_
_Verifier: Claude (gsd-verifier)_
