---
phase: 36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph
plan: 02
type: execute
wave: 2
depends_on:
  - 36.4-01
files_modified:
  - app/src-tauri/capabilities/default.json
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
gap_closure: true
requirements:
  - PH36.4-CLEAN-CLOSE
  - PH36.4-DIRTY-CHOICES
  - PH36.4-DISCARD
  - PH36.4-CANCEL
  - PH36.4-SAVE-CLOSE
must_haves:
  truths:
    - "User can close Physics Paint from a clean current Roto frame without a Tauri permission rejection."
    - "Dirty Roto close choices remain exactly Close without saving, Cancel, and Close saving after the capability fix."
    - "The clean-close permission gap is covered by an automated regression assertion against the Tauri capability file."
  artifacts:
    - path: "app/src-tauri/capabilities/default.json"
      provides: "Tauri capability permission allowing the efx-physic-paint window close path to destroy/close the window"
      contains: "core:window:allow-destroy"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Regression coverage that the Tauri capability grants the permission required by getCurrentWindow().close and that Phase 36.4 close behavior assertions still align"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src-tauri/capabilities/default.json"
      via: "closePhysicsPaintWindow awaits getCurrentWindow().close(), which Tauri authorizes through core:window:allow-destroy"
      pattern: "core:window:allow-destroy"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      to: "app/src-tauri/capabilities/default.json"
      via: "test reads the capability JSON and asserts the permission exists alongside the efx-physic-paint window"
      pattern: "allow-destroy"
---

<objective>
Close the Phase 36.4 UAT blocker where clean Physics Paint close rejects with `window.destroy not allowed` because the efx-physic-paint Tauri window lacks `core:window:allow-destroy`.

Purpose: Phase 36.4 Plan 01 implemented the clean/dirty close behavior in `PhysicsPaintStudio.tsx`, but live UAT proved the clean close path is blocked by Tauri capability policy before the user can proceed to dirty close verification.
Output: A small Tauri capability update and regression test coverage proving clean close has the required window permission while the existing dirty-close tests still validate the three-choice workflow.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-UAT.md
@.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-01-SUMMARY.md
@app/src-tauri/capabilities/default.json
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add capability regression for Physics Paint window close permission</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-UAT.md gap root cause and missing permission.
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts existing source-inspection pattern using `readFileSync` and `fileURLToPath`.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx `closePhysicsPaintWindow` implementation using `getCurrentWindow().close()`.
    - app/src-tauri/capabilities/default.json current `windows` and `permissions` arrays.
  </read_first>
  <behavior>
    - Test that `PhysicsPaintStudio.tsx` still closes through `windowApi.getCurrentWindow()` and awaits `appWindow.close()`.
    - Test that `app/src-tauri/capabilities/default.json` includes `efx-physic-paint` in `windows` and includes `core:window:allow-destroy` in `permissions`.
    - Keep the existing dirty close tests for `Close without saving`, `Cancel`, `Close saving`, save-close operation-id matching, and unstuck recovery unchanged except for import/path setup needed by the new capability assertion.
  </behavior>
  <action>Add a `defaultCapabilityPath` constant beside the existing source path constants in `PhysicsPaintStudio.test.ts`, pointing from the test file to `../../../../src-tauri/capabilities/default.json`. Add a focused regression test in the Phase 36.4 close-behavior describe block or a nearby Physics Paint Tauri capability describe block. The test must parse the capability JSON, assert `windows` contains `efx-physic-paint`, assert `permissions` contains `core:window:allow-destroy`, and assert the studio source still uses `windowApi.getCurrentWindow()` plus `await appWindow.close()`. Run the focused test before editing the capability file and record the RED failure caused by the missing permission. Do not alter the close workflow implementation in `PhysicsPaintStudio.tsx` for this task.</action>
  <verify>
    <automated>pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The focused test fails before the capability edit because `core:window:allow-destroy` is absent.
    - The failure is a clean assertion failure, not a path/import/syntax failure.
    - Existing Phase 36.4 dirty close assertions remain present and still check the three exact choices.
  </acceptance_criteria>
  <done>Automated coverage now captures the diagnosed clean-close Tauri permission gap before the config fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grant the Tauri destroy permission to the Physics Paint capability</name>
  <files>app/src-tauri/capabilities/default.json</files>
  <read_first>
    - app/src-tauri/capabilities/default.json current `main-capability` permissions list.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx `closePhysicsPaintWindow` fallback behavior.
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts RED capability assertion from Task 1.
  </read_first>
  <behavior>
    - The existing `main-capability` continues to cover both `main` and `efx-physic-paint` windows.
    - The permissions list grants `core:window:allow-destroy` alongside the existing window permissions.
    - No unrelated capability, filesystem scope, dialog permission, app config, or close workflow code changes are introduced.
  </behavior>
  <action>Edit only `app/src-tauri/capabilities/default.json` to add `core:window:allow-destroy` to the existing `permissions` array near the other `core:window:*` entries. Preserve valid JSON formatting and keep the `windows` array unchanged with both `main` and `efx-physic-paint`. Do not add broad capability scopes, do not rename the capability, and do not change `PhysicsPaintStudio.tsx`; the implementation already calls the correct Tauri close API and the gap is authorization.</action>
  <verify>
    <automated>pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `default.json` contains `core:window:allow-destroy` exactly once in `permissions`.
    - The focused PhysicsPaintStudio test passes, including existing clean close and dirty close assertions.
    - Typecheck passes.
    - No source implementation files are modified for this permission-only fix.
  </acceptance_criteria>
  <done>The efx-physic-paint Tauri window is authorized for the clean close path that Phase 36.4 already implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the UAT gap is ready for rerun</name>
  <files>app/src-tauri/capabilities/default.json, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-UAT.md Test 1 failure and pending Tests 2-6.
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-01-SUMMARY.md user-run UAT checklist.
  </read_first>
  <action>Run the focused PhysicsPaintStudio regression test and typecheck after the capability edit. If failures occur, fix only issues directly caused by the two planned files. In the plan summary, state that the executor did not run the dev server or Tauri app and list the exact user rerun sequence: clean close should close without the `window.destroy not allowed` rejection; then rerun dirty close choices, discard/reopen, cancel, save-close/reopen, and no-stuck-state UAT from the existing Phase 36.4 checklist. Do not start the server or app.</action>
  <verify>
    <automated>pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests and typecheck pass using existing pnpm/Vitest setup.
    - The summary includes the original UAT error text as resolved by capability authorization, not by close workflow code changes.
    - The summary includes manual rerun steps for Tests 1-6 from `36.4-UAT.md`.
    - The executor has not run the dev server or Tauri app.
  </acceptance_criteria>
  <done>Phase 36.4 UAT can be rerun from clean close through dirty close outcomes with the permission blocker removed.</done>
</task>

</tasks>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 36.4 | Animator can choose close behavior for unsaved Roto frames | 36.4-02 | COVERED | This gap plan unblocks the clean close entry point so dirty choices can be UAT-tested. |
| REQ | PH36.4-CLEAN-CLOSE | Clean Roto close closes without prompt or rejection | 36.4-02 | COVERED | Tasks 1-2 add permission assertion and capability grant. |
| REQ | PH36.4-DIRTY-CHOICES | Dirty close choices remain exact | 36.4-02 | COVERED | Tasks preserve and rerun existing dirty choice tests. |
| REQ | PH36.4-DISCARD | Close without saving preserves prior cache | 36.4-02 | COVERED | Task 3 reruns existing discard coverage and UAT instructions; no discard implementation change. |
| REQ | PH36.4-CANCEL | Cancel keeps window open and edit intact | 36.4-02 | COVERED | Task 3 reruns existing cancel coverage and UAT instructions; no cancel implementation change. |
| REQ | PH36.4-SAVE-CLOSE | Close saving saves then closes | 36.4-02 | COVERED | Task 3 reruns existing save-close coverage and UAT instructions; no save-close implementation change. |
| RESEARCH | UAT diagnosis | Missing `core:window:allow-destroy` blocks `getCurrentWindow().close()` | 36.4-02 | COVERED | Tasks 1-2 directly assert and add the permission. |
| CONTEXT | 36.4-CONTEXT | No 36.4 CONTEXT.md exists | 36.4-02 | COVERED | Uses UAT diagnosis, roadmap requirements, and Plan 01 summary; no locked D-XX decisions exist for this phase. |
| UAT | Test 1 | Clean close rejected with `window.destroy not allowed` | 36.4-02 | COVERED | Capability grant removes the diagnosed blocker. |
| UAT | Tests 2-6 | Dirty choices and outcome tests pending behind clean-close blocker | 36.4-02 | COVERED | Task 3 requires rerun instructions and existing automated assertions remain green. |

Excluded as out of scope: redesigning the close workflow, changing dirty-state semantics, adding broader Tauri scopes, running the dev server/app, Play close behavior, cache format changes, interpolation, generated frames, key utilities, and UI layout polish.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physics Paint window code → Tauri window capability policy | `getCurrentWindow().close()` crosses from frontend code into Tauri runtime authorization. |
| User close gesture → destructive window lifecycle | Closing the standalone window may discard unsaved in-memory edits when the user chooses discard or clean close. |
| Test suite → Tauri capability JSON | Regression test reads local capability configuration to prevent permission drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.4-GC-01 | Denial of Service | `app/src-tauri/capabilities/default.json` | mitigate | Add `core:window:allow-destroy` so authorized clean close is not blocked by Tauri policy. |
| T-36.4-GC-02 | Tampering | Tauri capability scope | mitigate | Add only the narrow required window destroy permission to the existing capability; do not expand filesystem/dialog scopes. |
| T-36.4-GC-03 | Repudiation | Permission regression coverage | mitigate | Add a test that asserts the efx-physic-paint window and required permission coexist in the capability file. |
| T-36.4-GC-04 | Tampering | Dirty close behavior | mitigate | Rerun existing Phase 36.4 tests to ensure the permission fix does not alter discard/cancel/save-close semantics. |
| T-36.4-GC-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; if an executor adds one, stop and require package legitimacy audit before continuing. |
</threat_model>

<verification>
- Focused regression: `pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts`.
- Typecheck: `pnpm --dir app typecheck`.
- User-run UAT after execution: rerun Test 1 clean close and confirm the `window.destroy not allowed` rejection is gone, then continue Tests 2-6 from `36.4-UAT.md`.
</verification>

<success_criteria>
- `app/src-tauri/capabilities/default.json` grants `core:window:allow-destroy` to the capability that covers `efx-physic-paint`.
- Automated coverage fails before and passes after the capability fix for the diagnosed permission gap.
- Existing dirty close tests for exact choices, discard, cancel, save-close, and stuck-state recovery remain green.
- No implementation code changes are made beyond the capability and its regression assertion unless a directly related test-path correction is required.
</success_criteria>

<output>
Create `.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-02-SUMMARY.md` when done.
</output>
