---
phase: 36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph
plan: 02
subsystem: tauri
status: complete
tags: [physics-paint, roto, close-workflow, tauri, tdd]

requires:
  - phase: 36.4-01
    provides: Explicit Roto close behavior and dirty close choices
provides:
  - Tauri capability permission for clean Physics Paint window close
  - Regression coverage for the Physics Paint close permission
  - Manual UAT rerun instructions for Phase 36.4 Tests 1-6
affects: [physics-paint, tauri-capabilities, roto-close]

tech-stack:
  added: []
  patterns:
    - Source-inspection test coverage for Tauri capability drift

key-files:
  created: []
  modified:
    - app/src-tauri/capabilities/default.json
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts

key-decisions:
  - "The clean close blocker is resolved by granting the narrow Tauri window destroy permission, not by changing PhysicsPaintStudio close workflow code."
  - "The existing main-capability continues to cover both main and efx-physic-paint windows."

patterns-established:
  - "PhysicsPaintStudio tests now assert that the close implementation and Tauri capability permission stay aligned."

requirements-completed:
  - PH36.4-CLEAN-CLOSE
  - PH36.4-DIRTY-CHOICES
  - PH36.4-DISCARD
  - PH36.4-CANCEL
  - PH36.4-SAVE-CLOSE

duration: 1 min
completed: 2026-06-20
---

# Phase 36.4 Plan 02: Clean Close Capability Gap Summary

**Physics Paint clean close is now authorized by Tauri capability policy while the existing dirty close workflow remains unchanged.**

## Performance

- **Duration:** 1 min
- **Started:** 2026-06-20T08:30:37Z
- **Completed:** 2026-06-20T08:31:25Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Added a RED regression test that reads `app/src-tauri/capabilities/default.json` and asserts the `efx-physic-paint` window has `core:window:allow-destroy`.
- Confirmed the RED failure was the diagnosed UAT blocker: `expected [ 'core:default', … ] to include 'core:window:allow-destroy'`.
- Added `core:window:allow-destroy` exactly once near the existing `core:window:*` permissions.
- Re-ran the focused PhysicsPaintStudio suite and typecheck after the capability fix.
- Did not modify `PhysicsPaintStudio.tsx`; the close workflow still uses `windowApi.getCurrentWindow()` and `await appWindow.close()`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add capability regression for Physics Paint window close permission** - `4c6b9c59` (test)
2. **Task 2: Grant the Tauri destroy permission to the Physics Paint capability** - `bd784fca` (fix)
3. **Task 3: Verify the UAT gap is ready for rerun** - automated verification completed before this summary

**Plan metadata:** this summary commit

## Files Created/Modified

- `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` - Added capability regression coverage for the clean close permission.
- `app/src-tauri/capabilities/default.json` - Granted `core:window:allow-destroy` to the existing capability that covers `efx-physic-paint`.

## Decisions Made

- The original UAT error, `Unhandled Promise Rejection: window.destroy not allowed. Permissions associated with this command: core:window:allow-destroy`, is resolved by capability authorization.
- No broader filesystem, dialog, app, or capability scope was added.
- No close workflow implementation code changed for this permission-only fix.

## Deviations from Plan

- The plan text suggested `../../../../src-tauri/capabilities/default.json` from the test file, but the correct relative path from `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` is `../../../src-tauri/capabilities/default.json`. The path was corrected so the RED failure was the missing permission rather than a file lookup error.

**Total deviations:** 1 auto-fixed path correction. **Impact:** Test coverage points to the intended capability file.

## Issues Encountered

None.

## Verification

- `pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts` — RED failed before the capability edit on missing `core:window:allow-destroy`.
- `pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts` — passed after the capability edit, 47 tests.
- `pnpm --dir app typecheck` — passed.
- `node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('app/src-tauri/capabilities/default.json','utf8')); console.log({windows:j.windows, allowDestroyCount:j.permissions.filter(p=>p==='core:window:allow-destroy').length});"` — reported `{ windows: [ 'main', 'efx-physic-paint' ], allowDestroyCount: 1 }`.

## User-Run UAT Required

The executor did not run the dev server or Tauri app.

Please rerun the existing Phase 36.4 UAT sequence:

1. **Clean close:** open Physics Paint on a clean Roto frame and close the window; confirm it closes without a prompt and without the `window.destroy not allowed` rejection.
2. **Dirty close choices:** dirty the current Roto frame and close the window; confirm exactly `Close without saving`, `Cancel`, and `Close saving` are offered.
3. **Discard and reopen outcome:** choose `Close without saving`; reopen Physics Paint on the same frame; confirm the previous cached reference is unchanged.
4. **Cancel preserves edit:** choose `Cancel`; confirm the window remains open and the unsaved canvas state is still available.
5. **Save-close and reopen outcome:** choose `Close saving`; confirm `Saving current frame…` appears, the window closes after save, and reopening shows the saved current frame.
6. **No stuck state:** after clean close, discard, cancel, save success, save error, or timeout, confirm the module is not stuck in blocked close/saving state and choices remain usable.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 36.4 automated gap-closure gates are green. Live close/reopen UAT should be rerun before marking the phase fully complete.

## Self-Check: PASSED

- All tasks from `36.4-02-PLAN.md` completed.
- `app/src-tauri/capabilities/default.json` grants `core:window:allow-destroy` exactly once to the capability covering `efx-physic-paint`.
- Focused tests and typecheck pass.
- No dev server or Tauri app was run.

---
*Phase: 36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph*
*Completed: 2026-06-20*
