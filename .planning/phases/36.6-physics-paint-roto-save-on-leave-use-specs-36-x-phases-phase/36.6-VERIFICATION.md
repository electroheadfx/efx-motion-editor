---
phase: 36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase
verified: 2026-06-20T20:33:42Z
status: human_needed
score: 0/7 must-haves verified
behavior_unverified: 7
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 0/7
  gaps_closed:
    - "Phase requirement IDs 36.6-AC-01, 36.6-AC-02, 36.6-AC-03, 36.6-AC-04, 36.6-AC-05, 36.6-AC-06, and 36.6-FB-01 are now accounted for in .planning/REQUIREMENTS.md."
  gaps_remaining: []
  regressions: []
behavior_unverified_items:
  - truth: "36.6-AC-01: Navigating away from a clean Roto frame opens the destination immediately."
    test: "In Roto mode on a clean frame, click another Roto frame or use an existing Roto navigation control."
    expected: "Destination opens immediately without invoking Save current/cache save."
    why_human: "Code and source-contract tests show the clean branch, but no runtime UI test exercises the actual click/control-to-frame transition."
  - truth: "36.6-AC-02: Navigating away from a dirty Roto frame saves the source frame first and opens the destination only after matching save success."
    test: "Paint on a Roto source frame, click another Roto frame, and wait for apply success."
    expected: "The source remains active while saving, then the requested destination opens after the matching apply result."
    why_human: "The coordinator/apply-result code is present and source-contract tests pass, but the end-to-end async UI/apply transition is not exercised by a runtime test."
  - truth: "36.6-AC-03: The saved source frame appears in the correct cache/timeline position."
    test: "After save-on-leave succeeds, inspect the source Roto cell/cache marker."
    expected: "The source frame is marked saved/cached at the original source frame, not the destination."
    why_human: "The success branch updates cache/saved-frame state for detail.startFrame, but visual timeline/cache placement needs runtime confirmation."
  - truth: "36.6-AC-04: Returning to the source frame shows the saved cached result."
    test: "After save-on-leave succeeds and destination opens, navigate back to the source frame."
    expected: "The saved cached/reference result for the source frame is displayed."
    why_human: "Frame-open helpers load editable/cached reference state, but no runtime navigation test proves the visible restored result."
  - truth: "36.6-AC-05: Save-on-leave feedback makes the wait visible without adding modal, toast, tutorial, or extra navigation controls."
    test: "Trigger save-on-leave and observe the workflow strip during the wait."
    expected: "The source cell appears pending/saving and compact status shows `Saving frame N…`; no modal, toast, tutorial, or new navigation controls appear."
    why_human: "String/helper/prop wiring is present and tested, but perceived UI feedback requires visual/UAT confirmation."
  - truth: "36.6-AC-06: Rapid frame changes during one dirty source save keep only the latest requested destination without corrupting source or destination state."
    test: "Paint a source frame, click one destination, then click a different destination while saving is still active."
    expected: "Only one source save runs; the latest clicked destination opens after success."
    why_human: "Queueing code and source-contract tests are present, but no runtime async rapid-click test exercises the whole active interval."
  - truth: "36.6-FB-01: Failed save-on-leave stays on the dirty source frame, preserves unsaved state, clears queued destination, and does not switch frames."
    test: "Force/observe a failed save-on-leave apply and inspect current frame, dirty state, and destination queue behavior."
    expected: "The source frame remains selected and dirty with previous cached output preserved; another explicit navigation can retry."
    why_human: "Failure code paths are present and source-contract tested, but no runtime failure injection test exercises the actual UI state transition."
human_verification:
  - test: "Dirty source save-on-leave and return-to-source UAT"
    expected: "Paint a Roto frame, click another existing Roto frame without pressing Save current, confirm the source saves before destination opens, then return to source and verify the paint remains."
    why_human: "Requires real editor/standalone bridge timing and visual confirmation; source checks cannot prove user-visible frame transition."
  - test: "Source-specific saving feedback and rapid latest-destination UAT"
    expected: "Paint a dirty Roto source frame, click another frame, confirm source cell pending/saving and compact line reads `Saving frame N…`; click a different destination during saving and confirm the latest destination opens after save."
    why_human: "Requires observing runtime UI feedback and rapid click behavior during the async save interval."
  - test: "Failure retention UAT"
    expected: "Simulate or observe a failed save-on-leave result and confirm the editor stays on the dirty source frame, preserves unsaved state, clears queued destination, and allows retry by explicit navigation."
    why_human: "Requires runtime failure injection or bridge failure observation; static checks cannot prove visible state retention after an actual failed apply."
---

# Phase 36.6: Physics Paint Roto Save On Leave Verification Report

**Phase Goal:** As a stop-motion animator, I want to move between Roto frames without manually pressing Save current every time, so that I can paint multiple frames without manually saving each one.
**Source of Truth:** `SPECS/36.x-phases/phase-36.6-save-on-leave/spec-36.6-save-on-leave.md`
**Verified:** 2026-06-20T20:33:42Z
**Status:** human_needed
**Re-verification:** Yes — after requirements traceability update

## Goal Achievement

Implementation evidence supports every Phase 36.6 requirement ID, and the prior traceability gap is closed. The phase is not `passed` only because this is MVP-mode user-story behavior: the remaining proof is runtime UAT of the actual Roto frame navigation, save timing, visible feedback, rapid-click interval, and failure retention in the editor/standalone bridge.

## User Flow Coverage

| Step | Expected | Evidence in codebase | Status |
| --- | --- | --- | --- |
| Clean Roto frame navigation | Clicking another frame opens it immediately without cache save. | `requestRotoFrameNavigation` validates the target, snapshots current frame, checks `dirtyRotoFramesRef.current.has(sourceFrame)`, and returns `navigateToSyncedFrame(targetFrame)` for clean frames. Source-contract test covers this branch in `PhysicsPaintStudio.test.ts`. | PRESENT_BEHAVIOR_UNVERIFIED |
| Dirty Roto frame navigation | Source frame saves once before destination opens. | Dirty branch sets `saveOnLeaveSourceFrameRef`, `rotoSavingFrame`, and `pendingRotoAdvanceRef`, then calls `flushRotoFrame(sourceFrame, { force: true, advanceToFrame: targetFrame })`. Matching success consumes `pendingRotoAdvanceRef` and calls `openSyncedRotoFrameAfterSave(nextFrame)`. | PRESENT_BEHAVIOR_UNVERIFIED |
| Saved source appears in cache/timeline | Saved/cached marker belongs to source frame. | Apply success uses `detail.startFrame` as `frame`, upserts cached Roto output, removes dirty state, and updates `setSavedRotoFrames` for that source frame. | PRESENT_BEHAVIOR_UNVERIFIED |
| Returning to source shows saved result | Source can be reopened from cached/editable reference. | `navigateToSyncedFrame` and `openSyncedRotoFrameAfterSave` load local editable state when present or reset/load cached reference state for the requested frame. | PRESENT_BEHAVIOR_UNVERIFIED |
| Save wait is visible | User sees source-specific saving feedback without extra modal/toast/tutorial/navigation controls. | Studio passes `rotoSavingFrame` to `PhysicsPaintWorkflowStrip`; strip calls `getRotoPendingLabel(..., props.rotoSavingFrame)` and renders `Dirty frames save when leaving.`; helper returns `Saving frame N…` for valid source frames. | PRESENT_BEHAVIOR_UNVERIFIED |
| Rapid clicks during saving | Latest requested destination wins; no duplicate source save. | During active save-on-leave, `requestRotoFrameNavigation` updates `pendingRotoAdvanceRef.current = targetFrame` and returns without starting another flush; Plan 03 regression covers the stale apply-status interval. | PRESENT_BEHAVIOR_UNVERIFIED |
| Failed save-on-leave | Stay on dirty source, clear queued destination, preserve cached output. | Failure and timeout branches clear `pendingRotoAdvanceRef`, clear save-on-leave refs/state, re-add the source to dirty state, and return before post-save opening. | PRESENT_BEHAVIOR_UNVERIFIED |

**Score:** 0/7 truths verified (7 present, behavior-unverified)

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | Save-on-leave coordinator, source tracking, apply-result advance, stale apply-status fix | VERIFIED | Exists/substantive. Contains `requestRotoFrameNavigation`, `saveOnLeaveSourceFrameRef`, `pendingRotoAdvanceRef`, `openSyncedRotoFrameAfterSave`, failure cleanup, timeout cleanup, and WorkflowStrip prop wiring. |
| `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` | Source-contract regression coverage | VERIFIED | Exists/substantive. Contains Phase 36.6 save-on-leave tests, stale applyStatus regression, failure retention, source tracking, origin/kind/frame guards, and input-disable coverage. |
| `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx` | Visible source-frame feedback and disabled Save current | VERIFIED | Exists/substantive. Accepts `rotoSavingFrame`, calls `getRotoPendingLabel`, renders the compact hint, keeps Save current visible, and disables Save current during `rotoSaveInFlight`. |
| `app/src/components/physic-paint/physicsPaintWorkflowState.ts` | Source-frame pending label helper | VERIFIED | Exists/substantive. `getRotoPendingLabel(true, true, savingFrame)` validates a non-negative integer and returns `Saving frame N…`. |
| `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts` | Strip/source-contract coverage | VERIFIED | Exists/substantive. Tests `rotoSavingFrame`, exact hint copy, Save current wording/disabled logic, click allowance, and absence of modal/toast/tutorial expansion in the save-on-leave status stack. |
| `app/src/components/physic-paint/physicsPaintWorkflowState.test.ts` | Helper coverage | VERIFIED | Exists/substantive. Tests `Saving frame 7…`, fallback `Saving current frame…`, invalid-frame fallback, and existing dirty/current generated semantics. |

## Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Studio navigation coordinator | Existing Roto save/cache path | `flushRotoFrame(sourceFrame, { force: true, advanceToFrame: targetFrame })` | WIRED | Manual code inspection verifies the dirty leave-boundary gate reuses `flushRotoFrame`; the automated regex verifier missed this due to pattern escaping, not because the link is absent. |
| Apply result handler | Internal post-save destination opener | `pendingRotoAdvanceRef` consumed after matching success and passed to `openSyncedRotoFrameAfterSave` | WIRED | Success branch snapshots `nextFrame`, clears pending/source refs, then opens the queued destination after the matching successful apply result. |
| Studio | WorkflowStrip | `rotoSavingFrame={rotoSavingFrame}` | WIRED | Studio owns `rotoSavingFrame` state and passes it into the strip. The automated regex verifier missed the JSX due to pattern escaping. |
| WorkflowStrip | Workflow state helper | `getRotoPendingLabel(hasPendingRotoFrames, Boolean(props.rotoSaveInFlight), props.rotoSavingFrame)` | WIRED | Strip calls the helper with the source-frame prop. The automated regex verifier missed the call due to pattern escaping. |
| Roto cell/transport navigation | Coordinator | Existing callbacks call `requestRotoFrameNavigation` | WIRED | Keyboard arrows/`g`, first/previous/next/last controls, and strip `onNavigateToSyncedFrame` route through the coordinator. |

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `PhysicsPaintStudio.tsx` | `pendingRotoAdvanceRef.current` | User Roto navigation requests in `requestRotoFrameNavigation`; consumed by `handleApplyResult` | Yes | FLOWING |
| `PhysicsPaintStudio.tsx` | `saveOnLeaveSourceFrameRef.current` / `rotoSavingFrame` | Dirty source frame captured from `currentFrame` before save | Yes | FLOWING |
| `PhysicsPaintStudio.tsx` | saved/cached Roto markers | Matching apply result `detail.startFrame` and deferred rendered/delete refs | Yes | FLOWING |
| `PhysicsPaintWorkflowStrip.tsx` | `props.rotoSavingFrame` | Studio `rotoSavingFrame` state prop | Yes | FLOWING |
| `physicsPaintWorkflowState.ts` | `savingFrame` | Strip passes Studio source frame to helper | Yes | FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Targeted Phase 36.6 test files pass | `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts PhysicsPaintWorkflowStrip.test.ts physicsPaintWorkflowState.test.ts --run` | `Test Files 39 passed | 3 skipped (42)`, `Tests 498 passed | 101 todo (599)`; existing audio/Tauri stderr warnings, no failures. | PASS |
| End-to-end Roto click/apply UI behavior | Not run; project instruction says not to run the server, and no runtime UI harness exists for this flow. | Requires user-side UAT. | SKIP |

## Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| N/A | N/A | No phase-declared or conventional probe scripts found for this phase. | SKIP |

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| 36.6-AC-01 | 36.6-01, `.planning/REQUIREMENTS.md` | Navigating away from a clean Roto frame opens the destination immediately. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md` and traceability table. Clean branch and source-contract test present; runtime UAT still needed. |
| 36.6-AC-02 | 36.6-01, 36.6-03, `.planning/REQUIREMENTS.md` | Dirty navigation saves the source frame first and opens the destination only after matching save success. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md`; dirty branch uses existing flush path, matching success uses internal post-save opener, and stale applyStatus regression passes. |
| 36.6-AC-03 | 36.6-01, 36.6-03, `.planning/REQUIREMENTS.md` | Saved source frame appears in correct cache/timeline position. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md`; success branch updates cached/saved state for `detail.startFrame`. |
| 36.6-AC-04 | 36.6-01, 36.6-03, `.planning/REQUIREMENTS.md` | Returning to the source frame shows the saved cached result. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md`; frame open helpers load editable or cached reference state. |
| 36.6-AC-05 | 36.6-02, 36.6-03, `.planning/REQUIREMENTS.md` | Save-on-leave feedback makes the wait visible without adding modal, toast, tutorial, or extra navigation controls. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md`; source-frame label/hint wiring and prohibition tests are present. |
| 36.6-AC-06 | 36.6-01, 36.6-02, 36.6-03, `.planning/REQUIREMENTS.md` | Rapid frame changes during one dirty source save keep only latest requested destination without corruption. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md`; active save-on-leave branch updates the queued destination without duplicate flush, including the stale apply-status interval. |
| 36.6-FB-01 | 36.6-01, 36.6-03, `.planning/REQUIREMENTS.md` | Failed save-on-leave stays on dirty source, preserves unsaved state, clears queued destination, and does not switch frames. | SATISFIED / UAT_NEEDED | Requirement now exists in `.planning/REQUIREMENTS.md`; failure/timeout branches preserve dirty source and clear queued destination before any post-save opener. |

No orphaned Phase 36.6 requirements were found. `.planning/REQUIREMENTS.md` includes all seven Phase 36.6 IDs in both the requirement list and traceability table.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| Phase-modified files | Multiple | `return null` / `return []` guard clauses | Info | Guard/default returns in established component/helper code; not stubbed rendering or phase-blocking placeholders. |
| Phase-modified files | N/A | `TBD`, `FIXME`, `XXX`, placeholder/coming-soon markers | None | No unresolved blocker debt markers found in source files modified by the phase. |

## Human Verification Required

### 1. Dirty source save-on-leave and return-to-source UAT

**Test:** Paint a Roto frame, click another existing Roto frame without pressing Save current, confirm the source saves before destination opens, then return to source.
**Expected:** Destination opens after source save succeeds; returning to source shows the saved paint.
**Why human:** Requires real editor/standalone bridge timing and visual confirmation; source checks cannot prove user-visible frame transition.

### 2. Source-specific saving feedback and rapid latest-destination UAT

**Test:** Paint a dirty Roto source frame, click another frame, confirm source cell pending/saving and compact line reads `Saving frame N…`; click a different destination during saving.
**Expected:** Existing Roto navigation remains clickable; Save current/destructive actions are disabled; the latest clicked destination opens after save.
**Why human:** Requires observing runtime UI feedback and rapid click behavior during the async save interval.

### 3. Failure retention UAT

**Test:** Simulate or observe a failed save-on-leave result.
**Expected:** The editor stays on the dirty source frame, preserves unsaved state, clears queued destination, and allows retry by explicit navigation.
**Why human:** Requires runtime failure injection or bridge failure observation; static checks cannot prove visible state retention after an actual failed apply.

## Gaps Summary

No implementation or requirements-traceability gaps remain. The previous blocker was closed by adding all seven Phase 36.6 requirement IDs to `.planning/REQUIREMENTS.md` and its traceability table. The remaining items are human UAT for MVP-mode runtime behavior, not implementation blockers.

---

_Verified: 2026-06-20T20:33:42Z_
_Verifier: Claude (gsd-verifier)_
