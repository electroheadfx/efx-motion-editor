---
phase: 36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase
plan: 02
type: execute
wave: 2
depends_on:
  - 36.6-01
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintWorkflowState.ts
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
autonomous: true
requirements:
  - 36.6-AC-05
  - 36.6-AC-06
must_haves:
  truths:
    - "36.6-AC-05: While save-on-leave is waiting, the source Roto cell appears pending/saving and the compact status line shows `Saving frame N…` for the source frame."
    - "36.6-AC-06: During a save-on-leave wait, existing Roto navigation clicks can update the latest queued destination without enabling Save current or destructive/convert actions."
    - "Manual Save current remains visible and useful, keeps the label `Save current`, and is disabled during save-on-leave."
    - "D-15: The workflow strip shows a compact non-tutorial hint near the existing Roto status/Save current surface with copy exactly Dirty frames save when leaving."
    - "After successful save-on-leave, no toast or extra success notification is added; destination opening and saved/cached cell state are sufficient."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintWorkflowState.ts"
      provides: "Source-frame-aware compact Roto pending label helper"
      exports: ["getRotoPendingLabel"]
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Existing workflow strip status stack and disabled actions for save-on-leave"
      contains: "rotoSavingFrame"
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts"
      provides: "Source-contract coverage for hint copy, preserved Save current wording, no modal/toast/tutorial/new controls, and disabled actions"
      contains: "Dirty frames save when leaving."
    - path: "app/src/components/physic-paint/physicsPaintWorkflowState.test.ts"
      provides: "Pure helper coverage for `Saving frame N…` compact status copy"
      contains: "getRotoPendingLabel"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      via: "Studio passes save-on-leave source frame as `rotoSavingFrame` or equivalent prop"
      pattern: "rotoSavingFrame=\\{"
    - from: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/physicsPaintWorkflowState.ts"
      via: "Workflow strip calls `getRotoPendingLabel(hasPendingRotoFrames, Boolean(props.rotoSaveInFlight), props.rotoSavingFrame)`"
      pattern: "getRotoPendingLabel\\(hasPendingRotoFrames, Boolean\\(props\\.rotoSaveInFlight\\), props\\.rotoSavingFrame\\)"
  prohibitions:
    - statement: "Do not add a modal or blocking overlay for normal saving state."
      status: "resolved by D-06"
      verification: "Workflow strip source-contract test asserts no new save-on-leave modal/overlay class or dialog branch is introduced."
    - statement: "Do not show a toast or extra success notification after successful save-on-leave."
      status: "resolved by D-09"
      verification: "Tests assert no toast/success notification code is added for save-on-leave success copy."
    - statement: "Do not add new navigation controls or tutorials."
      status: "resolved by D-04 and D-15"
      verification: "Workflow strip tests assert existing Save current wording remains and no new Roto navigation action labels are added."
---

<objective>
Add the minimal visible feedback required by D-06 through D-09, D-13, and D-15 for the save-on-leave coordinator from Plan 01.

Purpose: The animator understands that a dirty source frame is being saved and can continue clicking existing Roto destinations without mistaking the wait for a freeze or lost click.
Output: Tested workflow-state and strip changes that show `Saving frame N…`, keep Save current visible but disabled while saving, and avoid modal/toast/control expansion.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** move between Roto frames without manually pressing Save current every time, **so that** I can paint multiple frames without manually saving each one.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-RESEARCH.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-PATTERNS.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-VALIDATION.md
@SPECS/36.x-phases/phase-36.6-save-on-leave/spec-36.6-save-on-leave.md
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<dependency_graph>
This plan depends on Plan 01 because the UI needs the save-on-leave source frame produced by the coordinator. Task 1 defines the helper/test contract. Task 2 wires the strip UI and action disabled states. Task 3 validates integration with the Studio prop and no added controls/notifications.
</dependency_graph>

<artifacts_this_phase_produces>
- Optional third parameter on `getRotoPendingLabel(hasPending: boolean, isSaving: boolean, savingFrame?: number | null): string | null`.
- `rotoSavingFrame?: number | null` prop or equivalent on `PhysicsPaintWorkflowStripProps`.
- Source-frame compact status copy `Saving frame N…` for save-on-leave.
- Tests in `physicsPaintWorkflowState.test.ts` and `PhysicsPaintWorkflowStrip.test.ts` for pending copy, the D-15 hint copy `Dirty frames save when leaving.`, preserved Save current wording, disabled Save current, no modal/toast/tutorial/new controls, and queued-click allowance.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-frame-aware pending status helper tests and implementation</name>
  <files>app/src/components/physic-paint/physicsPaintWorkflowState.ts, app/src/components/physic-paint/physicsPaintWorkflowState.test.ts</files>
  <read_first>
    - SPECS/36.x-phases/phase-36.6-save-on-leave/spec-36.6-save-on-leave.md
    - .planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md
    - .planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-PATTERNS.md
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
  </read_first>
  <behavior>
    - `getRotoPendingLabel(true, true, 7)` returns `Saving frame 7…` using the single-character ellipsis for the compact visible status per D-07.
    - `getRotoPendingLabel(true, true)` preserves the existing fallback `Saving current frame…` for non-source-specific saves.
    - `getRotoPendingLabel(true, false)` preserves `Unsaved changes — click Save current to cache` so manual Save current remains useful per D-13.
    - Existing Roto cell aria/title labels may keep `Saving frame N...` with three periods unless the executor intentionally updates those tests; compact visible status is the locked ellipsis copy.
  </behavior>
  <action>Update `getRotoPendingLabel` to accept an optional `savingFrame?: number | null` parameter. If `isSaving` is true and `savingFrame` is a non-negative integer, return exactly `Saving frame ${savingFrame}…`. If `isSaving` is true without a valid source frame, return the existing `Saving current frame…`. Preserve all other return values. Add tests before implementation, then make them green. Do not change `getRotoCellStateLabel` unless needed by failing tests; if left unchanged, document the distinction in test names: compact visible status uses typographic ellipsis, cell aria/title label remains existing helper output.</action>
  <acceptance_criteria>
    - `physicsPaintWorkflowState.test.ts` expects `getRotoPendingLabel(true, true, 7)` to equal `Saving frame 7…`.
    - `physicsPaintWorkflowState.test.ts` still expects `getRotoPendingLabel(true, true)` to equal `Saving current frame…`.
    - `physicsPaintWorkflowState.ts` validates the optional saving frame with `Number.isInteger(savingFrame)` and `savingFrame >= 0` before using it.
    - Existing tests for dirty/current/generated/background-only Roto cell semantics still pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintWorkflowState.test.ts --run</automated>
  </verify>
  <done>The helper exposes exact source-frame status copy for save-on-leave without changing unrelated cell semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire pending feedback and disabled actions into the existing workflow strip</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - SPECS/36.x-phases/phase-36.6-save-on-leave/spec-36.6-save-on-leave.md
    - .planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md
    - .planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-PATTERNS.md
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
  </read_first>
  <behavior>
    - `PhysicsPaintWorkflowStripProps` accepts `rotoSavingFrame?: number | null` or an equivalent source-frame prop.
    - The compact status stack calls `getRotoPendingLabel(hasPendingRotoFrames, Boolean(props.rotoSaveInFlight), props.rotoSavingFrame)` so the visible status shows `Saving frame N…` per D-07.
    - Existing Roto cell buttons remain clickable during `rotoSaveInFlight` so users can update the latest queued destination per D-08; generated/render-only guards remain unchanged.
    - `Save current` remains visible with aria-label `Save current` and is disabled when `props.rotoSaveInFlight` is true per D-08 and D-13.
    - The workflow strip shows the D-15 compact non-tutorial hint copy exactly `Dirty frames save when leaving.` near the existing Roto status/Save current surface, without replacing the existing `Save current` wording.
    - No modal, blocking overlay, tutorial text, new Roto navigation controls, or success toast is added per D-06, D-09, and D-15.
  </behavior>
  <action>Extend `PhysicsPaintWorkflowStripProps` with `rotoSavingFrame?: number | null` if Plan 01 exposes that prop from Studio. Pass `props.rotoSavingFrame` into `getRotoPendingLabel`. Keep the existing status stack elements: `physics-paint-roto-status`, `physics-paint-roto-key-status`, and `physics-paint-roto-interpolation-status`. Add the D-15 compact hint near the existing Roto status/Save current surface with visible copy exactly `Dirty frames save when leaving.`; keep it as non-interactive hint text, not a tutorial, modal, toast, or new control. Keep Roto cell `onClick={() => handleRotoCellClick(frame, vm)}` active during `rotoSaveInFlight`, with only generated/render-only cells guarded. Keep the existing Save current button text and aria-label exactly `Save current`, and keep disabled logic including `props.rotoSaveInFlight`. Add source-contract tests asserting those exact props/calls/classes, asserting the hint copy exists, asserting Save current wording remains, and asserting prohibited UI terms/classes are absent from the save-on-leave code path.</action>
  <acceptance_criteria>
    - `PhysicsPaintWorkflowStrip.tsx` contains `rotoSavingFrame?: number | null` or the exact equivalent prop used by `PhysicsPaintStudio.tsx`.
    - The strip calls `getRotoPendingLabel` with three arguments including the saving source frame prop.
    - The workflow strip renders hint copy exactly `Dirty frames save when leaving.` near the existing Roto status/Save current surface.
    - `Save current` and `aria-label="Save current"` remain present.
    - The Save current disabled expression still includes `props.rotoSaveInFlight`.
    - The Roto cell click handler still calls `props.onNavigateToSyncedFrame(frame)` after generated/render-only guards and is not disabled globally by `props.rotoSaveInFlight`.
    - `PhysicsPaintWorkflowStrip.test.ts` asserts the hint copy is present, `Save current` wording remains, and no modal/dialog/blocking overlay/toast/tutorial/new navigation control strings are introduced for save-on-leave.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintWorkflowStrip.test.ts --run</automated>
  </verify>
  <done>The existing workflow strip shows source-frame save feedback, disables unsafe actions, and still lets existing navigation clicks update the queued destination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connect Studio save-on-leave source state to the strip and run the targeted phase gate</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintWorkflowState.test.ts</files>
  <read_first>
    - SPECS/36.x-phases/phase-36.6-save-on-leave/spec-36.6-save-on-leave.md
    - .planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
  </read_first>
  <action>Pass the save-on-leave source frame from Plan 01's Studio state/ref to `PhysicsPaintWorkflowStrip` as `rotoSavingFrame` or the exact prop name from Task 2. Set it when a dirty leave-boundary save starts; clear it on matching apply success, apply failure, send failure, and timeout. Do not set it for manual `saveRotoFrame(null)` unless the manual save path already uses the same in-flight visual without navigation; manual Save current must still stay on the current frame per D-14. Ensure destructive/convert actions that already key off `rotoSaveInFlight` remain disabled while the source save is active per D-08. Add/adjust source-contract tests in `PhysicsPaintWorkflowStrip.test.ts` or `PhysicsPaintStudio.test.ts` only as needed to assert the prop is passed from Studio.</action>
  <acceptance_criteria>
    - `PhysicsPaintStudio.tsx` passes the current save-on-leave source frame to `PhysicsPaintWorkflowStrip`.
    - The saving source frame is cleared in success and all failure branches.
    - Manual Save current remains a save-and-stay action and does not consume a queued destination.
    - No new success toast/copy is introduced after save-on-leave; existing apply messages do not add extra user notification beyond destination opening and saved/cached cell state.
    - Targeted Studio/Strip/State tests all pass together.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts PhysicsPaintWorkflowStrip.test.ts physicsPaintWorkflowState.test.ts --run</automated>
  </verify>
  <done>The visible save-on-leave feedback is wired from the coordinator to the existing workflow strip and the targeted Phase 36.6 test gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio state to Workflow Strip props | Source-frame save state crosses from coordinator logic into visible UI feedback. |
| User navigation clicks during saving | Existing cell/transport clicks remain accepted while unsafe save/destructive actions are disabled. |
| Status copy rendering | Runtime frame numbers are interpolated into visible text and aria/status surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.6-06 | Tampering | `getRotoPendingLabel` | mitigate | Use source frame only when it is a non-negative integer; otherwise fall back to existing current-frame saving copy. |
| T-36.6-07 | Denial of Service | Workflow strip navigation during save | mitigate | Keep existing navigation clicks available to update queued destination; disable only Save current/destructive actions during `rotoSaveInFlight`. |
| T-36.6-08 | Repudiation | ambiguous save feedback | mitigate | Show exact source-frame status `Saving frame N…` in the compact status line so UAT can identify which frame is being saved. |
| T-36.6-09 | Tampering | destructive/convert actions during save | mitigate | Preserve existing disabled conditions keyed to `rotoSaveInFlight` for Save current and destructive/convert actions. |
| T-36.6-SC | Tampering | package installs | accept | No package-manager install task exists for this plan; package legitimacy audit says no install. |
</threat_model>

<verification>
<automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts PhysicsPaintWorkflowStrip.test.ts physicsPaintWorkflowState.test.ts --run</automated>
<human-check>User-side UAT after this plan: paint a dirty Roto source frame, click another frame, confirm the source cell shows pending/saving and the compact line reads `Saving frame N…`; click a different destination during saving and confirm the latest destination opens after save.</human-check>
</verification>

<success_criteria>
- The user sees source-specific save feedback instead of a frozen-looking navigation wait.
- The workflow strip includes the D-15 compact non-tutorial hint `Dirty frames save when leaving.` near the existing Roto status/Save current surface.
- Save current stays visible and useful but cannot start a conflicting save during save-on-leave.
- Existing Roto navigation controls remain the only navigation surface and can update the queued destination while saving.
- The phase adds no modal, blocking overlay, tutorial, new controls, or success toast.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Move between Roto frames without manually pressing Save current every time | 36.6-01, 36.6-02 | COVERED | Plan 02 makes the wait observable and testable for UAT.
REQ | 36.6-AC-05 | Navigation feedback prevents freeze/lost click perception | 36.6-02 | COVERED | Pending source cell and compact status line.
REQ | 36.6-AC-06 | Rapid changes do not corrupt source/destination | 36.6-01, 36.6-02 | COVERED | Plan 02 keeps clicks enabled to update latest destination.
RESEARCH | — | Pending/saving UI uses existing workflow strip/status stack | 36.6-02 | COVERED | Helper/prop extension only.
RESEARCH | — | Source-specific status copy avoids ambiguity | 36.6-02 | COVERED | `Saving frame N…`.
CONTEXT | D-04 | No new navigation controls | 36.6-02 | COVERED | Existing controls only.
CONTEXT | D-06 | Pending cell plus compact status, no modal/overlay | 36.6-02 | COVERED | Status stack.
CONTEXT | D-07 | Use `Saving frame N…` copy | 36.6-02 | COVERED | Helper returns exact copy.
CONTEXT | D-08 | Allow navigation clicks to queue latest; disable Save current/destructive/convert | 36.6-02 | COVERED | Clicks stay active; save/actions disabled.
CONTEXT | D-09 | No success toast/extra notification | 36.6-02 | COVERED | Source-contract prohibition.
CONTEXT | D-13 | Keep Save current visible/useful | 36.6-02 | COVERED | Existing copy remains.
CONTEXT | D-14 | Manual Save current stays on frame | 36.6-01, 36.6-02 | COVERED | Manual save isolation maintained.
CONTEXT | D-15 | Subtle hint only, no tutorials/new controls | 36.6-02 | COVERED | Task 2 adds exact hint copy `Dirty frames save when leaving.` near the existing Roto status/Save current surface and tests preserved Save current wording plus no tutorial/control/modal/toast additions.
</source_audit>

<output>
Create `.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-02-SUMMARY.md` when done.
</output>
