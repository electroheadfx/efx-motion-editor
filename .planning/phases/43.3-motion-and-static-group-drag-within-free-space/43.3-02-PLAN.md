---
phase: 43.3-motion-and-static-group-drag-within-free-space
plan: 02
type: tdd
wave: 2
depends_on:
  - 43.3-01
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
autonomous: true
requirements:
  - GDRAG-04
  - GDRAG-05
  - GDRAG-06
  - GDRAG-08

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single exported pure clamp function in the resolver module computes the nearest free destination against frame 0, capacity/parent end, every other real key, and every other Group interval including linked occurrences, with the dragged Group's own interval as pass-through space (D-08)"
    - "The resolver clamps and commits at the clamped position; rejection happens only when zero valid movement exists in the dragged direction, surfaced through one new closed-union failure code (D-05, D-06 substrate)"
    - "Dragging a duplicated shared-source placement moves only its placement and linked interval — key mapping identical to input, shared source keys and their owned breaks never moved, cloned, or retimed (D-11)"
    - "A vacated interval with a successor real key gives that successor ownership/reuse of the incoming break; a landing gap gives the moved Group's first source key a new break; adjacent landings and no-successor/no-predecessor cases create no break (D-09, D-10, D-12)"
    - "Breaks already owned by moved keys travel with their identity unchanged; Group-local visibleRanges and deleted phases never convert into key-owned breaks (43.1 D-14, D-13)"
    - "Stale or ambiguous Group, source, or break authority rejects fail-closed before any mutation; a failed drag carries no partial proposal (D-18)"
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts exports the pure clamp function consumed by both the resolver branch and (plan 03) the preview"
    - "The closed failure-code union contains the zero-free-space code used by the no-space rejection"
    - "physicsPaintRotoPhysicalResolver.test.ts contains the clamp matrix, attachment cases, and break-derivation describe blocks"
  key_links:
    - "clamp function -> resolver branch -> proposal: the clamped destination the resolver commits is exactly what plan 03's ghost preview shows (preview-is-the-commit, D-05)"
    - "break derivation -> nextIncomingInterpolationBreakKeyIds -> finalizeProposal -> apply payload -> canonical revision: gaps persist through the existing 43.1 pipeline with no new gap authority (D-09)"
---

<objective>
Complete the resolver-side semantics of the Group drag: free-space clamping against every collision boundary, the duplicated shared-source placement-only branch, and the Phase 43.1 stable-key-owned break derivation that records vacated and newly opened intervals as persistent intentional gaps.

Purpose: This is the phase's composition-correctness core (RESEARCH: risk concentrates in clamping and break derivation, not new machinery). TDD mode: each task is RED-first against the existing resolver suite harness.
Output: A resolver branch that turns any rail-drag destination into a clamped, guarded, break-recording immutable proposal — the exact contract the plan 03 preview must mirror.

Planning notes: RESEARCH Open Question 1 (Infinity-Group rightward drag) is resolved here as: effective extent is fully derived, the drag is permitted, and derived occurrences re-derive without any Repeat change (43 next-clip-move precedent); a native UAT row in plan 04 covers it so the user can veto at UAT. RESEARCH Open Question 2 is resolved as: one exported pure clamp function in the resolver module, consumed by both resolver and preview.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-RESEARCH.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-PATTERNS.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure clamp authority — collision boundaries, clamp-and-commit, zero-space rejection</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 276-301 failure-code union, 859-919 placement-follow, 1649-1771 buildMoveGroupCandidate guard algebra, 3057-3060 Infinity effective-end derivation, 3089-3113 derivePhysicPaintRotoLoopRanges range contract, the plan-01 'move-group' branch)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md (D-05, D-06, D-08)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-RESEARCH.md (Pitfalls 3 and 7)
  </read_first>
  <behavior>
    - Clamp matrix (each a resolver test): drag right clamps at the next real key's frame minus interval width; drag left clamps at the previous real key's following frame; drag clamps at frame 0; drag clamps at physical capacity/parent end; drag clamps at another Group's interval including a linked occurrence interval of another Group; the dragged Group's own current interval is pass-through (a drag crossing its original span is not blocked by it)
    - Clamp-and-commit: a proposed destination beyond a boundary resolves ok with the clamped destinationPlacementStart, never a collision rejection (Pitfall 3 — do not import buildMoveGroupCandidate's duplicate-destination rejection semantic)
    - Zero-space rejection: when no valid movement exists in the dragged direction the branch fails with one new closed-union failure code (e.g. 'no-free-space-in-direction') and carries no proposal
    - Edge cases: an Infinity Group's dragged interval derives from phaseOrigin to resolved effectiveEnd (rightward drag legitimately shrinks derived occurrences — not a Repeat change, D-19); a 0f-effective Group (zero-width interval) clamps without division errors and remains draggable
    - Regression: all pre-existing resolver tests pass unmodified
  </behavior>
  <action>RED first: write the clamp-matrix tests in a new describe block of physicsPaintRotoPhysicalResolver.test.ts. Then implement. Add one new member to the closed PhysicPaintRotoPhysicalEditFailureCode union for the zero-valid-movement case. Export a pure clamp function from the resolver module (name at executor discretion, e.g. clampPhysicPaintGroupDragDestination) taking the dragged clip, its derived interval (from derivePhysicPaintRotoLoopRanges — the same projection the rail draws, resolver lines 3089-3113), the proposed destinationPlacementStart, the drag direction, validated identities, all loop ranges, and capacity; it returns the nearest destination whose interval overlaps none of the D-08 boundaries: frame 0, physical capacity/parent end, every previous/next real key not owned by the dragged Group, and every other Group interval including linked occurrences of other Groups; the dragged Group's own current interval is excluded from the boundary set (pass-through). Compute the dragged interval from the derived range record so Infinity Groups use the resolved effectiveEnd (lines 3057-3060) and zero-width intervals are handled explicitly (Pitfall 7). Wire the plan-01 'move-group' branch to route intent.destinationPlacementStart through this clamp BEFORE computing delta; replace the plan-01 fail-closed-on-collision tracer behavior with clamp-and-commit (D-05); fail with the new code only when zero valid movement exists in the dragged direction (D-06 substrate — product copy lands in plan 03). Keep move-key-group and buildMoveGroupCandidate byte-unchanged.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintRotoPhysicalResolver.ts exports the pure clamp function and the 'move-group' branch consumes it before delta computation
    - The failure-code union gains exactly one new member for zero-valid-movement; no existing code is renamed or removed
    - Every clamp-matrix behavior test passes, including linked-occurrence boundary, own-interval pass-through, Infinity derived-extent, and zero-width interval cases
    - Full resolver suite exits 0 with zero modifications to pre-existing test expectations
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The resolver clamps any proposed destination to the nearest free position against all D-08 boundaries and rejects only true zero-space drags with a dedicated failure code (GDRAG-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Duplicated shared-source placement — placement-only movement branch</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 870-879 attachment coincidence test, the plan-01 'move-group' branch and Task-1 clamp wiring)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (lines 256-310 Loop Clip record; isPhysicPaintRotoLoopClip lifecycle validation lines 561-585)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md (D-11 user-refined, D-19)
  </read_first>
  <behavior>
    - Test 1: given a Group whose placementStart differs from its first source key frame (duplicated placement sharing another source cycle), resolving 'move-group' returns ok with a key mapping identical to the input frames (zero key movement) and nextLoopClips carrying only the moved clip's clamped placementStart
    - Test 2: shared source keys referenced by OTHER Groups never appear in the mapping; their owned incoming breaks are byte-identical in nextIncomingInterpolationBreakKeyIds unless Task-3 vacated/residual-interval rules require otherwise
    - Test 3: ambiguous attachment authority (e.g. malformed clip failing isPhysicPaintRotoLoopClip lifecycle validation, or sourceKeyIds[0] missing from identities) rejects fail-closed with no proposal (D-18)
    - Test 4: the drag never materializes linked occurrences, never duplicates source assets, and never changes repeat or mode (D-19)
  </behavior>
  <action>RED first: add the duplicated-placement describe block. Then extend the plan-01 branch: after the resolver-side attachment derivation (placementStart === framesByKeyId.get(sourceKeyIds[0])), the source-attached arm keeps the plan-01 rigid translation plus Task-1 clamp; the duplicated arm emits a mapping that maps every key id to its current frame (identity mapping — no key moves), and nextLoopClips updates ONLY the dragged clip's placementStart to the clamped destination while lifecycle fields stay consistent with a pure placement move (translate phaseOrigin/originalEndExclusive/visibleRanges/frameOverrides by the placement delta exactly as the source-attached arm does — Group-local deleted phases keep their original relative source phases, D-13). Validate attachment fail-closed from canonical facts only; never accept a UI-supplied attachment flag (Pitfall 4). Add no new persisted field. Break changes for the duplicated arm are derived by Task 3's rules — if existing breaks already preserve every resulting empty interval, nextIncomingInterpolationBreakKeyIds echoes the input unchanged (D-11).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The duplicated-placement tests pass: identity key mapping, placement-only nextLoopClips, shared source keys and breaks untouched
    - Fail-closed rejection tests for ambiguous/malformed authority pass
    - Full resolver suite exits 0; no pre-existing test expectation modified
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>A duplicated shared-source placement drag moves only that placement and its linked interval; shared source keys and their owned breaks are provably untouched (GDRAG-04, D-11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stable-key-owned break derivation for vacated and newly opened intervals (D-09..D-13)</name>
  <reversibility rating="one-way">D-09 locked by the user in CONTEXT.md with explicit one-way rationale (breaks join canonical revision/history snapshots); the decision is already made, so no decision checkpoint is inserted — this annotation records the rating per the reversibility-gates contract.</reversibility>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 1059-1082 break-creation precedent, the finalizeProposal break threading visible near line 3015, plan-01/Task-1/Task-2 branch state)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (the existing "incoming interpolation break lifecycle" describe block at line 383 — the direct harness)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (lines 947-986 revision fingerprint with break parameter)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md (D-09 through D-13 verbatim)
  </read_first>
  <behavior>
    - Test 1 (vacated interval, successor exists): after a source-attached move, the next real key after the vacated interval owns the incoming break; when that key already owns a break it is reused, never duplicated; no standalone frame-range gap record exists anywhere in the proposal
    - Test 2 (landing gap): when the move opens an empty interval between the moved Group's first source key and its new predecessor, that first key owns a NEW incoming break; landing immediately adjacent to the predecessor creates no break
    - Test 3 (identity travel): breaks already owned by the moved keys travel unchanged (43.1 D-14); the drag adds or removes no break beyond the landing-gap break
    - Test 4 (D-12 no-break cases): a vacated interval at end of content (no successor key) or a landing before all content (no predecessor) produces zero break change
    - Test 5 (duplicated placement, D-11 refined): the next real key after a newly vacated interval owns/reuses the incoming break; existing destination-gap breaks are preserved and reused; when existing breaks already preserve every resulting empty interval, nextIncomingInterpolationBreakKeyIds equals the input collection
    - Test 6 (D-13 boundary): Group-local deleted phases and visibleRanges move rigidly with the Group and never appear as key-owned breaks; only external drag-opened gaps alter the break collection
  </behavior>
  <action>RED first: extend the existing incoming-interpolation-break describe harness (test line 383) with the six behavior groups. Then implement the break derivation inside the 'move-group' branch following the 43.1 precedent at resolver lines 1059-1082. Compute from the post-move document: (a) the vacated interval = the dragged Group's pre-move effective interval; find the next real key after it in post-move order — if one exists and would interpolate across the vacated span, it owns the incoming break (reuse an existing break on that key; never duplicate); if none exists, record nothing (D-12). (b) The landing gap = the interval between the moved Group's new predecessor real key and its first source key — if non-empty, the first source key owns a new incoming break; if adjacent, none (D-10). (c) Carry every break owned by moved keys through unchanged (43.1 D-14). (d) Apply (a) identically for duplicated placements (D-11) but never touch breaks owned by shared source-key identities merely because the placement moved; when the existing collection already preserves every resulting empty interval, emit the input collection unchanged. (e) Never derive breaks from Group-local visibleRanges/deleted phases — those travel as lifecycle fields only (D-13). Emit the complete nextIncomingInterpolationBreakKeyIds through the existing finalizeProposal threading so the break collection flows proposal -> apply payload -> canonical revision -> history exactly like 43.1 (no new gap authority, no new persisted field). The one-atomic-transaction requirement (GDRAG-07) is satisfied by construction: moved keys/placements and movement-created breaks live in the same proposal.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six break-derivation behavior groups pass inside the extended incoming-break describe block
    - The proposal carries nextIncomingInterpolationBreakKeyIds through the unchanged finalizeProposal path; no new gap record type, field, or module exists in the diff
    - Full resolver suite exits 0 with zero modifications to pre-existing test expectations
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Every vacated or newly opened interval becomes a persistent intentional gap recorded as a stable-key-owned incoming break under exact 43.1 semantics — reuse over duplication, no break when nothing would interpolate across, Group-local fragments never converted (GDRAG-06, D-09..D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proposed destination -> canonical document | Untrusted pointer-derived destination becomes accepted physical truth through the clamp/break derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.3-02-01 | Tampering | Clamp computation (resolver) | high | mitigate | Single exported pure clamp consumed by resolver and preview; interval-overlap math against derived ranges; zero-width and Infinity extents handled explicitly (Task 1, Pitfalls 3/7) |
| T-43.3-02-02 | Tampering | Duplicated shared-source move | critical | mitigate | Resolver-derived attachment only (no UI flag); identity mapping for shared keys; fail-closed on ambiguous authority (Task 2, D-11/D-18) |
| T-43.3-02-03 | Tampering / Repudiation | Break recording | high | mitigate | Breaks recorded only as stable-key-owned incoming breaks through the existing 43.1 revision/history pipeline; reuse-never-duplicate; complete-collection replacement semantics (Task 3, D-09) |
| T-43.3-02-04 | Denial of Service (local correctness) | Zero-movement and degenerate intervals | medium | mitigate | Dedicated closed-union failure code with no partial proposal; failure branch never carries a proposal (Task 1) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — green, including clamp matrix, attachment cases, and break-derivation blocks
- pnpm --dir app typecheck — exits 0
- git diff confirms move-key-group and buildMoveGroupCandidate byte-unchanged
</verification>

<success_criteria>
The 'move-group' resolver branch turns any proposed destination into a clamped, guarded proposal: source-attached rigid moves with lifecycle translation, duplicated placement-only moves, and 43.1-semantic break recording for every vacated or newly opened interval — with fail-closed rejection for stale/ambiguous authority and true zero-space drags (GDRAG-04, GDRAG-05, GDRAG-06, GDRAG-08 resolver substrate).
</success_criteria>

## Artifacts this phase produces (this plan)

- Exported pure clamp function, e.g. `clampPhysicPaintGroupDragDestination` (physicsPaintRotoPhysicalResolver.ts)
- New closed-union failure code for zero-valid-movement, e.g. `'no-free-space-in-direction'` (physicsPaintRotoPhysicalResolver.ts failure-code union)
- Duplicated placement-only arm and break-derivation logic inside the 'move-group' dispatch branch
- New test blocks: clamp matrix, duplicated-attachment cases, break derivation D-09..D-13 (physicsPaintRotoPhysicalResolver.test.ts)

<output>
Create `.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-02-SUMMARY.md` when done
</output>
