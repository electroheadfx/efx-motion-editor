---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 03
type: execute
wave: 2
depends_on: [43.6-01, 43.6-02]
files_modified:
  - app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: [BMOV-02, BMOV-03, BMOV-06, RSET-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dragging any selected rail moves the whole set; the grabbed rail is not special; dragging an unselected rail collapses the set to that rail first, then runs its own 43.3/43.4 drag (D-08)."
    - "Modifier pointer-downs (Cmd/Ctrl/Shift) never start a batch drag session — the selection gestures keep working during an active set (Pitfall 4, drag hook L140 guard verbatim)."
    - "Every selected rail ghosts at the clamped destination in its kind base color at 55% opacity; originals stay at 100% with orange selection lines for the whole drag (D-09, UI-SPEC M3)."
    - "A clamped drag paints one 2x12px #FF6B6B bar on the colliding member's ghost blocked edge (left edge of leftmost blocked ghost for leftward, right edge of rightmost for rightward); dropping while clamped commits at the clamp (D-07, UI-SPEC M3)."
    - "Would-open gaps preview as ordinary roto-fill-empty cells, byte-identical to the 43.2-43.5 gap treatment (D-09, UI-SPEC M3)."
    - "One live status-line set readout during the drag: 'Move Rails +{D} - set frames {A}-{B} -> {A'}-{B'}' with ', gap {G1}-{G2}' only when a gap would open; leftward uses U+2212 MINUS SIGN (D-09, UI-SPEC M3)."
    - "Zero-delta drop publishes nothing - no commit, no status, no history entry; the set stays selected; Escape during the drag cancels with originals byte-untouched and the set stays selected (D-13)."
    - "Zero valid movement on drop publishes 'No empty space in that direction.' with zero mutation; the straddle rejection publishes 'Can''t move the selected Rails: a selected Group shares its source with an unselected Group.' (D-10, UI-SPEC M3)."
    - "Acceptance publishes the locked status 'Moved {N} Rails by {D} frames - set now frames {A}-{B}.' plus ' Gap left at frames {G1}-{G2}.' only when a gap actually opened (D-12)."
    - "After an accepted batch Move the whole set stays selected at the new positions with the anchor following its rail's identity; Undo restores the set at the original positions; Redo re-applies at the destination (D-06, RSET-03)."
  artifacts:
    - "app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts — batch pointer-session hook (per-member ghost preview, set-level clamp ports)"
    - "app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts — Wave 0 test file (created by Task 1 RED)"
    - "useRotoTimelineActions.ts — move-rails prepare/commit ports + locked readout/status/rejection copy via the one mapper"
    - "PhysicsPaintWorkflowStrip.tsx + both rail components — set drag routing, ghost/gap/blocked-edge paint"
  key_links:
    - "Drag hook prepare port -> Plan 02 clamp authority -> commit of the retained publication: preview-is-the-commit (breakage = drop commits an unseen proposal)"
    - "Accepted move-rails -> recordRailSetSnapshot + resolveRailSetPostAcceptance: the D-06 set-survival/undo-restore link (breakage = UAT step 3 fails: Undo restores rails but not the set)"
    - "Live readout -> setStatus gate only: the no-churn playback-status discipline (Pitfall 7)"
---

<objective>
Wire the batch Move Rails gesture end-to-end: a new port-injected pointer-session hook (template: usePhysicsPaintKeyRailDrag) driving per-member ghost/gap/blocked-edge preview paint, committing the retained clamped publication through the Plan 02 `move-rails` resolver intent as one atomic history command, with the locked live readout, rejection, zero-delta, and Escape-cancel discipline, and the D-06 set aftermath (stay-selected after commit; exact set restore on Undo/Redo via the Plan 01 side-channel).

Implements BMOV-02 (drag any selected rail; collapse-first on unselected), BMOV-03 (ghost/gap/blocked-edge/live-readout paint), BMOV-06 (locked status copy, zero-delta no-publish, Escape cancel), and the Move half of RSET-03 (set survives batch ops + Undo/Redo).

Purpose: the only genuinely new interaction machinery of the phase — landing it against the proven Plan 02 intent keeps the gesture layer presentation-only.
Output: a user can drag any selected rail and move the whole set with full 43.3/43.4/43.5-grade preview and rejection fidelity.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md
@app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): usePhysicsPaintRailSetDrag pointer-session hook with per-member preview state</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts (FULL — copy its structure exactly; L140 modifier guard verbatim; L234-243 threshold/begin; L244-271 drop commit; L272-294 cancel)
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts (FULL — hoisted-mock harness + WindowDouble/SourceDouble; copy verbatim and generalize)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md §hooks/usePhysicsPaintRailSetDrag.ts (batch generalization notes)
  </read_first>
  <behavior>
    - Pointer-down guard identical to the Key Rail hook L140: non-primary button, non-left button, or any meta/ctrl/shift modifier returns immediately (selection gestures never start drags — Pitfall 4).
    - 4px horizontal threshold (KEY_RAIL_DRAG_THRESHOLD_PX discipline) before the session starts; release under threshold falls through to click behavior (collapse-to-rail).
    - Ports: projectDelta (pointer -> signed frame delta), clampDelta (proposed delta -> clamp result incl. blockedEdge + colliding member identity), prepareAtDelta (clamped delta -> publication or rejection), onDropCommit(publication) -> Promise<boolean>, onCancel, onRejected(reason, detail), onPreviewChange, clearClickSequence, windowLike — resolver/store concerns enter ONLY through ports.
    - Preview state carries the SET-level result: `{ delta, blockedEdge, collidingMemberId, gapIntervals }` — per-member ghost geometry is derived by the strip (Task 2), not the hook.
    - Preview-is-the-commit: the publication prepared during the drag is retained exactly and committed at pointer-up, never recomputed.
    - Zero-delta: a clamped delta of 0 prepares no publication; drop publishes nothing (no commit call, no rejection) — D-13.
    - Escape capture-phase cancel, pointercancel, and lostpointercapture all clean up exactly once with byte-untouched originals; trailing-click suppression arms only when a session actually started (43.5-04 deviation precedent).
  </behavior>
  <action>
    RED first: create usePhysicsPaintRailSetDrag.test.ts copying the Key Rail drag harness (hoisted preact/signals mocks, WindowDouble, SourceDouble) with failing cases for every <behavior> bullet, then GREEN.

    Create app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts mirroring usePhysicsPaintKeyRailDrag structure: same session ref lifecycle, capture, grabbing cursor, suppression, cleanup, unmount disposal. Generalize the ghost/preview signal to the set-level preview state above (the hook owns NO member geometry). The zero-delta discipline: when clampDelta returns delta 0, prepareAtDelta is not invoked and the session retains no publication, so pointer-up publishes nothing and reports neither rejection nor success (D-13); a non-zero clamped delta whose prepare fails retains the rejection for onRejected at drop. Keep the injectable WindowLike/SourceElement structural surfaces so the hook is unit-testable in node (43.4 discipline).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The new test file passes with cases for: modifier-guard no-start, sub-threshold release (click falls through), threshold start, preview-is-the-commit (prepared publication committed verbatim), zero-delta publishes nothing, prepare-failure rejection at drop, Escape/pointercancel/lostpointercapture cleanup exactly once, click suppression armed only after start.
    - The hook file contains the exact modifier guard condition from the Key Rail hook L140.
    - The hook imports nothing from the resolver, store, or Studio (ports only — source assertion: its import list matches the Key Rail hook's dependency discipline).
    - Typecheck green.
  </acceptance_criteria>
  <done>The batch drag session state machine exists, fully unit-tested in node, owning gesture lifecycle and set-level preview state only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip/rail wiring, ghost + gap + blocked-edge paint, prepare/commit ports, locked copy, and D-06 set aftermath</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (rail rendering loop + ghost paint sites; L1154-1260 Delete/Push control block for the busy/commit idiom; L1690-1730 capsule composition for the live readout seam)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx L130-190 (click/drag interplay, click suppression consumption, focusRailTargetOnPointerSelection) and PhysicsPaintKeyRail.tsx (same seam)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (prepare/commit port idiom — prepareRotoPush/commitRotoPush L985-1100 area; revalidation-before-dispatch precedent from 43.5 commitRotoPush)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx L2145 (strip prop bundle wiring) and the acceptance continuation where resolvePostAcceptanceRotoStudioSelection runs
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md §M3 (ghost geometry/paint, blocked-edge direction rule, readout copy), §M6 (accepted Move copy D-12)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md §Shared Patterns (parent-authoritative commit, playback status gate)
  </read_first>
  <behavior>
    - Pointer-down on a rail that IS in the active set arms the batch session; pointer-down on a rail NOT in the set collapses the set to that rail first and hands off to its own 43.3/43.4 drag (D-08).
    - During the drag every set member ghosts at the clamped destination in its kind base color at 55% opacity (Motion #8B5CF6 / Static #06B6D4 / Key Rail #8A939C); ghosts never show selection orange, endpoint cuts, lifecycle dots, or the anchor tick; originals stay at 100% with orange lines (UI-SPEC M3).
    - Would-open gaps preview as roto-fill-empty cells (border #3D454D, background #4D535A); adjacent landing previews zero gap cells (UI-SPEC M3/M1 overflow: ghosts clip at the shared viewport).
    - Clamped state paints one 2x12px #FF6B6B bar on the colliding member's ghost blocked edge per the direction rule; the bar is the sole blocked signal.
    - The live readout publishes through the existing setStatus gate only (Pitfall 7): 'Move Rails +{D} - set frames {A}-{B} -> {A'}-{B'}' (U+2212 for leftward), ', gap {G1}-{G2}' appended only when a gap would open; hover alone never changes the capsule (43.5 D-13).
    - Commit: the retained publication dispatches move-rails; commit revalidates the current proposal version and fails closed with zero mutation on mismatch (43.5 commitRotoPush precedent, T-43.6-02); acceptance publishes from the .then(accepted) continuation.
    - Accepted status (D-12): 'Moved {N} Rails by {D} frames - set now frames {A}-{B}.' + ' Gap left at frames {G1}-{G2}.' only when a gap opened; singular 'Moved 1 Rail by ...'; one mapper.
    - Rejections: zero valid movement -> 'No empty space in that direction.'; straddle -> 'Can't move the selected Rails: a selected Group shares its source with an unselected Group.'; stale/transport -> existing mapped controller reason; every failure mutates nothing and writes no history entry.
    - D-06/RSET-03: on acceptance the set stays selected at the new positions (Key Rail identities are move-stable; loopIds unchanged) with the anchor following its rail; before dispatch the Studio records the pre-command set via recordRailSetSnapshot keyed by the accepted operationId, and undo/redo restores the before/after set exactly.
    - A second batch drag session cannot start while a physical mutation is in flight (existing busy gate).
  </behavior>
  <action>
    useRotoTimelineActions.ts: add the move-rails prepare/commit port pair (planner-named `prepareRailSetMove` / `commitRailSetMove`) mirroring prepareRotoPush/commitRotoPush: prepare builds the move-rails intent from the set members + clamped delta (members derived from the current set via Plan 01 ordering; delta from the drag session), attaches the proposal version; commit revalidates the CURRENT proposal version before dispatch (T-43.6-02) and publishes the locked D-12 copy via the Plan 01 mapper on acceptance. Extend the mapper with the live readout + accepted + rejection strings (UI-SPEC M3/M6 locked tables) — the straddle sentence is Group-domain vocabulary, verbatim.

    Rail components + strip: wire pointer-down routing (D-08): if the rail's identity is in the active set, hand the event to the batch session; otherwise run the existing collapse-then-own-drag path unchanged. Render per-member ghosts from the hook's preview state: member interval + delta -> left/width at the existing 3px rail geometry, kind base color at 55% opacity (additive CSS in physicsPaintStudio.css); render the blocked-edge bar and roto-fill-empty gap cells from the same preview state; all preview paint aria-hidden + pointer-events none and removed on commit/cancel/reject (UI-SPEC M3 layer order: ghosts between layers 7 and 8).

    PhysicsPaintStudio.tsx: wire the ports into the strip bundle, record the pre-command set snapshot on accepted move-rails (recordRailSetSnapshot with the accepted operationId), and route 'move-rails' acceptance + undo/redo through resolveRailSetPostAcceptance so the set survives the commit and restores exactly on Undo/Redo (D-06). No logic beyond wiring in the Studio body (43.4 focused-module precedent).

    Tests: extend useRotoTimelineActions.test.ts (prepare/commit port: intent shape, version revalidation, copy strings via the mapper, rejection mapping) and physicsPaintRotoRailSetSelection.test.ts aftermath cases for 'move-rails' (keep set) — the undo/redo restore round-trip landed in Plan 01 Task 3.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - useRotoTimelineActions.test.ts asserts the exact locked strings: 'Moved 3 Rails by 12 frames - set now frames 24-100.' (with gap sentence appended only on gap), 'Moved 1 Rail by 8 frames - ...' singular, the straddle sentence verbatim, and 'No empty space in that direction.' for zero-movement rejection.
    - The live readout flows through the existing setStatus gate (source assertion: no direct capsule/status write from the drag preview path).
    - A modifier pointer-down during an active set performs selection, never drag (hook guard + routing test).
    - Dragging an unselected rail collapses the set and invokes that rail's own drag port, never the batch session (D-08 routing test).
    - After an accepted move-rails the set members are unchanged identities (stay-selected); undo restores the recorded pre-command set; redo re-applies (aftermath tests).
    - physicsPaintStudio.css ghost/blocked-edge rules carry exactly 55% opacity kind colors and the 2x12px #FF6B6B bar; gap preview reuses the existing roto-fill-empty class with no new gap visual.
    - Full physic-paint suite + typecheck green; single-rail drag and Push tests unchanged and green (regression boundary, UI-SPEC matrix row 9).
  </acceptance_criteria>
  <done>A user drags any selected rail to move the whole set rigidly with full preview fidelity, locked copy, atomic commit, and exact set survival across commit/Undo/Redo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer session -> prepare/commit ports | Drag-time input (delta, timing) is untrusted until the retained publication revalidates against the current revision |
| preview paint -> accepted document | Ghosts/gaps are presentation-only; nothing may present as moved before exact parent acknowledgement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-02 | Tampering/Repudiation | stale-authority commit after document change mid-drag | high | mitigate | commitRailSetMove revalidates the current proposal version before dispatch and fails closed with zero mutation (43.5 commitRotoPush precedent); Plan 01 reconcile clears stale sets |
| T-43.6-04 | Tampering | batch drag armed by a modifier click | medium | mitigate | The L140 modifier guard is verbatim in the hook; D-08 collapse-first routing (Task 1/2 tests) |
| T-43.6-05 | Information Disclosure (integrity) | optimistic paint before acceptance | medium | mitigate | Ghosts are aria-hidden paint-only; originals byte-untouched until the accepted continuation; zero-delta/rejection publish nothing (D-13) |
| T-43.6-SC | Tampering | package installs | low | mitigate | No package installs in this plan; legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/physic-paint` green after every task; full suite + typecheck before plan completion.
- VALIDATION.md row 43.6-BMOV-02 (BMOV-02/03/06) covered by the new drag test file; Wave 0 obligation for usePhysicsPaintRailSetDrag.test.ts satisfied by Task 1 RED.
- UI Considerations lifted: M3 batch ghosts populated/partial/overflow, M3 gap preview zero-one-many, M6 long-text/empty — all in must_haves.truths.
</verification>

<success_criteria>
- Batch Move is indistinguishable in discipline from the accepted 43.3/43.4/43.5 drags: clamp-or-reject as one unit, preview-is-the-commit, one atomic history command, exact set survival across Undo/Redo, and zero regression to single-rail drag or Push.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-03-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 03)

New symbols (for plan-review-convergence source grounding):
- `usePhysicsPaintRailSetDrag` (hook) + session input/preview/clamp types — hooks/usePhysicsPaintRailSetDrag.ts
- `prepareRailSetMove` / `commitRailSetMove` (ports) — hooks/useRotoTimelineActions.ts
- CSS ghost/blocked-edge rules for batch move (class names executor-chosen under physics-paint-rail-set-*) — physicsPaintStudio.css
- New files: app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts
</output>
