---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 10
type: execute
wave: 7
depends_on: ["43.6-07", "43.6-08", "43.6-09"]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
autonomous: true
requirements: [RSET-01, RSET-02, BDEL-01]
gap_closure: true
gap_ids: [WR-01, WR-02]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Plain-clicking rail A then Cmd+clicking (or Shift+clicking) rail B yields a set anchored on A for ANY rail-type pair — the plain-selected rail is carried into the set, never dropped (UI-SPEC M1, phase goal: explicit CROSS-TYPE rail set)"
    - "A modifier click on a Loop Clip rail ALWAYS commits set membership synchronously — a modifier click is never an open-editor double-click intent (43.6-08 Task 3 invariant, G-43.6-8 closure)"
  artifacts:
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  key_links:
    - "selectedRotoKeyRail.value / selectedLoopClipId.value (whichever is live, key-rail first) -> singleIdentity -> seedRailSetSelection -> updatePhysicsPaintRotoRailSetSelection in BOTH Studio handlers"
    - "Loop Clip modifier click inside the pending/220ms window -> gesture !== 'plain' synchronous branch -> onSelectLoopClip(loopId, gesture) -> railSetSelection -> .selected class -> isPhysicsPaintRotoDeleteTarget gate (physicsPaintStudioKeyboard.ts L54-88)"
---

<objective>
Close the two remaining verification gaps (WR-01, WR-02) from 43.6-VERIFICATION.md so the 43.6-08
seed bridge and synchronous-commit invariants hold on the cross-type and inside-the-window paths they
currently miss.

Purpose: WR-01 — `handleSelectRotoKeyRail` (PhysicsPaintStudio.tsx L1313-1316) and
`handleSelectRotoLoopClip` (L1375-1378) each derive the seed identity ONLY from their own rail type's
signal; plain-selecting one type nulls the other type's signal, so a cross-type plain-select ->
first-modifier-gesture sequence seeds null and silently drops the plain-selected rail (half of the
UI-SPEC M1 contract). WR-02 — PhysicsPaintLoopClipRail.tsx L228's double-click gate is gesture-blind
and sits BEFORE the 43.6-08 synchronous-commit branch (L241-245), so a modifier click inside the
250ms pending-single-click or 220ms fast-double-click window opens the Edit Group editor instead of
committing set membership.
Output: two surgical source edits (one derivation change per Studio handler, one gate-condition change
in the Loop Clip rail) plus regression tests proving both gesture paths. The fail-closed reducer
semantics in physicsPaintRotoRailSetSelection.ts are byte-untouched.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md

# The authoritative gap definitions (exact lines, exact missing changes)
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-VERIFICATION.md

# UI-SPEC M1 plain-selection carry rule
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md

# Established seed/commit patterns this plan extends
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED — failing regression tests for both gap paths (cross-type seed both directions; in-window modifier click)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (handleSelectRotoKeyRail L1303-1359, handleSelectRotoLoopClip L1360-1446 — the handlers under test and the exact slice boundaries the source-contract tests use)
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts (existing seed contract tests L485-527: 'routes Loop Rail modifier gestures...', 'routes Key Rail modifier gestures...', 'seeds the set from the plain-selected single-rail signals on the first modifier gesture (43.6-08 M1)')
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (handleClick L197-252 — gesture computation L212-218, the double-click gate L228, the synchronous modifier branch L241-245)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx (the existing fake-timers component test 'commits modifier-click set membership synchronously with no single-click timer (43.6-08 G-43.6-8)' L892-960 — reuse its fixture/harness verbatim)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-08-PLAN.md (the seed/commit patterns and test conventions this plan extends)
  </read_first>
  <behavior>
    - Studio test 1 (cross-type, loop-first direction): the handleSelectRotoLoopClip source slice (from 'const handleSelectRotoLoopClip = useCallback((' to 'const handleOpenRotoLoopEdit') derives its seed from the OPPOSITE type's live signal too — it must contain a cross-type guard reading `selectedRotoKeyRail.value !== null` (currently absent in that slice — RED).
    - Studio test 2 (cross-type, key-rail-first direction): the handleSelectRotoKeyRail source slice (from 'const handleSelectRotoKeyRail = useCallback((' to 'const handleSelectRotoLoopClip') must contain a cross-type guard reading `selectedLoopClipId.value !== null` (currently absent in that slice — RED). Use the `.value` suffixed token so `selectedLoopClipIds.value` cannot false-positive.
    - Studio test 3 (derivation order): both handler slices derive the seed with the key-rail signal checked before the loop signal (indexOf ordering assertion inside each slice), per the verification report's mandated derivation order.
    - Loop Clip component test (WR-02): with vi.useFakeTimers and the existing fixture pattern, fire a plain click on the Loop rail target, then WITHOUT advancing past LOOP_CLIP_SINGLE_CLICK_DELAY_MS fire a Cmd+click (metaKey: true) at a timeStamp within the pending window; assert onSelectLoopClip received ['loop-a', 'toggle'] synchronously and NEVER a 'plain' call from the second click, and assert onOpenLoopEdit was never called (currently the gate routes the second click to the editor branch — RED).
    - These tests must FAIL at this commit for the stated reasons (RED), and no existing test may be modified or deleted in this task.
  </behavior>
  <action>
    Add tests only — no source changes. In PhysicsPaintStudio.test.ts, extend the 'Physics Paint multi-rail selection SET wiring (43.6-01)' describe block (immediately after the existing 43.6-08 M1 seed test at L512-527) with one new it block asserting both cross-type derivations and the key-rail-first ordering, using the same studio.indexOf/slice source-contract pattern as the neighbors. In PhysicsPaintLoopClipRail.test.tsx, add one new it block directly after the existing synchronous-commit test (L892-960) using its exact fixture setup (one progressive clip 'loop-a', derivePhysicPaintRotoLoopRanges, projectPhysicsPaintLoopClipPresentation, materializeNamedComponents over PhysicsPaintLoopClipRailTarget) but with an onOpenLoopEdit vi.fn() spy that is asserted never-called. Run both suites and confirm the NEW assertions fail for the stated reasons while every pre-existing test still passes. Commit `test(43.6-10): add failing cross-type seed and in-window modifier click regression tests`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The new Studio it block fails on its cross-type assertions (both slices lack the opposite-type guard today).
    - The new Loop Clip component test fails because onOpenLoopEdit IS called by the in-window Cmd+click today (and/or onSelectLoopClip receives a second 'plain' call instead of 'toggle').
    - Every pre-existing test in both files still passes (RED is confined to the new assertions).
    - RED commit exists with the test filenames in the message.
  </acceptance_criteria>
  <done>
    Both gap paths are pinned by failing tests; the RED state is committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — cross-type seed derivation in both Studio handlers (WR-01)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (handleSelectRotoKeyRail L1303-1359 including its useCallback dependency array at L1359; handleSelectRotoLoopClip L1360-1446 including its dependency array at L1446; clearRotoLoopSelection L1295-1302 for context on which signals a plain click nulls)
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts (seedRailSetSelection L230-238, RailSetIdentity type L20-22 — unchanged, consumed as-is)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-08-PLAN.md (Task 2 wiring pattern this task mirrors)
  </read_first>
  <action>
    In PhysicsPaintStudio.tsx, widen the seed derivation in BOTH modifier branches per the verification report's missing-change directive: in handleSelectRotoKeyRail (currently L1313-1315) and symmetrically in handleSelectRotoLoopClip (currently L1375-1377), derive singleIdentity from whichever single-rail signal is live — check selectedRotoKeyRail.value first (yielding the key-rail identity from its firstKeyId), else selectedLoopClipId.value (yielding the loop identity), else null. Both handlers end up with the IDENTICAL two-signal derivation, because a plain click on either type nulls the other type's signal (L1346 via clearRotoLoopSelection, L1426 directly), so exactly one signal can be live and the key-rail-first order is deterministic. Keep the existing seedRailSetSelection(railSetSelection.peek(), singleIdentity) call and the updatePhysicsPaintRotoRailSetSelection(currentSet, ...) call byte-identical. Add selectedLoopClipId to the handleSelectRotoKeyRail useCallback dependency array (L1359) and selectedRotoKeyRail to the handleSelectRotoLoopClip dependency array (L1446). Do NOT touch: the plain-click branches, the next === null branches, clearRotoLoopSelection, seedRailSetSelection, or anything in physicsPaintRotoRailSetSelection.ts. Do NOT modify or weaken the Task 1 tests — they must pass as written.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the handleSelectRotoKeyRail slice (up to 'const handleSelectRotoLoopClip') contains `selectedLoopClipId.value !== null` and the handleSelectRotoLoopClip slice (up to 'const handleOpenRotoLoopEdit') contains `selectedRotoKeyRail.value !== null`.
    - Source assertion: in both slices the `selectedRotoKeyRail.value !== null` guard precedes the `selectedLoopClipId.value !== null` guard (key-rail-first derivation order).
    - Source assertion: `seedRailSetSelection(railSetSelection.peek(),` still appears in both handler slices (43.6-08 bridge preserved).
    - PhysicsPaintStudio.test.ts exits 0 (all pre-existing tests plus the new cross-type assertions green); physicsPaintRotoRailSetSelection.test.ts exits 0 (39/39 — reducer untouched).
    - git diff on physicsPaintRotoRailSetSelection.ts is empty.
  </acceptance_criteria>
  <done>
    A plain-selected Loop rail seeds the set when the first modifier gesture lands on a Key rail and vice versa; the Studio and reducer suites are green; commit `fix(43.6-10): seed the rail set from the live cross-type single-rail signal (WR-01)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — gesture-aware double-click gate on Loop Clip rails (WR-02) plus full regression sweep</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (handleClick L197-252 — gesture computation L212-218, the double-click gate and its comment L223-234, the synchronous modifier branch L241-245, the plain-click timer tail L246-251)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx (the Task 1 in-window test plus the existing double-click/plain-timing tests that must stay green)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-VERIFICATION.md (gap 2 'missing' directive — the exact gate condition)
  </read_first>
  <action>
    In PhysicsPaintLoopClipRail.tsx, make the double-click gate gesture-aware per the verification report's missing-change directive: the gate at L228 fires only for the plain gesture — add a leading plain-gesture condition to the existing `hasPendingSingleClick || (elapsed >= 0 && elapsed <= LOOP_CLIP_FAST_DOUBLE_CLICK_MS)` expression so modifier clicks fall through to the 43.6-08 synchronous-commit branch (L241-245) even inside the pending/220ms window. The gesture const is already computed at L212-218 above the gate, so no reordering is needed. Update the adjacent comment (L223-226) so it states the dead-zone close applies to plain double-click intent only, and that modifier clicks are never open-editor intent (keeps the WR-02 rationale accurate at the new line). Do NOT touch: the gesture computation, the synchronous modifier branch, the plain-click timer, clearPendingSingleClick/clearClickSequence, or any other branch. Do NOT modify the Task 1 component test — it must pass as written. After the Loop Clip suite is green, run the full five-suite regression sweep from the verification report and confirm 302+ tests pass (the two new tests raise the count).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the gate condition in PhysicsPaintLoopClipRail.tsx contains a `gesture === 'plain'` conjunct guarding the `hasPendingSingleClick` / fast-double-click window expression.
    - Source assertion: the synchronous modifier branch (the `gesture !== 'plain'` early return calling onSelectLoopClip) still exists below the gate and is reachable for modifier clicks inside the window.
    - PhysicsPaintLoopClipRail.test.tsx exits 0, including the Task 1 in-window modifier test (onOpenLoopEdit never called, synchronous 'toggle' commit) and all pre-existing plain-click timing and double-click editor tests.
    - The full five-suite sweep exits 0 with zero failures (302 pre-existing tests plus the tests added in Task 1).
    - Typecheck passes: `cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec tsc --noEmit`.
  </acceptance_criteria>
  <done>
    A modifier click inside the pending/220ms window commits set membership synchronously and never opens the Edit Group editor; plain double-click still opens the editor; the five-suite sweep and typecheck are green; commit `fix(43.6-10): gate the Loop Clip double-click branch on plain gestures (WR-02)`.
  </done>
</task>

</tasks>

## Artifacts this phase produces

This gap-closure plan modifies EXISTING functions only — no new exports, no new modules, no API changes.
New test surface (all added in Task 1):

- `PhysicsPaintStudio.test.ts` — one new it block in the 'Physics Paint multi-rail selection SET wiring (43.6-01)' describe asserting both handlers derive the seed from the opposite rail type's live signal with key-rail-first ordering (WR-01 regression, both cross-type directions).
- `PhysicsPaintLoopClipRail.test.tsx` — one new it block next to the 43.6-08 synchronous-commit test: plain click then in-window Cmd+click commits 'toggle' synchronously and never calls onOpenLoopEdit (WR-02 regression).

Modified (existing) production symbols:

- `handleSelectRotoKeyRail` (PhysicsPaintStudio.tsx) — seed derivation widened to both single-rail signals; useCallback deps gain selectedLoopClipId.
- `handleSelectRotoLoopClip` (PhysicsPaintStudio.tsx) — seed derivation widened to both single-rail signals; useCallback deps gain selectedRotoKeyRail.
- `handleClick` (PhysicsPaintLoopClipRail.tsx) — double-click gate gains a plain-gesture conjunct; adjacent comment updated.

Explicitly NOT produced: no changes to physicsPaintRotoRailSetSelection.ts (fail-closed reducer semantics byte-untouched), no changes to useRotoPhysicalEditHistory.ts, PhysicsPaintWorkflowStrip.tsx, or any plan 01–09 artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail click gesture -> set reducer input | gesture/identity flows into session-only selection state; the reducer validates and reconcile fails closed (unchanged by this plan) |

## STRIDE Threat Register

This is a UI-selection gap closure: no untrusted external input, no network surface, no persistence, no new dependencies. Two items considered and dismissed rather than invented:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-10-01 | Tampering | seedRailSetSelection cross-type input | low | accept | Considered and dismissed: the widened derivation feeds the SAME fail-closed helper/reducer path (isRailSetIdentity validation + reconcileRailSetSelection staleness drop) already tested in plans 01/08; this plan adds no new input authority, and physicsPaintRotoRailSetSelection.ts is byte-untouched. |
| T-43.6-10-02 | Denial of Service | gesture-aware double-click gate | low | accept | Considered and dismissed: the gate change only re-routes modifier clicks to the existing synchronous-commit branch; plain-click timer and double-click editor behavior are preserved and pinned by pre-existing tests. Session-only state, no resource consumption change. |
</threat_model>

<verification>
- Full five-suite regression sweep (from 43.6-VERIFICATION.md): `cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` — all green.
- Full physic-paint suite: `cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint` — all green.
- Typecheck: `cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec tsc --noEmit` passes.
- Reducer untouched: `git diff --stat` shows zero changes to physicsPaintRotoRailSetSelection.ts and useRotoPhysicalEditHistory.ts.
</verification>

<success_criteria>
- WR-01 closed: plain-select Loop A then Cmd+click (or Shift+click) Key Rail B — and the reverse — yields a set carrying A, anchored on A, for every rail-type pair.
- WR-02 closed: a modifier click on a Loop Clip rail inside the 250ms pending / 220ms fast-double-click window commits set membership synchronously and never opens the Edit Group editor.
- RSET-01, RSET-02, BDEL-01 move from PARTIAL to satisfiable at re-verification.
- NOTE (sequencing, per 43.6-VERIFICATION.md UAT cross-reference): native UAT re-runs for G-43.6-1, G-43.6-3, G-43.6-4, and G-43.6-8 are scheduled AFTER this closure executes — do not mark those UAT gaps resolved at code-green time.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-10-SUMMARY.md` when done
</output>
