---
status: passed
phase: 43.6-multi-rail-selection-and-batch-operations
source: [43.6-VERIFICATION.md]
started: 2026-08-19T14:05:00Z
updated: 2026-08-19T16:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Native UAT re-run G-43.6-1 — Set gestures and Solo disarm on plain Loop Rail click
expected: Plain-clicking a different Loop rail while Solo is armed ends the arm before the selection moves; cross-type set gestures seed from the plain-selected rail with key-rail-first canonical order; no silent solo retarget. End-to-end pointer-interaction behavior with live playback state requires native UI interaction; source-contract tests pin wiring but cannot exercise the live click pipeline.
result: pass

### 2. Native UAT re-run G-43.6-3 — Delete Rails atomicity with a multi-rail set active
expected: Delete/Backspace routes to Delete Rails; `Delete {N} Rails` scope announced; one command removes the set; one Undo restores every rail/key/break and the exact pre-delete set; Cmd+Shift+Z redo replays the deletion. Live keyboard+pointer routing and visual document state transition; code-level allowlist and round-trip tests are green but do not cover native interaction.
result: pass
reported: "Delete work with multi-select but without undo/redo. Delete with one rail select work with undo/redo but the multi-select with delete and undo test blocked the all delete for one select or multi-select, it happen one time I had not success to reproduce this bug, relaunch the app resolved the delete."
severity: major
code_fix: "G-43.6-2 closed at code level by plans 43.6-12 (bridge before-snapshot records true pre-op selection for delete-rails) and 43.6-13 (recovery-lease self-heal in executePhysicalEdit). Verification 9/9."
rerun_result: "PASSED natively 2026-08-19: Delete Rails commits as one command; one Undo restores every rail/key/break and the exact pre-delete selection set; Redo re-applies. No residual delete-block after a rejected edit — Delete works immediately without relaunching."

### 3. Native UAT re-run G-43.6-4 — Move Rails rigid translation
expected: Drag any selected rail; ghosting at 55% kind color; clamped drop at the red bar; rigid internal gaps; unselected neighbors untouched; one accepted move = one applied history command; undo/redo replay exact pre/post document snapshots; the set stays anchored on the plain-selected rail after the drag. Drag physics, ghost rendering, and clamp behavior are visual/runtime; history allowlist and round-trip tests are green but do not cover native drag interaction.
result: pass

### 4. Native UAT re-run G-43.6-8 — Focus and shortcut parity with a multi-rail set
expected: Delete/Backspace and Cmd+Z/Cmd+Shift+Z behave exactly as for single rails (now that the in-window synchronous modifier commit makes Delete reach the set); roving Tab/arrows cycle set members in canonical order; undo/redo atomic across the set. Keyboard focus traversal and shortcut parity require live interaction with focus state.
result: pass
reported: "pass except undo shortcuts with multi selection, reported in previous checkpoint: undo/redo no work except with one selection"
severity: major
note: "Same root cause as G-43.6-2 — undo/redo broken with multi-selection. Not a separate gap; covered by G-43.6-2 gap-closure (plans 43.6-12/13)."
rerun_result: "PASSED natively 2026-08-19: Cmd+Z / Cmd+Shift+Z parity confirmed with a multi-rail set active, for both Delete Rails and Move Rails."

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-43.6-2
  truth: "Delete Rails with a multi-rail set active: one command removes the set; one Undo restores every rail/key/break and the exact pre-delete set; Cmd+Shift+Z redo replays the deletion."
  status: resolved
  reason: "User reported: Delete work with multi-select but without undo/redo. Delete with one rail select work with undo/redo but the multi-select with delete and undo test blocked the all delete for one select or multi-select, it happen one time I had not success to reproduce this bug, relaunch the app resolved the delete."
  severity: major
  test: 2
  root_cause: "For multi-rail delete-rails, the coordinator overwrites targetSelectedKeyId/targetCursorAppFrame with POST-delete proposal values and ships them in the payload. Since delete-rails is in GROUP_LIFECYCLE_OPERATION_KINDS, the bridge's childBeforeAuthority branch records that POST-delete selection as the 'before' snapshot. On undo, the history hook submits the pre-delete selection, which fails the replay-target equality check (sameAcceptedPhysicalCommandSnapshot) and silently no-ops undo. Single-rail delete-key-rail is not in GROUP_LIFECYCLE_OPERATION_KINDS, so it records the true pre-op selection and undoes correctly — explaining the single/multi asymmetry."
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      issue: "targetSelectedKeyId/targetCursorAppFrame overwritten with POST-delete proposal values for rail-set deletes (:1585-1608, :1868-1870)"
    - path: "app/src/lib/physicPaintBridge.ts"
      issue: "GROUP_LIFECYCLE_OPERATION_KINDS + childBeforeAuthority misrecord the before snapshot selection (:1039-1047, :1633-1652)"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      issue: "undo submits pre-delete selection that can never match the recorded before (:630)"
    - path: "app/src/lib/physicPaintBridge.ts"
      issue: "replay-target equality check rejects on selection mismatch (:1583-1606)"
  missing:
    - "For delete-rails, record the true pre-operation selection/cursor in the ledger — exclude selection fields from childBeforeAuthority branch for delete-rails, or stop overwriting targetSelectedKeyId with post-delete proposal values in the coordinator"
    - "Wire a real caller for releasePhysicalEditRecoveryLease (or clear the recovery lease on successful settlement) to fix the transient delete-block — useRotoPhysicalEditCoordinator.ts:1260-1264 + physicPaintStore.ts:1480-1485 + PhysicsPaintStudio.tsx:627-642"
  debug_session: ".planning/debug/43-6-undo-redo-multi-rail-set.md"
