---
status: complete
phase: 43.6-multi-rail-selection-and-batch-operations
source: 43.6-01-SUMMARY.md, 43.6-02-SUMMARY.md, 43.6-03-SUMMARY.md, 43.6-04-SUMMARY.md, 43.6-05-SUMMARY.md, 43.6-06-SUMMARY.md
started: 2026-08-19T05:31:20Z
updated: 2026-08-19T05:36:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Set gestures
expected: Shift+click selects the canonical anchor→clicked range across all three rail kinds in first-key order; Cmd+click toggles one rail; Cmd+Shift+click unions the range; the anchor tick marks the anchor; the capsule shows `3 Rails selected — frames 12–88 (1 Motion, 2 Key).`-form copy.
result: issue
reported: "Cmd+click works perfectly, but Shift+click does nothing. So I can't test Cmd+Shift+click either."
severity: major

### 2. Empty set and collapse
expected: Cmd+clicking off the last member leaves a clean no-selection state; plain click collapses to one rail; frame/key selection clears the set; Escape peels solo then selection one layer at a time.
result: issue
reported: "All pass except in UI solo need to stay activated when I enter in solo mode."
severity: major

### 3. Delete Rails atomicity
expected: `Delete {N} Rails` scope announced before activation; one command removes the set with per-type semantics; intervals stay neutral intentional gaps through Interpolation Off/On and save/reopen; one Undo restores every rail, key, break, and the exact pre-delete set.
result: issue
reported: "I multiple select rails, only the last rail is deleted, and the undo/redo no work."
severity: major

### 4. Move Rails rigid translation
expected: Dragging any selected rail ghosts every member at 55% kind color; originals stay at 100% with orange lines; clamped drop commits at the clamp with the red bar on the colliding member's edge; internal gaps/breaks travel rigidly; unselected neighbors untouched; accepted status `Moved {N} Rails by {D} frames — set now frames {A}–{B}.` (+ gap sentence only when opened).
result: issue
reported: "Work but undo/redo no work, when a rail is already selected (without cmd) and I select another rail with cmd, I most the first rail from selection, I need to reselect with cmd for have the 2 rails selected."
severity: major

### 5. Move rejection paths
expected: Zero-space drag publishes `No empty space in that direction.` with zero mutation; straddle publishes the locked Group-domain sentence; zero-delta drop publishes nothing; Escape cancels with byte-untouched originals.
result: pass

### 6. Key Spacing on set
expected: The popover shows the scope line only with an active set; Apply respaces every selected rail with per-rail anchors as one command; any rail failing preflight rejects the whole command with the mapped reason; Undo/Redo atomic.
result: issue
reported: "Key Spacing works on grouped rails, but undo/redo does not work. The undo/redo shortcuts used in other sections should apply here too."
severity: major

### 7. Solo playback
expected: Solo enables on any rail selection; armed shows the orange tint plus persistent `Solo — {N} Rails, frames {A}–{B}.`; playing renders only selected rails' content within their effective range and loops; stopped scrubbing renders everything everywhere; exit on re-click/Escape/selection change; save/reopen always starts un-soloed; preview/export output is byte-identical with and without solo.
result: issue
reported: "Solo playback work perfect but when activated I'd would like to see the icon in orange until its actif."
severity: major

### 8. Focus and shortcut parity
expected: Roving Tab/arrows cycle set members in canonical order; Delete/Backspace and Cmd+Z/Cmd+Shift+Z behave exactly as for single rails; focus ring on the focused member only; no new shortcuts.
result: issue
reported: "Pass except Delete/Backspace and Cmd+Z/Cmd+Shift+Z no work with multi-rails."
severity: major

### 9. Preserved neighbors
expected: Single-rail behavior, 43.5 Push tools (never consuming the set), ToolCase layout beyond the scope line, and all 43.2–43.4 contracts remain unchanged.
result: pass

### 2. Empty set and collapse
expected: Cmd+clicking off the last member leaves a clean no-selection state; plain click collapses to one rail; frame/key selection clears the set; Escape peels solo then selection one layer at a time.
result: [pending]

### 3. Delete Rails atomicity
expected: `Delete {N} Rails` scope announced before activation; one command removes the set with per-type semantics; intervals stay neutral intentional gaps through Interpolation Off/On and save/reopen; one Undo restores every rail, key, break, and the exact pre-delete set.
result: [pending]

### 4. Move Rails rigid translation
expected: Dragging any selected rail ghosts every member at 55% kind color; originals stay at 100% with orange lines; clamped drop commits at the clamp with the red bar on the colliding member's edge; internal gaps/breaks travel rigidly; unselected neighbors untouched; accepted status `Moved {N} Rails by {D} frames — set now frames {A}–{B}.` (+ gap sentence only when opened).
result: [pending]

### 5. Move rejection paths
expected: Zero-space drag publishes `No empty space in that direction.` with zero mutation; straddle publishes the locked Group-domain sentence; zero-delta drop publishes nothing; Escape cancels with byte-untouched originals.
result: [pending]

### 6. Key Spacing on set
expected: The popover shows the scope line only with an active set; Apply respaces every selected rail with per-rail anchors as one command; any rail failing preflight rejects the whole command with the mapped reason; Undo/Redo atomic.
result: [pending]

### 7. Solo playback
expected: Solo enables on any rail selection; armed shows the orange tint plus persistent `Solo — {N} Rails, frames {A}–{B}.`; playing renders only selected rails' content within their effective range and loops; stopped scrubbing renders everything everywhere; exit on re-click/Escape/selection change; save/reopen always starts un-soloed; preview/export output is byte-identical with and without solo.
result: [pending]

### 8. Focus and shortcut parity
expected: Roving Tab/arrows cycle set members in canonical order; Delete/Backspace and Cmd+Z/Cmd+Shift+Z behave exactly as for single rails; focus ring on the focused member only; no new shortcuts.
result: [pending]

### 9. Preserved neighbors
expected: Single-rail behavior, 43.5 Push tools (never consuming the set), ToolCase layout beyond the scope line, and all 43.2–43.4 contracts remain unchanged.
result: [pending]

**Coverage auto-passed entries (#1602):** recorded as automated — not presented as checkpoints.

### A1. Pure rail-set reducer with plain/toggle/range/union gestures, canonical cross-type ordering, and fail-closed reconcile
expected: Pure rail-set reducer with plain/toggle/range/union gestures, canonical cross-type ordering, and fail-closed reconcile
result: pass
source: automated
coverage_id: D1

### A2. Studio session rail-set signal with render-phase reconcile and lifecycle wiring (collapse/clear/Escape layer/launch replacement)
expected: Studio session rail-set signal with render-phase reconcile and lifecycle wiring (collapse/clear/Escape layer/launch replacement)
result: pass
source: automated
coverage_id: D2

### A3. Modifier routing in both rail components (union-first), orange set paint on every member, and the 2x8px anchor tick
expected: Modifier routing in both rail components (union-first), orange set paint on every member, and the 2x8px anchor tick
result: pass
source: automated
coverage_id: D3

### A4. One deterministic set-copy mapper (buildRailSetCopy) feeding the status capsule and M1 tooltip set sentences
expected: One deterministic set-copy mapper (buildRailSetCopy) feeding the status capsule and M1 tooltip set sentences
result: pass
source: automated
coverage_id: D4

### A5. Post-acceptance set-aftermath scaffolding (D-06 carrier): snapshot side-channel keyed by operationId, pruned on launch replacement
expected: Post-acceptance set-aftermath scaffolding (D-06 carrier): snapshot side-channel keyed by operationId, pruned on launch replacement
result: pass
source: automated
coverage_id: D5

### A6. Exported pure set-derivation + clamp authority for the explicit rail set (derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta) with exact-match membership, moved-key set, and the generalized straddle verdict
expected: Exported pure set-derivation + clamp authority for the explicit rail set (derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta) with exact-match membership, moved-key set, and the generalized straddle verdict
result: pass
source: automated
coverage_id: D1

### A7. move-rails intent branch: rigid one-unit translation preserving relative offsets/internal gaps, set-level clamp/reject at unselected key / unselected Group occupancy / frame 0 / capacity, zero-delta valid no-change proposal
expected: move-rails intent branch: rigid one-unit translation preserving relative offsets/internal gaps, set-level clamp/reject at unselected key / unselected Group occupancy / frame 0 / capacity, zero-delta valid no-change proposal
result: pass
source: automated
coverage_id: D2

### A8. Shared-source algebra: source-attached Groups move source keys with identity and placementStart follows via nextLoopClips; duplicated placements move placement-only; unselected Group sharing a source cycle rejects fail-closed with 'move-rails-source-straddle'
expected: Shared-source algebra: source-attached Groups move source keys with identity and placementStart follows via nextLoopClips; duplicated placements move placement-only; unselected Group sharing a source cycle rejects fail-closed with 'move-rails-source-straddle'
result: pass
source: automated
coverage_id: D3

### A9. D-11 break/gap travel: internal breaks travel with moved key identity, vacated-successor break landing, landing-gap rule, no-merge on adjacent landing; nextIncomingInterpolationBreakKeyIds carries the complete next collection
expected: D-11 break/gap travel: internal breaks travel with moved key identity, vacated-successor break landing, landing-gap rule, no-merge on adjacent landing; nextIncomingInterpolationBreakKeyIds carries the complete next collection
result: pass
source: automated
coverage_id: D4

### A10. Transport + coordinator wiring: 'move-rails' in the operation-kind union and both strict parsers, intent payload round-trip, coordinator authorization pairing the exact intent
expected: Transport + coordinator wiring: 'move-rails' in the operation-kind union and both strict parsers, intent payload round-trip, coordinator authorization pairing the exact intent
result: pass
source: automated
coverage_id: D5

### A11. usePhysicsPaintRailSetDrag pointer-session hook: L140 modifier guard verbatim, 4px threshold, preview-is-the-commit (prepared publication committed verbatim), zero-delta publishes nothing, prepare-failure rejection at drop, Escape/pointercancel/lostpointercapture cleanup exactly once, click suppression armed only after start
expected: usePhysicsPaintRailSetDrag pointer-session hook: L140 modifier guard verbatim, 4px threshold, preview-is-the-commit (prepared publication committed verbatim), zero-delta publishes nothing, prepare-failure rejection at drop, Escape/pointercancel/lostpointercapture cleanup exactly once, click suppression armed only after start
result: pass
source: automated
coverage_id: D1

### A12. D-08 collapse-first routing in both rail families: set member pointer-down routes to the batch session (never the own drag), non-member runs its own 43.3/43.4 drag unchanged, set-member trailing click consumes the batch suppression first
expected: D-08 collapse-first routing in both rail families: set member pointer-down routes to the batch session (never the own drag), non-member runs its own 43.3/43.4 drag unchanged, set-member trailing click consumes the batch suppression first
result: pass
source: automated
coverage_id: D2

### A13. Per-member ghost/gap/blocked-edge preview paint: 55% kind colors (Motion #8B5CF6 / Static #06B6D4 / Key Rail #8A939C), 2x12px #FF6B6B bar on the colliding member's blocked edge, roto-fill-empty gap cells, ghosts on a layer between 7 and 8, all aria-hidden + pointer-events none
expected: Per-member ghost/gap/blocked-edge preview paint: 55% kind colors (Motion #8B5CF6 / Static #06B6D4 / Key Rail #8A939C), 2x12px #FF6B6B bar on the colliding member's blocked edge, roto-fill-empty gap cells, ghosts on a layer between 7 and 8, all aria-hidden + pointer-events none
result: pass
source: automated
coverage_id: D3

### A14. Locked copy via the one mapper: live readout 'Move Rails +{D} - set frames {A}-{B} -> {A'}-{B'}' (U+2212 leftward, ', gap {G1}-{G2}' only when a gap would open), accepted 'Moved {N} Rails by {D} frames - set now frames {A}-{B}.' with singular 'Moved 1 Rail', straddle sentence verbatim, 'No empty space in that direction.'; live readout flows through the capsule pendingOperation chain only
expected: Locked copy via the one mapper: live readout 'Move Rails +{D} - set frames {A}-{B} -> {A'}-{B'}' (U+2212 leftward, ', gap {G1}-{G2}' only when a gap would open), accepted 'Moved {N} Rails by {D} frames - set now frames {A}-{B}.' with singular 'Moved 1 Rail', straddle sentence verbatim, 'No empty space in that direction.'; live readout flows through the capsule pendingOperation chain only
result: pass
source: automated
coverage_id: D4

### A15. prepareRailSetMove / commitRailSetMove ports: intent shape from set members + clamped delta, proposal version attached, commit revalidates the CURRENT proposal version before dispatch and fails closed with zero mutation on mismatch (T-43.6-02)
expected: prepareRailSetMove / commitRailSetMove ports: intent shape from set members + clamped delta, proposal version attached, commit revalidates the CURRENT proposal version before dispatch and fails closed with zero mutation on mismatch (T-43.6-02)
result: pass
source: automated
coverage_id: D5

### A16. D-06 set aftermath: recordRailSetSnapshot keyed by the accepted operationId before dispatch, resolveRailSetPostAcceptance on 'move-rails' acceptance (keep set) and on undo/redo via historyProvenance.historyCommandId — the set stays selected at the new positions and restores exactly
expected: D-06 set aftermath: recordRailSetSnapshot keyed by the accepted operationId before dispatch, resolveRailSetPostAcceptance on 'move-rails' acceptance (keep set) and on undo/redo via historyProvenance.historyCommandId — the set stays selected at the new positions and restores exactly
result: pass
source: automated
coverage_id: D6

### A17. proposePhysicPaintRotoDeleteRails pure proposer: validation loop (unknown-member/stale-member/duplicate-member/malformed-member), deterministic order (placementStart asc, loopId asc), sequential composition over the accumulating document (loop -> proposePhysicPaintRotoDeleteGroup; key-rail -> applyKeyRailDeletion with mid-composition segment re-derivation and 'stale-member' fail-closed), impact { kind: 'delete-rails', members, cleanupKeyIds, previousRevision, nextRevision }
expected: proposePhysicPaintRotoDeleteRails pure proposer: validation loop (unknown-member/stale-member/duplicate-member/malformed-member), deterministic order (placementStart asc, loopId asc), sequential composition over the accumulating document (loop -> proposePhysicPaintRotoDeleteGroup; key-rail -> applyKeyRailDeletion with mid-composition segment re-derivation and 'stale-member' fail-closed), impact { kind: 'delete-rails', members, cleanupKeyIds, previousRevision, nextRevision }
result: pass
source: automated
coverage_id: D1

### A18. 'delete-rails' transport: operation-kind union + runtime guard, semantic-delta union with strict parsers (bounded ids, discriminant, non-empty, unique rails, valid revision transition), coordinator RotoRailSetDeleteExecuteInput with no child intent, semanticDeltaEquals delete-rails branch, bridge recompute via the shared proposer + exact-match with distinct error strings
expected: 'delete-rails' transport: operation-kind union + runtime guard, semantic-delta union with strict parsers (bounded ids, discriminant, non-empty, unique rails, valid revision transition), coordinator RotoRailSetDeleteExecuteInput with no child intent, semanticDeltaEquals delete-rails branch, bridge recompute via the shared proposer + exact-match with distinct error strings
result: pass
source: automated
coverage_id: D2

### A19. classifyRotoDeleteTarget 'rail-set' branch FIRST: active mixed set classifies as { kind: 'rail-set', members, firstFrame, lastFrame } before any single-rail branch (even with selectedLoopClipIds populated); stale key-rail member and stale loop member both fail closed as 'stale-key-rail' — never deletable
expected: classifyRotoDeleteTarget 'rail-set' branch FIRST: active mixed set classifies as { kind: 'rail-set', members, firstFrame, lastFrame } before any single-rail branch (even with selectedLoopClipIds populated); stale key-rail member and stale loop member both fail closed as 'stale-key-rail' — never deletable
result: pass
source: automated
coverage_id: D3

### A20. Locked copy verbatim: scope labels 'Delete 3 Rails' / 'Delete 1 Rail' (accessible name + guarded tooltip heading only; visible label stays 'Delete'), accepted 'Deleted 3 Rails - frames 12-88. The intervals stay intentional gaps.' and 'Deleted 1 Rail - frames 12-40. The interval stays an intentional gap.' via the one mapper
expected: Locked copy verbatim: scope labels 'Delete 3 Rails' / 'Delete 1 Rail' (accessible name + guarded tooltip heading only; visible label stays 'Delete'), accepted 'Deleted 3 Rails - frames 12-88. The intervals stay intentional gaps.' and 'Deleted 1 Rail - frames 12-40. The interval stays an intentional gap.' via the one mapper
result: pass
source: automated
coverage_id: D4

### A21. Direct no-modal dispatch: deleteRotoFrame 'rail-set' branch calls executeRailSetDelete with the classified members — no runPhysicalAction, no confirmation/warning call at any set size; rejection publishes the mapped reason and mutates nothing (no success status, no history entry)
expected: Direct no-modal dispatch: deleteRotoFrame 'rail-set' branch calls executeRailSetDelete with the classified members — no runPhysicalAction, no confirmation/warning call at any set size; rejection publishes the mapped reason and mutates nothing (no success status, no history entry)
result: pass
source: automated
coverage_id: D5

### A22. D-06 delete aftermath: accepted 'delete-rails' empties the set (resolveRailSetPostAcceptance -> null) and single-rail signals collapse to the accepted key (cursor stays on its current valid frame); the snapshot records before = pre-delete set, after = null so Undo restores the exact pre-delete set and Redo clears it again
expected: D-06 delete aftermath: accepted 'delete-rails' empties the set (resolveRailSetPostAcceptance -> null) and single-rail signals collapse to the accepted key (cursor stays on its current valid frame); the snapshot records before = pre-delete set, after = null so Undo restores the exact pre-delete set and Redo clears it again
result: pass
source: automated
coverage_id: D6

### A23. spacing-on-set resolver intent: per-rail fixed anchors (each rail anchors at its OWN first key's current frame; member key i -> anchor + i*(emptyFrames+1); Loop member scope = source cycle keys anchored at the first source key's frame), hard-wall collisions (equality + left-to-right crossing vs unselected keys), all-or-nothing validation (D-25), D-10 straddle guard with 'rails-spacing-source-straddle', break travel with moved key identity, exact-match membership, no-change when already exact
expected: spacing-on-set resolver intent: per-rail fixed anchors (each rail anchors at its OWN first key's current frame; member key i -> anchor + i*(emptyFrames+1); Loop member scope = source cycle keys anchored at the first source key's frame), hard-wall collisions (equality + left-to-right crossing vs unselected keys), all-or-nothing validation (D-25), D-10 straddle guard with 'rails-spacing-source-straddle', break travel with moved key identity, exact-match membership, no-change when already exact
result: pass
source: automated
coverage_id: D1

### A24. 'spacing-on-set' transport: intent union member + strict parser + serializer, operation-kind union + runtime guard + ordinary-kind guard entries, coordinator createAuthorizedPhysicalEditPayload case
expected: 'spacing-on-set' transport: intent union member + strict parser + serializer, operation-kind union + runtime guard + ordinary-kind guard entries, coordinator createAuthorizedPhysicalEditPayload case
result: pass
source: automated
coverage_id: D2

### A25. deriveForceSpacingScope rail-set branch FIRST: Plan 01 set authority expands identities into per-rail member descriptors (key-rail firstKeyId + keyIds via deriveKeyRailSegments / loop loopId), stale set fails closed with the mapped stale message, never invents fallback scope (D-26); applyForceSpacing dispatches spacing-on-set through the same coordinator discipline with the locked M5 copy (accepted plural/singular, rejection via mapSpacingOnSetProductReason, no-change via proposal.status.text)
expected: deriveForceSpacingScope rail-set branch FIRST: Plan 01 set authority expands identities into per-rail member descriptors (key-rail firstKeyId + keyIds via deriveKeyRailSegments / loop loopId), stale set fails closed with the mapped stale message, never invents fallback scope (D-26); applyForceSpacing dispatches spacing-on-set through the same coordinator discipline with the locked M5 copy (accepted plural/singular, rejection via mapSpacingOnSetProductReason, no-change via proposal.status.text)
result: pass
source: automated
coverage_id: D3

### A26. ToolCase popover scope line: ONE Body-role (11px/400) #9CA3AF tabular-nums line under the Key Spacing heading carrying the D-27 set copy verbatim when a set is active; with no set the line is absent and the popover is byte-identical to 43.5 (D-26); the popover never creates or modifies the set
expected: ToolCase popover scope line: ONE Body-role (11px/400) #9CA3AF tabular-nums line under the Key Spacing heading carrying the D-27 set copy verbatim when a set is active; with no set the line is absent and the popover is byte-identical to 43.5 (D-26); the popover never creates or modifies the set
result: pass
source: automated
coverage_id: D4

### A27. D-06 spacing aftermath: resolveRailSetPostAcceptance explicit 'spacing-on-set' branch keeps the current set (identities survive respacing) — one atomic history command, set stays selected after acceptance (D-24)
expected: D-06 spacing aftermath: resolveRailSetPostAcceptance explicit 'spacing-on-set' branch keeps the current set (identities survive respacing) — one atomic history command, set stays selected after acceptance (D-24)
result: pass
source: automated
coverage_id: D5

### A28. physicsPaintSoloArm.ts session arm module: isSoloArmed() subscribing read, toggleSolo() flips and returns true, disarmSolo() returns true ONLY when armed (one-Escape-one-layer consumption, D-04), disarm idempotence; NO commitInFlight guard (solo never gates mutations, D-14); doc-comment session-only contract — never persisted, serialized, or part of any document/history snapshot; save/reopen always starts disarmed
expected: physicsPaintSoloArm.ts session arm module: isSoloArmed() subscribing read, toggleSolo() flips and returns true, disarmSolo() returns true ONLY when armed (one-Escape-one-layer consumption, D-04), disarm idempotence; NO commitInFlight guard (solo never gates mutations, D-14); doc-comment session-only contract — never persisted, serialized, or part of any document/history snapshot; save/reopen always starts disarmed
result: pass
source: automated
coverage_id: D1

### A29. deriveSoloPlaybackWindow pure range + frame-attribution derivation: start = first selected placement start, endExclusive = last selected effective end (loop effectiveEnd includes generated/linked occurrences; Key Rail = lastKeyFrame + 1), clamped to capacity (D-19); attribution from accepted physical cells + loop ranges + Key Rail segments only (real cells whose keyId belongs to a selected rail, generated cells whose owning segment/loop belongs to a selected rail, empty cells inside a selected rail's span play transparent); frames outside every selected rail's span excluded; empty/unknown member set or malformed inputs fail closed to null; A3 single-attribution fixtures prove each frame attributes to at most one rail
expected: deriveSoloPlaybackWindow pure range + frame-attribution derivation: start = first selected placement start, endExclusive = last selected effective end (loop effectiveEnd includes generated/linked occurrences; Key Rail = lastKeyFrame + 1), clamped to capacity (D-19); attribution from accepted physical cells + loop ranges + Key Rail segments only (real cells whose keyId belongs to a selected rail, generated cells whose owning segment/loop belongs to a selected rail, empty cells inside a selected rail's span play transparent); frames outside every selected rail's span excluded; empty/unknown member set or malformed inputs fail closed to null; A3 single-attribution fixtures prove each frame attributes to at most one rail
result: pass
source: automated
coverage_id: D2

### A30. getFrames solo filter seam: optional getSoloWindow playback input port; absent or null returns the byte-identical 43.5 enumeration (disarmed = zero behavior change); armed enumerates ONLY [start, endExclusive) intersected with the playback end and yields { appFrame, frame: null } for in-range frames failing attribution — the existing missing-frame transparent/background treatment hides unselected content with zero rendering changes (D-17); downstream absorption (empty list -> status + no start, loop wrap over the filtered list, audio monitor range from the filtered list's first/last appFrame) requires zero changes to useRotoCachedPlayback.ts
expected: getFrames solo filter seam: optional getSoloWindow playback input port; absent or null returns the byte-identical 43.5 enumeration (disarmed = zero behavior change); armed enumerates ONLY [start, endExclusive) intersected with the playback end and yields { appFrame, frame: null } for in-range frames failing attribution — the existing missing-frame transparent/background treatment hides unselected content with zero rendering changes (D-17); downstream absorption (empty list -> status + no start, loop wrap over the filtered list, audio monitor range from the filtered list's first/last appFrame) requires zero changes to useRotoCachedPlayback.ts
result: pass
source: automated
coverage_id: D3

### A31. Pitfall 3 regression: solo armed + stopped leaves findCachedRotoDisplayFrame unfiltered — the stopped canvas renders everything at any cursor position (D-18); useRotoReferenceController.ts unmodified (git diff empty at task completion)
expected: Pitfall 3 regression: solo armed + stopped leaves findCachedRotoDisplayFrame unfiltered — the stopped canvas renders everything at any cursor position (D-18); useRotoReferenceController.ts unmodified (git diff empty at task completion)
result: pass
source: automated
coverage_id: D4

### A32. Solo strip button in its own action-row group immediately after the Push group: enabled whenever ANY rail selection exists (single rail = set of one, D-15) and not mutation-locked; disabled reason 'Select a Rail to solo.' when no rail selection else the mapped lock reason; accessible name 'Solo selected Rails' with aria-pressed; tooltips disarmed 'Solo the selected Rails - play only their content within their frame range. Click again or press Escape to exit.' / armed 'Exit solo playback.' (UI-SPEC M2 locked); armed paint reuses the 43.5 armed-tool classes byte-for-byte (border #F59E0B, background rgba(245,158,11,0.2), icon/label #FBBF24); Focus icon from lucide-preact 0.577.0; no keyboard shortcut arms solo
expected: Solo strip button in its own action-row group immediately after the Push group: enabled whenever ANY rail selection exists (single rail = set of one, D-15) and not mutation-locked; disabled reason 'Select a Rail to solo.' when no rail selection else the mapped lock reason; accessible name 'Solo selected Rails' with aria-pressed; tooltips disarmed 'Solo the selected Rails - play only their content within their frame range. Click again or press Escape to exit.' / armed 'Exit solo playback.' (UI-SPEC M2 locked); armed paint reuses the 43.5 armed-tool classes byte-for-byte (border #F59E0B, background rgba(245,158,11,0.2), icon/label #FBBF24); Focus icon from lucide-preact 0.577.0; no keyboard shortcut arms solo
result: pass
source: automated
coverage_id: D5

### A33. Persistent capsule line 'Solo - {N} Rails, frames {A}-{B}.' (singular 'Solo - 1 Rail, ...') via the one mapper buildRailSetSoloCopy (D-27) projected at render time from the arm signal + the Plan 01 mapper + deriveSoloPlaybackWindow — never a status event stream (Pitfall 7); highest-priority persistent term in the capsule composition (between the feedback recency winner and setCopy); clears on exit; exit layering: re-click toggles off, Escape consumes one layer after popover dismiss and push disarm before selection collapse (D-04), ANY rail-selection change/clear disarms, mutation-lock entry disarms, launch replacement/unmount starts disarmed; solo persists across stop/start while the selection is unchanged (D-16); arming never starts or stops transport
expected: Persistent capsule line 'Solo - {N} Rails, frames {A}-{B}.' (singular 'Solo - 1 Rail, ...') via the one mapper buildRailSetSoloCopy (D-27) projected at render time from the arm signal + the Plan 01 mapper + deriveSoloPlaybackWindow — never a status event stream (Pitfall 7); highest-priority persistent term in the capsule composition (between the feedback recency winner and setCopy); clears on exit; exit layering: re-click toggles off, Escape consumes one layer after popover dismiss and push disarm before selection collapse (D-04), ANY rail-selection change/clear disarms, mutation-lock entry disarms, launch replacement/unmount starts disarmed; solo persists across stop/start while the selection is unchanged (D-16); arming never starts or stops transport
result: pass
source: automated
coverage_id: D6

## Summary

total: 42
passed: 2
issues: 7
pending: 0
skipped: 0

## Gaps

- gap_id: G-43.6-1
  truth: "Shift+click selects the canonical anchor→clicked range across all three rail kinds in first-key order; Cmd+Shift+click unions the range to the current set."
  status: failed
  reason: "User reported: Cmd+click works perfectly, but Shift+click does nothing. So I can't test Cmd+Shift+click either."
  severity: major
  test: 1
  artifacts: []
  missing: []
- gap_id: G-43.6-2
  truth: "Solo button stays activated (armed orange tint, aria-pressed) when the user enters solo mode."
  status: failed
  reason: "User reported: All pass except in UI solo need to stay activated when I enter in solo mode."
  severity: major
  test: 2
  artifacts: []
  missing: []
- gap_id: G-43.6-3
  truth: "Delete Rails removes the whole multi-rail set as one atomic command, with Undo restoring the exact pre-delete set."
  status: failed
  reason: "User reported: I multiple select rails, only the last rail is deleted, and the undo/redo no work."
  severity: major
  test: 3
  artifacts: []
  missing: []
- gap_id: G-43.6-4
  truth: "A plain-selected rail is carried into a set when a Cmd+click adds another rail; batch operations Undo/Redo restore the exact pre-operation set."
  status: failed
  reason: "User reported: Work but undo/redo no work, when a rail is already selected (without cmd) and I select another rail with cmd, I most the first rail from selection, I need to reselect with cmd for have the 2 rails selected."
  severity: major
  test: 4
  artifacts: []
  missing: []
- gap_id: G-43.6-6
  truth: "Key Spacing applies to a grouped rail set; its Undo/Redo restores atomically using the same Cmd+Z / Cmd+Shift+Z shortcuts as other sections."
  status: failed
  reason: "User reported (corrected): Key Spacing works on grouped rails, but undo/redo does not work. The undo/redo shortcuts used in other sections should apply here too."
  severity: major
  test: 6
  artifacts: []
  missing: []
- gap_id: G-43.6-7
  truth: "The Solo button shows and keeps the orange armed tint while solo is active. (Shares root cause with G-43.6-2.)"
  status: failed
  reason: "User reported: Solo playback work perfect but when activated I'd would like to see the icon in orange until its actif."
  severity: major
  test: 7
  artifacts: []
  missing: []
- gap_id: G-43.6-8
  truth: "Delete/Backspace routes to Delete Rails and Cmd+Z/Cmd+Shift+Z undo/redo batch operations when a multi-rail set is active. (Delete relates to G-43.6-3; undo/redo is the shared root cause across G-43.6-3/4/6/8.)"
  status: failed
  reason: "User reported: Pass except Delete/Backspace and Cmd+Z/Cmd+Shift+Z no work with multi-rails."
  severity: major
  test: 8
  artifacts: []
  missing: []
