---
phase: 43.6-multi-rail-selection-and-batch-operations
verified: 2026-08-19T14:25:00Z
status: human_needed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 9/9
  gaps_closed:
    - "G-43.6-2 PRIMARY closed (43.6-12): the bridge's childBeforeAuthority before-snapshot block now excludes 'delete-rails' via the beforeSelectionFromPayload guard (physicPaintBridge.ts L1645) — the before snapshot records the TRUE pre-op selectedKeyId/cursorAppFrame from currentDocument (L1652-1657), not the POST-delete proposal selection the forward payload ships, so the undo replay payload matches and one Undo restores every rail/key/break AND the exact pre-delete selection set. Real-bridge regression test 'delete-rails undo/redo restore the exact pre-delete selection set through the real bridge (G-43.6-2)' (physicPaintBridge.test.ts L3939) drives the real applyPhysicPaintPayload forward+undo+redo with distinct pre/post selections ('ordinary' -> 'source-B') and passes; history-seam pin 'records one accepted delete-rails command restoring the exact pre-delete selection set (G-43.6-2)' (useRotoPhysicalEditHistory.test.ts L1900) passes with a non-null pre-delete selection 'F'. Commits 55c3fc25 (RED) + 1ebd8b02 (GREEN) + f2b857f3 (history pin)."
    - "G-43.6-2 SECONDARY closed (43.6-13): executePhysicalEdit now best-effort releases a stale recovery lease via the previously orphaned releasePhysicalEditRecoveryLease() at its top (useRotoPhysicalEditCoordinator.ts L1269-1270), BEFORE the serialize guard — the transient all-deletes-blocked-until-relaunch state self-heals on the next edit. The guard itself is unchanged (recoveryLeaseSignal.peek() still blocks at L1275; a parent refusal still blocks); deps array updated (L1933). Regression test 'self-heals a recovery lease left by a deferred Group publication failure so the next edit proceeds (G-43.6-2)' (useRotoPhysicalEditCoordinator.test.ts L2637) passes. Commits 286c6e01 (RED) + 4452d1e2 (GREEN)."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Native UAT re-run G-43.6-3 (UAT test 2) — Delete Rails atomicity with a multi-rail set active: Delete/Backspace routes to Delete Rails; one command removes the set; one Undo restores every rail/key/break and the exact pre-delete set; Cmd+Shift+Z redo replays the deletion. Also confirm the transient block is gone: after any failed publication, the next Delete works without relaunching the app."
    expected: "Delete {N} Rails scope announced; one Undo restores the exact pre-delete selection set (previously silently no-op'd); redo replays; no residual delete-block after a failed edit."
    why_human: "Live keyboard+pointer routing and visual document state transition; the real-bridge forward+undo+redo regression test and history-seam round-trip are green but do not cover native interaction."
  - test: "Native UAT re-run G-43.6-8 (UAT test 4) — Focus and shortcut parity with a multi-rail set: Delete/Backspace and Cmd+Z/Cmd+Shift+Z behave exactly as for single rails; roving Tab/arrows cycle set members in canonical order."
    expected: "Undo/redo shortcuts work with a multi-rail set active (previously broken with multi-selection, same G-43.6-2 root cause); undo/redo atomic across the set."
    why_human: "Keyboard focus traversal and shortcut parity require live interaction with focus state."
---

# Phase 43.6: Multi-Rail Selection and Batch Operations — Verification Report (Re-Verification After G-43.6-2 Gap Closure)

**Phase Goal:** Let the user select multiple Rails of any type — Key Rail, Motion Rail, Static Rail — in one shared selection set ordered by canonical first-key frame, then apply batch operations to the whole set: Delete Rails, Move Rails, Key Spacing, and selection-scoped (solo) playback. Multi-selection is a new explicit selection scope alongside single-rail and physical-frame selections; the 43.4 mutual-exclusion and selection-authority rules extend to the set. All invariants from Phases 43.3/43.4/43.5 remain binding.
**Verified:** 2026-08-19T14:25:00Z
**Status:** human_needed (9/9 truths verified at code level, G-43.6-2 both defects closed; 2 native UAT re-runs pending)
**Re-verification:** Yes — third round. The previous round (9/9, human_needed) left the native UAT re-runs; the user ran them and reported G-43.6-2 (multi-rail Delete Rails undo/redo silently no-ops + transient all-deletes-blocked-until-relaunch; UAT tests 2 and 4, both severity major). Gap-closure plans 43.6-12 (primary: bridge before-snapshot selection authority) and 43.6-13 (secondary: recovery-lease self-heal) executed and are confirmed CLOSED in the codebase. Prior closures REVIEW-WR-01 (43.6-11), WR-01/WR-02 (43.6-10) re-confirmed intact.

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | One accepted batch operation appends exactly one applied history command; availability {undo:1, redo:0} immediately after acceptance (43.6-07) | ✓ VERIFIED | isOrdinaryOperationKind still lists 'move-rails'/'spacing-on-set'/'delete-rails' (useRotoPhysicalEditHistory.ts L217-219); history suite green in the full 2594-test suite (grew 32 -> 33 with the 43.6-12 G-43.6-2 history-seam pin) |
| 2   | Cmd+Z / Cmd+Shift+Z replay the exact pre/post-operation document snapshots with the batch operationKind — INCLUDING the exact pre-delete selection set for multi-rail Delete Rails (43.6-07; G-43.6-2) | ✓ VERIFIED | STRENGTHENED: the new real-bridge regression test (physicPaintBridge.test.ts L3939) drives the REAL applyPhysicPaintPayload forward+undo+redo for delete-rails with distinct pre/post selections ('ordinary' -> 'source-B') and passes — undo.ok true, restored document toEqual seededDoc including selectedKeyId 'ordinary'; redo replays with 'source-B'. Behavioral evidence through the real bridge, not just the mocked coordinator. History-seam pin (useRotoPhysicalEditHistory.test.ts L1900) green with non-null pre-delete selection 'F'. |
| 3   | The D-06 rail-set snapshot side-channel is consumed by the created history command (43.6-07) | ✓ VERIFIED | Kind-agnostic Studio acceptance wiring unchanged by the closures (diff discipline confirmed: 1ebd8b02 touches only the guard + two ternaries + comment in production; 4452d1e2 touches only the self-heal statement + comment + deps entry) |
| 4   | Plain-clicking rail A then Cmd+clicking rail B yields a set anchored on A for ANY rail-type pair — the plain-selected rail is carried into the set, never dropped (UI-SPEC M1) (43.6-08/10, WR-01) | ✓ VERIFIED | Regression check: both handlers derive singleIdentity from the two live signals, key-rail first (handleSelectRotoKeyRail L1316-1320, handleSelectRotoLoopClip L1383-1387); untouched by closures 12/13 |
| 5   | Plain-click A then Shift+click B yields the canonical anchor-to-clicked range; Cmd+Shift+click unions; fail-closed preserved (43.6-08/10) | ✓ VERIFIED | Reducer byte-untouched by closures (diff stat confirms); reducer tests green in the full suite |
| 6   | A modifier click on a Loop Clip rail ALWAYS commits set membership synchronously — never an open-editor double-click intent, including inside the 250ms pending / 220ms fast-double-click window (43.6-08/10, WR-02) | ✓ VERIFIED | Regression check: gate still `gesture === 'plain' && ...` (PhysicsPaintLoopClipRail.tsx L231); untouched by closures |
| 7   | While Solo is armed the strip Solo button shows and keeps the 43.5 armed orange language; disarmed rendering byte-identical (43.6-09) | ✓ VERIFIED | Untouched by closures; strip suite green in the full suite |
| 8   | Solo playback window is gated on isSoloArmed() — disarm-then-select plays the full timeline (43.6-09) | ✓ VERIFIED | Untouched by closures; armed gate intact |
| 9   | Solo exits on re-click, Escape, or ANY rail-selection change/clear — including a plain Loop Rail click; exit clears button tint and capsule copy immediately (43.6-06 truth 8; D-04, D-14) | ✓ VERIFIED | Regression check: disarmSolo() still at PhysicsPaintStudio.tsx L1421 between the set collapse (L1416) and the selection write (L1453); all seven disarm call sites intact (L185, L386, L653, L656, L1298, L1421, L1955) |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `app/src/lib/physicPaintBridge.ts` | beforeSelectionFromPayload guard excluding delete-rails from payload-derived before-snapshot selection | ✓ VERIFIED | L1645 guard + rationale comment (L1638-1644) + ternaries L1652-1657; childBeforeAuthority (L1636-1637), after snapshot, accepted-document construction, and replay-target equality check all byte-identical |
| `app/src/lib/physicPaintBridge.test.ts` | Real-bridge delete-rails forward+undo+redo regression test with distinct pre/post selections | ✓ VERIFIED | L3939 it-block; substantive (seeds real store, launches on frame 20 to install 'ordinary' as live selection, drives real applyPhysicPaintPayload three times with leases and historyProvenance); green |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts` | History-seam pin: delete-rails round-trip with non-null pre-delete selection 'F' | ✓ VERIFIED | L1900 it-block; asserts proposal.selectedKeyId !== 'F' then expectBatchUndoRedoRoundTrip over the full before/after snapshots; green |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` | Best-effort recovery-lease self-heal at top of executePhysicalEdit | ✓ VERIFIED | L1269-1270 comment + releasePhysicalEditRecoveryLease() call before the guard; guard (L1271-1279) unchanged including recoveryLeaseSignal.peek(); deps array L1933 includes the callback; releasePhysicalEditRecoveryLease (L1260-1265) now has its first production caller |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` | Self-heal regression test | ✓ VERIFIED | L2637 it-block; reproduces the accepted-then-failed Group publication leaving recoveryLease set, asserts the second executeGroupPaint proceeds, releaseLease called with the recovery token, signal cleared; green |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | Prior closures intact (cross-type seed, solo disarm at L1421) | ✓ VERIFIED | Regression check: unchanged by closures 12/13 |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| delete-rails forward payload (POST-delete proposal selectedKeyId 'source-B') | before-snapshot selectedKeyId | beforeSelectionFromPayload guard -> currentDocument (pre-op 'ordinary') | ✓ WIRED | The exact G-43.6-2 seam: undo replay-target equality (original.before vs replay payload, both 'ordinary') now passes; proven end-to-end by the real-bridge test |
| history entry.before selection | bridge replay payload | undo() submits entry.before.selectedKeyId (useRotoPhysicalEditHistory.ts L630-632) | ✓ WIRED | History-seam pin proves the capture/submit/replay chain for a non-null pre-delete selection 'F' |
| finalizeFailed transferLeaseToRecovery (recoveryLeaseSignal set) | next executePhysicalEdit | best-effort releasePhysicalEditRecoveryLease() before the serialize guard | ✓ WIRED | The orphaned release now has its only production caller at L1270; self-heal regression test proves the chain clears the stale lease and the second edit proceeds |
| Store lease authority | coordinator self-heal | ports.lease.release(recoveryToken) | ✓ WIRED | Release routes through parent authority — a refused release leaves the guard blocking (fail-closed preserved) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| delete-rails before snapshot selection | beforeSnapshot.selectedKeyId/cursorAppFrame | currentDocument (pre-op accepted document) for delete-rails; payload for other lifecycle kinds | Yes — records the true pre-operation selection; real-bridge test asserts restored document toEqual seededDoc | ✓ FLOWING |
| recoveryLeaseSignal | held recovery token | finalizeFailed transfer; cleared only by successful ports.lease.release | Yes — self-heal clears it exactly once via parent authority | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Closure suites (bridge + history + coordinator) | `pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` | 3 files, 204 passed / 1 skipped | ✓ PASS |
| Full app suite (no regressions) | `cd app && pnpm exec vitest run` | 135 files passed / 3 skipped; 2594 passed / 1 skipped / 101 todo; exit 0 | ✓ PASS |
| Typecheck | `cd app && pnpm exec tsc --noEmit` | exit 0 | ✓ PASS |
| Diff discipline (43.6-12 fix) | `git show 1ebd8b02 -- app/src/lib/physicPaintBridge.ts` | Only the guard + two ternaries + comment; childBeforeAuthority, after snapshot, replay-target equality untouched | ✓ PASS |
| Diff discipline (43.6-13 fix) | `git show 4452d1e2 -- app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` | One comment + one statement before the guard + deps entry; guard branches byte-identical | ✓ PASS |
| RED/GREEN commit presence | git log | 55c3fc25 (RED) -> 1ebd8b02 (GREEN) -> f2b857f3 (pin); 286c6e01 (RED) -> 4452d1e2 (GREEN) | ✓ PASS |
| Prior closures intact | grep disarmSolo / beforeSelectionFromPayload | L1421 disarm present; all seven disarm sites intact; guard present | ✓ PASS |

### Probe Execution

No probes declared or implied by plans 43.6-12/13 — Step 7c SKIPPED.

### Requirements Coverage

REQUIREMENTS.md holds no 43.6 rows; the 21 IDs are planning-local (43.6-RESEARCH.md). All 21 are claimed by at least one plan's `requirements:` frontmatter (re-verified this round via grep across all *-PLAN.md); none orphaned.

| Requirement | Source Plan | Status | Evidence |
| ----------- | ---------- | ------ | -------- |
| RSET-01 | 01, 08, 10 | ✓ SATISFIED | Cross-type seed derivation in both handlers; WR-01 regression test green |
| RSET-02 | 01, 08, 10 | ✓ SATISFIED | Same-type and cross-type range/union paths verified; WR-02 window closed |
| RSET-03 | 02, 04, 07, 12 | ✓ SATISFIED | STRENGTHENED by 43.6-12: the exact pre-delete set now survives one Undo — before snapshot records the true pre-op selection; history-seam pin + real-bridge test green |
| RSET-04 | 01 | ✓ SATISFIED | Full 2594-test suite green |
| BMOV-01..06 | 02, 03, 07 | ✓ SATISFIED | History allowlist + round-trip tests green; native UAT test 3 (G-43.6-4) passed |
| BDEL-01 | 05, 08, 10, 13 | ✓ SATISFIED | STRENGTHENED by 43.6-13: Delete/Backspace routing can no longer be silently dead after an accepted-then-failed publication — the recovery-lease self-heal unblocks every subsequent physical edit |
| BDEL-02 | 04, 05, 07, 12, 13 | ✓ SATISFIED | STRENGTHENED by 43.6-12/13: atomic mixed-set Delete with one Undo/Redo now proven through the REAL bridge with a non-null pre-delete selection; the transient blocked-delete defect self-heals |
| BSPC-01..03 | 05, 07 | ✓ SATISFIED | spacing-on-set history round-trip test green |
| SOLO-01..02, SOLO-04..06 | 06, 09 | ✓ SATISFIED | Armed tint + armed gate + capsule; native UAT approved (G-43.6-1/-7) |
| SOLO-03 | 06, 11 | ✓ SATISFIED | disarmSolo at L1421 + regression test; native UAT test 1 (G-43.6-1) passed |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | - | No TBD/FIXME/XXX markers, no empty implementations, no placeholder returns in the 43.6-12/13 diffs (marker scan clean) | - | - |

Carried-forward info items from 43.6-REVIEW.md (non-blocking): IN-01 render-phase signal write in the rail-set reconcile; IN-02 drag-session stale closures; IN-03 unbounded railSetSnapshots map; IN-04 render-phase canceller registration; IN-05 dead ternary in cancelPhysicalEdit; IN-06 delete-rails cursorAppFrame null-document fallback.

**Follow-up observations from 43.6-REVIEW.md (non-blocking; NOT phase gaps — assessed against the locked requirements):**

- **WR-02 (review warning, deferred by explicit plan prohibition):** the same silent-undo-no-op mechanism is theoretically armed for `delete-group` / `delete-action-groups` when the pre-op selection sits inside the cleanup set (buildLifecycleProposal nulls it). Plan 43.6-12's must_haves prohibitions explicitly forbid altering childBeforeAuthority for those kinds — they are 43.2-approved paths (native UAT approved in Phase 43.2) and reopening them without a user report is out of scope. Not reachable through any 43.6 multi-rail batch operation (Delete Rails is the only set-scoped lifecycle delete, and it is now fixed). If the user ever reports undo failing after a Group Rail delete with the selection inside the group, this is the first place to look.
- **WR-03 (review warning, new edge beyond plan scope):** the self-heal releases only the CURRENT hook instance's recoveryLeaseSignal; a Studio unmount while a recovery lease is held (unmount effect L1936-1946 handles pending/settled but never a held recovery token — confirmed by code read) orphans the token in the store scope, and a remounted coordinator cannot see it (edits blocked until project reset). Narrower than the reported defect: requires a failed publication, then a window close before the next edit. The G-43.6-2 user report ("relaunch the app resolved the delete") is fully covered by the in-session self-heal. Recommend tracking as a future hardening item (release the recovery token in the unmount effect, per the review's suggested fix).

Neither warning defeats a locked requirement or a phase-goal truth; both are recorded for follow-up rather than as gaps.

### Human Verification Required

Code-green at 9/9 with G-43.6-2 closed at both seams, but two native UAT re-runs remain pending human verification (per 43.6-UAT.md, tests 2 and 4 reported the G-43.6-2 issues; tests 1 and 3 already passed natively):

### 1. Native UAT re-run G-43.6-3 (UAT test 2) — Delete Rails atomicity with a multi-rail set active

**Test:** With a multi-rail set active, press Delete/Backspace; verify `Delete {N} Rails` scope announcement, one-command removal, one Undo restoring every rail/key/break AND the exact pre-delete set, and redo replaying it. Also: after any failed publication, confirm the next Delete works without relaunching the app.
**Expected:** One Undo restores the exact pre-delete selection set (the previous silent no-op); no residual delete-block (the previous relaunch-only recovery).
**Why human:** Live keyboard+pointer routing and visual document state transition; the real-bridge regression test proves the code path but not the native interaction.

### 2. Native UAT re-run G-43.6-8 (UAT test 4) — Focus and shortcut parity with a multi-rail set

**Test:** With a multi-rail set active, verify Delete/Backspace and Cmd+Z/Cmd+Shift+Z behave exactly as for single rails; roving Tab/arrows cycle set members in canonical order.
**Expected:** Undo/redo shortcuts work with a multi-rail set active (previously broken — same G-43.6-2 root cause); undo/redo atomic across the set.
**Why human:** Keyboard focus traversal and shortcut parity require live interaction.

### Gaps Summary

No remaining verification gaps. Both defects of **G-43.6-2** are confirmed CLOSED in the codebase:

- **Primary (43.6-12):** `beforeSelectionFromPayload = childBeforeAuthority && payload.operationKind !== 'delete-rails'` (physicPaintBridge.ts L1645) switches the delete-rails before-snapshot selection authority to the pre-op currentDocument, so the undo replay payload (history entry.before) matches the recorded before snapshot and replay-target equality passes. Verified by reading the guard and by the passing real-bridge forward+undo+redo regression test with distinct pre/post selections ('ordinary' -> 'source-B') — undo.ok true, restored document toEqual seededDoc including the selection; redo replays with the post-delete selection. Plan prohibitions honored: the forward payload still ships the POST-delete proposal selection (coordinator untouched); every other lifecycle kind keeps payload authority byte-identical; no pre-existing test modified.
- **Secondary (43.6-13):** `releasePhysicalEditRecoveryLease();` now runs best-effort at the top of executePhysicalEdit (useRotoPhysicalEditCoordinator.ts L1270), before the unchanged serialize guard — the previously orphaned release has its first production caller, so a stale recovery lease self-heals on the next edit instead of blocking every edit until relaunch. The release routes through ports.lease.release(recoveryToken), so a parent refusal still blocks (fail-closed). Verified by reading the insertion/guard and by the passing coordinator regression test.

Full app suite: 135 files / 2594 tests passed (1 skipped, 101 todo), exit 0 — no regressions. `tsc --noEmit` clean. All 9 must-have truths verified, all 21 planning-local requirement IDs satisfied at code level, none orphaned. Review warnings WR-02/WR-03 assessed as follow-up observations, not gaps (see above). The phase is **automated-ready**: awaiting the two native UAT re-runs (G-43.6-3 and G-43.6-8) before the phase can be declared fully complete.

---

_Verified: 2026-08-19T14:25:00Z_
_Verifier: Claude (gsd-verifier)_
