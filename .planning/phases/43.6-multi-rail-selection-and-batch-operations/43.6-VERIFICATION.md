---
phase: 43.6-multi-rail-selection-and-batch-operations
verified: 2026-08-19T13:12:00Z
status: gaps_found
score: 8/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/8
  gaps_closed:
    - "WR-01 closed: both Studio handlers derive the seed from whichever single-rail signal is live, key-rail first (PhysicsPaintStudio.tsx handleSelectRotoKeyRail and handleSelectRotoLoopClip both read selectedRotoKeyRail.value then selectedLoopClipId.value); cross-type source-contract test green (commit 9392f442)"
    - "WR-02 closed: the Loop Clip double-click gate is conditioned on gesture === 'plain' (PhysicsPaintLoopClipRail.tsx); in-window modifier clicks fall through to the synchronous-commit branch; component test green (commit 6fa3e28d)"
  gaps_remaining:
    - "NEW (43.6-REVIEW.md warning, confirmed): the plain Loop Rail click success path in handleSelectRotoLoopClip never calls disarmSolo() — Solo stays armed and silently retargets to the newly plain-clicked Loop rail, violating D-14 and plan 06 truth 8"
  regressions: []
gaps:
  - truth: "Solo exits on re-click, Escape, or ANY rail-selection change/clear; exit clears button tint and capsule copy immediately (43.6-06 must_have truth 8; D-04, D-14 user-locked: 'changing or clearing the rail selection ends solo immediately')"
    status: partial
    reason: "Confirmed in code. clearRotoLoopSelection (PhysicsPaintStudio.tsx L1295-1302) documents the locked contract 'every rail-selection setter routes through this clear — ANY rail-selection change/clear disarms an armed Solo', and every selection-change path honors it EXCEPT handleSelectRotoLoopClip's plain-click success branch (L1415-1450): it sets railSetSelection.value = null and writes selectedLoopClipIds/loopSelectionAnchorId/selectedLoopClipId directly with no disarmSolo() call. Concrete consequence: arm Solo on Loop A (single rail = set of one, D-15), then plain-click Loop B — the set collapses, the single-rail selection moves to B, Solo stays armed, and getSoloWindow (L1154-1161) falls back to the single-rail selection, so playback and the armed capsule silently retarget to solo Loop B. The equivalent plain click on a Key Rail disarms via clearRotoLoopSelection (L1351). The same gap is reachable via the double-click-to-open-editor path, which plain-selects first (PhysicsPaintLoopClipRail.tsx L234). Only the next === null sub-branch of the loop plain path disarms (via clearRotoLoopSelection)."
    artifacts:
      - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
        issue: "handleSelectRotoLoopClip plain-click success branch (L1415-1450) writes the new single-rail loop selection without disarmSolo() — the only rail-selection change path that skips disarm (contrast key-rail plain path L1349-1363)"
    missing:
      - "Call disarmSolo() in the plain-click success branch before applying the new selection (do NOT call clearRotoLoopSelection() — it would clobber the selection being written), e.g. after `railSetSelection.value = null;` add `disarmSolo();` with a 43.6-06 D-14 comment"
      - "Add a Studio-level regression test: arm Solo with Loop A selected, plain-click Loop B, assert isSoloArmed() === false"
---

# Phase 43.6: Multi-Rail Selection and Batch Operations — Verification Report (Re-Verification After Gap Closure)

**Phase Goal:** Let the user select multiple Rails of any type — Key Rail, Motion Rail, Static Rail — in one shared selection set ordered by canonical first-key frame, then apply batch operations to the whole set: Delete Rails, Move Rails, Key Spacing, and selection-scoped (solo) playback. Multi-selection is a new explicit selection scope alongside single-rail and physical-frame selections; the 43.4 mutual-exclusion and selection-authority rules extend to the set. All invariants from Phases 43.3/43.4/43.5 remain binding.
**Verified:** 2026-08-19T13:12:00Z
**Status:** gaps_found
**Re-verification:** Yes — prior gaps WR-01/WR-02 confirmed closed by plan 43.6-10 (commits 22542c80, 9392f442, 6fa3e28d); one NEW gap confirmed from 43.6-REVIEW.md (plain Loop click skips Solo disarm).

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | One accepted batch operation appends exactly one applied history command; availability {undo:1, redo:0} immediately after acceptance (43.6-07) | ✓ VERIFIED | Regression: isOrdinaryOperationKind still lists 'move-rails'/'spacing-on-set'/'delete-rails' (useRotoPhysicalEditHistory.ts L217-219); history suite 32/32 green in the 304-test sweep |
| 2   | Cmd+Z / Cmd+Shift+Z replay the exact pre/post-operation document snapshots with the batch operationKind (43.6-07) | ✓ VERIFIED | Prior round; batch undo/redo regression tests still green (no diff to useRotoPhysicalEditHistory.ts since) |
| 3   | The D-06 rail-set snapshot side-channel is consumed by the created history command (43.6-07) | ✓ VERIFIED | Prior round; kind-agnostic Studio acceptance wiring unchanged; tests green |
| 4   | Plain-clicking rail A then Cmd+clicking rail B yields a set anchored on A for ANY rail-type pair — the plain-selected rail is carried into the set, never dropped (UI-SPEC M1) (43.6-08/10, WR-01) | ✓ VERIFIED | Both handlers now derive singleIdentity from `selectedRotoKeyRail.value !== null ? key-rail : selectedLoopClipId.value !== null ? loop : null` (identical two-signal derivation, key-rail first, in handleSelectRotoKeyRail L1316-1321 and handleSelectRotoLoopClip L1383-1387); a plain click on either type nulls the other type's signal so exactly one can be live; cross-type source-contract test 'seeds the set from the live cross-type single-rail signal with key-rail-first ordering (43.6-10 WR-01)' green (PhysicsPaintStudio.test.ts L528) |
| 5   | Plain-click A then Shift+click B yields the canonical anchor-to-clicked range; Cmd+Shift+click unions; fail-closed preserved (43.6-08/10) | ✓ VERIFIED | Cross-type seed now works (truth 4); reducer fail-closed paths byte-untouched (physicsPaintRotoRailSetSelection.ts unchanged in the closure commits); 39/39 reducer tests green; the rapid plain->Shift-in-window Loop path is unblocked by truth 6 |
| 6   | A modifier click on a Loop Clip rail ALWAYS commits set membership synchronously — never an open-editor double-click intent, including inside the 250ms pending / 220ms fast-double-click window (43.6-08/10, WR-02) | ✓ VERIFIED | Gate is now `if (gesture === 'plain' && (hasPendingSingleClick || (elapsed >= 0 && elapsed <= LOOP_CLIP_FAST_DOUBLE_CLICK_MS)))` (PhysicsPaintLoopClipRail.tsx L231); modifier clicks fall through to the synchronous `gesture !== 'plain'` commit branch (L241-245); component test 'commits an in-window Cmd+click synchronously and never opens the editor (43.6-10 WR-02)' green (PhysicsPaintLoopClipRail.test.tsx L962); pre-existing plain double-click editor tests still green |
| 7   | While Solo is armed the strip Solo button shows and keeps the 43.5 armed orange language; disarmed rendering byte-identical (43.6-09) | ✓ VERIFIED | Prior round; className pairing intact (PhysicsPaintWorkflowStrip.tsx L3330); strip suite 115/115 green; native UAT approved 2026-08-19 (G-43.6-2/-7) |
| 8   | Solo playback window is gated on isSoloArmed() — disarm-then-select plays the full timeline (43.6-09) | ✓ VERIFIED | Prior round; early null return intact (PhysicsPaintStudio.tsx L1153); armed-gate source-contract test green; native UAT approved |
| 9   | Solo exits on re-click, Escape, or ANY rail-selection change/clear (43.6-06 truth 8; D-04, D-14) | ✗ FAILED (partial) | NEW gap confirmed: the plain Loop Rail click success branch (PhysicsPaintStudio.tsx L1415-1450) is the only rail-selection change path that never calls disarmSolo(); armed Solo silently retargets to the plain-clicked Loop rail via the getSoloWindow single-rail fallback (L1156-1161). All other paths disarm (L185, L386, L653/656, L1298 via clearRotoLoopSelection, L1950). See gap in frontmatter |

**Score:** 8/9 truths verified

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` | Three batch kinds in isOrdinaryOperationKind | ✓ VERIFIED | L217-219 intact |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts` | Batch undo/redo regression tests | ✓ VERIFIED | 32/32 green in sweep |
| `app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts` | seedRailSetSelection pure helper, fail-closed | ✓ VERIFIED | L230; byte-untouched by 43.6-10 (closure commits touched only Studio/LoopClipRail + tests); 39/39 green |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | Cross-type seed wiring in BOTH handlers + solo armed gate + D-14 disarm coverage | ⚠️ PARTIAL | WR-01 closed (both handlers derive cross-type, key-rail first); solo armed gate intact (L1153); but the loop plain-click success branch skips disarmSolo (gap above) |
| `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx` | Gesture-aware double-click gate + synchronous modifier commit | ✓ VERIFIED | L231 gate conditioned on `gesture === 'plain'`; commit branch L241-245 reachable in-window; 43/43 suite green |
| `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` | Solo button base class | ✓ VERIFIED | L3330; 115/115 green |
| `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` | WR-01 cross-type seed regression test | ✓ VERIFIED | L528 it-block exists and passes (75/75 file green) |
| `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx` | WR-02 in-window modifier click regression test | ✓ VERIFIED | L962 it-block exists and passes |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| coordinator acceptedOutput | history applied stack | isOrdinaryOperationKind allowlist | ✓ WIRED | L217-219; regression tests prove one acceptance -> one command |
| history command operationId | D-06 snapshot side-channel | resolveRailSetPostAcceptance | ✓ WIRED | Kind-agnostic acceptance effect; round-trip tests green |
| selectedRotoKeyRail / selectedLoopClipId | seedRailSetSelection | singleIdentity derivation in BOTH Studio handlers | ✓ WIRED | WR-01 CLOSED: each handler reads the opposite type's live signal with key-rail-first ordering; cross-type source-contract test pins both directions |
| Loop Clip modifier click (incl. in-window) | railSetSelection -> .selected -> isPhysicsPaintRotoDeleteTarget | synchronous onSelectLoopClip | ✓ WIRED | WR-02 CLOSED: gate plain-only; in-window Cmd+click commits 'toggle' synchronously, onOpenLoopEdit never called (component test) |
| Solo button className | compound CSS selector | base + armed class pairing | ✓ WIRED | L3330 + physicsPaintStudio.css; native UAT approved |
| getSoloWindow port | isSoloArmed signal | early null return | ✓ WIRED | L1153 |
| Loop plain-click selection change | disarmSolo | D-14 layered exit discipline | ✗ NOT_WIRED | The only broken exit path — see gap in frontmatter |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full five-suite regression sweep | `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` | 5 files, 304/304 tests passed | ✓ PASS |
| WR-01 cross-type seed (both directions) | code read of PhysicsPaintStudio.tsx L1316-1321 / L1383-1387 + test L528 | both handlers derive from the live cross-type signal, key-rail first; test green | ✓ PASS |
| WR-02 in-window modifier click | code read of PhysicsPaintLoopClipRail.tsx L231-245 + test L962 | gate plain-gated; synchronous commit reachable in-window; test green | ✓ PASS |
| Solo disarm on plain Loop click | code read of PhysicsPaintStudio.tsx L1415-1450 + disarmSolo call-site enumeration (L185, 386, 653, 656, 1298, 1941, 1950) | no disarmSolo on the loop plain-click success path | ✗ FAIL (gap) |

### Probe Execution

No probes declared or implied by plans 43.6-07/08/09/10 — Step 7c SKIPPED.

### Requirements Coverage

REQUIREMENTS.md holds no 43.6 rows (confirmed by grep); the 21 IDs are planning-local, derived in 43.6-RESEARCH.md. All 21 are claimed by at least one plan; none are orphaned.

| Requirement | Source Plan | Status | Evidence |
| ----------- | ---------- | ------ | -------- |
| RSET-01 | 01, 08, 10 | ✓ SATISFIED | WR-01 closed: cross-type seed derivation in both handlers; regression test green |
| RSET-02 | 01, 08, 10 | ✓ SATISFIED | Same-type and cross-type range/union paths verified; WR-02 window closed for Loop-rail gestures |
| RSET-03 | 02, 04, 07 | ✓ SATISFIED | Batch history allowlist + regression tests green |
| RSET-04 | 01 | ✓ SATISFIED | Prior round; 304-test regression sweep green |
| BMOV-01..06 | 02, 03, 07 | ✓ SATISFIED | Prior round; undo/redo code-closed by 43.6-07; tests green |
| BDEL-01 | 05, 08, 10 | ✓ SATISFIED | Synchronous commit now reachable inside the double-click window (WR-02 closed) |
| BDEL-02 | 04, 05, 07 | ✓ SATISFIED | delete-rails history round-trip test green |
| BSPC-01..03 | 05, 07 | ✓ SATISFIED | spacing-on-set history round-trip test green |
| SOLO-01..02, SOLO-04..06 | 06, 09 | ✓ SATISFIED | Armed tint + armed gate + capsule; native UAT approved 2026-08-19 |
| SOLO-03 | 06 | ⚠️ PARTIAL | "Persists across stop/start while the selection is unchanged" presumes a selection CHANGE ends solo (D-14 exit discipline); the plain Loop click path changes the selection without ending solo — see gap |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | - | No TBD/FIXME/XXX markers, no empty implementations, no placeholder returns in the 43.6-10 diff | - | - |

Carried-forward info items from 43.6-REVIEW.md (non-blocking, documented for later): IN-01 render-phase signal write in the rail-set reconcile; IN-02 drag-session stale closures; IN-03 unbounded railSetSnapshots map; IN-04 render-phase canceller registration; IN-05 dead ternary in cancelPhysicalEdit.

### UAT Status Cross-Reference

- Resolved (native UAT approved): G-43.6-2, G-43.6-6, G-43.6-7.
- Code-closed, native UAT re-run still pending (human): G-43.6-1, G-43.6-3, G-43.6-4, G-43.6-8 — the WR-01/WR-02 code fixes are in and test-green, so these re-runs are now unblocked for the cross-type and in-window paths, BUT they should run AFTER the new Solo-disarm gap is closed, since the loop plain-click path is part of the G-43.6-1 selection flow and an armed-Solo retarget would surface during G-43.6-1/-8 interactions.

### Gaps Summary

The 43.6-10 gap-closure plan fully closed both prior gaps at code level — WR-01 (cross-type seed) and WR-02 (gesture-blind double-click gate) are confirmed fixed in the source, pinned by new regression tests, and the five-suite sweep is 304/304 green. However, the fresh code review (43.6-REVIEW.md) surfaced one new warning that this verification CONFIRMS as a failed must-have:

**Plain Loop Rail click does not disarm an armed Solo.** `handleSelectRotoLoopClip`'s plain-click success branch (PhysicsPaintStudio.tsx L1415-1450) is the only rail-selection change path that skips `disarmSolo()`, contradicting the D-14 user-locked invariant ("changing or clearing the rail selection ends solo immediately"), plan 06's must-have truth 8, and the module's own documented contract in `clearRotoLoopSelection` (L1296-1298). The armed Solo silently retargets to the newly clicked Loop rail through the `getSoloWindow` single-rail fallback (L1156-1161). The fix is a one-line `disarmSolo()` call in the plain branch plus a Studio-level regression test (arm Solo on Loop A, plain-click Loop B, assert `isSoloArmed() === false`).

This is a small, surgical gap — one selection path out of nine that honors the invariant — but it violates a user-locked D-14 invariant on a phase must-have, so it blocks phase completion. Native UAT re-runs for G-43.6-1/-3/-4/-8 should be scheduled after this closure.

---

_Verified: 2026-08-19T13:12:00Z_
_Verifier: Claude (gsd-verifier)_
