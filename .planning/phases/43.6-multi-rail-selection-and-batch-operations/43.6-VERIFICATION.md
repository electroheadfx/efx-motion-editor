---
phase: 43.6-multi-rail-selection-and-batch-operations
verified: 2026-08-18T23:10:00Z
status: passed
score: 16/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 15/16
  gaps_closed:
    - "WR-01: batch rail-set drag session's clearClickSequence was a no-op stub, so a pending 250 ms single-click timer on a set-member Loop Clip rail could fire mid-drag and collapse or mutate the set — fixed by a strip-owned registry of per-rail click-sequence cancellers (commit f7f5611b)"
  gaps_remaining: []
  regressions: []
---

# Phase 43.6: Multi-Rail Selection and Batch Operations — Verification Report

**Phase Goal:** Let the user select multiple Rails of any type — Key Rail, Motion Rail, Static Rail — in one shared selection set ordered by canonical first-key frame, then apply batch operations to the whole set: Delete Rails, Move Rails, Key Spacing, and selection-scoped (solo) playback. Multi-selection is a new explicit selection scope alongside single-rail and physical-frame selections; the 43.4 mutual-exclusion and selection-authority rules extend to the set. All invariants from Phases 43.3/43.4/43.5 remain binding.
**Verified:** 2026-08-18T23:10:00Z
**Status:** passed
**Re-verification:** Yes — after WR-01 gap closure (commit f7f5611b)

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | User can select multiple Rails of any kind (Key Rail / Motion / Static) in one shared set via Cmd/Ctrl toggle, Shift range, Cmd+Shift union, plain collapse; one canonical cross-type ordering by first-key frame | ✓ VERIFIED | physicsPaintRotoRailSetSelection.ts (4 gestures, deriveRailSetOrder, fail-closed); union-first routing in both rail components; Studio handlers route modifier gestures to the set reducer; 32 reducer tests + ordering-agreement test green |
| 2   | Every set member paints the existing orange selection line; the anchor carries the 2x8px #F2F5F7 70% tick; empty set shows nothing | ✓ VERIFIED | .physics-paint-rail-anchor-tick CSS (2px/8px/#f2f5f7/0.7/top 4px/pointer-events none); rail components render tick only for anchor; no new selection color |
| 3   | Set lifecycle: plain-click collapse, empty-strip/frame clear, frame/key selection clears the set, stale-authority reconcile clears the invalid set, one-Escape set-collapse layer | ✓ VERIFIED | reconcileRailSetSelection + render-phase reconcile in Studio; clearRotoLoopSelection/selectAll/launch-reset clearing; Escape chain order tested (physicsPaintStudioKeyboard.test.ts) |
| 4   | One deterministic set-copy mapper feeds the status capsule and M1 tooltip set sentences; capsule shows the set copy only while active | ✓ VERIFIED | buildRailSetCopy / buildRailSetTooltipSentence in useRotoTimelineActions.ts; setCopy capsule term; M6 strings asserted in tests; presentation tests green |
| 5   | Roving Tab/arrow cycling visits set members in canonical order through the shared 43.4 machinery — no set-aware focus fork | ✓ VERIFIED | physicsPaintRailKeyboardNavigation.test.ts set-contract case passes; the navigation module source is unchanged |
| 6   | The rail set is session-only: never in revision fingerprint, history, persistence, or bridge payloads | ✓ VERIFIED | No rail-set/solo state referenced in src/lib or src/types; strict-parse, revision-fingerprint, and persistence suites green in the full physic-paint run |
| 7   | Batch Move: dragging any selected rail moves the whole set rigidly; clamp/reject as ONE unit; straddle guard; break travel; ghost/gap/blocked-edge preview | ✓ VERIFIED | move-rails resolver branch, derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta, usePhysicsPaintRailSetDrag, ghost CSS verified and 227 resolver/parity + 14 drag tests green. WR-01 closed (commit f7f5611b): the batch drag session's clearClickSequence now iterates a strip-owned registry of per-rail cancellers, so the pending 250 ms single-click timer armed by the set-member click is cleared on threshold crossing — a click-then-drag cannot collapse or mutate the set mid-drag. Regression test PhysicsPaintLoopClipRail.test.tsx (registration, drag-begin cancellation, no-canceller control) green; drag-hook test asserts clearClickSequence fires on threshold crossing |
| 8   | Move locked copy + D-06 aftermath: set stays selected after acceptance; Undo restores the set at originals; Redo re-applies | ✓ VERIFIED | mapRotoRailSetMoveProductReason (live/accepted/rejection strings asserted); recordRailSetSnapshot + resolveRailSetPostAcceptance in Studio (historyProvenance.historyCommandId); move-rails aftermath tests green |
| 9   | Delete Rails: one classifier shared by Delete/Backspace/visible control routes to a rail-set scope; direct no-modal at any size | ✓ VERIFIED | classifyRotoDeleteTarget rail-set branch FIRST; deleteRotoFrame rail-set dispatch through executeRailSetDelete; scope labels 'Delete {N} Rails'/'Delete 1 Rail'; no confirmation call |
| 10  | Delete Rails: per-type semantics (Key Rail records+projection removed; Motion/Static follows 43.2 Group delete, Action never deleted), one atomic history command, persistent intentional gaps, 43.4 break normalization | ✓ VERIFIED | proposePhysicPaintRotoDeleteRails composes per-Group 43.2 + key-rail removal + break normalization; parity tests pass; parent bridge exact-match recomputes with distinct error strings (physicPaintBridge.test.ts green) |
| 11  | Delete D-06 aftermath: selection empties after acceptance, cursor stays valid; Undo restores the exact pre-delete set; Redo clears again | ✓ VERIFIED | resolveRailSetPostAcceptance 'delete-rails' -> null; delete-rails snapshot records after = null (redo-clears test green) |
| 12  | Key Spacing on a set: per-rail fixed first-key anchors, whole-set all-or-nothing validation, one atomic command | ✓ VERIFIED | spacing-on-set resolver branch (per-rail anchor + i*(N+1), hard walls, 'rails-spacing-source-straddle'); all-or-nothing by construction; 13 resolver tests + transport round-trip green |
| 13  | Spacing popover scope line (D-27 copy, set-active only) + locked M5 copy; no-set path byte-identical to 43.5 | ✓ VERIFIED | forceSpacingScopeLine static-chrome port + .physics-paint-toolbox-scope-line CSS; mapSpacingOnSetProductReason; strip test asserts heading < scope line < controls; existing spacing tests unchanged and green |
| 14  | Solo playback: session-only arm filters the playing-frame enumeration to the selected rails' content within their effective range; stopped canvas untouched | ✓ VERIFIED | physicsPaintSoloArm.ts (no-op-guard-free, boolean disarm); deriveSoloPlaybackWindow (D-19 boundaries, A3 attribution); getSoloWindow port applied only inside the getFrames closure; Pitfall 3 regression (useRotoReferenceController unmodified); 27 window + 7 arm + playback filter tests green |
| 15  | Solo button (own group after Push, guarded tooltip, armed orange state), persistent capsule line, layered exits (re-click/Escape/selection-change/lock) | ✓ VERIFIED | Solo button markup (Focus 18px, 'Solo', aria-pressed, armed classes byte-reused); buildRailSetSoloCopy + soloLine capsule term; Escape chain order push→solo→collapse tested; presentation tests assert 'Solo - 3 Rails, frames 12-88.' / 'Solo - 1 Rail, frames 12-40.' |
| 16  | Cross-phase invariants carried: break ownership by stable key identity, capacity as single end authority, atomic Undo/Redo via canonical physical history, replay snapshots read from accepted authority, one shared rail behavior with no per-type forks | ✓ VERIFIED | D-11 break travel in move-rails + spacing; common finalizer (capacity) reused; parent exact-match + coordinator lease discipline; replay uses accepted.historyProvenance; both rail families route through the same set reducer + shared focus machinery |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ---------| ------ | ------- |
| `app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts` | Pure reducer: identities, gestures, reconcile, ordering, D-06 snapshot side-channel | ✓ VERIFIED | 315 lines; 4 gestures, deriveRailSetOrder, reconcileRailSetSelection, record/resolve/clearRailSetSnapshots; dependency-free |
| `app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts` | Wave 0 test file | ✓ VERIFIED | 32 tests pass |
| `app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts` | Port-injected pointer-session hook, set-level preview state | ✓ VERIFIED | 321 lines; L139 modifier guard, 4px threshold, preview-is-the-commit, zero-delta no-publish, Escape/pointercancel/lostpointercapture cleanup; threshold crossing invokes `input.clearClickSequence()` (drag-hook test asserts the call) |
| `app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts` | Wave 0 test file | ✓ VERIFIED | 14 tests pass |
| `app/src/components/physic-paint/view/physicsPaintSoloArm.ts` | Session solo arm signal module | ✓ VERIFIED | isSoloArmed/toggleSolo/disarmSolo; session-only doc contract; no commitInFlight guard |
| `app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts` | Pure solo range + frame-attribution derivation | ✓ VERIFIED | 175 lines; effective boundaries (D-19), A3 attribution, fail-closed null |
| `physicsPaintRotoPhysicalResolver.ts` | move-rails + spacing-on-set intent branches, set clamp/derivation authorities | ✓ VERIFIED | Exported derive/clamp authorities; both intent branches consume them; straddle codes in failure union |
| `physicPaintBridge.ts` | delete-rails parent recompute + exact-match | ✓ VERIFIED | GROUP_LIFECYCLE gate + recompute branch via shared proposer + distinct error strings |
| `PhysicsPaintWorkflowStrip.tsx` | Set drag wiring, ghosts, Solo button, capsule/popover scope line | ✓ VERIFIED | WR-01 closed: strip owns `railClickSequenceCancellersRef` registry; batch `clearClickSequence` iterates it; `registerClickSequenceCanceller` prop adds/deletes per-rail cancellers |
| `PhysicsPaintLoopClipRail.tsx` | Loop Clip rail + click-sequence cancellation | ✓ VERIFIED | WR-01: mount-once ref-guarded registration of `clearClickSequence` with the strip registry; unmount unregisters via the existing cleanup effect |
| `PhysicsPaintLoopClipRail.test.tsx` | Loop Clip rail tests incl. WR-01 regression | ✓ VERIFIED | 41 tests pass; WR-01 test covers registration, drag-begin cancellation, no-canceller control |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| Rail components | Studio set signal | updatePhysicsPaintRotoRailSetSelection in handleSelectRotoLoopClip / handleSelectRotoKeyRail | WIRED | Modifier gestures reach the reducer; orange paint + anchor tick rendered |
| Studio set signal | Capsule/tooltips/a11y | buildRailSetCopy / buildRailSetTooltipSentence via railSetCopy memo + setCopy capsule term | WIRED | One mapper authority; popover scope line uses the same railSetCopy |
| Drag hook prepare port | Plan 02 clamp authority | clampDelta port -> clampPhysicPaintRailSetMoveDelta; commit -> commitRailSetMove | WIRED | Preview-is-the-commit; commit revalidates proposal version |
| Drag hook beginDrag | Rail canceller registry (WR-01) | input.clearClickSequence() on threshold crossing -> strip iterates railClickSequenceCancellersRef -> per-rail clearClickSequence clears pendingSingleClickRef | WIRED | Hook calls the port at beginDrag (drag-hook test asserts); strip port iterates the registry; rail registers on mount (ref-guarded) and unregisters on unmount; WR-01 regression test proves cancellation + control |
| Accepted move-rails | D-06 aftermath | recordRailSetSnapshot + resolveRailSetPostAcceptance (historyProvenance id) | WIRED | Set stays selected; Undo/Redo restore exact set |
| Delete classifier | executeRailSetDelete port | deleteRotoFrame rail-set branch | WIRED | Shared by keyboard + visible control; direct no-modal |
| Child delete proposal | Parent bridge recompute | proposePhysicPaintRotoDeleteRails (same pure proposer) | WIRED | Exact-match with distinct error strings |
| Solo arm signal | Playback enumeration | getSoloWindow port applied inside getFrames closure only | WIRED | Disarmed byte-identical; stopped canvas untouched |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Capsule set copy | railSetCopy | buildRailSetCopy over effectiveRailSetSelection members (real segments/ranges) | Yes | ✓ FLOWING |
| Set members paint | railSetSelection | Gesture reducer over deriveRailSetOrder(real segments + loop ranges) | Yes | ✓ FLOWING |
| Move ghosts | railSetDragApi.preview | clampPhysicPaintRailSetMoveDelta over real members/records/ranges | Yes | ✓ FLOWING |
| Solo capsule line | soloLine | buildRailSetSoloCopy over deriveSoloPlaybackWindow(real cells/segments/ranges) | Yes | ✓ FLOWING |
| Delete acceptance copy | buildDeleteRailSetSuccessMessage | Classified rail-set target (real members + frames) | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| WR-01 regression test (registration, drag-begin cancellation, no-canceller control) | `vitest run PhysicsPaintLoopClipRail.test.tsx -t "registers its click-sequence canceller"` | 1 passed | ✓ PASS |
| Loop Clip rail tests (incl. WR-01) | `vitest run PhysicsPaintLoopClipRail.test.tsx` | 41 passed | ✓ PASS |
| Rail-set reducer tests | `vitest run physicsPaintRotoRailSetSelection.test.ts` | 32 passed | ✓ PASS |
| Batch drag session tests | `vitest run usePhysicsPaintRailSetDrag.test.ts` | 14 passed (incl. threshold-crossing clearClickSequence assertion) | ✓ PASS |
| Strip tests | `vitest run PhysicsPaintWorkflowStrip.test.ts PhysicsPaintWorkflowStrip.viewport.test.ts` | 135 passed | ✓ PASS |
| Solo arm + window tests | `vitest run physicsPaintSoloArm.test.ts physicsPaintRotoSoloWindow.test.ts` | 34 passed | ✓ PASS |
| move-rails / spacing-on-set resolver + parity | `vitest run physicsPaintRotoPhysicalResolver.test.ts physicsPaintRotoGroupParity.test.ts` | 227 passed | ✓ PASS |
| Bridge exact-match + coordinator + copy + playback | `vitest run physicPaintBridge.test.ts useRotoPhysicalEditCoordinator.test.ts useRotoTimelineActions.test.ts useRotoCachedPlayback.test.ts` | 298 passed (1 skipped) | ✓ PASS |
| Focus/capsule/strip/keyboard | `vitest run physicsPaintRailKeyboardNavigation.test.ts physicsPaintWorkflowPresentation.test.ts PhysicsPaintWorkflowStrip.test.ts physicsPaintStudioKeyboard.test.ts PhysicsPaintKeyRail.test.tsx PhysicsPaintLoopClipRail.test.tsx` | 308 passed | ✓ PASS |
| Full physic-paint regression | `vitest run src/components/physic-paint` | 1878 passed (85 files) | ✓ PASS |
| Full app suite | `vitest run` (from app/) | 2574 passed, 1 skipped, 101 todo (138 files) | ✓ PASS |
| Typecheck | `pnpm run typecheck` | exit 0 | ✓ PASS |

### Probe Execution

No probes declared for this phase — plan 43.6-01 declares "Spec-less probe fallback skipped this run" (REQUIREMENTS.md holds no 43.6 rows). Not applicable.

### Requirements Coverage

All 21 planning-local requirement IDs are accounted for across the plans' `requirements-completed` frontmatter and implemented. REQUIREMENTS.md holds no 43.6 rows (per ROADMAP); traceability is carried by the summaries' coverage metadata.

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| RSET-01 | 43.6-01 | Gestures + canonical ordering + set paint | ✓ SATISFIED | Reducer + routing + ordering tests |
| RSET-02 | 43.6-01 | Lifecycle: collapse/clear/empty/Escape/reconcile | ✓ SATISFIED | Studio wiring + Escape chain tests |
| RSET-03 | 43.6-03/04 | Set survives batch ops + exact Undo/Redo restore | ✓ SATISFIED | record/resolve snapshot round-trips |
| RSET-04 | 43.6-01 | One copy mapper (D-27) | ✓ SATISFIED | buildRailSetCopy M6 strings asserted |
| BMOV-01 | 43.6-02 | Rigid translation, one-unit clamp/reject | ✓ SATISFIED | move-rails branch + clamp authority |
| BMOV-02 | 43.6-03 | Drag any selected rail; collapse-first on unselected | ✓ SATISFIED | D-08 routing tests (WR-01 edge race closed by f7f5611b) |
| BMOV-03 | 43.6-03 | Ghost/gap/blocked-edge/live-readout paint | ✓ SATISFIED | CSS + strip tests |
| BMOV-04 | 43.6-02 | Shared-source algebra + straddle rejection | ✓ SATISFIED | Straddle verdict + 'move-rails-source-straddle' |
| BMOV-05 | 43.6-02 | Break/gap travel (D-11) | ✓ SATISFIED | deriveRailSetMoveIncomingInterpolationBreakKeyIds + tests |
| BMOV-06 | 43.6-03 | Locked copy, zero-delta no-publish, Escape cancel | ✓ SATISFIED | Mapper strings + drag hook tests |
| BDEL-01 | 43.6-04 | One dynamic scope classifier + keyboard parity | ✓ SATISFIED | classifyRotoDeleteTarget rail-set branch FIRST |
| BDEL-02 | 43.6-04 | Atomic mixed-set delete with per-type semantics + parent validation | ✓ SATISFIED | Shared proposer + bridge exact-match |
| BSPC-01 | 43.6-05 | Per-rail anchors, one atomic command | ✓ SATISFIED | spacing-on-set branch + tests |
| BSPC-02 | 43.6-05 | Whole-set preflight (all-or-nothing) | ✓ SATISFIED | D-25 hard walls + zero-partial-proposal tests |
| BSPC-03 | 43.6-05 | Scope-aware popover + locked copy | ✓ SATISFIED | forceSpacingScopeLine + M5 strings |
| SOLO-01 | 43.6-06 | Hide others + restrict range while playing | ✓ SATISFIED | getFrames filter seam tests |
| SOLO-02 | 43.6-06 | Session-only, save/reopen un-soloed | ✓ SATISFIED | Session module + no leak into lib/types |
| SOLO-03 | 43.6-06 | Strip button enablement + guarded tooltip | ✓ SATISFIED | Button markup + tooltip strings |
| SOLO-04 | 43.6-06 | Arm not transport; stopped scrub unrestricted | ✓ SATISFIED | Pitfall 3 regression; useRotoReferenceController unmodified |
| SOLO-05 | 43.6-06 | Solo range from effective boundaries incl. linked occurrences | ✓ SATISFIED | deriveSoloPlaybackWindow tests |
| SOLO-06 | 43.6-06 | Capsule line + exit layering | ✓ SATISFIED | Presentation + Escape chain tests |

No orphaned requirements: all 21 IDs claimed by a plan are implemented and tested.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `PhysicsPaintStudio.tsx` | 541-542 | Render-phase signal write (railSetSelection.value = null) | ℹ️ Info | Self-limiting, documented as mirroring the effectiveSelectedRotoKeyRail pattern (review IN-01) |
| `usePhysicsPaintRailSetDrag.ts` | 189-248 | Drag handlers close over pointerdown-render closures | ℹ️ Info | Stale-authority mitigated by proposalVersion fingerprint at commit (review IN-02) |
| `physicsPaintRotoRailSetSelection.ts` | 264 | Unbounded snapshot Map within a session | ℹ️ Info | Pruned on launch replacement only (review IN-03) |

No TBD/FIXME/XXX/PLACEHOLDER markers found in the new modules or the WR-01 fix (commit f7f5611b). The former WR-01 stub (`clearClickSequence: () => {}` on the batch rail-set session port) is replaced by the registry-iteration implementation; the remaining `clearClickSequence: () => {}` at the push-drag port (L1544) is the unrelated push-tool port that arms no rail timers.

### Human Verification Required

Native UAT items remain pending user testing (they are visual/interaction checks the automated suite cannot exercise). The WR-01 code fix is in place and regression-tested; the live check below is the final user-visible confirmation.

1. **Native UAT (final oracle)** — Run the 7-step spec acceptance + UI-SPEC Visual Acceptance Matrix rows 1-9 from a fully restarted frozen-code session (per Plan 06 `<verification>` and VALIDATION.md). Expected: multi-rail selection, batch Move/Delete/Key Spacing, and Solo playback behave per spec with visuals matching UI-SPEC M1-M6.
2. **Pending-click-then-drag race (WR-01 live check)** — Build a set with Cmd+clicks, then immediately drag a member within ~250ms of the last click. Expected: the set must not collapse or lose members mid-drag; the whole set moves together. The code fix (registry-based cancellation) and its regression test are in place; this is the user-visible confirmation.
3. **Solo real playback** — Arm Solo with a mixed selection, press Play, verify only the selected rails' content renders within the effective range and loops there; stopped scrubbing renders everything; save/reopen starts un-soloed.

### Gaps Summary

No remaining gaps. The sole gap from the initial verification — **WR-01** (`clearClickSequence` wired as a no-op stub on the batch rail-set drag session) — is closed by commit `f7f5611b`:

- `PhysicsPaintLoopClipRail.tsx` — each Loop Clip rail target registers its `clearClickSequence` canceller with the strip-owned registry via a mount-once ref guard (`registrationRef`); the unmount cleanup effect unregisters. The canceller clears the rail's `pendingSingleClickRef` (the 250 ms single-click timer) and `lastClickTimestampRef`.
- `PhysicsPaintWorkflowStrip.tsx` — the strip owns `railClickSequenceCancellersRef` (a `Set<() => void>`), the batch rail-set session's `clearClickSequence` port now iterates the registry on threshold crossing, and the `registerClickSequenceCanceller` prop handed to `PhysicsPaintLoopClipRail` adds/deletes cancellers.
- `PhysicsPaintLoopClipRail.test.tsx` — a regression test proves registration (called once), that the set-member pointer-down routes to the batch session (D-08), that invoking the registered canceller prevents the armed 250 ms timer from firing, and (control) that without a canceller the timer fires the single click.

The trigger chain is fully verified: the drag hook calls `input.clearClickSequence()` at `beginDrag` when the pointer crosses `RAIL_SET_DRAG_THRESHOLD_PX` (asserted by the drag-hook test), the strip port iterates the registry, and each rail's canceller clears the pending timer. The drag-hook test file (14 tests), the Loop Clip rail tests (41 tests), the strip tests (135 tests), the full physic-paint suite (1878 tests / 85 files), and the full app suite (2574 passed) are all green, with a clean typecheck. No regressions in the 15 previously-passed must-haves (all 16 now verified).

All must-haves are verified: the set is session-only (zero references in src/lib or src/types), the one-mapper copy discipline holds, the D-06 aftermath carrier restores exact sets across Undo/Redo for every batch kind, and the 43.3/43.4/43.5 invariants (break ownership, capacity authority, atomic history, parent exact-match, shared rail behavior) are preserved.

---

_Verified: 2026-08-18T23:10:00Z_
_Verifier: Claude (gsd-verifier)_
