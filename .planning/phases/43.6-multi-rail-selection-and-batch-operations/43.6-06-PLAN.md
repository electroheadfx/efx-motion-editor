---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 06
type: execute
wave: 5
depends_on: [43.6-01]
files_modified:
  - app/src/components/physic-paint/view/physicsPaintSoloArm.ts
  - app/src/components/physic-paint/view/physicsPaintSoloArm.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts
  - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements: [SOLO-01, SOLO-02, SOLO-03, SOLO-04, SOLO-05, SOLO-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Solo means hide others + restrict range: while armed and playing, only the selected rails' content renders and the play range restricts to the selection's effective coverage, looping within it by default (D-14)."
    - "Solo is session-only: never persisted, never in the document/history/session restore, never across the bridge; save/reopen always starts un-soloed; preview stills, export, and the canonical render source are never touched (D-14, D-17)."
    - "The Solo button lives in the strip action row in its own group immediately after the Push tool group, enables whenever ANY rail selection exists (a single selected Rail is a set of one), and disables with the guarded tooltip 'Select a Rail to solo.' when no rail selection exists (D-15, UI-SPEC M2)."
    - "Solo is an arm, not a transport command: toggling arms the filter/range; the user presses Play (or playback continues); solo persists across stop/start while the selection is unchanged (D-16)."
    - "Stopped scrubbing is unrestricted: the filter applies ONLY to playing frames; the stopped canvas renders everything at any cursor position, inside or outside the solo range (D-18)."
    - "The solo range derives from effective boundaries: first selected placement start to last selected effective end, including generated/linked occurrences of selected Groups (D-19)."
    - "While armed, the status capsule persistently carries 'Solo - {N} Rails, frames {A}-{B}.' (singular 'Solo - 1 Rail, ...') through the one mapper, and the button shows the armed orange state; both clear on exit (D-20)."
    - "Solo exits on re-click, Escape (first Escape layer before selection collapse), or any rail-selection change/clear; exit clears button tint and capsule copy immediately (D-04, D-14)."
    - "Audio preview keeps following the playhead unchanged over the restricted range with zero audio-layer changes (D-14; the monitor range derives from the filtered frame list)."
    - "Intentional gaps inside a selected Key Rail play as transparent frames (empty cells -> frame null -> existing missing-frame treatment); no special-casing (Research Open Question 4)."
  artifacts:
    - "app/src/components/physic-paint/view/physicsPaintSoloArm.ts — session solo arm signal module (isSoloArmed/toggleSolo/disarmSolo)"
    - "app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts — pure solo range + frame-attribution derivation"
    - "useRotoNavigationCoordinator.ts — getFrames solo filter seam (armed-only; disarmed byte-identical)"
    - "PhysicsPaintWorkflowStrip.tsx — Solo button + capsule projection"
  key_links:
    - "Solo arm signal -> getFrames enumeration seam -> useRotoCachedPlayback filtered list: the only path solo influences (breakage into findCachedRotoDisplayFrame = D-18 violation, Pitfall 3)"
    - "deriveSoloPlaybackWindow -> loopResolutionContext.ranges + deriveKeyRailSegments: the effective-boundary authority (breakage = range excludes generated/linked occurrences, D-19)"
    - "Escape chain order: popover dismiss -> push disarm -> solo disarm -> selection collapse: one Escape per layer (D-04)"
  reversibility_note: "D-17 is rated one-way (user-locked in CONTEXT.md with rationale: the spec forbids any document/history touch for solo). Decision already made at discuss-phase; no decision checkpoint is inserted. The presentation-only filter discipline is enforced by Task 2's stopped-canvas regression test."
---

<objective>
Deliver Solo playback: a session-only arm that filters the cached-playback frame enumeration to the selected rails' content within their effective frame range — a pure presentation filter that never touches the canonical render source, preview stills, export, the stopped canvas, or the document — with the strip toggle button, persistent capsule line, and layered exit discipline.

Implements SOLO-01..06 per D-14..D-20.

Purpose: solo rides the existing playback machinery (empty-list handling, null-frame transparent treatment, loop wrap, audio-range derivation are already correct for arbitrary frame lists — Research Pattern 5); the only new authorities are the arm signal and the range/attribution derivation.
Output: a user solos any rail selection and plays just that content in a loop, with everything else restored the moment solo exits.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-RESEARCH.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md
@app/src/components/physic-paint/view/physicsPaintPushArmedTool.ts
@app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): Solo arm module + pure solo window/frame-attribution derivation</name>
  <files>app/src/components/physic-paint/view/physicsPaintSoloArm.ts, app/src/components/physic-paint/view/physicsPaintSoloArm.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts, app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintPushArmedTool.ts (FULL — the armed-signal template: module-scope signal, subscribing read, peek read, boolean-returning disarm; copy its shape exactly)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts L229-237 (cell model: real/generated/empty — single kind per frame) and L4535-4547 (PhysicPaintRotoLoopRange fields incl. effectiveEnd)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts L139-172 (render-source union — single render source per frame; A3 verification input)
    - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts L37+ (KeyRailSegment fields: firstKeyFrame/lastKeyFrame/keyIds)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-RESEARCH.md Assumption A3 + Open Questions 3/4
  </read_first>
  <behavior>
    - physicsPaintSoloArm.ts: module-scope signal; `isSoloArmed()` subscribing read; `toggleSolo()` flips and returns true; `disarmSolo()` returns true ONLY when armed (one-Escape-one-layer consumption, D-04); module doc-comment contract: session-only, NEVER persisted, serialized, or part of any document/history snapshot — save/reopen always starts disarmed. No commitInFlight guard (solo never gates mutations, D-14).
    - deriveSoloPlaybackWindow({ members, keyRailSegments, loopRanges, cells, capacity }): returns null for an empty/unknown member set; otherwise `{ start, endExclusive, includesFrame(appFrame) }` where start = first selected placement start, endExclusive = last selected effective end (loop effectiveEnd includes generated/linked occurrences; Key Rail = lastKeyFrame + 1), clamped to capacity (D-19).
    - Attribution: a frame is included iff the accepted physical cells + loop ranges attribute it to a selected rail — real cells whose keyId belongs to a selected Key Rail or a selected Group's source/override keys, generated cells whose owning segment/loop belongs to a selected rail, empty cells inside a selected rail's span (they play transparent by construction — Open Question 4). Frames outside every selected rail's span are excluded.
    - A3 verification: tests on mixed timelines (adjacent Groups, Key Rail between Groups, placement overlap via visibleRanges) prove each frame attributes to at most one rail and the predicate matches the timeline paint's inputs.
  </behavior>
  <action>
    RED first: create both test files — arm lifecycle (toggle/disarm return values, one-Escape consumption semantics, disarm idempotence) and window derivation (range from effective boundaries incl. linked occurrences; attribution for real/generated/empty cells; empty-member null; capacity clamp; gap-transparency; A3 single-attribution fixtures). Then GREEN.

    Create view/physicsPaintSoloArm.ts mirroring physicsPaintPushArmedTool.ts exactly (no commitInFlight). Create roto/physicsPaintRotoSoloWindow.ts as a pure dependency-free module exporting `SoloPlaybackWindow` and `deriveSoloPlaybackWindow(input)` per the behaviors above, deriving attribution ONLY from the accepted physical cells + loopResolutionContext ranges + Key Rail segments — never from session paint (Pattern 5 discipline). Fail closed: unknown member identities or malformed inputs return null (solo simply does not arm a window).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/view/physicsPaintSoloArm.test.ts src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintSoloArm.test.ts exits 0 covering toggle/disarm semantics and the boolean-consumption contract.
    - physicsPaintRotoSoloWindow.test.ts exits 0 covering every behavior bullet including the A3 single-attribution fixtures and gap-transparency.
    - physicsPaintSoloArm.ts contains the doc-comment session-only contract (source assertion: 'never persisted' discipline comment present) and exports isSoloArmed/toggleSolo/disarmSolo.
    - Typecheck green.
  </acceptance_criteria>
  <done>The solo arm and the range/attribution authority exist as pure tested modules; no playback or view code touched yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getFrames solo filter seam (armed-only) + stopped-canvas isolation regression + playback absorption tests</name>
  <files>app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts, app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts L46-60 (the getFrames enumeration seam — the ONLY place the filter lives)
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts L141-215 (empty-list, null-frame, loop-wrap, audio-range absorption) and L86-96 (status gate)
    - app/src/components/physic-paint/hooks/useRotoReferenceController.ts L79-89 (findCachedRotoDisplayFrame — the stopped-canvas path that must stay untouched, Pitfall 3)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md §Shared Patterns (playback absorption of filtered frame lists)
  </read_first>
  <behavior>
    - The playback input gains an OPTIONAL `getSoloWindow?: () => SoloPlaybackWindow | null` port; absent or null returns the byte-identical 43.5 enumeration (disarmed = zero behavior change).
    - Armed: getFrames enumerates ONLY [start, endExclusive) intersected with the playback end, and yields `{ appFrame, frame: null }` for in-range frames failing attribution — the existing 'missing frames play transparent/background' treatment hides unselected content with zero rendering changes (D-17).
    - Downstream absorption requires NO changes: empty list -> status + no start; loop wraps over the filtered list; the audio monitor range derives from the filtered list's first/last appFrame (D-14 audio unchanged over the restricted range).
    - Pitfall 3 regression: solo armed + stopped leaves findCachedRotoDisplayFrame unfiltered — the stopped canvas renders everything at any cursor position (D-18).
  </behavior>
  <action>
    RED first: extend useRotoCachedPlayback.test.ts (or the navigation coordinator's existing test file if one exists — check for useRotoNavigationCoordinator.test.ts first and prefer extending it) with failing cases: disarmed enumeration byte-identical (same frames as today); armed enumeration restricted to the window with unattributed frames nulled; armed + empty window -> empty list; armed + stopped -> the display lookup path returns unfiltered frames (regression assertion that the filter is not applied outside getFrames). Then GREEN.

    useRotoNavigationCoordinator.ts: extend the playback input type with the optional getSoloWindow port and apply it inside the L49-56 getFrames closure exactly as scoped in <behavior> — start/end intersection, attribution-null frames, nothing else. Do not touch useRotoReferenceController.ts or any display path (D-18). No changes to useRotoCachedPlayback.ts itself — the absorption is already correct (Pattern: playback absorption); only its test file gains the solo-shaped list cases.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts && pnpm exec vitest run src/components/physic-paint/hooks && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove: disarmed enumeration equals the pre-solo enumeration (same length, same frame references); armed enumeration starts/ends at the solo window; unattributed in-range frames yield frame null; empty window yields an empty list.
    - The Pitfall 3 regression test exists and passes: armed solo does not alter the stopped-canvas display lookup.
    - useRotoReferenceController.ts is unmodified (git diff empty for that file at task completion).
    - Full hooks suite + typecheck green.
  </acceptance_criteria>
  <done>Solo filters exactly one seam — the playback frame enumeration — with the stopped canvas, preview, export, and canonical render provably untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Solo strip button + persistent capsule line + exit layering (re-click/Escape/selection change/lock)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx L1200-1260 (Push tool button block — the armed-orange idiom, own-group placement; Solo goes in its own group immediately after the Push group) + L499-515 (capsule) + L1690-1730 (capsule composition)
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts L155-185 (Escape chain — solo disarm inserts after the push disarm layer, before selection collapse)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx L565-585 (mutation-lock disarm effect idiom — add solo disarm on lock; solo never gates mutations), L300-360 (selection-change sites that must disarm solo: D-14)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts L641-691 (mapper family — the solo capsule line joins it)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md §M2 (button geometry/copy/paint: 26px physics-paint-roto-key-icon-button, 18px Focus icon, 10px 'Solo' label, armed classes byte-reused from the push armed idiom, aria-pressed, guarded tooltip idiom) + §M6/D-20 (capsule line)
  </read_first>
  <behavior>
    - Button: enabled whenever ANY rail selection exists (single rail = set of one, D-15) and not mutation-locked; disabled reason 'Select a Rail to solo.' when no rail selection, else the existing mapped lock reason; accessible name 'Solo selected Rails' with aria-pressed; tooltips: disarmed 'Solo the selected Rails - play only their content within their frame range. Click again or press Escape to exit.', armed 'Exit solo playback.' (UI-SPEC M2 locked).
    - Armed paint: border #F59E0B, background rgba(245,158,11,0.2), icon/label #FBBF24 (hover/focus #FBBF24 border, rgba(245,158,11,0.28) background, #FCD34D icon/label) — the 43.5 armed-tool classes reused byte-for-byte; geometry never changes.
    - Capsule: while armed, the persistent line 'Solo - {N} Rails, frames {A}-{B}.' (singular 'Solo - 1 Rail, ...') projects at render time from the arm signal + the Plan 01 mapper + deriveSoloPlaybackWindow — never a status event stream (Pitfall 7); it clears on exit.
    - Exits: re-click toggles off; Escape consumes one layer (after popover dismiss and push disarm, before selection collapse — D-04); ANY rail-selection change/clear (including collapse to a different rail) disarms; mutation-lock entry disarms (solo never gates mutations — disarm-on-lock only, mirroring the push effect); launch replacement/unmount starts disarmed.
    - Solo persists across stop/start while the selection is unchanged (D-16); arming never starts or stops transport.
    - No keyboard shortcut arms solo (43.4/43.5 precedent).
  </behavior>
  <action>
    RED first: extend physicsPaintWorkflowPresentation.test.ts (capsule solo line projection: armed shows the locked line with correct singular/plural and window frames; disarmed shows nothing) and useRotoTimelineActions.test.ts (mapper solo strings verbatim). Then GREEN.

    useRotoTimelineActions.ts: extend the Plan 01 mapper with the solo capsule form (one function owns it, D-27).

    physicsPaintWorkflowPresentation.ts + PhysicsPaintWorkflowStrip.tsx: add the solo line as a persistent render-time projection input to the capsule composition (highest-priority persistent term while armed; existing pendingOperation/feedback/ambient grammar otherwise unchanged), and add the Solo button in its own action-row group immediately after the Push group per UI-SPEC M2 (Focus icon from lucide-preact 0.577.0 — verified export; guarded tooltip via the existing buildGuardedActionTooltipCopy idiom; aria-pressed; armed classes byte-reused).

    physicsPaintStudioKeyboard.ts: add the solo disarm layer to the Escape chain after the push disarm layer (actions.disarmSolo?.() consuming the boolean), keeping one-Escape-one-layer.

    PhysicsPaintStudio.tsx: wire the strip ports (isSoloArmed/toggleSolo reads, getSoloWindow port construction from the Plan 01 set/single-rail selection + Task 1 derivation), disarm solo on any rail-selection change/clear (the selection setters — set-switch, collapse, frame/key selection), on mutation-lock entry (beside the L576 push effect; solo never gates mutations), and on launch replacement (beside the L152-164 reset block). Wiring only — the Studio body gains no derivation logic (43.4 focused-module precedent).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - Presentation tests assert the exact strings: 'Solo - 3 Rails, frames 12-88.' and 'Solo - 1 Rail, frames 12-40.'; disarmed projects nothing.
    - The disabled tooltip string is exactly 'Select a Rail to solo.' (source assertion in the strip or a test).
    - The button carries aria-pressed and reuses the existing armed-tool class (source assertion: the Solo button markup references the same armed class the Push button uses; no new armed color literal).
    - Escape chain order test or source assertion: the solo disarm layer sits between the push disarm layer and selection collapse in physicsPaintStudioKeyboard.ts.
    - Solo module state is never read by persistence, history, or bridge code paths — the existing strict-parse and revision-fingerprint suites stay green (session-only boundary, T-43.6-03).
    - Full physic-paint suite + typecheck green; preview/export code paths unmodified (git diff shows no changes under export/preview modules).
  </acceptance_criteria>
  <done>Solo arms from any rail selection, filters playing frames only, shows the armed button + persistent capsule line, and exits cleanly via re-click/Escape/selection change — with zero document, export, or stopped-canvas impact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session arm -> playback enumeration | The solo window is derived from session selection but must never write back to document/history |
| playback filter -> canonical render | The filter consumes cached frames only; the canonical render source is out of reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-03 | Information Disclosure (integrity) | solo state leaking into document/history/persistence/export | high | mitigate | Module-scope session signal with the doc-comment contract; getFrames-only filter seam; strict-parse/revision suites green; export/preview modules untouched (Task 2/3 acceptance criteria) |
| T-43.6-02 | Tampering/Repudiation | solo range derived from stale selection | medium | mitigate | deriveSoloPlaybackWindow fails closed to null on unknown members; selection change disarms solo (D-14); Plan 01 reconcile clears stale sets |
| T-43.6-08 | Tampering | stopped-canvas filtering (wrong surface) | medium | mitigate | Filter exists only inside the getFrames enumeration; Pitfall 3 regression test pins findCachedRotoDisplayFrame isolation (D-18) |
| T-43.6-SC | Tampering | package installs | low | mitigate | No package installs in this plan; legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/physic-paint` green after every task; full suite + typecheck before plan completion.
- VALIDATION.md row 43.6-SOLO-01 (SOLO-01..06, T-43.6-03) covered by the two new test files + playback/presentation extensions. Wave 0 obligations (solo arm module test; solo range/attribution predicate tests) satisfied by Task 1 RED.
- UI Considerations lifted: M2 empty/populated/error/long-text/overflow, M6 status capsule rows — all in must_haves.truths.
- Native UAT (final oracle, user-run from a fully restarted frozen-code session): the prompt's 7-step acceptance list + UI-SPEC Visual Acceptance Matrix rows 1-9, including solo-specific row 7 (playing filters, stopped scrub renders everything, save/reopen starts un-soloed, preview/export byte-identical with and without solo).
</verification>

<success_criteria>
- Solo is a pure presentation arm: it hides unselected rails' content during playback within the selection's effective range, loops there, keeps audio following the playhead, and leaves every other surface — stopped canvas, preview, export, document, history — byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-06-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 06)

New symbols (for plan-review-convergence source grounding):
- `isSoloArmed` / `toggleSolo` / `disarmSolo` (functions) — view/physicsPaintSoloArm.ts
- `SoloPlaybackWindow` (type) + `deriveSoloPlaybackWindow` (function) — roto/physicsPaintRotoSoloWindow.ts
- `getSoloWindow` (optional playback input port) — hooks/useRotoNavigationCoordinator.ts
- `disarmSolo` keyboard action — view/physicsPaintStudioKeyboard.ts
- Solo capsule mapper form — hooks/useRotoTimelineActions.ts
- New files: app/src/components/physic-paint/view/physicsPaintSoloArm.ts, app/src/components/physic-paint/view/physicsPaintSoloArm.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts, app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts
</output>
