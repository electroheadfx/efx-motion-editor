---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 11
type: execute
wave: 8
depends_on: ["43.6-06", "43.6-09", "43.6-10"]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements: [SOLO-03]
gap_closure: true
gap_ids: [REVIEW-WR-01]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Solo exits on re-click, Escape, or ANY rail-selection change/clear — including a plain Loop Rail click that moves the single-rail selection; exit clears button tint and capsule copy immediately (43.6-06 must_have truth 8; D-04, D-14 user-locked: 'changing or clearing the rail selection ends solo immediately')"
    - "Solo persists across stop/start while the selection is unchanged, and the moment the rail selection changes by ANY path — including the plain Loop Rail click and the double-click-to-open-editor path that plain-selects first (PhysicsPaintLoopClipRail.tsx L234) — the arm ends (SOLO-03; D-14, D-16)"
    - "Selection change/clear exits solo immediately with copy clearing (UI-SPEC UI Considerations, M2 Solo button error row — resolved explicit, lifted)"
    - "An armed Solo never silently retargets: after a plain Loop Rail click the getSoloWindow armed gate (PhysicsPaintStudio.tsx L1153) returns null so playback and the armed capsule cannot fall back to soloing the newly clicked rail"
  artifacts:
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  key_links:
    - "handleSelectRotoLoopClip plain-click success branch -> disarmSolo() -> isSoloArmed() === false -> getSoloWindow early null return (L1153) -> playback enumeration byte-identical to disarmed (the D-14 exit path this plan wires)"
  prohibitions:
    - "The plain-click success branch of handleSelectRotoLoopClip must not call clearRotoLoopSelection() to achieve the disarm — it would clobber the selection being written; the disarm is a direct disarmSolo() call only (VERIFICATION missing directive)"
---

<objective>
Close the single remaining verification gap from 43.6-VERIFICATION.md (score 8/9): the plain
Loop Rail click success path in `handleSelectRotoLoopClip` (PhysicsPaintStudio.tsx L1415-1450)
never calls `disarmSolo()` — it is the ONLY rail-selection change path that skips the D-14
disarm. Arm Solo on Loop A (single rail = set of one, D-15), then plain-click Loop B: Solo
stays armed and `getSoloWindow` (L1154-1161) falls back to the single-rail selection, so
playback and the armed capsule silently retarget to solo Loop B. This violates must-have
truth 9 (43.6-06 truth 8; D-04, D-14 user-locked) and leaves SOLO-03 PARTIAL.

Purpose: restore the user-locked D-14 invariant on the one missed path so every rail-selection
change/clear ends solo immediately. The equivalent plain click on a Key Rail already disarms
via `clearRotoLoopSelection()` (L1351); the loop plain path cannot use that helper (it would
clobber the selection being written), so it disarms directly.

Output: a one-line production change (`disarmSolo();` with a 43.6-06 D-14 comment, inserted
after `railSetSelection.value = null;` in the plain-click success branch) plus a Studio-level
regression test pinning the exit path. No other selection path, the reducer, or any plan
01–10 artifact is touched.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md

# The authoritative gap definition (exact lines, exact missing change)
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-VERIFICATION.md

# The review warning (REVIEW-WR-01) that surfaced the gap, with the prescribed fix shape
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-REVIEW.md

# Origin of must_have truth 8 / D-14 solo exit discipline
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-06-PLAN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer, RED→GREEN): plain Loop Rail click disarms an armed Solo (REVIEW-WR-01)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (handleSelectRotoLoopClip L1365-1456 — the plain-click success branch L1415-1455 is the edit site; clearRotoLoopSelection L1295-1302 — the documented D-14 contract 'every rail-selection setter routes through this clear — ANY rail-selection change/clear disarms an armed Solo'; the key-rail plain path L1349-1364 — the sibling path that DOES disarm via clearRotoLoopSelection at L1351; getSoloWindow L1147-1171 — the armed gate at L1153 and the single-rail fallback L1156-1161 that causes the silent retarget; disarmSolo call sites L185, L386, L653/656, L1298, L1950 — the existing exit paths this fix joins)
    - app/src/components/physic-paint/view/physicsPaintSoloArm.ts (disarmSolo semantics — returns true only when armed, idempotent when disarmed; already behaviorally tested in physicsPaintSoloArm.test.ts, so calling it unconditionally in the plain branch is safe on the disarmed path)
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts (the 'Physics Paint multi-rail selection SET wiring (43.6-01)' describe L460-598 — the WR-01 cross-type test at L528 and the solo armed-gate test at L567 are the source-contract conventions the new test mirrors: studio.indexOf slice boundaries, positive toContain assertions, ordering assertions via indexOf)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-VERIFICATION.md (the gap's 'missing' directive — the exact insertion point and the explicit prohibition on clearRotoLoopSelection in this branch)
  </read_first>
  <behavior>
    - RED: the new Studio source-contract test fails today because the loop plain-click branch slice (from the plain-click collapse comment inside the handleSelectRotoLoopClip slice to the end of that handler) contains no `disarmSolo();` call.
    - GREEN: after the fix, the same slice contains `disarmSolo();` positioned BEFORE the new selection is applied (before `selectedLoopClipIds.value = next.selectedLoopClipIds;`), so the arm ends before the single-rail selection moves — the armed gate at L1153 then returns null for the new selection.
    - The pre-existing behavior of the branch is otherwise byte-identical: same collapse of railSetSelection, same reducer call, same next === null sub-branch, same selection writes, same script-selection tail.
    - No existing test is modified or deleted.
  </behavior>
  <action>
    RED first: in PhysicsPaintStudio.test.ts, extend the 'Physics Paint multi-rail selection SET wiring (43.6-01)' describe block (place the new it block immediately after the solo armed-gate test at L567-578) with one new it block named 'disarms an armed Solo when a plain Loop Rail click changes the selection (43.6-11)'. It slices the handleSelectRotoLoopClip handler (from 'const handleSelectRotoLoopClip = useCallback((' to 'const handleOpenRotoLoopEdit'), exactly like the WR-01 test at L545-547), then slices the plain-click branch from the 'Plain click collapses the set into the single-rail path' comment within that handler slice (unique inside the loop handler slice — the key-rail handler's identical comment is outside it) to the end of the handler. Assert the branch slice contains `disarmSolo();` and that its index is less than the index of `selectedLoopClipIds.value = next.selectedLoopClipIds;` (disarm before the selection write). Run the file and confirm ONLY the new test fails. Commit `test(43.6-11): add failing solo-disarm regression test for the plain Loop Rail click`.

    GREEN: in PhysicsPaintStudio.tsx, in the plain-click success branch of handleSelectRotoLoopClip (currently L1415-1416), insert `disarmSolo();` immediately after `railSetSelection.value = null;`, preceded by a comment citing 43.6-06 (D-14): a plain Loop Rail click is a rail-selection change, so an armed Solo disarms before the new selection is applied — direct call, NOT clearRotoLoopSelection(), because that helper would clobber the selection this branch is about to write. Do NOT touch: the reducer call, the next === null sub-branch (its clearRotoLoopSelection stays), the selection writes, the script-selection tail, the useCallback dependency array (disarmSolo is a module-scope import, not a reactive dependency), or any other handler. Run the Studio suite — the new test passes, all 75 pre-existing tests stay green. Then run the full five-suite regression sweep from 43.6-VERIFICATION.md and typecheck. Commit `fix(43.6-11): disarm an armed Solo on plain Loop Rail selection change (REVIEW-WR-01)`.

    REFACTOR: none expected — the change is one line plus a comment; if the comment wording needs alignment with the neighboring D-14 comment style (L1296-1298), tidy it in the GREEN commit.

    Harness note (verification fidelity): the verification's prescribed scenario is 'arm Solo with Loop A selected, plain-click Loop B, assert isSoloArmed() === false'. PhysicsPaintStudio.test.ts is a source-contract harness (it reads the component source as text — no mounted component), so the scenario is pinned as a wiring contract on the exact branch, while disarmSolo's behavioral semantics (isSoloArmed() flips false only when armed) are already covered in physicsPaintSoloArm.test.ts. The live end-to-end scenario is covered by the pending native UAT re-runs G-43.6-1/-8, which the verification explicitly scheduled after this closure.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/physicsPaintSoloArm.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts && cd app && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the handleSelectRotoLoopClip plain-click branch slice (from the plain-click collapse comment inside the handler slice to the end of the handler) contains `disarmSolo();`.
    - Source assertion: within that branch slice, `disarmSolo();` appears before `selectedLoopClipIds.value = next.selectedLoopClipIds;` (disarm precedes the selection write).
    - Source assertion: the branch still contains `updatePhysicsPaintRotoLoopClipSelection(` and the `next === null` sub-branch retains its `clearRotoLoopSelection();` call (branch structure preserved).
    - The new it block 'disarms an armed Solo when a plain Loop Rail click changes the selection (43.6-11)' exists in PhysicsPaintStudio.test.ts and passes.
    - PhysicsPaintStudio.test.ts exits 0 (all pre-existing tests plus the new one — 76 total).
    - The six-suite sweep in <verify> exits 0 with zero failures; `tsc --noEmit` exits 0.
    - git diff on PhysicsPaintStudio.tsx touches only the plain-click branch of handleSelectRotoLoopClip (one comment + one statement); physicsPaintRotoRailSetSelection.ts, useRotoPhysicalEditHistory.ts, PhysicsPaintLoopClipRail.tsx, and PhysicsPaintWorkflowStrip.tsx diffs are empty.
    - RED commit and GREEN commit exist with the messages specified in <action>.
  </acceptance_criteria>
  <done>
    Every rail-selection change path — including the plain Loop Rail click and, transitively, the double-click-to-open-editor path that plain-selects first — disarms an armed Solo before the new selection applies; the regression test pins the exit path; the sweep and typecheck are green.
  </done>
  <reversibility rating="reversible">One additive statement plus one test; reverting the GREEN commit restores the prior behavior with zero data or API impact.</reversibility>
</task>

</tasks>

## Artifacts this phase produces

This gap-closure plan modifies EXISTING functions only — no new exports, no new modules, no API changes.

New test surface:

- `PhysicsPaintStudio.test.ts` — one new it block in the 'Physics Paint multi-rail selection SET wiring (43.6-01)' describe: 'disarms an armed Solo when a plain Loop Rail click changes the selection (43.6-11)' (REVIEW-WR-01 regression).

Modified (existing) production symbols:

- `handleSelectRotoLoopClip` (PhysicsPaintStudio.tsx) — the plain-click success branch gains a direct `disarmSolo();` call with a 43.6-06 D-14 comment immediately after the set-collapse statement; useCallback dependency array unchanged (module-scope import).

Explicitly NOT produced: no changes to physicsPaintSoloArm.ts, physicsPaintRotoSoloWindow.ts, physicsPaintRotoRailSetSelection.ts, useRotoPhysicalEditHistory.ts, PhysicsPaintLoopClipRail.tsx, PhysicsPaintWorkflowStrip.tsx, or any plan 01–10 artifact.

## Scoped spec-less probe lift (43.6-EDGE-COVERAGE-FALLBACK.json)

Scope: only the probe rows for requirements this gap touches. The gap touches SOLO-03; the SOLO-03
row is the single unclassified row ('unclassified — review manually'). Surfaced: 1; authored: 1;
flagged assumptions: 0.

- SOLO-03 / unclassified — RESOLVED (verification: explicit): "Solo persists across stop/start while the selection is unchanged, and ANY rail-selection change — now including the plain Loop Rail click — ends the arm immediately." Lifted into `must_haves.truths` (second truth). The plans 01–10 verification record plus this plan's regression test are the evidence.

The remaining 58 probe rows (including the SOLO-01/02/04/05/06 rows) are owned by plans 01–10,
which were executed and verified 8/9 with the only failure being THIS gap; they are out of scope
for this closure and are not re-litigated here.

UI-SPEC `## UI Considerations` lift (same rule, scoped to the gap): the only applicable row is
'M2 Solo button / error' — resolved explicit: "selection change/clear exits solo immediately with
copy clearing". Lifted into `must_haves.truths` (third truth). All other rows concern surfaces
this plan does not touch.

Assumption-delta: no-change. This closure restores an existing invariant on one missed path; it
introduces no singular→plural, required→optional, or derived→chosen transition.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail click gesture -> session solo arm | the disarm consumes only session-only selection/arm state; no document, history, persistence, or bridge surface is reachable from this path |

## STRIDE Threat Register

This is a one-line UI-selection-state fix restoring an existing invariant: no untrusted external
input, no network surface, no persistence, no new dependencies — no new attack surface is
introduced. One item considered and dismissed rather than invented:

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-11-01 | Tampering | disarmSolo call ordering in the plain branch | low | accept | Considered and dismissed: the disarm runs before the selection write and disarmSolo is idempotent on the disarmed path (behaviorally tested in physicsPaintSoloArm.test.ts); calling it unconditionally cannot corrupt document or history state because the solo arm is session-only by construction (D-14/D-17, T-43.6-03 mitigations unchanged). |
</threat_model>

<verification>
- Studio suite: `cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts` — exits 0 with the new regression test green.
- Six-suite sweep (the verification report's five suites plus the solo arm suite): `pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/physicsPaintSoloArm.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` — all green.
- Typecheck: `cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec tsc --noEmit` passes.
- Diff discipline: only PhysicsPaintStudio.tsx (one branch) and PhysicsPaintStudio.test.ts (one it block) change.
</verification>

<success_criteria>
- REVIEW-WR-01 closed: arming Solo on Loop A and plain-clicking Loop B disarms Solo before the selection moves; the armed gate at L1153 returns null for the new selection, so playback and the capsule can never silently retarget.
- Must-have truth 9 (43.6-06 truth 8; D-04, D-14) restored to satisfiable at re-verification; SOLO-03 moves from PARTIAL to satisfiable.
- NOTE (sequencing, per 43.6-VERIFICATION.md UAT cross-reference): native UAT re-runs for G-43.6-1, G-43.6-3, G-43.6-4, and G-43.6-8 are unblocked by this closure but remain human-run — do not mark those UAT gaps resolved at code-green time.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-11-SUMMARY.md` when done
</output>
