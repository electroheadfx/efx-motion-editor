---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 09
subsystem: ui
tags: [preact, css, solo-playback, armed-state, tdd]

requires:
  - phase: 43.6-multi-rail-selection-and-batch-operations
    provides: 43.5 armed-tool orange language (border #F59E0B, background rgba(245,158,11,0.2), icon/label #FBBF24) and the 43.6-06 solo port wiring (getSoloWindow)
provides:
  - Solo strip button renders the armed orange tint while solo playback is active and clears on disarm
  - Solo playback window gated on isSoloArmed() — a plain rail selection no longer restricts playback
  - Source-contract test pinning the Solo button compound-selector class pairing
affects: [phase-44, solo, uat]

actuals:
  tokens: 1709
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Compound CSS selector contract pinned by a source-contract test (className template must carry base + armed classes together)"
    - "Derived playback windows must gate on the armed signal, not merely on the presence of a rail selection"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts

key-decisions:
  - "Mirror the Push button class template exactly on the Solo button — reuse the existing compound selector rather than adding new CSS"
  - "Gate getSoloWindow on isSoloArmed() first so a disarmed rail selection plays the full timeline"

patterns-established:
  - "Armed-tool visuals: base class + conditional armed class in one template literal, pinned by a source-contract test"

requirements-completed: [SOLO-02, SOLO-06]

coverage:
  - id: D1
    description: "Solo button carries the physics-paint-push-tool-button base class plus the conditional armed class, so the existing compound selector renders the orange tint while armed"
    requirement: SOLO-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts#Solo button template source contract"
        status: pass
      - kind: manual_procedural
        ref: "Native UAT — user verified orange tint renders, persists during solo playback, clears on disarm, and disarmed look is unchanged"
        status: pass
    human_judgment: true
    rationale: "The tint is a purely visual armed-state language; the source-contract test proves the class pairing but only a human can confirm the rendered color language reads correctly in the running app"
  - id: D2
    description: "Solo playback window derives from rail selection only when solo is armed — disarm-then-select-rail plays the full timeline, armed solo still restricts playback"
    requirement: SOLO-06
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#solo window armed gate"
        status: pass
      - kind: manual_procedural
        ref: "Native UAT — user verified disarm-then-select-rail plays the FULL timeline and armed solo still restricts playback correctly"
        status: pass
    human_judgment: true
    rationale: "Playback-range behavior is timing-sensitive in the real Studio loop; the unit test pins the gate logic but live playback confirmation required the human checkpoint"

duration: 12min automated + human-verify pause
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 09: Solo Armed Orange Tint + Solo Window Armed Gate Summary

**Solo strip button now renders the 43.5 armed orange tint (base-class fix), and the solo playback window is gated on the armed signal so a plain rail selection no longer restricts playback.**

## Performance

- **Duration:** ~12 min automated execution + human-verify pause
- **Started:** 2026-08-19T09:54:00+02:00
- **Completed:** 2026-08-19 (native UAT approved)
- **Tasks:** 3 (Task 1 fix, Task 2-RED test, Task 2-GREEN fix) + blocking human-verify checkpoint
- **Files modified:** 4

## Accomplishments
- Solo button renders the armed orange tint (border #F59E0B, translucent orange background, #FBBF24 icon/label) while solo playback is active and clears on disarm — root cause RC-C fixed by adding the missing `.physics-paint-push-tool-button` base class so the existing compound selector matches.
- Disarmed Solo rendering is byte-identical to the pre-fix state (the base class carries no visual when not armed).
- Solo playback window defect fixed: `getSoloWindow` in PhysicsPaintStudio.tsx previously derived a filter window from ANY rail selection without checking `isSoloArmed()`; it now gates on the armed signal first, so disarm-then-select-rail plays the FULL timeline while armed solo still restricts playback.
- Full physic-paint suite green (1892 tests / 85 files) and `tsc --noEmit` clean.
- Gap IDs closed: G-43.6-2, G-43.6-7.

## Task Commits

Each task was committed atomically:

1. **Task 1: add the physics-paint-push-tool-button base class to the Solo button template and pin it with a test** - `f44c30d7` (fix)
2. **Task 2-RED: failing test — solo window must gate on the armed signal** - `36929636` (test)
3. **Task 2-GREEN: gate the solo playback window on the armed signal** - `bd96df52` (fix)

**Plan metadata:** docs commit follows this summary.

_Note: Task 2 was executed TDD-style (RED then GREEN) as a UAT-driven deviation; the plan's original Task 2 was the blocking human-verify checkpoint, which the user approved._

## Files Created/Modified
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` - Solo button className template now carries `physics-paint-push-tool-button` base class + conditional armed class
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` - source-contract test pinning the base+armed class pairing in the Solo button template
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - `getSoloWindow` gated on `isSoloArmed()` before deriving the filter window from rail selection
- `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` - failing-then-passing test for the solo window armed gate

## Decisions Made
- Mirrored the Push button class template exactly on the Solo button (L3330) instead of adding a Solo-specific CSS rule — reuses the existing compound selector and keeps the 43.5 armed language single-sourced.
- Gated `getSoloWindow` on `isSoloArmed()` as the first check rather than filtering downstream — the window must not exist at all when solo is not armed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Solo playback window derived from rail selection without checking the armed signal**
- **Found during:** Task 2 (human-verify preparation / UAT triage)
- **Issue:** The plan's `files_modified` declared only the WorkflowStrip files, but UAT surfaced a second defect: with solo disarmed, selecting any rail still restricted playback to a solo window. Root cause lived in the 43.6-06 solo port wiring — `getSoloWindow` in PhysicsPaintStudio.tsx derived a filter window from any rail selection without checking `isSoloArmed()`.
- **Fix:** Gate `getSoloWindow` on `isSoloArmed()` first; executed TDD-style (failing test `36929636`, then implementation `bd96df52`).
- **Files modified:** app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts (both outside the plan's declared `files_modified` — approved deviation)
- **Verification:** New test fails before the fix and passes after; full physic-paint suite green (1892 tests / 85 files); native UAT confirmed disarm-then-select-rail plays the FULL timeline and armed solo still restricts playback.
- **Committed in:** `36929636` (RED), `bd96df52` (GREEN)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** The deviation was required to close G-43.6-7's full UAT surface; no scope creep — the fix is a two-line gate plus its test.

## Issues Encountered
None beyond the UAT-reported solo-window defect documented as the deviation above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 09 of 9 complete for phase 43.6; G-43.6-2 and G-43.6-7 closed.
- Solo armed visual language and playback-window gating are now consistent and UAT-approved; phase 43.6 gap-closure series can proceed to phase close-out.

## Self-Check: PASSED
- FOUND: `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` (commit f44c30d7)
- FOUND: `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` (commit f44c30d7)
- FOUND: `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` (commit 36929636)
- FOUND: `app/src/components/physic-paint/PhysicsPaintStudio.tsx` (commit bd96df52)
- FOUND commit: f44c30d7, 36929636, bd96df52 in `git log`

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*
