---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/physicsPaintRailKeyboardNavigation.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: [RSET-01, RSET-02, RSET-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cmd/Ctrl+click toggles one rail of any kind in/out of the set; every member paints the existing orange 3px selection line (D-01, UI-SPEC M1)."
    - "Shift+click replaces the set with the canonical anchor-to-clicked range ordered by canonical first-key frame across all three rail kinds in one shared ordering; the anchor stays put (D-01, D-02)."
    - "Cmd+Shift+click ADDS the anchor-to-clicked range to the current set (union); already-selected members stay selected; the anchor does not move (D-03)."
    - "The anchor is the last plain-clicked rail and carries a 2px wide x 8px high #F2F5F7 tick at 70% opacity at its leading edge, paint-only, aria-hidden (D-02, UI-SPEC M1)."
    - "Cmd+click toggling off the last member leaves a valid empty set: no orange line, no anchor tick, no set copy (D-05, UI-SPEC M1 empty)."
    - "Plain click on any rail collapses the set to that single rail; plain click on empty strip/frame clears the set; selecting a physical frame or key clears the rail set (D-04, 43.4 mutual exclusion extended)."
    - "Stale authority (membership or document revision changed under a set) clears the invalid set instead of inventing a fallback scope (UI-SPEC M1 error)."
    - "The status capsule carries one deterministic set copy: '{N} Rails selected - frames {A}-{B}.' with type breakdown when mixed and correct singular/plural; an empty set produces no set copy (D-27, UI-SPEC M6)."
    - "Every set member's tooltip gains the set sentence (' One of {S} selected Rails - drag moves the set, Delete removes the set.'; anchor form prefixed ' Range anchor.') (UI-SPEC M1)."
    - "Roving Tab/arrow cycling visits set members in canonical first-frame order through the existing shared 43.4 focus machinery (physicsPaintRailKeyboardNavigation.ts, ordered by data-rail-first-frame across all three rail kinds) and falls through to ordinary rail cycling outside the set - no set-aware focus fork exists (UI-SPEC Keyboard and focus, matrix row 8); Space on a focused member plain-selects it and collapses the set (D-04)."
    - "The rail set is session-only: it never enters the canonical revision fingerprint, history, persistence, or any bridge payload (process boundary; D-05)."
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts - pure reducer module (identities, state, gestures, reconcile, unified ordering, set aftermath scaffolding)"
    - "app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts - Wave 0 test file (created by Task 1 RED)"
    - "PhysicsPaintStudio.tsx - session set signals + reconcile wiring (additive, beside L127-139)"
    - "useRotoTimelineActions.ts - one set-copy mapper family (D-27)"
  key_links:
    - "Rail components -> updatePhysicsPaintRotoRailSetSelection -> Studio set signal -> membership orange paint (gesture-to-paint link; breakage = gestures do nothing)"
    - "Studio set signal -> one mapper (useRotoTimelineActions) -> capsule/tooltips/a11y (copy link; breakage = surfaces disagree)"
    - "Accepted-revision reconcile -> set cleared on stale membership (Pitfall 2; breakage = set copy names rails that no longer exist)"
---

<objective>
Establish the Phase 43.6 multi-rail selection SET as a new explicit, session-only selection scope: one pure reducer module owning rail-set state transitions (plain/toggle/range/union gestures over a caller-supplied canonical cross-type ordering, empty set allowed, fail-closed reconcile), Studio session signals, modifier routing in both rail components, orange set paint with the anchor tick, the single deterministic set-copy mapper, and post-acceptance set-aftermath scaffolding that Plans 03/04 wire to their batch operation kinds.

Implements RSET-01 (gestures + canonical ordering), RSET-02 (lifecycle: collapse/clear/empty/Escape layer/reconcile), RSET-04 (one copy mapper) per D-01..D-05 and D-27.

Purpose: every later 43.6 plan (batch Move, Delete, Key Spacing, Solo) consumes this set authority; landing it first with a working end-to-end gesture slice proves the cross-type ordering and session-state seams before any batch operation exists.
Output: working set gestures visible in the strip (toggle a Loop Rail in and out, watch orange paint and set copy), plus the pure reducer/copy/aftermath authorities all later plans consume.
</objective>

<assumption_delta_decision>
Detector fired (pluralization: "select multiple Rails … alongside single-rail"). Decision: **add-alongside**, locked by CONTEXT D-01..D-05 and the spec ("Multi-selection is a new explicit selection scope alongside the existing single-rail and physical-frame selections"; "no change to single-rail behavior"). The rail set is a NEW session scope; single-rail selection (`selectedLoopClipIds` / `selectedRotoKeyRail`) remains the primary degenerate path — plain click collapses the set into it (D-04). Solo treats a single selected rail as a set of one for enablement only (D-15); it does not promote the set over single-rail selection. Accepted debt if `add-alongside` were ever wrong: none anticipated — a later "promote" would unify the two rail-selection signals into the set representation; the reducer's discriminated identity already names the general noun (`RailSetIdentity`), so promotion would be a consumer-rewiring, not a model change.
Invariant test (accepted): Task 1 adds a contract test asserting the reducer's canonical ordering input is the SAME ordered identity list consumed by gestures, copy, and (in later plans) clamp/spacing/solo — one ordering authority, no per-surface re-derivation.
</assumption_delta_decision>

<specless_probe_note>
Spec-less probe fallback skipped this run: REQUIREMENTS.md holds no 43.6 IDs to probe and no SPEC Edge Coverage/Prohibitions sections exist. CONTEXT.md (D-01..D-27) and the approved 43.6-UI-SPEC.md are the authority. No probe predicates generated.
</specless_probe_note>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-RESEARCH.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md
@app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.ts
@app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): Pure rail-set reducer + unified ordering + Studio signals + Cmd/Ctrl+click toggle end-to-end on the Loop Rail</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.ts (gesture/anchor/reconcile template, L20-124 — copy its shape)
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts (Phase 37 reducer header contract L1-17; the never-empty no-op at L85 must NOT be ported — D-05)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx L90-140, L300-360, L1160-1240 (selection signals, reconcileRotoKeyRailSelection template, handleSelectRotoLoopClip)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx L60-200 (RailMouseEvent shape, modifier routing L159-163, selection paint)
    - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts L37+ (deriveKeyRailSegments signature — Key Rail ordering input)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts L4535-4547 (PhysicPaintRotoLoopRange fields: placementStart, effectiveEnd, loopId)
  </read_first>
  <behavior>
    - Reducer: plain click returns a fresh one-member set with anchor = clicked identity; toggle adds an absent identity (anchor unchanged) or removes a present one (anchor falls back to the first ordered member when the anchor was removed); toggling off the last member returns null (empty = no rail selection scope, D-05).
    - Reducer is fail-closed: unknown identity, malformed identity, or non-unique ordered list returns the unchanged selection (or null) — never fabricates membership.
    - deriveRailSetOrder: merges Key Rail segments (identity { kind: 'key-rail', firstKeyId }) and loop ranges (identity { kind: 'loop', loopId }) into one list sorted by canonical first frame ascending; ties break by kind ('key-rail' before 'loop') then identity id ascending (planner choice within Claude's Discretion — deterministic tie-break).
    - Union and range gestures are Task 2 scope; this task lands 'plain' and 'toggle' only.
    - Studio: new session-only signal holds the set (null when empty); selecting a Loop Rail with Cmd/Ctrl routes to the set reducer; plain click keeps the existing single-rail path and clears the set (D-04).
    - Paint: a Loop Rail whose loopId is a set member paints the same orange selection line as single selection; non-members are unchanged.
    - Contract test (invariant): the ordered identity list the strip passes to gestures is derived by deriveRailSetOrder from deriveKeyRailSegments + loopResolutionContext.ranges only — one ordering authority.
  </behavior>
  <action>
    RED first: create physicsPaintRotoRailSetSelection.test.ts with failing tests for the behaviors above, then implement GREEN.

    Create app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts — a dependency-free pure module (no Preact, no store imports; copy the Phase 37 module header contract in spirit: identity-only, session-local, "never persist, never cross the bridge"). Export:
    - `RailSetIdentity` = `{ readonly kind: 'loop'; readonly loopId: string } | { readonly kind: 'key-rail'; readonly firstKeyId: string }` (A1 — both id fields are existing authorities).
    - `RailSetSelectionState` = `{ readonly members: readonly RailSetIdentity[]; readonly anchor: RailSetIdentity | null }`, deeply frozen on return.
    - `deriveRailSetOrder(input: { keyRailSegments, loopRanges })` returning the unified ordered `RailSetIdentity[]` per the <behavior> sort rule.
    - `updatePhysicsPaintRotoRailSetSelection(selection, orderedIdentities, target, gesture)` with `gesture: 'plain' | 'toggle'` for this task (declare `'range' | 'union'` in the type now; implement in Task 2). 'plain' returns singleton + anchor = target. 'toggle' adds/removes; anchor unchanged (fallback to first ordered member if the anchor was toggled off); empty result returns null. Fail-closed on unknown/malformed identities.
    - `reconcileRailSetSelection(selection, orderedIdentities)`: returns null when selection is null, any member is absent from the ordered list, or identities are malformed/duplicated — clear the invalid set, never invent a fallback (UI-SPEC M1 error; Pitfall 2).

    In PhysicsPaintStudio.tsx add the session set signal beside the existing rail-selection signals (L127-139 pattern: `useSignal<RailSetSelectionState | null>(null)` — planner-named `railSetSelection`; Signals, no useState/useEffect mirrors per CLAUDE.md). Wire `handleSelectRotoLoopClip` (L1198-1230 area): when the gesture is Cmd/Ctrl (toggle), route through the set reducer using deriveRailSetOrder output, clear the single-rail signals (`selectedLoopClipIds`, `loopSelectionAnchorId`, `selectedRotoKeyRail`) while the set is non-null; when plain, clear the set and keep the existing single-rail path (D-04). In PhysicsPaintLoopClipRail.tsx pass set membership down so a member range paints its existing orange selection paint (same class/paint path as single selection — no new color, UI-SPEC M1). Launch replacement must clear the set alongside the existing selection reset (L152-164 block).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintRotoRailSetSelection.test.ts exists and exits 0 (plain/toggle/empty/fail-closed/ordering contract cases).
    - PhysicsPaintStudio.tsx contains a session-only set signal declared beside the existing L127-139 selection signals and cleared in the launch-replacement reset block.
    - PhysicsPaintLoopClipRail.tsx routes Cmd/Ctrl+click to the set reducer and paints members with the identical orange selection treatment as single selection (no new color literal introduced).
    - `pnpm run typecheck` exits 0.
    - Full physic-paint suite stays green: `pnpm exec vitest run src/components/physic-paint` exits 0.
  </acceptance_criteria>
  <done>A user can Cmd/Ctrl+click two Loop Rails and both paint orange as one set; Cmd+clicking both off leaves a clean no-selection state; reducer, ordering, and reconcile are one pure tested module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Range + union gestures, modifier routing in both rail components, anchor tick, and set lifecycle (collapse/clear/Escape/reconcile)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx, app/src/components/physic-paint/view/physicsPaintRailKeyboardNavigation.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts (Task 1 output — extend, do not fork)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx L150-190 (CURRENT modifier routing collapses Cmd+Shift to 'range' — Pitfall 1; union branch must be checked FIRST)
    - app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx L60-130 (RailMouseEvent currently exposes NO modifier keys — add metaKey/ctrlKey/shiftKey following the LoopClipRail RailMouseEvent shape)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx L215-260 (Escape/keyboard wiring), L1160-1180 (frame/key selection clears rail selection — extend to the set)
    - app/src/components/physic-paint/view/physicsPaintRailKeyboardNavigation.ts (FULL — the shared 43.4 roving module the set-contract test asserts against; it stays unchanged) and physicsPaintRailKeyboardNavigation.test.ts L11-67 (structural FakeRail/FakeScope harness to reuse)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md §M1 (anchor tick geometry: 2px wide x 8px high #F2F5F7 at 70% opacity, leading edge, vertically centered on the 3px line = top: 4px in the 12px target band; aria-hidden, pointer-events: none) + §Keyboard and focus (roving contract)
  </read_first>
  <behavior>
    - 'range': replaces the set with the ordered anchor-to-target slice; anchor unchanged (D-01/D-02). 'union': adds the ordered anchor-to-target slice to the current set; anchor unchanged; already-selected members stay selected (D-03).
    - Loop Rail routing: Cmd+Shift+click = 'union' checked BEFORE plain Shift (Pitfall 1); Shift = 'range'; Cmd/Ctrl = 'toggle'; none = 'plain'.
    - Key Rail gains the full modifier surface and identical routing; a Key Rail can join, anchor, and leave the set exactly like a Loop Rail (no per-type fork).
    - The anchor member paints the 2x8px #F2F5F7 70% tick at its leading edge (top: 4px in the 12px band); the tick never shows on non-anchor members and moves only when the anchor moves.
    - Plain click on any rail collapses the set to that rail (existing single-rail selection takes over); plain click on empty strip/frame clears the set; selecting a physical frame or key clears the set (43.4 mutual exclusion extended, D-04).
    - On every accepted physical revision the set reconciles against freshly derived segments + loop ranges; invalid sets clear (Pitfall 2). The Interpolation Off/On toggle and any external edit trigger the same reconcile.
    - Escape layering: the keyboard Escape chain gains a set-collapse layer consuming exactly one Escape (43.5 one-Escape-one-layer); the solo-exit layer lands before it in Plan 06 — this task keeps the chain order: popover dismiss, push disarm, set collapse, key-selection collapse (D-04).
    - Roving focus contract (UI-SPEC §Keyboard and focus; Visual Acceptance Matrix row 8): the existing shared 43.4 machinery (physicsPaintRailKeyboardNavigation.ts) is type-agnostic and orders every rail target by `data-rail-first-frame`, so with an active set the roving Tab/arrow cycle already visits set members in canonical first-frame order and falls through to ordinary rail cycling outside the set — no set-aware focus code is added (CONTEXT: one shared rail behavior, no per-type forks; planner investigation confirmed both rail components carry the attribute and dispatch through the shared handler). Space on a focused member invokes the same plain-select handler as plain click in both components, so the D-04 collapse wiring covers the Space path with no component change.
  </behavior>
  <action>
    RED first: extend physicsPaintRotoRailSetSelection.test.ts with range/union/anchor-preservation/fail-closed cases, then GREEN in the reducer: implement 'range' (ordered slice anchor..target, anchor unchanged) and 'union' (slice added to current members, de-duplicated, anchor unchanged), both fail-closed on unknown identities and on a null/unknown anchor (range/union with no valid anchor leave state unchanged — matching the Phase 37 range precedent).

    PhysicsPaintLoopClipRail.tsx: replace the L159-163 routing with union-first ordering (shiftKey && (metaKey || ctrlKey) → 'union'; shiftKey → 'range'; metaKey || ctrlKey → 'toggle'; else 'plain') and pass the gesture to the Studio selection handler for both the single-rail and set paths (the handler decides scope per Task 1 wiring; extend it to accept 'range'/'union').

    PhysicsPaintKeyRail.tsx: extend its RailMouseEvent interface (L82-86) with `metaKey`, `ctrlKey`, `shiftKey` following the LoopClipRail RailMouseEvent shape (L68-76), populate them from the real mouse event, and route through the same union-first mapping; wire `onSelectKeyRail` so modifier gestures feed the set reducer and plain clicks keep the existing single-rail path.

    Anchor tick: additive CSS in physicsPaintStudio.css (a class such as `physics-paint-rail-anchor-tick`: 2px wide, 8px high, #F2F5F7 at 70% opacity, positioned at the member's leading edge, top: 4px; `aria-hidden="true"`, `pointer-events: none`) rendered by both rail components only for the anchor member. No geometry change to the 3px rail line (UI-SPEC M1; layer 7 beside the selection line).

    Studio lifecycle: plain-click collapse (D-04), empty-strip/frame click clears the set, physical frame/key selection clears the set (extend the existing clear sites at L1169-1170, L1760-1796 area), and a reconcile pass on accepted revisions using reconcileRailSetSelection against fresh deriveRailSetOrder output — invalid sets become null (Pitfall 2). Add the set-collapse step into the keyboard Escape chain in the collapseRotoSelection path (PhysicsPaintStudio L1698-1708 area / physicsPaintStudioKeyboard.ts actions) so one Escape collapses the set without touching key selection.

    Roving focus contract tests (assertion-only — the existing machinery satisfies the contract by construction; do NOT extend dispatchRailTargetKeyDown with set-aware skipping, which would be the per-set fork CONTEXT forbids and matrix row 8 flags as failure): extend physicsPaintRailKeyboardNavigation.test.ts (structural FakeRail/FakeScope harness, L11-67) with a set-contract case — a lane of five mixed rails at frames 2/4/6/8/10 with a declared member subset at frames 2, 6, 10: ArrowRight/Tab dispatched from the frame-2 member walk the ring 2 → 4 (non-member) → 6 → 8 (non-member) → 10 and continue past the last member to the ordinary scroller exit with no wrap, proving members are visited in canonical order and cycling falls through outside the set. Add the cross-authority ordering-agreement case to physicsPaintRotoRailSetSelection.test.ts: for one mixed-timeline fixture, the first-frame order produced by getRailsInCanonicalOrder over structural fakes equals deriveRailSetOrder's canonical order over the same segments + loop ranges — gestures, copy, and focus share ONE ordering authority.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/view/physicsPaintRailKeyboardNavigation.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - Reducer tests cover: range replace from anchor, union add with anchor unchanged, union idempotent on already-selected members, range/union fail-closed on unknown identity, toggle-to-empty returns null.
    - PhysicsPaintLoopClipRail.tsx checks the union modifier combination BEFORE the plain Shift branch (source assertion: the routing expression orders shiftKey&&(metaKey||ctrlKey) first).
    - PhysicsPaintKeyRail.tsx RailMouseEvent exposes metaKey/ctrlKey/shiftKey and routes identically.
    - physicsPaintStudio.css contains the anchor-tick class with exactly 2px width, 8px height, #F2F5F7 color, 0.7 opacity, pointer-events: none.
    - Selecting a physical frame or key while a set is active clears the set (covered by a Studio-level test or an existing reconcile-path test extension).
    - physicsPaintRailKeyboardNavigation.test.ts contains the passing set-contract case: roving Tab/arrows from a set member visit members in canonical first-frame order and fall through to non-member rails and the scroller exit with no wrap (UI-SPEC matrix row 8 satisfied by the existing shared machinery; the navigation module source is unchanged).
    - physicsPaintRotoRailSetSelection.test.ts asserts getRailsInCanonicalOrder and deriveRailSetOrder produce the same ordering for the same mixed fixture (one ordering authority across gestures, copy, and focus).
    - Full physic-paint suite + typecheck green.
  </acceptance_criteria>
  <done>All four gestures work across both rail kinds in one shared ordering; the anchor tick marks the anchor; collapse/clear/Escape/reconcile lifecycle matches D-04/D-05 with zero stale-set survivors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One deterministic set-copy mapper (D-27), capsule + tooltip integration, and post-acceptance set-aftermath scaffolding (D-06 carrier)</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts L641-691 (buildRotoDeleteScopeLabel / mapRotoDeleteProductReason — the mapper family idiom this extends; D-27)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx L1690-1730 (getRotoStatusCapsuleViewModel composition — capsule injection seam)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (getRotoStatusCapsuleViewModel priority grammar)
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts L147-201 (resolvePostAcceptanceRotoSelection / resolvePostAcceptanceRotoStudioSelection — the operationKind-keyed aftermath template; Pitfall 6: the set must NOT fall through to default collapse)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md §M6 copy table + §M1 tooltip set sentences (locked copy, verbatim)
  </read_first>
  <behavior>
    - One exported mapper family owns ALL set copy: homogeneous '{N} Key Rails selected - frames {A}-{B}.' / mixed '{N} Rails selected - frames {A}-{B} ({t1}, {t2}).' / set of one '1 Rail selected - frames {A}-{B}.' / empty set produces no copy; type terms Motion/Static/Key with correct singular/plural (UI-SPEC M6, D-27).
    - {A}-{B} is the inclusive product frame range derived from canonical half-open intervals only at presentation time (A = first member first frame; B = last member effective end minus 1).
    - Frame numbers/ranges/counts render in tabular-nums (existing class discipline).
    - The status capsule shows the set copy while a set is active; the empty set shows nothing (capsule returns to its resting state).
    - Rail tooltips append the M1 set sentences (anchor form prefixed ' Range anchor.'); unavailable actions substitute their exact guarded reason (43.4 carried).
    - Aftermath scaffolding (D-06 carrier): a session-only snapshot side-channel keyed by command operationId (module-scope Map in the rail-set module — never persisted, pruned on launch replacement) plus `resolveRailSetPostAcceptance(input)` keyed on operationKind with EXPLICIT branches; unknown kinds (including 'undo'/'redo' without a recorded snapshot) leave the set to reconcile, never collapse by default (Pitfall 6).
  </behavior>
  <action>
    RED first: add mapper tests (useRotoTimelineActions.test.ts) for every UI-SPEC M6 row (homogeneous, mixed with breakdown, set of one, singular/plural, frame-range derivation from half-open intervals) and aftermath tests (physicsPaintRotoRailSetSelection.test.ts) for the snapshot record/resolve API, then GREEN.

    useRotoTimelineActions.ts: add the set-copy mapper (planner-named `buildRailSetCopy`) beside the L641-691 family — one function taking the ordered member descriptors ({kind, firstFrame, effectiveEndExclusive} per member, with loop members carrying mode 'static'|'motion') returning the locked strings. No per-component string assembly anywhere (D-27).

    physicsPaintWorkflowPresentation.ts + PhysicsPaintWorkflowStrip.tsx: feed the set copy into the capsule composition at the L1694 getRotoStatusCapsuleViewModel call as a persistent projection while a set is active (a new input term; the empty set contributes nothing). Rail tooltips: extend the existing Selected tooltip builders in both rail components' presentation modules with the M1 sentences via the mapper.

    physicsPaintRotoRailSetSelection.ts: add the session-only snapshot side-channel — `recordRailSetSnapshot(operationId, beforeSet, afterSet)` and `resolveRailSetPostAcceptance({ operationKind, operationId, current })`: 'move-rails' keeps the current set (identities are move-stable by construction); 'delete-rails' returns null; 'undo'/'redo' with a recorded snapshot restore the before/after set exactly (D-06); every other kind returns the current set unchanged so reconcile-on-revision (Task 2) remains the stale authority (Pitfall 6 — no default collapse). The Map is pruned on launch replacement (expose a `clearRailSetSnapshots()` the Studio launch-reset block calls).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - Mapper tests assert the exact locked strings: '2 Key Rails selected - frames 12-40.' (homogeneous), '3 Rails selected - frames 12-88 (1 Motion, 2 Key).' (mixed), '1 Rail selected - frames 12-40.' (set of one), and the en-dash + period forms verbatim per UI-SPEC M6.
    - Capsule shows the set copy only while a set is active; no set copy exists for the empty set (presentation test).
    - resolveRailSetPostAcceptance returns the unchanged set for unlisted operation kinds (Pitfall 6 regression test present).
    - record/resolve snapshot round-trip test: recorded before/after sets restore exactly on 'undo'/'redo'.
    - Full physic-paint suite + typecheck green.
  </acceptance_criteria>
  <done>All set copy flows through one mapper with locked wording; the capsule and tooltips show it; the D-06 aftermath carrier exists and is proven by unit round-trip so Plans 03/04 only wire calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail UI gesture -> session reducer | Untrusted pointer/keyboard input (modifier combos, stale targets) enters the set reducer |
| session set -> document/history | The set is session-only; it must never cross into the persisted document, revision fingerprint, or bridge payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-01 | Tampering | rail-set gesture input | medium | mitigate | Reducer is fail-closed: unknown/malformed identities, non-unique orderings, and invalid anchors return the unchanged state or null (isBoundedKeyId idiom; Task 1/2 tests) |
| T-43.6-02 | Tampering/Repudiation | stale set under an accepted revision | high | mitigate | reconcileRailSetSelection clears the invalid set on every accepted revision (Task 2); no fallback scope is invented (43.4 stale-selection rule) |
| T-43.6-03 | Information Disclosure (integrity) | session set leaking into document/history | medium | mitigate | Pure module with no store/persistence imports; snapshot side-channel is a module-scope Map pruned on launch replacement; existing strict-parse/revision-fingerprint suites must stay green (Task 3 verify) |
| T-43.6-SC | Tampering | package installs | low | mitigate | No package installs in this plan; legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/physic-paint` green after every task; `pnpm exec vitest run` + `pnpm run typecheck` green before plan completion.
- Wave 0 obligation (VALIDATION.md): physicsPaintRotoRailSetSelection.test.ts is created by Task 1 RED — covers RSET-01/02 rows.
- UI Considerations lifted (UI-SPEC ## UI Considerations, resolved-explicit rows): M1 empty/populated/zero-one-many/error/overflow, M1 tooltip long-text, M6 empty — all represented in must_haves.truths above.
- UI-SPEC Visual Acceptance Matrix row 8 (focus/shortcut parity): locked by the Task 2 roving set-contract test against the unchanged shared 43.4 focus machinery.
</verification>

<success_criteria>
- A user can build, shrink, and clear a mixed rail set with Shift/Cmd/Cmd+Shift gestures across all three rail kinds, see orange paint on every member plus the anchor tick, and read the locked set copy in the capsule.
- The set survives nothing it should not: stale membership clears it, frame/key selection clears it, launch replacement clears it.
- Session-only boundary proven: strict parsing, revision fingerprint, and persistence suites remain green with zero set state crossing them.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-01-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 01)

New symbols (for plan-review-convergence source grounding):
- `RailSetIdentity` (type) — physicsPaintRotoRailSetSelection.ts
- `RailSetSelectionState` (type) — physicsPaintRotoRailSetSelection.ts
- `RailSetGesture` (type: 'plain' | 'toggle' | 'range' | 'union') — physicsPaintRotoRailSetSelection.ts
- `deriveRailSetOrder` (function) — physicsPaintRotoRailSetSelection.ts
- `updatePhysicsPaintRotoRailSetSelection` (function) — physicsPaintRotoRailSetSelection.ts
- `reconcileRailSetSelection` (function) — physicsPaintRotoRailSetSelection.ts
- `recordRailSetSnapshot` / `resolveRailSetPostAcceptance` / `clearRailSetSnapshots` (functions) — physicsPaintRotoRailSetSelection.ts
- `buildRailSetCopy` (function) — useRotoTimelineActions.ts
- `railSetSelection` (Studio session signal) — PhysicsPaintStudio.tsx
- CSS class `physics-paint-rail-anchor-tick` — physicsPaintStudio.css
- New files: app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts
</output>
