---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 10
subsystem: ui
tags: [preact, signals, physics-paint, roto, rail-selection, gap-closure, tdd]

requires:
  - phase: 43.6-multi-rail-selection-and-batch-operations
    provides: 43.6-07/08/09 rail-set reducer wiring, seed bridge, synchronous modifier commit
provides:
  - Cross-type seed derivation in both Studio rail handlers (WR-01 closed)
  - Gesture-aware Loop Clip double-click gate (WR-02 closed)
  - Regression tests pinning both gap paths
affects: [43.6 verification re-run, RSET-01, RSET-02, BDEL-01, native UAT re-runs G-43.6-1/3/4/8]

actuals:
  tokens: 2825
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns: [cross-type single-signal seed derivation with key-rail-first ordering, plain-gesture-gated double-click intent]

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx

key-decisions:
  - "Both Studio handlers use the identical two-signal seed derivation (key-rail signal checked first): a plain click on either rail type nulls the other type's signal, so exactly one signal can be live and the order is deterministic"
  - "The Loop Clip double-click gate is gated on gesture === 'plain' rather than reordered: modifier clicks inside the pending/220ms window fall through to the existing 43.6-08 synchronous-commit branch"

patterns-established:
  - "Cross-type seed: seed identity derived from whichever single-rail signal is live, key-rail first"
  - "Gesture-aware timing gates: double-click/dead-zone logic applies to plain intent only; modifier clicks are never open-editor intent"

requirements-completed: [RSET-01, RSET-02, BDEL-01]

coverage:
  - id: D1
    description: "Cross-type seed: plain-selecting one rail type then modifier-clicking the other type seeds the set from the plain-selected rail, anchored on it, for every rail-type pair (WR-01)"
    requirement: RSET-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#seeds the set from the live cross-type single-rail signal with key-rail-first ordering (43.6-10 WR-01)"
        status: pass
    human_judgment: true
    rationale: "Source-contract test pins the derivation; native UAT re-runs for G-43.6-1/3/4 are scheduled after this closure per 43.6-VERIFICATION.md"
  - id: D2
    description: "A modifier click on a Loop Clip rail inside the 250ms pending / 220ms fast-double-click window commits set membership synchronously and never opens the Edit Group editor (WR-02)"
    requirement: RSET-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx#commits an in-window Cmd+click synchronously and never opens the editor (43.6-10 WR-02)"
        status: pass
    human_judgment: true
    rationale: "Component test pins the gesture routing; native UAT re-run for G-43.6-8 is scheduled after this closure per 43.6-VERIFICATION.md"

duration: 8min
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 10: Gap Closure WR-01/WR-02 Summary

**Cross-type rail-set seeding and gesture-aware double-click gating close the last two 43.6 verification gaps: plain-selected rails are carried into sets across rail types, and in-window modifier clicks commit membership synchronously instead of opening the editor.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-19T10:20:00Z
- **Completed:** 2026-08-19T10:28:43Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- WR-01 closed: both `handleSelectRotoKeyRail` and `handleSelectRotoLoopClip` derive the set seed from whichever single-rail signal is live (key-rail first), so a plain-selected rail is carried into the set for every cross-type pair
- WR-02 closed: the Loop Clip double-click gate fires only for plain gestures; modifier clicks inside the pending/220ms window commit set membership synchronously via the 43.6-08 branch and never open the Edit Group editor
- Fail-closed reducer semantics in `physicsPaintRotoRailSetSelection.ts` byte-untouched (verified via empty git diff)
- Full physic-paint suite green: 1894 tests / 85 files; five-suite regression sweep 304/304; `tsc --noEmit` clean

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — failing regression tests for both gap paths** - `22542c80` (test)
2. **Task 2: GREEN — cross-type seed derivation in both Studio handlers (WR-01)** - `9392f442` (fix)
3. **Task 3: GREEN — gesture-aware double-click gate on Loop Clip rails (WR-02)** - `6fa3e28d` (fix)

## Files Created/Modified
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - Widened seed derivation in both rail handlers to read the cross-type signal (key-rail first); useCallback deps gained the cross-type signal in each handler
- `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx` - Double-click gate gained a `gesture === 'plain'` conjunct; adjacent comment updated to scope the dead-zone close to plain intent
- `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` - New source-contract test asserting both cross-type derivations and key-rail-first ordering
- `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx` - New component test: plain click then in-window Cmd+click commits 'toggle' synchronously, `onOpenLoopEdit` never called

## Decisions Made
- Identical two-signal derivation in both handlers: a plain click on either type nulls the other type's signal (via `clearRotoLoopSelection` / direct null), so exactly one signal can be live; key-rail-first order is therefore deterministic and matches the verification report's mandated order
- Gate conditioned with a leading `gesture === 'plain'` conjunct rather than reordering branches: the gesture const is already computed above the gate, and the existing 43.6-08 synchronous-commit branch below stays byte-identical

## TDD Gate Compliance

- RED gate: `22542c80` (`test(43.6-10)`) — both new tests failed for the stated reasons (missing cross-type guard; in-window Cmd+click routed to 'plain'/editor), all 116 pre-existing tests in the two files passed
- GREEN gates: `9392f442` and `6fa3e28d` (`fix(43.6-10)` per the plan's mandated commit messages, serving the GREEN role)
- No REFACTOR commit needed — edits were minimal and surgical

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- WR-01 and WR-02 code-green: RSET-01, RSET-02, BDEL-01 are satisfiable at re-verification
- Per 43.6-VERIFICATION.md sequencing: native UAT re-runs for G-43.6-1, G-43.6-3, G-43.6-4, and G-43.6-8 are scheduled AFTER this closure and remain pending — they are not marked resolved at code-green time
- Phase 43.6 has no remaining plans; verification re-run is the next step

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*

## Self-Check: PASSED

All 4 modified files and SUMMARY.md exist on disk; commits `22542c80`, `9392f442`, `6fa3e28d` verified in git history.
