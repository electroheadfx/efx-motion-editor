---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 11
subsystem: ui
tags: [preact, signals, physics-paint, roto, rail-selection, solo, gap-closure, tdd]

requires:
  - phase: 43.6-multi-rail-selection-and-batch-operations
    provides: 43.6-06 solo armed gate + getSoloWindow single-rail fallback, 43.6-08 synchronous modifier commit, 43.6-09 armed-gate isolation
provides:
  - Plain Loop Rail click success path now disarms an armed Solo before the new single-rail selection is written (REVIEW-WR-01 closed)
  - Studio-level regression test pinning the disarm-before-write ordering on the exact branch
affects: [43.6 verification re-run, SOLO-03 satisfiability, native UAT re-runs G-43.6-1/3/4/8]

actuals:
  tokens: 482
  tasks: 1
  commits: 2

tech-stack:
  added: []
  patterns: [direct disarmSolo before a rail-selection write on branches that cannot route through clearRotoLoopSelection]

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts

key-decisions:
  - "The plain-click success branch of handleSelectRotoLoopClip calls disarmSolo() directly (immediately after the railSetSelection collapse) instead of routing through clearRotoLoopSelection(), because that helper would clobber the loop selection this branch is about to write (the VERIFICATION's explicit prohibition)"
  - "The disarm is positioned BEFORE the selection write (selectedLoopClipIds.value = next.selectedLoopClipIds), so the armed getSoloWindow gate (L1153) returns null for the new selection and playback/capsule can never silently retarget"

patterns-established:
  - "Exit-path parity: every rail-selection change/clear disarms an armed Solo; a plain Loop Rail click is a rail-selection change (D-14), closed on the last missed path"

requirements-completed: [SOLO-03]

coverage:
  - id: D1
    description: "A plain Loop Rail click that changes the single-rail selection disarms an armed Solo before the new selection is applied — playback and the armed capsule cannot silently retarget to the newly clicked rail (REVIEW-WR-01)"
    requirement: SOLO-03
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#disarms an armed Solo when a plain Loop Rail click changes the selection (43.6-11)"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/view/physicsPaintSoloArm.test.ts#disarmSolo behavioral semantics (isSoloArmed flips false only when armed)"
        status: pass
    human_judgment: true
    rationale: "Source-contract test pins the disarm-before-write wiring on the exact branch; disarmSolo's behavioral semantics (isSoloArmed() flips false only when armed) are covered by physicsPaintSoloArm.test.ts. The live end-to-end scenario (arm Solo on Loop A, plain-click Loop B, assert disarmed) is covered by the pending native UAT re-runs G-43.6-1/-8, which 43.6-VERIFICATION.md scheduled after this closure."

# Metrics
duration: 3min
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 11: Plain Loop Rail Click Solo Disarm (REVIEW-WR-01) Summary

**Plain Loop Rail click now disarms an armed Solo before the single-rail selection moves, closing the last rail-selection change path that skipped the D-14 exit discipline (REVIEW-WR-01, 43.6-VERIFICATION.md score 8/9 → closed)**

## Performance

- **Duration:** 3 min
- **Started:** 2026-08-19T11:54:00Z
- **Completed:** 2026-08-19T11:57:16Z
- **Tasks:** 1 (tracer, TDD RED→GREEN)
- **Files modified:** 2

## Accomplishments

- Added a direct `disarmSolo();` call (with a 43.6-06 D-14 comment) in the plain-click success branch of `handleSelectRotoLoopClip`, immediately after `railSetSelection.value = null;` and before `selectedLoopClipIds.value = next.selectedLoopClipIds;` — the armed `getSoloWindow` gate (L1153) now returns null for the new selection, so playback and the armed capsule cannot silently retarget to the newly clicked Loop rail.
- Used the direct call rather than `clearRotoLoopSelection()` per the VERIFICATION's explicit prohibition — that helper would clobber the loop selection this branch is about to write.
- Added the Studio source-contract regression test 'disarms an armed Solo when a plain Loop Rail click changes the selection (43.6-11)' (RED first), slicing the `handleSelectRotoLoopClip` handler and asserting the plain-click branch contains `disarmSolo();` positioned before the selection write.
- Verified the full six-suite regression sweep (312 tests) and `tsc --noEmit` stay green; the Studio suite grew from 75 to 76 tests with zero modifications to pre-existing tests.

## Task Commits

Each task was committed atomically (TDD split into two commits):

1. **Task 1 (RED): plain Loop Rail click disarms an armed Solo** - `5a4411dd` (test)
2. **Task 1 (GREEN): plain Loop Rail click disarms an armed Solo** - `8930b01e` (fix)

**Plan metadata:** final docs commit (docs: complete plan)

## Files Created/Modified

- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - one comment + one statement added to the plain-click success branch of `handleSelectRotoLoopClip` (after `railSetSelection.value = null;`): `disarmSolo();` with the 43.6-06 D-14 rationale and the clearRotoLoopSelection prohibition. No other handler, the reducer call, the `next === null` sub-branch, the script-selection tail, or the useCallback dependency array touched.
- `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` - one new `it` block 'disarms an armed Solo when a plain Loop Rail click changes the selection (43.6-11)' in the 'Physics Paint multi-rail selection SET wiring (43.6-01)' describe, immediately after the 43.6-09 armed-gate test.

## Decisions Made

- **Direct disarm, not the shared clear:** the plain-click success branch calls `disarmSolo()` directly because `clearRotoLoopSelection()` would clobber the selection being written (the VERIFICATION missing directive's explicit prohibition). The key-rail plain path keeps its `clearRotoLoopSelection()`-based disarm (L1351) unchanged because that branch writes its selection after the clear.
- **Disarm before the write:** `disarmSolo();` sits before `selectedLoopClipIds.value = next.selectedLoopClipIds;`, so the armed gate at L1153 returns null for the new selection — the arm ends before the single-rail selection moves.

## Deviations from Plan

None - plan executed exactly as written. No auto-fixes were needed: the RED test failed for exactly the expected reason (no `disarmSolo();` in the branch), the GREEN fix passed on the first run, and the six-suite sweep plus typecheck were green on the first run.

## Issues Encountered

None - the one-line change plus regression test landed exactly as the plan prescribed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- REVIEW-WR-01 is closed at code-green time: the only rail-selection change path that skipped the D-14 disarm now disarms before the new selection applies, so SOLO-03 moves from PARTIAL to satisfiable at re-verification.
- Per 43.6-VERIFICATION.md's UAT cross-reference, native UAT re-runs for G-43.6-1, G-43.6-3, G-43.6-4, and G-43.6-8 are unblocked by this closure but remain human-run — do not mark those UAT gaps resolved at code-green time.
- Explicitly NOT produced (per plan): no changes to physicsPaintSoloArm.ts, physicsPaintRotoSoloWindow.ts, physicsPaintRotoRailSetSelection.ts, useRotoPhysicalEditHistory.ts, PhysicsPaintLoopClipRail.tsx, PhysicsPaintWorkflowStrip.tsx, or any plan 01-10 artifact.

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*

## Self-Check: PASSED

- SUMMARY file exists: `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-11-SUMMARY.md`
- RED commit `5a4411dd` exists
- GREEN commit `8930b01e` exists
