---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 04
type: execute
wave: 3
depends_on: [43.6-01]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.test.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts
autonomous: true
requirements: [BDEL-01, BDEL-02, RSET-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An active rail set routes Delete, Backspace, and the visible Delete control through ONE classifier to a 'rail-set' scope; the accessible name and guarded tooltip heading carry 'Delete {N} Rails' ('Delete 1 Rail' for a one-member set); the visible compact label stays 'Delete' (D-21, D-22, UI-SPEC M4)."
    - "Delete Rails is fully direct - no confirmation modal at any set size or composition; the multi-selection itself is the explicit scope (D-23; 43.4 D-13 precedent)."
    - "Each selected rail deletes with its own type semantics: Key Rail deletion removes its real keys and derived projection; Motion/Static Rail deletion follows 43.2 Delete Group semantics and NEVER deletes the referenced Action (D-23)."
    - "The whole set deletes as ONE atomic history command; one Undo restores every rail, key, break, and the exact pre-delete selection set; Redo re-applies (D-06, D-23)."
    - "Vacated intervals remain persistent intentional gaps; break normalization follows the 43.4 rules per deleted rail; unselected rails, keys, Actions, and shared sources still referenced by another Group are never touched (D-23)."
    - "The parent recomputes the complete delete-rails semantic delta from its own document authority and exact-matches the child's proposed records/loopClips/breaks/interpolation/selection/cursor/revision; any divergence rejects with zero mutation (43.2 parent-authority pattern)."
    - "After acceptance the selection is empty and the cursor stays on its current valid frame (D-06, 43.4 delete precedent)."
    - "Accepted copy: 'Deleted {N} Rails - frames {A}-{B}. The intervals stay intentional gaps.' (singular: 'Deleted 1 Rail - frames {A}-{B}. The interval stays an intentional gap.') through the one mapper (UI-SPEC M4)."
    - "A stale, ambiguous, rejected, or no-op deletion produces no mutation, no success status, and no history entry (UI-SPEC M4 error)."
  artifacts:
    - "physicsPaintRotoGroupLifecycle.ts — proposePhysicPaintRotoDeleteRails pure proposer composing per-Group 43.2 semantics + Key Rail record removal + break normalization"
    - "app/src/types/physicPaint.ts — 'delete-rails' operation kind + semantic-delta kind + strict parsers"
    - "useRotoPhysicalEditCoordinator.ts — RotoRailSetDeleteExecuteInput + authorization switch case + semanticDeltaEquals branch"
    - "physicPaintBridge.ts — 'delete-rails' parent recompute + exact-match validation branch"
    - "useRotoTimelineActions.ts — classifier 'rail-set' branch + scope label + accepted copy + execution dispatch"
  key_links:
    - "Child proposal <-> parent recomputation via ONE shared pure proposer: the exact-match link whose breakage lets a forged/divergent delete commit (T-43.6-01)"
    - "classifyRotoDeleteTarget 'rail-set' branch -> deleteRotoFrame dispatch -> executeRailSetDelete port: the single activation path shared by keyboard and visible control (D-22)"
    - "Accepted delete-rails -> resolveRailSetPostAcceptance + snapshot side-channel: exact pre-delete set restore on Undo (RSET-03; breakage = UAT step 3 fails)"
---

<objective>
Deliver Delete Rails: one atomic, direct (no-modal), mixed-set deletion with per-type semantics, parent-side recompute-and-exact-match validation, and complete Undo/Redo restoration of the document AND the pre-delete selection set.

Implements BDEL-01 (one dynamic scope classifier + keyboard parity), BDEL-02 (atomic mixed-set delete with per-type semantics), and the Delete half of RSET-03 (empty-after-delete; exact set restore on Undo) per D-21, D-22, D-23, D-06.

Purpose: this is the phase's hardest correctness surface (Research Open Question 1 — resolved here as ONE pure proposer + ONE new semantic-delta kind composed from the existing per-Group proposer, keeping fail-closed parity and the exact-match contract). The 43.2 Group lifecycle authority is reused, never forked.
Output: Delete {N} Rails works from keyboard and the visible control as one history command with parent-validated atomicity.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-RESEARCH.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
@app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts
@app/src/lib/physicPaintBridge.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): proposePhysicPaintRotoDeleteRails — one pure proposer composing per-Group delete semantics + Key Rail record removal + break normalization</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts, app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts L600-700 (proposePhysicPaintRotoDeleteGroupFrame/DeleteGroup: cleanupKeyIds, buildLifecycleProposal, rejectLifecycle idiom) and the proposer family around L625+
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (delete-key-rail branch: record removal + complete break-collection normalization per 43.4 — reuse its exported helpers where they exist; the 43.4 rules are: breaks owned by removed keys drop, no dangling or deleted-key-referencing breaks)
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts (RailSetIdentity — the member descriptor input)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md §Pattern 3 (parent recompute + exact-match contract the proposer must satisfy)
  </read_first>
  <behavior>
    - Input: the accepted physical document + the ordered member list (loopIds + Key Rail firstKeyId/keyIds); output: one complete proposal (realKeyRecords, groupOverrideRecords, loopClips, incomingInterpolationBreakKeyIds, interpolation, selectedKeyId, cursorAppFrame, revision) + a 'delete-rails' impact, or a fail-closed rejection reason — never a partial proposal.
    - Group members apply the EXACT 43.2 Delete Group semantics (compose the existing per-Group proposal path so visibleRanges/provenance/sync/Action-retention behavior cannot fork): sequential composition over the accumulating document state in deterministic order (placementStart asc, then loopId asc); cleanupKeyIds only collects keys unreferenced by surviving Groups.
    - Key Rail members remove their real-key records and normalize the complete break collection per the 43.4 delete rules.
    - A member unknown to the current document (stale loopId, stale segment) rejects the WHOLE proposal fail-closed (all-or-nothing, D-23/D-25 discipline).
    - Characterization parity: composed delete-rails on a fixture equals the sequential outcome of the existing single delete-group and delete-key-rail authorities — no semantic drift.
    - The referenced Action library and script-library authority are never touched (43.2 Delete Group semantics carried).
  </behavior>
  <action>
    RED first: extend physicsPaintRotoGroupLifecycle.test.ts with failing cases: mixed-set composition (Key Rail + Motion + Static in one call), parity fixtures (composed result equals sequential single deletes), stale-member fail-closed rejection, break normalization (breaks owned by removed keys drop; survivors' breaks unchanged; no dangling references), cleanupKeyIds excluding keys still referenced by surviving Groups, selectedKeyId/cursor validity in the proposal. Then GREEN.

    Implement `proposePhysicPaintRotoDeleteRails(input: { document, members })` in physicsPaintRotoGroupLifecycle.ts beside the existing proposers. Compose: for each Group member in deterministic order, apply the existing delete-group semantics to the accumulating state (extract a private shared core from proposePhysicPaintRotoDeleteGroup if the composition requires it — behavior of the existing exported proposer must stay byte-identical, proven by its existing tests staying green); then remove each Key Rail member's records and normalize the break collection per the resolver's delete-key-rail rules (reuse exported pure helpers from the resolver where available; otherwise implement the same rules with a parity test proving equality on fixtures). Rebuild ONE final lifecycle proposal via the existing buildLifecycleProposal path so revision/records/loopClips derivation cannot diverge. Fail closed with a reason string on any stale member (no partial proposal). Return the impact as one 'delete-rails' delta carrying the ordered member descriptors, cleanupKeyIds, previousRevision, nextRevision.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Parity tests pass: composed delete-rails equals sequential single-authority deletes on at least three fixtures (Key Rail only; Groups only; mixed set with a shared-source survivor that must keep its source keys).
    - Stale member (unknown loopId or segment mismatch) rejects the whole proposal with no partial output (test).
    - Break collection after a mixed delete contains no id referencing a removed key (test).
    - All existing proposePhysicPaintRotoDeleteGroup/DeleteGroupFrame/Regenerate tests stay green unmodified (43.2 regression boundary).
    - Typecheck green.
  </acceptance_criteria>
  <done>One pure proposer produces the complete atomic mixed-delete target state with 43.2/43.4 semantics provably preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transport union + coordinator execute input + bridge parent recompute/exact-match for delete-rails</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts L720-760 (operation-kind union) + L1015-1060 (strict parsers) + the semantic-delta union (search PhysicPaintRotoPhysicalEditSemanticDelta; the delete-group delta shape carries groupId/cleanupKeyIds/previousRevision/nextRevision)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts L290-379 (RotoGroupLifecycleDeleteExecuteInput + createAuthorizedPhysicalEditPayload) + L381-440 (semanticDeltaEquals — add the delete-rails branch)
    - app/src/lib/physicPaintBridge.ts L1210-1303 (GROUP_LIFECYCLE_OPERATION_KINDS gate, delta dispatch, exact-match validation block)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md §Pattern 3 (the union/coordinator/bridge trio grows together)
  </read_first>
  <behavior>
    - 'delete-rails' joins the transport operation-kind union, the semantic-delta union, and every strict parser with identical literal strings (43.3 D-14).
    - The coordinator gains a specialized execute input RotoRailSetDeleteExecuteInput { operationKind: 'delete-rails', expectedLaunch { operationId, layerId }, members } — no child intent (Group-lifecycle discipline: the parent recomputes; the child proposes complete state).
    - createAuthorizedPhysicalEditPayload pairs 'delete-rails' with intent === undefined (specialized branch), and semanticDeltaEquals gains the delete-rails branch (members + cleanupKeyIds + revisions compared field-by-field).
    - The bridge adds 'delete-rails' to the lifecycle gate and a recompute branch calling proposePhysicPaintRotoDeleteRails against the parent's own document authority, then exact-matches impact + records + groupOverrideRecords + loopClips + breaks + interpolation + selectedKeyId + cursorAppFrame + revision with distinct error strings (the L1283-1302 pattern).
    - Missing/mismatched/stale/changed-content evidence fails closed with zero accepted-state settlement (43.2 carryover).
  </behavior>
  <action>
    RED first: extend physicPaintBridge.test.ts with delete-rails cases (accept on exact match; reject on divergent records/loopClips/breaks/selection/cursor/revision with the new distinct error strings; reject on missing members) and coordinator/transport parse round-trip cases. Then GREEN.

    types/physicPaint.ts: add 'delete-rails' to the operation-kind union and parsers; add the semantic-delta variant `{ kind: 'delete-rails'; members: readonly RailSetDeleteMember[]; cleanupKeyIds: readonly string[]; previousRevision: string; nextRevision: string }` with a strict parser validating member descriptors (bounded ids, discriminant, non-empty).

    useRotoPhysicalEditCoordinator.ts: add RotoRailSetDeleteExecuteInput to the execute-input union, the createAuthorizedPhysicalEditPayload specialized case, and the semanticDeltaEquals delete-rails branch.

    physicPaintBridge.ts: add 'delete-rails' to GROUP_LIFECYCLE_OPERATION_KINDS, the delta dispatch branch recomputing via proposePhysicPaintRotoDeleteRails({ document: targetDocument, members: delta.members }), and reuse the exact-match validation block (impact serialize compare + complete-state field comparisons) with delete-rails-specific error strings.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint src/lib</automated>
  </verify>
  <acceptance_criteria>
    - Bridge tests prove: exact-match acceptance, and fail-closed rejection on each divergent field (records, loopClips, breaks, interpolation, selection, cursor, revision) with distinct error strings.
    - The transport strict parser round-trips a valid delete-rails payload and rejects malformed member descriptors (test).
    - semanticDeltaEquals returns false on any member/cleanup/revision difference and true on exact equality (test).
    - Existing delete-group/delete-group-frame bridge tests stay green unmodified (43.2 regression boundary).
    - Typecheck green.
  </acceptance_criteria>
  <done>The delete-rails command transports, authorizes, and parent-validates exactly like the accepted 43.2 Group lifecycle operations — one atomic command, zero divergence tolerance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classifier rail-set branch + one-path Delete dispatch + locked copy + D-06 empty/restore aftermath</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts L458-639 (RotoDeleteTarget union + classifyRotoDeleteTarget — the set branch goes FIRST, before the L514-516 multi-loop 'no-target' rejection it supersedes), L641-691 (mapper family), L1409-1474 (deleteRotoFrame dispatch — add the rail-set branch)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx L860-960 (group delete execution wiring — executeGroupLifecycleDelete port construction) and L300-360 (selection clearing on delete acceptance)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md §M4 (locked scope/accepted copy) + §M4 Interaction Contract (routing)
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts (Plan 01 snapshot/aftermath API)
  </read_first>
  <behavior>
    - classifyRotoDeleteTarget gains a 'rail-set' branch evaluated BEFORE the loop/key-rail branches: an active non-empty set yields `{ kind: 'rail-set', members }` with members validated against current derivation (segments + loopClips); a stale set yields the existing stale/no-target mapping (fail-closed — never delete on stale authority).
    - Delete, Backspace, and the visible Delete control share this one classifier (D-22); the scope label reads 'Delete {N} Rails' / 'Delete 1 Rail' and lives only in the accessible name and guarded tooltip heading (visible label stays 'Delete').
    - Dispatch: deleteRotoFrame's 'rail-set' branch calls a new input port `executeRailSetDelete` (parent-authority execute, like executeGroupLifecycleDelete) — no runPhysicalAction, no confirmation modal (D-23).
    - Acceptance: selection empties (set -> null and single-rail signals cleared), cursor stays on its current valid frame, canvas reconciles from accepted state only; the accepted copy publishes via the mapper: 'Deleted {N} Rails - frames {A}-{B}. The intervals stay intentional gaps.' (singular variant for one).
    - RSET-03/D-06: the pre-delete set is recorded via recordRailSetSnapshot keyed by the accepted operationId; Undo restores the exact pre-delete set; Redo clears it again; the anchor follows rail identity.
    - Rejection publishes the mapped reason and mutates nothing; no success status or history entry on stale/rejected/no-op paths.
  </behavior>
  <action>
    RED first: extend useRotoTimelineActions.test.ts (classifier rail-set branch: mixed set classification, stale set fail-closed, scope label strings, dispatch routing to the port, accepted copy verbatim) and physicsPaintRotoRailSetSelection.test.ts (aftermath: 'delete-rails' empties the set; undo restores the recorded set; redo clears). Then GREEN.

    useRotoTimelineActions.ts: add the 'rail-set' variant to RotoDeleteTarget; add the classifier branch FIRST (before the selectedLoopClipIds block at L514) reading the set from the classification input (extend RotoDeleteTargetClassificationInput with the rail-set members — Plan 01 authority); extend buildRotoDeleteScopeLabel with the set forms; mapRotoDeleteProductReason returns null for a valid rail-set target; add the deleteRotoFrame 'rail-set' branch dispatching to the new `executeRailSetDelete` input port with the classified members; publish the locked accepted copy on success through the one mapper.

    PhysicsPaintStudio.tsx: construct the executeRailSetDelete port through the coordinator (RotoRailSetDeleteExecuteInput with expectedLaunch + classified members), record the pre-delete set snapshot on acceptance, route 'delete-rails' acceptance through resolveRailSetPostAcceptance (set empties; undo/redo restore/clear via the side-channel), and clear single-rail selection signals on acceptance. Wiring only — no logic in the Studio body.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - Classifier tests: an active mixed set classifies as 'rail-set' before any single-rail branch; a stale set never classifies as deletable.
    - Copy tests assert verbatim: 'Delete 3 Rails', 'Delete 1 Rail', 'Deleted 3 Rails - frames 12-88. The intervals stay intentional gaps.', 'Deleted 1 Rail - frames 12-40. The interval stays an intentional gap.'
    - Dispatch tests: keyboard Delete and the visible control converge on executeRailSetDelete for a set; no modal path exists for any set size (source assertion: the rail-set branch contains no confirmation/warning call).
    - Aftermath tests: accepted 'delete-rails' empties the set; undo restores the recorded set exactly; redo clears it.
    - Full physic-paint suite + typecheck green; existing Delete Frame / single-rail / group delete tests unchanged and green (43.2/43.4 regression boundary).
  </acceptance_criteria>
  <done>Delete Rails is one atomic, direct, parent-validated command with locked copy, one shared activation path, and exact selection restoration across Undo/Redo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child delete proposal -> parent bridge | The child's proposed complete state is untrusted until the parent recomputes and exact-matches |
| session set -> destructive command | A stale or forged set must never authorize deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-01 | Tampering | forged/malformed delete-rails payload | high | mitigate | Closed union + strict parsers (Task 2); specialized input carries no child intent; fail-closed classifier (Task 3) |
| T-43.6-02 | Tampering/Repudiation | stale-authority delete commit | high | mitigate | Parent recomputes via proposePhysicPaintRotoDeleteRails from its own document authority and exact-matches every complete-state field (Task 2); stale sets classify fail-closed (Task 3) |
| T-43.6-06 | Tampering | Action deletion via a Motion/Static Rail delete | high | mitigate | The proposer composes 43.2 Delete Group semantics verbatim — loopClips removal only, script-library authority untouched (Task 1 parity tests) |
| T-43.6-SC | Tampering | package installs | low | mitigate | No package installs in this plan; legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/physic-paint src/lib` green after every task; full suite + typecheck before plan completion.
- VALIDATION.md rows 43.6-BDEL-01 (classifier, T-43.6-01) and 43.6-BDEL-02 (bridge exact-match, T-43.6-02) covered by Task 3 and Task 2 respectively.
- UI Considerations lifted: M4 populated/error/long-text/overflow — all in must_haves.truths.
</verification>

<success_criteria>
- A mixed rail set deletes as one atomic command with per-type semantics, direct at any size, parent-validated, restoring everything including the exact selection set on Undo — with zero change to single-rail, frame, or Group deletion behavior.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-04-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 04)

New symbols (for plan-review-convergence source grounding):
- `proposePhysicPaintRotoDeleteRails` (function) — roto/physicsPaintRotoGroupLifecycle.ts
- `RailSetDeleteMember` (type) — app/src/types/physicPaint.ts
- Operation kind literal `'delete-rails'` + semantic-delta variant — app/src/types/physicPaint.ts
- `RotoRailSetDeleteExecuteInput` (type) — hooks/useRotoPhysicalEditCoordinator.ts
- `executeRailSetDelete` (input port) — hooks/useRotoTimelineActions.ts
- `RotoDeleteTarget` variant `{ kind: 'rail-set' }` — hooks/useRotoTimelineActions.ts
</output>
