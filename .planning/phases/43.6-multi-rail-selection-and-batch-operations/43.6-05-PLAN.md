---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 05
type: execute
wave: 4
depends_on: [43.6-01]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: true
requirements: [BSPC-01, BSPC-02, BSPC-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With an active rail set, the ToolCase Key Spacing section applies the spacing factor per selected rail, each rail keeping its OWN first key at its current frame as anchor (D-24); no cross-set spacing mode exists this phase."
    - "The whole set validates before commit: every selected rail's membership, anchor, capacity, and collisions are checked up front; if ANY rail fails, the entire command rejects with a mapped reason - no partial application (D-25)."
    - "Key Spacing on a set is ONE atomic history command with one Undo/Redo (D-24); the set stays selected after acceptance (force-spacing selection-preservation precedent carried)."
    - "The ToolCase popover gains exactly ONE scope line under the Key Spacing heading carrying the D-27 set copy verbatim when a set is active; with no set the popover is byte-identical to 43.5; the popover never creates or modifies the selection set (D-26)."
    - "Whole-set preflight rejection copy: 'Can''t apply Key Spacing to the selected Rails: {reason}.' with {reason} from the existing force-spacing reason family; accepted copy: 'Key Spacing applied to {N} Rails.' (singular: 'Key Spacing applied to 1 Rail.') (UI-SPEC M5)."
    - "An unselected Group sharing a source cycle with a selected Motion/Static Rail rejects the whole command with a mapped reason - the family is never silently affected (D-10 principle carried to spacing)."
    - "Without a set, Key Spacing behavior is exactly the 43.5 single-selection behavior - every existing spacing test stays green unmodified (scope boundary)."
  artifacts:
    - "physicsPaintRotoPhysicalResolver.ts — 'spacing-on-set' intent branch with per-rail fixed anchors and hard-wall collisions"
    - "app/src/types/physicPaint.ts — 'spacing-on-set' transport union + intent payload + strict parser"
    - "useRotoTimelineActions.ts — deriveForceSpacingScope rail-set branch + dispatch + locked copy"
    - "PhysicsPaintWorkflowStrip.tsx — popover scope line (one line, set-active only)"
  key_links:
    - "deriveForceSpacingScope set branch -> spacing-on-set intent -> resolver all-or-nothing validation: the preflight/commit agreement link (breakage = Pitfall 5 partial application)"
    - "Popover scope line -> Plan 01 buildRailSetCopy: one copy authority (breakage = popover copy disagrees with capsule)"
---

<objective>
Deliver Key Spacing on a rail set: a new resolver intent that respaces every selected rail with per-rail fixed first-key anchors and hard-wall collisions (all-or-nothing), the scope-aware ToolCase popover section with one scope line, and the locked copy — while the no-set path stays byte-identical to 43.5.

Implements BSPC-01 (per-rail anchors, one atomic command), BSPC-02 (whole-set preflight), BSPC-03 (scope-aware popover) per D-24, D-25, D-26.

Purpose: spacing semantics for an explicit set differ from both existing variants (the scoped variant anchors ONE earliest key; the linked variant anchors with cumulative ripple — resolver L2709-2748). D-24 locks per-rail fixed anchors, so a dedicated intent keeps the two existing behaviors untouched (scope boundary).
Output: Apply in the ToolCase Key Spacing section respaces the whole selected set as one undoable command.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-RESEARCH.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): spacing-on-set resolver intent — per-rail fixed anchors, hard-wall collisions, all-or-nothing</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts L2796-2870 (buildForceSpacingCandidate: anchor + i*(N+1) mapping; scoped variant hard-wall rule "unselected keys keep their frames as hard walls - any computed selected destination equal to an unselected frame rejects atomically"), L2709-2748 (buildLinkedForceSpacingCandidate — its cumulative-growth anchoring is NOT the D-24 semantic; do not reuse), L3331-3406 (validateLinkedSourceSpacingScopes — requires real loop cycles, so Key Rails cannot ride linked scopes; this is why a new intent exists), L3976-4015 (force-spacing dispatch branch + linked-source guards)
    - app/src/types/physicPaint.ts L720-760 + L1015-1060 (union + strict parser idiom)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts L330-379 (authorization switch)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md D-24/D-25 (per-rail anchors; all-or-nothing)
  </read_first>
  <behavior>
    - Intent `{ kind: 'spacing-on-set'; members; emptyFrames }`: members use the Plan 02 discriminated shape (key-rail firstKeyId + keyIds / loop loopId); emptyFrames is a finite non-negative integer (N = 0 adjacent allowed, per the existing candidate builder).
    - Each rail anchors at its OWN first key's CURRENT frame: rail member key i maps to anchor + i * (emptyFrames + 1). For a Loop member the scope is its source cycle keys anchored at the first source key's frame (placementStart follows automatically for attached Groups since placementStart === first source key frame; duplicated placements keep placement and resolve the same source keys).
    - Hard walls: any computed destination equal to an unselected key frame, colliding with another selected rail's computed destinations, crossing the left-to-right order of unselected keys, or exceeding capacity rejects the WHOLE intent fail-closed — zero partial proposal (D-25).
    - Straddle guard: a selected Loop member whose sourceCycleId is referenced by an UNSELECTED Group rejects with the new failure code 'rails-spacing-source-straddle' (D-10 principle; the family is never silently affected).
    - Break ownership travels with moved key identity (43.4 D-19); nextIncomingInterpolationBreakKeyIds carries the complete next collection when ownership changes.
    - Membership validation is exact-match against freshly derived segments + loop ranges (move-rails precedent); stale members reject.
    - An already-exact request yields a valid no-change proposal (computeChanged precedent), never a failure.
  </behavior>
  <action>
    RED first: resolver tests — two Key Rails respace with each first key fixed (e.g. rails at 0,3,6 and 20,23 with N=1 → 0,2,4 and 20,22); Loop member source cycle respaced with attached placement unchanged; collision into an unselected key rejects the whole intent; collision between two selected rails rejects; over-capacity rejects via the common finalizer; straddle guard rejects shared-source with unselected Group; stale member rejects; no-change when already exact; break travel with moved keys. Then GREEN.

    Resolver: add 'spacing-on-set' to PhysicPaintRotoPhysicalEditOperationKind + the intent union + the failure-code union ('rails-spacing-source-straddle'); implement the branch composing ONE candidate mapping across all rails (all rails' destinations computed into one map, then the existing duplicate-frame/order/finalizer checks apply once — all-or-nothing by construction, Pitfall 5). Break collection: nextIncomingInterpolationBreakKeyIds per the move-rails travel rules. status carries operationKind 'spacing-on-set' + changed + affectedKeyIds; semanticDelta null; product copy lands in Task 2 via the mapper.

    types/physicPaint.ts: add 'spacing-on-set' to the transport union, intent union, and strict parsers (literal parity with the resolver). useRotoPhysicalEditCoordinator.ts: add the createAuthorizedPhysicalEditPayload case pairing the exact intent.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove per-rail fixed anchors: each rail's first key frame is unchanged after spacing while interior keys respace (the two-Key-Rail case asserts both anchors verbatim).
    - Tests prove all-or-nothing: every rejection case yields the failure branch with zero proposal (D-25).
    - 'rails-spacing-source-straddle' asserted for the shared-source case; duplicated placements never straddle.
    - Existing force-spacing tests (scoped, linked, full-timeline) stay green unmodified — the 43.5/no-set behavior boundary.
    - types/physicPaint.ts strict parser round-trips a valid spacing-on-set intent and rejects malformed members/emptyFrames.
    - Typecheck green.
  </acceptance_criteria>
  <done>The spacing-on-set intent resolves one complete all-or-nothing proposal with D-24 per-rail anchors and zero drift into the existing spacing behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveForceSpacingScope rail-set branch + dispatch + popover scope line + locked copy</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts L1063-1246 (ForceSpacingScopeSnapshot + deriveForceSpacingScope — the set branch goes FIRST; its fail-closed message idiom) and the applyForceSpacing dispatch (search applyForceSpacing — it builds the force-spacing intent from the derived scope)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx L636-810 (popover structure; forceSpacing ports L546-555 interface block; the relocated Key Spacing section markup) and L1710-1720 (handleForceSpacingSubmit)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-UI-SPEC.md §M5 (scope line geometry/copy: D-27 set copy verbatim under the Key Spacing heading, Body role #9CA3AF; controls byte-identical otherwise) + §M5 Interaction Contract
  </read_first>
  <behavior>
    - deriveForceSpacingScope gains a rail-set branch evaluated FIRST: an active non-empty set yields per-rail member descriptors; a stale set returns ok:false with the mapped stale message; the branch never invents fallback scope.
    - applyForceSpacing dispatches the spacing-on-set intent when the derived scope is a rail set; the existing force-spacing paths are untouched for non-set selections (43.5 byte-parity).
    - Rejection copy: 'Can't apply Key Spacing to the selected Rails: {reason}.' with {reason} from the existing force-spacing reason family (stale selection, capacity, collision, straddle); accepted copy: 'Key Spacing applied to {N} Rails.' (singular 'Key Spacing applied to 1 Rail.').
    - The popover renders the D-27 set copy as ONE scope line directly under the Key Spacing heading when a set is active (Body role, #9CA3AF, tabular-nums); with no set the line is absent and the popover is byte-identical to 43.5 (no placeholder, no reserved space).
    - The popover never creates or modifies the selection set; opening/closing it changes nothing about the set (D-26).
    - After acceptance the set stays selected (force-spacing selection-preservation precedent; resolveRailSetPostAcceptance 'spacing-on-set' keeps the set — identities survive respacing).
  </behavior>
  <action>
    RED first: extend useRotoTimelineActions.test.ts — set-scope derivation (mixed set → per-rail members), stale set ok:false, dispatch chooses spacing-on-set with the derived members and the popover's emptyFrames value, locked copy strings verbatim (preflight rejection + accepted), and the Plan 01 aftermath module gains its 'spacing-on-set' keep-set case (physicsPaintRotoRailSetSelection.test.ts). Then GREEN.

    useRotoTimelineActions.ts: extend ForceSpacingScopeSnapshot with a rail-set variant carrying the ordered member descriptors; add the rail-set branch FIRST in deriveForceSpacingScope (before selectedLoopClipIds) reading the Plan 01 set from the input (extend its input type); on ok, applyForceSpacing builds the spacing-on-set intent and dispatches through the same runPhysicalAction discipline as force-spacing (proposal versioning, status publication); extend the mapper with the locked M5 strings.

    PhysicsPaintWorkflowStrip.tsx: render the scope line inside the Key Spacing section of the ToolCase popover only when a set is active — the D-27 copy from the Plan 01 mapper, one Body-role line under the heading, additive markup with no layout change to the relocated controls (D-26; UI-SPEC M5 geometry).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert the exact strings: 'Can't apply Key Spacing to the selected Rails: {reason}.' and 'Key Spacing applied to 3 Rails.' / 'Key Spacing applied to 1 Rail.'
    - Dispatch test: a rail-set scope produces a spacing-on-set intent with per-rail members; a non-set scope produces the unchanged force-spacing intent (43.5 parity).
    - Popover test (or strip-level source assertion + presentation test): the scope line renders only with an active set and carries the identical string the capsule shows (one mapper authority).
    - Aftermath test: 'spacing-on-set' keeps the set selected.
    - Full physic-paint suite + typecheck green; existing popover/spacing tests unchanged and green.
  </acceptance_criteria>
  <done>Key Spacing applies to the whole set as one atomic, all-or-nothing command with per-rail anchors, one scope line in the popover, and locked copy — with zero change to the no-set path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popover input -> spacing-on-set intent | User-supplied emptyFrames and the session set cross into a document mutation |
| child intent -> parent transport | The spacing-on-set intent crosses the bridge as a typed payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-01 | Tampering | forged/malformed spacing-on-set intent | high | mitigate | Closed union + strict parser (Task 1); fail-closed member/emptyFrames validation |
| T-43.6-07 | Tampering | partial application of a set-wide spacing command | high | mitigate | All rails' destinations compose into ONE candidate before the single finalizer; any failure rejects the whole intent with zero proposal (D-25, Task 1 tests) |
| T-43.6-02 | Tampering/Repudiation | stale set spacing | medium | mitigate | deriveForceSpacingScope set branch fails closed on stale membership; resolver revalidates membership exactly against fresh derivation |
| T-43.6-SC | Tampering | package installs | low | mitigate | No package installs in this plan; legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/physic-paint` green after every task; full suite + typecheck before plan completion.
- VALIDATION.md row 43.6-BSPC-01 (BSPC-01/02/03) covered by the resolver + timeline-actions test extensions.
- UI Considerations lifted: M5 populated/empty/long-text/overflow — all in must_haves.truths.
</verification>

<success_criteria>
- Key Spacing on a set is one atomic command with per-rail fixed anchors and all-or-nothing validation; the popover announces the scope in one line; the 43.5 no-set behavior is byte-preserved.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-05-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 05)

New symbols (for plan-review-convergence source grounding):
- Operation kind literal `'spacing-on-set'` + intent payload type — physicsPaintRotoPhysicalResolver.ts + app/src/types/physicPaint.ts
- Failure code literal `'rails-spacing-source-straddle'` — physicsPaintRotoPhysicalResolver.ts
- ForceSpacingScopeSnapshot rail-set variant — hooks/useRotoTimelineActions.ts
- Popover scope-line element (set-active only) — view/PhysicsPaintWorkflowStrip.tsx
</output>
