---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 12
subsystem: physics-paint-roto-history
tags: [delete-rails, undo-redo, replay-target, before-snapshot, selection-authority, gap-closure]

requires:
  - phase: 43.6-multi-rail-selection-and-batch-operations
    provides: plan 04 shared delete-rails proposer; plan 07 batch-kind history allowlist
provides:
  - G-43.6-2 PRIMARY defect closed — one Delete Rails on a multi-rail set records the TRUE pre-op selection; one Undo restores every rail/key/break AND the exact pre-delete selection set through the REAL bridge
  - Single-rail delete-key-rail and multi-rail delete-rails now share the same before-selection authority (currentDocument)
  - Real-bridge forward+undo+redo regression test with distinct pre/post selections ('ordinary' -> 'source-B')
  - History-seam round-trip pin with a non-null pre-delete selection 'F'
affects: [43.6-13 (secondary G-43.6-2 defect), Phase 44 integrated UAT]

actuals:
  tokens: 3448
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "beforeSelectionFromPayload guard: delete-rails excluded from payload-derived before-snapshot selection; all other lifecycle kinds keep payload authority"

key-files:
  created: []
  modified:
    - app/src/lib/physicPaintBridge.ts
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts

key-decisions:
  - "delete-rails before snapshot records currentDocument (pre-op) selectedKeyId/cursorAppFrame instead of the POST-delete proposal selection the forward payload ships — the undo replay submits history entry.before, so payload-derived recording fails replay-target equality and silently no-ops undo (G-43.6-2)"
  - "Replay-target equality check itself left strict and untouched (T-43.6-12-02 accepted) — the fix corrects the recorded before snapshot at the source"
  - "The launch frame drives the live parent selection (createPhysicPaintLaunchContext selects the key at the requested frame), so the real-bridge regression launches on the selected key's frame to install the pre-delete selection"

patterns-established:
  - "Launch-frame selection seeding: openPhysicPaintCanvas({ frame }) sets the store selection to the key at that frame — tests needing a non-null live selection must launch on a key frame"

requirements-completed: [BDEL-02, RSET-03]

coverage:
  - id: D1
    description: "Delete Rails undo/redo restore the exact pre-delete selection set through the real bridge (forward payload 'source-B' vs pre-delete 'ordinary')"
    requirement: BDEL-02
    verification:
      - kind: integration
        ref: "app/src/lib/physicPaintBridge.test.ts#delete-rails undo/redo restore the exact pre-delete selection set through the real bridge (G-43.6-2)"
        status: pass
    human_judgment: false
  - id: D2
    description: "History seam: one accepted delete-rails command captures, submits, and replays the exact pre-delete selection set (non-null 'F')"
    requirement: RSET-03
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts#records one accepted delete-rails command restoring the exact pre-delete selection set (G-43.6-2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Native UAT re-runs for G-43.6-3/-8 remain human-run per plan sequencing note"
    verification: []
    human_judgment: true
    rationale: "Plan explicitly reserves native UAT re-runs for the human; code-green does not resolve those UAT items."

duration: 9min
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 12: Delete Rails undo/redo before-snapshot selection authority (G-43.6-2) Summary

**Closed the G-43.6-2 PRIMARY defect: the bridge's delete-rails before snapshot now records the true pre-operation selection from the current document instead of the POST-delete proposal selection, so one Undo restores every rail/key/break AND the exact pre-delete selection set through the real applyPhysicPaintPayload.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-19T13:25:57Z
- **Completed:** 2026-08-19T13:35:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Real-bridge regression test proving the defect RED (replay-target rejection) then GREEN after the fix: forward+undo+redo for delete-rails with distinct pre/post selections ('ordinary' -> 'source-B')
- One scoped production change: `beforeSelectionFromPayload` guard excludes 'delete-rails' from payload-derived before-snapshot selection; every other lifecycle kind keeps payload authority byte-identical
- History-seam pin: delete-rails round-trip with non-null pre-delete selection 'F' proves the coordinator's captureSnapshot authority carries the pre-op selection into entry.before and the undo replay submits it
- Full gates green: bridge + history + coordinator suites 203 passed / 1 skipped; `tsc --noEmit` clean

## Task Commits

Each task was committed atomically:

1. **Task 1 (tracer, RED): real-bridge delete-rails forward + undo + redo regression test** - `55c3fc25` (test)
2. **Task 2 (GREEN): record the true pre-operation selection for delete-rails before snapshots** - `1ebd8b02` (fix)
3. **Task 3: pin the history seam — delete-rails round-trip with non-null pre-delete selection** - `f2b857f3` (test)

**Plan metadata:** committed separately (docs: complete plan)

## TDD Gate Compliance

- RED gate: `55c3fc25` — test committed failing with the exact documented rejection ("Roto physical replay target snapshot does not match the original accepted command.")
- GREEN gate: `1ebd8b02` — fix committed after RED, regression test passes
- No refactor commit needed (two ternary lines + guard inside one branch)

## Files Created/Modified
- `app/src/lib/physicPaintBridge.ts` — beforeSnapshot block: `beforeSelectionFromPayload` guard + two ternary selection lines + rationale comment (nothing else touched)
- `app/src/lib/physicPaintBridge.test.ts` — one new it block in the Phase 43.2 parent-authoritative describe: real-bridge delete-rails forward+undo+redo with distinct pre/post selections
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts` — one new it block after the existing null-selection delete-rails round-trip: same mixed set with beforeDocument.selectedKeyId 'F'

## Decisions Made
- delete-rails before-snapshot selection authority switches to currentDocument (pre-op); the strict replay-target equality check is preserved untouched (T-43.6-12-02 accepted — changing the check would mask genuine document drift for ALL replay kinds)
- The regression test launches on frame 20 (the selected key's frame) because createPhysicPaintLaunchContext installs the key at the requested frame as the live parent selection — this is the only way the real launch path produces the non-null pre-delete selection under test

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Launch frame 5 -> 20 in the Task 1 test setup**
- **Found during:** Task 2 (GREEN verification — undo still rejected after the production fix)
- **Issue:** The plan's `openPhysicPaintCanvas({ layer, frame: 5 })` (mirroring lifecycleHarness) nulls the live selection: createPhysicPaintLaunchContext sets the store selection to the key at the requested frame (none at frame 5) with cursor 5. The before snapshot then legitimately recorded null/5 and the test could never reproduce the distinct-selection defect — the plan's seed selection 'ordinary' never reached the store at apply time.
- **Fix:** Launch on frame 20 so the real launch path installs 'ordinary' as the live parent selection; re-read seededDoc after the launch; assert selectedKeyId 'ordinary'/cursorAppFrame 20.
- **Files modified:** app/src/lib/physicPaintBridge.test.ts
- **Verification:** Test fails RED pre-fix with the exact replay-target rejection, passes GREEN post-fix
- **Committed in:** `1ebd8b02` (carried with the Task 2 fix commit)

**2. [Rule 3 - Blocking] Empty frameOverrides on the seeded group-1 clip**
- **Found during:** Task 1 (test authoring)
- **Issue:** The plan asked for the group-1 clip "exactly as in lifecycleHarness" (frameOverrides referencing 'override-5') AND `groupOverrideRecords []` — but parsePhysicPaintRotoPhysicalDocument rejects an override reference without a matching override record ("override reference mismatch").
- **Fix:** Kept the plan's explicit `groupOverrideRecords []` (stated twice) and set the clip's frameOverrides to []; the deleted member is the key rail [ordinary], so the group and its lifecycle fields survive unchanged either way.
- **Files modified:** app/src/lib/physicPaintBridge.test.ts
- **Verification:** Seed parses; forward/undo/redo all pass
- **Committed in:** `55c3fc25` (Task 1 commit, noted in message)

**3. [Rule 3 - Blocking] Undo payload selectedAppFrame null -> 20**
- **Found during:** Task 1 (test authoring)
- **Issue:** The plan's undo payload specified `selectedAppFrame null` with selectedKeyId 'ordinary'; payload validation requires selection identity and frame to be both null or both present, and the selected record's appFrame must match.
- **Fix:** Used the established pattern `selectedKeyId === null ? null : cursorAppFrame` (20).
- **Files modified:** app/src/lib/physicPaintBridge.test.ts
- **Verification:** Payload passes validation; undo/redo round-trip green
- **Committed in:** `55c3fc25` (Task 1 commit)

---

**Total deviations:** 3 auto-fixed (all Rule 3 — plan literals that could not execute as written)
**Impact on plan:** No scope creep; production diff is exactly the planned guard + two ternaries + comment. All deviations confined to test setup fidelity.

## Issues Encountered
- GREEN initially still rejected undo — diagnosed via temporary snapshot-diff instrumentation that revealed the launch-time selection reset (createPhysicPaintLaunchContext), leading to deviation 1. Instrumentation removed before commit.

## Known Stubs
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- G-43.6-2 PRIMARY defect closed at code level; plan 43.6-13 owns the SECONDARY transient delete-block defect
- Per plan sequencing note: native UAT re-runs for G-43.6-3/-8 remain human-run and are NOT marked resolved at code-green time

## Self-Check: PASSED

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*
