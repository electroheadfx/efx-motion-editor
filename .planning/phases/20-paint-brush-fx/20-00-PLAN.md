---
phase: 20-paint-brush-fx
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Application/src/types/paint.test.ts
  - Application/src/lib/brushFxDefaults.test.ts
  - Application/src/lib/brushFlowField.test.ts
  - Application/src/lib/brushWatercolor.test.ts
  - Application/src/lib/spectralMix.test.ts
  - Application/src/lib/paintRenderer.test.ts
  - Application/src/lib/paintPersistence.test.ts
autonomous: true
requirements:
  - PAINT-01
  - PAINT-02
  - PAINT-03
  - PAINT-04
  - PAINT-05
  - PAINT-06
  - PAINT-07
  - PAINT-08
  - PAINT-09
  - PAINT-10
  - PAINT-11
  - PAINT-13

must_haves:
  truths:
    - "All 7 test stub files exist under Application/src/"
    - "Each stub imports vitest describe/it/expect and declares test cases as .todo or with placeholder assertions"
    - "Running npx vitest run produces no failures (stubs are skipped or trivially pass)"
    - "Test file names match the RESEARCH.md Wave 0 gap list exactly"
  artifacts:
    - path: "Application/src/types/paint.test.ts"
      provides: "Test stubs for BrushStyle type union, BrushFxParams interface, default constants"
    - path: "Application/src/lib/brushFxDefaults.test.ts"
      provides: "Test stubs for per-style default param values, visible param mapping per style"
    - path: "Application/src/lib/brushFlowField.test.ts"
      provides: "Test stubs for flow field creation, sampling, point distortion"
    - path: "Application/src/lib/brushWatercolor.test.ts"
      provides: "Test stubs for polygon deformation, midpoint displacement, layer generation"
    - path: "Application/src/lib/spectralMix.test.ts"
      provides: "Test stubs for spectral math validation (placeholder until JS-side spectral functions exposed)"
    - path: "Application/src/lib/paintRenderer.test.ts"
      provides: "Test stubs for conditional routing: flat strokes use Canvas2D, styled strokes route to WebGL2"
    - path: "Application/src/lib/paintPersistence.test.ts"
      provides: "Test stubs for sidecar JSON round-trip with brushStyle/brushParams fields"
  key_links: []
---

<objective>
Create Wave 0 test stub files for the paint brush FX phase, establishing the Nyquist verification scaffold before any implementation begins.

Purpose: Every subsequent plan's `<verify>` commands depend on these test files existing. The stubs declare the expected test cases as `.todo` placeholders. Implementation plans will fill in the test bodies as they build the corresponding modules.
Output: 7 test stub files matching the RESEARCH.md Wave 0 gap list, all passing (or skipped) in vitest.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-paint-brush-fx/20-CONTEXT.md
@.planning/phases/20-paint-brush-fx/20-RESEARCH.md
@.planning/phases/20-paint-brush-fx/20-VALIDATION.md

<interfaces>
<!-- Existing test pattern from codebase -->
From Application/src/stores/projectStore.test.ts:
```typescript
import {describe, it, expect, beforeEach} from 'vitest';
import {projectStore} from './projectStore';
// Tests use describe/it/expect pattern, no special test utilities
```

<!-- Vitest config -->
From Application/vitest.config.ts:
```typescript
export default defineConfig({
  test: {
    include: ['src/**/*.test.ts'],
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create type system and defaults test stubs (paint.test.ts, brushFxDefaults.test.ts)</name>
  <files>Application/src/types/paint.test.ts, Application/src/lib/brushFxDefaults.test.ts</files>
  <action>
Create two test stub files. These will be filled in by Plan 01 executor when the types are implemented.

**Application/src/types/paint.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('BrushStyle type system', () => {
  it.todo('BrushStyle union includes all 6 styles: flat, watercolor, ink, charcoal, pencil, marker');
  it.todo('BRUSH_STYLES array contains all 6 styles in correct order');
  it.todo('BrushFxParams interface has 5 optional numeric fields: grain, bleed, scatter, fieldStrength, edgeDarken');
  it.todo('DEFAULT_BRUSH_FX_PARAMS has entry for each BrushStyle');
  it.todo('DEFAULT_BRUSH_FX_PARAMS flat has empty object (no FX params)');
  it.todo('DEFAULT_BRUSH_FX_PARAMS watercolor has bleed, grain, fieldStrength');
  it.todo('BRUSH_FX_VISIBLE_PARAMS flat has empty array');
  it.todo('BRUSH_FX_VISIBLE_PARAMS watercolor includes bleed, grain, fieldStrength');
  it.todo('BRUSH_FX_VISIBLE_PARAMS ink includes edgeDarken, fieldStrength');
  it.todo('PaintStroke interface accepts optional brushStyle and brushParams fields');
});
```

**Application/src/lib/brushFxDefaults.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('Brush FX defaults', () => {
  it.todo('each style in DEFAULT_BRUSH_FX_PARAMS has only its relevant params set');
  it.todo('all param values are in 0-1 range');
  it.todo('BRUSH_FX_VISIBLE_PARAMS keys match BRUSH_STYLES');
  it.todo('visible params for each style are a subset of BrushFxParams keys');
  it.todo('flat and marker have no visible params');
  it.todo('charcoal visible params include grain and scatter');
  it.todo('pencil visible params include grain');
});
```
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && test -f src/types/paint.test.ts && test -f src/lib/brushFxDefaults.test.ts && npx vitest run src/types/paint.test.ts src/lib/brushFxDefaults.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>paint.test.ts and brushFxDefaults.test.ts exist with .todo test stubs. vitest recognizes them without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create module test stubs (flowField, watercolor, spectralMix, paintRenderer, paintPersistence)</name>
  <files>Application/src/lib/brushFlowField.test.ts, Application/src/lib/brushWatercolor.test.ts, Application/src/lib/spectralMix.test.ts, Application/src/lib/paintRenderer.test.ts, Application/src/lib/paintPersistence.test.ts</files>
  <action>
Create 5 test stub files for the implementation modules.

**Application/src/lib/brushFlowField.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('brushFlowField', () => {
  describe('createFlowField', () => {
    it.todo('creates a FlowField with correct cols and rows for given dimensions');
    it.todo('grid contains angles in 0 to 2*PI range');
    it.todo('uses default cellSize of 20 when not specified');
  });

  describe('sampleField', () => {
    it.todo('returns angle at given pixel position');
    it.todo('clamps to grid boundaries for out-of-range positions');
  });

  describe('applyFlowField', () => {
    it.todo('displaces points along flow field direction');
    it.todo('zero strength returns original points unchanged');
    it.todo('higher strength produces larger displacement');
  });

  describe('getFlowField', () => {
    it.todo('caches field for same dimensions');
    it.todo('recreates field when dimensions change');
  });
});
```

**Application/src/lib/brushWatercolor.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('brushWatercolor', () => {
  describe('seedableGaussian', () => {
    it.todo('produces deterministic sequence for same seed');
    it.todo('produces different sequences for different seeds');
    it.todo('output follows approximately Gaussian distribution');
  });

  describe('deformPolygon', () => {
    it.todo('doubles the number of vertices (midpoint insertion)');
    it.todo('midpoints are displaced by variance-scaled random values');
    it.todo('original vertices are preserved in output');
  });

  describe('renderWatercolorLayers', () => {
    it.todo('returns layerCount polygon arrays');
    it.todo('default layerCount is 7');
    it.todo('each layer has more vertices than the input outline');
    it.todo('same seed produces identical layers (deterministic)');
  });

  describe('polygonToTriangles', () => {
    it.todo('produces 3 * polygon.length float values (fan triangulation)');
    it.todo('each triangle includes the center point');
  });

  describe('polygonCenter', () => {
    it.todo('returns centroid of polygon vertices');
  });
});
```

**Application/src/lib/spectralMix.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('spectralMix', () => {
  // Note: spectral mixing is GPU-only (GLSL). These tests validate any
  // JavaScript-side spectral math helpers if exposed. WebGL2 shader tests
  // remain manual/visual since jsdom has no WebGL2 context.

  it.todo('spectral.glsl GLSL string is exported and contains spectral_mix function');
  it.todo('buildSpectralCompositeSrc produces valid GLSL with version header');
  it.todo('SPECTRAL_GLSL contains spectral_linear_to_concentration function');
  it.todo('SPECTRAL_GLSL contains all 38-band reflectance data arrays');
});
```

**Application/src/lib/paintRenderer.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('paintRenderer conditional routing', () => {
  // Note: WebGL2 rendering tests are visual-only (jsdom has no WebGL2).
  // These tests validate the routing logic and type guards.

  it.todo('isStyledStroke returns false for PaintShape elements');
  it.todo('isStyledStroke returns false for PaintFill elements');
  it.todo('isStyledStroke returns false for eraser strokes');
  it.todo('isStyledStroke returns false for flat brush strokes');
  it.todo('isStyledStroke returns false for strokes without brushStyle');
  it.todo('isStyledStroke returns true for watercolor brush strokes');
  it.todo('isStyledStroke returns true for ink brush strokes');
  it.todo('renderPaintFrame processes elements in correct order');
});
```

**Application/src/lib/paintPersistence.test.ts:**
```typescript
import {describe, it, expect} from 'vitest';

describe('paintPersistence brush FX round-trip', () => {
  it.todo('PaintStroke with brushStyle serializes to JSON correctly');
  it.todo('PaintStroke with brushParams serializes all FX param values');
  it.todo('PaintStroke without brushStyle deserializes with undefined (backward compat)');
  it.todo('Old sidecar JSON without brushStyle/brushParams loads without error');
  it.todo('Round-trip: serialize then deserialize preserves brushStyle and brushParams');
});
```
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && test -f src/lib/brushFlowField.test.ts && test -f src/lib/brushWatercolor.test.ts && test -f src/lib/spectralMix.test.ts && test -f src/lib/paintRenderer.test.ts && test -f src/lib/paintPersistence.test.ts && npx vitest run src/lib/brushFlowField.test.ts src/lib/brushWatercolor.test.ts src/lib/spectralMix.test.ts src/lib/paintRenderer.test.ts src/lib/paintPersistence.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>All 5 module test stubs exist with .todo test cases. vitest recognizes them without errors. Full suite passes.</done>
</task>

</tasks>

<verification>
- `cd Application && npx vitest run --reporter=verbose 2>&1 | tail -5` -- full suite passes (stubs are .todo, skipped)
- All 7 test files exist at the paths specified in RESEARCH.md Wave 0 gap list
- No existing tests are broken by the new stub files
</verification>

<success_criteria>
- 7 test stub files exist matching RESEARCH.md Wave 0 gap list
- Each stub has meaningful .todo test case descriptions covering the requirement it maps to
- vitest run completes with no failures
- Nyquist compliance: every plan's verify can reference these test files
</success_criteria>

<output>
After completion, create `.planning/phases/20-paint-brush-fx/20-00-SUMMARY.md`
</output>
