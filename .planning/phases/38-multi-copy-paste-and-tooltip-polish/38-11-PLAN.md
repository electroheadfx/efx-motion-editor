---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 11
type: execute
wave: 2
depends_on: []
gap_closure: true
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
autonomous: true
requirements:
  - 38-NAV-RENDER-LOCALIZATION

must_haves:
  truths:
    - "Timeline navigation (the rAF-batched startFrame propagation, 38.1 D-04) no longer repaints the right color sidebar or the left vertical tool rail: both subtrees are wrapped in preact/compat memo and receive referentially stable props, so a startFrame-only Studio render shallow-compares equal and skips them entirely (38.1-06 deferred follow-up #1; 38.1 D-03/D-04 spirit — UI work is bounded to what actually changed)."
    - "Zero visual and zero behavior change: every prop value reaches the components exactly as before (primitives compare by value; signals keep their identity and their internal .value subscriptions — memo only skips parent-prop-driven re-renders, never signal-driven ones, so ScriptsPanel signal updates and ToolRail history-availability updates keep flowing); any behavior delta is a bug (38.1 D-09 discipline)."
    - "The stabilized props provably exclude frame-derived inputs: both memo resolve calls carry a single-line deps array with an enumerated-real-inputs comment (38.1 onion-projection idiom), and neither line references the Studio's frame cursor."
    - "Render-skip is proven, not vibed: an ephemeral node script asserts the identity-memo semantics (same deps -> identical reference; one changed dep -> new reference) AND the preact/compat memo wrapper marker on both exported components."
    - "The WR-02 contract is not regressed: the rightPanel playWiggle source reads through the existing rotoLegacyInterpolationSettings memo (b74ac80a) — no fresh per-render getRotoInterpolationSettings clone feeds the stabilized props."
    - "D-15 discipline: the 3 known-red tests stay untouched; zero test files created, modified, renamed, or executed; no vitest invocation. Native visual confirmation (side panels no longer repaint on navigation) ships inside the 38-06 re-run."
  artifacts:
    - "app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts — NEW exported pure createIdentityMemo() factory (single-entry last-winner identity cache: resolve(nextDeps, build) with Object.is per-element comparison — the component-scope twin of the 38.1-07 store structural memo)."
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — toolRailPropsMemo / rightPanelPropsMemo held in refs; toolRail and rightPanel props assembled inside memo resolve calls with single-line enumerated deps; unstable callbacks wrapped in useCallback first (behavior unchanged)."
    - "app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx — exported component wrapped in preact/compat memo (implementation renamed to a private function; export name and props type unchanged)."
    - "app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx — same memo wrap; createPhysicsPaintPaneResizeDrag named export untouched (its existing test keeps importing it)."
    - "/tmp/38-11-memo-proof.mjs — ephemeral esbuild-bundle proof script (NOT a repo artifact, NOT a test file; D-15-safe)."
  key_links:
    - "Stable props + memo = skipped render: PhysicsPaintStudioView spreads the memoized objects ({...toolRail} / {...rightPanel}); each individual prop value is referentially stable across startFrame-only renders, so preact/compat memo's default shallow compare returns equal and Preact skips the subtree — the props object identity itself is irrelevant to the mechanism."
    - "Signals bypass memo (correctness): PhysicsPaintScriptsPanel reads library.rows.value etc. in its own render and re-renders on signal writes regardless of the parent memo — freezing the sidebar is impossible as long as stabilization never caches a signal's .value read into the memo deps; deps carry signal OBJECTS and primitives only."
---

<objective>
Localize navigation-driven UI updates to the timeline/canvas surface (38.1-06 deferred follow-up #1, confirmed on native UAT 2026-07-28): on timeline navigation the Studio's rAF-batched startFrame update re-renders the whole Studio, and the user SEES the right color sidebar (PhysicsPaintRightPanel) and left vertical tool rail (PhysicsPaintToolRail) repaint even though neither depends on the frame cursor (verified: zero frame-cursor references in either file). Memoize both subtrees behind referentially stable props so a startFrame-only Studio render skips them entirely; audit the view-model assembly for per-render fresh objects (inline closures/literals) and stabilize the minimal set needed.

Purpose: 38.1 made navigation fast; this plan removes the last visible repaint noise on the same hot path — the final polish on the approved "work now! its fast!" baseline, using Preact-native patterns (memo from preact/compat — the project already imports createPortal from preact/compat — plus a pure identity memo; signals over hooks; no effect sprawl per project CLAUDE.md).

Output: createIdentityMemo factory + two memo-wrapped components + stabilized Studio assembly + ephemeral render-skip proof; typecheck green. Zero test changes (D-15); native confirmation ships inside the 38-06 re-run.

File-scope note (planner deviation, recorded): the orchestrator's file list named the three component files; usePhysicsPaintStudioViewModel.ts is added as a fourth file because the pure memo factory must live in an import-light module so the ephemeral node proof can bundle it without the engine/import.meta graph — the file is the logical home of the view-model assembly contract.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.1-studio-render-path-performance/38.1-06-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
</context>

<!-- planner-discipline-allow: currentFrame -->

<tasks>

<task type="tracer">
  <name>Tracer: createIdentityMemo + memo-wrapped ToolRail with stabilized props, proven end-to-end</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, /tmp/38-11-memo-proof.mjs</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (whole file — currently a pass-through)
    - app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx (whole file — props usage; confirm the historyAvailability signal is read internally via .value)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:144-150 (historyAvailability signal), :1038-1042 (toolRail assembly)
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx:145-160 (how toolRail is spread into the component)
  </read_first>
  <action>
    Build the identity-memo primitive and prove the render-skip mechanism on the smaller of the two subtrees.

    1. In usePhysicsPaintStudioViewModel.ts, export a pure createIdentityMemo() factory: returns a resolve(nextDeps: readonly unknown[], build: () => T): T closure holding a single-entry last-winner cache — if nextDeps has the same length and every element Object.is-equals the previous entry, return the cached value WITHOUT calling build; otherwise call build, store deps+value, return the new value. No hooks, no module-level state (per-instance via factory — the Studio holds instances in refs; multiple Studios can never cross-pollinate). Keep the existing buildPhysicsPaintStudioViewModel / usePhysicsPaintStudioViewModel exports byte-identical.

    2. In PhysicsPaintToolRail.tsx, rename the component implementation to a private function (e.g. PhysicsPaintToolRailImpl) and export const PhysicsPaintToolRail = memo(PhysicsPaintToolRailImpl) with memo imported from preact/compat (project idiom: createPortal is already imported from preact/compat in ColorPickerModal/SequenceList). Props type export and component behavior unchanged. Record in the SUMMARY the read_first confirmation that the rail reads historyAvailability.value internally (signal subscription bypasses props — memo cannot freeze undo/redo availability).

    3. In PhysicsPaintStudio.tsx: hold const toolRailPropsMemo = useRef(createIdentityMemo()).current. Move the toolRail props object assembly inside toolRailPropsMemo.resolve(deps, build) where build constructs the exact current object shape (activeTool, physicsMode, activePhysicsAction, historyAvailability, disabled, onSelectTool, onUndo, onRedo, onClearFrame, onPhysicsStart, onPhysicsStop, onDryPaint) and deps is a SINGLE-LINE array enumerating exactly the values build references (38.1 onion-projection idiom: a comment above states the dependency array enumerates exactly its real inputs). Audit each callback first: any callback that is a plain per-render closure must be wrapped in useCallback with its own enumerated deps BEFORE entering the assembly (do not change what any callback does). Primitives (disabled = !engine || mutationLocked) compare by value — safe.

    4. Write /tmp/38-11-memo-proof.mjs (Write tool; outside the repo — NOT a test artifact, D-15-safe). Resolve esbuild via createRequire from the app package.json -> require.resolve('vite/package.json') -> require.resolve('esbuild', { paths: [vite dir] }). Bundle a tiny entry importing createIdentityMemo from the view-model module and PhysicsPaintToolRail from its file (format esm, jsx automatic jsxImportSource preact, loader .svg dataurl). Assert, exiting 1 on failure:
       (a) resolve(['a', 1], buildX) twice with the same array CONTENTS (fresh array, same element identities) returns the identical value reference and build runs ONCE (spy counter).
       (b) A third resolve with one element changed returns a NEW reference and build runs again.
       (c) The exported PhysicsPaintToolRail carries the preact/compat memo marker: _forwarded === true OR a displayName starting with Memo( (accept either — preact minor-version drift).

    5. Run the script, typecheck, commit on main: feat(38-11): memoize Studio tool rail behind identity-stable props (tracer). Do not invoke vitest (D-15).
  </action>
  <verify>
    <automated>node /tmp/38-11-memo-proof.mjs && pnpm --dir app typecheck && test "$(grep -c 'createIdentityMemo' app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts)" -ge "1" && test "$(grep -c "from 'preact/compat'" app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx)" = "1" && test "$(grep -c 'toolRailPropsMemo.resolve' app/src/components/physic-paint/PhysicsPaintStudio.tsx)" = "1" && test "$(grep 'toolRailPropsMemo.resolve' app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c 'currentFrame')" = "0" && test -z "$(git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*')"</automated>
  </verify>
  <acceptance_criteria>
    - Ephemeral proof green: identity-memo semantics (build runs once for identical deps; re-runs on a changed dep) and the memo wrapper marker on the exported rail.
    - The toolRail resolve call exists exactly once, its deps array is single-line, and that line contains no frame-cursor reference (gate above).
    - usePhysicsPaintStudioViewModel.ts keeps its existing exports; ToolRail export name/props unchanged; typecheck clean; zero test files touched; no vitest invocation.
  </acceptance_criteria>
  <done>A startFrame-only Studio render now shallow-compares the rail's props equal and skips the subtree; the mechanism is proven by the ephemeral script and the deps contract, not by observation.</done>
</task>

<task type="auto">
  <name>Task 2: memo-wrapped RightPanel with stabilized props (panelMotion, scripts object) + full assembly audit</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx, /tmp/38-11-memo-proof.mjs</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:239-247 (the WR-02 rotoLegacyInterpolationSettings memo, b74ac80a), :340-342 (scriptLoadAndApplyDisabledReason), :1019-1027 (rotoMotion / panelMotion / updatePanelMotion), :1154-1172 (rightPanel + nested scripts assembly)
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (whole file — props usage; confirm zero frame-cursor coupling and where the scripts prop flows to PhysicsPaintScriptsPanel)
  </read_first>
  <action>
    Stabilize the larger subtree and audit the whole assembly.

    1. panelMotion source unification (WR-02 contract): the rightPanel playWiggle input currently derives from a per-render getRotoInterpolationSettings read that returns a fresh clone each call. Re-route it through the existing rotoLegacyInterpolationSettings memo so no fresh clone feeds the stabilized props; then produce panelMotion inside the memoized build (deps carry the deform/position primitives or the memoized settings object — enumerate exactly).

    2. In PhysicsPaintStudio.tsx: hold const rightPanelPropsMemo = useRef(createIdentityMemo()).current. Move the ENTIRE rightPanel props object — including the nested scripts object and its closures — inside rightPanelPropsMemo.resolve(deps, build). The closures (onSave/onActivateRow/onLoadAndApply/onDiscardScript/onCopyScript/onApplyScript/onRefresh and the panel-level on* handlers) are created inside build, so they are cached with the object; deps is a SINGLE-LINE array enumerating every value build references: the settings.* primitives, onion, isPlaying, mutationLocked, applyStatus, applyMessage, lastError, scriptLoadAndApplyDisabledReason, the memoized settings/motion inputs, the stable controllers (rotoScriptLibrary, rotoPlayScript, rotoScript), playButtonRef, setLastError, the useCallback-stable handlers (handleScriptRowActivate, handleSelectedScriptLoadAndApply — verify their existing useCallback deps rather than changing them), and every other referenced binding. Audit-first: any referenced binding that is a per-render closure must be wrapped in useCallback (enumerated deps, behavior unchanged) before joining the assembly.

    3. Wrap PhysicsPaintRightPanel exactly like the rail: private impl function + export const PhysicsPaintRightPanel = memo(impl) from preact/compat. Keep the createPhysicsPaintPaneResizeDrag named export byte-identical (its existing test imports it — the file must keep exporting it unchanged; do not run that test, D-15).

    4. Extend /tmp/38-11-memo-proof.mjs: also bundle PhysicsPaintRightPanel and assert its memo marker (same _forwarded/displayName rule), plus one integration-style assertion: simulate two Studio renders by calling resolve twice with two deps arrays whose frame-irrelevant elements are identical references — the returned rightPanel object is the SAME reference (this is the component-memo proof: stable props + memo wrapper = skipped render).

    5. Final audit sweep: re-read the toolRail/rightPanel assembly in PhysicsPaintStudio.tsx — every prop on both objects is a primitive, a signal object, a stable callback, or a value created inside a memo build; no inline per-render object/closure remains outside the builds. Record the audit table (prop -> stability class) in the SUMMARY.

    6. Typecheck, whole-plan diff sweep, commit on main: feat(38-11): memoize Studio right panel behind identity-stable props. Do not invoke vitest (D-15).
  </action>
  <verify>
    <automated>node /tmp/38-11-memo-proof.mjs && pnpm --dir app typecheck && test "$(grep -c 'rightPanelPropsMemo.resolve' app/src/components/physic-paint/PhysicsPaintStudio.tsx)" = "1" && test "$(grep 'rightPanelPropsMemo.resolve' app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c 'currentFrame')" = "0" && test "$(grep -c "from 'preact/compat'" app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx)" = "1" && test "$(grep -c 'createPhysicsPaintPaneResizeDrag' app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx)" -ge "1" && test -z "$(git diff --name-only HEAD -- app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx app/src/components/physic-paint/physicsPaintStudio.css)" && test "$(git diff --name-only HEAD -- app/ | grep -vcE 'PhysicsPaintStudio.tsx|PhysicsPaintRightPanel.tsx|PhysicsPaintToolRail.tsx|usePhysicsPaintStudioViewModel.ts')" = "0" && test -z "$(git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*')"</automated>
  </verify>
  <acceptance_criteria>
    - Ephemeral proof green for both components: memo markers present AND the two-simulated-renders assertion returns the identical rightPanel reference.
    - Both resolve calls are single-line deps arrays with zero frame-cursor references; the enumerated-deps comments are present.
    - PhysicsPaintStudioView.tsx and physicsPaintStudio.css have zero diff (no markup/visual change); whole-plan app diff limited to the four files_modified entries; createPhysicsPaintPaneResizeDrag export intact.
    - Typecheck clean; zero test files touched; no vitest invocation.
  </acceptance_criteria>
  <done>Both the right color sidebar and the left tool rail are skipped on startFrame-only Studio renders, with a deterministic component-memo proof and an audited prop-stability table; behavior is byte-identical and native visual confirmation is queued for the 38-06 re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — render-path memoization only; no data crosses a trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-11-01 | Tampering | Stale UI via over-memoization | medium | mitigate | Deps arrays enumerate exactly the referenced inputs (single-line, grep-gated, frame cursor excluded); signals are passed as objects (never .value-cached) so signal-driven updates bypass the memo by construction; ephemeral proof asserts recompute on any changed dep. |
| T-38-11-02 | Denial of Service | Per-render identity comparison cost | low | accept | resolve() is O(deps length) of Object.is comparisons (~20 elements) — negligible versus the skipped subtree render; no allocation on the hit path beyond the deps array literal. |
</threat_model>

<verification>
- Ephemeral proof (node /tmp/38-11-memo-proof.mjs): identity-memo semantics, memo wrapper markers on both components, and the two-simulated-renders identical-reference assertion — all green after Task 2.
- pnpm --dir app typecheck clean after each task.
- Static gates: single-line deps arrays with zero frame-cursor references; preact/compat import present in both component files; StudioView/CSS zero-diff; whole-plan diff limited to the four files; zero test artifacts; no vitest invocation (D-15).
- Native visual confirmation (side panels visibly no longer repaint on timeline navigation; everything else identical) is deferred to the 38-06 re-run — flag it explicitly in the 38-06 SUMMARY handoff.
</verification>

<success_criteria>
Timeline navigation repaints only the timeline/canvas surfaces; the right sidebar and tool rail are skipped via memo + referentially stable props, proven by the ephemeral script and the deps contract; zero visual/behavior change; zero test artifacts; typecheck green.
</success_criteria>

## Artifacts this phase produces

- Modified: app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (createIdentityMemo factory; existing exports byte-identical)
- Modified: app/src/components/physic-paint/PhysicsPaintStudio.tsx (toolRailPropsMemo / rightPanelPropsMemo assembly; useCallback stabilization; WR-02 memo reuse)
- Modified: app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx (preact/compat memo wrap)
- Modified: app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (preact/compat memo wrap; createPhysicsPaintPaneResizeDrag untouched)
- Ephemeral (not committed): /tmp/38-11-memo-proof.mjs + bundle output
- .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-11-SUMMARY.md

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-11-SUMMARY.md` when done. Record: the memo mechanism (stable props + preact/compat memo + signals bypass), the prop-stability audit table, the ephemeral proof assertions, the WR-02 memo reuse, and the D-15 deferral of native visual confirmation to the 38-06 re-run.
</output>
