---
context: phase
phase: 38-multi-copy-paste-and-tooltip-polish
task: 38-06 (checkpoint rejected — paused for Phase 38.1)
total_tasks: 8 plans
status: paused
last_updated: 2026-07-27T16:42:04.321Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Perf-blind pre-UAT gates | Phase 38's automated gates (typecheck, grep, build) all passed while a 5-10x render-path regression shipped. Scaling ceilings are invisible to static gates — only native interaction with realistic key counts exposes them. | advisory | For render-path phases, include a many-keys interaction pass in native UAT before declaring automated-ready; consider a key-count stress fixture. |
| Lag reported as logic failure | The user reported "shift multi-select broken" — it was event-queue lag from the saturated render loop, not a reducer bug (18/18 tests green, works after reopen). Diagnose perceived input failures against the perf baseline before touching logic. | advisory | When interaction "stops working" alongside a slowdown, verify timing/lag first; do not patch reducers on the strength of a laggy session. |

<current_state>
Phase 38 is paused at the 38-06 blocking native UAT checkpoint. Waves 1-2 (plans 38-01..38-05) are complete and committed on main: group clipboard + copy, paste-key-group seam, capsule idle context, group paste activation, tooltip rework. The user ran the 33-step native UAT: every functional step passed, but the many-key render-path slowdown made interaction/playback too degraded to approve honestly. UAT is NOT approved. Work is paused while a new decimal Phase 38.1 "Studio render-path performance" fixes the scaling ceiling. Working tree clean; typecheck + build green; 3 known-red tests expected per D-15 (see decisions).
</current_state>

<completed_work>

Completed Plans:
- 38-01: Group clipboard slot (single|group union) + group Copy — c434ed12
- 38-02: paste-key-group literal through types/resolver/coordinator/bridge — 08264914, a5a826c4
- 38-03: Capsule idle current-cell context, static baseline deleted — d72a3eda
- 38-04: Group paste activation + post-paste group selection — 6ec5dcfb, e78e0649
- 38-05: Viewport tooltip placement, notch, multiline clamp (15 mounts, fixed-in-place, no portal) — 3d7fc193, 7f432f8a, 734d6616
</completed_work>

<remaining_work>

1. **Phase 38.1 "Studio render-path performance"** (NEW — not yet created): insert into ROADMAP after phase 38 → /gsd-discuss-phase 38.1 → plan → execute → native UAT.
2. **38-06 re-run**: full 33-step native UAT after 38.1 lands (script is in the 38-06 checkpoint return / this phase's 38-06-PLAN.md).
3. **38-07**: post-UAT regression tests for the group seam (resolver paste-key-group contract, multiSelection paste-key-group branch, new physicsPaintRotoSession.test.ts).
4. **38-08**: post-UAT presentation/tooltip regression tests + fix the 3 known-red assertions + final full gate (vitest run → typecheck → build).
</remaining_work>

<decisions_made>

- **Perf slowdown root cause (diagnosed 2026-07-27):** `useRotoTimelineModel.ts:77` rebuilds the entire timeline model on every `currentFrame` change; the strip re-renders all N cells per frame; the Studio view-model is reconstructed per frame. Pre-existing 36.x/37 scaling ceiling exposed by group paste multiplying key count. All phase-38 diffs statically cleared — nothing in this phase touches the per-frame render path.
- **Shift-click "regression" is event lag, not a bug** — reducer tests 18/18 green; works after Studio reopen. Do not patch selection logic.
- **User-locked priorities for 38.1:** (1) canvas paints first on scrub — instant canvas feedback when clicking keys; (2) Preact UI estimation/redraw may lag behind canvas; (3) during playback, disable non-essential UI updates entirely for speed.
- **Routing:** decimal Phase 38.1 (user approved) over gap-plan-in-38 or direct quick fix — render loop is regression-prone; decisions need discuss-phase locking.
- **D-15 test deferral:** 3 known-red failures expected until 38-08: 2 physicsPaintWorkflowPresentation baseline assertions (38-08 scope) + 1 PhysicsPaintWorkflowStrip `placement="below"` source-grep made stale by 38-05's intentional region mechanism. Do NOT fix before UAT passes.
- **Tooltip mechanism (38-05, locked):** fixed-in-place positioning (no portal — containing-block audit clean); notch viewport-fixed at --tooltip-notch-x/y. 38.1 must not regress this.
- **Planning-data gap:** phase-38 requirement IDs never registered in REQUIREMENTS.md (mark-complete → not_found); recorded in phase deferred-items.md.
</decisions_made>

<blockers>
- Many-key Studio slowdown (5-10x at high key counts): blocks honest 38-06 UAT. Resolved by Phase 38.1.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume state
2. `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-PLAN.md` — the UAT script to re-run after 38.1
3. `app/src/components/physic-paint/hooks/useRotoTimelineModel.ts` (line 77) — the per-frame model rebuild at the heart of the slowdown
4. 38-01..38-05 SUMMARY.md files — what shipped and the locked mechanisms 38.1 must preserve

## Infrastructure State

- Working tree: clean, all work committed on main
- Gates: typecheck green, build green, vitest 846/849 pass with 3 known-red (D-15, listed above)
- App/dev server: user-owned — never start it from the agent

<context>
Phase 38 shipped its full production surface cleanly. The only open issue is a pre-existing scaling ceiling that group paste made visible. The user's product instinct is clear: canvas responsiveness is the primary interaction contract; Preact UI is secondary and may lag; playback should strip UI work to the minimum. Phase 38.1 exists to restore that contract at realistic key counts. Once 38.1 passes its own native UAT, return here, re-run the 38-06 script end-to-end (all 33 steps — including shift multi-select and the two UI-SPEC tooltip backstops), then execute wave 4.
</context>

<next_action>
Insert Phase 38.1 "Studio render-path performance" into ROADMAP.md after phase 38, then run /gsd-discuss-phase 38.1 to lock canvas-first / deferred-UI / playback-suspension decisions with the user.
</next_action>
