---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 07
type: tdd
wave: 4
depends_on:
  - 38-06
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts
autonomous: true
requirements:
  - 38-GROUP-COPY
  - 38-GROUP-PASTE
tags:
  - physics-paint
  - roto
  - group-paste
  - regression-tests
  - post-uat

must_haves:
  truths:
    - "GP-1: a well-formed 2-entry paste-key-group intent onto all-empty in-range destinations resolves ok; the earliest copied key anchors at the destination appFrame and the second lands at destination + its relative source-appFrame offset (D-04)"
    - "GP-2: an accepted group paste produces complete frozen nextRecords equal to the current records PLUS exactly the N declared fresh identities at the computed destinations with payloads retargeted to those destinations, every existing record's keyId/appFrame/payload unchanged (zero ripple, D-06), every pasted keyId fresh and allocated once by the factory (D-07), and proposal.selectedKeyId equal to the earliest pasted key's fresh keyId"
    - "GP-3: any computed destination occupied by an existing real key rejects atomically with code duplicate-destination-frame and the conflicting frames listed; no proposal exists (all-empty-or-reject, D-05)"
    - "GP-4: computed destinations that would push the record count past capacity reject atomically with code over-capacity; computed destinations outside [0, capacity) reject atomically with code out-of-range-frame (D-06)"
    - "GP-5: mutually colliding computed destinations (duplicate sourceAppFrames in the entries) reject atomically with code duplicate-destination-frame"
    - "GP-6: the shared validator validatePhysicPaintRotoPhysicalEditSemanticDelta accepts a well-formed paste-key-group delta (ok: true) and fails closed (ok: false) when nextRecords change any existing identity, omit a declared fresh record, introduce an undeclared identity, or the delta kind does not match the operation kind"
    - "GP-7: createPhysicPaintRotoPasteKeyGroupIntent throws on fewer than two entries, on a non-integer or negative destination, and on a malformed entry (payload, sourceAppFrame, or sourceKeyId failing the existing guards); on success it returns a deeply frozen intent with one fresh newKeyId per entry and no offset table (D-03)"
    - "The paste-key-group aftermath branch in resolvePostAcceptanceRotoSelection returns the full pasted keyId set as selectedKeyIds with the accepted selectedKeyId (earliest pasted key) as anchor; absent or empty acceptedAddedKeyIds falls through to the existing default collapse (fail-safe)"
    - "session.copyKeyGroup freezes {kind: 'group', entries} into the ONE clipboard slot with feedback `Copied {N} keys` (verbatim, no trailing period) and fails closed via the existing failed() helper with `Select at least two real Roto keys to copy.` on fewer than two entries (D-01/D-02)"
    - "The single shared slot overwrites both ways: a group Copy after a single-key Copy replaces the single entry, and a single-key Copy after a group Copy replaces the group; the stored group value and its entries array are frozen (D-02/D-03)"
    - "isRotoSessionCopiedKeyGroup narrows the slot union (true for group values, false for single values and null); createRotoSession input normalization passes a well-formed group through unchanged and reduces a malformed group (fewer than 2 entries or non-integer sourceAppFrame) to null"
    - "Paste availability stays shape-agnostic: hasCopiedRotoKey derives from slot-non-null, so a group clipboard yields the same canPaste derivation as a single clipboard (D-02; RESEARCH Open Question 2 resolution)"
  assumptions:
    - "Plans 38-01/38-02/38-04 production code has landed and native user-owned UAT (38-06) is explicitly approved; per D-15 this plan is the FIRST test work of the phase and runs only `vitest run` (never watch mode)."
    - "The production behavior under test is UAT-locked: tests assert the shipped contract. If a test exposes a genuine production defect (not a test-construction error), the executor stops and reports rather than editing production source in this plan."
    - "physicsPaintRotoSession.test.ts does not exist at planning time (verified 2026-07-27): this plan creates it. VALIDATION.md's clipboard-shape row (flagged check-at-planning) is resolved by this new file."
  prohibitions:
    - "No production source edits: this plan touches ONLY the three test files in files_modified"
    - "No vitest watch mode, no test config changes, no one-off configs (project memory: no test config hacks; CLAUDE.md: vitest run only)"
    - "No new failure-code vocabulary asserted beyond the existing codes (duplicate-destination-frame, over-capacity, out-of-range-frame)"
    - "No legacy source/display timing fields anywhere in the new or extended test code (D-16 hygiene)"
    - "No deferred ideas tested: no group-aware Duplicate, no Copy/Paste keyboard shortcuts, no replace-style group paste (CONTEXT.md Deferred Ideas)"
    - "Do not run the full suite here — the full `pnpm --dir app vitest run` + typecheck + build gate belongs to plan 38-08"
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — new describes for paste-key-group accept (GP-1/GP-2), atomic reject (GP-3..GP-5), shared validator branch (GP-6), and factory fail-closed discipline (GP-7)"
    - "app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts — extended resolvePostAcceptanceRotoSelection describe with the paste-key-group aftermath branch and its fail-safe fallback"
    - "app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts — NEW file: group clipboard shape coverage (slot overwrite both ways, immutability, copyKeyGroup feedback/fail-closed, union guard, input normalization, shape-agnostic availability)"
  key_links:
    - "Resolver tests must supply records (not identities alone): paste-key-group is a semantic operation, so resolvePhysicPaintRotoPhysicalEdit requires input.records matching the identities one-for-one or it fails with malformed-records before reaching the group branch (resolver validateSemanticInputRecords)"
    - "Fresh keyIds are minted by the factory, so accept tests build the intent via createPhysicPaintRotoPasteKeyGroupIntent first, then assert the resolution against intent.entries[i].newKeyId — never hardcode a generated id"
    - "The shared validator re-parses records via parsePhysicPaintRotoRealKeyRecordCollection, so validator tests build plain (unfrozen is fine) record arrays with valid payloads and assert the {ok, error} union shape"
---

<objective>
Post-UAT regression coverage for the Phase 38 group Copy/Paste seam, per D-15 (production first, native UAT blocking, tests after explicit approval). Three test targets: (1) extend `physicsPaintRotoPhysicalResolver.test.ts` with the `paste-key-group` accept/reject/validator/factory contract (D-04..D-07) in the file's existing locked-mapping style (GD-1.., GDel-1.., GFS-1.. anchors); (2) extend `physicsPaintRotoMultiSelection.test.ts` with the `paste-key-group` selection-aftermath branch (pasted group becomes the selection, earliest pasted key anchor, fail-safe collapse fallback); (3) create `physicsPaintRotoSession.test.ts` for the group clipboard shape (single/group slot overwrite both ways, immutability, `copyKeyGroup` feedback and fail-closed guard, union narrowing, input normalization, shape-agnostic paste availability). Requirements: 38-GROUP-COPY, 38-GROUP-PASTE.

Purpose: the group seam is UAT-approved behavior with zero automated anchors; these tests convert the approved rulings into deterministic regression locks using the same anchored-mapping style Phase 37 established, so later work cannot silently regress anchor/offset math, atomic reject policy, or the one-slot clipboard contract.
Output: three test files (two extended, one new), each green under targeted `pnpm --dir app vitest run <file>`; the full-suite gate stays with 38-08.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-VALIDATION.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-01-PLAN.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-02-PLAN.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-04-PLAN.md

@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
@app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Resolver paste-key-group regression anchors — accept (anchor/offsets/fresh keyIds/zero ripple), atomic reject (occupied/over-capacity/out-of-range/self-collision), shared validator branch, factory fail-closed</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <precondition>Plans 38-01/38-02/38-04 are executed and plan 38-06 native UAT is explicitly approved; the production exports `createPhysicPaintRotoPasteKeyGroupIntent` and the `paste-key-group` branch of `validatePhysicPaintRotoPhysicalEditSemanticDelta` exist in the resolver module.</precondition>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (FULL file first — the existing baseline helpers `buildBaselineIdentities`/`resolveBaseline`, the A@1,B@3,C@5,D@10 baseline, the GD-1../GDel-1../GFS-1.. describe style, and the header comment convention)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (the paste-key-group intent union member, `createPhysicPaintRotoPasteKeyGroupIntent`, the group candidate builder, the `paste-key-group` branch of `validatePhysicPaintRotoPhysicalEditSemanticDelta` at ~:455-600, and `validateSemanticInputRecords` at ~:1938 — semantic operations REQUIRE input.records matching the identities or resolution fails with `malformed-records` before reaching the group branch)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (`PhysicPaintRotoRealKeyRecord { kind: 'real-key', keyId, appFrame, payload }`, `PhysicPaintRotoRealKeyPayload { frameIndex, appFrame, dataUrl, width?, height? }`, `clonePayloadAtFrame`)
    - app/src/types/physicPaint.ts (`PHYSIC_PAINT_MAX_APPLY_FRAMES` already imported by the test file)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-02-PLAN.md (Task 1 action steps 3-5: the exact accept math and failure-code contract under test)
  </read_first>
  <behavior>
    - GP-1: entries with sourceAppFrames [1, 5] pasted at destination 20 over baseline A@1,B@3,C@5,D@10 resolve ok; the entry with sourceAppFrame 1 lands at frame 20 and the entry with sourceAppFrame 5 lands at frame 24 (anchor = min sourceAppFrame, relative offsets preserved, D-04).
    - GP-2: the accepted proposal's nextRecords contain every baseline record unchanged (same keyId, appFrame, and payload — zero ripple, D-06) plus exactly two fresh records at 20 and 24 whose keyIds equal intent.entries[0].newKeyId / intent.entries[1].newKeyId and whose payloads are the entry payloads retargeted to their destination frames (payload.appFrame equals the destination); proposal.selectedKeyId is the fresh keyId of the earliest-source entry; proposal.operationKind is 'paste-key-group'; the declared semanticDelta is frozen with kind 'paste-key-group' and destinationAppFrame 20.
    - GP-3: pasting the same entries at destination 1 (computed destinations 1 and 5, both occupied) rejects with ok false, failure.code 'duplicate-destination-frame', failure.operationKind 'paste-key-group', and conflictingAppFrames listing the occupied frames; there is no proposal (D-05).
    - GP-4a: with capacity chosen so baseline count (4) + 2 entries exceeds it (e.g. capacity 5), resolution rejects with code 'over-capacity'. GP-4b: with capacity 25 and destination 22 (second entry computes to 26), resolution rejects with code 'out-of-range-frame' (D-06).
    - GP-5: entries carrying a duplicate sourceAppFrame (two entries at sourceAppFrame 5) reject with code 'duplicate-destination-frame' — computed destinations collide with each other.
    - GP-6: `validatePhysicPaintRotoPhysicalEditSemanticDelta` returns { ok: true } for a well-formed group delta (current = baseline records; next = baseline + the two retargeted fresh records; delta = { kind: 'paste-key-group', destinationAppFrame: 20, entries } with selectedKeyId/selectedAppFrame naming the earliest fresh identity at 20), and { ok: false } with a non-empty error string when: (a) next changes an existing record's appFrame, (b) next omits one declared fresh record, (c) next adds an undeclared extra identity, (d) semanticDelta.kind is 'paste-key' while operationKind is 'paste-key-group'.
    - GP-7: `createPhysicPaintRotoPasteKeyGroupIntent` throws for entries.length < 2, for a negative or fractional destinationAppFrame, and for an entry whose payload fails `isPhysicPaintRotoRealKeyPayload` or whose sourceKeyId/sourceAppFrame fails the bounded guards; a successful call returns an intent with `Object.isFrozen(intent) === true`, every entry frozen, a distinct newKeyId per entry, and no offset field anywhere on the intent (D-03).
  </behavior>
  <action>
    Extend the EXISTING resolver test file only — never edit production source in this plan. Mirror the file's established conventions exactly (38-PATTERNS.md section 14): same import block style, `Object.fromEntries(proposal.mapping)` mapping assertions, `if (!resolution.ok) throw new Error('GP-x must resolve ok')` narrowing idiom, and a header comment addition recording the anchor IDs (GP-1..GP-7) and the 38-06 UAT approval as their authority.

    1. Add builders beside `buildBaselineIdentities`: `buildBaselineRecords()` returning the four baseline identities as full `PhysicPaintRotoRealKeyRecord` values with minimal valid payloads (frameIndex 0, appFrame matching the identity, a short `data:image/png;base64,` dataUrl string, width/height 2); `buildGroupEntries()` returning two frozen entries cloned from baseline records A and C (payload from the record, sourceAppFrame from the record, sourceKeyId 'A'/'C' respectively); and `resolveBaselineWithRecords(intent, capacity?)` mirroring `resolveBaseline` but also passing `records: buildBaselineRecords()` and a capacity parameter defaulting to `PHYSIC_PAINT_MAX_APPLY_FRAMES`. Import `createPhysicPaintRotoPasteKeyGroupIntent` and `validatePhysicPaintRotoPhysicalEditSemanticDelta` from the resolver module and the record/payload types from the model module.

    2. New describe `resolvePhysicPaintRotoPhysicalEdit — paste-key-group (GP-1..GP-7, D-04..D-07)` containing one `it` per behavior bullet above. For GP-1/GP-2 build the intent FIRST via the factory at destination 20 (fresh keyIds are minted there — assert against `intent.entries[i].newKeyId`, never a hardcoded id), then resolve and assert mapping, nextRecords content, selectedKeyId, operationKind, and the frozen delta per the behavior bullets. For GP-3..GP-5 reuse the same factory with the reject destinations/capacities per bullet (GP-4a/GP-4b may share one `it` with two resolve calls, mirroring the existing multi-case `it` style at the force-spacing invalid-emptyFrames test). For GP-5 construct the duplicate-sourceAppFrame entries inline in the test.

    3. New describe `validatePhysicPaintRotoPhysicalEditSemanticDelta — paste-key-group branch (GP-6)` asserting the one accept and the four fail-closed reject cases from the behavior block. Build `next` by spreading the baseline records and appending the two fresh records with payloads retargeted via the same field-level shape (payload.appFrame = destination); each reject case mutates exactly one thing per bullet so the error cause is unambiguous. Assert the union shape (`validation.ok` boolean; on failure `typeof validation.error === 'string'` and non-empty).

    4. New describe (or trailing `it` block inside the GP describe) `createPhysicPaintRotoPasteKeyGroupIntent — fail-closed factory (GP-7)`: wrap each throw case in `expect(() => ...).toThrow()`; for the success case assert frozenness (`Object.isFrozen` on the intent and on each entry), distinct newKeyIds, and that no key on the intent or entries is named like an offset table (assert the exact key set of an entry: payload, sourceAppFrame, sourceKeyId, newKeyId).

    Keep every existing describe and helper byte-identical. Do not add any field using legacy source/display timing naming (D-16). Do not invoke the full suite — targeted run only.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
    Expected: exit 0; all pre-existing GD/GDel/GFS tests still pass plus the new GP describes. Run exactly once per change cycle; never watch mode.
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` exits 0.
    - `grep -c "GP-" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` returns at least 7 (one anchor ID per behavior bullet, in it titles or the header comment).
    - `grep -c "paste-key-group" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` returns at least 6 (describe titles, operationKind assertions, validator kind assertions).
    - `grep -cE "duplicate-destination-frame|over-capacity|out-of-range-frame" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` increases over its pre-task baseline by at least 3 (the three reused reject codes asserted).
    - `grep -cE "sourceFrame|displayFrame" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` equals its pre-task baseline (D-16 hygiene; note: fromSourceFrame/toSourceFrame substrings do not match this pattern's word usage in the new code — new test code introduces zero such identifiers).
    - `grep -c "buildBaselineIdentities" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` equals its pre-task baseline (existing helper untouched; new builders added alongside).
    - `git diff --stat` for this task shows ONLY `physicsPaintRotoPhysicalResolver.test.ts` modified.
  </acceptance_criteria>
  <done>The resolver test file locks the UAT-approved group paste contract: anchor/offset math, fresh keyIds allocated once, zero ripple, complete frozen nextRecords + semanticDelta, the three reused atomic-reject codes plus self-collision, the shared validator's accept and four fail-closed reject paths, and the factory's throw/freeze discipline — all green under the targeted vitest run with every pre-existing anchor untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Multi-selection aftermath regression anchors — paste-key-group selects the pasted set with the earliest pasted key anchor; fail-safe collapse fallback</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts</files>
  <precondition>Plan 38-04 Task 2 is executed: `resolvePostAcceptanceRotoSelection` accepts the optional `acceptedAddedKeyIds` input and carries the `paste-key-group` branch in production.</precondition>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts (FULL file — the `selection(...)` helper, the existing `resolvePostAcceptanceRotoSelection (D-17)` describe with its four its at the bottom of the file)
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts (`resolvePostAcceptanceRotoSelection` — the new `acceptedAddedKeyIds` optional input and the `paste-key-group` branch added by 38-04, plus the default collapse it must fall through to)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-04-PLAN.md (Task 2 action step 1: branch contract — pasted set selected, accepted selectedKeyId as anchor, absent/empty added-ids falls through to collapse)
  </read_first>
  <behavior>
    - Accepted group paste with `operationKind: 'paste-key-group'`, `acceptedAddedKeyIds: ['P', 'Q']` (appFrame-ordered, earliest first), `acceptedSelectedKeyId: 'P'`, prior state `{ selectedKeyIds: ['B', 'C'], anchorKeyId: 'B' }` returns `{ selectedKeyIds: ['P', 'Q'], anchorKeyId: 'P' }` — the pasted group replaces the selection and the earliest pasted key is the anchor (38-04 branch; mirrors the move-key-group shape).
    - The prior selection state is not mutated by the call (input state object unchanged after resolution).
    - Fail-safe fallback: same operation kind with `acceptedAddedKeyIds` absent returns the default collapse `{ selectedKeyIds: ['P'], anchorKeyId: 'P' }`; with `acceptedAddedKeyIds: []` returns the same collapse — never throws, never fabricates a set.
    - Regression: existing branches stay authoritative — `move-key-group`, `force-spacing`, `delete-key-group`, and the default collapse for `paste-key` behave exactly as the four pre-existing its assert (this task adds cases; it does not re-assert them beyond the suite passing).
  </behavior>
  <action>
    Extend the EXISTING multi-selection test file only. Add its INSIDE the existing `resolvePostAcceptanceRotoSelection (D-17)` describe, after the current cases, using the same `selection(...)` helper and `toEqual` whole-state assertion style. Three new its matching the behavior bullets: (1) `selects the accepted pasted group with the earliest pasted key as anchor after 'paste-key-group'`; (2) `does not mutate the prior selection state` (capture a deep copy of the input state before the call and assert equality after); (3) `falls back to the default collapse when acceptedAddedKeyIds is absent or empty` (two resolve calls in one it, mirroring the existing multi-case it style). Update the describe's doc/header comment line if the file header enumerates covered branches — keep every existing it byte-identical. Assert with plain literal keyIds ('P', 'Q'); the reducer input shape takes `acceptedAddedKeyIds` as a readonly string array field alongside the existing four fields.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts</automated>
    Expected: exit 0; all pre-existing selection reducer tests pass plus the new paste-key-group its. Never watch mode.
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` exits 0.
    - `grep -c "paste-key-group" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` returns at least 3 (the new its' operation kind and title mentions).
    - `grep -c "acceptedAddedKeyIds" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` returns at least 2 (branch case + fallback case).
    - `grep -c "resolvePostAcceptanceRotoSelection" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` increases over baseline by exactly 3 (one per new case construction: branch, non-mutation, fallback counting its resolve calls — verify by counting new call sites, minimum 3).
    - `git diff --stat` for this task shows ONLY `physicsPaintRotoMultiSelection.test.ts` modified.
  </acceptance_criteria>
  <done>The aftermath reducer's UAT-approved paste-key-group behavior is locked: accepted pasted set becomes the selection with the earliest pasted key as anchor, the input state is not mutated, and absent/empty acceptedAddedKeyIds collapses fail-safe to the accepted selectedKeyId — green under the targeted run with all pre-existing reducer anchors untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>NEW session test file — group clipboard shape: slot overwrite both ways, immutability, copyKeyGroup feedback/fail-closed, union guard, input normalization, shape-agnostic availability</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts</files>
  <precondition>Plan 38-01 is executed: `physicsPaintRotoSession.ts` exports `RotoSessionCopiedGroupEntry`, `RotoSessionCopiedKeyGroup`, `RotoSessionCopiedKeyValue`, `isRotoSessionCopiedKeyGroup`, the widened `copiedKey` slot, and the `copyKeyGroup(entries)` session method with `'copyKeyGroup'` in the action-name union.</precondition>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoSession.ts (FULL file — `RotoSessionInput` (~:36-47), `createRotoSession` (~:72), the widened slot type and signal, `copyKey` (~:143-152, byte-identical single path), `copyKeyGroup`, `normalizeCopiedKey` group branch (~:216+), `isRotoSessionCopiedKeyGroup`, and the `actionAvailability` derivation with `hasCopiedRotoKey` at ~:95)
    - app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts OR `rotoEditBufferTransactions.test.ts` (skim one existing roto test file for import/describe conventions in this directory — pick whichever exercises a create-style factory to mirror its construction idiom)
    - app/src/types/physicPaint.ts (`PhysicPaintRotoCacheFrame` at :405 — the realKeyFrames element shape the session input needs)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-01-PLAN.md (Task 1 action step 1: the exact copyKeyGroup contract — fail-closed message `Select at least two real Roto keys to copy.`, success message `Copied {N} keys` verbatim with no trailing period, frozen {kind: 'group', entries} slot value, effects: [])
  </read_first>
  <behavior>
    - `copyKeyGroup` with two valid entries returns `{ action: 'copyKeyGroup', ok: true, message: 'Copied 2 keys', effects: [] }`; the session `copiedKey.value` is a group value whose entries preserve input order and entry fields (payload reference, sourceAppFrame, sourceKeyId); `Object.isFrozen` is true on the slot value and on its entries array.
    - `copyKeyGroup` with fewer than two entries (empty array and single-entry array) returns `ok: false` with message `Select at least two real Roto keys to copy.` and leaves the previous clipboard content untouched.
    - Slot overwrite group-over-single: seed a single clipboard via `copyKey()` on a real key at the current frame, then `copyKeyGroup(twoEntries)` — the slot now holds the group and `isRotoSessionCopiedKeyGroup(slot)` is true.
    - Slot overwrite single-over-group: seed the group via `copyKeyGroup`, then `copyKey()` — the slot now holds the single shape (`{ frame, cachedFrame }`) and `isRotoSessionCopiedKeyGroup(slot)` is false (D-02: one slot, last Copy wins).
    - `isRotoSessionCopiedKeyGroup` returns false for null and for a single-shape value, true for a group value.
    - Input normalization: `createRotoSession({ ..., copiedKey: <well-formed group> })` exposes the group unchanged on `copiedKey.value`; a malformed group input (entries.length 1, or an entry with a non-integer sourceAppFrame) normalizes to null.
    - Shape-agnostic availability: with a group clipboard held, `actionAvailability.value.hasCopiedRotoKey`-driven canPaste derivation matches the single-clipboard case for the same frame state (assert the same availability predicate value both ways on an idle real-key frame).
  </behavior>
  <action>
    Create the NEW test file `physicsPaintRotoSession.test.ts` (this file does not exist at planning time — VALIDATION.md's flagged clipboard-shape row is resolved here). Follow the directory's vitest conventions: `import { describe, expect, it } from 'vitest';`, a short header comment recording the 38-06 UAT approval and D-01/D-02/D-03 as the behavior authority, then small builders.

    1. Builders: `buildCacheFrame(appFrame)` returning a minimal valid `PhysicPaintRotoCacheFrame` for a real key at the given frame (read the interface at app/src/types/physicPaint.ts:405 plus `PhysicPaintRenderedFrame` at :491 and fill every required field with minimal valid values — a short PNG dataUrl string, frameIndex 0, the real-key source discriminator used by the session's `normalizeRealKeyFrame` path); `buildGroupEntries()` returning two entry literals `{ payload: { frameIndex: 0, appFrame, dataUrl, width: 2, height: 2 }, sourceAppFrame, sourceKeyId: 'A' | 'C' }` (sourceAppFrames 1 and 5); `buildSession(overrides?)` calling `createRotoSession` with `currentFrame: 1`, `realKeyFrames: [buildCacheFrame(1)]`, `buildBlankRotoFrame: (appFrame) => buildCacheFrame(appFrame)`, spread overrides.

    2. Describes: `copyKeyGroup — group clipboard capture (D-01/D-03)` (success shape + frozenness + preserved fields; both fail-closed length cases, each asserting the previous slot value reference is unchanged); `clipboard slot overwrite — one shared slot (D-02)` (group-over-single and single-over-group, each asserting the guard `isRotoSessionCopiedKeyGroup` outcome and the surviving shape); `isRotoSessionCopiedKeyGroup — union narrowing` (null, single, group); `createRotoSession — group clipboard input normalization` (well-formed pass-through, two malformed-to-null cases); `actionAvailability — shape-agnostic paste availability` (build one session seeded with a single clipboard via copyKey and one seeded with a group via copyKeyGroup on the same frame state; assert the availability predicate the strip consumes is identical). For sessions seeded through input, use the `copiedKey` input field; for the fail-closed "clipboard untouched" assertions capture `session.copiedKey.value` before the failing call and assert `toBe` (reference equality) after.

    3. Keep the file self-contained: no imports from hooks, no store, no DOM. Signals are read via `.value` directly — no effects. Assert the success message verbatim `Copied 2 keys` and the fail message verbatim `Select at least two real Roto keys to copy.` (both locked by 38-01). Do not use any legacy source/display timing identifiers in builders or assertions (D-16).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts</automated>
    Expected: exit 0 on the new file's full describe set. Never watch mode.
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` exits 0.
    - `grep -c "copyKeyGroup" app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` returns at least 4 (success case, two fail-closed cases, action-name assertion).
    - `grep -F -c "Copied 2 keys" app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` returns exactly 1 (verbatim locked feedback asserted once).
    - `grep -F -c "Select at least two real Roto keys to copy." app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` returns at least 1 (verbatim locked fail message).
    - `grep -c "isRotoSessionCopiedKeyGroup" app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` returns at least 3 (import plus narrowing assertions).
    - `grep -c "Object.isFrozen" app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` returns at least 1 (immutability assertion).
    - `git status --porcelain -- app/src/components/physic-paint/roto/physicsPaintRotoSession.ts` returns EMPTY (production session module untouched).
  </acceptance_criteria>
  <done>The new session test file locks the UAT-approved group clipboard contract: frozen group capture with verbatim feedback, fail-closed entry-count guard preserving the prior slot, one-slot overwrite in both directions, union narrowing, input normalization fail-closed to null, and shape-agnostic paste availability — green under the targeted run with zero production edits.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

New test describes/its (all post-UAT per D-15; locked-mapping anchor style):

| Artifact | Kind | Location | Coverage |
|----------|------|----------|----------|
| describe `resolvePhysicPaintRotoPhysicalEdit — paste-key-group (GP-1..GP-7, D-04..D-07)` | extended test describe | `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` | GP-1 anchor/offsets; GP-2 fresh keyIds + zero ripple + complete frozen nextRecords/delta; GP-3 occupied reject; GP-4 over-capacity + out-of-range rejects; GP-5 self-collision reject; GP-7 factory fail-closed/frozen |
| describe `validatePhysicPaintRotoPhysicalEditSemanticDelta — paste-key-group branch (GP-6)` | extended test describe | same file | shared validator accept + four fail-closed rejects (changed identity, omitted fresh record, undeclared identity, kind mismatch) |
| 3 new its in `resolvePostAcceptanceRotoSelection (D-17)` | extended test its | `app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` | pasted-set selection with earliest-anchor; input-state immutability; absent/empty fail-safe collapse |
| `physicsPaintRotoSession.test.ts` (5 describes) | NEW test file | `app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` | copyKeyGroup capture/fail-closed; one-slot overwrite both ways; union narrowing; input normalization; shape-agnostic availability |

Explicitly NOT produced here (owned by 38-08): presentation/capsule test updates, tooltip placement describe, the full `pnpm --dir app vitest run` + typecheck + build final gate.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> production contract | Tests assert the UAT-locked production seam; a failing new assertion is a stop-and-report signal, not a license to edit production source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-07-01 | Tampering | test files under construction | low | mitigate | Tests-only diff gates per task (`git diff --stat` / porcelain gates); production modules verified untouched |
| T-38-07-02 | Repudiation | anchor-ID traceability | low | mitigate | GP-1..GP-7 anchor IDs plus header comments record the 38-06 UAT approval as the behavior authority, mirroring the 37-06 precedent |
| T-38-SC | Tampering | npm/pnpm installs | high | mitigate | Vacuous — no installs in this plan (tests use the existing vitest setup; no test config changes per project memory) |
</threat_model>

<verification>
Plan-level checks (post-UAT per D-15; targeted runs only):

1. `pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` — exit 0 (Task 1).
2. `pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` — exit 0 (Task 2).
3. `pnpm --dir app vitest run app/src/components/physic-paint/roto/physicsPaintRotoSession.test.ts` — exit 0 (Task 3).
4. Scope gate: `git status --porcelain` for this plan lists exactly the three test files in files_modified — zero production files.
5. D-16 hygiene: no legacy source/display timing identifiers introduced by the new test code (per-task grep gates).
6. The full suite, typecheck, and build are intentionally NOT run here — plan 38-08 owns the final gate (D-15 sequencing; 38-VALIDATION.md sampling table).
</verification>

<success_criteria>
- 38-GROUP-PASTE locked: resolver accept (anchor = min sourceAppFrame, relative offsets, exact frames, zero ripple, fresh keyIds allocated once, earliest pasted key selected) and atomic reject (occupied / over-capacity / out-of-range / self-collision via the three existing codes), the shared validator branch, and the factory's fail-closed frozen-output discipline are all anchored by green GP-1..GP-7 tests.
- 38-GROUP-PASTE aftermath locked: the pasted group becomes the selection with the earliest pasted key anchor; absent/empty added-ids collapses fail-safe.
- 38-GROUP-COPY locked: one shared slot with both-way overwrite, frozen group entries, verbatim locked feedback strings, fail-closed entry guard, union narrowing, normalization, and shape-agnostic availability.
- Every pre-existing anchor in both extended files passes unchanged; only the three test files are modified/created; no watch mode; no production edits; no full-suite run (38-08's gate).
</success_criteria>

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-07-SUMMARY.md` when done, recording: the per-task vitest run outputs (pass counts), the GP anchor list as implemented, confirmation that zero production files were touched, and any test-construction adjustments made against the shipped contract (e.g. exact record-builder fields) for 38-08's awareness.
</output>
