# Phase 38: Multi-Copy/Paste and Tooltip Polish — Plan Outline

**Mode:** chunked (outline only) · **Granularity:** fine · **Tracer-first:** on · **MVP:** on · **Plans:** 8 · **Waves:** 4

## Plan Table

| Plan ID | Objective | Wave | Depends On | Requirements |
|---------|-----------|------|------------|--------------|
| 38-01 | Group clipboard: widen the one shared slot to a frozen `single \| group` discriminated union (D-02/D-03) in `physicsPaintRotoSession.ts` + `useRotoKeyUtilities.ts`; group Copy (2+ selected) snapshots immutable `{payload, sourceAppFrame, sourceKeyId}` entries from `physicPaintStore.getRotoRealKeyRecords(layerId)` (never the frame-indexed session cache — Pitfall 6) with `Copied {N} keys` feedback; 1-key Copy stays byte-identical (D-01, Pitfall 7). Tracer: select 2 keys → Copy → capsule shows `Copied 2 keys`, slot holds frozen group entries, single-key copy path untouched. Reversibility: D-01 rated **costly** (flag). | 1 | [] | 38-GROUP-COPY |
| 38-02 | `paste-key-group` five-owner literal propagation with atomic reject (D-04..D-07): shared types union + fail-closed delta validator + semantic-op set (`app/src/types/physicPaint.ts`), resolver intent member + `createPhysicPaintRotoPasteKeyGroupIntent` factory (fresh keyIds allocated ONCE) + group candidate builder (anchor = min sourceAppFrame; dest_i = destination + offset; all-empty-or-reject via existing `duplicate-destination-frame`/`over-capacity`/`out-of-range-frame` codes; zero ripple) + shared validator branch + dispatch branch, coordinator `semanticDeltaEquals` + ordinary routing + per-entry payload retargeting, bridge semantic-validation branch, history literal verify-only (one accepted output = one Undo/Redo by construction). Tracer: full literal chain compiles and a pure resolver call accepts a 2-entry group onto empty destinations producing complete frozen nextRecords + semanticDelta. Reversibility: D-05 rated **costly** (flag). | 1 | [] | 38-GROUP-PASTE |
| 38-03 | Status capsule fix (D-08/D-09/D-10): delete `ROTO_STATUS_CAPSULE_BASELINE` and its fallback in `getRotoStatusCapsuleViewModel`; strip feeds the existing `ambient` slot with the UI-SPEC locked current-cell context mapping (`Real Roto key · Frame {n}` / `Generated frame · Frame {n}` / `Empty frame · Frame {n}`) derived from `currentSemanticCell` + `props.currentFrame`; arbitration priority unchanged (pending > saving > feedback > idle context); `useRotoCachedPlayback` missing-frame lines stay as the event-driven surface (wording review only). Tracer: idle on a real key shows `Real Roto key · Frame 5`, navigate to empty cell shows `Empty frame · Frame 7`, baseline string gone from source. | 1 | [] | 38-CAPSULE-IDLE-CONTEXT |
| 38-04 | Group paste activation + selection aftermath: `useRotoTimelineActions.pasteKeyGroup` route (factory → `runPhysicalAction`, operationKind `paste-key-group`, `Pasted {N} keys` success, UI-SPEC locked reject copy mapping `Paste rejected — key in the way` / `Paste rejected — not enough room` with full detail to LOG per 36.14 D-26); clipboard-shape branch in `useRotoKeyUtilities.pasteKey` (group → new route, single → existing byte-identical path incl. `addEmptyKey` reuse — Pitfall 7); `resolvePostAcceptanceRotoSelection` gains the `paste-key-group` branch (pasted group becomes selection, earliest pasted key current — Pitfall 2). Tracer: with a group clipboard, Paste at an empty destination lands all keys at exact computed frames with fresh keyIds, one history entry, group selected. | 2 | [38-01, 38-02] | 38-GROUP-PASTE, 38-DOWNSTREAM-PARITY |
| 38-05 | Tooltip rework (D-11..D-14): new `computeTooltipPlacement(anchorRect, region, pillSize)` pure utility (greenfield — opposite-of-region direction, flip-on-insufficient-room, 8px viewport clamp, notch offset = anchor center − pill left after clamping); `PhysicsPaintStyledTooltip` viewport-positioned rendering (fixed vs `createPortal(document.body)` decided by ancestor containing-block audit — RESEARCH Open Question 1) with `id`/`aria-describedby` wiring intact and `useStyledTooltip` contract untouched (1000ms/focus/Escape-while-visible); 10×6px same-fill directional notch; CSS block rework (`max-width: 280px; max-height: 96px; white-space: normal`, ellipsis/nowrap removed, direction modifiers replace `--below`); every mount converted (strip header/capsule/cells/action row → `top`/`bottom` regions; `PhysicsPaintScriptsPanel` → `right-edge`). Tracer: one bottom action-row mount renders viewport-positioned above its control with notch + wrapped text under horizontal scroll. Reversibility: D-12 rated **costly** (flag). | 2 | [38-03] | 38-TOOLTIP-VIEWPORT-PLACEMENT, 38-TOOLTIP-NOTCH-MULTILINE |
| 38-06 | BLOCKING native user-owned UAT checkpoint (D-15): user verifies group Copy/Paste (2+ copy, anchored paste with relative offsets, atomic collision/capacity rejects, generated-cell landing, one Undo/Redo, single-key paths unchanged), capsule idle context per cell kind with no permanent baseline, tooltip opposite-side placement/notch/multiline/viewport clamp across header, cells, action row, tube/log, sidebar — incl. the 2 UI-SPEC backstop items (viewport clamp/flip; notch tracks control when clamped). No test creation/modification/execution before explicit approval; known-red `physicsPaintWorkflowPresentation.test.ts:159-225` baseline assertions are expected, not a bug (Pitfall 5). | 3 | [38-04, 38-05] | 38-UAT-THEN-REGRESSION, 38-DOWNSTREAM-PARITY |
| 38-07 | Post-UAT regression tests — group seam (type: tdd): extend `physicsPaintRotoPhysicalResolver.test.ts` (accept: anchor/offsets/fresh keyIds/zero ripple; atomic reject: occupied/over-capacity/out-of-range; shared validator branch) and `physicsPaintRotoMultiSelection.test.ts` (`paste-key-group` aftermath); add group clipboard shape coverage (single/group slot overwrite, immutability) in session/key-utilities tests. `vitest run` only. | 4 | [38-06] | 38-GROUP-COPY, 38-GROUP-PASTE |
| 38-08 | Post-UAT regression tests — presentation + final gate (type: tdd): update `physicsPaintWorkflowPresentation.test.ts:159-225` baseline assertions to the idle-context contract (per cell kind, priority order, baseline deleted); add new `computeTooltipPlacement` pure-function describe (direction per region, flip, 8px clamp, notch offset after clamp); close with full `pnpm --dir app vitest run` → `pnpm --dir app typecheck` → build green. | 4 | [38-06] | 38-CAPSULE-IDLE-CONTEXT, 38-TOOLTIP-VIEWPORT-PLACEMENT, 38-TOOLTIP-NOTCH-MULTILINE, 38-UAT-THEN-REGRESSION |

## Wave Structure

- **Wave 1 (parallel, disjoint files):** 38-01 (session + keyUtilities + Studio selection read), 38-02 (types + resolver + coordinator + bridge + history), 38-03 (presentation selector + strip capsule wiring).
- **Wave 2 (parallel, disjoint files):** 38-04 (timelineActions + multiSelection + keyUtilities paste branch), 38-05 (tooltip component + CSS + strip mounts + scripts panel). 38-05 is sequenced after 38-03 solely because both modify `PhysicsPaintWorkflowStrip.tsx`; 38-04 keeps the strip untouched (availability stays shape-agnostic, feedback is controller-owned).
- **Wave 3:** 38-06 — BLOCKING user-owned native UAT; agent never launches server/browser/native app.
- **Wave 4 (parallel, disjoint test files):** 38-07, 38-08 — post-UAT only per D-15.

## Recorded Planning Decisions and Flags

- **Requirement IDs:** REQUIREMENTS.md has no registered `38-*` IDs; plans use the RESEARCH.md proposed set (`38-GROUP-COPY`, `38-GROUP-PASTE`, `38-CAPSULE-IDLE-CONTEXT`, `38-TOOLTIP-VIEWPORT-PLACEMENT`, `38-TOOLTIP-NOTCH-MULTILINE`, `38-DOWNSTREAM-PARITY`, `38-UAT-THEN-REGRESSION`) consistently so the coverage gate can match.
- **API coverage checkpoint:** no external API integration — phase is internal Preact UI + timeline state work; a reasoned `COVERAGE.md` declaration will be written in 38-01.
- **Assumption-delta (clipboard single → single|group):** D-02 already decided one shared slot as a discriminated union → record `<assumption_delta_decision>` as `no-change` (ONE slot stays the identity anchor; generalization is shape-widening, not pluralization) in 38-01 front matter.
- **Schema push gate:** no ORM/schema files touched — skip.
- **Spec-less probe fallback:** visibly skipped (no requirement IDs to probe); no probe predicates generated. 38-UI-SPEC `## UI Considerations` lift applies instead: 13 `covered` rows → `must_haves.truths` (plain strings), 2 `backstop` rows → `{ statement, verification: backstop }` flat-scalar markers, 0 `unresolved`.
- **User-story framing (MVP mode):** ROADMAP Phase 38 Goal is not in user-story format; per planner-mvp-mode the full planning pass will surface this rather than invent a story.
- **Geometry authority:** 161px strip bands / 34px action row / 18px cells (CONTEXT.md + current code) override ROADMAP boilerplate 155px/28px.
- **Reversibility flags:** D-01 (38-01), D-05 (38-02), D-12 (38-05) rated `costly` — flagged only, no decision checkpoints (none are one-way).
- **Structural-type seam:** 38-01 and 38-02 stay parallel because the group-entry shape `{payload, sourceAppFrame, sourceKeyId}` is structurally typed at the resolver factory boundary; the session module needs no import from shared types for the clipboard shape.
- **D-15 red-window:** `physicsPaintWorkflowPresentation.test.ts:159-225` stays knowingly red between 38-03 and 38-08; plans must not touch it pre-UAT.
- **Threat model:** each plan carries a `<threat_model>` (ASVS L1): T-38-01 fail-closed group intent/delta validation (V5), T-38-02 reject detail to LOG only, concise capsule strings (V7/36.14 D-26), T-38-03 bridge revalidation of group delta against authoritative records (V13), T-38-04 Preact text-children-only tooltip/capsule content (T-36.15-01/08), T-38-05 aria-describedby association preserved under viewport positioning. T-38-SC package gate satisfied vacuously (no new packages).
