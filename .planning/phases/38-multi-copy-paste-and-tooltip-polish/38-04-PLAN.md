---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 38-01
  - 38-02
files_modified:
  - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 38-GROUP-PASTE
  - 38-DOWNSTREAM-PARITY
tags:
  - physics-paint
  - roto
  - group-paste
  - selection-aftermath
  - acknowledged-transactions
mvp_user_story: unavailable # ROADMAP Phase 38 Goal is outcome-shaped but NOT in "As a / I want to / so that" format; surfaced per planner-mvp-mode (same as 38-01) instead of inventing a story

must_haves:
  truths:
    - "Group paste accept (UI-SPEC populated row: covered): with a group clipboard, Paste at an all-empty destination lands the earliest copied key at the current editing cell and every other copied key at destination + its relative physical offset, at exact computed frames with zero ripple, generated destinations becoming real keys, every pasted key carrying a fresh keyId, as ONE accepted transaction = exactly one Undo/Redo action (D-04/D-06/D-07)"
    - "Group paste collision reject (UI-SPEC error/partial row: covered): all-empty-or-reject — any occupied computed destination rejects the whole paste atomically with zero partial mutation; the capsule shows the locked line `Paste rejected — key in the way`, full destination detail goes to LOG only, and selection, map, and clipboard are fully preserved (D-05; T-38-02)"
    - "Group paste range/capacity reject (UI-SPEC error row: covered): out-of-range or over-capacity computed destinations reject atomically with the locked line `Paste rejected — not enough room` and no state or history change (D-06)"
    - "Post-paste selection (UI-SPEC populated row: covered): the pasted group becomes the selection with the earliest pasted key as the current editing key; focus/scroll follow via the existing accepted-selection sync because the 38-02 proposal sets selectedKeyId to the earliest pasted fresh keyId (D-07 + adopted discretion; Pitfall 2 closed — without the new branch the group collapses to one key)"
    - "Acknowledged group paste (UI-SPEC loading row: covered): the busy line `Pasting keys…` shows only while the acknowledged transaction is pending and is always overwritten by the success or reject line; rollback on rejection/timeout restores the pre-paste map and selection through the existing coordinator settlement path — no new machinery"
    - "Group paste success feedback is the locked line `Pasted {N} keys` published through the same successMessage slot as PASTE_SUCCESS_MESSAGE (N is known at route-call time, so runPhysicalAction's fixed-string contract is unchanged)"
    - "Reject copy split (36.14 D-26; T-38-02): the capsule only ever carries the three fixed concise lines; the resolver failure code and full text route to LOG via the existing publishDiagnostic port"
    - "Single-key paste path stays byte-identical including addEmptyKey reuse: the existing paste-key route, its factory call, PASTE_SUCCESS_MESSAGE, the toClipboardPayload path in the hook, and replace-style single-paste policy are untouched (Pitfall 7 — group all-empty-or-reject policy must not leak into the single path)"
    - "Downstream parity (38-DOWNSTREAM-PARITY): save/reopen, live pixel caches, playback, onion/reference, preview/export, timeline extent, and interpolation all derive from the accepted map only — group paste publishes solely through the existing replace-roto-physical-map coordinator route, never the retired legacy key-frames apply path (MEMORY-RECALL wrong-transaction-path gotcha), and the anchor key landing on the launch startFrame loads its cached reference frame exactly like single paste"
    - "Paste availability stays shape-agnostic and PhysicsPaintWorkflowStrip.tsx is NOT modified by this plan (wave-2 file-conflict rule vs 38-05; RESEARCH Open Question 2 resolution — the resolver supplies reject reasons at activation time, feedback is controller-owned)"
  assumptions:
    - "D-15: this plan is PRE-UAT production. No test file is created, modified, deleted, renamed, or executed; vitest is never invoked. Verification is `pnpm --dir app typecheck` plus bounded static grep gates, matching the 38-01/38-02 precedent; live end-to-end proof is the user-owned native UAT checkpoint in plan 38-06."
    - "Plan 38-01 has already landed: the clipboard slot is the widened single|group union, isRotoSessionCopiedKeyGroup is exported from physicsPaintRotoSession.ts, and the hook's pasteKey contains the interim fail-closed narrowing guard whose body THIS plan replaces (interface-first ordering, not a deferred feature)."
    - "Plan 38-02 has already landed: createPhysicPaintRotoPasteKeyGroupIntent is exported from the resolver, allocates fresh keyIds ONCE, and the group candidate sets proposal.selectedKeyId to the earliest pasted key's fresh keyId."
    - "Adding a required pasteKeyGroup member to RotoPhysicalKeyUtilityPort is typecheck-safe repo-wide: no test file constructs or references the port (verified 2026-07-27 — zero test references to RotoPhysicalKeyUtilityPort or duplicateKey), and useRotoNavigationCoordinator forwards the whole port object opaquely."
  prohibitions:
    - "No test creation, modification, deletion, renaming, or execution — pre-UAT production only (D-15)"
    - "No per-key executePhysicalEdit loop: one intent, one proposal, one transaction, one accepted output, one history entry (RESEARCH Anti-Patterns)"
    - "No parallel publish path: no direct store mutation, no bespoke bridge call, no child-side canonical mutation before exact parent acceptance (MemPalace split-authority regression family)"
    - "Do not modify PhysicsPaintWorkflowStrip.tsx, PhysicsPaintStyledTooltip.tsx, physicsPaintStudio.css, or PhysicsPaintScriptsPanel.tsx — 38-05 owns them in this wave"
    - "Do not touch the single-key paste-key route, addEmptyKey, the toClipboardPayload path, session.copyKey, or the 38-01 group Copy branch (Pitfall 7)"
    - "No new failure-code vocabulary and no rewording of the three UI-SPEC locked copy lines"
    - "No sourceFrame/displayFrame compatibility, migration code, forwarding wrappers, aliases, dual-write paths, or second timing authority (D-16)"
    - "No deferred ideas: no group-aware Duplicate, no Copy/Paste keyboard shortcuts, no replace-style group paste (CONTEXT.md Deferred Ideas)"
  artifacts:
    - "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts — RotoPhysicalKeyUtilityPort gains the required pasteKeyGroup member (group entries in, Promise<boolean> out)"
    - "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — PhysicalActionRunnerInput gains 'paste-key-group' + optional rejectedCopy; runPhysicalAction failure branch consumes rejectedCopy and extends the publishDiagnostic leg; new pasteKeyGroup route; physicalKeyUtilities port object entry"
    - "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — pasteKey clipboard-shape branch: group clipboards route to physicalKeyUtilities.pasteKeyGroup; single clipboards keep the byte-identical toClipboardPayload path; the 38-01 interim narrowing guard is removed"
    - "app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts — resolvePostAcceptanceRotoSelection gains optional acceptedAddedKeyIds input + the 'paste-key-group' branch (pasted set selected, earliest pasted key anchor)"
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — consumeBridgeApplyResult computes acceptedAddedKeyIds from the before/after record diff and passes it to the aftermath reducer; createdSelectedDestination condition extended to 'paste-key-group' for cached-reference load parity"
  key_links:
    - "useRotoKeyUtilities.pasteKey group branch -> RotoPhysicalKeyUtilityPort.pasteKeyGroup -> createPhysicPaintRotoPasteKeyGroupIntent -> runPhysicalAction -> coordinator -> bridge replace-roto-physical-map -> acceptedOutput -> ONE history command (the single-authority seam; MEMORY-RECALL)"
    - "failure.code -> rejectedCopy mapping in the route -> capsule concise line; the same failure routes code + full text to input.publishDiagnostic -> LOG (36.14 D-26)"
    - "accepted.before.records vs accepted.after.records keyId diff (appFrame-ordered) -> acceptedAddedKeyIds -> resolvePostAcceptanceRotoSelection 'paste-key-group' branch -> { selectedKeyIds: pasted set, anchorKeyId: acceptedSelectedKeyId } (Pitfall 2)"
    - "38-02 proposal.selectedKeyId = earliest pasted fresh keyId -> accepted.after.selectedKeyId -> existing single-selection sync makes the earliest pasted key current and focus/scroll follow (37 D-06/D-17 pattern) — no new focus machinery"
---

<objective>
Activate group paste end-to-end and give it the right selection aftermath. Three wiring moves: (1) a `pasteKeyGroup` route on `useRotoTimelineActions` that builds the 38-02 frozen intent and runs it through the existing `runPhysicalAction`/coordinator/bridge seam with the UI-SPEC locked feedback copy; (2) the clipboard-shape branch in `useRotoKeyUtilities.pasteKey` so a group clipboard takes the new route while a single clipboard keeps today's byte-identical path; (3) a `paste-key-group` branch in `resolvePostAcceptanceRotoSelection` so the accepted pasted group becomes the selection with the earliest pasted key current. Requirements: 38-GROUP-PASTE (activation half), 38-DOWNSTREAM-PARITY.

Purpose: 38-01 built the group clipboard, 38-02 built the pure resolver/coordinator/bridge seam; this plan is the switch that connects them to the user's Paste action — without it the seam is dead code and the guard installed by 38-01 rejects every group paste. It is also the plan that prevents the accepted group from collapsing to a single selected key (Pitfall 2).

Output: with a group clipboard, Paste at an all-empty destination lands all keys at exact computed frames with fresh keyIds, one history entry, the group selected, `Pasted {N} keys` in the capsule; collision and range rejects show the locked concise lines with detail in LOG.

**D-15 gate:** PRE-UAT production. No test files, no vitest runs, no build, no server. Verification is `pnpm --dir app typecheck` plus bounded static grep gates; live end-to-end proof is the user-owned native UAT checkpoint in plan 38-06.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-MEMORY-RECALL.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-01-PLAN.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-02-PLAN.md

@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
@app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end group paste activation — port member + pasteKeyGroup route through runPhysicalAction + hook clipboard-shape branch</name>
  <files>app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts (lines 46-68: RotoPhysicalKeyUtilityPort — duplicateKey/pasteKey/addEmptyKey signatures and the boundary doc comment)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 220-246: publishStatus/publishDiagnostic input ports + PhysicalActionRunnerInput + success-message constants; lines 294-345: runPhysicalAction — guards, resolvePhysicPaintRotoPhysicalEdit call, the !resolution.ok branch at 326-332, executePhysicalEdit at 334-340, success publish at 341-343; lines 381-446: duplicateKey/pasteKey/addEmptyKey route shapes; lines 560-600: prepareRotoKeyGroupDrag failure-code → concise-copy mapping pattern; lines 740-760: physicalKeyUtilities useMemo port object + dep array)
    - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts (lines 150-206: blocked definition, duplicateKey/copyKey/pasteKey routes INCLUDING the 38-01 interim narrowing guard inside pasteKey; lines 246-255: toClipboardPayload)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (line 314-319: PhysicPaintRotoPhysicalEditFailure shape; the 38-02 export createPhysicPaintRotoPasteKeyGroupIntent and its entries parameter type)
    - app/src/components/physic-paint/roto/physicsPaintRotoSession.ts (the 38-01 exports: RotoSessionCopiedGroupEntry, isRotoSessionCopiedKeyGroup)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md (section 8: the route shape + reject-copy mapping analogs; Pitfall 7)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md (Copywriting Contract: the locked group paste copy table)
  </read_first>
  <action>
Wire the group paste path through the EXISTING single-authority seam (per D-04..D-07 and the 36.14-20 protocol). Three files, no new machinery. Do not modify any test file and do not run vitest (D-15). Do not touch PhysicsPaintWorkflowStrip.tsx or any 38-05 file.

1. rotoCoordinatorPorts.ts — extend the port contract:
   - Add `import type { RotoSessionCopiedGroupEntry } from './physicsPaintRotoSession';` (same directory, type-only; the session module is stable post-38-01).
   - Add the required member to `RotoPhysicalKeyUtilityPort`, placed after `pasteKey`: `pasteKeyGroup: (destinationAppFrame: number, entries: readonly RotoSessionCopiedGroupEntry[]) => Promise<boolean>;` with a doc comment stating it is the atomic all-empty-or-reject group variant (D-05) — it never replaces an existing key, unlike pasteKey's replace-style single behavior. This new required member is typecheck-safe: no test file constructs the port (verified at planning) and useRotoNavigationCoordinator forwards the whole object opaquely.

2. useRotoTimelineActions.ts — runner extension + the new route:
   a. Extend `PhysicalActionRunnerInput` (lines 229-234): add `'paste-key-group'` to the hand-written operationKind union, and add an optional field `readonly rejectedCopy?: (failure: PhysicPaintRotoPhysicalEditFailure) => string;`. Import `PhysicPaintRotoPhysicalEditFailure` as a type from the resolver module alongside the existing resolver type imports.
   b. In `runPhysicalAction`'s `!resolution.ok` branch (lines 326-332): publish `runnerInput.rejectedCopy?.(resolution.failure) ?? (resolution.failure.text || 'The Roto timeline edit is invalid.')` so existing routes without rejectedCopy keep byte-identical behavior; extend the publishDiagnostic leg so it fires for BOTH `'delete-key-group'` and `'paste-key-group'`, building the diagnostic string from `runnerInput.operationKind + ' rejected: ' + resolution.failure.code + ' — ' + resolution.failure.text` (keep the existing delete-key-group string identical by this construction — full detail to LOG only, 36.14 D-26, T-38-02).
   c. Import `createPhysicPaintRotoPasteKeyGroupIntent` from the resolver (38-02 export) and `RotoSessionCopiedGroupEntry` as a type from '../roto/physicsPaintRotoSession'.
   d. Add the `pasteKeyGroup` route beside `pasteKey` (lines 394-422), mirroring its structure exactly:
      - Guard: if destinationAppFrame is not a nonnegative integer, publish 'Select a valid Roto frame before pasting.' (same line as the single route) and resolve false.
      - Build the intent inside try/catch via `createPhysicPaintRotoPasteKeyGroupIntent(destinationAppFrame, entries)`; on throw publish 'The copied Roto key group is unavailable.' (the single route's catch copy, pluralized) and resolve false. The factory is the fail-closed gate: it throws on fewer than two entries or malformed entry fields (T-38-01).
      - On successful intent construction, FIRST publish the busy line `Pasting keys…` (UI-SPEC locked, ellipsis character U+2026 — it shows only while the acknowledged transaction is pending and is always overwritten by the success or reject line), THEN return `runPhysicalAction({ intent, operationKind: 'paste-key-group', requiredKeyId: null, successMessage: `Pasted ${entries.length} keys`, rejectedCopy: <mapping> })`.
      - `requiredKeyId: null` per the delete-key-group precedent (line 364-371): the resolver is the destination-occupancy authority — every computed destination must be empty, so no existing keyId is required.
      - successMessage interpolates N at route-call time, so runPhysicalAction's fixed-string contract is unchanged; the line is verbatim `Pasted {N} keys` with no trailing period (UI-SPEC locked).
      - rejectedCopy maps the existing failure codes to the UI-SPEC locked lines, mirroring the prepareRotoKeyGroupDrag pattern (lines 572-583): code `duplicate-destination-frame` → `Paste rejected — key in the way`; code `over-capacity` or `out-of-range-frame` → `Paste rejected — not enough room`; anything else → `failure.text || 'The Roto key group paste is invalid.'` (em-dash U+2014 in both locked lines, matching the existing Move-rejected copy style).
   e. Add `pasteKeyGroup` to the `physicalKeyUtilities` useMemo object (lines 745-749) and to its dependency array.
   f. Keep the existing `pasteKey` route (lines 394-422), `addEmptyKey` (lines 424-446), `PASTE_SUCCESS_MESSAGE`, and every other route byte-identical (Pitfall 7 — single paste is replace-style and is reused by addEmptyKey and the script-target promotion path; the group all-empty-or-reject policy must not leak in).

3. useRotoKeyUtilities.ts — replace the interim guard with the real branch:
   - In `pasteKey` (lines 180-206 post-38-01), find the `isRotoSessionCopiedKeyGroup(copiedKey)` narrowing guard 38-01 installed. DELETE its interim message string entirely and replace the branch body with the real activation: `setKeyActionInFlight(true);` then `void input.physicalKeyUtilities.pasteKeyGroup(input.currentFrame, copiedKey.entries)` with a `.catch` mirroring the single path's catch shape but with the message 'Could not paste the copied Roto key group.' (setApplyStatus('error') + setApplyMessage + setLastError(detail)) and `.finally(() => { setKeyActionInFlight(false); })`, then `return;`.
   - The destination is `input.currentFrame` per D-07 — Paste targets the current editing cell, including while a multi-selection is active. No currentKeyId is passed (group destinations must be empty; an occupied current cell rejects via the resolver with the locked collision line).
   - Everything else in the function stays byte-identical: the `blocked` guard, the shape-agnostic `canPaste`/`pasteDisabledReason` guard (RESEARCH Open Question 2 resolution — availability does NOT branch on clipboard shape), the null check, and the single-key `toClipboardPayload` path below the branch (Pitfall 7).

Preact discipline (CLAUDE.md): no new useEffect, no new signals — the route reuses runPhysicalAction and the hook reuses keyActionInFlight exactly as today.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app typecheck && grep -c "pasteKeyGroup" app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts && grep -c "pasteKeyGroup" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "pasteKeyGroup" app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts && grep -c "'paste-key-group'" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -F -c 'Pasted ${entries.length} keys' app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -F -c "Paste rejected — key in the way" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -F -c "Paste rejected — not enough room" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -F -c "Pasting keys…" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "rejectedCopy" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "input.executePhysicalEdit" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "executePhysicalEdit" app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts && grep -F -c "successMessage: PASTE_SUCCESS_MESSAGE" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -F -c "const clipboardPayload = toClipboardPayload(copiedKey);" app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts && grep -c "createPhysicPaintRotoPasteKeyIntent" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts</automated>
    Expected: typecheck exits 0; pasteKeyGroup counts >= 1 in the port file, >= 3 in useRotoTimelineActions.ts (route + port object entry + useMemo dep), >= 1 in useRotoKeyUtilities.ts; 'paste-key-group' literal >= 2 in useRotoTimelineActions.ts (runner union + route); each locked-copy grep == 1; rejectedCopy count >= 3 (field declaration, runner consumption, route argument); input.executePhysicalEdit == 1 (the one existing call inside runPhysicalAction — no per-key loop); executePhysicalEdit == 0 in the hook (the controller never calls the coordinator directly); the three single-path byte-identity greps each == 1. NO vitest invocation (D-15).
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app typecheck` passes across all three files with the new required port member.
    - `grep -F -c "cannot paste through the single" app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts` returns 0 (the 38-01 interim guard is fully removed, replaced by the real route call).
    - `grep -c "replace-roto-key-frames" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` and the same for useRotoKeyUtilities.ts each return 0 (no publish through the retired legacy key-frames path — MEMORY-RECALL wrong-transaction-path gotcha).
    - The route publishes `Pasting keys…` only after the factory succeeds and before the awaited transaction; the success and reject lines always overwrite it.
    - Reject mapping: duplicate-destination-frame → `Paste rejected — key in the way`; over-capacity/out-of-range-frame → `Paste rejected — not enough room`; fallback preserves resolver text; the publishDiagnostic leg carries code + full text for the group kind.
    - The single-key paste route, addEmptyKey, PASTE_SUCCESS_MESSAGE, and the hook's toClipboardPayload path are byte-identical (grep gates above).
    - One runPhysicalAction call per paste — no per-key execute loop anywhere.
    - No test file created, modified, or executed; no vitest run (D-15); PhysicsPaintWorkflowStrip.tsx untouched.
  </acceptance_criteria>
  <done>The full activation path compiles: group clipboard → hook shape branch → port → factory → runPhysicalAction → coordinator → bridge, with the UI-SPEC locked busy/success/reject copy, concise-capsule + LOG-detail split, and the single-key path byte-identical. Live behavior proof (paste lands all keys, one Undo/Redo, locked copy) is deferred to the 38-06 user-owned UAT per D-15.</done>
  <reversibility rating="reversible">Route + port + branch-body additions on the existing seam; no protocol, acknowledgement-shape, or history-contract change — reverting removes the branch and route without disturbing single paste.</reversibility>
</task>

<task type="auto">
  <name>Selection aftermath — paste-key-group branch in resolvePostAcceptanceRotoSelection + Studio acceptedAddedKeyIds wiring + cached-reference load parity</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts (lines 129-156: resolvePostAcceptanceRotoSelection — the move-key-group and force-spacing branches and the default collapse at line 154; lines 40-60: collapseRotoKeySelection and RotoKeySelectionState shape)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 827-866: consumeBridgeApplyResult — the accepted-block selection aftermath call at 836-843 and the createdSelectedDestination cached-reference load at 849-864; line 8: the multiSelection import list)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md (section 7: the aftermath reducer analog; Pitfall 2)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md (D-07 + the adopted selection-aftermath discretion mirroring 37 D-06/D-17)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-02-PLAN.md (the group candidate sets proposal.selectedKeyId to the earliest pasted key's fresh keyId)
  </read_first>
  <action>
Close Pitfall 2: without a dedicated branch, an accepted group paste collapses the multi-selection to the single accepted keyId (the reducer's default). Two files.

1. physicsPaintRotoMultiSelection.ts — add the branch:
   - Extend the reducer's input type with an optional field `readonly acceptedAddedKeyIds?: readonly string[];` (keyIds present in the accepted after-map but absent from the before-map, appFrame-ordered — computed by the caller).
   - Add a branch BEFORE the default collapse: when `operationKind === 'paste-key-group'` and `acceptedAddedKeyIds` is present with length > 0, return `{ selectedKeyIds: [...acceptedAddedKeyIds], anchorKeyId: acceptedSelectedKeyId }`. This mirrors the move-key-group branch shape exactly: the pasted set becomes the selection and the anchor is the accepted selectedKeyId — which the 38-02 candidate sets to the EARLIEST pasted key's fresh keyId, so the earliest pasted key is the current editing key per the adopted discretion (37 D-06/D-17 pattern). The operationKind parameter stays a plain string by design (line 142) — the branch is behavior, not a type change.
   - Defensive fallback: when `acceptedAddedKeyIds` is absent or empty for the group kind, fall THROUGH to the existing default collapse (fail-safe, never throws, never fabricates a set).
   - Update the doc comment (lines 129-140) with a `paste-key-group` bullet describing the new branch; keep every existing branch byte-identical.

2. PhysicsPaintStudio.tsx — wire the input and extend reference-load parity (inside `consumeBridgeApplyResult`, lines 827-866):
   - In the `transition === 'accepted' && accepted` block (lines 831-844), compute the added keyIds from the accepted output BEFORE calling the reducer: build a Set of `accepted.before.records` keyIds, then `acceptedAddedKeyIds = accepted.after.records.filter((record) => !beforeKeyIds.has(record.keyId)).map((record) => record.keyId)`. after.records is appFrame-sorted by resolver contract, so the list is ordered earliest-first — no additional sort.
   - Pass `acceptedAddedKeyIds` as a new field in the `resolvePostAcceptanceRotoSelection({...})` call (line 836-841). The reducer ignores it for every other operation kind.
   - Extend `createdSelectedDestination` (lines 849-853): change the operationKind condition from `'paste-key'` only to `'paste-key' || 'paste-key-group'`, so when the pasted group's ANCHOR (the accepted selectedKeyId, a fresh key absent from before.records and present at selectedAppFrame in after.records) lands on the launch startFrame, the existing `loadCachedRotoReferenceFrame(selectedAppFrame, ...)` fires exactly as it does for single paste. This is the reference/onion half of 38-DOWNSTREAM-PARITY — the pasted paint on the current cell becomes visible through the same accepted-map-derived path; every other conjunct in the condition stays byte-identical.
   - Update the D-17 comment (lines 832-835) to mention that paste-key-group selects the pasted set with the earliest pasted key as anchor.
   - No other Studio changes: focus and minimal scroll already follow `accepted.after.selectedKeyId` through the existing accepted-selection sync (that is how move-key-group's anchor becomes current today), so no new focus machinery is added; the 38-01 keyUtilities port block and everything else in the file stay untouched.

Preact discipline (CLAUDE.md): the diff computation is plain render-scope logic inside an existing callback — no new signals, effects, or refs.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app typecheck && grep -c "paste-key-group" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts && grep -c "acceptedAddedKeyIds" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts && grep -c "acceptedAddedKeyIds" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -c "resolvePostAcceptanceRotoSelection" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -c "collapseRotoKeySelection" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts && grep -F -c "operationKind === 'move-key-group'" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts && grep -F -c "operationKind === 'force-spacing'" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts</automated>
    Expected: typecheck exits 0; 'paste-key-group' >= 2 in the reducer (doc bullet + branch); acceptedAddedKeyIds >= 2 in the reducer (input field + branch read) and >= 2 in Studio (computation + call-site field); resolvePostAcceptanceRotoSelection count in Studio unchanged from baseline (1 import + 1 call — extended, not duplicated); collapseRotoKeySelection count in the reducer unchanged from baseline (default branch preserved); the move-key-group and force-spacing branch greps each == 1 (existing branches byte-identical). NO vitest invocation (D-15).
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app typecheck` passes across both files.
    - `grep -F -c "'paste-key'" app/src/components/physic-paint/PhysicsPaintStudio.tsx` returns at least 2 (the unchanged script-promotion check plus the widened createdSelectedDestination condition) and `grep -F -c "'paste-key-group'" app/src/components/physic-paint/PhysicsPaintStudio.tsx` returns at least 1 (the widened condition).
    - The paste-key-group branch returns the full pasted set as selectedKeyIds with the accepted selectedKeyId as anchor; absent/empty acceptedAddedKeyIds falls through to the default collapse.
    - acceptedAddedKeyIds derives ONLY from the accepted before/after record keyId diff — no store reads, no session reads, no new authority (38-DOWNSTREAM-PARITY).
    - createdSelectedDestination's remaining conjuncts (fresh keyId absent from before, present in after at selectedAppFrame, launch/operation matching) are byte-identical; the single-paste reference-load behavior is unchanged.
    - Existing move-key-group, force-spacing, and default-collapse branches byte-identical (grep gates above).
    - No test file created, modified, or executed; no vitest run (D-15).
  </acceptance_criteria>
  <done>An accepted group paste selects the whole pasted group with the earliest pasted key as anchor/current via the existing sync, focus/scroll follow the anchor cell, the anchor landing on the launch startFrame loads its cached reference frame exactly like single paste, and every other operation kind's aftermath is byte-identical; typecheck green; live selection behavior is deferred to the 38-06 user-owned UAT per D-15.</done>
  <reversibility rating="reversible">One additive reducer branch + one additive call-site field + one widened condition; reverting restores the collapse default without touching other kinds.</reversibility>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Location | Consumer |
|----------|------|----------|----------|
| `RotoPhysicalKeyUtilityPort.pasteKeyGroup(destinationAppFrame, entries)` | new required port member | `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` | `useRotoKeyUtilities.pasteKey` group branch |
| `pasteKeyGroup` route | new physical action route | `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` (beside `pasteKey` :394-422) | the port object; the hook |
| `PhysicalActionRunnerInput.rejectedCopy` + `'paste-key-group'` union member | modified runner input | same file (:229-234) | `runPhysicalAction` failure branch |
| Feedback constants: `Pasted {N} keys` (interpolated), `Pasting keys…`, `Paste rejected — key in the way`, `Paste rejected — not enough room`, `The copied Roto key group is unavailable.`, `Could not paste the copied Roto key group.` | UI-SPEC locked copy | route + hook group branch | status capsule (concise) / LOG (detail via publishDiagnostic) |
| Hook clipboard-shape branch (group → `pasteKeyGroup`, single → byte-identical `toClipboardPayload` path) | modified branch body | `app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts` (`pasteKey`) | the strip's existing Paste intent |
| `resolvePostAcceptanceRotoSelection` `'paste-key-group'` branch + `acceptedAddedKeyIds` input | modified pure reducer | `app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` (:141-155) | Studio `consumeBridgeApplyResult` |
| Studio `acceptedAddedKeyIds` before/after diff + widened `createdSelectedDestination` condition | modified wiring | `app/src/components/physic-paint/PhysicsPaintStudio.tsx` (:827-866) | selection aftermath + cached-reference load parity |

Explicitly NOT produced here: the group clipboard slot and group Copy (38-01), the resolver/coordinator/bridge seam (38-02), the strip/tooltip surfaces (38-03/38-05), UAT (38-06), and any test file (38-07, post-UAT per D-15).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view -> controller | The strip forwards ONE Paste intent; clipboard-shape branching lives in the hook (UI-SPEC Preact and State Ownership Contract) — the view is never trusted to pick paste semantics |
| controller -> resolver | Clipboard entries cross into the intent factory; untrusted until the factory's fail-closed validation passes |
| child -> parent bridge | The group semantic delta crosses the process boundary; the parent's 38-02 revalidation is authoritative — this plan adds NO new crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering (V5 input validation) | `pasteKeyGroup` route + intent factory call | high | mitigate | Fail-closed: destination guard rejects non-integer/negative frames before the factory; the factory throws on fewer than two entries or malformed entries and the catch publishes a fixed concise line; the resolver's existing codes drive the reject mapping — no new code vocabulary (Task 1 step 2d) |
| T-38-02 | Information Disclosure (V7 error/logging) | reject/busy/success copy paths | medium | mitigate | Capsule carries only the four fixed concise lines (36.14 D-26); failure code + full resolver text route exclusively to `publishDiagnostic` → LOG; no keyIds, offsets, or capacity internals in user-facing lines (Task 1 steps 2b/2d) |
| T-38-03 | Tampering (V13 bridge / split authority) | group paste publish path | high | mitigate | One route only: hook → port → factory → runPhysicalAction → coordinator → replace-roto-physical-map → acceptedOutput → history; static gates forbid a per-key execute loop, any direct coordinator call from the hook, and any use of the retired legacy key-frames publish path (Task 1 acceptance criteria; MEMORY-RECALL) |
| T-38-SC | Tampering | npm/pnpm installs | high | mitigate | Vacuous — no new packages (38-RESEARCH.md Package Legitimacy Audit); no install tasks exist in this plan |
</threat_model>

<verification>
Plan-level checks (all bounded static, pre-UAT per D-15; no vitest, no build, no dev server):

1. `pnpm --dir app typecheck` — green across all five touched files with the new required port member (safe repo-wide: no test constructs the port, verified at planning).
2. Five-file scope gate: `git status --porcelain -- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx app/src/components/physic-paint/physicsPaintStudio.css` returns EMPTY (wave-2 file-conflict rule vs 38-05; the strip stays untouched).
3. Activation gates (Task 1): locked copy lines each present exactly once; `'paste-key-group'` literal >= 2 in useRotoTimelineActions.ts; the 38-01 interim guard fully removed; single-paste tokens byte-identical.
4. Aftermath gates (Task 2): reducer branch + Studio wiring present; existing aftermath branches byte-identical.
5. Authority hygiene: exactly one `input.executePhysicalEdit` call site (inside runPhysicalAction); zero `executePhysicalEdit` references in the hook; zero references to the retired legacy key-frames publish path in both touched hook files (38-DOWNSTREAM-PARITY — downstream systems derive from the accepted map only).
6. Requirement coverage: 38-GROUP-PASTE (Task 1 activation + Task 2 aftermath), 38-DOWNSTREAM-PARITY (Task 1 single-route gates + Task 2 reference-load parity).
7. Live end-to-end behavior (group paste lands all keys with fresh keyIds, one Undo/Redo, group selected, locked capsule copy, single-key paths unchanged) is verified by the user-owned native UAT checkpoint in plan 38-06 — the agent does not launch the server/app (CLAUDE.md).

Wave-2 coexistence check: this plan touches ONLY rotoCoordinatorPorts.ts, useRotoTimelineActions.ts, useRotoKeyUtilities.ts, physicsPaintRotoMultiSelection.ts, and PhysicsPaintStudio.tsx — zero overlap with 38-05 (tooltip component, CSS, strip, scripts panel), so both run in parallel. Overlap with wave-1 plans (useRotoKeyUtilities.ts, PhysicsPaintStudio.tsx) is sequentialized by depends_on [38-01, 38-02].
</verification>

<success_criteria>
- Group paste activates end-to-end: group clipboard → hook shape branch → port → frozen 38-02 intent → runPhysicalAction → coordinator → bridge → acceptedOutput → ONE history command (D-07 by construction).
- UI-SPEC locked copy is exact: `Pasting keys…` while pending, `Pasted {N} keys` on accept, `Paste rejected — key in the way` / `Paste rejected — not enough room` on atomic reject, full detail in LOG only (D-05/D-06, 36.14 D-26).
- The accepted pasted group becomes the selection with the earliest pasted key current; focus/scroll follow via the existing sync (Pitfall 2 closed); the anchor on the launch startFrame loads its cached reference frame exactly like single paste (38-DOWNSTREAM-PARITY).
- Single-key Copy/Paste, addEmptyKey, and the script-target promotion path are byte-identical (Pitfall 7); availability stays shape-agnostic; the strip is untouched.
- Typecheck green; zero vitest invocations and zero test files touched (D-15); no deferred ideas implemented.
</success_criteria>

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-04-SUMMARY.md` when done, recording: the final rejectedCopy mapping construction, confirmation that the 38-01 interim guard was fully removed, the grep gate outputs (including the empty strip/CSS porcelain gate), and confirmation that no new publish path or per-key execute loop was introduced (38-DOWNSTREAM-PARITY evidence for the 38-06 UAT checklist).
</output>
