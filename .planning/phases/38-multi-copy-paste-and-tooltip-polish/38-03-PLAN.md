---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
autonomous: true
requirements:
  - 38-CAPSULE-IDLE-CONTEXT

must_haves:
  truths:
    - "Static capsule baseline deleted (D-08): the capsule never shows a missing-frame line as idle filler, in any form."
    - "Idle capsule shows the current-cell context line per the UI-SPEC locked mapping: real → `Real Roto key · Frame {n}`, generated → `Generated frame · Frame {n}`, empty → `Empty frame · Frame {n}` (D-09)."
    - "The idle context line follows navigation/selection: it re-derives from the current cell on every render with no new controller state and no effects (D-09, Preact state ownership contract)."
    - "Missing-frame information is event-driven only: it appears via the existing playback/export status surfaces in `useRotoCachedPlayback`, never as permanent idle filler (D-10)."
    - "Capsule arbitration priority is unchanged: pending operation > saving indicator > guard/action feedback > idle current-cell context (new lowest rung replacing the deleted baseline)."
    - "The idle context line renders in the standard neutral capsule styling — one line, ellipsis truncation, no accent glyph, no emphasis change (UI-SPEC Visual State Contract)."
    - "The detailed missing-frame explanation stays in LOG/diagnostics per 36.14 D-26; capsule strings stay concise (T-38-02)."
    - "All capsule content renders as Preact text children only — controller strings are never injected as HTML (T-38-04 / T-36.15-08)."
  artifacts:
    - "app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — baseline constant and fallback removed; new exported pure idle-context helper."
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — capsule selector call feeds the `ambient` slot from `currentSemanticCell` + `props.currentFrame`."
    - "app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts — event-driven missing-frame wording reviewed (kept or tightened; never removed)."
  key_links:
    - "`getRotoStatusCapsuleViewModel` final fallback returns the supplied ambient line or an empty string — no static constant anywhere in the chain."
    - "Strip capsule wiring at PhysicsPaintWorkflowStrip.tsx:451-459 passes `ambient` derived from `currentSemanticCell` (computed at :351) — selector stays pure; the strip supplies already-resolved strings."
---

<objective>
Fix the Physics Paint Roto status capsule per D-08/D-09/D-10: delete the static `ROTO_STATUS_CAPSULE_BASELINE` idle filler and its fallback in `getRotoStatusCapsuleViewModel`, and feed the selector's existing (currently unused) `ambient` input slot with a current-cell context line derived in the strip from `currentSemanticCell` + `props.currentFrame` using the UI-SPEC locked mapping. Missing-frame information remains event-driven only through the existing `useRotoCachedPlayback` status lines (wording review only, D-10).

Purpose: The user found the permanent baseline message meaningless ("I don't know what is this fuck info and when to show it!") — the capsule must only ever say something tied to real state. The idle rung becomes a live current-cell descriptor instead of static filler.

Output: Selector without the baseline constant/fallback, strip feeding the `ambient` slot, reviewed playback wording. No test files touched (D-15; known-red `physicsPaintWorkflowPresentation.test.ts:159-225` is expected between this plan and 38-08 — Pitfall 5).

Tracer slice (per approved outline): idle on a real key shows `Real Roto key · Frame 5`; navigate to an empty cell shows `Empty frame · Frame 7`; the baseline string is gone from production source.

Note (MVP mode): the ROADMAP Phase 38 Goal line is not in user-story format; recorded in the approved outline — surface to the user after planning rather than inventing a story.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md

@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end capsule idle context — delete baseline, feed `ambient` from the strip (D-08, D-09)</name>
  <files>app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts:155-219 — capsule selector section: header comment (:155-168), `ROTO_STATUS_CAPSULE_BASELINE` export (:170), `RotoStatusCapsuleInput.ambient` (:190-191), `getRotoStatusCapsuleViewModel` body with the `ambient ?? ROTO_STATUS_CAPSULE_BASELINE` fallback at :212, `trimCapsuleLine` (:215-219).
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:351 — `const currentSemanticCell = physicalCellByAppFrame.get(props.currentFrame) ?? null;` (kind is `'real' | 'generated' | 'empty'`); :442-459 — capsule wiring comment block and the `getRotoStatusCapsuleViewModel({ pendingOperation, savingIndicator, feedback })` call that currently omits `ambient`; :961-971 — capsule mount (unchanged by this task).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md — "Status Capsule Idle Context" mapping table and "Status and LOG Routing" arbitration rule.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md §10 (selector pattern) and §11 (strip capsule wiring).
  </read_first>
  <behavior>
    - Idle on a real Roto key cell: capsule shows `Real Roto key · Frame {n}` with the physical appFrame verbatim.
    - Idle on a generated interpolation cell: capsule shows `Generated frame · Frame {n}`.
    - Idle on an empty cell: capsule shows `Empty frame · Frame {n}`.
    - When a pending operation, saving indicator, or feedback line exists, the idle context line never wins (priority unchanged).
    - When the ambient input is absent/blank and nothing higher-priority exists, the selector returns an empty string — never a static filler line.
  </behavior>
  <action>
    In `physicsPaintWorkflowPresentation.ts` (D-08): delete the exported baseline constant at :170 and change the selector's final fallback at :212 so it returns the trimmed ambient line or an empty string (`return ambient ?? '';`). Update the section header comment (:155-168) so it describes the new lowest rung as the caller-supplied current-cell context — do NOT quote the deleted baseline message text anywhere in this file, including comments (a negative gate greps for it). Keep the rest of the priority grammar byte-identical (pending > saving > feedback, most-recent-wins inside feedback). Do not rename `getRotoStatusCapsuleViewModel`, `RotoStatusCapsuleInput`, or the `ambient` slot — the signature is unchanged.

    In the same file, add one new exported pure helper (D-09; exact symbol name is planner discretion — use `getRotoStatusCapsuleIdleContext`): input `{ cellKind: 'real' | 'generated' | 'empty' | null, frame: number }`, output `string | null`. It implements the UI-SPEC locked mapping exactly: `'real'` → `Real Roto key · Frame {frame}`; `'generated'` → `Generated frame · Frame {frame}`; `'empty'` → `Empty frame · Frame {frame}`; `null` → `null`. Use the middle-dot separator `·` exactly as locked. Frame passes through verbatim (physical appFrame; it is already a nonnegative integer at the call site — do not re-clamp or re-derive). The helper stays pure: no store/signal/props reads, returns display strings only (T-36.15-08).

    In `PhysicsPaintWorkflowStrip.tsx` (D-09): in the existing `getRotoStatusCapsuleViewModel` call at :451-459, add the `ambient` property fed by the new helper with `currentSemanticCell?.kind ?? null` (already computed at :351) and `props.currentFrame`. This is render-time direct derivation from existing ports — no new controller state, no signal, no `useMemo`/`useEffect` added (the :442-447 comment documents this rule; extend that comment to mention the ambient feed). Do not touch the capsule mount at :961-971, the `pendingOperation`/`savingIndicator`/`feedback` wiring, or the tooltip `placement="below"` prop (38-05 owns tooltip placement). Do not touch `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` — its baseline assertions at :159-225 stay knowingly red until 38-08 (D-15, Pitfall 5).

    Before editing, grep the repo for other importers of the deleted constant (`grep -rn "ROTO_STATUS_CAPSULE_BASELINE" app/src --include='*.ts' --include='*.tsx'`). Expected references: the presentation file itself and the presentation test file only. If any other production file imports it, stop and report — do not improvise a replacement.
  </action>
  <verify>
    <automated>grep -c "Missing frames play transparent" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts | grep -q '^0$' && grep -c "getRotoStatusCapsuleIdleContext" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -qv ':0$' && grep -c "ambient:" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^1$'</automated>
    <human-check>After the user's native run (UAT is 38-06): idle on real key frame 5 shows `Real Roto key · Frame 5`; navigate to an empty cell shows `Empty frame · Frame 7`; no permanent missing-frame line ever appears while idle.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Missing frames play transparent" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` returns exactly 0 (constant, fallback, and comments all gone — region-scoped to this file only; `useRotoCachedPlayback.ts` legitimately keeps its event-driven lines).
    - The selector's last line returns the trimmed ambient line or `''`; arbitration order pending > saving > feedback > ambient is byte-identical above that line.
    - The new exported helper exists in `physicsPaintWorkflowPresentation.ts` and maps real/generated/empty to the exact locked strings with the `·` separator.
    - The strip's capsule selector call passes `ambient:` exactly once, derived from `currentSemanticCell?.kind ?? null` and `props.currentFrame`, with no new hook or effect.
    - `physicsPaintWorkflowPresentation.test.ts` is unmodified (D-15).
  </acceptance_criteria>
  <done>The baseline constant and fallback are deleted from production source, the strip feeds the idle context through `ambient`, all grep gates pass, and no test file was created, modified, renamed, or executed. State is "automated-ready" — native visible UAT happens in 38-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Missing-frame wording review — event-driven surface only (D-10)</name>
  <files>app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts:87-128 — playback status emissions: empty-cache start guard (:91), playing status with missing-frame count (:101-103), stopped status (:124).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md D-10 and "Specifics" (missing-frame info is event-driven only; detail in LOG per 36.14 D-26).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md "Status Capsule Idle Context" rules and "Status and LOG Routing".
  </read_first>
  <action>
    Review every missing-frame status string in `useRotoCachedPlayback.ts` against the new capsule policy (D-10): each emission must be tied to an explicit playback event (start / active playback / stop), never emitted as idle state. The three existing sites (:91, :101-103, :124) are all event-driven by construction — confirm this and keep them as the sanctioned event-driven surface.

    Wording tightening is allowed but minimal: the playback lines at :91 and :102-103 append the same explanatory clause that the deleted baseline carried. That clause is acceptable here ONLY because these lines appear during an explicit playback action (factually relevant, D-10). If any line reads as a permanent state claim rather than a playback-scoped fact, tighten it to name the playback context (e.g. keep the count and scope the clause to "during cached playback"). Do not remove missing-frame information from playback lines entirely, do not add any new missing-frame surface anywhere else in the strip or presentation layer, and do not route extra detail to the capsule — detailed diagnostics belong in LOG per 36.14 D-26 (add a `logDiagnostic`-style detail only if an existing diagnostic port is already wired in this hook; otherwise leave LOG routing unchanged).

    No other file changes in this task. No test changes (D-15).
  </action>
  <verify>
    <automated>grep -n "Missing frames play transparent" app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts | grep -cv "^\s*//" && grep -rn "Missing frames play transparent" app/src/components/physic-paint --include='*.ts' --include='*.tsx' | grep -v "useRotoCachedPlayback.ts" | grep -v ".test." | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <acceptance_criteria>
    - Every missing-frame status string in the app lives only in `useRotoCachedPlayback.ts` (production, non-comment, non-test) — the repo-wide grep above returns 0 outside that file.
    - Each remaining emission is provably event-driven: it fires from `start`, the playing-status branch, or the stop path — none fire on idle render.
    - The file's emissions still include the missing-frame count line during playback (the event-driven surface D-10 preserves); wording is unchanged or tightened, never deleted.
  </acceptance_criteria>
  <done>The review is complete, any needed wording tightening is applied, and the grep sweep proves no production file outside `useRotoCachedPlayback.ts` emits missing-frame text. No test file touched.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

- **New exported pure helper (symbol):** `getRotoStatusCapsuleIdleContext({ cellKind, frame })` in `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` — implements the UI-SPEC locked current-cell context mapping (`real`/`generated`/`empty` → locked strings, `null` → `null`). Consumed by `PhysicsPaintWorkflowStrip.tsx`; 38-08's post-UAT test updates target this helper and the selector.
- **New selector input usage:** the pre-existing `RotoStatusCapsuleInput.ambient?: string | null` slot (previously unused by the strip) is now fed at `PhysicsPaintWorkflowStrip.tsx:451-459`. No signature change.
- **Deleted symbols:** `ROTO_STATUS_CAPSULE_BASELINE` export and the `ambient ?? <baseline>` fallback — must never be reintroduced (D-08; UI-SPEC Validation Notes).
- **Downstream consumers:** 38-05 (tooltip rework) restacks the capsule mount's tooltip placement but must not alter this ambient feed; 38-08 (post-UAT tests) rewrites `physicsPaintWorkflowPresentation.test.ts:159-225` against the idle-context contract.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view | Resolved status/context strings cross from hooks/selectors into Preact rendering as text. |
| capsule ↔ LOG | User-visible concise line vs. internal diagnostic detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-02 | Information disclosure | `getRotoStatusCapsuleViewModel` / capsule mount | low | mitigate | Capsule shows only concise fixed-shape context strings; missing-frame detail stays in LOG/diagnostics per 36.14 D-26 (Task 2 review enforces no new capsule detail surface). |
| T-38-04 | Tampering | `physicsPaintWorkflowPresentation.ts`, `PhysicsPaintWorkflowStrip.tsx` capsule render | medium | mitigate | All capsule/idle-context strings render as Preact text children only (T-36.15-08); selector/helper return plain strings, never HTML; no `dangerouslySetInnerHTML` introduced. |
| T-38-SC | Tampering | package installs | high | mitigate | Vacuous — no new packages this plan (Package Legitimacy Audit satisfied vacuously per 38-RESEARCH.md). |
</threat_model>

<verification>
- Baseline elimination: `grep -c "Missing frames play transparent" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` → 0.
- Repo-wide surface audit: `grep -rn "Missing frames play transparent" app/src/components/physic-paint --include='*.ts' --include='*.tsx' | grep -v "useRotoCachedPlayback.ts" | grep -v ".test."` → no output.
- Idle-context wiring: `grep -c "ambient:" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` → 1; helper symbol present in both files.
- D-15 discipline: `git diff --name-only` shows no `*.test.ts` changes; no `vitest` invocation in this plan (tests are 38-07/38-08, post-UAT).
- Full typecheck/build gate is deferred to 38-08 per the D-15 sequence; this plan is "automated-ready", not "done" — native visible UAT is 38-06.
</verification>

<success_criteria>
- The capsule can never render the deleted static baseline; idle renders the current-cell context line from the UI-SPEC locked mapping and nothing else.
- Arbitration priority pending > saving > feedback > idle context is preserved byte-for-byte above the new fallback.
- Missing-frame information exists only as event-driven playback status in `useRotoCachedPlayback.ts`.
- Zero test files created, modified, renamed, or executed; the known-red baseline assertions in `physicsPaintWorkflowPresentation.test.ts:159-225` are untouched (Pitfall 5).
</success_criteria>

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-03-SUMMARY.md` when done.
</output>
