---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 05
type: execute
wave: 2
depends_on: [38-03]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
autonomous: true
requirements:
  - 38-TOOLTIP-VIEWPORT-PLACEMENT
  - 38-TOOLTIP-NOTCH-MULTILINE

must_haves:
  truths:
    - "Opposite-of-element-position placement for every styled tooltip (D-11): bottom-of-UI mounts (bottom action row, per-cell) render above; top-of-UI mounts (header capsule, interpolation, close) render below; right-edge mounts (Scripts panel) render left; left-edge mounts render right — computed by one shared utility, never per-mount hand placement."
    - "Tooltips are viewport-positioned (D-12): coordinates come from the anchor's getBoundingClientRect() at show time, so the pill stays correct under the strip's horizontal scroll and is never clipped by the strip shell's overflow; the 36.15 Gap B visual outcome for header controls is preserved (they still show below)."
    - "Every tooltip flips to the opposite side when the preferred side lacks room, then clamps inside the viewport with the locked 8px margin on all sides (UI-SPEC)."
    - "Directional notch (D-13): a 10px base x 6px height triangle on the control side, same #20262D fill as the pill, flipping direction with placement, centered on the source control — its offset tracks the anchor center after clamping, not the pill center."
    - "Multiline clamp (D-14): tooltip text wraps with max-width 280px and max-height 96px (8 lines at 10px/1.2), no internal scroll, no single-line truncation; overflow beyond the clamp is a copy defect, never a scrollbar."
    - "The useStyledTooltip controller contract is untouched (D-14): exactly 1000ms hover delay, instant show on keyboard focus, Escape hides with the listener registered only while visible, idempotent cleanup — Phase 37 Escape collapse-selection ordering preserved (Pitfall 8)."
    - "Tooltip content remains Preact text children only — controller strings are never injected as HTML (T-38-04 / T-36.15-01)."
    - "The id / aria-describedby association between each guarded control and its tooltip survives viewport positioning — the wiring moves with the content (T-38-05, UI-SPEC Accessibility contract)."
    - "Compact timeline geometry is unchanged: 161px strip bands, 34px action row, 18px cells, synchronized horizontal scroll — no mount wrapper gains layout-affecting markup."
    - "Tooltip multiline: text wraps at max-width 280px, clamped at max-height 96px (8 lines), no internal scroll, no single-line truncation; overflow is a copy defect, not a scrollbar (D-14) — UI Considerations covered row."
    - "Guarded Paste tooltip reason: the verbatim controller unavailability reason wraps multiline under the D-14 tooltip contract (280x96px clamp, no truncation, no scroll) — UI Considerations covered row."
    - { statement: "Native visual UAT confirms tooltips on header, bottom action row, and sidebar controls render on the opposite side of the source element, stay fully inside the viewport with the 8px margin at window edges, and flip sides when the preferred side lacks room.", verification: backstop }
    - { statement: "Native visual UAT confirms the notch points at the source control in all four directions, stays centered on the control when the pill is clamped, and reads as one shape with the pill (same dark fill).", verification: backstop }
  artifacts:
    - "app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx — greenfield exported pure utility computeTooltipPlacement(anchorRect, region, pillSize); TooltipRegion type ('top' | 'bottom' | 'left-edge' | 'right-edge'); viewport-positioned pill rendering with region prop replacing the legacy per-mount placement prop; useStyledTooltip controller byte-identical."
    - "app/src/components/physic-paint/physicsPaintStudio.css — reworked .physics-paint-styled-tooltip block (fixed/viewport positioning, 280x96 multiline clamp) plus directional notch rules and direction modifier classes --above/--below/--left/--right replacing the single --below modifier."
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — all 12 mounts converted to region hints: header capsule :970 / interpolation :1009 / close :1028 -> 'top'; per-cell RotoTimelineCellButton :315 and bottom action row :1139/:1167/:1195/:1223/:1251/:1279/:1307/:1348 -> 'bottom'."
    - "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx — all 3 mounts (:103/:131/:159) converted to region 'right-edge'."
  key_links:
    - "Anchor rect -> placement: at show time the pill reads its anchor wrapper's getBoundingClientRect() (viewport coordinates absorb strip horizontal scroll — Pitfall 3) and calls computeTooltipPlacement(anchorRect, region, pillSize) -> { direction, left, top, notchOffset }; the pill renders at those fixed/portal coordinates."
    - "Notch tracking: notchOffset = anchor center - pill left, recomputed AFTER viewport clamping, delivered to CSS via a custom property so the notch stays on the control when the pill shifts (Pitfall 4)."
    - "AT association: the pill keeps its id and each guarded control keeps aria-describedby pointing at it; if portal mounting is chosen, the id travels with the portaled content (T-38-05)."
---

<objective>
Rework the one shared styled tooltip per D-11..D-14: introduce the greenfield pure utility `computeTooltipPlacement(anchorRect, region, pillSize)` (opposite-of-region direction, flip-on-insufficient-room, 8px viewport clamp, notch offset tracking the anchor center after clamping), make `PhysicsPaintStyledTooltip` viewport-positioned (fixed vs `createPortal(document.body)` decided by an ancestor containing-block audit — RESEARCH Open Question 1, default to portal if in doubt), add the 10x6px same-fill directional notch, replace single-line truncation with the locked 280x96px multiline clamp, and convert every mount (strip header/capsule/cells/action row -> 'top'/'bottom'; PhysicsPaintScriptsPanel -> 'right-edge'). The `useStyledTooltip` controller contract stays byte-identical (1000ms hover, instant focus, Escape while visible).

Purpose: in-strip absolute positioning is clipped by the strip shell's overflow (36.15 Gap B) and goes stale under horizontal scroll; the permanent per-mount hand placement props contradict the locked opposite-of-element-position rule. One shared computation consumed by every mount removes the whole workaround class.

Output: reworked tooltip component + CSS block, all 15 mounts converted, zero test files touched (D-15 — this plan runs no tests; state is "automated-ready", native visible UAT is 38-06).

Tracer slice (per approved outline): one bottom action-row mount renders viewport-positioned above its control with notch + wrapped text under horizontal scroll.

Note (MVP mode): the ROADMAP Phase 38 Goal line is not in user-story format; recorded in the approved outline — surface to the user after planning rather than inventing a story.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-03-SUMMARY.md

@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end viewport tooltip — containing-block audit, computeTooltipPlacement, component + CSS rework, header mounts and one bottom action-row mount converted (D-11, D-12, D-13, D-14)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <precondition>38-03 is complete (its SUMMARY exists); the strip file on disk already contains the 38-03 capsule ambient feed — this plan is sequenced after 38-03 solely because both modify PhysicsPaintWorkflowStrip.tsx.</precondition>
  <reversibility rating="costly">D-12 viewport-positioned tooltips replace in-strip absolute positioning at every mount and risk regressing 36.15 UAT-locked visuals; reverting means restoring in-strip positioning everywhere. Flagged only — not one-way.</reversibility>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (whole file, 118 lines) — `useStyledTooltip` controller (:28-90, DO NOT CHANGE), `PhysicsPaintStyledTooltipProps` (:92-103) with the legacy placement prop, presentational pill (:110-118).
    - app/src/components/physic-paint/physicsPaintStudio.css:1717-1745 — the `.physics-paint-styled-tooltip` block and the `--below` modifier with its Gap B comment; :1338-1370 strip shell (overflow-x auto / overflow-y hidden — clips absolute, does NOT create a containing block for fixed).
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:284-318 (`RotoTimelineCellButton` wrapper pattern), :961-971 (capsule mount), :1000-1030 (interpolation + close mounts), :1199-1226 (Copy guarded action mount — the tracer mount).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md — Pattern 8 (placement rework steps), Open Question 1 (portal vs fixed), Pitfalls 3/4/8.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md §9 (component) and §12 (CSS block) — verified current-state excerpts.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md — "Tooltip Placement", "Tooltip Notch", "Tooltip Multiline", locked values (8px clamp margin, notch 10x6, max-width 280px, max-height 96px).
  </read_first>
  <action>
    Step A — containing-block audit (RESEARCH Open Question 1, decides D-12's mechanism): `position: fixed` resolves against the viewport only if no ancestor of a mounted pill has `transform`, `filter`, `perspective`, `will-change`, or `contain: layout/paint/strict`. Grep `physicsPaintStudio.css` for those properties and, for each hit, determine whether the ruled selector is an ANCESTOR of any tooltip mount (pill ancestors are: mount wrapper span/div -> action row / header band / cell lane -> `.physics-paint-workflow-strip` shell -> `.physics-paint-studio` -> body; for the Scripts panel: its sidebar section chain). Leaf rules on the buttons themselves (hover scale, active nudge) do NOT matter — the pill is a sibling, not a descendant, of the control. Record the verdict in the task commit message. If ANY mount has a containing-block-triggering ancestor — or if there is any doubt — use `createPortal(..., document.body)` from `preact/compat` (already installed, no new dependency); otherwise `position: fixed` with the pill rendered in place is allowed. Default to portal when unsure.

    Step B — greenfield utility in `PhysicsPaintStyledTooltip.tsx` (this is the phase's only genuinely new code shape; PATTERNS "No Analog Found"): export `type TooltipRegion = 'top' | 'bottom' | 'left-edge' | 'right-edge'` and `type TooltipDirection = 'above' | 'below' | 'left' | 'right'`, plus module constants `TOOLTIP_VIEWPORT_MARGIN = 8` and `TOOLTIP_PILL_MAX_WIDTH = 280` (UI-SPEC locked). Export the pure function `computeTooltipPlacement(anchorRect: DOMRect, region: TooltipRegion, pillSize: { width: number; height: number }): { direction: TooltipDirection; left: number; top: number; notchOffset: number }`. Rules (all locked): preferred direction is opposite-of-region — 'bottom' -> 'above', 'top' -> 'below', 'right-edge' -> 'left', 'left-edge' -> 'right' (D-11); if the preferred side lacks room between the anchor and the viewport edge (accounting for the 8px margin and the 6px notch gap), flip to the opposite direction; position the pill adjacent to the anchor on the resolved side with a 6px gap for the notch, centered on the anchor center along the cross axis; then clamp the pill's left/top into [8, innerWidth - pillWidth - 8] / [8, innerHeight - pillHeight - 8]; finally compute notchOffset = anchor center - pill left (for 'above'/'below') or anchor middle - pill top (for 'left'/'right'), AFTER clamping, clamped to stay inside the pill's rounded ends (Pitfall 4 — the notch tracks the control, not the pill center). Pure: no DOM reads inside the function; rects and sizes come in as parameters so 38-08 can unit-test it post-UAT.

    Step C — component rework in the same file (D-12, D-13, D-14): change `PhysicsPaintStyledTooltipProps` — remove the legacy placement prop entirely and add `region?: TooltipRegion` (OPTIONAL in this task, defaulting to 'bottom', so the mounts Task 2 has not converted yet keep rendering with their current visual outcome: every unconverted mount today either defaults to above — which 'bottom' -> 'above' reproduces — or is one of the 3 header mounts converted below). Keep `id`, `visible`, `children` unchanged. Keep the file's `useStyledTooltip` controller and `STYLED_TOOLTIP_DELAY_MS = 1000` BYTE-IDENTICAL (D-14 interaction contract; Pitfall 8 — Escape listener only while visible preserves Phase 37 Escape ordering). In the pill: while `visible`, read the anchor wrapper's rect at show time via `getBoundingClientRect()` (viewport coordinates absorb strip horizontal scroll — Pitfall 3). Fixed path: the pill span stays in place and the anchor is `pillRef.current.parentElement`. Portal path: render a zero-content marker span in place (its `parentElement` is the same wrapper) and portal the pill to `document.body`; the `id` prop goes on the pill so the `aria-describedby` association travels with the content (T-38-05). Measure the pill (`offsetWidth`/`offsetHeight`) on a hidden first render pass (e.g. `visibility: hidden` until positioned — never leave it unpositioned-visible), call `computeTooltipPlacement(anchorRect, region, pillSize)`, and apply the result as inline `left`/`top` plus a CSS custom property (suggested name `--tooltip-notch-offset`) carrying notchOffset, plus the direction modifier class. This layout synchronization with the DOM is a legitimate effect boundary (CLAUDE.md) — but prefer computing in a layout effect or ref callback over any state-copying chain; no new Signals, no listeners (tooltips hide on interaction, so no scroll/resize tracking is needed — recompute per show). Render a notch element (an aria-hidden child span, class `physics-paint-styled-tooltip-notch`) inside the pill. Content stays Preact text children only (T-38-04): no HTML-injection APIs, no string-to-markup conversion. Recompute on every show; do not cache rects across shows.

    Step D — CSS rework in `physicsPaintStudio.css` (:1717-1745 block): `.physics-paint-styled-tooltip` becomes `position: fixed` with `left`/`top` from inline style (remove the old `bottom`/`left: 50%`/`transform: translateX(-50%)` anchor-relative positioning and its z-index stays 60); set `max-width: 280px; max-height: 96px; white-space: normal; overflow: hidden;` and REMOVE the single-line white-space declaration and the `...` truncation declaration entirely (D-14 — no internal scroll; overflow is a copy defect). Keep `#20262d` background, `#eef4f8` color, `1px solid #5d6670` border, `border-radius: 999px`, `padding: 3px 8px`, font `10px/600/1.2`, `pointer-events: none`. Replace the single `--below` modifier (and delete its Gap B comment — the workaround class it documented is gone; do NOT quote the deleted workaround description in any new comment) with four direction modifiers `--above` / `--below` / `--left` / `--right` that position the notch on the control-facing edge. Add `.physics-paint-styled-tooltip-notch` rules: a 10px base x 6px height triangle (CSS borders or `clip-path`), fill/background `#20262d` with no border and no shadow so pill + notch read as one shape (D-13); per-direction variants rotate it to point up/down/left/right and place it on the pill edge facing the control; its cross-axis position comes from `var(--tooltip-notch-offset)` so it tracks the anchor after clamping. Do not put the removed single-line/truncation tokens anywhere in this file's tooltip blocks, including comments — a region-scoped negative gate greps for them. Do not touch any other CSS block (the status capsule keeps its own one-line truncation — out of scope here).

    Step E — mount conversions in `PhysicsPaintWorkflowStrip.tsx` (this task): convert the three header mounts from the legacy placement attribute to region hints — capsule :970, interpolation :1009, close :1028 become `region="top"` (top-of-UI -> below, preserving the 36.15 Gap B visual outcome, D-12); convert the tracer mount, the Copy guarded action at :1223, to `region="bottom"`. Do not touch any other mount in this task, do not alter the guarded `aria-disabled` / `aria-describedby` / sr-only reason wiring, and do not touch the 38-03 capsule ambient feed.

    No test file is created, modified, renamed, or executed anywhere in this task (D-15).
  </action>
  <verify>
    <automated>grep -c "computeTooltipPlacement" app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -qv '^0$' && grep -c "getBoundingClientRect" app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -qv '^0$' && grep -c "STYLED_TOOLTIP_DELAY_MS = 1000" app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -q '^1$' && grep -c 'region="top"' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^3$' && grep -c 'region="bottom"' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^1$' && grep -c 'placement=' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^0$' && awk '/^\.physics-paint-styled-tooltip/{p=1} p{print} p&&/^\}/{p=0}' app/src/components/physic-paint/physicsPaintStudio.css | grep -cE 'nowrap|ellipsis|text-overflow' | grep -q '^0$' && awk '/^\.physics-paint-styled-tooltip/{p=1} p{print} p&&/^\}/{p=0}' app/src/components/physic-paint/physicsPaintStudio.css | grep -c 'max-width: 280px' | grep -q '^1$' && awk '/^\.physics-paint-styled-tooltip/{p=1} p{print} p&&/^\}/{p=0}' app/src/components/physic-paint/physicsPaintStudio.css | grep -c 'max-height: 96px' | grep -q '^1$'</automated>
    <human-check>After the user's native run (UAT is 38-06): hover the Copy action-row icon for 1s — the tooltip appears ABOVE the row, fully inside the window, with a small dark notch pointing down at the Copy button; long guarded reasons wrap to multiple lines instead of truncating; scroll the timeline horizontally and re-hover — the pill still tracks the control. Header capsule tooltip still appears below.</human-check>
  </verify>
  <acceptance_criteria>
    - `computeTooltipPlacement` is exported from `PhysicsPaintStyledTooltip.tsx` as a pure function with the exact signature `(anchorRect, region, pillSize)` returning `{ direction, left, top, notchOffset }`; opposite-of-region mapping, flip, 8px clamp, and post-clamp notch offset are all implemented per the locked rules.
    - The containing-block audit verdict (fixed vs portal) is recorded in the commit message; if portal was chosen, `createPortal` comes from `preact/compat` and targets `document.body`, and the `id` prop is on the portaled pill.
    - The `useStyledTooltip` controller body and the 1000ms constant are byte-identical to the pre-task file (diff shows changes only in the props interface, pill component, and new utility/types/constants).
    - The tooltip CSS blocks (base + modifiers + notch) contain `max-width: 280px`, `max-height: 96px`, `white-space: normal`, `position: fixed`, and exactly zero occurrences of the removed single-line/truncation declarations; the four direction modifiers and the 10x6 same-fill notch rules exist.
    - The strip shows exactly 3 `region="top"` mounts (capsule, interpolation, close) and exactly 1 `region="bottom"` mount (Copy), and zero legacy placement attributes.
    - No `*.test.*` file appears in `git diff --name-only`; no vitest invocation happened.
  </acceptance_criteria>
  <done>The shared component renders viewport-positioned tooltips through the new pure utility, the CSS block implements the locked multiline clamp and directional notch, the three header mounts and the Copy tracer mount are converted, all grep gates pass, and no test file was touched. State is "automated-ready" — native visible UAT happens in 38-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert remaining strip mounts — per-cell and full bottom action row to explicit regions; make region required (D-11, D-12)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:284-318 (`RotoTimelineCellButton` — the per-cell mount at :315 inside the 120-cell map; hooks already live in the child component, one controller per cell), :1130-1360 (bottom action row: add :1139, duplicate :1167, insert :1195, copy :1223 — done in Task 1, paste :1251, delete :1279, select-all :1307, force-spacing :1348).
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx — the Task 1 props interface with the optional region default to remove.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md — "Region hints per mount" inventory (Pattern 8 end) and the per-mount-hand-placement anti-pattern.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md — "Tooltip Placement" (one shared computation consumed by every mount; no per-mount hand placement).
  </read_first>
  <action>
    In `PhysicsPaintWorkflowStrip.tsx`, add the explicit region attribute to every remaining unconverted tooltip mount, per the locked region map (D-11): the per-cell mount in `RotoTimelineCellButton` at :315 gets `region="bottom"` (cells live in the lower timeline bands — bottom-of-UI -> above); the seven remaining bottom action-row mounts get `region="bottom"` — add :1139, duplicate :1167, insert :1195, paste :1251, delete :1279, select-all :1307, force-spacing :1348. These are one-attribute additions at existing mount sites: do not reorder JSX, do not alter any guarded wiring (`aria-disabled`, `aria-describedby`, sr-only reason spans, hide-before-action onClick ordering), do not touch tooltip copy builders, and do not touch the 38-03 capsule ambient feed or the capsule mount converted in Task 1.

    In `PhysicsPaintStyledTooltip.tsx`, make `region` a REQUIRED prop (remove the Task 1 default) now that every strip mount supplies it — the Scripts panel mounts are converted in Task 3 of this same plan, so keep the type change and the panel conversion in the same commit sequence within this plan (commit Task 2 and Task 3 together if a required-prop type error in the panel would otherwise land in an intermediate commit; a single combined commit for Tasks 2+3 is acceptable). Do not change anything else in the component.

    After the edits, count the strip mounts: exactly 12 region attributes (1 cell + 3 header + 8 action row). No test file is created, modified, renamed, or executed (D-15).
  </action>
  <verify>
    <automated>grep -c 'region="' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^12$' && grep -c 'region="bottom"' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^9$' && grep -c 'region="top"' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^3$' && grep -c 'region?:' app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -q '^0$' && grep -c 'placement=' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - All 12 strip tooltip mounts carry an explicit region attribute: 9 `region="bottom"` (cell button + 8 action-row mounts) and 3 `region="top"` (capsule, interpolation, close); zero legacy placement attributes remain anywhere in the strip.
    - `region` is a required prop in `PhysicsPaintStyledTooltipProps` (no optional marker, no default value).
    - Guarded-control wiring is byte-identical at every touched mount: `aria-disabled`, `aria-describedby`, sr-only reason spans, and the hide-before-action click ordering are unchanged (diff shows only the added region attribute per mount).
    - No `*.test.*` file appears in `git diff --name-only`; no vitest invocation happened.
  </acceptance_criteria>
  <done>Every strip mount declares its UI region explicitly, the component requires it, all grep gates pass, and no test file was touched. State is "automated-ready" — native visible UAT happens in 38-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert PhysicsPaintScriptsPanel mounts to right-edge + final sweep (D-11, D-12, T-38-04, T-38-05)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx:90-165 — the three tooltip mounts (copy script :103, apply script :131, clear script buffer :159) and their wrapper structure (confirm each pill's parentElement is the intended anchor wrapper before relying on the Task 1 anchor resolution).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md — "Tooltip Placement" (right-edge elements -> tooltip to the left) and "Accessibility and Input Contract" (aria-describedby moves with the tooltip content; Escape semantics unchanged).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md — "Region hints per mount" (Scripts panel right-sidebar mounts -> 'right-edge'), Security Domain table (T-38-04/T-38-05 mitigations).
  </read_first>
  <action>
    In `PhysicsPaintScriptsPanel.tsx`, add `region="right-edge"` to all three tooltip mounts (:103, :131, :159) — the Scripts panel sits in the right sidebar, so its tooltips render to the LEFT of their controls per D-11. One-attribute additions only: do not alter the panel's guarded wiring, copy builders, or button structure.

    If the Task 1 audit chose the portal path, verify by reading each wrapper that the marker-span anchor resolution lands on the intended control wrapper (the pill must point at the button, not the panel section); adjust only the marker placement if a wrapper differs — never by adding per-mount placement overrides.

    Final sweep (whole repo, production sources only): zero legacy placement attributes remain in any consumer of `PhysicsPaintStyledTooltip`; zero references to the removed `--below`-only CSS API shape remain (the new `--below` DIRECTION modifier is expected and correct — the sweep targets JSX attributes, not CSS); `role="tooltip"` and the `id` prop path are intact in the component; no HTML-injection API appears anywhere in the tooltip component (T-38-04); every guarded control's `aria-describedby` target id still resolves to a rendered pill id (T-38-05). Geometry check: no mount wrapper gained layout-affecting markup — the portal marker (if used) is zero-content and unstyled, and 161px strip bands / 34px action row / 18px cells are untouched (no CSS outside the tooltip blocks was modified in this plan).

    No test file is created, modified, renamed, or executed (D-15).
  </action>
  <verify>
    <automated>grep -c 'region="right-edge"' app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx | grep -q '^3$' && grep -rc 'placement=' app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -c ':0$' | grep -q '^2$' && grep -c 'role="tooltip"' app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -q '^1$' && grep -c "dangerouslySetInnerHTML" app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -q '^0$' && grep -rn "PhysicsPaintStyledTooltip" app/src --include='*.tsx' -l | grep -cv 'PhysicsPaintStyledTooltip.tsx' | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - All 3 Scripts panel mounts carry `region="right-edge"`; zero legacy placement attributes remain in either consumer file.
    - Exactly two consumer files reference `PhysicsPaintStyledTooltip` (strip + scripts panel) — no new mount sites were introduced anywhere else.
    - The component still renders exactly one `role="tooltip"` element path with the `id` prop on the pill, and contains no HTML-injection API usage (T-38-04/T-38-05).
    - `git diff --name-only` for the whole plan shows only the four files in `files_modified`; no CSS block other than the tooltip blocks changed; no `*.test.*` file touched and no vitest invocation happened.
  </acceptance_criteria>
  <done>Every styled tooltip mount in the app (15 total: 12 strip + 3 panel) renders through the shared viewport-positioned computation with an explicit region, the sweeps prove no legacy placement API or injection vector remains, and no test file was touched. State is "automated-ready" — native visible UAT happens in 38-06.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

- **Greenfield pure utility (exported symbols):** `computeTooltipPlacement(anchorRect: DOMRect, region: TooltipRegion, pillSize: { width: number; height: number }): { direction: TooltipDirection; left: number; top: number; notchOffset: number }` in `app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx`, with exported types `TooltipRegion` ('top' | 'bottom' | 'left-edge' | 'right-edge') and `TooltipDirection` ('above' | 'below' | 'left' | 'right'), and module constants `TOOLTIP_VIEWPORT_MARGIN = 8` / `TOOLTIP_PILL_MAX_WIDTH = 280`. The phase's only genuinely new code shape (PATTERNS "No Analog Found"); 38-08's post-UAT tests add a pure-function describe against it (direction per region, flip, 8px clamp, notch offset after clamp).
- **Direction + notch CSS API:** modifier classes `.physics-paint-styled-tooltip--above` / `--below` / `--left` / `--right` and the `.physics-paint-styled-tooltip-notch` element rules (10x6 same-fill triangle, cross-axis position via `var(--tooltip-notch-offset)`) in `physicsPaintStudio.css`, replacing the legacy single `--below` modifier and its Gap B comment.
- **Viewport mount mechanism:** `position: fixed` inline left/top on the pill — plus, only if the Task 1 containing-block audit demands it, the portal mount symbol (`createPortal` imported from `preact/compat`, target `document.body`, zero-content in-place anchor marker). The chosen mechanism and audit verdict are recorded in the Task 1 commit message and must be copied into this plan's SUMMARY for 38-06 UAT and 38-08 tests.
- **New required component prop:** `region: TooltipRegion` on `PhysicsPaintStyledTooltipProps`; the legacy per-mount placement prop is deleted and must never be reintroduced (per-mount hand placement is a locked anti-pattern).
- **Converted mount inventory (15):** strip — cell button :315 (bottom), capsule :970 / interpolation :1009 / close :1028 (top), add :1139 / duplicate :1167 / insert :1195 / copy :1223 / paste :1251 / delete :1279 / select-all :1307 / force-spacing :1348 (bottom); panel — :103 / :131 / :159 (right-edge).
- **Downstream consumers:** 38-06 (blocking native UAT) verifies both UI-SPEC backstop rows against these mounts; 38-08 (post-UAT tests) unit-tests `computeTooltipPlacement` and must use the recorded fixed-vs-portal mechanism verbatim.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view | Tooltip copy strings (guarded reasons, cell semantic state) cross from controllers into Preact rendering as text. |
| view → DOM viewport | Pill coordinates and notch offsets computed from live DOM rects are written back as inline styles; portal mounting (if chosen) moves content outside the strip's DOM subtree to `document.body`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-04 | Tampering | `PhysicsPaintStyledTooltip` pill render | medium | mitigate | Tooltip content renders as Preact text children only (T-36.15-01); the rework adds no HTML-injection API and no string-to-markup path — Task 3's sweep negative-greps the component for injection APIs. |
| T-38-05 | Information disclosure (AT) | pill `id` / control `aria-describedby` under viewport positioning | medium | mitigate | The `id` prop stays on the pill element and travels with the content if portal mounting is chosen; guarded `aria-describedby` wiring is untouched at every mount (Task 2/Task 3 diffs are attribute-additions only); Task 3 verifies id targets still resolve. |
| T-38-SC | Tampering | package installs | high | mitigate | Vacuous — no new packages this plan; `preact/compat` is already installed (Package Legitimacy Audit satisfied vacuously per 38-RESEARCH.md). |
</threat_model>

<verification>
- Utility presence and purity: `computeTooltipPlacement` exported from `PhysicsPaintStyledTooltip.tsx`; `getBoundingClientRect` used at show time in the component, never inside the utility.
- Controller contract: `grep -c "STYLED_TOOLTIP_DELAY_MS = 1000" PhysicsPaintStyledTooltip.tsx` → 1; `useStyledTooltip` body byte-identical (diff-gated in Task 1 acceptance).
- CSS clamp: region-scoped awk extraction of the `.physics-paint-styled-tooltip*` blocks shows `max-width: 280px`, `max-height: 96px`, `white-space: normal`, `position: fixed`, four direction modifiers, notch rules — and zero single-line/truncation declarations.
- Mount inventory: strip `region="` count → 12 (9 bottom + 3 top); panel `region="right-edge"` → 3; legacy placement attributes → 0 in both consumers; exactly 2 consumer files.
- AT/injection sweeps: `role="tooltip"` → 1 in the component; injection-API grep → 0.
- D-15 discipline: `git diff --name-only` across the plan shows only the four `files_modified` entries; no `*.test.*` changes; no vitest invocation. Full typecheck/build gate is deferred to 38-08 per the D-15 sequence; this plan is "automated-ready", not "done" — native visible UAT is 38-06, including the two `{ verification: backstop }` rows in must_haves.
</verification>

<success_criteria>
- Every styled tooltip renders on the opposite side of its source element's UI region, viewport-positioned from show-time anchor rects, flipping when the preferred side lacks room and clamping inside the viewport with the locked 8px margin — including under horizontal strip scroll and at minimum host width.
- The 10x6 same-fill notch points at the source control in all four directions and stays centered on the control when the pill is clamped.
- Tooltip text wraps multiline within 280x96px with no truncation and no scroll; the 1000ms/focus/Escape interaction contract and Preact-text-children rule are preserved verbatim.
- All 15 mounts consume the shared computation via explicit region hints; no per-mount hand placement survives anywhere.
- 36.15 Gap B visual outcome preserved for header controls; 161px/34px/18px geometry untouched; zero test files created, modified, renamed, or executed.
</success_criteria>

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-SUMMARY.md` when done — it MUST record the Task 1 containing-block audit verdict (fixed vs portal) so 38-06 UAT and 38-08 tests use the same mechanism.
</output>
