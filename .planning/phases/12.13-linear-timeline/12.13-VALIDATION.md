---
phase: 12.13
slug: linear-timeline
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-19
---

# Phase 12.13 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | `Application/vitest.config.ts` |
| **Quick run command** | `cd Application && npx vitest run --reporter=verbose` |
| **Full suite command** | `cd Application && npx vitest run --reporter=verbose` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd Application && npx vitest run --reporter=verbose`
- **After every plan wave:** Run `cd Application && npx vitest run --reporter=verbose`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| LT-01 | 01 | 0 | layoutMode defaults to 'stacked' | unit | `cd Application && npx vitest run src/lib/timelineLayout.test.ts -t "defaults" --reporter=verbose` | ❌ W0 | ⬜ pending |
| LT-02 | 01 | 0 | totalContentHeight computes 1 row for linear | unit | `cd Application && npx vitest run src/lib/timelineLayout.test.ts -t "linear height" --reporter=verbose` | ❌ W0 | ⬜ pending |
| LT-03 | 01 | 0 | sequenceFromFrame maps frame to correct sequence | unit | `cd Application && npx vitest run src/lib/timelineLayout.test.ts -t "sequence from frame" --reporter=verbose` | ❌ W0 | ⬜ pending |
| LT-04 | 02 | 1 | Linear rendering draws name overlay | manual-only | Visual verification in browser | N/A | ⬜ pending |
| LT-05 | 02 | 1 | Toggle button switches layout mode | manual-only | Visual verification in browser | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `Application/src/lib/timelineLayout.test.ts` — stubs for LT-01, LT-02, LT-03 (pure function tests for layout calculations)
- No framework install needed — vitest already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Linear rendering draws name overlay | LT-04 | Canvas 2D visual output cannot be tested programmatically | Open timeline, switch to linear mode, verify sequence names overlay on thumbnails |
| Toggle button switches layout mode | LT-05 | UI interaction requires visual confirmation | Click layout toggle in TimelinePanel toolbar, verify view switches between stacked and linear |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
