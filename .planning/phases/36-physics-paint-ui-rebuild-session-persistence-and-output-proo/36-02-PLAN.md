---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/physicsPaintSessionFile.ts
  - app/src/components/physic-paint/physicsPaintSessionFile.test.ts
autonomous: true
requirements: [SAVE-01, SAVE-02]
must_haves:
  truths:
    - "User can save editable standalone physics paint state JSON per SAVE-01 and D-12."
    - "User can load only valid `SerializedProject` JSON and remain in the rebuilt UI per SAVE-02."
    - "Invalid loaded JSON shows `This file is not a valid Physics Paint state JSON. Choose a state file exported from Physics Paint.` per UI-SPEC."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintSessionFile.ts"
      provides: "Save/load helper functions for editable Physics Paint JSON"
      exports: ["SAVE_STATE_SUCCESS_COPY", "LOAD_STATE_SUCCESS_COPY", "LOAD_STATE_INVALID_COPY", "serializePhysicsPaintState", "parsePhysicsPaintStateFile", "downloadPhysicsPaintState"]
    - path: "app/src/components/physic-paint/physicsPaintSessionFile.test.ts"
      provides: "Wave 0 SAVE-01/SAVE-02 coverage required by 36-VALIDATION.md"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/physicsPaintSessionFile.ts"
      via: "later plan imports save/load helpers"
      pattern: "serializePhysicsPaintState|parsePhysicsPaintStateFile|downloadPhysicsPaintState"
---

<objective>
Extract editable JSON save/load behavior into a tested helper so the rebuilt UI can move `Save state` and `Load state` without losing current behavior.

Purpose: satisfy SAVE-01/SAVE-02 with validated `SerializedProject` JSON and keep editable state separate from rendered PNG output.
Output: tested file helpers for serialization, invalid-file handling, and browser/Tauri-compatible download orchestration.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/REQUIREMENTS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-RESEARCH.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/35-interactive-physics-paint-controls/35-07-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
</context>

## Artifacts this phase produces

- New file path: `app/src/components/physic-paint/physicsPaintSessionFile.ts`
- New file path: `app/src/components/physic-paint/physicsPaintSessionFile.test.ts`
- New constant: `SAVE_STATE_SUCCESS_COPY = 'Saved editable JSON state.'`
- New constant: `LOAD_STATE_SUCCESS_COPY = 'Loaded editable JSON state.'`
- New constant: `LOAD_STATE_INVALID_COPY = 'This file is not a valid Physics Paint state JSON. Choose a state file exported from Physics Paint.'`
- New interface: `PhysicsPaintStateDownloadAdapter`
- New function: `serializePhysicsPaintState(state: SerializedProject): string`
- New function: `parsePhysicsPaintStateFile(contents: string): SerializedProject`
- New function: `downloadPhysicsPaintState(state: SerializedProject, adapter?: PhysicsPaintStateDownloadAdapter): Promise<string>`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing session-file tests</name>
  <files>app/src/components/physic-paint/physicsPaintSessionFile.test.ts</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-VALIDATION.md
  </read_first>
  <behavior>
    - `serializePhysicsPaintState` returns pretty JSON containing `"version": 2`, `"strokes"`, and `"settings"` for a valid editable state per SAVE-01.
    - `parsePhysicsPaintStateFile` returns a `SerializedProject` for valid JSON and throws `LOAD_STATE_INVALID_COPY` for malformed JSON, missing `version`, or non-array `strokes` per SAVE-02.
    - `downloadPhysicsPaintState` calls an injected adapter with a filename beginning `efx-paint-state-` and ending `.json`, returns `SAVE_STATE_SUCCESS_COPY`, and never mutates rendered PNG output per D-12.
  </behavior>
  <action>Create Vitest tests for the helper contract above using the serialized project fixture shape from `36-PATTERNS.md`: `version: 2`, numeric `width`/`height`, `strokes: []`, and `settings: {}`. Import helper symbols from `./physicsPaintSessionFile` before implementation so RED fails for the intended missing module.</action>
  <verify>
    <automated>sh -c 'if pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintSessionFile.test.ts; then echo "Expected RED test command to fail before implementation, but it passed"; exit 1; else echo "RED verified: intentionally failing test command failed as expected"; exit 0; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at `app/src/components/physic-paint/physicsPaintSessionFile.test.ts`.
    - The automated command fails before Task 2 because the helper module or exports are missing.
    - Assertions include exact copy strings `Saved editable JSON state.`, `Loaded editable JSON state.`, and `This file is not a valid Physics Paint state JSON. Choose a state file exported from Physics Paint.`
  </acceptance_criteria>
  <done>RED tests define SAVE-01/SAVE-02 behavior without touching UI layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement editable state save/load helpers</name>
  <files>app/src/components/physic-paint/physicsPaintSessionFile.ts, app/src/components/physic-paint/physicsPaintSessionFile.test.ts</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintSessionFile.test.ts
    - /Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
    - /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint/src/types.ts
  </read_first>
  <action>Implement `physicsPaintSessionFile.ts` using the existing `isSerializedProject` guard from `../../types/physicPaint`. Keep `SerializedProject` as editable engine state only; do not include `PhysicPaintRenderedFrame`, `dataUrl`, PNG files, or apply payload fields in this helper. `downloadPhysicsPaintState` should accept an injected adapter for tests and otherwise use the browser Blob/anchor pattern already present in `PhysicsPaintStudioToolbar.tsx`; Tauri-specific dynamic imports remain in the UI/orchestrator until Plan 04 wires this helper.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintSessionFile.test.ts src/types/physicPaint.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The targeted Vitest command passes.
    - `physicsPaintSessionFile.ts` imports `isSerializedProject` and throws `LOAD_STATE_INVALID_COPY` on invalid input before engine mutation can occur.
    - The source contains no `data:image/png`, no `PhysicPaintRenderedFrame`, and no `apply-play-canvas` strings.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Editable JSON state helpers are implemented, tested, and ready for UI relocation.</done>
</task>

</tasks>

<source_audit>
GOAL covered: standalone sessions can be preserved through editable JSON save/load.
REQ covered: SAVE-01, SAVE-02.
RESEARCH covered: Wave 0 `physicsPaintSessionFile.test.ts`, validation-before-load threat, editable state/rendered output separation.
CONTEXT covered: D-04, D-12 and UI-SPEC save/load copy. Debug PNG export and artist publish are intentionally not implemented here because they are OUT-01/OUT-02 in Plan 03 and workflow wiring in later plans.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file to engine state | User-selected JSON crosses into `engine.load()` later. |
| Browser download adapter | Browser APIs receive serialized local editable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | `parsePhysicsPaintStateFile` | mitigate | Parse JSON and require `isSerializedProject` before returning state; throw exact invalid-state copy on failure. |
| T-36-05 | Denial of Service | JSON load | mitigate | Keep helper fail-closed and side-effect-free; later UI catches errors and does not call `engine.load()` on invalid content. |
| T-36-06 | Information Disclosure | State download | accept | Download is explicitly user-initiated local JSON export; no network or backend target is introduced. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned; keep existing pinned dependencies only per Package Legitimacy Audit. |
</threat_model>

<verification>
Run targeted session helper tests, existing type guard tests, and app typecheck. Do not run the dev server.
</verification>

<success_criteria>
- Valid editable state JSON serializes and parses.
- Invalid JSON is rejected with the required user-facing copy.
- Save/load helper module contains no rendered-output responsibilities.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-02-SUMMARY.md` when done.
</output>
