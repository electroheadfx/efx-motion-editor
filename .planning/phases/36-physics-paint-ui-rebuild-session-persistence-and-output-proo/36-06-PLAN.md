---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 06
type: execute
wave: 4
depends_on: [36-04, 36-05]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: [UI-REBUILD-01, SAVE-01, SAVE-02, OUT-01, OUT-02]
must_haves:
  truths:
    - "Bottom strip shows `Roto canvas` and `Play canvas` workflow tabs, both visible, with active mode controlling editability per D-11, D-20, D-21, D-22."
    - "New spreadsheet/timeline is physics-paint-specific, shares app visual style, and is not copied from the main EFX Motion timeline per D-09."
    - "Roto row uses per-frame cells and Play row uses a start square, orange range bar, and current marker per D-20, D-23."
    - "Frame navigation controls invoke concrete synced-frame callbacks that update both Physics Paint active frame and EFX Motion `timelineStore.currentFrame` via `navigateToSyncedFrame` / frame-sync bridge per D-26, D-27."
    - "Onion skin controls support previous/next/count 1..3; their props carry renderable previous/next preview frames including available Play canvas frames, and live Play canvas preview temporarily disables onion skin per D-32."
    - "Saving over existing output in the same mode replaces that same-mode output with clear feedback and no advanced conflict handling per D-36."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Bottom workflow tabs, save/play controls, lane rows, onion controls, destructive confirmations"
      exports: ["PhysicsPaintWorkflowStrip"]
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Workflow strip, lane, marker, confirmation dialog, and onion skin styles"
  key_links:
    - from: "PhysicsPaintWorkflowStrip.tsx"
      to: "physicsPaintWorkflowState.ts"
      via: "workflow predicates imported from Plan 01"
      pattern: "clampOnionCount|getActivePrimaryActionLabel|requiresDestructiveConfirmation|getPlayRangeMarker"
    - from: "PhysicsPaintWorkflowStrip.tsx"
      to: "PhysicsPaintStudio.tsx"
      via: "callback props supplied in Plan 07"
      pattern: "onSaveRotoFrame|onSavePlay|onPlayPreview|onStopPreview"
---

<objective>
Build the bottom Physics Paint workflow strip with Roto/Play workflows, state actions, timeline lanes, onion controls, and confirmation prompts.

Purpose: make the Phase 36 source-mode and timeline decisions executable without implementing deferred Phase 38 source-lane persistence.
Output: a Preact workflow strip component plus CSS for two workflow tabs, two lane rows, play range marker, primary actions, and confirmation dialogs.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/SPECS/physics-paint-ui/ui-feature-phase-36-and-more.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintWorkflowState.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintStudio.css
</context>

## Artifacts this phase produces

- New file path: `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx`
- New component: `PhysicsPaintWorkflowStrip`
- New props interface: `PhysicsPaintWorkflowStripProps`
- New type: `PhysicsPaintWorkflowStripFrameMarker`
- New type: `PhysicsPaintWorkflowConfirmation`
- New CSS classes: `.physics-paint-workflow-strip`, `.physics-paint-workflow-tabs`, `.physics-paint-workflow-tab`, `.physics-paint-primary-action`, `.physics-paint-timeline`, `.physics-paint-lane`, `.physics-paint-roto-cell`, `.physics-paint-play-range`, `.physics-paint-play-marker`, `.physics-paint-onion-controls`, `.physics-paint-confirmation`, `.physics-paint-onion-overlay`, `.physics-paint-onion-frame`, `.physics-paint-onion-prev`, `.physics-paint-onion-next`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create workflow tabs, actions, and lane rows</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
  </read_first>
  <action>Create `PhysicsPaintWorkflowStrip.tsx` with `Roto canvas` and `Play canvas` tabs in the same bottom strip. Build this as a D-09 physics-paint-specific spreadsheet/timeline that shares EFX Motion visual style but does not copy the main EFX Motion timeline implementation. Render primary CTA from `getActivePrimaryActionLabel`: `Save roto frame` in Roto mode and `Save play` in Play mode. Include `Save state` and `Load state` buttons. Render two visible rows labeled exactly `Roto frames` and `Play canvas`; inactive row must have disabled/subdued class and must not call edit callbacks. Roto frames are per-frame cells with saved-frame markers from props. Play canvas row renders a start square, range bar, and marker text/position based on `getPlayRangeMarker`; clicking inside the Play range calls `onInspectPlayFrame(frame)` only and never conversion callbacks. Add `Play`/`Stop`, frame-count input with clamp via existing frame count helper, `Preview only — use Save play to publish this range.` status during unsaved play preview, and publication summary display from props. When same-mode output already exists, route Save roto frame / Save play through replacement behavior with clear same-mode feedback per D-36 rather than conflict resolution UI.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx | grep -c 'Roto frames') -ge 1</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx | grep -c 'Play canvas') -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `PhysicsPaintWorkflowStrip.tsx` exports `PhysicsPaintWorkflowStrip` and `PhysicsPaintWorkflowStripProps`.
    - Source includes exact labels `Roto canvas`, `Play canvas`, `Roto frames`, `Save roto frame`, `Save play`, `Play`, `Stop`, `Save state`, and `Load state`.
    - Play range click handler only invokes inspection/navigation prop and does not call conversion or clear props.
    - D-09 is visible in the component shape: the workflow strip owns physics-paint-specific rows/range markers and does not import or render the main EFX Motion timeline component.
    - D-36 replacement feedback is represented by publication summary/status props instead of advanced conflict handling UI.
    - Typecheck and workflow-state tests pass.
  </acceptance_criteria>
  <done>Bottom strip renders the two exclusive workflow modes and timeline lane rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add onion controls and destructive confirmations</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
  </read_first>
  <action>Add Roto-mode onion controls: global on/off, previous toggle, next toggle, and count with `[`/`]`-compatible callbacks supplied as props. Use `clampOnionCount` and cap UI to 1..3. Render previous onion legend in cyan/blue and next onion legend in orange/yellow; accept `onionPreviewFrames` props with `{ frame, dataUrl, direction: 'previous' | 'next', distance, source: 'roto' | 'play' }` entries so Plan 07 can source saved roto frames and available Play canvas frames. Add overlay markup/classes that can render preview-only images in the main canvas region via absolute-positioned image layers; previous frames use cyan/blue tint classes, next frames use orange/yellow tint classes, and opacity decreases by distance. During live Play preview (`isPlaying === true`), hide or disable onion controls and suppress onion overlay rendering per D-32; show `Onion skin hidden during preview` only if an explanatory status prop is true. Add confirmation dialog state for `Clear Play canvas range`, `Convert Play to Roto?`, and `Convert Roto to Play?`; use exact copy from UI-SPEC including affected frame spans. Confirmed conversion buttons must call explicit props `onConvertPlayToRoto()` and `onConvertRotoToPlay()`; if `missingPlayFramesForConversion` is true, disable Play→Roto confirmation and render exact copy `Save or regenerate Play output before converting it to roto frames.` per D-39 rather than calling the conversion prop. Default focus behavior can be represented by Cancel button first in DOM order. For clear current Roto frame, call clear callback immediately and show `Cleared roto frame {frame}.` without modal.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx | grep -c 'Convert Play to Roto') -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Source includes exact copy `Clear Play canvas range`, `Convert Play to Roto?`, `Convert Roto to Play?`, `Cleared roto frame`, `Missing rendered frames for Play→Roto` or `Save or regenerate Play output before converting it to roto frames.`
    - Roto clear code path does not open confirmation state; Play clear and both conversions do.
    - Onion count UI uses `clampOnionCount` and exposes count range 1..3.
    - D-32 is explicit: live Play preview suppresses onion controls/overlays and can show `Onion skin hidden during preview`.
    - Source includes `onionPreviewFrames`, `physics-paint-onion-overlay`, `physics-paint-onion-prev`, and `physics-paint-onion-next` so Plan 07 can render actual canvas-region onion overlays, not only controls/legend.
    - Conversion confirmations call `onConvertPlayToRoto` / `onConvertRotoToPlay`, and missing Play frames block Play→Roto with the exact D-39 copy.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Workflow strip implements onion controls and destructive confirmation rules.</done>
</task>

</tasks>

<source_audit>
GOAL covered: bottom workflow strip delivers physics-paint-specific timeline and workflow controls.
REQ covered: UI-REBUILD-01, SAVE-01, SAVE-02, OUT-01, OUT-02 through workflow actions and output summaries.
RESEARCH covered: workflow strip component, not main timeline clone, exclusive source modes, onion behavior.
CONTEXT covered: D-09, D-11, D-12, D-13, D-14, D-15, D-20 through D-39, including concrete callback surfaces for D-26, D-31, D-33, D-37, D-38, and D-39. Deferred Phase 38 hybrid lanes, auto-publish, arbitrary stacked lanes, and advanced conflict handling are not implemented.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timeline clicks to workflow actions | User clicks can inspect frames, clear data, or request conversion. |
| Frame count input to preview/save | User input controls Play range length. |
| Destructive confirmations to source mutation callbacks | Confirmed dialogs trigger clear/convert callbacks supplied by Studio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-17 | Tampering | Play lane click handler | mitigate | Play lane clicks call inspection only and never conversion/clear callbacks. |
| T-36-18 | Tampering | clear/convert dialogs | mitigate | Require confirmation for Play clear and both conversion directions; Roto clear is immediate per locked decision. |
| T-36-19 | Denial of Service | frame-count input | mitigate | Clamp range length through existing helper before preview/save callbacks. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned; keep existing pinned dependencies only per Package Legitimacy Audit. |
</threat_model>

<verification>
Run workflow-state tests, app typecheck, and grep assertions. Do not run the dev server.
</verification>

<success_criteria>
- Bottom strip compiles and contains all required workflow labels.
- Play lane inspection cannot convert/delete data.
- Destructive actions require confirmations exactly where specified.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-06-SUMMARY.md` when done.
</output>
