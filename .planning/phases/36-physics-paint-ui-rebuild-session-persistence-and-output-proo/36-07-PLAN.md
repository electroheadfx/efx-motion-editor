---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 07
type: execute
wave: 5
depends_on: [36-06]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: false
requirements: [UI-REBUILD-01, UI-REBUILD-02, SAVE-01, SAVE-02, OUT-01, OUT-02]
must_haves:
  truths:
    - "User can use the complete five-region rebuilt Physics Paint UI per D-01."
    - "All moved controls remain functional from their new locations per D-04."
    - "Old toolbar structure and large diagnostics grid are no longer rendered per D-05."
    - "Normal artist workflow is `Save roto frame` / `Save play`; dev export is collapsed and development-only per D-16 through D-19."
    - "Shortcuts are contextual to the Physics Paint surface and ignored inside inputs per D-40."
    - "Accepted general shortcuts are wired exactly: Cmd+Z undo, Cmd+Shift+Z redo, Esc stop preview/close/cancel, Cmd+S save active workflow, and ? help per D-41."
    - "Accepted Roto shortcuts are wired exactly: arrows for synced frames, Shift+arrows for saved roto frames if implemented, G go-to-frame, O onion toggle, [/] onion count, Cmd+Enter save-and-next, and Delete clear current source with active-mode warning rules per D-42."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Integrated five-region rebuilt Physics Paint UI"
    - path: "app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx"
      provides: "Removed from render path or converted to compatibility export only"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Final integrated layout styles"
  key_links:
    - from: "PhysicsPaintStudio.tsx"
      to: "PhysicsPaintTopBar.tsx"
      via: "props and callbacks"
      pattern: "<PhysicsPaintTopBar"
    - from: "PhysicsPaintStudio.tsx"
      to: "PhysicsPaintToolRail.tsx"
      via: "props and callbacks"
      pattern: "<PhysicsPaintToolRail"
    - from: "PhysicsPaintStudio.tsx"
      to: "PhysicsPaintRightPanel.tsx"
      via: "props and callbacks"
      pattern: "<PhysicsPaintRightPanel"
    - from: "PhysicsPaintStudio.tsx"
      to: "PhysicsPaintWorkflowStrip.tsx"
      via: "props and callbacks"
      pattern: "<PhysicsPaintWorkflowStrip"
---

<objective>
Wire the rebuilt components into `PhysicsPaintStudio` and add contextual Physics Paint shortcuts.

Purpose: deliver the complete production-grade standalone package UI while preserving Phase 35 engine/apply behavior and Phase 36 save/load/export proof behavior.
Output: integrated five-region UI, contextual shortcuts, final CSS, and a human verification checkpoint for visual/interactive confirmation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/REQUIREMENTS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-VALIDATION.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintTopBar.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintToolRail.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintRightPanel.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/lib/shortcuts.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/overlay/ShortcutsOverlay.tsx
</context>

## Artifacts this phase produces

- Modified component: `PhysicsPaintStudio`
- Retired render path: `PhysicsPaintStudioToolbar` no longer used by `PhysicsPaintStudio` five-region layout
- New local callback in `PhysicsPaintStudio.tsx`: `handlePhysicsPaintKeyDown`
- New local helper in `PhysicsPaintStudio.tsx`: `isPhysicsPaintShortcutTarget`
- New local function/callback in `PhysicsPaintStudio.tsx`: `navigateToSyncedFrame(frame: number)`
- New local function/callback in `PhysicsPaintStudio.tsx`: `buildOnionPreviewFrames()`
- New local function/callback in `PhysicsPaintStudio.tsx`: `convertPlayToRoto()`
- New local function/callback in `PhysicsPaintStudio.tsx`: `convertRotoToPlay()`
- New CSS classes: `.physics-paint-layout`, `.physics-paint-main`, `.physics-paint-shell-error`, `.physics-paint-empty-state`, `.physics-paint-shortcuts-help`
- New UI copy rendered by integrated Studio: `No physics paint saved for this frame`, `Physics Paint could not complete this action. Check the message above, keep the window open, and try again after the engine is ready.`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire five-region layout into PhysicsPaintStudio</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintTopBar.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintToolRail.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintRightPanel.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
  </read_first>
  <action>Replace the old `PhysicsPaintStudioToolbar` render usage with the five regions: `<PhysicsPaintTopBar>`, `<PhysicsPaintToolRail>`, main `EfxPaintCanvas` region, `<PhysicsPaintRightPanel>`, and `<PhysicsPaintWorkflowStrip>`. Preserve `CanvasMountProbe`, `EfxPaintCanvas`, engine ref setup, launch context parsing, bridge listeners, apply timeouts, `AnimationPlayer`, and all action callbacks from Plan 04. Pass all current engine setting callbacks to the new regions so tool, color, brush size, opacity, background, paper grain, grain strength, physics last/all, dry/freeze, undo, clear, Save state, Load state, Play preview, Save roto frame, Save play, and debug export remain functional. Wire `Save roto frame` to the Plan 04 save-and-next callback so it publishes the current frame and then calls `navigateToSyncedFrame(nextFrame)`; wire previous/next frame controls and Play range inspection clicks to `navigateToSyncedFrame(frame)`, which updates local Physics Paint active frame and emits the frame-sync bridge event handled by the main app to call `timelineStore.seek(frame)` and `timelineStore.ensureFrameVisible(frame)` per D-13 and D-26. Render compact top status/errors only; remove the old bottom diagnostics grid render path. Keep `PhysicsPaintStudioToolbar.tsx` only if still imported elsewhere; otherwise remove its import from Studio. Do not run the server.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts src/components/physic-paint/physicsPaintSessionFile.test.ts src/components/physic-paint/physicsPaintDevExport.test.ts src/lib/physicPaintBridge.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c '<PhysicsPaintWorkflowStrip') -ge 1</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c 'diagnostics-grid') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `PhysicsPaintStudio.tsx` renders all four child components and the main canvas region.
    - `PhysicsPaintStudio.tsx` no longer imports or renders `PhysicsPaintStudioToolbar`.
    - `diagnostics-grid` does not appear in `PhysicsPaintStudio.tsx`.
    - Source includes exact empty/error copy listed in the artifacts section.
    - `PhysicsPaintStudio.tsx` wires `onPreviousFrame`, `onNextFrame`, and `onInspectPlayFrame` to `navigateToSyncedFrame`; app-side frame-sync listener calls `timelineStore.seek` and `timelineStore.ensureFrameVisible`.
    - Targeted tests and typecheck pass.
  </acceptance_criteria>
  <done>Complete rebuilt UI is wired into the standalone Physics Paint Studio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement synced onion overlays and workflow conversions</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - /Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
    - /Users/lmarques/Dev/efx-motion-editor/app/src/stores/timelineStore.ts
    - /Users/lmarques/Dev/efx-motion-editor/app/src/lib/physicPaintBridge.ts
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
  </read_first>
  <action>Implement D-31 through D-33 by adding `buildOnionPreviewFrames()` in `PhysicsPaintStudio.tsx` that derives previous/next preview frames from saved roto/output frames and latest saved/generated Play frames when available, excluding overlays while `isPlaying` is true. Render those frames as preview-only absolute overlays in the main canvas region using the Plan 06 overlay classes; previous frames use cyan/blue classes, next frames use orange/yellow classes, and opacity decreases as distance increases. Implement D-37 through D-39 conversion callbacks: `convertPlayToRoto()` verifies every frame in the current Play range exists in the latest saved/generated Play frame set before mutation; if any are missing, show exact D-39 copy `Save or regenerate Play output before converting it to roto frames.` and do not mutate. When confirmed and complete, Play→Roto writes each rendered Play frame to the rendered roto/output frame map via `physicPaintStore.setFrame(layerId, appFrame, frame)` and clears/deletes the editable Play source/local Play script state for that range. `convertRotoToPlay()` confirms the current range, builds/replaces the current Play source/range from the current editable state and range metadata, then removes rendered roto images in that range using a new focused store method if needed; bump `physicPaintVersion` through store mutations. Do not implement Phase 38 hybrid mixing or persisted source-lane schema. Also add the local `handlePhysicsPaintKeyDown` / `isPhysicsPaintShortcutTarget` shortcut handler in this same integration pass: bind it only to the Physics Paint root and ignore input/textarea/select/contenteditable targets per D-40. Wire D-41 accepted general shortcuts exactly: `Cmd+Z` undo, `Cmd+Shift+Z` redo, `Esc` stop preview before close/cancel fallback, `Cmd+S` save the active workflow (`Save roto frame` in Roto mode, `Save play` in Play mode), and `?` show shortcuts/help. Wire D-42 accepted Roto shortcuts exactly: `←`/`→` previous/next synced frame, `Shift+←`/`Shift+→` previous/next saved roto frame if implemented, `G` focus/go-to-frame, `O` onion on/off, `[`/`]` onion count down/up, `Cmd+Enter` save roto frame and go next, and `Delete` clear current roto frame/source with active-mode warning rules. Wire D-43 Play shortcuts: `Space` and `Enter` play/stop preview when not inside an input, `Cmd+S` save play, and `Esc` stop preview before fallback; do not modify global `app/src/lib/shortcuts.ts` unless a compile-time type import is required.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts src/lib/physicPaintBridge.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c 'buildOnionPreviewFrames') -ge 1</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c 'convertPlayToRoto') -ge 1</automated>
    <automated>test $(grep -v '^#' /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -c 'Save or regenerate Play output before converting it to roto frames') -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Onion overlays render in the main canvas region from `onionPreviewFrames`; controls/legend alone are not sufficient.
    - Onion overlay source includes available Play canvas frames when not live-playing and suppresses overlays while `isPlaying` is true.
    - `convertPlayToRoto` blocks on missing Play frames with exact D-39 copy, otherwise writes rendered frames to roto/output frames and deletes editable Play source state after confirmation.
    - `convertRotoToPlay` replaces the current Play range and removes rendered roto images in the affected span after confirmation.
    - Store mutations bump `physicPaintVersion`; no Phase 38 source-lane persistence is added.
    - D-41 general shortcuts are implemented in the local handler with input/contenteditable guards: Cmd+Z, Cmd+Shift+Z, Esc, Cmd+S, and ?.
    - D-42 Roto shortcuts are implemented in the local handler: arrows, optional Shift+arrows, G, O, [/], Cmd+Enter, and Delete with active-mode warning rules.
  </acceptance_criteria>
  <done>Canvas-region onion preview rendering and explicit destructive conversion behavior are implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual and interaction verification</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <action>Pause after automated verification so the user can inspect the rebuilt Physics Paint UI in their already-running app. Claude must not run the server. Capture any user-reported issue exactly before resuming execution.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <human-check>User follows the how-to-verify steps below and replies approved or reports exact issues.</human-check>
  </verify>
  <what-built>Claude integrated the rebuilt Physics Paint UI, moved controls into the five locked regions, preserved save/load/publish/export behavior, and added contextual shortcuts.</what-built>
  <how-to-verify>
    1. User starts/runs the app on their side; Claude must not run the server.
    2. Open a Physics Paint layer/window.
    3. Verify top bar contains brush size, opacity, background, paper grain, `Grain strength` with `None`, `Soft`, `Med`, `Hard`, compact `Engine ready`/`Engine not ready`, `Save state`, `Load state`, and collapsed dev export only in development.
    4. Verify left rail order: Paint, Paint with physics, Erase, Undo, Clear frame, Apply physics to last stroke, Apply physics to all strokes, Dry / freeze paint, using the provided SVG icons.
    5. Verify right panel changes brush color/options and erase strength appears only for erase.
    6. Verify bottom strip shows `Roto canvas` and `Play canvas`, rows `Roto frames` and `Play canvas`, `Save roto frame`, `Play`/`Stop`, `Save play`, and active/inactive lane states.
    7. Paint, save state JSON, load the saved JSON, export a still or debug proof, use Play preview, then `Save play`; confirm the window stays open and shows a summary.
    8. Verify shortcuts only work when focus is in the Physics Paint surface and not inside inputs.
  </how-to-verify>
  <resume-signal>Type `approved` if the rebuilt UI and interactions pass, or describe exact issues and screenshots.</resume-signal>
  <done>Human approval received, or reported issues are captured for a follow-up correction plan.</done>
</task>

</tasks>

<source_audit>
GOAL covered: complete five-region rebuilt production-grade standalone UI with session persistence and output proof.
REQ covered: UI-REBUILD-01, UI-REBUILD-02, SAVE-01, SAVE-02, OUT-01, OUT-02.
RESEARCH covered: component assembly, behavior preservation, no server run, shortcut guard, no old diagnostics grid.
CONTEXT covered: D-01 through D-43, including D-13/D-26 synced frame navigation, D-31/D-33 canvas-region onion overlays, and D-37/D-39 conversion mutations. Deferred ideas are not implemented: Phase 38 source-lane model, hybrid mixing, auto-publish, advanced conflict handling, arbitrary stacked roto lanes, `.mce` persistence, and editor integration.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integrated UI to engine/bridge | New components invoke existing engine and bridge behavior through Studio callbacks. |
| Keyboard events to destructive actions | Local keydown handler can trigger save, clear, play, and navigation actions. |
| Human runtime verification | Visual/runtime behavior requires user-run app because project instruction forbids Claude from running the server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-20 | Tampering | integrated callback wiring | mitigate | Preserve existing bridge validation tests and route all component actions through `PhysicsPaintStudio` callbacks. |
| T-36-21 | Elevation of Privilege | keyboard shortcuts | mitigate | Bind shortcuts locally to Physics Paint root and ignore inputs/contenteditable targets. |
| T-36-22 | Denial of Service | play/save keyboard repeat | mitigate | Reuse existing `isPlaying`/`applyStatus` guards and disabled states before action callbacks. |
| T-36-23 | Repudiation | save/publish feedback | mitigate | Render explicit success/error summaries for save roto, save play, save/load state, and debug export. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned; keep existing pinned dependencies only per Package Legitimacy Audit. |
</threat_model>

<verification>
Run targeted tests, app typecheck, grep source gates, then pause for human visual/runtime verification. Do not run the dev server.
</verification>

<success_criteria>
- `PhysicsPaintStudio` renders the full rebuilt UI and not the old toolbar/diagnostics grid.
- All Phase 36 requirements appear in the integrated UI path.
- Human verification confirms layout, synced frame navigation/save-and-next, onion overlays, conversions, and interactions in the user-run app.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-07-SUMMARY.md` when done.
</output>
