---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
gap_closure: true
requirements: [UI-REBUILD-01, UI-REBUILD-02, SAVE-01, OUT-01]
must_haves:
  truths:
    - "UAT gap 5: onion-skin controls update visible previous/next overlays when adjacent Roto frame sources exist, per D-29 through D-33."
    - "UAT gap 5: live Play preview suppresses onion overlays, per D-32."
    - "UAT gap 7 partial: Save play no longer leaves a yellow/orange overlay artifact from latest Play frames, per D-15 and D-32."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Real standalone onion preview source from adjacent editable Roto states or local rendered snapshots"
      contains: "buildOnionPreviewFrames"
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts"
      provides: "Regression coverage for onion filtering, preview suppression, and no Play-frame post-save overlay"
  key_links:
    - from: "rotoFrameStatesRef"
      to: "canvas-region onion overlay"
      via: "rendered snapshot cache / preview frame builder"
      pattern: "rotoFrameStatesRef.*buildOnionPreviewFrames|buildOnionPreviewFrames.*roto"
    - from: "savePlay"
      to: "onion overlay"
      via: "latestPlayFrames restricted away from post-save onion source"
      pattern: "latestPlayFrames"
---

<objective>
Close UAT gap 5 and the yellow-overlay portion of UAT gap 7 by giving onion skin a real standalone Roto preview source and preventing saved Play output from being reused as onion overlay pixels.

Purpose: The controls already exist, but they do not affect the canvas because `buildOnionPreviewFrames` normally has no adjacent frame images in standalone Roto workflow. Save play also reuses `latestPlayFrames` as onion preview frames, producing the reported yellow box/overlay after publish.
Output: Adjacent Roto frame content becomes available to the canvas overlay immediately after frame navigation/save, onion controls visibly affect overlays, live preview hides overlays, and Save play does not leave Play frames as yellow onion overlays.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UAT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/debug/phase-36-uat-gap-test-5.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/debug/phase-36-uat-gap-test-7.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-07-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add onion preview regression coverage for real Roto sources and preview suppression</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <behavior>
    - Test 1: Onion overlay rendering reacts to previous/next/count settings when supplied adjacent Roto preview frames.
    - Test 2: When `isPlaying` is true, onion overlay frames are not rendered even if preview frames exist, per D-32.
    - Test 3: Tests or source-contract assertions prevent `latestPlayFrames` from being used as the normal post-Save-play onion source that creates the yellow overlay artifact.
  </behavior>
  <action>Add focused regression tests around the existing workflow strip onion rendering and source-contract checks for Studio. Keep the tests targeted and runnable without starting the app server. The test should encode UAT gap 5 and the UAT gap 7 overlay symptom, not introduce new Phase 38 source-lane expectations.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Tests fail before implementation if onion overlays still depend on unavailable persisted store/Play frames or if live preview does not suppress overlays.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build standalone Roto onion preview snapshots and remove post-save Play overlay reuse</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <behavior>
    - Saving/navigating away from a Roto frame stores both editable state and a rendered PNG snapshot for that frame when the engine has content.
    - Navigating to adjacent Roto frames can show previous/next onion overlays from the local snapshot cache without requiring parent store persistence first.
    - `buildOnionPreviewFrames` filters by onion count, previous/next toggles, current frame distance, and `isPlaying` suppression.
    - `savePlay` does not populate onion preview frames in a way that persists as yellow/orange overlay after publish.
  </behavior>
  <action>In `PhysicsPaintStudio.tsx`, add a local rendered Roto preview source alongside `rotoFrameStatesRef`, for example a `rotoPreviewFramesRef` or state map keyed by app frame containing `PhysicPaintRenderedFrame` snapshots from `engine.exportCompositeCanvas()`. Update `navigateToSyncedFrame` and `saveRotoFrame` so adjacent Roto frames become previewable immediately in the standalone window. Update `buildOnionPreviewFrames` to prioritize this local Roto snapshot cache and persisted `physicPaintStore` Roto frames, and to exclude `latestPlayFrames` from ordinary post-save onion overlays unless the active workflow explicitly needs non-playing Play preview context. If any Play preview inclusion remains, gate it so `savePlay` success cannot leave yellow/orange overlays on the canvas. Keep live preview returning an empty array. Do not use headless replay or batch `renderFromStrokes`; preserve interactive incremental engine behavior.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>grep -n "latestPlayFrames.*addFrame\|addFrame.*latestPlayFrames" /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -v "preview"; test $? -ne 0</automated>
  </verify>
  <done>Changing onion controls changes real previous/next overlays when adjacent saved/navigated Roto frames exist, live preview hides overlays, and Save play no longer leaves a yellow/orange overlay artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine canvas -> img overlay | Rendered canvas data URLs are displayed as preview-only onion images. |
| user controls -> overlay state | User-controlled onion count/toggles determine which local snapshots are displayed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09-01 | Information Disclosure | onion preview data URLs | accept | Data is local canvas content already visible to the user in the standalone window; no network or file write added. |
| T-36-09-02 | Tampering | onion overlay DOM | mitigate | Overlays remain `aria-hidden`, preview-only, and do not accept pointer/editing events. |
| T-36-09-03 | Denial of Service | snapshot cache | mitigate | Cap onion count at existing 1..3 and only snapshot relevant Roto frames on save/navigation, not all frames. |
| T-36-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run targeted workflow strip tests and typecheck. User can verify by saving/navigating between at least three Roto frames, toggling previous/next/count controls, confirming overlays appear/disappear accordingly, starting Play preview to confirm overlays hide, then Save play to confirm no yellow box remains.
</verification>

<success_criteria>
- UAT gap 5 missing item 1 covered: real preview-frame source exists for standalone Roto onion overlays.
- UAT gap 5 missing item 2 covered: save/navigation makes adjacent Roto frames available immediately.
- UAT gap 5 missing item 3 covered: regression coverage includes previous/next/count overlays and suppression while preview is playing.
- UAT gap 7 yellow overlay symptom covered: saved Play frames no longer remain as onion/yellow overlay artifacts.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-09-SUMMARY.md` when done.
</output>
