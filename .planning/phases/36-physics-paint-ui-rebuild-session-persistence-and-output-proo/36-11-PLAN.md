---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 11
type: execute
wave: 2
depends_on: [36-08, 36-09]
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.tsx
  - app/src-tauri/src/lib.rs
autonomous: true
gap_closure: true
requirements: [SAVE-01, SAVE-02, OUT-01, OUT-02]
must_haves:
  truths:
    - "UAT gap 7: Save play publishes the selected range without closing the standalone studio, per D-15."
    - "UAT gap 7: Save play persists workflow mode, Play start frame/range, and editable Play source metadata so reopening restores the Play canvas context."
    - "UAT gap 7: reopening EFX Physics after saved Play output hydrates the Play canvas tab instead of hardcoded Roto."
    - "UAT gap 7: saved Play range remains editable from the restored source rather than losing all edits after close/reopen."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Typed Play metadata on serialized output and launch context"
      contains: "workflowMode"
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Project-serialized Play metadata beside rendered frames and editable state"
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Launch context hydration from persisted Play metadata"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Initial workflow mode/range/source hydration from launch context"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.tsx"
      provides: "Focused Studio relaunch hydration regression for saved Play workflow/range/source metadata"
    - path: "app/src-tauri/src/lib.rs"
      provides: "Native launch-context parity for new optional metadata fields"
  key_links:
    - from: "physicPaintStore.applySequence"
      to: "physicPaintStore.toMceOutputs"
      via: "persisted Play metadata in project outputs"
      pattern: "workflowMode|playStartFrame|playFrameCount"
    - from: "createPhysicPaintLaunchContext"
      to: "PhysicsPaintStudio initial state"
      via: "PhysicPaintLaunchContext optional workflow/range metadata"
      pattern: "workflowMode.*play|playFrameCount"
---

<objective>
Close the persistence/reopen portion of UAT gap 7 by saving Play canvas workflow metadata and hydrating the standalone studio from that metadata on relaunch.

Purpose: Save play currently stores rendered frames and one generic editable state, but it does not persist the active workflow, Play range, or editable Play source association. Reopening therefore loses the Play editing context and hardcodes the studio back to `Roto canvas`.
Output: Save play persists Play metadata beside the rendered output, project serialization retains it, bridge launch context carries it, Tauri native launch parity accepts it, and `PhysicsPaintStudio` initializes to Play mode with the saved range/source when reopened.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UAT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/debug/phase-36-uat-gap-test-7.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-04-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-07-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/lib/physicPaintBridge.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed persistence and Studio relaunch hydration tests for Play workflow metadata</name>
  <files>app/src/types/physicPaint.test.ts, app/src/stores/physicPaintStore.test.ts, app/src/lib/physicPaintBridge.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.tsx</files>
  <behavior>
    - Test 1: `apply-play-canvas` stores Play metadata including workflow mode `play`, start frame, frame count, and editable source state association.
    - Test 2: `toMceOutputs` and `loadFromMceOutputs` round-trip the metadata with rendered frames and editable state.
    - Test 3: `createPhysicPaintLaunchContext` includes persisted workflow/range metadata for a layer after Save play.
    - Test 4: launch-context validation accepts well-formed optional metadata and rejects invalid workflow mode, negative start, or invalid frame count.
    - Test 5: `PhysicsPaintStudio` opened with saved Play launch metadata initializes in `Play canvas` mode, uses the saved Play start frame/range, and preserves the saved editable Play source instead of falling back to Roto/default range.
  </behavior>
  <action>Add failing tests that encode UAT gap 7 persistence and the checker-required direct Studio relaunch hydration behavior. Use existing store, bridge, and React/Vitest component test patterns. Add or extend `PhysicsPaintStudio.test.tsx` with a focused regression that renders/initializes the Studio with a `PhysicPaintLaunchContext` containing `workflowMode: 'play'`, a non-default `playStartFrame`, a non-default `playFrameCount`, and saved editable Play source metadata; assert the UI/state starts on `Play canvas`, displays/uses the restored range/source, and does not initialize from hardcoded Roto/default values. Keep the metadata narrow and Phase-36-specific: active workflow mode, Play start frame, Play frame count, and editable Play source association. Do not add Phase 38 hybrid source lanes, overlap/conflict handling, `.mce` migration compatibility code, or editor integration beyond existing physics paint output serialization.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/PhysicsPaintStudio.test.tsx</automated>
  </verify>
  <done>Tests fail before implementation because output serialization and launch context do not carry Play workflow/range metadata, and because Studio relaunch still starts from hardcoded Roto/default range instead of saved Play launch metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and hydrate Save play workflow metadata</name>
  <files>app/src/types/physicPaint.ts, app/src/stores/physicPaintStore.ts, app/src/lib/physicPaintBridge.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src-tauri/src/lib.rs</files>
  <behavior>
    - `PhysicPaintLaunchContext` supports optional workflow/range metadata such as `workflowMode`, `playStartFrame`, and `playFrameCount` with validation.
    - Store serialization includes Play metadata after `applySequence` and restores it through `loadFromMceOutputs`.
    - Bridge launch context reads persisted metadata from `physicPaintStore` and carries it to both browser URL and Tauri native launch paths.
    - Studio initializes `workflowMode` and `framesToApply` from launch context instead of hardcoding Roto/defaults; saved Play output reopens in Play canvas with the restored range.
  </behavior>
  <action>Extend the physics paint output data shape with optional Play workflow metadata: `workflowMode: 'roto' | 'play'`, `playStartFrame`, `playFrameCount`, and any minimal editable-source association needed to distinguish saved Play source from single-frame Roto state. In `physicPaintStore.applySequence`, store this metadata whenever Save play succeeds; in `convertPlayToRoto` switch metadata back to Roto/clear Play metadata; in `convertRotoToPlay` set Play metadata for the converted range. Update `toMceOutputs` and `loadFromMceOutputs` to round-trip the fields. Update `createPhysicPaintLaunchContext` to include persisted metadata for the selected layer and update `isPhysicPaintLaunchContext` validation. In `PhysicsPaintStudio.tsx`, initialize `workflowMode` from `launchContext.workflowMode ?? 'roto'`, initialize `framesToApply` from `launchContext.playFrameCount` when present, and keep `launchContext.startFrame`/range consistent with `playStartFrame` when reopening saved Play output. Ensure the focused `PhysicsPaintStudio.test.tsx` relaunch hydration test from Task 1 passes by proving saved Play launch metadata selects Play mode and restored range/source at Studio startup. Update `app/src-tauri/src/lib.rs` structs/URL/context handling so optional metadata fields are accepted and forwarded for native launches. Do not add backwards-compat migration code for old projects; user memory says clean breaks on format changes are acceptable. Do not reintroduce headless replay or editor-driven batch rendering.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/PhysicsPaintStudio.test.tsx src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>cargo check --manifest-path /Users/lmarques/Dev/efx-motion-editor/app/src-tauri/Cargo.toml</automated>
    <automated>grep -n "useState<PhysicsPaintWorkflowMode>('roto')" /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx; test $? -ne 0</automated>
  </verify>
  <done>After Save play, closing/reopening EFX Physics restores Play canvas mode, the selected range, and editable source state; edits are not lost merely because the standalone window was closed; the direct Studio relaunch hydration regression test proves the user-observable reopen behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project file -> store | Serialized physics-paint outputs hydrate metadata into app state. |
| store -> launch context -> standalone window | Persisted metadata crosses into browser URL/Tauri native launch context. |
| standalone -> app apply payload | Save play payload mutates rendered output and metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11-01 | Tampering | launch context metadata | mitigate | Validate workflow mode, non-negative Play start, and clamped frame count before Studio uses metadata. |
| T-36-11-02 | Tampering | project output metadata | mitigate | Store/load only narrow optional primitive metadata fields; ignore malformed values. |
| T-36-11-03 | Repudiation | Save play persistence | mitigate | Success summary continues to show saved frame count/range after apply result. |
| T-36-11-04 | Denial of Service | oversized Play range | mitigate | Continue using `clampPhysicPaintFrameCount` and PHYSIC_PAINT_MAX_APPLY_FRAMES validation. |
| T-36-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run targeted type/store/bridge/Studio hydration/workflow tests, app typecheck, and Rust cargo check. Manual UAT: create Play output, click Save play, verify no auto-close, close and reopen EFX Physics for the same layer/frame, and confirm the studio opens in Play canvas with the saved range/source instead of Roto canvas with lost edits.
</verification>

<success_criteria>
- UAT gap 7 missing item 1 covered: Play source metadata is persisted with frames/editable state.
- UAT gap 7 missing item 2 covered: metadata is hydrated from project data into bridge and Tauri/browser launch context.
- UAT gap 7 missing item 3 covered: Studio initializes mode/range from launch context instead of hardcoded Roto/defaults.
- UAT gap 7 missing item 5 covered: tests verify persistence, direct Studio relaunch hydration in Play mode, and Play range restoration.
- Yellow overlay artifact is covered by Plan 09; this plan must not reintroduce `latestPlayFrames` as post-save onion overlays.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-11-SUMMARY.md` when done.
</output>
