---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/physicsPaintDevExport.ts
  - app/src/components/physic-paint/physicsPaintDevExport.test.ts
autonomous: true
requirements: [OUT-01, OUT-02, UI-REBUILD-02]
must_haves:
  truths:
    - "User can export or inspect the current rendered still PNG proof from live engine capture per OUT-01."
    - "User can produce PNG frame proof metadata plus `manifest.json` from live captured frames per OUT-02 and D-17 through D-19."
    - "Debug export remains a dev-only sidecar, not the normal artist workflow per D-16, D-18, D-19."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintDevExport.ts"
      provides: "Manifest and filename helpers for dev PNG+manifest proof export"
      exports: ["PHYSICS_PAINT_DEBUG_EXPORT_KIND", "buildPhysicsPaintDebugManifest", "buildPhysicsPaintStillExport", "dataUrlToBlobPart", "makePhysicsPaintFrameFilename"]
    - path: "app/src/components/physic-paint/physicsPaintDevExport.test.ts"
      provides: "Wave 0 OUT-01/OUT-02 coverage required by 36-VALIDATION.md"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/physicsPaintDevExport.ts"
      via: "later plan builds manifest from captured frames"
      pattern: "buildPhysicsPaintDebugManifest|buildPhysicsPaintStillExport"
---

<objective>
Create tested debug export proof helpers for rendered stills and frame-sequence manifests.

Purpose: satisfy OUT-01/OUT-02 without adding a second renderer or resurrecting headless batch replay.
Output: side-effect-free manifest/still export utilities that operate on existing `PhysicPaintRenderedFrame` data URLs captured from the live engine.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/REQUIREMENTS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-RESEARCH.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/35-interactive-physics-paint-controls/35-04-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

## Artifacts this phase produces

- New file path: `app/src/components/physic-paint/physicsPaintDevExport.ts`
- New file path: `app/src/components/physic-paint/physicsPaintDevExport.test.ts`
- New constant: `PHYSICS_PAINT_DEBUG_EXPORT_KIND = 'physics-paint-debug-export'`
- New interface: `PhysicsPaintDebugManifest`
- New interface: `PhysicsPaintDebugManifestFrame`
- New interface: `PhysicsPaintStillExport`
- New function: `makePhysicsPaintFrameFilename(frameIndex: number): string`
- New function: `buildPhysicsPaintDebugManifest(args: { layerId: string; operationId: string; startFrame: number; frameCount: number; frames: PhysicPaintRenderedFrame[]; fps: number; generatedAt?: string }): PhysicsPaintDebugManifest`
- New function: `buildPhysicsPaintStillExport(frame: PhysicPaintRenderedFrame): PhysicsPaintStillExport`
- New function: `dataUrlToBlobPart(dataUrl: string): { mimeType: string; bytes: Uint8Array }`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing dev-export helper tests</name>
  <files>app/src/components/physic-paint/physicsPaintDevExport.test.ts</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-VALIDATION.md
  </read_first>
  <behavior>
    - `makePhysicsPaintFrameFilename(0)` returns `frame-0000.png`; `makePhysicsPaintFrameFilename(42)` returns `frame-0042.png`.
    - `buildPhysicsPaintDebugManifest` returns `kind: 'physics-paint-debug-export'`, exact `startFrame`, exact `frameCount`, `fps`, `canvas.width`, `canvas.height`, and frame entries mapping `frameIndex`, `appFrame`, and filename.
    - Manifest creation rejects empty frames, `frameCount` mismatch, non-PNG data URLs, and frame counts outside existing physics paint clamp limits.
    - `buildPhysicsPaintStillExport` maps a single `PhysicPaintRenderedFrame` to a PNG filename and preserves width/height.
    - `dataUrlToBlobPart('data:image/png;base64,aGVsbG8=')` returns `mimeType: 'image/png'` and bytes for `hello`.
  </behavior>
  <action>Create Vitest tests for the helper module using `PhysicPaintRenderedFrame` fixtures with `data:image/png;base64,...`, `width: 1000`, and `height: 650`. The tests must encode OUT-01/OUT-02 proof behavior and D-17 through D-19 by keeping debug export as manifest/still helpers only, not normal workflow UI.</action>
  <verify>
    <automated>sh -c 'if pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintDevExport.test.ts; then echo "Expected RED test command to fail before implementation, but it passed"; exit 1; else echo "RED verified: intentionally failing test command failed as expected"; exit 0; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at `app/src/components/physic-paint/physicsPaintDevExport.test.ts`.
    - The automated command fails before Task 2 because `./physicsPaintDevExport` is missing.
    - Assertions include exact strings `physics-paint-debug-export`, `manifest.json`, `frame-0000.png`, and `image/png`.
  </acceptance_criteria>
  <done>RED tests define debug export proof behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement manifest and still export helpers</name>
  <files>app/src/components/physic-paint/physicsPaintDevExport.ts, app/src/components/physic-paint/physicsPaintDevExport.test.ts</files>
  <read_first>
    - /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintDevExport.test.ts
    - /Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
    - /Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-RESEARCH.md
  </read_first>
  <action>Implement `physicsPaintDevExport.ts` as a pure helper module. Use `clampPhysicPaintFrameCount` and `isPhysicPaintRenderedFrame` from `../../types/physicPaint` to validate frame counts and PNG frame payloads. Manifest frames must contain `{ frameIndex, appFrame, file, width, height }` with files named by `makePhysicsPaintFrameFilename`. The manifest filename is `manifest.json`. Do not import `AnimationPlayer`, do not call `engine.exportCompositeCanvas()`, and do not create a headless renderer; this module transforms already captured live-engine frames.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/physicsPaintDevExport.test.ts src/types/physicPaint.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Vitest command passes.
    - `physicsPaintDevExport.ts` exports every symbol listed in this plan's artifact section.
    - Source contains no `new AnimationPlayer`, no `renderFromStrokes`, no `forceDryAll`, and no import from `@efxlab/efx-physic-paint/animation`.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Debug still and manifest helpers are implemented and tested from existing rendered frame payloads.</done>
</task>

</tasks>

<source_audit>
GOAL covered: rendered output can be inspected as still and frame-sequence manifest proof.
REQ covered: OUT-01, OUT-02, UI-REBUILD-02.
RESEARCH covered: Wave 0 `physicsPaintDevExport.test.ts`, no headless batch replay, dev export sidecar of existing capture.
CONTEXT covered: D-16, D-17, D-18, D-19 and the rendered-output proof boundary. Normal artist `Save roto frame`/`Save play` UI wiring is handled in later plans.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered frame payloads to downloaded artifacts | Live engine PNG data URLs become local files/manifest metadata. |
| Frame count/range inputs to manifest | User-adjustable frame count can influence artifact size. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | Tampering | `buildPhysicsPaintDebugManifest` | mitigate | Validate each frame with existing rendered-frame guard and reject frameCount mismatches. |
| T-36-08 | Denial of Service | debug export frame count | mitigate | Use existing `clampPhysicPaintFrameCount` bounds and reject counts outside allowed range. |
| T-36-09 | Information Disclosure | manifest metadata | accept | Manifest contains local layer/operation/frame metadata only and is produced by explicit dev-only UI later. |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package install is planned; keep existing pinned dependencies only per Package Legitimacy Audit. |
</threat_model>

<verification>
Run targeted dev export tests, existing type tests, and app typecheck. Do not run the dev server.
</verification>

<success_criteria>
- Still export helper returns PNG proof metadata for one frame.
- Manifest helper returns bounded `manifest.json` metadata for live captured frames.
- No headless renderer, second engine, or editor integration is introduced.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-03-SUMMARY.md` when done.
</output>
