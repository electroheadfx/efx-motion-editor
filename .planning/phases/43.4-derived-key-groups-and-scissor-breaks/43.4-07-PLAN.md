---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 07
type: execute
wave: 5
depends_on:
  - 43.4-04
  - 43.4-06
files_modified:
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - TERM-01
  - REG-01

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every touched rail surface presents only the Motion Rail / Static Rail / Key Rail family vocabulary: rail tooltip and aria type labels, rail display-name fallbacks, the rail band accessible name 'Rails', the mode-resolved moved-rail status, and the mode-resolved deleted-rail status fallback (D-01)"
    - "Every migrated string's copy tests migrate in the same diff — the UI never forks into two vocabularies (D-01 reversibility note, RESEARCH Pitfall 8)"
    - "The forbidden legacy two-word Group type-label vocabulary and the Key-Group coinage are absent from every user-facing string in the touched modules (UI-SPEC acceptance row 11)"
    - "Untouched surfaces — the Edit Group dialog title, the Scripts panel entry point, and the Actions inspector — are byte-identical (D-01 scope boundary)"
    - "Internal TypeScript, transport, persistence, and planning identifiers keep their Group/loopClip names — no symbol, field, or file is renamed (D-02)"
    - "Locked regression boundaries hold: Motion/Static Group lifecycle, existing physical-key operations, Paint brush history, preview/export parity, and the 43.2 frozen repair paths are untouched (REG-01, RESEARCH Pitfall 9)"
  artifacts:
    - "physicsPaintLoopClipPresentation.ts emits the migrated type labels and display-name fallbacks"
    - "PhysicsPaintLoopClipRail.tsx carries the migrated band accessible name"
    - "useRotoTimelineActions.ts emits the mode-resolved moved-rail status line"
    - "PhysicsPaintStudio.tsx emits the mode-resolved deleted-rail status fallback"
    - "The copy test files covering each site assert the migrated strings"
  key_links:
    - "presentation type label -> rail tooltip/aria -> copy test: the three migrate atomically per surface"
    - "moved/deleted status lines -> product-reason mapper: the rail family stays coherent with plan-04/05 Key Rail copy (D-21)"
---

<objective>
Execute the D-01 terminology migration on exactly the touched rail surfaces — rail type labels, display-name fallbacks, the band accessible name, and the moved/deleted rail status lines — with each surface's copy tests migrating in the same diff, plus the copy-grep gate and the phase's locked-boundary non-modification proofs.

Purpose: D-01 is rated costly-reversible precisely because a partial migration forks the UI into two vocabularies; one task owns the map end-to-end including tests, and a second task proves no residue and no collateral edits. The migration map is enumerated verbatim in 43.4-PATTERNS.md (Terminology migration table) and 43.4-UI-SPEC.md (Copywriting Contract) — those tables are the source of truth for old/new string pairs; do not restate them in source comments.
Output: Coherent Motion Rail / Static Rail / Key Rail vocabulary on every touched surface, copy tests green, untouched surfaces asserted unchanged, regression boundaries proven.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration; no assumption-delta signal; no ORM schema files.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-04-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Terminology migration on the five enumerated copy sites with tests migrating together</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md (Terminology migration table — the five verified sites with exact current/migrated forms; the NOT-touched list)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (Copywriting Contract — Terminology migration map)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (line 35 groupTypeLabel and the display-name fallback builders)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (line 296 band accessible name)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (line 186 moved-status line)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (line 838 deleted-status fallback)
  </read_first>
  <behavior>
    - Test 1 (type labels): the presentation module's rail type label resolves to the Motion Rail / Static Rail family forms for both modes, and every tooltip/accessible-name consumer reflects them
    - Test 2 (display-name fallbacks): the action-name-prefixed fallback and the at-frame fallback render the migrated Rail forms for both modes
    - Test 3 (band accessible name): the rail band group exposes the migrated 'Rails' accessible name
    - Test 4 (status copy): an accepted Group-rail drag publishes the mode-resolved moved-rail sentence; the Group deletion fallback publishes the mode-resolved deleted-rail sentence — both interpolated with the same frame values as before
    - Test 5 (untouched surfaces): the Edit dialog title, the Scripts panel entry point, and the Actions inspector copy are asserted unchanged
  </behavior>
  <reversibility rating="costly">D-01 migration across labels, tooltips, status copy, accessibility names, and copy tests must land atomically — a partial migration forks the UI into two vocabularies; reverting means reverting every touched surface and its tests together.</reversibility>
  <action>RED first: update the copy tests at each site to the migrated expectations (they fail), then migrate the five sites exactly per the PATTERNS.md Terminology migration table — (1) the presentation module's groupTypeLabel union values and every consumer string, (2) the rail display-name fallbacks (action-name-prefixed and at-frame forms, both modes), (3) the rail band group accessible name in PhysicsPaintLoopClipRail.tsx, (4) the moved-status line in useRotoTimelineActions.ts made mode-resolved, (5) the deleted-status fallback in PhysicsPaintStudio.tsx made mode-resolved. Migrate each surface and its tests in the same diff (Pitfall 8). Per D-02 do NOT rename any identifier, type, field, file, intent kind, or persisted key — only user-facing strings change. Do NOT touch the Edit dialog title, the Scripts panel entry point, or the Actions inspector (scope boundary). Do NOT leave the replaced legacy strings behind in comments, test names that assert user-facing copy, or dead branches — the copy-grep gate in Task 2 counts comment lines too. Keep every other behavior at each site byte-identical (mode resolution already exists at the status sites; reuse it).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts app/src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior tests pass with the migrated strings asserted verbatim from the PATTERNS.md table
    - The four suites above exit 0
    - pnpm --dir app typecheck exits 0
    - git diff shows zero identifier/type/file renames and zero changes to the Edit dialog title, Scripts panel, or Actions inspector sources
  </acceptance_criteria>
  <done>Every touched rail surface speaks the Motion Rail / Static Rail / Key Rail family with its copy tests migrated atomically (TERM-01 implementation complete).</done>
</task>

<task type="auto">
  <name>Task 2: Copy-grep gate, untouched-surface assertions, and locked-boundary non-modification proofs</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (Visual Acceptance Matrix row 11 — the automatic-rejection conditions)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pitfall 8 forked vocabularies; Pitfall 9 frozen 43.2 repair paths)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md (43.2 carryover caution in the domain section)
  </read_first>
  <action>Run the copy-residue gate over the touched modules (physicsPaintLoopClipPresentation.ts, PhysicsPaintLoopClipRail.tsx, useRotoTimelineActions.ts, PhysicsPaintStudio.tsx, PhysicsPaintKeyRail.tsx, physicsPaintKeyRailPresentation.ts, PhysicsPaintWorkflowStrip.tsx and their test files): for each legacy two-word Group type-label form and the Key-Group coinage, count occurrences EXCLUDING lines that are internal identifiers (grep -n first, review every hit, then re-count with identifier-only lines filtered — e.g. groupTypeLabel symbol references are legal per D-02; user-facing string literals are not). Every user-facing string literal hit must be zero. Assert the untouched surfaces positively: the Edit dialog title string, the Scripts panel entry-point string, and the Actions inspector strings still appear in their sources (positive greps, count >= 1). Then produce the REG-01 non-modification proofs in the 43.3-04 pattern: git diff --stat for the full phase branch (git merge-base with the phase start) MUST show zero changes to the 43.2 frozen repair paths (project-local save staging, Paint-into-deleted-Group-gap, original-extent Regenerate), zero changes to buildMoveGroupCandidate, clampPhysicPaintGroupDragDestination, deriveMoveGroupIncomingInterpolationBreakKeyIds, buildProposalVersion, buildGroupDragProposalVersion, usePhysicsPaintGroupRailDrag.ts, and the move-key-group/delete-key-group/move-group resolver branches beyond the additive union/dispatch sites; zero changes under app/src-tauri; zero changes to Paint brush history modules. If any proof fails, fix the offending diff before closing the task — do not document around it. Record the gate outputs verbatim in the plan SUMMARY.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view app/src/components/physic-paint/hooks app/src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The copy-residue gate output shows zero user-facing string-literal hits for each legacy form across the touched modules (identifier-only lines documented)
    - Positive greps confirm the Edit dialog title, Scripts panel entry point, and Actions inspector strings remain in their sources
    - The non-modification proof diff shows zero changes to every listed locked path
    - The view/hooks/Studio suites exit 0; pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>No vocabulary residue on touched surfaces, untouched surfaces proven unchanged, and every locked regression boundary from Phases 43-43.3 proven non-modified (TERM-01 gate, REG-01 automated half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy surface -> user trust | Mixed vocabulary on rail surfaces reads as inconsistent product state (UX-trust) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-07-01 | Repudiation (UX-trust) | Terminology migration completeness | medium | mitigate | Surface-plus-tests atomic migration, copy-grep residue gate, and untouched-surface positive assertions (Tasks 1-2, D-01/Pitfall 8) |
| T-43.4-07-02 | Tampering (integrity) | Locked 43/43.1/43.2/43.3 regression boundaries | high | mitigate | Branch-wide non-modification proofs recorded in the SUMMARY; any violation blocks task closure (Task 2, REG-01) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/view app/src/components/physic-paint/hooks app/src/components/physic-paint/PhysicsPaintStudio.test.ts — green
- pnpm --dir app typecheck — exits 0
- Copy-residue gate output recorded in SUMMARY (zero user-facing residue; untouched surfaces positive)
- Branch-wide non-modification proofs recorded in SUMMARY
</verification>

<success_criteria>
The Rail family vocabulary is coherent across every touched surface with tests migrated together, no legacy residue, untouched surfaces proven stable, and the phase's regression boundaries proven byte-clean. The phase is now ready for the history/persistence integration proofs and the frozen-session native UAT (plan 08).
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — this plan migrates existing user-facing strings only (D-02: internal identifiers unchanged)
- Migrated copy test expectations across the four touched test files
- Copy-residue gate output and non-modification proofs recorded in the plan SUMMARY

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-07-SUMMARY.md` when done
</output>
