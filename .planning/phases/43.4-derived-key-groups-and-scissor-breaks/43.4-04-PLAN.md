---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 04
type: execute
wave: 2
depends_on:
  - 43.4-01
files_modified:
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
requirements:
  - KDEL-01
  - KDEL-02

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "classifyRotoDeleteTarget gains a key-rail arm checked BEFORE the ordinary-key-group arm, keyed off the Key Rail selection signal — rail selection routes Delete to Delete Key Rail, physical-frame selection routes to Delete Frame, Motion/Static Rail selection routes to existing Group deletion (D-12)"
    - "Keyboard Delete/Backspace and the visible Delete control converge on the same activation-time classifier; no scope is inferred from cursor position alone and no scope-choice modal exists (D-12, D-13)"
    - "The Delete control's accessible name and guarded tooltip heading carry the live scope: 'Delete Frame' / 'Delete Motion Rail — {displayName}' / 'Delete Static Rail — {displayName}' / 'Delete Key Rail — frames {A}–{B}, {N} keys.' / 'Delete Key Rail — frame {A}, 1 key.'; the visible compact label remains 'Delete' (D-12, UI-SPEC K3)"
    - "Accepted Delete Key Rail publishes exactly 'Deleted Key Rail — frames {A}–{B}, {N} keys. The interval stays an intentional gap.' (single: 'Deleted Key Rail — frame {A}, 1 key. The interval stays an intentional gap.') through runPhysicalAction (D-14)"
    - "Delete Key Rail is fully direct at any key count — no confirmation modal; stale, ambiguous, rejected, or no-op deletion produces no mutation, no success status, and no history entry (D-13, D-15)"
    - "UI-SPEC K3 populated: the control announces exactly one live scope at a time; scopes are mutually exclusive"
    - "UI-SPEC K3 error: stale/ambiguous/no-op deletion rejects before mutation with the mapped reason"
    - "UI-SPEC K3 overflow/long-text: scope strings are fixed-form with bounded tabular-nums frame numbers; no user-supplied text enters the scope string"
  artifacts:
    - "RotoDeleteTarget union and classifyRotoDeleteTarget contain the key-rail arm (useRotoTimelineActions.ts)"
    - "The PhysicalActionRunnerKind Extract and physicalActionAuthorization switch include 'delete-key-rail'"
    - "The Delete control renders the dynamic scope accessible name and tooltip heading (PhysicsPaintWorkflowStrip.tsx)"
  key_links:
    - "Key Rail selection signal -> classifier key-rail arm -> delete-key-rail intent: one activation-time authority shared by keyboard and visible Delete (D-12)"
    - "delete-key-rail acceptance -> D-14 status via the single mapper: preflight and rejection never disagree (D-21)"
---

<objective>
Wire Delete Key Rail into the actions layer: extend the single activation-time delete-scope classifier with a key-rail arm, give the Delete control its dynamic live-scope accessible name and tooltip heading, and route acceptance through runPhysicalAction with the locked D-14 copy.

Purpose: D-12 makes the classifier the only scope authority for both keyboard and visible Delete; extending it (not adding new decision logic in components) keeps preflight, rejection, and announcement in one place (D-21). TDD pins the scope table and routing before the rail host (plan 06) supplies the selection signal end.
Output: Dynamic Delete scope end-to-end in the actions/strip layers, driven by an injected Key Rail selection input the Studio wiring (plan 06) will feed.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration; no assumption-delta signal; no ORM schema files. Reversibility: additive classifier arm and copy — reversible; no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: key-rail classifier arm, runner extension, and D-14 acceptance copy</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 205-216 RotoDeleteTarget union, 232-344 classifyRotoDeleteTarget including the ordinary-key-group arm at 274-285, 626-674 runner Extract/authorization/success constants, 986-1042 delete routing with the keyboard-convergence comment)
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts (lines 90-130 Delete/Backspace routing into actions.deleteRotoKey)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K3 scope table + K4/K5 status copy)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pattern 4 — the pre-existing ordinary-key-group arm serves manual multi-key selection and is NOT relabeled)
  </read_first>
  <behavior>
    - Test 1 (routing): with a Key Rail selection input present, classifyRotoDeleteTarget returns the key-rail arm carrying the segment's member keyIds and span — checked before ordinary-key-group; with physical-frame selection it returns the ordinary arms exactly as today; with Motion/Static Rail selection the existing group arms are unchanged
    - Test 2 (mutual exclusion): the classifier never returns a key-rail arm while physical-frame selection owns the session, and never an ordinary-key arm while a rail selection owns it; no cursor-only inference path exists (D-12)
    - Test 3 (acceptance copy): an accepted key-rail deletion publishes the exact D-14 multiple form with the segment's span and count interpolated, and the single form for a one-key rail
    - Test 4 (fail-closed): a stale key-rail selection (membership no longer matches the accepted derivation) produces no mutation, no success status, and no history entry (D-15)
    - Test 5 (regression): all existing classifier, delete-routing, and keyboard tests pass unmodified
  </behavior>
  <action>Extend the closed RotoDeleteTarget union with Readonly<{ kind: 'key-rail'; firstKeyId: string; keyIds: readonly string[]; firstKeyFrame: number; lastKeyFrame: number }>. In classifyRotoDeleteTarget add the key-rail arm BEFORE the ordinary-key-group arm (L274-285), keyed off a new Key Rail selection input on readRotoDeleteTargetInput (firstKeyId anchor plus the accepted derivation for membership revalidation — plan 06's Studio signal feeds this input; the hook reads it through the same input-interface idiom as the existing selection inputs). Stale membership (the accepted derivation no longer contains a segment with that firstKeyId and identical member list) classifies to a fail-closed arm, never a fallback scope (RESEARCH Open Question 1 pinned: identity = firstKeyId + full member equality). Add 'delete-key-rail' to the PhysicalActionRunnerKind Extract and physicalActionAuthorization; route the arm through runPhysicalAction like the existing arms with requiredKeyId = firstKeyId and successMessage built from the D-14 forms (multiple/single via the shared singular/plural discipline). Extend mapRotoDeleteProductReason with the key-rail unavailable arms so preflight and rejection agree (D-21). Keep the pre-existing ordinary-key-group arm and its 'Keys deleted' copy byte-identical (it serves manual multi-key selection — RESEARCH Pattern 4). Delete/Backspace already converge on this classifier via physicsPaintStudioKeyboard.ts — verify, do not rewire.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The union, classifier, runner Extract, authorization switch, and delete mapper all contain the key-rail arm
    - All five behavior tests pass; the full actions and keyboard suites exit 0
    - pnpm --dir app typecheck exits 0
    - git diff shows zero changes to the ordinary-key-group arm and the existing success constants
  </acceptance_criteria>
  <done>Rail selection routes Delete to the delete-key-rail intent through the single classifier with locked D-14 acceptance copy and fail-closed stale semantics (KDEL-01, KDEL-02 routing half).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dynamic Delete scope on the visible control — accessible name and tooltip heading</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (the existing Delete block beside Paste — same icon-button idiom as the Cut block at 1843-1873; the plan-01 Scissor block immediately before Paste)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (the classifier output this task renders)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K3 contract: accessible name + guarded tooltip heading carry the live scope; visible label stays 'Delete')
  </read_first>
  <behavior>
    - Test 1 (scope strings): the Delete control's aria-label and tooltip heading equal exactly the UI-SPEC K3 scope string for each selection state — physical frame, Motion Rail, Static Rail, Key Rail multiple, Key Rail single
    - Test 2 (exclusivity): the control never announces a rail scope while a physical frame owns selection and never 'Delete Frame' while a rail owns selection
    - Test 3 (bounded copy): scope strings interpolate only bounded tabular-nums frame numbers and counts; the visible compact label remains 'Delete' in every state
    - Test 4 (regression): existing strip Delete tests pass with the updated expectations migrated in the same diff
  </behavior>
  <action>Thread the classifier's scope descriptor from the actions hook to the strip Delete block via the existing prop plumbing. Compute the accessible name and guarded-tooltip heading from the scope table: 'Delete Frame' for a physical frame; 'Delete Motion Rail — {displayName}' / 'Delete Static Rail — {displayName}' for the existing Group rail selections (mode-resolved; these are NEW strings written directly in the D-01 family vocabulary — the legacy Group-vocabulary migration of pre-existing copy is plan 07's scope, do not touch other copy here); 'Delete Key Rail — frames {A}–{B}, {N} keys.' / 'Delete Key Rail — frame {A}, 1 key.' for a Key Rail selection using the inclusive product range derived from the canonical half-open span at presentation time. The visible compact label stays 'Delete' in all states; keep the guarded-reason aria-describedby idiom and the physics-paint-roto-key-icon-button geometry unchanged. Frame numbers in the tooltip copy use tabular-nums exactly like neighboring tooltip copy. Update PhysicsPaintWorkflowStrip.test.ts expectations for the new dynamic names in the same diff (tests and surface migrate together).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Delete control announces exactly one live scope per selection state with the locked strings
    - The visible label remains 'Delete'; geometry, guarded-reason idiom, and toolbar order are unchanged
    - All four behavior tests pass; the strip suite exits 0
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The Delete control exposes its live selection scope through accessible name and tooltip heading with mutually exclusive routing and no modal (KDEL-01 complete; D-12/D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session selection -> classifier | Untrusted session state (possibly stale) determines the destructive operation's scope |
| classifier -> resolver | The key-rail arm's member list crosses into the canonical edit authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-04-01 | Tampering | Delete scope classification | high | mitigate | One activation-time classifier shared by keyboard and visible Delete; membership revalidated against the accepted derivation; stale selection fails closed with no fallback scope (Task 1, D-12/D-15) |
| T-43.4-04-02 | Repudiation | Destructive direct deletion | medium | mitigate | One atomic coordinator command with one Undo restoring keys, breaks, and selection; stale/rejected/no-op paths produce zero history (Task 1, D-13/D-15) |
| T-43.4-04-03 | Information Disclosure (UX-trust) | Scope announcement | low | mitigate | Fixed-form bounded scope strings from the single mapper; no raw resolver diagnostics or stable IDs in copy (Task 2, D-21) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — green
- pnpm --dir app typecheck — exits 0
</verification>

<success_criteria>
Selecting a Key Rail makes Delete, Backspace, and the visible Delete control all announce and execute Delete Key Rail with the locked scope and acceptance copy, direct at any key count, with stale or ambiguous selections failing closed. The rail selection signal itself is plan 06 scope; this plan consumes it through the actions input interface.
</success_criteria>

## Artifacts this phase produces (this plan)

- `RotoDeleteTarget` union member `{ kind: 'key-rail', firstKeyId, keyIds, firstKeyFrame, lastKeyFrame }` and the classifier key-rail arm (useRotoTimelineActions.ts)
- `'delete-key-rail'` in the `PhysicalActionRunnerKind` Extract and `physicalActionAuthorization` switch
- Dynamic Delete scope descriptor plumbing and scope-string builders (useRotoTimelineActions.ts + PhysicsPaintWorkflowStrip.tsx)
- New test blocks: classifier key-rail routing, D-14 acceptance copy, dynamic Delete scope rendering

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-04-SUMMARY.md` when done
</output>
