---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.test.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
requirements:
  - KRAIL-01
  - SCISSOR-01
  - SCISSOR-02
  - GUARD-01

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A pure deriveKeyRailSegments projection maps ordered real keys plus incomingInterpolationBreakKeyIds plus Group ownership into half-open [firstKeyFrame, lastKeyFrame + 1) segments: Group-owned keys excluded, a break splits immediately before its owner, one isolated key yields a one-cell segment, empty frames without a break stay inside one segment (KRAIL-01, D-09)"
    - "The D-10 locked copy family is produced by pure functions in the presentation module: base multiple/single and selected multiple/single forms with correct singular/plural wording, and Scissor is never advertised in rail copy (D-10, D-21)"
    - "A new scissor intent carrying one break-owner keyId round-trips the strict parser and canonical serializer and joins the closed intent union, operation-kind union, and runtime kind guard (43.3 D-14 precedent, GUARD-01)"
    - "Resolving the scissor intent on a real ordinary key returns a proposal whose nextIncomingInterpolationBreakKeyIds is the complete ascending collection plus the owner; Group-owned, unknown, malformed, duplicate, or already-owning targets fail closed with no proposal (SCISSOR-02, D-03)"
    - "Deriving segments from the scissor proposal yields two adjacent segments split at the owner key — derivation and interpolation suppression read the same owner set so they can never disagree (D-06 derivation half, RESEARCH Pattern 1)"
    - "An already-broken Scissor target short-circuits in the UI preflight BEFORE runPhysicalAction: zero mutation, zero status publication, zero history entry (D-04 exact no-op, RESEARCH Pitfall 7)"
    - "UI-SPEC K2 empty: with no eligible target the Scissor button is disabled with the mapped reason and is never hidden or removed from the toolbar order"
    - "UI-SPEC K2 error: disabled preflight and resolver rejection share one product-reason mapper; activation on an existing break leaves history depth unchanged"
    - "UI-SPEC K2 populated: an enabled Scissor shows the locked tooltip 'Split the Key Rail before this key.' and acceptance publishes 'Split Key Rail before frame {N}.' (D-04, D-06)"
    - "The Scissor button sits immediately after Cut labeled 'Scissor' with the SquareSplitHorizontal icon and accessible name 'Split Key Rail'; the Scissors glyph remains exclusive to Cut and the locked toolbar order is otherwise untouched (D-04, 43.2 A-04)"
  artifacts:
    - "app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts exists exporting deriveKeyRailSegments, the KeyRailSegment type, and the D-10 copy builders"
    - "app/src/types/physicPaint.ts contains the scissor union member, its hasOnlyKeys parser branch, its serializer case, and operation-kind validator entries"
    - "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts dispatches the scissor intent kind before the terminal fail-closed guard"
    - "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts exports the scissor availability classifier, product-reason mapper, and scissor action wired through runPhysicalAction"
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx renders the Scissor button between the Cut and Paste blocks"
  key_links:
    - "types union member -> resolver dispatch branch: the terminal exhaustive guard must not fire for the scissor kind (compile + runtime proof)"
    - "scissor proposal break collection -> deriveKeyRailSegments: one accepted cut re-derives one segment into two (the phase's core derivation loop)"
    - "scissor availability mapper -> guarded tooltip -> rejection copy: preflight and rejection can never disagree (D-21)"
---

<objective>
Establish the phase tracer: one end-to-end Scissor slice — pure Key Rail segment derivation, the scissor closed-union intent and resolver branch, the actions-layer availability/no-op preflight, and the toolbar button — so a real ordinary key can be split into two derived Key Rail segments through the canonical resolver with all automated gates green.

Purpose: Tracer-first decomposition (TRACER_MODE=true) proves the derivation projection, closed-union extension, and toolbar-to-resolver path compose correctly before Delete Key Rail, drag, selection, and presentation expansion build on them. TDD mode is enabled: the tracer starts red with failing derivation/transport/resolver tests, and every code-producing task carries explicit behavior expectations first.
Output: Working Scissor split (toolbar button to accepted break to two derived segments), the pure derivation module as the phase foundation, and the locked D-10 copy family.

Planning notes: spec-less probe fallback was skipped visibly at plan-phase init (phase has no requirement IDs in REQUIREMENTS.md; KRAIL/SCISSOR/KDEL/KDRAG/HIST/TERM/GUARD/REG are planner-adopted from RESEARCH.md per the 43.3 GDRAG precedent). No external API integration (detector negative); no assumption-delta signal (detector negative); no ORM schema files in scope. Reversibility: all decisions implemented here are reversible (additive closed-union members, new module, additive button) — no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end derivation-plus-scissor slice — pure Key Rail segment derivation module, scissor transport intent, resolver scissor branch (one path only)</name>
  <files>app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts, app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.test.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (lines 305-312 break collection field, 737-761 strict break parser, 947-960 revision builder default, 1015-1022 break revision participation)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 171-182 operation-kind union, 816-819 runtime kind guard area, 2525-2530 break suppression in the generated-cell projection, 3157 finalizeProposal seam, 3162-3207 delete-key-group branch as the fail-closed idiom model)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (lines 305-344, especially 328-331 startsInterpolationSegment predicate)
    - app/src/types/physicPaint.ts (lines 382-436 intent union, 438-444 hasUniqueBoundedPhysicalKeyIds, 508-580 strict parser, 581+ serializer)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md (derivation code-example shape and closed-union excerpts)
  </read_first>
  <behavior>
    - Test 1 (derivation, KRAIL-01): ordered ordinary real keys with no breaks and no Group ownership derive one segment [firstKeyFrame, lastKeyFrame + 1); a break owned by key K splits one segment into two adjacent segments with the right segment starting at K; Group-owned keys (loopClips sourceKeyIds plus frameOverrides keyIds) never join a segment; one isolated key yields a one-cell segment; empty frames between member keys without a break stay inside one segment (D-09)
    - Test 2 (copy family, D-10): the presentation module emits exactly the four locked forms — base multiple 'Key Rail — frames {A}–{B}, {N} keys.', base single 'Key Rail — frame {A}, 1 key.', selected multiple 'Selected Key Rail — frames {A}–{B}, {N} keys. Drag to move. Delete removes all keys in this rail.', selected single 'Selected Key Rail — frame {A}, 1 key. Drag to move. Delete removes this rail.' — and never advertises Scissor
    - Test 3 (transport round-trip): an intent { kind: 'scissor-key-rail', breakOwnerKeyId } parses via isPhysicPaintRotoPhysicalEditIntent, rejects payloads with excess/missing keys or unbounded IDs, serializes canonically through serializePhysicPaintRotoPhysicalEditIntent, and the operation kind passes the kind validators
    - Test 4 (resolver happy path, SCISSOR-02): resolving scissor on a real ordinary key returns ok with a proposal whose nextIncomingInterpolationBreakKeyIds is the prior collection plus the owner in canonical ascending order, finalized through the unchanged finalizeProposal seam
    - Test 5 (fail-closed): unknown keyId, a Group-owned keyId, malformed/duplicate payloads, or a keyId already owning a break (racing case) return failure resolutions carrying no proposal (D-03, GUARD-01)
    - Test 6 (tracer loop): deriving segments from the accepted scissor proposal of Test 4 yields two adjacent segments split at the owner — the derivation and the resolver read the same owner set
    - Test 7 (regression): all pre-existing resolver, transport, and presentation tests pass unmodified
  </behavior>
  <action>RED first: write the failing tests from the behavior block (new physicsPaintKeyRailPresentation.test.ts plus new describe blocks in the resolver test file and transport assertions). Then implement per D-02/D-03 and RESEARCH Pattern 1/6. In physicsPaintKeyRailPresentation.ts (NEW pure module, no Preact imports in the derivation core): export a frozen KeyRailSegment type with firstKeyId, keyIds (ordered, frozen), firstKeyFrame, lastKeyFrame; export deriveKeyRailSegments(input: { orderedRealKeys: readonly { keyId, appFrame }[] ascending, incomingInterpolationBreakKeyIds: ReadonlySet of string, groupOwnedKeyIds: ReadonlySet of string }) implementing the PATTERNS.md code-example shape — Group-owned key closes any current segment and is skipped, a break owner starts a new segment, output segments are built immutably (never mutate then freeze); export the D-10 copy builders (base/selected x single/multiple) as pure functions over a segment plus availability flags, substituting the exact guarded reason when drag or delete is unavailable and never advertising Scissor. In types/physicPaint.ts: add union member { readonly kind: 'scissor-key-rail'; readonly breakOwnerKeyId: string } (name locked for this phase), a hasOnlyKeys(value, ['kind','breakOwnerKeyId']) parser branch validating with isBoundedPhysicalKeyId, the exhaustive serializer case, and 'scissor-key-rail' entries in the operation-kind union and both kind validators. In the resolver: add 'scissor-key-rail' to PhysicPaintRotoPhysicalEditOperationKind, extend the runtime kind guard, and add a dispatch branch before the terminal fail-closed guard — validate breakOwnerKeyId is bounded, exists in identities, and is NOT Group-owned (loopClips sourceKeyIds + frameOverrides keyIds); fail closed on an already-owned break with an existing suitable closed-union failure code; the candidate sets nextIncomingInterpolationBreakKeyIds to the complete frozen ascending collection plus the owner (complete-collection replacement, never a delta) and finishes through the unchanged finalizeProposal seam. Scissor never creates a key, never touches records, never alters interpolationEnabled. Do NOT modify any existing branch, parser case, or serializer case (regression boundary); do NOT implement key-rail move/delete intents here (plan 03).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.test.ts app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The seven behavior tests pass; physicsPaintKeyRailPresentation.test.ts exists with derivation and D-10 copy coverage
    - types/physicPaint.ts contains the literal kind 'scissor-key-rail' in the intent union, one hasOnlyKeys branch, one serializer case, and both validator lists
    - physicsPaintRotoPhysicalResolver.ts dispatches intent.kind === 'scissor-key-rail' before the terminal fail-closed guard and its branch adds exactly one break owner via complete-collection replacement
    - pnpm --dir app typecheck exits 0 (mapped coordinator ports enumerate the new kind without edits to rotoCoordinatorPorts.ts)
    - git diff shows zero changes inside pre-existing resolver test blocks, parser cases, and serializer cases
  </acceptance_criteria>
  <done>The scissor intent parses, serializes, and resolves a break-split on a real ordinary key end-to-end through the pure resolver, and the accepted proposal re-derives into two adjacent Key Rail segments; all existing suites stay green unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scissor availability classifier, product-reason mapper, no-op preflight, and runPhysicalAction wiring</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 131-151 mapRotoInsertProductReason mapper idiom, 205-216 RotoDeleteTarget union shape, 232-237 classifier docstring, 626-674 PhysicalActionRunnerKind Extract plus physicalActionAuthorization plus success-message constants, 869-948 availability computed-triple idiom and runPhysicalAction body)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K2 copy table — every locked Scissor line)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pitfall 7 no-op short-circuit, Pattern 5 runner extension)
  </read_first>
  <behavior>
    - Test 1 (target resolution order, D-03): the classifier resolves the selected real ordinary key first, else the real ordinary key at the cursor frame; rail selection alone neither arms nor disables Scissor (RESEARCH Open Question 2 pinned: a cursor-frame ordinary key still arms Scissor when a Key Rail is selected)
    - Test 2 (fail-closed order): edit-in-flight then unavailable then generated then empty then group/linked then already-broken then ok; each non-ok arm maps to its locked UI-SPEC K2 sentence
    - Test 3 (no-op preflight, Pitfall 7): an already-broken target makes the scissor action return without calling runPhysicalAction — zero resolver invocation, zero publishStatus, zero executePhysicalEdit
    - Test 4 (acceptance): an ok target routes through runPhysicalAction with the scissor intent, successMessage 'Split Key Rail before frame {N}.' with the target's appFrame interpolated, and rejection copy flowing through the new mapper (D-06)
    - Test 5 (regression): existing insert/delete/paste availability and runner tests pass unmodified
  </behavior>
  <action>Model on computeInsertAvailability + mapRotoInsertProductReason (43.1 pattern). Add a pure scissor target classifier (closed union: 'ok' carrying keyId+appFrame, 'already-owns-break', 'generated', 'empty', 'group-or-linked', 'edit-in-flight', 'unavailable') reading the same accepted snapshot discipline as classifyRotoDeleteTarget (activation-time snapshot; never trust a previously rendered result). Add mapRotoScissorProductReason returning null for 'ok' and the exact UI-SPEC K2 disabled sentences otherwise — empty frame: 'Scissor is unavailable on an empty frame. Select a real ordinary key.'; generated frame: 'Scissor is unavailable on a generated frame. Select a real ordinary key.'; Group frame: 'Scissor is unavailable on a Motion or Static Group frame.'; linked frame: 'Scissor is unavailable on a linked Group frame.'; generic: 'Scissor is unavailable.'; already-broken arm surfaces 'This key already starts a Key Rail segment.' as the guarded-tooltip reason. Expose the computed triple (target, canScissor, disabledReason) following the insert triple idiom. Add 'scissor-key-rail' to the PhysicalActionRunnerKind Extract and physicalActionAuthorization switch; implement the scissorKeyRail action: resolve the target; if 'already-owns-break' return false immediately WITHOUT runPhysicalAction, status, or history (D-04 exact no-op, Pitfall 7); otherwise runPhysicalAction with requiredKeyId = target keyId, the locked D-06 success message, and rejectedCopy delegating to the mapper. Do not add a keyboard shortcut (D-05).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useRotoTimelineActions.ts exports the scissor classifier, mapRotoScissorProductReason, the computed availability triple, and the scissorKeyRail action
    - The no-op test proves zero resolver/status/history interaction on an already-broken target
    - All five behavior tests pass; the full useRotoTimelineActions.test.ts suite exits 0
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Scissor availability, guarding, no-op semantics, and acceptance status flow through one mapper and the existing runner authority with correct locked copy at every arm (D-03, D-04, D-06, D-21).</done>
</task>

<task type="auto">
  <name>Task 3: Scissor toolbar button after Cut with guarded tooltip</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 1 icon imports, 320-322 buildGuardedActionTooltipCopy, 1843-1873 the Cut block as the byte-level template, 1872 the Paste block insertion boundary)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (existing toolbar assertion idioms)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K2 geometry + copy table)
    - app/node_modules/lucide-preact/dist/lucide-preact.d.ts (confirm SquareSplitHorizontal export — verified at line 18692 per RESEARCH)
  </read_first>
  <action>Insert the Scissor block between the Cut block (~L1843-1873) and the Paste block (~L1872) so the visible order becomes Copy | Cut | Scissor | Paste | Delete; every other toolbar position stays byte-identical (43.2 A-04). Copy the Cut block's structure exactly: physics-paint-roto-key-icon-action span with tooltip pointer enter/leave; button physics-paint-roto-key-icon-button with aria-label 'Split Key Rail', conditional aria-disabled and aria-describedby='roto-key-action-reason-scissor', guarded onClick that hides the tooltip and returns early when disabled before invoking the new scissor action prop, onKeyDown preventDefault for disabled Enter/Space, an 18px SquareSplitHorizontal icon imported from lucide-preact (the Scissors import and glyph remain exclusive to Cut — do not reuse), a physics-paint-roto-key-icon-label span reading 'Scissor', the sr-only reason span, and a PhysicsPaintStyledTooltip region 'bottom' rendering buildGuardedActionTooltipCopy('Split the Key Rail before this key.', scissorDisabledReason). Wire the new props (canScissor, scissorDisabledReason, onScissorKeyRail) from the actions hook through the strip's existing prop plumbing idiom. Extend PhysicsPaintWorkflowStrip.test.ts with: button renders after Cut and before Paste; disabled state carries the mapped reason in the sr-only span and tooltip; enabled activation invokes the prop exactly once. No keyboard shortcut registration anywhere (D-05); the button follows the paint-mode shortcut/adjacency guards already in force for the key toolbar.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Scissor button renders between Cut and Paste with visible label 'Scissor', accessible name 'Split Key Rail', and the SquareSplitHorizontal icon
    - Disabled state publishes the mapped reason through aria-describedby plus the guarded tooltip; enabled tooltip is exactly 'Split the Key Rail before this key.'
    - Strip tests pass including the new order/guard assertions; the existing strip suites exit 0 unmodified
    - pnpm --dir app typecheck exits 0
    - grep -c "Scissors" on the Scissor block region returns 0 (icon exclusivity): node -e check or git diff shows the Scissors import untouched and used only by the Cut block
  </acceptance_criteria>
  <done>The Scissor button sits in the locked toolbar order with the distinct icon, guarded tooltip, and accessible name; activation drives the plan-01 action path (SCISSOR-01 complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child view -> resolver | Untrusted toolbar/cursor-derived break-owner keyId crosses into the pure canonical edit authority |
| child -> parent bridge | Intent + proposal cross the transport; parent revalidates before store replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-01-01 | Tampering | scissor intent parsing (types/physicPaint.ts) | high | mitigate | hasOnlyKeys allowlist branch + isBoundedPhysicalKeyId; no normalization/coercion; exhaustive serializer switch fails compile on a missed case (Task 1) |
| T-43.4-01-02 | Tampering | scissor resolver branch | high | mitigate | Fail-closed on unknown/Group-owned/already-owning/malformed targets; candidate flows through the unchanged finalizeProposal seam; complete-collection break replacement prevents duplicate/dangling owners (Task 1) |
| T-43.4-01-03 | Repudiation | no-op preflight path | medium | mitigate | Already-broken activation produces zero mutation, zero status, zero history — proven by a test spying on runPhysicalAction/publishStatus/executePhysicalEdit (Task 2, D-04) |
| T-43.4-01-04 | Tampering (integrity) | Locked regression boundary (existing intents/branches/Cut glyph) | high | mitigate | Purely additive union member, dispatch branch, mapper, and button; acceptance criteria forbid diffs inside pre-existing blocks and Scissors-glyph reuse (Tasks 1-3) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — green
- pnpm --dir app typecheck — exits 0
- git diff confirms zero modifications to pre-existing resolver branches, parser/serializer cases, runner arms, and the Cut toolbar block
</verification>

<success_criteria>
A real ordinary key can be split via the Scissor toolbar button: availability guards every ineligible target with the locked mapped reason, an already-broken target is a silent exact no-op, an accepted cut adds exactly one stable-key-owned break through the canonical resolver and re-derives one segment into two, and the status line publishes 'Split Key Rail before frame {N}.'. Delete Key Rail, Key Rail drag, rail rendering, and selection are explicitly later-plan scope and absent here.
</success_criteria>

## Artifacts this phase produces (this plan)

- New module `app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts`: `KeyRailSegment` type, `deriveKeyRailSegments`, D-10 copy builder functions
- Intent union member `{ kind: 'scissor-key-rail', breakOwnerKeyId }` plus operation-kind literal `'scissor-key-rail'` in `PhysicPaintRotoPhysicalEditOperationKind` and validators (app/src/types/physicPaint.ts)
- New resolver dispatch branch for `intent.kind === 'scissor-key-rail'` (physicsPaintRotoPhysicalResolver.ts)
- Scissor classifier, `mapRotoScissorProductReason`, availability computed triple, and the scissor action (useRotoTimelineActions.ts)
- Scissor toolbar button with `SquareSplitHorizontal` icon between Cut and Paste (PhysicsPaintWorkflowStrip.tsx)
- New test files/blocks: physicsPaintKeyRailPresentation.test.ts, scissor resolver describe block, transport round-trip assertions, scissor availability specs, strip button specs

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-01-SUMMARY.md` when done
</output>
