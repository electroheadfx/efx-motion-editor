---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 05
type: execute
wave: 3
depends_on:
  - 43.4-03
files_modified:
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
autonomous: true
requirements:
  - KDRAG-02
  - HIST-01
  - GUARD-01

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "prepareKeyRailDrag retains an immutable publication whose proposalVersion fingerprint includes the incoming break collection; commit re-checks the version and bails silently on staleness (RESEARCH Pitfall 2 — the break collection reaches BOTH the resolver input and the fingerprint)"
    - "commitKeyRailDrag forwards exactly the retained objects to executePhysicalEdit with zero recomputation — one atomic parent-acknowledged transaction per accepted drag (D-24, HIST-01)"
    - "An accepted drag publishes 'Moved Key Rail to frame {N}.' plus 'Gap left at frames {A}–{B}.' when a gap opened; a zero-space drop publishes exactly 'No empty space in that direction.' via the shared no-free-space-in-direction mapping (D-18, D-20)"
    - "The prepare/commit pair never routes through runPhysicalAction (its bounded Extract intentionally excludes drag commits) and carries the wrapper coherence guards: operation kind, intent kind, identity match, non-empty launch tuple (43.3 pattern)"
    - "Stale document authority between prepare and commit (records, interpolation, loopClips, OR breaks changed) rejects before mutation with zero state change (GUARD-01)"
    - "UI-SPEC K4/K5 status rows: all drag status copy is fixed-form with bounded frame numbers; rejection leaves every visible fact unchanged"
  artifacts:
    - "useRotoTimelineActions.ts exports prepareKeyRailDrag and commitKeyRailDrag plus a break-aware proposal-version helper for key-rail drags"
    - "The drag status mapper covers the D-18/D-20 Key Rail forms"
  key_links:
    - "prepare -> resolver (move-key-rail) -> retained publication -> commit -> executePhysicalEdit: one atomic transaction path (HIST-01)"
    - "break-inclusive fingerprint -> commit re-check: stale break authority can never be committed (Pitfall 2)"
---

<objective>
Wire Key Rail drag into the canonical edit path: a dedicated prepare/commit publication pair modeled byte-closely on the 43.3 group-drag pair, with the break collection threaded into both the resolver input and the staleness fingerprint, plus the locked drag status copy through the single product-reason mapper.

Purpose: The plan-02 session hook and plan-03 clamp/resolver semantics meet here. The prepare/commit idiom is the phase's atomicity guarantee (D-24) — TDD pins the fingerprint discipline (Pitfall 2 is the carried 43.3 Pitfall 1) before plan 06 mounts the gesture.
Output: prepareKeyRailDrag/commitKeyRailDrag exports with full staleness and coherence coverage, and the drag status mapper.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration; no assumption-delta signal; no ORM schema files. Reversibility: additive pair — reversible; no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-01-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prepareKeyRailDrag / commitKeyRailDrag publication pair with break-inclusive fingerprint</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 1370-1489 prepareRotoGroupDrag/commitRotoGroupDrag — the byte-close model; 1428-1432 break-inclusive fingerprint site; 1448-1465 coherence guards and staleness re-check; 1481-1487 status continuation; 626-629 the runner Extract this pair must NOT join)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (lines 947-960 revision builder whose 4th parameter defaults to empty breaks — the Pitfall 2 hazard)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md (Pattern 5 prepare/commit idiom)
  </read_first>
  <behavior>
    - Test 1 (guard order): prepare rejects with a product reason when launch is absent, ports are absent, or an operation is in flight — same order as prepareRotoGroupDrag
    - Test 2 (fingerprint): prepare threads input.getIncomingInterpolationBreakKeyIds?.() into BOTH the resolver input and a break-aware proposalVersion; two preparations identical except break authority produce different versions (Pitfall 2)
    - Test 3 (no-change rejection): a drag whose resolver result is a no-op (destination equals current) is rejected at prepare with no publication
    - Test 4 (commit coherence): commit returns false on kind/intent mismatch or empty launch tuple; on staleness (recomputed version differs) it returns false silently with zero mutation and zero status
    - Test 5 (commit forward): on success commit forwards exactly the retained { proposal, expectedLaunch, operationKind: 'move-key-rail', intent, selectedKeyId, selectedAppFrame } to executePhysicalEdit with no recomputation (object identity)
    - Test 6 (regression): existing prepare/commit tests for move-key, move-key-group, and move-group pass unmodified; buildGroupDragProposalVersion and buildProposalVersion are byte-unchanged
  </behavior>
  <action>Model byte-closely on prepareRotoGroupDrag/commitRotoGroupDrag. prepareKeyRailDrag(firstKeyId, destinationFirstKeyAppFrame): identical guard order; read the coherent snapshot (records, interpolation, capacity, loopClips, incomingInterpolationBreakKeyIds); resolve the dragged segment's member list from the plan-01 derivation over the accepted document (stale or non-segment firstKeyId rejects fail-closed); build the move-key-rail intent; invoke resolvePhysicPaintRotoPhysicalEdit with the break collection included; reject no-change results; compute proposalVersion with a NEW break-aware helper (e.g. buildKeyRailDragProposalVersion) passing records, interpolation, loopClips, AND the break collection as the 4th revision argument — do NOT change buildProposalVersion or buildGroupDragProposalVersion or their call sites (regression boundary). Freeze the publication exactly like the analog. commitKeyRailDrag(publication): verify operationKind === 'move-key-rail' and intent.kind === 'move-key-rail', run the same shallow member-equality and launch-tuple checks, recompute the version from current authority and bail silently on mismatch (try/catch returns false), then forward the retained objects to input.executePhysicalEdit with zero recomputation. Do not route through runPhysicalAction.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useRotoTimelineActions.ts exports prepareKeyRailDrag and commitKeyRailDrag plus the dedicated break-aware version helper
    - All six behavior tests pass; the full actions suite exits 0
    - pnpm --dir app typecheck exits 0
    - git diff shows zero changes to buildProposalVersion, buildGroupDragProposalVersion, and the existing prepare/commit pairs
  </acceptance_criteria>
  <done>The Key Rail drag prepare/commit pair retains immutable publications with break-aware staleness detection and commits through the existing coordinator as one atomic transaction (KDRAG-02 wiring, HIST-01 substrate).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Key Rail drag status mapper — D-18 acceptance and D-20 rejection copy</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 174-195 mapRotoGroupDragProductReason including the L182-183 no-free-space-in-direction mapping to reuse verbatim; L186 the moved-status line — its legacy Group wording migrates in plan 07, NOT here; 1481-1487 the .then status continuation idiom)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K4/K5 status table)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md (D-18, D-19, D-20 verbatim)
  </read_first>
  <behavior>
    - Test 1 (acceptance, no gap): an accepted move publishes exactly 'Moved Key Rail to frame {N}.' with the destination first-key frame interpolated
    - Test 2 (acceptance, gap opened): when the vacated interval is non-empty the status appends 'Gap left at frames {A}–{B}.' with the inclusive product range (43.3 D-07 adapted)
    - Test 3 (rejection): a zero-space drop publishes exactly 'No empty space in that direction.' — the same sentence as every other rail kind (D-20), mapped from the shared no-free-space-in-direction failure code
    - Test 4 (post-commit stability): the commit continuation requests moved-rail selection retention and canvas reconciliation only when the current frame's content changed — no automatic navigation (D-19; the selection/cursor effects are asserted as the same port calls the group-drag continuation makes)
    - Test 5 (regression): existing group-drag status tests pass unmodified
  </behavior>
  <action>Add a key-rail arm to the drag status mapping following mapRotoGroupDragProductReason: reuse the existing no-free-space-in-direction -> 'No empty space in that direction.' mapping (the plan-03 resolver branch already emits that code — assert the code path, do not duplicate the sentence in a second mapper); build the acceptance continuation in commitKeyRailDrag's .then idiom publishing 'Moved Key Rail to frame {N}.' and appending ' Gap left at frames {A}–{B}.' when the publication's proposal shows a vacated interval (same vacated-interval detection the group-drag continuation uses). Wire the post-commit continuation to the same selection-retention and conditional-canvas-reconciliation ports the group drag uses (D-19 verbatim — the Key Rail selection retention target is the rail signal plan 06 introduces; consume it through the input interface so this plan's tests stub it). Keep all copy fixed-form with bounded tabular-nums frame numbers; raw resolver diagnostics never enter product copy (D-21).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior tests pass with the exact locked sentences asserted verbatim
    - The no-space sentence appears once in the mapper layer (shared mapping reused)
    - The full actions suite exits 0; pnpm --dir app typecheck exits 0
    - git diff shows zero changes to mapRotoGroupDragProductReason's existing arms
  </acceptance_criteria>
  <done>Accepted and rejected Key Rail drags publish the locked D-18/D-20 copy through the single mapper with D-19 post-commit stability wired (KDRAG-02 complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepare-time snapshot -> commit-time authority | Document authority (including breaks) may change between prepare and commit |
| commit -> parent store | Retained publication crosses the bridge; parent revalidates before replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-05-01 | Tampering / Repudiation | proposalVersion staleness check | high | mitigate | Break-inclusive revision fingerprint (records + interpolation + loopClips + breaks) recomputed at commit; mismatch bails silently with zero mutation (Task 1, Pitfall 2) |
| T-43.4-05-02 | Tampering | Wrapper coherence at commit | high | mitigate | Operation-kind, intent-kind, identity-match, and launch-tuple guards before executePhysicalEdit; retained objects forwarded by identity (Task 1) |
| T-43.4-05-03 | Information Disclosure (UX-trust) | Drag status copy | low | mitigate | Fixed-form bounded copy via the single mapper; shared no-space sentence; no raw diagnostics (Task 2, D-21) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — green
- pnpm --dir app typecheck — exits 0
- git diff confirms zero modifications to buildProposalVersion, buildGroupDragProposalVersion, the existing prepare/commit pairs, and mapRotoGroupDragProductReason's existing arms
</verification>

<success_criteria>
A Key Rail drag gesture (once plan 06 mounts it) commits as one atomic parent-acknowledged transaction with break-aware staleness protection, the locked moved/gap/no-space copy, and D-19 post-commit stability. Full Undo/Redo and save/reopen proofs are plan 08 scope.
</success_criteria>

## Artifacts this phase produces (this plan)

- `prepareKeyRailDrag` / `commitKeyRailDrag` exports (useRotoTimelineActions.ts)
- Break-aware proposal-version helper for key-rail drags, e.g. `buildKeyRailDragProposalVersion` (useRotoTimelineActions.ts)
- Key Rail arms of the drag status mapper covering the D-18/D-20 forms
- New test blocks: prepare/commit guard order, fingerprint divergence, coherence, staleness, status copy

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-05-SUMMARY.md` when done
</output>
