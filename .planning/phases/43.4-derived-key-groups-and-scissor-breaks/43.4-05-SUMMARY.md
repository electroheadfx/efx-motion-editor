---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 05
subsystem: physics-paint-roto
tags: [typescript, preact, vitest, key-rail, drag, resolver, atomic-commit]
status: complete

requires:
  - phase: 43.4-01
    provides: Pure derived Key Rail segment projection
  - phase: 43.4-03
    provides: Strict move-key-rail transport, clamp, and resolver semantics
provides:
  - Immutable prepareKeyRailDrag and commitKeyRailDrag publication pair
  - Break-inclusive Key Rail drag staleness fingerprint
  - Exact retained-object forwarding through one parent-acknowledged transaction
  - Locked Key Rail moved, gap, and no-space product copy
affects: [43.4-06, 43.4-07, 43.4-08, key-rail-ui, roto-history]

actuals:
  tokens: 7309
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - Dedicated immutable prepare/commit publication outside runPhysicalAction
    - Break-aware authority fingerprint recomputed before commit
    - Product-copy delegation that reuses the shared no-space literal

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts

key-decisions:
  - "Key Rail drag derives and freezes the exact current segment once at prepare time; commit validates wrapper coherence and forwards the retained proposal and intent without resolver recomputation."
  - "The Key Rail fingerprint includes records, interpolation, Loop Clips, incoming breaks, and launch identity so every structural authority change fails closed before mutation."
  - "Key Rail rejection copy delegates to the existing Group drag mapper, keeping the shared no-space sentence in one literal source while preserving existing Group mapper arms byte-for-byte."

patterns-established:
  - "Key Rail drag authority: derive exact segment, resolve once, freeze publication, re-fingerprint current authority, then dispatch retained objects once."
  - "Vacated-gap presentation: retain the non-overlapping half-open source interval and convert its end to an inclusive product range only in the mapper."

requirements-completed: [KDRAG-02, HIST-01, GUARD-01]

coverage:
  - id: D1
    description: Immutable Key Rail drag prepare/commit publication with exact derived membership and retained-object forwarding
    requirement: HIST-01
    verification:
      - kind: unit
        ref: app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#useRotoTimelineActions Key Rail drag prepare/commit publication pair
        status: pass
      - kind: other
        ref: pnpm --dir app typecheck
        status: pass
    human_judgment: false
  - id: D2
    description: Break-inclusive staleness rejection across records, interpolation, Loop Clips, breaks, and launch authority
    requirement: GUARD-01
    verification:
      - kind: unit
        ref: app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#rejects incoherent publications and every stale structural authority silently
        status: pass
    human_judgment: false
  - id: D3
    description: Locked Key Rail moved, vacated-gap, and shared no-space status copy published only after accepted commit
    requirement: KDRAG-02
    verification:
      - kind: unit
        ref: app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#useRotoTimelineActions Key Rail drag status and post-commit stability
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-08-15
---

# Phase 43.4 Plan 05: Key Rail Drag Action and Status Summary

**Break-aware immutable Key Rail drag publications now commit exactly once through the coordinator and emit locked moved, gap, or no-space copy.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-15T16:45:22Z
- **Completed:** 2026-08-15T16:53:34Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `prepareKeyRailDrag` and `commitKeyRailDrag` to the physical action bundle without widening `runPhysicalAction`.
- Derived exact ordinary Key Rail membership from accepted records, breaks, and Group ownership, then retained a frozen publication containing the exact resolver proposal, intent, launch tuple, member identities, accepted destination, and vacated interval.
- Exported `buildKeyRailDragProposalVersion`, whose physical revision explicitly includes incoming interpolation breaks and is recomputed at commit against all current structural authority.
- Added fail-closed operation-kind, intent-kind, first-key, ordered-member, launch-tuple, and staleness guards before coordinator mutation.
- Added locked `Moved Key Rail` destination/gap copy and delegated no-space rejection to the existing shared literal mapping.

## Task Commits

Each TDD task was committed as an atomic RED/GREEN pair:

1. **Task 1 RED: Key Rail drag publication tests** - `57bf9aad` (test)
2. **Task 1 GREEN: prepare/commit publication pair** - `879b5841` (feat)
3. **Task 2 RED: Key Rail drag status tests** - `71929233` (test)
4. **Task 2 GREEN: accepted/rejected status mapping** - `af2b6151` (feat)

## Files Created/Modified

- `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` - Adds Key Rail publication types, prepare/commit actions, break-aware versioning, vacated-gap facts, and product status mapping.
- `app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts` - Adds guard-order, fingerprint, resolver-input, coherence, stale-authority, retained-identity, status-copy, and accepted-only publication coverage.

## Decisions Made

- Preparation owns exact derived-segment authorization. The resolver receives the same frozen ordered member array, preventing the action layer and resolver from interpreting Key Rail membership differently.
- Commit performs no resolver or mapping recomputation. It only validates the retained wrapper, re-fingerprints current authority, and forwards the exact retained objects to `executePhysicalEdit`.
- Vacated-gap facts retain only the non-overlapping source edge exposed by the rigid translation. The mapper converts the half-open interval to the inclusive user-facing range.
- Existing `mapRotoGroupDragProductReason`, Group prepare/commit actions, `buildProposalVersion`, and `buildGroupDragProposalVersion` remain unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Completed direct fingerprint test launch fixtures**
- **Found during:** Task 1 GREEN typecheck
- **Issue:** The direct `buildKeyRailDragProposalVersion` tests supplied only operation and layer IDs, while `PhysicPaintLaunchContext` also requires `startFrame`.
- **Fix:** Added the required bounded `startFrame: 0` fact to both direct helper fixtures.
- **Files modified:** `app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts`
- **Verification:** `pnpm --dir app typecheck`
- **Committed in:** `879b5841`

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fixture correction was required for the existing launch contract and added no production scope.

## Issues Encountered

- The plan's example test path included `app/src/...` after `pnpm --dir app`; execution used the repository's established `src/...` path so one-shot Vitest resolved the file correctly.

## Verification

- `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts` — 59 tests passed.
- `pnpm --dir app typecheck` — passed.
- Locked-boundary comparison against `ec9238cc` — `mapRotoGroupDragProductReason`, `prepareRotoGroupDrag`, `commitRotoGroupDrag`, `buildProposalVersion`, and `buildGroupDragProposalVersion` are unchanged.
- Shared no-space copy scan — `No empty space in that direction.` appears once in the mapper layer.
- Stub scan — no new TODO, FIXME, placeholder, mock-data, or unwired UI stub introduced.
- Threat surface scan — no new network, authentication, file-access, persistence, or schema trust boundary introduced beyond the plan's action/coordinator boundary.

## TDD Gate Compliance

- Task 1 has a failing `test(43.4-05)` RED commit followed by a passing `feat(43.4-05)` GREEN commit.
- Task 2 has a failing `test(43.4-05)` RED commit followed by a passing `feat(43.4-05)` GREEN commit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 06 can mount the gesture onto the dedicated prepare/commit pair and consume the retained accepted destination and vacated-gap facts.
- Plan 08 can prove Undo/Redo and persistence knowing each accepted drag enters history as one parent-acknowledged physical edit transaction.
- Automated verification is complete; native visible UAT remains owned by the later phase verification workflow.

## Self-Check: PASSED

- Both modified implementation/test files and this summary exist.
- All four RED/GREEN task commits exist.
- Targeted one-shot Vitest and application typecheck pass.
- The pre-existing untracked `.gitkeep` remains untouched.

---
*Phase: 43.4-derived-key-groups-and-scissor-breaks*
*Completed: 2026-08-15*
