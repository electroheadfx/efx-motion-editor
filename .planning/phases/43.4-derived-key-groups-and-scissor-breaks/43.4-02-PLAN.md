---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts
autonomous: true
requirements:
  - KDRAG-01

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A pointer press on a Key Rail target that moves more than 4px horizontally starts a drag session with pointer capture on the rail target, cursor grabbing, and a paint-only gray ghost state; a release at or below 4px resolves as plain click selection behavior (D-16)"
    - "Escape during a session cancels with zero state change, zero history entry, and immediate preview removal; selection and focus are unchanged (D-16)"
    - "The post-drop browser click event cannot re-fire Key Rail selection — the consumeClickSuppression idiom is forked with the session (RESEARCH Pitfall 6)"
    - "Ghost state is presentation-only: fixed gray Key Rail paint at 55% opacity, blockedEdge left/right/null, consumed for paint only and never by the commit path (D-16, parent-authoritative rule)"
    - "The session is unit-testable in node via an injected windowLike surface and a source-element interface — no real window or DOM required (43.3 hook test precedent)"
    - "UI-SPEC K4 ghost populated consideration: the ghost state carries the dragged segment's exact geometry at the clamped proposed destination while the original rail stays fully visible"
  artifacts:
    - "app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts exists exporting the session hook, KEY_RAIL_DRAG_THRESHOLD_PX, KeyRailDragGhostState, KeyRailDragPreviewState, KeyRailDragWindowLike, and KeyRailDragSourceElement"
    - "app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts exists with threshold/capture/Escape/suppression coverage"
  key_links:
    - "rail pointer-down -> session hook -> prepare/commit callback ports: the gesture reaches the canonical edit path without owning resolver logic (the hook never imports the resolver)"
    - "threshold crossing -> pending click timer cancellation + click suppression: post-drop click re-fire is impossible (Pitfall 6)"
---

<objective>
Create the Key Rail drag session state machine as a focused sibling hook forked from usePhysicsPaintGroupRailDrag — 4px horizontal threshold, pointer capture, Escape cancel, click suppression, gray ghost/preview state — with clip concepts (loopId, ranges, duplicated placement, effectiveZero) removed.

Purpose: The session hook is self-contained (injectable window/source element, callback ports for prepare/commit), so it can be built and fully tested in parallel with the plan-01 tracer without touching any shared file. Plan 06 mounts it; plan 05 supplies the prepare/commit pair it calls.
Output: A node-testable drag session hook with complete gesture coverage, ready to be wired to the rail host and the prepare/commit pair.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration (detector negative); no assumption-delta signal; no ORM schema files. Reversibility: new module only — reversible; no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Key Rail drag session state machine — threshold, capture, Escape, click suppression</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts (entire file — the fork source: threshold constant at line 17, ghost/preview state shapes lines 20-44, windowLike/source-element injection lines 47-80, session lifecycle)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (lines 127-145 consumeClickSuppression call site, 147-151 gesture mapping to reduce to plain-only)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K4 interaction table — every input/result row)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pitfall 6 post-drop click, anti-pattern: do not parameterize the original into a god-hook)
  </read_first>
  <behavior>
    - Test 1 (threshold): pointer-down then movement of 4px or less followed by release starts no session and leaves click behavior to the rail; movement beyond 4px horizontally starts the session exactly once
    - Test 2 (capture + cursor): on session start the source element receives setPointerCapture with the pointer id and the cursor contract switches to grabbing; release releases capture
    - Test 3 (Escape): Escape mid-session cancels — ghost/preview state cleared, no commit callback invoked, zero state change observable afterward
    - Test 4 (click suppression): after a drop, the subsequent click event on the capture target is consumed (consumeClickSuppression returns true exactly once) and any pending click timer was cancelled at threshold crossing
    - Test 5 (guards): non-primary pointer, non-left button, or meta/ctrl/shift-modified pointer-down never arms the session; a second session cannot start while one is active
  </behavior>
  <action>Fork usePhysicsPaintGroupRailDrag.ts into the new sibling module per PATTERNS.md (do NOT edit the original — 43.3 regression boundary). Export KEY_RAIL_DRAG_THRESHOLD_PX = 4. Copy verbatim: the windowLike injection interface (renamed KeyRailDragWindowLike), the source-element interface (KeyRailDragSourceElement), the arming guards (isPrimary, button 0, no modifiers), window pointermove/pointerup/pointercancel listeners plus capture-phase Escape keydown and lostpointercapture handling, threshold crossing semantics, pointer capture, click-suppression state with a consume-once API, and pending-click-timer cancellation at threshold crossing. Remove: loopId/ranges/presentation ports, effectiveZero, the duplicated-placement branch, and any loop-identity concept — every Key Rail drag is source-attached whole-segment rigid translation (D-17). The ghost state shape keeps active/left/width/blockedEdge but mode is removed (the Key Rail ghost is always the fixed gray paint; the CSS class carries the color). Session state lives in signals/refs inside the hook — never useEffect-mirrored state (CLAUDE.md Preact directives). The hook accepts callback ports (onSessionStart preparing the publication, onDropCommit receiving the retained publication, onCancel) but never imports the resolver or store.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - usePhysicsPaintKeyRailDrag.ts exists with the exported constant, state types, and injection interfaces named above
    - All five behavior tests pass against the injected windowLike/source-element doubles
    - git diff shows zero changes to usePhysicsPaintGroupRailDrag.ts
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The Key Rail drag session starts past 4px, captures the pointer, cancels cleanly on Escape, and suppresses the post-drop click — fully covered in node tests with the original Group hook byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ghost/preview publication state exposure for strip paint</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts (ghost/preview docstrings lines 20-44 — the presentation-only contract to carry)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 895-935 the group-drag gap preview consumption idiom the strip will mirror)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K4 drag preview geometry: ghost paint-only 3px at clamped destination, blocked-edge 2x12px bar side, gap preview cells)
  </read_first>
  <behavior>
    - Test 1 (ghost state): during a session the exposed ghost state carries left/width of the proposed destination geometry and blockedEdge left/right/null exactly as reported by the injected clamp port; when not clamped blockedEdge is null
    - Test 2 (preview publication): the preview state retains the immutable publication object handed in by the prepare port and is consumed for paint only; the commit path receives the same retained object identity (no recomputation)
    - Test 3 (cleanup): commit, cancel, or rejection clears ghost and preview state synchronously; no residual paint state is observable
    - Test 4 (clamp port): the session recomputes the proposed destination from live pointer position via an injected destination-projection port (18px pitch) and an injected clamp port — both are caller-supplied, keeping the hook pure of resolver imports
  </behavior>
  <action>Add to the Task-1 module: KeyRailDragGhostState { active, left, width, blockedEdge } and KeyRailDragPreviewState { publication } with the same presentation-only docstring discipline as the analog (consumed for paint only; never canonical revision, history, or persistence). Wire the destination pipeline as injected ports: a pointer-to-frame projection (the rail host supplies the 18px pitch projection) and a clamp function port (plan 05 wires clampPhysicPaintKeyRailDragDestination from the resolver — the hook only sees the port signature). On pointermove during a session, recompute proposed destination, invoke the clamp port, and update ghost state including blockedEdge. On drop, pass the retained publication to the commit port; on Escape or commit failure clear all session paint state and restore focus to the source element. Keep every preview field presentation-only — nothing here mutates the document or publishes status.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The module exposes ghost/preview state with the exact shapes named above and injected projection/clamp ports
    - All four behavior tests pass; the full hook suite exits 0
    - The module contains no import from the resolver, store, or model layers (grep the import block: zero matches for physicsPaintRotoPhysicalResolver, physicPaintStore, physicsPaintRotoPhysicalModel)
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The session exposes presentation-only ghost/clamp-edge/preview state driven by injected ports, ready for the strip's paint layer and the plan-05 prepare/commit pair (KDRAG-01 session half complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer events -> session state | Untrusted gesture input drives presentation-only session state |
| session -> commit port | The retained immutable publication crosses to the canonical edit path; the session never recomputes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-02-01 | Tampering | Session state machine | medium | mitigate | Arming guards (primary/left-button/no-modifier), single-active-session enforcement, pointer capture, and Escape zero-state-change cancel (Task 1, D-16) |
| T-43.4-02-02 | Repudiation (UX-trust) | Ghost/preview paint | medium | mitigate | Preview state is presentation-only and cleared on commit/cancel/rejection; the commit consumes only the retained publication — proven by object-identity test (Task 2) |
| T-43.4-02-03 | Tampering (integrity) | 43.3 Group-rail regression boundary | high | mitigate | Sibling fork: acceptance criterion requires zero diff on usePhysicsPaintGroupRailDrag.ts (Task 1) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts — green
- pnpm --dir app typecheck — exits 0
- git diff confirms zero modifications to usePhysicsPaintGroupRailDrag.ts and PhysicsPaintLoopClipRail.tsx
</verification>

<success_criteria>
The Key Rail drag session hook exists as a node-tested sibling module with threshold/capture/Escape/suppression semantics matching the 43.3 contract verbatim and presentation-only ghost/preview state exposed through injected ports. No UI mounts it yet (plan 06) and no prepare/commit pair exists yet (plan 05).
</success_criteria>

## Artifacts this phase produces (this plan)

- New module `app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts`: `KEY_RAIL_DRAG_THRESHOLD_PX`, `KeyRailDragGhostState`, `KeyRailDragPreviewState`, `KeyRailDragWindowLike`, `KeyRailDragSourceElement`, the session hook export, and its consume-once click-suppression API
- New test file `app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts`

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-02-SUMMARY.md` when done
</output>
