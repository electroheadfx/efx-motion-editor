# Phase 17.1: Adaptative sidebar's layer window - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-24
**Phase:** 17.1-adaptative-sidebar-s-layer-window
**Areas discussed:** Adaptive behavior, Layer grouping, Panel context, Selection routing

---

## Adaptive behavior

### Navigation back from layer view
| Option | Description | Selected |
|--------|-------------|----------|
| Back button in header | Panel header changes to 'LAYERS — [Sequence Name]' with back arrow | ✓ |
| Same layer icon toggles | Layer icon on sequence header toggles between views | |
| Click sequence name | Sequence name stays at top, clicking returns to list | |

**User's choice:** Back button in header
**Notes:** None

### Sequence context in layer view
| Option | Description | Selected |
|--------|-------------|----------|
| Yes, keep sequence context | Compact sequence header at top showing thumbnail, name, key count | ✓ |
| No, just layers | Header says 'LAYERS — [name]' with back arrow, entire body is layer list | |

**User's choice:** Yes, keep sequence context
**Notes:** None

### Standalone LAYERS panel fate
| Option | Description | Selected |
|--------|-------------|----------|
| Remove entirely | 2-panel sidebar: Sequences/Layers adaptive + Properties | ✓ |
| Hidden by default | Keep 3-panel structure but LAYERS starts collapsed | |

**User's choice:** Remove entirely
**Notes:** None

## Layer grouping

### Layer list layout
| Option | Description | Selected |
|--------|-------------|----------|
| Keep current layout | Same LayerList component, moved into adaptive panel | ✓ |
| Redesign the list | Layer thumbnails, inline opacity, blend mode indicator | |

**User's choice:** Keep current layout with the add button for add layers
**Notes:** User specified the AddLayerMenu button must be preserved in the adaptive view

### Sequence types with layer icon

**User clarification:** Question about FX/content-overlay sequences was irrelevant — the SEQUENCES panel only shows content sequences. FX and content-overlay sequences exist only on the timeline. Question withdrawn.

## Panel context

### Header in layer view
| Option | Description | Selected |
|--------|-------------|----------|
| ← LAYERS + Add Layer | Back arrow + 'LAYERS' title + AddLayerMenu | |
| ← [Sequence Name] + Add Layer | Back arrow + sequence name + AddLayerMenu | ✓ |
| ← LAYERS | Back arrow + title only, AddLayerMenu moves elsewhere | |

**User's choice:** ← [Sequence Name] + Add Layer
**Notes:** None

## Selection routing

### Layer selection behavior
| Option | Description | Selected |
|--------|-------------|----------|
| Same behavior | Select in layerStore, show properties — identical to current | ✓ |
| Auto-open layer view | Also auto-switch sidebar when clicking layers on timeline | |

**User's choice:** Same behavior
**Notes:** None

### Sequence switch behavior
| Option | Description | Selected |
|--------|-------------|----------|
| Yes, auto-close | Return to sequence list when switching sequences | ✓ |
| No, stay in layers | Switch to new sequence's layers instead | |

**User's choice:** Yes, auto-close
**Notes:** None

## Claude's Discretion

- Layer icon choice and placement
- View switching animation
- Compact sequence header row design
- Panel collapse/expand adjustments for 2-panel layout
- App config migration for persisted flex values

## Deferred Ideas

None
