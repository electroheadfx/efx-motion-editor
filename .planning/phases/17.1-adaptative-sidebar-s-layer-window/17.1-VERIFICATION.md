---
phase: 17.1-adaptative-sidebar-s-layer-window
verified: 2026-03-24T14:10:00Z
status: human_needed
score: 7/7 must-haves verified
re_verification: false
human_verification:
  - test: "Open app with a project containing 2+ content sequences, each with added layers. Verify sidebar shows exactly 2 panels with a single resize handle — no standalone LAYERS panel."
    expected: "Sidebar renders SEQUENCES/LAYERS adaptive panel on top and PROPERTIES panel below, separated by one drag handle."
    why_human: "Cannot verify rendered panel count programmatically in a Tauri app without running it."
  - test: "Click the Layers icon on any sequence row header. Verify the panel transitions to layer view showing: back arrow + sequence name in uppercase, thumbnail + name + key count info row, and the layer list below."
    expected: "Panel content replaces sequence list with the layer view. All three sub-sections (header, compact info, layer list) are visible and populated."
    why_human: "Conditional rendering driven by signal state requires running the app."
  - test: "While in layer view, click the back arrow. Verify the panel returns to the sequence list."
    expected: "Panel restores the sequence list without page reload or visual glitch."
    why_human: "Round-trip state change requires runtime observation."
  - test: "Open layer view for Sequence A, then click the Sequence B row. Verify layer view auto-closes and Sequence B becomes active."
    expected: "Panel shows sequence list, Sequence B is highlighted as active."
    why_human: "D-11 auto-close behavior requires two-step user interaction."
  - test: "Click a layer inside the adaptive layer view. Verify the PROPERTIES panel below updates to show that layer's properties."
    expected: "Clicking a layer row in LayerList triggers selection and the Properties section populates with that layer's settings."
    why_human: "LayerStore selection → Properties routing requires visual confirmation of signal propagation."
  - test: "Drag the resize handle between adaptive panel and PROPERTIES. Verify smooth resize. Close and reopen app — verify panel sizes persist."
    expected: "Resize is smooth. Reopened app restores saved flex values."
    why_human: "Persistence round-trip and drag-resize smoothness require running the Tauri app."
  - test: "Click the chevron on the SEQUENCES section header to collapse it. Verify it collapses to header-only height. Click again to expand."
    expected: "Panel animates collapse/expand correctly. Only the SEQUENCES/LAYERS adaptive panel collapses — the PROPERTIES panel is unaffected."
    why_human: "CollapsibleSection behavior requires runtime UI observation."
---

# Phase 17.1: Adaptive Sidebar Layer Window — Verification Report

**Phase Goal:** Replace the standalone LAYERS panel with an adaptive view inside the SEQUENCES panel, simplifying the sidebar from 3 panels to 2 panels with a layer icon button on each sequence header to switch between sequence list and layer view

**Verified:** 2026-03-24T14:10:00Z
**Status:** human_needed (all automated checks passed; 7 visual behaviors require human testing)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Sidebar has 2 panels (adaptive + Properties) with a single resizer | ? HUMAN | LeftPanel.tsx has exactly 1 `<PanelResizer`, no `title="LAYERS"` panel; requires running app to confirm render |
| 2 | User can click a Layers icon on any content sequence header to switch to layer view | ? HUMAN | `handleLayerView` callback wired to `uiStore.openLayerView(seq.id)` on `<Layers size={18}>` button; requires app |
| 3 | Layer view shows back arrow header, compact info, LayerList, and AddLayerMenu | ? HUMAN | All four elements present in LeftPanel.tsx conditional branch; requires running app to confirm |
| 4 | Back arrow returns to sequence list, switching sequences auto-closes layer view | ? HUMAN | `closeLayerView()` on ArrowLeft click; `uiStore.closeLayerView()` in SequenceList `handleSelect` switch branch; requires app |
| 5 | Panel flex persistence migrates from 3-value to 2-value format with backward compat | ✓ VERIFIED | `getPanelFlex` detects `stored.length === 3` and drops index 1; `setPanelFlex(seq, prop)` writes 2-element array; 30 tests pass |

**Automated score:** 1/5 truths fully verified programmatically (the persistence truth). The remaining 4 truths have all supporting code verified but require visual/runtime confirmation per human checks.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Application/src/stores/uiStore.ts` | layerViewSequenceId signal, 2-panel collapse/expand | ✓ VERIFIED | Signal present (5 occurrences), `openLayerView`/`closeLayerView` exported, `collapsePanel`/`expandPanel` typed `'seq' \| 'prop'` only, `reset()` includes `layerViewSequenceId.value = null` |
| `Application/src/lib/panelResize.ts` | calcFlexResize2 for 2-panel resize | ✓ VERIFIED | Exports `FlexSizes2`, `FlexResizeResult2`, `calcFlexResize2`; existing `calcFlexResize` (3-panel) and `calcResize` (legacy) intact |
| `Application/src/lib/appConfig.ts` | 2-value panel flex persistence with 3-value migration | ✓ VERIFIED | `getPanelFlex(): Promise<[number, number]>` with `stored.length === 3` migration; `setPanelFlex(seq: number, prop: number)` |
| `Application/src/lib/panelResize.test.ts` | Unit tests for calcFlexResize2 | ✓ VERIFIED | `describe('calcFlexResize2')` block at line 240; 30 total tests pass (7+ for calcFlexResize2) |
| `Application/src/components/layout/LeftPanel.tsx` | 2-panel layout with adaptive sequence/layer view | ✓ VERIFIED | imports `calcFlexResize2`, `ArrowLeft`, reads `layerViewSequenceId.value`, renders `<LayerList>` and `<AddLayerMenu>` in adaptive panel, exactly 1 `<PanelResizer` |
| `Application/src/components/sequence/SequenceList.tsx` | Layers icon button, auto-close on switch | ✓ VERIFIED | `Layers` imported, `handleLayerView` useCallback with `e.stopPropagation()` + `uiStore.openLayerView(seq.id)`, `title="Show layers"`, `uiStore.closeLayerView()` in sequence-switch branch |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `SequenceList.tsx` | `uiStore.ts` | `uiStore.openLayerView(seq.id)` on layer icon click | ✓ WIRED | Line 229: `uiStore.openLayerView(seq.id)` inside `handleLayerView` callback |
| `LeftPanel.tsx` | `uiStore.ts` | reads `layerViewSequenceId.value` to switch views | ✓ WIRED | Line 70: `const layerViewSeqId = uiStore.layerViewSequenceId.value` drives conditional rendering |
| `LeftPanel.tsx` | `layer/LayerList.tsx` | renders `<LayerList>` inside adaptive panel when layer view open | ✓ WIRED | Line 230: `<LayerList />` inside `layerViewSeq` truthy branch |
| `LeftPanel.tsx` | `lib/panelResize.ts` | uses `calcFlexResize2` for single resizer | ✓ WIRED | Line 18 import + line 94 call in `handleSeqPropResize` |
| `uiStore.ts` | `lib/appConfig.ts` | `initSidebarLayout` reads `getPanelFlex` (2-value) | ✓ WIRED | `const [sf, pf] = await getPanelFlex()` — 2-value destructure |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `LeftPanel.tsx` | `layerViewSeq` | `sequenceStore.sequences.value.find(s => s.id === layerViewSeqId)` | Yes — reads live signal from sequenceStore | ✓ FLOWING |
| `LeftPanel.tsx` | `seqFlex` / `propFlex` | `uiStore.seqPanelFlex.value` / `uiStore.propPanelFlex.value` — initialized by `initSidebarLayout` which reads `appConfig.getPanelFlex()` | Yes — reads persisted values via Tauri store | ✓ FLOWING |
| `LeftPanel.tsx` | `selectedLayer` | `layerStore.selectedLayerId.value` → lookup in `sequenceStore.sequences.value` | Yes — reads from layerStore signal updated by LayerList click | ✓ FLOWING |
| `SequenceList.tsx` | `sequences` | `sequenceStore.sequences.value.filter(s => s.kind === 'content')` | Yes — live signal from sequenceStore | ✓ FLOWING |

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| calcFlexResize2 unit tests (all 30) | `npx vitest run src/lib/panelResize.test.ts` | 30/30 passed | ✓ PASS |
| No forbidden signals in uiStore | `grep -c 'layPanelFlex\|layersSectionCollapsed\|_preCollapseLayFlex' uiStore.ts` | 0 | ✓ PASS |
| Exactly 1 PanelResizer in LeftPanel | `grep -c '<PanelResizer' LeftPanel.tsx` | 1 | ✓ PASS |
| No standalone LAYERS panel | `grep -c 'title="LAYERS"' LeftPanel.tsx` | 0 | ✓ PASS |
| All 6 commits present in git | `git log --oneline \| grep commit hashes` | All 6 found | ✓ PASS |
| Full vitest suite (phase-17.1 files) | `npx vitest run src/lib/panelResize.test.ts` | 30 pass, 0 fail | ✓ PASS |

**Note on full suite:** Running the complete vitest suite shows 3 pre-existing failures in `src/lib/audioWaveform.test.ts` (from Phase 15). These failures were present before Phase 17.1 (`audioWaveform.test.ts` has zero commits touching it in Phase 17.1) and are unrelated to this phase's changes.

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ASIDE-01 | 17.1-01, 17.1-02 | Sidebar simplifies from 3 panels to 2 panels with single resizer | ✓ SATISFIED | LeftPanel.tsx: 1 PanelResizer, no LAYERS title, 2-panel flex layout with `seqFlex`/`propFlex` only |
| ASIDE-02 | 17.1-02 | User can open layer view via layer icon, view/add layers, return via back arrow | ✓ CODE COMPLETE / ? HUMAN VISUAL | All code paths implemented and wired; REQUIREMENTS.md still marks as "Pending" — discrepancy noted below |
| ASIDE-03 | 17.1-01 | Panel flex persistence migrates 3-value to 2-value with backward compat | ✓ SATISFIED | `getPanelFlex` migration logic verified; `setPanelFlex(seq, prop)` writes 2-element array |

### REQUIREMENTS.md Discrepancy

REQUIREMENTS.md has ASIDE-02 marked `[ ]` (Pending) and the traceability table shows `Pending` for ASIDE-02. Both Plan 02 SUMMARY and the code clearly implement this requirement. The REQUIREMENTS.md file was not updated to mark ASIDE-02 complete. This is a documentation gap, not a code gap — the implementation is present and wired.

**Orphaned requirements:** None — all three ASIDE requirements are claimed by plans and verified against code.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | - | - | - | - |

No TODO/FIXME/PLACEHOLDER comments, no empty return stubs, no hardcoded empty arrays/objects in rendering paths across any of the 6 modified files.

---

## Human Verification Required

### 1. 2-Panel Layout Render

**Test:** Open the app with a project. Observe the left sidebar.
**Expected:** Two distinct panel regions visible — SEQUENCES/LAYERS on top, PROPERTIES below — separated by exactly one drag handle. No third LAYERS panel.
**Why human:** Cannot verify rendered panel count in Tauri app without running it.

### 2. Layer Icon Opens Adaptive Layer View

**Test:** Open a project with content sequences that have layers. Click the Layers icon (stack icon with green badge) on any sequence row.
**Expected:** The top panel switches from the sequence list to the layer view. The header shows a left-arrow, the sequence name in uppercase, and an Add button on the right. Below the header is a compact row with a thumbnail, the sequence name, and key count. Below that is the full layer list.
**Why human:** Conditional rendering driven by `layerViewSequenceId` signal requires runtime observation.

### 3. Back Arrow Returns to Sequence List

**Test:** While in layer view, click the back arrow (or the sequence name text next to it).
**Expected:** Panel transitions back to the sequence list view with all sequences visible.
**Why human:** Round-trip signal state change requires runtime observation.

### 4. Switching Sequences Auto-Closes Layer View

**Test:** Open layer view for Sequence A. Then click a different sequence (Sequence B) row in — note you must first go back to the list view or open a second window. Alternatively, directly click Sequence B's row when layer view is showing (it should be visible as the list behind, or navigate back first then switch).

Correction: Layer view replaces the sequence list. To trigger D-11, you need to click a sequence from somewhere else, OR the test scenario is: open the layer view, then switch to a DIFFERENT sequence via the back arrow + click, and verify the view auto-resets. The simpler test: open layer view for Sequence A, navigate back, click Sequence B — verify layer view does not reopen for A.

Actually per D-11 (auto-close on switch): while viewing the sequence list, if you click Sequence B when you were previously in Sequence A's layer view — the `closeLayerView()` call in `handleSelect` fires. Test: open layer view via layer icon, navigate back via back arrow, notice state is null. The more direct test: open layer view for Sequence A (seqA is now active), click the sequence list item for Sequence B — the `handleSelect` path fires `uiStore.closeLayerView()` but since layerViewSequenceId is already null from the back arrow, this only matters if layer view is open WHILE switching. To trigger it: open layer view for A, then somehow click Sequence B — but B's row is not visible when in layer view. The trigger is: while layer view shows seqA's layers, the user must use an external trigger (keyboard, timeline click) that changes selectedSequenceId. Programmatically this is `sequenceStore.setActive(seqB)` called from timeline interaction.

**Expected:** `layerViewSequenceId` resets to null when a different sequence becomes active, returning the panel to sequence list mode.
**Why human:** Auto-close requires multi-step interaction or timeline-triggered sequence switch.

### 5. Layer Click Updates Properties Panel

**Test:** With layer view open, click any layer row in the layer list.
**Expected:** The PROPERTIES panel below immediately updates to show that layer's properties (transform, opacity, blend mode, etc.).
**Why human:** Signal propagation from LayerList click → layerStore → LeftPanel properties routing requires visual confirmation.

### 6. Panel Resize Persistence

**Test:** Drag the resize handle between adaptive panel and PROPERTIES to adjust sizes. Close the app entirely. Reopen with the same project.
**Expected:** Panel sizes are restored to the dragged proportion.
**Why human:** Tauri LazyStore persistence round-trip requires app restart to verify.

### 7. Panel Collapse via Chevron

**Test:** Click the chevron/header of the SEQUENCES section in the adaptive panel.
**Expected:** The adaptive panel collapses to header-only height. PROPERTIES panel expands to fill the space. Click again to expand.
**Why human:** CollapsibleSection animation and flex collapse behavior requires runtime observation.

---

## Gaps Summary

No functional gaps found. All artifacts exist, are substantive, and are fully wired with real data flowing through them. The phase goal — restructuring the sidebar from 3 panels to 2 panels with adaptive sequence/layer view switching — is implemented in code.

The only outstanding items are:

1. **Human visual verification** (7 items above) — standard for UI phases. The code is complete; a human needs to confirm the rendered behavior.
2. **REQUIREMENTS.md documentation gap** — ASIDE-02 is still marked `[ ] Pending` in REQUIREMENTS.md even though the implementation is complete. This should be updated to `[x]` after visual verification passes.

---

_Verified: 2026-03-24T14:10:00Z_
_Verifier: Claude (gsd-verifier)_
