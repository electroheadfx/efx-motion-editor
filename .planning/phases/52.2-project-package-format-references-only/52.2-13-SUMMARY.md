---
phase: 52.2-project-package-format-references-only
plan: 13
subsystem: infra
tags: [xstate, effect, pnpm, budget, chunk-size, api-audit, skills, pilot-preconditions]

# Dependency graph
requires:
  - phase: 52.2-project-package-format-references-only
    provides: 52.2-12 (render counters + the Gate-2 close-out; SPECS/async-conventions.md approved)
provides:
  - pinned xstate 6.0.0-alpha.53 + effect 4.0.0-rc.115 (exact, unimported, lazily-placed)
  - recorded pilot-start re-verification (dist-tags, breaking changes, binding status)
  - measured budget decision (1320 -> 1340 kB, +0.00 kB install delta proven)
  - effect-ts guidance skill installed (v4) at .claude/skills/effect-ts
  - scripts/verify-async-api-surface.ts — the installed-surface API drift gate
affects: [52.2-14, 52.2-15, 52.2-16]

# Actuals (#2632) — same chars/4 scale as the plan estimate. The vendored
# effect-ts skill (28 files, ~200 KB) dominates the realized diff; authored
# work is ~35 KB.
actuals:
  tokens: 59127
  tasks: 4
  commits: 4
plan_head_before: c4f6976fd3258966c3d3fa928b22df9560fa033f

# Tech tracking
tech-stack:
  added: [xstate 6.0.0-alpha.53, effect 4.0.0-rc.115, effect-ts skill 0.6.5]
  patterns: [installed-surface API audit gate, exact-pin dependency policy, documented budget raise]

key-files:
  created:
    - scripts/verify-async-api-surface.ts
    - .claude/skills/effect-ts/
  modified:
    - app/package.json
    - pnpm-lock.yaml
    - app/vite.config.ts
    - app/src/viteBuild.test.ts
    - skills-lock.json
    - SPECS/async-conventions.md

key-decisions:
  - "Budget raised 1320 -> 1340 kB (measured 1,326.36 + ~13.6 kB headroom), recorded in vite.config.ts + viteBuild.test.ts; the install's own delta is 0.00 kB (byte-identical pre/post builds), so the raise is for the already-landed 52.2 format work, never anticipation of pilot code"
  - "Both libraries installed --save-exact at the Task-1 re-verified pins and left unimported: zero imports in app/src, pilot imports stay behind the PhysicsPaintStudio lazy boundary"
  - "The effect-ts skill's upstream (effect@4.0.0-beta.92) is older than the pinned rc.115 — the installed .d.ts is the authority where the skill and the package disagree, recorded in the doc"
  - "The API audit found ZERO drift and zero corrections; the doc gained an explicit pilot import block (the gate's binding list) and the gate also machine-checks cited-but-not-imported names, removed-creator absence, and dotted members"

patterns-established:
  - "Installed-surface audit: doc-cited API names are resolved against the shipped .d.ts of the installed tree, never the registry payload or research notes"
  - "Measured budget raise: pre-install and post-install builds compared by content hash, so the delta is proven rather than assumed, and the raise carries its attribution in both the config and the assertion"

requirements-completed: [D-15, D-16, D-19]

coverage:
  - id: D1
    description: "Pilot-start re-verification of XState v6 / Effect v4 recorded in SPECS/async-conventions.md ('Pilot re-verification — 2026-09-12')"
    requirement: D-15
    verification:
      - kind: other
        ref: "pnpm view xstate dist-tags --json && pnpm view effect dist-tags --json (exit 0; grep -c 'Pilot re-verification' SPECS/async-conventions.md = 1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "xstate 6.0.0-alpha.53 + effect 4.0.0-rc.115 installed exact-pinned, unimported, main chunk untouched"
    requirement: D-16
    verification:
      - kind: other
        ref: "grep -c '\"xstate\"|\"effect\"' app/package.json = 2; 0 library imports in app/src; pnpm install + pnpm run build both exit 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Budget re-measured with the deps installed and the raise recorded as an explicit decision (1320 -> 1340 kB)"
    requirement: D-19
    verification:
      - kind: unit
        ref: "app/src/viteBuild.test.ts#emits no chunk-size warning at the 1340 desktop budget"
        status: pass
    human_judgment: true
    rationale: "Raising a frozen milestone budget guardrail is the user's decision to accept, even though D-19 delegates the raise to this plan; the record (attribution + 0.00 kB install delta) is here for that sign-off"
  - id: D4
    description: "Published effect-ts guidance skill installed and its target version recorded (v4, upstream older than the pin)"
    requirement: D-16
    verification:
      - kind: other
        ref: "npx skills@1.5.26 add tenequm/skills -s effect-ts (28 files + skills-lock.json entry; skill 0.6.5, 'v4 default with v3 support')"
        status: pass
    human_judgment: false
  - id: D5
    description: "Every API name the conventions doc cites resolves against the installed .d.ts surface; zero unresolved, zero corrections"
    requirement: D-17
    verification:
      - kind: unit
        ref: "node scripts/verify-async-api-surface.ts SPECS/async-conventions.md (exit 0, ALL CHECKS PASSED)"
        status: pass
    human_judgment: false

duration: 7min
completed: 2026-09-12
status: complete
---

# Phase 52.2 Plan 13: Pilot preconditions — re-verified pins, measured budget, audited API surface Summary

**Pilot unblocked on re-verified exact pins (`xstate@6.0.0-alpha.53`, `effect@4.0.0-rc.115`) installed but unimported, a budget raise to 1340 kB backed by a proven 0.00 kB install delta, the effect-ts skill in place, and a new gate proving every API name the conventions doc cites exists in the installed `.d.ts`**

## Performance

- **Duration:** 7 min (from the plan-ledger write; Task 1's registry work and the Task 2 install ran just before it)
- **Started:** 2026-09-12T20:49:54Z
- **Completed:** 2026-09-12T20:57:08Z
- **Tasks:** 4/4
- **Files modified:** 34 tracked (2 package files, 2 build files, 1 lock, 28 skill files, 1 script) + `SPECS/async-conventions.md` (gitignored — see Deviations)

## Accomplishments

- **D-15 re-verification executed at the pilot's start date, not restated from research**: live dist-tags re-read (`xstate` alpha `6.0.0-alpha.53`, `effect` rc `4.0.0-rc.115`), five XState breaking changes in the window recorded (`alpha.1`, `.4`, `.5`, `.51`, `.53` — the guard-signature change is the last), no Preact binding still, `@effect/platform` still absent for v4, and the effect-ts skill still v4-default. Verdict recorded: no candidate-flow row invalidated.
- **Both libraries installed exact-pinned and provably inert**: `pnpm install` clean, `pnpm run build` exit 0, zero library imports anywhere in `app/src`; the main-chunk import boundary confirmed (`app/src/lib/physicPaintFlush.ts` stays a thin facade with no library imports, `projectStore.ts:31` / `exportEngine.ts:3` import only the flush facade).
- **The known-red budget is closed with evidence, not narration**: pre-install (plan base `c4f6976f`, throwaway worktree) and post-install builds emitted the **byte-identical** `index-knu_IwqL.js` (1,326.36 kB, same content hash) — the install's delta is 0.00 kB — so the raise 1320 → 1340 kB (measured + ~13.6 kB headroom) is attributed to the already-landed 52.2 format work and recorded in `app/vite.config.ts` and `app/src/viteBuild.test.ts`. `viteBuild.test.ts` is green (11/11) and the production build now emits no chunk-size warning.
- **effect-ts skill installed (D-16)** at `.claude/skills/effect-ts` (28 files) with its `skills-lock.json` entry; target version recorded as v4 (0.6.5, upstream `effect@4.0.0-beta.92` — older than the pin, so the installed `.d.ts` is declared the authority).
- **New audit gate `scripts/verify-async-api-surface.ts`**: extracts the doc's import bindings, resolves each against the shipped declaration trees, prints the declaring file per name, and exits non-zero on any absence — plus machine checks for the cited-but-not-imported names, the absence of every creator the doc records as removed, and the dotted members/option shapes. Run against the installed tree: **zero unresolved names, zero corrections**.

## Task Commits

Each task was committed atomically:

1. **Task 1: Re-verify XState v6 and Effect v4 at the pilot date** - no commit (`SPECS/` is gitignored — deliverable is in the doc, documented below)
2. **Task 2: Install the pinned dependencies without touching the main chunk** - `5dfd6c52` (chore)
3. **Task 3: Budget re-measurement + effect-ts skill install** - `f57ec8e7` (fix, budget raise + record), `a273519d` (chore, skill install)
4. **Task 4: Audit the conventions doc's API names against the installed .d.ts surface** - `4d6a4f47` (feat)

**Plan metadata:** this SUMMARY + STATE.md + ROADMAP.md commit (docs)

## Files Created/Modified

- `scripts/verify-async-api-surface.ts` (new, 397 lines) — the installed-surface API drift gate: import-binding extraction, entry export-list resolution, removed-creator absence, dotted-member checks, sanity failure on zero bindings
- `app/package.json` — `"effect": "4.0.0-rc.115"`, `"xstate": "6.0.0-alpha.53"` appended alphabetically, exact pins, no range operator
- `pnpm-lock.yaml` — 32 insertions (the two new importers + their transitive `saxes`)
- `app/vite.config.ts` — `chunkSizeWarningLimit: 1340` + the 2026-09-12 measurement note
- `app/src/viteBuild.test.ts` — the exactly-1340 assertion, the updated test names/messages, and the measurement comment in the historical trail
- `.claude/skills/effect-ts/` (28 files) + `skills-lock.json` — the vendored skill and its lock entry
- `SPECS/async-conventions.md` (gitignored, not committable) — three dated sections added: "Pilot re-verification", "Pilot dependencies", "Pilot API surface audit" (with the pilot's explicit import block)

## Decisions Made

1. **Budget raise 1320 → 1340 kB, documented** — the plan's D-19 delegates this decision; option (a) ("restore headroom via the Studio lazy boundary") was not available inside this plan's file scope, and the measured overage predates the install by four plans (52.2-07 → 52.2-10 → now all recorded). The raise carries its attribution and the 0.00 kB install proof in both the config and the assertion.
2. **Exact pins, zero imports** — `--save-exact` (this repo's convention for runtime deps), and nothing wired: the pilot's library-importing modules stay behind the `PhysicsPaintStudio` lazy boundary.
3. **The audit's scope was widened beyond the import block** — cited-but-not-imported names and removed-creator absence are machine-checked too, so a future alpha re-introducing a removed creator cannot silently re-enter the doc as "available".

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `app/vite.config.ts` was not in the plan's `files_modified`, but the budget assertion cannot move without it**
- **Found during:** Task 3 (Budget re-measurement)
- **Issue:** the plan says "edit the assertion in `app/src/viteBuild.test.ts`", but that test asserts the value resolved from `app/vite.config.ts` (`captured.chunkLimit`). Editing only the test would leave the gate red for the wrong reason — precisely what the plan's `key_links` warns against.
- **Fix:** raised `chunkSizeWarningLimit` to 1340 in `app/vite.config.ts` with the measurement comment, together with the test's assertion and comments.
- **Files modified:** `app/vite.config.ts`, `app/src/viteBuild.test.ts`
- **Verification:** `pnpm --filter efx-motion-editor exec vitest run src/viteBuild.test.ts` → 11/11 pass; production build exits 0 with no chunk-size warning
- **Committed in:** `f57ec8e7`

**2. [Rule 3 - Blocking] `skills-lock.json` + `.claude/skills/effect-ts/` were not in `files_modified`, but the skill install creates them and the repo tracks installed skills**
- **Found during:** Task 3 (effect-ts skill install)
- **Issue:** the plan mandates the install but lists no skill files; the repo versions all 40 installed skills plus `skills-lock.json`, so leaving the new ones untracked would break that convention.
- **Fix:** committed the vendored skill and its lock entry.
- **Files modified:** `skills-lock.json`, `.claude/skills/effect-ts/**`
- **Verification:** content scan (markdown + eval fixtures only, no scripts, no postinstall); skill discoverable; version recorded in the doc
- **Committed in:** `a273519d`

**3. [Rule 2 - Missing Critical] The doc's examples carried no explicit import lines, so the gate found zero bindings**
- **Found during:** Task 4 (API surface audit)
- **Issue:** the plan anticipated this ("the doc's examples lack explicit import lines — add them; the audit needs the binding list"): the conventions doc cited names in prose only, and the gate correctly sanity-fails on zero bindings.
- **Fix:** added the pilot's explicit import block to the doc's audit section (the binding list), and the gate's extra sections (cited-but-not-imported, removed-creator absence, dotted members) so no cited name is skipped.
- **Files modified:** `SPECS/async-conventions.md`, `scripts/verify-async-api-surface.ts`
- **Verification:** `node scripts/verify-async-api-surface.ts SPECS/async-conventions.md` exit 0; both negative cases (drifted name, empty binding list) exit 1 as designed
- **Committed in:** `4d6a4f47`

---

**Total deviations:** 3 auto-fixed (2 blocking, 1 missing critical)
**Impact on plan:** All three were required for the plan's own gates to be truthful. No scope creep — no pilot code was written, no library imported.

## Issues Encountered

- **Task 2's second `<verify>` could not pass as written** ("no chunk-size warning above the frozen 1320 kB budget"): the overage is pre-existing (plans 08–12) and is the exact red the plan owns in Task 3. Resolved by measuring the pre-install baseline in a throwaway worktree at the plan base, proving the install's delta is 0.00 kB (identical content hashes), then raising and recording in Task 3. The build now exits 0 with no warning.
- **Two bugs in the audit gate itself, caught by its own runs**: a double `.d.ts` suffix when resolving re-export specifiers, and declaration matching firing on jsdoc prose (`function or observer`) — both fixed; block comments are now blanked (newlines preserved) before any declaration match.
- **`SPECS/` is gitignored** (`.gitignore:47`), so Task 1's and Task 3's doc deliverables cannot be committed — the precedent from plan 52.2-12 ("no commit: SPECS/ is gitignored by project decision") is followed; every doc claim is reproducible from the committed gate and the recorded commands.
- **`WINDOWS.md` ledger still refuses writes** (pre-existing drift: `frontmatter 24/1/42/67 but entries yield 29/1/42/72`), so this plan's deviations are recorded here and in `deferred-items.md` rather than in the cross-phase register — same as plans 01–12.

## Known Stubs

None — this plan ships no runtime code. The two new dependencies are deliberately unimported (that is the plan's success criterion, "nothing yet imported into production code"), which is a precondition, not a stub: plan 14 is the first consumer.

## User Setup Required

None. The user-side BEFORE render-churn capture on the current dev build (Gate-2 resolution item 3) remains a user action before plan 14's first pilot code — unchanged by this plan.

## Next Phase Readiness

- **Plan 14 can start on a verified surface**: every name in the doc's import block resolves in the installed `.d.ts`; the gate re-runs in one command if the doc or the pins change.
- **Pilot placement is enforced by measurement**: the deps are installed but reach no chunk; any pilot import that escapes the `PhysicsPaintStudio` lazy boundary will show up as main-chunk growth against the 1340 kB budget (11/11 in `viteBuild.test.ts`).
- **Watch item**: the effect-ts skill's examples lag the pinned RC (upstream beta.92 vs rc.115) — the doc records the installed surface as authoritative.

---

*Phase: 52.2-project-package-format-references-only*
*Completed: 2026-09-12*

## Self-Check: PASSED

- `scripts/verify-async-api-surface.ts` — FOUND
- `.claude/skills/effect-ts/SKILL.md` — FOUND
- `52.2-13-SUMMARY.md` — FOUND
- commits `5dfd6c52`, `f57ec8e7`, `a273519d`, `4d6a4f47` — FOUND
