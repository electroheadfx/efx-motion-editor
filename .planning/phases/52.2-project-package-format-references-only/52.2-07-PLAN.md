---
phase: 52.2-project-package-format-references-only
plan: 07
type: execute
wave: 3
depends_on: ["52.2-01", "52.2-02", "52.2-05"]
files_modified:
  - app/src/lib/efxPaintPersistence.ts
  - app/src/lib/efxPaintPersistence.test.ts
  - app/src/stores/projectStore.ts
  - app/src/stores/projectStore.efxPaintCutover.test.ts
  - app/src/types/project.ts
  - app/src/testUtils/packageFixture.ts
  - app/src/lib/efxPaintPackageRoundTrip.test.ts
autonomous: true
requirements: [D-09, D-10, D-11]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Saving a project writes the manifest, one sub-file per CHANGED paint layer, and one media file per changed key — and nothing else"
    - "A second save with no change rewrites zero files; changing one key rewrites only that key's media file and its layer sub-file"
    - "Every authoritative file in the set is written into the package staging root first and reaches its canonical path only through the plan-05 transaction's publish step, so a refusal or a crash before commit leaves the pre-save package byte-identical"
    - "An empty change set performs no disk write at all: no staging root, no transaction, and the save still reports success"
    - "No path serializes the complete project as a single JSON string, and no emitted JSON contains an image payload"
    - "No emitted file contains a machine-local path"
  artifacts:
    - path: "app/src/lib/efxPaintPersistence.ts"
      provides: "per-file change tokens (frame:<layerId>:<keyId>, layer:<layerId>, manifest) + the staged multi-file package save"
    - path: "app/src/stores/projectStore.ts"
      provides: "saveProject/saveProjectAs orchestration over the staged package file set"
    - path: "app/src/types/project.ts"
      provides: "formatVersion + projectId on the persisted project shape"
    - path: "app/src/lib/efxPaintPackageRoundTrip.test.ts"
      provides: "the phase's central round-trip + change-set + zero-payload + refusal contract"
  key_links:
    - "media must be written before its layer sub-file, because the sub-file carries the digest the native command returns — reversing the order forces a second read pass or a fabricated digest"
    - "every authoritative write must target the staging generation and no canonical path — if a media write lands canonically before the transaction, the canonical file is not the bytes the marker recorded and rollback cannot restore it"
    - "the change-token key space must be keyed by keyId, never by appFrame: one keyId can be placed at several frames, and a frame-keyed token would rewrite unchanged media"
    - "projectStore's `requestPhysicPaintFlush()` must run before the write set is computed, otherwise the Studio's pending edits are saved as stale content"
---

<objective>
Replace the single-document save with the package save: stage only the files whose content changed into the package staging root (per-file change tokens extending `savedFrameTokens`, D-11), publish and commit the whole set through the extended transaction (D-10), and never serialize the project as one string again (spec §6). Layer granularity holds structurally because a layer sub-file contains exactly that layer's document (D-09).

Purpose: this is the plan that kills the measured ~4.6 s `persist.serialize` term. Everything after it — refusal gate, read-back, bridge — assumes the package exists on disk in this shape.

Output: the extended `efxPaintPersistence` funnel staging manifest + layer sub-files + media and committing them through plan 05's transaction, `saveProject`/`saveProjectAs` orchestration, per-file telemetry, and the round-trip contract test.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/phase-52.2-project-package-format.md
@.planning/phases/52.2-project-package-format-references-only/52.2-PATTERNS.md
@.planning/phases/52.2-project-package-format-references-only/52.2-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-file change tokens and the change set</name>
  <files>app/src/lib/efxPaintPersistence.ts, app/src/lib/efxPaintPersistence.test.ts</files>
  <read_first>
    - app/src/lib/efxPaintPersistence.ts:84-92 and :224-345 (`savedFrameTokens`, `prepareEfxPaintSave`, `settlePreparedEfxPaintSave` — the model being extended)
    - app/src/lib/efxPaintPackage.test.ts (plan 02: `buildFrameMediaRelativePath`, `buildMachineCacheRelativePath`)
    - app/src/lib/efxPaintPersistence.test.ts (the existing suite to extend)
  </read_first>
  <behavior>
    - the token key space is `frame:<layerId>:<keyId>` for media, `layer:<layerId>` for a sub-file, `manifest` for the top manifest
    - a keyId placed at two different appFrames yields ONE token and ONE media write
    - `computeChangedFiles(previous, next)` returns only entries whose token value differs (digest for media, revision token for documents)
    - the first save against an empty token map returns the complete set; a second save with identical input returns an empty set
    - changing one key's bytes returns exactly its media token plus its layer token
    - renaming a track inside one layer returns exactly that layer token (never the manifest)
    - the token map survives a settle and is dropped on rollback (so a failed save re-writes everything it had staged)
  </behavior>
  <action>
    Extend the token model in `efxPaintPersistence.ts` from frame-keyed to file-keyed (D-11). Keep the existing `stableSegment`/path helpers; add `packageFileToken(kind, ...)` helpers and `computeChangedFiles(previousTokens, nextTokens)` returning the changed entries in a deterministic order (sort by key). Values are opaque strings — a persisted media digest (`frame:` tokens) or a document revision token (`layer:`/`manifest`) — so the same comparison works for both.

    The key space must be keyed by keyId (D-09/D-11): one keyId can be placed at several appFrames, and a frame-keyed token would rewrite identical media and, worse, break the "one file per key" contract. Media tokens are only cleared after a successful settle of that file; a rollback leaves them dirty so the next save rewrites (fail-safe direction).

    Extend `efxPaintPersistence.test.ts` with the `<behavior>` cases. Write them first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPersistence.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPersistence.test.ts` exits 0 with all pre-existing cases passing plus at least 7 new ones
    - the two-placements-one-token case is present by name
    - the change-set result is asserted as an exact set, not a length
  </acceptance_criteria>
  <done>Change tracking is per file and per key identity, deterministic, and fail-safe on rollback.</done>
</task>

<task type="auto" string="true">
  <name>Task 2: The package save — media, layer sub-files, manifest, one transaction</name>
  <files>app/src/lib/efxPaintPersistence.ts, app/src/stores/projectStore.ts, app/src/types/project.ts, app/src/stores/projectStore.efxPaintCutover.test.ts</files>
  <read_first>
    - app/src/stores/projectStore.ts:111-133 (`buildEfxPaintDocuments`) and :720-855 (`saveProject`, `saveProjectAs`, the `requestPhysicPaintFlush()` call, the `ipcProjectSave` call, the `persist.serialize` / `persist.total` telemetry stages)
    - app/src/lib/efxPaintPersistence.ts:403-423 (`saveEfxPaintDocumentsWithProjectWrite` — the seam being replaced)
    - app/src/lib/efxPaintPackage.ts (plan 02: `buildPackageManifest`, `buildLayerFileRelativePath`, `collectPackageCacheRefs`)
    - app/src/types/project.ts:31-40 (the TS mirror of the persisted project)
    - app/src-tauri/src/commands/efx_paint_media.rs + app/src/lib/ipc.ts (plan 01: the media write command and wrapper)
  </read_first>
  <behavior>
    - every authoritative file in the set is written into the package staging root first; canonical `project.mce`, `layers/*.json` and `frames/*.webp` are untouched until the transaction publishes
    - the staging basename is minted per save against the Rust-validated prefix and the staging root is derived by Rust — the caller never names a destination root
    - saving writes `project.mce`, one `layers/<layerId>.json` per CHANGED paint layer, and one `frames/<layerId>/<keyId>.webp` per changed key; unchanged files are neither staged nor published
    - an empty change set performs no disk write at all — no staging root is created, no transaction is opened, and the save reports success
    - each layer sub-file's media references carry the digest returned by the native write for that key
    - the manifest carries `formatVersion` and `projectId` and the `efxPaint` index, and no layer content
    - the whole staged set is bound, published and committed through the plan-05 package transaction; a refusal or failure between staging and commit leaves the canonical package byte-identical to its pre-save state
    - the media files are written before the layer sub-files that reference their digests
    - the saved project type no longer requires an in-document paint payload carrier for the Studio to persist its layers
  </behavior>
  <action>
    Rewrite the save funnel in `efxPaintPersistence.ts` as `savePackage(packageDir, input)`:

    1. flush pending runtime work first (the caller keeps calling `requestPhysicPaintFlush()` before this point — verify it is still there and before the write set is computed).
    2. compute the changed file set with Task 1's tokens. If it is empty, return the committed result WITHOUT touching disk — the plan-05 bind refuses an empty set, and a no-op save must not create a staging generation or open a transaction.
    3. mint the staging basename and ensure the staging root exists. The basename uses the existing `createStagingBasename` literal and the root is whatever plan 01/05 derives from it in Rust — never a path this layer composes, because plan 01's guard exists precisely to keep the destination root out of renderer hands.
    4. for each CHANGED paint layer, project runtime records to persisted records (plan 06) while writing every changed key's bytes through `ipcEfxPaintWriteFrameMedia` (plan 01) pointed AT THE STAGING generation, and collect `(layerId, keyId) → FrameMediaReference` from what the command returned. Step 4 writes nothing canonical.
    5. build each changed layer sub-file with those references and `JSON.stringify` it INDIVIDUALLY, then write it into the staging root; then build the manifest with `buildPackageManifest` and write it into the staging root through the existing project-save wrapper pointed at the staged manifest path (plan 05 made that write take a supplied path). No step may serialize the whole project into one string, and no step may read bytes back just written.
    6. bind the staged set (changed media + changed layer sub-files + the manifest when its own token changed), publish it, and settle with commit. On ANY refusal or failure, settle with rollback and rethrow — the canonical package must be byte-identical to its pre-save state, including files that did not exist before.

    Keep the machine-local derived-frame cache write on the best-effort path plan 05 gave it — it must never be part of the bound authoritative set, and a cache failure must not fail the save.

    In `projectStore.ts`, keep `saveProject`/`saveProjectAs` as the orchestration seam: the project object passed to `ipcProjectSave` is the MANIFEST content (its existing fields plus `formatVersion`/`projectId`), staged as described above, and the layer sub-files plus media ride the package transaction. Retarget the `persist.serialize`/`persist.total` telemetry stages to per-file stages (e.g. `persist.manifest`, `persist.layers`, `persist.media`, `persist.commit`) so the Gate 4 before/after numbers are comparable per term rather than as one opaque total.

    Update `app/src/types/project.ts` with `formatVersion`/`projectId` on the persisted shape (leave the legacy carrier field to plan 08). Extend `projectStore.efxPaintCutover.test.ts` with the `<behavior>` cases.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/projectStore.efxPaintCutover.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/stores/projectStore.efxPaintCutover.test.ts` exits 0 with the new cases present by name
    - `pnpm --filter efx-motion-editor exec tsc --noEmit` exits 0
    - the test asserts the write ORDER (media before the layer file that references it) by capturing the mocked call sequence
    - the test asserts that every media write call carries the staging basename and that no call targets a canonical `frames/` path
    - the refused-save case asserts the canonical package files are byte-identical to their pre-save snapshot
    - the empty-change-set case asserts zero bind, publish and manifest calls
    - `grep -c 'persist.media' app/src/stores/projectStore.ts` returns at least 1
  </acceptance_criteria>
  <done>Saving stages the package file set, writes media before the sub-files that digest them, commits through one transaction, and leaves the canonical package untouched on any refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Round-trip contract — zero payloads, changed-files-only, no machine-local paths</name>
  <files>app/src/testUtils/packageFixture.ts, app/src/lib/efxPaintPackageRoundTrip.test.ts</files>
  <read_first>
    - app/src/viteBuild.test.ts (`makeTempDir` — the temp-dir idiom VALIDATION.md tells this plan to reuse rather than re-invent)
    - app/src/lib/efxPaintPersistence.ts (Task 2 output)
    - app/src/testUtils/testWebpBytes.ts (existing tiny WebP fixture bytes for media writes)
    - .planning/phases/52.2-project-package-format-references-only/52.2-VALIDATION.md (the per-requirement rows this file instantiates)
  </read_first>
  <behavior>
    - a save against a temp package dir produces a manifest and one layer sub-file, and reopening from those files reproduces the layer documents
    - no emitted file matches an image payload pattern (scan every emitted JSON for a string value of 512+ characters drawn from the base64 alphabet)
    - no emitted file contains an absolute path, a backslash path, or a drive letter
    - a second identical save rewrites zero files (assert the write list is empty)
    - changing one key's bytes rewrites exactly that media file and its layer sub-file
    - every media reference in the emitted layer file resolves to a file that exists in the fixture package
    - a save refused between staging and commit leaves every canonical file byte-identical to its pre-save snapshot, and removes a canonical file that did not exist before
    - no staging directory survives a completed save
  </behavior>
  <action>
    Create `app/src/testUtils/packageFixture.ts` (new helper) building a minimal 2-layer package on a temp dir — reuse the `makeTempDir` idiom from `viteBuild.test.ts` and the byte fixture from `app/src/testUtils/testWebpBytes.ts`. It must return the temp root, the layer ids, and the key ids so tests assert against real paths. Put it in `testUtils/` next to `testWebpBytes.ts` so test helpers stay in one place.

    Create `app/src/lib/efxPaintPackageRoundTrip.test.ts` covering every `<behavior>` case, using the mocked IPC boundary the other lib tests use. Co-locate it with `efxPaintPackage.test.ts` and `efxPaintPersistence.test.ts`: this repo keeps `*.test.ts` beside its source, so do not introduce a `__tests__/` directory. The payload scan must be implemented as a regex over serialized strings (`[A-Za-z0-9+/]{512,}={0,2}`) applied to each emitted file, never as a grep against source code — the assertion is about the artifacts on disk. Write the cases first, watch them fail, then make them pass (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPackageRoundTrip.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPackageRoundTrip.test.ts` exits 0 with at least 8 passing cases
    - the payload-scan case runs against every emitted file (assert the scanned file count is at least 3)
    - the unchanged-save case asserts an empty write list, not a reduced one
    - the refusal case asserts byte equality against a pre-save snapshot taken by the fixture, not merely that the save threw
    - `pnpm --filter efx-motion-editor exec vitest run` exits 0 (record the full-suite count in the summary)
  </acceptance_criteria>
  <done>The package save is proven on disk: round-trip identity, zero payloads, no machine-local paths, and changed-files-only writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime state → package files | Money-equivalent user content is written here; a partial write loses work |
| Package files → future readers | Whatever is written must be readable by the read-back leg and acceptable to the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-21 | Tampering | partial multi-file save | critical | mitigate | Every authoritative file is written into the staging generation and reaches its canonical path only through plan 05's publish step; the staged set is bound with the aggregate digest and committed as one, and any refusal settles with rollback. Nothing canonical is written before the transaction, so the pre-save package survives — asserted by the refusal case against a pre-save byte snapshot. |
| T-52.2-22 | Information Disclosure | raster payloads or machine-local paths in emitted JSON | high | mitigate | The round-trip test scans every emitted file for base64-shaped strings and for absolute/backslash/drive-letter paths; the parser (plan 02) refuses payload-carrying records on the way back in, so a leak fails both directions. |
| T-52.2-23 | Repudiation | a save silently rewriting unchanged files | medium | mitigate | The change set is asserted as an exact token set and the unchanged-save case asserts an empty write list, so "only changed files" is a test-observable contract rather than an intent. |
| T-52.2-24 | Denial of Service | whole-project serialization creeping back | medium | mitigate | Telemetry is split into per-file stages (`persist.manifest`, `persist.layers`, `persist.media`, `persist.commit`) so a regression to one big serialize shows up as a single dominant stage in the Gate 4 comparison instead of hiding inside `persist.total`. |
</threat_model>

<verification>
`pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPersistence.test.ts src/stores/projectStore.efxPaintCutover.test.ts src/lib/efxPaintPackageRoundTrip.test.ts` green; full suite green; `pnpm --filter efx-motion-editor exec tsc --noEmit` clean.
</verification>

<success_criteria>
A save stages exactly the changed package files and commits them through one transaction — manifest, layer sub-files, media — so no authoritative file reaches its canonical path outside the transaction, with zero payloads and zero machine-local paths in the emitted JSON, proven by an on-disk round-trip contract.
</success_criteria>

## Artifacts this phase produces

- `app/src/lib/efxPaintPersistence.ts`: `packageFileToken`, `computeChangedFiles`, `savePackage` (staged intake + bind/publish/settle)
- `app/src/stores/projectStore.ts`: `saveProject`/`saveProjectAs` over the staged package set + per-file telemetry stages
- `app/src/types/project.ts`: `formatVersion`, `projectId` on the persisted shape
- `app/src/testUtils/packageFixture.ts` (new), `app/src/lib/efxPaintPackageRoundTrip.test.ts` (new)

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-07-SUMMARY.md when done
</output>
