---
phase: 52.2-project-package-format-references-only
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements: [D-20, D-21, D-22]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low  # calibration: factor 1.0, sample_count 0 (<3) — figures are uncalibrated projections, not measurements

must_haves:
  truths:
    - "With the session rail-set pill armed, pressing Play starts at the solo window's first frame, not at the scrub cursor"
    - "With a persisted row-S track solo active, pressing Play starts at the first painted key of the soloed track(s); with no painted keys it starts at project frame 0"
    - "Every loop iteration returns to that same solo content start (never back to the pre-solo cursor)"
    - "With no solo armed, play-from-cursor behaves exactly as it does today (Phase 51 law, unchanged)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts"
      provides: "deriveSoloContentStart + resolvePlaybackStartIndex pure resolvers"
    - path: "app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts"
      provides: "solo-aware start index for both the start ref and the loop-wrap ref"
  key_links:
    - "`frameIndexRef.current` and `loopStartIndexRef.current` must be set from the SAME value — the loop-wrap branch reads `loopStartIndexRef`, so setting only the start ref makes iteration 2 jump back to the pre-solo cursor (violates D-22)"
    - "`PhysicsPaintStudio.tsx` is the only caller that derives the session solo window today; the row-S input must be threaded from that same call site so both solos share one resolver"
---

<objective>
Implement D-20/D-21/D-22: when any solo is armed, Play starts at the beginning of the isolated content — the session pill at its window's first frame, the persisted row-S track solo at the first painted key of the soloed track(s), falling back to project frame 0 — and every loop wrap returns there.

Purpose: solo means isolate-and-review, and a review starts at the beginning of what was isolated. This is a bounded UX deliverable that shares no files with the format workstream, so it lands in Wave 1 in parallel.

Output: two pure resolvers with tests, a solo-aware start index in `useRotoCachedPlayback` covering both the start and the loop-wrap refs, and the Studio call site supplying both solo inputs.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/phase-52.2-project-package-format.md
@.planning/phases/52.2-project-package-format-references-only/52.2-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure resolvers — solo content start and start index</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts, app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts (in full: `SoloPlaybackWindow.start` is already "first selected placement start" — the pill's content start; `includesFrame` fail-closed contract)
    - app/src/components/physic-paint/view/physicsPaintSoloArm.ts (the session arm signal; armed state is never persisted)
    - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts (the existing test idiom to extend)
  </read_first>
  <behavior>
    - `deriveSoloContentStart` returns `sessionWindow.start` when a session window is supplied (pill path, D-20 first system)
    - with no session window but one persisted row-S solo track whose painted keys are {7, 21, 40}, it returns 7 (D-20 second system)
    - with a row-S solo track and zero painted keys it returns 0 (project frame 0, D-21)
    - with neither solo active it returns 0
    - the result is clamped into [0, capacity - 1]; a capacity of 0 is handled without throwing
    - a session window whose start exceeds capacity still yields a value inside the frame list bounds in `resolvePlaybackStartIndex`
    - `resolvePlaybackStartIndex` returns the cursor's index when no solo is active (Phase 51 behavior byte-for-byte)
    - `resolvePlaybackStartIndex` returns the index of the first cached frame whose `appFrame >= contentStart` when a solo is active, and index 0 when no cached frame satisfies that
    - the resolvers never read session paint, the document, persistence or the bridge (pure, per the module's existing contract)
  </behavior>
  <action>
    Extend `app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts` with two pure exports:

    `deriveSoloContentStart(input: { sessionWindow: SoloPlaybackWindow | null; documentSoloTrackIds: readonly string[]; paintedKeyFrames: readonly number[]; capacity: number }): number` — precedence is session window first (its `.start` IS the D-21 content start by construction), then row-S (`documentSoloTrackIds.length > 0` → minimum of `paintedKeyFrames`, which the caller has already filtered to the soloed tracks), then 0. Clamp into `[0, capacity - 1]` and treat a non-finite or negative capacity as 0.

    `resolvePlaybackStartIndex(input: { soloActive: boolean; contentStart: number; cursorAppFrame: number; cachedFrames: readonly { appFrame: number }[] }): number` — when `soloActive` is false this must reproduce today's computation exactly (`cachedFrames.findIndex(entry => entry.appFrame === cursorAppFrame)`, falling back to 0 when absent); when true, return the index of the first entry with `appFrame >= contentStart`, falling back to 0. Both resolvers are total functions that never throw, matching the module's fail-closed style.

    Extend `physicsPaintRotoSoloWindow.test.ts` with all `<behavior>` cases, including an explicit case proving the no-solo path is unchanged. Write the cases first, watch them fail, then implement (tdd). Keep the existing `deriveSoloPlaybackWindow` behavior untouched — its test suite must stay green unchanged.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts` exits 0 with at least 9 new passing cases in addition to the pre-existing ones
    - grep of the test file shows a named case for each of: pill, row-S with keys, row-S without keys, no solo, clamp
    - `git diff --stat HEAD -- app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts` shows no change to the pre-existing `deriveSoloPlaybackWindow` assertions (new cases are additive)
  </acceptance_criteria>
  <done>Both resolvers exist as pure functions with full case coverage, and the pre-existing solo-window behavior is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Solo-aware start index in the cached playback hook (start ref + loop ref)</name>
  <files>app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts, app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts:180-245 (the cursor re-anchor block at :184-188 and the loop-wrap branch around :237 that reads `loopStartIndexRef`)
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts (the existing coverage — extend it, do not build a new harness)
    - app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts (Task 1 output)
  </read_first>
  <behavior>
    - with a solo active, arming/starting playback sets both `frameIndexRef.current` and `loopStartIndexRef.current` to the solo content start index
    - the first loop wrap returns to that same index (never to the pre-solo cursor index)
    - with no solo active, the existing cursor re-anchor behavior and its assertions are unchanged
    - the hook computes solo state from inputs supplied by its caller; it never reads solo signals or the document itself
  </behavior>
  <action>
    Replace the start-index computation at `useRotoCachedPlayback.ts:184-188` with a call to `resolvePlaybackStartIndex` from Task 1, and assign the result to BOTH `frameIndexRef.current` and `loopStartIndexRef.current`. D-22 is satisfied structurally by that single assignment because the wrap branch already reads `loopStartIndexRef`; do not add a second wrap path.

    Add `soloActive` and `soloContentStart` to the hook's inputs (or the smallest equivalent the hook's existing signature makes natural) and derive them in the caller — the hook must stay free of store/signal reads, consistent with its current design. Extend `useRotoCachedPlayback.test.ts` with the `<behavior>` cases: one asserting both refs, one asserting the wrap index, one asserting the no-solo path is unchanged. Write the cases first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts` exits 0 with at least 3 new passing cases
    - the test asserts `frameIndexRef.current === loopStartIndexRef.current` after a solo start
    - the test contains a named case proving the no-solo cursor path is byte-for-byte unchanged
  </acceptance_criteria>
  <done>The hook starts solo playback at the content start and every loop wrap returns there, with the no-solo path untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Studio call site supplies both solo inputs</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the `deriveSoloPlaybackWindow` call site and the `useRotoCachedPlayback` call site; the `setTrackSolo` usage that identifies the persisted row-S flags)
    - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts (check whether it also anchors a start index and needs the same inputs)
    - app/src/stores/efxPaintStore.ts (`setTrackSolo` — how a persisted per-track solo flag is read back)
  </read_first>
  <action>
    At the Studio call site, derive `soloActive` and `soloContentStart` per D-20/D-21 and pass them to `useRotoCachedPlayback`. The session path is the window already derived by `deriveSoloPlaybackWindow`. The row-S path needs the persisted per-track solo flags (from the document) plus the painted key appFrames of those tracks — collect them from the same authorities the timeline already reads and filter to the soloed tracks before calling `deriveSoloContentStart` (the resolver expects pre-filtered input). If `useRotoNavigationCoordinator.ts` independently anchors a start index, give it the same treatment in this task so both consumers share one rule.

    Keep the change minimal and local: no new global signal, no new store field — this is a derivation at the call site (D-21 applies only while a solo is active; the Phase 51 play-from-cursor law stands otherwise).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor run build</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output, or a chunk-size warning above the frozen 1320 kB budget</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor run build` exits 0 with no new chunk-size overage
    - `grep -c 'deriveSoloContentStart' app/src/components/physic-paint/PhysicsPaintStudio.tsx` returns at least 1
    - the change is a call-site derivation only: `git diff --stat HEAD -- app/src/components/physic-paint/PhysicsPaintStudio.tsx` shows the uncommitted working tree touched only at the playback call site, with no new signal or store field introduced
  </acceptance_criteria>
  <done>The Studio supplies both solo inputs so the pill and row-S paths resolve their content start through one shared rule; UI feel is confirmed in the Gate 5 UAT (plan 16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session solo arm → playback start | A session-only flag changes where playback begins |
| Persisted document solo flags → playback start | Document state influences transport behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-11 | Tampering | persisted row-S flags influencing transport | low | mitigate | Solo affects only the start index and the frame filter; `resolvePlaybackStartIndex` returns an index into `cachedFrames` (clamped), so no document value can move the playhead outside the frame list. |
| T-52.2-12 | Denial of Service | degenerate capacity / empty frame list | low | mitigate | Both resolvers are total: capacity <= 0 clamps to 0, an empty `cachedFrames` list yields index 0, and no division or index arithmetic can produce NaN — covered by explicit test cases. |
| T-52.2-13 | Spoofing | session arm persisted by accident | low | accept | `physicsPaintSoloArm.ts` keeps armed state in a session-only signal with no persistence path (pre-existing design, unchanged by this plan). |
</threat_model>

<verification>
`pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts` green; `pnpm --filter efx-motion-editor run build` clean and within the frozen chunk budget; no-solo behavior unchanged (asserted by the preserved existing cases).
</verification>

<success_criteria>
Both solo kinds start playback at their content start through one shared resolver, loop wraps return there, and the no-solo play-from-cursor law is provably untouched.
</success_criteria>

## Artifacts this phase produces

- `app/src/components/physic-paint/roto/physicsPaintRotoSoloWindow.ts`: `deriveSoloContentStart`, `resolvePlaybackStartIndex`
- `app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts`: solo-aware start and loop-wrap index
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx`: call-site derivation of both solo inputs

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-04-SUMMARY.md when done
</output>
