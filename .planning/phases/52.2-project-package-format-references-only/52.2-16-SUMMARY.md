---
phase: 52.2-project-package-format-references-only
plan: 16
subsystem: infra
tags: [package-format, rescue-converter, bundled-uat, d-25, d-01-miss, render-churn, pilot-verdict, skill, csp, close-out]

# Dependency graph
requires:
  - phase: 52.2-project-package-format-references-only
    provides: 52.2-08..15 (package format shipped, refusal gate + rescue converter, read-back leg, bridge by references, async pilot on the stroke-finalization + flush paths) + the 52r A-C/D-E/F/G-H defect closure
provides:
  - the fixture-conversion proof record (one run, fresh path, sha256 unchanged, 2 written / 0 skipped / 0 failed — mechanics, not scale)
  - the bundled-session outcomes: the five rows at `52.2-VALIDATION.md:79-83` on the rebuilt §7-pair package + the converted-fixture open proof
  - the D-25 re-measurement, the render-churn after capture and the pilot verdict in `SPECS/async-conventions.md` (local-only by design, gitignored)
  - `.claude/skills/efx-async-orchestration/SKILL.md` (the D-17 contract earned)
  - close-out chores: the 52r (E/H) diagnostic cluster removed; the body-background CSP fix
affects: [53]

# Actuals (#2632) — chars/4 over the realized code+skill diff (30,941 patch bytes,
# git diff 407de282..HEAD -- app .claude; SPECS records are gitignored local-only
# and excluded from this measurement).
actuals:
  tokens: 7735
  tasks: 3
  commits: 5      # MEASURED at close: git rev-list --count 407de282..HEAD = 4 + this metadata commit
plan_head_before: 407de2822aae65c25eb32eec3d7e8d1120b66abe

# Tech tracking
tech-stack:
  added: []
  patterns: [the packaged build as the only honest UAT surface for bundle/CSP behavior, dev runtime of the same commit for like-for-like telemetry, local-only measurement records (SPECS) alongside committed code and skills]

key-files:
  created:
    - .claude/skills/efx-async-orchestration/SKILL.md
  modified:
    - app/src/lib/physicPaintBridge.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
    - app/src/types/physicPaint.ts
    - app/index.html
    - app/src/index.css
  deleted:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalTermDigests.test.ts

key-decisions:
  - "D-01 process miss adjudicated option (a): the mandated post-format re-capture (B_med/B_p95/B_max) was never recorded → T1/T2 formally unevaluable; T3 recorded UNMET (max 10 891 ms — the waits are the by-design idle-gate deferral under a continuous train, recorded, not explained away); the ratified void-numbers branch applied by its rationale, not its letter; pilot ADMITTED on T4/T5 + inspectability; T4's 10-minute continuous-train leg recorded UNRUN, not passed"
  - "Skill written: D-17's condition (native UAT passes) is met — efx-async-orchestration authored as generic standards with the pilot as internal evidence; the render-churn conventions ride the measured before/after (§7 target met)"
  - "Telemetry option 1 executed and recorded: the numbers run in the dev runtime of the shipped commit (like-for-like with the dev-captured baselines) — the bundle emits no telemetry by design"
  - "Close-out chores folded into the plan: the temporary 52r (E/H) diagnostic cluster removed whole (child commit log, Play Script diff, parent console.warn, wire fields, helpers, test — no behavior change, every log site sat in a throwaway try/catch); the packaged-CSP inline-attribute finding fixed in the bundled stylesheet"
  - "The converter proof runs at fixture scale, recorded as mechanics-not-scale: the user's own 37 MB / 46-key original was discarded (confirmed 2026-09-14) and is not re-raised"

patterns-established:
  - "The bundled `.app` is the acceptance instrument; the dev runtime of the same commit is the measurement instrument — one protocol, one recorded rationale (bundle emits no telemetry by design)"
  - "Measurement records land in SPECS (local-only, gitignored) while repo commits carry code and skills — the two homes are deliberately different"

requirements-completed: [D-08, D-16, D-17, D-18, D-20, D-21, D-22, D-23, D-24, D-25, D-26]

coverage:
  - id: D1
    description: "The rescue converter proven live, once, on the pre-52.2 repo fixture into a fresh path — zero failed keys, written count equal to the fixture's real-key count (2), source sha256 unchanged before and after; recorded at fixture scale"
    requirement: D-08
    verification:
      - kind: other
        ref: "run 2026-09-14: `node scripts/mce-package-rescue.ts --in app/src/efx-paint/document/__fixtures__/pre-52.2-project.mce.json --out /tmp/efx-52.2-16-rescue` → 2 layers / 2 keys / 5 files written / 0 skipped / 0 failed; sha256 `52f22844c487a9b183c0f741207a3b5600f90e60101dddc775cc899a07a3f190` identical before and after, re-verified 2026-09-18"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every row at 52.2-VALIDATION.md:79-83 carries a recorded outcome with evidence on the bundled build against the rebuilt §7-pair package, plus the converted-fixture open proof"
    requirement: D-03, D-20..D-24
    verification:
      - kind: manual
        ref: "bundled session 2026-09-18 (bundle facf1351, subject ~/Desktop/efx-motion-editor-project-test/v1.0.0.mce): row 1 PASS (single EFX icon; warm + cold double-click open; unsaved-changes guard respected); row 2 PASS (⌘O + Save As file-style, .mce one item, no directory-picker fallback); row 3 PASS (project.mce + layers/ + frames/*.webp visible, webp previews readable, save writes only the changed .webp, no-change save near-instant, autosave commits with no bind modal); row 4 PASS (10-15-stroke train smooth, no freeze or progressive slowdown, finalization catches up during idle pauses, navigation responsive); row 5 PASS (pill solo starts at its window start + loop wraps there; row S starts at the first painted key with frame-0 fallback; steppers click-1-step / hold-repeat / keyboard / clamp); converted-fixture open PASS (/tmp/efx-52.2-16-rescue/pre-52.2-project.mce opens via ⌘O, both fixture keys present, near-blank canvas expected — 2×2-px fixtures)"
        status: pass
    human_judgment: true
  - id: D3
    description: "The after numbers, the render-churn after capture and the three-leg pilot verdict recorded in-place in SPECS/async-conventions.md; the skill written only because the pilot UAT passed"
    requirement: D-16, D-17, D-18, D-25
    verification:
      - kind: other
        ref: "grep gates: 'Pilot measurement' 2 · 'Render-churn after capture' 1 · 'Pilot verdict' 1 (SPECS/async-conventions.md); churn after: render.tracksStrip 123 vs 369 (−78% per-row normalized), render.rightPanel 4 vs 72, render.canvas 114 vs 248 — no surface increased, all three beat the ≥33% bar"
        status: pass
      - kind: other
        ref: ".claude/skills/efx-async-orchestration/SKILL.md exists (commit 4e40f2d5)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Close-out chores green: the 52r (E/H) diagnostic cluster removed with no behavior change; the packaged-CSP body-background fix applied"
    requirement: D-26 (record hygiene)
    verification:
      - kind: unit
        ref: "suite 210 files passed | 2 skipped (212); 3936 passed | 1 skipped | 101 todo (4038), 0 failed; `tsc --noEmit` exit 0; main chunk 1,333.07 kB (within the documented 1340 kB budget)"
        status: pass
    human_judgment: false

duration: multi-session (2026-09-14 amendment → 2026-09-18 close; human-gated checkpoint plan)
completed: 2026-09-18
status: complete
---

# Phase 52.2 Plan 16: Bundled UAT, D-25 re-measurement, pilot verdict Summary

**The phase is judged where it must be — on one bundled native session: all five human rows PASS on the rebuilt project package, the converted fixture opens, the D-25 re-measurement shows the input side collapsed (p95 104 ms, zero stalls) and the full-document serialize abolished (persist total 1 404 ms, manifest + layers 21 ms), the render-churn target is met, and the pilot verdict admits the async conventions on the T4/T5 + inspectability axis with the D-01 miss and T3-unmet recorded honestly — so the `efx-async-orchestration` skill is written and the phase closes.**

## Performance

- **Duration:** multi-session — amendment applied 2026-09-14, bundled session + numbers 2026-09-18 (human-gated checkpoint plan)
- **Tasks:** 3/3 (Task 2 is the blocking human-verify checkpoint; all rows recorded)
- **Commits:** 4 code/docs + this metadata commit
- **Files:** 10 tracked changed (67 insertions, 305 deletions) + 1 skill created

## Accomplishments

- **Rescue-converter proof — mechanics proven live at fixture scale.** One run on the git-committed fixture `app/src/efx-paint/document/__fixtures__/pre-52.2-project.mce.json` into a fresh path `/tmp/efx-52.2-16-rescue/pre-52.2-project.mce` (never in place): **2 layers / 2 keys → 5 files written / 0 skipped / 0 failed**, written count = the fixture's real-key count (2); source sha256 `52f22844…f190` identical before and after (re-verified 2026-09-18); converted document JSONs carry zero base64. **Recorded plainly: the fixture proves the mechanics, not the scale — the user's own 37 MB / 46-key original was discarded (confirmed 2026-09-14) and is not re-raised.**
- **Bundled session — Gates 4/5, five rows PASS + fixture-open PASS** (bundle `facf1351`, subject `~/Desktop/efx-motion-editor-project-test/v1.0.0.mce`): Finder ONE icon with warm and cold double-click (unsaved-changes guard respected); ⌘O/Save As file-style with no directory-picker fallback; package contents visible with webp previews, changed-media-only saves, near-instant no-change save, autosave committing past the debounce with no bind modal; 10-15-stroke train smooth with finalization catching up during idle pauses; both solos starting at content start with identical loop wraps; steppers click/hold/keyboard/clamp everywhere. The converted fixture opened via ⌘O with both keys present and the near-blank canvas expected (2×2-px fixtures).
- **D-25 re-measurement (dev runtime of the shipped commit, like-for-like with the dev-captured baselines — rationale recorded).** Root-cause hypothesis HELD: the base64 payload plus the decode path were the regression, and the physics render pipeline is not a regression target. Single finalization step: **median 113 ms** (pre-format ~900 ms); input delay: **median 16 ms / p95 104 ms / max 186 ms, 0 stalls**; queue waits stay in the idle-gate class by design (median 8 134 ms under a 26-stroke train, decaying 8 134 → 414 ms as the train ends; T3 as written recorded UNMET, not explained away). Persist leg: **`persist.total` 1 404 ms = commit 872 + media 460 + layers 11 + manifest 10** — the 2026-09-11 ~4.6 s full-document serialize is **abolished, not optimized** (n=1, recorded as an architectural proof).
- **Render-churn target met** (`§7`): `tracksStrip` 123 vs 369 (−66.7% raw, **−78% per row** normalized), `rightPanel` 4 vs 72 (−94.4%), `canvas` 114 vs 248 (−54.0%) — no surface increased; all three beat the ≥33% bar.
- **Pilot verdict — admitted on T4/T5 + inspectability.** Legs: step slicing schedulable YES (machine + bounded-turn queue in production, bounded drain in the capture); input-delay improved on every recorded comparison, reported with-confound (the mandated post-format baseline was never captured — the D-01 miss, recorded); 18 deterministic tests that were not writable against the implicit version exist (T5 asked ≥8). T4's 10-minute continuous-train leg recorded unrun. The D-17 contract's condition is met → `.claude/skills/efx-async-orchestration/SKILL.md` written.
- **Close-out chores landed:** the temporary 52r (E/H) diagnostic cluster removed whole (−304 lines, no behavior change) and the packaged-CSP finding fixed (body background moved from the inline style attribute into the bundled stylesheet).

## Task Commits

Task 2 is a checkpoint (human session) and Task 3's records live in gitignored `SPECS/`; the plan's repo commits are the amendment, the close-out chores and the skill:

1. **Task 0/1 prep: approved plan-16 amendment applied** — `1df36067` (docs — fixture rescue proof + rebuilt §7-pair as the UAT subject)
2. **Close-out: 52r (E/H) diagnostic cluster removed** — `33a92f3d` (chore)
3. **Close-out: base background out of the inline style (packaged CSP)** — `8e5f219d` (fix)
4. **Task 3: efx-async-orchestration skill (D-17 contract earned)** — `4e40f2d5` (feat)

**Plan metadata:** this SUMMARY + STATE.md + ROADMAP.md commit (docs)

## Files Created/Modified

- `.claude/skills/efx-async-orchestration/SKILL.md` (new) — the generic async-orchestration standard: adoption law, library placement, lifecycle machines, bounded-turn queues, failures-as-data, settle-through interruption, the yield rule, the signals bridge, port-preserving adoption, measurement discipline, regression detection.
- The 52r (E/H) diagnostic removal across `physicPaintBridge.ts`, `useRotoPhysicalEditCoordinator.ts`, `useRotoPlayScriptController.ts`, `physicsPaintRotoPlayScriptController.ts`, `physicsPaintRotoPhysicalModel.ts`, `app/src/types/physicPaint.ts`; deleted `physicsPaintRotoPhysicalTermDigests.test.ts`.
- `app/index.html` + `app/src/index.css` — the CSP fix.
- `SPECS/async-conventions.md` (local-only, gitignored) — `## Pilot measurement` (incl. the persist leg and the D-01 miss), `### Render-churn after capture`, `## Pilot verdict`.

## Decisions Made

1. **D-01 miss — option (a), recorded, not smoothed.** T1/T2 formally unevaluable; T3 UNMET on the capture with the idle-gate-premise note; the void-numbers branch applied by rationale (numbers are not evidence about the pilot when the baseline does not exist); admission on T4/T5 + inspectability; the unresolvable counterfactual stated (had B_* been captured and unmet, T3-unmet would have been the pilot's own failure). T4's 10-minute leg stays recorded as unrun.
2. **Skill written on the passing UAT** — the D-17 contract's condition, not the missing baseline, gates it; the record explains why so future readers can re-derive the T3 premise if the conventions are revisited.
3. **Close-out chores folded into the plan** — the user-queued H-diagnostic removal and the CSP finding were fixed here, before the summary, and are recorded as findings with their fixes (no silent absorption).
4. **The converter ran exactly once** (git is the fixture's backup; the source hash is the integrity proof) and the record states the scale plainly.

## Deviations from Plan

- **The D-01 process miss (inherited from plan 13's window)** — the mandatory post-format pilot-start re-capture was never recorded anywhere; discovered during this plan, adjudicated in the Task-3 record rather than papered over. Re-capturing B_* on a pre-pilot checkout was rejected as expensive (user's hand on a stroke train) and un-rescuing (it cannot change the admission axis).
- **The CSP finding (not in the plan)** — surfaced by the bundled session; cosmetic (no visible impact); fixed minimally per the named candidate (bundled stylesheet).
- **The persist capture needed a MAIN-window redo** — the first dump ran in the Studio console (per-window trace instances) and came back empty; the instrumentation-gap question was answered (no gap: `projectStore.ts:30` imports the trace module, so the MAIN window has the surface).
- **The per-row evidence required a relay** — the 5/5 report reached the user's side before the execution session; the per-row worksheet was relayed 2026-09-18 and is recorded here in full.

## Issues Encountered

- **T3 as ratified is unmet by design conflict** — "no queue-wait sample above 2 s" collides with the intact idle-gate deferral under a continuous train; recorded UNMET with the premise named as the first thing to re-derive if the conventions are revisited.
- **The UAT bundle predates the close-out chores** — the two chores (H removal, CSP fix) are no-behavior-change/сosmetic; the next bundle carries them. No passed row is affected: the H diagnostic was an extra console line, and the CSP finding was already visible (cosmetic) in the tested bundle.

## Known Stubs

None. Every plan-16 artifact exists: the records, the skill, the two close-out fixes, and the tracking updates.

## Threat Flags

None new. The plan's own threats are closed out: T-52.2-50 (every row recorded with evidence — this summary), T-52.2-58 (converter ran once on the committed fixture, fresh path, sha256 unchanged, mechanics-not-scale recorded), T-52.2-51 (verdict on the three named legs, same-session Task-2 capture, dev-runtime rationale recorded), T-52.2-52 (bundled surface — dev carries no Info.plist and enforces no CSP), T-52.2-53 (skill written only on the passing UAT).

## User Setup Required

None.

## Next Phase Readiness

- **Phase 52.2 is ready to close** — plan 16 was the last wave (16/16). Next GSD step: phase verification / close.
- **Carried phase-tail items (each needs the user's explicit go):** re-enable `ui.safety_gate`; delete the superseded `SPECS/phase-52.3-async-conventions.md` draft (user-owned); the save progress-modal UX idea stays a registered candidate (not folded); a main-app bug blocking project-sequence extension is routed as a later quick (undiagnosed, user-reported 2026-09-14).
- **The branch merge decision is the user's** (`v1.0.0/package-format-references-only` → `main`).
- **Phase 53** next: the signed release that exercises the Finder `.mce` association on a notarized build (the dev-mode folder presentation stays expected-by-design), plus the milestone acceptance pass.

## Self-Check: PASSED

- `SPECS/async-conventions.md` — Pilot measurement / Render-churn after capture / Pilot verdict present (grep 2/1/1) — FOUND
- `.claude/skills/efx-async-orchestration/SKILL.md` — FOUND (commit `4e40f2d5`)
- `/tmp/efx-52.2-16-rescue/pre-52.2-project.mce` — FOUND (5 files: manifest + 2 layers + 2 frames)
- source fixture sha256 `52f22844…f190` — re-verified 2026-09-18 — MATCHED
- commits `1df36067`, `33a92f3d`, `8e5f219d`, `4e40f2d5` — FOUND
- `git rev-list --count 407de282..HEAD` = 4 + this metadata commit — MEASURED
