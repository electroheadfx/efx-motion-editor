---
phase: 52.2-project-package-format-references-only
plan: 16
type: execute
wave: 9
depends_on: ["52.2-03", "52.2-04", "52.2-08", "52.2-09", "52.2-10", "52.2-11", "52.2-12", "52.2-13", "52.2-14", "52.2-15"]
files_modified:
  - SPECS/async-conventions.md
  - .claude/skills/efx-async-orchestration/SKILL.md
autonomous: false
requirements: [D-08, D-16, D-17, D-18, D-20, D-21, D-22, D-23, D-24, D-25, D-26]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low  # calibration: factor 1.0, sample_count 0 (<3) — figures are uncalibrated projections, not measurements

must_haves:
  truths:
    - "The phase is judged on ONE bundled native session covering spec Gates 4 and 5, not on the automated suite alone"
    - "The session opens the user's OWN project in its new package form: the pre-52.2 original converted once by the dev-only rescue converter into a fresh output path, with a per-key written/skipped/failed summary recorded and zero failed keys"
    - "Every human-check row in `52.2-VALIDATION.md` has a recorded outcome (pass, fail with evidence, or explicitly deferred with a reason)"
    - "The D-25 re-measurement runs immediately after this UAT, on the same real project, with the committed telemetry"
    - "The pilot verdict is numbers AND tests: a green suite with unchanged input-delay percentiles is a failure, not a pass"
    - "The async conventions enter the efx-motion skill family ONLY on a passing pilot UAT; on a failure the record says so and no skill is written"
    - "A residual performance finding attributable to the physics runtime core is recorded as a later, separate phase — never as scheduler slicing of the deferred render"
  artifacts:
    - path: "SPECS/async-conventions.md"
      provides: "the after numbers, the pilot verdict, the render-churn after numbers, and the skill-update decision"
    - path: ".claude/skills/efx-async-orchestration/SKILL.md"
      provides: "the generic async-orchestration standard (written only on a passing pilot UAT)"
  key_links:
    - "the user's pre-52.2 project is converted EXACTLY ONCE, into a fresh output path, never in place: it is the user's only copy of that material and the converter is one-way (D-08, spec §9)"
    - "`52.2-VALIDATION.md:79-83` names the five human rows this plan must close; each has a D-decision behind it, so an unrecorded row is an unverified decision"
    - "the packaged build is the only honest UAT surface for CSP and bundle behavior (dev enforces no CSP) — judge freshness by `app/src-tauri/target/release/bundle/macos/*.app`, never the dmg folder"
    - "D-25's gate is a decision record, not a fix: if a residual remains it is written down as the physics runtime core's own work in a later phase"
    - "the pilot's before numbers live in `SPECS/async-conventions.md` (plan 12) and the render-churn before numbers in the same section — plan 16 must extend them in place, not create a parallel section"
---

<objective>
Close the phase: the user's own real project converted into the package format by the dev-only rescue converter (D-08), then one bundled native session covering spec Gates 4 and 5, the committed D-25 re-measurement immediately after it, and the honest verdict — including whether the async conventions are earned enough to enter the skill family (D-17) or go back to the drawing board (spec §10).

Purpose: this phase changed where every pixel lives and rewired the busiest path in the app. The only way to accept it is to open the bundle, use the real project, and read the numbers — and after the clean break the real project only exists in a form the shipped reader accepts once the rescue converter has run on it. Everything automated was already proven plan by plan; this is the gate that decides the phase.

Output: the converted real project plus its per-key conversion summary, a recorded outcome for every human row, the after numbers in the conventions doc, the pilot verdict, and the skill decision.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/phase-52.2-project-package-format.md
@SPECS/async-conventions.md
@.planning/phases/52.2-project-package-format-references-only/52.2-VALIDATION.md
@.claude/skills/efx-preact-reactivity/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-UAT automated sweep + bundled build</name>
  <files>SPECS/async-conventions.md</files>
  <read_first>
    - .planning/phases/52.2-project-package-format-references-only/52.2-VALIDATION.md (the five human rows at :79-83 and the pre-verify requirement at :37 — the full suite plus the production build)
    - SPECS/async-conventions.md (the before numbers this sweep must not invalidate)
    - .planning/phases/52.2-project-package-format-references-only/52.2-09-PLAN.md (the read-back leg whose failure mode is an empty reopen — worth an explicit manual sanity note before the UAT session)
  </read_first>
  <action>
    Run the full automated gate before the native session and record the results in `SPECS/async-conventions.md` under the existing pilot section: full suite (`pnpm --filter efx-motion-editor exec vitest run`) with the test count, `pnpm --filter efx-motion-editor run build` with the measured main-chunk size, the Rust tests (`cargo test --manifest-path app/src-tauri/Cargo.toml`), and the zero-base64 contract scan over a freshly saved package.

    Then produce the bundle the UAT will run: `pnpm --filter efx-motion-editor run tauri build` (or the repo's existing release build script — read `app/package.json` first and use the script that exists, do not invent one). Report the .app path under `app/src-tauri/target/release/bundle/macos/`. Do NOT substitute a dev run: `tauri dev` carries no `Info.plist` and enforces no CSP, so it cannot exercise the document-package rows or the packaged-content rows at all.

    If any automated gate is red, STOP — do not hand a broken bundle to the native session, and record the failure in the summary.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor run build</automated>
    <fails_when>non-zero exit, or any "error TS" line, or a chunk-size warning above the frozen 1320 kB budget</fails_when>
    <automated>cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
    <fails_when>non-zero exit, or any "error[" line, or a failing test count above zero in the summary</fails_when>
    <automated>ls -d app/src-tauri/target/release/bundle/macos/*.app</automated>
    <fails_when>non-zero exit — the UAT needs a real bundle, not a dev run</fails_when>
  </verify>
  <acceptance_criteria>
    - the full suite exits 0 and its count is recorded in `SPECS/async-conventions.md`
    - the production build exits 0 within the frozen budget, and the measured main-chunk size is recorded next to the plan-13 pre-pilot measurement
    - the Rust test run exits 0
    - a `.app` exists under `app/src-tauri/target/release/bundle/macos/` and its path is recorded for the UAT session
    - the zero-base64 scan over a freshly saved package reports zero matches
  </acceptance_criteria>
  <done>Every automated gate is green on the exact bundle the native session will run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Convert the real project, then bundled native UAT — Gates 4 and 5</name>
  <files>SPECS/async-conventions.md</files>
  <action>
    FIRST convert the user's real project (D-08) — the shipped reader refuses pre-52.2 projects (plan 08 Task 2), so without this step the session has no subject to open. Ask the user to confirm the source path of their pre-52.2 project; CONTEXT.md's Verified Facts name `~/Desktop/efx-motion-editor-project-test/v1.0.0/`, whose 37 MB `.mce` file is the source — if that folder holds more than one `.mce` file, ask which one rather than guessing. Then run the dev-only converter from the repo root into a FRESH output path that is neither the source nor inside the repo: `node scripts/mce-package-rescue.ts --in "<source>.mce" --out "<fresh output dir>"`. Never run it in place and never write to the source: it is the user's only copy of that material and the converter is one-way (spec §9). Record the source path, the output path and the per-key written/skipped/failed summary the converter prints in the plan summary, and check the source file's digest before and after the run to prove it was not touched. Require zero failed keys AND a written count equal to the real-key count the source actually carries (CONTEXT.md's Verified Facts say 46 real keys across 2 layers — confirm against the source itself, do not trust the note). If any key fails or the counts disagree, STOP: a half-converted project is not a valid UAT subject, and the converter is fixed before any row is walked.

    THEN hand the bundled app to the user and walk the five rows at `52.2-VALIDATION.md:79-83` in ONE session on the bundled build, opening the CONVERTED package produced above — never the pre-52.2 original (the app refuses it) and never the committed fixture.

    Format rows (Gate 4): Finder shows ONE `.mce` icon and double-click opens the project; the open/save dialogs treat a package as one item (record which dialog style occurred); opening the folder shows the media files and a grep of the JSON shows zero base64; reopen composites the real keys; paint, save, and reopen a 2-layer project and report before/after persist timings.

    Pilot rows (Gate 4): the stroke-train painting capture — same real project, step sizes and input delays from the committed telemetry, against the baseline already recorded in `SPECS/async-conventions.md` (~900 ms atomic steps, ~9 s median queue waits). This capture IS the D-25 re-measurement: run it in the same session, immediately after the format rows, so the numbers describe the shipped build.

    UI/UX rows (Gate 5): each solo (the rail pill and the persisted row S) starts playback at its content start and every loop wrap returns there; the `− [field] +` steppers appear everywhere numeric inputs exist, suppress the native spinner arrows, keep keyboard editing and clamping, and hold-to-repeat after a short delay.

    Record each row as pass, fail with a screenshot or a number, or deferred with a reason. A fail is a legitimate outcome to record — what is not acceptable is an unrecorded row.
  </action>
  <what-built>
    The complete phase on a bundled macOS build: the user's real pre-52.2 project converted into a package with `frames/` media, `.mce` as a document package with zero base64 in JSON, the reference-shaped Studio bridge, digest-verified lazy reopen, the solo content-start rule, the `− [field] +` steppers, and the async pilot on the stroke-finalization and flush path.
  </what-built>
  <how-to-verify>
    First confirm the conversion: the per-key summary shows zero failed keys and a written count matching the source's real-key count, the source file is untouched, the bundled app opens the converted package, and the output path is recorded. Then run the bundled app (path recorded in Task 1) on that converted package and walk the five rows at `52.2-VALIDATION.md:79-83`: (1) Finder shows ONE `.mce` icon and double-click opens the project; (2) open and save-as dialogs treat the package as one item — record which dialog style occurred; (3) the folder shows the media files and a grep of the JSON shows zero base64, with before/after persist timings; (4) stroke-train painting feels right and the telemetry reports step sizes and input delays against the recorded baseline; (5) each solo (rail pill and row S) starts playback at its content start with every loop wrap returning there, and the steppers respond to click, hold-to-repeat, keyboard edit and clamping everywhere numeric inputs exist.
  </how-to-verify>
  <resume-signal>Report the conversion summary (source path, output path, written/skipped/failed per key) and then each of the five rows as pass, fail with evidence, or deferred with a reason — then continue to the D-25 record in Task 3.</resume-signal>
  <verify>
    <human-check>Confirm, first, the conversion of the real project: the source `.mce` the user named was converted into a fresh output path (never in place), the per-key summary shows zero failed keys and a written count equal to the source's real-key count, the source file is byte-identical to before, and the bundled app opens the converted package. Then confirm with the user, row by row, the five entries in `52.2-VALIDATION.md:79-83` on the bundled build: (1) single `.mce` icon + double-click opens; (2) open/save dialogs treat the package as one item; (3) folder shows media files and the JSON holds zero base64, with before/after persist timings; (4) stroke-train painting feel + step sizes + input delays against the recorded baseline; (5) solo content-start on Play with identical loop wraps, and the `− [field] +` steppers behaving (click, hold, keyboard, clamp). Approve, or report the specific failing rows.</human-check>
  </verify>
  <acceptance_criteria>
    - the user's pre-52.2 project was converted once, into a fresh output path and never in place, with the source path, the output path, the source's pre/post digest and the per-key written/skipped/failed summary all recorded in the plan summary
    - the conversion reports zero failed keys and a written count equal to the real-key count the source carries; a mismatch stopped the plan before any UAT row was walked
    - every one of the five rows has a recorded outcome with evidence (screenshot, measured number, or explicit deferral with the reason), reported against the CONVERTED package
    - the stroke-train capture produced step-size and input-delay numbers comparable to the recorded baseline
    - the findings are recorded in the plan summary before any fix work is proposed — the phase must not silently absorb a regression into a later task
  </acceptance_criteria>
  <done>The user's real project exists in the package format with a recorded per-key conversion summary, the bundled build has been exercised end-to-end on it, and every human row has an honest outcome.</done>
</task>

<task type="auto">
  <name>Task 3: D-25 re-measurement record + pilot verdict + skill decision</name>
  <files>SPECS/async-conventions.md, .claude/skills/efx-async-orchestration/SKILL.md</files>
  <read_first>
    - SPECS/async-conventions.md (the pilot section's baseline → target, the before numbers from plan 12, and the skill-update contract clause)
    - SPECS/phase-52.2-project-package-format.md (§10 honest-failure clause and the skill update contract; §13 gates)
    - .claude/skills/efx-preact-reactivity/SKILL.md (the authoring standard the new skill must match — generic, keyword-rich, standards-shaped, with incidents as evidence rather than the subject)
    - .planning/phases/52.2-project-package-format-references-only/52.2-14-SUMMARY.md (what the pilot actually shipped and which tests exist now)
  </read_first>
  <action>
    Extend `SPECS/async-conventions.md` IN PLACE (do not add a parallel section) under three headings the gates below look for by name: `## Pilot measurement` holding the after numbers from Task 2 next to their before counterparts (step sizes and input delays), `### Render-churn after capture` holding one row per instrumented surface key from plan 12 (`render.tracksStrip`, `render.rightPanel`, `render.canvas`, read from the same profile snapshot surface the before capture used) so the pair is diffable, and `## Pilot verdict` for the judgement. State explicitly whether the phase's root-cause hypothesis held — the base64 payload plus the decode path were the regression, and the physics render pipeline is not a regression target.

    Then state the pilot verdict against D-16's own success criterion, which is numbers AND tests: report whether step slicing became schedulable, whether input-delay percentiles improved measurably, and which targeted tests now exist that were not writable against the implicit version. If any leg fails, write the honest-failure outcome from spec §10 — conventions back to the drawing board — and say which leg failed. Do not smooth a failure into a partial success.

    Finally apply the skill-update contract (D-17): on a passing pilot UAT, author `.claude/skills/efx-async-orchestration/SKILL.md` as a generic async-orchestration standard (lifecycle machines, bounded-turn queues, backpressure, interruption, the signals bridge) with the pilot as internal evidence and the project's Preact reactivity rules as the binding constraint; on a failing pilot UAT, write no skill and record that the contract is not yet earned. Any residual attributable to the physics runtime core is recorded as a later, separate phase — never as scheduler slicing of the deferred render (D-25). D-26 stands as a regression law over this record: the native rewrite is settled NO (2026-09-12 — format debt is not a stack fault) and reopens only as v2.0 on post-52.2 measured WebView bottlenecks, so a residual must not be written up as a rewrite proposal.
  </action>
  <verify>
    <automated>grep -c 'Pilot measurement' SPECS/async-conventions.md</automated>
    <fails_when>0 matches — the after numbers and the verdict must land in the doc, not only in a summary</fails_when>
    <automated>grep -c 'Pilot verdict' SPECS/async-conventions.md</automated>
    <fails_when>0 matches — the numbers-and-tests verdict must be explicit</fails_when>
    <automated>grep -c 'Render-churn after capture' SPECS/async-conventions.md</automated>
    <fails_when>0 matches — the three instrumented surfaces must carry after numbers, not only the pre-pilot before capture</fails_when>
  </verify>
  <acceptance_criteria>
    - the doc contains the after numbers adjacent to their before counterparts, and the render-churn counters appear for all three instrumented surfaces
    - the verdict is stated against all three legs (slicing schedulable, input-delay percentiles, tests that were not previously writable), with the failing leg named if any
    - `.claude/skills/efx-async-orchestration/SKILL.md` exists only if the pilot UAT passed; otherwise the doc records why it does not
    - any residual is attributed to the physics runtime core and labeled a later, separate phase, with no rewrite proposal (D-26)
    - `SPECS/async-conventions.md` carries no unmeasured qualitative claim about performance
  </acceptance_criteria>
  <done>The phase's outcome is recorded as measurements and a verdict, and the skill family gains async rules only if they were earned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT session → phase acceptance | The only evidence that the shipped bundle works for a human |
| Verdict → skill family | A passing verdict changes how all future code is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-50 | Repudiation | a UAT row silently skipped | high | mitigate | Every row at `52.2-VALIDATION.md:79-83` must carry a recorded outcome (pass, fail with evidence, or deferral with a reason) in the summary before the plan can close. |
| T-52.2-58 | Tampering | the rescue converter writing into the user's only pre-52.2 copy | high | mitigate | The converter runs once, into a fresh output path, with the source path opened read-only; the source's digest is captured before and after the run and must be unchanged, and the write count must equal the source's real-key count with zero failures before any UAT row is walked (D-08, spec §9). |
| T-52.2-51 | Tampering | a green suite passed off as a pilot success | high | mitigate | The verdict is written against D-16's three named legs, and the numbers must be the same-session Task 2 capture — not the plan-13 pre-pilot measurement and not a suite result. |
| T-52.2-52 | Spoofing | UAT run on a dev build | medium | mitigate | Task 1 requires a `.app` under `bundle/macos/` and Task 2 is explicitly a bundled session; dev carries no `Info.plist` and enforces no CSP. |
| T-52.2-53 | Elevation of Privilege | unvalidated conventions entering the skill family | medium | mitigate | The skill is written only on a passing pilot UAT, per the D-17 contract clause already recorded in the doc before this plan ran. |
</threat_model>

<verification>
Full suite, production build and Rust tests green on the shipped bundle; a `.app` under `app/src-tauri/target/release/bundle/macos/`; the user's real project converted once into a fresh package with a recorded per-key summary and zero failed keys; the five `52.2-VALIDATION.md` rows recorded with evidence against that converted package; the after numbers and the three-leg verdict written into `SPECS/async-conventions.md`; the skill written only if earned.
</verification>

<success_criteria>
The phase closes on a recorded native verdict taken on the user's own converted project: the format's five human rows answer for themselves, the D-25 re-measurement is stated against its own hypothesis, and the async conventions either enter the skill family on measured evidence or go back to the drawing board — written down either way.
</success_criteria>

## Artifacts this phase produces

- the user's real project converted into a package at a fresh output path, plus its per-key written/skipped/failed summary — recorded in `52.2-16-SUMMARY.md` (the UAT's subject; the source is never written to)
- `SPECS/async-conventions.md`: after numbers beside their baselines, the render-churn after capture, the three-leg pilot verdict, and the skill-update decision
- `.claude/skills/efx-async-orchestration/SKILL.md` (conditional): the generic async-orchestration standard, written only on a passing pilot UAT
- UAT evidence for the five `52.2-VALIDATION.md` rows, recorded in the plan summary

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-16-SUMMARY.md when done
</output>
