---
phase: 52.2-project-package-format-references-only
plan: 12
type: execute
wave: 5
depends_on: ["52.2-10"]
files_modified:
  - app/src/components/physic-paint/performance/renderCounters.ts
  - app/src/components/physic-paint/performance/renderCounters.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
  - app/src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx
  - app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts
  - app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts
  - SPECS/async-conventions.md
autonomous: false
requirements: [D-01, D-17, D-18]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The async conventions doc answers, per candidate flow, the architectural problem, the chosen tool (XState / Effect / NEITHER), the signals bridge pattern and the expected measurable improvement"
    - "The doc names the non-imposition law: 'neither library here, and nowhere yet' is a legitimate verdict for any area"
    - "The user has reviewed and approved the doc BEFORE any XState/Effect code or dependency is introduced (spec Gate 2)"
    - "Render counters exist on the tracks strip, the right panel and the canvas area, and they are gated so they cost nothing unless profiling is enabled"
    - "The before numbers for the instrumented surfaces are recorded in the doc"
  artifacts:
    - path: "SPECS/async-conventions.md"
      provides: "the conventions doc the user reviews at Gate 2 + the render-churn inventory"
    - path: "app/src/components/physic-paint/performance/renderCounters.ts"
      provides: "gated render counters for the three instrumented surfaces"
  key_links:
    - "the depends_on on plan 10 is a measurement-integrity dependency, not a file conflict: the D-16/D-18 BEFORE numbers must be captured on the post-bridge code, otherwise the before/after comparison spans two different transports and measures the wrong delta"
    - "Gate 2 is blocking: plans 13/14 (pilot deps + pilot code) must not start until the checkpoint is approved — that is exactly why they depend on this plan"
    - "the coverage mask must say 'I read this' (distinguishing unchanged from wrong)"
    - "counters must stay off the render hot path when profiling is disabled — an always-on counter would itself become render churn"
---

<objective>
Produce the two artifacts Gate 2 depends on: the async conventions doc (D-17) — the per-flow decision record the user reviews BEFORE any XState/Effect code — and the render-churn inventory (D-18) with counters on the three worst surfaces plus the before numbers that make the pilot's and the conventions' effect measurable.

Purpose: the phase's async workstream is conventions-first by law. This plan is the gate that keeps "progressive adoption" honest: every candidate flow must answer a named architectural problem, and "neither library here" is a legitimate, recorded verdict.

Output: `SPECS/async-conventions.md` (reviewed at a blocking checkpoint) and gated render counters live on the tracks strip, the right panel and the canvas area.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/phase-52.2-project-package-format.md
@.planning/phases/52.2-project-package-format-references-only/52.2-CONTEXT.md
@.planning/phases/52.2-project-package-format-references-only/52.2-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gated render counters</name>
  <files>app/src/components/physic-paint/performance/renderCounters.ts, app/src/components/physic-paint/performance/renderCounters.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts (the committed, dev-gated telemetry instrument: the `efx.physicsPaint.profile` gate, capture/dump shape)
    - .planning/phases/52.2-project-package-format-references-only/52.2-RESEARCH.md (§State centralization & render discipline — the doc's render-churn requirement)
    - app/src/stores/canvasStore.ts or any store exposing a version signal (the version-bump discipline the counters will evidence)
  </read_first>
  <behavior>
    - with profiling disabled, counting is a no-op that allocates nothing per render
    - with profiling enabled, each named surface accumulates a render count retrievable as a snapshot
    - the snapshot is stable JSON (surface name → count) so two captures can be diffed
    - counts reset deterministically, so a before/after capture pair is comparable
  </behavior>
  <action>
    Create `app/src/components/physic-paint/performance/renderCounters.ts` (new) mirroring the existing trace module's gating so it shares one switch (`efx.physicsPaint.profile`) rather than inventing a second flag. Expose `countRender(surface)` and `snapshotRenderCounts()` / `resetRenderCounts()`. When profiling is off, `countRender` must be a no-op with no allocation and no signal write — an always-on counter would itself create render churn and would invalidate the measurement it exists to make.

    Write `renderCounters.test.ts` first (gating, accumulation, snapshot shape, reset), watch it fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/performance/renderCounters.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/performance/renderCounters.test.ts` exits 0 with at least 4 passing cases
    - the disabled case asserts the counter map is still empty after 100 calls
    - the snapshot case asserts a plain object with string keys and number values
  </acceptance_criteria>
  <done>Render counting is free when disabled and diffable when enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument the three surfaces and capture the before numbers</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx, app/src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx, app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts, app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx (the tracks-strip representative — it is the row that repeats per track)
    - app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts and app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (the right panel and why it is memoized today)
    - app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts and PhysicsPaintCanvasMount.tsx (the canvas area)
    - app/src/components/physic-paint/performance/renderCounters.ts (Task 1 output)
  </read_first>
  <action>
    Add a single `countRender('tracks-strip' | 'right-panel' | 'canvas')` call at the render body of each surface (one call per component, at the top of the function body, before any signal read that could early-return). Keep the call unconditional in the component but no-op when profiling is disabled — do not wrap it in a conditional subscription, which would change the component's reactive reads and therefore its render behavior. This is instrumentation only: do NOT restructure the components, and do NOT change what each component subscribes to (that is the point of measuring first).

    Extend `PhysicsPaintTrackRow.test.tsx` with a case asserting a render increments the 'tracks-strip' counter when profiling is enabled and does not when it is disabled.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx src/components/physic-paint/performance/renderCounters.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <human-check>Native capture (feeds the doc): with profiling enabled, exercise the Studio (select a track, scrub, paint a stroke) and capture the render counters for the three surfaces. Record the numbers in `SPECS/async-conventions.md`. This capture is the BEFORE half of the pilot's before/after evidence.</human-check>
  </verify>
  <acceptance_criteria>
    - both named suites exit 0 and the full suite exits 0
    - each of the three files contains exactly one `countRender(` call
    - the change is instrumentation-only: `git diff --stat HEAD -- app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx app/src/components/physic-paint/view/MemoizedPhysicsPaintRightPanel.ts app/src/components/physic-paint/engine/MemoizedPhysicsPaintCanvasMount.ts` shows the uncommitted working tree touched only by the added `countRender(` lines and their import
    - the before numbers are recorded in `SPECS/async-conventions.md` with the surface names matching the counter keys
  </acceptance_criteria>
  <done>Three surfaces are instrumented without behavioral change and their before numbers are captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Async conventions doc + Gate 2 review</name>
  <files>SPECS/async-conventions.md</files>
  <action>
    Write `SPECS/async-conventions.md` and submit it for review. Required content — for EVERY candidate flow, not only the pilot: (a) the architectural problem it solves; (b) the chosen tool, explicitly XState, Effect, or NEITHER; (c) the signals bridge pattern (how the flow's process state maps onto Preact signals, per spec §2.7); (d) the expected measurable improvement as baseline → target, telemetry-backed. Required sections: the candidate-flow table covering sensitivity-map rows 1-12 with a verdict per row (including explicit "neither library here, and nowhere yet" verdicts, which are legitimate per §2.10); the state-centralization conventions (domain stores with derived computeds, narrow-selector read discipline, version-bump discipline); the render-churn inventory with the before numbers from Task 2 and the target; the skill-update contract (async rules enter the efx-motion skill family only after pilot native UAT passes; until then all XState/Effect code stays behind the pilot module boundary); the honest-failure clause (§10: if the pilot cannot beat baseline on numbers AND tests, conventions go back to the drawing board). Guidance sources to use while authoring: Effect — Context7 `/effect-ts/effect`, `/websites/effect_website_v4`, `/kitlangton/effect-solutions`; XState — the v6 repo README/CHANGELOG/examples plus Stately announcements (Context7 covers v5 only and must not be used for the v6 API surface).

    Then STOP and ask the user to review it. Do not write XState or Effect code, and do not install the dependencies, until the checkpoint is approved (spec Gate 2, D-17).
  </action>
  <what-built>
    `SPECS/async-conventions.md` — the per-flow decision record (XState / Effect / NEITHER for sensitivity-map rows 1-12), the signals bridge pattern, the state-centralization conventions, the render-churn inventory with the before numbers captured in Task 2, the skill-update contract, and the honest-failure clause. No library has been installed and no XState/Effect code exists yet.
  </what-built>
  <how-to-verify>
    Read `SPECS/async-conventions.md` and check four things: (1) every candidate flow has a verdict, including NEITHER where that is the honest answer; (2) the pilot section states a numeric baseline and a numeric target, both traceable to the committed telemetry; (3) the signals bridge pattern is specified concretely enough to implement; (4) the skill-update contract and the honest-failure clause are present as explicit clauses.
  </how-to-verify>
  <resume-signal>Approve (Gate 2 closed — plans 13 and 14 may start), or return the specific corrections to apply before re-review.</resume-signal>
  <verify>
    <human-check>Read SPECS/async-conventions.md and confirm: every candidate flow has a verdict (including NEITHER where that is the honest answer); the pilot has a baseline → target with numbers drawn from committed telemetry; the signals bridge pattern is specified; the skill-update contract and the honest-failure clause are present. Approve, or return specific corrections.</human-check>
  </verify>
  <acceptance_criteria>
    - `SPECS/async-conventions.md` exists and covers sensitivity-map rows 1-12 with one verdict each
    - the pilot section states a numeric baseline and a numeric target, both traceable to the committed telemetry
    - the doc contains the non-imposition law and the skill-update contract as explicit clauses
    - the user has approved the doc (recorded in the plan summary with the date)
  </acceptance_criteria>
  <done>The conventions doc is written, contains a verdict for every candidate flow, and the user has approved it — Gate 2 closed. No XState/Effect code or dependency exists yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Instrumentation → production render path | Measurement code runs inside the hottest components |
| Conventions doc → later phases | The doc is the authority later adoption decisions reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-41 | Denial of Service | instrumentation causing the churn it measures | medium | mitigate | `countRender` is a no-op with no allocation when profiling is disabled; the check is a module-level gate read, not a subscription, and the component's reactive reads are unchanged (asserted by diff inspection). |
| T-52.2-42 | Repudiation | conventions adopted without a named problem | medium | mitigate | The doc requires a verdict per sensitivity-map row and explicitly permits NEITHER as an outcome; Gate 2 is blocking, so adoption cannot proceed on an unrecorded justification. |
| T-52.2-43 | Tampering | dependency installed before the gate | medium | mitigate | Plans 13 and 14 depend on this plan, so the install cannot be scheduled before Gate 2 approval; the gate is a blocking checkpoint, not advisory. |
</threat_model>

<verification>
`pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/performance/renderCounters.test.ts src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx` green; full suite green; `SPECS/async-conventions.md` complete; the blocking checkpoint approved by the user.
</verification>

<success_criteria>
The async workstream has an approved decision record covering every candidate flow, and the three worst surfaces have gated counters with their before numbers captured — so the pilot and the state conventions can be judged on measurements rather than impressions. Gate 2 closed, no library installed.
</success_criteria>

## Artifacts this phase produces

- `SPECS/async-conventions.md` (new): per-flow verdict table (XState / Effect / NEITHER), signals bridge pattern, state-centralization conventions, render-churn inventory, skill-update contract, honest-failure clause
- `app/src/components/physic-paint/performance/renderCounters.ts` (new): `countRender`, `snapshotRenderCounts`, `resetRenderCounts`
- `app/src/components/physic-paint/performance/renderCounters.test.ts` (new)
- Instrumented surfaces: `PhysicsPaintTrackRow.tsx`, `MemoizedPhysicsPaintRightPanel.ts`, `MemoizedPhysicsPaintCanvasMount.ts`

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-12-SUMMARY.md when done
</output>
