---
phase: 52.2-project-package-format-references-only
plan: 13
type: execute
wave: 6
depends_on: ["52.2-12"]
files_modified:
  - SPECS/async-conventions.md
  - app/package.json
  - pnpm-lock.yaml
  - app/src/viteBuild.test.ts
autonomous: true
requirements: [D-15, D-16, D-19]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low  # calibration: factor 1.0, sample_count 0 (<3) — figures are uncalibrated projections, not measurements

must_haves:
  truths:
    - "The library state is re-verified AT THE DATE THE PILOT STARTS, not trusted from the phase's research date"
    - "Dependencies are pinned to the exact verified versions, including the alpha/RC status recorded in the conventions doc"
    - "The frozen 1320 kB chunk budget is re-measured with the new dependencies, and any raise is an explicit documented decision"
    - "The main chunk does not grow by the size of the new libraries — the pilot code lives behind the Studio lazy boundary"
    - "The published effect-ts Claude skill is installed before pilot code is written"
  artifacts:
    - path: "app/package.json"
      provides: "pinned xstate + effect versions"
    - path: "SPECS/async-conventions.md"
      provides: "re-verification outcome (versions, breaking changes, bindings) + the budget decision record"
  key_links:
    - "the pilot's import boundary decides the budget outcome: `physicsPaintFlush.ts` is imported by the main chunk (projectStore.ts:31, exportEngine.ts:3) and must stay a thin facade with no library imports, while the machine/queue live behind the Studio lazy chunk"
    - "`app/src/viteBuild.test.ts` pins `chunkSizeWarningLimit` to exactly 1320 — a raise requires editing that assertion AND recording the decision, otherwise the build gate fails for the wrong reason"
---

<objective>
Close the pilot's preconditions: re-verify XState v6 and Effect v4 at the pilot's actual start date (D-15), install the exact verified versions with the published effect-ts skill (D-16), and re-measure the frozen chunk budget with them, recording any raise as an explicit decision (D-19).

Purpose: the format waves ran months of work between the phase's research and the pilot. Alpha/RC libraries move; the phase's own guidance says re-verify at that date. This plan is the boundary between "conventions approved" and "pilot code written".

Output: pinned dependencies, a recorded re-verification, a measured budget outcome, and the effect-ts skill installed.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/async-conventions.md
@.planning/phases/52.2-project-package-format-references-only/52.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify XState v6 and Effect v4 at the pilot date</name>
  <files>SPECS/async-conventions.md</files>
  <read_first>
    - SPECS/async-conventions.md (§Pilot + the candidate-flow table, authored in plan 12 — the verdicts this re-verification can invalidate)
    - .planning/phases/52.2-project-package-format-references-only/52.2-RESEARCH.md (the versions verified at research time and the guidance sources to re-consult)
    - .planning/phases/52.2-project-package-format-references-only/52.2-CONTEXT.md (D-15: what must be re-verified — XState npm dist-tag, breaking changes since 2026-06, Preact bindings; Effect RC vs stable)
  </read_first>
  <action>
    Re-verify, and record in `SPECS/async-conventions.md` under a dated "Pilot re-verification" heading: (a) the npm dist-tags for `xstate` and `effect` and the exact version to pin from each; (b) for XState v6, every breaking change since the research date — check the repository README/CHANGELOG/examples and the Stately announcements; Context7 covers v5 only and must not be used for the v6 API surface; (c) whether a Preact binding exists for v6 (research says none — the conventions doc's manual signals bridge is the plan, so confirm it is still the right plan); (d) for Effect, RC vs stable and whether `@effect/platform` is still to be avoided (it was: zero-dep core only); (e) whether the published effect-ts skill still targets v4.

    If any finding contradicts a verdict in the candidate-flow table, update the affected row and say so explicitly — do not silently reconcile.
  </action>
  <verify>
    <automated>pnpm view xstate dist-tags --json && pnpm view effect dist-tags --json</automated>
    <fails_when>non-zero exit, or a JSON payload that does not contain an alpha/beta/rc tag for xstate and a 4.x version for effect</fails_when>
    <automated>grep -c 'Pilot re-verification' SPECS/async-conventions.md</automated>
    <fails_when>0 matches — the re-verification outcome must be recorded in the doc, not only in this plan's summary</fails_when>
  </verify>
  <acceptance_criteria>
    - both package-manager view commands exit 0 and their output is quoted in the recorded section
    - the recorded section names an exact version string for each library, with no "latest"-style wording
    - the recorded section states whether a Preact binding exists and what the signals bridge will therefore be
    - any candidate-flow row invalidated by the findings is updated in the same commit
  </acceptance_criteria>
  <done>Versions and breaking changes are re-verified at the pilot date and recorded in the conventions doc.</done>
</task>

<task type="auto">
  <name>Task 2: Install the pinned dependencies without touching the main chunk</name>
  <files>app/package.json, pnpm-lock.yaml</files>
  <read_first>
    - app/package.json (current dependency set and version conventions in this repo)
    - app/src/components/physic-paint/bridge/physicPaintFlush.ts (the facade imported by the main chunk — it must stay free of library imports)
    - app/src/stores/projectStore.ts:31 and app/src/lib/exportEngine.ts:3 (the main-chunk import sites that make the facade's thickness a budget question)
    - .planning/phases/52.2-project-package-format-references-only/52.2-VALIDATION.md (the build gate this must not break)
  </read_first>
  <action>
    Install `xstate` and `effect` at the exact versions recorded in Task 1, pinned without a range that could resolve to a different major/minor (this repo's convention: read how existing dependencies are pinned before choosing the syntax). Add nothing else — no `@effect/platform`, no XState binding package.

    Confirm the import boundary before installing: the libraries may be imported ONLY from modules that are reachable exclusively through the Studio's lazy chunk. Record in the plan summary the list of files that will import them and confirm that none is reachable from the main chunk entry. Do not wire any import yet — this task installs and prepares, it does not use.
  </action>
  <verify>
    <automated>pnpm install</automated>
    <fails_when>non-zero exit, or any "ERR_PNPM" / "peer" error line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor run build</automated>
    <fails_when>non-zero exit, or any "error TS" line, or a chunk-size warning above the frozen 1320 kB budget</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 with no peer-dependency errors
    - `pnpm --filter efx-motion-editor run build` exits 0 with no chunk-size warning
    - `grep -c '"xstate"\|"effect"' app/package.json` returns 2, each with an exact pinned version
    - `grep -rc 'from .xstate.\|from .effect.' app/src/stores/projectStore.ts app/src/lib/exportEngine.ts` returns 0 for both files
  </acceptance_criteria>
  <done>Both libraries are installed and pinned, and the main chunk is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Budget re-measurement + effect-ts skill install</name>
  <files>app/src/viteBuild.test.ts, SPECS/async-conventions.md</files>
  <read_first>
    - app/src/viteBuild.test.ts:76-190 (the captured chunk budget, the exactly-1320 assertion, the lazy-chunk separation test, and the measured-size comments that record each historical raise)
    - SPECS/async-conventions.md (Task 1 output)
  </read_first>
  <action>
    Measure the production build with the new dependencies present but unused, and record the numbers in `SPECS/async-conventions.md` next to the historical measurements (`app/src/viteBuild.test.ts` carries the precedent comments: 1100 → 1110, then 1320). If the budget must change, edit the assertion in `app/src/viteBuild.test.ts` AND record the raise as an explicit documented decision with the measured justification — never change the number without the record, and never change it in anticipation of the pilot's code (the pilot lives behind the lazy boundary precisely so this becomes unnecessary).

    Install the published effect-ts Claude skill: `npx skills add tenequm/skills@effect-ts` (or the ClawHub equivalent), then confirm the skill is discoverable and that it targets v4 — if it targets v3, record that and note the parts of it that still apply, rather than adapting pilot code to it.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/viteBuild.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>grep -c 'Pilot dependencies' SPECS/async-conventions.md</automated>
    <fails_when>0 matches — the measured numbers and the budget decision must be recorded</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/viteBuild.test.ts` exits 0
    - the recorded section contains the measured main-chunk size with the dependencies installed and the delta versus the pre-install measurement
    - the PhysicsPaintStudio lazy chunk separation assertion still passes
    - the effect-ts skill is installed and its target version is recorded (v4, or the exact mismatch if not)
  </acceptance_criteria>
  <done>The budget is measured with evidence rather than assumed, and the guidance skill is in place before any pilot code is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → app | Two new runtime dependencies, one of them an alpha |
| Third-party skill → agent instructions | The effect-ts skill shapes how pilot code is written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-SC | Tampering | npm installs (`xstate`, `effect`) | high | mitigate | Both are the packages named by the phase decision (D-15), pinned to exact versions re-verified in Task 1 from the registry's own dist-tags; no transitive additions are requested, no `@effect/*` side package is installed, and the install is gated by a clean `pnpm install` + a green full build. No [SUS]/[SLOP] packages are introduced. |
| T-52.2-44 | Denial of Service | alpha/RC churn raising the frozen budget | medium | mitigate | The pilot imports are confined to the Studio lazy chunk; the budget assertion is tested explicitly and Task 3 requires a documented measurement before any number changes. |
| T-52.2-45 | Tampering | a third-party skill overriding project rules | medium | mitigate | The skill is guidance for library usage only; project reactivity rules (`efx-preact-reactivity`) and the conventions doc remain authoritative, and Task 3 records the skill's target version so a v3-oriented skill cannot silently shape v4 code. |
</threat_model>

<verification>
`pnpm install` clean; `pnpm --filter efx-motion-editor run build` clean within budget; `pnpm --filter efx-motion-editor exec vitest run src/viteBuild.test.ts` green; the re-verification and the measured budget decision recorded in `SPECS/async-conventions.md`; no library import reachable from the main chunk.
</verification>

<success_criteria>
The pilot starts on re-verified, exactly pinned libraries, with the chunk budget measured rather than assumed and the guidance skill in place — and nothing yet imported into production code.
</success_criteria>

## Artifacts this phase produces

- `app/package.json` / `pnpm-lock.yaml`: pinned `xstate` and `effect` at their re-verified versions
- `SPECS/async-conventions.md`: "Pilot re-verification" section (versions, breaking changes, binding status) and "Pilot dependencies" section (measured sizes + budget decision)
- `app/src/viteBuild.test.ts`: budget assertion updated only if the measurement requires it, with the precedent-style comment

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-13-SUMMARY.md when done
</output>
