---
phase: "52.2"
slug: "project-package-format-references-only"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: "2026-09-12"
---

# Phase 52.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from 52.2-RESEARCH.md §Validation Architecture (2026-09-12).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (JS/TS) + cargo test (Rust) |
| **Config file** | `app/vite.config.ts` (vitest picks it up; `viteBuild.test.ts` runs a real production build with a 300 s timeout) |
| **Quick run command** | `pnpm --filter efx-motion-editor exec vitest run <touched paths>` |
| **Full suite command** | `pnpm --filter efx-motion-editor exec vitest run` (project law: `vitest run`, NEVER watch) |
| **Rust suite command** | `cargo test --manifest-path app/src-tauri/Cargo.toml` |
| **Build gate** | `pnpm --filter efx-motion-editor run build` (`tsc --noEmit && vite build`) |
| **Estimated runtime** | not measured — quick path-scoped vitest runs are the per-task sample; full suite + build are wave-level |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter efx-motion-editor exec vitest run <touched paths>`
- **After every plan wave:** Run `pnpm --filter efx-motion-editor exec vitest run` + `cargo test --manifest-path app/src-tauri/Cargo.toml`
- **Before `/gsd-verify-work`:** Full suite green + `pnpm --filter efx-motion-editor run build`; then the bundled native UAT (spec Gates 4/5)
- **Max feedback latency:** 60 seconds (path-scoped vitest quick run)

---

## Per-Task Verification Map

Task IDs/plans/waves are assigned at plan time; rows below are the requirement-anchored checks the plans must instantiate.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| — | 52.2-08 | format W1+ | DOC-03 | T-52.2 path traversal / legacy carrier | pre-52.2 manifest refused, zero mutation | unit | `vitest run src/efx-paint/document/efxPaintCleanBreak.test.ts` (9 tests) | ✅ exists | ✅ green |
| — | 52.2-09 | format W1+ | DOC-05 | — | save → reopen round-trip preserves layer docs + media refs | unit (temp dir) | `vitest run src/lib/efxPaintPackageRoundTrip.test.ts` (11 tests) | ✅ exists | ✅ green |
| — | 52.2-09 | format W1+ | DOC-05 | T-52.2 path traversal (write side) | media bytes land as real files; JSON contains no base64 | integration (Rust temp dir) | `cargo test --manifest-path app/src-tauri/Cargo.toml` (`src-tauri/tests/efx_paint_package.rs`; 142 passed / 10 binaries, 2026-09-14 sweep) | ✅ exists | ✅ green |
| — | 52.2-09 | format W1+ | DOC-04 | — | no base64 payload survives a save (grep-level contract, both roto collections) | unit | round-trip test "emits no raster payload and no machine-local path" (512+ run scan, positive control) | ✅ exists | ✅ green |
| — | 52.2-07 | format W1+ | D-11 | — | second save writes only changed files (empty change set = no disk touch) | unit | round-trip test "writes nothing on an identical second save, and exactly one media file plus its layer sub-file when one key changes" | ✅ exists | ✅ green |
| — | 52.2-05/09 | format W1+ | D-05 | — | cache refs recomputed; no machine-local path in any package file | unit | round-trip tests "recomputes the machine-local cache location…" + the payload scan over every emitted JSON | ✅ exists | ✅ green |
| — | 52.2-09 | read-back leg | D-13 | T-52.2 corrupt digest / symlink escape | digest mismatch → refuse (no silent fallback; nothing cached) | unit | `vitest run src/lib/efxPaintMediaRead.test.ts` ("refuses a digest mismatch without decoding and without writing the LRU"; missing / native-refusal / io / decode-failure cases) | ✅ exists | ✅ green |
| — | 52.2-13 | format W1+ | D-19 | — | chunk budget holds with the new deps | build | `vitest run src/viteBuild.test.ts` (11 tests; main chunk 1,333.07 kB ≤ 1340 kB) | ✅ exists | ✅ green |
| — | 52.2-04 | UI W | D-20..D-22 | — | solo start-at-content-start + loop wrap | unit (pure index math) | `vitest run src/components/physic-paint/roto/physicsPaintRotoSoloWindow.test.ts` + `src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts` (42 + 32 tests) | ✅ exists | ✅ green |
| — | 52.2-03 | UI W | D-23/D-24 | — | stepper steps, clamps, hold-to-repeat, keyboard edit | unit (component) | `vitest run src/components/shared/NumericStepper.test.tsx.test.ts` (18 tests incl. the sweep contract) | ✅ exists | ✅ green |
| — | all | all | ACC-01 | — | typecheck + build + all gates | gate | `pnpm --filter efx-motion-editor run build` + full suite (210 files / 3936 passed / 0 failed, 2026-09-18) | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `app/src/lib/efxPaintPackageRoundTrip.test.ts` (+ `efxPaintPackage.test.ts`, `efxPaintMediaRead.test.ts`) — round-trip, change-set, cache-recompute, digest-mismatch; the refusal leg landed as `app/src/efx-paint/document/efxPaintCleanBreak.test.ts`
- [x] Rust integration tests in `app/src-tauri/tests/` — `efx_paint_package.rs` + `physic_paint_cache_publication.rs` (media write + digests + transaction rollback)
- [x] `app/src/testUtils/packageFixture.ts` — the temp-dir package fixture helper (the `makeTempDir` idiom, reused)
- [x] Solo playback tests — `physicsPaintRotoSoloWindow.test.ts` (42) + `useRotoCachedPlayback.test.ts` (32), pure resolvers, no hook-harness invention
- [x] Stepper component harness — `NumericStepper.test.tsx.test.ts` (the repo's component-test idiom, copied)
- [x] Framework install: none — vitest + cargo test already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Finder shows ONE `.mce` icon; double-click opens the project; open/save dialogs target packages | D-03 | macOS LaunchServices + Info.plist behaviour — no automated harness; requires a bundled build (`tauri dev` carries no Info.plist) | Bundled UAT: build the app, double-click a `.mce` package, confirm single-icon + open; screenshot evidence. Watch for stale LaunchServices registration (bump `CFBundleVersion` or `lsregister -f` if needed). |
| rfd package selection (open/save dialog accepts a package as one item) | D-03 / A6 | rfd uses deprecated `setAllowedFileTypes`; `treatsFilePackagesAsDirectories` default unverified at runtime | Bundled UAT: open + save-as through the dialogs on macOS; fallback is a directory-style dialog (record which occurred). |
| Zero base64 in any JSON + media files visible in the folder; before/after persist timings | Spec Gate 4 | Human-visible contract ("the user must open the folder and SEE the .webp files"); timings measured on the real project | Native UAT: open/paint/save/reopen a 2-layer project — manifest small, `frames/` files visible in Finder, grep shows zero base64, icon clicks instant, before/after timings reported (telemetry in place). |
| Pilot feel: stroke-train painting before/after + render-churn inventory | D-16, D-18 (spec Gate 4) | Live input feel is not automatable | Pilot UAT: stroke-train painting on the real project; report before/after feel + telemetry (baseline ~900 ms atomic steps, ~9 s median queue waits) + render counters. |
| Solo play starts at first frame; loop wrap returns there; steppers show only −/+ with easy click targets | D-20..D-24 (spec Gate 5) | Interactive UI behaviour | UI/UX UAT: arm each solo (pill + row S), press Play, confirm start at content start and identical loop wraps; exercise steppers everywhere (click, hold-to-repeat, keyboard edit, clamp). |

**Recorded 2026-09-18 (bundled session, bundle `facf1351`, subject `~/Desktop/efx-motion-editor-project-test/v1.0.0.mce`):** all five rows PASS with every sub-check, plus the converted-fixture open PASS — full evidence in `52.2-16-SUMMARY.md` (coverage D2) and `52.2-VERIFICATION.md` (truth 12).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [ ] Feedback latency < 60s — not measured; path-scoped vitest runs were the per-task sampler
- [x] `nyquist_compliant: true` set in frontmatter

---

## Validation Audit 2026-09-18

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Audit basis: `52.2-VERIFICATION.md` (12/12 must-haves verified, 0 behavior-unverified; targeted suites re-run green 2026-09-18) + the 52.2-01..16 summaries. Every map row resolves to an existing, green test; no requirement lacks automated verification. All Manual-Only rows carry recorded outcomes (bundled session 2026-09-18).

**Approval:** pending
