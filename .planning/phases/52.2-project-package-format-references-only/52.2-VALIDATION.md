---
phase: "52.2"
slug: "project-package-format-references-only"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-12"
---

# Phase 52.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from 52.2-RESEARCH.md §Validation Architecture (2026-09-12).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (JS/TS) + cargo test (Rust) |
| **Config file** | `app/vite.config.ts` (vitest picks it up; `viteBuild.test.ts` runs a real production build with a 300 s timeout) |
| **Quick run command** | `pnpm --filter efx-motion-editor exec vitest run <touched paths>` |
| **Full suite command** | `pnpm --filter efx-motion-editor exec vitest run` (project law: `vitest run`, NEVER watch) |
| **Rust suite command** | `cargo test --manifest-path app/src-tauri/Cargo.toml` |
| **Build gate** | `pnpm --filter efx-motion-editor run build` (`tsc --noEmit && vite build`) |
| **Estimated runtime** | not measured — quick path-scoped vitest runs are the per-task sample; full suite + build are wave-level |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter efx-motion-editor exec vitest run <touched paths>`
- **After every plan wave:** Run `pnpm --filter efx-motion-editor exec vitest run` + `cargo test --manifest-path app/src-tauri/Cargo.toml`
- **Before `/gsd-verify-work`:** Full suite green + `pnpm --filter efx-motion-editor run build`; then the bundled native UAT (spec Gates 4/5)
- **Max feedback latency:** 60 seconds (path-scoped vitest quick run)

---

## Per-Task Verification Map

Task IDs/plans/waves are assigned at plan time; rows below are the requirement-anchored checks the plans must instantiate.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| — | — | format W1+ | DOC-03 | T-52.2 path traversal / legacy carrier | pre-52.2 manifest refused, zero mutation | unit | extend the existing refusal-gate test next to `projectStore` (rename/extend the `findLegacyPhysicPaintRejection` test) | ❌ W0 | ⬜ pending |
| — | — | format W1+ | DOC-05 | — | save → reopen round-trip preserves layer docs + media refs | unit (temp dir) | new `app/src/lib/efxPaintPackage.test.ts` | ❌ W0 | ⬜ pending |
| — | — | format W1+ | DOC-05 | T-52.2 path traversal (write side) | media bytes land as real files; JSON contains no base64 | integration (Rust temp dir) | `cargo test --manifest-path app/src-tauri/Cargo.toml` (new test in `src-tauri/tests/`) | ❌ W0 | ⬜ pending |
| — | — | format W1+ | DOC-04 | — | no base64 payload survives a save (grep-level contract) | unit | assertion inside the round-trip test: serialized `layers/*.json` matches no `/^[A-Za-z0-9+/]{512,}={0,2}$/` payload | ❌ W0 | ⬜ pending |
| — | — | format W1+ | D-11 | — | second save writes only changed files | unit (staging file mtimes / change-set assertion) | part of the round-trip test | ❌ W0 | ⬜ pending |
| — | — | format W1+ | D-05 | — | cache refs recomputed; no machine-local path in any package file | unit | round-trip test assertion over every emitted JSON | ❌ W0 | ⬜ pending |
| — | — | read-back leg | D-13 | T-52.2 corrupt digest / symlink escape | digest mismatch → refuse (no silent fallback) | unit | round-trip test with a corrupted media file | ❌ W0 | ⬜ pending |
| — | — | format W1+ | D-19 | — | chunk budget holds with the new deps | build | `pnpm --filter efx-motion-editor exec vitest run src/viteBuild.test.ts` | ✅ exists | ⬜ pending |
| — | — | UI W | D-20..D-22 | — | solo start-at-content-start + loop wrap | unit (pure index math over `useRotoCachedPlayback` helpers) | new test beside the hook's existing tests | ❌ W0 | ⬜ pending |
| — | — | UI W | D-23/D-24 | — | stepper steps, clamps, hold-to-repeat | unit (component) | new test beside `NumericInput` | ❌ W0 | ⬜ pending |
| — | — | all | ACC-01 | — | typecheck + build + all gates | gate | `pnpm --filter efx-motion-editor run build` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/src/lib/efxPaintPackage.test.ts` — round-trip, change-set, refusal, cache-recompute, digest-mismatch
- [ ] Rust integration test in `app/src-tauri/tests/` — media write + digests + transaction-set rollback (extend the existing test directory, do not create a new harness)
- [ ] A test helper that builds a minimal package fixture on a temp dir (reuse the existing temp-dir idiom from `viteBuild.test.ts`'s `makeTempDir`)
- [ ] Solo playback test — verify whether `useRotoCachedPlayback` already has coverage; if not, extract the start-index computation to a pure function and test that (no hook harness invention)
- [ ] Stepper component test harness — confirm how the repo tests Preact components today before writing one (find an existing component test and copy its setup)
- [ ] Framework install: none — vitest + cargo test already configured

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Finder shows ONE `.mce` icon; double-click opens the project; open/save dialogs target packages | D-03 | macOS LaunchServices + Info.plist behaviour — no automated harness; requires a bundled build (`tauri dev` carries no Info.plist) | Bundled UAT: build the app, double-click a `.mce` package, confirm single-icon + open; screenshot evidence. Watch for stale LaunchServices registration (bump `CFBundleVersion` or `lsregister -f` if needed). |
| rfd package selection (open/save dialog accepts a package as one item) | D-03 / A6 | rfd uses deprecated `setAllowedFileTypes`; `treatsFilePackagesAsDirectories` default unverified at runtime | Bundled UAT: open + save-as through the dialogs on macOS; fallback is a directory-style dialog (record which occurred). |
| Zero base64 in any JSON + media files visible in the folder; before/after persist timings | Spec Gate 4 | Human-visible contract ("the user must open the folder and SEE the .webp files"); timings measured on the real project | Native UAT: open/paint/save/reopen a 2-layer project — manifest small, `frames/` files visible in Finder, grep shows zero base64, icon clicks instant, before/after timings reported (telemetry in place). |
| Pilot feel: stroke-train painting before/after + render-churn inventory | D-16, D-18 (spec Gate 4) | Live input feel is not automatable | Pilot UAT: stroke-train painting on the real project; report before/after feel + telemetry (baseline ~900 ms atomic steps, ~9 s median queue waits) + render counters. |
| Solo play starts at first frame; loop wrap returns there; steppers show only −/+ with easy click targets | D-20..D-24 (spec Gate 5) | Interactive UI behaviour | UI/UX UAT: arm each solo (pill + row S), press Play, confirm start at content start and identical loop wraps; exercise steppers everywhere (click, hold-to-repeat, keyboard edit, clamp). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
