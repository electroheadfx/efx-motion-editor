---
phase: 52.2-project-package-format-references-only
plan: 15
type: execute
wave: 8
depends_on: ["52.2-14"]
files_modified:
  - app/src/components/physic-paint/pilot/flushPipeline.ts
  - app/src/components/physic-paint/pilot/flushPipeline.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
autonomous: true
requirements: [D-16, D-17, D-19]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low  # calibration: factor 1.0, sample_count 0 (<3) — figures are uncalibrated projections, not measurements

must_haves:
  truths:
    - "A flush request that arrives mid-gesture drains the gesture's queued work instead of racing it, and a concurrent second request joins the in-flight flush rather than starting a second drain"
    - "The flush order is preserved: engine settle, then queued captures, then the documentSync push"
    - "A flush that fails still reports a result to the caller — the facade's fail-closed contract is unchanged"
    - "An interrupted flush (navigation during the drain) settles rather than hanging"
    - "`app/src/lib/physicPaintFlush.ts` is unchanged and contains no xstate or effect import, and nothing under `app/src/lib` or `app/src/stores` imports the pilot directory (D-17, D-19)"
    - "The main chunk is unchanged by the pilot: the production build stays inside the frozen 1320 kB budget (D-19)"
  artifacts:
    - path: "app/src/components/physic-paint/pilot/flushPipeline.ts"
      provides: "the Studio-side flush orchestration built on plan 14's machine + queue (join, wait, interrupt)"
    - path: "app/src/components/physic-paint/pilot/flushPipeline.test.ts"
      provides: "the join/wait/interrupt contract + the boundary source scan"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "the flush install site and the close-flush sequence routed through one pipeline"
    - path: "app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts"
      provides: "the retry unit scheduled through the pilot queue"
  key_links:
    - "`app/src/lib/physicPaintFlush.ts` is imported by the MAIN chunk (`projectStore.ts:31`, `exportEngine.ts:3`) while also holding the Studio-side listener — the pipeline must live under `physic-paint/pilot/` and the facade must stay byte-identical, or the frozen 1320 kB budget breaks for the main chunk (D-19)"
    - "the Studio has TWO flush paths (the `installPhysicPaintFlushRequestListener` closure and the close-flush block) — both must route through the pipeline, otherwise they diverge and only one of them gets the join semantics"
    - "plan 10 already narrowed the retry unit to the digest-keyed byte entry; this plan changes only the SCHEDULER around it, never what is sent"
    - "the facade's 5 s timeout stays the outer bound — the pipeline must settle before it or the caller sees a fail-closed result it did not earn"
---

<objective>
Ship the D-16 pilot's second vertical slice (sensitivity-map rows 2 and 4): the flush orchestration built on plan 14's machine and queue — one serialized drain that joins concurrent requests, waits out an active gesture, and interrupts cleanly on navigation — wired into both Studio flush paths and the retry scheduler, still behind the pilot module boundary (D-17) with no library import reachable from the main chunk (D-19).

Purpose: the hand-rolled 5 s flush timeout and the whole-payload retry path are the last two sensitivity-map rows plan 14 did not touch. Both are UI-side wiring: the primitives already exist, what is missing is one caller that owns ordering, joining and interruption instead of three call sites that each re-derive it.

Output: `pilot/flushPipeline.ts` with its tests, the Studio flush paths routed through it, and the retry path scheduled through the pilot queue.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/async-conventions.md
@.planning/phases/52.2-project-package-format-references-only/52.2-RESEARCH.md
@.claude/skills/efx-preact-reactivity/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Flush pipeline — one serialized drain (join, wait, interrupt)</name>
  <files>app/src/components/physic-paint/pilot/flushPipeline.ts, app/src/components/physic-paint/pilot/flushPipeline.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintFlush.ts (the whole file: `requestPhysicPaintFlush` on the main side and `installPhysicPaintFlushRequestListener` on the Studio side — the facade whose behavior must not change, including its timeout and its fail-closed result)
    - app/src/components/physic-paint/pilot/finalizationMachine.ts and app/src/components/physic-paint/pilot/finalizationQueue.ts (plan 14 outputs — the lifecycle snapshot the wait reads and the queue the drain drives)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the flush install site, and the close-flush block that calls `engineRef.current?.flushPendingStrokeFinalizations()`, `rotoPersistence.flushLivePixels(currentFrame)`, `rotoPlaybackSettingsController.flush()` and `flushDocumentSyncRef.current()` in sequence — that order is the contract)
    - SPECS/async-conventions.md (§Pilot: the named architectural problem for sensitivity-map rows 2 and 4, and the signals-bridge rules)
  </read_first>
  <behavior>
    - a flush requested while the machine is active waits for the gesture's queued captures and then drains them, instead of racing them
    - a flush requested while a flush is in flight joins the in-flight one: one drain, one result, no interleaved double push
    - the drain order is preserved: engine settle, then queued captures, then the documentSync push
    - a flush whose underlying step fails still resolves with a result the caller can read (never a rejection the facade would turn into a hang)
    - an interrupted flush (navigation during the drain) settles rather than hanging, and the interrupt is observable in the returned result
    - the module imports the pilot primitives only — it must not import `app/src/lib/physicPaintFlush.ts`, which would make the boundary check vacuous
    - `app/src/lib/physicPaintFlush.ts` contains no xstate or effect import, and nothing under `app/src/lib` or `app/src/stores` imports the pilot directory
  </behavior>
  <action>
    Create `app/src/components/physic-paint/pilot/flushPipeline.ts` (D-16, sensitivity-map rows 2 and 4) exporting the flush orchestration built on plan 14's machine and queue: one serialized drain that (a) waits until a gesture's queued captures are submitted and settled, (b) joins any concurrent request onto the same in-flight promise so two callers get one drain and one result, (c) preserves the existing step order, and (d) resolves — never rejects — on interruption, reporting the interruption in the result so the facade's fail-closed contract still has something to read.

    The steps the drain runs are supplied by the caller (the Studio passes its four existing calls in their current order), so this module owns scheduling and nothing else: no engine import, no store import, no `physicPaintFlush` import. That is what makes it TDD-able and what keeps the boundary check meaningful.

    Create `pilot/flushPipeline.test.ts` with the `<behavior>` cases including the source-scan boundary case: read `app/src/lib/physicPaintFlush.ts` and assert it contains no xstate/effect import, then walk `app/src/lib` and `app/src/stores` for any import of the pilot directory. Model the file-reading approach on `efxPaintCleanBreakContract.test.ts`. Write the cases first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/flushPipeline.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>! grep -rEq "physic-paint/pilot" app/src/lib app/src/stores</automated>
    <fails_when>exit 1 — a pilot import escaped the Studio boundary into the main chunk (D-17, D-19)</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/flushPipeline.test.ts` exits 0 with at least 6 passing cases
    - the concurrent case asserts exactly ONE push across two overlapping requests (count the calls, do not assert a boolean)
    - the mid-gesture case asserts the drain did not start before the queued captures settled
    - the interruption case asserts a resolved result carrying the interruption (not a rejection, not a timeout)
    - the boundary case asserts the scanned path list is non-empty (a scan that reads nothing cannot fail)
  </acceptance_criteria>
  <done>The flush pipeline joins, waits and interrupts behind the Studio boundary, proven without touching the facade or the main chunk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Studio wiring — both flush paths and the retry scheduler</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the `installPhysicPaintFlushRequestListener` install site and the close-flush block — both call sites of the sequence this task unifies)
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts:274-563 (the per-identity queue and the retry path plan 10 narrowed to a digest-keyed byte entry)
    - app/src/components/physic-paint/pilot/flushPipeline.ts (Task 1 output)
    - .claude/skills/efx-preact-reactivity/SKILL.md (the reactivity rules the wiring must respect: no signal writes in the render body, identity-stable effect dependencies)
  </read_first>
  <behavior>
    - the `installPhysicPaintFlushRequestListener` closure routes through the pipeline instead of running the sequence inline
    - the close-flush block routes through the same pipeline instance, so a close during an in-flight flush joins it rather than starting a second sequence
    - the four steps keep their existing order and their existing inputs
    - a retry in the persistence coordinator is scheduled through the pilot queue and no longer re-sends a whole payload
    - the flush listener is torn down on unmount exactly as before (no leaked subscription from the rewiring)
  </behavior>
  <action>
    Replace the inline flush closure at the `installPhysicPaintFlushRequestListener` call site with the pipeline from Task 1, and route the close-flush sequence through the SAME pipeline instance so the two paths cannot diverge (a close during a flush must join it, not queue a second drain). Keep the four steps and their order exactly as they are — this task changes who schedules them, not what they do.

    In `useRotoFramePersistenceCoordinator.ts`, schedule the retry through the pilot queue instead of the ad-hoc path, leaving the retry unit plan 10 narrowed intact: a retry must still send only the narrowed unit, never a whole payload. Do not restructure the coordinator's identity handling — the queue owns concurrency, the coordinator keeps owning identity.

    Respect the project's reactivity rules throughout (the `efx-preact-reactivity` skill is binding): the pipeline is created once and read narrowly, and no signal is written from the render body. Extend the coordinator's suite with the retry `<behavior>` case and assert the wiring by contract test where a component-level test is impractical. Write the cases first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/flushPipeline.test.ts src/lib/physicPaintFlush.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor run build</automated>
    <fails_when>non-zero exit, or any "error TS" line, or a chunk-size warning above the frozen 1320 kB budget</fails_when>
  </verify>
  <acceptance_criteria>
    - both named suites exit 0 and the full suite `pnpm --filter efx-motion-editor exec vitest run` exits 0 (record the count in the summary)
    - `pnpm --filter efx-motion-editor run build` exits 0 with no chunk-size warning — the main chunk is unchanged by the pilot (D-19)
    - `physicPaintFlush.test.ts` passes unmodified, and `app/src/lib/physicPaintFlush.ts` is untouched in the diff
    - the close-during-flush case asserts one sequence, not two
    - the retry case asserts the narrowed unit is what gets sent
  </acceptance_criteria>
  <done>Both Studio flush paths and the retry path run through the pilot's scheduler, with the facade contract and the main-chunk budget provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio (pilot libraries) → main chunk | Anything reachable from `physicPaintFlush.ts` is loaded by the main window |
| Flush caller → drain | A caller that never gets a result hangs Save/Export |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-48 | Denial of Service | a drain that never settles, hanging Save/Export | medium | mitigate | The pipeline resolves on interruption and on failure (never rejects into the facade's hands), the flush joins an in-flight drain rather than stacking one, and the facade's existing 5 s timeout remains the outer bound and is unchanged. |
| T-52.2-54 | Repudiation | two overlapping flushes pushing the document twice | high | mitigate | Joining is a single in-flight promise; a test counts the pushes across two overlapping requests and asserts exactly one, and the close path shares the same instance so it cannot bypass the join. |
| T-52.2-55 | Elevation of Privilege | library code reaching the main chunk through the flush facade | medium | mitigate | The pipeline lives under `physic-paint/pilot/`, imports no store and no lib module, and a negated source-scan gate plus the production build (frozen 1320 kB budget) both have to pass before the plan closes (D-19). |
| T-52.2-SC | Tampering | npm installs | high | accept | No new package is installed in this plan; the two packages were pinned to registry-verified versions under plan 13. |
</threat_model>

<verification>
`pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/flushPipeline.test.ts src/lib/physicPaintFlush.test.ts` green; full suite green; `pnpm --filter efx-motion-editor run build` clean within the frozen budget; `app/src/lib/physicPaintFlush.ts` unmodified; no `app/src/lib` or `app/src/stores` module imports the pilot.
</verification>

<success_criteria>
The pilot's flush path is one serialized, joinable, interruptible drain used by both Studio call sites, with the retry scheduled through the pilot queue — proven by tests, by an untouched facade, and by a main chunk that did not grow.
</success_criteria>

## Artifacts this phase produces

- `app/src/components/physic-paint/pilot/flushPipeline.ts` (new): the join/wait/interrupt flush orchestration
- `app/src/components/physic-paint/pilot/flushPipeline.test.ts` (new): contract + boundary source scan
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx`: both flush paths routed through one pipeline instance
- `app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts`: retry scheduled through the pilot queue
- `app/src/lib/physicPaintFlush.ts`: unchanged by design — the facade stays library-free (source-scan guarded)

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-15-SUMMARY.md when done
</output>
