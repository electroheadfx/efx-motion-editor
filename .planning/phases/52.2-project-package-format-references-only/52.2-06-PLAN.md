---
phase: 52.2-project-package-format-references-only
plan: 06
type: execute
wave: 2
depends_on: ["52.2-02"]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.test.ts
  - app/src/stores/efxPaintStore.ts
  - app/src/stores/efxPaintStore.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts
autonomous: true
requirements: [D-06, D-07, D-09]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low  # calibration: factor 1.0, sample_count 0 (<3) — figures are uncalibrated projections, not measurements

must_haves:
  truths:
    - "Serializing runtime state produces real-key records that carry a media reference and no raster payload"
    - "Hydrating runtime state from a document produces records whose pixels come from the media reference, never from a payload in the document"
    - "A key that has runtime bytes but no media reference cannot be serialized (fail closed, never a silent payload fallback)"
    - "BOTH persisted roto collections are projected unconditionally — `realKeyRecords` and `groupOverrideRecords` are the same `PhysicPaintRotoRealKeyRecord` type and go through the same two functions with no per-collection branch, no pass-through case and no conditional hedge (D-06)"
    - "A key's metadata (timing, track, keyId) survives a serialize → hydrate round trip unchanged"
    - "Two different raster contents for the same key produce two different media digests"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts"
      provides: "toPersistedRotoRecords / toRuntimeRotoRecords pure projections"
    - path: "app/src/stores/efxPaintStore.ts"
      provides: "serializeRuntimeIntoDocument emitting media references"
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "installRuntimeStateFromDocument accepting media-reference records"
  key_links:
    - "`physicPaintStore.ts` reads runtime bytes off the record payload when publishing fresh keys (the paste/duplicate path around :4153-4162) — that consumer must survive the payload field becoming optional"
    - "the projection must never invent a digest: an unresolved media reference is a hard failure, because a wrong digest would be refused on reopen and silently lose the user's key"
    - "D-06 Law-1 completeness: `FrameLoopClip.sourceFrameRefs` and `PhotoReferenceTrack.sourceFrameRefs` are `readonly string[]` asset ids with no payload field, so they pass through this projection untouched; plan 09 Task 2 carries the save/reopen round-trip case that proves a layer with a loop clip and a non-null photoReference keeps its ids and that each id resolves under the package's `images/` index"
    - "the two projections are typed over `PhysicPaintRotoRealKeyRecord` — the single record type shared by `realKeyRecords` and `groupOverrideRecords` (`physicsPaintRotoPhysicalModel.ts:316`) — so the call sites, not the projection module, decide coverage: `_buildRotoPhysicalDocumentForLayer` (`physicPaintStore.ts:1974-2009`, reads both maps at :1975-1979) and `installRuntimeStateFromDocument` (:2519-2542, installs both at :2531 and :2532-2534) must pass BOTH collections; a call site that passes one collection is the bug this link exists to prevent"
---

<objective>
Make the persisted document carry media references instead of rasters in both directions (D-06/D-09): serializing runtime state emits `media` and strips the payload; hydrating runtime state reads `media` and never looks for a payload. This applies to BOTH persisted roto record collections — `realKeyRecords` and `groupOverrideRecords`, which share the `PhysicPaintRotoRealKeyRecord` type — at the two store seams that own them. The bytes themselves move through the native commands from plan 01 — this plan owns only the mapping.

Purpose: the write funnel (plan 07) and the read-back leg (plan 09) both sit on top of this projection. Isolating it here, away from IO, is what makes it TDD-able and keeps the stores thin.

Output: `physicsPaintRotoMediaProjection.ts` with tests, `serializeRuntimeIntoDocument` emitting media references for both collections (projection before the revision computation), `installRuntimeStateFromDocument` accepting them in both collections, and the multi-track round-trip test proving metadata survives.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/phase-52.2-project-package-format.md
@.planning/phases/52.2-project-package-format-references-only/52.2-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure media projection in both directions</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts, app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (`PhysicPaintRotoRealKeyPayload`, `PhysicPaintRotoRealKeyRecord`, `parsePhysicPaintRotoRealKeyRecordCollection` :802, and plan 02's `FrameMediaReference` field)
    - app/src/lib/efxPaintPackage.ts (`buildFrameMediaRelativePath`, `isSafePackageRelativePath` — plan 02)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (this folder's vitest idiom)
  </read_first>
  <behavior>
    - `toPersistedRotoRecords(records, resolveRef)` returns records that carry `media` and whose payload object has no raster field at all
    - a record with runtime bytes whose `resolveRef(keyId)` returns undefined produces a typed failure naming the keyId — never a record without media, never a fallback to the payload
    - `toRuntimeRotoRecords(records)` returns records carrying `media` and no bytes; it never allocates a byte buffer
    - metadata round trip: `toRuntimeRotoRecords(toPersistedRotoRecords(records, ref)).map(r => [r.keyId, r.appFrame, r.frameIndex])` equals the input
    - two records with the same keyId but different media digests remain distinguishable in the persisted output
    - group-override records project exactly like real keys: `groupOverrideRecords` carries the same `PhysicPaintRotoRealKeyRecord` shape (`physicsPaintRotoPhysicalModel.ts:316`) with `payload.bytes`, so running `toPersistedRotoRecords` over a group-override collection strips the payload and emits media references, with NO branch, NO pass-through case and NO special handling anywhere in the module
    - an unresolved `resolveRef` for a group override produces the same typed failure naming THAT group override's keyId — never a dropped collection, never a record without media
    - `toRuntimeRotoRecords` over a group-override collection returns records carrying `media` and no bytes, exactly as for real keys
    - no function in the module reads files, calls IPC, or touches a store
  </behavior>
  <action>
    Create `app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts` (new, pure) exporting `toPersistedRotoRecords`, `toRuntimeRotoRecords`, and a typed failure result for the unresolved-reference case. Both functions take `readonly PhysicPaintRotoRealKeyRecord[]` and are collection-agnostic by signature: they never learn which collection their input came from, so no `realKeyRecords`-only overload, optional collection flag or conditional path may exist — coverage is the call sites' obligation (Task 2 for serialize, Task 3 for hydrate), and both call sites pass BOTH collections. `resolveRef` is a callback (`(keyId: string) => FrameMediaReference | undefined`) so the caller decides where references come from — in plan 07 that caller is the save funnel holding the digests returned by the native write command.

    The module must be total for the metadata path and fail-closed for the media path: metadata is copied verbatim; media comes only from `resolveRef`. Never synthesize a digest, never keep the bytes "just in case" (a persisted record carrying bytes would be rejected by plan 02's parser on reopen — the failure would surface as a lost key).

    Write the test file first covering every `<behavior>` case, watch it fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoMediaProjection.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoMediaProjection.test.ts` exits 0 with at least 9 passing cases
    - the test asserts the persisted record's payload has no raster field (assert the exact key set, not a falsy check)
    - the unresolved-reference case asserts a typed failure naming the keyId, for a real key AND for a group override (two named cases)
    - a named case runs both functions over a group-override collection and asserts the payload is stripped / no bytes are allocated
    - the module has no import from a store, an IPC module or a component
  </acceptance_criteria>
  <done>Both projection directions exist as pure functions with a fail-closed media path and a metadata-preserving round trip, and a group-override collection behaves identically to a real-key collection with no branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize path emits media references</name>
  <files>app/src/stores/efxPaintStore.ts, app/src/stores/efxPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/efxPaintStore.ts:`serializeRuntimeIntoDocument` (:1657) and its surrounding mutation/undo helpers; its call to `physicPaintStore.extractRuntimeStateForDocument` is at :1654
    - app/src/stores/physicPaintStore.ts:1974-2009 (`_buildRotoPhysicalDocumentForLayer` — the seam that reads BOTH `_rotoRealKeyRecords` (:1975-1977) and `_rotoGroupOverrideRecords` (:1978-1979), computes the revision over both at :1999-2005 and passes both to `parsePhysicPaintRotoPhysicalDocument` at :1990-2008) and :2474-2477 (`extractRuntimeStateForDocument`)
    - app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts (Task 1 output)
    - app/src/stores/efxPaintStore.test.ts (the existing suite to extend)
  </read_first>
  <behavior>
    - the document produced by `serializeRuntimeIntoDocument` contains, for every real key, a media reference and no raster payload
    - the projection covers BOTH collections: the layer's roto physical document carries media references in `realKeyRecords` AND in `groupOverrideRecords`, and neither collection carries a raster payload (assert both by name, and assert the exact payload key set of a projected group override)
    - the projection runs BEFORE the roto revision/content-hash computation for that document — the persisted revision is a function of the media references, so no payload bytes are read when the layer document is built (D-06)
    - a runtime group override whose `resolveRef` is undefined fails the serialize with the typed failure naming that group override's keyId — the same fail-closed rule as a real key
    - a runtime key with no resolved reference fails the serialize explicitly (the caller sees a typed failure, not a document with a missing key)
    - keys with no raster at all (generated cells, empty rails) are unaffected
    - a layer carrying a loop clip and a non-null photo reference serializes with both `sourceFrameRefs` arrays unchanged to the element — the ids are copied verbatim and no payload is ever derived from them (D-06 Law-1: references, never bytes)
    - the produced document is accepted by the fail-closed parser from plan 02 (parse the output in the test — this is the contract that matters)
    - serializing twice without a runtime change produces equal documents (determinism)
  </behavior>
  <action>
    Change the serialize path to build roto records through `toPersistedRotoRecords` with a `resolveRef` supplied by the caller (add it as a parameter/input rather than reading a store — the funnel in plan 07 owns the digest map). Where the current code copies the runtime record wholesale, replace the copy with the projection — and project BOTH collections at the seam that builds the roto physical document: `_buildRotoPhysicalDocumentForLayer` (`physicPaintStore.ts:1974-2009`) reads `_rotoRealKeyRecords` (:1975-1977) and `_rotoGroupOverrideRecords` (:1978-1979) and must feed the projected arrays, not the runtime ones, into `parsePhysicPaintRotoPhysicalDocument` (:1990-2008) and into `buildPhysicPaintRotoPhysicalRevision` (:1999-2005). Projecting before the revision call is required by plan 02's reference-total content encoding and is what keeps payload bytes off the persist path; projecting only `realKeyRecords` would leave group overrides carrying their payload into the sub-file, and plan 02's parser then refuses the whole layer on reopen — a whole-layer refusal, not one lost key.

    Every other field of `serializeRuntimeIntoDocument` keeps its current behavior in THIS task (undo ledger, revisions, loop clips, break key ids) — with one named exception that is not yours to change here: the derived-frame `cachePath` produced at :1658 must become the machine-relative reference, and that conversion is plan 07 Task 2, which owns the producer/guard pair atomically. Leave :1658 emitting exactly what it emits today so this plan's suite stays green; do not convert it here (the guard in `efxPaintPersistence.ts` still accepts only the current shape until plan 07 lands).

    Extend `efxPaintStore.test.ts` with the `<behavior>` cases, including the parser round trip (parse the produced document with the plan 02 parser and assert it is accepted). Write the cases first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.test.ts` exits 0 with all pre-existing cases passing plus at least 8 new ones
    - the test parses the produced document with the plan-02 parser and asserts acceptance
    - a named case asserts a layer carrying a group override serializes with a media reference in `groupOverrideRecords` and no raster payload in either collection
    - a named case asserts the persisted revision is produced from the projected records (no `payload.bytes` read on that path)
    - the loop-clip / photo-reference case is present by name and asserts both `sourceFrameRefs` arrays element-for-element (D-06 Law-1)
    - `pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.reveal.test.ts src/stores/efxPaintStore.photoReference.test.ts` also exits 0
  </acceptance_criteria>
  <done>The serialize path can only produce documents with media references in BOTH roto collections, proven by parsing its own output, with the revision computed from the projected records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hydrate path accepts references and drops the payload dependency</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts, app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:`installRuntimeStateFromDocument` (:2487), its roto physical install block (:2519-2542 — real keys land at :2530-2531, group overrides at :2532-2534), the `_rotoRealKeyRecords` and `_rotoGroupOverrideRecords` state (:385), `_compositorDecode` (:1120-1158), the bytes-token collector that already walks both collections (:930-937), and the paste helper's payload read around (:4153-4162)
    - app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts (the existing multi-track round-trip coverage to extend)
    - app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts (Task 1 output)
  </read_first>
  <behavior>
    - `installRuntimeStateFromDocument` accepts records carrying media references and installs runtime records without any byte buffer — in BOTH collections: `realKeyRecords` (:2530-2531) and `groupOverrideRecords` (:2532-2534) each land in their own runtime map as reference-only records
    - a group-override document record missing `media` is refused with the same fail-closed rule as a real key, and the refusal names the offending keyId
    - after install, the store's recomputed revision paths (`_resolveRotoPhysicalStructural` :867-920 and `getRotoPhysicalContentRevision` :3476) return a value for a reference-only document without dereferencing a byte buffer — plan 02's reference-total encoding is what makes that possible, and this case proves it end to end
    - the paste/duplicate helper publishes fresh runtime frames for duplicated keys using whatever runtime source is available; with no inline bytes it must not write an empty frame silently
    - a multi-track serialize → parse → hydrate round trip preserves every record's keyId, appFrame and track attribution, for real keys AND group overrides
  </behavior>
  <action>
    Route the hydrate path through `toRuntimeRotoRecords` and remove any assumption that pixels are present in the document. Both collections go through it: the install block at :2519-2542 must install `toRuntimeRotoRecords(physical.realKeyRecords)` into `_getOrCreateTrackRecords` (:2530-2531) and `toRuntimeRotoRecords(physical.groupOverrideRecords ?? [])` into `_rotoGroupOverrideRecords` (:2532-2534) — the group-override map is not a secondary concern and must not be left installing raw parsed records while the real-key path is projected. Then handle the runtime consumers that read inline bytes: the paste/duplicate helper around :4153-4162 currently copies `record.payload.bytes` when publishing a fresh key's frame — with the payload field now optional, decide explicitly between (a) resolving the frame from the runtime record's own published media reference, or (b) refusing the publish with a typed reason. Do not leave a path that can write `width: 0, height: 0` bytes for a duplicated key; that would render as an invisible key (Phase 49 slate is for missing sources, not for a bug here).

    Extend `physicPaintStore.test.ts` and `efxPaintPersistenceMultiTrackRoundTrip.test.ts` with the `<behavior>` cases. Write the cases first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/physicPaintStore.test.ts src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter efx-motion-editor exec vitest run` exits 0 (full suite, no regressions elsewhere — record the count in the summary)
    - `pnpm --filter efx-motion-editor exec tsc --noEmit` exits 0
    - the round-trip test asserts record metadata equality across serialize → parse → hydrate for at least two tracks, including a layer that carries a group override
    - a named case asserts the group-override map is populated from a reference-only document and a named case asserts the recomputed revision path does not throw for a reference-only document
  </acceptance_criteria>
  <done>Runtime state hydrates from media references with no payload dependency in BOTH roto collections, the recomputed revision path is total over reference-only records, and the paste path cannot publish an empty frame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime store → persisted document | Runtime-only fields must not leak into the persisted artifact |
| Persisted document → runtime store | A document record is untrusted input at hydrate time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-18 | Tampering | projection synthesizing media metadata | medium | mitigate | `resolveRef` is the only media source; an unresolved reference is a typed failure naming the keyId, and the produced document is validated by the plan-02 fail-closed parser inside the test — a fabricated or missing digest cannot reach disk. |
| T-52.2-19 | Information Disclosure | runtime bytes leaking into the persisted document | high | mitigate | The persisted record's payload key set is asserted explicitly in tests (no raster field) for real keys AND group overrides, and the parser rejects any record carrying one — so a leak is a test failure at write time and a refusal at read time, in either collection. |
| T-52.2-20 | Denial of Service | duplicated key publishing an empty frame | medium | mitigate | The paste path must either resolve pixels from a real source or refuse with a typed reason; a `width: 0` publish is treated as a bug, asserted by test. |
</threat_model>

<verification>
`pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/roto/physicsPaintRotoMediaProjection.test.ts src/stores/efxPaintStore.test.ts src/stores/physicPaintStore.test.ts src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts` green; full suite green; `pnpm --filter efx-motion-editor exec tsc --noEmit` clean.
</verification>

<success_criteria>
Runtime state and the persisted document exchange keys through media references only — for real keys AND group overrides: serialization cannot emit a payload in either collection, the persisted revision is computed from the projected records, hydration never expects a payload, and metadata survives the round trip.
</success_criteria>

## Artifacts this phase produces

- `app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.ts` (new): `toPersistedRotoRecords`, `toRuntimeRotoRecords` (typed over `PhysicPaintRotoRealKeyRecord`, so one implementation serves both roto collections), unresolved-reference failure type
- `app/src/components/physic-paint/roto/physicsPaintRotoMediaProjection.test.ts` (new)
- `app/src/stores/efxPaintStore.ts`: serialize path builds media-reference records via a caller-supplied resolver, projecting `realKeyRecords` AND `groupOverrideRecords` before the document revision is computed
- `app/src/stores/physicPaintStore.ts`: hydrate path installs reference-only records in both collections; paste path cannot publish an empty frame

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-06-SUMMARY.md when done
</output>
