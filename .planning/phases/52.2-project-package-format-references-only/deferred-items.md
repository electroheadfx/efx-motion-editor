# Deferred Items — Phase 52.2

Out-of-scope discoveries found during execution (not caused by this phase's plans; do not fix here).

- **WINDOWS.md ledger counts disagree with its entries** — `gsd-tools windows append` refuses every write with `Ledger counts disagree with entries: frontmatter open/waived/fixed/total=24/1/42/67 but entries yield 29/1/42/72`. Pre-existing drift (5 unaccounted `open` entries) means plan 52.2-01's deviation entry could not be recorded in the cross-phase defect register; it is instead documented in `52.2-01-SUMMARY.md` (Deviations, item 1) and coverage item D6. The ledger frontmatter needs a recount-repair pass before any later plan can append. Found during: 52.2-01, post-execution ledger write.
- **WINDOWS.md ledger still refuses writes (52.2-02)** — the same `Ledger counts disagree with entries ... 24/1/42/67 but entries yield 29/1/42/72` refusal. Plan 52.2-02's deviation entry (the Rule 3 move of the persisted payload mode to the read door) and its transitional-red entries (`physicsPaintRotoLoopClips.test.ts` ×5, `physicsPaintRotoGroupParity.test.ts` ×2 — files no phase plan lists, 8 total with `efxPaintPersistence.test.ts` ×1) are therefore documented in `52.2-02-SUMMARY.md` (Deviations and Issues Encountered) and in STATE.md's blockers instead. Found during: 52.2-02, post-execution ledger write.
- **WINDOWS.md ledger refuses writes again (52.2-03)** — same `24/1/42/67 but entries yield 29/1/42/72` refusal, so plan 52.2-03's deviation entries (the `.test.tsx.test.ts` launcher, the extra stepper props, the ColorPickerModal channel override, the Hold Frames close-commit, the Studio source-scan assertion updates) live in `52.2-03-SUMMARY.md` (Deviations) only. Found during: 52.2-03, post-execution ledger write.
- **`.physics-paint-topbar-number` CSS rule is now dead (52.2-03)** — `physicsPaintStudio.css` still holds `.physics-paint-topbar-number { width: 46px; … }`, but the Studio exact-value field now renders `NumericStepper` (wrapper class `physics-paint-topbar-number-stepper`, inline styles carry the Studio palette). The CSS file was outside plan 52.2-03's file list, so the rule was left untouched — remove it in a later CSS cleanup pass. Found during: 52.2-03, Task 3.
- **9 pre-existing suite failures confirmed as not-this-plan's (52.2-03)** — measured identically at the plan base `15e680cc` (throwaway worktree) and after execution: `physicsPaintRotoLoopClips.test.ts` ×5, `physicsPaintRotoGroupParity.test.ts` ×2, `efxPaintPersistence.test.ts` ×1, `efxPaintCleanBreakContract.test.ts` ×1 (`base64ToBytes` in `app/src/lib/ipc.ts`, from plan 52.2-01). They gate `vitest run`'s exit code for every later plan until their owners clear them. Found during: 52.2-03, Task 3 full-suite gate.
- **WINDOWS.md ledger refuses writes again (52.2-05)** — same `24/1/42/67 but entries yield 29/1/42/72` refusal, so plan 52.2-05's three stub entries could not reach the cross-phase register and live in `52.2-05-SUMMARY.md` (Known Stubs) instead: the accepted-not-forwarded `physicPaintCacheTransactionId` in `app/src/lib/ipc.ts` (plan 07 Task 3 stops passing it, plan 09 deletes it), the producer-side `cache/efx-paint` literals in `app/src/lib/efxPaintPersistence.ts` (plan 07 Task 2 converts them to the machine-relative `efx-paint/...` shape), and the four package wrappers + `stageEfxPaintPackageSave` having no production caller until plan 07's funnel. Found during: 52.2-05, post-execution ledger write.
- **Cache producers still pass a project directory where the cache root is expected (52.2-05)** — `efxPaintPersistence.ts` calls `publish/settle/hardlinkPhysicPaintCacheGeneration(projectDir, …)` positionally while the wrapper's first argument is now the machine cache root (`cacheRoot`). Deliberately not converted: the plan's `<action>` reserves it for plan 07 Task 2, and the existing cache tests pin the positional shape. Until plan 07 lands, a live save reaches the cache commands with a package directory as their root — the leg is best-effort (D-14), so this degrades the cache and never the authoritative save. Found during: 52.2-05, Task 3.
- **WINDOWS.md ledger refuses writes again (52.2-06)** — same `24/1/42/67 but entries yield 29/1/42/72` refusal, so plan 52.2-06's entries could not reach the cross-phase register and live in `52.2-06-SUMMARY.md` instead: the paste/duplicate typed refusal (`unresolved-key-pixels`, an intentional capability gap until plan 09 resolves pixels from the package) and the three deviations (the `buildFreshKeyRecord` carrier preservation, the pre-parse carrier guard, the typed paste reason). Found during: 52.2-06, post-execution ledger write.
- **Paste/duplicate against a reference-only document refuses (52.2-06)** — a freshly reopened (reference-only) roto key carries `media` with no inline pixels, so `_applyRotoTrackPaste` returns `{ ok: false, reason: 'unresolved-key-pixels' }` instead of publishing a frame. This is the plan's deliberate option (b); plan 02.2-09 (read-back leg) lifts it by resolving pixels from `payload.media`. Confined to the paste/duplicate flow and never a throw or an invisible key. Found during: 52.2-06, Task 3.
- **WINDOWS.md ledger refuses writes again (52.2-07)** — same `24/1/42/67 but entries yield 29/1/42/72` refusal, so plan 52.2-07's entries could not reach the cross-phase register and live in `52.2-07-SUMMARY.md` (Known Stubs, Deviations) instead: the Save As two-transaction split, the `layers/*.json` read-back gap, the orphan-file gap, and the two deviations (the collateral suite retargets, the duplicate-keyId acceptance case). No `--kind deviation` row was written. Found during: 52.2-07, post-execution ledger write.
- **Save As commits two transactions instead of one (52.2-07)** — `saveProjectAs` runs the package funnel into the destination folder, then hands the committed manifest to `projectSaveAsWithScriptLibrary`, which re-publishes it under its own transaction for the script-library migration. The manifest is byte-identical, so this is a correctness-safe redundancy, not a bug — but it is two publishes and two settles for one user action. Folding them into one transaction means forwarding the package staging basename and the staged path set through the script-library wrapper; plan 09 Task 1 owns `app/src/lib/ipc.ts` and is the natural owner. Found during: 52.2-07, Task 3.
- **The package read-back leg is not wired (52.2-07)** — the save writes layer content to `layers/<layerId>.json` and media to `frames/<layerId>/<keyId>.webp`, while `openProject` still passes `result.data.efx_paint_documents` to `loadEfxPaintDocuments`. A project saved by v1.0 therefore cannot be reopened yet with its paint layers. Deliberately not closed here (the plan's scope is the save side); plan 09 Task 2 is the read-back leg. Found during: 52.2-07, Task 3.
- **A removed key or layer leaves an orphan file in the package (52.2-07)** — `computeChangedFiles` iterates the NEXT token map only, so a token present only in the committed baseline (a deleted roto key, a deleted layer) never enters the write set and no removal path exists: `frames/<layerId>/<keyId>.webp` and `layers/<layerId>.json` stay canonical and unreferenced. No 52.2 plan lists this, so it also has no scheduled owner — it needs a plan or an explicit decision (a garbage-collect pass versus token-diff removals). Found during: 52.2-07, Task 3 acceptance review.
- **Build budget overage owned by plan 52.2-13 Task 3 (52.2-07)** — the production main chunk measures 1,322.93 kB (decimal) against `app/src/viteBuild.test.ts`'s pinned 1320 kB, so `vitest run` carries one red file that no plan before 13 can clear. Plan 52.2-13 Task 3 ("Budget re-measurement") owns the re-measure and the explicit documented raise. Kept red on purpose: editing the number in anticipation of its owner would be exactly the undocumented raise that task forbids. Found during: 52.2-07, full-suite gate.
- **The `efxPaintCleanBreakContract.test.ts` frame-transport red is plan 08's (52.2-07)** — `base64ToBytes` still appears in `app/src/lib/ipc.ts`, so the DOC-04 deletion-checklist contract test fails by design until plan 08 Task 2 deletes the legacy carrier. Measured identically before and after this plan's work. Found during: 52.2-07, full-suite gate. **RESOLVED in 52.2-08** — the file is green (3 tests) after the carrier deletion; see the entry below for the allowlist decision that closed it.
- **WINDOWS.md ledger refuses writes again (52.2-08)** — same `24/1/42/67 but entries yield 29/1/42/72` refusal, so plan 52.2-08's entries could not reach the cross-phase register and live in `52.2-08-SUMMARY.md` (Deviations, Known Stubs) instead. No `--kind deviation` row was written. Found during: 52.2-08, post-execution ledger write.
- **The pre-52.2 `efx_paint_documents` read is still reached (52.2-08)** — `openProject` still passes `result.data.efx_paint_documents` to `loadEfxPaintDocuments`, and both `MceProject` (TS) and `MceProject` (Rust) can still parse the retired key. Deliberate: the refusal gate now rejects every manifest without `formatVersion` before the read, so pre-52.2 paint documents can never reach the hydration path — but a package manifest cannot carry layer content inline, so the read-back leg must be replaced (not merely guarded). Plan 09 Task 2 (read-back leg) owns the deletion of `efx_paint_documents` from both models and of that read. Found during: 52.2-08, Task 2.
- **`efxPaintRejectionDialog.ts:5` names the deleted helper (52.2-08)** — the module's header doc-comment still says it serves `findLegacyPhysicPaintRejection`, a function plan 08 deleted in favour of `findPackageFormatRejection`. `app/src/lib/efxPaintRejectionDialog.ts` is not in the plan's `files_modified`, so the comment was left untouched. Stale prose only — no code path references the old name. Fix in a later doc-comment sweep. Found during: 52.2-08, Task 2.
- **`app/src/lib/ipc.ts` joined the frame-transport allowlist (52.2-08)** — `base64ToBytes` in `app/src/lib/ipc.ts` could not be deleted: `ipcEfxPaintReadFrameMedia` consumes it to decode the base64 body the native per-key read command returns, and the read-back leg plan 09 builds depends on that leg. The contract test therefore gained a seventh `FRAME_TRANSPORT_ALLOWLIST` entry with a rationale bullet, mirroring the existing `webpFrameCodec.ts` decode-leg exemption. The alternative (deleting the decode leg) would have removed the only package media read path. Found during: 52.2-08, Task 2.
