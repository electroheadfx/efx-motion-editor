---
phase: 52.2-project-package-format-references-only
plan: 14
type: execute
wave: 7
depends_on: ["52.2-13"]
files_modified:
  - app/src/components/physic-paint/pilot/finalizationMachine.ts
  - app/src/components/physic-paint/pilot/finalizationMachine.test.ts
  - app/src/components/physic-paint/pilot/finalizationQueue.ts
  - app/src/components/physic-paint/pilot/finalizationQueue.test.ts
  - app/src/components/physic-paint/bridge/gestureIdleScheduler.ts
  - app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.ts
  - app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.test.ts
autonomous: true
requirements: [D-16, D-17, D-19]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low  # calibration: factor 1.0, sample_count 0 (<3) — figures are uncalibrated projections, not measurements

must_haves:
  truths:
    - "The stroke-finalization lifecycle is a declared machine with a single readable snapshot, replacing the boolean cluster the callers each re-derive"
    - "A new gesture is never swallowed by a running drain: pointerdown during queued/draining returns the lifecycle to active"
    - "Capture produce/commit work runs with bounded concurrency and backpressure instead of an unbounded promise-per-key map"
    - "Navigation interrupts in-flight finalization work: an interrupted job never commits its result"
    - "Every xstate/effect import lives under the pilot module boundary; `app/src/lib/physicPaintFlush.ts` and everything the main chunk reaches stay library-free (D-17, D-19)"
    - "The pilot's targeted tests exist and fail against the pre-pilot implicit version — the success criterion is tests AND numbers, not numbers alone"
  artifacts:
    - path: "app/src/components/physic-paint/pilot/finalizationMachine.ts"
      provides: "the XState v6 stroke-finalization machine + its signals bridge"
    - path: "app/src/components/physic-paint/pilot/finalizationQueue.ts"
      provides: "the Effect v4 bounded-turn queue with backpressure and interruption"
    - path: "app/src/components/physic-paint/bridge/gestureIdleScheduler.ts"
      provides: "the existing pointer/idle scheduler driving the machine behind an unchanged public API"
  key_links:
    - "`app/src/lib/physicPaintFlush.ts` is imported by the MAIN chunk (`projectStore.ts:31`, `exportEngine.ts:3`) while also holding the Studio-side listener — so the pilot's orchestration must live under `physic-paint/pilot/` and the facade must gain no library import, or the frozen 1320 kB budget breaks for the main chunk (D-19). The flush orchestration itself is plan 15's; this plan must keep the facade untouched so that plan starts from a green boundary"
    - "`gestureIdleScheduler.test.ts` is the tracer's contract and is deliberately NOT in this plan's file list: the machine refactor must leave the existing public API and timing behavior observably identical, which is what makes the refactor reversible — the verify gate asserts the file is absent from the diff"
    - "the telemetry categories `input-delay` and `scheduled-wait` in `physicsPaintPerformanceTrace.ts` are the D-16 baseline (`~900 ms` atomic steps, `~9 s` median queue waits) — the pilot must not rename or bypass them"
    - "D-17: all XState/Effect code stays behind the pilot module boundary until native UAT passes; async rules enter the efx-motion skill family only after that (plan 16), so nothing in this plan touches a skill file"
---

<objective>
Ship the D-16 pilot's first vertical slice — the stroke-finalization lifecycle as an XState v6 machine and the capture produce/commit work as an Effect v4 bounded-turn queue, behind the pilot module boundary (D-17), with no library import reachable from the main chunk (D-19). The flush orchestration that consumes both is plan 15, which starts from the green boundary this plan leaves.

Purpose: sensitivity-map rows 1-3 are one problem wearing three hats — a hand-rolled idle FSM, a promise-per-key capture queue with ~9 s measured waits, and the retry path around it. This plan gives that problem one declared lifecycle and one bounded, interruptible queue, wired through the two existing call sites that own the input path and the capture path, with the committed telemetry as the judge.

Output: `physic-paint/pilot/finalizationMachine.ts` and `finalizationQueue.ts` with their tests, plus `gestureIdleScheduler.ts` and `rotoLivePixelCacheTransactions.ts` rewired to them behind unchanged public APIs.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/async-conventions.md
@.planning/phases/52.2-project-package-format-references-only/52.2-RESEARCH.md
@.claude/skills/efx-preact-reactivity/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stroke-finalization machine + signals bridge (same public API)</name>
  <files>app/src/components/physic-paint/pilot/finalizationMachine.ts, app/src/components/physic-paint/pilot/finalizationMachine.test.ts, app/src/components/physic-paint/bridge/gestureIdleScheduler.ts</files>
  <reversibility rating="reversible">The machine replaces hand-rolled internals behind an unchanged public API, so reverting is a two-file revert that the existing scheduler suite already guards.</reversibility>
  <read_first>
    - app/src/components/physic-paint/bridge/gestureIdleScheduler.ts (the whole file: `activePointers`, `interactionIdle`, `GESTURE_IDLE_WINDOW_MS`, `GESTURE_MOVE_REARM_THROTTLE_MS`, `lastInteractionAt`, `onInteractionIdle`, `markInteractionActive`, `readLastInteractionAt` — this is the public API that must not change)
    - app/src/components/physic-paint/bridge/gestureIdleScheduler.test.ts (the contract the refactor must leave green)
    - SPECS/async-conventions.md (§Pilot: the machine's state vocabulary, the signals bridge pattern, and the baseline numbers this must not regress)
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts (the `input-delay` and `scheduled-wait` categories)
  </read_first>
  <behavior>
    - the machine's state is readable as a single signal value drawn from idle / active / queued / draining / flushing
    - pointerdown from idle enters active; pointerdown from queued or draining also enters active (a new gesture is never swallowed)
    - the idle outcome is reached only when zero pointers are active AND GESTURE_IDLE_WINDOW_MS of silence have elapsed — the existing timing constants and re-arm-on-move rule are unchanged
    - markInteractionActive keeps its separate meaning (the quiet-window clock) and does not by itself change the machine state
    - the existing `interactionIdle` signal flips at exactly the same moments as before the refactor
    - no consumer can read the raw pointer count — the snapshot is the only lifecycle read
  </behavior>
  <action>
    Create `app/src/components/physic-paint/pilot/finalizationMachine.ts` with the XState v6 machine for the stroke-finalization lifecycle (D-16, sensitivity-map row 1), plus the signals bridge that exposes its snapshot (D-17: process state maps onto Preact signals; the project's `efx-preact-reactivity` rules apply — one signal write per transition, read narrowly at the consumer). The machine is schema-first with per-state context per the v6 API surface; consult the v6 repository README/CHANGELOG/examples recorded in `SPECS/async-conventions.md`, never Context7 (it covers v5 only).

    Rewire `gestureIdleScheduler.ts` internals so the pointer set, the silence window and the re-arm logic drive the machine instead of ad-hoc boolean flags, while keeping EVERY existing export and its observable behavior (`gestureIdleScheduler.test.ts` must pass unmodified — if a case needs editing, the refactor changed behavior and that is a defect, not a test update). Keep the timer ownership in this module: the machine declares the lifecycle, the scheduler owns the wall-clock arming.

    This task is the tracer: after it, the whole input path — pointer events through the machine to the `interactionIdle` signal every consumer reads — is one declared lifecycle end-to-end, and the app behaves exactly as before. Write the new machine cases first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/finalizationMachine.test.ts src/components/physic-paint/bridge/gestureIdleScheduler.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>git diff --stat HEAD -- app/src/components/physic-paint/bridge/gestureIdleScheduler.test.ts</automated>
    <fails_when>non-empty output — the existing scheduler contract must pass unmodified</fails_when>
  </verify>
  <acceptance_criteria>
    - both named suites exit 0 and `gestureIdleScheduler.test.ts` is not part of the diff
    - `pilot/finalizationMachine.test.ts` has at least 6 passing cases including the pointerdown-during-draining case and the silence-window case
    - every pre-existing export of `gestureIdleScheduler.ts` still exists with an unchanged signature (assert by inspection of the diff)
    - the machine reads exactly one signal per consumer render (no derived-signal chain that re-subscribes per state)
  </acceptance_criteria>
  <done>The stroke-finalization lifecycle is a declared machine driving the unchanged scheduler API, proven by the untouched existing suite plus new machine cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded-turn queue for capture produce/commit</name>
  <files>app/src/components/physic-paint/pilot/finalizationQueue.ts, app/src/components/physic-paint/pilot/finalizationQueue.test.ts, app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.ts, app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/pilot/finalizationMachine.ts (Task 1 output — the queue's gate reads the machine snapshot)
    - app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.ts (the whole file: `CAPTURE_PRODUCE_QUIET_MS`, `RotoLivePixelIdentity`, `RotoLivePixelCapture` with `produce`/`commit`/`resolveCurrent`, and the promise-per-key map the queue replaces)
    - app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.test.ts (the existing capture-scheduling contract, including the `recordPerformance` samples whose stage names feed the `scheduled-wait` baseline)
    - SPECS/async-conventions.md (§Pilot: bounded turns and backpressure as the named architectural problem for sensitivity-map row 3)
  </read_first>
  <behavior>
    - a submitted capture does not start producing while the machine is active — it starts on the queued → draining transition
    - at most a fixed number of turns run concurrently, and the constant is exported so the test asserts against the constant rather than a literal
    - submitting past the concurrency limit resolves only when a slot frees (backpressure), rather than growing an unbounded map
    - an interrupt (navigation) cancels the in-flight turns, and a cancelled turn's `commit` is never called
    - a turn that rejects reports its own failure and does not stop the queue
    - drain resolves once every accepted capture has settled, cancelled or committed
    - the existing `recordPerformance` sample stages are still emitted for started and settled turns
  </behavior>
  <action>
    Create `app/src/components/physic-paint/pilot/finalizationQueue.ts` on Effect v4 (zero-dep core only; do NOT install or import `@effect/platform`): a bounded-turn queue with backpressure and interruption, exposing submit / drain / interrupt against a caller-supplied concurrency constant. Interruption is the point of the module — an interrupted capture must be unable to commit, which is what the current promise-per-key map cannot express (D-16: "interruption on navigation").

    Rewire `rotoLivePixelCacheTransactions.ts` to submit through the queue instead of running `produce`/`commit` straight out of the per-key map. Preserve `CAPTURE_PRODUCE_QUIET_MS` and every existing export, and keep emitting the existing performance samples so the `scheduled-wait` baseline stays comparable. Do not change what is captured or when it commits — this task changes HOW the work is scheduled, and the observable commit outcome must be identical for the un-interrupted path.

    Extend `rotoLivePixelCacheTransactions.test.ts` with the `<behavior>` cases. Write them first, watch them fail, then implement (tdd).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/finalizationQueue.test.ts src/components/physic-paint/roto/rotoLivePixelCacheTransactions.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or any "FAIL" line in the output</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>! grep -rEq "physic-paint/pilot" app/src/lib app/src/stores</automated>
    <fails_when>exit 1 — a pilot import escaped the Studio boundary into the main chunk (D-17, D-19)</fails_when>
  </verify>
  <acceptance_criteria>
    - both named suites exit 0 with at least 6 new passing cases across them
    - the concurrency case asserts the observed concurrent turn count never exceeds the exported constant
    - the interrupt case asserts zero `commit` calls after interruption
    - the backpressure case asserts the producer promise is unresolved while all slots are held and resolves when one frees
    - no module under `app/src/lib` or `app/src/stores` imports the pilot directory
  </acceptance_criteria>
  <done>Capture finalization runs at bounded concurrency with real backpressure and interruptible turns, with the existing scheduling contract still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New runtime dependencies → shipped bundle | Two alpha/RC libraries enter the app's single-threaded render path (D-19) |
| Pilot module boundary → main chunk | Anything reachable from `physicPaintFlush.ts` is loaded by the main window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.2-46 | Denial of Service | an unbounded finalization queue under rapid painting | high | mitigate | The queue caps concurrent turns with an exported constant and applies backpressure to the producer instead of growing; asserted by a test that measures the observed concurrency against the constant. |
| T-52.2-47 | Tampering | an interrupted capture committing stale pixels | high | mitigate | Interruption is expressed in the queue, not by a caller flag: a cancelled turn's `commit` is never invoked, asserted by counting commit calls after interrupt. |
| T-52.2-49 | Elevation of Privilege | library code reaching the main chunk | medium | mitigate | Every import lives under `physic-paint/pilot/`; a negated source-scan gate asserts no `app/src/lib` or `app/src/stores` module imports the pilot while this plan's libraries are already installed. |
| T-52.2-SC | Tampering | npm installs already performed in plan 13 | high | accept | No new package is installed in this plan; the two packages were pinned to registry-verified versions under plan 13's legitimacy gate. |
</threat_model>

<verification>
`pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/pilot/finalizationMachine.test.ts src/components/physic-paint/bridge/gestureIdleScheduler.test.ts src/components/physic-paint/pilot/finalizationQueue.test.ts src/components/physic-paint/roto/rotoLivePixelCacheTransactions.test.ts` green; full suite green; `gestureIdleScheduler.test.ts` and `physicPaintFlush.ts` unmodified; no `app/src/lib` or `app/src/stores` module imports the pilot.
</verification>

<success_criteria>
The pilot's input path and capture path share one declared lifecycle and one bounded, interruptible queue — with the existing contracts untouched, the main chunk provably unaffected, and the D-16 baseline left to plan 16's re-measurement to judge.
</success_criteria>

## Artifacts this phase produces

- `app/src/components/physic-paint/pilot/finalizationMachine.ts` (new): XState v6 stroke-finalization machine + signals bridge
- `app/src/components/physic-paint/pilot/finalizationMachine.test.ts` (new)
- `app/src/components/physic-paint/pilot/finalizationQueue.ts` (new): Effect v4 bounded-turn queue, backpressure, interruption
- `app/src/components/physic-paint/pilot/finalizationQueue.test.ts` (new)
- Rewired call sites: `gestureIdleScheduler.ts`, `rotoLivePixelCacheTransactions.ts`
- `gestureIdleScheduler.test.ts`, `rotoLivePixelCacheTransactions`'s existing cases and `app/src/lib/physicPaintFlush.ts`: unchanged by design

<note>Wave boundary: the flush orchestration, the Studio wiring and the retry routing are plan 15 (same milestone, immediately after this one). Splitting there keeps each plan inside its file budget and keeps each plan's suite green on its own; the modules this plan creates are the contract plan 15 builds on.</note>

<output>
Create .planning/phases/52.2-project-package-format-references-only/52.2-14-SUMMARY.md when done
</output>
