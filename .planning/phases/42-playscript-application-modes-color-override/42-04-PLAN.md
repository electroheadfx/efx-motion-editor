---
phase: 42-playscript-application-modes-color-override
plan: 04
type: execute
wave: 4
depends_on:
  - 42-03
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: false
requirements:
  - PLAY-03

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Panel summary is a fixed two-line read-only block between the toolbar and the script list rendering the real controller's appliedSummary signals: line 1 = mode + color state + Motion values ('{Mode} · {Original colors | Override #rrggbb} · Motion {deformation}/{position}'); line 2 = destination range + generated-frame count/status ('F{start}–F{end} · {count} frames generated') (D-07, PLAY-03)"
    - "Summary line content order and composition are locked at the controller level (42-02 real-controller tests assert the exact strings, including an exact layer-end boundary range with end = start + count − 1, no off-by-one); this plan only renders the signals (edge: PLAY-03 ordering + adjacency)"
    - "Before the first successful Generate the summary renders the locked first-time defaults — line 1 'Progressive · Original colors · Motion {d}/{p}' from the Motion defaults, line 2 exactly 'No frames generated yet' (edge: PLAY-03 empty / UI-SPEC first-open, D-07, D-15)"
    - "Generated-frame count and range display exactly at capacity boundaries (e.g. Max-sized generations) with integer frame values and the existing tabular-nums/ellipsis conventions (edge: PLAY-03 boundary + precision; UI-SPEC E6)"
    - "Summary updates atomically on successful Generate and is preserved byte-identical across unsaved dialog edits, dialog Cancel, generation cancellation, and generation failure (D-07; UI-SPEC E5/E6 update contract)"
    - "Summary uses the existing dark-panel tokens (10px #aeb5be metadata, ellipsis) — no new panel color or surface (UI-SPEC E6 overflow/long-text)"
    - "Play Script toolbar tooltip covers both modes (Pitfall 8)"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (summary block + tooltip copy)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts (extended)
    - app/src/components/physic-paint/physicsPaintStudio.css (summary block rules, existing panel token scope)
  key_links:
    - "Summary block is a pure read-only projection of the controller appliedSummary signals built in 42-02 — no panel-local state"
  prohibitions:
    - "Never update the summary on unsaved dialog edits, cancellation, or generation failure (D-07)"
    - "Never import the dialog's light-surface label style into the dark panel (UI-SPEC scope discipline)"
    - "Never display the term 'clip bloquant' in any panel copy (locked terminology)"
---

<objective>
Land the Scripts panel two-line options summary and corrected Play tooltip, then run the phase's native visual UAT — the user oracle that closes PLAY-01..04.

Purpose: The summary is the always-visible half of PLAY-03 panel clarity (the dialog is the on-demand half); the UAT proves both modes, the override, the loop readout, and the overflow contract live.
Output: Summary block + tooltip update + extended panel tests + approved native UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md
@.planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md
@.planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md
@.planning/phases/42-playscript-application-modes-color-override/42-02-SUMMARY.md
@.planning/phases/42-playscript-application-modes-color-override/42-03-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-line panel summary + Play tooltip update (RED→GREEN)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (full file — toolbar lines 75-166, listbox 167-208, status line aria-live, Play tooltip line 78, IconButton pattern)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts (existing harness to extend)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (appliedSummary line1/line2 signals from 42-02)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Copywriting Contract — summary line 1/2 forms, tooltip update; panel surface color table)
    - .planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md (panel section — insertion point between toolbar and listbox)
  </read_first>
  <behavior>
    - RED first — new panel test cases: (a) before any successful Generate the summary renders the locked first-time defaults content (line 1 'Progressive · Original colors · Motion {d}/{p}'; line 2 'No frames generated yet'); (b) after a successful static+override Generate the block renders the controller-composed strings verbatim — line 1 'Static / Hold · Override #3366ff · Motion 25/40', line 2 'F4–F7 · 4 frames generated'; (c) a cancelled or failed generation leaves the previous summary text byte-identical; (d) the Play button tooltip no longer claims progressive-only
  </behavior>
  <action>
    RED: extend PhysicsPaintScriptsPanel.test.ts with cases (a)-(d) against a fake controller harness exposing appliedSummary line1/line2 signals carrying the exact 42-02 composed forms; run and confirm red. GREEN, panel: insert the two-line read-only summary block in PhysicsPaintScriptsPanel.tsx between the toolbar div and the listbox div, rendering playScript.appliedSummary.line1.value and line2.value verbatim (composition is controller-owned per 42-02: line 1 '{Mode} · {Original colors | Override #rrggbb} · Motion {deformation}/{position}'; line 2 'F{start}–F{end} · {count} frames generated'; the panel performs NO string assembly); the block is presentational only — no panel-local state, no new controls. Update the Play Script IconButton tooltip fallback copy to cover both modes, e.g. 'Play Script — Generate real Roto keys (progressive or static/hold)' (Pitfall 8). In physicsPaintStudio.css add summary rules using existing dark-panel tokens only (10px metadata at #aeb5be matching the physics-paint-script-provenance convention, #eef1f4 values, overflow hidden + text-overflow ellipsis + white-space nowrap per line) — class prefix physics-paint-scripts-summary; do not alter any pre-existing panel rule. Run the panel suite until green; commit: feat(42-04): add two-line play script options summary to scripts panel.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with cases (a)-(d) green and no panel regressions
    - Panel source renders the summary from playScript.appliedSummary signals between the toolbar and listbox markup
    - Panel source tooltip copy contains 'progressive or static/hold'
    - physicsPaintStudio.css diff for this task adds only .physics-paint-scripts-summary selectors and modifies no existing rules
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Scripts panel shows the locked two-line summary with success-only update semantics, dark-panel-native styling, and a tooltip that matches the two-mode reality.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Native visual UAT — PlayScript modes, color override, loop readout, panel summary</name>
  <files>.planning/phases/42-playscript-application-modes-color-override/42-04-UAT.md</files>
  <read_first>
    - .planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md (D-01..D-15 — the behaviors being accepted)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Copywriting + Interaction Contracts — verbatim copy to check)
    - .planning/ROADMAP.md Phase 42 success criteria 1-4
  </read_first>
  <what-built>
    Full PlayScript application-options workflow: Progressive | Static / Hold segmented mode control with helper line and arrow-key navigation; mode-dependent Frames/Cycle frames field; Original colors / override swatch with inline Box/TSL/RVB/CMYK picker; application-time Motion sliders with Reset to Motion defaults; Hold Loop Repeat + Infinity toggle with Requested/Effective/truncation readout; two-line Scripts panel summary updating only on successful Generate; updated Play tooltip.
  </what-built>
  <how-to-verify>
    Run the app natively (user-owned; per project convention agents never launch it). Open a saved project with durable scripts, open its Paint layer, and in the Scripts panel select a script containing BOTH paint and erase strokes, then:
    1. Mode control: open Play Script — the segmented control reads Progressive | Static / Hold with the Progressive helper 'The drawing builds stroke by stroke across frames.'; select Static / Hold — helper switches to 'The complete drawing is applied to every cycle frame.'; verify Left/Right arrows move selection with wrap and Tab reaches the group as one stop.
    2. Progressive unchanged: with default options (Progressive, Original colors), Generate — output matches pre-Phase-42 progressive behavior.
    3. Static/hold: switch to Static / Hold, set Cycle frames 5, Generate — every destination frame shows the COMPLETE script drawing (not a progressive build).
    4. Color override in both modes: pick an override color via the swatch (picker opens on first enable; simply opening and closing it leaves 'Original colors' intact); re-Generate in Static / Hold — paint strokes recolor, erase strokes still erase; re-Generate in Progressive with the override — same recoloring; the library script row and its thumbnail are unchanged.
    5. Motion: edit dialog Motion sliders, Generate — only the new generation reflects them; the Motion panel defaults are untouched; Reset to Motion defaults restores the current panel defaults through the controller; no Save as defaults exists.
    6. Hold Loop readout: Cycle 5 × Repeat 5 shows 'Requested: 25f (5f × 5) · Effective: ...' with the truncation clause '— shortened by the next clip' only when truncated; toggle Infinity on — repeat greys out keeping its value and the readout shows 'Cycle 5f × ∞'; toggle off — the previous repeat value returns. Generation still produces exactly Cycle frames of real keys (no repeat materialization).
    7. Panel summary + generation outcomes: after a successful Generate the two-line summary shows mode · color · Motion and range · count; open the dialog, change options, Cancel — summary unchanged; force a generation failure — the dialog stays open showing the failure reason in the red inline-error style with the progress bar gone and controls re-enabled, and the summary is unchanged; retry succeeds from a clean state; cancel mid-generation — the dialog returns idle with NO error shown and the summary unchanged.
    8. Overflow: shrink the window to the minimum supported size with all options visible — only the dialog body scrolls; title/mode and the Generate/Cancel row stay visible; no horizontal scroll.
    9. Packaged build (CSP divergence guard): repeat steps 1 and 4 in a packaged build — the dialog and inline picker render and function identically (project rule: prove CSP-sensitive UI on packaged builds).
    Record results in 42-04-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <acceptance_criteria>
    - 42-04-UAT.md records steps 1-9 with pass/fail each and user approval
    - All 9 steps pass on native run; step 9 passes on a packaged build
  </acceptance_criteria>
  <done>User approves native UAT: PLAY-01..04 behaviors verified live, including packaged-build picker/CSP check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller signals → panel DOM | read-only projection of session option state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04-01 | Information disclosure | summary text in panel DOM | low | accept | User's own session options rendered by Preact with framework escaping; no persistence |
| T-42-04-SC | Tampering | package installs | low | accept | No package installs in this plan (RESEARCH: zero new dependencies) |
</threat_model>

## Artifacts this phase produces

This plan creates:
- Modified component `PhysicsPaintScriptsPanel`: two-line read-only summary block between toolbar and listbox rendering the controller's `appliedSummary.line1/line2` signals verbatim (composition owned by 42-02); updated Play Script tooltip copy
- Extended test file `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts`
- New CSS selectors `.physics-paint-scripts-summary*` in app/src/components/physic-paint/physicsPaintStudio.css (dark-panel tokens only)
- New file `.planning/phases/42-playscript-application-modes-color-override/42-04-UAT.md` (native UAT record)

<verification>
- Task 1 gate: `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts` exits 0
- Phase gate before UAT: `pnpm --dir app exec vitest run` + `NODE_PATH=app/node_modules app/node_modules/.bin/vitest --run --config /dev/null packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts` + `pnpm --dir app typecheck` all exit 0
- Task 2: user-approved native UAT recorded in 42-04-UAT.md
</verification>

<success_criteria>
- Two-line summary live with locked content, order, and success-only update semantics
- Tooltip covers both modes
- Native UAT approved (steps 1-9) including packaged-build verification
</success_criteria>

<output>
Create `.planning/phases/42-playscript-application-modes-color-override/42-04-SUMMARY.md` when done
</output>
