---
phase: 42
plan: 04
kind: uat-record
status: pending
created: 2026-08-05
revised: 2026-08-06 (2) (final visual correction — compact dark modal overlay per Image #153; Image #154 light page rejected; Timing-left / Color-over-Motion grid; no-scroll compact fit)
---

# Phase 42 Plan 04 — Native Visual UAT: Compact Dark Play Script Modal, Live Custom Color, Loop Intent, Panel Summary

**Builds under test:** dev/native run (steps 1-8) + packaged build (step 9).
**Prerequisites:** open a saved project with durable scripts; open its Paint layer; in the Scripts panel select a script containing BOTH paint and erase strokes.
**Reference:** 42-UI-SPEC.md (final) Modal Architecture + Copywriting + Interaction Contracts; 42-CONTEXT.md D-01..D-19; Image #153 (approved target); ROADMAP.md Phase 42 success criteria 1-4.

| # | Step | Expected | Result (pass/fail) | Notes |
|---|------|----------|--------------------|-------|
| 1 | Modal architecture + layout | COMPACT DARK modal ABOVE the Paint interface via the Scripts-panel toolbar action; canvas/Studio layout stay in place behind it; never replaces the canvas, never resizes the layout; recognizably Image #153, NOT Image #154's light page. Header: `Play Script` left, `Max {N} · F{start}–F{end}` right. Mode card full width; Timing card LEFT at full row height; Color TOP-RIGHT; Motion wiggle DIRECTLY BELOW Color; right stack total height == Timing height; Requested/Effective beneath the main grid; footer (progress + Cancel + Generate) INSIDE the modal | | |
| 2 | Progressive unchanged | With default options (Progressive, Original colors), Generate output matches pre-Phase-42 progressive behavior | | |
| 3 | Static / Hold | Frames per cycle 5, Generate — every destination frame shows the COMPLETE script drawing (not a progressive build) | | |
| 4 | Live Custom color in both modes | `Custom color` shows the CURRENT right-panel brush color as chip + hex + note `Picked from the app's brush color panel`; changing the right-panel brush color while open updates the chip/hex live; Generate in Static / Hold — paint strokes recolor to the snapshot, erase strokes still erase; brush change AFTER Generate does NOT alter frames or summary; same in Progressive; `Original colors` disables the override; script row + thumbnail unchanged | | |
| 5 | Motion wiggle | Slider edits affect only the new generation; Motion panel defaults untouched; `Reset defaults` (heading link) restores current defaults through the controller; no `Save as defaults` | | |
| 6 | Loop intent readout | Frames per cycle 5 × Repeat 5 → `Requested: 25f (5f × 5) · Effective: ...` in the summary bar; `— shortened by the next clip` only when truncated; Infinity on — repeat greys out keeping its value, `Cycle 5f × ∞`; Infinity off — previous value returns; generation produces exactly Frames-per-cycle real keys (ONE source cycle; linked Loop Clips are Phase 43); Repeat + Infinity visible in Progressive too | | |
| 7 | Panel summary + generation outcomes | After Generate the two-line summary shows mode · color (snapshot hex) · Motion and range · count; edits + Cancel — summary unchanged; forced failure — modal stays open with inline error, progress gone, controls re-enabled, summary unchanged; retry succeeds; mid-generation cancel — idle with NO error, summary unchanged | | |
| 8 | Compact fit + close | At the minimum supported window size ALL modal rows fully visible — NO vertical/horizontal scrollbar, NO body scrolling region, no clipped card/helper/summary/footer; close via Cancel and via Escape — Paint layout unchanged after closing | | |
| 9 | Packaged build (CSP divergence guard) | Repeat steps 1 and 4 in a packaged build — dark modal and live Custom color link render and function identically | | |

## Verdict

- [ ] Approved
- [ ] Issues reported (describe below)

**User feedback:**

(filled during UAT)
