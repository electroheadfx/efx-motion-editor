---
phase: 42
plan: 04
kind: uat-record
status: pending
created: 2026-08-05
---

# Phase 42 Plan 04 — Native Visual UAT: PlayScript Application Modes, Color Override, Loop Readout, Panel Summary

**Builds under test:** dev/native run (steps 1-8) + packaged build (step 9).
**Prerequisites:** open a saved project with durable scripts; open its Paint layer; in the Scripts panel select a script containing BOTH paint and erase strokes.
**Reference:** 42-UI-SPEC.md Copywriting + Interaction Contracts (verbatim copy to check); 42-CONTEXT.md D-01..D-15; ROADMAP.md Phase 42 success criteria 1-4.

| # | Step | Expected | Result (pass/fail) | Notes |
|---|------|----------|--------------------|-------|
| 1 | Mode control | Segmented control reads `Progressive \| Static / Hold`; Progressive helper `The drawing builds stroke by stroke across frames.`; selecting Static / Hold switches helper to `The complete drawing is applied to every cycle frame.`; Left/Right arrows move selection with wrap; Tab reaches the group as one stop | | |
| 2 | Progressive unchanged | With default options (Progressive, Original colors), Generate output matches pre-Phase-42 progressive behavior | | |
| 3 | Static / Hold | Cycle frames 5, Generate — every destination frame shows the COMPLETE script drawing (not a progressive build) | | |
| 4 | Color override in both modes | Picker opens on first enable; open+close leaves `Original colors` intact; re-Generate in Static / Hold — paint strokes recolor, erase strokes still erase; same in Progressive; library script row and thumbnail unchanged | | |
| 5 | Motion | Dialog Motion slider edits affect only the new generation; Motion panel defaults untouched; `Reset to Motion defaults` restores current panel defaults; no `Save as defaults` exists | | |
| 6 | Hold Loop readout | Cycle 5 × Repeat 5 shows `Requested: 25f (5f × 5) · Effective: ...`; truncation clause `— shortened by the next clip` only when truncated; Infinity on — repeat greys out keeping its value, readout `Cycle 5f × ∞`; Infinity off — previous repeat value returns; generation produces exactly Cycle frames of real keys (no repeat materialization) | | |
| 7 | Panel summary + generation outcomes | After successful Generate the two-line summary shows mode · color · Motion and range · count; dialog edits + Cancel — summary unchanged; forced failure — dialog stays open with red inline error, progress bar gone, controls re-enabled, summary unchanged; retry succeeds from clean state; mid-generation cancel — dialog idle with NO error, summary unchanged | | |
| 8 | Overflow | Window at minimum supported size with all options visible — only the dialog body scrolls; title/mode and Generate/Cancel row stay visible; no horizontal scroll | | |
| 9 | Packaged build (CSP divergence guard) | Repeat steps 1 and 4 in a packaged build — dialog and inline picker render and function identically | | |

## Verdict

- [ ] Approved
- [ ] Issues reported (describe below)

**User feedback:**

(filled during UAT)
