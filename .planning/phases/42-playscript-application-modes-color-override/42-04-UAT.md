---
phase: 42
plan: 04
kind: uat-record
status: pending
created: 2026-08-05
revised: 2026-08-06 (revised dialog surface per approved playscript-proposal — card grid + live Custom color; inline-picker steps removed; loop-intent clarified)
---

# Phase 42 Plan 04 — Native Visual UAT: Revised Play Script Dialog, Live Custom Color, Loop Intent, Panel Summary

**Builds under test:** dev/native run (steps 1-8) + packaged build (step 9).
**Prerequisites:** open a saved project with durable scripts; open its Paint layer; in the Scripts panel select a script containing BOTH paint and erase strokes.
**Reference:** 42-UI-SPEC.md (revised) Copywriting + Interaction Contracts; 42-CONTEXT.md D-01..D-16; 42-UI-REVISION-DELTA.md (one-source-cycle architecture guard); ROADMAP.md Phase 42 success criteria 1-4.

| # | Step | Expected | Result (pass/fail) | Notes |
|---|------|----------|--------------------|-------|
| 1 | Layout + mode control | Mode card spans the top (`Progressive \| Static / Hold` + Progressive helper `The drawing builds stroke by stroke across frames.`); Timing and Color cards side by side; Motion wiggle card full width; Requested/Effective summary bar closes the body; progress + Cancel/Generate pinned in the footer. Selecting Static / Hold switches the helper to `The complete drawing is applied to every cycle frame.` and the frame label to `Frames per cycle`; arrows move selection with wrap; Tab reaches the group as one stop | | |
| 2 | Progressive unchanged | With default options (Progressive, Original colors), Generate output matches pre-Phase-42 progressive behavior | | |
| 3 | Static / Hold | Frames per cycle 5, Generate — every destination frame shows the COMPLETE script drawing (not a progressive build) | | |
| 4 | Live Custom color in both modes | Selecting `Custom color` shows the CURRENT right-panel brush color as chip + hex + note `Picked from the app's brush color panel`; changing the right-panel brush color while the dialog is open updates the chip/hex live; Generate in Static / Hold — paint strokes recolor to the snapshot, erase strokes still erase; changing the brush color AFTER Generate does NOT alter generated frames or the panel summary; same recoloring in Progressive; selecting `Original colors` disables the override; library script row and thumbnail unchanged throughout | | |
| 5 | Motion wiggle | Dialog slider edits affect only the new generation; Motion panel defaults untouched; `Reset defaults` (in the Motion wiggle heading) restores current panel defaults through the controller; no `Save as defaults` exists | | |
| 6 | Loop intent readout | Frames per cycle 5 × Repeat 5 shows `Requested: 25f (5f × 5) · Effective: ...` in the summary bar; truncation clause `— shortened by the next clip` only when truncated; Infinity on — repeat greys out keeping its value, summary bar shows `Cycle 5f × ∞`; Infinity off — previous repeat value returns; generation produces exactly Frames-per-cycle real keys (ONE source cycle — Repeat never materializes extra frames; linked Loop Clips are Phase 43); Repeat + Infinity also visible in Progressive mode as loop intent | | |
| 7 | Panel summary + generation outcomes | After successful Generate the two-line summary shows mode · color (snapshot hex) · Motion and range · count; dialog edits + Cancel — summary unchanged; forced failure — dialog stays open with red inline error, progress bar gone, controls re-enabled, summary unchanged; retry succeeds from clean state; mid-generation cancel — dialog idle with NO error, summary unchanged | | |
| 8 | Overflow | Window at minimum supported size with all cards visible — only the dialog body scrolls; title/mode and the fixed footer stay visible; no horizontal scroll | | |
| 9 | Packaged build (CSP divergence guard) | Repeat steps 1 and 4 in a packaged build — the card grid and the live Custom color link render and function identically | | |

## Verdict

- [ ] Approved
- [ ] Issues reported (describe below)

**User feedback:**

(filled during UAT)
