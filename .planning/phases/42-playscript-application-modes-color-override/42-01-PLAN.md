---
phase: 42-playscript-application-modes-color-override
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts
  - packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts
  - packages/efx-physic-paint/src/animation/index.ts
autonomous: true
requirements:
  - PLAY-01

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every destination frame of a static/hold cycle returns the complete script stroke set with full pointCount (PLAY-01)"
    - "An empty stroke list yields an empty schedule and zero strokes on every frame (edge: PLAY-01 empty)"
    - "Stroke output order equals input flatten order on every frame — stable and specified (edge: PLAY-01 ordering)"
    - "The first frame and the last frame of the cycle both return the complete stroke set — inclusive frame boundaries (edge: PLAY-01 adjacency)"
    - "frameCount normalization mirrors the progressive builder: Math.max(1, Math.trunc(frameCount))"
    - "progressiveStrokeSchedule.ts and its test file are byte-untouched by this plan (regression-locked, D-01/roadmap boundary note)"
  artifacts:
    - packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts
    - packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts
    - packages/efx-physic-paint/src/animation/index.ts (additive exports only)
  key_links:
    - "animation/index.ts re-exports buildStaticStrokeSchedule/getStaticFrameStrokes so the app vite alias '@efxlab/efx-physic-paint/animation' (app/vite.config.ts:158-160) resolves them with no build config change"
  prohibitions:
    - "Never modify, branch, or parameterize packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.ts or progressiveStrokeSchedule.test.ts (regression-locked per roadmap boundary note)"
---

<objective>
Ship the static/hold stroke schedule as a NEW additive sibling module in `@efxlab/efx-physic-paint/animation`, test-first. The static schedule is the deterministic heart of PLAY-01: every stroke, full point count, every frame.

Purpose: Phase 43's linked-loop resolver and Phase 42's renderer both consume this package export; the roadmap boundary note locks it as a package-level sibling — the regression-locked progressive module is never branched.
Output: `staticStrokeSchedule.ts` + package test + additive barrel exports, green alongside the untouched progressive suite.
</objective>

<assumption_delta_decision>
Detector fired (kind=optional, term "optional application-time color override"). Question answered: has a required field become optional / has the anchor moved?
- Noun now primary: per-stroke `PaintStroke.color` inside the immutable script document (unchanged — still the identity anchor for stroke color).
- Decision: `no-change` — the application-time override is render policy riding `RotoPlayScriptRenderInput`, not a modeling change; the script document model needs no generalization. No companion invariant test needed beyond the PLAY-02 geometry-parity tests planned in 42-02.
</assumption_delta_decision>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md
@.planning/phases/42-playscript-application-modes-color-override/42-RESEARCH.md
@.planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md
@packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.ts
@packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts
@packages/efx-physic-paint/src/animation/index.ts
@packages/efx-physic-paint/src/animation/types.ts
</context>

<feature>
  <name>Static/hold stroke schedule (buildStaticStrokeSchedule / getStaticFrameStrokes)</name>
  <files>packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts, packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts, packages/efx-physic-paint/src/animation/index.ts</files>
  <behavior>
    Test cases (input -> expected output):
    - 3 strokes, frameCount 5, frame 0 -> all 3 strokes, each with pointCount === stroke.points.length
    - 3 strokes, frameCount 5, frame 4 (last) -> identical complete set (inclusive boundaries)
    - frameCount 1 -> single frame still returns the complete set (minimum hold, D-15 semantics)
    - strokes [A, B, C], any frame -> output order exactly [A, B, C] (stable, specified)
    - empty stroke list -> build returns []; accessor returns [] for any frameIndex
    - frameCount 2.7 -> normalized to 2 (mirrors progressive Math.max(1, Math.trunc(...)))
    - frameCount 0 -> normalized to 1
    - transform callback provided -> invoked once per stroke per accessor call with (stroke, frameIndex, strokeIndex); result stroke used in output
    - erase-tool strokes pass through identically (schedule is tool-agnostic; color policy lives in the renderer)
  </behavior>
  <implementation>
    Mirror the progressive module shape exactly (PATTERNS): named exports, no semicolons, single quotes, JSDoc on public functions. Types `StaticStrokeTransform` (same triple signature as ProgressiveStrokeTransform) and `StaticStrokeFrame` ({ stroke, pointCount }). buildStaticStrokeSchedule(strokes, frameCount) returns FrameStroke[] with every entry startFrame: 0, endFrame: usableFrames - 1, pointsPerFrame: stroke.points.length. getStaticFrameStrokes(schedule, frameIndex, transform?) returns every entry with full pointCount, applying the optional transform. Additive exports appended to animation/index.ts — one value-export line + one type-export line, nothing else touched.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing package test for the static/hold schedule</name>
  <files>packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts (fixture factories `point`/`stroke`/`reveal`, semicolon style — copy verbatim, including erase-tool-capable factory)
    - packages/efx-physic-paint/src/animation/types.ts (FrameStroke shape)
    - packages/efx-physic-paint/src/types.ts (PaintStroke, PenPoint, ToolType)
    - .planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md (static schedule test section)
  </read_first>
  <behavior>
    - All 9 cases from the feature behavior block, written against the not-yet-existing module `./staticStrokeSchedule`
    - Import names: `buildStaticStrokeSchedule`, `getStaticFrameStrokes`
  </behavior>
  <action>
    Create packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts. Copy the `point` and `stroke` fixture factories verbatim from progressiveStrokeSchedule.test.ts (they produce valid PaintStroke fixtures; semicolon test style). Write the 9 cases from the feature behavior block: complete-set-on-every-frame, last-frame parity, frameCount 1 minimum hold, stable input order, empty list, fractional frameCount truncation to 2, zero frameCount normalization to 1, transform callback invocation with (stroke, frameIndex, strokeIndex), erase-tool pass-through. Run the package test command and confirm it FAILS because the module does not exist — this is the required red state. Commit: test(42-01): add failing test for static/hold stroke schedule.
  </action>
  <verify>
    <automated>NODE_PATH=app/node_modules app/node_modules/.bin/vitest --run --config /dev/null packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - staticStrokeSchedule.test.ts exists and contains describe/it blocks for all 9 behavior cases
    - The verify command exits non-zero (red) with a module-resolution or missing-export failure for ./staticStrokeSchedule
    - Test file uses semicolons and imports type { PaintStroke, PenPoint } from '../types'
  </acceptance_criteria>
  <done>Red state committed: 9 failing cases encoding PLAY-01 static schedule semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement staticStrokeSchedule.ts + additive barrel exports</name>
  <files>packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts, packages/efx-physic-paint/src/animation/index.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.ts (full file — shape to mirror: type declarations, builder normalization, accessor triple)
    - packages/efx-physic-paint/src/animation/index.ts (current 7-line barrel — append only)
    - packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts (the red suite to satisfy)
    - app/vite.config.ts lines 157-160 (source alias that makes new exports live without build config changes)
  </read_first>
  <action>
    Create packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts mirroring the progressive sibling: import type { PaintStroke } from '../types' and FrameStroke from './types'; export type StaticStrokeTransform = (stroke: PaintStroke, frameIndex: number, strokeIndex: number) => PaintStroke; export interface StaticStrokeFrame { stroke: PaintStroke; pointCount: number }; export function buildStaticStrokeSchedule(strokes: readonly PaintStroke[], frameCount: number): FrameStroke[] — usableFrames = Math.max(1, Math.trunc(frameCount)); empty input returns []; every stroke maps to { stroke, startFrame: 0, endFrame: usableFrames - 1, pointsPerFrame: stroke.points.length }; export function getStaticFrameStrokes(schedule, frameIndex, transform?) returning every entry with full pointCount, applying transform?.(entry.stroke, frameIndex, strokeIndex) ?? entry.stroke. No semicolons, single quotes, JSDoc on both public functions. In animation/index.ts append exactly two lines: a value export of buildStaticStrokeSchedule/getStaticFrameStrokes from './staticStrokeSchedule' and a type export of StaticStrokeFrame/StaticStrokeTransform — no other barrel line is edited. Do not open progressiveStrokeSchedule.ts for editing at all. Run the package tests (both static and progressive) until green. Commit: feat(42-01): implement static/hold stroke schedule with additive animation exports.
  </action>
  <verify>
    <automated>NODE_PATH=app/node_modules app/node_modules/.bin/vitest --run --config /dev/null packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0: all static schedule tests pass AND all 8 pre-existing progressive tests pass unmodified
    - git diff --exit-code -- packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts exits 0 (progressive module byte-untouched)
    - animation/index.ts contains export lines naming buildStaticStrokeSchedule and getStaticFrameStrokes; diff on index.ts is append-only (no deleted lines)
    - staticStrokeSchedule.ts exports StaticStrokeTransform, StaticStrokeFrame, buildStaticStrokeSchedule, getStaticFrameStrokes
  </acceptance_criteria>
  <done>Green state committed: static/hold schedule available from '@efxlab/efx-physic-paint/animation'; progressive output provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → schedule module | frameCount/strokes arrive from the app renderer; numeric normalization is the only input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01-01 | DoS | buildStaticStrokeSchedule frameCount | low | mitigate | Math.max(1, Math.trunc(frameCount)) normalization mirrors progressive; downstream renderer MAX_FRAME_COUNT=10_000 guard remains the sole volume bound (unchanged) |
| T-42-01-SC | Tampering | package installs | low | accept | No package installs in this plan — audit table not applicable (RESEARCH: zero new dependencies) |
</threat_model>

## Artifacts this phase produces

This plan creates:
- New file `packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts` exporting: type `StaticStrokeTransform`, interface `StaticStrokeFrame` ({ stroke, pointCount }), function `buildStaticStrokeSchedule(strokes, frameCount): FrameStroke[]`, function `getStaticFrameStrokes(schedule, frameIndex, transform?): StaticStrokeFrame[]`
- New file `packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts`
- Modified `packages/efx-physic-paint/src/animation/index.ts` — additive value export (buildStaticStrokeSchedule, getStaticFrameStrokes) + type export (StaticStrokeFrame, StaticStrokeTransform); public package symbols consumed via '@efxlab/efx-physic-paint/animation'

<verification>
- Package gate: `NODE_PATH=app/node_modules app/node_modules/.bin/vitest --run --config /dev/null packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts` exits 0
- Progressive lock gate: `git diff --exit-code -- packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts` exits 0
</verification>

<success_criteria>
- buildStaticStrokeSchedule/getStaticFrameStrokes importable from '@efxlab/efx-physic-paint/animation' via the app source alias
- 9 new static schedule tests + 8 existing progressive tests green in one command
- Zero diff on the progressive module and its test
</success_criteria>

<output>
Create `.planning/phases/42-playscript-application-modes-color-override/42-01-SUMMARY.md` when done
</output>
