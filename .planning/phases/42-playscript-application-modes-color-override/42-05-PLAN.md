---
phase: 42-playscript-application-modes-color-override
plan: 05
type: execute
wave: 5
depends_on:
  - 42-03
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts
  - app/src/components/physic-paint/view/MemoizedPhysicsPaintPlayScriptDialog.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - PLAY-02
  - PLAY-03
  - PLAY-04

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Color is a two-state segmented control 'Original colors' | 'Custom color' using the same W3C APG radiogroup pattern as the Mode control (aria-checked, roving tabindex, arrow keys with wrap, one Tab stop) (D-08R, UI-SPEC Interaction Contract)"
    - "Selecting 'Original colors' disables the override; selecting 'Custom color' immediately resolves the override from the CURRENT right-panel brush color; the Custom pane shows the live brush chip + hex + note 'Picked from the app's brush color panel' (D-08R)"
    - "The dialog renders the live brush color from a prop fed by Studio settings.color (single writer setBrushColor) — no copied dialog-side color state, no DOM queries, no new color store, no new global event (D-08R)"
    - "Generate snapshots the resolved color via the controller getBrushColor port at confirm time; later brush-color changes never retroactively change generated frames, remembered options, or the success-only panel summary (D-08R)"
    - "The in-dialog InlineColorPicker mount, its pick-guard logic, and the pick-guard tests are fully removed (D-08/D-09 superseded)"
    - "Dialog body is the approved card grid: Mode (full width) → Timing | Color (side by side) → Motion wiggle (full width, 'Reset defaults' link in the section heading) → Requested/Effective summary bar (verbatim loopReadout, tabular-nums); progress + Cancel/Generate stay in the fixed footer (D-16)"
    - "Frame field label is 'Frames' in Progressive and 'Frames per cycle' in Static / Hold; switching to Static / Hold with 'Max' in the field normalizes it to '1' (D-03 revised)"
    - "Repeat + Infinity render in the Timing card in BOTH modes as session-level loop intent; the summary bar renders the locked D-13 forms; generation count remains the frame field value alone — Repeat never multiplies frameCount (D-02/D-12/D-14)"
    - "Paint strokes recolor; erase strokes remain erase strokes; the override stays post-Motion and color-only; the reusable source script and thumbnail stay unchanged (D-11 — 42-02 renderer contract, re-verified)"
    - "Dialog overflow contract holds with the new grid: surface bounded to viewport/grid height, only the body scrolls, title/mode context and fixed footer stay visible, no horizontal scroll, focused controls scroll into view with visible focus outlines (UI-SPEC E1 overflow)"
    - "All markup stays inside .physics-paint-play-script-dialog/-surface/-content with the physics-paint-play-script-* class prefix (D-04, Pitfall 6)"
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (getBrushColor port resolution)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (card grid + color toggle)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts (rewritten color cases)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (brushColor prop + getBrushColor port wiring)
    - app/src/components/physic-paint/physicsPaintStudio.css (card grid, summary bar, chip styles — dialog scope only)
  key_links:
    - "Studio settings.color (PhysicsPaintStudio.tsx useState; sole writer setBrushColor in usePhysicsPaintEngineActions) is the single brush-color source; dialog receives it as a live prop via playScriptDialogPropsMemo; controller receives a getBrushColor port closure at construction (same pattern as getMotion)"
    - "Controller confirm() resolves renderOverrideColor = overrideEnabled.peek() ? ports.getBrushColor() : null — replacing the dialog-fed overrideColor signal peek; the 42-02 renderer input contract (overrideColor string | null) is unchanged"
    - "appliedSummary line 1 keeps the D-07 form '{Mode} · {Original colors | Override #rrggbb} · Motion {d}/{p}' composed from the confirm-time snapshot"
  prohibitions:
    - "Never multiply frameCount by Repeat or feed Effective duration into generation (D-02/D-14)"
    - "Never query the DOM, duplicate the picker, create another color store, or introduce a new global event for the brush color (D-08R)"
    - "Never write dialog Motion edits to updatePanelMotion, physicPaintStore, or per-stroke properties (D-06)"
    - "Never add a 'Save as defaults' action in Phase 42 (D-06)"
    - "Never display the term 'clip bloquant' or any French loop label in Phase 42 UI (locked terminology)"
    - "Never modify InlineColorPicker.tsx itself — it remains the right panel's picker; only the dialog mount is removed"
---

<objective>
Adopt the approved playscript-proposal UI revision in the Play Script dialog: card-grid layout, two-state live brush-color control replacing the in-dialog picker, summary-bar readout, and the loop-intent clarification — while preserving the 42-02 renderer contract and cycle-only generation.

Purpose: closes the UI gap found in native UAT (Repeat looked immediately generative; picker interaction superseded by the approved proposal). This is a UI/feed revision, not an architecture change.
Output: revised dialog + controller color resolution + Studio wiring + scoped CSS + rewritten dialog tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md
@.planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md
@.planning/phases/42-playscript-application-modes-color-override/42-UI-REVISION-DELTA.md
@.planning/phases/42-playscript-application-modes-color-override/42-RESEARCH.md
@.planning/phases/42-playscript-application-modes-color-override/42-03-SUMMARY.md
@SPECS/playscript-proposal/playscript-panel.html
@SPECS/playscript-proposal/ui-play-script-specs.md
@app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Controller getBrushColor port — override resolves live, snapshots at confirm (RED→GREEN)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (full file — overrideColor/overrideEnabled signals, openConfirmation first-open compose, confirm() renderOverrideColor resolution, composeSummaryLine1, return interface)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (existing override/mode/summary cases to revise)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-REVISION-DELTA.md §5 (locked color contract)
    - .planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md (D-08R, D-10 revised)
  </read_first>
  <behavior>
    - RED first — revised/new controller cases: (a) confirm with overrideEnabled true resolves the override color from the getBrushColor port AT CONFIRM TIME (two confirms with different port values produce different renderer overrideColor inputs — no stale stored color); (b) confirm with overrideEnabled false passes overrideColor null (Original colors disables the override); (c) the first-open appliedSummaryLine1 compose uses the CURRENT port value when overrideEnabled is true; (d) a successful Generate's appliedSummary line 1 keeps the confirm-time snapshot hex even when the port value changes afterwards (no retroactive rewrite); (e) the overrideColor signal is gone from the controller return interface — overrideEnabled remains; (f) all 42-02 cycle-only generation, repeat bound, readout, reset/E5 boundary tests stay green unchanged
  </behavior>
  <action>
    RED: revise physicsPaintRotoPlayScriptController.test.ts — replace cases that set overrideColor directly with port-driven cases (a)-(d) using a mutable fake getBrushColor port; keep every other suite intact; run and confirm red. GREEN, controller: add getBrushColor: () => string to the controller ports contract; remove the overrideColor signal (keep overrideEnabled); in confirm() resolve renderOverrideColor = overrideEnabled.peek() ? ports.getBrushColor() : null (normalize/validate the hex defensively — fall back to null + Original-colors behavior on a malformed port value, mirroring existing input discipline); update the openConfirmation first-open compose to use the same port resolution; remove overrideColor from the return interface and update composeSummaryLine1 call sites. Keep the renderer call shape and every other boundary byte-identical. Run the controller + renderer suites until green; commit: feat(42-05): resolve play script override color from live brush-color port.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with cases (a)-(f) green
    - Controller source contains no overrideColor signal; confirm() resolves via ports.getBrushColor() guarded by overrideEnabled
    - Renderer file is untouched (git diff --exit-code on physicsPaintRotoPlayScriptRenderer.ts)
    - pnpm --dir app typecheck exits 0 (dialog will temporarily fail typecheck until Task 2 — if so, land Task 1 and Task 2 typecheck together; do NOT leave a red typecheck at plan end)
  </acceptance_criteria>
  <done>The override color has a single live source (Studio brush color) resolved at confirm time, with the snapshot/no-retroactive contract locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dialog card grid + two-state live color control + Studio wiring (RED→GREEN)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts, app/src/components/physic-paint/view/MemoizedPhysicsPaintPlayScriptDialog.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - SPECS/playscript-proposal/playscript-panel.html (dialog export block ~lines 904-1008 — card structure; dialog CSS block ~line 214 — grid/layout reference only, tokens come from the existing design system)
    - SPECS/playscript-proposal/ui-play-script-specs.md (scope discipline + color two-state contract)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (current post-42-03 state — mode radiogroup, InlineColorPicker mount + pick-guard, Motion section, Hold Loop section)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts (current cases — picker-guard cases to remove)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx lines 703-722 (controller ports) and 1246-1250 (playScriptDialog props memo)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx line 179 + usePhysicsPaintEngineActions setBrushColor (single brush-color source)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Layout, Copywriting, Interaction Contracts — revised)
  </read_first>
  <behavior>
    - RED first — rewritten dialog test cases: (a) color control is a radiogroup with exactly two options 'Original colors' and 'Custom color', arrow-key navigation moves check with wrap; (b) default state has 'Original colors' checked and the override disabled; (c) checking 'Custom color' sets controller overrideEnabled true and renders the CURRENT brushColor prop as chip + hex with the note 'Picked from the app's brush color panel'; re-rendering with a different brushColor prop updates the chip/hex (live link, no dialog-side copy); (d) checking 'Original colors' sets overrideEnabled false; (e) no InlineColorPicker is mounted anywhere in the dialog; (f) frame label switches 'Frames' ↔ 'Frames per cycle' and switching to static with countText 'Max' normalizes the field to '1'; (g) layout structure: Mode card first, Timing and Color cards as siblings, Motion wiggle card with a 'Reset defaults' control inside its section heading, summary bar rendering loopReadout verbatim before the footer; (h) Repeat + Infinity render in the Timing card in BOTH modes; (i) 'Reset defaults' calls only the controller resetDialogMotion spy; no 'Save as defaults'; (j) E5 failure/cancellation rendering contract unchanged from 42-03 (inline error above the footer actions, no progress bar, controls re-enabled; cancelled renders no error)
  </behavior>
  <action>
    RED: rewrite PhysicsPaintPlayScriptDialog.test.ts — delete the pick-guard/picker cases, add cases (a)-(j) against a fake controller harness (now WITHOUT an overrideColor signal, WITH overrideEnabled) and a brushColor prop; run and confirm red. GREEN, dialog: restructure into the D-16 card grid — Mode card (existing radiogroup + helper, unchanged semantics), Timing card (frame field with revised label logic + Max→1 normalization on mode switch, Repeat field + Infinity toggle, both modes), Color card (APG radiogroup 'Original colors' | 'Custom color'; Original pane = dots + 'Keep each stroke's original paint color.'; Custom pane = chip + hex from the brushColor prop + note; checking Custom sets playScript.overrideEnabled.value = true, Original sets false), Motion wiggle card (existing sliders; 'Reset defaults' link inside the section heading calling only playScript.resetDialogMotion()), summary bar rendering playScript.loopReadout.value verbatim with tabular-nums; fixed footer keeps progress + Cancel/Generate. Remove the InlineColorPicker import, mount, picker-open state, and pick-guard logic entirely. Wire Studio: add getBrushColor to the useRotoPlayScriptController ports (closure reading the current settings color, mirroring the getMotion pattern — use a ref if the hook captures ports once); add settings.color to the playScriptDialogPropsMemo deps and pass brushColor: settings.color; update MemoizedPhysicsPaintPlayScriptDialog props typing. CSS: inside the dialog scope only — two-column card grid (Mode/Motion wiggle/summary bar span both columns), card chrome using existing tokens (#ffffff background, #d8d4ca border, 6px radius), segmented-control styles shared by Mode and Color groups, Custom chip (data color, never accent) + hex + note, summary bar (12px secondary, tabular-nums, top border separation), heading-link style for 'Reset defaults'; REMOVE the now-dead picker-well/swatch-row rules inside the dialog scope; never modify pre-existing non-dialog rules. Overflow contract must hold with the grid (only the body scrolls; footer fixed). Run dialog + panel + controller suites until green; commit: feat(42-05): adopt approved playscript dialog revision — card grid and live brush-color control.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with cases (a)-(j) green and no panel/controller/renderer regressions
    - Dialog source contains no InlineColorPicker import or mount and no pick-guard state
    - Dialog source contains the exact strings 'Original colors', 'Custom color', 'Picked from the app's brush color panel', 'Frames per cycle', 'Motion wiggle', 'Reset defaults' and does not contain 'Save as defaults'
    - Studio passes brushColor via the dialog props memo and provides a getBrushColor port; no new store, event, or DOM query introduced
    - physicsPaintStudio.css diff touches only .physics-paint-play-script-* selectors (dead picker rules removed within that scope)
    - Full app suite (`pnpm --dir app exec vitest run`) + package animation tests + pnpm --dir app typecheck all exit 0
  </acceptance_criteria>
  <done>The dialog matches the approved proposal — card grid, live two-state color control, summary bar — with the picker fully removed and the cycle-only/loop-intent architecture intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → dialog inputs | Frames/Repeat text, slider values, color toggle |
| Studio settings → dialog/controller | live brush color read (read-only; single writer setBrushColor unchanged) |
| dialog → controller signals | all edits flow through the validated signal layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-05-01 | Tampering | getBrushColor port value | low | mitigate | Controller validates/normalizes the hex at confirm; malformed port value falls back to no-override (Original-colors behavior) |
| T-42-05-02 | Tampering | numeric text fields | medium | mitigate | Controller-side strict-regex validation (42-02) remains the single gate; dialog mirrors aria-invalid/disabled only |
| T-42-05-SC | Tampering | package installs | low | accept | No package installs in this plan (zero new dependencies) |
</threat_model>

## Artifacts this phase produces

This plan creates:
- Revised component `PhysicsPaintPlayScriptDialog`: D-16 card grid, color segmented control with live brush chip/hex, summary bar, 'Reset defaults' heading link; InlineColorPicker mount + pick-guard removed
- Revised controller `physicsPaintRotoPlayScriptController`: `getBrushColor` port resolution at confirm; `overrideColor` signal removed
- Rewritten `PhysicsPaintPlayScriptDialog.test.ts` + revised controller tests
- Studio wiring: `brushColor` prop + `getBrushColor` port
- CSS revisions confined to `.physics-paint-play-script-*` (card grid, summary bar, chip; dead picker rules removed)

<verification>
- Task gates: per-task `<automated>` commands exit 0
- Wave gate: `pnpm --dir app exec vitest run` + package animation tests (`NODE_PATH=app/node_modules app/node_modules/.bin/vitest --run --config /dev/null packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts`) + `pnpm --dir app typecheck` all exit 0
</verification>

<success_criteria>
- Dialog matches the approved proposal structure and live-color contract
- Inline picker and pick-guard fully removed from the active contract and tests
- Cycle-only generation and the 42-02 renderer contract preserved (renderer byte-untouched)
- Loop intent clarified: Repeat never multiplies frameCount, in code and copy
</success_criteria>

<output>
Create `.planning/phases/42-playscript-application-modes-color-override/42-05-SUMMARY.md` when done
</output>
