---
phase: 42-playscript-application-modes-color-override
plan: 03
type: execute
wave: 3
depends_on:
  - 42-02
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - PLAY-03
  - PLAY-04

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Mode selector is a two-option radiogroup (Progressive | Static / Hold) with roving tabindex, arrow-key navigation with wrap, aria-checked, and the helper line updating on selection (D-05, PLAY-03)"
    - "The single shared numeric field shows label 'Frames' in Progressive and 'Cycle frames' in Static / Hold — one field, no second input (D-03)"
    - "Color state defaults to 'Original colors'; opening the picker does NOT create an override — the override exists only after a deliberate user pick; an explicit control returns to Original colors (D-08, D-09, Pitfall 3 contract test; UI-SPEC E2)"
    - "Repeat field, Infinity toggle (disabled/greyed at 0.5 opacity with value preserved and restored via the controller setInfinity boundary), and the requested/effective readout render in the dialog with locked English copy (D-12, D-13; UI-SPEC E4 disabled)"
    - "Invalid Frames/Repeat input shows an inline error (#a12f37, 12px) directly under the field and confirm stays disabled while invalid — the Repeat field renders the controller repeatError channel with its two distinct copies ('Enter a positive integer.' format; 'Repeat is too large for this cycle length.' safe-product bound) (UI-SPEC E1/E3/E4 error contract)"
    - "Generation failure renders through the same approved inline-error style (#a12f37, 12px) from the controller error generation channel: progress bar hidden, inputs/actions re-enabled, dialog open, failure reason shown, retry or cancel allowed; normal user cancellation shows NO error (UI-SPEC E5 — the failure and field channels are never ambiguous)"
    - "Dialog Motion sliders edit controller dialog-Motion signals only; 'Reset to Motion defaults' calls ONLY the controller resetDialogMotion() operation (re-reads the current defaults through the controller); the dialog never accesses controller construction ports; no 'Save as defaults' control exists (D-06; UI-SPEC E3)"
    - "During generation all new controls disable under the existing canCancel rule and 'Cancel generation' replaces 'Cancel' (UI-SPEC E5 busy contract)"
    - "Dialog overflow contract holds: surface bounded to viewport/grid height, only the body scrolls, title/mode context and action row stay visible, no horizontal scroll, focused controls scroll into view with visible focus outlines (UI-SPEC E1 overflow, locked structural contract)"
    - "All new markup stays inside .physics-paint-play-script-dialog/-surface/-content with the physics-paint-play-script-* class prefix on the light surface — the Phase 36.14 constrained-dark-pane regression cannot return (D-04, Pitfall 6)"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (expanded)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts (new)
    - app/src/components/physic-paint/physicsPaintStudio.css (new rules inside the .physics-paint-play-script-* scope only)
  key_links:
    - "Dialog reads/writes only the controller interface exposed in 42-02 (mode, overrideColor/overrideEnabled, dialogMotion, repeatText/infinity via setInfinity, repeatError, loopReadout, resetDialogMotion, error as the generation channel) — zero direct store access and zero access to controller construction ports"
    - "InlineColorPicker mounts inline within the dialog content column — preserves the existing Tab focus trap and CSS isolation (RESEARCH Open Question 3)"
  prohibitions:
    - "Never write dialog Motion edits to updatePanelMotion, physicPaintStore, or per-stroke properties (D-06)"
    - "Never add a 'Save as defaults' action in Phase 42 (D-06)"
    - "Never display the term 'clip bloquant' or any French loop label in Phase 42 UI (locked terminology; French capsule labels are Phase 43)"
    - "Never render the picker as a popover/overlay outside the dialog content column (focus-trap and isolation regression risk)"
---

<objective>
Expand the Play Script confirmation dialog into the full application-options surface: mode segmented control, mode-dependent Frames/Cycle frames field, color override swatch with inline picker, application-time Motion sliders, and the Hold Loop block with requested/effective readout — all inside the verified isolated dialog scope.

Purpose: This is the user-facing half of PLAY-03/PLAY-04; every control is a thin projection of the 42-02 controller signals.
Output: Expanded dialog component + new dialog component test + scoped CSS.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md
@.planning/phases/42-playscript-application-modes-color-override/42-RESEARCH.md
@.planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md
@.planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md
@.planning/phases/42-playscript-application-modes-color-override/42-02-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
@app/src/components/sidebar/InlineColorPicker.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mode segmented control, mode-dependent frame field, and Hold Loop block</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (full file — dialog shell, focus-trap Tab query, signal-bound input pattern, existing Frames field markup)
    - app/src/components/physic-paint/physicsPaintStudio.css lines 1216-1309 (isolated dialog scope, light-surface tokens, input/error rules)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (42-02 interface: mode, repeatText, repeatError, setInfinity, loopReadout, validationError, canCancel)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Copywriting Contract, Interaction Contract, Spacing/Color/Typography tables)
    - .planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md (dialog section — segmented control has no in-repo analog; W3C APG radio pattern)
  </read_first>
  <behavior>
    - Radiogroup: role="radiogroup" containing two role="radio" children labeled exactly 'Progressive' and 'Static / Hold'; aria-checked reflects controller mode; checked option tabindex 0, unchecked -1; ArrowLeft/ArrowRight move focus AND check with wrap-around; one Tab stop for the group (integrates with the existing trap query)
    - Helper line directly below the group, linked via aria-describedby, exactly 'The drawing builds stroke by stroke across frames.' for Progressive and 'The complete drawing is applied to every cycle frame.' for Static / Hold, updating on selection
    - Frame field label text is 'Frames' when mode is progressive and 'Cycle frames' when static — the same single input element (D-03); help copy 'Enter a positive integer or Max.' unchanged
    - Repeat field + Infinity toggle: repeat input bound to controller repeatText with the same signal-bound input pattern, aria-invalid driven by the controller repeatError channel, and the error span rendering repeatError verbatim (copies locked in 42-02: 'Enter a positive integer.' or 'Repeat is too large for this cycle length.'); Infinity toggle beside it wired through playScript.setInfinity(...) so preserve/restore stays controller-owned; when Infinity is on the repeat input renders disabled at 0.5 opacity with its value intact and the readout shows the 'Cycle {N}f × ∞' form (D-12)
    - Loop readout line renders controller loopReadout.value verbatim (forms locked in D-13/UI-SPEC) using tabular-nums
    - All new controls disable when playScript.canCancel.value is true
  </behavior>
  <action>
    In PhysicsPaintPlayScriptDialog.tsx, add a mode block at the top of the dialog content: the radiogroup per the behavior contract (W3C APG radio pattern — no in-repo analog, markup is discretion within D-05 constraints) with a segmented-control visual treatment; write selection into playScript.mode.value on click and on arrow keys. Make the existing Frames label mode-dependent per D-03 (single input, label text switches). Add the Hold Loop block below: Repeat numeric input (inputMode numeric, aria-invalid bound to playScript.repeatError, aria-describedby to a help/error span pair mirroring the Frames field, the error span rendering playScript.repeatError.value verbatim when non-null), an Infinity toggle control with label 'Infinity' whose changes call playScript.setInfinity(enabled) — never direct infinity/lastFiniteRepeat signal manipulation — and the readout paragraph rendering loopReadout. All new elements use class names prefixed physics-paint-play-script- and sit inside the existing content column. In physicsPaintStudio.css, add rules ONLY inside the existing dialog scope: segmented track (#ffffff background, 6px radius, #a9afb7 border), checked segment in accent #365ed6, 2px focus-visible outlines with 2px offset, disabled repeat at opacity 0.5, readout with font-variant-numeric tabular-nums, and the overflow structure — surface max-height bounded to its grid/viewport container, ONLY .physics-paint-play-script-content scrolls vertically (overflow-y auto, overflow-x hidden), title and action row outside the scroll region, helper/validation text wraps. Typography strictly 16px/400 body, 12px/700 uppercase labels, 12px/400 helper; spacing from the UI-SPEC scale (8px inner gaps, 16px group rhythm, 32px between major sections; inherited 18px/12px surface/content gaps preserved). Commit: feat(42-03): add mode segmented control and hold loop block to play script dialog.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 (dialog test file is created in Task 2 — until then the command runs the controller suite; after Task 2 both must pass)
    - Dialog source contains role="radiogroup" and exactly two role="radio" children with aria-checked bindings
    - Dialog source binds the frame-field label to the controller mode signal with the exact strings 'Frames' and 'Cycle frames'
    - Dialog source binds the repeat field's aria-invalid and error span to the controller repeatError channel, and wires the Infinity toggle through playScript.setInfinity
    - Dialog source contains the exact helper strings 'The drawing builds stroke by stroke across frames.' and 'The complete drawing is applied to every cycle frame.'
    - physicsPaintStudio.css diff adds only selectors beginning with .physics-paint-play-script- and never modifies the pre-existing dialog/panel rules
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Dialog shows the mode selector with keyboard-accessible semantics, the mode-dependent frame field, and the full Hold Loop block with live readout — all projected from controller signals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Color override swatch + inline picker, Motion sliders, and the dialog component test (RED→GREEN)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (current state after Task 1)
    - app/src/components/sidebar/InlineColorPicker.tsx lines 1-80 (props contract color/opacity/onChange/onClose; mount-onChange behavior — isExternalUpdate starts false)
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx lines 99-130 (PanelSlider pattern + 0-100 clamp)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts (component-test harness analog — role-match per PATTERNS)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (overrideColor/overrideEnabled/dialogMotion signals, resetDialogMotion operation, error generation channel and its lifecycle, canCancel; D-06 reset semantics)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Copywriting Contract — Original colors, Reset to Motion defaults; Interaction Contract — picker pick semantics)
  </read_first>
  <behavior>
    - RED first — new dialog test cases: (a) radiogroup renders with correct roles and arrow-key navigation moves check with wrap; (b) default color state shows exactly 'Original colors'; (c) opening the picker does NOT set overrideEnabled — close without a deliberate pick leaves 'Original colors' (Pitfall 3 contract test); (d) a deliberate pick sets overrideEnabled and the swatch shows the picked hex; the explicit reset control returns to 'Original colors'; (e) frame-field label switches with mode; (f) loop readout renders the controller copy; (g) first-time static defaults render cycle 1 / repeat 1 / infinity off (D-15); (h) Motion sliders write controller dialogMotion and 'Reset to Motion defaults' calls the controller resetDialogMotion operation (asserted by spy — the dialog never reads a defaults port itself); no 'Save as defaults' text exists anywhere; (i) generation failure state (fake controller: phase 'failed', error populated, progress null, canCancel false) renders the failure reason through the shared inline-error element (#a12f37, 12px), shows no progress bar, and leaves inputs/actions enabled; (j) normal cancellation state (phase 'cancelled', error null) renders NO generation-error element while the dialog stays open
  </behavior>
  <action>
    RED: create app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts following the ScriptsPanel test analog; mount the dialog with a fake controller harness exposing the 42-02 interface (including resetDialogMotion, setInfinity, repeatError, error, appliedSummary); write cases (a)-(j); run and confirm red where markup is missing. GREEN, dialog: add the override row — a compact swatch button showing the text 'Original colors' by default or the picked '#rrggbb' swatch when overrideEnabled, plus an explicit 'Original colors' reset control visible when an override exists; clicking the swatch mounts InlineColorPicker INLINE inside the dialog content column (never a popover) with props color/opacity/onChange/onClose per its interface. Guard the picker's mount-time onChange: treat onChange as a genuine pick only after a real user interaction inside the picker (arm the handler on first pointerdown/keydown within the picker container, or drop the first invocation) so overrideEnabled flips solely on deliberate picks (D-09, Pitfall 3); on pick set controller overrideColor + overrideEnabled and close; onClose without interaction leaves state untouched. Add the Motion group: two sliders mirroring the PanelSlider pattern (0-100 range with the same clamp) labeled for deformation and position, writing playScript.dialogMotion.value — never updatePanelMotion — plus a 'Reset to Motion defaults' button whose onClick calls playScript.resetDialogMotion() and nothing else (the controller re-reads the current defaults; the dialog has no port access); 'Save as defaults' must not appear. Add the generation-error region: when playScript.error.value is non-null, render it verbatim in a dialog-level element using the existing shared inline-error class physics-paint-script-inline-error (#a12f37, 12px per the approved style) placed above the action row, visually distinct from the per-field error spans; render no progress bar unless playScript.progress.value is non-null; all controls follow the existing canCancel disabled rule so a failed generation (canCancel false, phase 'failed') re-enables inputs and actions. Extend CSS within the dialog scope only: swatch button shows the user-picked color as data (never accent), picker well #ffffff, 4px swatch gap, 8px repeat↔toggle gap, slider rows consistent with the dialog rhythm. Run the dialog + controller tests until green; commit: feat(42-03): add color override picker, application-time motion controls, and generation-error surface to play script dialog.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with all (a)-(j) dialog cases green and no regressions in the panel/controller suites
    - Dialog test contains the Pitfall 3 contract case: picker opened and closed without interaction leaves the color state at 'Original colors'
    - Dialog test asserts the failure state renders the error text inside an element with class physics-paint-script-inline-error, no progress element is rendered, and the Generate/Cancel controls are enabled; and asserts the cancelled state renders no generation-error element
    - Dialog test asserts the 'Reset to Motion defaults' click invokes the fake controller's resetDialogMotion spy exactly once
    - Dialog source renders InlineColorPicker inside the dialog content column markup (not in a portal/popover container)
    - Dialog source contains the exact strings 'Original colors' and 'Reset to Motion defaults' and does not contain 'Save as defaults'
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The dialog delivers the complete PLAY-03 option surface with a component test locking radiogroup semantics, pick-guard behavior, mode label switching, readout copy, first-time defaults, the controller-only reset boundary, and the E5 failure/cancellation rendering contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → dialog inputs | Frames/Repeat text, slider values, picked color |
| dialog → controller signals | all edits flow through the 42-02 validated signal layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-03-01 | Tampering | numeric text fields | medium | mitigate | Controller-side strict-regex validation (42-02) is the single gate; dialog only mirrors aria-invalid/disabled state — no dialog-side parsing |
| T-42-03-02 | Information disclosure | color/motion values in DOM | low | accept | Values are user's own session data rendered by Preact with framework escaping; no persistence (D-10) |
| T-42-03-SC | Tampering | package installs | low | accept | No package installs in this plan (RESEARCH: zero new dependencies) |
</threat_model>

## Artifacts this phase produces

This plan creates:
- Expanded component `PhysicsPaintPlayScriptDialog` (no new exports): mode radiogroup block, mode-dependent Frames/Cycle frames label, override swatch row + inline `InlineColorPicker` mount, Motion slider group + 'Reset to Motion defaults' action wired exclusively to the controller `resetDialogMotion()` operation, Hold Loop block (Repeat field bound to `repeatError`, Infinity toggle via `setInfinity`, loop readout), and the generation-error region rendering the controller `error` channel through the shared `physics-paint-script-inline-error` style (E5)
- New file `app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts` (component test)
- New CSS selectors in app/src/components/physic-paint/physicsPaintStudio.css, all under the `.physics-paint-play-script-*` prefix (segmented track/checked states, swatch row, picker well, loop block, readout, overflow structure)

<verification>
- Task gates: `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts` exits 0
- Wave gate: `pnpm --dir app exec vitest run` + package animation tests + `pnpm --dir app typecheck` all exit 0
</verification>

<success_criteria>
- Dialog exposes every PLAY-03/PLAY-04 control with locked copy and accessibility semantics
- Picker-open-never-overrides contract locked by test
- CSS isolation preserved: diff confined to .physics-paint-play-script-* selectors
</success_criteria>

<output>
Create `.planning/phases/42-playscript-application-modes-color-override/42-03-SUMMARY.md` when done
</output>
