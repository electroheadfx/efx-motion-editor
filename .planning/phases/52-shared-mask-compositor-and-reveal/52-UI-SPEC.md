---
phase: "52"
slug: "shared-mask-compositor-and-reveal"
status: approved
reviewed_at: "2026-09-02"
shadcn_initialized: false
preset: none
created: "2026-09-02"
---

# Phase 52 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn — custom CSS-variable design system + Tailwind CSS v4) |
| Preset | not applicable |
| Component library | none (project-local Preact components under `app/src/components/physic-paint/`) |
| Icon library | lucide-preact @ 0.577.0 |
| Font | Inter (system fallback `sans-serif`) |

**Source:** Detected from `app/package.json` (Preact + `@preact/signals`, `lucide-preact`, `tailwindcss@^4.0.0` via `@tailwindcss/vite`) and `app/src/index.css` (CSS-variable theme tokens). No `components.json` exists — the shadcn gate is not applicable. This phase is a **Preact** project; the `efx-preact-reactivity` skill governs all component/hook/signal work (no `useState`, signals only).

**Existing surface this phase extends (do not re-specify):** the reveal rail is the **4th rail kind** on the single track type, reusing the Loop Clip rail surface (`PhysicsPaintLoopClipRail.tsx`), the Loop Clip `overrideColor` mechanism (43-06), the Loop Clip Regenerate control pattern, the 20x4px lifecycle status dot, the status capsule (`PhysicsPaintWorkflowStrip.tsx` `PhysicsPaintWorkflowLiveStatus`), and the photo-reference modal (`PhysicsPaintPhotoReferenceDialog.tsx`).

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Rail segment height, rail drop from cell top, status-dot height |
| sm | 8px | Rail anchor/target height, compact element spacing |
| md | 16px | Default element spacing, dialog padding |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions (rail-surface geometry, carried from the locked Phase 43/47 surface — not part of the 8-point scale):

- Rail segment: **4px** tall, dropped **4px** from the cell top (`inset: 4px 0 auto 0`).
- Lifecycle status dot: **20px × 4px** rectangle, `top: 4px`, centered over the rail segment (`left: 50%; transform: translate(-50%, 0)`).
- Rail anchor/target: **8px** tall, `min-width: 12px`.
- Playhead bar: **2px** wide, full-height, `#A6D334`.
- Timeline cell width: **18px** (`ROTO_CELL_WIDTH_PX`).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 13px | 400 | 1.5 |
| Label | 12px | 400 | 1.4 |
| Heading | 16px | 600 | 1.2 |
| Display | 20px | 600 | 1.2 |

**Weights (exactly 2):** 400 (regular) + 600 (semibold). Font family: `Inter`, `sans-serif`.

**Source:** Inter is the locked project font (`app/src/index.css` `@layer base`). Rail tooltips, status-capsule text, and dialog labels use the small 12–13px range; dialog titles use 16px; the photo-reference dialog title uses the heading weight. These are the existing Studio sizes — no new type scale is introduced by this phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0F0F0F` | Studio root background (`--color-bg-root`) |
| Secondary (30%) | `#1A1A1A` | Cards, panels, sidebar, canvas area (`--color-bg-card` / `--color-bg-right`) |
| Accent (10%) | `#2D5BE3` | Primary interactive elements only (see reserved list) |
| Destructive | `#FF4444` / `#FF6666` | Destructive actions and fail-closed error text only |

Accent reserved for: active/selected control states, primary CTA buttons, focus/active fills, and the `paint-style-btn-active` state. **Never** for rail identity colors, status dots, or passive chrome.

**Rail identity colors (this phase — D-22, green family):**

| Rail | Base | Hover | Selection |
|------|------|-------|-----------|
| Reveal / Motion | `#10b981` (emerald-500) | `#6ee7b7` (emerald-300) | `#f59e0b` (orange, shared) |
| Reveal / Static | `#14b8a6` (teal-500) | `#5eead4` (teal-300) | `#f59e0b` (orange, shared) |

The reveal rail **inherits the Loop Clip `overrideColor` mechanism (43-06)** — the variant color is the default, overridable per rail. The base/hover shades mirror the existing Loop Clip convention (violet-500/cyan-500 base → violet-300/cyan-300 hover). Selection orange `#f59e0b` and unresolved red `#ff6b6b` are shared across all rail families.

**Lifecycle status dot (shared, 47 close-out palette — D-23):**

| State | Value |
|-------|-------|
| synchronized | `#a6d334` |
| modified | `#fbbf24` |
| detached | `#bbc0c8` |
| unavailable | `#ff2e56` |

**Source:** All values read from `app/src/index.css` (theme tokens) and `app/src/components/physic-paint/physicsPaintStudio.css` (rail segment, status dot, playhead). The reveal rail green family is the only new color introduced; emerald/teal shades are chosen to match the existing violet/cyan 500→300 pattern.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | **"Reveal with script…"** (photo-reference modal entry — verb "Reveal" + noun "script") |
| Empty state heading | "No source imported" (existing `PHOTO_REFERENCE_EMPTY_SOURCE` — gates reveal-rail creation) |
| Empty state body | "Import a photo reference, then Reveal with script… to bake the reveal." |
| Error state (missing reference) | "Reveal unavailable — no reference placed. Place a reference to replay." |
| Error state (deleted script) | "Reveal unavailable — script deleted. Re-link a script to replay." |
| Destructive confirmation | Replay overwrite / rail delete: **no confirmation dialog** — both are single undo-ledger entries (RVL-06) |

**Freshness tooltip lines (D-23 — the one line the Loop Clip tooltip does not have):**

| State | Copy |
|-------|------|
| Fresh | "baked from current script & reference" |
| Stale | "stale — script or reference changed since bake, Replay to refresh" |

**Replay disabled reason (D-24 — `regenerateDisabledReason`-style):**

| Condition | Copy |
|-----------|------|
| No placed reference | "Replay unavailable — no reference placed." |
| Script deleted | "Replay unavailable — script deleted." |

**Source:** CTA and freshness/disabled copy are locked by CONTEXT.md D-16/D-19/D-23/D-24. Error-state copy follows the existing fail-closed pattern (`"Regenerate unavailable — …"` from `physicsPaintLoopClipPresentation.ts` and `"Missing reference source — use Replace source to re-link."` from Phase 50). All copy is **English** (Phase 47 correction).

---

## UI Considerations

> State coverage for the reveal rail surface. Empty/error COPY lives in `## Copywriting Contract` above — this section covers state coverage and references those rows.

Applicable state considerations resolved: 7 covered, 1 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | reveal rail, photo-ref modal | ✅ covered | No empty-list surface for the rail: reveal rails are created via the modal "Reveal with script…" or the track rail-creation flow; creation IS the bake (D-11). The photo-reference "No source imported" empty state (heading + body) gates creation. |
| loading | reveal rail, modal, progress bar, Replay, picker | ✅ covered | Creation/Replay runs the existing PlayScript per-frame render with the existing onProgress bar (completed/total) — same as motion/static rail creation (D-11/D-24); no persistent "created but not baked" state. The SCRIPTS picker is a synchronous local library list (no in-flight state). |
| error | reveal rail, modal, progress bar, Replay, picker | ✅ covered | Fail-closed rejections (missing reference D-12, deleted script D-13) surface in the status capsule (`role="alert"`, red `TriangleAlert`) with the documented error copy; existing baked keys untouched. Replay shows a `regenerateDisabledReason`-style disabled reason ("Replay unavailable — …"). |
| populated | reveal rail | ✅ covered | Green rail line (emerald `#10b981` motion / teal `#14b8a6` static) + 20x4px lifecycle status dot + tooltip with script name, variant, span, repeat, and freshness line (D-22/D-23). |
| partial | reveal rail, photo-ref modal | ✅ covered | Stale reveal (script/reference changed since bake) → `modified` status dot (`#fbbf24`) + "stale — script or reference changed since bake, Replay to refresh" tooltip line (D-23). Modal partial (reference placed, no script chosen) → SCRIPTS kind-filtered picker shown, CTA disabled until a script is selected. |
| overflow | status capsule, rail surface | ✅ covered | Status-capsule error text truncates with ellipsis + full text in tooltip (already in capsule CSS: `overflow: hidden` / `text-overflow: ellipsis` / `nowrap`); the spec makes it explicit and adds the tooltip. Many rails overflow the track vertically; the existing timeline scrolls (Phase 43/47 rail surface). |
| zero-one-many | reveal rail | ✅ covered | Multiple reveal rails per track (successive spans) and across tracks; each is an independent rail+keys unit; tracks composite via the existing v1.0 compositor law (D-03/D-04). |
| long-text | tooltip / accessible name / capsule / picker | 🧪 backstop | Long library script names (arbitrary length, D-13) render in the tooltip, accessible name, status-capsule error text, and SCRIPTS picker. Capsule truncation is specified (ellipsis + tooltip); the tooltip/accessible-name long-name rendering is held out as a visual UI-state test (lifts as `{ statement, verification: backstop }`). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn initialized |
| third-party | none | not applicable — no third-party registries declared |

**Source:** No `components.json`; no shadcn; no third-party registries. The registry vetting gate is not applicable to this phase.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: FLAG (non-blocking — no focal point declared for the Studio surface)
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS
- [x] Dimension 7 Inventory Provenance: PASS

**Approval:** approved 2026-09-02
