---
phase: 52-shared-mask-compositor-and-reveal
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - app/src/efx-paint/document/efxPaintDocument.ts
  - app/src/efx-paint/document/efxPaintDocumentParsers.ts
  - app/src/stores/efxPaintStore.ts
  - app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts
  - app/src/efx-paint/document/efxPaintDocumentParsers.reveal.test.ts
autonomous: true
requirements: [RVL-05, RVL-06]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The PhotoReferenceMode flag (reference-only / reveal-source / masked-transform-source) is removed from the document schema, parser, store, and controller — no vestigial state (D-15)."
    - "A saved v1.0 document with a PhotoReferenceTrack parses and round-trips without a mode field (clean break, Phase 45 no-compat)."
    - "The reveal rail bakes the reference as placed regardless of any mode; the real guard is RVL-05 (photo pixels reach output only through reveal keys)."
  artifacts:
    - app/src/efx-paint/document/efxPaintDocument.ts
    - app/src/efx-paint/document/efxPaintDocumentParsers.ts
    - app/src/stores/efxPaintStore.ts
    - app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts
    - app/src/efx-paint/document/efxPaintDocumentParsers.reveal.test.ts
  key_links:
    - "PHOTO_REFERENCE_KEYS drops 'mode'; PHOTO_REFERENCE_MODES set is deleted; parsePhotoReferenceTrack no longer validates or returns mode."
    - "setPhotoReferenceMode and the PHOTO_REFERENCE_MODES const are removed from efxPaintStore.ts; the mode signal is removed from the controller."
  prohibitions:
    - "MUST NOT keep the PhotoReferenceMode flag as a vestigial semantic marker — it is removed entirely, not retained for compatibility (D-15)."
---

<objective>
Remove the `PhotoReferenceMode` flag entirely (D-15 clean break): the `reference-only` / `reveal-source` / `masked-transform-source` union, the `mode` field on `PhotoReferenceTrack`, the parser allowlist + validation, the `setPhotoReferenceMode` store mutation, and the mode signal in the photo-reference controller. The reveal rail bakes the reference as placed regardless of any mode; the real guard is RVL-05.

Purpose: Enact the clean-break schema change that inverts the Phase 50 D-06 exclusion into a bake-time guarantee, so no vestigial mode state survives into the reveal model.
Output: A mode-free `PhotoReferenceTrack` schema + parser + store + controller, with the round-trip test green.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-CONTEXT.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-RESEARCH.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-PATTERNS.md

@app/src/efx-paint/document/efxPaintDocument.ts
@app/src/efx-paint/document/efxPaintDocumentParsers.ts
@app/src/stores/efxPaintStore.ts
@app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts
</context>

<tasks>

<task type="auto">
  <name>Remove PhotoReferenceMode — schema, parser, store, controller (D-15)</name>
  <files>app/src/efx-paint/document/efxPaintDocument.ts, app/src/efx-paint/document/efxPaintDocumentParsers.ts, app/src/stores/efxPaintStore.ts, app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts</files>
  <action>
    Enact D-15 as a clean break (Phase 45 no-compat — no migration, no legacy shim):

    1. efxPaintDocument.ts: delete the `PhotoReferenceMode` type union (line ~103) and the `mode: PhotoReferenceMode` field on `PhotoReferenceTrack` (line ~129). No vestigial state.

    2. efxPaintDocumentParsers.ts: remove `'mode'` from `PHOTO_REFERENCE_KEYS` (line ~68), delete the `PHOTO_REFERENCE_MODES` set (line ~69), delete the `value.mode` validation in `parsePhotoReferenceTrack` (lines ~356-358), and remove `mode` from the returned `Object.freeze({ ... })` (line ~374). The parser stays fail-closed: unknown members still throw, no normalization, no ID allocation.

    3. efxPaintStore.ts: delete the `setPhotoReferenceMode` mutation (line ~1181) and the `PHOTO_REFERENCE_MODES` const (line ~1042). Remove `'set-photo-reference-mode'` from `BackgroundEditOperationKind` (line ~549). `setPhotoReferenceSource` and the display-preference setters (visible/opacity/transform/transformLocked) are unchanged.

    4. physicsPaintPhotoReferenceController.ts: remove the mode signal, `PHOTO_REFERENCE_MODE_OPTIONS`, `PHOTO_REFERENCE_MODE_HINT`, and any mode-routing port. The controller keeps the source/visibility/opacity/transform/lock ports.

    Do NOT keep the mode flag as a semantic marker (D-15), do NOT add a replacement field, do NOT add backward-compat parsing for old mode-bearing documents.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/efx-paint/document/efxPaintDocumentParsers.reveal.test.ts</automated>
  </verify>
  <done>The PhotoReferenceMode flag is gone from schema, parser, store, and controller; a mode-free PhotoReferenceTrack parses and round-trips; no vestigial mode state remains.</done>
  <reversibility rating="one-way">D-15 removes the `mode` field from the v1.0 document schema, breaking parsing of Phase 50-era v1.0 documents; acceptable under the clean-break no-compat contract (Phase 45). Already locked in CONTEXT.md — no checkpoint:decision task is inserted.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Mode-free PhotoReferenceTrack round-trip test (RVL-06 save/reopen)</name>
  <files>app/src/efx-paint/document/efxPaintDocumentParsers.reveal.test.ts</files>
  <behavior>
    - A PhotoReferenceTrack with no mode field parses successfully and round-trips byte-identically (RVL-06).
    - A PhotoReferenceTrack carrying a legacy mode field is rejected fail-closed (unknown member throws) — clean break, no normalization.
    - The reveal rail record (railKind 'reveal') round-trips through the physical-level parser without loss.
  </behavior>
  <action>
    Write the round-trip test asserting the mode-free PhotoReferenceTrack parses and round-trips, that a legacy mode-bearing record is rejected fail-closed, and that the reveal rail record round-trips. Write the failing tests first (RED), then confirm GREEN against the Task 1 changes.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/efx-paint/document/efxPaintDocumentParsers.reveal.test.ts</automated>
  </verify>
  <done>The round-trip test is green: mode-free PhotoReferenceTrack round-trips, legacy mode is rejected fail-closed, and the reveal rail record round-trips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved document → parser | A PhotoReferenceTrack (with or without a legacy mode field) crosses the fail-closed parser boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-04 | Tampering | efxPaintDocumentParsers parsePhotoReferenceTrack | medium | mitigate | Fail-closed allowlist: `PHOTO_REFERENCE_KEYS` drops `mode`; a legacy mode-bearing record throws (unknown member), never normalized or silently accepted. |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages are installed this phase. |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run app/src/efx-paint/document/efxPaintDocumentParsers.reveal.test.ts` green.
- `pnpm --filter efx-motion-editor exec vitest run` green at wave end.
</verification>

<success_criteria>
- The PhotoReferenceMode flag is removed from schema, parser, store, and controller.
- A mode-free PhotoReferenceTrack parses and round-trips; a legacy mode-bearing record is rejected fail-closed.
- The reveal rail record round-trips through the physical-level parser.
</success_criteria>

<output>
Create `.planning/phases/52-shared-mask-compositor-and-reveal/52-02-SUMMARY.md` when done
</output>
