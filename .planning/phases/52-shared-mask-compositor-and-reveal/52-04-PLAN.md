---
phase: 52-shared-mask-compositor-and-reveal
plan: 04
type: execute
wave: 3
depends_on: [52-01, 52-02]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceDialog.tsx
  - app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
autonomous: true
requirements: [RVL-01]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The photo-reference modal gains a 'Reveal with script…' entry, gated on a placed reference (D-12 creation guard, D-16/D-19)."
    - "Choosing the entry opens the SCRIPTS picker (unfiltered — D-26), derives the variant from the creation-time choice, and creates the reveal rail on the current track AND bakes it in one action with the existing onProgress bar (D-11)."
    - "The variant is fixed at creation; re-linking a reveal rail to a different script is unfiltered (D-21/D-26)."
    - "The track's normal rail-creation flow can also create a reveal rail (kind 'reveal', then the unfiltered SCRIPTS picker) — both creation paths share one model (D-19)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceDialog.tsx
    - app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  key_links:
    - "The 'Reveal with script…' button calls the create-reveal-rail mutation from Plan 01, which IS the first bake (D-11)."
    - "The track rail-creation flow calls the SAME create-reveal-rail mutation from Plan 01 — one model, two entry points (D-19)."
    - "The SCRIPTS picker is the existing library picker, unfiltered — scripts carry no kind field (D-26)."
  prohibitions:
    - "MUST NOT filter the SCRIPTS picker by a script kind field that does not exist — the variant is a creation-time rail property, not a script property (D-26)."
    - "MUST NOT implement the track rail-creation flow as a separate model from the modal path — both paths call the same create-reveal-rail mutation (D-19)."
---

<objective>
Deliver BOTH locked reveal-rail creation paths (D-19): (1) the photo-reference modal's "Reveal with script…" entry — a button gated on a placed reference that opens the existing (unfiltered) SCRIPTS picker, derives the variant from the creation-time choice, and creates the reveal rail on the current track AND bakes it in one action with the existing onProgress bar; and (2) the track's normal rail-creation flow — kind `reveal`, then the same unfiltered SCRIPTS picker, wired to the SAME create-reveal-rail mutation.

Purpose: Deliver the locked authoring entry points (D-16/D-19) that guarantee a placed reference and pre-fill the script, so creation IS the first bake (D-11) — one model, two entry points.
Output: Both the modal's "Reveal with script…" flow and the track rail-creation flow create and bake a reveal rail in one action.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-CONTEXT.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-RESEARCH.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-PATTERNS.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-UI-SPEC.md

@app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceDialog.tsx
@app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts
</context>

<tasks>

<task type="auto">
  <name>"Reveal with script…" button + creation guard (D-12/D-16/D-19)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceDialog.tsx, app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts</files>
  <action>
    Add the "Reveal with script…" primary CTA to the photo-reference modal's actions row (PhysicsPaintPhotoReferenceDialog.tsx, near the existing import/remove buttons). Gate it on `hasSource` (D-12 creation guard: a reveal rail cannot be created without a placed reference). The button is a thin render shell — the state machine lives in `usePhysicsPaintPhotoReferenceController` (signals only, no useState, no render-body signal writes). Add a controller port/action for the reveal-creation flow. Copy is exactly "Reveal with script…" (UI-SPEC Copywriting Contract). Follow the `efx-preact-reactivity` skill.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintPhotoReferenceDialog.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>The "Reveal with script…" button renders in the modal, gated on a placed reference; the controller exposes the reveal-creation action.</done>
  <reversibility rating="costly">D-16/D-19 (the "Reveal with script…" entry point and the reveal rail-creation flow) are new surface; changing the authoring story later touches the modal, the rail flow, and the bake.</reversibility>
</task>

<task type="auto">
  <name>Wire the creation flow — picker → variant → create + bake (D-11/D-26)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceDialog.tsx, app/src/components/physic-paint/view/physicsPaintPhotoReferenceController.ts</files>
  <action>
    Wire the "Reveal with script…" flow: open the existing SCRIPTS picker (D-10: script chosen at rail creation via the existing SCRIPTS picker, the rail references the library script by ID — never a frozen copy; UNFILTERED — scripts carry no kind field, D-26), let the user choose the variant (reveal/motion vs reveal/static) at creation time (D-26), then call the create-reveal-rail mutation from Plan 01 on the current track. The default rail span is the script's natural duration (D-20: the frame count the script covers at the current motion parameters, starting at the current playhead/cursor). Creation IS the first bake: the existing onProgress bar runs during the per-frame render, and the rail lands baked (D-11). The variant is fixed at creation and never changes after (D-21). Do NOT filter the picker by a non-existent script kind, do NOT introduce a persistent "created but not baked" state.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintPhotoReferenceDialog.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>The "Reveal with script…" flow opens the unfiltered picker, derives the variant, and creates + bakes the reveal rail in one action with the onProgress bar.</done>
  <reversibility rating="reversible">D-26 (variant is a creation-time rail property; the picker is unfiltered) — changing the picker to filter later only affects future rail creation.</reversibility>
</task>

<task type="auto">
  <name>Track rail-creation flow — reveal as the 4th rail kind (D-19)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</files>
  <action>
    Wire `reveal` as the 4th rail kind into the track's normal rail-creation flow (D-19), so a reveal rail can be created from the track — not only from the modal. Both paths call the SAME create-reveal-rail mutation from Plan 01 (one model, two entry points).

    1. Rail-kind option (PhysicsPaintWorkflowStrip.tsx / PhysicsPaintTrackRow.tsx): add `reveal` to the rail-creation entry points the track surface exposes (the same flow that creates a motion/static PlayScript rail today). Choosing kind `reveal` opens the existing SCRIPTS picker UNFILTERED (D-26 — scripts carry no kind field), lets the user choose the variant (reveal/motion vs reveal/static) at creation time (D-26), then calls the create-reveal-rail mutation from Plan 01 on the current track.

    2. Creation guard (D-12): the track flow is gated on a placed reference — a reveal rail cannot be created from the track without one (same guard as the modal path). Surface the disabled reason when no reference is placed.

    3. Creation IS the first bake (D-11): the existing onProgress bar runs during the per-frame render, and the rail lands baked. The variant is fixed at creation and never changes after (D-21). Default rail span = the script's natural duration starting at the current playhead/cursor (D-20).

    4. Presentation (physicsPaintWorkflowPresentation.test.ts): add a rail-kind classification/projection that recognizes `reveal` (so the track surface and rail set copy classify a reveal rail correctly), and unit-test it.

    Do NOT implement a separate reveal model for the track path (D-19 — one model), do NOT filter the picker by a non-existent script kind (D-26), do NOT introduce a persistent "created but not baked" state (D-11). Follow the Preact reactivity rules (signals only, no useState, no render-body signal writes).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>The track rail-creation flow offers `reveal` as a rail kind, gated on a placed reference, wired to the same create-reveal-rail mutation as the modal path; the reveal rail kind is classified correctly in the presentation projection.</done>
  <reversibility rating="costly">D-19 (both creation paths, one model) is new surface; changing the authoring story later touches the modal, the rail flow, and the bake.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal → store | The "Reveal with script…" flow triggers the create-reveal-rail mutation; a missing reference must gate it before any write |
| track rail-creation → store | The track flow triggers the SAME create-reveal-rail mutation; a missing reference must gate it before any write (D-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-06 | Denial of service | PhysicsPaintPhotoReferenceDialog reveal entry + track rail-creation flow | low | mitigate | Both creation paths are gated on a placed reference (D-12); the create-reveal-rail mutation fail-closes when `document.photoReference === null`. |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages are installed this phase. |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintPhotoReferenceDialog.test.ts` green.
- `pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` green.
- Native UAT (manual): place reference, paint, save script, run "Reveal with script…", verify the rail lands baked (RVL-01 modal flow); also create a reveal rail from the track flow and verify it lands baked (RVL-01 track flow).
</verification>

<success_criteria>
- The "Reveal with script…" entry is gated on a placed reference.
- The flow opens the unfiltered picker, derives the variant, and creates + bakes the reveal rail in one action.
- The track rail-creation flow offers `reveal` as a rail kind, gated on a placed reference, wired to the same create-reveal-rail mutation.
</success_criteria>

<output>
Create `.planning/phases/52-shared-mask-compositor-and-reveal/52-04-SUMMARY.md` when done
</output>
