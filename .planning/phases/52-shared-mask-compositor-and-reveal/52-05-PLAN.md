---
phase: 52-shared-mask-compositor-and-reveal
plan: 05
type: execute
wave: 3
depends_on: [52-01, 52-02]
files_modified:
  - app/src/efx-paint/compositor/efxPaintRevealLeakContract.test.ts
autonomous: true
requirements: [RVL-05]

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Photo reference pixels reach flattened output ONLY through keys written by the Reveal bake — the four raster surfaces (compositor, flattenedCache, previewRenderer, exportRenderer) contain no reference-input tokens (RVL-05, D-15 bake-time guarantee)."
    - "The leak contract extends the Phase 50 D-06 token allow-list scan to the new reveal bake path."
  artifacts:
    - app/src/efx-paint/compositor/efxPaintRevealLeakContract.test.ts
  key_links:
    - "The token allow-list scan covers the four raster surfaces; the reveal bake path must not introduce a reference-input token into any of them."
  prohibitions:
    - "MUST NOT let photo reference pixels reach flattened output through any path other than reveal keys — the bake-time guarantee is the only leak guard (RVL-05)."
---

<objective>
Author the RVL-05 leak contract: a token allow-list scan over the four raster surfaces (compositor, flattenedCache, previewRenderer, exportRenderer) asserting that no reference-input token appears in any of them, extended to the new reveal bake path.

Purpose: Enforce the bake-time guarantee (D-15) that photo pixels reach output only through reveal keys, closing Pitfall 2 (reference leak) and Pitfall 14 (reference leak into output) by construction.
Output: The leak contract test is green, proving the four raster surfaces carry no reference-input tokens.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-CONTEXT.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-RESEARCH.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-PATTERNS.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-VALIDATION.md

@app/src/efx-paint/compositor/efxPaintCompositor.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Reveal leak contract — token allow-list over the four raster surfaces (RVL-05)</name>
  <files>app/src/efx-paint/compositor/efxPaintRevealLeakContract.test.ts</files>
  <behavior>
    - The four raster surfaces (compositor, flattenedCache, previewRenderer, exportRenderer) contain no reference-input token (RVL-05).
    - The reveal bake path introduces no reference-input token into any of the four surfaces.
    - The scan is a token allow-list (grep-based) over the source of the four surfaces, mirroring the Phase 50 D-06 contract.
  </behavior>
  <action>
    Author the leak contract test as a token allow-list scan over the four raster surfaces, mirroring the existing Phase 50 D-06 contract (which already scans compositor, flattenedCache, previewRenderer, exportRenderer for reference-input tokens). Extend the scan to cover the new reveal bake path so the bake's reference read cannot leak a reference-input token into any raster surface. The test asserts the forbidden tokens are absent from the four surfaces' source. Write the failing test first (RED), then confirm GREEN against the Plan 01/02 implementation. Do NOT name the exact forbidden tokens in prose — the token list lives in the test file itself.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/efx-paint/compositor/efxPaintRevealLeakContract.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>The leak contract test is green: the four raster surfaces carry no reference-input token, and the reveal bake path introduces none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference registry → flattened output | Photo reference pixels must cross only through reveal keys; any other path is a leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-07 | Information disclosure | efxPaintCompositor / flattenedCache / previewRenderer / exportRenderer | high | mitigate | Token allow-list scan over the four raster surfaces asserts no reference-input token appears in any of them; the bake reads the reference verdict, never the composited preview (RVL-05, D-15). |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages are installed this phase. |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run app/src/efx-paint/compositor/efxPaintRevealLeakContract.test.ts` green.
- `pnpm --filter efx-motion-editor exec vitest run` green at wave end.
</verification>

<success_criteria>
- The leak contract test is green: the four raster surfaces carry no reference-input token.
- The reveal bake path introduces no reference-input token into any raster surface.
</success_criteria>

<output>
Create `.planning/phases/52-shared-mask-compositor-and-reveal/52-05-SUMMARY.md` when done
</output>
