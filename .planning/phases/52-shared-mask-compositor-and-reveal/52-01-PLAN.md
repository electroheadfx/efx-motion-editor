---
phase: 52-shared-mask-compositor-and-reveal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
  - app/src/stores/efxPaintStore.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoRevealBake.test.ts
  - app/src/stores/efxPaintStore.reveal.test.ts
autonomous: true
requirements: [RVL-01, RVL-02, RVL-03, RVL-04, RVL-06]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reveal rail (railKind 'reveal') can be created on a track; creation IS the first bake — the rail lands with baked keys over its span (D-01, D-11)."
    - "The bake produces keys that carry reference pixels where the script coverage is, transparent elsewhere: empty coverage reveals nothing, full coverage reveals the full reference, partial alpha produces soft edges (RVL-02 generation-time, D-17)."
    - "reveal/motion bakes progressive coverage (the reveal extends frame after frame); reveal/static bakes full coverage per frame (D-09, RVL-03)."
    - "Baked keys are ordinary track content — they appear in flattened output through the unchanged shared compositor (D-02, RVL-04)."
    - "Undo of create/replay/delete/span restores the prior document by reference, never raster-byte snapshots (RVL-06)."
    - "Replay is deterministic — running it twice on the same script + reference produces identical baked keys (D-05, D-11)."
    - "An interrupted or aborted bake writes no keys; the acknowledged physical-edit transaction revalidates the document revision before commit (D-11)."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
    - app/src/stores/efxPaintStore.ts
    - app/src/stores/physicPaintStore.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoRevealBake.test.ts
    - app/src/stores/efxPaintStore.reveal.test.ts
  key_links:
    - "The bake reads _resolveReferenceSourceImage (frame-aligned reference verdict) plus the reference transform, never the composited preview (Pitfall 2)."
    - "Baked keys flow through getFlattenedFrame into efxPaintCompositor unchanged (D-02)."
    - "The reveal rail's mode field maps to the variant (progressive/static); railKind discriminates reveal from playscript."
  prohibitions:
    - "MUST NOT present a stale reveal bake as fresh — the freshness state must honestly reflect whether the script or reference changed since the bake (D-23)."
    - "MUST NOT silently discard the user's authoring strokes on Replay without a recovery path — Replay is one undo-ledger entry that restores the prior keys (D-05)."
---

<objective>
Wire the reveal rail end-to-end as the 4th rail kind: a `PhysicPaintRotoLoopClip`-shaped record with a `railKind: 'reveal'` discriminator, a bake render function that applies the script coverage alpha as a `destination-in` mask over the reference (as placed, full opacity), a store mutation that creates the rail AND bakes it in one action, and undo-by-reference. Baked keys are ordinary track content that flows through the unchanged compositor into flattened output.

Purpose: Prove the bake-into-keys architecture (D-01) end-to-end on the thinnest vertical slice before any horizontal expansion — one reveal rail created, baked, visible in flattened output, and undone.
Output: The reveal rail model + bake + store mutation + undo, with the bake and undo tests green.
</objective>

<assumption_delta_decision>
Detector fired on the pluralization signal "the reveal rail is the 4th rail kind (alongside paint, motion, static)". Decision: PROMOTE/ADD-ALONGSIDE — the rail-kind identity is generalized, not replaced. The single track type's rail-kind union becomes `paint | motion | static | reveal`; the reveal rail is a NEW member of the existing `PhysicPaintRotoLoopClip` record family, discriminated by an optional `railKind?: 'playscript' | 'reveal'` field (defaulting to `'playscript'` for existing records — no migration, no new track type, no new record type). The `mode` field (`progressive`/`static`) is reused for the reveal variant. This is the identity-model generalization the detector flagged; it is recorded here so the executor does not invent a separate reveal record type or a second track type.
</assumption_delta_decision>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-CONTEXT.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-RESEARCH.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-PATTERNS.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-VALIDATION.md

@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
@app/src/stores/efxPaintStore.ts
@app/src/stores/physicPaintStore.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end reveal rail — create + bake + flattened + undo, one path only</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts, app/src/stores/efxPaintStore.ts, app/src/stores/physicPaintStore.ts</files>
  <action>
    Wire ONE reveal rail through every layer, per D-01/D-03/D-11. No other call sites, no batching, real error handling on the single path.

    1. Model (physicsPaintRotoPhysicalModel.ts): add an optional discriminator `railKind?: 'playscript' | 'reveal'` to `PhysicPaintRotoLoopClip` (line ~273). Existing records omit it and are treated as `'playscript'` — no migration, no default serialization change. Extend `isPhysicPaintRotoLoopClip` (line ~621) and `parsePhysicPaintRotoLoopClips` (line ~689) to accept the optional `railKind` with a fail-closed allowlist (`'playscript' | 'reveal'`; unknown value throws; absent = `'playscript'`). The `mode` field (`progressive`/`static`) is reused as the reveal variant (D-09). The `repeat` field is inherited as-is (D-08: repeat/endless applies to the rail span Loop-Clip-like; repeats are derived at read time from the baked source cycle, never re-baked, never written).

    2. Bake (physicsPaintRotoPlayScriptRenderer.ts): add a sibling render function (do NOT modify `renderRotoPlayScriptFrames`) that reuses the existing coverage path — `buildProgressiveStrokeSchedule`/`buildStaticStrokeSchedule` → `getProgressiveFrameStrokes`/`getStaticFrameStrokes` → `engine.renderProgressiveAlphaFrame(frameStrokes)` — but REPLACES the `mergeRotoAlphaCanvases` step with a reference-mask composite: draw the reference image AS PLACED on the canvas (D-14: Phase 50 position/scale/rotation/adjustments, never raw source bytes) at FULL source opacity (D-18, using the `getReferenceBounds` transform) then apply the coverage alpha as `globalCompositeOperation = 'destination-in'`, producing reference pixels where coverage is and transparency elsewhere (D-17). Encode via the existing `encodeRotoFrameFromCanvas` (straight-alpha — Pitfall 1). Reuse the existing abort/`throwIfAborted`/`validateRenderInput`/`MAX_AGGREGATE_RGBA_BYTES` shape.

    3. Store (efxPaintStore.ts): add a create-reveal-rail mutation that (a) fail-closes when `document.photoReference === null` (D-12 creation guard), (b) builds the immutable next document with the new `PhysicPaintRotoLoopClip` (railKind 'reveal', mode from the chosen variant, scriptId, sourceKeyIds = the baked key ids), (c) bumps `documentRevision`, (d) records a `BackgroundEditDescriptor` with a new `'reveal-create'` operation kind and `before`/`after` by reference. Add the `'reveal-create'` (and reserve `'reveal-replay'`, `'reveal-delete'`, `'reveal-span'`) members to `BackgroundEditOperationKind` (line ~549). Replay stays fail-closed when the library script was deleted (D-13): status-capsule red warning, existing baked keys untouched, no silent re-bake.

    4. Store (physicPaintStore.ts): add the bake commit path that reads the reference via `_resolveReferenceSourceImage` (line ~1224, frame-aligned, null-on-missing → fail-closed) and the reference transform, runs the bake function, and commits the resulting `PhysicPaintRotoRealKeyRecord`s through the existing acknowledged physical-edit transaction (revalidates revision before commit). Wire the create-reveal-rail mutation to call this commit so creation IS the first bake (D-11).

    Do NOT add a runtime mask compositor (D-01), do NOT add a second compositor path (D-02), do NOT read the composited preview as the bake source (Pitfall 2).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoRevealBake.test.ts app/src/stores/efxPaintStore.reveal.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>The single happy path works end-to-end: a reveal rail is created (with a placed reference), baked into real keys, visible in flattened output, and undone by reference — all committed.</done>
  <reversibility rating="one-way">D-01 (bake-into-keys, no runtime mask) and D-03 (4th rail kind on the single track type) define what the flattened output and saved document contain; reverting to a runtime compositor or a separate track type later is a different product and invalidates saved reveals. Already locked in CONTEXT.md — no checkpoint:decision task is inserted.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Bake mask semantics — empty/full/partial/eraser + progressive vs static (RVL-02/RVL-03)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts, app/src/components/physic-paint/roto/physicsPaintRotoRevealBake.test.ts</files>
  <behavior>
    - Empty coverage (no strokes on a frame) bakes a fully transparent key — reveals nothing (RVL-02).
    - Full coverage bakes the full reference image at that frame (RVL-02).
    - Partial coverage bakes soft edges — reference pixels only where coverage alpha is non-zero, straight-alpha encoded (RVL-02, Pitfall 1).
    - reveal/motion bakes progressive coverage: coverage extends frame after frame across the span (RVL-03).
    - reveal/static bakes full coverage per frame: every frame carries the entire revealed photo (RVL-03).
    - The eraser is a normal key eraser afterward — it removes baked coverage like any track key (RVL-02).
  </behavior>
  <action>
    Implement the mask semantics in the reveal bake function from Task 1 so each behavior above holds. The `destination-in` composite must not premultiply alpha — encode straight-alpha via `encodeRotoFrameFromCanvas`. The progressive vs static schedule selection reuses the existing `mode` branch (progressive → `buildProgressiveStrokeSchedule`, static → `buildStaticStrokeSchedule`). Write the failing tests first (RED), then implement (GREEN), then refactor if needed.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoRevealBake.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>All six behaviors pass in physicsPaintRotoRevealBake.test.ts; the bake produces straight-alpha keys with correct empty/full/partial/eraser and progressive/static semantics.</done>
  <reversibility rating="costly">D-17 (baked keys contain the revealed photo only, transparent elsewhere) decides which pixels survive a bake; changing it later re-validates the undo semantics.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Reveal undo-by-reference — create/replay/delete/span (RVL-06)</name>
  <files>app/src/stores/efxPaintStore.ts, app/src/stores/efxPaintStore.reveal.test.ts</files>
  <behavior>
    - Create reveal rail is one undo-ledger entry; undo restores the pre-rail document, redo re-applies it.
    - Replay (overwrite) is one undo-ledger entry; undo restores the prior baked keys including any hand edits (D-05).
    - Delete reveal rail is one undo-ledger entry; undo restores the whole rail + keys unit (D-06).
    - Span shrink (shorten deletes outside keys) is one undo-ledger entry; undo recovers the deleted keys (D-07).
    - Undo/redo operate by reference (before/after document objects), never raster-byte snapshots (RVL-06).
  </behavior>
  <action>
    Implement the reveal mutation undo semantics in efxPaintStore.ts: each committed reveal mutation (create/replay/delete/span-shrink) emits a `BackgroundEditDescriptor` with a distinct `'reveal-*'` operation kind and `before`/`after` by reference. Replay overwrites every baked key in the span (D-05); delete removes the rail + keys as one unit (D-06); span shrink deletes keys now outside the span (D-07). Write the failing tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/stores/efxPaintStore.reveal.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>All five undo behaviors pass in efxPaintStore.reveal.test.ts; every reveal mutation is a single by-reference undo-ledger entry.</done>
  <reversibility rating="costly">D-05 (replay overwrites the whole span) changes which pixels survive a replay and requires re-validating the undo semantics if changed later.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved document → parser | A malformed reveal rail record in a saved `.mce` document crosses the fail-closed parser boundary |
| reference registry → bake | The frame-aligned reference verdict feeds the bake; a missing reference must fail closed, not bake garbage |
| bake → physical commit | The bake writes keys through the acknowledged physical-edit transaction; a stale document must not be overwritten |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Information disclosure | reveal bake / compositor | high | mitigate | The bake reads `_resolveReferenceSourceImage` + the reference transform, never the composited preview; photo pixels reach output only through reveal keys (RVL-05). Token allow-list scan lands in Plan 05. |
| T-52-02 | Tampering | physicPaintStore bake commit | high | mitigate | The bake commits through the existing acknowledged physical-edit transaction, which revalidates the document revision before any write (TRK-05). |
| T-52-03 | Tampering | physicsPaintRotoPhysicalModel parser | medium | mitigate | `railKind` is a fail-closed allowlist (`'playscript' | 'reveal'`); unknown values throw, absent defaults to `'playscript'`, no normalization, no ID allocation. |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages are installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoRevealBake.test.ts app/src/stores/efxPaintStore.reveal.test.ts` green.
- `pnpm --filter efx-motion-editor exec vitest run` green at wave end (no regressions in the existing suite).
</verification>

<success_criteria>
- A reveal rail (railKind 'reveal') is created on a track with a placed reference; creation IS the first bake.
- Baked keys carry reference pixels where coverage is, transparent elsewhere, straight-alpha encoded.
- Baked keys appear in flattened output through the unchanged compositor.
- Undo of create/replay/delete/span restores the prior document by reference.
- The bake and undo tests are green and committed.
</success_criteria>

<output>
Create `.planning/phases/52-shared-mask-compositor-and-reveal/52-01-SUMMARY.md` when done
</output>
