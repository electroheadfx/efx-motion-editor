---
phase: 52-shared-mask-compositor-and-reveal
plan: 03
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
autonomous: true
requirements: [RVL-04]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reveal rail renders with the green-family line color: emerald #10b981 for reveal/motion, teal #14b8a6 for reveal/static, inheriting the Loop Clip overrideColor mechanism (D-22)."
    - "The reveal rail carries the 20x4px lifecycle status dot and a tooltip with the Loop Clip facts plus a freshness line: 'baked from current script & reference' vs 'stale — script or reference changed since bake, Replay to refresh' (D-23)."
    - "The reveal rail reuses the Loop Clip Regenerate control for Replay, with a regenerateDisabledReason-style disabled reason when it cannot run (no placed reference / script deleted) (D-24)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  key_links:
    - "The freshness line is appended to presentation.tooltipLines after the Status line — same tooltipLines array, no new rendering path (D-23)."
    - "The variant color is the default overrideColor, overridable per rail via the existing 43-06 mechanism (D-22)."
  prohibitions:
    - "MUST NOT present a stale reveal bake as fresh — the freshness state must honestly reflect whether the script or reference changed since the bake (D-23)."
---

<objective>
Extend the Loop Clip rail surface to render the reveal rail: green-family line color (emerald motion / teal static) via the existing `overrideColor` mechanism, the 20x4px lifecycle status dot, a tooltip freshness line, and the Replay control reusing the Loop Clip Regenerate pattern with a disabled reason.

Purpose: Give the reveal rail its locked visual identity and Replay affordance (D-22/D-23/D-24) on the existing rail surface, without a second color system or a new custom button.
Output: The reveal rail renders with the correct color, status dot, tooltip freshness, and Replay control.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-CONTEXT.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-RESEARCH.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-PATTERNS.md
@.planning/phases/52-shared-mask-compositor-and-reveal/52-UI-SPEC.md

@app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
@app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
</context>

<tasks>

<task type="auto">
  <name>Reveal rail color + status dot + tooltip freshness (D-22/D-23)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts</files>
  <action>
    Extend the Loop Clip rail surface for the reveal rail (railKind 'reveal'):

    1. Color (D-22): the reveal rail inherits the Loop Clip `overrideColor` mechanism (43-06) — the variant color is the DEFAULT overrideColor, overridable per rail. Default emerald `#10b981` for reveal/motion, teal `#14b8a6` for reveal/static (hover shades emerald-300 `#6ee7b7` / teal-300 `#5eead4` per the UI-SPEC). Do NOT introduce a second color system.

    2. Status dot (D-23): the reveal rail carries the existing 20x4px lifecycle status dot (synchronized/modified/detached/unavailable) — reuse the existing `presentation.synchronizationDot` path, no new dot.

    3. Tooltip freshness (D-23): append a freshness line to `presentation.tooltipLines` AFTER the `Status:` line — "baked from current script & reference" when fresh, "stale — script or reference changed since bake, Replay to refresh" when stale. The freshness derives from the rail's lifecycle (script/reference changed since bake → `modified`). Re-authoring is edit-the-script-then-Replay (D-25): the library script IS the saved coverage, and the freshness state flags it stale after a script edit — no re-painting on the track. Same `tooltipLines` array, same accessible-name inclusion, no new rendering path.

    4. Drag (D-04): the reveal rail reuses the existing rail drag machinery — dragging it in time or across tracks TRANSLATES the rail + baked keys as a unit to the destination track, no re-bake on drag; refresh only via explicit Replay. No new drag code.

    Follow the Preact reactivity rules (signals only, no useState, no render-body signal writes) and the `efx-preact-reactivity` skill.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts</automated>
  </verify>
  <done>The reveal rail renders with the green-family color, the lifecycle status dot, and the tooltip freshness line; the presentation projection is unit-tested.</done>
</task>

<task type="auto">
  <name>Replay control reusing the Loop Clip Regenerate pattern (D-24)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts</files>
  <action>
    Reuse the Loop Clip Regenerate control for the reveal rail's Replay (D-24): same rail placement, same interaction, no new custom button. Add a `regenerateDisabledReason`-style disabled reason for the reveal rail when it cannot run:
    - no placed reference → "Replay unavailable — no reference placed."
    - script deleted → "Replay unavailable — script deleted." (D-13: deleted library script → fail-closed, existing baked keys untouched, no silent re-bake)

    The red unresolved state stays EXCLUSIVELY for the fail-closed cases (reference removed after creation / script deleted) — never for a normal pending state (D-24). The disabled reason is computed in `physicsPaintLoopClipPresentation.ts` (mirroring `regenerateDisabledReasonFor`) and surfaced on the rail target. Follow the Preact reactivity rules.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts</automated>
  </verify>
  <done>The reveal rail's Replay reuses the Regenerate control with the correct disabled reason for missing reference / deleted script; no new custom button exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rail surface → store | The Replay control triggers a store mutation; a disabled reason must gate it before any write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-05 | Denial of service | PhysicsPaintLoopClipRail Replay control | low | mitigate | The Replay control is disabled with a `regenerateDisabledReason`-style reason when no reference is placed or the script is deleted; the red unresolved state is reserved for fail-closed cases only (D-24). |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages are installed this phase. |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts` green.
- Native UAT (manual): reveal rail shows green-family color, 20x4px status dot, and the freshness tooltip line (RVL-04 visual look).
</verification>

<success_criteria>
- The reveal rail renders with emerald/teal color, the lifecycle status dot, and the tooltip freshness line.
- The Replay control reuses the Regenerate pattern with the correct disabled reason.
</success_criteria>

<output>
Create `.planning/phases/52-shared-mask-compositor-and-reveal/52-03-SUMMARY.md` when done
</output>
