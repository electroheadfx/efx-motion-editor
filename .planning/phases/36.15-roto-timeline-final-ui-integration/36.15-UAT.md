---
status: testing
phase: 36.15-roto-timeline-final-ui-integration
source: [36.15-VERIFICATION.md]
started: 2026-07-26T00:40:00Z
updated: 2026-07-26T11:45:00Z
---

## Current Test

number: 1
name: Final strip presentation + Tools dropdown + + Key
expected: |
  Strip matches the approved 36.15 UI-SPEC reference; bands never grow/wrap; Tools dropdown opens inside the strip without clipping (interpolation/playback/apply-spacing pills intact inside); + Key promotes the current frame to a green empty real key that is paintable; occupied-frame and in-flight cases show guarded reduced-emphasis state with the two-part tooltip reason.
awaiting: user response

## Tests

### 1. Final strip presentation + Tools dropdown + + Key
Launch the Physics Paint Roto studio with real Roto keys present and inspect the final strip: fixed 155px five-band geometry (header / ruler / lane / action row / scrollbar), header order navigation pill -> status capsule -> + Key -> Duplicate -> Tools dropdown -> Close, layer name in the bottom action row before the Key {n} chip, enlarged action-row icons, no dead space, horizontal scroll below minimum host width.
expected: Strip matches the approved 36.15 UI-SPEC reference; bands never grow/wrap; Tools dropdown opens inside the strip without clipping (interpolation/playback/apply-spacing pills intact inside); + Key promotes the current frame to a green empty real key that is paintable; occupied-frame and in-flight cases show guarded reduced-emphasis state with the two-part tooltip reason.
result: [passed — native UAT 2026-07-26]

### 2. Styled tooltip behavior and layering
Hover and keyboard-focus guarded actions across the icon row, Tools dropdown pills, status capsule, timeline cells, and the Scripts toolbar Discard Script button.
expected: Styled tooltip appears only after exactly 1000ms hover, instantly on keyboard focus, dismisses on leave/blur/Escape/activation; unavailable actions stay visible/focusable with '{Action} — unavailable: {reason}' copy; tooltip renders above studio chrome (z-index 60); no native tooltip double-fires.
result: [passed — native UAT 2026-07-26]

### 3. Canvas Roto key diamonds
In the main EFX Motion timeline, inspect a physic-paint FX layer row with many real Roto keys, including a layer with nonzero inFrame.
expected: Small orange (#F5A623) diamonds render at each real key's physical frame, always visible, generated in-betweens get no marker, no overlap with adjacent layer content, no interaction surface.
result: [passed — native UAT 2026-07-26]

### 4. Selection guard non-interference
Exercise the app shell: select/copy LOG tab messages, edit fps/N inputs and any contenteditable field, paint on canvas, drag timeline keys and the strip scrollbar.
expected: LOG text and inputs select/edit natively; painting, dragging, pointer capture, and keyboard focus are unaffected by the index.html selection guard.
result: [passed — native UAT 2026-07-26, "all work congrats"]

## Summary

total: 4
passed: 4
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

### Gap A — Strip control regrouping (remove Tools dropdown)
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-08 — awaiting native UAT re-run)
detail: |
  Remove the Tools dropdown button entirely. Top bar final order: playback pill · fps/auto pill · status capsule (Log) · interpolation pill · Close. + Key and Duplicate move back from the top bar to the bottom action row before Insert. Set Key Space moves from the top dropdown to the bottom action row after Delete Key. The interpolation pill becomes a dropdown offering "Frame duplicate" / "Frame blend".
gap_plan: 36.15-08

### Gap B — Header control masked by canvas (z-index)
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-08 — awaiting native UAT re-run)
detail: |
  The interpolation "Duplicate" pill renders with too-low z-index and is masked by the canvas above the strip. No strip control or open dropdown may be clipped/masked by the canvas or the strip overflow container.
gap_plan: 36.15-08

### Gap C — Copy/Apply script relocation to Scripts sidebar
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-08 — awaiting native UAT re-run)
detail: |
  Move Copy Script and Apply Script from the bottom timeline action row into the Scripts right-sidebar tab toolbar, before the clear-buffer (clipboard-x) icon, forming a proper second icon row styled like the first. Rename the clear-buffer action's strange name — the user calls it "clear script from buffer".
gap_plan: 36.15-08

### Gap D — Bottom action row labels, tooltips, icon size
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-08 — awaiting native UAT re-run)
detail: |
  Add a short text name after each icon in the bottom action row. Rename tooltips to drop the tool-name prefix (tooltip shows only description / "unavailable: {reason}"). Increase the bottom-row icon size.
gap_plan: 36.15-08

### Gap E — Cosmetic polish round 2
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-09 — awaiting final native UAT approval)
detail: |
  1. Scripts sidebar second-row icons need short name labels like the bottom row. 2. Remove the doubled circle background at the right edge of the APPLY button (Set Key Space form); rename its label "Space" → "Key spacing". 3. Selected key cell's orange border is clipped on the right edge (z-index/overlap with next cell) — the full selection outline must render. 4. Interpolation dropdown option "Frame blend" → "Frame blending".
gap_plan: 36.15-09

### Gap F — Bottom-row grouping + casing polish
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-10 — awaiting final native UAT approval)
detail: |
  1. Scripts sidebar second-row labels in lowercase ("Copy, Apply, Clear") and icons enlarged to match the first-row icon size. 2. Bottom action row becomes three visually separated groups (spaced like the top bar): [Layer name + Key number] · [Tools: Key, Duplicate, Insert, Copy, Paste, Delete] · [Key spacing form]. 3. Bottom-row tool labels in lowercase ("Key, Duplicate, Insert, Copy, Paste, Delete"). 4. Layer name + Key number pill is its own group, not fused with the Tools group. 5. "APPLY" → "Apply".
gap_plan: 36.15-10

### Gap G — Sidebar tabs restructure + strip polish round 3
status: fixed
reported: 2026-07-26
fixed: 2026-07-26 (plan 36.15-11 — awaiting final native UAT approval)
detail: |
  1. Scripts sidebar second-row labels render truncated ("C…", "A…", "Cl…") — the full short names must show. 2. Remove the background block around the tools group (.physics-paint-roto-key-utilities). 3. Vertically center all bottom-row groups in the band. 4. Scripts tab moves to first position in its tab group and is default-open. 5. Add vertical separation between the two Scripts toolbar icon rows. 6. Right sidebar becomes three tab groups: [Brush color] (LOG tab removed — obsolete), [Tool] (independent tab; remove 'Save state' / 'Load state' buttons), [Scripts, Onion, Motion].
gap_plan: 36.15-11

