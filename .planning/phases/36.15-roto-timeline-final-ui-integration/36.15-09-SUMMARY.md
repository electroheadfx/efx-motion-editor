---
phase: 36.15-roto-timeline-final-ui-integration
plan: 09
subsystem: ui
tags: [preact, cosmetic-polish, gap-closure, roto-timeline, scripts-panel, css-layering, tdd]

requires:
  - phase: 36.15-roto-timeline-final-ui-integration
    plan: 08
    provides: labeled bottom action row, Frame blend dropdown option, guarded Set Key Space form, Scripts second toolbar row
provides:
  - Short visible labels (Copy / Apply / Clear) on the Scripts toolbar second-row guarded icons (UAT Gap E-1)
  - Apply submit without the doubled right-edge ring (4px rounded rect replacing the 999px pill cap) and the 'Key spacing' form label (UAT Gap E-2)
  - Selected key cell renders its full orange selection border on all four sides via selected-state z-index (UAT Gap E-3)
  - Interpolation dropdown option 'Frame blending' replacing 'Frame blend' (UAT Gap E-4)
affects: [phase-level native visual UAT final approval pass on Gap E]

tech-stack:
  added: []
  patterns:
    - "Selected-cell layering fix: z-index on .physics-paint-roto-cell.current lifts the outline above the abutting right neighbor in the zero-gap 18px grid — no geometry change"
    - "De-ringed submit: a 999px pill cap nested inside a 999px pill form renders as two concentric arcs; the Apply submit is now a 4px rounded rect like the adjacent number input"
    - "Toolbar label reuse: the shared .physics-paint-roto-key-icon-label class labels the Scripts second-row guarded icons with a 2px gap/padding toolbar rule"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintStudio.css

key-decisions:
  - "The doubled APPLY ring was the button's own 999px pill cap echoing the form's 999px pill border; the fix restyles the submit as a 4px rounded rect (matching the adjacent number input) rather than touching the form pill"
  - "The clipped selection border was a paint-order issue in the zero-gap abutting grid — the next cell painted over the selected cell's right outline edge; z-index: 1 on .current is the whole fix, with the 18px pitch and 155px band geometry untouched"
  - "Second-row labels reuse the bottom row's shared label class and read Copy / Apply / Clear — one-word names consistent with the bottom action row grammar"

requirements-completed: [36.15-GROUP-SEPARATION, 36.15-ICON-ACTIONS]

coverage:
  - id: E1
    description: "Scripts sidebar second-row icons (Copy Script, Apply Script, Clear Script Buffer) each carry a short visible name label matching the bottom action row labeling style"
    requirement: 36.15-ICON-ACTIONS
    verification:
      - kind: unit
        ref: "PhysicsPaintScriptsPanel.test.ts#second-row label contract (36.15-09, UAT Gap E-1) — 2 cases"
        status: pass
    human_judgment: false
  - id: E2
    description: "Set Key Space APPLY button shows no doubled circle/ring at its right edge; bottom-row label reads 'Key spacing'"
    requirement: 36.15-ICON-ACTIONS
    verification:
      - kind: unit
        ref: "PhysicsPaintWorkflowStrip.test.ts#Gap E cosmetic contract — Key spacing label + 999px-radius removal cases"
        status: pass
    human_judgment: false
  - id: E3
    description: "Selected key cell renders the full orange selection border on all four sides (z-index layering fix; 18px pitch and band geometry unchanged)"
    requirement: 36.15-GROUP-SEPARATION
    verification:
      - kind: unit
        ref: "PhysicsPaintWorkflowStrip.test.ts#Gap E cosmetic contract — selected-cell z-index case; Plan 06 pitch/band guardrails stay green"
        status: pass
    human_judgment: false
  - id: E4
    description: "Interpolation dropdown option reads 'Frame blending' ('Frame duplicate' unchanged)"
    requirement: 36.15-GROUP-SEPARATION
    verification:
      - kind: unit
        ref: "PhysicsPaintWorkflowStrip.test.ts#interpolation dropdown option contract (updated)"
        status: pass
    human_judgment: false
  - id: E5
    description: "Rendered appearance of the four Gap E fixes matches the user's annotated screenshots"
    requirement: 36.15-ICON-ACTIONS
    verification: []
    human_judgment: true
    rationale: "Gap closure was specified against annotated native-UAT screenshots; source-contract tests prove structure and CSS hooks, not rendered pixels — the user runs the final native visual approval pass"

metrics:
  duration: 15min
  completed: 2026-07-26
status: complete
---

# Phase 36.15 Plan 09: UAT Gap E Closure — Sidebar Labels, APPLY Ring, Selection Border, Frame Blending Summary

**Final cosmetic polish closing all four 2026-07-26 second-pass native-UAT items: labeled Scripts-toolbar second-row icons, the doubled ring removed from the Set Key Space APPLY button with its label renamed 'Key spacing', the selected key cell's orange border unclipped on all four sides, and the interpolation option renamed 'Frame blending'.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-26T07:18:43Z
- **Completed:** 2026-07-26T07:33:52Z
- **Tasks:** 1 (RED contract tests → GREEN implementation)
- **Files modified:** 5 (0 created)

## Accomplishments

- **Task 1 — Gap E cosmetic polish:**
  - **E-1 (sidebar labels):** the three guarded Scripts-toolbar clipboard actions (Copy Script, Apply Script, Clear Script Buffer) now render a short visible label after their icons — Copy, Apply, Clear — reusing the shared `.physics-paint-roto-key-icon-label` class from the bottom action row, with a 2px icon-label gap and side padding rule scoped to the scripts toolbar.
  - **E-2 (APPLY ring + label):** the doubled circle/ring at the APPLY button's right edge was the button's own `border-radius: 999px` pill cap echoing the form's 999px pill border — two concentric arcs. The submit is now a 4px rounded rect matching the adjacent number input; the form's short label reads 'Key spacing' instead of 'Space'.
  - **E-3 (selection border):** in the zero-gap abutting 18px cell grid, the next cell painted over the selected cell's right outline edge. `.physics-paint-roto-cell.current` now carries `z-index: 1`, lifting the full four-side orange outline above the neighbor; the 18px pitch, 155px band geometry, and drag machinery are untouched (Plan 06 guardrail tests stay green).
  - **E-4 (dropdown rename):** the interpolation mode option reads 'Frame blending' ('Frame duplicate' unchanged; values unchanged).

## Task Commits

1. **Task 1 (RED+GREEN): cosmetic polish — sidebar labels, APPLY ring, selection border, Frame blending (Gap E)** - `f7b6cbab` (feat)

**Plan metadata:** see final docs commit below (SUMMARY.md + STATE.md + ROADMAP.md).

## Files Created/Modified

- `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx` — label spans (Copy / Apply / Clear) after the three guarded second-row icons
- `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts` — second-row label contract (label after icon per action; toolbar gap/padding rule)
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` — 'Space' → 'Key spacing' label; 'Frame blend' → 'Frame blending' option
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` — Gap E cosmetic contract (Key spacing label, APPLY 999px-radius removal, selected-cell z-index) plus updated Gap D/A label and option assertions
- `app/src/components/physic-paint/physicsPaintStudio.css` — toolbar label gap/padding rule; APPLY border-radius 999px → 4px; `.physics-paint-roto-cell.current` z-index: 1

## Decisions Made

- **The ring artifact was restyled away at the button, not the form.** The APPLY submit's 999px pill cap inside the 999px pill form produced the doubled ring; a 4px rounded rect (like the adjacent number input) removes the echo while keeping the form pill intact.
- **The clipped border was paint order, not geometry.** A positive z-index on the selected state is the entire fix — the 18px cell pitch and 155px band stack are untouched, per the Plan 06 contract.
- **One-word labels consistent with the bottom row.** Copy / Apply / Clear mirror the bottom action row's Key / Duplicate / Insert / Copy / Paste / Delete grammar.

## Deviations from Plan

None - plan executed exactly as written.

## TDD Gate Compliance

- RED run preceded GREEN: 7 failures (2 new panel label cases, 3 new strip Gap E cases, plus the 2 updated Gap D/A assertions for the renamed label and option).
- The plan type is `execute` (not plan-level `type: tdd`), so the RED test edits rode inside the single feat commit — matching the established pattern from Plans 06/07/08 in this phase.
- No refactor commit needed — no cleanup beyond the GREEN scope.

## Threat Mitigations Verified

- **T-36.15-02 (guarded controls):** all three labeled toolbar actions keep their aria-disabled + click/Enter/Space guards returning BEFORE the handler; zero native `disabled`/`title` added.
- **T-36.15-01 (tooltip injection):** new label spans are static Preact text children; no `dangerouslySetInnerHTML`.
- **Drag machinery guardrails:** `data-roto-app-frame` / `data-roto-kind` / `data-roto-key-id` / `classifyRotoDragTarget` / `elementFromPoint` / `setPointerCapture` assertions green; Plan 06 geometry contracts (18px pitch, 155px bands, 28px action row) green.

## Known Stubs

None — every change is fully wired presentation; no placeholders, TODOs, or mock data were introduced.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The user runs the final native visual approval pass on Gap E (coverage E5): second-row sidebar labels, de-ringed APPLY button with 'Key spacing' label, full four-side orange selection border, and the 'Frame blending' dropdown option.
- All phase requirements remain delivered at source-contract level; the 36.15-UAT.md Gap E entry can be marked resolved after the user's pass.

## Self-Check: PASSED

- All 5 key files verified on disk plus this SUMMARY.md.
- Task commit verified in git log: `f7b6cbab`.
- Targeted suite: 60/60 passed (2 files). Full app suite: 82 files passed / 800 tests passed (3 files skipped, 1 test skipped, 101 todo) via `pnpm vitest run` (exit 0).
- `pnpm typecheck` exits 0.
- Source audit: zero `>Frame blend</option>` and zero `>Space</span>` matches remain in the strip; `.physics-paint-roto-force-spacing-apply` has no 999px radius; `.physics-paint-roto-cell.current` carries z-index: 1.

---
*Phase: 36.15-roto-timeline-final-ui-integration*
*Completed: 2026-07-26*
