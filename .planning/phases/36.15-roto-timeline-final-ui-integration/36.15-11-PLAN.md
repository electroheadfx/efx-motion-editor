---
phase: 36.15-roto-timeline-final-ui-integration
plan: 11
type: execute
wave: 8
gap_closure: true
depends_on: [36.15-10]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 36.15-GROUP-SEPARATION
  - 36.15-ICON-ACTIONS
  - 36.15-SCRIPT-CONTROLS

must_haves:
  truths:
    - "Scripts sidebar second-row labels render in full ('Copy', 'Apply', 'Clear') — no truncation (UAT Gap G-1)."
    - "The tools group in the bottom action row has no background block/pill behind it (UAT Gap G-2), and all three bottom-row groups are vertically centered in the 28px band (UAT Gap G-3)."
    - "In the Scripts/Onion/Motion tab group, the Scripts tab is first and default-open (UAT Gap G-4), with visible vertical separation between the two toolbar icon rows (UAT Gap G-5)."
    - "The right sidebar is three tab groups: [Brush color] (LOG tab removed as obsolete), [Tool] (independent tab, 'Save state' and 'Load state' buttons removed), [Scripts, Onion, Motion] (UAT Gap G-6)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (tab group restructure, LOG removal, Tool independence, Save/Load state removal)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (full labels, row separation, Scripts first + default)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (tools group background removal, vertical centering)
    - app/src/components/physic-paint/physicsPaintStudio.css (all styling)
  key_links:
    - "LOG tab removal must also remove or orphan-check any log-messages CSS contract the selection guard test relies on (Plan 03 locked '.physics-paint-log-messages' as a selection exception — if the LOG tab is deleted, update that exception list and its test in the same commit)."
    - "'Save state' / 'Load state' removal is view-layer only; if the underlying persistence API becomes unreferenced, note it in SUMMARY but do not delete store logic in this plan."
---

<objective>
Close UAT Gap G — third polish round from native UAT (2026-07-26): fix truncated Scripts toolbar
labels, remove the tools-group background block, vertically center the bottom-row groups, make
Scripts the first + default-open tab with separated toolbar rows, and restructure the right
sidebar into three tab groups (Brush color without LOG, independent Tool without Save/Load state,
Scripts/Onion/Motion).

Purpose: Final presentation approval pass before the user signs off the phase UAT.
</objective>

<context>
Source of truth: the user's feedback message dated 2026-07-26 with annotated screenshots
(quoted in 36.15-UAT.md Gap G). Plans 08–10 built the labeled rows and group structure this
plan refines. The LOG tab removal interacts with Plan 03's selection-guard exception
('.physics-paint-log-messages') — keep the selection contract test in sync.
</context>

<tasks>

<task number="1">
<name>Sidebar tab restructure + strip/sidebar polish</name>
<read_first>
- app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/physicsPaintStudio.css
- app/src/components/physic-paint/view/applicationSelectionGuard.test.ts
- .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-10-SUMMARY.md
</read_first>
<files>
- app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- app/src/components/physic-paint/physicsPaintStudio.css
</files>
<action>
RED: update contract tests (full sidebar labels; no tools-group background class; Scripts tab first +
default; three tab groups; LOG tab absent; Save/Load state buttons absent; selection-guard exception
list updated if LOG surface is removed).
GREEN:
1. Scripts toolbar: labels 'Copy', 'Apply', 'Clear' render in full — size buttons to content instead
   of fixed-grid cells with ellipsis.
2. Bottom row: remove the background block/pill behind the tools group
   (.physics-paint-roto-key-utilities renders transparent); vertically center all three groups in
   the 28px band (align-items: center on the row).
3. Scripts/Onion/Motion group: Scripts tab first in order and the default-open tab.
4. Scripts toolbar: add clear vertical separation (gap) between the two icon rows.
5. Right sidebar restructure into three tab groups: [Brush color] (remove the LOG tab — obsolete),
   [Tool] as its own independent tab group (remove the 'Save state' and 'Load state' buttons),
   [Scripts, Onion, Motion].
6. If LOG removal orphans '.physics-paint-log-messages', update the Plan 03 selection-guard
   exception list + applicationSelectionGuard.test.ts in the same commit.
7. Keep the 28px action-row band and 155px strip geometry intact.
</action>
<verify>
- pnpm --dir app vitest run (full app suite, never watch mode)
- pnpm --dir app typecheck
</verify>
<commit>feat(36.15-11): sidebar tab restructure + strip/sidebar polish (Gap G)</commit>
</task>

</tasks>

<verification>
- Full app suite green and typecheck exit 0.
- Contract tests lock: full labels, no tools-group background, Scripts first + default, three tab
  groups, LOG/Save-state/Load-state absent, selection-guard exceptions in sync.
- Plan 06 geometry + drag machinery guardrails stay green.
</verification>

<success_criteria>
- [ ] All 6 Gap G items fixed
- [ ] Committed atomically (RED→GREEN)
- [ ] Full suite + typecheck green
</success_criteria>
