---
phase: 36.15-roto-timeline-final-ui-integration
plan: 05
type: execute
wave: 3
depends_on: [36.15-04]
files_modified:
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 36.15-STATUS-CAPSULE

must_haves:
  truths:
    - "The header status capsule shows exactly one prioritized line — pending operation > saving indicator > guard/action feedback > ambient info — with the ambient baseline 'Missing frames play transparent/background' when nothing higher-priority exists (D-15; UI-SPEC covered consideration: priority model keeps one line, baseline when idle, failures concise with LOG detail)."
    - "Within one priority class, the most recent line wins; the arbitration is a pure exported function in physicsPaintWorkflowPresentation.ts, unit-tested without rendering (RESEARCH Open Question 3 resolution)."
    - "The capsule is the sole flex:1, min-width:0 header region, truncates with an ellipsis, never grows the strip or pushes pill groups off-screen, and reveals its full text through the Plan 01 styled tooltip (D-18; UI-SPEC covered consideration: capsule overflow)."
    - "The drag-feedback aria-live status moves onto the capsule (role=status, aria-live=polite preserved); the retired status stack's 9 lines are classified per the RESEARCH removal map — capsule-bound lines route to the selector, tooltip-bound lines to per-cell tooltips, diagnostic lines deleted."
    - "The bottom cell-states legend is removed entirely (C-06/D-16) and each timeline cell exposes its semantic state through the styled per-cell tooltip with exact copy: Real key / Generated — render-only / Cached / Background only / Empty (UI-SPEC covered consideration: legend removal compensated by per-cell tooltips; long ranges scroll with fixed cell pitch)."
    - "The right-panel LOG tab remains the running history/diagnostic surface, unchanged; the capsule never duplicates it and no Log button appears in the timeline header (D-19)."
    - "The no-real-keys empty state keeps the exact documented copy 'No real Roto keys' / 'Paint on a real frame or duplicate a key to begin timing.' inside the existing compact surface without adding a panel or increasing strip height (UI-SPEC covered consideration)."
  artifacts:
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (capsule selector + cell tooltip copy)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (capsule in header; stack/legend deleted; cell tooltips)
    - app/src/components/physic-paint/physicsPaintStudio.css (.physics-paint-status-capsule; stack/legend CSS deleted)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts (selector cases)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (capsule/absence assertions)
  key_links:
    - "Capsule content is a computed derivation over EXISTING props/signals only (statusMessage, scriptStatus, pending operation id, drag feedback, playback status) — no new controller state, no useEffect copying props into local state."
    - "Every surface adopting the styled tooltip here (cells, capsule) loses its native title attribute — cell title={dragTitle} routes through the styled tooltip instead."
  prohibitions:
    - "No replacement band for the deleted status stack/legend: no toast rail, expandable diagnostics region, second status line, or multiline block anywhere under or beside the timeline (Fixed Layout Contract; UI-SPEC dismissed consideration)."
    - "No permanent legend, generated-frame explanation, cached-reference explanation, or interpolation diagnostic text may remain anywhere in the strip."
    - "Capsule selector must not read store internals or derive state — it receives already-resolved values from the strip's existing props/signal reads."
  assumptions:
    - "FLAGGED (unclassified edge, 36.15-STATUS-CAPSULE): capsule arbitration uses most-recent-wins within a priority class (RESEARCH Open Question 3), and the amber Info glyph #F8C96B is used per D-15/reference even though the Color contract lists the accent role for the status information glyph (RESEARCH assumption A4) — both are visual confirmations for native UAT, not blockers."
---

<objective>
Replace the permanent multi-line status stack and the cell-states legend with the elastic header status capsule (D-15/D-18/D-19) and per-cell styled state tooltips (D-16/C-06). The capsule's priority grammar lives in a pure, unit-tested function in the existing presentation module; the strip integration deletes the retired surfaces outright and routes cell state copy through the Plan 01 tooltip.

Purpose: 36.15-STATUS-CAPSULE is the largest information-architecture change of the phase — nine permanent status lines collapse into one prioritized line — and it must land before the 155px geometry cutover (Plan 06), which is impossible while the 66px stack/legend band exists.
Output: Pure capsule priority selector with tests; capsule integrated into the Plan 04 header; status stack, legend, and their CSS deleted; per-cell state tooltips live.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-04-SUMMARY.md
@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capsule priority selector + cell-state tooltip copy in the presentation module</name>
  <files>app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (full file; lines 300-305 template constants, 397-415 pure builder pattern)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts (lines 12-23 test idiom)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md (Status Stack & Legend Removal Map — the 9-line disposition table)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Status Capsule Priority Model, Cell-State Tooltips, Copywriting Contract)
  </read_first>
  <behavior>
    - Test 1 (RED): with only ambient info present, the selector returns the baseline 'Missing frames play transparent/background'.
    - Test 2 (RED): guard/action feedback beats ambient; saving indicator beats guard feedback; pending operation beats saving (full D-15 priority ladder, one case per rung).
    - Test 3 (RED): two simultaneous guard-class lines resolve most-recent-wins.
    - Test 4 (RED): busy copy passes through verbatim (e.g. 'Applying spacing…', 'Moving key…') as do verb-first success lines ('Frame inserted', 'Spacing applied', 'Key moved', 'Undo complete', 'Redo complete').
    - Test 5 (RED): the cell-state tooltip copy mapping returns exactly 'Real key', 'Generated — render-only', 'Cached', 'Background only', 'Empty' for the corresponding cell kinds.
  </behavior>
  <action>
    RED first: add the five behavior case groups to physicsPaintWorkflowPresentation.test.ts with exact string equality; watch them fail. GREEN: add to physicsPaintWorkflowPresentation.ts, following the existing pure-builder pattern (typed input interface, module-top copy templates, no signals — callers pass .value results): (1) an exported getRotoStatusCapsuleViewModel pure function taking the candidate status inputs (pending operation line, saving indicator, guard/action feedback lines with recency metadata, ambient line) and returning one prioritized line per the D-15 ladder with most-recent-wins inside a class; (2) an exported cell-state tooltip copy mapping (function or record) from semantic cell kind to the five exact D-16 strings. Neither function reads stores, signals, or props objects directly — the strip passes already-resolved values.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior case groups pass with exact string equality.
    - physicsPaintWorkflowPresentation.ts exports the capsule selector and the cell-state copy mapping; neither imports stores or signals.
    - The baseline string is exactly 'Missing frames play transparent/background'.
    - pnpm --dir app vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>The D-15 priority grammar and D-16 cell copy exist as pure, unit-tested presentation-module functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate capsule into header; delete status stack + legend; per-cell styled tooltips</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 50-55 ROTO_CELL_LEGEND_ITEMS constants, 854-908 cell render incl. title={dragTitle} at ~901, 933-958 status stack + legend block, Plan 04 header output)
    - app/src/components/physic-paint/physicsPaintStudio.css (lines 1900-1962 status stack + legend styles)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (line 41 generated-frame assertion and any stack-era assertions being retired)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md (Status Stack & Legend Removal Map; Pitfall 4 double tooltips)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-04-SUMMARY.md (header slot left for the capsule)
  </read_first>
  <behavior>
    - Test 1 (RED): strip source renders .physics-paint-status-capsule inside the header between the navigation pill and the interpolation pill, carrying role="status" and aria-live="polite", with the lucide Info glyph.
    - Test 2 (RED): strip source no longer contains the status-stack or cell-legend class hooks, the ROTO_CELL_LEGEND_ITEMS constant, the static generated-frame explanation line, or the interpolation diagnostic status line (retire the stale test-41 assertion in the same edit).
    - Test 3 (RED): the cell render maps semantic kind to the Task 1 tooltip copy and the cell element no longer carries a native title attribute.
    - Test 4 (RED): physicsPaintStudio.css contains .physics-paint-status-capsule with flex:1, min-width:0, ellipsis truncation, background #2D3741, border #51606D, 11px/700 text, and the Info glyph color #F8C96B; the retired stack/legend CSS blocks are gone.
  </behavior>
  <action>
    RED first: update PhysicsPaintWorkflowStrip.test.ts — add the four behavior groups and delete the stale generated-frame/stack-era assertions in the same edit (never leave stale assertions for a later wave); watch fail. GREEN: (1) Insert the capsule into the Plan 04 header slot: class physics-paint-status-capsule, role="status", aria-live="polite", lucide Info icon (size 16, glyph #F8C96B, aria-hidden), single truncating span fed by getRotoStatusCapsuleViewModel over the existing props/signal reads (statusMessage, scriptStatus, pending operation id, rotoDragFeedback, cached playback status), full text revealed through the Plan 01 styled tooltip (D-18). (2) Delete the status stack and legend render block and their constants from the strip; route the drag feedback line into the capsule input; delete the interpolation diagnostic and generated-frame explanation lines outright (their content now lives on the interpolation pill tooltip and per-cell tooltips). (3) Cells: replace the native title with the Plan 01 styled tooltip carrying the Task 1 state copy for the cell's semantic kind; keep every data-roto-* attribute, class hook, and drag handler untouched. (4) CSS: add .physics-paint-status-capsule per behavior test 4 (rounded-full, flex:1, min-width:0, single-line ellipsis, #DDE7F0 text); delete the retired stack/legend CSS blocks (css ~1900-1962) entirely — no replacement band.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four behavior assertion groups pass; the stale stack-era assertions are removed from the test file, not skipped.
    - Strip source contains physics-paint-status-capsule with role="status" and aria-live="polite" positioned between the navigation and interpolation pills, and contains no status-stack/legend class hooks (negative assertions pass).
    - Cell render carries no native title attribute and feeds the exact D-16 copy per kind through the styled tooltip; all data-roto-* attributes remain (existing drag assertions still pass).
    - physicsPaintStudio.css contains the capsule style per behavior test 4 and none of the retired stack/legend selectors.
    - No Log button is added to the strip header; PhysicsPaintRightPanel.tsx is untouched by this plan (D-19).
    - pnpm --dir app vitest run on both touched test files exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>One prioritized capsule line replaces the permanent stack; the legend is gone; cell states are discoverable via styled per-cell tooltips; LOG remains the only diagnostic surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→capsule/tooltip | Status and reason strings cross into rendered text (injection risk if ever rendered as HTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-08 | Tampering | Capsule + per-cell tooltip strings | medium | mitigate | All capsule/tooltip content renders as Preact text children (auto-escaped); selector is a pure string function with no HTML path; verified by presentation-module tests |
| T-36.15-09 | Information disclosure | Capsule failure copy | low | mitigate | Failures show only the concise '{Operation} failed — see LOG' summary in the capsule; full error objects and diagnostics stay in the LOG tab (D-19) |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` exits 0.
- `pnpm --dir app typecheck` exits 0.
- Source audit: no status-stack/legend selectors remain in TSX or CSS; no native title on cells or capsule; drag machinery assertions still green.
</verification>

<success_criteria>
- 36.15-STATUS-CAPSULE delivered: one elastic prioritized capsule line, legend removed (C-06), per-cell state tooltips, LOG untouched (D-19).
</success_criteria>

## Artifacts this phase produces (Plan 05)

- `getRotoStatusCapsuleViewModel` exported pure function — physicsPaintWorkflowPresentation.ts (NEW export)
- Cell-state tooltip copy mapping — physicsPaintWorkflowPresentation.ts (NEW export)
- `.physics-paint-status-capsule` CSS class (NEW)
- Capsule JSX in the strip header with lucide Info glyph (NEW render path; new lucide import Info)
- Per-cell styled tooltip wiring replacing native cell titles (NEW wiring)

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-05-SUMMARY.md` when done
</output>
