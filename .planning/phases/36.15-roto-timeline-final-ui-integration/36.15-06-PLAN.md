---
phase: 36.15-roto-timeline-final-ui-integration
plan: 06
type: execute
wave: 4
depends_on: [36.15-05]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 36.15-STRIP-GEOMETRY

must_haves:
  truths:
    - "The workflow strip is a fixed 155px high at every host width, composed of exactly these bands in order: 46px header / 1px divider / 28px ruler / 38px physical-frame lane / 28px fit-content context action row / 14px synchronized scrollbar band (UI-SPEC Fixed Layout Contract, D-04; STRIP-GEOMETRY boundary edge: no legacy strip height and no responsive height override remains anywhere in the CSS)."
    - "Timeline cells are 18px wide x 24px high and abut with gap: 0 in a repeat(120, 18px) grid; compact separation comes from the existing 1px cell border/inset inside each locked cell, never from layout gap (STRIP-GEOMETRY adjacency edge; UI-SPEC Spacing exceptions)."
    - "One derived lane-width constant (VIRTUAL_TIMELINE_FRAME_COUNT 120 x 18px = 2160px) feeds the ruler width, the lane width, and the cell grid; the legacy hardcoded lane-width literal appears nowhere in the strip TSX or the CSS (Pitfall 2; STRIP-GEOMETRY ordering edge)."
    - "Ruler ticks are fixed-pitch 54px boxes (RULER_STEP 3 x 18px cell pitch), not flex-distributed, so tick labels stay aligned to every 3rd cell boundary at scroll extremes (STRIP-GEOMETRY precision edge)."
    - "The key-utilities pill (Key {n} chip + 7 icon buttons from Plan 01) lives in its own 28px action-row band placed inside the horizontal scroll container as a sibling of the lane, so it stays horizontally synchronized with cell scrolling while occupying its own vertical band (D-03; Fixed Layout Contract band order)."
    - "Below the minimum host width the strip scrolls horizontally (overflow-x: auto, overflow-y: hidden) instead of crushing controls or invoking a breakpoint collapse; the max-width: 860px media query no longer reflows the studio grid, re-places the strip, or overrides right-panel/strip heights (D-18; Responsive Contract: one header row at every host-supported width, no breakpoint/density mode)."
    - "Drag machinery survives the cutover untouched: every data-roto-app-frame / data-roto-kind / data-roto-key-id attribute, the classifyRotoDragTarget hit-test path (document.elementFromPoint + closest('[data-roto-app-frame]')), pointer-capture handlers, and the caret/committing class hooks are present and behaviorally unchanged (Pitfall 1; UI-SPEC: presentation-only, no threshold/pointer-capture/caret/preview semantic changes)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (ROTO_CELL_WIDTH_PX / ROTO_LANE_WIDTH_PX derived constants; action-row band restructure)
    - app/src/components/physic-paint/physicsPaintStudio.css (155px strip shell + studio grid row, band geometry, 18px abutting cells, 2160px lane/ruler, 54px ticks, 14px scrollbar, media-query collapse removal)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (geometry source-contract assertions + drag-survival guardrails)
  key_links:
    - "The inline ruler width style in the strip TSX (~line 847) must bind to the same ROTO_LANE_WIDTH_PX constant the CSS lane/ruler/cells use — two sources of truth re-create Pitfall 2 desynchronization."
    - "Band heights must sum to exactly 155 (46+1+28+38+28+14): strip vertical padding and inter-band gap become 0; the 1px divider is a border on a band, not extra height."
    - "The occupied-cell midpoint boundary logic (strip ~lines 444-446) and classifyRotoDragTarget depend on cell rects via elementFromPoint + closest('[data-roto-app-frame]') — they survive the width change automatically only if the data attributes and class hooks stay."
  prohibitions:
    - "No cell-width, ruler-cadence, or band-height scaling at any breakpoint; do not turn the locked geometry values into new general spacing tokens (UI-SPEC Spacing)."
    - "Do not rewrite the cell render block, drag handlers, or the key-utilities pill contents; this plan changes CSS plus band wrapper structure only (Pitfall 1)."
    - "No replacement band for the Plan 05-deleted status stack/legend may appear while restructuring (Fixed Layout Contract; restated)."
    - "When removing legacy geometry, leave no tombstone comments carrying the old pixel literals — source-contract assertions grep the CSS/TSX for their absence."
  assumptions: []
---

<objective>
Execute the full 36.15-STRIP-GEOMETRY cutover quantified in the RESEARCH geometry gap table and locked by the UI-SPEC Fixed Layout Contract: fixed 155px strip (bands 46/1/28/38/28/14), 18px abutting cells, a single derived 2160px lane-width constant replacing the legacy hardcode, ruler ticks re-pitched to the 18px cell grid, the key-utilities pill re-banded into its own 28px action row, the 14px synchronized scrollbar band, removal of the 860px responsive collapse, and D-18 horizontal scroll below minimum host width. Task 1 proves the riskiest seam — ruler/cell pitch alignment around one derived constant — end-to-end before Task 2 restructures the band stack.

Purpose: This is the phase's only full-geometry cutover; it is impossible until Plan 05 deletes the 66px status stack + legend band (its stated precondition), and it must not perturb the drag machinery that Plans 01/04/05 left intact.
Output: Locked 155px strip geometry with derived-constant alignment, locked by source-contract assertions per the RESEARCH Validation Architecture, with drag-survival guardrail assertions green from the first run.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-05-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end pitch cutover — derived 2160px lane constant, 18px abutting cells, 54px ruler ticks, 14px scrollbar</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 128-145 — VIRTUAL_TIMELINE_FRAME_COUNT / RULER_STEP constants, visibleCount, rotoRulerTicks derivation; line ~847 — the ruler render with its inline width/minWidth style; lines 854-908 — lane and cell render)
    - app/src/components/physic-paint/physicsPaintStudio.css (lines 1984-2003 scrollbar + thumb, 2004-2032 ruler + tick + tick::after, 2035-2045 lane, 2057-2074 cells grid + cell box)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (the readFileSync source-contract idiom used for TSX and CSS assertions in Plans 01/04/05)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md (Geometry gap table; Pitfall 2 ruler/cell misalignment; Pitfall 1 drag machinery)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Fixed Layout Contract; Spacing exceptions — 18px x 24px abutting cells)
  </read_first>
  <behavior>
    - Test 1 (RED): the strip TSX declares a derived lane-width constant (e.g. ROTO_LANE_WIDTH_PX) computed as VIRTUAL_TIMELINE_FRAME_COUNT * 18, and the ruler render's inline width/minWidth binds that constant (assert the source contains the constant identifier at both the declaration and the ruler style); the strip TSX no longer contains the legacy hardcoded lane-width literal (negative assertion).
    - Test 2 (RED): the CSS .physics-paint-roto-cells rule declares grid-template-columns: repeat(120, 18px) and gap: 0; the legacy 13px repeat declaration is absent (negative assertion).
    - Test 3 (RED): the CSS .physics-paint-ruler and .physics-paint-lane rules declare 2160px widths (min-width / grid-template-columns) and the legacy lane-width literal is absent from the whole CSS file (negative assertion); .physics-paint-ruler-tick is fixed-pitch — width or flex-basis 54px — and no longer uses flex: 1 1 0.
    - Test 4 (RED): the CSS .physics-paint-timeline-scrollbar rule declares height: 14px (the legacy 8px-plus-margins treatment is gone).
    - Test 5 (guardrail — passes on first run; if it fails, STOP and report rather than patching production): the strip source still contains data-roto-app-frame, data-roto-kind, data-roto-key-id, classifyRotoDragTarget, elementFromPoint, and setPointerCapture.
  </behavior>
  <action>
    RED first: add the five behavior assertion groups to PhysicsPaintWorkflowStrip.test.ts using the established readFileSync idiom (TSX sibling, CSS one directory up); run vitest and watch 1-4 fail. GREEN: (1) In the strip TSX, beside the existing constants at lines 128-129, declare ROTO_CELL_WIDTH_PX = 18 and ROTO_LANE_WIDTH_PX = VIRTUAL_TIMELINE_FRAME_COUNT * ROTO_CELL_WIDTH_PX (resolves to 2160); rebind the ruler's inline width/minWidth style (~line 847) to ROTO_LANE_WIDTH_PX so the TSX and CSS share one source of truth. Do not touch VIRTUAL_TIMELINE_FRAME_COUNT or RULER_STEP — the 120-frame virtual window and every-3rd-frame tick cadence are approved behavior. (2) CSS cells: change the .physics-paint-roto-cells grid to repeat(120, 18px) with gap: 0; leave .physics-paint-roto-cell (24px height, 1px border, inset separation) otherwise untouched. (3) CSS lane/ruler: replace the legacy lane-width value with 2160px in .physics-paint-ruler (min-width) and .physics-paint-lane (grid-template-columns + min-width). (4) CSS ruler ticks: convert .physics-paint-ruler-tick from flex-distributed to fixed-pitch — width (or flex: 0 0) 54px with box-sizing: border-box — so each of the 40 ticks (120 frames / RULER_STEP 3) lands exactly on a 3-cell boundary (3 x 18px); keep the existing tick label styling and tick::after mark. (5) CSS scrollbar: restyle .physics-paint-timeline-scrollbar to a 14px-high band (height: 14px, zeroed vertical margins; thumb inset adjusted to remain centered) — full band synchronization polish lands with the band restructure in Task 2. Do not touch any drag handler, data-roto-* attribute, or the cell render block. Leave no comments carrying the removed legacy pixel literals.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip.test.ts passes all five behavior assertion groups (1-4 turned green by the cutover; 5 green throughout).
    - Strip TSX contains ROTO_LANE_WIDTH_PX derived from VIRTUAL_TIMELINE_FRAME_COUNT and the cell-width constant, and the ruler inline style binds it; a grep of the strip TSX for the legacy hardcoded lane-width literal returns zero matches.
    - physicsPaintStudio.css contains repeat(120, 18px), gap: 0 on the cells grid, 2160px on ruler and lane, a 54px fixed-pitch ruler tick, and height: 14px on the timeline scrollbar; greps of the CSS for the legacy lane-width literal and the legacy 13px cell repeat return zero matches.
    - VIRTUAL_TIMELINE_FRAME_COUNT (120) and RULER_STEP (3) are unchanged in the strip source.
    - pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>The tracer path — one derived 2160px constant aligning ruler ticks, lane, and abutting 18px cells, plus the 14px scrollbar — is cut over end-to-end with source-contract proof and the drag guardrails green, and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixed 155px band stack, action-row band restructure, responsive-collapse removal, D-18 horizontal scroll</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 840-932 — the full timeline region: scroll container, ruler, lane, key-utilities pill placement; the key-utilities block post-Plan-01 icon row)
    - app/src/components/physic-paint/physicsPaintStudio.css (line ~412 studio grid-template-rows; lines 1330-1345 strip shell; line ~1368 header block; the .physics-paint-timeline / .physics-paint-timeline-scroll rules; lines 2545-2570 — the max-width: 860px media query including the studio grid collapse, right-panel-shell/rail placement, the right-panel fixed-height override at lines 2555-2556, and the strip placement override)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Fixed Layout Contract band order and heights; Responsive Contract — no breakpoint, horizontal scroll below minimum host width)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md (Geometry gap table rows: strip height, frame lane, action row, status stack band already deleted by Plan 05)
  </read_first>
  <behavior>
    - Test 1 (RED): the CSS .physics-paint-workflow-strip rule declares height: 155px, and the .physics-paint-studio grid-template-rows third track is 155px; the CSS file contains no legacy strip-height literal and no responsive height-override literal anywhere (negative assertions — note the override lives on .physics-paint-right-panel inside the 860px media query, lines 2555-2556, not on the strip itself).
    - Test 2 (RED): the band geometry exists in CSS — header 46px, lane 38px (no min-height 74px / padding 8px 0 remnant), a .physics-paint-roto-action-row band at 28px, scrollbar 14px — and the strip shell declares zero vertical padding and zero inter-band gap so the bands sum to exactly 155 (46+1+28+38+28+14, the 1px divider carried as a band border).
    - Test 3 (RED): the strip TSX renders the key-utilities pill inside a .physics-paint-roto-action-row element that is a sibling of .physics-paint-lane inside .physics-paint-timeline-scroll (assert source ordering: the action-row class appears after the lane's closing structure and before the scroll container closes; the key-utilities class appears inside the action-row region, no longer inside the lane grid).
    - Test 4 (RED): the 860px media query contains no .physics-paint-workflow-strip rule and no .physics-paint-right-panel height override (negative assertions); .physics-paint-workflow-strip declares overflow-x: auto with overflow-y: hidden (D-18).
    - Test 5 (guardrail — passes on first run): same drag-machinery identifiers as Task 1 test 5, restated to catch restructure regressions (data-roto-app-frame, data-roto-kind, data-roto-key-id, classifyRotoDragTarget, elementFromPoint, setPointerCapture).
  </behavior>
  <action>
    RED first: add the five behavior assertion groups to PhysicsPaintWorkflowStrip.test.ts; run vitest and watch 1-4 fail. GREEN: (1) Band restructure in the strip TSX: move the key-utilities pill block out of the .physics-paint-lane grid into a new sibling div class physics-paint-roto-action-row placed after the lane inside .physics-paint-timeline-scroll, so the action row keeps horizontal scroll synchronization with the cells while owning its vertical band; move the pill's contents byte-for-byte (Key {n} chip + the 7 Plan 01 icon buttons, roles, aria-labels, guards, tooltip wiring all unchanged). (2) CSS strip shell: .physics-paint-workflow-strip becomes height: 155px with zero vertical padding and gap: 0 (horizontal padding only if the contract bands require it — the sum 46+1+28+38+28+14=155 must hold exactly), overflow-x: auto and overflow-y: hidden replacing the plain hidden overflow (D-18 horizontal scroll below minimum host width); keep the existing border-top and background. (3) CSS studio grid: change the .physics-paint-studio grid-template-rows third track (~line 412) from the legacy strip height to 155px. (4) CSS bands: header row 46px (finalize the Plan 04 target); the 1px divider between header and ruler as a border on an adjacent band (not extra height); .physics-paint-lane to height: 38px with padding: 0 (drop the min-height and vertical padding); new .physics-paint-roto-action-row rule — 28px fit-content band, display flex, align-items center, background in the #37393A lane/action-band tonal family, min-width matching the lane so it scrolls as one surface; .physics-paint-timeline loses its min-height remnant (bands are fixed); .physics-paint-timeline-scroll keeps overflow-x auto with the native scrollbar hidden (the 14px custom band stays the visible indicator). (5) Responsive: inside the max-width: 860px media query (css ~2545-2570), delete the studio-layout collapse rules — the .physics-paint-studio grid rows/columns override, the .physics-paint-right-panel-shell/.physics-paint-right-panel-rail placement override, the .physics-paint-right-panel fixed-height override (lines 2555-2556; RESEARCH cited ~2566), the right-section min-width overrides, and the .physics-paint-workflow-strip placement override — so the fixed layout contract holds at every width; keep the query's topbar-only cosmetic rules untouched. Leave the max-width: 1180px query untouched (it does not alter strip geometry). Leave no comments carrying the removed pixel literals.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip.test.ts passes all five behavior assertion groups (1-4 turned green; 5 green throughout).
    - physicsPaintStudio.css declares height: 155px on .physics-paint-workflow-strip and a 155px third track on .physics-paint-studio grid-template-rows; greps of the CSS for the legacy strip-height literal and the responsive height-override literal return zero matches.
    - CSS contains the .physics-paint-roto-action-row band at 28px, the lane at 38px, the header at 46px, and the strip shell with zeroed vertical padding/gap and overflow-x: auto / overflow-y: hidden.
    - Strip TSX renders .physics-paint-roto-action-row as a sibling of .physics-paint-lane inside .physics-paint-timeline-scroll, with the key-utilities pill inside it; the pill's contents (chip + 7 guarded icon buttons) are unchanged.
    - The 860px media query contains no .physics-paint-workflow-strip or right-panel height/placement rules; its topbar cosmetic rules remain.
    - pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>The strip is a fixed 155px five-band surface (46/1/28/38/28/14) with the action row re-banded and scroll-synchronized, the responsive collapse gone, horizontal scroll below minimum host width, and the drag machinery provably intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer→drag hit-test | Drag commit target resolution depends on cell geometry (elementFromPoint + cell rects); misaligned geometry could route a commit to the wrong physical frame |
| CSS contract→renderer | Static geometry values cross into interactive surfaces (ruler/cell/scroll synchronization) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-10 | Tampering | Ruler/cell/lane width provenance | high | mitigate | Single derived ROTO_LANE_WIDTH_PX constant feeds TSX inline style and is mirrored by fixed CSS values; fixed-pitch 54px ticks; source-contract assertions lock both the derived constant and the absence of the legacy hardcode (Task 1 tests 1-3) |
| T-36.15-11 | Elevation of privilege | Drag target classification after geometry change | high | mitigate | No drag code is touched; guardrail assertions (Task 1 test 5, Task 2 test 5) prove data-roto-* attributes, classifyRotoDragTarget, elementFromPoint, and pointer capture survive; midpoint boundary logic is rect-derived and tracks the new widths automatically |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` exits 0.
- `pnpm --dir app typecheck` exits 0.
- Source audit: no legacy lane-width, strip-height, or responsive-override literals remain in the strip TSX or CSS; band heights sum to exactly 155; drag identifiers intact.
- Manual (deferred to phase-level user-owned native UAT via /gsd-verify-work): fixed 155px strip with band geometry, abutting 18px cells, ruler alignment at scroll extremes, synchronized scrollbar, and horizontal scroll below minimum host width (UI-SPEC backstop rows).
</verification>

<success_criteria>
- 36.15-STRIP-GEOMETRY delivered: fixed 155px strip (46/1/28/38/28/14), 18px abutting cells, derived 2160px lane width, 54px ruler ticks, 28px action-row band, 14px scrollbar band, responsive collapse removed, D-18 horizontal scroll — all locked by source-contract assertions with drag machinery untouched.
</success_criteria>

## Artifacts this phase produces (Plan 06)

- `ROTO_CELL_WIDTH_PX` / `ROTO_LANE_WIDTH_PX` derived constants — app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (NEW)
- `.physics-paint-roto-action-row` 28px band + pill relocation out of the lane grid (NEW band)
- Fixed 155px strip shell + studio grid third track, band heights 46/1/28/38/28/14, zeroed strip padding/gap (CSS cutover)
- 18px abutting cell grid (`repeat(120, 18px)`, `gap: 0`), 2160px lane/ruler, 54px fixed-pitch ruler ticks, 14px scrollbar band (CSS cutover)
- `overflow-x: auto` strip shell (D-18) + 860px media-query collapse removal (CSS cutover)
- Geometry source-contract + drag-survival guardrail assertions — PhysicsPaintWorkflowStrip.test.ts (NEW cases)

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-06-SUMMARY.md` when done
</output>
