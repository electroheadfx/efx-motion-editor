---
phase: 36.15-roto-timeline-final-ui-integration
verified: 2026-07-26T00:35:00Z
status: human_needed
score: 5/6 must-haves verified
behavior_unverified: 1
overrides_applied: 0
human_verification:
  - test: "Launch the Physics Paint Roto studio with real Roto keys present and inspect the final strip: fixed 155px five-band geometry (header / ruler / lane / action row / scrollbar), header order navigation pill -> status capsule -> + Key -> Duplicate -> Tools dropdown -> Close, layer name in the bottom action row before the Key {n} chip, enlarged action-row icons, no dead space, horizontal scroll below minimum host width."
    expected: "Strip matches the approved 36.15 UI-SPEC reference; bands never grow/wrap; Tools dropdown opens inside the strip without clipping (interpolation/playback/apply-spacing pills intact inside); + Key promotes the current frame to a green empty real key that is paintable; occupied-frame and in-flight cases show guarded reduced-emphasis state with the two-part tooltip reason."
    why_human: "Rendered pixel appearance, dropdown fit inside overflow-y:hidden shell, and interactive + Key promotion cannot be proven by source-contract tests (Plan 06 coverage D5)."
  - test: "Hover and keyboard-focus guarded actions across the icon row, Tools dropdown pills, status capsule, timeline cells, and the Scripts toolbar Discard Script button."
    expected: "Styled tooltip appears only after exactly 1000ms hover, instantly on keyboard focus, dismisses on leave/blur/Escape/activation; unavailable actions stay visible/focusable with '{Action} — unavailable: {reason}' copy; tooltip renders above studio chrome (z-index 60); no native tooltip double-fires."
    why_human: "Timing has fake-timer unit tests but real pointer/focus feel and z-index layering against the live studio chrome need native confirmation (Plans 01/05/07 coverage D6)."
  - test: "In the main EFX Motion timeline, inspect a physic-paint FX layer row with many real Roto keys, including a layer with nonzero inFrame."
    expected: "Small orange (#F5A623) diamonds render at each real key's physical frame, always visible, generated in-betweens get no marker, no overlap with adjacent layer content, no interaction surface."
    why_human: "Geometry and data provenance are unit-tested, but the layer-local + inFrame coordinate assumption (RESEARCH A3) and visual non-overlap are backstop/native-visual items (Plan 02 backstop truth, coverage D5)."
  - test: "Exercise the app shell: select/copy LOG tab messages, edit fps/N inputs and any contenteditable field, paint on canvas, drag timeline keys and the strip scrollbar."
    expected: "LOG text and inputs select/edit natively; painting, dragging, pointer capture, and keyboard focus are unaffected by the index.html selection guard."
    why_human: "The guard is pure CSS policy verified by source contract; non-interference with live pointer/selection behavior needs native interactive UAT (Plan 03 coverage D4)."
---

# Phase 36.15: Roto Timeline Final UI Integration Verification Report

**Phase Goal:** As a stop-motion animator, I want the Physics Paint Roto timeline to present the approved final UI — fixed strip geometry with clearly separated control groups, icon-only actions with tooltips, an elastic status capsule, and key markers visible on the EFX Motion layer — so the physical-frame behavior approved in 36.14 is exposed through the intended presentation without duplicating business logic in the view.
**Verified:** 2026-07-26T00:35:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Fixed Layout Contract: fixed 155px strip, locked header hierarchy, fixed abutting cells, fit-content non-wrapping action row | VERIFIED | `physicsPaintStudio.css:1337` height:155px with gap:0, overflow-x:auto/overflow-y:hidden; grid track `minmax(58px, auto) minmax(0, 1fr) 155px` (:412); bands 46px header / 1px border-top divider / 28px ruler / 38px lane / 28px action row / 14px scrollbar = 155; `repeat(120, 18px)` cells gap:0 (:2122); `ROTO_LANE_WIDTH_PX = 120 * 18 = 2160` single derived constant in TSX (:128-131) mirrored by CSS 2160px lane/ruler/action-row; 54px fixed-pitch ticks; zero 256px/1800px/13px-repeat legacy literals anywhere (grep clean); band-stack + pitch source-contract tests pass |
| 2 | Control groups render as distinct gray pill islands: playback (loop/fps), interpolation (borderless blend icon), apply-key-spacing (align-horizontal-space-around before number), minimum 5px separation | VERIFIED | `.physics-paint-pill` + tonal variants (navigation/interpolation/playback/apply-spacing) with 8px container gap and no divider glyphs; borderless `Blend` toggle with `aria-pressed` (strip :964-972), no interpolation count input anywhere (grep clean); `AlignHorizontalSpaceAround` rendered before the N input (:1000); pills relocated into the user-approved Tools dropdown (aria-haspopup menu, :941) per Plan 06's documented scope expansion — pill groups byte-identical inside; CSS tonal-variant and header-contract tests pass |
| 3 | Bottom action row icon-only with tooltips per C-05; Discard Script lives in the right-panel Scripts toolbar | VERIFIED | Action row (:1104+) = layer label + `Key {n}` chip + guarded lucide icon buttons (Insert/Copy/Paste/Delete/Copy Script/Apply Script; Duplicate relocated to header :912 per approved expansion); zero native `disabled=`/`title=` in the row (grep clean); every action has `aria-disabled` + `aria-describedby` to an sr-only verbatim reason and guards returning before the controller call; Discard Script (ClipboardX) guarded in `.physics-paint-scripts-toolbar` (PhysicsPaintScriptsPanel.tsx :71-91) fed by controller `canDiscard`/`discardDisabledReason` ports (physicsPaintRotoScriptClipboard.ts :44-48, :254-267); `onDiscardRotoScript` fully absent from strip + Studio (grep exit 1); strip/panel/clipboard suites green |
| 4 | Bottom cell-states legend removed; elastic truncating header status capsule replaces the permanent status stack; LOG tab remains the only diagnostic surface | VERIFIED | Zero `roto-status-stack`/`ROTO_CELL_LEGEND_ITEMS`/legend remnants in TSX or CSS (grep clean); `.physics-paint-status-capsule` sole flex:1 min-width:0 header region, role=status aria-live=polite, Info glyph #F8C96B, ellipsis text (CSS :1368-1399); pure `getRotoStatusCapsuleViewModel` selector (pending > saving > feedback most-recent-wins > ambient baseline 'Missing frames play transparent/background') unit-tested in physicsPaintWorkflowPresentation.test.ts; per-cell exact D-16 copy via `getRotoCellStateTooltipCopy` + `RotoTimelineCellButton` child component; PhysicsPaintRightPanel.tsx untouched (liveness guard test) |
| 5 | Paint keys visually marked on the EFX Motion layer; application selection guard in app/index.html; guarded Script controls stay in order, focusable, with controller-supplied reasons | VERIFIED | `FxTrackLayout.rotoKeyFrames?: number[]` (types/timeline.ts:57) populated only for physic-paint layers from `physicPaintStore.getRotoRealKeyRecords` (frameMap.ts:197-198); `drawRotoKeyMarkers` clipped pure-canvas pass, #F5A623 fill, no stroke/shadow/hit-test, halfSize 5, gated on `fxTrack.rotoKeyFrames?.length` in the drawFxTrack physic-paint branch (TimelineRenderer.ts :500-539, :648-651); reactivity rides existing physicPaintVersion (reactivity tests pass); index.html guard: `html, body, #app` user-select:none then exactly `input, textarea, [contenteditable="true"], .physics-paint-log-messages` user-select:text after it (6/6 contract tests); script controls verified in row 3 |
| 6 | All wiring consumes existing controller/resolver seams — no business logic in the view; 36.14 physical contract unchanged; native visual UAT approves the final result | PRESENT_BEHAVIOR_UNVERIFIED | Source level VERIFIED: all availability/reasons read controller ports (`physicalActions.*DisabledReason`, `sessionKeyAvailability`, `scriptAvailability copy/applyDisabledReason`, `addEmptyKeyDisabledReason`) — strip :328-366; selector is pure with type-only imports; drag machinery (data-roto-* attributes, classifyRotoDragTarget, elementFromPoint + closest) intact with guardrail tests; TimelineInteraction untouched; + Key reuses the paste-to-empty resolver/coordinator path with occupied-destination rejection tested. The "native visual UAT approves" clause is the phase-level human gate — pending (see Human Verification) |

**Score:** 5/6 truths verified (1 present, behavior-unverified — the native-UAT clause of SC6)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx` | useStyledTooltip 1000ms/instant-focus/Escape-while-visible + text-children surface | VERIFIED | 110 lines; full hook + surface read and confirmed (delay constant 1000, focus show instant, Escape listener registered only while visible, mountedRef cleanup, role=tooltip, renders null while hidden) |
| `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` | Recomposed strip: capsule, pills/Tools dropdown, icon row, + Key, band geometry | VERIFIED | 1285 lines; all structural claims grep-verified; drag machinery intact |
| `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` | Pure capsule selector + D-16 cell copy | VERIFIED | 556 lines; type-only imports (no store/signal reads) |
| `app/src/components/physic-paint/physicsPaintStudio.css` | 155px strip, pill variants, tooltip, icon buttons, capsule, geometry constants | VERIFIED | All values verified by direct read; z-index 60 tooltip; 860px query retains only topbar cosmetic rules (collapse removed) |
| `app/index.html` | Selection guard style block | VERIFIED | Exact two-rule contract, exception rule after base rule |
| `app/src/types/timeline.ts` + `app/src/lib/frameMap.ts` | rotoKeyFrames field + population | VERIFIED | physic-paint-only population from getRotoRealKeyRecords |
| `app/src/components/timeline/TimelineRenderer.ts` | getPhysicPaintRotoKeyMarkerGeometry + drawRotoKeyMarkers | VERIFIED | Geometry mirrors drawKeyframeDiamonds math incl. nonzero inFrame; halfSize 5 |
| `app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts` | canDiscard/discardDisabledReason + DISCARD_REASONS | VERIFIED | Derived inside existing availability computed, no new signal |
| `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx` | Guarded Discard toolbar button | VERIFIED | No native disabled/title; two-part tooltip grammar verbatim |
| `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` + `rotoCoordinatorPorts.ts` + `useRotoKeyUtilities.ts` | addEmptyKey port + availability computeds | VERIFIED | Port :64, computeds :233-234, computeAddEmptyKeyAvailability :648, addKey wrapper wired |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` | onClose via getCurrentWindow().close(); onDiscardScript in scripts props; onAddRotoKey wiring | VERIFIED | :581-583 dynamic Tauri import + window.close fallback; :1018 verbatim discard handler; :1032/1035 wiring |

### Key Link Verification

| From | To | Via | Status |
| --- | --- | --- | --- |
| Strip guarded actions | Controller reason ports | getRotoKeyUtilityDisabledMessage + scriptAvailability/addEmptyKey ports, verbatim two-part grammar | WIRED |
| Capsule text | Existing props/signals | Pure selector over resolved candidates; no new state, no useEffect copying | WIRED |
| Timeline cells | Styled tooltip | RotoTimelineCellButton child component, one hook per cell, data-roto-* attrs preserved | WIRED |
| frameMap fxTrackLayouts | physicPaintStore | getRotoRealKeyRecords; reactivity via existing physicPaintVersion read | WIRED |
| drawFxTrack physic-paint branch | drawRotoKeyMarkers | Gated on fxTrack.rotoKeyFrames?.length | WIRED |
| Studio scripts props | Scripts panel | rotoScript + onDiscardScript (handler moved verbatim); RightPanel spreads unchanged | WIRED |
| Studio workflow props | Strip onClose | handleWorkflowClose -> getCurrentWindow().close() so onCloseRequested flush runs | WIRED |
| index.html guard | PhysicsPaintRightPanel | .physics-paint-log-messages exception target present (liveness test) | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| All 11 phase test suites | `pnpm vitest run <11 files>` | 143/143 tests passed, 11 files | PASS |
| Type safety | `pnpm typecheck` | exit 0, no output | PASS |
| Tooltip 1000ms timing boundary | Fake-timer cases in PhysicsPaintStyledTooltip.test.ts | Not visible at 999ms, visible at 1000ms, instant on focus, Escape/blur/leave dismissal | PASS |
| Capsule arbitration ladder | physicsPaintWorkflowPresentation.test.ts (16 tests) | Pending > saving > most-recent feedback > ambient baseline | PASS |
| addEmptyKey port behavior | useRotoTimelineActions.test.ts (5 tests) | Availability ladder, dispatch shape, occupied rejection | PASS |
| Marker geometry + reactivity | TimelineRenderer.test.ts (11 tests) | Nonzero-inFrame math, real-keys-only, recompute on replaceRotoPhysicalRecords | PASS |
| Claimed commits exist | git log --all | 18/18 spot-checked task commits found | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| 36.15-STRIP-GEOMETRY | 06 | Fixed 155px band stack, 18px cells, 2160px lane, 54px ticks, 14px scrollbar, no responsive collapse | SATISFIED | CSS + derived constants + band-stack contract tests |
| 36.15-GROUP-SEPARATION | 04 (06) | Distinct gray pill islands, borderless blend, align-horizontal icon, 8px gaps | SATISFIED | Pills byte-identical inside approved Tools dropdown; tonal-variant tests |
| 36.15-ICON-ACTIONS | 01 (06) | Icon-only guarded row with tooltips; Discard in Scripts toolbar | SATISFIED | Row/tooltip/guard contract tests (37 strip tests) |
| 36.15-STATUS-CAPSULE | 05 | Legend/stack removed; prioritized elastic capsule; LOG sole diagnostics | SATISFIED | Selector tests + absence greps + RightPanel untouched |
| 36.15-LAYER-KEY-MARKERS | 02 (06) | #F5A623 diamonds per real key on physic-paint FX rows | SATISFIED | Geometry/reactivity/source-contract tests; halfSize 5 fix applied |
| 36.15-SELECTION-GUARD | 03 | index.html guard with exact 4-selector exception list | SATISFIED | 6/6 contract tests incl. liveness/coexistence guardrails |
| 36.15-SCRIPT-CONTROLS | 01, 07 | Guarded ordered script controls with controller reasons; Discard relocated | SATISFIED | Clipboard port tests (4) + panel contract tests (4) + strip negative lock |

No orphaned requirements: REQUIREMENTS.md maps exactly these 7 IDs to Phase 36.15 and all 7 are claimed across plan frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| None | — | Debt-marker scan (TODO/FIXME/XXX/TBD/HACK) across all 10 phase production files: clean. `PLACEHOLDER_BG_A/B` constants in TimelineRenderer.ts are pre-existing checkerboard fill color names for empty frames, not debt markers. | — | — |

### Human Verification Required

Four consolidated native visual UAT items (the documented phase-level human gate, deferred in every plan's coverage D5/D6 and the Plan 02 backstop truth). See frontmatter `human_verification` for the full test/expected/rationale text:

1. **Final strip presentation + Tools dropdown + + Key** — rendered 155px layout, dropdown fit inside the overflow-hidden shell, + Key empty-real-key promotion.
2. **Styled tooltip behavior and layering** — 1000ms hover / instant focus feel, z-index 60 above studio chrome, guarded two-part reason copy.
3. **Canvas Roto key diamonds** — orange diamonds at correct physical frames incl. nonzero-inFrame layers, no overlap, no interaction.
4. **Selection guard non-interference** — LOG/inputs selectable, canvas painting and timeline dragging unaffected.

### Gaps Summary

No automated gaps. All six roadmap success criteria are satisfied at source-contract and unit-behavior level; the 143 phase tests and typecheck pass; the Plan 06 header restructure / + Key / Duplicate relocation / layer-name move and the five follow-up fixes are user-approved scope expansions (verified as implemented, not flagged as deviations). The only open item is the phase-level native visual UAT, which every plan explicitly deferred to this human gate.

---

_Verified: 2026-07-26T00:35:00Z_
_Verifier: Claude (gsd-verifier)_
