---
phase: 36.15-roto-timeline-final-ui-integration
plan: 12
type: execute
wave: 9
gap_closure: true
depends_on: [36.15-11]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 36.15-GROUP-SEPARATION
  - 36.15-STRIP-GEOMETRY

must_haves:
  truths:
    - "No 'Brush color' or 'Tool' tab header text/strip renders in the right sidebar — each section starts directly with its content (UAT Gap H-1/H-2)."
    - "A resize grab handle sits between the brush color content and the tool content, styled like the existing grab above Scripts/Onion/Motion (UAT Gap H-3)."
    - "Brush color, Tool, and Scripts/Onion/Motion sections share the sidebar height in thirds by default and are each resizable via grab handles (UAT Gap H-4)."
    - "The selected cell at frame 0 renders its full left orange border — a small left offset in the timeline lane prevents truncation (UAT Gap H-5)."
    - "The bottom action-row band is taller than 28px with its three groups vertically centered and visible top/bottom padding (user-approved adjustment to the Plan 06 28px band and the 155px total; UAT Gap H-6)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (tab chrome removal, three resizable sections)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lane left offset, taller action-row band)
    - app/src/components/physic-paint/physicsPaintStudio.css (grab handles, thirds layout, band height, centering)
  key_links:
    - "Gap H-6 deliberately relaxes the Plan 06 Fixed Layout Contract (28px action row / 155px total) — update the band-sum contract test in the same commit and document the new numbers in SUMMARY; all other bands stay fixed."
    - "Reuse the existing grab/resize implementation above Scripts/Onion/Motion for the new handles — same visual and pointer behavior, no new dependency."
---

<objective>
Close UAT Gap H — fourth polish round from native UAT (2026-07-26): remove the Brush color and
Tool tab headers, make the three sidebar sections equal-height thirds resizable via grab handles,
fix the truncated left selection border at frame 0 with a small lane offset, and give the bottom
action row more height with centered, padded groups.

Purpose: Final presentation approval pass before the user signs off the phase UAT.
</objective>

<context>
Source of truth: the user's feedback message dated 2026-07-26 with annotated screenshots
(quoted in 36.15-UAT.md Gap H). Plan 36.15-11 created the three tab groups this plan refines
into chrome-less resizable sections. The band-height change is a user-approved relaxation of
the Plan 06 fixed-geometry contract — keep every other band unchanged and update the contract
tests accordingly.
</context>

<tasks>

<task number="1">
<name>Sidebar thirds with grab resize + selection offset + taller bottom band</name>
<read_first>
- app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/physicsPaintStudio.css
- .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-11-SUMMARY.md
</read_first>
<files>
- app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- app/src/components/physic-paint/physicsPaintStudio.css
</files>
<action>
RED: update contract tests (no Brush color / Tool tab header text; grab handle between sections;
three sections default to equal thirds; lane left offset present; new action-row band height and
band sum; groups centered with top/bottom padding).
GREEN:
1. Right sidebar: remove the 'Brush color' and 'Tool' tab header strips — each section renders its
   content directly with no tab chrome. Keep [Scripts, Onion, Motion] tabs as-is.
2. Add a resize grab handle between brush color and tool sections (same look/behavior as the
   existing grab above Scripts/Onion/Motion); the three sections default to equal thirds of the
   available sidebar height and each grab drag resizes its neighbors.
3. Timeline lane: add a small left offset (a few px) so the selected cell at frame 0 shows its full
   left orange border; keep the 18px pitch and ruler alignment consistent with the offset.
4. Bottom action row: increase the band height modestly (e.g. ~34px — pick the smallest height that
   gives clear top/bottom padding), vertically center all three groups, and update the 155px band-sum
   contract test with the new total. All other bands keep their Plan 06 heights.
</action>
<verify>
- pnpm --dir app vitest run (full app suite, never watch mode)
- pnpm --dir app typecheck
</verify>
<commit>feat(36.15-12): sidebar resizable thirds, selection offset, taller bottom band (Gap H)</commit>
</task>

</tasks>

<verification>
- Full app suite green and typecheck exit 0.
- Contract tests lock: no Brush color / Tool tab chrome, grab handle present, thirds default,
  lane offset, new band height/sum, centered groups.
- Drag machinery + remaining Plan 06 geometry guardrails stay green.
</verification>

<success_criteria>
- [ ] All 6 Gap H items fixed
- [ ] Band-contract test updated for the new action-row height (documented deviation)
- [ ] Committed atomically (RED→GREEN)
- [ ] Full suite + typecheck green
</success_criteria>
