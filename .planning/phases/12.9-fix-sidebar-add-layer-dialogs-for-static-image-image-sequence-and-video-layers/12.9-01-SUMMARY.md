---
phase: 12.9-fix-sidebar-add-layer-dialogs-for-static-image-image-sequence-and-video-layers
plan: 01
subsystem: ui
tags: [preact, signals, layer-creation, intent-pattern, import-grid, video]

# Dependency graph
requires:
  - phase: 12.6-layer-sequence-auto-selection-and-key-photo-ux-fixes
    provides: pendingNewSequenceId multi-select pattern, key photo selection flow
provides:
  - AddLayerIntent signal and type in uiStore for intent-driven add-layer flows
  - Simplified AddLayerMenu dispatching intents instead of managing popovers
  - ImportGrid asset filtering (images-only, videos-only, all) and video selection support
  - Film icon badge on all video thumbnails in ImportGrid
  - ImportedView intent-based mode resolution for static-image, image-sequence, and video layer creation
  - Toolbar intent cleanup on mode toggle
affects: [add-layer, imported-view, layer-creation, sidebar]

# Tech tracking
tech-stack:
  added: []
  patterns: [intent-dispatch-pattern, signal-driven-mode-resolution, priority-based-mode-derivation]

key-files:
  created: []
  modified:
    - Application/src/stores/uiStore.ts
    - Application/src/components/layer/AddLayerMenu.tsx
    - Application/src/components/import/ImportGrid.tsx
    - Application/src/components/views/ImportedView.tsx
    - Application/src/components/layout/Toolbar.tsx

key-decisions:
  - "Intent dispatch pattern: AddLayerMenu sets intent signal + switches to imported mode, ImportedView reads intent to derive filter/selection/handlers"
  - "Priority-based mode resolution: addLayerIntent takes priority over pendingNewSequenceId which takes priority over activeSequenceId"
  - "Cancel-delete collision prevention: handleClose checks addLayerIntent before pendingNewSequenceId to avoid deleting pending sequences during add-layer cancel"

patterns-established:
  - "Intent dispatch: UI triggers set intent signal then switch mode, target view reads intent to derive behavior"
  - "Asset filtering: ImportGrid assetFilter prop controls which asset types render (images-only, videos-only, all)"

requirements-completed: [ADDLAYER-INTENT, ADDLAYER-STATIC, ADDLAYER-SEQUENCE, ADDLAYER-VIDEO, ADDLAYER-FILTER, ADDLAYER-CANCEL, ADDLAYER-TOOLBAR]

# Metrics
duration: 4min
completed: 2026-03-18
---

# Phase 12.9 Plan 01: Fix Sidebar Add Layer Dialogs Summary

**Intent-driven add-layer flows replacing popover dialogs with full ImportedView grid for static-image, image-sequence, and video layer creation**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-18T19:53:17Z
- **Completed:** 2026-03-18T19:57:39Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Replaced three popover-based add-layer dialogs with full-window Imported view flows using intent dispatch pattern
- AddLayerMenu simplified from 392 lines to 75 lines (81% reduction) by removing all popovers, layer creation logic, and Tauri plugin imports
- ImportGrid extended with asset filtering (images-only, videos-only, all) and clickable video thumbnails with Film icon badge
- ImportedView resolves mode from addLayerIntent signal with correct priority over existing key photo flows
- Cancel in add-layer flows clears intent without triggering pending sequence deletion
- Toolbar imported button clears stale intent on toggle

## Task Commits

Each task was committed atomically:

1. **Task 1: Add addLayerIntent signal, simplify AddLayerMenu, extend ImportGrid** - `d153702` (feat)
2. **Task 2: Refactor ImportedView for intent-based mode resolution, layer creation, Toolbar cleanup** - `3aae8f0` (feat)

## Files Created/Modified
- `Application/src/stores/uiStore.ts` - Added AddLayerIntent type, addLayerIntent signal, setter, and reset
- `Application/src/components/layer/AddLayerMenu.tsx` - Rewritten as simple intent dispatcher (75 lines, no popovers)
- `Application/src/components/import/ImportGrid.tsx` - Added assetFilter, onVideoSelect props, Film badge on video thumbnails
- `Application/src/components/views/ImportedView.tsx` - Intent-based mode resolution, three layer creation handlers, intent-aware close/import
- `Application/src/components/layout/Toolbar.tsx` - Clears addLayerIntent when toggling away from imported mode

## Decisions Made
- Intent dispatch pattern: AddLayerMenu sets intent signal + switches to imported mode, ImportedView reads intent to derive filter/selection/handlers
- Priority-based mode resolution: addLayerIntent takes priority over pendingNewSequenceId which takes priority over activeSequenceId
- Cancel-delete collision prevention: handleClose checks addLayerIntent before pendingNewSequenceId to avoid deleting pending sequences during add-layer cancel

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All add-layer flows work through the full Imported view with proper filtering
- All existing flows (key photo single-select, pending sequence multi-select, Imported browsing) remain functional
- TypeScript compiles clean, all 74 vitest tests pass, production build succeeds

## Self-Check: PASSED

All 5 modified files verified present. Both task commits (d153702, 3aae8f0) verified in git log.

---
*Phase: 12.9-fix-sidebar-add-layer-dialogs-for-static-image-image-sequence-and-video-layers*
*Completed: 2026-03-18*
