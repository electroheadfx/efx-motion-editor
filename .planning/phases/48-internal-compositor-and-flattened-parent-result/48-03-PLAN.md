---
phase: 48-internal-compositor-and-flattened-parent-result
plan: 03
type: tdd
wave: 3
depends_on: ["48-01", "48-02", "48-04"]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/lib/previewRenderer.ts
  - app/src/lib/previewRenderer.test.ts
  - app/src/lib/exportEngine.ts
  - app/src/lib/exportEngine.test.ts
  - app/src/stores/physicPaintStore.test.ts
autonomous: true
requirements: [CMP-01, CMP-03, CMP-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "physicPaintStore.getFlattenedFrame(layerId, frame) returns a PreviewPhysicPaintFrameSource-compatible record { layerId, frame, cacheKey, renderedFrame } carrying the flattened straight-alpha raster as a dataUrl plus the derived flattened cache key, extended with a missing-source report field (D-11; Open Question 3 resolved: physicPaintStore location, existing renderer-facing shape extended, no new transport)"
    - "previewRenderer's physic-paint path resolves content ONLY through getFlattenedFrame — collectPhysicPaintFrameSources, the hasDrawable check, and the main draw branch (previewRenderer.ts:313-322, 396-399, 497-525) all consume the flattened delivery; the main renderer never iterates internal tracks (D-11, CMP-01)"
    - "Parent opacity/blend application stays exclusively at the existing parent sites (the save/compositeOp/globalAlpha/drawImage/restore block at previewRenderer.ts:519-524); the compositor never reads parent properties — parent 50% × internal 50% = 25% effective (CMP-03, Pitfall 6)"
    - "blendModeToCompositeOp is exported from previewRenderer.ts and wired into the compositor's compositeOp port — the switch is never duplicated (Pitfall 8)"
    - "The flattened path never emits placeholder/diagnostic pixels: a missing source is transparent in the flattened raster and surfaced via the Studio status capsule (D-09); the D-28 stripe fill is excised from the previewRenderer physic-paint branch"
    - "The 43-09 export preflight (exportEngine.ts findUnresolvedExportLoop) scans ALL participating tracks per the 48-01 truth table — no longer only the document's activeTrackId — so an unresolvable Hold loop on a visible non-active track still blocks export with the locked error message (CMP-05; Pitfall P-48-4 retention decision, flagged for user confirmation in 48-06)"
    - "Main-editor parity: a single-track document's flattened output is pixel-equivalent to the previous active-track render path, including the per-track paper-background composite (drawRotoFrameComposite semantics preserved inside the per-track content resolution — DOC-06 carried into CMP-01)"
    - "Decode economics mirror the existing imageCache idiom: per-track images decode once per (trackId, contentRevision, frame) key; a frame whose track images are still decoding returns null for that tick and re-renders on the decode-complete signal — no async gap in the sync renderer (48-RESEARCH.md Pattern: previewRenderer.ts:726-747)"
  assumptions: []
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "getFlattenedFrame delivery + production compositor ports + per-track decode cache"
      exports: ["getFlattenedFrame"]
    - path: "app/src/lib/previewRenderer.ts"
      provides: "flattened-only physic-paint branch + exported blend map"
      exports: ["blendModeToCompositeOp"]
  key_links:
    - "physicPaintStore.getFlattenedFrame → efx-paint/compositor/compositeFrame with the production ports — the only seam previewRenderer.ts calls for physic-paint content (D-11)"
    - "exportEngine.ts findUnresolvedExportLoop → participatingPaintTracks(document) from efxPaintHideSolo.ts — the preflight scans exactly the tracks the compositor would draw"
    - "previewRenderer.ts imageCache decodes the FLATTENED dataUrl keyed by the flattened cache key; the per-track decode cache lives store-side (D-07) and mirrors imageCache's loadingImages/failedImages semantics"
  prohibitions:
    - "Flattened output and export MUST NOT contain placeholder or diagnostic pixels (the missing-source stripe pattern) — a missing source renders transparent and is surfaced through the status capsule (D-09)"
---

<objective>
Wire the compositor into the live delivery seams: the store's `getFlattenedFrame(layerId, frame)` (D-11) with the production ports (D-10 content resolution, per-track decode cache, flattened memo, paper-background parity), the previewRenderer branch replacement (flattened-only physic-paint path, exported blend map, placeholder-arm excision per D-09), and the export preflight generalized from active-track-only to all participating tracks (CMP-05).

Purpose: this is the CMP-01 keystone seam — after this plan, Studio-independent surfaces (main preview AND export, which routes through PreviewRenderer.renderFrame per exportRenderer.ts:1) consume the same shared composition path, and the main renderer never knows internal tracks exist.

Output: `getFlattenedFrame` live in physicPaintStore; previewRenderer + exportEngine switched; all seams proven by contract tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-RESEARCH.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-PATTERNS.md
@app/src/stores/physicPaintStore.ts
@app/src/lib/previewRenderer.ts
@app/src/lib/previewRenderer.test.ts
@app/src/lib/exportEngine.ts
@app/src/efx-paint/compositor/efxPaintCompositor.ts
@app/src/efx-paint/compositor/efxPaintHideSolo.ts
@app/src/efx-paint/compositor/efxPaintCompositeCache.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getFlattenedFrame(layerId, frame) in physicPaintStore — production ports, per-track decode cache, flattened memo, paper-background parity</name>
  <reversibility rating="costly">D-11 delivery mechanism: changing it later touches previewRenderer.ts and the store together. User-locked in CONTEXT.md; recorded, no checkpoint.</reversibility>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:2326-2450 (getRotoPhysicalRenderSource — the delivery shape to mirror: guard-first entry, contentRevision alongside content, revision-aware cacheRevision; verbatim conventions in 48-PATTERNS.md)
    - app/src/stores/physicPaintStore.ts:1133-1135 (getFrame — the non-roto content path) and 487-519 (identity-comparison memo idiom)
    - app/src/lib/previewRenderer.ts:139-149 (PreviewPhysicPaintFrameSource — the compatible return shape) and 255-257, 726-747 (imageCache decode-once semantics to mirror store-side)
    - app/src/lib/previewRenderer.ts:118-137, 220-233, 513-523 (getMissingRotoBackgroundState / resolveMissingRotoFrameDrawForLayer / resolvePhysicalRotoFrameBackgroundDrawForLayer / drawRotoFrameComposite — the paper-background composite the per-track content arm must preserve per-track, keyed by that track's _rotoBackgroundMetadata)
    - app/src/efx-paint/compositor/efxPaintCompositor.ts + efxPaintCompositeCache.ts (48-01 ports, key derivation, memo)
    - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (48-02 deriveEfxPaintBackgroundResolution / resolveEfxPaintBackgroundFrame — the Background port wiring)
    - app/src/types/physicPaint.ts:1773-1785 (PhysicPaintRenderedFrame shape the dataUrl payload must satisfy)
  </read_first>
  <behavior>
    - RED Test 1 (guard): getFlattenedFrame('unknown-layer', 5) returns null; non-integer/negative frame returns null (getRotoPhysicalRenderSource guard convention).
    - RED Test 2 (single-track parity): a document with one Paint track holding a real key at frame 5 → getFlattenedFrame returns { layerId, frame: 5, cacheKey } whose renderedFrame dataUrl decodes to the composite of fallback + that track's frame — asserted via the recording/stubbed canvas harness (decode stub), NOT via the old active-track accessor.
    - RED Test 3 (multi-track): two visible tracks with content at frame 5 → the flattened raster draws both in order; cacheKey equals deriveEfxPaintFlattenedCacheKey over the same inputs.
    - RED Test 4 (hidden track excluded): hiding track B (via the document) → B's pixels absent from the flattened raster and B's content revision absent from the key.
    - RED Test 5 (missing → transparent + report): a track whose resolution is null/loop-placeholder at frame 5 → result carries missing: [{ trackId, frame: 5, ... }] and the raster has no contribution from that track.
    - RED Test 6 (cache hit): two calls with unchanged state → the second returns the cached record (same renderedFrame reference), zero port recompute.
    - RED Test 7 (decode pending): with the image-decode stub left unresolved, the call returns null for that tick; after the stub fires onload and the decode-complete bump, the next call returns the raster (imageCache pending semantics mirrored).
    - RED Test 8 (paper background parity): a roto track with paper background metadata composites paper + frame pixels into the per-track contribution exactly as drawRotoFrameComposite does today (single-track document output equals the pre-48 render for the same track).
    - RED Test 9 (background port wiring): a document whose background carries a clip resolving content at frame 5 (built directly in the test) → the port's resolveBackgroundFrame returns the 48-02 resolution for that frame, with knownSources fed from the store-side resolvable source set; a clip ref with no resolvable bytes maps to { kind:'missing' }.
    - RED Test 10 (background source-image port, store-side wiring of the 48-04 port): ports.resolveBackgroundSourceImage(sourceRef) returns the decoded raster for a sourceRef with resolvable runtime bytes; an unknown/unresolvable ref returns null; while the decode is pending the call returns null for that tick and the next call after the onload-triggered physicPaintVersion bump returns the raster (same decode-economics semantics as Test 7, keyed by the sourceRef + its content terms).
  </behavior>
  <action>
    Add getFlattenedFrame(layerId: string, frame: number) to the physicPaintStore object literal (D-11; 48-RESEARCH.md Responsibility Map — the store owns the runtime maps the ports need). Implementation: guard-first (Test 1). Read the document via efxPaintStore.getDocument(layerId); null → null. Compute participatingPaintTracks(document) + backgroundParticipates(document) from the 48-01 module. Collect per-track content revisions from _resolveRotoPhysicalStructural(layerId, trackId).contentRevision for roto tracks or the dataUrl-slice idiom for cached-frame tracks (previewRenderer.ts:175). Compute the flattened key via deriveEfxPaintFlattenedCacheKey; consult a per-layer flattened memo (createKeyedMemo instance per layerId, cleared in _clearLayerState/reset alongside the structural cache). On miss: build the production EfxPaintCompositorPorts — createCanvas via document.createElement('canvas') at projectStore.width/height.peek() (Open Question 1: parent project canvas dims are the size authority); compositeOp ← the now-exported blendModeToCompositeOp; resolveTrackContent(trackId, frame) implementing D-10 precedence exactly as the current single-track path does (isPhysicalRotoWorkflowLayer → getRotoPhysicalRenderSource(layerId, trackId, frame) with the loop-placeholder/null → { kind:'missing' } mapping, else getFrame) and composing the track's paper background per its OWN _rotoBackgroundMetadata entry (the drawRotoFrameComposite semantics moved per-track — Test 8); resolveBackgroundFrame(frame) wired to the 48-02 adapter — deriveEfxPaintBackgroundResolution(document.background, capacity) once per background revision (capacity = the parent sequence frame count) then resolveEfxPaintBackgroundFrame per frame, with knownSources = the set of clip sourceFrameRefs that resolve to runtime bytes store-side (Test 9); until Phase 49's import UI exists, production documents carry empty background.clips so the port resolves 'gap' and the fallback shows — the wiring is proven by test-built documents. Also implement ports.resolveBackgroundSourceImage(sourceRef) store-side — the production wiring of the port 48-04 added to EfxPaintCompositorPorts (this plan depends on 48-04 for that port shape): decode the referenced source image through the same store-side decode-cache idiom keyed by the sourceRef plus its content terms, pending decode → null this tick, onload bumps physicPaintVersion (Test 10). Decode: per-track rasters decode dataUrl → image through a NEW store-side cache keyed by deriveEfxPaintTrackContentKey, mirroring imageCache's loadingImages/failedImages sets + an onload that bumps physicPaintVersion (never a new subscription surface — the existing clock, MEMORY: always bump AND subscribe); any pending decode → return null this tick (Test 7). Composite synchronously when all rasters are ready; encode the flattened canvas via toDataURL into a PhysicPaintRenderedFrame-shaped payload; store the frozen result { layerId, frame, cacheKey, renderedFrame, missing } in the flattened memo and return it. Keep getRotoPhysicalRenderSource and getFrame signatures untouched — existing Studio/onion consumers keep working. All 10 tests RED then GREEN. Commits: test(48-03): failing getFlattenedFrame tests; then feat(48-03): flattened frame delivery in physicPaintStore.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 10 tests pass; suite exits 0
    - physicPaintStore.ts exports getFlattenedFrame and the return record carries layerId/frame/cacheKey/renderedFrame/missing
    - The production ports include the store-side resolveBackgroundSourceImage implementation (48-04 port shape) backed by the same decode-cache idiom as the per-track rasters
    - The flattened memo and per-track decode cache are cleared by the same layer-teardown/reset paths as the structural cache
    - No new signal is introduced for decode completion — the onload path bumps the existing physicPaintVersion clock
    - Idempotent discipline: a repeat call with unchanged inputs never bumps any revision signal (efx-preact-reactivity skill §3)
  </acceptance_criteria>
  <done>The store delivers one flattened straight-alpha raster per frame with D-10 content precedence, per-track decode caching, D-08 memoization, missing-source reporting, and paper-background parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: previewRenderer flattened seam — branch replacement, blendModeToCompositeOp export, placeholder-arm excision</name>
  <reversibility rating="one-way">D-02 straight-alpha boundary becomes load-bearing here: the main editor consumes the flattened raster under the unmultiplied-alpha contract; every alpha seam is tested against it. User-locked in CONTEXT.md; recorded, no re-ask checkpoint.</reversibility>
  <files>app/src/lib/previewRenderer.ts, app/src/lib/previewRenderer.test.ts</files>
  <read_first>
    - app/src/lib/previewRenderer.ts:76-178 (blendModeToCompositeOp to export; resolvePhysicPaintTrackVisibility and the active-track frame-source resolver at 152-178 being superseded)
    - app/src/lib/previewRenderer.ts:180-218 (the placeholder-source resolver + stripe fill — excised from the physic-paint branch per D-09) and 300-322 (collectRotoPaperTextures + collectPhysicPaintFrameSources preload path) and 393-399 (the hasDrawable check) and 497-525 (the main physic-paint draw branch)
    - app/src/lib/previewRenderer.test.ts:124-230 (RecordingCanvasContext harness + the source-contract readSource/toContain seam-test idiom at 215-230)
    - app/src/lib/exportRenderer.ts:1, 180-358 (export routes through PreviewRenderer.renderFrame — one seam change covers main preview AND export)
  </read_first>
  <behavior>
    - RED Test 1 (seam contract, behavioral): with physicPaintStore mocked, a physic-paint layer render calls getFlattenedFrame(paintLayerId, lookupFrame) exactly once and never calls getRotoPhysicalRenderSource or getFrame from the renderer.
    - RED Test 2 (parent application intact): the parent draw block still applies effectiveOpacity and blendModeToCompositeOp(layer.blendMode) around the flattened image draw; a parent 50% opacity × internal 50% opacity scenario records effective alpha 0.25 at the parent draw only (CMP-03 — the compositor's internal alpha ops are asserted in 48-01; this test pins the renderer never re-applies internal track properties).
    - RED Test 3 (null → no draw): getFlattenedFrame returning null (no document / decode pending) → the branch draws nothing and contributes false to hasDrawable, matching the previous no-content behavior.
    - RED Test 4 (preload path): collectPhysicPaintFrameSources returns the flattened source record so preloadPhysicPaintFrames decodes the flattened dataUrl via the existing imageCache.
    - RED Test 5 (no placeholder pixels): a missing Hold source frame renders through the flattened raster (transparent contribution) — the recording context never records the stripe-fill fillRect pattern; the placeholder draw path is unreachable from the physic-paint branch (D-09).
  </behavior>
  <action>
    Export blendModeToCompositeOp from previewRenderer.ts (currently module-private at 76-91 — export it, do not duplicate). Replace the three consumer sites of the superseded active-track frame-source resolver (previewRenderer.ts:313-322 collectPhysicPaintFrameSources, :396-399 hasDrawable check, :497-525 main branch) with the flattened delivery: source ← physicPaintStore.getFlattenedFrame(paintLayerId, physicPaintLookupFrame); null → no draw / not drawable; non-null → existing getPhysicPaintImageSource(source) decode + the UNCHANGED parent save/compositeOp/globalAlpha/drawImage/restore block at 519-524 (Pitfall 6 — the parent application sites stay exactly where they are). Excise the placeholder arm (504-512) and the placeholder-source resolver/stripe-fill helpers (180-218) from the flattened path — D-09: missing sources are transparent in the raster and surface via the Studio capsule; if hasMissingRotoBackground/resolvePhysicalRotoFrameBackgroundDrawForLayer become dead after the per-track paper parity moved into the store (Task 1), remove them; otherwise keep them ONLY for any non-flattened consumer that remains (grep first). Keep resolvePhysicPaintTrackVisibility exported — PhysicsPaintStudio.tsx:25,2104 still consumes it for the active-track editing surface until 48-05. Keep the `void physicPaintVersion.value` subscription idiom at 444. The superseded active-track resolver (152-178) and its cache-key helper usage are removed; getPreviewPhysicPaintFrameCacheKey stays for the flattened source's cacheKey passthrough. All 5 tests RED then GREEN. Commits: test(48-03): failing flattened seam tests; then feat(48-03): previewRenderer consumes getFlattenedFrame only.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/previewRenderer.test.ts src/lib/exportRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 seam tests pass; the exportRenderer suite stays green (export parity via the shared seam)
    - previewRenderer.ts exports blendModeToCompositeOp and the compositor store port consumes that export (no second switch exists anywhere — grep across app/src for "case 'multiply'" finds exactly one mapping)
    - The physic-paint draw branch contains no active-track content resolution: sed -n of the branch region (the block after `layer.type === 'physic-paint'`) contains getFlattenedFrame and not the superseded resolver name
    - The parent opacity/blend block at the physic-paint branch is byte-equivalent in structure to the pre-change save/compositeOp/alpha/drawImage/restore shape (CMP-03)
  </acceptance_criteria>
  <done>Main preview and export consume only the flattened delivery; parent application is unchanged and exclusive; no placeholder pixels can reach the flattened path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export preflight generalized to all participating tracks (43-09 block retained)</name>
  <files>app/src/lib/exportEngine.ts, app/src/lib/exportEngine.test.ts</files>
  <read_first>
    - app/src/lib/exportEngine.ts:50-160 (findUnresolvedExportLoop — the active-track-only scan at the getEfxPaintDocument(paintLayerId)?.activeTrackId lines, plus the locked error-message construction and earliest-first ordering)
    - app/src/stores/physicPaintStore.ts:2277 (getRotoPhysicalUnresolvedLoops signature)
    - app/src/efx-paint/compositor/efxPaintHideSolo.ts (participatingPaintTracks — the exact track set the compositor draws)
  </read_first>
  <behavior>
    - RED Test 1 (non-active participating track scanned): a document with tracks A (active) and B (visible, not active) where B holds an unresolvable Hold loop in the export range → preflight blocks with the locked message naming B's loop (before this change the scan missed B).
    - RED Test 2 (hidden track not scanned): B hidden → its unresolvable loop does NOT block export (the compositor would not draw B — scan set equals participating set).
    - RED Test 3 (solo narrows the scan): A soloed, B visible-not-soloed with an unresolvable loop → no block (B does not participate per the truth table).
    - RED Test 4 (locked message preserved): the blocked message for the active track's unresolved loop is byte-identical to the pre-change copy; earliest-global-placement ordering and loopId tiebreak unchanged.
  </behavior>
  <action>
    In exportEngine.ts findUnresolvedExportLoop, replace the active-track-only resolution (`getEfxPaintDocument(paintLayerId)?.activeTrackId ?? ''` at both the unresolved-loops query and the clip lookup) with an iteration over participatingPaintTracks(document) imported from efx-paint/compositor/efxPaintHideSolo — for each participating track call physicPaintStore.getRotoPhysicalUnresolvedLoops(paintLayerId, track.id, localFrom, localTo) and collect hits with their trackId; the clip lookup for message construction uses the hit's own trackId. Keep the seqStart translation, the locked error copy, and the earliest-first + loopId tiebreak ordering byte-identical (add track.id as a final tiebreak term after loopId so cross-track ordering is deterministic). This is the P-48-4 retention decision: the preflight stays a hard block (stricter than D-09 transparency, consistent with CMP-05 'explicit and recoverable'); it is flagged for user confirmation at the 48-06 UAT. All 4 tests RED then GREEN. Commits: test(48-03): failing participating-tracks preflight tests; then feat(48-03): export preflight scans all participating tracks.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/exportEngine.test.ts && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; full suite green; typecheck green
    - The preflight's track set is exactly participatingPaintTracks(document) — hidden and non-soloed-under-solo tracks cannot block, visible participating tracks always can
    - The locked export-blocked message copy is unchanged (Test 4)
  </acceptance_criteria>
  <done>Export never silently ships a flattened raster whose visible non-active track had an unresolvable loop; hidden/excluded tracks never false-block.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New symbols (physicPaintStore.ts): `getFlattenedFrame(layerId, frame)` returning `PreviewPhysicPaintFrameSource & { missing: readonly EfxPaintMissingSourceReport[] }`; internal per-layer flattened memo instances; internal per-track decode cache (loadingImages/failedImages sets keyed by `deriveEfxPaintTrackContentKey`)
- New exports (previewRenderer.ts): `blendModeToCompositeOp`
- Modified: `app/src/stores/physicPaintStore.ts`, `app/src/lib/previewRenderer.ts`, `app/src/lib/exportEngine.ts`, `app/src/lib/previewRenderer.test.ts`, `app/src/stores/physicPaintStore.test.ts`, `app/src/lib/exportEngine.test.ts` (created if absent)
- Removed (dead after seam switch): the active-track-only frame-source resolver and the placeholder-source resolver + stripe-fill helpers in previewRenderer.ts (conditional on grep proving no remaining consumer)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compositor → renderer/export | the flattened raster crosses into the main-editor boundary as straight-alpha RGBA; the renderer must treat it as final internal content (parent properties only) |
| export preflight scan set | the preflight's authority is the same truth table the compositor draws with; divergence silently ships or silently blocks content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-07 | Tampering | stale flattened cache serving wrong pixels after an edit | high | mitigate | derived-revision keys (48-01 Task 2) + Task 1 Test 6/7 cache-hit/pending semantics; invalidation matrix completed in 48-04 Task 2 |
| T-48-08 | Tampering | placeholder pixels leaking into export | high | mitigate | placeholder arm excised from the flattened path (Task 2 Test 5); missing → transparent + report (D-09); preflight hard-block retained and generalized (Task 3) |
| T-48-09 | Information Disclosure (content correctness) | preflight scanning only the active track | medium | mitigate | participatingPaintTracks is the single scan authority (Task 3 Tests 1-3); the compositor and the preflight cannot diverge |
| T-48-10 | Tampering | double application of parent opacity/blend | high | mitigate | parent sites untouched and exclusive (Task 2 Test 2 — 25% contract); the compositor never reads parent layer properties (48-01 prohibition) |
| T-48-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate — NOT APPLICABLE: zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/stores/physicPaintStore.test.ts src/lib/previewRenderer.test.ts src/lib/exportEngine.test.ts src/lib/exportRenderer.test.ts` — touched suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- getFlattenedFrame is the only content seam the main renderer uses for physic-paint layers (CMP-01, D-11).
- Parent opacity/blend applied exactly once by the main editor; internal opacity/blend exactly once inside the compositor (CMP-03).
- Missing sources are transparent + reported; export preflight covers every participating track (CMP-05, D-09).
- Export parity holds through the unchanged PreviewRenderer.renderFrame route.
</success_criteria>

<output>
Create `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-03-SUMMARY.md` when done
</output>
