---
phase: 48-internal-compositor-and-flattened-parent-result
plan: 04
type: tdd
wave: 2
depends_on: ["48-01", "48-02"]
files_modified:
  - app/src/efx-paint/compositor/efxPaintCompositor.ts
  - app/src/efx-paint/compositor/efxPaintCompositor.test.ts
  - app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts
autonomous: true
requirements: [CMP-04, CMP-06]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The composite order is exactly: document fallback → Background contribution (only when backgroundParticipates) → participating Paint tracks bottom-to-top (spec composition order steps 1-8, D-04)"
    - "A Background gap reveals the document fallback (solid color or transparency) in the composite — the same pixels the flattened output, main preview, and export will show (BKG-06 semantics resolved now per D-03; Pitfall 13)"
    - "A Background clip resolving content at a frame draws the referenced source image beneath all Paint tracks; a missing Background source contributes transparent pixels AND a missing report entry with the clip's refs (D-09 chain from 48-02)"
    - "Per-track raster results are cached keyed by deriveEfxPaintTrackContentKey(trackId, contentRevision, frame); editing track B never recomputes or redraws track A's cached raster (D-07)"
    - "The flattened cache key changes when ANY participating input changes: a track content revision, track order/visibility/solo/opacity/blendMode, a Background clip add/edit/repeat change, background fallback, or background visibility (CMP-04, Pitfall P-48-3)"
    - "An infinite Background loop is bounded by the capacity (parent end) and never materializes per-frame records — the composite queries the derived context per frame (Pitfall 11, carried from 48-02)"
  assumptions:
    - "flagged assumption (edge CMP-04 unclassified, status unresolved): the complete cache-invalidation dependency set cannot be auto-resolved by the probe; it is covered by the Task 2 invalidation matrix tests enumerating every dependency class named in CMP-04"
  artifacts:
    - path: "app/src/efx-paint/compositor/efxPaintCompositor.ts"
      provides: "Background step inside compositeFrame + per-track raster memo consumption"
      exports: ["compositeFrame"]
  key_links:
    - "compositeFrame's Background step consumes ports.resolveBackgroundFrame (48-01 hook) whose production wiring is the 48-02 adapter via the 48-03 store port — the compositor never maps FrameLoopClip records itself (Pitfall P-48-2 separation)"
    - "deriveEfxPaintFlattenedCacheKey's background terms (background.revision + per-clip revisions + fallback via buildEfxPaintCompositeRevision) are what make a Background clip edit invalidate the flattened raster (CMP-04)"
  prohibitions: []
---

<objective>
Fill the compositor's Background step (D-03): consume the 48-02 per-frame resolution through the 48-01 port, draw resolved Background content beneath all Paint tracks over the fallback, map gaps to fallback and missing refs to transparent+report, and complete the CMP-04 invalidation matrix — per-track raster memo isolation plus the full flattened-key dependency coverage.

Purpose: after this plan the composite pass implements the spec's composition order end-to-end (steps 1-9) and the cache architecture (D-07/D-08) is provably correct against every CMP-04 dependency class — the performance and correctness foundation the Studio monitor (48-05) and pixel matrix (48-06) stand on.

Output: extended `efxPaintCompositor.ts` with the Background step + matrix tests green.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-RESEARCH.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-PATTERNS.md
@app/src/efx-paint/compositor/efxPaintCompositor.ts
@app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts
@app/src/efx-paint/compositor/efxPaintCompositeCache.ts
@app/src/efx-paint/document/efxPaintDocument.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Background step in compositeFrame — content beneath Paint tracks, gap → fallback, missing → transparent + report</name>
  <files>app/src/efx-paint/compositor/efxPaintCompositor.ts, app/src/efx-paint/compositor/efxPaintCompositor.test.ts</files>
  <read_first>
    - app/src/efx-paint/compositor/efxPaintCompositor.ts (48-01 pipeline — the resolveBackgroundFrame hook currently exercised on the gap arm)
    - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (48-02 EfxPaintBackgroundFrameResolution union: content / gap / missing)
    - SPECS/milestone-v1.0.0-plan.md:443-457 (composition order steps 1-3: fallback, then Background clip resolution, then Background beneath all Paint tracks)
    - app/src/lib/previewRenderer.test.ts:45-127 (RecordingCanvasContext — assert Background draw op lands before any track draw op)
  </read_first>
  <behavior>
    - RED Test 1 (content beneath tracks): background resolution returns { kind:'content', clipId, sourceRef } and ports.resolveBackgroundSourceImage(sourceRef) returns a raster → the recording context shows: fallback fill (if solid), then the background drawImage at 0,0 with globalAlpha 1 and 'source-over', then the Paint track draws — background never re-scaled by track opacity (D-04: Background has no opacity/blend; its draw is a plain source-over).
    - RED Test 2 (hidden background): backgroundParticipates false → no background draw op; the fallback still paints (fallback is document-owned, independent of background.visible).
    - RED Test 3 (gap): resolution { kind:'gap' } → no background draw op; the fallback pixels remain visible (Test 3 asserts no extra fill over the fallback).
    - RED Test 4 (missing): resolution { kind:'missing', clipId, missingRefs } → no draw op; the result's missing[] gains an entry carrying the clipId and refs (D-09 chain).
    - RED Test 5 (solo keeps background): a soloed Paint track + visible background → background still draws beneath the soloed track (D-04).
    - RED Test 6 (infinite loop bound): an infinite clip composited at frames 0, 19, 20 with capacity 20 → 0 and 19 draw content, 20 resolves gap (parent end).
  </behavior>
  <action>
    Extend compositeFrame's Background arm (the 48-01 ports.resolveBackgroundFrame hook) to handle all three resolution kinds. Extend EfxPaintCompositorPorts with resolveBackgroundSourceImage(sourceRef) returning a decoded raster or null — this plan owns ONLY the port type extension and its compositor-side consumption (exercised through test-stub ports); the store-side production implementation is owned by 48-03 Task 1, which depends on this plan for the port shape (a pending decode there returns null this tick, mirroring 48-03 Test 7/10 semantics). Draw order per spec steps 1-3: fallback fill first (unchanged), then when backgroundParticipates(document) and the resolution is content, draw the background raster at 0,0 with globalAlpha 1 and 'source-over' inside save/restore; then the participating Paint tracks exactly as 48-01. kind 'gap' → nothing drawn (fallback shows); kind 'missing' → nothing drawn + missing report entry { trackId: document.background.id, frame, missingRefs } so the capsule path treats background-missing like any track-missing (D-09). Background never participates in solo/opacity/blend (D-04 — no opacity field exists on BackgroundTrack; do not invent one). All 6 tests RED then GREEN. Commits: test(48-04): failing background composite-step tests; then feat(48-04): background contribution in the composite pass.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass; the compositor suite exits 0
    - Recording-fixture op order proves: fallback → background → tracks (Test 1); hidden background suppresses only the background draw (Test 2); solo never suppresses the background (Test 5)
    - Missing background sources produce a report entry keyed by the background track id and zero draw ops (Test 4, D-09)
    - The compositor file gains no FrameLoopClip mapping logic — it consumes the 48-02 resolution union only (grep of efxPaintCompositor.ts for 'FrameLoopClip' finds nothing outside comments)
  </acceptance_criteria>
  <done>The composite pass implements spec steps 1-3 fully: fallback, resolved Background content beneath all Paint tracks, gaps revealing fallback, missing sources transparent + reported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CMP-04 invalidation matrix — per-track memo isolation + full flattened-key dependency coverage</name>
  <files>app/src/efx-paint/compositor/efxPaintCompositor.test.ts, app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts</files>
  <read_first>
    - app/src/efx-paint/compositor/efxPaintCompositeCache.ts (48-01 key derivation — the matrix exercises every input class)
    - app/src/efx-paint/compositor/efxPaintCompositor.ts (the memo integration from 48-01 Task 3 + Task 1's background step)
    - 48-RESEARCH.md Pitfall P-48-3 (the under-coverage failure mode this matrix exists to kill)
  </read_first>
  <behavior>
    - RED Test 1 (per-track isolation): tracks A and B cached at frame 5; bump only B's content revision term → next composite re-queries B's content port and redraws B, while A's cached raster reference is reused (spy counts: A's resolve = 0 new calls, B's = 1) (D-07).
    - RED Test 2 (track content invalidation): editing track B's content changes the flattened key (cache miss) while track A's content-key term is byte-identical.
    - RED Test 3 (config invalidation): toggling B's visible / solo / opacity / blendMode / order each changes the flattened key (5 sub-cases).
    - RED Test 4 (background clip invalidation): adding a clip, changing a clip's repeat count, and changing a clip's revision each changes the flattened key.
    - RED Test 5 (fallback + visibility invalidation): flipping background.fallback (transparent ↔ solid '#112233') and toggling background.visible each changes the flattened key.
    - RED Test 6 (non-participating isolation): a HIDDEN track's content revision term does not appear in the participating key computation used for the composite (changing hidden track B's content does not change the flattened result for the currently-participating set — document the chosen semantics: the key includes the config hash which covers visibility; content terms cover participating tracks only).
  </behavior>
  <action>
    Write the invalidation matrix as parameterized tests over the 48-01/48-04 modules: build a two-track document with a Background clip, compute deriveEfxPaintFlattenedCacheKey / run compositeFrame, apply ONE input-class change per case, assert the key/result deltas exactly as each RED test states. Where a content term is supplied by the caller (the store computes per-track content revisions), the tests drive the same input record shape the store produces. Test 6 pins the participating-only content-term semantics (config hash already flips on visibility, so a hidden track's content edits cannot cause stale VISIBLE pixels; the re-composite on re-showing is guaranteed because the config hash changed when it was hidden). All 6 tests RED then GREEN. Commits: test(48-04): failing invalidation matrix; then feat(48-04): CMP-04 invalidation matrix coverage.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 6 matrix tests pass; compositor suite, full suite, and typecheck green
    - Every CMP-04 dependency class has exactly one failing-then-passing matrix row: track content, order, visibility, solo, opacity, blendMode, background clip add/edit/repeat, fallback, background visibility, frame
    - Per-track memo isolation proven by port spy counts (Test 1)
  </acceptance_criteria>
  <done>Cache invalidation is provably complete against CMP-04 — no participating change can serve a stale flattened raster, and single-track edits recompute only that track.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New symbols: `EfxPaintCompositorPorts.resolveBackgroundSourceImage(sourceRef)` port method; background-step internals inside `compositeFrame` (no new exports beyond the port shape)
- Modified: `app/src/efx-paint/compositor/efxPaintCompositor.ts`, `app/src/efx-paint/compositor/efxPaintCompositor.test.ts`, `app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background resolution → composite pass | the 48-02 union narrows what the pass can draw; a resolution that fabricates a source is impossible by construction (knownSources oracle) |
| cache key ↔ participating inputs | the key is the integrity boundary between an edit and the pixels it should invalidate; under-coverage serves stale pixels (Pitfall P-48-3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-11 | Tampering | stale flattened raster after a Background clip/fallback edit | high | mitigate | Task 2 matrix rows 4-5 pin key rotation on clip add/edit/repeat, fallback flips, and visibility toggles (CMP-04) |
| T-48-12 | Denial of Service | infinite Background loop composite cost | high | mitigate | per-frame query over the derived context, capacity-bounded (Task 1 Test 6); flattened memo absorbs playback redraws (D-08) |
| T-48-13 | Tampering | per-track cache cross-contamination | high | mitigate | content keys carry trackId + contentRevision + frame (Task 2 Test 1 spy-count proof; identity discipline, Pitfall 1) |
| T-48-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate — NOT APPLICABLE: zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor` — compositor suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- The composite pass implements the spec's composition order end-to-end including the Background contribution (D-03, D-04).
- Gaps reveal the fallback identically on every surface that consumes compositeFrame (Pitfall 13 closed by construction).
- CMP-04 invalidation matrix is fully green; per-track recompute isolation proven.
</success_criteria>

<output>
Create `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-04-SUMMARY.md` when done
</output>
