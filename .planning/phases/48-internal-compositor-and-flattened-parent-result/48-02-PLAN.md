---
phase: 48-internal-compositor-and-flattened-parent-result
plan: 02
type: tdd
wave: 1
depends_on: ["47-05"]
files_modified:
  - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts
  - app/src/efx-paint/compositor/efxPaintBackgroundResolution.test.ts
autonomous: true
requirements: [CMP-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Background clips fully resolve now — modulo source mapping, finite/infinite repeat, gaps, and next-clip interruption — through a pure FrameLoopClip → PhysicPaintRotoLoopClip adapter over the existing resolver; no modulo math is copied into efx-paint/ (D-03, Pitfall P-48-2, Pitfall 10)"
    - "Spec Required example holds: a 5-image cycle repeated 3 times from frame 0 resolves [0,15); a 10-image cycle repeated twice from frame 15 resolves [15,35); after frame 35 the document fallback shows (SPECS/milestone-v1.0.0-plan.md:533-552, CMP-06)"
    - "A finite clip ending before the next clip leaves a visible fallback gap; the next clip interrupts a full or partial cycle without overlap; the requested repeat count stays stored while the effective duration is shortened; moving the next clip later re-expands the previous loop deterministically (spec loop rules:513-531)"
    - "Touching Background clips (next clip's startFrame equals the previous clip's effective end) never double-resolve a frame — ranges are half-open [placementStart, effectiveEnd) per the resolver contract (edge CMP-01 adjacency, resolved explicit)"
    - "Background resolution derives ONE context per derivation input (background.revision + per-clip revisions + capacity) and queries it per frame — never per effective-range frame (D-32 discipline, 48-RESEARCH.md Pitfall 11)"
    - "A Background clip whose sourceFrameRefs cannot resolve reports the missing references without throwing (D-31 discipline carried from the resolver); the per-frame result maps them to { kind:'missing', missingRefs } so the compositor renders transparent + a report entry (D-09)"
  assumptions:
    - "flagged assumption (edge CMP-06 unclassified, status unresolved): the complete pixel acceptance matrix cannot be auto-resolved by the probe; the Background unit rows land here and in 48-04, and pixel truth (Studio vs main preview vs export) is the 48-06 native UAT"
  artifacts:
    - path: "app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts"
      provides: "FrameLoopClip → resolver adapter + per-frame Background resolution"
      exports: ["deriveEfxPaintBackgroundResolution", "resolveEfxPaintBackgroundFrame", "EfxPaintBackgroundFrameResolution"]
  key_links:
    - "The adapter consumes derivePhysicPaintRotoLoopRanges / resolvePhysicPaintRotoLoopFrame from physicsPaintRotoPhysicalResolver.ts:5478/5655 — the single effective-duration authority (Pitfall 10); CONTEXT.md's citation of physicsPaintRotoLoopClips.ts is stale (Pitfall P-48-1) and must not be followed"
    - "deriveEfxPaintBackgroundResolution output feeds compositeFrame's ports.resolveBackgroundFrame hook created in 48-01; 48-04 wires it into the pipeline"
  prohibitions:
    - "Infinite Background loops MUST NOT be expanded into per-frame records — the resolver derives interval ranges and the adapter queries per frame; materializing an ∞ loop is a DoS surface (Pitfall 11)"
---

<objective>
Build the pure Background-resolution adapter (D-03): map the document's `FrameLoopClip` records into the existing Loop Clip resolver's derivation input, derive one memoized resolution context per background revision, and expose a per-frame query returning content / gap / missing. This closes the Pitfall P-48-2 type gap (document `FrameLoopClip` ≠ resolver `PhysicPaintRotoLoopClip`) so the compositor's Background step (48-04) is a thin consumption, and Phase 49 only adds the import/repeat/fallback UI on top of resolved behavior.

Purpose: CMP-06's Background loop/gap pixel-matrix rows are testable in unit tests with documents built directly — but only if the modulo/repeat/interruption math comes from the proven resolver rather than a hand-rolled copy. One scheduler owns effective duration (Pitfall 10).

Output: `efxPaintBackgroundResolution.ts` + its test file, green under vitest.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-RESEARCH.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-PATTERNS.md
@app/src/efx-paint/document/efxPaintDocument.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/stores/physicPaintStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FrameLoopClip → resolver derivation-input adapter + memoized derivation context</name>
  <reversibility rating="costly">D-03 resolves Background clips in the compositor now; deferring would force revisiting the shared path in Phase 49. User-locked in CONTEXT.md; recorded, no checkpoint.</reversibility>
  <files>app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts, app/src/efx-paint/compositor/efxPaintBackgroundResolution.test.ts</files>
  <read_first>
    - app/src/efx-paint/document/efxPaintDocument.ts:29-37, 62-68 (FrameLoopClip + BackgroundTrack — the adapter's input contract, verbatim)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts:273-294 (PhysicPaintRotoLoopClip — the resolver's clip contract: loopId, placementStart, sourceKeyIds, repeat: number|'infinity', mode)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts:5338-5364 (PhysicPaintRotoLoopRange — placementStart/phaseOrigin/cycleLength/sourceKeyIds/repeat/effectiveEnd/truncated/partialCycle/unresolved.missingSourceKeyIds) and 5436-5490 (PhysicPaintRotoLoopDerivationInput + derivePhysicPaintRotoLoopRanges signature)
    - app/src/stores/physicPaintStore.ts:487-517 (the store's derivation-input assembly + memo shape — identities sorted, derive once per content revision; verbatim in 48-PATTERNS.md)
    - app/src/efx-paint/document/efxPaintDocumentRevision.ts:1-11 (validate-then-hash discipline for the memo key)
  </read_first>
  <behavior>
    - RED Test 1 (field mapping): a document FrameLoopClip { id:'c1', startFrame:15, sourceFrameRefs:['s1','s2','s3'], repeat:{mode:'finite',count:2}, sourceKind:'imported-background', revision:7 } maps to a resolver clip with loopId 'c1', placementStart 15, sourceKeyIds ['s1','s2','s3'], repeat 2, mode 'progressive' — and { mode:'infinite' } maps to repeat 'infinity'.
    - RED Test 2 (identities): each sourceFrameRef yields a synthetic PhysicPaintRotoKeyIdentity { keyId: ref, appFrame: startFrame + index } so modulo source mapping addresses the clip's own source cycle.
    - RED Test 3 (memo): deriveEfxPaintBackgroundResolution(background, capacity) called twice with the same background record identity returns the SAME context object; a changed clip revision (new record identity) re-derives.
    - RED Test 4 (fail-closed validation): malformed clips (empty sourceFrameRefs, negative startFrame, finite count < 1) surface the resolver's validation throw at derivation time — never silently clamped.
  </behavior>
  <action>
    Create app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (pure — import type from the document model; value imports only from physicsPaintRotoPhysicalResolver.ts and physicsPaintRotoPhysicalModel.ts). Export deriveEfxPaintBackgroundResolution(background: BackgroundTrack, capacity: number): a memoized context built by mapping every background.clips entry to PhysicPaintRotoLoopClip exactly as Test 1 pins (loopId ← clip.id, placementStart ← clip.startFrame, sourceKeyIds ← clip.sourceFrameRefs, repeat ← finite count or 'infinity', mode ← 'progressive' — the resolver's static/progressive distinction is a Hold/PlayScript concern, not a Background one), building identities per Test 2, then calling derivePhysicPaintRotoLoopRanges({ identities, loopClips, capacity, interpolationEnabled: false }) — interpolationEnabled is false because Background gaps reveal the fallback, never generated frames. Memoize by record identity (same BackgroundTrack object + same capacity → same context), mirroring the _rotoPhysicalStructuralCache identity-compare idiom at physicPaintStore.ts:452-519. capacity is the parent sequence frame count (the spec's parentEnd boundary); the caller (48-03 store port) supplies it. Never copy the modulo/effective-duration math (Pitfall 10). Never resurrect the deleted projectBackgroundFrameLoopClipCapsule projection (Pitfall P-48-1 — removed in commit 346d47bc). All 4 tests RED then GREEN. Commits: test(48-02): failing background adapter tests; then feat(48-02): FrameLoopClip resolver adapter + memoized derivation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor/efxPaintBackgroundResolution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; suite exits 0
    - The adapter file contains no modulo/arithmetic re-implementation of cycle mapping — grep of efxPaintBackgroundResolution.ts for '%' finds nothing outside comments
    - The only resolver imports are derivePhysicPaintRotoLoopRanges / resolvePhysicPaintRotoLoopFrame and their types
    - Derivation is identity-memoized: same background record → same context reference (Test 3)
  </acceptance_criteria>
  <done>Document Background clips feed the proven Loop Clip resolver through a pure adapter; derivation is memoized and fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-frame Background resolution — content / gap / missing with the spec's loop rules</name>
  <files>app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts, app/src/efx-paint/compositor/efxPaintBackgroundResolution.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts:5655-5720 (resolvePhysicPaintRotoLoopFrame — the per-frame query contract, half-open ranges, out-of-domain resolves 'empty')
    - SPECS/milestone-v1.0.0-plan.md:513-552 (loop resolution rules + the Required example: 5×3 → [0,15), 10×2 from 15 → [15,35), fallback after 35)
    - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (Task 1 context this task queries)
  </read_first>
  <behavior>
    - RED Test 1 (spec example part 1): clip c1 start 0, 5 refs, repeat finite 3 → frames 0-14 resolve { kind:'content', clipId:'c1', sourceRef } with sourceRef cycling s0..s4 by (frame - 0) mod 5; frame 15 resolves { kind:'gap' }.
    - RED Test 2 (spec example part 2): clip c2 start 15, 10 refs, repeat finite 2, after c1 → frames 15-34 resolve content with c2 refs; frame 35 resolves gap (fallback shows).
    - RED Test 3 (interruption without overlap): c1 start 0, 4 refs, repeat 'infinity'; c2 start 6 → frames 0-5 resolve c1 (partial cycle: 6 frames = 1.5 cycles, source mapping by modulo), frame 6+ resolves c2; no frame resolves both clips (half-open ranges, edge CMP-01 adjacency).
    - RED Test 4 (finite gap): c1 start 0, 2 refs, finite 1; c2 start 10 → frames 2-9 resolve gap; a gap resolution carries no clipId.
    - RED Test 5 (requested preserved while shortened): c1 4 refs finite 5 interrupted by c2 at frame 6 → c1's stored repeat count is still 5 in the document (the adapter never rewrites the clip) and the range reports truncated/partialCycle from the resolver record.
    - RED Test 6 (missing refs): a clip whose sourceFrameRefs are absent from the injected known-source set resolves { kind:'missing', missingRefs } naming the refs — no throw (D-31 → D-09 chain).
    - RED Test 7 (hidden background): resolution against background.visible === false is the compositor's concern (48-04) — this query is visibility-agnostic; test pins that resolveEfxPaintBackgroundFrame does not read the visible flag.
    - RED Test 8 (capacity bound): an infinite clip with capacity 20 → frame 20+ resolves gap (parent end bound).
  </behavior>
  <action>
    Add resolveEfxPaintBackgroundFrame(context, frame, knownSources: ReadonlySet<string>): EfxPaintBackgroundFrameResolution to the same module — a discriminated union { kind:'content', clipId, sourceRef } | { kind:'gap' } | { kind:'missing', clipId, missingRefs }, Object.frozen. Implementation: query the Task 1 context via resolvePhysicPaintRotoLoopFrame(context, frame); map the resolver's resolution kinds — 'empty' → gap; a loop-range resolution → look up the owning range's clipId and compute the sourceRef from the range's sourceKeyIds at the resolver-reported cycle position (use the range record's phaseOrigin/cycleLength fields, never re-derived modulo — read how getRotoPhysicalRenderSource consumes the same resolution at physicPaintStore.ts:2326-2450 and mirror the source-selection step); validate the resolved sourceRef against knownSources — absent → { kind:'missing' } with the range's unresolved.missingSourceKeyIds when present. The function takes the pre-derived context (never re-derives per frame — D-32). knownSources is injected so the pure module never touches the store; 48-03's store port supplies the real set. All 8 tests RED then GREEN. Commits: test(48-02): failing per-frame background resolution tests; then feat(48-02): per-frame background resolution with spec loop rules.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests pass; the compositor suite and full suite exit 0; typecheck green
    - The spec Required example rows (5×3 → [0,15); 10×2 from 15 → [15,35); fallback after 35) pass as written (Tests 1-2)
    - Interruption rows prove half-open no-overlap behavior (Test 3) and stored-repeat preservation (Test 5)
    - Missing refs resolve to a report, never a throw (Test 6)
  </acceptance_criteria>
  <done>Per-frame Background resolution returns content/gap/missing exactly per the spec's loop rules, ready for the compositor's Background step in 48-04.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts`, `app/src/efx-paint/compositor/efxPaintBackgroundResolution.test.ts`
- New symbols: `deriveEfxPaintBackgroundResolution(background, capacity)`, `resolveEfxPaintBackgroundFrame(context, frame, knownSources)`, `EfxPaintBackgroundFrameResolution` union ({ kind:'content', clipId, sourceRef } | { kind:'gap' } | { kind:'missing', clipId, missingRefs }), internal `mapFrameLoopClipToResolverClip`
- Modified: none

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document clips → resolver input | FrameLoopClip records are parser-validated at the store boundary; the adapter re-validates by passing them through the resolver's own fail-closed derivation validation |
| known-source set → missing report | the injected knownSources set is the fail-closed oracle for dangling refs; the adapter never fabricates a source |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-04 | Tampering | adapter type confusion (FrameLoopClip → PhysicPaintRotoLoopClip) | high | mitigate | single mapping function with the exact field correspondence pinned by Test 1; resolver's own validation re-checks the mapped input (fail-closed throw at derivation) |
| T-48-05 | Denial of Service | infinite repeat materialization | high | mitigate | derive-once context + per-frame query (D-32); Test 8 pins capacity bounding; no per-range frame arrays are ever built (Pitfall 11) |
| T-48-06 | Tampering | dangling source refs | medium | mitigate | missing refs resolve to a typed 'missing' result carrying the ref ids (D-31 → D-09 chain); never a fabricated or cross-track lookup (46-06 foreign-source-refs discipline) |
| T-48-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate — NOT APPLICABLE: zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor` — new suite green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Document Background clips resolve through the existing Loop Clip resolver (D-03) with the spec's Required example proven in tests.
- Gaps, finite/infinite repeat, next-clip interruption (full and partial), and parent-end bounding all resolve per the spec's loop rules.
- Missing sources are typed report values, never throws and never fabricated pixels.
</success_criteria>

<output>
Create `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-02-SUMMARY.md` when done
</output>
