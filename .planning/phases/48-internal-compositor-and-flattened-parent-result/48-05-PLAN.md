---
phase: 48-internal-compositor-and-flattened-parent-result
plan: 05
type: execute
wave: 4
depends_on: ["48-03", "48-04"]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintProgramMonitor.tsx
  - app/src/components/physic-paint/view/physicsPaintProgramMonitor.test.ts
  - app/src/stores/physicPaintStore.ts
autonomous: true
requirements: [CMP-01, CMP-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Studio canvas shows the flattened composite — the program monitor — exactly what the main editor shows (D-05); painting, onion-skin, and stroke-editing still target the active track"
    - "While editing, the composite base under the live engine canvas contains every participating track EXCEPT the active track (the engine canvas supplies the active track's live pixels) — semi-transparent active-track strokes are never double-drawn (base + engine) — and during playback the program monitor presents getFlattenedFrame(layerId, currentFrame) including the active track"
    - "Onion skinning ghosts the ACTIVE track's raw previous/next frames on top of the composite — the ghost is the active track's raw frames, never re-composited (D-06); the existing projectRotoOnionPreviewFrames projection at PhysicsPaintStudio.tsx:2507-2515 is preserved"
    - "When the current frame's flattened result carries a non-empty missing report, the Studio surfaces it through the existing status capsule with the red warning triangle via the setApplyStatus('error') / setApplyMessage port with a fixed English message naming the count and first track (D-09); the publish is compare-then-write idempotent so a steady missing state does not re-fire"
    - "A hidden or non-soloed-under-solo track's pixels never appear on the Studio canvas (CMP-02 reflected in Studio per ROADMAP SC 3); hiding the Background track removes its contribution (D-04)"
    - "Composite output flows to a narrow leaf canvas component (PhysicsPaintProgramMonitor) — no .value reads added to the Studio root render body, no signal writes in render bodies, memo dep arrays enumerate exactly real inputs, store setters stay idempotent (efx-preact-reactivity skill §3-§6; the 16 GB OOM incident class)"
    - "Studio playback availability derives from the flattened path so the program monitor and playback transport never diverge from the frames the main editor would paint (CMP-01; the 1971-1993 memo switches its source)"
  assumptions: []
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintProgramMonitor.tsx"
      provides: "leaf canvas presenting the flattened composite (editing base + playback program monitor)"
      exports: ["PhysicsPaintProgramMonitor"]
  key_links:
    - "PhysicsPaintProgramMonitor ← physicPaintStore.getFlattenedFrame (48-03) — the Studio consumes the SAME shared composition path as main preview and export (CMP-01, Pitfall 8)"
    - "missing-report publication → the existing status capsule port (setApplyStatus/setApplyMessage at PhysicsPaintStudio.tsx:622, 705, 3251) — the Phase 46/47 rejection-surface pattern reused verbatim (D-09)"
    - "the editing-base composite excludes the active track; the 48-01 compositor supports this through its participating-set computation driven by a document whose active track is treated as engine-supplied — the monitor calls the compositor with an excludeTrackIds parameter threaded through the store port, never by rewriting the document"
  prohibitions:
    - "The Studio MUST NOT implement a second composition path — no per-track drawImage loops inside PhysicsPaintStudio.tsx outside the compositor module (Pitfall 8; the monitor's only math is 'which frame do I show')"
---

<objective>
Switch the Studio canvas from the active-track view to the program monitor (D-05): a narrow leaf canvas component presents the flattened composite — during editing as the base under the live engine canvas (active track excluded to prevent double-draw), during playback as the full `getFlattenedFrame` output — with the active track's onion ghosts on top (D-06) and the compositor's missing-source report surfaced through the status capsule (D-09).

Purpose: CMP-01's user-visible half — the Studio shows exactly what the main editor and export will show, so hide/solo/opacity/blend edits are immediately reflected in the Studio composite (ROADMAP SC 3).

Output: `PhysicsPaintProgramMonitor.tsx` leaf component wired into the Studio canvas stack; Studio playback availability sourced from the flattened path; capsule publication of missing sources.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-RESEARCH.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-PATTERNS.md
@.claude/skills/efx-preact-reactivity/SKILL.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/stores/physicPaintStore.ts
@app/src/efx-paint/compositor/efxPaintCompositor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PhysicsPaintProgramMonitor leaf canvas — editing base (active track excluded) + playback program monitor (full flattened frame)</name>
  <reversibility rating="costly">D-05 program-monitor surface: reverting to an active-track-only view later means unwinding the composite preview and its reactivity wiring. User-locked in CONTEXT.md; recorded, no checkpoint.</reversibility>
  <files>app/src/components/physic-paint/view/PhysicsPaintProgramMonitor.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/physicsPaintProgramMonitor.test.ts</files>
  <read_first>
    - .claude/skills/efx-preact-reactivity/SKILL.md (MANDATORY before touching this component — idempotent setters, identity-stable effect deps, narrow signal reads, no render-body signal writes, loop termination)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:1962-1993 (the playback availability memo to re-source), 2501-2520 (onion projection + the memo dep discipline comment), 2095-2115 (hidden-active-track blank-canvas effect), 288-289 (rotoPlaybackFrameIndex/Count signals)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the canvas stack region — where the engine canvas mounts; find the exact mount point and z-order before editing)
    - app/src/stores/physicPaintStore.ts (getFlattenedFrame from 48-03 — return shape { layerId, frame, cacheKey, renderedFrame, missing })
    - app/src/efx-paint/compositor/efxPaintCompositor.ts (compositeFrame ports — how an excludeTrackIds/ participating-set override is threaded if the editing base needs it; prefer computing the editing base through the store so the component stays math-free)
    - 48-PATTERNS.md §PhysicsPaintStudio.tsx (the never-fallback exhaustive switch convention + the 38.1-D-01 live-surface leaf pattern)
  </read_first>
  <action>
    Create app/src/components/physic-paint/view/PhysicsPaintProgramMonitor.tsx as a narrow leaf canvas component (the 38.1-D-01 live-surface pattern): props are concrete values (layerId, currentFrame, isPlaying, activeTrackId, width, height); it subscribes to physicPaintVersion.value in its own render/effect (never the Studio root) and draws the composite into its canvas. Two modes, selected by the isPlaying prop: (1) playback/program-monitor — draw getFlattenedFrame(layerId, currentFrame)'s renderedFrame (full composite INCLUDING the active track); (2) editing base — draw the composite of the participating set EXCLUDING the active track, because the live engine canvas stacked above supplies the active track's in-progress pixels and including it would double-apply semi-transparent strokes; obtain this through a store-side variant (add getFlattenedFrameExcluding(layerId, frame, excludeTrackIds) in physicPaintStore if the cleanest seam is store-side, implemented by the SAME compositeFrame with a filtered participating set — the component never filters tracks itself). Pending decode (null return) keeps the last drawn frame (no flicker-to-blank). Mount the monitor in the Studio canvas stack BELOW the engine canvas and BELOW the onion ghost layer, at the same logical size as the engine canvas; pointer-events: none. Re-source the playback availability memo at 1971-1993: replace the per-frame getRotoPhysicalRenderSource active-track probe with the flattened path — a frame is available when getFlattenedFrame(layerId, appFrame) returns a record (the flattened raster always exists when the document does, so availability becomes document-presence per frame in the playback range; keep the memo's return shape and the selectRotoPlaybackAvailable consumer contract unchanged so the transport is untouched). Keep the hidden-active-track blank-canvas effect at 2095-2115 working: when the active track is hidden, the engine canvas stays blank AND the monitor's editing base simply excludes it (truth table already does) — no behavior change needed there beyond verifying the effect still compiles against the moved resolution. Reactivity discipline (skill rules, hard gates): the monitor's refresh effect deps are exactly [layerId, currentFrame, isPlaying, activeTrackId] plus the physicPaintVersion clock read inside the effect; no new signals in the Studio root render body; the monitor's draw is idempotent — drawing the same cacheKey twice is a no-op (compare-then-draw guard on cacheKey). Unit tests (physicsPaintProgramMonitor.test.ts, node env with stubbed canvas/Image mirroring previewRenderer.test.ts:196-213): (a) playback mode draws the flattened renderedFrame for the current frame; (b) editing mode draws the active-track-excluded composite; (c) same cacheKey twice draws once; (d) null (pending) keeps the previous frame; (e) hidden active track → base excludes it. Commits: feat(48-05): program monitor leaf canvas + playback availability re-source; test coverage rides the same plan (tests written with the implementation per house execute-plan convention).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintProgramMonitor.test.ts && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
    <human-check>Studio opens on a multi-track document: the canvas shows the composite of all visible tracks (program monitor); hiding a track removes its pixels immediately; soloing one track isolates it while the Background stays (D-04); painting on the active track does not double-draw semi-transparent strokes; playback shows the full composite including the active track.</human-check>
  </verify>
  <acceptance_criteria>
    - The 5 unit tests pass; full suite green; typecheck green
    - PhysicsPaintStudio.tsx contains no new per-track draw logic — the only new call sites are getFlattenedFrame / getFlattenedFrameExcluding and the monitor mount (CMP-01 single path)
    - The availability memo's deps and consumer contract are unchanged in shape; its source is the flattened path
    - No signal writes in any render body; the monitor's effect deps enumerate exactly its real inputs (skill §4-§6)
  </acceptance_criteria>
  <done>The Studio canvas is the program monitor: editing shows composite-minus-active under the live engine canvas; playback shows the full flattened frame; hide/solo/blend edits reflect immediately.</done>
</task>

<task type="auto">
  <name>Task 2: Onion ghosts preserved on top (D-06) + missing-source status capsule publication (D-09)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/physicsPaintProgramMonitor.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:2501-2515 (the onion projection — preserved verbatim; deps enumerate exactly real inputs), 622, 700-705, 3245-3255 (applyStatus/setApplyStatus/setApplyMessage + the capsule statusMessage/statusIsError bundle — the existing red-warning-triangle surface)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:2059 (an existing setApplyStatus('error') call site — the publication idiom to mirror)
    - .claude/skills/efx-preact-reactivity/SKILL.md (compare-then-write idempotence for the capsule publish)
    - app/src/stores/physicPaintStore.ts (the missing report field on the flattened result)
  </read_first>
  <action>
    Onion (D-06): verify and pin with a source assertion that the onion layer still renders ABOVE the program monitor in the canvas stack z-order and still sources the ACTIVE track's raw frames via projectRotoOnionPreviewFrames with physicPaintStore.getRotoPhysicalRenderSource(launchContext.layerId, trackIdOfLaunch(launchContext), appFrame) — the ghost projection is NOT re-sourced through the flattened path. If Task 1's stack reorganization changed z-order, restore monitor &lt; engine &lt; onion. Missing-source capsule (D-09): add a narrow effect (in the monitor integration, not the Studio root render body) that reads the current frame's flattened result missing report; when non-empty, publish setApplyStatus('error') + setApplyMessage with the fixed English copy 'Missing source on N track(s) — first: &lt;track name or id&gt;' (naming the document track name when resolvable, id otherwise); when the report clears, restore setApplyStatus('idle') — BOTH directions compare-then-write against the last published key (`${frame}:${missing.length}:${firstTrackId}`) so a steady missing state fires exactly once and a cleared state restores exactly once (idempotent setter law; no render-body writes). Do not trap or block any interaction — the capsule is advisory chrome, consistent with the Phase 46/47 rejection surface. Tests (extend physicsPaintProgramMonitor.test.ts): (a) a missing report publishes error status + message once; (b) a repeated identical report does not re-publish; (c) a cleared report restores idle once; (d) onion projection sources the active track via getRotoPhysicalRenderSource (mock-spy assertion), never getFlattenedFrame. Commit: feat(48-05): onion-over-composite pinning + missing-source capsule publication.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintProgramMonitor.test.ts && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
    <human-check>Detach a Hold source on a visible track: the Studio canvas shows that track's frames as transparent (no stripe placeholder in the composite), the status capsule shows the red warning triangle with the missing-source message, and resolving the source clears the capsule. Onion skin ghosts still render over the composite when enabled.</human-check>
  </verify>
  <acceptance_criteria>
    - The 4 tests pass; full suite green; typecheck green
    - The onion projection's getRenderSource still calls getRotoPhysicalRenderSource with the ACTIVE track id (D-06; spy assertion in test d)
    - The capsule publish is compare-then-write in both directions (tests b/c)
    - Canvas stack z-order: program monitor below engine canvas below onion layer
  </acceptance_criteria>
  <done>Onion ghosts are active-track raw frames over the composite; missing sources surface in the status capsule exactly once per state change; the Studio never shows placeholder stripes for the flattened content.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/PhysicsPaintProgramMonitor.tsx`, `app/src/components/physic-paint/view/physicsPaintProgramMonitor.test.ts`
- New symbols: `PhysicsPaintProgramMonitor` component; `getFlattenedFrameExcluding(layerId, frame, excludeTrackIds)` store variant (if the store-side seam is chosen per Task 1); internal capsule-publish memo key
- Modified: `app/src/components/physic-paint/PhysicsPaintStudio.tsx` (monitor mount, availability memo re-source, capsule effect), `app/src/stores/physicPaintStore.ts` (the excluding variant, if chosen)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composite output → Studio canvas | the monitor draws only what getFlattenedFrame returns; no Studio-side pixel mutation |
| missing report → status capsule | the capsule publish is the user's only missing-source signal; a re-fire loop would spam the surface and mask real state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-14 | Denial of Service | reactivity feedback loop in the Studio canvas stack (OOM incident class) | high | mitigate | efx-preact-reactivity skill gates: narrow signal reads, identity-stable deps, compare-then-draw on cacheKey, compare-then-write capsule publish, no render-body signal writes (Tasks 1-2 acceptance criteria + tests b/c) |
| T-48-15 | Tampering | Studio/main/export pixel divergence | high | mitigate | the monitor consumes getFlattenedFrame only — the same seam previewRenderer/export use (CMP-01); test (a) pins the draw source; 48-06 UAT compares surfaces |
| T-48-16 | Information Disclosure (state confusion) | editing base double-drawing the active track | medium | mitigate | active-track exclusion in editing mode (Task 1 test b); full inclusion in playback (test a) |
| T-48-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate — NOT APPLICABLE: zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint` — Studio suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
- Human checks embedded above are harvested at end-of-phase (human_verify_mode default) and ride the 48-06 UAT.
</verification>

<success_criteria>
- The Studio canvas shows the flattened composite in both editing and playback (D-05).
- Onion ghosts remain the active track's raw frames on top (D-06).
- Missing sources surface via the status capsule, idempotently (D-09).
- No second composition path exists in the Studio (CMP-01).
</success_criteria>

<output>
Create `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-05-SUMMARY.md` when done
</output>
