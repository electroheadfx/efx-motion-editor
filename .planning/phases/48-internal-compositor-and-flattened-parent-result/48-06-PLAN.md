---
phase: 48-internal-compositor-and-flattened-parent-result
plan: 06
type: execute
wave: 5
depends_on: ["48-04", "48-05"]
files_modified:
  - app/src/efx-paint/compositor/efxPaintCompositorMatrix.test.ts
autonomous: false
requirements: [CMP-03, CMP-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every spec pixel acceptance matrix row has a recording-context contract assertion (op order, alpha values, composite ops, cache keys): two opaque normal Paint tracks; semi-transparent upper track; multiply; screen; overlay; add/lighter; hidden upper track; one soloed track; multiple soloed tracks; hidden-and-soloed precedence; empty upper frame over lower content; one-image Background loop; multi-image Background loop; finite and infinite repeats; Background gap over solid fallback and over transparency; next-clip interruption after a full cycle and after a partial cycle without overlap; parent Paint opacity/blend over other outer main-editor layers (SPECS/milestone-v1.0.0-plan.md:469-490, CMP-06)"
    - "The straight-alpha boundary contract holds structurally and visually: a 50%-alpha white pixel composites as 50% white at the parent boundary, never dark gray (D-02 pixel test, Pitfall 7)"
    - "Parent 50% opacity × internal 50% opacity = 25% effective exactly once — the compositor's internal ops and the renderer's parent op never double-apply (CMP-03)"
    - "Studio flattened pixels, main preview, and export satisfy the existing pixel tolerance convention per matrix row — verified by native UAT (Open Question 4 resolution: recording-context unit gates + native UAT pixel truth; no canvas test dependency added)"
  assumptions: []
  artifacts:
    - path: "app/src/efx-paint/compositor/efxPaintCompositorMatrix.test.ts"
      provides: "pixel acceptance matrix contract suite"
      exports: []
  key_links:
    - "The matrix suite drives compositeFrame through the same recording-context fixture as 48-01 — op-order/alpha/op assertions stand in for pixel truth in node (48-RESEARCH.md Validation Architecture)"
    - "Native UAT compares Studio preview vs main preview vs export per matrix row — the three surfaces provably share one path (48-03 seam + 48-05 monitor), so UAT evidence is summarized per row group (MEMORY: summarized render UAT evidence is accepted)"
  prohibitions: []
---

<objective>
Close CMP-06: consolidate the spec's pixel acceptance matrix into one contract suite (recording-context assertions per row over the shared compositor), then run the blocking native UAT — Studio vs main preview vs export parity per matrix row group, the missing-source surface confirmation (Pitfall P-48-4 decision), and playback smoothness at 3+ tracks.

Purpose: the matrix is the phase's success criterion 5 and the visual-fidelity lock for every decision in this phase (D-01/D-02 order and alpha, D-04 Background-under-solo, D-09 transparent missing). Pixel truth cannot run in vitest (node env, mocked canvas) — unit gates assert the drawing contract; humans assert the pixels.

Output: `efxPaintCompositorMatrix.test.ts` green + user-confirmed UAT record in the summary.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-RESEARCH.md
@app/src/efx-paint/compositor/efxPaintCompositor.ts
@app/src/efx-paint/compositor/efxPaintCompositor.test.ts
@app/src/lib/previewRenderer.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pixel acceptance matrix contract suite (recording-context rows)</name>
  <files>app/src/efx-paint/compositor/efxPaintCompositorMatrix.test.ts</files>
  <read_first>
    - SPECS/milestone-v1.0.0-plan.md:469-490 (the matrix rows, verbatim — every row needs a case)
    - app/src/efx-paint/compositor/efxPaintCompositor.test.ts (48-01/48-04 fixtures to reuse — recording context, document builders, port fakes)
    - app/src/lib/previewRenderer.test.ts:124-230 (harness idiom + seam-contract style)
    - app/src/lib/previewRenderer.ts:519-524 (the parent application block the parent-row assertions target)
  </read_first>
  <action>
    Create app/src/efx-paint/compositor/efxPaintCompositorMatrix.test.ts with one test per spec matrix row, driving compositeFrame through the recording-context fixture and asserting the drawing contract per row: (1) two opaque normal tracks → two drawImage ops, both globalAlpha 1, both 'source-over', lower track first; (2) semi-transparent upper → upper drawn with globalAlpha 0.5 (opacity BEFORE blend, D-01); (3) multiply → composite op 'multiply'; (4) screen → 'screen'; (5) overlay → 'overlay'; (6) add → 'lighter'; (7) hidden upper → no draw op for the upper track; (8) one soloed → only the soloed visible track draws; (9) multiple soloed → all soloed+visible draw in stable order; (10) hidden-and-soloed → hidden wins, no draw; (11) empty upper frame over lower content → upper contributes zero draw ops, lower draws (edge: empty frame is a content miss rendered transparent, not a removal of the lower track); (12) one-image Background loop → same sourceRef drawn at frames 0..N per the cycle; (13) multi-image loop → refs cycle by modulo; (14) finite repeat stops at cycleLength × count, gap after; (15) infinite repeat bounded by capacity; (16) gap over solid fallback → fallback fill op, no background draw; (17) gap over transparent fallback → clearRect only; (18) next-clip interruption after a full cycle and (19) after a partial cycle → half-open no-overlap (no frame draws two clips); (20) parent row — at the previewRenderer seam, parent 50% × internal 50% records exactly one parent draw with effective alpha 0.25 and the compositor's internal 0.5 op is separate (CMP-03, asserted via the renderer test harness); (21) straight-alpha contract — the pipeline contains no manual premultiply step and the result record documents straight alpha (D-02; the 50%-white pixel check itself is UAT). Reuse the 48-01 document/port fixtures; build matrix documents directly. Commit: test(48-06): pixel acceptance matrix contract suite.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 21 row tests pass; compositor suite, full suite, and typecheck green
    - Every spec matrix row from SPECS:469-488 maps to at least one test (row names listed in the test file header comment)
    - No new dependency is introduced (Open Question 4: recording-context idiom only)
  </acceptance_criteria>
  <done>The full spec matrix has executable contract coverage over the shared compositor; the drawing contract per row is pinned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Native UAT — pixel matrix parity, missing-source surface, playback smoothness</name>
  <files></files>
  <read_first>
    - .planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md (D-01..D-11 the UAT is evidencing)
    - SPECS/milestone-v1.0.0-plan.md:469-490 (matrix rows)
  </read_first>
  <action>
    Present the user with the native UAT protocol below and wait for explicit confirmation per part. Any failure halts the phase for a fix, not a workaround. Record pass/fail plus user notes per part in the summary.
  </action>
  <what-built>Plans 48-01 through 48-05 delivered: the pure compositor (hide/solo truth table with Background exemption, opacity-before-blend composite pass, straight-alpha flattened result, missing-source report), the Background Loop Clip resolution adapter (modulo/finite/infinite/gaps/interruption), the derived flattened cache with the full CMP-04 invalidation matrix, the store's getFlattenedFrame delivery, the previewRenderer + exportEngine seam switch (flattened-only, placeholder-free, participating-tracks preflight), and the Studio program monitor with onion ghosts and the missing-source capsule. All automated gates (vitest, typecheck) are green.</what-built>
  <how-to-verify>Native UAT on the user's machine (the assistant does not run the app):
    1. Pixel matrix parity: build the matrix row scenarios in Studio on a multi-track document (two opaque tracks; a 50%-opacity upper track; multiply/screen/overlay/add upper tracks; hidden upper; soloed track; hidden-and-soloed; empty upper frame over lower content) and confirm Studio preview, main preview, and a PNG export of the same frames show identical pixels per row (summarized evidence per row group is sufficient).
    2. Straight-alpha spot check: a 50%-alpha white stroke over a dark lower track composites as 50% white on every surface — no dark gray halo (D-02).
    3. Background rows: with test-authored Background clips (a short image cycle repeated finitely, then infinitely), confirm loop cycling, the gap revealing the fallback (solid, then transparent), and a next clip interrupting a partial cycle without overlap — identical in Studio, main preview, and export.
    4. Missing-source surface (Pitfall P-48-4 decision confirmation): detach a Hold source on a visible track — confirm the composite renders those frames transparent (no stripe placeholder anywhere in Studio composite, main preview, or export output), the status capsule shows the red warning triangle with the missing-source message, and export of the affected range is blocked by the preflight with the locked message. Confirm this behavior split (transparent + capsule + export block) is the desired surface.
    5. Parent application: set parent layer opacity 50% over other outer layers with an internal track at 50% — confirm the effective 25% exactly once (no double-dimming) in main preview and export.
    6. Playback smoothness: play a 3+ track document at 15 and 24 fps — confirm smooth playback drawing cached flattened rasters (no per-tick stutter), and that editing one track during paused playback updates only the next composite (Pitfall P-48-6).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing matrix row/surface with a screenshot</resume-signal>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/efx-paint/compositor/efxPaintCompositorMatrix.test.ts`
- New symbols: none (contract tests only)
- Modified: none beyond the new test file

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit contract ↔ pixel truth | the recording-context suite asserts the drawing contract; only the native UAT asserts pixels — neither substitutes for the other |
| Studio ↔ main preview ↔ export | the three surfaces are one path by construction (48-03/48-05); the UAT is the evidence that construction holds visually |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-17 | Tampering | pixel drift between surfaces (Pitfall 8) | high | mitigate | UAT part 1 compares all three surfaces per matrix row group; the shared-path construction (one compositeFrame) makes divergence a build error, not a behavior |
| T-48-18 | Tampering | double-premultiplied alpha halos (Pitfall 7) | high | mitigate | UAT part 2 is the D-02 pixel test (50% white, never dark gray); contract row 21 pins the structural half |
| T-48-19 | Denial of Service | composite cost at playback rates (Pitfall P-48-6) | medium | mitigate | UAT part 6 at 3+ tracks × 15/24 fps; D-07/D-08 caches are the construction |
| T-48-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate — NOT APPLICABLE: zero packages installed this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
- Native UAT parts 1-6 confirmed by the user (blocking-human gate)
</verification>

<success_criteria>
- All 21 matrix contract rows green (CMP-06 unit half).
- User confirms pixel parity Studio/main/export, the straight-alpha spot check, Background loop/gap rows, the missing-source surface decision, the 25% parent contract, and playback smoothness (CMP-06 pixel half + CMP-03 + CMP-05).
</success_criteria>

<output>
Create `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-06-SUMMARY.md` when done
</output>
