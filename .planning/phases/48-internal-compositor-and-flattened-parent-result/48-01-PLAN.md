---
phase: 48-internal-compositor-and-flattened-parent-result
plan: 01
type: tracer
wave: 1
depends_on: ["47-05"]
files_modified:
  - app/src/efx-paint/compositor/efxPaintHideSolo.ts
  - app/src/efx-paint/compositor/efxPaintCompositor.ts
  - app/src/efx-paint/compositor/efxPaintCompositeCache.ts
  - app/src/efx-paint/compositor/efxPaintHideSolo.test.ts
  - app/src/efx-paint/compositor/efxPaintCompositor.test.ts
  - app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One shared pure composition path in app/src/efx-paint/compositor/ resolves all participating Paint tracks into one flattened raster per frame — the same module later consumed by Studio preview, main preview, and export (CMP-01, Pitfall 8)"
    - "Hide/solo truth table: no solo armed → every track with visible !== false participates; any solo armed → only visible+soloed tracks participate; hide wins over solo; unknown track or absent document fails closed to not-participating (CMP-02, Pitfall M8)"
    - "The Background track never participates in the solo truth table — it is governed only by document.background.visible and composites beneath all Paint tracks (D-04)"
    - "Internal track opacity is applied before blend mode: each track draw is ctx.save() → ctx.globalAlpha = track.opacity → ctx.globalCompositeOperation = blendModeToCompositeOp(track.blendMode) → ctx.drawImage(...) → ctx.restore() (D-01, After Effects convention)"
    - "The flattened raster is straight (unmultiplied) alpha at the boundary to the main editor — a 50%-alpha white pixel must composite as 50% white, never dark gray (D-02, Pitfall 7)"
    - "A track whose content is missing (null or 'loop-placeholder' render source) contributes transparent pixels AND a missing-source report entry — never a placeholder raster (D-09, CMP-05)"
    - "Per-track content precedence is Roto-timeline-wins: real key > generated interpolation > Hold Loop Clip > cached frame, resolved through an injected port so the pure module never imports the store (D-10)"
    - "Compositing a frame with no participating content (zero visible tracks, no Background clip, transparent fallback) returns a fully transparent raster at the injected project canvas size — never null, never a throw (edge CMP-01 empty, resolved explicit; Open Question 1 resolved: the parent project canvas dimensions are the size authority)"
    - "Two Paint tracks with equal order values composite deterministically — order ascending, ties broken by track.id localeCompare; identity is always track.id, never an array index (edge CMP-01 ordering, resolved explicit; Pitfall 1)"
    - "A track that is both hidden and soloed is hidden — hide wins over solo (edge CMP-02 adjacency, resolved explicit)"
    - "With zero Paint tracks the participating set is empty and the composite is Background + fallback only (edge CMP-02 empty, resolved explicit)"
    - "Multiple soloed tracks all participate and composite bottom-to-top in stable order (edge CMP-02 ordering, resolved explicit)"
    - "The composite pass result is cached per frame, keyed by a derived revision tuple (buildEfxPaintCompositeRevision config hash + per-track content revisions + background revision + per-clip revision terms + frame) built with the canonical encoder helpers — never ad-hoc string concatenation (D-08, CMP-04)"
  assumptions:
    - "flagged assumption (edge CMP-03 unclassified, status unresolved): the complete once-only opacity/blend matrix across every blend mode cannot be auto-resolved by the probe; covered by 48-01 op-order/alpha recording-context tests and the 48-06 pixel acceptance matrix"
    - "flagged assumption (edge CMP-05 unclassified, status unresolved): the complete missing-source/asset recoverability matrix cannot be auto-resolved by the probe; covered by 48-01 missing-report unit tests, the 48-03 preflight generalization, and 48-06 native UAT"
    - "Open Question 2 partial resolution (flagged for user confirmation in the 48-06 UAT): the flattened path renders missing sources transparent + status capsule per D-09, and the 43-09 export preflight block is RETAINED (generalized to all participating tracks in 48-03) — export hard-blocking on an unresolvable loop is stricter than transparent output and consistent with CMP-05 'explicit and recoverable'"
  artifacts:
    - path: "app/src/efx-paint/compositor/efxPaintCompositor.ts"
      provides: "compositeFrame — the shared pure composition pipeline"
      exports: ["compositeFrame", "EfxPaintCompositeResult", "EfxPaintCompositorPorts"]
    - path: "app/src/efx-paint/compositor/efxPaintHideSolo.ts"
      provides: "hide/solo truth table"
      exports: ["participatingPaintTracks", "backgroundParticipates"]
    - path: "app/src/efx-paint/compositor/efxPaintCompositeCache.ts"
      provides: "derived flattened cache key + keyed memo"
      exports: ["deriveEfxPaintFlattenedCacheKey", "deriveEfxPaintTrackContentKey", "createKeyedMemo"]
  key_links:
    - "participatingPaintTracks(document) in efxPaintHideSolo.ts is the single source of truth for CMP-02 — previewRenderer's resolvePhysicPaintTrackVisibility (previewRenderer.ts:108-116) is the active-track-only predecessor and stays for its remaining consumers until 48-03"
    - "blendModeToCompositeOp is reused, never duplicated — the compositor imports it (48-03 exports it from previewRenderer.ts; until then the compositor takes the op mapping through its injected ports so the pure module has zero lib imports)"
    - "deriveEfxPaintFlattenedCacheKey consumes buildEfxPaintCompositeRevision (efxPaintDocumentRevision.ts:139-157) PLUS per-track content revisions and background/clip terms — the config hash alone is under-covering (Pitfall P-48-3)"
  prohibitions:
    - "A track whose content fails to resolve MUST NOT be silently omitted from the composite's missing-source report — transparent pixels AND a report entry, always (CMP-05 transparency to the user)"
    - "The compositor MUST NOT read or apply parent-layer opacity/blend/transform — parent application stays exclusively in the main editor (CMP-03, Pitfall 6)"
---

<objective>
Create the pure compositor core in `app/src/efx-paint/compositor/`: the hide/solo truth table (CMP-02, D-04), the shared `compositeFrame` pipeline implementing the spec's composition order for the Paint-track half (fallback → participating tracks → opacity-before-blend composite pass → straight-alpha flattened result + missing-source report), and the derived-key per-frame flattened cache (D-08, CMP-04 foundation). This is the phase's keystone module — every later plan (Background step, store delivery, Studio monitor, pixel matrix) builds on these exact ports and the cache key.

Purpose: CMP-01 demands ONE shared internal composition path. Building it pure (no Preact, no DOM — injected decode/canvas/content ports per the efxPaintDocument.ts:1-9 purity contract) makes it callable identically from the store delivery seam (48-03), the Studio surface (48-05), and persistence/export — killing Pitfall 8 by construction.

Output: three pure modules + three test files, all green under `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor`.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-CONTEXT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-RESEARCH.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-PATTERNS.md
@app/src/efx-paint/document/efxPaintDocument.ts
@app/src/efx-paint/document/efxPaintDocumentRevision.ts
@app/src/efx-paint/document/efxPaintCanonicalEncoder.ts
@app/src/lib/previewRenderer.ts
@app/src/lib/previewRenderer.test.ts
</context>

<assumption_delta_decision>
The assumption-delta detector fired on the word "fallback" (pluralization cue) in the Phase 48 success criterion "parent cache invalidates when any participating track/clip/source/fallback changes". Primary noun: the document-owned Background fallback (`{ mode: 'transparent' } | { mode: 'solid'; color }`, efxPaintDocument.ts:20-22). Decision: **no-change** — the fallback has been a singular document-owned value since Phase 45 D-08 and this phase introduces no second variant; the fallback only joins the flattened cache-key terms (48-01 Task 2). No promote/add-alongside restructuring is warranted; the detector hit is a vocabulary false positive, recorded here so the seal-time audit sees the decision was made explicitly.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end pure composite slice — participatingPaintTracks truth table + compositeFrame pipeline (fallback → tracks → opacity-before-blend → straight-alpha result + missing report)</name>
  <reversibility rating="one-way">D-02 straight-alpha is a published boundary contract to the main editor; switching to premultiplied later breaks main-editor compositing and forces re-testing every alpha seam. Decision already locked by the user in CONTEXT.md — rating recorded, no re-ask checkpoint.</reversibility>
  <files>app/src/efx-paint/compositor/efxPaintHideSolo.ts, app/src/efx-paint/compositor/efxPaintCompositor.ts, app/src/efx-paint/compositor/efxPaintHideSolo.test.ts, app/src/efx-paint/compositor/efxPaintCompositor.test.ts</files>
  <read_first>
    - app/src/lib/previewRenderer.ts:76-116 (blendModeToCompositeOp + resolvePhysicPaintTrackVisibility — the exact truth-table predicate to generalize; verbatim in 48-PATTERNS.md)
    - app/src/lib/previewRenderer.ts:486-490 (the parent-site save/compositeOp/alpha/drawImage/restore idiom — the per-track draw shape; verbatim in 48-PATTERNS.md)
    - app/src/efx-paint/document/efxPaintDocument.ts:14-80 (BlendMode union, BackgroundFallback, InternalPaintTrack, BackgroundTrack, EfxPaintDocument — the only types the pure module imports, via `import type`)
    - app/src/lib/previewRenderer.test.ts:14-127 (the RecordingCanvasContext fixture idiom + store-mock/stubbed-globals harness to mirror — verbatim class shape in 48-PATTERNS.md)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts:146-185 (the 'real' | 'generated' | 'loop-placeholder' render-source union the content port surfaces)
    - SPECS/milestone-v1.0.0-plan.md:443-467 (the normative 11-step composition order + requirements)
  </read_first>
  <behavior>
    - RED Test 1 (truth table, no solo): document with tracks A(visible), B(visible), C(hidden); participatingPaintTracks returns [A, B] in order ascending.
    - RED Test 2 (solo armed): A(visible, solo), B(visible), C(visible, solo) → [A, C]; B excluded.
    - RED Test 3 (hide wins over solo): A(hidden, solo), B(visible) → [B] (edge CMP-02 adjacency).
    - RED Test 4 (multiple soloed, stable order): order ties (A.order === B.order) → deterministic tiebreak by track.id localeCompare; identity keyed by track.id never index (edge CMP-01 ordering + CMP-02 ordering).
    - RED Test 5 (Background exemption): solo armed on a Paint track does not affect backgroundParticipates(document) — governed only by document.background.visible (D-04).
    - RED Test 6 (composite op order): two tracks, upper at opacity 0.5 blendMode 'multiply' → the recording context shows per track: save → set globalAlpha 0.5 → set globalCompositeOperation 'multiply' → drawImage → restore, lower track drawn first (D-01, spec step 7-8).
    - RED Test 7 (missing source): content port returns null for track B at frame F → composite result carries a missing[] entry { trackId: B, frame: F } and B contributes zero draw ops (transparent, D-09) — no placeholder fill op is ever recorded.
    - RED Test 8 (empty composite): zero visible tracks + transparent fallback → result raster is produced (clearRect only) at the injected size, result is non-null (edge CMP-01 empty).
    - RED Test 9 (fallback solid): document.background.fallback { mode:'solid', color:'#112233' } and no clips → the first recorded op fills the fallback color before any track draw (spec step 1).
    - RED Test 10 (straight alpha contract): the result record documents straight alpha and the composite never calls any manual premultiply — asserted structurally (the pipeline produces an unmultiplied RGBA canvas; the pixel-level 50%-white check is the 48-06 matrix).
  </behavior>
  <action>
    Create app/src/efx-paint/compositor/efxPaintHideSolo.ts exporting participatingPaintTracks(document: EfxPaintDocument): InternalPaintTrack[] — pure; sorts a copy of document.tracks by order ascending with track.id localeCompare tiebreak (never identity-by-order, Pitfall 1), then applies the locked truth table generalized from previewRenderer.ts:108-116 (no solo armed → visible !== false participates; solo armed → visible !== false AND solo === true; hide always wins; absent document/unknown state fails closed to empty) — and backgroundParticipates(document): boolean reading ONLY document.background.visible (D-04). Create app/src/efx-paint/compositor/efxPaintCompositor.ts exporting compositeFrame(document, frame, size: {width, height}, ports: EfxPaintCompositorPorts): EfxPaintCompositeResult. Ports are injected so the module stays pure (zero Preact/DOM/store imports — the efxPaintDocument.ts:1-9 contract): ports.createCanvas(width, height) returns a recording/real canvas+ctx pair; ports.resolveTrackContent(trackId, frame) returns { kind:'content', raster } | { kind:'missing', missingRefs: string[] } (the store side in 48-03 implements the D-10 precedence real > generated > Hold > cached via getRotoPhysicalRenderSource/getFrame); ports.compositeOp(blendMode) maps the BlendMode union to GlobalCompositeOperation with the exact mapping from previewRenderer.ts:76-91 (normal→source-over, screen→screen, multiply→multiply, overlay→overlay, add→lighter) injected now so 48-03 can wire the exported blendModeToCompositeOp without the pure module importing lib/. Pipeline per SPECS steps 1-9 for the Paint half: (1) clear the target canvas; if document.background.fallback is solid, fill it (transparent fallback = cleared canvas); (2-3) Background contribution is a ports.resolveBackgroundFrame(frame) hook returning { kind:'content'|'gap'|'missing' } — the tracer wires 'gap'-only behavior (fallback already painted) and 48-04 fills the content arm without changing this port (D-03 seam); (4-8) for each participatingPaintTracks(document): resolve content via ports.resolveTrackContent; missing → push { trackId, frame, missingRefs } into the report and draw NOTHING (transparent, D-09 — never any placeholder fill); content → save/globalAlpha=track.opacity/compositeOp=ports.compositeOp(track.blendMode)/drawImage at 0,0/restore per track (D-01); (9) return Object.freeze({ raster: canvas, missing: readonly report, participates: { trackIds, background: boolean } }). The size parameter is the parent project canvas dimensions (Open Question 1 resolution — same source renderFrame uses, previewRenderer.ts:362-375). Result documents straight alpha (D-02). Tests use a RecordingCanvasContext fixture mirroring previewRenderer.test.ts:45-127 and document fixtures built via createEfxPaintDocument then spread-overridden tracks (mirroring makeTrackDocument at previewRenderer.test.ts:31-39). All 10 tests RED then GREEN. Commits: test(48-01): failing compositor core contract tests; then feat(48-01): pure compositor pipeline + hide/solo truth table.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor</automated>
  </verify>
  <acceptance_criteria>
    - All 10 behavior tests pass; the suite exits 0
    - efxPaintHideSolo.ts and efxPaintCompositor.ts contain no Preact/DOM/store imports — grep of both files for "from '@preact" and "from '../../stores" finds nothing
    - The per-track draw sequence in the recording fixture is exactly save → globalAlpha → globalCompositeOperation → drawImage → restore (Test 6 asserts op sequence and values)
    - A 'loop-placeholder'/null content resolution produces a missing[] entry and zero draw ops for that track (D-09; Test 7)
    - participatingPaintTracks keys everything by track.id and sorts with order-then-id determinism (Tests 1-4)
  </acceptance_criteria>
  <done>The pure compositor composes a multi-track document frame end-to-end with the locked truth table, D-01 opacity-before-blend order, D-04 Background exemption, D-09 missing-source mapping, and D-02 straight-alpha result — proven by recording-context contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derived flattened cache key + keyed memo (efxPaintCompositeCache.ts)</name>
  <reversibility rating="costly">D-07/D-08 cache architecture (per-track caches + per-frame composite cache, derived keys) — switching to a full flattened-only cache later means re-keying and losing incremental recompute. User-locked in CONTEXT.md; recorded, no checkpoint.</reversibility>
  <files>app/src/efx-paint/compositor/efxPaintCompositeCache.ts, app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts</files>
  <read_first>
    - app/src/efx-paint/document/efxPaintDocumentRevision.ts:1-11, 117-157 (validate-then-hash discipline; buildEfxPaintTrackRevision + buildEfxPaintCompositeRevision verbatim — the config hash is necessary but NOT sufficient, 48-RESEARCH.md Pattern 4)
    - app/src/efx-paint/document/efxPaintCanonicalEncoder.ts:12-28 (encodeCanonicalString/encodeCanonicalNumber/hashCanonicalPhysicalValue — the only key-building helpers allowed)
    - app/src/stores/physicPaintStore.ts:452-519 (the identity-comparison memo idiom to mirror for createKeyedMemo)
    - app/src/lib/previewRenderer.ts:147-149, 175 (getPreviewPhysicPaintFrameCacheKey + the dataUrl-slice content-key idiom the track content key mirrors)
  </read_first>
  <behavior>
    - RED Test 1 (determinism): same document + same content revisions + same frame → identical key string across two calls; key is order-independent in track iteration (sorted by track.id).
    - RED Test 2 (content invalidation): changing ONE track's content revision changes the key; the other track's content-key term is unchanged (Pitfall P-48-3).
    - RED Test 3 (config invalidation): toggling a track's visible/solo/opacity/blendMode or order changes the key (config hash term).
    - RED Test 4 (background terms): changing background.revision, a clip's revision, or the fallback changes the key.
    - RED Test 5 (frame term): same inputs, different frame → different key.
    - RED Test 6 (memo): createKeyedMemo get/set/has/delete/clearByPrefix semantics; setting an equal key overwrites; values are deep-frozen on set.
  </behavior>
  <action>
    Create app/src/efx-paint/compositor/efxPaintCompositeCache.ts (pure). Export deriveEfxPaintTrackContentKey(trackId, contentRevision, frame): string and deriveEfxPaintFlattenedCacheKey(input): string where input = { document: EfxPaintDocument, trackContentRevisions: ReadonlyMap<string, string> (trackId → content revision string, e.g. the store's _resolveRotoPhysicalStructural(...).contentRevision or the dataUrl-slice idiom from previewRenderer.ts:175), backgroundClipRevisions: readonly string[] (per-clip `${clip.id}:${clip.revision}` terms), frame: number }. Key composition: hashCanonicalPhysicalValue over the canonical encoding of [buildEfxPaintCompositeRevision(document), track terms sorted by track.id localeCompare (encodeCanonicalString(trackId) + encodeCanonicalString(revision)), encodeCanonicalNumber(document.background.revision), clip revision terms sorted, encodeCanonicalNumber(frame)] — canonical-encoder helpers only, never template concatenation (delimiter-collision discipline, efxPaintDocumentRevision.ts:1-11). Do NOT read document.compositeRevision — that counter is unwired (48-RESEARCH.md finding c). Also export createKeyedMemo<K,V>(): a tiny pure memo ({ get, set, has, delete, clear, size }) that Object.freezes stored values, mirroring the memo semantics at physicPaintStore.ts:452-519. All 6 tests RED then GREEN. Commits: test(48-01): failing flattened cache key tests; then feat(48-01): derived flattened cache key + keyed memo.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor/efxPaintCompositeCache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass; suite exits 0
    - The key derivation imports buildEfxPaintCompositeRevision plus the canonical encoder helpers; no hand-built delimiter strings
    - document.compositeRevision is never read by the cache module (grep of efxPaintCompositeCache.ts for 'compositeRevision' finds only the buildEfxPaintCompositeRevision import)
    - Key changes proven for: single track content edit, config toggle, background revision, clip revision, fallback change, frame change
  </acceptance_criteria>
  <done>The flattened cache key provably covers every CMP-04 dependency class and the memo primitive exists for the store-side per-frame cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cache integration — compositeFrame consults/stores the flattened per-frame memo</name>
  <files>app/src/efx-paint/compositor/efxPaintCompositor.ts, app/src/efx-paint/compositor/efxPaintCompositor.test.ts</files>
  <read_first>
    - app/src/efx-paint/compositor/efxPaintCompositor.ts (the Task 1 pipeline — the memo lookup wraps the composite pass)
    - app/src/efx-paint/compositor/efxPaintCompositeCache.ts (Task 2 key + memo)
    - app/src/lib/previewRenderer.ts:726-747 (decode-once/draw-many imageCache idiom — the playback re-draw economics the memo reproduces)
  </read_first>
  <behavior>
    - RED Test 1 (cache hit): two compositeFrame calls with identical inputs → the content port's resolveTrackContent is consulted once per track on the FIRST call only; the second call returns a result whose raster is the SAME canvas reference (no second composite pass — D-08, Pitfall P-48-6).
    - RED Test 2 (content invalidation): after bumping track B's content revision term, the next call re-runs the pass; track A's content port is re-queried only if its own content key changed (per-track isolation of recompute).
    - RED Test 3 (config invalidation): toggling track B's solo flips the key → full re-run with the new participating set.
    - RED Test 4 (missing report cached): a missing-source frame caches its missing[] report alongside the raster; the cached hit returns the identical frozen report.
  </behavior>
  <action>
    Extend EfxPaintCompositorPorts with memo: the memo instance and the key terms are supplied by the caller (store side in 48-03 owns the concrete memo lifetime per layerId); compositeFrame computes deriveEfxPaintFlattenedCacheKey first, returns the frozen cached EfxPaintCompositeResult on hit, and on miss runs the Task 1 pipeline then stores the frozen result. The compositor also consults ports.trackRasterMemo keyed by deriveEfxPaintTrackContentKey so a per-track raster decode/draw result survives when only a sibling track changed (D-07; the actual dataUrl→image decode lives store-side in 48-03 — the pure side caches whatever raster object the port produced). Keep the result record shape from Task 1 unchanged. All 4 tests RED then GREEN. Commits: test(48-01): failing cache-integration tests; then feat(48-01): per-frame flattened memo integration.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor && pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass; full suite green; typecheck green
    - A second identical compositeFrame call performs zero content-port queries and zero draw ops (Test 1 asserts on recording fixture counts)
    - The cached result and its missing[] report are frozen (Object.isFrozen asserted)
  </acceptance_criteria>
  <done>compositeFrame is memoized per frame with per-track recompute isolation; playback redraws hit the cached flattened raster.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/efx-paint/compositor/efxPaintHideSolo.ts`, `app/src/efx-paint/compositor/efxPaintCompositor.ts`, `app/src/efx-paint/compositor/efxPaintCompositeCache.ts`, `app/src/efx-paint/compositor/efxPaintHideSolo.test.ts`, `app/src/efx-paint/compositor/efxPaintCompositor.test.ts`, `app/src/efx-paint/compositor/efxPaintCompositeCache.test.ts`
- New symbols: `participatingPaintTracks(document)`, `backgroundParticipates(document)`, `compositeFrame(document, frame, size, ports)`, `EfxPaintCompositorPorts` (createCanvas / resolveTrackContent / resolveBackgroundFrame / compositeOp / memos), `EfxPaintCompositeResult` ({ raster, missing, participates }), `EfxPaintMissingSourceReport` entry ({ trackId, frame, missingRefs }), `deriveEfxPaintTrackContentKey(trackId, contentRevision, frame)`, `deriveEfxPaintFlattenedCacheKey(input)`, `createKeyedMemo()`
- Modified: none (greenfield module)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed document → compositor | the compositor consumes EfxPaintDocument records that crossed the fail-closed parsers at the store boundary; it must never accept `unknown` |
| content port → composite pass | per-track rasters arrive through the injected port; a hostile/buggy port returning malformed shapes is a store defect, and the port contract narrows the surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | compositeFrame input | high | mitigate | typed to EfxPaintDocument (never `unknown`); parse-at-boundary stays in efxPaintStore/parseEfxPaintDocument (ASVS V5 inherited); tests construct documents via createEfxPaintDocument + parse round-trip |
| T-48-02 | Tampering | track identity in caches/participation | high | mitigate | identity is always track.id (UUID); order is sort-only; Test 4 pins the tiebreak; no array-index addressing anywhere in the module (Pitfall 1) |
| T-48-03 | Denial of Service | composite pass cost | medium | mitigate | per-frame flattened memo + per-track content keys (D-07/D-08); the resolver derives ranges and never materializes infinite loops (Pitfall 11 discipline carried into 48-02) |
| T-48-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate — NOT APPLICABLE: this phase installs zero packages (48-RESEARCH.md Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/compositor` — new suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green (no existing behavior touched)
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- The shared pure composition path exists with the locked truth table (CMP-02, D-04), D-01 op order, D-02 straight alpha, D-09 missing mapping, D-10 content port.
- The flattened per-frame cache key provably covers config + per-track content + background + clip + frame terms (CMP-04 foundation).
- Zero imports from stores/Preact/DOM inside `app/src/efx-paint/compositor/`.
</success_criteria>

<output>
Create `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-01-SUMMARY.md` when done
</output>
