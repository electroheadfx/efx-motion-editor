---
phase: 35-interactive-physics-paint-controls
plan: 05
type: execute
wave: 4
depends_on: [35-01, 35-02, 35-03, 35-04]
files_modified:
  - app/src/components/sidebar/PhysicPaintProperties.tsx
  - app/src/lib/physicPaintBridge.ts
  - packages/efx-physic-paint/demo/src/App.tsx
autonomous: false
requirements: [PAINT-01, PAINT-02, PAINT-03, PAINT-04, DIAG-01]
user_setup: []
must_haves:
  truths:
    - "User can validate the full D-01 workflow from editor layer to standalone and back."
    - "User can verify D-08 apply still and D-09/D-10 apply sequence results in editor preview/timeline."
    - "D-03/D-08/D-09/D-10: User sees operation result feedback after standalone `physic-paint:apply` transport is consumed by the app listener."
    - "User can verify PAINT-01/02/03 interactive paint, erase, settings, and physics diagnostics in the live standalone surface."
    - "D-06: Standalone controls primarily mutate/control the live paint/physics paint session; import/export is only the Save state/Load state exception for editable state files, not rendered-output apply-back."
    - "User can verify PAINT-04: efx-physic-paint remains additional and does not replace basic paint or p5.brush FX paint."
  artifacts:
    - path: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      provides: "Final UAT-facing app status/error copy"
      contains: "Applied"
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Apply success/failure result feedback for app/standalone"
      contains: "physic-paint:apply-result"
    - path: "packages/efx-physic-paint/demo/src/App.tsx"
      provides: "Final UAT-facing standalone diagnostics/apply feedback"
      contains: "Applied"
  key_links:
    - from: "packages/efx-physic-paint/demo/src/App.tsx"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "apply event/result feedback"
      pattern: "physic-paint:apply"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "packages/efx-physic-paint/demo/src/App.tsx"
      via: "operationId-matched apply-result transport"
      pattern: "physic-paint:apply-result"
    - from: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      to: "app/src/stores/physicPaintStore.ts"
      via: "output status after apply"
      pattern: "hasOutput"
---

<objective>
Finalize feedback loops and run the Phase 35 verification/UAT checkpoint.

Purpose: After implementation wiring exists, the user needs exact live verification steps because the project forbids Claude from running the server. This plan ensures app/standalone error/success copy is present, the apply transport/result feedback is visible, and execution pauses for user UAT of the complete editor-to-standalone-to-editor workflow.
Output: Final status/error polish for UAT and a blocking human verification checkpoint with exact steps.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
@.planning/phases/35-interactive-physics-paint-controls/35-RESEARCH.md
@.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
@.planning/phases/35-interactive-physics-paint-controls/35-VALIDATION.md
@.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
@.planning/phases/35-interactive-physics-paint-controls/35-02-SUMMARY.md
@.planning/phases/35-interactive-physics-paint-controls/35-03-SUMMARY.md
@.planning/phases/35-interactive-physics-paint-controls/35-04-SUMMARY.md
@app/src/components/sidebar/PhysicPaintProperties.tsx
@app/src/lib/physicPaintBridge.ts
@packages/efx-physic-paint/demo/src/App.tsx
</context>

## Artifacts this phase produces

- App-side success copy for still output: `Applied to frame {frame}`
- App-side success copy for sequence output: `Applied {count} frames starting at frame {frame}`
- App-side replacement warning copy: `This frame already has physics paint output. Applying will replace it.`
- Standalone/app error copy: `Could not apply physics paint output. Keep the standalone open and try again from the current layer/frame.`
- Apply transport source assertion: standalone emits/sends `physic-paint:apply` for `[apply canvas]` and `[apply play canvas]`
- Apply result source assertion: app returns `physic-paint:apply-result` with matching `operationId`, success/failure state, and applied frame metadata
- Human verification checkpoint for full Phase 35 UAT

<tasks>

<task type="auto">
  <name>Task 1: Align final app and standalone feedback copy for UAT</name>
  <files>app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/lib/physicPaintBridge.ts, packages/efx-physic-paint/demo/src/App.tsx</files>
  <read_first>
    @.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
    @.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
    @app/src/components/sidebar/PhysicPaintProperties.tsx
    @app/src/lib/physicPaintBridge.ts
    @packages/efx-physic-paint/demo/src/App.tsx
  </read_first>
  <action>Review and align status/error/success copy across the app properties panel, bridge result handling, and standalone apply strip. Ensure successful still apply displays `Applied to frame {frame}` and successful sequence apply displays `Applied {count} frames starting at frame {frame}` somewhere visible to the user after `physic-paint:apply-result` is received with a matching `operationId`. Ensure app-side existing-output warning uses `This frame already has physics paint output. Applying will replace it.` before or during still replacement. Ensure apply failures use `Could not apply physics paint output. Keep the standalone open and try again from the current layer/frame.`. Ensure errors remain visible until the next successful action or explicit dismissal per UI-SPEC. Verify D-06: standalone controls primarily mutate/control the live paint/physics paint session; import/export is limited to editable paint state files via Save state/Load state and must not become the rendered-output apply-back path. Verify the final source contains the full apply feedback loop: standalone `[apply canvas]` and `[apply play canvas]` send rendered-output-only `physic-paint:apply` payloads over Tauri runtime or browser/dev fallback; app `installPhysicPaintApplyListener` consumes them; app emits/dispatches `physic-paint:apply-result`; standalone/app UI renders the result. Do not add new features, new dependencies, or change the rendered-output-only contract.</action>
  <verify>
    <automated>pnpm --dir app typecheck && pnpm --filter @efxlab/efx-physic-paint check && pnpm --filter @efxlab/efx-physic-paint demo:build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: success and error strings from UI-SPEC exist in the app/standalone files listed above.
    - Source assertion: errors are stateful UI values, not console-only logs.
    - Source assertion: D-06 is preserved by keeping standalone controls focused on live session mutation/control, with import/export limited to editable state files and not used as rendered-output apply-back.
    - Source assertion: standalone `App.tsx` sends `physic-paint:apply` for both `[apply canvas]` and `[apply play canvas]` and listens for `physic-paint:apply-result`.
    - Source assertion: `physicPaintBridge.ts` emits or dispatches `physic-paint:apply-result` after handling both success and failure cases.
    - Source assertion: result matching uses `operationId`, and sequence success reports `appliedFrameCount` and `startFrame`.
    - Source assertion: no `engine.save()`/strokes/serialized project data is used for app apply-back.
    - Command assertion: `pnpm --dir app typecheck && pnpm --filter @efxlab/efx-physic-paint check && pnpm --filter @efxlab/efx-physic-paint demo:build` exits 0.
  </acceptance_criteria>
  <done>Phase 35 user-facing feedback copy and apply transport result loop are coherent and ready for live UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies the full interactive physics paint workflow</name>
  <files>app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/lib/physicPaintBridge.ts, packages/efx-physic-paint/demo/src/App.tsx</files>
  <read_first>
    @.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
    @.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
    @.planning/phases/35-interactive-physics-paint-controls/35-VALIDATION.md
  </read_first>
  <action>Pause after the automated verification command passes and ask the user to run the live app/demo locally. Do not run any dev server. Record the user's pass/fail response in the plan summary, including failing step number and screenshot reference if the user provides one.</action>
  <what-built>Phase 35 built the additive `physic-paint` editor layer, `[open fx paint canvas]` app entry point, standalone live physics controls/diagnostics, `[apply canvas]`, `[apply play canvas]`, Tauri/browser fallback `physic-paint:apply` transport, `physic-paint:apply-result` result feedback, and editor preview rendering of returned rendered outputs.</what-built>
  <how-to-verify>
    1. User runs the app/demo locally using the normal project workflow. Claude must not run the server.
    2. In EFX Motion Editor, create or select a `Physic Paint` / `physic-paint` layer and move to a nonzero timeline frame.
    3. In layer parameters, confirm the button label is exactly `[open fx paint canvas]` and click it.
    4. In the standalone window, confirm the canvas is live and the diagnostics strip shows layer/frame context, engine ready, canvas mounted, active tool, color, brush size, opacity, physics mode, bridge transport mode, and either `Ready to apply` or an actionable not-ready condition.
    5. Paint on the canvas, switch to Erase, erase part of the output, change color/brush size/opacity/physics controls, and confirm the live canvas responds through the real engine controls.
    6. Confirm toolbar parity: the existing demo toolbar controls remain visible; any unconnected control is disabled with clear disabled helper copy; D-06 Save state and Load state refer to editable state files, not app apply-back or rendered-output transport.
    7. Click `[apply canvas]`; confirm the standalone reports applying, the app listener consumes the `physic-paint:apply` payload, the editor `physic-paint` layer receives rendered output at the frame selected/opened from the app, preview updates, and visible feedback says `Applied to frame {frame}`.
    8. Set `Frames to apply` to a small count such as 5 and click `[apply play canvas]`; confirm the generated rendered frames are applied starting at the captured app frame, preview/timeline can show the sequence, and visible feedback says `Applied 5 frames starting at frame {frame}`.
    9. Trigger or observe one invalid/not-ready condition if practical, such as opening standalone without a valid app layer context in browser fallback; confirm the error is visible and uses `Could not apply physics paint output. Keep the standalone open and try again from the current layer/frame.` or a more specific not-ready condition.
    10. Confirm existing Paint / Rotopaint, perfect-freehand basic paint, and p5.brush FX paint remain available and were not replaced.
  </how-to-verify>
  <verify>
    <automated>pnpm --dir app typecheck && pnpm --dir app test --run && pnpm --filter @efxlab/efx-physic-paint check && pnpm --filter @efxlab/efx-physic-paint demo:build</automated>
    <human-check>User completes the steps above and replies `approved` or describes exact failures/screenshots.</human-check>
  </verify>
  <acceptance_criteria>
    - Human assertion: live paint/erase/settings controls visibly affect the standalone canvas.
    - Human assertion: D-06 is visible in UX semantics: controls mutate/control the live paint/physics session, while Save state/Load state only save/reload editable state files.
    - Human assertion: `[apply canvas]` produces editor-visible rendered output at the captured frame and visible result feedback appears.
    - Human assertion: `[apply play canvas]` applies the requested count starting at the captured frame and visible result feedback appears.
    - Human assertion: diagnostics are visible without console inspection and errors are actionable.
    - Human assertion: existing basic/FX paint paths remain available.
  </acceptance_criteria>
  <done>User replies `approved`, or the execution summary captures the exact failed UAT step and issue details for follow-up planning.</done>
  <resume-signal>Type `approved` if all checks pass, or describe issues with the failing step number and screenshot if available.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User live UAT -> acceptance | Visual/manual verification validates behavior automation cannot observe without running servers. |
| App/standalone diagnostics -> user decisions | Error/status messages guide user action and must not hide invalid state. |
| Apply result transport -> standalone feedback | App mutation result crosses back into standalone UI and must match the initiating operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-15 | Repudiation | UAT checkpoint | mitigate | Record exact pass/fail step and user resume signal in the execution summary. |
| T-35-16 | Tampering | final feedback copy | mitigate | Use UI-SPEC exact strings and do not introduce new apply-back data paths. |
| T-35-17 | Denial of Service | live sequence UAT | mitigate | UAT instructs a small frame count first; implementation still clamps to 600. |
| T-35-20 | Spoofing | apply result feedback | mitigate | Match `physic-paint:apply-result` to the initiating `operationId` before showing success/failure. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this plan; RESEARCH.md Package Legitimacy Audit says no external package installation is recommended. |
</threat_model>

<verification>
Automation: `pnpm --dir app typecheck && pnpm --dir app test --run && pnpm --filter @efxlab/efx-physic-paint check && pnpm --filter @efxlab/efx-physic-paint demo:build`.
Manual: user performs the full UAT checkpoint because project instructions prohibit Claude from running the server.
</verification>

<success_criteria>
- Automated app/package type/build/test checks pass.
- User confirms full editor-to-standalone-to-editor workflow.
- Standalone-to-app `physic-paint:apply` and app-to-standalone `physic-paint:apply-result` feedback are visible in UAT.
- D-06 remains true: controls primarily mutate/control the live paint/physics session, while import/export only saves/reloads editable state files and is not rendered-output apply-back.
- Required labels and diagnostics are visible.
- Existing basic paint and p5.brush FX paint remain available.
</success_criteria>

<output>
Create `.planning/phases/35-interactive-physics-paint-controls/35-05-SUMMARY.md` when done.
</output>
