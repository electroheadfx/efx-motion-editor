---
phase: 36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/physicsPaintRotoSession.ts
  - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
  - app/src/components/physic-paint/physicsPaintRotoKeyController.ts
  - app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
autonomous: true
requirements:
  - 36.8-REG-01
  - 36.8-REG-02
  - 36.8-REG-03
  - 36.8-REG-04
must_haves:
  truths:
    - "36.8-REG-01 / D-01 / D-12 / D-15: Inserted blank Roto keys remain clean after navigation bookkeeping and become dirty only after markDirty for that frame."
    - "36.8-REG-02 / D-01 / D-12 / D-15: Deleted real keys are removed from real-key ownership, generated markers, reference/canvas restore intent, editable maps, and preview maps so they cannot reappear from stale state."
    - "36.8-REG-03 / D-01 / D-12 / D-16: Paste onto empty or generated targets creates source real-key payloads and clears generated/render-only state for the target frame."
    - "36.8-REG-04 / D-09 / D-15: Duplicate key transaction behavior remains byte-for-byte equivalent at the public helper contract level."
  artifacts:
    - "app/src/components/physic-paint/physicsPaintRotoSession.ts exports a factory-created compact Roto session boundary."
    - "app/src/components/physic-paint/physicsPaintRotoSession.test.ts covers boundary key transaction regressions."
  key_links:
    - "createRotoSession delegates key shifting/cleanup to existing physicsPaintRotoKeyController pure helpers per D-09."
    - "Session action results return plain descriptors and do not import bridge, Tauri, or engine APIs per D-02 and D-07."
---

<objective>
Create the first regression-first Roto session boundary for clean key transactions, proving D-01, D-03, D-04, D-06, D-07, D-08, D-09, D-12, D-14, D-15, and D-16 before any UI rewiring.

Purpose: Move real-key ownership, generated/render-only markers, blank/deleted cleanup, copied key state, restore intent, and derived action availability into a compact Preact-native boundary while preserving Phase 36.7 behavior.
Output: A tested `physicsPaintRotoSession.ts` factory plus boundary regression tests for insert/delete/paste/duplicate coherence.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-CONTEXT.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-RESEARCH.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-PATTERNS.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-VALIDATION.md
@app/src/components/physic-paint/physicsPaintRotoKeyController.ts
@app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
@app/src/stores/canvasStore.ts
@app/src/types/physicPaint.ts
</context>

<artifacts_this_phase_produces>
- New file path: `app/src/components/physic-paint/physicsPaintRotoSession.ts`.
- New file path: `app/src/components/physic-paint/physicsPaintRotoSession.test.ts`.
- New exported factory: `createRotoSession` or an equivalently named compact Roto session factory.
- New exported interface/type family: `RotoSession`, `RotoSessionInput`, `RotoSessionActionResult`, `RotoSessionEffect`, `RotoSessionSaveReason`, `RotoSessionRestoreIntent` if these names are selected by the executor.
- New action methods on the session object: `duplicateKey`, `insertBlankKey`, `deleteKey`, `copyKey`, `pasteKey`, `requestFrame`, `markDirty` per D-06.
- New effect descriptor variants: `saveFrame`, `replaceKeys`, `restoreFrame`, `clearCanvas`, `showCachedReference`, `navigate`, plus a cache/reference clearing descriptor if needed by existing adapter naming per D-07.
- New computed/Signal fields for current frame, real key frames, generated frame markers, dirty/pending frames, copied key state, restore intent, feedback, and derived action availability per D-01 and D-04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add boundary regression tests for clean key transactions</name>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
    - app/src/components/physic-paint/physicsPaintRotoKeyController.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.ts if it already exists
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts if it already exists
    - app/src/types/physicPaint.ts
    - .planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-PATTERNS.md
  </read_first>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.test.ts, app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts</files>
  <behavior>
    - Test 36.8-REG-01: after `insertBlankKey()` on a real key at frame 5, the result includes a `replaceKeys` descriptor and a blank-real-key restore intent, `dirtyFrames.value` does not contain frame 5, `requestFrame(5)`/return navigation bookkeeping does not add dirty state, and `markDirty(5)` is the first operation that marks frame 5 dirty.
    - Test 36.8-REG-02: after `deleteKey()` removes frame 5 from real keys with generated/reference entries around frame 5, the session no longer exposes frame 5 as a real key and returned cleanup/restore descriptors contain deleted/generated/reference cleanup so stale preview/background state cannot restore frame 5.
    - Test 36.8-REG-03: after `copyKey()` from a real key and `pasteKey()` onto an empty or generated target, the target is a clean `source: 'real-key'` frame, generated/render-only markers for that target are cleared, and `nearestRealKeyFrame` is absent from the pasted real key.
    - Test 36.8-REG-04: `duplicateKey()` produces the same frame numbers, mappings, changed frames, removed frames, and success message as `buildRotoKeyUtilityTransaction({ operation: 'duplicate', ... })` for the same fixtures.
  </behavior>
  <action>Add failing Vitest tests before implementation per D-14 and D-15. Use the fixture style from `physicsPaintRotoKeyController.test.ts`: local `frame()` and `blankFrame()` helpers with deterministic `data:image/png;base64,...` values. Import the intended session factory from `./physicsPaintRotoSession`; this import should fail or tests should fail until Task 2 creates the boundary. Extend existing controller tests only to preserve duplicate parity and cleanup contracts; do not weaken current controller assertions. Do not add packages, do not use a dev server, and do not introduce XState per D-08.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts physicsPaintRotoKeyController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The focused Vitest command fails in RED because `physicsPaintRotoSession.ts` or the expected session API/effect descriptors are missing, not because of syntax errors in the tests.
    - Test names cite 36.8-REG-01, 36.8-REG-02, 36.8-REG-03, and 36.8-REG-04.
    - Test bodies cite D-01, D-04, D-06, D-07, D-09, D-12, D-14, D-15, and D-16 in descriptions or comments where useful for traceability.
  </acceptance_criteria>
  <done>RED tests exist and fail for missing boundary behavior before production implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement compact Signals-backed Roto key/session boundary</name>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/physicsPaintRotoKeyController.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - app/src/stores/canvasStore.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-RESEARCH.md
  </read_first>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.ts, app/src/components/physic-paint/physicsPaintRotoKeyController.ts</files>
  <action>Create `physicsPaintRotoSession.ts` as a factory-created boundary, not a singleton. Use existing `@preact/signals` APIs already in the project (`signal`, `computed`, and `batch` if needed) for external reactive state and derived action availability per D-04. The factory input must include current frame, real key frames, cached/generated frames, dirty frames, copied key seed if any, canvas size, and a `buildBlankRotoFrame` function. The returned compact object must expose named methods `duplicateKey`, `insertBlankKey`, `deleteKey`, `copyKey`, `pasteKey`, `requestFrame`, and `markDirty` per D-06. Methods that require outside work must return plain `RotoSessionActionResult` values containing ordered `RotoSessionEffect` descriptors named with state-machine-shaped types such as `saveFrame`, `replaceKeys`, `restoreFrame`, `clearCanvas`, `showCachedReference`, and `navigate` per D-07 and D-08. Delegate key transforms to `buildRotoKeyUtilityTransaction`, `applyRotoKeyUtilityTransactionToLocalState`, and `deriveRotoKeyUtilityActionState` rather than duplicating frame-shift logic per D-09. Keep `PhysicsPaintStudio` bridge/canvas/window concerns out of this file per D-02. Do not add UI controls, shortcuts, modals, toasts, interpolation, cached playback, Play/Roto conversion, package installs, or XState.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts physicsPaintRotoKeyController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The RED tests from Task 1 pass without weakening their assertions.
    - `physicsPaintRotoSession.ts` imports from `@preact/signals` and `./physicsPaintRotoKeyController`.
    - `physicsPaintRotoSession.ts` does not import from `@tauri-apps/api`, `../../lib/physicPaintBridge`, `@efxlab/efx-physic-paint`, or `PhysicsPaintStudio`.
    - Insert/delete/paste/duplicate session actions return effect descriptors instead of calling bridge, engine, or window APIs.
    - Inserted blank and pasted target real keys are clean until `markDirty` is explicitly called for that frame.
  </acceptance_criteria>
  <done>The compact Roto session boundary exists and all key transaction regression tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — stabilize exports and preserve controller public contracts</name>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/physicsPaintRotoKeyController.ts
    - app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
  </read_first>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.ts, app/src/components/physic-paint/physicsPaintRotoKeyController.ts, app/src/components/physic-paint/physicsPaintRotoSession.test.ts</files>
  <action>Clean up only if needed after GREEN. Keep public controller exports available for existing imports while making the new session boundary the owner of D-01 state. Name exported action/effect types clearly enough for Phase 36.9 to map to transition events, without adding XState. Ensure computed action availability uses normalized real/generated/dirty/copied state and preserves existing disabled messages from `deriveRotoKeyUtilityActionState`. Do not perform unrelated formatting or hook-to-Signal rewrites.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts physicsPaintRotoKeyController.test.ts && pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests and typecheck pass.
    - Existing imports of `physicsPaintRotoKeyController.ts` continue to compile.
    - Type names and effect descriptor names are explicit; there is no generic UI-facing dispatch method replacing the named methods required by D-06.
  </acceptance_criteria>
  <done>Session boundary exports are stable and controller compatibility is preserved for downstream adapter rewiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI action -> Roto session boundary | User-selected frame numbers and cache state enter pure/session transaction logic. |
| Roto session boundary -> Studio adapter | Effect descriptors cross from internal state decisions into bridge/canvas execution in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.8-01 | Tampering | `physicsPaintRotoSession.ts` frame/key transaction inputs | high | mitigate | Normalize non-negative integer frames, filter real keys to `source: 'real-key'`, and preserve generated/reference cleanup assertions in Task 1/2. |
| T-36.8-02 | Tampering | generated/cache frame state | high | mitigate | Return explicit cleanup and restore descriptors for insert/delete/paste so generated/render-only frames cannot masquerade as real keys. |
| T-36.8-03 | Repudiation | dirty key state | medium | mitigate | Tests prove clean inserted/pasted keys stay clean until explicit `markDirty`. |
| T-36.8-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks in this plan; existing `@preact/signals` and Vitest dependencies are used without upgrade. |
</threat_model>

<verification>
Run `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts physicsPaintRotoKeyController.test.ts && pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
- D-01, D-03, D-04, D-06, D-07, D-08, D-09, D-12, D-14, D-15, and D-16 are implemented or tested in this plan.
- 36.8-REG-01 through 36.8-REG-04 have failing RED tests and passing GREEN implementation.
- No deferred Phase 36.9 XState or new user-visible UI scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-01-SUMMARY.md` when done.
</output>
