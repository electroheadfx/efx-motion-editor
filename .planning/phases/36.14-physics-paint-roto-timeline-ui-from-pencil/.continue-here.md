---
context: phase
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
task: 0
total_tasks: 3
status: in_progress
last_updated: 2026-07-22T05:37:33.864Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Verified GSD update boundary — the project-local installation is now @opengsd/gsd-core 1.8.0 at commit `5222fa44`, `/gsd-resume-work` reloaded the refreshed workflow, and Plan 11 may start only from this verified clean boundary.
- [ ] CONSTRAINT: UAT before regression — Plans 01–12 must not create, modify, delete, rename, or execute regression tests; Plan 13 unlocks only after exact canonical `36.14-12-APPROVAL.txt` evidence exists.
- [ ] CONSTRAINT: No compatibility shims — do not recreate `rotoCacheTransactions.d.ts`, a forwarding wrapper, alias, tsconfig exclusion, test configuration workaround, or duplicate timing/transaction authority.
- [ ] CONSTRAINT: Approval-gated dormant cleanup — `useRotoPersistenceIntegration.ts`, `rotoSaveTransactions.ts`, and `rotoCacheTransactions.ts` may remain only as dormant sources with zero production importers; Plan 13 deletes them together with their stale tests after valid assertion transfer and Plan 12 approval.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Forcing wrapper deletion before ownership and tests can move | Plan 09 Task 3 could not delete persistence wrappers because one still owned Plan 10 cache/visual behavior and both tests remained TypeScript inputs. | blocking | Keep wrappers dormant with zero production importers; Plan 10 migrates final production ownership; Plan 13 deletes wrappers and tests together after approval. |
| Declaration-only shim for stale tests | Plan 10 briefly created `rotoCacheTransactions.d.ts` after deleting the runtime module so pre-UAT typecheck would still resolve the stale test import. | blocking | The shim was removed in `11bc8910`; retain the exact dormant module with zero production importers and delete module plus test together in Plan 13. Never solve this with declarations, aliases, tsconfig exclusions, or test config changes. |
| Resuming past the requested update boundary | A Plan 11 agent dispatch was attempted immediately before the user requested a pause for a GSD update. The request was interrupted before execution. | blocking | On resume, first verify the GSD update, clean status, and HEAD at or after `b8d0ff0a`; only then dispatch Plan 11. Never assume the interrupted agent ran. |
| Running generic regression gates before native approval | The generic execute-phase post-wave gate would run tests even though the locked Phase 36.14 sequence prohibits regression execution through Plan 12. | blocking | Through Plan 11 use only the bounded verification explicitly allowed by each plan; Plan 12 collects native approval evidence; Plan 13 then unlocks Vitest cleanup. |

<current_state>
Phase 36.14 execution has resumed from the exact boundary after Wave 10 / Plan 36.14-10. Plans 01–10 have committed SUMMARY files. `STATE.md` reports Plan 11 of 18 ready to execute and 102/110 project plans complete.

GSD 1.8.0 is installed at `5222fa44`. Resume validation found three production imports from dormant `rotoSaveTransactions.ts`; correction commit `6abacbee` moved active edit-buffer types and persistence classification into `rotoEditBufferTransactions.ts`. The corrected static precondition and `pnpm --dir app typecheck` pass. Plan 11 has not started and its PLAN is aligned with the approved dormant-module sequencing.
</current_state>

<completed_work>

Completed this session:

- Plan 36.14-09 Task 1 — canonical physical project documents and identity-owned sidecar persistence — commit `3983bf95`.
- Plan 36.14-09 Task 2 — revision-checked project open, bridge, and launch reconstruction — commit `4c008c0f`.
- Plan 36.14-09 checkpoint resolution — user approved sequencing wrapper retirement across Plans 10/13; planning handoff, SUMMARY, and tracking committed as `f9436df5`.
- Plan 36.14-10 Task 1 — stable-key cache/live-pixel ownership — commit `04debfab`.
- Plan 36.14-10 Task 2 — physical playback/onion/reference traversal — commit `3528a651`.
- Plan 36.14-10 Task 3 — shared physical preview/export composition and extent — commit `8201b6c5`.
- Plan 36.14-10 completion and tracking — commit `198c865a`.
- Plan 36.14-10 shim correction — removed forbidden `rotoCacheTransactions.d.ts`, restored exact dormant `rotoCacheTransactions.ts`, confirmed zero production importers, and assigned module/test deletion to Plan 13 — commits `11bc8910` and `b8d0ff0a`.
- Resume ownership correction — moved active edit-buffer types and `shouldPersistRotoFrame` from dormant `rotoSaveTransactions.ts` into `rotoEditBufferTransactions.ts`, redirected both controllers, and confirmed all three dormant modules have zero non-test production importers — commit `6abacbee`.
- Bounded `pnpm --dir app typecheck` passed after Plans 09 and 10, after the shim correction, and after the resume ownership correction.
- Wave 9/10 schema-drift, codebase-drift, and UI-safety gates returned non-blocking results.
- No regression test was modified or executed. No dev server, native app, package change, or watch process was started.
</completed_work>

<remaining_work>

1. Execute Plan 36.14-11: bounded production reachability audit and the plan-authorized chained typecheck/build gate; do not run Vitest.
2. Stop at Plan 36.14-12 for blocking user-owned native physical-model UAT and canonical approval evidence.
3. After exact Plan 12 approval, execute Plan 13 stale-regression cleanup, including deletion of:
   - `useRotoPersistenceIntegration.ts` plus its obsolete test,
   - `rotoSaveTransactions.ts` plus its obsolete test,
   - dormant `rotoCacheTransactions.ts` plus `rotoCacheTransactions.test.ts`,
   after transferring every still-valid assertion to final owners.
4. Execute Plans 14–17 in dependency order.
5. Stop at Plan 18 for final user-owned native visual and interaction UAT.
6. Only after all plans and UAT gates pass: run phase review, regression/verification, and completion tracking.
</remaining_work>

<decisions_made>

- Plan 09 wrapper retirement was rescheduled instead of partially implemented. Plan 10 owns remaining cache/live-pixel/navigation/visual migration; Plan 13 owns wrapper and stale-test deletion after Plan 12 approval.
- No shim, alias, forwarding export, duplicate transaction, or second persistence authority is authorized during the sequencing delay.
- `rotoCacheTransactions.d.ts` was rejected as a test-compilation shim. The exact historical runtime source remains dormant only because its stale test is still compiled; production has zero importers.
- The dormant cache module and its test must be deleted together in Plan 13 after valid assertion transfer.
- Plans 01–12 remain production/UAT-first. The generic build/test gate must not override the explicit plan contract; Plan 11 alone runs its authorized typecheck/build, and Vitest remains locked until Plan 13.
- Plan 11 has not begun and must not be treated as partially executed.
</decisions_made>

<blockers>

- No active human or technical blocker remains before Plan 11.
- The Plan 10 ownership omission found during resume is resolved by `6abacbee`; the Plan 11 static module precondition and typecheck pass.
- No async external job, watcher, server, native application, or background process is active.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state and exact next action.
2. This `.continue-here.md` — blocking constraints and the Wave 9/10 decision history.
3. `.planning/STATE.md` — confirms Plan 11 is next and Plans 01–10 are complete.
4. `36.14-09-SUMMARY.md` — canonical persistence/open/bridge/launch implementation and Plan 10/13 wrapper handoff.
5. `36.14-10-SUMMARY.md` — cache/render cutover and corrected dormant-module sequencing.
6. `36.14-11-PLAN.md` — next bounded production reachability audit and authorized typecheck/build gate.
7. `36.14-12-PLAN.md` — blocking native physical-model UAT and exact approval evidence contract.
8. `36.14-13-PLAN.md` — approval-gated stale regression transfer and dormant source/test deletion ownership.
9. `36.14-CONTEXT.md`, `36.14-RESEARCH.md`, `36.14-PATTERNS.md`, and `36.14-VALIDATION.md` — locked decisions, final-owner mapping, and UAT-first sequence.
10. `.planning/METHODOLOGY.md` if it exists — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- Do not infer that the interrupted Plan 11 Agent executed; verify git history before dispatch.
- Do not recreate `rotoCacheTransactions.d.ts` or another declaration/config workaround for stale tests.
- Do not delete dormant wrappers/modules before Plan 13 can delete their tests and transfer valid assertions.
- Do not add a compatibility adapter, alias, dual-write, source/display projection, durable generated timing truth, or operation-specific transaction route.
- Do not run Vitest before canonical Plan 12 approval evidence exists.
- Do not run the dev server or native app; native UAT belongs to the user.

## Infrastructure State

- Branch: `main`.
- Worktree isolation is disabled for this project; execution commits are atomic on main.
- Working tree was clean before writing this handoff.
- Current pre-handoff HEAD: `b8d0ff0a` (`docs(36.14-10): record dormant cache correction`).
- No server, watcher, test process, native app, background shell, or async external job is active.
- Project-local GSD at `.claude/gsd-core` is updated to @opengsd/gsd-core 1.8.0 by commit `5222fa44`.
- Resume ownership correction `6abacbee` removes all non-test production imports of the three approval-gated dormant modules.

<context>
The execution is intentionally stopped at the safest possible boundary: Wave 10 is complete and verified, while Wave 11 has produced nothing. The most important resumption risk is accidentally trusting the interrupted Plan 11 dispatch or reintroducing a compatibility shim to satisfy stale pre-UAT tests. Resume from the refreshed GSD workflow, preserve the explicit dormant-cleanup sequence, and keep Vitest locked until Plan 13.
</context>

<next_action>
Start with `/gsd-execute-phase 36.14 --wave 11`. The refreshed GSD workflow, clean boundary, corrected Plan 10 ownership, aligned Plan 11, static dormant-module reachability check, and bounded typecheck have already been verified. Do not redispatch Plans 09 or 10.
</next_action>
