---
context: phase
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
task: 2
total_tasks: 2
status: paused
last_updated: 2026-07-24T19:36:42.246Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual native failures. Acknowledge each before changing the repaired surface.

- [ ] **Parent-authoritative settlement** — Never allow a parent-accepted physical transaction to roll back only in the child because of derived cache/reference state or stale parent selection. Preserve one acknowledged physical authority.
- [ ] **Playback readiness** — Never hide the static engine canvas until the delayed-mounted dry canvas has measurable bounds and the replacement background/alpha overlay is ready. The earlier eager hide produced a black playback window.
- [ ] **D-30 boundary** — Do not run tests, test discovery, typecheck, build/package commands, a server, browser automation, Tauri, or the native app without fresh explicit authorization.
- [ ] **Historical plan lock** — Do not resume rejected Plan 12 or historical Plans 13-18. Continue from the approved Plan 29 result and current recovery commits only.

**Do not proceed until these boxes are understood.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Child-only rollback after parent acceptance | Generated repaint ownership and stale selection checks previously split parent/child physical revisions after an accepted interpolation operation. | blocking | Keep derived display references out of ownership validation and accept structurally valid submitted selection atomically with the physical document. |
| Hide-before-overlay-ready | Querying canvases before engine mount and hiding static canvases immediately produced a black playback window. | blocking | Preserve delayed canvas discovery, MutationObserver/ResizeObserver tracking, exact dry-canvas bounds, and the `cached-roto-playback-ready` hide guard. |
| Resume stale GSD debt | Historical Plans 13-18 belong to the rejected D-12 sequence and would bypass the approved D-30 recovery result. | blocking | Route from `36.14-29-SUMMARY.md`; never infer eligibility from old unchecked plans. |

<current_state>
Phase 36.14 Plan 29 native gap UAT is fully approved by the user. The complete implementation repair is committed as `d6169775`, and the Plan 29 approval summary is committed as `5ca0ca8b`. The working tree was clean before this handoff was written.

This is a pause after native UAT closure, not a Phase 36.14 completion claim. No executable verification gates were run under D-30. The separately authorized final Pencil UI integration remains future work and historical Plans 13-18 remain locked.
</current_state>

<completed_work>

Completed:
- Restored canonical Duplicate/Blend interpolation, reliable On/Off settlement, generated rendering parity, and accepted-only replay behavior.
- Persisted per-layer playback loop/FPS outside physical history and merged preference acknowledgement into close flushing.
- Preserved current paper/background during cached playback and bound playback to the exact live dry-canvas rectangle.
- Fixed the delayed engine-canvas mount race with MutationObserver, ResizeObserver, requestAnimationFrame retry, and a playback-ready hide guard.
- Regrouped workflow controls as navigator, loop/FPS, interpolation, then Force Spacing; removed visible `Mode` while retaining accessibility.
- Completed repeated user-owned native UAT. Final user confirmation: “yes all work now, now all UAT is approved”.
- Committed implementation: `d6169775`.
- Recorded Plan 29 approval: `5ca0ca8b`.
</completed_work>

<remaining_work>

- Finalize Phase 36.14 tracking and routing from the approved Plan 29 summary.
- Do not mark the phase complete until the active GSD completion contract is reviewed.
- Ask the user whether the next separately authorized scope is final Pencil UI integration, deferred regression/typecheck/build verification, or another release-closing task.
- Do not resume Plans 13-18.
</remaining_work>

<decisions_made>

- The user's explicit final approval closes Plan 29 native UAT for gaps 1-10 and 13.
- Plan 29 approval creates no approval TXT and does not revive rejected Plan 12.
- Playback preferences remain per-layer project metadata outside `roto_physical`, physical revisions, snapshots, and Undo/Redo history.
- Cached playback uses the current Studio background and one presentation path; it does not add a second background renderer.
- Playback static canvases are hidden only after exact overlay geometry is available.
- Final Pencil integration and executable gates require separate authorization.
</decisions_made>

<blockers>
- None. Native UAT is approved; only scope/routing decisions remain.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume state and next action.
2. `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-29-SUMMARY.md` — authoritative approved native UAT result and stop boundary.
3. `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-29-PLAN.md` — D-30 scope constraints and historical-plan lock.
4. `.planning/STATE.md` — currently still points at Plan 28 and must be updated only through the appropriate finalization/routing workflow.
5. `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md` — active physical-frame and D-30 decisions.
6. `.planning/METHODOLOGY.md` (if it exists) — apply project analytical lenses before assumption analysis.

## Critical Anti-Patterns (do NOT repeat these)

- Do not replace the delayed dry-canvas discovery with a one-time mount query.
- Do not hide static playback canvases on `cached-roto-playback-active` alone.
- Do not reintroduce launch-level `rotoBackground`; canonical runtime background is `rotoPhysical.background`, while playback uses current Studio settings.
- Do not couple playback preference traffic to physical pending state or physical history.
- Do not add a second apply-result subscription or close-flush registration.
- Do not route to rejected Plan 12 or Plans 13-18.

## Infrastructure State

- Git branch: `main`.
- Implementation commit: `d6169775`.
- Plan 29 UAT summary commit: `5ca0ca8b`.
- No development server, watcher, browser session, Tauri process, or assistant-owned native app was started.
- No external async jobs are running.
- No tests, typecheck, build, or package command was run under D-30.
- The user owns native visual UAT and has approved the repaired surface.

<context>
The hard part is complete: the user repeatedly exercised the repaired interpolation, replay, persistence, playback, and UI behavior and approved all UAT. Resume conservatively from the two commits above. The next session should perform GSD tracking/final routing, not reopen implementation or stale plan debt. Preserve the exact playback readiness seam and parent-authoritative settlement if later Pencil work touches these surfaces.
</context>

<next_action>
Start with: read `36.14-29-SUMMARY.md`, confirm the four blocking constraints above, then run the appropriate Phase 36.14 finalization/routing workflow without resuming Plans 13-18. Ask before any executable gates or final Pencil UI work.
</next_action>
