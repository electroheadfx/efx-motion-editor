---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 19
type: execute
wave: 11
depends_on:
  - 36.14-10
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
  - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
prohibitions:
  - "Do not modify any production file outside the exact five-file owner boundary."
  - "Do not create compatibility, projection, migration, duplicate resolver/coordinator, or operation-specific settlement authority."
  - "Do not create, modify, delete, rename, discover, or execute tests; do not run Vitest or build."
  - "Do not change package, manifest, lockfile, generated dependency, TypeScript/test configuration, dormant module, or approval artifacts."
  - "Do not start the server or native application and do not claim UAT or phase completion."
must_haves:
  truths:
    - "Per D-01 and D-03, the production-reachable Roto key-utility route uses stable keyId plus direct physical appFrame and exposes no source/display, live interpolation-count, segment-spacing, or moved-spacing authority."
    - "Per D-09, Duplicate and Paste can change physical ownership only through the existing complete proposal, acknowledged coordinator, exact settlement, rollback, and accepted-only history path."
    - "Copy remains an immutable reusable copied-paint clipboard operation, while Clear and navigation resolve the current stable key and physical frame without creating another timing or transaction authority."
    - "PhysicsPaintStudio composes explicit physical ports directly; it contains no temporal ref bridge, transaction engine, resolver copy, rollback copy, history copy, or cache copy."
    - "Basic perfect-freehand, FX p5.brush, non-Roto Physics Paint, Script clipboard/library, deterministic Script Motion, and the approval-gated dormant-module sequence remain unchanged."
    - "The five-file cutover typechecks without creating, modifying, deleting, discovering, or executing tests and leaves Plan 36.14-11 as the sole final pre-UAT typecheck/build audit."
  artifacts:
    - path: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      provides: "Explicit semantic physical key-utility/navigation port contracts without legacy transaction staging primitives."
    - path: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      provides: "Physical utility actions delegated to the existing resolver and acknowledged coordinator."
    - path: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      provides: "Clipboard and utility orchestration that emits semantic physical intent without local physical transaction settlement."
    - path: "app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts"
      provides: "Direct composition of runtime/display navigation with the explicit physical utility port."
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Order-independent composition of timeline actions and navigation/key utilities with no ref bridge."
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      via: "direct hook result and explicit physical utility action bundle"
    - from: "app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts"
      to: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      via: "required semantic physical utility port plus the existing display/runtime ports"
    - from: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      to: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      via: "existing resolver/coordinator-facing execute contract and pending-operation authority"
    - from: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      to: "stable copied-paint clipboard"
      via: "immutable copy snapshot retained across repeated Paste operations until Copy, Discard, or disposal"
---

<objective>
Cut the remaining production-reachable Roto key-utility composition route over to the physical identity and acknowledged-edit authority before the central Plan 36.14-11 audit.

Purpose: Plans 01-10 established stable physical records, one resolver, one acknowledged coordinator, accepted-only history, and direct downstream consumers, but the Studio → navigation coordinator → key utilities route still exposes source/display projection, segment-spacing input, a legacy transaction type, a no-op persistence port, and a temporal save bridge. Per D-01, D-03, D-09, D-10, D-11, and D-12, this plan removes that authority from exactly the five approved production owners without widening scope or touching regression artifacts.

Output: Explicit physical utility ports, direct Preact composition, preserved clipboard/navigation behavior, a bounded production typecheck, and a clean handoff to the unchanged central production reachability audit in Plan 36.14-11 at Wave 12.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-06-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md

# Exact approved production boundary
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
@app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
</context>

<user_story>
**As a** stop-motion animator, **I want to** use every real Roto key and key utility through one deterministic physical-frame authority, **so that** Duplicate, Paste, Clear, Copy, navigation, persistence, rollback, Undo, Redo, and rendered output cannot disagree about key ownership or position.
</user_story>

<scope_constraints>
- Modify exactly the five production files in frontmatter. If a coherent cutover requires another production owner, stop and report the missing interface, its current file owner, and why the approved boundary cannot compose it; do not add a sixth production file.
- Do not create a compatibility alias, adapter, forwarding wrapper, fallback, dual-write, operation-specific settlement route, duplicate resolver, duplicate coordinator, duplicate rollback, duplicate history, or duplicate cache authority per D-03 and D-09.
- Do not create, modify, delete, rename, discover, or execute any regression test. Do not run Vitest. Do not change test configuration, TypeScript configuration, package manifests, lockfiles, generated dependencies, or dormant approval-gated modules.
- Do not run the build in this plan. Plan 36.14-11 remains the sole final pre-UAT chained typecheck/build gate.
- Do not start the development server or native application. Native physical-model UAT remains user-owned Plan 36.14-12 work.
- Do not change Basic perfect-freehand, FX p5.brush, non-Roto Physics Paint, the Script clipboard/library, deterministic Script Motion, or the exact Plan 12 approval contract.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Cut one Duplicate path through the explicit physical utility port</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts — preserve the existing physical execute/pending contracts and replace only the legacy key-utility persistence boundary.
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — reuse its existing resolver/coordinator access rather than rebuilding proposal or settlement logic.
    - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — identify the Duplicate effect, clipboard state, local transaction stage, and status behavior.
    - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts — identify the current persistence/display/runtime composition points.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — identify the hook order and current timeline-action ref bridge.
  </read_first>
  <action>
Define an explicit semantic physical key-utility port in `rotoCoordinatorPorts.ts` and expose it from `useRotoTimelineActions.ts` through the already-established resolver and `executePhysicalEdit` coordinator input. The port must accept semantic intent and stable physical identity, not frame-list mutation callbacks or a generic transaction object. Wire one production-quality Duplicate path end-to-end through Studio, navigation, and key utilities: derive the selected stable `keyId` and its current direct `appFrame`; allocate a fresh identity only at the existing explicit new-real-key creation seam; obtain a complete validated proposal from the existing physical resolver; submit it through the existing acknowledged coordinator; and let that coordinator remain the only pending, settlement, rollback, and accepted-history authority per D-01, D-05, and D-09. Preserve current user-visible availability and concise status behavior. Do not adapt the old transaction into the new port and do not add coordinator behavior inside the hook or Studio. If the existing five-file-visible APIs cannot produce a valid Duplicate proposal or operation kind, stop before modifying files and report the exact missing dependency and current owner.
  </action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <done>Duplicate reaches the existing physical resolver/coordinator from the visible utility action without local frame-map staging or fire-and-forget persistence, uses a fresh identity for the new durable key, preserves all payload owned by the source snapshot, and the app typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Move Paste, Clear, Copy, and navigation off the legacy transaction route</name>
  <files>app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — preserve immutable copied-paint clipboard lifecycle, action guards, canvas/display effects, and status messages while removing physical mutation authority.
    - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts — keep runtime/display ports distinct from physical mutation ports and remove configurable persistence ownership.
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — keep all physical proposals delegated to the existing resolver/coordinator and remove legacy save/interpolation outputs from the production API.
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts — delete the legacy utility transaction/persistence interface and retain narrowly semantic contracts.
  </read_first>
  <action>
Expand the explicit physical utility port to the legitimate Paste, Clear, Copy, and navigation behaviors and remove the legacy local transaction stage. Paste must consume the immutable reusable copied-paint snapshot; replacing an existing real key preserves that destination `keyId` and physical `appFrame`, while creating a durable key at an eligible physical destination allocates a fresh identity only through the existing creation seam and uses the existing resolver/coordinator. Generated and empty cells remain non-durable unless the existing physical creation contract explicitly promotes them. Clear targets the current selected stable identity and current physical frame and must not infer an owner from projected coordinates. Copy remains non-mutating, retains an immutable reusable clipboard across repeated Paste operations, and changes only on Copy, Discard, or disposal. Navigation consumes direct physical `appFrame` through the existing display/runtime port and never changes timing authority. Remove local frame-list application, fire-and-forget persistence, and the configurable no-op persistence port. Remove the legacy save and mixed interpolation-update methods from the timeline-action production return while preserving the enabled-only interpolation state and separate Script Motion contract per D-02 and D-04. If any operation requires a production owner outside the five approved files, stop and report that precise dependency rather than moving or duplicating its logic.
  </action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <done>Paste, Clear, Copy, and navigation use stable physical identity and explicit semantic ports; Copy remains reusable and immutable; no key utility locally stages a physical transaction or launches unacknowledged persistence; and the app typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the Studio temporal bridge and prove the five-owner authority cutover</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — remove the ref/time-order bridge and projected key-utility inputs without widening Studio responsibility.
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — consume its final explicit physical utility bundle directly.
    - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts — pass its required physical utility port at construction rather than configure mutation authority later.
    - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — confirm its final input contract has no projection, spacing, or generic transaction authority.
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts — confirm only semantic physical utility and existing coordinator/display/runtime contracts remain.
  </read_first>
  <action>
Reorder or otherwise directly compose `useRotoTimelineActions` before `useRotoNavigationCoordinator` so Studio passes the explicit physical utility bundle as a normal required input. Delete the temporal ref bridge and delete every key-utility input that supplies source/display conversion, live interpolation count, segment-spacing state, moved-spacing state, or save-target derivation. Keep Studio as a Preact composition and external-boundary adapter: use direct values or existing Signals, add no effect-based mirror, and do not reconstruct resolver, transaction, acknowledgement, rollback, history, cache, or clipboard semantics. Preserve the existing runtime/display ports, physical coordinator instance, store invalidation, Script behavior, Basic/FX/non-Roto branches, and dormant-module sequencing. Finish with a bounded static scan of exactly the five approved production owners and the app typecheck. Do not run the build, Vitest, a server, or the native application.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/components/physic-paint/PhysicsPaintStudio.tsx','app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts','app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts','app/src/components/physic-paint/hooks/useRotoTimelineActions.ts','app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts'];const blocked=[['source','Frame'].join(''),['display','Frame'].join(''),['inBetween','Count'].join(''),['segmentSpacing','Overrides'].join(''),['RotoKeyUtility','Transaction'].join(''),['RotoKeyPersistence','Port'].join(''),['saveRealKeyAt','DisplayFrame'].join('')];const hits=[];for(const file of files){const text=fs.readFileSync(file,'utf8');for(const token of blocked){if(text.includes(token))hits.push(file+': '+token)}}if(hits.length){console.error(hits.join('\n'));process.exit(1)}" &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <done>The Studio bridge is gone; all seven legacy authority symbols are absent from the exact five-file production boundary; direct physical composition typechecks; no test/build/UAT work occurred; and Plan 36.14-11 can perform the central reachability audit at Wave 12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI intent → key-utility/navigation hooks | User actions and current selection enter orchestration code and must not be allowed to supply durable timing or transaction primitives. |
| Key-utility semantic port → physical resolver/coordinator | A utility operation crosses into physical ownership mutation and must use validated complete proposals plus exact acknowledged settlement. |
| Standalone webview → parent bridge | Optimistic state becomes authoritative only after the matching parent acknowledgement for the same launch, layer, operation, and revision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-19-01 | Spoofing | Utility operation identity | high | mitigate | Resolve the current stable key and physical frame at execution, then rely on existing launch/layer/revision revalidation before publication and settlement. |
| T-36.14-19-02 | Tampering | Legacy local transaction stage | high | mitigate | Remove frame-list mutation and fire-and-forget persistence from key utilities; permit physical changes only through complete resolver proposals and the existing acknowledged coordinator. |
| T-36.14-19-03 | Repudiation | Optimistic utility result | medium | mitigate | Preserve accepted-only history and coordinator settlement so rejected, stale, timed-out, or transport-failed work cannot appear as an accepted operation. |
| T-36.14-19-04 | Information Disclosure | Utility failure status | low | mitigate | Keep the capsule/status message concise and route diagnostic detail through the existing LOG boundary rather than exposing bridge internals in controls. |
| T-36.14-19-05 | Denial of Service | Repeated Duplicate/Paste activation | medium | mitigate | Reuse the coordinator pending-operation signal and existing disabled reasons; do not introduce another pending flag or concurrent settlement path. |
| T-36.14-19-06 | Elevation of Privilege | Generic mutation-capable utility port | medium | mitigate | Replace generic transaction and persistence callbacks with narrowly semantic operations that cannot stage arbitrary frame maps outside the resolver/coordinator. |

No npm, pip, or cargo install occurs; the package-legitimacy gate and supply-chain install checkpoint are not applicable.
</threat_model>

## Multi-Source Coverage Audit

### GOAL coverage

| Source item | Coverage |
|---|---|
| ROADMAP goal: one deterministic physical-frame authority followed by polished UI | Plan 19 removes the remaining production-reachable utility authority; Plan 11 audits the binary production cutover; Plans 12-18 retain the approved UAT, regression, UI, and final-UAT sequence. COVERED. |

### REQ coverage

| Requirement | Coverage |
|---|---|
| 36.14-PHYSICAL-IDENTITY | Plan 19 Tasks 1-3 remove projected utility ownership and route operations by stable identity and direct physical frame. COVERED. |
| 36.14-DERIVED-INTERPOLATION | Plan 19 Tasks 2-3 remove live count and spacing inputs while preserving enabled-only interpolation; existing Plans 02, 03, 10, and 14 cover exact interiors. COVERED. |
| 36.14-ATOMIC-FRAME-MAPPING | Plan 19 Tasks 1-2 require complete proposals and the existing acknowledged coordinator; Plans 04-08 and 13 cover the core transaction and regressions. COVERED. |
| 36.14-RIPPLE-INSERT-DELETE | Existing Plans 06, 13, and 18 retain ownership; Plan 19 preserves the shared physical action composition. COVERED. |
| 36.14-RIPPLE-DRAG | Existing Plans 07, 13, 16, and 18 retain ownership and are unchanged. COVERED. |
| 36.14-FORCE-SPACING | Existing Plans 08, 13, 15, and 18 retain ownership; Plan 19 removes stale spacing authority without changing the session-only command. COVERED. |
| 36.14-DOWNSTREAM-PARITY | Plan 19 closes the remaining Studio/navigation/key-utility route; Plans 09, 10, 11, and 14 cover persistence and render consumers. COVERED. |
| 36.14-UI-INTEGRATION | Existing Plans 15-18 retain the final UI and UAT work and are unchanged except for wave metadata. COVERED. |
| 36.14-UAT-THEN-REGRESSION | Plan 19 forbids all test work and hands off to Plan 11, then the byte-exact Plan 12 approval gate and existing Plans 13-18. COVERED. |

### RESEARCH coverage

| Research feature or constraint | Coverage |
|---|---|
| One stable identity and direct physical location | Plan 19 Tasks 1-3. COVERED. |
| One deep pure physical resolver | Plan 19 delegates proposals and forbids copied mapping logic; existing Plan 02 remains owner. COVERED. |
| Complete immutable snapshot reconstruction | Plan 19 delegates settlement and rollback; existing Plans 04, 09, and 10 remain owners. COVERED. |
| One acknowledged physical-edit coordinator | Plan 19 removes the bypass and reuses the existing coordinator. COVERED. |
| Direct/computed Preact view state; no effect orchestration | Plan 19 Task 3 explicitly requires direct composition and no reactive mirror. COVERED. |
| Presentation emits intent and does not compute timing or availability | Plan 19 semantic ports plus existing Plans 15-17. COVERED. |
| Avoid half-migrated readers, generic utility settlement, incremental capacity checks, and split preview/commit logic | Plan 19 removes the remaining generic utility route and delegates validation to the existing resolver/coordinator. COVERED. |
| Clean cutover with no migration or compatibility layer | Plan 19 scope constraints and all three tasks. COVERED. |
| No external package | Plan 19 changes no dependency artifact. COVERED. |
| Production → typecheck/build → native UAT → regression | Plan 19 runs bounded typecheck only, Plan 11 owns the final typecheck/build, Plan 12 owns approval, and Plans 13-14 own regressions. COVERED. |

### CONTEXT coverage

| Decision | Coverage |
|---|---|
| D-01 | Tasks 1-3 use stable key identity and direct physical frame. COVERED. |
| D-02 | Tasks 2-3 preserve enabled-only strict-interior interpolation and remove utility count authority. COVERED. |
| D-03 | Tasks 2-3 remove projection, count, spacing, transaction, persistence-port, and bridge authority without aliases or migration. COVERED. |
| D-04 | Tasks 2-3 preserve the separate Script Motion contract. COVERED. |
| D-05 | Task 1 preserves shared physical action composition; Plan 06 remains the Insert owner. COVERED. |
| D-06 | Tasks 2-3 preserve shared Delete/clear/navigation composition; Plan 06 remains the Delete owner. COVERED. |
| D-07 | Unchanged Plans 07, 13, 16, and 18 own ripple Drag. COVERED. |
| D-08 | Plan 19 removes stale spacing authority while unchanged Plans 08, 15, and 18 own Force Spacing. COVERED. |
| D-09 | Tasks 1-2 remove the bypass and require the existing complete acknowledged transaction. COVERED. |
| D-10 | Task 3 closes the remaining production route; Plans 09-11 and 14 cover all downstream consumers. COVERED. |
| D-11 | Plan 19 is a bounded corrective wave before the separate audit/UAT/regression/UI waves. COVERED. |
| D-12 | Scope constraints and all verifications preserve the UAT-before-regression sequence. COVERED. |
| D-13, D-14, D-15, D-16 | Unchanged Plan 15 retains compact responsive geometry and scrolling decisions. COVERED. |
| D-17, D-18, D-19, D-20 | Unchanged Plans 08 and 15 retain explicit session-only Force Spacing Apply behavior. COVERED. |
| D-21, D-22, D-23, D-24 | Unchanged Plan 16 retains complete ripple-drag presentation and identity-following focus. COVERED. |
| D-25, D-26 | Plan 19 preserves concise status/LOG boundaries; unchanged Plans 15 and 17 finish presentation. COVERED. |
| D-27, D-28 | Plan 19 preserves Script clipboard/library behavior; unchanged Plans 15 and 17 own final control placement and guarded focusability. COVERED. |
| Deferred multi-selection/group movement | EXCLUDED by CONTEXT; not introduced. |
| Deferred migration/compatibility support | EXCLUDED by CONTEXT and D-03; not introduced. |

<verification>
1. Confirm Git changes during execution are limited to the exact five production files listed in frontmatter plus the Plan 19 SUMMARY generated by the executor.
2. Run the Task 3 bounded static authority scan and `pnpm --dir app typecheck`; both must pass.
3. Confirm no regression file, test configuration, package file, manifest, lockfile, generated dependency, dormant module, approval artifact, or native-UAT artifact changed.
4. Confirm no Vitest, build, server, watcher, browser, or native application was run.
5. Confirm Plan 36.14-11 remains next and owns the central production reachability audit plus the sole final pre-UAT `pnpm --dir app typecheck && pnpm --dir app build` gate.
</verification>

<success_criteria>
- The Studio → navigation coordinator → key utilities route reaches physical resolver/coordinator authority through explicit semantic ports.
- The exact five approved owners contain no source/display timing, live count, segment-spacing, generic key-utility transaction, legacy persistence-port, or temporal save-bridge authority.
- Duplicate and Paste use stable identity, complete proposals, acknowledged settlement, rollback, and accepted-only history when they change physical ownership.
- Copy remains immutable and reusable; Clear and navigation use current stable physical identity without creating timing authority.
- Preact composition uses direct values/existing Signals and adds no effect mirror or duplicate pending state.
- Basic, FX, non-Roto Physics Paint, Script, deterministic Script Motion, cache, dormant-module sequencing, and approval contracts remain unchanged.
- Bounded static verification and app typecheck pass; no tests or build run; no UAT or phase-completion claim is made.
- The repository is ready for existing Plan 36.14-11 at Wave 12.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-19-SUMMARY.md` when done, recording the five-file cutover, exact removed authority, bounded verification, hard-stop outcome if applicable, and handoff to Plan 36.14-11. Do not claim native UAT or phase completion.
</output>
