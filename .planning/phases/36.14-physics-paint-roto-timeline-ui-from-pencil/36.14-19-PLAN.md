---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 19
type: execute
wave: 12
depends_on:
  - 36.14-20
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
  - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
prohibitions:
  - "Do not modify any production file outside the exact five-file owner boundary."
  - "Do not create compatibility, projection, migration, duplicate resolver/coordinator, or operation-specific settlement authority."
  - "Do not create, modify, delete, rename, discover, or execute tests; do not run Vitest or build."
  - "Do not change package, manifest, lockfile, generated dependency, TypeScript/test configuration, dormant module, or approval artifacts."
  - "Do not start the server or native application and do not claim UAT or phase completion."
must_haves:
  truths:
    - "Per D-01 and D-03, the production-reachable Roto key-utility route uses stable keyId plus direct physical appFrame and exposes no source/display, live interpolation-count, segment-spacing, or moved-spacing authority."
    - "Per D-09, Duplicate and Paste compose through Plan 20's explicit ordinary operation kinds, complete next-record proposals, acknowledged coordinator, exact settlement, rollback, and accepted-only history path without rebuilding those semantics."
    - "Copy remains an immutable reusable copied-paint clipboard operation, navigation consumes direct physical appFrame, and the existing Clear route is preserved unchanged for Plan 11's stable-identity cleanup through useRotoFrameEditingController.ts."
    - "PhysicsPaintStudio composes Plan 20's public seams through explicit physical ports; `saveRealKeyAtDisplayFrame`, `rotoTimelineActionsRef`, `resolvePasteTargetForDisplayFrame`, and all approved producer/consumer forwarding disappear together in one typecheckable Task 3 cutover."
    - "Basic perfect-freehand, FX p5.brush, non-Roto Physics Paint, Script clipboard/library, deterministic Script Motion, and the approval-gated dormant-module sequence remain unchanged."
    - "The five-file cutover typechecks without tests or build and leaves Plan 36.14-11 as the sole final pre-UAT typecheck/build audit."
  artifacts:
    - path: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      provides: "Explicit semantic physical key-utility/navigation port contracts without legacy transaction staging primitives."
    - path: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      provides: "Physical utility actions delegated to the existing resolver and acknowledged coordinator."
    - path: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      provides: "Clipboard and utility orchestration that emits semantic physical intent without local physical transaction settlement."
    - path: "app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts"
      provides: "Direct composition of runtime/display navigation with the explicit physical utility port."
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Order-independent composition of timeline actions and navigation/key utilities with no ref bridge."
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      via: "direct hook result and explicit physical utility action bundle"
    - from: "app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts"
      to: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      via: "required semantic physical utility port plus the existing display/runtime ports"
    - from: "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts"
      to: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      via: "existing resolver/coordinator-facing execute contract and pending-operation authority"
    - from: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      to: "stable copied-paint clipboard"
      via: "immutable copy snapshot retained across repeated Paste operations until Copy, Discard, or disposal"
---

<objective>
Cut the remaining production-reachable Roto key-utility composition route over to the physical identity and acknowledged-edit authority before the central Plan 36.14-11 audit.

Purpose: Plans 01-10 established stable physical records, one resolver, one acknowledged coordinator, accepted-only history, and direct downstream consumers, but the Studio → navigation coordinator → key utilities route still exposes source/display projection, segment-spacing input, a legacy transaction type, a no-op persistence port, and a temporal save bridge. Per D-01, D-03, D-09, D-10, D-11, and D-12, this plan removes that authority from exactly the five approved production owners without widening scope or touching regression artifacts.

Output: Explicit physical utility ports consuming Plan 20's ordinary Duplicate/Paste foundation, direct Preact composition, preserved Copy/navigation/Clear behavior, a bounded production typecheck, and a clean handoff to the central production reachability audit in Plan 36.14-11 at Wave 13.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-06-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-20-SUMMARY.md

# Plan 20 authoritative interfaces — read-only in this plan
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/lib/physicPaintBridge.ts

# Exact approved production boundary
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
@app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
</context>

<user_story>
**As a** stop-motion animator, **I want to** use Duplicate, Paste, Copy, and navigation through one deterministic physical-frame authority while preserving the existing Clear behavior, **so that** utility composition, persistence, rollback, Undo, Redo, and rendered output cannot disagree about key ownership or position.
</user_story>

<scope_constraints>
- Modify exactly the five production files in frontmatter. If a coherent cutover requires another production owner, stop and report the missing interface, its current file owner, and why the approved boundary cannot compose it; do not add a sixth production file.
- Treat `36.14-20-SUMMARY.md` and its five final interfaces as authoritative. Compose `duplicate-key` and `paste-key` through those public seams; do not edit Plan 20's five owners in this plan.
- Do not create a compatibility alias, adapter, forwarding wrapper, fallback, dual-write, operation-specific settlement route, duplicate resolver, duplicate coordinator, duplicate rollback, duplicate history, or duplicate cache authority per D-03 and D-09.
- Preserve the existing Clear route unchanged in Plan 19. Stable-identity Clear cleanup belongs only to Plan 11 through `useRotoFrameEditingController.ts`.
- Do not create, modify, delete, rename, discover, or execute any regression test. Do not run Vitest. Do not change test configuration, TypeScript configuration, package manifests, lockfiles, generated dependencies, or dormant approval-gated modules.
- Do not run the build in this plan. Plan 36.14-11 remains the sole final pre-UAT chained typecheck/build gate.
- Do not start the development server or native application. Native physical-model UAT remains user-owned Plan 36.14-12 work.
- Do not change Basic perfect-freehand, FX p5.brush, non-Roto Physics Paint, the Script clipboard/library, deterministic Script Motion, or the exact Plan 12 approval contract.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Compose one Duplicate path through Plan 20's ordinary physical-edit seam</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts</files>
  <read_first>
    - `36.14-20-SUMMARY.md` — use its exact `duplicate-key` intent factory, resolver proposal, coordinator execute input, accepted output, and history behavior; do not infer names from the pre-Plan 20 source.
    - `app/src/types/physicPaint.ts`, `physicsPaintRotoPhysicalResolver.ts`, `useRotoPhysicalEditCoordinator.ts`, and `useRotoPhysicalEditHistory.ts` — read-only Plan 20 owners and authoritative interfaces.
    - `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` — replace only the legacy key-utility transaction/persistence boundary with a narrow semantic utility port.
    - `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` — expose Plan 20's public Duplicate seam without rebuilding proposal, staging, settlement, or history logic.
    - `app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts` — preserve visible guards/status while replacing the Duplicate transaction effect.
    - `app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts` and `PhysicsPaintStudio.tsx` — replace the configurable persistence/temporal save bridge with direct composition.
  </read_first>
  <action>
Per D-01/D-03/D-09, define a narrow semantic physical key-utility port in `rotoCoordinatorPorts.ts` whose Duplicate method accepts the current stable source identity and one direct destination `appFrame`, then delegates to the exact public Plan 20 intent/proposal/coordinator seam. Do not expose a frame-list mutator, payload mutator, generic transaction object, bridge payload, settlement callback, or replay input.

Wire one production-quality Duplicate path through `useRotoTimelineActions`, `useRotoKeyUtilities`, `useRotoNavigationCoordinator`, and Studio. Resolve the current source by stable `keyId` from accepted physical records, preserve the existing visible eligibility and status behavior, invoke Plan 20's operation-specific intent factory so the fresh identity is allocated exactly once, pass the resulting `duplicate-key` intent to the canonical resolver, and submit the exact complete proposal through the existing `executePhysicalEdit`. Plan 20 remains the sole operation-kind, complete-record validation, staging, pending, acknowledgement, rollback, accepted-output, and history authority. Remove Duplicate's legacy local frame-list application and fire-and-forget persistence. Keep each file typecheckable at the end of this task and add no effect/state mirror. If `36.14-20-SUMMARY.md` does not expose a seam sufficient for this five-file composition, stop and report the exact missing Plan 20 interface; do not add a sixth owner or reimplement it here.
  </action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <done>Visible Duplicate composes Plan 20's `duplicate-key` intent, complete proposal, coordinator, exact acknowledgement, and ordinary history without local mutation/persistence authority; a fresh identity and source payload remain Plan 20 semantics; and the app typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Compose Paste and preserve immutable Copy, physical navigation, and existing Clear</name>
  <files>app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts</files>
  <read_first>
    - Task 1 final semantic utility port and direct composition.
    - `36.14-20-SUMMARY.md` — use the exact `paste-key` creation/replacement intent and proposal seams; Plan 20 owns all identity/payload delta validation.
    - `app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts` — preserve the immutable reusable copied-paint lifecycle, guards, and messages while removing Paste transaction effects.
    - `app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts` — preserve direct runtime/display navigation and remove configurable key persistence ownership.
    - `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` — expose semantic Duplicate/Paste actions without changing physical coordinator internals; retain `saveRealKeyAtDisplayFrame` until Task 3 removes its producer and every approved consumer atomically.
    - `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` — retain narrow semantic utility plus display/runtime contracts only.
  </read_first>
  <action>
Extend the Task 1 semantic utility port with Paste composition through Plan 20's exact public `paste-key` seam. Consume an immutable copied-paint snapshot without consuming or mutating it. For an eligible empty destination, invoke Plan 20's creation intent so exactly one fresh identity and complete next-record proposal are produced; for an existing real destination, pass its current stable `keyId` and direct `appFrame` so Plan 20 preserves both and replaces only payload. Submit the resolver-owned proposal unchanged through the existing coordinator. Do not stage frame lists, write payloads directly, call replay, construct bridge envelopes, or infer ownership from source/display projection.

Preserve Copy as non-mutating and reusable across repeated Paste operations and routine refreshes until the existing Copy/Discard/disposal lifecycle changes it. Preserve navigation through the existing display/runtime port using direct physical `appFrame`; it never changes timing or transaction authority. Preserve the current Clear callbacks and behavior unchanged in this plan, including toolbar and shortcut routing; do not migrate, reinterpret, or remove Clear. Plan 11 alone audits and cleans stable-identity Clear through `useRotoFrameEditingController.ts`.

Remove the legacy local Duplicate/Paste transaction stage, fire-and-forget persistence callbacks, and configurable no-op key persistence port only where their consumers are removed in this same task. Retain the existing `saveRealKeyAtDisplayFrame` producer/output and its Studio/ref/target consumer wiring unchanged through this task so the Task 2 commit remains typecheckable; Task 3 owns their atomic removal. Keep enabled-only interpolation and separate Script Motion unchanged. Use direct values/existing Signals without state/effect mirrors. If Plan 20's final seam or the five approved files cannot compose either Paste form, stop with the exact missing interface/owner rather than widening or adapting the old route.
  </action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <done>Paste-to-empty and Paste-to-existing compose Plan 20's ordinary complete-record path; Copy remains immutable and reusable; navigation uses direct physical appFrame; Clear remains behaviorally unchanged for Plan 11; obsolete Duplicate/Paste mutation and persistence authority is removed; the temporary save producer/consumer bridge remains internally complete for Task 3; and the app typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Atomically remove the temporal save producer and every approved consumer</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts</files>
  <read_first>
    - `app/src/components/physic-paint/PhysicsPaintStudio.tsx` — remove `rotoTimelineActionsRef`, its producer assignment, and the `resolvePasteTargetForDisplayFrame` consumer in the same task as the timeline-action producer removal.
    - `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` — remove the `saveRealKeyAtDisplayFrame` producer and returned output only after all approved consumers are removed in this task.
    - `app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts` and `useRotoKeyUtilities.ts` — remove any forwarding/input/output contract for `resolvePasteTargetForDisplayFrame` in this same atomic cutover while preserving direct physical navigation and Plan 20 utility composition.
    - `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` — remove only any remaining approved temporal-save utility contract residue; retain semantic physical utility and existing coordinator/display/runtime contracts.
  </read_first>
  <action>
Atomically remove the complete temporal save bridge from the exact five Plan 19 owners: delete the `saveRealKeyAtDisplayFrame` producer and hook output, `rotoTimelineActionsRef` declaration/assignment, `resolvePasteTargetForDisplayFrame` consumer and all approved forwarding/input/output wiring in one task and one commit. Do not leave either a producer without consumers or a consumer without its producer at any task boundary. Compose `useRotoTimelineActions` and `useRotoNavigationCoordinator` directly so Studio passes the semantic Plan 20-backed Duplicate/Paste utility bundle as a normal required input.

Inspect source/display/spacing concepts only inside the key-utility input/output contracts, navigation contracts, timeline-action contracts, and Studio's key-utility composition block. Remove those concepts when they still determine utility ownership, destination, persistence, or timing. Explicitly allow published fields with similar spelling in Script, external engine, result, telemetry, cache-display, and performance contracts until their owning Plan 11 audit; do not turn this task into a global token rename. Preserve the existing Clear route unchanged.

Keep Studio as a Preact composition and external-boundary adapter: use direct values or existing Signals, add no effect-based mirror, and do not reconstruct resolver, transaction, acknowledgement, rollback, history, cache, or clipboard semantics. Preserve the existing runtime/display ports, physical coordinator instance, store invalidation, Script behavior, Basic/FX/non-Roto branches, and dormant-module sequencing. Finish with the exact three-symbol scan, targeted contract-region checks, and app typecheck only. Do not run build, Vitest, a server, Vite, Tauri, browser, watcher, or the native application.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/components/physic-paint/PhysicsPaintStudio.tsx','app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts','app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts','app/src/components/physic-paint/hooks/useRotoTimelineActions.ts','app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts'];const exact=[['saveRealKeyAt','DisplayFrame'].join(''),['rotoTimelineActions','Ref'].join(''),['resolvePasteTargetFor','DisplayFrame'].join(''),['RotoKeyUtility','Transaction'].join(''),['RotoKeyPersistence','Port'].join('')];const texts=new Map(files.map(f=>[f,fs.readFileSync(f,'utf8')]));const hits=[];for(const [file,text] of texts)for(const token of exact)if(text.includes(token))hits.push(file+': '+token);const between=(text,start,end)=>{const a=text.indexOf(start);if(a&lt;0)throw new Error('missing scope '+start);const b=text.indexOf(end,a+start.length);if(b&lt;0)throw new Error('missing scope end '+end);return text.slice(a,b)};const scopes=[['key utilities',between(texts.get(files[1]),'export interface RotoKeyUtilitiesInput','export interface RotoKeyUtilities')],['navigation',between(texts.get(files[2]),'export interface UseRotoNavigationCoordinatorInput','export function useRotoNavigationCoordinator')],['timeline actions',between(texts.get(files[3]),'export interface RotoTimelineActionsInput','interface PhysicalActionRunnerInput')]];const studio=texts.get(files[0]);const marker=studio.indexOf('keyUtilities:');if(marker&lt;0)throw new Error('missing Studio keyUtilities composition');scopes.push(['Studio key utilities',studio.slice(marker,Math.min(studio.length,marker+5000))]);const concepts=[['source','Frame'].join(''),['display','Frame'].join(''),['inBetween','Count'].join(''),['segmentSpacing','Overrides'].join('')];for(const [name,text] of scopes)for(const token of concepts)if(text.includes(token))hits.push(name+': '+token);if(hits.length){console.error(hits.join('\n'));process.exit(1)}" &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <done>The temporal save producer, ref, target resolver, and every approved forwarding/consumer contract are removed atomically from the five-owner route; the exact symbols and targeted temporal concepts are absent; direct physical composition typechecks at the task boundary; no test/build/UAT work occurred; and Plan 36.14-11 can perform the central reachability audit at Wave 13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI intent → key-utility/navigation hooks | User actions and current selection enter orchestration code and must not be allowed to supply durable timing or transaction primitives. |
| Key-utility semantic port → physical resolver/coordinator | A utility operation crosses into physical ownership mutation and must use validated complete proposals plus exact acknowledged settlement. |
| Standalone webview → parent bridge | Optimistic state becomes authoritative only after the matching parent acknowledgement for the same launch, layer, operation, and revision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-19-01 | Spoofing | Utility operation identity | high | mitigate | Resolve the current stable key and physical frame at execution, then rely on existing launch/layer/revision revalidation before publication and settlement. |
| T-36.14-19-02 | Tampering | Legacy local transaction stage | high | mitigate | Remove frame-list mutation and fire-and-forget persistence from key utilities; permit physical changes only through complete resolver proposals and the existing acknowledged coordinator. |
| T-36.14-19-03 | Repudiation | Optimistic utility result | medium | mitigate | Preserve accepted-only history and coordinator settlement so rejected, stale, timed-out, or transport-failed work cannot appear as an accepted operation. |
| T-36.14-19-04 | Information Disclosure | Utility failure status | low | mitigate | Keep the capsule/status message concise and route diagnostic detail through the existing LOG boundary rather than exposing bridge internals in controls. |
| T-36.14-19-05 | Denial of Service | Repeated Duplicate/Paste activation | medium | mitigate | Reuse the coordinator pending-operation signal and existing disabled reasons; do not introduce another pending flag or concurrent settlement path. |
| T-36.14-19-06 | Elevation of Privilege | Generic mutation-capable utility port | medium | mitigate | Replace generic transaction and persistence callbacks with narrowly semantic operations that cannot stage arbitrary frame maps outside the resolver/coordinator. |

No npm, pip, or cargo install occurs; the package-legitimacy gate and supply-chain install checkpoint are not applicable.
</threat_model>

## Multi-Source Coverage Audit

### GOAL coverage

| Source item | Coverage |
|---|---|
| ROADMAP goal: one deterministic physical-frame authority followed by polished UI | Plan 20 establishes semantic Duplicate/Paste proposal, acknowledgement, and accepted-history ownership; Plan 19 composes those seams through the remaining production-reachable utility route; Plan 11 audits the binary production cutover; Plans 12-18 retain the approved UAT, regression, UI, and final-UAT sequence. COVERED. |

### REQ coverage

| Requirement | Coverage |
|---|---|
| 36.14-PHYSICAL-IDENTITY | Plan 19 Tasks 1-3 remove projected utility ownership and route operations by stable identity and direct physical frame. COVERED. |
| 36.14-DERIVED-INTERPOLATION | Plan 19 Tasks 2-3 remove live count and spacing inputs while preserving enabled-only interpolation; existing Plans 02, 03, 10, and 14 cover exact interiors. COVERED. |
| 36.14-ATOMIC-FRAME-MAPPING | Plan 20 owns Duplicate/Paste complete proposals, centralized staging, exact acknowledgement, rollback, and accepted-only history; Plan 19 Tasks 1-2 compose those public seams without rebuilding them. Plans 04-08 and 13 cover the existing edit transaction and regressions. COVERED. |
| 36.14-RIPPLE-INSERT-DELETE | Existing Plans 06, 13, and 18 retain ownership; Plan 19 preserves the shared physical action composition. COVERED. |
| 36.14-RIPPLE-DRAG | Existing Plans 07, 13, 16, and 18 retain ownership and are unchanged. COVERED. |
| 36.14-FORCE-SPACING | Existing Plans 08, 13, 15, and 18 retain ownership; Plan 19 removes stale spacing authority without changing the session-only command. COVERED. |
| 36.14-DOWNSTREAM-PARITY | Plan 19 closes the remaining Studio/navigation/key-utility route; Plans 09, 10, 11, and 14 cover persistence and render consumers. COVERED. |
| 36.14-UI-INTEGRATION | Existing Plans 15-18 retain the final UI and UAT work and are unchanged except for wave metadata. COVERED. |
| 36.14-UAT-THEN-REGRESSION | Plan 19 forbids all test work and hands off to Plan 11, then the byte-exact Plan 12 approval gate and existing Plans 13-18. COVERED. |

### RESEARCH coverage

| Research feature or constraint | Coverage |
|---|---|
| One stable identity and direct physical location | Plan 19 Tasks 1-3. COVERED. |
| One deep pure physical resolver | Plan 20 extends the canonical resolver with semantic Duplicate/Paste complete-record proposals; Plan 19 delegates to those public seams and forbids copied mapping logic. COVERED. |
| Complete immutable snapshot reconstruction | Plan 19 delegates settlement and rollback; existing Plans 04, 09, and 10 remain owners. COVERED. |
| One acknowledged physical-edit coordinator | Plan 20 extends the existing coordinator and accepted-history classification for Duplicate/Paste; Plan 19 removes the utility bypass and reuses that authority. COVERED. |
| Direct/computed Preact view state; no effect orchestration | Plan 19 Task 3 explicitly requires direct composition and no reactive mirror. COVERED. |
| Presentation emits intent and does not compute timing or availability | Plan 19 semantic ports plus existing Plans 15-17. COVERED. |
| Avoid half-migrated readers, generic utility settlement, incremental capacity checks, and split preview/commit logic | Plan 19 removes the remaining generic utility route and delegates validation to the existing resolver/coordinator. COVERED. |
| Clean cutover with no migration or compatibility layer | Plan 19 scope constraints and all three tasks. COVERED. |
| No external package | Plan 19 changes no dependency artifact. COVERED. |
| Production → typecheck/build → native UAT → regression | Plan 19 runs bounded typecheck only, Plan 11 owns the final typecheck/build, Plan 12 owns approval, and Plans 13-14 own regressions. COVERED. |

### CONTEXT coverage

| Decision | Coverage |
|---|---|
| D-01 | Tasks 1-3 use stable key identity and direct physical frame. COVERED. |
| D-02 | Tasks 2-3 preserve enabled-only strict-interior interpolation and remove utility count authority. COVERED. |
| D-03 | Tasks 2-3 remove projection, count, spacing, transaction, persistence-port, and bridge authority without aliases or migration. COVERED. |
| D-04 | Tasks 2-3 preserve the separate Script Motion contract. COVERED. |
| D-05 | Task 1 preserves shared physical action composition; Plan 06 remains the Insert owner. COVERED. |
| D-06 | Tasks 2-3 preserve shared Delete/navigation composition and leave Clear unchanged; Plan 06 remains the Delete owner and Plan 11 owns stable-identity Clear cleanup through `useRotoFrameEditingController.ts`. COVERED. |
| D-07 | Unchanged Plans 07, 13, 16, and 18 own ripple Drag. COVERED. |
| D-08 | Plan 19 removes stale spacing authority while unchanged Plans 08, 15, and 18 own Force Spacing. COVERED. |
| D-09 | Plan 20 owns semantic Duplicate/Paste proposal, acknowledgement, rollback, and accepted-only history; Tasks 1-2 remove the utility bypass and compose those exact seams. COVERED. |
| D-10 | Task 3 closes the remaining production route; Plans 09-11 and 14 cover all downstream consumers. COVERED. |
| D-11 | Plan 19 is a bounded corrective wave before the separate audit/UAT/regression/UI waves. COVERED. |
| D-12 | Scope constraints and all verifications preserve the UAT-before-regression sequence. COVERED. |
| D-13, D-14, D-15, D-16 | Unchanged Plan 15 retains compact responsive geometry and scrolling decisions. COVERED. |
| D-17, D-18, D-19, D-20 | Unchanged Plans 08 and 15 retain explicit session-only Force Spacing Apply behavior. COVERED. |
| D-21, D-22, D-23, D-24 | Unchanged Plan 16 retains complete ripple-drag presentation and identity-following focus. COVERED. |
| D-25, D-26 | Plan 19 preserves concise status/LOG boundaries; unchanged Plans 15 and 17 finish presentation. COVERED. |
| D-27, D-28 | Plan 19 preserves Script clipboard/library behavior; unchanged Plans 15 and 17 own final control placement and guarded focusability. COVERED. |
| Deferred multi-selection/group movement | EXCLUDED by CONTEXT; not introduced. |
| Deferred migration/compatibility support | EXCLUDED by CONTEXT and D-03; not introduced. |

<verification>
1. Confirm Git changes during execution are limited to the exact five production files listed in frontmatter plus the Plan 19 SUMMARY generated by the executor.
2. Confirm Task 2 retains a complete producer/consumer temporal-save bridge and typechecks; then run Task 3's bounded scan proving `saveRealKeyAtDisplayFrame`, `rotoTimelineActionsRef`, `resolvePasteTargetForDisplayFrame`, the legacy utility transaction, and the persistence port are absent from the five owners, followed by `pnpm --dir app typecheck`.
3. Confirm Duplicate and both Paste forms consume the public interfaces recorded by `36.14-20-SUMMARY.md`, while Copy remains immutable/reusable, navigation remains direct physical `appFrame`, and Clear remains unchanged for Plan 11.
4. Confirm no regression file, test configuration, package file, manifest, lockfile, generated dependency, dormant module, approval artifact, or native-UAT artifact changed.
5. Confirm no Vitest, build, server, watcher, browser, or native application was run.
6. Confirm Plan 36.14-11 remains next at Wave 13 and owns the central production reachability audit, stable-identity Clear cleanup through `useRotoFrameEditingController.ts`, and the sole final pre-UAT `pnpm --dir app typecheck && pnpm --dir app build` gate.
</verification>

<success_criteria>
- The Studio → navigation coordinator → key utilities route reaches physical resolver/coordinator authority through explicit semantic ports.
- The temporal save bridge's exact producer/ref/target-resolver symbols and the legacy utility transaction/persistence-port symbols are absent from all five approved owners after one atomic Task 3 removal, and source/display/count/spacing concepts are absent from the targeted contract/composition regions without rejecting unrelated published fields.
- Duplicate and both Paste forms consume Plan 20's semantic ordinary operation kinds, complete proposals, acknowledged settlement, rollback, and accepted-only history.
- Copy remains immutable and reusable; navigation uses direct physical `appFrame`; Clear remains behaviorally unchanged and explicitly awaits Plan 11 cleanup through `useRotoFrameEditingController.ts`.
- Preact composition uses direct values/existing Signals and adds no effect mirror or duplicate pending state.
- Basic, FX, non-Roto Physics Paint, Script, deterministic Script Motion, cache, dormant-module sequencing, and approval contracts remain unchanged.
- Bounded static verification and app typecheck pass; no tests or build run; no UAT or phase-completion claim is made.
- The repository is ready for existing Plan 36.14-11 at Wave 13.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-19-SUMMARY.md` when done, recording Plan 20's five-interface consumption, the exact five-file cutover, Task 2's typecheckable retained bridge, Task 3's atomic removal of `saveRealKeyAtDisplayFrame`, `rotoTimelineActionsRef`, `resolvePasteTargetForDisplayFrame`, and all approved forwarding, targeted contract-region results, preserved Copy/navigation/Clear behavior, bounded verification, hard-stop outcome if applicable, and handoff to Plan 36.14-11 at Wave 13. Do not claim native UAT or phase completion.
</output>
