---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 30
subsystem: physics-paint-roto
tags: [regression, contract-transfer, physical-cutover, gate-closure]
requirements:
  - 36.14-UAT-THEN-REGRESSION
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
dependency-graph:
  requires:
    - 36.14-29 native gap UAT approval (commits d6169775, 5ca0ca8b)
    - gate-baseline-typecheck.txt / gate-baseline-vitest.txt measured baselines
  provides:
    - Final three-command gate green in one pass from repo root (typecheck, vitest run, build)
    - 17 repaired test files + 1 wholesale-deleted test file with per-assertion triage ledger
    - Production diff confined to three authorized files, no new production symbols
  affects:
    - app/src/lib/physicPaintBridge.ts (dead declaration removed)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (import + port type boundary)
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts (unused declaration)
tech-stack:
  added: []
  patterns:
    - Canonical physical document fixtures via replaceRotoPhysicalDocument + buildPhysicPaintRotoPhysicalRevision
    - Document-mock + registerRotoAlphaCanvasFrame pattern for blend rendering in node environment
    - Exact physical render-source collector mirrors in renderer mocks
key-files:
  created:
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-30-SUMMARY.md
  modified:
    - app/src/lib/physicPaintBridge.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
    - 17 test files (see ledger)
  deleted:
    - app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts
decisions:
  - Task 2 blocking checkpoint resolved authorize-all: three minimal production fixes (a/b/c) applied with no runtime behavior change
  - Blend-mode cache scenarios that never exercised blend rendering transferred to duplicate; blend rendering itself re-proven through the document-mock canvas pattern
  - Closed-persistence fail-closed behavior (unsafe paths, malformed outputs) asserted as rejection instead of retired skip-and-continue
metrics:
  duration: ~3h20m (Tasks 3-11; Task 1 tracer and Task 2 checkpoint completed in the prior session)
  completed: 2026-07-25
  tasks: 11
  files: 21 (3 production + 17 test + 1 summary; 1 test file deleted)
status: complete
---

# Phase 36.14 Plan 30: Bounded Stale-Test Contract Transfer Summary

**One-liner:** All 18 baseline-failing test files triaged assertion-by-assertion to the canonical physical-frame contract (17 repaired, 1 deleted wholesale), three checkpoint-authorized minimal production fixes applied, and the deferred three-command gate is green in one pass from repo root.

## Final Gate Evidence (one sequential pass from repo root, 2026-07-25)

| Command | Exit | Result |
|---------|------|--------|
| `pnpm --filter efx-motion-editor typecheck` | 0 | tsc --noEmit clean (was 73 error lines at baseline) |
| `pnpm --filter efx-motion-editor exec vitest run` | 0 | 78 passed / 3 skipped files; **717 passed, 0 failed**, 1 skipped, 101 todo (was 18 files / 85 tests failed at baseline) |
| `pnpm build` | 0 | @efxlab/efx-physic-paint package build + app `tsc --noEmit && vite build` both succeed |

**Skipped/todo accounting vs baseline (1 skipped / 82 todo):** 1 skipped test and 3 skipped files are identical to baseline. The todo count moved 82 -> 101 solely because `exportRenderer.test.ts` failed at collection time in the baseline (its 19 pre-existing `it.todo` entries were never counted); 82 + 19 = 101. This plan introduced zero new `it.skip`/`it.todo` entries.

**Production diff confinement (verified via `git diff 415a3e21..HEAD --stat -- app/src -- ':(exclude)*.test.ts'`):** exactly `physicPaintBridge.ts` (7 deletions), `PhysicsPaintStudio.tsx` (3/3), `useRotoFramePersistenceCoordinator.ts` (1 deletion). No new production symbols, adapters, wrappers, aliases, or config changes.

## Task 2 Checkpoint Resolution (decision-only task, no separate commit)

The blocking decision checkpoint was presented with the three baseline production-file typecheck errors. **User response: authorize-all.** Recorded in the Task 3 commit message body per the checkpoint resolution instructions:

- (a) `PhysicsPaintStudio.tsx:1` — removed unused `useEffect` import (TS6133).
- (b) `useRotoFramePersistenceCoordinator.ts:64` — deleted unused `const buffer = editBuffer.bufferRef.current;` (TS6133).
- (c) `PhysicsPaintStudio.tsx:448` — minimal type-level correction at the `executePhysicalEdit` port boundary (`as RotoPhysicalEditCoordinatorExecuteInput<SerializedProject>`, matching the coordinator's `useRotoPhysicalEditCoordinator<SerializedProject>` instantiation; `as` casts are an established convention in that file). No runtime behavior change.

## Commit Log

| Task | Commit | Scope |
|------|--------|-------|
| 1 (tracer, prior session) | c97bd5f0 | Script-clipboard cluster to green (32/32) |
| 3 | 0289dcf4 | buildRotoRevision removal (proven dead repo-wide first) + authorized fixes a/b/c |
| 4 | 54149fde | rotoLaunchHydration.test.ts wholesale deletion; launch context files repaired |
| 5 | a62e85bc | Bridge tests to canonical physical launch contract |
| 6 | 1ec41371 | Store tests to gap-derived regeneration triggers |
| 7 | 55261b76 | Preview/export/frame-map parity to direct physical positions |
| 8 | 39db13bd | Persistence + Play Script bridge to canonical document |
| 9 | c4d9c76f | Three hook contract files to current port/input surfaces |
| 10 | 59c42f2a | Play Script controller + three UI contract files |
| 11 | (this commit) | Final gate + summary |

## Per-File Triage Ledger

### 1. physicsPaintRotoScriptClipboard.test.ts (Task 1, tracer — prior session)
Snapshot literals -> `{ selectionKind, layerId, keyId, appFrame }`; `prepareEmptyTarget` -> async `prepareTarget`; `copiedSourceFrame` -> `copiedAppFrame`; accepted-target expectations -> `{ keyId, appFrame, publishPixels, publicationIdentity }`; retired interpolationSettings accepted-target expectation deleted; empty-target error message updated to the physical destination contract. 32/32 green.

### 2. rotoLaunchHydration.test.ts (Task 4) — DELETED WHOLESALE
Production `hydrateRotoLaunchContext` unconditionally throws "Legacy Roto launch hydration was retired by the physical persistence cutover." (D-03). All four tests exercise a dead contract with zero transferable assertions. Whole-file coverage deletion is acceptable only because the covered function is itself retired; recovery is one git revert away.

### 3. physicsPaintLaunchContext.test.ts (Task 4) — TRANSFER + selective DELETE
- Fixtures rebuilt as canonical physical launch envelopes (project + rotoPhysical with canonical revision via `buildPhysicPaintRotoPhysicalRevision`).
- Retired launch-level `rotoBackground` literals removed from both Partial literals (TS2353 x2); background fidelity re-pointed to `rotoPhysical.background`.
- Flat query-param assertions (`?layer=...&op=...`) DELETED as positive parses — flat encoding was retired by Plan 09's canonical launch validation; the same inputs are now asserted to reject (null), preserving the "rejecting incomplete input" intent at full strength.

### 4. usePhysicsPaintLaunchIntegration.test.ts (Task 4) — TRANSFER
- Snapshot literals (x3) -> canonical `{ selectionKind, layerId, keyId, appFrame }`; `prepareEmptyTarget` -> async `prepareTarget`; accepted-target expectation -> `{ keyId, appFrame, publishPixels, publicationIdentity }`.
- `applyLatest` mock return type fixed (TS2322) with a block body.
- One added microtask settle after `applyScript()` before the replacement request: the retired port was synchronous, the canonical `prepareTarget` is async; the drain-a-real-accepted-Apply contract requires the Apply to be registered as the active operation first. Drain/retention assertions kept at full strength.

### 5. physicPaintBridge.test.ts (Task 5) — TRANSFER + selective DELETE
- Launch hydration tests seed `replaceRotoPhysicalDocument`; assertions re-pointed from retired `cachedRotoFrames`/`rotoInterpolationSettings` launch fields to `rotoPhysical.records`/selection/interpolation (deleted field assertions recorded; no replacement launch-level field added).
- Defensive paper-metadata copy asserted on `rotoPhysical.background` (equals + not same reference), removing the retired `context.rotoBackground` access (TS2339).
- D-16 generated-only launch target re-proven physically: interior frame 13 between real keys 12/14 launches render-only (no editableState, no selected key, records stay [12, 14]).
- Invalid-launch rejection expects the current canonical-construction error message; `open` still not called.
- Generated-cache publication test uses duplicate mode: blend regeneration requires registered DOM alpha canvases (pseudo-PNG fallback retired by 36.14-23); blend rendering coverage stays in store tests.
- Browser fallback listener tests flush the asynchronous `applyPreparedPhysicPaintPayload` seam (3 microtasks) before asserting the exactly-one dispatch and the child postMessage reply — dispatch-count strength unchanged.

### 6. physicPaintStore.test.ts (Task 6) — TRANSFER + selective DELETE
Deletions (retired D-03/Plan 09 contracts):
- "D-01/D-02 restores only persisted spacing..." — per-segment spacing restoration on load; load now rejects generated cache metadata and no longer regenerates.
- "D-04 serializes source-endpoint overrides..." — override serialization + rebuild without generated metadata; asserts the retired regeneration-on-load path.
- "D-05/D-06 prunes loaded stale trailing rendered frames..." — source/display metadata pruning; load installs durable frames verbatim.

Transfers:
- Two regeneration-on-load tests now re-assert `{ enabled: true }` after `loadFromMceOutputs`, driving the live regeneration path; all generated-frame expectations unchanged.
- Blend -> duplicate for scenarios that never exercised blend rendering (count-change authority, toggle, provenance, alpha-only span): pseudo-PNG blend fallback was deliberately retired by 36.14-23 (fail-closed without registered canvases).
- Blend scenarios kept in blend and given the file's established document-mock + `registerRotoAlphaCanvasFrame` pattern with input-sensitive `toDataURL`: mode normalization (`alpha-blend` -> `blend` branch renders distinct output), PNG derivation from both neighboring alpha sources (dataUrl carries both canvas ids; backgroundOnly/onion metadata proven inert), regeneration after upsert/removal/replacement (changed right neighbor changes output).
- 14 null-safety typecheck errors resolved via optional chaining/narrowing consistent with file conventions (no new `as` casts).

### 7. previewRenderer.test.ts (Task 7) — TRANSFER
- All Roto render tests seed physical documents; the renderer's physical branch (`isPhysicalRotoWorkflowLayer`) now engages.
- Interior missing frame -> paper texture; paper baseline before transparent real pixels; renderer-owned paper before generated alpha — all re-proven with the physical render-source path.
- Generated-alpha expectations re-pointed from legacy cache payloads to physically derived duplicate output (left-key alpha at interior frames); right-neighbor pixels proven absent.
- 36.13 span expectation moved from override-aware display positions to gap-derived direct appFrames 3-5 (no override concept); parity assertion kept.
- 36.11 merged repaint drives the apply-canvas physical payload update (existing record at frame 5) before asserting paper-before-alpha.
- Source-contract patterns updated to current production (`resolvePhysicPaintFrameSource`, `physicalBackgroundDraw`).

### 8. exportRenderer.test.ts (Task 7) — TRANSFER (collection failure diagnosed first)
- Collection-time `TypeError: _setPaintMarkDirtyCallback is not a function` diagnosed as the paintStore/projectStore late-bound callback cycle evaluated paintStore-first through `previewRenderer.ts:15 -> paintStore.ts:6 -> projectStore.ts:24`. Fixed test-side by evaluating projectStore first (import order + comment); no production change, no shim.
- Source-contract patterns updated (`resolvePhysicPaintFrameSource`, `getRotoPhysicalRenderSource`, `getRotoRealKeyRecords`).
- Collector mocks mirror the production physical render-source resolver; parity/preload assertions re-proven at identical physical appFrames on both sides; 36.13 span at appFrames 3-5 after save/load; stale-FX-range expansion via `getRotoPhysicalEndFrame` (keys 0/4/8).

### 9. frameMap.test.ts (Task 7) — TRANSFER
- Timeline/FX range extension moved from display frames to physical frames: keys at appFrames 0/4/8 with interpolation enabled -> physical end frame 9 -> 9 entries, FX outFrame 9. Extension expectation at full strength.

### 10. physicPaintPersistence.test.ts (Task 8) — TRANSFER + selective DELETE
- Cache paths use the stable hashed layer directory (anchored regex `physic_layer_1-[0-9a-f]{8}`, not a hardcoded hash).
- DELETED "stores transparent Roto onion frames as project cache files": the closed physical output contract (`OUTPUT_KEYS = layer_id/frames/roto_physical/roto_playback`) rejects `roto_cache_metadata`/onion channels outright; no canonical onion persistence channel exists.
- Real-key hydration transferred to the canonical `roto_physical` document with sidecar `cache_path` payloads; generated metadata proven absent from serialization; hydration round-trips dataUrls and interpolation.
- Unsafe cache paths: skip-and-continue expectations replaced by fail-closed rejection assertions plus proof the secret file is never read (stronger contract, same security intent).
- Malformed outputs: same fail-closed transfer.
- Non-PNG rejection asserts the current closed-validation message.
- Per-test unique outputs avoid the production `savedOutputCache` dedupe between tests (test isolation fix).

### 11. physicPaintPlayScriptBridge.test.ts (Task 8) — TRANSFER
- Parent authority fixtures seed the canonical physical document; authority frames asserted by `appFrame` identity (payloads carry no sourceFrame).
- Batch fixtures echo the authority frame for the untouched key (mirrors the child contract; avoids legacy display metadata mismatch in `sameDurableRealKey`).
- Large-range capping expectation updated to the physical-capacity bound: `min(remaining, 600-4, 600)` = 596, end 600.
- Omission/modification/injection rejections use the production dual `sourceFrame ?? appFrame` lookup; onion-modification rejection kept (added onion field mismatches the authority frame).
- Background persistence asserted at `roto_physical.background` (canonical serialization); transparent background round-trip kept.
- Exact-retry idempotency and altered-operation-ID collision assertions unchanged and passing at full strength (ATOMIC-FRAME-MAPPING re-proof).
- Strict-base64 PNG payloads for physical records (canonical payload guard).

### 12. useRotoCachedPlayback.test.ts (Task 9) — TRANSFER
- Retired `initialFps` input (x4) -> `initialSettings: { loop, fps }`; `resetForLaunch` now passes the required settings argument (TS2554).
- Final-transient-frame test: the last frame displays before the next tick finishes and clears it (current non-loop finish contract); stale ticks after stop still ignored; `setIsPlaying` transition count unchanged.

### 13. useRotoPlayScriptController.test.ts (Task 9) — TRANSFER + selective DELETE
- Retired `mirrorAccepted`/`getBackground` port mocks deleted (ports no longer exist).
- Coordinator port surface added (`executePhysicalEdit`, `pendingOperationId`, `acceptedOutput`); selection literals -> `{ kind, keyId, appFrame }`.
- Proxy-freshness assertions kept at full strength against first vs second input ports; `requestAuthority`/`commit` wiring asserted on the captured controller ports.

### 14. useRotoReferenceController.test.ts (Task 9) — TRANSFER
- Resolution-order test rewritten for the exact physical-cell lookup: current generated PNG source (revision-correlated cache key), real source with key identity, dirty preview fallback gated by keyId+contentRevision (matching preview served; stale keyId falls back to the real source), stale generated revision -> null, no generic-store fallback (legacy inputs proven ignored).
- Dirty-frame refusal test: updated to the current "Cached physical base loaded" status message; refusal/repaint-base semantics unchanged.
- Canonical PNG signature payloads (`isRotoPngDataUrl` requires real PNG signature bytes).

### 15. physicsPaintRotoPlayScriptController.test.ts (Task 10) — TRANSFER
- Authority fixtures carry `physicalCapacity`/`physicalRevision`/`physicalRecords` (TS2322); selections `{ kind, keyId, appFrame }` (TS2353 x3); commit mocks return `RotoPlayScriptCommitResult` with acknowledgement echo (TS2322 x2).
- Additive commit asserts the physical publication: records [1,4,5] with existing key payload preserved, `semanticDelta` (kind/affected range/expected capacity/end/freshKeyIds x2), interpolation echo, selected identity = fresh key at start.
- Retired `rotoBackground`/`mirrorAccepted` assertions deleted; acknowledgement validation (mismatched parent ack rejected) and authority/selection revalidation before commit kept at full strength.
- Staged renderer frames use canonical PNG payloads (`buildPhysicalPublication` validates `isRotoPngDataUrl`).
- Availability computed is signal-driven; the generated-selection case bumps the phase signal to force re-evaluation (same technique as the pre-existing test).

### 16. PhysicsPaintStudio.test.ts (Task 10) — TRANSFER
- Delete-shortcut wiring asserts the current shared `rotoPhysicalActions.deleteRotoFrame` reference across keyboard (`deleteRotoKey:`) and visible-button (`onDeleteRotoFrame:`) paths.

### 17. PhysicsPaintWorkflowStrip.test.ts (Task 10) — TRANSFER
- Control presence: interpolation controls/toggle/mode-select/force-spacing labels replace the retired connector class; onion and key utility props unchanged.
- Mutation lock: exact current disable clause (`props.ready === false || Boolean(props.mutationLocked) || Boolean(props.rotoInterpolationPending)`) and handler guards (interpolation toggle guard + force-spacing guard) matched to production source text.
- Generated non-editable cells: physical `semanticCell` kinds (`generated`/`real`), drag eligibility real-key only; map-block slicer re-anchored to `physics-paint-roto-key-utilities`.

### 18. PhysicsPaintScriptsPanel.test.ts (Task 10) — TRANSFER
- The accessible Play Script dialog contract now asserts against `PhysicsPaintPlayScriptDialog.tsx` (dialog extracted from the panel) plus its `PhysicsPaintStudioView` mount: role/aria-modal/aria-labelledby, Max capacity, validation copy, Escape/Enter/Tab handling, focus management (`inputRef`/`returnFocusRef`), and no cached-playback conflation.

## Deviations from Plan

**1. [Rule 1 - Bug] Persistence onion cache-file test deleted instead of transferred**
- **Found during:** Task 8.
- **Issue:** The plan listed "transparent Roto onion frames stored as project cache files" as a live contract to transfer, but the current closed persistence contract (`OUTPUT_KEYS`) rejects `roto_cache_metadata`/onion channels; no canonical onion persistence channel exists to transfer to.
- **Fix:** Deleted the test with the recorded reason (closed physical output contract); no production change.
- **Files modified:** app/src/lib/physicPaintPersistence.test.ts

**2. [Rule 1 - Bug] exportRenderer collection failure fixed test-side via import order**
- **Found during:** Task 7.
- **Issue:** paintStore/projectStore late-bound callback cycle crashed when evaluated paintStore-first through previewRenderer. Production import order (app entry) is unaffected; fixing the cycle structurally would be a production architectural change (Rule 4 territory) and is NOT needed for the gate.
- **Fix:** Evaluate projectStore first in the test file (documented with a comment). No production change, no mock, no shim. Flagged here for visibility: the latent cycle remains in production module topology.

**3. [Rule 2 - Missing critical functionality] Async seam settling in two tests**
- **Found during:** Tasks 4 and 5.
- **Issue:** The canonical `prepareTarget` port and `applyPreparedPhysicPaintPayload` seam are asynchronous where the retired ports were synchronous; two tests needed explicit microtask flushes to preserve their original contract (drain-before-clear/load; exactly-one dispatch).
- **Fix:** Added documented microtask settles; assertions unchanged in strength.

No other deviations. No architectural changes. No native UAT triggered (production behavior unchanged beyond the three authorized type-level fixes; all fixes are deletion-level or type-level with zero runtime effect, matching the approved native surface).

## Known Stubs

None. No stub patterns introduced; no expectation-free tests remain.

## Threat Flags

None. No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries. The unsafe-path persistence test strengthens the existing fail-closed file-read boundary assertion.

## Self-Check

- Commits verified: c97bd5f0, 0289dcf4, 54149fde, a62e85bc, 1ec41371, 55261b76, 39db13bd, c4d9c76f, 59c42f2a all present in `git log`.
- `grep -c buildRotoRevision app/src/lib/physicPaintBridge.ts` = 0.
- All 18 baseline-failing files addressed: 17 green, 1 deleted with justification.
- Final gate: three commands, one pass, all exit 0.

## Self-Check: PASSED
