---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 11
type: execute
wave: 11
depends_on:
  - 36.14-10
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/project.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintPersistence.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts
  - app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts
  - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
  - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts
  - app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts
  - app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSession.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.ts
  - app/src/components/physic-paint/roto/rotoApplyTransactions.ts
  - app/src/components/physic-paint/roto/rotoCacheTransactions.ts
  - app/src/components/physic-paint/roto/rotoCanvasFrames.ts
  - app/src/components/physic-paint/roto/rotoKeyTransactions.ts
  - app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.ts
  - app/src/components/physic-paint/roto/rotoPhysicalOwnership.ts
  - app/src/components/physic-paint/roto/rotoSaveTransactions.ts
  - app/src/components/physic-paint/roto/rotoSourceDisplayModel.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "D-01/D-03: Every durable real Roto key has one stable keyId and one direct physical appFrame; source/display coordinates, frame-pair aliases, nearest-owner fields, interpolation counts, spacing overrides, generated timing records, migration readers, compatibility readers, hydration fallbacks, and dual writes are absent from production Roto authority."
    - "D-02/D-04: Interpolation persistence contains enabled only; every generated cell is derived at runtime from each strict interior between adjacent real keys, while Script Motion deformation and position remain a separate sibling contract and never determine key spacing."
    - "D-05/D-06/D-07/D-08: Insert, Delete, Drag, Force Spacing, Undo, and Redo reach one pure physical resolver, one acknowledged coordinator, and one generic accepted-only history route; no operation-specific settlement, rollback, move history, or view-side remap path remains."
    - "D-09/D-10: Persistence, reopen, bridge, caches, dirty state, selection, playback, onion/reference, preview, export, missing/background rendering, timeline extent, Script Copy/Apply, Script library, and Play Script consume stable identity plus direct placement or runtime-derived semantic cells without rebuilding timing locally."
    - "D-09: Accepted complete keyId-to-appFrame mappings publish atomically; malformed, partial, duplicate, stale, unknown, and acknowledgement-mismatched data fail closed without partial store, cache, selection, history, or visual publication."
    - "D-11/D-12: Production cleanup is binary and precedes native UAT; no regression artifact is created, modified, deleted, renamed, or executed, and the only final validation commands are app typecheck followed by app build."
  artifacts:
    - "Closed production schemas in physicPaint.ts and project.ts expose physical real-key identity, direct appFrame, enabled-only interpolation, separate Script Motion, and complete physical edit/acknowledgement contracts."
    - "physicPaintStore.ts, physicPaintPersistence.ts, and physicPaintBridge.ts accept and publish only validated complete physical state with runtime generated cells and identity-owned payload/cache state."
    - "physicsPaintRotoPhysicalResolver.ts, useRotoPhysicalEditCoordinator.ts, and useRotoPhysicalEditHistory.ts are the sole resolver, settlement coordinator, and accepted history authorities for physical timeline edits."
    - "Roto Studio, workflow, controllers, session, cache, persistence, and Script surfaces call the physical authority directly and contain no source/display adapter or operation-specific edit transaction path."
    - "Legacy-only source/display, move settlement/history, key transaction, save transaction, apply transaction, and cache projection modules are deleted after their remaining non-timing responsibilities are moved to focused physical modules."
    - "The Plan 11 summary records the production consumer reachability audit, exact deleted modules, typecheck/build results, and readiness for user-owned native UAT in Plan 12 without claiming approval."
  key_links:
    - "Persisted/project/bridge Roto input -> closed runtime validator -> one complete physical snapshot -> atomic store publication."
    - "Timeline/toolbar/shortcut/drag/Force Spacing/Undo/Redo intent -> one pure resolver -> one acknowledged coordinator -> one generic accepted-only history command."
    - "Stable keyId ownership -> paint/editable/cache/dirty/selection/Script target state -> current direct appFrame without frame-number ownership transfer."
    - "Enabled-only interpolation plus adjacent real physical keys -> runtime strict interiors -> downstream playback/onion/reference/preview/export/timeline consumers."
    - "Script Copy/Apply, durable library, and Play Script -> immutable Script snapshot plus separate Motion settings -> complete physical batch through canonical parent authority."
    - "Production-only legacy scan and consumer reachability audit -> typecheck -> build -> hard stop before native UAT."
  prohibitions:
    - "No regression file creation, modification, deletion, renaming, or execution; no Vitest command and no test runner command."
    - "No application, Vite, preview, Tauri, browser, watch, or development server process."
    - "No package installation, removal, upgrade, manifest edit, lockfile edit, generated dependency file, or package-manager audit."
    - "No sourceFrame, displayFrame, fromSourceFrame, toSourceFrame, nearestRealKeyFrame, inBetweenCount, segmentSpacingOverrides, RotoSourceDisplayModel, getRotoDisplayProjection, coordinate fallback, generated durable timing record, migration branch, compatibility adapter, or physical-state dual write in production Roto contracts or consumers."
    - "No operation-specific physical apply kind, move-only settlement, move-only rollback, move-only history, pairwise remap, stale-spacing restoration, view-owned physical mutation, or direct bridge commit outside the acknowledged coordinator."
    - "No generated keyId, generated payload ownership, generated selection owner, generated persisted frame, generated timeline-length authority, leading generated cell, or trailing generated cell."
    - "No useState/useEffect mirror, effect dependency chain, ref-owned business truth, prop-drilled coordinate reconstruction, or duplicate Signal for physical projection, pending edit, history, selection, or Script Motion."
    - "No change to Basic perfect-freehand behavior, FX p5.brush behavior, blend modes, paint backgrounds, non-Roto Physics Paint behavior, approved Script Copy/Apply reuse, durable Script library behavior, or Play Script behavior."
---

## Phase Goal

**As a** stop-motion animator, **I want to** have every real Roto key, generated in-between, timeline edit, persisted frame, and rendered output use one deterministic physical-frame authority, followed by a polished timeline UI that exposes the approved behavior without duplicating business logic, **so that** editing, reopening, previewing, and exporting the same Roto work cannot disagree about frame identity.

<objective>
Complete the binary production cleanup after Plans 01-10 per D-01 through D-12: remove every remaining Roto source/display timing field, alias, reader, count/override, durable generated timing record, compatibility path, dual write, operation-specific edit transaction/history path, and stale view-side edit semantic; prove every production consumer reaches stable `keyId`, direct `appFrame`, runtime-derived strict interiors, one pure resolver, one acknowledged coordinator, one generic accepted-only history path, and separate Script Motion.

Purpose: Close every alternate production route before native validation. The physical model is not authoritative while a persistence fallback, cache alias, Script adapter, view callback, move settlement, or hidden bridge branch can still reconstruct or mutate a second timeline.

Output: A closed production graph, deleted legacy-only modules, preserved Basic/FX and approved Script behavior, a deterministic production-only audit, passing `pnpm --dir app typecheck` and `pnpm --dir app build`, and a hard stop that reports readiness for user-owned native UAT in Plan 12 rather than approval or phase completion.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PLAN-OUTLINE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-01-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-02-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-03-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-04-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-06-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-07-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-08-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-09-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md

The executed Plan 01-10 SUMMARY files, not the pre-phase source shape, are authoritative for actual exported names and final file ownership. Before editing, verify all ten SUMMARY files exist and that Plan 10 reports production readiness for final cleanup. If a required physical model, resolver, coordinator, history, ownership, persistence, or downstream port is absent, stop and report the dependency delta; do not recreate source/display timing, add a compatibility layer, or widen Plan 11 to reimplement an earlier plan.
</context>

<assumptions>
- The repository is clean at Plan 11 start except for executor-owned planning state. Before the first production edit, record the current `HEAD` in `/tmp/efx-motion-editor-36.14-11-base`; Task 3 uses that immutable baseline to prove no test, package, manifest, or lockfile path changed during this plan. If the marker cannot be created or does not name the actual pre-plan commit, stop before editing.
- Stable `keyId` owns real-key paint, editable state, cached pixels, dirty state, selection, pending mutation membership, history identity, Script target identity, and current placement. `appFrame` is direct placement only and can change without transferring payload ownership.
- Physical capacity and complete-map validation remain those established by Plans 02, 04, and 09. This plan removes bypasses; it does not introduce a second bound, resolver, revision, equality function, or acknowledgement tuple.
- Interpolation persistence is exactly `{ enabled }`. Script Motion keeps deformation and position in the separate Plan 01 sibling contract; no interpolation count, spacing override, or timing mode survives under either contract.
- Generated cells have no durable identity. With interpolation enabled, the runtime projection contains every integer strictly between adjacent real-key `appFrame` values and no cell outside the first/last pair. Cached generated pixels may exist only as revision-bound derived render output and never establish timing.
- The external physics engine may require an API argument whose published name contains `destinationSourceFrame`. That package-boundary spelling may remain only at the direct engine call, populated from the target physical `appFrame`; it must not appear in local persisted types, ownership types, Script snapshots, store keys, timeline models, transaction contracts, or fallback readers. Record this exact allowlisted call site in the summary if it remains.
- Generic animation or non-Roto code outside the declared Physics Paint Roto graph may use a term such as source frame for an unrelated domain. Do not rename by repository-wide text substitution. The audit is scoped to the files and Roto symbols enumerated by Plans 01-10 plus every production importer reached from them.
- Existing `useEffect` calls remain only for external synchronization and cleanup such as timers, bridge subscriptions, pointer listeners, engine/reference lifecycle, and disposal. Physical state and derived values use existing Signals, computed values, event handlers, pure functions, or coordinator state.
- No external service, dependency, package change, test artifact, test execution, browser, application process, or server is required. Stop and report a scope delta if any becomes necessary.
</assumptions>

<scope_boundary>
Included: closed Roto runtime/project/bridge schemas; store, persistence, hydration, equality, revision, cache, live-pixel, dirty, selection, and visual publication paths; all production Insert/Delete/Drag/Force Spacing/Undo/Redo callers; physical transaction settlement/history; Roto workflow and frame-edit surfaces; Script Copy/Apply, durable Script library, and Play Script physical targeting; final deletion of source/display/count/override/generated-timing and operation-specific modules; production consumer reachability audit; no-test boundary audit; typecheck and build.

Preserved: Basic paint through perfect-freehand; FX paint through p5.brush; blend modes and paint backgrounds; non-Roto Physics Paint behavior; approved immutable reusable Script clipboard; repeated Apply; explicit library load-to-clipboard; durable Script scan/save/load/rename/delete and WebP thumbnail behavior; Play Script saved-project guard, capacity authority, cancellation, revision recheck, progressive render, alpha merge, complete parent commit, progress/status, and deterministic held-pose Motion; existing external lifecycle effects with cleanup.

Excluded: native UAT execution or approval (36.14-12); regression artifact creation and execution (36.14-13 and 36.14-14); final timeline visual implementation and visual UAT (36.14-15 through 36.14-18); deferred group movement/multi-selection; migration or recovery for old project formats.
</scope_boundary>

<dependency_graph>
- Task 1 needs Plans 01-05 and 09-10 outputs; it seals all durable and parent-authoritative boundaries and creates the only state accepted by Task 2 consumers.
- Task 2 needs Task 1's closed contracts plus Plans 06-08 edit wiring and approved Script behavior; it removes remaining alternate callers and modules.
- Task 3 needs Tasks 1-2 complete; it performs the repository production audit, proves the no-test/package boundary from the pre-plan baseline, runs the only final validation commands, and stops before native UAT.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Seal every durable and parent-authoritative boundary to physical identity</name>
  <files>app/src/types/physicPaint.ts, app/src/types/project.ts, app/src/stores/physicPaintStore.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintPersistence.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/components/physic-paint/roto/rotoPhysicalOwnership.ts, app/src/components/physic-paint/roto/rotoCacheTransactions.ts, app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.ts, app/src/components/physic-paint/roto/rotoCanvasFrames.ts, app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts</files>
  <read_first>
- All executed `36.14-01-SUMMARY.md` through `36.14-10-SUMMARY.md`; extract actual physical type names, validator names, store ports, bridge payloads, ownership keys, revisions, equality functions, generated cache contracts, and deleted/retained files before changing a caller
- `36.14-CONTEXT.md` D-01 through D-04 and D-09 through D-12 for the clean format break, identity ownership, enabled-only interpolation, separate Motion, complete mapping, downstream parity, and UAT-first boundary
- `36.14-PATTERNS.md` production inventory, binary deletion sweep, hydration/persistence guidance, cache ownership, `physicPaintVersion`, Preact Signals, and explicit companion-file cleanup list
- `36.14-RESEARCH.md` closed schema, responsibility map, runtime validation, canonical equality/revision, acknowledgement, malformed-data, and cache-race guidance
- `app/src/types/physicPaint.ts` and `app/src/types/project.ts` for every post-Plan-10 durable/runtime Roto declaration and validator
- `app/src/stores/physicPaintStore.ts`, `app/src/lib/physicPaintPersistence.ts`, and `app/src/lib/physicPaintBridge.ts` for all reads, writes, hydration, launch, apply, equality, revision, dirty, selection, cache, and publication paths
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` and `rotoPhysicalOwnership.ts` for the canonical stable record, complete mapping, semantic cell, ownership, and revision contracts
- `app/src/components/physic-paint/roto/rotoCacheTransactions.ts`, `rotoLivePixelCacheTransactions.ts`, `rotoCanvasFrames.ts`, and `physicsPaintPerformanceTrace.ts` for remaining frame-keyed caches, payload aliases, and diagnostic terminology
  </read_first>
  <action>
Before any production edit, confirm the ten prerequisite SUMMARY files exist, confirm the working tree does not contain unrelated uncommitted production/test/package changes, and write the current `HEAD` SHA to `/tmp/efx-motion-editor-36.14-11-base`. Stop on a dirty-boundary conflict rather than absorbing another change into this plan.

Per D-01/D-03/D-09, close `physicPaint.ts` and `project.ts` around the executed physical contracts. A durable real-key record contains stable `keyId`, direct `appFrame`, and its approved non-timing metadata only. Interpolation settings contain `enabled` only. Script Motion deformation and position live in the separate D-04 contract. Physical proposals, apply payloads, acknowledgements, launch context, project output, cached real payloads, and generated render output use the Plan 01-09 identity/revision contracts without source/display/provenance aliases. Remove optional compatibility fields rather than leaving them accepted-but-unused. Runtime validators reject unknown keys, partial mappings, duplicate key IDs, duplicate physical positions, out-of-capacity positions, malformed adjacent ownership, and stale revisions atomically.

Per D-02/D-03, remove interpolation counts, segment and moved-key spacing overrides, nearest-key ownership, frame-pair timing aliases, persisted generated timing metadata, and all settings normalization that reconstructs them. Runtime generated semantic cells come only from the physical projector; generated pixel caches remain derived output keyed by current adjacent stable identities and revisions. No hydration, project reopen, bridge launch, cache merge, or equality branch may infer timing from rendered frames or use one coordinate as fallback for another.

Make `physicPaintStore.ts`, persistence, and bridge expose one canonical complete physical snapshot and one canonical equality/revision implementation. Persistence loads the complete Roto payload or fails the Roto payload closed; it does not skip malformed members and publish the remainder. The bridge validates the complete mapping and acknowledgement tuple before parent publication, uses the single accepted physical apply contract from Plan 04, and contains no Roto branch for delete, replace-keys, interpolation-settings update, or view-specific move. Preserve generic Basic/FX apply behavior where it is a separate non-Roto contract. `physicPaintVersion` increments once for each accepted visual publication and remains invalidation only; content identity and acknowledgement matching use the canonical physical revision.

Convert real paint, editable state, cache, live pixel encoding, dirty state, and selection ownership to `keyId`; resolve placement through the current accepted physical map at use time. Remove numeric-frame revision maps and source/display cache merges. Keep PNG alpha encoding, live-cache flush/cancellation, and monotonic visual invalidation behavior. Rename local performance and canvas payload terminology to physical `keyId`/`appFrame` language without changing profiling architecture or the external engine package signature described in assumptions.

Delete `rotoCacheTransactions.ts` if Plan 09 already moved every retained identity-owned cache operation to `rotoPhysicalOwnership.ts` or another focused physical module. If a non-timing cache operation remains, move it to the executed physical ownership module first and then delete the alias-based module; do not preserve the file as a forwarding wrapper. Apply the same no-wrapper rule to any compatibility-only helper discovered in the boundary graph.
  </action>
  <acceptance_criteria>
- Runtime, project, bridge, and launch Roto schemas expose stable `keyId`, direct `appFrame`, enabled-only interpolation, separate Script Motion, complete physical proposals/acknowledgements, and revision-bound derived render output only.
- Persisted/project/bridge input with an unknown field, missing mapping member, duplicate key ID, duplicate position, invalid position, malformed generated ownership, or stale revision is rejected as one Roto unit; no surviving subset reaches the store.
- Store publication, cache ownership, dirty state, selection, equality, and revisions use stable identity; moving a key changes placement without transferring or cloning another key's payload.
- Generated cells are derived from adjacent real keys at runtime and generated pixel output cannot create a cell, extend duration, own selection, or survive anchor/revision mismatch.
- No boundary reader or writer contains source/display fallback, interpolation count, segment-spacing override, moved-key spacing override, persisted generated timing record, migration, compatibility adapter, or dual write.
- `physicPaintVersion` is monotonic visual invalidation only and publishes once after an accepted complete state; semantic no-change, rejection, stale acknowledgement, timeout, cancellation, or rollback publishes no accepted visual state.
- PNG alpha encoding, cache cancellation, Basic/perfect-freehand, FX/p5.brush, blend modes, and non-Roto Physics Paint paths remain unchanged.
- No test, package, manifest, lockfile, or server artifact is touched or executed, and typecheck/build have not yet run.
  </acceptance_criteria>
  <verify>
    <automated>BASE=$(cat /tmp/efx-motion-editor-36.14-11-base) && test -n "$BASE" && test "$(git cat-file -t "$BASE")" = commit && ! rg -n --glob '!**/*.test.*' --glob '!**/*.spec.*' --glob '!**/__tests__/**' '(inBetweenCount|segmentSpacingOverrides|nearestRealKeyFrame|fromSourceFrame|toSourceFrame|RotoSourceDisplayModel|getRotoDisplayProjection|sourceFrame\s*\?\?|displayFrame\s*\?\?|\?\?\s*[^;\n]*appFrame)' app/src/types/physicPaint.ts app/src/types/project.ts app/src/stores/physicPaintStore.ts app/src/lib/physicPaintBridge.ts app/src/lib/physicPaintPersistence.ts app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts app/src/components/physic-paint/roto/rotoPhysicalOwnership.ts app/src/components/physic-paint/roto/rotoLivePixelCacheTransactions.ts app/src/components/physic-paint/roto/rotoCanvasFrames.ts app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts</automated>
  </verify>
  <done>Reopen, launch, persistence, bridge publication, store state, caches, dirty state, selection, and visual invalidation now accept one complete stable-identity/direct-placement model and cannot reconstruct an alternate Roto timeline.</done>
</task>

<task type="auto">
  <name>Task 2: Remove alternate edit, view, history, and Script transaction routes</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts, app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts, app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts, app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts, app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts, app/src/components/physic-paint/roto/physicsPaintRotoSession.ts, app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.ts, app/src/components/physic-paint/roto/rotoApplyTransactions.ts, app/src/components/physic-paint/roto/rotoKeyTransactions.ts, app/src/components/physic-paint/roto/rotoSaveTransactions.ts, app/src/components/physic-paint/roto/rotoSourceDisplayModel.ts</files>
  <read_first>
- Task 1 final closed schemas, validators, store ports, ownership, revision, and bridge apply/acknowledgement contract
- Executed `36.14-02-SUMMARY.md`, `36.14-04-SUMMARY.md`, and `36.14-05-SUMMARY.md` for the actual one-resolver, one-coordinator, complete settlement tuple, accepted-only history, Undo/Redo replay, pending state, timeout, cancellation, and rollback APIs
- Executed `36.14-06-SUMMARY.md`, `36.14-07-SUMMARY.md`, and `36.14-08-SUMMARY.md` for actual Insert/Delete/Drag/Force Spacing action ports, preview contracts, toolbar/shortcut wiring, and disconnected old routes
- Executed `36.14-09-SUMMARY.md` and `36.14-10-SUMMARY.md` for stable Script/cache ownership and downstream physical consumer ports
- `36.14-CONTEXT.md` D-01 through D-12, especially D-03 clean break, D-04 separate Motion, D-05 one resolver, D-06 one coordinator, D-07 generic history, D-08 shared UI intent routes, D-10 downstream parity, and D-12 UAT-first sequencing
- `36.14-PATTERNS.md` deletion sweep for Studio, session, key/save/apply/cache transactions, move lifecycle/history/persistence, frame editing, Script surfaces, workflow/view props, and Preact Signals boundaries
- Every production import of `rotoSourceDisplayModel`, `rotoKeyTransactions`, `rotoSaveTransactions`, `rotoApplyTransactions`, `useRotoApplyLifecycle`, `useRotoKeyMoveHistory`, and `useRotoPersistenceIntegration` before deleting those modules
- `physicsPaintRotoScriptClipboard.ts`, `useRotoScriptClipboardController.ts`, `physicsPaintRotoScriptLibrary.ts`, `physicsPaintRotoPlayScriptController.ts`, `useRotoPlayScriptController.ts`, and `physicsPaintRotoPlayScriptRenderer.ts` for immutable reuse, library lifecycle, physical target selection, Motion, cancellation, revision, complete batch, alpha merge, and progress behavior to preserve
- `PhysicsPaintStudio.tsx`, workflow strip/panel, timeline actions, frame editing controller, key utilities/controller, session, workflow, persistence coordinator, physical coordinator, and physical history for all remaining callers and stale local state
  </read_first>
  <action>
Per D-05/D-06/D-07/D-08, inventory every production entry point for Insert, Delete, Drag, Force Spacing, Undo, and Redo after Plans 06-08. Each intent must provide semantic input to the executed physical action port, which calls the single pure resolver, sends the complete proposal through the single acknowledged coordinator, and records exactly one command in the generic accepted-only history after exact acknowledgement. Undo and Redo replay complete before/after mappings through that same coordinator. Remove direct bridge writes, local map mutation, pairwise cache/editable remap, selected-frame claim transactions, moved-key timing restoration, move-specific snapshot/rollback, operation-specific pending refs, and accepted-history writes outside that route.

Make `PhysicsPaintStudio`, workflow strip, Scripts panel, timeline actions, frame-edit controller, key utilities/controller, persistence coordinator, session, and workflow expose physical semantics only: selected stable `keyId`, direct current/target `appFrame`, semantic real/generated/empty cell, pending edit state, and the shared action callbacks. Toolbar and Delete/Backspace retain the same semantic delete callback and global shortcuts retain `isPaintEditMode()` conflict guards. Drag preview and commit retain the same immutable resolver proposal. Force Spacing retains session-local raw input but persists no count or override. Clear, Duplicate, Paste, navigation, and reference behavior must resolve current ownership by stable identity and direct placement, not a source/display fallback. Keep refs/effects only for pointer, timer, bridge, engine, and disposal lifecycles with cleanup; remove mirrored timing state and synchronization effects.

Migrate Script Copy/Apply before deleting old transaction types. The reusable clipboard and library snapshot identify copied real keys by stable Script-local identity and approved paint payload, and each Apply resolves its current destination to a stable physical target plus direct `appFrame`. Preserve immutable repeated Apply, explicit library load-to-clipboard, stale launch/engine protection, accepted mutation draining, source publication flush, non-consumption of clipboard content, scan/save/load/rename/delete, selection, WebP thumbnails, and stale-operation protection. Empty-target preparation and replacement use the same closed physical proposal/parent authority as other accepted physical batches; no selected-frame claim or source/display transaction adapter remains.

Keep Play Script behavior but convert its selection, rendered batch, and final complete map to stable `keyId` plus direct `appFrame`. Preserve saved-project guard, physical capacity authority, immutable Script snapshot, deterministic held-pose deformation/position, progressive rendering, cancellation, revision recheck, existing-paint alpha merge, progress/status, and one complete parent commit. Populate any unavoidable external `destinationSourceFrame` engine argument from physical `appFrame` only at the package call; do not export that spelling into local contracts.

After migrating all callers, delete `rotoSourceDisplayModel.ts`, `rotoKeyTransactions.ts`, `useRotoApplyLifecycle.ts`, `useRotoKeyMoveHistory.ts`, and `useRotoPersistenceIntegration.ts`. Delete `rotoSaveTransactions.ts` and `rotoApplyTransactions.ts` if their retained non-timing guards have been moved into the physical coordinator/model; otherwise reduce neither file to a forwarding wrapper—move each retained generic guard to its rightful physical owner and delete the old file. Remove all imports, re-exports, types, callbacks, props, refs, comments, and dead branches associated with the deleted routes. Do not leave aliases for old names.

Perform a caller-by-caller reachability review from UI event to accepted parent state and from accepted state to every downstream consumer named by D-10. Record the matrix in the Plan 11 SUMMARY, not a new repository document. For each row, name the production entry file, physical port used, stable identity/direct frame input, generated-cell policy where relevant, and absence of an alternate route. Stop on any unresolved consumer instead of allowlisting it.
  </action>
  <acceptance_criteria>
- Insert, Delete, Drag, Force Spacing, Undo, and Redo each have one production path through the same resolver/coordinator/history authorities; rejected, stale, timed-out, cancelled, rolled-back, or semantic no-change operations produce no accepted history command.
- Toolbar, keyboard, pointer, frame-edit, session, and workflow callers express stable identity/direct frame intent and contain no local remap, coordinate conversion, count/override mutation, direct bridge commit, or operation-specific settlement.
- Deleted modules have no production import, re-export, type reference, callback, prop, ref, dead branch, or forwarding compatibility wrapper.
- Script Copy/Apply remains immutable and reusable across repeated targets and routine refreshes; library scan/save/load/rename/delete, WebP thumbnail, explicit load-to-clipboard, selection, and stale-operation behavior remain intact.
- Play Script retains deterministic held-pose Motion, saved-project/capacity guards, cancellation, revision recheck, progressive render, alpha merge, progress/status, and complete parent commit while using stable physical identity/direct placement.
- External package argument spelling is confined to the direct package call if required; no local schema, store, cache, Script, transaction, or timeline contract adopts it.
- Every D-10 production consumer reaches accepted physical records/runtime semantic cells and does not reconstruct source/display timing, interpolation spacing, generated existence, identity ownership, equality, revision, or timeline extent locally.
- Preact state remains in the executed Signals/coordinator/history architecture; no state/effect mirror or prop-drilled timing model is introduced.
- Basic perfect-freehand, FX p5.brush, blend/background behavior, and non-Roto Physics Paint remain untouched.
- No test, package, manifest, lockfile, server, typecheck, or build command is used in this task.
  </acceptance_criteria>
  <verify>
    <automated>test -f app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && test -f app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts && test -f app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts && test ! -e app/src/components/physic-paint/roto/rotoSourceDisplayModel.ts && test ! -e app/src/components/physic-paint/roto/rotoKeyTransactions.ts && test ! -e app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts && test ! -e app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts && test ! -e app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts && ! rg -n --glob '!**/*.test.*' --glob '!**/*.spec.*' --glob '!**/__tests__/**' '(RotoSourceDisplayModel|getRotoDisplayProjection|resolveRotoRealKeySaveTarget|RotoSaveRealKeyTransaction|RotoSelectedFrameClaim|selectedSourceFrame|selectedDisplayFrame|currentFrameSourceFrame|RotoKeyMove|recordAcceptedMove|registerMoveSettlement|replaceRotoKeyMoveLocal|commitRotoKeyMove|buildRotoKeyMoveTransaction|resolveRotoKeyMoveTiming|inBetweenCount|segmentSpacingOverrides|nearestRealKeyFrame|fromSourceFrame|toSourceFrame|\bsourceFrame\b|\bdisplayFrame\b)' app/src/components/physic-paint app/src/types/physicPaint.ts app/src/types/project.ts app/src/stores/physicPaintStore.ts app/src/lib/physicPaintBridge.ts app/src/lib/physicPaintPersistence.ts</automated>
  </verify>
  <done>All physical edit, view, session, history, persistence, and Script callers now enter the same stable-identity physical authority, and every legacy-only source/display or operation-specific module is absent rather than dormant.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the production graph is binary-clean, typecheck, build, and stop</name>
  <files>No repository files; verification and the executor-authored 36.14-11-SUMMARY.md only</files>
  <read_first>
- Tasks 1-2 final production graph and deleted-file list
- `/tmp/efx-motion-editor-36.14-11-base` for the immutable pre-plan commit
- Executed `36.14-01-SUMMARY.md` through `36.14-10-SUMMARY.md` for the complete expected artifact and consumer inventory
- `36.14-PATTERNS.md` final binary cleanup gate and companion-file list
- `36.14-VALIDATION.md` production-before-UAT order and authorized Plan 11 commands
- `36.14-CONTEXT.md` D-01 through D-12 and deferred ideas
- `36.14-PLAN-OUTLINE.md` Plan 12 native UAT handoff and Plans 13-18 ownership
  </read_first>
  <action>
Build the production audit set from the explicit Plan 01-10 changed-file inventories, the files in this plan, and every current production importer of the physical model, resolver, coordinator, history, ownership, Script, persistence, bridge, and store ports. Exclude paths under tests, `__tests__`, fixtures, snapshots, and files with test/spec basenames from the symbol scan; do not edit, rename, delete, or execute those excluded files.

Run a binary negative audit over that production set. Zero occurrences are required for `inBetweenCount`, `segmentSpacingOverrides`, nearest-real-key timing ownership, source/display frame-pair fields and readers, `RotoSourceDisplayModel`, display projection helpers, coordinate nullish fallbacks, selected/current source-display props, persisted generated timing records, migration/compatibility adapters, moved-key spacing restoration, move-only settlement/history symbols, pairwise remap helpers, operation-specific Roto apply kinds, and imports/re-exports of deleted modules. A comment, type alias, unused branch, compatibility wrapper, dead export, or diagnostic field counts as a failure. The direct external engine call described in assumptions is the sole possible spelling allowlist and must map physical `appFrame` into the package API without exposing local authority.

Run a positive reachability audit. Confirm exactly one production physical resolver module, one acknowledged coordinator module, and one generic physical history module exist; all six physical operations reach them through the executed action ports; all complete parent commits leave through the coordinator; history records accepted operations only and replays through the coordinator; persistence/bridge/store use the canonical complete physical validators/equality/revision; generated cells come only from the projector; and every D-10 consumer uses the accepted physical projection or identity-owned render port. Confirm Script Motion is separate and Script Copy/Apply/library/Play flows use physical identity while retaining approved behavior. Confirm Basic/perfect-freehand and FX/p5.brush imports and branches remain present and unchanged by this plan.

Use the recorded base SHA to inspect all paths changed by Plan 11, including committed, staged, and unstaged state. Fail if any path is a test/spec, test directory, fixture, snapshot, package manifest, lockfile, generated dependency artifact, or Basic/FX implementation file outside compiler-required type adaptation. Fail if the baseline is missing or if unrelated pre-existing changes make attribution ambiguous. Record the exact changed production files, deleted files, external package-boundary allowlist if any, consumer reachability matrix, and no-test/package result in `36.14-11-SUMMARY.md`.

Only after all static audits pass, run the two authorized final commands in order: `pnpm --dir app typecheck`, then `pnpm --dir app build`. Do not invoke any test script, Vitest, package install/audit, Vite/Tauri/preview/application server, browser, or watch process before, between, or after them. Fix only production type/build errors caused by the declared physical cleanup; then rerun the same two commands in order. If correction requires a test/config/package/server change or reveals an earlier-plan dependency gap, stop and report it.

After both commands pass, stop. Do not perform native interaction, visual verification, regression work, final timeline UI work, or phase closure. The summary must say production gates passed and Plan 12 is ready for user-owned native UAT; it must not say native approved, UAT passed, phase complete, or feature complete.
  </action>
  <acceptance_criteria>
- The production audit set covers every Plan 01-11 physical authority artifact and every current production importer; test/spec/fixture/snapshot paths are excluded from scanning and unchanged from the baseline.
- Every forbidden field, reader, alias, count, override, generated timing record, fallback, migration/compatibility path, dual write, move-specific path, stale view semantic, deleted-module import, and dead wrapper has zero production occurrences, except the documented direct external engine argument allowlist if required.
- Exactly one resolver, one coordinator, and one generic accepted-only history authority remain, and the consumer reachability matrix proves all edit and downstream paths connect to them or to accepted physical projection/ownership ports.
- Script Motion remains separate; approved Script Copy/Apply reuse, durable library, and Play Script behavior remain present under physical targeting.
- Basic/perfect-freehand and FX/p5.brush behavior, imports, blend modes, backgrounds, and non-Roto Physics Paint are not removed or refactored.
- The baseline diff contains no test/spec/fixture/snapshot path, package manifest, lockfile, dependency artifact, or unauthorized Basic/FX implementation change.
- `pnpm --dir app typecheck` passes, followed by `pnpm --dir app build` passing; no other final validation command runs.
- Execution stops after production gates and reports readiness for Plan 12 native UAT without claiming native approval or phase completion.
  </acceptance_criteria>
  <verify>
    <automated>BASE=$(cat /tmp/efx-motion-editor-36.14-11-base) && test "$(git cat-file -t "$BASE")" = commit && CHANGED=$(git diff --name-only "$BASE" --) && ! printf '%s\n' "$CHANGED" | rg '(^|/)(__tests__|tests?|fixtures?|snapshots?)(/|$)|\.(test|spec)\.[^/]+$|(^|/)(package\.json|pnpm-lock\.yaml|package-lock\.json|yarn\.lock)$' && ! rg -n --glob '!**/*.test.*' --glob '!**/*.spec.*' --glob '!**/__tests__/**' '(inBetweenCount|segmentSpacingOverrides|nearestRealKeyFrame|fromSourceFrame|toSourceFrame|RotoSourceDisplayModel|getRotoDisplayProjection|resolveRotoRealKeySaveTarget|RotoSaveRealKeyTransaction|RotoSelectedFrameClaim|selectedSourceFrame|selectedDisplayFrame|currentFrameSourceFrame|RotoKeyMove|recordAcceptedMove|registerMoveSettlement|replaceRotoKeyMoveLocal|commitRotoKeyMove|buildRotoKeyMoveTransaction|resolveRotoKeyMoveTiming|sourceFrame\s*\?\?|displayFrame\s*\?\?)' app/src/components/physic-paint app/src/types/physicPaint.ts app/src/types/project.ts app/src/stores/physicPaintStore.ts app/src/lib/physicPaintBridge.ts app/src/lib/physicPaintPersistence.ts app/src/lib/previewRenderer.ts app/src/lib/exportRenderer.ts app/src/lib/rotoFrameDraw.ts app/src/lib/frameMap.ts</automated>
    <automated>pnpm --dir app typecheck
pnpm --dir app build</automated>
  </verify>
  <done>The production Roto graph contains one physical authority with no timing or operation-specific bypass, the no-test/package boundary is proven, typecheck and build pass, and execution stops ready for user-owned native UAT in Plan 12.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Artifact | Required interface or behavior |
|---|---|---|
| `app/src/types/physicPaint.ts` | Closed physical runtime/bridge schema | Stable `keyId`, direct `appFrame`, enabled-only interpolation, separate Script Motion, complete proposal/acknowledgement validation, and no timing aliases or compatibility fields. |
| `app/src/types/project.ts` | Closed physical project schema | Persists real-key identity/direct placement and approved payload metadata only; generated timing authority and partial fallback hydration are impossible. |
| `app/src/stores/physicPaintStore.ts` | Canonical physical state authority | Atomically publishes complete physical records, identity-owned payload/cache/dirty/selection state, canonical equality/revision, runtime generated cells, and monotonic visual invalidation. |
| `app/src/lib/physicPaintPersistence.ts` | Fail-closed physical persistence | Loads or rejects the complete Roto payload and saves only the closed physical format with no recovery or compatibility branch. |
| `app/src/lib/physicPaintBridge.ts` | Parent-authoritative physical transport | Validates complete proposals and exact acknowledgements, publishes one accepted mapping, preserves non-Roto contracts, and exposes no operation-specific Roto apply branch. |
| `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` | Sole pure physical resolver | Resolves Insert, Delete, Drag, and Force Spacing as complete deterministic proposals; Undo/Redo replay their complete mappings through the same authority. |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` | Sole acknowledged edit coordinator | Owns barriers, revalidation, complete stage, parent publication, exact acknowledgement, timeout, rollback, cancellation, and pending state. |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` | Sole accepted physical history | Records one command per accepted operation and replays Undo/Redo through the coordinator without move-only state. |
| `app/src/components/physic-paint/PhysicsPaintStudio.tsx` and physical action/controller files | Physical-only UI composition | Routes toolbar, shortcut, pointer, Clear/Duplicate/Paste, Force Spacing, Undo/Redo, navigation, and reference semantics through stable identity/direct placement ports. |
| Script clipboard/controller/library/renderer files | Physical-target Script workflows | Preserve immutable repeated Apply, durable library, explicit load-to-clipboard, deterministic Motion, progressive Play, alpha merge, and complete parent commit without source/display adapters. |
| Cache/live-pixel/canvas/performance files | Identity-owned derived output | Key real payloads by stable identity, generated output by current anchors/revisions, and diagnostics by physical terminology while preserving alpha encoding and lifecycle behavior. |
| Deleted legacy modules | Binary removal | Source/display projection, selected-frame transactions, move settlement/history/persistence, alias cache projection, and operation-specific wrappers are absent with no forwarding exports. |
| `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md` | Production gate evidence | Records changed/deleted files, external API allowlist if any, consumer reachability, no-test/package proof, typecheck/build results, and Plan 12 readiness only. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Project file / launch payload -> runtime validator | Persisted content is untrusted and must become one complete valid physical state or no Roto state. |
| Studio/action intent -> resolver | UI, pointer, keyboard, Script, Undo, and Redo inputs can contain stale identities, frames, selections, or raw Force Spacing values. |
| Resolver proposal -> acknowledged coordinator -> parent bridge | A complete optimistic mapping crosses an asynchronous parent boundary and must settle only against the exact operation, launch, layer, revision, and mapping. |
| Accepted physical state -> cache/selection/history/downstream consumers | Consumers must not recover timing or payload ownership from frame collisions, aliases, generated metadata, or stale revisions. |
| Script snapshot / library data -> physical target and engine renderer | Reusable Script content and Motion cross durable and external-engine boundaries without becoming timeline timing authority. |
| Production source -> final build | Dead compatibility code, unreviewed dependency changes, or test-surface edits can bypass the intended pre-UAT production gate. |

## Abuse Cases — OWASP ASVS L1

| Abuse case | Expected disposition |
|---|---|
| A project or bridge payload omits one key, duplicates a key/frame, adds a removed field, or supplies a partial mapping. | Reject the complete Roto payload/proposal before any store, cache, history, selection, or visual publication. |
| A stale acknowledgement has a plausible frame map but belongs to another operation, launch, layer, or base revision. | Ignore it, keep exact pending ownership, and follow timeout/cancellation/rollback without accepted history. |
| A frame-number collision attempts to transfer paint, dirty state, cache, selection, or Script target ownership to another key. | Resolve ownership by stable `keyId`; placement is read from the accepted map and never owns payload. |
| Generated metadata claims a leading, trailing, non-adjacent, or stale cell. | Discard it; cell existence comes only from the current enabled runtime projection and pixel output requires matching adjacent identities/revisions. |
| A Script library entry or external renderer field reintroduces source/display coordinates into local state. | Validate/translate at the narrow boundary into stable identity/direct placement; reject local persistence or propagation of the alias. |
| An alternate view, session, or history path writes directly to store/bridge. | Delete the route; all physical mutation and replay pass through resolver/coordinator/history. |
| Cleanup modifies tests, packages, Basic paint, or FX paint before native UAT. | Fail the baseline diff gate and stop before typecheck/build handoff. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-11-01 | Spoofing | Real-key and Script target ownership | high | mitigate | Require stable `keyId` in physical records and ownership-bearing payloads; resolve current `appFrame` from the accepted complete map and reject unknown/deleted identities. |
| T-36.14-11-02 | Tampering | Project/bridge hydration | high | mitigate | Use closed runtime validators and atomic complete-payload acceptance; removed fields, partial maps, duplicates, invalid positions, and malformed ownership reject the Roto unit. |
| T-36.14-11-03 | Tampering | Physical edit acknowledgement | high | mitigate | Match the exact Plan 04 operation/launch/layer/revision/mapping tuple before acceptance; timeout, cancellation, mismatch, and rejection roll back without history or accepted publication. |
| T-36.14-11-04 | Repudiation | Physical history | medium | mitigate | Record exactly one generic command only after acceptance, retain complete before/after mappings, and replay Undo/Redo through the same coordinator. |
| T-36.14-11-05 | Information Disclosure | Cache and missing-frame paths | low | mitigate | Reject unknown/stale identity payloads and invalid generated caches; never substitute neighboring key paint when ownership validation fails. |
| T-36.14-11-06 | Denial of Service | Physical projection and Script batch | medium | mitigate | Preserve bounded capacity, complete validation, cancellation, revision checks, and existing progressive rendering; do not add unbounded compatibility scans or replay loops. |
| T-36.14-11-07 | Elevation of Privilege | View/session/direct bridge routes | high | mitigate | Delete operation-specific and view-owned mutation paths so only the acknowledged coordinator can publish physical edits. |
| T-36.14-11-08 | Tampering | Generated timing and timeline extent | high | mitigate | Derive exact strict interiors at runtime from accepted adjacent real keys; generated records cannot persist, own selection, or extend duration. |
| T-36.14-11-09 | Tampering | Preact reactive state | medium | mitigate | Keep physical truth in existing Signals/coordinator/history; prohibit mirrored hook state, effect-driven copying, and ref-owned business state. |
| T-36.14-11-10 | Tampering | Production cleanup boundary | medium | mitigate | Compare all Plan 11 changes to the recorded base SHA, reject test/package/lockfile and unauthorized Basic/FX paths, then run typecheck and build only. |
| T-36.14-11-SC | Tampering | pnpm package supply chain | low | accept | No package installation, removal, upgrade, audit, manifest change, or lockfile change occurs; pnpm only invokes existing app typecheck and build scripts. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One deterministic physical-frame authority across edit, persistence, reopen, playback, preview, and export | Tasks 1-3 close boundaries, remove alternate routes, audit every consumer, and gate the production build | COVERED |
| GOAL | Production implementation and binary cleanup precede native UAT | Task 3 stops after typecheck/build and hands off to Plan 12 | COVERED |
| REQ | `36.14-PHYSICAL-IDENTITY` | Task 1 closes stable `keyId`/direct `appFrame` schemas and ownership; Task 2 migrates every caller | COVERED |
| REQ | `36.14-DERIVED-INTERPOLATION` | Task 1 removes counts/overrides/durable timing; Tasks 2-3 verify runtime strict interiors only | COVERED |
| REQ | `36.14-ATOMIC-FRAME-MAPPING` | Task 1 enforces complete validation/publication; Task 2 routes all operations and history through coordinator | COVERED |
| REQ | `36.14-DOWNSTREAM-PARITY` | Task 2 migrates Scripts and all edit/view callers; Task 3 audits every D-10 consumer | COVERED |
| REQ | `36.14-UAT-THEN-REGRESSION` | No regression artifact or execution; final gate is typecheck/build followed by a hard stop before Plan 12 | COVERED |
| RESEARCH | Closed validators and fail-closed hydration | Task 1 | COVERED |
| RESEARCH | One resolver, one acknowledged coordinator, one accepted-only history | Tasks 2-3 | COVERED |
| RESEARCH | Stable identity owns payload while physical frame owns placement | Tasks 1-2 | COVERED |
| RESEARCH | Generated cells are runtime strict interiors and generated pixels are revision-bound derived output | Tasks 1-3 | COVERED |
| RESEARCH | Canonical equality/revision and monotonic visual invalidation | Task 1 | COVERED |
| RESEARCH | Script Motion remains separate from timing | Tasks 1-3 | COVERED |
| RESEARCH | Preact Signals/direct derivation; effects only for external lifecycle | Tasks 1-2 and prohibitions | COVERED |
| RESEARCH | Basic perfect-freehand and FX p5.brush remain intact | All tasks preserve and Task 3 audits | COVERED |
| CONTEXT | D-01 stable identity and direct physical placement | Tasks 1-3 | COVERED |
| CONTEXT | D-02 enabled-only interpolation and exact strict interiors | Tasks 1-3 | COVERED |
| CONTEXT | D-03 no source/display/count/override/generated timing/migration/compatibility | Tasks 1-3 binary cleanup | COVERED |
| CONTEXT | D-04 separate Script Motion | Tasks 1-3 | COVERED |
| CONTEXT | D-05 one pure resolver | Tasks 2-3 | COVERED |
| CONTEXT | D-06 one acknowledged coordinator | Tasks 1-3 | COVERED |
| CONTEXT | D-07 one accepted-only generic history | Tasks 2-3 | COVERED |
| CONTEXT | D-08 toolbar/shortcut/drag/Force Spacing share semantic action routes | Task 2 | COVERED |
| CONTEXT | D-09 complete persistence/cache/selection/ownership mapping | Task 1 | COVERED |
| CONTEXT | D-10 downstream consumers use physical authority | Tasks 2-3 reachability audit | COVERED |
| CONTEXT | D-11 bounded sequential plan chain | Wave 11 depends only on completed Plan 10 and creates no parallel ownership conflict | COVERED |
| CONTEXT | D-12 production, native UAT, then regression/UI | Task 3 hard stop and output wording | COVERED |
| CONTEXT | Deferred old-project migration/compatibility | Explicitly removed, not implemented | EXCLUDED — DEFERRED |
| CONTEXT | Deferred group movement/multi-selection | No group operation or selection model added | EXCLUDED — DEFERRED |
| UI-SPEC | Final UI exposes physical behavior without source/display/count/override concepts | Task 2 removes stale view props and semantics; visual implementation remains Plans 15-18 | COVERED FOR PRE-UI PRODUCTION CONTRACT |
| VALIDATION | No regression work before explicit native approval | Plan-wide prohibitions and baseline diff gate | COVERED |
| VALIDATION | Production gate is app typecheck then app build | Task 3 final commands | COVERED |

## Production Consumer Reachability Audit

The executor must verify and record each row in `36.14-11-SUMMARY.md`; no row may be marked through an adapter, alias, fallback, or duplicated local derivation.

| Consumer group | Required authority path |
|---|---|
| Insert / Delete | Semantic action -> pure physical resolver -> acknowledged coordinator -> accepted history |
| Drag preview / commit | One immutable resolver proposal -> preview -> same proposal through coordinator -> accepted history |
| Force Spacing | Raw session input -> resolver complete mapping -> coordinator -> accepted history; no persisted count/override |
| Undo / Redo | Generic history complete mapping -> same coordinator -> exact acknowledgement |
| Project save / reopen / launch | Closed project/bridge validator -> complete physical records -> atomic store/ownership publication |
| Paint/editable/cache/dirty/selection | Stable `keyId` ownership -> current accepted `appFrame`; no frame collision ownership |
| Generated output | Enabled plus adjacent real keys -> runtime strict interior -> adjacent identity/revision-bound render cache |
| Playback / onion / reference | Accepted physical projection and exact identity-owned render lookup |
| Preview / export / missing background | Shared Plan 10 physical renderer and semantic cell; no local timing recovery |
| Timeline extent | Highest accepted real-key direct `appFrame` plus one; no generated-cache authority |
| Script Copy / Apply | Immutable reusable Script snapshot -> stable physical target -> canonical complete parent authority |
| Durable Script library | Closed physical Script metadata -> explicit immutable clipboard load; no coordinate alias |
| Play Script | Stable physical selection and capacity -> separate Motion -> progressive render -> complete physical batch through parent authority |
| Visual invalidation | Accepted visual publication -> one monotonic `physicPaintVersion`; never identity/equality/revision |

## Edge-Case Coverage

| Edge | Required deterministic result |
|---|---|
| Empty project or no real keys | Valid empty physical state; no generated cells, selected owner, cache-derived timing, or Roto extent. |
| One real key | Stable identity at one direct frame; no generated cells regardless of interpolation enabled state. |
| Adjacent real keys | No strict interior generated cell. |
| Wide gap with interpolation enabled | Every and only strict interior integer is generated at runtime; no stored count chooses a subset. |
| Interpolation disabled | Gap cells are empty; key placement and Motion remain unchanged. |
| Duplicate key ID, duplicate appFrame, out-of-capacity frame, partial mapping | Reject the complete payload/proposal; publish no partial state, history, or visual invalidation. |
| Move to the same complete mapping | Semantic no-change; no parent mutation, accepted history, or invalidation. |
| Stale/unknown/deleted key in UI or Script target | Reject or clear selection according to the established physical contract; never retarget by frame collision. |
| Acknowledgement mismatch, timeout, cancellation, launch replacement | Exact pending operation does not accept; complete rollback/cancellation path leaves no accepted history. |
| Undo/Redo after another accepted edit | Revalidate complete replay through the coordinator; reject stale replay rather than patching a pair. |
| Generated cache with stale anchors/revisions | Ignore as unavailable derived output; it cannot establish a cell or borrow neighboring paint. |
| Async live-pixel completion after move/replacement | Stable identity/revision ownership prevents stale pixels from publishing as current. |
| Reopen malformed Roto data | Fail the Roto payload closed; no source/display or partial-frame recovery. |
| Script applied repeatedly | Clipboard remains immutable and reusable; each target resolves independently to current physical identity. |
| Play Script cancellation or revision change | Preserve existing cancellation/recheck behavior and avoid partial parent publication. |
| External engine field uses published source-frame spelling | Allow only at direct package call with value derived from target physical `appFrame`; no local propagation. |
| Basic or FX paint present beside Roto | Existing perfect-freehand/p5.brush paths, blend modes, and backgrounds remain unchanged. |
| Test or package file appears in Plan 11 diff | Fail before final typecheck/build and report boundary violation. |

<verification>
1. Confirm all ten prerequisite SUMMARY files exist and the recorded `/tmp/efx-motion-editor-36.14-11-base` identifies the exact pre-plan commit.
2. Complete the production-only negative scan across the explicit Plan 01-11 artifacts and all current importers. Zero forbidden timing, compatibility, fallback, generated-authority, operation-specific, and deleted-module symbols are allowed except the documented direct external package call.
3. Complete the positive reachability matrix for all physical edits, persistence/reopen, ownership/cache/dirty/selection, downstream rendering, timeline extent, Script workflows, visual invalidation, and Basic/FX preservation.
4. Compare the full Plan 11 diff with the recorded baseline. Reject test/spec/fixture/snapshot, package manifest, lockfile, dependency, and unauthorized Basic/FX paths. Do not inspect this as permission to alter them.
5. Run only `pnpm --dir app typecheck` as the first final command.
6. Run only `pnpm --dir app build` as the second and last final command.
7. Do not run Vitest, any test script, package install/audit, server, preview, Vite, Tauri, browser, watch process, native UAT, or visual UAT.
8. Stop after the build and create the normal Plan 11 SUMMARY with production-gate evidence and Plan 12 readiness wording only.
</verification>

<success_criteria>
- Production Roto data has one closed stable-identity/direct-placement schema across runtime, project, bridge, store, persistence, cache, Script, and downstream consumers.
- Interpolation persistence is enabled-only; exact strict interiors are runtime-derived and generated output never becomes durable timing or ownership authority.
- Insert, Delete, Drag, Force Spacing, Undo, and Redo use one resolver, one coordinator, and one accepted-only history route with complete mappings and exact acknowledgement.
- Source/display fields and readers, counts, spacing overrides, generated timing records, aliases, migration/compatibility, hydration fallbacks, dual writes, operation-specific settlement/history, pairwise remaps, and stale view semantics have zero production reachability and no forwarding wrappers.
- Script Motion remains separate, and approved Script Copy/Apply reuse, durable library, and Play Script behavior operate on physical targets without timing aliases.
- Persistence/reopen, caches, dirty state, selection, playback, onion/reference, preview/export, missing/background rendering, and timeline extent all consume accepted physical authority.
- Preact uses existing Signals/computed/direct derivation and focused external lifecycle effects; no mirrored timing state or effect chain exists.
- Basic perfect-freehand, FX p5.brush, blend modes, paint backgrounds, and non-Roto Physics Paint behavior are preserved.
- No regression artifact or package boundary changes, no test execution, and no server process occur.
- App typecheck and app build pass in that order, after which execution stops ready for user-owned native UAT in Plan 12 without claiming native approval or phase completion.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md` when execution is complete. Include the production changed/deleted-file inventory, consumer reachability matrix, external engine call allowlist if present, no-test/package diff proof, and exact typecheck/build results. Report only that production gates passed and Plan 12 is ready for user-owned native UAT; do not report native approval, UAT success, phase completion, regression completion, or final UI completion.
</output>
