---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 20
type: execute
wave: 11
depends_on:
  - 36.14-10
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/lib/physicPaintBridge.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
prohibitions:
  - "Do not modify a sixth production owner; the five-file boundary is final and any additional required owner is a hard stop."
  - "Do not abuse Undo/Redo replay, masquerade under an existing operation kind, forward a generic transaction, bypass parent bridge validation, create a second coordinator, or add operation-specific settlement/history/rollback authority."
  - "Do not create compatibility aliases, migration, shims, hidden registries, dual writes, fallback coordinates, or durable generated timing truth."
  - "Do not create, modify, delete, rename, discover, or execute tests; do not run Vitest or build."
  - "Do not change packages, manifests, lockfiles, generated dependencies, configuration, dormant modules, approval artifacts, Basic paint, FX p5.brush, non-Roto Physics Paint, Script behavior, or deterministic Motion."
must_haves:
  truths:
    - "Per D-01 and D-09, Duplicate and Paste are explicit ordinary physical-edit operation kinds carried consistently through resolver proposal, coordinator request, parent validation, discriminated exact acknowledgement, accepted output, and accepted-only history."
    - "Duplicate and Paste-to-empty add exactly one fresh stable key identity at one validated direct appFrame and carry an immutable copy of the source-key or clipboard paint payload inside the ordinary complete proposal."
    - "Paste-to-existing preserves the destination keyId and appFrame while replacing only its payload from the immutable reusable clipboard snapshot."
    - "The resolver returns one closed, complete, validated next-record proposal for identity/payload-changing operations; malformed, duplicate, unknown, stale, partial, or over-capacity work produces no stageable proposal."
    - "Before store mutation or ordinary-ledger insertion, the parent bridge independently proves the operation-specific semantic delta for Duplicate, Paste-to-empty, or Paste-to-existing while preserving all structural, revision, capacity, selection, ordinary-ledger, and replay-ledger checks."
    - "The parent bridge returns authoritative operation, launch, layer, project, expected/staged/accepted revision, selection, success/failure, and replay-provenance fields in the discriminated physical result; the coordinator matches and consumes those received fields without reconstructing them or falling back to pending local values."
    - "The existing coordinator remains the only pending, snapshot, optimistic publication, bridge send, exact acknowledgement, rollback, settlement, and accepted-output authority, and existing history records Duplicate/Paste only after that exact parent acceptance."
    - "Existing Insert, Delete, Move, Force Spacing, Undo, and Redo behavior and Preact Signal/direct-derivation ownership remain unchanged."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Closed Duplicate/Paste operation literals in request, result, and active bridge validators."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "Semantic Duplicate/Paste intents and immutable complete next-record proposals using existing physical validators and identity allocation."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Ordinary complete-record staging and exact acknowledgement through the existing serialized coordinator lifecycle."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      provides: "Accepted-only ordinary history classification for Duplicate/Paste with existing complete-snapshot replay."
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Parent-authoritative operation-specific semantic-delta validation and discriminated exact physical-edit acknowledgement before mutation and ledger insertion."
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      via: "one immutable proposal carrying exact operation kind, complete mapping, selection, and complete next records when identity or payload changes"
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "shared pure operation-specific semantic-delta validator invoked independently by the parent against authoritative current and proposed records"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/types/physicPaint.ts"
      via: "discriminated replace-roto-physical-map result echoing the authoritative operation tuple, revisions, selection, outcome, and replay provenance only when applicable"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      via: "received exact parent acknowledgement produces matching accepted output with immutable before/after snapshots and the ordinary Duplicate/Paste kind"
---

<objective>
Extend the existing closed physical-edit foundation so Duplicate and Paste are first-class ordinary acknowledged operations before the five-owner Studio/key-utility composition cutover.

Purpose: Plans 01-10 established stable physical records, one resolver, one acknowledged coordinator, accepted-only history, and a parent replacement bridge, but Duplicate and Paste still lack protocol-level operation kinds, complete identity/payload proposals, operation-specific parent semantic-delta validation, and a parent-echoed exact result. Per D-01, D-03, D-09, D-10, D-11, and D-12, this plan adds those semantics inside exactly the five approved owners so Plan 19 can compose the visible utilities without replay abuse, a generic transaction adapter, child-side acknowledgement reconstruction, or a sixth production owner.

Output: Public semantic Duplicate/Paste intent and proposal seams, shared pure semantic-delta validation invoked by the parent, discriminated authoritative request/result validation, ordinary complete-record staging through the existing coordinator, accepted-only history classification, and a bounded typecheck-only handoff to Plan 19.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-02-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-04-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md

# Exact approved production boundary
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/lib/physicPaintBridge.ts

# Read-only existing public dependencies; do not modify
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.ts
@app/src/stores/physicPaintStore.ts
</context>

<user_story>
**As a** stop-motion animator, **I want to** Duplicate and Paste Roto paint through the same acknowledged physical authority as every other timeline edit, **so that** key identity, payload ownership, rollback, Undo/Redo, persistence, and rendered output cannot disagree.
</user_story>

<scope_constraints>
- Modify exactly the five production files in frontmatter. `physicsPaintRotoPhysicalModel.ts`, `rotoCoordinatorPorts.ts`, `usePhysicsPaintApplyResultController.ts`, store, Plan 19 owners, dormant modules, and tests are read-only in this plan. The result controller must only pass the discriminated physical result through unchanged to the coordinator; it must not become a matching or reconstruction authority. If correctness requires a sixth production owner, stop and report the exact missing public seam rather than widening the plan.
- Reuse `createPhysicPaintRotoKeyId`, `isPhysicPaintRotoRealKeyPayload`, `isPhysicPaintRotoRealKeyRecord`, `parsePhysicPaintRotoRealKeyRecordCollection`, and the existing revision/store validators. Do not recreate UUID, payload, record, collection, capacity, selection, revision, ledger, or replay validation.
- Keep the core resolver deterministic for a supplied intent. If operation-specific intent factories allocate a new identity, allocate exactly once through `createPhysicPaintRotoKeyId`, freeze the resulting intent, and pass that same identity through proposal and commit; validation, parsing, hydration, and replay never allocate or repair IDs.
- Keep Duplicate/Paste on `replace-roto-physical-map`. They are ordinary kinds with no replay provenance and must use the existing coordinator pending/snapshot/send/match/restore lifecycle and parent ordinary-command ledger.
- Keep current mapping-only behavior for Insert/Delete/Move/Force Spacing and complete-snapshot behavior for Undo/Redo. Do not route Duplicate/Paste through `replayRecords` or synthesize an old operation kind.
- Preserve existing Signals/computed outputs and lifecycle-only effects. Add no `useState`, copied pending state, effect-driven mirror, second accepted-output channel, or duplicated history stack.
- Run only the automated commands named below. Do not run build, Vitest, server, Vite, Tauri, browser, native application, watcher, install, or audit commands.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Carry Duplicate through parent-validated proposal, exact acknowledgement, and accepted history</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - `app/src/types/physicPaint.ts` — extend the one shared operation union and make the active `replace-roto-physical-map` result a discriminated exact physical acknowledgement while generic results for other apply kinds remain unchanged.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` — preserve `resolvePhysicPaintRotoPhysicalEdit` as the sole resolution seam and expose one shared pure operation-specific semantic-delta validator for resolver/coordinator/parent use.
    - `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` — preserve `executePhysicalEdit`, complete snapshot/restore, one pending tuple, one bridge send, and one accepted output; remove child reconstruction of parent-authoritative result fields.
    - `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` — extend `isOrdinaryOperationKind`; do not change the existing accepted-output subscription or replay stack movement.
    - `app/src/lib/physicPaintBridge.ts` — preserve all existing parent structural, revision, capacity, selection, ordinary-ledger, and replay-ledger checks while adding Duplicate semantic validation and an authoritative discriminated result.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts`, `app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.ts`, `app/src/stores/physicPaintStore.ts`, and `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` — read-only dependencies; reuse validators and pass the new result through existing seams without changing their owners.
  </read_first>
  <action>
Per D-01/D-03/D-09, add the literal ordinary kind `duplicate-key` to the shared apply operation union and every closed physical payload/result validator in `physicPaint.ts`. Define the active `replace-roto-physical-map` result as a discriminated exact acknowledgement carrying operation ID and kind, launch operation ID, layer ID, project context ID when present, start frame, expected revision, staged revision, actual accepted revision or null, selected stable identity/direct frame where applicable, success/failure fields, and replay provenance only for Undo/Redo. Preserve the generic result shape for unrelated apply kinds.

In the physical resolver, add a semantic `duplicate-key` intent that names the source stable `keyId`, one direct destination `appFrame`, and one fresh `newKeyId` allocated exactly once by an exported Duplicate intent factory through `createPhysicPaintRotoKeyId`. Extend the resolver input/proposal only as needed to carry current canonical records and one immutable complete `nextRecords` result. Validate the source and destination, clone and retarget the source payload, and derive mapping, ordering, cells, selection, changes/status, and complete records from the same candidate. Export one pure semantic-delta validator that compares authoritative current records with proposed records and proves Duplicate adds exactly one fresh declared identity containing the declared source payload retargeted to the declared destination while every other identity, frame, and payload is semantically unchanged.

In the coordinator, use the same pure validator as defense in depth before staging, then preserve the current barrier/revision recheck, optimistic whole-record publication, one pending tuple, one bridge send, rollback, timeout, and accepted-output lifecycle. Consume the discriminated bridge result fields exactly as received. Match operation kind, operation ID, launch, layer, project context, expected revision, staged revision, accepted revision, selection, outcome, and absence of replay provenance before local acceptance; do not reconstruct those values from pending state and do not substitute the staged revision when the parent omits or disagrees on accepted revision.

In the parent bridge, invoke Duplicate's pure semantic-delta validator against authoritative current records and the proposed complete records after existing envelope/record/revision/capacity/selection checks but before store mutation and before ordinary accepted-command ledger insertion. Reject a generic structurally valid replacement that does not satisfy the declared Duplicate delta. On success, perform the existing authoritative mutation, read the actual accepted revision and accepted selection, insert the ordinary ledger entry only after validation/mutation succeeds, and return the exact discriminated acknowledgement populated from parent-authoritative values. Preserve every existing replay-ledger and ordinary-ledger rule.

In history, classify `duplicate-key` as ordinary. Only the coordinator's exactly matched parent acceptance records one complete before/after command; rejection, mismatch, and no-change record nothing, while Undo/Redo continue through the existing replay provenance and ledger path. Keep current Signals, direct derivation, and accepted-output ownership unchanged.
  </action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <done>One Duplicate adds exactly one fresh identity with source-owned paint at the validated destination; the parent independently proves that semantic delta before mutation and ledger insertion; the parent returns the full authoritative discriminated acknowledgement; the coordinator accepts only that exact received result; one accepted ordinary history command is produced; and the app typechecks without a sixth owner, test, or build artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Add both Paste deltas to the same parent-authoritative ordinary path</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - Task 1 final `duplicate-key` union, discriminated acknowledgement, shared pure semantic-delta validator, parent validation order, coordinator exact matching, and ordinary-history classification.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` — reuse strict immutable payload/record/collection validation and the one ID allocator without modifying this owner.
    - `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts`, `app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.ts`, and `app/src/stores/physicPaintStore.ts` — preserve existing public seams and ownership; no modification is authorized.
  </read_first>
  <action>
Per D-01/D-09/D-10, add the literal ordinary kind `paste-key` to the same request/result union and closed validators as Duplicate, with replay provenance forbidden. Add a semantic Paste intent and exported factory that carry an immutable clipboard payload, direct destination `appFrame`, and either an existing destination `keyId` or one newly allocated `newKeyId` for an empty destination. Validate and clone the clipboard payload inside the proposal and retarget only its physical payload frame.

For Paste-to-empty, require an unoccupied bounded destination, allocate exactly one fresh stable identity, retain every existing record unchanged, append exactly one canonical record, and select the new identity. Extend the shared pure semantic-delta validator to prove those exact facts from authoritative current/proposed records. For Paste-to-existing, require the declared destination identity/frame, preserve the complete identity and frame sets, replace exactly that one destination payload with the declared clipboard payload retargeted to the same frame, retain every other payload semantically unchanged, and select the preserved destination identity. Reject malformed clipboard data, unknown or mismatched destination, duplicate identity/frame, stale or partial collections, additional changes, and over-capacity output.

Use the same semantic validator in the coordinator before staging and in the parent bridge before mutation and before ordinary-ledger insertion. The parent must distinguish creation from replacement using the declared intent/result data and authoritative current records; structural validity alone is insufficient. Preserve all existing launch, layer, project, revision, capacity, selection, replay provenance, replay-ledger, ordinary-ledger, and store mutation checks. Return the same discriminated exact result contract from authoritative parent values for both Paste forms, including actual accepted revision and accepted selection. The read-only result controller passes this result unchanged; the coordinator matches the received tuple/result directly and publishes accepted output only after exact agreement.

Classify `paste-key` as ordinary history so accepted empty-target creation or existing-target payload replacement records exactly one complete before/after command and clears redo once; rejected, stale, malformed, semantically invalid, transport-failed, mismatched, or no-change Paste leaves history unchanged. Preserve every pre-existing operation kind and replay rule, one coordinator, one pending authority, one bridge envelope, and existing Signals/direct derivation.

Finish with a bounded five-owner inspection proving both semantic literals and the discriminated result contract reach types, resolver, coordinator, history, and parent bridge; the bridge invokes operation-specific semantic validation before mutation/ledger; and no child reconstruction, accepted-revision fallback, Duplicate/Paste replay provenance, second result channel, or sixth owner exists.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/types/physicPaint.ts','app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts','app/src/lib/physicPaintBridge.ts'];for(const file of files){const text=fs.readFileSync(file,'utf8');for(const kind of ['duplicate-key','paste-key'])if(!text.includes(kind))throw new Error(file+' missing '+kind)}const coordinator=fs.readFileSync(files[2],'utf8');if((coordinator.match(/export function useRotoPhysicalEditCoordinator/g)||[]).length!==1)throw new Error('coordinator authority count changed');" &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <done>Duplicate, Paste-to-empty, and Paste-to-existing are explicit ordinary complete-record operations; the parent independently proves each declared semantic delta before mutation and ledger insertion; exact authoritative acknowledgements settle the sole coordinator and history path; existing operations and ledgers remain intact; and bounded five-owner static/typecheck verification passes without tests or build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 19 utility intent → physical resolver | Source identity, clipboard payload, destination identity/frame, and fresh identity enter the canonical physical proposal boundary and must fail closed. |
| Resolver proposal → acknowledged coordinator | A complete ordinary next-record candidate crosses into optimistic runtime publication and must be revalidated against current launch, records, capacity, selection, and revision. |
| Standalone coordinator → parent bridge | Optimistic records cross a trust boundary; the parent must independently validate the declared operation delta before mutation and return its authoritative exact acknowledgement. |
| Parent bridge → coordinator/result listener | The discriminated physical result must pass through unchanged and match operation, launch, layer, project, revisions, selection, outcome, and replay provenance before acceptance. |
| Accepted output → history | Only exact ordinary parent acceptance may create one complete before/after command; replay acceptance must only move an existing command. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-20-01 | Spoofing | Duplicate/Paste source or destination identity | high | mitigate | Validate bounded IDs, require exact membership or absence according to the semantic branch, and match selected identity/frame against the complete proposal. |
| T-36.14-20-02 | Tampering | Clipboard/source payload and complete next records | high | mitigate | Reuse strict payload/record/collection validators, clone allowlisted fields, retarget only direct payload appFrame, and reject duplicate, partial, unknown, or over-capacity collections before staging. |
| T-36.14-20-03 | Repudiation | Ordinary versus replay classification | high | mitigate | Carry explicit `duplicate-key`/`paste-key` literals through request, exact result, accepted output, and `isOrdinaryOperationKind`; forbid replay provenance on both kinds. |
| T-36.14-20-04 | Information Disclosure | Failure details | low | mitigate | Preserve concise coordinator status and existing LOG-only diagnostics; add no payload logging or new telemetry channel. |
| T-36.14-20-05 | Denial of Service | Repeated utility activation | medium | mitigate | Reuse the existing single pending operation and barrier/revision revalidation; add no second pending or settlement path. |
| T-36.14-20-06 | Elevation of Privilege | Generic record replacement disguised as Duplicate or Paste | high | mitigate | Require the parent bridge to invoke the shared pure operation-specific semantic-delta validator against authoritative current/proposed records before store mutation and ordinary-ledger insertion. |
| T-36.14-20-07 | Spoofing | Child-reconstructed physical acknowledgement | high | mitigate | Return a discriminated parent-authoritative result with the full operation tuple, revisions, selection, outcome, and replay provenance, pass it through unchanged, and require exact coordinator matching with no local accepted-revision fallback. |
| T-36.14-20-SC | Tampering | pnpm package supply chain | low | accept | No install, audit, manifest, lockfile, or dependency change occurs; pnpm invokes the existing typecheck script only. |
</threat_model>

## Multi-Source Coverage Audit

### GOAL coverage

| Source item | Coverage |
|---|---|
| ROADMAP goal: one deterministic physical-frame authority before polished UI | Tasks 1-2 place Duplicate/Paste inside the same physical proposal/coordinator/history authority before Plan 19 composition and Plans 11-18 gates. COVERED. |

### REQ coverage

| Requirement | Coverage |
|---|---|
| 36.14-PHYSICAL-IDENTITY | Fresh identities are allocated once for Duplicate/Paste-to-empty; Paste-to-existing preserves destination identity/frame. COVERED. |
| 36.14-DERIVED-INTERPOLATION | Complete proposal finalization reuses the existing physical projection; no generated durable records or timing inputs are added. COVERED. |
| 36.14-ATOMIC-FRAME-MAPPING | Both operations stage one validated complete record set through the existing acknowledged coordinator and accepted-only history. COVERED. |
| 36.14-DOWNSTREAM-PARITY | Accepted complete records use the same parent/store/downstream publication path already established by Plans 09-10. COVERED. |
| 36.14-UAT-THEN-REGRESSION | This plan runs static/typecheck verification only and leaves tests locked until Plan 12 approval. COVERED. |
| 36.14-RIPPLE-INSERT-DELETE, 36.14-RIPPLE-DRAG, 36.14-FORCE-SPACING | Existing operation branches are explicitly preserved and remain covered by Plans 06-08, 12-18. COVERED ELSEWHERE. |
| 36.14-UI-INTEGRATION | No UI is changed; Plans 15-18 remain the approved owner. COVERED ELSEWHERE. |

### RESEARCH coverage

| Research feature or constraint | Coverage |
|---|---|
| Stable identity plus direct physical location | Tasks 1-2 use keyId/appFrame only. COVERED. |
| One deep resolver and one acknowledged coordinator | Duplicate/Paste extend the existing resolver and coordinator rather than adding utility-local authority. COVERED. |
| Complete immutable proposals and fail-closed validation | Both tasks produce complete next records and require shared operation-specific semantic-delta validation in the resolver/coordinator and independently in the parent before mutation. COVERED. |
| Parent revalidation and exact settlement | The bridge preserves existing structural/revision/capacity/selection/ledger checks, adds Duplicate/Paste delta validation, and returns the authoritative discriminated result consumed unchanged by the coordinator. COVERED. |
| Accepted-only complete history | Both kinds are ordinary and record only after exact parent acceptance. COVERED. |
| Preact Signals/direct derivation and lifecycle-only effects | Scope constraints preserve current Signal/computed state and existing subscription/disposal effects. COVERED. |
| Clean cutover, no compatibility or package | Prohibitions and final five-owner boundary. COVERED. |
| Production → typecheck/build → native UAT → regressions | Plan 20 typechecks only; Plan 19 typechecks; Plan 11 owns build; Plan 12 owns UAT approval; Plan 13 unlocks tests. COVERED. |

### CONTEXT coverage

| Decision | Coverage |
|---|---|
| D-01 | Stable identity, direct appFrame, fresh creation IDs, and destination preservation in Tasks 1-2. COVERED. |
| D-02 | Existing strict-interior runtime projection is reused; no durable generated state. COVERED. |
| D-03 | No source/display, alias, migration, compatibility, or dual authority. COVERED. |
| D-04 | Script Motion is untouched and remains separate. COVERED. |
| D-05/D-06/D-07/D-08 | Existing Insert/Delete/Move/Force Spacing branches are preserved. COVERED. |
| D-09 | Complete proposal, parent-side declared-delta proof, one coordinator transaction, parent-echoed exact acceptance, rollback, and one history command. COVERED. |
| D-10 | Existing parent/store/downstream complete-record path is reused with authoritative parent acknowledgement. COVERED. |
| D-11 | One bounded final five-owner foundation wave precedes Plan 19 composition. COVERED. |
| D-12 | No tests/build/native process; Plan 12 approval remains the regression gate. COVERED. |
| D-13 through D-28 | No UI, drag presentation, status layout, or Script control decision is changed; Plans 15-18 retain ownership. COVERED ELSEWHERE. |
| Deferred multi-selection/group movement | Not introduced. EXCLUDED — DEFERRED. |
| Deferred migration/compatibility support | Not introduced. EXCLUDED — DEFERRED. |

<verification>
1. Confirm execution changes only the exact five production files listed in frontmatter plus `36.14-20-SUMMARY.md`; no sixth production owner, test, package, manifest, lockfile, configuration, generated dependency, dormant module, or approval artifact changes.
2. Confirm `duplicate-key` and `paste-key` are accepted ordinary literals in shared payload/result validation, resolver status/proposal, coordinator request/received acknowledgement/accepted output, parent bridge validation/result construction, and history classification.
3. Confirm Duplicate and Paste-to-empty each add exactly one fresh identity and leave every other record unchanged; Paste-to-existing preserves identity/frame sets and changes exactly one destination payload; every success carries one complete validated next-record set.
4. Confirm the parent bridge invokes operation-specific semantic-delta validation against authoritative current/proposed records before store mutation and before ordinary-ledger insertion while preserving all existing structural, launch, layer, project, revision, capacity, selection, ordinary-ledger, and replay-ledger checks.
5. Confirm the parent returns the discriminated exact physical result with authoritative operation tuple, expected/staged/accepted revisions, accepted selection, outcome, and replay provenance only when applicable; the read-only result controller passes it unchanged; the coordinator performs exact received-field matching with no reconstruction or accepted-revision fallback.
6. Confirm no replayRecords staging for Duplicate/Paste, generic transaction forwarding, second coordinator, direct bridge bypass, operation-specific settlement, duplicate result channel, duplicate history, or duplicate rollback path exists.
7. Run only the two task verification commands. Confirm no test, build, package, server, Vite, Tauri, browser, watcher, or native boundary was crossed.
8. Confirm the resulting five public owner surfaces are consumable by Plan 19 while model, store, result controller, ports, tests, and every sixth owner remain read-only.
</verification>

<success_criteria>
- The exact five-file foundation carries explicit Duplicate/Paste ordinary semantics through resolver, coordinator, parent authority, exact acknowledgement, and accepted-only history.
- Duplicate and Paste-to-empty add one fresh stable identity with immutable source/clipboard paint at one validated direct destination and leave every other record unchanged.
- Paste-to-existing preserves destination keyId/appFrame and all identity/frame sets, replaces exactly one immutable payload, and leaves every other record unchanged.
- Resolver/coordinator validate one complete proposal, and the parent independently validates the declared operation-specific semantic delta before mutation or ledger insertion; failure carries no partial state.
- The parent-echoed discriminated acknowledgement is the sole settlement input; exact received-field matching produces one accepted output and one ordinary history command, while Undo/Redo preserve the existing replay provenance and ledgers.
- Insert, Delete, Move, Force Spacing, Undo, Redo, structural/revision/capacity/selection checks, Signals, Script, Basic, FX, non-Roto, dormant modules, and approval contracts remain unchanged.
- Bounded five-owner static checks and app typecheck pass; no build, test, server, native process, package/configuration change, or UAT claim occurs.
- Plan 19 can consume `36.14-20-SUMMARY.md` and the five final interfaces at Wave 12 without adding a sixth owner.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-20-SUMMARY.md` when done. Record the exact five-file diff; operation literals and intent/proposal interfaces; shared semantic-delta validator; parent invocation order before mutation/ledger; preserved structural/revision/capacity/selection/ordinary-ledger/replay-ledger checks; discriminated parent-authoritative acknowledgement fields; coordinator exact received-field matching with no reconstruction/fallback; ordinary-history classification; bounded verification; and Plan 19 handoff. Do not claim native UAT or phase completion.
</output>
