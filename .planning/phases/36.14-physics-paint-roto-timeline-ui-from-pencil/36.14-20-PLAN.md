---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 20
type: execute
wave: 11
depends_on:
  - 36.14-10
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
prohibitions:
  - "Do not modify a fifth production owner; stop and report the exact missing owner if the four-file boundary cannot expose the required public seam."
  - "Do not abuse Undo/Redo replay, masquerade under an existing operation kind, forward a generic transaction, bypass the parent bridge, create a second coordinator, or add operation-specific settlement/history/rollback authority."
  - "Do not create compatibility aliases, migration, shims, hidden registries, dual writes, fallback coordinates, or durable generated timing truth."
  - "Do not create, modify, delete, rename, discover, or execute tests; do not run Vitest or build."
  - "Do not change packages, manifests, lockfiles, generated dependencies, configuration, dormant modules, approval artifacts, Basic paint, FX p5.brush, non-Roto Physics Paint, Script behavior, or deterministic Motion."
must_haves:
  truths:
    - "Per D-01 and D-09, Duplicate and Paste are explicit ordinary physical-edit operation kinds carried consistently through resolver proposal, coordinator request, exact acknowledgement, accepted output, and accepted-only history."
    - "Duplicate and Paste-to-empty add exactly one fresh stable key identity at one validated direct appFrame and carry an immutable copy of the source-key or clipboard paint payload inside the ordinary complete proposal."
    - "Paste-to-existing preserves the destination keyId and appFrame while replacing only its payload from the immutable reusable clipboard snapshot."
    - "The resolver returns one closed, complete, validated next-record proposal for identity/payload-changing operations; malformed, duplicate, unknown, stale, partial, or over-capacity work produces no stageable proposal."
    - "The existing coordinator remains the only pending, snapshot, optimistic publication, bridge send, exact acknowledgement, rollback, settlement, and accepted-output authority for Duplicate and Paste."
    - "The existing history hook classifies Duplicate and Paste as ordinary edits, records exactly one immutable before/after command only after exact acceptance, and replays that command through the existing Undo/Redo path."
    - "Existing Insert, Delete, Move, Force Spacing, Undo, and Redo behavior and Preact Signal/direct-derivation ownership remain unchanged."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Closed Duplicate/Paste operation literals in request, result, and active bridge validators."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "Semantic Duplicate/Paste intents and immutable complete next-record proposals using existing physical validators and identity allocation."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Ordinary complete-record staging and exact acknowledgement through the existing serialized coordinator lifecycle."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      provides: "Accepted-only ordinary history classification for Duplicate/Paste with existing complete-snapshot replay."
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      via: "one immutable proposal carrying exact operation kind, complete mapping, selection, and complete next records when identity or payload changes"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "app/src/types/physicPaint.ts"
      via: "validated replace-roto-physical-map request/result with the same semantic operation kind and no replay provenance"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      via: "matching accepted output containing immutable before/after snapshots and the ordinary Duplicate/Paste kind"
---

<objective>
Extend the existing closed physical-edit foundation so Duplicate and Paste are first-class ordinary acknowledged operations before the five-owner Studio/key-utility composition cutover.

Purpose: Plans 01-10 established stable physical records, one resolver, one acknowledged coordinator, and accepted-only history, but Duplicate and Paste still lack protocol-level operation kinds and complete identity/payload proposals. Per D-01, D-03, D-09, D-10, D-11, and D-12, this plan adds those semantics inside exactly the four approved foundation owners so Plan 19 can compose the visible utilities without replay abuse, a generic transaction adapter, or a fifth production owner.

Output: Public semantic Duplicate/Paste intent and proposal seams, closed request/result validation, ordinary complete-record staging through the existing coordinator, accepted-only history classification, and a bounded typecheck-only handoff to Plan 19.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-02-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-04-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md

# Exact approved production boundary
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts

# Read-only existing public dependencies; do not modify
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
@app/src/lib/physicPaintBridge.ts
</context>

<user_story>
**As a** stop-motion animator, **I want to** Duplicate and Paste Roto paint through the same acknowledged physical authority as every other timeline edit, **so that** key identity, payload ownership, rollback, Undo/Redo, persistence, and rendered output cannot disagree.
</user_story>

<scope_constraints>
- Modify exactly the four production files in frontmatter. `physicsPaintRotoPhysicalModel.ts`, `rotoCoordinatorPorts.ts`, `physicPaintBridge.ts`, store, result controller, ports, Plan 19 owners, dormant modules, and tests are read-only in this plan. If one of those owners must change for correctness, stop and report the exact missing public seam rather than widening the plan.
- Reuse `createPhysicPaintRotoKeyId`, `isPhysicPaintRotoRealKeyPayload`, `isPhysicPaintRotoRealKeyRecord`, and `parsePhysicPaintRotoRealKeyRecordCollection` from the existing physical model. Do not recreate UUID, payload, record, collection, capacity, or revision validation.
- Keep the core resolver deterministic for a supplied intent. If operation-specific intent factories allocate a new identity, allocate exactly once through `createPhysicPaintRotoKeyId`, freeze the resulting intent, and pass that same identity through proposal and commit; validation, parsing, hydration, and replay never allocate or repair IDs.
- Keep Duplicate/Paste on `replace-roto-physical-map`. They are ordinary kinds with no replay provenance and must use the existing coordinator pending/snapshot/send/match/restore lifecycle and parent ordinary-command ledger.
- Keep current mapping-only behavior for Insert/Delete/Move/Force Spacing and complete-snapshot behavior for Undo/Redo. Do not route Duplicate/Paste through `replayRecords` or synthesize an old operation kind.
- Preserve existing Signals/computed outputs and lifecycle-only effects. Add no `useState`, copied pending state, effect-driven mirror, second accepted-output channel, or duplicated history stack.
- Run only the automated commands named below. Do not run build, Vitest, server, Vite, Tauri, browser, native application, watcher, install, or audit commands.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Carry one semantic Duplicate through proposal, acknowledgement, and accepted history</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts</files>
  <read_first>
    - `app/src/types/physicPaint.ts` — extend the one shared request/result operation union and all three active physical payload/result guards; ordinary kinds must continue forbidding replay provenance.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` — preserve `resolvePhysicPaintRotoPhysicalEdit` as the sole resolution seam and reuse its common finalizer/projection rather than adding a utility resolver.
    - `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` — preserve `executePhysicalEdit`, `transitionPhysicalEditResult`, complete snapshot/restore, one pending tuple, one bridge send, and one accepted output.
    - `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` — extend `isOrdinaryOperationKind`; do not change the existing accepted-output subscription or replay stack movement.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` — import the public ID allocator and strict payload/record/collection validators; do not duplicate them.
    - `app/src/lib/physicPaintBridge.ts` — read-only confirmation that every non-replay physical kind already uses the ordinary accepted-command ledger and complete parent replacement.
  </read_first>
  <action>
Per D-01/D-03/D-09, add the literal ordinary kind `duplicate-key` to the shared apply operation union and every closed payload/result validator in `physicPaint.ts`. It must be accepted on the existing `replace-roto-physical-map` envelope, echoed by exact acknowledgement, and forbidden from carrying Undo/Redo provenance.

In the physical resolver, add a semantic `duplicate-key` intent that names the source stable `keyId`, one direct destination `appFrame`, and one fresh `newKeyId` allocated exactly once by an exported Duplicate intent factory through `createPhysicPaintRotoKeyId`. Extend the resolver input/proposal only as needed to carry current canonical records and one immutable complete `nextRecords` result for identity/payload-changing ordinary operations. Validate current records with the existing collection parser, require exactly one matching source identity, require a bounded unoccupied direct destination, require the fresh ID to be valid and absent from the current set, clone the source paint payload without caller-owned mutation, retarget the cloned payload's physical `appFrame` to the destination, and derive mapping, ordering, cells, selection, changes/status, and complete records from the same candidate. The success result adds exactly one record; every malformed, duplicate, unknown, occupied, partial, or over-capacity input returns the existing closed failure branch with no proposal.

In the coordinator, add one centralized ordinary-proposal staging branch that accepts Duplicate's complete `nextRecords`, validates it again with the existing collection parser against current capacity, proves proposal operation kind, mapping, selected identity/frame, and exact current-to-next identity delta are coherent, and rejects unknown or partial work before snapshot publication. Preserve the current expected-revision recheck after paint barriers, one optimistic whole-record replacement, one payload send, one pending tuple, exact result matching, timeout/transport/launch rollback, and accepted output. Do not add a Duplicate-specific bridge call, pending slot, settlement callback, rollback function, or history channel.

In history, classify `duplicate-key` as ordinary. Matching acceptance records one complete before/after command after exact settlement; rejection and no-change record nothing; Undo/Redo continue replaying the stored complete snapshots through the existing provenance path. Keep the existing Preact Signal subscription and stacks unchanged apart from the ordinary-kind classification.
  </action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <done>One Duplicate request adds exactly one fresh identity with source-owned paint at the validated destination, crosses the existing exact acknowledged lifecycle as `duplicate-key`, produces one accepted ordinary history command, remains replayable through existing Undo/Redo, and the app typechecks without modifying a fifth owner or any test/build artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Add Paste creation and payload replacement to the same ordinary proposal path</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts</files>
  <read_first>
    - Task 1 final `duplicate-key` operation union, complete-record proposal shape, centralized coordinator staging, and ordinary-history classification.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` — reuse strict immutable payload/record/collection validation and the one ID allocator.
    - `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` — keep the existing execute input, pending tuple, accepted output, records port, and bridge port; no port-owner modification is authorized here.
  </read_first>
  <action>
Per D-01/D-09/D-10, add the literal ordinary kind `paste-key` to the same request/result union and closed validators as Duplicate, with no replay provenance. Add a semantic Paste intent and exported intent factory that carry an immutable clipboard payload, direct destination `appFrame`, and either an existing destination `keyId` or one newly allocated `newKeyId` for an empty destination. Validate clipboard payload with `isPhysicPaintRotoRealKeyPayload`, clone it inside the ordinary proposal, and retarget only its physical payload `appFrame` to the destination.

For Paste-to-empty, require no real record at the destination, allocate exactly one fresh stable identity through the existing factory seam, retain every existing record unchanged, append exactly one canonical record, and select the new identity. For Paste-to-existing, require exactly one destination record whose `keyId` and `appFrame` match the intent, preserve that identity and frame, replace only its immutable payload, retain every other record byte-for-byte at the semantic field level, and select the preserved destination identity. Route both cases through the resolver's one common complete-record finalization so mapping, records, selection, status, and physical cells cannot disagree; a malformed clipboard, unknown destination, mismatched key/frame, duplicate ID/frame, stale/partial collection, or over-capacity result returns no proposal.

Extend the Task 1 centralized coordinator validation so both Paste forms are accepted only when `proposal.status.operationKind`, submitted `operationKind`, mapping, complete records, selected identity/frame, and allowed identity/payload delta agree. Revalidate current revision before staging and complete records before publication; then reuse the same pending, snapshot, bridge, exact acknowledgement, rollback, timeout, accepted-output, and parent ordinary-command path. Classify `paste-key` as ordinary history so an accepted empty-target creation or existing-target payload replacement records exactly one complete before/after command and clears redo once; rejected, stale, malformed, duplicate, transport-failed, or no-change Paste leaves history unchanged. Preserve every pre-existing operation kind and replay rule.

Finish with a bounded four-owner static inspection proving both semantic literals appear in the shared validator, resolver, coordinator, and ordinary history classification; there is still one coordinator hook, one bridge envelope, one pending authority, and no Duplicate/Paste replay provenance branch. Use existing Signals/direct derivation only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/types/physicPaint.ts','app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts'];for(const file of files){const text=fs.readFileSync(file,'utf8');for(const kind of ['duplicate-key','paste-key'])if(!text.includes(kind))throw new Error(file+' missing '+kind)}const coordinator=fs.readFileSync(files[2],'utf8');if((coordinator.match(/export function useRotoPhysicalEditCoordinator/g)||[]).length!==1)throw new Error('coordinator authority count changed');" &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <done>Duplicate, Paste-to-empty, and Paste-to-existing are explicit ordinary complete-record operations; fresh identities and preserved destination identities follow the required semantics; malformed/stale/partial work fails before staging; exact acceptance records one history command; existing operations remain intact; and bounded static/typecheck verification passes without tests or build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 19 utility intent → physical resolver | Source identity, clipboard payload, destination identity/frame, and fresh identity enter the canonical physical proposal boundary and must fail closed. |
| Resolver proposal → acknowledged coordinator | A complete ordinary next-record candidate crosses into optimistic runtime publication and must be revalidated against current launch, records, capacity, selection, and revision. |
| Standalone coordinator → parent bridge | Optimistic records become authoritative only after the matching operation kind, operation ID, launch/layer/project tuple, and expected revision are acknowledged. |
| Accepted output → history | Only exact ordinary acceptance may create one complete before/after command; replay acceptance must only move an existing command. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-20-01 | Spoofing | Duplicate/Paste source or destination identity | high | mitigate | Validate bounded IDs, require exact membership or absence according to the semantic branch, and match selected identity/frame against the complete proposal. |
| T-36.14-20-02 | Tampering | Clipboard/source payload and complete next records | high | mitigate | Reuse strict payload/record/collection validators, clone allowlisted fields, retarget only direct payload appFrame, and reject duplicate, partial, unknown, or over-capacity collections before staging. |
| T-36.14-20-03 | Repudiation | Ordinary versus replay classification | high | mitigate | Carry explicit `duplicate-key`/`paste-key` literals through request, exact result, accepted output, and `isOrdinaryOperationKind`; forbid replay provenance on both kinds. |
| T-36.14-20-04 | Information Disclosure | Failure details | low | mitigate | Preserve concise coordinator status and existing LOG-only diagnostics; add no payload logging or new telemetry channel. |
| T-36.14-20-05 | Denial of Service | Repeated utility activation | medium | mitigate | Reuse the existing single pending operation and barrier/revision revalidation; add no second pending or settlement path. |
| T-36.14-20-06 | Elevation of Privilege | Generic record replacement disguised as Paste | high | mitigate | Require resolver-owned semantic delta proof and coordinator revalidation for exactly one creation or one destination payload replacement before ordinary staging. |
| T-36.14-20-SC | Tampering | pnpm package supply chain | low | accept | No install, audit, manifest, lockfile, or dependency change occurs; pnpm invokes the existing typecheck script only. |
</threat_model>

## Multi-Source Coverage Audit

### GOAL coverage

| Source item | Coverage |
|---|---|
| ROADMAP goal: one deterministic physical-frame authority before polished UI | Tasks 1-2 place Duplicate/Paste inside the same physical proposal/coordinator/history authority before Plan 19 composition and Plans 11-18 gates. COVERED. |

### REQ coverage

| Requirement | Coverage |
|---|---|
| 36.14-PHYSICAL-IDENTITY | Fresh identities are allocated once for Duplicate/Paste-to-empty; Paste-to-existing preserves destination identity/frame. COVERED. |
| 36.14-DERIVED-INTERPOLATION | Complete proposal finalization reuses the existing physical projection; no generated durable records or timing inputs are added. COVERED. |
| 36.14-ATOMIC-FRAME-MAPPING | Both operations stage one validated complete record set through the existing acknowledged coordinator and accepted-only history. COVERED. |
| 36.14-DOWNSTREAM-PARITY | Accepted complete records use the same parent/store/downstream publication path already established by Plans 09-10. COVERED. |
| 36.14-UAT-THEN-REGRESSION | This plan runs static/typecheck verification only and leaves tests locked until Plan 12 approval. COVERED. |
| 36.14-RIPPLE-INSERT-DELETE, 36.14-RIPPLE-DRAG, 36.14-FORCE-SPACING | Existing operation branches are explicitly preserved and remain covered by Plans 06-08, 12-18. COVERED ELSEWHERE. |
| 36.14-UI-INTEGRATION | No UI is changed; Plans 15-18 remain the approved owner. COVERED ELSEWHERE. |

### RESEARCH coverage

| Research feature or constraint | Coverage |
|---|---|
| Stable identity plus direct physical location | Tasks 1-2 use keyId/appFrame only. COVERED. |
| One deep resolver and one acknowledged coordinator | Duplicate/Paste extend the existing resolver and coordinator rather than adding utility-local authority. COVERED. |
| Complete immutable proposals and fail-closed validation | Both tasks produce and revalidate complete next records with no partial success. COVERED. |
| Accepted-only complete history | Both kinds are ordinary and record only after exact acceptance. COVERED. |
| Preact Signals/direct derivation and lifecycle-only effects | Scope constraints preserve current Signal/computed state and existing subscription/disposal effects. COVERED. |
| Clean cutover, no compatibility or package | Prohibitions and four-owner boundary. COVERED. |
| Production → typecheck/build → native UAT → regressions | Plan 20 typechecks only; Plan 19 typechecks; Plan 11 owns build; Plan 12 owns UAT approval; Plan 13 unlocks tests. COVERED. |

### CONTEXT coverage

| Decision | Coverage |
|---|---|
| D-01 | Stable identity, direct appFrame, fresh creation IDs, and destination preservation in Tasks 1-2. COVERED. |
| D-02 | Existing strict-interior runtime projection is reused; no durable generated state. COVERED. |
| D-03 | No source/display, alias, migration, compatibility, or dual authority. COVERED. |
| D-04 | Script Motion is untouched and remains separate. COVERED. |
| D-05/D-06/D-07/D-08 | Existing Insert/Delete/Move/Force Spacing branches are preserved. COVERED. |
| D-09 | Complete proposal, one coordinator transaction, exact acceptance, rollback, and one history command. COVERED. |
| D-10 | Existing parent/store/downstream complete-record path is reused. COVERED. |
| D-11 | One bounded four-owner foundation wave precedes Plan 19 composition. COVERED. |
| D-12 | No tests/build/native process; Plan 12 approval remains the regression gate. COVERED. |
| D-13 through D-28 | No UI, drag presentation, status layout, or Script control decision is changed; Plans 15-18 retain ownership. COVERED ELSEWHERE. |
| Deferred multi-selection/group movement | Not introduced. EXCLUDED — DEFERRED. |
| Deferred migration/compatibility support | Not introduced. EXCLUDED — DEFERRED. |

<verification>
1. Confirm execution changes only the four production files listed in frontmatter plus `36.14-20-SUMMARY.md`.
2. Confirm `duplicate-key` and `paste-key` are accepted ordinary literals in shared payload/result validation, resolver status/proposal, coordinator request/acknowledgement/accepted output, and history classification.
3. Confirm Duplicate and Paste-to-empty add exactly one fresh identity; Paste-to-existing preserves destination identity/frame; every success carries a complete validated next-record set.
4. Confirm coordinator staging rejects malformed, duplicate, unknown, stale, partial, mismatched-kind, mismatched-selection, and over-capacity work before optimistic publication.
5. Confirm no replay provenance, replayRecords staging, generic transaction forwarding, second coordinator, direct bridge bypass, operation-specific settlement, duplicate history, or duplicate rollback path handles Duplicate/Paste.
6. Run only the two task verification commands. Confirm no test, build, package, server, browser, or native boundary was crossed.
7. Confirm the resulting public seams are consumable by Plan 19 without modifying model, store, bridge, result controller, ports, tests, or a fifth production owner.
</verification>

<success_criteria>
- The exact four-file foundation carries explicit Duplicate/Paste ordinary semantics end-to-end.
- Duplicate and Paste-to-empty add one fresh stable identity with immutable source/clipboard paint at one validated direct destination.
- Paste-to-existing preserves destination keyId/appFrame and replaces only immutable payload data.
- Resolver and coordinator independently validate one complete next-record proposal before publication; failure carries no partial state.
- Exact acknowledgement produces one accepted output and one ordinary history command; Undo/Redo replay complete snapshots through the existing route.
- Insert, Delete, Move, Force Spacing, Undo, Redo, Signals, Script, Basic, FX, non-Roto, dormant modules, and approval contracts remain unchanged.
- Static checks and app typecheck pass; no build, test, server, native process, package change, or UAT claim occurs.
- Plan 19 can consume `36.14-20-SUMMARY.md` and the four final interfaces at Wave 12 without adding a sixth owner.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-20-SUMMARY.md` when done. Record the exact operation literals, intent/proposal interfaces, complete-record validation, identity-allocation seam, coordinator staging/acknowledgement behavior, ordinary-history classification, four-file diff, bounded verification, and Plan 19 handoff. Do not claim native UAT or phase completion.
</output>
