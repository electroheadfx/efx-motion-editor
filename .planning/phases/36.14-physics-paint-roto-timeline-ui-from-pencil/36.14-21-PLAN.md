---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 21
type: execute
wave: 21
depends_on:
  - 36.14-11
files_modified:
  - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
gap_closure: true
gap_ids:
  - G-36.14-2
  - G-36.14-3
  - G-36.14-5
  - G-36.14-6
  - G-36.14-7
  - G-36.14-8
  - G-36.14-9
  - G-36.14-10
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-RIPPLE-INSERT-DELETE
  - 36.14-RIPPLE-DRAG
  - 36.14-FORCE-SPACING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "Per D-01 and D-09, every accepted Insert, Delete, Drag, Force Spacing, Duplicate, and Paste command retains one immutable complete before/after child snapshot keyed by stable keyId and direct appFrame."
    - "The existing uncommitted signal() to useSignal() correction remains intact so pending, accepted, failure, and presentation publication use stable per-component Signal identities."
    - "Undo and Redo stage the exact immutable target snapshot, including confirmedFrames, caches, buffers, selection, reference, and engine state, while the parent remains authoritative only for canonical records, interpolation, revision, and provenance."
    - "History stacks move only after exact matching parent acceptance; rejection, timeout, mismatch, transport failure, and lifecycle cancellation restore the captured current snapshot and do not move history."
    - "Rejected ordinary work remains history-neutral, and visible availability continues to represent the existing combined physical-command and paint-barrier ledger only after accepted transitions."
  artifacts:
    - path: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      provides: "Complete physical snapshot and restore/replay contracts including confirmedFrames and replayTargetSnapshot."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Stable Signal publication plus exact target-snapshot replay, accepted settlement, and rollback."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      provides: "Immutable target-snapshot dispatch and accepted-only multi-level stack transitions."
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Complete confirmed-frame capture/restore and engine/cache/reference ports without replacing intentional first-paint fixes."
  key_links:
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      via: "replayTargetSnapshot carries the command's immutable before or after child snapshot"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "snapshot/restore ports stage every child-owned category, including confirmedFrames"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      via: "unchanged exact parent replay acknowledgement triggers the sole stack transition"
---

<objective>
Close the shared accepted-history and exact-replay defects behind G-36.14-2, G-36.14-3, the history portions of G-36.14-5 through G-36.14-10, while preserving the intentional working-tree Signal and first-paint corrections.

Purpose: D-09 requires one complete snapshot, one parent-authoritative acknowledgement, accepted-only history, and complete rollback. The current working tree repairs unstable coordinator Signal identity, but successful Undo/Redo still transports only canonical records/interpolation and cannot restore deleted or newly created identity-owned child state.

Output: Stable coordinator Signals, a complete snapshot contract containing confirmedFrames, exact immutable target-snapshot replay, complete failure restoration, and trustworthy accepted-only multi-level Undo/Redo availability. No tests, typecheck, build, server, browser, or native application are run per D-12 and the renewed UAT-first decision.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-PLAN.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md
@.planning/debug/uat-36-14-history.md

@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<scope_constraints>
- Preserve all intentional uncommitted production changes. In particular, retain the four hook-stable `useSignal()` allocations, deferred post-flush authority reads, direct payload appFrame retargeting, first-paint target creation, and accepted Paste refresh.
- Preserve the parent ledger as canonical records/interpolation/revision/provenance authority only. Do not move child buffers, caches, confirmed frames, reference state, or engine snapshots into the parent bridge.
- Preserve one coordinator, one pending operation, one accepted output, one failure output, one complete rollback path, and the existing physical-plus-paint history ledger.
- Do not add compatibility coordinates, aliases, migrations, forwarding wrappers, dual writes, a second history stack, or a second transaction authority.
- D-12 and the current user decision override workflow TDD for this pre-UAT gap pass: do not create, modify, discover, or execute tests; do not run typecheck, build, server, browser, or native application.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Replay one Insert command through an exact immutable target snapshot</name>
  <files>app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - `rotoCoordinatorPorts.ts` snapshot, buffer, restore, and execute-input interfaces.
    - `useRotoPhysicalEditCoordinator.ts` current uncommitted `useSignal()` allocations, `captureSnapshot`, `restoreSnapshot`, replay staging, acceptance, and failure paths.
    - `useRotoPhysicalEditHistory.ts` immutable command entries, pending replay provenance, `undo`, `redo`, and `recordAcceptedEdit`.
    - `PhysicsPaintStudio.tsx` ownership snapshot, confirmed-frame storage, engine snapshot, reference, cache, and edit-buffer ports; preserve all pre-existing working-tree edits.
  </read_first>
  <action>
Per D-01/D-09, extend `RotoPhysicalEditSnapshot` and the buffer capture/restore port with `confirmedFrames` using the same immutable map semantics as preview/captured frames. Add `replayTargetSnapshot?: RotoPhysicalEditSnapshot<EngineState>` to `RotoPhysicalEditExecuteInput`; this is a child-local object and must not enter the bridge payload.

Retain the existing `useSignal()` allocations exactly as hook-stable component state. For replay, capture the current complete snapshot first for rollback, validate that the target snapshot matches the active launch/layer and the replay provenance's target records/interpolation/revision, then stage the target snapshot's child-owned dirty/editable/live-overlay/frame/preview/captured/confirmed/reference/selection/current-frame/engine state directly. Send only target canonical records, interpolation, selection, expected revision, and replay provenance to the parent.

Make one accepted Insert Undo/Redo path the tracer: a matching parent acceptance leaves the exact staged target snapshot in place and publishes the existing accepted output; any rejection, timeout, transport failure, launch replacement, disposal, or acknowledgement mismatch restores the current snapshot captured before replay. Do not reconstruct deleted/new identity state from the post-edit ownership maps and do not move history stacks inside the coordinator.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const ports=fs.readFileSync('app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts','utf8');const coordinator=fs.readFileSync('app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','utf8');const studio=fs.readFileSync('app/src/components/physic-paint/PhysicsPaintStudio.tsx','utf8');for(const token of ['confirmedFrames','replayTargetSnapshot'])if(!ports.includes(token))throw new Error('ports missing '+token);for(const token of ['useSignal','replayTargetSnapshot','confirmedFrames'])if(!coordinator.includes(token))throw new Error('coordinator missing '+token);if(!studio.includes('confirmedFrames'))throw new Error('Studio missing confirmedFrames restore wiring');" &amp;&amp; git diff --check -- app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx</automated>
  </verify>
  <done>One Insert command can be dispatched toward its immutable before/after child snapshot, the parent still receives only canonical replay authority, exact acceptance leaves the target snapshot staged, every failure restores the complete captured current snapshot, confirmedFrames participates in both directions, and all four coordinator Signals remain hook-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize exact accepted-only replay across every physical command and availability transition</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - Task 1 final complete snapshot and replay contract.
    - All ordinary operation kinds in `isOrdinaryOperationKind` and the paint-barrier reconciliation path.
    - Exact accepted-output provenance matching and current-revision guards for both history directions.
  </read_first>
  <action>
Route Undo to `entry.before` and Redo to `entry.after` through `replayTargetSnapshot` for Insert, Delete, Move, Force Spacing, Duplicate, and Paste. Preserve immutable command entries and exact command ID/direction/source/target revision provenance. Keep stack movement deferred until `recordAcceptedEdit` observes the exact accepted replay output; only then move one command between applied and redo and publish availability. A new accepted ordinary action clears redo once; rejection and no-change append nothing.

Ensure identity-set-changing replay uses the stored target's child maps directly, so Undo Delete restores removed identity-owned paint/cache/buffers/confirmed frames and Redo Insert/Duplicate/Paste restores the same created identity and payload instead of allocating or rebuilding from current maps. Preserve existing paint barriers in the combined availability count and reconcile them only through the established engine history events; do not present pending or rejected work as an available physical command.

Perform a bounded static review of the four files. Confirm there is exactly one coordinator hook, one history hook, one accepted-output subscription, one replay pending reference, and no new state authority. Leave all tests and executable validation for the renewed user-owned native UAT and its later explicit regression unlock.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts','utf8');const c=fs.readFileSync('app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','utf8');for(const token of ['insert-slot','delete-key','move-key','force-spacing','duplicate-key','paste-key','replayTargetSnapshot'])if(!h.includes(token))throw new Error('history missing '+token);if((c.match(/export function useRotoPhysicalEditCoordinator/g)||[]).length!==1)throw new Error('coordinator authority count changed');if((h.match(/export function useRotoPhysicalEditHistory/g)||[]).length!==1)throw new Error('history authority count changed');" &amp;&amp; git diff --check -- app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx</automated>
  </verify>
  <done>Every accepted physical command supports exact multi-level Undo and Redo through its immutable complete child snapshot; stable parent acceptance alone moves the cursor; lifecycle/rejection paths restore all child state including confirmedFrames; rejections remain history-neutral; combined availability changes only after established physical or paint transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| History command → coordinator replay | An immutable target snapshot and replay provenance enter optimistic child staging. |
| Child snapshot → parent bridge | Only canonical records/interpolation/selection/revision/provenance may cross; child engine/cache state stays local. |
| Parent result → history cursor | A result must match command ID, direction, launch, layer, project, and revisions before stack movement. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-21-01 | Spoofing | Replay acceptance | high | mitigate | Preserve exact provenance and authoritative revision matching before accepted output or cursor movement. |
| T-36.14-21-02 | Tampering | Complete target snapshot | high | mitigate | Capture immutable maps including confirmedFrames and validate launch/layer plus canonical target fields before staging. |
| T-36.14-21-03 | Repudiation | History cursor | high | mitigate | Move one stack entry only after matching parent acceptance; rejection/no-change creates no transition. |
| T-36.14-21-04 | Information Disclosure | Snapshot diagnostics | low | accept | Keep existing concise status/LOG behavior and add no payload logging. |
| T-36.14-21-05 | Denial of Service | Replay dispatch | medium | mitigate | Reuse the sole pending coordinator and existing timeout/cancellation lifecycle. |
| T-36.14-21-06 | Elevation of Privilege | Child state sent to parent | high | mitigate | Keep buffers/caches/reference/engine/confirmed frames child-local; bridge receives canonical authority only. |
| T-36.14-21-SC | Tampering | Package supply chain | low | accept | No install, audit, manifest, lockfile, package-manager, or dependency action occurs. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Editing and history agree on one physical authority | Exact target snapshots preserve stable identity/direct appFrame while parent canonical replay remains authoritative | COVERED |
| REQ | PHYSICAL-IDENTITY / ATOMIC-FRAME-MAPPING | Complete immutable child before/after snapshots and exact accepted replay | COVERED |
| REQ | RIPPLE-INSERT-DELETE / RIPPLE-DRAG / FORCE-SPACING | Existing operation semantics are unchanged; their accepted snapshots become exactly replayable | COVERED |
| REQ | DOWNSTREAM-PARITY | Caches, reference, confirmed frames, engine state, and selection restore with the target | COVERED |
| REQ | UAT-THEN-REGRESSION | Static checks only; native UAT remains the next executable oracle | COVERED |
| RESEARCH | One coordinator, complete rollback, accepted-only history | Tasks 1-2 preserve one authority and complete the missing replay state | COVERED |
| CONTEXT | D-01 / D-09 / D-10 / D-12 | Stable identities, complete transaction, child/parent ownership, and UAT-first order are explicit | COVERED |
| CONTEXT | Deferred multi-selection and migration | Neither is introduced | EXCLUDED — DEFERRED |

<verification>
1. Confirm only the four listed production files plus the normal summary change during this plan.
2. Confirm the intentional working-tree `useSignal()` correction and first-paint/Paste fixes remain present.
3. Confirm `confirmedFrames` is captured, stored, staged, and restored with complete snapshots.
4. Confirm history passes immutable target snapshots and moves stacks only after exact parent acceptance.
5. Confirm rejected work remains history-neutral and no second coordinator/history/transaction authority appears.
6. Run only the two static task commands; do not run tests, typecheck, build, server, browser, or native application.
</verification>

<success_criteria>
- G-36.14-2, G-36.14-3, and the history portions of G-36.14-5 through G-36.14-10 have a complete production fix ready for renewed native UAT.
- Exact multi-level Undo/Redo can restore identity-owned child state, including confirmedFrames, across identity-preserving and identity-changing physical commands.
- Stable accepted settlement and complete failure restoration preserve D-09 without moving child-only state into the parent.
- No test, typecheck, build, server, browser, native, package, compatibility, migration, alias, dual-write, or second-authority work occurs.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-21-SUMMARY.md` when done. Record preserved uncommitted fixes, final snapshot/replay interfaces, confirmedFrames coverage, exact acceptance/cursor rules, static verification, and explicit readiness for later gap UAT without claiming native success or phase completion.
</output>
