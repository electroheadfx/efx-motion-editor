---
status: diagnosed
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
source:
  - 36.14-01-SUMMARY.md
  - 36.14-02-SUMMARY.md
  - 36.14-03-SUMMARY.md
  - 36.14-04-SUMMARY.md
  - 36.14-05-SUMMARY.md
  - 36.14-06-SUMMARY.md
  - 36.14-07-SUMMARY.md
  - 36.14-08-SUMMARY.md
  - 36.14-09-SUMMARY.md
  - 36.14-10-SUMMARY.md
  - 36.14-11-SUMMARY.md
  - 36.14-12-PLAN.md
started: 2026-07-23T12:20:02Z
updated: 2026-07-23T21:23:17Z
---

## Current Test

[testing complete]

## Tests

### 1. Physical identity and strict interpolation
expected: In a disposable project, A@1, B@2, and C@5 retain their payloads; enabled interpolation generates only frames 3/4, disabling it empties 3/4 without moving real keys, and Script Motion does not change timing.
result: issue
reported: >-
  Active interpolation is a checkbox that does not show its on/off state and requires two clicks to enable. Generated frames 3/4 render empty instead of showing derived paint. Painting a new key while interpolation is enabled does not create a green key, does not save its cache, loses the key, and breaks interpolation.
severity: blocker
evidence:
  - image-12: interpolation checkbox has no visible pressed/unpressed state
  - image-13: generated-frame canvas is empty
  - image-14: source real-key paint expected in generated frames
  - image-15: newly painted key is not persisted while interpolation is enabled

### 2. Ripple Insert and capacity rejection
expected: From 1,2,3, two Inserts produce exactly 2,3,4 and then 2,4,5 with identity, payload, and selection preserved. Undo/Redo restores each map, and an over-capacity Insert changes nothing and adds no history.
result: issue
reported: "Insert ripple works, but Undo and Redo do not work."
severity: major
passed_observation: Insert ripple mappings work.

### 3. Ripple Delete and deterministic selection
expected: Deleting B from A@1,B@3,C@5 produces A@1,C@4, selects C as the deterministic survivor, preserves C's payload, and behaves identically through toolbar, Delete, and Backspace. Undo/Redo restores complete snapshots.
result: issue
reported: "Delete works perfectly, but Undo and Redo do not work."
severity: major
passed_observation: Delete ripple, deterministic survivor selection, payload preservation, and toolbar/keyboard routing work.

### 4. Drag to an empty or generated cell
expected: From A@1,B@3,C@5,D@8, moving B to physical frame 6 produces exactly A@1,C@4,B@6,D@8, preserves each payload, and selects B. Undo/Redo restores both exact maps.
result: issue
reported: "Drag works only with interpolation disabled. With interpolation enabled, the dropped key returns to its original position instead of remaining at the destination."
severity: major
passed_observation: Drag commits correctly when interpolation is disabled.

### 5. Drag to an occupied boundary
expected: Under D-29, start from A@1,B@3,C@5,D@8 and release B specifically on D's visible `after-key` caret. The occupied-boundary result is exactly A@1,C@5,D@8,B@9: B alone leaves its source slot open, D is not overwritten, every payload stays with its stable keyId, B remains selected, and Undo/Redo restores the exact accepted maps.
result: issue
reported: >-
  The prior release produced A@1,C@4,D@7,B@9, which fingerprints the adjacent direct `physical-cell(9)` target rather than D's occupied `after-key` boundary. Movement Undo/Redo also did not work, and the controls showed incorrect counts: Undo 21 and Redo 0. Renewed Plan 29 native UAT must exercise D's visible after-key caret and compare against A@1,C@5,D@8,B@9.
severity: blocker
evidence:
  - image-16: Undo badge shows 21 while Redo shows 0
current_authority: D-29 resolves the former conflict in favor of no source-gap closure for occupied before/after boundaries only. From A@1,B@3,C@5,D@8, before D is A@1,C@5,B@8,D@9 and after D is A@1,C@5,D@8,B@9. Direct empty/generated `physical-cell` Drag retains its source-closing behavior, including A@1,C@4,B@6,D@8 for frame 6.

<!-- D-30-HISTORICAL-START: Rejected occupied-boundary oracle retained as non-executable evidence -->
historical_rejected_oracle: The former locked Wave 14 occupied result was A@1,C@4,D@7,B@8. Native UAT rejected that product semantic; it is not the current acceptance result.
<!-- D-30-HISTORICAL-END -->

### 6. Force Spacing and validation
expected: From 1,2,5, N=2 produces 1,4,7 and N=0 produces 1,2,3. Every accepted Apply is one history action; invalid values and over-capacity output are complete no-ops, and N does not become persisted timing authority.
result: issue
reported: >-
  Force Spacing works perfectly only when interpolation is disabled. It does not work with interpolation enabled. Generated frames render empty, and Undo/Redo does not work.
severity: major
passed_observation: Force Spacing works correctly when interpolation is disabled.

### 7. Duplicate with occupied ripple
expected: From A@1,B@2,C@4,D@6, Duplicate on B creates a fresh identity at 3 and produces A@1,B@2,dupB@3,C@5,D@7. Every payload remains with its identity, and Undo/Redo restores exact snapshots.
result: issue
reported: >-
  Duplicate works without interpolation. With interpolation enabled it fails with parent rejection: Roto physical revision became stale before commit. Undo/Redo also does not work.
severity: blocker
error_location:
  - PhysicsPaintStudio.tsx:499
  - useRotoPhysicalEditCoordinator.ts:283
  - useRotoPhysicalEditCoordinator.ts:300
  - PhysicsPaintStudio.tsx:920
  - usePhysicsPaintApplyResultController.ts:77
  - usePhysicsPaintParentBridge.ts:235
passed_observation: Duplicate ripple works when interpolation is disabled.

### 8. Paste to a free frame and an existing real key
expected: Paste to a free/generated frame creates a fresh identity exactly at the target and immediately displays the copied payload. Paste to an existing real key preserves its identity/frame and replaces only its payload. The clipboard remains reusable, and Undo/Redo restores exact snapshots.
result: issue
reported: >-
  Paste works except when interpolation is enabled. In that state the parent rejects the edit because the physical revision became stale before commit. Undo works for only one level, and Redo does not work.
severity: blocker
error_location:
  - PhysicsPaintStudio.tsx:499
  - useRotoPhysicalEditCoordinator.ts:283
  - useRotoPhysicalEditCoordinator.ts:300
  - PhysicsPaintStudio.tsx:920
  - usePhysicsPaintApplyResultController.ts:77
  - usePhysicsPaintParentBridge.ts:235
passed_observation: Both Paste forms and immediate free-frame canvas refresh work when interpolation is disabled.

### 9. Atomic Duplicate and Paste rejection
expected: Over-capacity Duplicate and any observable invalidated Paste target change no records, payloads, selection, caches, revision, dirty state, or history. The clipboard remains intact. A branch too fast to induce is explicitly recorded as non-inducible.
result: issue
reported: "All rejection and clipboard behavior works except Undo/Redo."
severity: major
passed_observation: Atomic rejection and reusable clipboard behavior work.

### 10. Pending serialization, rollback, and history
expected: Only one physical operation can be pending. Concurrent actions publish no partial state. An inducible lifecycle failure restores the complete snapshot, or is recorded as non-inducible. Four accepted actions undo and redo exactly, with no history entry for rejections.
result: issue
reported: "All physical actions work when interpolation is disabled. With interpolation enabled they fail, and Undo/Redo does not work."
severity: blocker
passed_observation: Physical actions complete successfully when interpolation is disabled.

### 11. Save, reopen, cache, and live pixels
expected: At least four distinct keys survive save/close/reopen at the same appFrames with their payloads, green markers, and caches. Repainting and moving a key preserves its identity; a second save/reopen does not revive stale pixels.
result: pass
reported: "pass"

### 12. Playback, onion, Preview/Export, and duration
expected: Playback/scrub, onion/reference, Preview, and Export agree frame by frame for real keys, generated interiors, empty frames, alpha, and background. No neighbor is substituted, and duration follows the highest real key rather than stale generated cache.
result: pass
reported: "pass"

### 13. Basic, Physics Paint, and Script preservation
expected: Basic perfect-freehand and Physics Paint preserve rendering, alpha, blend, and background after save/Preview/Export. Copy/Apply Script remains reusable, the durable library works, and Play Script uses the full-height light presentation, preserves guards, deterministic Motion, progress, cancellation, final commit, and generated caches without altering Roto timing.
result: issue
reported: >-
  The tested paint mode is Physics Paint, not p5.brush. Copy/Apply Script works. Play Script has regressed: its panel is constrained to a small-height area with a dark background instead of the previous full-height presentation, and Play Script caches no longer work. The child window also logs an unhandled notification permission rejection because notification.is_permission_granted is allowed only on the main window.
severity: blocker
evidence:
  - image-19: Play Script is constrained inside a short dark panel instead of the prior full-height light presentation
passed_observation: Copy/Apply Script remains reusable.

## Summary

total: 13
passed: 2
issues: 11
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-36.14-1
  truth: "Real keys retain stable payloads, interpolation has a clear enabled state, generated strict-interior frames render their derived paint, and painting a new key while interpolation is enabled persists that real key and cache."
  status: failed
  reason: >-
    Interpolation needs two clicks and has no visible state, generated frames 3/4 render empty, and painting a new key while interpolation is enabled loses the key and cache.
  severity: blocker
  test: 1
  root_cause: >-
    The interpolation control splits authority between child canonical physical interpolation and legacy interpolation settings. The child mutates canonical enabled state locally, the checkbox reads the stale legacy value, and the parent updates only legacy settings. Because canonical physical revision includes interpolation.enabled, subsequent physical edits are rejected as stale. First-paint key/cache publication then cannot obtain an accepted stable keyId. Separately, generated rendering emits a custom base64 string mislabeled as PNG when transient source-canvas registrations are absent, so hydrated generated frames can render empty.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      issue: "Mutates child canonical interpolation but sends a legacy parent command."
    - path: "app/src/stores/physicPaintStore.ts"
      issue: "Maintains split interpolation authorities and an invalid pseudo-PNG generated fallback."
  missing:
    - "Route interpolation changes through the acknowledged canonical physical transaction and bind the control to accepted physical state."
    - "Add an explicit visible enabled/disabled presentation."
    - "Generate valid composited images from canonical source PNG payloads without transient-registry dependence."
  debug_session: ".planning/debug/uat-36-14-interpolation-revision.md"

- gap_id: G-36.14-2
  truth: "Accepted Insert actions create exact complete-map history entries, and Undo/Redo restores each prior and accepted map with identity-owned payloads and selection intact."
  status: failed
  reason: "Insert ripple works, but Undo and Redo do not work."
  severity: major
  test: 2
  root_cause: >-
    The committed coordinator created render-local signal() instances while stable settlement callbacks mutated earlier instances, so history could miss accepted edits and replay acceptance. The current working tree already changes these to hook-stable useSignal(). Independently, successful replay stages only records, interpolation, selection, and provenance instead of the command's complete immutable child snapshot, preventing exact restoration of caches, buffers, reference, engine state, and confirmed frames.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      issue: "Acceptance publication used unstable Signal identities in committed HEAD; successful replay still omits complete target snapshot restoration."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      issue: "Moves history stacks only after observing exact accepted output."
  missing:
    - "Retain the hook-stable useSignal correction."
    - "Replay the complete immutable child snapshot after parent acceptance, including confirmed frames."
  debug_session: ".planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-3
  truth: "Accepted Delete actions create exact complete-map history entries, and Undo/Redo restores the deleted key, survivor positions, payloads, and deterministic selection."
  status: failed
  reason: "Delete ripple and selection work, but Undo and Redo do not work."
  severity: major
  test: 3
  root_cause: >-
    History acceptance could be missed because coordinator Signals were recreated per render while stable callbacks published to older identities. Even when replay is accepted, the child reconstructs from current state rather than applying the stored complete target snapshot, so deleted identity-owned caches, buffers, confirmed frames, reference, and engine state cannot be restored exactly.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      issue: "Unstable acceptance Signals in committed HEAD and incomplete successful replay staging."
    - path: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      issue: "Replay contract and snapshot ports omit complete target state, including confirmedFrames."
  missing:
    - "Use stable coordinator Signals for accepted settlement."
    - "Apply the complete stored before/after child snapshot for Delete Undo/Redo."
  debug_session: ".planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-4
  truth: "Drag commits the locked complete physical map identically whether interpolation is enabled or disabled; generated projection must not restore the moved key to its source position."
  status: failed
  reason: "Drag works with interpolation disabled, but with interpolation enabled the key returns to its original position instead of remaining at the destination."
  severity: major
  test: 4
  root_cause: >-
    Enabling interpolation mutates only the child's canonical physical state while the parent retains canonical enabled=false. The coordinator therefore submits a revision encoded with enabled=true, the parent recomputes a different revision, rejects the Drag, and the coordinator correctly restores the pre-drag snapshot, making the key return to its source.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      issue: "Creates child/parent canonical interpolation divergence."
    - path: "app/src/lib/physicPaintBridge.ts"
      issue: "Legacy interpolation command does not update the parent physical document."
  missing:
    - "Make canonical physical interpolation the single acknowledged authority before Drag staging."
  debug_session: ".planning/debug/uat-36-14-interpolation-revision.md"

- gap_id: G-36.14-5
  truth: "Under D-29, occupied before/after Drag removes only the moved stable identity, leaves its former physical slot open, re-resolves the occupied target by keyId, ripples only at the destination boundary, preserves every identity-owned payload, and records one exact Undo/Redo command with trustworthy availability counts. From A@1,B@3,C@5,D@8, releasing B on D's visible after-key caret must produce A@1,C@5,D@8,B@9."
  status: failed
  reason: >-
    The prior observed A@1,C@4,D@7,B@9 map came from the adjacent direct physical-cell(9) gesture, not D's visible after-key caret. D-29 now makes the occupied-boundary result A@1,C@5,D@8,B@9 while preserving source-closing direct-cell behavior. Movement Undo/Redo was also broken and the controls showed Undo 21 / Redo 0, so the gap remains failed until Plan 29 native UAT verifies the renewed oracle.
  severity: blocker
  test: 5
  root_cause: >-
    The UI correctly publishes different target kinds for D's right-half after-key boundary and adjacent frame 9. Before D-29, the resolver closed the source gap for both paths; the direct physical-cell(9) branch then preserved literal frame 9. D-29 changes only occupied before/after resolution: remove B without compacting survivors, resolve D by stable keyId at frame 8, and insert B at frame 9 with destination-only ripple. History/count failures were separately caused by unstable coordinator acceptance Signals and incomplete snapshot replay.
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      issue: "Adjacent occupied-boundary and direct-cell gestures intentionally publish different target kinds; Plan 29 must exercise D's visible after-key caret."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      issue: "D-29 requires occupied before/after to bypass source closure while physical-cell remains source-closing."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      issue: "History settlement and combined availability require renewed native verification after the accepted recovery fixes."
  missing:
    - "Run the Plan 29 user-owned native scenario by releasing B specifically on D's visible after-key caret and expect A@1,C@5,D@8,B@9."
    - "Verify exact accepted-only movement history and reliable Undo/Redo availability in that scenario."
  debug_session: ".planning/debug/uat-36-14-occupied-drag.md; .planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-6
  truth: "Force Spacing applies the same complete-map result with interpolation enabled or disabled, generated frames render their derived paint, and each accepted Apply is available to Undo/Redo."
  status: failed
  reason: "Force Spacing works only with interpolation disabled; interpolation-enabled execution fails, generated canvases are empty, and Undo/Redo does not work."
  severity: major
  test: 6
  root_cause: >-
    Force Spacing is rolled back under interpolation because child and parent canonical interpolation states encode different physical revisions. Generated frames separately depend on an invalid pseudo-PNG fallback when transient canvas registrations are absent. Undo/Redo additionally misses accepted settlement through unstable coordinator Signals and cannot restore the complete stored target snapshot.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      issue: "Splits canonical interpolation authority."
    - path: "app/src/stores/physicPaintStore.ts"
      issue: "Generated fallback is not a valid PNG."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      issue: "History relies on exact accepted output and incomplete replay state."
  missing:
    - "Unify interpolation under the acknowledged physical transaction."
    - "Produce valid generated composites from canonical PNGs."
    - "Restore stable accepted settlement and complete snapshot replay."
  debug_session: ".planning/debug/uat-36-14-interpolation-revision.md; .planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-7
  truth: "Duplicate commits the same exact ripple semantic with interpolation enabled or disabled, the parent accepts the current physical revision, and the accepted action records exact Undo/Redo history."
  status: failed
  reason: >-
    Duplicate works with interpolation disabled, but interpolation-enabled Duplicate is rejected because the physical revision became stale before commit. Undo/Redo also does not work. The same error likely explains prior interpolation-enabled action failures.
  severity: blocker
  test: 7
  related_gaps: [G-36.14-1, G-36.14-4, G-36.14-6]
  root_cause: >-
    Duplicate itself is valid, but interpolation enablement leaves the child's canonical physical revision ahead of the parent's because the parent receives only a legacy settings update. The parent rejects the subsequent Duplicate as stale. Its history entry and replay are also affected by unstable coordinator acceptance Signals and incomplete child snapshot replay.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      issue: "Locally changes revision-bearing interpolation before parent acknowledgement."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      issue: "Publishes expected revision and accepted output; committed Signal lifecycle was unstable."
  missing:
    - "Commit interpolation through canonical parent/coordinator authority before Duplicate."
    - "Record and replay Duplicate through stable accepted Signals and complete snapshots."
  debug_session: ".planning/debug/uat-36-14-interpolation-revision.md; .planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-8
  truth: "Both Paste forms commit with interpolation enabled or disabled, the parent accepts the current physical revision, the free-frame payload appears immediately, the clipboard remains reusable, and complete Undo/Redo history is available."
  status: failed
  reason: >-
    Paste works with interpolation disabled, but interpolation-enabled Paste is rejected because the physical revision became stale before commit. Undo works for only one level, and Redo does not work.
  severity: blocker
  test: 8
  related_gaps: [G-36.14-1, G-36.14-2, G-36.14-3, G-36.14-7]
  root_cause: >-
    Paste is rejected only after interpolation creates a child/parent canonical revision mismatch through the legacy settings command. The one-level Undo/zero-Redo behavior follows missed accepted replay settlement from render-local coordinator Signals, while exact Paste identity restoration is impossible because successful replay does not apply the stored complete child snapshot.
  artifacts:
    - path: "app/src/lib/physicPaintBridge.ts"
      issue: "Parent validates canonical physical revision that legacy interpolation updates never changed."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      issue: "Replay stacks move only after exact accepted output."
  missing:
    - "Use one canonical acknowledged interpolation authority."
    - "Retain stable coordinator Signals and replay complete identity-owned Paste snapshots."
  debug_session: ".planning/debug/uat-36-14-interpolation-revision.md; .planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-9
  truth: "Rejected Duplicate/Paste work remains mutation-free and history-free, while the prior accepted action remains fully available through exact Undo/Redo."
  status: failed
  reason: "Atomic rejection and clipboard behavior work, but Undo/Redo remains broken."
  severity: major
  test: 9
  related_gaps: [G-36.14-2, G-36.14-3, G-36.14-5, G-36.14-8]
  root_cause: >-
    Rejections are correctly history-neutral. The remaining failure belongs to the prior accepted command: coordinator acceptance could be published to a stale Signal identity and therefore never move history stacks, and successful replay lacks the complete immutable child snapshot required for exact restoration.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      issue: "Committed acceptance Signal identity could diverge across rerenders."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      issue: "Prior accepted command remains stranded when replay acceptance is missed."
  missing:
    - "Preserve rejection neutrality while fixing accepted settlement observation."
    - "Apply complete stored snapshots during accepted replay."
  debug_session: ".planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-10
  truth: "Serialized physical actions commit identically with interpolation enabled or disabled, lifecycle interruption restores the complete pre-operation snapshot, and four accepted operations provide four exact Undo and Redo steps without rejected-history entries."
  status: failed
  reason: "Physical actions work when interpolation is disabled, but interpolation-enabled execution fails and Undo/Redo does not work."
  severity: blocker
  test: 10
  related_gaps: [G-36.14-1, G-36.14-2, G-36.14-3, G-36.14-4, G-36.14-5, G-36.14-6, G-36.14-7, G-36.14-8, G-36.14-9]
  root_cause: >-
    Interpolation-enabled serialization fails before meaningful concurrency validation because child and parent canonical interpolation revisions diverge. History settlement is separately broken by render-local coordinator Signals, and successful replay stages only canonical records/interpolation rather than the stored complete child snapshot. The existing rollback path is broad but confirmedFrames is absent from snapshot/restore ports, so complete lifecycle restoration is not yet structurally guaranteed.
  artifacts:
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      issue: "Interpolation revision mismatch blocks acceptance; Signal identity and successful replay remain incomplete."
    - path: "app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts"
      issue: "Snapshot/restore contract omits confirmedFrames and complete replay target input."
  missing:
    - "Fix canonical interpolation acknowledgement before rerunning pending-operation serialization UAT."
    - "Retain stable accepted Signals and restore the complete immutable replay snapshot, including confirmedFrames."
    - "Reverify lifecycle interruption and one-pending-operation behavior after implementation."
  debug_session: ".planning/debug/uat-36-14-interpolation-revision.md; .planning/debug/uat-36-14-history.md"

- gap_id: G-36.14-13
  truth: "Physics Paint survives save, Preview, and Export; Copy/Apply Script remains reusable; and Play Script uses the full-height light presentation while producing stable generated caches without altering Roto timing."
  status: failed
  reason: >-
    Copy/Apply Script works, but Play Script has regressed to a short dark panel instead of the prior full-height presentation and its generated caches no longer work. The child also logs an unhandled notification permission rejection.
  severity: blocker
  test: 13
  root_cause: >-
    The Play Script dialog is mounted inside the lower split Scripts pane and reuses compact, absolutely positioned, height-constrained dark confirmation styling, which directly produces the short dark surface. Separately, Play Script still commits and mirrors legacy replace-roto-key-frames data even though Phase 36.14 timeline, rendering, persistence, and hydration now use canonical physical records; generated PNGs therefore never become durable keyId/appFrame payloads. The notification rejection is an independent child-window capability defect in the post-export path and does not cause Play Script generation or cache failure.
  artifacts:
    - image-19
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      issue: "Mounts Play Script inside the constrained Scripts pane."
    - path: "app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts"
      issue: "Still submits legacy replace-roto-key-frames."
    - path: "app/src/stores/physicPaintStore.ts"
      issue: "Physical serialization ignores legacy Play Script frame caches."
    - path: "app/src/lib/exportEngine.ts"
      issue: "Queries notification permission from a child window lacking the capability; unrelated to Play Script."
  missing:
    - "Hoist the Play Script surface to a Studio-level full-height light host while preserving its controller and guards."
    - "Publish Play Script output atomically through the canonical physical-map transaction with stable identities and PNG payloads."
    - "Handle notification permission only from an authorized window or deliberately grant the child capability as a separate fix."
  debug_session: ".planning/debug/uat-36-14-play-script.md"
