---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 12
type: execute
wave: 14
depends_on:
  - 36.14-11
files_modified:
  - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt
autonomous: false
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-RIPPLE-INSERT-DELETE
  - 36.14-RIPPLE-DRAG
  - 36.14-FORCE-SPACING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: In the user-owned native application, every distinct real-key payload stays with one identity while its direct physical appFrame changes, interpolation produces every and only strict interior physical frame, and Script Motion remains separate from timing."
    - "D-05/D-06/D-07/D-08: Native Insert, Delete, empty/generated Drag, occupied-boundary Drag, and Force Spacing produce the locked ripple mappings, preserve every surviving identity-owned payload, reject invalid or over-capacity intent without mutation, and never overwrite an occupied target."
    - "D-09: Each accepted physical operation is one acknowledged action; rapid competing input cannot create overlapping mutations, failure or lifecycle cancellation restores the complete pre-operation state, and Undo/Redo replay exact complete mappings without moving history for rejected work."
    - "D-10: Save/reopen, live pixels, playback, onion/reference, preview, export, missing/background rendering, and timeline extent agree on direct physical positions and runtime-derived interiors."
    - "D-10: Basic perfect-freehand, FX p5.brush, Script Copy/Apply reuse, durable Script library behavior, and Play Script remain usable and retain their established payload/render behavior."
    - "D-11/D-12: The cumulative Plan 11 typecheck/build evidence is green before native UAT starts, the agent starts no application process, and no regression artifact or test command is touched before exact user approval."
  artifacts:
    - "36.14-11-SUMMARY.md contains explicit successful evidence for pnpm --dir app typecheck followed by pnpm --dir app build and reports readiness for this native gate without claiming approval."
    - "The user-owned native UAT record covers every scenario and expected mapping in this plan, including any explicitly recorded non-inducible failure branch rather than silently marking it passed."
    - "36.14-12-APPROVAL.txt is absent before the user responds and, after the checkpoint resumes with the exact approval token, contains exactly that one token line as deterministic approval evidence."
    - "36.14-12-SUMMARY.md records either the verified approval-evidence result or the blocking defect report, without changing production, test, validation, roadmap, or state artifacts."
  key_links:
    - "Plan 11 production-gate evidence -> Plan 12 readiness check -> user starts and controls the native application."
    - "Distinct visual payload fingerprint -> stable keyId ownership -> accepted direct appFrame after Insert/Delete/Drag/Force Spacing -> same payload after Undo/Redo and reopen."
    - "Adjacent real physical keys plus interpolation enabled state -> runtime strict interiors -> identical playback/onion/preview/export placement."
    - "Physical edit intent -> one pending acknowledged coordinator -> exact acceptance or complete rollback -> accepted-only history cursor."
    - "Exact approval phrase -> post-checkpoint evidence task -> byte-exact 36.14-12-APPROVAL.txt verification -> Plan 13 becomes eligible, followed by Plans 14-17 in dependency order; regression implementation remains outstanding and VALIDATION wave_0_complete remains false until those regressions land."
  prohibitions:
    - "No production, test, fixture, snapshot, configuration, manifest, lockfile, roadmap, state, validation, or other repository artifact may be created, modified, deleted, or renamed while executing this plan, apart from 36.14-12-APPROVAL.txt created only by Task 3 after the exact user response and the executor-authored 36.14-12-SUMMARY.md after the plan resolves."
    - "No regression test creation, modification, deletion, renaming, generation, or execution; no Vitest, test script, test runner, coverage, watch, snapshot-update, or test-discovery command before exact approval."
    - "Do not rerun typecheck or build in this plan; inspect the cumulative green evidence already produced by Plan 11."
    - "The agent must not start the native application, Vite, Tauri, preview, browser, development server, or any watch process. Native launch, interaction, screenshots, and judgment belong to the user."
    - "No package installation, removal, upgrade, audit, manifest change, lockfile change, or generated dependency artifact."
    - "Approval does not complete regression work, does not complete Phase 36.14, does not approve the final UI, and does not set 36.14-VALIDATION.md wave_0_complete or nyquist_compliant to true."
---

## Phase Goal

**As a** stop-motion animator, **I want to** have every real Roto key, generated in-between, timeline edit, persisted frame, and rendered output use one deterministic physical-frame authority, followed by a polished timeline UI that exposes the approved behavior without duplicating business logic, **so that** editing, reopening, previewing, and exporting the same Roto work cannot disagree about frame identity.

<objective>
Stop at the blocking user-owned native physical-model UAT gate required by D-01 through D-12. First verify that Plan 11 recorded successful cumulative production typecheck/build evidence. Then hand the user exact native scenarios for physical identity and payload stability, strict interpolation, ripple Insert/Delete, empty/generated and occupied Drag, Force Spacing, occupied-adjacent ripple Duplicate, Paste-to-empty/generated, Paste-to-existing, pending-operation serialization, rejection/rollback, accepted-only complete-snapshot Undo/Redo, save/reopen, and rendered-output parity. Perform no implementation or regression work in this plan.

Purpose: Establish direct user evidence that the binary physical-frame cutover behaves correctly in the native product before any regression expectation is authored from it. This preserves D-12's required order and prevents automated tests from canonizing an unapproved physical model.

Output: A resolved blocking checkpoint followed, only on the exact approval response, by a deterministic `36.14-12-APPROVAL.txt` evidence artifact whose byte-exact verification unlocks Plan 13 and then Plans 14-17 in dependency order. A precise blocking defect report keeps all post-UAT plans locked. Recorded UAT approval does not complete regression work or Validation Wave 0.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PLAN-OUTLINE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-06-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-07-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-08-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-09-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md

Plans 01-11 own all production implementation and cumulative production verification. Plan 12 owns no code, test, package, design, or tracking-file change; its only phase-directory write is the exact-token approval evidence created after the human checkpoint approves. The user's normal native launch method is intentionally not prescribed or automated because CLAUDE.md makes native/server startup user-owned.
</context>

<assumptions>
- UAT uses a disposable saved project or disposable duplicate so destructive Insert/Delete/Drag/Force Spacing and reopen checks cannot damage valuable work.
- Distinct visual payload fingerprints identify keys during manual validation because durable keyId is not required to be exposed in the UI. Use unmistakably different paint marks for A, B, C, and D; identity passes only when each exact payload follows its key through movement, rollback, history, reopen, and rendering.
- The authoritative Delete survivor-selection result is the deterministic selectedKeyId documented by the executed Plan 06 resolver and `36.14-06-SUMMARY.md. Before handoff, state that expected survivor explicitly in the UAT record; do not invent a different UI policy in this checkpoint.
- No pre-approval diagnostic failure injector may be added. The native rollback scenario therefore uses an already available user-inducible lifecycle boundary such as immediate project/layer replacement or Physics Paint disposal while an operation is pending. If acknowledgement is too fast to induce any asynchronous failure through existing native behavior, record that exact branch as `NOT INDUCIBLE IN NATIVE UAT`; do not mark it passed. Rejection, timeout, transport, launch-replacement, and disposal branches remain mandatory post-approval regression cases in Plans 13-14.
- The current pre-final-UI surface may not yet have the final Phase 36.14 geometry or presentation polish. This checkpoint judges physical behavior and rendered parity only; Plans 15-18 own final layout, complete drag presentation polish, accessibility, status/LOG polish, and final visual approval.
- Approval is all-or-nothing for this physical-model gate. A partial pass, screenshot, absence of reported defects, or approval of an individual scenario is not approval of Plan 12.
</assumptions>

<scope_boundary>
Included: cumulative Plan 11 gate evidence; user-owned native validation of stable payload identity, direct physical positions, strict runtime interpolation, separate Script Motion, Insert, Delete, empty/generated Drag, occupied-boundary Drag, Force Spacing, occupied-adjacent ripple Duplicate, Paste-to-empty/generated, Paste-to-existing, validation/capacity no-op behavior, pending serialization, user-inducible rejection/rollback, accepted-only history, exact complete-snapshot Undo/Redo, save/reopen, bridge/cache/live-pixel parity, playback, onion/reference, preview/export, missing/background rendering, timeline extent, Basic/FX preservation, approved Script preservation, and post-checkpoint recording of the exact approval token in `36.14-12-APPROVAL.txt`.

Excluded: any implementation change; any regression/test/config/snapshot change or execution; rerunning typecheck/build; starting the application; final timeline visual implementation; final visual approval; multi-selection/group movement; old-project migration or compatibility; editing `36.14-VALIDATION.md`, ROADMAP, STATE, or requirement status.
</scope_boundary>

<dependency_graph>
- Task 1 requires the executed Plan 11 SUMMARY and all prerequisite summaries to exist. It checks cumulative green production evidence and the pre-UAT boundary without running a build, test, or application process.
- Task 2 requires Task 1 to pass. It is the only blocking user-owned native checkpoint and resolves only through exact approval or a defect report.
- Task 3 runs only after Task 2 resumes with the exact approval token. It refuses any other response, creates the phase-local approval evidence, and verifies its exact bytes deterministically.
- Plan 13 remains blocked until Task 3 succeeds and Plan 12 completes; Plans 14-17 then follow their existing dependency order. Approval does not execute them or satisfy their regression obligations.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Verify cumulative production-gate evidence and freeze the pre-UAT boundary</name>
  <files>No repository files; read-only inspection of executed SUMMARY and validation artifacts</files>
  <read_first>
- `36.14-01-SUMMARY.md` through `36.14-11-SUMMARY.md`; confirm all eleven executed summaries exist and no earlier plan is unresolved
- `36.14-11-SUMMARY.md`; locate the exact successful results for `pnpm --dir app typecheck` followed by `pnpm --dir app build`, the production consumer reachability audit, the no-test/package boundary proof, and wording that hands control to Plan 12 without claiming native approval
- `36.14-VALIDATION.md`; confirm the mandatory sequence still places native UAT before every regression change and that `wave_0_complete` and `nyquist_compliant` remain false
- `36.14-CONTEXT.md` D-01 through D-12 and the deferred ideas; use these as the acceptance authority and do not reopen them
- `36.14-PLAN-OUTLINE.md`; confirm Plan 12 depends on Plan 11 and that Plans 13-17 remain post-approval work
  </read_first>
  <action>
Perform read-only evidence inspection. Confirm all eleven prerequisite SUMMARY files exist. In `36.14-11-SUMMARY.md`, verify that app typecheck completed successfully, app build completed successfully after typecheck, the no-test/package audit passed, and Plan 11 stopped ready for user-owned native UAT. A command name without an explicit successful result is not sufficient evidence. If either production gate failed, is absent, is ambiguous, or was followed by an unauthorized regression/test run, stop and report the evidence defect; do not rerun any command and do not open the native checkpoint.

Confirm `36.14-VALIDATION.md` still records `wave_0_complete: false`, `nyquist_compliant: false`, and pending approval. Do not edit those fields. Confirm no Plan 12 action requires production/test/package changes. Freeze the boundary: from this point until exact approval, do not create, modify, delete, rename, discover, or execute any regression artifact, and do not run any test runner, typecheck, build, server, browser, or native launch command.

Prepare a concise handoff containing the green Plan 11 evidence and the scenario checklist from Task 2. The handoff must state that the agent will stop and the user must launch and operate the native application. It must not say that UAT passed, the physical model is approved, regressions are complete, Validation Wave 0 is complete, or the phase is complete.
  </action>
  <acceptance_criteria>
- All eleven prerequisite SUMMARY files exist and report no unresolved dependency that blocks Plan 12.
- Plan 11 records explicit success for `pnpm --dir app typecheck` followed by explicit success for `pnpm --dir app build`.
- Plan 11 records the production reachability and no-test/package boundary checks and stops at native-UAT readiness rather than approval.
- Validation remains pending with `wave_0_complete: false` and `nyquist_compliant: false`.
- No repository file is changed, no production gate is rerun, no test-related action occurs, and no application or server process starts.
  </acceptance_criteria>
  <verify>
    <automated>PHASE_DIR=.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil; for n in $(seq -w 1 11); do test -f "$PHASE_DIR/36.14-$n-SUMMARY.md" || exit 1; done; node -e 'const fs=require("fs"); const p=".planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md"; const s=fs.readFileSync(p,"utf8"); for (const cmd of ["pnpm --dir app typecheck","pnpm --dir app build"]) { const i=s.indexOf(cmd); if (i&lt;0) process.exit(1); const window=s.slice(Math.max(0,i-500),i+700); if (!/(pass(?:ed)?|success(?:ful|fully)?|exit(?: code)?\s*0|green)/i.test(window)) process.exit(1); }'; rg -n '^wave_0_complete: false$|^nyquist_compliant: false$|^\*\*Approval:\*\* pending$' "$PHASE_DIR/36.14-VALIDATION.md"</automated>
  </verify>
  <done>The cumulative production gate is evidenced as green, the pre-UAT no-regression boundary remains intact, and control is handed to the user without starting the native application or claiming approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute and explicitly approve the native physical-model UAT</name>
  <read_first>
- The Task 1 handoff and exact successful Plan 11 typecheck/build evidence
- `36.14-CONTEXT.md` D-01 through D-12 and locked examples in `## Specific Ideas`
- `36.14-06-SUMMARY.md` for actual Insert/Delete action routing and the exact deterministic survivor-selection result
- `36.14-07-SUMMARY.md` for the actual empty/generated and occupied-boundary Drag ports, preview/commit proposal, pending behavior, and focus-follow contract
- `36.14-08-SUMMARY.md` for Force Spacing validation, capacity, history, and status behavior
- `36.14-09-SUMMARY.md` and `36.14-10-SUMMARY.md` for persistence, reopen, bridge/cache, playback, onion/reference, preview/export, missing/background, timeline extent, Basic/FX, and Script preservation
- `36.14-VALIDATION.md` manual gate and post-approval Wave 0 obligations
  </read_first>
  <files>No repository files; user-owned native interaction and checkpoint response only</files>
  <action>
Stop all agent execution and present the complete native checklist below after Task 1 passes. The user alone launches and operates the native application, captures screenshots or LOG evidence, and judges the observed behavior. Do not shorten, sample, automate, or substitute browser behavior for any required scenario. Do not touch production or regression artifacts while waiting.

Classify the response only through the exact state transition in section L. On exact approval, resume only into Task 3 so the token can be recorded and verified; Plans 13-17 remain blocked until that evidence task succeeds. Regression work and Validation Wave 0 remain incomplete. On any defect, ambiguity, partial pass, or non-approval response, keep this checkpoint and all dependent plans blocked, create no approval evidence, and request no code work from inside this plan.
  </action>
  <what-built>Plans 01-11 completed the production-only deterministic physical-frame cutover: stable real-key identity, direct appFrame placement, runtime strict-interior interpolation, one complete-map resolver/coordinator/history route, ripple edits, physical persistence/cache/render consumers, and binary legacy cleanup. Task 1 verified cumulative green typecheck/build evidence. No regression work has begun.</what-built>
  <how-to-verify>
The user owns every step below. The agent must remain stopped and must not launch the app, start a server, drive the UI, or run a test command.

### A. Native UAT preparation

1. Launch the native application using the user's normal local workflow. Do not ask the agent to start it.
2. Create a disposable saved project or a disposable duplicate containing a Physics Paint Roto layer with enough timeline capacity for frame 8 and for an intentionally over-capacity Force Spacing request.
3. Use unmistakably different visual payloads for real keys A, B, C, and D: different shapes, colors, positions, or marks. Record a simple payload fingerprint for each key. These fingerprints are the manual identity oracle.
4. Keep Basic and FX layers available in the same disposable project for preservation checks. Ensure the FX layer continues to use p5.brush rather than substituting another renderer.
5. Record `expected -> actual -> PASS/FAIL` for every numbered scenario. Reset to the named baseline with accepted Undo operations or by reopening a saved baseline copy. Do not infer a pass from a later scenario.
6. If the native surface uses different labels before Plans 15-17, invoke the existing controller-backed equivalent of Insert, Delete, Drag, interpolation enabled, and Force Spacing. Judge physical behavior, not final visual polish.

### B. Physical identity, direct placement, and exact interpolation — D-01/D-02/D-03/D-04

1. Build real keys `A@1, B@2, C@5`, each with its distinct payload. Enable automatic interpolation.
2. Verify frames 1, 2, and 5 remain real keys. Verify adjacent keys A/B generate no cell between frames 1 and 2. Verify frames 3 and 4 are the only generated cells between B and C. Verify there is no generated paint before frame 1 or after frame 5.
3. Disable interpolation. Frames 3 and 4 must become empty without moving A, B, or C and without changing their payloads. Re-enable interpolation; exactly frames 3 and 4 must return as runtime-derived cells.
4. Change the available Script Motion deformation and position settings, then inspect the timeline again. Motion changes must not move real keys, change the number or positions of generated cells, or create timing state.
5. Navigate among all cells. A, B, and C must retain their exact payload fingerprints at direct physical frames; generated cells must not become editable real keys or durable payload owners.

Expected locked result: real keys `1,2,5`; generated frames exactly `3,4` while interpolation is enabled; no leading/trailing interpolation; no real-key movement from interpolation or Motion settings.

### C. Ripple Insert and capacity rejection — D-05/D-09

1. Reset to real keys `A@1, B@2, C@3` with distinct payloads and interpolation in either state.
2. Select A and invoke Insert Frame once. Expected positions are `A@2, B@3, C@4`. No key is created. A remains selected, and all payload fingerprints stay with A/B/C.
3. Select B at frame 3 and invoke Insert Frame once. Expected positions are `A@2, B@4, C@5`. No key is created, and B remains selected.
4. Verify the locked position sequence exactly: `1,2,3 -> 2,3,4 -> 2,4,5`.
5. Prepare a key layout at the physical capacity boundary and invoke Insert where shifting the selected and later keys would exceed capacity. The request must fail before accepted publication: every key, payload, generated cell, selection, and history position remains unchanged.
6. Undo the two accepted Inserts one at a time and verify exact maps `2,4,5 -> 2,3,4 -> 1,2,3`. Redo them one at a time and verify the forward maps. The rejected capacity request must add no Undo step.

### D. Ripple Delete, shared routing, and deterministic selection — D-06/D-09

1. Reset to `A@1, B@3, C@5` with distinct payloads. From `36.14-06-SUMMARY.md`, state the resolver's exact expected selected survivor after deleting B; use that documented identity as the acceptance oracle.
2. Select B and invoke the toolbar Delete action. Expected surviving positions are `A@1, C@4`: B and its payload are gone, the removed physical slot closes, and C shifts left exactly one slot without inheriting B's payload.
3. Verify selection is exactly the deterministic survivor documented by Plan 06. Undo restores `A@1, B@3, C@5`, B's original payload, and the pre-delete selection. Redo restores `A@1, C@4` and the same deterministic survivor selection.
4. Reset and repeat through the Delete keyboard route, then through Backspace where supported by the existing shortcut contract. Each route must produce the same physical map, payload result, selected survivor, acknowledgement, and one history action. Global shortcuts must remain guarded by paint-edit mode.
5. Delete the last or only selected real key in a disposable variant and verify the Plan 06 resolver's documented empty/remaining selection result is deterministic and repeatable; no neighboring payload is substituted for the removed identity.

### E. Empty/generated-cell ripple Drag — D-01/D-02/D-07/D-09

1. Reset to `A@1, B@3, C@5, D@8`, enable interpolation, and confirm the destination at frame 6 is represented as a generated physical cell between C and D.
2. Drag B to the whole physical destination at frame 6. Before release, use the current available preview to identify the proposed moved key and shifted keys; release on the same destination.
3. Expected accepted map is `A@1, C@4, B@6, D@8`: the source slot is cut, later keys close left, B is inserted at direct physical frame 6, keys at or after the insertion boundary shift as required, and no identity is duplicated or lost.
4. Verify A/C/B/D retain their original payload fingerprints, B is selected at frame 6 after acknowledgement, and runtime-generated cells are re-derived from the accepted real-key map.
5. Undo must restore exactly `A@1, B@3, C@5, D@8` with B selected and all payloads intact. Redo must restore exactly `A@1, C@4, B@6, D@8`. One accepted drag equals one history action.

### F. Occupied-boundary ripple Drag — D-07/D-09

1. Reset to the authoritative baseline `A@1, B@3, C@5, D@8`.
2. Drag B to the **after D** occupied-key boundary. The target is D's identity, not a replaceable frame number. D must never be overwritten.
3. Expected locked accepted map is exactly `A@1, C@4, D@7, B@8`. Verify D is still the original frame-8 identity and payload now at frame 7, B's original payload is at frame 8, and A/C payloads remain intact.
4. Verify selection and focus follow B to frame 8 only after matching acknowledgement. Any scrolling must merely reveal B and must not change the map.
5. Undo restores exactly `A@1, B@3, C@5, D@8`; Redo restores exactly `A@1, C@4, D@7, B@8`. The occupied target is never erased, replaced, or assigned B's payload.

### G. Force Spacing validation and complete-map behavior — D-08/D-09

1. Reset to `A@1, B@2, C@5`. Enter `N = 2` and invoke the explicit Apply action. Expected map: `A@1, B@4, C@7`. The first key remains anchored and there are exactly two physical slots between each adjacent real key.
2. Undo to `1,2,5`, enter `N = 0`, and Apply. Expected map: `A@1, B@2, C@3`; adjacent keys are valid and no generated interior exists between them.
3. Verify each accepted Apply is exactly one history action and preserves each payload fingerprint.
4. Try every invalid value the current input can represent without changing code: negative integer, fractional value, empty value, nonnumeric text if accepted by the field, and an integer outside the supported range. Each invalid request must leave the complete map, payloads, selection, generated cells, caches, and history cursor unchanged.
5. Enter a valid nonnegative integer large enough that the resulting complete map exceeds physical capacity. Apply must fail before accepted publication and leave the same complete state and history cursor unchanged.
6. Verify the last entered N may remain for the current native window/session but saving and reopening the project does not treat N as project timing truth. Only the accepted direct key positions and interpolation enabled state persist.

### G1. Duplicate with occupied-adjacent ripple — D-01/D-09

1. Reset to unmistakable payload fingerprints `A@1, B@2, C@4, D@6`. Record the complete identity/payload/frame state and select B.
2. Invoke Duplicate once. The accepted destination must be exactly B's accepted source frame plus one: the new duplicate is at frame 3. Existing C and D, because they are at or after destination 3, must shift exactly one physical frame to `C@5, D@7`; A remains `A@1`; source B remains `B@2` with its original payload.
3. Verify the duplicate has one fresh identity distinct from A/B/C/D and an immutable clone of B's paint retargeted to frame 3. Verify shifted C/D retain their original identities and unmistakable payload fingerprints, including any canonical payload-frame placement required by their accepted frames. No existing key may receive B's identity or paint.
4. Compare the entire accepted state, not only visible positions: exact real-key records, all paint payloads, selection/focus, interpolation-derived cells, caches/live pixels, dirty state, and accepted revision must agree with the one-ripple result. The composition must not expose or use a second destination calculation.
5. Undo once and verify the exact complete prior state `A@1, B@2, C@4, D@6` with every original payload and identity. Redo once and verify the exact complete accepted state `A@1, B@2, duplicate-of-B@3, C@5, D@7`, including the same fresh duplicate identity and every payload.

### G2. Paste to empty/generated target and existing real key — D-01/D-09

1. Copy an unmistakable immutable paint payload P from a real key, then retain the clipboard for repeated use. Reset a target layout with an eligible empty or runtime-generated frame 3 and existing real keys on other frames. Record the complete pre-Paste state.
2. Paste to frame 3. Verify exactly one fresh destination identity is created at the exact requested physical frame 3, its payload is an immutable clone of P retargeted to frame 3, and every pre-existing identity, physical frame, and paint payload is semantically unchanged. No timing ripple or clipboard consumption occurs.
3. Undo and verify the exact complete pre-Paste state and every payload; Redo and verify the exact complete accepted fresh-identity state and the same immutable payload clone.
4. Reuse the still-available clipboard on an existing real destination with a distinct identity and payload fingerprint Q. Record the complete state, then Paste onto that real key. Verify the destination keeps its existing identity and physical frame, only its payload changes from Q to an immutable clone of P retargeted to that frame, and all other records remain semantically unchanged.
5. Undo and verify the exact complete Q-bearing prior state; Redo and verify the exact complete accepted P-bearing replacement state. Repeated Paste must not consume or mutate the clipboard.

### G3. Duplicate/Paste rejection and history atomicity — D-09

1. Place the selected source so Duplicate's required `sourceAppFrame + 1` insertion and resulting ripple would exceed physical capacity. Invoke Duplicate and verify no visible or hidden mutation: records, identities, payloads, generated cells, caches/live pixels, selection/focus, dirty state, accepted revision, and history cursor remain byte-for-byte/semantically equal to the captured prior state.
2. Verify Undo immediately targets the last earlier accepted action rather than the rejected Duplicate; the rejection contributes no history entry.
3. If the existing native surface permits a stale Paste target to be induced without code changes, copy P, begin Paste, and replace the active project/layer or otherwise invalidate the target before acknowledgement. Verify no accepted mutation and no history entry. If acknowledgement settles too quickly, record the existing exact line `NOT INDUCIBLE IN NATIVE UAT — acknowledgement settled before lifecycle action`; do not mark this branch passed and assign stale/rejected Paste settlement explicitly to Plans 13-14 regression evidence.
4. Any identity, frame, capacity, overflow, stale-target, or acknowledgement rejection that is observable must be atomic and must leave the reusable clipboard unchanged.

### H. One-pending-operation serialization, rollback, and accepted-only history — D-09

1. On a layout with several keys, invoke one physical edit and immediately attempt a second Insert/Delete/Drag/Force Spacing/Undo/Redo action before the first acknowledgement settles. Repeat with rapid mixed inputs if acknowledgement is visible long enough.
2. Verify at most one physical operation owns pending state. The second action is blocked or serialized by the established controller; it must not publish an overlapping partial map, duplicate history, transfer payloads, or leave selection/cache state between proposals.
3. Induce one existing native lifecycle failure without modifying code: begin a physical operation and immediately replace the active project/layer or dispose/close the Physics Paint native window before settlement. Reopen the affected project/layer.
4. If the operation did not accept before the lifecycle boundary, verify complete rollback to the immutable pre-operation state: real-key map, payloads/editable state, generated cells, caches/live pixels, dirty state, selection/focus, interpolation enabled state, relevant Motion/reference state, and history cursor all match the snapshot. No accepted operation/status/history entry may survive.
5. If native acknowledgement always completes before a user can induce launch replacement/disposal, record `NOT INDUCIBLE IN NATIVE UAT — acknowledgement settled before lifecycle action` with the attempted steps. Do not label rejection, timeout, transport failure, launch replacement, or disposal as manually passed. This explicit limitation remains required regression coverage in Plans 13-14 and is not silently omitted.
6. Trigger a validation rejection through invalid or over-capacity Force Spacing and through over-capacity Insert. Verify both are mutation-free and do not advance Undo/Redo.
7. Execute a sequence of four accepted actions—Insert, Delete, Drag, Force Spacing—recording every complete map. Undo four times and compare every reverse state exactly; Redo four times and compare every forward state exactly. A rejected attempt inserted between accepted actions must not appear in the history sequence.

### I. Save/reopen, bridge/cache ownership, and live-pixel stability — D-01/D-09/D-10

1. From a nontrivial accepted layout containing at least four distinct real keys and interpolation enabled, repaint or edit one real key so live pixel/cache publication is exercised. Wait for the native UI to show accepted completion.
2. Save the project, close the disposable project or native Physics Paint window normally, and reopen it.
3. Verify every real key reopens at the exact accepted direct appFrame with the same payload fingerprint. Selection may restore only according to the established contract; no frame collision may transfer payload, dirty, cache, or editable ownership to another key.
4. Verify interpolation reopens as enabled/disabled only and re-derives every strict interior from current adjacent real keys. No obsolete count, spacing override, generated timing record, or nearest-owner behavior may change the reopened map.
5. Repaint a moved key after reopen. Verify the edit applies to that same identity at its current physical frame and does not alter another key that previously occupied that frame.
6. Move a key again, save, and reopen a second time. Confirm bridge revision, accepted cache publication, project dirty/save state, and visible pixels converge on the latest accepted complete map with no stale pre-move pixels appearing under another identity.

### J. Playback, onion/reference, preview/export, missing/background, and timeline extent — D-02/D-10

1. Use a layout with adjacent real keys, at least one wider interpolated gap, at least one empty gap with interpolation disabled in a comparison pass, and distinct transparent paint over a visible background.
2. Scrub and play through every real, generated, and empty physical frame. Real payloads must appear at their direct positions; generated output must exist only at strict interiors while enabled; empty frames must not borrow a neighboring key's paint.
3. Inspect onion/reference behavior around real and generated cells. It must resolve the correct adjacent physical identities and must not show a stale source/display owner after movement, Undo/Redo, or reopen.
4. Generate Preview and Export for the same saved accepted state. Compare frame by frame with native timeline playback: real-key positions, generated interiors, empty/missing frames, alpha paint, paper/background composition, and final duration must agree.
5. Verify missing/background frames render the intended background separately from alpha-only paint. A missing cached frame must not substitute another key's payload or become durable generated timing truth.
6. Verify timeline extent is derived from the highest accepted real-key physical frame plus the established terminal-frame rule documented by Plan 10. A stale generated cache, prior key position, or disabled interpolation gap must not extend or shorten output.
7. Repeat Preview/Export after one accepted Drag and after Undo. Each output must match the corresponding accepted physical map exactly, with no stale render from the other revision.

### K. Preserved Basic, FX, and Script workflows — D-04/D-10

1. Draw or inspect Basic perfect-freehand paint and FX p5.brush paint in the disposable project. Save/reopen, Preview, and Export. Verify both retain their established brush, opacity/alpha, blend, background, and layer behavior; the physical Roto cutover must not replace or alter either paint path.
2. Copy a Roto Script from a known real-key range and Apply it to two valid physical targets in succession. The clipboard must remain immutable and reusable, each Apply must resolve current physical identities, and no application may consume or retarget the clipboard through an obsolete coordinate.
3. Load a durable Script-library preset explicitly into the clipboard, confirm its thumbnail/library lifecycle remains intact, and Apply it to a physical target. Existing scan/save/load/rename/delete behavior must remain available according to the approved workflow.
4. Run Play Script on a saved disposable project. Verify the saved-project/capacity guard, progressive output, deterministic held-pose deformation/position Motion, alpha merge, cancellation/revision behavior, progress/status, and final complete parent commit remain functional. Motion must not rewrite real-key spacing or interpolation timing.
5. Reopen after accepted Script work and compare playback, Preview, and Export. Script-produced paint and Motion results must resolve on the accepted physical map without source/display timing drift.

### L. Result classification and exact state transition

- Mark the checkpoint **rejected** if any required expected mapping differs, any payload changes identity, an occupied key is overwritten, a rejected operation mutates state/history, Undo/Redo is not exact, save/reopen drifts, or native playback/Preview/Export disagree. Report the first failing scenario plus all additional observed failures.
- A failure branch explicitly recorded as non-inducible is not silently passed. Its unobserved statuses must remain named in the UAT record and in Plans 13-14 regression scope.
- Do not approve based only on typecheck/build, partial scenarios, or absence of visible errors.
- Exact approval phrase: `APPROVED 36.14-12: native physical-model UAT passed`
- Exact rejection prefix: `REJECTED 36.14-12:` followed by scenario IDs, expected result, actual result, reproduction steps, and screenshots/log excerpts if available.
- Only the exact approval phrase resolves this human gate as approved and permits Task 3 to run. It does not directly unlock Plan 13. Plan 13 becomes eligible only after Task 3 records and deterministically verifies `36.14-12-APPROVAL.txt`; Plans 14-17 then follow their declared dependency order. Plan 13 must still create and run the approved physical regressions; Plan 14 must still create and run downstream regressions and the full suite; Plans 15-17 must still implement and validate the final UI.
- Recorded approval evidence does **not** set `36.14-VALIDATION.md` `wave_0_complete: true`. That field becomes true only after the required post-approval regression coverage has landed and passed. Approval evidence also does not set `nyquist_compliant: true`, approve Plan 18, or complete Phase 36.14.
  </how-to-verify>
  <acceptance_criteria>
- Task 1 established explicit green cumulative typecheck/build evidence before the user launched native UAT.
- The user executed and recorded every scenario B-K plus G1-G3 against a disposable native project, using distinct payload fingerprints as the stable-identity oracle.
- Locked maps pass exactly: interpolation `1,2,5 -> generated 3,4 only`; Insert `1,2,3 -> 2,3,4 -> 2,4,5`; empty/generated Drag `A@1, B@3, C@5, D@8 -> A@1, C@4, B@6, D@8`; occupied B-after-D Drag `A@1, C@4, D@7, B@8`; Force Spacing 2 `1,2,5 -> 1,4,7`; Force Spacing 0 makes keys adjacent.
- Delete removes only the selected identity, shifts later survivors one slot left, preserves all survivor payloads, uses the Plan 06 deterministic selection result, and behaves identically through toolbar and supported keyboard routes.
- Duplicate creates exactly one fresh identity at `sourceAppFrame + 1`, keeps the source and earlier records unchanged, ripples every existing record at or after the destination exactly `+1` with identity/payload ownership preserved, and round-trips the exact complete before/after state through Undo/Redo.
- Paste-to-empty/generated creates one fresh identity at the exact requested physical target with an immutable clipboard clone and no unrelated change; Paste-to-existing preserves destination identity/frame, replaces only its payload, and both forms round-trip exact complete snapshots through Undo/Redo while keeping the clipboard reusable.
- Invalid and over-capacity actions are complete no-ops; at least one rejected Duplicate/Paste branch is mutation-free and history-free; rapid competing operations do not overlap; one user-inducible timing failure restores complete state, or the exact inability to induce that timing branch is explicitly recorded without marking it passed and assigned to Plans 13-14.
- Each accepted physical operation is one history action; rejected work adds none; multi-action Undo/Redo restores every recorded complete map and identity-owned payload exactly.
- Save/reopen, bridge/cache/live pixels, playback, onion/reference, Preview, Export, missing/background rendering, and timeline extent agree with the accepted physical map and runtime interpolation.
- Basic perfect-freehand, FX p5.brush, Script Copy/Apply reuse, durable Script library, and Play Script behavior remain intact.
- No agent-started native/server process, repository edit, regression/test action, typecheck/build rerun, package action, or Validation state edit occurs before approval.
- The checkpoint resolves only with the exact approval phrase or remains blocked with an exact defect report.
  </acceptance_criteria>
  <verify>
    <human-check>Complete scenarios A-L plus G1-G3 and respond with exactly `APPROVED 36.14-12: native physical-model UAT passed`, or with `REJECTED 36.14-12:` plus failing scenario IDs and evidence.</human-check>
  </verify>
  <done>The user has either explicitly approved the complete native physical-model UAT with the exact phrase, authorizing only the deterministic evidence task while Plans 13-17 remain locked, or has supplied a blocking defect report and all later plans remain locked.</done>
  <resume-signal>Respond exactly `APPROVED 36.14-12: native physical-model UAT passed` to approve, or begin with `REJECTED 36.14-12:` and include scenario IDs, expected versus actual behavior, and reproduction evidence.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record and verify exact physical-model UAT approval evidence</name>
  <files>.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt</files>
  <read_first>
- The resumed Task 2 checkpoint response; treat the complete user response as the only authority for this transition
- `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md`; confirm status remains draft and both Nyquist flags remain false
  </read_first>
  <action>
Run this task only after the blocking checkpoint resumes. Compare the complete user response byte-for-byte with `APPROVED 36.14-12: native physical-model UAT passed`. If it differs in any character, contains extra prose, uses the rejection prefix, or is absent, stop immediately, leave Plan 13 blocked, and do not create or modify the evidence file.

After exact equality is established from the actual resumed human response, first require that `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` does not already exist; an unexpected pre-existing file is a blocking evidence-integrity defect and must not be overwritten. Then create that file with exactly one line containing the approved token and one trailing newline. Do not derive the token from this plan, a SUMMARY, command history, or agent prose, and do not create evidence speculatively before the user response.

Run the deterministic byte comparison in `<verify>`. Only a passing comparison completes Plan 12 and unlocks Plan 13. Leave `36.14-VALIDATION.md` unchanged: `status: draft`, `wave_0_complete: false`, and `nyquist_compliant: false` remain required until the later regression and final-acceptance gates land.
  </action>
  <acceptance_criteria>
- The evidence file was absent before the exact user approval response and was created only after the checkpoint resumed with that complete exact response.
- The file contains exactly `APPROVED 36.14-12: native physical-model UAT passed` followed by one newline and no other bytes.
- Plan 13 remains blocked unless the deterministic comparison passes.
- No production, test, configuration, package, roadmap, state, requirements, or validation file changes, and no test/build/server/native-launch command, occur in this task.
  </acceptance_criteria>
  <verify>
    <automated>PHASE_DIR=.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil; test -f "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - "$PHASE_DIR/36.14-12-APPROVAL.txt"</automated>
  </verify>
  <done>The exact human approval response is recorded as byte-exact phase-local evidence; its deterministic check passes, Plan 12 can complete, and Plan 13 is now eligible while all regression and final-UAT obligations remain outstanding.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path or record | Artifact | Required interface or behavior |
|---|---|---|
| `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md` | Read-only cumulative production-gate evidence | Must already record successful app typecheck/build, production reachability/no-test proof, and Plan 12 readiness without native approval. |
| User-owned native UAT record | Physical-model acceptance evidence | Records expected/actual/pass-fail for scenarios B-K plus G1-G3, distinct key payload fingerprints, exact locked maps including Duplicate and both Paste forms, complete-snapshot Undo/Redo, mutation/history-free rejection, rollback induction result or explicit non-inducibility, and downstream parity observations. It is supplied through the checkpoint response, not created by the agent as a repository document. |
| `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` | Deterministic approval evidence | Created only after the exact human response; contains exactly one approval-token line and must pass the byte comparison before Plan 13 becomes eligible. |
| `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md` | Checkpoint resolution | Created by the normal executor after Task 3 succeeds or after a blocking rejection; records the approval-evidence result or defects, confirms no pre-approval regression work, and states the correct next-plan/Validation status without claiming phase completion. |
| Exact approval phrase | Post-UAT transition token | `APPROVED 36.14-12: native physical-model UAT passed` authorizes Task 3; only its verified evidence artifact unlocks Plan 13, followed by Plans 14-17 in dependency order. It does not complete regressions, Validation Wave 0, final visual UAT, or Phase 36.14. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 11 SUMMARY -> Plan 12 readiness | Planning evidence can be absent, ambiguous, or falsely interpreted; native UAT must not start without explicit successful cumulative production gates. |
| User gesture -> physical resolver/coordinator -> parent acknowledgement | Native input and asynchronous settlement cross process/state boundaries; stale identity, overlapping operations, or mismatched acknowledgement must not become accepted state. |
| Accepted physical state -> history/cache/selection/render consumers | Identity-owned payload and complete physical placement must remain consistent across mutable UI, caches, persistence, and rendered output. |
| Project save/bridge payload -> reopen hydration | Persisted or transported state can be stale, partial, malformed, or from another launch/revision; native UAT must expose drift rather than accepting partial recovery. |
| Native UAT result -> exact-token evidence -> post-approval plan eligibility | Partial success, ambiguous wording, or an unverified/pre-existing evidence file must not unlock test creation and final UI work. |
| Pre-approval repository -> regression toolchain | Test changes or execution before approval would violate the user-owned truth-establishment sequence and could canonize incorrect behavior. |

## Abuse Cases — OWASP ASVS Level 1

| Abuse case | Expected disposition |
|---|---|
| A summary mentions typecheck/build commands but does not prove successful completion. | Keep the checkpoint closed; do not rerun commands or infer success. |
| A frame collision attempts to make one key inherit another key's paint, cache, dirty state, or Script target. | Fail UAT; stable payload fingerprints must follow identity through every accepted map. |
| A stale or mismatched acknowledgement appears after project/layer replacement or disposal. | It must not accept; the complete pre-operation snapshot and history cursor remain authoritative. |
| Rapid input attempts to publish two overlapping complete maps. | One operation owns pending state; later actions are blocked/serialized without partial mutation. |
| Generated output or stale cache attempts to establish timing, selection ownership, or duration. | Fail UAT; generated cells come only from current adjacent real keys and current revisions. |
| Partial UAT success is phrased as general approval. | Do not record evidence or unlock later plans; require the exact approval phrase after all required scenarios. |
| Approval evidence exists before the exact checkpoint response or differs by any byte. | Treat it as an integrity defect; do not overwrite it and keep Plan 13 blocked. |
| Approval is used to mark Validation Wave 0 complete before regressions exist. | Reject the transition; verified approval evidence only unlocks post-UAT work. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-12-01 | Spoofing | Plan 11 gate evidence | high | mitigate | Require all prerequisite summaries plus explicit nearby successful outcomes for typecheck and build; command-name presence alone is insufficient. |
| T-36.14-12-02 | Spoofing | Real-key ownership | high | mitigate | Use distinct native payload fingerprints and verify the same payload follows the moved stable identity through edits, history, reopen, and renders. |
| T-36.14-12-03 | Tampering | Complete physical mapping | high | mitigate | Compare every accepted operation with the locked full-map results; invalid/capacity failures must leave map, payload, selection, cache, and history untouched. |
| T-36.14-12-04 | Tampering | Parent acknowledgement and rollback | high | mitigate | Exercise one-pending serialization and an existing lifecycle failure when inducible; require complete restoration and explicitly record any non-inducible branch for Plans 13-14. |
| T-36.14-12-05 | Repudiation | Native UAT result and approval evidence | medium | mitigate | Require scenario-indexed expected/actual evidence, one exact approval or rejection prefix, creation only after the resumed human response, and byte-exact evidence verification before Plan 13 eligibility. |
| T-36.14-12-06 | Information Disclosure | Project/log evidence | low | accept | Use a disposable project and share only scenario evidence necessary to diagnose failures; no external service or credential is involved. |
| T-36.14-12-07 | Denial of Service | Rapid physical operations and large spacing | medium | mitigate | Verify pending serialization and bounded capacity rejection; do not use unbounded values beyond the single over-capacity no-op check. |
| T-36.14-12-08 | Elevation of Privilege | Pre-approval regression boundary | high | mitigate | No regression artifact or runner may be touched; exact approval permits only Task 3, and its verified phase-local evidence is the sole transition that makes Plan 13 eligible. |
| T-36.14-12-09 | Tampering | Persistence/cache/render parity | high | mitigate | Compare accepted maps and payloads after save/reopen across playback, onion/reference, Preview, Export, missing/background, and timeline extent. |
| T-36.14-12-SC | Tampering | pnpm package supply chain | low | accept | This plan performs no package installation, audit, manifest/lockfile change, typecheck/build rerun, or test execution; it only reads existing evidence. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One deterministic physical-frame authority agrees across editing, reopening, preview, and export | Native scenarios B-J compare exact physical maps, identity-owned payloads, persistence, and render consumers | COVERED |
| GOAL | Stop for user-owned native UAT before regressions and final UI | Task 2 is blocking; exact approval permits Task 3, and only byte-exact recorded evidence unlocks Plan 13 | COVERED |
| REQ | `36.14-PHYSICAL-IDENTITY` | Scenarios B-I plus G1-G2 validate stable payload identity, fresh-versus-preserved key ownership, direct placement, and exact complete-state history/reopen behavior | COVERED |
| REQ | `36.14-DERIVED-INTERPOLATION` | Scenarios B, E, G, I, and J validate exact strict interiors, adjacency, disable/enable, and no timing persistence | COVERED |
| REQ | `36.14-ATOMIC-FRAME-MAPPING` | Scenarios C-H plus G1-G3 validate complete maps, Duplicate ripple, both Paste forms, validation no-op, acknowledgement serialization, rollback, accepted-only history, and exact complete-snapshot Undo/Redo | COVERED |
| REQ | `36.14-RIPPLE-INSERT-DELETE` | Scenarios C-D use locked Insert sequence, capacity rejection, Delete ripple, shared shortcuts, and survivor selection | COVERED |
| REQ | `36.14-RIPPLE-DRAG` | Scenarios E-F cover generated whole-cell destination and locked occupied B-after-D boundary mapping | COVERED |
| REQ | `36.14-FORCE-SPACING` | Scenario G covers N=2, N=0, invalid values, capacity rejection, session-only input, and one history action | COVERED |
| REQ | `36.14-DOWNSTREAM-PARITY` | Scenarios I-K cover reopen, cache/live pixels, playback, onion/reference, Preview/Export, missing/background, extent, Basic/FX, and Scripts | COVERED |
| REQ | `36.14-UAT-THEN-REGRESSION` | Plan-wide prohibition, exact approval token, post-response evidence task, deterministic byte check, and Wave 0 transition wording enforce the sequence | COVERED |
| RESEARCH | One resolver, coordinator, pending operation, exact acknowledgement, and accepted-only history | Scenarios C-H plus G1-G3 validate observable native outcomes for Duplicate, both Paste forms, accepted-only complete snapshots, and rejected/stale rollback without adding test/diagnostic code | COVERED |
| RESEARCH | Fail-closed rejection/timeout/transport/launch/disposal restoration | Scenario H exercises an existing user-inducible lifecycle path and explicitly records unobservable branches for mandatory post-approval regressions | COVERED WITHOUT SILENT OMISSION |
| RESEARCH | Persistence, cache, downstream render, and duration parity | Scenarios I-J | COVERED |
| RESEARCH | Preserve Basic, FX, Script reuse/library/Play | Scenario K | COVERED |
| RESEARCH | Preact/Signals architecture and no presentation-owned business logic | Already produced/audited by Plans 01-11; Plan 12 makes no implementation change | COVERED BY DEPENDENCY EVIDENCE |
| CONTEXT | D-01 stable keyId and physical appFrame | Scenarios B-I | COVERED |
| CONTEXT | D-02 enabled-only exact strict interiors | Scenarios B, E, G, I, J | COVERED |
| CONTEXT | D-03 binary removal of old timing authority | Task 1 Plan 11 audit evidence plus reopen/render observations | COVERED |
| CONTEXT | D-04 separate Script Motion | Scenarios B and K | COVERED |
| CONTEXT | D-05 Insert ripple | Scenario C | COVERED |
| CONTEXT | D-06 Delete ripple | Scenario D | COVERED |
| CONTEXT | D-07 cut-and-insert Drag | Scenarios E-F | COVERED |
| CONTEXT | D-08 Force Spacing | Scenario G | COVERED |
| CONTEXT | D-09 complete acknowledged transaction and rollback/history | Scenarios C-H plus G1-G3, including exact Duplicate ripple, both Paste deltas, accepted-only history, and complete-snapshot Undo/Redo | COVERED |
| CONTEXT | D-10 downstream physical consumers | Scenarios I-K | COVERED |
| CONTEXT | D-11 bounded sequential plan chain | Wave 12 depends only on completed Plan 11 and owns no shared production files | COVERED |
| CONTEXT | D-12 production -> build -> native UAT -> regressions | Task 1, exact human checkpoint, and verified post-response approval evidence before Plan 13 | COVERED |
| CONTEXT | Deferred multi-selection/group movement | No group selection or movement is tested or added | EXCLUDED — DEFERRED |
| CONTEXT | Deferred old-project migration/compatibility | UAT uses a new disposable physical-format project; no legacy fixture or migration is added | EXCLUDED — DEFERRED |
| UI-SPEC | Final visual layout, guarded controls, selection policy, and status/LOG polish | Explicitly retained for Plans 15-18; this checkpoint judges physical behavior only | EXCLUDED — LATER PHASE WORK WITHIN 36.14 |
| VALIDATION | Typecheck/build before native UAT | Task 1 verifies Plan 11 evidence without rerunning | COVERED |
| VALIDATION | No regression work before explicit approval | Frontmatter prohibitions, Tasks 1-3, checkpoint token, deterministic evidence gate, and threat controls | COVERED |
| VALIDATION | Wave 0 only after approval and regressions land | Exact state-transition wording leaves flags false at approval | COVERED |

## Edge-Case Coverage

| Edge | Required native result |
|---|---|
| Zero or one real key | No generated cell; deleting the only key leaves the deterministic empty selection state and no borrowed payload. |
| Adjacent keys | Valid direct positions with zero strict interior cells. |
| Interpolation toggle | Changes only runtime generated-cell availability; never moves real keys or Motion settings. |
| Insert at capacity | Entire request rejected without map, payload, selection, cache, invalidation, or history change. |
| Delete first/last/only key | Exact survivor set and Plan 06 deterministic selection; no payload substitution. |
| Drag to generated/empty cell | Direct whole-cell destination, complete cut-and-insert map, preview/commit agreement, identity payload preservation. |
| Drag after occupied D | Exact locked result `A@1, C@4, D@7, B@8`; D survives and is not overwritten. |
| Force Spacing 0 | First key anchored and all ordered keys adjacent. |
| Invalid/over-capacity Force Spacing | Mutation-free and history-free rejection. |
| Duplicate beside occupied later keys | Destination is exactly source + 1; all existing records at/after it ripple +1; source/earlier records stay unchanged; one fresh immutable source-paint clone is added. |
| Duplicate at capacity | Atomic mutation-free and history-free rejection; source, neighbors, payloads, selection, caches, revision, and clipboard remain unchanged. |
| Paste to empty/generated | One fresh identity at the exact target with immutable clipboard paint; no unrelated timing, identity, payload, or history change. |
| Paste to existing | Destination identity/frame remain fixed; only destination payload changes; all other records remain semantically unchanged. |
| Duplicate/Paste Undo/Redo | One accepted action restores exact complete before/after snapshots and every payload in both directions. |
| Rapid mixed actions | One pending owner; no overlapping partial maps or duplicated history. |
| Lifecycle failure before acceptance | Full snapshot restoration; if not user-inducible, explicitly record the limitation for post-approval regressions. |
| Rejected action amid accepted history | Rejected action has no Undo/Redo entry and does not move the cursor. |
| Save/reopen after move/repaint | Same identity owns same payload at latest accepted direct frame; stale pixels do not transfer. |
| Missing/stale generated cache | No cell, duration, selection, or neighboring paint is inferred from it. |
| Preview/Export after edit then Undo | Each render matches its own accepted revision and exact physical map. |
| Basic and FX beside Roto | Existing perfect-freehand and p5.brush behavior remains unchanged. |
| Repeated Script Apply | Clipboard remains immutable/reusable and each current physical target resolves independently. |

<verification>
1. Read all prerequisite summaries and verify explicit successful Plan 11 typecheck/build evidence; do not rerun either command.
2. Confirm Validation remains pending and Wave 0 incomplete; do not edit it.
3. Freeze every code/test/package/tracking-file boundary and stop all agent-owned launch behavior.
4. Hand scenarios A-L plus G1-G3 to the user and wait at the blocking checkpoint.
5. Accept only the exact phrase `APPROVED 36.14-12: native physical-model UAT passed` after all scenarios are complete; every other response keeps the evidence file absent and Plan 13 blocked.
6. On rejection, retain the gate and record exact scenario IDs, expected/actual behavior, reproduction, and available native evidence.
7. After exact approval, run Task 3, require the evidence file to have been absent, record the one-line token, and verify exact bytes with `printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt`.
8. Only after that command passes, state that Plan 13 is unlocked, followed by Plans 14-17 in dependency order; regression work, Validation Wave 0, final visual UAT, and phase completion remain outstanding.
</verification>

<success_criteria>
- Cumulative Plan 11 production gates are explicitly evidenced as green before native UAT.
- The agent changes no repository artifact, runs no test/build/typecheck/package command, and starts no server/native/browser process.
- User-owned native UAT covers every required physical operation, including exact occupied-adjacent Duplicate ripple, Paste-to-empty/generated, and Paste-to-existing; every locked mapping, identity/payload invariant, rejection/capacity case, serialization/rollback path, accepted-only complete-snapshot Undo/Redo sequence, persistence/reopen path, and downstream rendered consumer is recorded.
- Any asynchronous failure branch that cannot be induced without code changes is named explicitly rather than silently passed and remains mandatory in post-approval regressions.
- Basic perfect-freehand, FX p5.brush, Script Copy/Apply, durable Script library, and Play Script remain intact.
- Exact approval authorizes only the evidence task; the byte-exact `36.14-12-APPROVAL.txt` check must pass before Plan 13, then Plans 14-17 in dependency order, can proceed.
- Verified evidence does not execute later plans or mark regression obligations, Validation Wave 0, final visual approval, or Phase 36.14 complete.
- Any defect, non-exact response, pre-existing evidence file, or failed byte comparison keeps Plan 12 and every dependent plan blocked.
</success_criteria>

<output>
After the blocking checkpoint resolves, run Task 3 only for the exact approval response and create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` with the exact one-line token. Then create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md` through the normal execution workflow. Record Task 1 evidence, the user's scenario-indexed UAT result, exact approval or rejection wording, explicitly non-inducible branches, the deterministic evidence command/result when approved, and the resulting plan eligibility. Do not modify `36.14-VALIDATION.md`, ROADMAP, STATE, production code, tests, packages, or any other artifact in this plan. Only after the evidence comparison passes, report that Plan 13 is unlocked, followed by Plans 14-17 in dependency order, and regressions remain required; do not report Validation Wave 0, final UI, Plan 18, or Phase 36.14 complete.
</output>
