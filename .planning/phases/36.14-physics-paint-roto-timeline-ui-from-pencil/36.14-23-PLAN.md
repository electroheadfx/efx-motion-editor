---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 23
type: execute
wave: 21
depends_on:
  - 36.14-11
files_modified:
  - app/src/components/physic-paint/roto/rotoCanvasFrames.ts
  - app/src/components/physic-paint/roto/rotoLaunchHydration.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/components/physic-paint/hooks/useRotoReferenceController.ts
autonomous: true
gap_closure: true
gap_ids:
  - G-36.14-1
  - G-36.14-6
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "Per D-01, D-02, and D-10, every strict-interior generated frame is composited from the two adjacent canonical real-key PNG payloads and is never a durable timing or identity record."
    - "Hydrated canonical PNG payloads are decoded and registered before generated-frame publication depends on them; rendering does not require transient history from the process that originally painted the keys."
    - "Every generated image exposed to canvas/reference/playback consumers is a decodable PNG data URL with a real PNG signature; missing or invalid sources fail closed instead of being mislabeled as an image."
    - "Real-key payloads, paper/background composition, direct appFrame positions, and runtime strict-interior classification remain unchanged."
  artifacts:
    - path: "app/src/components/physic-paint/roto/rotoCanvasFrames.ts"
      provides: "Canonical PNG decode/register helper and valid alpha-canvas encoding."
    - path: "app/src/components/physic-paint/roto/rotoLaunchHydration.ts"
      provides: "Hydration barrier that reconstructs source canvases from canonical real-key PNG payloads."
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Valid generated alpha compositing with no non-image fallback payload."
    - path: "app/src/components/physic-paint/hooks/useRotoReferenceController.ts"
      provides: "Generated reference consumption that accepts only valid composed images."
  key_links:
    - from: "app/src/components/physic-paint/roto/rotoLaunchHydration.ts"
      to: "app/src/components/physic-paint/roto/rotoCanvasFrames.ts"
      via: "decode and register each unique canonical real-key PNG before document installation completes"
    - from: "app/src/stores/physicPaintStore.ts"
      to: "app/src/components/physic-paint/hooks/useRotoReferenceController.ts"
      via: "strict-interior lookup returns a valid composited PNG data URL or no image"
---

<objective>
Replace the invalid generated-frame fallback with valid compositing from canonical real-key PNG payloads and reconstruct required source canvases during hydration.

Purpose: G-36.14-1 and G-36.14-6 report correctly classified generated cells whose canvases are empty. The store currently depends on transient canvas registrations and can emit custom bytes labeled as PNG. D-02 and D-10 require runtime-derived interiors whose rendered output is determined by canonical physical payloads alone.

Output: Canonical PNG decode/registration, hydration ordering, valid alpha compositing, and fail-closed generated reference consumption. No durable generated authority, tests, typecheck, build, server, browser, or native application are introduced before renewed UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
@.planning/debug/uat-36-14-interpolation-revision.md

@app/src/components/physic-paint/roto/rotoCanvasFrames.ts
@app/src/components/physic-paint/roto/rotoLaunchHydration.ts
@app/src/stores/physicPaintStore.ts
@app/src/components/physic-paint/hooks/useRotoReferenceController.ts
</context>

<scope_constraints>
- Preserve canonical physical records as the only durable real-key authority and runtime projection as the only generated-cell timing authority.
- Composite alpha paint only; preserve the existing separate paper/background path and downstream final-composite behavior.
- Reuse browser Canvas/Image APIs and existing registry ownership. Add no dependency, worker, alternate cache store, durable generated records, nearest-owner fallback, or second rendering authority.
- Keep decoding bounded to unique canonical real-key payload URLs for the active layer/document and dispose temporary image resources after registration.
- D-12 and the user's UAT-first decision prohibit tests and executable build gates in this plan. Do not run tests, typecheck, build, server, browser, or native application.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Hydrate two canonical PNG keys and compose one strict-interior image</name>
  <files>app/src/components/physic-paint/roto/rotoCanvasFrames.ts, app/src/components/physic-paint/roto/rotoLaunchHydration.ts, app/src/stores/physicPaintStore.ts</files>
  <read_first>
    - `rotoCanvasFrames.ts` current encode/register helpers and registry key semantics.
    - `rotoLaunchHydration.ts` physical document validation and installation order.
    - `physicPaintStore.ts` physical generated-cell projection, registry lookup, alpha blend renderer, and current fallback branch.
  </read_first>
  <action>
Per D-01/D-02/D-10, add an exported `isRotoPngDataUrl` guard that verifies the PNG media type and decoded PNG signature, plus an async `registerRotoAlphaCanvasFrameFromDataUrl` helper that accepts only URLs passing that guard, decodes them into a correctly sized canvas, verifies successful image decoding and nonzero dimensions, registers the canvas under the exact payload URL, and releases temporary decode resources. Reuse the existing live-paint registration helper for already materialized canvases.

Make physical launch hydration await registration of every unique real-key payload PNG in the prepared canonical document before completing document installation and exposing generated projection. Fail hydration with the existing launch error path if a required canonical payload cannot be decoded; do not silently install a document whose generated interiors depend on unavailable source pixels.

In the store's generated renderer, preserve the current adjacent-key blend math and alpha-only output, but return only a canvas-encoded `data:image/png` result. Remove the custom non-image byte fallback. If a source registration is unexpectedly absent after the hydration/publication barrier, fail closed with no generated image and existing diagnostic routing rather than inventing image bytes, borrowing a neighbor, or persisting generated state.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const frames=fs.readFileSync('app/src/components/physic-paint/roto/rotoCanvasFrames.ts','utf8');const hydration=fs.readFileSync('app/src/components/physic-paint/roto/rotoLaunchHydration.ts','utf8');const store=fs.readFileSync('app/src/stores/physicPaintStore.ts','utf8');for(const token of ['isRotoPngDataUrl','registerRotoAlphaCanvasFrameFromDataUrl'])if(!frames.includes(token))throw new Error('PNG helper missing '+token);for(const token of ['isRotoPngDataUrl','registerRotoAlphaCanvasFrameFromDataUrl'])if(!hydration.includes(token))throw new Error('hydration missing '+token);if(store.includes('roto-alpha:'))throw new Error('invalid generated pseudo-image fallback remains');if(!store.includes("toDataURL('image/png')") &amp;&amp; !store.includes('toDataURL(\"image/png\")'))throw new Error('generated renderer lacks PNG encoding');" &amp;&amp; git diff --check -- app/src/components/physic-paint/roto/rotoCanvasFrames.ts app/src/components/physic-paint/roto/rotoLaunchHydration.ts app/src/stores/physicPaintStore.ts</automated>
  </verify>
  <done>Two hydrated adjacent real-key PNG payloads are registered before use, one strict-interior frame is blended from those canonical sources, only a real canvas-encoded PNG can be published, and absent/invalid source data fails hydration or generated publication without creating alternate timing or paint authority.</done>
</task>

<task type="auto">
  <name>Task 2: Carry valid generated PNGs through reference consumption and all physical gaps</name>
  <files>app/src/components/physic-paint/roto/rotoCanvasFrames.ts, app/src/components/physic-paint/roto/rotoLaunchHydration.ts, app/src/stores/physicPaintStore.ts, app/src/components/physic-paint/hooks/useRotoReferenceController.ts</files>
  <read_first>
    - Task 1 decode barrier and generated renderer.
    - `useRotoReferenceController.ts` current real/generated/empty source selection and engine reference handoff.
    - Store cache invalidation/revision behavior after real-key payload or interpolation changes.
  </read_first>
  <action>
Ensure every unique canonical real-key PNG entering through launch hydration is registered once for the active document and that replacement/reopen cannot retain a stale canvas under a different payload URL. Preserve the existing live encoder's registration behavior so freshly painted keys and reopened keys converge on the same source lookup contract.

Update generated reference consumption to call the shared `isRotoPngDataUrl` guard and accept the store's generated URL only when it is a valid PNG data URL from the current physical revision. Empty, missing, invalid, stale, disabled-interpolation, leading, and trailing cells must provide no generated alpha image; they must continue through the existing separate background/empty behavior. Do not cache generated output as durable key data or extend timeline duration from it.

Audit all four files for source/display coordinates, nearest-key substitution, custom image labels, persistent generated records, or paper/background flattening. Correct only within these files. Preserve the current synchronous generated read seam by doing asynchronous decoding at hydration/publication barriers rather than adding effect-driven rendering races in presentation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/components/physic-paint/roto/rotoCanvasFrames.ts','app/src/components/physic-paint/roto/rotoLaunchHydration.ts','app/src/stores/physicPaintStore.ts','app/src/components/physic-paint/hooks/useRotoReferenceController.ts'];for(const file of files){const text=fs.readFileSync(file,'utf8');if(text.includes('roto-alpha:'))throw new Error(file+' still emits invalid generated bytes')}const ref=fs.readFileSync(files[3],'utf8');if(!/generated/i.test(ref) || !ref.includes('isRotoPngDataUrl'))throw new Error('reference controller lacks shared generated PNG guard');" &amp;&amp; git diff --check -- app/src/components/physic-paint/roto/rotoCanvasFrames.ts app/src/components/physic-paint/roto/rotoLaunchHydration.ts app/src/stores/physicPaintStore.ts app/src/components/physic-paint/hooks/useRotoReferenceController.ts</automated>
  </verify>
  <done>Fresh and hydrated canonical payloads feed the same generated compositor; every valid strict interior can produce a real PNG; invalid/missing/disabled/outside-gap frames remain empty/background-only; reference consumption rejects non-PNG or stale output; no durable generated authority is added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted PNG data URL → hydration decoder | Serialized image bytes are untrusted until decoded and dimension-validated. |
| Canonical source canvases → generated compositor | Only current adjacent real-key payloads may contribute alpha pixels. |
| Generated URL → engine/reference consumer | The consumer must not accept mislabeled, stale, or non-image data. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-23-01 | Spoofing | PNG data URL | high | mitigate | Require PNG media type plus successful browser decode and valid dimensions before registry insertion. |
| T-36.14-23-02 | Tampering | Adjacent source selection | high | mitigate | Resolve sources only from current canonical neighboring real-key records and payload URLs. |
| T-36.14-23-03 | Repudiation | Generated revision | medium | mitigate | Tie reads to current physical document/revision and invalidate through existing store paths. |
| T-36.14-23-04 | Information Disclosure | Image diagnostics | low | accept | Add no external logging or payload export beyond existing LOG behavior. |
| T-36.14-23-05 | Denial of Service | Payload decode | medium | mitigate | Deduplicate active-document URLs, validate dimensions, and release temporary decode resources. |
| T-36.14-23-06 | Elevation of Privilege | Durable generated cache | high | mitigate | Publish runtime alpha only; add no real-key identity, persisted timing record, or nearest-owner fallback. |
| T-36.14-23-SC | Tampering | Package supply chain | low | accept | Native Canvas/Image APIs only; no dependency action. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Generated, reopened, and rendered output agree | Canonical PNGs are decoded on hydration and drive runtime composition | COVERED |
| REQ | PHYSICAL-IDENTITY | Only real-key payloads own source pixels | COVERED |
| REQ | DERIVED-INTERPOLATION | Every and only strict interiors are composited at runtime | COVERED |
| REQ | DOWNSTREAM-PARITY | Store and reference consumer receive valid PNGs after reopen | COVERED |
| REQ | UAT-THEN-REGRESSION | Static checks precede native UAT | COVERED |
| RESEARCH | Canonical payloads, alpha/background separation, no durable generated truth | Tasks 1-2 preserve all three | COVERED |
| CONTEXT | D-01 / D-02 / D-03 / D-10 / D-12 | Stable ownership, exact interiors, clean cutover, downstream parity, UAT-first | COVERED |
| CONTEXT | Deferred compatibility/migration | No fallback or migration is added | EXCLUDED — DEFERRED |

<verification>
1. Confirm only the four production files and summary change.
2. Confirm active physical hydration awaits unique canonical PNG decoding/registration.
3. Confirm generated rendering returns only canvas-encoded PNG data or no image.
4. Confirm strict-interior source selection remains adjacent canonical real keys and background stays separate.
5. Confirm no durable generated records, source/display coordinates, nearest-owner fallback, or second renderer appears.
6. Run only static task commands; no tests, typecheck, build, server, browser, or native application.
</verification>

<success_criteria>
- The generated-image root causes in G-36.14-1 and G-36.14-6 are fixed in production.
- Reopened canonical PNG payloads can drive the same alpha compositor as freshly painted keys.
- Invalid pseudo-images cannot reach reference/playback consumers.
- The implementation remains ready for user-owned native validation without claiming live success or phase completion.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-23-SUMMARY.md` when done. Record decode/registration ownership, hydration ordering, removed invalid fallback, fail-closed behavior, valid PNG consumer guard, static verification, and native-UAT readiness only.
</output>
