---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 27
type: execute
wave: 21
depends_on:
  - 36.14-11
files_modified:
  - app/src/lib/exportEngine.ts
autonomous: true
gap_closure: true
gap_ids:
  - G-36.14-13
requirements:
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "The notification permission rejection is treated as a separate export-completion capability defect, not as a Play Script generation or cache root cause."
    - "Notification permission and dispatch are attempted only from the authorized `main` Tauri window; the `efx-physic-paint` child completes export without invoking the notification plugin."
    - "Export completion, progress, generated files, cleanup, Preview/Export rendering, and error handling remain unchanged whether notification is eligible, denied, unavailable, or skipped."
    - "The child capability file is not broadened and no new permission is granted without a product requirement."
  artifacts:
    - path: "app/src/lib/exportEngine.ts"
      provides: "Main-window-only notification boundary after successful export completion."
  key_links:
    - from: "app/src/lib/exportEngine.ts"
      to: "@tauri-apps/api/window"
      via: "read the current label before importing or invoking notification APIs"
    - from: "app/src/lib/exportEngine.ts"
      to: "@tauri-apps/plugin-notification"
      via: "permission/request/send path reachable only for label main"
---

<objective>
Eliminate the unrelated child-window notification capability rejection without changing Play Script or broadening Tauri permissions.

Purpose: G-36.14-13 captured an unhandled `notification.is_permission_granted` rejection from `efx-physic-paint`. Static diagnosis proves this post-export path is independent of Play generation. The bounded fix is to skip notification APIs outside the already authorized main window.

Output: One main-window guard in `exportEngine.ts`, preserving export success and silently optional notifications. No capability, package, test, typecheck, build, server, browser, or native execution occurs.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
@.planning/debug/uat-36-14-play-script.md

@app/src/lib/exportEngine.ts
@app/src-tauri/capabilities/default.json
@app/src-tauri/capabilities/physics-paint.json
</context>

<scope_constraints>
- Modify only `exportEngine.ts`; capability JSON is read-only evidence.
- Determine the current Tauri window label before importing/calling the notification plugin. Only `main` is eligible.
- Preserve notification as non-critical best effort after export completion. A denied, unavailable, skipped, or failed notification must never change export status or reject the export promise.
- Do not grant `notification:default` to the child, move export ownership, change generated files, alter cleanup, or conflate this fix with Play Script.
- Do not add dependencies or run tests, typecheck, build, server, browser, or native application.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Skip notification APIs in the Physics Paint child and preserve main-window delivery</name>
  <files>app/src/lib/exportEngine.ts</files>
  <read_first>
    - `exportEngine.ts` successful completion, progress update, notification dynamic import, permission request, send, and outer error boundary.
    - `default.json` authorized `main` window and `physics-paint.json` child permissions, read-only.
  </read_first>
  <action>
After export status becomes complete, retain the existing `document.hidden` condition and add a current-window-label guard before any notification plugin import or permission call. Resolve the label through the installed Tauri window API and continue into `isPermissionGranted`, optional `requestPermission`, and `sendNotification` only when the label is exactly `main`.

Keep the complete notification branch best effort under its own error handling. Await or otherwise contain every Promise returned by permission and send APIs so rejection cannot escape as an unhandled Promise. If current-window lookup, plugin import, permission, request, or send fails, silently skip notification after export has already succeeded. Do not mutate export status, files, cleanup, progress, or outer error handling.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const text=fs.readFileSync('app/src/lib/exportEngine.ts','utf8');for(const token of ['getCurrentWindow','main','isPermissionGranted','sendNotification'])if(!text.includes(token))throw new Error('notification guard missing '+token);const label=text.indexOf('getCurrentWindow');const permission=text.indexOf('isPermissionGranted');if(label&lt;0 || permission&lt;0 || label&gt;permission)throw new Error('window label must be checked before notification permission');" &amp;&amp; git diff --check -- app/src/lib/exportEngine.ts</automated>
  </verify>
  <done>The `efx-physic-paint` window never imports or invokes notification permission/send APIs, the authorized main window retains best-effort background export notification, and every notification failure is contained after export completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Current Tauri window → notification capability | Window label determines whether permission-bound plugin APIs may be invoked. |
| Notification Promise → export completion | Optional OS integration must not reject or alter successful export state. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-27-01 | Spoofing | Window identity | medium | mitigate | Read current Tauri window label and require exact `main`. |
| T-36.14-27-02 | Tampering | Export completion state | medium | mitigate | Place optional notification after complete status and never mutate export state from its failure path. |
| T-36.14-27-03 | Repudiation | Notification outcome | low | accept | Notification remains best effort and non-authoritative. |
| T-36.14-27-04 | Information Disclosure | Notification body | low | accept | Preserve existing project/frame summary and authorized main-window scope. |
| T-36.14-27-05 | Denial of Service | Plugin rejection | medium | mitigate | Guard before import and contain/await every optional Promise. |
| T-36.14-27-06 | Elevation of Privilege | Child capability | high | mitigate | Do not broaden `physics-paint.json`; child skips the API. |
| T-36.14-27-SC | Tampering | Package supply chain | low | accept | Existing installed Tauri APIs only; no package action. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Export remains reliable across windows | Optional notification cannot fail child export | COVERED |
| REQ | DOWNSTREAM-PARITY | Export files/status remain unchanged | COVERED |
| REQ | UAT-THEN-REGRESSION | Static guard precedes native confirmation | COVERED |
| RESEARCH | Per-window capability boundary | Exact main-window guard matches diagnosed configuration | COVERED |
| CONTEXT | D-10 / D-12 | Export parity and UAT-first order preserved | COVERED |
| CONTEXT | Deferred permission expansion | Child capability is not broadened | EXCLUDED — NOT REQUIRED |

<verification>
1. Confirm only `exportEngine.ts` and the normal summary change.
2. Confirm current label is checked before notification plugin import/permission call.
3. Confirm only `main` reaches permission/request/send and child export simply skips notification.
4. Confirm every optional Promise is contained and export status/files/cleanup remain unchanged.
5. Run the static command only; no tests, typecheck, build, server, browser, native app, capability edit, or package action.
</verification>

<success_criteria>
- The notification portion of G-36.14-13 is fixed independently of Play Script.
- No child-window capability rejection can originate from this export-completion branch.
- Main-window notifications remain best effort.
- Native confirmation is deferred to the user-owned gap UAT; phase completion is not claimed.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-27-SUMMARY.md` when done. Record the exact window guard, contained Promise behavior, unchanged capability files/export semantics, static verification, and separation from Play Script root causes.
</output>
