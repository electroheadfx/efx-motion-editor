---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 25
type: execute
wave: 23
depends_on:
  - 36.14-22
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
gap_ids:
  - G-36.14-13
requirements:
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UI-INTEGRATION
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "Play Script retains the existing fused Scripts workflow and controller but its open dialog is mounted at Studio level, not inside the lower split Scripts pane."
    - "The Play Script surface is full-height relative to the Studio/canvas region, light, readable, and unconstrained by the right-panel pane split or compact delete-confirmation styles."
    - "Saved-project/capacity guards, count validation, Enter/Escape behavior, focus trap, initial input focus, return focus to the Play button, progress, generation cancellation, error/status text, and Generate/Cancel actions remain intact."
    - "The durable Scripts library, Copy/Apply Script, delete confirmation, toolbar, pane resizing, and retired separate Play workflow remain unchanged."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx"
      provides: "Focused accessible Play dialog presentation and lifecycle using the existing controller."
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "Scripts toolbar/list without a locally mounted Play dialog and with shared Play-button focus ref."
    - path: "app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx"
      provides: "Studio-level dialog host outside the split right panel."
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Dedicated full-height light Play surface independent of compact confirmation styling."
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx"
      via: "same RotoPlayScriptController and shared Play-button return-focus ref"
    - from: "app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx"
      to: "app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx"
      via: "Studio-level sibling host outside PhysicsPaintRightPanel"
---

<objective>
Restore Play Script's full-height light Studio presentation while preserving the approved fused Scripts workflow and every existing controller behavior.

Purpose: The UI portion of G-36.14-13 is structural: the dialog is a child of the short lower Scripts pane and reuses compact dark confirmation CSS. Increasing pane height cannot satisfy the required surface. The presentation must move while controller and workflow ownership remain unchanged.

Output: A focused Play dialog component mounted by `PhysicsPaintStudioView`, shared return-focus wiring from the Scripts toolbar, and dedicated full-height light styles. No separate Play workflow, controller rewrite, test, typecheck, build, server, browser, or native execution is added.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
@.planning/debug/uat-36-14-play-script.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-22-SUMMARY.md

@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<scope_constraints>
- Move presentation only. Keep `RotoPlayScriptController` as the sole validation/generation/progress/cancellation authority and preserve its existing API in this plan.
- Do not restore the retired separate Play workflow, add a route/tab/pane, duplicate generation controls, or move durable script-library state out of the Scripts panel.
- Do not solve the cache/publication root cause here; Plan 26 owns canonical physical publication after this UI boundary is stable.
- Use Preact-native props/refs and focused lifecycle effects only for focus synchronization. Add no copied controller state, broad effect chain, portal dependency, React compatibility workaround, or new package.
- Preserve Plan 22 canonical interpolation changes and all intentional uncommitted Studio/workflow changes.
- Per D-12, do not modify or run tests and do not run typecheck, build, server, browser, or native application.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Extract the existing Play dialog behavior into a focused component</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - `PhysicsPaintScriptsPanel.tsx` Play button ref, dialog/input refs, focus-return effect, keyboard trap, validation, progress, errors, and actions.
    - `physicsPaintRotoPlayScriptController.ts` read-only controller Signal/callback interface.
    - `PhysicsPaintStudio.tsx` `rotoPlayScript` construction and current right-panel Scripts props.
  </read_first>
  <action>
Create `PhysicsPaintPlayScriptDialog.tsx` as a presentation-only component accepting the existing `RotoPlayScriptController` and a shared `RefObject<HTMLButtonElement>` return-focus ref. Move the current Play dialog markup, input focus, previous-open tracking, Escape cancellation, Enter confirmation guard, Tab focus trap, accessible dialog/title/help/error relationships, progress, error, Generate, and Cancel behavior into this component without changing controller semantics or copy.

Move `playButtonRef` ownership to `PhysicsPaintStudio` so the same ref can be passed to the Scripts toolbar and Studio-level dialog. Extend `PhysicsPaintScriptsPanelProps` with that ref, attach it to the existing Play Script toolbar button, and remove only the local Play dialog refs/effect/markup. Keep durable library rows, rename/delete focus handling, status, Copy/Apply behavior, and toolbar order unchanged.

Do not render the new component yet outside its focused source contract; finish this tracer with static proof that one button still opens the same controller confirmation and one extracted component contains all dialog lifecycle behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const panel=fs.readFileSync('app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx','utf8');const dialog=fs.readFileSync('app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx','utf8');const studio=fs.readFileSync('app/src/components/physic-paint/PhysicsPaintStudio.tsx','utf8');for(const token of ['role=\"dialog\"','playScript.confirmationOpen','playScript.confirm','playScript.cancel'])if(!dialog.includes(token))throw new Error('dialog missing '+token);if(panel.includes('physics-paint-play-script-dialog'))throw new Error('Play dialog still mounted in Scripts panel');if(!panel.includes('playButtonRef') || !studio.includes('playButtonRef'))throw new Error('shared return-focus ref missing');" &amp;&amp; git diff --check -- app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx</automated>
  </verify>
  <done>The Scripts toolbar still opens the same Play controller; one extracted component owns all prior dialog accessibility, focus, keyboard, validation, progress, cancellation, and action behavior; the Scripts panel no longer mounts the Play surface locally.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the dialog at Studio level with a full-height light surface</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - Task 1 final dialog props and shared focus ref.
    - `PhysicsPaintStudioView.tsx` Studio/canvas/right-panel/workflow sibling composition.
    - `physicsPaintStudio.css` `.physics-paint-scripts-panel`, compact confirmation, current Play dialog, canvas-region, and Studio layout containing blocks.
  </read_first>
  <action>
Add a typed `playScriptDialog` prop to `PhysicsPaintStudioViewProps` carrying the existing controller and shared return-focus ref. Render `PhysicsPaintPlayScriptDialog` as a direct child of `.physics-paint-studio` outside `PhysicsPaintRightPanel`, after the canvas/right-panel composition and before or above the workflow strip in stacking order. Studio passes the same `rotoPlayScript` instance used by the toolbar; no duplicate controller or state is created.

Replace the inherited compact Play styles with three dedicated Studio-level selectors: `.physics-paint-play-script-dialog` for the Studio/canvas-region host, `.physics-paint-play-script-surface` for the light readable dialog surface, and `.physics-paint-play-script-content` for the internal scrolling content area. The host must occupy the canvas row/column (`grid-row: 2`, `grid-column: 2`) with full inset/stretch containment and `min-height: 0`; the surface must use an explicit light background and dark foreground and fill the host without exceeding it; the content selector must own vertical overflow. Keep these selectors in standalone rule blocks rather than grouping them with `.physics-paint-scripts-panel` or `.physics-paint-script-confirmation`, so lower-pane `min-height`/overflow and compact confirmation inset/max-height/background cannot constrain Play. Preserve the right-panel split, canvas input geometry, workflow-strip height, and z-index/focus visibility.

Audit visual/accessibility structure statically: one dialog instance, one `aria-modal`, one titled surface, one initial input, one focus trap, one return target, and no retired Play tab/pane/route. Keep Copy/Apply Script and durable Scripts UI unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const view=fs.readFileSync('app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx','utf8');const dialog=fs.readFileSync('app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx','utf8');const css=fs.readFileSync('app/src/components/physic-paint/physicsPaintStudio.css','utf8');if(!view.includes('PhysicsPaintPlayScriptDialog') || !view.includes('playScriptDialog'))throw new Error('Studio-level Play host missing');if((dialog.match(/aria-modal/g)||[]).length!==1)throw new Error('dialog modal count must be one');const block=(selector)=>{const escaped=selector.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');const match=css.match(new RegExp(escaped+'\\s*\\{([^}]*)\\}'));if(!match)throw new Error('missing dedicated selector '+selector);return match[1]};const host=block('.physics-paint-play-script-dialog');const surface=block('.physics-paint-play-script-surface');const content=block('.physics-paint-play-script-content');for(const token of ['grid-row: 2','grid-column: 2','min-height: 0'])if(!host.includes(token))throw new Error('Play host missing '+token);if(!/(inset:\s*0|align-self:\s*stretch)/.test(host))throw new Error('Play host lacks full Studio/canvas containment');if(!/background(?:-color)?:\s*(#(?:f|e|d)[0-9a-f]{2,5}|rgb\(2(?:[0-4][0-9]|5[0-5]))/i.test(surface))throw new Error('Play surface lacks explicit light background');if(!/color:\s*(#(?:0|1|2|3|4|5)[0-9a-f]{2,5}|rgb\((?:[0-8]?[0-9]|9[0-9]|1[01][0-9]),)/i.test(surface))throw new Error('Play surface lacks explicit dark foreground');if(!/overflow-y:\s*(auto|scroll)/.test(content))throw new Error('Play content lacks internal vertical overflow');for(const selector of ['.physics-paint-scripts-panel','.physics-paint-script-confirmation']){const grouped=new RegExp('(?:'+selector.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+'[^{}]*physics-paint-play-script|physics-paint-play-script[^{}]*'+selector.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+')\\s*\\{');if(grouped.test(css))throw new Error('Play selectors grouped with '+selector)}" &amp;&amp; git diff --check -- app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx app/src/components/physic-paint/physicsPaintStudio.css</automated>
  </verify>
  <done>Play Script opens one Studio-level full-height light dialog outside the split Scripts pane; all prior guards, focus, validation, progress, cancellation, and actions remain; right-panel/library/Copy-Apply behavior and the fused workflow are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scripts toolbar → Studio dialog | One button opens a surface outside its local pane while sharing controller/focus identity. |
| Controller Signals → presentation | UI renders trusted derived state but must not duplicate or mutate generation authority. |
| Modal keyboard input → controller actions | Escape, Enter, Tab, Generate, and Cancel require guarded accessible behavior. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-25-01 | Spoofing | Duplicate dialog/controller | medium | mitigate | Pass the same controller instance and render exactly one dialog component. |
| T-36.14-25-02 | Tampering | Count input/action guards | medium | mitigate | Preserve controller validation and existing guarded handlers unchanged. |
| T-36.14-25-03 | Repudiation | Focus/cancel behavior | low | mitigate | Keep explicit initial focus, trap, Escape, and return-focus lifecycle. |
| T-36.14-25-04 | Information Disclosure | Dialog errors | low | accept | Show existing user-facing errors only; no new logs or payload detail. |
| T-36.14-25-05 | Denial of Service | Modal trapping | medium | mitigate | Keep finite focusable query and reliable cancellation/return focus. |
| T-36.14-25-06 | Elevation of Privilege | Presentation-owned generation | high | mitigate | Component receives controller state/actions only and adds no generation logic. |
| T-36.14-25-SC | Tampering | Package supply chain | low | accept | No portal/library/package dependency is added. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Polished UI without duplicated business logic | Studio-level presentation consumes the existing controller | COVERED |
| REQ | UI-INTEGRATION | Full-height light Play surface and preserved accessibility | COVERED |
| REQ | DOWNSTREAM-PARITY | Existing generation controller is unchanged; cache fix belongs to Plan 26 | COVERED BY DEPENDENCY |
| REQ | UAT-THEN-REGRESSION | Static checks precede native visual UAT | COVERED |
| RESEARCH | Preact direct props/Signals and lifecycle-only effects | Shared controller/ref and focused focus effects only | COVERED |
| CONTEXT | D-04 / D-10 / D-12 / D-27 / D-28 | Script behavior, preservation, UAT order, and guarded controls remain | COVERED |
| CONTEXT | Deferred separate workflow/migration | Neither is restored | EXCLUDED — DEFERRED |

<verification>
1. Confirm only the five listed files and summary change.
2. Confirm ScriptsPanel contains the Play button but no Play dialog.
3. Confirm StudioView mounts exactly one extracted dialog outside the right panel.
4. Confirm dedicated Play host/surface/content selectors establish canvas-region height/inset containment, explicit light/dark contrast, internal vertical overflow, and no selector grouping with compact confirmation or Scripts-pane constraints.
5. Confirm all controller, guard, focus, progress, cancellation, durable library, and Copy/Apply behavior remains wired.
6. Run static commands only; no tests, typecheck, build, server, browser, or native application.
</verification>

<success_criteria>
- The UI root cause of G-36.14-13 is fixed in production and ready for user-owned visual UAT.
- Play Script remains fused into Scripts but displays at Studio/canvas scale with a light full-height surface.
- No separate workflow, duplicate controller, broad effect, package, test, or executable gate is introduced.
- No claim is made about cache publication until Plan 26 or about live UI acceptance until Plan 29.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-25-SUMMARY.md` when done. Record the extracted component, Studio host, shared focus ref, preserved controller behaviors, dedicated styles, static verification, and explicit deferral of cache publication/native acceptance.
</output>
