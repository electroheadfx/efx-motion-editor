# Phase 36.14: Deterministic Physical-Frame Roto Timeline Cutover and Final UI Integration - Context

**Gathered:** 2026-07-19
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 36.14 is the final v0.8.0 phase. It replaces the dual source/display and spacing-override Roto timing model with one stable physical-frame authority, routes every timeline edit through one acknowledged identity-preserving batch transaction, proves downstream cache/persistence/render parity, stops for user-owned native UAT, adds deterministic regressions only after explicit UAT approval, and then applies the corrected final Roto timeline UI.

The phase must preserve all approved painting, script, playback, onion, cache, preview/export, Undo/Redo, and rapid-input behavior. It must not add legacy migration, compatibility timing aliases, multi-selection, group movement, a second script clipboard, or business logic inside timeline presentation components.

</domain>

<decisions>
## Implementation Decisions

### Physical Key Identity and Interpolation
- **D-01:** Every durable real Roto key has one stable `keyId` and one physical `appFrame`. Source, stored, displayed, selected, cached, reopened, previewed, and exported position are the same coordinate; every key-owned payload moves with the identity.
- **D-02:** Automatic interpolation persists only enabled/disabled state and derives exactly `max(0, rightFrame - leftFrame - 1)` interior frames from adjacent physical real keys. Adjacent keys are valid, no leading/trailing paint interpolation exists, and toggling interpolation cannot move real keys.
- **D-03:** Remove global `inBetweenCount`, source/display projection, minimum-spacing rules, segment and moved-key spacing overrides, stale-spacing restoration, durable generated timing truth, and every production reader/writer of those concepts. Do not retain compatibility aliases, adapters, hydration fallbacks, or old-project migration.
- **D-04:** Script Motion `deform` and `position` remain supported but move into a separate script-motion settings contract rather than surviving inside interpolation timing state.

### Atomic Physical Timeline Edits
- **D-05:** Insert Frame shifts the selected identity and every later real key right by one physical slot, creates no key, preserves selected identity, validates full capacity, and records one acknowledged Undo/Redo action.
- **D-06:** Delete Frame removes the selected identity and its physical slot, shifts every later survivor left by one, shares toolbar and Delete/Backspace routing, preserves survivor payload/identity, selects deterministically, and records one acknowledged Undo/Redo action.
- **D-07:** Drag is ripple cut-and-insert. Empty/generated physical cells are direct destinations; occupied real keys expose before/after boundaries and are never overwritten. Preview and commit use the same pure resolver and track source/target by identity.
- **D-08:** Force Spacing is an explicit action where integer `N >= 0` creates exactly `N` empty physical slots between adjacent ordered real keys, anchors the first key, accepts `N = 0`, validates full capacity, and records one acknowledged Undo/Redo action.
- **D-09:** Insert, Delete, Drag, Force Spacing, Undo, and Redo use one complete `keyId -> appFrame` mapping transaction: validate the final map, stop/finalize pending paint, capture one immutable complete snapshot, stage locally once, publish once, await one matching parent acknowledgement, finalize history only on acceptance, and restore complete state on rejection/timeout/transport failure.
- **D-10:** Reopen, live cache publication, playback, onion/reference, preview, timeline length, missing-frame rendering, and export consume direct physical positions and runtime-derived interior gaps. No downstream consumer may revive source/display ownership.

### Delivery and Validation Sequence
- **D-11:** Plan this as bounded sequential plans, not as the rejected monolithic quick. Separate physical model/schema, shared transaction, ripple Insert/Delete, ripple Drag, Force Spacing, downstream parity, native UAT, post-UAT regressions, and final UI integration into reviewable boundaries.
<!-- D-30-HISTORICAL-START: original sequencing evidence; superseded and non-executable for the current rejected-UAT recovery track -->
- **D-12 (historical baseline):** Production implementation and binary legacy-field cleanup run first, followed by typecheck/build. Stop ready for user-owned native UAT. Do not add or run regression tests until the user explicitly approves the physical model; afterward use `vitest run`, including the deferred deterministic drag coverage, before final UI validation. D-30 supersedes this order only for the current rejected-UAT recovery track.
<!-- D-30-HISTORICAL-END -->
- **D-30:** For the current rejected-UAT recovery track only, supersede D-12's pre-UAT compile sequence with this exact active order: (1) Plans 21-27 deliver production gap fixes using bounded static checks only; (2) Plan 28 performs an integrated bounded static read-only review; (3) Plan 29 stops for user-owned native UAT; and (4) only after the user's exact native approval may separate later regression, typecheck, and build planning become eligible. This recovery sequence does not execute or unlock Plans 13-18 and must not create `36.14-12-APPROVAL.txt` or any replacement approval token.

### Responsive Timeline Layout
- **D-13:** The final header remains one compact row at the minimum supported width. Every control group stays visible; the central status capsule shrinks and truncates before controls compress, wrap, hide, or create a separate header scrollbar.
- **D-14:** On wider windows, extra horizontal space goes to the status capsule only. Transport, quick actions, interpolation/Force Spacing, cadence, and close remain tightly grouped rather than spreading across the width.
- **D-15:** Keep the corrected reference's compact fixed timeline height. Long status text truncates instead of growing the bottom panel or recreating the removed developer-status block.
- **D-16:** Frame cells and ruler proportions remain fixed across window sizes. Longer ranges use the existing synchronized horizontal timeline scroll/scrollbar; do not scale cells continuously or add density breakpoints.

### Force Spacing Control
- **D-17:** Replace the old live in-between count with an explicit compact nonnegative `N` input plus an **Apply** button inside the interpolation group, after the interpolation icon and enabled checkbox. The UI must read as a one-shot action, not a persisted live timing setting.
- **D-18:** The action button label is **Apply**.
- **D-19:** Keep the last entered `N` value only for the current window/session to support repeated timing work. It is not project timing truth and must not be persisted.
- **D-20:** Apply immediately after validation with no confirmation dialog. Capacity/validation failure leaves all state and history untouched; successful application is one undoable acknowledged action.

### Ripple Drag Feedback
- **D-21:** Occupied real-key targets use a clear left/right edge insertion caret plus concise boundary text. Do not split the locked 18px cell into tiny half-cell hit areas or add permanent gap elements.
- **D-22:** Before release, render the complete resolver-proposed mapping: moved identity, every shifted real key, and visually re-derived generated cells. The preview must expose the actual ripple result, not only the destination.
- **D-23:** Empty and generated physical destinations use a whole-cell target highlight. Generated styling remains visible underneath the target treatment; occupied cells continue to use before/after edge carets.
- **D-24:** After matching acknowledgement, selection and focus follow the moved `keyId` to its final physical cell. Auto-scroll only enough to keep that cell visible.

### Status and Script Routing
- **D-25:** The compact header capsule shows the latest successful relevant operation until navigation, playback, or another operation replaces it. When there is no newer event, show the concise missing-frame baseline.
- **D-26:** Failures show a concise actionable capsule summary such as `Apply failed — see LOG`; full errors and diagnostic detail go only to the existing right-panel LOG tab.
- **D-27:** The final timeline action row ends with `Copy Script | Apply Script`. Move the approved manual volatile-clipboard `Discard Script` action to the right-panel Scripts toolbar so the lifecycle remains available without violating the corrected timeline order.
- **D-28:** Unavailable `Copy Script` and `Apply Script` controls remain focusable through guarded `aria-disabled` presentation. Hover/focus exposes the controller-supplied reason through tooltip/accessibility text, and attempted activation may place the concise reason in the status capsule. Do not reconstruct availability in the UI.

### Rejected-UAT Recovery Behavior
- **D-29:** Occupied `before-key`/`after-key` Drag removes only the moved stable identity without closing its former physical slot, re-resolves the target by stable `keyId`, and ripples only at the occupied destination boundary. From `A@1,B@3,C@5,D@8`, releasing B on D's visible after-key caret must produce exactly `A@1,C@5,D@8,B@9`. Empty/generated `physical-cell` Drag retains D-07's existing source-closing direct-destination behavior.

### Claude's Discretion
- Exact CSS values, icons, tooltip implementation, status wording, caret animation, and component extraction are flexible when they preserve the corrected reference, fixed geometry, accessible behavior, and all decisions above.
- Exact TypeScript names and plan boundaries are flexible, but the single physical model, shared acknowledged transaction, D-30 recovery sequencing, and presentation/business-logic separation are not.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase Scope and Physical Cutover
- `.planning/ROADMAP.md` — Current Phase 36.14 goal, requirements, success criteria, locked operation examples, bounded planning sequence, and final-phase boundary.
- `.planning/REQUIREMENTS.md` — Pending Phase 36.14 UI, LOG, selection-guard, and regression requirements plus milestone traceability.
- `.planning/PROJECT.md` — Product constraints, Preact/Signals architecture, rendering pipeline, and Physics Paint non-replacement boundary.
- `.planning/STATE.md` — Current phase position, approved prerequisite quicks, and recent single-key drag status.
- `.planning/quick/260719-gmn-new-deterministic-physical-frame-roto-in/260719-gmn-RESEARCH.md` — High-confidence production integration inventory, target physical model, transaction architecture, pitfalls, downstream consumers, and validation sequencing.
- `.planning/quick/260719-gmn-new-deterministic-physical-frame-roto-in/260719-gmn-PLAN.md` — Aborted monolithic plan retained only as restructuring input. D-01 through D-11 remain authoritative; D-12 is historical for the current rejected-UAT recovery and is superseded there by D-30; its old occupied Drag example is superseded by D-29; its two-task execution shape is rejected.

### Final Timeline UI
- `SPECS/36.x-phases/phase-36.14-timeline-ui/spec-36.14-timeline-ui.md` — Final visual hierarchy, obsolete UI removal, script callback boundary, status/LOG routing, selection guard, and acceptance/UAT contract. Its old count-control and Phase-36.13 projection wording are superseded by the current roadmap physical-frame cutover.
- `SPECS/36.x-phases/timeline-ui/TimelineBottomWindow.tsx` — Corrected Preact/Tailwind visual reference for proportions, colors, grouping, fixed-height bands, ruler, cells, and fit-content action row; sample timing values are non-authoritative.
- `SPECS/36.x-phases/timeline-ui/timeline-bottom-window.html` — Browser-renderable corrected visual reference corresponding to the TSX design.

### Accepted Behavior to Preserve
- `.planning/quick/260718-m2f-add-single-real-physics-paint-roto-key-d/260718-m2f-SUMMARY.md` — Approved pointer gesture, save barrier, acknowledgement, rollback, payload preservation, and selection behavior for the current single-key move seam.
- `.planning/quick/260718-m2f-add-single-real-physics-paint-roto-key-d/260718-m2f-VERIFICATION.md` — Independent verification of the approved move implementation and deferred deterministic test obligation.
- `.planning/quick/260715-kgf-implement-functional-physics-paint-roto-/260715-kgf-CONTEXT.md` — Immutable reusable active-session Copy Script / Apply Script clipboard and destination behavior.
- `.planning/quick/260715-kgf-implement-functional-physics-paint-roto-/260715-kgf-VERIFICATION.md` — Verified script-copy/apply controller contract that timeline presentation must consume without duplication.
- `.planning/quick/260717-9hw-polish-physics-paint-right-sidebar-tabs-/260717-9hw-CONTEXT.md` — Approved right-panel Brush/LOG and Onion/Motion/Scripts pane organization and script interaction boundary.
- `.planning/quick/260717-9hw-polish-physics-paint-right-sidebar-tabs-/260717-9hw-VERIFICATION.md` — Verified right-sidebar and Scripts behavior that must remain stable when Discard moves into the Scripts toolbar.
- `.planning/quick/260717-m9k-merge-physics-paint-play-into-the-roto-s/260717-m9k-CONTEXT.md` — Approved fusion of Play behavior into durable Roto Scripts.
- `.planning/quick/260717-m9k-merge-physics-paint-play-into-the-roto-s/260717-m9k-VERIFICATION.md` — Verified Play Script behavior and downstream timing/cache preservation.
- `.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-CONTEXT.md` — Historical source/display and spacing decisions to remove cleanly while preserving user-visible distant-key intent through physical positions.
- `.planning/phases/36.12-physics-paint-roto-generated-interpolation/36.12-CONTEXT.md` — Generated render-only, alpha/background separation, preview-only selection, and real-key authority behavior to preserve under gap-derived interpolation.
- `.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md` — Additive cached-key repaint, immutable cached base, Clear/Delete separation, and compact feedback behavior.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` — Existing Pointer Events drag threshold/capture/cancellation/edge-scroll mechanics, fixed cells, synchronized scrollbar, key/script callbacks, availability signals, and status surfaces. Preserve gesture mechanics while replacing projected timing, target classification, header composition, and developer status stack.
- `app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts` — Current transaction builders and move resolver provide the seam to replace with complete identity-based physical mappings rather than adding parallel Insert/Delete/Drag/Force implementations.
- `app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts` and `app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts` — Existing operation-bound publication/settlement seams to generalize for every physical batch edit.
- `app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts` — Existing accepted-only move snapshots and paint-history barriers to generalize into one physical-edit history while retaining exact 10-level per-brush behavior.
- `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` — Current source/display interpolation update path that must be simplified to enabled-only interpolation plus explicit Force Spacing intent.
- `app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts` and `app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts` — Approved Copy/Apply/Discard callbacks, signals, and controller-supplied availability/status; presentation must consume these directly.
- `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx` — Existing right-panel Scripts toolbar where manual volatile clipboard Discard should be presented without changing durable script-library semantics.
- `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` and `app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.ts` — Existing compact presentation and apply-result boundaries for concise capsule copy plus detailed LOG routing.
- `app/src/stores/physicPaintStore.ts`, `app/src/lib/physicPaintPersistence.ts`, `app/src/lib/physicPaintBridge.ts`, `app/src/lib/previewRenderer.ts`, `app/src/lib/exportRenderer.ts`, and `app/src/lib/frameMap.ts` — Store, persistence, authority, preview/export, and timeline-length consumers that must converge on direct physical frames.

### Established Patterns
- This is Preact, not React. Prefer direct derivation, existing Signals/version subscriptions, and explicit event/controller actions; do not add broad effects that copy resolver or store state.
- `PhysicsPaintStudio` remains a composition and external-boundary adapter. Pure physical resolution and transaction construction belong in focused Roto modules/hooks, not in presentation components or new Studio effect chains.
- Parent acknowledgement is authoritative for durable timeline mutations. Optimistic local state, selection, history, cache, and generated output are provisional until the matching result settles.
- Real keys are the only editable authority; generated frames remain runtime-derived/render-only; paper/background remains separate from alpha-only paint caches.
- Store mutations that affect preview/export must continue to use the accepted `physicPaintVersion`/project-dirty invalidation path.
- Timeline geometry uses fixed cells and a synchronized horizontal scroll surface; preserve Pointer Events, focus, keyboard, slider/input, and canvas behavior when adding the global text-selection guard.

### Integration Points
- Replace `rotoSourceDisplayModel.ts` and source/display readers with a focused physical model that orders stable identities, derives interior gaps, and resolves all operation intents.
- Generalize Studio save-barrier, snapshot, local remap, parent publication, settlement, rollback, selection, and history wiring around one physical batch transaction.
- Update shared/persisted/launch/bridge types and validators in one clean cutover so no half-migrated production caller graph survives between plans.
- Update workflow-strip props to receive physical preview mappings, occupied boundary intents, Force Spacing action/availability, concise status state, and existing script controller state without deriving business rules.
- Recompose the header and action row from the corrected references, remove the permanent legend/status stack and obsolete controls, and move Discard into `PhysicsPaintScriptsPanel`.
- Add the application selection rule in `app/index.html`, preserving selectable inputs, textareas, contenteditable fields, and `.physics-paint-log-messages`.
- Verify direct physical lookup parity through persistence/hydration, cache publication, playback/navigation, onion/reference, Preview, Export, missing/background rendering, and timeline-length calculation before UAT.

</code_context>

<specifics>
## Specific Ideas

- Authoritative physical examples: keys `1,2,5` with interpolation ON generate only frames `3,4`; Insert examples `1,2,3 -> 2,3,4 -> 2,4,5`; Force Spacing `2` maps `1,2,5 -> 1,4,7`; Force Spacing `0` makes ordered keys adjacent.
- D-29 authoritative occupied-target Drag example: from `A@1,B@3,C@5,D@8`, releasing B on target identity D's visible after-key caret resolves to `A@1,C@5,D@8,B@9`; B's former slot stays open, D remains at frame 8, and every payload follows its stable identity.
<!-- D-30-HISTORICAL-START: rejected occupied-Drag evidence; non-authoritative for current recovery -->
- Historical D-07/Plan 12 occupied result `A@1,C@4,D@7,B@8` is rejected and retained only as superseded evidence. It must not drive implementation, static review, or native UAT.
<!-- D-30-HISTORICAL-END -->
- Header mental model: fixed compact control islands around one elastic status capsule; the status capsule is the only region that grows or truncates.
- Drag mental model: occupied targets show insertion boundaries, empty/generated targets show physical destinations, and the entire proposed mapping is visible before release.
- Final action row: `Key N | Insert | Dup | Copy | Paste | Delete | Copy Script | Apply Script`; Discard moves to the right-panel Scripts toolbar.

</specifics>

<deferred>
## Deferred Ideas

- Multi-selection UI and group movement — future capability; the complete identity-mapping transaction should not block it, but Phase 36.14 adds no UI or behavior for it.
- Legacy Roto timing migration or compatibility support — explicitly excluded; the phase performs a clean schema replacement.

</deferred>

---

*Phase: 36.14-Deterministic Physical-Frame Roto Timeline Cutover and Final UI Integration*
*Context gathered: 2026-07-19*
