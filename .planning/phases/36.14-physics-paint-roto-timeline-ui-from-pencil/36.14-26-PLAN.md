---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 26
type: execute
wave: 24
depends_on:
  - 36.14-22
  - 36.14-23
  - 36.14-25
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/lib/physicPaintBridge.ts
autonomous: true
gap_closure: true
gap_ids:
  - G-36.14-13
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UI-INTEGRATION
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "Per D-01, D-09, and D-10, Play Script publishes one complete canonical physical record map through the existing coordinator and parent bridge, never through the legacy frame-indexed cache route."
    - "Existing real keys outside the generated range remain byte-for-byte/semantically unchanged; an existing destination inside the range preserves its keyId/appFrame while receiving the generated PNG payload; each new destination receives one fresh stable keyId allocated exactly once."
    - "Every accepted Play payload is a valid alpha PNG attached to the canonical record at its direct appFrame, survives save/reopen, and is available to timeline, playback, reference, Preview, and Export consumers."
    - "The parent validates saved-project/capacity/range/revision/identity/payload semantics atomically and returns an exact physical acknowledgement; the child mirrors only that accepted physical document."
    - "Script Motion remains separate, interpolation enabled state and existing real-key spacing are not rewritten, Copy/Apply Script remains unchanged, and no legacy/physical dual write is retained."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Explicit play-script physical operation and exact acknowledgement validation."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      provides: "Rendered PNG-to-physical-record publication contract with stable destination identities."
    - path: "app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts"
      provides: "Adapter from Play generation to the sole physical coordinator rather than direct legacy apply."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "One Play pending/snapshot/stage/send/match/restore lifecycle using complete physical records."
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Parent-authoritative Play semantic-delta validation and atomic physical replacement."
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Accepted physical mirror/launch update with no legacy cachedRotoFrames or rotoBackground write."
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts"
      via: "complete staged physical records plus range, stable IDs, PNG payloads, and expected revision"
    - from: "app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      via: "explicit play-script operation through the existing serialized coordinator"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "exact accepted physical document/revision mirrored only after parent acknowledgement"
---

<objective>
Migrate Play Script generation from the retired frame-indexed cache path to one canonical physical-map transaction with stable identities and durable PNG payloads.

Purpose: The cache portion of G-36.14-13 is an incomplete authority cutover. Rendering produces staged images, but commit and mirror still update legacy frames that physical serialization, hydration, timeline projection, and rendering ignore. D-01/D-09/D-10 require accepted Play output to become canonical real-key records.

Output: An explicit `play-script` physical operation, stable physical record construction, coordinator staging/rollback, parent semantic validation, exact acknowledgement, and accepted-only physical mirror. Copy/Apply stays unchanged; no tests or executable gates run before native UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
@.planning/debug/uat-36-14-play-script.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-21-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-22-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-23-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-25-SUMMARY.md

@app/src/types/physicPaint.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/lib/physicPaintBridge.ts

# Read-only canonical helpers/consumers
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/components/physic-paint/roto/rotoCanvasFrames.ts
@app/src/stores/physicPaintStore.ts
@app/src/lib/physicPaintPersistence.ts
@app/src/components/physic-paint/roto/rotoLaunchHydration.ts
</context>

<scope_constraints>
- Preserve the existing Play renderer, deterministic held-pose Motion algorithm, progressive staging, saved-project/capacity guards, count validation, cancellation, progress, and final-commit timing.
- Preserve Copy/Apply Script code and durable script-library behavior exactly; do not route them through this operation or alter the reusable clipboard.
- Add `play-script` as an explicit non-history physical operation. Preserve established Play behavior rather than silently adding an Undo/Redo command; ordinary physical edit history remains owned by its existing operation kinds.
- Reuse `createPhysicPaintRotoKeyId`, canonical payload/record validators, Plan 23 PNG registration, the sole Plan 21 coordinator, and parent physical-map replacement. Allocate every new identity once before dispatch; never allocate during validation, acknowledgement, mirror, hydration, or replay.
- Preserve existing records outside the affected range and existing destination keyIds inside it. Do not maintain a synchronization layer between legacy frames and physical records, and do not write both forms.
- Preserve all intentional uncommitted Studio/coordinator fixes and Plans 21-25 changes.
- Per D-12, do not create/modify/run tests and do not run typecheck, build, server, browser, or native application.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Publish one generated destination as an acknowledged physical record</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - `physicPaint.ts` physical operation/result unions and canonical record/payload types.
    - `physicsPaintRotoPlayScriptController.ts` authority capture, staged PNG merge, commit/mirror ports, range, revision, and cancellation checks.
    - `useRotoPlayScriptController.ts` current direct legacy apply adapter.
    - `useRotoPhysicalEditCoordinator.ts` Plans 21-22 complete snapshot, explicit operation, pending, validation, send, exact match, restore, and accepted output.
    - `physicPaintBridge.ts` operation-specific semantic-delta validation before physical mutation/ledger.
  </read_first>
  <action>
Per D-01/D-09/D-10, add the explicit non-history physical operation kind `play-script` to shared physical request/result validation. Define its semantic metadata as affected start/end appFrame, expected layer capacity/end, and the complete proposed canonical real-key records. The operation uses the existing `replace-roto-physical-map` envelope, sole coordinator, canonical revision, and exact result; it does not use replay provenance or ordinary history.

Change the Play controller's commit port from legacy frame/background/settings data to a complete physical publication object. For the tracer's one generated destination, validate the rendered result with Plan 23's shared `isRotoPngDataUrl` guard, then resolve the current canonical destination: preserve the existing destination keyId if occupied, otherwise allocate one fresh keyId exactly once; create/replace one canonical payload at the direct appFrame; retain every record outside the affected destination unchanged; preserve current canonical interpolation enabled state and Script Motion separation.

Change `useRotoPlayScriptController` to dispatch this explicit proposal through `executePhysicalEdit` instead of sending a legacy apply payload. The coordinator revalidates range, complete records, current physical revision, and PNG payload before staging, then uses its one snapshot/pending/send/match/restore lifecycle. In the parent bridge, independently validate authoritative current/proposed records: outside range unchanged; destination identity preserved or declared fresh exactly once; direct appFrame and payload frame agree; PNG payload is valid; capacity/range/revision/selection are valid; interpolation is unchanged. Only then replace the physical document and return the exact acknowledgement.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/types/physicPaint.ts','app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts','app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','app/src/lib/physicPaintBridge.ts'];for(const file of files){const text=fs.readFileSync(file,'utf8');if(!text.includes('play-script'))throw new Error(file+' missing play-script physical operation')}const hook=fs.readFileSync(files[2],'utf8');if(!hook.includes('executePhysicalEdit'))throw new Error('Play adapter bypasses physical coordinator');" &amp;&amp; git diff --check -- app/src/types/physicPaint.ts app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts app/src/lib/physicPaintBridge.ts</automated>
  </verify>
  <done>One generated Play PNG becomes one stable canonical destination record through the sole coordinator and parent physical bridge; existing identity is preserved or one new identity is allocated once; the parent validates the exact delta; no legacy frame commit, dual write, replay provenance, or ordinary history entry occurs.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the complete Play range and mirror only the accepted physical document</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - Task 1 final `play-script` metadata, complete record proposal, and exact result.
    - Play controller progressive render/cancel/revision flow and `mirrorAccepted` timing.
    - `PhysicsPaintStudio.tsx` current Play local mirror that writes legacy store/cache/launch-context fields.
    - Canonical read-only store replacement, persistence filename, hydration, and Plan 23 PNG registration helpers.
  </read_first>
  <action>
Extend physical record construction across the complete accepted Play range. Preserve every record outside the range. For each in-range destination, preserve an existing keyId and replace only its payload, or allocate one fresh keyId once for a new destination. Attach each final staged alpha PNG to exactly one direct appFrame record, retarget canonical payload placement to that frame, reject duplicate/missing frames or identities, and preserve deterministic generated order. Do not let progressive intermediate frames mutate canonical records; one complete final map is the sole commit.

Keep the renderer's cancellation and pre-commit revision recheck. The parent must atomically prove the complete range delta, saved project/layer capacity, current canonical revision, stable/fresh identity rules, valid PNG payloads, unchanged out-of-range records, unchanged interpolation enabled state, and no unrelated mutation before replacement. Any stale/cancelled/invalid/over-capacity/mismatched result restores the Plan 21 complete snapshot and leaves no partial records, cache, launch context, or history.

Replace Studio's accepted mirror with the accepted canonical physical document/revision from the exact coordinator result. Install/mirror only after parent acceptance, refresh canonical physical projection/cache through existing store APIs, update launch context only through `rotoPhysical`, and register accepted PNGs through the Plan 23 canonical helper as needed. Remove Play-specific writes to legacy frame caches/background/interpolation launch fields. Preserve Copy/Apply controller wiring and the Plan 25 dialog.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const play=fs.readFileSync('app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts','utf8');const hook=fs.readFileSync('app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts','utf8');const studio=fs.readFileSync('app/src/components/physic-paint/PhysicsPaintStudio.tsx','utf8');if(hook.includes('replace-roto-key-frames'))throw new Error('Play hook still commits legacy frames');if(/mirrorAccepted[\s\S]*replaceRotoKeyFrames/.test(studio))throw new Error('Studio still mirrors legacy Play frames');for(const token of ['keyId','appFrame','isRotoPngDataUrl'])if(!play.includes(token))throw new Error('Play physical publication missing '+token);if(!studio.includes('rotoPhysical'))throw new Error('Studio missing canonical Play launch mirror');" &amp;&amp; git diff --check -- app/src/types/physicPaint.ts app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/lib/physicPaintBridge.ts</automated>
  </verify>
  <done>The complete Play range commits once as canonical physical records with preserved/reused or once-allocated stable identities and valid PNG payloads; parent acceptance is atomic; Studio mirrors only the accepted physical document; save/reopen/render consumers can reach the payloads; legacy Play commit/mirror and dual writes are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Play renderer → physical proposal | Progressive generated images become a single complete candidate only at final commit. |
| Child coordinator → parent bridge | Stable/fresh identities, PNG payloads, range, capacity, and revisions cross the authority boundary. |
| Parent acceptance → local mirror/persistence | Only the accepted physical document may update store, launch context, and downstream consumers. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-26-01 | Spoofing | Destination keyId | high | mitigate | Preserve authoritative existing IDs and accept only declared once-allocated fresh IDs for empty destinations. |
| T-36.14-26-02 | Tampering | Complete Play record map | high | mitigate | Parent independently proves range delta, out-of-range equality, payload validity, capacity, and revision before mutation. |
| T-36.14-26-03 | Repudiation | Final Play commit | high | mitigate | One explicit operation ID/kind and exact acknowledgement identify the sole accepted complete publication. |
| T-36.14-26-04 | Information Disclosure | Generated PNG/error detail | low | accept | Preserve existing user error/status and LOG routing; add no payload logging. |
| T-36.14-26-05 | Denial of Service | Large Play range | medium | mitigate | Preserve capacity/count guards, cancellation, progressive staging, and one final bounded commit. |
| T-36.14-26-06 | Elevation of Privilege | Legacy direct apply/mirror | high | mitigate | Route through the sole coordinator and remove Play-specific legacy frame writes and launch mirrors. |
| T-36.14-26-SC | Tampering | Package supply chain | low | accept | Existing code/helpers only; no package action. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Script-produced paint agrees across save/reopen/render | Complete physical records carry durable PNGs to canonical consumers | COVERED |
| REQ | PHYSICAL-IDENTITY | Existing IDs preserved; new IDs allocated once; direct appFrame payloads | COVERED |
| REQ | DERIVED-INTERPOLATION | Enabled state unchanged; Play adds real records rather than generated timing truth | COVERED |
| REQ | ATOMIC-FRAME-MAPPING | One coordinator and parent-validated complete range transaction | COVERED |
| REQ | DOWNSTREAM-PARITY | Accepted physical document reaches store, persistence, hydration, playback, Preview, Export | COVERED |
| REQ | UI-INTEGRATION | Existing Plan 25 controller/dialog remains the trigger/status surface | COVERED |
| REQ | UAT-THEN-REGRESSION | Static checks precede native Play validation | COVERED |
| RESEARCH | Stable physical records, exact parent acceptance, no dual authority | Tasks 1-2 implement all three | COVERED |
| CONTEXT | D-01 / D-03 / D-04 / D-09 / D-10 / D-12 | Stable identity, clean cutover, separate Motion, atomic transaction, downstream parity, UAT-first | COVERED |
| CONTEXT | Deferred separate Play workflow/migration | Neither is introduced | EXCLUDED — DEFERRED |

<verification>
1. Confirm only the six listed production files and summary change.
2. Confirm Play has an explicit physical operation and uses the sole coordinator/parent bridge.
3. Confirm complete range identity rules, valid PNG payloads, out-of-range equality, capacity, and revision are parent-validated before mutation.
4. Confirm Studio mirrors only exact accepted `rotoPhysical` state and no Play-specific legacy frame/cache/background/interpolation write remains.
5. Confirm Copy/Apply Script, durable library, deterministic Motion, progressive rendering, cancellation, and Plan 25 UI remain unchanged.
6. Run only static commands; no tests, typecheck, build, server, browser, or native application.
</verification>

<success_criteria>
- The cache/publication root cause of G-36.14-13 is fixed in production.
- Accepted Play Script output is canonical, identity-stable, PNG-backed, atomic, durable, and reachable by all physical consumers.
- No legacy frame publication, dual write, second transaction authority, compatibility layer, or hidden history decision remains.
- The result is ready for user-owned native Play validation without claiming live success or phase completion.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-26-SUMMARY.md` when done. Record operation and proposal interfaces, stable/fresh identity algorithm, parent semantic validation, exact acknowledgement, accepted physical mirror, removed legacy paths, preserved Script behavior, static commands, and native-UAT readiness only.
</output>
