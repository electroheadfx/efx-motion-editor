---
phase: 36.14
slug: physics-paint-roto-timeline-ui-from-pencil
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-19
---

# Phase 36.14 — Validation Strategy

> Active recovery validation contract for Plans 21-29. This contract supersedes the rejected Plan 12 approval path without rewriting its historical record.

---

## Active Validation Boundary

| Property | Active contract |
|----------|-----------------|
| **Recovery plans** | Plans 21-27 production fixes, Plan 28 bounded static review, Plan 29 user-owned native UAT |
| **Automated pre-UAT evidence** | Only the bounded source-level and artifact-level commands written in Plans 21-28, plus `git diff --check` |
| **Executable gates before native UAT** | None: no tests, test discovery, typecheck, build, package command, server, browser, Vite, Tauri, or native app |
| **Native ownership** | The user alone launches and operates the native application in Plan 29 |
| **Post-UAT automation** | Not authorized by this recovery contract; any regression, typecheck, or build work requires separate follow-up planning after the native result |
| **Approval artifacts** | Prohibited; do not create `36.14-12-APPROVAL.txt`, a replacement TXT, or any token file |
| **Locked plans** | Plans 13-18 remain unchecked, unexecuted, and outside the recovery sequence |

---

## D-29 Occupied-Boundary Authority

D-29 is the active occupied-boundary Drag decision. Starting from `A@1,B@3,C@5,D@8`, releasing B on D's visible `after-key` caret must produce exactly `A@1,C@5,D@8,B@9`.

The occupied `before-key`/`after-key` branch removes only the moved stable `keyId`, leaves every survivor at its original direct `appFrame`, re-resolves the destination by the target stable `keyId`, and performs destination-only insertion/ripple. D remains present and is never overwritten; every payload remains owned by its original `keyId`.

Empty/generated `physical-cell` Drag remains the existing source-closing direct-destination behavior. Its accepted frame-6 oracle remains `A@1,C@4,B@6,D@8`.

### Rejected historical evidence

`A@1,C@4,D@7,B@8` is retained only as the rejected historical occupied-boundary contract from D-07/Plan 12. It is not an active acceptance result and must not drive implementation, static review, or native UAT. The previously observed `A@1,C@4,D@7,B@9` remains evidence of an adjacent direct-cell release, not accepted occupied-boundary behavior.

---

## Mandatory Recovery Sequence

1. Plans 21-27 implement the diagnosed production root-cause fixes while preserving stable `keyId`, direct `appFrame`, canonical `rotoPhysical` authority, one coordinator/parent transaction, and all intentional pre-gap working-tree fixes.
2. Each production plan runs only its written bounded static verification. No plan creates, modifies, discovers, or runs tests, and no executable compile or application gate runs.
3. Plan 24 records D-29 across CONTEXT, ROADMAP, UI-SPEC, UAT, this VALIDATION contract, and the occupied-boundary resolver branch before renewed native judgment.
4. Plan 28 performs a read-only integrated source/artifact audit. It must confirm D-29, the exact `A@1,C@5,D@8,B@9` oracle, preserved empty/generated behavior, the absence of approval transitions, and the locked state of Plans 13-18.
5. Only after Plan 28 reports `READY FOR USER-OWNED GAP UAT`, Plan 29 stops for the user's native scenarios covering G-36.14-1 through G-36.14-10 and G-36.14-13.
6. A Plan 29 pass or rejection is recorded only in the normal Plan 29 summary. Either result stops this recovery sequence; it does not authorize tests, typecheck, build, approval evidence, Plans 13-18, Validation flag promotion, or phase completion.

---

## Static Sampling Contract

- **Plans 21-27:** Run only each plan's targeted source/artifact assertion and scoped `git diff --check` command.
- **Plan 24:** Statically trace distinct occupied-boundary and `physical-cell` resolver paths; token presence alone is insufficient.
- **Plan 25:** Inspect dedicated Play Script selectors for light/dark contrast, Studio/canvas-region full-height containment, internal overflow, and isolation from Scripts-pane and compact confirmation constraints.
- **Plan 27:** Prove the exact `main` label guard occurs before the notification plugin import and every permission/request/send API, with the optional branch fully contained so export success cannot reject.
- **Plan 28:** Read all Plans 21-27 summaries, active source artifacts including this file, and the current diff without modifying repository state.
- **Maximum feedback latency:** Keep every check bounded to the active plan's listed files and avoid broad repository or toolchain execution.

---

## Requirement Verification Map

| Requirement | Active recovery evidence | Native evidence | Status |
|-------------|--------------------------|-----------------|--------|
| 36.14-PHYSICAL-IDENTITY | Stable `keyId`/payload/direct `appFrame` traces across resolver, coordinator, history, persistence, Play publication, and hydration | Plan 29 scenarios A-E | pending |
| 36.14-DERIVED-INTERPOLATION | Canonical acknowledged enabled state and valid strict-interior PNG generation | Plan 29 scenarios A, B, and E | pending |
| 36.14-ATOMIC-FRAME-MAPPING | One complete proposal, exact acknowledgement, complete snapshot rollback/replay, and accepted-only history | Plan 29 scenarios B and C | pending |
| 36.14-RIPPLE-INSERT-DELETE | Existing complete-map production paths plus repaired exact history | Plan 29 scenario C | pending |
| 36.14-RIPPLE-DRAG | D-29 occupied destination-only branch and unchanged empty/generated source-closing branch | Plan 29 scenarios B-D | pending |
| 36.14-FORCE-SPACING | Existing validated complete-map path plus canonical revision agreement and exact history | Plan 29 scenarios B-C | pending |
| 36.14-DOWNSTREAM-PARITY | Canonical physical save/reopen, valid generated PNGs, Play publication, Preview/Export, and guarded optional notification | Plan 29 scenarios E-F | pending |
| 36.14-UI-INTEGRATION | Visible interpolation state, exact occupied caret oracle, and isolated Studio-level full-height light Play surface | Plan 29 scenarios A, D, and F | pending |
| 36.14-UAT-THEN-REGRESSION | Production fixes and bounded static review occur before user-owned native UAT; executable regression/compile work remains outside this recovery contract | Plan 29 exact pass/reject response | pending |

---

## Manual-Only Verification

Plan 29 is the sole active native checkpoint. The user uses a disposable project and performs the complete scenarios in `36.14-29-PLAN.md`/`36.14-UAT.md`. The agent does not launch the app, start a server, drive a browser, run executable gates, infer acceptance from static evidence, or shorten the checklist.

A pass records native gap acceptance only. A rejection records exact scenario-indexed evidence. Neither outcome creates an approval artifact, changes Plan 12's rejected history, advances Plans 13-18, or completes the phase.

---

## Forbidden Transitions

- Do not create `36.14-12-APPROVAL.txt`, `36.14-18-APPROVAL.txt`, or any replacement approval/token artifact.
- Do not mark Plan 12 passed or reinterpret its rejection as approval.
- Do not check off, unlock, execute, edit, or use Plans 13-18 as the next active sequence.
- Do not run tests, test discovery, typecheck, build, package commands, server, browser, Vite, Tauri, or native application from Plans 21-28.
- Do not promote `status`, `wave_0_complete`, or `nyquist_compliant` from a static or native gap result.

---

## Security Verification

- Runtime-validate every bridge field and reconstruct allowlisted plain records.
- Require a complete final identity-to-frame mapping; reject partial mappings.
- Reject duplicate IDs, duplicate frames, invalid ranges, unknown operation kinds, stale revisions, and incomplete maps before mutation.
- Resolve occupied Drag targets by stable `keyId`; never overwrite a target identity or infer it from a shifted frame.
- Match settlement by operation ID, kind, layer, launch context, and expected revision.
- Validate capacity against the complete proposal before optimistic publication.
- Serialize physical edits while one acknowledged operation is pending.
- Restore all snapshot categories and history cursor state on rejection, timeout, transport failure, launch replacement, or disposal.
- Keep child-window notification capability unchanged; skip permission-bound notification APIs outside exact label `main`.

---

## Validation Sign-Off

- [ ] Plans 21-27 have completed their bounded production fixes and static commands without executable validation.
- [ ] D-29 is present in active artifacts and the occupied after-D oracle is exactly `A@1,C@5,D@8,B@9`.
- [ ] The B@8 map appears only under rejected historical evidence, and empty/generated Drag remains `A@1,C@4,B@6,D@8`.
- [ ] Plan 28 has confirmed integrated source reachability, preserved intentional changes, forbidden-transition absence, and locked Plans 13-18.
- [ ] Plan 29 has received a complete user-owned native pass or exact scenario-indexed rejection.
- [ ] No approval TXT, regression/compile gate, later-plan transition, or phase-completion claim has occurred.
- [ ] Frontmatter remains `status: draft`, `wave_0_complete: false`, and `nyquist_compliant: false` throughout this recovery sequence.

**Approval:** pending user-owned Plan 29 native gap UAT; no approval artifact is authorized
