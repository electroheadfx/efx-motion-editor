---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
verified: 2026-07-25T09:50:00Z
status: human_needed
score: 8/9 requirement truths verified (5/5 roadmap success criteria verified)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Decide whether 36.14-UI-INTEGRATION is complete as marked in REQUIREMENTS.md"
    expected: "D-27 Discard Script relocated to the right-panel Scripts toolbar; application selection guard in app/index.html; compact header status capsule replacing the permanent status stack"
    why_human: "Code shows Discard Script still rendered in PhysicsPaintWorkflowStrip.tsx:919 (not relocated), no user-select guard in app/index.html, and the permanent physics-paint-roto-status-stack retained — yet REQUIREMENTS.md marks the requirement Complete, Plan 29 records the final Pencil UI integration as a separately deferred future decision, and the user approved the current native UI on 2026-07-24. The governing documents conflict with the spec text; only the user can route this."
  - test: "Decide whether deterministic regression coverage of the locked physical examples is required before phase closure"
    expected: "Automated tests exercising insert-slot, delete-slot, force-spacing, before-key/after-key occupied drag, and source-closing empty-cell drag against the locked examples (A@1,C@5,B@8,D@9 / A@1,C@5,D@8,B@9 / A@1,C@4,B@6,D@8)"
    why_human: "grep finds zero test files referencing move-key, before-key, after-key, insert-slot, delete-slot, force-spacing, or ripple; the 1685-line physical resolver has no direct unit tests. The deferred deterministic drag test obligation from quick 260718-m2f-VERIFICATION is unmet. Behavior was user-approved via native UAT and the full gate is green, so this is a regression-protection decision, not a behavior defect."
---

# Phase 36.14: Deterministic Physical-Frame Roto Timeline Cutover and Final UI Integration — Verification Report

**Phase Goal:** As a stop-motion animator, I want every real Roto key, generated in-between, timeline edit, persisted frame, and rendered output to use one deterministic physical-frame authority, followed by a polished timeline UI that exposes the approved behavior without duplicating business logic.
**Verified:** 2026-07-25T09:50:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Scope Notes (per orchestrator context)

- Executable scope = plans 01-12 and 19-30 (all have SUMMARY.md). Plans 13-18 are historical, locked, non-executable under D-30; their missing summaries are not gaps.
- Plan 29 native UAT was user-approved on 2026-07-24 (verbatim: "yes all work now, now all UAT is approved"). Native-visible behavior is treated as user-approved and was not re-tested.
- Plan 30 gate evidence was verified and spot-checked, not fully re-run.
- 36.14-REVIEW.md findings (2 critical, 4 warning) are advisory follow-ups; recorded below as known issues, not phase-goal failures.

## Goal Achievement

### Observable Truths (roadmap success criteria — the contract)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Every durable real key has one stable identity and one physical frame; obsolete count/projection/spacing paths removed from authority; interpolation persists only enabled state and renders exact physical interiors | ✓ VERIFIED | `physicsPaintRotoPhysicalModel.ts` (736 lines): `PhysicPaintRotoRealKeyRecord` (keyId + appFrame), `PhysicPaintRotoInterpolationState = { enabled, mode }` only, separate `PhysicPaintRotoScriptMotionSettings` (D-04). Resolver doc-comment: "No sourceFrame, displayFrame, inBetweenCount, segment-spacing override". Caveat in Known Issues K4. |
| SC2 | Insert/Delete/Drag/Force Spacing/Undo/Redo use one validated complete identity-to-frame mapping transaction with one stage, one publication, one acknowledgement, complete rollback, one accepted history action | ✓ VERIFIED | `useRotoPhysicalEditCoordinator.ts` (1166 lines): serialized pending op, exact `operationId`/`layerId` acknowledgement matching (lines 187, 530, 652, 726, 816), fail-closed restoration. `useRotoPhysicalEditHistory.ts` (478 lines): accepted-only applied/redo stacks, replay via immutable before-snapshots. |
| SC3 | Ripple Insert/Delete, occupied-key-safe cut-and-insert drag, generated-cell destinations, and Force Spacing N satisfy locked examples and capacity constraints | ✓ VERIFIED (code) | `physicsPaintRotoPhysicalResolver.ts`: `before-key`/`after-key` occupied boundaries remove only the moved identity and ripple at destination (lines 886-919); direct-cell target occupied by another key is rejected (line 886); source-closing empty/generated destination retained (line 849 doc); `force-spacing` validates nonnegative integer N, anchors first key, fails closed on invalid/over-capacity (lines 1107-1146). Native UAT approved all of these. No automated test exercises the locked examples — see human item 2. |
| SC4 | Cache, persistence, reopen, playback, onion/reference, preview, timeline length, missing-frame rendering, and export consume physical positions and runtime-derived interiors | ✓ VERIFIED | `physicPaintPersistence.ts`: closed `OUTPUT_KEYS = layer_id/frames/roto_physical/roto_playback`, `parsePhysicPaintRotoPhysicalDocument` validation. `previewRenderer.ts:100-154` physical branch via `getRotoPhysicalRenderSource`. `frameMap.ts:125` `getRotoPhysicalEndFrame`. `exportRenderer.ts` physical pixel pipeline. Plan 30 transferred all parity tests to direct physical positions (717 passing). UAT approved Duplicate/Blend parity across paint, preview, export, reopen. |
| SC5 | D-30 recovery sequence honored: plans 21-27 bounded static only, plan 28 review, plan 29 user UAT gate, post-approval regression eligibility; plans 13-18 locked; no approval TXT | ✓ VERIFIED | `36.14-12-APPROVAL.txt` confirmed absent. Plans 13-18 have no SUMMARY.md and remain locked in ROADMAP. Plan 29 SUMMARY records exact user approval 2026-07-24. Plan 30 executed only after approval. Plan 29 boundary statement confirms no tests/typecheck/build ran before approval. |

**Roadmap score: 5/5 success criteria verified.**

### Requirement Truths (REQUIREMENTS.md cross-reference)

| Requirement | Status | Evidence |
|-------------|--------|----------|
| 36.14-PHYSICAL-IDENTITY | ✓ SATISFIED | SC1 evidence; payload moves with identity per UAT-approved Insert/Delete/Drag/Undo/Redo. |
| 36.14-DERIVED-INTERPOLATION | ✓ SATISFIED | Interpolation state is `{enabled, mode}`; interiors runtime-derived from adjacent physical keys (model doc-comments + resolver); no leading/trailing generation. |
| 36.14-ATOMIC-FRAME-MAPPING | ✓ SATISFIED | SC2 evidence; Plan 30 ledger re-proved exact-retry idempotency and altered-operation-ID rejection in `physicPaintPlayScriptBridge.test.ts`. |
| 36.14-RIPPLE-INSERT-DELETE | ✓ SATISFIED | Resolver intents + shared keyboard/button routing (`physicsPaintStudioKeyboard.ts:98-101` → same `deleteRotoFrame` as visible button, per Plan 30 Studio test). UAT approved. |
| 36.14-RIPPLE-DRAG | ✓ SATISFIED | D-29 semantics in resolver (before/after boundary, source gap preserved); empty/generated source-closing retained. UAT approved. No automated locked-example tests (human item 2). |
| 36.14-FORCE-SPACING | ✓ SATISFIED | Resolver `force-spacing` (N>=0, anchor first key, capacity fail-closed); UI exposes explicit N input + "Apply force spacing" button (`PhysicsPaintWorkflowStrip.tsx:832-833`, `canApplyForceSpacing` at :277); session-only N. UAT approved. |
| 36.14-DOWNSTREAM-PARITY | ✓ SATISFIED | SC4 evidence. |
| 36.14-UI-INTEGRATION | ? UNCERTAIN (WARNING) | Partially delivered. Control regrouping (navigator, loop/FPS, interpolation, Force Spacing; Mode label removed) and Play Script dialog extraction exist and were UAT-approved. But D-27 Discard relocation is absent (button still at `PhysicsPaintWorkflowStrip.tsx:919`; `PhysicsPaintScriptsPanel.tsx` has no Discard), the `app/index.html` selection guard was never added (only studio-scoped `user-select: none` in `physicsPaintStudio.css:12,1342`), and the permanent `physics-paint-roto-status-stack` (:933-957) remains instead of the corrected-reference compact capsule. Plan 29 SUMMARY explicitly defers "final Pencil UI integration" as a distinct future decision, while REQUIREMENTS.md marks the requirement Complete. Conflict routed to the user (human item 1). |
| 36.14-UAT-THEN-REGRESSION | ✓ SATISFIED | SC5 evidence; Plan 30 final gate green (see spot-checks). |

**Score: 8/9 requirement truths verified; 1 uncertain (UI-INTEGRATION).**

All 9 requirement IDs are marked `[x] Complete` in `.planning/REQUIREMENTS.md` (lines 129-137) and mapped to Phase 36.14. No orphaned requirements: REQUIREMENTS.md assigns no other IDs to this phase. Plan frontmatter requirements (Plan 29: all 9; Plan 30: 5) are a subset of the 9 — fully accounted for.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` | Canonical physical schema/validators | ✓ VERIFIED | 736 lines; closed state keys; no generated-cell or source/display members |
| `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` | Pure resolver for all operation intents | ✓ VERIFIED | 1685 lines; insert/delete/move/force-spacing with fail-closed validation |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` | Single acknowledged transaction | ✓ VERIFIED | 1166 lines; exact ack matching; rollback on rejection/timeout/launch replacement |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` | Accepted-only Undo/Redo | ✓ VERIFIED | 478 lines; replay semantics per D-09 |
| `app/src/lib/physicPaintPersistence.ts` | Closed physical document persistence | ✓ VERIFIED | `roto_physical` closed contract, fail-closed unsafe-path rejection |
| `app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx` | Studio-level Play Script dialog (Plan 25) | ✓ VERIFIED | Exists; `aria-modal` present; tests assert dialog contract |
| `app/index.html` selection guard | D-context integration point | ✗ NOT IMPLEMENTED | No `user-select` rule; part of deferred final UI integration — routed to human item 1, not scored as a must-have failure |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `useRotoTimelineActions` | `useRotoPhysicalEditCoordinator` | `executePhysicalEdit` port (Studio.tsx:448) | ✓ WIRED |
| `useRotoInterpolationController` | coordinator | `set-interpolation-enabled`/`set-interpolation-mode` operation kinds (:53,:68) | ✓ WIRED (Plan 22 enabled-only authority) |
| Keyboard Delete/Backspace | `rotoPhysicalActions.deleteRotoFrame` | `physicsPaintStudioKeyboard.ts:98-101` → same reference as visible button | ✓ WIRED |
| Workflow strip | script clipboard controller | `onCopyRotoScript`/`onApplyRotoScript`/`onDiscardRotoScript` from `rotoScript` controller (Studio.tsx:1022) | ✓ WIRED (presentation consumes controller; no duplicated business logic) |
| Preview/export/frameMap | store physical seams | `getRotoPhysicalRenderSource`, `getRotoPhysicalEndFrame`, `isPhysicalRotoWorkflowLayer` | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Typecheck gate (Plan 30 claim) | `pnpm --filter efx-motion-editor typecheck` | exit 0, tsc --noEmit clean | ✓ PASS |
| Bridge canonical contract (single named file, not full suite) | `npx vitest run src/lib/physicPaintBridge.test.ts` | 31 passed, 1 skipped, 0 failed | ✓ PASS |
| Plan 30 commits exist | `git log` | c97bd5f0, 0289dcf4, 54149fde, a62e85bc, 1ec41371, 55261b76, 39db13bd, c4d9c76f, 59c42f2a, d6169775 all present | ✓ PASS |
| D-30 prohibition: no approval token | `ls 36.14-12-APPROVAL.txt` | No such file | ✓ PASS |
| Full vitest suite | not re-run | Plan 30 SUMMARY records 717 passed / 0 failed in one pass on 2026-07-25; no contradictory evidence found | ? ACCEPTED (per orchestrator scope) |

### Anti-Patterns Found

None blocking. `rotoLaunchHydration.ts` retains a production function that unconditionally throws ("Legacy Roto launch hydration was retired by the physical persistence cutover") — deliberate D-03 fail-closed retirement, confirmed dead by Plan 30 (its test file deleted wholesale). No TBD/FIXME/XXX debt markers found in phase-modified files.

### Known Issues (advisory — routing belongs to the user, per orchestrator)

- **K1 (REVIEW CR-01, critical):** Export resume wipes `outputPath` before the reuse check (`exportEngine.ts:89-110`) — resume path is dead code; resumed exports split frames across directories.
- **K2 (REVIEW CR-02, critical):** Play Script renderer releases a canvas after registering it in the shared alpha-canvas registry (`physicsPaintRotoPlayScriptRenderer.ts:73-80`) — blend-mode generated in-betweens render blank after a Play Script commit until layer rehydration. This touches UAT-approved blend behavior; recommend prioritizing.
- **K3 (REVIEW WR-01..WR-04):** Unbound `mutationLocked.peek` (Studio.tsx:305) silently disables the engine-action mutation-lock guard; keyboard Delete throws uncaught when no real key is selected (`useRotoTimelineActions.ts:545-551`); `completeNavigation` can wedge script-controller busy if the engine is torn down mid-lock; browser-fallback launch serializes full PNG payloads into the window URL.
- **K4 (new, this verification):** Vestigial legacy timing fields remain in production — `inBetweenCount` and `segmentSpacingOverrides` in `types/physicPaint.ts`, `physicPaintStore.ts:49-288`, and the legacy `selectRotoTimelineView` path (`rotoTimelineSelectors.ts:117-176`, consumed by `useRotoTimelineModel.ts:49` and Studio `getModel`). Code comments mark them temporary/non-authoritative; the physical pipeline is authoritative. Strictly, D-03's "every production reader/writer removed" is not yet satisfied.
- **K5 (new, this verification):** Zero automated tests reference the resolver's operation kinds (`insert-slot`, `delete-slot`, `force-spacing`, `before-key`, `after-key`, `move-key`) or the locked D-29/D-07 examples. Regression gate is green but the physical-edit core has no deterministic guards. See human item 2.

### Human Verification / Decisions Required

#### 1. Is 36.14-UI-INTEGRATION complete?

**Test:** Decide; no code action implied by this report.
**Expected (per requirement text + D-27 + CONTEXT):** Discard Script in the right-panel Scripts toolbar; selection guard in `app/index.html`; compact header capsule replacing the permanent status stack; corrected-reference final timeline UI.
**Actual:** None of those three code changes exist; the workflow-strip status stack remains. Plan 29 records the final Pencil UI integration as a separately deferred future decision; REQUIREMENTS.md marks the requirement Complete; the user approved the current native UI on 2026-07-24.
**Why human:** Governing documents conflict with spec text. If the user considers the UAT-approved UI sufficient, this phase's goal is met (the roadmap success criteria — the contract — contain no UI criterion). If the full corrected-reference integration is still owed, a follow-up plan is needed (it is not scheduled in any later phase; phases 36.15/37 were retired).

#### 2. Is deterministic regression coverage of the locked physical examples required before closure?

**Test:** Decide whether to authorize a follow-up test plan for resolver-level coverage.
**Expected:** Automated tests for insert-slot/delete-slot/force-spacing/before-key/after-key/source-closing drag against the locked examples, fulfilling the deferred obligation from quick 260718-m2f-VERIFICATION.
**Why human:** Behavior is user-approved and the gate is green; this is a regression-protection scope decision.

### Gaps Summary

No gaps against the roadmap success criteria: all five are verified in code and gate evidence. The two open items are (a) a documentation/scope conflict on 36.14-UI-INTEGRATION — spec items verifiably absent from code while the requirement is marked complete and the full UI integration was explicitly deferred by Plan 29 — and (b) absent deterministic test coverage of the physical resolver's locked examples. Both are routed to the user as decisions rather than failed truths, consistent with the user-approved native UAT and the D-30 recovery record.

---

_Verified: 2026-07-25T09:50:00Z_
_Verifier: Claude (gsd-verifier)_
