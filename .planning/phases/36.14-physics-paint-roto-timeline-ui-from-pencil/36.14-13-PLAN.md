---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 13
type: execute
wave: 13
depends_on:
  - 36.14-12
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts
  - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-RIPPLE-INSERT-DELETE
  - 36.14-RIPPLE-DRAG
  - 36.14-FORCE-SPACING
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "D-12: No regression file is created, modified, deleted, renamed, or executed until 36.14-12-SUMMARY.md contains the exact approval token `APPROVED 36.14-12: native physical-model UAT passed`."
    - "D-01/D-02/D-03/D-04: Focused regression tests prove stable keyId-owned payload, direct physical appFrame identity, exact runtime strict interiors, no extrapolation, no durable generated timing truth, and separation of Script Motion from interpolation timing."
    - "D-05/D-06/D-09: Insert and Delete tests assert complete literal mappings, payload preservation, deterministic survivor selection, capacity rejection before mutation, and one accepted history action only."
    - "D-07/D-09: Empty/generated whole-cell Drag and occupied before/after Drag tests assert exact complete cut-and-insert proposals, never overwrite an occupied identity, and prove preview and commit consume the same immutable proposal."
    - "D-08/D-09: Force Spacing tests cover N=0, N=2, malformed input, and over-capacity proposals, with the first key anchored and no stage, publication, or history change on rejection."
    - "D-09: Exact acknowledgement matching, one-pending serialization, complete rollback for every failure/lifecycle path, and accepted-only Undo/Redo cursor movement are deterministic and independently asserted."
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts contains direct-physical strict-interior regression cases."
    - "app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts contains stable-identity, complete-map, Insert/Delete, Drag, and Force Spacing regression cases."
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts contains only the bounded proposal-delegation presentation contract needed by this plan, not final UI geometry coverage."
    - "app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts contains exact physical acknowledgement and stale/mismatch settlement regressions."
    - "app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts contains serialization, side-effect ordering, rollback, and accepted-only history/Undo/Redo regressions."
  key_links:
    - "approved physical records -> physical model/resolver public seam -> literal keyId/appFrame/payload assertions"
    - "physical edit intent -> one immutable complete proposal -> identical preview/commit proposal identity"
    - "complete proposal + launch/layer/revision tuple -> coordinator stage/publish -> exact parent acknowledgement"
    - "matching acceptance -> history finalize/cursor movement; every other settlement -> complete rollback with unchanged history"
prohibitions:
  - "Regression edits or Vitest execution before the exact Plan 12 approval token exists."
  - "Production source changes, validation weakening, compatibility aliases, legacy source/display fixtures, migration fixtures, or generated durable timing fixtures."
  - "New test files, a one-off Vitest configuration, watch mode, the full Vitest suite, or server startup."
  - "Expected mappings computed with the production resolver under test instead of independent literal arrays and payload markers."
  - "Final timeline geometry, visual styling, accessibility, or complete UI-integration regression scope owned by Plans 15-17."
---

<objective>
After explicit 36.14-12 approval, add deterministic regression coverage for physical identity, exact interiors, complete-map validation, Insert/Delete, Drag, Force Spacing, acknowledgement matching, serialization, rollback, and accepted-only history using focused `vitest run` commands.

Purpose: Freeze the user-approved direct physical-frame behavior at the pure resolver and acknowledged transaction seams before downstream parity and final UI integration proceed.
Output: Five focused existing Vitest files rewritten or extended with independent literal contracts; no production file, test configuration, package, or server process is changed.
</objective>

<blocked_precondition>
## BLOCKED UNTIL EXPLICIT PLAN 12 APPROVAL

Do not touch any listed regression file and do not invoke Vitest unless `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md` exists and contains this exact token:

`APPROVED 36.14-12: native physical-model UAT passed`

A missing summary, a differently worded approval, a reported defect, or an incomplete Plan 12 result blocks this entire plan. Stop and return the gate failure; do not infer approval from Plan 12 completion, typecheck/build success, prior discussion, or native behavior observed by the executor.
</blocked_precondition>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PLAN-OUTLINE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md

This is deliberately an `execute` regression plan rather than a synthetic RED/GREEN production plan. D-12 requires production implementation, native UAT, and explicit approval before regression work. The approved implementation should satisfy these tests immediately; do not manufacture a failing RED state or change production code to make the plan appear TDD-shaped.
</context>

<assumptions>
- Plans 01-11 have completed and Plan 12 has recorded the exact approval token before execution starts.
- The approved implementation exposes the physical model, resolver, coordinator, and history behavior through public/pure seams created by Plans 01-11. Tests may import those seams from existing regression files, but this plan does not modify their production definitions.
- Delete selection follows the locked deterministic rule already carried by the prior plans: select the original successor after ripple when one survives; otherwise select the previous survivor; deleting the sole key selects no key.
- Capacity assertions use the approved physical-capacity boundary exported by the production contract. Over-capacity fixtures must be constructed independently and must fail before any optimistic local stage.
- Plan 13 owns core physical edits and transaction/history behavior. Plan 14 owns persistence/reopen and downstream render parity plus the full suite. Plans 15-17 own final UI geometry, interaction polish, accessibility, and final UI regression coverage.
- If a focused regression exposes behavior that contradicts approved Plan 12 UAT, stop with the exact failing contract. Do not weaken the expectation, add a compatibility fixture, or patch production in this plan.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Freeze direct physical identity, strict interiors, complete-map validation, and ripple Insert/Delete</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts</files>
  <read_first>
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts
    - app/src/types/physicPaint.ts
  </read_first>
  <action>
First, before editing either test file or running any test command, require the exact Plan 12 approval token from the blocked precondition. If it is absent, stop without changing either file.

After approval, rewrite stale source/display/count/override expectations in the two existing files into direct physical contracts; do not preserve them as compatibility coverage. Exercise the approved public pure model/resolver seams and use independent literal expected arrays rather than deriving expectations with production helpers.

Per D-01/D-03, create compact stable-key fixtures with distinct payload markers and assert exact ordered `[keyId, appFrame, payloadMarker]` tuples before and after operations. Assert duplicate key IDs, duplicate frames, partial survivor maps, omitted identities, unknown identities, non-integer/negative/out-of-range frames, and complete proposals beyond capacity are rejected with no partial proposal and no input mutation. Generated cells must never appear as durable keyed records.

Per D-02/D-04, assert real keys at physical frames `1, 2, 5` derive exactly generated frames `3, 4`; adjacent `1, 2` derive no interior; no frame is generated before the first key or after the last key; disabling interpolation removes generated cells without moving real keys; persisted interpolation input is enabled-only; Script Motion fields are tested only through their separate contract and never as timing inputs.

Per D-05, assert the locked two-step Insert sequence exactly: `1,2,3 -> 2,3,4 -> 2,4,5`. Each Insert shifts the selected identity and every later identity right by one, creates no key, preserves each payload marker with its original keyId, keeps selection on the selected identity at its new appFrame, and rejects capacity overflow before producing a proposal.

Per D-06, cover middle, first, last, and sole-key Delete. Assert the selected identity and its slot disappear, every later survivor shifts left by one, earlier survivors retain position, payload remains attached to survivor keyId, and selection resolves to original successor, otherwise previous survivor, otherwise null. Include toolbar/shortcut intent equivalence at the resolver/action boundary without testing final UI layout.
  </action>
  <acceptance_criteria>
    - Stable IDs and payload markers survive every accepted mapping and never transfer between identities.
    - Strict interiors are exactly `[3, 4]` for physical real keys `[1, 2, 5]`, with no adjacency output or leading/trailing extrapolation.
    - Every malformed, partial, duplicate, unknown, invalid-range, or over-capacity map returns no proposal and leaves input fixtures unchanged.
    - Insert produces the two locked literal mappings and deterministic selection without creating an identity.
    - Delete covers all survivor-selection branches and preserves every surviving identity-owned payload.
    - The two files contain no compatibility fixture that treats source/display projection, count-based timing, spacing overrides, or durable generated entries as valid authority.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md';const token='APPROVED 36.14-12: native physical-model UAT passed';if(!fs.existsSync(p)||!fs.readFileSync(p,'utf8').includes(token))process.exit(1)" &amp;&amp; pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts</automated>
  </verify>
  <done>The approved pure physical authority is protected by deterministic literal tests for identity, exact interiors, complete validation, Insert, Delete, and survivor selection, and the focused two-file command passes.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze complete Drag proposals and Force Spacing outcomes</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  </read_first>
  <action>
Recheck the exact Plan 12 approval token before making this task's edits or invoking its focused command. Extend only the two listed existing test files.

Per D-07/D-09, add literal whole-cell Drag cases for empty and runtime-generated destinations and boundary Drag cases for occupied identities. Starting from `A@1, B@3, C@5, D@8`, assert moving B to direct physical frame 6 resolves exactly to `A@1, C@4, B@6, D@8`; assert moving B after occupied D resolves exactly to `A@1, C@4, D@7, B@8`. Add an occupied-before case that proves destination identity plus side determines insertion, occupied keys are never overwritten, all shifted identities appear in the complete map, and payload markers follow keyId. Include invalid source identity, stale target identity, same-position/no-op, duplicate final frame, and capacity rejection with no proposal.

Exercise the approved preview and commit callers with one resolver proposal object and assert both paths consume the same proposal identity and exact complete mapping rather than independently recomputing timing. The bounded WorkflowStrip source/presentation test may assert delegation, whole-cell versus occupied-boundary target metadata, and proposal identity transport only. Do not lock the final geometry, control order, colors, auto-scroll, focus, pointer polish, or accessibility details owned by Plans 15-17.

Per D-08/D-09, assert Force Spacing with `N=2` maps ordered keys at `1,2,5` to `1,4,7`, while `N=0` makes ordered keys adjacent from the first anchored frame. Cover negative, fractional, nonnumeric, non-finite, and missing input plus an independently constructed over-capacity result. Every invalid case must leave the original complete map, selection, pending state, stage/publish spies, and history spies unchanged. Accepted Force Spacing returns one complete proposal and one operation intent, not pairwise moves.
  </action>
  <acceptance_criteria>
    - Empty/generated direct Drag and occupied before/after Drag are covered through the same resolver contract.
    - The B-to-6 and B-after-D examples assert the exact locked mappings and stable payload ownership.
    - Preview and commit are proven to consume one immutable proposal identity and identical complete map.
    - Force Spacing accepts N=0 and N=2 with exact literal positions and anchors the first identity.
    - Invalid and over-capacity spacing or drag intents cause no proposal, local stage, parent publication, or history mutation.
    - WorkflowStrip coverage remains a bounded delegation contract and does not pre-empt later final UI regression scope.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md';const token='APPROVED 36.14-12: native physical-model UAT passed';if(!fs.existsSync(p)||!fs.readFileSync(p,'utf8').includes(token))process.exit(1)" &amp;&amp; pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>The approved ripple Drag and Force Spacing contracts are protected by exact complete-map, proposal-identity, validation, and no-side-effect regressions, and the focused two-file command passes.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze exact settlement, serialization, complete rollback, and accepted-only history</name>
  <files>app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts, app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts</files>
  <read_first>
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md
    - app/src/components/physic-paint/roto/rotoApplyTransactions.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
    - app/src/types/physicPaint.ts
    - app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts
    - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts
  </read_first>
  <action>
Recheck the exact Plan 12 approval token before making this task's edits or invoking either focused command. Extend only the two listed existing files, using the existing exact-transition and recorded-call-order styles from PATTERNS.

Per D-09, construct one complete physical edit request with independent literal values for operationId, kind, layerId, launchOperationId, projectContextId, expectedRevision, complete keyId-to-appFrame mapping, and selected identity. Assert that settlement occurs only when every required acknowledgement field and accepted revision/result match. Separately vary each tuple field, use stale and duplicate results, and assert they are ignored or fail closed without clearing or settling the current pending operation. Assert a second Insert/Delete/Drag/Force Spacing/Undo/Redo request is rejected or serialized while one operation is pending.

Record exact side-effect sequences for the accepted path: stop/finalize pending paint, capture one immutable complete snapshot, validate, stage locally once, publish once, await exact settlement, then finalize selection/history. Assert there is no second stage, second send, or operation-specific publication path.

For rejection, timeout with deterministic fake timers, transport failure, launch replacement, disposal, stale/invalid settlement, and settlement-handler failure, assert one complete rollback restores physical records, interpolation enabled state, editable states, preview frames, captured frames, dirty frames, live-overlay action counts, editable-frame list, selected keyId, physical cursor, engine state, cached reference, cached repaint base, cache/live-pixel ownership, and history applied/redo cursor state. Use distinct sentinels per category so omissions or cross-category swaps are visible.

Exercise the generic physical history through its public pure/controller seam from these existing integration tests. Matching acceptance adds exactly one immutable before/after command and advances the applied cursor once. Rejection and every failure path add none. Undo and Redo must include historyCommandId, historyDirection, sourceRevision, and targetRevision, replay through the same coordinator, and move their visible cursor only after exact acceptance. Failed Undo/Redo leaves physical state and both cursor stacks unchanged/restored. A newly accepted physical edit clears redo; a rejected/cancelled edit does not. Do not recreate move-only history or inspect hook internals through implementation-coupled mocks when a public transition/controller seam exists.
  </action>
  <acceptance_criteria>
    - Every acknowledgement tuple field is independently mismatch-tested; stale and duplicate settlements cannot settle or replace the active pending operation.
    - Exactly one physical operation may be pending, and accepted execution records one stage, one publication, and one history finalization in the locked order.
    - Rejection, timeout, transport failure, launch replacement, disposal, stale/invalid settlement, and settlement-handler failure restore every named snapshot category.
    - History records and cursor movement occur only after exact acceptance; failed new work and failed Undo/Redo preserve redo and cursor state.
    - Undo/Redo replay the exact stored complete map through the shared coordinator with explicit provenance and no move-only compatibility path.
    - No production validator, coordinator, history module, bridge implementation, or persistence implementation is modified to satisfy these tests.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md';const token='APPROVED 36.14-12: native physical-model UAT passed';if(!fs.existsSync(p)||!fs.readFileSync(p,'utf8').includes(token))process.exit(1)" &amp;&amp; pnpm --dir app exec vitest run src/components/physic-paint/roto/rotoApplyTransactions.test.ts src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts</automated>
  </verify>
  <done>The approved coordinator and generic history are protected against acknowledgement spoofing, overlapping operations, incomplete rollback, and pre-acceptance history movement, and the focused two-file command passes.</done>
</task>

</tasks>

## Artifacts this phase produces

- Updated `app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts` with enabled-only direct physical interpolation tests.
- Updated `app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts` with stable identity, complete-map, Insert/Delete, Drag, and Force Spacing tests.
- Updated `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` with only the Plan 13 resolver-proposal delegation contract.
- Updated `app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts` with exact physical acknowledgement transition tests.
- Updated `app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts` with serialization, side-effect order, complete rollback, and accepted-only history tests.
- Execution summary at `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-13-SUMMARY.md` recording the approval token check and focused command results.

## Multi-Source Coverage Audit

| Source | Item | Coverage | Disposition |
|---|---|---|---|
| GOAL | Post-approved physical-frame authority is deterministic before downstream/UI work | Tasks 1-3 freeze pure model, operation, settlement, rollback, and history behavior | COVERED |
| REQ | 36.14-PHYSICAL-IDENTITY | Task 1 stable keyId/appFrame/payload tuples; Task 2 payload-preserving ripple maps | COVERED |
| REQ | 36.14-DERIVED-INTERPOLATION | Task 1 exact strict interiors, adjacency, disabled state, and no extrapolation | COVERED |
| REQ | 36.14-ATOMIC-FRAME-MAPPING | Task 1 complete validation; Task 3 exact acknowledged transaction and rollback | COVERED |
| REQ | 36.14-RIPPLE-INSERT-DELETE | Task 1 locked Insert sequence and deterministic Delete survivor selection | COVERED |
| REQ | 36.14-RIPPLE-DRAG | Task 2 direct and occupied-boundary cut-and-insert mappings plus proposal identity | COVERED |
| REQ | 36.14-FORCE-SPACING | Task 2 N=0/N=2/invalid/over-capacity outcomes | COVERED |
| REQ | 36.14-UAT-THEN-REGRESSION | Exact approval gate precedes every edit and every focused command | COVERED |
| RESEARCH | Direct physical records and runtime-only generated interiors | Task 1 replaces stale projection fixtures with stable physical fixtures | COVERED |
| RESEARCH | One pure complete-map resolver for all four edit intents | Tasks 1-2 test the shared resolver with literal complete proposals | COVERED |
| RESEARCH | One acknowledged coordinator and one pending operation | Task 3 exact side-effect and serialization tests | COVERED |
| RESEARCH | Immutable complete rollback and accepted-only generic history | Task 3 sentinels cover every snapshot category and cursor transition | COVERED |
| RESEARCH | Fail-closed runtime validation and exact settlement tuple | Tasks 1 and 3 malformed-map and field-by-field acknowledgement tests | COVERED |
| RESEARCH | Focused deterministic Vitest after native approval; no package install/server | Every verify command is approval-gated and file-focused; prohibitions exclude installs/config/server/full suite | COVERED |
| CONTEXT | D-01 through D-04 | Task 1 identity, direct frames, exact interiors, legacy timing removal, Script Motion separation | COVERED |
| CONTEXT | D-05 and D-06 | Task 1 Insert/Delete complete mappings, selection, capacity, history acceptance | COVERED |
| CONTEXT | D-07 | Task 2 direct/occupied Drag and preview/commit proposal identity | COVERED |
| CONTEXT | D-08 | Task 2 Force Spacing validation and exact mappings | COVERED |
| CONTEXT | D-09 | Tasks 1-3 complete validation, one stage/send/ack, rollback, and accepted-only history | COVERED |
| CONTEXT | D-10 | Direct physical position is asserted here; downstream consumer parity remains assigned to Plan 14 | COVERED IN SCOPE; downstream items explicitly scoped to Plan 14 |
| CONTEXT | D-11 | This is the bounded post-UAT regression plan defined by the approved sequential outline | COVERED |
| CONTEXT | D-12 | Hard approval token gate plus focused `vitest run` commands only | COVERED |
| CONTEXT | D-13 through D-28 | Final header/timeline geometry, Force Spacing UI, drag presentation polish, status/LOG, Scripts controls | EXCLUDED — explicitly scoped to Plans 15-17, not a Plan 13 gap |
| CONTEXT | Deferred Ideas | Cache footprint/compression and any other deferred work | EXCLUDED — no deferred idea is implemented |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture -> physical validator/resolver | Untrusted IDs, frames, operation intents, and complete maps must fail closed before mutation. |
| Child operation -> parent acknowledgement authority | A result crosses process authority and must match operation, kind, layer, launch/project context, and revision. |
| Pending coordinator -> local stores/engine/cache | Optimistic state may be visible temporarily and must be fully reversible on every non-accepted outcome. |
| Accepted settlement -> history cursor | Only an exact accepted result may create a command, move Undo/Redo cursors, or clear redo. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-13-01 | Spoofing | Physical acknowledgement transition | high | mitigate | Task 3 varies every acknowledgement identity/revision field independently and proves stale, duplicate, or mismatched results cannot settle the active operation. |
| T-36.14-13-02 | Tampering | Complete keyId-to-appFrame proposal | high | mitigate | Tasks 1-2 inject partial maps, duplicate IDs/frames, unknown identities, invalid ranges, and over-capacity proposals and require no proposal or side effect. |
| T-36.14-13-03 | Denial of Service | Pending physical-edit coordinator | medium | mitigate | Task 3 proves one-pending serialization across Insert/Delete/Drag/Force Spacing/Undo/Redo and deterministic timeout/disposal release through rollback. |
| T-36.14-13-04 | Repudiation | Accepted-only history and replay provenance | medium | mitigate | Task 3 asserts immutable before/after maps plus historyCommandId, direction, sourceRevision, and targetRevision, with cursor movement only after acceptance. |
| T-36.14-13-05 | Information Disclosure | Regression payload fixtures | low | accept | Tests use synthetic payload markers only; no project pixels, credentials, paths, or user content are serialized into fixtures or snapshots. |
| T-36.14-13-06 | Elevation of Privilege | Local optimistic stage versus parent authority | high | mitigate | Task 3 proves local staging cannot finalize state/history without exact parent acceptance and every authority failure restores the pre-edit snapshot. |

No npm, pip, or cargo package installation occurs in this plan, so the package-legitimacy checkpoint is not applicable.
</threat_model>

<verification>
1. Confirm the exact Plan 12 approval token exists before any file modification or test execution.
2. Run each task's focused approval-gated `pnpm --dir app exec vitest run ...` command after that task.
3. Finish with one combined focused command covering only the five Plan 13 files:
   `node -e "const fs=require('fs');const p='.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md';const token='APPROVED 36.14-12: native physical-model UAT passed';if(!fs.existsSync(p)||!fs.readFileSync(p,'utf8').includes(token))process.exit(1)" && pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/roto/rotoApplyTransactions.test.ts src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts`
4. Do not run the full suite, typecheck/build as a substitute for these focused regressions, watch mode, a custom Vitest config, or any server command in this plan. Plan 14 owns downstream parity and the full suite.
5. If any focused test fails because approved production behavior differs from the locked contract, stop and report the exact mismatch without editing production or weakening the test.
</verification>

<success_criteria>
- The exact Plan 12 approval token was verified before the first regression edit and before every Vitest invocation.
- All five listed existing test files contain deterministic independent expectations for their assigned contracts; no new test file exists.
- Stable identity/payload, exact interiors, complete-map validation, locked Insert/Delete behavior, direct and occupied Drag, Force Spacing, proposal identity, exact acknowledgement, one-pending serialization, complete rollback, and accepted-only history/Undo/Redo are covered.
- All task-focused commands and the final combined five-file command pass.
- Production validation, physical implementation, compatibility policy, test configuration, package graph, and server state remain unchanged.
- Plan 14 and Plans 15-17 retain their downstream parity, full-suite, and final UI regression scope.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-13-SUMMARY.md` when done.
</output>
