---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 13
type: execute
wave: 15
depends_on:
  - 36.14-12
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/hooks/useRotoInterpolationController.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.test.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/components/physic-paint/roto/rotoSourceDisplayModel.test.ts
  - app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts
  - app/src/components/physic-paint/roto/rotoKeyTransactions.test.ts
  - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts
  - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts
  - app/src/components/physic-paint/roto/rotoSaveTransactions.ts
  - app/src/components/physic-paint/roto/rotoSaveTransactions.test.ts
  - app/src/components/physic-paint/roto/rotoCacheTransactions.ts
  - app/src/components/physic-paint/roto/rotoCacheTransactions.test.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-RIPPLE-INSERT-DELETE
  - 36.14-RIPPLE-DRAG
  - 36.14-FORCE-SPACING
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "D-12: No regression file is created, modified, deleted, renamed, or executed until the canonical 36.14-12-APPROVAL.txt artifact byte-matches the exact approved token; summary text is never approval authority."
    - "D-01/D-02/D-03/D-04: Final-owner focused tests prove stable keyId-owned payload, direct physical appFrame identity, enabled-only strict interiors, no extrapolation, and Script Motion separation without stale source/display/count/override fixtures."
    - "D-05/D-06/D-07/D-08/D-09: Resolver, controller, coordinator, result-controller, and history tests own all still-valid Insert/Delete/Drag/Force Spacing, acknowledgement, settlement, rollback, and Undo/Redo behavior."
    - "Six obsolete wrapper tests are removed only after their still-valid assertions are transferred to final production-owner tests; after byte-exact Plan 12 approval, useRotoPersistenceIntegration.ts, rotoSaveTransactions.ts, and the dormant rotoCacheTransactions.ts module are deleted together with their obsolete tests, while useRotoInterpolationController.test.ts is rewritten against its surviving enabled-only controller."
    - "The Plan 13 focused matrix passes after stale-test and dormant-module deletion plus rewrite, so Plan 14 can run downstream regressions and the full suite without imports of deleted production modules."
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts and physicsPaintRotoKeyController.test.ts contain direct physical interpolation and resolver-operation contracts formerly obscured by source/display and key-transaction wrappers."
    - "app/src/components/physic-paint/hooks/useRotoInterpolationController.test.ts contains enabled-only interpolation and separate Script Motion regressions."
    - "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, useRotoPhysicalEditHistory.test.ts, and usePhysicsPaintApplyResultController.test.ts contain exact acknowledgement, settlement, rollback, side-effect-order, and accepted-only history contracts transferred from deleted wrappers."
    - "app/src/stores/physicPaintStore.test.ts contains final-owner cache identity, generated-cache filtering, real-cache precedence, and immutable publication contracts transferred before rotoCacheTransactions.test.ts is deleted."
    - "rotoSourceDisplayModel.test.ts, rotoApplyTransactions.test.ts, rotoKeyTransactions.test.ts, useRotoPersistenceIntegration.test.ts, rotoSaveTransactions.test.ts, and rotoCacheTransactions.test.ts no longer exist; useRotoPersistenceIntegration.ts, rotoSaveTransactions.ts, and dormant rotoCacheTransactions.ts are also deleted in the same approval-gated cleanup after valid behavior transfer."
  key_links:
    - "canonical approval artifact -> byte-exact printf/cmp gate -> every Plan 13 edit and Vitest command"
    - "obsolete wrapper assertions -> surviving public owner tests -> approval-gated deletion of stale importing tests and all three production-unimported modules"
    - "complete proposal + launch/layer/revision tuple -> coordinator/result controller -> exact parent acknowledgement -> accepted-only history"
    - "accepted physical cache publication -> physicPaintStore final-owner tests -> stale wrapper deletion -> Plan 14 downstream/full-suite eligibility"
prohibitions:
  - "Regression edits, deletions, or Vitest execution before byte-exact Plan 12 approval evidence passes."
  - "Treating 36.14-12-SUMMARY.md or any other summary as approval authority; summary inspection is supplementary only."
  - "Production source changes other than deleting useRotoPersistenceIntegration.ts, rotoSaveTransactions.ts, and dormant rotoCacheTransactions.ts after approved behavior transfer; validation weakening, compatibility aliases, legacy source/display fixtures, migration fixtures, or generated durable timing fixtures remain prohibited."
  - "Any file outside the seventeen declared files, a one-off Vitest configuration, watch mode, the full Vitest suite, or server startup."
  - "Deleting a stale wrapper test before its still-valid behavior is covered by an assigned final-owner test."
  - "Expected mappings computed with the production resolver under test instead of independent literal arrays and payload markers."
  - "Final timeline geometry, visual styling, accessibility, or complete UI-integration regression scope owned by Plans 15-17."
---

<objective>
After canonical byte-exact 36.14-12 approval evidence, transfer every still-valid stale-wrapper assertion to final production-owner tests, delete six obsolete wrapper tests, delete the now-production-unimported `useRotoPersistenceIntegration.ts` and `rotoSaveTransactions.ts` wrappers plus dormant `rotoCacheTransactions.ts` in the same clean cutover, rewrite the surviving interpolation-controller test for enabled-only interpolation and separate Script Motion, then run a bounded focused `vitest run` matrix.

Purpose: Freeze the user-approved direct physical-frame and acknowledged-transaction behavior while ensuring Plan 14's downstream and full-suite runs cannot encounter stale imports, dormant production modules, or compatibility-era fixtures.
Output: Eight surviving final-owner Vitest files created or updated, six obsolete wrapper test files and three obsolete production modules deleted, and one focused post-cleanup matrix; no other production file, test configuration, package, or server process is changed.
</objective>

<blocked_precondition>
## BLOCKED UNTIL CANONICAL PLAN 12 APPROVAL EVIDENCE

Before reading for modification, editing, deleting, or executing any listed regression file, run this exact deterministic gate:

`PHASE_DIR=.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil; test -f "$PHASE_DIR/36.14-12-APPROVAL.txt" && printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - "$PHASE_DIR/36.14-12-APPROVAL.txt"`

Only a successful byte comparison against `36.14-12-APPROVAL.txt` unlocks this plan. `36.14-12-SUMMARY.md` may be read for executed interface details but is supplementary and cannot establish approval. A missing, extra-byte, differently worded, or pre-existing invalid artifact blocks the entire plan. Stop without touching tests or running Vitest; do not infer approval from plan completion, summaries, typecheck/build success, prior discussion, or observed native behavior.
</blocked_precondition>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PLAN-OUTLINE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-SUMMARY.md

This is deliberately an `execute` regression plan rather than a synthetic RED/GREEN production plan. D-12 requires production implementation, native UAT, and explicit approval before regression work. The approved implementation should satisfy these tests immediately; do not manufacture a failing RED state or change production code to make the plan appear TDD-shaped.
</context>

<assumptions>
- Plans 01-11 have completed and Plan 12 has created the canonical `36.14-12-APPROVAL.txt` artifact only after the exact user response; the byte gate must still be rerun before every Plan 13 edit/delete group and every Vitest command.
- The approved implementation exposes the physical model, resolver, interpolation controller, coordinator, result listener, history, and store behavior through public/pure seams created by Plans 01-11. Plan 10 has already migrated all production callers and cache/live-pixel/navigation/visual responsibilities out of `useRotoPersistenceIntegration.ts` and `rotoCacheTransactions.ts`; tests import final owners directly, and this plan deletes those dormant modules plus `rotoSaveTransactions.ts` without modifying any surviving production definition or recreating any deleted module.
- Delete selection follows the locked deterministic rule already carried by the prior plans: select the original successor after ripple when one survives; otherwise select the previous survivor; deleting the sole key selects no key.
- Capacity assertions use the approved physical-capacity boundary exported by the production contract. Over-capacity fixtures must be constructed independently and must fail before any optimistic local stage.
- Plan 13 owns core physical edits and transaction/history behavior. Plan 14 owns persistence/reopen and downstream render parity plus the full suite. Plans 15-17 own final UI geometry, interaction polish, accessibility, and final UI regression coverage.
- If a focused regression exposes behavior that contradicts approved Plan 12 UAT, stop with the exact failing contract. Do not weaken the expectation, add a compatibility fixture, or patch production in this plan.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Transfer physical-model behavior, rewrite interpolation ownership, and remove projection wrappers</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/hooks/useRotoInterpolationController.test.ts, app/src/components/physic-paint/roto/rotoSourceDisplayModel.test.ts, app/src/components/physic-paint/roto/rotoKeyTransactions.test.ts</files>
  <read_first>
    - `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` through the blocked byte gate before any test-file inspection for modification.
    - `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-03-SUMMARY.md` and `36.14-06-SUMMARY.md` for executed physical model, interpolation controller, resolver, and final key-controller interfaces; these summaries describe interfaces but do not establish approval.
    - `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts`, `physicsPaintRotoPhysicalResolver.ts`, `physicsPaintRotoKeyController.ts`, `app/src/components/physic-paint/hooks/useRotoInterpolationController.ts`, and `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` for final public owners.
    - All six assigned test files to inventory still-valid assertions before deleting either obsolete wrapper test.
  </read_first>
  <behavior>
    - Stable `[keyId, appFrame, payloadMarker]` tuples and literal complete maps cover D-01/D-03 and all accepted/rejected Insert, Delete, Drag, and Force Spacing cases.
    - Real keys at physical frames 1, 2, and 5 derive exactly strict interiors 3 and 4 only when enabled; disabling interpolation moves no real key.
    - The interpolation controller accepts and updates only enabled state, respects the existing script-mutation lock, and never reads or writes Script Motion as timing state.
    - Projection/count/override wrapper behaviors survive only when restated through final physical owners; obsolete wrapper tests are then deleted.
  </behavior>
  <action>
Run the canonical `printf`/`cmp` approval gate before reading these tests for modification, before the first edit/delete, and again before the focused command. If it fails, stop with all six files unchanged.

Per D-01/D-02/D-03/D-04, first transfer every still-valid assertion from `rotoSourceDisplayModel.test.ts` and `rotoKeyTransactions.test.ts` into `physicsPaintRotoWorkflow.test.ts`, `physicsPaintRotoKeyController.test.ts`, or `useRotoInterpolationController.test.ts` according to the final production owner. Use stable-key fixtures, independent literal arrays, direct physical `appFrame`, enabled-only interpolation, strict interiors, and separate Script Motion state. Remove every fixture or assertion involving `sourceFrame`, `displayFrame`, `inBetweenCount`, spacing overrides, minimum-spacing restoration, source/display projection, or compatibility behavior.

Rewrite `useRotoInterpolationController.test.ts` around the surviving controller: enabled transitions only; disabled-to-enabled and enabled-to-disabled outcomes; no movement of real keys; no durable generated records; script-mutation locking that blocks interpolation updates through the final port; and a separate Script Motion sentinel proving deformation/position remain untouched and are never merged into interpolation input. Do not retain count-shaped update calls or reconstruct mixed interpolation settings in fixtures.

Retain the locked D-05 through D-09 resolver/controller coverage: literal Insert `1,2,3 -> 2,3,4 -> 2,4,5`; deterministic Delete survivor selection; direct/generated and occupied before/after Drag including the A/B/C/D mapping; Force Spacing N=0 and N=2; complete-map validation; payload preservation; capacity rejection; and one immutable preview/commit proposal. Keep WorkflowStrip coverage limited to proposal delegation metadata.

Only after the transferred final-owner cases are present, delete `rotoSourceDisplayModel.test.ts` and `rotoKeyTransactions.test.ts`; their deleted production modules must not be imported or recreated.
  </action>
  <acceptance_criteria>
    - The approval artifact byte gate succeeds before this task's inspection-for-change, edits/deletions, and focused Vitest command.
    - `useRotoInterpolationController.test.ts` tests enabled-only interpolation and a distinct untouched Script Motion sentinel, with no mixed timing fixture.
    - Final physical workflow/key-controller tests own every still-valid projection/key-transaction behavior through direct public seams and independent literal expectations.
    - Insert/Delete/Drag/Force Spacing and strict-interior assertions preserve stable identity and payload and reject malformed or over-capacity maps before side effects.
    - `rotoSourceDisplayModel.test.ts` and `rotoKeyTransactions.test.ts` are absent only after transfer, and no surviving assigned test imports their deleted production modules.
  </acceptance_criteria>
  <verify>
    <automated>PHASE_DIR=.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil; test -f "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoSourceDisplayModel.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoKeyTransactions.test.ts &amp;&amp; pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/hooks/useRotoInterpolationController.test.ts</automated>
  </verify>
  <done>Physical model, interpolation, and operation behavior is owned by surviving public seams; the two obsolete projection/key-transaction tests are gone; the focused four-file command passes.</done>
</task>

<task type="auto">
  <name>Task 2: Transfer acknowledgement, settlement, rollback, and history behavior before deleting lifecycle wrappers</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts, app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.test.ts, app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts, app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts, app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts, app/src/components/physic-paint/roto/rotoSaveTransactions.ts, app/src/components/physic-paint/roto/rotoSaveTransactions.test.ts</files>
  <read_first>
    - `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` through the blocked byte gate before any test-file inspection for modification.
    - `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md` and `36.14-09-SUMMARY.md` for executed coordinator, history, result listener, persistence, and rollback ownership; summaries remain supplementary.
    - `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts`, `useRotoPhysicalEditHistory.ts`, `usePhysicsPaintApplyResultController.ts`, `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts`, and `app/src/lib/physicPaintBridge.ts` for final public owners.
    - All six assigned test files plus `useRotoPersistenceIntegration.ts` and `rotoSaveTransactions.ts` to confirm Plan 10 removed every production importer/authority and to map each still-valid stale-wrapper assertion to a final owner before deleting wrappers and tests together.
  </read_first>
  <behavior>
    - Only an exact operation/kind/project/launch/layer/expected-revision/accepted-revision tuple settles the active physical edit.
    - Accepted execution stages and publishes once, finalizes once, and records one history command; every non-accepted path restores the complete snapshot and preserves history/redo/cursor state.
    - Final-owner tests preserve the exact external side-effect ordering formerly asserted by persistence wrappers, including preview-base clearing, background reset, and engine clear where the final rollback owner performs them.
    - Obsolete apply, persistence-integration, and save wrapper tests are deleted only after all still-valid behavior is covered through final owners.
  </behavior>
  <action>
Run the canonical `printf`/`cmp` approval gate before reading these tests for modification, before the first edit/delete, and again before the focused command. If it fails, stop with all six files unchanged.

Per D-09, inventory `rotoApplyTransactions.test.ts`, `useRotoPersistenceIntegration.test.ts`, and `rotoSaveTransactions.test.ts`, then transfer only still-valid behavior to the final owner files. Put actual bridge-result intake, exact tuple mismatch, stale/duplicate/unknown result, accepted revision, timeout, and listener-failure cases in `usePhysicsPaintApplyResultController.test.ts`; put one-pending serialization, finalize/snapshot/stage/publish order, transport/lifecycle failure, complete rollback, and final engine/reference restoration order in `useRotoPhysicalEditCoordinator.test.ts`; put accepted-only command recording, Undo/Redo provenance, redo invalidation, and post-acceptance cursor movement in `useRotoPhysicalEditHistory.test.ts`.

Exercise public ports with independent literal requests and sentinels. Vary operation ID, kind, project context, launch, layer, expected revision, accepted result/revision, `historyCommandId`, direction, source revision, and target revision independently. Assert one pending operation across Insert, Delete, Drag, Force Spacing, Undo, and Redo; one local stage and one parent publication; exact acceptance only; and inert or fail-closed handling for stale, duplicate, mismatched, malformed, old-launch, timeout, transport, disposal, and listener-failure outcomes.

Assert complete rollback of physical records, enabled interpolation, editable/preview/captured/dirty collections, live overlays, editable frame list, selected keyId, physical cursor, engine state, cached reference, repaint base, and identity-owned cache/live pixels. Preserve the still-valid external restoration sequence from the deleted persistence integration test through the final coordinator owner, including `clearPreviewBaseImage`, `resetBackground`, then engine `clear` when that is the executed final-owner contract. Remove obsolete source/display/count/override/save-payload assertions rather than translating them into compatibility tests.

Only after all retained behaviors are present in final-owner tests, confirm no production importer remains and delete `rotoApplyTransactions.test.ts`, `useRotoPersistenceIntegration.ts`, `useRotoPersistenceIntegration.test.ts`, `rotoSaveTransactions.ts`, and `rotoSaveTransactions.test.ts` together. This is the deferred clean cutover from Plan 09: do not leave a forwarding export, alias, compatibility shim, duplicate transaction, or second authority, and do not recreate wrapper internals when a final public port exists.
  </action>
  <acceptance_criteria>
    - The approval artifact byte gate succeeds before this task's inspection-for-change, edits/deletions, and focused Vitest command.
    - Every acknowledgement tuple field and accepted revision is independently mismatch-tested through the actual result listener and coordinator ports.
    - Accepted execution has exactly one stage, publication, settlement, and history finalization; every named failure restores the complete snapshot.
    - History and cursor transitions occur only after exact acceptance, with Undo/Redo provenance and redo preservation/invalidation covered.
    - Final-owner tests retain the valid external restoration call order without importing obsolete wrapper production modules.
    - The three stale lifecycle/save test files and the two obsolete persistence production wrappers are absent only after behavior transfer and production-importer removal.
  </acceptance_criteria>
  <verify>
    <automated>PHASE_DIR=.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil; test -f "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts &amp;&amp; test ! -e app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoSaveTransactions.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoSaveTransactions.test.ts &amp;&amp; pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.test.ts</automated>
  </verify>
  <done>Acknowledgement, settlement, rollback, side-effect ordering, and accepted-only history are covered at final owners; the three obsolete lifecycle/save tests are gone; the focused three-file command passes.</done>
</task>

<task type="auto">
  <name>Task 3: Transfer cache ownership behavior, delete the dormant cache module and test, and prove the post-cleanup matrix</name>
  <files>app/src/stores/physicPaintStore.test.ts, app/src/components/physic-paint/roto/rotoCacheTransactions.ts, app/src/components/physic-paint/roto/rotoCacheTransactions.test.ts</files>
  <read_first>
    - `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` through the blocked byte gate before any test-file inspection for modification.
    - `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md` for executed physical cache/store ownership; the summary is supplementary only.
    - `app/src/stores/physicPaintStore.ts`, `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts`, and the assigned dormant module plus test files for final public cache behavior and stale assertions.
  </read_first>
  <behavior>
    - Real rendered/editable/live/alpha cache state follows stable key identity and accepted revision through physical reindexing.
    - Runtime generated cache covers exact strict interiors only, is tied to adjacent anchor identities/revisions, and never overrides accepted real-key cache ownership.
    - Stale asynchronous publication and malformed/generated durable candidates publish nothing; accepted complete publication invalidates once.
    - The dormant cache transaction module and its obsolete test are deleted together only after these still-valid behaviors exist in the store's final-owner test and production reachability remains zero.
  </behavior>
  <action>
Run the canonical `printf`/`cmp` approval gate before reading these tests for modification, before editing/deleting, before the cache-focused command, and before the final combined Plan 13 command. If it fails at any point, stop without further changes or test execution.

Transfer every still-valid assertion from `rotoCacheTransactions.test.ts` to `physicPaintStore.test.ts` through the actual final store API per D-01/D-02/D-03/D-09. Cover immutable cache replacement, stable identity ownership through physical moves/reopen, exact revision binding, generated strict-interior filtering, real-cache precedence, stale async rejection, malformed/partial candidate rejection, dirty-state behavior, and one monotonic `physicPaintVersion` invalidation per accepted complete publication. Use no source/display/count/override fixture and never make `physicPaintVersion` content identity.

Confirm `rotoCacheTransactions.ts` still has zero non-test production importers. Delete the dormant module and `rotoCacheTransactions.test.ts` together only after the final-owner cache cases are present. Then rerun the approval byte gate and execute the eight surviving Plan 13 test files together. This combined command is the sole Plan 13 completion matrix; do not run the full suite, which remains Plan 14 ownership.
  </action>
  <acceptance_criteria>
    - The approval artifact byte gate succeeds before cache edits/deletion and before both Vitest commands.
    - `physicPaintStore.test.ts` owns the valid immutable cache, identity/revision, generated filtering, real precedence, stale publication, dirty-state, and invalidation contracts.
    - `rotoCacheTransactions.ts` and `rotoCacheTransactions.test.ts` are absent together, and no stale source/display/count/override cache fixture remains in the eight surviving Plan 13 files.
    - All six obsolete wrapper tests are absent, the interpolation-controller test is rewritten, and the final eight-file focused matrix passes.
    - No production file, test configuration, package manifest, lockfile, server, watch process, or full-suite command is touched.
  </acceptance_criteria>
  <verify>
    <automated>PHASE_DIR=.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil; test -f "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoSourceDisplayModel.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoApplyTransactions.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoKeyTransactions.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts &amp;&amp; test ! -e app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoSaveTransactions.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoSaveTransactions.test.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoCacheTransactions.ts &amp;&amp; test ! -e app/src/components/physic-paint/roto/rotoCacheTransactions.test.ts &amp;&amp; test -e app/src/components/physic-paint/hooks/useRotoInterpolationController.test.ts &amp;&amp; pnpm --dir app exec vitest run src/stores/physicPaintStore.test.ts &amp;&amp; printf '%s\n' 'APPROVED 36.14-12: native physical-model UAT passed' | cmp -s - "$PHASE_DIR/36.14-12-APPROVAL.txt" &amp;&amp; pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoWorkflow.test.ts src/components/physic-paint/roto/physicsPaintRotoKeyController.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/hooks/useRotoInterpolationController.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.test.ts src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <done>Cache behavior is protected at the store owner, the dormant cache module and all six obsolete wrapper tests are deleted, the interpolation-controller test is rewritten, and the approval-gated eight-file Plan 13 matrix passes.</done>
</task>

</tasks>

## Artifacts this phase produces

- Updated `physicsPaintRotoWorkflow.test.ts`, `physicsPaintRotoKeyController.test.ts`, and `PhysicsPaintWorkflowStrip.test.ts` with direct physical interpolation, operation, and bounded proposal-delegation contracts.
- Rewritten `useRotoInterpolationController.test.ts` with enabled-only interpolation, mutation-lock, and separate Script Motion assertions.
- Created or updated `useRotoPhysicalEditCoordinator.test.ts`, `useRotoPhysicalEditHistory.test.ts`, and `usePhysicsPaintApplyResultController.test.ts` with final-owner acknowledgement, settlement, side-effect ordering, complete rollback, and accepted-only history coverage.
- Updated `physicPaintStore.test.ts` with final-owner cache identity/revision, generated strict-interior filtering, real-cache precedence, stale publication, dirty-state, and monotonic invalidation coverage.
- Deleted `rotoSourceDisplayModel.test.ts`, `rotoApplyTransactions.test.ts`, `rotoKeyTransactions.test.ts`, `useRotoPersistenceIntegration.test.ts`, `rotoSaveTransactions.test.ts`, and `rotoCacheTransactions.test.ts` after transferring their still-valid behavior; deleted the production-unimported `useRotoPersistenceIntegration.ts`, `rotoSaveTransactions.ts`, and dormant `rotoCacheTransactions.ts` modules in the same approval-gated clean cutover.
- Execution summary at `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-13-SUMMARY.md` recording canonical approval-artifact byte checks, the exact deletion/rewrite set, behavior-transfer destinations, focused command results, and the standalone exact completion marker `PLAN 13 STALE REGRESSION CLEANUP PASSED` only after the eight-file matrix passes.

## Multi-Source Coverage Audit

| Source | Item | Coverage | Disposition |
|---|---|---|---|
| GOAL | Post-approved physical-frame authority is deterministic before downstream/UI work | Tasks 1-3 freeze pure model, operation, settlement, rollback, and history behavior | COVERED |
| REQ | 36.14-PHYSICAL-IDENTITY | Task 1 stable keyId/appFrame/payload tuples; Task 2 payload-preserving ripple maps | COVERED |
| REQ | 36.14-DERIVED-INTERPOLATION | Task 1 strict interiors plus enabled-only interpolation-controller rewrite; Task 3 generated-cache filtering | COVERED |
| REQ | 36.14-ATOMIC-FRAME-MAPPING | Task 1 complete validation; Task 2 exact acknowledged transaction, rollback, and history | COVERED |
| REQ | 36.14-RIPPLE-INSERT-DELETE | Task 1 locked Insert sequence and deterministic Delete survivor selection | COVERED |
| REQ | 36.14-RIPPLE-DRAG | Task 1 direct and occupied-boundary cut-and-insert mappings plus proposal identity | COVERED |
| REQ | 36.14-FORCE-SPACING | Task 1 N=0/N=2/invalid/over-capacity outcomes | COVERED |
| REQ | 36.14-UAT-THEN-REGRESSION | Canonical approval-artifact byte gate precedes every edit/delete group and every focused command | COVERED |
| RESEARCH | Direct physical records, enabled-only interpolation, and runtime-only generated interiors | Task 1 replaces projection/key-wrapper fixtures and rewrites the surviving interpolation test; Task 3 transfers cache behavior | COVERED |
| RESEARCH | One pure complete-map resolver for all four edit intents | Task 1 tests the shared resolver with literal complete proposals | COVERED |
| RESEARCH | One acknowledged coordinator and one pending operation | Task 2 transfers wrapper behavior to exact final-owner side-effect and serialization tests | COVERED |
| RESEARCH | Immutable complete rollback and accepted-only generic history | Task 2 sentinels cover every snapshot category, restoration order, and cursor transition | COVERED |
| RESEARCH | Final store owns cache identity/revision and runtime-generated filtering | Task 3 transfers all still-valid cache-wrapper behavior before deletion | COVERED |
| RESEARCH | Delete obsolete wrapper tests rather than preserving imports of removed production modules | Tasks 1-3 delete six tests only after explicit final-owner transfer and rewrite the one surviving controller test | COVERED |
| RESEARCH | Focused deterministic Vitest after native approval; no package install/server | Every verify command byte-gates approval and remains file-focused; prohibitions exclude installs/config/server/full suite | COVERED |
| CONTEXT | D-01 through D-04 | Task 1 identity, direct frames, exact interiors, legacy timing removal, Script Motion separation | COVERED |
| CONTEXT | D-05 and D-06 | Task 1 Insert/Delete complete mappings, selection, capacity, and final-owner history intent | COVERED |
| CONTEXT | D-07 | Task 1 direct/occupied Drag and preview/commit proposal identity | COVERED |
| CONTEXT | D-08 | Task 1 Force Spacing validation and exact mappings | COVERED |
| CONTEXT | D-09 | Tasks 1-3 complete validation, one stage/send/ack, rollback, accepted-only history, and cache publication ownership | COVERED |
| CONTEXT | D-10 | Direct physical position/cache ownership is asserted here; broader downstream consumer parity remains assigned to Plan 14 | COVERED IN SCOPE; downstream items explicitly scoped to Plan 14 |
| CONTEXT | D-11 | User-approved sequencing adds the three dormant production-module deletions to the existing fourteen-file post-UAT cleanup, yielding one bounded seventeen-file clean cutover before Plan 14 | COVERED BY APPROVED RESCOPE |
| CONTEXT | D-12 | Canonical approval-file byte gate plus focused `vitest run` commands only | COVERED |
| CONTEXT | D-13 through D-28 | Final header/timeline geometry, Force Spacing UI, drag presentation polish, status/LOG, Scripts controls | EXCLUDED — explicitly scoped to Plans 15-17, not a Plan 13 gap |
| CONTEXT | Deferred Ideas | Cache footprint/compression and any other deferred work | EXCLUDED — no deferred idea is implemented |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture -> physical validator/resolver | Untrusted IDs, frames, operation intents, and complete maps must fail closed before mutation. |
| Child operation -> parent acknowledgement authority | A result crosses process authority and must match operation, kind, layer, launch/project context, and revision. |
| Pending coordinator -> local stores/engine/cache | Optimistic state may be visible temporarily and must be fully reversible on every non-accepted outcome. |
| Accepted settlement -> history cursor | Only an exact accepted result may create a command, move Undo/Redo cursors, or clear redo. |
| Obsolete wrapper tests and dormant modules -> final production owners | Stale tests or dormant implementations can preserve removed authority, miss current public behavior, or make the later full suite fail by importing deleted modules. |
| Async cache publication -> accepted store ownership | Late or generated cache data must not spoof current stable identity/revision or override real-key cache state. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-13-01 | Spoofing | Physical acknowledgement transition | high | mitigate | Task 3 varies every acknowledgement identity/revision field independently and proves stale, duplicate, or mismatched results cannot settle the active operation. |
| T-36.14-13-02 | Tampering | Complete keyId-to-appFrame proposal | high | mitigate | Tasks 1-2 inject partial maps, duplicate IDs/frames, unknown identities, invalid ranges, and over-capacity proposals and require no proposal or side effect. |
| T-36.14-13-03 | Denial of Service | Pending physical-edit coordinator | medium | mitigate | Task 3 proves one-pending serialization across Insert/Delete/Drag/Force Spacing/Undo/Redo and deterministic timeout/disposal release through rollback. |
| T-36.14-13-04 | Repudiation | Accepted-only history and replay provenance | medium | mitigate | Task 3 asserts immutable before/after maps plus historyCommandId, direction, sourceRevision, and targetRevision, with cursor movement only after acceptance. |
| T-36.14-13-05 | Information Disclosure | Regression payload fixtures | low | accept | Tests use synthetic payload markers only; no project pixels, credentials, paths, or user content are serialized into fixtures or snapshots. |
| T-36.14-13-06 | Elevation of Privilege | Local optimistic stage versus parent authority | high | mitigate | Task 2 proves local staging cannot finalize state/history without exact parent acceptance and every authority failure restores the pre-edit snapshot. |
| T-36.14-13-07 | Tampering | Obsolete wrapper regression ownership | high | mitigate | Tasks 1-3 inventory and transfer still-valid assertions to final production-owner tests before deleting six stale tests and three dormant production modules; the combined matrix proves no deleted-module import remains. |
| T-36.14-13-08 | Spoofing | Cache identity/revision publication | high | mitigate | Task 3 binds accepted cache state to stable key identity and exact revision, preserves real-cache precedence, and rejects stale/generated ownership before publication. |
| T-36.14-13-09 | Repudiation | Native-UAT approval authority | high | mitigate | Every task uses byte-exact `printf`/`cmp` verification of the canonical approval artifact; summaries cannot unlock edits or test execution. |

No npm, pip, or cargo package installation occurs in this plan, so the package-legitimacy checkpoint is not applicable.
</threat_model>

<verification>
1. Before each test-file inspection for modification, edit/delete group, and Vitest invocation, byte-verify `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-12-APPROVAL.txt` with the exact `printf`/`cmp` command from the blocked precondition. Summary inspection is supplementary only.
2. Run each task's focused approval-gated `pnpm --dir app exec vitest run ...` command after transferring behavior and deleting only that task's obsolete wrappers.
3. Confirm all six obsolete test files and all three obsolete production modules are absent, and `useRotoInterpolationController.test.ts` remains present with enabled-only coverage.
4. Finish with the approval-gated combined command covering the eight surviving Plan 13 final-owner files exactly as specified in Task 3.
5. Do not run the full suite, typecheck/build as a substitute, watch mode, a custom Vitest config, or any server command. Plan 14 owns downstream parity and the full suite only after this cleanup succeeds.
6. If a focused test exposes approved production behavior that contradicts the locked contract, stop and report the exact mismatch without editing production, restoring a deleted wrapper, or weakening the test.
</verification>

<success_criteria>
- Canonical Plan 12 approval evidence is byte-verified before every Plan 13 edit/delete boundary and every Vitest invocation; no summary acts as approval authority.
- Seventeen declared files are the complete user-approved Plan 13 clean-cutover scope: the original fourteen regression files plus deletion of the three dormant production modules.
- Six obsolete wrapper tests and the three obsolete production modules are deleted only after their still-valid behavior is transferred to eight surviving final-owner tests and production importers are gone; `useRotoInterpolationController.test.ts` is rewritten rather than deleted.
- No surviving fixture contains source/display/count/spacing-override or compatibility authority.
- Stable identity/payload, enabled-only strict interiors, all four physical edits, complete-map validation, exact acknowledgement, one-pending serialization, complete rollback, accepted-only history/Undo/Redo, and cache identity/revision ownership are covered.
- All task-focused commands and the final combined eight-file command pass; no full-suite command runs in Plan 13.
- Production implementation, test configuration, package graph, lockfiles, and server state remain unchanged.
- Plan 14 is eligible only after `36.14-13-SUMMARY.md` records this deletion/rewrite/transfer set, the successful focused matrix, and the standalone exact marker `PLAN 13 STALE REGRESSION CLEANUP PASSED`; Plans 15-17 retain final UI scope.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-13-SUMMARY.md` when done.
</output>
