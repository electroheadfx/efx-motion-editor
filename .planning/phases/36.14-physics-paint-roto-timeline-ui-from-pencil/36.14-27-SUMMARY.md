---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 27
subsystem: export
status: complete
tags: [tauri, notifications, window-capabilities, export, bounded-static]

requires:
  - phase: 36.14-11
    provides: Canonical physical Roto persistence, Preview/Export parity, and the D-30 UAT-first recovery boundary
provides:
  - Main-window-only export completion notification boundary
  - Child-window early return before notification plugin import or permission APIs
  - Awaited and contained best-effort notification delivery after successful export completion
affects: [36.14-28, 36.14-29, export-notifications, physics-paint-native-uat]

tech-stack:
  added: []
  patterns:
    - Window capability eligibility is checked before dynamically importing an optional Tauri plugin
    - Optional OS notification Promises are awaited inside a dedicated best-effort failure boundary

key-files:
  created:
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-27-SUMMARY.md
  modified:
    - app/src/lib/exportEngine.ts

key-decisions:
  - "Only the Tauri main window may enter the export-completion notification plugin path; every child window returns before plugin import and permission calls."
  - "Notification lookup, permission request, and awaited dispatch remain optional best-effort work after export status is already complete."
  - "The Physics Paint capability remains unchanged; the unrelated notification defect is not treated as a Play Script generation or cache root cause."

patterns-established:
  - "Capability-first optional integration: verify exact window identity before loading or calling permission-bound APIs."
  - "Successful export ownership is independent from notification availability, denial, skipping, or failure."

requirements-completed:
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION

coverage:
  - id: D1
    description: "The efx-physic-paint child returns before notification plugin import, permission checks, permission requests, or notification dispatch, while main-window background notification remains eligible."
    requirement: 36.14-DOWNSTREAM-PARITY
    verification:
      - kind: other
        ref: "Plan 36.14-27 bounded Node source-order assertion and scoped git diff --check"
        status: pass
    human_judgment: true
    rationale: "The user-owned Plan 29 native UAT remains responsible for confirming the child export has no notification capability rejection and the main-window notification remains best effort."
  - id: D2
    description: "Notification failure cannot reject or alter an already successful export because current-window lookup, plugin import, permission calls, and awaited send are contained in one dedicated try/catch after complete status."
    requirement: 36.14-UAT-THEN-REGRESSION
    verification:
      - kind: other
        ref: "Plan 36.14-27 bounded Node assertion confirms complete status, hidden check, try/catch containment, exact guard ordering, and awaited sendNotification"
        status: pass
    human_judgment: true
    rationale: "Visible export completion and notification behavior are intentionally deferred to the user-owned native UAT; this plan provides bounded static readiness only."

duration: 2min
completed: 2026-07-24
---

# Phase 36.14 Plan 27: Main-Window Export Notification Guard Summary

**Export completion notifications now stop at an exact main-window capability boundary before plugin loading, while successful export state remains independent from optional notification delivery**

## Performance

- **Duration:** 2 min
- **Started:** 2026-07-24T06:54:02Z
- **Completed:** 2026-07-24T06:55:34Z
- **Tasks:** 1
- **Files modified:** 1 production file plus this summary

## Accomplishments

- Added `getCurrentWindow().label` lookup after export reaches `status: 'complete'` and while the existing `document.hidden` condition is active.
- Added the exact `if (currentWindowLabel !== 'main') return;` guard before the dynamic notification plugin import and before `isPermissionGranted`, `requestPermission`, or `sendNotification` can be reached.
- Awaited `sendNotification` so its rejection is contained by the same dedicated best-effort `try/catch` as window lookup, plugin import, and permission handling.
- Preserved generated files, cleanup, progress, Preview/Export rendering, error handling, and the authorized main-window notification path without granting the Physics Paint child any new capability.
- Kept the notification capability defect separate from the independently diagnosed Play Script presentation and physical-cache authority failures.

## Task Commits

Each task was committed atomically:

1. **Task 1: Skip notification APIs in the Physics Paint child and preserve main-window delivery** - `de038bde` (fix)

## Files Created/Modified

- `app/src/lib/exportEngine.ts` - Imports the current-window accessor, returns for every non-main window before notification plugin loading, and awaits optional notification dispatch inside the existing best-effort branch.
- `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-27-SUMMARY.md` - Records the bounded static implementation evidence and deferred native-UAT boundary.

## Decisions Made

- The existing `main` capability remains the only notification-authorized window. The `efx-physic-paint` child is excluded in code rather than receiving broader Tauri permissions.
- The exact window-label guard belongs before the dynamic plugin import, not merely before dispatch, so child windows cannot invoke any notification plugin command or permission API.
- Notification remains post-export, background-only, and non-authoritative. Any lookup, import, permission, or dispatch failure is silently contained after export completion.
- Play Script generation and cache publication remain separate root causes and were not modified by this plan.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Deferred Issues

- Native confirmation remains assigned to Plan 29 user-owned UAT under D-30.
- Play Script full-height presentation and canonical physical-map cache publication are separate gap work and were not changed here.

## Known Stubs

None. The modified production branch introduces no placeholder value, TODO, FIXME, skipped test, mock data, or unwired UI source.

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required.

## Verification

Passed only the Plan 27-authorized bounded static verification:

- Ran the exact source-order assertion over `app/src/lib/exportEngine.ts`.
- Confirmed export complete status precedes the existing hidden-document branch.
- Confirmed `const currentWindowLabel = getCurrentWindow().label` precedes the exact non-main return guard.
- Confirmed the guard precedes the dynamic `@tauri-apps/plugin-notification` import and every permission/request/send API reference.
- Confirmed current-window lookup through awaited `sendNotification` is contained by one dedicated `try/catch`.
- Ran scoped `git diff --check -- app/src/lib/exportEngine.ts`.
- Confirmed `app/src-tauri/capabilities/default.json` and `app/src-tauri/capabilities/physics-paint.json` were unchanged.

No tests or test discovery were created, modified, opened, or executed. No typecheck, build, package, server, browser, Vite, Tauri, native application, install, capability, manifest, or lockfile command was run.

## Threat Flags

None. The change narrows an existing permission-bound integration surface and introduces no endpoint, auth path, file-access boundary, schema change, or expanded capability.

## Next Phase Readiness

- Plan 28 can include this main-window-only notification branch in its integrated bounded static read-only review.
- Plan 29 remains the sole user-owned native UAT gate for confirming child export completion without notification permission rejection and preserving main-window best-effort behavior.
- This summary does not claim native acceptance, phase completion, or permission to execute regression, typecheck, build, Plans 13-18, or any approval-artifact path.

## Self-Check: PASSED

- `app/src/lib/exportEngine.ts` exists with the committed Plan 27 guard.
- `36.14-27-SUMMARY.md` exists at the required phase path.
- Task commit `de038bde` exists in Git history.
- The task commit deleted no tracked files.
- Capability files remained unchanged.

---
*Phase: 36.14-physics-paint-roto-timeline-ui-from-pencil*
*Completed: 2026-07-24*
