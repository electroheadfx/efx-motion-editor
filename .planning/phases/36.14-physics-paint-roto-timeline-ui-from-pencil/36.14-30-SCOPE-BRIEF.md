---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 30
created: 2026-07-24
status: authorized-scope
---

# Plan 30 Scope Brief — Bounded Stale-Test Contract Transfer

## Authorization

User decision on 2026-07-24, after Plan 29 native gap UAT approval (commits `d6169775`, `5ca0ca8b`) and after the user explicitly authorized running the deferred executable gates:

> Plan a fresh bounded GSD plan that reviews all 18 failing files, transfers still-valid assertions to the physical contract, deletes obsolete tests, and fixes the unused buildRotoRevision.

This brief is the planning authority for the new plan. It does NOT revive historical Plans 12–18, which remain rejected/locked/non-executable under D-30.

## Measured Gate Baseline (captured 2026-07-24)

- `gate-baseline-typecheck.txt` — 73 `error TS` lines from `pnpm --filter efx-motion-editor typecheck` (tsc --noEmit, exit 2).
- `gate-baseline-vitest.txt` — full `vitest run` output: **18 test files failed / 85 tests failed** (628 passed, 1 skipped, 82 todo).
- `edge-probe-coverage.json` — deterministic edge probe output for the 9 phase requirements (26 applicable edges).
- `@efxlab/efx-physic-paint` package build: **PASS** (verified 2026-07-24).
- App build is blocked only by its `tsc --noEmit` first step; no independent app-build failure is known.

## Failure Clusters (from baseline triage)

1. **Retired legacy launch hydration** — `src/components/physic-paint/roto/rotoLaunchHydration.test.ts`: `hydrateRotoLaunchContext` intentionally throws (retired by physical persistence cutover). Tests exercise a dead contract.
2. **Removed `sourceFrame` field** — `src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts`: 21 TS2353 errors; `RotoScriptSourceSnapshot` no longer has `sourceFrame`.
3. **Launch-level `rotoBackground`** — `src/lib/physicPaintBridge.test.ts` (TS2339) and related persistence/store tests use a removed launch-context field. Canonical runtime background is `rotoPhysical.background`; launch-level `rotoBackground` must NOT be reintroduced.
4. **Pre-physical source/display interpolation semantics** — `src/stores/physicPaintStore.test.ts` (12 failing tests + null-safety TS18047 errors), `src/lib/previewRenderer.test.ts`, `src/lib/frameMap.test.ts`, `src/lib/exportRenderer.test.ts`, `src/lib/physicPaintPersistence.test.ts`, `src/lib/physicPaintPlayScriptBridge.test.ts`.
5. **Controller/hook contract drift** — `useRotoCachedPlayback`, `useRotoReferenceController`, `useRotoPlayScriptController`, `usePhysicsPaintLaunchIntegration`, `physicsPaintLaunchContext`, `physicsPaintRotoPlayScriptController`, `PhysicsPaintStudio` (delete shortcut wiring), `PhysicsPaintWorkflowStrip`, `PhysicsPaintScriptsPanel` tests.
6. **Production code issue (the ONLY authorized production change)** — `src/lib/physicPaintBridge.ts:484`: `buildRotoRevision` declared but never read (TS6133). Remove it (or wire it if triage proves it load-bearing — any such finding must be surfaced, not silently expanded).

## Hard Constraints

- This is a NEW plan (30). Historical Plans 12–18 stay locked; do not consult them as executable scope.
- No compatibility adapters, forwarding wrappers, aliases, TypeScript exclusion, or test-config workarounds. Tests are edited or deleted, never shimmed around.
- No reintroduction of launch-level `rotoBackground` or any source/display compatibility field.
- Preserve the playback-readiness seam (MutationObserver/ResizeObserver dry-canvas bounds + `cached-roto-playback-ready` hide guard) and parent-authoritative settlement if any touched test exercises them.
- Triage each failing assertion individually: **transfer** to the canonical physical contract when the underlying behavior is still a live contract, **delete** when the contract itself was retired. No blanket file deletion without per-file justification recorded in the plan.
- Native behavior is user-approved. Production source changes are limited to the `buildRotoRevision` removal; if triage reveals any other production defect, the plan must route it as a flagged finding for separate authorization, not fix it in place.

## Final Gate (definition of done)

From repo root, all green in one run:

1. `pnpm --filter efx-motion-editor typecheck` — exit 0.
2. `pnpm --filter efx-motion-editor exec vitest run` — 0 failed (skips/todos acceptable only where pre-existing).
3. `pnpm build` — package + app build exit 0.

No native UAT required (test/contract alignment only), unless production code beyond the unused declaration changes — in that case the plan must stop and surface the deviation.
