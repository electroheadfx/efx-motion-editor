---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 22
type: execute
wave: 22
depends_on:
  - 36.14-21
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/hooks/useRotoInterpolationController.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/lib/physicPaintBridge.ts
autonomous: true
gap_closure: true
gap_ids:
  - G-36.14-1
  - G-36.14-4
  - G-36.14-6
  - G-36.14-7
  - G-36.14-8
  - G-36.14-10
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-RIPPLE-DRAG
  - 36.14-FORCE-SPACING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UI-INTEGRATION
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "Per D-02 and D-09, interpolation enabled state changes through one explicit acknowledged canonical physical transaction; child state is not changed before parent acceptance."
    - "The parent validates an unchanged complete real-key record set plus exactly one enabled-state transition against its authoritative physical revision, then returns the normal exact physical acknowledgement."
    - "The Physics Paint interpolation control reads the accepted canonical physical interpolation state only and displays an unmistakable checked/enabled versus unchecked/disabled state on the first activation."
    - "Drag, Force Spacing, Duplicate, Paste, first-paint target creation, and serialized physical actions use the same accepted canonical interpolation revision whether interpolation is enabled or disabled."
    - "Legacy interpolation settings remain isolated to any still-approved non-physical Script Motion behavior and are not read, written, mirrored, or dual-written by the physical interpolation control."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Explicit interpolation-enabled physical operation kind and closed acknowledgement validation."
    - path: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      provides: "Intent-only canonical toggle dispatch with no pre-acknowledgement local mutation."
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Unchanged-record interpolation transaction through the sole pending/snapshot/acknowledgement lifecycle."
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Parent-authoritative enabled-only semantic validation and physical document replacement."
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Controlled, visible canonical enabled state and pending guard."
  key_links:
    - from: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      via: "one user intent carrying only the requested enabled boolean"
    - from: "app/src/components/physic-paint/hooks/useRotoInterpolationController.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      via: "explicit set-interpolation-enabled physical proposal with unchanged records"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "existing replace-roto-physical-map envelope and exact parent acknowledgement"
---

<objective>
Make canonical physical interpolation the only authority used by the Physics Paint interpolation control and every revision-bearing physical action.

Purpose: G-36.14-1, G-36.14-4, G-36.14-6, G-36.14-7, G-36.14-8, and the interpolation portion of G-36.14-10 share one deterministic defect: the child changes canonical state locally while the parent changes only a legacy settings model. D-02 and D-09 require one enabled-only physical authority and one acknowledged transaction.

Output: An explicit enabled-only physical operation, parent-authoritative validation and acknowledgement, a controller that emits intent without local mutation, and a first-click visible control bound to accepted canonical state. No tests or executable build gates run before renewed native UAT per D-12.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
@.planning/debug/uat-36-14-interpolation-revision.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-21-SUMMARY.md

@app/src/types/physicPaint.ts
@app/src/components/physic-paint/hooks/useRotoInterpolationController.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
@app/src/lib/physicPaintBridge.ts
</context>

<scope_constraints>
- Preserve the Plan 21 complete replay contract and all intentional uncommitted working-tree changes.
- Keep canonical real-key records byte-for-byte/semantically unchanged during an interpolation toggle; only physical `interpolation.enabled`, physical revision, accepted selection metadata, and normal invalidation may change.
- Preserve Script Motion deform/position as the separate D-04 contract. Do not route Script Motion through this toggle and do not delete unrelated legacy fields merely because their names overlap historical interpolation.
- Preserve current non-history toggle behavior: this plan changes authority and settlement, not the user's established Undo/Redo command taxonomy.
- Add no local mirror, effect-driven synchronization, copied state, second coordinator, second transaction kind that bypasses the physical map, compatibility alias, migration, forwarding wrapper, or dual write.
- D-12 and the current user decision require production/static work before renewed native UAT. Do not create, modify, discover, or run tests; do not run typecheck, build, server, browser, or native application.
</scope_constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Toggle interpolation once through the acknowledged physical authority</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoInterpolationController.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - `physicPaint.ts` physical operation-kind, apply payload, result, and validator unions.
    - `useRotoInterpolationController.ts` current local canonical setter followed by legacy apply payload.
    - `useRotoPhysicalEditCoordinator.ts` Plan 21 pending/snapshot/send/match/restore lifecycle and operation-kind routing.
    - `PhysicsPaintStudio.tsx` reactive canonical `rotoInterpolationState`, controller composition, and current workflow callback.
    - `physicPaintBridge.ts` canonical physical-map parent validation, replacement, exact result construction, and legacy settings branch.
  </read_first>
  <action>
Per D-02/D-09, add the explicit non-history physical operation kind `set-interpolation-enabled` to the shared request/result validation used by `replace-roto-physical-map`. Extend the existing coordinator proposal seam so this operation carries the complete current canonical real-key records unchanged and a target `PhysicPaintRotoInterpolationState` whose only changed field is `enabled`. It must capture the normal complete before snapshot, compute expected and staged physical revisions, publish once, await the exact parent result, and restore completely on every failure.

Refactor `useRotoInterpolationController` into an intent adapter that accepts the current accepted canonical state and the existing coordinator dispatch. On activation, request the opposite enabled value through `set-interpolation-enabled`; do not call the store's local canonical setter, do not update launch context optimistically outside the coordinator, and do not send the historical settings payload. Repeated activation while the coordinator is pending must be guarded by the existing pending authority.

In the parent bridge, validate the authoritative current physical revision, complete record equality, capacity, launch/layer/project tuple, and an enabled-only interpolation delta before replacing the canonical physical document. Reject any record, keyId, appFrame, payload, count, spacing, Motion, or additional interpolation change. Return the normal discriminated exact physical acknowledgement from parent-authoritative values. Preserve unrelated Script Motion handling and all existing ordinary/replay ledger rules; this toggle must not create an ordinary history command.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/src/types/physicPaint.ts','app/src/components/physic-paint/hooks/useRotoInterpolationController.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','app/src/lib/physicPaintBridge.ts'];for(const file of files){const text=fs.readFileSync(file,'utf8');if(!text.includes('set-interpolation-enabled'))throw new Error(file+' missing canonical interpolation operation')}const controller=fs.readFileSync(files[1],'utf8');if(!controller.includes('executePhysicalEdit'))throw new Error('interpolation controller does not use physical coordinator');" &amp;&amp; git diff --check -- app/src/types/physicPaint.ts app/src/components/physic-paint/hooks/useRotoInterpolationController.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/lib/physicPaintBridge.ts</automated>
  </verify>
  <done>One control activation produces one acknowledged enabled-only canonical physical transaction with unchanged records, exact parent validation, no child pre-mutation or dual write, and the same accepted physical revision on both sides before later Drag/Force Spacing/Duplicate/Paste work.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the control and first-paint/action consumers to accepted canonical state</name>
  <files>app/src/components/physic-paint/hooks/useRotoInterpolationController.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - Task 1 final controller result and coordinator pending/accepted outputs.
    - `PhysicsPaintStudio.tsx` existing reactive `rotoInterpolationState` and intentional first-paint target flow.
    - `PhysicsPaintWorkflowStrip.tsx` interpolation group, checkbox `checked` source, accessibility labels, and disabled/pending presentation.
    - Existing compact control-island colors and focus styles in `physicsPaintStudio.css`.
  </read_first>
  <action>
Pass the accepted canonical `rotoInterpolationState.enabled` value and the coordinator-backed toggle intent from Studio to the workflow strip. Remove the physical control's read dependency on `getRotoInterpolationSettings`. Keep direct Preact derivation from the existing reactive canonical state; add no copied `useState` or synchronization effect.

Render the checkbox as a controlled native checkbox whose first accepted activation is visually unmistakable. Preserve the locked compact interpolation group and accessible checkbox semantics, but add an explicit checked glyph/fill/border treatment and an enabled group state class or data attribute that remains distinguishable from focus, orange current-frame state, and disabled/pending state. While the canonical transaction is pending, guard another toggle without displaying an accepted checked state prematurely; after exact acceptance, the checked state and accessible value update from canonical state.

Confirm first-paint target creation, Drag, Force Spacing, Duplicate, Paste, and serialized physical actions continue reading the same canonical state and revision. Do not alter their accepted semantics in this task. Keep the existing cached-green key styling and other intentional workflow-strip changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const studio=fs.readFileSync('app/src/components/physic-paint/PhysicsPaintStudio.tsx','utf8');const strip=fs.readFileSync('app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx','utf8');const css=fs.readFileSync('app/src/components/physic-paint/physicsPaintStudio.css','utf8');if(!studio.includes('rotoInterpolationState'))throw new Error('Studio missing canonical interpolation state');for(const token of ['checked','onRotoInterpolationEnabledChange'])if(!strip.includes(token))throw new Error('workflow control missing '+token);if(!/interpolation[\s\S]*(enabled|checked|active)/i.test(css))throw new Error('CSS missing visible interpolation enabled treatment');" &amp;&amp; git diff --check -- app/src/components/physic-paint/hooks/useRotoInterpolationController.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx app/src/components/physic-paint/physicsPaintStudio.css</automated>
  </verify>
  <done>The physical interpolation checkbox reads only accepted canonical state, shows a clear checked/enabled treatment after the first accepted activation, guards pending input, and leaves all other physical actions on the same parent-accepted revision without changing Script Motion or introducing copied state.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the physical control has one authority and no legacy synchronization seam</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoInterpolationController.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - Final diffs from Tasks 1-2.
    - `36.14-CONTEXT.md` D-02, D-03, D-04, D-09, and D-12.
    - `.planning/debug/uat-36-14-interpolation-revision.md` exact stale-revision mechanism.
  </read_first>
  <action>
Perform a bounded static call-path audit from the workflow checkbox through the controller, sole physical coordinator, parent bridge, accepted canonical document, and back to controlled presentation. Confirm the parent and child encode the same accepted interpolation boolean before any later physical action can dispatch. Confirm the old physical-control command path and local-first mutation are unreachable from this control, while unrelated Script Motion remains available under D-04.

Inspect the seven-file diff for accidental test imports, copied state/effect synchronization, record changes during toggles, history insertion, second pending authority, compatibility coordinates, aliases, wrappers, migrations, or dual writes. Correct any violation inside the listed files only; if correctness requires another production owner, stop and report the exact missing seam instead of widening scope.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const controller=fs.readFileSync('app/src/components/physic-paint/hooks/useRotoInterpolationController.ts','utf8');const studio=fs.readFileSync('app/src/components/physic-paint/PhysicsPaintStudio.tsx','utf8');const strip=fs.readFileSync('app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx','utf8');if(controller.includes('update-roto-interpolation-settings'))throw new Error('physical interpolation still sends legacy command');if(controller.includes('setRotoPhysicalInterpolationState'))throw new Error('controller still mutates canonical state locally');if(/rotoInterpolationSettings:\s*launchContext\s*\?\s*physicPaintStore\.getRotoInterpolationSettings/.test(studio))throw new Error('Studio still feeds legacy state to physical control');if(strip.includes('inBetweenCount'))throw new Error('physical control revived count timing');" &amp;&amp; git diff --check -- app/src/types/physicPaint.ts app/src/components/physic-paint/hooks/useRotoInterpolationController.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx app/src/components/physic-paint/physicsPaintStudio.css app/src/lib/physicPaintBridge.ts</automated>
  </verify>
  <done>The physical interpolation control has one end-to-end canonical authority, no local-first or legacy command path, no count/timing compatibility state, no history or second transaction authority, and a bounded static proof ready for native validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checkbox intent → physical coordinator | Untrusted repeated UI intent requests an enabled-only physical change. |
| Coordinator proposal → parent bridge | Complete records and target interpolation cross the child/parent authority boundary. |
| Parent acknowledgement → controlled UI | Only exact accepted canonical state may become visible and usable by later edits. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-22-01 | Spoofing | Toggle acknowledgement | high | mitigate | Match operation, launch, layer, project, expected/staged/accepted revisions, selection, and outcome exactly. |
| T-36.14-22-02 | Tampering | Enabled-only proposal | high | mitigate | Parent proves complete record equality and rejects every delta except the requested boolean. |
| T-36.14-22-03 | Repudiation | Physical revision | high | mitigate | Parent returns its accepted revision; later operations derive from that canonical document only. |
| T-36.14-22-04 | Information Disclosure | Failure detail | low | accept | Preserve concise status and existing LOG routing. |
| T-36.14-22-05 | Denial of Service | Repeated checkbox activation | medium | mitigate | Reuse one pending coordinator and guard input until settlement. |
| T-36.14-22-06 | Elevation of Privilege | Legacy settings path | high | mitigate | Remove the physical control's local setter and legacy command dependency; preserve separate Script Motion only. |
| T-36.14-22-SC | Tampering | Package supply chain | low | accept | No package action or dependency change occurs. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One deterministic physical authority | The enabled boolean uses the same canonical document/revision as all physical actions | COVERED |
| REQ | DERIVED-INTERPOLATION | Enabled-only state is accepted once and does not move keys | COVERED |
| REQ | ATOMIC-FRAME-MAPPING / RIPPLE-DRAG / FORCE-SPACING | Later actions no longer fail from child/parent revision divergence | COVERED |
| REQ | UI-INTEGRATION | Controlled first-click visible canonical checkbox state | COVERED |
| REQ | UAT-THEN-REGRESSION | Static production checks precede renewed native UAT | COVERED |
| RESEARCH | One coordinator and parent revalidation | Explicit physical operation reuses both established authorities | COVERED |
| CONTEXT | D-02 / D-03 / D-04 / D-09 / D-12 | Enabled-only state, no count compatibility, separate Motion, acknowledged transaction, UAT-first order | COVERED |
| CONTEXT | Deferred migration/multi-selection | Neither is added | EXCLUDED — DEFERRED |

<verification>
1. Confirm all three task commands pass and only the seven listed production files plus the summary change.
2. Trace one toggle from intent to exact parent acceptance and back to the controlled canonical checked state.
3. Confirm complete real-key records are unchanged by the toggle and the physical revision agrees on child and parent.
4. Confirm the physical control no longer reads/writes the legacy settings command or local-first canonical setter.
5. Confirm Drag, Force Spacing, Duplicate, Paste, first paint, and serialized work still use the existing coordinator after the toggle.
6. Confirm no tests, typecheck, build, server, browser, native process, package action, compatibility path, dual write, or second authority occurs.
</verification>

<success_criteria>
- G-36.14-1 control-state failure and the interpolation revision causes of G-36.14-4, G-36.14-6, G-36.14-7, G-36.14-8, and G-36.14-10 are fixed in production.
- One accepted toggle is visibly reflected after the first activation and later physical edits share the same canonical revision.
- Script Motion remains separate and unchanged.
- The result is statically ready for user-owned native UAT without claiming live success or phase completion.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-22-SUMMARY.md` when done. Record the operation literal, enabled-only parent validation, removed physical-control legacy/local-first paths, controlled visual state, preserved Script Motion and working-tree fixes, static commands, and UAT readiness only.
</output>
