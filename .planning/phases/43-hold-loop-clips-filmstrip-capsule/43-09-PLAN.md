---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 09
type: execute
wave: 4
depends_on:
  - 43-02
  - 43-03
files_modified:
  - app/src/lib/exportEngine.ts
  - app/src/lib/exportEngine.loops.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.rotoLoopClips.test.ts
  - app/src/lib/previewRenderer.ts
  - app/src/lib/previewRenderer.loops.test.ts
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/roto/rotoOnionPreview.ts
  - app/src/components/physic-paint/hooks/useRotoReferenceController.ts
autonomous: true
requirements:
  - HOLD-04
  - HOLD-05

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PNG sequence export is BLOCKED before the first frame renders when any loop in the export range is unresolvable, with: `Export blocked — Loop Clip at frame {S} references a missing source frame ({F}). Repair or unlink the loop, then export again.` where S = the loop's placementStart (D-28)"
    - "Preview/playback of an unresolved loop frame shows a marked, visible placeholder driven by the typed 'linked-unresolved' result — never a blank frame, never a crash, never blocking (D-28, audit finding 3)"
    - "A deliverable never silently contains placeholder frames — export either resolves every loop or refuses to run (D-28)"
    - "A VALID loop exports the same content as preview: for the same document revision and the same project frames, the export-side resolution and the preview-side resolution return the same sourceKeyId and provenance for every frame — finite repeated Progressive cycle, finite Static/Hold cycle, Infinity bounded by current parent end, complete-cycle truncation, partial-cycle truncation, and local real-key override inside a linked range (D-27, audit finding 8)"
    - "Export resolves linked occurrences through the same canonical per-frame seam as preview — one resolver, one typed contract; Infinity is bounded by the current parent end at export time (D-27)"
    - "The placeholder variant is never persisted into the durable cache, never treated as real Paint content by any consumer, and every render-source consumer handles the union exhaustively (audit finding 6)"
    - "Studio display remains non-blocking on unresolved frames (D-28)"
  artifacts:
    - path: "app/src/lib/exportEngine.ts"
      provides: "loop-resolution preflight at startExport before any frame renders; valid-loop export resolution through the canonical seam"
      contains: "Export blocked"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      provides: "loop-placeholder render-source variant aligned with the 43-02 typed 'linked-unresolved' contract"
      contains: "placeholder"
    - path: "app/src/lib/previewRenderer.ts"
      provides: "marked placeholder rendering for unresolved loop frames"
    - path: "app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts"
      provides: "explicit placeholder-variant rejection — never written into durable cache"
  key_links:
    - from: "app/src/lib/exportEngine.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "startExport calls the 43-03 unresolved-loop query across the export range and fails fast; valid exports resolve per frame through the same canonical store seam as preview"
      pattern: "UnresolvedLoops"
    - from: "app/src/lib/previewRenderer.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "placeholder render-source variant rendered as marked placeholder"
      pattern: "placeholder"
  prohibitions:
    - statement: "MUST NOT export blank or placeholder frames silently for unresolved loops (D-28)"
      status: resolved
      verification: test
    - statement: "MUST NOT block preview/playback on unresolved loops — placeholders are visible and non-blocking (D-28)"
      status: resolved
      verification: test
    - statement: "MUST NOT add a surface-specific resolution path for export — the canonical store seam is the only source; preview/export parity is asserted by spec (D-27, audit finding 8)"
      status: resolved
      verification: test
    - statement: "MUST NOT persist the placeholder variant into durable cache or treat it as real Paint content in any consumer (audit finding 6)"
      status: resolved
      verification: test
    - statement: "MUST NOT leave any render-source consumer without explicit union handling — exhaustiveness is compile-time enforced and spec-asserted (audit finding 6)"
      status: resolved
      verification: test
---

<objective>
Implement the D-28 split policy on the shared 43-02 typed contract: preview/playback render a marked placeholder for 'linked-unresolved' frames (visible, non-blocking, never persisted), PNG export runs a loop-resolution preflight that fails fast with the locked error, and a valid loop exports exactly what preview shows (parity proven by spec).

Purpose: a deliverable must never silently contain placeholder frames (HOLD-04's compositing guarantee extended to failure states); Pitfall 8 closes by distinguishing "unresolvable loop" from "empty frame" at the canonical seam; audit finding 8 locks preview/export parity for valid loops; audit finding 6 declares every consumer of the placeholder variant.
Output: export preflight block + valid-loop parity spec + placeholder render-source variant consumed by preview, with every render-source consumer explicitly handled.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-02-SUMMARY.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-03-SUMMARY.md
@app/src/lib/exportEngine.ts
@app/src/lib/previewRenderer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export preflight block (failure path) + valid-loop preview/export parity (success path)</name>
  <files>app/src/lib/exportEngine.ts, app/src/lib/exportEngine.loops.test.ts</files>
  <read_first>
    - app/src/lib/exportEngine.ts (startExport at line 55; resumeExport at 408)
    - app/src/stores/physicPaintStore.ts (getRotoPhysicalUnresolvedLoops from 43-03; getRotoPhysicalRenderSource with the typed linked/linked-unresolved branch)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-27, D-28)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 8)
  </read_first>
  <behavior>
    Failure path (D-28):
    - startExport with an unresolved loop inside the export range fails fast before rendering any frame, surfacing `Export blocked — Loop Clip at frame {S} references a missing source frame ({F}). Repair or unlink the loop, then export again.` with S = loop placementStart and F = the first missing source frame
    - With multiple unresolved loops the error names the first (earliest placementStart); fixing it and re-exporting surfaces the next
    - An unresolved loop entirely outside the export range does not block
    - Infinity loops are checked over the effective export range (bounded by parent end at export time)
    Success path (D-27 parity, audit finding 8) — for the SAME document revision and the same project frames, the export-side resolution equals the preview-side resolution (same sourceKeyId, same provenance) for every frame of:
    - a finite repeated Progressive cycle (e.g. 5f × 5)
    - a finite Static/Hold cycle
    - an Infinity loop bounded by the current parent end
    - a loop truncated on a complete-cycle boundary
    - a loop truncated mid-cycle (partial cycle)
    - a local real-key override materialized inside a linked range (that frame resolves 'real' on both surfaces; neighbors stay linked)
    - Where the existing test seam supports pixels, the resolved Paint raster/output for a frame is compared deterministically between the preview path and the export path (exact dataUrl/byte equality between the two paths — never brittle fixed PNG hashes)
  </behavior>
  <action>
    RED: create app/src/lib/exportEngine.loops.test.ts covering every behavior case — the failure-path cases (block with exact locked message and substituted S/F values, earliest-first naming, out-of-range unresolved loop ignored, infinity bounded by parent end, zero renderer invocations on block) AND the success-path parity cases (for each scenario above: drive the export-side per-frame resolution and the preview-side store resolution for the same document revision and assert identical sourceKeyId/provenance per frame; where the seam exposes rasters, assert deterministic equality BETWEEN the two paths, not against fixed hashes); commit test(43-09). GREEN in exportEngine.ts: (1) at the top of startExport (and resumeExport if it can cross unresolved ranges), query the 43-03 store unresolved-loop API across the export range for every physic-paint layer in the project; on any hit, abort before rendering with the locked error surfaced through the existing export error channel — no new resolution logic for the preflight. (2) Ensure valid loop frames render through the same canonical getRotoPhysicalRenderSource seam preview uses — if the export path currently bypasses it for physic-paint frames, route it through the seam; do NOT add export-specific loop math. Export resolves incrementally per frame with the existing progress/cancellation (D-32) — no range-sized materialization. Commit feat(43-09).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run exportEngine</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run exportEngine exits 0 including the new loop preflight AND parity cases
    - The spec asserts the exact locked error string with substituted frame values
    - The preflight runs before any frame render call (spec asserts zero renderer invocations on block)
    - Each of the six parity scenarios asserts identical sourceKeyId/provenance between the export-side and preview-side resolution for the same document revision; at least one scenario asserts deterministic raster equality between the two paths (path-vs-path comparison, no fixed PNG hashes)
    - The unresolved-loop export-block cases and the valid-loop parity cases are separate, independently named test blocks (failure path vs success path)
  </acceptance_criteria>
  <done>Export refuses to produce a deliverable containing unresolved loop frames with the locked actionable error; a valid loop exports exactly what preview resolves, proven per scenario; no export-specific resolution path exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: typed placeholder variant on the render-source union + store branch + marked preview placeholder</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.rotoLoopClips.test.ts, app/src/lib/previewRenderer.ts, app/src/lib/previewRenderer.loops.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoPhysicalRenderSource union at line 122)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (typed 'linked-unresolved' contract from 43-02 — the variant MUST align with it)
    - app/src/lib/previewRenderer.ts (physic-paint consumption at line 127; loading-placeholder pattern at 566)
    - app/src/components/timeline/TimelineRenderer.ts (PLACEHOLDER_BG_A #1A1A2A / PLACEHOLDER_BG_B #1A2A1A at 66-67, 864)
    - app/src/stores/physicPaintStore.ts (linked-frame branch from 43-03)
  </read_first>
  <behavior>
    - When the per-frame query returns 'linked-unresolved', getRotoPhysicalRenderSource returns a placeholder variant (never null-as-blank) carrying { loopId, appFrame, placementStart, missingSourceKeyIds } — the same information as the 43-02 typed contract, sufficient for the capsule error state, destination-frame placeholder, missing-source tooltip, export preflight, and repair/relink actions
    - previewRenderer renders the placeholder variant as a marked, visible placeholder frame (reusing the existing placeholder fill discipline — alternating #1A1A2A/#1A2A1A pattern or the established loading-placeholder treatment) with a visible marker distinguishing it from an empty frame
    - Preview and playback continue past the placeholder without blocking; the frame counter/scrubber is unaffected
    - An unresolved loop never blanks unrelated frames: neighboring real/generated/linked frames render normally on both sides of the placeholder
  </behavior>
  <action>
    RED: create app/src/lib/previewRenderer.loops.test.ts (placeholder variant renders marked placeholder; playback continues; null-as-blank never produced for unresolved loops; unrelated neighbors render normally) and extend physicPaintStore.rotoLoopClips.test.ts (the linked-unresolved query result maps to the placeholder variant with the full contract payload); commit test(43-09). GREEN: (1) extend the PhysicPaintRotoPhysicalRenderSource union in physicsPaintRotoPhysicalModel.ts with a placeholder variant (name discretion, e.g. kind: 'loop-placeholder') carrying loopId, appFrame, placementStart, missingSourceKeyIds — one contract aligned with the 43-02 typed union, not a parallel shape. (2) In the 43-03 linked-frame branch of getRotoPhysicalRenderSource, return the placeholder variant when the per-frame query returns 'linked-unresolved'. (3) In previewRenderer.ts, render the variant with the marked placeholder treatment — visibly distinct from an empty frame — and never block. Commit feat(43-09).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run previewRenderer physicPaintStore.rotoLoopClips</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run previewRenderer exits 0 with the placeholder cases
    - pnpm --dir app exec vitest run physicPaintStore.rotoLoopClips exits 0 (43-03 branch returns the variant with the full contract payload)
    - The spec asserts the placeholder frame is visually marked (distinct from an empty frame) and that playback continues past it
    - The spec asserts unrelated frames beside an unresolved loop still resolve normally
  </acceptance_criteria>
  <done>Unresolved loop frames are visibly marked in preview/playback through the single typed contract — never blank, never blocking, never poisoning neighbors; export remains the blocking surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: render-source consumer exhaustiveness — placeholder never persisted, Studio non-blocking, no consumer treats it as content</name>
  <files>app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/roto/rotoOnionPreview.ts, app/src/components/physic-paint/hooks/useRotoReferenceController.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts (durable cache write path — the placeholder variant must be rejected here)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (getRotoPhysicalRenderSource consumption at 210, 808, 1077; cell-kind switches at 298-338)
    - app/src/components/physic-paint/roto/rotoOnionPreview.ts (render-source kind check at 107)
    - app/src/components/physic-paint/hooks/useRotoReferenceController.ts (kind checks at 37, 44, 54)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 7 exhaustiveness convention)
  </read_first>
  <behavior>
    - The frame persistence/cache coordinator explicitly rejects the placeholder variant: no durable cache write, no persisted metadata, no cache ownership for that frame
    - PhysicsPaintStudio display handles the placeholder variant explicitly: the current-frame display shows the marked placeholder treatment (or the established non-blocking fallback) and never blocks, never treats it as a real key, and never offers it as key content
    - rotoOnionPreview and useRotoReferenceController handle the variant explicitly: it is skipped by onion/reference projection (kind !== 'real' already excludes it — make the exclusion explicit with a never-fallback exhaustiveness check so a future variant is a compile error)
    - Every consumer of the PhysicPaintRotoPhysicalRenderSource union handles the placeholder variant explicitly; unhandled variants are compile-time errors
  </behavior>
  <action>
    Extend or add colocated specs FIRST (RED): coordinator spec asserts a placeholder-variant frame produces zero durable-cache writes; Studio spec asserts the placeholder display path is non-blocking and never surfaces the frame as key content; onion/reference specs assert the variant is excluded from projections. Commit test(43-09). GREEN: update the four declared consumer files — coordinator: explicit variant arm rejecting persistence writes; PhysicsPaintStudio: explicit variant arm rendering the non-blocking marked placeholder/fallback in the display path; rotoOnionPreview + useRotoReferenceController: explicit exclusion arms with never-fallback exhaustiveness. No other source files may be edited for this behavior — if an additional undeclared consumer surfaces during implementation, STOP and request a bounded plan deviation declaring it (audit finding 6). Commit feat(43-09).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run useRotoFramePersistenceCoordinator PhysicsPaintStudio rotoOnionPreview useRotoReferenceController useRotoPersistenceIntegration && pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The consumer specs exit 0 with the placeholder-variant cases
    - pnpm --dir app run typecheck exits 0 with exhaustiveness switches covering the new variant in every declared consumer
    - The coordinator spec asserts zero durable-cache writes for a placeholder-variant frame
    - The Studio spec asserts non-blocking placeholder display and no treatment as real Paint content
    - grep -rn "loop-placeholder\|placeholder" app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/components/physic-paint/roto/rotoOnionPreview.ts app/src/components/physic-paint/hooks/useRotoReferenceController.ts shows explicit handling in each
    - No source file outside this task's declared list was modified (git diff --stat limited to declared files + colocated specs)
  </acceptance_criteria>
  <done>Every render-source consumer handles the placeholder variant explicitly; the durable cache can never be poisoned by a placeholder; Studio stays non-blocking; union exhaustiveness is compile-time enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document state → deliverable | Export turns project data into user deliverables; unresolved references must halt it |
| placeholder variant → durable cache | A placeholder mistaken for content would poison the cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-09-01 | Tampering | export of unresolved loop | high | mitigate | Preflight fails fast with the locked error before any frame renders; spec asserts zero renderer invocations on block (D-28) |
| T-43-09-02 | Tampering | placeholder persisted as content | medium | mitigate | Placeholder variant explicitly rejected from cache writes in the persistence coordinator; asserted by spec (audit finding 6) |
| T-43-09-03 | DoS | preflight over long export ranges | low | mitigate | Query iterates loop interval records and boundary math only — O(loops + keys in range), no frame materialization (D-32) |
| T-43-09-04 | Tampering | preview/export divergence for valid loops | high | mitigate | Parity spec asserts identical sourceKeyId/provenance per frame between the two surfaces across six scenarios, plus deterministic path-vs-path raster equality where the seam supports it (D-27, audit finding 8) |
| T-43-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run exportEngine previewRenderer physicPaintStore.rotoLoopClips useRotoFramePersistenceCoordinator PhysicsPaintStudio rotoOnionPreview useRotoReferenceController — exit 0
- pnpm --dir app run typecheck — exit 0
- Full suite green
</verification>

<success_criteria>
- Export blocks on unresolved loops with the exact locked error (failure path, separately tested)
- A valid loop exports exactly what preview resolves across all six parity scenarios (success path, separately tested)
- Preview/playback show marked placeholders and continue; unrelated frames unaffected
- The placeholder variant is never persisted and never treated as content; every consumer is exhaustively handled
- Export and preview read the same canonical seam (D-27)
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- Loop-resolution preflight in `startExport` / `resumeExport` (exportEngine.ts)
- Valid-loop export resolution through the canonical store seam (exportEngine.ts — no export-specific loop math)
- Placeholder variant on `PhysicPaintRotoPhysicalRenderSource` aligned with the 43-02 typed 'linked-unresolved' contract (physicsPaintRotoPhysicalModel.ts)
- Store branch returning the placeholder variant (physicPaintStore.ts)
- Marked placeholder rendering (previewRenderer.ts)
- Explicit placeholder handling: cache-write rejection (useRotoFramePersistenceCoordinator.ts), non-blocking Studio display (PhysicsPaintStudio.tsx), exclusion arms (rotoOnionPreview.ts, useRotoReferenceController.ts)
- `exportEngine.loops.test.ts` and `previewRenderer.loops.test.ts` (new specs); consumer spec extensions
