---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 09
type: execute
wave: 4
depends_on:
  - 43-03
files_modified:
  - app/src/lib/exportEngine.ts
  - app/src/lib/exportEngine.loops.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/lib/previewRenderer.ts
  - app/src/lib/previewRenderer.loops.test.ts
autonomous: true
requirements:
  - HOLD-04
  - HOLD-05

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PNG sequence export is BLOCKED before the first frame renders when any loop in the export range is unresolvable, with: `Export blocked — Loop Clip at frame {S} references a missing source frame ({F}). Repair or unlink the loop, then export again.` (D-28)"
    - "Preview/playback of an unresolved loop frame shows a marked, visible placeholder — never a blank frame, never a crash, never blocking (D-28)"
    - "A deliverable never silently contains placeholder frames — export either resolves every loop or refuses to run (D-28)"
    - "Export resolves linked occurrences through the same canonical per-frame seam as preview — export never differs from preview; Infinity is bounded by the current parent end at export time (D-27)"
  artifacts:
    - path: "app/src/lib/exportEngine.ts"
      provides: "loop-resolution preflight at startExport before any frame renders"
      contains: "Export blocked"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      provides: "loop-placeholder render-source variant"
      contains: "placeholder"
    - path: "app/src/lib/previewRenderer.ts"
      provides: "marked placeholder rendering for unresolved loop frames"
  key_links:
    - from: "app/src/lib/exportEngine.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "startExport calls the 43-03 unresolved-loop query across the export range and fails fast"
      pattern: "UnresolvedLoops"
    - from: "app/src/lib/previewRenderer.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "placeholder render-source variant rendered as marked placeholder"
      pattern: "placeholder"
  prohibitions:
    - statement: "MUST NOT export blank or placeholder frames silently for unresolved loops (D-28)"
      status: resolved
      verification: test
    - statement: "MUST NOT block preview/playback on unresolved loops — placeholders are visible and non-blocking (D-28)"
      status: resolved
      verification: test
    - statement: "MUST NOT add a surface-specific resolution path for export — the canonical store seam is the only source (D-27)"
      status: resolved
      verification: judgment
---

<objective>
Implement the D-28 split policy: preview/playback render a marked placeholder for unresolved loop frames (visible, non-blocking), while PNG export runs a loop-resolution preflight that fails fast with the locked error naming the loop and frame.

Purpose: a deliverable must never silently contain placeholder frames (HOLD-04's compositing guarantee extended to failure states); Pitfall 8 closes by distinguishing "unresolvable loop" from "empty frame" at the canonical seam.
Output: export preflight block + placeholder render-source variant consumed by preview.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-03-SUMMARY.md
@app/src/lib/exportEngine.ts
@app/src/lib/previewRenderer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: export preflight — block unresolved loops with the locked error</name>
  <files>app/src/lib/exportEngine.ts, app/src/lib/exportEngine.loops.test.ts</files>
  <read_first>
    - app/src/lib/exportEngine.ts (startExport at line 55; resumeExport at 408)
    - app/src/stores/physicPaintStore.ts (getRotoPhysicalUnresolvedLoops from 43-03; getRotoPhysicalRenderSource)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-28)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 8)
  </read_first>
  <behavior>
    - startExport with an unresolved loop inside the export range fails fast before rendering any frame, surfacing `Export blocked — Loop Clip at frame {S} references a missing source frame ({F}). Repair or unlink the loop, then export again.` with S = loop canonical start and F = the first missing source frame
    - With multiple unresolved loops the error names the first (earliest canonical start); fixing it and re-exporting surfaces the next
    - An export range with all loops resolvable proceeds unchanged
    - An unresolved loop entirely outside the export range does not block
    - Infinity loops are checked over the effective export range (bounded by parent end at export time)
  </behavior>
  <action>
    RED: create app/src/lib/exportEngine.loops.test.ts covering every behavior case (block with exact locked message and substituted S/F values, earliest-first naming, resolvable range proceeds, out-of-range unresolved loop ignored, infinity bounded by parent end); commit test(43-09). GREEN in exportEngine.ts: at the top of startExport (and resumeExport if it can cross unresolved ranges), query the 43-03 store unresolved-loop API across the export range for every physic-paint layer in the project; on any hit, abort before rendering with the locked error surfaced through the existing export error channel. No new resolution logic — the preflight consumes the store query only. Commit feat(43-09).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run exportEngine</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run exportEngine exits 0 including the new loop preflight cases
    - The spec asserts the exact locked error string with substituted frame values
    - The preflight runs before any frame render call (spec asserts zero renderer invocations on block)
  </acceptance_criteria>
  <done>Export refuses to produce a deliverable containing unresolved loop frames, with the locked actionable error; resolvable exports are unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: marked placeholder for unresolved loop frames in preview/playback</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/lib/previewRenderer.ts, app/src/lib/previewRenderer.loops.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoPhysicalRenderSource union at line 122)
    - app/src/lib/previewRenderer.ts (physic-paint consumption at line 127; loading-placeholder pattern at 566)
    - app/src/components/timeline/TimelineRenderer.ts (PLACEHOLDER_BG_A #1A1A2A / PLACEHOLDER_BG_B #1A2A1A at 66-67, 864)
    - app/src/stores/physicPaintStore.ts (linked-loop branch from 43-03)
  </read_first>
  <behavior>
    - When a linked-loop cell's source record is missing, getRotoPhysicalRenderSource returns a placeholder variant (never null-as-blank) identifying the loop and missing source frame
    - previewRenderer renders the placeholder variant as a marked, visible placeholder frame (reusing the existing placeholder fill discipline — alternating #1A1A2A/#1A2A1A pattern or the established loading-placeholder treatment) with a visible marker distinguishing it from an empty frame
    - Preview and playback continue past the placeholder without blocking; the frame counter/scrubber is unaffected
    - Consumers that cannot render the variant (Studio display, cache coordinator) treat it as non-cacheable and skip persistence writes for that frame
  </behavior>
  <action>
    RED: create app/src/lib/previewRenderer.loops.test.ts (placeholder variant renders marked placeholder; playback continues; null-as-blank never produced for unresolved loops); commit test(43-09). GREEN: (1) extend the PhysicPaintRotoPhysicalRenderSource union in physicsPaintRotoPhysicalModel.ts with a placeholder variant carrying { loopId, appFrame, missingSourceFrame } (field names discretion). (2) In the 43-03 linked-loop branch of getRotoPhysicalRenderSource, return the placeholder variant when the source record is missing. (3) In previewRenderer.ts, render the variant with the marked placeholder treatment — visibly distinct from an empty frame — and never block. (4) Audit the other render-source consumers (PhysicsPaintStudio display, useRotoFramePersistenceCoordinator) so the placeholder variant is never written into the durable cache as if it were real content. Commit feat(43-09).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run previewRenderer physicPaintStore.rotoLoopClips</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run previewRenderer exits 0 with the placeholder cases
    - pnpm --dir app exec vitest run physicPaintStore.rotoLoopClips exits 0 (43-03 branch updated to return the variant)
    - The placeholder variant is excluded from durable cache writes (spec or source assertion on the coordinator path)
  </acceptance_criteria>
  <done>Unresolved loop frames are visibly marked in preview/playback, never blank, never blocking, never persisted as real content; export remains the blocking surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document state → deliverable | Export turns project data into user deliverables; unresolved references must halt it |
| placeholder variant → durable cache | A placeholder mistaken for content would poison the cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-09-01 | Tampering | export of unresolved loop | high | mitigate | Preflight fails fast with the locked error before any frame renders; spec asserts zero renderer invocations on block (D-28) |
| T-43-09-02 | Tampering | placeholder persisted as content | medium | mitigate | Placeholder variant excluded from cache writes in the persistence coordinator; asserted by spec |
| T-43-09-03 | DoS | preflight over long export ranges | low | mitigate | Query iterates loops and boundary math only — O(loops + keys in range), no frame materialization (D-32) |
| T-43-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run exportEngine previewRenderer physicPaintStore.rotoLoopClips — exit 0
- Full suite green
</verification>

<success_criteria>
- Export blocks on unresolved loops with the exact locked error
- Preview/playback show marked placeholders and continue
- Export and preview read the same canonical seam (D-27)
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- Loop-resolution preflight in `startExport` / `resumeExport` (exportEngine.ts)
- Placeholder variant on `PhysicPaintRotoPhysicalRenderSource` (physicsPaintRotoPhysicalModel.ts)
- Marked placeholder rendering (previewRenderer.ts)
- `exportEngine.loops.test.ts` and `previewRenderer.loops.test.ts` (new specs)
