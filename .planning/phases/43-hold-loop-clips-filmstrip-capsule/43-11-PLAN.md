---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 11
type: execute
wave: 8
depends_on:
  - 43-08
  - 43-09
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts
  - app/src/lib/frameMap.ts
  - app/src/types/timeline.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
autonomous: false
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-34R/D-35R UI-empty: zero Loop Clips render no rail DOM, focus target, label, placeholder, or layout footprint; the workflow strip remains exactly 161px and the physical row remains 38px."
    - "D-34R/D-47 UI-populated: canonical accepted ranges render as compact 3px segments inside the existing physical row without stacking, another row, or repeated-frame materialization."
    - "D-37R UI-loading: controller preparation/commit keeps accepted rail geometry visible with aria-busy and no optimistic width or fact changes."
    - "D-37R UI-error: unresolved ranges remain visible, selectable, and actionable; rejected operations preserve prior geometry and selection."
    - "D-37R/D-38R UI-partial: truncated loops retain canonical Effective duration and `Loop shortened by next clip`."
    - "D-35R UI-overflow: long rails clip at the existing horizontal viewport, return on shared scroll, and never wrap, stack, add a scrollbar, or change canvas allocation."
    - "D-35R UI-zero-one-many: zero hides the group, one renders one target, and many follow canonical placement order in the same overlay."
    - "D-38R tooltip-overflow and long-text: the tooltip is above the rail, viewport-clamped, accessible, and never falls back to a raw identifier."
    - "D-43 sidebar-empty: with no selected Loop Clip the existing Scripts context and Play Script primary action remain unchanged."
    - "D-43/D-44 sidebar-populated: one selected Loop Clip replaces Play with Pencil/Edit in the same slot and shows display name, source, placement, Cycle, Effective, mode, and status in exact order."
    - "D-43/D-44 sidebar-overflow and zero-one-many: existing sidebar geometry remains unchanged and exactly one shared session selection drives the inspector."
    - "D-42 linked-cell empty/populated: physical cells preserve their accepted palette and blue linked indicator behavior."
    - "D-33R Motion Editor visibility-only boundary: the tracer publishes only `{startFrame, frameCount}` and paints one exact 3px `#8B5CF6` passive strip per effective interval inside the existing PPaint FX bar, with no identity, metadata, text, badge, capsule, own hit target, tooltip, hover/focus, selection, keyboard route, navigation, Edit, drag, context menu, callback, command, or mutation."
    - "D-49: all nine native ownership checks and the separate Issue #2 Key Spacing matrix pass in this same existing checkpoint before Plan 43-12 begins."
    - "D-50 timing authority: ordered real source-key appFrame positions derive normalized offsets, cycle duration, generated interiors, finite/Infinity cadence, rail duration, preview/playback/export, and cache resolution with no timing field or schema migration."
    - "D-50 proxy provenance: any 2+ unique exact source positions from one ordered cycle, selected through original cells or linked occurrences, deduplicate across repeats and every Loop Clip sharing identical ordered sourceKeyIds; the full cycle also works."
    - "D-50 hard walls and atomicity: unselected source positions remain fixed; one accepted Force Spacing transaction moves only selected authoritative real source keys, updates every shared loop rhythm, and Undo/Redo restores/reapplies the whole result."
    - "D-11/D-23 remain fail-closed outside D-50: no single-key or linked drag, ordinary Force Spacing without provenance, generated/gap/unresolved proxy, occurrence materialization, unlink, clone, placement/repeat mutation, or durable proxy state."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx"
      provides: "focused integrated rail target and paint surface"
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "pure derived display name, visible geometry, Cycle/Effective/status projection"
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "contextual selected-loop inspector and Play-to-Edit primary-slot swap"
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts"
      provides: "one shared Studio/session selected-loop identity and focused action ports"
    - path: "app/src/lib/frameMap.ts"
      provides: "interval-only `{startFrame, frameCount}` passive marker projection"
    - path: "app/src/components/timeline/TimelineRenderer.ts"
      provides: "pure Canvas painter for exact 3px `#8B5CF6` strips inside the existing PPaint FX bar"
  key_links:
    - from: "canonical rotoLoopResolutionContext.ranges"
      to: "PhysicsPaintLoopClipRail"
      via: "visible-window projection at the existing frame pitch"
      pattern: "rotoLoopResolutionContext|visibleFrameWindow"
    - from: "shared selected Loop Clip Signal"
      to: "rail highlight and PhysicsPaintScriptsPanel"
      via: "PhysicsPaintStudio view-model props without effect mirroring"
      pattern: "selectedLoopClipId"
    - from: "rail double-click, focused Enter, and sidebar Edit"
      to: "physicsPaintRotoPlayScriptController.openLoopEdit"
      via: "one Studio-local focused port"
      pattern: "openLoopEdit"
  prohibitions:
    - statement: "No separate Loop Clip row, full filmstrip capsule, repetition band, permanent Cycle badge, raw UUID product copy, identity-bearing main-timeline projection, or Loop Clip-specific main-timeline interaction may remain in the tracer surface; the passive interval marker is explicitly allowed."
      status: resolved
      verification: automated-and-human
    - statement: "No Loop Clip placement drag, pointer capture, drag threshold, preview, drop target, or placement mutation is implemented per D-41."
      status: resolved
      verification: automated
    - statement: "No persisted Loop Clip naming field, schema migration, dependency, or alternate mutation path is introduced per D-45/D-46."
      status: resolved
      verification: automated
---

<objective>
Replace the rejected lane/rich-capsule tracer with the approved integrated Loop Rail and contextual Scripts inspector plus the D-33R passive Motion Editor marker, add D-50 timed source-position Key Spacing proxies to this current correction, and block expansion until the user approves all nine visible ownership checks plus the separate Issue #2 matrix in the same existing checkpoint per D-33R through D-50.

Purpose: Prove the corrected ownership boundary end to end: Physics Paint Studio owns Loop Clip identity and interaction, while the Motion Editor receives only canonical effective intervals for passive PPaint FX-bar paint with zero Loop Clip-specific interaction.
Output: Production integrated rail, contextual inspector, passive main-timeline marker projection/painter, authoritative source-offset timing, session-only exact-source proxy selection, one atomic shared-loop Force Spacing path, fresh RED/GREEN tracer contracts, and the existing blocking native approval checkpoint.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-MEMORY-RECALL.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/physicsPaintStudio.css

<interfaces>
Use `physicsPaintRotoPlayScriptController.openLoopEdit(loopId)` as the sole modal-open path. Consume existing compact `rotoLoopResolutionContext.ranges`, `visibleFrameWindow`, and frame pitch. Keep selected Loop Clip identity in one Preact Signal/view-model boundary shared by the workflow strip and Scripts panel; do not mirror it with `useEffect`. Extend the canonical physical resolver—not visual cell heuristics—to expose exact source-position provenance and derive source offsets/cycle duration from real source-key `appFrame` positions. Feed only a validated deduplicated proxy scope into the existing Force Spacing authority/history path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN one production ownership path through all nine tracer checks and Issue #2 timed proxy spacing</name>
  <reversibility rating="costly">D-33R removes duplicate Motion Editor ownership; reversal would restore a second presentation and interaction surface.</reversibility>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts, app/src/lib/frameMap.ts, app/src/types/timeline.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33R through D-49; D-41 remains deferred)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (nine-check matrix and exact geometry)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (physical-row host and cell event ordering)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (existing Play slot and script-name surface)
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (Preact Signal/view-model seam)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (canonical per-frame source resolution, exact source-position provenance, and source-key timing derivation)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (existing Force Spacing publication path and linked guard call sites)
    - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts (current D-11 rejection baseline to supersede only for validated D-50 provenance)
    - app/src/lib/frameMap.ts (main-timeline projection feed to narrow to interval-only marker data before rich-type cleanup in 43-13)
    - app/src/types/timeline.ts and app/src/components/timeline/TimelineRenderer.ts (existing rich capsule type/draw seams to narrow for the tracer and finish cleaning in 43-13)
  </read_first>
  <behavior>
    - Sentinel test: `integrates Loop Clip ownership through all nine tracer checks`.
    - The ownership sentinel mounts the rail/strip/sidebar seam with loop and no-loop fixtures and proves exact 161px/38px/3px/12px EFX geometry, tooltip facts, loop-only selection, local Edit routing, Play-to-Edit context, preserved linked indicators, plus an exact passive 3px `#8B5CF6` PPaint FX-bar marker from `{startFrame, frameCount}` only and zero Loop Clip-specific main-timeline interaction.
    - Issue #2 sentinel `derives timed loop cadence from authoritative source-key appFrame positions` proves normalized offsets, `lastOffset + 1` cycle duration, exact-source/generated-interior resolution, finite/Infinity cadence, and unchanged Loop Clip records.
    - Issue #2 sentinel `spaces validated exact source-position proxies as one shared atomic transaction` proves 2+ unique/full-cycle eligibility, original/linked mixing, repeat/shared-loop deduplication, hard walls, generated/gap/unresolved rejection, one publication/history command, and Undo/Redo.
  </behavior>
  <action>Use one vertical TDD slice only. RED: add exactly one test named `integrates Loop Clip ownership through all nine tracer checks` to `PhysicsPaintLoopClipRail.test.tsx`. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx -t "integrates Loop Clip ownership through all nine tracer checks"`. The precise RED failure is that the assertion expecting one 3px rail target in the existing row plus one passive 3px `#8B5CF6` PPaint FX-bar marker instead observes the rejected separate-lane/rich-capsule ownership, missing/incorrect marker paint, identity-bearing projection, or Loop Clip-specific main-timeline input. Commit the RED evidence. GREEN: make only the production changes needed by that sentinel: mount a focused rail in the existing physical row per D-34R/D-35R/D-36R; derive compact visible geometry and tooltip facts from accepted ranges; share one selected-loop Preact Signal through the existing Studio view-model; route double-click, focused Enter, and sidebar Edit to local `openLoopEdit(loopId)`; swap Play to the contextual Edit/facts view per D-43 through D-45; isolate rail events from physical cells per D-42; preserve linked indicators; and make `frameMap.ts` publish only `{startFrame, frameCount}` effective-interval markers per D-33R, with the timeline renderer painting exact 3px `#8B5CF6` strips inside the existing PPaint FX bar while leaving generic frame-map output and generic FX-track behavior intact. Do not expose `loopId`, source keys, repeat metadata, status, selection, callbacks, or commands, and do not add marker hit testing or any Loop Clip-specific main-timeline input route. Keep the pointer target non-capturing and add no placement mutation per D-41. In the same task, add D-50 through the canonical resolver/action/history seam: derive normalized source offsets and cycle duration from ordered real source-key `appFrame` positions; return explicit exact-source proxy provenance for original real cells and linked occurrences; deduplicate by ordered `sourceKeyIds` plus source index across repeats/shared loops; require 2+ unique positions; keep unselected source positions as hard walls; reject generated interiors, linked gaps, unresolved frames, stale/mixed provenance, single-key movement, drag, and ordinary linked Force Spacing; then stage only selected real source-key positions through one existing atomic Force Spacing publication/history command. Every shared loop, generated interior, finite/Infinity cadence, rail duration, preview/playback/export, and cache resolution must rederive from accepted source positions, while all Loop Clip records remain byte-identical and no occurrence is materialized, unlinked, cloned, or persisted. Do not delete broad timeline or rejected-lane residue yet; 43-13/43-14 own structural cleanup. Run each of the three named sentinel commands byte-for-byte unchanged from RED to GREEN; each must pass before any broader command. Only after GREEN run the focused rail, workflow-strip, Scripts-panel, resolver, guard, action, history, frame-map, renderer, and interaction regressions.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx -t "integrates Loop Clip ownership through all nine tracer checks" && pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts -t "derives timed loop cadence from authoritative source-key appFrame positions" && pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts -t "spaces validated exact source-position proxies as one shared atomic transaction" && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/timeline/TimelineInteraction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The named sentinel has recorded RED evidence for the exact ownership mismatch and GREEN evidence from the identical command.
    - D-49's nine ownership checks and D-50's separate Issue #2 matrix are represented in named sentinels and the broader focused regressions pass.
    - Source-key `appFrame` positions are the only timing authority; non-uniform cadence, generated interiors, finite/Infinity repeats, rail duration, preview/playback/export, and shared loops rederive without a schema field.
    - Valid proxies require 2+ unique exact positions from one ordered cycle, deduplicate across repeats/shared loops, preserve unselected hard walls, commit atomically, and Undo/Redo across every shared loop.
    - Invalid provenance and every non-spacing linked mutation remain fail-closed; no occurrence or Loop Clip placement/repeat record changes.
    - The visible rail is exactly 3px, target exactly 12px, zero-loop DOM is absent, and no layout height changes.
    - Rail, sidebar Edit, and existing modal converge on one local `openLoopEdit(loopId)` route.
    - No rejected persistent filmstrip/rich-capsule presentation remains active; the only main-timeline projection is interval-only `{startFrame, frameCount}` passive marker data, and no D-41 drag, identity, metadata, interaction route, or persisted name is introduced.
  </acceptance_criteria>
  <done>The current branch keeps production Loop Clip interaction exclusively in EFX Paint/Roto, exposes only the passive interval marker in the Motion Editor, and is automated-ready for the blocking nine-check native verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve all nine ownership checks and the separate Issue #2 matrix before expansion</name>
  <what-built>Production integrated 3px Loop Rail, shared Loop Clip selection, contextual Scripts inspector, local Edit Loop Clip routing, preserved linked cells, the Motion Editor passive 3px `#8B5CF6` PPaint FX-bar marker with zero Loop Clip-specific interaction, and D-50 exact-source Key Spacing proxies using authoritative timed source positions.</what-built>
  <how-to-verify>
    1. Open the user-run native app and the same EFX Paint/Roto loop fixture described in the rewritten 43-UAT.md tracer section.
    2. Confirm the workflow strip remains 161px with and without loops and the physical row remains 38px; no extra row appears.
    3. Confirm cells, outlines, blue linked dot/inset border, drag feedback, and the 34px toolbar are fully visible and operable.
    4. Confirm loops show only a 3px rail with a 12px interaction band, while a no-loop track has no rail DOM or reserved space.
    5. Hover and confirm the tooltip appears above with derived name, Cycle math, Effective duration, and status.
    6. Single-click and confirm only the Loop Clip is selected, local actions open, and playhead/physical selection/multi-selection do not change.
    7. Double-click, then separately focus and press Enter; each must open the existing Edit Loop Clip modal once.
    8. Confirm normal script context shows Play; selected Loop Clip context swaps that slot to Pencil/Edit and shows name, source, placement, Cycle, Effective, mode, and status.
    9. Inspect the Motion Editor PPaint FX bar and confirm one exact 3px `#8B5CF6` passive strip appears per effective interval with no new row/height, text, badge, capsule, own hit target, tooltip, hover/focus, selection, keyboard route, navigation, Edit, drag, context menu, callback, command, or mutation.
    10. Record Issue #1 playback confirmation separately using `43-UAT.md`; do not merge its result with spacing behavior.
    11. For Issue #2, select any 2+ unique exact source positions through linked occurrences, then a mixed original/linked scope, then the full cycle. Confirm equivalent positions deduplicate across repeats and a second Loop Clip sharing identical ordered `sourceKeyIds`.
    12. Leave source positions unselected as hard walls, run Key Spacing, and confirm only selected authoritative real source-key `appFrame` positions move while every shared loop adopts the timed cadence, including generated interiors and finite/Infinity repeats.
    13. Confirm one Undo restores the prior rhythm everywhere and one Redo reapplies it; Loop Clip placement/repeat/Infinity/source records remain unchanged and no occurrence gains a real-key diamond or durable record.
    14. Confirm one-position, mixed-cycle, generated-interior, linked-gap, unresolved, stale, and ordinary linked-source Force Spacing requests reject; pointer/single-key/group drag from linked occurrences remains rejected and no materialization, unlink, clone, placement change, or persistence occurs.
  </how-to-verify>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/timeline/TimelineInteraction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The user explicitly approves all nine ownership checks in the same build.
    - The same checkpoint records Issue #1 playback separately and approves the complete Issue #2 Key Spacing matrix; no later plan/checkpoint owns this correction.
    - Any failed ownership or Issue #2 check blocks Plan 43-12 and is reported with the observed surface and screenshot.
  </acceptance_criteria>
  <resume-signal>Type "approved" only when all nine ownership checks, the separate Issue #1 playback confirmation, and the complete Issue #2 matrix pass in this build, or describe the failed check(s).</resume-signal>
  <done>Native tracer approval is recorded and Plan 43-12 may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical physical document → rail/sidebar | Accepted compact ranges and source metadata become visible product facts |
| rail/sidebar input → local controller | Pointer/keyboard intent may request an edit or mutation but has no authority itself |
| physical row → overlapping rail target | The top 12px target must not spoof or trigger physical-cell navigation/drag |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-11-01 | Tampering | shared loop selection and controller routing | high | mitigate | One Signal identity and narrow existing controller ports; no optimistic canonical facts |
| T-43-11-02 | Elevation of Privilege | overlapping rail target | high | mitigate | Structural event isolation and tests proving no cell navigation, selection, or drag dispatch |
| T-43-11-03 | Spoofing | identity-bearing or interactive Motion Editor surface | high | mitigate | Constrain projection to `{startFrame, frameCount}`, paint only the exact passive strip, remove all Loop Clip-specific input/tooltip routes, and require native passive-marker-only proof |
| T-43-11-04 | Denial of Service | Infinity/long ranges | high | mitigate | Visible-window geometry from compact intervals; no destination list or repeated assets |
| T-43-11-05 | Information Disclosure | unresolved IDs/name fallback | medium | mitigate | Derived product name and escaped text only; no raw IDs in tooltip/sidebar/ARIA |
| T-43-11-06 | Tampering | inferred proxy provenance or partial shared-loop retiming | high | mitigate | Canonical exact-source resolver, ordered-cycle deduplication, hard-wall tests, one authority publication/history command, and unchanged Loop Clip records |
| T-43-11-07 | Elevation of Privilege | linked occurrence treated as durable key | high | mitigate | Session-only proxy identity only; generated/gap/unresolved rejection; no materialization, diamond, persistence, drag, Delete, Cut/Copy, unlink, or clone |
| T-43-11-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Execute the focused one-shot Vitest command; never start the server.
- Confirm the RED commit precedes the GREEN commit for the tracer behavior.
- Block Wave 9 until the human approves all nine native ownership checks, records Issue #1 playback separately, and approves the complete Issue #2 native matrix in this same checkpoint.
</verification>

<success_criteria>
- D-33R through D-50 tracer-visible requirements are implemented and cited.
- HOLD-05 canonical behavior includes source-position timing authority and the exact D-50 Key Spacing exception; HOLD-06 corrected information contract remains on the EFX-owned surface.
- The user has approved the integrated tracer; until then the result is automated-ready, not accepted.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Integrated `PhysicsPaintLoopClipRail` production surface and pure rail projection
- Shared Studio/session Loop Clip selection consumed by the rail and Scripts sidebar
- Contextual Scripts inspector with Play-to-Edit swap and text-trigger script rename
- Interval-only Motion Editor marker projection and exact passive PPaint FX-bar Canvas paint with no Loop Clip-specific interaction
- Fresh RED/GREEN ownership plus timed-proxy automated evidence and the existing blocking native verdict covering nine ownership checks, separate Issue #1 playback, and Issue #2
