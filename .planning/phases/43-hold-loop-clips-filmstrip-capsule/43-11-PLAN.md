---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 11
type: execute
wave: 8
depends_on:
  - 43-08
  - 43-09
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.test.ts
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/lib/frameMap.ts
  - app/src/types/timeline.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
autonomous: false
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-34R/D-35R UI-empty: zero Loop Clips render no rail DOM, focus target, label, placeholder, or layout footprint; the workflow strip remains exactly 161px and the physical row remains 38px."
    - "D-34R/D-47 UI-populated: canonical accepted ranges render as compact 3px segments inside the existing physical row without stacking, another row, or repeated-frame materialization."
    - "D-37R UI-loading: controller preparation/commit keeps accepted rail geometry visible with aria-busy and no optimistic width or fact changes."
    - "D-37R UI-error: unresolved ranges remain visible, selectable, and actionable; rejected operations preserve prior geometry and selection."
    - "D-37R/D-38R UI-partial: truncated loops retain canonical Effective duration and `Loop shortened by next clip`."
    - "D-35R UI-overflow: long rails clip at the existing horizontal viewport, return on shared scroll, and never wrap, stack, add a scrollbar, or change canvas allocation."
    - "D-35R UI-zero-one-many: zero hides the group, one renders one target, and many follow canonical placement order in the same overlay."
    - "D-38R tooltip-overflow and long-text: the tooltip is above the rail, viewport-clamped, accessible, and never falls back to a raw identifier."
    - "D-43 sidebar-empty: with no selected Loop Clip the existing Scripts context and Play Script primary action remain unchanged."
    - "D-43/D-44 sidebar-populated: one selected Loop Clip replaces Play with Pencil/Edit in the same slot and shows display name, source, placement, Cycle, Effective, mode, and status in exact order."
    - "D-43/D-44 sidebar-overflow and zero-one-many: existing sidebar geometry remains unchanged and exactly one shared session selection drives the inspector."
    - "D-42 linked-cell empty/populated: physical cells preserve their accepted palette and blue linked indicator behavior."
    - "D-53 Motion Editor visibility-only boundary: the tracer publishes only `{startFrame, frameCount, mode}` and paints exact 3px Progressive-purple or Static/Hold-cyan passive strips with white canonical endpoint cuts inside the existing PPaint FX bar, with no identity, source/repeat metadata, text, badge, own hit target, tooltip, hover/focus, selection, keyboard route, navigation, Edit, drag, context menu, callback, command, or mutation."
    - "D-49/D-57/D-58: all nine ownership checks and the corrected Issue #2 rail-selection, ordered-ripple, atomic-history, interpolation, and Play Script background matrix pass in this same checkpoint."
    - "D-57 timing and selection authority: ordered real source-key appFrame positions own cadence; Loop Rails own complete multi-capsule cycles through plain/range/toggle gestures; physical keys own ordinary operations and partial spacing within one cycle only; the modes are mutually exclusive and Select All is exact."
    - "D-57 ripple and atomicity: selected cycle groups process left-to-right, later real keys ripple cumulatively, source-attached Loop Clip placement follows its first source key, Interpolation remains unchanged, and records plus Loop Clips share one rollback/history transaction."
    - "D-58 background parity: every Play Script publication carries the current valid Paint background, including fresh-layer document creation; ordinary physical edits reject that field."
    - "D-11/D-23 remain fail-closed outside D-57: no single-key linked movement, linked drag, unauthorized Force Spacing, generated/gap/unresolved source scope, occurrence materialization, unlink, clone, persisted selection, or independent placement mutation."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx"
      provides: "focused integrated rail target and paint surface"
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "pure derived display name, visible geometry, Cycle/Effective/status projection"
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "contextual selected-loop inspector and Play-to-Edit primary-slot swap"
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts"
      provides: "one shared Studio/session selected-loop identity and focused action ports"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.ts"
      provides: "immutable plain/range/toggle Loop Rail selection reducer"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "atomic records-plus-Loop-Clips staging/rollback and Play Script background publication"
    - path: "app/src/lib/frameMap.ts"
      provides: "paint-only `{startFrame, frameCount, mode}` passive marker projection"
    - path: "app/src/components/timeline/TimelineRenderer.ts"
      provides: "pure Canvas painter for purple/cyan 3px strips and white canonical endpoint cuts inside the existing PPaint FX bar"
  key_links:
    - from: "canonical rotoLoopResolutionContext.ranges"
      to: "PhysicsPaintLoopClipRail"
      via: "visible-window projection at the existing frame pitch"
      pattern: "rotoLoopResolutionContext|visibleFrameWindow"
    - from: "shared selected Loop Clip Signal"
      to: "rail highlight and PhysicsPaintScriptsPanel"
      via: "PhysicsPaintStudio view-model props without effect mirroring"
      pattern: "selectedLoopClipId"
    - from: "rail double-click, focused Enter, and sidebar Edit"
      to: "physicsPaintRotoPlayScriptController.openLoopEdit"
      via: "one Studio-local focused port"
      pattern: "openLoopEdit"
  prohibitions:
    - statement: "No separate Loop Clip row, full filmstrip capsule, repetition band, permanent Cycle badge, raw UUID product copy, identity-bearing main-timeline projection, or Loop Clip-specific main-timeline interaction may remain in the tracer surface; the passive interval marker is explicitly allowed."
      status: resolved
      verification: automated-and-human
    - statement: "No Loop Clip placement drag, pointer capture, drag threshold, preview, drop target, or placement mutation is implemented per D-41."
      status: resolved
      verification: automated
    - statement: "No persisted Loop Clip naming field, schema migration, dependency, or alternate mutation path is introduced per D-45/D-46."
      status: resolved
      verification: automated
---

<objective>
Replace the rejected lane/rich-capsule tracer with the approved integrated Loop Rail and contextual Scripts inspector, then implement the D-57/D-58 recovery: rail-owned multi-capsule Key Spacing, mutually exclusive physical selection, ordered cumulative ripple with source-attached placement follow, atomic records-plus-Loop-Clips history, unchanged Interpolation, and current Play Script background publication.

Purpose: Prove the corrected ownership and authority boundary end to end: Physics Paint Studio owns Loop Clip identity, selection, and mutation; the Motion Editor receives passive paint only; the parent accepts one complete physical document transaction.
Output: Production integrated rail and plural selection, contextual inspector, passive mode marker, authoritative source timing, ordered ripple resolver/action path, atomic coordinator/history staging, strict Play Script background transport, focused regressions, and the existing blocking native approval checkpoint.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-MEMORY-RECALL.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/physicsPaintStudio.css

<interfaces>
Use `physicsPaintRotoPlayScriptController.openLoopEdit(loopId)` as the sole dialog-open path. Consume compact ranges and frame pitch. Keep plural selected Loop Clip IDs, one rail anchor, and one primary inspector identity in the Studio Signal/view-model boundary; keep physical IDs/anchor separate and synchronously enforce mutual exclusion without effects. At Apply time snapshot selection, records, Loop Clips, interpolation, capacity, and launch identity once; derive complete rail cycles or exact one-cycle physical provenance and feed one immutable ordered-ripple proposal into the existing authority/history path. Forward `buildRotoBackgroundMetadata(settings)` only through Play Script physical publication.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN integrated ownership, rail selection, cumulative ripple, atomic settlement, and background recovery</name>
  <reversibility rating="costly">D-33R removes duplicate Motion Editor ownership; reversal would restore a second presentation and interaction surface.</reversibility>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts, app/src/lib/frameMap.ts, app/src/types/timeline.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33R through D-49; D-41 remains deferred)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (nine-check matrix and exact geometry)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (physical-row host and cell event ordering)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (existing Play slot and script-name surface)
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (Preact Signal/view-model seam)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (canonical per-frame source resolution, exact source-position provenance, and source-key timing derivation)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (existing Force Spacing publication path and linked guard call sites)
    - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts (current D-11 rejection baseline plus D-57 mode-specific authorization/ripple guards)
    - app/src/lib/frameMap.ts (main-timeline projection feed to narrow to interval-only marker data before rich-type cleanup in 43-13)
    - app/src/types/timeline.ts and app/src/components/timeline/TimelineRenderer.ts (existing rich capsule type/draw seams to narrow for the tracer and finish cleaning in 43-13)
  </read_first>
  <behavior>
    - Ownership sentinel: integrated rail/strip/sidebar geometry and routing remain EFX-local while passive `{startFrame, frameCount, mode}` markers remain non-interactive.
    - Selection sentinels prove plain/range/toggle rail gestures, stable anchor, plural selected IDs with one primary inspector target, mutually exclusive physical selection, exact Select All scope, same-cycle physical partial authorization, and multi-cycle physical rejection.
    - Resolver/action sentinels prove complete rail-cycle derivation, identical-cycle deduplication, left-to-right cumulative ripple, source-attached placement follow, final order/capacity validation, and unchanged Interpolation Off/On semantics.
    - Coordinator/history sentinels prove records and Loop Clips stage/rollback/accept together and produce one Undo/Redo command.
    - Background sentinels prove valid Play Script metadata is forwarded into fresh/current parent documents, ordinary physical injection rejects, and preview/export composites remain aligned.
  </behavior>
  <action>Use focused RED→GREEN slices at the real seams. First retain the integrated rail/sidebar/passive-marker ownership sentinel. Add pure rail selection tests for plain/range/toggle and stale reconciliation; Studio/WorkflowStrip tests for synchronous physical/rail mutual exclusion, exact Select All scope, visible selected-cycle highlights, and navigation-only generated/gap/unresolved cells; action tests for complete rail-cycle derivation, identical-cycle deduplication, one-cycle physical authorization, cross-cycle guidance, and fail-closed stale/reordered/duplicate/ambiguous provenance. Add resolver tests matching adjacent Progressive A and Static/Hold B: space A by 2, anchor A's first key, ripple all later records, move B rigidly, follow B's source-attached placement, preserve all other Loop Clip fields, and keep Interpolation Off/On unchanged; then select A+B and prove left-to-right cumulative growth. Add coordinator/history tests proving complete records-plus-Loop-Clips staging, rollback on replacement/transport/settlement failure, one accepted history command, and one Undo/Redo. Add strict Play Script background tests through controller, hook, coordinator, payload guard, fresh-layer parent bridge, preview, and export; ordinary physical kinds reject the field. Implement only the required Preact Signals/reducers, Apply-time snapshot/authorization, resolver mapping, placement-follow helper, coordinator staging, and existing publication chain. Do not persist selection/provenance, materialize linked occurrences, toggle interpolation, add a second background mutation, introduce schema/dependency changes, or start a server/native app. Run the focused recovery matrix from 43-VALIDATION.md, then full gates.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/lib/physicPaintBridge.test.ts src/types/physicPaint.test.ts src/lib/previewRenderer.loops.test.ts src/lib/exportEngine.loops.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The named sentinel has recorded RED evidence for the exact ownership mismatch and GREEN evidence from the identical command.
    - D-49's nine ownership checks and the corrected D-57/D-58 recovery matrix are represented in focused regressions.
    - Source-key `appFrame` positions remain timing authority; rail selection authorizes complete cycles, physical selection authorizes ordinary and one-cycle partial scope, the modes are mutually exclusive, and Select All is exact.
    - Selected cycles deduplicate and process left-to-right; later records ripple cumulatively, source-attached placement follows, all other Loop Clip fields remain unchanged, and Interpolation is never toggled.
    - Invalid provenance and every non-spacing linked mutation remain fail-closed; no linked occurrence or selection state is materialized or persisted.
    - Records and Loop Clips stage, rollback, settle, and enter history together; one Undo/Redo covers both.
    - Play Script requires the live background snapshot for fresh/current document publication; ordinary physical edits reject it and preview/export parity remains green.
    - The visible rail is exactly 3px, target exactly 12px, zero-loop DOM is absent, and no layout height changes.
    - Rail, sidebar Edit, and existing dialog converge on one local `openLoopEdit(loopId)` route.
    - The only main-timeline projection is paint-only `{startFrame, frameCount, mode}` passive marker data; no D-41 drag, identity, source/repeat metadata, interaction route, or persisted name is introduced.
  </acceptance_criteria>
  <done>The current branch keeps production Loop Clip interaction exclusively in EFX Paint/Roto, exposes only the passive interval marker in the Motion Editor, and is automated-ready for the blocking nine-check native verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve all nine ownership checks and the corrected recovery matrix</name>
  <what-built>Production integrated mode-colored Loop Rails with plural rail selection, mutually exclusive physical selection, contextual Scripts inspector, local Edit routing, passive Motion Editor markers, ordered cumulative Key Spacing ripple with source-attached placement follow, atomic records-plus-Loop-Clips history, unchanged Interpolation semantics, and Play Script background publication.</what-built>
  <how-to-verify>
    1. Open the user-run native app and the same EFX Paint/Roto loop fixture described in the rewritten 43-UAT.md tracer section.
    2. Confirm the workflow strip remains 161px with and without loops and the physical row remains 38px; no extra row appears.
    3. Confirm cells, outlines, blue linked dot/inset border, drag feedback, and the 34px toolbar are fully visible and operable.
    4. Confirm loops show only 3px Progressive-purple or Static/Hold-cyan rails with 12px targets, orange selection, white actual endpoint cuts, and no-loop DOM absence.
    5. Hover and confirm the tooltip appears above with derived name, Cycle math, Effective duration, mode, and status.
    6. Plain-click rail A, Shift-click rail B, and Cmd/Ctrl-toggle rails; confirm only selected rail lines change paint, complete source cycles remain invisible Key Spacing scope, physical frames receive no rail-derived selected state, the anchor stays stable, one inspector target remains primary, and the playhead does not navigate.
    7. Select a physical key and use Select All, then select a rail again; confirm physical and rail modes synchronously clear one another and visible Select All exactly equals operation scope.
    8. Double-click, then separately focus and press Enter; each opens the existing Edit Loop Clip dialog once. Confirm normal script context shows Play and selected Loop Clip context shows Pencil/Edit plus all facts.
    9. Confirm the passive Motion Editor markers use mode colors and white actual endpoint cuts with no Loop Clip-specific interaction.
    10. Record Issue #1 playback separately.
    11. With adjacent Progressive A and Static/Hold B, verify Interpolation Off/On semantics, then space A by 2 and confirm B's keys, placement, rail, and boundaries ripple together.
    12. Rail-select A+B and confirm complete cycles process left-to-right with cumulative growth; identical selected source cycles deduplicate.
    13. Confirm one Undo/Redo restores/reapplies records and placements together; near-capacity rejection changes nothing and creates no history.
    14. Confirm multi-cycle physical spacing directs the user to Loop Rails and generated/gap/unresolved/stale/reordered/duplicate/ambiguous scopes reject without fallback or materialization.
    15. On a fresh textured-background layer, make Play Script the first operation, then change background and apply again; confirm Physics Paint, main Studio, preview, export, and save/reopen use the current accepted background. Complete UAT steps 14 and 15 for persistence and ordinary non-loop spacing.
  </how-to-verify>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/lib/physicPaintBridge.test.ts src/types/physicPaint.test.ts src/lib/previewRenderer.loops.test.ts src/lib/exportEngine.loops.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The user explicitly approves all nine ownership checks in the same build.
    - The same checkpoint records Issue #1 playback separately and approves the complete Issue #2 rail-selection/ripple/background matrix; no later plan/checkpoint reinterprets this correction.
    - Any failed ownership, spacing, atomicity, interpolation, persistence, or background check blocks handoff and is reported with the observed surface and screenshot.
  </acceptance_criteria>
  <resume-signal>Type "approved" only when all nine ownership checks, Issue #1 playback, and the complete Issue #2 rail-selection/ripple/background recovery matrix pass in this build, or describe the failed check(s).</resume-signal>
  <done>Native tracer approval is recorded and Plan 43-12 may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical physical document → rail/sidebar | Accepted compact ranges and source metadata become visible product facts |
| rail/sidebar input → local controller | Pointer/keyboard intent may request an edit or mutation but has no authority itself |
| physical row → overlapping rail target | The top 12px target must not spoof or trigger physical-cell navigation/drag |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-11-01 | Tampering | shared loop selection and controller routing | high | mitigate | One Signal identity and narrow existing controller ports; no optimistic canonical facts |
| T-43-11-02 | Elevation of Privilege | overlapping rail target | high | mitigate | Structural event isolation and tests proving no cell navigation, selection, or drag dispatch |
| T-43-11-03 | Spoofing | identity-bearing or interactive Motion Editor surface | high | mitigate | Constrain projection to `{startFrame, frameCount, mode}`, paint only passive mode strips/cuts, remove all Loop Clip-specific input/tooltip routes, and require native passive-marker-only proof |
| T-43-11-04 | Denial of Service | Infinity/long ranges | high | mitigate | Visible-window geometry from compact intervals; no destination list or repeated assets |
| T-43-11-05 | Information Disclosure | unresolved IDs/name fallback | medium | mitigate | Derived product name and escaped text only; no raw IDs in tooltip/sidebar/ARIA |
| T-43-11-06 | Tampering | stale selection authorization, partial ripple, or split record/placement settlement | high | mitigate | Canonical rail/physical scope validation, final-order/capacity guards, source-attached placement follow, complete rollback, and one history command |
| T-43-11-07 | Elevation of Privilege | linked occurrence treated as durable key | high | mitigate | Session-only selection identity only; generated/gap/unresolved rejection; no materialization, diamond, persistence, drag, Delete, Cut/Copy, unlink, or clone |
| T-43-11-08 | Tampering | stale or missing parent background | high | mitigate | Strict Play Script-only background payload, same document transaction, ordinary-kind rejection, and preview/export parity tests |
| T-43-11-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Execute the focused one-shot Vitest command; never start the server.
- Confirm the RED commit precedes the GREEN commit for the tracer behavior.
- Block final handoff until the human approves all nine native ownership checks, records Issue #1 playback separately, and approves the complete Issue #2 rail-selection/ripple/background matrix in this same checkpoint.
</verification>

<success_criteria>
- D-33R through D-58 tracer-visible requirements are implemented and cited.
- HOLD-05 includes source-position timing, rail-owned multi-capsule selection, exact physical one-cycle scope, cumulative ripple, atomic placement history, and unchanged interpolation; HOLD-03/HOLD-04 include Play Script background parity; HOLD-06 remains EFX-owned.
- The user has approved the integrated tracer; until then the result is automated-ready, not accepted.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Integrated `PhysicsPaintLoopClipRail` production surface and pure rail projection
- Shared Studio/session Loop Clip selection consumed by the rail and Scripts sidebar
- Contextual Scripts inspector with Play-to-Edit swap and text-trigger script rename
- Paint-only Motion Editor marker projection and exact passive mode-colored PPaint FX-bar Canvas paint with no Loop Clip-specific interaction
- Rail-owned selection, ordered cumulative ripple, source-attached placement follow, atomic coordinator/history, and strict Play Script background publication
- Fresh RED/GREEN recovery evidence and the existing blocking native verdict covering nine ownership checks, separate Issue #1 playback, and corrected Issue #2
