---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 11
type: execute
wave: 8
depends_on:
  - 43-08
  - 43-09
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/components/timeline/TimelineCanvas.tsx
  - app/src/components/timeline/TimelineRenderer.test.ts
  - app/src/components/timeline/TimelineInteraction.test.ts
autonomous: false
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-34R/D-35R UI-empty: zero Loop Clips render no rail DOM, focus target, label, placeholder, or layout footprint; the workflow strip remains exactly 161px and the physical row remains 38px."
    - "D-34R/D-47 UI-populated: canonical accepted ranges render as compact 3px segments inside the existing physical row without stacking, another row, or repeated-frame materialization."
    - "D-37R UI-loading: controller preparation/commit keeps accepted rail geometry visible at 55% opacity with aria-busy and no optimistic width or fact changes."
    - "D-37R UI-error: unresolved ranges remain red, visible, selectable, and actionable; rejected operations preserve prior geometry and selection."
    - "D-37R/D-38R UI-partial: truncated loops retain canonical Effective duration, an amber terminal treatment, and `Loop shortened by next clip`."
    - "D-35R UI-overflow: long rails clip at the existing horizontal viewport, return on shared scroll, and never wrap, stack, add a scrollbar, or change canvas allocation."
    - "D-35R UI-zero-one-many: zero hides the group, one renders one target, and many follow canonical placement order in the same overlay."
    - "D-38R tooltip-overflow: the styled tooltip is above the rail when space permits and clamps to an 8px viewport margin."
    - "D-38R/D-45 tooltip-long-text: long derived names wrap within the tooltip, remain fully accessible, and never fall back to a raw identifier."
    - "D-43 sidebar-empty: with no selected Loop Clip the existing Scripts context and Play Script primary action remain unchanged."
    - "D-43/D-44 sidebar-populated: one selected Loop Clip replaces Play with Pencil/Edit in the same slot and shows display name, source, placement, Cycle, Effective, mode, and status in exact order."
    - "D-43/D-44 sidebar-overflow: the existing sidebar geometry, scroll, and resizer remain unchanged; 88px labels plus minmax values avoid horizontal scrolling at 1280x720."
    - "D-43 sidebar-zero-one-many: exactly one session-selected Loop Clip drives the inspector even when many clips exist, while clearing loop selection restores normal script context without mutating records."
    - "D-42 linked-cell-empty: unlinked physical cells retain their existing palette and render no blue linked indicator."
    - "D-42 linked-cell-populated: linked cells preserve the accepted inset rgba(45,91,227,0.9) border and 4px dot across normal/current/selected/drag states."
    - "D-33 Motion-Editor-overflow: every zoom and scroll boundary has zero Loop Clip drawing, label, hit region, tooltip mount, keyboard target, selection, context action, or edit command."
    - "D-49: all nine native tracer checks pass together before Plan 43-12 begins."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx"
      provides: "focused integrated rail target and paint surface"
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "pure derived display name, visible geometry, Cycle/Effective/status projection"
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "contextual selected-loop inspector and Play-to-Edit primary-slot swap"
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts"
      provides: "one shared Studio/session selected-loop identity and focused action ports"
  key_links:
    - from: "canonical rotoLoopResolutionContext.ranges"
      to: "PhysicsPaintLoopClipRail"
      via: "visible-window projection at the existing 18px frame pitch"
      pattern: "rotoLoopResolutionContext|visibleFrameWindow"
    - from: "shared selected Loop Clip Signal"
      to: "rail highlight and PhysicsPaintScriptsPanel"
      via: "PhysicsPaintStudio view-model props without effect mirroring"
      pattern: "selectedLoopClipId"
    - from: "rail double-click, focused Enter, and sidebar Edit"
      to: "physicsPaintRotoPlayScriptController.openLoopEdit"
      via: "one Studio-local focused port"
      pattern: "openLoopEdit"
  prohibitions:
    - statement: "No separate Loop Clip row, 32px lane, full filmstrip capsule, source-thumbnail strip, repetition band, permanent Cycle badge, or raw UUID product copy may remain in the tracer surface."
      status: resolved
      verification: automated-and-human
    - statement: "No Loop Clip placement drag, pointer capture, drag threshold, preview, drop target, or placement mutation is implemented per D-41."
      status: resolved
      verification: automated
    - statement: "No persisted Loop Clip naming field, schema migration, schema push, new dependency, or alternate mutation path is introduced per D-45/D-46."
      status: resolved
      verification: automated
---

<objective>
Replace the rejected lane/capsule tracer with the approved integrated Loop Rail and contextual Scripts inspector, and block expansion until the user approves all nine visible checks together per D-33, D-34R, D-35R, D-36R, D-38R, D-39R, D-40R, D-42, D-43, D-44, D-45, D-46, D-47, D-48, and D-49.

Purpose: Prove the corrected EFX Paint/Roto ownership end to end from canonical Loop Clip ranges through one shared Studio selection, local Edit Loop Clip routing, sidebar context, preserved physical cells, and complete Motion Editor exclusion.
Output: Production integrated rail, contextual inspector, fresh RED/GREEN tracer contracts, and a blocking native approval checkpoint.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-MEMORY-RECALL.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/physicsPaintStudio.css

<interfaces>
Use `physicsPaintRotoPlayScriptController.openLoopEdit(loopId)` as the sole modal-open path. Consume existing compact `rotoLoopResolutionContext.ranges`, `visibleFrameWindow`, and frame pitch. Keep selected Loop Clip identity in one Preact Signal/view-model boundary shared by the workflow strip and Scripts panel; do not mirror it with `useEffect`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED-GREEN the integrated rail and contextual inspector through all nine checks</name>
  <reversibility rating="costly">D-33 removes duplicate Motion Editor ownership; reversal would restore coordinated projection, hit-testing, and action call sites.</reversibility>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineCanvas.tsx, app/src/components/timeline/TimelineRenderer.test.ts, app/src/components/timeline/TimelineInteraction.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33 through D-49; D-41 is deferred interaction scope)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (exact 161px/38px/3px/12px geometry, copy, focus, sidebar, nine-check matrix)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx (rejected lane/body/badge implementation to replace)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (rejected sibling-row mount, local selection, physical-cell event ordering, linked indicators)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (Play slot, current script rename, summary host)
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (identity memo seam)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (`openLoopEdit` and accepted controller ports)
    - app/src/components/physic-paint/physicsPaintStudio.css (161px strip, 38px row, rejected 32px/24px/16px rules, linked-cell rules)
    - app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineCanvas.tsx (entry points to disable before residue deletion in 43-13)
  </read_first>
  <behavior>
    - RED 1: loop and no-loop fixtures both preserve a 161px workflow strip, 38px physical row, unobstructed 24px cells and 34px action toolbar; only the loop fixture contains a 3px rail with exactly 12px targets.
    - RED 2: tooltip order is derived display name, finite/infinity Cycle math, Effective duration, then canonical status; no raw internal identifier or persistent rail text appears.
    - RED 3: single click selects only the Loop Clip, opens local actions, updates the Scripts inspector, and leaves playhead, real-key selection, multi-selection, and drag state unchanged.
    - RED 4: double-click and focused Enter each call the Studio-local `openLoopEdit(loopId)` exactly once; the second click cannot reopen the popover over the modal.
    - RED 5: normal script context retains Play; selected-loop context uses Lucide Pencil/Edit in the same primary slot and renders the seven required facts.
    - RED 6: blue linked-cell inset border and 4px dot remain present and physical-cell navigation/selection/drag contracts remain green below y=12px.
    - RED 7: Motion Editor render, pointer, keyboard, tooltip, and canvas entry points expose no Loop Clip UI or command while generic timeline behavior remains green.
  </behavior>
  <action>Start with one failing integrated tracer test at a time, commit the RED gate, then implement the production path and commit GREEN. Replace the rejected `PhysicsPaintLoopClipLane` sibling-row/full-capsule implementation with a focused `PhysicsPaintLoopClipRail` absolutely hosted at the top edge of the existing physical row per D-34R/D-35R/D-36R. Paint exactly 3px, expose exactly 12px targets, preserve the 161px/38px/34px contracts, and omit the rail group entirely when canonical ranges are empty. Derive visible half-open geometry from accepted resolver ranges and the existing visible window per D-24, D-25, D-26, D-30, D-32, and D-47; do not materialize repetitions. Replace strip-local loop selection with one Preact Signal/session identity threaded through the existing Studio view-model so the rail and Scripts panel consume the same selection without effect synchronization. Implement D-38R through D-40R: tooltip above the rail, loop-only click selection, double-click/focused Enter to the existing local `openLoopEdit(loopId)` exactly once, and event isolation from cell handlers. Implement D-43 through D-45 in the existing Scripts panel: Play remains for normal script context; selected loop swaps the same slot to Pencil/Edit, renders the required inspector facts, derives the non-persisted display name, and changes script rename to a text-name trigger with no dedicated rename Pencil. Remove/disable all Motion Editor Loop Clip entry-point drawing, hit, keyboard, tooltip mounting, and edit routing now so the tracer proves D-33; Plan 43-13 will delete the now-dead projection/types/geometry residue. Keep controller operations, authority, persistence, resolver, preview/export, and linked-cell styling unchanged per D-01 through D-14 and D-24 through D-32. Reserve non-capturing pointer geometry only; do not implement the deferred D-41 drag feature.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/timeline/TimelineRenderer.test.ts src/components/timeline/TimelineInteraction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fresh RED evidence fails for the missing integrated behavior before production changes; GREEN evidence passes after implementation.
    - Automated contracts cover all nine D-49 checks and preserve generic timeline/physical-cell behavior.
    - The visible rail is exactly 3px, target exactly 12px, zero-loop DOM absent, and no layout height changes.
    - Rail, sidebar Edit, and existing modal converge on one local `openLoopEdit(loopId)` route.
    - No rejected lane/capsule/badge presentation remains active and no deferred placement drag or new persisted name exists.
  </acceptance_criteria>
  <done>The current branch exposes one production Loop Clip ownership path exclusively in EFX Paint/Roto, automated checks are green, and the build is ready for the nine-part native tracer verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve all nine integrated tracer checks before expansion</name>
  <what-built>Production integrated 3px Loop Rail, shared Loop Clip selection, contextual Scripts inspector, local Edit Loop Clip routing, preserved linked cells, and zero Motion Editor Loop Clip UI.</what-built>
  <how-to-verify>
    1. Open the user-run native app and the same EFX Paint/Roto loop fixture described in the rewritten 43-UAT.md tracer section.
    2. Confirm the workflow strip remains 161px with and without loops and the physical row remains 38px; no extra row appears.
    3. Confirm cells, outlines, blue linked dot/inset border, drag feedback, and the 34px toolbar are fully visible and operable.
    4. Confirm loops show only a 3px rail with a 12px interaction band, while a no-loop track has no rail DOM or reserved space.
    5. Hover and confirm the tooltip appears above with derived name, Cycle math, Effective duration, and status.
    6. Single-click and confirm only the Loop Clip is selected, local actions open, and playhead/physical selection/multi-selection do not change.
    7. Double-click, then separately focus and press Enter; each must open the existing Edit Loop Clip modal once.
    8. Confirm normal script context shows Play; selected Loop Clip context swaps that slot to Pencil/Edit and shows name, source, placement, Cycle, Effective, mode, and status.
    9. Inspect the Motion Editor main timeline and confirm there is no Loop Clip drawing, label, hit target, tooltip, selection, keyboard target, context action, or edit command.
  </how-to-verify>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/timeline/TimelineRenderer.test.ts src/components/timeline/TimelineInteraction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The user explicitly approves all nine checks in the same build.
    - Any failed check blocks Plan 43-12 and is reported with the observed surface and screenshot.
  </acceptance_criteria>
  <resume-signal>Type "approved" only when all nine checks pass together, or describe the failed numbered check(s).</resume-signal>
  <done>Native tracer approval is recorded and Plan 43-12 may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical physical document → rail/sidebar | Accepted compact ranges and source metadata become visible product facts |
| rail/sidebar input → local controller | Pointer/keyboard intent may request an edit or mutation but has no authority itself |
| physical row → overlapping rail target | The top 12px target must not spoof or trigger physical-cell navigation/drag |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-11-01 | Tampering | shared loop selection and controller routing | high | mitigate | One Signal identity and narrow existing controller ports; no optimistic canonical facts |
| T-43-11-02 | Elevation of Privilege | overlapping rail target | high | mitigate | Structural event isolation and tests proving no cell navigation, selection, or drag dispatch |
| T-43-11-03 | Spoofing | duplicate Motion Editor surface | high | mitigate | Remove active render/interaction/tooltip routes and require native zero-surface proof |
| T-43-11-04 | Denial of Service | Infinity/long ranges | high | mitigate | Visible-window geometry from compact intervals; no destination list or repeated assets |
| T-43-11-05 | Information Disclosure | unresolved IDs/name fallback | medium | mitigate | Derived product name and escaped text only; no raw IDs in tooltip/sidebar/ARIA |
| T-43-11-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Execute the focused one-shot Vitest command; never start the server.
- Confirm the RED commit precedes the GREEN commit for the tracer behavior.
- Block Wave 9 until the human approves all nine native checks together.
</verification>

<success_criteria>
- D-33 through D-49 tracer-visible requirements are implemented and cited.
- HOLD-05 canonical behavior and HOLD-06 corrected information contract are present on the EFX-owned surface.
- The user has approved the integrated tracer; until then the result is automated-ready, not accepted.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Integrated `PhysicsPaintLoopClipRail` production surface and pure rail projection
- Shared Studio/session Loop Clip selection consumed by the rail and Scripts sidebar
- Contextual Scripts inspector with Play-to-Edit swap and text-trigger script rename
- Fresh RED/GREEN automated tracer evidence and blocking nine-check native verdict
