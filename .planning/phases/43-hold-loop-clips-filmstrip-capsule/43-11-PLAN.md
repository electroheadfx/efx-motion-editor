---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 11
type: execute
wave: 8
depends_on:
  - 43-08
  - 43-09
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-33: the Motion Editor main timeline renders no Loop Clip capsule; D-34: one canonical Loop Clip is visible and selectable in a dedicated EFX Paint lane."
    - "D-37: the tracer badge opens the existing local EFX Paint Loop Edit path; D-39: it reaches the existing controller without a parent-to-child Loop Clip request."
    - "D-35: a no-loop project retains exactly the existing 161px workflow-strip geometry; a loop project adds exactly one 32px lane for a 193px total."
    - "D-38: Loop Clip lane activation does not invoke physical-cell navigation, key selection, modifier selection, or drag handlers."
    - "D-40: the lane consumes compact canonical ranges and derives only the visible window without materializing repeated frames or an Infinity destination list."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx"
      provides: "focused EFX-owned frame-aligned Loop Clip lane and tracer controls"
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "pure visible-window capsule presentation projection"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "conditional lane host between ruler and physical cells"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "exact hidden-when-empty 161px/193px geometry"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      via: "canonical useRotoTimelineModel resolution context and focused openLoopEdit port"
      pattern: "rotoLoopResolutionContext|onOpenRotoLoopEdit"
    - from: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx"
      via: "same scroller, visible frame window, and 18px frame pitch"
      pattern: "PhysicsPaintLoopClipLane"
    - from: "app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx"
      to: "existing Roto Play Script controller"
      via: "badge activation awaits the local openLoopEdit result"
      pattern: "onOpenLoopEdit"
  prohibitions:
    - statement: "MUST NOT retain a Loop Clip drawing, summary, or navigation shortcut on the Motion Editor main timeline (D-33)."
      status: resolved
      verification: automated
    - statement: "MUST NOT mirror canonical Loop Clip props into effect-synchronized component state."
      status: resolved
      verification: automated
    - statement: "MUST NOT add a second scroller, cursor, or physical-cell overlay for the lane (D-34, D-38)."
      status: resolved
      verification: automated
---

<objective>
Deliver the production tracer for the corrected ownership boundary: one real canonical Loop Clip renders, selects, and opens Loop Edit locally inside EFX Paint, while the Motion Editor main timeline emits no Loop Clip drawing.

Purpose: Close the failed Step 1 architectural gap before enriching the filmstrip. D-33 is a locked costly relocation, so this plan implements it without reopening a decision checkpoint.
Output: EFX-owned lane skeleton, local Loop Edit activation, bounded visible-window presentation, exact conditional strip geometry, and main-timeline rendering absence.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-08-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
@app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts
@app/src/components/timeline/TimelineRenderer.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end corrected Loop Clip ownership — one local EFX path</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33, D-34, D-37, D-38, D-39)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S1 placement; capsule body/badge controls)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (lane host, Studio wiring, renderer-removal assignments)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (current scroller and physical-cell event contract)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (useRotoTimelineModel and useRotoPlayScriptController ownership)
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (identity-memo workflow construction)
    - app/src/components/timeline/TimelineRenderer.ts (stale main-timeline draw invocation)
  </read_first>
  <behavior>
    - Test 1: a non-empty canonical loop-resolution context renders one `Loop Clips` lane between the ruler and physical lane, and its body selects the loop without calling the physical-cell navigation handler.
    - Test 2: badge activation calls the focused local `openLoopEdit(loopId)` port exactly once and does not open the body details path.
    - Test 3: TimelineRenderer produces no Loop Clip capsule drawing even when stale layout input contains loop projection data.
  </behavior>
  <action>
    Start RED with the three observable tests, then implement the thinnest production path through the existing authority seams. Per D-34, create `PhysicsPaintLoopClipLane` as a separate DOM lane inside the existing workflow scroller immediately after the ruler and before the physical lane. Feed it the canonical `rotoTimelineModel.loopResolutionContext.value`, visible frame window, and existing frame pitch; do not import frameMap, timeline stores, or playback globals. Per D-37 and D-39, expose only the existing local `rotoPlayScript.openLoopEdit(loopId)` method through the Studio workflow view model and await its result from the badge control; do not construct or send a bridge request. Keep body and badge as separate keyboard-reachable controls, with body selection lane-local and no automatic navigation. Per D-38, stop lane pointer propagation and leave the physical-cell click/modifier/drag code unchanged. Per D-33, remove the `drawLoopCapsules` invocation from `TimelineRenderer` so the same canonical data has no visible main-timeline authoring surface. D-33 has reversibility rating `costly`: this is intentional removal of duplicate ownership, not a temporary toggle.
  </action>
  <reversibility rating="costly">D-33 removes the second product surface and its ownership path; reversal would restore duplicate interaction ownership.</reversibility>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A canonical Loop Clip appears in EFX Paint in a dedicated lane, body selection is lane-local, and badge activation reaches the existing local Loop Edit controller.
    - The lane is in the same horizontal scroller and precedes the physical lane.
    - Main-timeline renderer tests prove no Loop Clip drawing path executes.
    - Existing physical-cell plain click, modifier selection, and drag source remain unchanged.
  </acceptance_criteria>
  <done>The failed UAT Step 1 ownership path is reversed end-to-end: one real Loop Clip is actionable in EFX Paint and absent from the main timeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive compact visible-window capsule presentation</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-35, D-36, D-40)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (pure helper and lazy derivation assignments)
    - app/src/components/timeline/loopCapsuleGeometry.ts (host-neutral badge, zoom, ghost, truncation, anchor algorithms only)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (pure exhaustive helper convention)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (authoritative effective ranges and modulo resolution)
  </read_first>
  <behavior>
    - Test 1: finite and Infinity intervals return only source/ghost segments intersecting the supplied visible frame window.
    - Test 2: distant occurrence frames resolve to the expected source index by modulo without allocating an occurrence list.
    - Test 3: requested duration remains distinct from resolver-authoritative effective duration; truncated and zero-effective records produce a diagonal or `0f` anchor presentation respectively.
    - Test 4: badge copy is exactly `Cycle 5f × 5 = 25f` for finite and `Cycle 5f × ∞` for Infinity.
  </behavior>
  <action>
    RED-GREEN-REFACTOR the host-neutral algorithms into the EFX-owned pure module. Per D-36, retain full capsule semantics as immutable presentation records; per D-40, accept compact resolver ranges plus a visible window and compute local x/width from frame offsets and pitch. Reuse or relocate pure badge/zoom/ghost/truncation/first-cycle logic from the stale timeline namespace, but do not copy its header-offset coordinate transform or duplicate next-content boundary algebra. Keep store, Signal, DOM, controller, and asset reads outside this module. Represent Infinity symbolically and bound iteration to visible frames/cycles so no destination-frame list or repeated durable/cache record can exist. Connect the tracer lane to this model while preserving the single canonical resolver context.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Pure tests independently pin finite, Infinity, distant modulo, truncation, and zero-effective outputs.
    - Presentation uses effective range data supplied by the canonical resolver and never persists Effective duration.
    - Visible-window work is bounded by intersecting source/occurrence segments, not requested Infinity length.
    - No main-timeline type or store is imported.
  </acceptance_criteria>
  <done>The EFX lane has a deterministic, exhaustive, lazy presentation model suitable for the complete filmstrip expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock conditional strip geometry and cell isolation</name>
  <files>app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S1 exact 161px/193px geometry and interaction separation)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (conditional geometry and regression assignments)
    - app/src/components/physic-paint/physicsPaintStudio.css (current 161px strip, ruler, physical lane, action row, scrollbar)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (existing CSS/source-contract helpers)
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts (retained selection behavior)
  </read_first>
  <behavior>
    - Test 1: empty ranges render no lane element or loop-present modifier and preserve the exact 161px Studio/strip rules.
    - Test 2: non-empty ranges apply exactly 193px total, a 32px lane, and a 24px capsule with 4px top/bottom clearance.
    - Test 3: DOM order is ruler, Loop Clips lane, physical lane inside one scroller; the existing 28px ruler, 38px physical lane, 34px action row, 14px scrollbar, 18px pitch, and 24px cell rules remain.
    - Test 4: body and badge activation do not call physical-cell navigation/selection/drag handlers.
  </behavior>
  <action>
    Add a loop-present class/data modifier driven directly from non-empty canonical ranges per D-35; do not change the base 161px rules. Apply the exact approved 193px and 32px conditional dimensions, lane background/border, and capsule vertical geometry. Per D-34 and D-38, keep the lane as a sibling above `.physics-paint-lane`, sharing its scroller and frame origin, with no overlay and no second scroll state. Preserve all existing physical-cell palette and additive linked badge behavior. Extend source/DOM/CSS tests to pin the existing physical-cell event ordering and real-key-only drag guard alongside the new lane isolation.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No-loop geometry remains exactly 161px with no lane footprint.
    - Loop-present geometry is exactly 193px with one 32px lane and 24px capsule visuals.
    - Existing ruler, physical lane, action row, scrollbar, pitch, palette, and key-selection/drag behavior remain regression-green.
    - Lane controls cannot fall through to physical-cell handlers.
  </acceptance_criteria>
  <done>The corrected tracer is geometrically exact, hidden when empty, and isolated from established Roto cell interactions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical physical document → EFX lane | Resolver-owned Loop Clip ranges enter a local presentation surface |
| EFX lane → local controller | Untrusted pointer/keyboard intent requests a parent-authoritative physical edit/dialog operation |
| physical data → visible copy | Missing source identifiers may be rendered to the user |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-11-01 | Tampering | lane/controller wiring | high | mitigate | Use canonical resolver context and the existing exact accepted local controller path; do not create a second mutable projection authority |
| T-43-11-02 | Spoofing | main-timeline duplicate surface | medium | mitigate | Remove renderer invocation and test that the main timeline emits no Loop Clip visual control per D-33 |
| T-43-11-03 | Denial of Service | Infinity presentation | high | mitigate | Symbolic Infinity plus visible-window bounded derivation; no materialized destination list per D-40 |
| T-43-11-04 | Information Disclosure | unresolved source IDs | low | mitigate | Render identifiers only as escaped Preact text nodes in later popover expansion |
| T-43-11-SC | Tampering | package supply chain | high | mitigate | No package installs or dependency changes are permitted in this plan |
</threat_model>

<verification>
- Run each task's exact one-shot Vitest command.
- Confirm the tracer test demonstrates canonical data → EFX lane → local Loop Edit and main-timeline absence.
- Confirm conditional CSS and physical-cell regressions pass without starting the server.
</verification>

<success_criteria>
- Native UAT Step 1 can be restarted on the correct EFX Paint surface.
- One production Loop Clip path works locally end-to-end and the main timeline draws none.
- Hidden-when-empty geometry and lazy visible-window invariants are test-pinned.
- The result is automated-ready only; native approval remains the existing 43-10 checkpoint.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx`
- `app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts`
- Focused tracer, geometry, lazy-derivation, renderer-absence, and physical-cell-isolation regressions
