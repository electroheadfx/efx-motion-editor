---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 12
type: execute
wave: 9
depends_on:
  - 43-11
files_modified:
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: medium
revised: 2026-08-08
revision_reason: supersede the unimplemented dedicated actions popover with the user-approved rail, tooltip, contextual Scripts sidebar, and Studio-local Edit contract
must_haves:
  truths:
    - "D-37R: normal, selected/focused, truncated, unresolved, busy, rejected, and Effective 0f rail states remain visible without geometry changes or color-only status."
    - "D-38R: the styled tooltip remains above the rail with display name, Cycle, Effective, mode, and status, with no raw identifiers."
    - "D-39R/D-57: plain/range/toggle rail gestures update session selection and contextual Scripts state only; rail selection remains visually line-only and mutually exclusive with physical selection."
    - "D-40R: rail double-click, focused Enter, and sidebar Edit call the same Studio-local openLoopEdit(loopId) route exactly once."
    - "D-43 through D-45: the Scripts panel preserves the Play/Edit slot swap, seven accepted facts, text-only source rename, narrow layout, and long-text accessibility."
    - "D-59: no PhysicsPaintLoopClipPopover, anchored actions dialog, popover focus lifecycle, hidden menu, rail destructive shortcut, or specialized bridge substitute is introduced."
    - "Canonical Duplicate, Repair, Relink, Unlink, and Delete controller operations remain regression-protected internal authority without a new Phase 43 UI surface."
    - "D-41 remains deferred: no horizontal Loop Clip placement drag is implemented."
  artifacts:
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "exhaustive normal/truncated/unresolved/zero-effective rail projection"
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx"
      provides: "complete tooltip, selection, keyboard, focus, and exact-once Edit activation"
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "contextual Loop Clip inspector and local Edit control"
  key_links:
    - from: "rail and sidebar Edit controls"
      to: "existing rotoPlayScript controller"
      via: "one Studio-local openLoopEdit port"
      pattern: "openLoopEdit"
    - from: "accepted Loop Clip state"
      to: "rail geometry and inspector facts"
      via: "canonical presentation projection after exact acknowledgement"
      pattern: "loopId|placementStart|effective"
  prohibitions:
    - statement: "No optimistic rail width, Cycle, Effective, status, source reference, or inspector fact update is permitted before accepted authority state."
      status: resolved
      verification: automated
    - statement: "No controller guard, authority check, revision check, history rule, or canonical resolver logic may be copied into presentation components."
      status: resolved
      verification: automated
    - statement: "No dedicated actions popover, window.prompt, raw key-ID entry, main-timeline store, playback dependency, or specialized Loop Clip bridge is used by the final surface."
      status: resolved
      verification: automated
---

<objective>
After the D-49 tracer approval, complete the integrated Loop Rail state, accessibility, tooltip, contextual Scripts inspector, and exact-once Studio-local Edit behavior while explicitly omitting the superseded dedicated actions popover.

Purpose: Complete HOLD-06 on the compact user-approved rail/sidebar surfaces without recreating controller or resolver authority in UI code.
Output: Exhaustive rail state presentation, robust contextual inspector, exact-once local Edit activation, and structural no-popover regressions.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
@app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN exhaustive rail states, tooltip, and keyboard behavior</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <behavior>
    - Sentinel test: `projects exhaustive rail states without changing accepted geometry`.
    - The sentinel covers normal, selected/focused, busy, truncated, unresolved, rejected, and Effective 0f presentation from canonical fixtures, including accessible names and tooltip order.
  </behavior>
  <action>RED: add exactly one test named `projects exhaustive rail states without changing accepted geometry` to `physicsPaintLoopClipPresentation.test.ts`. Run the named one-shot Vitest command. GREEN: make only the pure presentation, rail, and CSS changes required for deterministic state precedence, 0f geometry, tooltip facts, pressed/busy/described-by semantics, one stable tab stop per loop, line-only rail selection, keyboard distinctions, long-text safety, and reduced-motion-safe paint changes. Keep canonical geometry immutable and do not copy resolver boundary logic into the UI. Run the sentinel byte-for-byte unchanged, then both presentation and rail files together.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts -t "projects exhaustive rail states without changing accepted geometry" && pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx</automated>
  </verify>
  <done>The integrated rail has complete deterministic visual, tooltip, keyboard, selection, and accessibility behavior for every canonical state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the contextual sidebar, local Edit route, and no-popover boundary</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <behavior>
    - Sentinel test: `preserves contextual loop facts through busy rejection and text-only rename`.
    - Rail and Studio regressions prove double-click, focused Enter, and sidebar Edit converge on one local openLoopEdit route and no dedicated popover component or mount exists.
  </behavior>
  <action>RED: add the named Scripts-panel sentinel and exact local-route source/render assertions. GREEN: preserve the existing tab/resizer/scroll shell, exact fact order, explicit unresolved values, accessible long-text treatment, text-only source rename, and the contextual Pencil/Edit action. Keep one selected-loop Signal boundary. Plain/range/toggle rail selection updates the inspector only; Space selects, Escape hides tooltip state, and Enter/double-click/sidebar Edit call openLoopEdit exactly once. Do not add PhysicsPaintLoopClipPopover, portal/outside-click state, rail destructive shortcuts, hidden menus, or specialized bridge calls. Keep canonical unexposed loop operations in controller/history tests only.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx -t "preserves contextual loop facts through busy rejection and text-only rename" && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts</automated>
  </verify>
  <done>The contextual inspector and Studio-local Edit route are complete and accessible, and the superseded dedicated actions popover is structurally absent.</done>
</task>

</tasks>

<verification>
- Run each focused one-shot Vitest command after its RED/GREEN slice.
- Confirm Plan 43-11 native approval exists before starting this plan.
- Run controller/history suites as read-only authority oracles; do not imply that their internal methods have new UI exposure.
</verification>

<success_criteria>
- Rail, tooltip, sidebar, and modal states are implemented and test-mapped without D-41 drag.
- Exact-once local Edit activation and one selected-loop Signal boundary remain green.
- D-59 is structural: no dedicated actions popover or replacement bridge/menu surface exists.
- HOLD-05 authority/history and corrected HOLD-06 rail/sidebar information remain green.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Complete integrated-rail state, tooltip, keyboard, focus, and reduced-motion behavior
- Robust contextual Scripts inspector and Studio-local exact-once Edit activation
- Explicit structural no-popover contract with canonical controller operations retained internally
