---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 12
type: execute
wave: 9
depends_on:
  - 43-11
files_modified:
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-36: EFX Paint renders the complete approved filmstrip contract from canonical ordered real-key payloads: source thumbnails, linked repetitions, finite/Infinity badge, requested/effective truncation, amber diagonal, unresolved state, and zero-effective anchor."
    - "D-40: ordered sourceKeyIds join to PhysicPaintRotoRealKeyRecord.payload.dataUrl without repeated thumbnail/cache assets; dangling IDs remain explicit unresolved entries."
    - "D-34/D-35: zero loops render no lane, while one or many loops share exactly one 32px lane and never create stacked rows."
    - "D-37: capsule body activation opens the local occurrence popover and badge activation opens the existing Loop Edit dialog; both are keyboard reachable and remain inside EFX Paint."
    - "D-39: Signal-controlled Duplicate and Relink flows gather validated local input, route through existing controller/coordinator/authority/history seams, and close/reset only after exact accepted success."
    - "Rejected operations keep the active subflow and popover open, preserve lane selection/focus, and display the controller-provided reason; busy actions expose a reason rather than silently doing nothing."
    - "S2-S6 remain intact: Loop/Source Edit, Link/Create, additive linked-cell treatment, guard copy, placeholder preview, export block, and atomic Undo/Redo are reused rather than reimplemented."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx"
      provides: "EFX-local occurrence facts, ordered actions, accessibility, busy and rejection states"
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx"
      provides: "complete S1 capsule composition and selected/focus/error state precedence"
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "complete pure copy, ordered source-payload lookup, compatible-cycle options, zoom, truncation, occurrence, and unresolved derivation"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts"
      provides: "canonical Studio-to-lane payload threading and one-shared-lane contracts"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      via: "ordered real-key records threaded through identity memo, workflow strip, and lane; sourceKeyIds join to payload.dataUrl"
      pattern: "orderedRealKeyRecords|sourceKeyIds|payload.dataUrl"
    - from: "app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx"
      to: "app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts"
      via: "Signal-controlled Duplicate/Relink forms call narrow async operation ports forwarded by Studio"
      pattern: "duplicateLinkedLoop|unlinkLoop|repairLoop|relinkLoop"
    - from: "app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx"
      to: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      via: "visible immutable presentation records"
      pattern: "derive.*LoopClip"
    - from: "accepted controller result"
      to: "popover lifecycle"
      via: "await result; close only when ok is true"
      pattern: "ok|reason"
  prohibitions:
    - statement: "MUST NOT infer mutation success from dispatch, pending operation ID, or optimistic local geometry (D-39)."
      status: resolved
      verification: automated
    - statement: "MUST NOT use Delete/Backspace as a Loop Clip lane shortcut (D-37/UI contract)."
      status: resolved
      verification: automated
    - statement: "MUST NOT import sequenceStore, playbackEngine, main-timeline layout state, or Loop Clip bridge requests into the EFX popover."
      status: resolved
      verification: automated
    - statement: "MUST NOT reduce the approved full filmstrip to a plain range block or list (D-36)."
      status: resolved
      verification: automated
    - statement: "MUST NOT use window.prompt, raw key-ID entry, or a restored main-timeline bridge for Duplicate or Relink (D-37, D-39)."
      status: resolved
      verification: automated
    - statement: "MUST NOT synthesize data URLs or durable thumbnail records for virtual repetitions (D-40)."
      status: resolved
      verification: automated
---

<objective>
Expand the corrected EFX tracer into the complete approved S1/S1a filmstrip and local operation contract while preserving all accepted Loop Clip authority, S2-S6 behavior, and physical-cell interaction boundaries.

Purpose: Deliver HOLD-06 on its correct host and reconnect every user action directly to the already-proven EFX controller instead of recreating business logic.
Output: Complete filmstrip visuals, accessible local occurrence popover, accepted-only local operations, and focused regressions for every state and action.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
@app/src/components/import/UsagePopover.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread canonical source payloads into the full EFX filmstrip</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.test.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-34, D-35, D-36, D-40 and carried-forward D-15..D-32)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S1 source thumbnails, one-many lane composition, zoom bands, palette, copy, visual-state precedence)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (canonical EFX data and local geometry assignments)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoRealKeyRecord keyId and payload.dataUrl authority)
    - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts (orderedRealKeyRecords and compact loopResolutionContext Signals)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (canonical records and controller ownership)
    - app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts (identity-memo workflow construction)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (same-scroller lane host)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (43-11 compact model)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx (43-11 tracer lane)
    - app/src/components/timeline/loopCapsuleGeometry.ts (remaining host-neutral algorithms to relocate before deletion)
  </read_first>
  <behavior>
    - Test 1: ordered sourceKeyIds join by keyId to canonical PhysicPaintRotoRealKeyRecord payload.dataUrl values in source-cycle order; a dangling ID remains an explicit unresolved slot with the verbatim ID rather than being dropped or assigned a neighboring image.
    - Test 2: a normal finite source-backed loop renders those canonical thumbnails/real-key diamonds, virtual repeated ghost cells without diamonds, and exact finite badge math.
    - Test 3: a duplicated placement reuses the same source dataUrl references with dashed linked first-cycle treatment and no destination real-key diamonds; repetitions add no payload-map entries, data URLs, durable records, or cache assets.
    - Test 4: zero loops render no lane, while one loop and multiple overlapping/non-overlapping loops render as capsules in the same single 32px lane without additional rows.
    - Test 5: high/default/low frame-pitch bands derive ghost cells, hatch band, and compact solid band respectively, while preserving the truncation edge.
    - Test 6: partial and complete-cycle truncation keep requested badge math and derive exact English truncation facts; zero-effective renders a keyboard-reachable `0f` anchor.
    - Test 7: unresolved records remain visible with red precedence and verbatim missing IDs represented as text data, not markup.
  </behavior>
  <action>
    Start with the canonical payload-link tests, then enrich the pure model and lane. Keep geometry/range authority in `rotoTimelineModel.loopResolutionContext.value`, and separately thread `rotoTimelineModel.orderedRealKeyRecords.value` from PhysicsPaintStudio through the identity-memo workflow props, PhysicsPaintWorkflowStrip, and PhysicsPaintLoopClipLane. In `physicsPaintLoopClipPresentation.ts`, build one immutable lookup keyed by real-key `keyId`; project each ordered `sourceKeyIds` entry to its existing `payload.dataUrl`, dimensions when present, and real-key identity. Do not add data URLs to `PhysicPaintRotoLoopResolutionContext`, import `frameMap`/timeline layout types, or synthesize payloads for unresolved or repeated cells. Preserve dangling IDs as unresolved presentation entries. Per D-36, render every approved source-cycle, repetition, badge, requested/effective, truncation, unresolved, stale, and zero-effective semantic from S1. Distinguish source-backed originals from duplicated linked placements so diamonds appear only on real source keys while duplicate placements reference the same canonical image values. Preserve the three zoom bands and visual precedence unresolved → focus → selected → truncation → hover → idle. Keep ghost cells visual subdivisions, not Tab stops, and body/badge as the only controls. Per D-34, D-35, and D-40, render all capsules inside one conditionally present 32px lane and bound derivation to the visible window without creating per-occurrence image/cache records. The broad file set is intentional and sequential: this task establishes the complete Studio→view-model→strip→lane→pure-presentation payload path before 43-13 deletes the former timeline projection.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipLane.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests pin ordered canonical dataUrl mapping, dangling IDs, original diamonds, duplicate image sharing without diamonds, and zero repeated assets.
    - Tests cover one shared 32px lane for one or many loops plus finite, Infinity, all zoom bands, partial/complete truncation, unresolved, stale, and zero-effective states.
    - Exact locked badge and truncation copy is produced in English.
    - Every repeated destination remains virtual and non-key; no repeated durable/cache assets are produced.
    - Error, focus, selection, and truncation remain simultaneously legible under the declared precedence.
  </acceptance_criteria>
  <done>S1 receives canonical EFX real-key images end-to-end and renders the complete approved filmstrip in one shared lane without a timeline projection or repeated assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the accessible local occurrence popover and exact action model</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-37)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S1a facts, action ordering, 260-320px sizing, keyboard/focus contract)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (popover lifecycle, copy/action relocation, local operation routing)
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (focus/hover and viewport-clamp idiom)
    - app/src/components/import/UsagePopover.tsx (portal, outside-click, propagation isolation)
    - app/src/components/timeline/TimelineCapsuleTooltip.tsx (host-neutral facts/action ordering only; dependencies explicitly excluded)
  </read_first>
  <behavior>
    - Test 1: body pointer or Enter/Space opens a 260-320px local popover with facts ordered badge, start, Requested/Effective, mode, occurrence, then truncation/unresolved detail; opening does not navigate.
    - Test 2: applicable actions appear in the exact prescribed order and inapplicable actions are omitted for normal, unresolved, zero-effective, and truncated states.
    - Test 3: Escape and outside click close without an operation, clear only lane selection emphasis, and return focus to the originating body; Delete/Backspace executes no Loop Clip action.
    - Test 4: unresolved identifiers render verbatim as text and the remedy line follows them; a long missing-ID list wraps inside a viewport-bounded facts region with max-height and overflow-y scrolling rather than expanding beyond the application surface.
    - Test 5: badge remains a separate control whose Enter/Space activation opens Loop Edit rather than the body popover.
  </behavior>
  <action>
    Implement `PhysicsPaintLoopClipPopover` as the focused S1a component. Use local hooks only for document-level outside click, Escape, focus return, and measured/clamped placement; do not synchronize model props into state. Relocate the pure fact/action selection into the EFX presentation module and preserve exact UI-SPEC order and copy. The popover stays inside EFX Paint, 8px from its anchor, width-bounded to 260-320px, horizontally clamped, and text-wrapping for unresolved IDs. Give the unresolved-detail region an application-height-aware `max-height` and `overflow-y: auto` so long dangling-ID lists scroll inside the popover without horizontal overflow or viewport escape. Keep body and badge activation distinct per D-37. Do not import or call sequenceStore, playbackEngine, window.prompt, requestPhysicPaintLoopOperation, or any global Loop Clip request Signal. Busy actions may remain visible only with `aria-disabled="true"` and a reason; they must not silently no-op. Delete and Unlink retain separate labels but share unlink-only data semantics.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx src/components/physic-paint/view/PhysicsPaintLoopClipLane.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Popover facts and action ordering exactly match S1a for all declared states.
    - Pointer, Enter/Space, Escape, Tab order, outside click, and focus return are covered.
    - Long unresolved lists wrap and scroll within a max-height-bounded region without escaping the application viewport.
    - Delete/Backspace does not trigger a lane mutation.
    - The component has no main-editor store, playback, prompt, or Loop Clip bridge dependency.
  </acceptance_criteria>
  <done>S1a is a complete accessible EFX-local occurrence/action surface with no cross-window ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement controlled Duplicate and Relink flows through accepted authority</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/usePhysicsPaintStudioViewModel.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-37, D-39)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (accepted/rejected lifecycle, Duplicate destination form, compatible-cycle Relink chooser)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (focused ports, Preact Signals, and accepted-only pattern)
    - app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts (existing adapter and exact accepted result)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (duplicateLinkedLoop/relinkLoop signatures, authoritative collision/source validation, unlink-only semantics)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (ordered sourceKeyIds and canonical real-key records)
    - app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts (atomic loop snapshot Undo/Redo)
  </read_first>
  <behavior>
    - Test 1: Edit source frame uses the existing EFX navigation/real-key selection port for the modulo-resolved source frame; Repair, Unlink, and Delete call their corresponding existing focused controller ports, with Delete and Unlink sharing unlink-only semantics.
    - Test 2: Duplicate opens a controlled local form with a labeled destination input and Cancel; only trimmed base-10 non-negative safe-integer text enables submit, while malformed, negative, decimal, empty, and overflow values show local shape errors without controller dispatch.
    - Test 3: valid Duplicate submission calls `duplicateLinkedLoop(loopId, destinationStart)` once; Cancel returns to the action list without mutation, rejection keeps the form open and refocuses the destination input with the returned reason, and acceptance closes/resets only after awaited `ok: true`.
    - Test 4: Relink opens a controlled chooser of compatible existing source cycles derived from current canonical loop ranges/real-key records, deduplicated by ordered sourceKeyIds, excluding the current unresolved cycle and any option containing a dangling key; raw key-ID entry is unavailable.
    - Test 5: selected Relink submission calls `relinkLoop(loopId, selectedSourceKeyIds)` once; Cancel, rejection focus/reason retention, pending lock, and accepted-only close/reset match Duplicate.
    - Test 6: while any operation promise is pending, repeated pointer or keyboard activation cannot dispatch twice and the active control exposes its busy reason.
  </behavior>
  <action>
    Per D-39, forward narrow async ports from the existing `rotoPlayScript` controller through the identity-memo workflow props: `openLoopEdit`, `duplicateLinkedLoop`, `unlinkLoop`, `repairLoop`, and `relinkLoop`; reuse the existing frame-navigation callback for Edit source frame. Use focused `@preact/signals` state owned by the local popover interaction surface for the active subflow, destination draft, selected compatible cycle, pending action, and rejection reason. Derive validation and compatible-cycle options directly/purely; do not mirror model props with `useEffect`. Duplicate must render a semantic form with a labeled numeric/text input whose parser accepts only a complete non-negative safe integer before invoking `duplicateLinkedLoop`; this is input-shape validation only, while collision, overlap, authority, revision, and commit validation remain solely in the controller. Relink must render a radio/select-style chooser from existing fully resolved compatible source cycles, deduplicated by exact ordered `sourceKeyIds`; label options with human-readable source-frame/cycle facts and pass the selected immutable key-ID list to `relinkLoop`. Do not expose raw key-ID entry, `window.prompt`, main-timeline state, or a bridge request. Both forms provide Cancel back to the action list, preserve lane selection, retain/refocus the active field or chooser after `ok: false`, show the controller `reason`, lock duplicate submission while pending, and close/reset the subflow plus occurrence popover only after awaited `ok: true`. Do not copy guards, authority requests, revision checks, publications, acknowledgement assertions, or history logic into UI components. Keep S2 Loop Edit, S3 Source Edit/Repair, S4 Link/Create, S5 additive linked cells, and S6 guard/preview/export behavior on their existing implementations. This task intentionally revisits the Studio/view-model/strip files after Task 1 in the same sequential plan to add operation ports only after canonical presentation data is established; no speculative shared-state abstraction is introduced.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every S1a action reaches an existing EFX controller/navigation port and no controller business rule is duplicated in the component.
    - Duplicate has executable validated destination and Cancel flows; Relink has executable compatible-cycle selection and Cancel flows with no raw ID input.
    - Signal-owned drafts, accepted-only close/reset, rejected-open reason/focus, busy lock, and no double-dispatch are test-pinned.
    - Neither flow imports or calls window.prompt, main-timeline state, or the removed Loop Clip bridge.
    - Unlink/Delete preserve source keys and remain atomic Undo/Redo operations.
    - Existing controller and history suites remain green for Update, Duplicate, Unlink, Repair, Relink, exact acknowledgement, and rejection paths.
  </acceptance_criteria>
  <done>All Loop Clip authoring actions, including concrete Duplicate and Relink input flows, are local, accessible, parent-authoritative, exact-acknowledged, and correctly reflected in the EFX lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lane/popover → controller | User-selected identifiers and destinations request physical mutations |
| controller → UI acknowledgement | Only exact accepted output may authorize visible success/closure |
| canonical real-key payloads → filmstrip | Ordered key IDs join to persisted image data URLs and dangling references must fail closed |
| unresolved document → popover | Persisted dangling identifiers are untrusted display text |
| local Duplicate/Relink forms → controller | User-entered destination and selected source-cycle identities request physical mutations |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-12-01 | Tampering | operation acknowledgement | high | mitigate | Await existing exact accepted controller result; rejection preserves UI state and reason per D-39 |
| T-43-12-02 | Elevation of Privilege | local action routing | high | mitigate | Expose narrow existing controller ports only; retain project/layer authority, revision, and publication validation |
| T-43-12-03 | Denial of Service | repeated busy activation | medium | mitigate | Mutation lock prevents duplicate dispatch while preserving an accessible reason |
| T-43-12-04 | Information Disclosure | unresolved identifiers | medium | mitigate | Preact text-node rendering only; bounded scrolling; no HTML injection or prompt interpolation |
| T-43-12-05 | Tampering | source thumbnail association | high | mitigate | Join ordered sourceKeyIds only to canonical real-key payload.dataUrl records; dangling IDs remain unresolved and repetitions create no assets |
| T-43-12-06 | Tampering | Duplicate/Relink input | high | mitigate | Controlled Signal forms validate destination shape and offer only canonical compatible cycles; controller retains all domain/authority validation |
| T-43-12-07 | Repudiation | unlink/delete history | medium | mitigate | Existing atomic physical snapshot history records accepted operation and supports Undo/Redo |
| T-43-12-SC | Tampering | package supply chain | high | mitigate | No package installs or dependency changes are permitted in this plan |
</threat_model>

<verification>
- Run the three exact one-shot Vitest commands.
- Confirm canonical real-key dataUrl threading, one shared lane for one/many loops, bounded unresolved scrolling, and all S1/S1a presentation states are behaviorally covered.
- Confirm Duplicate/Relink controlled input, Cancel, focus, rejection, pending, and accepted-only routes are covered without prompt or bridge use.
- Confirm existing controller/history tests prove accepted authority, rejection, and atomic Undo/Redo.
</verification>

<success_criteria>
- HOLD-06's full filmstrip receives canonical ordered real-key thumbnails and the local popover is complete on the corrected EFX host.
- One or many loops share one 32px lane; long unresolved lists remain viewport-bounded and scroll internally.
- Duplicate and Relink are executable local controlled flows that close/reset only after accepted authority.
- Touched HOLD-05 operations retain canonical persistence, resolution, authority, and history invariants.
- S2-S6 remain regression-green without duplicate implementations.
- No new dependency or server run is required.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx`
- Canonical ordered real-key thumbnail threading from Studio through the EFX lane/presentation
- Complete one-shared-lane filmstrip presentation, bounded popover styles, and Signal-controlled Duplicate/Relink flows
- Focused payload, presentation, accessibility, action-routing, rejection, focus, and accepted-only regressions
