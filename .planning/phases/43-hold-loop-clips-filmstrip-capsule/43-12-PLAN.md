---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 12
type: execute
wave: 9
depends_on:
  - 43-11
files_modified:
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-39R/D-46 popover-loading: busy actions remain visible only when their reason is discoverable, use aria-disabled, prevent double dispatch, and wait for accepted authority state."
    - "D-39R/D-46 popover-error: rejection keeps the popover open, preserves selection/focus/input, and announces `Loop Clip was not changed. {reason}`."
    - "D-39R popover-populated: facts appear in the locked order followed by applicable Duplicate, Repair, Relink, Unlink, and Delete actions."
    - "D-39R popover-partial: inapplicable actions are omitted and temporarily unavailable actions expose an explicit reason rather than silently doing nothing."
    - "D-38R popover-overflow: the 272–304px surface clamps to 8px viewport margins and only its body may scroll vertically."
    - "D-39R popover-zero-one-many: one labelled non-modal dialog is anchored to the one selected rail target and its action set varies deterministically by loop state."
    - "D-45 popover-long-text: fact values wrap or ellipsize inside the clamped surface, expose full accessible text, and never show raw IDs as product copy."
    - "D-43/D-46 sidebar-loading: inspector facts stay visible during preparation/commit and Edit remains discoverably unavailable rather than silently inert."
    - "D-44 sidebar-error: unresolved/rejected state preserves facts and selection, shows status/recovery copy, and exposes Repair/Relink through existing flows."
    - "D-44/D-45 sidebar-partial: missing source data is explicit, no required fact silently disappears, and no UUID becomes product copy."
    - "D-45 sidebar-long-text: display/source names ellipsize on one line, styled tooltip/ARIA exposes the full value, and rename remains a text-only trigger."
    - "D-01/D-40R modal-loading: existing `Preparing Loop Clip…` and commit progress remain visible without optimistic values or layout replacement."
    - "D-01/D-46 modal-error: validation/authority rejection preserves current inputs, focus, modal availability, and exact controller reason."
    - "D-31 modal-partial: unresolved references route to existing repair/relink-capable behavior rather than silently missing fields."
    - "D-01 modal-overflow: the carried-forward compact modal remains contained at the minimum application window without changing Studio layout."
    - "D-45 modal-long-text: long script/loop values wrap or ellipsize accessibly without exposing raw identifiers."
    - "D-37R: normal, selected/focused, truncated, unresolved, busy, rejected, and Effective 0f rail states remain visible without geometry changes or color-only status."
    - "D-39R/D-40R accepted-operation focus: Duplicate/Repair/Relink restore the surviving selected rail target; Unlink/Delete choose the nearest visible loop by placement order; no surviving loop focuses Scripts and restores normal script context."
    - "D-41 remains deferred: no horizontal Loop Clip placement drag is implemented."
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx"
      provides: "accessible EFX-local facts and applicable operation controls"
    - path: "app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts"
      provides: "exhaustive normal/truncated/unresolved/zero-effective display and action derivation"
    - path: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx"
      provides: "complete keyboard, focus, tooltip, status, and focus-restoration behavior"
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "robust contextual inspector loading/error/overflow behavior"
  key_links:
    - from: "rail/popover/sidebar actions"
      to: "existing rotoPlayScript controller"
      via: "focused async ports returning explicit accepted/rejected results"
      pattern: "duplicateLinkedLoop|unlinkLoop|repairLoop|relinkLoop|openLoopEdit"
    - from: "controller accepted state"
      to: "rail geometry, inspector facts, and popover lifecycle"
      via: "canonical state refresh after exact acknowledgement"
      pattern: "ok|reason"
    - from: "focused rail or sidebar control"
      to: "Edit Loop Clip modal close"
      via: "invoker-specific focus restoration"
      pattern: "focus"
  prohibitions:
    - statement: "No optimistic rail width, Cycle, Effective, status, source reference, popover close, or inspector fact update is permitted before accepted authority state."
      status: resolved
      verification: automated
    - statement: "No controller guard, authority check, revision check, history rule, or canonical resolver logic may be copied into presentation components."
      status: resolved
      verification: automated
    - statement: "No window.prompt, raw key-ID entry, main-timeline store, playback dependency, or specialized Loop Clip bridge is used by the local popover."
      status: resolved
      verification: automated
---

<objective>
After the D-49 tracer approval, expand the integrated Loop Rail into its complete state, accessibility, tooltip, local-popover, focus, and accepted-only operation contract per D-37R through D-47 while retaining D-01 through D-32 behavior.

Purpose: Complete HOLD-06 on the approved compact rail/sidebar surfaces without recreating controller or resolver authority in UI code.
Output: Exhaustive rail state presentation, accessible local actions popover, robust contextual inspector, and focused behavior regressions.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
@app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN exhaustive rail states, tooltip, and keyboard/focus behavior</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (rail state precedence, copy, roles/names, focus, reduced motion, 0f marker)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-11-SUMMARY.md (approved tracer interfaces and native verdict)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (pure exhaustive presentation pattern)
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (1000ms delay, placement, ARIA idiom)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (authoritative Requested/Effective/status inputs)
  </read_first>
  <behavior>
    - Sentinel test: `projects exhaustive rail states without changing accepted geometry`.
    - The sentinel covers normal, selected/focused, busy, truncated, unresolved, rejected, and Effective 0f presentation from one canonical fixture table, including the exact accessible name and tooltip order.
  </behavior>
  <action>RED: add exactly one test named `projects exhaustive rail states without changing accepted geometry` to `physicsPaintLoopClipPresentation.test.ts`. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts -t "projects exhaustive rail states without changing accepted geometry"`. The precise RED failure is that the unresolved fixture does not override rail paint while preserving the same accepted left/width geometry and the Effective 0f fixture does not expose the specified marker/accessible status. GREEN: make only the pure presentation and rail rendering changes required by this table per D-24 through D-32, D-37R, D-38R, and D-40R: deterministic state precedence, 0f glyph/target, exact tooltip facts, pressed/busy/described-by semantics, one stable tab stop per loop, keyboard distinctions, focus restoration, long-text safety, and immediate reduced-motion-safe paint changes. Keep canonical geometry immutable and do not copy resolver boundary logic into the UI. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts -t "projects exhaustive rail states without changing accepted geometry"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run both presentation and rail files together.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts -t "projects exhaustive rail states without changing accepted geometry" && pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All declared rail states and state precedence are independently test-pinned.
    - Keyboard, focus restoration, accessible names, tooltip order, and reduced-motion behavior match the UI-SPEC.
    - No status disappears, no status relies on color alone, and no state changes accepted geometry optimistically.
  </acceptance_criteria>
  <done>The integrated rail has complete deterministic visual, keyboard, tooltip, and accessibility behavior for every canonical state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-GREEN the local actions popover through existing accepted controller ports</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-39R, D-40R, D-46 accepted-only local actions)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S1b facts/action order, placement, focus, busy/rejection lifecycle)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (existing operation signatures and explicit results)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (approved trigger/focus contract)
    - app/src/components/import/UsagePopover.tsx (anchored portal/outside-click isolation pattern where applicable)
  </read_first>
  <behavior>
    - Sentinel test: `keeps rejected local actions open and restores accepted focus deterministically`.
    - The sentinel drives one selected rail target through a rejected operation and an accepted removal, proving accepted-only close/update plus nearest-loop-or-Scripts focus restoration.
  </behavior>
  <action>RED: add exactly one test named `keeps rejected local actions open and restores accepted focus deterministically` to `PhysicsPaintLoopClipPopover.test.tsx`. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx -t "keeps rejected local actions open and restores accepted focus deterministically"`. The precise RED failure is that a rejected action closes or loses the selected trigger instead of announcing `Loop Clip was not changed. {reason}`, and/or accepted Unlink/Delete fails to focus the nearest surviving loop or Scripts when none remains. GREEN: make only the local popover/rail/CSS changes required by that lifecycle. Use the existing controller ports supplied by the tracer; do not add a second mutation seam. Keep fact/action ordering per D-39R, visible unavailable reasons, non-modal dialog semantics, viewport clamp/internal scrolling, accepted-only close, rejection preservation, and deterministic focus destinations per D-40R/D-46. Hooks are limited to document listeners, measured placement, and focus cleanup; shared canonical state remains Signal/controller-owned. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx -t "keeps rejected local actions open and restores accepted focus deterministically"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run the popover with controller/history regression oracles.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx -t "keeps rejected local actions open and restores accepted focus deterministically" && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Facts, action order/applicability, keyboard/outside-click behavior, overflow, and accessible dialog semantics match the UI-SPEC.
    - Every mutation uses existing controller/coordinator/authority/history paths with accepted-only close/update and no double dispatch.
    - Accepted Duplicate/Repair/Relink restore the surviving selected rail target; accepted Unlink/Delete choose the nearest visible loop by placement order; an empty result focuses Scripts and restores normal script context.
    - Rejected operations preserve prior visible state and expose the exact reason.
  </acceptance_criteria>
  <done>Every applicable Loop Clip operation is reachable locally and remains parent-authoritative, atomic, explicit, and recoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden contextual sidebar and carried-forward modal states</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S1c/S2 loading, error, partial, overflow, long-text contracts)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (43-11 contextual inspector and inline rename trigger)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (existing Loop/Source Edit lifecycle)
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (approved resizer/scroll shell to preserve)
  </read_first>
  <behavior>
    - Sentinel test: `preserves contextual loop facts through busy rejection and text-only rename`.
    - The sentinel covers the exact seven-fact order, explicit missing-source value, discoverable busy/rejection reason, long-name accessibility, and rename/Edit separation in one selected-loop flow.
  </behavior>
  <action>RED: add exactly one test named `preserves contextual loop facts through busy rejection and text-only rename` to `PhysicsPaintScriptsPanel.test.tsx`. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx -t "preserves contextual loop facts through busy rejection and text-only rename"`. The precise RED failure is that busy or rejected selected-loop context drops at least one required fact or the source-name interaction invokes Edit instead of the existing inline rename flow. GREEN: make only the Scripts-panel changes required by D-43 through D-45: preserve the existing tab/resizer/scroll shell, exact fact order, 88px label/minmax value layout, explicit unresolved values, accessible long-text treatment, discoverable unavailable/rejection reasons, and text-only source rename distinct from the primary Pencil/Edit action. Do not copy modal/controller logic into the sidebar. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx -t "preserves contextual loop facts through busy rejection and text-only rename"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run the full Scripts-panel file plus the carried-forward Play Script dialog regression file as a read-only oracle for D-01, D-02, D-03, D-05, D-10, D-20, and D-31.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx -t "preserves contextual loop facts through busy rejection and text-only rename" && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar loading/error/partial/overflow/long-text and rename distinctions are test-pinned.
    - Existing Loop/Source Edit behavior retains inputs, focus, status, and compact geometry on rejection or unresolved data.
    - Approved sidebar resizer, scrolling, and control ordering remain unchanged.
  </acceptance_criteria>
  <done>The contextual inspector and carried-forward modal remain complete and accessible across normal, busy, unresolved, rejected, narrow, and long-name states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| popover/sidebar → controller | User input requests canonical mutations but cannot establish authority |
| controller acknowledgement → UI lifecycle | Only exact accepted state can close controls or update canonical facts |
| unresolved metadata → product copy | Persisted names/references are untrusted text |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-12-01 | Tampering | accepted-only UI lifecycle | high | mitigate | Await explicit controller result; no optimistic geometry/facts/close |
| T-43-12-02 | Elevation of Privilege | operation routing | high | mitigate | Narrow existing controller ports only; retain project/layer/revision authority |
| T-43-12-03 | Denial of Service | repeated busy activation and long unresolved content | medium | mitigate | Pending lock plus bounded vertical scrolling |
| T-43-12-04 | Information Disclosure | raw identifiers and long names | medium | mitigate | Derived display names and escaped text nodes; no raw-ID product fallback |
| T-43-12-05 | Repudiation | unlink/delete actions | medium | mitigate | Existing atomic history and Undo/Redo tests retain accepted operation evidence |
| T-43-12-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Run each focused one-shot Vitest command after its RED/GREEN slice.
- Confirm Plan 43-11 native approval exists before starting this plan.
- Run the controller/history suites with the UI suites to prove local actions did not fork authority.
</verification>

<success_criteria>
- All remaining 16 covered UI-consideration rows are implemented and test-mapped.
- D-37R through D-47 expansion behavior is complete without implementing deferred D-41 drag.
- HOLD-05 authority/history and corrected HOLD-06 rail/sidebar information remain green.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Complete integrated-rail state, tooltip, keyboard, focus, and reduced-motion behavior
- Accessible EFX-local facts/actions popover with accepted-only controller lifecycle
- Robust contextual Scripts inspector and carried-forward modal regressions
