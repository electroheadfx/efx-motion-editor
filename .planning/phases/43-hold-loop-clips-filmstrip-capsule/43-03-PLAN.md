---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 03
type: execute
wave: 3
depends_on:
  - 43-02
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.rotoLoopClips.test.ts
  - app/src/lib/physicPaintBridge.ts
autonomous: true
requirements:
  - HOLD-04
  - HOLD-05

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A linked repetition frame returns the SOURCE key's rendered payload through getRotoPhysicalRenderSource with a source-scoped cache revision — one source cache entry serves every occurrence (D-26, D-27); the frame's sourceKeyId comes from the lazy per-frame query: sourceKeyIds[(frame - placementStart) % cycleLength]"
    - "A source-frame Paint edit invalidates the single source cache entry so every linked occurrence reflects the edit (D-26, HOLD-05 edit propagation)"
    - "A 5-frame cycle repeated 5 times resolves 25 timeline frames while storing only 5 source frame assets (ROADMAP success criterion 4)"
    - "Main-editor timeline length accounts for loops: getRotoPhysicalEndFrame returns max(last real key + 1, max loop effective end), bounded by parent end and the 600-frame capacity, computed from the interval derivation only — never by iterating virtual frames (Pitfall 3, D-25, Q4, D-32)"
    - "A commit carrying loopClips applies atomically with records through replace-roto-physical-map — the store never observes keys without loops or loops without keys (D-06, D-10)"
    - "An unresolved linked frame surfaces the typed 'linked-unresolved' result from the shared 43-02 contract — never a blank, never a throw, and never blanking unrelated frames (audit finding 3)"
    - "The store exposes which loops are unresolvable over a frame range so export preflight can block — computed from interval records' missingSourceKeyIds, O(loops + keys in range), no frame materialization (D-28 wiring; the block itself lands in 43-09)"
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "linked-frame render-source branch (typed contract), loop-aware end frame, loopClips state, unresolved-loop query"
      contains: "linked-unresolved"
    - path: "app/src/stores/physicPaintStore.rotoLoopClips.test.ts"
      provides: "store-level loop resolution and cache-identity spec"
  key_links:
    - from: "app/src/stores/physicPaintStore.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      via: "getRotoPhysicalRenderSource calls the lazy per-frame query and resolves 'linked' results via result.sourceKeyId; 'linked-unresolved' surfaces the typed contract"
      pattern: "linked-unresolved"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "replace-roto-physical-map acceptance applies records + loopClips in one store update"
      pattern: "loopClips"
  prohibitions:
    - statement: "MUST NOT create per-occurrence cache entries or rasters — cache weight scales with source cycles only (D-26, D-32)"
      status: resolved
      verification: test
    - statement: "MUST NOT feed linked occurrences through the legacy _rotoCacheMetadata display model — the physical path is the only authority (D-27)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let export silently differ from preview — every surface reads the same canonical resolution (D-27)"
      status: resolved
      verification: test
---

<objective>
Wire loop resolution into the canonical store seams: the linked-loop branch of `getRotoPhysicalRenderSource` (source-scoped cache identity), the loop-aware end-frame read (Pitfall 3), loopClips state acceptance on the bridge commit path, and the unresolved-loop query export preflight will consume.

Purpose: HOLD-04's one-resolved-raster-per-frame guarantee extends to linked occurrences; D-27 locks one canonical resolver consumed by preview, Studio, persistence coordinator, and export.
Output: every existing surface resolves linked repetition frames to the source raster with no consumer changes.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-02-SUMMARY.md
@app/src/stores/physicPaintStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: linked-loop render-source branch + loop-aware end frame + unresolved query</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.rotoLoopClips.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts (getRotoPhysicalRenderSource at 1405-1453; getRotoPhysicalEndFrame at 1399-1402; structural cache at 100-160; revision call sites 136, 746)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoPhysicalRenderSource union at line 122)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (typed resolution union + interval derivation + lazy per-frame query from 43-02)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pattern 4, Pitfall 3)
  </read_first>
  <behavior>
    - A linked resolution at frame F (kind 'linked' from the 43-02 per-frame query) resolves to the record of the query's sourceKeyId and returns kind 'real'-equivalent payload with cacheRevision `${contentRevision}:real:${sourceKeyId}` — identical cache identity for every occurrence of the same source frame
    - Editing the source key bumps contentRevision once and every occurrence's cacheRevision changes — one invalidation, all occurrences reflect it
    - A frame whose per-frame query returns 'linked-unresolved' surfaces that typed result (loopId, placementStart, missingSourceKeyIds) through the store — it never returns null-as-blank and unrelated frames resolve normally
    - getRotoPhysicalEndFrame with an infinity loop and last real key at frame 9 returns the loop's effective end (bounded by parent end and 600), not 10 — computed from the interval derivation, no virtual-frame iteration
    - getRotoPhysicalEndFrame with no loops and no keys still returns null (existing behavior preserved)
    - An unresolved-loop query over [from, to) returns each unresolvable loop with its placementStart and missing source key ids/frames; empty when all resolve; computed from interval records only
    - The 5×5 case: 25 resolvable frames, 5 distinct cache identities
    - A duplicated loop placed away from its source keys resolves at its placementStart via the shared sourceKeyIds — cache identity is the shared source key's, so duplicate occurrences add zero cache weight
  </behavior>
  <action>
    RED first: create app/src/stores/physicPaintStore.rotoLoopClips.test.ts (follow the physicPaintStore.rotoPhysicalStructuralCache.test.ts harness pattern) covering every behavior case; confirm RED; commit test(43-03). GREEN in physicPaintStore.ts: (1) add the linked-frame branch to getRotoPhysicalRenderSource — call the 43-02 lazy per-frame resolution query; on { kind: 'linked' } resolve the sourceKeyId's record and return its payload with the source-scoped cacheRevision `${contentRevision}:real:${sourceKeyId}`; on { kind: 'linked-unresolved' } surface the typed unresolved result (carrying loopId, placementStart, missingSourceKeyIds) rather than silently returning a blank — the preview placeholder rendering for this result lands in 43-09. (2) Make getRotoPhysicalEndFrame loop-aware: max of last real key + 1 and every loop's effective end from the 43-02 interval derivation, bounded by parent end and PHYSIC_PAINT_MAX_APPLY_FRAMES (600); no loops and no keys still returns null; never iterate virtual frames. (3) Hold loopClips in the per-layer physical state alongside records so the structural cache recomputes when loops change (revision fingerprint already covers loopClips per 43-01). (4) Export an unresolved-loop query (name discretion, e.g. getRotoPhysicalUnresolvedLoops(layerId, fromFrame, toFrame)) returning loop id, placementStart, and missing source key ids/frames for the export preflight — computed from the interval derivation's missingSourceKeyIds in O(loops + keys in range), no frame materialization. Do not touch _rotoCacheMetadata. Commit feat(43-03).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicPaintStore.rotoLoopClips physicPaintStore</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicPaintStore.rotoLoopClips exits 0
    - pnpm --dir app exec vitest run physicPaintStore exits 0 (existing store specs green)
    - The 5×5 case asserts exactly 5 distinct cacheRevision values across 25 resolved frames
    - getRotoPhysicalEndFrame returns the loop-bounded value in the infinity test case
  </acceptance_criteria>
  <done>Linked occurrences resolve to source rasters with one shared cache identity; timeline length accounts for loops; unresolved loops are queryable; existing store behavior green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bridge commit acceptance applies records + loopClips atomically</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/stores/physicPaintStore.rotoLoopClips.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts (replace-roto-physical-map apply path; apply-payload validation)
    - app/src/types/physicPaint.ts (allowlists extended in 43-01, lines 330 and 367)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts (snapshot replay path from 43-01)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Architecture diagram: commit path)
  </read_first>
  <behavior>
    - An apply payload carrying records + loopClips + expectedRevision is accepted only when the revision matches the loopClips-aware fingerprint (43-01)
    - On acceptance the store's records and loopClips update in one state transition — no intermediate observable state with one but not the other
    - A payload with a stale expectedRevision (loop-only change since) is rejected by the authority check
    - Undo replay of a loop operation restores records and loopClips together; Redo replay restores the operation result — both directions are one coherent state transition each
  </behavior>
  <action>
    Extend the replace-roto-physical-map acceptance in physicPaintBridge.ts so the validated loopClips collection (allowlisted in 43-01) is applied to the store in the same atomic update as records and selection — one state transition, one revision check against the loopClips-aware fingerprint. Add spec cases to physicPaintStore.rotoLoopClips.test.ts: atomic application, stale-revision rejection after a loop-only change, and undo AND redo replay restoring both records and loopClips in each direction. Commit feat(43-03).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicPaintStore.rotoLoopClips</automated>
  </verify>
  <acceptance_criteria>
    - The atomic-application and stale-revision spec cases pass
    - grep -n "loopClips" app/src/lib/physicPaintBridge.ts shows the acceptance path applying the collection (not just validating it)
    - pnpm --dir app exec vitest run exits 0
  </acceptance_criteria>
  <done>The single commit path carries loops atomically with keys; authority checks see loop-only changes; undo restores both together.</done>
</task>

</tasks>

## Flagged Assumptions

- **HOLD-04 edge-probe row (unclassified):** the probe could not classify an edge category for "one resolved raster per frame." Planner assumption: linked occurrences composite through the same single-raster-per-frame seam as real/generated keys (D-27), so HOLD-04 is satisfied by the canonical-path prohibition above plus the cache-identity tests. If a surface ever bypasses getRotoPhysicalRenderSource, HOLD-04 is violated — the executor must flag any such bypass found during implementation rather than routing around the seam.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child commit → store | Apply payload crosses into the document authority; revision is the guard |
| store → all render surfaces | One canonical read feeds preview, Studio, cache coordinator, export |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-03-01 | Tampering | replace-roto-physical-map acceptance | high | mitigate | loopClips-aware expectedRevision check rejects stale or forged commits; allowlist validation from 43-01 rejects unknown keys |
| T-43-03-02 | DoS | getRotoPhysicalEndFrame with infinity loops | medium | mitigate | Bounded by parent end and 600-frame capacity; no iteration over virtual frames |
| T-43-03-03 | Tampering | _rotoCacheMetadata pollution | medium | mitigate | Prohibition: linked occurrences never touch the legacy display model; code-review gate plus architectural note in SUMMARY |
| T-43-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicPaintStore.rotoLoopClips physicPaintStore — exit 0
- Full suite green
</verification>

<success_criteria>
- One source cache entry serves all occurrences; one source edit invalidates all
- Timeline length and end-frame reads are loop-aware and capacity-bounded
- Commits apply keys + loops atomically with revision authority intact
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- linked-frame branch of `getRotoPhysicalRenderSource` consuming the 43-02 typed resolution union (physicPaintStore.ts)
- loop-aware `getRotoPhysicalEndFrame` driven by the interval derivation (physicPaintStore.ts)
- `getRotoPhysicalUnresolvedLoops(layerId, fromFrame, toFrame)` returning loopId, placementStart, missing source key ids/frames (store export — final name is executor discretion)
- loopClips acceptance in the replace-roto-physical-map apply path (physicPaintBridge.ts)
- `physicPaintStore.rotoLoopClips.test.ts` (new spec)
