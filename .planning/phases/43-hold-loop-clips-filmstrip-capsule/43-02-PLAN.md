---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts
  - app/src/components/physic-paint/roto/rotoTimelineSelectors.ts
  - app/src/components/physic-paint/roto/rotoPhysicalTimelinePorts.ts
  - app/src/components/physic-paint/roto/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: true
requirements:
  - HOLD-05

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A frame inside a loop's effective range resolves in O(1) to the modulo source keyId: sourceKeyIds[(appFrame - canonicalStart) % cycleLength], with repeatIndex = floor((appFrame - canonicalStart) / cycleLength) (D-26, D-32)"
    - "Real keys always win over virtual linked-loop cells — materialize-local-key (D-12) and shrink (D-06) are emergent, not special-cased"
    - "Effective end = min(requested end, next boundary, parentEndExclusive) with half-open [start, end) intervals; valid boundaries are exactly a non-owned real key, another loop's canonical start, or parentEndExclusive; a loop never truncates itself (D-24)"
    - "Loop B (later canonical start) begins at its canonical start and is NOT pushed after loop A; A's effective end = min(A requested end, B start, parent end); same-start collisions are never resolved by hidden creation-order priority (D-14)"
    - "A boundary landing exactly at the loop's canonical start yields Effective = 0f and the loop object survives (D-08)"
    - "Moving or removing blocking content or a later loop re-expands the earlier loop's effective range automatically with no regeneration (D-08, D-14, D-25)"
    - "Infinity effective range is bounded by min(parent end, PHYSIC_PAINT_MAX_APPLY_FRAMES = 600) and never materializes a frame list (D-25, D-32; Q4 resolved: capacity-bounded)"
    - "A truncation landing mid-cycle is distinguishable from one landing exactly on a cycle boundary: (effectiveEnd - canonicalStart) % cycleLength !== 0 means partial (D-21)"
    - "An unresolved loop (dangling source keyIds) surfaces as a typed unresolved/error state through the projectionFailure idiom — it never throws in the read path and never silently disappears (D-31, D-23)"
    - "Resolving the same document twice yields identical effective ranges — derivation is pure (D-30)"
    - "Ghost/linked-loop cells are excluded from key selection and drag eligibility in every cell-kind consumer (D-23, D-11)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "linked-loop virtual cell kind, loop boundary derivation, shared capsule-model export"
      contains: "linked-loop"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts"
      provides: "RED spec for modulo resolution, boundary algebra, loop-loop priority, zero-effective, re-expansion"
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      via: "projection reads loopClips from the parsed physical document"
      pattern: "loopClips"
    - from: "app/src/components/physic-paint/roto/rotoTimelineSelectors.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      via: "selectors consume the extended cell union with exhaustiveness switches"
      pattern: "linked-loop"
  prohibitions:
    - statement: "MUST NOT materialize per-repetition rasters, cache entries, or frame lists — linked occurrences are virtual cells resolved by O(1) modulo (D-26, D-32)"
      status: resolved
      verification: test
    - statement: "MUST NOT make ghost/linked-loop cells key-selectable or draggable (D-23, D-11)"
      status: resolved
      verification: test
    - statement: "MUST NOT let a loop truncate itself — the boundary query excludes every entity the loop owns (D-24)"
      status: resolved
      verification: test
    - statement: "MUST NOT throw on unresolved loops in the read path — typed unresolved state via the projectionFailure idiom (D-31)"
      status: resolved
      verification: test
---

<objective>
Extend the physical resolver with the virtual `linked-loop` cell kind and the D-24 boundary algebra — the single canonical derivation of requested/effective ranges, next-clip priority, zero-effective survival, and re-expansion that the store, the dialog, and the capsule all consume.

Purpose: HOLD-05's resolution half. D-24 locks one shared boundary definition; D-26 locks the resolver as its only home; Pitfall 4 requires the dialog and capsule to consume this same derivation, never a local recomputation.
Output: projection cells gain `linked-loop`; an exported pure derivation produces the per-loop capsule model (canonicalStart, cycleLength, requestedEnd, effectiveEnd, boundary kind, truncated, partialCycle, unresolved) consumed by later plans.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-01-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Loop resolver spec — modulo, boundaries, priority, zero-effective, re-expansion</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (cell union at 193-201; projection seam at 2057-2085; projectionFailure idiom at 2087-2096)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (existing harness)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-06, D-08, D-14, D-24, D-25, D-26, D-30, D-32)
  </read_first>
  <behavior>
    - 5-frame cycle, repeat 5, canonicalStart 10: frame 10..34 map to sourceKeyIds[(f-10)%5] with repeatIndex floor((f-10)/5); frame 35 is outside the effective range
    - A real key at frame 22 (not owned by the loop) truncates effective end to 22; inserting it is what shortens the loop — removing it re-expands to 35
    - Loop B canonicalStart 20, loop A canonicalStart 10 repeat 5: A effective end = 20; B is NOT pushed after A; B resolves from its own canonical start
    - Real key exactly at canonicalStart 10 (not owned) yields Effective = 0f and the loop record survives
    - Infinity loop: requested end tracks parentEndExclusive dynamically; effective end capped at min(parent end, 600)
    - Boundary candidates exclude the loop's own source keyIds, its own virtual occurrences, its own canonical start, and other layers' content
    - Truncation at frame 22 (22-10=12, 12%5!=0) reports partial cycle; truncation at frame 25 reports complete cycles
    - Loop with dangling sourceKeyIds yields a typed unresolved state, not a throw
    - Identical document resolved twice yields identical results
  </behavior>
  <action>
    Write the full RED spec app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts using the existing resolver test harness. Cover every behavior case above plus: single-frame cycle (cycleLength 1, repeat 1) resolves exactly one frame; repeat 1 equals one bare cycle with no repetition region; a loop whose requested end is below every boundary is untruncated. Import the new symbols (linked-loop cell kind, derivation export) from the resolver module — the spec must fail at this point. Run and confirm RED. Commit test(43-02).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopResolver 2>&1 | grep -E "FAIL|failed" </automated>
  </verify>
  <acceptance_criteria>
    - The new spec file exists and fails (RED) before Task 2
    - The spec contains at least one case per behavior bullet, including the D-24 self-exclusion case and the D-14 not-pushed case
  </acceptance_criteria>
  <done>Failing spec committed; every locked boundary/priority/zero-effective semantic has an explicit failing assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): linked-loop cell kind + shared boundary/capsule derivation in the resolver</name>
  <reversibility rating="costly">D-24/D-26: the boundary algebra and the resolver extension are the shared contract between resolver, ops interactions, and capsule; changing them later re-opens all three. Locked by the user — flag only, no checkpoint.</reversibility>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (cell union 193-201; intent union 93-141; buildProjectionFromMapping path 2057-2085; projectionFailure 2087-2096)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoLoopClip from 43-01)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pattern 1, Code Examples: modulo resolution and effective-range derivation sketches)
  </read_first>
  <behavior>
    - All Task 1 spec cases pass
    - projectPhysicPaintRotoPhysicalTimeline assigns linked-loop cells only where no real cell exists (real keys always win)
    - A pure exported derivation (name discretion, e.g. derivePhysicPaintRotoLoopRanges) returns per-loop: canonicalStart, cycleLength, repeat, requestedEnd (finite) or infinity, effectiveEnd, boundary { kind: 'real-key' | 'loop-start' | 'parent-end', frame }, truncated, partialCycle, unresolved flag with missing keyId list — consumed later by the dialog readout (Pitfall 4) and the capsule projection (43-07)
    - Infinity effective end = min(parentEndExclusive, PHYSIC_PAINT_MAX_APPLY_FRAMES 600)
    - No frame lists are materialized for infinity; per-frame resolution is O(1)
  </behavior>
  <action>
    Implement in physicsPaintRotoPhysicalResolver.ts: (1) extend PhysicPaintRotoPhysicalCell with { kind: 'linked-loop'; appFrame; loopId; sourceKeyId; repeatIndex } — virtual, never persisted. (2) In the projection path, after real cells are assigned from the validated identity mapping, assign linked-loop cells for frames inside each resolvable loop's effective range where no real cell exists; frames at or beyond effectiveEnd get no virtual cell. (3) Implement the D-24 boundary query scoped to the same parent Paint authority: candidates are real keys not in loop.sourceKeyIds with appFrame > canonicalStart, other loops' canonicalStarts greater than this loop's, and parentEndExclusive; exclude the loop's own source keyIds, its own virtual occurrences, its own canonical start, caches/previews/interpolated render-only frames, and other layers. (4) Export the pure derivation described in behavior, computing effectiveEnd = min(requestedEnd, nextBoundary, parentEndExclusive) with half-open intervals, partialCycle via modulo, and unresolved loops surfaced through the projectionFailure idiom (frozen { ok: false, failure }) — never thrown in the read path. (5) Keep everything O(1) per frame and O(keys + loops in range) per re-derivation; no caps beyond the existing 600 capacity. Run the spec until GREEN; commit feat(43-02).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopResolver physicsPaintRotoPhysicalResolver</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoLoopResolver exits 0
    - pnpm --dir app exec vitest run physicsPaintRotoPhysicalResolver exits 0 (existing resolver behavior unchanged)
    - grep -c "linked-loop" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts returns >= 3
    - The derivation export exists and is pure (no store or signal imports in its implementation)
  </acceptance_criteria>
  <done>The resolver resolves linked loops per the locked D-14/D-24/D-25 algebra; the shared derivation export exists for dialog and capsule consumers; spec green; existing resolver specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pitfall-7 exhaustiveness sweep — linked-loop excluded from selection and drag</name>
  <files>app/src/components/physic-paint/roto/rotoTimelineSelectors.ts, app/src/components/physic-paint/roto/rotoPhysicalTimelinePorts.ts, app/src/components/physic-paint/roto/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/hooks/useRotoTimelineModel.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoTimelineSelectors.ts
    - app/src/components/physic-paint/roto/rotoPhysicalTimelinePorts.ts
    - app/src/components/physic-paint/roto/physicsPaintWorkflowPresentation.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (cell derivation block 1297-1343; dragEligible at 1315)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 7)
  </read_first>
  <behavior>
    - Every cell-kind consumer handles 'linked-loop' explicitly; an unhandled kind is a compile-time error via never-fallback exhaustiveness switches
    - Selection ports never return a key selection for a linked-loop cell
    - Drag eligibility (dragEligible in PhysicsPaintWorkflowStrip.tsx line 1315 pattern) stays real-key-only; linked-loop cells are not draggable
    - The Studio strip still renders linked repetition cells with their existing cell-state semantics (the additive badge lands in 43-08)
  </behavior>
  <action>
    Audit every cell.kind === / cell.kind !== site in the five listed files. Where a switch or chain handles cell kinds, add the linked-loop arm with a never-fallback exhaustiveness check so a future kind is a compile error. Selection and drag ports explicitly exclude linked-loop: a virtual cell produces no key selection, no drag start, no Force Spacing eligibility. Keep linked-loop cells visible to read-only consumers (strip cell-state derivation, workflow presentation) using their existing empty/cached/generated semantics — no new first-class cell state (D-18). Add or extend colocated specs asserting: clicking/selecting a linked-loop frame yields no key selection; drag eligibility is false for linked-loop cells; presentation derivation treats linked-loop cells with existing fill classes only. Commit feat(43-02).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run rotoTimelineSelectors rotoPhysicalTimelinePorts physicsPaintWorkflowPresentation useRotoTimelineModel PhysicsPaintWorkflowStrip</automated>
  </verify>
  <acceptance_criteria>
    - The listed consumer specs exit 0
    - pnpm --dir app exec tsc --noEmit (or the repo's typecheck script) exits 0 with the exhaustiveness switches in place
    - grep -rn "linked-loop" across the five consumer files shows explicit handling in each
  </acceptance_criteria>
  <done>No consumer can treat a ghost cell as a key; type-level exhaustiveness guards every cell-kind switch; Studio strip visuals unchanged pending 43-08's additive badge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted document → projection | Loop records parsed from disk drive the derivation; malformed structure already threw at parse (43-01) |
| projection → all read surfaces | Derived ranges feed store, dialog, capsule, export; a poisoned derivation poisons every surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-02-01 | DoS | infinity loop resolution | medium | mitigate | O(1) modulo per frame; effective end capped at min(parent end, 600); no materialized frame lists (D-32) — asserted by spec cases |
| T-43-02-02 | Tampering | boundary query self-exclusion | medium | mitigate | D-24 exclusion set enforced in the derivation and pinned by spec: own keyIds, own occurrences, own start never truncate the loop |
| T-43-02-03 | DoS | re-derivation cost on boundary move | low | mitigate | O(keys + loops in range) re-resolution, no cache rebuild (D-32) |
| T-43-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicsPaintRotoLoopResolver physicsPaintRotoPhysicalResolver — exit 0
- Full suite pnpm --dir app exec vitest run — exit 0
- Typecheck green with exhaustiveness switches
</verification>

<success_criteria>
- Modulo resolution, half-open boundaries, three boundary kinds, loop-loop priority, zero-effective survival, and re-expansion all proven by the spec
- One pure derivation export is the single source of requested/effective/truncation truth for dialog, capsule, and export
- No per-repetition rasters or frame lists anywhere
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `PhysicPaintRotoPhysicalCell` linked-loop variant: `{ kind: 'linked-loop'; appFrame; loopId; sourceKeyId; repeatIndex }` (physicsPaintRotoPhysicalResolver.ts)
- `derivePhysicPaintRotoLoopRanges` (pure derivation export, physicsPaintRotoPhysicalResolver.ts — final name is executor discretion within the documented return shape)
- Loop boundary query (D-24) inside the resolver derivation
- `physicsPaintRotoLoopResolver.test.ts` (new spec)
- Exhaustiveness `never`-fallback switches in the five cell-kind consumers
