---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts
  - app/src/components/physic-paint/roto/rotoTimelineSelectors.ts
  - app/src/components/physic-paint/roto/rotoPhysicalTimelinePorts.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: true
requirements:
  - HOLD-05

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Loop-range derivation produces ONE compact interval record per Loop Clip — loopId, placementStart, cycleLength, ordered sourceKeyIds, requestedEnd (finite or Infinity), effectiveEnd, boundary kind/frame, truncation state, partial-cycle state, unresolved metadata — with complexity scaling with key/loop count, never repetition duration (D-32, audit finding 2)"
    - "Per-frame resolution is a lazy canonical query: real key first, then the applicable effective Loop Clip interval, then sourceIndex = (frame - placementStart) % cycleLength and repeatInstance = floor((frame - placementStart) / cycleLength); the modulo step is O(1) after interval lookup, and NO physical projection entry is populated per virtual frame (D-26, D-32)"
    - "Interval lookup uses the closest existing sorted/indexed physical-document convention; its stated complexity matches the actual structure (O(log loops) or O(loops) — never claimed O(1) unless the structure proves it)"
    - "A very large finite Repeat and an Infinity loop create no duration-proportional collections — one interval record per loop, no frame lists, no per-repetition entries (D-32; proven by spec)"
    - "The per-frame query returns ONE typed contract shared by resolver, store, preview, timeline, Studio, and export: 'real' | 'linked' | 'linked-unresolved' | 'empty' (audit finding 3)"
    - "A 'linked-unresolved' result carries loopId, appFrame, placementStart, the effective interval, ordered sourceKeyIds, and missingSourceKeyIds — enough for the capsule error state, destination-frame placeholder, missing-source tooltip, export preflight, and repair/relink actions (D-31)"
    - "An unresolved loop never aborts unrelated physical projection and never turns unrelated frames blank; loop-range derivation survives unresolved source references and keeps the interval record with its missingSourceKeyIds (D-31)"
    - "Real keys always win over virtual linked-loop resolution — materialize-local-key (D-12) and shrink (D-06) are emergent, not special-cased"
    - "Effective end = min(requested end, next boundary, parentEndExclusive) with half-open [start, end) intervals; valid boundaries are exactly a non-owned real key, another loop's placementStart, or parentEndExclusive; a loop never truncates itself (D-24)"
    - "Loop B (later placement start) begins at its placement start and is NOT pushed after loop A; A's effective end = min(A requested end, B start, parent end); same-start collisions compare placementStart and are never resolved by hidden creation-order priority (D-14)"
    - "A boundary landing exactly at the loop's placementStart yields Effective = 0f and the loop object survives (D-08)"
    - "Moving or removing blocking content or a later loop re-expands the earlier loop's effective range automatically with no regeneration (D-08, D-14, D-25)"
    - "Infinity effective range is bounded by min(parent end, PHYSIC_PAINT_MAX_APPLY_FRAMES = 600) and never materializes a frame list (D-25, D-32; Q4 resolved: capacity-bounded)"
    - "A truncation landing mid-cycle is distinguishable from one landing exactly on a cycle boundary: (effectiveEnd - placementStart) % cycleLength !== 0 means partial (D-21)"
    - "Resolving the same document twice yields identical effective ranges — derivation is pure (D-30)"
    - "Virtual linked occurrences are excluded from key selection and drag eligibility in every cell-kind consumer (D-23, D-11)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "loop-range interval derivation, lazy per-frame resolution query, typed resolution union ('real' | 'linked' | 'linked-unresolved' | 'empty'), loop boundary derivation"
      contains: "linked-unresolved"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts"
      provides: "RED spec for lazy modulo resolution, interval derivation, boundary algebra, loop-loop priority, zero-effective, re-expansion, typed unresolved contract, no-duration-proportional-collections"
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      via: "derivation reads loopClips from the parsed physical document"
      pattern: "loopClips"
    - from: "app/src/components/physic-paint/roto/rotoTimelineSelectors.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      via: "selectors consume the typed resolution union with exhaustiveness switches"
      pattern: "linked-unresolved"
  prohibitions:
    - statement: "MUST NOT materialize per-repetition rasters, cache entries, projection entries, or frame lists — linked occurrences resolve lazily per requested frame or visible range (D-26, D-32, audit finding 2)"
      status: resolved
      verification: test
    - statement: "MUST NOT assign one linked-loop cell for every frame inside every effective range — duration-sized virtual-cell materialization is forbidden for finite AND infinite loops (audit finding 2)"
      status: resolved
      verification: test
    - statement: "MUST NOT make virtual linked occurrences key-selectable or draggable (D-23, D-11)"
      status: resolved
      verification: test
    - statement: "MUST NOT let a loop truncate itself — the boundary query excludes its own placementStart, its own virtual occurrences, and its referenced source key ids (D-24)"
      status: resolved
      verification: test
    - statement: "MUST NOT throw on unresolved loops in the read path and MUST NOT use a global projectionFailure that blanks unrelated frames — the typed 'linked-unresolved' result is per-frame and per-loop (D-31, audit finding 3)"
      status: resolved
      verification: test
    - statement: "MUST NOT overload placementStart with the source keys' location — placement and source identity are separate (placement/source correction)"
      status: resolved
      verification: test
---

<objective>
Extend the physical resolver with the two canonical loop layers — (a) a compact per-loop interval derivation and (b) a lazy per-frame resolution query returning ONE typed contract (`real` / `linked` / `linked-unresolved` / `empty`) — plus the D-24 boundary algebra, next-clip priority, zero-effective survival, and re-expansion that the store, the dialog, the capsule, and export all consume.

Purpose: HOLD-05's resolution half. D-24 locks one shared boundary definition; D-26 locks the resolver as its only home; D-32 + audit finding 2 forbid duration-sized virtual materialization; audit finding 3 locks the single typed unresolved contract; Pitfall 4 requires every consumer to use this same derivation, never a local recomputation.
Output: an exported pure interval derivation (one record per Loop Clip) and an exported lazy per-frame resolution query; no virtual occurrence is ever written into a projection map, durable cache, or any duration-proportional collection.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-01-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Loop resolver spec — lazy interval derivation, per-frame query, typed contract, boundaries, priority, zero-effective, re-expansion</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (cell union at 193-201; projection seam at 2057-2085; projectionFailure idiom at 2087-2096 — loops must NOT reuse the global projectionFailure for unresolved state; the typed per-frame contract replaces it for loops)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (existing harness)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-06, D-08, D-14, D-24, D-25, D-26, D-30, D-32 — including the placement/source correction)
  </read_first>
  <behavior>
    - 5-frame cycle, repeat 5, placementStart 10: the per-frame query maps frames 10..34 to sourceKeyIds[(f-10)%5] with repeatInstance floor((f-10)/5); frame 35 resolves 'empty' (outside the effective range)
    - A duplicated loop (placementStart 40, sourceKeyIds of a cycle whose real keys live at 10..14): frame 40 resolves linked to sourceKeyIds[0] with repeatInstance 0; the loop's source location stays derived from the key records, never from placementStart
    - A real key at frame 22 (not owned by the loop) truncates effective end to 22; inserting it is what shortens the loop — removing it re-expands to 35
    - Loop B placementStart 20, loop A placementStart 10 repeat 5: A effective end = 20; B is NOT pushed after A; B resolves from its own placementStart
    - Real key exactly at placementStart 10 (not owned) yields Effective = 0f and the loop record survives
    - Infinity loop: requested end tracks parentEndExclusive dynamically; effective end capped at min(parent end, 600)
    - Boundary candidates exclude the loop's own placementStart, its own virtual occurrences, its referenced source keyIds, and other Paint authorities' content
    - Truncation at frame 22 (22-10=12, 12%5!=0) reports partial cycle; truncation at frame 25 reports complete cycles
    - Laziness: a loop with repeat 100000 (within the safe-product bound) and an Infinity loop each produce exactly ONE interval record from the derivation; the spec asserts no array, map, or projection structure whose size scales with repeat count is created (e.g. by spying on collection sizes / asserting the derivation output length equals the loop count, and asserting distant-frame queries like frame 499995 resolve correctly without materializing intermediate frames)
    - Typed contract: a resolvable linked frame returns { kind: 'linked', loopId, sourceKeyId, sourceIndex, repeatInstance }; a frame inside an unresolved loop's effective range returns { kind: 'linked-unresolved', loopId, appFrame, placementStart, missingSourceKeyIds } — never a throw, and unrelated real/generated frames still resolve normally (no global failure state)
    - Identical document resolved twice yields identical interval records and per-frame results
  </behavior>
  <action>
    Write the full RED spec app/src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts using the existing resolver test harness. Cover every behavior case above plus: single-frame cycle (cycleLength 1, repeat 1) resolves exactly one frame; repeat 1 equals one bare cycle with no repetition region; a loop whose requested end is below every boundary is untruncated; an unresolved loop whose missing keyIds are a strict subset of sourceKeyIds still reports the interval with the exact missing list. Import the new symbols (interval derivation export, per-frame resolution query, typed resolution union) from the resolver module — the spec must fail at this point. Run and confirm RED. Commit test(43-02).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopResolver 2>&1 | grep -E "FAIL|failed" </automated>
  </verify>
  <acceptance_criteria>
    - The new spec file exists and fails (RED) before Task 2
    - The spec contains at least one case per behavior bullet, including the D-24 self-exclusion case, the D-14 not-pushed case, the duplicated-loop placement-identity case, the no-duration-proportional-collections case (huge finite repeat + Infinity), and the typed 'linked-unresolved' contract case
  </acceptance_criteria>
  <done>Failing spec committed; every locked laziness/contract/boundary/priority/zero-effective semantic has an explicit failing assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): two canonical layers — compact interval derivation + lazy per-frame typed resolution</name>
  <reversibility rating="costly">D-24/D-26: the boundary algebra and the resolver extension are the shared contract between resolver, ops interactions, capsule, store, preview, and export; changing them later re-opens every consumer. Locked by the user — flag only, no checkpoint.</reversibility>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (cell union 193-201; intent union 93-141; buildProjectionFromMapping path 2057-2085 — linked occurrences must NOT join this per-frame projection map)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoLoopClip from 43-01 — placementStart + sourceKeyIds)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pattern 1, Code Examples: modulo resolution and effective-range derivation sketches)
  </read_first>
  <behavior>
    - All Task 1 spec cases pass
    - The interval derivation (name discretion, e.g. derivePhysicPaintRotoLoopRanges) returns ONE record per Loop Clip: { loopId, placementStart, cycleLength, sourceKeyIds, repeat, requestedEnd (finite) | infinity, effectiveEnd, boundary { kind: 'real-key' | 'loop-start' | 'parent-end', frame }, truncated, partialCycle, unresolved: { missingSourceKeyIds } | null } — consumed later by the dialog readout (Pitfall 4), the capsule projection (43-07), and export preflight (43-09)
    - The derivation survives unresolved source references: an unresolved loop keeps its full interval record with the missing keyId list; it is never dropped and never poisons other loops' records
    - The per-frame query (name discretion, e.g. resolvePhysicPaintRotoLoopFrame(layerState, appFrame)) checks a real key first, then finds the applicable effective interval via the existing sorted/indexed physical-document convention, then computes sourceIndex/repeatInstance by modulo and returns the typed result
    - Infinity effective end = min(parentEndExclusive, PHYSIC_PAINT_MAX_APPLY_FRAMES 600); no frame lists materialized; no per-frame projection entries for virtual occurrences
  </behavior>
  <action>
    Implement in physicsPaintRotoPhysicalResolver.ts: (1) Export the typed resolution union (name discretion, e.g. PhysicPaintRotoFrameResolution): { kind: 'real'; keyId; appFrame } | { kind: 'linked'; loopId; appFrame; sourceKeyId; sourceIndex; repeatInstance } | { kind: 'linked-unresolved'; loopId; appFrame; placementStart; sourceKeyIds; missingSourceKeyIds } | { kind: 'empty' } — virtual, never persisted; this is the SINGLE contract 43-03 (store), 43-07 (capsule), 43-08 (tooltip), and 43-09 (preview/export) all consume. (2) Implement the interval derivation exactly as specified in behavior: one compact record per Loop Clip; complexity O(keys + loops) per derivation, independent of repeat count; unresolved loops keep their record with missingSourceKeyIds. (3) Implement the D-24 boundary query scoped to the same parent Paint authority: candidates are real keys not in loop.sourceKeyIds with appFrame > placementStart, other loops' placementStarts greater than this loop's, and parentEndExclusive; exclude the loop's own placementStart, its own virtual occurrences, its referenced source keyIds, caches/previews/interpolated render-only frames, and other Paint authorities. (4) Implement the lazy per-frame query: real-key check first (real always wins), interval lookup via the closest existing sorted/indexed physical-document convention (state the actual lookup complexity in a code comment — O(log loops) if binary search over sorted interval starts, O(loops) if a linear scan is the existing convention; do NOT claim O(1) lookup), then O(1) modulo for sourceIndex = (frame - placementStart) % cycleLength and repeatInstance = floor((frame - placementStart) / cycleLength); frames at or beyond effectiveEnd return 'empty'; a frame inside an unresolved loop's effective range returns 'linked-unresolved' with the missing list. (5) Do NOT extend the eager projection cells map with virtual linked-loop entries — physical projection entries remain real/generated only; virtual occurrences exist only as query results for the requested frame or visible range. Keep everything O(1) per frame after lookup and O(keys + loops in range) per re-derivation; no caps beyond the existing 600 capacity. Run the spec until GREEN; commit feat(43-02).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopResolver physicsPaintRotoPhysicalResolver</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoLoopResolver exits 0
    - pnpm --dir app exec vitest run physicsPaintRotoPhysicalResolver exits 0 (existing resolver behavior unchanged)
    - grep -c "linked-unresolved" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts returns >= 2 (union declaration + query return)
    - The derivation export and the per-frame query export exist and are pure (no store or signal imports in their implementations)
    - The spec's laziness cases pass: derivation output length == loop count for repeat 100000 and Infinity; no duration-proportional collection is created
    - grep -n "O(" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts shows an honest complexity comment on the interval lookup matching the actual structure
  </acceptance_criteria>
  <done>The resolver provides the compact interval derivation and the lazy per-frame typed resolution per the locked D-14/D-24/D-25/D-32 algebra; no virtual occurrence is materialized anywhere; the single typed contract exists for all consumers; spec green; existing resolver specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pitfall-7 exhaustiveness sweep — virtual occurrences excluded from selection and drag</name>
  <files>app/src/components/physic-paint/roto/rotoTimelineSelectors.ts, app/src/components/physic-paint/roto/rotoPhysicalTimelinePorts.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/hooks/useRotoTimelineModel.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoTimelineSelectors.ts
    - app/src/components/physic-paint/roto/rotoPhysicalTimelinePorts.ts
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (cell derivation block 1297-1343; dragEligible at 1315)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 7)
  </read_first>
  <behavior>
    - Every consumer of the frame-resolution union handles 'linked' and 'linked-unresolved' explicitly; an unhandled kind is a compile-time error via never-fallback exhaustiveness switches
    - Selection ports never return a key selection for a linked or linked-unresolved frame
    - Drag eligibility (dragEligible in PhysicsPaintWorkflowStrip.tsx line 1315 pattern) stays real-key-only; linked frames are not draggable
    - The Studio strip derives linked repetition cells for its VISIBLE frame window only, via the lazy per-frame query (never a full-range scan); cells keep their existing empty/cached/generated cell-state semantics (the additive badge lands in 43-08)
    - A 'linked-unresolved' frame in the visible window renders with its existing cell-state fill — the strip remains non-blocking and never treats the frame as real Paint content (the additive error affordance is the capsule's, D-23)
  </behavior>
  <action>
    Audit every cell.kind === / cell.kind !== / resolution-kind site in the five listed files. Where a switch or chain handles resolution kinds, add the linked and linked-unresolved arms with a never-fallback exhaustiveness check so a future kind is a compile error. Selection and drag ports explicitly exclude linked results: a virtual occurrence produces no key selection, no drag start, no Force Spacing eligibility. The Studio strip's visible-cell derivation queries the lazy per-frame resolution for the visible window only (window bounds from the existing strip viewport logic) — complexity O(visible frames + loops in window), never O(effective range). Keep linked results visible to read-only consumers (strip cell-state derivation, workflow presentation) using their existing empty/cached/generated semantics — no new first-class cell state (D-18). Add or extend colocated specs asserting: selecting a linked frame yields no key selection; drag eligibility is false for linked frames; presentation derivation treats linked frames with existing fill classes only; the strip's derivation issues queries bounded to the visible window (assert via a query-count spy that a 100000-repeat loop does not produce range-proportional queries). Commit feat(43-02).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run rotoTimelineSelectors rotoPhysicalTimelinePorts physicsPaintWorkflowPresentation useRotoTimelineModel PhysicsPaintWorkflowStrip</automated>
  </verify>
  <acceptance_criteria>
    - The listed consumer specs exit 0
    - pnpm --dir app run typecheck exits 0 with the exhaustiveness switches in place
    - grep -rn "linked-unresolved" across the five consumer files shows explicit handling in each consumer that switches on the resolution union
    - The strip spec asserts visible-window-bounded querying (no range-proportional query count for a huge-repeat loop)
  </acceptance_criteria>
  <done>No consumer can treat a virtual occurrence as a key; type-level exhaustiveness guards every resolution-kind switch; Studio strip visuals unchanged pending 43-08's additive badge; visible-window querying proven bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted document → derivation | Loop records parsed from disk drive the derivation; malformed structure already threw at parse (43-01) |
| derivation/query → all read surfaces | Derived intervals and typed frame results feed store, dialog, capsule, export; a poisoned derivation poisons every surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-02-01 | DoS | infinity / huge-repeat loop resolution | medium | mitigate | Lazy per-frame query only — O(1) modulo after interval lookup; effective end capped at min(parent end, 600); NO materialized frame lists, projection entries, or duration-proportional collections (D-32, audit finding 2) — asserted by the spec's laziness cases |
| T-43-02-02 | Tampering | boundary query self-exclusion | medium | mitigate | D-24 exclusion set enforced in the derivation and pinned by spec: own placementStart, own occurrences, own referenced keyIds never truncate the loop |
| T-43-02-03 | DoS | re-derivation cost on boundary move | low | mitigate | O(keys + loops in range) re-resolution, no cache rebuild (D-32) |
| T-43-02-04 | Tampering | global failure blanking unrelated frames | medium | mitigate | Typed per-frame 'linked-unresolved' result replaces any global projectionFailure for loops; spec asserts unrelated frames resolve normally beside an unresolved loop (audit finding 3) |
| T-43-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicsPaintRotoLoopResolver physicsPaintRotoPhysicalResolver — exit 0
- Full suite pnpm --dir app exec vitest run — exit 0
- pnpm --dir app run typecheck — exit 0 with exhaustiveness switches
</verification>

<success_criteria>
- Compact interval derivation (one record per loop) + lazy per-frame typed resolution proven by the spec, including huge-repeat and Infinity laziness cases
- ONE typed contract (real / linked / linked-unresolved / empty) is the single source of frame truth for store, dialog, capsule, preview, and export
- Modulo resolution, half-open boundaries, three boundary kinds, loop-loop priority, zero-effective survival, and re-expansion all proven
- No per-repetition rasters, frame lists, projection entries, or duration-proportional collections anywhere
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `PhysicPaintRotoFrameResolution` typed union: `{ kind: 'real' } | { kind: 'linked'; loopId; sourceKeyId; sourceIndex; repeatInstance } | { kind: 'linked-unresolved'; loopId; placementStart; sourceKeyIds; missingSourceKeyIds } | { kind: 'empty' }` (physicsPaintRotoPhysicalResolver.ts — final name is executor discretion within the documented shape; THE single contract for all consumers)
- Loop-range interval derivation export, e.g. `derivePhysicPaintRotoLoopRanges` — one compact interval record per Loop Clip (physicsPaintRotoPhysicalResolver.ts)
- Lazy per-frame resolution query export, e.g. `resolvePhysicPaintRotoLoopFrame` (physicsPaintRotoPhysicalResolver.ts)
- Loop boundary query (D-24) inside the resolver derivation
- `physicsPaintRotoLoopResolver.test.ts` (new spec)
- Exhaustiveness `never`-fallback switches in the five resolution/cell-kind consumers
