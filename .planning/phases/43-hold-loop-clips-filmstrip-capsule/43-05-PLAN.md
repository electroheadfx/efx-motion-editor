---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 05
type: execute
wave: 4
depends_on:
  - 43-01
  - 43-02
  - 43-03
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
autonomous: true
requirements:
  - HOLD-05

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Deleting a source-cycle key while any loop references the cycle is rejected with: `This key belongs to a source cycle used by {N} linked loop(s). Unlink the loop(s) before deleting it.` (D-07)"
    - "Single-key drag (ripple) on a linked source key and Force Spacing on selections containing linked source keys are rejected with: `Linked source-cycle keys move only as a rigid group. Select the whole cycle to drag it.` (D-11)"
    - "Rigid group drag (Phase 37) on the whole source cycle moves the keys; a loop whose placementStart coincides with the cycle's first key frame (original loop) follows the drag, while a duplicated loop keeps its own placementStart and keeps resolving the moved source keys by id — loops have no independent drag (D-04, placement/source correction)"
    - "Delete-key on a linked repetition frame is rejected verbatim: `No real key exists at this linked frame. Use Clear to create an empty real key, or select the Loop Clip capsule to delete the loop.` — it never touches the modulo-resolved source key and never unlinks (D-13)"
    - "Clear on a linked repetition frame materializes a local empty real key; the loop shortens; one Undo removes the key and the loop re-expands; one Redo restores the empty key and the derived shrink (D-13)"
    - "Painting or erasing on a linked repetition frame materializes a new local real key whose base is the loop-resolved pixels plus the new stroke; canvas and playhead stay at the current frame; one Undo removes the key and re-expands the loop; one Redo restores the key and the derived shrink (D-12)"
    - "Before a batch generation confirms, preflight reports: `This operation will shorten {N} linked loop(s), starting at frame {F}.` — and the commit plus derived shrink remain one undoable outcome (D-06)"
    - "Copy/paste never carries loop identity — pasted source-cycle keys land as ordinary real keys (D-09)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "loop-aware intent guards on delete-key, move-key, force-spacing, paste intents"
      contains: "linked"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts"
      provides: "RED spec for D-07/D-11/D-13 guard rejections with locked copy"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      provides: "D-06 preflight shorten warning computed from the canonical derivation"
      contains: "will shorten"
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      via: "intent guards consult loopClips source keyId membership before applying delete/move/force-spacing"
      pattern: "sourceKeyIds"
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      via: "preflight calls the shared derivation to compute which loops the destination range truncates"
      pattern: "derivePhysicPaintRotoLoop"
  prohibitions:
    - statement: "MUST NOT delete or modify modulo-resolved source keys through any linked-frame operation (D-07, D-13)"
      status: resolved
      verification: test
    - statement: "MUST NOT reject a guarded operation silently — every rejection carries the locked reason copy (D-07, D-11, D-13)"
      status: resolved
      verification: test
    - statement: "MUST NOT propagate loop identity through copy/paste (D-09)"
      status: resolved
      verification: test
    - statement: "MUST NOT leave a generation commit and its derived loop shrink as separately undoable steps (D-06)"
      status: resolved
      verification: test
---

<objective>
Land the loop-aware operation guards (D-07 source-key deletion, D-11 rigid linked keys, D-13 Clear vs Delete-key, D-12 materialize-on-paint, D-09 paste identity) and the D-06 preflight shorten warning on the Play Script confirm path.

Purpose: these guards are what make loops safe to combine with every existing Roto operation; they follow the approved Cut-tool fail-closed precedent with locked user-facing copy.
Output: guarded rejections with exact locked messages, materialization semantics, and preflight reporting — all proven by spec.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-02-SUMMARY.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-03-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loop-aware intent guards in the resolver (D-07, D-11, D-13, D-09) + Clear/paint materialization (D-12, D-13)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (intent union 93-141; fail idiom 2014-2035; delete-key/move-key/force-spacing/paste-key handlers)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PhysicPaintRotoLoopClip from 43-01)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-04, D-07, D-09, D-11, D-12, D-13)
  </read_first>
  <behavior>
    - delete-key / delete-key-group on any keyId in a referenced loop's sourceKeyIds → typed failure with `This key belongs to a source cycle used by {N} linked loop(s). Unlink the loop(s) before deleting it.` where N counts loops referencing that cycle
    - move-key (single, ripple semantics) on a linked source key → typed failure with `Linked source-cycle keys move only as a rigid group. Select the whole cycle to drag it.`
    - force-spacing on a selection containing any linked source key → same typed failure
    - move-key-group covering the whole source cycle (rigid group drag) succeeds: a loop whose placementStart equals the cycle's pre-move first key frame (original loop) has its placementStart updated to the moved first key's frame; a duplicated loop (placementStart elsewhere) keeps its placementStart unchanged and continues resolving the same sourceKeyIds
    - delete-key targeting a frame whose per-frame resolution is 'linked' or 'linked-unresolved' (no local real key) → typed failure with the verbatim D-13 message; Clear on that frame creates an empty real key and the loop's effective range shortens to that frame
    - A new real key materialized at a linked frame carries the loop-resolved source payload as its base (D-12, sourced through the 43-03 canonical seam); after commit, that frame resolves 'real' and the loop shortens
    - Clear-materialization and paint-materialization are each one history command: Undo removes the materialized key and the loop re-expands; Redo restores the key and the derived shrink
    - paste-key / paste-key-group of copied source-cycle keys create ordinary real keys unreferenced by any loop
    - A materialized empty key can later be deleted normally, re-expanding the loop (D-13)
  </behavior>
  <action>
    RED first: create app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts covering every behavior case with the exact locked copy strings; confirm RED; commit test(43-05). GREEN in physicsPaintRotoPhysicalResolver.ts: guard the delete-key, delete-key-group, move-key, and force-spacing intent handlers by consulting loopClips source keyId membership — reject via the existing fail(code, operationKind, text) typed-failure idiom (lines 2014-2035) with the exact locked messages; move-key-group that includes all of a cycle's keys moves rigidly and updates placementStart ONLY for loops whose placementStart equaled the cycle's pre-move first key frame (original loops follow; duplicated loops keep their destination placement — single position truth per D-04 plus the placement/source correction). Add the linked-frame delete-key rejection (per-frame resolution is 'linked'/'linked-unresolved', no local real key) with the verbatim D-13 text. Implement materialization support: the intent/path that creates a real key at a linked frame accepts the loop-resolved source payload as the new key's base (used by Clear-as-empty-key and by paint/erase commits per D-12), sourced through the 43-03 canonical store seam; after the commit the frame resolves 'real' and the loop's derived range shortens automatically (43-02 derivation). Materialization ops are one history command each — assert Undo re-expands AND Redo re-applies the shrink. Paste intents never reference loop identity (D-09) — assert by construction and spec. Keep every guard fail-closed with a reason; no silent no-ops. Commit feat(43-05).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopGuards physicsPaintRotoLoopResolver physicsPaintRotoPhysicalResolver</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoLoopGuards exits 0
    - Existing resolver and loop-resolver specs still exit 0
    - The spec asserts the three locked rejection strings verbatim (D-07, D-11, D-13)
    - The rigid-group case asserts an original loop's placementStart follows the moved first key AND a duplicated loop's placementStart is unchanged by the same drag
    - The Clear and paint materialization cases assert Undo re-expansion AND Redo re-application of the derived shrink
  </acceptance_criteria>
  <done>All linked-key guards reject fail-closed with locked copy; rigid group drag moves original loops while duplicated loops keep their placement; Clear/paint materialization shortens loops atomically with Undo AND Redo coherence; paste never carries loop identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-06 preflight shorten warning on the Play Script confirm path</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (confirm() 220-438; loopReadout 144-158; parseRepeat 340-353)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (shared derivation export from 43-02)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-06)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S6 preflight, Copywriting Contract)
  </read_first>
  <behavior>
    - When the pending generation's destination range intersects an existing loop's effective range, confirm() surfaces the preflight line `This operation will shorten {N} linked loop(s), starting at frame {F}.` with N = affected loop count and F = the earliest truncation frame, before the commit proceeds
    - When no loop is affected, no preflight line appears
    - The accepted commit and the derived loop shrink are one history command — one Undo removes the generated keys and the loop re-expands, one Redo restores the generated keys and the derived shrink (covered jointly with the 43-01 history spec)
    - The preflight computation uses the 43-02 shared derivation, never a controller-local boundary recomputation (Pitfall 4)
  </behavior>
  <action>
    RED: extend physicsPaintRotoPlayScriptController.test.ts with the preflight cases (affected loop present/absent, N and F values, one-undo AND one-redo coherence assertions alongside the 43-01 snapshot contract); commit test(43-05). GREEN: in physicsPaintRotoPlayScriptController.ts, compute the shorten preflight inside confirm() by calling the 43-02 shared derivation with the pending destination range — collect loops whose effective range the destination intersects, derive N and the earliest truncation frame F, and expose the locked preflight line on a controller signal the dialog renders before final confirmation. Do not recompute boundaries locally; do not change the staged-commit flow — the preflight is advisory text on the existing confirm path, and the commit-plus-shrink stays one snapshot (43-01). Commit feat(43-05).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoPlayScriptController physicsPaintRotoLoopHistory</automated>
  </verify>
  <acceptance_criteria>
    - The preflight spec cases pass, including the exact string `This operation will shorten {N} linked loop(s), starting at frame {F}.` with substituted values
    - pnpm --dir app exec vitest run physicsPaintRotoLoopHistory exits 0 (one-undo coherence intact)
    - grep -n "derivePhysicPaintRotoLoop" app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts shows the shared derivation in use (no local boundary math added)
  </acceptance_criteria>
  <done>Preflight warns with the locked line before any loop-shortening generation; commit + shrink remain one undoable outcome; the dialog and resolver share one boundary derivation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → resolver intent | Delete/drag/paste intents may target linked keys; guards are the enforcement point |
| controller → resolver derivation | Preflight trusts the canonical derivation, never local math |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-05-01 | Tampering | guarded intent handlers | medium | mitigate | Fail-closed typed rejections via the existing fail() idiom; spec pins the exact locked copy for D-07/D-11/D-13 |
| T-43-05-02 | Tampering | materialize-local-key base payload | medium | mitigate | Base comes only from the canonical loop-resolved source record (43-03 seam); never from user-supplied payloads |
| T-43-05-03 | DoS | preflight derivation on large ranges | low | mitigate | Derivation is O(keys + loops in range) per D-32; no frame-list materialization |
| T-43-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicsPaintRotoLoopGuards physicsPaintRotoPlayScriptController physicsPaintRotoLoopHistory — exit 0
- Full suite green
</verification>

<success_criteria>
- Every locked guard (D-07/D-11/D-13) rejects with its exact locked copy
- D-12 materialization and D-13 Clear produce real keys that shorten loops atomically and re-expand on Undo
- D-06 preflight reports the locked line before confirm
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- Loop-aware guards on delete-key / delete-key-group / move-key / force-spacing / paste intents (physicsPaintRotoPhysicalResolver.ts)
- Linked-frame delete-key rejection + Clear/paint materialize-local-key base-payload support (physicsPaintRotoPhysicalResolver.ts)
- Preflight shorten signal + locked warning line on the Play Script controller (physicsPaintRotoPlayScriptController.ts)
- `physicsPaintRotoLoopGuards.test.ts` (new spec)
