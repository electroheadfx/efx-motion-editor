---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 14
type: execute
wave: 11
depends_on:
  - 43-13
files_modified:
  - app/src/components/timeline/TimelineCapsuleTooltip.tsx
  - app/src/components/timeline/TimelineCapsuleTooltip.test.ts
  - app/src/components/timeline/loopCapsuleGeometry.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/lib/physicPaintLoopOperationBridge.test.ts
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-33R/D-36R: now-unmounted rich Motion Editor tooltip/geometry and rejected EFX lane/full-filmstrip code, tests, styles, and listeners are deleted rather than hidden, while the passive marker type/projection/renderer/tests remain protected."
    - "D-34R/D-35R: the integrated 3px rail remains the only EFX presentation, with a 12px target, 161px strip, 38px physical row, and no extra layout allocation."
    - "D-42: physical-cell geometry, selection, multi-selection, drag, toolbar, and blue linked indicators remain regression-green."
    - "D-39R/D-40R/D-46: Studio mounts no specialized main-to-child Loop Clip listener; rail, popover, and sidebar continue through local controller ports."
    - "D-43 through D-45 contextual sidebar and D-01/D-31 carried modal behavior remain green from Plan 43-12 without a new inspector or mutation path."
    - "D-41 remains deferred: cleanup introduces no placement drag or keyboard movement."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "integrated rail rules with rejected lane/full-filmstrip selectors removed"
    - path: "app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts"
      provides: "generic parent bridge hooks without specialized Loop Clip listeners"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "local Loop Clip action ownership without specialized listener mounts"
  key_links:
    - from: "PhysicsPaintWorkflowStrip"
      to: "PhysicsPaintLoopClipRail"
      via: "one integrated host inside the existing physical row"
      pattern: "PhysicsPaintLoopClipRail"
    - from: "rail/popover/sidebar"
      to: "rotoPlayScript controller"
      via: "direct local ports established in 43-11/43-12"
      pattern: "openLoopEdit|duplicateLinkedLoop|unlinkLoop|repairLoop|relinkLoop"
  prohibitions:
    - statement: "Rejected rich tooltip, geometry, lane, capsule, band, badge, tests, styles, listeners, and specialized child-listener residue must not remain behind dead exports or hidden CSS; the new passive marker type/projection/renderer/tests must not be deleted or broadened."
      status: resolved
      verification: automated
    - statement: "Generic project context, authority/apply, save, launch/focus, and frame-sync hooks must remain mounted."
      status: resolved
      verification: automated
    - statement: "No package, schema, test-configuration, naming-field, or placement-drag change is permitted."
      status: resolved
      verification: automated
---

<objective>
Delete the specialized rich EFX/timeline residue left after the active ownership cutover and remove specialized child listener mounts while preserving generic bridge hooks, the approved integrated rail/sidebar behavior, and the passive Motion Editor marker path per D-33R through D-46.

Purpose: Make the corrected ownership boundary structural before public transport cleanup and final evidence in 43-15 without deleting the visibility-only marker.
Output: Deleted stale rich tooltip/geometry/lane/style/test/listener residue plus Studio-local Loop Clip actions with no specialized child listeners; passive marker type/projection/renderer/tests retained.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md
@app/src/components/timeline/TimelineCapsuleTooltip.tsx
@app/src/components/timeline/loopCapsuleGeometry.ts
@app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx
@app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete now-unmounted Motion Editor tooltip and rich capsule geometry residue</name>
  <files>app/src/components/timeline/TimelineCapsuleTooltip.tsx, app/src/components/timeline/TimelineCapsuleTooltip.test.ts, app/src/components/timeline/loopCapsuleGeometry.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33R structural zero-surface boundary)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md (proof that active mounts/callers are gone)
    - app/src/components/timeline/TimelineCapsuleTooltip.test.ts (stale positive surface contract)
  </read_first>
  <behavior>
    - Sentinel test: `has no Motion Editor Loop Clip tooltip module after ownership removal`.
    - The sentinel is an absence contract proving no generic timeline source imports or mounts the obsolete tooltip/geometry namespace.
  </behavior>
  <action>RED: replace the stale positive tooltip test with exactly one test named `has no Motion Editor Loop Clip tooltip module after ownership removal` in `TimelineCapsuleTooltip.test.ts`. Run `pnpm --dir app exec vitest run src/components/timeline/TimelineCapsuleTooltip.test.ts -t "has no Motion Editor Loop Clip tooltip module after ownership removal"`. The precise RED failure is that the obsolete tooltip or geometry module still exists as an importable Motion Editor surface after 43-13 removed all callers. GREEN: make only the residue deletion required by D-33R/D-36R: delete the rich tooltip component, its stale test subject, and the obsolete main-timeline capsule geometry helper after confirming no caller remains. Do not touch EFX rail geometry or the new passive marker type/projection/pure Canvas painter/tests. Run `pnpm --dir app exec vitest run src/components/timeline/TimelineCapsuleTooltip.test.ts -t "has no Motion Editor Loop Clip tooltip module after ownership removal"` byte-for-byte unchanged using the repository's established deleted-module absence-test pattern; it must pass before broader verification. Only after GREEN run renderer/interaction/EFX presentation regressions.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/timeline/TimelineCapsuleTooltip.test.ts -t "has no Motion Editor Loop Clip tooltip module after ownership removal" && pnpm --dir app exec vitest run src/components/timeline/TimelineRenderer.test.ts src/components/timeline/TimelineInteraction.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts</automated>
  </verify>
  <done>No obsolete Motion Editor Loop Clip tooltip or rich capsule geometry namespace remains; the passive marker projection/painter and EFX rail presentation stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete rejected EFX lane/full-filmstrip code, tests, and styles</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (D-34R/D-35R/D-36R integrated geometry and superseded presentation)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx (rejected separate lane/full-filmstrip residue)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (retained physical-cell and geometry contracts)
  </read_first>
  <behavior>
    - Sentinel test: `keeps only the integrated rail inside the unchanged physical row`.
    - The sentinel proves 161px/38px/3px/12px geometry, zero-loop absence, preserved linked indicators/cells/toolbar, and no separate lane/full-filmstrip mount.
  </behavior>
  <action>RED: add exactly one test named `keeps only the integrated rail inside the unchanged physical row` to `PhysicsPaintWorkflowStrip.test.ts`. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts -t "keeps only the integrated rail inside the unchanged physical row"`. The precise RED failure is that a rejected sibling lane/full-filmstrip node or its extra-height style contract still exists. GREEN: delete `PhysicsPaintLoopClipLane.tsx`, remove rejected lane/full-filmstrip/band/badge selectors, and keep only the approved integrated rail rules plus existing 161px strip, 38px row, 24px cell, 34px toolbar, linked-indicator, selection, and drag contracts per D-34R through D-42. Add no placement drag per D-41. Run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts -t "keeps only the integrated rail inside the unchanged physical row"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run WorkflowStrip, Rail, and multi-selection regressions.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts -t "keeps only the integrated rail inside the unchanged physical row" && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts</automated>
  </verify>
  <done>The integrated rail is the only EFX Loop Clip timeline presentation and physical-cell behavior remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove specialized child listeners and retain local controller authority</name>
  <files>app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/lib/physicPaintLoopOperationBridge.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-39R/D-40R local ownership and D-46 generic retention)
    - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts (specialized versus generic listeners)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (listener mounts and direct local ports)
    - app/src/lib/physicPaintLoopOperationBridge.test.ts (obsolete cross-window operation subject)
  </read_first>
  <behavior>
    - Sentinel test: `routes Loop Clip operations locally without specialized child listeners`.
    - The sentinel proves Studio uses direct local controller ports while generic project context, authority/apply, save, launch/focus, and frame-sync hooks remain mounted.
  </behavior>
  <action>RED: replace the obsolete cross-window positive case with exactly one test named `routes Loop Clip operations locally without specialized child listeners` in `physicPaintLoopOperationBridge.test.ts`. Run `pnpm --dir app exec vitest run src/lib/physicPaintLoopOperationBridge.test.ts -t "routes Loop Clip operations locally without specialized child listeners"`. The precise RED failure is that Studio still mounts a specialized open/edit or operation listener instead of relying solely on the local controller ports established in 43-11/43-12. GREEN: delete specialized listener hooks, exports, Studio calls, and protocol-only child in-flight state per D-39R/D-40R/D-46; retain all generic bridge hooks and existing controller authority/acknowledgement/history behavior. Retire the obsolete bridge-operation test subject once the sentinel's local-route replacement is green. Run `pnpm --dir app exec vitest run src/lib/physicPaintLoopOperationBridge.test.ts -t "routes Loop Clip operations locally without specialized child listeners"` byte-for-byte unchanged using the established retired-protocol test pattern; it must pass before broader verification. Only after GREEN run Studio and controller regression files.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintLoopOperationBridge.test.ts -t "routes Loop Clip operations locally without specialized child listeners" && pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</automated>
  </verify>
  <done>Loop Clip actions are local to EFX Paint, generic parent bridge hooks remain, and the specialized child listener surface is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local EFX UI → physical controller | Local intent still requires exact accepted parent authority |
| generic child bridge hooks | Project context, authority/apply, save, launch/focus, and frame-sync remain security-sensitive |
| deleted legacy surfaces → retained UI | Cleanup must not remove canonical output consumers or physical-cell behavior |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-14-01 | Spoofing | stale tooltip/lane surfaces | high | mitigate | Delete obsolete modules/selectors and replace positive tests with absence contracts |
| T-43-14-02 | Elevation of Privilege | specialized child listeners | high | mitigate | Remove specialized listener mounts while retaining local controller authority and generic hooks |
| T-43-14-03 | Tampering | physical cells and integrated rail | medium | mitigate | Exact geometry, linked-indicator, selection, and drag regressions remain green |
| T-43-14-04 | Denial of Service | dead listeners and protocol state | medium | mitigate | Remove protocol-only child replay/dedup state after callers are gone |
| T-43-14-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Run each exact one-test RED/GREEN command, then its broader focused regression command.
- Confirm deleted rich tooltip/geometry/lane/style/listener surfaces have no callers, the passive marker type/projection/renderer/tests remain intact, and the integrated rail/physical-cell regressions remain green.
- Confirm Studio has no specialized Loop Clip child listener while generic bridge hooks and controller regressions remain green.
</verification>

<success_criteria>
- D-33R/D-36R stale rich Motion Editor and rejected EFX presentation residue is structurally gone while the passive marker path remains exact and protected.
- D-34R through D-45 integrated rail, tooltip, popover, sidebar, focus, and physical-cell behavior remains green.
- D-46 local action ownership is complete at the child seam; public transport removal and full evidence remain isolated to 43-15.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-14-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Deleted now-unmounted Motion Editor tooltip/rich capsule geometry and rejected EFX lane/full-filmstrip residue
- Integrated rail-only CSS and physical-row regression contracts
- Studio-local Loop Clip action path with specialized child listeners removed and generic bridge hooks retained
