---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 14
type: execute
wave: 11
depends_on:
  - 43-13
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts
  - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/lib/physicPaintLoopOperationBridge.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
autonomous: true
gap_closure: true
requirements:
  - HOLD-01
  - HOLD-02
  - HOLD-03
  - HOLD-04
  - HOLD-05
  - HOLD-06

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-39: the obsolete main-timeline open-loop-edit and Loop Clip operation request/result protocol is gone after all callers are removed, while generic launch, project context, authority, apply-result, save, and frame-sync bridges remain."
    - "D-40: focused regressions preserve compact interval/lazy modulo behavior, no repeated durable assets, and bounded Infinity resolution after the surface relocation."
    - "D-01: Loop/Source Edit, Link/Create, unlink-only deletion, guards, materialization, re-expansion, and atomic Undo/Redo remain unchanged; HOLD-01 through HOLD-04 remain green through their established regression suites."
    - "D-15: all filmstrip visual/copy semantics now live in EFX Paint; D-24: canonical boundaries, resolver, persistence, preview/export parity, derived Effective duration, and verbatim unresolved references remain unchanged; D-32: virtual resolution creates no repeated durable/cache assets."
    - "The failed Step 1 UAT is reset to inspect and operate the complete filmstrip inside EFX Paint, and every later UAT step uses the corrected ownership and no lane Delete/Backspace shortcut."
    - "Fresh post-correction integrated evidence is recorded only after full Vitest, app typecheck, production build, and the original Phase 43 dependency-diff gate all execute successfully on Waves 8-11."
    - "The existing 43-10 human checkpoint remains the sole native approval gate and resumes only after those fresh gates and the 43-UAT.md evidence update; this plan creates no competing checkpoint."
  artifacts:
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "generic Physics Paint bridge without Loop Clip-specific main-to-child operations"
    - path: "app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts"
      provides: "generic parent bridge listeners without Loop Clip request handlers"
    - path: ".planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md"
      provides: "corrected EFX-owned native UAT script and preserved Step 1 failure evidence"
  key_links:
    - from: "EFX lane/popover"
      to: "existing local Roto Play Script controller"
      via: "direct focused ports proven by 43-12"
      pattern: "openLoopEdit|duplicateLinkedLoop|unlinkLoop|repairLoop|relinkLoop"
    - from: "generic child physical edit"
      to: "parent authority/apply-result"
      via: "retained generic bridge contracts"
      pattern: "authority|apply-result"
    - from: "post-correction integrated repository"
      to: ".planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md"
      via: "fresh full Vitest, typecheck, production build, and Phase 43 dependency-diff results recorded before handoff"
      pattern: "vitest run|typecheck|pnpm build|git diff --quiet"
    - from: ".planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md"
      to: "43-10 Task 2"
      via: "resume the existing native user-oracle checkpoint only after fresh automated gate evidence"
      pattern: "EFX Paint|Loop Clips|Gate evidence"
  prohibitions:
    - statement: "MUST NOT remove or weaken generic launch, context, authority, apply-result, save, or frame synchronization bridge behavior."
      status: resolved
      verification: automated
    - statement: "MUST NOT create a second native UAT checkpoint; 43-10 resumes after this plan."
      status: resolved
      verification: judgment
    - statement: "MUST NOT resume 43-10 Task 2 from pre-correction gate evidence; Waves 8-11 require fresh full-suite, typecheck, build, and dependency-diff evidence."
      status: resolved
      verification: automated
    - statement: "MUST NOT claim Phase 43 complete before the resumed 43-10 native UAT passes."
      status: resolved
      verification: judgment
    - statement: "MUST NOT access signing identities, certificates, Keychain certificate data, Apple credentials, notarization material, or release secrets."
      status: resolved
      verification: judgment
---

<objective>
Remove the now-unused Loop Clip-specific cross-window protocol, run the correction regression matrix, and rewrite the native UAT script so the existing Plan 43-10 checkpoint can resume on the corrected EFX Paint ownership.

Purpose: Finish the architectural cleanup, prove no valid Phase 43 substrate regressed, and hand native validation back to the already-established user oracle without duplicating checkpoints.
Output: Smaller generic bridge boundary, correction-wide focused regressions plus fresh integrated full-suite/typecheck/build/dependency evidence, and an EFX-owned 43-UAT.md ready for 43-10 continuation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-04-SUMMARY.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-09-SUMMARY.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md
@app/src/lib/physicPaintBridge.ts
@app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
@app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
@app/src/types/physicPaint.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove Loop Clip-specific bridge contracts and transport senders</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts, app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-39 retained versus removed bridge boundary)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (loop-specific protocol removal identifiers and generic-retention rule)
    - app/src/lib/physicPaintBridge.ts (open-edit and operation constants/functions plus generic contracts)
    - app/src/types/physicPaint.ts (strict Loop Clip request/result envelopes and canonical physical types)
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts (loop-specific versus generic transport senders)
  </read_first>
  <behavior>
    - Test 1: public bridge API exposes no main-timeline Loop Clip open or mutation client, retry delivery, or request/result event channel.
    - Test 2: physicPaint public transport types expose no removed Loop Clip request/result envelope or guard while canonical Loop Clip records, physical publications, authority, and accepted apply-result types remain.
    - Test 3: generic launch, project context, physical authority, apply-result, save, and frame-sync transport tests remain green in Browser and Tauri modes.
  </behavior>
  <action>
    After 43-13 removed every caller, delete the Loop Clip-specific open-edit and operation request/result constants, clients, retries, request/result envelopes, exact-key guards, and transport senders identified by `43-PATTERNS.md`. Per D-39, retain all generic launch/focus, project-context, physical authority, apply-result, save, and frame synchronization behavior and tests. Remove only tests whose subject is the obsolete protocol; convert shared bridge suites to explicit generic-retention contracts. Keep canonical `PhysicPaintRotoLoopClip`, publication, authority, and accepted output types intact. Do not broaden remaining envelopes to compensate for removed specialized types.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The obsolete open-loop-edit and Loop Clip mutation bridge API, events, envelopes, guards, and senders have no repository API surface.
    - Generic Browser/Tauri launch, context, authority, apply-result, save, and frame-sync tests remain green.
    - Canonical physical Loop Clip and accepted transaction types remain available.
    - No package or transport abstraction is added.
  </acceptance_criteria>
  <done>The shared bridge is reduced to generic EFX Paint responsibilities with no main-timeline Loop Clip protocol.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove child Loop Clip listeners and preserve local controller authority</name>
  <files>app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/lib/physicPaintLoopOperationBridge.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-37, D-39 local ownership)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (usePhysicsPaintOpenLoopEditBridge/usePhysicsPaintLoopOperationBridge removal)
    - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts (loop listener hooks and generic hooks)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (bridge hook calls plus local controller)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (canonical local operation and exact acknowledgement suites)
  </read_first>
  <behavior>
    - Test 1: PhysicsPaintStudio mounts no Loop Clip-specific bridge listener and still exposes local lane/popover operation ports.
    - Test 2: generic parent bridge hooks remain mounted for launch/context/authority/apply/save/frame synchronization as applicable.
    - Test 3: local open/update/duplicate/unlink/repair/relink operations still validate active authority, commit once, require exact acknowledgement, and return explicit rejection reasons.
  </behavior>
  <action>
    Remove `usePhysicsPaintOpenLoopEditBridge` and `usePhysicsPaintLoopOperationBridge` definitions, exports, Studio calls, in-flight request deduplication/replay state used only by those hooks, and the dedicated cross-window operation test file. Keep local lane/popover ports wired to the existing `rotoPlayScript` controller per D-37 and D-39. Preserve generic parent bridge lifecycle and exact physical edit coordinator authority. Update Studio source/behavior tests to prove local ownership and generic listener retention, then run the canonical controller suite to ensure guards, revision checks, source validation, accepted acknowledgement, unlink-only deletion, and atomic operation results remain unchanged.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Studio has no Loop Clip-specific cross-window listener hook or in-flight protocol state.
    - Local EFX lane/popover operation ports still reach the canonical controller.
    - Generic bridge hooks and accepted physical-edit authority remain intact.
    - The obsolete dedicated operation-bridge test file is deleted; controller authority tests remain green.
  </acceptance_criteria>
  <done>The EFX Studio owns Loop Clip actions locally and the unnecessary main-to-child attack surface is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Run fresh post-correction gates and reset the existing native UAT handoff</name>
  <files>.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md (failed Step 1 evidence and stale main-timeline instructions)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (corrected S1/S1a and retained S2-S6 native checks)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-10-PLAN.md (existing sole human checkpoint and unsigned smoke boundary)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-04-SUMMARY.md (HOLD-01..04 exact regression files)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-09-SUMMARY.md (preview/export parity and unresolved policy regressions)
  </read_first>
  <action>
    First run the exact focused regression matrix in `<verify>` without starting the server. It must preserve HOLD-01..04 through their established tests, HOLD-05 compact resolution/controller/history/preview/export invariants, HOLD-06 canonical thumbnail threading, one-shared-lane geometry, bounded popover, controlled Duplicate/Relink behavior, main-timeline absence, and generic bridge retention. After the focused matrix passes, execute fresh integrated post-correction gates on the final Waves 8-11 repository state, in this order: `pnpm --dir app exec vitest run`, `pnpm --dir app run typecheck`, `pnpm build`, and the same Phase 43 dependency-diff gate used by 43-10: `git diff --quiet $(git log --format="%H" --grep="(43-01)" | tail -1)^ HEAD -- app/package.json pnpm-lock.yaml`. Investigate any failure at its source; do not weaken tests, change test configuration, install dependencies, or hand off to UAT with a failed/skipped gate. Only after all four integrated commands exit 0, rewrite `43-UAT.md`: preserve the original 2026-08-07 Step 1 failure evidence in a short history section, add a new dated post-correction gate-evidence section containing the actual command outcomes (including current Vitest counts/build budget output when emitted and the dependency-diff base/result), and reset executable result boxes for a fresh run. Every capsule inspection and action must occur inside EFX Paint's dedicated Loop Clips lane: Step 1 verifies canonical source thumbnails and the full 5f×5 capsule there, one/many loops sharing one lane, and total Motion Editor main-timeline absence; body opens the local occurrence popover; badge opens local Loop Edit; Duplicate uses its validated destination form and Cancel/rejection behavior; Relink uses its compatible-cycle chooser and Cancel/rejection behavior; Repair/Unlink/Delete use local actions; a long unresolved list remains bounded and scrollable; linked physical cells below remain navigation/non-drag cells. Replace the stale Studio-open/closed bridge test with local open/focus behavior inside EFX Paint. Replace the stale focused Delete/Backspace deletion expectation with the locked no-shortcut behavior and explicit popover Delete action. Preserve all valid later checks for Infinity, truncation/re-expansion, three zoom bands, Update and Source Edit Undo/Redo, Link/Create, guards, materialization, zero-effective anchor, additive linked-cell treatment, save/reopen, unresolved repair/relink/verbatim persistence, preview placeholder, export block, valid PNG parity, and unsigned packaged smoke. Keep the no-signing boundary verbatim in effect: do not access signing identities, certificates, Keychain certificate data, Apple credentials, notarization material, or release secrets. End the document with an explicit handoff stating that the fresh four-gate evidence and 43-11..43-14 automated correction summaries must exist before resuming 43-10 Task 2; do not add another checkpoint and do not claim approval until the user completes native UAT.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoHoldDeterminism.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/stores/physicPaintStore.rotoHoldComposite.test.ts src/components/physic-paint/hooks/useRotoTimelineModel.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipLane.test.tsx src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/timeline/TimelineInteraction.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts src/lib/exportEngine.loops.test.ts src/lib/previewRenderer.loops.test.ts && pnpm --dir app exec vitest run && pnpm --dir app run typecheck && pnpm build && git diff --quiet $(git log --format="%H" --grep="(43-01)" | tail -1)^ HEAD -- app/package.json pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - The focused one-shot matrix exits 0 and covers HOLD-01..06 plus every corrected ownership/removal seam.
    - Fresh full Vitest, app typecheck, production build, and the unchanged Phase 43 dependency-diff command all execute after bridge/timeline cleanup and exit 0.
    - `43-UAT.md` preserves the failed Step 1 diagnosis, records fresh post-correction command evidence, and uses EFX Paint as the sole Loop Clip authoring/visualization host.
    - Step 1 explicitly proves canonical thumbnails, one shared lane for one/many loops, full EFX filmstrip presence, and total main-timeline absence; Duplicate/Relink and long unresolved-list checks match the corrected local UI.
    - Keyboard UAT expects body/badge Enter/Space and Escape/focus return, with no Loop Clip Delete/Backspace shortcut.
    - The final handoff resumes 43-10 Task 2 as the only human checkpoint only after gate evidence is recorded; status remains automated-ready pending user approval.
  </acceptance_criteria>
  <done>The focused matrix and all four fresh integrated gates are green and recorded in 43-UAT.md; only then is the existing 43-10 native UAT checkpoint ready to resume against the corrected product surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remaining generic bridge | Cross-window project/layer authority and physical apply messages remain security-sensitive |
| EFX local UI → physical controller | Local intents still require exact parent-authoritative acceptance |
| planning handoff → native user oracle | Automated evidence must not be misrepresented as visual approval |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-14-01 | Elevation of Privilege | generic bridge cleanup | high | mitigate | Remove only specialized callers/contracts; retain active project/layer and exact authority/apply guards |
| T-43-14-02 | Tampering | local Loop Clip mutation | high | mitigate | Canonical controller suite proves revision checks, exact acknowledgement, atomic commit, and rejection |
| T-43-14-03 | Repudiation | UAT status | medium | mitigate | Preserve failed evidence, record fresh post-correction gate outcomes, and explicitly keep 43-10 checkpoint pending until user verdict |
| T-43-14-04 | Tampering | stale automated evidence | high | mitigate | Execute full Vitest, typecheck, production build, and unchanged dependency diff after Waves 8-11; handoff is blocked unless every command exits 0 |
| T-43-14-05 | Denial of Service | Infinity and repeated loops | high | mitigate | Focused lazy-resolution tests prove compact intervals, visible-window bounds, and no repeated assets per D-40 |
| T-43-14-06 | Information Disclosure | signing material | high | mitigate | No certificate, Keychain, Apple credential, notarization, or release-secret access in correction planning/execution |
| T-43-14-SC | Tampering | package supply chain | high | mitigate | No package installs or dependency changes are permitted in this plan |
</threat_model>

<verification>
- Run the two task-focused one-shot Vitest commands, then Task 3's focused correction matrix.
- On the final corrected repository, run full `pnpm --dir app exec vitest run`, `pnpm --dir app run typecheck`, `pnpm build`, and the unchanged Phase 43 dependency-diff command; all must exit 0.
- Confirm removed protocol identifiers have no remaining production or test references after cleanup, while generic bridge suites stay green.
- Confirm `43-UAT.md` records fresh gate evidence, is executable on the EFX-owned lane, and explicitly hands back to 43-10 Task 2 only after those gates.
</verification>

<success_criteria>
- Every `43-PATTERNS.md` loop-specific bridge removal assignment is complete.
- HOLD-01..04 remain regression-pinned; HOLD-05 and HOLD-06 corrected integration is green.
- Fresh full-suite, typecheck, production-build, and dependency-diff evidence is captured after all correction waves.
- UAT failure evidence is preserved and the corrected script is ready only after those gates.
- Phase status remains automated-ready until the existing 43-10 user checkpoint passes.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-14-SUMMARY.md` when done, then resume 43-10 Task 2 using the corrected `43-UAT.md`.
</output>

## Artifacts this phase produces (this plan)

- Generic Physics Paint bridge with the Loop Clip-specific main-to-child protocol removed
- Correction-wide HOLD-01..06 focused regression evidence plus fresh full Vitest, typecheck, production-build, and dependency-diff results
- Corrected `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md` with new gate evidence for the existing 43-10 checkpoint

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Deterministic static/hold plus linked Loop Clips without duplicated durable assets, visualized as a full filmstrip capsule | 43-11..43-14 | COVERED | HOLD substrate retained; capsule moved intact to the corrected EFX host |
| REQ | HOLD-01 | Complete static/hold stroke set on every destination frame | 43-14 | COVERED | Existing hardening suite rerun; no reimplementation |
| REQ | HOLD-02 | Deterministic held output across reopen/cache regeneration | 43-14 | COVERED | Existing determinism suite rerun |
| REQ | HOLD-03 | Existing atomic commit/cancel/failure/Undo/Redo path | 43-14 | COVERED | Existing controller/history tests rerun |
| REQ | HOLD-04 | Parent Paint ownership and one resolved raster per frame | 43-14 | COVERED | Existing composite and preview/export parity tests rerun |
| REQ | HOLD-05 | Canonical linked regions, modulo resolution, boundaries, re-expansion, source propagation | 43-11, 43-12, 43-14 | COVERED | Canonical model/resolver/store/controller retained and focused regressions run |
| REQ | HOLD-06 | Full filmstrip, badges, requested/effective, English truncation | 43-11, 43-12 | COVERED | S1/S1a implemented on EFX lane with full semantics |
| CONTEXT | D-33 | Remove every main-timeline Loop Clip surface and interaction | 43-11, 43-13 | COVERED | Costly locked relocation; renderer absence starts in tracer, complete deletion follows |
| CONTEXT | D-34 | Dedicated EFX range-object lane above physical cells | 43-11 | COVERED | Separate same-scroller lane, cell isolation pinned |
| CONTEXT | D-35 | Hidden when empty; exact existing geometry retained | 43-11 | COVERED | 161px empty and 193px loop-present contracts |
| CONTEXT | D-36 | Full filmstrip semantics retained on new host | 43-12 | COVERED | Ordered canonical real-key dataUrl thumbnails, repetitions, badges, truncation, error, anchor, zoom bands |
| CONTEXT | D-37 | Body local popover; badge local Loop Edit; local actions | 43-11, 43-12 | COVERED | Signal-controlled Duplicate destination form and compatible-cycle Relink chooser; no prompt or main-editor bridge round trip |
| CONTEXT | D-38 | Existing Roto navigation/selection/multi-select/drag unchanged | 43-11, 43-14 | COVERED | Lane isolation plus established reducer/strip regressions |
| CONTEXT | D-39 | Existing accepted controller/history path; remove specialized protocol only | 43-12, 43-14 | COVERED | Exact accepted result drives closure; generic bridge retained |
| CONTEXT | D-40 | Compact intervals, visible-window derivation, O(1) modulo, no repeated assets/Infinity list | 43-11, 43-12, 43-14 | COVERED | Pure model joins one canonical real-key payload lookup; duplicate/repetition tests prove image reuse with no repeated assets |
| RESEARCH | — | Retain physical model, persistence, resolver, store, history, controller, preview/export substrate | 43-11..43-14 | COVERED | Historical main-timeline host recommendation explicitly superseded by D-33..D-40 |
| RESEARCH | — | Fail closed on malformed/unresolved data and preserve verbatim references | 43-12, 43-14 | COVERED | Text-only unresolved display; controller/export guards retained |
| RESEARCH | — | No new dependencies; Preact-native focused components | 43-11, 43-12 | COVERED | New lane/popover/pure module; no Studio presentation sprawl |
| UAT | Step 1 | Failed because capsule shipped on `PPaint #1` instead of EFX Paint/Roto | 43-11, 43-13, 43-14 | COVERED | Tracer corrects ownership, cleanup removes stale host, fresh integrated gates precede UAT reset |
| UI | S1 | Dedicated EFX Loop Clip lane and complete filmstrip | 43-11, 43-12 | COVERED | Canonical thumbnails, exact geometry, one shared lane for one/many loops, and all visual states |
| UI | S1a | Local occurrence popover | 43-12 | COVERED | Exact facts/actions, bounded unresolved scrolling, controlled Duplicate/Relink, accessibility, and accepted behavior |
| UI | S2 | Existing Loop Edit dialog | 43-11, 43-12, 43-14 | COVERED | Badge opens existing local controller/dialog path |
| UI | S3 | Existing Source Edit / Repair | 43-12, 43-14 | COVERED | Existing controller path reused and UAT retained |
| UI | S4 | Existing Link/Create choice | 43-12, 43-14 | COVERED | Retained unchanged and UAT retained |
| UI | S5 | Additive linked-cell treatment | 43-11, 43-14 | COVERED | Palette/geometry and non-key behavior preserved |
| UI | S6 | Guards, placeholder, preflight, export errors | 43-12, 43-14 | COVERED | Existing suites and corrected UAT retained |
| PATTERNS | projection/types | frameMap feed, main-editor range builders, timeline capsule types/field | 43-13 | COVERED | Removed without replacement adapter |
| PATTERNS | render | TimelineRenderer draw path | 43-11, 43-13 | COVERED | Disabled in tracer, definition/imports deleted in cleanup |
| PATTERNS | interaction | Hit testing, selection, hover/focus, keyboard, operations | 43-13 | COVERED | Visible and invisible ownership removed |
| PATTERNS | tooltip/canvas/geometry | TimelineCanvas mount, TimelineCapsuleTooltip, timeline geometry namespace | 43-13 | COVERED | Host-neutral logic relocated to EFX before deletion |
| PATTERNS | bridge service/types | Loop events, clients, envelopes, guards, transport senders | 43-14 | COVERED | Specialized protocol removed; generic contracts retained |
| PATTERNS | child handlers | Open/edit and operation listener hooks in Studio | 43-14 | COVERED | Local direct controller ports replace them |
