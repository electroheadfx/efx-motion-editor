---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 10
type: execute
wave: 6
depends_on:
  - 43-04
  - 43-08
  - 43-09
files_modified:
  - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md
autonomous: false
requirements:
  - HOLD-01
  - HOLD-02
  - HOLD-03
  - HOLD-04
  - HOLD-05
  - HOLD-06

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Full automated gate suite is green: pnpm --dir app exec vitest run, typecheck, and build all pass on the integrated phase"
    - "Zero new package dependencies were added during the phase (UI-SPEC Registry Safety)"
    - "The user has exercised the capsule, loop editing, guards, re-expansion, unresolved-loop repair/relink, export block, and save/reopen natively and approved"
  artifacts:
    - path: ".planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md"
      provides: "numbered native UAT script with expected outcomes per step"
  key_links:
    - from: ".planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md"
      to: "app (dev run)"
      via: "user-run native visual UAT — the project's final oracle"
      pattern: "step"
  prohibitions:
    - statement: "MUST NOT declare the phase done before native UAT passes — automated-ready is the ceiling until the user approves (project convention)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT add dependencies to satisfy a gate failure — fix the source (Registry Safety)"
      status: resolved
      verification: judgment
---

<objective>
Final integration gate: run the full automated suite, assemble the numbered native UAT script covering every locked behavior, and hand off to the user's native visual UAT — the project's final oracle.

Purpose: per project convention nothing is "done" until live UAT passes; this plan proves automated-readiness and captures the UAT verdict.
Output: green gates + an executed 43-UAT.md with the user's verdict.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: full automated gates + UAT script assembly</name>
  <files>.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (all surfaces S1-S6, copy contract)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md (Manual-Only Verifications table)
    - .planning/ROADMAP.md (Phase 43 success criteria; Phase 44 UAT step list)
  </read_first>
  <action>
    Run the full gate suite: pnpm --dir app exec vitest run (full), the repo typecheck, and pnpm build — all must pass; investigate and fix any failure at the source, never by weakening assertions. Verify zero dependency changes: git diff on app/package.json and pnpm-lock.yaml across the phase shows no added dependencies. Then author 43-UAT.md — a numbered native UAT script with exact steps and expected outcomes covering: (1) create a 5-frame cycle × 5 loop and verify the badge `Cycle 5f × 5 = 25f` and 25 resolved frames storing 5 source assets; (2) infinity loop badge `Cycle Nf × ∞` resolving to the parent end; (3) truncation by a next clip with the amber diagonal and the `Loop shortened by next clip` tooltip for partial vs complete cycles; (4) moving/removing the next clip re-expands the loop with no regeneration; (5) capsule at all three zoom bands (ghost cells ≥16px, hatched band 8-15px, band+badge <8px); (6) badge click opens loop-edit mode with Studio open AND closed; `Update loop` one-Undo; (7) `Edit source cycle…` regeneration updates every linked loop with the shared-count notice; (8) Link to existing cycle vs Create new cycle; (9) `Duplicate linked loop`, `Unlink loop`, `Delete loop` (unlink-only) each one-Undo; (10) guards: source-key deletion rejection, single-key drag and Force Spacing rejections, Delete-key rejection verbatim copy, Clear materialization; (11) paint on a linked frame materializes a local key and shortens the loop; (12) zero-effective anchor flag visible and interactive; (13) occurrence tooltip `Repeat 3 · Source frame 2 of 5` + `Edit source frame` seek; (14) Studio strip additive link badge with unchanged palette/geometry; (15) save/reopen preserves loops; a v0.8.1 project opens unchanged; (16) export blocked on an unresolved loop with the locked error; preview shows the marked placeholder; (17) keyboard: focus ring, Enter/Escape/Delete on capsule; (18) the unresolved-loop repair/relink flow (D-31): the executor prepares a fixture project by saving a loop project, then removing one source-cycle key entry from the saved JSON so the record dangles — the user opens the fixture natively and verifies the capsule keeps its red error outline, the error tooltip lists the missing reference(s) and the remedy line, and invoking `Repair loop…` opens the prefilled source-edit flow where `Regenerate source cycle` recreates the source keys and retargets the loop in one atomic commit (one Undo restores the unresolved record verbatim; Redo re-applies), placeholders are replaced by real content, and export unblocks; then on a second fixture the user invokes `Relink loop…`, chooses an existing compatible source cycle, and verifies the loop re-resolves against the new source with one-Undo semantics; finally the user verifies an unrepaired fixture survives save/reopen with the unresolved record and its dangling references preserved exactly.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run && pnpm --dir app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest suite exits 0
    - Typecheck exits 0
    - pnpm build succeeds
    - git diff on app/package.json across the phase shows no added dependencies
    - 43-UAT.md exists with numbered steps covering all 18 areas above, each with an explicit expected outcome
  </acceptance_criteria>
  <done>All automated gates green on the integrated phase; the UAT script is complete and executable by the user without interpretation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: native visual UAT — user oracle</name>
  <what-built>The complete Phase 43 feature set: deterministic static/hold hardening, linked Loop Clips (modulo resolution, next-clip priority, re-expansion), the filmstrip capsule (thumbnails, zoom-adaptive band/ghost cells, badges, truncation diagonal, anchor flag, tooltips, keyboard model), loop-edit/source-edit dialog modes, Link/Create, Duplicate/Unlink/Delete loop ops, the D-31 Repair/Relink recovery ops for unresolved loops, all guarded rejections, Studio strip link badge, export preflight block, and placeholder preview policy.</what-built>
  <how-to-verify>
    Run the dev app (user runs the server per project convention) and execute every numbered step in .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md, in order. Each step lists its expected outcome — report any deviation with a screenshot per the user's usual workflow. Pay special attention to: the three zoom bands, partial vs complete truncation diagonal landing, the Studio-closed badge click, and the export-block error text.
  </how-to-verify>
  <resume-signal>Type "approved" if every step matches its expected outcome, or describe the failing steps (with screenshots) for a gap-closure round.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integrated phase → user deliverable | Final gate before the phase can seal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-10-01 | Tampering | dependency drift during phase | high | mitigate | git diff gate on package.json/pnpm-lock.yaml proves zero new dependencies |
| T-43-10-02 | DoS | degraded full-suite signal | medium | mitigate | Full suite + typecheck + build must all pass; no watch mode, no test-count assertions against recalled baselines |
| T-43-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs permitted in this plan; any install request triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run — full suite green
- Typecheck + build green
- 43-UAT.md executed natively by the user with an approval verdict
</verification>

<success_criteria>
- Every automated gate green
- User approves native UAT across capsule visuals, loop editing, guards, re-expansion, export block, and save/reopen
- Phase ends "approved", not merely "automated-ready"
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-10-SUMMARY.md` when done, recording the gate results and the user's UAT verdict
</output>

## Artifacts this phase produces (this plan)

- `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md` (numbered native UAT script + verdict record)
