---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts
  - app/src/lib/physicPaintPersistence.ts
  - app/src/types/project.ts
  - app/src/types/physicPaint.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts
  - app/src/stores/physicPaintStore.ts
autonomous: true
requirements:
  - HOLD-05

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Loop Clip record with a finite repeat or infinity state round-trips save then reopen byte-identically through the physical document (D-29)"
    - "A v0.8.1-shaped physical document with no loopClips member loads as an empty loop collection with no migration and no error (D-29)"
    - "A structurally malformed loopClips member fails parsing closed with a thrown error; a well-formed record with dangling source keyIds loads successfully and is preserved verbatim (D-31)"
    - "A loop-only edit (changing Repeat) produces a different canonical revision and a restorable Undo snapshot, because loopClips join the single revision fingerprint and history snapshot (D-10, Q1 resolved: extend the single fingerprint)"
    - "Effective duration, boundaries, repeat-instance mappings, and resolved destination frames are never written into the persisted document (D-30)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      provides: "PhysicPaintRotoLoopClip record, isPhysicPaintRotoLoopClip guard, parsePhysicPaintRotoLoopClips, loopClips document member, loopClips-aware revision fingerprint"
      contains: "loopClips"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts"
      provides: "RED-then-GREEN persistence gauntlet spec"
    - path: "app/src/lib/physicPaintPersistence.ts"
      provides: "loopClips in PERSISTED_DOCUMENT_KEYS, save mapping, hydration"
      contains: "loopClips"
    - path: "app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts"
      provides: "loop-only op snapshot/Undo and generation+shrink one-undo coherence spec"
  key_links:
    - from: "app/src/lib/physicPaintPersistence.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      via: "hydration mapping calls parsePhysicPaintRotoPhysicalDocument with loopClips present"
      pattern: "loopClips"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      via: "snapshotRevision calls buildPhysicPaintRotoPhysicalRevision(records, interpolation, loopClips)"
      pattern: "buildPhysicPaintRotoPhysicalRevision"
  prohibitions:
    - statement: "MUST NOT break the v0.8.1 project format — documents without loopClips load with no migration (D-29)"
      status: resolved
      verification: test
    - statement: "MUST NOT persist derived loop state — Effective duration, next-clip boundary, repeat-instance mappings, resolved destination frames (D-30)"
      status: resolved
      verification: test
    - statement: "MUST NOT drop, normalize, or rewrite missing/stale source keyId references at load — they are preserved verbatim (D-31)"
      status: resolved
      verification: test
    - statement: "MUST NOT create a sidecar loop file — loopClips live only inside the physical-frame document (D-29)"
      status: resolved
      verification: test
---

<objective>
Land the Loop Clip persistence foundation: the `PhysicPaintRotoLoopClip` record, its fail-closed parsers, and the four-allowlist gauntlet (model document keys, persistence keys + save/hydrate mapping, `types/project.ts` document type, bridge apply-payload allowlists) threaded in ONE task per RESEARCH Pitfall 1, followed by revision-fingerprint and history-snapshot coverage so every later loop operation is atomic and undoable.

Purpose: D-29/D-30/D-31 lock additive, no-migration, no-silent-data-loss persistence; Q1 is resolved here by extending the single canonical revision fingerprint to cover loopClips (research recommendation; D-06/D-10 atomicity requires keys and loops in one snapshot).
Output: loopClips survive save/reopen byte-identically; v0.8.1 projects load unchanged; loop-only edits are revision-visible and undoable.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/lib/physicPaintPersistence.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): End-to-end loopClips persistence — record, four-allowlist gauntlet, round-trip</name>
  <reversibility rating="one-way">D-29: once user projects save loopClips records the field becomes a persisted document contract; removing or renaming it later requires a migration (v1.0.0 clean break is the designated escape). Door already walked through by the user in discuss-phase — rating kept for the record, no re-ask checkpoint.</reversibility>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/lib/physicPaintPersistence.ts, app/src/types/project.ts, app/src/types/physicPaint.ts, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (PHYSIC_PAINT_ROTO_PHYSICAL_DOCUMENT_KEYS at lines 269-278; hasOnlyAllowedKeys idiom at 322-336; buildPhysicPaintRotoPhysicalRevision at 595-601; parsePhysicPaintRotoPhysicalDocument at 653-702)
    - app/src/lib/physicPaintPersistence.ts (PERSISTED_DOCUMENT_KEYS at line 18; save mapping at 180-189; parsePersistedPhysicalDocument at 216-219; hydration at 255-264; isSafePhysicPaintCachePath at 85-89)
    - app/src/types/project.ts (McePhysicPaintRotoPhysicalDocument at 68-77)
    - app/src/types/physicPaint.ts (apply-payload hasOnlyKeys at line 330; result allowlist at line 367; PHYSIC_PAINT_MAX_APPLY_FRAMES at line 13)
    - app/src/lib/physicPaintBridge.ts (apply-payload validation + acceptance path)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-29, D-30, D-31)
  </read_first>
  <behavior>
    - Saving a document with one loop clip { loopId, canonicalStart: 10, sourceKeyIds: ['k1','k2','k3','k4','k5'], repeat: 5, mode: 'progressive' } then reopening yields a byte-identical loopClips collection
    - Loading a document JSON without the loopClips member yields an empty collection, no throw (v0.8.1 shape)
    - A loopClips member that is not an array, or a record missing loopId/canonicalStart/sourceKeyIds/repeat/mode or carrying an unknown key, throws (fail-closed)
    - A record whose sourceKeyIds reference keyIds absent from realKeyRecords loads successfully and the stored JSON keeps those references verbatim
    - repeat: 'infinity' persists and reloads as the explicit infinity state, never as a number
    - Commit apply payload carrying loopClips passes the types/physicPaint.ts allowlist guards
  </behavior>
  <action>
    RED first: create app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts following the physicsPaintRotoPlayScriptController.test.ts harness idiom (vi.hoisted/vi.mock, valid-PNG dataUrl factory) covering every behavior case; run it and confirm it fails; commit test(43-01). Then GREEN, in one coherent change set per RESEARCH Pitfall 1 (all four seams together):
    1. physicsPaintRotoPhysicalModel.ts — add exported interface PhysicPaintRotoLoopClip { readonly loopId: string; readonly canonicalStart: number; readonly sourceKeyIds: readonly string[]; readonly repeat: number | 'infinity'; readonly mode: 'progressive' | 'static' } (field names are D-31 discretion; semantics locked by D-29). Add guard isPhysicPaintRotoLoopClip and parser parsePhysicPaintRotoLoopClips mirroring the hasOnlyAllowedKeys + isRecord idiom: unknown keys, wrong types, empty sourceKeyIds, non-positive/non-integer finite repeat, or repeat values other than a positive integer or the string 'infinity' all throw. Do NOT validate sourceKeyIds against existing keys at parse time — dangling references are legal and preserved verbatim (D-31). Add 'loopClips' to PHYSIC_PAINT_ROTO_PHYSICAL_DOCUMENT_KEYS. In parsePhysicPaintRotoPhysicalDocument, accept loopClips as the first genuinely optional member: absent means empty array; present means parsed via parsePhysicPaintRotoLoopClips.
    2. app/src/lib/physicPaintPersistence.ts — add 'loopClips' to PERSISTED_DOCUMENT_KEYS; add loopClips to the field-by-field save mapping (lines 180-189) and to the hydration mapping (lines 255-264) so the field survives save and reload.
    3. app/src/types/project.ts — add exported McePhysicPaintRotoLoopClip interface and optional readonly loopClips member on McePhysicPaintRotoPhysicalDocument. Rust needs nothing (roto_physical is opaque serde_json::Value).
    4. app/src/types/physicPaint.ts — add 'loopClips' to the apply-payload hasOnlyKeys allowlist (line 330) and the matching result allowlist (line 367).
    5. app/src/lib/physicPaintBridge.ts — thread loopClips through the apply-payload validation/acceptance so a commit carrying the collection is accepted and delivered to the store apply path unchanged.
    No sidecar file, no migration code, no normalization of dangling references anywhere in this task.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopClips</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoLoopClips exits 0
    - grep -c "loopClips" app/src/lib/physicPaintPersistence.ts returns >= 3 (allowlist, save mapping, hydration)
    - grep -c "'loopClips'" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts returns >= 1
    - grep -c "loopClips" app/src/types/project.ts returns >= 2
    - The spec contains a case asserting a document without the loopClips key parses to an empty collection, and a case asserting dangling source keyIds are preserved verbatim in the reloaded record
  </acceptance_criteria>
  <done>Loop Clip records persist additively end-to-end: parse, save, hydrate, reopen byte-identical; v0.8.1 documents load as empty collection; malformed input throws; dangling references preserved verbatim; commit payload allowlists accept loopClips. RED commit precedes GREEN commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loopClips join the canonical revision fingerprint and the Undo/Redo snapshot (Q1)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts, app/src/stores/physicPaintStore.ts, app/src/lib/physicPaintPersistence.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (buildPhysicPaintRotoPhysicalRevision at 595-601; encodePhysicPaintRotoPhysicalContent at 609-632; buildPhysicPaintRotoProjectEquality at 635-646; revision recompute at 687-690)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts (RotoPhysicalEditCommand at 83-92; snapshotRecordsEqual at 138-159; snapshotRevision at 161-163)
    - app/src/stores/physicPaintStore.ts (revision call sites at lines 136 and 746)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 2)
  </read_first>
  <behavior>
    - Two documents identical in records+interpolation but differing only in one loop's repeat produce different canonical revisions
    - A loop-only operation (repeat change) creates a history command whose Undo restores the prior repeat value
    - A batch generation whose commit shortens an existing loop is one history command: one Undo removes the generated keys and restores the loop's prior effective range (D-06 coherence — the shrink itself is derived, so restoring the document snapshot restores it)
    - snapshotRecordsEqual reports equal when only derived state differs, unequal when loopClips differ
  </behavior>
  <action>
    RED first: create app/src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts covering the behavior cases; confirm failure; commit test(43-01). GREEN: extend buildPhysicPaintRotoPhysicalRevision to (records, interpolation, loopClips) and thread loopClips through encodePhysicPaintRotoPhysicalContent so the fingerprint changes on any loop record change; update the parse-time revision recompute (model lines 687-690) accordingly. Update every call site: physicPaintStore.ts lines 136 and 746, useRotoPhysicalEditHistory.ts snapshotRevision (lines 161-163), and buildPhysicPaintRotoProjectEquality (model 635-646) so persisted equality also covers loopClips. Extend RotoPhysicalEditSnapshot with a loopClips member, extend snapshotRecordsEqual (history 138-159) to compare it, and ensure command capture stores before/after loopClips so Undo replay through the existing replace-roto-physical-map path restores loop state with operationKind 'undo'. Keep one snapshot covering keys + loops together (D-06/D-10); do not create a parallel loop-only history channel.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoLoopHistory useRotoPhysicalEditHistory</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoLoopHistory exits 0
    - pnpm --dir app exec vitest run useRotoPhysicalEditHistory exits 0 (existing history behavior unchanged)
    - buildPhysicPaintRotoPhysicalRevision accepts loopClips and every call site passes it (grep -rn "buildPhysicPaintRotoPhysicalRevision" app/src shows no two-argument call remaining)
    - pnpm --dir app exec vitest run exits 0 (full suite green after signature change)
  </acceptance_criteria>
  <done>Loop-only edits are revision-visible, authority-checked, and undoable through the single existing snapshot history; generation+shrink is one undoable outcome; full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project file → parser | Untrusted .mce JSON crosses into the physical document authority at load |
| child window → parent commit | Apply payload crosses the bridge into the document authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01-01 | Tampering | parsePhysicPaintRotoLoopClips | high | mitigate | Fail-closed allowlist parse mirroring hasOnlyAllowedKeys; unknown keys/wrong types throw; dangling keyIds preserved verbatim and marked unresolved downstream (D-31) — never normalized |
| T-43-01-02 | DoS | repeat field | medium | mitigate | Finite repeat must be a positive integer; the existing parseRepeat safe-product bound (controller 340-353) plus the 600-frame capacity bound downstream prevent materialization blowups; parser rejects non-integer/zero/negative |
| T-43-01-03 | Tampering | bridge apply payload | high | mitigate | loopClips added to both hasOnlyKeys allowlists so unknown sibling keys still reject; payload revalidated on acceptance in physicPaintBridge.ts |
| T-43-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan (RESEARCH Package Legitimacy Audit: zero new dependencies); any addition during execution triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicsPaintRotoLoopClips physicsPaintRotoLoopHistory useRotoPhysicalEditHistory — all exit 0
- pnpm --dir app exec vitest run — full suite green
- A saved .mce containing loopClips reopens with the collection intact; a v0.8.1-shaped document (no loopClips key) opens with zero loops
</verification>

<success_criteria>
- loopClips persist byte-identically through save/reopen and Save As
- v0.8.1 projects load unchanged (additive optional member, absent means empty)
- Loop-only edits change the canonical revision and are undoable in one step
- No derived loop state is ever persisted
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `PhysicPaintRotoLoopClip` (interface, physicsPaintRotoPhysicalModel.ts) — fields: loopId, canonicalStart, sourceKeyIds, repeat, mode
- `isPhysicPaintRotoLoopClip` (guard, physicsPaintRotoPhysicalModel.ts)
- `parsePhysicPaintRotoLoopClips` (parser, physicsPaintRotoPhysicalModel.ts)
- `McePhysicPaintRotoLoopClip` (interface, types/project.ts)
- `McePhysicPaintRotoPhysicalDocument.loopClips` (optional member, types/project.ts)
- `buildPhysicPaintRotoPhysicalRevision(records, interpolation, loopClips)` (extended signature, physicsPaintRotoPhysicalModel.ts)
- `RotoPhysicalEditSnapshot.loopClips` (new snapshot member, useRotoPhysicalEditHistory.ts)
- `physicsPaintRotoLoopClips.test.ts` (new spec)
- `physicsPaintRotoLoopHistory.test.ts` (new spec)
