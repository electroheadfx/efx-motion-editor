---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 13
type: execute
wave: 10
depends_on:
  - 43-12
files_modified:
  - app/src/lib/frameMap.ts
  - app/src/lib/frameMap.test.ts
  - app/src/types/timeline.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/components/timeline/TimelineInteraction.test.ts
  - app/src/components/timeline/TimelineCanvas.tsx
  - app/src/components/timeline/TimelineCapsuleTooltip.tsx
  - app/src/components/timeline/TimelineCapsuleTooltip.test.ts
  - app/src/components/timeline/loopCapsuleGeometry.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-33: the Motion Editor main timeline has no Loop Clip projection field, renderer, hit region, tooltip, selection, keyboard target, context action, edit command, or hidden navigation shortcut."
    - "D-34R/D-36R: the rejected separate lane, 32px geometry, full capsule, thumbnail filmstrip, repetition band, permanent badge, and stale tests/styles are deleted rather than hidden or renamed."
    - "D-42: generic main-timeline rendering/interaction and EFX physical-cell navigation, real-key selection, multi-selection, drag, toolbar geometry, and blue linked indicators remain regression-green."
    - "D-46: canonical Loop Clip persistence, resolver, store, controller, physical-edit authority, preview/playback, save/reopen, and export consumption remain untouched."
    - "No invisible Loop Clip pointer or keyboard surface survives the visual cleanup."
  artifacts:
    - path: "app/src/lib/frameMap.ts"
      provides: "generic frame map without a main-editor Loop Clip projection"
    - path: "app/src/types/timeline.ts"
      provides: "timeline public types without capsule fields"
    - path: "app/src/components/timeline/TimelineInteraction.ts"
      provides: "generic timeline input routing without Loop Clip ownership"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "integrated rail rules with rejected lane/capsule residue removed"
  key_links:
    - from: "canonical physical-frame document"
      to: "preview/playback/save/export"
      via: "retained store and resolver consumers, not a main-timeline authoring projection"
      pattern: "resolvePhysicPaintRoto"
    - from: "PhysicsPaintWorkflowStrip"
      to: "PhysicsPaintLoopClipRail"
      via: "one integrated host inside the physical row"
      pattern: "PhysicsPaintLoopClipRail"
  prohibitions:
    - statement: "The removed Motion Editor surface may not return as a read-only summary, rail, badge, label, or navigation adapter."
      status: resolved
      verification: automated
    - statement: "The rejected EFX lane may not remain behind display:none, dead CSS, compatibility exports, or stale positive tests."
      status: resolved
      verification: automated
    - statement: "Canonical physical/resolver/controller/preview/export code must not be deleted as collateral cleanup."
      status: resolved
      verification: automated
---

<objective>
Delete the rejected dedicated-lane/full-capsule residue and every Motion Editor Loop Clip projection or interaction after the approved EFX rail/popover/sidebar replacement is complete, per D-33, D-34R, D-35R, D-36R, D-42, D-46, and D-48.

Purpose: Make the corrected ownership boundary structural rather than cosmetic while preserving generic timeline behavior and canonical physical output consumers.
Output: Removed obsolete projection/types/render/interaction/tooltip/geometry/lane residue and focused absence/regression contracts.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md
@app/src/lib/frameMap.ts
@app/src/types/timeline.ts
@app/src/components/timeline/TimelineRenderer.ts
@app/src/components/timeline/TimelineInteraction.ts
@app/src/components/timeline/TimelineCanvas.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove main-timeline projection, public types, and renderer residue</name>
  <files>app/src/lib/frameMap.ts, app/src/lib/frameMap.test.ts, app/src/types/timeline.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33 and D-46 ownership/consumer boundary)
    - app/src/lib/frameMap.ts (`loopCapsules`, range derivation, capsule builder)
    - app/src/types/timeline.ts (capsule source/capsule/layout types)
    - app/src/components/timeline/TimelineRenderer.ts (now-disabled draw definition/imports)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (approved EFX replacement facts/geometry owner)
  </read_first>
  <behavior>
    - Test 1: frame-map output has no Loop Clip capsule projection while ordinary FX track ordering/layout remains unchanged.
    - Test 2: public timeline types expose no Loop Clip capsule source, capsule, or layout field.
    - Test 3: TimelineRenderer contains and executes no Loop Clip draw operation while all established non-Loop elements remain green.
  </behavior>
  <action>RED: first replace one stale positive assertion in `frameMap.test.ts` or `TimelineRenderer.test.ts` with the D-33 absence contract, then run `pnpm --dir app exec vitest run src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts`. The command must fail while `loopCapsules`, timeline capsule public types/layout, or the obsolete draw route still exists. GREEN: delete the main-editor projection feed/builders, timeline capsule types/layout field, and renderer definition/imports. Do not create an alias or replacement summary. Keep canonical physical Loop Clip model/types under the physic-paint domain and retain preview/playback/export consumption through store/resolver paths per D-24 through D-32 and D-46. Rerun the exact focused command; it must pass after removal while ordinary FX track ordering/layout and non-Loop rendering remain green.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Main timeline data, type, and paint layers expose no Loop Clip presentation.
    - Generic frame-map and renderer behavior remains green.
    - Canonical physical Loop Clip records and resolution are untouched.
  </acceptance_criteria>
  <done>The Motion Editor data and rendering pipeline no longer contains Loop Clip presentation code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove main-timeline hit testing, keyboard routing, tooltip, and geometry</name>
  <files>app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineInteraction.test.ts, app/src/components/timeline/TimelineCanvas.tsx, app/src/components/timeline/TimelineCapsuleTooltip.tsx, app/src/components/timeline/TimelineCapsuleTooltip.test.ts, app/src/components/timeline/loopCapsuleGeometry.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33 zero-interaction boundary)
    - app/src/components/timeline/TimelineInteraction.ts (capsule hit, hover/focus, selection, keyboard, operation callers)
    - app/src/components/timeline/TimelineCanvas.tsx (tooltip import/mount)
    - app/src/components/timeline/TimelineCapsuleTooltip.tsx (stale main-editor dependencies/actions)
    - app/src/components/timeline/loopCapsuleGeometry.ts (confirm required compact rail derivation is already owned by EFX presentation tests)
  </read_first>
  <behavior>
    - Test 1: pointer movement/click at former capsule coordinates creates no Loop Clip hover, focus, selection, navigation, or operation intent.
    - Test 2: Enter, Delete, and Backspace in the Motion Editor never open, edit, unlink, or delete a Loop Clip.
    - Test 3: TimelineCanvas renders no Loop Clip tooltip mount or global Loop Clip tooltip state.
    - Test 4: ordinary timeline playhead, selection, layer interaction, drag, keyboard, and unrelated tooltip/overlay behavior remains unchanged.
  </behavior>
  <action>RED: add the former-coordinate pointer/keyboard absence assertion to `TimelineInteraction.test.ts` and the no-tooltip-mount source contract to the same focused slice, then run `pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx`. The command must fail while capsule hit testing, hover/focus publication, keyboard/operation routing, the TimelineCanvas tooltip mount, or obsolete geometry remains reachable. GREEN: delete those interaction routes, the tooltip mount/component/test, and the main-timeline geometry namespace per D-33. Confirm the already-green EFX rail/presentation/popover suites cover every retained compact fact before deleting shared-looking helpers. Remove files when no caller remains rather than leaving dead exports. Preserve unrelated timeline interactions, tooltips, overlays, and playback behavior. Rerun the same focused command; it must pass with zero visible or invisible Motion Editor Loop Clip route.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No visible or invisible Motion Editor Loop Clip input/floating-UI surface remains.
    - Obsolete tooltip and geometry files have no callers and are deleted.
    - Generic timeline interaction and EFX-owned rail/popover suites remain green.
  </acceptance_criteria>
  <done>The Motion Editor has zero Loop Clip interaction ownership at pointer, keyboard, selection, tooltip, and geometry layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete rejected EFX lane/full-capsule CSS and test residue</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (integrated rail geometry and rejected presentation list)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipLane.tsx (rejected lane/full capsule/permanent badge residue)
    - app/src/components/physic-paint/physicsPaintStudio.css (rejected 32px lane, 24px capsule, 16px badge selectors)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (stale sibling-row/badge assertions and retained cell geometry contracts)
  </read_first>
  <behavior>
    - Test 1: no component, CSS selector, DOM assertion, or product copy describes a dedicated Loop Clip lane, full capsule, source-thumbnail filmstrip, repetition band, or permanent Cycle badge.
    - Test 2: integrated rail remains exactly 3px/12px inside the 38px row and the strip remains 161px for loop/no-loop fixtures.
    - Test 3: linked indicators, 24px cells, physical interaction ordering, real-key-only drag, and 34px toolbar geometry remain green.
  </behavior>
  <action>RED: replace the stale dedicated-lane assertions in `PhysicsPaintWorkflowStrip.test.ts` with integrated 3px/12px/161px/38px absence-and-geometry assertions, then run `pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts`. The command must fail while the rejected lane component, its extra-height CSS, or positive sibling-row assertions still exist. GREEN: delete `PhysicsPaintLoopClipLane.tsx` after the integrated rail has all callers, remove its 32px/24px/16px CSS and stale assertions, and retain only the approved integrated rail rules plus existing linked-cell/cell/toolbar contracts. Remove rejected product vocabulary from active tests and selectors without weakening exact geometry and event-isolation assertions. Rerun the same focused command; it must pass with the integrated rail as the only EFX Loop Clip presentation.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Rejected lane/full-capsule code, CSS, and tests are deleted rather than dormant.
    - Approved integrated rail and all physical-cell geometry/interaction regressions remain green.
    - No horizontal placement drag was introduced during cleanup.
  </acceptance_criteria>
  <done>Only the approved integrated rail presentation remains inside EFX Paint/Roto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical physical output → Motion Editor | Main editor may consume resolved pixels but must not acquire Loop Clip authoring UI |
| timeline pointer/keyboard → commands | Removed coordinates and keys must not reach stale Loop Clip operations |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-13-01 | Spoofing | duplicate main-timeline surface | high | mitigate | Delete projection, rendering, tooltip, and interaction ownership together |
| T-43-13-02 | Elevation of Privilege | invisible hit/keyboard routes | high | mitigate | Behavioral absence tests prove removed coordinates/keys emit no Loop Clip intent |
| T-43-13-03 | Tampering | generic timeline and physical cells | medium | mitigate | Retain focused non-Loop renderer/interaction/cell regressions |
| T-43-13-04 | Information Disclosure | stale tooltip state | low | mitigate | Delete tooltip mount/component/state and related sequence dependencies |
| T-43-13-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Run all three focused one-shot Vitest commands.
- Confirm deleted files have no imports/callers and EFX replacement tests remain green.
- Confirm canonical model/resolver/store/controller/preview/export files are unchanged except for unrelated pre-existing work.
</verification>

<success_criteria>
- D-33 is structural across projection, type, render, pointer, keyboard, tooltip, selection, and action layers.
- D-34R/D-36R rejected lane/capsule residue is gone.
- D-42/D-46 generic and canonical behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Main timeline frame map, types, renderer, interaction, and canvas with zero Loop Clip UI
- Deleted stale TimelineCapsuleTooltip, loopCapsuleGeometry, and rejected PhysicsPaintLoopClipLane residue
- Absence contracts plus preserved generic timeline and physical-cell regressions
