---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 13
type: execute
wave: 10
depends_on:
  - 43-12
files_modified:
  - app/src/lib/frameMap.ts
  - app/src/lib/frameMap.test.ts
  - app/src/types/timeline.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/components/timeline/TimelineInteraction.test.ts
  - app/src/components/timeline/TimelineCanvas.tsx
  - app/src/components/timeline/TimelineCapsuleTooltip.tsx
  - app/src/components/timeline/TimelineCapsuleTooltip.test.ts
  - app/src/components/timeline/loopCapsuleGeometry.ts
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06

estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-33, the Motion Editor main timeline has no Loop Clip projection, type field, drawing, hit target, selection, tooltip, keyboard route, badge route, or operation request."
    - "Per D-34 and D-37, the complete presentation/action ownership now exists only in the EFX lane/popover delivered by 43-11 and 43-12."
    - "Generic main-timeline frame mapping, rendering, interaction, selection, playback, and non-Loop tooltips remain regression-green."
    - "No invisible Loop Clip hit region or stale tooltip mount remains after visual removal."
  artifacts:
    - path: "app/src/lib/frameMap.ts"
      provides: "generic frame map with no main-editor Loop Clip projection"
    - path: "app/src/types/timeline.ts"
      provides: "timeline layout types with no Loop Clip capsule fields"
    - path: "app/src/components/timeline/TimelineRenderer.ts"
      provides: "main timeline renderer with no Loop Clip drawing subsystem"
    - path: "app/src/components/timeline/TimelineInteraction.ts"
      provides: "main timeline interactions with no Loop Clip ownership"
  key_links:
    - from: "canonical physical Loop Clip document"
      to: "Motion Editor preview/playback/export consumers"
      via: "retained store/preview/export resolution, not timeline authoring projection"
      pattern: "resolvePhysicPaintRoto"
    - from: "main timeline canvas"
      to: "generic renderer and interaction"
      via: "layout fields and hit regions excluding Loop Clip capsule data"
      pattern: "TimelineRenderer|TimelineInteraction"
  prohibitions:
    - statement: "MUST NOT replace the removed projection with a renamed read-only main-timeline adapter (D-33)."
      status: resolved
      verification: automated
    - statement: "MUST NOT leave invisible Loop Clip hit regions, tooltip Signals, or keyboard handlers after drawing removal."
      status: resolved
      verification: automated
    - statement: "MUST NOT remove preview, playback, persistence, reopen, export, or generic timeline behavior that consumes canonical physical output."
      status: resolved
      verification: automated
---

<objective>
Delete the unintended Motion Editor Loop Clip projection and interaction subsystem now that the complete EFX-owned replacement is proven.

Purpose: Enforce D-33 as a real single-owner boundary, eliminate stale visual and invisible interaction surfaces, and retain the main editor only as a canonical preview/playback/persistence/export consumer.
Output: Main timeline types, frame map, renderer, interaction, canvas, tooltip, geometry, and tests cleaned of Loop Clip authoring ownership.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md
@app/src/lib/frameMap.ts
@app/src/types/timeline.ts
@app/src/components/timeline/TimelineRenderer.ts
@app/src/components/timeline/TimelineInteraction.ts
@app/src/components/timeline/TimelineCanvas.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove main-timeline Loop Clip projection, types, and renderer path</name>
  <files>app/src/lib/frameMap.ts, app/src/lib/frameMap.test.ts, app/src/types/timeline.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33 ownership removal)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (main-timeline projection/types/renderer removal assignments)
    - app/src/lib/frameMap.ts (loopCapsules feed, deriveMainEditorLoopRanges, buildTimelineLoopCapsules)
    - app/src/types/timeline.ts (TimelineLoopCapsuleSourceCell, TimelineLoopCapsule, FxTrackLayout.loopCapsules)
    - app/src/components/timeline/TimelineRenderer.ts (drawLoopCapsules definition and invocation)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (confirmed EFX replacement owner)
  </read_first>
  <behavior>
    - Test 1: frame-map output contains no Loop Clip capsule projection while ordinary FX track layout remains unchanged.
    - Test 2: TimelineRenderer has no Loop Clip drawing operation for any layout input and continues drawing all established non-Loop timeline elements.
    - Test 3: timeline public types no longer expose Loop Clip capsule source cells, capsules, or `FxTrackLayout.loopCapsules`.
  </behavior>
  <action>
    Replace stale positive tests with absence and generic-regression contracts, then remove the entire main-editor projection per D-33: `loopCapsules` output/feed, `deriveMainEditorLoopRanges`, `buildTimelineLoopCapsules`, main-timeline capsule source/capsule types and layout field, `drawLoopCapsules`, and its invocation. Do not create a renamed projection or read-only summary. Keep canonical physical Loop Clip types in the physic-paint domain and keep preview/playback/export consumption through existing store/resolver paths. Preserve all non-Loop frame-map ordering, layout, renderer, and test behavior.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No frame-map result or timeline type exposes Loop Clip capsule data.
    - TimelineRenderer contains and executes no Loop Clip drawing path.
    - Generic track layout and rendering regressions remain green.
    - Canonical physical Loop Clip records/resolver/store output remain untouched.
  </acceptance_criteria>
  <done>The Motion Editor data and drawing pipeline no longer owns or visualizes Loop Clips.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove capsule hit testing, selection, keyboard, and operation routing</name>
  <files>app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineInteraction.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33, D-37)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (TimelineInteraction removal assignment)
    - app/src/components/timeline/TimelineInteraction.ts (capsule hit test, hover/focus publication, selection, badge/body and keyboard routing)
    - app/src/components/timeline/TimelineInteraction.test.ts (stale positive capsule tests and generic interaction regressions)
  </read_first>
  <behavior>
    - Test 1: pointer movement/click over coordinates formerly occupied by a capsule does not create Loop Clip hover, focus, selection, or operation intent.
    - Test 2: main-timeline Enter, Delete, and Backspace paths never open/edit/unlink a Loop Clip.
    - Test 3: ordinary timeline selection, playhead navigation, layer interaction, drag, and keyboard behavior remain unchanged.
  </behavior>
  <action>
    Remove capsule hit testing, hover/focus request publication, capsule selection state, badge/body routing, open/edit/delete keyboard handling, and loop-operation requests from TimelineInteraction per D-33. Remove stale positive tests and add source/behavior absence contracts around the public interaction surface. Ensure no invisible hit region survives now that drawing is gone. Preserve generic timeline pointer/keyboard behavior and do not redirect removed actions to EFX automatically; Loop Clip authoring begins only from the EFX lane per D-37.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Main-timeline pointer and keyboard paths emit no Loop Clip intent.
    - No stale capsule selection or hover/focus state remains.
    - Existing non-Loop interaction suite remains green.
    - EFX lane remains the only Loop Clip selection/action surface.
  </acceptance_criteria>
  <done>The Motion Editor has neither visible nor invisible Loop Clip interaction ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete the main-timeline tooltip mount and stale geometry namespace</name>
  <files>app/src/components/timeline/TimelineCanvas.tsx, app/src/components/timeline/TimelineCapsuleTooltip.tsx, app/src/components/timeline/TimelineCapsuleTooltip.test.ts, app/src/components/timeline/loopCapsuleGeometry.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md (TimelineCanvas, TimelineCapsuleTooltip, and geometry relocation/removal assignments)
    - app/src/components/timeline/TimelineCanvas.tsx (tooltip import and mount)
    - app/src/components/timeline/TimelineCapsuleTooltip.tsx (stale sequenceStore/playback/request dependencies)
    - app/src/components/timeline/loopCapsuleGeometry.ts (confirm host-neutral algorithms were relocated by 43-11/43-12)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (new EFX-owned replacement)
  </read_first>
  <behavior>
    - Test 1: TimelineCanvas source and rendered tree contain no Loop Clip tooltip mount.
    - Test 2: EFX presentation tests still provide every relocated badge/zoom/ghost/truncation/anchor behavior before timeline geometry deletion.
    - Test 3: no global timeline tooltip Signal or main-editor sequence/playback dependency remains for Loop Clip occurrence actions.
  </behavior>
  <action>
    Remove the `TimelineCapsuleTooltip` import and mount from TimelineCanvas. Delete `TimelineCapsuleTooltip.tsx` and its stale tests after confirming all required pure facts/action ordering live in the EFX presentation/popover tests. Delete `loopCapsuleGeometry.ts` once every host-neutral algorithm has moved to `physicsPaintLoopClipPresentation.ts`; do not retain a stale main-timeline capsule namespace. Remove stale imports and update renderer/canvas source contracts. Preserve unrelated timeline tooltips and canvas overlays.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/timeline/TimelineRenderer.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TimelineCanvas imports/mounts no Loop Clip tooltip.
    - TimelineCapsuleTooltip and stale timeline geometry files/tests are deleted with no remaining callers.
    - All relocated copy, geometry, accessibility, and action behavior remains covered under EFX-owned tests.
    - Unrelated timeline tooltip/overlay behavior remains intact.
  </acceptance_criteria>
  <done>The unintended main-timeline Loop Clip UI subsystem is fully removed, not merely hidden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical physical data → main editor | Main editor consumes resolved output but must not gain authoring authority |
| timeline pointer/keyboard input → editor commands | Untrusted interaction must not reach removed Loop Clip operations |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-13-01 | Spoofing | duplicate main-timeline authoring surface | high | mitigate | Remove projection, render, tooltip, and interaction ownership together; absence tests cover all layers |
| T-43-13-02 | Elevation of Privilege | invisible hit regions | high | mitigate | Delete hit testing and operation routing, not only drawing |
| T-43-13-03 | Tampering | generic timeline behavior | medium | mitigate | Retain and run non-Loop frame-map, renderer, and interaction regressions |
| T-43-13-04 | Information Disclosure | stale tooltip state | low | mitigate | Delete global Loop Clip tooltip mount/state and its sequence/playback dependencies |
| T-43-13-SC | Tampering | package supply chain | high | mitigate | No package installs or dependency changes are permitted in this plan |
</threat_model>

<verification>
- Run each exact one-shot Vitest command.
- Search the changed main-timeline modules through their tests for absence of projection, drawing, hit testing, tooltip mounting, and operation routing.
- Confirm EFX-owned presentation/popover suites remain green after stale geometry and tooltip deletion.
</verification>

<success_criteria>
- Every `43-PATTERNS.md` main-timeline projection/render/interaction/tooltip removal assignment is complete.
- D-33 is enforced across data, type, paint, hit-test, keyboard, and floating UI layers.
- Main editor preview/playback/persistence/export consumption remains intact.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Main timeline frame map, types, renderer, interaction, and canvas with zero Loop Clip authoring surface
- Deleted stale `TimelineCapsuleTooltip` and timeline capsule geometry subsystem
- Behavioral absence contracts plus retained generic timeline regressions
