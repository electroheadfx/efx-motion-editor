---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 13
type: execute
wave: 10
depends_on:
  - 43-12
files_modified:
  - app/src/lib/frameMap.ts
  - app/src/lib/frameMap.test.ts
  - app/src/types/timeline.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/components/timeline/TimelineInteraction.test.ts
  - app/src/components/timeline/TimelineCanvas.tsx
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-33: the Motion Editor main timeline has no Loop Clip projection field, renderer, hit region, tooltip, selection, keyboard target, context action, edit command, or hidden navigation shortcut."
    - "D-34R/D-36R: active Motion Editor ownership is removed here; the now-unmounted tooltip/geometry and rejected EFX lane/style residue are explicitly handed to dependent Plan 43-14 for deletion."
    - "D-42: generic main-timeline rendering/interaction and EFX physical-cell navigation, real-key selection, multi-selection, drag, toolbar geometry, and blue linked indicators remain regression-green."
    - "D-46: canonical Loop Clip persistence, resolver, store, controller, physical-edit authority, preview/playback, save/reopen, and export consumption remain untouched."
    - "No invisible Loop Clip pointer or keyboard surface survives the visual cleanup."
  artifacts:
    - path: "app/src/lib/frameMap.ts"
      provides: "generic frame map without a main-editor Loop Clip projection"
    - path: "app/src/types/timeline.ts"
      provides: "timeline public types without capsule fields"
    - path: "app/src/components/timeline/TimelineInteraction.ts"
      provides: "generic timeline input routing without Loop Clip ownership"
    - path: "app/src/components/timeline/TimelineCanvas.tsx"
      provides: "generic timeline canvas with no Loop Clip tooltip mount or state"
  key_links:
    - from: "canonical physical-frame document"
      to: "preview/playback/save/export"
      via: "retained store and resolver consumers, not a main-timeline authoring projection"
      pattern: "resolvePhysicPaintRoto"
    - from: "PhysicsPaintWorkflowStrip"
      to: "PhysicsPaintLoopClipRail"
      via: "one integrated host inside the physical row"
      pattern: "PhysicsPaintLoopClipRail"
  prohibitions:
    - statement: "The removed Motion Editor surface may not return as a read-only summary, rail, badge, label, or navigation adapter."
      status: resolved
      verification: automated
    - statement: "The rejected EFX lane may not remain behind display:none, dead CSS, compatibility exports, or stale positive tests."
      status: resolved
      verification: automated
    - statement: "Canonical physical/resolver/controller/preview/export code must not be deleted as collateral cleanup."
      status: resolved
      verification: automated
---

<objective>
Delete the rejected dedicated-lane/full-capsule residue and every Motion Editor Loop Clip projection or interaction after the approved EFX rail/popover/sidebar replacement is complete, per D-33, D-34R, D-35R, D-36R, D-42, D-46, and D-48.

Purpose: Make the corrected ownership boundary structural rather than cosmetic while preserving generic timeline behavior and canonical physical output consumers.
Output: Removed active Motion Editor projection, public types, rendering, input routing, and tooltip mount, with focused absence/regression contracts; dependent Plan 43-14 deletes the now-unmounted helper and rejected EFX residue.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md
@app/src/lib/frameMap.ts
@app/src/types/timeline.ts
@app/src/components/timeline/TimelineRenderer.ts
@app/src/components/timeline/TimelineInteraction.ts
@app/src/components/timeline/TimelineCanvas.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove main-timeline projection, public types, and renderer residue</name>
  <files>app/src/lib/frameMap.ts, app/src/lib/frameMap.test.ts, app/src/types/timeline.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33 and D-46 ownership/consumer boundary)
    - app/src/lib/frameMap.ts (`loopCapsules`, range derivation, capsule builder)
    - app/src/types/timeline.ts (capsule source/capsule/layout types)
    - app/src/components/timeline/TimelineRenderer.ts (now-disabled draw definition/imports)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (approved EFX replacement facts/geometry owner)
  </read_first>
  <behavior>
    - Sentinel test: `omits Loop Clip projection from the Motion Editor frame map`.
    - The sentinel asserts the public frame-map result has no Loop Clip presentation field while ordinary FX ordering/layout remains unchanged.
  </behavior>
  <action>RED: add exactly one test named `omits Loop Clip projection from the Motion Editor frame map` to `frameMap.test.ts`. Run `pnpm --dir app exec vitest run src/lib/frameMap.test.ts -t "omits Loop Clip projection from the Motion Editor frame map"`. The precise RED failure is that the returned frame-map object still contains the obsolete Loop Clip projection field or data. GREEN: make only the D-33 production removal required by the sentinel and its direct compile consequences: delete the main-editor projection feed/builders, remove the public timeline capsule field/types, and remove renderer imports/draw definitions. Retain canonical physical Loop Clip records and preview/playback/save/export resolver consumers per D-24 through D-32 and D-46; do not add a read-only replacement. Run `pnpm --dir app exec vitest run src/lib/frameMap.test.ts -t "omits Loop Clip projection from the Motion Editor frame map"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run frame-map and renderer regression files together.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/frameMap.test.ts -t "omits Loop Clip projection from the Motion Editor frame map" && pnpm --dir app exec vitest run src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Main timeline data, type, and paint layers expose no Loop Clip presentation.
    - Generic frame-map and renderer behavior remains green.
    - Canonical physical Loop Clip records and resolution are untouched.
  </acceptance_criteria>
  <done>The Motion Editor data and rendering pipeline no longer contains Loop Clip presentation code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove main-timeline hit testing, keyboard routing, tooltip, and geometry</name>
  <files>app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineInteraction.test.ts, app/src/components/timeline/TimelineCanvas.tsx</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33 zero-interaction boundary)
    - app/src/components/timeline/TimelineInteraction.ts (capsule hit, hover/focus, selection, keyboard, operation callers)
    - app/src/components/timeline/TimelineCanvas.tsx (tooltip import/mount)
    - app/src/components/timeline/TimelineCapsuleTooltip.tsx (stale main-editor dependencies/actions)
    - app/src/components/timeline/loopCapsuleGeometry.ts (confirm required compact rail derivation is already owned by EFX presentation tests)
  </read_first>
  <behavior>
    - Sentinel test: `ignores former Loop Clip coordinates and keys in the Motion Editor`.
    - The sentinel sends pointer and Enter/Delete/Backspace input at former coordinates and expects no Loop Clip hover, focus, selection, navigation, edit, unlink, or delete intent.
  </behavior>
  <action>RED: add exactly one test named `ignores former Loop Clip coordinates and keys in the Motion Editor` to `TimelineInteraction.test.ts`. Run `pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts -t "ignores former Loop Clip coordinates and keys in the Motion Editor"`. The precise RED failure is that former coordinates or keys still emit a Loop Clip hover/selection/action intent. GREEN: make only the active interaction cleanup required by D-33: remove every caller, import, mount, hit route, hover/focus publication, keyboard/action route, and other executable reference to the obsolete `TimelineCapsuleTooltip` and `loopCapsuleGeometry` modules from the files owned by this task. Preserve ordinary playhead, layer selection, drag, keyboard, unrelated tooltip/overlay, and playback behavior. The now-unreferenced tooltip/geometry files may remain on disk when 43-13 completes; physical deletion of those dead modules is exclusively Plan 43-14's responsibility. Run `pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts -t "ignores former Loop Clip coordinates and keys in the Motion Editor"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run interaction, renderer, EFX presentation, and popover regression files.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts -t "ignores former Loop Clip coordinates and keys in the Motion Editor" && pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipPopover.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No visible or invisible Motion Editor Loop Clip input/floating-UI surface remains.
    - Obsolete tooltip and geometry modules have no caller, import, mount, hit route, or executable reference; their now-unreferenced files are explicitly allowed to remain on disk at 43-13 completion.
    - Physical deletion of the dead tooltip and geometry modules is exclusively Plan 43-14's responsibility.
    - Generic timeline interaction and EFX-owned rail/popover suites remain green.
  </acceptance_criteria>
  <done>The Motion Editor has zero Loop Clip interaction ownership or executable reference at pointer, keyboard, selection, tooltip, and geometry layers; the unreferenced tooltip/geometry files may remain on disk until Plan 43-14 deletes them.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical physical output → Motion Editor | Main editor may consume resolved pixels but must not acquire Loop Clip authoring UI |
| timeline pointer/keyboard → commands | Removed coordinates and keys must not reach stale Loop Clip operations |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-13-01 | Spoofing | duplicate main-timeline surface | high | mitigate | Remove projection, rendering, tooltip mounts/imports/callers, and interaction ownership together; Plan 43-14 deletes dead module files |
| T-43-13-02 | Elevation of Privilege | invisible hit/keyboard routes | high | mitigate | Behavioral absence tests prove removed coordinates/keys emit no Loop Clip intent |
| T-43-13-03 | Tampering | generic timeline and physical cells | medium | mitigate | Retain focused non-Loop renderer/interaction/cell regressions |
| T-43-13-04 | Information Disclosure | stale tooltip state | low | mitigate | Remove tooltip mount/state and every executable reference; physical component-file deletion remains exclusive to Plan 43-14 |
| T-43-13-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Run both one-test RED/GREEN commands, then their broader focused regressions.
- Confirm active Motion Editor projection, render, pointer, keyboard, selection, action, and tooltip-mount routes are gone while EFX replacement tests remain green.
- Confirm canonical model/resolver/store/controller/preview/export files are unchanged; Plan 43-14 owns deletion of now-unmounted helper and rejected EFX residue.
</verification>

<success_criteria>
- D-33 is structural across projection, type, render, pointer, keyboard, tooltip, selection, and action layers: no caller, import, mount, hit route, or executable reference remains.
- D-34R/D-36R active rejected lane/capsule ownership is gone; now-unreferenced tooltip/geometry files are allowed to remain on disk at 43-13 completion because physical deletion is exclusively Plan 43-14's responsibility.
- D-42/D-46 generic and canonical behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Main timeline frame map, types, renderer, interaction, and canvas with zero active Loop Clip UI
- Explicit cleanup handoff for now-unmounted tooltip/geometry and rejected EFX lane residue in 43-14
- Absence contracts plus preserved generic timeline and EFX-owned regressions
