---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoHoldDeterminism.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - app/src/stores/physicPaintStore.rotoHoldComposite.test.ts
autonomous: true
requirements:
  - HOLD-01
  - HOLD-02
  - HOLD-03
  - HOLD-04

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A progressive script and a static/hold script applied to adjacent destination ranges each materialize their complete stroke set with no frame overlap and no gap (half-open boundaries, HOLD-01)"
    - "A single-stroke script held over N frames produces N frames each containing that one stroke; an empty selection cannot be applied (HOLD-01)"
    - "Identical script, destination, and options produce byte-identical dataUrls across regeneration, save/reopen, and cache regeneration — zero variation returns the input stroke unchanged; nonzero variation is deterministic per frame (HOLD-02)"
    - "Applying the same static/hold generation twice to the same inputs produces identical document state — the atomic commit path makes re-application idempotent (HOLD-02, HOLD-03)"
    - "Cancellation or failure mid-generation leaves zero committed destination frames — no partial range (HOLD-03)"
    - "Generated static/hold keys are committed in deterministic frame order and remain paint content of the opened parent Paint layer, composited as one resolved raster per frame via the canonical store seam (HOLD-03, HOLD-04)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoHoldDeterminism.test.ts"
      provides: "HOLD-02 byte-identical determinism proof across regeneration, zero and nonzero Motion"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts"
      provides: "HOLD-01 complete-stroke-set and adjacent-range cases"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts"
      provides: "HOLD-03 commit-path reuse, cancel/failure atomicity, one Undo/Redo"
    - path: "app/src/stores/physicPaintStore.rotoHoldComposite.test.ts"
      provides: "HOLD-04 one-resolved-raster-per-frame via getRotoPhysicalRenderSource"
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoHoldDeterminism.test.ts"
      to: "packages/efx-physic-paint/src/animation/recordedStrokeMotion.ts"
      via: "transformRecordedStrokeForHeldPose purity proof — same inputs, identical output"
      pattern: "transformRecordedStrokeForHeldPose"
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      via: "confirm() staged commit reuse — no new commit path"
      pattern: "buildPhysicalPublication"
  prohibitions:
    - statement: "MUST NOT introduce random render-time jitter or new RNG for hold frames — the hash-seeded held-pose transform is the only variation source (HOLD-02)"
      status: resolved
      verification: test
    - statement: "MUST NOT modify the regression-locked progressive schedule module — static/hold shipped as an additive sibling in Phase 42 and stays byte-untouched (carried-forward Phase 42 lock)"
      status: resolved
      verification: test
    - statement: "MUST NOT build a second commit path for static/hold — the existing staged atomic commit is reused verbatim (HOLD-03)"
      status: resolved
      verification: test
---

<objective>
Prove the HOLD-01..04 hardening contract with tests: static/hold complete-stroke-set materialization on adjacent ranges (HOLD-01), byte-identical determinism across save/reopen and cache regeneration (HOLD-02), commit-path atomicity with one Undo/Redo (HOLD-03), and one-resolved-raster-per-frame compositing (HOLD-04).

Purpose: RESEARCH confirms the machinery already shipped in Phase 42 (`buildStaticStrokeSchedule`, `transformRecordedStrokeForHeldPose`, staged commit); Phase 43's obligation is regression proof, not new machinery. These are the phase's Wave 0 determinism/hardening specs.
Output: green specs pinning the deterministic static/hold contract; any failure is a real regression surfaced through the bounded deviation protocol below — never asserted away and never fixed under a generic instruction.

**Bounded deviation protocol (audit finding 11 — HARD LIMIT):** this plan is a hardening/test plan. Its declared file list is test files ONLY; it does NOT authorize production edits. If a new or extended spec fails RED against shipped machinery, that is a genuine Phase 42 regression signal. The executor MUST then: (1) STOP — do not edit any engine, controller, renderer, or store source file; (2) record the failing feedback loop (exact spec case, command, output, implicated production file) in this plan's deviation log; (3) request a bounded plan deviation/revision naming the specific production files the fix requires; and (4) resume only after the deviation is approved with the production files explicitly declared. Weakening an assertion to force green is forbidden in every case.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md
@packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts
@packages/efx-physic-paint/src/animation/recordedStrokeMotion.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOLD-01/HOLD-02 specs — complete stroke set, adjacent ranges, byte-identical determinism</name>
  <precondition>Phase 42 static/hold schedule exists: packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts exports buildStaticStrokeSchedule and recordedStrokeMotion.ts exports transformRecordedStrokeForHeldPose.</precondition>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoHoldDeterminism.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts (buildStaticStrokeSchedule, lines 16-29)
    - packages/efx-physic-paint/src/animation/recordedStrokeMotion.ts (transformRecordedStrokeForHeldPose 14-92; FNV seed 50-58; poseNoise 87-92)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts (destinationSourceFrame seeding at 66-71)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts (existing harness, vi.mock pattern)
  </read_first>
  <behavior>
    - buildStaticStrokeSchedule maps every stroke to startFrame 0, endFrame usableFrames - 1 — every destination frame receives the complete stroke set
    - Progressive on frames 10-14 then static/hold on frames 15-19: no frame receives strokes from the other range; no frame is empty of its own script's strokes
    - Same script + destination + options rendered twice produce byte-identical dataUrls — for zero Motion and for nonzero position/deformation Motion
    - Zero variation (deformation 0, position 0) returns the input stroke object unchanged
    - Re-simulating save/reopen (serialize the schedule inputs, deserialize, re-render) yields byte-identical dataUrls
    - A single-stroke script over 3 frames yields 3 frames each containing exactly that stroke
  </behavior>
  <action>
    Create app/src/components/physic-paint/roto/physicsPaintRotoHoldDeterminism.test.ts (harness idiom from physicsPaintRotoPlayScriptController.test.ts: vi.hoisted + valid-PNG dataUrl factory) covering the determinism cases: double-render byte-equality at zero and nonzero Motion, zero-variation identity, serialize/deserialize re-render equality. Extend physicsPaintRotoPlayScriptRenderer.test.ts with the HOLD-01 cases: complete stroke set per destination frame, progressive-then-hold adjacent ranges with no overlap/gap, single-stroke cycle. These are hardening specs against shipped machinery — they are expected to PASS on first run. If any fails RED, apply the plan-level bounded deviation protocol: STOP, record the failing feedback loop and the implicated production file in the deviation log, and request a bounded revision — do NOT edit packages/efx-physic-paint, the renderer, or any other production file under this plan, and never weaken the assertion. Commit test(43-04). A fix(43-04) commit is permitted ONLY after an approved deviation has explicitly declared the production files in scope.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoHoldDeterminism physicsPaintRotoPlayScriptRenderer</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoHoldDeterminism exits 0
    - pnpm --dir app exec vitest run physicsPaintRotoPlayScriptRenderer exits 0 including the new adjacent-range case
    - The determinism spec asserts byte-identical dataUrl strings (exact equality, not similarity) for both zero and nonzero Motion
    - packages/efx-physic-paint/src/animation/ unchanged (git diff --stat packages/efx-physic-paint shows no lines) unless an approved bounded deviation explicitly declared a production fix — in which case the deviation record and the fix commit explain the regression
  </acceptance_criteria>
  <done>HOLD-01 complete-stroke-set and HOLD-02 determinism are pinned by green specs; any discovered regression is surfaced through the bounded deviation protocol with declared production files — never asserted away and never fixed under a generic instruction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HOLD-03/HOLD-04 specs — commit-path atomicity, one Undo/Redo, one raster per frame</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts, app/src/stores/physicPaintStore.rotoHoldComposite.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (confirm() + buildPhysicalPublication, lines 220-438; cancellation path)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (existing harness)
    - app/src/stores/physicPaintStore.ts (getRotoPhysicalRenderSource 1405-1453)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts (snapshot Undo/Redo 83-92)
  </read_first>
  <behavior>
    - Cancelling a static/hold generation mid-stage commits zero destination keys — the document is byte-identical to before the attempt
    - A renderer failure mid-generation commits zero destination keys and surfaces the inline error — no partial range
    - A completed static/hold generation is exactly one history command: one Undo removes every generated key, one Redo restores them
    - Generated keys resolve through getRotoPhysicalRenderSource as one rendered raster per frame, keyed to the parent Paint layer
    - Re-applying the same generation after Undo produces the same committed state (idempotent re-application)
  </behavior>
  <action>
    Extend physicsPaintRotoPlayScriptController.test.ts with the HOLD-03 cases: mid-stage cancellation commits nothing (drive the controller's cancellation hook between staged frames), renderer failure commits nothing and surfaces the error, and the accepted generation is one history command with one-Undo/one-Redo semantics. Create app/src/stores/physicPaintStore.rotoHoldComposite.test.ts for HOLD-04: after a committed static/hold generation on a parent Paint layer, every destination frame resolves via getRotoPhysicalRenderSource to exactly one rendered raster owned by that layer. Same bounded deviation protocol as Task 1: these pin shipped machinery; a RED result is a real regression — STOP, record the failing feedback loop, request a bounded revision declaring the production files; never edit production source under this plan and never weaken the assertion. Commit test(43-04).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoPlayScriptController physicPaintStore.rotoHoldComposite</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoPlayScriptController exits 0 with the new cancellation/failure/undo cases
    - pnpm --dir app exec vitest run physicPaintStore.rotoHoldComposite exits 0
    - The cancellation case asserts zero committed keys after cancel (document byte-equality before/after)
    - pnpm --dir app exec vitest run exits 0 (full suite green)
  </acceptance_criteria>
  <done>HOLD-03 atomicity and HOLD-04 single-raster compositing are pinned green; no second commit path exists anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → engine package | Specs exercise the deterministic transform with adversarial inputs (zero/huge variation, empty stroke lists) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-04-01 | DoS | adversarial Motion percent inputs | low | mitigate | clampPercent already bounds inputs; spec includes extreme-value cases proving no hang/NaN |
| T-43-04-02 | Tampering | weakened determinism assertions | medium | mitigate | Byte-exact equality mandated in acceptance criteria; similarity-based assertions rejected at review |
| T-43-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicsPaintRotoHoldDeterminism physicsPaintRotoPlayScriptRenderer physicsPaintRotoPlayScriptController physicPaintStore.rotoHoldComposite — exit 0
- Full suite green
</verification>

<success_criteria>
- HOLD-01..04 each have at least one green automated proof
- No production code changed under this plan; any regression fix required an approved bounded deviation with explicitly declared production files
- Wave 0 determinism/hardening obligations from VALIDATION.md are satisfied
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `physicsPaintRotoHoldDeterminism.test.ts` (new spec — HOLD-02)
- HOLD-01 adjacent-range + complete-stroke-set cases in `physicsPaintRotoPlayScriptRenderer.test.ts`
- HOLD-03 cancel/failure/undo cases in `physicsPaintRotoPlayScriptController.test.ts`
- `physicPaintStore.rotoHoldComposite.test.ts` (new spec — HOLD-04)
