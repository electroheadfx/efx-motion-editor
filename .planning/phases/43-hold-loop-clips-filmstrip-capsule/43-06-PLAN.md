---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 06
type: execute
wave: 4
depends_on:
  - 43-05
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/types/physicPaint.ts
autonomous: true
requirements:
  - HOLD-05

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Loop-edit mode exposes ONLY Repeat + Infinity plus the Requested/Effective readout; Frames-per-cycle and all source fields render disabled at reduced opacity with values preserved; primary action is `Update loop`; secondary is `Edit source cycle…` (D-01)"
    - "The loop-edit Requested/Effective readout comes from the canonical 43-02 derivation, never a controller-local boundary computation (Pitfall 4)"
    - "Source-edit mode prefills mode, Frames per cycle, color, and Motion from the source cycle; shows `Confirming regenerates the source cycle and updates every linked Loop Clip referencing it.` plus `This source cycle is shared by {N} loops.` only when N > 1; confirmation button is `Regenerate source cycle` (D-02)"
    - "Regeneration reuses the existing staged atomic commit verbatim — capacity, authority, cancellation, Undo/Redo; Cancel changes nothing; after success every linked loop re-resolves requested/effective duration and truncation (D-02)"
    - "`Update loop`, `Unlink loop`, and `Duplicate linked loop` are each one atomic undoable operation through the existing commit path (D-10, D-05, D-03)"
    - "Repeat validation reuses the inherited positive-integer + safe-product rules: malformed/zero/negative/fractional/unsafe values disable `Update loop`; Infinity disables Repeat without clearing its last valid finite value and restoring re-validates against the fixed cycle length; the readout never shows NaN or overflow products (S2 Repeat validation)"
    - "The apply-time `Link to existing cycle` / `Create new cycle` choice appears only when an identical source cycle exists, matched on (scriptId, mode, cycleLength, motion, overrideColor) plus canonical start when Motion is nonzero (D-05; Q2 resolved)"
    - "Both dialog modes open prefilled from the canonical record — never blank (S2/S3 first-open)"
    - "Phase 42 compact-fit and generation-lifecycle contracts hold unchanged in both modes: all rows visible at minimum window size, inputs disabled during regeneration, `Cancel generation` replaces `Cancel`, failure shows the inline error and leaves no partial state (S2/S3 overflow, S3 busy)"
    - "A badge click with the Studio window closed launches the Studio and then opens the dialog in loop-edit mode targeting that loop; with the Studio open it focuses and opens loop-edit mode (D-01; Q3 resolved: launch-or-focus)"
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      provides: "loop-edit and source-edit modes, loop ops (update/unlink/duplicate), Link/Create matching, resolver-driven readout"
      contains: "loopEdit"
    - path: "app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx"
      provides: "S2/S3/S4 dialog surfaces with locked copy"
      contains: "Edit Loop Clip"
    - path: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts"
      provides: "parent→child open-loop-edit message sender"
      contains: "loop-edit"
  key_links:
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      via: "loop-edit readout consumes the shared derivation (Pitfall 4 closure)"
      pattern: "derivePhysicPaintRotoLoop"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts"
      via: "launch-or-focus then deliver open-loop-edit message with target loopId"
      pattern: "loopId"
  prohibitions:
    - statement: "MUST NOT build a second commit path for regeneration or loop ops — everything rides the Phase 42 staged atomic commit (HOLD-03, D-02)"
      status: resolved
      verification: test
    - statement: "MUST NOT ship any non-English capsule/dialog copy; the superseded French truncation label never ships; the prohibited French term for a blocking clip never appears in any language (D-20)"
      status: resolved
      verification: test
    - statement: "MUST NOT offer Link to an existing cycle whose held poses would differ at the new start — Motion-nonzero matching includes canonical start (D-05, Q2)"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce React-style effect-dependency control flow — all dialog/loop state is Preact controller signals (CLAUDE.md)"
      status: resolved
      verification: judgment
---

<objective>
Deliver the Play Script dialog's loop-edit (S2) and source-edit (S3) modes, the apply-time Link/Create choice (S4), the loop operations (Update/Unlink/Duplicate) as atomic undoable commits, and the parent→child bridge message that opens loop-edit mode from the main-timeline badge (launch-or-focus when the Studio is closed).

Purpose: D-01/D-02/D-05 are the user's locked editing flow; Q2 (matching key) and Q3 (Studio-closed behavior) are resolved here per RESEARCH recommendations; Pitfall 4 closes by routing the readout through the 43-02 canonical derivation.
Output: badge click reopens the dialog in loop-edit mode; `Update loop` and `Regenerate source cycle` work through the existing commit machinery with one-Undo semantics.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-05-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: controller loop modes + loop ops + Link/Create matching (Q2)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (signals 68-80; loopReadout 144-158; confirm() 220-438; parseRepeat 340-353; ports 1-66)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (shared derivation from 43-02)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts (destinationSourceFrame seeding 66-71 — Pitfall 6)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-01, D-02, D-03, D-05, D-10)
  </read_first>
  <behavior>
    - openLoopEdit(loopId) exposes: target loop's repeat/infinity state, Requested/Effective readout from the shared derivation, locked source fields
    - updateLoop(newRepeat | 'infinity') commits one atomic operation through the existing commit path; one Undo restores the prior repeat
    - unlinkLoop(loopId) removes only the loop record; source keys remain ordinary Roto keys; one Undo restores the loop
    - duplicateLinkedLoop(loopId, destinationStart) validates same-start collision and D-14 overlap, creates a loop sharing sourceKeyIds with NO regeneration; one Undo removes only the duplicate
    - Source-edit confirm regenerates via the existing staged commit; after success every linked loop re-resolves; the affected-loop count N is exposed for the S3 notice
    - findIdenticalSourceCycle matches on (scriptId, mode, cycleLength, motion settings, overrideEnabled/overrideColor) and includes canonicalStart when Motion is nonzero; with Motion 0/0 the start is excluded (held-pose transform is identity)
    - Repeat input validation: malformed/zero/negative/fractional/unsafe-product values keep `Update loop` disabled; Infinity toggling preserves and restores the last valid finite value
  </behavior>
  <action>
    RED: extend physicsPaintRotoPlayScriptController.test.ts with all behavior cases (loop-edit open state, update/unlink/duplicate atomicity via the ports mock, matching-key cases including Motion-nonzero canonical-start sensitivity and Motion-zero start-insensitivity, Repeat validation matrix, affected-loop count); commit test(43-06). GREEN in physicsPaintRotoPlayScriptController.ts: add controller signals and methods for loop-edit and source-edit modes (openLoopEdit(loopId), openSourceEdit(loopId), updateLoop, unlinkLoop, duplicateLinkedLoop, findIdenticalSourceCycle); route the loop-edit Requested/Effective readout through the 43-02 shared derivation export and retire the local boundary math from loopReadout for loop mode (Pitfall 4); regeneration confirms through the existing confirm() → staged render → buildPhysicalPublication verbatim; every loop op builds its publication through the same commit port so 43-01's snapshot history gives one-Undo semantics for free. All state lives on controller signals — no useState/useEffect control flow (CLAUDE.md). Commit feat(43-06).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintRotoPlayScriptController</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run physicsPaintRotoPlayScriptController exits 0 with all new loop-mode cases
    - The Motion-nonzero matching case asserts two cycles with identical script+options but different starts do NOT match; the Motion-zero case asserts they DO
    - Each of updateLoop/unlinkLoop/duplicateLinkedLoop asserts a single commit-port call (one atomic operation)
  </acceptance_criteria>
  <done>Controller owns loop-edit/source-edit state, atomic loop ops, and Link/Create matching per Q2; readout is resolver-derived; specs green.</done>
</task>

<task type="auto">
  <name>Task 2: dialog S2/S3/S4 surfaces with locked copy and Phase 42 inheritance</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (Phase 42 structure 37-116; PLAY_SCRIPT_MODES 16-26; focus discipline 48-52; early-exit 106)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (Copywriting Contract, Interaction Contract, S2/S3/S4 rows)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (inherited --ps-* tokens, compact fit, focus trap)
  </read_first>
  <action>
    Extend PhysicsPaintPlayScriptDialog.tsx with the two new modes driven by the controller signals from Task 1 — no new dialog component, no new token set. Loop-edit mode (S2): title `Edit Loop Clip` with range readout `F{start} · Cycle {N}f` at right; ONLY Repeat + Infinity editable; Frames-per-cycle and all source fields rendered disabled at reduced opacity with values preserved; Requested/Effective readout from controller signals; primary `Update loop` (accent primary, disabled while Repeat invalid); secondary `Edit source cycle…`. Source-edit mode (S3): title `Edit Source Cycle`; full dialog prefilled from the source cycle; notice `Confirming regenerates the source cycle and updates every linked Loop Clip referencing it.` and, only when shared, `This source cycle is shared by {N} loops.`; confirmation `Regenerate source cycle`; generation lifecycle identical to Phase 42 (disabled inputs, `Cancel generation`, footer progress, `--ps-error` inline on failure). Apply-time choice (S4): segmented control reusing the `.physics-paint-play-script-mode-group` pattern with options `Link to existing cycle` / `Create new cycle`, shown only when the controller reports an identical source cycle; Link helper `Reuses the existing source cycle. Future source edits update every linked loop.` rendered with the existing source range and already-linked loop count; Create helper `Creates an independent source cycle. Future edits do not affect the existing loops.` English only. Keep compact-fit: all rows visible at minimum window size, no scrollbars. Focus discipline per Phase 42 (focus input on open, returnFocusRef on close, focus trap, Escape/Enter conventions).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run PhysicsPaintPlayScriptDialog physicsPaintRotoPlayScriptController</automated>
  </verify>
  <acceptance_criteria>
    - Dialog specs exit 0 (extend or add colocated spec asserting mode rendering, locked copy strings, disabled-source-fields state)
    - The strings `Edit Loop Clip`, `Edit Source Cycle`, `Update loop`, `Edit source cycle…`, `Regenerate source cycle`, `Link to existing cycle`, `Create new cycle` appear verbatim in the dialog source
    - grep -c "clip bloquant" app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx == 0 (the D-20 prohibited term is absent)
    - No new CSS color tokens introduced (only the Phase 42 --ps-* set is referenced)
  </acceptance_criteria>
  <done>S2/S3/S4 render with locked verbatim copy, locked fields disabled with preserved values, compact-fit and generation lifecycle inherited; automated specs green; visual polish verified in 43-10 native UAT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: parent→child open-loop-edit bridge message with launch-or-focus (Q3)</name>
  <files>app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts, app/src/lib/physicPaintBridge.ts, app/src/types/physicPaint.ts</files>
  <read_first>
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts (sender idiom 57-69: Tauri emitTo + Browser-fallback postMessage)
    - app/src/lib/physicPaintBridge.ts (event constants; openPhysicPaintCanvas 1304-1342; tryOpenTauriPhysicPaintWindow 1371)
    - app/src/types/physicPaint.ts (typed payload + isX guard precedent, e.g. isPhysicPaintThumbnailEncodeResult)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-RESEARCH.md (Pitfall 5, Open Question 3)
  </read_first>
  <behavior>
    - sendOpenLoopEdit(loopId) emits the new parent→child event via Tauri emitTo in Tauri mode and window postMessage in Browser-fallback mode, mirroring the existing sender idiom
    - The child receives the message and opens the Play Script dialog in loop-edit mode targeting that loopId
    - When the Studio window is closed, the flow launches it via the existing openPhysicPaintCanvas path first, then delivers the loop-edit message after launch (launch-or-focus; never silently drops the click)
    - A malformed message payload (missing/invalid loopId) is rejected by the typed guard and ignored
  </behavior>
  <action>
    Add a new event constant (e.g. PHYSIC_PAINT_OPEN_LOOP_EDIT_EVENT) beside the existing ones in physicPaintBridge.ts; add the typed request payload { loopId: string } and an isPhysicPaintOpenLoopEditRequest guard in types/physicPaint.ts following the existing guard precedent; add sendPhysicPaintOpenLoopEdit to physicsPaintBridgeTransport.ts mirroring the sendPhysicPaintRotoAuthorityRequest idiom (Tauri emitTo, Browser-fallback postMessage, throw when unavailable). Wire the parent-side entry point: badge click (consumed by 43-08) calls a bridge helper that focuses the existing Studio window or launches it via openPhysicPaintCanvas when closed, then delivers the loop-edit message (queue-until-ready if the child is still initializing). Wire the child-side listener to call the controller's openLoopEdit(loopId). Add spec coverage: sender emits with correct event name and payload in both bridge modes; guard rejects malformed payloads; child listener routes to openLoopEdit. Commit feat(43-06).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run physicsPaintBridgeTransport physicPaintBridge</automated>
  </verify>
  <acceptance_criteria>
    - Bridge specs exit 0 with the new sender/guard/listener cases
    - The launch-or-focus helper calls openPhysicPaintCanvas only when no Studio window handle exists (assert via mock)
    - grep -n "PHYSIC_PAINT_OPEN_LOOP_EDIT" across the three files shows the constant, the sender, and the listener wiring
  </acceptance_criteria>
  <done>Badge click reaches loop-edit mode whether the Studio is open or closed; message is typed, guarded, and covered by specs; live window behavior verified in 43-10 native UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent window → child window | New parent→child bridge message carries a loopId; child must not trust payload shape |
| dialog input → commit path | Repeat/Infinity user input flows into an atomic commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-06-01 | Tampering | open-loop-edit message payload | medium | mitigate | Typed payload + isPhysicPaintOpenLoopEditRequest guard; malformed messages rejected and ignored; loopId validated against the document before opening |
| T-43-06-02 | DoS | Repeat input | medium | mitigate | parseRepeat safe-product bound (cycleLength × repeat ≤ Number.MAX_SAFE_INTEGER) reused verbatim; Update loop disabled while invalid |
| T-43-06-03 | Tampering | regeneration commit | high | mitigate | Reuses the staged atomic commit with authority/revision checks (43-01 fingerprint covers loopClips); no new commit path |
| T-43-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run physicsPaintRotoPlayScriptController PhysicsPaintPlayScriptDialog physicsPaintBridgeTransport physicPaintBridge — exit 0
- Full suite green
</verification>

<success_criteria>
- Loop-edit, source-edit, and Link/Create modes work with locked copy and one-Undo atomicity
- Readout is resolver-derived everywhere (Pitfall 4 closed)
- Badge click opens loop-edit mode with Studio open or closed
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- Controller: `openLoopEdit(loopId)`, `openSourceEdit(loopId)`, `updateLoop`, `unlinkLoop`, `duplicateLinkedLoop(loopId, destinationStart)`, `findIdenticalSourceCycle` + loop-mode signals (physicsPaintRotoPlayScriptController.ts)
- Dialog modes S2/S3/S4 (PhysicsPaintPlayScriptDialog.tsx)
- `PHYSIC_PAINT_OPEN_LOOP_EDIT_EVENT` constant (physicPaintBridge.ts)
- `sendPhysicPaintOpenLoopEdit` sender (physicsPaintBridgeTransport.ts)
- `isPhysicPaintOpenLoopEditRequest` guard (types/physicPaint.ts)
- Parent-side launch-or-focus helper (physicPaintBridge.ts)
