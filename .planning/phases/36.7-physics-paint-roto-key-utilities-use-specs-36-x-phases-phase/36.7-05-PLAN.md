---
phase: 36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase
plan: 05
type: execute
wave: 5
depends_on:
  - 36.7-04
files_modified:
  - app/src/components/physic-paint/physicsPaintRotoKeyController.ts
  - app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
autonomous: false
gap_closure: true
requirements:
  - 36.7-DUP
  - 36.7-INS
  - 36.7-DEL
  - 36.7-COPY-PASTE
  - 36.7-STUDIO
  - 36.7-UI
must_haves:
  truths:
    - "After Insert, the inserted target is a clean real Roto key whose canvas opens without cached/generated/reference/onion pixels blended over it (D-02, D-04, D-10)."
    - "After Delete, the removed real key and all stale generated/reference cache entries are absent from parent persistence, launch context cache, timeline cells, and canvas restore paths when navigating back (D-03, D-04, D-10)."
    - "After Paste, empty/generated/real targets become coherent clean real-key cache truth without treating generated frames as editable sources (D-05, D-06, D-10)."
    - "Duplicate behavior remains preserved while sharing the same cache transaction controller as Insert/Delete/Paste (D-01, D-04, D-10)."
    - "Dirty source actions save first and cancel without cache mutation on save failure; dirty in-memory canvas remains visible (D-08, D-09)."
    - "The existing contextual Roto key UI remains visually unchanged; repair is state/cache coherence only (D-11, D-12, D-13, D-14, D-15)."
  artifacts:
    - path: app/src/components/physic-paint/physicsPaintRotoKeyController.ts
      provides: "Small tested controller for Roto key utility cache transactions and active-frame restore intent"
      exports:
        - buildRotoKeyUtilityTransaction
        - applyRotoKeyUtilityTransactionToLocalState
        - type RotoKeyUtilityOperation
        - type RotoKeyUtilityTransaction
    - path: app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
      provides: "Regression proof for Insert/Delete/Paste cache, generated/reference cleanup, and active canvas restore intent"
    - path: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      provides: "Studio integration delegating key utility cache coordination to the controller while preserving existing UI"
  key_links:
    - from: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      to: app/src/components/physic-paint/physicsPaintRotoKeyController.ts
      via: "Studio builds one transaction for Duplicate/Insert/Delete/Paste and applies the returned cache frames plus restore intent"
      pattern: "buildRotoKeyUtilityTransaction|applyRotoKeyUtilityTransactionToLocalState"
    - from: app/src/components/physic-paint/physicsPaintRotoKeyController.ts
      to: app/src/stores/physicPaintStore.ts
      via: "Transaction frames are persisted through existing replace-roto-key-frames bridge/store path"
      pattern: "replace-roto-key-frames|getRotoCacheFrames"
    - from: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      to: app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
      via: "Existing callbacks, copied-key state, and keyActionInFlight props stay unchanged visually"
      pattern: "onInsertRotoFrame|onDeleteRotoFrame|onPasteRotoFrame|keyActionInFlight"
---

<objective>
Close the Phase 36.7 failed-UAT gap by repairing the Roto key utility state model so Insert, Delete, and Paste keep active frame, real-key cache, generated/render-only frames, background/reference overlays, canvas contents, and parent persistence coherent.

Purpose: Live UAT proved automated helper/source-contract tests were insufficient: Insert/Delete/Paste can leave stale yellow/translucent reference or generated cache paths visible even when timeline keys should be clean. This plan requires a failing regression proof first, then a small controller extraction so Studio no longer coordinates key utility cache truth through scattered ad-hoc mutations.
Output: A tested `physicsPaintRotoKeyController.ts` controller and narrow Studio integration that preserve the existing UI while making key utility transactions coherent end-to-end.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** duplicate, insert, delete, copy, and paste real Roto keys, **so that** I can build frame-by-frame animation efficiently.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-CONTEXT.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-RESEARCH.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-PATTERNS.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-UI-SPEC.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-VALIDATION.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-01-SUMMARY.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-02-SUMMARY.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-03-SUMMARY.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-04-SUMMARY.md
@SPECS/36.x-phases/phase-36.7-key-utilities/spec-36.7-key-utilities.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@app/src/stores/physicPaintStore.ts
@app/src/lib/physicPaintBridge.ts
@app/src/types/physicPaint.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prove Insert/Delete/Paste cache and canvas coherence failures</name>
  <files>app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    @app/src/components/physic-paint/physicsPaintWorkflowState.ts
    @app/src/stores/physicPaintStore.ts
    @app/src/lib/physicPaintBridge.ts
    @app/src/types/physicPaint.ts
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-CONTEXT.md
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-04-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: Insert on real keys `[2, 5]` at frame `5` returns real cache frames `[2, 5, 6]`, creates a blank real-key payload at frame `5`, shifts the old frame `5` payload to `6`, removes generated/cache-reference entries not present in the new real-key transaction, and returns active restore intent `blank-real-key` for frame `5` (D-02, D-04, D-10).
    - Test 2: Delete on real keys `[2, 5, 6]` at frame `5` returns real cache frames `[2, 5]`, moves old frame `6` payload to `5`, removes the deleted payload, removes generated/render-only entries for the affected layer, and returns active restore intent `load-real-key` when the current frame still has a shifted real key or `clear-blank` when it does not (D-03, D-04, D-07, D-10).
    - Test 3: Paste after copying a real key writes target frame cache truth with `source: 'real-key'`, clears any generated/render-only metadata for that target, and returns active restore intent `load-real-key` without requiring the target to be a source real key (D-05, D-06, D-10).
    - Test 4: Duplicate still creates next-frame real-key cache truth and shifts later real keys through the same transaction builder so the existing working behavior is preserved (D-01, D-04, D-10).
    - Test 5: Source-contract proof in `PhysicsPaintWorkflowStrip.test.ts` fails until Studio delegates Duplicate/Insert/Delete/Paste cache layout and active-frame restore decisions to `physicsPaintRotoKeyController.ts` instead of scattering those decisions in separate Studio callbacks.
  </behavior>
  <action>Create `physicsPaintRotoKeyController.test.ts` under the existing Vitest setup and add RED tests for the exact UAT failures: inserted blank real key must not inherit cached/reference/generated pixels; deleted real key must not reappear after navigation; pasted generated targets must become clean real-key cache truth; duplicate must remain intact. Use small in-memory `PhysicPaintRotoCacheFrame` fixtures with distinctive `dataUrl` values such as `data:image/png;base64,real-two`, `real-five`, `generated-five`, and `yellow-reference` so assertions can prove stale generated/reference payloads are absent from returned transaction frames. Extend `PhysicsPaintWorkflowStrip.test.ts` only with source-contract assertions that Studio imports and calls the controller; do not add a one-off test config, do not run a dev server, and do not change the existing UI component in this RED task.</action>
  <acceptance_criteria>
    - RED test run fails because `physicsPaintRotoKeyController.ts`, `buildRotoKeyUtilityTransaction`, and Studio delegation do not exist yet.
    - Tests cite D-01 through D-10 in names or comments and directly cover the user-reported Insert/Delete/Paste stale yellow/reference/cache failure modes.
    - Test fixtures prove generated/render-only and cached-reference payloads are excluded from the returned real-key transaction after Insert/Delete/Paste.
    - No production files are edited in this task.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoKeyController.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Failing regression proof exists before implementation and describes the coherent state model needed to close the UAT gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extract and integrate a Roto key utility transaction controller</name>
  <files>app/src/components/physic-paint/physicsPaintRotoKeyController.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
    @app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    @app/src/components/physic-paint/physicsPaintWorkflowState.ts
    @app/src/stores/physicPaintStore.ts
    @app/src/lib/physicPaintBridge.ts
    @app/src/types/physicPaint.ts
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-RESEARCH.md
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-PATTERNS.md
  </read_first>
  <behavior>
    - The controller builds one authoritative transaction for Duplicate/Insert/Delete/Paste containing `realKeyFrames`, `realKeyFrameNumbers`, `removedFrames`, `changedFrames`, `activeFrame`, `activeRestore`, and `successMessage`.
    - Insert uses `buildBlankRotoFrame(width, height, targetFrame)` supplied by Studio, marks it `source: 'real-key'`, clears editable state for the inserted blank, clears cached reference/background image for the active inserted frame, and suppresses onion/reference overlay until the user paints or navigates (D-02, D-10).
    - Delete removes stale local editable/preview state for the deleted frame, shifts later real-key local state in the same order as cache frames, clears generated/render-only cache from local launch context, and resets/loads the active canvas according to the controller's `activeRestore` result (D-03, D-04, D-07, D-10).
    - Paste upserts copied real-key payload at empty/generated/real targets as `source: 'real-key'`, removes generated target metadata, restores the pasted real key into the active canvas/reference path, and does not call the source-only real-key guard for the target (D-05, D-06, D-10).
    - Dirty Duplicate/Insert/Copy/Delete still use the existing `flushRotoFrame` save-before-action path and cancel without running the controller transaction when save fails (D-08, D-09).
  </behavior>
  <action>Create `physicsPaintRotoKeyController.ts` as a pure controller next to `physicsPaintWorkflowState.ts`. Export `RotoKeyUtilityOperation`, `RotoKeyUtilityTransaction`, `buildRotoKeyUtilityTransaction`, and `applyRotoKeyUtilityTransactionToLocalState` or equivalently named narrow symbols that satisfy the RED tests. Keep the controller free of Preact imports, engine imports, DOM access, and store singleton access; it receives current real-key cache frames, generated cache frames, copied key payload, current frame, canvas size/blank-frame factory, and local editable/preview maps as data arguments and returns deterministic transaction data.

In `PhysicsPaintStudio.tsx`, replace the separate Duplicate/Insert/Delete/Paste cache shifting branches with calls to the controller, then persist exactly `transaction.realKeyFrames` through the existing `replace-roto-key-frames` bridge/store path. Update `syncRotoKeyFrameLists`/launch context from `physicPaintStore.getRotoCacheFrames(layerId)` after persistence and ensure generated/render-only cache entries are not preserved from stale `launchContext.cachedRotoFrames` when the transaction removes or regenerates them. Use the controller's active restore intent to call the existing engine operations in one place: reset background, clear canvas for blank/empty, load cached reference only when the transaction says a real key remains at the active frame, clear `cachedRotoReferenceUrl` for blank/deleted-empty outcomes, and keep `setSuppressRotoOnionOverlay(true)` for inserted blank and deleted current-frame cleanup. Preserve existing `runRotoKeyAction`, `runRotoKeyCopyAction`, status copy, `keyActionInFlight`, and contextual pill props; do not add more visual UI, shortcuts, modals, toasts, interpolation controls, cached playback UI, or broad Studio effects. Do not introduce Signals unless the implementation moves key utility state outside Studio; if state remains local, keep existing Preact hooks/refs and document that locality in the SUMMARY.</action>
  <acceptance_criteria>
    - `physicsPaintRotoKeyController.test.ts` passes and proves Insert/Delete/Paste remove stale generated/reference payloads from transaction output.
    - `PhysicsPaintWorkflowStrip.test.ts` passes and proves Studio imports/uses the controller for key utility transactions, while the visible workflow strip markup/CSS remain unchanged by this repair plan.
    - Duplicate still uses the same success copy and next-frame splice behavior from the passed UAT portion.
    - Inserted blank real keys produce a blank real-key cache payload and active canvas restore intent that clears reference/background/onion overlays at the inserted frame.
    - Deleted real keys do not remain in local editable maps, local preview maps, launch context cached frames, or parent persisted real-key frames after the transaction.
    - Paste to a generated target replaces generated/render-only metadata with real-key cache truth and keeps Paste target eligibility separate from source eligibility.
    - Dirty save-before-action failure exits before transaction application and leaves dirty frame state visible/in memory.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoKeyController.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts && pnpm --dir app exec tsc --noEmit</automated>
  </verify>
  <done>The GREEN implementation centralizes key utility cache coordination in the controller and removes the UAT-observed stale canvas/cache states without changing the UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify repaired key utility coherence on the live app</name>
  <files>app/src/components/physic-paint/physicsPaintRotoKeyController.ts, app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/physicsPaintRotoKeyController.ts
    @app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-UI-SPEC.md
    @SPECS/36.x-phases/phase-36.7-key-utilities/spec-36.7-key-utilities.md
  </read_first>
  <what-built>A controller-backed Roto key utility state/cache transaction path for Duplicate, Insert, Delete, and Paste, integrated into Studio while keeping the existing contextual UI unchanged.</what-built>
  <action>After automated checks are green, stop and request user-owned live UAT. The executor must not run the dev server. Ask the user to verify the exact previously failing flows first, then record approval or the exact failing step in `36.7-05-SUMMARY.md`. Do not mark Phase 36.7 complete from this checkpoint unless the user explicitly approves the live behavior.</action>
  <how-to-verify>
    1. Agent runs automated checks only; the user uses their already-running app or starts it themselves.
    2. User opens Physics Paint in Roto mode on a layer with at least two real keys and at least one generated/render-only or empty target.
    3. Insert before a selected real key. Expected: the inserted frame is a clean real key; the canvas is blank/clean for that key and does not show yellow/translucent blended cached/reference pixels; the timeline cell is a clean real/cached Roto key state.
    4. Navigate away, return to the inserted frame, and paint on it. Expected: it remains a normal clean editable real key and does not restore stale generated/reference/background overlay content.
    5. Delete a real key with later real keys present. Expected: later real keys shift backward, deleted frame does not reappear as non-empty/yellow/translucent after navigating away and back, and cells reflect the new clean real-key layout.
    6. Delete the last remaining real key if available. Expected: the layer can return to no real Roto keys; returning to that frame shows no stale deleted canvas/cache.
    7. Copy a real key and Paste onto an empty frame, then onto a generated/render-only target if available. Expected: each target becomes a clean real key, generated target no longer behaves as render-only for that frame, and no stale blended overlay remains.
    8. Duplicate a real key. Expected: Duplicate still behaves at least as well as before, using next-frame splice and clean shifted cache/cell state.
    9. Dirty a current real key, then trigger Insert/Delete/Copy/Duplicate. Expected: save-before-action feedback appears, all key tools disable while saving/actioning, and a save failure cancels the action without mutating cache or losing the dirty visible canvas.
    10. Confirm the UI is unchanged: same contextual pill, same labels, no new controls, no new shortcuts, no new modal/toast/tutorial/interpolation/cached playback/Play-Roto conversion UI.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the Insert first-use and return/paint flows no longer show strange yellow/translucent blended canvas state.
    - User confirms Delete does not allow deleted empty frames to reappear as non-empty stale cache when navigating back.
    - User confirms clean real/cached Roto key cells remain visually coherent after Insert/Delete/Paste.
    - User confirms Duplicate remains preserved and the UI did not change.
    - Automated focused tests and typecheck pass before requesting UAT.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoKeyController.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts && pnpm --dir app exec tsc --noEmit</automated>
    <human-check>User confirms the numbered UAT steps pass in the running app, or reports exact failing step(s) and observed canvas/timeline state.</human-check>
  </verify>
  <resume-signal>Type "approved" if UAT passes, or describe the exact failing step and observed state.</resume-signal>
  <done>Live UAT confirms the Roto key utility state/cache model is coherent after Insert/Delete/Paste without UI changes.</done>
</task>

</tasks>

## Artifacts this phase produces

- `app/src/components/physic-paint/physicsPaintRotoKeyController.ts` — new pure controller for key utility transactions.
- `RotoKeyUtilityOperation` — operation type for `duplicate`, `insert`, `delete`, and `paste` transaction inputs.
- `RotoKeyUtilityTransaction` — transaction output containing real-key frames, changed/removed frames, success message, and active-frame restore intent.
- `buildRotoKeyUtilityTransaction(...)` — new exported transaction builder for cache/metadata layout after Duplicate/Insert/Delete/Paste.
- `applyRotoKeyUtilityTransactionToLocalState(...)` — new exported helper, if needed, for deterministic local editable/preview map updates.
- `app/src/components/physic-paint/physicsPaintRotoKeyController.test.ts` — new regression suite proving Insert/Delete/Paste/Duplicate cache and canvas restore intent.
- Narrow `PhysicsPaintStudio.tsx` integration that delegates Roto key utility cache coordination to the controller and keeps the existing UI unchanged.
- Source-contract assertions in `PhysicsPaintWorkflowStrip.test.ts` proving controller delegation and no UI scope expansion.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | As a stop-motion animator, duplicate, insert, delete, copy, and paste real Roto keys efficiently | 05 | COVERED | Gap plan repairs failed live coherence for the already-built vertical slice.
REQ | 36.7-DUP | Duplicate creates expected new real key | 05 | COVERED | Regression preserves D-01 next-frame splice through shared controller.
REQ | 36.7-INS | Insert creates expected real key | 05 | COVERED | Regression repairs D-02 inserted blank real-key cleanliness and stale reference cleanup.
REQ | 36.7-DEL | Delete removes selected real key and updates visible state | 05 | COVERED | Regression repairs D-03/D-07 deletion, shift, and no-reappear behavior.
REQ | 36.7-COPY-PASTE | Copy then paste reproduces copied key at target frame | 05 | COVERED | Regression repairs D-06 paste target cache truth and generated target cleanup.
REQ | 36.7-STUDIO | Cache metadata, preview, and cell state update consistently | 05 | COVERED | Controller centralizes Studio cache/local state/restore coordination.
REQ | 36.7-UI | Minimal contextual visible controls and state feedback | 05 | COVERED | UI remains unchanged; checkpoint verifies no visual scope expansion.
RESEARCH | — | Use existing Preact/CSS workflow strip, no new packages | 05 | COVERED | No packages, no UI system changes, no server run.
RESEARCH | — | Separate paste target eligibility from source eligibility | 05 | COVERED | Paste regression explicitly targets empty/generated/real frames without source guard.
RESEARCH | — | Dirty save-before-action wrapper | 05 | COVERED | Existing `runRotoKeyAction`/`flushRotoFrame` remains required before controller mutation.
RESEARCH | — | Store shifts must use authoritative real-key cache APIs and generated cache refresh | 05 | COVERED | Transaction is persisted through existing replace-roto-key-frames bridge/store path.
CONTEXT | D-01 | Duplicate next-frame splice | 05 | COVERED | Task 1/2 tests and controller preserve Duplicate.
CONTEXT | D-02 | Insert blank key before selected key | 05 | COVERED | Task 1/2 target inserted blank clean real key.
CONTEXT | D-03 | Delete closes gap | 05 | COVERED | Task 1/2 target delete shift and no stale reappear.
CONTEXT | D-04 | Shift only real keys; regenerate/refresh generated frames | 05 | COVERED | Task 1/2 require generated/render-only cleanup and transaction real frames only.
CONTEXT | D-05 | Source actions require real selected key | 05 | COVERED | Task 2 keeps source guard and excludes generated/empty sources.
CONTEXT | D-06 | Paste can target empty/generated/real | 05 | COVERED | Task 1/2 paste regression covers target flexibility.
CONTEXT | D-07 | Delete may remove last key | 05 | COVERED | Task 1/3 cover last-key deletion.
CONTEXT | D-08 | Dirty source saves before Duplicate/Copy/Insert/Delete | 05 | COVERED | Task 2 preserves existing save-before-action path before controller mutation.
CONTEXT | D-09 | Save failure cancels action and preserves dirty truth | 05 | COVERED | Task 2/3 acceptance requires no transaction on save failure.
CONTEXT | D-10 | Successful actions update cache/timeline/preview immediately | 05 | COVERED | Controller returns cache frames plus active restore intent for immediate coherence.
CONTEXT | D-11 | Use spec placement | 05 | COVERED | UI unchanged; source-contract/chkpt prevent placement changes.
CONTEXT | D-12 | Contextual Key pill labels | 05 | COVERED | UI unchanged; UAT confirms labels remain.
CONTEXT | D-13 | Visible disabled buttons and compact status | 05 | COVERED | Existing props/status preserved; dirty/action in-flight verified.
CONTEXT | D-14 | No new keyboard shortcuts | 05 | COVERED | Source-contract and UAT prohibit shortcuts.
CONTEXT | D-15 | Compact success feedback, no new toast/modal | 05 | COVERED | Existing success/status flow preserved; no UI additions.
UAT | 1 | Insert works once then returning/painting shows strange blended/yellow translucent paint | 05 | COVERED | Task 1/2/3 directly target inserted blank clean canvas/reference cleanup.
UAT | 2 | Cached frame rendering and background/reference rendering inconsistent | 05 | COVERED | Controller active restore intent centralizes cache/reference/background decisions.
UAT | 3 | Deleted/empty frame later reappears as strange/yellow translucent canvas | 05 | COVERED | Delete regression requires removed frames absent from transaction, launch context, and restore.
UAT | 4 | Clean real/cached Roto key cells not consistently green/clean | 05 | COVERED | UAT and controller transaction keep real-key cache metadata coherent.
UAT | 5 | Duplicate works better than Insert/Delete, suggesting blank/delete cache cleanup issue | 05 | COVERED | Duplicate preserved while Insert/Delete/Paste share the same transaction path.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User key utility click → Studio transaction | Repeated local UI events trigger real-key cache mutations and destructive delete operations. |
| Studio local canvas/reference state → parent persisted cache | Local engine state, reference backgrounds, and cache metadata must not diverge before persistence. |
| Generated/render-only cache → editable real-key cache | Paste and splice actions must not accidentally promote generated frames as editable sources or preserve stale generated overlays. |
| Automated tests → live UAT confidence | Unit/source-contract tests cannot fully prove the visible canvas surface; user-owned UAT remains required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.7-11 | Tampering | `physicsPaintRotoKeyController.ts` transactions | mitigate | Pure tests assert exact real-key frame outputs, removed frames, active restore intent, and generated/reference exclusion for Insert/Delete/Paste. |
| T-36.7-12 | Tampering | `PhysicsPaintStudio.tsx` cache persistence | mitigate | Persist controller output through existing `replace-roto-key-frames` bridge/store path and update launch context from store cache, not stale preserved generated entries. |
| T-36.7-13 | Denial of Service | rapid key utility clicks during save/action | mitigate | Preserve `rotoKeyActionInFlight`, `rotoFlushInFlightRef`, and `applyStatus === 'applying'` gating before transaction application. |
| T-36.7-14 | Repudiation | failed UAT closure | mitigate | Blocking human checkpoint records exact numbered live UAT result; Phase 36.7 is not marked complete without user approval. |
| T-36.7-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit lists none. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoKeyController.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts` passes.
- `pnpm --dir app exec tsc --noEmit` passes.
- The agent does not run the dev server.
- User-owned live UAT confirms the previously failed Insert/Delete/Paste canvas/cache/cell flows now pass.
</verification>

<success_criteria>
- Inserted blank real keys are clean editable/cache truth when first created, revisited, and painted.
- Deleted real keys and stale generated/reference payloads do not reappear when navigating back.
- Paste turns empty/generated/real targets into coherent real-key cache truth.
- Duplicate remains preserved.
- Dirty save-before-action semantics remain intact.
- UI remains unchanged and no out-of-scope controls or shortcuts are added.
</success_criteria>

<output>
Create `.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-05-SUMMARY.md` when done.
</output>
