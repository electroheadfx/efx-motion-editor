# Phase 36.7: Physics Paint Roto Key Utilities - Context

**Gathered:** 2026-06-22
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 36.7 delivers the five real Roto key utility actions from the source spec: Duplicate, Insert, Delete, Copy, and Paste. The goal is to let a stop-motion animator reuse, create, remove, and transfer real cached Roto keys while preserving predictable frame ownership and immediate cache/timeline/preview feedback.

This phase is not about interpolation, cached Roto playback, Play/Roto conversion, final timeline redesign, broader Physics Paint cleanup, or new keyboard shortcut systems. It must follow the Phase 36.7 spec and its referenced UI assets as the source of truth.

</domain>

<decisions>
## Implementation Decisions

### Placement Rules
- **D-01:** Duplicate creates the new real Roto key on the next frame and shifts later real Roto keys forward. This is a splice-like timeline operation, not a “next empty only” operation.
- **D-02:** Insert creates a blank real Roto key before the current selected real key and shifts the current/later real Roto keys forward.
- **D-03:** Delete removes the current real Roto key and shifts later real Roto keys backward to close the gap.
- **D-04:** Splice actions shift only real Roto keys. Generated/interpolated frames must not be treated as editable keys; they should be regenerated or refreshed from the updated real-key layout.

### Target Eligibility
- **D-05:** Duplicate, Copy, Delete, and Insert require the current selected frame to be a real Roto key. Generated in-betweens and empty frames are not valid sources/splice anchors for those actions.
- **D-06:** Paste may create or replace a real Roto key at the current target frame, including an empty frame or generated frame. This makes generated frames valid paste targets but not editable/generated sources.
- **D-07:** Delete may remove the last remaining real Roto key for the layer. The layer may return to having no real Roto keys if the animator chooses that action.

### Dirty State Handling
- **D-08:** If the current real key is dirty and the animator triggers Duplicate, Copy, Insert, or Delete, save the current frame first through the existing Roto save path, then run the requested key utility action using the saved result.
- **D-09:** If the save-before-action fails, cancel the requested key utility action. Leave the dirty frame visible/in memory as the unsaved truth and surface failure feedback for retry.
- **D-10:** After a successful Duplicate, Insert, Delete, or Paste, the affected key layout is already saved/clean in the parent cache. The action itself must update cache, timeline cell state, and preview state immediately.

### UI and Feedback
- **D-11:** The five key utility controls must use the placement defined by `SPECS/36.x-phases/phase-36.7-key-utilities/spec-36.7-key-utilities.md` and its referenced UI assets/specs. Do not invent a new placement. If an implementation fallback is needed, use the spec-defined contextual Roto key utility group near the Roto timeline controls.
- **D-12:** The referenced UI assets show a contextual pill under the frame cells labeled like `Key 0` with `Insert`, `Dup`, `Copy`, `Paste`, and destructive `Delete`. Treat that as the visual source for placement and grouping unless the Phase 36.7 spec says otherwise.
- **D-13:** Disabled-state feedback must follow the Phase 36.7 spec and referenced UI assets/specs. If they do not define a more specific pattern, keep the five buttons visible but disabled and reuse the existing compact workflow-strip status/message area to explain why the action is unavailable, such as real key required, no copied key, save in progress, or save failed.
- **D-14:** Phase 36.7 is buttons-only for the five utilities. Do not add new keyboard shortcuts unless the source spec explicitly requires them.
- **D-15:** Success feedback must follow the Phase 36.7 spec and referenced UI assets/specs. If they do not define a more specific pattern, update timeline cell state and preview immediately and show compact workflow-strip status copy naming the action and affected frame, such as “Duplicated to frame 12” or “Deleted frame 12”. Do not add a new modal or unrelated toast surface for MVP UAT.

### Claude's Discretion
No decisions were delegated to Claude. User selected or clarified every discussed gray area.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 36.7 Source of Truth
- `SPECS/36.x-phases/phase-36.7-key-utilities/spec-36.7-key-utilities.md` — Source of truth for the Phase 36.7 behavior contract, planning boundary, and acceptance checklist.
- `SPECS/36.x-phases/phase-36.7-key-utilities/GSD.md` — Phase 36.7 companion planning/reference material if more context is needed.
- `SPECS/36.x-phases/phase-36.7-key-utilities/GSD.html` — Visual/reference companion to the Phase 36.7 spec if needed for UI review.

### Timeline UI References
- `SPECS/36.x-phases/timeline-ui/timeline-bottom-window.html` — UI reference for the bottom Roto timeline window. Lines around the contextual `Key 0` pill show `Insert`, `Dup`, `Copy`, `Paste`, and destructive `Delete` placement.
- `SPECS/36.x-phases/timeline-ui/TimelineBottomWindow.tsx` — React/TSX version of the timeline bottom window reference. Includes `contextualActions = ['Insert', 'Dup', 'Copy', 'Paste']` and the contextual key utility row.
- `SPECS/36.x-phases/timeline-ui/timeline-bottom-window-check.png` — Visual check image for the timeline bottom window reference.

### Prior Phase Constraints
- `SPECS/36.x-phases/Done/phase-36.3-durable-core/spec-36.3-durable-core.md` — Defines the durable Roto cache foundation and existing Save current path that Phase 36.7 must reuse.
- `.planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-CONTEXT.md` — Locks prior Save current UX decisions: cached PNG is durable truth, editable restore is not guaranteed, and Save current saves only the current Roto frame.
- `SPECS/36.x-phases/Done/phase-36.5-cell-semantics/spec-36.5-cell-semantics.md` — Defines Roto timeline cell state semantics that Phase 36.7 must preserve/update after utility actions.
- `.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-CONTEXT.md` — Locks dirty/pending/cached/current/empty cell meanings and deferred key-utility scope now activated by Phase 36.7.
- `.planning/phases/36.5-physics-paint-roto-cell-semantics-this-phase-in-specs-36-x-p/36.5-UI-SPEC.md` — UI design contract for Phase 36.5 Roto cell semantics.
- `SPECS/36.x-phases/Done/phase-36.6-save-on-leave/spec-36.6-save-on-leave.md` — Defines save-on-leave behavior and failure semantics to mirror for save-before-key-utility actions.
- `.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md` — Locks save-on-leave decisions: dirty source saves first, failed save blocks navigation/action, and dirty in-memory state remains unsaved truth.

### Project Context
- `.planning/PROJECT.md` — Project context and non-replacement boundary for Basic, FX, and Physics paint modes.
- `.planning/REQUIREMENTS.md` — v0.8.0 Physics Paint milestone requirements and traceability.
- `.planning/STATE.md` — Current phase state: Phase 36.7 is current and ready for planning.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/src/components/physic-paint/physicsPaintWorkflowState.ts` — Existing helper seams define real-key operations: `duplicateRotoKeyFrame`, `insertRotoKeyFrame`, `deleteRotoKeyFrame`, `replaceRotoKeyFrame`, and `getNearestRealRotoKeyFrame`. Use these as implementation starting points, but align them with the decisions in this CONTEXT.md and the Phase 36.7 spec.
- `app/src/components/physic-paint/physicsPaintWorkflowState.test.ts` — Existing tests cover provisional helper behavior for sorted real keys. Update or extend tests to match the locked decisions, especially paste target eligibility.
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` — Main integration seam for current frame state, Roto save path, dirty frame handling, copy/paste state, cache mutation, and workflow strip callbacks.
- `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx` — UI integration seam for Roto timeline controls, cell state, disabled/action feedback, and key utility button callbacks.
- `app/src/stores/physicPaintStore.ts` and `app/src/lib/physicPaintBridge.ts` — Existing store/bridge surfaces for cached Roto frames and rendered frame payloads.

### Established Patterns
- Real Roto keys are distinct from generated/interpolated frames. Generated frames are render-only and must not become editable sources for key utilities.
- Roto cache updates must propagate to timeline cell state and preview state immediately after successful actions.
- Dirty Roto edits save through the existing Roto save path before actions that would move/copy/delete the visible source. Failed save cancels the action and preserves dirty in-memory state.
- UI feedback should stay compact inside the existing Physics Paint workflow strip. Avoid new modal/toast surfaces for this MVP unless the source spec explicitly requires them.
- Preact project conventions apply. Prefer existing local state/store patterns in the Physics Paint components and avoid introducing React-style abstractions or unrelated state-management changes.

### Integration Points
- `PhysicsPaintStudio.tsx` should coordinate save-before-action, dirty failure handling, cache/store updates, and `PhysicsPaintWorkflowStrip` callbacks.
- `PhysicsPaintWorkflowStrip.tsx` should render controls according to the Phase 36.7 spec and timeline UI references, including disabled states and compact status feedback.
- Helper logic in `physicsPaintWorkflowState.ts` should remain narrowly focused on real-key frame-list transformations and be testable without UI.
- Store/cache mutation paths must preserve unrelated real keys and ensure project save/load continues to preserve the updated real-key layout.

</code_context>

<specifics>
## Specific Ideas

- The UI placement decision is locked to the spec and referenced assets, not a new design. The fallback interpretation is the contextual Roto key utility group under/near the Roto timeline cells.
- The UI asset labels show `Insert`, `Dup`, `Copy`, `Paste`, and `Delete`; downstream planning should decide exact production labels only by following the Phase 36.7 spec/assets.
- Paste is intentionally broader than the current provisional studio guard: it may target empty or generated frames and create/replace a real key there, while generated frames remain render-only as sources.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 36.7-Physics Paint Roto Key Utilities*
*Context gathered: 2026-06-22*
