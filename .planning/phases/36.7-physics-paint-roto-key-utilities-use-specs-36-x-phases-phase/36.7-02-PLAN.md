---
phase: 36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase
plan: 02
type: execute
wave: 2
depends_on:
  - 36.7-01
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
requirements:
  - 36.7-DUP
  - 36.7-INS
  - 36.7-DEL
  - 36.7-COPY-PASTE
  - 36.7-STUDIO
must_haves:
  truths:
    - "Duplicate, Insert, Copy, and Delete save a dirty current real key first, then run the requested action only after save success (D-08)."
    - "If save-before-action fails, the requested utility action is cancelled, dirty in-memory frame remains visible, and failure feedback is shown (D-09)."
    - "Successful Duplicate, Insert, Delete, and Paste update cache, timeline cell state, and preview state immediately (D-10)."
    - "Paste creates or replaces a real key at empty, generated, or real target frames without requiring the target to be an existing real key (D-06)."
  artifacts:
    - path: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      provides: "Studio-level async key utility orchestration, cache mutation, copied-key state, and save-before-action ordering"
      exports: []
    - path: app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
      provides: "Source-contract coverage for Studio save-before-action and paste eligibility wiring"
  key_links:
    - from: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      to: app/src/components/physic-paint/physicsPaintWorkflowState.ts
      via: "Studio callbacks use tested helper exports from Plan 01"
      pattern: "duplicateRotoKeyFrame|insertRotoKeyFrame|deleteRotoKeyFrame|replaceRotoKeyFrame|canUseRotoKeySource|canPasteRotoKeyTarget"
    - from: app/src/components/physic-paint/PhysicsPaintStudio.tsx
      to: app/src/stores/physicPaintStore.ts
      via: "Cache mutations and interpolation regeneration update preview-visible state"
      pattern: "physicPaintStore\.(setFrame|removeFrameRange|regenerateRotoInterpolationCache)"
  prohibitions:
    - statement: "Do not create a new Roto save path for key tools."
      status: resolved
      verification: "Studio source-contract assertions and code review confirm existing `flushRotoFrame`/save-current path is reused."
    - statement: "Do not require Paste target to be a current real key."
      status: resolved
      verification: "Studio source-contract assertions confirm `pasteRotoFrame` does not call the real-source guard for its target."
---

<objective>
Wire the tested helper contract into the Studio so the animator can perform real Roto key utility actions against the actual cache, timeline, and preview state.

Purpose: The five controls are only useful when their callbacks preserve dirty save ordering and durable cache truth.
Output: Studio action handlers that save dirty sources before source actions, cancel on save failure, mutate real-key cache/layout consistently, and expose correct copied-key/paste behavior.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** duplicate, insert, delete, copy, and paste real Roto keys, **so that** I can build frame-by-frame animation efficiently.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-CONTEXT.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-RESEARCH.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-PATTERNS.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-VALIDATION.md
@.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-01-SUMMARY.md
@SPECS/36.x-phases/phase-36.7-key-utilities/spec-36.7-key-utilities.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/stores/physicPaintStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Studio source-contract tests for save-before-action and paste eligibility</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/physicsPaintWorkflowState.ts
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-PATTERNS.md
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-VALIDATION.md
  </read_first>
  <behavior>
    - Studio has one shared async save-before-action path for Duplicate, Insert, Copy, and Delete, per D-08 and D-09.
    - The shared path sets pending feedback copy `Saving frame {frame} before {action}...` and failure copy `Could not save frame {frame}; {action} was cancelled.` per UI-SPEC.
    - Paste eligibility does not call the current-real-key source guard, per D-06.
  </behavior>
  <action>Extend the existing source-contract test file rather than adding a browser/component harness. Add assertions that `PhysicsPaintStudio.tsx` contains a shared key-action runner or equivalent shared function, calls `flushRotoFrame` before source actions when dirty, emits the required pending/failure copy, passes key action in-flight state to the workflow strip, and keeps `pasteRotoFrame` separate from the source-only real-key guard. Do not create one-off test configs.</action>
  <acceptance_criteria>
    - Tests fail before the Studio implementation if the shared save-before-action/paste eligibility contract is missing.
    - Tests reference D-06, D-08, and D-09 in names or comments.
    - No watch-mode command is introduced.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Focused source-contract tests cover Studio orchestration gaps before modifying Studio behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Studio key utility orchestration and cache mutation</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/physicsPaintWorkflowState.ts
    @app/src/stores/physicPaintStore.ts
    @app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    @.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-CONTEXT.md
  </read_first>
  <behavior>
    - Source actions Duplicate, Insert, Copy, and Delete require the current selected frame to be a real Roto key (D-05).
    - Dirty current real key source actions call existing save path before mutation (D-08), cancel on save failure (D-09), and disable all five tools while save/action is in flight (D-13).
    - Duplicate/Insert/Delete shift only real Roto key data and refresh generated interpolation/cache state (D-01 through D-04, D-10).
    - Paste copies saved/cached source data into the current target, including empty/generated/real target frames, then marks target as real-key cache truth (D-06, D-10).
  </behavior>
  <action>In `PhysicsPaintStudio.tsx`, convert the current synchronous key utility callbacks into async-safe handlers using one shared operation path such as `runRotoKeyAction` or an equivalent narrow helper local to the component. Reuse `snapshotCurrentRotoFrame`, `dirtyRotoFramesRef`, `flushRotoFrame`, `setRotoSavingFrame`, `syncPendingRotoFrames`, and existing apply/error state patterns; do not invent a parallel save mechanism. Ensure action failure after successful save keeps durable cache visible and uses the UI-SPEC error copy. For cache shifts, update both `rotoFrameStatesRef` and `physicPaintStore` frame data so saved markers, cache metadata, generated interpolation refresh, and preview-visible state remain consistent. Paste must not call the source-only guard for the target frame.</action>
  <acceptance_criteria>
    - Source actions cannot run from generated or empty frames and surface source-required feedback.
    - Paste can run after a copied real key against empty, generated, or real target frames.
    - During save/action in-flight, all five key tools are disabled through props passed to the strip.
    - Success copy matches UI-SPEC intent: `Duplicated to frame {frame}.`, `Inserted blank key before frame {frame}.`, `Copied key {frame}.`, `Pasted key to frame {frame}.`, and `Deleted key {frame}.`
    - Store/cache updates call existing visual invalidation paths indirectly through `physicPaintStore` APIs; no manual version bump is added unless an existing store helper requires it.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts && pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts && pnpm --dir app exec tsc --noEmit</automated>
  </verify>
  <done>Studio callbacks implement the five real-key utilities against real cache/timeline/preview state with save-before-action ordering.</done>
</task>

</tasks>

## Artifacts this phase produces

- `runRotoKeyAction(...)` or equivalent local Studio helper — coordinates source action save-before-action, pending state, failure cancellation, and action mutation.
- Updated `duplicateRotoKey`, `insertRotoFrame`, `deleteRotoFrame`, `copyRotoFrame`, and `pasteRotoFrame` Studio callbacks — real cache/timeline/preview actions for the five controls.
- Updated `hasCopiedRotoKey` / key-action in-flight Studio state — enables Paste and disables all tools while saving/actioning.
- Source-contract tests in `PhysicsPaintWorkflowStrip.test.ts` for Studio save-before-action and Paste eligibility wiring.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkflowStrip callback → Studio mutation | User button events enter cache/timeline mutation code. |
| Studio dirty state → parent cache Store | Unsaved engine/editor state becomes durable cache metadata and preview-visible frame data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.7-03 | Tampering | `duplicateRotoKey`, `insertRotoFrame`, `deleteRotoFrame` | mitigate | Require current real-key source, shift only real-key data, regenerate generated interpolation cache after layout mutation. |
| T-36.7-04 | Tampering | dirty save-before-action | mitigate | Reuse existing `flushRotoFrame` path before source actions and cancel mutation when it returns no payload. |
| T-36.7-05 | Denial of Service | rapid repeated key utility clicks | mitigate | Track key action in-flight state and pass disabled state to all five buttons during save/action. |
| T-36.7-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit lists none. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts` passes.
- `pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintWorkflowState.test.ts` passes.
- `pnpm --dir app exec tsc --noEmit` passes.
</verification>

<success_criteria>
- The Studio implements real key utility actions with existing save-current/save-on-leave semantics mirrored for key actions.
- Failed dirty save cancels the requested action and keeps the dirty source frame as visible unsaved truth.
- Successful actions immediately update cache metadata, timeline cell state, and preview-visible state.
</success_criteria>

<output>
Create `.planning/phases/36.7-physics-paint-roto-key-utilities-use-specs-36-x-phases-phase/36.7-02-SUMMARY.md` when done.
</output>
