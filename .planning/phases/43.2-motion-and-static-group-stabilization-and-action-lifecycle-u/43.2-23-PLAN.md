---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 23
type: execute
wave: 18
depends_on: [43.2-05]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/lib/physicPaintPersistence.test.ts
autonomous: true
gap_closure: true
requirements: [GRP-02, GRP-08]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Per D-02/D-36, a supported pre-43.2 persisted finite five-field Loop Clip with its exact historical revision loads through the real persistence seam and becomes a lifecycle-complete canonical Group."
    - "Per D-35, legacy acceptance validates the exact pre-hydration canonical fingerprint; changing persisted content while retaining the old revision still rejects with no broad stale-revision bypass."
    - "The retained user project remains untouched, the targeted native retest opens only a copy, and Plan 17 stays blocked after this isolated save/reopen correction."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
    - app/src/lib/physicPaintPersistence.test.ts
  key_links:
    - "literal persisted five-field fixture -> loadPhysicPaintData sidecar hydration -> exact legacy fingerprint validation -> lifecycle hydration -> current canonical document/revision"
    - "changed repeat + retained historical revision -> exact legacy fingerprint mismatch -> canonical rejection"
---
<objective>
Restore supported pre-43.2 finite Group save/reopen without weakening canonical stale-content rejection.

Purpose: Close the diagnosed `[SAVE-REOPEN]` canonical-revision mismatch caused by lifecycle hydration occurring before persisted revision validation, while preserving D-35 fail-closed authority and leaving Plan 17 blocked.
Output: One literal persistence-seam regression fixture, one adjacent stale-content rejection, and a minimal model-only compatibility correction.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-05-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/lib/physicPaintPersistence.ts
@app/src/lib/physicPaintPersistence.test.ts</context>

## Artifacts this gap closure produces

A narrowly gated legacy fingerprint path at the canonical parser boundary plus persistence-load evidence using historical bytes and revision literals rather than a current revision builder.

## Multi-Source Coverage Audit

- GOAL: Durable Motion/Static Groups reopen from supported persisted canonical authority; Plan 23 closes the diagnosed finite pre-lifecycle revision gap without reopening unrelated Phase 43.2 failures.
- REQ: GRP-02 canonical lifecycle persistence and GRP-08 save/reopen parity are covered by both tasks.
- RESEARCH: The additive canonical model remains owned by `physicsPaintRotoPhysicalModel.ts`, while `physicPaintPersistence.ts` remains the unchanged sidecar hydration seam.
- CONTEXT: D-02 preserves supported persisted wire values without nomenclature migration; D-35 keeps exact stale guards; D-36 requires save/reopen canonical parity; D-39 governs the targeted frozen-session retest. Deferred ideas and Motion Editor production remain outside this plan.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Load one literal pre-43.2 finite Group through persistence</name>
  <read_first>app/src/lib/physicPaintPersistence.test.ts loadPhysicPaintData filesystem mocks and lifecycle save/reopen cases; app/src/lib/physicPaintPersistence.ts parsePersistedPhysicalDocument, hydratePhysicalDocument, and loadPhysicPaintData; app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts parsePhysicPaintRotoLoopClips, encodePhysicPaintRotoPhysicalContent, encodeCanonicalLoopClips, and parsePhysicPaintRotoPhysicalDocument; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-05-SUMMARY.md finite legacy hydration decision</read_first>
  <files>app/src/lib/physicPaintPersistence.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts</files>
  <behavior>
    - RED: a literal persisted output with one real key sidecar, one finite five-field Loop Clip, no lifecycle fields, and historical revision `physical-163-beed993b` reaches `loadPhysicPaintData` and currently rejects at canonical revision mismatch;
    - GREEN: the same fixture loads as a synchronized, attached Group with `phaseOrigin: 0`, `originalEndExclusive: 2`, `visibleRanges: [{ start: 0, endExclusive: 2 }]`, `frameOverrides: []`, and current canonical revision `physical-225-6e46481e`;
    - the persisted fixture stores revision as a literal and does not call `buildPhysicPaintRotoPhysicalRevision` to manufacture historical evidence.
  </behavior>
  <action>First add the persistence-seam regression and run it to capture RED. Seed `/project/cache/physic-paint/legacy/key-000000-key-0.png` with literal bytes `[1, 2, 3]`; pass a literal persisted output containing capacity 16, one `key-0` real-key record at frame 0 with that safe cache path and 100×50 dimensions, duplicate interpolation disabled, zero Motion settings, null background/selection, cursor 0, no incoming breaks, and exactly `{ loopId: 'loop-legacy', placementStart: 0, sourceKeyIds: ['key-0'], repeat: 2, mode: 'progressive' }`. Store `physical-163-beed993b` literally and assert the complete hydrated lifecycle and `physical-225-6e46481e` result.

Then modify only `physicsPaintRotoPhysicalModel.ts`. Preserve the unhydrated Loop Clip input long enough to recognize the exact supported legacy shape and compute its historical canonical fingerprint before lifecycle defaults are added. Gate this path to finite records carrying exactly the five pre-43.2 members and no lifecycle/provenance additions; reuse the existing strict record, real-key, interpolation, incoming-break, ordering, length-prefix, and FNV fingerprint rules rather than accepting an arbitrary alternate revision. Compare the persisted revision with that exact legacy fingerprint first. Only after a match, return the normal lifecycle-hydrated document with the newly computed current canonical revision. Current lifecycle-complete documents continue through the existing current fingerprint path, and malformed, partial-lifecycle, infinity, unknown-key, or mismatched legacy candidates continue to reject. Do not change `physicPaintPersistence.ts`, transport/project schemas, the retained user fixture, or any store/publication path.</action>
  <verify><automated>pnpm --dir app exec vitest run src/lib/physicPaintPersistence.test.ts -t "pre-43.2 finite Group"</automated></verify>
  <acceptance_criteria>The focused test is observed failing before production work, then passes through `loadPhysicPaintData`; its input contains the literal five-field Group and `physical-163-beed993b`, and its output contains all six lifecycle fields plus `physical-225-6e46481e`.</acceptance_criteria>
  <done>The supported historical document crosses the real persisted sidecar seam and emerges as current canonical authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove legacy validation remains content-exact and run closure gates</name>
  <read_first>app/src/lib/physicPaintPersistence.test.ts new literal pre-43.2 load fixture and adjacent malformed/stale persistence cases; app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts new legacy fingerprint gate and current canonical mismatch branch; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md `[SAVE-REOPEN]` and `[PRE-FROZEN-PARENT-CHILD]` rows</read_first>
  <files>app/src/lib/physicPaintPersistence.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts</files>
  <behavior>
    - a neighboring literal persisted fixture changing only `repeat` from 2 to 3 while retaining `physical-163-beed993b` rejects at `loadPhysicPaintData`;
    - rejection does not hydrate and accept stale content merely because the record has the five-field legacy shape;
    - current lifecycle-complete persistence, malformed partial lifecycle rejection, typecheck, and production build remain green.
  </behavior>
  <action>Add the stale-content case immediately beside the accepted literal fixture. Clone only test data, change `repeat` to 3, retain the exact old revision string, and assert canonical revision mismatch from the public persistence load seam. If this test exposes permissive fallback behavior, tighten the model gate so acceptance requires equality with the fingerprint of the exact raw legacy bytes/content before hydration; do not add a revision allowlist, schema-wide bypass, catch-and-retry acceptance, or fallback based only on missing lifecycle fields. Run the complete persistence suite, typecheck, and production build without starting any server and without changing test configuration.

After automated gates pass, prepare the targeted D-39 native retest only: preserve the retained supported project unchanged, make a copy outside the application, fully quit and restart the frozen-code parent and Physics Paint child, and open the copy. The expected result is that the copied project opens without canonical revision mismatch and presents lifecycle-complete Groups. This targeted result does not approve the remaining Phase 43.2 checklist and must not unblock Plan 17.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintPersistence.test.ts && pnpm --dir app run typecheck && pnpm build</automated>
    <human-check>On one frozen-code build, duplicate the supported untouched project, open only the copy after fully restarting parent and child, and confirm that it opens without the canonical revision mismatch. Keep the original fixture unchanged and keep Plan 17 blocked regardless of this targeted result.</human-check>
  </verify>
  <acceptance_criteria>The changed-repeat fixture rejects with its old revision; the full persistence suite, typecheck, and build exit 0; no server is started; native retest instructions protect the original fixture and explicitly preserve the Plan 17 block.</acceptance_criteria>
  <done>Legacy compatibility is exact rather than permissive, automated closure gates are green, and the isolated native save/reopen retest is ready without altering broader UAT status.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted project JSON and sidecars→canonical physical document | Historical structured input and claimed revision are untrusted until exact content validation succeeds. |
| legacy fingerprint→lifecycle hydration | Compatibility must not convert stale or malformed content into accepted current authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-01 | Tampering | pre-43.2 revision validation | high | mitigate | Recompute the exact historical fingerprint from the raw finite five-field content before hydration and require equality. |
| T-43.2-02 | Tampering | lifecycle canonicalization | high | mitigate | Hydrate only after historical validation, then return the lifecycle-complete document with the current canonical revision. |
| T-43.2-03 | Repudiation | retained native fixture | medium | mitigate | Test only a copy under one frozen parent/child build and preserve the original untouched fixture. |
| T-43.2-SC | Tampering | packages | low | accept | No installs, dependency changes, or lockfile changes are required. |
</threat_model>
<verification>The focused RED/GREEN command proves the original persistence failure and correction. The stale neighbor, complete persistence suite, typecheck, and production build prove exact rejection and integration safety. The targeted frozen-session check opens only a copy and does not release Plan 17.</verification>
<success_criteria>A literal supported pre-43.2 finite Group reopens as a lifecycle-complete current document, altered content with the historical revision still rejects, only the model and persistence regression test change, the original user fixture is untouched, and Plan 17 remains blocked.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-23-SUMMARY.md` when done.</output>
