# Phase 43.2 Native UAT Checklist

| ID/Anchor | Owner | Scenario / Precondition | Expected Result | Observed Result | Evidence | Status | Approval |
|---|---|---|---|---|---|---|---|
| [UAT-01] | Phase 43.2 native UAT owner | Inspect every affected EFX Paint Roto label, tooltip, dialog, inspector heading, status message, and accessibility name. | Group, Motion, Static, and Action vocabulary is used consistently; retired Loop Clip, Progressive, Static/Hold, Script, Scripts, Play Script, and Edit Loop Clip product copy is absent. | RE-VERIFICATION PENDING — frozen `2418ed6c` displayed “Play Script physical commit was rejected or timed out.” Candidate `69d07703` dispatches Regenerate as `regenerate-group` and formats rejection, transport, and timeout outcomes with current Group Regenerate terminology plus the canonical reason. | Prior failure screenshot #266 and attached close-up retained. Automated adapter coverage asserts `Group Regenerate rejected — Group lifecycle target document does not match parent recomputation.` Native copy remains blocking targeted retest 7. | PENDING | PENDING |
| [UAT-02] | Phase 43.2 native UAT owner | Open a project with contiguous and fragmented Groups at the minimum supported 1280×720 window size. | The workflow strip remains 161px, the Group Rail remains 3px inside the existing physical row, and no new lane, badge row, or layout jump appears. | RE-VERIFICATION PENDING — The `26ab9328` Key Spacing failure was traced to source-key retiming that left `phaseOrigin`, `originalEndExclusive`, and `visibleRanges` at the pre-spacing extent. Commit `cd67423f` rebuilds finite lifecycle extents from the post-spacing source cycle while preserving Group identity, Repeat 3, placement, phase, Action provenance, sharing, and one-command Undo/Redo. The separate single-rail presentation correction is tracked under `[UAT-11]`. | Prior failure screenshots #250–#252. Plan 22 focused suites passed 115 tests; aggregate shared-loop forward/Undo/Redo coverage was aligned in `614c3b45`. Full frozen-HEAD automation passed 1,817 Vitest tests, 58 Cargo tests, typecheck, and production build. | PENDING | PENDING |
| [UAT-03] | Phase 43.2 native UAT owner | Display synchronized, modified, detached, unavailable, and unresolved Groups with Interpolation both On and Off. | Each visible fragment has the correct passive centered lifecycle dot; unresolved red presentation suppresses the dot and all states remain understandable without color alone. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | PENDING |
| [UAT-04] | Phase 43.2 native UAT owner | Paint one repeated physical occurrence in a Group whose source cycle is shared by other occurrences and another Group. | Only the targeted occurrence changes, the rail stays continuous, every fragment dot becomes Modified, and the persisted Action plus all unaffected repeats remain unchanged. | FAIL — frozen `69d07703` local Paint over an existing Group-owned frame removes the prior accepted Group content, loses the requested new Paint result, and publishes no replacement cache. Copy-on-write/rejection atomicity is not preserved. | Fully quit/restarted parent and Physics Paint child; no hot reload. Native reproduction: create valid Group, Paint one existing occurrence, commit, then observe both old and new content absent with no accepted cache. | FAIL | REJECTED |
| [UAT-05] | Phase 43.2 native UAT owner | Invoke Delete, Backspace, and the visible Delete action first with one Group-owned physical frame selected, then with the complete logical Group selected on the Group Rail, then on an ordinary real key. | Selection scope determines the command directly: individual physical-frame selection invokes Delete Frame, Group Rail selection invokes Delete Group, and no routine Delete Group / Delete Frame choice modal appears. Ordinary-key deletion preserves its prior behavior; only genuinely destructive edge cases may show a focused warning. | RE-VERIFICATION PENDING — Plan 25 removes the routine scope-choice modal. Activation-time classification now routes an owned occurrence directly to Delete Frame, prioritizes a selected Group Rail for Delete Group even when the cursor is elsewhere, and reserves the locked warning for the sole visible occurrence only. | Automated evidence on frozen implementation commit `2418ed6c`: timeline/Studio source-contract tests pass inside the 318-test focused stabilization gate; the complete Vitest suite passes 1,844 tests with one skip and 101 todo. Native selection, keyboard, warning, focus, and accepted settlement still require one fully restarted frozen parent/child session. | PENDING | PENDING |
| [UAT-06] | Phase 43.2 native UAT owner | Delete a middle frame from a multi-cycle Motion Group and inspect both resulting ranges around the gap. | One persistent empty physical frame separates two explicit ranges of the same Group identity, and the right range resumes the original source-cycle phase. | FAIL — frozen `69d07703` structurally removes the interior occurrence and its Undo/Redo now restore and reapply the complete/gap documents, but the accepted empty position remains green rather than neutral gray. | Fully quit/restarted parent and Physics Paint child; no hot reload. Native evidence confirms Delete Frame structural history passes while the post-delete presentation remains stale green. | FAIL | REJECTED |
| [UAT-07] | Phase 43.2 native UAT owner | Delete a fragmented Group that has uniquely owned and shared source or cache data. | Every fragment and uniquely owned Group datum is removed in one history action, while the Action and any data referenced by surviving Groups remain available. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | PENDING |
| [UAT-08] | Phase 43.2 native UAT owner | Open Regenerate for one Modified Group and for a shared-source impact set. | The confirmation lists the required range, paint, deletion, fragment, gap, result, source/cache, affected-count, and placement-ordered Group facts before enabling the exact commit action. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | PENDING |
| [UAT-09] | Phase 43.2 native UAT owner | Accept Regenerate while the cursor is on a locally painted frame and again while it is on a deleted-frame gap. | Accepted Action output, full range, source phase, gaps, fragments, green synchronized state, initiating selection, cursor, and immediate canvas are restored without changing Group identity. | FAIL — frozen `69d07703` executes Regenerate after an interior Delete Frame, but the deleted occurrence remains suppressed and empty. The complete original Action-generated extent is not restored because current fragmented visibility remains authoritative. | Fully quit/restarted parent and Physics Paint child; no hot reload. Native reproduction: delete an interior occurrence from a synchronized Group, accept Regenerate, and observe that the missing frame and fragmented range persist. | FAIL | REJECTED |
| [UAT-10] | Phase 43.2 native UAT owner | Delete an Action referenced by one or more Groups and review every choice before committing. | The dialog offers exactly Keep Groups, Delete Action and Groups, and Cancel, with affected counts, ranges, cleanup, status, and occupied-space consequences disclosed. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | PENDING |
| [UAT-11] | Phase 43.2 native UAT owner | Select an Action linked to multiple contiguous and fragmented Motion and Static Groups while another Group is selected. | Every linked fragment gains the correct mode-colored halo without cursor movement; the actually selected Group remains orange and each logical Group is counted once. | RE-VERIFICATION PENDING — The `26ab9328` parasite outline was the linked-Action `box-shadow` painted around the full 12px interaction target after individual-frame selection cleared Group selection but retained Action linkage. Commit `14f4e9fd` keeps the passive Motion/Static linked treatment on the existing 3px segment only; selected Groups remain orange, while Signals, Action linkage, cursor, counts, endpoint cuts, dots, focus, accessibility, and zero-added-height geometry remain unchanged. | Prior failure screenshots #253–#255. Plan 24 rail/workflow suites passed 98 tests, and full frozen-HEAD automation passed 1,817 Vitest tests, 58 Cargo tests, typecheck, and production build. | PENDING | PENDING |
| [UAT-12] | Phase 43.2 native UAT owner | Select Actions with zero, one, and several linked Groups from cursor positions before, inside, and after their ranges, then use Previous and Next. | Initial current selection is cursor-relative without seeking; navigation is placement ordered, non-wrapping, boundary-disabled, and seeks only on explicit activation to the Group start. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | PENDING |
| [UAT-13] | Phase 43.2 native UAT owner | Exercise cancellation, stale rejection, accepted commit, Undo, and Redo for Paint, Delete, Regenerate, Group editing, and referenced Action deletion. | No optimistic accepted state appears, focus returns to the invoking control, each accepted operation is one history command, and rejection preserves all prior authority for immediate reuse. | FAIL — frozen `69d07703` confirms Repeat and Delete Frame accepted history settlement, but local Paint over Group content destroys the prior accepted frame and cache without accepting the replacement. Paint rejection/partial settlement therefore still violates zero-mutation atomicity. | Fully quit/restarted parent and Physics Paint child; no hot reload. Preserve the confirmed Repeat and Delete Frame history passes. Native local-Paint reproduction records loss of both old and new content with no cache. | FAIL | REJECTED |
| [UAT-14] | Phase 43.2 native UAT owner | Compare the Motion Editor timeline before and after all Phase 43.2 Group and Action interactions. | The Motion Editor timeline has no Phase 43.2 visual, copy, identity, tooltip, status, navigation, geometry, or interaction change. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | PENDING |
| [PRE-FROZEN-PARENT-CHILD] | Phase 43.2 native UAT owner | Fully quit and restart both the frozen-code parent application and Physics Paint child before collecting any native evidence; record the tested commit and bundle identity. | Mixed or hot-reloaded parent-child evidence is rejected, and all remaining rows use one fully restarted frozen-code session. | PASS — implementation `69d07703` / checkpoint `942a5c26` were tested after fully quitting and restarting both parent and Physics Paint child; no hot reload was used. | User frozen native retest report, 2026-08-12. | PASS | APPROVED |
| [PAINT-COW-GAP] | Phase 43.2 native UAT owner | Paint a repeated Group occurrence, then paint directly into a frame previously emptied by Delete Frame. | Copy-on-write isolates the first edit; the second edit fills only the gap, reunites adjacent ranges when contiguous, keeps the Group Modified, and preserves the Action. | FAIL — frozen `69d07703` leaves the deleted Group-owned position unpaintable. Direct Paint accepts no exact-frame override, publishes no cache, and leaves the gap empty; Insert is not an acceptable substitute. | Fully quit/restarted parent and Physics Paint child; no hot reload. Native reproduction: Delete an interior occurrence, select the empty Group-owned position, Paint and commit, then observe no override/cache publication. | FAIL | N/A |
| [DELETE-FRAME] | Phase 43.2 native UAT owner | Select an individual Group-owned physical frame and invoke Delete directly. | Delete Frame executes without a scope-choice modal. Middle deletion creates explicit same-identity ranges, edge deletion shortens the range, and sole-frame deletion shows only its required focused warning before removing the Group. | FAIL — frozen `69d07703` accepts the structural Delete Frame command and its Undo/Redo restore and reapply the exact complete/gap documents, but the accepted empty gap remains green instead of neutral gray. | Fully quit/restarted parent and Physics Paint child; no hot reload. Preserve the passing structural forward/Undo/Redo path; repair only the post-delete ownership/presentation boundary. | FAIL | N/A |
| [DELETE-GROUP] | Phase 43.2 native UAT owner | Select the complete logical Group on any Group Rail fragment and invoke Delete directly. | Delete Group executes without a scope-choice modal, removes every fragment and uniquely owned authority atomically from any placement start, and keeps the referenced Action and shared data. | RE-VERIFICATION PENDING — candidate `69d07703` removes optimistic child mutation and late split publication; accepted Delete Group and history replay each replace one complete document after exact parent acknowledgement. | Prior `2418ed6c` Undo-overwrite observation retained. Native Delete Group → Undo → Redo remains blocking targeted retest 1. | PENDING | N/A |
| [EDIT-GROUP-REPEAT] | Phase 43.2 native UAT owner | Create Groups with Repeat 1/default and Repeat 3, then edit each Group and change Repeat to another finite value such as 2. | Update Group commits the exact new Repeat and lifecycle extent atomically, preserves stable Group identity, source cycle, placement, phase, Action provenance/sharing, selection, and one-command Undo/Redo, and remains immediately deletable afterward. | PASS — frozen `69d07703` creates and edits Groups, changes Repeat, restores the exact previous Repeat and complete Group on Undo, reapplies the edited Repeat and complete Group on Redo, and leaves the edited Group usable. | Fully quit/restarted parent and Physics Paint child; no hot reload. User frozen native retest report, 2026-08-12. | PASS | N/A |
| [REGENERATE-ONE] | Phase 43.2 native UAT owner | Modify one Group with local Paint and Delete Frame, then accept its one-Group Regenerate confirmation. | All local changes are discarded, the original full Action result and synchronized state return, and one Undo and one Redo restore each complete side exactly. | FAIL — frozen `69d07703` Regenerate leaves the locally deleted occurrence suppressed. It does not restore the original complete Group range, caches, or synchronized state because reconstruction preserves the currently fragmented `visibleRanges`. | Fully quit/restarted parent and Physics Paint child; no hot reload. Native reproduction confirms Regenerate executes but retains the deleted gap. | FAIL | N/A |
| [REGENERATE-SHARED] | Phase 43.2 native UAT owner | Regenerate a Modified Group whose accepted source cycle is shared with other affected Groups. | The exact affected count and placement-ordered list appear before commit, all disclosed Groups update atomically, and the initiating Group remains selected. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACTION-KEEP-FORWARD] | Phase 43.2 native UAT owner | Delete a referenced Action using Keep Groups from an accepted attached state. | The Action row disappears only after durable commit; every Group, range, key, cache, render result, selection, cursor, and occupied span remains, with detached neutral status. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACTION-KEEP-UNDO] | Phase 43.2 native UAT owner | Undo the accepted Keep Groups Action deletion. | The exact retained Action bytes and attached Group provenance return in one command without changing preserved physical output, selection, cursor, or placement. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACTION-KEEP-REDO] | Phase 43.2 native UAT owner | Redo the previously undone Keep Groups Action deletion. | The same Action is deleted again, the same Groups detach once, and no duplicate version, pointer, selection, or library settlement occurs. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACTION-CASCADE-FORWARD] | Phase 43.2 native UAT owner | Delete a referenced Action using Delete Action and Groups with shared and uniquely owned Group data present. | The Action and all referencing logical Groups are removed atomically after durable commit; uniquely owned data is cleaned and externally shared data survives. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACTION-CASCADE-UNDO] | Phase 43.2 native UAT owner | Undo the accepted Delete Action and Groups command. | Exact Action bytes, Groups, fragments, keys, caches, gaps, placement, selection, cursor, and rendered facts return from retained history as one settlement. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACTION-CASCADE-REDO] | Phase 43.2 native UAT owner | Redo the restored Delete Action and Groups command. | The exact previously restored set is deleted again once, retained-history identity remains stable, and unrelated Groups or shared data are unchanged. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [RECOVERY-PREPARED] | Phase 43.2 native UAT owner | Interrupt the application after durable prepare but before the direction-specific committed marker, then restart. | Recovery restores prior Action authority, performs no physical, version, history, selection, or visible library settlement, cleans active state, and then permits ordinary scan. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [RECOVERY-COMMITTED] | Phase 43.2 native UAT owner | Interrupt after the Rust committed marker but before frontend settlement or acknowledge, then restart on the unchanged expected document. | Startup reconstructs the recovery lease, applies the exact target once, advances the pointer once, settles the library once, and leaves only cleanup work for acknowledge. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [LEASE-STALE-CONCURRENT] | Phase 43.2 native UAT owner | Hold an active referenced-deletion lease while attempting Paint, Delete, Regenerate, Undo, Redo, project replacement, and a stale token or generation replay. | Every concurrent or stale authority-changing operation rejects without mutating accepted document, library, selection, cursor, history, version, or recovery evidence. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [UNDO-INTERRUPTION] | Phase 43.2 native UAT owner | Interrupt an Undo once before its committed marker and once after commit but before settlement or acknowledge. | Prepared Undo restores the prior deleted Action state with no frontend events; committed Undo restores Action and physical before-state exactly once after restart. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [REDO-INTERRUPTION] | Phase 43.2 native UAT owner | Interrupt a Redo once before its committed marker and once after commit but before settlement or acknowledge. | Prepared Redo restores the prior restored Action state with no frontend events; committed Redo re-deletes and settles the exact target once after restart. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [HISTORY-EVICTION-RELEASE] | Phase 43.2 native UAT owner | Execute more than ten referenced Action history commands, allowing the oldest command to leave the owned history. | The evicted command’s retained artifact is released only when unreferenced by active recovery; the newest ten commands remain fully Undoable and Redoable. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [REDO-BRANCH-TRUNCATION] | Phase 43.2 native UAT owner | Undo a referenced Action command, then execute a new history-bearing command that truncates the redo branch. | Retained artifacts for truncated commands are explicitly released unless active recovery still references them; surviving history remains intact. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [ACKNOWLEDGE-RETRY] | Phase 43.2 native UAT owner | Force acknowledge cleanup to fail after exact local settlement, then retry acknowledge and restart if needed. | Cleanup-pending authority preserves the lease handoff; retry removes only journal and working tombstone, with no duplicate physical, version, pointer, selection, or library events. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [SAVE-REOPEN] | Phase 43.2 native UAT owner | Save and reopen a project containing synchronized, modified, detached, fragmented, regenerated, and deleted Group states. | Stable Group identity, explicit ranges, lifecycle status, provenance, exact-frame overrides, gaps, Action availability, selection eligibility, and rendered output reopen from canonical authority. | FAIL — frozen `69d07703` cannot save a valid project because Tauri rejects the generated project-local `cache/.physic-paint-staging-<uuid>` path for `allow-exists`. Atomic cache publication never completes, so close/reopen cannot be verified. | Exact native error: `forbidden path: /Users/lmarques/Desktop/efx-motion-editor-project-test/toto/cache/.physic-paint-staging-06749e49-5ae2-44ec-860a-a54159ca5ab3, maybe it is not allowed on the scope for allow-exists permission in your capability file`. Fully quit/restarted parent/child, no hot reload. | FAIL | N/A |
| [PLAYBACK] | Phase 43.2 native UAT owner | Play through contiguous Groups, Delete Frame gaps, exact-frame overrides, detached Groups, and regenerated ranges in Studio. | Playback uses the same accepted Group resolution as the stopped canvas, preserves source-cycle phase across fragments, and shows intentional gaps without fallback content. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [PREVIEW] | Phase 43.2 native UAT owner | Preview frames covering synchronized, modified, detached, fragmented, gap, regenerated, and unresolved Group states. | Preview matches Studio accepted pixels and gaps exactly; unresolved output retains explicit placeholder/error precedence and never falls back to stale source content. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [EXPORT] | Phase 43.2 native UAT owner | Export a valid lifecycle project and separately attempt export with unresolved Group output. | Valid export matches preview and playback for overrides, fragments, and gaps; unresolved output fails preflight before rendering with the canonical reason. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [MOTION-EDITOR-REGRESSION] | Phase 43.2 native UAT owner | Exercise Motion Editor timeline selection, scrubbing, playback, tooltips, and ordinary interactions after completing Group lifecycle operations. | Motion Editor behavior and appearance remain unchanged, with no Group Rail identity, Action copy, lifecycle dots, linked navigation, or new interaction surface. | PENDING — record during Plan 17 Task 3 | PENDING — attach during Plan 17 Task 3 | PENDING | N/A |
| [EVIDENCE-FIELDS] | Phase 43.2 native UAT owner | For every completed checklist row, record the concrete observed result and attach a reproducible screenshot, video, log, project fixture, or command reference. | No PASS or FAIL row retains a Wave 0 pending sentinel, generic statement, missing artifact reference, or evidence from a different code/session identity. | PASS — candidate `69d07703` passed the focused exact-once settlement matrix (353 tests), complete Vitest (118 files passed, 3 skipped; 1,852 tests passed, 1 skipped, 101 todo), all 58 Cargo tests, TypeScript typecheck, production build, `git diff --check`, and independent read-only review with no findings. Native rows remain explicitly pending one frozen restarted session. | Commands: focused Vitest matrix across coordinator, controller/adapter, bridge, history, and presentation; `pnpm --filter efx-motion-editor exec vitest run`; `cargo test --manifest-path app/src-tauri/Cargo.toml`; `pnpm --filter efx-motion-editor typecheck`; `pnpm build`. All exited 0 on 2026-08-12. Build retained pre-existing Vite CJS/dependency chunk warnings without failure. | PASS | N/A |
| [APPROVAL-FIELDS] | Phase 43.2 native UAT owner | Review all automated and native evidence after every scenario has an explicit PASS or FAIL outcome. | Approval fields use only APPROVED, REJECTED, or N/A as applicable, and final phase approval is withheld whenever any required row fails or lacks evidence. | REJECTED — frozen `69d07703` passes Create/Edit Group, Repeat history/usability, and Delete Frame structural history, but five independent native failures remain. Plan 43.2-17 and phase completion stay blocked. | Fully restarted `69d07703` / `942a5c26` native report, 2026-08-12. | FAIL | REJECTED |

## Frozen `2418ed6c` Native Settlement Failure Register — 2026-08-12

The following failures are recorded independently even though they indicate one shared accepted-operation settlement defect:

1. **Delete Group history overwrite** — A fresh Group deletes successfully. Undo briefly restores the complete Group, then another authority removes it again. Redo cannot be trusted until the late overwrite is eliminated.
2. **Repeat edit rejection** — Changing Repeat on a fresh otherwise valid Group reports an error. The Group can still be deleted, so Repeat acceptance itself remains defective rather than the layer becoming globally unusable.
3. **Delete Frame partial commit** — Deleting F12 fragments the same stable Group into two ranges, but F12 remains green instead of becoming a neutral grey accepted gap. Undo does not restore the complete prior Group.
4. **Paint-into-gap unavailable** — Direct Paint cannot fill deleted Group frame F12. Insert creates a blue-dot empty real key, but the frame remains empty and does not satisfy exact-frame Group copy-on-write repair.
5. **Regenerate partial settlement and trapped dialog** — Regenerate proposes the correct F3–F28 synchronized result and visibly restores F12, then reports the retired generic “Play Script physical commit was rejected or timed out” message, leaves the dialog open/unusable, and leaves the Group Modified.

### Locked Diagnosis Boundary

Trace the complete accepted-operation settlement path before patching: canonical before/after document, lifecycle record and ranges, selection/key/cursor, revisions, history snapshots, lease acquisition/release, bridge accepted event, parent publication, child settlement, resolver/cache refresh, lifecycle status, dialog completion, timeout cleanup, and every late event that can overwrite Undo or accepted Regenerate state.

- A successful operation settles exactly once.
- A rejected operation settles zero times.
- Undo and Redo cannot be overwritten by delayed publication or derived reconstruction.
- Do not use modal dismissal, forced refresh, longer timeout, restart, layer recreation, project reload, silent revision refresh, rejection suppression, or local status overrides.

### Automated Settlement Repair Candidate — `69d07703`

The shared defect was split child settlement after the parent had already established canonical authority. Structural operations could optimistically or incrementally mutate records/Groups, pending cleanup could race the accepted event, and late publication or runtime reconstruction could overwrite the accepted or history-restored document. Regenerate additionally traveled through the Play Script operation identity and validated only an initiating Group rather than the complete truly shared impact set.

The frozen candidate enforces one invariant across Delete, Repeat, Paint, Regenerate, Undo, and Redo:

- Parent acceptance precedes child structural publication.
- One accepted operation publishes one complete canonical physical document exactly once.
- Rejection, transport failure, and timeout publish zero child structural state.
- Terminal acknowledgement is correlated by exact operation ID and is independent from pending cleanup.
- Runtime key/frame ownership is reconciled before the complete document replacement.
- Group gaps route to exact-frame Group copy-on-write Paint and render neutral when outside `visibleRanges`.
- Regenerate uses `regenerate-group`; the parent independently derives every truly shared attached Group, preserves per-Group settings, and validates one aggregate canonical target.
- Current Group Regenerate status/error copy exposes the canonical rejection reason.

Automated evidence: 353 focused tests; complete Vitest 118 files passed / 3 skipped and 1,852 tests passed / 1 skipped / 101 todo; 58 Cargo tests; typecheck; production build; diff check; independent review with no findings.

### Frozen `69d07703` Native Retest — 2026-08-12

The implementation and checkpoint were tested after fully quitting and restarting both parent and Physics Paint child; no hot reload was used.

Confirmed passing behavior that must remain frozen:

- Create Group and Edit Group work.
- Repeat changes accept.
- Repeat Undo restores the exact previous value and complete Group.
- Repeat Redo restores the edited value and complete Group.
- The edited Group remains usable.
- Delete Frame Undo/Redo restore and reapply the structural operation.

Five failures remain independently recorded:

1. **`[SAVE-REOPEN]` project-local staging permission** — Save rejects `cache/.physic-paint-staging-<uuid>` under Tauri `allow-exists`. Preserve atomic staging/publication, grant no broad Desktop/home/arbitrary scope, clean failed staging safely, and preserve the prior accepted cache.
2. **`[UAT-06]` / `[DELETE-FRAME]` stale green gap** — Structural deletion and history work, but the accepted empty Group gap remains green instead of neutral gray.
3. **`[PAINT-COW-GAP]` deleted-position ownership loss** — Direct Paint into the empty Group-owned occurrence accepts no exact-frame override or cache; Insert is not a substitute.
4. **`[UAT-04]` / `[UAT-13]` destructive local Paint** — Paint over existing Group content loses both the old accepted content/cache and the requested replacement, violating copy-on-write and zero-mutation rejection atomicity.
5. **`[UAT-09]` / `[REGENERATE-ONE]` shortened reconstruction** — Regenerate retains the deleted occurrence because current `visibleRanges` remain authoritative instead of the stored original Group extent.

### Required Retests for the Next Frozen Candidate

1. Save → close → reopen, including project-local staging cleanup.
2. Delete Frame produces a neutral gray empty gap.
3. Undo and Redo preserve the correct complete/gap presentations.
4. Paint directly into the deleted Group gap.
5. Paint over an existing Group frame while preserving old content until commit.
6. Rejected local Paint preserves the original Group frame and cache.
7. Regenerate restores deleted frames, complete range, caches, and Synchronized state.
8. Regenerate Undo/Redo restores both complete states exactly.

Use one newly built, fully quit/restarted parent and Physics Paint child from the next exact frozen commit. Keep `[SAVE-REOPEN]` separate from the Group Paint and Regenerate failures. Plan 43.2-17 and Phase 43.2 remain blocked; do not resume the remaining native checklist until all eight targeted retests pass.
