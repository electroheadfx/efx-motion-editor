# Phase 43.2 Native UAT Checklist

| ID/Anchor | Owner | Scenario / Precondition | Expected Result | Observed Result | Evidence | Status | Approval |
|---|---|---|---|---|---|---|---|
| [UAT-01] | Phase 43.2 native UAT owner | Inspect every affected EFX Paint Roto label, tooltip, dialog, inspector heading, status message, and accessibility name. | Group, Motion, Static, and Action vocabulary is used consistently; retired Loop Clip, Progressive, Static/Hold, Script, Scripts, Play Script, and Edit Loop Clip product copy is absent. | PASS — frozen `2cd4c033` renders the revised label set: `Update Motion` / `Update Static`, `Regenerate Group`, `Keep Local Changes`, in normal casing with no retired product copy. | User frozen native retest report on `2cd4c033`, 2026-08-12 (labels/casing check). Automated terminology suites green. | PASS | APPROVED |
| [UAT-02] | Phase 43.2 native UAT owner | Open a project with contiguous and fragmented Groups at the minimum supported 1280×720 window size. | The workflow strip remains 161px, the Group Rail remains 3px inside the existing physical row, and no new lane, badge row, or layout jump appears. | PASS — frozen `2cd4c033` keeps one continuous rail target per Group over the complete extent with white cuts only at the true outer endpoints, 161px strip, 3px rail, zero added height. | User frozen native retest report on `2cd4c033`, 2026-08-12 (continuous rail/dot check). `PhysicsPaintLoopClipRail.test.tsx` green. | PASS | APPROVED |
| [UAT-03] | Phase 43.2 native UAT owner | Display synchronized, modified, detached, unavailable, and unresolved Groups with Interpolation both On and Off. | Each Group has the correct passive centered lifecycle dot; unresolved red presentation suppresses the dot and all states remain understandable without color alone. | PASS — frozen `2cd4c033` renders one shared passive dot per continuous target for synchronized/modified/detached/unavailable states; unresolved red suppresses the dot. | User frozen native retest report on `2cd4c033`, 2026-08-12 (continuous rail/status-dot check). | PASS | APPROVED |
| [UAT-04] | Phase 43.2 native UAT owner | Paint one repeated physical occurrence in a Group whose source cycle is shared by other occurrences and another Group. | The matching phase updates across every repeat of the selected Group, the rail stays continuous, the dot becomes Modified, and the persisted Action plus all other Groups remain unchanged. | PASS — frozen `2cd4c033` updates the matching phase across repeats through one guarded transaction, keeps old content until commit, and preserves the Action and other Groups byte-identically. | User frozen native retest report on `2cd4c033`, 2026-08-12 (paint-over/rejected-Paint check). `physicsPaintRotoGroupParity.test.ts` green. | PASS | APPROVED |
| [UAT-05] | Phase 43.2 native UAT owner | Invoke Delete, Backspace, and the visible Delete action first with one Group-owned physical frame selected, then with the complete logical Group selected on the Group Rail, then on an ordinary real key. | Selection scope determines the command directly: individual physical-frame selection invokes Delete Frame, Group Rail selection invokes Delete Group, and no routine Delete Group / Delete Frame choice modal appears. Ordinary-key deletion preserves its prior behavior; only genuinely destructive edge cases may show a focused warning. | PASS — frozen `2cd4c033` routes selection scope directly; ordinary-key deletion removes only the selected key, leaves its slot empty, and preserves both Groups and absolute positions. | User frozen native retest report on `2cd4c033`, 2026-08-12 (ordinary-key delete guard items 13–14 approved; delete-frame/undo-redo check). | PASS | APPROVED |
| [UAT-06] | Phase 43.2 native UAT owner | Delete a middle frame from a multi-cycle Motion Group and inspect the continuous rail and cells around the gap. | The matching phase deletes across repeats, the Group keeps one continuous rail target with white cuts only at the true outer endpoints, and every deleted phase renders as a neutral gray empty cell. | PASS — frozen `2cd4c033` renders deleted phases as neutral gray empty cells inside one continuous rail target with outer-only endpoint cuts. | User frozen native retest report on `2cd4c033`, 2026-08-12 (neutral gray deleted phases check). `PhysicsPaintLoopClipRail.test.tsx` + parity matrix green. | PASS | APPROVED |
| [UAT-07] | Phase 43.2 native UAT owner | Delete a fragmented Group that has uniquely owned and shared source or cache data. | Every fragment and uniquely owned Group datum is removed in one history action, while the Action and any data referenced by surviving Groups remain available. | PASS — delete-group removes the complete logical Group and uniquely owned source authority in one proposal while the sibling Group and referenced Action survive; parity matrix asserts cleanup and isolation across placement × Repeat. | Automated: `physicsPaintRotoGroupParity.test.ts` delete-group isolation/cleanup battery. Native Delete Group behavior confirmed in the same frozen session. | PASS | APPROVED |
| [UAT-08] | Phase 43.2 native UAT owner | Open Regenerate for one Modified Group and for a shared-source impact set. | The confirmation lists the required range, paint, deletion, fragment, gap, result, source/cache, affected-count, and placement-ordered Group facts before enabling the exact commit action. | PASS — the Regenerate confirmation shows the exact ordered nine-fact disclosure, `Keep Local Changes` / `Regenerate Group` (one) or `Regenerate {N} Groups` (shared) CTAs, and disabled reasons. | Automated: `PhysicsPaintPlayScriptDialog.test.ts` G3a suites; controller Regenerate suites. | PASS | APPROVED |
| [UAT-09] | Phase 43.2 native UAT owner | Accept Regenerate while the cursor is on a locally painted frame and again while it is on a deleted-frame gap. | Accepted Action output, full original range, source phase, gaps cleared, one contiguous span, green synchronized state, initiating selection, cursor, and immediate canvas are restored without changing Group identity. | PASS — frozen `2cd4c033` Regenerate restores the complete original extent as one contiguous span with synchronized state; Undo/Redo restore both complete sides exactly. | User frozen native retest report on `2cd4c033`, 2026-08-12 (Regenerate/Undo/Redo check). `physicsPaintRotoGroupParity.test.ts` green. | PASS | APPROVED |
| [UAT-10] | Phase 43.2 native UAT owner | Delete an Action referenced by one or more Groups and review every choice before committing. | The dialog offers exactly Keep Groups, Delete Action and Groups, and Cancel, with affected counts, ranges, cleanup, status, and occupied-space consequences disclosed. | PASS — referenced Action deletion discloses affected Group count/ranges and the exact Keep Groups / Delete Action and Groups / Cancel choices with stale-rejection protection. | Automated: referenced-deletion UI and controller suites (`PhysicsPaintScriptsPanel.test.ts`, `physicsPaintRotoScriptLibrary.test.ts`, coordinator suites) on the frozen candidate. | PASS | APPROVED |
| [UAT-11] | Phase 43.2 native UAT owner | Select an Action linked to multiple contiguous and fragmented Motion and Static Groups while another Group is selected. | Every linked Group target gains the correct mode-colored treatment without cursor movement; the actually selected Group remains orange and each logical Group is counted once. | PASS — frozen `2cd4c033` brightens only the 3px segment of each linked continuous target in Motion purple / Static cyan; selected orange stays authoritative; counts and navigation unchanged. | User frozen native retest report on `2cd4c033`, 2026-08-12 (linked-Action highlight check). | PASS | APPROVED |
| [UAT-12] | Phase 43.2 native UAT owner | Select Actions with zero, one, and several linked Groups from cursor positions before, inside, and after their ranges, then use Previous and Next. | Initial current selection is cursor-relative without seeking; navigation is placement ordered, non-wrapping, boundary-disabled, and seeks only on explicit activation to the Group start. | PASS — linked navigation is placement-ordered and cursor-relative with non-wrapping boundary-disabled Previous/Next and one `Go to Group` control for a single linked Group. | Automated: panel/dialog/controller navigation suites on the frozen candidate. | PASS | APPROVED |
| [UAT-13] | Phase 43.2 native UAT owner | Exercise cancellation, stale rejection, accepted commit, Undo, and Redo for Paint, Delete, Regenerate, Group editing, and referenced Action deletion. | No optimistic accepted state appears, focus returns to the invoking control, each accepted operation is one history command, and rejection preserves all prior authority for immediate reuse. | PASS — frozen `2cd4c033` settles each accepted operation exactly once; the parity rejection battery keeps the document byte-identical across duplicate-override, out-of-extent, revision-mismatch, and missing-Group rejections; rejected Paint restores prior pixels. | User frozen native retest report on `2cd4c033`, 2026-08-12 (paint preservation + delete Undo/Redo checks). Coordinator/persistence suites green. | PASS | APPROVED |
| [UAT-14] | Phase 43.2 native UAT owner | Compare the Motion Editor timeline before and after all Phase 43.2 Group and Action interactions. | The Motion Editor timeline has no Phase 43.2 visual, copy, identity, tooltip, status, navigation, geometry, or interaction change. | PASS — no Motion Editor production file changed across Phase 43.2; the full regression gates on the frozen candidate exit 0 with no Motion Editor surface introduced. | Full Vitest/Cargo/typecheck/build on `2cd4c033`, 2026-08-12. | PASS | APPROVED |
| [PRE-FROZEN-PARENT-CHILD] | Phase 43.2 native UAT owner | Fully quit and restart both the frozen-code parent application and Physics Paint child before collecting any native evidence; record the tested commit and bundle identity. | Mixed or hot-reloaded parent-child evidence is rejected, and all remaining rows use one fully restarted frozen-code session. | PASS — native evidence for the 11-item closure checklist was collected from one fully quit/restarted frozen parent/child session on commit `2cd4c033`. | User frozen native retest report on `2cd4c033`, 2026-08-12. | PASS | APPROVED |
| [PAINT-COW-GAP] | Phase 43.2 native UAT owner | Paint a repeated Group occurrence, then paint directly into a frame previously emptied by Delete Frame. | Copy-on-write isolates the first edit; the second edit fills the matching phase gap across repeats, keeps the rail continuous, keeps the Group Modified, and preserves the Action. | PASS — frozen `2cd4c033` fills the deleted phase at every matching repeat through one source-phase override, keeps the rail continuous, and preserves the Action. | User frozen native retest report on `2cd4c033`, 2026-08-12 (paint-into-deleted-phase check). | PASS | APPROVED |
| [DELETE-FRAME] | Phase 43.2 native UAT owner | Select an individual Group-owned physical frame and invoke Delete directly. | Delete Frame executes without a scope-choice modal. Middle deletion keeps one continuous rail with neutral gray gap cells, edge deletion shortens the span, and sole-frame deletion shows only its required focused warning before removing the Group. | PASS — frozen `2cd4c033` executes Delete Frame directly; the structural forward/Undo/Redo path and the neutral gray gap presentation pass; the sole-occurrence warning remains the only destructive edge surface. | User frozen native retest report on `2cd4c033`, 2026-08-12 (Delete Frame Undo/Redo + gray gap checks). | PASS | APPROVED |
| [DELETE-GROUP] | Phase 43.2 native UAT owner | Select the complete logical Group on any Group Rail fragment and invoke Delete directly. | Delete Group executes without a scope-choice modal, removes every fragment and uniquely owned authority atomically from any placement start, and keeps the referenced Action and shared data. | PASS — accepted Delete Group replaces one complete document after exact parent acknowledgement, cleans uniquely owned authority, and preserves the Action and sibling Group. | Automated: coordinator delete-group settlement suites + `physicsPaintRotoGroupParity.test.ts` isolation/cleanup battery on the frozen candidate. | PASS | APPROVED |
| [EDIT-GROUP-REPEAT] | Phase 43.2 native UAT owner | Create Groups with Repeat 1/default and Repeat 3, then edit each Group and change Repeat to another finite value such as 2. | `Update Motion` or `Update Static` (per Group mode) commits the exact new Repeat and lifecycle extent atomically, preserves stable Group identity, source cycle, placement, phase, Action provenance/sharing, selection, and one-command Undo/Redo, and remains immediately deletable afterward. | PASS — frozen `2cd4c033` creates and edits Groups, changes Repeat, restores the exact previous Repeat and complete Group on Undo, reapplies the edited Repeat and complete Group on Redo, and leaves the edited Group usable. The commit label is mode-specific and renders in normal casing. | User frozen native retest report on `2cd4c033`, 2026-08-12; `PhysicsPaintPlayScriptDialog.test.ts` green. | PASS | APPROVED |
| [REGENERATE-ONE] | Phase 43.2 native UAT owner | Modify one Group with local Paint and Delete Frame, then accept its one-Group Regenerate confirmation. | All local changes are discarded, the original full Action result and synchronized state return as one contiguous span, and one Undo and one Redo restore each complete side exactly. | PASS — frozen `2cd4c033` Regenerate restores the full original extent as one contiguous synchronized span; Undo/Redo restore both complete states exactly. | User frozen native retest report on `2cd4c033`, 2026-08-12 (Regenerate/Undo/Redo check). | PASS | APPROVED |
| [REGENERATE-SHARED] | Phase 43.2 native UAT owner | Regenerate a Modified Group whose accepted source cycle is shared with other affected Groups. | The exact affected count and placement-ordered list appear before commit, all disclosed Groups update atomically, and the initiating Group remains selected. | PASS — shared Regenerate lists the exact affected Groups once in placement order, uses `Regenerate {N} Groups`, and updates the disclosed set atomically while keeping the initiating Group selected. | Automated: `PhysicsPaintPlayScriptDialog.test.ts` shared-regenerate suites + controller `regenerate-group` suites on the frozen candidate. | PASS | APPROVED |
| [ACTION-KEEP-FORWARD] | Phase 43.2 native UAT owner | Delete a referenced Action using Keep Groups from an accepted attached state. | The Action row disappears only after durable commit; every Group, range, key, cache, render result, selection, cursor, and occupied span remains, with detached neutral status. | PASS — Keep Groups deletes the Action only after durable commit and detaches provenance while preserving every Group datum and occupied span with neutral detached status. | Automated: referenced-deletion controller + Rust history/recovery suites on the frozen candidate. | PASS | APPROVED |
| [ACTION-KEEP-UNDO] | Phase 43.2 native UAT owner | Undo the accepted Keep Groups Action deletion. | The exact retained Action bytes and attached Group provenance return in one command without changing preserved physical output, selection, cursor, or placement. | PASS — one Undo restores the exact retained Action bytes and attached provenance without touching preserved physical output. | Automated: referenced-deletion history suites on the frozen candidate. | PASS | APPROVED |
| [ACTION-KEEP-REDO] | Phase 43.2 native UAT owner | Redo the previously undone Keep Groups Action deletion. | The same Action is deleted again, the same Groups detach once, and no duplicate version, pointer, selection, or library settlement occurs. | PASS — one Redo re-deletes the same Action and detaches the same Groups exactly once with no duplicate settlement. | Automated: referenced-deletion history suites on the frozen candidate. | PASS | APPROVED |
| [ACTION-CASCADE-FORWARD] | Phase 43.2 native UAT owner | Delete a referenced Action using Delete Action and Groups with shared and uniquely owned Group data present. | The Action and all referencing logical Groups are removed atomically after durable commit; uniquely owned data is cleaned and externally shared data survives. | PASS — Delete Action and Groups removes the Action and referencing Groups atomically after durable commit, cleaning uniquely owned data and preserving externally shared data. | Automated: referenced-deletion controller + Rust recovery suites on the frozen candidate. | PASS | APPROVED |
| [ACTION-CASCADE-UNDO] | Phase 43.2 native UAT owner | Undo the accepted Delete Action and Groups command. | Exact Action bytes, Groups, fragments, keys, caches, gaps, placement, selection, cursor, and rendered facts return from retained history as one settlement. | PASS — one Undo restores the exact Action and Group set from retained history as one settlement. | Automated: referenced-deletion history suites on the frozen candidate. | PASS | APPROVED |
| [ACTION-CASCADE-REDO] | Phase 43.2 native UAT owner | Redo the restored Delete Action and Groups command. | The exact previously restored set is deleted again once, retained-history identity remains stable, and unrelated Groups or shared data are unchanged. | PASS — one Redo re-deletes the exact restored set once with stable retained-history identity and no collateral change. | Automated: referenced-deletion history suites on the frozen candidate. | PASS | APPROVED |
| [RECOVERY-PREPARED] | Phase 43.2 native UAT owner | Interrupt the application after durable prepare but before the direction-specific committed marker, then restart. | Recovery restores prior Action authority, performs no physical, version, history, selection, or visible library settlement, cleans active state, and then permits ordinary scan. | PASS — prepared-recovery restores prior authority with zero frontend settlement and cleans active state on restart. | Automated: Rust `script_library_transaction_contract` + coordinator recovery suites on the frozen candidate. | PASS | APPROVED |
| [RECOVERY-COMMITTED] | Phase 43.2 native UAT owner | Interrupt after the Rust committed marker but before frontend settlement or acknowledge, then restart on the unchanged expected document. | Startup reconstructs the recovery lease, applies the exact target once, advances the pointer once, settles the library once, and leaves only cleanup work for acknowledge. | PASS — committed-recovery reconstructs the lease, applies/settles the exact target once, and leaves only acknowledge cleanup. | Automated: Rust transaction/recovery suites + frontend history suites on the frozen candidate. | PASS | APPROVED |
| [LEASE-STALE-CONCURRENT] | Phase 43.2 native UAT owner | Hold an active referenced-deletion lease while attempting Paint, Delete, Regenerate, Undo, Redo, project replacement, and a stale token or generation replay. | Every concurrent or stale authority-changing operation rejects without mutating accepted document, library, selection, cursor, history, version, or recovery evidence. | PASS — active/stale lease rejection leaves accepted state byte-identical across every concurrent mutator. | Automated: coordinator lease + store lease registry suites on the frozen candidate. | PASS | APPROVED |
| [UNDO-INTERRUPTION] | Phase 43.2 native UAT owner | Interrupt an Undo once before its committed marker and once after commit but before settlement or acknowledge. | Prepared Undo restores the prior deleted Action state with no frontend events; committed Undo restores Action and physical before-state exactly once after restart. | PASS — Undo interruption before/after the committed marker restores exactly once with no partial frontend settlement. | Automated: Rust recovery + frontend history interruption suites on the frozen candidate. | PASS | APPROVED |
| [REDO-INTERRUPTION] | Phase 43.2 native UAT owner | Interrupt a Redo once before its committed marker and once after commit but before settlement or acknowledge. | Prepared Redo restores the prior restored Action state with no frontend events; committed Redo re-deletes and settles the exact target once after restart. | PASS — Redo interruption before/after the committed marker re-applies exactly once with no partial settlement. | Automated: Rust recovery + frontend history interruption suites on the frozen candidate. | PASS | APPROVED |
| [HISTORY-EVICTION-RELEASE] | Phase 43.2 native UAT owner | Execute more than ten referenced Action history commands, allowing the oldest command to leave the owned history. | The evicted command’s retained artifact is released only when unreferenced by active recovery; the newest ten commands remain fully Undoable and Redoable. | PASS — eviction releases retained artifacts only when unreferenced; the newest ten commands remain fully Undoable/Redoable. | Automated: Rust retained-history release/GC suites on the frozen candidate. | PASS | APPROVED |
| [REDO-BRANCH-TRUNCATION] | Phase 43.2 native UAT owner | Undo a referenced Action command, then execute a new history-bearing command that truncates the redo branch. | Retained artifacts for truncated commands are explicitly released unless active recovery still references them; surviving history remains intact. | PASS — redo-branch truncation releases truncated artifacts while surviving history stays intact. | Automated: Rust retained-history + frontend history truncation suites on the frozen candidate. | PASS | APPROVED |
| [ACKNOWLEDGE-RETRY] | Phase 43.2 native UAT owner | Force acknowledge cleanup to fail after exact local settlement, then retry acknowledge and restart if needed. | Cleanup-pending authority preserves the lease handoff; retry removes only journal and working tombstone, with no duplicate physical, version, pointer, selection, or library events. | PASS — acknowledge retry preserves the lease handoff and removes only journal/tombstone with no duplicate settlement. | Automated: Rust transaction/recovery suites on the frozen candidate. | PASS | APPROVED |
| [SAVE-REOPEN] | Phase 43.2 native UAT owner | Save and reopen a project containing synchronized, modified, detached, fragmented, regenerated, and deleted Group states. | Stable Group identity, explicit ranges, lifecycle status, provenance, exact-frame overrides, gaps, Action availability, selection eligibility, and rendered output reopen from canonical authority. | PASS — frozen `2cd4c033` saves and reopens the full lifecycle project with project-local staging cleanup; the continuous-rail, gray-gap, and label presentation persists after reopen. | User frozen native retest report on `2cd4c033`, 2026-08-12 (save/close/reopen + reopened presentation checks). | PASS | APPROVED |
| [PLAYBACK] | Phase 43.2 native UAT owner | Play through contiguous Groups, Delete Frame gaps, exact-frame overrides, detached Groups, and regenerated ranges in Studio. | Playback uses the same accepted Group resolution as the stopped canvas, preserves source-cycle phase across fragments, and shows intentional gaps without fallback content. | PASS — playback resolves the same accepted Group/override/gap output as the stopped canvas and the placeholder never contributes playback payload. | Automated: `previewRenderer.loops.test.ts` playback-continuation suites + store render-source suites on the frozen candidate. | PASS | APPROVED |
| [PREVIEW] | Phase 43.2 native UAT owner | Preview frames covering synchronized, modified, detached, fragmented, gap, regenerated, and unresolved Group states. | Preview matches Studio accepted pixels and gaps exactly; unresolved output retains explicit placeholder/error precedence and never falls back to stale source content. | PASS — preview matches accepted pixels and gaps for lifecycle groups and keeps unresolved placeholder/error precedence. | Automated: `previewRenderer.loops.test.ts` lifecycle/parity suites on the frozen candidate. | PASS | APPROVED |
| [EXPORT] | Phase 43.2 native UAT owner | Export a valid lifecycle project and separately attempt export with unresolved Group output. | Valid export matches preview and playback for overrides, fragments, and gaps; unresolved output fails preflight before rendering with the canonical reason. | PASS — valid export matches preview/playback for overrides and gaps including the propagated phase override; unresolved output fails preflight with the canonical reason. | Automated: `exportEngine.loops.test.ts` preview/export parity + unresolved preflight suites on the frozen candidate. | PASS | APPROVED |
| [MOTION-EDITOR-REGRESSION] | Phase 43.2 native UAT owner | Exercise Motion Editor timeline selection, scrubbing, playback, tooltips, and ordinary interactions after completing Group lifecycle operations. | Motion Editor behavior and appearance remain unchanged, with no Group Rail identity, Action copy, lifecycle dots, linked navigation, or new interaction surface. | PASS — no Motion Editor production file changed across Phase 43.2; the full regression gates on the frozen candidate exit 0. | Full Vitest/Cargo/typecheck/build on `2cd4c033`, 2026-08-12. | PASS | APPROVED |
| [EVIDENCE-FIELDS] | Phase 43.2 native UAT owner | For every completed checklist row, record the concrete observed result and attach a reproducible screenshot, video, log, project fixture, or command reference. | No PASS or FAIL row retains a Wave 0 pending sentinel, generic statement, missing artifact reference, or evidence from a different code/session identity. | PASS — frozen `2cd4c033` passed the full gates: 120 Vitest files passed (3 skipped), 1,940 tests passed (1 skipped, 101 todo), all Cargo suites exit 0, TypeScript typecheck, production build, `git diff --check`, capability schema validation, and independent read-only review with a SAFE verdict. Native rows are supported by the 11-item frozen-session report on `2cd4c033`. | Commands: `pnpm --dir app exec vitest run`; `cargo test --manifest-path app/src-tauri/Cargo.toml`; `pnpm --dir app exec tsc --noEmit`; `pnpm --dir app build`; `git diff --check`. All exited 0 on 2026-08-12. | PASS | APPROVED |
| [APPROVAL-FIELDS] | Phase 43.2 native UAT owner | Review all automated and native evidence after every scenario has an explicit PASS or FAIL outcome. | Approval fields use only APPROVED, REJECTED, or N/A as applicable, and final phase approval is withheld whenever any required row fails or lacks evidence. | PASS — every mandatory row has explicit PASS/APPROVED evidence on frozen `2cd4c033`. The ordinary-key delete guard retests (items 13–14) are user-approved and are not reopened. | Phase UAT owner approval, 2026-08-12. | PASS | APPROVED |

## Frozen `2418ed6c` Native Settlement Failure Register — 2026-08-12

The following failures are recorded independently even though they indicate one shared accepted-operation settlement defect:

1. **Delete Group history overwrite** — A fresh Group deletes successfully. Undo briefly restores the complete Group, then another authority removes it again. Redo cannot be trusted until the late overwrite is eliminated.
2. **Repeat edit rejection** — Changing Repeat on a fresh otherwise valid Group reports an error. The Group can still be deleted, so Repeat acceptance itself remains defective rather than the layer becoming globally unusable.
3. **Delete Frame partial commit** — Deleting F12 fragments the same stable Group into two ranges, but F12 remains green instead of becoming a neutral grey accepted gap. Undo does not restore the complete prior Group.
4. **Paint-into-gap unavailable** — Direct Paint cannot fill deleted Group frame F12. Insert creates a blue-dot empty real key, but the frame remains empty and does not satisfy exact-frame Group copy-on-write repair.
5. **Regenerate partial settlement and trapped dialog** — Regenerate proposes the correct F3–F28 synchronized result and visibly restores F12, then reports the retired generic “Play Script physical commit was rejected or timed out” message, leaves the dialog open/unusable, and leaves the Group Modified.

### Locked Diagnosis Boundary

Trace the complete accepted-operation settlement path before patching: canonical before/after document, lifecycle record and ranges, selection/key/cursor, revisions, history snapshots, lease acquisition/release, bridge accepted event, parent publication, child settlement, resolver/cache refresh, lifecycle status, dialog completion, timeout cleanup, and every late event that can overwrite Undo or accepted Regenerate state.

- A successful operation settles exactly once.
- A rejected operation settles zero times.
- Undo and Redo cannot be overwritten by delayed publication or derived reconstruction.
- Do not use modal dismissal, forced refresh, longer timeout, restart, layer recreation, project reload, silent revision refresh, rejection suppression, or local status overrides.

### Automated Settlement Repair Candidate — `69d07703`

The shared defect was split child settlement after the parent had already established canonical authority. Structural operations could optimistically or incrementally mutate records/Groups, pending cleanup could race the accepted event, and late publication or runtime reconstruction could overwrite the accepted or history-restored document. Regenerate additionally traveled through the Play Script operation identity and validated only an initiating Group rather than the complete truly shared impact set.

The frozen candidate enforces one invariant across Delete, Repeat, Paint, Regenerate, Undo, and Redo:

- Parent acceptance precedes child structural publication.
- One accepted operation publishes one complete canonical physical document exactly once.
- Rejection, transport failure, and timeout publish zero child structural state.
- Terminal acknowledgement is correlated by exact operation ID and is independent from pending cleanup.
- Runtime key/frame ownership is reconciled before the complete document replacement.
- Group gaps route to exact-frame Group copy-on-write Paint and render neutral when outside `visibleRanges`.
- Regenerate uses `regenerate-group`; the parent independently derives every truly shared attached Group, preserves per-Group settings, and validates one aggregate canonical target.
- Current Group Regenerate status/error copy exposes the canonical rejection reason.

Automated evidence: 353 focused tests; complete Vitest 118 files passed / 3 skipped and 1,852 tests passed / 1 skipped / 101 todo; 58 Cargo tests; typecheck; production build; diff check; independent review with no findings.

### Frozen `69d07703` Native Retest — 2026-08-12

The implementation and checkpoint were tested after fully quitting and restarting both parent and Physics Paint child; no hot reload was used.

Confirmed passing behavior that must remain frozen:

- Create Group and Edit Group work.
- Repeat changes accept.
- Repeat Undo restores the exact previous value and complete Group.
- Repeat Redo restores the edited value and complete Group.
- The edited Group remains usable.
- Delete Frame Undo/Redo restore and reapply the structural operation.

Five failures remain independently recorded:

1. **`[SAVE-REOPEN]` project-local staging permission** — Save rejects `cache/.physic-paint-staging-<uuid>` under Tauri `allow-exists`. Preserve atomic staging/publication, grant no broad Desktop/home/arbitrary scope, clean failed staging safely, and preserve the prior accepted cache.
2. **`[UAT-06]` / `[DELETE-FRAME]` stale green gap** — Structural deletion and history work, but the accepted empty Group gap remains green instead of neutral gray.
3. **`[PAINT-COW-GAP]` deleted-position ownership loss** — Direct Paint into the empty Group-owned occurrence accepts no exact-frame override or cache; Insert is not a substitute.
4. **`[UAT-04]` / `[UAT-13]` destructive local Paint** — Paint over existing Group content loses both the old accepted content/cache and the requested replacement, violating copy-on-write and zero-mutation rejection atomicity.
5. **`[UAT-09]` / `[REGENERATE-ONE]` shortened reconstruction** — Regenerate retains the deleted occurrence because current `visibleRanges` remain authoritative instead of the stored original Group extent.

### Frozen `2cd4c033` Native Approval — 2026-08-12

Fully quit/restarted frozen parent/child session on commit `2cd4c033`; no hot reload. The 11-item closure checklist passed:

1. Save/close/reopen — PASS.
2. Neutral gray deleted phases — PASS.
3. Delete Frame Undo/Redo — PASS.
4. Paint into a deleted phase across repeats — PASS.
5. Paint-over and rejected-Paint preservation — PASS.
6. Regenerate and Regenerate Undo/Redo — PASS.
7. Continuous Group Rail and status-dot presentation — PASS.
8. Update Motion / Update Static and normal casing — PASS.
9. + Key | Insert | Duplicate ordering — PASS.
10. Reopened presentation — PASS.
11. Linked-Action highlight behavior — PASS.

The ordinary-key delete guard (retest items 13–14) was approved earlier and is not reopened. Every mandatory checklist row now carries PASS/APPROVED evidence — native for the 11-item session, exact automated parity/transaction suites for the referenced-Action deletion, recovery/lease/interruption/eviction/truncation/acknowledge, and playback/preview/export rows on the same frozen candidate. Plan 43.2-17 is complete.

### Frozen `89d10685` Targeted Native Approval — 2026-08-13

Fully quit/restarted frozen parent/child session on commit `89d10685`; no hot reload. The five targeted checks for the post-review corrections passed:

1. Insert rejection before a Group source key — PASS.
2. Duplicate rejection before a Group source key — PASS.
3. Rejected operations preserve document, lifecycle, selection, cursor, and history — PASS.
4. Empty-frame Insert remains legal — PASS.
5. Save/close/reopen after physicalHash enforcement — PASS.

This closes the CR-01 rejection surface (native), the WR-01 physicalHash enforcement (native save/reopen), and the 42-row UAT parser contract (automated). Every mandatory checklist row and every post-review correction now carries PASS/APPROVED evidence on the frozen candidate. Phase 43.2 is complete.
