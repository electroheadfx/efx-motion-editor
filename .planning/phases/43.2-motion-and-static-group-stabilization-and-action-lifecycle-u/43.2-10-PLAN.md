---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 10
type: execute
wave: 8
depends_on: [43.2-09]
files_modified:
  - app/src-tauri/src/services/script_library.rs
  - app/src-tauri/src/commands/script_library.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/script_library_transaction_contract.rs
  - app/src-tauri/tests/script_library_command_contract.rs
autonomous: true
requirements: [GRP-06]
estimate: { tokens: 38000, raw_tokens: 38000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Per D-25–D-28/D-35, Rust owns temporary prepared/committed/acknowledged active recovery state with hidden working tombstone, exact physical target payload, atomic marker, scan gating, and restart recovery."
    - "Active recovery state is structurally separate from the retained Action history artifact implemented by Plan 19; acknowledge cleans only active journal/tombstone state."
  artifacts:
    - app/src-tauri/src/services/script_library.rs
    - app/src-tauri/src/commands/script_library.rs
  key_links:
    - "prepare journal -> commit tombstone/marker -> recovery-required scan -> acknowledge cleanup"
---
<objective>
Implement the temporary durable Rust Action transaction journal, working tombstone, command surface, scan gating, and restart recovery foundation.

Purpose: Build the filesystem authority required by GRP-06 and D-25 through D-28 without partial publication.
Output: Closed service/commands, durable markers, recovery, idempotent acknowledge, and exact Cargo coverage.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-03-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md</context>

## Artifacts this phase produces

A crash-recoverable Rust transaction whose ordinary scans can never expose a half-deleted Action library.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement durable prepare records and closed validation</name>
  <read_first>app/src-tauri/src/services/script_library.rs managed path, revision, atomic temp-write, scan, and delete helpers; app/src-tauri/tests/script_library_transaction_contract.rs prepare fixtures; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md Pattern 4 steps 1–2</read_first>
  <files>app/src-tauri/src/services/script_library.rs, app/src-tauri/tests/script_library_transaction_contract.rs</files>
  <behavior>
    - prepare validates closed mode, managed path/symlink policy, Action/physical expected authorities, exact impact digest, and complete target recovery payload;
    - hidden active record contains token, mode, direction, stable history command ID/generation, expected authorities, retained-artifact reference, and exact target physical/selection payload;
    - Action remains logically visible and ordinary state is unchanged.
  </behavior>
  <action>Implement unique-token active prepared records using existing atomic temp-write/rename, file sync, and directory sync patterns under the authority lock. Keep active journal paths distinct from Plan 19 retained-history paths. Require exact target physical document and selection/cursor recovery payload plus lease/authority correlation; reject unknown fields/modes/directions, stale authorities, conflicting transactions, and replayed tokens. Do not move the Action or publish a deletion during prepare.</action>
  <verify><automated>cargo test --manifest-path app/src-tauri/Cargo.toml --test script_library_transaction_contract prepare</automated></verify>
  <acceptance_criteria>Closed request, stale authority, replay, durable payload, sync, and logically visible Action tests pass.</acceptance_criteria>
  <done>Prepare is durable and mutation-free from the user's accepted perspective.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement commit marker, scan gating, and recovery</name>
  <read_first>app/src-tauri/src/services/script_library.rs scan and direct delete implementation; app/src-tauri/tests/script_library_transaction_contract.rs commit/recovery fixtures; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md Pattern 4 steps 3 and 6</read_first>
  <files>app/src-tauri/src/services/script_library.rs, app/src-tauri/tests/script_library_transaction_contract.rs</files>
  <behavior>
    - commit moves Action to hidden tombstone then atomically records committed;
    - before marker recovery restores Action before normal scans; committed-unacknowledged scans return recovery-required;
    - startup recovery is deterministic and stale/replayed tokens fail closed.
  </behavior>
  <action>Implement commit and startup recovery under the same authority lock. Sync tombstone and marker directory transitions. Ordinary scan must gate on unresolved transaction state; it never returns a normal list from a half-state. Preserve direct delete only for unreferenced Actions.</action>
  <reversibility rating="costly">Durable on-disk journal format governs crash recovery and must remain backward-readable within this phase.</reversibility>
  <verify><automated>cargo test --manifest-path app/src-tauri/Cargo.toml --test script_library_transaction_contract</automated></verify>
  <acceptance_criteria>Commit marker, pre-marker restoration, committed restart recovery, scan gating, interruption, and no ordinary half-state scan pass.</acceptance_criteria>
  <done>Rust can distinguish and recover every interrupted logical state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Expose closed commands and idempotent acknowledge cleanup</name>
  <read_first>app/src-tauri/src/commands/script_library.rs request/result facades; app/src-tauri/src/lib.rs invoke_handler registration; app/src-tauri/tests/script_library_command_contract.rs command fixtures; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md Pattern 4 step 5</read_first>
  <files>app/src-tauri/src/commands/script_library.rs, app/src-tauri/src/lib.rs, app/src-tauri/src/services/script_library.rs, app/src-tauri/tests/script_library_command_contract.rs</files>
  <behavior>
    - prepare/commit/status/recover/acknowledge commands use exact request/result variants;
    - acknowledge removes only the working tombstone/active journal idempotently, preserves any retained history artifact, and does not reverse committed state;
    - unknown/stale/replayed tokens fail closed while repeated acknowledge succeeds safely.
  </behavior>
  <action>Add thin command facades and handler registration; keep orchestration/recovery in the service. Acknowledge failure leaves durable cleanup-pending ownership for the Plan 18 lease handoff, does not alter committed logical status, and never removes the retained artifact referenced by the history command.</action>
  <verify><automated>cargo test --manifest-path app/src-tauri/Cargo.toml --test script_library_command_contract && cargo test --manifest-path app/src-tauri/Cargo.toml services::script_library</automated></verify>
  <acceptance_criteria>Service recovery, command recovery, exact variants, stale/replayed tokens, acknowledge idempotence, and handler registration pass.</acceptance_criteria>
  <done>The Rust journal protocol is closed, recoverable, and callable end to end.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| frontend request→filesystem | Paths, modes, revisions, tokens, and payloads are untrusted. |
| journal→ordinary scan | Recovery state must gate visibility. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-03 | Tampering | Rust journal | high | mitigate | ASVS L1 closed allowlists/expected-authority checks, managed path policy, durable marker, replay rejection, Cargo recovery evidence. |
| T-43.2-R1 | Repudiation | token/state transitions | medium | mitigate | Unique token and durable explicit states. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>All concrete Cargo commands pass from the Tauri manifest; then existing crate tests pass.</verification>
<success_criteria>No Rust path can expose an ordinary half-deleted library, including after interruption/restart.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-10-SUMMARY.md` when done.</output>
