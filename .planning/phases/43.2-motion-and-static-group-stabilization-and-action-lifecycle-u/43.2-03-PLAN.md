---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src-tauri/tests/script_library_transaction_contract.rs
  - app/src-tauri/tests/script_library_command_contract.rs
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts
  - app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts
  - .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
autonomous: true
requirements: [GRP-06, GRP-08]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Per D-25–D-28/D-35, passing contracts separate active recovery journal/tombstone from a retained Action history artifact and define forward, Undo, and Redo as one leased bidirectional history command."
    - "Per D-39, one owned checklist requires a fully restarted frozen-code parent/child native session."
  artifacts:
    - app/src-tauri/tests/script_library_transaction_contract.rs
    - app/src-tauri/tests/script_library_command_contract.rs
    - .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
  key_links:
    - "durable journal fixtures -> frontend event ledger -> final frozen-session UAT checklist"
---
<objective>
Create passing Wave 0 retained-history, bidirectional Action-deletion/replay, release/GC, and deterministic native-UAT contracts without changing Rust or TypeScript production authority.

Purpose: Remove the partial-commit architecture before GRP-06 implementation and own D-39 acceptance evidence.
Output: Passing Rust/frontend harness contracts and the final native checklist.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UI-SPEC.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md</context>

## Artifacts this phase produces

A passing journal/recovery state-machine contract and the checklist consumed by the final blocking native checkpoint.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock durable Rust journal and recovery states</name>
  <read_first>app/src-tauri/src/services/script_library.rs symbols delete_script and managed_path; app/src-tauri/src/commands/script_library.rs delete command facade; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md Pattern 4</read_first>
  <files>app/src-tauri/tests/script_library_transaction_contract.rs, app/src-tauri/tests/script_library_command_contract.rs</files>
  <behavior>
    - fixtures separate prepared/committed/acknowledged active recovery state from retained history artifacts keyed by stable command ID and generation;
    - forward, Undo restore, and Redo re-delete each have direction-specific target physical/selection payloads, committed markers, interruption and restart recovery;
    - history release covers ten-level eviction, redo-branch truncation, and intentional clear while protecting artifacts referenced by active recovery;
    - stale/replayed directions/tokens/generations, scan gating, orphan cleanup, and acknowledge idempotence are explicit.
  </behavior>
  <action>Create standalone integration contract harnesses using fixture parsers/state transitions and controlled implementation markers so Cargo exits 0 before production support. Require closed modes and directions, stable history command ID, generation, exact original managed Action bytes and path/revision/integrity metadata, exact physical/selection before-after revisions/hashes, hidden working tombstone, direction-specific active recovery journal, atomic committed marker, cleanup-only acknowledge, explicit release/GC, active-recovery reference protection, and recovery-required scans per D-35.</action>
  <verify><automated>cargo test --manifest-path app/src-tauri/Cargo.toml --test script_library_transaction_contract --test script_library_command_contract</automated></verify>
  <acceptance_criteria>Service/command forward-Undo-Redo recovery, retained-artifact integrity, marker behavior, stale/replayed directions/tokens/generations, scan gating, prepared restoration, committed restart recovery, eviction/release, redo truncation, orphan cleanup, active-recovery protection, acknowledge idempotence, and no ordinary half-state scan all pass as contracts.</acceptance_criteria>
  <done>The Rust protocol is executable and green before production files change.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Lock frontend committed-only settlement events</name>
  <read_first>app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts rows/generation/delete state fixtures; app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts request correlation and deletion orchestration cases; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md decisions D-25–D-28</read_first>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts, app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts</files>
  <behavior>
    - forward/Undo/Redo prepare and commit-pending produce zero physical replacements, version bumps, history-pointer moves, selection changes, and visible library settlements;
    - committed local settlement produces exactly one replacement, version, pointer movement, selection, and library event; acknowledge failure produces none additional;
    - prepared failure restores prior Action authority, committed recovery settles once under reconstructed lease, and invariant mismatch never overwrites a newer document;
    - eviction, redo truncation, and session clear issue release only for unreferenced retained artifacts.
  </behavior>
  <action>Add passing fake-port event-ledger tests and controlled markers for the leased durable protocol. Cover Keep Groups and cascade forward/Undo/Redo exact physical and provenance facts, history pointer movement only after accepted settlement, interruption in both replay directions, stale/replayed operations, cleanup-pending acknowledge handoff, retained-artifact release triggers, and newer-document recovery rejection. Preserve simple unreferenced deletion per D-25.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts</automated></verify>
  <acceptance_criteria>Every forward/Undo/Redo pre-commit zero-event and post-commit exact-once assertion passes, including interruption, acknowledge failure, startup lease recovery, stale/replayed rejection, eviction/release, redo truncation, and newer-document protection.</acceptance_criteria>
  <done>The frontend cannot claim an accepted referenced deletion before Rust committed.</done>
</task>
<task type="auto">
  <name>Task 3: Own the structurally verified frozen-session native UAT checklist</name>
  <read_first>.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UI-SPEC.md Visual and Interaction Acceptance Matrix; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md decision D-39; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-VALIDATION.md Per-Task Verification Map</read_first>
  <files>.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md</files>
  <action>Create exactly one Markdown checklist table with the header `| ID/Anchor | Owner | Scenario / Precondition | Expected Result | Observed Result | Evidence | Status | Approval |` and separator `|---|---|---|---|---|---|---|---|`. Populate one row for each `[UAT-01]` through `[UAT-14]` and `[PRE-FROZEN-PARENT-CHILD]`, `[PAINT-COW-GAP]`, `[DELETE-FRAME]`, `[DELETE-GROUP]`, `[REGENERATE-ONE]`, `[REGENERATE-SHARED]`, `[ACTION-KEEP-FORWARD]`, `[ACTION-KEEP-UNDO]`, `[ACTION-KEEP-REDO]`, `[ACTION-CASCADE-FORWARD]`, `[ACTION-CASCADE-UNDO]`, `[ACTION-CASCADE-REDO]`, `[RECOVERY-PREPARED]`, `[RECOVERY-COMMITTED]`, `[LEASE-STALE-CONCURRENT]`, `[UNDO-INTERRUPTION]`, `[REDO-INTERRUPTION]`, `[HISTORY-EVICTION-RELEASE]`, `[REDO-BRANCH-TRUNCATION]`, `[ACKNOWLEDGE-RETRY]`, `[SAVE-REOPEN]`, `[PLAYBACK]`, `[PREVIEW]`, `[EXPORT]`, `[MOTION-EDITOR-REGRESSION]`, `[EVIDENCE-FIELDS]`, and `[APPROVAL-FIELDS]`. Set every Owner cell exactly to `Phase 43.2 native UAT owner`; write concrete non-placeholder Scenario/Precondition and Expected Result text; initialize Observed Result to `PENDING — record during Plan 17 Task 3`, Evidence to `PENDING — attach during Plan 17 Task 3`, Status to `PENDING`, and Approval to `PENDING` or `N/A`. The precondition row's Scenario must contain `Fully quit and restart both the frozen-code parent application and Physics Paint child`, and its Expected Result must contain `Mixed or hot-reloaded parent-child evidence is rejected`, enforcing D-39. Plan 17 Task 3 replaces sentinels with actual observations/evidence and `PASS`/`FAIL` plus the explicit approval enum.</action>
  <verify><automated>f=.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md; test -f "$f" && node -e 'const fs=require("fs");const p=process.argv[1];const s=fs.readFileSync(p,"utf8");const req=[...Array.from({length:14},(_,i)=>`UAT-${String(i+1).padStart(2,"0")}`),"PRE-FROZEN-PARENT-CHILD","PAINT-COW-GAP","DELETE-FRAME","DELETE-GROUP","REGENERATE-ONE","REGENERATE-SHARED","ACTION-KEEP-FORWARD","ACTION-KEEP-UNDO","ACTION-KEEP-REDO","ACTION-CASCADE-FORWARD","ACTION-CASCADE-UNDO","ACTION-CASCADE-REDO","RECOVERY-PREPARED","RECOVERY-COMMITTED","LEASE-STALE-CONCURRENT","UNDO-INTERRUPTION","REDO-INTERRUPTION","HISTORY-EVICTION-RELEASE","REDO-BRANCH-TRUNCATION","ACKNOWLEDGE-RETRY","SAVE-REOPEN","PLAYBACK","PREVIEW","EXPORT","MOTION-EDITOR-REGRESSION","EVIDENCE-FIELDS","APPROVAL-FIELDS"];const header="| ID/Anchor | Owner | Scenario / Precondition | Expected Result | Observed Result | Evidence | Status | Approval |";const sep="|---|---|---|---|---|---|---|---|";const lines=s.split(/\r?\n/);const hs=lines.map((l,i)=>l.trim()===header?i:-1).filter(i=>i>=0);if(hs.length!==1)throw Error("checklist header must occur exactly once");const h=hs[0];if((lines[h+1]||"").trim()!==sep)throw Error("malformed checklist separator");const rows=[];for(let i=h+2;i<lines.length&&lines[i].trim().startsWith("|");i++)rows.push(lines[i].trim());if(rows.length!==req.length)throw Error(`expected ${req.length} checklist rows, got ${rows.length}`);const seen=new Set();const byId=new Map();const bad=/^(?:TBD|TODO|PENDING|N\/A|NA|NONE|NULL|-|—|\.\.\.)$/i;const obsSent="PENDING — record during Plan 17 Task 3";const evSent="PENDING — attach during Plan 17 Task 3";for(const row of rows){if(!row.endsWith("|"))throw Error("malformed checklist row");const c=row.slice(1,-1).split("|").map(v=>v.trim());if(c.length!==8)throw Error(`malformed column count: ${row}`);if(c.some(v=>!v))throw Error(`empty checklist field: ${row}`);if(!/^\[[A-Z0-9-]+\]$/.test(c[0]))throw Error(`malformed anchor: ${c[0]}`);if(seen.has(c[0]))throw Error(`duplicate anchor: ${c[0]}`);seen.add(c[0]);byId.set(c[0],c);if(c[1]!=="Phase 43.2 native UAT owner")throw Error(`wrong owner: ${c[0]}`);if(bad.test(c[2])||bad.test(c[3])||/\b(?:TBD|TODO)\b/i.test(`${c[2]} ${c[3]}`))throw Error(`placeholder scenario/expected result: ${c[0]}`);if(!/^(?:PENDING|PASS|FAIL)$/.test(c[6]))throw Error(`invalid status: ${c[0]}`);if(!/^(?:PENDING|N\/A|APPROVED|REJECTED)$/.test(c[7]))throw Error(`invalid approval: ${c[0]}`);if(c[6]==="PENDING"){if(c[4]!==obsSent||c[5]!==evSent)throw Error(`missing explicit pending sentinels: ${c[0]}`)}else if(c[4]===obsSent||c[5]===evSent||bad.test(c[4])||bad.test(c[5])||/\b(?:TBD|TODO)\b/i.test(`${c[4]} ${c[5]}`))throw Error(`missing populated UAT evidence: ${c[0]}`)}for(const id of req){const key=`[${id}]`;if(!byId.has(key))throw Error(`missing checklist row: ${key}`);const count=[...s.matchAll(/\[([A-Z0-9-]+)\]/g)].filter(m=>m[1]===id).length;if(count!==1)throw Error(`anchor must occur exactly once in the checklist table: ${key}`)}const pre=byId.get("[PRE-FROZEN-PARENT-CHILD]");if(!pre[2].includes("Fully quit and restart both the frozen-code parent application and Physics Paint child"))throw Error("missing fully restarted frozen parent/child precondition");if(!pre[3].includes("Mixed or hot-reloaded parent-child evidence is rejected"))throw Error("missing mixed/hot-reload rejection");' "$f"</automated></verify>
  <acceptance_criteria>The deterministic parser exits 0 only when every mandatory lifecycle/UI/recovery/concurrency/regression/evidence/approval anchor is one fully populated eight-column checklist row with the exact owner, valid enums, Wave 0 sentinels or Plan 17 evidence, and the frozen parent/child precondition; duplicate, heading-only, outside-table, empty, malformed, `TBD`, generic placeholder, and mixed/hot-reload cases exit nonzero.</acceptance_criteria>
  <done>The final native checkpoint has one machine-checkable authoritative checklist that starts pending and can record real Plan 17 evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| frontend preflight→Rust journal | Requests and revisions are untrusted. |
| restart→ordinary scan | Recovery must finish before library availability. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-03 | Tampering | durable transaction contract | high | mitigate | ASVS L1 input/authorization purpose: closed modes, expected authorities, token replay rejection, scan gating, recovery evidence. |
| T-43.2-R1 | Repudiation | transaction markers | medium | mitigate | Unique token and explicit state transitions in fixtures. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>Both Cargo and Vitest contracts plus checklist structure exit 0.</verification>
<success_criteria>Durable deletion and final UAT are fully specified with no intentional nonzero completion.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-03-SUMMARY.md` when done.</output>
