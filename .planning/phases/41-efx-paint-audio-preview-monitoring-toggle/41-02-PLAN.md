---
phase: 41-efx-paint-audio-preview-monitoring-toggle
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src-tauri/src/lib.rs
  - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts
  - app/src/components/physic-paint/audio/efxPaintAudioPreviewContext.ts
  - app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts
  - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
  - app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-06]
tags: [audio, tracer, bridge, web-audio]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Main-side launch context carries an audioPreview section { revision, fps, tracks[] } built from audioStore.tracks, and the child parses, validates, and stores it through the same closed-key plus revision discipline as rotoPhysical (D-01, AUDIO-02)"
    - "Pressing Play in EFX Paint with the default (On) monitoring state decodes each track's bytes via fetch of its efxasset://localhost URL and starts sample-scheduled Web Audio playback at the mapped cursor position using the playbackEngine.ts:192-224 math — one path, end to end (AUDIO-03 start path)"
    - "EFX Paint holds no mutable audio authority: the audio module imports nothing from audioStore, timelineStore, or playbackEngine; all track data arrives via validated payload (AUDIO-01 — edge row covered by source assertion)"
    - "A track whose asset fetch fails (404 from the efxasset handler) is skipped with a console.warn while remaining tracks still play; playback never throws (AUDIO-06 missing-asset tolerance)"
    - "Exactly one audioEngine instance exists in the child window (module singleton per webview) and is reused across hydration and updates (D-08)"
  artifacts:
    - app/src/components/physic-paint/audio/efxPaintAudioPreviewContext.ts
    - app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts
    - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
  key_links:
    - "createPhysicPaintLaunchContext (app/src/lib/physicPaintBridge.ts ~line 1118) embeds the audioPreview section; Rust PhysicsPaintLaunchContext passes it through as Option<Value> renamed audioPreview"
    - "useRotoCachedPlayback start/stop are the monitor's only Play/Stop wiring seam in the child"
    - "41-01 RED schema/revision/path-leak tests turn green against the new modules"
  prohibitions:
    - "No base64 or data: URLs for audio, and no reuse of the v0.8.1 img-src data: grant for audio (D-04)"
    - "No filePath or relativePath fields in the audioPreview payload (D-04 path-leak; path-leak test enforces)"
    - "No raw audio bytes crossing the bridge — bytes flow only via the local efxasset protocol fetch inside the child webview (D-04)"
    - "No imports of audioStore, timelineStore, or playbackEngine from app/src/components/physic-paint/audio/** (AUDIO-01 authority boundary)"
    - "No second audioEngine instantiation in the child — the existing singleton import is the engine (D-08)"
---

<objective>
Deliver the phase tracer: one complete, production-quality path — main editor builds a revisioned audioPreview section into the launch payload, Rust passes it through, the child validates and stores it, fetches and decodes track bytes through the efxasset protocol, and plays them at the Paint cursor on Play. Every layer the phase touches (types, bridge, Rust struct, launch-context parse, child audio module, playback hook) is proven on this single path before expansion plans add push-updates, ownership, toggle, and close handling.

Purpose: Catch any architectural dead-end (CSP fetch, Rust serde boundary, engine reuse) on the cheapest possible slice, per tracer-first decomposition and the D-04 proof discipline.
Output: Working single-path audio monitoring wired to the child's Play, with the 41-01 RED suite green for schema/revision/mapping/path-leak cases.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-RESEARCH.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-PATTERNS.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-FRAME-AUDIO-TRUTH-TABLE.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-01-SUMMARY.md
@app/src/types/physicPaint.ts
@app/src/lib/physicPaintBridge.ts
@app/src/lib/audioEngine.ts
@app/src/lib/ipc.ts
@app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts
@app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "launch carries audio context" — payload plumbing through every layer</name>
  <files>app/src/types/physicPaint.ts, app/src/lib/physicPaintBridge.ts, app/src-tauri/src/lib.rs, app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts, app/src/components/physic-paint/audio/efxPaintAudioPreviewContext.ts, app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts (lines 734-752 isPhysicPaintLaunchContext optional-section chain; 869-877 isPhysicPaintRotoPlaybackSettings closed-key guard style; ~456 revision field)
    - app/src/lib/physicPaintBridge.ts (createPhysicPaintLaunchContext ~lines 1100-1145 where rotoPhysical/rotoPlayback sections are built; imports of audioStore and assetUrl to add)
    - app/src/lib/ipc.ts (lines 25-30 assetUrl)
    - app/src/stores/audioStore.ts (tracks signal shape — read-only peek only)
    - app/src-tauri/src/lib.rs (lines 64-93 PhysicsPaintLaunchContext struct; rotoPhysical is Option<Value> renamed — audioPreview follows identically)
    - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts (lines 13-14 key sets, 52-98 parse funnel, line 82 conditional-spread idiom)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-FRAME-AUDIO-TRUTH-TABLE.md (locked decisions from 41-01 checkpoint)
  </read_first>
  <action>
    Per D-01, extend the existing revisioned handoff — one channel, one revision discipline:
    1. types/physicPaint.ts: add EfxPaintAudioPreviewTrack (id: string; assetUrl: string; offsetFrame, inFrame, outFrame, slipOffset, fadeInFrames, fadeOutFrames: number; volume: number; muted: boolean; fadeInCurve, fadeOutCurve: FadeCurve imported from types/audio.ts) and EfxPaintAudioPreviewContext (revision: number; fps: number; tracks: EfxPaintAudioPreviewTrack[]). Add closed-key guards isEfxPaintAudioPreviewTrack and isEfxPaintAudioPreviewContext in the isPhysicPaintRotoPlaybackSettings style, requiring Number.isInteger(revision) and revision greater-or-equal 0. Extend isPhysicPaintLaunchContext with one optional clause: audioPreview === undefined || isEfxPaintAudioPreviewContext(audioPreview). Never include filePath/relativePath (D-04).
    2. lib/physicPaintBridge.ts: add module-level let nextAudioPreviewRevision = 1 and export buildPhysicPaintAudioPreviewSection(): EfxPaintAudioPreviewContext mapping audioStore.tracks.peek() to payload entries with assetUrl: assetUrl(track.filePath), fps: projectStore.fps.peek(), revision: nextAudioPreviewRevision++ (revision counter discipline per 41-01 locked decision). In createPhysicPaintLaunchContext, embed the section via conditional spread only when audioStore.tracks.peek().length greater-than 0 (absent section = no audio; keeps existing launches byte-stable). Reuse the same builder for both launch embed and (plan 41-03) push events so revision ordering is total across both.
    3. src-tauri/src/lib.rs: add field to struct PhysicsPaintLaunchContext: audio_preview: Option<Value> with serde rename "audioPreview" and skip_serializing_if Option::is_none, mirroring the rotoPhysical Option<Value> field. No handler logic change — pass-through only. Do not touch the efxasset protocol handler or MIME table (RESEARCH Pitfall 8: fetch+decodeAudioData ignores MIME).
    4. bridge/physicsPaintLaunchContext.ts: add 'audioPreview' to LAUNCH_KEYS; add AUDIO_PREVIEW_KEYS closed set (revision, fps, tracks) plus AUDIO_PREVIEW_TRACK_KEYS; extend parseCanonicalPhysicsPaintLaunchValue with the same closed-key check and conditional-spread rebuild used for rotoPlayback, validating via isEfxPaintAudioPreviewContext.
    5. NEW audio/efxPaintAudioPreviewContext.ts: export parseEfxPaintAudioPreviewSection(value): EfxPaintAudioPreviewContext | null (fail-null funnel; reuse the isPlainRecord/hasOnlyKeys/isStructuredClonePlainData idiom — duplicate the three module-private helpers into this file, consistent with current codebase style) and applyRevisionedEfxPaintAudioPreview(store, incoming): boolean implementing the strict newer-than guard (equal/lower dropped silently, returns false; same-revision re-application is a no-op per AUDIO-04 idempotency). Export the pure resolveTrackPlayback(track, cursorFrame, playbackRangeEnd, projectFps) implementing truth-table section 3 verbatim (immediate/delayed/null kinds, playbackRangeEnd cap).
    6. NEW audio/efxPaintAudioPreviewStore.ts: soloStore-shaped session module — signal holding the current EfxPaintAudioPreviewContext | null, a computed hasAudio, and set/clear methods. Session-only, nothing persisted.
    7. Turn the 41-01 RED schema/revision/mapping/path-leak tests green. Do not touch the monitor/playback wiring in this task beyond what tests require.
    Verify gates: the vitest suite for efxPaintAudioPreview passes; typecheck passes; cargo check passes for the Rust struct change.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts &amp;&amp; pnpm --dir app typecheck &amp;&amp; cargo check --manifest-path app/src-tauri/Cargo.toml 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts exits 0 (41-01 RED suite now green)
    - pnpm --dir app typecheck exits 0
    - cargo check --manifest-path app/src-tauri/Cargo.toml succeeds with the new audio_preview field
    - app/src/lib/physicPaintBridge.ts contains buildPhysicPaintAudioPreviewSection and createPhysicPaintLaunchContext embeds audioPreview via conditional spread
    - app/src-tauri/src/lib.rs struct PhysicsPaintLaunchContext contains rename = "audioPreview"
    - grep of app/src/components/physic-paint/audio/ shows no import from stores/audioStore, lib/playbackEngine, or stores/timelineStore (authority boundary source assertion)
    - Existing suites still pass: pnpm --dir app exec vitest run src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts and src/lib/physicPaintBridge.test.ts exit 0
  </acceptance_criteria>
  <done>One path proven end-to-end at the transport layer: launch payload carries a validated revisioned audioPreview section main→Rust→child, schema/revision/mapping/path-leak tests green, and the authority boundary holds by source assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Child audio monitor — fetch, decode, play-at-cursor on Play</name>
  <files>app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts, app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/lib/audioEngine.ts (ensureContext lines 17-26, play line 45, playDelayed line 105, stopAll, decode, getBuffer; constraints: fresh AudioBufferSourceNode per play, exponentialRamp target 0.001, clamped offsets — call the engine, never reimplement)
    - app/src/lib/playbackEngine.ts (lines 192-224 per-track math template; 99-103 seek-restart template)
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts (start/stop/toggle surface lines 46-48, start funnel ~line 128, stop funnel ~line 112, publishStatus ~lines 202-208)
    - app/src/lib/playbackEngine.test.ts (Web Audio mock style)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-FRAME-AUDIO-TRUTH-TABLE.md (sections 2, 3, 7, 8)
  </read_first>
  <behavior>
    - prepare(context): for each non-muted track, fetch(track.assetUrl) then arrayBuffer then audioEngine.decode(track.id, buf); per-track try/catch — a failed fetch or decode logs console.warn and skips only that track (AUDIO-06)
    - playAtCursor(cursorAppFrame, playbackRangeEnd): audioEngine.ensureContext() (called from the Play gesture chain — autoplay suspension handled), then for each prepared track call resolveTrackPlayback and dispatch audioEngine.play(track.id, sourceOffsetSec, trackLike, fps, maxPlaySec) or audioEngine.playDelayed(track.id, delaySec, sourceOffsetSec, trackLike, fps, maxPlaySec) exactly as playbackEngine.ts:192-224 does; trackLike is the payload entry (engine reads volume/fades/muted from it)
    - stop(): audioEngine.stopAll() and anchor cleared; positionedAt(cursorAppFrame) updates the anchor without sound (D-09 silent scrub)
    - Single engine: the module imports the existing audioEngine singleton — one instance per child webview (D-08); play/stop while already in that state is a no-op guard (idempotent control funnel)
    - Wiring: useRotoCachedPlayback start() calls monitor.playAtCursor when monitoring context exists and toggle is On (toggle lands in 41-04; until then default-On behavior is unconditional), stop() calls monitor.stop()
  </behavior>
  <action>
    Create audio/efxPaintAudioMonitor.ts as a focused module (per extract-hooks-for-hard-fixes convention): state machine idle | positioned | playing held in module refs; public API prepare(context), playAtCursor(cursorAppFrame, playbackRangeEnd), stop(), positionedAt(cursorAppFrame), isPlaying(). Mapping goes through resolveTrackPlayback from Task 1 — never duplicate the formula. The trackLike argument passed to audioEngine.play/playDelayed is the payload track entry; the engine's fade/volume math consumes it unchanged (AudioTrack-compatible field names). Wire into useRotoCachedPlayback: inside start(), after the existing start funnel begins, call prepare-then-playAtCursor with the current appFrame and the playback range end (loop window per truth table section 2); inside stop()/finishPlayback call monitor.stop(). All playback-loop reads use .peek() (38.1-D-01 discipline). Extend the test file: mock audioEngine and fetch (playbackEngine.test.ts style); assert playAtCursor dispatches play/playDelayed with the truth-table worked-example numbers, a rejecting fetch for one track still plays the others and calls console.warn, stop() calls stopAll exactly once, and repeated playAtCursor without intervening stop performs stopAll-then-restart (seek-restart discipline, template for D-03/D-11). Commit as feat(41-02): monitor fetch/decode/play-at-cursor wired to EFX Paint Play.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert the exact worked-example numbers from 41-FRAME-AUDIO-TRUTH-TABLE.md for play/playDelayed dispatch (sourceOffsetSec, delaySec, maxPlaySec)
    - Test: one track's fetch rejects, others still dispatch play, console.warn called with the failing track id, no throw
    - Test: stop() after playAtCursor calls audioEngine.stopAll once; second stop() is a no-op
    - Test: playAtCursor while playing performs stopAll before re-dispatch (seek-restart)
    - efxPaintAudioMonitor.ts contains no AudioContext construction (engine singleton only) and no imports of audioStore/timelineStore/playbackEngine
    - pnpm --dir app exec vitest run exits 0 for the touched test file; typecheck exits 0
  </acceptance_criteria>
  <done>Play in EFX Paint (with audio context present) starts frame-mapped monitoring through the existing engine; missing assets warn-and-skip; the full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main window → child window (launch payload) | audioPreview section crosses here; closed-key + type-guard + revision validation |
| child webview → efxasset protocol (Rust) | Protocol URLs fetched here; handler is read-only, 404 on missing |
| main window → Rust serde boundary | Launch context struct passes audioPreview as Option<Value> pass-through |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-03 | Tampering | Path injection via crafted protocol URL in payload | high | mitigate | URLs are built main-side only via assetUrl(); child validates closed-key + type guard before any fetch; efxasset handler serves bytes read-only; path-leak test asserts no filePath/relativePath fields |
| T-41-04 | Information disclosure | Absolute paths leaking into bridge payload | high | mitigate | Payload schema excludes filePath/relativePath by construction (buildPhysicPaintAudioPreviewSection maps only allowed fields); RED-derived path-leak test pins JSON output |
| T-41-05 | Spoofing | Forged audioPreview section via browser-fallback postMessage | medium | mitigate | Same validation funnel as Tauri path (single parse entry); revision compare-and-drop; plan 41-03 uses emitTo window-label targeting for updates |
| T-41-06 | DoS | Oversized/foreign payload exhausting child parse | low | mitigate | Section is small metadata (track entries + URLs); structured-clone check rejects non-plain data before field validation; bytes never cross the bridge |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs no external packages (RESEARCH Package Legitimacy Audit); no install tasks exist |
</threat_model>

<verification>
- 41-01 RED suite fully green; existing bridge/launch-context suites still green; typecheck + cargo check green
- Tracer path demonstrably wired: launch build → Rust pass-through → child parse/store → Play starts monitoring
- Native audible confirmation is deferred to the 41-05 packaged-app UAT checkpoint (sync quality is a native-UAT judgment)
</verification>

<success_criteria>
One complete monitoring path works end-to-end in code and tests, with the D-04 transport boundary and AUDIO-01 authority boundary enforced by source assertions and failing-test-derived guards.
</success_criteria>

<output>
Create `.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-02-SUMMARY.md` when done.

## Artifacts this phase produces (this plan)

- New types: `EfxPaintAudioPreviewTrack`, `EfxPaintAudioPreviewContext` (app/src/types/physicPaint.ts)
- New guards: `isEfxPaintAudioPreviewTrack`, `isEfxPaintAudioPreviewContext` (app/src/types/physicPaint.ts)
- New bridge exports: `buildPhysicPaintAudioPreviewSection`, module counter `nextAudioPreviewRevision` (app/src/lib/physicPaintBridge.ts)
- New Rust field: `PhysicsPaintLaunchContext.audio_preview` (serde rename `audioPreview`)
- New module: `app/src/components/physic-paint/audio/efxPaintAudioPreviewContext.ts` — `parseEfxPaintAudioPreviewSection`, `applyRevisionedEfxPaintAudioPreview`, `resolveTrackPlayback`
- New module: `app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts` — session audio-preview store (signal + hasAudio computed)
- New module: `app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts` — `prepare`, `playAtCursor`, `stop`, `positionedAt`, `isPlaying`
- New private key sets: `AUDIO_PREVIEW_KEYS`, `AUDIO_PREVIEW_TRACK_KEYS` (physicsPaintLaunchContext.ts)
</output>
