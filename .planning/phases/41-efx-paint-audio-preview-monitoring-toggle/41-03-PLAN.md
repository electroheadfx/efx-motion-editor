---
phase: 41-efx-paint-audio-preview-monitoring-toggle
plan: 03
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
  - app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts
autonomous: true
requirements: [AUDIO-03, AUDIO-04]
tags: [audio, playback-sync, bridge-updates]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Seek, pause, resume, loop, and stop stay synchronized with the Paint cursor: scrub is silent and repositions the engine (D-09), each loop wrap re-seeks audio to the mapped loop-start via stopAll + restart (D-11), and sustained playback free-runs with correction only when drift exceeds 40ms (D-10)"
    - "Main-editor audio changes while EFX Paint is open arrive as revisioned push events on the same channel/discipline as launch (D-01/D-02); the child applies only newer revisions and drops stale silently"
    - "A newer context arriving mid-playback restarts audio at the current Paint cursor with the new context — never deferred to next play (D-03)"
    - "Idempotency: applying the same revisioned update twice is a no-op (AUDIO-04 edge, pinned by test)"
    - "Concurrency: interleaved re-hydration and push events resolve through the single applyRevisionedEfxPaintAudioPreview funnel; final child state always equals the newest revision received (AUDIO-04 edge, pinned by out-of-order delivery test)"
    - "When child playback fps differs from project fps, a non-blocking status note is shown and no playbackRate scaling occurs (41-01 locked A6 decision)"
  artifacts:
    - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts (drift corrector + loop-wrap/scrub hooks)
    - app/src/lib/physicPaintBridge.ts (PHYSIC_PAINT_AUDIO_CONTEXT_EVENT + publishPhysicPaintAudioContext)
  key_links:
    - "Loop-wrap hook: useRotoCachedPlayback showNextFrame frameIndex reset (settingsRef.current.loop) triggers monitor seek-restart — mirrors playbackEngine.ts:287-291"
    - "Publisher installed as a signal effect() over audioStore.tracks in the main window, reusing buildPhysicPaintAudioPreviewSection so the revision counter stays total across launch + push"
    - "Child listener in usePhysicsPaintParentBridge.ts uses the triple-transport + disposed-guard idiom (lines 100-119)"
  prohibitions:
    - "No per-frame audio re-sync — drift checks throttled (~every 10 ticks); correction is always full seek-restart (D-10)"
    - "No nudging or re-starting used AudioBufferSourceNodes — every discontinuity is stopAll + fresh play via the engine (one-shot node lifecycle)"
    - "No broadcast emit for audio events — emitTo window-label targeting only, plus the existing CustomEvent/opener.postMessage browser fallbacks"
    - "No exponentialRampToValueAtTime targeting 0 in any touched code (engine targets 0.001 — do not regress)"
---

<objective>
Expand the proven tracer path into full playback sync and live updates: silent-scrub positioning, loop-wrap re-seek, free-run drift correction, and the push-on-change revisioned update channel with mid-playback restart. Covers AUDIO-03 (all sync behaviors beyond the start path) and AUDIO-04 (revisioned updates) including both flagged edge rows.

Purpose: These behaviors share one discipline — every discontinuity is a seek-restart — so they land together against the same monitor funnel and the same test file.
Output: Monitor with complete sync behavior + live revisioned update channel main→child.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-PATTERNS.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-FRAME-AUDIO-TRUTH-TABLE.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-02-SUMMARY.md
@app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
@app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
@app/src/lib/physicPaintBridge.ts
@app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
@app/src/lib/playbackEngine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Monitor sync behaviors — silent scrub, loop-wrap re-seek, drift corrector</name>
  <files>app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts, app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts (current tracer implementation from 41-02)
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts (showNextFrame ~line 159 frameIndex wrap when settingsRef.current.loop; updateFps re-entrant start note ~line 132; publishStatus ~lines 202-208)
    - app/src/lib/playbackEngine.ts (lines 99-103 seek-restart; 287-291 loop-wrap template)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-FRAME-AUDIO-TRUTH-TABLE.md (sections 5, 6, 8)
  </read_first>
  <behavior>
    - Scrub while playing or positioned: monitor.positionedAt(cursorAppFrame) updates the anchor to the new cursor with zero audio dispatch (D-09); a subsequent Play starts at that anchor
    - Loop wrap: when showNextFrame resets the frame index (loop enabled), monitor.notifyLoopWrap(loopStartAppFrame, playbackRangeEnd) performs stopAll + playAtCursor at the loop-start mapping (D-11); source audio metadata untouched
    - Drift corrector: on playAtCursor, capture anchor { appFrame, audioTime, ctxTime: ctx.currentTime }; a throttled checkDrift (invoked from the playback tick, internally no-ops except every ~10th call) computes expected vs actual per truth table section 5 and calls the seek-restart path only when absolute drift exceeds 0.040s; never per frame (D-10)
    - FPS mismatch: when current playback fps differs from context.fps (project fps), publish a non-blocking status note via the existing publishStatus funnel once per playback session; no playbackRate mutation (locked A6)
  </behavior>
  <action>
    Extend efxPaintAudioMonitor.ts with the anchor model and the notifyLoopWrap/checkDrift API, keeping the single control funnel (idle/positioned/playing). Wire useRotoCachedPlayback: at the showNextFrame wrap branch, call monitor.notifyLoopWrap with the loop-start appFrame and range end; on cursor/scrub changes call monitor.positionedAt; inside the tick path call monitor.checkDrift (which self-throttles — the tick itself stays per 38.1-D-01 single-write discipline, all reads via .peek()). For the fps-mismatch note, compare settingsRef.current.fps against the stored context.fps at start() and route through the existing publishStatus gate (queued during playback, cleared on stop). Extend the test file: mock AudioContext.currentTime progression; assert (a) scrub dispatches no play/stopAll, (b) notifyLoopWrap dispatches stopAll then play at the mapped loop-start offset, (c) drift of 30ms triggers nothing while 50ms triggers exactly one stopAll+restart, (d) checkDrift called 9 times does nothing and fires its comparison on the 10th, (e) fps mismatch publishes the note and never touches playbackRate. Commit as feat(41-03): monitor scrub/loop/drift sync behaviors.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tests (a)-(e) from action pass with exact dispatch counts and the 0.040 threshold constant
    - efxPaintAudioMonitor.ts contains the 40ms threshold as a named constant and the throttle counter; no per-tick stopAll possible below threshold
    - useRotoCachedPlayback.ts wrap branch calls notifyLoopWrap; no new useEffect added (signal/ref wiring only, per Preact guidelines)
    - pnpm --dir app exec vitest run exits 0 for the touched file; typecheck exits 0
  </acceptance_criteria>
  <done>Scrub/loop/drift behaviors implemented and test-pinned; sustained playback corrects only beyond 40ms; fps-mismatch surfaces a note with no pitch scaling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Push-on-change revisioned updates with mid-playback restart</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts (lines 26-44 event constants; 823-834 publishPhysicPaintProjectContext publisher shape; the bridge listener-install location where a signal effect can be registered main-side)
    - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts (lines 100-119 usePhysicsPaintProjectContextBridge triple-transport listener with disposed guard)
    - app/src/components/physic-paint/audio/efxPaintAudioPreviewContext.ts (applyRevisionedEfxPaintAudioPreview from 41-02)
    - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts (control funnel from Task 1)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <behavior>
    - Main: any change to audioStore.tracks while EFX Paint may be open publishes the full rebuilt section (buildPhysicPaintAudioPreviewSection — the shared counter keeps revisions total across launch + push) via PHYSIC_PAINT_AUDIO_CONTEXT_EVENT = 'physic-paint:audio-context' using emitTo('efx-physic-paint', ...) plus CustomEvent + window.opener.postMessage fallbacks, exactly the publishPhysicPaintProjectContext shape (D-01/D-02)
    - Child: listener accepts only validated sections, applies via applyRevisionedEfxPaintAudioPreview (newer-only, stale dropped silently), and when the monitor is playing performs stopAll + playAtCursor at the current Paint cursor with the new context (D-03); when idle/positioned it only updates stored context and repositions the anchor
    - Idempotency: the same revision delivered twice (Tauri event + postMessage fallback both firing) applies once; second delivery is a silent drop
    - Concurrency: an out-of-order sequence (rev 3, then rev 2, then rev 4) leaves the child at rev 4 with exactly two applications
  </behavior>
  <action>
    In lib/physicPaintBridge.ts add the event constant and export publishPhysicPaintAudioContext(): Promise<void> following the publishPhysicPaintProjectContext shape verbatim (Tauri emitTo first, then CustomEvent dispatch and opener.postMessage). Install the push trigger main-side: a Preact effect() over audioStore.tracks registered from the same module/init path that installs the other bridge listeners (per CLAUDE.md, effects synchronize with something external — this qualifies); debounce is NOT allowed to skip revisions — every effect run publishes (revision counter absorbs frequency). In usePhysicsPaintParentBridge.ts add useEfxPaintAudioContextBridge(handleSection) copying the usePhysicsPaintProjectContextBridge listener shape (Tauri listen + CustomEvent + message with origin check + disposed flag + unlisten cleanup), and wire the handler to validate → applyRevisionedEfxPaintAudioPreview → if monitor.isPlaying() then mid-playback restart at the current cursor via the monitor funnel, else reposition anchor. Extend tests: (1) publisher emits on tracks change with incrementing revisions, (2) double-delivery of one revision applies once, (3) out-of-order 3/2/4 ends at 4 with two applications, (4) mid-playback application calls stopAll then playAtCursor at the current cursor, (5) stale event while playing causes zero audio dispatch. Commit as feat(41-03): revisioned audio-context push channel with mid-playback restart.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts src/lib/physicPaintBridge.test.ts &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - physicPaintBridge.ts exports PHYSIC_PAINT_AUDIO_CONTEXT_EVENT = 'physic-paint:audio-context' and publishPhysicPaintAudioContext; emitTo targets the 'efx-physic-paint' label (no bare emit)
    - Tests (1)-(5) from action pass, including exact application counts for idempotency and out-of-order cases
    - usePhysicsPaintParentBridge.ts listener uses the disposed-guard + unlisten cleanup idiom; no stale application possible after unmount
    - pnpm --dir app exec vitest run exits 0 for both touched test files; typecheck exits 0
  </acceptance_criteria>
  <done>Live main-editor audio changes reach the child as revisioned updates; mid-playback updates restart at the cursor; idempotency and out-of-order delivery are test-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main window → child window (push events) | Revisioned audio-context updates cross here repeatedly during a session |
| browser fallback (postMessage) | Same channel without Tauri event isolation; origin-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-07 | Tampering | Stale/replayed update regresses child audio state mid-playback | high | mitigate | Strict newer-than compare at the single application funnel; out-of-order and double-delivery tests pin exactly-once-newest-wins; D-03 restart occurs only after the newer context commits |
| T-41-08 | Spoofing | Forged 'physic-paint:audio-context' via opener.postMessage fallback | medium | mitigate | Origin check (window.location.origin) + full closed-key/type-guard validation + revision guard; Tauri path uses emitTo window-label targeting, never broadcast emit |
| T-41-09 | DoS | Rapid main-editor edits flooding the child with publishes | low | mitigate | Payload is small metadata; revision counter absorbs frequency (no debounce that could skip state); application is O(tracks) with stale drops costing one compare |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs no external packages (RESEARCH Package Legitimacy Audit); no install tasks exist |
</threat_model>

<verification>
- All sync behaviors (scrub/loop/drift) and the push channel covered by the touched test files; full suite stays green
- Native sync-quality judgment (drift audibility, loop tightness) deferred to the 41-05 packaged-app UAT checkpoint
</verification>

<success_criteria>
AUDIO-03 and AUDIO-04 are fully implemented in code with tests: synchronized seek/pause/resume/loop/stop with 40ms-threshold correction, and revisioned live updates with mid-playback restart, idempotent application, and newest-wins ordering.
</success_criteria>

<output>
Create `.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-03-SUMMARY.md` when done.

## Artifacts this phase produces (this plan)

- New bridge exports: `PHYSIC_PAINT_AUDIO_CONTEXT_EVENT` ('physic-paint:audio-context'), `publishPhysicPaintAudioContext` (app/src/lib/physicPaintBridge.ts)
- New hook: `useEfxPaintAudioContextBridge` (app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts)
- New monitor API: `notifyLoopWrap`, `checkDrift`, anchor model, fps-mismatch status note (app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts)
- New constant: drift threshold (0.040s named constant) in efxPaintAudioMonitor.ts
</output>
