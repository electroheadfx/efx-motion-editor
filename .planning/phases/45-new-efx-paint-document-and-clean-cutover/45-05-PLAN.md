---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 05
type: execute
wave: 4
depends_on: ["45-02", "45-03", "45-04"]
files_modified:
  - app/src/stores/projectStore.ts
  - app/src/lib/efxPaintRejectionDialog.ts
  - app/src/components/timeline/AddFxMenu.tsx
  - app/src/stores/projectStore.efxPaintCutover.test.ts
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-05, DOC-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "openProject runs findLegacyPhysicPaintRejection immediately after the result.ok check and before loadPhysicPaintData, closeProject, hydrateFromMce, and startAutoSave; on rejection it shows a blocking error dialog and returns with zero store mutation (D-05, Pitfall F4)"
    - "Auto-save never engages for a rejected project; the currently open project remains open and untouched (D-05)"
    - "The rejection dialog is explicit with no recourse: single acknowledge action, states the project contains pre-v1.0 EFX Physic Paint data that v1.0.0 does not support and cannot be opened — no partial open, no continue-anyway, no converter offer (D-07)"
    - "saveProject and saveProjectAs persist efx_paint_documents through saveEfxPaintDocumentsWithProjectWrite with the bound cache transaction — there is exactly one save path after this plan (no dual old+new paths)"
    - "New projects are written with .mce version 16 (A1) and reopen through the v1.0 loader: documents hydrate into efxPaintStore and the default track projects into the runtime (DOC-05)"
    - "Adding a 'physic-paint' layer via AddFxMenu registers exactly one v1.0 document with one default Paint track + fixed Background track + transparent fallback (DOC-01, DOC-02)"
    - "Main-editor sequence timing and outer layer composition code paths are untouched: previewRenderer.ts diff is empty and the existing timing/composition tests stay green (DOC-06)"
  artifacts:
    - app/src/stores/projectStore.ts (modified)
    - app/src/lib/efxPaintRejectionDialog.ts
    - app/src/components/timeline/AddFxMenu.tsx (modified)
    - app/src/stores/projectStore.efxPaintCutover.test.ts
  key_links:
    - "Gate placement between projectStore.ts:767 (result.ok check) and :772 (loadPhysicPaintData) — verified insertion point; validation already precedes closeProject at :783, keep that ordering"
    - "Both save call sites (saveProject ~685-691 and saveProjectAs ~727-745) switch in the same commit — the research anti-pattern forbids two live save paths"
    - "closeProject must call efxPaintStore.reset() alongside the existing physicPaintStore.reset(...) at ~831"
  prohibitions:
    - "Never mutate the project file, hydrate stores, close the current project, or start auto-save when the gate rejects (D-05)"
    - "No partial open, no 'continue anyway', no converter offer, no stripped-copy option anywhere in the rejection UX (D-07)"
    - "No dual old+new save paths after this plan — the legacy savePhysicPaintDataWithProjectWrite call sites are removed from projectStore in this plan (module deletion lands in 45-07)"
    - "Do not modify paintStore.ts, paintRenderer.ts, PaintOverlay.tsx, paintPersistence.ts (inline EFX Paint, D-01) or previewRenderer.ts (DOC-06)"
---

<objective>
Wire the cutover into the live open/save funnel: rejection gate + blocking no-recourse dialog on open, v1.0 document persistence on save, version bump, document registration at layer creation, and store lifecycle hooks.

Purpose: This is where DOC-03 becomes user-visible (D-05/D-07) and where DOC-01/DOC-02/DOC-05 become real for the main editor. After this plan the app runs a single v1.0 persistence path; the legacy module is dead code awaiting 45-07's deletion.

Output: Modified `projectStore.ts` funnel, rejection dialog helper, AddFxMenu creation hook, and a cutover test suite proving no-mutation-on-reject.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@app/src/stores/projectStore.ts
@app/src/components/timeline/AddFxMenu.tsx
@app/src/lib/unsavedGuard.ts
@app/src/efx-paint/document/efxPaintCleanBreak.ts
@app/src/stores/efxPaintStore.ts
@app/src/lib/efxPaintPersistence.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rejection gate wired into openProject + blocking no-recourse dialog (end-to-end rejection path)</name>
  <files>app/src/stores/projectStore.ts, app/src/lib/efxPaintRejectionDialog.ts, app/src/stores/projectStore.efxPaintCutover.test.ts</files>
  <read_first>
    - app/src/stores/projectStore.ts:763-806 (openProject current flow — gate inserts after the result.ok check at 767, BEFORE loadPhysicPaintData at 772, closeProject at 783, startAutoSave at 801)
    - app/src/lib/unsavedGuard.ts:14-25 (native message() dialog idiom — Candidate A from PATTERNS, recommended for D-07)
    - app/src/efx-paint/document/efxPaintCleanBreak.ts (gate predicate + reason union from 45-03)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Code Example 3 gate call site; Pitfall F4)
  </read_first>
  <behavior>
    - RED Test 1: opening a project whose parsed data matches a rejection fixture → openProject shows the rejection dialog and returns; closeProject, hydrateFromMce, loadPhysicPaintData(-equivalent v1.0 loader), and startAutoSave are never called (spies assert zero invocations).
    - RED Test 2: opening a clean project (gate returns null) proceeds through the normal hydration path exactly as today.
    - RED Test 3: the dialog copy contains the explicit unsupported statement and offers exactly one acknowledge action (D-07); copy is an exported *_COPY constant.
    - RED Test 4: a second open attempt of a rejected file behaves identically (gate is stateless; no latch).
  </behavior>
  <action>
    Create app/src/lib/efxPaintRejectionDialog.ts exporting LEGACY_PHYSIC_PAINT_REJECTED_COPY (plain, explicit wording per D-07 — state that the project contains pre-v1.0 EFX Physic Paint data which v1.0.0 does not support and that the project cannot be opened; exact wording is Claude's discretion but must name EFX Physic Paint, pre-v1.0, and the impossibility of opening) and showLegacyPhysicPaintRejectionDialog(reason) implemented with the native message() idiom (kind: 'error', single OK button — no backdrop-dismiss ambiguity, no recourse). In projectStore.openProject insert the gate immediately after the result.ok check: const rejection = findLegacyPhysicPaintRejection(result.data); if (rejection) { await showLegacyPhysicPaintRejectionDialog(rejection); return; } — before any sidecar IO, closeProject, hydration, or startAutoSave. Test with mocked ipcProjectOpen returning fixture data and spies on the downstream functions. RED commit test(45-05): add failing open-gate tests → GREEN commit feat(45-05): wire clean-break rejection gate into project open.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/projectStore.efxPaintCutover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behavior tests pass
    - projectStore.ts calls findLegacyPhysicPaintRejection before any loadPhysicPaintData/closeProject/startAutoSave call in openProject (source-order assertion)
    - efxPaintRejectionDialog.ts exports the copy constant; dialog uses kind 'error' with a single button
    - The previously-open project remains open after a rejected open (test 1 asserts closeProject not called)
  </acceptance_criteria>
  <done>A legacy project open is refused end-to-end with an explicit blocking dialog and zero mutation; clean projects open exactly as before.</done>
  <reversibility rating="costly">Implements user-locked D-05 (hard-fail whole open). The user confirmation already happened at discuss-phase (the CONTEXT.md lock IS the decision checkpoint, so no checkpoint:decision is inserted); the code change itself is git-revertible, but softening the published format-break contract later would create a second compatibility surface the milestone forbids.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save/load funnel switch to the v1.0 document + version bump 16 + store lifecycle</name>
  <files>app/src/stores/projectStore.ts, app/src/stores/projectStore.efxPaintCutover.test.ts</files>
  <read_first>
    - app/src/stores/projectStore.ts:266 (version: 15), 685-691 (saveProject legacy block), 727-745 (saveProjectAs legacy block), 763-806 (openProject), ~831 (closeProject reset calls), hydrateFromMce area
    - app/src/lib/efxPaintPersistence.ts (saveEfxPaintDocumentsWithProjectWrite / loadEfxPaintDocuments from 45-04)
    - app/src/stores/efxPaintStore.ts (serializeRuntimeIntoDocument / hydrateRuntimeFromDocument / reset from 45-04)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md (projectStore save-path switch excerpt, verbatim legacy block)
  </read_first>
  <behavior>
    - Test 1: saveProject on a project with one physic-paint layer calls writeProject with an efx_paint_documents payload (one entry keyed by the layerId) and a cache transaction id — and never sends physic_paint_outputs.
    - Test 2: saveProjectAs performs the identical switch (same assertion on its call path).
    - Test 3: buildMceProject writes version 16.
    - Test 4: openProject on a v1.0 project hydrates efxPaintStore with the persisted document and projects the default track into the runtime (stroke present in runtime maps after open — UAT part 2 automated foundation).
    - Test 5: closeProject calls efxPaintStore.reset() (spy assertion) so no document leaks across projects.
    - Test 6: a save with no physic-paint layers writes an empty/absent efx_paint_documents key and skips sidecar staging entirely.
  </behavior>
  <action>
    In projectStore.ts: replace the legacy savePhysicPaintDataWithProjectWrite blocks in BOTH saveProject and saveProjectAs with saveEfxPaintDocumentsWithProjectWrite(projectDir, documents, writeProject) where documents come from efxPaintStore after serializeRuntimeIntoDocument(layerId) for each physic-paint layer, and writeProject sends { ...project, efx_paint_documents } with the cache transaction id — one save path only, no physic_paint_outputs emission (research anti-pattern: never dual paths). Bump the project version constant from 15 to 16 (A1). In the open path, after the gate passes, load documents via loadEfxPaintDocuments, register them into efxPaintStore, and hydrateRuntimeFromDocument per document during hydration. In closeProject add efxPaintStore.reset() next to the existing physicPaintStore.reset call. Remove projectStore's import of the legacy persistence module. Tests RED then GREEN with mocked ipc + fs. Commit test(45-05) then feat(45-05): cut project open/save over to the v1.0 document.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/projectStore.efxPaintCutover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behavior tests pass; full vitest suite green (legacy module tests still pass — module deleted in 45-07)
    - projectStore.ts contains `efx_paint_documents` in both save paths and no import of physicPaintPersistence
    - projectStore.ts version constant is 16
    - pnpm --dir app run typecheck exits 0
  </acceptance_criteria>
  <done>Save/reopen round-trips the v1.0 document through the real funnel; version 16 is written; there is exactly one save path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AddFxMenu registers the v1.0 document at physic-paint layer creation</name>
  <files>app/src/components/timeline/AddFxMenu.tsx, app/src/stores/projectStore.efxPaintCutover.test.ts</files>
  <read_first>
    - app/src/components/timeline/AddFxMenu.tsx:135-156 (handleAddPhysicPaintLayer current state — layer shape unchanged, source.layerId is the parentLayerId anchor)
    - app/src/efx-paint/document/efxPaintDocument.ts (createEfxPaintDocument from 45-01)
    - app/src/stores/efxPaintStore.ts (registerDocument from 45-04)
  </read_first>
  <behavior>
    - Test 1: handleAddPhysicPaintLayer creates the layer AND registers exactly one document in efxPaintStore keyed by the new layerId (DOC-01: one parent layer owns exactly one document).
    - Test 2: the registered document has one default Paint track, one fixed Background track with { mode: 'transparent' } fallback, version 1, documentRevision 0, activeTrackId === tracks[0].id (DOC-02, D-08).
    - Test 3: the layer object itself is unchanged (type 'physic-paint', source.layerId === layer id, defaultTransform) — the main-editor layer model is untouched except the document registration.
  </behavior>
  <action>
    In AddFxMenu.tsx handleAddPhysicPaintLayer: after layer creation (both the isolated-range and standard branches), call registerDocument(createEfxPaintDocument(layerId)) on efxPaintStore. Do not change the layer shape, name, or sequence-store calls. Tests RED then GREEN (component-level test idiom already used for this menu or store-level test invoking the handler path — follow the existing test convention for this file; if AddFxMenu has no existing test, cover the handler via the cutover test file with mocked stores). Commit test(45-05) then feat(45-05): register v1.0 document on physic-paint layer creation.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/projectStore.efxPaintCutover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behavior tests pass
    - AddFxMenu.tsx calls createEfxPaintDocument exactly once per physic-paint layer creation
    - No changes to layer.ts, paintStore.ts, paintRenderer.ts, PaintOverlay.tsx, paintPersistence.ts, or previewRenderer.ts in this plan's diff (git diff --name-only assertion)
  </acceptance_criteria>
  <done>New physic-paint layers own exactly one spec-shaped v1.0 document from the moment of creation.</done>
</task>

</tasks>

## Flagged assumptions (spec-less probe fallback — unclassified edges, not silently dropped)

- DOC-06 edge probe was unclassified: the invariant "main-editor sequence timing and outer layer composition remain unchanged" is enforced by the diff gate (previewRenderer.ts and inline-paint files untouched) plus the existing timing/composition suite staying green — verified at 45-07's full-gate sweep and natively at 45-08 UAT part 4. Flagged for checker review; no additional acceptance criterion authored.

## Artifacts this phase produces (this plan)

- New files: `app/src/lib/efxPaintRejectionDialog.ts`, `app/src/stores/projectStore.efxPaintCutover.test.ts`
- New symbols: `LEGACY_PHYSIC_PAINT_REJECTED_COPY`, `showLegacyPhysicPaintRejectionDialog`
- Modified: `app/src/stores/projectStore.ts` (gate call, save-path switch x2, version 16, v1.0 hydration, efxPaintStore.reset in closeProject), `app/src/components/timeline/AddFxMenu.tsx` (document registration)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce file → openProject funnel | Untrusted project JSON reaches store hydration only through the gate |
| dialog → user | Rejection UX must be unspoofable by alternate open entry points |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-15 | Tampering | openProject gate placement | high | mitigate | Gate runs before any sidecar IO, closeProject, hydration, or startAutoSave; spies assert zero downstream invocation on reject (Task 1 test 1; Pitfall F4) |
| T-45-16 | Tampering | alternate open paths (recent projects, last-project restore, drag-drop) | high | mitigate | Audit in this plan: every open entry point routes through projectStore.openProject — verify by grepping ipcProjectOpen call sites and asserting the single funnel; any bypass found is wired through openProject in this plan |
| T-45-17 | Tampering | rejection dialog | medium | mitigate | Native modal message() with kind 'error' and a single OK — no backdrop-dismiss, no recourse actions (D-07) |
| T-45-18 | Tampering | save-path switch | medium | mitigate | Both save call sites switched in one commit; no physic_paint_outputs emission remains in projectStore (acceptance criterion + 45-07 grep contract) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/stores/projectStore.efxPaintCutover.test.ts` — cutover suite green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green
- `pnpm --dir app run typecheck`
- `git diff --name-only` for this plan shows no change to previewRenderer.ts / paintStore.ts / paintRenderer.ts / PaintOverlay.tsx / paintPersistence.ts (DOC-06/D-01 diff gate)
</verification>

<success_criteria>
- Rejected projects cannot mutate anything or reach auto-save (D-05/D-06/D-07 live).
- v1.0 documents save and reopen through the real app funnel (DOC-05 end-to-end at the app level).
- Layer creation produces the DOC-01/DOC-02 document shape automatically.
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-05-SUMMARY.md` when done
</output>
