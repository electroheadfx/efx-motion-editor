---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 03
type: tdd
wave: 2
depends_on: ["45-01"]
files_modified:
  - app/src/efx-paint/document/efxPaintCleanBreak.ts
  - app/src/efx-paint/document/efxPaintCleanBreak.test.ts
  - app/src/efx-paint/document/__fixtures__/legacy-physic-paint-outputs.mce.json
  - app/src/efx-paint/document/__fixtures__/legacy-cache-reference.mce.json
  - app/src/efx-paint/document/__fixtures__/documentless-physic-paint-layer.mce.json
  - app/src/efx-paint/document/__fixtures__/clean-pre-v1-project.mce.json
  - app/src/efx-paint/document/__fixtures__/fresh-v1-project.mce.json
autonomous: true
requirements: [DOC-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "findLegacyPhysicPaintRejection returns the first matching typed reason for: non-empty physic_paint_outputs, any cache/physic-paint path reference, or a physic-paint layer with no corresponding efx_paint_documents entry — and null otherwise (D-06)"
    - "An old project WITHOUT Physic Paint data (including one with inline 'paint' layers) passes the gate and opens normally (D-06 final clause)"
    - "A freshly saved v1.0 project containing a physic-paint layer WITH an efx_paint_documents entry passes the gate — the literal 'any physic-paint layer' trigger is NOT applied (Pitfall F2)"
    - "The gate predicate is pure, contract-testable, operates on raw parsed .mce JSON pre-hydration, and never mutates its input (D-05/D-06)"
  artifacts:
    - app/src/efx-paint/document/efxPaintCleanBreak.ts
    - app/src/efx-paint/document/efxPaintCleanBreak.test.ts
    - 5 committed synthetic .mce fixtures under app/src/efx-paint/document/__fixtures__/
  key_links:
    - "Gate trigger strings ('physic_paint_outputs', 'cache/physic-paint', layer type 'physic-paint', document key 'efx_paint_documents') are the exact same tokens plan 45-07's DOC-04 grep contract must allowlist for this module"
    - "45-05 wires this predicate into projectStore.openProject immediately after the result.ok check and before loadPhysicPaintData (Pitfall F4 placement)"
  prohibitions:
    - "The gate never reads sidecar files, never touches the filesystem, and never mutates the parsed project — detection is a pure scan (D-04/D-05)"
    - "No auto-fix, migration hint, converter branch, or stripped-copy suggestion in gate output — reasons are terminal (D-07)"
---

<objective>
Build the single parse-time rejection gate predicate — the pure, contract-testable heart of the clean-break cutover (DOC-03, D-05/D-06) — with a committed fixture truth table covering every trigger and every must-pass case.

Purpose: v1.0.0 intentionally does not open, migrate, or render pre-v1.0 Physic Paint data. Detection must be one explicit gate at project parse time, discriminated by structure so the app's own new v1.0 projects are not rejected (Pitfall F2). This plan delivers only the predicate + fixtures; plan 45-05 wires it into the open funnel with the blocking dialog.

Output: `efxPaintCleanBreak.ts` + full truth-table tests + 5 synthetic fixtures (distinct from the D-12 real-project UAT copy).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@app/src/efx-paint/document/efxPaintDocument.ts
@app/src/types/project.ts
@app/src/types/layer.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gate predicate + reason union + first-trigger fixture (end-to-end detection path)</name>
  <files>app/src/efx-paint/document/efxPaintCleanBreak.ts, app/src/efx-paint/document/efxPaintCleanBreak.test.ts, app/src/efx-paint/document/__fixtures__/legacy-physic-paint-outputs.mce.json</files>
  <read_first>
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Code Example 2 gate skeleton; Pitfall F2 discrimination rules)
    - app/src/types/project.ts:20-38 (MceProject shape the raw JSON follows; physic_paint_outputs at 37)
    - app/src/types/layer.ts:1-17,33-34 (LayerType union; 'physic-paint' layer source shape)
    - app/src/lib/physicPaintPersistence.ts:17 (legacy prefix 'cache/physic-paint' the gate scans for)
  </read_first>
  <behavior>
    - RED Test 1: fixture legacy-physic-paint-outputs.mce.json (a minimal raw .mce JSON: version 15, one sequence whose layer is type 'physic-paint', non-empty physic_paint_outputs array with one blob) → findLegacyPhysicPaintRejection returns { kind: 'legacy-physic-paint-outputs' }.
    - RED Test 2: the predicate is pure — calling it twice on the same parsed object returns equal results and the input object is deep-unchanged.
    - RED Test 3: non-record input (null, array, string) returns null — the gate scans, it does not throw; parse errors remain the existing open_project/serde concern.
  </behavior>
  <action>
    Create app/src/efx-paint/document/efxPaintCleanBreak.ts exporting the union LegacyPhysicPaintRejection = { kind: 'legacy-physic-paint-outputs' } | { kind: 'legacy-physic-paint-cache-reference'; path: string } | { kind: 'physic-paint-layer-without-document'; layerId: string } and the pure predicate findLegacyPhysicPaintRejection(project: unknown): LegacyPhysicPaintRejection | null per RESEARCH.md Code Example 2. Implement trigger 1 (non-empty physic_paint_outputs array at top level) for this task; triggers 2-3 land in Task 2. Scan raw parsed JSON defensively with the isPlainRecord idiom (no throwing on unexpected shapes — return null and let downstream parse handle true corruption). Commit the fixture (hand-authored minimal JSON, committed test data — NOT a copy of user data; the D-12 real-project copy is native-UAT-only and never enters the repo). TDD sequence: RED commit test(45-03): add failing rejection-gate fixture test → GREEN commit feat(45-03): clean-break gate predicate, outputs trigger.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document/efxPaintCleanBreak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - efxPaintCleanBreak.test.ts exits 0 with the 3 behavior tests passing
    - efxPaintCleanBreak.ts exports findLegacyPhysicPaintRejection and the LegacyPhysicPaintRejection union with exactly the 3 kinds listed
    - Fixture file exists at app/src/efx-paint/document/__fixtures__/legacy-physic-paint-outputs.mce.json and contains a non-empty physic_paint_outputs array
  </acceptance_criteria>
  <done>The outputs trigger detects a legacy project end-to-end from raw JSON to typed reason; predicate is pure and non-throwing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full D-06 truth table — cache-ref + document-less-layer triggers, must-pass cases, precedence order</name>
  <files>app/src/efx-paint/document/efxPaintCleanBreak.ts, app/src/efx-paint/document/efxPaintCleanBreak.test.ts, app/src/efx-paint/document/__fixtures__/legacy-cache-reference.mce.json, app/src/efx-paint/document/__fixtures__/documentless-physic-paint-layer.mce.json, app/src/efx-paint/document/__fixtures__/clean-pre-v1-project.mce.json, app/src/efx-paint/document/__fixtures__/fresh-v1-project.mce.json</files>
  <read_first>
    - app/src/efx-paint/document/efxPaintCleanBreak.ts (current state after Task 1)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Pitfall F2: the 3-rule discriminated predicate, verbatim)
    - app/src/efx-paint/document/efxPaintDocument.ts (EFX_PAINT_DOCUMENT_VERSION and document key expectations for the fresh-v1 fixture)
  </read_first>
  <behavior>
    - Test 1: legacy-cache-reference fixture (empty physic_paint_outputs but a frame cache_path string containing 'cache/physic-paint' inside a legacy blob OR a legacy cache reference anywhere in the project JSON) → { kind: 'legacy-physic-paint-cache-reference', path } with the offending path reported.
    - Test 2: documentless-layer fixture (a 'physic-paint' layer present, no efx_paint_documents entry for its layerId, empty outputs, no cache refs) → { kind: 'physic-paint-layer-without-document', layerId }.
    - Test 3: clean-pre-v1 fixture (version 15 project with ordinary layers including an inline 'paint' layer, no Physic Paint anything) → null (D-06: old projects without Physic Paint data open normally).
    - Test 4: fresh-v1 fixture (version 16 project with a 'physic-paint' layer AND a matching efx_paint_documents entry keyed by that layerId) → null (Pitfall F2: the app's own new projects must reopen).
    - Test 5 (precedence): a project matching multiple triggers returns the FIRST reason in the fixed order outputs → cache-reference → documentless-layer.
    - Test 6: a 'physic-paint' layer whose efx_paint_documents entry exists but is itself malformed is NOT this gate's concern (returns null) — document parse failures belong to the fail-closed document parser at load time.
  </behavior>
  <action>
    Complete the predicate: trigger 2 scans all string values in the project JSON recursively for the 'cache/physic-paint' prefix (report the first offending path); trigger 3 collects every layer of type 'physic-paint' from all sequences and rejects when the top-level efx_paint_documents map has no entry for that layer's source.layerId. Fix the evaluation order outputs → cache-reference → documentless-layer and return the first match only. Author the 4 new fixtures as minimal hand-written JSON (fresh-v1 uses version 16 and a one-entry efx_paint_documents map whose value is a minimal plausible v1.0 document — shape consistent with 45-01's factory). All 6 tests RED first, then GREEN. Commit test(45-03) then feat(45-03): complete clean-break gate truth table.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document/efxPaintCleanBreak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 truth-table tests pass plus Task 1's 3 tests
    - 5 fixture files exist under app/src/efx-paint/document/__fixtures__/
    - efxPaintCleanBreak.ts contains the fixed precedence order (outputs check precedes cache-reference scan precedes documentless-layer check)
    - No filesystem or IPC import anywhere in efxPaintCleanBreak.ts
  </acceptance_criteria>
  <done>The full D-06 truth table is contract-tested: all three triggers reject with typed reasons, both must-pass cases pass, precedence is fixed.</done>
</task>

</tasks>

## Flagged assumptions (spec-less probe fallback — unclassified edges, not silently dropped)

- DOC-03 edge probe was unclassified: rejection semantics are fully pinned by D-05/D-06/D-07 (hard-fail whole open, single parse-time gate, no recourse) and the 6-case truth table above. No additional edge acceptance criterion authored; flagged for checker review.

## Artifacts this phase produces (this plan)

- New files: `app/src/efx-paint/document/efxPaintCleanBreak.ts`, `efxPaintCleanBreak.test.ts`, 5 fixtures under `app/src/efx-paint/document/__fixtures__/`
- New symbols: `LegacyPhysicPaintRejection` (union: `legacy-physic-paint-outputs` | `legacy-physic-paint-cache-reference` | `physic-paint-layer-without-document`), `findLegacyPhysicPaintRejection`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce file → gate predicate | Untrusted project JSON scanned before any hydration or sidecar IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-08 | Tampering | findLegacyPhysicPaintRejection | high | mitigate | Pure scan over raw JSON; no throws on unexpected shapes, no mutation, no IO — a hostile file can only be rejected or passed, never partially acted on (D-05) |
| T-45-09 | Tampering | gate discrimination | high | mitigate | Pitfall F2 structure-discrimination contract-tested (fresh-v1 must-pass fixture) so the gate cannot be turned against the app's own projects |
| T-45-10 | Repudiation | rejection reasons | low | accept | Typed reason union gives the dialog (45-05) an explicit cause; no logging pipeline required for a single-user desktop app |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document` — gate + document suites green
- `pnpm --filter efx-motion-editor exec vitest run` — no regressions
</verification>

<success_criteria>
- One pure predicate decides reject/pass for every legacy-data shape enumerated in D-06, proven against committed fixtures.
- The app's own v1.0 projects provably pass the gate (F2 closed with a contract test).
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-03-SUMMARY.md` when done
</output>
