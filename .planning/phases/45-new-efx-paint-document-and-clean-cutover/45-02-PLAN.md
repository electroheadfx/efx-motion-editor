---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - app/src-tauri/src/models/project.rs
  - app/src-tauri/src/services/project_io.rs
  - app/src-tauri/src/services/physic_paint_cache.rs
  - app/src/types/project.ts
autonomous: true
requirements: [DOC-04, DOC-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rust MceProject carries efx_paint_documents as an opaque serde_json map with serde default + skip_serializing_if empty, so a v1.0 document key written on save survives save → open round-trip byte-identical (DOC-05, Pitfall F1)"
    - "Rust physic_paint_outputs is demoted to an opaque Vec<serde_json::Value> presence carrier: it round-trips presence into TS for the rejection gate but is never interpreted, never migrated, never rendered (D-02/D-06)"
    - "Legacy Rust structs McePhysicPaintOutput, McePhysicPaintCachedFrame, McePhysicPaintRotoPlaybackSettings no longer exist (DOC-04)"
    - "create_project_dir creates cache/efx-paint and never creates cache/physic-paint; pre-existing legacy cache directories on disk are not read, moved, or deleted (D-04)"
    - "The native cache transaction service (publish/bind/settle/recover) operates on cache/efx-paint with staging prefix .efx-paint-staging- ; no code path can publish into cache/physic-paint (DOC-04)"
  artifacts:
    - app/src-tauri/src/models/project.rs (modified)
    - app/src-tauri/src/services/project_io.rs (modified)
    - app/src-tauri/src/services/physic_paint_cache.rs (modified)
    - app/src/types/project.ts (modified)
  key_links:
    - "efx_paint_documents added to BOTH models/project.rs and types/project.ts in the same commit — TS-only addition silently drops the document on save (Pitfall F1, DOC-05 killer)"
    - "Rust round-trip test extended at project_io.rs test module proves the new key survives; TS-side tests mock IPC and cannot catch a missing Rust field"
  prohibitions:
    - "Never read, rewrite, move, or delete legacy on-disk data: existing cache/physic-paint directories and physic_paint_outputs blobs in user projects stay byte-untouched (D-04)"
    - "No new cargo or npm dependencies; serde/serde_json only as already used (STACK.md)"
---

<objective>
Land the Rust + TS serde co-change for the v1.0 document key and re-point the native cache machinery at the new v1.0 cache directory — the persistence substrate that plan 45-04 (TS persistence) and plan 45-05 (funnel wiring) build on.

Purpose: Pitfall F1 is the DOC-05 killer — Rust `MceProject` uses explicit serde fields and silently drops unknown keys on save. The document key MUST land in `models/project.rs` and `types/project.ts` together, proven by a Rust round-trip test. Simultaneously, the legacy Rust output structs and legacy cache-dir creation are removed/re-pointed so no legacy cache path remains creatable (DOC-04), while an opaque presence carrier keeps legacy blobs visible to the TS rejection gate (D-06).

Output: Rust model + IO + cache service on the v1.0 surface; TS type mirror; all cargo tests green.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@app/src-tauri/src/models/project.rs
@app/src-tauri/src/services/project_io.rs
@app/src-tauri/src/services/physic_paint_cache.rs
@app/src/types/project.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rust MceProject efx_paint_documents key + opaque legacy carrier + round-trip proof (F1 co-change)</name>
  <files>app/src-tauri/src/models/project.rs, app/src-tauri/src/services/project_io.rs, app/src/types/project.ts</files>
  <read_first>
    - app/src-tauri/src/models/project.rs:15-60 (current MceProject + legacy output structs to remove)
    - app/src-tauri/src/services/project_io.rs:37-85 (save/open strict serde round-trip) and 189-260 (test idiom test_save_and_open_roundtrip)
    - app/src/types/project.ts:20-127 (MceProject TS mirror, physic_paint_outputs, legacy output types)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Pitfall F1 verbatim evidence)
  </read_first>
  <behavior>
    - Test 1 (round-trip): a saved project containing one efx_paint_documents entry (arbitrary nested JSON document payload) reopens with the entry byte-identical (serde_json::Value equality).
    - Test 2 (opaque legacy carrier): a project file containing a legacy physic_paint_outputs array opens with physic_paint_outputs.len() == 1 (presence visible to the TS gate) without any field of the blob being interpreted.
    - Test 3 (skip-when-empty): saving a project with empty efx_paint_documents and empty physic_paint_outputs produces JSON containing neither key.
    - Test 4 (compile-enforced deletion): McePhysicPaintOutput, McePhysicPaintCachedFrame, McePhysicPaintRotoPlaybackSettings no longer exist; existing project_io.rs tests that constructed typed physic_paint_outputs (lines ~214, 286, 339, 545) are updated to the opaque form in the same diff.
  </behavior>
  <action>
    In app/src-tauri/src/models/project.rs: delete the legacy structs McePhysicPaintOutput, McePhysicPaintCachedFrame, McePhysicPaintRotoPlaybackSettings and their serde impls; change MceProject.physic_paint_outputs to Vec&lt;serde_json::Value&gt; keeping #[serde(default, skip_serializing_if = "Vec::is_empty")] (opaque presence carrier for the gate — never interpreted per D-02); add pub efx_paint_documents: std::collections::HashMap&lt;String, serde_json::Value&gt; with #[serde(default, skip_serializing_if = "HashMap::is_empty")] (opaque document map — TS owns the fail-closed schema, mirroring the existing roto_physical: Option&lt;serde_json::Value&gt; escape hatch at models/project.rs:36-37). In app/src/types/project.ts: add efx_paint_documents?: Record&lt;string, unknown&gt; to MceProject and mirror on RuntimeMceProject; do NOT touch the legacy TS output types yet (they remain until the legacy module is deleted in plan 45-07 — changing them now breaks the still-present legacy module's compile). In project_io.rs: update every test constructing typed physic_paint_outputs to construct serde_json::Value blobs instead; extend test_save_and_open_roundtrip with behaviors 1-3. RED first (write/extend tests, watch them fail against the old struct), then implement to green. Commit test(45-02) then feat(45-02): add v1.0 document key to Rust+TS project models.
  </action>
  <verify>
    <automated>cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - cargo test exits 0 including the extended round-trip test
    - models/project.rs contains `efx_paint_documents` and does NOT contain `struct McePhysicPaintOutput`
    - types/project.ts contains `efx_paint_documents` on both MceProject and RuntimeMceProject
    - project_io.rs save/open functions unchanged in behavior except the model change (atomic temp+rename + sync_all pattern intact at 37-85)
  </acceptance_criteria>
  <done>The v1.0 document key round-trips through Rust save/open; legacy typed structs are gone; the opaque carrier preserves gate visibility of legacy blobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create_project_dir creates the v1.0 cache dir, never the legacy dir</name>
  <files>app/src-tauri/src/services/project_io.rs</files>
  <read_first>
    - app/src-tauri/src/services/project_io.rs:7-33 (create_project_dir current state, legacy cache/physic-paint creation at 14, 26-27) and the dir-assertion test (~line 185)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md (D-04: legacy data never read, never deleted)
  </read_first>
  <behavior>
    - Test 1: create_project_dir on a fresh path creates cache/efx-paint (assert exists).
    - Test 2: create_project_dir on a fresh path does NOT create cache/physic-paint (assert absent).
    - Test 3: create_project_dir on a path that already contains a legacy cache/physic-paint directory leaves it byte-untouched (directory still exists with its prior contents afterward — no read, move, or delete; D-04).
  </behavior>
  <action>
    In project_io.rs create_project_dir: replace the legacy cache/physic-paint creation block with creation of cache/efx-paint (same create_dir_all + mapped error style). Update the existing dir-assertion test: assert the new dir exists AND the legacy dir does not. Add behavior test 3 for the D-04 non-destruction guarantee. Tests RED first, then GREEN. Commit test(45-02) then feat(45-02): create v1.0 cache dir for new projects.
  </action>
  <verify>
    <automated>cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - cargo test exits 0 with all 3 behavior tests passing
    - project_io.rs contains `cache/efx-paint` (as joined segments cache + efx-paint) and no longer creates cache/physic-paint in create_project_dir
    - No fs::remove / rename / read_dir call targeting physic-paint exists in create_project_dir (D-04)
  </acceptance_criteria>
  <done>New projects get cache/efx-paint; the legacy dir is neither created nor touched on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-point native cache transaction service to the v1.0 directory and staging prefix</name>
  <files>app/src-tauri/src/services/physic_paint_cache.rs</files>
  <read_first>
    - app/src-tauri/src/services/physic_paint_cache.rs:1-80 (cache root constants, publish_cache_generation at 49) and 140, 175, 205 (bind/settle/recover)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md (staging/commit two-resource transaction rules)
  </read_first>
  <behavior>
    - Test 1: publish_cache_generation stages under .efx-paint-staging-&lt;uuid&gt; and publishes into cache/efx-paint.
    - Test 2: settle commit swaps the staged generation into cache/efx-paint; rollback removes the staging dir leaving the previously committed generation intact.
    - Test 3: no service function can produce a path under cache/physic-paint (path construction is prefix-locked to the v1.0 root).
    - Test 4: existing command names and transaction-id payload validation are unchanged (no new IPC surface — Elevation-of-Privilege control).
  </behavior>
  <action>
    Re-point physic_paint_cache.rs: change the cache root constant from cache/physic-paint to cache/efx-paint and the staging prefix from .physic-paint-staging- to .efx-paint-staging-. Keep the command surface (publish_cache_generation / bind_cache_transaction_to_project_write / settle_cache_generation / recover_cache_transaction) and all transaction-id validation identical — this is a re-point, not a rewrite (research Pattern 2). Update the service's existing tests to the new paths and add behavior test 3 (attempting to publish with a crafted staging basename containing ../ or an absolute path fails the existing validation and never escapes the v1.0 root). Tests RED then GREEN. Commit test(45-02) then feat(45-02): re-point native cache service to v1.0 directory.
  </action>
  <verify>
    <automated>cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - cargo test exits 0 for the whole crate (cache service + project_io + models)
    - physic_paint_cache.rs contains `efx-paint` path constants and no `cache/physic-paint` path construction remains
    - No new #[tauri::command] functions added (grep: command count unchanged)
  </acceptance_criteria>
  <done>The native staging/commit transaction operates exclusively on cache/efx-paint; the legacy cache path is unreachable from native code.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- Modified files: `app/src-tauri/src/models/project.rs`, `app/src-tauri/src/services/project_io.rs`, `app/src-tauri/src/services/physic_paint_cache.rs`, `app/src/types/project.ts`
- New struct fields: `MceProject.efx_paint_documents: HashMap<String, serde_json::Value>` (Rust); `MceProject.efx_paint_documents?: Record<string, unknown>` + `RuntimeMceProject` mirror (TS)
- Deleted symbols: `McePhysicPaintOutput`, `McePhysicPaintCachedFrame`, `McePhysicPaintRotoPlaybackSettings` (Rust structs)
- New path constants: `cache/efx-paint`, `.efx-paint-staging-` (native cache service)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce file → Rust serde | Untrusted project JSON parsed by open_project |
| project file → native cache service | Crafted cache paths could attempt traversal into arbitrary dirs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-04 | Tampering | physic_paint_cache.rs path construction | high | mitigate | Prefix-locked v1.0 root; staging basename validation rejects ../, absolute paths, NUL (Task 3 behavior test 3); mirrors isSafe*CachePath segment allow-list (ASVS V12) |
| T-45-05 | Tampering | legacy on-disk data | high | mitigate | D-04 non-destruction: create_project_dir never reads/moves/deletes a pre-existing legacy cache dir (Task 2 behavior test 3) |
| T-45-06 | Elevation of Privilege | IPC command surface | medium | mitigate | No new commands; existing publish/bind/settle/recover keep transaction-id validation unchanged (Task 3 behavior test 4) |
| T-45-07 | Information Disclosure | opaque legacy carrier | low | accept | Legacy blob presence is round-tripped to TS for gate detection only; never interpreted or rendered (D-02/D-06) |
</threat_model>

<verification>
- `cargo test --manifest-path app/src-tauri/Cargo.toml` — whole crate green
- `pnpm --filter efx-motion-editor exec vitest run` — TS suite still green (types/project.ts addition is additive)
- `pnpm --dir app run typecheck` — no type regressions
</verification>

<success_criteria>
- The v1.0 document key survives a real Rust save → open round-trip (F1 closed with proof, not hope).
- No legacy Rust struct, legacy cache-dir creation, or legacy cache-path construction remains reachable from native code.
- Legacy on-disk user data is provably untouched (D-04 test).
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-02-SUMMARY.md` when done
</output>
