---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 06
type: execute
wave: 4
depends_on: ["45-04"]
files_modified:
  - app/src/components/physic-paint/bridge/physicsPaintSessionFile.ts
  - app/src/components/physic-paint/bridge/physicsPaintSessionFile.test.ts
  - app/src/types/physicPaint.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts
  - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts
  - app/src/components/physic-paint/roto/rotoLaunchHydration.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - packages/efx-physic-paint/src/types.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
autonomous: true
requirements: [DOC-04, DOC-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Session-file save writes the v1.0 document payload; parsePhysicsPaintStateFile accepts v1.0 session files and rejects legacy version:2 files with a distinct explicit pre-v1.0 unsupported message, while genuinely malformed files keep the generic invalid-file message (D-03, Pitfall F5)"
    - "PhysicPaintLaunchContext carries the v1.0 document (research Q3 recommendation: carry the document, matching current editableState behavior — no fetch round-trip); the legacy fields editableState, rotoPhysical, cachedRotoFrames, rotoInterpolationSettings are gone from the launch contract"
    - "The standalone @efxlab/efx-physic-paint engine save()/load() round-trips the v1.0 document shape; loading a version:2 payload rejects with the same distinct pre-v1.0 message (D-03: one document format everywhere)"
    - "Studio launch receives the document from efxPaintStore via the bridge and hydrates the engine from it; a documentless launch (fresh document from AddFxMenu) opens the Studio on the default track (UAT part 1 foundation)"
    - "The DF-04 postMessage surface is not widened: no new bridge events, no new launch fields beyond the document carrier swap"
  artifacts:
    - app/src/components/physic-paint/bridge/physicsPaintSessionFile.ts (modified)
    - app/src/components/physic-paint/bridge/physicsPaintSessionFile.test.ts
    - app/src/types/physicPaint.ts (modified)
    - app/src/lib/physicPaintBridge.ts (modified — launch-context constructor)
    - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts (modified — launch-payload validator re-authored)
    - app/src/components/physic-paint/roto/rotoLaunchHydration.ts (modified — document hydration)
    - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts (modified)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (modified)
    - packages/efx-physic-paint/src/types.ts (modified)
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts (modified)
  key_links:
    - "The session payload IS the 45-01 EfxPaintDocument — the standalone package declares a structurally identical v1.0 document type (workspace package cannot import app/src); the app-side bridge validates inbound payloads fail-closed with parseEfxPaintDocument"
    - "LOAD_STATE_INVALID_COPY stays for malformed JSON; the new LOAD_STATE_UNSUPPORTED_VERSION_COPY fires only for recognized legacy version:2 shapes (F5)"
    - "Native save (PHYSIC_PAINT_STATE_SAVE_REQUEST_EVENT round-trip) and browser blob download paths both stay — only the serialized payload changes (PATTERNS adapter split)"
  prohibitions:
    - "Old session files are never migrated, partially loaded, or best-effort guessed — recognized legacy shapes reject explicitly (D-03, spec clean-break boundary)"
    - "No new bridge events or postMessage surfaces; the DF-04 deferred surface stays exactly as wide as it is today"
---

<objective>
Re-wire the Studio session-file contract, the parent bridge launch context, and the standalone package engine to the v1.0 document format — one document format everywhere (D-03) — with explicit, distinct rejection of legacy version:2 session files (F5).

Purpose: The spec requires updating session-file and parent bridge contracts "for the new format only." The standalone app remains the reference oracle and adopts the same v1.0 document; its old session files are rejected the same way old .mce data is.

Output: New session payload, new launch-context carrier, re-wired engine save/load, and contract tests for both rejection classes.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@app/src/components/physic-paint/bridge/physicsPaintSessionFile.ts
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/bridge/physicPaintBridge.ts
@app/src/efx-paint/document/efxPaintDocumentParsers.ts
@app/src/stores/efxPaintStore.ts
@packages/efx-physic-paint/src/types.ts
@packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Session-file v1.0 format — save/load round-trip + distinct legacy rejection (end-to-end session path)</name>
  <files>app/src/components/physic-paint/bridge/physicsPaintSessionFile.ts, app/src/components/physic-paint/bridge/physicsPaintSessionFile.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/bridge/physicsPaintSessionFile.ts (whole file, 155 lines: copy constants 6-10, parse idiom 46-54, filename idiom 85, native save round-trip 95-134, browser download 141-154)
    - app/src/types/physicPaint.ts:2244 (isSerializedProject v2 guard being replaced)
    - app/src/efx-paint/document/efxPaintDocumentParsers.ts (parseEfxPaintDocument — the fail-closed validator for inbound session payloads)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Pitfall F5 verbatim)
  </read_first>
  <behavior>
    - RED Test 1 (round-trip): serializing a session produces JSON whose payload is a v1.0 EfxPaintDocument; parsePhysicsPaintStateFile of that payload returns the document with identity intact (version, parentLayerId, track IDs, fallback).
    - RED Test 2 (legacy distinct rejection): a well-formed legacy file with version: 2 and the old strokes/settings shape throws LOAD_STATE_UNSUPPORTED_VERSION_COPY — a message explicitly stating the file is a pre-v1.0 Physics Paint session that v1.0.0 does not support (F5; D-07 tone: no recourse).
    - RED Test 3 (malformed stays generic): invalid JSON or an unrecognized shape throws LOAD_STATE_INVALID_COPY (unchanged constant).
    - RED Test 4 (fail-closed): a v1.0-shaped payload with an unknown member throws (delegates to parseEfxPaintDocument) — surfaced as LOAD_STATE_INVALID_COPY.
    - RED Test 5: the save filename marker distinguishes v1.0 sessions (new prefix, e.g. 'efx-paint-doc-&lt;timestamp&gt;.json') so old 'efx-paint-state-*' files are detectable by content AND name.
  </behavior>
  <action>
    Rewrite physicsPaintSessionFile.ts around the v1.0 document: exported copy constants keep LOAD_STATE_INVALID_COPY verbatim and add LOAD_STATE_UNSUPPORTED_VERSION_COPY (explicit pre-v1.0 wording, English constant following existing convention). parsePhysicsPaintStateFile: JSON.parse → if the parsed record has version === 2 (or matches the legacy strokes/settings shape) throw the UNSUPPORTED copy → else validate via parseEfxPaintDocument, mapping any parse throw to LOAD_STATE_INVALID_COPY. Save path serializes the document from efxPaintStore; keep both the native save-request event round-trip and the browser blob download, changing only the payload and filename prefix. Replace isSerializedProject usage accordingly (the v2 guard is deleted here; its type declaration cleanup lands with the types/physicPaint.ts work in Task 2). Tests RED commit test(45-06): add failing v1.0 session-file tests → GREEN commit feat(45-06): v1.0 session-file format with distinct legacy rejection.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/bridge/physicsPaintSessionFile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behavior tests pass
    - physicsPaintSessionFile.ts exports both LOAD_STATE_INVALID_COPY and LOAD_STATE_UNSUPPORTED_VERSION_COPY with distinct messages
    - Legacy version:2 fixture input rejects with the UNSUPPORTED copy; malformed input rejects with the INVALID copy (two distinct assertions)
    - No code path converts or partially reads legacy strokes/settings (grep: file contains no mapping of legacy stroke fields)
  </acceptance_criteria>
  <done>Session files are the v1.0 document; old files reject with an unmistakable pre-v1.0 message; corrupt files keep the generic error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Launch-context contract swap — bridge carries the v1.0 document (atomic cutover: declaration, guard, validator, constructor, and every consumer in one commit)</name>
  <files>app/src/types/physicPaint.ts, app/src/lib/physicPaintBridge.ts, app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts, app/src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts, app/src/components/physic-paint/roto/rotoLaunchHydration.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/bridge/physicsPaintSessionFile.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts:1690-1706 (PhysicPaintLaunchContext legacy payload fields) and :1935-1953 (isPhysicPaintLaunchContext guard referencing them)
    - app/src/lib/physicPaintBridge.ts:214-220 (activatePhysicalLaunchAuthority reads context.rotoPhysical capacity) and :2576-2656 (createPhysicPaintLaunchContext builds the rotoPhysical envelope section and self-validates through parseCanonicalPhysicsPaintLaunchValue)
    - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts (whole file: LAUNCH_KEYS 15, PHYSICAL_KEYS 16, parseCanonicalPhysicsPaintLaunchValue 56-133 — the launch-payload validator to re-author)
    - app/src/components/physic-paint/roto/rotoLaunchHydration.ts:30-80 (prepareRotoPhysicalLaunch parses context.rotoPhysical into the roto physical model)
    - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts:87 (cachedRotoFrames type reference) and :122-123 (launch background resolved from rotoPhysical.background)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:278 (initial roto frames from launchContext.cachedRotoFrames) and :1517 (launchContext.rotoPhysical.layerEndExclusive)
    - app/src/stores/efxPaintStore.ts (getDocument / document source at launch)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Open Question Q3 — carry the document, verbatim recommendation; A4 — retained runtime-state names)
  </read_first>
  <behavior>
    - Test 1: PhysicPaintLaunchContext exposes a document carrier (serialized v1.0 EfxPaintDocument payload) and the 4 removed launch-payload field declarations no longer exist on the type (compile-enforced; typecheck is the gate).
    - Test 2: createPhysicPaintLaunchContext populates the carrier from efxPaintStore.getDocument(parentLayerId) at Studio launch; bridge messages include parentLayerId and documentRevision (spec identity rule for bridge messages).
    - Test 3: parseCanonicalPhysicsPaintLaunchValue validates the carried document fail-closed via parseEfxPaintDocument — an envelope with an unknown document member or a missing document parses to null (launch refused, no partial hydration).
    - Test 4 (launch-contract scoping gate — checker revision): within the launch-contract surface ONLY (the PhysicPaintLaunchContext interface body, isPhysicPaintLaunchContext, parseCanonicalPhysicsPaintLaunchValue, createPhysicPaintLaunchContext), the removed launch-payload fields are neither declared nor referenced; typecheck green proves every former consumer was rewritten in this commit. Retained runtime state is explicitly OUT of scope and keeps its existing names — the physicPaintStore interpolation-settings runtime map, the physicsPaintRotoSession cached-frames signal, the rotoSaveTransactions editable-state types, and the per-track rotoPhysical schema field retained by 45-01 (research A4; 45-07 runtime maps stay). No mass renames; no tree-wide identifier ban.
    - Test 5 (Studio-side hydration — behavior-significant, NOT mechanical): prepareRotoPhysicalLaunch parses the roto physical model from the carried document's ACTIVE track (via the existing physicsPaintRotoPhysicalModel parser) instead of the removed envelope section; usePhysicsPaintLaunchIntegration resolves the launch background from the active track's rotoPhysical background; PhysicsPaintStudio sources its initial roto frames and layerEndExclusive from the hydrated document/runtime instead of the removed launch fields; a launch carrying a fresh AddFxMenu document opens the Studio with the default track active.
  </behavior>
  <action>
    This task intentionally exceeds the 5-file guideline: removing fields from PhysicPaintLaunchContext breaks every consumer at typecheck, so the type change and all consumer rewrites must land in ONE commit to keep every commit green. Steps, in order: (a) In types/physicPaint.ts remove the 4 legacy payload fields from PhysicPaintLaunchContext and add the document carrier field (serialized EfxPaintDocument payload); rewrite isPhysicPaintLaunchContext to validate the carrier via parseEfxPaintDocument and drop the removed-field guard lines; remove the now-dead isSerializedProject guard and SerializedProject import usages on the app side. (b) In lib/physicPaintBridge.ts createPhysicPaintLaunchContext: build the carrier from efxPaintStore.getDocument(layerId) — launch carries the full document per Q3, no fetch round-trip — replacing the constructed rotoPhysical envelope section (the document's active track already holds the same roto physical model JSON); keep parentLayerId + documentRevision in bridge messages per the spec identity rules; repoint activatePhysicalLaunchAuthority's capacity read to the carried document's active track. (c) Re-author the launch-payload validator in bridge/physicsPaintLaunchContext.ts: update LAUNCH_KEYS for the carrier swap, re-validate the document fail-closed via parseEfxPaintDocument, and keep the canonical-envelope self-check in createPhysicPaintLaunchContext passing. (d) In roto/rotoLaunchHydration.ts prepareRotoPhysicalLaunch: parse the roto physical model from the carried document's active track rotoPhysical via parsePhysicPaintRotoPhysicalDocument; keep the cursor/projection checks and the fail-closed result union. (e) In hooks/usePhysicsPaintLaunchIntegration.ts: replace the removed-field type reference and resolve the launch background from the active track's rotoPhysical background. (f) In PhysicsPaintStudio.tsx: source the initial roto frames ref and the layerEndExclusive read from the hydrated document/runtime (the post-launch physicPaintStore state), not the removed launch fields. The Studio-side engine session save/load call sites (usePhysicsPaintSessionController engine.save()/engine.load()) keep compiling against the package's CURRENT engine API here — the package re-wire lands in Task 3 of this plan. Extend physicsPaintLaunchContext.test.ts, usePhysicsPaintLaunchIntegration.test.ts, PhysicsPaintStudio.test.ts, and physicsPaintSessionFile.test.ts for behaviors 2-5 with the mocked bridge surface; typecheck is the primary gate for 1 and 4. Commit feat(45-06): bridge launch context carries the v1.0 document.
  </action>
  <verify>
    <automated>pnpm --dir app run typecheck && pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/bridge/physicsPaintSessionFile.test.ts src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exits 0
    - types/physicPaint.ts PhysicPaintLaunchContext contains the document carrier and none of the 4 removed launch-payload field declarations (region-scoped check on the interface body)
    - Bridge test asserts the launch context document equals the efxPaintStore document for that parentLayerId
    - Studio hydration tests assert the roto session resolves from the carried document's active track and a fresh document opens on the default track (Test 5 behaviors)
    - Retained runtime modules (physicPaintStore.ts, physicsPaintRotoSession.ts, rotoSaveTransactions.ts) are NOT renamed in this diff (research A4)
    - No new postMessage event types introduced (diff assertion on the bridge events enum/union)
  </acceptance_criteria>
  <done>The launch contract speaks v1.0 documents only; the validator, constructor, and all Studio-side consumers are re-authored in one typecheck-green commit; retained runtime state names are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Standalone package engine save/load re-wired to the v1.0 document (D-03)</name>
  <files>packages/efx-physic-paint/src/types.ts, packages/efx-physic-paint/src/engine/EfxPaintEngine.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/types.ts:186-207 (legacy SerializedProject version: 2 shape being replaced)
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:1082-1091 (save()/load() entry points) and 2141-2143 (serializeProject writes version: 2)
    - app/src/efx-paint/document/efxPaintDocument.ts (the app-side schema the package mirrors structurally)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md (D-03: one document format everywhere; standalone remains the reference oracle)
  </read_first>
  <behavior>
    - Test 1: engine.save() returns a v1.0 document-shaped payload (version 1 document shape matching the app-side schema: parentLayerId/documentRevision/activeTrackId/tracks/background/photoReference/compositeRevision); engine.load() of that payload restores the engine state (strokes on the default track render identically — assert via the engine's existing state inspection/test hooks).
    - Test 2: engine.load() of a legacy version:2 payload throws/rejects with the distinct pre-v1.0 unsupported error (same semantics as the app-side UNSUPPORTED copy).
    - Test 3: engine.load() of a malformed payload throws the generic invalid error (fail-closed; unknown members throw).
    - Test 4: the package's SerializedProject type is replaced by the v1.0 document declaration (structural mirror of the app schema — the workspace package cannot import app/src); serializeProject no longer writes version: 2.
  </behavior>
  <action>
    In packages/efx-physic-paint/src/types.ts: replace SerializedProject { version: 2, width, height, strokes, settings } with a v1.0 document interface structurally identical to the app-side EfxPaintDocument (declare it locally; keep field names identical so payloads inter-operate across the bridge without mapping). In EfxPaintEngine.ts: re-wire save()/serializeProject() to emit the v1.0 document (default-track payload carries the engine's strokes/settings per the 45-01 track payload shape) and load()/loadProjectData() to hydrate from it fail-closed — legacy version:2 detected explicitly and rejected with the distinct unsupported error; unknown members throw. Keep flushPendingStrokeFinalizations() behavior on both entry points. Run the package's existing test suite plus the new behaviors using the package's own test setup (vitest run only; no new configs). Commit test(45-06) then feat(45-06): standalone engine adopts the v1.0 document format.
  </action>
  <verify>
    <automated>pnpm --filter @efxlab/efx-physic-paint exec vitest run && pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/bridge</automated>
  </verify>
  <acceptance_criteria>
    - Package tests exit 0 including the 4 behavior tests
    - packages/efx-physic-paint/src/types.ts no longer declares version: 2 SerializedProject; EfxPaintEngine.ts serializeProject writes the v1.0 document shape
    - App-side bridge suites still green after the package change (integration compile)
    - pnpm --dir app run typecheck exits 0
  </acceptance_criteria>
  <done>One document format everywhere: standalone engine, session files, and bridge all speak the v1.0 document; legacy v2 payloads reject explicitly.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New symbols: `LOAD_STATE_UNSUPPORTED_VERSION_COPY` (session file), v1.0 document interface in `packages/efx-physic-paint/src/types.ts` (replacement for `SerializedProject`), document carrier field on `PhysicPaintLaunchContext`
- Deleted symbols: `isSerializedProject` (v2 guard), `SerializedProject` (version-2 interface), the 4 removed launch-payload fields from `PhysicPaintLaunchContext` (launch contract only — retained runtime modules keep their existing names per research A4)
- New session filename prefix: `efx-paint-doc-<timestamp>.json`
- Modified: `physicsPaintSessionFile.ts`, `lib/physicPaintBridge.ts` (launch-context constructor), `bridge/physicsPaintLaunchContext.ts` (validator re-authored), `roto/rotoLaunchHydration.ts` (document hydration), `hooks/usePhysicsPaintLaunchIntegration.ts`, `PhysicsPaintStudio.tsx`, `types/physicPaint.ts`, `packages/efx-physic-paint/src/types.ts`, `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session file → parsePhysicsPaintStateFile | User-supplied JSON file imported into the app |
| parent window ↔ Studio window (postMessage) | Existing DF-04 surface — must not be widened |
| app ↔ workspace package | Document payloads cross the package boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-19 | Tampering | session-file parse | high | mitigate | Fail-closed via parseEfxPaintDocument; recognized legacy v2 → distinct unsupported rejection; malformed → generic invalid; no partial read (Task 1 tests 2-4) |
| T-45-20 | Elevation of Privilege | bridge/postMessage surface | medium | mitigate | No new events or channels; launch-context swap reuses the existing message flow (Task 2 acceptance: events union unchanged). DF-04 origin-authentication debt acknowledged as-is, not widened |
| T-45-21 | Tampering | engine load() | high | mitigate | Fail-closed hydrate; legacy v2 explicit reject; unknown members throw (Task 3 tests 2-3) |
</threat_model>

<verification>
- `pnpm --filter @efxlab/efx-physic-paint exec vitest run` — package suite green
- `pnpm --filter efx-motion-editor exec vitest run` — app suite green
- `pnpm --dir app run typecheck`
- `pnpm build` (workspace) — tsup rebuild of the package succeeds with the new types
</verification>

<success_criteria>
- Session save/load, bridge launch, and the standalone engine all operate on the v1.0 document (D-03).
- Legacy v2 artifacts are explicitly and distinctly rejected everywhere they can appear (F5).
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-06-SUMMARY.md` when done
</output>
