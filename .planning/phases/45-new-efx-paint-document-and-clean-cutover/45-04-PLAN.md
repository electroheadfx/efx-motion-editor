---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 04
type: execute
wave: 3
depends_on: ["45-01", "45-02"]
files_modified:
  - app/src/stores/efxPaintStore.ts
  - app/src/lib/efxPaintPersistence.ts
  - app/src/lib/efxPaintPersistence.test.ts
  - app/src/stores/efxPaintStore.test.ts
  - app/src/stores/physicPaintStore.ts
autonomous: true
requirements: [DOC-01, DOC-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "efxPaintStore maps layerId → EfxPaintDocument in a non-reactive Map with an efxPaintVersion counter-signal bumped on every mutation, an injected dirty callback, and a reset() hook (physicPaintStore conventions)"
    - "Saving a v1.0 document stages PNG sidecars under cache/efx-paint via .efx-paint-staging-, writes the .mce with the bound cache transaction, and settles commit/rollback — the proven two-resource transaction shape (DOC-05)"
    - "Saving an unchanged document twice produces identical persisted output and skips sidecar rewrites via content-fingerprint dedup (edge: idempotency, resolved explicit)"
    - "If the project write throws after staging, the staged cache generation is rolled back and the previously committed generation plus prior .mce remain intact (edge: concurrency/interruption, resolved explicit)"
    - "Loading a v1.0 document hydrates efxPaintStore and projects the default track's frames/rotoPhysical payload into the layerId-keyed physicPaintStore runtime maps (research A4/Q1 boundary projection)"
    - "Serializing projects the layerId-keyed runtime maps into the document's single default track payload — track-local re-addressing is NOT pulled into Phase 45 (research A4)"
    - "All v1.0 sidecar paths pass isSafeEfxPaintCachePath: prefix-locked, no backslash, no absolute, no NUL, no empty/dot segments (ASVS V12)"
  artifacts:
    - app/src/stores/efxPaintStore.ts
    - app/src/lib/efxPaintPersistence.ts
    - app/src/lib/efxPaintPersistence.test.ts
    - app/src/stores/efxPaintStore.test.ts
  key_links:
    - "efxPaintPersistence calls the same native publish/settle commands re-pointed in 45-02 — TS orchestrator shape copied from savePhysicPaintDataWithProjectWrite (physicPaintPersistence.ts:320-340)"
    - "physicPaintStore.toMceOutputs/loadFromMceOutputs are replaced as the serialization entry points by the document projection (their deletion lands in 45-07; this plan adds the replacement)"
    - "efxPaintStore.reset() must slot into projectStore.closeProject alongside the existing physicPaintStore.reset call (wired in 45-05)"
  prohibitions:
    - "No useEffect for internal state derivation and no React-style hook/effect sprawl — signals, computed, and plain functions only (CLAUDE.md)"
    - "No vitest watch mode and no one-off test configs — existing vitest setup with `vitest run` only (CLAUDE.md)"
    - "Never direct-write canonical cache paths — stage → write project → settle, always (PATTERNS shared transaction rule)"
---

<objective>
Build the reactive document store and the v1.0 TS persistence service: staging/commit save, fail-closed load, runtime↔document projection at the single default track, path safety, and content-fingerprint dedup.

Purpose: DOC-05 demands save/reopen preserve document, track, and cache identity. Research Pattern 2 reuses the proven staging/commit transaction verbatim with the new dir/prefix from 45-02. Research A4 (runtime seam) keeps the existing layerId-keyed runtime maps in Phase 45 and projects them into the document's default track at the persistence boundary — this plan implements that projection in both directions.

Output: `efxPaintStore.ts` + `efxPaintPersistence.ts` fully unit-tested with mocked fs/IPC.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@app/src/efx-paint/document/efxPaintDocument.ts
@app/src/efx-paint/document/efxPaintDocumentParsers.ts
@app/src/lib/physicPaintPersistence.ts
@app/src/stores/physicPaintStore.ts
@app/src/lib/ipc.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: efxPaintStore + one-document save→load round-trip through staging/commit (end-to-end persistence path)</name>
  <files>app/src/stores/efxPaintStore.ts, app/src/lib/efxPaintPersistence.ts, app/src/lib/efxPaintPersistence.test.ts, app/src/stores/efxPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:1-100 (imports, counter-signal, dirty-callback injection, non-reactive Map conventions)
    - app/src/lib/physicPaintPersistence.ts:17-24 (constants block to substitute), 265-294 (staging write + native publication), 320-340 (savePhysicPaintDataWithProjectWrite orchestrator shape to reuse)
    - app/src/lib/physicPaintPersistence.test.ts:1-75 (hoisted-mock pattern for fs + IPC, exchangeGeneration helper)
    - app/src/lib/ipc.ts:327-340 (publish/settle wrapper signatures)
  </read_first>
  <behavior>
    - RED Test 1 (store): registerDocument(createEfxPaintDocument('layer-x')) → getDocument('layer-x') returns the document; efxPaintVersion increments; injected dirty callback fires.
    - RED Test 2 (round-trip): save a store holding one document via saveEfxPaintDocumentsWithProjectWrite with mocked fs/ipc → staging writes occur under cache/.efx-paint-staging-&lt;uuid&gt;, writeProject callback receives the efx_paint_documents payload + transaction id, settle commit fires; then loadEfxPaintDocuments from the persisted payload restores a document deep-equal on version/parentLayerId/documentRevision/activeTrackId/track IDs/background/fallback.
    - RED Test 3 (fail-closed load): loadEfxPaintDocuments with an unknown document member throws (delegates to parseEfxPaintDocument).
    - RED Test 4 (reset): reset() empties the map and bumps the version signal.
  </behavior>
  <action>
    Create app/src/stores/efxPaintStore.ts mirroring physicPaintStore conventions: non-reactive Map&lt;string, EfxPaintDocument&gt;, export const efxPaintVersion = signal(0), _setEfxPaintMarkDirtyCallback injection, registerDocument/getDocument/hasDocument/removeDocument/reset, every mutation bumps efxPaintVersion AND fires the dirty callback (MEMORY: always bump AND subscribe). No useEffect anywhere (CLAUDE.md). Create app/src/lib/efxPaintPersistence.ts with constants EFX_PAINT_CACHE_DIR = 'cache/efx-paint', EFX_PAINT_CACHE_PARENT_DIR = 'cache', EFX_PAINT_STAGING_PREFIX = '.efx-paint-staging-' (A2) and the orchestrator saveEfxPaintDocumentsWithProjectWrite(projectDir, documents, writeProject) copying the shape of savePhysicPaintDataWithProjectWrite: prepare (serialize documents via the 45-01 model, stage sidecars under a UUID staging basename, publishPhysicPaintCacheGeneration) → try writeProject(payload, transactionId) catch → settle rollback + rethrow → settle commit. Loader loadEfxPaintDocuments(projectRoot, persistedMap) validates via parseEfxPaintDocument (fail-closed) and reads sidecar PNGs through the existing plugin-fs idiom. Tests use the hoisted-mock + in-memory files/exchangeGeneration pattern from physicPaintPersistence.test.ts. RED commit test(45-04): add failing store+persistence round-trip tests → GREEN commit feat(45-04): efx-paint document store and v1.0 persistence.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPersistence.test.ts src/stores/efxPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exit 0 with the 4 behavior tests passing
    - efxPaintPersistence.ts contains `cache/efx-paint` and `.efx-paint-staging-` and contains no `cache/physic-paint` path construction
    - efxPaintStore.ts imports @preact/signals and contains no useEffect/useState import
    - writeProject failure path calls settle with rollback (test proves it in Task 3; orchestrator code path present)
  </acceptance_criteria>
  <done>One document saves and reloads with identity intact through the real staging/commit transaction shape; store reactivity follows project conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime ↔ default-track projection (serialization boundary, research A4)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/efxPaintStore.ts, app/src/stores/efxPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:921-989 (toMceOutputs/loadFromMceOutputs — the legacy entry points being replaced; their runtime map structure is the projection source)
    - app/src/efx-paint/document/efxPaintDocument.ts (InternalPaintTrack.frames / rotoPhysical payload shapes from 45-01)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Open Question Q1 + assumption A4 — projection at the boundary, verbatim recommendation)
  </read_first>
  <behavior>
    - Test 1 (outbound): with runtime paint/roto state present for layerId L, projecting into L's document writes that state into the document's SINGLE default track payload (frames + rotoPhysical) and increments documentRevision; no other track is touched.
    - Test 2 (inbound): hydrating a document whose default track carries frames/rotoPhysical restores the runtime maps for the parent layerId so the Studio shows the same strokes (UAT part 2 foundation).
    - Test 3 (round-trip identity): runtime → document → runtime yields the same strokes/keys/caches by reference-stable identity (frame keys, roto real key IDs preserved).
    - Test 4 (empty runtime): projecting an empty runtime produces a schema-valid document with empty default-track payload (parser accepts it).
    - Test 5 (isolation): projecting layer A never reads or writes layer B's runtime maps.
  </behavior>
  <action>
    Add to efxPaintStore.ts the projection entry points serializeRuntimeIntoDocument(layerId) and hydrateRuntimeFromDocument(document) that call NEW exported accessors on physicPaintStore (e.g. extractRuntimeStateForDocument(layerId) / installRuntimeStateFromDocument(layerId, payload)) — these replace toMceOutputs/loadFromMceOutputs as the serialization seam (the legacy functions themselves are deleted in 45-07; do not delete them in this plan to keep the legacy test suite compiling until then). The projection addresses exactly the document's single default track (document.tracks[0], asserted === activeTrackId); if a document ever has more than one Paint track, outbound projection throws (Phase 46 owns multi-track addressing — fail loud rather than guess). Document revision increments via the 45-01 revision builders. All 5 tests RED then GREEN. Commit test(45-04) then feat(45-04): runtime-to-document default-track projection.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 projection tests pass
    - efxPaintStore.ts exports serializeRuntimeIntoDocument and hydrateRuntimeFromDocument
    - Outbound projection throws on multi-Paint-track documents (test 4 companion assertion present)
    - physicPaintStore.ts keeps toMceOutputs/loadFromMceOutputs present (deletion is 45-07 scope) while new accessors are added alongside
  </acceptance_criteria>
  <done>The layerId-keyed runtime round-trips through the document's default track with identity preserved; multi-track documents fail loud instead of guessing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Path safety, content-fingerprint dedup, and rollback-on-failure hardening</name>
  <files>app/src/lib/efxPaintPersistence.ts, app/src/lib/efxPaintPersistence.test.ts</files>
  <read_first>
    - app/src/lib/efxPaintPersistence.ts (current state after Task 1)
    - app/src/lib/physicPaintPersistence.ts:48-59 (sanitizeSegment/stableSegment FNV-1a), 89-93 (isSafePhysicPaintCachePath guard shape), 26 + 112-120 (savedOutputCache dedup)
  </read_first>
  <behavior>
    - Test 1 (path guard): isSafeEfxPaintCachePath accepts 'cache/efx-paint/&lt;stable-segment&gt;/frame.png' and rejects absolute paths, backslashes, NUL bytes, empty segments, '.' and '..' segments, and wrong prefixes.
    - Test 2 (idempotency truth): saving the same unchanged document twice produces an identical persisted payload and performs ZERO sidecar writeFile calls on the second save (content-fingerprint dedup, mirroring savedOutputCache).
    - Test 3 (concurrency/interruption truth): when the writeProject callback throws after staging, settle is called with 'rollback', the staging generation is removed, and the previously committed generation remains published (exchangeGeneration mock asserts canonical path still holds the prior generation).
    - Test 4 (stableSegment): same input → same segment across calls; different layerIds → different segments; sanitization replaces unsafe characters.
  </behavior>
  <action>
    Port the path-safety helpers into efxPaintPersistence.ts: sanitizeSegment + stableSegment (FNV-1a) copied verbatim, and isSafeEfxPaintCachePath mirroring the guard shape with the v1.0 prefix substituted. Add a saved-document content-fingerprint cache (reuse buildEfxPaintDocumentRevision from 45-01 as the fingerprint) so no-op saves skip sidecar staging and reuse the prior persisted payload. Wire the rollback path test through the mocked exchangeGeneration helper. All 4 tests RED then GREEN. Commit test(45-04) then feat(45-04): path safety, dedup, and rollback hardening.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPersistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 hardening tests pass; full efx-paint suites remain green
    - efxPaintPersistence.ts exports isSafeEfxPaintCachePath and contains the stableSegment FNV-1a implementation
    - Second-save test asserts writeFile mock call count is 0 for unchanged content
    - Rollback test asserts settle mock received 'rollback' and the error re-throws
  </acceptance_criteria>
  <done>Sidecar paths are traversal-proof, no-op saves are free, and an interrupted save provably leaves the last committed state intact.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/stores/efxPaintStore.ts`, `app/src/lib/efxPaintPersistence.ts`, `app/src/lib/efxPaintPersistence.test.ts`, `app/src/stores/efxPaintStore.test.ts`
- New symbols: `efxPaintVersion` (signal), `registerDocument`, `getDocument`, `hasDocument`, `removeDocument`, `reset` (efxPaintStore), `serializeRuntimeIntoDocument`, `hydrateRuntimeFromDocument`, `saveEfxPaintDocumentsWithProjectWrite`, `loadEfxPaintDocuments`, `isSafeEfxPaintCachePath`, `EFX_PAINT_CACHE_DIR`, `EFX_PAINT_STAGING_PREFIX`, `_setEfxPaintMarkDirtyCallback`
- Modified: `app/src/stores/physicPaintStore.ts` (new exported accessors `extractRuntimeStateForDocument` / `installRuntimeStateFromDocument`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted document JSON → parseEfxPaintDocument | Untrusted on-disk content enters the store only through the fail-closed parser |
| document payload → filesystem sidecars | Crafted cache paths could traverse outside cache/efx-paint |
| TS orchestrator → native cache service | Two-resource (project file + cache) commit must never tear |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-11 | Tampering | sidecar path construction | high | mitigate | isSafeEfxPaintCachePath prefix-lock + segment allow-list; stableSegment FNV-1a sanitization (Task 3 tests 1, 4; ASVS V12) |
| T-45-12 | Tampering | save transaction | high | mitigate | Stage → write → settle commit/rollback; writeProject failure rolls back and leaves prior committed generation intact (Task 3 test 3) |
| T-45-13 | Tampering | load path | high | mitigate | loadEfxPaintDocuments delegates to fail-closed parseEfxPaintDocument — unknown/malformed members throw before any store hydration (Task 1 test 3) |
| T-45-14 | Denial of Service | sidecar decode | low | accept | PNG data-URL decode bounded by project file size; desktop single-user, no remote input channel |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/lib/efxPaintPersistence.test.ts src/stores/efxPaintStore.test.ts src/efx-paint/document` — all new suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green (legacy tests still pass; legacy entry points not yet deleted)
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Document save/load preserves identity through the real transaction shape (DOC-05 TS side).
- Idempotency and interruption edges are contract-tested, not assumed.
- The runtime projection satisfies research A4 without pulling Phase 46 scope forward.
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-04-SUMMARY.md` when done
</output>
