---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/efx-paint/document/efxPaintDocument.ts
  - app/src/efx-paint/document/efxPaintDocumentParsers.ts
  - app/src/efx-paint/document/efxPaintDocumentRevision.ts
  - app/src/efx-paint/document/efxPaintDocument.test.ts
autonomous: true
requirements: [DOC-01, DOC-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "createEfxPaintDocument(parentLayerId) returns a deep-frozen document with version=1, documentRevision=0, compositeRevision=0, exactly one default Paint track, exactly one fixed Background track, activeTrackId equal to the default track's ID, and photoReference=null (DOC-01, DOC-02, D-08)"
    - "The Background track is always the single fixed track beneath all Paint tracks; a new document contains exactly one Paint track and one Background track, so compositor adjacency is unambiguous (edge: adjacency, resolved explicit)"
    - "A new document has an empty Background clips array and resolves every frame to the transparent fallback { mode: 'transparent' }; the empty clips collection is schema-valid (edge: empty, resolved explicit)"
    - "The tracks array order is stable and equals compositor order; with one default track, tracks[0].id === activeTrackId and reorder cannot rewrite IDs (edge: ordering, resolved explicit)"
    - "parseEfxPaintDocument round-trips a factory-produced document through JSON serialize/parse preserving version, parentLayerId, documentRevision, activeTrackId, track IDs, background ID, and fallback (DOC-01, DOC-05 foundation)"
    - "Parsers throw on unknown members, malformed members, wrong version, duplicate track IDs, and an activeTrackId that references no existing track (fail-loud per spec identity rules)"
    - "Document/track/composite revision builders are deterministic: equal content yields equal revision strings regardless of input member order; empty additive collections contribute no term"
  artifacts:
    - app/src/efx-paint/document/efxPaintDocument.ts
    - app/src/efx-paint/document/efxPaintDocumentParsers.ts
    - app/src/efx-paint/document/efxPaintDocumentRevision.ts
    - app/src/efx-paint/document/efxPaintDocument.test.ts
  key_links:
    - "EFX_PAINT_DOCUMENT_VERSION constant is the single version discriminator consumed later by the rejection gate (45-03) and parsers"
    - "createDefaultPaintTrack ID via crypto.randomUUID() is the stable track ID every later phase addresses (Pitfall: track identity)"
  prohibitions:
    - "No background fallback configuration UI of any kind in Phase 45; the fallback is schema-persisted only (D-09)"
    - "No Preact imports, signals, or side effects in the document model modules — pure, frozen, serializable only (pure/reactive split convention)"
    - "No zod/io-ts or any validation library — hand-rolled fail-closed guards only (STACK.md)"
---

<objective>
Build the pure, fail-closed v1.0 EFX Physic Paint document model: types, factory, parsers, and deterministic revision builders. This is the identity root every later plan (gate, persistence, store, bridge) and every later phase (46-52) builds on.

Purpose: DOC-01/DOC-02 require exactly one versioned document per parent layer with stable internal track IDs, a document revision, an active track ID, one default Paint track, and one fixed Background track with the transparent fallback. The spec's `EfxPaintDocument` sketch is illustrative; this plan locks the field-level schema (Claude's discretion per CONTEXT.md).

Output: `app/src/efx-paint/document/` module with 100% unit-tested factory, fail-closed parser, and revision builders.
</objective>

<assumption_delta_decision>
- Noun now primary: **EfxPaintDocument** (the multi-track document owned by one parent layer).
- Decision: **promote** — the document becomes the identity root; the legacy one-track runtime model is not demoted to a variant, it is hard-deleted (D-02). Phase 45 keeps the existing layerId-keyed in-memory runtime maps only as a projection source into the document's single default track at the persistence boundary (research A4/Q1); track-local re-addressing lands in Phase 46.
- Suggested contract test: `efxPaintDocument.test.ts` asserts a document always contains exactly one Background track whose ID is stable and whose fallback is transparent at creation — encoding the generalized "one document, many tracks, one fixed Background" intent.
</assumption_delta_decision>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@SPECS/milestone-v1.0.0-plan.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/lib/physicPaintPersistence.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Document types + factory + serialize/parse round-trip (end-to-end identity path)</name>
  <files>app/src/efx-paint/document/efxPaintDocument.ts, app/src/efx-paint/document/efxPaintDocumentParsers.ts, app/src/efx-paint/document/efxPaintDocument.test.ts</files>
  <read_first>
    - SPECS/milestone-v1.0.0-plan.md (sections "Canonical document concept" + "Identity rules" + "Clean-break v1.0 document boundary" — the schema sketch and locked identity rules)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts:843-878 (the canonical fail-closed parser idiom to mirror)
    - app/src/lib/physicPaintPersistence.ts:30-46 (guard primitives isPlainRecord/hasOnlyKeys/isNonEmptyString/isNonNegativeInteger to copy)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Code Example 1 factory skeleton; D-08)
  </read_first>
  <behavior>
    - RED Test 1 (round-trip): createEfxPaintDocument('layer-abc') → JSON.stringify → parseEfxPaintDocument → result deep-equals factory output on version, parentLayerId, documentRevision, activeTrackId, tracks, background, photoReference, compositeRevision.
    - RED Test 2 (factory shape, DOC-02): version === 1 (EFX_PAINT_DOCUMENT_VERSION); documentRevision === 0; compositeRevision === 0; tracks.length === 1; tracks[0] is a Paint track with stable UUID id, name 'Track 1', order 0, visible true, solo false, opacity 1, blendMode 'normal', revision 0; activeTrackId === tracks[0].id; photoReference === null; background has stable UUID id, clips deep-equal [], fallback deep-equals { mode: 'transparent' }, visible true, revision 0.
    - RED Test 3 (frozen): Object.isFrozen(document), Object.isFrozen(document.tracks), Object.isFrozen(document.background), Object.isFrozen(document.background.fallback) all true.
    - RED Test 4 (fresh IDs): two factory calls produce different track IDs and different background IDs.
  </behavior>
  <action>
    Create app/src/efx-paint/document/efxPaintDocument.ts exporting: EFX_PAINT_DOCUMENT_VERSION = 1; types EfxPaintDocument, InternalPaintTrack, BackgroundTrack, FrameLoopClip, BackgroundFallback ({ mode: 'transparent' } | { mode: 'solid'; color: string }); and factory createEfxPaintDocument(parentLayerId: string). Lock this field-level schema (spec sketch + identity rules; discretion per CONTEXT.md): EfxPaintDocument = { version, parentLayerId, documentRevision, activeTrackId, tracks: InternalPaintTrack[], background: BackgroundTrack, photoReference: null, compositeRevision }. InternalPaintTrack = { id, name, order, visible, solo, opacity, blendMode, revision, frames, rotoPhysical, loopClips } where frames is a frame-number-keyed record of cached-frame reference records (sidecar cachePath + width/height), rotoPhysical is the canonical roto physical document JSON shape validated by the existing physicsPaintRotoPhysicalModel parser (the runtime model file stays as the per-track model per research Q1), and loopClips is a FrameLoopClip[] per the spec sketch (id, startFrame, sourceFrameRefs, repeat finite/infinite, sourceKind, revision) — factory creates frames/rotoPhysical empty and loopClips []. IDs via crypto.randomUUID() (AddFxMenu idiom). All factory output deep-frozen. Write the four RED tests first, watch them fail, then implement until green, then commit.
    In efxPaintDocumentParsers.ts implement parseEfxPaintDocument(value: unknown): EfxPaintDocument using the copied guard primitives with allowed-key sets for every record level (document, track, background, fallback, loop clip, cached-frame ref). For this task only the happy path must pass; hostile-input tests land in Task 2.
    Commit sequence: RED commit test(45-01): add failing document factory round-trip tests → GREEN commit feat(45-01): v1.0 document types, factory, happy-path parser.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document/efxPaintDocument.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - efxPaintDocument.test.ts exits 0 with the 4 behavior tests present and passing
    - efxPaintDocument.ts contains `export const EFX_PAINT_DOCUMENT_VERSION = 1` and `export function createEfxPaintDocument(`
    - Factory output has fallback exactly { mode: 'transparent' } (D-08) — no reference to paintBgColor anywhere in the new module (D-08: no inheritance)
    - No Preact or signals imports in efxPaintDocument.ts or efxPaintDocumentParsers.ts
  </acceptance_criteria>
  <done>Factory → JSON → parse round-trip preserves full document identity; new document shape matches DOC-02 exactly; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-closed parser hostile-input suite (unknown members, duplicates, dangling activeTrackId, wrong version)</name>
  <files>app/src/efx-paint/document/efxPaintDocumentParsers.ts, app/src/efx-paint/document/efxPaintDocument.test.ts</files>
  <read_first>
    - app/src/efx-paint/document/efxPaintDocumentParsers.ts (current state after Task 1)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts:843-878 (throw-on-unknown / throw-on-invalid shape, error message style: 'TypeName: reason.')
    - app/src/lib/physicPaintPersistence.ts:129-133 (duplicate-ID fail-loud idiom)
  </read_first>
  <behavior>
    - Test 1: unknown top-level member throws with message naming EfxPaintDocument and the expected member set.
    - Test 2: unknown member inside a track, background, fallback, or loopClip record throws.
    - Test 3: version !== 1 throws (no migration, no normalization — clean-break).
    - Test 4: two tracks with the same id throw (duplicate track IDs fail loud — spec identity rule).
    - Test 5: activeTrackId not matching any track id throws (malformed track reference fails loud).
    - Test 6: fallback.mode outside 'transparent' | 'solid' throws; mode 'solid' without a string color throws.
    - Test 7: non-record input (null, array, string) throws.
    - Test 8: parsers never allocate IDs and never normalize — parsing a valid-but-different-member-order JSON yields a document deep-equal to canonical parse (determinism foundation for revisions).
  </behavior>
  <action>
    Extend parseEfxPaintDocument and per-level parsers (track, background, fallback, loopClip, cached-frame ref) to full fail-closed behavior: every record level has an explicit allowed-key Set; unknown members throw; malformed members throw; never normalize, never allocate IDs (physicsPaintRotoPhysicalModel idiom). Duplicate track IDs detected via a seen-set and throw. activeTrackId cross-checked against parsed track IDs after all tracks parse. Error messages follow the existing 'TypeName: reason.' style. Write all 8 tests RED first, then implement to green. Commit test(45-01) RED then feat(45-01) GREEN per TDD gate sequence.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document/efxPaintDocument.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 hostile-input tests pass; the round-trip tests from Task 1 still pass
    - efxPaintDocumentParsers.ts contains a seen-set duplicate check that throws on duplicate track ids
    - No catch-and-default anywhere in the parser module (grep: parser file contains no `catch` followed by a return of a default value)
  </acceptance_criteria>
  <done>Parser is fail-closed at every record level; duplicate track IDs and dangling activeTrackId fail loud per spec identity rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic document/track/composite revision builders</name>
  <files>app/src/efx-paint/document/efxPaintDocumentRevision.ts, app/src/efx-paint/document/efxPaintDocument.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts:947-1022 (buildPhysicPaintRotoPhysicalRevision + canonical encoding rules: stable-identity sort, length-prefixed strings, empty additive collections contribute no term)
    - app/src/efx-paint/document/efxPaintDocument.ts (current state after Task 1)
  </read_first>
  <behavior>
    - Test 1: buildEfxPaintDocumentRevision returns a prefixed string (e.g. 'efxdoc-&lt;hash&gt;') and is pure.
    - Test 2 (determinism): two documents with identical content but different JSON member insertion order produce identical revisions.
    - Test 3 (sensitivity): changing any track id, track revision, fallback mode, or activeTrackId changes the document revision.
    - Test 4 (empty-collection rule): an empty loopClips array and an empty frames record contribute no term — revision equals the revision of the same document computed without those members (D-29 compatibility idiom).
    - Test 5: buildEfxPaintTrackRevision and buildEfxPaintCompositeRevision are deterministic and sensitive to their own inputs.
  </behavior>
  <action>
    Create efxPaintDocumentRevision.ts exporting buildEfxPaintDocumentRevision(document), buildEfxPaintTrackRevision(track), buildEfxPaintCompositeRevision(document). Mirror the canonical encoding from buildPhysicPaintRotoPhysicalRevision: validate-then-hash; records sorted by stable identity (track id localeCompare); strings length-prefixed to prevent delimiter collisions; empty additive collections contribute NO term; prefixed revision string. Do not add a hash library — reuse the in-repo canonical-hash approach used by the roto physical model (import or mirror its encoder per existing module boundaries; if the existing encoder is not exported, extract it into a shared pure helper in the same efx-paint/document folder and re-use it from both — do not duplicate logic). Tests RED first, then GREEN. Commit per TDD gate sequence.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document/efxPaintDocument.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 revision tests pass; full efx-paint/document suite green
    - efxPaintDocumentRevision.ts exports buildEfxPaintDocumentRevision, buildEfxPaintTrackRevision, buildEfxPaintCompositeRevision
    - No new dependency in app/package.json (grep package.json: no additions)
  </acceptance_criteria>
  <done>Deterministic revision builders exist and are proven order-insensitive, content-sensitive, and empty-collection-neutral.</done>
</task>

</tasks>

## Flagged assumptions (spec-less probe fallback — unclassified edges, not silently dropped)

- DOC-01 edge probe was unclassified: no adjacency/empty/ordering/idempotency edge applies beyond the identity rules already locked as must_haves truths above (stable IDs, revisions, active track). Flagged for checker review; no additional acceptance criterion authored.

## Artifacts this phase produces (this plan)

- New files: `app/src/efx-paint/document/efxPaintDocument.ts`, `efxPaintDocumentParsers.ts`, `efxPaintDocumentRevision.ts`, `efxPaintDocument.test.ts`
- New symbols: `EFX_PAINT_DOCUMENT_VERSION`, `EfxPaintDocument`, `InternalPaintTrack`, `BackgroundTrack`, `FrameLoopClip`, `BackgroundFallback`, `createEfxPaintDocument`, `parseEfxPaintDocument`, `buildEfxPaintDocumentRevision`, `buildEfxPaintTrackRevision`, `buildEfxPaintCompositeRevision`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce JSON → document parser | Untrusted file content crosses into the domain model here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering | parseEfxPaintDocument | high | mitigate | Fail-closed parse at every record level: unknown members throw, malformed members throw, wrong version throws; never normalize, never allocate IDs (Task 2 behavior tests 1-7) |
| T-45-02 | Tampering | track identity | medium | mitigate | Duplicate track IDs and dangling activeTrackId throw (spec fail-loud identity rules; Task 2 tests 4-5) |
| T-45-03 | Tampering | revision builders | low | accept | Non-cryptographic canonical fingerprint; collision risk acceptable for change-detection lease, not a security boundary |

Package legitimacy: no packages installed by this plan (audit satisfied vacuously per 45-RESEARCH.md Package Legitimacy Audit).
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/efx-paint/document` — full new suite green
- `pnpm --filter efx-motion-editor exec vitest run` — no regressions in the existing suite
</verification>

<success_criteria>
- A v1.0 document can be created, serialized, parsed, and fingerprinted with total determinism.
- Every hostile input class fails closed with a loud throw.
- DOC-01 identity fields (version, parentLayerId, documentRevision, activeTrackId, stable track IDs) and DOC-02 initial shape (1 default Paint track + fixed Background track + transparent fallback) are contract-tested.
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-01-SUMMARY.md` when done
</output>
