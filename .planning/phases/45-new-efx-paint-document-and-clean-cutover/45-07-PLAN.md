---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 07
type: execute
wave: 5
depends_on: ["45-05", "45-06"]
files_modified:
  - app/src/efx-paint/efxPaintCleanBreakContract.test.ts
  - app/src/lib/physicPaintPersistence.ts
  - app/src/lib/physicPaintPersistence.test.ts
  - app/src/types/project.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/lib/physicPaintPlayScriptBridge.test.ts
  - app/src/types/physicPaint.test.ts
autonomous: true
requirements: [DOC-04, DOC-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A grep contract test proves no reference remains to the legacy persistence module, the legacy cache dir 'cache/physic-paint', the legacy staging prefix '.physic-paint-staging-', the legacy output type names, or the legacy serialization entry points anywhere in app/src, app/src-tauri/src, or packages/efx-physic-paint/src — outside the explicit allowlist (the gate detection module, its fixtures, and this contract test) (DOC-04)"
    - "app/src/lib/physicPaintPersistence.ts and its test no longer exist; legacy output types are removed from app/src/types/project.ts; physicPaintStore.toMceOutputs/loadFromMceOutputs no longer exist (DOC-04)"
    - "Legacy test files that pinned the old format (physicPaintStore.test.ts + rotoHoldComposite/rotoPhysicalStructuralCache/rotoLoopClips variants, physicPaintBridge.test.ts, physicPaintPlayScriptBridge.test.ts, physicPaint.test.ts) are deleted or rewritten against the v1.0 document — none pin the legacy format (DOC-04)"
    - "Full gates green after deletion: vitest run, typecheck, cargo test (Pitfall F3 mechanical safety net)"
    - "git diff for the entire phase shows zero changes to previewRenderer.ts, paintStore.ts, paintRenderer.ts, PaintOverlay.tsx, paintPersistence.ts (DOC-06, D-01)"
  artifacts:
    - app/src/efx-paint/efxPaintCleanBreakContract.test.ts
    - Deletions: app/src/lib/physicPaintPersistence.ts, app/src/lib/physicPaintPersistence.test.ts
  key_links:
    - "The contract test allowlist is exactly: efxPaintCleanBreak.ts (gate trigger strings), efxPaintCleanBreak.test.ts, __fixtures__ (legacy-shaped committed fixtures), and the contract test itself — anything else matching fails the build"
    - "Rust-side legacy structs and cache-dir creation were already removed in 45-02; this plan removes the TS-side legacy surface and proves the whole inventory unreachable"
  prohibitions:
    - "Git history is the only archive — no quarantine files, no .legacy copies, no commented-out code, no 'just in case' readers (D-02)"
    - "Never read, rewrite, or delete legacy on-disk user data (cache/physic-paint dirs, old blobs in user projects) — deletion is code-only (D-04)"
    - "Do not modify paintStore.ts, paintRenderer.ts, PaintOverlay.tsx, paintPersistence.ts, or previewRenderer.ts while deleting (D-01, DOC-06)"
---

<objective>
Hard-delete the legacy one-track Physic Paint code surface and prove — with a mechanical grep contract test plus full gates — that no legacy reader, converter, renderer, cache path, or compatibility branch remains reachable (DOC-04), and that the main-editor boundary files are byte-untouched (DOC-06, D-01).

Purpose: D-02 makes deletion, not quarantine, the audit standard: "DOC-04 audit = the code does not exist." All replacements landed in 45-01 through 45-06; this plan removes the dead surface and closes the phase's automated evidence.

Output: Contract test + deletions + green full-gate sweep.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md
@app/src/stores/physicPaintStore.ts
@app/src/types/project.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: DOC-04 grep contract test (RED) — the deletion checklist made mechanical</name>
  <files>app/src/efx-paint/efxPaintCleanBreakContract.test.ts</files>
  <read_first>
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Legacy Deletion Inventory table, verbatim)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-PATTERNS.md (Legacy Deletion Map + grep contract test rule)
    - app/vitest.config.ts (existing config — the contract test runs inside the standard suite; no new config per project constraint)
  </read_first>
  <action>
    Create app/src/efx-paint/efxPaintCleanBreakContract.test.ts: a vitest contract test that walks app/src, app/src-tauri/src, and packages/efx-physic-paint/src (node:fs readdir recursive, skipping node_modules/dist) and asserts ZERO matches — after stripping comment lines (grep -v '^#' hygiene: tokenize per line, strip // and block-comment lines before matching so header prose cannot self-invalidate) — for each forbidden token: 'physicPaintPersistence', 'cache/physic-paint', '.physic-paint-staging-', 'McePhysicPaintOutput', 'McePhysicPaintCachedFrame', 'McePhysicPaintRotoPlaybackSettings', 'toMceOutputs', 'loadFromMceOutputs', 'efx-paint-state-', 'editableState', 'rotoInterpolationSettings', 'cachedRotoFrames'. Allowlist (exact relative paths): app/src/efx-paint/document/efxPaintCleanBreak.ts, app/src/efx-paint/document/efxPaintCleanBreak.test.ts, app/src/efx-paint/document/__fixtures__/*, app/src/efx-paint/efxPaintCleanBreakContract.test.ts — the gate module must reference legacy tokens to detect them; nothing else may. Note: 'physic_paint_outputs' remains present in models/project.rs and types/project.ts as the OPAQUE presence carrier for the gate (45-02 design) — the contract asserts it appears ONLY in those two declared carrier locations plus the gate module/fixtures, never in a reader/renderer/serializer. Run the test now and watch it FAIL on the still-present legacy surface (this RED is the deletion checklist). Commit test(45-07): add DOC-04 clean-break grep contract (red).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/efx-paint/efxPaintCleanBreakContract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Contract test file exists, runs inside the standard suite, and currently FAILS listing the legacy surfaces still present
    - Allowlist is exactly the 4 declared locations; failure output names offending file:line for each match
    - Comment-stripping is implemented (no bare == 0 gates on unfiltered file content)
  </acceptance_criteria>
  <done>The DOC-04 audit is mechanical and red against the not-yet-deleted legacy surface.</done>
</task>

<task type="auto">
  <name>Task 2: Hard-delete the legacy surface and turn the contract green + full gates</name>
  <files>app/src/lib/physicPaintPersistence.ts, app/src/lib/physicPaintPersistence.test.ts, app/src/types/project.ts, app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts, app/src/lib/physicPaintBridge.test.ts, app/src/lib/physicPaintPlayScriptBridge.test.ts, app/src/types/physicPaint.test.ts</files>
  <read_first>
    - app/src/efx-paint/efxPaintCleanBreakContract.test.ts (the RED failure list IS the work queue — delete until green)
    - app/src/types/project.ts:44-127 (legacy output types to remove)
    - app/src/stores/physicPaintStore.ts:921-989 (toMceOutputs/loadFromMceOutputs and any now-dead serialization helpers)
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-RESEARCH.md (Legacy Deletion Inventory — every row must be dispositioned)
  </read_first>
  <action>
    Delete until the contract test passes: (1) delete app/src/lib/physicPaintPersistence.ts and app/src/lib/physicPaintPersistence.test.ts entirely (replaced by efxPaintPersistence.ts in 45-04); (2) remove the legacy output types from app/src/types/project.ts (McePhysicPaintOutput and related legacy-only types at 44-127), keeping physic_paint_outputs declared as the opaque unknown[]/Record presence carrier the gate needs (45-02 design); (3) remove toMceOutputs/loadFromMceOutputs and dead serialization helpers from physicPaintStore.ts (runtime maps and the 45-04 projection accessors stay); (4) delete or rewrite the legacy test files — physicPaintStore.test.ts + its rotoHoldComposite/rotoPhysicalStructuralCache/rotoLoopClips variants, physicPaintBridge.test.ts, physicPaintPlayScriptBridge.test.ts, physicPaint.test.ts: rewrite the still-relevant runtime behavior coverage against the v1.0 document/projection APIs, delete pure legacy-format pinning; (5) chase remaining contract failures (imports, type references) until green. Then run the full sweep: pnpm --filter efx-motion-editor exec vitest run, pnpm --dir app run typecheck, cargo test --manifest-path app/src-tauri/Cargo.toml, and git diff --name-only for the whole phase asserting previewRenderer.ts, paintStore.ts, paintRenderer.ts, PaintOverlay.tsx, paintPersistence.ts are absent (DOC-06/D-01). Commit chore(45-07): hard-delete legacy physic-paint persistence and format surface.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run && pnpm --dir app run typecheck && cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - efxPaintCleanBreakContract.test.ts passes (zero non-allowlisted matches)
    - app/src/lib/physicPaintPersistence.ts and physicPaintPersistence.test.ts do not exist
    - physicPaintStore.ts contains no toMceOutputs/loadFromMceOutputs
    - types/project.ts contains none of the legacy output type names
    - Full vitest suite, typecheck, and cargo test all exit 0
    - Phase-cumulative git diff contains none of the 5 protected main-editor files
  </acceptance_criteria>
  <done>DOC-04 is mechanically proven (the legacy surface does not exist, not merely unused); all gates green; protected files untouched.</done>
  <reversibility rating="costly">Implements user-locked D-01/D-02 (hard delete; git history is the archive). The user confirmation already happened at discuss-phase (the CONTEXT.md lock IS the decision checkpoint, so no checkpoint:decision is inserted); restoration is possible only by reverting commits and re-breaking the format contract.</reversibility>
</task>

</tasks>

## Flagged assumptions (spec-less probe fallback — unclassified edges, not silently dropped)

- DOC-04 edge probe was unclassified: the deletion inventory + grep contract test enumerate the complete legacy surface from RESEARCH.md (verified by direct file reads); "reachable" is operationalized as "exists in source outside the allowlist." Flagged for checker review; no additional acceptance criterion authored.

## Artifacts this phase produces (this plan)

- New file: `app/src/efx-paint/efxPaintCleanBreakContract.test.ts`
- Deleted files: `app/src/lib/physicPaintPersistence.ts`, `app/src/lib/physicPaintPersistence.test.ts`, plus legacy-format test files deleted or rewritten (`physicPaintStore.test.ts` + variants, `physicPaintBridge.test.ts`, `physicPaintPlayScriptBridge.test.ts`, `physicPaint.test.ts`)
- Deleted symbols: legacy output types (`McePhysicPaintOutput` family, TS side), `toMceOutputs`, `loadFromMceOutputs`, and any remaining legacy-only helpers in `physicPaintStore.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted code ↔ remaining callers | A missed reference re-opens a legacy path (partial cutover, Pitfall F3) |
| user legacy data on disk | Deletion sweep must never touch user files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-22 | Tampering | partial cutover | high | mitigate | Grep contract test enumerates every legacy token with a strict allowlist; typecheck + cargo test catch dangling references mechanically (F3 safety net) |
| T-45-23 | Tampering | user legacy on-disk data | high | mitigate | Deletion is code-only; no filesystem operation targets user project dirs; D-04 enforced by 45-02's non-destruction test and by the absence of any cleanup code in this plan |
| T-45-24 | Repudiation | loss of legacy behavior reference | low | accept | Git history is the archive (D-02); no quarantine copies retained |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green including the contract test
- `pnpm --dir app run typecheck`
- `cargo test --manifest-path app/src-tauri/Cargo.toml`
- `git diff --name-only <phase-base>..HEAD` — protected-file assertion (DOC-06/D-01)
</verification>

<success_criteria>
- DOC-04: the legacy one-track surface does not exist in source; the audit is a test, not a claim.
- DOC-06/D-01: the main-editor boundary and inline EFX Paint are provably untouched.
- Every automated gate is green, leaving only the D-10 native UAT (45-08).
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-07-SUMMARY.md` when done
</output>
