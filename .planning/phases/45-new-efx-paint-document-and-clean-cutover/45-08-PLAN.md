---
phase: 45-new-efx-paint-document-and-clean-cutover
plan: 08
type: execute
wave: 6
depends_on: ["45-07"]
files_modified: []
autonomous: false
requirements: [DOC-01, DOC-02, DOC-03, DOC-05, DOC-06]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "UAT part 1: new project → add EFX Physic Paint layer → Studio opens on a v1.0 document; the user paints a stroke on the default track (D-10)"
    - "UAT part 2: save/quit/reopen → the stroke and document identity (version, parentLayerId, documentRevision, activeTrackId, track IDs, background, transparent fallback) are intact, verified via the on-disk saved project file plus observable behavior — no throwaway Studio indicator UI (D-10, D-11)"
    - "UAT part 3: opening a COPY of a real v0.9-era project containing Physic Paint work shows the explicit rejection dialog; nothing opens, nothing mutates, the original file is never touched (D-10, D-12, D-05)"
    - "UAT part 4: main editor unchanged — sequence timing, outer layers, and inline EFX Paint layers behave exactly as before (D-10, DOC-06)"
  artifacts: []
  key_links:
    - "The saved .mce on disk is the D-11 evidence surface: it contains efx_paint_documents with the document structure and contains no legacy keys"
  prohibitions:
    - "The UAT copy of the real v0.9 project is never mutated — the original stays byte-untouched (D-12)"
    - "No Studio indicator UI or diagnostic surface is built for verification (D-11)"
---

<objective>
Native UAT of the complete Phase 45 surface, exactly per the locked D-10 4-part protocol. All automated gates are green from 45-07; this plan is the human evidence bar.

Purpose: DOC-01/02/03/05/06 have user-visible acceptance criteria that only a native run can prove — Studio opening on the v1.0 document, stroke persistence across save/reopen, explicit legacy rejection against real-world data, and main-editor parity.

Output: User-confirmed 4-part UAT record in the plan summary.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: D-10 four-part native UAT (document creation, save/reopen, legacy rejection, main-editor parity)</name>
  <files></files>
  <read_first>
    - .planning/phases/45-new-efx-paint-document-and-clean-cutover/45-CONTEXT.md (D-10/D-11/D-12 verbatim UAT evidence bar)
  </read_first>
  <action>
    Present the user with the 4-part native UAT protocol and wait for explicit confirmation of each part. Part 1: create a new project, add an EFX Physic Paint layer, confirm Studio opens on a v1.0 document, paint a stroke on the default track. Part 2: save, quit, reopen; confirm the stroke is present and inspect the saved .mce on disk with the user: it contains efx_paint_documents with exactly one document showing version 1, parentLayerId matching the layer, documentRevision, activeTrackId equal to the single default track's ID, one Paint track, one fixed Background track with transparent fallback (D-11 evidence). Part 3: the user supplies/points at a real v0.9-era project containing Physic Paint work; make a copy (the original is never opened or mutated per D-12), open the copy, confirm the explicit no-recourse rejection dialog appears and nothing opens or mutates. Part 4: in a v1.0 project with ordinary content, confirm main-editor sequence timing, outer layer composition, and inline EFX Paint layers behave as before. Record pass/fail plus user notes per part in the summary. Any failure halts the phase for a fix, not a workaround.
  </action>
  <what-built>Plans 45-01 through 45-07 delivered: the v1.0 EfxPaintDocument model (factory/parsers/revisions), the Rust+TS efx_paint_documents persistence key with re-pointed cache/efx-paint transaction service, the parse-time legacy rejection gate, the efxPaintStore + efxPaintPersistence runtime/projection layer, the wired open/save funnel with blocking rejection dialog and AddFxMenu document registration, the v1.0 session-file/bridge/standalone-engine format, and the DOC-04 hard deletion with grep contract proof. All automated gates (vitest, typecheck, cargo test) are green.</what-built>
  <how-to-verify>Native 4-part UAT on the user's machine (the assistant does not run the app): (1) new project + EFX Physic Paint layer + Studio opens on v1.0 document + paint a stroke on the default track; (2) save/quit/reopen → stroke present + on-disk .mce shows efx_paint_documents with version/parentLayerId/documentRevision/activeTrackId/one Paint track/Background track with transparent fallback; (3) open a COPY of a real v0.9-era Physic Paint project → explicit no-recourse rejection dialog, nothing opens or mutates, original untouched; (4) main-editor sequence timing, outer layers, and inline EFX Paint layers behave as before.</how-to-verify>
  <resume-signal>User confirms each of the 4 UAT parts passed (or reports the failing part with evidence); record the outcome in 45-08-SUMMARY.md.</resume-signal>
  <verify>
    <human-check>User confirms each of the 4 parts explicitly; .mce on-disk inspection matches the D-11 structure list; rejection copy matches D-07 tone.</human-check>
  </verify>
  <acceptance_criteria>
    - All 4 UAT parts confirmed passed by the user
    - Saved .mce inspected: efx_paint_documents present with the full D-11 field list; no legacy keys written
    - Rejection dialog observed on the real v0.9 project copy; original file untouched
  </acceptance_criteria>
  <done>User has natively verified document creation, save/reopen identity, explicit legacy rejection on real data, and main-editor parity.</done>
</task>

</tasks>

## Flagged assumptions (spec-less probe fallback — unclassified edges, not silently dropped)

- DOC-06 edge probe was unclassified and is closed here natively: UAT part 4 is the human evidence that main-editor timing and outer composition are unchanged, complementing 45-07's diff gate.

## Artifacts this phase produces (this plan)

- None (verification-only plan). New symbols produced by the phase are declared in plans 45-01 through 45-07.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user UAT actions ↔ project files | UAT must not mutate protected data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-25 | Tampering | real v0.9 project used as UAT evidence | high | mitigate | D-12: only a copy is opened; the original is never touched by the app or the tester |
</threat_model>

<verification>
- All 4 UAT parts confirmed by the user (native, on their machine; the assistant does not run the app per CLAUDE.md)
</verification>

<success_criteria>
- D-10 fully satisfied with D-11/D-12 evidence standards; phase ready for verification and seal.
</success_criteria>

<output>
Create `.planning/phases/45-new-efx-paint-document-and-clean-cutover/45-08-SUMMARY.md` when done
</output>
