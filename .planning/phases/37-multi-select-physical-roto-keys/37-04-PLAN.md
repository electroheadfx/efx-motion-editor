---
phase: 37-multi-select-physical-roto-keys
plan: 04
type: execute
wave: 3
depends_on: [37-03]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 37-UI-INTEGRATION
  - 37-GROUP-DRAG
  - 37-SELECT-ALL
  - 37-MULTI-SELECT-IDENTITY
tags:
  - physics-paint
  - roto
  - selection
  - group-operations
  - timeline-ui
  - preact

must_haves:
  truths:
    - "Cmd/Ctrl-click on a real-key cell toggles that keyId in/out of the selection through the 37-02 controller reducer and NEVER calls onNavigateToSyncedFrame and NEVER arms a drag session; plain click on a real key collapses the multi-selection to that key via the collapse callback and keeps today's navigation behavior; generated/empty cells ignore every selection gesture entirely (D-01, D-02, D-05, Pitfall 6; UI-INTEGRATION adjacency/ordering probe rows: covered — selection membership and order come from the controller prop, the strip never reorders)"
    - "Shift-click on a real-key cell selects the contiguous physical range from the anchor keyId to the clicked keyId (real keys only, generated/empty skipped by construction) through the 37-02 range reducer, without navigation steal or drag arming (D-01)"
    - "Secondary selected real-key cells render the new `.selected` class (2px #F2F5F7-family outline at 1px offset with the 36.15-09 z-index lift technique), expose `aria-selected=\"true\"` on the cell button, and show the styled per-cell tooltip copy `Selected key` (composed with the semantic base when it differs, e.g. `Selected key — cached`); the current editing key keeps the `.current` orange ring as the strongest highlight and keeps its `Real key` tooltip (D-04; zero-one-many probe row: covered — selection never empty, one key = no `.selected` cell, many = secondary treatment)"
    - "Grabbing any selected real key with a selection of size >= 2 starts a GROUP drag session that calls the 37-03 prepareRotoKeyGroupDrag / commitRotoKeyGroupDrag pair through the reused gesture session (threshold, pointer capture, edge-scroll, Escape cancel, validity key verbatim); grabbing an unselected real key first collapses the selection to that key and starts the unchanged single-key path (D-06; 37-GROUP-DRAG)"
    - "Valid group drags render the complete-mapping preview with role 'moved' on EVERY selected key at its proposed appFrame (generalized from drag.movedKeyIds), 'shifted' on unselected rippled keys, vacated/generated/target treatments unchanged, and no new preview color (D-22/D-23, Pitfall 2)"
    - "Invalid group-drop previews render `roto-drag-target-blocked` (destructive-family dotted outline + existing invalid fade) on exactly the resolver-supplied conflictingAppFrames cells and `cursor: not-allowed` on the grabbed key while the proposal is invalid; out-of-range/over-capacity reuses the existing roto-drag-target-invalid fade (D-08, Pitfall 1)"
    - "Release-time publication contract (owned by this plan per 37-03): a group session released on an invalid target publishes the preparation's concise reason to the status capsule and its full detail to the diagnostic channel EXACTLY ONCE via the new onRotoGroupDragRejected Studio-wired callback; gesture-hover re-runs of prepare never publish anything; release with a matching target signature commits the retained publication unchanged (D-07/D-09)"
    - "After an accepted group commit, focus follows the grabbed key at its accepted appFrame through the existing cssEscape-escaped selector path (D-24); the moved group stays selected with the grabbed key current per the 37-02 D-17 rule — the strip adds no selection-aftermath logic of its own"
    - "Select All renders as a guarded focusable icon at the END of the key-utilities pill immediately after the Delete action and before the Key spacing group (Pitfall 7 placement), lucide ListChecks glyph, visible label `All`, tooltip copy `Select all keys`, consuming the 37-03 canSelectAllKeys / selectAllKeysDisabledReason computeds and calling the Studio selectAllRotoKeys callback; zero real keys renders guarded-unavailable with the verbatim controller reason (D-03; zero-one-many Select All probe row: covered)"
    - { statement: "Native visual UAT confirms blocked destination cells render the destructive-family dotted outline plus fade and the grabbed key shows the cannot-drop cursor before release, clearly distinct from all valid green target treatments.", verification: backstop }
    - { statement: "Native visual UAT confirms secondary selected keys are instantly distinguishable from the current key's orange ring and from drag-preview treatments at 18px cell size, across empty/cached/real semantic fills.", verification: backstop }
    - { statement: "Native visual UAT confirms the icon-only action row stays fit-content and non-wrapping in its 28px band with the Select All icon added, and the strip geometry (155px bands, 18px cells) is unchanged.", verification: backstop }
  assumptions:
    - "UNRESOLVED PROBE ROW (37-MULTI-SELECT-IDENTITY, carried from 37-02): shift-click making the clicked key the current editing key, and toggle-out-of-the-current-key transfer semantics, are planner-defined interpretations of D-01/D-02/D-04 implemented in the 37-02 reducers; this plan wires the gestures to those reducers exactly as defined. Flagged for explicit confirmation at native UAT (plan 37-05). Do not treat as silently covered."
  prohibitions:
    - "The native disabled attribute is never used on the Select All icon or any guarded control — guarded presentation is aria-disabled=\"true\" + focusable + sr-only verbatim reason + styled tooltip, with activation blocked before the controller call (36.15 D-28)"
    - "The view never re-derives mapping legality or collision detection: blocked-target preview renders only resolver-supplied conflictingAppFrames from the preparation result; no string-parsing of failure text, no view-side mapping recomputation (Pitfall 1)"
    - "Modifier-click selection gestures never steal navigation (no currentFrame change) and never arm a drag session; drag arming stays plain-button-0-without-modifiers only (Pitfall 6)"
    - "No second window-level capture-phase Escape listener is added anywhere; the strip's existing capture-phase drag-cancel listener stays authoritative during gestures, and selection-collapse Escape stays in the 37-02 keyboard dispatcher (Pitfall 4, D-02)"
    - "Selection state never lives in the view and is never derived from frames or DOM order: the strip receives the keyId-only set as a prop and emits keyId intents only (D-05)"
    - "No selection-count badges on cells, no marquee/rubber-band visuals, no checkbox chrome, no second highlight color for range selection, no permanent selection legend; the Key {n} chip never switches to a count display (37-UI-SPEC Visual State Contract, D-16)"
    - "Copy, Duplicate, Insert, Paste, + Key, and the Key spacing input/Apply controls keep their exact current single-key visuals and behavior (D-16); no group Duplicate or group Copy/Paste is introduced and no Shift+Arrow keyboard range extension is added (CONTEXT.md Deferred Ideas)"
    - "No compatibility fields, migration code, forwarding wrappers, aliases, dual-write paths, legacy dual-frame identifiers, or additional timing/transaction authority appear in any touched file (D-19)"
    - "No test file is created, modified, deleted, renamed, or executed in this plan (D-18); verification is bounded static checks plus full typecheck only; no dev server, browser, or native process is launched"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
    - app/src/components/physic-paint/physicsPaintStudio.css
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  key_links:
    - "Studio selectedKeyIds signal (37-02) -> new rotoSelectedKeyIds strip prop -> cellClass `.selected` + aria-selected + `Selected key` tooltip composer"
    - "handleRotoCellClick modifier branches (Pitfall 6) -> Studio onToggleRotoKeySelection / onExtendRotoKeySelection / onCollapseRotoSelectionToKey callbacks -> 37-02 pure reducers -> signals -> props round-trip"
    - "Group gesture session -> 37-03 prepareRotoKeyGroupDrag -> frozen publication -> generalized getRotoDragPreviewViewModel moved-set roles; preparation failure -> conflictingAppFrames -> roto-drag-target-blocked class + cannot-drop cursor; release-reject -> onRotoGroupDragRejected -> Studio setApplyMessage (capsule) + console diagnostic (D-26 detail leg)"
    - "37-03 canSelectAllKeys / selectAllKeysDisabledReason computeds -> guarded Select All icon -> props.onSelectAllRotoKeys -> Studio selectAllRotoKeys callback -> 'All keys selected' status entry"
---

<objective>
Integrate multi-selection into the shipped 36.15 icon-only workflow strip: modifier-click / Shift-click / plain-click gesture handling with no navigation steal and no drag arming on modifiers, a group drag session reusing the UAT-hardened gesture mechanics with the complete-mapping preview generalized to the moved set and a resolver-driven blocked-target preview (`conflictingAppFrames` -> blocked cell class + cannot-drop cursor), the Select All guarded icon at the end of the key-utilities pill after Delete (Pitfall 7 placement), the `.selected` secondary cell treatment with `Selected key` tooltip copy and `aria-selected`, the group-aware preview view model in `physicsPaintWorkflowPresentation.ts`, the new CSS classes in `physicsPaintStudio.css`, `cssEscape` on every new keyId selector, and the release-time drag publication contract this plan owns per 37-03.

Purpose: 37-01 made the resolver understand group intents, 37-02 gave the controller the selection state, 37-03 wired the action bundle — this plan is the visible surface where the user actually performs every Phase 37 gesture. Presentation renders resolver/controller output only and emits keyId intents only (37-UI-SPEC Preact and State Ownership Contract).

Output: extended strip (gestures, group session, Select All icon, cell states), generalized presentation view model + tooltip composer, new CSS state classes, and Studio prop/callback wiring.

**D-18 gate:** this plan is PRE-UAT production. No test files, no vitest runs, no dev server. Verification is bounded static checks plus full typecheck.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md
@.planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md
@.planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md
@.planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md
@.planning/phases/37-multi-select-physical-roto-keys/37-02-PLAN.md
@.planning/phases/37-multi-select-physical-roto-keys/37-03-PLAN.md

@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
@app/src/components/physic-paint/physicsPaintStudio.css
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<precondition>
Plans 37-01 through 37-03 are complete: `grep -c "prepareRotoKeyGroupDrag" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` >= 3, `grep -c "canSelectAllKeys" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` >= 3, `grep -c "conflictingAppFrames" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` >= 1, `grep -c "movedKeyIds" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` >= 1, `grep -c "selectedKeyIds" app/src/components/physic-paint/PhysicsPaintStudio.tsx` >= 2, and `grep -cE "export (function|const) (toggle|extend|collapse)" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` >= 3. Halt if unmet — this plan consumes those exact symbols.
</precondition>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): End-to-end modifier-click selection — Cmd/Ctrl-click toggle from cell gesture through the controller reducer to the rendered `.selected` secondary treatment (D-01, D-02, D-04, D-05)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — the file being modified; anchor sections: PhysicsPaintWorkflowStripProps (95-124), RotoDragPreviewState + RotoDragGestureSession (163-192), cssEscape helper (225-227), RotoTimelineCellButton + its props (240-291), handleRotoCellClick (440-450), handleRotoCellPointerDown guard (608-610), cell-class assembly template string (989), RotoTimelineCellButton render site (990-1002).
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — composition anchors: selectedKeyIds / selectionAnchorKeyId signals beside line 59 (37-02), the 37-02 toggle/collapse reducer imports from physicsPaintRotoMultiSelection.ts, the workflow-strip JSX props block where rotoPhysicalActions is passed (near line 1034).
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — tooltip vocabulary RotoCellSemanticTooltipKind / ROTO_CELL_STATE_TOOLTIP_COPY / getRotoCellStateTooltipCopy (229-241).
    - app/src/components/physic-paint/physicsPaintStudio.css — `.physics-paint-roto-cell.current` z-index/outline technique (2216-2226) that `.selected` mirrors.
    - .planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md — Multi-Selection Gestures, Visual State Contract (secondary selected row), Accessibility and Input Contract (aria-selected; no added tab stops), Copywriting Contract (`Selected key` tooltip copy).
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md — Pitfall 6 (modifier-click vs drag arming and click suppression).
  </read_first>
  <action>
    1. Strip props (PhysicsPaintWorkflowStripProps, beside rotoPhysicalActions at 103): add `rotoSelectedKeyIds?: readonly string[]` (controller-owned selection set, 37-02), `onToggleRotoKeySelection?: (keyId: string) => void`, and `onCollapseRotoSelectionToKey?: (keyId: string) => void` — all optional so existing hosts compile unchanged. Derive `const rotoSelectedKeyIdSet = useMemo(() => new Set(props.rotoSelectedKeyIds ?? []), [props.rotoSelectedKeyIds])` beside keyIdByAppFrame (386-390). The strip NEVER mutates or reorders this set (D-05).

    2. RotoTimelineCellButton: change `onCellClick: () => void` to `onCellClick: (event: MouseEvent) => void` and forward the click event (Pitfall 6: modifier branching needs the mouse event); add `ariaSelected?: boolean` and render `aria-selected={props.ariaSelected === true ? 'true' : undefined}` on the cell button. Roving focus is unchanged — selection adds no tab stops (UI-SPEC Accessibility).

    3. handleRotoCellClick (440-450): accept the event and branch on modifiers BEFORE the navigate fallback, after the suppressNextRotoClickRef check:
       - Real-key cells only (`cellKeyId` resolvable — pass it in, or derive via keyIdByAppFrame.get(frame); generated/empty cells and `isEditableTarget === false` cells skip every selection branch and keep today's exact navigate behavior, D-05).
       - `event.metaKey || event.ctrlKey` (no shift): call `props.onToggleRotoKeySelection?.(keyId)` and RETURN — never call onNavigateToSyncedFrame on this path (no navigation steal, Pitfall 6).
       - Plain click (no meta/ctrl/shift) on a real key: when `rotoSelectedKeyIdSet.size >= 2` call `props.onCollapseRotoSelectionToKey?.(keyId)` first (D-02 collapse), then fall through to the existing navigate call — navigation behavior preserved.
       The Shift-click branch is Task 3; keep this task's handler compiling without it.

    4. No drag arming on modifiers: in handleRotoCellPointerDown (608-610) extend the early-return guard with `|| event.metaKey || event.ctrlKey || event.shiftKey` so a modifier pointer-down never creates a gesture session (Pitfall 6). A modifier-click that never moves therefore produces exactly one selection toggle via the click branch — no drag, no suppression flag left set.

    5. Cell rendering (989-1002): compute `const isSecondarySelected = cellKeyId !== null && rotoSelectedKeyIdSet.has(cellKeyId) && rotoSelectedKeyIdSet.size >= 2 && !vm.overlays.includes('current')` — the current editing key keeps only its `.current` treatment (D-04 strongest-highlight rule). Append `${isSecondarySelected ? 'selected' : ''}` to the cellClass template string; pass `ariaSelected={isSecondarySelected}`; when isSecondarySelected, replace the tooltip copy with the new composer from step 6 and extend the aria label with ` Selected.` appended to dragLabel. Nothing else in the class string changes.

    6. Presentation (physicsPaintWorkflowPresentation.ts, beside getRotoCellStateTooltipCopy at 239): export `getRotoCellSelectedTooltipCopy(base: RotoCellSemanticTooltipKind): string` returning exactly `Selected key` when base is `'real-key'`, otherwise `Selected key — ${ROTO_CELL_STATE_TOOLTIP_COPY[base].toLowerCase()}` (UI-SPEC copy contract: current key keeps `Real key`; secondary selected cells append their semantic base when it differs, e.g. `Selected key — cached`). Pure function, no other change to the vocabulary.

    7. CSS (physicsPaintStudio.css, immediately after the `.physics-paint-roto-cell.current` block at 2216-2226): add `.physics-paint-roto-cell.selected` with `z-index: 1` (same abutting-neighbor outline fix; geometry unchanged), `outline: 2px solid #F2F5F7`, `outline-offset: 1px` — the cool neutral family per 37-UI-SPEC, visually subordinate to the orange current ring; no fill change, no box-shadow. Add a one-line comment citing D-04 and the three-way distinction rule (current orange / secondary white / drag green-white).

    8. Studio wiring (workflow-strip props block near line 1034): add `rotoSelectedKeyIds={selectedKeyIds.value}`, `onToggleRotoKeySelection={(keyId) => { /* 37-02 toggle reducer over rotoKeyRecords-ordered ids; sync selectedKeyId from the reducer's resolved current; assign both selection signals */ }}`, and `onCollapseRotoSelectionToKey={(keyId) => { /* 37-02 collapse reducer: selectedKeyIds = [keyId], anchor = keyId */ }}`. Both callbacks use the 37-02 pure reducers and `rotoKeyRecords` (line 81) as the ordered identity list; neither publishes any status entry (selection-only changes produce no capsule entry, UI-SPEC). No useEffect copies — Signals assigned directly inside the event callbacks (Preact contract).

    Trace-check by hand: with selection {B} and current B, Cmd/Ctrl-click C's cell -> toggle reducer -> {B,C} with C added -> C's cell renders `.selected` + `Selected key` tooltip + aria-selected while B keeps `.current`; a second Cmd/Ctrl-click on C -> toggle removes C -> only B selected, `.selected` disappears. Record the derivation in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "rotoSelectedKeyIds" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "onToggleRotoKeySelection" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "aria-selected" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "getRotoCellSelectedTooltipCopy" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts && grep -c "physics-paint-roto-cell.selected" app/src/components/physic-paint/physicsPaintStudio.css && grep -c "rotoSelectedKeyIds={selectedKeyIds.value}" app/src/components/physic-paint/PhysicsPaintStudio.tsx && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The strip exposes the three new optional props; RotoTimelineCellButton forwards the click event and renders `aria-selected` only when its new prop is true.
    - `awk '/function handleRotoCellClick/,/^  }/' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -c "onNavigateToSyncedFrame"` equals 2 (the two pre-existing navigate calls; every modifier branch returns before them).
    - The handleRotoCellPointerDown early-return guard line contains metaKey, ctrlKey, and shiftKey checks (positive grep each returns >= 1 in the file).
    - `.physics-paint-roto-cell.selected` exists with the 2px `#F2F5F7` outline at 1px offset and z-index 1; `getRotoCellSelectedTooltipCopy` returns the exact strings `Selected key` and the `Selected key — ` prefixed composition.
    - `grep -c "disabled=" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` equals its pre-task baseline (no native disabled introduced anywhere in this plan).
    - Studio wires all three props from the 37-02 signals/reducers; `pnpm --dir app typecheck` exits 0.
    - The toggle trace derivation (B + Cmd/Ctrl-click C) is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>One Cmd/Ctrl-click flows end-to-end: cell gesture -> Studio callback -> 37-02 reducer -> selection signals -> prop round-trip -> rendered `.selected` cell with `Selected key` tooltip and aria-selected, with zero navigation steal and zero drag arming on modifiers; typecheck green.</done>
  <reversibility rating="reversible">Additive optional props, one new exported composer, one new scoped CSS class, and three Studio callbacks; removing them restores prior behavior with no data or contract impact.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Group drag session — 37-03 prepare/commit pair, moved-set preview view model, blocked-target preview, release-time reject publication (D-06..D-09, D-22)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — anchor sections: RotoDragPreviewState + RotoDragGestureSession (163-192), updateRotoDragCandidate preparation call (547-573), handleRotoCellPointerDown session construction (608-631), pointer-up release / signature-equality / commit / focus-follow (694-761), capture-phase Escape listener (776-791 — do NOT add any listener), cell-class assembly (989).
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — getRotoDragPreviewViewModel (405-483): single-movedKeyId role assignment (413, 438-447), return shape (472).
    - app/src/components/physic-paint/physicsPaintStudio.css — `.physics-paint-roto-cell.roto-drag-source` cursor (2233-2234), `.physics-paint-roto-cell.roto-drag-target-invalid` fade (2248+).
    - .planning/phases/37-multi-select-physical-roto-keys/37-03-PLAN.md — artifacts_produced: prepareRotoKeyGroupDrag/commitRotoKeyGroupDrag signatures, RotoDragPublication.movedKeyIds, RotoDragPreparationResult failure-branch conflictingAppFrames + detail, and the documented contract that THIS plan owns release-time publication (prepare publishes nothing during the gesture).
    - .planning/phases/37-multi-select-physical-roto-keys/37-01-PLAN.md — PhysicPaintRotoPhysicalDragPresentation.movedKeyIds/grabbedKeyId (single-key sites populate both; movedKeyId == grabbed).
    - .planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md — Group Drag & Drop interaction contract and the blocked-target color contract.
  </read_first>
  <action>
    1. Session/preview state: add `groupDrag: boolean` to RotoDragGestureSession and `groupDrag: boolean` + `conflictingAppFrames: readonly number[] | null` to RotoDragPreviewState. In handleRotoCellPointerDown, compute `const selectedSet = new Set(props.rotoSelectedKeyIds ?? [])` and `const groupDrag = selectedSet.size >= 2 && selectedSet.has(movedKeyId)`; when `!groupDrag && selectedSet.size >= 2 && !selectedSet.has(movedKeyId)`, call `props.onCollapseRotoSelectionToKey?.(movedKeyId)` BEFORE constructing the session (UI-SPEC: grabbing an unselected real key first collapses selection to it and starts a single-key drag). Store groupDrag on the session. All threshold/capture/edge-scroll/Escape/validity-key mechanics stay byte-verbatim (reused per Don't Hand-Roll).

    2. updateRotoDragCandidate: branch the preparation call — `session.groupDrag ? physicalActions.prepareRotoKeyGroupDrag(session.movedKeyId, candidate.target) : physicalActions.prepareRotoKeyDrag(session.movedKeyId, candidate.target)`. On a failed GROUP preparation, also retain `session.candidateConflicts = preparation.conflictingAppFrames ?? null` and `session.candidateDetail = preparation.detail ?? null` (add both nullable fields to the session interface); clear them on every new candidate computation and on single-key paths. Publish nothing here — hover re-runs must not churn the capsule (37-03 contract). setRotoDragPreview carries groupDrag and conflictingAppFrames.

    3. Pointer-up release (694-761): the signature-equality gate and commit path are unchanged in shape; the commit call branches to `physicalActions!.commitRotoKeyGroupDrag(retainedPublication!)` when session.groupDrag (the 37-03 coherence checks own publication integrity; the strip still passes the retained publication unchanged, D-09). Post-accept focus-follow (742-754) needs no change — session.movedKeyId IS the grabbed key and the selector already uses cssEscape. For the non-matching-release path (710-716): when `session.groupDrag && session.candidateDetail !== null` (a resolver-level reject, not an outside/locked classification), call the new prop `props.onRotoGroupDragRejected?.(session.candidateError ?? 'Move rejected — key in the way', session.candidateDetail)` exactly once before restoreSourceFocus — classification-level invalids (outside/locked) and single-key releases stay silent exactly as today. Add `onRotoGroupDragRejected?: (reason: string, detail: string) => void` to PhysicsPaintWorkflowStripProps.

    4. Presentation (getRotoDragPreviewViewModel): generalize the moved-role assignment — build `const movedSet = new Set(drag.movedKeyIds ?? [drag.movedKeyId])` (the 37-01 fallback keeps single-key proposals safe) and assign role 'moved' when `cell.keyId !== null && movedSet.has(cell.keyId)`; shifted/target/generated/vacated logic unchanged. Keep returning movedKeyId = drag.movedKeyId (grabbed) for the strip's source-cell rendering. Pure re-projection of proposal cells/changes only — no legality derivation (Pitfall 2).

    5. Blocked-target rendering: in the cell-class assembly (989) append `${rotoDragPreview?.groupDrag && rotoDragPreview.conflictingAppFrames?.includes(frame) ? 'roto-drag-target-blocked' : ''}` and `${rotoDragPreview?.groupDrag && !rotoDragPreview.candidateValid && isDragSource ? 'roto-drag-cannot-drop' : ''}` — blocked cells come ONLY from the resolver-supplied conflictingAppFrames (Pitfall 1: never re-derived in the view).

    6. CSS: add `.physics-paint-roto-cell.roto-drag-target-blocked` with `opacity: 0.42; filter: saturate(0.35); outline: 1px dotted rgba(255, 176, 184, 0.85); outline-offset: 1px;` (existing invalid fade + destructive-family dotted outline per 37-UI-SPEC, distinct from every valid green D-23 treatment) and `.physics-paint-roto-cell.roto-drag-source.roto-drag-cannot-drop { cursor: not-allowed; }` (overrides the grabbing cursor on the grabbed key while the group mapping is invalid; valid group drags keep grabbing). One comment line each citing D-08.

    7. Studio wiring: add `onRotoGroupDragRejected={(reason, detail) => { setApplyMessage(reason); console.error('[PhysicsPaintStudio] physical edit:', detail); }}` to the strip props — the capsule keeps single-owner arbitration (36.15 D-15) and the detail leg mirrors the line-426 diagnostic style (37-03 retired-LOG-tab assumption).

    Trace-check by hand against GD-2 wiring: selection {B,C}, grab B, hover frame 6 -> prepareRotoKeyGroupDrag fails with the conflict list -> conflicting cells render `roto-drag-target-blocked`, grabbed key shows cannot-drop -> release -> onRotoGroupDragRejected fires once with `Move rejected — key in the way` + full resolver text -> zero mutation, zero history entry. And GD-1 wiring: drop on empty frame 7 -> signature equality -> commitRotoKeyGroupDrag -> accept -> focus lands on B at its accepted appFrame; the group stays selected via the 37-02 D-17 rule (strip adds nothing). Record both derivations in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "prepareRotoKeyGroupDrag" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "commitRotoKeyGroupDrag" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "onRotoGroupDragRejected" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "roto-drag-target-blocked" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "roto-drag-target-blocked" app/src/components/physic-paint/physicsPaintStudio.css && grep -c "roto-drag-cannot-drop" app/src/components/physic-paint/physicsPaintStudio.css && grep -c "movedKeyIds" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts && grep -c "onRotoGroupDragRejected" app/src/components/physic-paint/PhysicsPaintStudio.tsx && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addEventListener('keydown'" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` equals its pre-plan baseline (exactly the one existing capture-phase Escape listener; Pitfall 4).
    - `grep -c "prepareRotoKeyDrag(session.movedKeyId" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` still returns 1 (single-key path untouched beside the group branch); the commit call sites for single vs group each appear exactly once.
    - The release path calls onRotoGroupDragRejected only inside the groupDrag + candidateDetail guard; hover-time updateRotoDragCandidate contains no status-prop call (grep the function region for `onRotoGroupDragRejected` returns 0).
    - getRotoDragPreviewViewModel assigns 'moved' via a Set built from drag.movedKeyIds with the drag.movedKeyId fallback; every `[data-roto-key-id="..."]` selector in the file still routes through cssEscape (`grep -c 'data-roto-key-id="\${cssEscape' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` equals its pre-task count — no new unescaped selector added).
    - CSS contains both new classes with the exact outline/cursor declarations; `pnpm --dir app typecheck` exits 0.
    - GD-1 and GD-2 wiring derivations recorded in the SUMMARY match the locked mappings and the UI-SPEC reject copy.
  </acceptance_criteria>
  <done>A group drag flows end-to-end through the reused gesture session into the 37-03 prepare/commit pair; valid previews show the full moved set via the generalized view model; invalid previews show resolver-supplied blocked cells plus the cannot-drop cursor; release-time reject publishes concise copy + detail exactly once; single-key drag behavior is provably unchanged; typecheck green.</done>
  <reversibility rating="costly">Group drag preview/reject semantics become user-visible behavior locked by native-UAT mappings GD-1..GD-3 and the blocked-preview backstop in 37-05; changing them afterward means re-locking mappings and re-running native UAT.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Select All guarded icon (Pitfall 7 placement) + Shift-click range branch + plan-level D-18/D-19 gates (D-03)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — the guarded Delete icon action block to copy verbatim (1152-1179), the key-utilities pill close tag (1180) and the Key spacing form that follows (1181-1182 — Select All goes BEFORE it, inside the pill, immediately after Delete), buildGuardedActionTooltipCopy (158-160), the existing lucide-preact import line, useStyledTooltip instances (380-384), the availability derivation block (350-375), handleRotoCellClick as modified by Task 1.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — selectAllRotoKeys callback (37-02, publishes 'All keys selected' via 37-03 wiring), extendRotoKeySelectionRange reducer import (37-02), strip props block.
    - .planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md — Select All interaction contract (guarded presentation, idempotent, verbatim controller reason), tooltip copy `Select all keys`, visible-label grammar; Pitfall 7 placement note.
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md — guarded-icon code example; lucide-preact 0.577.0 ListChecks / SquareCheckBig presence [VERIFIED: node_modules].
  </read_first>
  <action>
    1. Select All icon: copy the Delete guarded-action block structure verbatim (1152-1179) and insert the new action immediately AFTER it, still inside the `physics-paint-roto-key-utilities` pill (before its closing tag at 1180) — Pitfall 7: "after Delete" is the anchor; the shipped row has no Copy Script (36.15 Plan 08 moved it to the Scripts toolbar). Differences from the Delete block: NOT the destructive class (neutral `physics-paint-roto-key-icon-button`); `aria-label="Select all keys"`; `aria-describedby` id `roto-key-action-reason-select-all`; a new `selectAllTooltip = useStyledTooltip()` instance beside deleteKeyTooltip; glyph `ListChecks` (add it to the existing lucide-preact import; SquareCheckBig is the acceptable fallback if ListChecks fails to resolve) at size 18; visible label `All`; tooltip copy `buildGuardedActionTooltipCopy('Select all keys', selectAllDisabledReason)`.
    2. Availability derivation (beside the other guarded derivations at 350-375): `const canSelectAllRotoKeys = (physicalActions?.canSelectAllKeys.value ?? false) && props.ready !== false && !keyUtilitiesDisabledByBusyState;` and `const selectAllDisabledReason = canSelectAllRotoKeys ? null : (keyUtilitiesDisabledByBusyState && physicalActions?.canSelectAllKeys.value ? getRotoKeyUtilityDisabledMessage('delete') : (physicalActions?.selectAllKeysDisabledReason.value ?? 'Select all keys is unavailable.'));` — the 37-03 computeds supply the three verbatim controller reasons; busy-state wording reuses the existing utility message. Guarded activation blocks BEFORE the controller call: `if (!canSelectAllRotoKeys) return;` then `props.onSelectAllRotoKeys?.()`. Enter/Space keydown prevention mirrors the Delete block. Never the native disabled attribute (36.15 D-28).
    3. Strip prop + Studio wiring: add `onSelectAllRotoKeys?: () => void` to PhysicsPaintWorkflowStripProps and wire `onSelectAllRotoKeys={selectAllRotoKeys}` in the Studio props block — the 37-02 callback already publishes `All keys selected` on the successful path (37-03 Task 3), so the icon and the Cmd/Ctrl+A route share exactly one feedback line per invocation.
    4. Shift-click range branch: in handleRotoCellClick, add BEFORE the plain-click branch (after the Cmd/Ctrl branch): `event.shiftKey && !event.metaKey && !event.ctrlKey` on a real-key cell calls `props.onExtendRotoKeySelection?.(keyId)` and RETURNS (no navigation steal). Add `onExtendRotoKeySelection?: (keyId: string) => void` to the props. Studio wires it to the 37-02 range reducer: `extendRotoKeySelectionRange` over the rotoKeyRecords-ordered identity list with the current signal state and anchor, assigning both selection signals and syncing selectedKeyId to the reducer-returned target (the flagged UNRESOLVED probe assumption: shift-click makes the clicked key current — record it for 37-05 UAT). Anchor fallback: when selectionAnchorKeyId is null the reducer treats the current editing key as anchor (37-02 contract).
    5. Plan-level gates (run after all edits are final):
       a. D-18: `git status --porcelain` shows no `.test.` path.
       b. D-19 added-line scan over all four touched files: `git diff -U0 -- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts app/src/components/physic-paint/physicsPaintStudio.css app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -E '^\+' | grep -cE 'sourceFrame|displayFrame|inBetweenCount'` equals 0.
       c. Transaction-authority scan: added lines in the strip contain no direct bridge import — `git diff -U0 -- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -E '^\+' | grep -c 'physicPaintBridge'` equals 0.
       d. Deferred-idea scan: the strip must contain no keyboard range-extension handler (CONTEXT.md Deferred Ideas — Shift+Arrow is out of scope); the exact negative-grep command is recorded in the acceptance criteria below and must return 0.
       e. Full typecheck green with all edits in final state.
    6. Record in the SUMMARY: the five gate outputs verbatim, the Select All placement evidence (pill ordering: ... Paste, Delete, All | Key spacing), and confirmation that the Key spacing form and every other action-row control are byte-untouched (`git diff` shows no change inside the force-spacing form region).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "Select all keys" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "ListChecks" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "roto-key-action-reason-select-all" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "canSelectAllKeys" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "onSelectAllRotoKeys" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "onExtendRotoKeySelection" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx && grep -c "onSelectAllRotoKeys={selectAllRotoKeys}" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -c "onExtendRotoKeySelection" app/src/components/physic-paint/PhysicsPaintStudio.tsx && ! git status --porcelain | grep -q '\.test\.' && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The Select All action sits immediately after the Delete action inside the key-utilities pill and before the Key spacing form; `grep -c "disabled=" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` still equals its pre-plan baseline (aria-disabled only).
    - The icon consumes canSelectAllKeys / selectAllKeysDisabledReason from the 37-03 bundle, blocks guarded activation before the controller call, and renders the sr-only verbatim reason plus buildGuardedActionTooltipCopy('Select all keys', ...) tooltip; visible label is exactly `All`.
    - The Shift-click branch returns before any navigate call (the function-region navigate count stays 2 per the Task-1 assertion) and the Studio wiring routes through the 37-02 range reducer with anchor fallback.
    - D-18 gate: no `.test.` path in `git status --porcelain`. D-19 added-lines scan returns 0. Transaction-authority scan returns 0. Deferred-idea keyboard-range-extension scan `grep -cE "shiftKey.*Arrow|Arrow(Left|Right).*shiftKey" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` returns 0.
    - `pnpm --dir app typecheck` exits 0 with all plan edits final.
    - Placement evidence and the shift-click current-transfer UAT flag are recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Select All is discoverable as a guarded focusable icon at the end of the key-utilities pill sharing one feedback line with the Cmd/Ctrl+A route; Shift-click range selection completes the D-01 gesture set; all plan-level D-18/D-19/authority/deferred-idea gates pass; typecheck green.</done>
  <reversibility rating="reversible">One additive guarded icon, one additive click branch, two additive prop wirings; removing them restores prior behavior with no data or contract impact.</reversibility>
</task>

</tasks>

<!-- planner-discipline-allow: sourceFrame|displayFrame|inBetweenCount -->
<!-- planner-discipline-allow: physicPaintBridge -->
<!-- (D-19/authority literals appear only inside Task 3's gate commands and acceptance-criteria grep patterns, never as implementation guidance) -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM/gesture layer -> view | Click/pointer events carry modifier state and hit-test results; DOM-derived keyIds enter selection intents. |
| View -> controller (new props/callbacks) | keyId intents and release-reject reasons cross from the strip into Studio callbacks that mutate selection signals and the status capsule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04-01 | Tampering | CSS attribute-selector injection via keyId in focus-follow queries | high | mitigate | Every `[data-roto-key-id="..."]` selector in the strip routes through the existing `cssEscape` helper; Task 2 acceptance asserts the escaped-selector count is unchanged and no new unescaped selector was added (the group focus-follow reuses the existing escaped grabbed-key selector verbatim). |
| T-37-04-02 | Tampering | Selection intents carrying DOM-derived keyIds | medium | mitigate | The strip emits keyIds only from real-key cells whose cellKeyId resolved from physical records; the 37-02 reducers validate membership fail-closed against the ordered real-key identity list, so a forged/unknown keyId leaves state unchanged. |
| T-37-04-03 | Repudiation | Status-capsule churn or phantom rejects | medium | mitigate | Release-time publication fires exactly once per rejected group release and only for resolver-level failures (candidateDetail non-null); hover-time preparation re-runs publish nothing (37-03 contract); classification-level invalids stay silent exactly like the single-key path. |
| T-37-04-04 | Elevation of Privilege (UX integrity) | Modifier-click stealing navigation or arming a drag; Escape double-handling | medium | mitigate | Task 1: modifier branches return before the navigate fallback (asserted by the region-scoped navigate-count gate) and the pointer-down guard rejects modifier presses so no gesture session is created; Pitfall 4 preserved — no new keydown listener (baseline-count assertion), the capture-phase drag-cancel listener stays authoritative. |
| T-37-04-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (37-RESEARCH Package Legitimacy Audit: not applicable); ListChecks/SquareCheckBig come from the already-installed lucide-preact 0.577.0; no install tasks, so no slop-check checkpoint is required. |
</threat_model>

<verification>
Plan-level bounded static gates (D-18: no test creation, modification, deletion, renaming, or execution; no dev server, browser, or native process):

1. `pnpm --dir app typecheck` exits 0 with all four files in final state.
2. Gesture wiring presence: `rotoSelectedKeyIds`, `onToggleRotoKeySelection`, `onExtendRotoKeySelection`, `onCollapseRotoSelectionToKey`, `onSelectAllRotoKeys`, `onRotoGroupDragRejected`, `aria-selected`, `prepareRotoKeyGroupDrag`, `commitRotoKeyGroupDrag` present in the strip; the three gesture callbacks and the two drag callbacks wired in PhysicsPaintStudio.tsx.
3. Presentation/CSS presence: `getRotoCellSelectedTooltipCopy` + `movedKeyIds` in physicsPaintWorkflowPresentation.ts; `.physics-paint-roto-cell.selected`, `.physics-paint-roto-cell.roto-drag-target-blocked`, `.roto-drag-cannot-drop` in physicsPaintStudio.css.
4. Safety invariants: navigate-call count inside handleRotoCellClick unchanged (2); keydown-listener count unchanged (1, the existing capture listener); escaped-selector count unchanged; `disabled=` count unchanged (aria-disabled only).
5. D-18 gate: `git status --porcelain` shows no `.test.` path.
6. D-19 gate: added-lines scans show zero legacy dual-frame identifiers and zero bridge imports in the strip's added lines; Shift+Arrow deferred-idea scan returns 0.
7. Wiring evidence: toggle trace (Task 1), GD-1/GD-2 traces (Task 2), Select All placement evidence (Task 3) recorded in the SUMMARY.
</verification>

<success_criteria>
- Cmd/Ctrl-click toggle, Shift-click range, and plain-click collapse flow from cell gestures through the 37-02 reducers with no navigation steal and no drag arming on modifiers (37-MULTI-SELECT-IDENTITY, D-01/D-02/D-05).
- Secondary selected cells render `.selected` + `Selected key` tooltip + aria-selected while the current key keeps the strongest orange highlight (37-UI-INTEGRATION, D-04).
- Group drag reuses the UAT-hardened gesture session with the 37-03 prepare/commit pair, moved-set complete-mapping preview, resolver-driven blocked-target preview with cannot-drop cursor, and exactly-once release-time reject publication (37-GROUP-DRAG, D-06..D-09).
- Select All sits guarded at the end of the key-utilities pill after Delete, sharing one `All keys selected` feedback line with the Cmd/Ctrl+A route (37-SELECT-ALL, D-03).
- Single-key drag, single-key actions, and every existing action-row control are provably unchanged (D-16); typecheck green; zero test artifacts; D-19 prohibitions hold.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (for downstream plans 37-05..37-06)

New strip props (app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx):
- `rotoSelectedKeyIds?: readonly string[]` — controller-owned selection set (37-02 signal).
- `onToggleRotoKeySelection?: (keyId: string) => void` / `onExtendRotoKeySelection?: (keyId: string) => void` / `onCollapseRotoSelectionToKey?: (keyId: string) => void` — selection gesture intents.
- `onSelectAllRotoKeys?: () => void` — Select All icon route (shares the Studio callback with Cmd/Ctrl+A).
- `onRotoGroupDragRejected?: (reason: string, detail: string) => void` — release-time reject publication (concise capsule copy + full diagnostic detail).

New view/presentation symbols:
- `RotoTimelineCellButtonProps.ariaSelected?: boolean` + forwarded click event; `RotoDragGestureSession.groupDrag` / `candidateConflicts` / `candidateDetail`; `RotoDragPreviewState.groupDrag` / `conflictingAppFrames`.
- `getRotoCellSelectedTooltipCopy(base)` in physicsPaintWorkflowPresentation.ts — exact copy `Selected key` / `Selected key — {base}`.
- `getRotoDragPreviewViewModel` moved-set generalization (role 'moved' over drag.movedKeyIds with single-key fallback).

New CSS classes (physicsPaintStudio.css):
- `.physics-paint-roto-cell.selected` — 2px #F2F5F7 outline at 1px offset, z-index 1 (secondary selected treatment).
- `.physics-paint-roto-cell.roto-drag-target-blocked` — destructive-family dotted outline + invalid fade (blocked group-drop targets).
- `.physics-paint-roto-cell.roto-drag-source.roto-drag-cannot-drop` — cannot-drop cursor on the grabbed key.

New UI element:
- Guarded Select All icon (`ListChecks`, label `All`, tooltip `Select all keys`) at the end of the key-utilities pill after Delete, consuming 37-03 `canSelectAllKeys` / `selectAllKeysDisabledReason`.

Modified in PhysicsPaintStudio.tsx: strip prop wiring for the selection set, three gesture callbacks (toggle/extend/collapse via 37-02 reducers), `onSelectAllRotoKeys={selectAllRotoKeys}`, and `onRotoGroupDragRejected` (setApplyMessage + console diagnostic).

Consumed by:
- 37-05: native UAT anchors — D-01 gesture set, the 3 UI-SPEC backstops (blocked preview, secondary distinctness at 18px, row fit), Select All availability/status, GD-1..GD-3 gesture-level flows, and the flagged shift-click current-transfer assumption.
- 37-06: post-UAT regression tests over the group presentation view model (blocked conflicts, moved-set roles) and selection-gesture wiring.
</artifacts_produced>

<output>
Create `.planning/phases/37-multi-select-physical-roto-keys/37-04-SUMMARY.md` when done, including: the Task-1 toggle trace derivation, the GD-1/GD-2 wiring derivations, the five Task-3 gate outputs verbatim, the Select All placement evidence, confirmation that no test file or listener was added and the Key spacing form is byte-untouched, and the flagged shift-click current-transfer assumption for 37-05 UAT.
</output>
