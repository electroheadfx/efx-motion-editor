---
phase: 37-multi-select-physical-roto-keys
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
autonomous: true
requirements:
  - 37-MULTI-SELECT-IDENTITY
  - 37-SELECT-ALL
tags:
  - physics-paint
  - roto
  - selection
  - signals
  - preact

must_haves:
  truths:
    - "Select-all over the real key identity set yields every real keyId ordered by physical frame (input is the store-ordered identity list; generated/empty cells are inherently excluded because they are not real-key identities) — D-03/D-05 (SELECT-ALL ordering/adjacency probe rows: covered)"
    - "Select-all on an empty identity set produces an empty selection and never fabricates a selected identity (SELECT-ALL empty probe row: covered)"
    - "Toggle adds a keyId when absent and removes it when present; the selection set never contains duplicate keyIds — D-01"
    - "Range extension selects the contiguous run of real keys from the anchor keyId to the target keyId inclusive, ordered by physical frame — D-01"
    - "Collapse always reduces the selection to exactly the single current editing keyId (or empty only when no real key exists); while any real key exists the selection is never empty — D-02"
    - "Post-acceptance selection aftermath follows D-17: group drag keeps the moved set with the grabbed key current; force-spacing keeps the set (keyIds survive retiming); group delete collapses to the survivor; every other accepted operation kind collapses to the single accepted selectedKeyId"
    - "Launch replacement (operationId/layerId change) and launch startFrame re-derivation reset the multi-selection set and anchor exactly the way the existing single selectedKeyId is reset (PhysicsPaintStudio.tsx:66-71 pattern)"
    - "Escape with no modifiers routes to the collapse action; Cmd/Ctrl+A routes to the select-all action only when focus is inside the workflow strip and mutationLocked is false — D-02/D-03, Pitfalls 4/5"
    - "The D-17 aftermath resolver accepts the operation kind as a plain string parameter so this plan compiles standalone in wave 1 before plan 37-01 extends the operation-kind union"
  assumptions:
    - "UNRESOLVED PROBE ROW (37-MULTI-SELECT-IDENTITY, unclassified): toggle-out-of-the-current-editing-key transfer semantics (current moves to the next selected key in frame order, falling back to previous, removal no-ops when it would empty the set) and shift-click making the clicked key the current editing key are planner-defined interpretations of D-01/D-02/D-04. Flagged for explicit confirmation at native UAT (plan 37-05). Do not treat as silently covered."
  prohibitions:
    - "Selection state, reducers, and dispatcher branches must never reference legacy dual-frame identity fields, frame numbers, or projected ownership as selection identity — stable keyId values only (D-05/D-19)"
    - "Generated or empty physical cells must never become selected identities; reducers only ever receive and emit real-key keyIds (D-05)"
    - "The multi-selection set and anchor must never persist, never cross the bridge, and never be added to PHYSIC_PAINT_ROTO_PHYSICAL_DOCUMENT_KEYS — only the single selectedKeyId persists (Pattern 5; document allowlist unchanged)"
    - "While any real key exists, no reducer or collapse path may produce an empty selection — one key always holds editing context (D-02)"
    - "No compatibility shims, migration code, forwarding wrappers, aliases, dual-write paths, or additional timing/transaction authority (D-19)"
    - "No deferred ideas: no Shift+Arrow keyboard range extension, no group Duplicate, no group Copy/Paste (CONTEXT.md Deferred Ideas)"
    - "No test creation, modification, deletion, renaming, or execution in this plan — pre-UAT production only (D-18); verification is bounded static checks on touched files"
    - "Selection state and reducers must not live in the view layer (PhysicsPaintWorkflowStrip) — controller boundary only; the strip receives props in plan 37-04"
    - "No second window-level capture-phase Escape listener for collapse — the drag session's existing capture listener stays authoritative during gestures (Pitfall 4)"
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts — new pure selection-reducer module (no Preact imports, no store imports)"
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — selectedKeyIds/selectionAnchorKeyId signals, launch-reset extension, D-17 post-acceptance rule, keyboard action wiring"
    - "app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts — Escape-collapse and Cmd/Ctrl+A dispatcher branches, extended actions interface"
  key_links:
    - "Invariant link: whenever selectedKeyIds is non-empty it contains selectedKeyId (D-02) — enforced by every reducer output and by the launch-reset path"
    - "dispatchPhysicsPaintStudioKeyDown actions object -> Studio collapseRotoSelection/selectAllRotoKeys callbacks -> selectedKeyIds/selectionAnchorKeyId signals"
    - "D-17 aftermath resolver <- accepted.operationKind + accepted.after.selectedKeyId at physicalEditCoordinatorRouteRef.current.consumeBridgeApplyResult (PhysicsPaintStudio.tsx:797-823)"
    - "Select-all reducer input <- store-ordered real key records (rotoKeyRecords, PhysicsPaintStudio.tsx:81) — the same validated identity list the resolver consumes"
---

<objective>
**As a** stop-motion animator, **I want to** select multiple real Physics Paint Roto keys — including Select All — **so that** group drag/drop, delete, and Force Spacing (plans 37-03/37-04) have a correct, session-local selection model to operate on.

This plan builds the multi-selection state layer at the Studio controller boundary: a pure keyId-only selection-reducer module, the `selectedKeyIds` / `selectionAnchorKeyId` Signals beside the existing `selectedKeyId` signal, launch-replacement reset, the D-17 post-acceptance selection aftermath rules, and the Escape-collapse / Cmd/Ctrl+A keyboard dispatcher branches. It covers requirements 37-MULTI-SELECT-IDENTITY and 37-SELECT-ALL per D-01, D-02, D-03 (keyboard half), D-05, and D-17.

Purpose: every group operation in later plans reads this selection state; getting identity semantics (keyId-only, never empty, never persisted, never in the view) right here prevents the failure class of frame-derived selection breaking under retiming.
Output: one new pure module, two modified modules; selection state fully functional at the controller boundary (view/strip consumption is plan 37-04).

**D-18 gate:** this plan is PRE-UAT production. No test files, no vitest runs. Verification is bounded static checks on the touched files only (scoped tsc filtering + grep/source assertions).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md
@.planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md
@.planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md
@.planning/phases/37-multi-select-physical-roto-keys/37-VALIDATION.md

@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintStudioKeyboard.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end select-all: pure selection module + Studio signal boundary wired for one path</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 59-74: existing `selectedKeyId` useSignal + launch-reset block; line 81: `rotoKeyRecords` store-ordered identity list; lines 377-386: selection ports `getSelectedKeyId`/`setSelectedKeyId`)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (stable keyId identity records — the identity set the selection references; read the exported record/identity types only)
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md (Pattern 5: Multi-selection state at the controller boundary)
    - .planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md (`PhysicsPaintStudio.tsx` section: signal ownership + launch-reset analog)
  </read_first>
  <action>
    Create the pure selection-reducer module `physicsPaintRotoMultiSelection.ts` in the roto module directory. It has zero Preact/store imports and operates only on plain keyId strings and readonly keyId arrays (selection identity is keyId-only per D-05; frame-derived or legacy dual-frame identity never enters this module per D-19).

    1. Define and export a `RotoKeySelectionState` type: `{ readonly selectedKeyIds: readonly string[]; readonly anchorKeyId: string | null }` (names are Claude's discretion per CONTEXT.md).
    2. Define and export the select-all reducer (e.g. `selectAllRotoKeyIds(orderedRealKeyIds: readonly string[], currentKeyId: string | null): RotoKeySelectionState`): returns every keyId in the given order (the caller passes the store-ordered real-key identity list, so physical-frame ordering and real-key-only membership are guaranteed by construction — D-03/D-05), anchor set to the current keyId when non-null else the first keyId, empty input yields `{ selectedKeyIds: [], anchorKeyId: null }` and never fabricates an identity.
    3. In `PhysicsPaintStudio.tsx`, beside the existing signal at line 59, add `selectedKeyIds = useSignal<readonly string[]>([])` and `selectionAnchorKeyId = useSignal<string | null>(null)` — mirroring the ownership pattern exactly; session-local, never persisted, never sent across the bridge (Pattern 5).
    4. Add a `selectAllRotoKeys` callback in the Studio that reads the ordered real-key keyIds from `rotoKeyRecords` (line 81 — `map` each record to its `keyId`), runs the reducer with the current `selectedKeyId.value`, and assigns both signals. No-op (leave signals untouched) when there are no real keys. This callback is the single Select All entry point that plan 37-03's guarded-availability wiring and plan 37-04's icon will reuse; Task 3 of this plan routes Cmd/Ctrl+A to it.

    This is the tracer: one real selection operation flowing from a Studio-level action through the pure reducer into the signal boundary, production-quality, kept for good. Expansion tasks add the remaining reducers and rules.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | grep -E 'physicsPaintRotoMultiSelection\.ts|PhysicsPaintStudio\.tsx' ; echo "tsc-touched-lines:$?" — before starting, capture this filtered output as the baseline; after the task, the filtered output must be identical to baseline (no new type errors attributable to touched files; pre-existing project-wide debt per VALIDATION.md is out of scope)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectedKeyIds" app/src/components/physic-paint/PhysicsPaintStudio.tsx` increases from 0 to at least 2 (declaration + select-all callback assignment)
    - `grep -c "selectionAnchorKeyId" app/src/components/physic-paint/PhysicsPaintStudio.tsx` increases from 0 to at least 2
    - `grep -cE "export (function|const) selectAll" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` returns at least 1
    - `grep -cE "from '(@preact/signals|preact)" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` returns 0 (pure module)
    - `grep -cE "sourceFrame|displayFrame" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` returns 0 (D-19 identity hygiene on the new file)
    - `grep -c "PHYSIC_PAINT_ROTO_PHYSICAL_DOCUMENT_KEYS" app/src/components/physic-paint/PhysicsPaintStudio.tsx` equals its pre-task baseline (document allowlist untouched)
  </acceptance_criteria>
  <done>Select-all flows end-to-end at the controller boundary: Studio callback -> pure reducer -> both signals populated with all real keyIds in store order; module is pure and keyId-only; scoped tsc shows zero new errors in touched files.</done>
  <reversibility rating="reversible">Additive session-local signals and a new pure module; nothing persisted or sent over the bridge — trivially reverted.</reversibility>
</task>

<task type="auto">
  <name>Collapse/toggle/extend reducers, launch-replacement reset, and D-17 post-acceptance selection aftermath</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 66-71: launch-reset branches; lines 797-823: `physicalEditCoordinatorRouteRef.current.consumeBridgeApplyResult` — the accepted-output consumption point where `accepted.operationKind` and `accepted.after.selectedKeyId` are read)
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts (as created in the tracer task)
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md (D-01, D-02, D-05, D-14, D-17)
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md (Pattern 5 rules; Pitfall 4)
  </read_first>
  <action>
    1. Extend the module with three pure reducers (names discretion):
       - `collapseRotoKeySelection(currentKeyId)`: returns `{ selectedKeyIds: currentKeyId ? [currentKeyId] : [], anchorKeyId: currentKeyId }` — the D-02 collapse; while any real key exists the caller guarantees a non-null current, so the selection never empties.
       - `toggleRotoKeySelection(state, orderedRealKeyIds, keyId, currentKeyId)` (D-01 Cmd/Ctrl-click): adds the keyId (keeping the set ordered by the given identity order, never duplicating) and sets anchor to it when absent; when present, removes it — but if removal would empty the set, keep the current key (D-02 never-empty). When the removed key was the current editing key and others remain, the reducer reports the next selected key in identity order (fallback previous) as the new current; when the removed key was the anchor, anchor falls back to the current key. Return the new state plus the resolved current keyId so the Studio can sync `selectedKeyId`. (These transfer semantics are the flagged UNRESOLVED probe assumption in must_haves — implement exactly as stated; UAT 37-05 confirms.)
       - `extendRotoKeySelectionRange(state, orderedRealKeyIds, targetKeyId)` (D-01 Shift-click): selects the contiguous run from `anchorKeyId` to `targetKeyId` inclusive over the ordered identity list (generated/empty cells excluded by construction); anchor unchanged so repeated shift-clicks re-extend from the same anchor; returns the target keyId as the new current editing key.
       All three validate membership against `orderedRealKeyIds` fail-closed: an unknown keyId leaves state unchanged (Security: no forged identities enter selection state).
    2. Launch-replacement reset (PhysicsPaintStudio.tsx lines 66-71): in BOTH branches that reassign `selectedKeyId.value` (operationId/layerId change at line 67; startFrame re-derivation at lines 68-70), also reset `selectedKeyIds.value` to `[selectedKeyId.value]` when non-null else `[]`, and `selectionAnchorKeyId.value` to `selectedKeyId.value` — exactly mirroring the single-selection reset so a replaced launch never inherits a stale multi-selection (Pattern 5).
    3. D-17 post-acceptance aftermath: export a pure `resolvePostAcceptanceRotoSelection(input: { operationKind: string; acceptedSelectedKeyId: string | null; state: RotoKeySelectionState; currentKeyId: string | null }): RotoKeySelectionState` from the module. The kind parameter is a plain `string` so this plan compiles standalone in wave 1 before plan 37-01 extends the operation-kind union. Rules keyed on the kind literal: `'move-key-group'` -> keep the set unchanged, set anchor to `acceptedSelectedKeyId` (the resolver sets proposal selectedKeyId = grabbed key, and the existing single-selection sync already moves `selectedKeyId` to it — so current = grabbed with the moved group still selected); `'force-spacing'` -> keep the set unchanged (keyIds survive retiming, D-17); `'delete-key-group'` -> collapse to `acceptedSelectedKeyId` (the survivor, D-14); every other kind (single-key ops, undo, redo, paste, duplicate, insert, play-script) -> collapse to `acceptedSelectedKeyId` (A4).
    4. Apply the rule in `physicalEditCoordinatorRouteRef.current.consumeBridgeApplyResult` (lines 797-823): when `transition === 'accepted'`, call the resolver with `accepted.operationKind`, `accepted.after.selectedKeyId`, and the current signal state, then assign both signals from the result. Place it after the existing `acceptedOutput.peek()` read; do not alter the existing paste-key reference-frame logic or the return value.
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | grep -E 'physicsPaintRotoMultiSelection\.ts|PhysicsPaintStudio\.tsx' — filtered output identical to the baseline captured in the tracer task (zero new errors in touched files)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "export (function|const) (collapse|toggle|extend)" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` returns at least 3
    - `grep -c "resolvePostAcceptanceRotoSelection" app/src/components/physic-paint/PhysicsPaintStudio.tsx` returns at least 1 (import or call site)
    - `grep -c "'move-key-group'" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` returns at least 1, and the same for `'delete-key-group'` (D-17 group-kind literals present as plain strings)
    - `grep -n "selectedKeyIds.value" app/src/components/physic-paint/PhysicsPaintStudio.tsx` shows assignments inside the launch-reset block (between the existing lines-66-71 region boundaries) and inside `consumeBridgeApplyResult`
    - `grep -cE "sourceFrame|displayFrame" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` still returns 0
    - `grep -cE "from '(@preact/signals|preact)" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` still returns 0
  </acceptance_criteria>
  <done>All four reducers + the D-17 aftermath resolver exist as pure keyId-only functions with fail-closed membership validation; launch replacement resets the set and anchor in both reset branches; accepted edits route their operationKind through the D-17 resolver and update both signals exactly once per accepted output.</done>
  <reversibility rating="reversible">Selection-state semantics only; the D-17 rules read from the already-accepted output and never influence the transaction — changing them later is a local edit, not a contract change.</reversibility>
</task>

<task type="auto">
  <name>Escape-collapse and Cmd/Ctrl+A dispatcher branches with Studio action wiring</name>
  <files>app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts (full file: actions interface lines 9-18, `isPhysicsPaintShortcutTarget` lines 20-26, guarded Backspace/Delete branch lines 90-102 — the shape both new branches copy)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 824-837: `usePhysicsPaintStudioKeyboard` actions object where the two new callbacks are wired)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (line 845: root element class `physics-paint-workflow-strip` used for the focus-scope check; lines 776-791: drag session's capture-phase Escape listener with `stopImmediatePropagation` — do not add any window-level listener)
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md (Pitfalls 4 and 5)
  </read_first>
  <action>
    1. Extend `PhysicsPaintStudioKeyboardActions` with two optional callbacks: `collapseRotoSelection?: () => void` and `selectAllRotoKeys?: () => void` (optional so existing callers/tests are untouched).
    2. Add an Escape branch in `dispatchPhysicsPaintStudioKeyDown`, placed before the space/arrow section and following the guarded-branch shape (target guard already applied at line 63): fire only when `event.key === 'Escape'` with no meta/ctrl/alt/shift modifiers and `!event.repeat` and `actions.collapseRotoSelection` is provided; then `event.preventDefault()` and call it. No new window-level or capture-phase listener — the drag session's existing capture listener (strip lines 776-791) already wins during an active gesture via `stopImmediatePropagation`, so drag-cancel keeps precedence per D-02/36.14 D-24 (Pitfall 4).
    3. Add a Cmd/Ctrl+A branch: `(event.metaKey || event.ctrlKey) && !event.shiftKey && !event.altKey && key === 'a'` (reuse the existing `meta`/`key` locals at lines 64-65), requiring `actions.selectAllRotoKeys` provided, `!state.mutationLocked`, and focus inside the strip — `event.target instanceof Element && event.target.closest('.physics-paint-workflow-strip') !== null`; then `event.preventDefault()` and call the action. The top-of-function `isPhysicsPaintShortcutTarget` guard (line 63) already excludes inputs/textareas/contenteditable, so LOG text selection keeps its native select-all (Pitfall 5). Paint-edit-mode shortcut-guard memory is satisfied structurally: this dispatcher only runs inside the Physics Paint Studio window and the branch is strip-focus-scoped; no change to the global `shortcuts.ts` is made or needed.
    4. Wire both actions in the Studio's keyboard actions object (lines 827-836): `collapseRotoSelection` collapses via the Task-2 collapse reducer only when `selectedKeyIds.value.length > 1` (no-op otherwise, preserving D-02 single-selection semantics), assigning both signals; `selectAllRotoKeys` is the tracer-task callback (shared entry point with the future strip icon per D-03).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | grep -E 'physicsPaintStudioKeyboard\.ts|PhysicsPaintStudio\.tsx|physicsPaintRotoMultiSelection\.ts' — filtered output identical to the tracer-task baseline (zero new errors in touched files)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "collapseRotoSelection" app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts` returns at least 2 (interface member + branch call)
    - `grep -c "selectAllRotoKeys" app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts` returns at least 2
    - `grep -c "closest('.physics-paint-workflow-strip')" app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts` returns at least 1 (strip-focus scope per Pitfall 5)
    - `grep -c "event.key === 'Escape'" app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts` returns at least 1
    - `grep -c "addEventListener" app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts` returns 0 (no new listeners; Pitfall 4)
    - `grep -c "collapseRotoSelection" app/src/components/physic-paint/PhysicsPaintStudio.tsx` returns at least 1 (wiring in the actions object)
    - `grep -c "state.mutationLocked" app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts` increases by at least 1 over baseline (Cmd/Ctrl+A guard)
  </acceptance_criteria>
  <done>Escape with no modifiers collapses an active multi-selection to the current key (and cancels drags first during gestures via the pre-existing capture listener); Cmd/Ctrl+A with strip focus and no mutation lock selects all real keys; neither branch affects LOG/input native behavior; scoped tsc clean against baseline.</done>
  <reversibility rating="reversible">Two additive dispatcher branches behind optional action callbacks; removing them restores prior behavior with no data or contract impact.</reversibility>
</task>

</tasks>

<!-- planner-discipline-allow: sourceFrame, displayFrame (D-19 literals appear only inside acceptance-criteria grep patterns targeting the new module, never inside task action bodies) -->

## Artifacts this phase produces (this plan)

| Artifact | Kind | Location | Consumer |
|----------|------|----------|----------|
| `RotoKeySelectionState` type (`selectedKeyIds` + `anchorKeyId`) | new export | `app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` | Studio controller; plans 37-03/37-04 |
| `selectAllRotoKeyIds` reducer | new export | same module | Studio `selectAllRotoKeys` callback; Cmd/Ctrl+A branch; 37-04 Select All icon |
| `collapseRotoKeySelection` reducer | new export | same module | Escape branch; plain-click collapse (37-04); D-17 default path |
| `toggleRotoKeySelection` reducer | new export | same module | Cmd/Ctrl-click gesture (37-04) |
| `extendRotoKeySelectionRange` reducer | new export | same module | Shift-click gesture (37-04) |
| `resolvePostAcceptanceRotoSelection` (D-17 aftermath) | new export | same module | `consumeBridgeApplyResult` accepted path |
| `selectedKeyIds` Signal | new state | `PhysicsPaintStudio.tsx` (beside line 59) | 37-03 group actions; 37-04 strip props |
| `selectionAnchorKeyId` Signal | new state | `PhysicsPaintStudio.tsx` (beside line 59) | Shift-click re-extension (37-04) |
| `selectAllRotoKeys` / `collapseRotoSelection` callbacks | new controller actions | `PhysicsPaintStudio.tsx` | keyboard dispatcher; 37-03/37-04 UI |
| `collapseRotoSelection?` / `selectAllRotoKeys?` action members + Escape / Cmd-Ctrl-A branches | modified | `physicsPaintStudioKeyboard.ts` | Studio keyboard handling |

(Names marked as discretion per CONTEXT.md Claude's Discretion; whatever final names are chosen, the roles above are the contract downstream plans import.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view -> controller | KeyIds from click/keyboard gestures enter selection state; untrusted in the sense that gesture targets are DOM-derived |
| controller -> persisted document / bridge | Selection must remain session-local; only the single `selectedKeyId` crosses this boundary today |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02-01 | Tampering | selection reducers (`physicsPaintRotoMultiSelection.ts`) | medium | mitigate | Fail-closed membership validation: every reducer validates incoming keyIds against the ordered real-key identity list; unknown/forged keyIds leave state unchanged (Task 2 action step 1) |
| T-37-02-02 | Tampering | persisted physical document / bridge payload | high | mitigate | Multi-selection set and anchor are session-local Signals only; static gate: `PHYSIC_PAINT_ROTO_PHYSICAL_DOCUMENT_KEYS` untouched and no persistence/bridge call references the set (tracer-task acceptance criteria) — blocks merge if violated |
| T-37-02-03 | Elevation of Privilege (UX integrity) | keyboard dispatcher (`physicsPaintStudioKeyboard.ts`) | medium | mitigate | Cmd/Ctrl+A scoped to strip focus via `.physics-paint-workflow-strip` closest check + `mutationLocked` guard + top-level `isPhysicsPaintShortcutTarget` input exclusion; Escape handled only in the existing bubble dispatcher (no second capture listener) so drag-cancel precedence is preserved (Pitfalls 4/5) |
| T-37-02-04 | Denial of Service (state integrity) | collapse/never-empty invariant | low | mitigate | Reducers structurally prevent an empty selection while a real key exists: collapse emits exactly the current keyId; toggle removal that would empty the set keeps the current key (D-02) |
| T-37-02-SC | Tampering | package installs | high | mitigate | No installs in this plan (RESEARCH: zero new dependencies); if any install is ever introduced, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] applies |
</threat_model>

<verification>
Plan-level checks (all bounded static, pre-UAT per D-18; no vitest, no dev server):

1. Scoped typecheck: `cd app && pnpm exec tsc --noEmit 2>&1 | grep -E 'physicsPaintRotoMultiSelection\.ts|PhysicsPaintStudio\.tsx|physicsPaintStudioKeyboard\.ts'` — output identical to the pre-plan baseline (zero new errors in touched files).
2. Purity/identity hygiene: `grep -cE "sourceFrame|displayFrame" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` == 0 and `grep -cE "from '(@preact/signals|preact)" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` == 0.
3. Boundary hygiene: `PHYSIC_PAINT_ROTO_PHYSICAL_DOCUMENT_KEYS` reference counts unchanged in every touched file; no new `addEventListener` in the dispatcher.
4. Full coverage of this plan's requirement IDs: 37-MULTI-SELECT-IDENTITY (Tasks 1-2), 37-SELECT-ALL (Tasks 1, 3).
</verification>

<success_criteria>
- Select-all flows end-to-end at the controller boundary (tracer proven by scoped static checks).
- All reducers exist as pure, keyId-only, fail-closed functions; D-17 aftermath applied at the accepted-output seam with the plain-string kind parameter (wave-1 standalone compile).
- Launch replacement resets set + anchor in both reset branches; the never-empty invariant (D-02) holds structurally.
- Escape-collapse and strip-scoped Cmd/Ctrl+A branches wired with mutation-lock and focus guards; no new listeners; LOG/input native behavior preserved.
- Zero new type errors in touched files vs baseline; no test artifacts created or executed (D-18); no deferred ideas implemented.
</success_criteria>

<output>
Create `.planning/phases/37-multi-select-physical-roto-keys/37-02-SUMMARY.md` when done, recording: final exported symbol names (if they differ from the discretion names above), the captured tsc baseline vs final filtered output, and the flagged UNRESOLVED probe assumption (toggle-out-of-current + shift-click current-transfer semantics) as pending native-UAT confirmation in plan 37-05.
</output>
