---
phase: 50-photo-reference-track
plan: 03
type: execute
wave: 3
depends_on: ["50-02"]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx
autonomous: true
requirements: [REF-01, REF-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A fixed Photo row renders directly ABOVE the Bg row in the pinned fixed-row block (Paint rows scroll above; Photo row, then Bg row, pinned below — UI-SPEC S1); the header carries a camera/image glyph (NEVER the Bg checker swatch), label `Photo`, a lock indicator, an eye toggle driving `visibleInStudio` (D-11), and an Import/Replace control (D-03)"
    - "The Photo row is NOT selectable as a track (no `role=\"button\"`, no tab-select) — the photo/reference track is never the active Paint track (D-06, not a content track); the lane renders a passive muted reference band spanning frame 0..parent end when a source exists, and an empty lane when none (no rails, no clips, no drag, no reorder, no duplicate/delete)"
    - "The Import/Replace control opens the Phase 49 `BackgroundAssetPickerView` as a full-area swap of the canvas region (D-01 — NOT a bare macOS dialog, NOT a port of `ImportedView`); the picker title reads `Import reference images`; the engine stays mounted underneath (paused); `Cancel` returns to the Studio untouched"
    - "Confirm REPLACES the source in one operation (D-03): the new selection becomes `sourceFrameRefs` ordered by `sortImagesByOriginalFilename` of original filenames (`shot_1 < shot_2 < shot_10`), NEVER asset UUID or click order (D-02); the source revision bumps (REF-04); one undoable record; `Reference source replaced.` capsule note"
    - "The CTA reads `Import images` when no source exists and `Replace source` when a source is present (UI-SPEC Copywriting Contract); a single image is a cycle of length 1"
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx"
      provides: "PhysicsPaintTrackRowKind extended with 'photo-reference' + Photo row header branch (camera glyph, eye toggle, Import/Replace control, passive band lane)"
      exports: ["PhysicsPaintTrackRowKind"]
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Photo row mount above the Bg row in the pinned fixed-row block"
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "reference picker region swap reusing BackgroundAssetPickerView + Confirm flow with natural sort + replace"
      exports: []
    - path: "app/src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx"
      provides: "Photo row header + band + empty-state contract suite"
      exports: []
  key_links:
    - "PhysicsPaintTrackRow.tsx Photo row header ↔ PhysicsPaintWorkflowStrip.tsx mount ↔ PhysicsPaintStudio.tsx picker swap — the Import/Replace control must route to the reference picker (not the Background picker) or the wrong source is replaced (D-03)"
    - "Confirm path ↔ sortImagesByOriginalFilename ↔ setPhotoReferenceSource (Plan 50-02) — natural sort before the store call, never after (D-02)"
  prohibitions:
    - statement: "The Photo row must NOT use the Bg checker swatch as its identity glyph — a camera/image icon only (spec stop condition: Background confused with the photo/reference track)"
      status: kept
      verification: flagged-unverified
    - statement: "The reference picker must NOT be a bare macOS dialog or a port of the main editor's ImportedView — it reuses BackgroundAssetPickerView (D-01)"
      status: kept
      verification: flagged-unverified
    - statement: "Source ordering must NOT use asset UUID or click order — natural filename sort of original filenames only (D-02)"
      status: kept
      verification: flagged-unverified
---

<objective>
Add the fixed Photo row to the Studio timeline strip and wire the reference source import through the reused Phase 49 asset picker, with natural-filename-sort ordering and replace semantics.

Purpose: the store operations (Plan 50-02) need a user-facing entry point. This plan delivers the Photo row (S1) and the picker surface (S2) so a user can import or replace the reference source — the first user-visible capability of the phase.

Output: Photo row (header + passive band + empty state) + reference picker swap + Confirm flow with natural sort and replace, with a row contract suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-photo-reference-track/50-CONTEXT.md
@.planning/phases/50-photo-reference-track/50-UI-SPEC.md
@.planning/phases/50-photo-reference-track/50-PATTERNS.md
@app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/BackgroundAssetPickerView.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/efx-paint/utils/naturalFilenameSort.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo row (S1) — header, passive band, eye toggle, and Import/Replace control</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx (PhysicsPaintTrackRowKind at line 85; the Bg header branch at lines 845-882)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (Bg row mount + fixed-row block composition)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S1 Photo row contract; Geometry and Layering Contract; Copywriting Contract)
  </read_first>
  <action>
    Extend `PhysicsPaintTrackRowKind` to `'paint' | 'background' | 'photo-reference'` (line 85). Add a Photo row header branch mirroring the Bg header (lines 845-882) but with: a camera/image glyph (lucide-preact `Camera` or `Image`, NEVER the Bg checker swatch), label `Photo`, a lock indicator with tooltip `Reference layer — fixed position`, an eye toggle (`aria-label="Toggle reference visibility"`, `aria-pressed` reflecting `visibleInStudio`) driving `setPhotoReferenceVisible` (D-11), and an Import/Replace control (`aria-label` reads `Import images` / `Replace source` per state) that calls an `onImportReference` port (D-03). The header is a plain non-selectable cell (no `role="button"`, no tabIndex). The lane renders a passive muted reference band spanning frame 0..parent end when a source exists (Phase 43 passive-marker family, muted desaturated tone distinct from every Paint rail color), and an empty lane when none — no rails, no clips, no drag, no reorder, no duplicate/delete. Mount the Photo row directly ABOVE the Bg row in the pinned fixed-row block in PhysicsPaintWorkflowStrip.tsx. Follow the `efx-preact-reactivity` rules (no useState, narrow signal reads, no render-body signal writes).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx</automated>
    <fails_when>non-zero exit, or a "FAIL" / "failed" line in the vitest summary for this file</fails_when>
  </verify>
  <acceptance_criteria>
    - The Photo row renders above the Bg row with camera glyph, `Photo` label, lock indicator, eye toggle, and Import/Replace control (S1, D-11, D-03)
    - The row is not selectable as a track; the lane shows a passive band when a source exists and an empty lane when none
    - The eye toggle drives `visibleInStudio`; the CTA reads `Import images` / `Replace source` per state
  </acceptance_criteria>
  <done>The Photo row renders with the full header and passive band/empty lane; the eye toggle and Import/Replace control are wired; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: reference picker swap (S2) — reuse BackgroundAssetPickerView, natural sort, replace source</name>
  <reversibility rating="costly">The picker variant is a full-area surface (D-01); reverting to a dialog means unwinding the region-swap machinery.</reversibility>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/BackgroundAssetPickerView.tsx (BackgroundAssetPickerViewProps at line 148; buildConfirmedImageIds at line 139; useBackgroundAssetPickerController at line 55)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the existing Background picker region swap mount)
    - app/src/efx-paint/utils/naturalFilenameSort.ts (sortImagesByOriginalFilename at line 35)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S2 picker contract; Interaction Contract Import/Replace)
  </read_first>
  <action>
    Wire the reference picker in PhysicsPaintStudio.tsx by reusing the existing `BackgroundAssetPickerView` region swap (D-01): a new "importing reference" state opens the same full-area picker with title `Import reference images`, images-only `ImportGrid`, multi-select, `Confirm`/`Cancel`, library import inside it, engine mounted underneath (paused). On Confirm, order the confirmed image IDs by `sortImagesByOriginalFilename` of their ORIGINAL filenames (`shot_1 < shot_2 < shot_10`), NEVER asset UUID or click order (D-02), then call `setPhotoReferenceSource(layerId, orderedRefs)` (Plan 50-02) — this REPLACES the source in one undoable operation (D-03) and bumps the source revision (REF-04). Announce `Reference source replaced.` through the status capsule. On Cancel, return to the Studio untouched (no source change, no document mutation). Selecting already-imported images reuses existing assets (no durable duplication, Phase 49 D-09). A single image is a cycle of length 1.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - The Import/Replace control opens the reused picker with title `Import reference images` (D-01)
    - Confirm replaces the source ordered by natural filename sort, never UUID/click order (D-02, D-03)
    - Cancel returns untouched; the replacement is one undoable operation bumping the source revision (D-03, REF-04)
  </acceptance_criteria>
  <done>The reference picker swap works end-to-end: import → natural-sort → replace → capsule note; Cancel is a no-op; `pnpm --dir app run typecheck` exits 0.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC `## Edge Coverage` section; the probe fallback report carried 6 unresolved items (REF-01..REF-05 unclassified + REF-05 idempotency/concurrency). Items authored into this plan's must_haves: REF-01 (one photo/reference track with stable source identity — the fixed Photo row + source import) and REF-04 (source revision invalidates dependents — the replace bumps the revision). The remaining unclassified rows resolve to Plans 50-01/50-02/50-04/50-05. Assumption for this plan: none — every probe item landing on REF-01/REF-04 import semantics is resolved explicit above.

## Artifacts this phase produces (this plan)

- Modified symbols: `PhysicsPaintTrackRowKind` (extended with `'photo-reference'`); `PhysicsPaintTrackRow.tsx` (Photo row header + band lane); `PhysicsPaintWorkflowStrip.tsx` (Photo row mount); `PhysicsPaintStudio.tsx` (reference picker swap + Confirm flow)
- New files: none (extends `PhysicsPaintTrackRow.test.tsx`)
- New exported symbols: none beyond the widened `PhysicsPaintTrackRowKind` union

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker selection → setPhotoReferenceSource | user-selected image IDs cross into the store; ordering and asset-ID provenance must be enforced here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-03-01 | Tampering | Confirm ordering path | medium | mitigate | natural filename sort of original filenames only (D-02); never asset UUID or click order — the sort is applied before the store call |
| T-50-03-02 | Tampering | picker source | medium | mitigate | library asset IDs only (Phase 49 D-09), never external paths; already-imported images reuse existing assets |
| T-50-03-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintTrackRow.test.tsx` green
- `pnpm --dir app run typecheck` green
- The Photo row is not selectable as a track; the picker reuses BackgroundAssetPickerView (D-01)
</verification>

<success_criteria>
A user can import or replace the reference source through the reused picker, ordered by natural filename sort, and see the Photo row reflect the source state (band vs empty). Plan 50-04 (ghost draw) can consume the imported source for the overlay.
</success_criteria>

<output>
Create `.planning/phases/50-photo-reference-track/50-03-SUMMARY.md` when done, recording the picker swap state shape and the Confirm ordering path as decisions for Plan 50-04.
</output>
