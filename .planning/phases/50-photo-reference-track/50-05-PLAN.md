---
phase: 50-photo-reference-track
plan: 05
type: execute
wave: 5
depends_on: ["50-04", "50-02"]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.tsx
  - app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements: [REF-02, REF-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A persistent right-panel `Photo Reference` section (S5) renders a 3-segment `Mode` control (`Reference only` / `Reveal source` / `Masked transform` — D-05), an `Overlay opacity` slider (0-100%, default 50%, live preview during drag, commit on release — D-12), a `Lock reference transform` toggle (D-13), and source facts (`{N} image(s)` with original filenames in natural sort order)"
    - "The mode switch is ONE undoable document mutation (D-07) via `setPhotoReferenceMode`; it is flag-only in Phase 50 — all three modes show the ghost identically, the ONLY visible difference is the active segment (D-06 HARD LOCK)"
    - "The opacity slider is a persisted display preference (D-12) via `setPhotoReferenceOpacity` — NOT an undoable mutation, never touches the flattened raster; the transform and lock state are display properties (D-13) via `setPhotoReferenceTransform` / `setPhotoReferenceTransformLocked`"
    - "The reference transform (S4) reuses the main editor `TransformOverlay` pattern — drag body to move, corner handles to scale, rotation handle to rotate, counter-scaled fixed screen-pixel handle sizes; locked by default (painting works normally), unlocking enters reference-transform mode (canvas gestures move the overlay), `Escape` or re-pressing the toggle re-locks (D-13)"
    - "The transform is IDENTICAL in all three modes (what you align is what Reveal will reveal) and NEVER affects the flattened raster or export (D-13, D-06)"
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.tsx"
      provides: "right-panel Photo Reference section (mode radiogroup + opacity slider + lock toggle + source facts) with controller/view split + injectable ports"
      exports: ["PhysicsPaintPhotoReferenceSection", "usePhysicsPaintPhotoReferenceSectionController", "PhysicsPaintPhotoReferenceSectionPorts"]
    - path: "app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts"
      provides: "mode-switch + opacity-release-commit + lock-toggle contract suite"
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "section mount in the right panel + transform handles on the monitor surface + Escape re-lock"
      exports: []
  key_links:
    - "PhysicsPaintPhotoReferenceSection.tsx mode control ↔ setPhotoReferenceMode (Plan 50-02) ↔ the undo ledger — the mode switch must record undo by reference (D-07)"
    - "PhysicsPaintPhotoReferenceSection.tsx opacity slider ↔ setPhotoReferenceOpacity (Plan 50-02) — release-commit, no undo, no revision bump (D-12)"
    - "PhysicsPaintStudio.tsx transform handles ↔ setPhotoReferenceTransform / setPhotoReferenceTransformLocked (Plan 50-02) ↔ drawReferenceGhost (Plan 50-04) — the transform writes to display properties and the ghost applies it (D-13)"
  prohibitions:
    - statement: "The mode switch must NOT change what the compositor, previewRenderer, or export draws — flag-only in Phase 50 (D-06 HARD LOCK)"
      status: kept
      verification: flagged-unverified
    - statement: "The opacity slider, transform, and lock toggle must NOT record undo or bump the revision counter — display preferences only (D-12/D-13)"
      status: kept
      verification: flagged-unverified
    - statement: "The transform must NOT write to the compositor or cache keys — monitor paint only (D-13)"
      status: kept
      verification: flagged-unverified
---

<objective>
Add the right-panel `Photo Reference` section (mode selector, opacity slider, lock toggle, source facts) and the canvas reference transform (drag/scale/rotate with lock), reusing the TransformOverlay pattern.

Purpose: the ghost (Plan 50-04) is drawn but not controllable. This plan delivers the mode flag (S5), the opacity display preference (S5), and the transform surface (S4) so a user can switch modes, tune overlay opacity, and align the reference to the framing they need.

Output: right-panel section component + transform handles + Studio mount + Escape re-lock, with a section contract suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-photo-reference-track/50-CONTEXT.md
@.planning/phases/50-photo-reference-track/50-UI-SPEC.md
@.planning/phases/50-photo-reference-track/50-PATTERNS.md
@app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.tsx
@app/src/components/canvas/TransformOverlay.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: right-panel Photo Reference section (S5) — mode, opacity, lock, source facts</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.tsx, app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintBackgroundClipSection.tsx (controller + view split at lines 31-71, 97-239; section render at lines 241-380; defaultPorts at the bottom)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S5 right-panel section; Copywriting Contract; Accessibility Contract — radiogroup, slider, toggle)
  </read_first>
  <action>
    Create `PhysicsPaintPhotoReferenceSection.tsx` following the `PhysicsPaintBackgroundClipSection` controller + view split with injectable `ports` and `defaultPorts`. The section renders: (1) a `Mode` 3-segment control (`role="radiogroup"`, `aria-label="Mode"`, one `role="radio"` per segment with `aria-checked`) with copy `Reference only` / `Reveal source` / `Masked transform` driving `setPhotoReferenceMode` (D-05, D-07 — one undoable mutation); (2) an `Overlay opacity` slider (`role="slider"`, `aria-valuemin="0"`, `aria-valuemax="100"`, `aria-valuenow` live during drag) with a `{N}%` readout, live preview during drag and commit on release via `setPhotoReferenceOpacity` (D-12 — display preference, no undo); (3) a `Lock reference transform` toggle (`aria-pressed` reflecting lock state) driving `setPhotoReferenceTransformLocked` (D-13); (4) source facts `{N} image(s)` with original filenames in natural sort order in the tooltip, and `No source imported` when empty. The mode exclusion hint `The reference never appears in flattened output.` is `aria-describedby` on the group. Follow the `efx-preact-reactivity` rules (signals-only, narrow reads, no render-body writes).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts</automated>
    <fails_when>non-zero exit, or a "FAIL" / "failed" line in the vitest summary for this file</fails_when>
  </verify>
  <acceptance_criteria>
    - The section renders the 3-segment mode control, opacity slider, lock toggle, and source facts (S5)
    - Mode switch calls setPhotoReferenceMode (one undoable mutation, D-07); opacity commits on release (D-12); lock toggle drives transformLocked (D-13)
    - The exclusion hint is present; empty state shows `No source imported`
  </acceptance_criteria>
  <done>The section passes its contract suite; mode/opacity/lock controls are wired to the store setters; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: reference transform handles (S4) — drag/scale/rotate reusing the TransformOverlay pattern</name>
  <reversibility rating="costly">The canvas-transform surface is new (D-13); reverting to a fixed overlay means unwinding the handle machinery.</reversibility>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts</files>
  <read_first>
    - app/src/components/canvas/TransformOverlay.tsx (counter-scaled handles at lines 190-194 — cornerSize = 8/zoom, edgeSize = 6/zoom, strokeWidth = 1.5/zoom; drag/scale/rotate hit-testing at lines 463-557, 691-729)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S4 transform handles; Interaction Contract Reference transform; Geometry — fixed screen-pixel handles)
  </read_first>
  <action>
    Implement the reference transform handles on the monitor surface, reusing the `TransformOverlay` PATTERN (counter-scaled fixed screen-pixel handles, drag-to-move body hit, corner scale handles, rotation handle) but writing to the reference display properties via `setPhotoReferenceTransform` — NOT to `layerStore`/`keyframeStore` (the reference is not a layer). The transform defaults to centered at natural size, no rotation (D-13). While `transformLocked` is true (default), the overlay has no handles and no canvas grab — painting works normally. Unlocking enters reference-transform mode: drag moves the reference, corner handles scale, the rotation handle rotates; painting gestures are intercepted by the transform mode. The transform is applied identically in all three modes (what you align is what Reveal will reveal) and NEVER affects the flattened raster or export (D-13, D-06). Handle geometry reads accepted display state only (no optimistic changes).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Locked by default: no handles, no canvas grab, painting works normally (D-13)
    - Unlocked: drag moves, corner handles scale, rotation handle rotates; painting gestures intercepted (D-13)
    - The transform writes to display properties only, never the compositor or cache keys (D-13, D-06)
  </acceptance_criteria>
  <done>The transform handles work with the lock toggle; the transform writes to display properties only; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Studio mount + Escape re-lock</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (right-panel section mount sites; the keyboard shortcut surface)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S5 section mount; Interaction Contract — Escape re-locks from anywhere in reference-transform mode)
  </read_first>
  <action>
    Mount `PhysicsPaintPhotoReferenceSection` in the right panel (reusing the Track-section card geometry) and the transform handles on the monitor surface. Wire `Escape` to re-lock the transform from anywhere in reference-transform mode (D-13) — re-pressing the lock toggle also re-locks. Ensure the section and handles read accepted canonical state only (no optimistic changes), and that the mode switch never changes what the compositor draws (D-06 flag-only). Follow the `efx-preact-reactivity` rules.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from either command, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - The section is mounted in the right panel; the transform handles are on the monitor surface (S5, S4)
    - Escape re-locks the transform from anywhere in reference-transform mode (D-13)
    - The mode switch is flag-only — no compositor change (D-06)
  </acceptance_criteria>
  <done>The section and transform handles are mounted; Escape re-locks; the mode switch is flag-only; `pnpm --dir app run typecheck` exits 0.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC `## Edge Coverage` section; the probe fallback report carried 6 unresolved items (REF-01..REF-05 unclassified + REF-05 idempotency/concurrency). Items authored into this plan's must_haves: REF-02 (three modes — the 3-segment control + flag-only behavior) and REF-03 (reference-only visibility excluded from flattened output — the transform/opacity never touch the compositor). The remaining unclassified rows resolve to Plans 50-01/50-02/50-03/50-04. Assumption for this plan: none — every probe item landing on REF-02/REF-03 control semantics is resolved explicit above.

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.tsx`, `app/src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts`
- Modified symbols: `PhysicsPaintStudio.tsx` (section mount + transform handles + Escape re-lock)
- New exported symbols: `PhysicsPaintPhotoReferenceSection`, `usePhysicsPaintPhotoReferenceSectionController`, `PhysicsPaintPhotoReferenceSectionPorts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| right-panel controls → store setters | mode/opacity/lock inputs cross into the store; the mutation vs display-preference split must hold here |
| transform handles → compositor | the transform must never write to the compositor or cache keys (D-13, D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-05-01 | Tampering | mode control | medium | mitigate | mode switch is one undoable mutation via setPhotoReferenceMode; flag-only — no compositor change (D-06, D-07) |
| T-50-05-02 | Information disclosure | transform handles | medium | mitigate | transform writes to display properties only, never the compositor or cache keys (D-13, D-06) |
| T-50-05-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintPhotoReferenceSection.test.ts` green
- `pnpm --dir app run typecheck` green
- The mode switch is flag-only; opacity/transform/lock are display preferences (no undo, no revision bump)
</verification>

<success_criteria>
A user can switch the reference mode, tune overlay opacity, and align the reference via the canvas transform with lock/Escape, all without the reference ever reaching flattened output. Plan 50-06 (persistence + native UAT) can verify the full flow end-to-end.
</success_criteria>

<output>
Create `.planning/phases/50-photo-reference-track/50-05-SUMMARY.md` when done, recording the section ports shape and the transform handle reuse shape as decisions for Plan 50-06.
</output>
