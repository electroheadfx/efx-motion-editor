---
phase: 50-photo-reference-track
plan: 04
type: execute
wave: 4
depends_on: ["50-02", "50-03"]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.ts
  - app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements: [REF-03, REF-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The reference ghost draws as a semi-transparent overlay ON TOP of the composite in the onion-ghost family (D-09), at the overlay opacity (default 0.5, `ONION_DEPTH_OPACITY[0]`), transformed by the reference display transform (D-13); it is monitor paint only and NEVER enters the flattened raster, main preview, or export (D-06)"
    - "The ghost resolves the source frame per cursor frame (frame N → source frame N, clamped at the sequence end — D-15), never once at import (Pitfall M5); a single image is a cycle of length 1"
    - "The ghost is visible ONLY while painting/editing on the active track — it hides during playback and export (D-14, onion-skin behavior); the overlay is INDEPENDENT of Paint-track hide/solo (D-10), controlled only by its own `visibleInStudio` toggle"
    - "Missing source is fail-closed (D-04): the ghost renders absent (no placeholder fill, no stripe) and the status capsule reports `Missing reference source — use Replace source to re-link.` with the red warning triangle; recovery is the Replace flow (no separate recovery surface)"
    - "The overlay toggle-off state removes the ghost entirely — there is no 0-opacity residual rendering (UI-SPEC Color contract)"
  artifacts:
    - path: "app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.ts"
      provides: "shouldDrawReferenceGhost decision + drawReferenceGhost monitor-paint draw (onion-ghost family, transform-applied, fail-closed)"
      exports: ["shouldDrawReferenceGhost", "drawReferenceGhost"]
    - path: "app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts"
      provides: "ghost draw-decision + fail-closed + playback-hide contract suite"
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "ghost draw mount in the monitor-paint layer + missing-source status capsule report"
      exports: []
  key_links:
    - "PhysicsPaintReferenceGhost.ts drawReferenceGhost ↔ physicPaintStore.ts getReferenceSourceFrameVerdict (Plan 50-02) — the fail-closed null is what the ghost and capsule consume (D-04)"
    - "PhysicsPaintStudio.tsx monitor-paint mount ↔ the compositor boundary — the ghost draws in the monitor layer seat only, never threaded into getFlattenedFrame (D-06)"
  prohibitions:
    - statement: "The ghost must NOT draw into the flattened raster, main preview, or export in ANY mode — monitor paint only (D-06 HARD LOCK)"
      status: kept
      verification: flagged-unverified
    - statement: "A missing source must NOT render as a placeholder fill or stripe — the ghost is absent and the capsule reports (D-04, Phase 48 D-09 placeholder excision)"
      status: kept
      verification: flagged-unverified
    - statement: "The ghost must NOT be visible during playback or export — it hides by not drawing (D-14)"
      status: kept
      verification: flagged-unverified
---

<objective>
Draw the reference ghost overlay on the Studio monitor (onion-ghost family) with frame-aligned fail-closed resolution, and surface missing-source through the status capsule.

Purpose: the imported source (Plan 50-03) is invisible until it is drawn. This plan delivers the ghost overlay (S3) and the missing-source capsule (S6) so a user can see the reference while painting — the core reference-only visibility of the phase.

Output: a monitor-paint ghost draw module (decision + draw) + the Studio mount + the missing-source capsule report, with a draw-decision contract suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-photo-reference-track/50-CONTEXT.md
@.planning/phases/50-photo-reference-track/50-UI-SPEC.md
@.planning/phases/50-photo-reference-track/50-PATTERNS.md
@app/src/components/physic-paint/roto/rotoOnionPreview.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/stores/physicPaintStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ghost draw module (S3) — frame-aligned fail-closed monitor paint</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.ts, app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoOnionPreview.ts (ONION_DEPTH_OPACITY at line 34 — the default 0.5 overlay opacity)
    - app/src/stores/physicPaintStore.ts (getReferenceSourceFrameVerdict from Plan 50-02; _resolveReferenceSourceImage)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S3 ghost overlay contract; Color contract — no tint, no blend-mode change, no outline)
  </read_first>
  <action>
    Create `PhysicsPaintReferenceGhost.ts` with two exports. (1) `shouldDrawReferenceGhost(document, frame, isPlaying)` — a pure decision returning `{ draw: boolean, verdict: { ref, dataUrl, clamped } | null }`: returns `draw: false` when `photoReference` is null, when `visibleInStudio` is false (D-11), when `isPlaying` is true (D-14), or when the resolved source frame is missing (fail-closed, D-04); otherwise `draw: true` with the verdict from `getReferenceSourceFrameVerdict(document, frame)` (frame-aligned, clamped — D-15). (2) `drawReferenceGhost(ctx, document, frame, zoom, isPlaying)` — calls the decision, and when `draw: true`, draws the resolved image as a semi-transparent ghost at `track.opacity` (default 0.5), transformed by `track.transform` (position/scale/rotation — D-13), with no tint, no blend-mode change, no outline. The draw is monitor paint only — it never touches the compositor. Follow the `efx-preact-reactivity` rules (pure functions, no signal writes).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts</automated>
    <fails_when>non-zero exit, or a "FAIL" / "failed" line in the vitest summary for this file</fails_when>
  </verify>
  <acceptance_criteria>
    - The decision returns draw:false for null track, invisible, playing, or missing source (D-11, D-14, D-04)
    - The decision returns draw:true with the frame-aligned clamped verdict otherwise (D-15)
    - The draw applies the overlay opacity and the display transform with no tint/blend/outline (D-09, D-13)
  </acceptance_criteria>
  <done>The ghost decision + draw module passes its contract suite; frame-aligned fail-closed resolution and playback-hide are proven; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Studio mount + missing-source capsule (S6)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the monitor-paint layer seat where onion ghosts draw; the status capsule setApplyStatus('error') surface)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S6 status capsule; Error states — missing source copy)
  </read_first>
  <action>
    Mount `drawReferenceGhost` in the Studio's monitor-paint layer seat (the same layer family onion ghosts use — above the composite, beneath selection/tool paint), passing the current cursor frame, zoom, and playback state. When `shouldDrawReferenceGhost` reports a missing source (verdict null while a track exists), surface `Missing reference source — use Replace source to re-link.` through the existing status capsule with the red warning triangle (D-04, Phase 48 D-09 family); the report is independent of the visibility preference (fail-closed reporting fires even when the overlay toggle is off). The ghost is absent during playback (D-14) by not drawing — no opacity trick, no cache entry, no export residue. Do not thread any reference input into the compositor or export path (D-06).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - The ghost draws in the monitor-paint layer seat, above the composite, beneath tool paint (S3)
    - Missing source surfaces through the status capsule with the red warning triangle (D-04)
    - The ghost is absent during playback; no reference input reaches the compositor (D-14, D-06)
  </acceptance_criteria>
  <done>The ghost is mounted in the monitor layer and the missing-source capsule reports fail-closed; `pnpm --dir app run typecheck` exits 0.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC `## Edge Coverage` section; the probe fallback report carried 6 unresolved items (REF-01..REF-05 unclassified + REF-05 idempotency/concurrency). Items authored into this plan's must_haves: REF-03 (reference-only visibility excluded from flattened output — the monitor-paint-only ghost) and REF-04 (missing source visible and recoverable — the fail-closed capsule). The remaining unclassified rows resolve to Plans 50-01/50-02/50-03/50-05. Assumption for this plan: none — every probe item landing on REF-03/REF-04 draw semantics is resolved explicit above.

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.ts`, `app/src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts`
- Modified symbols: `PhysicsPaintStudio.tsx` (ghost draw mount + missing-source capsule report)
- New exported symbols: `shouldDrawReferenceGhost`, `drawReferenceGhost`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| monitor-paint layer → compositor | the ghost draw must never cross into the flattened compositor path (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-04-01 | Information disclosure | drawReferenceGhost | high | mitigate | monitor-paint-only draw; the compositor and export receive no reference input (D-06); the ghost never enters getFlattenedFrame |
| T-50-04-02 | Denial of service | shouldDrawReferenceGhost | medium | mitigate | fail-closed null on missing source + capsule report; never a placeholder fill or silent transparency (D-04, Pitfall M6) |
| T-50-04-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintReferenceGhost.test.ts` green
- `pnpm --dir app run typecheck` green
- The ghost draws monitor-paint-only; the missing-source capsule reports fail-closed
</verification>

<success_criteria>
A user sees the reference ghost over the composite while painting, hidden during playback, with a fail-closed missing-source capsule. Plan 50-05 (right panel + transform) can add the mode selector, opacity slider, and transform handles on top of this draw path.
</success_criteria>

<output>
Create `.planning/phases/50-photo-reference-track/50-04-SUMMARY.md` when done, recording the ghost draw signature and the monitor-paint layer seat as decisions for Plan 50-05.
</output>
