---
phase: 50-photo-reference-track
plan: 02
type: tdd
wave: 2
depends_on: ["50-01"]
files_modified:
  - app/src/stores/efxPaintStore.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/efxPaintStore.photoReference.test.ts
autonomous: true
requirements: [REF-02, REF-03, REF-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Source replace (D-03) and mode switch (D-07) are ONE undoable document mutation each on the unified 10-level ledger (Phase 46 D-01..D-03), bumping the photo/reference track `revision` AND the document `documentRevision` counter; one Undo restores the prior source/mode by reference (no raster-byte snapshots)"
    - "`visibleInStudio` (D-11), `opacity` (D-12), `transform` (D-13), and `transformLocked` (D-13) are persisted display preferences — they survive save/reopen but record NO undo entry and do NOT bump the revision counter (the `_setTrackDisplayProperty` vs mutation-setter split, RESEARCH Pattern 1)"
    - "Frame-aligned resolution (D-15): application frame N resolves to source frame N, 1:1 from frame 0, clamped at the sequence end (last source frame holds); resolved per cursor frame, never once at import (Pitfall M5); a single image is a cycle of length 1"
    - "Missing source is fail-closed (D-04): a missing library asset resolves to `null` (never a placeholder, never silent transparency — Pitfall M6) and the source revision term carries a `:missing` suffix so dependents see the absence"
    - "D-06 exclusion lock (REF-03): `getFlattenedFrame` output is byte-identical whether the photo reference is absent, present, or `visibleInStudio` toggled — the compositor and export receive NO reference input (structural exclusion, not a runtime check)"
    - "Serialize/hydrate (REF-05): `serializeRuntimeIntoDocument` writes the photo reference track and `hydrateRuntimeFromDocument` + `hydrateBackgroundSourceImagesFromLibrary` restore it and register its source images; the round-trip is idempotent (serialize → hydrate → serialize is stable)"
  artifacts:
    - path: "app/src/stores/efxPaintStore.ts"
      provides: "photo reference CRUD ops (setPhotoReferenceSource, setPhotoReferenceMode, setPhotoReferenceVisible, setPhotoReferenceOpacity, setPhotoReferenceTransform, setPhotoReferenceTransformLocked) + serialize/hydrate inclusion"
      exports: ["setPhotoReferenceSource", "setPhotoReferenceMode", "setPhotoReferenceVisible", "setPhotoReferenceOpacity", "setPhotoReferenceTransform", "setPhotoReferenceTransformLocked"]
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "parallel _referenceSourceImages registry + registerReferenceSourceImage + _referenceSourceRevision + _resolveReferenceSourceImage (frame-aligned, fail-closed)"
      exports: ["registerReferenceSourceImage", "getReferenceSourceFrameVerdict"]
    - path: "app/src/stores/efxPaintStore.photoReference.test.ts"
      provides: "mode-mutation + undo, exclusion, source-revision + missing-source contract suite"
      exports: []
  key_links:
    - "efxPaintStore.ts mutation setters ↔ useRotoPhysicalEditHistory.ts recordBackgroundEdit (undo ledger) — the source/mode mutations must record by reference or Undo silently breaks (D-07)"
    - "physicPaintStore.ts _referenceSourceImages registry ↔ _resolveReferenceSourceImage ↔ the ghost draw path (Plan 50-04) — the fail-closed null is what the ghost and capsule consume (D-04)"
    - "physicPaintStore.ts _resolveFlattenedFrame / getFlattenedFrame ↔ the reference registry — the exclusion is structural: no reference ref is ever threaded into the flattened path (D-06)"
  prohibitions:
    - statement: "The opacity slider, transform, and lock toggle must NOT record an undo entry or bump the revision counter — they are display preferences, not document mutations (D-12/D-13)"
      status: kept
      verification: flagged-unverified
    - statement: "The source must NOT be resolved once at import — frame-aligned resolution happens per cursor frame (D-15, Pitfall M5)"
      status: kept
      verification: flagged-unverified
    - statement: "A missing source must NOT render as silent transparency — it resolves to null and surfaces through the status capsule (D-04, Pitfall M6)"
      status: kept
      verification: flagged-unverified
---

<objective>
Add the photo/reference track CRUD operations to the store layer: undoable source-replace and mode-switch mutations, non-undoable display-preference setters, a parallel source registry with a deterministic revision term, frame-aligned fail-closed source resolution, and the structural D-06 exclusion from the flattened path — plus serialize/hydrate persistence.

Purpose: the document model (Plan 50-01) is inert without store operations. This plan is the runtime authority: it classifies every setter into exactly one of the two field classes (document mutation vs display preference — the highest-risk mistake in this phase, RESEARCH Pattern 1), wires the undo ledger, and proves the reference never leaks into flattened output.

Output: store CRUD + registry + resolution + exclusion + persistence, with a contract suite covering REF-02 (mode + undo), REF-03 (exclusion), and REF-04 (source revision + missing source).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-photo-reference-track/50-CONTEXT.md
@.planning/phases/50-photo-reference-track/50-RESEARCH.md
@.planning/phases/50-photo-reference-track/50-PATTERNS.md
@app/src/stores/efxPaintStore.ts
@app/src/stores/physicPaintStore.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/stores/efxPaintStore.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: photo reference CRUD — undoable source/mode mutations vs non-undoable display-preference setters</name>
  <reversibility rating="one-way">The replace contract (D-03) is a published behavior — re-opening the picker REPLACES the source and bumps the revision; changing it later changes which source lands where for every saved project.</reversibility>
  <files>app/src/stores/efxPaintStore.ts, app/src/stores/efxPaintStore.photoReference.test.ts</files>
  <read_first>
    - app/src/stores/efxPaintStore.ts (_setTrackDisplayProperty at lines 331-351; setTrackVisible/Solo/Opacity/Blend at lines 354-395; BackgroundEditDescriptor at line 565; BackgroundEditOperationKind at line 549; serializeRuntimeIntoDocument revision-bump idiom at lines 1054-1061)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts (recordBackgroundEdit at line 688; BackgroundEditCommand at line 180)
    - .planning/phases/50-photo-reference-track/50-PATTERNS.md (Pattern Assignments 5 — the mutation-setter vs display-preference split)
  </read_first>
  <behavior>
    - Test 1 (SOURCE REPLACE UNDO): `setPhotoReferenceSource(layerId, ['a','b'])` creates the track (mode defaults to `reference-only`, visibleInStudio true, opacity 0.5, transform centered, transformLocked true, revision 0) and records ONE undo entry; `setPhotoReferenceSource(layerId, ['c'])` replaces it, bumps the track revision AND documentRevision, and records a second undo entry; undo restores `['a','b']` by reference
    - Test 2 (MODE SWITCH UNDO): `setPhotoReferenceMode(layerId, 'reveal-source')` records ONE undo entry and bumps the track revision AND documentRevision; undo restores `reference-only` (D-07)
    - Test 3 (DISPLAY PREF NO-UNDO): `setPhotoReferenceOpacity(layerId, 0.8)`, `setPhotoReferenceVisible(layerId, false)`, `setPhotoReferenceTransform(layerId, {...})`, `setPhotoReferenceTransformLocked(layerId, false)` each persist the value but record NO undo entry and do NOT bump documentRevision (D-11/D-12/D-13)
    - Test 4 (IDEMPOTENCE): calling a setter with the current value is a no-op (returns ok, no revision bump, no undo entry)
  </behavior>
  <action>
    Implement the six setters in efxPaintStore.ts. Classify each FIRST (RESEARCH Pattern 1): `setPhotoReferenceSource` and `setPhotoReferenceMode` are document mutations — they create/replace the track, bump the track `revision` and the `documentRevision` counter (following the serializeRuntimeIntoDocument revision-bump idiom at lines 1054-1061), and record by reference on the unified ledger via `recordBackgroundEdit` with new operation kinds `'set-photo-reference-source'` and `'set-photo-reference-mode'` (extend `BackgroundEditOperationKind`). `setPhotoReferenceVisible`, `setPhotoReferenceOpacity`, `setPhotoReferenceTransform`, `setPhotoReferenceTransformLocked` are display preferences — they follow the `_setTrackDisplayProperty` shape (no documentRevision bump, no undo record). The track is created on first source import with the locked defaults (mode `reference-only`, visibleInStudio true, opacity 0.5, transform `{x:0,y:0,scaleX:1,scaleY:1,rotation:0}`, transformLocked true, revision 0). All setters are idempotent (same-value no-op). Do not touch the flattened path in this task (Task 3 owns the exclusion).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.photoReference.test.ts</automated>
    <fails_when>non-zero exit, or a "FAIL" / "failed" line in the vitest summary for this file</fails_when>
  </verify>
  <acceptance_criteria>
    - Source replace and mode switch are single undoable mutations bumping track revision + documentRevision (D-03, D-07, REF-02)
    - Display-preference setters persist without undo or revision bump (D-11, D-12, D-13)
    - All setters are idempotent
  </acceptance_criteria>
  <done>The six setters pass Tests 1-4; source/mode mutations record undo by reference and bump revisions; display-preference setters persist without undo or revision bump; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: source registry + revision + frame-aligned fail-closed resolution</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/efxPaintStore.photoReference.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts (_backgroundSourceImages Map at line 434; registerBackgroundSourceImage at line 435; hydrateBackgroundSourceImages at line 491; hydrateBackgroundSourceImagesFromLibrary at line 614; _backgroundSourceRevision at line 1584; _resolveBackgroundSourceImage at line 1122; getBackgroundFrameVerdict at line 1810)
    - .planning/phases/50-photo-reference-track/50-PATTERNS.md (Pattern Assignments 4 — the registry + revision seam to mirror)
  </read_first>
  <behavior>
    - Test 5 (FRAME-ALIGNED): a track with `sourceFrameRefs: ['f0','f1','f2']` resolves frame 0 → f0, frame 1 → f1, frame 2 → f2, frame 3 → f2 (clamped at sequence end, D-15); a single-image track resolves every frame to the same ref (cycle length 1)
    - Test 6 (MISSING FAIL-CLOSED): a source ref absent from the registry resolves to `null` (never a placeholder); the revision term for that ref carries a `:missing` suffix (D-04)
    - Test 7 (REVISION TERM): `_referenceSourceRevision` changes when a source ref is replaced or a dataUrl changes; it is stable when the registry is unchanged; it is empty when `photoReference` is null
  </behavior>
  <action>
    Add a parallel `_referenceSourceImages` Map and `registerReferenceSourceImage` to physicPaintStore.ts (mirroring `_backgroundSourceImages`/`registerBackgroundSourceImage` at lines 434-447 — a parallel map keeps the reference's fail-closed resolution independent of the Background clip lifecycle, RESEARCH Open Question 2). Add `_referenceSourceRevision(document)` mirroring `_backgroundSourceRevision` (lines 1584-1593) with the `:missing` suffix. Add `_resolveReferenceSourceImage(document, frame)` implementing the frame-aligned law: `index = min(frame, sourceFrameRefs.length - 1)`, resolve the ref through the registry, return `null` on missing. Add a `getReferenceSourceFrameVerdict` accessor returning `{ ref, dataUrl, clamped }` (or `null` when no track/missing) for the ghost draw path (Plan 50-04) and the band tooltip. Do not resolve once at import — resolution is per cursor frame (Pitfall M5).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.photoReference.test.ts src/stores/physicPaintStore.test.ts</automated>
    <fails_when>non-zero exit, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Frame N resolves to source frame N, clamped at sequence end (D-15, REF-04)
    - Missing source resolves to null with a `:missing` revision suffix (D-04)
    - The revision term is deterministic and changes on source/dataUrl change (REF-04)
  </acceptance_criteria>
  <done>Tests 5-7 pass; the registry, revision term, and frame-aligned fail-closed resolution are in place; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-06 exclusion lock + serialize/hydrate persistence</name>
  <reversibility rating="one-way">The exclusion guarantee (D-06) is a published output contract — reference pixels leaking into flattened output before Phase 52 is the release stop condition "Reference-only photo pixels leak into output"; relaxing it later breaks that stop condition.</reversibility>
  <files>app/src/stores/efxPaintStore.ts, app/src/stores/physicPaintStore.ts, app/src/stores/efxPaintStore.photoReference.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts (_resolveFlattenedFrame at line 1609; getFlattenedFrame at line 1768)
    - app/src/stores/efxPaintStore.ts (serializeRuntimeIntoDocument at line 1039; hydrateRuntimeFromDocument at line 1072; hydrateBackgroundSourceImagesFromLibrary call at line 1088)
    - .planning/phases/50-photo-reference-track/50-PATTERNS.md (Pattern Assignments 4 — the D-06 exclusion lock and the serialize/hydrate seam)
  </read_first>
  <behavior>
    - Test 8 (EXCLUSION): `getFlattenedFrame` output is byte-identical for a document with no photo reference, with a photo reference present, and with `visibleInStudio` toggled true/false — the reference never enters the flattened raster (REF-03, D-06)
    - Test 9 (SERIALIZE/HYDRATE): serialize → hydrate → serialize is stable (idempotent); the hydrated document carries the same sourceFrameRefs, mode, visibleInStudio, opacity, transform, transformLocked, and revision (REF-05)
    - Test 10 (SOURCE HYDRATION): after hydrate, the reference source images are registered in `_referenceSourceImages` via the library-hydration path (REF-05)
  </behavior>
  <action>
    Enforce the D-06 exclusion structurally: do NOT thread any reference source ref into `_resolveFlattenedFrame`, `getFlattenedFrame`, `compositeFrame`, or `previewRenderer.ts` — the reference lives only in `document.photoReference` and the parallel `_referenceSourceImages` registry, which the flattened path never reads. Add the photo reference track to `serializeRuntimeIntoDocument` (write `photoReference` into the serialized document) and `hydrateRuntimeFromDocument` (restore it), and register its source images through `hydrateBackgroundSourceImagesFromLibrary` (line 1088) so reopen warms the registry. Keep the exclusion test (Test 8) as the structural proof — it asserts the flattened output is unchanged by any reference state.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.photoReference.test.ts src/stores/physicPaintStore.test.ts && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from either command, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - getFlattenedFrame output is byte-identical regardless of reference state (REF-03, D-06)
    - Serialize/hydrate round-trip is idempotent and preserves all track fields (REF-05)
    - Reference source images hydrate through the library path (REF-05)
  </acceptance_criteria>
  <done>Tests 8-10 pass; the exclusion is structural and the persistence round-trip is idempotent; `pnpm --dir app run typecheck` exits 0.</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC `## Edge Coverage` section; the probe fallback report carried 6 unresolved items (REF-01..REF-05 unclassified + REF-05 idempotency/concurrency). Items authored into this plan's must_haves: REF-02 (mode mutation + undo — Test 2), REF-03 (exclusion — Test 8), REF-04 (source revision + missing source — Tests 5-7), and the REF-05 idempotency/concurrency concern (serialize→hydrate→serialize stability — Test 9). The REF-01 unclassified row resolves to Plan 50-01 (model + revision term). Assumption for this plan: none — every probe item landing on REF-02/03/04/05 store semantics is resolved explicit above.

## Artifacts this phase produces (this plan)

- Modified symbols: `efxPaintStore.ts` (six photo-reference setters + serialize/hydrate inclusion); `physicPaintStore.ts` (parallel `_referenceSourceImages` registry + `_referenceSourceRevision` + `_resolveReferenceSourceImage` + `getReferenceSourceFrameVerdict`)
- New files: `app/src/stores/efxPaintStore.photoReference.test.ts`
- New exported symbols: `setPhotoReferenceSource`, `setPhotoReferenceMode`, `setPhotoReferenceVisible`, `setPhotoReferenceOpacity`, `setPhotoReferenceTransform`, `setPhotoReferenceTransformLocked`, `registerReferenceSourceImage`, `getReferenceSourceFrameVerdict`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce disk JSON → hydrateRuntimeFromDocument | persisted photoReference data crosses into the runtime store; source refs are attacker/legacy-writable |
| store registry → getFlattenedFrame | the reference registry must never cross into the flattened compositor path (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-02-01 | Tampering | setPhotoReferenceSource | high | mitigate | library asset IDs only (Phase 49 D-09), never external paths; source refs validated as non-empty strings at parse (Plan 50-01) |
| T-50-02-02 | Information disclosure | getFlattenedFrame / export | high | mitigate | structural D-06 exclusion — no reference ref threaded into the flattened path; Test 8 asserts byte-identical output (REF-03) |
| T-50-02-03 | Denial of service | _resolveReferenceSourceImage | medium | mitigate | fail-closed null on missing asset + `:missing` revision suffix; never silent transparency (D-04, Pitfall M6) |
| T-50-02-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintStore.photoReference.test.ts src/stores/physicPaintStore.test.ts` green
- `pnpm --dir app run typecheck` green
- The mutation/display-preference split is enforced (no undo on display prefs, no revision bump)
</verification>

<success_criteria>
The store layer exposes undoable source/mode mutations and non-undoable display-preference setters, resolves the source frame-aligned and fail-closed, excludes the reference from flattened output structurally, and persists the track through serialize/hydrate. Plans 50-03 (row + picker), 50-04 (ghost draw), and 50-05 (right panel) can consume these operations without further store work.
</success_criteria>

<output>
Create `.planning/phases/50-photo-reference-track/50-02-SUMMARY.md` when done, recording the exact setter signatures and the `BackgroundEditOperationKind` extension as decisions for Plans 50-03..50-05.
</output>
