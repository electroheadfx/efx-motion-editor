---
phase: 50-photo-reference-track
plan: 06
type: execute
wave: 6
depends_on: ["50-05", "50-03"]
files_modified:
  - app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements: [REF-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Save/reopen preserves the photo reference track end-to-end: `sourceFrameRefs` (natural filename sort order), `mode`, `visibleInStudio`, `opacity`, `transform`, `transformLocked`, and `revision` all survive serialize → parse → hydrate (REF-05, success criterion 5); the round-trip is idempotent (serialize → hydrate → serialize is stable)"
    - "The full user flow works end-to-end: import source → Photo row band → ghost overlay → mode switch → opacity slider → transform with lock/Escape → save → reopen → everything restored (native UAT)"
    - "The reference never reaches flattened output in any mode, even after save/reopen — the D-06 exclusion holds through the persistence path (D-06)"
  artifacts:
    - path: "app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts"
      provides: "photo reference track round-trip contract (serialize → parse → hydrate preserves all track fields, idempotent)"
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "end-to-end Studio integration contract (import → ghost → mode → opacity → transform → save/reopen wiring)"
      exports: []
  key_links:
    - "serializeRuntimeIntoDocument ↔ hydrateRuntimeFromDocument ↔ the photo reference track (Plan 50-02) — the persistence round-trip is the proof that the mutation/display-preference split survives save/reopen (REF-05)"
    - "The full Studio flow ↔ the store setters from Plan 50-02 and the draw path from Plan 50-04 — the end-to-end contract asserts the wiring, not just the units (REF-05)"
  prohibitions:
    - statement: "The persistence round-trip must NOT drop the display-preference fields (visibleInStudio, opacity, transform, transformLocked) — they survive save/reopen alongside the mutation fields (D-11/D-12/D-13)"
      status: kept
      verification: flagged-unverified
    - statement: "The reference must NOT reach flattened output after save/reopen — the D-06 exclusion holds through the persistence path (D-06)"
      status: kept
      verification: flagged-unverified
---

<objective>
Prove the photo reference track survives save/reopen (REF-05) and verify the full user flow end-to-end, closing the phase with a native UAT.

Purpose: Plans 50-01..50-05 built the model, store, row, ghost, and right panel in isolation. This plan is the integration proof: the persistence round-trip (serialize → parse → hydrate) preserves every track field, and the end-to-end Studio contract asserts the whole flow is wired — import → ghost → mode → opacity → transform → save/reopen. It is the last gate before the phase is declared complete.

Output: an extended persistence round-trip contract + an extended end-to-end Studio integration contract, with a native UAT checklist for the user.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-photo-reference-track/50-CONTEXT.md
@.planning/phases/50-photo-reference-track/50-UI-SPEC.md
@.planning/phases/50-photo-reference-track/50-PATTERNS.md
@app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/stores/efxPaintStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: persistence round-trip — photo reference track survives save/load (REF-05)</name>
  <files>app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts</files>
  <read_first>
    - app/src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts (the existing multi-track round-trip scenario at lines 46-113 — makeTrackDocument, makeFrame, pngDataUrl, rotoRecord helpers; the serialize → parse → hydrate flow)
    - app/src/stores/efxPaintStore.ts (serializeRuntimeIntoDocument at line 1039; hydrateRuntimeFromDocument at line 1072; the six photo-reference setters from Plan 50-02)
    - .planning/phases/50-photo-reference-track/50-CONTEXT.md (D-11 visibleInStudio, D-12 opacity, D-13 transform/lock — display preferences that must survive save/reopen)
  </read_first>
  <action>
    Extend `efxPaintPersistenceMultiTrackRoundTrip.test.ts` with a photo-reference round-trip scenario. Build a document, register it, then drive the Plan 50-02 setters to establish a full track: `setPhotoReferenceSource(layerId, ['shot_1','shot_2','shot_10'])` (natural sort order), `setPhotoReferenceMode(layerId, 'reveal-source')`, `setPhotoReferenceOpacity(layerId, 0.8)`, `setPhotoReferenceVisible(layerId, false)`, `setPhotoReferenceTransform(layerId, {x:12,y:34,scaleX:1.5,scaleY:0.75,rotation:0.3})`, `setPhotoReferenceTransformLocked(layerId, false)`. Serialize via `serializeRuntimeIntoDocument`, parse via `parseEfxPaintDocument`, hydrate via `hydrateRuntimeFromDocument`, then assert the hydrated document's `photoReference` deep-equals the pre-save track on ALL fields: `sourceFrameRefs` (ordered, natural sort), `mode`, `visibleInStudio`, `opacity`, `transform`, `transformLocked`, and `revision`. Add a second assertion that serialize → hydrate → serialize is stable (idempotent — the REF-05 idempotency/concurrency probe item). Do not assert on the flattened output here (Task 2 owns the D-06 exclusion-through-persistence proof).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts</automated>
    <fails_when>non-zero exit, or a "FAIL" / "failed" line in the vitest summary for this file</fails_when>
  </verify>
  <acceptance_criteria>
    - The photo reference track round-trips through serialize → parse → hydrate preserving all seven fields (REF-05)
    - The round-trip is idempotent (serialize → hydrate → serialize stable)
    - The display-preference fields (visibleInStudio, opacity, transform, transformLocked) survive alongside the mutation fields (D-11/D-12/D-13)
  </acceptance_criteria>
  <done>The persistence round-trip contract passes; the photo reference track survives save/load with all fields intact and idempotent; `pnpm --dir app run typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: end-to-end Studio integration contract + native UAT (REF-05)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts (the source-code contract style — readFileSync + toContain assertions; the compositor/previewRenderer/exportRenderer non-regression surface at lines 40-45)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (the section mount, ghost draw mount, transform handles, and Escape re-lock from Plans 50-03/50-04/50-05)
    - .planning/phases/50-photo-reference-track/50-UI-SPEC.md (S1-S7 surfaces; the full user flow)
  </read_first>
  <action>
    Extend `PhysicsPaintStudio.test.ts` with an end-to-end integration contract asserting the full flow is wired in the Studio source: the Photo row mount (Plan 50-03), the reference picker swap reusing `BackgroundAssetPickerView` (D-01), the ghost draw mount in the monitor-paint layer (Plan 50-04), the right-panel `PhysicsPaintPhotoReferenceSection` mount (Plan 50-05), the transform handles on the monitor surface, and the `Escape` re-lock. Add a D-06 non-regression assertion: the compositor, flattened cache, preview renderer, and export renderer source still contain NO reference-input threading (the reference never reaches flattened output in any mode, even after save/reopen). Follow the existing `toContain` source-contract style — no new test config, no watch mode.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm --dir app run typecheck</automated>
    <fails_when>non-zero exit from either command, or any "failed" count &gt; 0 in the vitest summary line</fails_when>
    <human-check>
      Native UAT (user runs the app — Claude does NOT claim done before this passes):
      1. Open the EFX Physic Paint Studio and import a reference source (Import images) — the Photo row appears above the Bg row with a camera glyph and a passive band.
      2. Confirm the ghost overlay draws over the composite while painting, at 50% opacity by default.
      3. Switch the Mode control between Reference only / Reveal source / Masked transform — the active segment changes, the ghost looks identical (flag-only).
      4. Drag the Overlay opacity slider — live preview during drag, commit on release; the value persists.
      5. Unlock the reference transform, drag/scale/rotate the overlay, then press Escape — it re-locks and painting works normally.
      6. Save the project, close, and reopen — the source, mode, opacity, transform, and lock state are all restored.
      7. Confirm the reference NEVER appears in flattened output or export in any mode.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The end-to-end Studio contract asserts the full flow is wired (import → ghost → mode → opacity → transform → save/reopen)
    - The D-06 non-regression assertion holds: no reference input reaches the compositor, cache, preview, or export (D-06)
    - The native UAT checklist is recorded for the user (no done-before-UAT claim)
  </acceptance_criteria>
  <done>The end-to-end Studio contract passes; the D-06 exclusion holds through the persistence path; the native UAT checklist is recorded and the phase is automated-ready (not claimed done until the user's UAT passes).</done>
</task>

</tasks>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC `## Edge Coverage` section; the probe fallback report carried 6 unresolved items (REF-01..REF-05 unclassified + REF-05 idempotency/concurrency). Item authored into this plan's must_haves: REF-05 (save/reopen preserves source identity and mode — the persistence round-trip) and the REF-05 idempotency/concurrency concern (serialize → hydrate → serialize stability — Task 1). The remaining unclassified rows resolve to Plans 50-01/50-02/50-03/50-04/50-05. Assumption for this plan: none — every probe item landing on REF-05 persistence semantics is resolved explicit above.

## Artifacts this phase produces (this plan)

- Modified symbols: `efxPaintPersistenceMultiTrackRoundTrip.test.ts` (photo reference round-trip scenario); `PhysicsPaintStudio.test.ts` (end-to-end integration contract + D-06 non-regression assertion)
- New files: none (extends existing test files)
- New exported symbols: none

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mce disk JSON → hydrateRuntimeFromDocument | persisted photoReference data crosses into the runtime store on reopen; the round-trip must preserve every field and stay idempotent (REF-05) |
| store registry → getFlattenedFrame | the reference registry must never cross into the flattened compositor path, even after save/reopen (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-06-01 | Tampering | persistence round-trip | medium | mitigate | the round-trip contract asserts all seven track fields survive serialize → parse → hydrate and the round-trip is idempotent (REF-05); source refs are validated at parse (Plan 50-01) |
| T-50-06-02 | Information disclosure | getFlattenedFrame / export | high | mitigate | the D-06 non-regression assertion proves no reference input reaches the compositor, cache, preview, or export after save/reopen (D-06) |
| T-50-06-SC | Tampering | npm installs | low | mitigate | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintPersistenceMultiTrackRoundTrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts` green
- `pnpm --dir app run typecheck` green
- The persistence round-trip preserves all seven track fields and is idempotent (REF-05)
- The D-06 exclusion holds through the persistence path (D-06)
- Native UAT checklist recorded; the phase is automated-ready, not claimed done before the user's UAT
</verification>

<success_criteria>
The photo reference track survives save/reopen with every field intact and idempotent, the full user flow is wired end-to-end, and the D-06 exclusion holds through persistence. The phase is automated-ready; the user's native UAT is the final gate before Phase 50 is declared complete.
</success_criteria>

<output>
Create `.planning/phases/50-photo-reference-track/50-06-SUMMARY.md` when done, recording the persistence round-trip shape and the native UAT checklist as the phase close-out evidence.
</output>
