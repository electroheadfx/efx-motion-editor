# Phase 36.12: Physics Paint Roto Generated Interpolation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-29
**Phase:** 36.12-physics-paint-roto-generated-interpolation
**Areas discussed:** Frame placement, Generated look, Refresh timing, Controls feedback

---

## Frame Placement

| Question | Options | User's choice | Notes |
|---|---|---|---|
| When interpolation is enabled between two real Roto keys, which frames should become generated? | Fill the gap; Use count control; You decide | Fill the gap | Generate every integer frame strictly between adjacent real keys. |
| If an interior gap already has background-only support frames, what should generated interpolation do there? | Generated wins; Keep background; You decide | Generated wins | Generated interpolation may replace derived background-only support state, but paper/background composition stays separate and must not be baked into alpha caches. |
| Should interpolation generate frames for the whole Roto layer at once, or only for the span around the current selected key? | Whole layer; Current span; You decide | Whole layer | Generate every interior gap between every adjacent pair of real keys. |
| When a generated frame would collide with an existing real key after rounding or layout changes, what should happen? | Real key wins; Abort regen; You decide | Real key wins | Never overwrite real keys; skip generated output for that frame. |

---

## Generated Look

| Question | Options | User's choice | Notes |
|---|---|---|---|
| For the first MVP, what should the generated in-between image look like between two real Roto keys? | Alpha blend; Hold previous; Motion warp | Alpha blend | The generated look should come from real-key alpha caches, not restored strokes. |
| Should the blend include both neighboring real keys at proportional opacity, or use a stepped hold? | Proportional blend; Stepped hold; You decide | Proportional blend | Blend previous and next real-key alpha by normalized position in the gap. |
| Should Phase 36.12 expose multiple interpolation modes, or lock the visible MVP to proportional alpha blend? | Blend only; Blend + hold; Keep all modes | Blend only | No visible duplicate/hold, Move, Deform, or mode matrix in this phase. |
| Should generated alpha blending account for source-key paper/background differences? | Alpha only; Blend full image; You decide | Alpha only | Paper/background follows existing Roto background/missing-frame rules and is not baked into generated caches. |

---

## Refresh Timing

| Question | Options | User's choice | Notes |
|---|---|---|---|
| When a source real key is saved, pasted, duplicated, inserted, deleted, or replaced, what should happen to generated interpolation frames? | Auto-refresh; Mark stale; Mixed | Auto-refresh | Clear and regenerate dependent generated frames from the new real-key layout. |
| Should generated interpolation stay enabled persistently once the user turns it on, or should Generate be one-shot? | Persistent toggle; One-shot generate; You decide | Persistent toggle | Generated frames keep auto-refreshing until interpolation is disabled. |
| If auto-regeneration fails after a source-key change, what should the user see? | Keep real keys; Rollback action; Keep old generated | Keep real keys | Preserve real-key edits, remove/ignore generated output, and show compact regeneration failure feedback. |
| When interpolation is disabled, what should happen to already generated frames? | Remove generated; Keep cached; You decide | Remove generated | Disabled state immediately clears generated frames. |

---

## Controls Feedback

| Question | Options | User's choice | Notes |
|---|---|---|---|
| What visible control should turn generated interpolation on and off for UAT? | Toggle only; Toggle + count; Generate button | Toggle only | Use a simple Generate/Interpolation toggle in the existing workflow strip. |
| How should generated frames be labeled in the Roto timeline/status area? | Generated label; Connector only; Badge + label | Generated label | Keep generated cells visually distinct and show render-only copy. |
| What should happen when the user clicks a generated frame cell? | Preview only; Blocked click; Convert prompt | Preview only | Select/show generated render-only output without editing or conversion. |
| While a generated frame is preview-selected, which controls should remain available? | Navigation only; Key tools only; You decide | Navigation only | Allow navigation/timeline/playback only; disable Save current, paint editing, and key utilities with render-only status copy. |

---

## Claude's Discretion

None. The user selected or clarified every discussed gray area.

## Deferred Ideas

- Duplicate/hold, Move/Deform, and multiple interpolation modes are deferred beyond Phase 36.12.
- Convert-generated-to-real-key behavior is out of scope for this phase.
- Final Pencil timeline layout and visual polish remain Phase 36.13.
