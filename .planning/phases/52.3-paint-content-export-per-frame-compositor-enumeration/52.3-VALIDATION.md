---
phase: "52.3"
slug: "paint-content-export-per-frame-compositor-enumeration"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-19"
---

# Phase 52.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (node env for the export harnesses) |
| **Config file** | existing — `app/vite.config.ts` (chunk budget 1340 kB at :257); no test-config changes permitted |
| **Quick run command** | `cd app && pnpm vitest run src/lib/exportEngine.paintEnumeration.test.ts` |
| **Full suite command** | `pnpm --filter efx-motion-editor exec vitest run` (never watch mode) |
| **Estimated runtime** | ~60 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `cd app && pnpm vitest run src/lib/exportEngine.paintEnumeration.test.ts src/lib/frameMap.test.ts src/lib/exportRenderer.test.ts`
- **After every plan wave:** Run `pnpm --filter efx-motion-editor exec vitest run` + `tsc --noEmit` + `cargo test` + chunk budget build
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

Requirement-level map (from RESEARCH.md §Validation Architecture); planner refines per-task rows in PLAN.md frontmatter/tasks.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 52.3-AC-C | TBD | 1 | AC-C (paint-only enumeration → frameMap length 5) | T-52.3-01 | N/A | integration (real computed, real stores) | `cd app && pnpm vitest run src/lib/exportEngine.paintEnumeration.test.ts -t "Case C"` | ✅ parked `it.todo` → reinstate | ⬜ pending |
| 52.3-AC-D | TBD | 1 | AC-D (paint-only export completes, 5 renders) | — | N/A | integration | same file, `-t "Case D"` | ✅ parked `it.todo` → reinstate | ⬜ pending |
| 52.3-AC-E | TBD | 2 | AC-E (fx active + selectedSequenceOnly → filtered export completes; empty timeline still refuses) | — | N/A | integration | same file, `-t "Case E"` | ✅ amend in place (Pitfall 4) | ⬜ pending |
| 52.3-AC-CLEAR | TBD | 1 | AC-CLEAR (D-06 clearRect on no-content frames; content frames byte-identical) | T-52.3-03 | No stale-canvas ghost pixels across frames | unit (real `renderGlobalFrame`, stub canvas recording ops) | `cd app && pnpm vitest run src/lib/exportRenderer.test.ts` | ✅ file exists; new tests | ⬜ pending |
| 52.3-AC-MIX | TBD | 1 | AC-MIX (Control A/B stay green; mixed tail-hold) | — | N/A | integration | same paintEnumeration file (Controls A/B) | ✅ green today — regression pins | ⬜ pending |
| 52.3-PITFALL1 | TBD | 2 | Pitfall 1 fix (fx at `inFrame > 0` selected-only renders paint; gate on `entry.globalFrame`) | — | N/A | integration | same file, new case | ❌ add with the fix | ⬜ pending |
| 52.3-FRAMEMAP | TBD | 1 | frameMap unit: dense enumeration, paint coverage, gap frames, hidden fx excluded, loop-aware extent | T-52.3-02 | No unbounded N allocation | unit | `cd app && pnpm vitest run src/lib/frameMap.test.ts` | ✅ file exists; new tests | ⬜ pending |
| 52.3-D09 | TBD | 3 | D-09 info line reflects filtered count for fx active | — | N/A | component | FormatSelector test — none exists today | ❌ Wave 0 decision (or native UAT row) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] None blocking — the acceptance harness (`exportEngine.paintEnumeration.test.ts`) and both subject test files exist with the correct mock discipline.
- [ ] Optional: FormatSelector component test for D-09 if the planner wants an automated pin (no such test file exists today; otherwise the info line is verified on the native UAT row).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Native UAT: §7-pair (31 keys) exports full PNG range | AC-UAT | Requires real app + real project | Per phase acceptance rows |
| Native UAT: fresh paint-only project in EACH orientation exports | AC-UAT | Requires real app launch | Vertical + horizontal projects |
| Native UAT: mixed project exports; frames visually match on-canvas composite | AC-UAT | Visual parity judgment | Compare exported PNGs to canvas |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
