---
phase: "52.3"
slug: "paint-content-export-per-frame-compositor-enumeration"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: false
created: "2026-09-19"
---

# Phase 52.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.1.9 (node env for the export harnesses) |
| **Config file** | existing — `app/vite.config.ts` (chunk budget 1340 kB at :257); no test-config changes permitted |
| **Quick run command** | `cd app && pnpm vitest run src/lib/exportEngine.paintEnumeration.test.ts` |
| **Full suite command** | `pnpm --filter efx-motion-editor exec vitest run` (never watch mode) |
| **Estimated runtime** | ~60 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `cd app && pnpm vitest run src/lib/exportEngine.paintEnumeration.test.ts src/lib/frameMap.test.ts src/lib/exportRenderer.test.ts`
- **After every plan wave:** Run `pnpm --filter efx-motion-editor exec vitest run` + `tsc --noEmit` + `cargo test` + chunk budget build
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

Requirement-level map (from RESEARCH.md §Validation Architecture); planner refines per-task rows in PLAN.md frontmatter/tasks.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 52.3-AC-C | 01 | 1 | AC-C (paint-only enumeration → frameMap length 5) | T-52.3-01 | N/A | integration (real computed, real stores) | `cd app && pnpm vitest run src/lib/exportEngine.paintEnumeration.test.ts -t "Case C"` | ✅ reinstated at :287 (52.3-01) | ✅ green |
| 52.3-AC-D | 01 | 1 | AC-D (paint-only export completes, 5 renders) | — | N/A | integration | same file, `-t "Case D"` (:297 + motion-blur variant :360) | ✅ reinstated (52.3-01) | ✅ green |
| 52.3-AC-E | 01/02 | 1–2 | AC-E (fx active + selectedSequenceOnly → filtered export completes; empty timeline still refuses) | — | N/A | integration | same file — Case E :309, Case E2 :325, D-10 negative :350 | ✅ amended in place per Pitfall 4 (52.3-01) | ✅ green |
| 52.3-AC-CLEAR | 01 | 1 | AC-CLEAR (D-06 clearRect on no-content frames; content frames byte-identical) | T-52.3-03 | No stale-canvas ghost pixels across frames | unit (real `renderGlobalFrame`, stub canvas recording ops) | `cd app && pnpm vitest run src/lib/exportRenderer.test.ts` — describe at :590 | ✅ added (52.3-01) | ✅ green |
| 52.3-AC-MIX | 01 | 1 | AC-MIX (Control A/B stay green; mixed tail-hold) | — | N/A | integration | same paintEnumeration file — Controls A :233 / B :250 | ✅ regression pins | ✅ green |
| 52.3-PITFALL1 | 02 | 2 | Pitfall 1 fix (fx at `inFrame > 0` selected-only renders paint; gate on `entry.globalFrame`) | T-52.3-04 | Overlay-leg gate derives only from internal entry.globalFrame | unit | `cd app && pnpm vitest run src/lib/exportRenderer.test.ts -t "Pitfall 1"` (:507; shared-fix pin :541) | ✅ added with the fix (52.3-02) | ✅ green |
| 52.3-FRAMEMAP | 01 | 1 | frameMap unit: dense enumeration, paint coverage, gap frames, hidden fx excluded, loop-aware extent | T-52.3-02 | No unbounded N allocation | unit | `cd app && pnpm vitest run src/lib/frameMap.test.ts` — describe at :423 (6 cases) | ✅ added (52.3-01) | ✅ green |
| 52.3-D09 | 02 | 3 | D-09 info line reflects filtered count for fx active | — | N/A | manual (native UAT) | source pin: grep `frameMap.value.filter(e => e.sequenceId === activeSeq.id)` FormatSelector.tsx → 1; verified on UAT row 5 | ✅ wiring pinned (52.3-02) | ✅ manual-only (UAT pass 2026-09-19) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] None blocking — the acceptance harness (`exportEngine.paintEnumeration.test.ts`) and both subject test files exist with the correct mock discipline.
- [x] Optional: FormatSelector component test for D-09 — declined per the Wave-0 decision; the info line is verified on the native UAT row (52.3-03 row 5, pass). Source wiring + tsc pinned in 52.3-02.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Native UAT: §7-pair (31 keys) exports full PNG range | AC-UAT | Requires real app + real project | Per phase acceptance rows — PASS 2026-09-19 |
| Native UAT: fresh paint-only project in EACH orientation exports | AC-UAT | Requires real app launch | Vertical + horizontal projects — PASS 2026-09-19 |
| Native UAT: mixed project exports; frames visually match on-canvas composite | AC-UAT | Visual parity judgment | Compare exported PNGs to canvas — PASS 2026-09-19 |
| D-09 info line shows real filtered count (not 0) for an fx active | D-09 | No FormatSelector component test exists (Wave-0 declared optional) | 52.3-03 UAT row 5 — PASS 2026-09-19; source wiring grep-pinned |
| Transparent-fond preview ghost check while scrubbing | AC-UAT (row 6) | Visual judgment on live preview | 52.3-03 UAT row 6 — PASS 2026-09-19 |

---

## Validation Audit 2026-09-20

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Audit notes: all 8 Per-Task Map rows verified against the live tree — required cases exist at the cited lines and the 4-file targeted run (`exportEngine.paintEnumeration` + `frameMap` + `exportRenderer` + `playbackEngine`) reports 65 passed | 26 todo (pre-existing todos outside the acceptance harness; zero `it.todo(` in the paintEnumeration file). D-09 reclassified manual-only per the Wave-0 optional decision with UAT row 5 evidence. Full-suite gate (215 files / 4002 passed, tsc, cargo, chunk 1,336.25 kB ≤ 1340 kB) recorded in 52.3-03-SUMMARY at HEAD 6f38f92e.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-09-20 — zero gaps; D-09 manual-only by design with UAT evidence
