---
phase: 52.3-paint-content-export-per-frame-compositor-enumeration
verified: 2026-09-19T20:25:00Z
status: passed
score: 14/14 must-haves verified
covered_files:
  - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-01-PLAN.md
  - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-01-SUMMARY.md
  - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-02-PLAN.md
  - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-02-SUMMARY.md
  - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-03-PLAN.md
  - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-03-SUMMARY.md
  - app/src/components/Preview.tsx
  - app/src/components/export/FormatSelector.tsx
  - app/src/components/layout/CanvasArea.tsx
  - app/src/lib/exportEngine.loops.test.ts
  - app/src/lib/exportEngine.paintEnumeration.test.ts
  - app/src/lib/exportEngine.test.ts
  - app/src/lib/exportRenderer.test.ts
  - app/src/lib/exportRenderer.ts
  - app/src/lib/frameMap.test.ts
  - app/src/lib/frameMap.ts
  - app/src/lib/playbackEngine.test.ts
  - app/src/lib/previewRenderer.test.ts
  - app/src/lib/previewRenderer.ts
  - app/src/types/timeline.ts
covered_digest: "v1:sha256:5bd0e77f68ac9a69d54fb05ffe4e9d6004c0efdd11d4b64a6df6a00a20eade65"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 52.3: Paint content export — per-frame compositor enumeration — Verification Report

**Phase Goal:** Any project containing paint exports its painted frames — PNG sequence, paint-only and mixed projects alike, both canvas orientations. Exporting a paint-only project renders every frame of the export range through the compositor; the "No frames to export (timeline is empty)" refusal becomes unreachable for paint content.
**Verified:** 2026-09-19T20:25:00Z (HEAD 3e84c120)
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Must-haves merged from the ROADMAP acceptance block (rows 510-513) and the three plans' frontmatter `must_haves.truths`. Post-SUMMARY CR-01 event folds into truth 7.

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | AC-C: paint-only fx (inFrame 0/outFrame 5, keys 0..4, no content) enumerates exactly 5 dense FrameEntry objects (D-04/D-05) | ✓ VERIFIED | Case C integration test passes (exportEngine.paintEnumeration.test.ts:287); enumeration branch at frameMap.ts:58-71 gated on `entries.length === 0`; 6-case unit block at frameMap.test.ts:423 passes |
| 2 | AC-D: startExport on a paint-only project completes with 5 rendered frames; the refusal is unreachable for paint content (D-10) | ✓ VERIFIED | Case D + motion-blur variant pass (paintEnumeration.test.ts:297, :360); refusal at exportEngine.ts:152-156 fires only on `fm.length === 0`, unreachable once frameMap enumerates paint |
| 3 | AC-CLEAR: every no-content frame starts from clean pixels — one top-level identity-transform clearRect gated on !hasContentEntry (D-06); content frames byte-identical | ✓ VERIFIED | exportRenderer.ts:164-174 (gate on textually unchanged `!hasContentEntry`, save/setTransform/clearRect/restore shape); AC-CLEAR describe (exportRenderer.test.ts:590-686) passes — fx-owned entry, empty fm, content byte-identical guard |
| 4 | AC-MIX: content projects unchanged — Controls A/B green; mixed frames stay content entries (D-02); tail-pad hold preserved (D-03) | ✓ VERIFIED | Controls A/B pass in-file; enumeration branch structurally cannot fire with content present (`entries.length === 0` guard); full suite re-run by verifier: 215 files, 4003 passed \| 1 skipped \| 101 todo, exit 0 |
| 5 | AC-E: fx-active selectedSequenceOnly exports that fx's paint entries to completion (D-08); genuinely empty timeline still refuses with locked copy (D-10) | ✓ VERIFIED | Case E (paintEnumeration.test.ts:309), Case E2 mixed refusal pin (:325), D-10 negative (:350) all pass; the existing `fm.filter(e => e.sequenceId === activeId)` predicate at exportEngine.ts:148 is textually unchanged (D-08 mechanism honored literally) |
| 6 | Gap frames (inside N, outside every span) carry kind 'gap' with sequenceId '' and export transparent (D-07) | ✓ VERIFIED | frameMap.ts:68 pushes GapFrameEntry with `sequenceId: ''`; frameMap.test.ts:456-458 asserts gap entries 0-2 before an offset fx; empty-fm AC-CLEAR case confirms clean (transparent) pixels |
| 7 | AC-E robustness: fx at inFrame > 0 with selectedSequenceOnly renders its paint on every filtered frame AND preloads its decodes — never an all-transparent export (Pitfall 1 + CR-01) | ✓ VERIFIED | Render half: `overlayGlobalFrame = entry ? entry.globalFrame : frameIndex` at exportRenderer.ts:162, pinned by 'fx overlay gates on entry.globalFrame (Pitfall 1)' (:507). Preload half (post-SUMMARY CR-01): collectExportPhysicPaintFrameSources overlay leg at :84-102 iterates entries and gates on `entry.globalFrame` (comment cites CR-01); CR-01 test (:761) passes in isolation (`vitest run -t "CR-01"` — 1 passed) |
| 8 | The overlay leg's gates and fx-local frame math read the entry's own globalFrame (D-08) | ✓ VERIFIED | Gates at exportRenderer.ts:339-340, local-frame derivations at :344 (overlayLocalFrame) and :371 (fxLocalFrame) all read `overlayGlobalFrame`; the content branch's positional seqStart walk is textually untouched |
| 9 | D-09: the Selected-sequence-only info line shows the filtered entry count an fx active actually exports — never the always-zero keyPhotos sum | ✓ VERIFIED | FormatSelector.tsx:48 derives `activeSeqFrameCount` from `frameMap.value.filter(e => e.sequenceId === activeSeq.id).length` (the same predicate as exportEngine.ts:148); rendered at :97; the keyPhotos summation is gone. Rendered-text adequacy confirmed by user on UAT Row 5 |
| 10 | Playback entering a paint frame activates the owning fx sequence (Pitfall 3) — pinned as deliberate, D-08-consistent | ✓ VERIFIED | 'playback into a paint frame activates the owning fx sequence' passes against the REAL sequenceStore (playbackEngine.test.ts); playbackEngine.ts reads only `entry.sequenceId` (:193-197, :397) — kind-agnostic as planned |
| 11 | AC-UAT-1: the §7-pair project (31 keys) exports its full PNG range; frames visually match the on-canvas composite | ✓ VERIFIED | User UAT 2026-09-19, Row 1 pass (52.3-03-SUMMARY.md); user verbatim: "All work. I tried different formats, resolution, x0.5, x1 and x2, all fine." |
| 12 | AC-UAT-2: a fresh paint-only project in EACH orientation exports with no empty-timeline refusal | ✓ VERIFIED | User UAT Rows 2 (HD 1920×1080) and 3 (HD Vertical) pass |
| 13 | AC-UAT-3: a mixed project exports both sources with the D-03 tail-hold intact | ✓ VERIFIED | User UAT Row 4 pass |
| 14 | Gates: full vitest suite green, tsc clean, cargo clean, chunk budget 1340 kB holds | ✓ VERIFIED | Re-run independently by verifier at HEAD 3e84c120: vitest 4003 passed \| 1 skipped \| 101 todo (exit 0); `tsc --noEmit` exit 0; production build exit 0 with main chunk 1,336.26 kB ≤ 1340 kB. Cargo: zero Rust diff since the 52.3-03 gate HEAD 6f38f92e (`git diff --stat 6f38f92e..HEAD -- app/src-tauri/**` empty) — untouched surface, gate carries |

**Score:** 14/14 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `app/src/types/timeline.ts` | FrameEntry discriminated union (D-01) | ✓ VERIFIED | ContentFrameEntry (:29-39, kind 'content' + legacy fields), PaintFrameEntry (:44-49, kind/globalFrame/sequenceId/layerId, NO keyPhotoId/imageId), GapFrameEntry (:54-58), union alias at :60 |
| `app/src/lib/frameMap.ts` | Paint enumeration branch + efxPaintVersion subscription | ✓ VERIFIED | Branch at :58-71 (owner-resolution loop, coverage predicate shared with overlay leg); `void efxPaintVersion.value` at :21 with Pitfall-2 comment; `kind: 'content'` on construction literal at :31 (tail pad inherits); N law untouched |
| `app/src/lib/exportRenderer.ts` | D-06 clear, overlayGlobalFrame, narrowed preload collect, CR-01 entry-driven preload | ✓ VERIFIED | Clear at :164-174; overlayGlobalFrame at :162; preload imageId collect narrowed to content-kind at :479; CR-01 overlay leg at :84-102 |
| `app/src/lib/previewRenderer.ts`, `Preview.tsx`, `CanvasArea.tsx` | Content-kind narrowing at every FrameEntry field read | ✓ VERIFIED | previewRenderer.ts:334, :484, :634; Preview.tsx:88; CanvasArea.tsx:56 — tsc exit 0 proves exhaustiveness |
| `app/src/components/export/FormatSelector.tsx` | D-09 info-line count from frameMap filter | ✓ VERIFIED | frameMap import :7, derivation :48, render :97 |
| `exportEngine.paintEnumeration.test.ts` | Full acceptance matrix green, zero parked todos | ✓ VERIFIED | 8 tests pass (Cases C/D/E/E2 + D-10 negative + motion-blur variant + Controls A/B); zero `it.todo(` in file |
| `frameMap.test.ts` / `exportRenderer.test.ts` / `playbackEngine.test.ts` | Enumeration units, AC-CLEAR, Pitfall-1/CR-01/Pitfall-3 pins | ✓ VERIFIED | frameMap 23 pass; exportRenderer 41 pass (19 pre-existing todos); playbackEngine 19 pass (7 skipped) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| frameMap computed | exportEngine.startExport | paint entries carry owning fx sequenceId (frameMap.ts:66) so the existing filter at exportEngine.ts:148 matches them (D-08) | ✓ WIRED | Case E integration test exercises the real computed → real startExport path |
| enumeration coverage predicate | overlay-leg gate predicate | both call `getTimelineOverlaySequenceOutFrame` (frameMap.ts:63; exportRenderer.ts:93, :340) — anti-drift lock | ✓ WIRED | Same function at all three sites, grep-confirmed |
| renderGlobalFrame hasContentEntry gate | CMP-01 flattened raster | paint/gap entries skip the content branch (:151 gate; fx id → `seq.kind === 'fx'` → false; gap '' id → seq undefined → false); fx overlay leg draws via PreviewRenderer (:392) | ✓ WIRED | UAT visual-match rows + hydrated-paint-only render case (exportRenderer.test.ts:462) |
| preload overlay leg (CR-01) | fx-local decode collection | entry-driven loop gates `entry.globalFrame` against [inFrame, outFrame) (:96-100) | ✓ WIRED | CR-01 test asserts collected + preloaded frames [0..4] for a rebased inFrame-5 selection |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| frameMap paint branch | entries[] | sequenceStore.sequences + getTimelineRequiredFrameCount + getTimelineOverlaySequenceOutFrame + getLayerId | Yes — real stores, real N law | ✓ FLOWING |
| exportEngine total | fm.length | frameMap.peek() (filtered per D-08) | Yes | ✓ FLOWING |
| FormatSelector info count | activeSeqFrameCount | frameMap.value.filter (same predicate as export) | Yes | ✓ FLOWING |
| renderGlobalFrame overlay leg | overlayGlobalFrame | entry.globalFrame (internally constructed) | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Acceptance matrix green | `vitest run src/lib/exportRenderer.test.ts src/lib/exportEngine.paintEnumeration.test.ts` | 2 files, 30 passed \| 19 todo (pre-existing), exit 0 | ✓ PASS |
| Enumeration units + playback pin | `vitest run src/lib/frameMap.test.ts src/lib/playbackEngine.test.ts` | 2 files, 35 passed \| 7 todo, exit 0 | ✓ PASS |
| CR-01 regression test (single named test) | `vitest run src/lib/exportRenderer.test.ts -t "CR-01"` | 1 passed, exit 0 | ✓ PASS |
| Full suite (one run) | `vitest run` | 215 files, 4003 passed \| 1 skipped \| 101 todo, exit 0 | ✓ PASS |
| Type gate | `tsc --noEmit` | exit 0 | ✓ PASS |
| Build + chunk budget | `pnpm run build` | exit 0; main chunk 1,336.26 kB ≤ 1340 kB | ✓ PASS |
| Cargo surface | `git diff --stat 6f38f92e..HEAD -- app/src-tauri/**` | empty — untouched since gate | ✓ PASS (carried) |

### Requirements Coverage

Phase 52.3 has no formal REQ-IDs in REQUIREMENTS.md (grep-confirmed empty); the de-facto contracts (AC-rows + D-01..D-10 per 52.3-CONTEXT.md) are the traceability surface. Every plan-frontmatter ID is accounted for in the corresponding SUMMARY `requirements-completed` — no orphans either direction.

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| AC-C | 52.3-01 | 5 dense paint entries for fx 0..5 keys 0..4 | ✓ SATISFIED | Case C + frameMap units |
| AC-D | 52.3-01 | Paint-only export completes; refusal unreachable | ✓ SATISFIED | Case D + motion-blur variant |
| AC-E | 52.3-01, 52.3-02 | D-08 selected-fx export; robust at any inFrame | ✓ SATISFIED | Case E + Pitfall-1 pin + CR-01 fix/test |
| AC-CLEAR | 52.3-01 | D-06 clean pixels on no-content frames | ✓ SATISFIED | AC-CLEAR describe (3 cases) |
| AC-MIX | 52.3-01 | Content projects byte-identical (D-02/D-03) | ✓ SATISFIED | Controls A/B + full suite 4003 green |
| AC-UAT | 52.3-03 | 7-row native UAT | ✓ SATISFIED | 7/7 rows user-signed 2026-09-19 |
| D-01..D-10 | 52.3-01/02 | Locked design decisions | ✓ SATISFIED | Union (D-01), content-wins (D-02), tail-hold (D-03), N law (D-04), dense map (D-05), clear (D-06), transparent gaps (D-07), filter semantics (D-08), info line (D-09), refusal scope (D-10) — each pinned in code + tests above |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | Debt-marker scan (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) across all 7 phase-modified production files: zero hits; no empty-implementation or hardcoded-empty stub patterns in the enumeration/render path (test fixtures excluded) | — | — |

### Code-Review Disposition Record (52.3-REVIEW.md — post-SUMMARY events)

- **CR-01 (critical) — export preload missed fx frames for rebased selected exports with inFrame > 0:** FIXED. RED commit 50ecd5e4 (test-only, +55 lines, 2026-09-19T20:13:21Z) precedes fix commit 3e84c120 (2026-09-19T20:14:27Z). The preload overlay leg (exportRenderer.ts:84-102) now iterates entries and gates on `entry.globalFrame` — the same predicate as the render gate. The CR-01 test passes in isolation. **WR-04(a) is closed by this test; WR-04(b)** (content-at-global->0 + riding fx overlay preload) has no dedicated test but is covered by construction — the same entry-driven loop handles it, and the render half is pinned by 'selected export keeps later-sequence overlays riding' (:541). Recorded as informational, not a gap.
- **WR-02 (D-06 clear on live Preview → transient cold-decode flash):** DISPOSITIONED AS ACCEPTED BEHAVIOR by the orchestrator with user approval — the user explicitly passed the current preview surface in UAT Row 6 (no stale ghost pixels), the flash is transient (one decode + ≤400ms throttle), and gating the clear would resurrect the ghost D-06 exists to kill. Recorded here so it does not resurface as an open finding.
- **WR-01 (gap entries clearing the active sequence on scrub) and WR-03 (single-owner enumeration vs composite-all rendering for three selected-export shapes):** behavior/design observations. The user has not been asked yet — recorded as follow-up candidates for a later phase, NOT phase-52.3 gaps. Neither affects the phase goal (paint export correctness); Case E2 deliberately pins the mixed refusal per D-02/D-03.
- **WR-05, IN-01..IN-05:** pre-existing or informational; IN-03 (render-body writes) and IN-01 (fx frame arg) predate the phase and were not introduced by it.

### Human Verification Required

None pending. The phase's blocking human checkpoint (7-row native UAT, 52.3-03 Task 2) was executed and signed off by the user on 2026-09-19 — 7/7 rows pass, plus an extra matrix of formats, resolutions, and 0.5×/1×/2× scales. The D-09 info-line rendered-text check (52.3-02 coverage D3, classified human_judgment) was confirmed on UAT Row 5. The visual WYSIWYG bar (the only check automation cannot perform) is therefore already evidenced.

### Gaps Summary

No gaps. All 14 must-have truths are verified against the codebase: the FrameEntry discriminated union is fail-closed (tsc exit 0), the paint enumeration branch enumerates dense [0, N) entries through the real computed, the D-06 clear is wired and pinned, the D-08/D-10 semantics are integration-tested end-to-end, the post-review CR-01 preload corruption is fixed with a RED-first regression test that passes, and the 7-row native UAT is user-signed. All four automated gates were re-run by the verifier at HEAD 3e84c120 and pass. The phase goal — any project containing paint exports its painted frames, refusal unreachable for paint content — is achieved.

---

_Verified: 2026-09-19T20:25:00Z_
_Verifier: Claude (gsd-verifier)_
