---
phase: 52.3-paint-content-export-per-frame-compositor-enumeration
plan: 03
type: execute
wave: 3
depends_on: ["52.3-02"]
files_modified: []
autonomous: false
requirements: [AC-UAT]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "AC-UAT-1: the §7-pair project (31 keys) exports its full PNG range; exported frames visually match the on-canvas composite"
    - "AC-UAT-2: a fresh paint-only project in EACH orientation (HD horizontal + HD Vertical) exports with no empty-timeline refusal"
    - "AC-UAT-3: a mixed project (content clip + paint beyond the clip span) exports both sources with the D-03 tail-hold intact"
    - "Gates: full vitest suite green, tsc clean, cargo clean, chunk budget 1340 kB holds"
  artifacts:
    - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-03-SUMMARY.md — gate results + per-row UAT evidence record
  key_links: []
  prohibitions:
    - "watch-mode vitest (vitest run only, per CLAUDE.md)"
    - "one-off test-config changes (no test config hacks)"
    - "agent-launched server or dev build (the user drives all native runs)"
---

<objective>
Prove the phase on the real surface: run every automated gate, then drive the blocking native UAT rows that automated tests cannot cover — the user's real §7-pair project, fresh paint-only projects in both canvas orientations, a mixed project, and the D-09/D-10 UI behaviors — with "exported frames visually match the on-canvas composite" as the acceptance bar on every row (WYSIWYG law, D-05).

Purpose: Phase 52.3 blocks Phase 53 acceptance; this plan is the evidence gate that paint export works on real projects, not just in the harness.
Output: gate results plus a signed per-row UAT record in the plan SUMMARY.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Full automated gates: suite, tsc, cargo, chunk budget</name>
  <files>none (verification only — no source files modified)</files>
  <read_first>
    - app/vite.config.ts (:240-258 — chunk budget 1340 kB and its measurement history)
    - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-VALIDATION.md (sampling-rate and phase-gate contract)
  </read_first>
  <action>
    Run the four phase gates in order from the repo root, per the VALIDATION phase-gate row: (1) the full vitest suite via the config.json workflow.test_command; (2) tsc --noEmit; (3) the cargo test surface — untouched by this phase, must stay green exactly as shipped; (4) the production build, confirming the main chunk stays within the 1340 kB budget at app/vite.config.ts:257 (this phase adds only a few dozen lines of in-repo code — no budget raise is expected or permitted without the documented measurement pattern). If any gate fails, halt the plan and report; do not proceed to the UAT checkpoint on red gates. Do not launch the dev server or any native build — that is the user's side (CLAUDE.md).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or 'failed' with a non-zero count in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any 'error TS' line in output</fails_when>
    <automated>cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
    <fails_when>non-zero exit, or 'FAILED' in the test summary</fails_when>
    <automated>pnpm --filter efx-motion-editor run build 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, any build error output, or any emitted main-chunk line reporting above 1340 kB</fails_when>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0 (zero failures across the repo)
    - tsc exits 0 with zero error lines
    - cargo test exits 0 on the untouched Rust surface
    - Build succeeds and the reported main-chunk size is at or under 1340 kB
  </acceptance_criteria>
  <done>All four automated gates are green and recorded for the SUMMARY; the plan proceeds to the blocking UAT checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Native UAT — paint export acceptance rows (AC-UAT)</name>
  <files>none (user-driven verification; evidence recorded in the plan SUMMARY)</files>
  <precondition>The user runs the current dev build (Claude never launches the server, per CLAUDE.md); the §7-pair project at ~/Desktop/efx-motion-editor-project-test/v1.0.0.mce (31 keys, per the 52.2-16 record) is available on disk.</precondition>
  <read_first>
    - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-CONTEXT.md (acceptance rows + specifics: WYSIWYG is the design law, D-05)
    - .planning/ROADMAP.md (Phase 52.3 Acceptance block: native UAT rows)
  </read_first>
  <what-built>
    Phase 52.3 complete implementation: the FrameEntry discriminated union (content/paint/gap, D-01) with the paint enumeration branch in frameMap (D-04/D-05), the D-06 top-level canvas clear, the Pitfall-1 overlay-gate fix (entry.globalFrame), the D-09 info-line count, and the fully green acceptance matrix (Cases C/D/E + E2 + D-10 negative + AC-CLEAR + enumeration units). All four automated gates passed in Task 1 of this plan.
  </what-built>
  <how-to-verify>
    Present the user with the seven UAT rows below and wait for explicit per-row pass/fail evidence. Every row's bar: exported frames visually match the on-canvas composite (D-05 WYSIWYG law). Record each row's outcome and evidence (export folder path, PNG count, visual comparison notes) for the SUMMARY.

    Row 1 — §7-pair full range (headline row): open ~/Desktop/efx-motion-editor-project-test/v1.0.0.mce, export PNG sequence. Expected: PNG count equals the timeline's total frame count (31-key paint extent included); no 'No frames to export (timeline is empty)' refusal; first/middle/last exported frames match the canvas.
    Row 2 — fresh paint-only, horizontal (HD 1920×1080): new project, add a Physic Paint fx layer, paint keys on several frames, export. Expected: PNG count matches the timeline total; painted content present in the PNGs; no refusal.
    Row 3 — fresh paint-only, vertical (HD Vertical): same as Row 2 in the vertical orientation (both canvas orientations must export, per the phase goal).
    Row 4 — mixed project: a content clip plus an fx paint layer whose keys extend beyond the clip span, export. Expected: frames beyond the clip show the clip's last frame beneath the paint (D-03 tail-hold); both sources composite; visual match.
    Row 5 — selected sequence only with the fx sequence active (paint-only project): enable 'Selected sequence only' and export. Expected: the export contains the fx's frames over clean pixels (D-08); the info line shows the real frame count, not 0 (D-09).
    Row 6 — transparent-fond preview ghost check: paint-only project with a transparent document fond; scrub the playhead across painted and empty frames in the main preview. Expected: no stale ghost pixels on empty frames (the shared-function D-06 clear, RESEARCH Assumption A3).
    Row 7 — playback activation callout (Pitfall 3): play the paint-only project. Expected and correct: the sidebar activates the fx row as the playhead enters paint frames — deliberate, D-08-consistent, not a bug.
  </how-to-verify>
  <resume-signal>Reply with per-row pass/fail plus evidence (export folder paths, PNG counts, visual notes). All 7 rows passing closes the checkpoint and the phase proceeds to /gsd-verify-work; any failing row routes back as a gap with the row's arrange and observed behavior.</resume-signal>
  <verify>
    <human-check>User confirms each of the 7 rows passes with exported-PNG evidence and visual comparison to the on-canvas composite; any failing row routes back as a gap with the row's arrange and observed behavior.</human-check>
  </verify>
  <acceptance_criteria>
    - All 7 rows explicitly confirmed passing by the user
    - Row 1's PNG count matches the §7-pair timeline total; Rows 2-3 show no empty-timeline refusal in either orientation
    - The UAT evidence (per-row outcome + export paths + counts) is recorded in 52.3-03-SUMMARY.md
  </acceptance_criteria>
  <done>All seven native UAT rows pass with recorded evidence; Phase 52.3's paint-export acceptance is proven on real projects and the phase is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: no production code changes; UAT exercises the shipped exportWritePng IPC to user-chosen directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.3-05 | Tampering | UAT evidence integrity (a stale dev build validating old code) | low | mitigate | UAT runs on the current dev build after Task 1's gates pass on the same working tree; the SUMMARY records the HEAD commit alongside the row evidence |
| T-52.3-SC | Tampering | npm/pip/cargo installs | — | accept | Not triggered: zero new packages this phase |
</threat_model>

<verification>
- Task 1: all four automated gates green (suite, tsc, cargo, build + 1340 kB chunk budget)
- Task 2: all seven native UAT rows confirmed by the user with evidence
</verification>

<success_criteria>
- The phase's ROADMAP acceptance block is satisfied: parked contracts green (Plans 01-02), paint-only exports in both orientations, mixed export with tail-hold, §7-pair full range, visual match to canvas, all gates green
- The UAT record stands as the Phase 53 unblock evidence
</success_criteria>

## Artifacts this phase produces (Plan 03 scope)

New symbols: none.
New file paths: `.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-03-SUMMARY.md` (verification record only).

<output>
Create `.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-03-SUMMARY.md` when done, recording the four gate results, the HEAD commit, and per-row UAT evidence.
</output>
