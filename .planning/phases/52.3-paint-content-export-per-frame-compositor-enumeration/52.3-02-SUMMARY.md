---
phase: 52.3-paint-content-export-per-frame-compositor-enumeration
plan: 02
subsystem: export
tags: [export, overlay-gating, positional-vs-global, pitfall-1, d-08, d-09, format-selector, playback-activation, pitfall-3, tdd, vitest]

requires:
  - phase: 52.3-01
    provides: FrameEntry discriminated union + dense paint enumeration (entries carry globalFrame; selectedSequenceOnly filter matches fx paint entries) — this plan hardens the render-side gating that enumeration exposes
provides:
  - "overlayGlobalFrame derivation in renderGlobalFrame — overlay-leg inFrame/outFrame gates and overlayLocalFrame/fxLocalFrame math read the entry's own globalFrame, never the positional frameIndex (Pitfall 1 closed, D-08 correct at any inFrame)"
  - "Shared-fix repair of the pre-existing content-selected overlay-drop quirk (RESEARCH Open Question 1) with a regression pin"
  - "D-09: FormatSelector info line renders activeSeqFrameCount — the exact frameMap filter count the export filter will process (the always-zero keyPhotos summation is gone)"
  - "Pitfall-3 pin: playback into a paint frame activates the owning fx sequence through the real sequenceStore.setActive — deliberate, D-08-consistent"
affects: [52.3-03, 53, export, preview]

actuals:
  tokens: 2894
  tasks: 2
  commits: 3
  plan_head_before: 8919baba0a31eb5c944a048987e7a1e5433bab12

tech-stack:
  added: []
  patterns:
    - "Positional-vs-global discipline in renderGlobalFrame: entry-derived overlayGlobalFrame for all overlay-leg reads; the content branch's seqStart walk stays genuinely positional — the two frames of reference are now textually distinct"
    - "UI count truthfulness: info lines source counts from the same frameMap filter the export engine applies (D-09), never from sequence-shape summations that lie for non-content kinds"

key-files:
  created:
    - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-02-RED-EVIDENCE.json
  modified:
    - app/src/lib/exportRenderer.ts
    - app/src/lib/exportRenderer.test.ts
    - app/src/components/export/FormatSelector.tsx
    - app/src/lib/playbackEngine.test.ts

key-decisions:
  - "overlayGlobalFrame = entry ? entry.globalFrame : frameIndex derived once immediately after the entry read; used by both overlay gates and both local-frame derivations; renderFrame's globalFrame pass-through arg and the motion-blur sub-frame flooring deliberately untouched (full exports byte-identical: positional === global there)"
  - "Shared fix adopted for the content-selected overlay-drop quirk (RESEARCH Open Question 1): one identical line either way, pinned by 'selected export keeps later-sequence overlays riding'"
  - "Pitfall-3 playback activation is pinned as DELIBERATE: dense enumeration makes paint frames activatable via the unchanged syncActiveSequence path; setActive clearing selectedKeyPhotoId is setActive's own tested behavior, out of scope"

patterns-established:
  - "Selected-export re-basing contract: when fm is positionally re-based by a filter, any per-entry true-global read must come from entry.globalFrame — the positional param is only valid as a fallback for empty-map callers (preview)"

requirements-completed: [AC-E, D-08, D-09]

coverage:
  - id: D1
    description: "Pitfall 1 closed: a selected-fx export of an inFrame > 0 sequence renders its paint on every filtered frame (never all-transparent) — the overlay leg gates and fx-local math read entry.globalFrame (AC-E robustness, D-08)"
    requirement: AC-E
    verification:
      - kind: unit
        ref: "app/src/lib/exportRenderer.test.ts#fx overlay gates on entry.globalFrame (Pitfall 1)"
        status: pass
      - kind: other
        ref: "pnpm --filter efx-motion-editor exec vitest run src/lib/exportRenderer.test.ts src/lib/playbackEngine.test.ts src/lib/exportEngine.paintEnumeration.test.ts — 3 files, 41 passed | 26 todo, exit 0 (2026-09-19)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Positional/global divergence cannot silently drop overlays in ANY filtered export: the pre-existing content-selected overlay-drop quirk is repaired by the same overlayGlobalFrame line and pinned (RESEARCH Open Question 1 shared fix, D-08)"
    requirement: D-08
    verification:
      - kind: unit
        ref: "app/src/lib/exportRenderer.test.ts#selected export keeps later-sequence overlays riding"
        status: pass
    human_judgment: false
  - id: D3
    description: "D-09: the Selected-sequence-only info line shows the filtered entry count an fx active actually exports — sourced from frameMap.value.filter(e => e.sequenceId === activeSeq.id), the same predicate the export filter at exportEngine.ts:148 applies; the always-zero keyPhotos summation is removed"
    requirement: D-09
    verification:
      - kind: other
        ref: "grep -c 'frameMap.value.filter(e => e.sequenceId === activeSeq.id)' app/src/components/export/FormatSelector.tsx → 1"
        status: pass
      - kind: other
        ref: "pnpm --filter efx-motion-editor exec tsc --noEmit → exit 0"
        status: pass
    human_judgment: true
    rationale: "No FormatSelector component test exists today (RESEARCH Wave-0 note); source wiring and type safety are pinned above, but the rendered info-line text is a user-visible string whose adequacy is verified on the 52.3-03 native UAT row (D-09 info-line row), per the plan's own verification split"
  - id: D4
    description: "Pitfall 3 pinned as deliberate: playback entering a paint frame activates the owning fx sequence via the real sequenceStore.setActive through the unchanged syncActiveSequence path (D-08-consistent)"
    verification:
      - kind: unit
        ref: "app/src/lib/playbackEngine.test.ts#playback into a paint frame activates the owning fx sequence"
        status: pass
    human_judgment: false

duration: 13min
completed: 2026-09-19
status: complete
---

# Phase 52.3 Plan 02: Selected-FX Export Hardening — Overlay-Gate GlobalFrame Fix + D-09 Info Line Summary

**A selected-sequence export of an fx sequence at any inFrame now renders its paint (Pitfall 1 closed: the overlay leg gates on the entry's own globalFrame, never the positional index — the same line repairs the pre-existing content-selected overlay-drop quirk), the FormatSelector info line tells the truth for fx actives (D-09), and playback activation on paint frames is pinned as deliberate D-08-consistent behavior (Pitfall 3) — TDD RED→GREEN honored, 41 tests green across the three files, tsc clean.**

## Performance

- **Duration:** 13 min
- **Started:** 2026-09-19T14:51:34Z
- **Completed:** 2026-09-19T15:05:07Z
- **Tasks:** 2
- **Files modified:** 4 (2 production, 2 test) + 1 planning artifact created (RED evidence)

## Accomplishments

- Pitfall 1 closed with two regression pins: `renderGlobalFrame` derives `overlayGlobalFrame = entry ? entry.globalFrame : frameIndex` immediately after the entry read (`exportRenderer.ts:152`) and the overlay leg reads it at the inFrame gate (:329), the `getTimelineOverlaySequenceOutFrame` gate (:330), and both local-frame derivations (:334 overlayLocalFrame, :361 fxLocalFrame). Untouched per plan: the content branch's positional seqStart walk, the renderFrame `globalFrame` pass-through args, and the motion-blur sub-frame flooring — full exports are byte-identical (positional === global there), confirmed by every pre-existing exportRenderer.test.ts case staying green in the same run.
- RESEARCH Open Question 1 resolved as planned: the shared one-line fix also repairs the pre-existing content-selected overlay-drop quirk, pinned by 'selected export keeps later-sequence overlays riding' (positional 0 → globalFrame 5, overlay spanning 5..10 rides).
- D-09: `FormatSelector.tsx` info line renders `activeSeqFrameCount` — `frameMap.value.filter(e => e.sequenceId === activeSeq.id).length`, the exact count the export filter at exportEngine.ts:148 will process; the always-zero `activeSeq.keyPhotos.reduce(...)` summation is gone. Narrow `.value` read in the component body per efx-preact-reactivity; no useState, no render-body writes, no new effect.
- Pitfall 3 pinned: 'playback into a paint frame activates the owning fx sequence' drives the same `scrubToFrame` entry point the AUDIO cases drive against the REAL sequenceStore/uiStore/timelineStore (only physicPaintBridge/audioEngine/frameMap mocked) and asserts `setActive` observed 'fx-p' plus `activeSequenceId.value === 'fx-p'`.

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Pitfall-1 pins** — `4ec72e75` (test)
2. **Task 1 (GREEN): overlayGlobalFrame fix** — `507a628e` (feat)
3. **Task 2: D-09 info line + Pitfall-3 playback activation pin** — `0d1125e3` (feat)

## Files Created/Modified

- `app/src/lib/exportRenderer.ts` — `overlayGlobalFrame` derivation after the entry read; overlay-leg gates (:329-330) and local-frame derivations (:334, :361) read it
- `app/src/lib/exportRenderer.test.ts` — two Pitfall-1 pins in the renderGlobalFrame describe ('fx overlay gates on entry.globalFrame (Pitfall 1)', 'selected export keeps later-sequence overlays riding')
- `app/src/components/export/FormatSelector.tsx` — frameMap import; `activeSeqFrameCount` derivation; info line renders it
- `app/src/lib/playbackEngine.test.ts` — sequenceStore/FrameEntry imports; new 'playbackEngine paint-frame activation (52.3-02, Pitfall 3)' describe with the activation pin and frameMap-mock restore hygiene
- `.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-02-RED-EVIDENCE.json` — persisted RED evidence record (verbatim vitest output; rides the docs commit per the 52.3-01 precedent)

## Decisions Made

- `overlayGlobalFrame` placement and scope: one derivation immediately after the entry read, consumed by exactly the four overlay-leg reads the plan named; the `frameIndex` fallback preserves preview/empty-map callers (the existing empty-frameMap tests at positional 41 stay byte-identical). — per plan action, RESEARCH Code Example 4.
- The content-selected quirk repair rides this plan as a shared fix (one identical line either way) with its own pin — RESEARCH Open Question 1 disposition adopted at planning time, not relitigated here.
- The Pitfall-3 test drives `scrubToFrame` (the same public funnel the AUDIO cases exercise) with `audioStore.tracks` empty so the snippet path is silent; the frameMap mock signal is restored to `[]` in afterEach so no paint entries leak into sibling describes.
- RED-EVIDENCE.json rides the final docs commit (not the RED commit), matching the 52.3-01 file placement.

## Deviations from Plan

None — plan executed exactly as written. (Process note: `check tdd-red-evidence` returned INVALID_RED / zero_tests_discovered because the classifier parses node --test TAP summaries which vitest never emits — the documented 52.2-01 / 260919-azh tooling limitation cited in this plan's tdd.md embed. RED evidence is the plan-defined alternative: the verbatim pre-implementation failing run of the exact target tests, persisted in 52.3-02-RED-EVIDENCE.json. Tooling limitation, not a plan deviation.)

## TDD Gate Compliance

- RED commit `4ec72e75` (`test(52.3-02): ...`) exists, touches only `app/src/lib/exportRenderer.test.ts`, and PRECEDES the GREEN commit `507a628e` (`feat(52.3-02): ...`) — gate order validated via `git log --oneline -E --grep`.
- RED was intentional: exactly the two TARGET tests named by the plan failed in one run, both on the planned behavior assertion — 'fx overlay gates on entry.globalFrame (Pitfall 1)' `expected undefined to be defined` (exportRenderer.test.ts:534) and 'selected export keeps later-sequence overlays riding' `expected undefined to be defined` (:579) — while all 19 other tests in the file passed and 19 todo stayed parked. Exit code 1.
- RED evidence not machine-verified (`check tdd-red-evidence` → INVALID_RED / zero_tests_discovered on parser grounds; vitest emits no TAP `# tests`/`# fail` summary — same documented limitation as 52.2-01 and 52.3-01). Verbatim record: `.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-02-RED-EVIDENCE.json`.
- REFACTOR commit absent (optional; the GREEN diff is one derivation + four read-site swaps — no cleanup needed).
- Task 2 was `type="auto"` (not tdd) per the plan; its single feat commit `0d1125e3` carries the D-09 wiring and the Pitfall-3 pin together as planned.

## Issues Encountered

None — both tasks landed on the first attempt; pre-existing tests stayed green throughout.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Ready for 52.3-03: plan-level gates (full suite, cargo, chunk budget) and the native UAT matrix — §7-pair 31 keys full range, fresh paint-only × 2 orientations, mixed project, visual match to canvas, and the D-09 info-line row.
- D-08 is now correct at any inFrame: the enumeration (52.3-01) and the render-side gating (this plan) read the same per-entry global truth, so the acceptance matrix's Case E shape generalizes to shifted fx sequences.
- The D-09 info-line deliverable (coverage D3) is classified human_judgment: its rendered-text adequacy is the 52.3-03 native UAT row; the source wiring and type safety are pinned here.

## Self-Check: PASSED

- Commit `4ec72e75` exists (`git log --oneline` — Task 1 RED, test-only)
- Commit `507a628e` exists (`git log --oneline` — Task 1 GREEN)
- Commit `0d1125e3` exists (`git log --oneline` — Task 2)
- File check: `app/src/lib/exportRenderer.ts` contains `overlayGlobalFrame` at 5 sites (derivation + 4 reads); `app/src/components/export/FormatSelector.tsx` contains the frameMap filter count (grep -c → 1)
- Plan verification: `pnpm --filter efx-motion-editor exec vitest run src/lib/exportRenderer.test.ts src/lib/playbackEngine.test.ts src/lib/exportEngine.paintEnumeration.test.ts` — 3 files passed, 41 passed | 26 todo, exit 0
- Type gate: `pnpm --filter efx-motion-editor exec tsc --noEmit` exit 0
- TDD gate: RED `4ec72e75` precedes GREEN `507a628e` in git log; REFACTOR absent (optional)
- Commit ledger measured: 3 commits over plan base `8919baba` (`git rev-list --count`)

---
*Phase: 52.3-paint-content-export-per-frame-compositor-enumeration*
*Completed: 2026-09-19*
