---
phase: "52.3"
slug: "paint-content-export-per-frame-compositor-enumeration"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: "2026-09-20"
---

# Phase 52.3 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| (none new) | Enumeration derives from validated in-memory stores (sequenceStore, physicPaintStore — parsed fail-closed at write time); PNG writes flow through the existing exportWritePng IPC to the user-chosen export directory; no new IPC commands, paths, or scopes introduced by Plans 01/02/03 | Frame pixels (in-memory → existing IPC → user-chosen dir) |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-52.3-01 | Tampering | PaintFrameEntry.sequenceId assignment (frameMap.ts branch) | low | mitigate | Construction-side only: `frameMap.ts:66` assigns `sequenceId: owner.id` from the owning fx sequence object in the same loop; the `kind` discriminant + the unchanged `hasContentEntry` gate (`exportRenderer.ts:151`, `seq.kind !== 'fx'`) keep paint entries out of the content branch even if ids collided | closed |
| T-52.3-02 | Denial of Service | Dense [0, N) entry array on pathological paint extent | low | accept | Existing bounds stand: roto capacity is clamped at write (`PHYSIC_PAINT_MAX_APPLY_FRAMES = 600`, `physicPaint.ts:371`, clamp test pinned at `physicPaint.test.ts:128`); N derives from the unchanged `getTimelineRequiredFrameCount` law the timeline already materializes for display — no new unbounded allocation | closed |
| T-52.3-03 | Information Disclosure (intra-document) | Stale-canvas ghost pixels exporting prior-frame content | medium | mitigate | D-06 top-level clear at `exportRenderer.ts:168-174` gated on `!hasContentEntry` (identity-transform `clearRect`, save/restore-wrapped); AC-CLEAR regression pin at `exportRenderer.test.ts:590` ('canvas clear lifecycle (D-06, AC-CLEAR)') | closed |
| T-52.3-04 | Tampering | Overlay-leg gating frame derivation (exportRenderer.ts) — a wrong derivation renders the wrong fx-local frame or drops overlays (output integrity) | low | mitigate | `overlayGlobalFrame = entry ? entry.globalFrame : frameIndex` derives only from the internally constructed entry (`exportRenderer.ts:162`); read by both overlay gates (:339-340) and both local-frame derivations (:344, :371); two RED pins lock the gate for both paint-selected and content-selected arranges (`exportRenderer.test.ts:507`, `:541`) | closed |
| T-52.3-05 | Tampering | UAT evidence integrity (a stale dev build validating old code) | low | mitigate | UAT ran on the current dev build after Plan-03 Task 1's gates passed on the same working tree; `52.3-03-SUMMARY.md` records HEAD `6f38f92e` alongside the per-row evidence table | closed |
| T-52.3-SC | Tampering | npm/pip/cargo installs | — | accept | Not triggered: zero new packages this phase (RESEARCH Package Legitimacy Audit: gate not applicable); empty `package.json` / lockfile diff vs plan base `12d5e085` | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-52.3-01 | T-52.3-02 | Dense [0, N) paint enumeration is bounded by the pre-existing write-time clamp (`PHYSIC_PAINT_MAX_APPLY_FRAMES = 600`) and the shared N law — the array the export path materializes is the same extent the timeline already renders; no new unbounded allocation surface introduced | plan-authored (52.3-01 threat model) | 2026-09-19 |
| AR-52.3-02 | T-52.3-SC | Supply-chain gate not triggered — zero new packages across Plans 01/02/03 | plan-authored (all three threat models) | 2026-09-19 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-20 | 6 | 6 | 0 | gsd-secure-phase (L1 grep-depth; asvs_level 1; register authored at plan time across 52.3-01/02/03) |

## Security Audit 2026-09-20

| Metric | Count |
|--------|-------|
| Threats found | 6 |
| Closed | 6 |
| Open | 0 |

Mitigation evidence (L1 grep-verified against HEAD on main):
- T-52.3-01: `frameMap.ts:66` (`kind: 'paint'` + `sequenceId: owner.id` in one construction literal); `exportRenderer.ts:151` (`hasContentEntry = !!seq && seq.kind !== 'fx'`, textually unchanged)
- T-52.3-02: `physicPaint.ts:371` bound + `physicPaint.test.ts:128` clamp pin
- T-52.3-03: `exportRenderer.ts:168-174` D-06 clear; `exportRenderer.test.ts:590` AC-CLEAR describe
- T-52.3-04: `exportRenderer.ts:162` derivation, gates at `:339-340`, local-frame math at `:344`/`:371`; pins at `exportRenderer.test.ts:507`/`:541`
- T-52.3-05: `52.3-03-SUMMARY.md` records HEAD `6f38f92e` with the 7/7 UAT evidence table
- T-52.3-SC: empty `package.json`/lockfile diff vs plan base `12d5e085`

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-20
