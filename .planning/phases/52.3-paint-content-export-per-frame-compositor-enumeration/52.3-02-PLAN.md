---
phase: 52.3-paint-content-export-per-frame-compositor-enumeration
plan: 02
type: execute
wave: 2
depends_on: ["52.3-01"]
files_modified:
  - app/src/lib/exportRenderer.ts
  - app/src/lib/exportRenderer.test.ts
  - app/src/components/export/FormatSelector.tsx
  - app/src/lib/playbackEngine.test.ts
autonomous: true
requirements: [AC-E, D-08, D-09]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "AC-E robustness: an fx sequence at inFrame > 0 with selectedSequenceOnly renders its paint on every filtered frame — never an all-transparent export (Pitfall 1 fix, D-08)"
    - "The overlay leg's gates and fx-local frame math read the entry's own globalFrame, so positional/global divergence in any filtered export cannot silently drop overlays (D-08)"
    - "D-09: the Selected-sequence-only info line shows the filtered entry count an fx active actually exports — never the always-zero keyPhotos sum"
    - "Playback entering a paint frame activates the owning fx sequence (Pitfall 3) — pinned as deliberate, D-08-consistent behavior"
  artifacts:
    - app/src/lib/exportRenderer.ts — overlayGlobalFrame derivation feeding the :305/:306 gates and :310/:337 local-frame math
    - app/src/components/export/FormatSelector.tsx — info-line count sourced from the frameMap filter (D-09)
    - app/src/lib/exportRenderer.test.ts — two Pitfall-1 pins (fx at inFrame > 0; content-selected overlay quirk)
    - app/src/lib/playbackEngine.test.ts — paint-frame activation pin
  key_links:
    - "renderGlobalFrame overlay gate -> entry.globalFrame (never the positional frameIndex when the two diverge)"
    - "FormatSelector info line -> the same frameMap.value.filter(e => e.sequenceId === activeSeq.id) count the export filter at exportEngine.ts:148 will process (D-09)"
  prohibitions:
    - "selectedSequenceOnly filter redesign or any fx-specific bypass in exportEngine.ts (D-08 locks the existing predicate)"
    - "changes to the content branch's positional seqStart walk at exportRenderer.ts:143-151 (genuinely positional)"
    - "resolution-policy or 1920 long-edge clamp changes (out of scope, v1.2.0)"
    - "useState or render-body signal writes in FormatSelector.tsx (efx-preact-reactivity law)"
---

<objective>
Harden the D-08 selected-fx export beyond the inFrame: 0 case: fix the positional-vs-global conflation in renderGlobalFrame's overlay leg (RESEARCH Pitfall 1 — without it, a selected-fx export of an inFrame > 0 sequence renders all-transparent), make the FormatSelector info line tell the truth for fx actives (D-09), and pin the playback-activation behavior that dense enumeration newly enables (Pitfall 3).

Purpose: Plan 01 wired enumeration; this plan makes the user-visible selected-export semantics correct in every arrange and stops the UI from lying about frame counts.
Output: the overlayGlobalFrame fix with two regression pins, the D-09 info line, and the playback activation pin.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-CONTEXT.md
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-RESEARCH.md
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-01-SUMMARY.md
@app/src/lib/exportRenderer.ts
@app/src/components/export/FormatSelector.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Pitfall-1: gate the fx overlay leg on entry.globalFrame (RED → GREEN)</name>
  <files>app/src/lib/exportRenderer.ts, app/src/lib/exportRenderer.test.ts</files>
  <read_first>
    - app/src/lib/exportRenderer.ts (renderGlobalFrame :123-363 — entry read :138, overlay leg :300-362, gates :305-306, local-frame derivations :310/:337)
    - app/src/lib/exportRenderer.test.ts (renderGlobalFrame describe + stub harness :406-498; the literal sites now carry kind: 'content' from Plan 01)
    - app/src/lib/exportEngine.ts (:241-296 — the positional render loop that passes the positional index as the globalFrame param; :502-506 resumeExport positional re-entry)
    - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-RESEARCH.md (Pitfall 1, Code Example 4, Open Question 1)
  </read_first>
  <behavior>
    - RED (a): fm = five paint-kind entries global 2..6, fx sequence inFrame 2 / outFrame 7 → pre-fix, positional frame 0 renders NO fx layers (gate skips: positional 0 < inFrame 2); post-fix, renderFrame receives the fx layers with fxLocalFrame 0
    - RED (b): fm = a second content sequence's entries global 5..9, fx overlay inFrame 5 / outFrame 10 → pre-fix, positional frame 0 drops the overlay; post-fix, the overlay renders (shared-fix pin for the pre-existing content-selected quirk, RESEARCH Open Question 1)
    - GREEN: both pins pass; every pre-existing exportRenderer.test.ts case stays green (full exports are unaffected because positional === global there)
  </behavior>
  <action>
    RED first (test-only commit): add two cases to exportRenderer.test.ts in the renderGlobalFrame describe. Case (a) 'fx overlay gates on entry.globalFrame (Pitfall 1)': build fm as five PaintFrameEntry literals (kind: 'paint', globalFrame 2..6, sequenceId 'fx-p', layerId set) and allSeqs containing one fx sequence (kind 'fx', inFrame 2, outFrame 7, one physic-paint layer); call the real renderGlobalFrame at positional index 0 and assert the stub renderer.renderFrame was called with the fx layers and fxLocalFrame 0 (it is skipped pre-fix). Case (b) 'selected export keeps later-sequence overlays riding': build fm as content-kind literals with globalFrame 5..9 owned by a second content sequence, allSeqs = that content sequence plus an fx overlay spanning inFrame 5 / outFrame 10; positional 0; assert the overlay leg renders (dropped pre-fix).
    GREEN (behavior commit): in renderGlobalFrame, immediately after the entry read at :138, derive const overlayGlobalFrame = entry ? entry.globalFrame : frameIndex; (per RESEARCH Code Example 4). Use overlayGlobalFrame in the overlay-leg inFrame gate at :305 and the getTimelineOverlaySequenceOutFrame gate at :306, and in the local-frame derivations at :310 (overlayLocalFrame) and :337 (fxLocalFrame). Leave untouched: the content branch's positional seqStart walk (:143-151 — genuinely positional), the renderFrame globalFrame arguments at :333/:358, and the motion-blur sub-frame flooring at :133-134. Note in the commit body that the shared line also repairs the pre-existing content-selected quirk (deliberate side effect per RESEARCH Open Question 1 — one identical line either way).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/exportRenderer.test.ts</automated>
    <fails_when>non-zero exit, or 'failed' with a non-zero count in the summary line</fails_when>
    <automated>grep -c "overlayGlobalFrame" app/src/lib/exportRenderer.ts</automated>
    <fails_when>prints 0 (the derivation never landed)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both new cases pass post-fix and were observed failing pre-fix (RED commit precedes the behavior commit in git log)
    - The gates at exportRenderer.ts:305-306 and the local-frame derivations at :310/:337 read overlayGlobalFrame; the content branch walk at :143-151 is textually unchanged
    - Every pre-existing exportRenderer.test.ts case passes in the same run
  </acceptance_criteria>
  <done>A selected-fx export of an inFrame > 0 sequence renders its paint on every filtered frame (never all-transparent), and the content-selected overlay-drop quirk is repaired by the same line with a pin.</done>
</task>

<task type="auto">
  <name>D-09 info-line count for fx actives + Pitfall-3 playback activation pin</name>
  <files>app/src/components/export/FormatSelector.tsx, app/src/lib/playbackEngine.test.ts</files>
  <read_first>
    - app/src/components/export/FormatSelector.tsx (:25-46 signal-read style; info line :88-92)
    - app/src/lib/frameMap.ts (:55-59 activeSequenceFrames — kind-agnostic filter)
    - app/src/lib/playbackEngine.test.ts (full — the ./frameMap mockSignal at :29-35 and the harness's store-mock shape)
    - app/src/lib/playbackEngine.ts (:183-199 syncActiveSequence — the setActive call site; sequenceStore.setActive also clears selectedKeyPhotoId per sequenceStore.ts:1091-1094)
  </read_first>
  <action>
    FormatSelector.tsx (D-09): import frameMap from '../../lib/frameMap' (same relative-shape convention as the existing store imports at :2-6). In the component body beside :38-41 derive const activeSeqFrameCount = activeSeq ? frameMap.value.filter(e => e.sequenceId === activeSeq.id).length : 0; and render activeSeqFrameCount as the frame count in the info line at :88-91, replacing the hold-frames summation over activeSeq.keyPhotos (always 0 for fx sequences — the UI lie D-09 kills). Signal discipline per the efx-preact-reactivity rules: narrow .value reads in the component body exactly like the existing reads at :26-41; no useState, no render-body signal writes, no new effect.
    playbackEngine.test.ts (Pitfall 3 pin): the file mocks ./frameMap with mockSignal (:29-35). Add a case 'playback into a paint frame activates the owning fx sequence': set the frameMap mock to return PaintFrameEntry-shaped entries owned by 'fx-p' at the playhead position, drive the same engine entry point the existing AUDIO cases drive so syncActiveSequence runs (playbackEngine.ts:188-199), and assert the harness's sequenceStore mock observed setActive('fx-p'). Add a one-line comment citing Pitfall 3: dense frameMap makes paint frames activatable — deliberate, D-08-consistent (setActive clearing selectedKeyPhotoId is setActive's own tested behavior, out of scope here).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/playbackEngine.test.ts src/lib/exportEngine.paintEnumeration.test.ts</automated>
    <fails_when>non-zero exit, or 'failed' with a non-zero count in the summary line</fails_when>
    <automated>grep -c "frameMap.value.filter(e => e.sequenceId === activeSeq.id)" app/src/components/export/FormatSelector.tsx</automated>
    <fails_when>prints 0 (the info line is not sourced from the frameMap filter)</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any 'error TS' line in output</fails_when>
  </verify>
  <acceptance_criteria>
    - FormatSelector.tsx info line renders the frameMap filter count; the hold-frames summation over activeSeq.keyPhotos no longer feeds that line
    - The new playbackEngine.test.ts case passes and asserts setActive observed 'fx-p'
    - tsc --noEmit exits 0; the paintEnumeration acceptance file stays green (no Plan-01 regressions)
  </acceptance_criteria>
  <done>The info line tells the truth for fx actives (D-09), and the playback activation side effect of dense enumeration is pinned as deliberate (Pitfall 3) — Plan 52.3-02 closes with D-08 correct at any inFrame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The overlay-gate fix changes only which frame number an internal gate reads; the info line reads the same validated in-memory frameMap the export filter reads; no new IPC, paths, or input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.3-04 | Tampering | Overlay-leg gating frame derivation (exportRenderer.ts) — a wrong derivation renders the wrong fx-local frame or drops overlays (output integrity) | low | mitigate | overlayGlobalFrame derives only from the internally constructed entry.globalFrame (never external input); the two Task-1 RED pins lock the gate for both paint-selected and content-selected arranges |
| T-52.3-SC | Tampering | npm/pip/cargo installs | — | accept | Not triggered: zero new packages this phase |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/lib/exportRenderer.test.ts src/lib/playbackEngine.test.ts src/lib/exportEngine.paintEnumeration.test.ts` green
- `pnpm --filter efx-motion-editor exec tsc --noEmit` clean
- Full gates + native UAT (including the D-09 info-line row) run in Plan 52.3-03.
</verification>

<success_criteria>
- Selected-fx export is correct for any inFrame (Pitfall 1 closed with pins)
- The D-09 info line reflects the exact count the export filter will process
- Playback activation on paint frames is deliberate and pinned
</success_criteria>

## Artifacts this phase produces (Plan 02 scope)

New symbols: none exported.
- `overlayGlobalFrame` (local const, app/src/lib/exportRenderer.ts renderGlobalFrame)
- `activeSeqFrameCount` (local const, app/src/components/export/FormatSelector.tsx)

New test cases: 'fx overlay gates on entry.globalFrame (Pitfall 1)', 'selected export keeps later-sequence overlays riding' (exportRenderer.test.ts); 'playback into a paint frame activates the owning fx sequence' (playbackEngine.test.ts)

New file paths: none.

<output>
Create `.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-02-SUMMARY.md` when done.
</output>
