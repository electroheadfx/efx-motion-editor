---
phase: 52.3-paint-content-export-per-frame-compositor-enumeration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/timeline.ts
  - app/src/lib/frameMap.ts
  - app/src/lib/exportRenderer.ts
  - app/src/lib/previewRenderer.ts
  - app/src/components/Preview.tsx
  - app/src/components/layout/CanvasArea.tsx
  - app/src/lib/exportEngine.paintEnumeration.test.ts
  - app/src/lib/exportRenderer.test.ts
  - app/src/lib/frameMap.test.ts
  - app/src/lib/exportEngine.test.ts
  - app/src/lib/exportEngine.loops.test.ts
  - app/src/lib/previewRenderer.test.ts
autonomous: true
requirements: [AC-C, AC-D, AC-E, AC-CLEAR, AC-MIX, D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-10]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "AC-C: a paint-only fx sequence (inFrame 0 / outFrame 5, real keys 0..4, no content sequence) enumerates exactly 5 dense FrameEntry objects (D-04/D-05)"
    - "AC-D: startExport on a paint-only project completes with 5 rendered frames; the 'No frames to export (timeline is empty)' refusal is unreachable for paint content (D-10)"
    - "AC-CLEAR: every no-content frame starts from clean pixels — one top-level identity-transform clearRect gated on !hasContentEntry (D-06); content frames keep byte-identical behavior"
    - "AC-MIX: content projects are unchanged — Controls A/B stay green; mixed frames stay content entries (D-02); the tail-pad hold beneath paint is preserved as designed (D-03)"
    - "AC-E: with an fx sequence active, selectedSequenceOnly exports that fx's paint entries to completion (D-08); a genuinely empty timeline still refuses with the locked copy (D-10)"
    - "Gap frames (inside N, outside every content/fx span) carry kind 'gap' with an empty sequenceId and export transparent (D-07)"
  artifacts:
    - app/src/types/timeline.ts — FrameEntry converted to the ContentFrameEntry | PaintFrameEntry | GapFrameEntry discriminated union (D-01)
    - app/src/lib/frameMap.ts — paint enumeration branch after the tail pad + conscious efxPaintVersion subscription (D-04/D-05, Pitfall 2)
    - app/src/lib/exportRenderer.ts — D-06 top-level clear, buildSequenceFrames discriminant, content-narrowed preload collect
    - app/src/lib/previewRenderer.ts, app/src/components/Preview.tsx, app/src/components/layout/CanvasArea.tsx — content-kind narrowing at every FrameEntry field read
    - app/src/lib/exportEngine.paintEnumeration.test.ts — Cases C/D reinstated, Case E re-pinned (D-08), Case E2 mixed characterization, D-10 negative, Case D motion-blur variant — all green
    - app/src/lib/frameMap.test.ts — paint enumeration unit block; app/src/lib/exportRenderer.test.ts — AC-CLEAR block
  key_links:
    - "frameMap computed -> exportEngine.startExport: paint entries carry the owning fx sequenceId so the existing selectedSequenceOnly predicate matches them (D-08)"
    - "enumeration coverage predicate === overlay-leg gate predicate: both call getTimelineOverlaySequenceOutFrame so enumeration and render gating can never drift (D-04)"
    - "renderGlobalFrame hasContentEntry gate -> paint/gap entries skip the content branch; the fx overlay leg draws via PreviewRenderer -> CMP-01 getFlattenedFrame (fond-inclusive, D-07)"
  prohibitions:
    - "hasContentEntry broadened to treat fx-owned entries as content (D-02)"
    - "sentinel keyPhotoId/imageId values on paint or gap entries (D-01)"
    - "a second total-frame computation or any paint-specific clamp (D-04)"
    - "iteration over internal paint tracks anywhere in the export path (CMP-01)"
    - "mocking ./frameMap or ../stores/sequenceStore in the acceptance harness"
    - "per-frame new Image()/createImageBitmap decodes inside the render loop"
    - "resolution-policy or 1920 long-edge clamp changes (held for v1.2.0)"
---

<objective>
Make a paint-only project export its painted frames end-to-end: enumerate one dense FrameEntry per frame of [0, N) via a house-style discriminated union (D-01), render every no-content frame onto a clean canvas (D-06), and turn the parked RED contracts (Cases C/D) green while re-pinning Case E to the D-08 filter semantics.

Purpose: quick 260919-azh proved (verdict NEVER-WIRED) that frameMap materializes entries only from content keyPhotos — this plan wires the missing paint enumeration branch; it is designed behavior, not a regression repair (CONTEXT specifics).
Output: the FrameEntry union, the enumeration branch, the D-06 clear, and the fully green acceptance matrix with tsc clean.

Recorded notes (visible, deliberate):
- Spec-less probe fallback: no SPEC.md and no phase requirement IDs exist; acceptance coverage is lifted from the AC-rows and D-01..D-10 into must_haves.truths instead (recorded skip, no probe predicates generated).
- Assumption-delta decision (advisory): promote — the discriminated FrameEntry union becomes the primary representation; the legacy flat interface survives only as ContentFrameEntry's field set (aligns with locked D-01). Detector signal was a prose false-positive ("Also out:"); decision recorded anyway.
- D-08/D-10 mixed-project reconciliation: D-02/D-03 (locked) keep content winning per frame and the tail-hold intact, so in a MIXED project an fx-active selectedSequenceOnly filter matches zero entries and still refuses. D-08's mechanism (locked: the existing sequenceId predicate) is honored literally; D-10 is read scoped to paint content per the phase goal. Case E2 pins the mixed refusal deliberately so the semantics are explicit at plan review — flag to the user if the mixed case should instead synthesize paint entries (a D-08 "redesign" reading the discuss agenda declined).
- A3 preview note: renderGlobalFrame is shared with Preview.tsx; the gated D-06 clear also fixes the latent transparent-fond preview ghost. Read D-07's "preview unchanged" as "no preview redesign", not "diverge the shared function" (surfaced per RESEARCH Assumption A3; UAT row 6 verifies).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-CONTEXT.md
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-RESEARCH.md
@.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-PATTERNS.md
@app/src/lib/frameMap.ts
@app/src/lib/exportRenderer.ts
@app/src/lib/exportEngine.paintEnumeration.test.ts
@app/src/types/timeline.ts
@app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer (RED): reinstate the parked paint-export contracts + the canvas-clear pin, end-to-end failing</name>
  <files>app/src/lib/exportEngine.paintEnumeration.test.ts, app/src/lib/exportRenderer.test.ts</files>
  <read_first>
    - app/src/lib/exportEngine.paintEnumeration.test.ts (full — the harness, the header comments at :269-295 preserving the parked bodies, the Case E characterization at :297-319)
    - app/src/lib/exportRenderer.test.ts (the renderGlobalFrame describe block and stub harness at :406-498)
    - app/src/lib/frameMap.ts (current enumeration at :16-47 — the subject under test)
    - app/src/lib/exportRenderer.ts (renderGlobalFrame at :123-363 — the clear insertion point at :140)
    - .planning/quick/260919-azh-paint-content-never-reaches-export-accep/260919-azh-SUMMARY.md (NEVER-WIRED verdict; the escalation clause that parked these contracts)
  </read_first>
  <behavior>
    - Case C (reinstated as it): fx physic-paint sequence inFrame 0 / outFrame 5, runtime real keys at appFrames 0..4, NO content sequence → FAILS today: frameMap.value length 0, expected 5
    - Case D (reinstated as it): same arrange → await startExport() → FAILS today: status 'error' with the locked refusal copy, expected 'complete'
    - AC-CLEAR (new case): a frame whose fm entry is fx-owned (or an empty fm) → FAILS today: no clearRect op recorded on the stub 2d context
    - Controls A/B and every pre-existing exportRenderer.test.ts case stay green (zero regressions ride the RED commit)
  </behavior>
  <action>
    In exportEngine.paintEnumeration.test.ts convert the two parked contracts at :288 (Case C) and :295 (Case D) from it.todo to active it, using the bodies preserved verbatim in the header comments at :283-294. Case C arrange: sequenceStore.sequences.value = [makeFxPaintSequence('fx-c', LAYER, 0, 5)] plus installRotoDocument(LAYER, [0, 1, 2, 3, 4]) — no content sequence anywhere — asserting frameMap.value has length 5. Case D: identical arrange, then await startExport(), asserting exportStore.progress.peek().status is 'complete', renderGlobalFrameMock was called exactly 5 times, and errorMessage is not the locked refusal copy. Retitle both dropping the 'Phase 53:' parking label while keeping the 'Case C'/'Case D' prefixes (the -t filters bind on those prefixes). Update the parked-contracts header comment block (:269-281) with one line recording reinstatement under Phase 52.3 per D-01..D-10.
    In exportRenderer.test.ts add a new describe block 'canvas clear lifecycle (D-06, AC-CLEAR)' next to the existing renderGlobalFrame describe (:406-498). Use the RecordingCanvasContext op-recording pattern (model: exportEngine.paintEnumeration.test.ts:80-97) behind a canvas stub whose getContext('2d') returns the recorder. In the same test-only commit, upgrade the shared makeCanvasStub at :421-422 so its getContext('2d') returns a minimal no-op 2d context (save/restore/setTransform/clearRect no-ops; the willReadFrequently options arg accepted and ignored). This upgrade is REQUIRED for Task 2, not optional: the pre-existing empty-fm cases at :436 and :466 call renderGlobalFrame with fm=[], so once the D-06 clear lands its !hasContentEntry gate fires on those cases and a getContext-less stub would TypeError. The upgrade is inert pre-GREEN (nothing calls canvas.getContext today) so the RED run's zero-regression bar stays intact. Case 1: call the real renderGlobalFrame with an fm whose single entry is owned by an fx sequence id present in allSeqs as kind 'fx' (and a second call with an empty fm) — assert a clearRect op is recorded before any draw op. Case 2 (byte-identical guard): a content-owned entry — assert no additional top-level clearRect is recorded ahead of the content render path (the pre-existing guard test at :471-491 asserting clearCanvas arg true/false stays green untouched).
    Run the two files and confirm exactly the three documented RED signatures. Commit as a test-only commit (repo convention: RED commits carry no production change).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/exportEngine.paintEnumeration.test.ts src/lib/exportRenderer.test.ts</automated>
    <fails_when>exit 0 (any of Case C, Case D, or the new clear case unexpectedly green), failure output differing from the documented signatures (length 0 vs expected 5; status 'error' with the locked refusal copy; missing clearRect op), or any pre-existing case newly failing</fails_when>
  </verify>
  <acceptance_criteria>
    - vitest output shows Case C failing on frameMap.value length 0 vs expected 5, Case D failing on status 'error' + 'No frames to export (timeline is empty)' vs expected 'complete', and the AC-CLEAR case failing on no recorded clearRect
    - Controls A/B and all pre-existing exportRenderer.test.ts cases pass in the same run
    - git show --stat for the commit lists only the two test files
  </acceptance_criteria>
  <done>Three documented RED signatures reproduced in one run and committed test-only; the tracer's end-to-end path (real frameMap computed -> real startExport) is proven to fail exactly where the NEVER-WIRED diagnosis said it would.</done>
</task>

<task type="auto" tdd="true">
  <name>Tracer (GREEN): FrameEntry discriminated union + paint enumeration branch + D-06 top-level clear (D-01/D-02/D-04/D-05/D-06)</name>
  <files>app/src/types/timeline.ts, app/src/lib/frameMap.ts, app/src/lib/exportRenderer.ts, app/src/lib/previewRenderer.ts, app/src/components/Preview.tsx, app/src/components/layout/CanvasArea.tsx</files>
  <read_first>
    - app/src/types/timeline.ts (:26-35 — the interface being converted)
    - app/src/efx-paint/compositor/efxPaintBackgroundResolution.ts (:40-53 house-style union analog; :193-196 never-exhaustiveness idiom)
    - app/src/lib/frameMap.ts (full — construction :23-39, tail pad :41-45, N law :244-259, coverage fn :261-270, getLayerId :123-125)
    - app/src/lib/exportRenderer.ts (full — buildSequenceFrames :17-36, hasContentEntry :138-140, clearRect style precedent :203-208, preload collect :444)
    - app/src/lib/previewRenderer.ts (:320-350 optional-field read, :470-505 solid/gradient/transparent draw block, :615-640 imageId read)
    - app/src/components/Preview.tsx (:81-91 preload effect), app/src/components/layout/CanvasArea.tsx (:50-56)
    - .planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-RESEARCH.md (Patterns 1-3, Code Examples 1-3, Pitfall 2)
  </read_first>
  <behavior>
    - Case C green: paint-only fx sequence (inFrame 0/outFrame 5, keys 0..4) → frameMap.value length 5
    - Case D green: same arrange → startExport status 'complete', renderGlobalFrameMock called 5 times, refusal copy absent
    - AC-CLEAR green: no-content frames record a clearRect before any draw; content frames unchanged
    - Controls A/B stay green (AC-MIX; D-02 content-wins and D-03 tail-hold hold structurally via the entries.length === 0 guard)
    - Known intermediate state: tsc stays RED on test literals lacking the discriminant — swept in Task 3 (repo convention: behavior commit first, test-only sweep second, per Pitfall 5)
  </behavior>
  <action>
    types/timeline.ts (D-01): convert the FrameEntry interface (:26-35) into a discriminated union of three exported interfaces mirroring the efxPaintBackgroundResolution house style. ContentFrameEntry: readonly kind: 'content' plus every current field unchanged (globalFrame, sequenceId, keyPhotoId, imageId, localFrame, optional solidColor/isTransparent/gradient). PaintFrameEntry: readonly kind: 'paint', globalFrame: number, sequenceId: string (the owning fx sequence id — the D-08 filter target), layerId: string (paint layer identity per D-01; the fx sequence's first physic-paint layer, resolved via getLayerId at construction) — deliberately no keyPhotoId/imageId (sentinel ids are the banned anti-pattern). GapFrameEntry: readonly kind: 'gap', globalFrame: number, sequenceId: string (always the empty string — mirrors the precedent's ownerless gap arm; RESEARCH Open Question 2). FrameEntry becomes the union type alias; keep the GradientData import. (Member names/shape are Claude's Discretion within D-01/D-02 and are recorded here.)
    frameMap.ts: add kind: 'content' to the construction literal at :26-35 (the tail-pad spread at :44 inherits the discriminant automatically). Immediately after the tail pad (:41-45) add the paint enumeration branch (D-04/D-05), gated on entries.length === 0 (RESEARCH Finding F1: with any content, the tail pad already fills [0, N), so the branch fires only in content-empty projects): collect fxSeqs = sequences filtered to kind 'fx', visible !== false, having at least one layer of type 'physic-paint'; for each f in [0, targetLength) find the first fxSeq whose span covers f (f >= (seq.inFrame ?? 0) AND f < getTimelineOverlaySequenceOutFrame(seq, targetLength) — the identical predicate the overlay leg gates on at exportRenderer.ts:306, the anti-drift lock); push a PaintFrameEntry (globalFrame f, sequenceId owner.id, layerId from the owner's first physic-paint layer via the existing getLayerId helper at :123-125) or a GapFrameEntry (globalFrame f, sequenceId '') when no owner covers f. Do NOT touch getTimelineRequiredFrameCount (D-04 — N law reused unchanged) and do not add any clamp. Add the Pitfall-2 defensive subscription: void efxPaintVersion.value; beside the existing void physicPaintVersion.value; at :17 with a one-line comment citing Pitfall 2 (enumeration reads getActiveTrackId -> getEfxPaintDocument; document-only mutations bump only efxPaintVersion) — import efxPaintVersion from '../stores/efxPaintStore' (exported at efxPaintStore.ts:42; getDocument is already imported from there).
    exportRenderer.ts: add kind: 'content' to the buildSequenceFrames literal at :22-31 (one of only two production construction sites). Insert the D-06 top-level clear immediately after hasContentEntry is computed at :140 and before the content branch, gated if (!hasContentEntry): acquire the 2d context with willReadFrequently: true, then save -> setTransform(1, 0, 0, 1, 0, 0) -> clearRect(0, 0, canvas.width, canvas.height) -> restore, copying the shape at :203-208 verbatim minus the drawImage. This clear also fires in the two pre-existing empty-fm cases at exportRenderer.test.ts:436/:466 (fm=[] → hasContentEntry false); they exercise it through the no-op getContext stub installed in Task 1 and must stay green with zero production-side accommodation — if either TypeErrors, the Task-1 stub upgrade is missing, do not patch production to compensate. Do NOT broaden hasContentEntry (D-02 — paint/gap entries flow through the existing seq.kind !== 'fx' gate by construction). Narrow the preload collect at :444: replace the fm.map over imageId with a flatMap that yields imageId only from kind === 'content' entries, keeping the id !== '' filter.
    previewRenderer.ts: at :333 guard the gradient/solidColor/isTransparent read with entry.kind === 'content'; apply the same content-kind narrowing to the solid/gradient/transparent draw-block reads at :482-500 (narrow entry once, then read); at :628-630 return null unless entry && entry.kind === 'content' before the entry.imageId read (that path only ever receives content seqFrames today — the guard makes it explicit).
    Preview.tsx:88: narrow the preload map to flatMap yielding imageId only from kind === 'content' entries (keep the frameMapLength subscription shape at :82-85 untouched).
    CanvasArea.tsx:51-56: guard the imageStore.getById(frame.imageId) lookup with frame.kind === 'content' (activeSequenceFrames at frameMap.ts:55-59 is kind-agnostic and can now return paint entries when an fx sequence is active).
    Do not touch playbackEngine.ts (sequenceId-only reads, kind-agnostic), exportEngine.ts (D-08 needs zero structural change), or any cross-dissolve path.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/exportEngine.paintEnumeration.test.ts src/lib/exportRenderer.test.ts</automated>
    <fails_when>non-zero exit, or any of Case C / Case D / AC-CLEAR / Controls A-B reported as failed in the summary line</fails_when>
    <automated>grep -c "kind: 'paint'" app/src/lib/frameMap.ts && grep -c "if (!hasContentEntry)" app/src/lib/exportRenderer.ts && grep -c "GapFrameEntry" app/src/types/timeline.ts</automated>
    <fails_when>any of the three counts prints 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Case C and Case D pass; the AC-CLEAR cases pass; Controls A/B pass in the same run
    - frameMap.ts contains the entries.length === 0 gated branch and calls getTimelineOverlaySequenceOutFrame inside it
    - exportRenderer.ts contains exactly one new top-level clearRect site gated on !hasContentEntry; the hasContentEntry expression at :140 is textually unchanged
    - types/timeline.ts exports ContentFrameEntry, PaintFrameEntry, GapFrameEntry and the FrameEntry union alias; PaintFrameEntry declares no keyPhotoId/imageId members
    - tsc is expected RED on test literals at this commit (Task 3 owns the sweep) — vitest is the green authority for this task
  </acceptance_criteria>
  <done>The tracer's end-to-end path is green: a paint-only project enumerates 5 dense entries and exports 5 frames to completion through the real computed and the real startExport, with clean pixels on every no-content frame.</done>
  <reversibility rating="costly">D-01: the variant enters the shared FrameEntry type consumed by frameMap, exportRenderer, preview, and test harnesses — removing it later means touching every discriminated consumer the compiler surfaces (CONTEXT D-01 reversibility note; flagged, no gate).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Test-side sweep: discriminant literals, enumeration unit tests, Case E re-pin (D-08/D-10), tsc gate</name>
  <files>app/src/lib/exportRenderer.test.ts, app/src/lib/exportEngine.test.ts, app/src/lib/exportEngine.loops.test.ts, app/src/lib/previewRenderer.test.ts, app/src/lib/frameMap.test.ts, app/src/lib/exportEngine.paintEnumeration.test.ts</files>
  <read_first>
    - app/src/lib/exportEngine.paintEnumeration.test.ts (post-Task-1 state; Case E at :297-319 to amend in place per Pitfall 4)
    - app/src/lib/frameMap.test.ts (fixtures :14-124, tail-pad/roto-extension pin :285-346, as-any escape idiom :235/:249/:261/:278-282)
    - app/src/lib/exportRenderer.test.ts (literal sites: :211-212, :373-374, :480-481, :525, plus the Array.from FrameEntry[] literal at :553-559 feeding the preloadExportImages arrange)
    - app/src/lib/exportEngine.test.ts (makeFm helper :228-235), app/src/lib/exportEngine.loops.test.ts (makeFm helper :301-308), app/src/lib/previewRenderer.test.ts (literal :526-531)
    - app/src/lib/frameMap.ts (post-Task-2 state — the branch under unit test)
  </read_first>
  <behavior>
    - frameMap enumeration unit block (D-04/D-05): paint-only fx inFrame 0 → 5 paint entries with fx sequenceId and layerId; fx at inFrame 3 → frames 0-2 gap (sequenceId '') + frames 3-7 paint; hidden fx (visible: false) → no enumeration; loop-clip extension beyond the last real key extends N; fx with only a non-physic-paint layer → gap entries for its span (documented side-effect pin); empty sequences → length 0
    - Case E re-pinned (D-08): fx-e paint-only arrange, fx active + selectedSequenceOnly → 5 matched entries, status 'complete', renderGlobalFrameMock ×5
    - Case E2 characterization: the original mixed arrange (content-e + fx-e) with fx active + selectedSequenceOnly still refuses with the locked copy — content wins per D-02/D-03
    - D-10 negative: zero sequences → status 'error' + 'No frames to export (timeline is empty)'
    - Case D motion-blur variant (Pitfall 6): motionBlurEnabled → status 'complete', renderFrameWithMotionBlurMock ×5
    - tsc --noEmit clean across the repo
  </behavior>
  <action>
    Literal churn (Pitfall 5 — keep as its own test-only commit): add kind: 'content' to every FrameEntry literal — exportRenderer.test.ts at :211-212, :373-374, :480-481, :525, and the Array.from FrameEntry[] literal at :553-559 (the preloadExportImages arrange — easy to overlook because the entries are produced by the Array.from callback, not written as a flat list); the makeFm helpers in exportEngine.test.ts (:228-235) and exportEngine.loops.test.ts (:301-308); the Array.from literal in previewRenderer.test.ts (:526-531). If tsc flags any further literal sites beyond this list, sweep them in the same commit — the tsc gate is the final enumerator. In frameMap.test.ts add a local asContent narrow helper (throws unless e.kind === 'content', per PATTERNS) and route the file's 6 .imageId assertions through it instead of scattering escapes.
    frameMap.test.ts — new describe 'paint enumeration (D-04/D-05)' mirroring the fixture shape at :285-346 minus any content sequence: (a) fx-only inFrame 0/outFrame 5 with keys 0..4 → length 5, every entry kind 'paint', sequenceId the fx id, layerId non-empty; (b) fx at inFrame 3/outFrame 8 with keys 0..4 → entries 0-2 kind 'gap' with sequenceId '', entries 3-7 kind 'paint'; (c) fx visible: false with keys installed → length 0 (the N law skips hidden sequences at frameMap.ts:247); (d) a Hold loop clip extending beyond the last real key extends N (reuse the loop-fixture builders from :14-124); (e) an fx sequence whose only layer is a non-physic-paint type (generator literal) → gap entries across its authored span (N law includes the span; no paint owner — documented side-effect pin); (f) sequences = [] → length 0.
    exportEngine.paintEnumeration.test.ts — amend Case E in place (Pitfall 4: amend, never delete; git history preserves the characterization): change the arrange to paint-only (drop content-e, keep makeFxPaintSequence('fx-e', LAYER, 0, 5), installRotoDocument(LAYER, [0,1,2,3,4]), activeSequenceId 'fx-e', setSelectedSequenceOnly(true)) and flip assertions to status 'complete', renderGlobalFrameMock ×5, frameMap.value filtered by sequenceId 'fx-e' having length 5. Add Case E2 immediately after, preserving the ORIGINAL mixed arrange (content-e 3 keyPhotos + fx-e + keys 0..4, fx active, selectedSequenceOnly) asserting status 'error' with the locked refusal copy, with a comment citing D-02/D-03 (content wins per frame; tail-hold preserved) and D-08's locked mechanism (the existing sequenceId predicate) — the deliberate mixed semantics surfaced in this plan's objective. Add the D-10 negative: sequences = [] → status 'error' + 'No frames to export (timeline is empty)'. Add the Case D motion-blur variant: Case D's arrange plus exportStore.motionBlurEnabled.value = true → status 'complete', renderFrameWithMotionBlurMock called 5 times.
    Then run the full affected set plus tsc.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/exportEngine.paintEnumeration.test.ts src/lib/frameMap.test.ts src/lib/exportRenderer.test.ts src/lib/exportEngine.test.ts src/lib/exportEngine.loops.test.ts src/lib/previewRenderer.test.ts</automated>
    <fails_when>non-zero exit, or 'failed' with a non-zero count in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any 'error TS' line in output</fails_when>
    <automated>grep -v '^\s*//' app/src/lib/exportEngine.paintEnumeration.test.ts | grep -c 'it\.todo('</automated>
    <fails_when>prints anything other than 0 (a parked contract survived the sweep)</fails_when>
  </verify>
  <acceptance_criteria>
    - All six test files pass in one run; the paint enumeration describe block asserts kind discriminants, gap boundaries, hidden-fx exclusion, and loop-aware extent
    - Case E asserts the D-08 filter behavior (complete ×5); Case E2 asserts the mixed refusal with the D-02/D-03 citation comment present; the D-10 negative and the motion-blur variant exist and pass
    - tsc --noEmit exits 0 with zero error lines
    - No it.todo( remains in non-comment lines of the acceptance harness
  </acceptance_criteria>
  <done>The acceptance matrix is fully green (Cases C/D/E + E2 + D-10 negative + AC-CLEAR + enumeration units), the discriminant churn is committed test-only, and tsc is clean — Plan 52.3-01 closes with the paint-only export wired end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Enumeration derives from validated in-memory stores (sequenceStore, physicPaintStore — parsed fail-closed at write time); PNG writes flow through the existing exportWritePng IPC to the user-chosen export directory; no new IPC commands, paths, or scopes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.3-01 | Tampering | PaintFrameEntry.sequenceId assignment (frameMap.ts branch) | low | mitigate | Construction-side only: the branch assigns sequenceId from the owning fx sequence object in the same loop; the kind discriminant + the unchanged hasContentEntry gate (seq.kind !== 'fx') keep paint entries out of the content branch even if ids collided |
| T-52.3-02 | Denial of Service | Dense [0, N) entry array on pathological paint extent | low | accept | Existing bounds stand: roto capacity is clamped at write (PHYSIC_PAINT_MAX_APPLY_FRAMES); N derives from the same getTimelineRequiredFrameCount law the timeline already materializes for display — no new unbounded allocation |
| T-52.3-03 | Information Disclosure (intra-document) | Stale-canvas ghost pixels exporting prior-frame content | medium | mitigate | D-06 top-level clear gated on !hasContentEntry; AC-CLEAR is the regression pin (Task 1 RED, Task 2 GREEN) |
| T-52.3-SC | Tampering | npm/pip/cargo installs | — | accept | Not triggered: zero new packages this phase (RESEARCH Package Legitimacy Audit: gate not applicable) |
</threat_model>

<verification>
- The reinstated acceptance matrix passes: `pnpm --filter efx-motion-editor exec vitest run src/lib/exportEngine.paintEnumeration.test.ts src/lib/frameMap.test.ts src/lib/exportRenderer.test.ts`
- Type gate: `pnpm --filter efx-motion-editor exec tsc --noEmit` clean
- Plan-level gates (full suite, cargo, chunk budget 1340 kB) run in Plan 52.3-03.
</verification>

<success_criteria>
- A paint-only project exports exactly the N frames the timeline shows (WYSIWYG, D-05) with clean pixels on every frame
- Content projects are byte-identical (Controls A/B green; D-02/D-03 hold structurally)
- The FrameEntry union is fail-closed by type: every consumer compiles only after confronting the variant (D-01)
- tsc clean; zero it.todo( left in the acceptance harness
</success_criteria>

## Artifacts this phase produces (Plan 01 scope)

New exported symbols:
- `ContentFrameEntry` (interface, app/src/types/timeline.ts)
- `PaintFrameEntry` (interface, app/src/types/timeline.ts) — members: kind, globalFrame, sequenceId, layerId
- `GapFrameEntry` (interface, app/src/types/timeline.ts) — members: kind, globalFrame, sequenceId
- `FrameEntry` (type alias converted to the union, app/src/types/timeline.ts)

New local symbols (non-exported):
- `asContent` narrow helper (app/src/lib/frameMap.test.ts)
- paint enumeration branch locals: fxSeqs / owner (app/src/lib/frameMap.ts computed)

New test cases: Case C, Case D (reinstated as it); AC-CLEAR describe (exportRenderer.test.ts); paint enumeration describe (frameMap.test.ts, 6 cases); Case E (amended), Case E2, D-10 negative, Case D motion-blur variant (paintEnumeration.test.ts)

New file paths: none — every change lands in existing git-tracked files.

<output>
Create `.planning/phases/52.3-paint-content-export-per-frame-compositor-enumeration/52.3-01-SUMMARY.md` when done.
</output>
