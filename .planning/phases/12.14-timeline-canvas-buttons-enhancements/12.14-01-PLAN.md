---
phase: 12.14-timeline-canvas-buttons-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/layout/TimelinePanel.tsx
  - Application/src/components/layout/CanvasArea.tsx
autonomous: true
requirements:
  - BTN-ICON
  - BTN-TOOLTIP
  - BTN-STYLE

must_haves:
  truths:
    - "Timeline playback buttons display SVG icons (Rewind, Play/Pause, SkipBack, SkipForward) instead of Unicode characters"
    - "Timeline zoom buttons display SVG icons (Minus, Shrink, Plus) instead of text/Unicode"
    - "Canvas playback buttons display SVG icons (SkipBack, Play/Pause, SkipForward) instead of Unicode characters"
    - "Canvas zoom and utility buttons display SVG icons (Minus, Plus, Maximize2/Minimize2, Maximize) instead of text/Unicode"
    - "Canvas play/pause button retains its emphasis styling (rounded-full, accent bg, white icon, larger size)"
    - "All replaced buttons have title attributes with Label (Shortcut) format"
  artifacts:
    - path: "Application/src/components/layout/TimelinePanel.tsx"
      provides: "Timeline controls with lucide-preact icons"
      contains: "import.*lucide-preact"
    - path: "Application/src/components/layout/CanvasArea.tsx"
      provides: "Canvas controls with lucide-preact icons"
      contains: "import.*lucide-preact"
  key_links:
    - from: "Application/src/components/layout/TimelinePanel.tsx"
      to: "lucide-preact"
      via: "named icon imports"
      pattern: "import.*Rewind.*Play.*Pause.*SkipBack.*SkipForward.*Plus.*Minus.*Shrink.*lucide-preact"
    - from: "Application/src/components/layout/CanvasArea.tsx"
      to: "lucide-preact"
      via: "named icon imports"
      pattern: "import.*Play.*Pause.*SkipBack.*SkipForward.*Plus.*Minus.*Maximize.*lucide-preact"
---

<objective>
Replace all Unicode text-based button characters in TimelinePanel and CanvasArea with lucide-preact SVG icons and add tooltip title attributes with keyboard shortcuts.

Purpose: Timeline and canvas playback/zoom controls currently use Unicode characters (triangles, symbols, text) which render inconsistently. SVG icons provide crisp rendering at any size and proper theme-aware coloring via currentColor.

Output: Two updated component files with icon imports and tooltip attributes.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.14-timeline-canvas-buttons-enhancements/12.14-CONTEXT.md
@.planning/phases/12.14-timeline-canvas-buttons-enhancements/12.14-RESEARCH.md

<interfaces>
<!-- Established lucide-preact usage pattern from KeyframeNavBar.tsx -->
From Application/src/components/sidebar/KeyframeNavBar.tsx:
```tsx
import { ChevronLeft, ChevronRight, Plus, RefreshCw, Trash2 } from 'lucide-preact';

// Usage pattern: <IconName size={N} /> inside button elements
// Color via currentColor inheritance from parent or via class
<button class={`${btnBase} ${btnEnabled}`}>
  <ChevronLeft size={14} />
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace TimelinePanel Unicode buttons with lucide-preact icons</name>
  <files>Application/src/components/layout/TimelinePanel.tsx</files>
  <read_first>
    - Application/src/components/layout/TimelinePanel.tsx
    - Application/src/components/sidebar/KeyframeNavBar.tsx
  </read_first>
  <action>
Replace all Unicode text-based button content in TimelinePanel.tsx with lucide-preact SVG icons. All icons at size={14} for the compact timeline bar.

1. Add import at top of file:
```tsx
import { Play, Pause, SkipBack, SkipForward, Rewind, Plus, Minus, Shrink } from 'lucide-preact';
```

2. Replace "Seek to start" button (lines 33-40):
   - Remove: `<span class="text-[11px] text-[var(--color-text-secondary)]">|&#9664;</span>`
   - Replace with: `<Rewind size={14} />`
   - Add to button: `title="Seek to start"` and `class="text-[var(--color-text-secondary)]"` on the button itself (so currentColor propagates to icon)
   - Full button becomes:
     ```tsx
     <button
       class="rounded bg-[var(--color-bg-input)] px-2 py-[5px] text-[var(--color-text-secondary)]"
       onClick={() => playbackEngine.seekToFrame(0)}
       title="Seek to start"
     >
       <Rewind size={14} />
     </button>
     ```

3. Replace "Play/Pause" button (lines 43-50):
   - Remove: `<span class="text-[11px] text-white">{timelineStore.isPlaying.value ? '\u23F8' : '\u25B6'}</span>`
   - Replace with: `{timelineStore.isPlaying.value ? <Pause size={14} /> : <Play size={14} />}`
   - Add `title` attribute and `text-white` on button:
     ```tsx
     <button
       class="rounded bg-[var(--color-accent)] px-2 py-[5px] text-white"
       onClick={() => playbackEngine.toggle()}
       title={timelineStore.isPlaying.value ? 'Pause (Space)' : 'Play (Space)'}
     >
       {timelineStore.isPlaying.value ? <Pause size={14} /> : <Play size={14} />}
     </button>
     ```

4. Replace "Step backward" button (lines 53-60):
   - Remove: `<span class="text-[11px] text-[var(--color-text-secondary)]">&#9664;|</span>`
   - Replace with: `<SkipBack size={14} />`
   - Full button:
     ```tsx
     <button
       class="rounded bg-[var(--color-bg-input)] px-2 py-[5px] text-[var(--color-text-secondary)]"
       onClick={() => playbackEngine.stepBackward()}
       title="Step backward"
     >
       <SkipBack size={14} />
     </button>
     ```

5. Replace "Step forward" button (lines 63-70):
   - Remove: `<span class="text-[11px] text-[var(--color-text-secondary)]">&#9654;|</span>`
   - Replace with: `<SkipForward size={14} />`
   - Full button:
     ```tsx
     <button
       class="rounded bg-[var(--color-bg-input)] px-2 py-[5px] text-[var(--color-text-secondary)]"
       onClick={() => playbackEngine.stepForward()}
       title="Step forward"
     >
       <SkipForward size={14} />
     </button>
     ```

6. Replace "Zoom out" button (lines 82-92):
   - Remove: `<span class="text-[11px] text-[var(--color-text-secondary)]">-</span>`
   - Replace with: `<Minus size={14} />`
   - Move `text-[var(--color-text-secondary)]` to button class. Keep existing disabled styling and title.
   - Full button:
     ```tsx
     <button
       class={`rounded-[5px] px-2.5 py-[5px] text-[var(--color-text-secondary)] ${
         timelineStore.isAtMinZoom.value
           ? 'bg-[var(--color-bg-input)] opacity-40 cursor-default'
           : 'bg-[var(--color-bg-input)] hover:bg-[var(--color-bg-hover-item)] cursor-pointer'
       }`}
       onClick={() => timelineStore.zoomOut()}
       title="Timeline zoom out (-)"
     >
       <Minus size={14} />
     </button>
     ```

7. Replace "Fit All" button (lines 94-100):
   - Remove: `<span class="text-[10px] text-[var(--color-text-secondary)]">Fit All</span>`
   - Replace with: `<Shrink size={14} />`
   - Move color to button, keep title:
     ```tsx
     <button
       class="rounded bg-[var(--color-bg-input)] px-2.5 py-[5px] text-[var(--color-text-secondary)] hover:bg-[var(--color-bg-hover-item)] cursor-pointer"
       onClick={handleFitAll}
       title="Fit all frames"
     >
       <Shrink size={14} />
     </button>
     ```

8. Replace "Zoom in" button (lines 102-112):
   - Remove: `<span class="text-[11px] text-[var(--color-text-secondary)]">+</span>`
   - Replace with: `<Plus size={14} />`
   - Move color to button:
     ```tsx
     <button
       class={`rounded-[5px] px-2.5 py-[5px] text-[var(--color-text-secondary)] ${
         timelineStore.isAtMaxZoom.value
           ? 'bg-[var(--color-bg-input)] opacity-40 cursor-default'
           : 'bg-[var(--color-bg-input)] hover:bg-[var(--color-bg-hover-item)] cursor-pointer'
       }`}
       onClick={() => timelineStore.zoomIn()}
       title="Timeline zoom in (+/=)"
     >
       <Plus size={14} />
     </button>
     ```

IMPORTANT: Do NOT modify the AddLayerMenu component, the timecode display text, or the zoom multiplier text display (these are out of scope per deferred decisions).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TimelinePanel.tsx contains `import { Play, Pause, SkipBack, SkipForward, Rewind, Plus, Minus, Shrink } from 'lucide-preact'`
    - TimelinePanel.tsx does NOT contain `\u23F8` or `\u25B6` or `&#9664;` or `&#9654;` or `>Fit All<`
    - TimelinePanel.tsx contains `<Rewind size={14}` and `<Play size={14}` and `<Pause size={14}` and `<SkipBack size={14}` and `<SkipForward size={14}` and `<Minus size={14}` and `<Shrink size={14}` and `<Plus size={14}`
    - TimelinePanel.tsx contains `title="Seek to start"` and `title="Step backward"` and `title="Step forward"` and `title="Fit all frames"`
    - TimelinePanel.tsx contains `title={timelineStore.isPlaying.value ? 'Pause (Space)' : 'Play (Space)'}`
    - No `<span class="text-[11px]` wrappers remain around button content (all removed)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 7 timeline buttons use lucide-preact SVG icons with title tooltips. No Unicode characters remain in button content. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace CanvasArea Unicode buttons with lucide-preact icons</name>
  <files>Application/src/components/layout/CanvasArea.tsx</files>
  <read_first>
    - Application/src/components/layout/CanvasArea.tsx
    - Application/src/components/sidebar/KeyframeNavBar.tsx
  </read_first>
  <action>
Replace all Unicode text-based button content in CanvasArea.tsx (the "Preview Controls" bar at lines 288-383) with lucide-preact SVG icons. Canvas icons use 16px standard, 18px for the emphasis play/pause button.

1. Add import at top of file (after existing imports):
```tsx
import { Play, Pause, SkipBack, SkipForward, Plus, Minus, Maximize, Maximize2, Minimize2 } from 'lucide-preact';
```

2. Replace "Step backward" button (lines 290-297):
   - Remove: `<span class="text-xs text-[var(--color-text-secondary)]">{'\u23EE'}</span>`
   - Replace with: `<SkipBack size={16} />`
   - Add color to button:
     ```tsx
     <button
       tabIndex={-1}
       class="flex items-center justify-center w-8 h-8 rounded bg-[var(--color-bg-settings)] cursor-pointer text-[var(--color-text-secondary)]"
       onClick={() => playbackEngine.stepBackward()}
       title="Step backward"
     >
       <SkipBack size={16} />
     </button>
     ```

3. Replace "Play / Pause" button (lines 299-308):
   - Remove: `<span class="text-sm text-white">{isPlaying ? '\u23F8' : '\u25B6'}</span>`
   - Replace with: `{isPlaying ? <Pause size={18} /> : <Play size={18} />}`
   - CRITICAL: Keep the emphasis styling intact (rounded-full, accent bg, w-9 h-9). Add dynamic title.
     ```tsx
     <button
       tabIndex={-1}
       class="flex items-center justify-center w-9 h-9 rounded-full bg-[var(--color-accent)] cursor-pointer text-white"
       onClick={() => playbackEngine.toggle()}
       title={isPlaying ? 'Pause (Space)' : 'Play (Space)'}
     >
       {isPlaying ? <Pause size={18} /> : <Play size={18} />}
     </button>
     ```

4. Replace "Step forward" button (lines 310-317):
   - Remove: `<span class="text-xs text-[var(--color-text-secondary)]">{'\u23ED'}</span>`
   - Replace with: `<SkipForward size={16} />`
   - Add color to button:
     ```tsx
     <button
       tabIndex={-1}
       class="flex items-center justify-center w-8 h-8 rounded bg-[var(--color-bg-settings)] cursor-pointer text-[var(--color-text-secondary)]"
       onClick={() => playbackEngine.stepForward()}
       title="Step forward"
     >
       <SkipForward size={16} />
     </button>
     ```

5. Replace "Zoom out" button (lines 328-338):
   - Remove: `<span class="text-sm text-[var(--color-text-button)]">-</span>`
   - Replace with: `<Minus size={16} />`
   - Move color to button:
     ```tsx
     <button
       class={`rounded-[5px] px-2.5 py-1 text-[var(--color-text-button)] ${
         canvasStore.isAtMinZoom.value
           ? 'bg-[var(--color-bg-settings)] opacity-40 cursor-default'
           : 'bg-[var(--color-bg-settings)] hover:bg-[var(--color-bg-input)] cursor-pointer'
       }`}
       onClick={() => canvasStore.zoomOut()}
       title="Zoom out (-)"
     >
       <Minus size={16} />
     </button>
     ```

6. Replace "Zoom in" button (lines 342-352):
   - Remove: `<span class="text-sm text-[var(--color-text-button)]">+</span>`
   - Replace with: `<Plus size={16} />`
   - Move color to button:
     ```tsx
     <button
       class={`rounded-[5px] px-2.5 py-1 text-[var(--color-text-button)] ${
         canvasStore.isAtMaxZoom.value
           ? 'bg-[var(--color-bg-settings)] opacity-40 cursor-default'
           : 'bg-[var(--color-bg-settings)] hover:bg-[var(--color-bg-input)] cursor-pointer'
       }`}
       onClick={() => canvasStore.zoomIn()}
       title="Zoom in (=)"
     >
       <Plus size={16} />
     </button>
     ```

7. Replace "Fit" button (lines 354-370):
   - Remove: `<span class={...}>Fit</span>` (the entire conditional span)
   - Replace with conditional icon: `{canvasStore.fitLocked.value ? <Minimize2 size={16} /> : <Maximize2 size={16} />}`
   - The icon color follows from the button text color. When locked (accent bg), icon should be white. When unlocked, icon should be secondary color. Move color class to button:
     ```tsx
     <button
       class={`rounded px-2.5 py-1.5 cursor-pointer ${
         canvasStore.fitLocked.value
           ? 'bg-[var(--color-accent)] text-white'
           : 'bg-[var(--color-bg-settings)] text-[var(--color-text-secondary)]'
       }`}
       onClick={() => canvasStore.toggleFitLock()}
       title={canvasStore.fitLocked.value ? 'Fit to window \u2014 locked (F)' : 'Fit to window (F)'}
     >
       {canvasStore.fitLocked.value ? <Minimize2 size={16} /> : <Maximize2 size={16} />}
     </button>
     ```

8. Replace "Fullscreen" button (lines 372-380):
   - Remove: `<span class="text-[11px] text-[var(--color-text-secondary)]">{'\u26F6'}</span>`
   - Replace with: `<Maximize size={16} />`
   - Move color to button, keep existing title:
     ```tsx
     <button
       class="rounded px-2.5 py-1.5 bg-[var(--color-bg-settings)] hover:bg-[var(--color-bg-input)] cursor-pointer text-[var(--color-text-secondary)]"
       onClick={() => enterFullscreen()}
       title="Fullscreen (\u21E7\u2318F)"
     >
       <Maximize size={16} />
     </button>
     ```

IMPORTANT: Do NOT modify the timecode display, zoom percentage text, SpeedBadge, FullSpeedBadge, or any non-button elements. Do NOT change the preview/canvas area, zoom handlers, or pan handlers.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CanvasArea.tsx contains `import { Play, Pause, SkipBack, SkipForward, Plus, Minus, Maximize, Maximize2, Minimize2 } from 'lucide-preact'`
    - CanvasArea.tsx does NOT contain `\u23EE` or `\u23ED` or `\u23F8` or `\u25B6` or `\u26F6` or `>Fit<`
    - CanvasArea.tsx contains `<SkipBack size={16}` and `<SkipForward size={16}` and `<Minus size={16}` and `<Plus size={16}` and `<Maximize size={16}` and `<Maximize2 size={16}` and `<Minimize2 size={16}`
    - CanvasArea.tsx contains `<Play size={18}` and `<Pause size={18}` (emphasis play/pause button)
    - CanvasArea.tsx contains `rounded-full bg-[var(--color-accent)]` on the play/pause button (emphasis preserved)
    - CanvasArea.tsx contains `title={isPlaying ? 'Pause (Space)' : 'Play (Space)'}`
    - No `<span` wrappers remain around button icon content in the Preview Controls section
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 7 canvas buttons use lucide-preact SVG icons with title tooltips. Play/pause retains emphasis styling (rounded-full, accent bg, 18px). Fit-lock toggles between Maximize2/Minimize2 icons. No Unicode characters remain in button content. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit` -- TypeScript compiles without errors
2. `grep -c 'lucide-preact' Application/src/components/layout/TimelinePanel.tsx` -- returns 1 (import present)
3. `grep -c 'lucide-preact' Application/src/components/layout/CanvasArea.tsx` -- returns 1 (import present)
4. `grep -c '\u23F8\|\u25B6\|\u23EE\|\u23ED\|\u26F6\|&#9664;\|&#9654;' Application/src/components/layout/TimelinePanel.tsx Application/src/components/layout/CanvasArea.tsx` -- returns 0 (no Unicode chars remain)
</verification>

<success_criteria>
- All 14 buttons (7 timeline + 7 canvas) display lucide-preact SVG icons instead of Unicode characters
- Canvas play/pause button retains its emphasis styling (rounded-full, accent bg, white, larger 18px icon)
- All buttons have title attributes with "Label (Shortcut)" format where applicable
- Text displays (timecode, zoom %, zoom multiplier) remain unchanged
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12.14-timeline-canvas-buttons-enhancements/12.14-01-SUMMARY.md`
</output>
