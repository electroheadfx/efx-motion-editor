---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 03
type: execute
wave: 2
depends_on: [43.5-01]
files_modified:
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
autonomous: true
requirements: [PUSH-05, PUSH-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "prepareRotoPush freezes a publication carrying the proposal, intent, break-aware proposal version, and expected launch tuple; stale authority at commit fails closed with zero mutation (PUSH-05, T-43.5-01)"
    - "Zero-delta push (changed === false) publishes nothing and creates no history entry (D-15)"
    - "commitRotoPush re-validates the break-aware proposal version against current state, forwards retained objects with zero resolver recomputation, and publishes accepted status from the .then continuation — never via runPhysicalAction (bounded Extract)"
    - "One deterministic mapper mapRotoPushProductReason owns live-readout, accepted (singular/plural, gap sentence only when opened), no-space, straddle, empty-anchor, and disabled copy; the no-space sentence reuses the Group mapper's literal source (D-15, D-16, D-17)"
    - "The complete push — records, Loop Clips, breaks, selection, cursor — is one atomic parent-acknowledged history command: one Undo restores it, one Redo reapplies it, including movement-created breaks (PUSH-06)"
    - "Fixed-form readout/status copy flows through the existing aria-live status line; rejections name the problem and leave every visible fact unchanged (UI Considerations: T6 long-text/overflow/error/empty)"
  artifacts:
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — prepareRotoPush, commitRotoPush, mapRotoPushProductReason, break-aware push proposal version
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts — push-rails execute-input arm
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — prepare/commit + mapper coverage
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts — one-Undo/one-Redo push coverage
  key_links:
    - "break collection reaches BOTH the resolver input AND the proposal fingerprint (Pitfall 3) — a concurrent Scissor/break edit invalidates a prepared push"
    - "coordinator execute-input arm: intent.kind === 'push-rails' ⇒ { ...base, operationKind, intent } (auto-extended apply-payload mapped type)"
    - "commit bypasses runPhysicalAction; accepted status publishes only from the .then(accepted => ...) continuation after exact parent acknowledgement"
---

<objective>
Wire the push intent through the prepare/commit publication pair, the single product-reason mapper with the locked copy family, the coordinator execute-input arm, and prove the atomic one-Undo/one-Redo history contract.

Purpose: This is the transaction boundary the drag session (43.5-04) and strip wiring (43.5-05) consume; it carries the stale-authority and zero-mutation guarantees (PUSH-05, PUSH-06, D-15).
Output: prepareRotoPush/commitRotoPush modeled byte-closely on the Group drag pair, with the full locked copy family and history replay tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-RESEARCH.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prepareRotoPush + mapRotoPushProductReason with the locked copy family</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 273-323 product-reason mappers incl. no-space literal source at L281-283, 1650-1758 prepareRotoGroupDrag — guard order L1667-1679, break-aware snapshot L1680-1701, zero-delta path L1717-1720, proposal version L1745, frozen publication L1745-1757)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts (existing prepare/mapper test idioms)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md (D-15/D-16/D-17 locked copy)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md (T6 copy table — verbatim strings)
  </read_first>
  <behavior>
    - Guard order: no launch ⇒ disabled 'Select a real Roto key before editing the timeline.'; missing ports ⇒ 'Timeline editing is unavailable.'; in-flight operation ⇒ 'A Roto physical edit is already in flight.'
    - changed === false ⇒ { ok: false, reason: 'This move would not change the timeline.' } — zero-delta publishes nothing (D-15)
    - Live readout: `Push Right +{D} — frames {A}–{B} → {A′}–{B′}` with `, gap {G1}–{G2}` appended only when a gap would open; `left` mirror for Push Left
    - Accepted plural: `Pushed {N} Rails right by {D} frames — moved set now frames {A}–{B}. Gap opened at frames {G1}–{G2}.` (gap sentence only when a gap opened); singular: `Pushed 1 Rail right by {D} frames — ...`; `left` mirrors
    - No-space: `No empty space in that direction.` via delegation to the Group mapper (one literal source)
    - Straddle: `Can't push: a Group in the moved set shares its source with a fixed Group.` (verbatim, D-16)
    - Empty anchor: `Push is unavailable on an empty frame. Drag from a key or rail.` (D-07/D-17)
    - Ranges in copy are inclusive product frame ranges derived from canonical half-open ranges only at presentation time; frame numbers use tabular-nums at render
  </behavior>
  <action>Add prepareRotoPush to useRotoTimelineActions.ts modeled byte-closely on prepareRotoGroupDrag (L1666-1758): same guard order (launch → ports → no in-flight → identity well-formed), thread incomingInterpolationBreakKeyIds from input.getIncomingInterpolationBreakKeyIds into BOTH the resolvePhysicPaintRotoPhysicalEdit input AND a break-aware proposal fingerprint (copy buildGroupDragProposalVersion's field set: records, interpolation, loopClips, incomingInterpolationBreakKeyIds, launch — Pitfall 3), and return the frozen publication { proposal, intent, proposalVersion, expectedLaunch: { operationId, layerId } }. The prepare input is the push intent (anchor + direction + delta); the resolver (plan 43.5-01) owns set derivation, clamp, straddle, and breaks — prepare does NO set math itself. Add mapRotoPushProductReason beside mapRotoGroupDragProductReason/mapRotoKeyRailDragProductReason owning the entire locked copy family per the behavior block (D-15/D-16/D-17, UI-SPEC T6): live-readout variant, accepted singular/plural with conditional gap sentence, disabled pass-through, no-space delegated to mapRotoGroupDragProductReason so the sentence has one literal source (43.4 precedent), straddle sentence verbatim, empty-anchor sentence verbatim. Raw resolver diagnostics never enter product copy — the detail channel only.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useRotoTimelineActions.ts exports prepareRotoPush and mapRotoPushProductReason
    - Tests assert each behavior bullet verbatim (copy strings compared as exact strings)
    - A test proves the proposal version changes when only the break collection changes (Pitfall 3 regression)
    - A test proves changed === false returns the no-change reason with no publication (D-15)
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>prepareRotoPush produces a frozen break-aware publication or a mapped reason; the mapper owns every push copy string with exact locked wording; all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commitRotoPush + coordinator execute-input arm + one-Undo/one-Redo history proof</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 1760-1809 commitRotoGroupDrag — wrapper coherence, version revalidation, executePhysicalEdit call, .then accepted-status continuation)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts (lines 332-375 execute-input arms, e.g. L349 move-key-rail arm)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts (existing replay test idioms)
  </read_first>
  <behavior>
    - Commit rejects when publication.proposal.status.operationKind or intent.kind is not push-rails (wrapper coherence)
    - Commit rejects when the recomputed current break-aware proposal version differs from the retained one — stale authority ⇒ zero mutation, zero status, zero history (T-43.5-01)
    - Commit forwards the retained proposal/intent/expectedLaunch with zero resolver recomputation
    - Accepted push publishes the accepted copy from the .then continuation; one Undo restores the complete pre-push state including breaks, selection, and cursor; one Redo reapplies
  </behavior>
  <action>Add commitRotoPush modeled byte-closely on commitRotoGroupDrag (L1760-1809): verify wrapper coherence (operationKind === 'push-rails' and intent.kind === 'push-rails', launch tuple non-empty), recompute the current break-aware proposal version from live state and return false on mismatch (stale ⇒ fail closed, zero mutation), then await input.executePhysicalEdit with the retained proposal/expectedLaunch/operationKind/intent/selectedKeyId/selectedAppFrame — zero recomputation — and publish the accepted status via mapRotoPushProductReason from the .then(accepted => ...) continuation, NOT via runPhysicalAction whose kind union is a bounded Extract. Add the push-rails arm to the coordinator's execute-input builder (useRotoPhysicalEditCoordinator.ts L332-375): one if arm returning { ...base, operationKind, intent } when intent?.kind === 'push-rails', else null; the apply-payload mapped type auto-extends. Extend useRotoPhysicalEditHistory.test.ts: a committed push (with an opened gap and its movement-created break) replays through one Undo restoring the complete before state — records, Loop Clip placements, incoming break collection, selection, cursor — and one Redo reapplying it exactly (43.4 D-24 single-command contract; 43.2 history equality includes break ownership).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - useRotoTimelineActions.ts exports commitRotoPush with the wrapper-coherence and version-revalidation guards
    - useRotoPhysicalEditCoordinator.ts contains a push-rails execute-input arm returning null for mismatched kinds
    - History test proves one Undo + one Redo round-trips the complete push including the movement-created break collection, selection, and cursor
    - A test proves a mid-gesture break change (concurrent edit) invalidates the commit with zero mutation
    - Full suite + typecheck green
  </acceptance_criteria>
  <done>The push intent flows through prepare → coordinator → exact parent acknowledgement → one history command with proven Undo/Redo; stale authority fails closed everywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepared publication → commit | Time-of-check to time-of-use: state may change between prepare and commit |
| UI copy → accepted state | Status copy must never claim a mutation the parent did not acknowledge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.5-01 | Tampering | stale/malformed intent replayed into the coordinator | high | mitigate | Break-aware proposal version revalidated at commit; wrapper-coherence kind/intent/launch tuple checks before executePhysicalEdit; stale ⇒ fail closed with zero mutation — Task 2 tests |
| T-43.5-03 | Tampering/Repudiation | partial mutation or phantom status on failure | high | mitigate | Accepted status publishes only from the .then continuation after exact parent acknowledgement; rejection/zero-delta publish zero state and zero history — Tasks 1-2 tests |
| T-43.5-SC | Tampering | npm installs | high | mitigate | No package installs this phase |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` green
- `pnpm --dir app exec vitest run` + `pnpm --dir app typecheck` green before wave merge
</verification>

<success_criteria>
Push commits are atomic, parent-acknowledged, stale-safe, and exactly one Undo/Redo restores/reapplies the complete movement including breaks; every copy surface agrees through one mapper.
</success_criteria>

## Artifacts this phase produces (this plan)

- `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts`: `prepareRotoPush`, `commitRotoPush`, `mapRotoPushProductReason`, break-aware push proposal-version builder
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts`: `push-rails` execute-input arm
- Test coverage in `useRotoTimelineActions.test.ts` and `useRotoPhysicalEditHistory.test.ts`

<output>
Create `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-03-SUMMARY.md` when done
</output>
