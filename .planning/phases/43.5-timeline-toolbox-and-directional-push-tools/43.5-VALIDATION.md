---
phase: 43.5
slug: timeline-toolbox-and-directional-push-tools
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-17
---

# Phase 43.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^2.1.9 (node environment) |
| **Config file** | `app/vitest.config.ts` — `include: ['src/**/*.test.ts']` |
| **Quick run command** | `pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts` |
| **Full suite command** | `pnpm --dir app exec vitest run` (plus `pnpm --dir app typecheck`) |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** targeted module tests (quick run command above)
- **After every plan wave:** `pnpm --dir app exec vitest run` + `pnpm --dir app typecheck`
- **Before `/gsd-verify-work`:** Full suite must be green + `pnpm build`; native UAT is the final oracle (43.2 D-39)
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

(Requirement IDs are planning-local derivations; planner assigns final IDs per plan.)

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 43.5-TBX-01 | TBD | TBD | TBX-01 | — | N/A | unit | `pnpm --dir app exec vitest run src/components/physic-paint/view/` | ❌ W0 | ⬜ pending |
| 43.5-TBX-02 | TBD | TBD | TBX-02 | T-43.5-02 | Popover open must not suspend Delete/Backspace/Cmd+Z routing; Escape layering preserved | unit | `pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts` | ✅ extend | ⬜ pending |
| 43.5-PUSH-01 | TBD | TBD | PUSH-01/02 | T-43.5-01 | Strict intent parse/serialize round-trip; closed-union exhaustiveness; fail-closed on malformed intent | unit | `pnpm --dir app exec vitest run src/components/physic-paint/` (types-adjacent tests) | ❌ W0 | ⬜ pending |
| 43.5-PUSH-03 | TBD | TBD | PUSH-03 | T-43.5-01 | Resolver derives set membership from canonical records only; straddle fail-closed; clamp authorities | unit | `pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` | ✅ extend | ⬜ pending |
| 43.5-PUSH-04 | TBD | TBD | PUSH-04 | T-43.5-01 | prepare/commit revalidates break-aware proposal version; stale ⇒ zero mutation | unit | `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts` | ✅ extend | ⬜ pending |
| 43.5-PUSH-05 | TBD | TBD | PUSH-05 | — | N/A | unit | `pnpm --dir app exec vitest run src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts` | ❌ W0 | ⬜ pending |
| 43.5-PUSH-06 | TBD | TBD | PUSH-06 | T-43.5-03 | One Undo/Redo restores complete push incl. breaks; rejection publishes zero state and zero history | unit | `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts` | ✅ extend | ⬜ pending |
| 43.5-UAT | TBD | TBD | All | — | N/A | manual | user-run native UAT (13-step prompt list + UI-SPEC matrix) from fully restarted frozen-code session | ❌ phase gate | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts` — new session-hook suite (mirror `usePhysicsPaintKeyRailDrag.test.ts` structure)
- [ ] Push intent parse/serialize tests (extend types-adjacent suite or resolver test file)
- [ ] Resolver push branch tests: set derivation, straddle rejection, clamp boundaries (frame 0 / capacity), gap break ownership both directions, reverse-close normalization, zero-delta no-change, duplicated-placement-only sets (Pitfall 5)
- [ ] Keyboard dispatcher test: popover-open Delete routing (Pitfall 1) and Escape layering (Pitfall 2) — extend `physicsPaintStudioKeyboard.test.ts`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Native visual UAT (13-step prompt list + UI-SPEC matrix) | All | Visual/interaction oracle per 43.2 D-39; automated tests cannot judge rendered popover/drag preview fidelity | Run from fully restarted frozen-code session; follow UI-SPEC acceptance matrix |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
