---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-05]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A push-rails intent round-trips through the strict parser and canonical serializer with allowlisted keys only (strict transport parsing discipline)"
    - "Push Right from an anchor Rail translates the anchor Rail and every Rail after it by one signed delta; Push Left translates every Rail before the anchor plus the anchor; the opposite side is byte-position fixed (PUSH-01)"
    - "Records, Loop Clip placements, phaseOrigin, originalEndExclusive, every visibleRanges entry, and every frameOverrides appFrame translate by the same delta; Repeat/Infinity/Motion/Static type/provenance/sync/source sharing are untouched (PUSH-02)"
    - "A push-opened gap is recorded as a complete-collection replacement of incomingInterpolationBreakKeyIds: moved set's first key owns the break for Push Right, fixed right side's first key owns/reuses it for Push Left, reverse-close normalizes, existing breaks are reused never duplicated (PUSH-03)"
    - "One exported pure clamp authority bounds Push Left at frame 0 and Push Right at capacity/parent end with directional nearest-free search; ok:false means zero valid movement (PUSH-05)"
    - "A source-attached Group in the moved set sharing sourceCycleId with any fixed-side Group fails closed with zero mutation and zero partial proposal (PUSH-05, D-16)"
    - "Set membership, attachment, and straddle derive from canonical records/loopClips only — never from caller-supplied flags (Pitfall 6)"
    - "A moved set whose only movable content is duplicated placements still reports changed from the placement delta (Pitfall 5)"
  artifacts:
    - app/src/types/physicPaint.ts — push-rails union member, parser branch, serializer case
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — push-rails operation kind, dispatch branch, clamp authority, set/straddle derivation, break derivation
    - app/src/types/physicPaint.test.ts — intent parse/serialize round-trip coverage
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — push branch coverage
  key_links:
    - "intent union member ↔ isPhysicPaintRotoPhysicalEditIntent branch ↔ serializePhysicPaintRotoPhysicalEditIntent case ↔ resolver operation-kind union ↔ dispatch branch (compile fails closed if any disagree)"
    - "clamp authority exported from the resolver is the SAME function the strip preview will consume in plan 05 (preview-is-the-commit)"
    - "straddle/set derivation is one shared pure export consumed by the resolver branch now and the hover preflight port in plan 05 (D-17: preflight and rejection can never disagree)"
---

<objective>
Add the `push-rails` physical edit intent to the closed transport union and implement its pure resolver branch: directional Rail-set derivation, rigid atomic translation, clamp authority, shared-source straddle guard, and opened/closed gap break derivation.

Purpose: The resolver is the sole physical edit authority; every later surface (prepare/commit in 43.5-03, drag session in 43.5-04, strip paint in 43.5-05) consumes the pure exports this plan creates. Getting set derivation, straddle, and break ownership right here is the phase's composition risk (RESEARCH key insight).
Output: A fail-closed `push-rails` intent that resolves a complete immutable proposal (records + Loop Clips + lifecycle fields + complete break collection) or a typed failure, fully unit-tested.
</objective>

<assumption_delta_decision>
Detector fired on pluralization terms ("no second state authority", "no multiple internal Paint tracks"). Both are prohibitions restating existing invariants, not singular→plural transitions. Primary noun: the single timeline-owned state authority (and single Paint track). Decision: no-change — this phase preserves the monopoly; the popover is an editing surface only (D-03) and Push derives its set from the anchor position, never a second selection authority (D-20).
</assumption_delta_decision>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-RESEARCH.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end push-rails intent — transport union through resolver happy path (Push Right suffix translation)</name>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts (lines 359, 382-445, 447-585, 599-650, 910-917 — capacity constant, intent union, strict parser, canonical serializer, apply-payload mapped type)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 172-186, 818-827, 1862-1991, 2053, 2292-2355, 3840-3980 — operation-kind union, isResolverOperationKind, buildMoveGroupNextLoopClips/buildMoveGroupClipCandidate, attachment test, break derivation, finalizeProposal call sites)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md (verbatim analog excerpts for all five surfaces)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (existing move-group/move-key-rail test structure to mirror)
  </read_first>
  <behavior>
    - RED: resolver test — intent `{ kind: 'push-rails', direction: 'right', anchorKeyId: <first key of mid Key Rail>, deltaFrames: 5 }` on a fixture of [Key Rail A (frames 0-9)][Key Rail B (frames 20-29)] resolves ok with Key Rail B's keys at 25-34 and Key Rail A byte-position fixed
    - RED: types test — the intent round-trips `isPhysicPaintRotoPhysicalEditIntent` and `serializePhysicPaintRotoPhysicalEditIntent`; an intent with an extra key or unbounded ID fails parsing
    - GREEN: minimal implementation across all five surfaces makes both tests pass
  </behavior>
  <action>Add one intent member to the closed union per RESEARCH Pattern 1, copying the move-key-rail lifecycle exactly across all five surfaces: (1) union member in app/src/types/physicPaint.ts — shape `{ readonly kind: 'push-rails'; readonly direction: 'right' | 'left'; readonly anchorKeyId?: string; readonly anchorLoopId?: string; readonly deltaFrames: number }` (exactly one of anchorKeyId/anchorLoopId present); (2) strict parser branch with hasOnlyKeys([...]) allowlist, bounded anchor ID via the existing isBoundedPhysicalKeyId-family helpers, isNonNegativeInteger(deltaFrames), direction literal check, and XOR on the anchor pair — never normalize or coerce; (3) canonical serializer case in the exhaustive switch (missing case = compile error); (4) add 'push-rails' to PhysicPaintRotoPhysicalEditOperationKind and isResolverOperationKind; (5) resolver dispatch branch before the terminal fail-closed guard. The happy-path branch: resolve the anchor to its containing Rail (ordinary key/Key Rail member via deriveKeyRailSegments; anchorLoopId via derivePhysicPaintRotoLoopRanges), derive the directional suffix set (Push Right: anchor Rail + every Rail whose interval starts at/after the anchor Rail's start — RESEARCH A3: the anchor Rail's interval is the pivot, empty space belongs to neither side), compute moved keys (source keys of source-attached Groups per the canonical attachment test frameByKeyId.get(clip.sourceKeyIds[0]) === clip.placementStart at resolver L2053, Key Rail member keys, ordinary keys; duplicated placements contribute NO moved keys), translate every moved identity by +deltaFrames using the buildMoveGroupClipCandidate algebra (resolver L1924-1991) and every moved clip's lifecycle fields using the buildMoveGroupNextLoopClips pattern (L1862-1922: phaseOrigin, originalEndExclusive, EVERY visibleRanges entry, EVERY frameOverrides appFrame, infinity pinning), then finalizeProposal with the existing incomingInterpolationBreakKeyIds threaded as its last parameter. Do NOT copy buildMoveGroupClipCandidate's duplicate-destination-frame collision rejection — push clamps and commits (43.3 critical deviation; the clamp lands in Task 2, so for the tracer use the unclamped happy path with over-capacity fail-closed already present in the algebra). changed must be true when any key moved OR any clip placement changed (destination !== clip.placementStart) — Pitfall 5. Per D-16 the straddle guard lands in Task 2; the tracer fixture uses source-free Key Rails only.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/src/types/physicPaint.ts contains kind: 'push-rails' in the intent union, a hasOnlyKeys parser branch, and an exhaustive-serializer case for it
    - physicsPaintRotoPhysicalResolver.ts contains 'push-rails' in PhysicPaintRotoPhysicalEditOperationKind, isResolverOperationKind, and a dispatch branch
    - RED-then-GREEN: the new tests failed before implementation and pass after
    - pnpm --dir app typecheck exits 0 (exhaustive switch proves all surfaces extended)
    - The happy-path test asserts Key Rail A's frames are unchanged (byte-position fixed opposite side, PUSH-01)
  </acceptance_criteria>
  <done>push-rails intent parses, serializes, and resolves a Push Right suffix translation end-to-end through the pure resolver with lifecycle fields translated; all extended tests green; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Directional set/straddle/clamp authority — shared pure derivation, Push Left prefix, break open/close derivation</name>
  <reversibility rating="one-way">D-16 straddle rejection semantics join the resolver guard vocabulary and product copy; decision already user-locked verbatim in CONTEXT (door walked through at discuss-phase — no checkpoint re-asked).</reversibility>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 2053, 2126-2203, 2292-2355, 4064 — attachment test, clampPhysicPaintKeyRailDragDestination + nearest-free search loop, deriveMoveGroupIncomingInterpolationBreakKeyIds rules (a)/(b), PhysicPaintRotoLoopRange.sourceCycleId derivation)
    - app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.ts:42 (getPhysicsPaintRotoSourceCycleId)
    - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts:37-72 (deriveKeyRailSegments)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md (clamp + break derivation verbatim excerpts and CRITICAL DEVIATIONS)
  </read_first>
  <behavior>
    - Push Left from anchor Rail moves every Rail whose interval ends at/before the anchor Rail's end, plus the anchor; right side byte-position fixed
    - Clamp: Push Left clamps at frame 0; Push Right clamps at capacity/parent end (physical capacity is the single end authority — never the main-editor display outFrame); directional nearest-free search copied from L2193-2202; ok:false when zero valid movement exists
    - Straddle: a source-attached Group in the moved set whose sourceCycleId equals any fixed-side Group's sourceCycleId ⇒ fail closed with a new typed failure code (e.g. 'push-source-straddle' joining the closed PhysicPaintRotoPhysicalEditFailureCode union); duplicated placements never straddle
    - Breaks: Push Right — moved set's first key owns the opened-gap break (travels with the set); Push Left — fixed right side's first key owns/reuses it; existing breaks reused never duplicated; a reverse push closing a gap normalizes the collection; breaks owned by moved keys travel with stable identity
  </behavior>
  <action>Extend the resolver branch with three pure exported authorities, all derived from canonical records/loopClips only (Pitfall 6 — the view will pass anchor + delta, never attachment flags). (1) One shared pure derivation export (name at discretion, e.g. derivePhysicPaintPushSet) mapping (anchor descriptor, direction, ordered real keys, loopRanges, incomingInterpolationBreakKeyIds, capacity) to the directional Rail set plus a straddle verdict; anchor resolution accepts either a keyId (ordinary/Key Rail member) or a loopId (Motion/Static Rail) and a frame inside a Group resolves to the complete Group — never an implicit cut (D-07). This export is consumed by the resolver branch now and by the strip hover preflight port in plan 05, so preflight and rejection can never disagree (D-17, Pitfall 4). (2) One exported pure clamp authority (e.g. clampPhysicPaintPushDestination) modeled on clampPhysicPaintKeyRailDragDestination (L2126-2203): Push Right boundary = capacity/parent end, Push Left boundary = frame 0, copying the directional nearest-free search loop verbatim; result ok:false maps to failure code no-free-space-in-direction. Wire the clamp INTO the dispatch branch so the committed delta is the clamped delta (preview-is-the-commit, D-14). (3) Break derivation generalizing deriveMoveGroupIncomingInterpolationBreakKeyIds (L2292-2355): complete-collection replacement, never a delta; apply rules (a) vacated-gap ownership is a pre-move fact and (b) landing-gap ownership belongs to the moved set's first source key for Push Right, and to the fixed right side's first key for Push Left (CONTEXT carried D); deterministic ascending-frame sort; thread the result into finalizeProposal at the push call site. Group-local visibleRanges fragments move rigidly with their Group and NEVER convert to key-owned breaks (43.3 D-13).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintRotoPhysicalResolver.ts exports the shared set/straddle derivation and the pure push clamp; the dispatch branch consumes both (no second clamp math anywhere)
    - Tests cover: Push Left prefix semantics; clamp at frame 0 and at capacity/parent end; ok:false on zero valid movement; straddle fail-closed with the new failure code and no partial proposal; duplicated-placement straddle impossible; break ownership for both directions; existing break reuse; reverse-push close normalization; break on a moved key travels with stable identity
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Both directions resolve with clamp-and-commit semantics, straddle rejects fail-closed from canonical facts, and the complete next break collection is derived per the 43.1 ownership rules; all resolver tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-closed edges — duplicated-placement-only sets, zero-delta no-change, malformed intent, boundary fixtures</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts, app/src/types/physicPaint.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 276-301 failure-code union, 313-327 resolution shape, 3911 changed-comment precedent)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (existing fail-closed test idioms)
  </read_first>
  <behavior>
    - A moved set containing only duplicated placements still resolves changed:true from the placement delta (Pitfall 5)
    - Zero-delta (clamped to current position) resolves as a valid no-change (status.changed === false), never a failure
    - Malformed intents (extra keys, missing anchor pair, both anchors, negative/non-integer delta, unbounded IDs, unknown direction) fail strict parsing
    - Frame-0-adjacent Push Left with no leading space and capacity-adjacent Push Right with no trailing space fail closed with no-free-space-in-direction and zero mutation
  </behavior>
  <action>Close out the resolver branch's fail-closed matrix. Verify changed accounts for nextLoopClips placement deltas (destination !== clip.placementStart), not only the key mapping — fix the computation if the Task 1/2 implementation misses it. Confirm the zero-delta path flows through the existing valid-no-change channel (the prepare layer in plan 03 maps changed === false to the no-publish result; the resolver itself must not fail on it). Add the malformed-intent parser cases to app/src/types/physicPaint.test.ts mirroring the existing per-kind negative tests. Add boundary fixtures: anchor at frame 0 pushing left, anchor's set already at capacity pushing right. Every failure branch carries a typed code in the closed union and never a partial proposal (fail-closed resolution shape, resolver L313-327). Do not weaken any existing move-group/move-key-rail behavior — direct Group/Key Rail drag stays local and never pushes neighbors (PUSH-08 regression boundary: this plan adds a new kind; it modifies no existing dispatch branch semantics).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tests exist and pass for each behavior bullet above
    - grep -v '^//' on the resolver diff shows no edit inside buildMoveGroupCandidate/buildMoveGroupClipCandidate/clampPhysicPaintKeyRailDragDestination/deriveMoveGroupIncomingInterpolationBreakKeyIds bodies (regression boundary PUSH-08) — push composes them, never rewrites them
    - Full targeted suite + typecheck green
  </acceptance_criteria>
  <done>The push resolver branch is fail-closed on every malformed/stale/boundary input, zero-delta is a valid no-change, duplicated-placement-only sets commit, and no existing resolver behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/bridge → resolver | The push intent crosses from UI-originated state into the pure physical authority; all fields are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.5-01 | Tampering | push-rails intent parsing/serialization | high | mitigate | Strict hasOnlyKeys allowlist parser, bounded IDs (PHYSIC_PAINT_MAX_APPLY_FRAMES), exhaustive canonical serializer, fail-closed dispatch — Tasks 1 and 3 negative tests |
| T-43.5-02 | Tampering | anchor/attachment authority | high | mitigate | Resolver derives set membership, attachment, and straddle from canonical records/loopClips only; the intent carries anchor + delta, never attachment or set flags (Pitfall 6) — Task 2 |
| T-43.5-03 | Tampering/Repudiation | partial mutation on failure | high | mitigate | Failure branch never carries a partial proposal; zero valid movement and straddle produce zero mutation and zero history — Tasks 2 and 3 |
| T-43.5-SC | Tampering | npm installs | high | mitigate | No package installs this phase (lucide-preact 0.577.0 already installed, exports verified); any install attempt is out of scope |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` green
- `pnpm --dir app typecheck` green (closed-union exhaustiveness proves all five surfaces)
- Full suite `pnpm --dir app exec vitest run` green before wave merge
</verification>

<success_criteria>
A `push-rails` intent resolves end-to-end through the pure resolver for both directions with clamp-and-commit, straddle fail-closed, and 43.1-conformant break derivation; every malformed/boundary input fails closed; no existing intent behavior changed.
</success_criteria>

## Artifacts this phase produces (this plan)

- `app/src/types/physicPaint.ts`: union member `{ kind: 'push-rails', direction, anchorKeyId?, anchorLoopId?, deltaFrames }`; parser branch in `isPhysicPaintRotoPhysicalEditIntent`; serializer case in `serializePhysicPaintRotoPhysicalEditIntent`
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts`: `'push-rails'` in `PhysicPaintRotoPhysicalEditOperationKind` + `isResolverOperationKind`; dispatch branch; exported pure clamp authority `clampPhysicPaintPushDestination`; exported shared set/straddle derivation `derivePhysicPaintPushSet`; push break derivation; new failure code `'push-source-straddle'` in `PhysicPaintRotoPhysicalEditFailureCode`
- Test coverage in `app/src/types/physicPaint.test.ts` and `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts`

<output>
Create `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-01-SUMMARY.md` when done
</output>
