# Phase 43.5 — Native Visual UAT (plan 06)

> **Requirement:** run this from a **fully restarted frozen-code parent/child session** — never an active hot-reload state (43.2 D-39). The Push tool armed state is session-only (D-19), so a fresh session proves save/reopen starts disarmed.
>
> **Frozen candidate commit:** `05c5fd4e` — automated gate green: `vitest run` (2383 passed), `typecheck`, `build` all exit 0.
>
> Record pass/fail per row below; screenshots/notes for any failure.

---

## Part A — 13 prompt acceptance steps

| # | Step | Pass/Fail | Notes |
|---|------|-----------|-------|
| 1 | ToolCase "Tools" button opens the relocated **Interpolation** and **Key Spacing** controls with unchanged values and behavior; popover anchors above the strip; rails/cells/spacing proxies stay visible and clickable while open. | | |
| 2 | Existing shortcuts, partial spacing, Group Rail-owned multi-Group spacing, guards, and **Undo/Redo** remain intact (incl. Cmd+Z and Delete/Backspace while the popover is open). | | |
| 3 | Direct dragging of any Motion/Static/Key **Group still moves only that Group** inside free space (no neighbor push). | | |
| 4 | **Push Right** from Group #2 moves #2 and everything after it; Group #1 stays byte-position fixed. | | |
| 5 | **Push Left** from Group #2 moves Group #1 plus #2; everything after #2 stays fixed. | | |
| 6 | **Motion, Static, and Key Rails** all participate in either directional set. | | |
| 7 | A frame inside a Group targets the whole Group — never an implicit cut. | | |
| 8 | Every push-opened gap stays empty with **Interpolation On** and survives Interpolation Off/On exactly; neither tool toggles Interpolation. | | |
| 9 | Group synchronization status, Action provenance, source sharing, and internal timing unchanged after pushes. | | |
| 10 | Frame-0/capacity clamp commits at the clamp (red bar shown); zero-space drag publishes `No empty space in that direction.` with zero mutation; straddled anchor hover shows not-allowed with the straddle tooltip and never starts a drag; zero-delta commit publishes nothing. | | |
| 11 | **One Undo/Redo** restores and reapplies the complete push including movement-created breaks, selection, and cursor. | | |
| 12 | **Save/reopen** (starts disarmed), Studio playback, preview, and export preserve moved sets and exact gaps. | | |
| 13 | Toolbar readable, unclipped, and usable at 1280x720; strip stays exactly **161px**. | | |

---

## Part B — UI-SPEC Visual Acceptance Matrix (visual rows)

| # | Row | Pass/Fail | Notes |
|---|-----|-----------|-------|
| M1 | ToolCase badge on/off at a glance (Blend active tint family). | | |
| M2 | Push button armed tint + directional grab cursor; disarmed returns to the rest idiom. | | |
| M3 | Armed-anchor pre-highlight: per-kind brightening + pivot tick; fixed side dark; **never** selection orange. | | |
| M4 | **Mode toggle:** Push arms freely with no Rail selected; grabbing any rail selects it and moves its set. | | |
| M5 | Push ghosts at **55%** kind color at the clamped destination; originals stay **100%** until commit. | | |
| M6 | `roto-fill-empty` gap preview for the would-open interval (byte-identical gap treatment). | | |
| M7 | Red **blocked-edge bar** (2×12px #FF6B6B) on the moved set's outermost clamped ghost edge. | | |
| M8 | Live status readout during the drag only (`Push Right +{D} — frames {A}–{B} → {A′}–{B′}`, `, gap {G1}–{G2}`); no status change on arm/hover. | | |
| M9 | Anchor Rail's orange capsule + selected key box stays visible above hover/ghost through armed→drag→commit. | | |
| M10 | **Stay armed after commit:** Push stays in push mode for chained pushes; disarms only via Escape / another tool / re-click. | | |

---

## Result

- [ ] All rows pass → type "approved" to close plan 43.5-06.
- [ ] A row failed → describe it (with screenshot) for gap-closure planning.
