---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 05
type: execute
wave: 3
depends_on: [43.5-02, 43.5-03, 43.5-04]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  # NEW FILE: armed-tool session-state module (session-only Signals, focused sibling
  # per 43.4-02 precedent; exact name at executor's discretion)
  - app/src/components/physic-paint/view/physicsPaintPushArmedTool.ts
autonomous: true
requirements: [PUSH-04, PUSH-07, PUSH-08, TBX-05]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Push Right / Push Left sit as one pill group in the freed toolbar space beside the ToolCase button; the strip stays exactly 161px; the toolbar is readable and unclipped at 1280x720; nothing covers cells or rails (D-05, TBX-05)"
    - "Clicking a Push button arms the tool: armed tint, aria-pressed=true, directional cursor (e-resize / w-resize); it stays armed until Escape, re-click, arming the other Push tool, any other toolbar action including Select All, or entering a mutation-locked state (D-06, D-18, D-20)"
    - "Armed state is session-only and never persisted — save/reopen always starts disarmed; nothing about arming enters the document, history, or session restore (D-19)"
    - "While playback/busy locks mutations both Push buttons are disabled with the existing mapped key-tool lock reason; a lock transition disarms an armed tool (D-18)"
    - "Hovering a valid anchor while armed pre-highlights exactly the prospective set's rails (per-kind hover brightening, never orange) plus the 2x8px pivot tick at the set's boundary edge; the fixed side shows nothing; empty/gap or straddled anchors show not-allowed with the guarded tooltip carrying the mapped reason; no status-line change on hover (D-07, D-13, D-17)"
    - "A straddled anchor never starts a drag — hover preflight and commit rejection share the same pure derivation from plan 43.5-01 and the same mapper copy (D-16, D-17, Pitfall 4)"
    - "During the drag every moved-set rail ghosts at 55% kind-color opacity at the clamped destination, the would-open gap previews as roto-fill-empty cells, a 2x12px #FF6B6B bar marks the clamped blocked edge, originals stay at 100% until commit, and the status line carries the live readout `Push Right +{D} — frames {A}–{B} → {A′}–{B′}`-form only during the drag (D-11, D-12, D-14)"
    - "With zero rails the Push buttons remain visible and every hover shows the empty-anchor guard; armed state shows the tint, disarmed returns to the rest idiom (UI Considerations: T3 empty/error/populated/long-text)"
    - "Pre-highlight zero-one-many: one rail brightens one rail, many brighten every set rail with the pivot tick at the boundary; a straddled anchor shows no highlight at all — partial highlight states do not exist (UI Considerations: T4 zero-one-many/partial)"
    - "Ghosts clip at the shared horizontal viewport; clamped drag paints the bar on the moved set's outermost blocked ghost edge; gap preview shows zero cells for adjacent landing and every covered cell uniformly for a multi-frame interval (UI Considerations: T5 populated/partial/overflow, gap preview zero-one-many)"
    - "After an accepted push the anchor rail stays selected, the physical cursor stays, no automatic navigation occurs, and the canvas reconciles from the accepted document only on content change (43.3 D-17 carried)"
    - "Push tools expose no keyboard shortcut this phase — activation is the toolbar buttons plus guarded tooltips only; the keyboard dispatcher gains disarm handling but no arm/push binding (D-10, 43.4 D-05 Scissor precedent)"
    - "Direct Group/Key Rail drag remains local and disarmed-only — ordinary rail drag never pushes neighbors (PUSH-08)"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — Push pill, armed wiring, hover pre-highlight, ghost/gap/blocked-edge paint, live readout
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — armed-state disarm wiring on lock transitions (wiring only)
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts — armed-disarm Escape layer, Select All disarm
    - app/src/components/physic-paint/physicsPaintStudio.css — additive armed tint, pivot tick, push ghost, blocked-edge classes
    - app/src/components/physic-paint/view/physicsPaintPushArmedTool.ts — NEW armed-tool session-state module (session-only Signals; exact name at executor's discretion per 43.4-02 precedent)
  key_links:
    - "armed/pre-highlight/preview state lives in session-only Signals in a focused sibling module — never in PhysicsPaintStudio.tsx logic, never persisted (D-19, 43.4-02 precedent)"
    - "strip preview consumes the SAME exported pure clamp from the resolver (43.5-01) — preview-is-the-commit; ghost destination is never recomputed in the view"
    - "hover preflight consumes the SAME shared set/straddle derivation (43.5-01) as the resolver branch — preflight and rejection can never disagree (D-17, Pitfall 4)"
    - "session hook ports wired: prepareAtDestination = prepareRotoPush, onDropCommit = commitRotoPush (43.5-03), clampDestination = resolver pure clamp (43.5-01)"
---

<objective>
Wire the Push tools into the Studio UI: the armed Push pill, armed-tool lifecycle and disarm rules, hover pre-highlight with straddle preflight, and the full drag preview paint (ghosts, gap, blocked edge, live readout).

Purpose: This is the user-facing surface of the Push workstream — it composes the resolver authority (43.5-01), the transaction pair and copy mapper (43.5-03), and the gesture session (43.5-04) into the armed directional tools, in the toolbar space freed by the toolbox relocation (43.5-02).
Output: Fully interactive Push Right / Push Left tools with locked presentation semantics and all disarm/guard rules wired.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-01-SUMMARY.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-02-SUMMARY.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-03-SUMMARY.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end armed Push — pill, armed lifecycle, and a real drag committing through the wired ports</name>
  <reversibility rating="costly">D-06 armed-mode joins the Studio tool/session state surface, toolbar active-state presentation, shortcut/disarm routing, and UAT; converting to one-shot gestures later would rework every interaction test. Decision user-locked at D-06.</reversibility>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/physicsPaintPushArmedTool.ts (new — exact name at executor's discretion per 43.4-02 precedent)</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 337-339 buildGuardedActionTooltipCopy, 1848-1876 icon-button idiom, 2102-2142 freed bottom-row space after plan 43.5-02, rail drag wiring and preview consumption L657-1007)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 564-567 mutation-lock authority; strip prop wiring)
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts (lines 129-155 Select All + Escape branches after plan 43.5-02)
    - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts (port interface — plan 43.5-04)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (prepareRotoPush/commitRotoPush signatures — plan 43.5-03)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md (T3 geometry/color/copy, armed lifecycle, keyboard contract)
  </read_first>
  <action>Add the Push Right / Push Left pill group in the freed bottom-row space beside the ToolCase button (D-05, TBX-05): two physics-paint-roto-key-icon-button instances (26px, 18px ArrowRightFromLine / ArrowLeftFromLine lucide-preact icons — exports verified in installed 0.577.0, 10px labels `Push Right` / `Push Left`) with the guarded-tooltip idiom (enabled tooltips `Push the anchor Rail and everything after it to the right.` / `Push everything before the anchor Rail, and the anchor, to the left.`; disabled under mutation lock carrying the existing mapped key-tool lock reason verbatim — D-18). Create the armed-tool state in a focused sibling module (session-only Signals: armed direction or null; NEVER in PhysicsPaintStudio.tsx logic, NEVER persisted — D-19, 43.4-02 precedent). Arm/disarm rules (D-06, D-18, D-20): click arms (armed tint rgba(184,199,255,0.2) background / #B8C7FF border / #DBE4FF icon+label, aria-pressed=true); re-click disarms; arming the other Push tool switches direction in one click; any other toolbar action disarms; Select All disarms (extend the dispatcher's Select All branch L129-137); entering a locked state disarms (wire the disarm into PhysicsPaintStudio.tsx's lock transition at L564-567 — wiring only); Escape layering: active drag (capture, already wins) > popover dismiss > armed disarm > collapseRotoSelection, one Escape handles at most one layer and armed disarm must NOT also collapse selection (Pitfall 2). Directional cursor while armed: e-resize for Push Right, w-resize for Push Left over the strip; not-allowed over invalid anchors. Wire the session hook ports end-to-end: clampDestination delegates to the resolver's exported pure clamp (43.5-01), prepareAtDestination to prepareRotoPush, onDropCommit to commitRotoPush (43.5-03), onRejected publishing through the existing status channel — so arming Push Right and dragging a valid anchor past 4px performs a REAL push through the full resolver/coordinator/history stack. Anchor resolution at the strip: any non-empty physical cell (real key, generated frame inside a Group, linked occurrence) or rail band resolves to the containing Motion/Static/Key Rail via the shared derivation from 43.5-01; a frame inside a Group targets the complete Group (D-07). After an accepted push: anchor rail stays selected, cursor stays, no automatic navigation, canvas reconciles only on content change (43.3 D-17). Ordinary rail drag remains disarmed-only — the push session originates exclusively from armed state (PUSH-08). Extend physicsPaintStudioKeyboard.test.ts: Select All disarms an armed tool; Escape disarms without collapsing selection; lock transition disarms. All armed/preview state in Signals consumed for paint only — no useEffect-mirrored state (CLAUDE.md Preact rules).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts src/components/physic-paint/hooks/ && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip.tsx renders the Push pill with both buttons, armed tint, aria-pressed, and guarded tooltips per the locked copy
    - grep confirms armed state is Signals-only and absent from persistence/history modules (D-19): no armed-tool field in any serialized document or history snapshot type
    - Keyboard tests pass: Select All disarm, Escape layering (disarm without selection collapse), lock-transition disarm
    - The wired session ports call prepareRotoPush/commitRotoPush and the resolver clamp — no clamp or set math reimplemented in the strip (grep: no inline nearest-free loops in PhysicsPaintWorkflowStrip.tsx)
    - usePhysicsPaintGroupRailDrag.ts and usePhysicsPaintKeyRailDrag.ts byte-untouched (PUSH-08)
    - No keyboard shortcut arms or triggers the Push tools (D-10): grep confirms physicsPaintStudioKeyboard.ts gains only disarm handling (Escape layer, Select All disarm) — no key binding activates or commits a push
    - Full suite + typecheck green
  </acceptance_criteria>
  <done>A user can arm Push Right, drag a valid anchor past 4px, and drop to commit a real atomic push; every disarm rule fires; the strip stays 161px with the pill in the freed space.</done>
</task>

<task type="auto">
  <name>Task 2: Hover pre-highlight + straddle preflight + drag preview paint (ghosts, gap, blocked edge, live readout)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (rail hover/ghost paint plumbing, collectRotoDragVacatedAppFrames usage, status-line publication path)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (lines 720-741 collectRotoDragVacatedAppFrames — gap-preview cell paint precedent)
    - app/src/components/physic-paint/physicsPaintStudio.css (line 2993 .roto-fill-empty; rail kind base colors; hover brightening idioms)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md (T4/T5/T6 geometry, color, state precedence, layer order — locked)
  </read_first>
  <action>Implement the armed hover pre-highlight (D-13, UI-SPEC T4): while armed, hovering a valid anchor brightens every rail in the prospective moved set using the established per-kind hover brightening (Key Rail #A7B0B9; Motion/Static per 43-UI-SPEC), paint-only on the existing 3px rail geometry — never selection orange, never a ghost, no translation or height change — plus the pivot boundary marker: 2px wide x 8px high #F2F5F7 at 70% opacity in the lower half of the 12px rail band (y 4-12), at the left edge of the anchor rail's first cell for Push Right, at the right edge of the anchor rail's last cell for Push Left; the fixed side shows nothing. The prospective set derives from the SAME shared pure derivation the resolver uses (43.5-01) — never reimplemented in the view (Pitfall 4). Hovering an empty/gap frame shows the not-allowed cursor and no highlight, with the guarded tooltip `Push is unavailable on an empty frame. Drag from a key or rail.` (D-07/D-17). Hovering a straddled anchor shows not-allowed and the guarded tooltip `Can't push: a Group in the moved set shares its source with a fixed Group.` (verbatim, D-16/D-17) — the drag never starts. Pre-highlight clears on mouse leave and disarm, is replaced by ghosts past the threshold, and produces NO status-line change (D-13). Implement the drag preview paint (D-11/D-12/D-14, UI-SPEC T5/T6): per-rail ghosts at the clamped destination in each rail's kind base color at 55% opacity (Motion #8B5CF6, Static #06B6D4, Key Rail #8A939C), aria-hidden, pointer-events none, never orange/endpoint-cuts/lifecycle-dots; originals stay at 100% until exact parent acknowledgement (Pitfall 9); the would-open gap previews as roto-fill-empty cells via the collectRotoDragVacatedAppFrames precedent (border #3D454D, background #4D535A — byte-identical gap treatment); the clamped blocked edge paints a 2x12px #FF6B6B bar on the moved set's outermost blocked ghost edge (left edge of leftmost ghost for Push Left, right edge of rightmost ghost for Push Right — 43.3 G4 blocked-edge direction rule); the live readout publishes to the existing aria-live=polite status line at pointer-move cadence ONLY during the drag: `Push Right +{D} — frames {A}–{B} → {A′}–{B′}` with `, gap {G1}–{G2}` appended only when a gap would open (mapper from 43.5-03 owns the string). Layer order per UI-SPEC: hover/pivot on rail layer 7, ghosts and blocked edge between 7 and 8, gap preview on the cell layer beneath all rail paint; all preview paint removed on commit/cancel/rejection. CSS strictly additive (armed tint, pivot tick, push ghost, blocked-edge classes); zero strip geometry change; state precedence per UI-SPEC (focus ring > selected > pre-highlight > base > ghost > gap). Frame numbers in readout copy use tabular-nums. Under prefers-reduced-motion all changes are immediate paint with no transitions.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run && pnpm --dir app typecheck && pnpm --dir app build</automated>
  </verify>
  <acceptance_criteria>
    - Strip paint consumes only session preview Signals and the retained publication — originals never dim or translate before acceptance (grep: no accepted-rail paint path reads preview state)
    - The straddle and empty-anchor guarded tooltips carry the exact locked strings from mapRotoPushProductReason (one copy owner; no string literals duplicated in the strip)
    - CSS diff is additive only (no modified existing selectors); strip height stays 161px
    - Full suite, typecheck, and build green
  </acceptance_criteria>
  <done>Hover pre-highlight, straddle preflight, per-rail ghosts, gap preview, blocked-edge bar, and live readout all render per the locked UI-SPEC with presentation-only state; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer/hover position → preflight/preview | Pointer position is untrusted; preflight must never authorize what the resolver would reject |
| session Signals → accepted document | Preview state must never cross into canonical state before exact parent acknowledgement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.5-02 | Tampering | UI-derived anchor/attachment authority | high | mitigate | Strip passes anchor + delta only; set membership, attachment, and straddle derive from canonical facts in the resolver; hover preflight consumes the same pure derivation (Pitfalls 4/6) |
| T-43.5-03 | Tampering/Repudiation | optimistic paint before acknowledgement | medium | mitigate | Originals stay at 100%; all preview paint aria-hidden and session-only; commit consumes only the retained publication (Pitfall 9, 43.3 D-01/D-17) |
| T-43.5-06 | Tampering | armed state leaking into persistence/history | medium | mitigate | Armed state is session-only Signals; acceptance criterion greps serialized document/history types for armed-tool fields (D-19) |
| T-43.5-SC | Tampering | npm installs | high | mitigate | No package installs this phase (lucide-preact arrow icons verified in installed 0.577.0) |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run` + `pnpm --dir app typecheck` + `pnpm --dir app build` green
- Keyboard disarm-layering tests green
- Visual/interaction confirmation deferred to plan 43.5-06 native UAT (UI-SPEC acceptance matrix rows 4-13)
</verification>

<success_criteria>
Push Right / Push Left are fully interactive armed tools: arm/disarm lifecycle, lock gating, hover pre-highlight with straddle preflight, clamped ghost/gap/blocked-edge preview, live readout, and atomic commit — with direct drag untouched and strip geometry unchanged.
</success_criteria>

## Artifacts this phase produces (this plan)

- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx`: Push Right/Push Left pill group, armed-state wiring, anchor resolution, hover pre-highlight + pivot tick paint, push ghost/gap-preview/blocked-edge paint, live status readout wiring
- Armed-tool session state module (focused sibling, session-only Signals; file at executor's discretion per 43.4-02 precedent)
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx`: lock-transition disarm wiring (wiring only)
- `app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts`: armed-disarm Escape layer, Select All disarm
- `app/src/components/physic-paint/physicsPaintStudio.css`: additive armed tint, pivot tick, push ghost, blocked-edge classes
- `app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts`: disarm-rule coverage

<output>
Create `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-05-SUMMARY.md` when done
</output>
