---
phase: 12-layer-keyframe-animation
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified: []
autonomous: false
requirements:
  - KF-01
  - KF-02
  - KF-03
  - KF-04
  - KF-05
  - KF-09
  - KF-10
  - KF-11
  - KF-12
  - KF-13

must_haves:
  truths:
    - "Complete keyframe workflow is functional end-to-end"
    - "Preview playback shows smooth interpolated animation"
    - "Timeline diamonds are visible and interactive"
    - "Undo/redo works for all keyframe operations"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete keyframe animation system.

Purpose: Confirm that all keyframe features work together end-to-end: creating keyframes, seeing diamonds on timeline, smooth preview playback, interpolation popover, undo/redo.
Output: Verified working keyframe animation system
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-layer-keyframe-animation/12-CONTEXT.md
@.planning/phases/12-layer-keyframe-animation/12-01-SUMMARY.md
@.planning/phases/12-layer-keyframe-animation/12-02-SUMMARY.md
@.planning/phases/12-layer-keyframe-animation/12-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end keyframe animation verification</name>
  <files>None</files>
  <action>
Present the following verification steps to the user. This is a human-verify checkpoint -- no code changes needed.

Complete layer keyframe animation system: data model, interpolation engine, keyframe store, preview rendering, properties panel [+ Keyframe] button, timeline diamond markers with click/drag/delete/double-click interactions, interpolation popover, keyboard shortcut, .mce v6 format support.

Steps to verify:

1. Open the app: `cd Application && npm run tauri dev`
2. Open or create a project with at least one content sequence with multiple key photos
3. Add a static-image content layer (base layer is not animatable)

Test 1 - Create keyframes: Select content layer, position playhead at frame 0, adjust opacity to 0 and X to -100, click [+ Keyframe]. Move to frame 20, set opacity to 1 and X to 0, click [+ Keyframe].

Test 2 - Diamond markers: Gold diamonds should appear at frame 0 and 20. Click diamond to select + snap playhead. Shift+click for multi-select.

Test 3 - Preview interpolation: Play with Space -- smooth animation from opacity 0 to 1 and X -100 to 0. Scrub manually -- property values update continuously.

Test 4 - Interpolation popover: Double-click diamond for popover with Linear/Ease In/Ease Out/Ease In-Out. Change easing and replay to see different curves.

Test 5 - Keyframe manipulation: Drag diamond to move. Delete key removes selected. Cmd+Z undoes.

Test 6 - Keyboard shortcut: I key adds keyframe at current frame.

Test 7 - Save/Load: Save (Cmd+S), close, reopen -- keyframes persist.

Test 8 - Deselect: Escape/click outside deselects layer, diamonds disappear. Reselect, diamonds reappear.
  </action>
  <verify>User confirms "approved" or describes issues</verify>
  <done>All 8 test scenarios pass. Animation is visually smooth. No crashes or console errors. Undo/redo works. Save/load preserves keyframes.</done>
</task>

</tasks>

<verification>
Human visual verification of all keyframe features working end-to-end.
</verification>

<success_criteria>
- All 8 test scenarios pass
- Animation is visually smooth during playback
- No crashes or console errors
- Undo/redo works correctly
- Save/load preserves keyframes
</success_criteria>

<output>
After completion, create `.planning/phases/12-layer-keyframe-animation/12-04-SUMMARY.md`
</output>
