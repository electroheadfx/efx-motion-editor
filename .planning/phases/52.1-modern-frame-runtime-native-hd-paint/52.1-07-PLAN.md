---
phase: 52.1-modern-frame-runtime-native-hd-paint
plan: 07
type: execute
wave: 7
depends_on: [52.1-06]
files_modified:
  - app/src/efx-paint/efxPaintCleanBreakContract.test.ts
autonomous: true
requirements: [D-19]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "the clean-break contract proves no dataUrl-keyed registry and no base64 frame transport survives outside the allowlist"
    - "the full phase gate suite is green: vitest + tsc + cargo test"
    - "the native roto UAT passes: paint, reopen, playback, save, export HD crispness"
    - "the straight-alpha contract (D-14) is validated in the packaged WKWebView build against the Phase 48 D-02 flattened raster"
  artifacts:
    - path: "app/src/efx-paint/efxPaintCleanBreakContract.test.ts"
      provides: "extended forbidden-token list covering the frame-transport surface"
  key_links:
    - from: "app/src/efx-paint/efxPaintCleanBreakContract.test.ts"
      to: "app/src/**"
      via: "grep contract walks the source tree and proves the frame-transport tokens are absent outside the allowlist"
      pattern: "FORBIDDEN_TOKENS"
---

<objective>
Extend the Phase 45 clean-break grep contract with the frame-transport forbidden tokens (D-19) and run the full phase gate suite plus the native roto UAT, proving the base64/PNG frame runtime is fully retired.

Purpose: This is the phase's closing gate — the large test retarget (D-19) is proven complete by a grep contract that walks the source tree, and the full native UAT confirms the runtime + codec + LRU + cap-raise stack works end-to-end.

Output: the extended clean-break contract test, a green full gate suite, and a passing native roto UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the clean-break contract with frame-transport forbidden tokens (D-19)</name>
  <files>app/src/efx-paint/efxPaintCleanBreakContract.test.ts</files>
  <read_first>
    - app/src/efx-paint/efxPaintCleanBreakContract.test.ts (FORBIDDEN_TOKENS list, ALLOWLIST, SCAN_ROOTS, comment-stripping walk)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md (efxPaintCleanBreakContract section)
  </read_first>
  <behavior>
    - Test 1 (RED): the contract test's FORBIDDEN_TOKENS includes the frame-transport tokens (the base64 PNG prefix, the synchronous string export, the JS PNG encode/decode helpers, the base64 bridge helpers, the string-keyed cache names).
    - Test 2: the contract walk proves those tokens are absent from app/src, app/src-tauri/src, and packages/efx-physic-paint/src outside the allowlist.
    - Test 3: the allowlist covers only the gate module, its test, its fixtures, and this contract file — nothing else.
  </behavior>
  <action>
    Extend app/src/efx-paint/efxPaintCleanBreakContract.test.ts by adding the frame-transport forbidden tokens to `FORBIDDEN_TOKENS`: the base64 PNG prefix (`data:image/png`), the synchronous canvas string export (`toDataURL`), the JS PNG encode/decode helpers (`encodePngDataUrl`/`decodePngDataUrl`), the base64 bridge helpers (`bytesToBase64`/`base64ToBytes`), and the string-keyed cache names (`_compositorImageCache`/`_rotoAlphaCanvasRegistry`). Add the import/export interchange path (PNG survives as import/export interchange, D-01) and the background-only-support marker (a distinct non-raster sentinel, never decoded — Pitfall 4) to the allowlist where they legitimately remain. The RED failure list of this test IS the deletion checklist for the phase — run it and fix any surviving reference outside the allowlist.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run efxPaintCleanBreakContract</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - FORBIDDEN_TOKENS includes the frame-transport tokens (grep "data:image/png" and "toDataURL" in the test file)
    - the contract walk passes (no forbidden token outside the allowlist)
    - vitest run efxPaintCleanBreakContract exits 0 with a non-zero passed count
  </acceptance_criteria>
  <done>The clean-break contract proves no dataUrl-keyed registry and no base64 frame transport survives outside the allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase gates + native roto UAT (D-19)</name>
  <files>app/src/efx-paint/efxPaintCleanBreakContract.test.ts</files>
  <read_first>
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-CONTEXT.md (Phase Gates section)
  </read_first>
  <action>
    Run the full phase gate suite: `pnpm --filter efx-motion-editor exec vitest run` (never watch mode), `pnpm --filter efx-motion-editor exec tsc --noEmit`, and `cargo test --manifest-path app/src-tauri/Cargo.toml`. Then run the native roto UAT (human): paint dense Physics strokes at 1920 (no gesture jank), reopen a many-frame project (open time must not regress vs the eager-load baseline), playback, save, and export HD (crispness check — no upscale blur). Validate the straight-alpha contract (D-14): semi-transparent stroke edges render correctly against the Phase 48 D-02 straight-alpha flattened raster in the packaged WKWebView build — no color fringing or brightness shift vs Chrome dev (Safari is documented to ignore `premultiplyAlpha: 'none'`, Pitfall 2). Confirm the profiler (`localStorage efx.physicsPaint.profile=1`) shows flat `render.*` counters during a 10s scrub at 1920, and memory is stable across a 100+ frame HD sequence (the LRU holds).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <automated>cargo test --manifest-path app/src-tauri/Cargo.toml</automated>
    <fails_when>non-zero exit, or "test result: FAILED" in the output</fails_when>
    <human-check>Native roto UAT: paint dense Physics strokes at 1920 (no jank), reopen a many-frame project (no open-time regression), playback, save, export HD (no upscale blur), and validate the straight-alpha contract (D-14) — semi-transparent stroke edges render correctly against the Phase 48 D-02 straight-alpha flattened raster in the packaged WKWebView build (no color fringing/brightness shift vs Chrome dev).</human-check>
  </verify>
  <acceptance_criteria>
    - vitest run exits 0 with a non-zero passed count
    - tsc --noEmit exits 0 with no "error TS" lines
    - cargo test exits 0
    - the native roto UAT passes (paint/reopen/playback/save/export HD crispness)
    - the straight-alpha contract (D-14) passes: semi-transparent edges render correctly against the Phase 48 D-02 flattened raster in the packaged WKWebView build
  </acceptance_criteria>
  <done>The full phase gate suite is green and the native roto UAT passes — the base64/PNG frame runtime is fully retired and the straight-alpha contract (D-14) is validated in WKWebView.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The contract test and gate suite are verification-only; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.1-SC | Tampering | npm/pnpm/cargo installs | high | mitigate | Package-legitimacy audit passed (RESEARCH.md); no new crates — `webp`/`image`/`libwebp-sys` already in Cargo.lock. |
</threat_model>

<verification>
vitest run (full) + tsc --noEmit + cargo test green; the clean-break contract passes; the native roto UAT passes (paint/reopen/playback/save/export HD crispness).
</verification>

<success_criteria>
The clean-break contract proves the base64/PNG frame runtime is retired, the full gate suite is green, and the native roto UAT confirms the bytes/ImageBitmap + Rust WebP + LRU + 1920-cap stack works end-to-end.
</success_criteria>

## Artifacts this phase produces

- Extended clean-break contract test (frame-transport forbidden tokens) — app/src/efx-paint/efxPaintCleanBreakContract.test.ts

<output>
Create .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-07-SUMMARY.md when done
</output>
