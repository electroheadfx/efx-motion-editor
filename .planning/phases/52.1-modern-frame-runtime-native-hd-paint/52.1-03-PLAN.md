---
phase: 52.1-modern-frame-runtime-native-hd-paint
plan: 03
type: execute
wave: 3
depends_on: [52.1-02]
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/rotoCanvasFrames.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
autonomous: true
requirements: [D-05, D-06, D-07, D-18]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "the runtime frame object carries compact WebP bytes (Uint8Array), never a base64 string"
    - "the three registries are keyed by stable sourceRef/library asset IDs, not by frame string content"
    - "capture reads getImageData and invokes encode_webp_frame; it no longer runs a synchronous canvas string export"
    - "the WebP RIFF/VP8L probe accepts valid WebP bytes and rejects PNG and the background-only-support marker"
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "PhysicPaintRenderedFrame with a bytes field (compact WebP Uint8Array) + WebP byte probe"
    - path: "app/src/components/physic-paint/roto/rotoCanvasFrames.ts"
      provides: "WebP RIFF/VP8L magic-byte probe + capture off the synchronous string export"
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "three registries re-keyed to stable IDs + resolveContentToken/dedup retargeted"
    - path: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts"
      provides: "apply path retargeted to raw-byte transfer (invoke), not JSON events"
  key_links:
    - from: "app/src/components/physic-paint/roto/rotoCanvasFrames.ts"
      to: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts"
      via: "capture invokes encode_webp_frame and returns compact WebP bytes"
      pattern: "encode_webp_frame"
    - from: "app/src/stores/physicPaintStore.ts"
      to: "app/src/types/physicPaint.ts"
      via: "registries keyed by stable sourceRef/library asset IDs, values are the bytes-bearing frame"
      pattern: "sourceRef"
---

<objective>
Retarget the runtime frame identity from base64 string to bytes/ImageBitmap (D-05/D-18): the frame type carries compact WebP bytes, capture moves off the synchronous canvas string export, the three dataUrl-keyed registries are re-keyed to stable sourceRef/library asset IDs (D-06), and the child→main apply path stops transporting base64 over JSON events (D-07).

Purpose: This is D-17 step (1) "runtime frame identity" — the in-heap identity change that every later plan (LRU, compositor, cap raise) builds on. The tracer (Plan 02) already proved the Rust codec + raw-byte invoke seam; this plan retargets the frame object and registries onto that seam.

Output: `PhysicPaintRenderedFrame` with a `bytes` field, the WebP RIFF/VP8L probe, capture off the string export, re-keyed registries, and the raw-byte apply path.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frame type retarget — bytes field + WebP byte probe (D-05, D-18)</name>
  <files>app/src/types/physicPaint.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts (PhysicPaintRenderedFrame at :1774, PhysicPaintApplyCanvasPayload.renderedFrame at :1797, onionDataUrl at :1800, PhysicPaintThumbnailEncodeResult.webpBase64 at :1770, RENDERED_DATA_URL_PREFIX at :1625, isRenderedPngDataUrl at :2397)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md (frame type + WebP magic-byte probe sections)
  </read_first>
  <behavior>
    - Test 1: a frame object with a `bytes: Uint8Array` field typechecks and round-trips through the type without a string field.
    - Test 2: the WebP byte probe accepts a Uint8Array whose first 4 bytes are RIFF (0x52 0x49 0x46 0x46) and offset 8 is WEBP (0x57 0x45 0x42 0x50).
    - Test 3: the WebP byte probe rejects a PNG signature (0x89 0x50 0x4e 0x47) and rejects a non-array value.
  </behavior>
  <action>
    In app/src/types/physicPaint.ts, replace the `dataUrl: string` field on `PhysicPaintRenderedFrame` (:1774) with `bytes: Uint8Array` — the compact WebP bytes held on the runtime frame object until save stages them to sidecars (D-18). Retarget `PhysicPaintApplyCanvasPayload.renderedFrame` (:1797) and the onion field (:1800) to carry the same bytes-bearing frame. Replace `RENDERED_DATA_URL_PREFIX` (:1625) and `isRenderedPngDataUrl` (:2397) with a WebP RIFF/VP8L byte probe: a `Uint8Array` whose bytes 0-3 are RIFF (0x52 0x49 0x46 0x46), bytes 8-11 are WEBP (0x57 0x45 0x42 0x50), and bytes 12-15 are VP8L (0x56 0x50 0x38 0x4c) for lossless. Retarget `PhysicPaintThumbnailEncodeResult.webpBase64` (:1770) to a raw `bytes: Uint8Array` field. Do NOT introduce a parallel string field — the string identity is removed, not aliased (D-06 one-way).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - app/src/types/physicPaint.ts defines `bytes: Uint8Array` on PhysicPaintRenderedFrame (grep "bytes: Uint8Array")
    - the WebP byte probe is defined (grep "0x52, 0x49, 0x46, 0x46" and "0x57, 0x45, 0x42, 0x50")
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The frame type carries compact WebP bytes and the WebP byte probe replaces the PNG string validator; tsc is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture off the string export + WebP magic-byte probe (D-03, D-07)</name>
  <files>app/src/components/physic-paint/roto/rotoCanvasFrames.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoCanvasFrames.ts (RenderedFramePayload at :12, PNG_SIGNATURE at :15, isRotoPngDataUrl at :22, buildRotoFrameFromCanvas at :188, buildRenderedFramePayload at :202, decodeRotoPngOffMainThread at :78)
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts (encodeWebpFrame helper added in Plan 02)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md (capture path + WebP magic-byte probe sections)
  </read_first>
  <behavior>
    - Test 1: the WebP RIFF/VP8L probe accepts a valid WebP byte array and rejects a PNG signature and the background-only-support marker.
    - Test 2: buildRotoFrameFromCanvas returns a payload whose frame carries `bytes` (Uint8Array), not a string.
  </behavior>
  <action>
    In app/src/components/physic-paint/roto/rotoCanvasFrames.ts, replace `PNG_SIGNATURE` (:15) and `isRotoPngDataUrl` (:22) with a WebP RIFF/VP8L magic-byte probe over `Uint8Array` (RIFF at 0, WEBP at 8, VP8L at 12). Retarget `buildRotoFrameFromCanvas` (:188) so capture no longer calls the synchronous canvas string export: read the canvas via `getImageData` (the one remaining synchronous readback, D-03), then `invoke('encode_webp_frame', { rgba, width, height })` (the helper added in Plan 02) to produce compact WebP bytes, and return a `RenderedFramePayload` (:12) whose frame carries `bytes`. Retarget `buildRenderedFramePayload` (:202) to carry `bytes` instead of the string. Treat the `background-only-support` fabricated marker as a distinct non-raster value that the probe must NOT reject (Pitfall 4) — it is never decoded. Do NOT delete `decodeRotoPngOffMainThread` yet; its decode path is absorbed by the LRU in Plan 04.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run rotoCanvasFrames</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - rotoCanvasFrames.ts defines the WebP RIFF/VP8L probe (grep "0x52, 0x49, 0x46, 0x46")
    - buildRotoFrameFromCanvas invokes encode_webp_frame (grep "encode_webp_frame")
    - vitest run rotoCanvasFrames exits 0 with a non-zero passed count
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Capture reads getImageData and invokes the Rust encoder; the WebP probe validates bytes; the string export is gone from the capture path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Registries re-keyed + resolveContentToken/dedup + raw-byte apply path (D-05, D-06, D-07)</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts (_rotoAlphaCanvasRegistry at :332, _backgroundSourceImages at :443, _referenceSourceImages at :467, resolveContentToken at :403, _collectFrameDataUrls at :905, _getTrackDataUrls at :914, _trackContentRevision at :1127, _makeBackgroundOnlySupportFrame at :1586)
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts (sendPhysicPaintApplyPayload at :152, bytesToBase64/base64ToBytes at :169-180)
    - app/src/lib/ipc.ts (safeInvoke/invoke pattern)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md (three registries + resolveContentToken + dedup + bridge transport sections)
  </read_first>
  <behavior>
    - Test 1: the three registries are keyed by a stable sourceRef/library asset ID, and a lookup by that ID returns the frame (not a string-keyed lookup).
    - Test 2: resolveContentToken returns a stable token for a given content revision and reuses it on repeat calls.
    - Test 3: the apply path sends raw bytes (invoke), not a JSON event carrying a base64 string.
  </behavior>
  <!-- planner-discipline-allow: bytesToBase64 -->
  <action>
    In app/src/stores/physicPaintStore.ts, re-key the three registries — `_rotoAlphaCanvasRegistry` (:332), `_backgroundSourceImages` (:443), `_referenceSourceImages` (:467) — from the frame string to the existing stable `sourceRef`/library asset ID (D-06). Retarget `resolveContentToken` (:403) and the dedup collectors `_collectFrameDataUrls` (:905) / `_getTrackDataUrls` (:914) to key on the stable ID / byte term instead of the string; retarget `_trackContentRevision` (:1127) to use a byte/stable-ID content term. Keep `_makeBackgroundOnlySupportFrame` (:1586) as a distinct non-raster marker (never decoded). In app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts, retarget `sendPhysicPaintApplyPayload` (:152) so frame bytes cross the child→main boundary as raw bytes via `invoke` (the Plan 02 helper pattern), NOT `emitTo` JSON events (which re-base64, Pitfall 3). Delete `bytesToBase64`/`base64ToBytes` (:169-180) — no base64 remains in JS (D-07). If the apply path cannot use `invoke` directly, use a Rust sidecar-write + stable-ref (the existing `physic_paint_cache` staging machinery) and send only the ref over the event; never a base64 string.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run physicPaintStore</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - the three registries are keyed by stable IDs (grep "sourceRef" in physicPaintStore.ts registry declarations)
    - resolveContentToken and the dedup collectors no longer key on the frame string
    - physicsPaintBridgeTransport.ts apply path uses invoke (grep "invoke(" in sendPhysicPaintApplyPayload), not emitTo
    - bytesToBase64/base64ToBytes are removed (grep -c "bytesToBase64" physicsPaintBridgeTransport.ts == 0)
    - vitest run physicPaintStore exits 0; tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The three registries are re-keyed to stable IDs, resolveContentToken/dedup are retargeted, and the apply path transports raw bytes with no base64 in JS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS (invoke) → Rust command | Untrusted frame bytes and dimensions cross the IPC boundary |
| child webview → main webview | Frame bytes cross the apply-path boundary (raw bytes, not JSON) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.1-04 | Information disclosure | bridge transport | low | mitigate | Apply path uses `invoke` raw byte bodies or a Rust sidecar-write + stable-ref, never `emitTo` JSON (D-07) — no base64 re-encoding of frame bytes. |
| T-52.1-05 | Tampering | WebP byte probe | low | mitigate | The RIFF/VP8L probe validates bytes before decode; the background-only-support marker is a distinct non-raster value, never decoded (Pitfall 4). |
| T-52.1-SC | Tampering | npm/pnpm/cargo installs | high | mitigate | Package-legitimacy audit passed (RESEARCH.md); no new crates — `webp`/`image`/`libwebp-sys` already in Cargo.lock. |
</threat_model>

<verification>
vitest run rotoCanvasFrames + physicPaintStore green; tsc --noEmit clean; the frame type carries bytes; the three registries are keyed by stable IDs; the apply path transports raw bytes.
</verification>

<success_criteria>
The runtime frame identity is bytes/ImageBitmap: the frame object carries compact WebP bytes, capture is off the synchronous string export, the three registries are re-keyed to stable IDs, and no base64 remains in the JS frame path.
</success_criteria>

## Artifacts this phase produces

- `PhysicPaintRenderedFrame.bytes: Uint8Array` — app/src/types/physicPaint.ts
- WebP RIFF/VP8L byte probe — app/src/types/physicPaint.ts + app/src/components/physic-paint/roto/rotoCanvasFrames.ts
- Capture off the string export (getImageData → invoke encode_webp_frame) — app/src/components/physic-paint/roto/rotoCanvasFrames.ts
- Three registries re-keyed to stable sourceRef/library asset IDs — app/src/stores/physicPaintStore.ts
- Raw-byte apply path (invoke) — app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts

<output>
Create .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-03-SUMMARY.md when done
</output>
