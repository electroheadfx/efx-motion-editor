---
phase: 52.1-modern-frame-runtime-native-hd-paint
plan: 01
subsystem: infra
tags: [tauri, preact, dependency-bump, security, ipc-acl, cve]

# Dependency graph
requires: []
provides:
  - Bumped Tauri 2.11.5 (IPC ACL security fix) and Preact 10.29.8 (CVE-2026-22028) baseline
  - Green D-04 gate suite (pnpm install, vitest, tsc, cargo build) for the codec/LRU plans to build on
affects:
  - 52.1-02 (Rust FrameCodec tracer)
  - 52.1-03 (runtime frame identity retarget)
  - 52.1-04 (byte-budgeted LRU)
  - 52.1-05 (main-editor readers)
  - 52.1-06 (cap raise 1000→1920)
  - 52.1-07 (clean-break contract)

# Actuals (#2632)
actuals:
  tokens: 11480
  tasks: 2
  commits: 1
  plan_head_before: b941994ac25bd015766cd3e31212f57ce2318fb1

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - app/package.json
    - app/src-tauri/Cargo.toml
    - app/src-tauri/Cargo.lock
    - package.json
    - pnpm-lock.yaml

key-decisions:
  - "Corrected the plan's target version numbers to the actual latest available on npm/crates.io: @tauri-apps/api 2.11.1 (not 2.11.5), @tauri-apps/cli 2.11.4 (not 2.11.5), tauri-build 2.6.3 (not 2.11.5), and the five tauri plugins to their independent latest (fs 2.5.2, store 2.4.4, dialog 2.7.3, notification 2.4.0, shell 2.3.6). Only the Rust `tauri` crate and `preact` have a 2.11.5 / 10.29.8 release; the plugins and tauri-build are versioned independently."
  - "Raised the root pnpm overrides (preact ^10.29.8, @preact/signals ^2.11.2) so the bump is not silently pinned back to the old floors."

patterns-established: []

requirements-completed: [D-04]

# Coverage metadata (#1602)
coverage:
  - id: D1
    description: "Dependency floors bumped — Tauri 2.11.5 (IPC ACL fix) and Preact 10.29.8 (CVE-2026-22028) with plugins/tauri-build at their latest compatible versions"
    requirement: D-04
    verification:
      - kind: other
        ref: "pnpm install"
        status: pass
      - kind: other
        ref: "cargo build --manifest-path app/src-tauri/Cargo.toml"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-04 automated gate suite green — vitest run and tsc --noEmit clean on the bumped deps"
    requirement: D-04
    verification:
      - kind: unit
        ref: "pnpm --filter efx-motion-editor exec vitest run (3471 passed)"
        status: pass
      - kind: other
        ref: "pnpm --filter efx-motion-editor exec tsc --noEmit"
        status: pass
    human_judgment: false
  - id: D3
    description: "Native UAT smoke — app launches and open/paint/playback/save passes with no regression vs the pre-bump build"
    requirement: D-04
    verification: []
    human_judgment: true
    rationale: "Visual/interactive verification (launch the packaged app, paint a stroke, playback, save) requires a human; no automated test asserts the native WKWebView smoke path."

# Metrics
duration: 2 min
completed: 2026-09-06
status: complete
---

# Phase 52.1 Plan 01: Dependency Bumps (D-04) Summary

**Bumped Tauri to 2.11.5 (IPC ACL security fix) and Preact to 10.29.8 (CVE-2026-22028) with a green pnpm/vitest/tsc/cargo gate baseline.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-09-06T08:26:48Z
- **Completed:** 2026-09-06T08:28:58Z
- **Tasks:** 2 completed
- **Files modified:** 5

## Accomplishments
- Bumped the Tauri stack to 2.11.5 (Rust `tauri` crate) with `@tauri-apps/api` 2.11.1, `@tauri-apps/cli` 2.11.4, `tauri-build` 2.6.3, and the five plugins (fs/store/dialog/notification/shell) to their independent latest — the 2.11.1 IPC ACL security fix is now in place.
- Bumped Preact to 10.29.8 (CVE-2026-22028 patch) plus `@preact/signals` 2.11.2 and `@preact/preset-vite` 2.10.6, and raised the root pnpm overrides to match.
- Green D-04 gate suite: `pnpm install` clean (no peer errors), `cargo build` succeeds, `vitest run` 3471 passed, `tsc --noEmit` clean.

## Task Commits

Each task was committed atomically:

1. **Task 1: Bump JS + Rust dependency floors (D-04)** - `7dd75fcd` (chore)
2. **Task 2: Full D-04 gates + native UAT smoke** - no code changes (verification-only; automated gates passed, native UAT smoke deferred to end-of-phase human verification)

**Plan metadata:** committed separately as `docs(52.1-01)`.

## Files Created/Modified
- `app/package.json` - bumped @tauri-apps/api 2.11.1, @tauri-apps/cli 2.11.4, preact 10.29.8, @preact/signals 2.11.2, @preact/preset-vite 2.10.6
- `app/src-tauri/Cargo.toml` - bumped tauri 2.11.5, tauri-build 2.6.3, plugins fs/store/dialog/notification/shell to latest
- `app/src-tauri/Cargo.lock` - regenerated by cargo build
- `package.json` - raised root pnpm overrides (preact ^10.29.8, @preact/signals ^2.11.2)
- `pnpm-lock.yaml` - regenerated by pnpm install

## Decisions Made
- Corrected the plan's target version numbers to the actual latest available on the registries (see frontmatter key-decisions). The plan specified 2.11.5 for `@tauri-apps/api`, `@tauri-apps/cli`, `tauri-build`, and all five plugins, but only the Rust `tauri` crate has a 2.11.5 release — the JS packages and plugins are versioned independently.
- Raised the root pnpm overrides so the Preact/signals bump is not silently pinned back to the old floors.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Plan's target version numbers did not exist on the registries**
- **Found during:** Task 1 (Bump JS + Rust dependency floors)
- **Issue:** The plan specified `@tauri-apps/api` 2.11.5, `@tauri-apps/cli` 2.11.5, `tauri-build` 2.11.5, and `tauri-plugin-fs/store/dialog/notification/shell` 2.11.5. Verified against npm/crates.io: only the Rust `tauri` crate (2.11.5) and `preact` (10.29.8) have those exact releases. `@tauri-apps/api` latest is 2.11.1, `@tauri-apps/cli` latest is 2.11.4, `tauri-build` latest is 2.6.3, and the plugins are versioned independently (fs 2.5.2, store 2.4.4, dialog 2.7.3, notification 2.4.0, shell 2.3.6). Pinning the non-existent versions would have broken `pnpm install` and `cargo build`.
- **Fix:** Bumped to the actual latest available versions that satisfy the D-04 intent (latest 2.11.x Tauri + latest Preact 10.x for the security fixes).
- **Files modified:** app/package.json, app/src-tauri/Cargo.toml
- **Verification:** `pnpm install` clean, `cargo build` succeeds, `vitest run` 3471 passed, `tsc --noEmit` clean.
- **Committed in:** 7dd75fcd

**2. [Rule 2 - Missing critical functionality] Root pnpm overrides pinned the old Preact/signals floors**
- **Found during:** Task 1 (after first `pnpm install` resolved preact to 10.28.4 / signals 2.8.1 despite the app/package.json bump)
- **Issue:** The root `package.json` `pnpm.overrides` carried `preact: ^10.28.4` and `@preact/signals: ^2.8.1`, which silently pinned the resolution back to the old floors, defeating the bump.
- **Fix:** Raised the overrides to `preact: ^10.29.8` and `@preact/signals: ^2.11.2`.
- **Files modified:** package.json
- **Verification:** Re-ran `pnpm install`; lockfile now resolves preact 10.29.8 and @preact/signals 2.11.2.
- **Committed in:** 7dd75fcd

---

**Total deviations:** 2 auto-fixed (1 Rule 3, 1 Rule 2)
**Impact on plan:** Both fixes were necessary for correctness — the plan's version numbers were factually wrong, and the root overrides would have silently reverted the bump. No scope creep; the D-04 intent (latest 2.11.x Tauri + latest Preact 10.x) is fully met.

## Issues Encountered
None — the two deviations above were handled automatically via the deviation rules.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
The dependency baseline is in place for the codec and LRU plans. Plan 52.1-02 (Rust FrameCodec tracer) can build against Tauri 2.11.5 and Preact 10.29.8.

**Pending human verification:** the native UAT smoke (open/paint/playback/save) from Task 2 is deferred to end-of-phase human verification — it is not an automated gate.

## Self-Check: PASSED

- SUMMARY.md exists on disk
- Task commit `7dd75fcd` present in git history
- app/package.json and app/src-tauri/Cargo.toml carry the bumped versions
- pnpm install clean, cargo build succeeds, vitest 3471 passed, tsc --noEmit clean

---
*Phase: 52.1-modern-frame-runtime-native-hd-paint*
*Completed: 2026-09-06*
