---
phase: 52.1-modern-frame-runtime-native-hd-paint
verified: 2026-09-11T09:00:46Z
status: human_needed
score: 7/7 plans executed; 18/19 locked decisions satisfied (16 SATISFIED + D-05/D-07 satisfied-with-recorded-caveat); 1 PARTIAL (D-11 production pinning wiring unproven — user-accepted AR-52.1-03); 0 roadmap REQ-IDs (inserted phase maps to D-01..D-19)
behavior_unverified: 1
overrides_applied: 1
gaps:
  - decision: D-11
    verdict: PARTIAL
    detail: "The pinning correctness rule is implemented and unit-tested (frameLru.ts:69-78 pin/unpin; evictor skips pinCount > 0 at :103-115; only the evictor calls bitmap.close()) but no production draw/composite path calls pin()/unpin() (52.1-SECURITY.md T-52.1-06/T-52.1-08; AR-52.1-03, user-accepted 2026-09-10). The pinned-until-released production behavior is therefore unproven; current draw safety rests on the synchronous draw path, not on the pin mechanism. Non-blocking, below the high threshold, earmarked Phase 53 hardening."
deferred:
  - "D-11 production pinning wiring (AR-52.1-03, user-accepted 2026-09-10) — non-blocking; earmarked Phase 53 hardening."
  - "Decoded-dimension bound before RGBA allocation in decode_webp_frame (T-52.1-02/T-52.1-07; AR-52.1-01, user-accepted 2026-09-10) — no-panic Option decode is present; bound absent, practical risk low."
  - "Capture debounce — deferred 2026-09-07 (52.1-CONTEXT.md §Deferred Ideas): post-fix UAT shows leave at ~120ms and painting instant; revisit only if fast-painting-without-idle pile-up resurfaces."
behavior_unverified_items:
  - "D-11: production draw paths never call pin()/unpin() — the pinned-until-released behavior is not exercised in production (AR-52.1-03); the mechanism is unit-tested only (frameLru.test.ts). Correctness currently relies on draw-path synchronicity (see coincidental_reliance_items)."
coincidental_reliance_items:
  - "D-11 draw safety: no production draw path pins its frame (AR-52.1-03); correctness relies on the composite/draw being synchronous — no LRU eviction can interleave mid-drawImage — rather than on the pin mechanism."
  - "D-05/D-07 transport: Uint8Array frame bytes survive the Tauri emitTo JSON hop only through the allowlisted base64 round-trip (webpBytes.ts:104-136 toTransportPayload/fromTransportPayload) consumed by the apply path (physicsPaintBridgeTransport.ts:148-167) — correct because the parent-side validator accepts the canonical base64 form (FRAME_TRANSPORT_ALLOWLIST, efxPaintCleanBreakContract.test.ts:105-111; AR-52.1-02/T-52.1-04)."
human_verification:
  - "Project open-time on a many-frame project vs the eager-load baseline (52.1-VALIDATION.md manual-only row 3): the refs-only mechanism is unit-verified (52.1-04 efxPaintPersistence.test.ts) but no wall-clock comparison is recorded — re-confirm during Phase 53 native UAT."
  - "Profiler render.* counters flat during a 10s scrub at 1920 (52.1-CONTEXT.md §Gates): no profiler-counter measurement is recorded in the phase artifacts — the phase closed on the green automated suite + feeling-based native UAT; re-confirm during Phase 53 native UAT."
  - "Memory stable across a 100+ frame HD sequence (52.1-CONTEXT.md §Gates): the 512 MB byte ceiling and eviction are unit-verified (frameLru.test.ts) but no live memory trace is recorded — re-confirm during Phase 53 native UAT."
---

# Phase 52.1: Modern frame runtime + native HD paint — Verification Report

**Phase Goal:** Replace the base64-dataUrl frame runtime with a bytes/ImageBitmap runtime, move PNG→WebP-lossless encode/decode into Rust, lazy-load sidecars behind a byte-budgeted LRU, and raise the working-resolution cap 1000→1920 — so HD projects paint at native resolution without main-thread jank or heap blowup (ROADMAP.md:469).
**Verified:** 2026-09-11T09:00:46Z
**Status:** human_needed
**Re-verification:** No — initial verification (backfill 2026-09-11 from the phase's recorded evidence; no test re-run, no source modification)

## Goal Achievement

### Observable Truths (Phase Gates — 52.1-CONTEXT.md §Gates)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `vitest run` + `tsc --noEmit` clean | ✓ VERIFIED (recorded) | 52.1-07-SUMMARY: `tsc --noEmit` clean; `vitest run` 3511 passed / 0 failed (excluding the known-red chunk-size test); `cargo test` 16 passed (one flaky concurrency test passes on re-run). Earlier recorded runs: 3471 passed (52.1-01, `7dd75fcd`), 3474 passed (52.1-03, `b2e77b86`). |
| 2 | Profiler (`efx.physicsPaint.profile=1`): `render.*` counters flat during a 10s scrub at 1920 | ◐ NOT SEPARATELY RECORDED | No profiler-counter measurement appears in the phase artifacts. The perf-sensitive paths were measured live during execution (52.1-CONTEXT.md:166: leave ~120ms, painting instant, async Rust encode off-main-thread, idle-time finalization drain proven by the 60s-idle test). Carried to Phase 53 native UAT (see `human_verification`). |
| 3 | Memory stable across a 100+ frame HD sequence (LRU must hold) | ◐ NOT SEPARATELY RECORDED | The 512 MB byte ceiling + eviction/pinning behavior are unit-verified (`frameLru.test.ts`, 52.1-04 coverage D9/D11); no live memory trace is recorded. Carried to Phase 53 native UAT. |
| 4 | Project open time on a many-frame project must not regress vs the eager-load baseline | ◐ MECHANISM VERIFIED, WALL CLOCK NOT RECORDED | Refs-only `loadEfxPaintDocuments` returns `cachePath` refs with empty bytes — no per-frame `readFile` on open (52.1-04 `7541446b`; coverage D8; `efxPaintPersistence.test.ts`). No wall-clock comparison is recorded; carried to Phase 53 native UAT. |
| 5 | Keyframe-creation fluidity UAT: dense Physics strokes at 1920 — no gesture jank | ✓ VERIFIED (recorded, feeling-based) | Native roto UAT signed off on feeling — drag, paint-burst, and general fluidity all pass (52.1-07-SUMMARY); the user declined the numeric docSync count, recorded as feeling-based. Post-fix leave path ~120ms (52.1-CONTEXT.md:166). |
| 6 | Full native roto workflow UAT: paint, reopen, playback, save, export HD crispness (no upscale blur) | ✓ VERIFIED (recorded, feeling-based) | 52.1-07 coverage row UAT (human_judgment): "Native roto UAT — paint, reopen, playback, save, export HD crispness + straight-alpha (D-14) against the Phase 48 D-02 flattened raster". Post-close fixes also passed native UAT 2026-09-10: `f9aacaf5` (drop Apply render-mode choice — always live with progress pill) and `d1e84594` (redrawAll skips strokes still queued for finalization). |

**Score:** 7/7 plans executed; 16/19 decisions SATISFIED, 2 satisfied-with-recorded-caveat (D-05/D-07), 1 PARTIAL (D-11); phase gates 1/5/6 recorded green, gates 2/3/4 carried to the Phase 53 backstop.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `app/src-tauri/src/services/frame_codec.rs` | Codec-agnostic `FrameCodec` trait + WebP-lossless impl (D-01/D-02) | ✓ VERIFIED | `WebPLosslessCodec` via `encode_advanced` with `exact = 1` (config_exact); decode expands RGB→RGBA; round-trip/RIFF/dimension tests (`round_trip_preserves_rgb_under_transparent_pixel`) pass under `cargo test` (52.1-02 coverage D1/D2) |
| `app/src-tauri/src/commands/frame_codec.rs` | `encode_webp_frame` / `decode_webp_frame` Tauri commands | ✓ VERIFIED | Registered in `lib.rs` `generate_handler`; `DecodedWebpFrame { width, height, rgba }` serde struct (52.1-02) |
| `app/src/lib/webpFrameCodec.ts` | Raw-byte invoke helpers (D-07) | ✓ VERIFIED | `encodeWebpFrame` `:43-49` and `decodeWebpFrame` `:55-57` use `invoke` with raw `Uint8Array` bodies; `encodeCanvasAsWebp` `:65-74` reads transient `getImageData` then invokes the Rust encoder |
| `app/src/lib/webpBytes.ts` | WebP RIFF/VP8L probe + transport byte codec (D-05/D-07) | ✓ VERIFIED | `isWebpBytes` `:17-23` (rejects PNG/lossy VP8/non-array); `bytesToBase64` `:45`; `toTransportPayload` `:104-136` (allowlisted JSON-hop base64, see caveats) |
| `app/src/lib/frameLru.ts` | 512 MB byte-budgeted LRU with pinning + `bitmap.close()` (D-09/D-10/D-11) | ✓ VERIFIED (with D-11 caveat) | `FRAME_LRU_BYTE_CEILING` `:19`; `frameByteSize = width*height*4` `:14-16`; `pin`/`unpin` `:69-78`; eviction owns `close()` `:86`/`:97`/`:113`, skips pinned `:107-110` |
| `app/src/lib/efxPaintPersistence.ts` | Refs-only load + WebP sidecar filename (D-08) | ✓ VERIFIED | `loadEfxPaintDocuments` returns `cachePath` refs with empty bytes, no per-frame `readFile` (52.1-04 `7541446b`; coverage D8) |
| `app/src/stores/physicPaintStore.ts` | Single decode authority: LRU decode, prewarm, await (D-12/D-13/D-14) | ✓ VERIFIED | `_compositorDecode` path `:1105-1130` (`frameLru.get` → `decode_webp_frame` → `createImageBitmap(..., { premultiplyAlpha: 'none' })` → `frameLru.put`); `awaitPendingDecodes` `:1153`; `prefetchNeighborFrames` `:1286` (`-1`/`+1`/`+2`) |
| `app/src/components/physic-paint/roto/rotoCanvasFrames.ts` | Capture off the string export (D-03/D-18) | ✓ VERIFIED | `encodeCanvasAsWebp` capture path; no synchronous canvas string export in the frame path (52.1-03 coverage D18; `rotoCanvasFrames.test.ts`) |
| `app/src/lib/previewRenderer.ts` | Preview readers draw the LRU handle (D-05) | ✓ VERIFIED | `isPhysicPaintFrameResolved` `:225` and the fallback draw `:653` resolve `physicPaintStore.getDecodedImage(bytes)`; `awaitPhysicPaintDecodes` `:229`; flattened seam `getFlattenedFrame` `:210`/`:266` |
| `app/src/lib/exportRenderer.ts` | Export preload gated on decode completion (D-05/D-13) | ✓ VERIFIED | `preloadExportImages` `:438` triggers per-frame decodes → `await renderer.awaitPhysicPaintDecodes()` `:451` → re-collects warm flattened rasters |
| `app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts` | Cap raised 1000→1920 (D-15) | ✓ VERIFIED | `PHYSICS_PAINT_WORKING_LONG_EDGE = 1920` `:3`; `getPhysicsPaintWorkingSize` scales by it (52.1-06 `266cdb0e`; coverage D15) |
| `app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts` | Brush default rescaled ~1.9x (D-17) | ✓ VERIFIED | brush `size` default 6 → 11 (52.1-06 `266cdb0e`; coverage D17) |
| `packages/efx-physic-paint/src/types.ts` | Physics default rescaled ~1.9x (D-17) | ✓ VERIFIED | `BLOW_STRENGTH` 8.0 → 15.2 (exact 1.9x); `DENSITY_NORM`/`LUT_SIZE` untouched (52.1-06 coverage D17) |
| `app/src/efx-paint/efxPaintCleanBreakContract.test.ts` | D-19 frame-transport clean-break contract | ✓ VERIFIED | `FRAME_TRANSPORT_FORBIDDEN_TOKENS` `:76-92`; import/export + JSON-transport allowlist `:105-111`; extended and green in 52.1-07 (`0e76ff3f`) |
| `.planning/phases/52.1-.../52.1-VALIDATION.md` | Validation strategy, Nyquist-compliant | ✓ VERIFIED | `status: validated`, `nyquist_compliant: true`, 16/16 task rows green, approval 2026-09-10 (`0601573c`) |
| `.planning/phases/52.1-.../52.1-SECURITY.md` | Threat register | ✓ VERIFIED | 8 threats / 3 closed / 5 open-below-threshold / 3 accepted-risk rows; `threats_open: 0`; audit trail 2026-09-10 (`f49573a4`) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| Frame capture/commit (Studio) | Rust `encode_webp_frame` | `encodeCanvasAsWebp`: transient `getImageData` → raw-byte `invoke` | ✓ WIRED | `webpFrameCodec.ts:65-74`; compact WebP bytes on the frame (D-03/D-18; 52.1-03 coverage D18) |
| Frame bytes | Decoded `ImageBitmap` in the LRU | `_compositorDecode`: `frameLru.get` → `decode_webp_frame` → `createImageBitmap` → `frameLru.put` | ✓ WIRED | `physicPaintStore.ts:1105-1130`; straight-alpha via `premultiplyAlpha: 'none'` (coverage D13/D14) |
| Project open | Refs-only load → on-demand decode | `loadEfxPaintDocuments` (`cachePath`, empty bytes) → `getDecodedImage(bytes)` | ✓ WIRED | 52.1-04 `7541446b`; coverage D8; `previewRenderer.ts:653` fallback |
| Export preload | Warm flattened rasters | `preloadExportImages` → `awaitPhysicPaintDecodes()` → re-collect | ✓ WIRED | `exportRenderer.ts:438-451`; cold LRU miss never renders a missing layer (52.1-05 `7299ded4`) |
| Playback lookahead | `prefetchNeighborFrames` | `useRotoNavigationCoordinator` onFrame → Studio `launchContext.layerId` | ✓ WIRED | 52.1-04 `863c8dbd`; `physicPaintStore.ts:1286`; one 512 MB budget (D-10) |
| Compositor draw | `drawImage(bitmap)` | Store ports return ImageBitmap; composite cache key `buildFrameBytesToken` | ✓ WIRED | `efxPaintCompositor.ts` / `efxPaintCompositeCache.ts` (already retargeted 52.1-03/04, verified 52.1-05 coverage D13 matrix suite) |
| Child apply path | Parent window | `emitTo` JSON + `toTransportPayload` base64 (allowlisted) | ✓ WIRED (allowlisted) | `physicsPaintBridgeTransport.ts:148-167`; `FRAME_TRANSPORT_ALLOWLIST` `efxPaintCleanBreakContract.test.ts:105-111`; AR-52.1-02/T-52.1-04 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Frame sidecar ref | `cachePath` | `loadEfxPaintDocuments` (refs-only) | Yes — path refs only, no bytes read on open | ✓ FLOWING |
| Decoded frame handle | `ImageBitmap` | `frameLru` ← Rust `decode_webp_frame` → `ImageData` → `createImageBitmap` | Yes — libwebp decode, GPU-backed handle | ✓ FLOWING |
| Flattened raster | raster record | `getFlattenedFrame` (Phase 48 seam) ← LRU handles | Yes — compositor draws the LRU bitmap | ✓ FLOWING |
| Frame bytes | `Uint8Array` | `encode_webp_frame` at capture/commit | Yes — compact WebP (0.1–2 MB typical); raw RGBA never held per frame in the JS heap | ✓ FLOWING |

### Behavioral Spot-Checks (recorded results only — none re-run in this backfill)

| Behavior | Recorded command | Recorded result | Status |
| -------- | ---------------- | --------------- | ------ |
| Rust codec round-trip + WebP probe | `cargo test frame_codec` (52.1-02) | pass (52.1-02 coverage D1/D2) | ✓ PASS (recorded) |
| LRU eviction / pinning / close / clear | `vitest run src/lib/frameLru.test.ts` (52.1-04) | pass (coverage D9/D11) | ✓ PASS (recorded) |
| Refs-only lazy load | `vitest run src/lib/efxPaintPersistence.test.ts` (52.1-04) | pass (coverage D8) | ✓ PASS (recorded) |
| Straight-alpha contract | `vitest run .../#premultiplyAlpha` (52.1-04) | pass (coverage D14) | ✓ PASS (recorded) |
| Preview/export readers + compositor LRU | `previewRenderer` / `exportEngine` / `exportRenderer` / `efxPaintCompositorMatrix` (52.1-05) | pass (coverage D5/D13) | ✓ PASS (recorded) |
| Cap raise + rescale | `physicsPaintCanvasSizing` / `physicsPaintStudioSettings` / `efxPaintStore.reveal` (52.1-06) | 6 RED (`e7388453`) → green (`266cdb0e`) | ✓ PASS (recorded) |
| Clean-break contract (D-19) | `vitest run src/efx-paint/efxPaintCleanBreakContract.test.ts` (52.1-07) | pass (`0e76ff3f`) | ✓ PASS (recorded) |
| Full JS suite | `vitest run` (52.1-07) | 3511 passed / 0 failed (excluding the known-red `viteBuild` chunk-size test) | ✓ PASS (recorded) |
| Full Rust suite | `cargo test` (52.1-07) | 16 passed (one flaky concurrency test passes on re-run) | ✓ PASS (recorded) |
| Typecheck | `tsc --noEmit` (52.1-07) | clean | ✓ PASS (recorded) |
| Chunk budget | `viteBuild` chunk-size test (52.1-07) | RED at 1300 kB — main chunk 1305.11 kB (attributed ~4.8 kB deps + ~8.9 kB feature); budget subsequently raised 1300→1320 kB, signed (`6325e2e8`) — 1305.11 kB sits under the raised ceiling; no post-raise run is recorded in this backfill | ◐ RESOLVED BY RAISE (recorded) |
| Native roto UAT | live session (52.1-07 + 2026-09-10 fixes) | signed off on feeling (drag, paint-burst, fluidity; numeric docSync count declined by user); post-close render-mode + double-apply fixes passed native UAT (`f9aacaf5`, `d1e84594`) | ✓ PASS (recorded) |

### Requirements Coverage

**Mapping note:** this inserted phase maps to locked decisions **D-01..D-19**, NOT to milestone REQ-IDs — ROADMAP.md:470 reads "Requirements: TBD (D-01..D-19 locked decisions)", and the v1.0.0 milestone audit records: "Phase 52.1 maps to locked decisions D-01..D-19, not to milestone REQ-IDs (inserted phase, no traceability rows)." The table below is therefore the decision coverage; it must not be read as REQ-ID traceability.

| Decision | Source plans | Requirement (52.1-CONTEXT.md) | Status | Evidence |
| -------- | ------------ | ----------------------------- | ------ | -------- |
| D-01 | 52.1-02 | Sidecars PNG → WebP lossless via Rust, `config_exact` alpha | ✓ SATISFIED | `frame_codec.rs` round-trip test `round_trip_preserves_rgb_under_transparent_pixel` passes; `2b21ac29` (RED) → `f6d8fbd6` (GREEN); coverage D1 |
| D-02 | 52.1-02 | Codec-agnostic trait seam | ✓ SATISFIED | `FrameCodec` trait; commands call the codec through the trait, never a direct `webp::` call (coverage D2) |
| D-03 | 52.1-03, 52.1-04 | Encode never during the gesture; async at commit/save/bake | ✓ SATISFIED | `encodeCanvasAsWebp` (transient `getImageData` → Rust encode); prefetch rides the playback onFrame funnel (`863c8dbd`); coverage D18 |
| D-04 | 52.1-01 | Dependency bumps + gates (Tauri 2.11.5, Preact 10.29.8) | ✓ SATISFIED | `7dd75fcd`; `pnpm install` clean, `cargo build` succeeds, vitest 3471 passed, tsc clean (coverage D1/D2); plan version numbers corrected to real registry releases (Rule 3 deviation, recorded) |
| D-05 | 52.1-03, 52.1-05 | Runtime frame identity is bytes/ImageBitmap, not dataUrl string | ✓ SATISFIED (recorded caveat) | `b2e77b86` (119 files); readers draw the LRU handle (`previewRenderer.ts:225`,`:653`; `7299ded4`/`18709b95`); registries re-keyed (coverage D6). Caveat: base64 survives at the allowlisted JSON transport boundary (webpBytes.ts:104-136; `physicsPaintBridgeTransport.ts:148-167`; `FRAME_TRANSPORT_ALLOWLIST` test:105-111; AR-52.1-02/T-52.1-04). |
| D-06 | 52.1-03 | All three dataUrl-keyed registries re-keyed to stable IDs | ✓ SATISFIED | 52.1-03 coverage D6 (`physicPaintStore.test.ts`); `_backgroundSourceImages`/`_referenceSourceImages` become `Map<string, Uint8Array>`, dedup via `buildFrameBytesToken` |
| D-07 | 52.1-02, 52.1-03 | Rust encode/decode over bytes — no base64 in the codec transport | ✓ SATISFIED (recorded caveat) | `encodeWebpFrame`/`decodeWebpFrame` raw-byte `invoke` (webpFrameCodec.ts:43-57); `isWebpBytes` RIFF/VP8L probe (webpBytes.ts:17-23); coverage D7 (`grep bytesToBase64 == 0` in the transport file). Caveat: the "no base64 anywhere" literal is qualified by the same allowlisted JSON-hop transport surfaces (see D-05) — recorded, not silent. |
| D-08 | 52.1-04 | Lazy sidecar load: open reads refs only; decode on demand | ✓ SATISFIED | `7541446b`; refs-only `loadEfxPaintDocuments` + `cachePath` field; coverage D8 (`efxPaintPersistence.test.ts`) |
| D-09 | 52.1-04 | LRU byte ceiling = fixed 512 MB | ✓ SATISFIED | `FRAME_LRU_BYTE_CEILING = 512 * 1024 * 1024` (frameLru.ts:19); `frameByteSize = width*height*4` (:14-16); eviction past the ceiling (:103-115); coverage D9 |
| D-10 | 52.1-04 | Neighbor prewarm shares the 512 MB budget | ✓ SATISFIED | `prefetchNeighborFrames` (physicPaintStore.ts:1286; offsets -1/+1/+2) into the same `frameLru`; one budget, one eviction policy (coverage D10; `863c8dbd`) |
| D-11 | 52.1-04 | Pinning correctness rule (in-flight draws unevictable; eviction owns `close()`) | ◐ PARTIAL | `pin`/`unpin` (:69-78) + evictor skips pinned (:103-115) + eviction owns `bitmap.close()` — implemented and unit-tested; BUT no production draw path calls them (T-52.1-06/T-52.1-08; AR-52.1-03, user-accepted 2026-09-10). See `gaps` + `behavior_unverified_items`. |
| D-12 | 52.1-04 | ONE decoded-frame cache authority | ✓ SATISFIED | `_compositorImageCache` absorbed into the LRU; decode authority `physicPaintStore.ts:1105-1130`; recorded scope note: `rotoAlphaCanvasRegistry` persists as a re-keyed canvas (not decode) registry for interpolation/reveal bake (52.1-04 key-decisions); coverage D12 |
| D-13 | 52.1-04, 52.1-05 | ImageBitmap is the canonical decoded handle; compositor `drawImage(bitmap)` | ✓ SATISFIED | decode path `createImageBitmap(ImageData)` (physicPaintStore.ts:1105-1130); compositor draws the store portrait; ImageData transient only; coverage D13 (compositor matrix suite) |
| D-14 | 52.1-04 | Straight alpha preserved (`premultiplyAlpha: 'none'`) + Safari contract test | ✓ SATISFIED | coverage D14 `premultiplyAlpha` contract test passes; packaged WKWebView visual comparison recorded in the 52.1-07 native UAT row (feeling-based) against the Phase 48 D-02 flattened raster |
| D-15 | 52.1-06 | Working cap raised 1000 → 1920 | ✓ SATISFIED | `PHYSICS_PAINT_WORKING_LONG_EDGE = 1920` (physicsPaintCanvasSizing.ts:3); 2K/4K downscales to the 1920 long edge; legacy keys keep recorded size via `drawCanvasAtSize`; coverage D15 |
| D-16 | 52.1-06 | 2K/4K activation stays a single-constant change + gates | ✓ SATISFIED | D-16 audit table in 52.1-06-SUMMARY (every `1000` literal retargeted or justified); LRU/IPC/cache byte-budgeted, never frame-count (frameLru.ts:8-10); coverage D16 |
| D-17 | all plans | Strict dependency order; ~1.9x rescale ships with the cap raise | ✓ SATISFIED | ROADMAP waves 1-7 follow deps → codec → identity → LRU → readers → cap+rescale → contract; rescale shipped in 52.1-06 with the cap raise (brush 6→11, `BLOW_STRENGTH` 8.0→15.2); coverage D17 |
| D-18 | 52.1-03, 52.1-06 | Compact WebP bytes at rest; raw RGBA never held per-frame | ✓ SATISFIED | Capture/commit `getImageData` (transient) → `encode_webp_frame` → compact bytes (webpFrameCodec.ts:65-74); D-18 watch in 52.1-06-SUMMARY; coverage D18 (`rotoCanvasFrames.test.ts`) |
| D-19 | 52.1-07 | Large test retarget + clean-break contract | ✓ SATISFIED | `0e76ff3f` extends `FRAME_TRANSPORT_FORBIDDEN_TOKENS` + allowlist (test:76-111); close-out gates recorded (vitest 3511, tsc clean, cargo 16); 52.1-07 coverage D19 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `app/src/lib/frameLru.ts` | `:69-78` | D-11 pin/unpin implemented and unit-tested but unwired into any production draw/composite path (T-52.1-06/T-52.1-08, AR-52.1-03) | ⚠️ Warning (user-accepted) | Non-blocking (below high threshold): compositor draw is synchronous, so no eviction can interleave mid-`drawImage`; earmarked Phase 53 hardening. Recorded as `gaps`, `behavior_unverified_items`, `coincidental_reliance_items`. |
| `app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts` | `:148-167` | Apply path serializes frame bytes as base64 over `emitTo` JSON (AR-52.1-02/T-52.1-04) | ⚠️ Warning (accepted, allowlisted) | Non-blocking: encode/decode use raw-byte `invoke`; base64 is confined to the JSON hop and explicitly allowlisted in the D-19 contract (`FRAME_TRANSPORT_ALLOWLIST`, efxPaintCleanBreakContract.test.ts:105-111). |
| `app/src-tauri/src/services/frame_codec.rs` | decode path | Decoded-dimension bound before RGBA allocation absent (T-52.1-02/T-52.1-07, AR-52.1-01) | info (accepted) | Practical risk low: decode bytes originate from the app's own encoder or on-disk sidecars; the no-panic `Option` decode is present. Non-blocking. |
| `app/src/vitest` / build | `viteBuild.test.ts` | plan-07 left the 1300 kB chunk budget red at 1305.11 kB (attributed ~4.8 kB deps, ~8.9 kB feature code) | info | Resolved by the signed raise 1300→1320 kB (`6325e2e8`, 2026-09-09); the measured 1305.11 kB sits under the raised ceiling. |
| cargo test | — | `repeated_exchanges_never_make_canonical_absent_or_mix_directory_entries` flaked once (passes on re-run) | info | Pre-existing concurrency-test flakiness; not a 52.1 regression (the phase did not touch the cache-publication transaction machinery). NOT re-run in this backfill. |
| 52.1-04 | — | TDD commit-separation deviation: all 4 `tdd="true"` tasks committed tests bundled inside `feat` commits — no `test(52.1-04)` RED-first history; non-vacuity mutation spot-check performed (eviction order + sidecar filename) and reverted; conscious `--force-mvp-gate` override at phase close, on the record | ⚠️ Warning (override) | Process deviation only — the tests exist and pass; recorded in `overrides_applied: 1` (52.1-04-SUMMARY "Phase-close override"). |

No dataUrl-keyed frame registry, no base64 frame transport, and no `toDataURL` capture survives in production code outside the explicit D-19 allowlist (clean-break contract walk, `0e76ff3f`).

### Human Verification Required

The five 52.1-VALIDATION.md manual-only rows status:

- Keyframe-creation fluidity at 1920 — recorded: native roto UAT signed off on feeling (drag, paint-burst, fluidity) 52.1-07; post-fix leave ~120ms (52.1-CONTEXT.md:166). No numeric docSync count — declined by the user, recorded as feeling-based.
- HD export crispness (no upscale blur) — recorded in the 52.1-07 native UAT coverage row (feeling-based).
- Straight-alpha in the packaged WKWebView build vs the Phase 48 D-02 flattened raster — recorded in the 52.1-07 native UAT coverage row; automated `premultiplyAlpha` contract test green (coverage D14).
- Native UAT smoke (open/paint/playback/save) — covered by the end-of-phase native roto UAT (52.1-07); 52.1-01 deferred the smoke row to end-of-phase human verification.
- **Project open time on a many-frame project** — OUTSTANDING: mechanism unit-verified (refs-only load, coverage D8); no wall-clock comparison recorded.

Additionally, two 52.1-CONTEXT.md phase gates have no separately recorded live measurement and are carried to the **Phase 53 native UAT backstop** (ACC-02): profiler `render.*` counters flat during a 10s scrub at 1920, and memory stability across a 100+ frame HD sequence. These are the only outstanding human-verification items; D-11 is tracked separately in `gaps`.

### Gaps Summary

One PARTIAL: **D-11** — the pinning correctness rule is implemented and unit-tested, but no production draw/composite path calls `pin()`/`unpin()`, so the pinned-until-released production behavior is unproven (T-52.1-06/T-52.1-08). The user accepted this as a non-blocking risk (AR-52.1-03, 2026-09-10) — practical risk is low because the draw path is synchronous — and it is earmarked for Phase 53 hardening. It is the sole driver of `status: human_needed`; there are no blocking gaps and no roadmap REQ-ID gaps (the phase carries none by design).

Two decisions are SATISFIED with recorded caveats rather than gaps: D-05/D-07 — the runtime identity and codec transport are bytes, and the surviving base64 surfaces are explicitly allowlisted in the D-19 contract and accepted as AR-52.1-02/T-52.1-04. The remaining security posture is 5 open-below-threshold threat rows (T-52.1-02, -04, -06, -07, -08) with 3 accepted-risk rows, `threats_open: 0` — no open threat at or above the block threshold. The chunk-budget sequence is resolved by the signed 1300→1320 kB raise (`6325e2e8`), and the flaky cargo concurrency test is recorded as pre-existing (not re-run). Phase 52.1 is closed and shipped; the Phase 53 acceptance backstop re-confirms the unrecorded live gates listed under Human Verification Required.

---

_Verified: 2026-09-11T09:00:46Z_
_Verifier: Claude (gsd-executor) — backfill compiled from recorded phase evidence (52.1-01..07 summaries, 52.1-VALIDATION.md, 52.1-SECURITY.md, 52.1-CONTEXT.md, ROADMAP.md); no test re-run, no source modified, every cited commit hash resolves via `git log -1` at write time_
