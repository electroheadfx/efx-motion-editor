---
phase: 52.1-modern-frame-runtime-native-hd-paint
plan: 05
type: execute
wave: 5
depends_on: [52.1-04]
files_modified:
  - app/src/lib/previewRenderer.ts
  - app/src/lib/exportRenderer.ts
  - app/src/lib/exportEngine.ts
  - app/src/efx-paint/compositor/efxPaintCompositor.ts
  - app/src/efx-paint/compositor/efxPaintCompositeCache.ts
autonomous: true
requirements: [D-05, D-13]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "the main-editor preview and export readers draw frames from the LRU ImageBitmap handle, not from a string"
    - "the compositor draws via drawImage(bitmap) with straight-alpha semantics"
    - "the composite cache key is a stable ID / byte term, not a string slice"
  artifacts:
    - path: "app/src/lib/previewRenderer.ts"
      provides: "preview reader retargeted to the LRU handle"
    - path: "app/src/lib/exportRenderer.ts"
      provides: "export reader retargeted to the LRU handle"
    - path: "app/src/efx-paint/compositor/efxPaintCompositor.ts"
      provides: "compositor drawImage(bitmap) path"
    - path: "app/src/efx-paint/compositor/efxPaintCompositeCache.ts"
      provides: "composite cache keyed by stable ID / byte term"
  key_links:
    - from: "app/src/lib/previewRenderer.ts"
      to: "app/src/lib/frameLru.ts"
      via: "preview resolves the LRU handle instead of setting img.src to a string"
      pattern: "frameLru"
    - from: "app/src/efx-paint/compositor/efxPaintCompositor.ts"
      to: "app/src/lib/frameLru.ts"
      via: "compositor draws the LRU ImageBitmap via drawImage(bitmap)"
      pattern: "drawImage"
---

<objective>
Retarget the main-editor preview/export readers and the shared compositor draw path from the string identity to the LRU ImageBitmap handle (D-05/D-13), so every consumer of a Physic Paint frame draws the decoded bitmap instead of decoding a string.

Purpose: This completes D-17 step (1)/(3) on the read side — the compositor is the single shared composition path (Phase 48 CMP-01) and must draw `drawImage(bitmap)` with straight-alpha semantics (D-13), while the preview/export readers stop round-tripping through the string.

Output: preview/export readers and the compositor drawing from the LRU handle; the composite cache keyed by stable ID.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Main-editor preview + export readers draw the LRU handle (D-05)</name>
  <files>app/src/lib/previewRenderer.ts, app/src/lib/exportRenderer.ts, app/src/lib/exportEngine.ts</files>
  <read_first>
    - app/src/lib/previewRenderer.ts (cache key at :121 using a string slice + length; img.src assignment at :645)
    - app/src/lib/exportRenderer.ts (drawImage path at :207)
    - app/src/lib/exportEngine.ts (blobToUint8Array + exportWritePng at :260-266 — PNG export interchange, unchanged)
    - app/src/lib/frameLru.ts (the LRU from Plan 04)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md (preview/export reader retarget)
  </read_first>
  <behavior>
    - Test 1: the preview cache key is derived from a stable ID / byte term, not a string slice + length.
    - Test 2: the preview reader draws the LRU ImageBitmap handle instead of assigning a string to img.src.
    - Test 3: the export reader draws frames from the LRU handle; the final PNG export (import/export interchange) still writes PNG bytes.
  </behavior>
  <action>
    In app/src/lib/previewRenderer.ts, retarget the cache key (:121) from the string slice + length to a stable ID / byte term (the `source.cacheKey` or `sourceRef`/library asset ID), and retarget the frame draw (:645) from assigning a string to `img.src` to drawing the LRU ImageBitmap handle via `drawImage(bitmap)`. In app/src/lib/exportRenderer.ts, retarget the frame draw path (:207) to draw the LRU handle. In app/src/lib/exportEngine.ts, keep the final PNG export (`blobToUint8Array` + `exportWritePng`, :260-266) unchanged — PNG survives as import/export interchange (D-01) — but source the frame pixels from the LRU handle, not a string. Do NOT reintroduce a string round-trip in any reader.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run previewRenderer exportRenderer exportEngine</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - previewRenderer.ts cache key uses a stable ID / byte term (grep "cacheKey" or "sourceRef")
    - previewRenderer.ts draws the LRU handle (grep "drawImage" in the frame draw path)
    - exportEngine.ts still writes PNG (grep "exportWritePng")
    - vitest run previewRenderer exportRenderer exportEngine exits 0; tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The preview and export readers draw frames from the LRU ImageBitmap handle; the final PNG export interchange is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compositor drawImage(bitmap) + stable-ID composite cache key (D-13)</name>
  <files>app/src/efx-paint/compositor/efxPaintCompositor.ts, app/src/efx-paint/compositor/efxPaintCompositeCache.ts</files>
  <read_first>
    - app/src/efx-paint/compositor/efxPaintCompositor.ts (dataUrl→image decode at :243, drawImage at :267/:317)
    - app/src/efx-paint/compositor/efxPaintCompositeCache.ts (dataUrl-slice cache-key idiom at :12/:46, runtime source-bytes state at :85-154)
    - app/src/lib/frameLru.ts (the LRU from Plan 04)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md (Pattern 3 + Pitfall 5)
  </read_first>
  <behavior>
    - Test 1: the compositor draws the LRU ImageBitmap via drawImage(bitmap), not a string→image decode.
    - Test 2: the composite cache key is a stable ID / byte term, not a string slice.
    - Test 3: straight-alpha semantics are preserved through drawImage(bitmap) (Phase 48 D-02).
  </behavior>
  <action>
    In app/src/efx-paint/compositor/efxPaintCompositor.ts, retarget the decode path (:243) from a string→image decode to resolving the LRU ImageBitmap handle, and draw it via `drawImage(bitmap)` (:267/:317) with straight-alpha semantics (D-13, Phase 48 D-02). In app/src/efx-paint/compositor/efxPaintCompositeCache.ts, retarget the cache-key idiom (:12/:46) from a string slice to a stable ID / byte term, and keep the runtime source-bytes state (:85-154) keyed by the same stable ID. The compositor is the single shared composition path — it must draw the LRU handle, never a parallel string decode (Pitfall 5).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run efxPaintCompositor efxPaintCompositeCache</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - efxPaintCompositor.ts draws the LRU handle (grep "drawImage" and "frameLru")
    - efxPaintCompositeCache.ts cache key uses a stable ID / byte term (grep "cacheKey" or "sourceRef")
    - vitest run efxPaintCompositor efxPaintCompositeCache exits 0; tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The compositor draws the LRU ImageBitmap with straight-alpha semantics; the composite cache is keyed by stable ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LRU → compositor draw | ImageBitmap handles cross into drawImage; use-after-close is the risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.1-08 | DoS | compositor draw path | medium | mitigate | Pinning rule (D-11): in-flight draws pin the LRU entry; only the LRU evictor calls bitmap.close() — no use-after-close in drawImage. |
| T-52.1-SC | Tampering | npm/pnpm/cargo installs | high | mitigate | Package-legitimacy audit passed (RESEARCH.md); no new crates — `webp`/`image`/`libwebp-sys` already in Cargo.lock. |
</threat_model>

<verification>
vitest run previewRenderer + exportRenderer + exportEngine + efxPaintCompositor + efxPaintCompositeCache green; tsc --noEmit clean; every reader draws the LRU handle; the composite cache is keyed by stable ID.
</verification>

<success_criteria>
The preview/export readers and the shared compositor draw frames from the LRU ImageBitmap handle with straight-alpha semantics; no reader round-trips through the string identity.
</success_criteria>

## Artifacts this phase produces

- Preview reader drawing the LRU handle — app/src/lib/previewRenderer.ts
- Export reader drawing the LRU handle (PNG interchange preserved) — app/src/lib/exportRenderer.ts + app/src/lib/exportEngine.ts
- Compositor drawImage(bitmap) path — app/src/efx-paint/compositor/efxPaintCompositor.ts
- Composite cache keyed by stable ID — app/src/efx-paint/compositor/efxPaintCompositeCache.ts

<output>
Create .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-05-SUMMARY.md when done
</output>
