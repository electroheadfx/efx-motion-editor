---
phase: 52.1-modern-frame-runtime-native-hd-paint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/src-tauri/Cargo.toml
autonomous: true
requirements: [D-04]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "pnpm install completes clean with the bumped dependency set (no peer-dependency errors)"
    - "vitest run is green and tsc --noEmit is clean on the bumped deps"
    - "the app launches and a native UAT smoke (open/paint/playback/save) passes"
  artifacts:
    - path: "app/package.json"
      provides: "bumped JS deps: @tauri-apps/api 2.11.5, @tauri-apps/cli 2.11.5, preact 10.29.8, @preact/signals + @preact/preset-vite latest compatible"
    - path: "app/src-tauri/Cargo.toml"
      provides: "bumped Rust deps: tauri/tauri-build 2.11.5, plugins fs/store/dialog/notification/shell 2.11.5"
  key_links: []
---

<objective>
Bump the Tauri and Preact dependency floors (D-04) before any runtime/codec work, so the codec and LRU plans build against the patched IPC-ACL and CVE-fixed runtime.

Purpose: D-04 is Step 0 of the phase — the 2.11.1 IPC ACL security fix and the CVE-2026-22028 Preact patch must be in place before the Rust codec commands and the JS byte-transfer bridge are written.

Output: app/package.json and app/src-tauri/Cargo.toml carrying the bumped versions; a green vitest + tsc + cargo build baseline.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump JS + Rust dependency floors (D-04)</name>
  <files>app/package.json, app/src-tauri/Cargo.toml</files>
  <read_first>
    - app/package.json (current dependency versions)
    - app/src-tauri/Cargo.toml (current tauri/plugin versions)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md (Standard Stack + Installation sections)
  </read_first>
  <action>
    Bump app/package.json dependencies: @tauri-apps/api ^2.10.1 → 2.11.5, @tauri-apps/cli ^2.10.0 → 2.11.5, preact ^10.28.4 → 10.29.8, @preact/signals ^2.8.1 → latest compatible, @preact/preset-vite ^2.10.3 → latest compatible. Do NOT touch Preact 11 (RC, breaking — D-04). Bump app/src-tauri/Cargo.toml: tauri = "2" → "2.11.5", tauri-build = "2" → "2.11.5", and the tauri-plugin-fs/store/dialog/notification/shell entries to 2.11.5. Leave webp = "0.3.1" and image (webp feature) unchanged. Run pnpm install and cargo build --manifest-path app/src-tauri/Cargo.toml.
  </action>
  <verify>
    <automated>pnpm install</automated>
    <fails_when>non-zero exit, or any "ERR_PNPM" / "peer" error line in the output</fails_when>
    <automated>cargo build --manifest-path app/src-tauri/Cargo.toml</automated>
    <fails_when>non-zero exit, or "error[" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - app/package.json lists @tauri-apps/api 2.11.5, @tauri-apps/cli 2.11.5, preact 10.29.8 (grep the exact version strings)
    - app/src-tauri/Cargo.toml lists tauri 2.11.5 and tauri-build 2.11.5
    - pnpm install exits 0 with no peer-dependency errors
    - cargo build exits 0
  </acceptance_criteria>
  <done>Both manifest files carry the bumped versions; pnpm install and cargo build succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Full D-04 gates + native UAT smoke</name>
  <files>app/package.json, app/src-tauri/Cargo.toml</files>
  <read_first>
    - app/package.json (confirm bumped versions)
  </read_first>
  <action>
    Run the D-04 gate suite: pnpm --filter efx-motion-editor exec vitest run (never watch mode), pnpm --filter efx-motion-editor exec tsc --noEmit, and confirm the app launches. Native UAT smoke (human): open a project, paint a stroke, playback, save.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
    <human-check>Launch the app and smoke-test open/paint/playback/save on a small project — no crash, no regression vs the pre-bump build.</human-check>
  </verify>
  <acceptance_criteria>
    - vitest run exits 0 with a non-zero passed count
    - tsc --noEmit exits 0 with no "error TS" lines
    - the app launches and the native UAT smoke (open/paint/playback/save) passes
  </acceptance_criteria>
  <done>All D-04 gates pass: pnpm install clean, vitest green, tsc clean, app launches, native UAT smoke passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/pnpm registry → app | Dependency versions are pulled from the registry at install time |
| crates.io → Rust build | Cargo resolves tauri/webp/image from crates.io |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.1-SC | Tampering | npm/pnpm/cargo installs | high | mitigate | Package-legitimacy audit already passed (RESEARCH.md §Package Legitimacy Audit: webp/image/libwebp-sys all OK/Approved, already in Cargo.lock); D-04 is a version bump of already-approved packages, gated by the D-04 gates (pnpm install clean, vitest green, tsc clean, app launches, native UAT smoke). No [ASSUMED]/[SUS]/[SLOP] packages. |
</threat_model>

<verification>
pnpm install clean; vitest run green; tsc --noEmit clean; cargo build succeeds; app launches; native UAT smoke passes.
</verification>

<success_criteria>
The dependency floors are bumped (Tauri 2.11.5, Preact 10.29.8) and the full D-04 gate suite is green, establishing the baseline the codec and LRU plans build on.
</success_criteria>

## Artifacts this phase produces

- No new symbols — this plan only bumps dependency versions in app/package.json and app/src-tauri/Cargo.toml.

<output>
Create .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-01-SUMMARY.md when done
</output>
