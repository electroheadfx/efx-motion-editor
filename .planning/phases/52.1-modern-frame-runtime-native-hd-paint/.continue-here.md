---
context: phase
phase: 52.1-modern-frame-runtime-native-hd-paint
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-06T10:48:56.205Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Executor output-token cap — two gsd-executor agents died mid-plan at the 32k output-token maximum (their final responses were too long). Mitigation: every executor prompt MUST instruct "keep your final report under 2000 tokens" and commit per task so a second cap hit stays recoverable.
- [ ] CONSTRAINT: Isolation guard sentinel — the PreToolUse hook blocks Agent dispatch when (a) the sentinel's `plan` id mismatches the dispatch description, or (b) the sentinel is older than 10 minutes. Mitigation: re-record the sentinel IMMEDIATELY before each dispatch (`gsd_run query dispatch-isolation --raw --phase "52.1" --plan "52.1-0N" --force-isolation none`) and use description `"Execute plan 52.1-0N of phase 52.1"` (the guard extracts plan id from the description via regex).
- [ ] CONSTRAINT: Plan 03's estimate (50k tokens) was wildly low — the dataUrl→bytes type retarget rippled to ~50 files and ~450 tsc errors. Plans 04-07 touch the same frame pipeline; expect similar ripple and budget accordingly.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Long executor final reports | Two executors died at the 32k output cap; the second left 12 modified files uncommitted and no SUMMARY | blocking | Prompt executors to keep final reports <2000 tokens; commit per task |
| Stale/mismatched isolation sentinel | Dispatch blocked twice by the guard hook (plan id "01" vs "52.1-01" mismatch; 10-min staleness) | blocking | Re-record sentinel per plan immediately before dispatch; description must carry the full plan id "52.1-0N" |
| Mechanical regex passes over test fixtures | Broad sed-style replacements broke nested backtick templates and renamed engine-API string params (settle listeners take URL strings, not bytes) | advisory | Fix test fixtures per-site; engine-facing APIs (setPreviewBaseImageUrl, settle listeners) stay URL-string based |

<current_state>
Phase 52.1 plan 03 (frame identity retarget, D-05/D-06/D-07/D-18) recovery, executed INLINE by the orchestrator after two executors died at the output cap. The user approved a recovery protocol: verify (tsc/vitest/greps) BEFORE committing, commit the inherited GREEN work as ONE atomic commit, continue remaining tasks, write SUMMARY.md last.

Current git state: RED test commit a610f2ae landed. The entire GREEN implementation (production + tests) sits UNCOMMITTED in the working tree — 108 modified files + 1 new file (app/src/testUtils/testWebpBytes.ts). `cd app && pnpm exec tsc --noEmit` = 0 errors. `pnpm exec vitest run` = 69 failures (all semantic assertion mismatches), 2647 passing.

Plans 52.1-01 and 52.1-02 are COMPLETE (committed, SUMMARYs exist). Plans 04-07 not started.
</current_state>

<completed_work>

- Task 1 (frame type): `PhysicPaintRenderedFrame.bytes: Uint8Array` replaces dataUrl; `isWebpBytes` RIFF/VP8L probe; `buildFrameBytesToken` (length+head-64+tail-64 hex); thumbnail result retargeted to bytes; background-only-support marker = empty bytes (validator accepts).
- Task 2 (capture): `buildRotoFrameFromCanvas` is now async — getImageData + `invoke('encode_webp_frame')`; `encodeRotoFrameFromCanvas` same; `buildBlankRotoFrame` stays sync via `canvasToWebpBytes` (browser WebP encoder); `registerRotoAlphaCanvasFrameFromBytes` decodes via Blob+createImageBitmap; `prepareRotoPhysicalRealKeyFrames` (renamed from ...Pngs).
- Task 3 (registries + apply): `_rotoAlphaCanvasRegistry` keyed by bytes token; `_backgroundSourceImages`/`_referenceSourceImages` values are Uint8Array; `_compositorDecode` takes bytes (Blob URL); dedup collectors use bytes tokens; `_trackContentRevision` uses the token; apply payload carries `onionBytes`; `bytesToBase64`/`base64ToBytes` deleted from the transport; document-sync backgroundSources carry base64 at the JSON boundary (`encodeSourceBytesForDocumentSync` in src/lib/physicPaintBridge.ts — documented deviation).
- Rust: thumbnail command retargeted to raw bytes (rgba Vec<u8> in / bytes Vec<u8> out); `canonical_physical_hash` retargeted to the bytes token (`d<len>:<head-hex>:<tail-hex>;`) matching JS `buildFrameBytesToken`.
- Canonical intent serialization: bytes → base64 in the canonical JSON (`canonicalPhysicalEditPayload`); the payload validator accepts both Uint8Array and canonical base64 forms.
- Legacy pre-G-52-6 revision acceptance DELETED (clean break, no compat).
- Test fixtures retargeted across ~60 test files (722 → 0 tsc errors); `testWebpBytes` helper created.
</completed_work>

<remaining_work>

1. **Fix 69 vitest failures** (semantic). Known clusters:
   - `src/types/physicPaint.test.ts` (4): intent serialization expectations still pin old dataUrl JSON strings (lines ~285, ~374) — update to the base64 canonical form; the legacy-revision test must now expect REJECTION (clean break); the Rust hash pin `'project-416-550fa14e'` must be updated to the new value (run the test to see the actual, then pin it — the hash legitimately changed with the bytes-token cutover).
   - Reveal renderer tests (`physicsPaintRotoPlayScriptRenderer`): reference input is now bytes; the mock engine's image load path changed (Blob URL).
   - Roto reference controller tests: `setPreviewBaseImageUrl` now receives a Blob URL from `getFrameBlobUrl` — assertions comparing dataUrl strings need updating.
   - Persistence tests (`efxPaintPersistence.test.ts`): sidecars are now raw WebP bytes (no PNG encode/decode); assertions on PNG dataUrls need updating.
   - Flattened draw-log tests: decode bytes with `new TextDecoder().decode(bytes)` before `.match(/draw\(/g)` assertions (the mock canvas's toDataURL log is now inside the WebP bytes).
   - Timeline selectors / play script loop tests: bytes content comparisons (use `testWebpBytes(seed)` consistently).
2. **Grep-verify must_haves**: no dataUrl-keyed registry survives; `resolveContentToken`/dedup key on bytes tokens; transport apply path uses invoke; `grep -c "bytesToBase64" app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts == 0`.
3. **Commit the GREEN work as ONE atomic commit** (user-approved recovery protocol): message noting completion by a recovery executor, e.g. `feat(52.1-03): retarget runtime frame identity to WebP bytes (D-05/D-06/D-07/D-18) — completed by recovery executor`.
4. **Write 52.1-03-SUMMARY.md**: must state what was inherited uncommitted vs written fresh, plus the tsc/vitest/must-have verification evidence. Then commit metadata (SUMMARY + STATE + ROADMAP + REQUIREMENTS).
5. **Wave 3 post-merge gate + wave:post gates** (build `pnpm run build`, test `pnpm --filter efx-motion-editor exec vitest run`, then `gsd_run check verify.schema-drift 52.1`, `verify.codebase-drift 52.1`, `ui.safety-gate 52.1`).
6. **Waves 4-7**: dispatch gsd-executor agents sequentially for plans 52.1-04 (LRU), 05 (preview/export/compositor retarget), 06 (cap 1000→1920), 07 (clean-break contract). Per dispatch: re-record sentinel, description "Execute plan 52.1-0N of phase 52.1", model="opus", prompt = the standard sequential-mode template (execute-plan.md + summary.md + checkpoints.md + tdd.md + worktree-path-safety.md + executor-examples.md inlined) + "keep final report under 2000 tokens". TDD mode is ON (tdd_mode=true) — RED/GREEN discipline applies to behavior-adding tasks.
7. **Phase tail**: aggregate_results → code_review_gate (code-review capability active; tdd.review-checkpoint gate active — TDD_MODE=true so a block escalates) → regression-gate (section_manifest included) → verify_phase_goal (spawn gsd-verifier, model="sonnet"; mempalace contribution active — inject the capture fragment into the verifier prompt) → update_roadmap (phase.complete).
</remaining_work>

<decisions_made>

- ISOLATION=none (base-check #683 degrade — HEAD diverged from origin/HEAD): all 7 plans run sequentially on the main tree. Intended behavior, not a bug.
- Blend/interpolation path uses sync `canvasToWebpBytes` (toDataURL('image/webp')+atob) — the store's regeneration API is synchronous; the async Rust encoder would ripple through the whole store mutation API. Documented deviation.
- Document-sync backgroundSources carry base64 at the JSON persistence boundary only; the frame apply path uses raw invoke. Documented deviation.
- Persisted script thumbnail keeps `dataUrl` (disk JSON format, out of scope).
- Legacy revision acceptance deleted (clean break).
- Canonical intent serialization: bytes → base64; validator accepts both forms.
- Rust canonical hash: bytes token matching JS `buildFrameBytesToken`.
- `getFrameBlobUrl` (memoized bytes→Blob URL) so the completion guard's URL identity comparison stays stable; absorbed by the LRU in Plan 04.
</decisions_made>

<blockers>
- None blocking. The 69 test failures are the only remaining plan-03 work.
</blockers>

## Required Reading (in order)
1. `.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-03-PLAN.md` — the plan being executed (must_haves, acceptance criteria).
2. `.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-CONTEXT.md` — locked decisions D-01..D-19.
3. `.planning/HANDOFF.json` — machine-readable state.
4. `app/src/testUtils/testWebpBytes.ts` — the fixture helper every test uses.
5. `app/src/types/physicPaint.ts` — `isWebpBytes`, `buildFrameBytesToken`, `canonicalPhysicalEditPayload`.

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Long executor final reports → 32k output cap killed two executors → instruct executors to keep reports <2000 tokens and commit per task.
- [ANTI-PATTERN]: Stale/mismatched isolation sentinel → guard hook blocks dispatch → re-record per plan immediately before dispatch; description carries the full plan id.
- [ANTI-PATTERN]: Broad regex passes over test fixtures → broke nested backtick templates and engine-API string params → fix per-site; engine-facing APIs stay URL-string based.

## Infrastructure State
- No background processes. No async jobs. No servers running (CLAUDE.md: never run the server).
- Branch: v1.0.0/modern-runtime-hd. Worktrees disabled for this run (ISOLATION=none).
- pnpm monorepo; tests via `pnpm exec vitest run` (never watch mode); app dir is `app/`.

<context>
The orchestrator executed the plan-03 recovery inline after two executor deaths. The retarget design: runtime frames carry compact WebP bytes (Uint8Array) end-to-end; capture = getImageData + Rust encode_webp_frame; registries keyed by stable IDs/bytes tokens; decode via Blob URL + createImageBitmap; the LRU (Plan 04) will absorb the decode-once caches. The remaining test failures are all mechanical assertion updates — the production code compiles clean and the architecture is sound.
</context>

<next_action>
Start with: `cd app && pnpm exec vitest run 2>&1 | grep -E "×" | head -80` to list the 69 failures, then fix them cluster by cluster (types test pins first, then reveal renderer, reference controller, persistence, flattened draw-log decodes). After all green: grep-verify must_haves, commit the GREEN work as ONE atomic commit, write SUMMARY.md, then continue waves 4-7.
</next_action>
