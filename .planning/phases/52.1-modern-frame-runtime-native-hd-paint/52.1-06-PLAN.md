---
phase: 52.1-modern-frame-runtime-native-hd-paint
plan: 06
type: execute
wave: 6
depends_on: [52.1-05]
files_modified:
  - app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts
  - app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts
  - packages/efx-physic-paint/src/types.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
autonomous: true
requirements: [D-15, D-16]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "the working-resolution cap is 1920 (native HD 1080p), a single-constant change"
    - "pixel-relative physics/brush defaults are rescaled ~1.9x in the same plan as the cap raise"
    - "nothing in the LRU, IPC, or cache code hard-codes a resolution assumption (2K/4K stays a single-constant change)"
  artifacts:
    - path: "app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts"
      provides: "PHYSICS_PAINT_WORKING_LONG_EDGE = 1920"
    - path: "app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts"
      provides: "brush size default rescaled ~1.9x"
    - path: "packages/efx-physic-paint/src/types.ts"
      provides: "pixel-relative physics constants rescaled ~1.9x"
    - path: "packages/efx-physic-paint/src/engine/EfxPaintEngine.ts"
      provides: "brushRenderRadius rescaled via size"
  key_links:
    - from: "app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts"
      to: "app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts"
      via: "the single cap constant drives the working size; the brush default scales with it"
      pattern: "PHYSICS_PAINT_WORKING_LONG_EDGE"
---

<objective>
Raise the working-resolution cap 1000→1920 (D-15) as a single-constant change and rescale the pixel-relative physics/brush defaults ~1.9x in the same plan (D-17), while keeping 2K/4K activation a single-constant change (D-16).

Purpose: This is D-17 step (4) "cap raise LAST" — it lands only after the runtime (identity + codec + LRU) is proven, so a UAT regression can be bisected between runtime and resolution. The ~1.9x rescale ships here, never deferred.

Output: `PHYSICS_PAINT_WORKING_LONG_EDGE = 1920`, rescaled brush/physics defaults, and the single-constant contract verified.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md
@.planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap raise 1000→1920 + single-constant contract (D-15, D-16)</name>
  <files>app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts, app/src/components/physic-paint/engine/physicsPaintCanvasSizing.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts (PHYSICS_PAINT_WORKING_LONG_EDGE at :3, getPhysicsPaintWorkingSize at :8)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-PATTERNS.md (physicsPaintCanvasSizing section)
  </read_first>
  <behavior>
    - Test 1 (RED): getPhysicsPaintWorkingSize(1920, 1080) returns { width: 1920, height: 1080 } (cap raised, no downscale).
    - Test 2: getPhysicsPaintWorkingSize(3840, 2160) still scales down to the 1920 long edge (2K/4K not yet activated).
    - Test 3: the cap is a single constant — changing PHYSICS_PAINT_WORKING_LONG_EDGE is the only edit needed to change the working size.
  </behavior>
  <action>
    In app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts, change `PHYSICS_PAINT_WORKING_LONG_EDGE` from 1000 to 1920 (:3). `getPhysicsPaintWorkingSize` (:8) already scales by the constant — no other change. Verify the single-constant contract (D-16): grep the LRU, IPC, and cache code for any hard-coded resolution assumption (e.g. a literal 1000 or a frame-count budget) and confirm none exists — the byte-budgeted LRU (Plan 04) is resolution-agnostic by construction. Create/retarget app/src/components/physic-paint/engine/physicsPaintCanvasSizing.test.ts with the three tests above.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run physicsPaintCanvasSizing</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - PHYSICS_PAINT_WORKING_LONG_EDGE is 1920 (grep "1920" in physicsPaintCanvasSizing.ts)
    - getPhysicsPaintWorkingSize(1920, 1080) returns 1920×1080 (test green)
    - no hard-coded resolution assumption in LRU/IPC/cache code (grep confirms byte-budget, not frame-count)
    - vitest run physicsPaintCanvasSizing exits 0 with a non-zero passed count
  </acceptance_criteria>
  <done>The working cap is 1920 as a single-constant change; the single-constant contract (D-16) is verified across LRU/IPC/cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ~1.9x physics/brush rescale (D-15)</name>
  <files>app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts, packages/efx-physic-paint/src/types.ts, packages/efx-physic-paint/src/engine/EfxPaintEngine.ts</files>
  <read_first>
    - app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts (brush size default at :26)
    - packages/efx-physic-paint/src/types.ts (DEFAULT_WIDTH/DEFAULT_HEIGHT at :8-9, DENSITY_NORM/LUT_SIZE at :12-13, BLOW_STRENGTH at :15)
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts (brushRenderRadius at :172-174)
    - .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-RESEARCH.md (Assumptions A3/A4/A5 — which constants are pixel-relative)
  </read_first>
  <behavior>
    - Test 1: the brush size default is rescaled ~1.9x (6 → ~11-12) so relative stroke width is preserved at 1920.
    - Test 2: brushRenderRadius = size/2 remains pixel-relative and scales with the rescaled size.
    - Test 3: per-pixel density normalization constants (DENSITY_NORM/LUT_SIZE) are NOT rescaled.
  </behavior>
  <action>
    In app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts, rescale the brush `size` default (:26) from 6 to ~11-12 (~1.9x, D-15) so relative stroke width is preserved at 1920. In packages/efx-physic-paint/src/types.ts, rescale the pixel-relative physics constants ~1.9x: `BLOW_STRENGTH` (:15, per-cell fluid force — A3) scales ~1.9x; `DENSITY_NORM`/`LUT_SIZE` (:12-13, per-pixel density normalization — A5) do NOT rescale. In packages/efx-physic-paint/src/engine/EfxPaintEngine.ts, `brushRenderRadius` (:172-174) stays `size/2` (pixel-relative, A4) and scales via the rescaled size. The exact calibration values are determined by a native UAT at 1920 (dense Physics strokes) — the ~1.9x is the target, not a hard-coded guess; keep the rescale in this same plan (D-17, never deferred).
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run physicsPaintStudioSettings</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - brush size default is rescaled ~1.9x (grep "size:" in physicsPaintStudioSettings.ts shows ~11-12)
    - BLOW_STRENGTH is rescaled ~1.9x (grep "BLOW_STRENGTH" in types.ts)
    - DENSITY_NORM/LUT_SIZE are unchanged (grep "DENSITY_NORM" and "LUT_SIZE" still 3000)
    - vitest run physicsPaintStudioSettings exits 0; tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The pixel-relative physics/brush defaults are rescaled ~1.9x in the same plan as the cap raise; per-pixel density normalization is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The cap raise and rescale are config/constant changes; no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52.1-SC | Tampering | npm/pnpm/cargo installs | high | mitigate | Package-legitimacy audit passed (RESEARCH.md); no new crates — `webp`/`image`/`libwebp-sys` already in Cargo.lock. |
</threat_model>

<verification>
vitest run physicsPaintCanvasSizing + physicsPaintStudioSettings green; tsc --noEmit clean; the cap is 1920 as a single constant; the ~1.9x rescale ships in the same plan.
</verification>

<success_criteria>
The working-resolution cap is 1920 (native HD) as a single-constant change, the pixel-relative physics/brush defaults are rescaled ~1.9x in the same plan, and 2K/4K activation remains a single-constant change.
</success_criteria>

## Artifacts this phase produces

- `PHYSICS_PAINT_WORKING_LONG_EDGE = 1920` — app/src/components/physic-paint/engine/physicsPaintCanvasSizing.ts
- Rescaled brush size default (~1.9x) — app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts
- Rescaled pixel-relative physics constants — packages/efx-physic-paint/src/types.ts
- brushRenderRadius scaling via size — packages/efx-physic-paint/src/engine/EfxPaintEngine.ts

<output>
Create .planning/phases/52.1-modern-frame-runtime-native-hd-paint/52.1-06-SUMMARY.md when done
</output>
