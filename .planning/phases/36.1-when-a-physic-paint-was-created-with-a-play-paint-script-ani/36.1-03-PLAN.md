---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 03
type: execute
wave: 2
depends_on:
  - 36.1-01
files_modified:
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
autonomous: true
requirements:
  - PH36.1-TL
must_haves:
  truths:
    - "The main EFX Motion timeline shows multiple saved Play script markers nested inside the existing Physic Paint layer bar per D-01."
    - "Markers are polished endpoint/range graphics, use active accent highlighting, and follow existing timeline zoom/scroll frame math per D-02 through D-05."
  artifacts:
    - path: "app/src/components/timeline/TimelineRenderer.ts"
      provides: "Canvas 2D nested Play marker drawing for physic-paint FX bars"
      exports: ["TimelineRenderer"]
    - path: "app/src/components/timeline/TimelineRenderer.test.ts"
      provides: "Source/geometry tests for marker math and UI constraints"
  key_links:
    - from: "app/src/components/timeline/TimelineRenderer.ts"
      to: "timeline draw state / FX track layout"
      via: "saved Play marker data passed through DrawState or FxTrackLayout"
      pattern: "frameWidth.*scrollX.*TRACK_HEADER_WIDTH"
---

<objective>
Render saved Play script ranges as nested polished markers inside the existing Physic Paint timeline layer bar.

Purpose: The user can see where each saved physics Play animation lives over time and which one is active under the editor scrubber, without adding rows or ASCII/text marker badges.
Output: Timeline renderer marker drawing and tests for PH36.1-TL.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-01-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/timeline/TimelineRenderer.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
</context>

## Artifacts this phase produces

- Type/interface: `TimelinePlayScriptMarker`
- Type/interface field on `DrawState` or `FxTrackLayout`: `playScriptMarkers`
- Function/helper: `getTimelinePlayScriptMarkerGeometry`
- Method/helper: `drawPhysicPaintPlayScriptMarkers`
- New file: `app/src/components/timeline/TimelineRenderer.test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tested marker geometry without direct store reads</name>
  <files>app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - app/src/components/timeline/TimelineRenderer.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
  </read_first>
  <behavior>
    - Test marker geometry for `{ startFrame: 8, frameCount: 4 }` at `frameWidth: 60`, `scrollX: 120` returns x `8 * 60 - 120 + TRACK_HEADER_WIDTH` and width `4 * 60` per D-04.
    - Test the source does not import `physicPaintStore` in `TimelineRenderer.ts`, preserving pure DrawState rendering.
    - Test marker source does not contain literal ASCII UI examples `[4]`, `---`, `|-----|`, or frame badge label rendering per D-02 and D-03.
  </behavior>
  <action>Create a small exported or testable helper `getTimelinePlayScriptMarkerGeometry(marker, frameWidth, scrollX)` in `TimelineRenderer.ts` or a colocated helper only if the project pattern already allows it. Add a marker data shape with concrete fields `id`, `startFrame`, `frameCount`, and `active`. Thread marker data through `DrawState` or `FxTrackLayout` as `playScriptMarkers` rather than importing `physicPaintStore` into the renderer. Use the same coordinate formula as FX bars: `marker.startFrame * frameWidth - scrollX + TRACK_HEADER_WIDTH` for x and `marker.frameCount * frameWidth` for width. Add `TimelineRenderer.test.ts` using Vitest source/geometry assertions.</action>
  <acceptance_criteria>
    - Source assertion: `TimelineRenderer.ts` has `playScriptMarkers` data on renderer input types and no `physicPaintStore` import.
    - Behavior assertion: helper math uses `frameWidth`, `scrollX`, and `TRACK_HEADER_WIDTH` exactly as the FX bar range math does.
    - Source assertion: no default marker frame badge text is rendered in the marker helper or drawing method.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run app/src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <done>Timeline marker geometry is testable, frame-scale based, and renderer-pure for D-01 and D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draw polished nested Play script endpoint/range markers</name>
  <files>app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - app/src/components/timeline/TimelineRenderer.ts
    - app/src/components/timeline/TimelineRenderer.test.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test source contains a dedicated draw helper such as `drawPhysicPaintPlayScriptMarkers` and uses `roundRect`, endpoint drawing, and active/subdued styles.
    - Test source uses an accent color for active markers and a subdued alpha/color for inactive markers per D-05.
  </behavior>
  <action>Add `drawPhysicPaintPlayScriptMarkers(ctx, markers, barX, barW, barY, barH, frameWidth, scrollX, canvasWidth)` or equivalent private renderer method. Draw each marker centered within the existing Physic Paint layer bar height, clipped to the parent bar and viewport. Render a slim rounded range fill plus start/end endpoint dots or caps, with active marker accent highlight and inactive markers subdued. Do not render markers as separate rows, text labels, frame badges, ASCII-looking bracket/dash graphics, or DOM overlays. Draw markers after the parent FX bar fill/border and before label/handle overlays if that keeps them visible inside the bar.</action>
  <acceptance_criteria>
    - UI behavior assertion: a Physic Paint layer with saved ranges `[0..4]`, `[8..23]`, `[30..34]` renders three nested marker graphics inside one existing layer bar.
    - UI behavior assertion: active marker under the timeline frame uses accent highlight; other markers remain subdued.
    - Source assertion: marker drawing is Canvas 2D in `TimelineRenderer.ts`, not a DOM overlay and not a new timeline row.
    - Source assertion: marker x/width are clipped to the parent bar and viewport before drawing.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run app/src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <done>Saved Play script markers are visible, polished, nested, active-aware, and zoom/scroll synchronized per D-01 through D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store/layout data -> Canvas renderer | Saved range metadata is visualized on the timeline and must not execute code or mutate state. |
| Timeline UI -> user interpretation | Malformed marker positions can misrepresent saved script ownership and cause accidental edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-07 | Tampering | `TimelineRenderer` marker data | mitigate | Consume validated layout marker data only and keep renderer pure; no direct store mutation or cross-window messages in drawing code. |
| T-36.1-08 | Repudiation | Timeline marker UI | mitigate | Active marker is derived from scrubber containment and rendered distinctly so users can identify which saved script will open. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned or required for this phase. |
</threat_model>

<verification>
Run the timeline renderer focused test. Visual verification occurs in Plan 06 because Claude must not run the app/server.
</verification>

<success_criteria>
- Marker UI is nested inside one Physic Paint bar, not a new row.
- Marker geometry follows zoom/scroll and uses no default frame badges.
- Renderer remains pure and receives data through draw state/layout.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-03-SUMMARY.md` when done.
</output>
