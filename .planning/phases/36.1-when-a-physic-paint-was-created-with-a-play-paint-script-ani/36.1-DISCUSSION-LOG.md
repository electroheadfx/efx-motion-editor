# Phase 36.1: Physics Paint Play-Script Timeline Markers and Sequential Playback - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-13
**Phase:** 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
**Areas discussed:** Layer markers, Open correct script, Preview scrub, One-hand playback

---

## Layer markers

| Question | Options | User's choice | Notes |
|----------|---------|---------------|-------|
| In the main EFX Motion timeline, how should multiple Play scripts appear inside one Physic Paint layer? | Nested mini-ranges; Separate rows; Only markers; You decide | Nested mini-ranges | Keep one Physic Paint layer bar and draw multiple saved Play script ranges inside it. |
| What information should each nested Play script marker show directly on the timeline? | Frame endpoints; Script name; Both when space; You decide | Visual range marker, not frame text | User clarified the examples like `[4]---[10]`, `|-----|`, and `•----•` were explanatory only. The timeline already shows frame/time; the UI should be a polished centered graphic inside the layer block height that zooms/moves with the timeline. |
| How should the active Play script marker be distinguished in the editor timeline? | Accent highlight; Playhead-linked; Manual selection; You decide | Accent highlight | Active marker highlighted; inactive markers subdued. |
| When there are several saved Play scripts on one physics layer, should their ranges be allowed to overlap? | No overlap; Allow overlap; Warn only; You decide | No overlap | Keeps opening and rendering deterministic. |

---

## Open correct script

| Question | Options | User's choice | Notes |
|----------|---------|---------------|-------|
| When the user clicks `[open fx paint canvas]`, how should the app choose which saved Play script to open? | Scrubber-inside; Click marker; Nearest segment; You decide | Scrubber-inside | Positioning the editor scrubber over a saved script range opens that script. |
| If the editor scrubber is not inside any saved Play script range, what should `[open fx paint canvas]` do? | Open new script; Open Roto; Ask first; You decide | Open Roto with Play max constraint available | User clarified: open Roto at the current frame. If switching to Play canvas from there, evaluate/send the maximum frame count based on the gap before the next script. |
| When the editor scrubber is inside a saved Play script, what should the standalone open to? | Play tab at frame; Play tab start; Ask mode; You decide | Play tab at frame | Open the Play canvas for that script at the relative frame under the scrubber. |
| If there is only a tiny gap before the next saved script, how should the maximum new Play duration be handled? | Clamp duration; Disable Play; Allow replace; You decide | Clamp duration | Clamp to available no-overlap gap and show a clear max/limit message. |

---

## Preview scrub

| Question | Options | User's choice | Notes |
|----------|---------|---------------|-------|
| Inside the Physics Paint Play canvas tab, what should happen when you drag/scrub the local Play timeline to an in-between frame? | Preview only; Sync editor too; Autosave frame; You decide | Preview only | Scrub is inspection/preview only until `Save play`. |
| Should local Play canvas scrubbing move the editor scrubber/playhead while the Physics Paint window is open? | No editor move; Move editor; On release; You decide | No editor move | Avoid surprising main-timeline jumps during standalone inspection. |
| What should the local Play timeline show for a saved script range when opened at an in-between frame? | Start-end-current; Frame ticks; Progress percent; You decide | Start-end-current | Show start, end, and current preview position. |
| When scrubbing in a saved Play script, which source should the preview render from? | Editable script; Cached frames; Fallback both; You decide | Cached frames for saved scripts; live editable after remake | User clarified cached frames should be faster. If clearing/remaking a new animation, old cached frames are no longer preview truth; preview comes from live editable state until `Save play` replaces the cache. |

---

## One-hand playback

| Question | Options | User's choice | Notes |
|----------|---------|---------------|-------|
| For Play script rendering, how should strokes be distributed across the animation duration? | Sequential strokes; Timestamp groups; Current parallel; You decide | Sequential strokes | Like one artist hand: C then h then l, not multiple letters at the same time. |
| Within each stroke, what should determine how many frames it receives during sequential playback? | By stroke length; Equal per stroke; By point count; You decide | By stroke length | Longer strokes receive more frames; shorter strokes may complete quickly. |
| If the animation duration is too short for all strokes to animate smoothly, what should happen? | Compress all; Warn user; Extend range; You decide | Compress all | The chosen timeline range/duration is authoritative. |
| Should sequential playback become the only Play animation behavior, or should the old parallel timestamp behavior remain selectable? | Only sequential; Mode toggle; Hidden fallback; You decide | Only sequential | No user-facing parallel/timestamp mode. |

---

## Claude's Discretion

- Exact storage shape for multiple saved Play script ranges.
- Exact marker geometry/styling, as long as it is polished and centered inside the layer bar.
- Exact stroke-length allocation formula for sequential playback.

## Deferred Ideas

- Later performance/cache phase: think more broadly about caching for other physics paint preview-play paths because drawing physics paint is very slow.
- Future source-lane model and advanced overlap/merge/conflict handling remain out of Phase 36.1.
