---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 08
type: execute
wave: 6
depends_on:
  - 36.1-07
files_modified:
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/PhysicsPaintTopBar.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements:
  - PH36.1-BRIDGE
  - PH36.1-PLAY-SCRUB
must_haves:
  truths:
    - "Standalone EFX Physics opens in the selected `Roto paint` or `Play paint` mode without an internal Roto/Play tab switch per UAT Test 2 and UI-SPEC section 2."
    - "The standalone timeline starts at the launch start frame for gap opens and shows only the available Play frames when max duration is one per UAT Test 2."
    - "Render actions are text-labeled `Render` in Roto and Play contexts, and the icon-only Play render button is removed per UAT Test 4 and UI-SPEC section 5."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Mode-specific standalone strip with no Roto/Play tab switch and clear Render buttons"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Selected-mode launch behavior and max-duration propagation"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "UI-SPEC compliant layout/style for mode labels, Render buttons, cached/limit states"
  key_links:
    - from: "launchContext.workflowMode"
      to: "PhysicsPaintWorkflowStrip"
      via: "fixed selected mode and maxPlayFrameCount props"
      pattern: "workflowMode|maxPlayFrameCount|Render"
    - from: "PhysicsPaintWorkflowStrip"
      to: "savePlay/playPreview/saveRotoFrame"
      via: "text Render button for render generation, Save play remains publishing boundary"
      pattern: "Render|Save play|onRenderPlay|onRenderRoto"
---

<objective>
Make the standalone Physics Paint window mode-specific and replace icon-only render/play controls with clear `Render` text actions.

Purpose: UAT Tests 2 and 4 failed because users had to switch tabs inside EFX Physics and the Play icon was actually rendering, not full playback.
Output: Mode-specific standalone workflow strip, no internal tab switch, launch-frame timeline origin, clear Render labels, and focused UI/source tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UAT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-04-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-07-PLAN.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintStudio.css
</context>

## Artifacts this phase produces

- Prop on `PhysicsPaintWorkflowStripProps`: fixed/locked selected-mode behavior, if needed as `modeLocked?: boolean` or equivalent.
- UI label: `Roto paint` or `Play paint` as a non-tab active mode label.
- Removed internal switch affordance: `physics-paint-workflow-tabs`/`role="tablist"` no longer appears for Phase 36.1 selected-mode opens.
- UI action label: `Render` for Roto and Play render-generation contexts.
- Tooltip/copy: `Render cached frames for this script. Watch full playback in EFX Motion.`
- Tests/source assertions for no icon-only render button and no internal Roto/Play tab switch.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove internal Roto/Play tabs and lock standalone to launch mode</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Test/source-contract that `role="tablist"`, `Roto canvas`, and `Play canvas` switching buttons are absent from Phase 36.1 workflow strip output/source.
    - Test/source-contract that the strip shows a non-tab mode label exactly `Roto paint` or `Play paint`.
    - Test/source-contract that no in-window mode-change callback can switch from selected Roto to Play or Play to Roto.
    - Test that launch start frame is used as the first Roto/Play timeline frame; frame 11 before a max count of 1 shows only frame 11 in Play cells.
  </behavior>
  <action>Following `36.1-PATTERNS.md` PhysicsPaintWorkflowStrip local preview and PhysicsPaintStudio launch-context analogs, refactor `PhysicsPaintWorkflowStrip` so selected mode is displayed as a non-tab label `Roto paint` or `Play paint` and the internal Roto/Play tab switch is removed for this phase. Keep conversion helper code only if still used by explicit non-tab destructive flows; otherwise remove conversion tab-trigger UI from the strip. In `PhysicsPaintStudio`, treat `launchContext.workflowMode` from Plan 07 as the fixed opened mode and do not expose `requestWorkflowModeChange` from the strip as a user switch. Ensure the Roto timeline uses `launchContext.startFrame` as the visible origin for gap opens instead of centering around frame 0, and the Play timeline uses `startFrame` plus clamped `frameCount` so a frame-11 one-frame gap renders a single cell for 11. Do not run the server.</action>
  <acceptance_criteria>
    - Source assertion: `PhysicsPaintWorkflowStrip.tsx` no longer renders `role="tablist"` or the old labels `Roto canvas` / `Play canvas` as switch buttons.
    - Source assertion: visible active mode copy is exactly `Roto paint` or `Play paint`.
    - Behavior assertion: opening at frame 11 with `maxPlayFrameCount: 1` produces Play frame cells `[11]` only.
    - Behavior assertion: local Play scrub remains preview-only and does not call `sendPhysicPaintFrameSyncMessage`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>Standalone EFX Physics opens into a selected mode with no confusing internal mode tabs and correct timeline origin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename render/play controls to text Render actions</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintTopBar.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/PhysicsPaintTopBar.tsx
    - app/src/components/physic-paint/physicsPaintStudio.css
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Test/source-contract that render-generation buttons are labeled exactly `Render` in both Roto and Play contexts where rendering is required.
    - Test/source-contract that no icon-only Play render button remains with `aria-label="Play cached preview"` or `aria-label="Render and save Play"`.
    - Test/source-contract that `Save play` remains separate from `Render` and remains the publishing boundary.
    - Test/source-contract that tooltip/body copy contains `Render cached frames for this script. Watch full playback in EFX Motion.`.
  </behavior>
  <action>Following `36.1-PATTERNS.md` Save Play publishing boundary and local Play scrub analogs, replace the Play icon-only primary button in `PhysicsPaintWorkflowStrip.tsx` with a text button labeled `Render`. In Play mode, `Render` must generate/refresh cached frames for the current script and keep `Save play` as the explicit publish action; do not make Render full playback. In Roto context, expose a text `Render` action where the current UI previously used save/render ambiguity, while preserving the existing explicit save action label only where it actually publishes to the editor. Use UI-SPEC copy `Render cached frames for this script. Watch full playback in EFX Motion.` as the title/help text. Remove unused `Play` icon import if it only supported the old icon-only render button. Adjust CSS so `Render` uses accent color, readable 13px/11px text, 2px accent focus outline, and no new arbitrary colors beyond UI-SPEC.</action>
  <acceptance_criteria>
    - Source assertion: `PhysicsPaintWorkflowStrip.tsx` contains text `Render` for render-generation action.
    - Source assertion: `PhysicsPaintWorkflowStrip.tsx` does not import/use lucide `Play` for the render action.
    - Source assertion: `Save play` still exists and is not renamed to Render.
    - Source assertion: full playback copy points users to EFX Motion from cached frames, not an in-window Play icon.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Render controls are clear text-labeled actions, the old icon-only Play render affordance is gone, and Save play remains explicit publishing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch context -> standalone UI | External launch context selects the only active mode shown in EFX Physics. |
| User render action -> cached frames | Render action creates/refreshes cached visual frames before explicit Save play publishes. |
| Local preview -> editor timeline | Local Play interactions must remain local and not seek editor playhead. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-20 | Tampering | mode switching UI | mitigate | Remove internal tab switch so launch mode cannot silently reinterpret the editor-selected context. |
| T-36.1-21 | Repudiation | Render vs Save play actions | mitigate | Use explicit labels and status copy so user can distinguish cache rendering from publishing. |
| T-36.1-22 | Tampering | local preview frame sync | mitigate | Preserve Plan 04 preview-only path and keep Play scrub away from `sendPhysicPaintFrameSyncMessage`. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned or required. |
</threat_model>

<verification>
Focused component/source-contract Vitest. No server-running. User visual verification remains in Plan 10.
</verification>

<success_criteria>
- EFX Physics no longer shows Roto/Play tabs after opening from the editor.
- The active standalone mode is labeled `Roto paint` or `Play paint`.
- Opening from frame 11 before a frame-12 saved script shows only frame 11 for Play duration.
- Render controls are text-labeled `Render`; full playback is not represented by an in-window Play icon.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-08-SUMMARY.md` when done.
</output>
