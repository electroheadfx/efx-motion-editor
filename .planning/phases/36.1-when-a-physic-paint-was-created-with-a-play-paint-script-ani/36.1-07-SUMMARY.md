---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 07
subsystem: ui
tags: [preact, vitest, physics-paint, bridge, sidebar]

requires:
  - phase: 36.1-06
    provides: Saved Play script range storage and gap duration calculations
provides:
  - Explicit Roto/Play launch-mode request contract
  - Scrubber-aware gap Play creation constrained by saved range boundaries
  - Editor sidebar Roto paint and Play paint launch buttons
affects: [physics-paint-bridge, physics-paint-sidebar, standalone-launch-context]

tech-stack:
  added: []
  patterns:
    - Bridge owns saved range selection and explicit launch mode handling
    - Sidebar sends the live scrubber frame and requested workflow mode
    - Source-contract Vitest coverage for Preact sidebar copy and bridge calls

key-files:
  created:
    - app/src/components/sidebar/PhysicPaintProperties.test.ts
  modified:
    - app/src/types/physicPaint.ts
    - app/src/types/physicPaint.test.ts
    - app/src/lib/physicPaintBridge.ts
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/components/sidebar/PhysicPaintProperties.tsx

key-decisions:
  - "Explicit editor launch intent is carried as requestedWorkflowMode and validated against the existing roto/play union before opening the standalone window."
  - "The bridge remains authoritative for selecting saved Play ranges versus gap creation; the sidebar only passes currentFrame and requested mode."
  - "Gap Play creation starts at the current editor frame and uses UI-SPEC max-duration copy from the bridge launch context."

patterns-established:
  - "Explicit launch request: openPhysicPaintCanvas accepts requestedWorkflowMode and createPhysicPaintLaunchContext returns it in validated launch contexts."
  - "Two-button sidebar launch: Roto paint and Play paint share one handler that reads timelineStore.currentFrame.value at click time."

requirements-completed: [PH36.1-BRIDGE, PH36.1-PLAY-SCRUB]

duration: 8min
completed: 2026-06-14
---

# Phase 36.1 Plan 07: Explicit Roto/Play Editor Launch Summary

**Explicit Roto and Play editor launch buttons now open scrubber-aware Physics Paint contexts with gap-constrained Play durations.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-06-14T10:49:01Z
- **Completed:** 2026-06-14T10:56:43Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Added `requestedWorkflowMode?: 'roto' | 'play'` to the Physics Paint open request and launch-context validation path.
- Updated bridge launch selection so explicit Roto opens at the current editor frame, explicit Play in a gap creates a Play range from the current frame, and explicit Play inside a saved range reopens that saved range at the relative preview frame.
- Replaced the editor Physics Paint sidebar's single unclear open button with text-labeled `Roto paint` and `Play paint` buttons, per UI-SPEC copy.
- Added focused Vitest coverage for launch validation, gap duration limits, saved-range reopening, sidebar labels, status copy, and requested-mode/current-frame bridge calls.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add explicit Roto/Play launch-mode contract to bridge** - `ceae2b9` (feat)
2. **Task 2: Replace editor open surface with Roto paint and Play paint buttons** - `d50f829` (feat)
3. **Task 2 follow-up: remove stale launch label literal from sidebar contract** - `e34ef16` (test)

**Plan metadata:** pending final docs commit

_Note: TDD tasks were implemented as focused behavior/source-contract tests plus feature commits. The follow-up test commit removed the forbidden stale copy from test source after verification exposed the exact literal._

## Files Created/Modified

- `app/src/types/physicPaint.ts` - Adds and validates `requestedWorkflowMode` on launch contexts.
- `app/src/types/physicPaint.test.ts` - Covers accepted and rejected requested launch modes.
- `app/src/lib/physicPaintBridge.ts` - Accepts requested Roto/Play opens, keeps saved-range containment authoritative for Play, and applies UI-SPEC gap duration copy.
- `app/src/lib/physicPaintBridge.test.ts` - Covers frame 11 one-frame gap, frame 6 two-frame gap, explicit Play saved-range reopening, explicit Roto inside saved ranges, and sidebar bridge-call source contracts.
- `app/src/components/sidebar/PhysicPaintProperties.tsx` - Replaces the single open action with `Roto paint` and `Play paint` buttons and requested-mode success/opening copy.
- `app/src/components/sidebar/PhysicPaintProperties.test.ts` - Adds sidebar source-contract coverage for button labels, status strings, invalid context copy, and requested mode/current-frame bridge requests.

## Decisions Made

- Used the existing `PhysicPaintWorkflowMode` union for requested editor launch intent instead of adding a new enum.
- Kept range selection in `physicPaintBridge` rather than moving saved-range lookup into the sidebar.
- Preserved backward behavior for calls that omit `requestedWorkflowMode`: if the scrubber is inside a saved Play range, the bridge still opens that Play range.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed existing workspace dependencies in the manual worktree**
- **Found during:** Task 1 verification
- **Issue:** `pnpm --dir .../app test` failed because `vitest` was unavailable and `node_modules` were missing in the manual worktree.
- **Fix:** Ran `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/.claude/worktrees/manual-36-1-07/app install` using existing lockfile/package metadata. Reverted an install-only `pnpm-lock.yaml` rewrite and did not commit dependency metadata.
- **Files modified:** none committed
- **Verification:** Focused Vitest commands ran successfully afterward.
- **Committed in:** not committed; environment-only setup

**2. [Rule 1 - Bug] Removed forbidden stale launch copy from the new source-contract test**
- **Found during:** Overall stale-label verification after Task 2
- **Issue:** The source-contract test itself contained the exact forbidden `[open fx paint canvas]` string, causing a source search to report the stale copy even though the UI component no longer rendered it.
- **Fix:** Changed the test assertion to construct the forbidden string dynamically so the exact stale literal no longer appears in source.
- **Files modified:** `app/src/components/sidebar/PhysicPaintProperties.test.ts`
- **Verification:** Focused Vitest passed and `grep -R "\[open fx paint canvas\]"` against changed source/test files returned no matches.
- **Committed in:** `e34ef16`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes were required for verification and UI-SPEC stale-copy compliance. No scope creep.

## Issues Encountered

- Initial focused test execution failed because the manual worktree had no installed dependencies. Resolved with `pnpm install` in the worktree app using the existing project package metadata.
- A generated lockfile change from dependency installation was reverted before commits so no package metadata changed.

## User Setup Required

None - no external service configuration required. The dev server was not run.

## Verification

- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/.claude/worktrees/manual-36-1-07/app test --run src/types/physicPaint.test.ts src/lib/physicPaintBridge.test.ts`
- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/.claude/worktrees/manual-36-1-07/app test --run src/lib/physicPaintBridge.test.ts src/components/sidebar/PhysicPaintProperties.test.ts`
- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/.claude/worktrees/manual-36-1-07/app test --run src/types/physicPaint.test.ts src/lib/physicPaintBridge.test.ts src/components/sidebar/PhysicPaintProperties.test.ts`
- `grep -R "\[open fx paint canvas\]" -n app/src/components/sidebar/PhysicPaintProperties.tsx app/src/lib/physicPaintBridge.ts app/src/types/physicPaint.ts app/src/components/sidebar/PhysicPaintProperties.test.ts app/src/lib/physicPaintBridge.test.ts app/src/types/physicPaint.test.ts || true` returned no matches.

## Known Stubs

None found in files created/modified by this plan.

## Threat Flags

None - no new endpoints, auth paths, file access patterns, or trust-boundary schema changes beyond the planned validated launch-mode field.

## Next Phase Readiness

- Editor opening surface now matches UI-SPEC copy and sends explicit launch intent.
- Standalone mode-specific UI cleanup and tab removal can consume `requestedWorkflowMode` from launch contexts in subsequent plans.
- Gap Play creation now exposes current-frame start, max frame count, and exact limit reason for downstream standalone duration controls.

## Self-Check: PASSED

- Found created/modified files:
  - `app/src/types/physicPaint.ts`
  - `app/src/types/physicPaint.test.ts`
  - `app/src/lib/physicPaintBridge.ts`
  - `app/src/lib/physicPaintBridge.test.ts`
  - `app/src/components/sidebar/PhysicPaintProperties.tsx`
  - `app/src/components/sidebar/PhysicPaintProperties.test.ts`
- Found commits: `ceae2b9`, `d50f829`, `e34ef16`

---
*Phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani*
*Completed: 2026-06-14*
