---
phase: 47-internal-multi-track-timeline-filmstrip-capsules-and-control
plan: 04
type: tdd
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts
  - app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.tsx
  - app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: [TML-06, TML-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Hold and Background Loop Clips render as adaptive filmstrip capsules: a source-cycle cell band at the capsule head, a ×N/∞ badge, a requested-duration badge, and the existing Phase 43 rail (purple/cyan, passive markers, white endpoint cuts) preserved untouched around them (TML-06, D-11, Phase 43 locked semantics)"
    - "The capsule badge always shows the requested duration — `Cycle {N}f × {R} = {T}f`, `Cycle {N}f × {R}`, or `Cycle {N}f × ∞` via cycleLabel — never the effective duration (TML-06, Pitfall m2/UI-SPEC badge)"
    - "When a next clip truncates the loop (range.truncated true), the capsule switches to a distinct shortened visual and the label 'Loop shortened by next clip'; the full reason line 'next clip — interrupts the loop' lives in the tooltip (TML-06, D-12/D-14, English copy per the user correction)"
    - "A partial-cycle truncation renders a diagonal cut across the repetition band; a full-cycle truncation (N cycles complete, next clip starts exactly at the end) renders no diagonal cut (TML-06, UI-SPEC diagonal partial-cycle cut)"
    - "Below a zoom threshold, repeated occurrences render as a compact perforated/hatched band; above it, each repeated occurrence expands into lighter linked cells with the same source-frame mapping and a lighter tone (TML-06, D-13)"
    - "The ×N/∞ badge and requested/effective facts derive from the single resolver path (derivePhysicPaintRotoLoopRanges in physicsPaintRotoPhysicalResolver.ts) — the capsule never computes its own loop math (TML-06, Pitfall m2)"
    - "The Background row displays Background clips through the same capsule when present; with no clips it shows the transparent checkerboard or solid fallback swatch (TML-07, D-11, CONTEXT Background-row scope)"
    - "The full capsule detail (repeat instance, source-frame index, source asset, provenance) is in the tooltip (TML-06, D-12)"
    - "The capsule never ships French copy — 'Loop shortened by next clip' and 'next clip — interrupts the loop' are the shipped English labels (D-14)"
  assumptions:
    - "flagged assumption (edge TML-06 unclassified, status unresolved): the exact high-zoom expansion threshold is derived from cell width per D-13 ('derived from cell width so the expansion is gradual and predictable') — the precise pixel formula is authored here and may be tuned in UAT"
  artifacts:
    - app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.tsx
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (shortened state added)
  key_links:
    - "physicsPaintFilmstripCapsule ↔ projectPhysicsPaintLoopClipPresentation (presentation source: cycleLabel, effectiveLabel, statusLabel, tooltipLines) and the range's truncated/partialCycle facts — the capsule renders presentation, never raw resolver data"
    - "physicsPaintFilmstripCapsule ↔ PhysicsPaintLoopClipRail — the capsule wraps/evolves the rail (D-11) so the Phase 43 selection/drag/spacing/playback semantics stay intact"
    - "The Bg row (47-02) mounts the capsule for Background clips — clips render when present, fallback display otherwise (TML-07, D-11)"
  prohibitions:
    - statement: "Never ship the spec's French loop copy (clip suivant / clip bloquant / Boucle raccourcie) — shipped labels are 'Loop shortened by next clip' and 'next clip — interrupts the loop' (D-14)"
      status: active
      verification: manual-review
    - statement: "Never replace or break the Phase 43 rail semantics — the capsule is additive around the rail; selection, drag, spacing, playback, purple/cyan rails, passive markers, and white endpoint cuts stay locked (D-11)"
      status: active
      verification: manual-review
    - statement: "Never compute loop math inside the capsule — all requested/effective/partial-cycle facts come from the single resolver path (Pitfall m2)"
      status: active
      verification: contract-test
    - statement: "Never let the capsule badge show the effective duration as the requested duration — the badge always shows requested via cycleLabel; shortened is a distinct visual + label, not a badge value swap (Pitfall m2)"
      status: active
      verification: contract-test
---

<objective>
Build the adaptive filmstrip capsule for Hold and Background Loop Clips: the presentation layer gains the shortened-state facts, and a new capsule component renders the source-cycle cells, ×N/∞ and requested-duration badges, the diagonal partial-cycle cut, and the high-zoom expansion — all wrapped around the existing Phase 43 rail without breaking its locked semantics. The capsule mounts in the multi-row timeline (Paint rows from 47-01, Bg row from 47-02).

Purpose: TML-06 requires Hold and Background Loop Clips to show as adaptive filmstrip capsules with source cycle, linked repetition band, ×N/∞, requested/effective duration, and partial-cycle interruption. The presentation already carries cycleLabel/effectiveLabel/tooltipLines; this plan adds the truncated/shortened facts and the capsule rendering. TML-07 requires the Bg row to display clips through the same capsule when present.

Output: `physicsPaintLoopClipPresentation.ts` extended with the shortened state and 'Loop shortened by next clip' label; `physicsPaintFilmstripCapsule.tsx` (new capsule: source-cycle cells, ×N/∞ badge, diagonal partial-cycle cut, high-zoom expansion); the rail evolved to host the capsule head; the strip mounts the capsule in rows and the Bg row displays clips through it.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-CONTEXT.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-RESEARCH.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-PATTERNS.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-UI-SPEC.md
@app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts
@app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Presentation extension — shortened state, 'Loop shortened by next clip' label, interruption tooltip line</name>
  <files>app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts, app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts:57-130 (projectPhysicsPaintLoopClipPresentation — the cycleLabel/effectiveLabel construction to extend with the shortened facts)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts:5338-5360 (PhysicPaintRotoLoopRange — the truncated and partialCycle fields the shortened state reads)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts (existing test conventions for the presentation)
    - 47-UI-SPEC.md Filmstrip Capsule section (shortened visual + label, requested badge, interruption tooltip) and Copywriting Contract
  </read_first>
  <behavior>
    - RED Test 1 (shortened finite, TML-06/Pitfall m2): a finite loop whose range.truncated is true projects a presentation with shortened true, a shortenedLabel 'Loop shortened by next clip', cycleLabel still `Cycle {N}f × {R} = {T}f` (requested, never effective), and a tooltip line 'next clip — interrupts the loop'.
    - RED Test 2 (shortened infinity, TML-06): an infinite loop truncated by a next clip projects shortened true with cycleLabel still `Cycle {N}f × ∞`; effectiveLabel shows the effective frames.
    - RED Test 3 (not shortened, TML-06): a loop whose effectiveEnd equals the requested/natural end projects shortened false with no shortenedLabel and no interruption tooltip line.
    - RED Test 4 (partial-cycle fact, TML-06): a mid-cycle truncation projects partialCycle true and a repeatInstance count of the completed cycles; a full-cycle truncation projects partialCycle false.
  </behavior>
  <action>
    Extend PhysicsPaintLoopClipPresentation with `shortened: boolean`, `partialCycle: boolean`, `shortenedLabel: string | null`, `repeatInstanceCount: number`, and `interruptionTooltipLine: string | null`. In projectPhysicsPaintLoopClipPresentation derive them from the range: shortened = Boolean(range.truncated) (the resolver already sets truncated exactly when the effective end falls short of the natural/requested bound, physicsPaintRotoPhysicalResolver.ts:5331-5333); partialCycle + repeatInstanceCount from the partial-cycle facts the resolver exposes. When shortened, set shortenedLabel to 'Loop shortened by next clip' (English, D-14) and interruptionTooltipLine to 'next clip — interrupts the loop'; append the interruption line to tooltipLines. Never change cycleLabel — the badge always shows requested (Pitfall m2). All 4 tests RED then GREEN. Commit test(47-04) then feat(47-04): capsule presentation shortened state.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintLoopClipPresentation.test.ts exits 0 with the 4 behavior tests passing (existing presentation tests still green)
    - PhysicsPaintLoopClipPresentation includes shortened, partialCycle, shortenedLabel, repeatInstanceCount, interruptionTooltipLine (typecheck proves it)
    - cycleLabel is byte-unchanged by the shortened state (test 1 asserts the requested label still projects)
    - The interruption tooltip line is exactly 'next clip — interrupts the loop' (test 1 asserts the literal)
  </acceptance_criteria>
  <done>The presentation projects the shortened state, partial-cycle facts, and the English interruption labels, proven by unit tests (TML-06 facts).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filmstrip capsule component — source-cycle cells, ×N/∞ badge, diagonal partial-cycle cut, high-zoom expansion</name>
  <files>app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.tsx, app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts:57-130 (the presentation the capsule renders) and 224-280 (projectPhysicsPaintLoopClipGeometry — the capsule's left/width placement)
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx:29-60 and 359-449 (the rail the capsule evolves — locked Phase 43 semantics, purple/cyan rails, passive markers, white endpoint cuts)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:1197 (getRotoCellDerivation — the per-cell resolution the source-cycle cells reuse) and 355-414 (structuralIndex frameCells — the 18px pitch the capsule cells align to)
    - 47-UI-SPEC.md Filmstrip Capsule section (source cycle cells, linked repetition band, ×N/∞ badge, high-zoom expansion, diagonal partial-cycle cut)
  </read_first>
  <behavior>
    - RED Test 1 (source-cycle cells, TML-06): a capsule with a 5-frame source cycle renders 5 source-cycle cells at the head; each cell carries the source-frame index.
    - RED Test 2 (×N/∞ badge, TML-06): a finite loop renders a '×{N}' badge and an infinite loop renders a '×∞' badge; the badge text derives from cycleLabel (requested), never from effective.
    - RED Test 3 (diagonal partial-cycle cut, TML-06): a partial-cycle truncation (partialCycle true) renders a diagonal cut across the repetition band; a full-cycle truncation (partialCycle false) renders no diagonal cut.
    - RED Test 4 (high-zoom expansion, D-13): below the cell-width-derived threshold the repetition band renders as a compact perforated/hatched band; above it each repeated occurrence expands into lighter linked cells with the same source-frame mapping.
    - RED Test 5 (shortened visual, TML-06/D-12): a shortened presentation renders the distinct shortened visual plus the 'Loop shortened by next clip' label; the requested badge stays visible.
  </behavior>
  <action>
    Create physicsPaintFilmstripCapsule.tsx exporting a capsule that takes the presentation (from Task 1), the loop geometry (left/width from projectPhysicsPaintLoopClipGeometry), and a zoom/cellWidth value. Render: (1) the source-cycle cell band at the capsule head — one cell per source frame, each showing the source-frame index; (2) a compact badge line showing the requested cycle label and a ×N/×∞ marker derived from the presentation cycleLabel — never effective duration (Pitfall m2); (3) the repetition band — compact perforated/hatched below the threshold, expanded lighter linked cells above it, with the diagonal cut when partialCycle is true; (4) the shortened state — when presentation.shortened, apply the distinct shortened visual class and the 'Loop shortened by next clip' label (English, D-14), keeping the requested badge visible (D-12: shortened is a distinct visual + label, not a badge swap); (5) the full tooltip from presentation.tooltipLines (D-12). Evolve PhysicsPaintLoopClipRail.tsx (D-11) so the capsule head renders around the existing rail surface without altering selection/drag/spacing/playback semantics. CSS: capsule classes (head cells, badges, perforated band, expanded cells, diagonal cut, shortened state). All 5 tests RED then GREEN. Commit test(47-04) then feat(47-04): filmstrip capsule component.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintFilmstripCapsule.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintFilmstripCapsule.test.ts exits 0 with the 5 behavior tests passing
    - physicsPaintFilmstripCapsule.tsx exports the capsule and consumes the Task 1 presentation fields (typecheck proves it)
    - The ×N/∞ badge derives from cycleLabel (requested) — the tests assert the badge text for both finite and infinite loops
    - The diagonal cut class renders only when partialCycle is true (test 3)
    - No French copy in the capsule (grep for 'clips|bloquant|raccourcie' finds none)
  </acceptance_criteria>
  <done>The filmstrip capsule renders source-cycle cells, ×N/∞ + requested badges, the diagonal partial-cycle cut, and the high-zoom expansion, unit-proven (TML-06).</done>
</task>

<task type="auto">
  <name>Task 3: Mount the capsule in the timeline rows and the Bg row</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:1197 (getRotoCellDerivation — the per-cell resolution per row the capsule mounts alongside) and 355-414 (structuralIndex frameCells — the 18px pitch alignment)
    - app/src/stores/physicPaintStore.ts:1983-2000 (getRotoPhysicalLoopClips — the per-track clip list the strip maps to capsules)
    - app/src/efx-paint/document/efxPaintDocument.ts:62-80 (BackgroundTrack + EfxPaintDocument.background — the Bg-row clip source, when present)
    - 47-PATTERNS.md "Capsule render in row" shared pattern
  </read_first>
  <action>
    In PhysicsPaintWorkflowStrip.tsx, for each row render its Loop Clips through PhysicsPaintLoopClipRail wrapped with the Task 2 capsule head (per-track via physicPaintStore.getRotoPhysicalLoopClips(layerId, trackId)); the capsule's projectPhysicsPaintLoopClipPresentation + projectPhysicsPaintLoopClipGeometry come from the same resolver path the rail already uses — no new loop math (Pitfall m2). In the Bg row (47-02), when document.background clips are present render them through the same capsule; with no clips keep the fallback display (transparent checkerboard or solid fallback swatch) per the CONTEXT Background-row scope (import/repeat/fallback-config UI stays Phase 49). CSS: capsule-in-row alignment to the 18px frame pitch. This task is glue/wiring of already-tested components — run the affected suites to prove no regression.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The strip and viewport suites exit 0 (existing rail/capsule consumers still pass)
    - PhysicsPaintLoopClipRail's exported semantics (selection/drag callbacks, rail colors, markers, endpoint cuts) are unchanged — the capsule head is additive
    - The Bg row renders capsules only when background clips exist; otherwise the fallback display remains (source assertion)
    - No new resolver math in the strip (grep: the strip calls the existing projection functions, never computes loop ranges itself)
  </acceptance_criteria>
  <done>The capsule renders for every row's Loop Clips and for Bg-row clips when present, with no regression to the existing strip suites (TML-06/07).</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.tsx`
- New symbols (physicsPaintLoopClipPresentation.ts): `shortened: boolean`, `partialCycle: boolean`, `shortenedLabel: string | null`, `repeatInstanceCount: number`, `interruptionTooltipLine: string | null` on `PhysicsPaintLoopClipPresentation`; derived in `projectPhysicsPaintLoopClipPresentation` from `range.truncated` and the partial-cycle facts
- New component: `physicsPaintFilmstripCapsule` (source-cycle cells, ×N/∞ + requested badges, diagonal partial-cycle cut, high-zoom expansion, shortened visual)
- Modified: `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx` (capsule head around the untouched rail), `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` (capsule mount per row + Bg-row clip display), `app/src/components/physic-paint/physicsPaintStudio.css`
- Modified tests: `app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts`, new `app/src/components/physic-paint/view/physicsPaintFilmstripCapsule.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver facts → capsule render | the capsule reads truncated/partialCycle facts from the resolver; a stale or mismatched fact must render the conservative non-shortened state, never an invented loop shape |
| capsule → rail | the capsule wraps the Phase 43 rail; a render regression must not alter selection/drag/spacing/playback semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-04-01 | Tampering | capsule badge value | high | mitigate | the badge always derives from cycleLabel (requested) — never effective; the shortened state is a distinct visual + label, never a badge swap (Task 2 Test 2, Pitfall m2) |
| T-47-04-02 | Tampering | partial-cycle cut rendering | medium | mitigate | the diagonal cut class renders only when partialCycle is true — a full-cycle truncation renders no cut (Task 2 Test 3) |
| T-47-04-03 | Denial of Service | high-zoom expansion | medium | mitigate | the repetition-band expansion stays bounded by the repeat count and the cell-width threshold — no unbounded cell generation (Task 2 Test 4, D-13) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/physicsPaintFilmstripCapsule.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts` — new suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green (rail consumers unchanged)
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Hold and Background Loop Clips render as adaptive filmstrip capsules with source-cycle cells, ×N/∞ + requested badges, the diagonal partial-cycle cut, and the high-zoom expansion (TML-06).
- The shortened state shows 'Loop shortened by next clip' with the interruption tooltip, all English, with the requested badge intact (TML-06, D-12/D-14).
- The Bg row displays clips through the same capsule when present and the fallback otherwise (TML-07, D-11).
</success_criteria>

<output>
Create `.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-04-SUMMARY.md` when done
</output>
</plan>



