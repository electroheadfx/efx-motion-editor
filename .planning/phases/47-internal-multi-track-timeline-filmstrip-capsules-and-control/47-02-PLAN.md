---
phase: 47-internal-multi-track-timeline-filmstrip-capsules-and-control
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx
  - app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintDeleteTrackDialog.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
autonomous: true
requirements: [TML-02, TML-03, TML-05, TML-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The timeline shows a fixed-width (~140px, resizable) header column pinned while the frame rows scroll beneath it; every InternalPaintTrack in document.tracks lists as a row with its name, plus the fixed 'Bg' Background row always at the bottom (TML-01/07, D-01/D-06, UI-SPEC header column)"
    - "Track names truncate with an ellipsis and show the full name in a hover tooltip; the frame area never depends on name length (TML-02, D-02)"
    - "The active track row is visually unambiguous via an accent left border + subtle row tint + bold name, distinct from selection orange #F59E0B and rail purple #8B5CF6/cyan #06B6D4; switching tracks or undo auto-activation moves the accent (TML-03, D-04)"
    - "The Background row is labeled 'Bg', uses a muted/darker tone versus Paint rows, shows a lock indicator, and cannot reorder above Paint rows (TML-07, D-06, Pitfall 12)"
    - "Every track row exposes hover duplicate and delete icon buttons in its header; a '+' button at the bottom of the header column adds a track via addTrack (TML-02, D-07)"
    - "Rename is edit-in-place on double-click in the header column; empty/whitespace-only/control-character names are rejected fail-closed and keep the prior name; an accepted rename calls renameTrack and the new name renders immediately (TML-02, D-03, ASVS V5)"
    - "Duplicate deep-copies the track with fresh identities via duplicateTrack and the copy appears as a new row named '<source> Copy' then '<source> Copy 2'; the source row is byte-untouched (TML-02, D-09, Phase 46 D-05)"
    - "Delete is acknowledge-and-delete: a dialog shows the preview (frame count, loop clip count, Hold reference count) via requestDeleteTrack and commits only with explicit acknowledgment via commitDeleteTrack; deleting the last Paint track is refused with 'At least one Paint track is required' and nothing is written (TML-02, D-17, Phase 46 D-14)"
    - "Reorder is a drag of the row header's distinct grab area with a live insertion indicator; releasing commits via reorderTrack writing only the order field; the drag never rewrites track ids and never fires during a content drag (TML-05, D-08/D-18, Pitfall 1)"
    - "The active row auto-scrolls into view when it changes (row click, undo auto-activation, keyboard navigation); a slim vertical scrollbar on the right scrolls the frame rows while the header column stays pinned (TML-03, D-05)"
    - "Exactly one active track exists at all times; the active track survives save/reopen through the existing serialize/hydrate path (TML-03/08, D-10)"
  assumptions:
    - "flagged assumption (edge TML-02 unclassified, status unresolved): the exact delete-confirmation copy is authored here in English (Phase 46 D-14 acknowledge-and-delete) per the user copy correction; wording may be polished in UAT"
    - "flagged assumption (edge TML-05 unclassified, status unresolved): the reorder drag in this plan is header-grab only; the content cross-track drag (real keys/rails) is plan 47-05 — D-18 keeps the two gestures distinct"
  artifacts:
    - app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx
    - app/src/components/physic-paint/view/PhysicsPaintDeleteTrackDialog.tsx
  key_links:
    - "physicsPaintTrackHeaderColumn ↔ efxPaintStore ops from 47-01 (addTrack/renameTrack/duplicateTrack/reorderTrack/requestDeleteTrack/commitDeleteTrack/setTrackVisible/setTrackSolo) — the header column is the only CRUD entry surface (D-07)"
    - "Header-column reorder grab (47-02) and content cross-track drag (47-05) are distinct gestures with different grab areas and cursors — a drag that starts in the grab area never mutates frame content and vice versa (D-18, TML-05)"
    - "The 'Bg' row reads document.background and is always the last row — its order is fixed and never written by reorderTrack (D-06, Pitfall 12)"
  prohibitions:
    - statement: "Never ship French copy in the delete/rename/duplicate surfaces — all labels are English per the user copy correction (D-14): 'At least one Paint track is required', 'Delete track?', '<name> Copy'"
      status: active
      verification: manual-review
    - statement: "Never let the reorder drag write array positions or rewrite track ids — reorderTrack writes only the order field on the stable UUID (Pitfall 1)"
      status: active
      verification: contract-test
    - statement: "Never let the Background row participate in reorder — it is fixed at the bottom and its order is never mutated (Pitfall 12)"
      status: active
      verification: contract-test
    - statement: "Never bind track CRUD shortcuts globally — they are guarded by isPhysicsPaintShortcutTarget and the isPaintEditMode() pattern (Pitfall m4, covered by plan 47-03 Task 2)"
      status: active
      verification: contract-test
---

<objective>
Deliver the track-management UI surface of the multi-track timeline: the pinned header column (names, hide/solo toggles, active accent, Bg lock, hover duplicate/delete, '+', distinct reorder grab), the track CRUD interactions (edit-in-place rename, duplicate flow, acknowledge-and-delete dialog, header-drag reorder with a live insertion indicator), and vertical scrolling with ensure-active-row-visible. Every control binds to a store op built in 47-01; none mutates a row other than the one the user targeted.

Purpose: TML-02 (track CRUD) and TML-03 (unambiguous active-track marking) are impossible without a header column that exposes add/rename/duplicate/delete/reorder and a scroll model that keeps the active row visible. TML-07's fixed Background row and TML-05's reorder-without-identity-mutation land here too. This plan is the pure UI layer over the 47-01 store-op surface — no new store mutations, no preview-path changes.

Output: `physicsPaintTrackHeaderColumn.tsx` (new header column), `PhysicsPaintDeleteTrackDialog.tsx` (new acknowledge-and-delete dialog), the strip mounted with the header column + vertical scroll + active accent, and the CSS that realizes the 140px pinned column and 264/141/48 geometry.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-CONTEXT.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-RESEARCH.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-PATTERNS.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-UI-SPEC.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/stores/efxPaintStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pinned header column component — names, hide/solo toggles, active accent, Bg lock, hover actions, '+', reorder grab</name>
  <files>app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx, app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/stores/efxPaintStore.ts:97-115 (setActiveTrackId — the row-click handler) and 152-164 (requestDeleteTrack preview shape)
    - app/src/components/physic-paint/view/physicsPaintSoloArm.ts:1-46 (isSoloArmed/toggleSolo/disarmSolo — the existing solo-arm pattern the per-row solo toggle reuses)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:180-260 (PhysicsPaintWorkflowStripProps — the prop bundle the header column consumes) and 1020-1100 (the existing header/action row that becomes the global toolbar)
    - app/src/efx-paint/document/efxPaintDocument.ts:47-80 (InternalPaintTrack visible/solo/name/order; BackgroundTrack — the Bg row source)
    - 47-UI-SPEC.md Header Column section (140px width, resizable, name truncation, hover actions, '+' bottom, active accent) and Copywriting Contract (English labels, 'Bg' row, 'Add track')
    - 47-PATTERNS.md "Header column + rows" shared pattern (column/row split, pinned column) and the strip cells render analog
  </read_first>
  <behavior>
    - RED Test 1 (rows render, TML-01/07): given tracks [Paint A, Paint B] + background, the column renders 2 Paint rows and exactly 1 'Bg' row at the bottom; each row carries its data-track-id and name.
    - RED Test 2 (active accent, TML-03): the row whose trackId equals activeTrackId gets the active class (accent left border + tint); switching activeTrackId re-renders the accent to the new row.
    - RED Test 3 (hide/solo toggles, TML-04 surface): clicking a row's hide toggle calls onToggleVisible(trackId); clicking the solo toggle calls onToggleSolo(trackId); the solo arm state reflects isSoloArmed.
    - RED Test 4 (name truncation + tooltip, D-02): a long name renders with an ellipsis class and a title attribute carrying the full name.
    - RED Test 5 (Bg lock + muted tone, D-06): the 'Bg' row renders a lock indicator, a muted-tone class, and never exposes duplicate/delete/hover actions.
    - RED Test 6 ('+' + hover actions, D-07): the '+' button calls onAddTrack; hovering a Paint row reveals duplicate and delete icon buttons that call onDuplicateTrack / onRequestDeleteTrack with that row's trackId.
  </behavior>
  <action>
    Create physicsPaintTrackHeaderColumn.tsx exporting a component that receives `{ tracks: readonly InternalPaintTrack[], activeTrackId: string, background: BackgroundTrack, onSelectTrack(trackId), onToggleVisible(trackId), onToggleSolo(trackId), onAddTrack(), onDuplicateTrack(trackId), onRequestDeleteTrack(trackId) }`. Render one row per track plus a fixed 'Bg' row reading document.background (D-06). Each Paint row: name cell with ellipsis truncation (title tooltip = full name, D-02), hide (eye) and solo (S) toggles reusing the physicsPaintSoloArm pattern, the active accent class when trackId === activeTrackId (D-04), hover duplicate/delete icon buttons (D-07), and a distinct reorder grab area with a drag-handle cursor (D-08, never wired to content drag). The 'Bg' row: label 'Bg', muted tone, lock indicator, no hover actions, no grab. The '+' button sits at the bottom of the column (D-07). Row click calls onSelectTrack(trackId) which the strip wires to setActiveTrackId (47-01). CSS in physicsPaintStudio.css: header column fixed ~140px with user-resize handle (pointerdown capture + width update), pinned within the vertical scroll, active accent (left border + tint + bold name) distinct from selection orange and rail purple. All 6 tests RED then GREEN. Commit test(47-02) then feat(47-02): header column component.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintTrackHeaderColumn.test.ts exits 0 with the 6 behavior tests present and passing
    - physicsPaintTrackHeaderColumn.tsx exports the component with the full prop bundle (typecheck proves it)
    - The 'Bg' row renders the lock indicator and muted class; no duplicate/delete/hover action exposes for it (test 5)
    - The reorder grab area carries a distinct drag-handle cursor class and is the ONLY element wired to header drag (grep: no other pointerdown handler in the column targets frame content)
    - No label in the component is French (grep for 'copie|supprimer|renommer|clips|bloquant' finds none)
  </acceptance_criteria>
  <done>The pinned header column renders every track plus the locked Bg row, marks the active track, exposes hide/solo/hover/+/reorder-grab controls, and is unit-proven (TML-02/03/07 surface).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Track CRUD interactions — edit-in-place rename, duplicate flow, acknowledge-and-delete dialog, header-drag reorder with insertion indicator</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintDeleteTrackDialog.tsx, app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/stores/efxPaintStore.ts:118-164 (TrackDeletePreview interface + requestDeleteTrack — the dialog's data source) and 182-231 (commitDeleteTrack — acknowledge-and-delete, last-track refusal, active re-pointing)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:1263 (publishStatus — the status-capsule publisher pattern the dialog and reorder reuse for rejections)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:2408-2440 and 2620-2640 (setPointerCapture drag session pattern — the header-drag reorder reuses it)
    - 47-UI-SPEC.md Track CRUD section (edit-in-place rename, hover duplicate/delete, header-drag reorder, insertion indicator) and Copywriting Contract
    - 47-PATTERNS.md "Store mutation + notify" and "Drag session + rejection publication" shared patterns
  </read_first>
  <behavior>
    - RED Test 1 (rename validation, TML-02/ASVS V5): a double-click opens the edit field; committing an empty, whitespace-only, over-length, or control-character name is rejected and keeps the prior name with no store call; a valid name calls renameTrack(layerId, trackId, name) exactly once.
    - RED Test 2 (duplicate flow, TML-02/D-09): clicking the row's duplicate button calls onDuplicateTrack(trackId) once; the flow surfaces the store's { ok: false } result via the status capsule without mutating anything.
    - RED Test 3 (delete dialog preview, D-17): PhysicsPaintDeleteTrackDialog renders the requestDeleteTrack preview fields — frame count, loop clip count, Hold reference count, and the track name; the Confirm button calls commitDeleteTrack(layerId, trackId, true) once.
    - RED Test 4 (delete last-track refusal, D-17): with isLastTrack true the dialog disables Confirm and shows 'At least one Paint track is required'; commitDeleteTrack is never called; the document is byte-unchanged.
    - RED Test 5 (delete cancellation, Phase 46 D-14): closing the dialog without confirmation calls commitDeleteTrack with acknowledged false or no call at all; the track survives.
    - RED Test 6 (reorder insertion indicator, TML-05/D-08): dragging the grab area computes a target insertion index from the pointer Y over the row list; the insertion indicator renders at that index and release calls reorderTrack(layerId, trackId, newOrder) exactly once.
  </behavior>
  <action>
    In PhysicsPaintWorkflowStrip.tsx wire the header column (Task 1) to the 47-01 store ops: onSelectTrack → setActiveTrackId(layerId, trackId); onToggleVisible → setTrackVisible(layerId, trackId, !track.visible); onToggleSolo → setTrackSolo(layerId, trackId, !track.solo); onAddTrack → addTrack(layerId) then publishStatus on the result; onDuplicateTrack → duplicateTrack(layerId, trackId) then publishStatus (ok or the rejection error); onRequestDeleteTrack → open the delete dialog with requestDeleteTrack(layerId, trackId). Create PhysicsPaintDeleteTrackDialog.tsx rendering the preview (frameCount, loopClipCount, holdReferenceCount — English labels 'frames', 'loop clip(s)', 'Hold reference(s)'), a Cancel button (close, no commit) and a Confirm button that calls commitDeleteTrack(layerId, trackId, true) and publishes the result; when the preview's isLastTrack is true, disable Confirm and show 'At least one Paint track is required' (D-17). Implement rename edit-in-place: double-click swaps the name cell for an input; Enter commits through the 47-01 renameTrack validation (empty/whitespace/control-char rejected fail-closed, name kept); Escape cancels. Implement header-drag reorder: pointerdown on the grab area (distinct cursor), setPointerCapture, pointermove computes the insertion index from the pointer Y against row offsets and renders a live insertion indicator, pointerup calls reorderTrack(layerId, trackId, newOrder); the drag never attaches to a non-grab area and never fires during a content drag (D-18). CSS: dialog surface, edit input, insertion indicator line, grab cursor. All 6 tests RED then GREEN. Commit test(47-02) then feat(47-02): track CRUD interactions.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintTrackHeaderColumn.test.ts and PhysicsPaintWorkflowStrip.test.ts exit 0 with the 6 behavior tests passing (Task 1 tests still green)
    - PhysicsPaintDeleteTrackDialog.tsx exports the dialog and is opened only through onRequestDeleteTrack → requestDeleteTrack (grep: no other delete entry in the strip)
    - The delete Confirm button is disabled when isLastTrack is true and commitDeleteTrack is never called in that state (test 4)
    - The reorder drag writes only the order field — the tests assert reorderTrack is called with the same trackId and a new numeric order, never rewriting ids
    - All dialog and status copy is English (grep for 'copie|supprimer|confirmer|annuler' finds none)
  </acceptance_criteria>
  <done>Rename, duplicate, acknowledge-and-delete, and header-drag reorder all work against the 47-01 store ops, reject fail-closed through the status capsule, and are unit-proven (TML-02/05 surface).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vertical scroll with pinned header column and ensure-active-row-visible</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:3390-3427 (the existing timeline scrollbar — the horizontal scroller the vertical one parallels) and 1020-1100 (the strip body where the rows region + vertical scroll container live)
    - app/src/components/physic-paint/physicsPaintStudio.css:412 (the studio grid rows minmax(58px, auto) minmax(0, 1fr) 161px — the strip slot grows to 264px) and 1964-1972 (.physics-paint-workflow-strip height 161px — the value to change)
    - 47-PATTERNS.md "Header column + rows" and "Scroll + active-row visibility" shared patterns
  </read_first>
  <behavior>
    - RED Test 1 (ensure-active-row-visible, TML-03/D-05): a pure helper computeEnsureRowScrollDelta(laneTop, laneBottom, viewportTop, viewportBottom) returns a positive delta when the active row is below the viewport, a negative delta when above, and 0 when fully visible.
    - RED Test 2 (edge clamping, D-05): when the row is taller than the viewport, the delta clamps so the row's top aligns with the viewport top (never scrolls beyond the scroll extent).
    - RED Test 3 (scrollbar presence, TML-01): with N rows whose total height exceeds the 141px rows region, a vertical scrollbar renders and the rows region scrolls; with fewer rows, no vertical scrollbar renders.
    - RED Test 4 (pinned header column, D-01): scrolling the rows region leaves the header column fixed — the column's scrollTop stays 0 while the rows region scrollTop advances.
  </behavior>
  <action>
    In physicsPaintStudio.css grow the studio grid strip slot and .physics-paint-workflow-strip from 161px to the UI-SPEC 264px total (rows region 141px, row height 48px). In PhysicsPaintWorkflowStrip.tsx wrap the row region (Task 1/47-01 rows) in a vertical scroll container with a slim right scrollbar; keep the header column absolutely pinned so it never scrolls vertically (D-01/D-05). Extract and export the pure helper computeEnsureRowScrollDelta(laneTop, laneBottom, viewportTop, viewportBottom): number used both by the test and by an effect that runs when activeTrackId changes (row click, undo auto-activation, keyboard navigation) to scroll the active row into view; clamp per test 2 and never scroll beyond the scroll extent. The vertical scrollbar follows the existing horizontal scrollbar pattern (pointerdown → setPointerCapture → pointermove sets scrollTop → pointerup releases). All 4 tests RED then GREEN. Commit test(47-02) then feat(47-02): vertical scroll and ensure-active-row-visible.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The 4 behavior tests pass and computeEnsureRowScrollDelta is exported from the strip module (source assertion)
    - physicsPaintStudio.css: the strip slot and .physics-paint-workflow-strip target 264px total / 141px rows region / 48px rows
    - The header column element keeps scrollTop 0 while the rows region scrolls (test 4)
    - No new watch-mode flags and no one-off test config (per the project test rule)
  </acceptance_criteria>
  <done>The timeline scrolls vertically with the header column pinned and the active row always in view when it changes (TML-03, D-05), all committed and green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx`, `app/src/components/physic-paint/view/PhysicsPaintDeleteTrackDialog.tsx`
- New symbols (physicsPaintTrackHeaderColumn.tsx): `physicsPaintTrackHeaderColumn` component with the full prop bundle (tracks, activeTrackId, background, onSelectTrack, onToggleVisible, onToggleSolo, onAddTrack, onDuplicateTrack, onRequestDeleteTrack)
- New symbols (PhysicsPaintWorkflowStrip.tsx): `computeEnsureRowScrollDelta(laneTop, laneBottom, viewportTop, viewportBottom): number` (exported pure helper); the vertical scroll container + pinned header column + slim right scrollbar; the CRUD wiring of the header column to the 47-01 store ops
- New component: `PhysicsPaintDeleteTrackDialog` (acknowledge-and-delete preview surface via requestDeleteTrack + commitDeleteTrack, last-track refusal)
- Modified tests: `app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts`, `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts`
- Modified CSS: strip 161px → 264px total, rows region 141px, pinned 140px header column, active accent, insertion indicator, dialog/input surfaces

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| header column → store ops | every CRUD control carries a trackId that must name a document track; rename text crosses the rename validation boundary fail-closed |
| header drag → reorder | the reorder gesture writes the order field only; a drag starting outside the grab area must never reorder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02-01 | Tampering | rename edit-in-place input | high | mitigate | commit path reuses 47-01 renameTrack validation (trim + length-cap + reject empty/whitespace/control-characters) fail-closed before any store write (Task 2 Test 1; ASVS V5) |
| T-47-02-02 | Tampering | delete dialog confirm | high | mitigate | confirm only enabled when requestDeleteTrack preview exists and isLastTrack is false; commitDeleteTrack refuses without acknowledged true (Task 2 Tests 3-4; Phase 46 D-17) |
| T-47-02-03 | Tampering | header-drag reorder | medium | mitigate | reorder fires only from the distinct grab area with the drag-handle cursor; reorderTrack writes only the order field and never the id (Task 2 Test 6, Pitfall 1) |
| T-47-02-04 | Tampering | Background row | medium | mitigate | the 'Bg' row exposes no hover/duplicate/delete/grab controls and is never a reorder destination — its order is fixed (Task 1 Test 5, Pitfall 12) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` — new suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green (existing strip consumers still compile)
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- The pinned header column exposes every track plus the locked Bg row with the active accent, and all CRUD controls work against the 47-01 store ops (TML-02/03/07).
- Rename, duplicate, acknowledge-and-delete, and header-drag reorder are fail-closed and English-copy (TML-02/05).
- The timeline scrolls vertically with the header column pinned and the active row auto-scrolled into view (TML-03).
</success_criteria>

<output>
Create `.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-02-SUMMARY.md` when done
</output>
</plan>

