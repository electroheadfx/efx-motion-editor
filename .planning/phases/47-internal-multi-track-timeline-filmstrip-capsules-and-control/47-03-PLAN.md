---
phase: 47-internal-multi-track-timeline-filmstrip-capsules-and-control
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements: [TML-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The right panel gains a Track section for the active Paint track: its name, an opacity slider 0-1 that calls setTrackOpacity(layerId, activeTrackId, value), and a blend-mode select that calls setTrackBlend(layerId, activeTrackId, mode) (TML-04, UI-SPEC track controls)"
    - "The opacity slider and blend select read the active track's current opacity/blendMode from efxPaintStore.getDocument and re-render when the active track or its values change (TML-04, D-05 routing)"
    - "The blend select offers exactly the five BlendMode values 'normal' | 'screen' | 'multiply' | 'overlay' | 'add' and the active selection reflects the track's stored value (TML-04, efxPaintDocument.ts:17)"
    - "Track CRUD keyboard shortcuts are guarded by isPhysicsPaintShortcutTarget and skipped when mutationLocked or a text input has focus; they never fire while painting (Pitfall m4, the isPaintEditMode() guard pattern)"
    - "The guarded keyboard shortcuts add a track (Cmd/Ctrl+Shift+N) and duplicate the active track (Cmd/Ctrl+Shift+D); track deletion stays dialog-only to avoid conflicting with the roto Delete/Backspace classifier (D-17, CONTEXT discretion)"
    - "Every keyboard shortcut failure surfaces in the status capsule (fail-closed result from the store op, red warning triangle for rejections) exactly like the pointer-path CRUD (TML-02, Phase 46 paste UX)"
    - "Nothing in the right-panel Track section or the keyboard path ships French copy (user copy correction, D-14)"
  assumptions:
    - "flagged assumption (edge TML-04 unclassified, status unresolved): the Track section opacity slider controls the internal track opacity (stored per track); the Phase 48 compositor applies it — Phase 47 only stores and reflects it in the UI"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (Track section added)
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts (track CRUD actions added)
  key_links:
    - "PhysicsPaintRightPanel Track section ↔ setTrackOpacity/setTrackBlend (47-01) — the panel reads the active track via efxPaintStore.getDocument(layerId) and writes through the fail-closed setters"
    - "physicsPaintStudioKeyboard dispatch ↔ the store ops (47-01) via new actions — every new shortcut routes through isPhysicsPaintShortcutTarget and mutationLocked before any store call (Pitfall m4)"
  prohibitions:
    - statement: "Never add an unguarded global key binding for track CRUD — every shortcut checks isPhysicsPaintShortcutTarget and the isPaintEditMode() pattern before dispatching (Pitfall m4)"
      status: active
      verification: contract-test
    - statement: "Never ship French labels in the Track section or status messages — 'Opacity', 'Blend', 'Add track', 'Duplicate track' are English (D-14)"
      status: active
      verification: manual-review
    - statement: "Never bind Delete/Backspace to track deletion — roto Delete already owns that key through the selection-scope classifier; track delete stays acknowledge-and-delete dialog-only (D-17)"
      status: active
      verification: contract-test
    - statement: "Never apply opacity/blend math in Phase 47 — the panel stores values through the setters; application is Phase 48 (Pitfall M8)"
      status: active
      verification: manual-review
---

<objective>
Add the hide/solo/opacity/blend control surface that reflects track state (TML-04): a Track section in the right panel for the active Paint track (opacity slider + blend-mode select bound to the 47-01 setters) and guarded keyboard shortcuts for track CRUD that never conflict with painting or roto keys. The panel is the only opacity/blend entry surface; the keyboard path is the only non-pointer CRUD entry surface — both fail closed through the status capsule.

Purpose: TML-04 requires that the user can set internal track opacity and blend mode; the 47-01 store ops exist but nothing binds them. This plan binds them in the right panel and adds the guarded keyboard CRUD path the CONTEXT discretion calls for, without any Phase 48 compositor math.

Output: `PhysicsPaintRightPanel.tsx` with the Track section (opacity slider via the existing PanelSlider, blend select over the five BlendMode values), `physicsPaintStudioKeyboard.ts` with addTrack/duplicateTrack actions guarded by the shortcut-target pattern, and the `PhysicsPaintStudio.tsx` wiring that feeds both from the active document.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-CONTEXT.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-RESEARCH.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-PATTERNS.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-UI-SPEC.md
@app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
@app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Right-panel Track section — opacity slider and blend-mode select bound to the 47-01 setters</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx:23-46 (PhysicsPaintRightPanelProps — the prop bundle to extend) and 99-155 (PanelSlider — the existing slider primitive the opacity control reuses) and 155-200 (the panel body structure where the Track section slots in)
    - app/src/stores/efxPaintStore.ts:53-108 (the setter shape setTrackOpacity/setTrackBlend follow) — read the 47-01 additions in the working tree
    - app/src/efx-paint/document/efxPaintDocument.ts:17 (BlendMode union) and 47-59 (opacity/blendMode fields on InternalPaintTrack)
    - 47-UI-SPEC.md Track Controls section (opacity slider, blend select) and Copywriting Contract
    - 47-PATTERNS.md "Panel section" and "Store mutation + notify" shared patterns
  </read_first>
  <behavior>
    - RED Test 1 (track section render, TML-04): given a document with an active track named 'Paint 1', opacity 0.5, blendMode 'multiply', the panel renders a Track section showing the name, an opacity slider at 0.5, and a blend select whose value is 'multiply'.
    - RED Test 2 (opacity commit, TML-04/A5): dragging the slider to 0.8 calls onTrackOpacityChange(0.8) once; the slider clamps its display to 0..1.
    - RED Test 3 (blend commit, TML-04): selecting 'screen' calls onTrackBlendChange('screen') once; every BlendMode union member is offered as an option and no other option exists.
    - RED Test 4 (active-track switch, D-05): when the active track changes to a track with opacity 1 and blendMode 'normal', the section re-renders to the new values.
  </behavior>
  <action>
    Extend PhysicsPaintRightPanelProps with `trackName: string`, `trackOpacity: number`, `trackBlendMode: BlendMode`, `onTrackOpacityChange(opacity: number)`, `onTrackBlendChange(mode: BlendMode)`. Add a Track section to PhysicsPaintRightPanel (English labels 'Track', 'Opacity', 'Blend'): the active track name as a section title, an opacity slider reusing PanelSlider with 0..1 range that calls onTrackOpacityChange on commit, and a select element with exactly the five BlendMode options ('normal', 'screen', 'multiply', 'overlay', 'add') whose value is trackBlendMode and onChange calls onTrackBlendChange. Do not apply any opacity/blend math — this section only stores and reflects values (Pitfall M8; application is Phase 48). All 4 tests RED then GREEN. Commit test(47-03) then feat(47-03): right-panel track section.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintRightPanel.test.ts exits 0 with the 4 behavior tests present and passing
    - PhysicsPaintRightPanelProps includes trackName, trackOpacity, trackBlendMode, onTrackOpacityChange, onTrackBlendChange (typecheck proves it)
    - The blend select offers exactly the five BlendMode members (test 3 asserts option count)
    - No opacity/blend compositing math appears in the panel (grep for 'globalCompositeOperation' in PhysicsPaintRightPanel.tsx finds none)
    - Section labels are English (grep for 'Opacit|Mélange|Piste' finds none)
  </acceptance_criteria>
  <done>The right panel exposes the active track's opacity and blend mode through fail-closed setters, reflects changes live, and is unit-proven (TML-04 surface).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guarded track CRUD keyboard shortcuts — add and duplicate the active track</name>
  <files>app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts:4-45 (PhysicsPaintStudioKeyboardState/Actions — the interfaces to extend with addTrack/duplicateTrack) and 90-130 (dispatchPhysicsPaintStudioKeyDown — the meta+key dispatch block where the new shortcuts slot, after the existing redo/undo handlers)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:1204-1270 (the dispatch wiring + publishStatus injection — the new actions bind here)
    - app/src/stores/efxPaintStore.ts (the 47-01 addTrack/duplicateTrack ops the actions call)
    - 47-PATTERNS.md "Store mutation + notify" and "Shortcut guard" shared patterns (isPaintEditMode guard per Pitfall m4)
  </read_first>
  <behavior>
    - RED Test 1 (add shortcut, TML-02/Pitfall m4): Cmd+Shift+N with a non-input target calls actions.addTrack() once and prevents default; with a focused input/textarea target it does NOT fire (isPhysicsPaintShortcutTarget false).
    - RED Test 2 (duplicate shortcut, TML-02/Pitfall m4): Cmd+Shift+D calls actions.duplicateTrack() once and prevents default; while mutationLocked it is skipped without a store call.
    - RED Test 3 (no delete binding, D-17): pressing Delete/Backspace never invokes a track-delete action — the existing roto delete flow owns the key (assert no trackDelete branch in the dispatch).
    - RED Test 4 (meta modifier handling, Pitfall m4): the shortcuts only fire with meta on macOS / ctrl on other platforms via the existing `event.metaKey || event.ctrlKey` convention and never on a bare letter key.
  </behavior>
  <action>
    Extend PhysicsPaintStudioKeyboardActions with optional `addTrack?: () => void;` and `duplicateTrack?: () => void;`. In dispatchPhysicsPaintStudioKeyDown add, inside the existing isPhysicsPaintShortcutTarget guard and after the redo/undo block: Cmd/Ctrl+Shift+N → if (state.mutationLocked) return; preventDefault; actions.addTrack?.(); and Cmd/Ctrl+Shift+D → same guard; actions.duplicateTrack?.(). Never add a Delete/Backspace track binding — roto delete owns those keys through the selection-scope classifier and track deletion stays acknowledge-and-delete dialog-only (D-17). In PhysicsPaintStudio.tsx bind the new actions: addTrack calls efxPaintStore.addTrack(layerId) then publishStatus on the result; duplicateTrack calls efxPaintStore.duplicateTrack(layerId, activeTrackId) then publishStatus (fail-closed rejection surfaces with the red warning triangle, identical to the pointer-path CRUD from 47-02). All 4 tests RED then GREEN. Commit test(47-03) then feat(47-03): guarded track CRUD shortcuts.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintStudioKeyboard.test.ts exits 0 with the 4 behavior tests present and passing
    - PhysicsPaintStudioKeyboardActions includes optional addTrack and duplicateTrack (typecheck proves it)
    - No track-delete key binding exists in the dispatch (test 3 asserts; grep for 'deleteTrack' in physicsPaintStudioKeyboard.ts finds no branch beyond the roto delete path)
    - The shortcuts call preventDefault only when they fire (tests 1-2 assert)
    - PhysicsPaintStudio.tsx wires the actions to addTrack/duplicateTrack + publishStatus (source assertion)
  </acceptance_criteria>
  <done>The guarded track CRUD shortcuts add and duplicate the active track fail-closed, never conflict with painting or roto keys, and are unit-proven (TML-02/04, Pitfall m4).</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New symbols (PhysicsPaintRightPanel.tsx): Track section — `trackName`, `trackOpacity`, `trackBlendMode`, `onTrackOpacityChange`, `onTrackBlendChange` props + opacity slider + five-option blend select
- New symbols (physicsPaintStudioKeyboard.ts): `PhysicsPaintStudioKeyboardActions.addTrack?` / `duplicateTrack?` + the Cmd/Ctrl+Shift+N / Cmd/Ctrl+Shift+D guarded dispatch branches
- Modified tests: `app/src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts`, `app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts`
- Modified wiring: `app/src/components/physic-paint/PhysicsPaintStudio.tsx` (panel props from the active document; keyboard actions bound to the 47-01 ops + publishStatus)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel controls → store setters | opacity/blend values cross the setter validation boundary (union check for blend, 0..1 clamp for opacity) |
| keyboard events → store ops | shortcut dispatches carry no trackId (always the active track) but must be guarded so they never fire mid-paint or in a text input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-03-01 | Tampering | blend select value | high | mitigate | the select offers only the five BlendMode members and onTrackBlendChange → setTrackBlend rejects anything else fail-closed (Task 1 Test 3; ASVS V5) |
| T-47-03-02 | Tampering | opacity slider value | medium | mitigate | the slider display clamps to 0..1 and setTrackOpacity clamps the stored value (Task 1 Test 2; ASVS V5) |
| T-47-03-03 | Tampering | keyboard shortcuts | high | mitigate | every shortcut passes isPhysicsPaintShortcutTarget and is skipped while mutationLocked or painting; Delete/Backspace never binds track deletion (Task 2 Tests 1-3, Pitfall m4) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts` — new suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green (existing panel and keyboard consumers still compile)
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- The right panel lets the user set the active track's opacity and blend mode through the 47-01 setters, reflected live (TML-04).
- Track CRUD keyboard shortcuts add and duplicate the active track, fully guarded against paint/shortcut conflicts (TML-02, Pitfall m4).
- No opacity/blend math and no French copy ship in this plan (Pitfall M8, D-14).
</success_criteria>

<output>
Create `.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-03-SUMMARY.md` when done
</output>
</plan>
