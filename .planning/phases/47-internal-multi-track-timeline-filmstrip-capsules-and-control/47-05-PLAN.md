---
phase: 47-internal-multi-track-timeline-filmstrip-capsules-and-control
plan: 05
type: execute
wave: 4
depends_on: ["47-01", "47-02"]
files_modified:
  - app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: [TML-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every existing draggable (real keys, Key Rails, Loop Clip Rails, rail sets) can cross track rows: a drag that starts on the source row and crosses a row boundary becomes a cross-track move with no modifier key (TML-05, D-15/D-16)"
    - "While a cross-track drag is over another row, the destination row highlights as the drop target and a live insertion preview shows the exact frame position where the dragged content will land in the destination row (TML-05, D-16)"
    - "Releasing over a destination row commits the move with the Phase 46 D-09 semantics via physicPaintStore.moveTrackItems(layerId, fromTrackId, toTrackId, keys): fresh identities in the destination, source items removed, Hold refs re-pointed fail-closed (TML-05, D-17)"
    - "A rejected move (occupied destination frame, partial-loop overlap, missing key, empty set, unknown track) leaves both rows byte-identical and surfaces through the existing status capsule with the red warning triangle (TML-05, D-17)"
    - "The row-reorder drag (header grab area, plan 47-02) and the content cross-track drag are distinct interactions with different grab areas and cursors — a gesture on one never mutates the other (TML-05, D-18)"
    - "Releasing the drag without crossing a row boundary commits nothing — the drag resolves to its normal same-row behavior (TML-05, D-16 plain-drag preservation)"
    - "The destination highlight and insertion preview are read-only feedback during the gesture — they never mutate the destination row until release commits the move (TML-05, D-16)"
  assumptions:
    - "flagged assumption (edge TML-05 unclassified, status unresolved): the exact live insertion preview rendering (frame-position indicator inside the destination row) is authored here per D-16 and may be polished in UAT"
  artifacts:
    - app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.ts
    - app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts
  key_links:
    - "usePhysicsPaintCrossTrackDrag ↔ physicPaintStore.moveTrackItems (physicPaintStore.ts:2711) — the single commit path with D-09 copy-paste-delete semantics and fail-closed rejection reasons"
    - "Cross-track drag destination (this plan) and header reorder grab (47-02) are disjoint gestures — the grab-area drag never fires content moves and the content drag never reorders (D-18)"
    - "Rejection publication reuses the publishStatus injection at PhysicsPaintStudio.tsx:1263 — the same capsule path as the Phase 46 paste rejections"
  prohibitions:
    - statement: "Never mutate the destination row during the gesture — destination highlight and insertion preview are read-only; only release commits via moveTrackItems (D-16)"
      status: active
      verification: contract-test
    - statement: "Never let the cross-track drag fire from the header reorder grab area — the grab drag (47-02) owns reorder and the content drag owns moves; they never overlap (D-18)"
      status: active
      verification: contract-test
    - statement: "Never ship French copy in the cross-track rejection messages — status-capsule text is English (D-14)"
      status: active
      verification: manual-review
    - statement: "Never compute move semantics in the hook — moveTrackItems owns copy-paste-delete; the hook only captures destination + calls the store (D-09, D-17)"
      status: active
      verification: contract-test
---

<objective>
Build the cross-track drag gesture deferred from Phase 46 (D-08/D-09): any existing draggable can cross from its source row into a destination row, with the destination row highlighting and a live insertion preview showing where the content lands, and release commits through physicPaintStore.moveTrackItems. Rejections surface in the existing status capsule with the red warning triangle, and the gesture stays strictly separate from the header reorder drag (D-18).

Purpose: TML-05's acceptance requires that "timeline interactions never mutate another row accidentally" while every draggable can still move content across rows. The data op (moveTrackItems) exists from Phase 46 but has no UI gesture; this plan is the gesture layer that captures the destination and commits fail-closed.

Output: `usePhysicsPaintCrossTrackDrag.ts` (new gesture hook with a pure destination-computation helper), the strip-mounted gesture with destination highlight + insertion preview, and the commit/rejection wiring to moveTrackItems + publishStatus.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-CONTEXT.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-RESEARCH.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-PATTERNS.md
@.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-UI-SPEC.md
@app/src/stores/physicPaintStore.ts
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-track drag gesture — destination trackId capture, destination highlight, live insertion preview</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:2408-2440 and 2620-2640 (the setPointerCapture drag-session pattern the cross-track gesture reuses)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:180-260 (PhysicsPaintWorkflowStripProps — the gesture wiring point) and 355-414 (structuralIndex frameCells + 18px pitch — the frame-position preview math)
    - app/src/stores/physicPaintStore.ts:2711-2757 (moveTrackItems signature + fail-closed reasons the hook's commit path maps)
    - 47-PATTERNS.md "Drag session + rejection publication" and "Row geometry" shared patterns
    - 47-UI-SPEC.md Cross-track Drag section (destination highlight, live insertion preview, plain drag no modifier)
  </read_first>
  <behavior>
    - RED Test 1 (destination capture, TML-05/D-16): a pointer position over the row below the source resolves to that row's trackId via the pure helper computeCrossTrackDestination; a position still on the source row resolves to the source trackId (no crossing).
    - RED Test 2 (insertion frame, TML-05/D-16): the helper resolves the insertion frame from the pointer X against the 18px frame pitch and the horizontal scrollLeft — the preview shows the frame where the dragged content lands.
    - RED Test 3 (destination highlight, TML-05/D-16): the hook exposes a destinationTrackId signal; while it differs from the source, the destination row renders the highlight class and no destination-row mutation occurs (the document is byte-unchanged during the gesture).
    - RED Test 4 (plain-drag preservation, TML-05/D-16): when the pointer never crosses a row boundary, destinationTrackId stays null and the gesture behaves exactly as the same-row drag — no cross-track preview, no store call.
  </behavior>
  <action>
    Create usePhysicsPaintCrossTrackDrag.ts exporting a hook that tracks a drag session (pointerdown with setPointerCapture on the source row lane, pointermove, pointerup) and exposes `{ destinationTrackId, insertionFrame, isCrossing }` signals plus a release callback. Export the pure helper computeCrossTrackDestination(rowBounds: readonly {trackId, top, bottom}[], pointerY, sourceTrackId): string and computeInsertionFrame(pointerX, scrollLeft, zoom, framePitch): number. The destination highlight class renders only while isCrossing and the pointer is over a different row; the insertion preview renders the resolved frame position inside that row. The gesture never mutates any document — all state is read-only signals until release (D-16). Mount the hook in PhysicsPaintWorkflowStrip.tsx so the existing draggables (real keys, Key Rails, Loop Clip Rails, rail sets) start the cross-track session when they cross row boundaries; the header reorder grab area (47-02) stays untouched and never starts this gesture (D-18). CSS: destination-highlight and insertion-preview classes. All 4 tests RED then GREEN. Commit test(47-05) then feat(47-05): cross-track drag gesture.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - usePhysicsPaintCrossTrackDrag.test.ts exits 0 with the 4 behavior tests present and passing
    - computeCrossTrackDestination and computeInsertionFrame are exported from the hook module (typecheck proves it)
    - During the gesture no document mutation occurs — the tests assert the document is byte-unchanged until release (test 3)
    - The header reorder grab area (47-02) never starts the cross-track session (source assertion: the grab handler does not call the cross-track pointerdown)
  </acceptance_criteria>
  <done>The cross-track gesture captures the destination row and insertion frame with read-only highlight + preview, proven by unit tests (TML-05 gesture).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit via moveTrackItems and rejection publication with gesture separation</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:2711-2757 (moveTrackItems — the commit call and its rejection reasons 'track-missing' / 'duplicate-destination-frame' / 'empty-set' / 'missing-key' / 'partial-loop-overlap' / 'apply-failed')
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:1263 (publishStatus — the injection the rejection messages publish through, identical to the Phase 46 paste rejections)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts (the publishStatus harness the cross-track rejection tests reuse)
    - 47-UI-SPEC.md Cross-track Drag section (release commit, red warning triangle rejections)
  </read_first>
  <behavior>
    - RED Test 1 (commit on release, TML-05/D-17): releasing over a destination row calls moveTrackItems(layerId, fromTrackId, toTrackId, keys) exactly once with the source keys and destination trackId; on { ok: true } the source row loses the items and the destination gains fresh-identity copies.
    - RED Test 2 (rejection publication, TML-05/D-17): when moveTrackItems returns { ok: false, reason: 'partial-loop-overlap' }, both rows stay byte-identical and the status capsule receives an English rejection message (red warning triangle); the message text includes the specific reason.
    - RED Test 3 (no-crossing release, TML-05/D-16): releasing without crossing a row boundary calls moveTrackItems zero times — the same-row drag path owns the release.
    - RED Test 4 (grab-area separation, TML-05/D-18): a release that started on the header reorder grab area never reaches moveTrackItems — the reorder path owns it.
  </behavior>
  <action>
    Extend the Task 1 hook with a release handler: when destinationTrackId differs from the source and the pointer is over a destination row, call moveTrackItems(layerId, fromTrackId, destinationTrackId, keys) with the dragged items' keyIds; map the result to the publishStatus message — { ok: true } publishes the successful move summary, { ok: false, reason } publishes the English rejection (the reason map: 'track-missing' → 'Track not found', 'duplicate-destination-frame' → 'Destination frame is occupied', 'partial-loop-overlap' → 'Loop would be partially moved', 'empty-set' → 'Nothing to move', 'missing-key' → 'Key not found') with the red warning triangle styling, identical to the Phase 46 paste rejection UX (D-17). When destinationTrackId is null or the gesture started on the header grab area, call nothing (D-16/D-18). Clear the destination signals on release. CSS: rejection capsule tone reuse (no new pattern). All 4 tests RED then GREEN. Commit test(47-05) then feat(47-05): cross-track drag commit and rejection.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - usePhysicsPaintCrossTrackDrag.test.ts and PhysicsPaintWorkflowStrip.test.ts exit 0 with the 4 behavior tests passing (Task 1 tests still green)
    - moveTrackItems is called exactly once per cross-track release and zero times for no-crossing or grab-area releases (tests 1/3/4)
    - Rejection messages are English and include the specific reason (test 2 asserts the literal)
    - On rejection both rows are byte-identical (test 2 asserts a full store snapshot equality)
  </acceptance_criteria>
  <done>The cross-track drag commits through moveTrackItems with fresh identities, publishes English rejections through the status capsule, and stays strictly separated from the header reorder drag (TML-05).</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

- New files: `app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.ts`, `app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts`
- New symbols (usePhysicsPaintCrossTrackDrag.ts): the cross-track drag hook exposing `{ destinationTrackId, insertionFrame, isCrossing }` read-only signals + release handler; pure helpers `computeCrossTrackDestination(rowBounds, pointerY, sourceTrackId): string` and `computeInsertionFrame(pointerX, scrollLeft, zoom, framePitch): number`
- Modified: `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` (gesture mount over the existing draggables), `app/src/components/physic-paint/physicsPaintStudio.css` (destination-highlight + insertion-preview classes), `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts`
- Modified tests: `app/src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts` (new), `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer gesture → store commit | the release handler passes fromTrackId/toTrackId/keys to moveTrackItems; a stale or fabricated destination must fail closed with no partial mutation |
| store result → status capsule | rejection reasons cross into user-visible English messages; reasons must map deterministically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-05-01 | Tampering | cross-track commit path | high | mitigate | release calls moveTrackItems exactly once with the captured destination and source keys; the store revalidates track/key existence and Hold overlap fail-closed before any write (Task 2 Test 1; D-17) |
| T-47-05-02 | Tampering | gesture separation | high | mitigate | the header reorder grab area never starts or releases the cross-track gesture and vice versa — disjoint grab areas and cursors (Task 2 Test 4, D-18) |
| T-47-05-03 | Tampering | rejection message mapping | medium | mitigate | every moveTrackItems reason maps to a fixed English message; unmapped reasons publish a generic English failure, never an empty or French capsule (Task 2 Test 2, D-14) |
| T-47-05-04 | Tampering | read-only gesture state | medium | mitigate | destination signals are read-only during the gesture — the tests assert the document is byte-unchanged until release (Task 1 Test 3, D-16) |
</threat_model>

<verification>
- `pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/hooks/usePhysicsPaintCrossTrackDrag.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` — new suites green
- `pnpm --filter efx-motion-editor exec vitest run` — full suite green (existing drag machinery consumers still pass)
- `pnpm --dir app run typecheck`
</verification>

<success_criteria>
- Every existing draggable crosses track rows with the destination highlight and live insertion preview (TML-05, D-15/D-16).
- Release commits through moveTrackItems with fresh identities; rejections surface in English through the status capsule with the red warning triangle (TML-05, D-17).
- The cross-track drag and the header reorder drag are strictly separate — no gesture mutates a row it did not target (TML-05, D-18).
</success_criteria>

<output>
Create `.planning/phases/47-internal-multi-track-timeline-filmstrip-capsules-and-control/47-05-SUMMARY.md` when done
</output>
</plan>



