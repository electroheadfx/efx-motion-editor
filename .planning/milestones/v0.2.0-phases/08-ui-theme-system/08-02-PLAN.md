---
phase: 08-ui-theme-system
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Application/src/components/layer/AddFxMenu.tsx
  - Application/src/components/layout/LeftPanel.tsx
  - Application/src/components/layout/PropertiesPanel.tsx
  - Application/src/components/layer/AddLayerMenu.tsx
  - Application/src/components/sequence/SequenceList.tsx
  - Application/src/components/layer/LayerList.tsx
  - Application/src/components/layout/Toolbar.tsx
  - Application/src/components/overlay/ShortcutsOverlay.tsx
  - Application/src/components/sequence/KeyPhotoStrip.tsx
autonomous: true
requirements: [THEME-03]

must_haves:
  truths:
    - "AddFxMenu renders correctly in all three themes with no hardcoded hex colors"
    - "LeftPanel section headers, subsections, dividers adapt to theme"
    - "PropertiesPanel inputs, dividers, text adapt to theme"
    - "AddLayerMenu context menu adapts to theme"
    - "SequenceList selected states, context menu, edit inputs adapt to theme"
    - "LayerList selected backgrounds, alternating rows adapt to theme"
    - "Toolbar background, dividers, button text adapt to theme"
    - "ShortcutsOverlay dialog and keyboard badges adapt to theme"
    - "KeyPhotoStrip thumbnails, menus adapt to theme"
  artifacts:
    - path: "Application/src/components/layer/AddFxMenu.tsx"
      provides: "Theme-aware FX menu"
    - path: "Application/src/components/layout/LeftPanel.tsx"
      provides: "Theme-aware left panel"
    - path: "Application/src/components/layout/PropertiesPanel.tsx"
      provides: "Theme-aware properties panel"
    - path: "Application/src/components/layer/AddLayerMenu.tsx"
      provides: "Theme-aware layer menu"
    - path: "Application/src/components/sequence/SequenceList.tsx"
      provides: "Theme-aware sequence list"
    - path: "Application/src/components/layer/LayerList.tsx"
      provides: "Theme-aware layer list"
    - path: "Application/src/components/layout/Toolbar.tsx"
      provides: "Theme-aware toolbar"
    - path: "Application/src/components/overlay/ShortcutsOverlay.tsx"
      provides: "Theme-aware shortcuts overlay"
    - path: "Application/src/components/sequence/KeyPhotoStrip.tsx"
      provides: "Theme-aware key photo strip"
  key_links:
    - from: "All 9 component files"
      to: "Application/src/index.css"
      via: "var(--color-*) CSS variable references"
      pattern: "var\\(--color-"
---

<objective>
Convert all hardcoded hex colors in the 9 highest-impact component files to CSS variable references. These files contain ~104 of the ~140 total hardcoded colors.

Purpose: After this plan, the major UI panels (left panel, properties, toolbar, menus, overlays) fully adapt to theme changes.
Output: 9 component files with zero hardcoded hex colors (except identity/fixed colors).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-theme-system/08-CONTEXT.md
@.planning/phases/08-ui-theme-system/08-RESEARCH.md
@.planning/phases/08-ui-theme-system/08-01-SUMMARY.md

<interfaces>
<!-- CSS variables defined in Plan 01 that conversions must target -->

From Application/src/index.css (Plan 01 output):
```css
/* Background variables */
--color-bg-root, --color-bg-sidebar, --color-bg-right, --color-bg-card,
--color-bg-card-alt, --color-bg-input, --color-bg-settings,
--color-bg-toolbar, --color-bg-menu, --color-bg-section-header,
--color-bg-subsection, --color-bg-shell, --color-bg-selected,
--color-bg-hover-item, --color-bg-divider

/* Accent */
--color-accent, --color-accent-hover

/* Borders */
--color-separator, --color-border-subtle, --color-border-kbd

/* Text hierarchy */
--color-text-primary, --color-text-secondary, --color-text-muted,
--color-text-dim, --color-text-dimmer, --color-text-white,
--color-text-link, --color-text-button, --color-text-heading

/* Overlays */
--color-hover-overlay, --color-hover-overlay-strong

/* Semantic */
--color-error-bg, --color-error-text, --color-error-text-faded

/* Identity (FIXED - do NOT convert these, they stay as-is) */
--color-thumb-blue, --color-thumb-purple, --color-thumb-green
--color-dot-blue, --color-dot-purple, --color-dot-green, --color-dot-orange
--color-badge-bg, --color-badge-text, --color-icon-placeholder, --color-open-icon
```

Colors that MUST stay hardcoded (NOT converted):
- FX type indicator dots: #EC4899, #14B8A6, #3B82F6, #8B5CF6, #F97316 (FX identity)
- Sequence identity dots: already use var(--color-dot-*) CSS variables
- Traffic light dots: #FF5F57, #FFBD2E, #28CA41 (macOS standard -- in TitleBar, not in this plan)
- Playhead: #E55A2B (functional high-visibility)
- Drop indicator: #4488FF (functional)
- Export button: #F97316 (identity CTA color)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert hardcoded colors in menu and panel components (AddFxMenu, LeftPanel, PropertiesPanel, AddLayerMenu, SequenceList)</name>
  <files>Application/src/components/layer/AddFxMenu.tsx, Application/src/components/layout/LeftPanel.tsx, Application/src/components/layout/PropertiesPanel.tsx, Application/src/components/layer/AddLayerMenu.tsx, Application/src/components/sequence/SequenceList.tsx</files>
  <action>
For each file, read it fully, identify every hardcoded hex color in Tailwind classes (`bg-[#...]`, `text-[#...]`, `border-[#...]`) and inline styles, then replace with the appropriate CSS variable reference.

**Conversion mapping (apply consistently across all files):**

| Hardcoded | CSS Variable | Usage |
|-----------|-------------|-------|
| `bg-[#0F0F0F]` | `bg-[var(--color-bg-root)]` | Root backgrounds |
| `bg-[#111111]` | `bg-[var(--color-bg-section-header)]` | Section headers |
| `bg-[#131313]` | `bg-[var(--color-bg-subsection)]` | Subsection backgrounds |
| `bg-[#151515]` | `bg-[var(--color-bg-shell)]` | Shell background |
| `bg-[#1A1A1A]` | `bg-[var(--color-bg-card)]` | Card backgrounds |
| `bg-[#1C1C1C]` | `bg-[var(--color-bg-toolbar)]` | Toolbar background |
| `bg-[#1E1E1E]` | `bg-[var(--color-bg-menu)]` | Menu/dropdown backgrounds |
| `bg-[#2A2A2A]` | `bg-[var(--color-bg-hover-item)]` or `bg-[var(--color-bg-divider)]` | Hoverable items or dividers (use semantic meaning) |
| `bg-[#2A2A3A]` | `bg-[var(--color-bg-selected)]` | Selected item highlight |
| `bg-[#2A1A1A]` | `bg-[var(--color-error-bg)]` | Error backgrounds |
| `bg-[#ffffff10]` or `hover:bg-[#ffffff10]` | `bg-[var(--color-hover-overlay)]` | Hover overlays |
| `bg-[#ffffff15]` | `bg-[var(--color-hover-overlay-strong)]` | Stronger hover overlays |
| `text-[#CCCCCC]` | `text-[var(--color-text-button)]` | Button/menu item text |
| `text-[#E0E0E0]` | `text-[var(--color-text-heading)]` | Headings, highlighted text |
| `text-[#FF6666]` | `text-[var(--color-error-text)]` | Error text |
| `text-[#FF666680]` | `text-[var(--color-error-text-faded)]` | Faded error text |
| `text-[#888888]` | `text-[var(--color-text-secondary)]` | Secondary text (already a var, but some files hardcode it) |
| `text-[#666666]` | `text-[var(--color-text-muted)]` | Muted text |
| `text-[#555555]` | `text-[var(--color-text-dim)]` | Dim text |
| `text-[#444444]` | `text-[var(--color-text-dimmer)]` | Dimmer text |
| `text-[#AAAAAA]` | `text-[var(--color-text-link)]` | Link text |
| `border-[#333333]` or `border-[#333]` | `border-[var(--color-border-subtle)]` | Thin borders |
| `border-[#444444]` or `border-[#444]` | `border-[var(--color-border-kbd)]` | Keyboard badges, heavier borders |
| `border-[#222222]` or `border-[#222]` | `border-[var(--color-separator)]` | Separators |
| `bg-[#333333]` (dividers, 1px lines) | `bg-[var(--color-border-subtle)]` | Divider lines using bg instead of border |

**Special cases:**
- Colors close but not exact (e.g., `#161616`): Map to nearest semantic variable (`--color-bg-card-alt`)
- Opacity variations (e.g., `#ffffff10`): Map to overlay variables
- FX type dots (`#EC4899`, `#14B8A6`, `#3B82F6`, `#8B5CF6`, `#F97316`): Keep hardcoded -- these are FX identity colors
- Any color already using `var(--color-*)`: Leave unchanged

**Per-file expected conversions (from research audit):**
- **AddFxMenu.tsx** (~15): Menu bg, borders, hover states, text colors, FX dots stay hardcoded
- **LeftPanel.tsx** (~17): Section headers, subsections, dividers, hover states, button text
- **PropertiesPanel.tsx** (~14): Panel bg, dividers, input text, section backgrounds
- **AddLayerMenu.tsx** (~14): Menu bg, borders, hover states, text colors
- **SequenceList.tsx** (~13): Selected state, context menu, edit input, dividers

After converting each file, verify no unconverted hardcoded colors remain (except the explicitly allowed identity/fixed colors listed above).
  </action>
  <verify>
    <automated>cd Application && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - All 5 files have zero hardcoded hex colors in Tailwind classes (except FX identity dots)
    - All use semantic CSS variable references from the theme palette
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert hardcoded colors in Toolbar, LayerList, ShortcutsOverlay, KeyPhotoStrip</name>
  <files>Application/src/components/layout/Toolbar.tsx, Application/src/components/layer/LayerList.tsx, Application/src/components/overlay/ShortcutsOverlay.tsx, Application/src/components/sequence/KeyPhotoStrip.tsx</files>
  <action>
Apply the same conversion mapping from Task 1 to these 4 remaining files.

**Per-file expected conversions (from research audit):**

- **Toolbar.tsx** (~8): `bg-[#1C1C1C]` -> `bg-[var(--color-bg-toolbar)]`, `text-[#CCCCCC]` -> `text-[var(--color-text-button)]`, `bg-[#333333]` dividers -> `bg-[var(--color-border-subtle)]`. The Export button `bg-[#F97316]` MUST stay hardcoded -- it is the primary CTA identity color.
- **LayerList.tsx** (~9): `bg-[#2A2A3A]` -> `bg-[var(--color-bg-selected)]`, alternating row backgrounds, drag handle color, delete hover state
- **ShortcutsOverlay.tsx** (~7): Dialog background `bg-[#1C1C1C]` -> `bg-[var(--color-bg-toolbar)]`, `border-[#444]` -> `border-[var(--color-border-kbd)]`, keyboard badge styling
- **KeyPhotoStrip.tsx** (~7): Thumbnail bg `bg-[#2A2A2A]` -> `bg-[var(--color-bg-hover-item)]`, menu backgrounds, add button styling

Use the exact same conversion mapping table from Task 1. Apply the same exception rules for identity colors.

After conversion, do a final grep across ALL 9 files (both tasks) to confirm no unconverted hex colors remain:
```bash
grep -En 'bg-\[#|text-\[#|border-\[#' Application/src/components/layer/AddFxMenu.tsx Application/src/components/layout/LeftPanel.tsx Application/src/components/layout/PropertiesPanel.tsx Application/src/components/layer/AddLayerMenu.tsx Application/src/components/sequence/SequenceList.tsx Application/src/components/layer/LayerList.tsx Application/src/components/layout/Toolbar.tsx Application/src/components/overlay/ShortcutsOverlay.tsx Application/src/components/sequence/KeyPhotoStrip.tsx
```

Any remaining hardcoded colors in the output MUST be from the allowed-fixed list (FX dots, Export button #F97316). If others remain, convert them.
  </action>
  <verify>
    <automated>cd Application && npx tsc --noEmit 2>&1 | head -20 && echo "--- Remaining hardcoded check ---" && grep -cE 'bg-\[#|text-\[#|border-\[#' src/components/layer/AddFxMenu.tsx src/components/layout/LeftPanel.tsx src/components/layout/PropertiesPanel.tsx src/components/layer/AddLayerMenu.tsx src/components/sequence/SequenceList.tsx src/components/layer/LayerList.tsx src/components/layout/Toolbar.tsx src/components/overlay/ShortcutsOverlay.tsx src/components/sequence/KeyPhotoStrip.tsx 2>/dev/null || echo "grep done"</automated>
  </verify>
  <done>
    - All 4 files have zero hardcoded hex colors (except Export button #F97316 in Toolbar, FX dots in AddFxMenu)
    - Combined 9-file grep shows only allowed-fixed colors remaining
    - TypeScript compiles without errors
    - All panels, menus, overlays use semantic CSS variables
  </done>
</task>

</tasks>

<verification>
- `cd Application && npx tsc --noEmit` passes
- `cd Application && npx vite build --mode development` succeeds
- Grep for hardcoded colors across all 9 files returns only allowed-fixed colors
</verification>

<success_criteria>
- ~104 hardcoded hex colors converted to CSS variable references across 9 files
- FX identity dots, Export button, and sequence identity colors remain hardcoded (by design)
- All converted components render correctly with CSS variable system
- TypeScript compiles and Vite builds succeed
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-theme-system/08-02-SUMMARY.md`
</output>
