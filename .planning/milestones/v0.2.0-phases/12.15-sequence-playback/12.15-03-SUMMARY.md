---
phase: 12.15-sequence-playback
plan: 03
subsystem: ui
tags: [preact, signals, isolation, loop, shortcuts, sidebar]

# Dependency graph
requires:
  - phase: 12.15-01
    provides: isolationStore with toggleIsolation, clearIsolation, removeSequence, toggleLoop, loadLoopPreference
provides:
  - Clickable sidebar accent bar for isolation toggle (orange when isolated)
  - Loop toggle button in timeline controls with Repeat/Repeat1 icons
  - Escape shortcut clears isolation with correct priority chain
  - Sequence deletion cleanup removes from isolation set
affects: [12.15-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Accent bar color toggle via conditional backgroundColor (orange #E5841B vs CSS var)"
    - "stopPropagation on nested clickable elements to prevent parent row selection"
    - "useEffect loadLoopPreference on mount for persisted config hydration"

key-files:
  created: []
  modified:
    - Application/src/components/sequence/SequenceList.tsx
    - Application/src/components/layout/TimelinePanel.tsx
    - Application/src/lib/shortcuts.ts
    - Application/src/stores/sequenceStore.ts

key-decisions:
  - "Skipped 'l' keyboard shortcut for loop toggle -- conflicts with JKL shuttle KeyL binding in tinykeys"
  - "Removed (L) hint from loop button tooltip to avoid referencing non-existent shortcut"

patterns-established:
  - "Escape priority chain: fullscreen > isolation > layer deselect"
  - "Isolation cleanup in both remove() and removeLayerFromSequence() for complete coverage"

requirements-completed: [ISO-02, ISO-07, ISO-09, ISO-10]

# Metrics
duration: 4min
completed: 2026-03-20
---

# Phase 12.15 Plan 03: UI Controls & Shortcuts Summary

**Sidebar accent bar isolation toggle, loop button in timeline controls, Escape clears isolation, and deletion cleanup**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-20T12:24:03Z
- **Completed:** 2026-03-20T12:28:17Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Sidebar accent bar is clickable and turns solid orange (#E5841B) when sequence is isolated
- Loop toggle button in timeline controls with Repeat/Repeat1 icons and persisted preference loading
- Escape key clears isolation with correct priority: fullscreen > isolation > layer deselect
- Sequence deletion (both remove and removeLayerFromSequence) cleans up isolation set

## Task Commits

Each task was committed atomically:

1. **Task 1: Make sidebar accent bar clickable with isolation toggle and add loop toggle button** - `9725c6e` (feat)
2. **Task 2: Add Escape isolation clear and sequence deletion cleanup** - `a50b37f` (feat)

**Plan metadata:** `b772ca6` (docs: complete plan)

## Files Created/Modified
- `Application/src/components/sequence/SequenceList.tsx` - Added isolationStore import, isIsolated state, clickable accent bar with conditional orange color
- `Application/src/components/layout/TimelinePanel.tsx` - Added Repeat/Repeat1 imports, isolationStore import, useEffect for loadLoopPreference, loop toggle button
- `Application/src/lib/shortcuts.ts` - Added isolationStore import, Escape handler isolation priority, removed conflicting 'l' shortcut
- `Application/src/stores/sequenceStore.ts` - Added isolationStore import, removeSequence calls in remove() and removeLayerFromSequence()

## Decisions Made
- Skipped 'l' keyboard shortcut for loop toggle because it conflicts with the JKL shuttle's KeyL binding in tinykeys (both would fire on the same key press)
- Removed "(L)" from loop button tooltip since the shortcut doesn't exist
- Restored TimelineInteraction.ts to committed state (uncommitted Plan 02 changes were in working tree causing TS errors)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Skipped conflicting 'l' shortcut to prevent JKL shuttle breakage**
- **Found during:** Task 2 (Escape and shortcuts)
- **Issue:** Plan specified 'l' shortcut for loop toggle, but 'KeyL' is already bound to JKL shuttle pressL(). tinykeys would fire both handlers on same key press.
- **Fix:** Omitted the 'l' shortcut entirely. Updated loop button tooltip to remove "(L)" hint.
- **Files modified:** Application/src/lib/shortcuts.ts, Application/src/components/layout/TimelinePanel.tsx
- **Verification:** tsc --noEmit passes, no duplicate handler conflict
- **Committed in:** a50b37f (Task 2 commit)

**2. [Rule 3 - Blocking] Restored TimelineInteraction.ts from uncommitted Plan 02 changes**
- **Found during:** Task 2 (verification)
- **Issue:** TimelineInteraction.ts had uncommitted changes from Plan 02 in working tree, adding unused imports (TRACK_HEIGHT, isolationStore) causing TS6133 errors
- **Fix:** Restored file to HEAD state via git checkout since changes are out of scope for Plan 03
- **Files modified:** Application/src/components/timeline/TimelineInteraction.ts (restored)
- **Verification:** tsc --noEmit passes clean
- **Committed in:** Not committed (restoration only)

---

**Total deviations:** 2 auto-fixed (1 bug prevention, 1 blocking)
**Impact on plan:** Loop toggle shortcut omitted to prevent JKL shuttle conflict. All other plan items implemented as specified.

## Issues Encountered
- Uncommitted TimelineInteraction.ts changes from Plan 02 were present in the working tree after git stash/pop, causing TS errors. Resolved by restoring to committed state.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All UI surface for isolation is complete (sidebar, timeline controls, keyboard)
- Plan 04 can proceed with remaining isolation features
- Loop toggle shortcut could be revisited with a non-conflicting key in a future plan

## Self-Check: PASSED

All files exist, all commits verified (9725c6e, a50b37f).

---
*Phase: 12.15-sequence-playback*
*Completed: 2026-03-20*
