---
phase: 12.15-sequence-playback
plan: 04
type: execute
wave: 3
depends_on: ["12.15-02", "12.15-03"]
files_modified: []
autonomous: false
requirements:
  - ISO-01
  - ISO-02
  - ISO-03
  - ISO-04
  - ISO-05
  - ISO-06
  - ISO-07
  - ISO-08
  - ISO-09
  - ISO-10

must_haves:
  truths:
    - "All 10 ISO requirements verified through manual walkthrough"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete isolation and loop feature through manual walkthrough.

Purpose: This is the final verification checkpoint for the entire phase. All visual, interactive, and persistence behaviors are verified end-to-end.

Output: User approval or issues list
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.15-sequence-playback/12.15-CONTEXT.md
@.planning/phases/12.15-sequence-playback/12.15-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of sequence isolation and loop feature</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for approval. The complete isolation feature was built across Plans 01-03:
- Isolation store (Set-based signal for sequence IDs)
- Isolation-aware playback engine (skips non-isolated frames, loop toggle)
- Timeline name label click to toggle isolation (Canvas 2D hit detection)
- Orange border + orange name label rendering for isolated sequences
- Sidebar accent bar click to toggle isolation
- Loop toggle button (Repeat/Repeat1) in timeline controls
- Loop toggle persisted to ~/.config/efx-motion/builder-config.yaml
- Escape clears all isolation (priority: fullscreen > isolation > deselect)
- L key toggles loop
- Sequence deletion cleans up isolation set

**Verification steps for the user (open a project with 3+ content sequences):**

ISO-01: Click name labels on timeline to toggle isolation -- verify orange border + orange label
ISO-02: Click sidebar accent bar to toggle isolation -- verify orange bar color
ISO-03: Isolate sequences 1 and 3, play -- verify playhead skips sequence 2
ISO-04: Add FX overlapping isolated sequence -- verify FX renders during isolated playback
ISO-05: Enable loop, play isolated -- verify seamless loop back to first isolated
ISO-06: Disable loop, play isolated -- verify stop at end, seek to first isolated start
ISO-07: Press Escape -- verify isolation cleared (fullscreen exit takes priority if active)
ISO-08: Toggle loop ON, reload app -- verify loop state persisted
ISO-09: Isolate from timeline, check sidebar accent bar is orange for that sequence
ISO-10: Isolate a sequence, delete it -- verify no errors, clean state
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>All 10 ISO requirements pass manual verification</done>
</task>

</tasks>

<verification>
All 10 ISO requirements verified through the manual walkthrough above.
</verification>

<success_criteria>
User confirms all 10 ISO requirements pass the manual verification steps.
</success_criteria>

<output>
After completion, create `.planning/phases/12.15-sequence-playback/12.15-04-SUMMARY.md`
</output>
