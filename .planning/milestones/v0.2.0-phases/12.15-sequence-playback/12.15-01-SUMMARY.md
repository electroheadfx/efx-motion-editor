---
phase: 12.15-sequence-playback
plan: 01
subsystem: playback
tags: [preact-signals, tauri-ipc, isolation, loop, playback-engine]

requires:
  - phase: 12.13
    provides: linear timeline layout, trackLayouts computed
  - phase: 12.11
    provides: full-speed playback mode, isFullSpeed signal
provides:
  - isolationStore with Set-based sequence isolation and loop toggle
  - Tauri IPC loop_enabled persistence in builder-config.yaml
  - Isolation-aware playback engine (skip non-isolated frames, loop/stop behavior)
affects: [12.15-02, 12.15-03, 12.15-04]

tech-stack:
  added: []
  patterns:
    - "Set<string> signal for multi-select isolation state"
    - "Isolated range computation from trackLayouts for tick loop"
    - "Loop-conditional auto-stop in playback engine"

key-files:
  created:
    - Application/src/stores/isolationStore.ts
  modified:
    - Application/src-tauri/src/commands/config.rs
    - Application/src-tauri/src/lib.rs
    - Application/src/lib/ipc.ts
    - Application/src/lib/appConfig.ts
    - Application/src/lib/playbackEngine.ts

key-decisions:
  - "Tick reads isolation state via .peek() for zero-overhead when no isolation active"
  - "Normal playback auto-loop now conditional on loopEnabled (was always-on)"

patterns-established:
  - "Set-based signal isolation pattern: new Set(signal.peek()) for immutable updates"
  - "Range-based frame navigation: nextIsolatedFrame/prevIsolatedFrame for gap-skipping"

requirements-completed: [ISO-03, ISO-04, ISO-05, ISO-06, ISO-08, ISO-10]

duration: 3min
completed: 2026-03-20
---

# Phase 12.15 Plan 01: Isolation Store and Playback Engine Summary

**Set-based isolation store with Tauri-persisted loop toggle and isolation-aware playback engine that skips non-isolated frames**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-20T12:17:02Z
- **Completed:** 2026-03-20T12:20:37Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created isolationStore with Set-based signal, toggle/clear/remove helpers, computed hasIsolation, and loop toggle with persistence
- Full Tauri IPC pipeline: Rust BuilderConfig field, get/set commands, lib.rs registration, TS IPC wrappers, appConfig persistence helpers
- Playback engine now skips non-isolated frames, loops or stops at isolation boundaries, and auto-seeks to first isolated frame on start

## Task Commits

Each task was committed atomically:

1. **Task 1: Create isolation store and Tauri IPC loop persistence** - `5cb2e9f` (feat)
2. **Task 2: Modify playback engine for isolation-aware tick and loop toggle** - `41dcdf9` (feat)

## Files Created/Modified
- `Application/src/stores/isolationStore.ts` - New isolation store with Set-based sequence isolation, loop toggle, and persistence loading
- `Application/src-tauri/src/commands/config.rs` - Added loop_enabled field to BuilderConfig, config_get_loop_enabled and config_set_loop_enabled commands
- `Application/src-tauri/src/lib.rs` - Registered two new config commands in invoke_handler
- `Application/src/lib/ipc.ts` - Added configGetLoopEnabled and configSetLoopEnabled IPC wrappers
- `Application/src/lib/appConfig.ts` - Added getLoopEnabled and setLoopEnabled persistence helpers
- `Application/src/lib/playbackEngine.ts` - Added isolation-aware tick loop with range helpers, loop-conditional stop, isolation seek on start

## Decisions Made
- Tick reads isolation state via .peek() for zero-overhead when no isolation active -- the hot path (no isolation) has only a single `isolatedIds.size > 0` check added
- Normal playback auto-loop now conditional on loopEnabled signal -- previously playback always looped, now it respects the toggle even without isolation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Isolation store exports ready for Plan 02 (timeline UI: Solo/Mute buttons) and Plan 03 (sidebar controls)
- playbackEngine isolation behavior complete, Plan 04 (keyboard shortcuts) can wire to isolationStore.toggleLoop()
- TypeScript and Rust compilation both pass clean

---
*Phase: 12.15-sequence-playback*
*Completed: 2026-03-20*
