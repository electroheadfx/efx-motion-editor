---
phase: 12.1.1-big-ui-frontend-design-changes-with-new-sidebar-layout
plan: 03
subsystem: ui
tags: [preact, lucide-preact, css-variables, sidebar, sequence-cards, layer-icons]

# Dependency graph
requires:
  - phase: 12.1.1-01
    provides: CSS --sidebar-* variables, panel resize infrastructure
provides:
  - Sequence cards with inline key photos and slide animation
  - Layer rows with Eye/EyeOff visibility toggle and GripVertical drag handle
  - NumericInput updated typography with sidebar colors
affects: [12.1.1-04, 12.1.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lucide icons for all interactive icons (GripVertical, Eye, EyeOff, Lock, X, Plus, Ellipsis)"
    - "Inline key photos rendered inside SequenceItem card with maxHeight transition"
    - "JSX inline style objects for sidebar CSS variable usage"

key-files:
  created: []
  modified:
    - Application/src/components/sequence/SequenceList.tsx
    - Application/src/components/sequence/KeyPhotoStrip.tsx
    - Application/src/components/layer/LayerList.tsx
    - Application/src/components/shared/NumericInput.tsx

key-decisions:
  - "Used Ellipsis icon instead of MoreHorizontal (renamed in lucide-preact v0.460+)"
  - "AddKeyPhotoButton opens full importer via setEditorMode('imported') instead of popover"
  - "Removed isDraggingLabel state from NumericInput (cursor is always cursor-ew-resize)"

patterns-established:
  - "Card layout pattern: rounded-lg overflow-hidden with border via --sidebar-border-selected/unselected"
  - "Slide animation pattern: transition-[max-height] duration-150 ease-out with conditional maxHeight"

requirements-completed: [KEY-PHOTOS-INLINE, LAYER-EYE, TYPOGRAPHY, SCRUB-AUTO-SELECT]

# Metrics
duration: 5min
completed: 2026-03-17
---

# Phase 12.1.1 Plan 03: Sequence & Layer Styling Summary

**Sequence cards with inline key photos (150ms slide animation), layer rows with lucide Eye/Lock/GripVertical icons, and NumericInput 12px sidebar typography**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-17T09:10:06Z
- **Completed:** 2026-03-17T09:15:49Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- SequenceItem redesigned as bordered cards with left accent bar, inline key photos that slide in/out with 150ms animation
- Layer rows updated to 44px height with Eye/EyeOff visibility toggle, GripVertical drag handle, Lock icon for base, X delete button
- NumericInput typography updated to 12px/600 label and 12px/400 value with sidebar color variables
- All components now use --sidebar-* CSS variables for consistent theming

## Task Commits

Each task was committed atomically:

1. **Task 1: SequenceItem card redesign with inline key photos** - `a28f130` (feat)
2. **Task 2: LayerRow redesign with eye icon, grip handle, and NumericInput typography** - `1c5fbab` (feat)

## Files Created/Modified
- `Application/src/components/sequence/SequenceList.tsx` - Card layout with border styling, accent bar, GripVertical/Ellipsis icons, inline KeyPhotoStripInline
- `Application/src/components/sequence/KeyPhotoStrip.tsx` - Renamed KeyPhotoStripInner to KeyPhotoStripInline (exported), simplified AddKeyPhotoButton to open importer, Plus icon
- `Application/src/components/layer/LayerList.tsx` - 44px rows with Eye/EyeOff, GripVertical, Lock, X lucide icons, --sidebar-* colors
- `Application/src/components/shared/NumericInput.tsx` - 12px/600 label, 12px/400 value, sidebar color variables

## Decisions Made
- Used `Ellipsis` icon instead of `MoreHorizontal` -- the lucide-preact version installed has renamed MoreHorizontal to Ellipsis
- AddKeyPhotoButton now opens full imported view via `uiStore.setEditorMode('imported')` per plan spec -- removed entire popover state/rendering
- Removed `isDraggingLabel` state from NumericInput since cursor styling is now always `cursor-ew-resize` (the state was only toggling the same value)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed unused isDraggingLabel state from NumericInput**
- **Found during:** Task 2 (NumericInput typography)
- **Issue:** After removing the conditional class that used `isDraggingLabel`, TypeScript errored on the unused variable declaration
- **Fix:** Removed the state declaration and both `setIsDraggingLabel()` calls
- **Files modified:** Application/src/components/shared/NumericInput.tsx
- **Verification:** Build passes
- **Committed in:** 1c5fbab (Task 2 commit)

**2. [Rule 3 - Blocking] Used Ellipsis instead of MoreHorizontal icon**
- **Found during:** Task 1 (SequenceItem redesign)
- **Issue:** lucide-preact version installed has renamed MoreHorizontal to Ellipsis -- the old name does not exist
- **Fix:** Used `Ellipsis` icon import which is functionally identical (three horizontal dots)
- **Files modified:** Application/src/components/sequence/SequenceList.tsx
- **Verification:** Build passes, icon renders correctly
- **Committed in:** a28f130 (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking)
**Impact on plan:** Both auto-fixes necessary for build success. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Sequence cards and layer rows are fully styled with sidebar CSS variables
- Key photos inline in sequence cards, ready for Plan 04 (keyframe navigation + interpolation)
- Scrub auto-select behavior preserved through the existing selectSequence signal path

---
*Phase: 12.1.1-big-ui-frontend-design-changes-with-new-sidebar-layout*
*Completed: 2026-03-17*
