# Phase 05.2: Opacity Fix + Local Physics Mode - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 05.2-opacity-is-broken-because-of-the-new-physics-engine-so-add-local-physics-to-brush-paint-in-live-which-have-transparence-opacity-with-settings
**Areas discussed:** Per-stroke opacity model, Local physics trigger, Opacity settings/UI, directRender path

---

## Per-stroke opacity model

| Option | Description | Selected |
|--------|-------------|----------|
| Per-pixel opacity accumulator | Add Float32Array (wet.strokeOpacity) that accumulates intended opacity per pixel. Compositor reads per-pixel instead of global. Physics still on full-intensity buffers. | ✓ |
| Deposit at reduced intensity | Revert D-12: deposit at opacity-scaled intensity. Physics sees weaker paint. | |
| Dual-buffer: full + display | Separate full buffer for physics and display buffer for opacity. Doubles memory. | |

**User's choice:** Per-pixel opacity accumulator
**Notes:** User also asked about a mode for 100% opacity that applies physics — led to local physics discussion.

### Overlap compositing

| Option | Description | Selected |
|--------|-------------|----------|
| Porter-Duff over | result = a1 + a2*(1-a1). Two 50% → 75%. Natural buildup, never exceeds 1.0. | ✓ |
| Max of stroke opacities | result = max(existing, new). No buildup from same-opacity strokes. | |

**User's choice:** Porter-Duff over
**Notes:** User note: "Is it possible to add a mode which work for 100% opacity which mean it apply too physics in this mode instead of global?" — captured as local physics mode requirement.

---

## Local physics trigger

Initial question about opacity-routed physics was rejected — user clarified they want a **local physics mode** that works for both 100% and <100% opacity, scoping the Stam solver to the active stroke area using global physics strength. Not an opacity-conditional mode.

### Spread range

| Option | Description | Selected |
|--------|-------------|----------|
| Margin from physics settings | Spread distance derived from global physics strength (viscosity, water amount). | ✓ |
| Fixed margin (2x brush radius) | Fixed pixel multiplier of brush radius. | |
| You decide | Claude picks during implementation. | |

**User's choice:** Margin from physics settings

### Mode switching

| Option | Description | Selected |
|--------|-------------|----------|
| UI toggle | Button in toolbar: Local / Global physics. Default: Local. | ✓ |
| Automatic based on opacity | 100% = global, <100% = local automatically. | |
| Always both available | Local during painting, global toggle for ambient. | |

**User's choice:** UI toggle

---

## Opacity settings/UI

| Option | Description | Selected |
|--------|-------------|----------|
| Physics mode toggle | Button: Local / Global physics mode. Default: Local. | ✓ |
| Keep existing opacity slider as-is | Opacity slider (10-100) controls per-stroke opacity via accumulator. | ✓ |
| Local physics spread strength | Slider for how aggressively local physics spreads paint. Separate from global viscosity. | ✓ |
| You decide on controls | Claude picks minimal controls. | |

**User's choice:** Physics mode toggle + existing opacity slider + local spread strength

---

## directRender path

| Option | Description | Selected |
|--------|-------------|----------|
| Unify to wet layer path | Remove directRender split. ALL paint through wet layer + compositor with per-pixel opacity. | ✓ |
| Keep both paths, sync opacity | Keep directRender for performance, update both paths for opacity consistency. | |
| You decide | Claude picks based on complexity. | |

**User's choice:** Unify to wet layer path

---

## Claude's Discretion

- Exact bounding box margin calculation for local physics
- strokeOpacity behavior during diffusion (advect or static)
- Performance optimization for local Stam solver
- wetPaper blending adjustments with unified path
- Mode transition behavior

## Deferred Ideas

- Auto-physics trigger when painting over existing wet paint — future enhancement
- Per-stroke physics parameter overrides — future phase
- Opacity-dependent physics strength — future phase
