---
phase: 05-library-demo-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - paint-rebelle-new/src/core/wet-layer.ts
  - paint-rebelle-new/src/core/diffusion.ts
  - paint-rebelle-new/src/core/drying.ts
  - paint-rebelle-new/src/core/paper.ts
  - paint-rebelle-new/src/brush/stroke.ts
  - paint-rebelle-new/src/brush/paint.ts
  - paint-rebelle-new/src/brush/erase.ts
  - paint-rebelle-new/src/brush/water.ts
autonomous: true
requirements: [LIB-02]

must_haves:
  truths:
    - "All physics core functions from v3.html are extracted to typed modules"
    - "All brush functions from v3.html are extracted to typed modules"
    - "No module holds mutable state — all buffers passed as arguments"
    - "No module accesses DOM (document, window) — only receives contexts as args"
  artifacts:
    - path: "paint-rebelle-new/src/core/wet-layer.ts"
      provides: "Wet layer buffer operations"
      exports: ["createWetBuffers", "createSavedWetBuffers", "createTmpBuffers", "clearWetLayer", "depositToWetLayer", "depositToWetLayerWithColors", "transferToWetLayer", "transferToWetLayerClipped"]
    - path: "paint-rebelle-new/src/core/diffusion.ts"
      provides: "FBM-displaced darken-flood diffusion"
      exports: ["precomputeDisplacement", "diffuseStep", "buildColorMap", "sampleChannel", "sampleColorPremul", "physicsStep"]
    - path: "paint-rebelle-new/src/core/drying.ts"
      provides: "LUT-driven drying system"
      exports: ["initDryingLUT", "dryStep", "forceDryAll"]
    - path: "paint-rebelle-new/src/core/paper.ts"
      provides: "Paper texture loading and brush grain"
      exports: ["loadPaperTexture", "createMirroredBrushGrain", "sampleBrushGrain", "sampleH", "sampleTexH", "ensureHeightMap"]
    - path: "paint-rebelle-new/src/brush/stroke.ts"
      provides: "Stroke processing pipeline"
      exports: ["smooth", "resample", "ribbon", "deform", "deformN", "avgPenData", "pressureAtT", "speedAtT", "tiltAtT"]
    - path: "paint-rebelle-new/src/brush/paint.ts"
      provides: "Paint brush rendering"
      exports: ["renderPaintStroke", "renderPaintStrokeSingleColor", "fillPolyGrain", "fillFlat", "drawBristleTraces", "sampleAreaColor", "buildCarriedColors"]
    - path: "paint-rebelle-new/src/brush/erase.ts"
      provides: "Erase brush"
      exports: ["applyEraseStroke"]
    - path: "paint-rebelle-new/src/brush/water.ts"
      provides: "Water/smear/blend/blow/wet/dry brush stubs"
      exports: ["applyWaterStroke", "applySmearChunk", "applyBlendStroke", "applyBlowChunk", "applyWetChunk", "applyDryChunk"]
  key_links:
    - from: "paint-rebelle-new/src/core/diffusion.ts"
      to: "paint-rebelle-new/src/types.ts"
      via: "import WetBuffers, TmpBuffers, ColorMap, DiffusionParams"
      pattern: "import.*WetBuffers.*from.*types"
    - from: "paint-rebelle-new/src/brush/paint.ts"
      to: "paint-rebelle-new/src/core/wet-layer.ts"
      via: "import depositToWetLayer"
      pattern: "import.*depositToWetLayer.*from.*wet-layer"
    - from: "paint-rebelle-new/src/core/drying.ts"
      to: "paint-rebelle-new/src/types.ts"
      via: "import DryingLUT, LUT_SIZE"
      pattern: "import.*LUT_SIZE.*from.*types"
---

<objective>
Extract all core physics modules (wet-layer, diffusion, drying, paper) and all brush modules (stroke processing, paint, erase, water+stubs) from v3.html into typed TypeScript.

Purpose: These are the engine internals — the actual paint physics algorithms. They must be pure functional modules that receive all state as arguments and never touch DOM. This is the largest extraction task in the phase.
Output: 8 TypeScript modules containing all ~60 physics and brush functions from v3.html.
</objective>

<execution_context>
@/Users/lmarques/Dev/paint-rebelle-experiment/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/paint-rebelle-experiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-library-demo-polish/05-CONTEXT.md
@.planning/phases/05-library-demo-polish/05-RESEARCH.md

<interfaces>
<!-- Types from plan 01 that this plan imports. Executor reads types.ts but these are the key contracts. -->

From paint-rebelle-new/src/types.ts (created in plan 01):
```typescript
export interface WetBuffers { r: Float32Array; g: Float32Array; b: Float32Array; alpha: Float32Array; wetness: Float32Array }
export interface SavedWetBuffers { r: Float32Array; g: Float32Array; b: Float32Array; alpha: Float32Array }
export interface TmpBuffers { r: Float32Array; g: Float32Array; b: Float32Array; alpha: Float32Array }
export interface ColorMap { r: Float32Array; g: Float32Array; b: Float32Array }
export interface DiffusionParams { physicsStrength: number; blowDX: Float32Array; blowDY: Float32Array }
export interface DryingLUT { dryLUT: Float32Array; invLUT: Float32Array; dryPos: Float32Array }
export interface PenPoint { x: number; y: number; p: number; tx: number; ty: number; tw: number; spd: number }
export interface BrushOpts { size: number; opacity: number; pressure: number; waterAmount: number; dryAmount: number; edgeDetail: number; pickup: number; eraseStrength: number }
export const LUT_SIZE = 3000
export const DENSITY_NORM = 3000
export const DENSITY_K = 3.5
export const MAX_DISPLAY_ALPHA = 245
export const BLOW_DECAY = 0.92
export const BLOW_STRENGTH = 8.0
export const TEXTURE_SIZE = 512
export const TEXTURE_HALF = 256
export const TEXTURE_MASK = 511
```

From paint-rebelle-new/src/util/math.ts:
```typescript
export function gauss(m?: number, s?: number): number
export function lerp(a: number, b: number, t: number): number
export function clamp(v: number, lo: number, hi: number): number
export function curveBounds(curve: Array<[number,number]>, radius: number): {...}
export function polyBounds(pts: Array<[number,number]>): {...}
export function lerpPt(a: PenPoint, b: PenPoint, t: number): PenPoint
```

From paint-rebelle-new/src/util/noise.ts:
```typescript
export function noise(px: number, py: number): number
export function fbm(x: number, y: number, octaves?: number): number
```

From paint-rebelle-new/src/util/color.ts:
```typescript
export function hexRgb(hex: string): [number, number, number]
export function rgbHex(r: number, g: number, b: number): string
export function mixSubtractive(c1: [number,number,number], c2: [number,number,number], ratio: number): [number,number,number]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract core physics modules (wet-layer, diffusion, drying, paper)</name>
  <files>
    paint-rebelle-new/src/core/wet-layer.ts,
    paint-rebelle-new/src/core/diffusion.ts,
    paint-rebelle-new/src/core/drying.ts,
    paint-rebelle-new/src/core/paper.ts
  </files>
  <read_first>
    efx-paint-physic-v3.html,
    paint-rebelle-new/src/types.ts,
    paint-rebelle-new/src/util/noise.ts,
    paint-rebelle-new/src/util/math.ts
  </read_first>
  <action>
    Extract v3.html functions into 4 core modules. CRITICAL: No module-level mutable state. All buffers are passed as function arguments. No `document` or `window` access.

    **core/wet-layer.ts** — Functions from v3.html that operate on wet layer Float32Arrays:

    - `createWetBuffers(size: number): WetBuffers` — factory: allocates 5 Float32Arrays of given size
    - `createSavedWetBuffers(size: number): SavedWetBuffers` — factory: allocates 4 Float32Arrays
    - `createTmpBuffers(size: number): TmpBuffers` — factory: allocates 4 Float32Arrays
    - `clearWetLayer(wet: WetBuffers, saved: SavedWetBuffers, dryPos: Float32Array): void` — from v3 `clearWetLayer()` line 2559: calls `.fill(0)` on all wet arrays, saved arrays, and dryPos
    - `depositToWetLayer(curve, color, radius, opacity, wet, wetBuffers, savedWet, dryPos, lastStrokeMask, width, height, embossStrength, paperHeight, brushGrain)` — from v3 line 755-803: polygon fill into wet layer with grain/emboss modulation. Uses the same dual-loop (per polygon, then per scanline) pattern.
    - `depositToWetLayerWithColors(curve, carriedColors, radius, opacity, wet, wetBuffers, savedWet, dryPos, lastStrokeMask, width, height, embossStrength, paperHeight, brushGrain)` — from v3 line 805-853: like deposit but with per-polygon color from pickup
    - `transferToWetLayer(offCtx: CanvasRenderingContext2D, wetBuffers: WetBuffers, savedWet: SavedWetBuffers, dryPos: Float32Array, lastStrokeMask: Uint8Array, width: number, height: number): void` — from v3 line 855-882: reads canvas ImageData and writes to wet arrays for non-zero alpha pixels
    - `transferToWetLayerClipped(offCtx, wetBuffers, savedWet, dryPos, lastStrokeMask, bounds, width, height)` — from v3 line 884-919: clipped version using bounds rect

    These functions are the most complex extraction. Keep v3's exact algorithm but rename single-letter vars:
    - `W` -> `width`, `H` -> `height`
    - `wetR/wetG/wetB` -> access via `wetBuffers.r/g/b`
    - `wetAlpha` -> `wetBuffers.alpha`
    - `wetness` -> `wetBuffers.wetness`

    **core/diffusion.ts** — FBM-displaced darken-flood diffusion from v3 lines 211-1955:

    - `buildColorMap(wet: WetBuffers, colorMap: ColorMap, width: number, height: number): void` — from v3 line 211: computes alpha-weighted average color, fills colorMap arrays
    - `sampleChannel(buf: Float32Array, fx: number, fy: number, width: number, height: number): number` — from v3 line 397: bilinear interpolation of a single channel
    - `sampleColorPremul(colorBuf: ColorMap, fx: number, fy: number, width: number, height: number): [number, number, number]` — from v3 line 412: bilinear sample of premultiplied color
    - `precomputeDisplacement(width: number, height: number, paperHeight: Float32Array | null, dispPxX: Float32Array, dispPxY: Float32Array): void` — from v3 line 1737: FBM noise + paper gradient displacement
    - `diffuseStep(wet: WetBuffers, tmp: TmpBuffers, colorMap: ColorMap, dispPxX: Float32Array, dispPxY: Float32Array, blowDX: Float32Array, blowDY: Float32Array, width: number, height: number, strength: number): void` — from v3 line 1828: single frame of darken-flood diffusion with blow bias
    - `physicsStep(wet: WetBuffers, tmp: TmpBuffers, colorMap: ColorMap, drying: DryingLUT, dispPxX: Float32Array, dispPxY: Float32Array, blowDX: Float32Array, blowDY: Float32Array, width: number, height: number, strength: number, drySpeed: number): void` — from v3 line 1926: orchestrates diffuseStep + dryStep + blow decay per tick

    Import `fbm` from `../util/noise`, `clamp` from `../util/math`, `BLOW_DECAY` from `../types`.

    **core/drying.ts** — LUT-driven drying from v3 lines 250-1735:

    - `initDryingLUT(dryLUT: Float32Array, invLUT: Float32Array): void` — from v3 line 260: fills dryLUT with cumulative S-curve (same 0.003 base with biased polynomial), fills invLUT as inverse mapping. Exact algorithm from v3 lines 260-284.
    - `dryStep(wet: WetBuffers, drying: DryingLUT, ctx: CanvasRenderingContext2D, width: number, height: number, drySpeed: number, densityK: number): void` — from v3 line 1654: advances dryPos, transfers wet paint to dry canvas via LUT delta, handles full-dry cleanup. The ctx is the dry canvas (hidden `c`) context.
    - `forceDryAll(wet: WetBuffers, saved: SavedWetBuffers, drying: DryingLUT, ctx: CanvasRenderingContext2D, width: number, height: number): void` — from v3 line 2526: instantly transfers all wet paint to dry layer using a=min(wetAlpha/800,1) opacity formula (the "sacred" /800)

    Import `LUT_SIZE`, `DENSITY_K`, `DENSITY_NORM`, `MAX_DISPLAY_ALPHA` from `../types`. Import `clamp` from `../util/math`.

    **core/paper.ts** — Paper texture and brush grain from v3:

    - `loadPaperTexture(url: string, width: number, height: number): Promise<{ heightMap: Float32Array, textureData: ImageData }>` — from v3 line 286: loads image, extracts red channel to Float32Array heightmap (512x512, tiled across canvas)
    - `createMirroredBrushGrain(src128: Uint8ClampedArray, textureSize: number): Uint8Array` — from v3 line 338: creates 4-quadrant mirrored grain texture (256x256 input -> 512x512 output)
    - `sampleBrushGrain(brushGrain: Uint8Array | null, x: number, y: number): number` — from v3 line 353: returns grain value 0-1 at coordinate, uses TEXTURE_MASK for wrapping
    - `sampleH(paperHeight: Float32Array | null, x: number, y: number, width: number, height: number): number` — from v3 line 601: paper height at pixel, returns 0.5 if no texture
    - `sampleTexH(textureHeight: Float32Array | null, x: number, y: number): number` — from v3 line 609: raw 512x512 texture height sample
    - `ensureHeightMap(texHeight: Float32Array | null, bgData: ImageData | null, width: number, height: number): Float32Array` — from v3 line 1763: creates heightmap from bgData if texHeight is null

    NOTE: `loadPaperTexture` is the ONE function in core/ that touches Image/canvas — it must create an offscreen canvas to read image pixel data. This is acceptable because it's an async loader, not a render-path function. Use `new Image()` and offscreen canvas pattern from v3 line 286-336.

    For ALL modules: use `import type` for type-only imports. Use regular `import` for value imports (constants, functions).
  </action>
  <verify>
    <automated>cd paint-rebelle-new && pnpm run check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - paint-rebelle-new/src/core/wet-layer.ts contains `export function createWetBuffers`
    - paint-rebelle-new/src/core/wet-layer.ts contains `export function depositToWetLayer`
    - paint-rebelle-new/src/core/wet-layer.ts contains `export function clearWetLayer`
    - paint-rebelle-new/src/core/wet-layer.ts does NOT contain `document` or `window`
    - paint-rebelle-new/src/core/diffusion.ts contains `export function diffuseStep`
    - paint-rebelle-new/src/core/diffusion.ts contains `export function buildColorMap`
    - paint-rebelle-new/src/core/diffusion.ts contains `export function precomputeDisplacement`
    - paint-rebelle-new/src/core/diffusion.ts contains `export function physicsStep`
    - paint-rebelle-new/src/core/diffusion.ts does NOT contain `document` or `window`
    - paint-rebelle-new/src/core/drying.ts contains `export function initDryingLUT`
    - paint-rebelle-new/src/core/drying.ts contains `export function dryStep`
    - paint-rebelle-new/src/core/drying.ts contains `export function forceDryAll`
    - paint-rebelle-new/src/core/paper.ts contains `export function loadPaperTexture`
    - paint-rebelle-new/src/core/paper.ts contains `export function createMirroredBrushGrain`
    - `pnpm run check` exits 0
  </acceptance_criteria>
  <done>All 4 core physics modules extracted from v3.html with typed signatures: wet-layer.ts (8 functions), diffusion.ts (6 functions), drying.ts (3 functions), paper.ts (6 functions). No DOM access in any module except paper.ts loader. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract brush modules (stroke, paint, erase, water stubs)</name>
  <files>
    paint-rebelle-new/src/brush/stroke.ts,
    paint-rebelle-new/src/brush/paint.ts,
    paint-rebelle-new/src/brush/erase.ts,
    paint-rebelle-new/src/brush/water.ts
  </files>
  <read_first>
    efx-paint-physic-v3.html,
    paint-rebelle-new/src/types.ts,
    paint-rebelle-new/src/core/wet-layer.ts,
    paint-rebelle-new/src/util/math.ts
  </read_first>
  <action>
    Extract all brush-related functions from v3.html into 4 brush modules. Same rules: no module-level mutable state, no DOM access, all buffers as arguments.

    **brush/stroke.ts** — Stroke processing pipeline from v3 lines 488-580:

    - `avgPenData(pts: PenPoint[]): PenPoint` — from v3 line 496: averages pen data across points
    - `pressureAtT(pts: PenPoint[], t: number): number` — from v3 line 504: interpolate pressure along stroke
    - `speedAtT(pts: PenPoint[], t: number): number` — from v3 line 508
    - `tiltAtT(pts: PenPoint[], t: number): { tx: number, ty: number }` — from v3 line 512
    - `smooth(pts: PenPoint[], iterations?: number): PenPoint[]` — from v3 line 520: Catmull-Rom smoothing, default iterations=3
    - `resample(pts: PenPoint[], spacing: number): PenPoint[]` — from v3 line 533: equidistant resampling along curve
    - `ribbon(curve: Array<[number,number]>, halfWidth: number, tPow?: number, penData?: PenPoint[]): Array<[number,number]>` — from v3 line 549: creates polygon from centerline with varying width from pressure. Default tPow=0.8. Returns closed polygon as [x,y] array.
    - `deform(poly: Array<[number,number]>, variance: number): Array<[number,number]>` — from v3 line 574: midpoint displacement for organic edges
    - `deformN(poly: Array<[number,number]>, depth: number, variance: number): Array<[number,number]>` — from v3 line 580: recursive deform

    Import PenPoint from '../types', lerpPt from '../util/math'.

    **brush/paint.ts** — Paint brush rendering from v3 lines 586-1098:

    - `genTexture(type: 'soft' | 'flat'): HTMLCanvasElement` — from v3 line 586: creates offscreen radial gradient texture. This is the ONE brush function that creates canvas elements (offscreen texture generation). Returns canvas.
    - `fillPolyGrain(ctx: CanvasRenderingContext2D, pts: Array<[number,number]>, r: number, g: number, b: number, alpha: number, grain: (x:number,y:number)=>number, embossStrength: number, paperHeight: Float32Array|null, width: number, height: number): void` — from v3 line 618: fill polygon with grain/emboss modulation
    - `fillFlat(ctx: CanvasRenderingContext2D, pts: Array<[number,number]>, color: [number,number,number], alpha: number): void` — from v3 line 646: flat polygon fill
    - `drawBristleTraces(ctx: CanvasRenderingContext2D, curve: Array<[number,number]>, radius: number, color: [number,number,number], opacity: number, penData: PenPoint[], pressureFn: (pts:PenPoint[],t:number)=>number, tiltFn: (pts:PenPoint[],t:number)=>{tx:number,ty:number}): void` — from v3 line 657: bristle traces along stroke
    - `sampleAreaColor(imgData: ImageData, cx: number, cy: number, radius: number): [number,number,number,number] | null` — from v3 line 707: samples average color from canvas area
    - `buildCarriedColors(curve, pickerColor, pickup, canvasSnap, radius): Array<[number,number,number]>` — from v3 line 724: builds per-segment color from surface sampling + subtractive mixing
    - `renderPaintStroke(rawPts: PenPoint[], color: string, opts: BrushOpts, ctx: CanvasRenderingContext2D, wetBuffers: WetBuffers, savedWet: SavedWetBuffers, dryPos: Float32Array, lastStrokeMask: Uint8Array, paperHeight: Float32Array|null, brushGrain: Uint8Array|null, width: number, height: number, hasPenInput: boolean, wetPaper: boolean, embossStrength: number, directRender: boolean): {bounds: any, previewData: any}` — from v3 line 921: MAIN paint stroke entry point. Orchestrates smooth -> resample -> ribbon -> deformN -> fillPolyGrain -> depositToWetLayer pipeline.
    - `renderPaintStrokeSingleColor(curve, color, radius, opacity, wet, speedDeplete, opts, ctx, wetBuffers, savedWet, dryPos, lastStrokeMask, paperHeight, brushGrain, width, height, embossStrength, directRender)` — from v3 line 1020: simplified single-color path (no pickup)
    - `applyPaperEmboss(ctx: CanvasRenderingContext2D, bounds: any, paperHeight: Float32Array|null, width: number, height: number, embossStrength: number): void` — from v3 line 989: applies emboss effect in bounding box

    Import: `smooth`, `resample`, `ribbon`, `deformN`, `pressureAtT`, `speedAtT`, `tiltAtT`, `avgPenData` from `./stroke`. Import `depositToWetLayer`, `depositToWetLayerWithColors`, `transferToWetLayer`, `transferToWetLayerClipped` from `../core/wet-layer`. Import `hexRgb`, `mixSubtractive` from `../util/color`. Import `gauss`, `lerp`, `clamp`, `curveBounds` from `../util/math`. Import `sampleBrushGrain` from `../core/paper`.

    **brush/erase.ts** — Erase brush from v3 line 1214-1296:

    - `applyEraseStroke(rawPts: PenPoint[], opts: BrushOpts, ctx: CanvasRenderingContext2D, wetBuffers: WetBuffers, savedWet: SavedWetBuffers, dryPos: Float32Array, width: number, height: number, hasPenInput: boolean, embossStrength: number, paperHeight: Float32Array|null): void` — from v3 line 1214: erases by drawing transparent circles on dry canvas and clearing wet layer in area. Uses `globalCompositeOperation = 'destination-out'` for transparent erase.

    Import: `smooth`, `resample` from `./stroke`. Import `clamp` from `../util/math`.

    **brush/water.ts** — Water/smear/blend/blow/wet/dry brush stubs from v3. These tools exist in v3 code but only paint+erase are wired per D-12. Extract ALL functions so they can be enabled later without re-extraction:

    - `applyWaterStroke(rawPts, opts, ...)` — from v3 line 1298
    - `applySmearChunk(pts, opts, ...)` — from v3 line 1331
    - `applyBlendStroke(rawPts, opts, ...)` — from v3 line 1396
    - `applyBlowChunk(pts, opts, ...)` — from v3 line 1456
    - `applyWetChunk(pts, opts, ...)` — from v3 line 1501
    - `applyDryChunk(pts, opts, ...)` — from v3 line 1548
    - `rehydrateArea(cx, cy, radius, ...)` — from v3 line 1134
    - `maybeRehydrate(cx, cy, radius, ...)` — from v3 line 1201
    - `applyLiquifyChunk(pts, radius, strength, ...)` — from v3 line 1100
    - `applyWetComposite(mc, sc, wet)` — from v3 line 1056
    - `applyWetCompositeClipped(mc, sc, wet, bounds)` — from v3 line 1076

    For water.ts: extract the full function bodies. These are complex functions that need all their buffer arguments but they DO work in v3. The only thing that makes them "stubs" is that the engine won't wire them to tool dispatch yet — the code itself is complete.
  </action>
  <verify>
    <automated>cd paint-rebelle-new && pnpm run check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - paint-rebelle-new/src/brush/stroke.ts contains `export function smooth`
    - paint-rebelle-new/src/brush/stroke.ts contains `export function resample`
    - paint-rebelle-new/src/brush/stroke.ts contains `export function ribbon`
    - paint-rebelle-new/src/brush/stroke.ts contains `export function deformN`
    - paint-rebelle-new/src/brush/paint.ts contains `export function renderPaintStroke`
    - paint-rebelle-new/src/brush/paint.ts contains `export function fillPolyGrain`
    - paint-rebelle-new/src/brush/paint.ts contains `export function buildCarriedColors`
    - paint-rebelle-new/src/brush/paint.ts does NOT contain `let ` at module scope (no mutable state)
    - paint-rebelle-new/src/brush/erase.ts contains `export function applyEraseStroke`
    - paint-rebelle-new/src/brush/water.ts contains `export function applyWaterStroke`
    - paint-rebelle-new/src/brush/water.ts contains `export function applyBlowChunk`
    - paint-rebelle-new/src/brush/water.ts contains `export function applyDryChunk`
    - `pnpm run check` exits 0
  </acceptance_criteria>
  <done>All 4 brush modules extracted: stroke.ts (9 functions), paint.ts (8+ functions), erase.ts (1 function), water.ts (11 functions). All functions typed, no module-level mutable state, tsc passes.</done>
</task>

</tasks>

<verification>
1. `cd paint-rebelle-new && pnpm run check` — tsc --noEmit passes
2. `grep -c "export function" paint-rebelle-new/src/core/wet-layer.ts` — should be >= 6
3. `grep -c "export function" paint-rebelle-new/src/core/diffusion.ts` — should be >= 5
4. `grep -c "export function" paint-rebelle-new/src/core/drying.ts` — should be >= 3
5. `grep -c "export function" paint-rebelle-new/src/core/paper.ts` — should be >= 5
6. `grep -c "export function" paint-rebelle-new/src/brush/stroke.ts` — should be >= 8
7. `grep -c "export function" paint-rebelle-new/src/brush/paint.ts` — should be >= 6
8. `grep -rn "let \|var " paint-rebelle-new/src/core/ paint-rebelle-new/src/brush/ | grep -v "function\|=>" | head -5` — should find no module-level lets (only function-internal lets are OK)
9. `grep -rn "document\.\|window\." paint-rebelle-new/src/core/ | grep -v "paper.ts"` — should be empty (only paper.ts may access DOM for image loading)
</verification>

<success_criteria>
- All ~60 physics and brush functions from v3.html extracted to 8 typed modules
- No module-level mutable state in any module
- No DOM access in core/ except paper.ts image loader
- tsc --noEmit passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-library-demo-polish/05-02-SUMMARY.md`
</output>
