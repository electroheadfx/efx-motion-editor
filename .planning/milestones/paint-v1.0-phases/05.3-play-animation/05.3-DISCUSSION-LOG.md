# Phase 05.3: play-animation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-02
**Phase:** 05.3-play-animation
**Areas discussed:** Frame distribution, Playback model, API surface, Demo UI controls

---

## Scope Clarification

Phase had no defined goal. User clarified:

| Option | Description | Selected |
|--------|-------------|----------|
| Stroke replay controls | Enhance existing replayAnimated() with play/pause/speed/scrubber UI | |
| Frame-by-frame animation | Multiple paintings as animation frames for video export | |
| Live painting playback API | Public API for programmatic stroke playback | |
| Something else | User describes | |

**User's choice:** Custom — frame-based animation with configurable frame count via UI and API. Demo app has simple replay button + frame controls. efx-motion-editor uses API for video export.

---

## Frame distribution

### How should strokes be distributed across frames?

| Option | Description | Selected |
|--------|-------------|----------|
| By timestamp ratio | Map stroke timestamps proportionally to frame range | ✓ |
| Even split by count | Divide total strokes evenly across frames | |
| One stroke per frame | Frame count = stroke count automatically | |

**User's choice:** By timestamp ratio
**Notes:** Natural pacing that preserves painting rhythm

### Should each frame show complete strokes or point-by-point drawing?

| Option | Description | Selected |
|--------|-------------|----------|
| Complete strokes | Each frame adds finished strokes | |
| Point-by-point drawing | Strokes animate progressively across frames they span | ✓ |
| You decide | Claude picks | |

**User's choice:** Point-by-point drawing
**Notes:** More cinematic painting-being-made effect

### Should physics animate between frames?

| Option | Description | Selected |
|--------|-------------|----------|
| Stroke drawing only | Physics runs after full stroke completes | |
| Physics per frame | Physics ticks between frames | |
| You decide | Claude picks based on performance | ✓ |

**User's choice:** You decide (Claude's discretion)

---

## Playback model

### Default FPS?

| Option | Description | Selected |
|--------|-------------|----------|
| 24 fps | Film standard | |
| 30 fps | Common for web video | |
| 12 fps | Traditional hand-drawn animation rate | |

**User's choice:** Custom — FPS is a data input (configurable via UI field and API), not a preset default

### Playback controls?

| Option | Description | Selected |
|--------|-------------|----------|
| Play + Stop only | Stop resets to start | |
| Play + Pause + Stop | Mid-complexity | |
| Full transport | Play, pause, stop, scrub, loop, speed | |

**User's choice:** Play + Stop only, but stop resets to END (final paint rendered), not to start

### Should playback loop?

| Option | Description | Selected |
|--------|-------------|----------|
| Play once, stop at end | Shows final painting | ✓ |
| Loop continuously | Clear and restart | |
| Configurable | Default play-once with loop option | |

**User's choice:** Play once, stop at end

---

## API surface

### Callback model for frame data?

| Option | Description | Selected |
|--------|-------------|----------|
| onFrame callback | API emits onFrame(frameIndex, canvas) per frame | ✓ |
| Promise-based getFrame() | Consumer calls getFrame(n) for random access | |
| Both | onFrame + getFrame() | |

**User's choice:** onFrame callback

### API structure?

| Option | Description | Selected |
|--------|-------------|----------|
| Methods on EfxPaintEngine | Add to existing engine class | |
| Separate AnimationPlayer | New class wrapping engine | ✓ |

**User's choice:** Separate AnimationPlayer class

### Export path?

| Option | Description | Selected |
|--------|-------------|----------|
| Main entry | import from '@efxlab/efx-physic-paint' | |
| Sub-path export | import from '@efxlab/efx-physic-paint/animation' | ✓ |
| You decide | | |

**User's choice:** Sub-path export

---

## Demo UI controls

### Where should animation controls live?

| Option | Description | Selected |
|--------|-------------|----------|
| In the toolbar | Alongside existing controls | ✓ |
| Separate panel below canvas | Dedicated bar | |
| Floating overlay | Toggleable panel on canvas | |

**User's choice:** In the toolbar

### Frame counter display?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, show frame counter | Display 'current / total' during playback | ✓ |
| No, keep it minimal | Just play/stop and inputs | |

**User's choice:** Yes, show frame counter

### Disable painting during playback?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, disable painting | Lock canvas during playback | ✓ |
| No, allow painting | Strokes overlay on current frame | |

**User's choice:** Yes, disable painting

---

## Claude's Discretion

- Physics between frames during playback (user deferred this decision)
- Internal frame scheduler implementation
- Handling of existing replayAnimated() method
- tsup sub-path export configuration

## Deferred Ideas

- Video export encoding (efx-motion-editor responsibility)
- Scrubbing/seeking to arbitrary frame
- Loop toggle
- Pause/resume
- Frame-level stroke assignment editing
