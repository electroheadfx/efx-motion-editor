---
phase: 01-algorithm-port-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - efx-paint-physic-v1.html
autonomous: false
requirements:
  - PHYS-01
  - PHYS-02
  - PHYS-03
  - DEMO-01

must_haves:
  truths:
    - "Paint tool deposits color ONLY to wet layer arrays, never directly to canvas"
    - "Wet layer compositing on displayCanvas is the PRIMARY visual output for fresh paint"
    - "Drying is the ONLY mechanism that transfers paint from wet layer to the main canvas"
    - "Flow/diffusion spreads wet paint visibly along paper texture valleys before it dries"
    - "Paper texture grain modulates wet paint visibility (peaks show through, valleys hold paint)"
    - "The main animation loop calls compositeWetLayer each frame"
  artifacts:
    - path: "efx-paint-physic-v1.html"
      provides: "Complete rendering flow redesign with wet-layer-only painting"
      contains: "depositToWetLayer"
  key_links:
    - from: "renderPaintStroke"
      to: "depositToWetLayer"
      via: "wet layer arrays only, no canvas rendering"
      pattern: "depositToWetLayer.*curve"
    - from: "compositeWetLayer"
      to: "displayCanvas"
      via: "requestAnimationFrame loop reading wet arrays"
      pattern: "compositeWetLayer"
    - from: "dryStep"
      to: "canvas X"
      via: "wet-to-canvas transfer is the only canvas write path"
      pattern: "dryStep"
    - from: "flowStep"
      to: "wet layer arrays"
      via: "neighbor diffusion biased by paperHeight"
      pattern: "flowStep"
---

<objective>
Redesign the rendering flow in efx-paint-physic-v1.html so all 5 physics features work together correctly.

Purpose: Currently paint renders both directly to canvas AND deposits to wet layer, creating a broken dual-write. The rendering flow must be: paint -> wet layer ONLY -> compositing displays wet paint -> drying transfers wet to canvas -> flow spreads wet paint. This is the core Phase 1 work.

Output: A working efx-paint-physic-v1.html where wet paint visibly spreads along paper texture, dries over time, and compositing shows the correct result.
</objective>

<execution_context>
@/Users/lmarques/Dev/paint-rebelle-experiment/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/paint-rebelle-experiment/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-algorithm-port-foundation/01-CONTEXT.md
@efx-paint-physic-v1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove direct canvas rendering from paint tool, deposit to wet layer only</name>
  <files>efx-paint-physic-v1.html</files>
  <read_first>
    - efx-paint-physic-v1.html (lines 466-551: renderPaintStroke, renderPaintStrokeSingleColor)
    - efx-paint-physic-v1.html (lines 557-647: depositToWetLayer, depositToWetLayerWithColors)
    - efx-paint-physic-v1.html (lines 652-670: applyWetComposite)
    - efx-paint-physic-v1.html (lines 1071-1079: redrawAll)
    - efx-paint-physic-v1.html (lines 1198-1221: onPointerUp)
    - efx-paint-physic-v1.html (the requestAnimationFrame loop — find the rAF callback that calls compositeWetLayer to confirm it remains wired after the redesign)
  </read_first>
  <action>
Rewrite `renderPaintStroke` and `renderPaintStrokeSingleColor` to ONLY deposit to the wet layer, removing all direct canvas rendering. Per D-06 feature 1: "Paint deposits to wet layer ONLY (not direct canvas rendering)."

Specific changes:

1. **`renderPaintStrokeSingleColor` (lines 530-551):** Remove the entire offscreen canvas rendering pipeline (the `off` canvas, `fillPolyGrain` calls, `fillFlat` calls, `drawBristleTraces`, `applyWetComposite`/`drawImage`). Keep ONLY the curve processing (smooth, resample) and call `depositToWetLayer`. The function should become:
   ```javascript
   function renderPaintStrokeSingleColor(curve, color, radius, opac, wet, speedDeplete) {
     depositToWetLayer(curve, color, radius, opac, wet);
   }
   ```

2. **`renderPaintStroke` (lines 466-528):** Remove the offscreen canvas rendering in the pickup path (lines 505-524: the `off2` canvas, polygon fills, bristle traces, wet composite). Keep the `buildCarriedColors` call and replace the rendering block with `depositToWetLayerWithColors`. The non-pickup path already calls `depositToWetLayer` — just remove the `renderPaintStrokeSingleColor` call before it. The function should become:
   ```javascript
   function renderPaintStroke(rawPts2, color, opts) {
     const {radius, wetness, opacity, pickup} = opts;
     const wet = wetness / 100;
     const pickupAmt = (pickup || 0) / 100;
     const sm = smooth(rawPts2, 3), curve = resample(sm, Math.max(3, radius * 0.25));
     if (curve.length < 3) return;

     const pen = avgPenData(curve);
     const pressureMod = hasPenInput ? 0.4 + pen.p * 0.6 : 1;
     const opac = (opacity / 100) * pressureMod;

     if (pickupAmt < 0.01) {
       depositToWetLayer(curve, color, radius, opac, wet);
       return;
     }

     const canvasSnap = X.getImageData(0, 0, W, H);
     const carriedColors = buildCarriedColors(curve, color, pickupAmt, canvasSnap, radius);
     depositToWetLayerWithColors(curve, carriedColors, radius, opac, wet);
   }
   ```

3. **`onPointerUp` (lines 1198-1221):** Remove the `X.putImageData(snap, 0, 0)` restore before rendering for paint/mix tools (lines 1210, 1214). The snap restore was needed because the preview drew directly on canvas — now preview should also NOT draw on the main canvas. Instead, preview should draw on the displayCanvas or be removed.

4. **`onPointerMove` (lines 1176-1196):** For the paint/mix preview path (lines 1192-1195), remove `X.putImageData(snap, 0, 0)` and `drawPreview(rawPts, ...)`. Instead, draw a lightweight cursor indicator on the displayCanvas or skip preview entirely (paint deposits will show immediately via wet compositing). Replace with:
   ```javascript
   } else if (tool === 'paint' || tool === 'mix') {
     // No canvas preview needed — wet compositing shows paint immediately
     // Optionally: deposit incrementally during drag for immediate feedback
   }
   ```

5. **`redrawAll` (lines 1071-1079):** This replays all actions to the main canvas. For paint strokes, it should deposit to wet layer instead. Update the paint/mix cases to call `renderPaintStroke`/`renderMixStroke` which now only deposit to wet layer:
   ```javascript
   function redrawAll() {
     drawBg();
     clearWetLayer();
     for (const a of allActions) {
       if (a.tool === 'paint') renderPaintStroke(a.pts, a.color, a.opts);
       else if (a.tool === 'smudge') applySmudgeChunk(a.pts, a.opts.radius, a.opts.wetness / 100 * .6 + .1);
       else if (a.tool === 'liquify') applyLiquifyChunk(a.pts, a.opts.radius, a.opts.wetness / 100 * .8 + .1);
       else if (a.tool === 'mix') renderMixStroke(a.pts, a.color, a.opts);
     }
   }
   ```

6. **`renderMixStroke` (lines 732-809):** When `paintStr > 0.01`, it calls `renderPaintStroke` which now deposits to wet layer. The smudge/liquify parts still operate directly on the canvas, which is correct (smudge/liquify manipulate dried paint). No change needed.

7. **Remove `snap` usage for paint tool:** In `onPointerDown` (line 1173), `snap` is still needed for undo. Keep it. But remove its use as a canvas restore mechanism in the paint path.

8. **Keep `fillPolyGrain`, `fillFlat`, `drawBristleTraces`, `strokeEdge`, `deform`, `deformN`, `ribbon`, `polyBounds`, `applyWetComposite` functions** — they are still used by smudge/liquify or may be needed later. Do NOT delete them.

9. **Verify rAF loop wiring:** After all changes, confirm that the requestAnimationFrame callback still calls `compositeWetLayer()` each frame. This is critical — the rAF loop is the ONLY path for wet paint to become visible on the displayCanvas. If the rAF callback does not include `compositeWetLayer()`, add it.
  </action>
  <verify>
    <automated>grep -c "fillPolyGrain\|fillFlat\|drawBristleTraces" efx-paint-physic-v1.html | head -1 && echo "--- Checking renderPaintStroke does not call canvas rendering ---" && grep -A 20 "function renderPaintStroke\b" efx-paint-physic-v1.html | grep -c "fillPolyGrain\|fillFlat\|drawBristleTraces\|applyWetComposite" | xargs -I{} test {} -eq 0 && echo "PASS: renderPaintStroke has no canvas rendering calls" || echo "FAIL: renderPaintStroke still contains canvas rendering calls" && echo "--- Checking renderPaintStrokeSingleColor does not call canvas rendering ---" && grep -A 10 "function renderPaintStrokeSingleColor\b" efx-paint-physic-v1.html | grep -c "fillPolyGrain\|fillFlat\|drawBristleTraces" | xargs -I{} test {} -eq 0 && echo "PASS: renderPaintStrokeSingleColor has no canvas rendering calls" || echo "FAIL: renderPaintStrokeSingleColor still contains canvas rendering calls" && echo "--- Checking compositeWetLayer in rAF loop ---" && grep -n "requestAnimationFrame\|compositeWetLayer" efx-paint-physic-v1.html</automated>
    Functions still exist in the file (count > 0) but are no longer called from renderPaintStroke or renderPaintStrokeSingleColor. The rAF loop must call compositeWetLayer.
    Manual: open efx-paint-physic-v1.html in browser, draw a stroke with paint tool. The main canvas (c) should NOT show the stroke immediately. The displayCanvas overlay SHOULD show the wet paint. Over 5-10 seconds, the stroke should gradually appear on the main canvas via drying.
  </verify>
  <acceptance_criteria>
    - `renderPaintStrokeSingleColor` body contains `depositToWetLayer` and does NOT contain `fillPolyGrain` or `fillFlat` or `drawBristleTraces`
    - `renderPaintStroke` function does NOT contain `off2.getContext` or `applyWetComposite(X`
    - `renderPaintStroke` calls `depositToWetLayer` for non-pickup path and `depositToWetLayerWithColors` for pickup path
    - `onPointerMove` paint/mix branch does NOT contain `X.putImageData(snap`
    - `onPointerUp` paint branch does NOT contain `X.putImageData(snap,0,0)` before the render call
    - `redrawAll` paint case calls `renderPaintStroke` (which now only deposits to wet layer)
    - Functions `fillPolyGrain`, `fillFlat`, `drawBristleTraces`, `ribbon`, `deform`, `deformN` still exist in the file
    - The main animation loop (requestAnimationFrame callback) calls `compositeWetLayer` each frame
  </acceptance_criteria>
  <done>Paint tool deposits ONLY to wet layer arrays. No direct canvas rendering from paint strokes. Wet compositing on displayCanvas shows the wet paint via the rAF loop calling compositeWetLayer each frame. Smudge/liquify still work on the main canvas directly.</done>
</task>

<task type="auto">
  <name>Task 2: Make wet compositing primary display and tune drying + flow physics</name>
  <files>efx-paint-physic-v1.html</files>
  <read_first>
    - efx-paint-physic-v1.html (lines 815-877: dryStep — drying simulation)
    - efx-paint-physic-v1.html (lines 883-964: flowStep — diffusion flow)
    - efx-paint-physic-v1.html (lines 980-1022: compositeWetLayer — wet display overlay)
    - efx-paint-physic-v1.html (lines 966-973: physicsStep and setInterval)
    - efx-paint-physic-v1.html (lines 108-116: wet layer array declarations)
  </read_first>
  <action>
Per D-06 features 2-5: wet compositing is PRIMARY display, drying transfers to canvas, flow spreads paint, paper texture modulates.

1. **Tune `compositeWetLayer` (lines 980-1022) for primary display:** Currently the wet overlay alpha is `(wetAlpha[i] / 3000) * 255` with paper modulation. This is correct but needs adjustment now that wet compositing is the ONLY way fresh paint is seen:
   - Increase alpha mapping: change `(wetAlpha[i] / 3000) * 255` to `Math.min(255, (wetAlpha[i] / 2000) * 255)` so paint is more visible at lower wetAlpha values (since we removed direct canvas rendering, the wet display must compensate)
   - Paper grain modulation is good, keep it

2. **Tune `dryStep` (lines 815-877) for slower, more visible drying:** Currently `DRY_RATE = 8` and `WETNESS_DECAY = 12`. Since drying is now the ONLY path to the main canvas, slow it down for visible progression:
   - Change `DRY_RATE` from `8` to `3` — slower alpha transfer per tick
   - Change `WETNESS_DECAY` from `12` to `5` — slower wetness loss
   - Change `DRY_ALPHA_THRESHOLD` from `2` to `1` — clear earlier
   - This gives ~10-15 second visible drying for a typical stroke

3. **Tune `flowStep` (lines 883-964) for stronger visible capillary spreading:**
   - Change `FLOW_THRESHOLD` from `30` to `15` — trigger flow at lower wetness
   - Change `FLOW_FRACTION` from `0.012` to `0.025` — more paint moves per tick
   - Change `GRAVITY_BIAS` from `0.003` to `0.005` — slightly more downward pull
   - In the neighbor weight computation (line 919), increase the base flow: change `Math.max(0, h0 - nh + 0.01)` to `Math.max(0, h0 - nh + 0.02)` — more base flow even on flat areas
   - These changes make capillary spreading visually obvious

4. **Ensure physics interval is correct (line 973):** Keep `setInterval(physicsStep, 100)` (10fps) — this is correct per CONTEXT.md established patterns.

5. **Adjust `depositToWetLayer` deposit strength (lines 557-601):** Since we removed direct canvas rendering, the deposit strength needs tuning:
   - Change `depositStrength` from `opacity * 0.6` to `opacity * 1.2` — double deposit since there's no canvas rendering to supplement visibility
   - Change `depositAlpha` scale from `120` to `200` — `depositStrength * ff * 200` — more alpha deposited per step
   - Change `waterDeposit` from `waterSlider * 800` to `waterSlider * 1200` — more wetness deposited so flow has more to work with
   - Apply the same changes to `depositToWetLayerWithColors` (lines 604-647): change `depositStrength = opacity * 0.6` to `opacity * 1.2`, `depositAlpha` scale `120` to `200`, `waterDeposit` from `waterSlider * 800` to `waterSlider * 1200`

6. **Paper texture modulation in dryStep (lines 833-839):** The existing modulation `paperMod = clamp(1.2 - ph * 0.5, 0.4, 1.2)` is reasonable. Increase the effect slightly:
   - Change to `paperMod = clamp(1.4 - ph * 0.8, 0.3, 1.4)` — valleys hold more, peaks dry faster, creating more visible paper texture grain in dried strokes
  </action>
  <verify>
    <automated>grep -n 'DRY_RATE = 3\|WETNESS_DECAY = 5\|FLOW_THRESHOLD = 15\|FLOW_FRACTION = 0.025\|GRAVITY_BIAS = 0.005\|depositStrength = opacity \* 1.2\|waterSlider \* 1200' efx-paint-physic-v1.html</automated>
    Verify constants are updated to exact new values. Each grep pattern must match at least once. Manual: open in browser, draw a stroke, observe: (a) paint appears immediately as wet overlay, (b) over 10-15 seconds paint gradually appears on main canvas, (c) wet paint visibly spreads sideways/downward along paper texture grain.
  </verify>
  <acceptance_criteria>
    - `DRY_RATE` is set to `3` (not 8)
    - `WETNESS_DECAY` is set to `5` (not 12)
    - `DRY_ALPHA_THRESHOLD` is set to `1` (not 2)
    - `FLOW_THRESHOLD` is set to `15` (not 30)
    - `FLOW_FRACTION` is set to `0.025` (not 0.012)
    - `GRAVITY_BIAS` is set to `0.005` (not 0.003)
    - `depositToWetLayer` has `depositStrength = opacity * 1.2` (not 0.6)
    - `depositToWetLayer` has `depositStrength * ff * 200` (not 120)
    - `depositToWetLayerWithColors` has `depositStrength = opacity * 1.2` (not 0.6)
    - `compositeWetLayer` alpha mapping uses `/ 2000` (not / 3000)
    - `paperMod` in dryStep uses `1.4 - ph * 0.8` (not 1.2 - ph * 0.5)
    - `flowStep` neighbor weight uses `h0 - nh + 0.02` (not + 0.01)
  </acceptance_criteria>
  <done>Wet compositing is the primary visual output for fresh paint. Drying takes 10-15 seconds for visible progression. Flow/diffusion creates visible capillary spreading along paper texture. Paper texture grain modulates both wet display and drying rate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all 5 physics features working together</name>
  <files>efx-paint-physic-v1.html</files>
  <action>
Human verification of the complete rendering flow redesign. All 5 physics features from D-06 must be visually confirmed working together in the browser.
  </action>
  <what-built>Complete rendering flow redesign: paint deposits to wet layer only, wet compositing is primary display, drying transfers to canvas over 10-15 seconds, flow/diffusion spreads paint along paper texture valleys.</what-built>
  <how-to-verify>
    1. Open `efx-paint-physic-v1.html` in a browser
    2. Select the "paperTex" background (last swatch in Bg row) to see paper texture
    3. Draw a stroke with the Paint tool
    4. Verify: stroke appears IMMEDIATELY as a semi-transparent wet overlay (on the upper display canvas)
    5. Watch for 10-15 seconds: wet paint should gradually fade from the overlay as it transfers (dries) onto the main canvas below
    6. Look at edges of the stroke: wet paint should spread slightly sideways, biased toward paper texture valleys (lower areas appear darker in texture)
    7. Draw two strokes close together with high wetness (slider ~70+): flow should cause them to bleed into each other slightly
    8. Switch to transparent background: dried paint should be visible with transparency (not opaque blocks)
    9. Test undo (Undo button): should undo the last action
    10. Test clear (Clear button): should clear everything including wet paint
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify — manual browser testing required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms wet paint appears immediately as overlay
    - User confirms drying takes visible time (10-15 seconds)
    - User confirms flow/diffusion spreading is visible at stroke edges
    - User confirms paper texture grain visible through wet paint
    - User confirms all 4 tools still function
  </acceptance_criteria>
  <done>User approves the 5 physics features as visually working together.</done>
  <resume-signal>Type "approved" if the 5 physics features are visually working, or describe what needs adjustment (e.g., "drying too fast", "flow not visible", "paint not appearing")</resume-signal>
</task>

</tasks>

<verification>
All 5 physics features from D-06 verified together:
1. Paint deposits to wet layer ONLY: `renderPaintStroke` does not contain canvas rendering calls
2. Wet compositing is PRIMARY display: wet paint visible immediately on displayCanvas
3. Drying transfers to canvas: paint appears on main canvas gradually over 10-15 seconds
4. Flow spreads wet paint: visible capillary spreading at stroke edges
5. Paper texture modulates: grain visible in both wet overlay and dried paint
</verification>

<success_criteria>
- Fresh paint strokes appear immediately as wet overlay (not on main canvas)
- Over 10-15 seconds, wet paint gradually transfers to the main canvas via drying
- Wet paint visibly spreads along paper texture valleys (capillary action)
- Paper texture grain is visible through wet paint
- All 4 tools (paint, smudge, liquify, mix) still function
- Undo and clear work correctly
- No JavaScript console errors
- The rAF loop calls compositeWetLayer each frame (verified by grep)
</success_criteria>

<output>
After completion, create `.planning/phases/01-algorithm-port-foundation/01-02-SUMMARY.md`
</output>
