---
phase: 05.3-play-animation
plan: 02
subsystem: animation-ui
tags: [animation, toolbar, sub-path-export, tsup, preact]

# Dependency graph
requires:
  - phase: 05.3-play-animation
    plan: 01
    provides: AnimationPlayer class, AnimationConfig/AnimationState types
provides:
  - Animation controls in demo Toolbar (frame count, FPS, play/stop, frame counter)
  - Sub-path export '@efxlab/efx-physic-paint/animation'
  - AnimationPlayer lifecycle wiring in App.tsx
affects: [efx-motion-editor-integration]

# Tech tracking
tech-stack:
  added: []
  patterns: [tsup multi-entry with sub-path exports, preact useRef for class instance lifecycle]

key-files:
  created:
    - paint-rebelle-new/src/animation/index.ts
  modified:
    - paint-rebelle-new/tsup.config.ts
    - paint-rebelle-new/package.json
    - paint-rebelle-new/src/demo/Toolbar.tsx
    - paint-rebelle-new/src/demo/App.tsx
    - paint-rebelle-new/src/engine/EfxPaintEngine.ts

key-decisions:
  - "clearRect before drawImage in renderPartialStrokes -- transparent bg doesn't erase existing dry canvas pixels"
  - "Frame counter is 1-indexed in UI (animFrame + 1) while AnimationPlayer uses 0-indexed internally"
  - "AnimationPlayer created in useEffect keyed on engine ref, stored in useRef"
---

## Self-Check: PASSED

## What was built
Wired AnimationPlayer into the demo app with full toolbar UI controls (frame count input, FPS input, play/stop toggle button, live frame counter). Configured the `@efxlab/efx-physic-paint/animation` sub-path export via tsup multi-entry build and package.json exports map.

## Tasks completed
1. **Sub-path export barrel + build config** -- Created `src/animation/index.ts` barrel exporting AnimationPlayer, AnimationConfig, AnimationState. Added `animation` entry to tsup.config.ts. Added `./animation` sub-path to package.json exports.
2. **Toolbar animation controls + App wiring** -- Added Animation section to Toolbar with Frames (10-600, default 120), FPS (1-60, default 24), Play/Stop button, and live frame counter. Wired AnimationPlayer lifecycle in App.tsx via useRef + useEffect.
3. **Visual verification** -- Confirmed animation playback works on all backgrounds including transparent. Fixed clearRect bug for transparent bg in renderPartialStrokes.

## Deviations
- Added `clearRect` call before `drawImage` in `renderPartialStrokes` to fix transparent background rendering (transparent bg drawImage doesn't erase existing pixels on dry canvas)

## Issues
None remaining.
