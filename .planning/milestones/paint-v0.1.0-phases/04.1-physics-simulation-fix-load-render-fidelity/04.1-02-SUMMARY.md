---
phase: 04.1-physics-simulation-fix-load-render-fidelity
plan: "02"
subsystem: physics
tags: [diffusion, replay, serialization, blow-tool, displacement-bias, deterministic-replay]
status: checkpoint

# Dependency graph
requires:
  - phase: 04.1-physics-simulation-fix-load-render-fidelity
    plan: "01"
    provides: FBM-displaced darken-flood diffusion (diffuseStep), precomputed displacement map, bilinear sampling, ping-pong buffers
provides:
  - Diffusion frame counter for deterministic replay (diffusionFramesSinceLastStroke)
  - Updated serialization format v2 with per-stroke diffusionFrames
  - Frame-counted replay with v1 timestamp fallback (backward compatible)
  - Directional blow tool with displacement bias (blowDX/blowDY)
  - loadProject() uses replayWithPhysics() for organic bloom on load
affects: [visual-verification, tool-testing, save-load-fidelity]

# Tech tracking
tech-stack:
  added: []
  patterns: [diffusion-frame-counting, directional-displacement-bias, per-stroke-frame-serialization]

key-files:
  created: []
  modified: [efx-paint-physic-v3.html]

key-decisions:
  - "Diffusion frame counter (not timestamps) as primary replay timing: deterministic frame counts recorded per-stroke for exact reproduction of inter-stroke diffusion"
  - "Backward compatible v1 fallback: replay uses timestamps when diffusionFrames not present in loaded v1 files"
  - "Blow displacement as additive bias on FBM displacement: blowDX/blowDY added to dispPxX/dispPxY in diffuseStep for directional push"
  - "BLOW_DECAY=0.92 for ~0.8s half-life: 8% per physics tick creates temporary directional effect"
  - "BLOW_STRENGTH=8.0: displacement pixels at full blow strength"
  - "Serialization version bumped to 2 to distinguish format with diffusionFrames"

patterns-established:
  - "Per-stroke metadata recording: allActions entries carry diffusionFrames for physics state between strokes"
  - "Additive displacement bias: tool effects layer onto precomputed displacement map rather than replacing it"
  - "Decaying per-pixel arrays: blow displacement decays each physics tick with small-value zeroing for performance"

requirements-completed: [STROKE-03, DEMO-04]

# Metrics
duration: 3min
completed: 2026-03-31
---

# Phase 04.1 Plan 02: Replay Fidelity & Blow Tool Summary

**Deterministic diffusion frame counting for save/load replay fidelity with directional blow tool displacement bias -- pending human visual verification**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-31T07:44:59Z
- **Tasks completed:** 2 of 3 (checkpoint at Task 3)
- **Files modified:** 1

## Accomplishments
- Added diffusion frame counter tracking physics ticks between strokes for deterministic replay (D-06, D-07)
- Updated serialization to v2 format with per-stroke diffusionFrames; backward compatible with v1 timestamp-based files
- Rewrote replayWithPhysics() to use frame-counted diffusion passes with trailing diffusion for last stroke
- Changed loadProject() to use replayWithPhysics() instead of redrawAll() -- loaded paintings now get organic watercolor bloom
- Added directional blow tool with per-pixel displacement bias (blowDX/blowDY) that decays 8% per physics tick (D-08, D-09)
- Blow displacement integrates additively with FBM displacement in diffuseStep for directional paint push

## Task Commits

Each task was committed atomically:

1. **Task 1: Add diffusion frame counter, update serialization and replay for deterministic diffusion** - `5d06680` (feat)
2. **Task 2: Adapt blow tool for directional displacement bias in diffusion** - `6ae741d` (feat)
3. **Task 3: Verify diffusion physics, save/load fidelity, and blow tool** - PENDING (checkpoint:human-verify)

## Files Created/Modified
- `efx-paint-physic-v3.html` - Added diffusion frame counter, updated serialization to v2, rewrote replayWithPhysics for frame-counted diffusion, added blow displacement bias arrays and constants, integrated blow into diffuseStep/physicsStep

## Decisions Made
- Diffusion frame counter as primary replay mechanism (not timestamps) for deterministic reproduction
- v1 timestamp fallback for backward compatibility with pre-v2 saved files
- Blow displacement as additive bias on FBM displacement map (not replacement)
- BLOW_DECAY=0.92 (~0.8s half-life), BLOW_STRENGTH=8.0 displacement pixels
- Serialization version bumped to 2

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## Known Stubs
None - all systems are fully wired. Diffusion frame counter increments in physicsStep, records in stroke data, serializes to JSON, and replays via frame-counted physics passes. Blow displacement writes in applyBlowChunk, integrates in diffuseStep, and decays in physicsStep.

## User Setup Required
None - no external service configuration required.

## Checkpoint: Awaiting Human Visual Verification

Task 3 requires human verification of:
1. Diffusion produces visible watercolor bloom
2. Save/load round-trip produces paintings with organic spreading (not flat stamps)
3. Blow tool pushes paint directionally
4. All existing tools still function
5. No console errors

## Next Phase Readiness
- Pending visual verification checkpoint (Task 3)
- If approved: Phase 04.1 complete with deterministic replay, directional blow, and organic save/load
- All 8 brush types preserved and blow tool enhanced with directional displacement

## Self-Check: PASSED

- efx-paint-physic-v3.html: FOUND
- 04.1-02-SUMMARY.md: FOUND
- 5d06680 (Task 1 commit): FOUND
- 6ae741d (Task 2 commit): FOUND

---
*Phase: 04.1-physics-simulation-fix-load-render-fidelity*
*Status: checkpoint (Task 3 pending)*
