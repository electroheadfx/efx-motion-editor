---
phase: 36.9-physics-paint-roto-cached-playback-auto-play
plan: 02
type: tdd
wave: 2
depends_on:
  - 36.9-01
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 36.9-CACHED-PLAYBACK
user_setup: []
must_haves:
  truths:
    - "Play advances over already-cached Roto frames at preview timing after the user confirms timed preview is needed."
    - "Each playback tick displays cached frame URLs through the existing cached overlay path."
    - "Playback is preview-only and does not rewrite, regenerate, remove, apply, or mutate cached frames."
    - "Stop clears active playback state and leaves manual Roto navigation coherent."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Hardened cached Roto playback controller and overlay wiring."
      exports: []
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "TDD source-contract tests for cached playback no-mutation and Stop/navigation behavior."
      exports: []
  key_links:
    - from: "PhysicsPaintWorkflowStrip.onToggleRotoPlayback"
      to: "PhysicsPaintStudio.toggleRotoCachedPlayback"
      via: "existing workflow-strip prop wiring"
      pattern: "onToggleRotoPlayback={toggleRotoCachedPlayback}"
    - from: "PhysicsPaintStudio.toggleRotoCachedPlayback"
      to: "PhysicsPaintCanvasStack.cachedRotoPlaybackUrl"
      via: "cachedRotoPlaybackFrame?.dataUrl overlay"
      pattern: "cachedRotoPlaybackUrl={cachedRotoPlaybackFrame?.dataUrl ?? null}"
---

<objective>
Use TDD to lock and harden the cached Roto playback controller before exposing visible Play/Stop controls.

Purpose: Timed preview must reuse the existing cached-frame display path and remain preview-only.
Output: RED/GREEN/REFACTOR commits proving cached Roto playback advances cached overlays, stops coherently, and does not mutate cache or render state.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** press Play to automatically advance through cached Roto frames inside Physics Paint, **so that** I can preview timing without manually pressing previous and next.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-RESEARCH.md
@.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-PATTERNS.md
@.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-VALIDATION.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-03-SUMMARY.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-05-SUMMARY.md
@SPECS/36.x-phases/phase-36.9-cached-autoplay/spec-36.9-cached-autoplay.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
</context>

## Artifacts this phase produces

- Test name in `PhysicsPaintStudio.test.ts`: `previews cached Roto playback from cached frames only and avoids mutation paths in the Roto branch`.
- Test name in `PhysicsPaintStudio.test.ts`: `stops cached Roto playback on edit intent manual Roto navigation and mode changes`.
- Hardened `toggleRotoCachedPlayback` behavior in `PhysicsPaintStudio.tsx`.
- Hardened `stopRotoCachedPlayback` behavior in `PhysicsPaintStudio.tsx`.
- Preserved `getRotoCachedPlaybackFrames`, `findCachedRotoPlaybackFrame`, `cachedRotoPlaybackFrame`, `rotoCachedPlaybackTimerRef`, and `cachedRotoPlaybackUrl` wiring.

<feature>
  <name>Cached Roto playback controller</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    @.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-PATTERNS.md
    @.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-RESEARCH.md
    @SPECS/36.x-phases/phase-36.9-cached-autoplay/spec-36.9-cached-autoplay.md
  </read_first>
  <behavior>
    - RED case: source contract finds the cached Roto playback block and asserts it reads frames through `getRotoCachedPlaybackFrames`, sets `cachedRotoPlaybackFrame`, and routes overlay output through `cachedRotoPlaybackUrl`.
    - RED case: source contract asserts the Roto cached playback branch does not use `AnimationPlayer`, `flushRotoFrame`, `sendPhysicPaintApplyPayload`, `regenerateRotoInterpolationCache`, `removeFrameRange`, `removeRealRotoKeyFrame`, `upsertCachedRotoFrameInLaunchContext`, or `removeCachedRotoFrameFromLaunchContext`.
    - RED case: source contract asserts manual previous/next remains routed through `requestRotoFrameNavigation`, and navigation/edit intent/mode changes call `stopRotoCachedPlayback` and clear `cachedRotoPlaybackFrame`.
    - GREEN case: implementation passes these source contracts while keeping playback preview-only and preserving the existing cached reference lookup path.
  </behavior>
  <implementation>Extend `PhysicsPaintStudio.test.ts` first, using the existing read-file source-contract style. Then update `PhysicsPaintStudio.tsx` only where needed to satisfy the tests: keep `toggleRotoCachedPlayback`, `stopRotoCachedPlayback`, `rotoCachedPlaybackTimerRef`, `getRotoCachedPlaybackFrames`, and `PhysicsPaintCanvasStack` overlay wiring as the public implementation shape; add cleanup or dependency fixes only if the RED tests reveal a gap. Do not introduce XState, broad new useEffect orchestration, new render/export paths, cache mutation helpers, or package dependencies.</implementation>
  <acceptance_criteria>
    - RED commit exists before implementation with failing assertions for the cached playback behavior.
    - GREEN commit passes focused Studio tests.
    - The cached playback block contains no cache apply/regenerate/remove/upsert calls and no `AnimationPlayer` usage.
    - Manual previous/next navigation remains routed through `requestRotoFrameNavigation`.
    - Stop, edit intent, mode changes, and unmount clear interval state and cached playback overlay state.
  </acceptance_criteria>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add cached playback controller source contracts</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-PATTERNS.md
    @SPECS/36.x-phases/phase-36.9-cached-autoplay/spec-36.9-cached-autoplay.md
  </read_first>
  <behavior>
    - Test cached Roto playback uses cached frame lookup and overlay state.
    - Test playback branch avoids rendering/apply/regenerate/cache mutation calls.
    - Test manual previous/next navigation and edit intent stop cached playback.
  </behavior>
  <action>Add or tighten `PhysicsPaintStudio.test.ts` source-contract tests for `toggleRotoCachedPlayback`, `stopRotoCachedPlayback`, manual navigation, and `PhysicsPaintCanvasStack` overlay wiring. The RED test must fail for a real missing 36.9 contract, not for syntax/import errors. Use existing helper style from the file; do not add runtime component-test infrastructure. Commit with `test(36.9-02): add cached Roto playback controller contract`.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - Focused command fails in RED because the source contract catches a missing/hardened 36.9 behavior.
    - The test file names the preview-only branch and no-mutation boundary explicitly.
    - No application implementation is changed in this task.
  </acceptance_criteria>
  <done>RED tests are committed and fail for the intended cached playback contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN/REFACTOR — harden cached playback behavior without cache mutation</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    @app/src/components/physic-paint/PhysicsPaintStudio.tsx
    @app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    @.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-PATTERNS.md
    @.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-03-SUMMARY.md
  </read_first>
  <behavior>
    - Pressing/toggling Play sets active playback and cached overlay state from existing cached frames.
    - Pressing/toggling Stop clears the interval and cached overlay state.
    - Playback ending clears active state and interval state.
    - Manual navigation/edit intent remains unchanged except that it stops active cached playback first.
  </behavior>
  <action>Update `PhysicsPaintStudio.tsx` only as needed to make the RED tests pass. Preserve existing Preact hook patterns and Phase 36.8 session boundary ownership: Studio remains the adapter for timers and overlays, while the session boundary stays focused on Roto state/key decisions. Ensure Roto cached playback does not call apply, regenerate, remove, upsert, bridge send, or physics animation player paths. Run focused tests and typecheck. Commit with `feat(36.9-02): harden cached Roto playback controller`; if cleanup is needed after GREEN, commit `refactor(36.9-02): stabilize cached Roto playback contracts`.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts -x</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Focused Studio tests pass.
    - App typecheck passes.
    - No `AnimationPlayer` or cache mutation helper is introduced into the cached Roto playback branch.
    - `cachedRotoPlaybackUrl={cachedRotoPlaybackFrame?.dataUrl ?? null}` remains wired into `PhysicsPaintCanvasStack`.
  </acceptance_criteria>
  <done>GREEN implementation is committed, optional refactor remains green, and cached playback is preview-only by source contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow strip to Studio controller | UI event triggers local preview timer and cached overlay state. |
| Studio controller to cache/store | Playback reads cached frames but must not write or regenerate cache data. |
| browser timer to UI state | Interval callback updates local playback overlay state over time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.9-02 | Tampering | `toggleRotoCachedPlayback` | high | mitigate | Source-contract tests assert the Roto cached playback branch does not call apply, regenerate, remove, upsert, bridge send, or `AnimationPlayer` paths. |
| T-36.9-03 | Denial of Service | `rotoCachedPlaybackTimerRef` | medium | mitigate | Preserve interval cleanup in Stop, sequence end, mode switch/edit intent, and unmount cleanup; source tests assert stop paths. |
| T-36.9-04 | Repudiation | manual previous/next navigation | medium | mitigate | Preserve source-contract tests proving navigation still routes through `requestRotoFrameNavigation` and not the playback overlay branch. |
| T-36.9-SC | Tampering | package installs | low | accept | No package-manager install tasks exist in this plan; package legitimacy audit in research records no installs. |
</threat_model>

<verification>
Run focused Studio tests and app typecheck. Do not start the dev server.
</verification>

<success_criteria>
- TDD RED/GREEN gate sequence is visible in commits.
- Cached Roto playback uses existing cached display and overlay paths.
- Playback cannot mutate cache by source contract.
- Manual previous/next cached-frame navigation remains unchanged by source contract.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Press Play automatically advances cached Roto frames | 02 | COVERED | Controller timer and overlay path are hardened here; visible control is Plan 03. |
| REQ | 36.9-CACHED-PLAYBACK | Play advances through cached frames; Stop halts coherently; no cache mutation | 02 | COVERED | Main requirement for this TDD plan. |
| RESEARCH | Existing cached display path | Use `findCachedRotoReferenceFrame`, `findCachedRotoPlaybackFrame`, and overlay state | 02 | COVERED | Plan explicitly preserves these identifiers. |
| RESEARCH | Avoid AnimationPlayer/render/apply/cache mutation | Playback is preview-only | 02 | COVERED | Negative source contracts required. |
| RESEARCH | Manual navigation remains authoritative | Previous/next continues through `requestRotoFrameNavigation` | 02 | COVERED | Source-contract test required. |
| CONTEXT | — | No CONTEXT.md exists | — | N/A | User chose to continue without discuss-phase context. |

<output>
Create `.planning/phases/36.9-physics-paint-roto-cached-playback-auto-play/36.9-02-SUMMARY.md` when done.
</output>
