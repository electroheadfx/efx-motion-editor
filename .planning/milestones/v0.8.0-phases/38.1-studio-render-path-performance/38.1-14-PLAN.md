---
phase: 38.1-studio-render-path-performance
plan: 14
type: tdd
wave: 9
depends_on:
  - 38.1-12
files_modified:
  - packages/efx-physic-paint/src/types.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts
autonomous: true
gap_closure: true
requirements:
  - 38.1-D-09
  - R-D-08
must_haves:
  truths:
    - "Stopping a `last` physics session finalizes only that session's affected wet pixels and leaves unrelated saved wet pixels unchanged (38.1-D-09, R-D-08)."
    - "The completed physics mode, not the restored prior mode, is recorded and used by deterministic replay."
    - "The prior engine physics mode is restored after completed-session finalization."
  artifacts:
    - path: packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
      provides: "Correct stopPhysics ordering and completed-mode replay propagation"
    - path: packages/efx-physic-paint/src/types.ts
      provides: "Recorded/serialized physics-session mode contract"
    - path: packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts
      provides: "Wet-buffer, restoration, and replay-metadata regression coverage"
  key_links:
    - from: packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
      to: packages/efx-physic-paint/src/types.ts
      via: "PaintStroke and SerializedProject physicsMode"
      pattern: "physicsMode"
    - from: packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
      to: packages/efx-physic-paint/src/core/diffusion.ts
      via: "replayDiffusion forwards the recorded completed mode"
      pattern: "replayDiffusion"
---

<objective>
Close the second behavior-preservation blocker by making `stopPhysics()` finalize and record the mode that actually completed (38.1-D-09, R-D-08).

Purpose: Last-stroke physics must not overwrite unrelated wet state, and deterministic replay must describe the completed session accurately.
Output: Correct engine ordering, a complete recorded-mode contract, and partial-engine behavior tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md
@.planning/phases/38.1-studio-render-path-performance/38.1-REVIEW.md
@packages/efx-physic-paint/src/types.ts
@packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
@packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts
@packages/efx-physic-paint/src/core/diffusion.ts
</context>

## Artifacts this phase produces

- Production source: corrected engine finalization/replay behavior in `EfxPaintEngine.ts`; widened recorded-session mode types in `types.ts`.
- Tests: expanded cooperative-finalization behavior suite.
- Documentation: none.
- Configuration: none.
- Dependencies: none.
- Persisted data or migrations: none; the existing version-2 project field accepts the completed engine modes without a compatibility layer.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve completed physics mode through finalization and replay</name>
  <reversibility rating="costly">Recorded mode semantics flow through in-memory history, replay, and project serialization, so reverting requires a coordinated engine/type change.</reversibility>
  <read_first>
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:613-750 — start/stop ordering and current replay action creation
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:1075-1138 — public recorded-stroke and partial replay surface
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:1946-1977 — redraw/replay mode propagation
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:1984-2042 — serialized mode coercion that must preserve completed modes
    - packages/efx-physic-paint/src/types.ts:167-205 — `PaintStroke` and `SerializedProject` contracts
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts:1-160 — established partial-engine harness
    - .planning/phases/38.1-studio-render-path-performance/38.1-VERIFICATION.md:71-86 — blocker acceptance contract
  </read_first>
  <files>packages/efx-physic-paint/src/types.ts, packages/efx-physic-paint/src/engine/EfxPaintEngine.ts, packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts</files>
  <behavior>
    - RED 1: after `startPhysics("last")` and a completed tick, `stopPhysics()` updates masked/currently wet pixels while preserving an unrelated sentinel pixel in `savedWet`.
    - RED 2: the resulting zero-point diffusion action returned by `getStrokes()` records `physicsMode: "last"` and the completed tick count.
    - RED 3: a prior mode such as `local` is restored only after finalization, so the public engine state ends in the prior mode without affecting the completed-mode decisions.
    - RED 4: redraw/serialization round trips preserve and replay `last` (and the same typed path remains valid for `local`, `all`, and null) instead of coercing the completed mode away.
  </behavior>
  <action>
    RED: Extend the existing partial-engine harness with distinct wet/savedWet sentinel values, a last-stroke mask, a nonzero physics tick count, and a saved prior mode. Assert pixels, `getStrokes()` metadata, final restored state, and replay/serialization behavior through public or already-established harness surfaces. Confirm the current implementation fails because it restores first and records the restored value.

    GREEN: Capture `completedMode` at the start of `stopPhysics()` after the running guard. Stop the interval, finalize wet buffers using `completedMode`, dry the live layer, and append replay metadata using `completedMode`; restore `savedPhysicsMode` only after completed-session finalization and action recording. Broaden the recorded and serialized physics-mode field to the existing engine mode union required to represent the completed session, and thread that recorded mode through diffusion replay rather than relying on the engine's ambient restored state. Preserve local-stroke finalization behavior, history notification order, mutation publication, interval teardown, and the clean project-format break policy; do not add a legacy migration branch. This implements 38.1-D-09 and R-D-08.

    REFACTOR: If repeated mode unions remain, introduce one internal exported type alias in `types.ts` and use it consistently without changing public method signatures. Run the focused engine suite after each cleanup.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run --root ../packages/efx-physic-paint src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A completed `last` session leaves the unrelated `savedWet` sentinel unchanged.
    - The continuation action records the completed mode `last`, not the prior/restored mode.
    - The prior engine mode is restored after finalization.
    - Redraw and version-2 serialization retain the completed mode needed for deterministic replay.
    - Existing local physics, history, pointer-input, and cooperative-finalization tests remain green.
  </acceptance_criteria>
  <done>`stopPhysics()` uses one captured completed mode for wet synchronization and replay metadata, then restores the prior mode (38.1-D-09, R-D-08).</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

This plan introduces no authentication, session, access-control, network, or new untrusted-input surface. ASVS L1 integrity and availability controls apply to simulation-buffer finalization, recorded replay metadata, and physics interval teardown already mitigated by the STRIDE register below.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable simulation buffers → saved project/history state | Wet pixel arrays and mode metadata become durable replay inputs. |
| Recorded actions → deterministic replay | Stored mode controls later diffusion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-38.1-14-01 | Tampering | `stopPhysics()` wet synchronization | high | mitigate | Capture one completed mode and test unrelated-pixel preservation with independent sentinel values. |
| T-38.1-14-02 | Tampering | replay metadata and serialization | high | mitigate | Preserve the completed mode through `PaintStroke`, serialization, and diffusion replay tests. |
| T-38.1-14-03 | Denial of Service | physics interval teardown | medium | mitigate | Retain immediate interval cancellation and existing non-running guard while reordering only finalization/restoration. |
| T-38.1-14-SC | Tampering | package supply chain | low | accept | No dependency or package-manager change is planned. |
</threat_model>

<verification>
- Focused engine behavior suites pass with `vitest run`.
- `pnpm -C packages/efx-physic-paint check` passes.
- No server, browser, or native runtime is launched.
</verification>

<success_criteria>
- Wet-buffer finalization is selected by the completed session mode.
- Replay metadata accurately records `last` and remains serializable/replayable.
- Prior-mode restoration happens after finalization without changing external start/stop APIs.
- TDD history contains RED then GREEN commits, with optional REFACTOR after green.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-14-SUMMARY.md` when done.
</output>
