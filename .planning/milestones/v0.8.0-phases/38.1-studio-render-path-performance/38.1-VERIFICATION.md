---
phase: 38.1-studio-render-path-performance
verified: 2026-07-29T12:34:56Z
status: passed
score: 20/20 decision contracts verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 18/20
  gaps_closed:
    - "Cached playback now preserves transparency and independently applies background texture, paper grain, and grain strength through the canonical compositor."
    - "stopPhysics() now finalizes and records the completed physics mode before restoring the prior mode."
    - "Capacity-only physical timeline replacements now clamp, rebuild, publish, and notify instead of returning as no-ops."
  warnings_closed:
    - "Optional profiling now fails closed when localStorage property access or getItem() throws."
    - "CanvasMount callback/effect/resource behavior now has executable Node coverage, with real Preact reconciliation accepted in the user-owned native runtime."
  gaps_remaining: []
  regressions: []
---

# Phase 38.1: Studio Render-Path Performance Verification Report

**Phase Goal:** Preserve the approved canvas-first timing architecture from Plans 01–07 and complete the reopened localized-render acceptance track: deterministic render/observer/lifecycle counters, static Studio and Workflow localization, persistent CanvasMount/Efx isolation, and renewed user-owned native proof before unblocking Phase 38-06.

**Verified:** 2026-07-29T12:34:56Z  
**Status:** PASSED  
**Re-verification:** Yes — after gap-closure Plans 13–18

## Final Verdict

Phase 38.1 achieves its goal. The three former behavior-preservation blockers are closed in live production code and exercised by focused behavioral tests. Both former warnings are closed: profiling storage access is fail-closed, and CanvasMount now has executable callback/effect/resource coverage supplemented by authoritative user-owned native proof of actual Preact reconciliation and Efx instance ownership.

The user-owned Plan 18 evidence is accepted as authoritative: sections A–E passed; the complete grouped threshold matrix passed in both forward and reverse directions; raw delta JSON was explicitly optional and has not been invented. The rigid Roto group-drag rejection found during native acceptance was corrected in commit `25c7cf9e` and passed native re-UAT without reopening or modifying the completed debug artifact during this verification.

Phase 38 Plan 38-06 is eligible to resume.

## Goal Achievement

### Observable Decision Contracts

| # | Contract | Status | Evidence |
|---:|---|---|---|
| 1 | `38.1-D-01` Playback freeze and one catch-up | VERIFIED | Quick regression check: the previously approved per-tick Signal/catch-up architecture remains present; Plan 18 native sections and final gate reported no playback regression. |
| 2 | `38.1-D-02` Missing-frame event queue | VERIFIED | Previously verified queue/flush behavior remains in the live playback path; Plan 18 accepted affected playback behavior. |
| 3 | `38.1-D-03` Canvas-first paint ordering | VERIFIED | Previously verified navigation ordering remains present; Plans 13–17 do not replace the navigation pipeline, and Plan 18 native acceptance preserved responsiveness/behavior. |
| 4 | `38.1-D-04` rAF-batched UI propagation | VERIFIED | The approved scheduler remains the propagation seam; Plan 18 section E passed the grouped forward/reverse localized-render matrix. |
| 5 | `38.1-D-05` Latest-wins navigation | VERIFIED | Existing generation-token path remains present; stable callback forwarding reaches current parent behavior in `PhysicsPaintCanvasMount.runtime.test.ts`. |
| 6 | `38.1-D-06` Real-project native acceptance | VERIFIED | User-owned Plan 18 sections A–E passed, including both grouped threshold directions and post-fix rigid group-drag re-UAT. |
| 7 | `38.1-D-07` Deterministic instrumentation | VERIFIED | `physicsPaintPerformanceTrace.ts:82-89` catches both storage access modes; deterministic explicit-zero snapshots remain at lines 157-177. Two named fail-closed tests passed independently. |
| 8 | `38.1-D-08` Full path instrumentation | VERIFIED | The canonical 26-counter registry remains in `physicsPaintPerformanceTrace.ts:33-60`; Plan 18 section E used the accepted counter matrix in both directions. |
| 9 | `38.1-D-09` Timing-only exact behavior preservation | VERIFIED | All three old defects are fixed and behavior-tested; Plan 18 sections A–D and rigid group-drag re-UAT passed. |
| 10 | `38.1-D-10` Tooltip and capsule preservation | VERIFIED | Previously approved interaction contracts remain; no gap-closure plan replaced tooltip/capsule ownership, and Plan 18 reported no regression. |
| 11 | `R-D-01` Preserve historical timing work | VERIFIED | Plans 13–17 are localized fixes/tests; existing canvas-first scheduling, playback freeze, structural caching, and decoded-preview work remain intact. |
| 12 | `R-D-02` Extend the existing profiler | VERIFIED | The existing profiler seam is retained; only `profilingEnabled()` gained one localized fail-closed exception boundary. |
| 13 | `R-D-03` Instrument before localization | VERIFIED | Historical ordering is unchanged: Plan 08 instrumentation precedes Plans 09–11 localization. |
| 14 | `R-D-04` Exact render thresholds | VERIFIED | User-owned Plan 18 section E passed every grouped threshold in both forward and reverse directions. Raw deltas were optional and are not required evidence. |
| 15 | `R-D-05` Preact-native memoization and Signal reactivity | VERIFIED | Live code retains dedicated compat memo boundaries and render-updated refs. Plans 13–17 add no React-style effect/state synchronization layer. |
| 16 | `R-D-06` Workflow localization | VERIFIED | Static/live Workflow boundaries and mount-stable observer ownership remain; rigid group-drag correction changes resolver semantics, not localization ownership. |
| 17 | `R-D-07` Persistent CanvasMount/Efx isolation | VERIFIED | `PhysicsPaintStudioView.tsx:113` keys `MemoizedPhysicsPaintCanvasMount`; `PhysicsPaintCanvasMount.tsx:17-30,67-89` uses latest refs with stable callbacks. The 3-test runtime suite passed, and Plan 18 section D accepted real same-key persistence, dimension reconfiguration, key replacement, and exactly-once cleanup. |
| 18 | `R-D-08` Preserve behavior and pixel invariants | VERIFIED | Cached pixels, completed physics mode, capacity publication, CanvasMount lifecycle, and rigid group drag all have automated evidence plus authoritative native acceptance where runtime ownership matters. |
| 19 | `R-D-09` No environment/dependency/launch changes | VERIFIED | Plans 13–17 add no dependency or test-config changes. The exact Plan 18 gate passed; the agent launched no server/browser/native runtime. |
| 20 | `R-D-10` Two-stage acceptance and final automation | VERIFIED | Exact Plan 18 automation passed before user-owned native acceptance; sections A–E and native re-UAT were explicitly approved before this phase verdict. |

**Score:** 20/20 decision contracts verified.  
**Behavior-unverified:** 0.

## Former Blocker Closure

### 1. Cached playback background parity

**Status:** CLOSED

**Production evidence:**

- `app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx:49-60` retains one external-sync effect and delegates to `subscribeRotoPlaybackBackground()`.
- `app/src/components/physic-paint/view/rotoPlaybackBackground.ts:22-32` resolves complete metadata through `resolveMissingRotoFrameDraw()`, clears transparency synchronously, skips paper subscription for transparent output, subscribes using the resolved background texture, and calls `drawRotoFrameComposite()`.
- `app/src/lib/rotoFrameDraw.ts:65-75` resolves `background`, `paperGrain`, and `grainStrength` independently.
- `app/src/lib/rotoFrameDraw.ts:101-122` composes prepared paper/texture/color first and then applies independent positive-strength grain.
- Cleanup is idempotent at `rotoPlaybackBackground.ts:34-39`.

**Behavioral evidence reproduced by verifier:**

- Transparent playback named test: 1 passed.
- Independent texture/grain metadata named test: 1 passed.
- User-owned Plan 18 section A: PASS.

### 2. Completed physics mode finalization and replay

**Status:** CLOSED

**Production evidence:**

- `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:709-755` captures `completedMode` before teardown, uses it for selective `last` synchronization and action metadata, then restores `savedPhysicsMode` after finalization.
- `packages/efx-physic-paint/src/types.ts:169-199` represents recorded/serialized `local | last | all | null` through `PhysicsMode`.
- The engine replay path is wired to recorded mode rather than ambient restored state; the plan key-link query verified both type and diffusion-replay links.

**Behavioral evidence reproduced by verifier:**

- `finalizes a completed last session before restoring the prior physics mode`: 1 passed.
- The test asserts unrelated `savedWet` pixels remain unchanged, action metadata records `last`, and public mode returns to `local`.
- User-owned Plan 18 section B: PASS.

### 3. Capacity-only physical timeline publication

**Status:** CLOSED

**Production evidence:**

- `app/src/stores/physicPaintStore.ts:1202-1208` reads `previousCapacity` and requires records revision, interpolation revision, and capacity all to match before returning a no-op.
- A capacity difference continues through selection/cursor reconciliation, capacity installation, structural revision increment, and `_notifyVisualChange()` at lines 1211-1226.

**Behavioral evidence reproduced by verifier:**

- `capacity-only replacement clamps the cursor, replaces the projection, and publishes once`: 1 passed.
- The test proves capacity `13`, cursor clamp to `12`, new 13-cell projection identity, one physical revision, one visible version, one dirty notification, and an exact-triple no-op on repetition.
- User-owned Plan 18 section C: PASS.

## Former Warning Closure

### 1. Restricted profiling storage

**Status:** CLOSED

`physicsPaintPerformanceTrace.ts:82-89` encloses both the `window.localStorage` property access and `getItem()` call in one fail-closed `try/catch`. The verifier ran the `fails closed` filter: both restricted-storage tests passed. Both cases leave samples/counters untouched while snapshots expose the complete explicit-zero registry.

### 2. Persistent CanvasMount automated/runtime evidence

**Status:** CLOSED

The evidence is correctly split by authority:

- `app/src/test/preactHookRuntime.ts` executes hook slots, dependency-aware effects, cleanup transitions, and idempotent unmount without claiming keyed reconciliation.
- `app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts` directly executes the real component and verifies stable-forwarder freshness, equal-dimension effect retention, changed-dimension cleanup/reconfiguration, and exactly-once ResizeObserver/rAF cleanup. All 3 tests passed independently.
- `app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx:113` preserves the keyed memoized mount architecture.
- User-owned Plan 18 section D supplies the evidence unavailable to Node direct execution: actual same-key instance persistence, latest callbacks, dimension-only reconfiguration without engine replacement, changed-key replacement, and exactly-once real cleanup. Section D passed.

## Rigid Roto Group-Drag Acceptance Correction

**Status:** ACCEPTED; does not reopen the completed debug artifact.

Commit `25c7cf9e` changes the physical resolver to a rigid selected-only translation:

- `physicsPaintRotoPhysicalResolver.ts:1413-1473` computes one delta from the grabbed key and keeps all unselected frames fixed.
- Lines 1475-1500 reject capacity violations and collisions with unselected keys atomically.
- Lines 1503-1527 update only selected destinations and retain grabbed-key selection/drag presentation.

The verifier independently ran `translates A@0 and B@1 to A@5 and B@6 without moving D@7`: 1 passed. Additional live tests cover retained publication, one commit, rejection without dispatch, one Undo/Redo command, selection aftermath, and preview roles. The user-owned native re-UAT accepted the exact A@0/B@1/D@7 to A@5/B@6/D@7 result after commit `25c7cf9e`.

`.planning/debug/roto-group-drag-rigid-move.md` was read only through commit metadata and was not reopened or edited.

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/src/components/physic-paint/view/rotoPlaybackBackground.ts` | Canonical playback background subscription/composition seam | VERIFIED | Substantive and wired from StudioView to shared resolver/compositor. |
| `app/src/components/physic-paint/view/rotoPlaybackBackground.test.ts` | Transparency, independent metadata, cleanup behavior | VERIFIED | Focused behavior tests; named tests reproduced. |
| `app/src/lib/rotoFrameDraw.ts` | Shared canonical resolver/compositor | VERIFIED | Independent texture/grain fields flow to drawing operations. |
| `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts` | Completed-mode finalization/replay behavior | VERIFIED | Correct ordering and recorded mode are present and tested. |
| `packages/efx-physic-paint/src/types.ts` | Recorded/serialized physics mode contract | VERIFIED | `PhysicsMode` covers all engine modes plus null. |
| `packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts` | Finalization, replay, serialization regression | VERIFIED | Named finalization test reproduced. |
| `app/src/stores/physicPaintStore.ts` | Capacity-aware replacement guard | VERIFIED | Capacity is the third structural identity member. |
| `app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts` | Capacity-only mutation/no-op matrix | VERIFIED | Named publication test reproduced. |
| `app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts` | Fail-closed profiling gate | VERIFIED | One localized exception boundary; deterministic snapshots retained. |
| `app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts` | Throwing getter/getItem coverage | VERIFIED | Both fail-closed tests reproduced. |
| `app/src/test/preactHookRuntime.ts` | Bounded Node hook/effect runtime | VERIFIED | Substantive, dependency-aware, idempotent, and test-only. |
| `app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.runtime.test.ts` | Executed callback/effect/resource behavior | VERIFIED | 3/3 tests reproduced. |
| `.planning/phases/38.1-studio-render-path-performance/38.1-18-SUMMARY.md` | Exact automation and user-owned native verdict | VERIFIED AS ACCEPTANCE RECORD | Exists and records the user-supplied Plan 18 result; current file is an untracked planning artifact pending orchestrator commit. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `PhysicsPaintStudioView.tsx` | `rotoPlaybackBackground.ts` | Existing playback effect delegates subscription/drawing | WIRED | Direct import and call at lines 17 and 54. |
| `rotoPlaybackBackground.ts` | `rotoFrameDraw.ts` | Resolver plus `drawRotoFrameComposite()` | WIRED | Direct imports/calls; real metadata reaches drawing operations. |
| `EfxPaintEngine.ts` | `types.ts` | `PhysicsMode` in recorded/serialized strokes | WIRED | Shared type is used by engine save/load/replay surfaces. |
| `EfxPaintEngine.ts` | diffusion replay | Recorded completed mode forwarded to replay | WIRED | Key-link query passed; round-trip/replay test asserts `last`. |
| `physicPaintStore.ts` | Studio subscribers | `rotoPhysicalRevision` plus `_notifyVisualChange()` | WIRED | Capacity-only mutation publishes both structural and visible change. |
| `physicsPaintPerformanceTrace.ts` | Studio render counters | Every recorder consults `profilingEnabled()` | WIRED | Storage failure disables all render-path calls uniformly. |
| `PhysicsPaintCanvasMount.runtime.test.ts` | `PhysicsPaintCanvasMount.tsx` | Direct real-component hook execution | WIRED | Captures actual child callback props and effect resources. |
| `PhysicsPaintStudioView.tsx` | `MemoizedPhysicsPaintCanvasMount` | Keyed persistent VNode | WIRED | `key={props.canvasKey}` remains at line 113. |
| Plan 18 automated preconditions | Native checkpoint | Automation before user-owned runtime acceptance | VERIFIED | User explicitly supplied exact passing gate and subsequent A–E approval. |
| Native acceptance | Phase 38-06 eligibility | Explicit approval only | VERIFIED | This phase-level report records the required passing verdict. |

## Data-Flow Trace

| Artifact | Data | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Playback background canvas | Complete `PhysicPaintRotoBackgroundMetadata` | Studio playback composition props -> canonical resolver -> paper subscription/compositor | Yes | FLOWING |
| Physics continuation/replay | `completedMode`, tick count, wet buffers | Active engine session -> action history -> serialization/replay | Yes | FLOWING |
| Physical timeline projection | records, interpolation, capacity | Validated replacement payload -> store maps/cache -> Signals/visible notification | Yes | FLOWING |
| Performance snapshots | named samples/counters | Instrumented render/observer/lifecycle calls gated by localStorage opt-in | Yes; explicit zero while unavailable | FLOWING |
| CanvasMount/Efx lifecycle | keyed mount props, latest callback refs, dimensions | Studio -> memoized keyed mount -> Efx child and effect resources | Yes | FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Transparent cached playback | `pnpm -C app exec vitest run ...rotoPlaybackBackground.test.ts -t "keeps transparent playback clear..."` | 1 passed | PASS |
| Independent texture/grain metadata | `pnpm -C app exec vitest run ...rotoPlaybackBackground.test.ts -t "subscribes to the background texture..."` | 1 passed | PASS |
| Completed `last` mode before restoration | `pnpm -C app exec vitest run --root ../packages/efx-physic-paint ... -t "finalizes a completed last session..."` | 1 passed | PASS |
| Capacity-only publication | `pnpm -C app exec vitest run ...rotoPhysicalStructuralCache.test.ts -t "capacity-only replacement..."` | 1 passed | PASS |
| Restricted profiler storage | `pnpm -C app exec vitest run ...physicsPaintPerformanceTrace.test.ts -t "fails closed"` | 2 passed | PASS |
| CanvasMount callback/effect/resource behavior | `pnpm -C app exec vitest run ...PhysicsPaintCanvasMount.runtime.test.ts` | 3 passed | PASS |
| Rigid selected-only group drag | `pnpm -C app exec vitest run ...physicsPaintRotoPhysicalResolver.test.ts -t "translates A@0..."` | 1 passed | PASS |

### Exact Plan 18 Automation

The user supplied the authoritative exact Plan 18 automation result, and this report does not substitute SUMMARY narration for that user-owned statement:

- App: 9 files / 115 tests passed.
- Engine package: 3 files / 58 tests passed.
- App `tsc --noEmit`: passed.
- Package `check`: passed.
- App production build: passed.
- Clean tree before and after the exact gate.

At re-verification time, production and test files remained unchanged; only newly materialized planning artifacts (`38.1-18-SUMMARY.md`, then this report) make the current working tree non-clean pending orchestrator handling.

## Probe Execution

No phase probe scripts were declared or implied. Step 7c is not applicable.

## Requirements Coverage

Phase 38.1 uses decision contracts rather than registered formal requirement IDs. All 20 phase/reopen decision contracts are mapped and verified above. No orphaned formal requirement ID was found for this phase.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts` | 744 | Existing comment uses the word `placeholder` for the established zero-point continuation marker | INFO | Git blame attributes the line to pre-phase commit `43b111f40`; behavior is substantive and replay-tested, so it is not a stub. |

No unreferenced `TBD`, `FIXME`, or `XXX` markers were found in the gap-closure production files. No disconnected placeholder UI/data path was found.

## Human Verification

No further human verification is required for this verdict. The runtime-only items were already completed and explicitly approved by the user:

- Plan 18 A — cached playback pixels: PASS.
- Plan 18 B — completed `last` physics mode: PASS.
- Plan 18 C — capacity-only publication: PASS.
- Plan 18 D — actual Preact CanvasMount/Efx lifecycle: PASS.
- Plan 18 E — grouped render/lifecycle thresholds: PASS forward and reverse.
- Rigid group-drag post-fix native re-UAT: PASS.

Raw profiler delta JSON was optional by the accepted protocol and has not been requested, inferred, or fabricated.

## Planning Metadata Note

The live `.planning/ROADMAP.md` and `.planning/STATE.md` still show Plan 18 as pending/Plan 17 as the stopping point even though the phase directory contains all 18 summaries and `roadmap.analyze` reports `summary_count: 18` and `disk_status: complete`. This is administrative tracking lag, not a code or goal-achievement failure. Per the task boundary, this verification modified no ROADMAP/STATE, production, test, debug, or runtime file.

## Gaps Summary

No blocking gaps remain. All three old blockers and both warnings are closed, the localized render/lifecycle matrix is accepted in both directions, and the native-only Preact lifecycle claims have authoritative user evidence.

---

_Verified: 2026-07-29T12:34:56Z_  
_Verifier: Claude (gsd-verifier)_
