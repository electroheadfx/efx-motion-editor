---
phase: 38.1-studio-render-path-performance
plan: 03
type: execute
wave: 2
depends_on: [38.1-01, 38.1-02]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 38.1-D-03
  - 38.1-D-04
  - 38.1-D-05
  - 38.1-D-08
  - 38.1-D-09

must_haves:
  truths:
    - "On any key-click or navigation, the engine canvas paint (loadCachedRotoReferenceFrame / clear+blank path) is issued in the SAME TICK as the navigation intent, BEFORE any Preact UI state propagation (38.1-D-03) — the primary interaction contract."
    - "Preact UI frame propagation (setLaunchContext startFrame update) is routed through the 38.1-02 scheduler: at most one Studio render per animation frame regardless of click rate (38.1-D-04)."
    - "A navigation superseded by a newer one before its canvas paint never paints (generation token check after every await, 38.1-D-05); a discrete click whose generation is still latest ALWAYS paints (RESEARCH Pitfall 5 — the destination paint is never queued behind stale work, and the save-before-leave flush itself is never skipped or weakened)."
    - "Studio render-body per-frame O(N) derivations (getRotoCachedPlaybackFrames feeding selectRotoPlaybackAvailable at :912-916, projectRotoOnionPreviewFrames at :903-911) are computed from structural inputs with stable identities — a pure frame change re-runs neither (38.1-D-08 link 3; RESEARCH Pitfall 7)."
    - "Every observable value is identical to the pre-refactor build: Play availability boolean (including no-launch and all-missing cases), onion overlays, selection sync, parent frame-sync message, error paths (38.1-D-09)."
  artifacts:
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — navigation pipeline rewired canvas-first with generation tokens; frame propagation through the rAF scheduler; playback-availability and onion projections behind structural memoization."
  key_links:
    - "Model identity contract (38.1-01) -> Studio render: the model no longer rebuilds per frame, so the rAF-flushed Studio render reads stable structural identities instead of recreating 14 computeds."
    - "Scheduler + generation tokens (38.1-02) -> navigateToSyncedPhysicalFrame: schedule() gates setLaunchContext; begin()/isLatest() gates the canvas paint after the awaited flush."
    - "Studio per-tick surface -> playback freeze (38.1-05): this plan must NOT convert playback per-tick state (animFrame/animTotal/rotoCachedPlayback.frame) — that is 38.1-05's exclusive scope (shared-file sequencing)."
---

<objective>
Rewire the Studio navigation pipeline to the locked ordering invariants: canvas-first same-tick paint with latest-wins generation tokens (38.1-D-03/D-05), rAF-batched UI frame propagation (38.1-D-04), and structural memoization of the two per-render O(N) derivations (38.1-D-08 link 3) — with zero observable behavior change (38.1-D-09).

Purpose: this is the user-facing half of the phase — the user's locked priority is "canvas instant on every key click; UI estimation may lag." With 38.1-01's identity contract and 38.1-02's primitives, this plan is pure wiring at the exact call sites the research verified.

Output: one modified file; full suite + typecheck green; interactive verification deferred to the 38.1-06 native UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md
@.planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md
@.planning/phases/38.1-studio-render-path-performance/38.1-01-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-02-SUMMARY.md

@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts
@app/src/components/physic-paint/view/physicsPaintStudioSelectors.ts
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end canvas-first navigation — same-tick paint, generation-guarded stale skips, rAF-batched frame propagation (38.1-D-03, D-04, D-05)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <precondition>38.1-01 and 38.1-02 SUMMARYs exist: the model split is committed (frame writes no longer rebuild the model) and rotoUiFlushScheduler.ts exports the scheduler + generation helper this task wires.</precondition>
  <reversibility rating="costly">38.1-D-03 is user-locked as the render-ordering invariant the whole Studio hot path organizes around; reverting means re-architecting the navigation pipeline. Flagged only — the decision itself is already locked in CONTEXT.md.</reversibility>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:60-86 (selectedKeyId signal + setLaunchContext selection-sync callback at :66-86 — note the startFrame branch at :78-83 that also writes selection signals; deferring setLaunchContext defers this sync, which is UI work and CORRECT under D-04), :211 (currentFrame = launchContext?.startFrame ?? 0 — the full-render driver), :717-746 (navigateToSyncedPhysicalFrame — the surgery surface), :747-767 (rotoNavigation display port: restoreFrame/clearCanvas/navigate call sites that also paint), :898 and :1145 (navigateRotoFrame / onNavigateToSyncedFrame handler entries).
    - app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts — the exact exported names/signatures shipped by 38.1-02.
    - app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts — how requestNavigation funnels into the navigate port (confirm navigateToSyncedPhysicalFrame is the single navigation chokepoint; if a second path paints the canvas on navigation, this task covers it too).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Patterns 1-2, Pitfall 3 (write site), Pitfall 5 (flush-before-paint await), Open Question 2 (flush boundary scope: ONLY the per-frame channel routes through the scheduler; event-driven status updates like applyStatus/applyMessage/script status stay immediate).
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §PhysicsPaintStudio.tsx (navigation pipeline excerpt, guarded write idiom).
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts — the 7 source-grep assertions on this file; every asserted literal in the blocks you touch must survive verbatim (moves allowed, mutations not).
  </read_first>
  <action>
    Restructure navigateToSyncedPhysicalFrame (:717-746) into the locked order, keeping every existing behavior:

    Step 1 — validation and guards verbatim: the Number.isInteger(frame) && frame >= 0 guard and the rotoCachedPlayback.stop() call stay first, unchanged.

    Step 2 — begin a navigation generation via the 38.1-02 helper BEFORE any await.

    Step 3 — save-before-leave flush verbatim: engine?.flushPendingStrokeFinalizations() and the awaited rotoPersistence.flushLivePixels(currentFrame) with its exact try/catch error path (setApplyStatus('error') + the verbatim `Could not save Roto frame ${currentFrame} before navigation.` message + return false) are preserved byte-for-byte in semantics (RESEARCH Pitfall 5: never skip or weaken the flush; 36.6 contract). NOTE the flush uses the PRE-navigation currentFrame — do not substitute the destination frame.

    Step 4 — generation gate immediately after every await: if the generation is no longer latest, return without painting or propagating (a newer navigation owns the canvas). A generation that is still latest ALWAYS proceeds (discrete-click rule, 38.1-D-05).

    Step 5 — canvas-first paint (38.1-D-03): setCachedRotoReferenceUrl(null), engine.clearPreviewBaseImage(), resetBackground(), engine.clear(), and loadCachedRotoReferenceFrame(frame, engine) issue NOW, in this same synchronous continuation, BEFORE any UI state propagation. Same-tick means: no scheduler, no microtask detour, between the generation check and any Preact-updating call.

    Step 6 — cheap immediate signal writes: selectedKeyId.value and the physicPaintStore.setRotoPhysicalSelection(layerId, keyId, frame) call stay immediate (in-place signal write + store selection sync are the cheap computeds path; they do not rebuild anything post-38.1-01). Guard the signal write by equality per the Studio:70-72 idiom.

    Step 7 — rAF-batched UI propagation (38.1-D-04): the setLaunchContext((current) => current ? { ...current, startFrame: frame } : current) update — the full-Studio-render driver via :211 — is routed through the 38.1-02 scheduler so a click burst coalesces to one Studio render per animation frame showing the LATEST frame. Instantiate ONE scheduler + ONE generation helper per Studio component instance (module-level singletons are forbidden — two Studio windows must not share navigation generations); dispose the scheduler from the existing component cleanup path so an unmounted Studio never flushes. The pendingFrameSyncRef.current = frame write and the awaited sendPhysicPaintFrameSyncMessage(frame, bridgeMode) keep their existing per-navigation behavior and ordering AFTER the canvas paint — the parent frame-sync contract is behavior, not UI estimation; do NOT coalesce it in this phase (38.1-D-09).

    Step 8 — cover the display-port restore path (:747-767): restoreFrame's load-real-key/blank-real-key branch paints via loadCachedRotoReferenceFrame; route its setLaunchContext startFrame update through the same scheduler, and give its paint the same canvas-first ordering within its own flow. clearCanvas and clearCachedReferenceFrame stay verbatim.

    Scope guards: do NOT touch the playback per-tick surface (animFrame/animTotal useState at :54-56, the playback input at :558-567, rotoCachedPlayback.frame) — 38.1-05 owns it. Do NOT touch PhysicsPaintWorkflowStrip.tsx, useRotoCachedPlayback.ts, or any test file. No derivation-semantics changes; no new useEffect chains (the scheduler is external-sync scheduling, created once per instance, not an effect dependency).
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/hooks/useRotoFrameEditingController.test.ts src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts && pnpm --dir app typecheck</automated>
    <human-check>Interactive feel (fast key-click canvas response, drag-scrub latest-wins) is verified in the 38.1-06 blocking native UAT with the user's real project — not in this plan.</human-check>
  </verify>
  <acceptance_criteria>
    - In navigateToSyncedPhysicalFrame, source order provably matches the locked pipeline: integer guard -> playback stop -> generation begin -> stroke finalization + awaited live-pixel flush with the verbatim error path -> generation recheck -> engine clear/paint calls -> selection signal/store sync -> scheduler-routed setLaunchContext -> frame-sync message. The canvas paint call site has no scheduler or UI-state call before it.
    - One scheduler and one generation helper are created per Studio instance (grep: creation sites are inside the component, not module scope) and the scheduler is disposed on unmount.
    - The flushLivePixels await and its catch body (status 'error', the Could-not-save message, return false) are present verbatim; the flush argument is the pre-navigation currentFrame.
    - PhysicsPaintStudio.test.ts passes UNMODIFIED (all 7 source-grep assertions green); no test file appears in git diff --name-only.
    - No edit touches the playback input block at :558-567, animFrame/animTotal, or rotoCachedPlayback (grep the diff: zero changed lines in those regions).
    - pnpm --dir app typecheck clean; the 3 known-red D-15 tests are untouched.
  </acceptance_criteria>
  <done>One navigation path (cell click or keyboard nav) paints the engine canvas in the same tick as the intent, skips only superseded paints, and coalesces Studio renders to at most one per animation frame — with the save-before-leave flush, selection sync, error path, and parent frame-sync behavior byte-identical. The end-to-end canvas-first contract is committed on the single path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Structural memoization of Studio per-render O(N) derivations — playback availability + onion projection (38.1-D-08 link 3, RESEARCH Pitfall 7)</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:698 (getRotoCachedPlaybackFrames — full frame array with per-frame findCachedRotoDisplayFrame cache lookups), :903-911 (projectRotoOnionPreviewFrames re-projected per render), :912-916 (selectRotoPlaybackAvailable consuming the per-render array), :93-94 (rotoKeyRecords/rotoInterpolationState memos subscribed to physicPaintVersion — the sanctioned structural-memo idiom to copy).
    - app/src/components/physic-paint/view/physicsPaintStudioSelectors.ts — selectRotoPlaybackAvailable implementation (the .some(Boolean)-style availability semantics whose boolean output MUST stay identical, including no-launch and all-missing cases).
    - app/src/components/physic-paint/roto/rotoOnionPreview.ts — projectRotoOnionPreviewFrames inputs: which are structural (realKeyRecords, getRenderSource, previewFrames, dirtyFrames) versus per-frame (currentFrame, isPlaying, onion).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pitfall 7 (availability check), defect link 3 (view-model reconstruction).
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts — re-check asserted literals in any block you touch.
  </read_first>
  <action>
    Playback availability: stop building the full getRotoCachedPlaybackFrames() array (with per-frame cache lookups) on every render just to answer the Play-enabled boolean. First PROVE the equivalence by reading selectRotoPlaybackAvailable: it reduces frames to a boolean (.some-style check). Derive that same boolean from cheap structural inputs — e.g. a useMemo over [workflowMode, launchContext?.layerId, launchContext === null, rotoSession.playbackFrameNumbers.value] that maps frame numbers to cached availability the same way, so the memo recomputes ONLY when the structural frame list or launch identity changes, not on every Studio render. The boolean output must be identical in ALL cases: no launch context, empty playback list, all frames missing (findCachedRotoDisplayFrame returning null), and mixed. If exact equivalence cannot be achieved from structural inputs alone, keep the existing derivation but memoize the ARRAY construction on [rotoSession.playbackFrameNumbers.value, latestRotoFramesRef.current identity] so the per-frame cache lookups stop running per render — document the chosen path in the commit message.

    Onion projection: memoize projectRotoOnionPreviewFrames (:903-911) with useMemo keyed on its actual inputs: currentFrame, isPlaying, onion, rotoKeyRecords (structural, post-38.1-01 stable across paintVersion-irrelevant renders), rotoPreviewFramesRef.current, dirtyRotoFramesRef.current. The projection still re-runs on every genuine input change — including currentFrame — but stops re-running on unrelated Studio renders (status updates, playback ticks pre-38.1-05, panel toggles). Do not change what it computes (38.1-D-09).

    View-model literal (:943-1148): no restructuring in this plan beyond the two derivations above — with the model split (38.1-01), the rAF render cap (Task 1), and these memos, the remaining literal construction is O(1) property copies. Do NOT split the view-model hook or reorder its properties (source-grep surface + 38.1-05 needs a stable base).

    Scope guards: same exclusions as Task 1 (no playback per-tick surface, no strip, no playback hook, no test files). Keep every asserted source literal verbatim.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm --dir app vitest run src/components/physic-paint/view && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - selectRotoPlaybackAvailable's boolean output is provably unchanged: the commit message records the equivalence analysis (no-launch, empty list, all-missing, mixed cases) and which memoization path was chosen.
    - The per-render full-array construction with per-frame cache lookups no longer executes on every Studio render (grep: getRotoCachedPlaybackFrames is either removed from the render body or wrapped in a structural memo whose dependency array contains no per-render-churning value).
    - projectRotoOnionPreviewFrames is wrapped in useMemo whose dependency array enumerates exactly its real inputs; the call's arguments are unchanged.
    - PhysicsPaintStudio.test.ts and the view test suite pass UNMODIFIED; no test file in git diff; typecheck clean; 3 known-red D-15 tests untouched.
  </acceptance_criteria>
  <done>Studio renders triggered by non-structural changes no longer pay the O(N) availability or onion re-derivation; Play availability and onion visuals are bit-identical to the pre-refactor build in all launch/cache configurations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigation intent -> engine canvas | superseded async navigation painting after a newer intent corrupts visible frame state |
| Studio -> parent frame-sync bridge | navigation notifications cross to the parent editor; contract must stay exact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-03-01 | Tampering | navigateToSyncedPhysicalFrame | high | mitigate | Generation recheck after EVERY await; discrete-click rule test-locked in 38.1-02; save-flush error path verbatim so a failed save can never navigate |
| T-38.1-03-02 | Denial of Service | Studio render loop | high | mitigate | rAF scheduler caps renders at 1/frame (38.1-D-04); structural memos remove per-render O(N) work; 38.1-01 D-07 spy test guards the model layer |
| T-38.1-03-03 | Tampering | sendPhysicPaintFrameSyncMessage | medium | mitigate | Per-navigation bridge contract deliberately NOT coalesced (38.1-D-09); ordering after canvas paint preserved |
| T-38.1-SC | Tampering | package installs | high | mitigate | No installs — wiring consumes 38.1-02 in-repo utilities only |
</threat_model>

<verification>
- pnpm --dir app vitest run — full suite: exactly 3 failures, all known-red D-15
- pnpm --dir app typecheck — clean
- Source-order inspection of navigateToSyncedPhysicalFrame against the locked pipeline (Task 1 acceptance criteria)
</verification>

<success_criteria>
- Canvas paints same-tick on navigation, before any Preact UI work (38.1-D-03).
- Click bursts produce at most one Studio render per animation frame showing the latest frame (38.1-D-04).
- Superseded navigations never paint; discrete clicks always paint (38.1-D-05).
- Per-render O(N) availability/onion derivations are structurally memoized with identical outputs (38.1-D-08 link 3, D-09).
</success_criteria>

## Artifacts this phase produces

- No new files and no new exported symbols — this plan rewires existing call sites in `app/src/components/physic-paint/PhysicsPaintStudio.tsx` using the 38.1-01/38.1-02 artifacts. Any new local bindings (scheduler instance, generation helper instance, memo hooks) are component-scoped and unexported.

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-03-SUMMARY.md` when done
</output>
