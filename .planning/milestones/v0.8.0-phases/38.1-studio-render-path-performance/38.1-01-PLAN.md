---
phase: 38.1-studio-render-path-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineModel.test.ts
  - app/src/components/physic-paint/roto/rotoTimelineSelectors.ts
autonomous: true
requirements:
  - 38.1-D-07
  - 38.1-D-08
  - 38.1-D-09

must_haves:
  truths:
    - "Writing a new currentFrame or selectedKeyId into the model triggers ZERO new calls to projectPhysicPaintRotoPhysicalTimeline and zero legacy source/display model rebuilds — the O(N)/O(capacity) structural work runs only when records, interpolation, capacity, cached frames, or interpolation settings change identity (38.1-D-07, root-cause link 1)."
    - "rotoTimelineModel.physicalCells.value keeps reference identity across a pure frame change, so downstream memos (strip physicalCellByAppFrame, rotoDragVacatedAppFrames) stop recomputing per frame (RESEARCH Pitfall 2)."
    - "Every model consumer observes byte-identical VALUES for all 14 exposed computeds across real-key, generated, and empty frames — only WHEN derivations run changes, never WHAT they compute (38.1-D-09)."
    - "The exported RotoTimelineModel interface and useRotoTimelineModel(input) signature remain call-compatible for useRotoTimelineActions (getModel: () => rotoTimelineModel.view.value.model), the Studio body, and useRotoFrameEditingController (PATTERNS hard constraint)."
  artifacts:
    - "app/src/components/physic-paint/hooks/useRotoTimelineModel.ts — persistent signal graph: structural computeds fed by structural inputs only; frame/selection written in place through an additive write seam; useMemo dependency array no longer contains the frame or selection inputs."
    - "app/src/components/physic-paint/roto/rotoTimelineSelectors.ts — a structural/frame-separated seam for BOTH selectRotoPhysicalTimelineView and selectRotoTimelineView; both existing exports keep identical signatures and return shapes."
    - "app/src/components/physic-paint/hooks/useRotoTimelineModel.test.ts — NEW node-env .test.ts: D-07 spy-counter instrumentation plus value-parity assertions; no jsdom, no happy-dom, no new test config."
  key_links:
    - "Model split -> strip memos: stable physicalCells identity is what un-breaks PhysicsPaintWorkflowStrip.tsx:331-337 useMemo — this plan ships the identity contract plan 38.1-04 consumes."
    - "Model split -> Studio render cost: useRotoTimelineModel.ts:77 dep array drops input.currentFrame/input.selectedKeyId, so the Studio's per-navigation render no longer recreates 14 computeds."
    - "physicPaintVersion bump-AND-subscribe: the Studio's rotoKeyRecords/rotoInterpolationState memos (PhysicsPaintStudio.tsx:93-94) keep flowing through structural inputs — never peek()/untracked() around them (project memory contract)."
---

<objective>
Split the Roto timeline model's signal graph into structural inputs (records, interpolation, capacity, cached frames, settings) versus frame/selection inputs (currentFrame, selectedKeyId), so a navigation frame change costs O(find) instead of rebuilding the entire model (the useRotoTimelineModel.ts:77 defect, root-cause link 1 of 4 per 38.1-D-08). Ship the D-07 instrumentation test proving frame writes cause zero structural rebuilds and bounded recompute.

Purpose: this is the load-bearing fix of the phase — every downstream plan (Studio nav pipeline, strip cheap-render, playback freeze) depends on structural outputs keeping reference identity across a pure frame change.

Output: restructured model + selector seam, byte-compatible public surface, new D-07 test green, all existing model-consumer tests green.

Tracer slice: one in-place currentFrame write flows through the split graph — structural projection untouched, currentCell computed recomputed, D-07 spy counters prove it — verified end-to-end by the new instrumentation test plus the full existing consumer suite (useRotoTimelineActions.test.ts, useRotoFrameEditingController.test.ts) passing unchanged.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md
@.planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/.continue-here.md

@app/src/components/physic-paint/hooks/useRotoTimelineModel.ts
@app/src/components/physic-paint/roto/rotoTimelineSelectors.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end model split tracer — RED: D-07 instrumentation test; GREEN: physical-view structural seam + persistent model graph (38.1-D-07, D-08 link 1, D-09)</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineModel.test.ts, app/src/components/physic-paint/roto/rotoTimelineSelectors.ts, app/src/components/physic-paint/hooks/useRotoTimelineModel.ts</files>
  <reversibility rating="reversible">Additive selector seam + internal model restructure behind a byte-compatible interface; reverting is a single-file rollback with no data-model impact.</reversibility>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineModel.ts (whole file, 78 lines) — the defect at :77 (useMemo dep array contains input.currentFrame and input.selectedKeyId); the RotoTimelineModel interface at :27-45 (14 computeds — byte-compatible target); createRotoTimelineModel at :47-74.
    - app/src/components/physic-paint/roto/rotoTimelineSelectors.ts:117-152 (selectRotoTimelineView — currentFrame used ONLY at :127-140 finds/includes), :193-284 (selectRotoPhysicalTimelineView — currentAppFrame used ONLY at :262-263 find; selectedKeyId only at :265-267 find; fail-closed empty-view branch at :242-259; projection call at :236-240).
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts:1-74 — the project harness idiom to copy: vi.mock('preact/hooks') with useMemo/useCallback passthroughs, Object.freeze fixture records, signal-driven input, assertions on exposed computed .value.
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §useRotoTimelineModel.ts (in-place write idiom from PhysicsPaintStudio.tsx:60-64, guarded write idiom from :70-72) and §useRotoTimelineModel.test.ts (D-07 spy-counter assertion shape).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pattern 1 (structural/frame split), Pitfall 2 (identity churn), Pitfall 3 (write loops), Pitfall 6 (legacy consumers), Code Examples (decoupled model consumption + D-07 test).
  </read_first>
  <behavior>
    - Test 1 (structural stability, RED first): build a model with 60 frozen real-key records + interpolation enabled + capacity 200; subscribe via effect(() => { model.currentCell.value; }); spy on projectPhysicPaintRotoPhysicalTimeline through the selectors/resolver module namespace; write 50 successive frames through the model's additive write seam; expected: spy call count unchanged (ZERO structural rebuilds) and model.physicalCells.value reference-equal before vs after the 50 writes.
    - Test 2 (frame-dependent correctness): after writing frame F that is a real key, model.currentCell.value.kind === 'real' and selectedRealKey/selectedAppFrame track setSelectedKeyId; after writing an empty frame, currentCell is the { kind: 'empty', appFrame: F } fallback; after writing a generated-interior frame, currentCell.kind === 'generated'.
    - Test 3 (structural invalidation still works): replacing the records input (new array identity) DOES re-run the projection exactly once and produces a new physicalCells identity — the split must not freeze structural updates.
    - Test 4 (fail-closed parity): input that makes the projection fail (duplicate appFrame identities per the resolver's validation) yields the same empty-view shape the selector returns today: physicalCells [], currentCell { kind: 'empty', appFrame: currentAppFrame }, selectedRealKey null, projection null.
  </behavior>
  <action>
    RED: create app/src/components/physic-paint/hooks/useRotoTimelineModel.test.ts (node env — the file MUST be .test.ts to match app/vitest.config.ts include; no jsdom, no happy-dom, no test config changes per project rule). Copy the harness idioms from useRotoTimelineActions.test.ts: vi.mock('preact/hooks', ...) passthroughs so createRotoTimelineModel/useRotoTimelineModel run outside a component; Object.freeze fixture records. Import the selectors module as a namespace and vi.spyOn it for projectPhysicPaintRotoPhysicalTimeline (the PATTERNS-endorsed pattern — verify the spy intercepts; if the physical projection lives in physicsPaintRotoPhysicalResolver.ts and is re-exported, spy on the module the model actually calls). Write the four behavior tests against the NEW model surface: an additive write seam on RotoTimelineModel (suggested method names setCurrentFrame(frame: number) and setSelectedKeyId(keyId: string | null) — additive interface members, never removals). Run pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoTimelineModel.test.ts and confirm RED (the seam does not exist yet). Commit the failing test per TDD gate.

    GREEN — selector seam in rotoTimelineSelectors.ts: separate each view selector into a structural computation and a frame/selection assembly while keeping BOTH existing exports (selectRotoTimelineView, selectRotoPhysicalTimelineView) signature- and shape-identical. Suggested shape (mechanism detail is executor latitude; invariants are not): export a structural helper for the physical view that computes identities -> projectPhysicPaintRotoPhysicalTimeline -> the fail-closed branch -> all frame-INDEPENDENT fields (orderedRealKeyRecords, physicalCells, generatedCells, orderedKeyIds, interpolation, capacity, projection), and assemble the frame-dependent fields (currentCell find with the exact ?? { kind: 'empty', appFrame: currentAppFrame } fallback from :262-263, selectedRealKey/selectedAppFrame finds from :265-268) in a separate step. The existing selectRotoPhysicalTimelineView delegates to the seam so behavior stays provably identical. Apply the same separation to the legacy selectRotoTimelineView: the legacy model/projection build (createRotoSourceDisplayModelLegacy + getRotoDisplayProjectionLegacy, :118-122) is structural; the three finds and includes at :127-140 are frame-dependent.

    GREEN — model split in useRotoTimelineModel.ts: make the model a persistent signal graph. Structural inputs (cachedRotoFrames, interpolationSettings, rotoKeyRecords, rotoInterpolationState, capacity) feed structural computeds; currentFrame and selectedKeyId live in their own signals written in place ONLY through the new additive write-seam methods (equality-guarded: write only when the new value differs from .peek() — RESEARCH Pitfall 3). Frame-dependent legacy fields (currentFrameSelectionKind, currentFrameOwnerSourceFrame, currentFrameIsGenerated) become cheap computeds exactly like currentCell (Pitfall 6). Keep every one of the 14 RotoTimelineModel members with the same Signal-wrapped types; add the write seam as new members. In useRotoTimelineModel(input): (a) remove the frame and selection inputs from the useMemo dependency array so structural input identity alone recreates the graph; (b) at the top of the hook body (NEVER in an effect, NEVER in a render body downstream — RESEARCH A3), sync input.currentFrame/input.selectedKeyId into the model through the guarded write seam; (c) structural inputs continue to recreate the graph via the surviving dependency-array entries. Do NOT introduce untracked()/peek() around anything subscribed to physicPaintVersion — the Studio's rotoKeyRecords/rotoInterpolationState memos must keep invalidating normally (bump-AND-subscribe contract).

    Scope guard: this task touches ONLY the three listed files. No Studio, strip, or playback edits. No derivation-semantics changes anywhere (38.1-D-09): every computed must return byte-identical values for identical inputs — the refactor changes WHEN computations run, never WHAT they return.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoTimelineModel.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/hooks/useRotoFrameEditingController.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The new test file passes all four behavior tests; the spy-counter test shows zero added projectPhysicPaintRotoPhysicalTimeline calls across 50 frame writes and reference-stable physicalCells; the structural-invalidation test shows exactly one rebuild on records replacement.
    - grep of the useMemo dependency array in useRotoTimelineModel.ts shows no frame or selection input entry; the RotoTimelineModel interface still exposes all 14 original members (view, occupiedRotoFrames, savedRotoFrames, cachedRotoFrames, currentFrameSelectionKind, currentFrameOwnerSourceFrame, currentFrameIsGenerated, physicalView, currentCell, selectedKeyId, selectedRealKey, selectedAppFrame, orderedRealKeyRecords, generatedCells, physicalCells) plus the additive write seam.
    - selectRotoTimelineView and selectRotoPhysicalTimelineView keep their exact exported names, parameter types, and return types in rotoTimelineSelectors.ts; the fail-closed empty-view branch semantics are unchanged.
    - useRotoTimelineActions.test.ts and useRotoFrameEditingController.test.ts pass UNMODIFIED; pnpm --dir app typecheck is clean.
    - No occurrences of untracked( added in the touched files (grep count 0 on new diff lines); no test config files touched; no *.test.* file other than the new useRotoTimelineModel.test.ts appears in git diff --name-only.
    - The 3 known-red D-15 tests (2 physicsPaintWorkflowPresentation baseline assertions + 1 PhysicsPaintWorkflowStrip placement source-grep) are NOT touched and remain red — they are 38-08 scope.
  </acceptance_criteria>
  <done>A frame or selection write costs O(find) with zero structural rebuilds (spy-proven), structural input changes still rebuild exactly once, all 14 model members return identical values for identical inputs, all existing consumer tests pass unmodified, and the D-07 instrumentation file exists and is green under vitest run in node env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-suite parity sweep — prove every model consumer is value-identical and identity-stable after the split (38.1-D-09, RESEARCH Pitfalls 2/6)</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineModel.ts, app/src/components/physic-paint/roto/rotoTimelineSelectors.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — getModel: () => rotoTimelineModel.view.value.model consumption of the legacy view.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:211-236 — currentFrame derivation and the Studio's reads of currentPhysicalCell / currentFrameSelectionKind / currentFrameIsGeneratedRoto.
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pitfall 6 (legacy consumers silently stale: keyboard nav, strip markers, playback availability) and Pitfall 2 (identity churn).
  </read_first>
  <behavior>
    - Test: the D-07 file gains a parity block asserting the legacy frame-dependent computeds track frame writes: writing a real-key frame yields currentFrameSelectionKind 'real-key'; a generated frame yields 'generated-interpolation' with currentFrameIsGenerated true; an empty frame yields 'empty' with currentFrameOwnerSourceFrame null — matching the pre-split selector semantics at rotoTimelineSelectors.ts:127-140.
    - Test: occupiedRotoFrames/savedRotoFrames/cachedRotoFrames keep reference identity across 50 frame writes (legacy structural outputs, same contract as physicalCells).
  </behavior>
  <action>
    Add the parity assertions above to useRotoTimelineModel.test.ts (RED against any gap found, GREEN via the split). Then run the FULL app suite with pnpm --dir app vitest run and triage every failure: any failure OUTSIDE the 3 known-red D-15 tests (2 physicsPaintWorkflowPresentation baseline assertions + 1 PhysicsPaintWorkflowStrip placement source-grep) is caused by this refactor — diagnose identity-consumption fallout per Pitfalls 2/6 and fix in the two source files of this plan (never by editing consumer tests, never by weakening assertions). Known-stale consumers to watch: keyboard navigation targeting (uses model view model/projection), strip markers (occupiedRotoFrames), playback availability inputs. If a consumer legitimately needs a stable-identity adaptation, keep the adaptation inside the model's exposed computeds, not in the consumer. Do not fix, skip, or quarantine the 3 known-red D-15 tests. Finish with pnpm --dir app typecheck clean.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run 2>&1 | tee /tmp/38.1-01-full-suite.txt >/dev/null; grep -E "Tests +3 failed \|" /tmp/38.1-01-full-suite.txt && test "$(grep -cE '^ *FAIL ' /tmp/38.1-01-full-suite.txt)" -eq 3 && test "$(grep -E '^ *FAIL ' /tmp/38.1-01-full-suite.txt | grep -vcE 'physicsPaintWorkflowPresentation\.test\.ts|PhysicsPaintWorkflowStrip\.test\.ts')" -eq 0 && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app vitest run reports failures ONLY from the 3 known-red D-15 tests; total failed-test count is exactly 3; every other test passes including the new parity block.
    - The parity block proves legacy frame-dependent computeds match selector semantics for real/generated/empty frames and that legacy structural outputs are reference-stable across frame writes.
    - No consumer test file was edited; no assertion was weakened; typecheck is clean.
  </acceptance_criteria>
  <done>The full app suite is green except the 3 sanctioned known-red D-15 tests, the parity block is part of the D-07 file, and no consumer required modification — the split is behavior-invisible (38.1-D-09) at the model layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal graph -> Studio/strip subscribers | derived state flows to render consumers; a poisoned derivation would render stale or wrong frame state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-01-01 | Denial of Service | useRotoTimelineModel write seam | medium | mitigate | Equality-guarded writes (peek-compare before .value assignment) prevent write→render→write loops (RESEARCH Pitfall 3); D-07 spy test proves bounded recompute |
| T-38.1-01-02 | Tampering | structural/frame split | high | mitigate | Fail-closed selector branch preserved verbatim; value-parity tests prove identical outputs for identical inputs; structural invalidation test proves store changes still propagate |
| T-38.1-SC | Tampering | package installs | high | mitigate | No installs in this plan — @preact/signals/preact/vitest already installed and sanctioned (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoTimelineModel.test.ts — D-07 model instrumentation green
- pnpm --dir app vitest run — full suite: exactly 3 failures, all known-red D-15
- pnpm --dir app typecheck — clean
</verification>

<success_criteria>
- Frame/selection writes never rebuild the structural model (spy-proven zero added projection calls across 50 writes).
- Structural outputs (physicalCells, occupiedRotoFrames, savedRotoFrames, cachedRotoFrames) are reference-stable across pure frame changes.
- All 14 RotoTimelineModel members and both selector exports are value- and shape-identical for identical inputs (38.1-D-09).
- The identity contract downstream plans depend on is committed and test-locked.
</success_criteria>

## Artifacts this phase produces

- `app/src/components/physic-paint/hooks/useRotoTimelineModel.test.ts` — NEW test file (D-07 instrumentation + parity block).
- `RotoTimelineModel.setCurrentFrame(frame: number): void` — NEW additive write-seam member (exact name may vary; additive only).
- `RotoTimelineModel.setSelectedKeyId(keyId: string | null): void` — NEW additive write-seam member (exact name may vary; additive only).
- Structural/frame-separated selector seam in `rotoTimelineSelectors.ts` — NEW exported helper(s) for structural computation (exact name is executor latitude; both legacy exports preserved).

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-01-SUMMARY.md` when done
</output>
