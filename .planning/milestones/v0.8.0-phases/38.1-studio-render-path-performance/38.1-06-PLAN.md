---
phase: 38.1-studio-render-path-performance
plan: 06
type: execute
wave: 4
depends_on: [38.1-05]
files_modified: []
autonomous: false
requirements:
  - 38.1-D-01
  - 38.1-D-02
  - 38.1-D-03
  - 38.1-D-04
  - 38.1-D-05
  - 38.1-D-06
  - 38.1-D-09
  - 38.1-D-10

must_haves:
  truths:
    - "The UAT fixture is the user's CURRENT REAL PROJECT — the one that measured 5-10x slow after group paste (38.1-D-06); success = the same project feels instant, the same bar applied when rejecting 38-06."
    - "The user confirms canvas-first navigation: instant canvas feedback on EVERY key click before any UI work (38.1-D-03); fast clicking never queues more than one UI render behind the canvas (38.1-D-04); drag-scrub always shows where the playhead IS, never a stale frame, while discrete clicks each paint (38.1-D-05)."
    - "The user confirms the playback freeze: cached playback runs at FULL project fps while ONLY the canvas image, the nav-pill current-frame indicator, and Play/Stop control state are live; cells, capsule, and tube/log are frozen (38.1-D-01)."
    - "The user confirms stop behavior: exactly ONE catch-up render on stop (no flicker), the queued missing-frame line appears ONCE, then the idle context line resumes (38.1-D-01/D-02, 38 D-10 wording verbatim)."
    - "The user confirms ZERO behavior change across selection gestures, capsule wording/arbitration, tooltips, drag, undo/redo, clipboard (single + group), scripts panel, and save/reopen (38.1-D-09) — any behavior delta is a bug and fails this checkpoint."
    - "The user confirms the Phase 38 locked mechanisms are preserved verbatim: tooltip fixed-in-place positioning with viewport-fixed notch, and capsule single-owner arbitration with the ambient idle-context feed (38.1-D-10)."
    - "Explicit native UAT approval is recorded by the user; the agent never launches the server, browser, or native app — the user runs everything natively (project CLAUDE.md)."
  artifacts: []
  key_links:
    - "User verdict -> 38.1-06-SUMMARY.md records approval (or rejection with failing step numbers routed to owning plans); approval closes the phase and unblocks the re-run of the 38-06 33-step UAT script in phase 38 (RESEARCH Validation Architecture)."
    - "Rejection routing: each numbered UAT step names its owning plan (38.1-01 model, 38.1-02 primitives, 38.1-03 navigation, 38.1-04 strip, 38.1-05 playback freeze) so a failure routes back to exactly one plan."
---

<objective>
BLOCKING native user-owned UAT checkpoint for Phase 38.1, scripted against 38.1-D-01..D-06, D-09, and D-10 on the user's real project (the one that measured 5-10x slow — 38.1-D-06). The user runs the app natively and executes the numbered verification script below covering: canvas-first instant feedback per key click, fast-click and drag-scrub ordering invariants, cached playback at full project fps with frozen cells/capsule/tube-log and the live nav-pill frame indicator, the missing-frame line appearing once on stop, exactly one catch-up render on stop, zero behavior change across selection gestures/tooltips/drag/undo-redo/clipboard/scripts/save-reopen, and tooltip + capsule preservation.

Purpose: 38.1-D-07 makes native UAT the blocking acceptance gate for the phase (alongside the already-green deterministic render-count instrumentation tests). Plans 38.1-01..38.1-05 are "automated-ready", not "done" (project memory: do not claim done before UAT) — only this checkpoint converts them into accepted behavior.

Output: a recorded verdict (approval, or a rejection list of failing step numbers routed to owning plans) in 38.1-06-SUMMARY.md. No production code, no test files, no other artifacts.

**Gate reminder:** NO code or test creation, modification, deletion, renaming, or execution inside this plan beyond the automated no-diff guard below. The 3 known-red D-15 tests remain expected pre-38-08 state, not a bug. The agent does not launch the server, browser, or native app (project CLAUDE.md) — the user runs everything natively.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md
@.planning/phases/38.1-studio-render-path-performance/38.1-VALIDATION.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>BLOCKING native UAT — canvas-first navigation, playback UI freeze, zero behavior change, preserved mechanisms (38.1-D-01..D-06, D-09, D-10)</name>
  <precondition>Plans 38.1-01 through 38.1-05 are complete with SUMMARYs on disk; the full app suite is green except exactly the 3 known-red D-15 tests (deterministic gate run per 38.1-05 Task 2) and typecheck is clean; the user has the native app running on their side with their CURRENT REAL PROJECT — the one that measured 5-10x slow after group paste (38.1-D-06). The agent never starts the server.</precondition>
  <read_first>
    - .planning/phases/38.1-studio-render-path-performance/38.1-01-PLAN.md — model split expectations: frame changes never rebuild the structural timeline model; structural edits (paste, key add/delete) still propagate.
    - .planning/phases/38.1-studio-render-path-performance/38.1-02-PLAN.md — primitive semantics behind the feel: at most one UI render per animation frame; latest-wins canvas by supersession only; discrete clicks always paint.
    - .planning/phases/38.1-studio-render-path-performance/38.1-03-PLAN.md — navigation expectations: canvas paints same-tick before any UI work; save-before-leave flush preserved; selection sync and parent frame-sync unchanged.
    - .planning/phases/38.1-studio-render-path-performance/38.1-04-PLAN.md — strip expectations: identical cell visuals (current ring, occupied fills, selection states, drag preview, tooltips) with cheap per-frame renders.
    - .planning/phases/38.1-studio-render-path-performance/38.1-05-PLAN.md — playback freeze expectations: the three live surfaces (canvas image, nav-pill <output class="physics-paint-current-frame">, Play/Stop state), frozen cells/capsule/tube-log, D-02 queue flush once on stop, one catch-up render.
    - .planning/phases/38.1-studio-render-path-performance/38.1-01-SUMMARY.md, 38.1-02-SUMMARY.md, 38.1-03-SUMMARY.md, 38.1-04-SUMMARY.md, 38.1-05-SUMMARY.md — what each production plan actually shipped (final symbol names, chosen mechanisms, any deviations from plan).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-PLAN.md — the structural template for this checkpoint and the 33-step script that will be re-run in phase 38 after this phase closes.
  </read_first>
  <what-built>
    Plans 38.1-01..38.1-05 (waves 1–3) delivered the full render-path restructure, verified pre-UAT by the deterministic vitest gates + typecheck only: the timeline-model structural/frame split with the D-07 spy-counter instrumentation test (38.1-01); the rAF UI flush scheduler and latest-wins navigation generation tokens (38.1-02); the canvas-first navigation pipeline with generation-guarded stale skips and structural memoization of the per-render O(N) derivations (38.1-03); the cheap strip render with byte-identical DOM contract (38.1-04); and the playback UI freeze with the three-surface live set, the D-02 event queue, and the single catch-up render on stop (38.1-05). This checkpoint supplies the human acceptance evidence for all of it — nothing further is built here.
  </what-built>
  <action>
Present the numbered UAT script below to the user verbatim (translated to French if the user prefers — the user's native UAT reports come in French), then WAIT for their explicit verdict. Do not run the app, do not open a browser, do not run vitest. Do not fix anything during this checkpoint: failures are recorded and routed back to the owning plan; this task's only output is the verdict.

--- USER-OWNED NATIVE UAT SCRIPT (Phase 38.1) ---

Setup: open YOUR current real project — the one that measured 5-10x slow after the group-paste work (38.1-D-06). Use the Physics Paint layer with the many Roto keys that felt sluggish before this phase.

A. Canvas-first navigation (owning plan: 38.1-03; primitives 38.1-01/38.1-02) — D-03/D-04/D-05/D-06
1. Click through real keys one at a time: the CANVAS updates instantly on EVERY click — the canvas responds before any UI estimation. It must feel instant on this project (the pre-phase build felt 5-10x slow here).
2. Click fast through many keys, as fast as you can: every click still paints the canvas; the UI (cells, capsule) may lag a frame but catches up immediately — no growing backlog, no runaway lag (D-04).
3. Drag-scrub across the timeline quickly: the canvas always shows where the playhead IS, never a stale frame behind your pointer; when you stop, canvas and UI agree on the same frame (D-05).
4. Navigate with the keyboard (arrow keys): same instant canvas feedback; selection follows exactly as before this phase.

B. Cached playback freeze (owning plan: 38.1-05) — D-01/D-06
5. Start cached playback (Play) at the project fps: playback runs at FULL project fps — smooth, no stutter, on this project.
6. During playback, ONLY three things move: the canvas image, the small current-frame number in the nav pill (next to the Play/Stop button), and the Play/Stop button state. The cells, the status capsule, and the tube/log sidebar are FROZEN for the whole playback (D-01).
7. The per-frame `Previewing {n} / {total}` capsule line no longer updates during playback — the nav-pill frame number is the live position indicator instead. This is the intended D-01 behavior, not a bug.

C. Stop behavior (owning plan: 38.1-05) — D-01/D-02
8. Stop playback: the UI comes back in ONE catch-up render — no flicker, no double flash, no two-step repaint.
9. If the playback had missing frames, the missing-frame line appears ONCE on stop, then the idle context line resumes (`Real Roto key · Frame {n}` / `Generated frame · Frame {n}` / `Empty frame · Frame {n}` per the current cell).
10. After stop, the cells, capsule, and tube/log are fully current for the stop frame — nothing stale from the frozen period.

D. Zero behavior change (owning plans: 38.1-01 model, 38.1-03 navigation, 38.1-04 strip, 38.1-05 playback) — D-09
11. Selection gestures: single click, Ctrl/Cmd-click multi-select, range select, collapse, Select All — all identical to the pre-phase build.
12. Capsule wording and arbitration: action feedback lines (Copy, Paste, rejects) win over the idle context line, then the idle context returns — identical wording everywhere.
13. Tooltips: 1000ms hover / instant keyboard focus / Escape; placement sides and the dark notch pointing at the control — identical.
14. Key drag with live preview, and the occupied/current/selected cell treatments — identical visuals.
15. Undo/Redo, and single-key + group Copy/Paste with the locked capsule lines (`Copied {N} keys`, `Pasted {N} keys`, `Paste rejected — key in the way`, `Paste rejected — not enough room`) — identical.
16. Scripts panel, and save/close/reopen with all keys and paint intact — identical.

E. Preserved mechanisms (owning plans: 38.1-04 strip, 38.1-05 playback) — D-10
17. Tooltips render fixed-in-place with the viewport-fixed notch pointing at their source control, including near window edges — same as the Phase 38-05 build.
18. The capsule keeps its single-owner arbitration with the ambient idle-context feed — the idle line follows the current cell exactly as Phase 38.

--- END OF SCRIPT ---

Record the verdict: "approved" if every step passes, or a list of failing step numbers with a short description. Each failing step routes back to exactly one owning plan: A → 38.1-03 (with 38.1-01/38.1-02); B/C → 38.1-05; D (model/selection) → 38.1-01, D (navigation) → 38.1-03, D (strip visuals) → 38.1-04, D (playback) → 38.1-05; E → 38.1-04/38.1-05. On rejection, this checkpoint does NOT retry or patch — the orchestrator replans the owning plan(s) and returns to this checkpoint afterward.
  </action>
  <how-to-verify>
    The user runs the numbered script above in the native app on their real project (they own the server/app — the agent never launches it). Expected results are stated inline per step. The fixture IS the user's current real project per 38.1-D-06; no synthetic fixture is substituted.
  </how-to-verify>
  <verify>
    <automated>git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*' 'app/src/**' | wc -l | grep -q '^ *0$'; echo "No code or test artifacts touched during this checkpoint."</automated>
    <human-check>The user reports either explicit approval of the full script (steps 1–18) or a rejection listing failing step numbers; the verdict is recorded in 38.1-06-SUMMARY.md.</human-check>
  </verify>
  <acceptance_criteria>
    - Explicit user approval recorded in 38.1-06-SUMMARY.md (approval closes the phase and unblocks the re-run of the 38-06 33-step script in phase 38).
    - Any rejection lists failing step numbers and names the owning plan(s) for each; no fixes attempted inside this checkpoint.
    - The verdict explicitly covers: canvas-first feel on the real project (A), the playback freeze with exactly three live surfaces (B), once-only missing-frame line + one catch-up render (C), zero behavior change (D), and tooltip/capsule preservation (E).
    - Zero production or test files created, modified, renamed, or executed during this checkpoint; the 3 known-red D-15 tests remain untouched and are treated as expected, not a bug.
    - The agent never launched the server, browser, or native app.
  </acceptance_criteria>
  <done>The user has explicitly approved the native UAT script on their real project (or rejection is recorded with owning-plan routing); the verdict is written to 38.1-06-SUMMARY.md; on approval Phase 38.1 closes and the 38-06 UAT re-run in phase 38 is unblocked.</done>
  <resume-signal>Type "approved" if all 18 steps pass, or list the failing step numbers with a short description of what you saw (screenshots welcome).</resume-signal>
</task>

</tasks>

## Artifacts this phase produces (this plan)

None. This checkpoint plan produces no files and modifies no code — its only output is the recorded user verdict in `38.1-06-SUMMARY.md` (state, not artifact).

Explicitly NOT produced here: any production change (a rejected step routes back to its owning plan — 38.1-01/38.1-02/38.1-03/38.1-04/38.1-05 — and is fixed there), any test file (the D-07 instrumentation tests already exist from waves 1–2; the 3 known-red D-15 tests are 38-08 scope).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Checkpoint-only plan: no code changes, no new surfaces, no trust boundaries crossed. The agent presents a script and records a verdict; the user owns the native runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-06-UAT | Tampering (process) | acceptance gate | medium | mitigate | The checkpoint is blocking and non-autonomous (`autonomous: false`); approval is the only condition that closes the phase; the acceptance criteria and verify guards forbid touching code or test artifacts, preserving the automated-ready-then-UAT order (38.1-D-07) |
| T-38.1-SC | Tampering | npm/pnpm installs | high | mitigate | Vacuous — no installs exist in this plan (Package Legitimacy Audit: not applicable per 38.1-RESEARCH.md) |
</threat_model>

<verification>
- Verdict recorded: 38.1-06-SUMMARY.md contains the explicit user approval or the rejection list with owning-plan routing.
- Discipline: `git diff --name-only` across this plan shows zero production and zero test files; no vitest invocation happened.
- Fixture fidelity: the verdict explicitly states the script ran on the user's current real project (38.1-D-06), not a synthetic fixture.
- Gate semantics: approval is the only condition that closes Phase 38.1 and unblocks the 38-06 UAT re-run in phase 38; a rejection keeps the phase open and routes work back to the owning plan(s).
</verification>

<success_criteria>
- The user explicitly approved the full 18-step native UAT script on their real project, and the approval is recorded in 38.1-06-SUMMARY.md.
- Canvas-first navigation feels instant on the project that previously measured 5-10x slow (38.1-D-03/D-06).
- Cached playback runs at full project fps with the frozen UI and the three-surface live set (38.1-D-01); stop produces one catch-up render and the once-only missing-frame line (38.1-D-02).
- Zero behavior change confirmed across the full interaction surface (38.1-D-09); tooltip and capsule mechanisms preserved verbatim (38.1-D-10).
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-06-SUMMARY.md` when done, recording: the user's verbatim verdict (approved, or failing step numbers + descriptions), the owning-plan routing for any rejection, confirmation that the fixture was the user's real project (38.1-D-06), confirmation that sections A–E were all covered, and confirmation that zero code/test files were touched.
</output>
