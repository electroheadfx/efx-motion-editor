---
phase: 38.1-studio-render-path-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts
  - app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts
autonomous: true
requirements:
  - 38.1-D-04
  - 38.1-D-05
  - 38.1-D-07

must_haves:
  truths:
    - "The UI flush scheduler coalesces N same-frame schedule() calls into AT MOST ONE flush per animation frame (38.1-D-04) — a scheduled-flag guard, never a cancel-and-repost loop that starves the UI under continuous scrub (RESEARCH State of the Art)."
    - "Each flush reads the LATEST state at fire time, so fast clicking never queues more than one UI render behind the canvas (38.1-D-04)."
    - "The navigation generation token helper marks every superseded navigation as stale: only the newest generation is allowed to paint the canvas (38.1-D-05); a discrete key-click whose generation was never superseded ALWAYS paints (discrete-click rule)."
    - "Both utilities are pure, synchronous, DOM-free, and testable in node with a stubbed requestAnimationFrame — no new hook layer, no new dependency (project CLAUDE.md signals-native constraint)."
  artifacts:
    - "app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts — NEW small utility module exporting the scheduled-flag rAF flush scheduler and the monotonic navigation generation token helper."
    - "app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts — NEW node-env .test.ts with stubbed-rAF flush-count and generation-supersession assertions (38.1-D-07 scheduler half)."
  key_links:
    - "Scheduler -> Studio nav pipeline: plan 38.1-03 routes setLaunchContext frame propagation through schedule() while the canvas paints same-tick — this module is the D-04 gate."
    - "Generation tokens -> canvas paint: plan 38.1-03 checks isLatest(generation) after the awaited flushLivePixels so a superseded navigation never paints a stale frame (RESEARCH Pitfall 5)."
---

<objective>
Create the two small pure utilities the Studio navigation pipeline (plan 38.1-03) is built from: (1) a scheduled-flag requestAnimationFrame UI flush scheduler implementing 38.1-D-04 (at most one Preact UI flush per animation frame, latest-state-at-fire-time), and (2) a monotonic navigation generation token helper implementing 38.1-D-05 (latest-wins canvas under saturation; discrete clicks always paint). Cover both with the scheduler half of the D-07 instrumentation test.

Purpose: D-04 explicitly rejects debounce/cancel-and-repost idioms (they starve the UI under continuous input); the scheduled-flag guard is the sanctioned project idiom (PhysicsPaintWorkflowStrip.tsx:653-684 edge-scroll loop). Building and testing these primitives in isolation keeps plan 38.1-03 a pure wiring task.

Output: one new utility module + one new test file, both green under vitest run in node env; zero production callers yet (wired in 38.1-03).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md
@.planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md

@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scheduled-flag rAF flush scheduler — RED: stubbed-rAF coalescing tests; GREEN: implement (38.1-D-04, D-07 scheduler)</name>
  <files>app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts, app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts</files>
  <reversibility rating="reversible">New leaf utility with no callers until 38.1-03; deleting the file reverts completely.</reversibility>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:653-684 — the edge-scroll rAF loop: window.requestAnimationFrame(tick) with a null-id scheduled-flag guard. THIS is the copy source for style and idiom (38.1-D-04 hard constraint from the planning context).
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts:50 — the project's vi.stubGlobal('requestAnimationFrame', ...) stub idiom.
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §rotoUiFlushScheduler.ts (target shape; the sidebar color-picker component is the named anti-analog — do not copy its cancel-and-repost style).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pattern 2 (scheduled-flag batch), Don't Hand-Roll (timer throttles fire off-vs-cycle), State of the Art (rejected debounce variant).
  </read_first>
  <behavior>
    - Test 1: scheduling 5 flushes synchronously, then firing the pending rAF callback once, invokes the flush callback EXACTLY ONCE (flush counter === 1).
    - Test 2: after the pending callback fires, the scheduler is re-armable — a new schedule() posts a new rAF and the second fire invokes the flush again (no permanent latch).
    - Test 3: latest-state-at-fire-time — schedule twice with different payloads where the flush reads a shared mutable slot; the single fire observes the SECOND payload (never the first, never both).
    - Test 4: a disposal/cancel path (e.g. dispose() before fire) prevents the pending flush from running and resets the guard so later schedule() calls work — needed for Studio unmount safety.
    - Test 5 (source guard): the module never defers UI until input settles — no timeout-based settling, and a second schedule() while one is pending does NOT postpone the already-posted frame (the pending rAF id is never revoked to be re-posted).
  </behavior>
  <action>
    RED: create app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts (node env, MUST be .test.ts per app/vitest.config.ts include; no DOM). Stub requestAnimationFrame with a manual queue: vi.stubGlobal('requestAnimationFrame', vi.fn((cb) => { pending.push(cb); return pending.length; })) plus a matching cancel stub recording revocations, modelled on physicsPaintRotoPlayScriptRenderer.test.ts:50 but driving callbacks MANUALLY (not immediately) so coalescing is observable. Write the five behavior tests against the module's intended export (suggested factory name createRotoUiFlushScheduler returning { schedule(flush: () => void): void, dispose(): void, isPending(): boolean } — exact names are executor latitude, the semantics are not). Confirm RED (module does not exist). Commit the failing test.

    GREEN: create app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts implementing the scheduled-flag guard in the PhysicsPaintWorkflowStrip.tsx:653-684 idiom: a module-or-closure-held pending id; schedule() returns immediately when a flush is already posted (guard check first — that ordering is the D-04 contract); the rAF callback clears the flag BEFORE invoking flush so a flush that itself schedules work re-arms cleanly; dispose() revokes the pending frame through the stubbed cancel global and clears the flag. Pure and synchronous; no Preact imports, no signals, no hooks, no timers other than rAF. Never revoke a posted frame in order to re-post it later — that settling behavior is the explicitly rejected anti-pattern for D-04 (do not transcribe the rejected idiom's method name into this module, including in comments — a negative source gate checks for it).
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts && grep -c "requestAnimationFrame" app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior tests pass under pnpm --dir app vitest run; the flush counter in Test 1 is exactly 1 across 5 synchronous schedule() calls.
    - The test file's cancel-stub records ZERO revocations during the coalescing tests (Test 5 proves the pending frame is never revoked-and-reposted).
    - The module contains a guard-check-then-post structure (guard read before any rAF post in schedule()), a disposal path, and zero Preact/signals/hooks imports (grep import lines: only type imports or none).
    - No setTimeout/setInterval anywhere in the module (grep count 0).
    - Node env only: no jsdom/happy-dom import, no new vitest config, file is .test.ts.
  </acceptance_criteria>
  <done>The scheduler coalesces any burst of schedule() calls into at most one flush per animation frame with latest-state-at-fire-time semantics, is re-armable and disposable, matches the project's scheduled-flag rAF idiom, and is test-locked in node env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Latest-wins navigation generation tokens — RED: supersession tests; GREEN: implement (38.1-D-05)</name>
  <files>app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts, app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:717-746 — navigateToSyncedPhysicalFrame: the await rotoPersistence.flushLivePixels(currentFrame) at :723 is where a stale navigation can outlive its welcome; the generation check belongs after such awaits (RESEARCH Pitfall 5).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pattern 2 (monotonic nav generation; discrete-click rule), Pitfall 5 (canvas-first ordering broken by the save flush).
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §rotoUiFlushScheduler.ts (generation token target shape).
  </read_first>
  <behavior>
    - Test 1: begin() returns strictly increasing generation tokens (1, 2, 3...).
    - Test 2: isLatest(token) is true only for the most recent begin() token; every earlier token reports false once a newer begin() has occurred.
    - Test 3 (discrete-click rule, 38.1-D-05): a token that was never superseded remains latest indefinitely — skipping applies only when a NEWER navigation exists, never to the newest intent.
  </behavior>
  <action>
    RED: extend rotoUiFlushScheduler.test.ts with the three generation-token tests against the intended export (suggested factory name createRotoNavigationGeneration returning { begin(): number, isLatest(generation: number): boolean } living in the same small utility module — exact names are executor latitude). Confirm RED.

    GREEN: implement the helper in rotoUiFlushScheduler.ts as a monotonically increasing counter closure — begin() increments and returns the new generation; isLatest(generation) compares against the current counter. Pure, synchronous, no imports. Add a one-line doc comment citing the discrete-click rule (38.1-D-05): a generation that is still latest always paints; only superseded generations skip. Do not add time-based or distance-based staleness heuristics — supersession by a newer navigation is the ONLY staleness criterion (any heuristic would drop discrete clicks, violating D-05).
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three generation tests pass; tokens are strictly increasing; only supersession (never time, distance, or count heuristics) marks a token stale.
    - The module exports both the scheduler and the generation helper; the file stays free of Preact/signals/hooks imports and timer APIs other than requestAnimationFrame.
    - Full new-file test run green; pnpm --dir app typecheck clean.
  </acceptance_criteria>
  <done>The generation helper gives plan 38.1-03 a test-locked latest-wins canvas primitive: newest navigation intent always paints, superseded intents are skipped, discrete clicks are never dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigation intent -> canvas paint | a stale async navigation that paints after being superseded would show a frame the playhead has left (visible-state integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-02-01 | Tampering | generation token helper | high | mitigate | Monotonic supersession is the ONLY staleness criterion; Test 3 locks the discrete-click rule so no intent is silently dropped |
| T-38.1-02-02 | Denial of Service | flush scheduler | medium | mitigate | Scheduled-flag guard caps UI flushes at 1/frame under arbitrary input burst; Test 1 counter assertion locks the bound |
| T-38.1-SC | Tampering | package installs | high | mitigate | No installs — module uses only platform requestAnimationFrame (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- pnpm --dir app vitest run src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts — all scheduler + generation tests green
- pnpm --dir app typecheck — clean
</verification>

<success_criteria>
- 38.1-D-04 primitive exists and is test-proven: at most one UI flush per animation frame, latest-state-at-fire-time, never settling-deferred.
- 38.1-D-05 primitive exists and is test-proven: latest-wins by monotonic supersession only; discrete clicks always paint.
- Plan 38.1-03 can consume both as pure wiring with no further utility design.
</success_criteria>

## Artifacts this phase produces

- `app/src/components/physic-paint/hooks/rotoUiFlushScheduler.ts` — NEW utility module.
- `createRotoUiFlushScheduler()` — NEW export: scheduled-flag rAF flush scheduler with schedule/dispose/isPending (exact member names may vary).
- `createRotoNavigationGeneration()` — NEW export: monotonic navigation generation tokens with begin/isLatest (exact member names may vary).
- `app/src/components/physic-paint/hooks/rotoUiFlushScheduler.test.ts` — NEW test file (38.1-D-07 scheduler half + generation supersession).

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-02-SUMMARY.md` when done
</output>
