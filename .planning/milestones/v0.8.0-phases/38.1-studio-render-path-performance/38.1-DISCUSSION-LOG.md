# Phase 38.1: Studio render-path performance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-27
**Phase:** 38.1-studio-render-path-performance
**Areas discussed:** Playback UI scope, Scrub update policy, Acceptance bar, Surgery scope

---

## Playback UI scope

| Option | Description | Selected |
|--------|-------------|----------|
| Canvas + frame number only | Canvas plays at full fps; only a minimal frame indicator and play/stop state live; cells/capsule/tube-log frozen until stop, then one catch-up render | ✓ |
| Canvas + frame + capsule | Status capsule playback line also stays live | |
| Throttled full UI | Everything updates 2-4x/sec during playback | |

**User's choice:** Canvas + frame number only

| Option | Description | Selected |
|--------|-------------|----------|
| Queue, show on stop | Missing-frame events queued during playback, shown once on stop, then idle context resumes | ✓ |
| Drop entirely | Never surfaced during/after playback | |
| Live exception | Capsule live only for missing-frame events | |

**User's choice:** Queue, show on stop (preserves Phase 38 D-10 deferred, not dropped)

---

## Scrub update policy

| Option | Description | Selected |
|--------|-------------|----------|
| Canvas first, UI same-tick | Strict ordering only, no deferral | |
| Canvas + rAF-batched UI | Canvas instant; UI batched to max 1 render per animation frame regardless of click rate | ✓ |
| Canvas + settle-deferred UI | UI catches up only when scrubbing settles (100-150ms) | |

**User's choice:** Canvas + rAF-batched UI

| Option | Description | Selected |
|--------|-------------|----------|
| Paint every frame | Every visited frame paints in order | |
| Latest-wins canvas | Skip stale queued paints; canvas always shows current playhead | ✓ |
| Clicks exact, drags latest-wins | Two policies | |

**User's choice:** Latest-wins canvas

---

## Acceptance bar

| Option | Description | Selected |
|--------|-------------|----------|
| Your current project | The real project that measured 5-10x slow becomes the UAT fixture | ✓ |
| Synthetic 50-key fixture | Repeatable stress fixture built via group paste | |
| Both | | |

**User's choice:** Your current project

| Option | Description | Selected |
|--------|-------------|----------|
| UAT feel only | No new test code | |
| UAT + render-count guard | Native UAT blocking + deterministic render-count instrumentation test | ✓ |
| UAT + guard + timing budget | Adds ms-budget assertions (flaky) | |

**User's choice:** UAT + render-count guard

---

## Surgery scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full per-frame path | Model + strip + view-model + tube/log in one pass, one UAT | ✓ |
| Model + strip only | Hot spots only; tube/log later if still laggy | |

**User's choice:** Full per-frame path

| Option | Description | Selected |
|--------|-------------|----------|
| Timing-only, zero behavior change | Any behavior delta is a bug and fails UAT | ✓ |
| Timing-first, flagged adjustments | Small UX adjustments allowed with UAT callouts | |

**User's choice:** Timing-only, zero behavior change

---

## Claude's Discretion

- Timeline-model decoupling mechanism (structural signals vs current-frame computeds)
- rAF batching mechanism (Preact-signals-native per project CLAUDE.md)
- Playback freeze mechanism (subscription gating vs render short-circuit)
- Plan boundaries and test placement

## Deferred Ideas

- Synthetic 50-key stress fixture for repeatable perf UAT (revisit if perf UATs recur)
- Timing-budget CI assertions (rejected as flaky)
- Roto cache footprint reduction (already tracked from 36.x; separate concern)
