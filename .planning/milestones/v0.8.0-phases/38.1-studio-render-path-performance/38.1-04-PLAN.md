---
phase: 38.1-studio-render-path-performance
plan: 04
type: execute
wave: 2
depends_on: [38.1-01]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: true
requirements:
  - 38.1-D-04
  - 38.1-D-08
  - 38.1-D-09
  - 38.1-D-10

must_haves:
  truths:
    - "A pure currentFrame change (stable rotoPhysicalCells identity, guaranteed by 38.1-01) recomputes cell view models for AT MOST the previously-current and newly-current cells — the other ~118 cells reuse cached derivations (38.1-D-08 link 2; RESEARCH Pattern 3)."
    - "physicalCellByAppFrame (:331-334) and keyIdByAppFrame (:425-429) memos survive frame changes — their dependency identities no longer churn per frame (RESEARCH Pitfall 2)."
    - "rotoDragValidityKey (:433) is no longer string-built on every render — it recomputes only when its structural inputs change (38.1-D-08 link 2)."
    - "Every data-roto-app-frame, data-roto-kind, data-roto-key-id, class hook, aria string, tooltip child, handler snippet, and guard expression asserted by PhysicsPaintWorkflowStrip.test.ts (55 source-grep assertions) is BYTE-IDENTICAL after the refactor — moves are fine, mutations are not (38.1-D-09 + RESEARCH Pitfall 1)."
    - "The capsule single-owner arbitration with ambient idle-context feed (:455-467) and the 38-05 tooltip fixed-in-place mechanism are preserved verbatim (38.1-D-10); the known-red placement source-grep stays red, untouched."
  artifacts:
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — per-cell derivation memoized against (frame, currentFrame, stable structural inputs); drag validity key behind a structural memo; DOM output byte-identical for identical inputs."
  key_links:
    - "38.1-01 identity contract -> strip memos: stable rotoPhysicalCells across a pure frame change is what makes every memo in this plan effective; if identities churn, nothing here helps (verify first)."
    - "Strip render cost -> D-04 budget: with renders capped at 1/frame by 38.1-03 and per-render work made O(changed-cells) here, a scrub frame costs two cell derivations instead of 120 x O(cachedFrames)."
---

<objective>
Make the workflow strip's per-frame render cheap: exploit 38.1-01's stable structural identities so a pure frame change recomputes only the two affected cells' view models, and move the O(N) rotoDragValidityKey string build behind a structural memo — while keeping every source-grep-asserted literal and the locked capsule/tooltip mechanisms byte-identical (38.1-D-09/D-10).

Purpose: today each Studio render re-derives all ~120 cells (each getRotoCellViewModel call scanning cachedFrames) plus an O(N) drag-validity string. The model split makes the identities stable; this plan collects the win without touching the DOM contract the 55 source-grep assertions pin.

Output: one modified file; PhysicsPaintWorkflowStrip.test.ts green with exactly the same known-red set as before (the placement source-grep stays red by design); typecheck clean.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md
@.planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md
@.planning/phases/38.1-studio-render-path-performance/38.1-01-SUMMARY.md

@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Structural memo for rotoDragValidityKey + identity verification gate (38.1-D-08 link 2)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <precondition>38.1-01 SUMMARY exists and its D-07 test proves rotoTimelineModel.physicalCells.value is reference-stable across frame writes — the Studio passes that stable array as props.rotoPhysicalCells (:1099 area of PhysicsPaintStudio.tsx).</precondition>
  <reversibility rating="reversible">Pure memoization change inside one component body; reverting is a local rollback.</reversibility>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:321-360 (component top: currentPhysicalCells, physicalCellByAppFrame, generatedRotoFrames, realCachedRotoFrames, frameCells memos), :424-442 (keyIdByAppFrame, rotoSelectedKeyIdSet, rotoDragValidityKey at :433, rotoDragVacatedAppFrames), :443-467 (capsule derivation — DO NOT TOUCH, 38.1-D-10).
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — grep this file for every literal asserted in the regions you will touch (validity key construction, memo blocks); those literals must survive verbatim.
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pitfall 1 (source-grep contract), Pitfall 2 (dep-array identity churn).
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §PhysicsPaintWorkflowStrip.tsx (identity-sensitive memos excerpt; per-render O(N) string key excerpt).
  </read_first>
  <action>
    Step A — verify the identity contract holds: confirm props.rotoPhysicalCells arrives with reference-stable identity across a pure frame change (38.1-01 guarantees the model side; the Studio passes rotoTimelineModel.physicalCells.value directly at PhysicsPaintStudio.tsx:1099 — no intermediate mapping). If a per-frame mapping exists at the Studio call site, note it in the SUMMARY as a deviation for 38.1-05 follow-up; do not edit the Studio in this plan.

    Step B — rotoDragValidityKey (:433): wrap the template-string construction in useMemo with dependency array [props.rotoDragContextKey, frameCells, currentPhysicalCells, rotoDragLocked, rotoKeyRecords] — the exact values the string interpolates, no more, no less (Pitfall 2: adding a frame-derived value here re-churns it per frame; omitting a real input makes drag validity stale). The produced string for identical inputs must be byte-identical to today's.

    Step C — audit the neighboring memos: physicalCellByAppFrame (:331), generatedRotoFrames (:335), realCachedRotoFrames (:340), keyIdByAppFrame (:425), rotoSelectedKeyIdSet (:432), rotoDragVacatedAppFrames (:439) — their dependency arrays must reference ONLY structural identities that 38.1-01 made stable. Fix any dep array that sneaks in props.currentFrame or a frame-derived array (frameCells legitimately depends on props.currentFrame at :344 — the visible window follows the playhead; leave it, and note that physicalCellByAppFrame/keyIdByAppFrame do NOT depend on frameCells and must stay that way).

    Scope guards: do NOT touch the capsule block (:443-467), any tooltip mount or region prop, the RotoTimelineCellButton component (:266-319 — Task 2 scope), or the cell render loop (:1051-1113 — Task 2 scope). No test files. Every asserted literal verbatim.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts 2>&1 | tee /tmp/38.1-04-t1-strip.txt >/dev/null; grep -E "Tests +1 failed \|" /tmp/38.1-04-t1-strip.txt && test "$(grep -cE '^ *FAIL ' /tmp/38.1-04-t1-strip.txt)" -eq 1 && grep -E '^ *FAIL ' /tmp/38.1-04-t1-strip.txt | grep -q 'places header tooltips below their anchors' && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - rotoDragValidityKey is computed inside useMemo with exactly the five-input dependency array listed above; the string format is unchanged.
    - PhysicsPaintWorkflowStrip.test.ts reports the SAME result set as the pre-plan baseline: all previously-green assertions green, the one known-red placement source-grep still red and unmodified.
    - No dependency array in the touched memos contains props.currentFrame or a frame-derived array except the pre-existing frameCells memo at :344.
    - git diff shows zero changed lines in the capsule derivation block (:443-467 region) and zero test file changes; typecheck clean.
  </acceptance_criteria>
  <done>The O(N) validity string and all structural memos recompute only on structural input changes; the strip's DOM contract and the locked capsule/tooltip mechanisms are byte-identical; the known-red test set is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fine-grained per-cell derivation — a frame change recomputes only old+new current cells (38.1-D-08 link 2, RESEARCH Pattern 3)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:266-319 (RotoTimelineCellButton — the existing narrow-child pattern; its props interface is the memoization boundary), :344-356 (frameCells, currentSemanticCell, currentCellFrames, currentRotoCell — the parent's own per-frame reads), :1051-1113 (the cell render loop: per-cell getRotoCellViewModel call, drag preview lookups, tooltip kind derivation, cellClass string assembly at :1099).
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — getRotoCellViewModel signature and cost (per-call cachedFrames scan): this stays pure and unchanged; only WHEN it runs changes.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — grep every literal asserted inside the cell loop and RotoTimelineCellButton (data-roto-* attributes, class hooks, aria strings, handler snippets like props.onAddRotoKey?.()): all must survive verbatim.
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pattern 3 (fine-grained current-cell subscription; hard constraint: DOM byte-identical), Pitfall 1.
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §PhysicsPaintWorkflowStrip.tsx (cell render loop excerpt; fine-grained child-component pattern).
  </read_first>
  <action>
    Make per-cell derivation cheap across a pure frame change using ONE of these mechanisms (executor's choice — record the choice and rationale in the commit message; the invariants below are not negotiable):

    Option A — per-cell view-model cache: a component-scoped memo cache (e.g. a useRef-held Map keyed by frame) that stores each cell's derived bundle (view model + cellClass + tooltip kind + drag-preview lookups); on each render, recompute ONLY entries whose inputs changed: the cell at the previous currentFrame, the cell at the new currentFrame, cells affected by an active drag preview, and everything when a structural input (physicalCellByAppFrame, cachedFramesForCell, rotoSelectedKeyIdSet, drag state) changes identity. Invalidate the whole cache on any structural identity change.

    Option B — narrow child with stable props: move the per-cell derivation INTO RotoTimelineCellButton (or a new intermediate child) so the parent map passes stable structural props plus the frame number and current marker; wrap the child in preact/compat memo ONLY IF every prop can be made referentially stable across a frame change (handlers included — inline closures in the loop defeat memo; stabilizing them is part of this option). Do NOT read a frame signal inside each cell to subscribe all 120 cells to it (that re-renders every cell per frame — the opposite of the goal); the current marker may flow as a plain boolean prop computed by the parent.

    Invariants (both options): the rendered DOM for identical inputs is BYTE-IDENTICAL to today — same data-roto-app-frame / data-roto-kind / data-roto-key-id attributes, same class hooks in the same order within cellClass, same aria-label / aria-selected strings, same tooltip children, same handler wiring; the parent's own current-frame derivations (:352-356: currentSemanticCell, currentCellFrames, currentRotoCell) keep feeding the capsule and status surfaces exactly as today (that block is NOT in scope for caching beyond Task 1's work); drag preview visuals, occupied/selected/current treatments, and tooltip copy are computed by the same pure helpers with the same arguments.

    Explicit non-goals: no signal subscriptions inside cells; no restructuring of the capsule/tooltip mounts; no changes to buildPhysicsPaintRotoFrameCells or the window-follows-playhead behavior; no edits to physicsPaintWorkflowPresentation.ts; no test file edits.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts 2>&1 | tee /tmp/38.1-04-t2-strip.txt >/dev/null; grep -E "Tests +1 failed \|" /tmp/38.1-04-t2-strip.txt && test "$(grep -cE '^ *FAIL ' /tmp/38.1-04-t2-strip.txt)" -eq 1 && grep -E '^ *FAIL ' /tmp/38.1-04-t2-strip.txt | grep -q 'places header tooltips below their anchors' && pnpm --dir app vitest run src/components/physic-paint/view 2>&1 | tee /tmp/38.1-04-t2-view.txt >/dev/null; grep -E "Tests +3 failed \|" /tmp/38.1-04-t2-view.txt && test "$(grep -cE '^ *FAIL ' /tmp/38.1-04-t2-view.txt)" -eq 3 && test "$(grep -E '^ *FAIL ' /tmp/38.1-04-t2-view.txt | grep -vcE 'physicsPaintWorkflowPresentation\.test\.ts|PhysicsPaintWorkflowStrip\.test\.ts')" -eq 0 && pnpm --dir app typecheck</automated>
    <human-check>Visual parity of cells (current ring, occupied fills, selection states, drag preview, tooltips) is exercised in the 38.1-06 blocking native UAT as part of the zero-behavior-change sweep.</human-check>
  </verify>
  <acceptance_criteria>
    - The commit message records which mechanism (A or B) was chosen and why.
    - A pure currentFrame change with stable structural inputs recomputes per-cell derivations for at most the previously-current and newly-current cells (plus active drag-preview cells); the derivation path for the other cells serves cached/unchanged values — stated and evidenced in the SUMMARY (e.g. instrumented count during a manual node probe or a code-path argument tied to the cache/memo keys).
    - PhysicsPaintWorkflowStrip.test.ts: same baseline result set — all previously-green assertions green, the known-red placement source-grep still red and unmodified; the full view suite otherwise green.
    - grep the diff: zero changed lines in RotoTimelineCellButton's rendered attribute/aria/tooltip JSX (unless Option B moved derivation INTO it — in which case the JSX literals themselves are unchanged, only their computation site), zero changed lines in the capsule block, zero test file changes.
    - typecheck clean; the 3 known-red D-15 tests untouched.
  </acceptance_criteria>
  <done>A frame change costs two cell derivations instead of 120 scans; the strip's rendered DOM and source-grep contract are byte-identical; the mechanism choice and its evidence are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model computeds -> strip render props | stale cached derivations would render the wrong cell state (visible-state integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-04-01 | Tampering | per-cell derivation cache/memo | high | mitigate | Full invalidation on ANY structural identity change; recompute keyed on previous+new currentFrame; DOM byte-identity checked by the 55-assertion source-grep suite plus UAT visual sweep |
| T-38.1-04-02 | Denial of Service | strip render loop | medium | mitigate | This plan IS the mitigation: per-frame work drops from O(cells x cachedFrames) to O(changed cells); validity-key memo removes the per-render O(N) string build |
| T-38.1-SC | Tampering | package installs | high | mitigate | No installs — preact/compat memo (if Option B) is already a project dependency used by 38-05 |
</threat_model>

<verification>
- pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — baseline result set preserved (green stays green, one known-red stays red)
- pnpm --dir app vitest run — full suite: exactly 3 failures, all known-red D-15
- pnpm --dir app typecheck — clean
</verification>

<success_criteria>
- Strip per-frame render cost is O(changed cells) with stable structural identities (38.1-D-08 link 2).
- rotoDragValidityKey recomputes only on structural input changes.
- Every source-grep-asserted literal, the capsule arbitration, and the tooltip mechanism are byte-identical (38.1-D-09/D-10).
</success_criteria>

## Artifacts this phase produces

- No new files and no new exported symbols — memoization and (optionally) one intermediate unexported child component inside `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx`. If Option B introduces a named child, it is file-local and unexported.

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-04-SUMMARY.md` when done
</output>
