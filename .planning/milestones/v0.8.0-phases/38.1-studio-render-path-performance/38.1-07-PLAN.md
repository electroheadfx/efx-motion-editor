---
phase: 38.1-studio-render-path-performance
plan: 07
type: execute
wave: 3
depends_on: [38.1-03, 38.1-04]
gap_closure: true
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.previewBaseImageCache.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 38.1-D-03
  - 38.1-D-07
  - 38.1-D-08
  - 38.1-D-09

must_haves:
  truths:
    - "Per-navigation store reads are O(1) in total key count: repeated getRotoPhysicalProjection / getRotoPhysicalContentRevision / getRotoPhysicalRenderSource calls across many frames trigger ZERO recomputes of projectPhysicPaintRotoPhysicalTimeline and buildPhysicPaintRotoPhysicalRevision (spy-proven, 38.1-D-07 idiom), and repeated projection reads return the SAME object reference."
    - "Each structural mutation class — add key, delete key, move key, payload write (updateRotoPhysicalRealKeyPayload), interpolation change (setRotoPhysicalInterpolationState), undo/redo (replaceRotoPhysicalRecords round-trip, the publish path rotoMoveHistory uses) — triggers exactly ONE recompute on the first post-mutation read, and the recomputed projection and content revision are byte-identical to a fresh compute from store inputs (38.1-D-09)."
    - "Selection writes (setRotoPhysicalSelection) and cursor/frame navigation trigger ZERO recomputes — structural invalidation keys on records/interpolation/capacity identity only, never on selection."
    - "Revisiting a frame whose dataUrl was already painted through the engine's setPreviewBaseImageUrl path performs ZERO new Image decodes (cached decoded image applied under the same requestId/destroyed/animationMode/drawing guards); resetBackground with unchanged background inputs performs no drawBg/redraw work; all pixels remain identical to the uncached behavior (38.1-D-09)."
    - "navigateToSyncedPhysicalFrame paints the destination canvas in the same synchronous tick as the navigation intent, BEFORE awaiting flushLivePixels (38.1-D-03 strengthened); the flush itself is always initiated and awaited afterward for error surfacing with the verbatim error path — never skipped, never weakened (save-before-leave, RESEARCH Pitfall 5); on flush completion, if the navigation generation is still latest AND the destination cell is generated, the destination paint re-issues once to pick up the flushed neighbor."
    - "The 3 known-red D-15 tests (2 physicsPaintWorkflowPresentation + 1 PhysicsPaintWorkflowStrip placement source-grep) fail exactly as before — untouched, deterministic failure-count gate per the 38.1-01 Task 2 idiom."
  artifacts:
    - "app/src/stores/physicPaintStore.ts — per-layer structural memo for the physical projection + content revision, validity keyed on (recordMap identity, frozen interpolation identity, capacity); getRotoPhysicalProjection / getRotoPhysicalContentRevision read through it; getRotoPhysicalRenderSource unchanged in semantics."
    - "app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts — NEW node-env invalidation-matrix + navigation zero-recompute + byte-identity suite (no jsdom, no test config changes)."
    - "packages/efx-physic-paint/src/engine/EfxPaintEngine.ts — decoded-Image cache keyed by dataUrl in setPreviewBaseImageUrl; input/identity-guarded early-return in resetBackground."
    - "packages/efx-physic-paint/src/engine/EfxPaintEngine.previewBaseImageCache.test.ts — NEW engine test: decode-cache hit/miss and resetBackground skip/execute matrix with stubbed Image."
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — paint-before-flush reorder in navigateToSyncedPhysicalFrame with post-flush generated-cell repaint."
  key_links:
    - "Structural validity = identity triple (recordMap, interpolation object, capacity): every mutation site in the store REPLACES the inner record Map (1159, 1197, 1435, hydration 795), the frozen interpolation object (1309, 1160, 1198, 796), or the capacity primitive (1169, 1202, 800) — the memo needs no explicit invalidation hooks and can never serve stale data; any future in-place recordMap.set after install would silently break this and is grep-gated."
    - "Cached revision string -> cacheRevision composition: getRotoPhysicalRenderSource builds `${contentRevision}:real:...` / `:generated:...` cacheRevision strings consumed by the persistence coordinator (useRotoFramePersistenceCoordinator.ts) and previewRenderer — the memoized revision MUST be the identical string to a fresh buildPhysicPaintRotoPhysicalRevision, or acknowledged-edit transactions reject (byte-identity test-locked)."
    - "Paint-before-flush -> live-pixel capture: flushLivePixels (useRotoFramePersistenceCoordinator.ts:245-249) flushes ALREADY-CAPTURED live-pixel transaction buffers and parent deliveries — it captures no engine pixels at await time, so initiating it before engine.clear() cannot save cleared pixels; the executor asserts this by reading flush() before reordering."
---

<objective>
Close the store-side per-navigation O(total key count) defect — the store-side twin of 38.1-01's structural/frame split. `getRotoPhysicalRenderSource` (physicPaintStore.ts:1360), the per-navigation display lookup, currently rebuilds on EVERY call: `getRotoPhysicalProjection` (1331) re-sorts all records and re-projects every cell across capacity, and `getRotoPhysicalContentRevision` (1346) canonical-encodes and hashes ALL records INCLUDING multi-MB PNG dataUrl payloads via `buildPhysicPaintRotoPhysicalRevision` (physicsPaintRotoPhysicalModel.ts:595). User-measured: 2 keys = real-time, 9 = reasonable, 18 = ~1s per navigation. Make both structurally cached (recompute only on key structure/content change, never on navigation or selection reads), plus the approved secondary engine moves (decoded-Image cache, resetBackground skip) and the approved paint-before-flush reorder in the Studio navigation pipeline.

Purpose: the render-path plans (38.1-01..05) removed per-frame O(N) work in the UI layer, but every navigation still pays O(N)-over-keys (with N-key payload hashing) in the STORE read path — this is now the dominant per-navigation cost at realistic key counts and fails the D-06 "same project feels instant" bar.

Output: per-layer structural memo in physicPaintStore with a node-env invalidation-matrix test suite; engine decode cache + resetBackground skip with an engine test; Studio navigation reorder; full suite green except exactly the 3 known-red D-15 failures.

Diagnosis is LOCKED (orchestrator): do not re-diagnose, do not re-profile. The fix requirements 1-9 in the planning context are hard constraints.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md
@.planning/phases/38.1-studio-render-path-performance/38.1-01-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-03-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (RED then GREEN): per-layer structural cache for the physical projection + content revision in physicPaintStore</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts:1331-1408 (getRotoPhysicalProjection / getRotoPhysicalContentRevision / getRotoPhysicalRenderSource — the read path being cached; note the absent-layer null semantics at 1335 and 1347)
    - app/src/stores/physicPaintStore.ts:1115-1174 (replaceRotoPhysicalRecords — add/delete/move/undo/redo publish path; replaces recordMap at 1159, interpolation at 1160, capacity at 1169; early-return on equal revisions at 1154)
    - app/src/stores/physicPaintStore.ts:1177-1209 (replaceRotoPhysicalDocument), 1303-1316 (setRotoPhysicalInterpolationState — replaces the frozen interpolation object at 1309), 1411-1440 (updateRotoPhysicalRealKeyPayload — payload write; replaces recordMap at 1435), 1446-1456 (clearRotoPhysicalRecords), 1232-1243 (setRotoPhysicalSelection — touches ONLY _rotoPhysicalSelectedKeyId/_rotoPhysicalCursorAppFrame; must NOT invalidate)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts:595-632 (buildPhysicPaintRotoPhysicalRevision + encodePhysicPaintRotoPhysicalContent — pure, deterministic, dataUrl-inclusive)
    - app/src/components/physic-paint/hooks/useRotoTimelineModel.test.ts (38.1-01 D-07 spy-counter idiom: vi.spyOn on the resolver module namespace, baseline after warm-up, zero-call assertion)
    - app/src/stores/physicPaintStore.test.ts:1-35 (store test harness: imports, beforeEach reset pattern, frozen fixture records)
  </read_first>
  <behavior>
    - Navigation reads: after warm-up, 25 mixed reads (getRotoPhysicalProjection, getRotoPhysicalContentRevision, getRotoPhysicalRenderSource across many appFrames) add ZERO calls to both spies (vi.spyOn on the physicsPaintRotoPhysicalModel module namespace for buildPhysicPaintRotoPhysicalRevision and projectPhysicPaintRotoPhysicalTimeline — the store imports both from that module at lines 11/23, and the 38.1-01 idiom proved namespace spying intercepts these calls).
    - Reference stability: repeated getRotoPhysicalProjection calls return the SAME projection object reference across reads (the O(1) cached-cell-read contract).
    - Each mutation class triggers exactly ONE recompute on the FIRST post-mutation read and zero on subsequent reads: add key, delete key, move key (all via replaceRotoPhysicalRecords), payload write (updateRotoPhysicalRealKeyPayload with expectedContentRevision read beforehand), interpolation change (setRotoPhysicalInterpolationState to a different enabled/mode), undo/redo (replaceRotoPhysicalRecords restoring the prior collection — the same publish path rotoMoveHistory drives via replacePhysicalRecordsWithOwnership, PhysicsPaintStudio.tsx:833).
    - Byte-identity (38.1-D-09): after each mutation, the cached projection deep-equals a fresh projectPhysicPaintRotoPhysicalTimeline over current store inputs and the cached revision string === a fresh buildPhysicPaintRotoPhysicalRevision over current store inputs.
    - Selection: setRotoPhysicalSelection adds ZERO spy calls on subsequent reads.
    - Absent-layer semantics preserved: getRotoPhysicalProjection / getRotoPhysicalContentRevision return null for a layer with no record map; a layer present with zero records still projects (current 1335 semantics).
    - No-op mutations stay free: replaceRotoPhysicalRecords with equal content returns early at 1154 without replacing identities, so subsequent reads add zero spy calls.
  </behavior>
  <action>
    RED first: create the new test file with the full behavior matrix above failing against the current uncached implementation (navigation reads currently re-invoke both pure functions per call). Node env, `.test.ts` under app/src (vitest include is src/**/*.test.ts — no jsdom, no config changes, vitest run only). Fixtures: tiny valid payloads (short dataUrl strings) built through replaceRotoPhysicalRecords so validation runs; follow the physicPaintStore.test.ts reset-in-beforeEach pattern. Assert spy deltas with explicit baselines (clear mock counts after warm-up / after each mutation).

    GREEN: add a module-level per-layer memo in physicPaintStore.ts: `_rotoPhysicalStructuralCache: Map<string, { recordMap; interpolation; capacity; projection; contentRevision }>` and a module-level resolver that (a) reads `_rotoRealKeyRecords.get(layerId)` — absent means delete any cache entry and return a miss signaled to callers as the current null behavior; (b) reads capacity via the same `?? PHYSIC_PAINT_MAX_APPLY_FRAMES` default and interpolation via the same `?? PHYSIC_PAINT_ROTO_INTERPOLATION_DISABLED` default as the current getters; (c) returns the cached entry when `cached.recordMap === recordMap && cached.interpolation === interpolation && cached.capacity === capacity` (identity triple — every mutation site REPLACES these objects, so no explicit invalidation hooks exist to forget); (d) otherwise recomputes with the EXACT current logic (sorted records via the same Array.from(recordMap.values()).sort by appFrame; identities = keyId+appFrame; projectPhysicPaintRotoPhysicalTimeline with interpolationEnabled: interpolation.enabled, null on !ok; buildPhysicPaintRotoPhysicalRevision(records, interpolation)), stores, and returns it. Rewire getRotoPhysicalProjection and getRotoPhysicalContentRevision to read through the resolver, preserving the current null semantics byte-for-byte (projection: null when layer absent OR projection !ok; revision: null when layer absent). getRotoPhysicalRenderSource keeps its current body — only its two reads become memo hits. Also rewire the revision read inside getRotoPhysicalDocument (line ~1228) to the memoized getter (identical string; removes the same per-read hash from that path).

    Before GREEN, grep-verify the identity-invalidation invariant: `grep -n "_rotoRealKeyRecords" app/src/stores/physicPaintStore.ts` and confirm every write installs a freshly built Map (1157-1159, 1197, 1429/1435, hydration 771-795) or deletes (145, 1448) — NO in-place `.get(layerId).set(...)` mutation of an installed inner map may exist. If one is found, STOP and surface it (the identity memo would serve stale data); do not ship around it.

    Constraints: no behavior change (38.1-D-09) — same pure functions, same inputs, same outputs; the memo changes only WHEN they run. Do not touch the 3 known-red D-15 tests. Do not add untracked()/peek() around physicPaintVersion-subscribed code elsewhere (bump-AND-subscribe contract). Keep mutation-time revision computations (1152-1153, 1433) as-is — they are not the per-navigation defect.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test file green: navigation zero-recompute, per-mutation-class exactly-one-recompute, byte-identity, selection zero-invalidation, absent-layer nulls, no-op-mutation free reads.
    - Existing physicPaintStore.test.ts passes UNMODIFIED (grep gate: `git diff --name-only` shows zero changes to that file).
    - Grep gate: `grep -n "_rotoRealKeyRecords.get(layerId).set\|recordMap.set" app/src/stores/physicPaintStore.ts | grep -v "^.*://" | grep -vc "new Map"` — every inner-map write belongs to a freshly built map before install (inspect output; zero post-install in-place writes).
    - In-place write guard: `grep -c "getRotoPhysicalProjection(layerId)" app/src/stores/physicPaintStore.ts` resolves through the memo (source inspection: both public getters call the resolver).
  </acceptance_criteria>
  <done>Per-navigation store reads are O(1) spy-proven; every mutation class recomputes exactly once with byte-identical projection/revision; selection and navigation never invalidate; the store's existing suite is untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: engine moves — decoded-Image cache in setPreviewBaseImageUrl + resetBackground early-return</name>
  <files>packages/efx-physic-paint/src/engine/EfxPaintEngine.ts, packages/efx-physic-paint/src/engine/EfxPaintEngine.previewBaseImageCache.test.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:477-525 (setBackgroundImageUrl, resetBackground, setPreviewBaseImageUrl, clearPreviewBaseImage — requestId guard semantics at 478/481, 498, 505/508)
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:174-182 (userPhoto/previewBaseImage fields) and all bgData writers: lines 350, 464, 485, 499, 877, 1499, 2022 (every one REPLACES this.bgData — the identity guard's invalidation set)
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.liveAlphaCache.test.ts (engine test harness: stubbed contexts, engine construction without a DOM)
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.cooperativeFinalization.contract.red.test.ts:515-522 (ctx stubbing idiom with vi.fn)
  </read_first>
  <behavior>
    - First setPreviewBaseImageUrl(dataUrl) constructs one Image and applies on load; a later call with the SAME dataUrl constructs ZERO new Images and applies the cached decoded image under the identical guards (requestId latest, !destroyed, !animationMode, !state.drawing) — the revisit skips the decode round-trip entirely.
    - A superseding call (new dataUrl) invalidates an older in-flight load exactly as today (requestId semantics verbatim); clearPreviewBaseImage keeps its exact current behavior.
    - resetBackground with unchanged background inputs and an untouched bgData executes no drawBg/redraw work; after any other background writer runs (setBgMode, setBackgroundImageUrl apply, hard reset, replay reset — all replace this.bgData), resetBackground executes fully again.
    - resetBackground still bumps previewBackgroundRequestId on every call, including skipped ones (pending setBackgroundImageUrl load cancellation is behavior, not work — 38.1-D-09).
    - Pixels identical: the draw/putImageData call sequence on stubbed contexts for a navigate-away-and-back scenario equals the uncached baseline sequence minus the redundant decode/drawBg work.
  </behavior>
  <action>
    RED first: create the engine test file mirroring the liveAlphaCache harness; stub global Image with a counting, manually-loadable constructor (vi.stubGlobal('Image', ...) — trigger onload by assignment capture), construct the engine with stubbed canvases/contexts, and encode the behavior matrix above (it fails: today every call constructs a new Image and every resetBackground re-runs drawBg). Run from packages/efx-physic-paint with its existing vitest setup — no config changes.

    GREEN — decode cache: add `private previewBaseImageCache = new Map<string, HTMLImageElement>()`. Extract the guarded apply body of setPreviewBaseImageUrl's onload into a private method (e.g. applyPreviewBaseImage(image, requestId)) used by BOTH the onload path and the cache-hit path so the guards stay single-sourced. setPreviewBaseImageUrl keeps `const requestId = ++this.previewBaseRequestId` as its first statement; on cache hit, apply synchronously (image already decoded — this is the approved timing win; guard semantics identical); on miss, construct the Image, and inside onload (after the guard passes) insert into the cache then apply. Eviction: FIFO cap of 32 entries via Map insertion order (delete oldest key on insert beyond cap) — bounded memory for multi-MB decoded bitmaps; clear the cache in destroy(). NO speculative decode-ahead or neighbor pre-decoding — the cache is populated ONLY by actual setPreviewBaseImageUrl calls (user-rejected alternative).

    GREEN — resetBackground skip: keep `this.previewBackgroundRequestId += 1` unconditional. Then early-return when `this.bgData === this.lastResetBackgroundData` AND the input tuple (state.bgMode, width, height, paperTextures reference, userPhoto reference) matches `this.lastResetBackgroundInputs`. Otherwise execute drawBg/redrawPreviewBase/redrawAll verbatim and stamp both memo fields. The bgData-identity half covers every other writer (350, 464, 485, 877, 1499, 2022) because each reassigns this.bgData — verify this by reading each site; if any writer mutates the existing ImageData in place instead of reassigning, STOP and surface it. Initialize memo fields so the first resetBackground always executes.

    Constraints: zero behavior change beyond WHEN work runs (38.1-D-09) — same draw calls, same order, same guard semantics; the engine's public signatures unchanged; no new dependencies.
  </action>
  <verify>
    <automated>cd packages/efx-physic-paint && pnpm vitest run src/engine/EfxPaintEngine.previewBaseImageCache.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New engine test green: decode-cache hit performs zero Image constructions and applies synchronously under the same guards; miss decodes once; requestId supersession and clearPreviewBaseImage behavior verbatim; resetBackground skip/execute matrix proven including the post-writer re-execution and the unconditional requestId bump.
    - Pre-existing engine suites pass UNMODIFIED (git diff touches zero existing test files).
    - Source inspection: setPreviewBaseImageUrl's first statement is still the requestId bump; resetBackground's requestId bump is outside/above the early-return guard.
  </acceptance_criteria>
  <done>Frame revisits skip the decode entirely; unchanged-background resets are free; every guard and cancellation semantic is byte-identical; engine suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Studio navigation — paint the destination BEFORE awaiting flushLivePixels, with post-flush generated-cell repaint</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:755-797 (navigateToSyncedPhysicalFrame — the 38.1-03 pipeline being reordered; the generation/isLatest/scheduler comments are the locked ordering invariants)
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts:245-269 (flushLivePixels — flushes ALREADY-CAPTURED live-pixel transaction buffers and parent deliveries; captures no engine pixels at await time, so initiating before engine.clear() cannot save cleared pixels — assert this by reading livePixelTransactionsRef.flush's capture model before editing)
    - .planning/phases/38.1-studio-render-path-performance/38.1-03-SUMMARY.md (D1 coverage: the locked source-order pipeline and its rationale)
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts (7 source-grep assertions that must pass UNMODIFIED)
  </read_first>
  <action>
    Reorder navigateToSyncedPhysicalFrame into the approved pipeline, preserving every existing statement's semantics and the 38.1-03/38.1-05 comment contract style:
    1. integer guard, rotoCachedPlayback.stop(), generation begin (unchanged — still before any async work).
    2. engine?.flushPendingStrokeFinalizations() (unchanged position).
    3. INITIATE `const flushPromise = rotoPersistence.flushLivePixels(currentFrame)` WITHOUT awaiting. Save-before-leave is preserved: the flush is always initiated and always awaited later; it is never skipped, never weakened, and its error path stays verbatim (RESEARCH Pitfall 5). The flush operates on already-captured buffers (see read_first), so the subsequent engine.clear() cannot corrupt it.
    4. SAME TICK paint (38.1-D-03, strengthened): setCachedRotoReferenceUrl(null); if engine: clearPreviewBaseImage(); (engine as PreviewBackgroundEngine).resetBackground(); engine.clear(); loadCachedRotoReferenceFrame(frame, engine as PreviewBackgroundEngine) — the exact current paint block, now reached with zero intervening awaits, so every navigation (including discrete clicks) paints in the intent tick. No pre-paint isLatest recheck is needed: nothing awaits between begin() and the paint, so the generation cannot be superseded in between (Task 1/2 make this paint cheap: cached projection read + decode-cache hit + resetBackground skip).
    5. `try { await flushPromise } catch { setApplyStatus('error'); setApplyMessage(`Could not save Roto frame ${currentFrame} before navigation.`); return false; }` — error surfacing byte-identical to today (message literal verbatim).
    6. `if (!rotoNavigationGeneration.isLatest(generation)) return false;` — the 38.1-D-05 post-await recheck, now gating UI propagation AND the repaint below (a superseded navigation still painted at step 4 — that is the approved same-tick-paint trade — but never propagates and never repaints).
    7. Post-flush neighbor pickup: if launchContext && engine && the destination cell is generated — `physicPaintStore.getRotoPhysicalProjection(launchContext.layerId)?.cells[frame]?.kind === 'generated'` (O(1) cached read after Task 1; do NOT call getRotoPhysicalRenderSource for the kind check — it would run the interpolation render) — re-issue the exact step-4 paint block once so the destination picks up the just-flushed neighbor key pixels.
    8. Selection sync block, scheduleRotoStartFramePropagation(frame), pendingFrameSyncRef, sendPhysicPaintFrameSyncMessage, return true — all verbatim in current order.
    Update the useCallback dependency array to list exactly what the body closes over (38.1-03 Rule-3 idiom). restoreFrame/clearCanvas display-port flows are NOT touched (their flows have no await between intent and paint — 38.1-03 decision).
    Constraints: do not touch the playback per-tick surface (38.1-05 exclusive scope), the capsule/tooltip blocks (38.1-D-10), or any test file; PhysicsPaintStudio.test.ts 7/7 must pass UNMODIFIED.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm vitest run && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source-order gate: within navigateToSyncedPhysicalFrame, the statement initiating flushLivePixels precedes the paint block, the paint block precedes the `await` of that flush, and the isLatest recheck follows the await (grep -n line ordering, inspected).
    - The error-message literal `Could not save Roto frame` survives verbatim: `grep -c "Could not save Roto frame" app/src/components/physic-paint/PhysicsPaintStudio.tsx` equals 1.
    - The generated-cell repaint is gated on the cached projection kind check and the generation token (source inspection; no getRotoPhysicalRenderSource call in the kind check).
    - Full app suite: same pass count as the 38.1-05 baseline plus the new Task 1 tests, exactly 3 failures — all known-red D-15 (deterministic failure-count gate per the 38.1-01 Task 2 idiom: run once with the diff stashed if the baseline is in doubt; the 3 known-red tests are never edited).
    - Typecheck clean; git diff touches zero existing test files and zero playback-surface lines (animFrame/animTotal, playback input block, rotoCachedPlayback.frame — `git diff -U0 | grep` shows none).
  </acceptance_criteria>
  <done>The destination canvas paints in the navigation intent tick ahead of the flush await; save-before-leave and its error path are verbatim; superseded generations never propagate or repaint; generated destinations repaint once after flush completion to pick up the flushed neighbor; the full suite matches the known-red baseline exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store read path -> Studio/engine | projection + revision strings flow into cacheRevision composition and acknowledged-edit transactions; a stale cache would corrupt transaction identity |
| dataUrl -> engine decode cache | multi-MB payload strings key a decoded-bitmap cache; unbounded growth is a memory-exhaustion vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-07-01 | Tampering | _rotoPhysicalStructuralCache | high | mitigate | identity-triple validity (recordMap/interpolation/capacity replaced on every mutation) + byte-identity tests per mutation class + grep gate forbidding post-install in-place inner-map writes |
| T-38.1-07-02 | Denial of Service | previewBaseImageCache | medium | mitigate | FIFO eviction cap (32 decoded images) + cache cleared on destroy; no decode-ahead (also user-rejected) |
| T-38.1-07-03 | Tampering | paint-before-flush reorder | medium | mitigate | flush always initiated AND awaited with verbatim error path; isLatest recheck gates propagation/repaint; generated-cell repaint restores neighbor freshness |
| T-38.1-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | not applicable — zero package installs in this plan (RESEARCH Package Legitimacy Audit: no new packages) |
</threat_model>

<verification>
- Task 1: `cd app && pnpm vitest run src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts src/stores/physicPaintStore.test.ts` — new matrix green, existing store suite unmodified.
- Task 2: `cd packages/efx-physic-paint && pnpm vitest run src/engine/EfxPaintEngine.previewBaseImageCache.test.ts src/engine/EfxPaintEngine.liveAlphaCache.test.ts src/engine/EfxPaintEngine.pointerInput.test.ts` — new engine suite green, existing engine suites unmodified.
- Task 3 + phase gate: `cd app && pnpm vitest run && pnpm typecheck` — full suite green except exactly the 3 known-red D-15 failures (2 physicsPaintWorkflowPresentation + 1 PhysicsPaintWorkflowStrip placement source-grep), baseline-identical; typecheck clean; zero existing test files in the diff.
- Interactive confirmation (navigation at 18 keys feels instant, canvas-first on every click) is verified in the 38.1-06 blocking native UAT on the user's real project (D-06) — do not claim UAT done; this plan is automated-ready evidence only.
</verification>

<success_criteria>
- Per-navigation cost independent of total key count: spy-proven zero rebuilds on navigation/selection reads; exactly one byte-identical rebuild per structural mutation class.
- Engine revisits skip decode; unchanged-background resets skip redraw; pixels identical.
- Navigation paints same-tick before the flush await; save-before-leave contract verbatim; generated destinations repaint once post-flush when latest.
- Zero behavior change (38.1-D-09): identical pixels, identical revision strings, identical capsule lines; the 3 known-red D-15 tests fail exactly as before.
</success_criteria>

## Artifacts this phase produces

New symbols (names are executor latitude within the action constraints):
- `app/src/stores/physicPaintStore.ts`: module-level `_rotoPhysicalStructuralCache` (per-layer memo) + a module-level structural resolver used by getRotoPhysicalProjection / getRotoPhysicalContentRevision / getRotoPhysicalDocument.
- `packages/efx-physic-paint/src/engine/EfxPaintEngine.ts`: `previewBaseImageCache` (Map<string, HTMLImageElement>, FIFO-capped), a shared guarded apply helper for setPreviewBaseImageUrl, `lastResetBackgroundData` / `lastResetBackgroundInputs` memo fields.
- `app/src/stores/physicPaintStore.rotoPhysicalStructuralCache.test.ts` (new node-env suite).
- `packages/efx-physic-paint/src/engine/EfxPaintEngine.previewBaseImageCache.test.ts` (new engine suite).

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-07-SUMMARY.md` when done
</output>
