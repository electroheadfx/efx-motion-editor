---
phase: 38.1-studio-render-path-performance
plan: 08
type: execute
wave: 4
depends_on:
  - 38.1-07
files_modified:
  - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
  - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx
  - app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx
  - app/src/components/physic-paint/engine/usePhysicsPaintEngineLifecycle.ts
autonomous: false
requirements:
  - 38.1-D-01
  - 38.1-D-04
  - 38.1-D-07
  - 38.1-D-08
  - 38.1-D-09
  - R-D-01
  - R-D-02
  - R-D-03
  - R-D-04
  - R-D-08
  - R-D-09

must_haves:
  truths:
    - "The existing development-gated Physics Paint profiler exposes immutable named-counter snapshots and deterministic snapshot deltas without creating a second profiling subsystem (R-D-02)."
    - "Every R-D-04 render surface, Workflow cell execution, timeline/canvas observer setup and cleanup, app-owned Efx child request, and app lifecycle proxy has one stable counter name and increments only while the existing profiler gate is enabled (38.1-D-07/R-D-02)."
    - "Plan 08 blocks until the user returns actual forward and reverse warm-up/delta snapshots that are RED against the exact zero/three/restart thresholds; no pending handoff can complete the plan (R-D-03)."
    - "Profiling-disabled production behavior remains a no-op with bounded timing samples and no package, test-environment, server, browser, or native-app changes (38.1-D-09/R-D-09)."
  artifacts:
    - path: "app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts"
      provides: "Existing profiler extended with counter recording, immutable snapshots, delta calculation, and native API methods"
      exports: ["PHYSICS_PAINT_PERFORMANCE_COUNTER_NAMES", "recordPhysicsPaintPerformanceCounter", "snapshotPhysicsPaintPerformance", "diffPhysicsPaintPerformanceSnapshots"]
    - path: "app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts"
      provides: "Recorder semantics, reset, disabled-gate, immutability, and delta tests"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Source contract covering all non-Workflow instrumentation sites and absence of localization changes"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts"
      provides: "Workflow render/cell/observer counter source contracts"
  key_links:
    - from: "all instrumented component and lifecycle sites"
      to: "physicsPaintPerformanceTrace.ts"
      via: "one named no-op-when-disabled counter call per render/event"
      pattern: "recordPhysicsPaintPerformanceCounter"
    - from: "window.__EFX_PHYSICS_PAINT_PROFILE__"
      to: "snapshot/diff recorder functions"
      via: "clear, summary, snapshot, and delta methods on the existing native object"
      pattern: "__EFX_PHYSICS_PAINT_PROFILE__"
---

<objective>
Extend the existing Physics Paint performance trace into the deterministic render/event acceptance seam required by the reopened phase, instrument the complete per-navigation path, and capture the failing baseline before any localization work (R-D-02/R-D-03/R-D-04; 38.1-D-07/D-08).

Purpose: Plans 09–11 need objective evidence that a frame-only committed navigation updates only sanctioned surfaces. This plan supplies that evidence without changing the measured architecture.

Output: tested counter/snapshot API, complete development-only instrumentation, source-contract coverage, and a recorded RED baseline in `38.1-08-SUMMARY.md`.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-07-SUMMARY.md
@app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts
@app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (RED then GREEN): extend the existing profiler with named counters, snapshots, deltas, and native access</name>
  <files>app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts, app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.ts — preserve the existing timing sample/category/summary API, bounded sample buffer, DEV + localStorage gate, and `window.__EFX_PHYSICS_PAINT_PROFILE__` object.
    - app/src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts — preserve current summary, bounded-sample, and disabled-profiling cases.
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md — R-D-02, R-D-03, R-D-04, and the warm-up/snapshot protocol.
  </read_first>
  <behavior>
    - Counter recording: enabled `recordPhysicsPaintPerformanceCounter(name)` increments a named integer by one; an optional positive integer amount adds exactly that amount; disabled profiling mutates nothing.
    - Reset: `clearPhysicsPaintPerformance()` clears timing samples and all counters while preserving the existing native API object.
    - Registry: one exported readonly canonical counter-name tuple contains every Plan 08 ownership-table name; duplicate or unregistered names fail tests/type checking.
    - Snapshot: each call returns a new detached object containing the current timing summary and every registered counter name, explicitly set to its current integer or zero; later recorder writes cannot mutate an earlier snapshot.
    - Delta: diffing before/after snapshots iterates the canonical registry, returns every name with an explicit numeric difference, treats malformed external missing values as zero, and retains zero values so UAT can assert lifecycle/observer rows that did not fire.
    - Native contract: the existing window profiler object retains `clear` and `summary` and adds `snapshot` plus `delta` wired to the same recorder state.
  </behavior>
  <action>
    RED: extend the existing test file first with the behavior matrix above, including a test that immediately after `clear()` both `snapshot()` and a same-state `delta()` contain every canonical name with value `0`. Keep tests in the current node environment; stub only the existing development/localStorage gate as current tests do. Run the focused test and confirm the new cases fail because the registry/counter/snapshot methods do not exist.

    GREEN per R-D-02: extend the existing module, not a parallel profiler. Export `PHYSICS_PAINT_PERFORMANCE_COUNTER_NAMES` as the readonly canonical tuple matching the Task 2 ownership table, derive the counter-name type from it, and use a module-local `Map`. Add `recordPhysicsPaintPerformanceCounter`, `snapshotPhysicsPaintPerformance`, and `diffPhysicsPaintPerformanceSnapshots`. Keep the existing sample bound and timing summaries byte-compatible. Snapshot results must be detached and readonly and must enumerate the whole registry with explicit zeros; delta must compare two supplied snapshots without reading live state and must also enumerate the whole registry. Extend the existing global Window declaration and `window.__EFX_PHYSICS_PAINT_PROFILE__` assignment with `snapshot` and `delta`. Every public recorder operation must honor the same `import.meta.env.DEV` plus `efx.physicsPaint.profile` gate already used by timing samples. Do not add a second localStorage key, dependency, test environment, or wall-clock threshold (R-D-09).
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Focused recorder tests pass for canonical registry uniqueness/completeness, increments, amount, reset, disabled no-op, detached snapshots, all-zero post-clear snapshots/deltas, missing-as-zero defensive handling, explicit zero retention, and native API shape.
    - Existing timing summary and bounded-sample tests remain green without rewritten expectations.
    - The module still exposes one profiler gate and one `window.__EFX_PHYSICS_PAINT_PROFILE__` object (R-D-02).
  </acceptance_criteria>
  <done>The existing profiler is a tested timing + counter snapshot seam suitable for native warm-up/delta acceptance, with disabled behavior remaining a no-op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: instrument Studio, static regions, CanvasStack, CanvasMount, and app-owned lifecycle events</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx, app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx, app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx, app/src/components/physic-paint/engine/usePhysicsPaintEngineLifecycle.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — current profiler callback, identity memos, and fresh view-model assembly; instrument only.
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx — StudioView body, CanvasStack body, CanvasStack ResizeObserver/MutationObserver setup and cleanup, right-panel region markup, dialog and Workflow mounts.
    - app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx — count `PhysicsPaintToolRailImpl`, not the compat wrapper; preserve narrow history Signal reads.
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx — count the plain implementation and keep compat out of this module.
    - app/src/components/physic-paint/engine/PhysicsPaintCanvasMount.tsx — component body, dimension observer, and Efx child request.
    - app/src/components/physic-paint/engine/usePhysicsPaintEngineLifecycle.ts — canvasKey reset, tablet listener, background application, and external-state cleanup effects.
  </read_first>
  <behavior>
    - One component execution increments exactly one of: `render.studio`, `render.studioView`, `render.topBar`, `render.toolRailImpl`, `render.rightPanelRegion`, `render.rightPanelImpl`, `render.playScriptDialog`, `render.canvasStack`, and `render.canvasMount`.
    - The monolithic pre-split Workflow records both `render.workflowStrip` and conceptual baseline `render.workflowStaticChrome`; Plan 10 relocates the latter into the extracted static implementation without renaming or duplicating it.
    - CanvasStack ResizeObserver and MutationObserver installs and cleanups have separate counters; CanvasMount ResizeObserver install/cleanup has separate counters.
    - The app-owned Efx child request, engine-ready callback, pre-destroy callback, and app lifecycle setup/cleanup are distinguishable counters rather than inferred from timing samples.
    - Instrumentation does not stabilize props/callbacks, change effect dependencies, add memo wrappers, alter markup, or change lifecycle ordering; transparent lifecycle proxies retain per-render baseline identity while preserving exact semantics (R-D-03/R-D-08).
  </behavior>
  <counter_ownership>

  | Counter | Plan 08 baseline owner | Post-localization owner |
  |---|---|---|
  | `render.studio` | `PhysicsPaintStudio` body | unchanged |
  | `render.studioView` | `PhysicsPaintStudioView` body | unchanged |
  | `render.topBar` | `PhysicsPaintTopBar` body | unchanged behind Plan 09 wrapper |
  | `render.toolRailImpl` | `PhysicsPaintToolRailImpl` body | unchanged |
  | `render.rightPanelRegion` | StudioView branch conceptual call | Plan 09 region implementation |
  | `render.rightPanelImpl` | plain `PhysicsPaintRightPanel` body | unchanged |
  | `render.playScriptDialog` | plain dialog body | unchanged behind Plan 09 wrapper |
  | `render.canvasStack` | `PhysicsPaintCanvasStack` body | same body behind Plan 11 memo |
  | `render.canvasMount` | plain `PhysicsPaintCanvasMount` body | unchanged behind Plan 11 wrapper |
  | `render.efxChildRequest` | immediately before app requests `EfxPaintCanvas` | unchanged |
  | `render.workflowStrip` | outer strip body | unchanged |
  | `render.workflowStaticChrome` | monolithic strip conceptual call | Plan 10 static implementation |
  | `render.rotoTimelineCellButton` | private cell body | Plan 10 memoized implementation body |
  | `observer.canvasStack.resize.install/cleanup` | existing CanvasStack ResizeObserver effect | unchanged |
  | `observer.canvasStack.mutation.install/cleanup` | existing CanvasStack MutationObserver effect | unchanged |
  | `observer.canvasMount.resize.install/cleanup` | existing CanvasMount ResizeObserver effect | unchanged |
  | `observer.timeline.resize.install/cleanup` | existing Workflow timeline observer effect | same effect after Plan 10 install-once change |
  | `lifecycle.canvasMount.engineReady` | behavior-transparent app proxy | same proxy stabilized in Plan 11 |
  | `lifecycle.canvasMount.beforeDestroy` | behavior-transparent app proxy | same proxy stabilized in Plan 11 |
  | `lifecycle.engine.tabletListener.install/cleanup` | existing engine-lifecycle tablet effect | unchanged |
  | `lifecycle.engine.externalState.cleanup` | existing engine-lifecycle cleanup effect | unchanged |

  Source contracts require each counter literal exactly once at its current owner, except paired `install/cleanup` names. Relocation plans delete the old conceptual owner before adding the new one.
  </counter_ownership>
  <action>
    RED: add source-contract assertions to `PhysicsPaintStudio.test.ts` that enumerate the required stable app counter literals and their owning function/effect regions. Include guards that Plan 08 introduces no new `memo(` calls, no new identity-memo resolve calls, no package source changes, and no changed observer dependency arrays. Confirm the new assertions fail before instrumentation.

    GREEN: import the counter recorder at each app layer and increment once at the top of the actual implementation body named above. Count the existing ToolRail private implementation and plain RightPanel implementation so wrapper calls cannot produce false positives. Count the dialog body whether open or closed; native deltas distinguish the closed case. At the current StudioView right-panel branch, record conceptual `render.rightPanelRegion`; Plan 09 must move that exact counter into the extracted region implementation, delete the baseline owner, and retain exactly one owner. In CanvasStack and CanvasMount effects, increment install counters immediately after observer construction/observation and cleanup counters in the existing cleanup closures without moving statements.

    Immediately before `PhysicsPaintCanvasMount` requests its `EfxPaintCanvas` child, increment the app-owned child-request counter. Add behavior-transparent app-owned proxy functions for engine ready and before destroy: each records once, forwards the exact original engine argument to the current callback, and preserves return value, Promise behavior, thrown errors, and call order. The Plan 08 proxies deliberately retain current per-render identity; Plan 11 owns stabilization through refs. Count `usePhysicsPaintEngineLifecycle` setup/cleanup events at their current boundaries. Do not instrument or modify package code. Per R-D-03, do not add memoization, stable-prop work, observer dependency changes, or render localization.

    Counter ownership table required in source contracts and the Plan 08 summary: each stable counter name maps to exactly one baseline owner, one post-localization owner (unchanged or moved by Plan 09/10/11), and one Plan 12 acceptance row. Missing counters must fail source contracts rather than appear as implicit zero.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every required component/lifecycle/observer surface has exactly one stable counter owner and source-contract coverage.
    - ToolRail counts the implementation body; RightPanel remains a plain directly callable implementation; package source remains untouched.
    - Existing effect dependencies, wrapper exports, callback-ref behavior, and lifecycle ordering are unchanged except for recorder calls (R-D-03/R-D-08).
    - Focused app tests pass; no package source, test config, or package file changes exist (R-D-09).
  </acceptance_criteria>
  <done>Studio through Efx lifecycle has deterministic, development-gated render/event instrumentation without any measured-path optimization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: instrument WorkflowStrip, conceptual static chrome, cells, and timeline observers</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — strip body, private `RotoTimelineCellButton`, `frameCells` cache invalidation, ResizeObserver effect keyed by frameCells/updateScrollbar, wheel effect, drag listeners, tooltip mounts, scrollbar and capsule markup.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts — all existing source contracts for tooltip, drag, selection, wheel, scrollbar, capsule, DOM classes, and aria behavior.
    - .planning/phases/38.1-studio-render-path-performance/38.1-REOPEN-CONTEXT.md — R-D-03/R-D-04 baseline and snapshot protocol.
  </read_first>
  <behavior>
    - Each strip execution increments `render.workflowStrip` and conceptual baseline `render.workflowStaticChrome`; each private cell body execution increments `render.rotoTimelineCellButton`.
    - Timeline ResizeObserver install and cleanup increment separate counters at the current effect boundaries.
    - Existing frameCells cache behavior, observer dependencies, wheel listener, pointer drag lifecycle, tooltip, selection, custom scrollbar, capsule, and accessibility contracts remain unchanged in Plan 08.
    - The native warm-up/delta baseline is recorded as RED when any R-D-04 zero counter is positive, cell delta exceeds three, or observer cleanup/reinstall occurs.
  </behavior>
  <action>
    RED: add source-contract assertions for the five Workflow counter sites under a dedicated `localized render instrumentation` describe block and retain every current behavior contract unchanged. Confirm failure before recorder calls exist.

    GREEN instrumentation only: add strip-body and conceptual static-chrome calls at the current monolithic strip owner, one private-cell-body call, and install/cleanup calls inside the existing timeline ResizeObserver effect. Plan 10 must move `render.workflowStaticChrome` into the extracted static implementation and remove the baseline owner without renaming or duplicating it. Do not memoize the strip or cell, split static/live markup, stabilize callbacks, or change `[frameCells, updateScrollbar]`; those are Plan 10 responsibilities per R-D-03/R-D-06.
  </action>
  <verify>
    <automated>pnpm -C app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts -t "localized render instrumentation|navigation render localization|Physics Paint performance trace" && pnpm -C app exec tsc --noEmit && pnpm -C app build</automated>
  </verify>
  <acceptance_criteria>
    - Workflow strip, cell, observer install, and observer cleanup counters are source-contract locked.
    - All existing Workflow behavioral assertions remain green without test-environment changes.
    - Typecheck and build pass with the exact repository commands.
    - Automated instrumentation is complete and the exact blocking native baseline protocol is ready; SUMMARY creation waits for actual user-provided forward and reverse RED delta objects.
    - Diff inspection confirms Plan 08 contains instrumentation/tests only and no memoization/localization or observer dependency changes (R-D-03).
  </acceptance_criteria>
  <done>The complete frame-navigation path is countable without localization changes and is ready for the blocking user-owned RED baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>BLOCKING native baseline: capture actual forward and reverse RED render deltas before Plan 09</name>
  <precondition>Tasks 1–3 automated gates pass; instrumentation-only diff is confirmed; the user has the native development app/project running with the existing profiler gate enabled.</precondition>
  <files>None</files>
  <what-built>Tasks 1–3 extended the existing profiler with app-owned render/event counters and instrumented the current architecture without memoization, prop stabilization, observer dependency changes, or other localization. This checkpoint gathers the required actual native RED baseline before any optimization begins.</what-built>
  <action>
    Present the exact warm-up/snapshot protocol from `38.1-REOPEN-CONTEXT.md` and WAIT. The agent must not launch the server, browser, or native app and must not add localization while waiting.

    The user clears the profiler, warms one adjacent navigation in each direction, captures before/after snapshots for one forward and one reverse adjacent committed navigation, and returns both delta objects. The returned deltas must include explicit entries for every R-D-04 zero surface, `render.rotoTimelineCellButton`, and observer/lifecycle rows. Missing required counter names are an instrumentation failure routed back to Tasks 1–3, not implicit zero.

    Expected verdict is RED because this plan intentionally changes no render boundaries. Record the actual positive/over-limit counters as the baseline evidence for Plans 09–11. Plan 09 is ineligible until actual forward and reverse delta objects are present in `38.1-08-SUMMARY.md`.
  </action>
  <how-to-verify>The user follows the reopen-context warm-up protocol and returns both actual forward and reverse delta objects. Verify every counter in the Plan 08 ownership table is explicitly present in both deltas, the cell and observer/lifecycle rows are present, and at least one R-D-04 zero/three/restart threshold is RED. Missing counters fail instrumentation rather than counting as zero.</how-to-verify>
  <verify>
    <human-check>The user returns actual forward and reverse native profiler delta objects; every required counter is present; at least one localized-render threshold is RED; no localization change was made.</human-check>
  </verify>
  <acceptance_criteria>
    - Actual forward and reverse delta objects are recorded, not a pending handoff.
    - Every required counter name exists in both deltas.
    - The RED owning surfaces are identified for Plans 09, 10, and 11.
    - The agent launched no runtime and changed no code in response.
  </acceptance_criteria>
  <done>Plan 08 has an objective native RED baseline and can create its SUMMARY; Plan 09 may begin.</done>
  <resume-signal>Return the forward and reverse profiler delta objects after following the warm-up protocol.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native window -> profiler snapshot | Development console reads internal render/lifecycle counts; API must expose copied state and remain unavailable as an active recorder outside the existing gate. |
| app -> existing Efx callbacks | App-owned request/engine-ready/pre-destroy proxies count and transparently forward the existing package boundary without modifying package code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-08-01 | Tampering | profiler snapshots | medium | mitigate | Detached snapshot objects, pure before/after delta function, explicit missing-as-zero semantics, and recorder unit tests. |
| T-38.1-08-02 | Denial of Service | per-render instrumentation | low | mitigate | Integer Map increment behind the existing development/localStorage gate; disabled path is tested as a no-op; no sample-per-render arrays. |
| T-38.1-08-03 | Tampering | lifecycle instrumentation | medium | mitigate | Counter calls are inserted at existing setup/cleanup boundaries without moving statements or changing effect dependencies; source-contract tests lock ownership. |
| T-38.1-08-SC | Tampering | package installs | high | mitigate | No installs or package manifest changes; existing Preact, Signals, and Vitest only (R-D-09). |
</threat_model>

<verification>
- `pnpm -C app exec vitest run src/components/physic-paint/performance/physicsPaintPerformanceTrace.test.ts`
- `pnpm -C app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts`
- `pnpm -C app exec tsc --noEmit`
- `pnpm -C app build`
- Blocking user-owned native baseline; the agent does not launch any runtime. The actual forward/reverse RED deltas are required Plan 08 completion evidence and input to Plans 09–11.
</verification>

<success_criteria>
- Existing profiler seam provides tested counter snapshots and deltas.
- Every required render/event/observer/lifecycle surface is instrumented with stable names.
- Automated suites, typecheck, and build pass with no environment/package changes.
- Plan 08 makes zero localization changes and records actual forward/reverse native RED deltas with every required counter present before Plan 09 begins.
</success_criteria>

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-08-SUMMARY.md` only after the blocking checkpoint returns actual forward and reverse RED delta objects. Record the counter ownership/relocation table, recorder API, exact files instrumented, automated results, confirmation that no localization change occurred, both native deltas, and the owning positive/over-limit surfaces that Plans 09–11 must close.
</output>
