---
phase: 38.1-studio-render-path-performance
plan: 05
type: execute
wave: 3
depends_on: [38.1-03, 38.1-04]
files_modified:
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: true
requirements:
  - 38.1-D-01
  - 38.1-D-02
  - 38.1-D-08
  - 38.1-D-09
  - 38.1-D-10

must_haves:
  truths:
    - "During cached playback, per-tick updates reach exactly THREE live surfaces (38.1-D-01): the playback canvas image (the <img class=\"physics-paint-cached-roto-playback\"> slot at PhysicsPaintStudioView.tsx:105), the minimal current-frame indicator (the EXISTING nav-pill <output class=\"physics-paint-current-frame\"> at PhysicsPaintWorkflowStrip.tsx:956 — RESEARCH Open Question 1 RESOLVED: element and placement pinned, unchanged), and play/stop control state (transport active state + isPlaying). Cells, capsule, and tube/log receive ZERO per-tick renders."
    - "The animFrame/animTotal useState pair (PhysicsPaintStudio.tsx:55-56) is gone and no per-tick setState remains anywhere in the playback path — per-tick data flows through signals read only by the three live surfaces (38.1-D-08 playback item; RESEARCH Pattern 4)."
    - "On playback stop, exactly ONE catch-up render restores full UI currency (38.1-D-01): capsule/status, cells, and tube/log all reflect the stop state in a single render — no flicker, no double render."
    - "Phase-38 D-10 missing-frame events raised during playback are QUEUED, shown once inside the stop catch-up render, then the idle context line resumes (38.1-D-02) — the 38 D-10 event-driven behavior is deferred, never dropped."
    - "Every playback status string, the start/stop/toggle/updateFps/resetForLaunch semantics, clampRotoPlaybackFps, the onStart/onFrame/setIsPlaying callback contract, the cleanup effect, and the workflowMode stop effect are verbatim (38.1-D-09); the 36.15/38 capsule single-owner arbitration with ambient idle-context feed and the 38-05 tooltip fixed-in-place mechanism are untouched (38.1-D-10)."
  artifacts:
    - "app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts — per-tick frame surface backed by a signal instead of per-tick useState; public interface and callback contract preserved; D-02 event queue with single-flush-on-stop."
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — animFrame/animTotal useState removed; playback input onFrame/onStart write the per-tick signal; no per-tick signal .value read in the Studio body or view-model literal."
    - "app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx — the playback <img> slot reads the per-tick signal through a narrow subscriber; cached-roto-playback-active/ready classes key off start/stop transitions only."
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — the nav-pill output slot reads the playback appFrame signal through a narrow child during active playback and shows props.currentFrame otherwise; DOM shape, class, and placement byte-identical."
  key_links:
    - "Per-tick signal -> three live surfaces: the signal is read ONLY by the playback <img> subscriber, the nav-pill output child, and start/stop-state consumers — any fourth subscriber reintroduces the per-tick Studio render this plan removes (grep-gated in both tasks)."
    - "Stop path -> D-02 flush: queued capsule events flush inside the SAME synchronous block as the isActive/setIsPlaying restoration, so Preact's render queue produces exactly one catch-up render showing the queued line once; the existing arbitration then resumes the idle context per 38 D-10."
    - "38.1-03/38.1-04 base -> freeze: with navigation rAF-capped (38.1-03) and strip renders cheap (38.1-04), removing the per-tick Studio render makes cached playback cost only the image swap plus two narrow signal subscribers."
---

<objective>
Implement the locked playback UI suspension (38.1-D-01/D-02): convert the playback per-tick surface from useState-driven full-Studio renders to a signal surface read only by the three sanctioned live surfaces — the playback canvas image, the minimal current-frame indicator (the existing nav-pill `<output class="physics-paint-current-frame">`, pinned per the resolved RESEARCH Open Question 1), and play/stop control state — while cells, capsule, and tube/log stay frozen for the duration, missing-frame events queue and flush once on stop, and exactly one catch-up render restores full UI currency.

Purpose: today every playback tick calls setFrame + setAnimFrame, re-rendering the whole Studio tree (cells, capsule, right panel) at up to 60 fps — root-cause link 4 of 4 (38.1-D-08). The user's locked requirement: "For playback it needs to disable every UI for speed."

Output: four modified files; the playback hook test contract and all source-grep suites green; full suite green except the 3 known-red D-15 tests; interactive verification deferred to the 38.1-06 blocking native UAT.

Sequencing note: the checker drafted this plan at wave 2 with depends_on [38.1-01, 38.1-02]; the wave-computation rule (zero files_modified overlap within a wave — this plan edits PhysicsPaintStudio.tsx and PhysicsPaintWorkflowStrip.tsx, owned by 38.1-03 and 38.1-04 at wave 2) places it at wave 3 with depends_on [38.1-03, 38.1-04]. 38.1-06 moves to wave 4 accordingly. 38.1-03's key_links already reserve the playback per-tick surface as this plan's exclusive scope.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@.planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md
@.planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md
@.planning/phases/38.1-studio-render-path-performance/38.1-03-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-04-SUMMARY.md

@app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end playback tick on signals — per-tick surface narrowed to canvas image + nav-pill indicator + transport state (38.1-D-01, D-08 playback item)</name>
  <files>app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <precondition>38.1-03 and 38.1-04 SUMMARYs exist: the navigation pipeline is canvas-first with the rAF flush boundary pinned to the per-frame channel (resolved Open Question 2), and the strip renders cheaply with stable structural identities. The playback per-tick surface (animFrame/animTotal, the playback input block, rotoCachedPlayback.frame) is untouched by those plans — it is this plan's exclusive scope.</precondition>
  <reversibility rating="reversible">Subscription-gating change in the playback render wiring, not a data-model contract (38.1-D-01 reversibility note): the playback engine semantics, status strings, and callback contract are untouched; reverting is a wiring rollback.</reversibility>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts (whole file, 166 lines) — per-tick setFrame at :113 and onFrame at :114 are the tick surface; the preserved-verbatim patterns are enumerated in PATTERNS §useRotoCachedPlayback.ts (clampRotoPlaybackFps :39-42, inputRef sync :57, status strings :91/:101-103/:124, start/stop/toggle/updateFps :87-140, resetForLaunch :82-85, both useEffects :144-148).
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts — the contract to preserve: playback.frame readable as a plain value (:117/:130/:152/:157/:162/:167), playback.isActive boolean, onStart called with (frameCount), onFrame last-called with (frameIndex, appFrame), setIsPlaying transition counts.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx:54-56 (isPlaying/animFrame/animTotal useState), :558-567 (playback input block: onStart → setAnimTotal, onFrame → setAnimFrame), :960-961 (cachedRotoPlaybackUrl / cachedRotoPlaybackActive view-model entries), :1096-1097 (isRotoCachedPlaybackActive + transport handlers), :1144 (statusMessage Previewing line).
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx:86-106 — the canvas-stack classes keyed off cachedRotoPlaybackActive/composition (:86-89) and the playback <img class="physics-paint-cached-roto-playback"> slot (:105): the live canvas image surface.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:955-965 — the transport row: Play/Stop button (:955), the nav-pill <output class="physics-paint-current-frame"> (:956, the PINNED minimal indicator per resolved Open Question 1), loop toggle (:962), fps input (:965).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pattern 4 (freeze gate; three live surfaces enumerated), Pitfall 3 (signal writes from event sources, never render bodies), Pitfall 4 (freezing the wrong surfaces — D-01's live set is small and explicit; over-gating is as much a behavior change as under-gating), Open Questions (RESOLVED) item 1.
    - .planning/phases/38.1-studio-render-path-performance/38.1-PATTERNS.md — §useRotoCachedPlayback.ts (per-tick surface to narrow; patterns to preserve verbatim) and §PhysicsPaintStudio.tsx (playback wiring excerpt).
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts — the 7 source-grep assertions; every asserted literal in touched blocks must survive verbatim (moves allowed, mutations not).
  </read_first>
  <action>
    Convert the playback per-tick surface from useState to signals and wire exactly three live surfaces, keeping the mechanism signals-native per project CLAUDE.md (no new hook layer; effects only for external sync):

    Step 1 — useRotoCachedPlayback.ts: move the per-tick frame off useState onto a signal surface. Public-contract preservation (useRotoCachedPlayback.test.ts must keep passing — preferably UNMODIFIED): keep isActive as render state (it transitions only on start/stop — transport is a sanctioned live surface); convert the per-tick setFrame (:113) to a signal write and expose the per-tick surface as signal-backed members (suggested additive member: a playbackTick signal carrying { frameIndex, appFrame, dataUrl } — exact name/shape is executor latitude). Keep playback.frame readable as a plain value for the existing test (e.g. a getter over the signal on the returned object); if the test must change, update it deliberately in the same commit and record the justification in the commit message (RESEARCH Pitfall 1). VERBATIM, untouched: the onStart(frameCount)/onFrame(frameIndex, appFrame)/setIsPlaying callback contract and call counts, the three status strings (:91, :101-103, :124), clampRotoPlaybackFps, inputRef.current = input, start/stop/toggle/updateFps/resetForLaunch semantics, both useEffects.

    Step 2 — PhysicsPaintStudio.tsx: delete the animFrame/animTotal useState (:55-56). Rewire the playback input block (:558-567): onStart and onFrame write the per-tick signal(s) (every tick is a real advance — no equality guard needed) instead of setAnimTotal/setAnimFrame; setIsPlaying stays as-is (start/stop transitions only). The statusMessage computation at :1144 keeps the VERBATIM `Previewing ${frameIndex + 1} / ${total}` format but reads the tick signal values — during playback the capsule is frozen so this line does not re-render per tick (resolved Open Question 1: the Previewing line is NOT the live indicator; it freezes with the capsule per 38.1-D-01). CRITICAL: never read the per-tick signal's .value in the Studio render body or in the view-model literal (:943-1148) — that subscribes the whole Studio per tick and recreates the defect.

    Step 3 — live surface 1, canvas image: the playback <img> slot (PhysicsPaintStudioView.tsx:105) must swap src per tick WITHOUT a Studio render. Convert that slot to a narrow subscriber component that reads the per-tick signal (dataUrl) directly. The cached-roto-playback-active / cached-roto-playback-ready classes (:86-89) key off isActive/composition — start/stop transitions only; do NOT route per-tick values into their inputs. Adjust the view-model entries at Studio:960-961 so the Studio does not subscribe per tick (pass the signal through, or move consumption into the narrow child).

    Step 4 — live surface 2, minimal current-frame indicator (Open Question 1 RESOLVED — pinned): the EXISTING nav-pill <output class="physics-paint-current-frame"> (PhysicsPaintWorkflowStrip.tsx:956), placement unchanged in the transport row between the Play/Stop transport button (:955) and the loop toggle (:962). Wrap its VALUE in a narrow child component that reads the playback appFrame signal during active playback and shows props.currentFrame otherwise. Element, class, placement, and surrounding DOM byte-identical (38.1-D-09); NO other Strip component reads the per-tick signal — cells, capsule, and the parent Strip body stay unsubscribed (frozen). Do NOT read the signal in the Strip render body (that subscribes all ~120 cells per tick — the opposite of the goal).

    Step 5 — live surface 3, transport state: the Play/Stop button active state (:955) and isPlaying continue to render on start/stop transitions only — no per-tick involvement.

    Scope guards: do NOT touch the capsule derivation block (Strip:443-467), any tooltip mount, the cell render loop, or the right sidebar; no derivation-semantics changes anywhere (38.1-D-09); keep every source-grep-asserted literal in touched blocks verbatim. The D-02 event queue and the single-catch-up stop path are Task 2 scope — this task lands the per-tick signal path end-to-end on the three live surfaces.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts 2>&1 | tee /tmp/38.1-05-t1-strip.txt >/dev/null; grep -E "Tests +1 failed \|" /tmp/38.1-05-t1-strip.txt && test "$(grep -cE '^ *FAIL ' /tmp/38.1-05-t1-strip.txt)" -eq 1 && grep -E '^ *FAIL ' /tmp/38.1-05-t1-strip.txt | grep -q 'places header tooltips below their anchors' && pnpm --dir app typecheck</automated>
    <human-check>Live playback feel (full-fps playback, frozen UI, live indicator) is verified in the 38.1-06 blocking native UAT — not in this plan.</human-check>
  </verify>
  <acceptance_criteria>
    - grep of PhysicsPaintStudio.tsx shows zero remaining animFrame/animTotal/setAnimFrame/setAnimTotal identifiers, and zero per-tick signal .value reads in the Studio render body or the view-model literal.
    - grep evidence that the per-tick signal is read (.value) ONLY inside the named narrow subscribers (the playback img slot child and the nav-pill output child) — zero reads in the Strip body, the capsule block, the cell render loop, or the right sidebar.
    - useRotoCachedPlayback.test.ts passes (unmodified, or deliberately updated in the same commit with recorded justification); the callback spy contract (onStart args, onFrame last-call args, setIsPlaying transition counts) is intact.
    - The three playback status strings are byte-identical in source (grep-verified); both useEffects and clampRotoPlaybackFps are untouched.
    - The nav-pill indicator keeps the exact element `<output class="physics-paint-current-frame">` at its current placement; the `Previewing ${...} / ${...}` statusMessage format string is verbatim in source.
    - PhysicsPaintStudio.test.ts passes UNMODIFIED; the strip gate shows exactly the one known-red placement test failing (all other assertions green); no other test file in git diff --name-only; typecheck clean; the 3 known-red D-15 tests untouched.
  </acceptance_criteria>
  <done>One playback session path runs end-to-end on signals: per-tick advances reach only the canvas image, the nav-pill frame indicator, and transport state; no per-tick setState remains; the hook's public contract and every preserved-verbatim pattern are intact. The end-to-end freeze is committed on the single path; the D-02 queue and one-catch-up stop path land in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-02 event queue + exactly-one catch-up render on stop + tube/log freeze guard (38.1-D-01, D-02, D-08 tube/log item)</name>
  <files>app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts — finishPlayback (:64-69) is the single stop funnel used by stop(), toggle() off, loop-end finish, and resetForLaunch; the workflowMode stop effect at :146-148 also routes through stop(). The missing-frame status line is set in start() at :101-103; toggle() sets 'Cached Roto playback stopped.' at :124.
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:455-467 — the capsule arbitration (feedback recency rungs + ambient idle-context feed) that shows the flushed line once and then resumes idle context per 38 D-10. DO NOT TOUCH (38.1-D-10).
    - .planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md — D-01 (exactly one catch-up render on stop), D-02 (queue, flush once on stop, idle context resumes; deferred — not dropped).
    - .planning/phases/38.1-studio-render-path-performance/38.1-RESEARCH.md — Pattern 4 (freeze gate + event queue), Pitfall 4 (capsule loses events; two catch-up renders flicker on stop).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md — 38 D-10 capsule event semantics this queue defers (wording verbatim).
    - app/src/components/physic-paint/PhysicsPaintRightSidebar.test.ts — source-grep surface for the right sidebar (tube/log); re-check asserted literals if any touched block overlaps.
  </read_first>
  <action>
    Step 1 — D-02 queue: capsule-bound status/event updates raised while playback is ACTIVE (the missing-frame status set in start() and any setStatus calls during active playback) are appended to a queue/pending slot instead of publishing to render state per tick. On the stop path — finishPlayback, the single funnel for stop(), toggle() off, loop-end finish, resetForLaunch, and the workflowMode stop effect — flush the queued events ONCE into the existing status channel inside the SAME synchronous block as the isActive/setIsPlaying restoration (wrap signal writes in batch() from @preact/signals where signals are involved), so Preact's render queue produces exactly ONE catch-up render. The 'Cached Roto playback stopped.' line (toggle :124) keeps its existing stop-time behavior as part of that same render. After the flush, the existing 36.15/38 capsule arbitration (Strip:455-467, untouched) shows the flushed line once and then resumes the idle context line per 38 D-10. Never drop a queued event; never flush twice — the queue is cleared on flush, and a second stop without an intervening start flushes nothing.

    Step 2 — catch-up render cardinality: all stop-path state restoration happens in ONE synchronous block — no await between the queue flush and the play-state restoration, no second render pass. Record the code-inspection evidence (the single block, the batch wrapper if used) in the SUMMARY.

    Step 3 — tube/log freeze guard (38.1-D-08 tube/log-during-playback item): audit the right sidebar (PhysicsPaintRightSidebar and its children) and the view-model literal for any read of the per-tick signal — there must be NONE; the tube/log re-renders only on start/stop transitions and genuine structural updates. Combine this with the Task 1 strip-side grep gate and record both halves of the subscriber audit in the SUMMARY (the complete list of per-tick signal subscribers, proving it is exactly the three sanctioned live surfaces).

    Scope guards: capsule arbitration + idle feed verbatim (38.1-D-10); status strings verbatim (38.1-D-09); tooltip mechanism untouched; no test file edits except the deliberately-justified hook test update from Task 1 (if any).
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintRightSidebar.test.ts && pnpm --dir app vitest run 2>&1 | tee /tmp/38.1-05-t2-full.txt >/dev/null; grep -E "Tests +3 failed \|" /tmp/38.1-05-t2-full.txt && test "$(grep -cE '^ *FAIL ' /tmp/38.1-05-t2-full.txt)" -eq 3 && test "$(grep -E '^ *FAIL ' /tmp/38.1-05-t2-full.txt | grep -vcE 'physicsPaintWorkflowPresentation\.test\.ts|PhysicsPaintWorkflowStrip\.test\.ts')" -eq 0 && pnpm --dir app typecheck</automated>
    <human-check>The single catch-up render, the once-only missing-frame line, and the idle-context resumption are verified in the 38.1-06 blocking native UAT (script section C).</human-check>
  </verify>
  <acceptance_criteria>
    - The queue flush occurs on every stop path (enumerated in code: stop(), toggle() off, loop-end finish, resetForLaunch, workflowMode stop effect) and is single-shot — the queue is cleared on flush and a repeated stop flushes nothing.
    - The flush and the play-state restoration sit in ONE synchronous block with no intervening await; the evidence is recorded in the SUMMARY.
    - The subscriber audit is recorded in the SUMMARY: the per-tick signal has exactly the three sanctioned subscribers (playback img slot, nav-pill output child, start/stop-state consumers); grep shows zero reads in the right sidebar, the Strip body, the capsule block, and the cell render loop.
    - git diff shows zero changed lines in the capsule derivation block (Strip:455-467 region) and zero changed tooltip code; status strings byte-identical.
    - Full-suite gate: exactly 3 failed tests, all in the two known-red D-15 files (physicsPaintWorkflowPresentation.test.ts, PhysicsPaintWorkflowStrip.test.ts); everything else green; typecheck clean.
  </acceptance_criteria>
  <done>Missing-frame events queue during playback and flush exactly once on every stop path inside the single catch-up render; the idle context resumes per 38 D-10; the tube/log freeze is grep-proven; the full suite is green except the 3 sanctioned known-red D-15 tests. The playback freeze (38.1-D-01/D-02) is complete and automated-ready for the 38.1-06 native UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| playback tick -> render subscribers | a stray fourth subscriber to the per-tick signal reintroduces the 60fps full-Studio render this plan removes |
| event queue -> capsule | a dropped or double-flushed queued event corrupts the 38 D-10 missing-frame contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38.1-05-01 | Denial of Service | per-tick signal surface | high | mitigate | This plan IS the mitigation: per-tick writes no longer trigger Studio renders; both tasks carry grep gates proving the signal has only the three sanctioned subscribers, and the audit is recorded in the SUMMARY |
| T-38.1-05-02 | Tampering | D-02 event queue | medium | mitigate | Queue never drops events, flushes exactly once on every stop path (single funnel through finishPlayback), clears on flush; status strings and capsule arbitration verbatim (38.1-D-09/D-10) |
| T-38.1-05-03 | Tampering | playback hook public contract | high | mitigate | Callback contract, status strings, and control semantics preserved verbatim; the existing hook test suite gates the contract (updated only deliberately with recorded justification) |
| T-38.1-SC | Tampering | package installs | high | mitigate | No installs — signal primitives come from the already-installed, already-sanctioned @preact/signals (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- pnpm --dir app vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts — hook contract green
- pnpm --dir app vitest run — full suite: exactly 3 failures, all known-red D-15 (deterministic grep gate, Task 2)
- pnpm --dir app typecheck — clean
- Subscriber audit in the SUMMARY: exactly three per-tick signal subscribers (38.1-D-01 live set)
</verification>

<success_criteria>
- During cached playback only the canvas image, the nav-pill current-frame indicator, and play/stop control state update; cells, capsule, and tube/log are frozen (38.1-D-01).
- On stop, exactly one catch-up render restores full UI currency; queued missing-frame events show once, then idle context resumes (38.1-D-01/D-02).
- animFrame/animTotal and every per-tick setState are gone from the playback path.
- Hook contract, status strings, capsule arbitration, and tooltip mechanisms are verbatim (38.1-D-09/D-10).
</success_criteria>

## Artifacts this phase produces

- `RotoCachedPlayback` per-tick signal surface — NEW additive signal-backed member(s) on the playback hook result carrying the per-tick frame (suggested shape: { frameIndex, appFrame, dataUrl }); exact name is executor latitude, additive only.
- Narrow playback-image subscriber component in `app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx` — NEW file-local, unexported; reads the per-tick signal for the playback `<img>` src.
- Narrow nav-pill output child in `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` — NEW file-local, unexported; renders the exact `<output class="physics-paint-current-frame">` element, reading the playback appFrame signal during active playback and props.currentFrame otherwise.
- D-02 playback event queue inside `useRotoCachedPlayback.ts` — internal, unexported; single-flush-on-stop semantics.

<output>
Create `.planning/phases/38.1-studio-render-path-performance/38.1-05-SUMMARY.md` when done
</output>
