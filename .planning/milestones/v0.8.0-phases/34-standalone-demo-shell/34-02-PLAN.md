---
phase: 34-standalone-demo-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - packages/efx-physic-paint/demo/vite.config.ts
  - packages/efx-physic-paint/demo/index.html
  - packages/efx-physic-paint/demo/src/main.tsx
  - packages/efx-physic-paint/demo/src/styles.css
autonomous: false
requirements:
  - RUN-02
must_haves:
  truths:
    - "RUN-02: User can iterate on the package-local Vite/Preact demo with browser HMR while library build remains separate per D-08 and D-11."
    - "User can identify the shell as standalone @efxlab/efx-physic-paint and not EFX Motion Editor integration per D-10, D-12, D-13, and D-14."
    - "D-15: User sees a minimal visible error if the root mount point is missing or if the canvas wrapper fails to create a canvas."
  artifacts:
    - path: "packages/efx-physic-paint/demo/vite.config.ts"
      provides: "Demo-local Vite config with Preact plugin and public-subpath source alias"
      exports: ["default"]
      contains: "@efxlab/efx-physic-paint/preact"
    - path: "packages/efx-physic-paint/demo/index.html"
      provides: "Demo HTML entry"
      contains: "/src/main.tsx"
    - path: "packages/efx-physic-paint/demo/src/main.tsx"
      provides: "Preact DemoApp mounting EfxPaintCanvas with fixed empty-paper config and D-15 visible mount failures"
      contains: "Vite demo / public Preact API / no editor runtime"
    - path: "packages/efx-physic-paint/demo/src/styles.css"
      provides: "Plain CSS shell/header/status/canvas/error layout"
      contains: ".demo-shell"
  key_links:
    - from: "packages/efx-physic-paint/demo/src/main.tsx"
      to: "@efxlab/efx-physic-paint/preact"
      via: "public subpath import"
      pattern: "import .*EfxPaintCanvas.* from '@efxlab/efx-physic-paint/preact'"
    - from: "packages/efx-physic-paint/demo/vite.config.ts"
      to: "packages/efx-physic-paint/src/preact.tsx"
      via: "resolve.alias for source HMR"
      pattern: "new URL\\('../src/preact.tsx', import.meta.url\\)"
---

<objective>
Create the package-local standalone Vite/Preact demo shell.

Purpose: RUN-02 requires a browser demo that edits with Vite/Preact HMR while proving the public Preact wrapper and staying outside the EFX Motion Editor/Tauri runtime. This implements D-01, D-02, D-03, D-04, D-08, D-09, D-10, D-12, D-13, D-14, D-15, and D-16.
Output: Demo-local Vite config, HTML entry, Preact shell, and plain CSS that mount the real `EfxPaintCanvas` public wrapper without controls/diagnostics scope creep.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-standalone-demo-shell/34-CONTEXT.md
@.planning/phases/34-standalone-demo-shell/34-RESEARCH.md
@.planning/phases/34-standalone-demo-shell/34-PATTERNS.md
@packages/efx-physic-paint/package.json
@packages/efx-physic-paint/src/preact.tsx
@packages/efx-physic-paint/src/types.ts
@packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
@packages/efx-physic-paint/tsup.config.ts
</context>

## Artifacts this phase produces

- New file: `packages/efx-physic-paint/demo/vite.config.ts`.
- New default export: `packages/efx-physic-paint/demo/vite.config.ts#default defineConfig(...)`.
- New Vite alias key: `@efxlab/efx-physic-paint/preact` mapped to `fileURLToPath(new URL('../src/preact.tsx', import.meta.url))`.
- New file: `packages/efx-physic-paint/demo/index.html`.
- New HTML root element: `div#app`.
- New module entry: `packages/efx-physic-paint/demo/src/main.tsx`.
- New Preact component: `DemoApp`.
- New local helper function: `mountDemo` or equivalent root-mount routine.
- New demo-local canvas wrapper probe: `CanvasMountProbe` or equivalent D-15 mount-readiness component.
- New demo status string: `Vite demo / public Preact API / no editor runtime`.
- New D-15 root error string: `Unable to mount standalone paint demo: #app root not found`.
- New D-15 canvas-wrapper error string: `Unable to mount standalone paint demo: canvas wrapper did not create a canvas`.
- New stylesheet: `packages/efx-physic-paint/demo/src/styles.css`.
- New CSS selectors/classes: `.demo-shell`, `.demo-header`, `.demo-status`, `.demo-canvas-shell`, `.demo-canvas`, `.demo-error`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add demo-local Vite config and HTML entry</name>
  <files>packages/efx-physic-paint/demo/vite.config.ts, packages/efx-physic-paint/demo/index.html</files>
  <read_first>
    - packages/efx-physic-paint/package.json
    - packages/efx-physic-paint/tsup.config.ts
    - app/vite.config.ts
    - app/index.html
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
    - .planning/phases/34-standalone-demo-shell/34-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: Vite config imports `defineConfig`, `preact`, and `fileURLToPath`/`URL`.
    - Test 2: Vite alias maps `@efxlab/efx-physic-paint/preact` to `fileURLToPath(new URL('../src/preact.tsx', import.meta.url))` per D-08.
    - Test 3: Vite config does not import Tauri, Tailwind, Motion Canvas, or `app/` per D-10.
    - Test 4: HTML contains `div#app` and `/src/main.tsx`.
  </behavior>
  <action>Create `packages/efx-physic-paint/demo/vite.config.ts` and `packages/efx-physic-paint/demo/index.html` per D-01 and D-02. In `vite.config.ts`, export `defineConfig({ plugins: [preact()], resolve: { alias: { '@efxlab/efx-physic-paint/preact': fileURLToPath(new URL('../src/preact.tsx', import.meta.url)) } } })`. Do not include Tauri server config, Tailwind, Motion Canvas plugin, editor aliases, or app imports per D-10. In `index.html`, use title `@efxlab/efx-physic-paint Standalone Demo`, root `<div id="app"></div>`, and module script `/src/main.tsx`. Do not add routing, additional HTML pages, or diagnostics pages per D-04.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const vite = fs.readFileSync('packages/efx-physic-paint/demo/vite.config.ts','utf8');
const html = fs.readFileSync('packages/efx-physic-paint/demo/index.html','utf8');
for (const required of ["from 'vite'", "from '@preact/preset-vite'", "from 'node:url'", "@efxlab/efx-physic-paint/preact", "new URL('../src/preact.tsx', import.meta.url)"]) if (!vite.includes(required)) throw new Error('vite config missing '+required);
for (const forbidden of ['@tauri-apps', '@efxlab/motion-canvas-vite-plugin', '@tailwindcss/vite', "from 'app/", 'from "app/']) if (vite.includes(forbidden)) throw new Error('vite config contains forbidden '+forbidden);
for (const required of ['<div id="app"></div>', '<script type="module" src="/src/main.tsx"></script>', '@efxlab/efx-physic-paint Standalone Demo']) if (!html.includes(required)) throw new Error('html missing '+required);
NODE</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `demo/vite.config.ts` contains `plugins: [preact()]`.
    - Source assertion: `demo/vite.config.ts` contains alias key `@efxlab/efx-physic-paint/preact` and target `new URL('../src/preact.tsx', import.meta.url)`.
    - Source assertion: `demo/vite.config.ts` contains none of `@tauri-apps`, `@efxlab/motion-canvas-vite-plugin`, `@tailwindcss/vite`, or `app/`.
    - Source assertion: `demo/index.html` contains `@efxlab/efx-physic-paint Standalone Demo`, `div id="app"`, and `/src/main.tsx`.
  </acceptance_criteria>
  <done>Demo-local Vite config and HTML entry exist and prove the public-subpath alias for HMR without editor runtime imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the standalone Preact canvas shell</name>
  <files>packages/efx-physic-paint/demo/src/main.tsx, packages/efx-physic-paint/demo/src/styles.css</files>
  <read_first>
    - packages/efx-physic-paint/demo/src/main.tsx
    - packages/efx-physic-paint/demo/src/styles.css
    - packages/efx-physic-paint/src/preact.tsx
    - packages/efx-physic-paint/src/types.ts
    - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
    - app/src/main.tsx
    - app/src/index.css
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
    - .planning/phases/34-standalone-demo-shell/34-RESEARCH.md
    - .planning/phases/34-standalone-demo-shell/34-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `main.tsx` imports `render`, Preact hooks for the canvas mount probe, `EfxPaintCanvas`, and `./styles.css`.
    - Test 2: `DemoApp` renders `@efxlab/efx-physic-paint standalone demo` and `Vite demo / public Preact API / no editor runtime` per D-13 and D-14.
    - Test 3: `DemoApp` mounts `<EfxPaintCanvas width={1000} height={650} papers={[]} />` using current wrapper props; it does not use `paperPath`, `defaultPaper`, or `onEngine`.
    - Test 4: `main.tsx` does not import from `app/`, `@tauri-apps/*`, package engine internals, editor stores, or editor shortcuts per D-03 and D-10.
    - Test 5: mount failure is visible in `.demo-error` text if `document.getElementById('app')` is missing, and canvas/wrapper mount failure is visible if the wrapper does not create a canvas inside `.demo-canvas-shell` per D-15; no diagnostic panel or controls are added per D-16.
  </behavior>
  <action>Create `demo/src/main.tsx` and `demo/src/styles.css` for a single-page shell per D-04, D-12, and D-16. In `main.tsx`, define `DemoApp` with the header `@efxlab/efx-physic-paint standalone demo`, status line `Vite demo / public Preact API / no editor runtime`, and a demo-local `CanvasMountProbe` or equivalent component that renders `<EfxPaintCanvas width={1000} height={650} papers={[]} class="demo-canvas" onEngineReady={(engine) => { engine.setTool('paint') }} />` imported from `@efxlab/efx-physic-paint/preact` per D-08 and D-09. Use `papers={[]}` exactly; 34-RESEARCH.md resolved that `EfxPaintEngine.init()` tolerates an empty papers array, so do not add a demo/public paper asset, `defaultPaper`, or conditional paper branch. Implement two minimal visible D-15 error paths without building a diagnostic panel: if `document.getElementById('app')` is missing, write/render `.demo-error` text `Unable to mount standalone paint demo: #app root not found`; inside the canvas shell, use a `ref` plus Preact state/effect to check after render that a canvas exists under `.demo-canvas-shell`, and if not, display `.demo-error` text `Unable to mount standalone paint demo: canvas wrapper did not create a canvas`. Clear or suppress the canvas-wrapper error once `onEngineReady` fires. Do not use `dangerouslySetInnerHTML`. Do not add custom controls for tool, color, brush size, opacity, physics settings, or diagnostics per D-16. In CSS, use plain CSS only with `.demo-shell`, `.demo-header`, `.demo-status`, `.demo-canvas-shell`, `.demo-canvas`, and `.demo-error`. Do not import Tailwind or app CSS.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const main = fs.readFileSync('packages/efx-physic-paint/demo/src/main.tsx','utf8');
const css = fs.readFileSync('packages/efx-physic-paint/demo/src/styles.css','utf8');
for (const required of ["from 'preact'", "from 'preact/hooks'", "from '@efxlab/efx-physic-paint/preact'", "./styles.css", '@efxlab/efx-physic-paint standalone demo', 'Vite demo / public Preact API / no editor runtime', 'EfxPaintCanvas', 'papers={[]}', 'onEngineReady', 'querySelector', 'canvas', 'Unable to mount standalone paint demo: #app root not found', 'Unable to mount standalone paint demo: canvas wrapper did not create a canvas']) if (!main.includes(required)) throw new Error('main missing '+required);
for (const forbidden of ['paperPath', 'defaultPaper', 'onEngine=', '/paper_1.jpg', 'demo/public', '@tauri-apps', '../src/engine', 'from "app/', "from 'app/", 'mountShortcuts', 'paintStore', 'dangerouslySetInnerHTML']) if (main.includes(forbidden)) throw new Error('main contains forbidden '+forbidden);
for (const required of ['.demo-shell', '.demo-header', '.demo-status', '.demo-canvas-shell', '.demo-canvas', '.demo-error']) if (!css.includes(required)) throw new Error('css missing '+required);
NODE</automated>
    <automated>pnpm --filter @efxlab/efx-physic-paint demo:build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `main.tsx` imports `EfxPaintCanvas` from `@efxlab/efx-physic-paint/preact` and not from `../src/preact.tsx` or `../src/engine/EfxPaintEngine`.
    - Source assertion: rendered copy includes `@efxlab/efx-physic-paint standalone demo` and `Vite demo / public Preact API / no editor runtime`.
    - Source assertion: `main.tsx` contains `papers={[]}` and `onEngineReady` and contains none of `paperPath`, `defaultPaper`, `/paper_1.jpg`, nor `onEngine=`.
    - Source assertion: `main.tsx` contains both D-15 visible error strings: `Unable to mount standalone paint demo: #app root not found` and `Unable to mount standalone paint demo: canvas wrapper did not create a canvas`.
    - Source assertion: CSS includes `.demo-shell`, `.demo-header`, `.demo-status`, `.demo-canvas-shell`, `.demo-canvas`, and `.demo-error`.
    - CLI assertion: `pnpm --filter @efxlab/efx-physic-paint demo:build` exits 0.
  </acceptance_criteria>
  <done>The demo shell mounts the real public Preact canvas with standalone copy, fixed empty-paper configuration, visible root and canvas-wrapper mount errors, and no Phase 35 controls or editor runtime dependency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies the standalone dev server manually</name>
  <files>packages/efx-physic-paint/demo/vite.config.ts, packages/efx-physic-paint/demo/index.html, packages/efx-physic-paint/demo/src/main.tsx, packages/efx-physic-paint/demo/src/styles.css, package.json, packages/efx-physic-paint/package.json</files>
  <read_first>
    - package.json
    - packages/efx-physic-paint/package.json
    - packages/efx-physic-paint/demo/src/main.tsx
    - packages/efx-physic-paint/demo/src/styles.css
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
  </read_first>
  <action>Pause for user-run UAT after automated `demo:build` passes. Do not run `pnpm dev:paint` yourself because project CLAUDE.md says the user runs servers. Ask the user to run `pnpm dev:paint` from repository root, open the Vite URL, verify the standalone title/status/canvas, and edit `packages/efx-physic-paint/demo/src/main.tsx` or `packages/efx-physic-paint/demo/src/styles.css` to confirm HMR. This checkpoint verifies D-05, D-08, D-10, D-13, and D-14 at browser runtime.</action>
  <what-built>Package-local Vite/Preact demo shell and root `pnpm dev:paint` command were automated.</what-built>
  <how-to-verify>
    1. From repository root, run `pnpm dev:paint`.
    2. Open the Vite URL printed by the command in a browser.
    3. Confirm the page header includes `@efxlab/efx-physic-paint standalone demo`.
    4. Confirm the status line reads exactly `Vite demo / public Preact API / no editor runtime`.
    5. Confirm the page displays the physics paint canvas and does not show app/Tauri/editor UI.
    6. Edit `packages/efx-physic-paint/demo/src/main.tsx` or `packages/efx-physic-paint/demo/src/styles.css` and confirm Vite updates the browser without running `tsup --watch`.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @efxlab/efx-physic-paint demo:build</automated>
    <human-check>User confirms `pnpm dev:paint` launches the standalone browser demo and HMR updates the package-local demo files.</human-check>
  </verify>
  <acceptance_criteria>
    - Human assertion: root `pnpm dev:paint` starts the standalone browser demo.
    - Human assertion: browser page contains `@efxlab/efx-physic-paint standalone demo`.
    - Human assertion: browser page contains `Vite demo / public Preact API / no editor runtime`.
    - Human assertion: browser page does not display EFX Motion Editor/Tauri shell UI.
    - Human assertion: editing a demo file triggers browser HMR.
  </acceptance_criteria>
  <done>User has approved the standalone demo launch and HMR behavior or provided actionable runtime issues for follow-up.</done>
  <resume-signal>Type "approved" if the standalone demo and HMR behave as expected, or describe the exact issue and screenshot if not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> demo DOM | Demo code renders visible HTML in the browser; injected HTML or runtime errors could affect local testing. |
| Vite dev server -> local source files | Vite serves demo files and resolves aliases into package source for HMR. |
| Demo shell -> package public wrapper | The demo exercises `@efxlab/efx-physic-paint/preact`, which creates engine canvases and pointer handlers in a DOM container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | `demo/src/main.tsx` rendered copy | mitigate | Render only static JSX strings for title/status/error; do not use `dangerouslySetInnerHTML`. |
| T-34-04 | Information Disclosure | `demo/vite.config.ts` and served files | mitigate | Do not configure demo public serving for secrets; Phase 34 uses `papers={[]}` and no demo public paper asset. |
| T-34-05 | Elevation of Privilege | Demo runtime boundary | mitigate | Verify no imports from `app/`, `@tauri-apps/*`, editor stores, or package engine internals; use public Preact subpath alias only. |
| T-34-06 | Denial of Service | Missing mount root or failed canvas wrapper mount | mitigate | Implement visible mount errors `Unable to mount standalone paint demo: #app root not found` and `Unable to mount standalone paint demo: canvas wrapper did not create a canvas` instead of failing silently. |
| T-34-SC | Tampering | Vite/Preact dev dependency execution | mitigate | Depend on packages approved in 34-RESEARCH.md and validate through `demo:build`; no additional package install beyond Plan 01. |
</threat_model>

<verification>
Run `pnpm --filter @efxlab/efx-physic-paint demo:build` after creating the demo files. Do not run `pnpm dev:paint` or any Vite dev server in automation; Task 3 gives the user exact manual UAT steps.
</verification>

<success_criteria>
- RUN-02 demo shell builds through the package-local Vite config.
- D-01 through D-04 and D-08 through D-16 are implemented without deferred Phase 35 controls.
- D-15 is covered by visible root-mount and canvas-wrapper mount-failure error paths.
- User can manually confirm repo-root launch and HMR without Claude running the server.
</success_criteria>

<output>
Create `.planning/phases/34-standalone-demo-shell/34-02-SUMMARY.md` when done.
</output>
