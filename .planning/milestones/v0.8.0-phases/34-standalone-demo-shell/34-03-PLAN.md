---
phase: 34-standalone-demo-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - packages/efx-physic-paint/README.md
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
must_haves:
  truths:
    - "RUN-03: User can follow package README instructions that match actual root and package scripts."
    - "RUN-01: README documents repo-root `pnpm dev:paint` as the standalone browser demo command."
    - "RUN-02: README explains Vite demo/HMR is separate from package build/check and library outputs per D-07 and D-11."
    - "User can identify standalone physics paint as package demo, not editor paint-layer integration per D-13."
  artifacts:
    - path: "packages/efx-physic-paint/README.md"
      provides: "Accurate standalone demo, Preact wrapper, and development command documentation"
      contains: "pnpm dev:paint"
  key_links:
    - from: "packages/efx-physic-paint/README.md"
      to: "package.json scripts.dev:paint"
      via: "documented root command"
      pattern: "pnpm dev:paint"
    - from: "packages/efx-physic-paint/README.md"
      to: "packages/efx-physic-paint/package.json scripts.demo:build"
      via: "documented package build smoke command"
      pattern: "pnpm --filter @efxlab/efx-physic-paint demo:build"
---

<objective>
Update package documentation so users can run the standalone demo and understand the workflow boundaries.

Purpose: RUN-03 requires README instructions that match actual scripts. D-07 also requires the README to distinguish root app Vite, app Tauri desktop workflow, and standalone browser paint demo.
Output: Package README commands and examples match Phase 34 scripts and current `EfxPaintCanvas` props while explicitly stating this is not EFX Motion Editor integration.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-standalone-demo-shell/34-CONTEXT.md
@.planning/phases/34-standalone-demo-shell/34-RESEARCH.md
@.planning/phases/34-standalone-demo-shell/34-PATTERNS.md
@package.json
@app/package.json
@packages/efx-physic-paint/package.json
@packages/efx-physic-paint/README.md
@packages/efx-physic-paint/src/preact.tsx
</context>

## Artifacts this phase produces

- Updated README section: standalone browser demo instructions for `pnpm dev:paint`.
- Updated README section: workflow distinction among root `pnpm dev`, app Tauri command, and root `pnpm dev:paint`.
- Updated README command block containing `pnpm --filter @efxlab/efx-physic-paint demo:build`.
- Updated README command block containing `pnpm --filter @efxlab/efx-physic-paint build`.
- Updated README command block containing `pnpm --filter @efxlab/efx-physic-paint check`.
- Updated README Preact example using `EfxPaintCanvas`, `papers`, optional `defaultPaper`, and `onEngineReady`.
- Removed stale README example identifiers: `paperPath`, `onEngine`.

<tasks>

<task type="auto">
  <name>Task 1: Replace stale README Preact example with current public wrapper props</name>
  <files>packages/efx-physic-paint/README.md</files>
  <read_first>
    - packages/efx-physic-paint/README.md
    - packages/efx-physic-paint/src/preact.tsx
    - packages/efx-physic-paint/src/types.ts
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
    - .planning/phases/34-standalone-demo-shell/34-PATTERNS.md
  </read_first>
  <action>Update the README Preact usage section to use the current public wrapper import `import { EfxPaintCanvas } from '@efxlab/efx-physic-paint/preact'` per D-03 and D-09. The example must pass `width={1000}`, `height={650}`, `papers={[{ name: 'canvas1', url: '/img/paper_1.jpg' }]}`, `defaultPaper="canvas1"`, and `onEngineReady={(engine) => { engine.setTool('paint') }}`. Remove stale prop names `paperPath` and `onEngine`. Keep the example focused on wrapper usage only; do not document Phase 35 custom controls for color, brush size, opacity, physics settings, or diagnostics per D-16.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const md = fs.readFileSync('packages/efx-physic-paint/README.md','utf8');
for (const required of ["import { EfxPaintCanvas } from '@efxlab/efx-physic-paint/preact'", "papers={[{ name: 'canvas1', url: '/img/paper_1.jpg' }]}", 'defaultPaper="canvas1"', 'onEngineReady={(engine) => {', "engine.setTool('paint')"]) if (!md.includes(required)) throw new Error('README missing '+required);
for (const forbidden of ['paperPath', 'onEngine={(engine)']) if (md.includes(forbidden)) throw new Error('README still contains stale '+forbidden);
NODE</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: README contains `import { EfxPaintCanvas } from '@efxlab/efx-physic-paint/preact'`.
    - Source assertion: README example contains `papers={[{ name: 'canvas1', url: '/img/paper_1.jpg' }]}`.
    - Source assertion: README example contains `defaultPaper="canvas1"` and `onEngineReady`.
    - Source assertion: README contains neither `paperPath` nor `onEngine={(engine)`.
  </acceptance_criteria>
  <done>README public wrapper example matches current `EfxPaintCanvasProps` and does not suggest stale or deferred API usage.</done>
</task>

<task type="auto">
  <name>Task 2: Document executable Phase 34 commands and workflow boundaries</name>
  <files>packages/efx-physic-paint/README.md</files>
  <read_first>
    - packages/efx-physic-paint/README.md
    - package.json
    - app/package.json
    - packages/efx-physic-paint/package.json
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
    - .planning/phases/34-standalone-demo-shell/34-RESEARCH.md
    - .planning/phases/34-standalone-demo-shell/34-PATTERNS.md
  </read_first>
  <action>Update the README Development section so the documented commands exactly match actual scripts after Plans 01 and 02. Document root `pnpm dev:paint` as the standalone browser `@efxlab/efx-physic-paint` demo per D-05. Document `pnpm --filter @efxlab/efx-physic-paint demo:dev` as the package-local equivalent, `pnpm --filter @efxlab/efx-physic-paint demo:build` as the demo build smoke, `pnpm --filter @efxlab/efx-physic-paint build` as the library dist/types build, and `pnpm --filter @efxlab/efx-physic-paint check` as TypeScript checking per D-11. Add a workflow distinction note per D-07: root `pnpm dev` runs the app Vite frontend, the app Tauri command runs the desktop app, and root `pnpm dev:paint` runs the standalone physics paint browser demo. State that the standalone demo runs `packages/efx-physic-paint` and is not an editor paint-layer integration per D-10 and D-13. Do not tell users to run `pnpm dev` as the package demo. Do not document a root alias for `dev:watch` per D-06.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const md = fs.readFileSync('packages/efx-physic-paint/README.md','utf8');
for (const required of ['pnpm dev:paint', 'pnpm --filter @efxlab/efx-physic-paint demo:dev', 'pnpm --filter @efxlab/efx-physic-paint demo:build', 'pnpm --filter @efxlab/efx-physic-paint build', 'pnpm --filter @efxlab/efx-physic-paint check', 'root `pnpm dev` runs the app Vite frontend', 'Tauri', 'standalone physics paint browser demo', 'not an editor paint-layer integration']) if (!md.includes(required)) throw new Error('README missing '+required);
if (/pnpm dev\s+# Start demo app/.test(md)) throw new Error('README still documents pnpm dev as demo app');
NODE</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: README documents `pnpm dev:paint` as the root standalone browser demo command.
    - Source assertion: README documents `pnpm --filter @efxlab/efx-physic-paint demo:dev`, `demo:build`, `build`, and `check` commands.
    - Source assertion: README states root `pnpm dev` runs the app Vite frontend and the app Tauri command is the desktop app workflow.
    - Source assertion: README states standalone paint demo is not an editor paint-layer integration.
    - Source assertion: README no longer contains a command note equivalent to `pnpm dev # Start demo app`.
  </acceptance_criteria>
  <done>README command instructions match actual scripts and clearly distinguish app, desktop, and standalone paint workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Verify README commands against package scripts and demo boundaries</name>
  <files>packages/efx-physic-paint/README.md</files>
  <read_first>
    - packages/efx-physic-paint/README.md
    - package.json
    - packages/efx-physic-paint/package.json
    - packages/efx-physic-paint/tsup.config.ts
    - packages/efx-physic-paint/demo/vite.config.ts
    - .planning/phases/34-standalone-demo-shell/34-CONTEXT.md
  </read_first>
  <action>Perform final static verification for RUN-01, RUN-02, and RUN-03. Ensure every README command documented for Phase 34 appears in root or package scripts: `dev:paint`, package `demo:dev`, package `demo:build`, package `build`, and package `check`. Ensure README does not claim demo files are part of package exports, published files, or `tsup` entries per D-02. Ensure `packages/efx-physic-paint/tsup.config.ts` still has entries only for `index`, `preact`, and `animation` and does not include `demo`. Do not modify source code outside README in this task unless a prior task left README mismatched with the actual scripts.</action>
  <verify>
    <automated>node - <<'NODE'
const fs = require('fs');
const root = require('./package.json');
const pkg = require('./packages/efx-physic-paint/package.json');
const md = fs.readFileSync('packages/efx-physic-paint/README.md','utf8');
const tsup = fs.readFileSync('packages/efx-physic-paint/tsup.config.ts','utf8');
const checks = [
  [root.scripts?.['dev:paint'], 'pnpm --filter @efxlab/efx-physic-paint demo:dev', 'root dev:paint'],
  [pkg.scripts?.['demo:dev'], 'vite --config demo/vite.config.ts', 'pkg demo:dev'],
  [pkg.scripts?.['demo:build'], 'vite build --config demo/vite.config.ts', 'pkg demo:build'],
  [pkg.scripts?.build, 'tsup', 'pkg build'],
  [pkg.scripts?.check, 'tsc --noEmit', 'pkg check'],
];
for (const [actual, expected, label] of checks) if (actual !== expected) throw new Error(`${label} expected ${expected} got ${actual}`);
for (const command of ['pnpm dev:paint', 'pnpm --filter @efxlab/efx-physic-paint demo:dev', 'pnpm --filter @efxlab/efx-physic-paint demo:build', 'pnpm --filter @efxlab/efx-physic-paint build', 'pnpm --filter @efxlab/efx-physic-paint check']) if (!md.includes(command)) throw new Error('README missing command '+command);
if (JSON.stringify(pkg.exports||{}).includes('demo')) throw new Error('demo exported');
if (JSON.stringify(pkg.files||[]).includes('demo')) throw new Error('demo in files');
if (/demo\//.test(tsup) || /demo:/.test(tsup)) throw new Error('tsup config references demo');
NODE</automated>
    <automated>pnpm --filter @efxlab/efx-physic-paint check</automated>
    <automated>pnpm --filter @efxlab/efx-physic-paint build</automated>
    <automated>pnpm --filter @efxlab/efx-physic-paint demo:build</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: Node script confirms root/package script values and README command coverage.
    - CLI assertion: `pnpm --filter @efxlab/efx-physic-paint check` exits 0.
    - CLI assertion: `pnpm --filter @efxlab/efx-physic-paint build` exits 0.
    - CLI assertion: `pnpm --filter @efxlab/efx-physic-paint demo:build` exits 0.
    - Source assertion: `tsup.config.ts` does not reference `demo/`.
  </acceptance_criteria>
  <done>README, scripts, library build, and demo build agree; RUN-01, RUN-02, and RUN-03 are statically verifiable without running a dev server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> developer shell | Users copy documented commands into a shell; stale or wrong commands cause execution of wrong workflows. |
| README -> public API expectations | Users copy Preact examples; stale props cause runtime/type failures or encourage internal API usage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Spoofing | README workflow identity | mitigate | State that `pnpm dev:paint` is standalone `packages/efx-physic-paint` browser demo and not editor paint-layer integration. |
| T-34-08 | Tampering | README command examples | mitigate | Verify documented commands match actual root/package script values with a Node assertion. |
| T-34-09 | Information Disclosure | README workflow guidance | mitigate | Distinguish app Vite and Tauri workflows from standalone browser demo; avoid suggesting editor/Tauri coupling or secret/public asset handling. |
| T-34-10 | Tampering | README API examples | mitigate | Replace stale `paperPath` and `onEngine` with current public `papers`, `defaultPaper`, and `onEngineReady` props. |
| T-34-SC | Tampering | package manager commands in docs | accept | README documents pnpm commands only; package legitimacy handled in Plan 01 and no new installs occur in this plan. |
</threat_model>

<verification>
Run the task-level Node README checks, then `pnpm --filter @efxlab/efx-physic-paint check`, `pnpm --filter @efxlab/efx-physic-paint build`, and `pnpm --filter @efxlab/efx-physic-paint demo:build`. Do not run `pnpm dev:paint`.
</verification>

<success_criteria>
- RUN-03 README instructions match actual scripts.
- README documents RUN-01 root launch and RUN-02 demo/build separation.
- README does not reintroduce stale props or Phase 35 controls.
</success_criteria>

<output>
Create `.planning/phases/34-standalone-demo-shell/34-03-SUMMARY.md` when done.
</output>
