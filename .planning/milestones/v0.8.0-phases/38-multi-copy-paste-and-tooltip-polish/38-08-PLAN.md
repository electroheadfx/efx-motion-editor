---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 08
type: tdd
wave: 4
depends_on:
  - 38-06
files_modified:
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts
autonomous: true
requirements:
  - 38-CAPSULE-IDLE-CONTEXT
  - 38-TOOLTIP-VIEWPORT-PLACEMENT
  - 38-TOOLTIP-NOTCH-MULTILINE
  - 38-UAT-THEN-REGRESSION
tags:
  - physics-paint
  - roto
  - capsule
  - tooltip
  - regression-tests
  - post-uat

must_haves:
  truths:
    - "SC-1: getRotoStatusCapsuleIdleContext maps each cell kind to the UI-SPEC locked idle line — real → `Real Roto key · Frame {n}`, generated → `Generated frame · Frame {n}`, empty → `Empty frame · Frame {n}` — with the physical appFrame verbatim and the `·` middle-dot separator, and maps a null cell kind to null (D-09)."
    - "SC-2: getRotoStatusCapsuleViewModel with no higher-priority input returns the trimmed ambient idle-context line, and returns an empty string when ambient is absent, null, or blank — the deleted static baseline never reappears in any form (D-08)."
    - "SC-3: capsule arbitration priority is pending operation > saving indicator > most-recent guard/action feedback > ambient idle context; the idle context line never wins over any higher rung (36.15 D-15 ladder preserved byte-identical above the new fallback)."
    - "SC-4: simultaneous feedback lines resolve most-recent-wins with the later-candidate tie-break, and blank pending/feedback/ambient candidates are ignored (existing rung behavior unchanged)."
    - "The presentation test file contains zero occurrences of the deleted baseline message text, in assertions and comments alike."
    - "TP-1: computeTooltipPlacement maps region to opposite-side direction — 'bottom' → 'above', 'top' → 'below', 'right-edge' → 'left', 'left-edge' → 'right' — when the preferred side has room (D-11)."
    - "TP-2: the placement flips to the opposite direction when the preferred side lacks room for the pill plus the 6px notch gap plus the 8px viewport margin (D-11, UI-SPEC flip rule)."
    - "TP-3: pill left/top clamp into [8, viewport − pillSize − 8] on both axes — the pill never renders outside the viewport (UI-SPEC locked 8px margin, TOOLTIP_VIEWPORT_MARGIN = 8)."
    - "TP-4: notchOffset equals anchor center − pill left (above/below) or anchor middle − pill top (left/right), computed AFTER clamping, so the notch tracks the source control rather than the pill center (D-13, Pitfall 4)."
    - "TP-5: the tooltip CSS contract locks max-width: 280px, max-height: 96px, and white-space: normal in the tooltip blocks with no single-line nowrap or ellipsis truncation, and the 10x6 same-fill notch rules with the four direction modifiers (--above/--below/--left/--right) exist (D-13/D-14)."
    - "Final gate: full `pnpm --dir app vitest run`, `pnpm --dir app typecheck`, and `pnpm --dir app build` all exit 0 with the updated presentation and tooltip describes green (38-UAT-THEN-REGRESSION, D-15 closing sequence)."
  assumptions:
    - "Plans 38-03 and 38-05 production code has landed and plan 38-06 native user-owned UAT is explicitly approved; per D-15 this plan runs only after that approval and uses `vitest run` (never watch mode)."
    - "The production behavior under test is UAT-locked: tests assert the shipped contract. If a test exposes a genuine production defect (not a test-construction error), the executor stops and reports rather than editing production source in this plan."
    - "38-07 (sibling wave-4 plan) owns the group-seam test files (resolver, multiSelection, session); this plan's two view test files are disjoint from those, so the wave-4 no-shared-files rule holds."
  prohibitions:
    - "No production source edits: this plan touches ONLY the two test files in files_modified."
    - "No vitest watch mode, no test config changes, no one-off configs (CLAUDE.md: vitest run only; project memory: no test config hacks)."
    - "The deleted baseline message text must not be quoted anywhere in the updated test files, including comments (region-scoped negative gate)."
    - "No legacy source/display timing fields anywhere in new or updated test code (D-16 hygiene)."
    - "No deferred ideas tested: no group-aware Duplicate, no Copy/Paste keyboard shortcuts, no replace-style group paste (CONTEXT.md Deferred Ideas)."
    - "Do not modify the `useStyledTooltip` controller describe in PhysicsPaintStyledTooltip.test.ts — the 1000ms/focus/Escape contract is UAT-locked and already covered."
  artifacts:
    - "app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts — the 36.15-era capsule describe (:159-228) rewritten against the idle-context contract (SC-2/SC-3/SC-4) plus a new getRotoStatusCapsuleIdleContext mapping describe (SC-1)."
    - "app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts — new computeTooltipPlacement pure-function describe (TP-1..TP-4) and an extended tooltip CSS surface contract (TP-5)."
  key_links:
    - "The capsule tests import getRotoStatusCapsuleIdleContext and getRotoStatusCapsuleViewModel from './physicsPaintWorkflowPresentation' — the exact exported helper symbol shipped by 38-03 (verify against 38-03-SUMMARY before writing imports)."
    - "The tooltip tests import computeTooltipPlacement (and its exported region/direction types and margin/max-width constants) from './PhysicsPaintStyledTooltip' — the exact shipped signature (anchorRect, region, pillSize) → { direction, left, top, notchOffset } from 38-05 (verify against 38-05-SUMMARY)."
    - "Viewport dimensions inside computeTooltipPlacement come from the global window; tests stub window.innerWidth/innerHeight using the file's existing vi.stubGlobal idiom — fixtures, never DOM mounts."
---

<objective>
Post-UAT regression coverage for the Phase 38 presentation half, per D-15 (production first, native UAT blocking, tests after explicit approval), plus the phase-closing full gate. Two test targets: (1) rewrite the knowingly-red baseline assertions in `physicsPaintWorkflowPresentation.test.ts:159-228` against the shipped idle-context contract — per-cell-kind idle lines via `getRotoStatusCapsuleIdleContext`, the empty-string final fallback with the baseline deleted, and the unchanged pending > saving > feedback > ambient arbitration ladder; (2) add a pure-function describe for the greenfield `computeTooltipPlacement` in the existing `PhysicsPaintStyledTooltip.test.ts` — direction per region, flip on insufficient room, 8px viewport clamp, post-clamp notch offset — and extend the tooltip CSS surface contract with the locked 280x96 multiline clamp and notch rules. Close with the full `vitest run` → `typecheck` → `build` gate. Requirements: 38-CAPSULE-IDLE-CONTEXT, 38-TOOLTIP-VIEWPORT-PLACEMENT, 38-TOOLTIP-NOTCH-MULTILINE, 38-UAT-THEN-REGRESSION.

Purpose: the capsule idle-context mapping and the tooltip placement math are UAT-approved behavior with zero automated anchors (the presentation test file has been knowingly red since 38-03 per Pitfall 5). These tests convert the approved rulings into deterministic regression locks so later work cannot silently reintroduce a static capsule filler, break the arbitration ladder, or regress the opposite-of-region placement, flip, clamp, and notch-tracking math — the phase's only genuinely new code shape.

Output: two updated view test files, each green under targeted `pnpm --dir app vitest run <file>`, then the full phase gate green (vitest run + typecheck + build). This is the final plan of Phase 38.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-VALIDATION.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-03-PLAN.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-PLAN.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-03-SUMMARY.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-SUMMARY.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-SUMMARY.md

@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts
@app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capsule idle-context regression anchors — rewrite the status-capsule describe against the post-D-08 contract and add the getRotoStatusCapsuleIdleContext mapping describe (SC-1..SC-4)</name>
  <files>app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</files>
  <precondition>Plan 38-03 production code has landed (baseline deleted, `getRotoStatusCapsuleIdleContext` exported) and plan 38-06 native UAT is explicitly approved — 38-06-SUMMARY.md records the approval. This is the FIRST modification of this knowingly-red test file since 38-03 (D-15, Pitfall 5).</precondition>
  <read_first>
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts:159-228 — the capsule describe under rewrite: five `it` blocks whose ambient inputs and final-fallback expectations still reference the deleted static baseline; note which blocks assert the ladder (:168-187), most-recent-wins (:190-210), verbatim pass-through (:213-220), and blank-candidate handling (:223-226) so their non-baseline behavior carries over.
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts:155-220 — the SHIPPED selector: header comment, `RotoStatusCapsuleInput.ambient`, the `getRotoStatusCapsuleViewModel` body ending in the empty-string fallback, `trimCapsuleLine`, and the shipped `getRotoStatusCapsuleIdleContext` helper (confirm the exact exported symbol name and its input shape `{ cellKind, frame }` before writing imports).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-03-SUMMARY.md — the shipped helper name/contract and the exact locked idle-context strings.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-06-SUMMARY.md — the UAT approval record that authorizes this test work (D-15).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-03-PLAN.md Task 1 behavior block — the locked mapping and arbitration contract under test.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md §10 (selector pattern) and §14 (test conventions).
  </read_first>
  <behavior>
    - SC-1: `getRotoStatusCapsuleIdleContext({ cellKind: 'real', frame: 5 })` returns `Real Roto key · Frame 5`; `{ cellKind: 'generated', frame: 9 }` returns `Generated frame · Frame 9`; `{ cellKind: 'empty', frame: 7 }` returns `Empty frame · Frame 7`; `{ cellKind: null, frame: 3 }` returns null. The separator is the middle dot `·` exactly; the frame passes through verbatim.
    - SC-2: `getRotoStatusCapsuleViewModel({})` returns `''`; `{ pendingOperation: null, savingIndicator: null, feedback: [], ambient: null }` returns `''`; `{ ambient: 'Real Roto key · Frame 5' }` returns `Real Roto key · Frame 5`; `{ ambient: '   ' }` (blank) returns `''`.
    - SC-3 (one rung at a time, ambient as the lowest rung): feedback beats ambient; saving indicator beats feedback + ambient; pending operation beats saving + feedback + ambient. Use an idle-context line (e.g. `Real Roto key · Frame 5`) as the ambient input in every case.
    - SC-4 (carried over unchanged): simultaneous feedback lines resolve most-recent-wins by recency; equal recency resolves to the later candidate in the list; a blank pending operation falls through to the saving indicator; blank/null feedback entries fall through to ambient, and to `''` when ambient is also null.
  </behavior>
  <action>
    Edit ONLY this test file. Rewrite the capsule describe at :159-228 in place: retitle it to record the new authority, e.g. `getRotoStatusCapsuleViewModel — idle-context contract (38-08, D-08/D-09, post-UAT)`, and add a header comment recording that the 38-06 native UAT approval authorizes the rewrite and that the static baseline was deleted by D-08 — WITHOUT quoting the deleted baseline message text anywhere in the file, assertions or comments (a region-scoped negative gate greps for it).

    Replace the first `it` (the old baseline-fallback assertions) with the SC-2 cases from the behavior block: empty input → `''`, all-null input → `''`, supplied ambient → that line verbatim, blank ambient → `''`.

    Keep the ladder, most-recent-wins, verbatim pass-through, and blank-candidate `it` blocks with their structure and non-baseline assertions intact, but replace every ambient input that used the deleted baseline string with an idle-context line (`Real Roto key · Frame 5`), and change the final blank-candidates fallback expectation from the baseline to `''` per SC-4.

    Add a new describe `getRotoStatusCapsuleIdleContext — current-cell idle mapping (38-08, D-09, UI-SPEC locked)` with the four SC-1 cases. Add `getRotoStatusCapsuleIdleContext` to the existing import from './physicsPaintWorkflowPresentation' (verify the exact shipped symbol name first).

    Keep every other describe in the file byte-identical: cell fills/view models, missing-frame status copy, overlays, onion clamps, engine tone, dev export gating, the 37-06 drag-preview group describes, and the selected-tooltip-copy describe. Never edit production source: if the shipped selector or helper contradicts the UAT-locked contract above, stop and report rather than "fixing" production or weakening the assertion.

    RED→GREEN note: this describe is currently red by design (Pitfall 5); run the targeted file BEFORE editing to observe the known-red baseline failures, then confirm green after the rewrite. Targeted run only — never the full suite in this task, never watch mode.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts && grep -c "Missing frames play transparent" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts | grep -q '^0$' && grep -c "getRotoStatusCapsuleIdleContext" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app vitest run app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` exits 0.
    - `grep -c "Missing frames play transparent" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` returns exactly 0 (assertions and comments).
    - The new helper describe asserts all four SC-1 mapping cases with the exact locked strings including the `·` separator, and the null-cellKind → null case.
    - The capsule describe asserts the empty-string fallback for absent/null/blank ambient, the four-rung ladder with ambient lowest, most-recent-wins with the later-candidate tie-break, and blank-candidate pass-through — all against idle-context ambient inputs.
    - Every other describe in the file is byte-identical to its pre-task form (diff touches only the capsule describe, the new helper describe, the import line, and the header comment).
    - `git diff --name-only` shows exactly one file: this test file. No production source touched. No watch-mode invocation.
  </acceptance_criteria>
  <done>The capsule describe locks the UAT-approved idle-context contract (SC-1..SC-4), the deleted baseline appears nowhere in the file, the targeted vitest run is green, and no other file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeTooltipPlacement pure-function describe + tooltip CSS multiline-clamp contract (TP-1..TP-5)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts</files>
  <precondition>Plan 38-05 production code has landed (`computeTooltipPlacement`, `TooltipRegion`/`TooltipDirection`, and the margin/max-width constants exported from `PhysicsPaintStyledTooltip.tsx`; the CSS tooltip blocks reworked) and plan 38-06 native UAT is explicitly approved. 38-05-SUMMARY.md records the fixed-vs-portal verdict and the shipped signature.</precondition>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx — the SHIPPED `computeTooltipPlacement` implementation, its exact signature and exported types/constants, and specifically HOW it reads viewport dimensions (global `window.innerWidth`/`innerHeight` vs injected) — the test fixtures must stub whatever the implementation actually reads. Also the shipped notch-offset formula and flip thresholds (6px notch gap + 8px margin).
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts (FULL file) — the existing `vi.mock('preact/hooks')` hoisted harness, the `installWindowStub`/`vi.stubGlobal` idiom, the `source()`/`css()` file readers, and the existing surface-contract describe that slices the CSS from the first `.physics-paint-styled-tooltip` occurrence.
    - app/src/components/physic-paint/physicsPaintStudio.css — the shipped `.physics-paint-styled-tooltip*` blocks (base, four direction modifiers, notch rules) so the TP-5 extraction covers ALL tooltip blocks, not just the first rule.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-SUMMARY.md — fixed-vs-portal verdict, shipped symbol names.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-PLAN.md Task 1 Step B — the locked placement rules under test.
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md — locked values: 8px viewport margin, 6px notch gap, notch 10x6, max-width 280px, max-height 96px.
  </read_first>
  <behavior>
    - TP-1 (direction per region, room available): with a 1000x700 viewport, a centered anchor (left 400, top 300, width 40, height 20) and a 120x24 pill: region 'bottom' → direction 'above'; 'top' → 'below'; 'left-edge' → 'right'; 'right-edge' → 'left'.
    - TP-2 (flip on insufficient room): region 'bottom' with the anchor near the top edge (no room above for pill height + 6px gap + 8px margin) → direction flips to 'below'; region 'top' with the anchor near the bottom edge → flips to 'above'; region 'right-edge' with the anchor near the left edge → flips to 'right'; region 'left-edge' with the anchor near the right edge → flips to 'left'.
    - TP-3 (8px viewport clamp): region 'bottom' with the anchor at the left viewport edge (left 2) → returned left is exactly 8; anchor at the right edge → returned left is exactly innerWidth − pillWidth − 8 (= 872 at 1000 wide with a 120 pill); a vertical case (anchor top 0, region 'top' resolving below with the pill pushed past the top bound, or a pill taller than the available band) → returned top is clamped into [8, innerHeight − pillHeight − 8].
    - TP-4 (notch tracks the control after clamping): unclamped centered case → notchOffset equals anchor center − pill left (= pillWidth / 2 when centered). Clamped case: anchor left 2 width 40 (center 22) with pill left clamped to 8 → notchOffset = 22 − 8 = 14, NOT 60. For a 'left'/'right' direction case, notchOffset is anchor middle − pill top on the cross axis, likewise post-clamp.
    - TP-5 (CSS contract): every `.physics-paint-styled-tooltip*` block (base + modifiers + notch) contains no `nowrap` and no `text-overflow: ellipsis`; the base block contains `max-width: 280px`, `max-height: 96px`, `white-space: normal`, `position: fixed`, `pointer-events: none`, and the `#20262d` fill; the notch rules and the four direction modifiers exist with the same `#20262d` fill; the exported constants equal the locked values (`TOOLTIP_VIEWPORT_MARGIN` 8, `TOOLTIP_PILL_MAX_WIDTH` 280) if the implementation exports them.
  </behavior>
  <action>
    Edit ONLY this test file. First read the shipped implementation and pin the fixtures to reality: the exact exported symbol names, the signature `(anchorRect, region, pillSize) → { direction, left, top, notchOffset }`, how viewport dimensions are read (stub `window.innerWidth`/`window.innerHeight` with `vi.stubGlobal` in the new describe's `beforeEach`, restoring in `afterEach` — mirroring the file's existing `vi.useFakeTimers`/`installWindowStub` setup discipline), and the exact flip thresholds so TP-2 fixtures sit unambiguously on the no-room side.

    Add one new describe `computeTooltipPlacement — viewport placement contract (38-08, D-11/D-13, post-UAT)` with one `it` per behavior bullet (TP-1..TP-4; TP-4 may be one `it` with the unclamped and clamped assertions, or two). Build anchor rect fixtures as plain objects carrying left/top/right/bottom/width/height cast to `DOMRect` (consistent with the project's test casting style) — never mount DOM. Import `computeTooltipPlacement` (and the exported constants/types if shipped) from './PhysicsPaintStyledTooltip'; the file's existing `vi.mock('preact/hooks')` harness already makes importing from this module safe.

    Extend the existing surface-contract describe (or add a sibling `it` in it) for TP-5: extract the FULL tooltip CSS region — from the first `.physics-paint-styled-tooltip` occurrence through the end of the notch rules (the existing single-rule slice only covers the base rule; extend the slice window so modifiers and notch rules are included) — then assert the positive locked declarations and the absence of the removed single-line/truncation declarations per the TP-5 bullet. Do not quote the removed declarations in a way that would match a naive grep of the test file for CSS content — assert via `expect(tooltipCss).not.toContain(...)` tokens, which is the assertion itself and is fine.

    Keep the existing `useStyledTooltip` describe and its hoisted hook mock BYTE-IDENTICAL (the 1000ms/focus/Escape contract is UAT-locked and already covered). Keep the existing surface-contract assertions that still hold (pointer-events, fill, role, no injection API).

    These tests are written against shipped, UAT-approved code and are expected GREEN immediately. If any assertion fails against the shipped implementation, re-check fixture construction first (thresholds, stub shape); if the failure is a genuine production deviation from the UAT-locked contract, stop and report — do not edit production source in this plan. Targeted run only; never watch mode.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts && grep -c "computeTooltipPlacement" app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app vitest run app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts` exits 0.
    - The new describe covers all four region→direction mappings (TP-1), at least one flip per axis (TP-2), left/right/top clamp cases at the exact 8px boundaries (TP-3), and unclamped + clamped notch-offset cases proving post-clamp anchor tracking (TP-4).
    - The CSS contract assertions (TP-5) span the base rule, the four direction modifiers, and the notch rules: positive locked declarations present, removed single-line/truncation declarations absent.
    - The existing `useStyledTooltip` describe and its hoisted mock are byte-identical (diff shows only the new describe, the extended CSS assertions, and import additions).
    - `git diff --name-only` shows exactly one file: this test file. No production source touched. No watch-mode invocation.
  </acceptance_criteria>
  <done>The placement utility's UAT-approved direction/flip/clamp/notch math is locked by deterministic pure-function tests, the tooltip CSS multiline-clamp and notch contract is asserted, the targeted vitest run is green, and no other file changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase-closing gate — full vitest run, typecheck, build, all green (38-UAT-THEN-REGRESSION)</name>
  <files>none — verification gate only (no file modifications)</files>
  <precondition>Tasks 1 and 2 of this plan are committed. If the sibling wave-4 plan 38-07 (group-seam tests) is executing in parallel, run this gate AFTER its commits have landed so the full suite includes the group-seam regression anchors; if 38-07 has not executed yet, this gate must still pass and MUST be re-run after 38-07 lands.</precondition>
  <read_first>
    - app/package.json — script names: `vitest run` (test), `typecheck` (tsc --noEmit), `build` (tsc --noEmit && vite build).
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-VALIDATION.md — Sampling Rate and the pre-verify-work full-suite-green requirement.
  </read_first>
  <action>
    Run the closing sequence in order, from the repo root, each command only after the previous exits 0:

    1. `pnpm --dir app vitest run` — the FULL suite (never watch mode, no filter, no coverage flag). Long gates may run in the background per the user's approved workflow; resume only after the success notification.
    2. `pnpm --dir app typecheck`.
    3. `pnpm --dir app build`.

    Do not fix anything by editing production source. If a failure originates in this plan's two test files (fixture or assertion construction), fix the test and re-run from step 1. If a failure originates in production code, in 38-07's test files, or anywhere else outside this plan's files_modified, STOP and report the failing command, the exact failure output, and the implicated files — the D-15 sequence treats a red closing gate as a phase-level blocker, not something to patch around. Do not retry in a sleep loop; diagnose the root cause of any flaky-looking failure before re-running.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run && pnpm --dir app typecheck && pnpm --dir app build</automated>
  </verify>
  <acceptance_criteria>
    - Full `pnpm --dir app vitest run` exits 0 with the updated presentation and tooltip describes included and green.
    - `pnpm --dir app typecheck` exits 0.
    - `pnpm --dir app build` exits 0 (typecheck + vite build).
    - No file was created, modified, renamed, or deleted by this task; no watch-mode or filtered invocation was used for the final full-suite run.
    - The plan SUMMARY records the three command results verbatim (exit codes and headline output) and whether 38-07's tests were included in the full-suite run.
  </acceptance_criteria>
  <done>The full suite, typecheck, and build are all green in sequence, closing the D-15 delivery sequence for Phase 38.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

- **Rewritten describe (test):** `getRotoStatusCapsuleViewModel — idle-context contract (38-08, D-08/D-09, post-UAT)` in `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts`, replacing the knowingly-red 36.15-era baseline assertions at :159-228 — empty-string fallback (SC-2), four-rung arbitration ladder with ambient lowest (SC-3), most-recent-wins/tie-break/blank-candidate carry-over (SC-4). The deleted baseline message text appears nowhere in the file.
- **New describe (test):** `getRotoStatusCapsuleIdleContext — current-cell idle mapping (38-08, D-09, UI-SPEC locked)` in the same file — the four SC-1 mapping cases (real/generated/empty/null) with the exact locked strings and `·` separator.
- **New describe (test):** `computeTooltipPlacement — viewport placement contract (38-08, D-11/D-13, post-UAT)` in `app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts` — direction per region (TP-1), flip on insufficient room (TP-2), exact 8px viewport clamp boundaries (TP-3), post-clamp notch-offset tracking (TP-4).
- **Extended CSS contract (test):** the tooltip surface-contract assertions in the same file now span the base rule, four direction modifiers, and notch rules — 280x96 multiline clamp present, removed truncation declarations absent (TP-5).
- **Phase gate record:** the plan SUMMARY's verbatim record of the full `vitest run` → `typecheck` → `build` results — the closing evidence for 38-UAT-THEN-REGRESSION and the input to `/gsd-verify-work` (VALIDATION.md: full suite must be green before verification).
- **Downstream consumers:** `/gsd-verify-work` for Phase 38 reads the SUMMARY gate record; the phase retrospective/closure uses these two test files as the regression anchors for the capsule and tooltip contracts.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production contract | Test assertions cross into production modules only as imports of pure functions/selectors; no DOM mounts, no server, no native app (user-owned surfaces stay user-owned). |
| capsule/tooltip ↔ user surface | The locked concise strings and CSS declarations asserted here are the same user-visible contract approved in 38-06 UAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-02 | Information disclosure | capsule/tooltip test assertions | low | accept | Tests assert only the UAT-locked concise fixed-shape strings and CSS values; no resolver internals or LOG detail enter the test fixtures (36.14 D-26 surface split unchanged). |
| T-38-04 | Tampering | tooltip surface-contract assertions | medium | mitigate | The extended CSS/DOM contract keeps asserting text-children-only rendering and the absence of HTML-injection APIs in the component source (T-36.15-01 carried forward); Task 2 keeps those existing assertions intact. |
| T-38-SC | Tampering | package installs | high | mitigate | Vacuous — no new packages this plan; tests run under the existing vitest setup with no config changes (Package Legitimacy Audit satisfied vacuously per 38-RESEARCH.md). |
</threat_model>

<verification>
- Task 1 targeted: `pnpm --dir app vitest run app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` exits 0; baseline-string grep on that file → 0; `getRotoStatusCapsuleIdleContext` referenced in the file.
- Task 2 targeted: `pnpm --dir app vitest run app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts` exits 0; `computeTooltipPlacement` describe present.
- Plan scope: `git diff --name-only` across the plan shows exactly the two test files in files_modified — zero production source changes.
- Closing gate: `pnpm --dir app vitest run && pnpm --dir app typecheck && pnpm --dir app build` all exit 0, in that order, full suite unfiltered, never watch mode.
- D-15 sequencing: this plan started only after the 38-06 SUMMARY recorded explicit native UAT approval; the gate re-runs after 38-07 if the sibling wave-4 plan lands later.
</verification>

<success_criteria>
- The capsule idle-context contract (per-cell-kind mapping, empty-string fallback, unchanged arbitration ladder) is locked by green deterministic tests, and the deleted baseline string exists nowhere in the presentation test file.
- The tooltip placement utility's direction/flip/clamp/notch math and the 280x96 multiline + notch CSS contract are locked by green deterministic tests.
- The full phase gate — complete vitest suite, typecheck, build — is green, closing the D-15 sequence: production → blocking native UAT → regression tests → full gate.
- No production source was edited anywhere in this plan; no watch-mode invocation; no test config changes.
</success_criteria>

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-08-SUMMARY.md` when done — it MUST record the three closing-gate command results verbatim (exit codes + headline output) and whether 38-07's group-seam tests were included in the full-suite run.
</output>
