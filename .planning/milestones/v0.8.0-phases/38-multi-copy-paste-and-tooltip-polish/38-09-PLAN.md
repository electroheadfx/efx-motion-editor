---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 09
type: execute
wave: 2
depends_on: []
gap_closure: true
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
autonomous: true
requirements:
  - 38-TOOLTIP-VIEWPORT-PLACEMENT

must_haves:
  truths:
    - "Row-collision flip (refines D-11 for row-mounted right-edge controls): when a right-edge mount's left-direction pill band would horizontally overlap a sibling control in the anchor's own row band, the tooltip renders 'above' instead of covering the sibling — the Clear Script Buffer guarded-reason pill no longer masks the Copy/Apply buttons (38.1-06 run-1 item E, both UAT screenshots)."
    - "Notch and clamp contracts preserved (D-12/D-13): after the flip the notch still tracks the anchor center (existing post-clamp notchOffset math is untouched), and the 8px viewport margin clamp applies exactly as before."
    - "Byte-identical behavior for every other mount: the 12 strip mounts (header/cells/action-row, UAT-locked by 38-05) never pass a collision input, and a 3-argument call to computeTooltipPlacement returns exactly the pre-change result for identical inputs (proven by hardcoded expected numbers derived from the pre-change algorithm)."
    - "The useStyledTooltip controller contract is byte-identical (D-14): exactly 1000ms hover delay, instant show on keyboard focus, Escape hides, idempotent cleanup — no line in the controller body changes."
    - "D-15 discipline: zero test files created, modified, renamed, or executed; no vitest invocation. Placement math is proven by an ephemeral node script in /tmp (outside the repo suite), and native visual UAT ships inside the 38-06 re-run, not here."
    - "Zero CSS change: the flip reuses the existing .physics-paint-styled-tooltip--above modifier and viewport-fixed notch rules shipped by 38-05."
  artifacts:
    - "app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx — computeTooltipPlacement gains an optional 4th parameter rowObstacles (readonly rect-like array); new collision branch; PhysicsPaintStyledTooltip gains an opt-in prop enabling same-row sibling-rect collection in the layout effect; controller untouched."
    - "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx — all 3 styled-tooltip mounts (Copy :103, Apply :131, Clear :159) opt into the row-collision flip while keeping region right-edge."
    - "/tmp/38-09-placement-check.mjs — ephemeral esbuild-bundle placement-math gate (NOT a repo artifact, NOT a test file; D-15-safe)."
  key_links:
    - "Obstacle collection -> pure function: the layout effect reads rects of the anchor wrapper's sibling element children (anchor.parentElement children minus the anchor), keeps those whose vertical band intersects the anchor's vertical band (same toolbar row), and passes them as the 4th argument — the pure function stays node-testable and reads no element state."
    - "Flip ordering: preferred left (region right-edge, D-11) -> existing room flip to right when the left side lacks room -> NEW collision flip to above when the left band intersects an obstacle -> existing above/below room fallback -> documented last resort: keep the side direction and let the viewport clamp handle it. Clamp + notchOffset math runs after, unchanged."
---

<objective>
Close the tooltip row-placement defect (38.1-06 run-1 item E, reported again on native UAT 2026-07-28): the scripts panel bottom-row buttons (Copy/Apply/Clear in PhysicsPaintScriptsPanel) mount with region="right-edge", so computeTooltipPlacement picks direction 'left' and vertically centers the pill on the anchor — landing the pill ON TOP of the sibling buttons in the same toolbar row (user screenshot: the Clear script button is covered by its own guarded-reason pill). Fix the placement contract: when the left-direction band horizontally overlaps other controls in the anchor's row band, flip to 'above' (preferred for bottom-row controls) while keeping the notch anchor-tracking, the 8px viewport clamp, and every other mount's behavior byte-identical (D-11..D-14).

Purpose: the 38-05 fixed-in-place viewport mechanism is correct and UAT-locked everywhere except this one row-mounted right-edge case; this plan refines the shared pure placement contract without touching the locked mechanism (no portal, no CSS change, no controller change).

Output: extended pure computeTooltipPlacement with an opt-in sibling-collision input, the 3 scripts-panel mounts converted, an ephemeral placement-math proof, typecheck green. Visual confirmation is deferred to the 38-06 re-run per D-15; no test changes here (38-08 owns the tooltip test rewrite).

Requirement-ID note: phase-38 requirement IDs were never registered in REQUIREMENTS.md (recorded planning-data gap); IDs above are the phase-38 descriptive style, consistent with prior plans.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-06-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
</context>

<tasks>

<task type="tracer">
  <name>Tracer: row-collision branch in computeTooltipPlacement + opt-in prop + Clear mount, proven end-to-end</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, /tmp/38-09-placement-check.mjs</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (whole file — the pure function :132-191, the layout effect :223-254, the props interface :193-204)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx:135-162 (the Clear Script Buffer mount — the screenshot case)
    - .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-05-SUMMARY.md (locked mechanism: fixed-in-place, no portal, viewport-fixed notch)
  </read_first>
  <action>
    Extend the pure placement utility and wire ONE mount through the new path.

    1. computeTooltipPlacement gains an optional 4th parameter rowObstacles: a readonly array of rect-like objects { left, right, top, bottom } (structural type, not DOMRect, so node callers can pass plain objects). Default undefined. The function stays pure and reads no element state (D-15 testability contract for 38-08 preserved).

    2. New collision branch, AFTER the existing preferred-direction and room-flip computation and BEFORE the left/top computation: only when the resolved direction is a side direction ('left' or 'right') AND rowObstacles was provided AND at least one obstacle AABB-intersects the candidate pill rect (candidate = the side placement rect at the anchor's vertical center, pre-clamp, using TOOLTIP_NOTCH_GAP). On collision, recompute with direction 'above' (preferred for bottom-row controls); then apply the existing vertical room check — if above lacks room (anchor top - gap - pill height < margin) use 'below'; if below also lacks room, keep the original side direction as the documented last resort and let the existing clamp handle it. Everything downstream (viewport clamp, notchOffset tracking the anchor center after clamping) is untouched, so the notch follows the flip automatically (D-13).

    3. PhysicsPaintStyledTooltip gains an optional boolean prop enabling row-overlap avoidance (name it exactly avoidRowOverlap). When true, the existing layout effect collects obstacles before calling the pure function: anchor = pill.parentElement (unchanged); siblings = element children of anchor.parentElement minus the anchor; keep each sibling's getBoundingClientRect() whose vertical band intersects the anchor rect's vertical band (same toolbar row); pass the kept rects as the 4th argument. When the prop is absent/false, call the pure function with exactly 3 arguments — the 12 strip mounts keep byte-identical behavior (38-05 UAT-locked).

    4. Convert ONE mount as the tracer: the Clear Script Buffer PhysicsPaintStyledTooltip (PhysicsPaintScriptsPanel.tsx:159) gains avoidRowOverlap. Its region stays right-edge; guarded aria-describedby wiring untouched (T-38-05); content stays Preact text children (T-38-04).

    5. Write /tmp/38-09-placement-check.mjs (Write tool; outside the repo — NOT a test artifact, D-15-safe). The script: stubs globalThis.window = { innerWidth: 1440, innerHeight: 900 }; resolves esbuild via createRequire from the app package.json, then require.resolve('vite/package.json'), then require.resolve('esbuild', { paths: [vite dir] }); bundles a tiny virtual entry that imports computeTooltipPlacement from the absolute component path (format esm, jsx automatic with jsxImportSource preact, loader .svg dataurl) to /tmp/38-09-placement-bundle.mjs; imports the bundle; asserts with process.exit(1) on failure:
       (a) BASELINE (byte-identical 3-arg path): anchor { left: 1200, top: 300, width: 60, height: 26, right: 1260, bottom: 326 }, region right-edge, pill { width: 280, height: 40 }, NO 4th arg -> direction 'left', left 914, top 293 (numbers derived by hand from the pre-change algorithm: left = 1200 - 6 - 280; top = 313 - 20).
       (b) COLLISION: same call plus 4th arg [ { left: 1120, right: 1194, top: 300, bottom: 326 } ] (the Apply sibling, same row band) -> direction 'above', top 254 (= 300 - 6 - 40), and notchOffset = 1230 - left (anchor center X projection, D-13).
       (c) NON-ROW OBSTACLE: same as (b) but obstacle top 600 / bottom 626 (different band; also verify the caller-side pre-filter contract is not the only guard — the pure function's AABB test against the candidate rect [914,1194]x[293,333] does not intersect it) -> direction 'left', same numbers as (a).
       (d) EQUIVALENCE: 4th arg [] (empty) returns the identical object to the 3-arg call.
    6. Run the script, then typecheck. Do not invoke vitest (D-15 — execution banned pre-UAT). Commit on main: feat(38-09): add row-collision flip to styled tooltip placement (tracer: Clear mount).
  </action>
  <verify>
    <automated>node /tmp/38-09-placement-check.mjs && pnpm --dir app typecheck && test "$(grep -c 'avoidRowOverlap' app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx)" = "1" && test "$(git diff -- app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -c 'STYLED_TOOLTIP_DELAY_MS\|escapeHandlerRef\|timerRef')" = "0" && test -z "$(git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*')"</automated>
  </verify>
  <acceptance_criteria>
    - Ephemeral script exits 0: baseline numbers (a/c/d) prove the 3-arg path is byte-identical; collision case (b) proves the 'above' flip with anchor-tracked notch.
    - avoidRowOverlap appears exactly once in PhysicsPaintScriptsPanel.tsx (the Clear mount only).
    - useStyledTooltip controller byte-identical: zero changed lines mentioning its timer/handler internals (gate above).
    - Typecheck clean; zero test files touched; no vitest invocation; app diff limited to the two files_modified entries.
  </acceptance_criteria>
  <done>The Clear mount renders 'above' when its left band would cover same-row siblings, placement math proven deterministically outside the repo suite; every other mount's computed placement is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: convert Copy/Apply mounts + final locked-mechanism sweeps</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx:79-134 (the Copy and Apply mounts)
    - The Task 1 diff of app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
  </read_first>
  <action>
    Complete the scripts-panel conversion and lock the boundaries.

    1. Add avoidRowOverlap to the remaining two PhysicsPaintStyledTooltip mounts: Copy Script (:103) and Apply Script (:131). Regions stay right-edge; no other prop, markup, or wiring change in the panel.

    2. No CSS change — the flip renders through the existing --above direction modifier and viewport-fixed notch custom properties shipped by 38-05 (verify by reading the class assignment in the layout effect; do not add rules to physicsPaintStudio.css).

    3. Final sweeps, then commit on main: feat(38-09): convert scripts panel Copy/Apply tooltips to row-collision flip.
  </action>
  <verify>
    <automated>node /tmp/38-09-placement-check.mjs && pnpm --dir app typecheck && test "$(grep -c 'avoidRowOverlap' app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx)" = "3" && test "$(grep -c 'region="right-edge"' app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx)" = "3" && test "$(grep -c ' placement="' app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" = "0" && test -z "$(git diff --name-only HEAD -- app/src/components/physic-paint/physicsPaintStudio.css app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" && test -z "$(git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*')"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 scripts-panel mounts opt into the row-collision flip (count = 3) and keep region right-edge (count = 3).
    - The legacy per-mount placement JSX attribute remains absent from both consumer files (count = 0).
    - Zero diff in physicsPaintStudio.css and PhysicsPaintWorkflowStrip.tsx across the whole plan; zero test files touched; typecheck clean; ephemeral placement script still green.
  </acceptance_criteria>
  <done>All three scripts-panel row tooltips flip above on sibling collision; the 38-05 locked mechanism (fixed-in-place, no portal, notch, clamp, controller) is provably untouched; the plan is automated-ready with visual UAT owned by the 38-06 re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — placement math and DOM-rect reads only; no network, auth, or file-system surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-09-01 | Tampering | Tooltip content rendering | low | mitigate | Unchanged contract verified by gate: tooltip content remains Preact text children only; no HTML injection API is introduced (T-38-04 / T-36.15-01 carried forward). |
| T-38-09-02 | Information Disclosure | aria-describedby association | low | mitigate | Guarded reason-to-tooltip association untouched; Task 2 sweeps assert the mounts' wiring is unchanged (T-38-05). |
</threat_model>

<verification>
- Ephemeral placement-math gate (node /tmp/38-09-placement-check.mjs): baseline byte-identity + collision flip + notch tracking, all green after both tasks.
- pnpm --dir app typecheck clean after each task.
- Static gates: avoidRowOverlap count 1 then 3; region right-edge count 3; legacy placement attribute absent; zero diff in CSS/strip/test files; controller body untouched.
- D-15: no vitest invocation anywhere in this plan. Native visual UAT (the re-run of the 38-06 33-step script, which includes the two UI-SPEC tooltip backstops) is the human acceptance gate for this fix — flag the Clear-button masking scenario explicitly in the 38-06 SUMMARY handoff.
</verification>

<success_criteria>
The Clear/Copy/Apply guarded-reason pills render above the toolbar row instead of masking sibling buttons; all other 12 mounts compute identical placements; typecheck green; zero test artifacts; zero CSS diff; zero controller diff.
</success_criteria>

## Artifacts this phase produces

- Modified: app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (optional rowObstacles param + avoidRowOverlap prop + obstacle collection in the layout effect)
- Modified: app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (3 mounts opt in)
- Ephemeral (not committed): /tmp/38-09-placement-check.mjs + /tmp/38-09-placement-bundle.mjs
- .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-09-SUMMARY.md

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-09-SUMMARY.md` when done. Record: the flip-ordering contract, the byte-identity evidence (script assertions), the explicit deferral of visual UAT to the 38-06 re-run, and the fact that no test artifacts were touched (D-15).
</output>
