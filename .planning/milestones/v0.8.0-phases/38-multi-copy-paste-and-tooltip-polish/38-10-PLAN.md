---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 10
type: execute
wave: 2
depends_on: []
gap_closure: true
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 38-STRIP-HORIZONTAL-SCROLL

must_haves:
  truths:
    - "Plain mouse wheel over the timeline scroller scrolls the timeline horizontally: vertical wheel delta drives scrollLeft; Shift+wheel keeps the browser default; a horizontally dominant wheel delta (trackpad two-finger pan) keeps native behavior untouched."
    - "The bottom custom scrollbar is two-way synchronous: wheel-driven scrolling moves the thumb through the existing native scroll -> updateScrollbar path, and dragging the thumb still drives scrollLeft (existing pointer handler untouched — its geometry math provably unchanged)."
    - "The bottom action toolbar row (Key/Duplicate/Insert/Copy/Paste/Delete/All/Key-spacing/Apply) is reachable on narrow windows: it remains inside the shared scroll container (the cheapest layout — zero restructure), so the Task 1 wheel and the thumb both reach the left controls; the markup-order contract is gate-locked."
    - "No scroll hijacking outside the strip: exactly one wheel listener, attached to the timeline scroller element only, with idempotent cleanup (same listener discipline as the styled-tooltip Escape listener); the rest of the Studio and the page are unaffected."
    - "The strip edge-scroll drag session and the per-cell derivation cache (38.1-04, WR-03 eviction) are untouched — the wheel handler writes DOM scrollLeft only, never state, so no rAF throttling is introduced and no render-path timing changes (38.1 D-04 spirit: this plan adds zero per-render work)."
    - "D-15 discipline: the 3 known-red tests stay untouched and no test file is created, modified, renamed, or executed; no vitest invocation. This plan IS an explicitly user-requested behavior change (38.1-06 deferred follow-up #2) — the only sanctioned behavior delta in the phase-38 gap set; native confirmation ships inside the 38-06 re-run."
  artifacts:
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — one non-passive wheel listener on the timeline scroller (layout effect + cleanup), shift/dominance/deltaMode guards, scrollLeft write."
    - "app/src/components/physic-paint/physicsPaintStudio.css — modified ONLY if the Task 2 audit finds a rule that clips or pins the action row outside the shared scroll extent; otherwise zero CSS diff (recorded in the SUMMARY)."
  key_links:
    - "Wheel -> scrollLeft -> native scroll event -> updateScrollbar -> thumb: the two-way sync rides the EXISTING onScroll wiring; the wheel handler must not call the scrollbar state setter directly (single sync path, no double derivation)."
    - "Action row inside .physics-paint-timeline-scroll: the scroller (overflow-x auto, min-width content 2160px) already carries the ruler, lane, AND action row; making the wheel functional makes the toolbar's left controls reachable without any layout restructure."
---

<objective>
Ship the user-requested Studio timeline scroll behavior (38.1-06 deferred follow-up #2, confirmed on native UAT 2026-07-28): plain mouse wheel currently does nothing over the timeline strip (Shift+wheel required), and on narrow windows the bottom action toolbar row clips with its left buttons unreachable. Deliver: (1) plain-wheel horizontal scrolling of the timeline, (2) verified two-way sync with the bottom custom scrollbar, (3) a reachable bottom action toolbar via the shared horizontal scroll.

Purpose: the strip already has the full scroll infrastructure (overflow-x scroller with hidden native scrollbar, custom thumb with drag + onScroll sync); the only missing link is a wheel-to-horizontal mapping. This plan adds that link with surgical scope — no layout restructure, no state-path changes, no render-path timing changes.

Output: wheel listener + guards in PhysicsPaintWorkflowStrip.tsx, conditional minimal CSS correction, typecheck green. Visual/interactive confirmation is deferred to the 38-06 re-run (D-15); no test changes here.

Requirement-ID note: phase-38 requirement IDs were never registered in REQUIREMENTS.md (recorded planning-data gap); the ID above follows the phase-38 descriptive style.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.1-studio-render-path-performance/38.1-06-SUMMARY.md
@.planning/phases/38.1-studio-render-path-performance/38.1-CONTEXT.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
</context>

<tasks>

<task type="tracer">
  <name>Tracer: plain-wheel horizontal scrolling on the timeline scroller (one listener, guarded)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:360-380 (scrollbar state), :650-680 (updateScrollbar), :1153-1160 (the scroller JSX with timelineScrollRef + onScroll), :770-800 (edge-scroll drag session — do not disturb), :1030-1060 (thumb drag handler — do not disturb)
    - app/src/components/physic-paint/physicsPaintStudio.css:2041-2055 (.physics-paint-timeline-scroll: overflow-x auto, scrollbar-width none)
  </read_first>
  <action>
    Add plain-wheel horizontal scrolling to the timeline scroller.

    1. In PhysicsPaintWorkflowStrip.tsx, register exactly ONE wheel listener on the scroller element (timelineScrollRef.current) inside a layout effect with idempotent cleanup and a mountedRef guard — the same listener discipline as the styled-tooltip Escape listener (38-05 pattern: register once, remove in cleanup, no duplicate registration across renders). Use addEventListener with options { passive: false } so preventDefault is guaranteed to take effect in every browser (do not rely on framework listener passivity defaults).

    2. Handler contract, in order:
       - event.shiftKey -> return immediately (browser default shift-wheel horizontal mapping preserved; the user's existing habit keeps working byte-identically).
       - Math.abs(event.deltaX) > Math.abs(event.deltaY) -> return immediately (trackpad two-finger horizontal pans keep fully native behavior; no preventDefault, no writes).
       - Otherwise: event.preventDefault(), then element.scrollLeft += normalized delta, where normalized delta = event.deltaY * (event.deltaMode === 1 ? 16 : 1) (line-mode wheels, e.g. Firefox, get a 16px line height; pixel mode passes through).

    3. The handler writes DOM scrollLeft ONLY — it must not call the scrollbar state setter or any other state setter (the existing native scroll event -> onScroll -> updateScrollbar path moves the thumb; one sync path, no double derivation, no rAF throttle needed since no state is written).

    4. Boundary behavior (documented choice): wheel events at the scroll extremes are still consumed while the pointer is over the scroller — page scroll-while-hovering-the-strip would be surprising. Do not add overscroll-behavior CSS.

    5. Do not touch: the edge-scroll drag session, the thumb pointer handler, the cell-derivation cache, the strip's fixed geometry (161px bands / 34px action row / 18px cells).

    6. Typecheck, then commit on main: feat(38-10): plain-wheel horizontal scroll on the Studio timeline strip. Do not invoke vitest (D-15).
  </action>
  <verify>
    <automated>pnpm --dir app typecheck && test "$(grep -c "addEventListener('wheel'" app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" = "1" && test "$(grep -c 'passive: false' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" = "1" && test "$(grep -c 'shiftKey' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" -ge "1" && test "$(grep -c 'deltaMode' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" = "1" && test "$(grep -c 'onScroll={updateScrollbar}' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)" = "1" && test "$(awk '/addEventListener\(.wheel./,/^  \}, \[\]\);/' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -c 'setScrollbar')" = "0" && test -z "$(git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one wheel listener registration with passive: false; shiftKey and deltaMode guards present; the scroller's onScroll wiring unchanged.
    - The wheel-handler region (awk-extracted from the listener registration to the effect close) contains zero scrollbar-state-setter calls.
    - Typecheck clean; zero test files touched; no vitest invocation; app diff limited to PhysicsPaintWorkflowStrip.tsx at this point.
  </acceptance_criteria>
  <done>Plain wheel scrolls the timeline horizontally with native-feeling guards (shift passthrough, trackpad dominance, line-mode normalization); the thumb follows through the existing scroll-event path; no state writes in the handler.</done>
</task>

<task type="auto">
  <name>Task 2: two-way scrollbar sync + bottom action toolbar reachability audit/lock</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx:1153-1160 (scroller open), :1225-1230 (action row open), :1465-1478 (scroller close + custom scrollbar markup)
    - app/src/components/physic-paint/physicsPaintStudio.css:2032-2039 (.physics-paint-timeline), :2041-2055 (scroller), :2134-2150 (.physics-paint-roto-action-row, min-width 2160px)
  </read_first>
  <action>
    Verify and lock the two-way sync and toolbar reachability — minimal, audit-driven changes only.

    1. Markup-order lock: confirm (and keep) the DOM order — the ruler, lane, AND .physics-paint-roto-action-row are all children of .physics-paint-timeline-scroll, with .physics-paint-timeline-scrollbar as its following sibling. This shared-scroll layout is the sanctioned cheap answer for toolbar reachability: once Task 1's wheel works, the toolbar's left controls (identity chip, Key, Duplicate) are reachable by scrolling back to scrollLeft 0 on narrow windows; no pinning/sticky restructure.

    2. Two-way sync audit (read-only verification, record findings in the SUMMARY): wheel -> scrollLeft -> native scroll event -> onScroll -> updateScrollbar -> thumb state (Task 1 path); thumb pointer-down -> scrollFromPointer -> scrollLeft (existing). Both directions ride one state path.

    3. CSS audit: read .physics-paint-timeline, the scroller block, and .physics-paint-roto-action-row. If any rule clips the action row out of the scroller's scroll extent or pins it (e.g. an unexpected position/sticky/overflow interaction), make the minimal correction in physicsPaintStudio.css. If the audit is clean (expected — the row already scrolls with content via min-width 2160px inside the scroller), make ZERO CSS changes and record that in the SUMMARY.

    4. Preserve the thumb geometry math and the drag handler byte-identically; preserve the 4px padding-left offset comment block (36.15-12, UAT Gap H-5) and all fixed geometry.

    5. Typecheck, final whole-plan diff sweep, then commit on main: feat(38-10): lock shared-scroll toolbar reachability + scrollbar two-way sync audit. Do not invoke vitest (D-15).
  </action>
  <verify>
    <automated>pnpm --dir app typecheck && test "$(awk '/physics-paint-timeline-scroll/,/physics-paint-timeline-scrollbar-thumb/' app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -c 'physics-paint-roto-action-row')" = "1" && test "$(git diff HEAD -- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -c 'thumbWidth\|thumbRange\|scrollFromPointer')" = "0" && test "$(git diff --name-only HEAD -- app/ | grep -vcE 'PhysicsPaintWorkflowStrip.tsx|physicsPaintStudio.css')" = "0" && test -z "$(git diff --name-only -- 'app/**/*.test.*' 'app/**/*.spec.*')"</automated>
  </verify>
  <acceptance_criteria>
    - Markup gate: the awk-extracted scroller region contains the action-row class exactly once (shared-scroll layout locked).
    - Whole-plan app diff touches only the two files_modified entries; the thumb geometry/drag math has zero changed lines (gate above).
    - CSS diff is either zero (clean audit) or a minimal documented correction; fixed strip geometry and the 36.15 Gap H-5 offset preserved.
    - Typecheck clean; zero test files touched; no vitest invocation.
  </acceptance_criteria>
  <done>Wheel, thumb drag, and toolbar reachability all ride the existing shared-scroll infrastructure; the plan is automated-ready with interactive confirmation (wheel scrolls, thumb follows, narrow-window toolbar reachable) owned by the 38-06 re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — DOM scroll events only; no input data crosses a trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-10-01 | Denial of Service | Wheel handler on the scroller | low | mitigate | Handler is O(1) per event with no state writes and no allocation; the render path (38.1 D-04) is untouched — verified by the no-state-setter region gate. |
| T-38-10-02 | Tampering | Global shortcut/scroll surface | low | accept | Listener is element-scoped (no window/document capture), so global shortcuts and page scroll outside the strip cannot be intercepted by construction (memory: guard shortcuts in paint mode — unaffected). |
</threat_model>

<verification>
- pnpm --dir app typecheck clean after each task.
- Static gates per task: single non-passive wheel listener with shift/dominance/line-mode guards; no state writes in the handler; onScroll wiring intact; markup-order lock; whole-plan diff limited to the two files; zero test artifacts; no vitest invocation (D-15).
- Interactive confirmation (wheel scrolls horizontally, thumb follows both ways, narrow-window toolbar reachable) is deferred to the 38-06 re-run — flag these three checkpoints explicitly in the 38-06 SUMMARY handoff.
</verification>

<success_criteria>
Plain wheel scrolls the Studio timeline horizontally (shift and trackpad-horizontal preserved), the bottom custom scrollbar stays two-way synchronous, and the bottom action toolbar's left controls are reachable on narrow windows — with zero layout restructure, zero render-path timing change, and zero test artifacts.
</success_criteria>

## Artifacts this phase produces

- Modified: app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (wheel listener + guards)
- Modified (conditional): app/src/components/physic-paint/physicsPaintStudio.css (only if the Task 2 audit finds a clipping/pinning rule; otherwise zero diff, recorded in the SUMMARY)
- .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-10-SUMMARY.md

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-10-SUMMARY.md` when done. Record: the handler contract (guards, line-mode normalization, boundary-consumption choice), the two-way sync audit findings, the CSS audit verdict (zero-change or the minimal correction), and the D-15 deferral of interactive confirmation to the 38-06 re-run.
</output>
