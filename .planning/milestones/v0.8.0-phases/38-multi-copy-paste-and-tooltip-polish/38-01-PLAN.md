---
phase: 38-multi-copy-paste-and-tooltip-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoSession.ts
  - app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - .planning/phases/38-multi-copy-paste-and-tooltip-polish/COVERAGE.md
autonomous: true
requirements:
  - 38-GROUP-COPY
assumption_delta_decision: no-change # D-02: ONE clipboard slot stays the identity anchor; single -> single|group is shape-widening, not pluralization
mvp_user_story: unavailable # ROADMAP Phase 38 Goal is outcome-shaped but NOT in "As a / I want to / so that" format; surfaced here per planner-mvp-mode instead of inventing a story
reversibility_flags:
  D-01: costly # flag only — no decision checkpoint; reverting means re-locking Phase 37 UAT semantics
must_haves:
  truths:
    - "Exactly 1 selected key: Copy is today's single-key copy, byte-identical — guard, feedback line `Copied key {appFrame}.`, clipboard shape, and the session.copyKey() implementation are untouched (D-01; UI-SPEC zero-one-many row)."
    - "2+ selected keys: Copy captures the whole group as frozen entries into the ONE shared clipboard slot; feedback is the UI-SPEC locked line `Copied {N} keys` (D-01/D-02; UI-SPEC zero-one-many + Group Copy feedback rows)."
    - "Group Copy overwrites a single-key clipboard and vice versa — one slot, immutable and reusable until the next Copy or resetSession disposal (D-02; UI-SPEC empty-clipboard row, slot half)."
    - "Each group entry carries an immutable payload snapshot + source physical appFrame + stable source keyId provenance, snapshotted from physicPaintStore.getRotoRealKeyRecords(layerId) — never the frame-indexed session cache (D-03; RESEARCH Pitfall 6)."
    - "Copy (single or group) is not a mutation: no transaction, no history entry, no dirty state, no bridge round-trip (UI-SPEC Group Copy feedback row)."
    - "Paste availability stays shape-agnostic: hasCopiedRotoKey derives from slot-non-null exactly as today, so a group clipboard enables the guarded Paste control without any availability change (D-02; RESEARCH Open Question 2 resolution)."
    - "The single-key paste route compiles and behaves identically after the slot-type widening; the group-shape branch is a fail-closed guard whose body plan 38-04 replaces with the real group paste route (interface-first ordering across plans, not a deferred feature)."
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoSession.ts — exports RotoSessionCopiedGroupEntry, RotoSessionCopiedKeyGroup, RotoSessionCopiedKeyValue (union), isRotoSessionCopiedKeyGroup guard; widened copiedKey signal + RotoSessionInput.copiedKey; new copyKeyGroup(entries) session method; RotoSessionActionName gains 'copyKeyGroup'"
    - "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts — RotoKeyUtilitiesInput gains getSelectedKeyIds + getRotoKeyRecords ports; copiedKeyRef widened to RotoSessionCopiedKeyValue | null; copyKey selection-size branch (1 -> existing session.copyKey(), 2+ -> group freeze via session.copyKeyGroup)"
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — keyUtilities input block (line ~533) wires the two new ports from selectedKeyIds signal + physicPaintStore.getRotoRealKeyRecords(launchContext.layerId)"
    - ".planning/phases/38-multi-copy-paste-and-tooltip-polish/COVERAGE.md — no-external-API declaration"
  key_links:
    - "Studio getRotoKeyRecords port reads physicPaintStore.getRotoRealKeyRecords(launchContext.layerId) FRESH at Copy activation time (store getter at app/src/stores/physicPaintStore.ts:1262) — not the memoized rotoKeyRecords, not the session cache (Pitfall 6)"
    - "copiedKeyRef <-> session.copiedKey mirror through runSessionResult (useRotoKeyUtilities.ts:127) keeps exactly ONE slot across single/group overwrite and resetSession({clearClipboard}) disposal (D-02)"
    - "hasCopiedRotoKey: copiedKey.value !== null (physicsPaintRotoSession.ts:95) feeds deriveRotoKeyUtilityActionState unchanged — shape-agnostic availability"
    - "Structural-type seam to parallel plan 38-02: entry shape {payload, sourceAppFrame, sourceKeyId} is structurally typed at the resolver factory boundary; this plan imports NOTHING from app/src/types/physicPaint.ts for the clipboard shape"
---

<objective>
Widen the one shared Roto clipboard slot from a single-key entry to a frozen `single | group` discriminated union (D-02/D-03) and add group Copy: with 2+ real Roto keys selected, Copy snapshots immutable `{payload, sourceAppFrame, sourceKeyId}` entries — read fresh from `physicPaintStore.getRotoRealKeyRecords(layerId)`, never the frame-indexed session cache (Pitfall 6) — into the slot with the UI-SPEC locked feedback `Copied {N} keys`. With exactly 1 key selected, Copy stays byte-identical to today (D-01, Pitfall 7). This plan covers requirement 38-GROUP-COPY only; group paste activation (38-04) and the paste-key-group resolver seam (38-02) are separate plans in the same phase.

Purpose: This is the clipboard half of the group Copy/Paste feature (Phase 38 scope item 1). The slot shape established here is the contract the paste side (38-02/38-04) consumes.
Output: Widened session clipboard types + `copyKeyGroup` session method, controller hook branch with two new input ports, Studio port wiring, and the phase COVERAGE.md no-external-API declaration.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-CONTEXT.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PATTERNS.md
@.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-UI-SPEC.md
@app/src/components/physic-paint/roto/physicsPaintRotoSession.ts
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end group Copy — 2+ selected keys freeze into the one shared slot with `Copied {N} keys` feedback; single-key Copy untouched</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoSession.ts, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <reversibility rating="costly">Implements D-01 (2+ selection changes Copy semantics, superseding 37 D-16 for the Copy case only) — becomes UAT-locked behavior plus a clipboard contract consumed by 38-02/38-04; reverting means re-locking Phase 37 UAT semantics. Flag only; not one-way, no decision checkpoint.</reversibility>
  <read_first>app/src/components/physic-paint/roto/physicsPaintRotoSession.ts (full file — slot type at lines 13-16, signal at 59/79, availability derivation at 91-100, copyKey at 143-152, normalizeCopiedKey at 216-220), app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts (full file — copiedKeyRef at 47, resetSession at 73-78, mirror at 127, copyKey at 170-178, pasteKey at 180-206, toClipboardPayload at 246-255), app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts lines 56-97 (PhysicPaintRotoKeyIdentity {keyId, appFrame}, PhysicPaintRotoRealKeyPayload {frameIndex, appFrame, dataUrl, width?, height?}, PhysicPaintRotoRealKeyRecord {kind: 'real-key', keyId, appFrame, payload}), app/src/components/physic-paint/PhysicsPaintStudio.tsx lines 55-110 (selectedKeyIds signal at 63, rotoKeyRecords memo at 93) and 530-575 (keyUtilities input block at 533, copyRotoFrame at 571)</read_first>
  <action>
Implement the group Copy slice across three files, per D-01/D-02/D-03. Do not modify any test file and do not run vitest (D-15 — production only in this wave).

1. physicsPaintRotoSession.ts — widen the slot to a discriminated union (D-02/D-03):
   - Add `import type { PhysicPaintRotoRealKeyPayload } from './physicsPaintRotoPhysicalModel';` (same roto/ directory; the session module must NOT import from app/src/types/physicPaint.ts for this shape — the structural-type seam keeps this plan parallel with 38-02).
   - New exported interface `RotoSessionCopiedGroupEntry` with readonly fields `payload: PhysicPaintRotoRealKeyPayload`, `sourceAppFrame: number`, `sourceKeyId: string` — payload snapshot + source physical appFrame + stable source keyId provenance (D-03). No offset field anywhere: relative offsets derive from sourceAppFrames at paste time (RESEARCH Anti-Pattern: offset table in the clipboard is forbidden).
   - New exported interface `RotoSessionCopiedKeyGroup` with readonly fields `kind: 'group'` and `entries: readonly RotoSessionCopiedGroupEntry[]`.
   - New exported type `RotoSessionCopiedKeyValue = RotoSessionCopiedKey | RotoSessionCopiedKeyGroup`. Keep the existing `RotoSessionCopiedKey` interface (lines 13-16) EXACTLY as-is — the single shape carries no discriminant; discrimination is the group-only `kind: 'group'` member, so the 1-key path stays byte-identical (D-01, Pitfall 7).
   - New exported type guard `isRotoSessionCopiedKeyGroup(value: RotoSessionCopiedKeyValue | null): value is RotoSessionCopiedKeyGroup` (checks value !== null and 'kind' in value and value.kind === 'group') — reused by the hook narrowing below and by plan 38-04.
   - Widen `RotoSessionInput.copiedKey` (line 41) and the `copiedKey` signal declaration (lines 59/79) and the `RotoSession.copiedKey` interface member (line 59) to `RotoSessionCopiedKeyValue | null`.
   - Extend `normalizeCopiedKey` (lines 216-220): add a group branch before the existing single-key logic — if `isRotoSessionCopiedKeyGroup(copiedKey)`, return the group as-is when `copiedKey.entries.length >= 2` and every entry has a nonnegative integer sourceAppFrame, otherwise return null. Entries arrive already frozen from the copy path; store payloads are canonical and immutable, so no re-normalization and no cloning (dataUrl strings are immutable; a reference to the accepted record's payload IS the point-in-time snapshot because store mutations replace records immutably).
   - Add `'copyKeyGroup'` to the `RotoSessionActionName` union (line 10).
   - Add session method `copyKeyGroup(entries: readonly RotoSessionCopiedGroupEntry[]): RotoSessionActionResult` next to `copyKey` (lines 143-152, which must remain byte-identical): fail-closed via the existing `failed(...)` helper with message `Select at least two real Roto keys to copy.` when entries.length < 2; otherwise set `copiedKey.value` to `Object.freeze({ kind: 'group', entries: Object.freeze([...entries]) })`, set feedback and return message `Copied ${entries.length} keys` (the UI-SPEC locked string `Copied {N} keys` — no trailing period, verbatim), effects: []. Mirror the copyKey result shape exactly (`{ action: 'copyKeyGroup', ok: true, message, effects: [] }`).
   - The availability derivation `hasCopiedRotoKey: copiedKey.value !== null` (line 95) stays UNCHANGED — shape-agnostic per D-02 (RESEARCH Open Question 2 resolution).

2. useRotoKeyUtilities.ts — controller branch + two new ports:
   - `RotoKeyUtilitiesInput` gains two required readonly ports: `getSelectedKeyIds: () => readonly string[]` and `getRotoKeyRecords: () => readonly PhysicPaintRotoRealKeyRecord[]` (import `PhysicPaintRotoRealKeyRecord` as a type from '../roto/physicsPaintRotoPhysicalModel' alongside the existing PhysicPaintRotoRealKeyPayload import at line 4; also import `RotoSessionCopiedKeyValue`, `RotoSessionCopiedGroupEntry`, and `isRotoSessionCopiedKeyGroup` from the session module at line 3).
   - Widen `copiedKeyRef` (line 47) to `useRef<RotoSessionCopiedKeyValue | null>(null)`. The mirror at line 127 and the resetSession disposal at lines 73-78 compile unchanged — ONE slot, one disposal path (D-02).
   - Rework `copyKey` (lines 170-178): keep the `blocked` guard first; then read `const selectedKeyIds = input.getSelectedKeyIds()`. When `selectedKeyIds.length >= 2`, take the GROUP branch: read `input.getRotoKeyRecords()` (fresh store read supplied by Studio — Pitfall 6: never the session's frame-indexed realKeyFrames cache); for each selected keyId find the record by `record.keyId === keyId`; if ANY selected keyId has no record, set `input.setApplyMessage('The selected Roto keys are no longer available.')` (mirrors the duplicateKey stale-selection message at line 156, pluralized) and return WITHOUT touching the clipboard (fail-closed, D-02 slot preserved). Otherwise build entries as `Object.freeze({ payload: record.payload, sourceAppFrame: record.appFrame, sourceKeyId: record.keyId })`, sort ascending by sourceAppFrame, and call `void runSessionResult(session.copyKeyGroup(entries))`. Do NOT consult `actionAvailability.currentIsRealKey` in the group branch — a 2+ selection only contains real keys per the Phase 37 selection model, and the store-record lookup is the authoritative check.
   - When `selectedKeyIds.length < 2`, fall through to the EXISTING single-key path byte-identical: the `currentIsRealKey` guard with its disabledReason message and `void runSessionResult(session.copyKey())` (Pitfall 7 — do not reword, reorder, or re-indent the existing lines).
   - In `pasteKey` (lines 180-206), narrow the union so the file compiles: after the existing null check, add `if (isRotoSessionCopiedKeyGroup(copiedKey)) { input.setApplyMessage('The copied Roto key group cannot paste through the single-key route.'); return; }` BEFORE the `toClipboardPayload(copiedKey)` call. This is the fail-closed seam guard for the interface-first plan split: plan 38-04 replaces this branch body with the real group paste route (factory -> runPhysicalAction); the single-key path below it (`toClipboardPayload` + `input.physicalKeyUtilities.pasteKey(...)`) stays byte-identical (Pitfall 7 — single paste is replace-style and reused by addEmptyKey; group all-empty-or-reject policy must not leak in here).
   - Update the `copyKey` useCallback dependency array to include the new input port usages (input is already a single dep object — verify no eslint exhaustive-deps regression).

3. PhysicsPaintStudio.tsx — wire the two ports inside the existing `keyUtilities: { ... }` input block (line 533):
   - Add `getSelectedKeyIds: () => selectedKeyIds.value,` (the signal declared at line 63; the identical getter already exists at line 473 for another port — mirror it).
   - Add `getRotoKeyRecords: () => launchContext ? physicPaintStore.getRotoRealKeyRecords(launchContext.layerId) : [],` — a FRESH store read at activation time (store getter at app/src/stores/physicPaintStore.ts:1262 returns `PhysicPaintRotoRealKeyRecord[]`). Do NOT pass the memoized `rotoKeyRecords` (line 93): the port contract is "authoritative store records at Copy time".
   - No other Studio changes: the view still forwards ONE Copy intent (`onCopyRotoFrame: copyRotoFrame`, line 1087); the view never branches on selection size (UI-SPEC Preact and State Ownership Contract); `hasCopiedRotoKey: rotoSession.copiedKey.value !== null` (line 1087) compiles unchanged against the widened union.

Preact discipline (CLAUDE.md): no new useEffect; the clipboard remains session-signal + ref state exactly as today. No new packages. No `as` casts on the new types except where the existing file already uses the pattern (toClipboardPayload's existing cast is untouched).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app typecheck && grep -c "copyKeyGroup" app/src/components/physic-paint/roto/physicsPaintRotoSession.ts && grep -F -c "Copied \${entries.length} keys" app/src/components/physic-paint/roto/physicsPaintRotoSession.ts && grep -c "isRotoSessionCopiedKeyGroup" app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts && grep -c "getSelectedKeyIds: () => selectedKeyIds.value" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -c "getRotoKeyRecords:" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -F -c "Copied key \${appFrame}." app/src/components/physic-paint/roto/physicsPaintRotoSession.ts && grep -F -c "copiedKey.value = { frame: appFrame, cachedFrame: normalized };" app/src/components/physic-paint/roto/physicsPaintRotoSession.ts && grep -F -c "const clipboardPayload = toClipboardPayload(copiedKey);" app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts</automated>
    Expected: typecheck exits 0; copyKeyGroup count >= 3 (action-name union member, interface method, implementation); group feedback line count == 1; isRotoSessionCopiedKeyGroup count in hook >= 2 (import + narrowing use); each Studio port grep == 1; the three single-key byte-identity greps each == 1 (proves D-01/Pitfall 7 preservation). NO vitest invocation (D-15).
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app typecheck` passes with the widened union across all three files.
    - Group branch: 2+ selected keyIds resolve against fresh store records; any missing record aborts with the stale-selection message and leaves the clipboard untouched; otherwise entries are frozen, sorted by sourceAppFrame, and land in the one slot via session.copyKeyGroup with feedback `Copied {N} keys`.
    - Single-key Copy: the existing guard, `session.copyKey()` call, `Copied key {appFrame}.` feedback, and slot shape are byte-identical (grep gates prove the tokens survive exactly once).
    - Single-key Paste: `toClipboardPayload` path unchanged; group clipboards hit only the new narrowing guard.
    - resetSession({clearClipboard}) and the runSessionResult mirror require zero changes (one slot, one disposal path — D-02).
    - No test file created, modified, or executed; no vitest run (D-15).
  </acceptance_criteria>
  <done>With 2+ real Roto keys selected, Copy produces frozen `{payload, sourceAppFrame, sourceKeyId}` entries (sorted by sourceAppFrame, store-sourced) in the single shared slot and the `Copied {N} keys` feedback line; with exactly 1 key selected, Copy is byte-identical to today; typecheck is green; the group-shape paste branch is a fail-closed guard awaiting 38-04's real route.</done>
</task>

<task type="auto">
  <name>Write the phase COVERAGE.md no-external-API declaration and run the plan's final gates</name>
  <files>.planning/phases/38-multi-copy-paste-and-tooltip-polish/COVERAGE.md</files>
  <read_first>.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-PLAN-OUTLINE.md (Recorded Planning Decisions — API coverage checkpoint entry), .planning/phases/38-multi-copy-paste-and-tooltip-polish/38-RESEARCH.md (Environment Availability + Standard Stack: no new packages, internal Preact UI + timeline state work only)</read_first>
  <action>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/COVERAGE.md` with the reasoned API-coverage declaration required by the outline's API coverage checkpoint. Content: a heading `Phase 38 — API Coverage Declaration`; the verdict line `No external API integration: internal Preact UI + timeline state work`; and a short rationale paragraph stating that Phase 38 extends the in-app Physics Paint Roto clipboard, resolver seam, and styled tooltip/capsule presentation; the only process boundary crossed is the EXISTING `physicPaintBridge.ts` parent bridge (owned by plans 38-02/38-04, not this one); no network endpoints, no auth surface, no new packages (Package Legitimacy Gate satisfied vacuously per 38-RESEARCH.md). Keep the artifact in English (project convention). Then re-run the plan-level gates below.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -F -c "No external API integration: internal Preact UI + timeline state work" .planning/phases/38-multi-copy-paste-and-tooltip-polish/COVERAGE.md && pnpm --dir app typecheck</automated>
    Expected: declaration grep == 1; typecheck exits 0. NO vitest invocation (D-15).
  </verify>
  <acceptance_criteria>
    - COVERAGE.md exists at the phase directory root with the exact verdict line and rationale.
    - Full plan gate green: typecheck passes with Task 1's changes in place.
    - No test file created, modified, or executed in this plan (D-15).
  </acceptance_criteria>
  <done>COVERAGE.md declaration committed; plan-level typecheck gate green; the 38-GROUP-COPY production slice is complete and ready for wave-2 consumers (38-04).</done>
</task>

</tasks>

## Artifacts this plan produces

Symbols and files created by this plan (the contract consumed by plans 38-02 and 38-04):

| Artifact | Kind | Location | Notes |
|----------|------|----------|-------|
| `RotoSessionCopiedGroupEntry` | interface (exported) | `app/src/components/physic-paint/roto/physicsPaintRotoSession.ts` | `{ readonly payload: PhysicPaintRotoRealKeyPayload; readonly sourceAppFrame: number; readonly sourceKeyId: string }` — structurally typed seam for 38-02's `createPhysicPaintRotoPasteKeyGroupIntent` factory input (no import from `app/src/types/physicPaint.ts`) |
| `RotoSessionCopiedKeyGroup` | interface (exported) | same | `{ readonly kind: 'group'; readonly entries: readonly RotoSessionCopiedGroupEntry[] }` — group half of the slot union (D-02) |
| `RotoSessionCopiedKeyValue` | union type (exported) | same | `RotoSessionCopiedKey \| RotoSessionCopiedKeyGroup` — widened slot value; single shape keeps NO discriminant (byte-identical 1-key path, D-01) |
| `isRotoSessionCopiedKeyGroup` | type guard (exported) | same | Narrowing helper; reused by the hook now and by 38-04's paste branch |
| `'copyKeyGroup'` | `RotoSessionActionName` member | same | New session action name |
| `copyKeyGroup(entries)` | `RotoSession` method | same | Fail-closed on `< 2` entries; freezes `{kind:'group', entries}` into the one slot; feedback `Copied {N} keys` |
| `RotoKeyUtilitiesInput.getSelectedKeyIds` | input port (required) | `app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts` | `() => readonly string[]` — Phase 37 selection set at the controller boundary |
| `RotoKeyUtilitiesInput.getRotoKeyRecords` | input port (required) | same | `() => readonly PhysicPaintRotoRealKeyRecord[]` — fresh store read at Copy time (Pitfall 6) |
| Feedback copy: `Copied {N} keys` | UI-SPEC locked string | session `copyKeyGroup` | Verbatim, no trailing period |
| Guard copy: `The selected Roto keys are no longer available.` | concise capsule string | hook group branch | Fail-closed stale-selection abort; clipboard untouched |
| Guard copy: `The copied Roto key group cannot paste through the single-key route.` | concise capsule string | hook `pasteKey` narrowing | Interface-first seam guard; branch body replaced by 38-04's real group route |
| `COVERAGE.md` | phase artifact | `.planning/phases/38-multi-copy-paste-and-tooltip-polish/COVERAGE.md` | No-external-API declaration |

Explicitly NOT produced here (owned by sibling plans): the `paste-key-group` intent/factory/resolver branch (38-02), the `useRotoTimelineActions.pasteKeyGroup` route and real paste-branch body (38-04), selection aftermath branch (38-04), any test file (38-07, post-UAT per D-15).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view -> controller | Strip forwards ONE Copy intent; selection-size branching and clipboard semantics live in the hook (UI-SPEC Preact and State Ownership Contract) — the view is never trusted to pick copy semantics |
| controller -> store read | Group entries snapshot from authoritative `physicPaintStore.getRotoRealKeyRecords(layerId)`; stale keyIds in the selection set are untrusted input and must fail closed |
| clipboard slot (in-memory) | Session-local, never crosses the bridge, never persisted; payloads are rendered PNG dataUrl strings consumed only by existing image decode paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-02 | Information Disclosure | Group copy guard/feedback strings (hook + session) | low | mitigate | Fixed concise strings only (`Copied {N} keys`, stale-selection guard, single-route guard); no resolver internals, keyIds, or payload data in user-facing lines; any future reject detail routes to LOG only (36.14 D-26) |
| T-38-04 | Tampering | Capsule feedback rendering of copy/guard strings | medium | mitigate | Strings render as Preact text children only through the existing setApplyMessage/capsule path (T-36.15-01/08); no HTML injection surface introduced |
| T-38-01a | Tampering | Clipboard payload mutation between Copy and Paste | medium | mitigate | Entries frozen with Object.freeze at copy time (entry + entries array + group wrapper); store payloads are immutable identity-owned records replaced wholesale on acceptance, so the held reference is a true point-in-time snapshot (D-03) |
| T-38-01b | Denial of Service | Malformed/stale selection set (keyIds with no store record, entries < 2) | low | mitigate | Fail-closed at both layers: hook aborts with the stale-selection message leaving the clipboard untouched; session copyKeyGroup rejects entries.length < 2 via the existing failed() helper |
| T-38-SC | Tampering | npm/pip/cargo installs | high | mitigate | Vacuous — no new packages (Package Legitimacy Audit, 38-RESEARCH.md); no install tasks exist in this plan |
</threat_model>

<verification>
Plan-level verification (D-15: NO test creation/modification/execution in this plan — `vitest` is never invoked; typecheck is a compile gate, not a test):

1. `pnpm --dir app typecheck` — green with the widened union across session, hook, and Studio.
2. Byte-identity gates (D-01/Pitfall 7): single-key tokens each appear exactly once — `Copied key ${appFrame}.`, `copiedKey.value = { frame: appFrame, cachedFrame: normalized };`, `const clipboardPayload = toClipboardPayload(copiedKey);`.
3. Group-path presence gates: `copyKeyGroup` (>= 3 occurrences in session), `Copied ${entries.length} keys` (== 1), `isRotoSessionCopiedKeyGroup` in the hook (>= 2), Studio ports (== 1 each).
4. COVERAGE.md contains the exact verdict line.
5. Live end-to-end behavior (select 2 keys -> Copy -> `Copied 2 keys` capsule line, slot holds frozen group entries, single-key copy/paste unchanged) is verified by the user-owned native UAT checkpoint in plan 38-06 — the agent does not launch the server/app (CLAUDE.md).

Wave-1 coexistence check: this plan touches ONLY `physicsPaintRotoSession.ts`, `useRotoKeyUtilities.ts`, `PhysicsPaintStudio.tsx`, and `COVERAGE.md` — zero overlap with 38-02 (types/resolver/coordinator/bridge/history) and 38-03 (presentation selector + strip), so all three run in parallel.
</verification>

<success_criteria>
- 38-GROUP-COPY production slice complete: group Copy freezes store-sourced `{payload, sourceAppFrame, sourceKeyId}` entries into the one shared slot with `Copied {N} keys` feedback; 1-key Copy and single-key Paste are byte-identical.
- One slot, one disposal path, shape-agnostic paste availability (D-02).
- Paste group-shape narrowing compiles and fails closed; 38-04 owns the real route.
- COVERAGE.md declaration written.
- Typecheck green; zero vitest invocations (D-15); zero test files touched.
</success_criteria>

<output>
Create `.planning/phases/38-multi-copy-paste-and-tooltip-polish/38-01-SUMMARY.md` when done.
</output>
