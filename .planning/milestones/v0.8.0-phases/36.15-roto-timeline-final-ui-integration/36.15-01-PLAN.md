---
phase: 36.15-roto-timeline-final-ui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 36.15-ICON-ACTIONS
  - 36.15-SCRIPT-CONTROLS

must_haves:
  truths:
    - "Bottom action row renders the Key {n} chip first, then exactly 7 lucide icon buttons in locked order with accessible names: Insert key before, Duplicate key, Copy key, Paste key, Delete key, Copy Script, Apply Script (D-10, D-13; SCRIPT-CONTROLS ordering edge)."
    - "Guarded/unavailable actions stay visible, in order, and focusable: real buttons WITHOUT native disabled, using aria-disabled=\"true\" plus aria-describedby, with pointer/Enter/Space activation guarded BEFORE the controller call (D-12; ICON-ACTIONS boundary edge; SCRIPT-CONTROLS adjacency edge)."
    - "With no copied key/script, the styled tooltip carries the two-part grammar '{Action name} — unavailable: {verbatim controller reason}', e.g. 'Paste key — unavailable: no copied key'; reasons come only from controller ports (physicalActions.*DisabledReason, scriptAvailability copy/applyDisabledReason, sessionKeyAvailability) and are never shortened or re-derived in the view (D-12; SCRIPT-CONTROLS empty edge)."
    - "The shared styled tooltip appears only after exactly 1000ms of pointer hover (never instantly), appears immediately on keyboard focus, and dismisses on pointer leave, blur, Escape, or activation; its surface has pointer-events: none and never changes layout (D-14, D-17; ICON-ACTIONS precision edge)."
    - "Discard Script no longer renders anywhere in the bottom action row; Copy/Apply Script remain in the row (D-11 row portion)."
    - "UI-SPEC consideration (covered): the locked icon sequence plus Key {n} chip stays one fit-content non-wrapping pill in the action band; every icon carries an accessible name and a styled tooltip."
    - "UI-SPEC consideration (covered): tooltip multi-part guarded copy truncates gracefully inside the dark pill without changing layout."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (icon-only row)
    - app/src/components/physic-paint/physicsPaintStudio.css (.physics-paint-roto-key-icon-button, .physics-paint-styled-tooltip)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (rewritten icon/aria-label contract)
  key_links:
    - "Every surface adopting the styled tooltip loses its native title attribute so tooltips never double-fire (icon row in this plan; cells/capsule in Plan 05)."
    - "Guarded activation runs in the click/keydown handler BEFORE invoking onPaste/onCopyRotoScript/etc.; controllers re-validate regardless."
    - "Icon imports extend the existing single lucide-preact named-import line in PhysicsPaintWorkflowStrip.tsx — no second icon source."
  prohibitions:
    - "Presentation must not infer availability from frame type, mode, paint count, clipboard presence, or busy state; controller availability/reason ports are the only source (D-12)."
    - "No new packages: all required lucide icons are already vendored in lucide-preact 0.577.0; no icon-font, SVG-sprite, or third-party tooltip library."
    - "Tooltip, capsule, and reason strings render as Preact text children only; never inject controller-supplied reason strings as HTML."
  assumptions:
    - []
---

<objective>
Deliver the first production-quality vertical slice of the final Roto timeline UI: the new shared styled tooltip component (D-14/D-17) and the icon-only, guarded bottom action row (D-10..D-13), recomposed in place inside the existing `PhysicsPaintWorkflowStrip.tsx`. The tracer task proves the riskiest new pattern — one focusable `aria-disabled` guarded icon button carrying a verbatim controller reason through the styled tooltip — end-to-end (component, CSS, test contract) before expanding to all seven actions.

Purpose: The guarded focusable-button + styled-tooltip pattern is the phase's only genuinely novel UI mechanism; proving it on a single button first catches contract misunderstandings (1000ms delay, two-part reason grammar, no native disabled) before they replicate across seven controls.
Output: New `PhysicsPaintStyledTooltip` component with fake-timer tests; rewritten strip source-contract tests (icon/aria-label assertions replacing text-button assertions); icon-only action row wired to existing controller reason ports.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md
@SPECS/36.x-phases/phase-36.15-final-ui/spec-36.15-final-ui.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end styled tooltip + one guarded icon action (Paste) — tracer slice</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx, app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (full file; especially lines 1-3 imports, 272-289 reason-port derivations, 352-363 getRotoKeyUtilityDisabledMessage, 571-604 drag-session listener/cleanup discipline, 774-779 nav icon-button shape, 910-920 text-button row)
    - app/src/components/physic-paint/physicsPaintStudio.css (lines 1557-1567 key-utilities pill, 1608-1613 disabled-button treatment)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md (section 9 — tooltip no-analog contract; Shared Patterns — guarded action)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Styled Tooltip Behavior, Guarded Script Controls, Color, Typography)
  </read_first>
  <behavior>
    - Tooltip test 1 (fake timers): pointerenter, advance 999ms → tooltip NOT visible; advance to 1000ms → tooltip visible (D-17 exact delay).
    - Tooltip test 2: keyboard focus → tooltip visible immediately with no timer; blur → hidden.
    - Tooltip test 3: Escape while visible → hidden; pointerleave before 1000ms → never appears.
    - Tooltip test 4: surface class is physics-paint-styled-tooltip with pointer-events: none; content renders as text children (no HTML injection path).
    - Strip test (new assertions, RED): PhysicsPaintWorkflowStrip.tsx contains aria-label="Paste key"; the Paste button carries aria-disabled (not a native disabled attribute) and an aria-describedby hook when unavailable; its guarded click path returns before invoking the paste handler when the controller reports unavailability.
  </behavior>
  <action>
    RED first: create PhysicsPaintStyledTooltip.test.ts with the four fake-timer behavior cases above, and append the new Paste icon-button assertions to PhysicsPaintWorkflowStrip.test.ts; run vitest and watch them fail. Then GREEN: (1) Create PhysicsPaintStyledTooltip.tsx — a single shared component/hook (planner-discretion shape per D-14, e.g. a useStyledTooltip(delayMs = 1000) hook plus a tooltip surface element) implementing: pointerenter starts a 1000ms timer cleared by pointerleave; focus shows instantly, blur hides; a window keydown listener for Escape is registered only while visible (copy the drag-session listener discipline from strip lines 571-604: idempotent cleanup, mountedRef guard); surface is a dark pill with class physics-paint-styled-tooltip, role="tooltip", pointer-events: none, text rendered as Preact text children only. (2) Add CSS in physicsPaintStudio.css: .physics-paint-styled-tooltip (background #20262D, text #EEF4F8 at 10px/600/1.2, subtle border in the #5D6670–#69727C family, rounded pill, positioned absolutely, max-width with ellipsis truncation) and .physics-paint-roto-key-icon-button (22px-high button, neutral action surface #3A424C / border #58616B / text #EEF4F8 per the Color contract, [aria-disabled="true"] state reusing the reduced-emphasis treatment from css lines 1608-1613). (3) In PhysicsPaintWorkflowStrip.tsx convert ONLY the Paste text button (strip lines 910-920 block) to the guarded icon pattern: type="button", class physics-paint-roto-key-icon-button, aria-label="Paste key", lucide ClipboardPaste at size 15 with aria-hidden="true", aria-disabled='true' when unavailable, aria-describedby pointing at the reason id when a reason exists, onClick/onKeyDown guards that preventDefault and return BEFORE calling the paste handler when unavailable, styled tooltip content "Paste key" or "Paste key — unavailable: {verbatim controller reason}" (D-12 grammar). Remove the native title attribute from this button. Extend the existing single lucide-preact import line; do not add a second import source. Leave the other six text buttons untouched in this task.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintStyledTooltip.test.ts passes: not visible at 999ms hover, visible at 1000ms, instant on focus, hidden on Escape/blur/pointerleave.
    - PhysicsPaintWorkflowStrip.tsx source contains the exact strings aria-label="Paste key" and ClipboardPaste, contains aria-disabled on the Paste button, and the Paste button markup contains no native disabled attribute.
    - PhysicsPaintWorkflowStrip.tsx Paste tooltip wiring produces the literal two-part grammar prefix "Paste key — unavailable: " followed by the controller reason value (no locally re-derived fallback string).
    - physicsPaintStudio.css contains .physics-paint-styled-tooltip with pointer-events: none and background #20262D, and .physics-paint-roto-key-icon-button with an [aria-disabled="true"] selector.
    - The Paste button has no title= attribute in the strip source.
    - pnpm --dir app vitest run on the two touched test files exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>The tracer path — one focusable guarded icon button with a 1s-delay styled tooltip carrying a verbatim controller reason — works end-to-end through component, CSS, and source-contract tests, and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand to the full 7-icon guarded action row and rewrite the strip test contract</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 272-289 reason ports, 352-363 getRotoKeyUtilityDisabledMessage fallbacks, 910-920 full text-button row, the Key {n} chip render at the row start)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (full file; lines 66-77 text-button assertions being replaced)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Bottom Action Row Tooltips table, Guarded-State Tooltip Grammar, Color contract for neutral/destructive/script action surfaces)
  </read_first>
  <behavior>
    - Test 1: the row block contains the Key {n} chip class before any action button.
    - Test 2: aria-labels appear in locked source order — Insert key before, Duplicate key, Copy key, Paste key, Delete key, Copy Script, Apply Script (assert via indexOf ordering on the seven exact aria-label strings).
    - Test 3: the strip source no longer contains text-button markup for the seven actions and contains no Discard Script button in the row (D-11).
    - Test 4: no button inside the key-utilities row uses a native disabled attribute; guarded buttons use aria-disabled + aria-describedby; each guarded activation path returns before its controller handler when unavailable (both click and Enter/Space keydown).
    - Test 5: each icon button's tooltip copy constant matches the UI-SPEC table exactly (Insert key before / Duplicate key / Copy key / Paste key / Delete key / Copy Script / Apply Script).
  </behavior>
  <action>
    RED first: rewrite PhysicsPaintWorkflowStrip.test.ts lines 66-77 (and any adjacent text-button assertions) into the icon/aria-label contract described in behavior — the existing text-button markup assertions must be replaced, not supplemented, so stale assertions never survive the cutover; run vitest and watch the new assertions fail. GREEN: convert the remaining six text buttons (Insert, Dup, Copy, Delete, Copy Script, Apply Script) to the Task 1 guarded icon pattern with lucide icons BetweenVerticalStart, CopyPlus, ClipboardCopy, Trash2, Clipboard, ClipboardPen (size 15, aria-hidden="true") and the exact aria-label/tooltip names from the UI-SPEC table (D-10). Keep the Key {n} chip (.physics-paint-roto-key-context) at row start unchanged (D-13). Delete uses the destructive surface (#503238 with #FFB0B8 emphasis); Copy/Apply Script use the script action surface (#34475E / #5F7593 / #DDEBFA); the other four use the neutral surface. Remove the Discard Script button from this row entirely and drop its row-level usage (D-11 row portion — the capability is not deleted: Plan 07 re-homes it in the right-panel Scripts toolbar). Keep the onDiscardRotoScript prop declared in the strip props interface for now — Plan 07 removes it from the interface together with the Studio rewiring. Remove native title attributes from every converted button; route all availability through the existing controller reason ports (physicalActions insert/delete DisabledReason, scriptAvailability copy/apply DisabledReason, sessionKeyAvailability reasons) with the getRotoKeyUtilityDisabledMessage local fallbacks DEFERRING to controller reasons wherever a port exists (D-12). Wire all seven buttons to the shared styled tooltip from Task 1 with the two-part unavailable grammar.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip.test.ts passes and asserts the seven exact aria-label strings in locked source order via indexOf comparisons.
    - Strip source contains all seven lucide icon identifiers: BetweenVerticalStart, CopyPlus, ClipboardCopy, ClipboardPaste, Trash2, Clipboard, ClipboardPen.
    - Strip source contains no text-button markup for the seven actions (the rewritten tests' negative assertions pass) and no Discard Script button in the row block.
    - The key-utilities row block contains zero native disabled= attributes; every guarded button carries aria-disabled and an aria-describedby reference when a reason exists.
    - Strip source still contains the .physics-paint-roto-key-context chip render before the first icon button (D-13).
    - pnpm --dir app vitest run on the two touched test files exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>All seven bottom-row actions are icon-only, guarded, focusable, tooltip-equipped, and ordered per D-10/D-13; Discard Script is gone from the row; the rewritten source-contract suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→view | Availability flags and reason strings cross from controller ports into tooltip/aria surfaces (untrusted-copy risk: injection, drift) |
| user→guarded control | Crafted DOM events (synthetic click/keydown) targeting unavailable actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-01 | Tampering | PhysicsPaintStyledTooltip / reason strings | medium | mitigate | Render all tooltip and reason strings as Preact text children (auto-escaped); no HTML injection API anywhere in the tooltip surface; asserted by Task 1 tooltip test 4 |
| T-36.15-02 | Elevation of privilege | Guarded icon buttons | high | mitigate | Guard pointer/Enter/Space activation in the handler BEFORE invoking the controller (both paths, asserted by Task 2 test 4); controllers re-validate availability regardless |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan; all lucide icons already vendored in lucide-preact 0.577.0 (verified on disk in RESEARCH.md) |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintStyledTooltip.test.ts` exits 0.
- `pnpm --dir app typecheck` exits 0.
- Source audit: the key-utilities row block contains no native `disabled=` and no `title=` on the seven icon buttons.
</verification>

<success_criteria>
- Icon-only guarded action row complete per D-10..D-14/D-17 with the locked order and exact tooltip copy.
- Styled tooltip component exists with the exact 1000ms hover / instant focus contract proven by fake-timer tests.
- Existing strip behaviors (navigation, interpolation, force spacing, drag machinery) untouched — the remaining old text assertions unrelated to the row still pass.
</success_criteria>

## Artifacts this phase produces (Plan 01)

- `PhysicsPaintStyledTooltip` component + `useStyledTooltip` hook — app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (NEW)
- `PhysicsPaintStyledTooltip.test.ts` (NEW — fake-timer contract tests)
- `.physics-paint-styled-tooltip` CSS class (NEW)
- `.physics-paint-roto-key-icon-button` CSS class + `[aria-disabled="true"]` state (NEW)
- Rewritten icon/aria-label source-contract assertions in PhysicsPaintWorkflowStrip.test.ts
- New lucide imports in the strip: Blend (Plan 04), BetweenVerticalStart, CopyPlus, ClipboardCopy, ClipboardPaste, Trash2, Clipboard, ClipboardPen (this plan)

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-01-SUMMARY.md` when done
</output>
