---
phase: 36.15-roto-timeline-final-ui-integration
plan: 08
type: execute
wave: 5
gap_closure: true
depends_on: [36.15-06]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 36.15-GROUP-SEPARATION
  - 36.15-ICON-ACTIONS
  - 36.15-SCRIPT-CONTROLS

must_haves:
  truths:
    - "The Tools dropdown button is removed from the header. Top bar final order, left to right: playback pill · fps/auto pill · status capsule · interpolation pill · Close button (UAT Gap A)."
    - "The interpolation pill is a dropdown offering 'Frame duplicate' and 'Frame blend' options (UAT Gap A)."
    - "+ Key and Duplicate live in the bottom action row before Insert; Set Key Space lives in the bottom action row after Delete Key (UAT Gap A)."
    - "No strip control or open dropdown is clipped or masked by the canvas or the strip overflow container (UAT Gap B — the interpolation 'Duplicate' pill was masked by the canvas)."
    - "Copy Script and Apply Script are removed from the bottom action row and appear in the Scripts right-sidebar tab toolbar before the clear-buffer (clipboard-x) icon, forming a proper second icon row styled like the first row (UAT Gap C)."
    - "The clear-buffer action's user-facing name/tooltip uses the user's 'clear script from buffer' wording instead of the current strange name (UAT Gap C)."
    - "Every bottom action row icon carries a short visible text label after the icon; tooltips drop the tool-name prefix and show only description / 'unavailable: {reason}' copy; bottom-row icons are enlarged beyond the current size (UAT Gap D)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (header/bottom-row regrouping, interpolation dropdown, labeled icons)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (Copy/Apply toolbar row, clear-buffer rename)
    - app/src/components/physic-paint/physicsPaintStudio.css (z-index/clipping fix, second-row toolbar styling, larger icons, label styling)
  key_links:
    - "The guarded-icon + styled-tooltip pattern from Plan 01 (aria-disabled, verbatim controller reason, no native disabled/title) applies to every moved or renamed action."
    - "Tooltip grammar change (Gap D) touches the two-part '{Action} — unavailable: {reason}' copy introduced in Plan 01 — update the shared copy builder and its tests, not per-call sites."
---

<objective>
Close the four native-UAT gaps reported on 2026-07-26 after the user approved the 36.15 visuals:
regroup strip controls (remove the Tools dropdown, restore groups into the top and bottom bars in the
user-specified order), fix the canvas-masking z-index issue, relocate Copy/Apply Script into the
Scripts sidebar toolbar with a proper second row and a sensible clear-buffer name, and add short
labels plus bigger icons to the bottom action row with de-prefixed tooltips.

Purpose: Final presentation polish on the approved 36.15 UI — pure view-layer regrouping; no
business-logic changes.
</objective>

<context>
Source of truth: the user's native-UAT feedback message dated 2026-07-26 (quoted in
36.15-UAT.md Gaps A–D). Prior art: 36.15-01-SUMMARY.md (guarded icon + styled tooltip),
36.15-06-SUMMARY.md (header restructure, + Key tool, band geometry),
36.15-07-SUMMARY.md (Scripts toolbar Discard/clear-buffer action).
All work is presentation re-composition inside the existing strip and Scripts panel;
controller ports (canDiscard, addEmptyKey availability, etc.) already exist and are reused.
</context>

<tasks>

<task number="1">
<name>Regroup strip controls: remove Tools dropdown, restore top/bottom bar groups</name>
<read_first>
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/physicsPaintStudio.css
- .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-06-SUMMARY.md
</read_first>
<files>
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- app/src/components/physic-paint/physicsPaintStudio.css
- app/src/components/physic-paint/PhysicsPaintStudio.tsx
</files>
<action>
RED: update the strip source-contract tests for the new layout (header order, no Tools dropdown,
bottom-row order, interpolation dropdown options, no-clipping guard).
GREEN:
1. Remove the Tools dropdown button and its open/close machinery.
2. Header final order: playback pill · fps/auto pill · status capsule · interpolation pill · Close.
3. Interpolation pill becomes a dropdown with "Frame duplicate" and "Frame blend" options
   (wire to the existing blend/duplicate interpolation state; keep aria semantics).
4. Move + Key and Duplicate from the header to the bottom action row, before Insert.
5. Move Set Key Space (apply-spacing) from the header dropdown to the bottom action row, after Delete Key.
6. Fix the z-index/clipping issue: no header/bottom control or open dropdown may be masked by the
   canvas or clipped by the strip container (the interpolation 'Duplicate' pill was hidden behind canvas).
7. Keep the fixed 155px band geometry intact (Plan 06 contract) — regrouping must not reintroduce
   height overrides or dead space.
</action>
<verify>
- pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- pnpm --dir app typecheck
</verify>
<commit>feat(36.15-08): regroup strip controls into top and bottom bars (Gap A + B)</commit>
</task>

<task number="2">
<name>Relocate Copy/Apply Script to Scripts sidebar + clear-buffer rename</name>
<read_first>
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (Copy/Apply removal)
- .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-07-SUMMARY.md
</read_first>
<files>
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- app/src/components/physic-paint/PhysicsPaintStudio.tsx
- app/src/components/physic-paint/physicsPaintStudio.css
</files>
<action>
RED: update Scripts panel + strip tests (Copy/Apply present in sidebar toolbar before the
clear-buffer icon, absent from the bottom row; clear-buffer renamed).
GREEN:
1. Remove Copy Script and Apply Script from the bottom action row.
2. Add them to the Scripts right-sidebar tab toolbar immediately before the clear-buffer
   (clipboard-x) icon, reusing the existing guarded-icon + styled-tooltip pattern and controller ports.
3. Lay the toolbar out so the icons form a proper second row styled like the first row (no orphan
   single icon dangling on line 2).
4. Rename the clear-buffer action's user-facing label/tooltip to the user's wording
   ("clear script from buffer" family — e.g. 'Clear Script Buffer' with tooltip
   'Clear script from buffer') in place of the current strange name; keep aria labels in sync.
</action>
<verify>
- pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- pnpm --dir app typecheck
</verify>
<commit>feat(36.15-08): relocate Copy/Apply Script to Scripts sidebar toolbar (Gap C)</commit>
</task>

<task number="3">
<name>Bottom row labels, de-prefixed tooltips, larger icons</name>
<read_first>
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
- app/src/components/physic-paint/physicsPaintStudio.css
</read_first>
<files>
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- app/src/components/physic-paint/physicsPaintStudio.css
</files>
<action>
RED: strip tests assert each bottom-row action renders its short visible label after the icon,
tooltips no longer carry the tool-name prefix, and the enlarged icon size class/style is applied.
GREEN:
1. Add a short text name after each icon in the bottom action row (e.g. 'Key', 'Duplicate',
   'Insert', 'Delete', 'Space'…); keep the row inside its 28px band — enlarge icons and add labels
   without breaking the fixed band height (adjust band only if the user-visible result demands it,
   documenting any deviation).
2. Update the shared tooltip copy builder so tooltips drop the '{Action} — ' prefix and show only
   the description or 'unavailable: {reason}' part; update all affected tests.
3. Increase the bottom-row icon size beyond the current 16px.
</action>
<verify>
- pnpm --dir app vitest run (full app suite, never watch mode)
- pnpm --dir app typecheck
</verify>
<commit>feat(36.15-08): label and enlarge bottom action row icons, de-prefix tooltips (Gap D)</commit>
</task>

</tasks>

<verification>
- Full app suite green (pnpm --dir app vitest run) and typecheck exit 0 after each task.
- Source-contract tests lock: top bar order, no Tools dropdown, bottom row order
  (+ Key, Duplicate before Insert; Set Key Space after Delete Key), Copy/Apply in Scripts toolbar
  before clear-buffer, clear-buffer rename, labels present, tooltip copy without tool-name prefix.
- Drag machinery guardrails from Plan 06 stay green.
</verification>

<success_criteria>
- [ ] UAT Gaps A–D each closed by a dedicated commit
- [ ] No native disabled/title attributes; guarded-icon pattern preserved everywhere
- [ ] 155px band geometry and drag machinery untouched
- [ ] Full suite + typecheck green
</success_criteria>
