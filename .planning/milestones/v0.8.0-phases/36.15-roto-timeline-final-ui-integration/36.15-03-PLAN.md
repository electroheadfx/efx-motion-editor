---
phase: 36.15-roto-timeline-final-ui-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/index.html
  - app/src/components/physic-paint/view/applicationSelectionGuard.test.ts
autonomous: true
requirements:
  - 36.15-SELECTION-GUARD

must_haves:
  truths:
    - "app/index.html applies user-select: none (with -webkit- prefix) to exactly html, body, and #app (SELECTION-GUARD empty edge: the guard covers the whole shell including empty/chrome regions)."
    - "The selectable exception list is exactly input, textarea, [contenteditable=\"true\"], and .physics-paint-log-messages with user-select: text — no more, no fewer (SELECTION-GUARD adjacency edge: elements just inside/outside the exception boundary behave per this exact list)."
    - "The exception rule appears after the base rule in the same style block so it wins the cascade (SELECTION-GUARD ordering edge)."
    - "The guard does not interfere with canvas painting, sliders, timeline dragging, pointer capture, keyboard focus, native input editing, contenteditable fields, or LOG message selection/copying (UI-SPEC covered consideration: no layout or pointer geometry change; inputs and LOG preserve native selection for content of any length)."
    - "The exception target .physics-paint-log-messages remains present in production (PhysicsPaintRightPanel.tsx) so the exception cannot rot into a dead selector."
  artifacts:
    - app/index.html (new style block)
    - app/src/components/physic-paint/view/applicationSelectionGuard.test.ts (new source-contract test)
  key_links:
    - "Guard lives in app/index.html (the contract-named location), not app/src/index.css."
    - "Existing scoped user-select: none rules in physicsPaintStudio.css (studio root, strip) remain — the app-wide guard extends, not replaces, them."
  prohibitions:
    - "Never broaden the selectable exception list beyond the four contract selectors; any new selectable surface is an explicit spec change, not an implementation convenience."
  assumptions: []
---

<objective>
Deliver 36.15-SELECTION-GUARD: the application-level text-selection policy from the 36.14 base UI-SPEC (§Application Selection Guard), applied at `app/index.html` with the exact contract selectors and exception list, locked by a new source-contract test that reads the HTML file the same way the strip tests read TSX.

Purpose: Small, independent, zero-risk wave-1 plan that removes one of the six complement-scope anchors without touching any file the other wave-1 plans own.
Output: Selection-guard style block in app/index.html plus a permanent source-contract test including exception-target liveness.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md
@app/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selection-guard source-contract test + style block in app/index.html</name>
  <files>app/index.html, app/src/components/physic-paint/view/applicationSelectionGuard.test.ts</files>
  <read_first>
    - app/index.html (full file — 13 lines, currently no style block; keep body style="background:#0F0F0F")
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (lines 1-15 — the readFileSync source-contract idiom to copy)
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md (§Application Selection Guard, lines 309-330 — the exact CSS contract)
  </read_first>
  <behavior>
    - Test 1 (RED): app/index.html contains a style block applying user-select: none and -webkit-user-select: none to a selector group covering exactly html, body, and #app.
    - Test 2 (RED): the same style block applies user-select: text and -webkit-user-select: text to exactly input, textarea, [contenteditable="true"], and .physics-paint-log-messages.
    - Test 3 (RED): the exception rule appears after the base rule in the file (indexOf ordering).
    - Test 4: no other selector appears in either rule's selector group (guard against silent exception-list broadening).
  </behavior>
  <action>
    RED first: create applicationSelectionGuard.test.ts beside the strip test, copying its readFileSync idiom but resolving ../../../../index.html (app root) instead of a sibling TSX; implement the four behavior assertions; run vitest and watch them fail. GREEN: add a style block inside the head of app/index.html containing exactly the 36.14 UI-SPEC contract CSS — base rule for html, body, #app with both user-select prefixes set to none, followed by the exception rule for input, textarea, [contenteditable="true"], .physics-paint-log-messages with both prefixes set to text. Do not alter the existing body background attribute, the #app mount point, or the script tag. Do not add any other rule to the style block.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/applicationSelectionGuard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - applicationSelectionGuard.test.ts passes all four assertions.
    - app/index.html contains the base selector group html, body, #app with user-select: none and the exception selector group input, textarea, [contenteditable="true"], .physics-paint-log-messages with user-select: text, exception rule positioned after the base rule.
    - app/index.html retains the existing body background attribute and the #app mount element unchanged.
    - The style block contains no selectors beyond the two contract groups.
    - pnpm --dir app vitest run src/components/physic-paint/view/applicationSelectionGuard.test.ts exits 0.
  </acceptance_criteria>
  <done>The exact selection-guard contract is live in app/index.html and locked by a source-contract test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exception-target liveness + scoped-rule coexistence assertions</name>
  <files>app/src/components/physic-paint/view/applicationSelectionGuard.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx (line ~531 — the .physics-paint-log-messages element)
    - app/src/components/physic-paint/physicsPaintStudio.css (lines ~12 and ~1342 — existing scoped user-select: none rules)
    - app/src/components/physic-paint/view/applicationSelectionGuard.test.ts (Task 1 output)
  </read_first>
  <behavior>
    - Test 1 (RED-safe, passes immediately): PhysicsPaintRightPanel.tsx still renders the physics-paint-log-messages class — the contract exception target is live, not a dead selector.
    - Test 2: physicsPaintStudio.css retains its existing scoped user-select rules (the app-wide guard must extend, not delete, the studio/strip scoping).
  </behavior>
  <action>
    Add the two assertions to applicationSelectionGuard.test.ts using the same readFileSync idiom (resolve PhysicsPaintRightPanel.tsx as a sibling; resolve physicsPaintStudio.css one directory up). These are guardrails against future rot, not RED drivers — they should pass on first run; if either fails, stop and report rather than "fixing" production to satisfy the test. No production file changes in this task.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/applicationSelectionGuard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file asserts physics-paint-log-messages is present in PhysicsPaintRightPanel.tsx source.
    - The test file asserts the pre-existing scoped user-select rules in physicsPaintStudio.css are still present.
    - Full test file green: pnpm --dir app vitest run src/components/physic-paint/view/applicationSelectionGuard.test.ts exits 0.
  </acceptance_criteria>
  <done>The guard's exception target and its coexistence with existing scoped rules are permanently test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell→user | Global CSS selection policy applied to every surface, including surfaces owned by other features |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-05 | Tampering | Selection guard exception list | medium | mitigate | Keep the exact four-selector exception list; Task 1 test 4 fails on any added selectable selector; Task 2 liveness test keeps .physics-paint-log-messages honest |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/physic-paint/view/applicationSelectionGuard.test.ts` exits 0.
- Manual (deferred to phase-level user-owned native UAT via /gsd-verify-work): LOG text remains selectable, fps/N inputs remain editable, canvas painting and timeline drag unaffected.
</verification>

<success_criteria>
- 36.15-SELECTION-GUARD delivered with the exact contract CSS in the contract-named file, locked by source-contract tests.
</success_criteria>

## Artifacts this phase produces (Plan 03)

- Selection-guard style block — app/index.html (NEW block)
- `applicationSelectionGuard.test.ts` — app/src/components/physic-paint/view/ (NEW test file; readFileSync source-contract idiom against app/index.html)

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-03-SUMMARY.md` when done
</output>
