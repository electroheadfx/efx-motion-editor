---
phase: 36.15-roto-timeline-final-ui-integration
plan: 03
subsystem: ui
tags: [css, selection-guard, source-contract-test, app-shell, tdd]

requires:
  - phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
    provides: 36.14-UI-SPEC §Application Selection Guard exact CSS contract
provides:
  - Application-level selection guard style block in app/index.html (html/body/#app user-select: none; four-selector exception list user-select: text)
  - applicationSelectionGuard.test.ts source-contract suite (4 contract assertions + 2 liveness/coexistence guardrails)
affects: [36.15 native visual UAT (LOG text selection, input editing, canvas/timeline interaction unaffected)]

tech-stack:
  added: []
  patterns:
    - "readFileSync source-contract idiom against app/index.html (../../../../index.html from the view test dir), parsing <style> rules into selector-group + declarations for exact-set assertions"

key-files:
  created:
    - app/src/components/physic-paint/view/applicationSelectionGuard.test.ts
  modified:
    - app/index.html

key-decisions:
  - "Guard lives in app/index.html (the contract-named location), not app/src/index.css; existing scoped rules in physicsPaintStudio.css (body:has(.demo-shell), .physics-paint-workflow-strip) remain and are test-locked as coexistence guardrails"
  - "Exception rule placed after the base rule in the same style block so it wins the cascade; both rules carry -webkit- prefixed and unprefixed declarations per the 36.14 UI-SPEC contract"

requirements-completed: [36.15-SELECTION-GUARD]

coverage:
  - id: D1
    description: "app/index.html applies user-select: none (with -webkit- prefix) to exactly html, body, and #app"
    requirement: 36.15-SELECTION-GUARD
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/applicationSelectionGuard.test.ts#applies user-select: none to exactly html, body, and #app"
        status: pass
    human_judgment: false
  - id: D2
    description: "Selectable exception list is exactly input, textarea, [contenteditable=\"true\"], .physics-paint-log-messages with user-select: text, exception rule positioned after the base rule, and no other selector in either rule"
    requirement: 36.15-SELECTION-GUARD
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/applicationSelectionGuard.test.ts#exception group / cascade ordering / no extra selectors"
        status: pass
    human_judgment: false
  - id: D3
    description: "Exception target .physics-paint-log-messages remains present in PhysicsPaintRightPanel.tsx; pre-existing scoped user-select rules in physicsPaintStudio.css remain"
    requirement: 36.15-SELECTION-GUARD
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/applicationSelectionGuard.test.ts#liveness and coexistence guardrails"
        status: pass
    human_judgment: false
  - id: D4
    description: "Guard does not interfere with canvas painting, sliders, timeline dragging, pointer capture, keyboard focus, native input editing, contenteditable fields, or LOG message selection/copying"
    requirement: 36.15-SELECTION-GUARD
    verification: []
    human_judgment: true
    rationale: "Native visual/interactive UAT is the phase-level final gate per 36.15-UI-SPEC validation notes; the guard is a pure CSS selection-policy change with no layout or pointer geometry impact"

metrics:
  duration: 3min
  completed: 2026-07-25
status: complete
---

# Phase 36.15 Plan 03: Application Selection Guard in app/index.html Summary

**The 36.14 UI-SPEC application-level text-selection policy applied at the app shell — user-select: none on html/body/#app with an exact four-selector user-select: text exception list — locked by a permanent source-contract test including exception-target liveness and scoped-rule coexistence guardrails.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-07-25T18:16:28Z
- **Completed:** 2026-07-25T18:19:30Z
- **Tasks:** 2 (both auto, both TDD)
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- New `<style>` block in `app/index.html` head applying the exact 36.14 UI-SPEC contract CSS: base rule for `html, body, #app` with `-webkit-user-select: none; user-select: none`, followed by the exception rule for `input, textarea, [contenteditable="true"], .physics-paint-log-messages` with `-webkit-user-select: text; user-select: text`. Exception rule positioned after the base rule so it wins the cascade. Existing `body style="background:#0F0F0F"` attribute, `#app` mount element, and module script tag untouched; no other rules in the block.
- New `applicationSelectionGuard.test.ts` beside the workflow-strip test, copying its readFileSync source-contract idiom but resolving `../../../../index.html` (app root). Four contract assertions parse the `<style>` block into rules and verify exact selector sets, both vendor prefixes, cascade ordering, and that no selectors exist beyond the two contract groups (T-36.15-05).
- Two rot guardrails in the same suite: `.physics-paint-log-messages` must remain present in `PhysicsPaintRightPanel.tsx` source (line 531 confirmed), and `physicsPaintStudio.css` must retain its scoped `user-select: none` rules on `body:has(.demo-shell)` and `.physics-paint-workflow-strip` — the app-wide guard extends, never replaces, the studio/strip scoping.

## Task Commits

Each task was committed atomically (TDD: RED test gate, then GREEN implementation):

1. **Task 1 RED: failing selection-guard source-contract tests** - `e9eab19b` (test)
2. **Task 1 GREEN: selection-guard style block in app/index.html** - `8c0f1138` (feat)
3. **Task 2: liveness + coexistence guardrail assertions** - `735675c0` (test)

**Plan metadata:** see final docs commit below (SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md).

## Files Created/Modified

- `app/index.html` — NEW `<style>` block in head with the exact two contract rule groups; nothing else changed
- `app/src/components/physic-paint/view/applicationSelectionGuard.test.ts` — NEW: 4 contract assertions + 2 liveness/coexistence guardrails (6/6 green)

## Decisions Made

- **Guard location is `app/index.html`**, the contract-named location, rather than `app/src/index.css` — keeps the shell-level policy at the shell entry point per the 36.14 UI-SPEC.
- **Coexistence asserted, not assumed:** the pre-existing scoped rules in `physicsPaintStudio.css` are test-locked so a future cleanup cannot silently delete them while "consolidating" selection policy.

## Deviations from Plan

None - plan executed exactly as written.

## TDD Gate Compliance

- RED commit `e9eab19b` precedes GREEN commit `8c0f1138` in git log — verified. RED run failed all 4 contract assertions (no `<style>` block in app/index.html) before GREEN turned them green.
- Task 2's two guardrails passed on first run without a RED failure — the plan-sanctioned outcome ("RED-safe, passes immediately; if either fails, stop and report rather than fixing production"). Both targets were verified present in production before writing the assertions.
- No refactor commit needed.

## Threat Mitigations Verified

- **T-36.15-05 (Tampering / exception-list broadening):** test 4 asserts the guard contains exactly the two contract rules with exactly the seven contract selectors; any added selectable selector fails the suite. Task 2's liveness test keeps `.physics-paint-log-messages` honest against dead-selector rot.
- **T-36.15-SC (dependency installs):** none performed.

## Issues Encountered

- Shell cwd resets between Bash calls required explicit `cd app/` per pnpm invocation (recurring environment quirk); no functional impact.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The selection guard is live app-wide; remaining wave-1+ plans (04, 05, 06, 07) own disjoint files and are unaffected.
- Open item for native visual UAT (phase-level gate): LOG text remains selectable/copyable, fps/N inputs remain editable, canvas painting and timeline drag unaffected (coverage D4).

## Self-Check: PASSED

- Both key files verified on disk (1 created, 1 modified) plus this SUMMARY.md.
- All 3 task commits verified in git log: `e9eab19b`, `8c0f1138`, `735675c0`.
- `pnpm vitest run src/components/physic-paint/view/applicationSelectionGuard.test.ts`: 6/6 pass.
- `pnpm typecheck` exits 0.

---
*Phase: 36.15-roto-timeline-final-ui-integration*
*Completed: 2026-07-25*
