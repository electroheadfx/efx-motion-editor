---
phase: 36.15-roto-timeline-final-ui-integration
plan: 09
type: execute
wave: 6
gap_closure: true
depends_on: [36.15-08]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - 36.15-GROUP-SEPARATION
  - 36.15-ICON-ACTIONS

must_haves:
  truths:
    - "Scripts sidebar toolbar second-row icons (Copy Script, Apply Script, Clear Script Buffer) each carry a short visible name label, matching the bottom action row labeling style (UAT Gap E-1)."
    - "The Set Key Space form shows no doubled circle/ring background at the right edge of the APPLY button, and its bottom-row label reads 'Key spacing' instead of 'Space' (UAT Gap E-2)."
    - "The selected key cell renders its full orange selection border on all four sides — the right edge is not clipped by the adjacent cell (z-index/overlap fix; UAT Gap E-3)."
    - "The interpolation dropdown option reads 'Frame blending' instead of 'Frame blend' ('Frame duplicate' unchanged) (UAT Gap E-4)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (second-row labels)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (Key spacing label, Frame blending option)
    - app/src/components/physic-paint/physicsPaintStudio.css (APPLY ring removal, selection-border z-index)
  key_links:
    - "Selection-border clipping is a stacking/overlap issue between the selected cell and its right neighbor inside the 18px abutting grid — fix via z-index/box-shadow layering on the selected state, not by changing the 18px pitch or the 155px band geometry (Plan 06 contract)."
---

<objective>
Close UAT Gap E — four cosmetic items from the user's second native-UAT pass (2026-07-26):
label the Scripts sidebar second-row icons, remove the doubled ring on the Set Key Space APPLY
button and rename its label to 'Key spacing', fix the clipped right edge of the selected key
cell's orange border, and rename the interpolation option 'Frame blend' → 'Frame blending'.

Purpose: Final cosmetic approval pass before the user signs off the phase UAT.
</objective>

<context>
Source of truth: the user's feedback message dated 2026-07-26 with annotated screenshots
(quoted in 36.15-UAT.md Gap E). Plan 36.15-08 introduced the labeled bottom row, the
'Frame blend' dropdown option, and the Set Key Space guarded form this plan polishes.
</context>

<tasks>

<task number="1">
<name>Cosmetic polish: sidebar labels, APPLY ring, selection border, Frame blending</name>
<read_first>
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/physicsPaintStudio.css
- .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-08-SUMMARY.md
</read_first>
<files>
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
- app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
- app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
- app/src/components/physic-paint/physicsPaintStudio.css
</files>
<action>
RED: update contract tests (sidebar second-row labels present; 'Key spacing' label; 'Frame blending'
option text; selected-cell layering hook present).
GREEN:
1. Scripts sidebar: add a short visible name label to each second-row toolbar icon (Copy Script,
   Apply Script, Clear Script Buffer), consistent with the bottom action row label styling.
2. Set Key Space form: remove the doubled circle/ring background artifact at the right edge of the
   APPLY button (likely a duplicated background/box-shadow on the submit button or its wrapper);
   rename the bottom-row label 'Space' → 'Key spacing'.
3. Selected key cell: fix the clipped right orange border — the selection outline must render on
   all four sides above the adjacent cell (selected-state z-index / outline-vs-border fix; do NOT
   change the 18px cell pitch or band geometry).
4. Interpolation dropdown: rename option 'Frame blend' → 'Frame blending' (keep 'Frame duplicate').
</action>
<verify>
- pnpm --dir app vitest run (full app suite, never watch mode)
- pnpm --dir app typecheck
</verify>
<commit>feat(36.15-09): cosmetic polish — sidebar labels, APPLY ring, selection border, Frame blending (Gap E)</commit>
</task>

</tasks>

<verification>
- Full app suite green and typecheck exit 0.
- Contract tests lock: second-row sidebar labels, 'Key spacing' label, 'Frame blending' option,
  selected-cell layering fix.
- Plan 06 geometry guardrails (155px bands, 18px cells, drag machinery) stay green.
</verification>

<success_criteria>
- [ ] All 4 Gap E items fixed in one atomic commit (or split RED/GREEN)
- [ ] No regression to strip geometry, guarded-icon pattern, or drag machinery
- [ ] Full suite + typecheck green
</success_criteria>
