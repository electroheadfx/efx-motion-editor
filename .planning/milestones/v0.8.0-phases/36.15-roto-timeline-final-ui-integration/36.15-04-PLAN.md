---
phase: 36.15-roto-timeline-final-ui-integration
plan: 04
type: execute
wave: 2
depends_on: [36.15-01]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 36.15-GROUP-SEPARATION

must_haves:
  truths:
    - "Header is one non-wrapping flex row in D-01 order: mode label, navigation pill, (capsule slot added by Plan 05), interpolation pill, playback pill, apply-spacing pill, close button — at every host width (GROUP-SEPARATION ordering edge; UI-SPEC covered consideration: fixed controls never move, no breakpoint/wrap/overflow menu)."
    - "The four control groups render as distinct gray pill islands (rounded-full, tonal-variant backgrounds) separated by the 8px sm gap with NO literal divider glyphs (D-01..D-04; GROUP-SEPARATION adjacency edge: groups touch only through container gap, never borders; the old playback border-left divider is gone; GROUP-SEPARATION boundary edge: gap is exactly the 8px sm token, satisfying C-03's minimum-5px rule)."
    - "Pill structure is render-deterministic: each of the four pill classes appears exactly once per render regardless of state (GROUP-SEPARATION idempotency edge)."
    - "Interpolation pill: borderless blend icon toggle (aria-pressed reflects enabled state; active state via surface/glyph emphasis only, never an added border — D-05/C-01) plus the mode select; NO count input anywhere (GROUP-SEPARATION empty edge: pill is fully operable with zero real keys; UI-SPEC covered consideration: toggling never moves keys, mode select stays operable)."
    - "Apply-spacing pill ('key space' group — D-02: same group): align-horizontal-space-around icon rendered BEFORE the N input (C-02), then the nonnegative integer input (min=0, step=1, accessible name 'Empty frames between real keys'), then a text Apply button; invalid/empty input publishes nothing and stays session-local, and a pending Apply blocks a second submission (GROUP-SEPARATION concurrency edge; UI-SPEC covered consideration)."
    - "Playback pill keeps loop toggle + fps input (bounded 1-60) at fixed width with no label truncation path (UI-SPEC covered consideration)."
    - "Header Close button invokes the existing close path: strip receives a plain onClose prop; Studio's handler calls getCurrentWindow().close() (Tauri) or window.close() (browser bridge) so the onCloseRequested flush runs (UI-SPEC covered consideration: inherited close affordance, existing close/dispose flow owns behavior)."
    - "Mode label keeps its current structural appearance with no pill treatment (D-03; UI-SPEC covered consideration: dynamic PPaint #n / Roto #n label, fixed shrink-0 width family)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (recomposed header)
    - app/src/components/physic-paint/physicsPaintStudio.css (.physics-paint-pill* classes, borderless blend toggle, 46px header row)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (onClose handler)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (header contract assertions)
  key_links:
    - "Playback and interpolation groups SWAP positions versus current production; the capsule is inserted mid-header by Plan 05 — this plan's assertions must not hardcode a capsule adjacency that Plan 05 then breaks."
    - "Close intent routes through getCurrentWindow().close() so the existing onCloseRequested flush (usePhysicsPaintParentBridge.ts:30, PhysicsPaintStudio.tsx:568) runs — never a direct window destroy."
    - "The strip view never imports Tauri; the onClose prop is a plain () => void."
  prohibitions:
    - "No second design-token system: pill styling extends physicsPaintStudio.css BEM classes using the reference palette values; no shadcn, no Tailwind arbitrary-value classes copied from the reference file (D-04)."
    - "No interpolation count input, count persistence, or count-derived UI may reappear anywhere in the header — count authority is retired by the 36.14 physical contract."
  assumptions: []
---

<objective>
Recompose the workflow-strip header into the locked D-01 pill-island layout: four visually distinct gray control groups in the corrected order (playback ↔ interpolation swap), the borderless `blend` interpolation toggle (C-01/D-05), the `align-horizontal-space-around` apply-spacing pill (C-02), and the header Close button wired through the existing guarded close path. Geometry stays at current band sizes in this plan — the 155px cutover is Plan 06.

Purpose: Group separation (36.15-GROUP-SEPARATION) is the visual heart of the complement spec; doing it after Plan 01 lets the header reuse the now-proven icon/tooltip conventions while the strip's drag machinery and action row remain untouched.
Output: Pill-styled header with all four groups, close affordance, and a source-contract test suite locking order, membership, borderlessness, and the no-count-input rule.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-01-SUMMARY.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header pill recomposition — order, pills, blend toggle, apply-spacing pill</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 774-839 — navigation/playback/interpolation/force-spacing groups; lines 795-811 interpolation toggle; 812-823 mode select; 826-839 force-spacing form; 316-320 handleForceSpacingSubmit guard)
    - app/src/components/physic-paint/physicsPaintStudio.css (lines 1368-1376 header grid, 1459-1462 playback border-left divider, 1557-1567 key-utilities pill treatment, 1621-1643 interpolation group wrapper, 1654-1716 interpolation toggle border/green)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md (section 2 CSS cutover notes; Shared Patterns — pill-island styling with the exact per-group colors)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Header Order, Color, Spacing Scale, Interaction Contract — Force Spacing and Interpolation Toggle)
  </read_first>
  <behavior>
    - Test 1 (RED): strip source declares the four pill classes (.physics-paint-pill--navigation, --interpolation, --playback, --apply-spacing) in D-01 source order (indexOf ordering: navigation before interpolation before playback before apply-spacing), and the mode label keeps its existing class with no pill class.
    - Test 2 (RED): the interpolation toggle renders the Blend lucide icon, carries aria-pressed, and its class list contains no bordered variant; the source contains no interpolation count input and no INTERPOLATION text label.
    - Test 3 (RED): within the apply-spacing pill, AlignHorizontalSpaceAround appears before the N input (indexOf ordering), and the submit control is a text Apply button (the Check-icon submit is gone).
    - Test 4 (RED): physicsPaintStudio.css contains the pill tonal-variant backgrounds (navigation #34383C / border #575E66; interpolation #323A43 / border #596775; playback #34383C / border #59616A) with 8px inter-pill gaps, the playback group no longer has a border-left divider, and the interpolation toggle has no border property (the old green active fill is removed in favor of neutral surface + #B8C7FF glyph emphasis).
    - Test 5: the force-spacing guard line (ready/mutationLocked/forceSpacingAvailable early return) survives verbatim; the mutation-lock interpolation guard survives verbatim.
  </behavior>
  <action>
    RED first: add the five behavior assertion groups to PhysicsPaintWorkflowStrip.test.ts and watch them fail. GREEN: (1) Rebuild the header as a single non-wrapping flex row (46px target band height is fine to apply now; full strip geometry is Plan 06): mode label first, then the four groups wrapped in pill containers with the BEM classes above in D-01 order — note the playback and interpolation groups SWAP versus current production — leaving a clearly-named flex:1 spacer/capsule slot between navigation and interpolation that Plan 05 fills (a plain flex-1 spacer div, not a styled capsule). (2) Interpolation pill: replace the text toggle with a borderless Blend icon toggle (lucide Blend, size 15, glyph #B8C7FF, aria-pressed bound to enabled state, keep aria-busy and the mutation-lock guard verbatim), active state conveyed by surface/glyph emphasis only; keep the mode select beside it; delete every trace of the old bordered/green treatment in CSS. (3) Apply-spacing pill: render AlignHorizontalSpaceAround (size 15) before the existing N input (keep min="0" step="1" and accessible name "Empty frames between real keys"), replace the Check-icon submit with a text Apply button, and keep handleForceSpacingSubmit's validation guard exactly as-is — canonical parsing stays controller-owned. (4) CSS: add .physics-paint-pill base (rounded-full, 32px outer height, inline-flex, 8px container gap) plus the three tonal variants above (apply-spacing may reuse the navigation tonal variant); remove the playback border-left divider; keep all four pills fixed-width (flex: 0 0 auto) so only the Plan 05 capsule will flex (D-18). Extend the existing lucide import line with Blend and AlignHorizontalSpaceAround.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior assertion groups pass in PhysicsPaintWorkflowStrip.test.ts.
    - Strip source contains Blend and AlignHorizontalSpaceAround in the lucide import and render paths; interpolation toggle carries aria-pressed and no border class.
    - Strip source contains a text Apply submit for force spacing and retains the exact guard line `if (props.ready === false || props.mutationLocked || !forceSpacingAvailable) return;`.
    - physicsPaintStudio.css contains .physics-paint-pill--navigation, --interpolation, --playback, --apply-spacing with the specified tonal-variant backgrounds and an 8px group gap; no border-left remains on the playback group; no border property on the interpolation toggle; the old green active fill value is absent from the interpolation styles.
    - No count input exists anywhere in the strip header (negative assertion in test 2 passes).
    - pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>The header renders four distinct pill islands in D-01 order with the borderless blend toggle, icon-prefixed apply-spacing pill with text Apply, and all existing guards intact.</done>
</task>

<task type="auto">
  <name>Task 2: Header Close button wiring through the existing close-flush path</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 568 close flush, 1000-1024 workflow props composition)
    - app/src/components/physic-paint/hooks/usePhysicsPaintParentBridge.ts (line ~30 onCloseRequested)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (header region; the empty .physics-paint-state-actions placeholder div)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md (Pitfall 6, Assumption A1 — close path contract)
  </read_first>
  <action>
    Add an optional onClose?: () => void to PhysicsPaintWorkflowStripProps. In the strip header, render a Close icon button (lucide X, size 15, aria-label="Close", same neutral icon-button treatment as Plan 01's row buttons, styled tooltip "Close" via the Plan 01 tooltip component) inside the existing .physics-paint-state-actions header slot, invoking props.onClose. In PhysicsPaintStudio.tsx, add the handler: call getCurrentWindow().close() from @tauri-apps/api/window, falling back to window.close() when the Tauri API is unavailable (browser bridge mode) — this routes close intent through the existing onCloseRequested flush; pass it as onClose in the workflow props. The strip view must not import @tauri-apps anything. Add a source-contract assertion to PhysicsPaintWorkflowStrip.test.ts: strip source contains onClose?: () => void and aria-label="Close", and contains no @tauri-apps import.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Strip props interface declares onClose?: () => void; header renders a button with aria-label="Close" inside .physics-paint-state-actions.
    - Strip source contains no @tauri-apps import (negative assertion passes).
    - PhysicsPaintStudio.tsx workflow props include onClose whose handler calls getCurrentWindow().close() with a window.close() fallback.
    - pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>Header Close affordance exists and routes through the guarded close-flush path with Tauri imports confined to Studio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→close affordance | Close intent crosses from view into the window/bridge lifecycle (unsaved-work risk if the flush is bypassed) |
| user→force-spacing input | Numeric input crossing into a controller transaction (inherited validation authority) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-06 | Tampering | Close button path | high | mitigate | Close intent calls getCurrentWindow().close() so the existing onCloseRequested flush always runs; strip receives a plain prop and cannot destroy the window directly; typecheck + source-contract assertion lock the boundary |
| T-36.15-07 | Tampering | Force-spacing input | low | mitigate | Validation stays controller-owned (parseCanonicalForceSpacing); the view adds no new validation authority beyond native min/step affordances; guard line asserted verbatim |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` exits 0.
- `pnpm --dir app typecheck` exits 0.
- Source audit: header order, pill classes, borderless blend toggle, no count input, no playback border-left, no green interpolation fill.
</verification>

<success_criteria>
- 36.15-GROUP-SEPARATION delivered: four distinct pill islands in D-01 order with the complement's icon treatments (C-01, C-02, C-03) and a working header Close routed through the guarded close path.
</success_criteria>

## Artifacts this phase produces (Plan 04)

- `.physics-paint-pill` base class + `.physics-paint-pill--navigation` / `--interpolation` / `--playback` / `--apply-spacing` tonal variants (NEW CSS)
- Borderless Blend interpolation toggle treatment (replaces bordered text toggle)
- `onClose?: () => void` prop on PhysicsPaintWorkflowStripProps (NEW prop)
- Header Close button in `.physics-paint-state-actions` (NEW control)
- New lucide imports in the strip: Blend, AlignHorizontalSpaceAround, X

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-04-SUMMARY.md` when done
</output>
