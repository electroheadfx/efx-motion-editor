---
phase: 36.15-roto-timeline-final-ui-integration
plan: 07
type: execute
wave: 5
depends_on: [36.15-01]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: true
requirements:
  - 36.15-SCRIPT-CONTROLS

must_haves:
  truths:
    - "Discard Script (lucide clipboard-x, styled tooltip 'Discard Script') renders inside the right-panel Scripts toolbar (.physics-paint-scripts-toolbar in PhysicsPaintScriptsPanel.tsx) as a guarded, focusable action: real button WITHOUT native disabled, aria-disabled='true' when unavailable, aria-describedby reason hook, pointer/Enter/Space activation guarded BEFORE the controller call (D-11, D-12; SCRIPT-CONTROLS adjacency edge)."
    - "Discard availability and its reason come only from the clipboard controller: a new canDiscard / discardDisabledReason pair on RotoScriptActionAvailability, computed from clipboard presence and busy state beside the existing copy/apply ports, and re-surfaced verbatim by the view in the two-part grammar 'Discard Script — unavailable: {reason}' (D-12; SCRIPT-CONTROLS empty edge: with an empty clipboard the action stays visible, in place, focusable, and self-explaining)."
    - "The relocation completes the D-11 contract: the strip props interface no longer declares onDiscardRotoScript, Studio's workflow props no longer pass it, and the discard handler (rotoScript.discardScript(); setLastError(null)) moves verbatim into Studio's scripts props (SCRIPT-CONTROLS ordering edge: the bottom row keeps exactly the 7 locked Plan 01 actions)."
    - "Durable script-library behavior is unchanged: rows, Save, Load and Apply, Play, Rename, Delete with its confirmation copy, and Refresh keep current semantics; Discard acts only on the volatile clipboard and shows no confirmation dialog (UI-SPEC covered consideration: toolbar with Discard Script)."
    - "The toolbar Discard button uses the Plan 01 styled tooltip (exactly 1000ms hover delay, instant on keyboard focus) and carries no native title attribute (D-14/D-17; Pitfall 4 double tooltips)."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts (canDiscard / discardDisabledReason port + DISCARD_REASONS)
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts (availability port cases)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (guarded toolbar Discard button)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts (NEW source-contract test)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (discard threading via scripts props; workflow props cleanup)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (props interface cleanup)
  key_links:
    - "Studio already composes the discard handler inline in the workflow props (~line 1022): move that exact handler body (rotoScript.discardScript(); setLastError(null)) into the rightPanel.scripts props — do not create a second discard path."
    - "The panel already receives controllers directly (library, playScript); pass rotoScript the same way and read rotoScript.availability.value.canDiscard / .discardDisabledReason in the view rather than deriving availability in Studio or the panel (D-12)."
    - "Plan 01 left the onDiscardRotoScript prop declared in PhysicsPaintWorkflowStripProps after removing the row button; this plan deletes that leftover declaration — the strip test suite (Plans 01/04/05/06) must stay green after the deletion."
  prohibitions:
    - "Do not reuse the panel's native-disabled IconButton helper for Discard — the guarded contract (no native disabled, aria-disabled, guarded activation, styled tooltip) is mandatory for script controls (RESEARCH Guarded Controls Pattern)."
    - "Presentation must not infer discard availability from clipboard presence or busy state locally; the controller's canDiscard/discardDisabledReason port is the only source (D-12)."
    - "Do not alter discardScript() semantics (its internal busy guard stays) and do not touch the durable-library delete confirmation flow."
    - "Leave no tombstone comments referencing the removed strip prop — source-contract assertions grep for its absence."
  assumptions: []
---

<objective>
Complete locked decision D-11: relocate Discard Script from the retired bottom-row button (removed in Plan 01) into the right-panel Scripts toolbar as a guarded `clipboard-x` icon action with a verbatim controller reason. Task 1 adds the missing controller port (`canDiscard` / `discardDisabledReason`) so the view never infers availability; Task 2 wires the toolbar button through the Plan 01 styled-tooltip/guarded-icon pattern, threads the handler through `PhysicsPaintStudio.tsx`, and removes the leftover `onDiscardRotoScript` strip prop.

Purpose: Without this plan the Discard capability is deleted from the UI — the D-11 relocation is a move, not a removal. It executes after Plans 04/05/06 purely for file ownership (the strip TSX and Studio are owned by waves 2-4); its only logical dependency is Plan 01's styled tooltip + guarded-icon pattern.
Output: Guarded Discard Script action in the Scripts toolbar, controller-owned availability/reason port, strip props interface cleaned, source-contract tests locking the relocation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-01-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clipboard controller discard availability port (canDiscard / discardDisabledReason)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts (lines 179-193 COPY_REASONS / APPLY_REASONS constant shape; lines 41-47 RotoScriptActionAvailability interface; lines 229-254 availability computed; lines 613-618 discardScript; the controller return block ~lines 829-845)
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts (lines 112-131 — existing hasCopiedScript / discardScript test idiom and harness setup)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md (Guarded Controls Pattern; Scripts toolbar IconButton seam code example with the availability port)
  </read_first>
  <behavior>
    - Test 1 (RED): with an empty clipboard, availability.value.canDiscard is false and discardDisabledReason is exactly 'Copy a Roto paint script before discarding it.'.
    - Test 2 (RED): after a successful copy (existing harness copy path), availability.value.canDiscard is true and discardDisabledReason is null.
    - Test 3 (RED): while the controller is busy (existing harness busy path, e.g. during an apply), availability.value.canDiscard is false and discardDisabledReason is exactly 'Finish the current script operation before discarding.'.
    - Test 4 (RED): RotoScriptActionAvailability exposes canDiscard and discardDisabledReason alongside the existing canCopy/canApply/reason fields (type-level presence via a typed read in the test).
  </behavior>
  <action>
    RED first: add the four behavior cases to physicsPaintRotoScriptClipboard.test.ts reusing the file's existing controller harness; run vitest and watch them fail. GREEN: in physicsPaintRotoScriptClipboard.ts — (1) add a DISCARD_REASONS constant beside COPY_REASONS/APPLY_REASONS with exactly two members: missing: 'Copy a Roto paint script before discarding it.' and busy: 'Finish the current script operation before discarding.'; (2) extend the RotoScriptActionAvailability interface with canDiscard: boolean and discardDisabledReason: string | null; (3) in the availability computed, derive discardDisabledReason as: controller busy/disposal-pending → DISCARD_REASONS.busy; otherwise clipboard.value === null → DISCARD_REASONS.missing; otherwise null — and canDiscard = discardDisabledReason === null; include both in the returned object. Do not change discardScript() (its internal busy guard stays as the second line of defense), do not touch copy/apply reason derivation, and do not add any new signal — this is derived state inside the existing computed.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four behavior cases pass with exact string equality on the two reason strings.
    - RotoScriptActionAvailability declares canDiscard: boolean and discardDisabledReason: string | null; the availability computed populates both from busy state and clipboard presence only.
    - discardScript() body is unchanged; COPY_REASONS/APPLY_REASONS and copy/apply derivation are unchanged (existing clipboard tests stay green).
    - pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>The clipboard controller owns discard availability and verbatim reasons; the view layer has a D-12-compliant port to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guarded Discard toolbar button + Studio threading + strip props cleanup</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (lines 7-16 props interface; lines 50-57 toolbar seam; lines 121-125 existing IconButton helper — pattern reference only, NOT reusable for the guarded contract)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines ~1002-1009 rightPanel.scripts props block; line ~1022 workflow props with the inline onDiscardRotoScript handler to relocate)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (line ~119 — the leftover onDiscardRotoScript prop declaration)
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (Plan 01 output — the shared styled tooltip component/hook to consume)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-01-SUMMARY.md (guarded-icon + tooltip usage pattern established for the row buttons)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-UI-SPEC.md (Guarded Script Controls; Bottom Action Row Tooltips table — Discard Script / clipboard-x / 'Discard Script')
  </read_first>
  <behavior>
    - Test 1 (RED): PhysicsPaintScriptsPanel.tsx source contains ClipboardX (lucide import), aria-label="Discard Script", and aria-disabled on the discard control; the discard control carries NO native disabled attribute and no title= attribute (negative assertions).
    - Test 2 (RED): the panel source wires the Plan 01 styled tooltip with the two-part grammar — the literal prefix "Discard Script — unavailable: " followed by the controller discardDisabledReason value — and an aria-describedby hook when a reason exists; guarded click and Enter/Space keydown paths return BEFORE invoking the discard handler when canDiscard is false.
    - Test 3 (RED): PhysicsPaintScriptsPanelProps declares rotoScript (clipboard controller type) and onDiscardScript: () => void; the Discard button renders inside the physics-paint-scripts-toolbar element.
    - Test 4 (RED): PhysicsPaintWorkflowStrip.tsx no longer contains the onDiscardRotoScript identifier, and PhysicsPaintStudio.tsx no longer passes onDiscardRotoScript in the workflow props while its rightPanel.scripts props pass onDiscardScript invoking rotoScript.discardScript() (negative + positive source assertions, readable from the new panel test file via the same readFileSync idiom).
  </behavior>
  <action>
    RED first: create PhysicsPaintScriptsPanel.test.ts with the four behavior assertion groups using the readFileSync source-contract idiom (resolve the panel TSX as a sibling; Studio and the strip TSX by relative path); run vitest and watch them fail. GREEN: (1) Panel props: extend PhysicsPaintScriptsPanelProps with rotoScript: RotoScriptClipboardController (the type already exported from physicsPaintRotoScriptClipboard.ts) and onDiscardScript: () => void. (2) Panel toolbar: render the Discard button inside .physics-paint-scripts-toolbar (append it after the Refresh Scripts button) as a guarded control per the Plan 01 pattern — type="button", class physics-paint-script-icon-button (toolbar visual consistency) plus the guarded treatment, aria-label="Discard Script", ClipboardX at size 16 with aria-hidden="true", aria-disabled='true' when rotoScript.availability.value.canDiscard is false, aria-describedby pointing at a reason id (useId) when discardDisabledReason is non-null, onClick/onKeyDown guards that preventDefault and return BEFORE calling onDiscardScript when unavailable, and the Plan 01 styled tooltip carrying 'Discard Script' or 'Discard Script — unavailable: {discardDisabledReason}' verbatim. Extend the existing lucide import line with ClipboardX. Do NOT route this button through the panel's native-disabled IconButton helper; render it as its own guarded element. (3) Studio: in the rightPanel.scripts props block (~lines 1002-1009) add rotoScript and onDiscardScript with the handler body moved verbatim from the workflow props (rotoScript.discardScript(); setLastError(null)); delete onDiscardRotoScript from the workflow props (~line 1022). PhysicsPaintRightPanel.tsx needs no edit — it spreads the scripts props object into the panel. (4) Strip cleanup: delete the leftover onDiscardRotoScript?: () => void declaration from PhysicsPaintWorkflowStripProps (~line 119). Leave no tombstone comments referencing the removed prop. (5) Run the full strip + panel + clipboard test files to confirm Plans 01/04/05/06 contracts still hold after the interface deletion.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintScriptsPanel.test.ts passes all four behavior assertion groups.
    - Panel source contains ClipboardX, aria-label="Discard Script", aria-disabled, and the styled-tooltip wiring with the literal two-part grammar prefix; the discard control has no native disabled and no title= attribute.
    - PhysicsPaintScriptsPanelProps declares rotoScript and onDiscardScript; the Discard button renders inside physics-paint-scripts-toolbar.
    - Greps for onDiscardRotoScript return zero matches in PhysicsPaintWorkflowStrip.tsx and in PhysicsPaintStudio.tsx; Studio's scripts props pass onDiscardScript whose handler invokes rotoScript.discardScript().
    - Durable-library flows untouched: the existing Save/Load/Play/Rename/Delete/Refresh IconButtons and the delete-confirmation dialog markup are unchanged.
    - pnpm --dir app vitest run on the three touched test files exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>Discard Script lives in the Scripts toolbar as a guarded, focusable, tooltip-equipped action fed by controller-owned availability/reasons; the strip's Discard surface is fully retired; D-11 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→view | Discard availability and reason strings cross from the clipboard controller into tooltip/aria surfaces (verbatim-copy contract) |
| user→guarded control | Crafted DOM events (synthetic click/keydown) targeting the unavailable Discard action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-12 | Tampering | Discard reason strings | medium | mitigate | Reason strings render as Preact text children (auto-escaped) via the Plan 01 tooltip; reasons originate only from the controller's DISCARD_REASONS constants — no view-side derivation (asserted by Task 2 test 2 grammar prefix + Task 1 exact-string tests) |
| T-36.15-13 | Elevation of privilege | Guarded Discard button | high | mitigate | Pointer and Enter/Space activation guarded in the handler BEFORE invoking onDiscardScript (Task 2 test 2); discardScript() retains its internal busy guard as defense in depth |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan; ClipboardX is already vendored in lucide-preact 0.577.0 (verified on disk in RESEARCH.md) |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts` exits 0.
- `pnpm --dir app typecheck` exits 0.
- Source audit: no onDiscardRotoScript remnant in strip or Studio; no native disabled/title on the toolbar Discard control.
- Manual (deferred to phase-level user-owned native UAT via /gsd-verify-work): Discard Script appears in the Scripts toolbar, is guarded with a verbatim reason when the clipboard is empty, discards the copied script when available, and durable library behavior is unchanged.
</verification>

<success_criteria>
- D-11 delivered: Discard Script relocated to the right-panel Scripts toolbar with the guarded focusable contract and controller-supplied reasons (36.15-SCRIPT-CONTROLS), and the strip props surface fully cleaned of the retired row action.
</success_criteria>

## Artifacts this phase produces (Plan 07)

- `canDiscard` / `discardDisabledReason` + `DISCARD_REASONS` on the script clipboard controller — physicsPaintRotoScriptClipboard.ts (NEW port)
- Clipboard availability port test cases — physicsPaintRotoScriptClipboard.test.ts (NEW cases)
- Guarded Discard Script `clipboard-x` toolbar button — PhysicsPaintScriptsPanel.tsx (NEW control; new lucide import ClipboardX)
- `rotoScript` + `onDiscardScript` props on PhysicsPaintScriptsPanelProps, threaded through PhysicsPaintStudio.tsx (NEW wiring; handler relocated from workflow props)
- `PhysicsPaintScriptsPanel.test.ts` (NEW source-contract test file)
- `onDiscardRotoScript` removed from PhysicsPaintWorkflowStripProps and Studio workflow props (cleanup completing D-11)

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-07-SUMMARY.md` when done
</output>
