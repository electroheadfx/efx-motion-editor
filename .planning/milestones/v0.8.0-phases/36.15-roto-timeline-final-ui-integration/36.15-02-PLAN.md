---
phase: 36.15-roto-timeline-final-ui-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/timeline.ts
  - app/src/lib/frameMap.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
autonomous: true
requirements:
  - 36.15-LAYER-KEY-MARKERS

must_haves:
  truths:
    - "Every physic-paint FX layer row in the main EFX Motion timeline draws one ~9px diamond per REAL Roto key at the key's physical frame, always visible (not selection-gated), with generated in-betweens receiving no marker (D-06, D-07; UI-SPEC covered consideration: zero keys draws no markers, one/many draw one diamond each)."
    - "Markers are pure canvas drawing: fill #F5A623, no stroke, no shadow, no click target, no hit-test path, no change to TimelineInteraction (D-08, D-09)."
    - "Marker x position equals (fxTrack.inFrame + appFrame) * frameWidth - scrollX + TRACK_HEADER_WIDTH, mirroring the existing drawKeyframeDiamonds coordinate math, so layers with nonzero inFrame place diamonds correctly."
    - "rotoKeyFrames data flows store → frameMap fxTrackLayouts computed → FxTrackLayout → renderer, recomputing when replaceRotoPhysicalRecords bumps physicPaintVersion (existing subscription; no new pub/sub channel)."
    - "Only layers whose primaryLayer.type is 'physic-paint' receive a rotoKeyFrames value; other layer types get undefined and draw nothing."
    - { statement: "Native visual UAT on a physic-paint layer with many real Roto keys confirms orange diamonds render at correct physical frames without overlapping adjacent layer content.", verification: backstop }
  artifacts:
    - app/src/types/timeline.ts (FxTrackLayout.rotoKeyFrames?: number[])
    - app/src/lib/frameMap.ts (rotoKeyFrames population)
    - app/src/components/timeline/TimelineRenderer.ts (getPhysicPaintRotoKeyMarkerGeometry export + drawRotoKeyMarkers pass)
    - app/src/components/timeline/TimelineRenderer.test.ts (geometry + reactivity + source-contract cases)
  key_links:
    - "physicPaintStore.getRotoRealKeyRecords(layerId) is the only data source — it returns real keys only, satisfying D-07 structurally."
    - "The draw pass lives inside the existing drawFxTrack physic-paint branch (TimelineRenderer.ts ~line 572), gated on fxTrack.rotoKeyFrames?.length."
  prohibitions:
    - "Do not revive or populate the dormant playScriptMarkers field for this feature; C-04 uses the purpose-named rotoKeyFrames field."
    - "Do not reuse drawLosange directly: it hardcodes gold fills and selection/hover shadows; the roto marker is a dedicated #F5A623 fill with no selection states."
    - "Markers add no interaction surface: no hit-test, no cursor change, no context menu, no TimelineInteraction edit."
  assumptions:
    - "FLAGGED (unclassified edge, 36.15-LAYER-KEY-MARKERS): rotoKeyFrames are layer-LOCAL physical appFrames and the renderer adds fxTrack.inFrame (RESEARCH assumption A3). The nonzero-inFrame geometry test guards this; if UAT shows offset markers on nonzero-inFrame layers, the coordinate choice — not the test — is revisited."
---

<objective>
Implement complement spec C-04 (D-06..D-09): real-Roto-key diamond markers on the physic-paint layer row of the main EFX Motion canvas timeline. One new optional field on `FxTrackLayout`, one population line in the existing `fxTrackLayouts` computed, one draw pass in `TimelineRenderer.drawFxTrack`, and one exported pure geometry helper — all following the established `playScriptMarkers`/`getTimelinePlayScriptMarkerGeometry` pattern without reviving the dormant Play path.

Purpose: This is the phase's only main-editor (non-standalone) surface; it proves the store→layout→canvas data path with pure-function tests before the strip recomposition waves begin.
Output: Always-visible #F5A623 diamonds per real Roto key, unit-tested geometry including the nonzero-inFrame case, plus a reactivity test proving marker data refreshes on physical record replacement.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-CONTEXT.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md
@.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-RESEARCH.md
@app/src/components/timeline/TimelineRenderer.ts
@app/src/lib/frameMap.ts
@app/src/types/timeline.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rotoKeyFrames field, frameMap population, renderer draw pass + pure geometry helper</name>
  <files>app/src/types/timeline.ts, app/src/lib/frameMap.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - app/src/types/timeline.ts (FxTrackLayout interface, ~lines 45-59, playScriptMarkers at line 56)
    - app/src/lib/frameMap.ts (full fxTrackLayouts computed, lines 120-202; getLayerId helper lines 120-122; physicPaintVersion subscription line 172)
    - app/src/components/timeline/TimelineRenderer.ts (lines 17-32 pure-helper export pattern, 419-426 clip context, 572-584 drawFxTrack physic-paint branch, 1253-1288 drawLosange geometry, 1454 iconSize=9, 1484-1492 drawKeyframeDiamonds FX coordinate math)
    - app/src/components/timeline/TimelineRenderer.test.ts (lines 10-17 store reset boilerplate, 22-42 dynamic-import geometry test idiom, 60-78 source-contract slicing idiom)
    - .planning/phases/36.15-roto-timeline-final-ui-integration/36.15-PATTERNS.md (section 5 TimelineRenderer, section 6 frameMap, section 7 types)
  </read_first>
  <behavior>
    - Test 1 (geometry, RED): getPhysicPaintRotoKeyMarkerGeometry({ appFrame: 5, inFrame: 0, frameWidth: 4, scrollX: 12 }) returns x = (0 + 5) * 4 - 12 + TRACK_HEADER_WIDTH.
    - Test 2 (nonzero inFrame, RED): same inputs with inFrame: 10 returns x = (10 + 5) * 4 - 12 + TRACK_HEADER_WIDTH — guards the layer-local vs timeline-global pitfall.
    - Test 3 (source-contract, RED): TimelineRenderer.ts drawFxTrack physic-paint branch references fxTrack.rotoKeyFrames and the marker fill is the literal #F5A623; the marker pass is NOT gated on playScriptMarkers.
    - Test 4 (source-contract, RED): frameMap.ts populates rotoKeyFrames from physicPaintStore.getRotoRealKeyRecords for physic-paint layers only.
  </behavior>
  <action>
    RED first: add the four behavior cases to TimelineRenderer.test.ts using the established dynamic-import idiom and store-reset boilerplate; run vitest and watch them fail. GREEN: (1) types/timeline.ts — add `rotoKeyFrames?: number[];` to FxTrackLayout beside playScriptMarkers with a comment "layer-local physical appFrames of real Roto keys (C-04 markers)". (2) frameMap.ts — inside fxTrackLayouts, populate rotoKeyFrames only when primaryLayer?.type === 'physic-paint', mapping physicPaintStore.getRotoRealKeyRecords(getLayerId(primaryLayer)) to r.appFrame; otherwise undefined. Do not add any new subscription — physicPaintVersion.value is already read at the top of the computed. (3) TimelineRenderer.ts — export a pure getPhysicPaintRotoKeyMarkerGeometry function with the exact coordinate math from behavior (mirror lines 1484-1492: inFrame + appFrame, times frameWidth, minus scrollX, plus TRACK_HEADER_WIDTH); add a private drawRotoKeyMarkers method using the drawLosange diamond path (moveTo top / lineTo right / lineTo bottom / lineTo left / closePath) at half-extent matching the 9px keyframe icon size, fill #F5A623, no stroke, no shadow, per-marker viewport culling, clipped to the FX bar like drawPhysicPaintPlayScriptMarkers (lines 419-426); call it from the drawFxTrack physic-paint branch when fxTrack.rotoKeyFrames?.length is truthy — always visible, never selection-gated (D-06).
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - TimelineRenderer.test.ts passes, including the nonzero-inFrame geometry case with the exact expected x formula.
    - types/timeline.ts FxTrackLayout contains rotoKeyFrames?: number[].
    - frameMap.ts contains a rotoKeyFrames assignment guarded by the physic-paint layer-type check and sourced from getRotoRealKeyRecords.
    - TimelineRenderer.ts exports getPhysicPaintRotoKeyMarkerGeometry and its drawFxTrack physic-paint branch invokes the roto marker pass gated on rotoKeyFrames length; the marker fill literal is #F5A623 and no shadow/stroke is set in the marker path.
    - TimelineRenderer.ts contains no new hit-test, cursor, or TimelineInteraction wiring for roto markers.
    - pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>Real-key diamonds render on physic-paint FX rows with tested geometry, tested data provenance, and zero interaction-surface changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reactivity proof — markers refresh on physical record replacement</name>
  <files>app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - app/src/components/timeline/TimelineRenderer.test.ts (lines 10-17 physicPaintStore.reset() boilerplate)
    - app/src/lib/frameMap.ts (fxTrackLayouts computed, physicPaintVersion subscription)
    - app/src/stores/physicPaintStore.ts (getRotoRealKeyRecords ~line 1262, replaceRotoPhysicalRecords ~line 1171, _notifyVisualChange ~line 240)
  </read_first>
  <behavior>
    - Test 1 (RED): after seeding a physic-paint layer with real Roto key records, fxTrackLayouts.value for that layer exposes rotoKeyFrames containing exactly the seeded appFrames (real keys only — generated interiors never appear).
    - Test 2 (RED): after replaceRotoPhysicalRecords with a changed record set, fxTrackLayouts.value recomputes and rotoKeyFrames reflects the new appFrames without any manual invalidation call.
    - Test 3: a non-physic-paint layer's FxTrackLayout has rotoKeyFrames undefined.
  </behavior>
  <action>
    RED first: add the three reactivity cases to TimelineRenderer.test.ts (or, if the fxTrackLayouts import graph makes the renderer test file awkward, co-locate them in the same file following its existing store-driven computed-value patterns — do not create a new test file). Use the existing physicPaintStore.reset() afterEach boilerplate; seed records through the store's real record-replacement API, never by poking private fields. GREEN: only if the tests expose a wiring defect — the expected outcome is that Task 1's population plus the existing physicPaintVersion subscription already pass; if the computed does not recompute, fix the population seam in frameMap.ts (not by adding a second subscription channel).
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/timeline/TimelineRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three reactivity tests pass against the real store + computed path (no mocks of physicPaintStore or frameMap internals).
    - Test assertions prove generated/derived frames are absent from rotoKeyFrames (D-07).
    - pnpm --dir app vitest run src/components/timeline/TimelineRenderer.test.ts exits 0; pnpm --dir app typecheck exits 0.
  </acceptance_criteria>
  <done>Marker data is proven reactive through the existing store→computed→layout path with real-keys-only semantics and no new invalidation machinery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store→renderer | Physical key records cross from physicPaintStore into canvas drawing code (data-integrity risk only; no user-controlled strings) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.15-03 | Tampering | rotoKeyFrames data path | low | mitigate | Data sourced exclusively from physicPaintStore.getRotoRealKeyRecords (real keys only); renderer performs pure numeric geometry with viewport culling — no DOM, no strings, no injection surface |
| T-36.15-04 | Elevation of privilege | Marker draw pass | low | accept | Visual-only pass adds no interaction surface by contract (D-08); enforced by Task 1 acceptance gate (no hit-test wiring) |
| T-36.15-SC | Tampering | dependency installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `pnpm --dir app vitest run src/components/timeline/TimelineRenderer.test.ts` exits 0.
- `pnpm --dir app typecheck` exits 0.
- Source audit: no reference to playScriptMarkers was added by this plan; the dormant Play path is untouched.
</verification>

<success_criteria>
- C-04 delivered: always-visible #F5A623 ~9px diamonds per real Roto key at physical frames on physic-paint FX rows.
- Geometry, reactivity, real-keys-only, and non-physic-paint exclusion all covered by automated tests.
</success_criteria>

## Artifacts this phase produces (Plan 02)

- `FxTrackLayout.rotoKeyFrames?: number[]` field — app/src/types/timeline.ts (NEW field)
- `getPhysicPaintRotoKeyMarkerGeometry` exported pure function — app/src/components/timeline/TimelineRenderer.ts (NEW export)
- `drawRotoKeyMarkers` private renderer method (NEW)
- rotoKeyFrames population in `fxTrackLayouts` computed — app/src/lib/frameMap.ts (NEW field population)
- Marker geometry + reactivity test cases — app/src/components/timeline/TimelineRenderer.test.ts (NEW cases)

<output>
Create `.planning/phases/36.15-roto-timeline-final-ui-integration/36.15-02-SUMMARY.md` when done
</output>
