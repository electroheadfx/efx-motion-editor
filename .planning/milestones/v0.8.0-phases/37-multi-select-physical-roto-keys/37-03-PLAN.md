---
phase: 37-multi-select-physical-roto-keys
plan: 03
type: execute
wave: 2
depends_on: [37-01, 37-02]
files_modified:
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - 37-GROUP-DRAG
  - 37-GROUP-DELETE
  - 37-GROUP-FORCE-SPACING
  - 37-ATOMIC-TRANSACTIONS
  - 37-SELECT-ALL
tags:
  - physics-paint
  - roto
  - selection
  - group-operations
  - preact

must_haves:
  truths:
    - "prepareRotoKeyGroupDrag(grabbedKeyId, target) reads the controller-supplied selection set via the new getSelectedKeyIds input port, mirrors the single-key guard order exactly (launch -> ports -> pending -> bounded grabbed -> unique grabbed record -> resolve -> no-change reject -> target signature -> frozen publication), and resolves one move-key-group intent through resolvePhysicPaintRotoPhysicalEdit into one immutable RotoDragPublication carrying proposal, proposalVersion, expectedLaunch, movedKeyId = grabbedKeyId, and the frozen movedKeyIds set (D-06/D-09; 37-GROUP-DRAG)"
    - "commitRotoKeyGroupDrag passes the exact retained publication unchanged to executePhysicalEdit with operationKind 'move-key-group' after wrapper-coherence checks (proposal kind, drag.movedKeyId == publication.movedKeyId, drag.movedKeyIds shallow-equals publication.movedKeyIds, non-empty launch tuple); no resolver call, no cloning, no mapping recomputation at commit (D-09 contract; 37-ATOMIC-TRANSACTIONS)"
    - "Group drag preparation failures return the concise UI-SPEC reject copy ('Move rejected — key in the way' for duplicate-destination-frame, 'Move rejected — not enough room' for over-capacity/out-of-range-frame, else the resolver failure text) plus the structured conflictingAppFrames passthrough and the full resolver failure text as detail — without publishing during the gesture (37-04 owns release-time publication; gesture-hover invalid targets must not churn the capsule, D-07/D-08)"
    - "deleteRotoFrame is group-aware: selection size >= 2 routes one delete-key-group intent carrying the controller-supplied frozen keyId set through runPhysicalAction with successMessage 'Keys deleted'; selection size < 2 (or the port absent) executes the unchanged single-key path with the existing success message; Backspace/Delete key and the toolbar Delete icon already reference this same bundle action, so all delete routes share one transaction with zero Studio/strip routing changes (D-13; 37-GROUP-DELETE)"
    - "Group delete rejects publish the resolver failure text via publishStatus (capsule) and the full failure detail (code + text) via the new publishDiagnostic port; acceptance records exactly one history entry through the 37-01-extended isOrdinaryOperationKind accepted-only effect (D-13, D-26; 37-ATOMIC-TRANSACTIONS)"
    - "applyForceSpacing is scope-aware per D-10: selection size >= 2 adds scopeKeyIds (frozen controller-supplied set) to the existing force-spacing intent; selection size < 2 passes scopeKeyIds null and executes the byte-identical 36.14 full-timeline path (GFS-3); scoped failures publish 'Spacing rejected — not enough room' for hard-wall/capacity codes via publishStatus plus full detail via publishDiagnostic; scoped success publishes proposal.status.text unchanged and never becomes a silent no-op (D-10..D-12; 37-GROUP-FORCE-SPACING)"
    - "canSelectAllKeys / selectAllKeysDisabledReason are reactive computeds on the action bundle deriving Select All availability from launch presence, no pending physical edit, and at least one real key record, with verbatim reasons for each guard ('Select a Physics Paint Roto timeline before selecting keys.' / 'A Roto physical edit is already in flight.' / 'No real Roto keys to select.') — the guarded-icon contract data 37-04 renders (D-03, 36.15 D-28; 37-SELECT-ALL)"
    - "The Studio selectAllRotoKeys callback publishes the 'All keys selected' status entry after a successful select-all so both the keyboard route (37-02 Cmd/Ctrl+A) and the future icon route produce the UI-SPEC feedback line; selection-only gestures (toggle/range/collapse) publish nothing (D-03, UI-SPEC status contract)"
  assumptions:
    - "LOG ROUTING SURFACE: 36.15-11 retired the right-panel LOG tab, so 36.14 D-26's 'detail goes to LOG' leg routes through the surviving diagnostic channel — a new optional publishDiagnostic input port wired in the Studio to the same console diagnostic style as the coordinator's logDiagnostic (PhysicsPaintStudio.tsx:426). No new LOG surface is built. Flagged for native-UAT confirmation in 37-05."
  prohibitions:
    - "Commit never recomputes, clones, or normalizes the retained publication; the proposal object passes to executePhysicalEdit unchanged (D-09)"
    - "No parallel/second transaction path: every group operation flows through runPhysicalAction or the prepare/commit seam into the one generic executePhysicalEdit coordinator — no direct bridge sends, no bypass (D-19; 37-ATOMIC-TRANSACTIONS)"
    - "The selection set is only ever READ from the controller port (getSelectedKeyIds); the hook never derives selection from frames, never mutates the set, and never persists it or sends it across the bridge (D-05)"
    - "Single-key behavior is unchanged: prepareRotoKeyDrag/commitRotoKeyDrag bodies untouched; single delete keeps its intent kind, operation kind, and success message; single-scope force-spacing keeps the full-timeline intent and copy (GFS-3, D-16)"
    - "Copy, Duplicate, Insert, Paste, and + Key bodies are untouched (D-16); no group Duplicate or group Copy/Paste is introduced (CONTEXT.md Deferred Ideas)"
    - "No compatibility fields, migration code, forwarding wrappers, aliases, dual-write paths, or additional timing/transaction authority; no legacy dual-frame identifiers added to touched code (D-19)"
    - "No test file is created, modified, deleted, renamed, or executed in this plan (D-18); verification is bounded static checks plus full typecheck only"
    - "No selection state, gesture handling, cell classes, or view logic is added to the workflow strip in this plan — strip integration is plan 37-04"
  artifacts:
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  key_links:
    - "Studio selectedKeyIds signal (37-02) -> getSelectedKeyIds input port (PhysicsPaintStudio.tsx composition, beside line 444) -> prepareRotoKeyGroupDrag / deleteRotoFrame / applyForceSpacing group branches"
    - "prepareRotoKeyGroupDrag -> resolvePhysicPaintRotoPhysicalEdit move-key-group intent (37-01) -> frozen RotoDragPublication (opaque retention) -> commitRotoKeyGroupDrag -> executePhysicalEdit -> exact parent ack -> one accepted-only history entry"
    - "deleteRotoFrame group branch -> runPhysicalAction delete-key-group -> coordinator -> acceptedPhysicalCommands -> useRotoPhysicalEditHistory accepted-only effect (37-01-extended guard) = exactly one Undo/Redo action (D-13)"
    - "canSelectAllKeys/selectAllKeysDisabledReason -> 37-04 Select All guarded icon; selectAllRotoKeys callback (37-02) -> 'All keys selected' publishStatus entry"
---

<objective>
Wire the Phase 37 group operations into the physical timeline action bundle: `prepareRotoKeyGroupDrag` / `commitRotoKeyGroupDrag` reusing the publication / opaque-retention / target-signature commit contract, group-aware `deleteRotoFrame` (one shared transaction for every delete route), scope-aware `applyForceSpacing` (selection-size-dependent scope per D-10), and Select All availability / guarded-reason derivation — all rejection reasons routed to the status capsule and diagnostic channel per D-26.

Purpose: 37-01 made the resolver understand group intents; 37-02 made the controller own the multi-selection state. This plan is the seam between them — the one bundle every gesture and keyboard route already calls — so group operations inherit the acknowledged transaction, rollback, and accepted-only history guarantees by construction (37-ATOMIC-TRANSACTIONS).

Output: extended action bundle (two new drag actions, group-aware delete/spacing, Select All availability computeds), extended input ports (`getSelectedKeyIds`, `publishDiagnostic`), and the Studio composition wiring for both ports plus the Select All status entry.

**D-18 gate:** this plan is PRE-UAT production. No test files, no vitest runs. Verification is bounded static checks plus full typecheck.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md
@.planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md
@.planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md
@.planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md

@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<precondition>
Plans 37-01 and 37-02 are complete: `grep -c "move-key-group" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` >= 4, `grep -c "delete-key-group" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` >= 3, `grep -c "scopeKeyIds" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` >= 1, `grep -c "selectedKeyIds" app/src/components/physic-paint/PhysicsPaintStudio.tsx` >= 2, and `grep -c "selectAllRotoKeys" app/src/components/physic-paint/PhysicsPaintStudio.tsx` >= 1. Halt if unmet — this plan consumes those exact symbols.
</precondition>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): End-to-end group drag wiring — one move-key-group publication from prepare through the acknowledged commit seam (D-06..D-09)</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — the file being modified; anchor sections: RotoDragTarget/TargetSignature/Publication/PreparationResult types (lines 53-90), RotoPhysicalTimelineActionBundle interface (92-144), RotoTimelineActionsInput (146-173), PhysicalActionRunnerInput (175-180), runPhysicalAction (237-285), prepareRotoKeyDrag (374-424), commitRotoKeyDrag (426-441), bundle useMemo (518-537), targetSignatureOf/isBoundedKeyId/buildProposalVersion helpers (558-576).
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — composition anchor: useRotoTimelineActions input object (lines 430-451, getSelectedKeyId at 444), logDiagnostic diagnostic style (line 426).
    - .planning/phases/37-multi-select-physical-roto-keys/37-01-PLAN.md — artifacts_produced: exact move-key-group intent shape ({ kind, movedKeyIds, grabbedKeyId, target }), PhysicPaintRotoPhysicalDragPresentation.movedKeyIds/grabbedKeyId, PhysicPaintRotoPhysicalEditFailure.conflictingAppFrames, proposal selectedKeyId = grabbed key.
    - .planning/phases/37-multi-select-physical-roto-keys/37-02-PLAN.md — selectedKeyIds Signal ownership and D-17 aftermath (post-accept group-drag selection rule already lives in the Studio; this plan does not touch it).
    - .planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md — useRotoTimelineActions section (drag prepare/commit seam shape, group-variant notes).
  </read_first>
  <action>
    1. Input ports (RotoTimelineActionsInput, beside getSelectedKeyId at line 160): add `getSelectedKeyIds?: () => readonly string[]` (controller-owned session selection set, 37-02; read-only here) and `publishDiagnostic?: (message: string) => void` (D-26 detail leg; see must_haves.assumptions for the retired-LOG-tab routing).

    2. Publication/preparation types (lines 76-90): add `readonly movedKeyIds?: readonly string[]` to RotoDragPublication (present and frozen for group publications; absent on single-key publications so existing consumers are untouched). On the RotoDragPreparationResult failure branch add `readonly conflictingAppFrames?: readonly number[]` and `readonly detail?: string` (the full resolver failure text for release-time diagnostic routing; 37-04 owns when to publish).

    3. Bundle interface (beside prepareRotoKeyDrag/commitRotoKeyDrag declarations): add `prepareRotoKeyGroupDrag: (grabbedKeyId: string, target: RotoDragTarget) => RotoDragPreparationResult` and `commitRotoKeyGroupDrag: (publication: RotoDragPublication) => Promise<boolean>` with JSDoc mirroring the single-key contract wording (opaque retention, no recomputation).

    4. prepareRotoKeyGroupDrag implementation — mirror the guard order of prepareRotoKeyDrag (lines 374-424) EXACTLY: (a) launch missing -> ok:false 'Select a real Roto key before editing the timeline.'; (b) executePhysicalEdit/getRotoKeyRecords/getRotoInterpolationState/getCapacity missing -> 'Timeline editing is unavailable.'; (c) pendingOperationId non-null -> 'A Roto physical edit is already in flight.'; (d) !isBoundedKeyId(grabbedKeyId) -> 'The dragged Roto key identity is malformed.'; (e) read `const selectedKeyIds = input.getSelectedKeyIds?.() ?? []` and fail closed with 'Select at least two real Roto keys to move as a group.' when the set has fewer than 2 members, contains an unbounded or duplicate member, or does not contain grabbedKeyId (the 37-04 strip routes single-key grabs to prepareRotoKeyDrag; this guard is defense-in-depth — the resolver remains the membership authority); (f) grabbed record exists exactly once (mirror the two movedMatches checks and their messages); (g) resolve with intent `{ kind: 'move-key-group', movedKeyIds: Object.freeze([...selectedKeyIds]), grabbedKeyId, target }`; (h) on resolution failure return ok:false with reason mapped by failure code — 'duplicate-destination-frame' -> 'Move rejected — key in the way', 'over-capacity' or 'out-of-range-frame' -> 'Move rejected — not enough room', anything else -> resolution.failure.text — plus `conflictingAppFrames: resolution.failure.conflictingAppFrames` and `detail: resolution.failure.text`; do NOT call publishStatus from prepare (during-gesture hovers re-run prepare; capsule publication on release-reject is 37-04's gesture-timing responsibility — documented contract); (i) no-change -> ok:false 'This move would not change the timeline.'; (j) build targetSignature + proposalVersion with the existing helpers and return the frozen publication including `movedKeyId: grabbedKeyId` and the same frozen movedKeyIds array passed to the resolver.

    5. commitRotoKeyGroupDrag implementation — mirror commitRotoKeyDrag (lines 426-441): return false when executePhysicalEdit is missing, when proposal.status.operationKind is not 'move-key-group', when drag metadata is absent or drag.movedKeyId !== publication.movedKeyId, when publication.movedKeyIds is absent or not shallow-equal to drag.movedKeyIds (length plus index-wise identity), or when either expectedLaunch member is empty; otherwise return input.executePhysicalEdit({ proposal: publication.proposal, expectedLaunch: publication.expectedLaunch, operationKind: 'move-key-group', selectedKeyId: publication.proposal.selectedKeyId, selectedAppFrame: publication.proposal.selectedAppFrame }). No resolver call, no cloning — the exact retained objects pass through (D-09). Success status ('Keys moved', 37-01 buildStatusText) flows through the existing accepted-output path exactly like single-key 'Key moved' does today; commit publishes nothing itself.

    6. Bundle useMemo: add both new actions and their dependencies.

    7. Studio wiring (lines 430-451): add `getSelectedKeyIds: () => selectedKeyIds.value,` beside the getSelectedKeyId line, and `publishDiagnostic: (message) => { console.error('[PhysicsPaintStudio] physical edit:', message); },` mirroring the line-426 diagnostic style. No other Studio change in this task.

    Trace-check the wiring by hand against GD-1: grabbed B with set {B,C}, target physical-cell 7 -> intent reaches the 37-01 resolver -> publication proposal carries the GD-1 map with selectedKeyId B -> commit executes kind 'move-key-group' unchanged. Record the derivation in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "getSelectedKeyIds" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "publishDiagnostic" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "prepareRotoKeyGroupDrag" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "commitRotoKeyGroupDrag" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "'move-key-group'" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "movedKeyIds" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "conflictingAppFrames" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "getSelectedKeyIds: () => selectedKeyIds.value" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -c "publishDiagnostic" app/src/components/physic-paint/PhysicsPaintStudio.tsx && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `RotoTimelineActionsInput` exposes optional `getSelectedKeyIds` and `publishDiagnostic`; the Studio composition wires both (selection signal read for the first; console diagnostic mirroring line 426 for the second).
    - `RotoDragPublication` exposes optional `movedKeyIds`; the failure branch of `RotoDragPreparationResult` exposes optional `conflictingAppFrames` and `detail`; single-key construction sites are untouched.
    - `prepareRotoKeyGroupDrag` guard order matches prepareRotoKeyDrag step-for-step plus the selection-set fail-closed validation, builds the move-key-group intent with a frozen copied set, maps the two collision/room failure codes to the exact UI-SPEC concise copy, and never calls publishStatus.
    - `commitRotoKeyGroupDrag` performs the four coherence checks (kind, grabbed match, moved-set shallow equality, launch tuple) and calls executePhysicalEdit with operationKind 'move-key-group' and the retained proposal object; `grep -c "operationKind: 'move-key'"` on the file is unchanged from its pre-task count (single-key path untouched).
    - Both actions appear in the bundle interface and the bundle useMemo.
    - `pnpm --dir app typecheck` exits 0.
    - GD-1 wiring derivation recorded in the SUMMARY.
  </acceptance_criteria>
  <done>One move-key-group publication flows end-to-end from bundle prepare through the frozen opaque publication into the acknowledged commit seam; preparation failures carry concise copy + structured conflicts + detail without gesture-time publishing; typecheck green.</done>
  <reversibility rating="reversible">Purely additive seam (new actions, optional type members, two input ports); nothing consumes them until 37-04 — trivially reverted.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Group-aware deleteRotoFrame (one shared transaction, D-13) + scope-aware applyForceSpacing (D-10..D-12)</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — PhysicalActionRunnerInput (175-180), DELETE_SUCCESS_MESSAGE constant (183), runPhysicalAction failure/success publish branches (269-284), deleteRotoFrame (297-305), applyForceSpacing (447-516) including the action-time snapshot comment (467-469) and the no-change publish (497-502).
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — proof of the shared delete route: keyboard dispatcher wiring `deleteRotoKey: rotoPhysicalActions.deleteRotoFrame` (line 830) and strip prop `onDeleteRotoFrame: rotoPhysicalActions.deleteRotoFrame` (line 1034) — both already reference the one bundle action; this task must not change either site.
    - .planning/phases/37-multi-select-physical-roto-keys/37-01-PLAN.md — delete-key-group intent shape ({ kind, keyIds }), 'unknown-operation-identity' idempotency guard, scoped force-spacing semantics and wire kind 'force-spacing' (research A2).
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — D-10..D-15 and GDel/GFS locked mappings.
  </read_first>
  <action>
    1. Widen PhysicalActionRunnerInput.operationKind to include `'delete-key-group'` (the wire/history allowlists already admit it per 37-01 Task 3).

    2. Add a `GROUP_DELETE_SUCCESS_MESSAGE = 'Keys deleted'` constant beside DELETE_SUCCESS_MESSAGE (UI-SPEC copy contract).

    3. deleteRotoFrame group branch: read `const selectedKeyIds = input.getSelectedKeyIds?.() ?? []` FIRST. When its length is >= 2, return runPhysicalAction({ intent: { kind: 'delete-key-group', keyIds: Object.freeze([...selectedKeyIds]) }, operationKind: 'delete-key-group', requiredKeyId: null, successMessage: GROUP_DELETE_SUCCESS_MESSAGE }) — requiredKeyId stays null because the 37-01 resolver is the membership authority ('unknown-operation-identity' rejects absent/unknown members fail-closed). When the length is < 2, execute the existing single-key body verbatim (ensureSelectedKeyId, delete-key intent, DELETE_SUCCESS_MESSAGE). D-13's shared-transaction requirement is satisfied structurally: the keyboard route (Studio line 830) and the toolbar icon route (Studio line 1034) already call this one action — do not touch either site.

    4. runPhysicalAction diagnostic leg: in the resolution-failure branch, when runnerInput.operationKind is 'delete-key-group', additionally call `input.publishDiagnostic?.('delete-key-group rejected: ' + resolution.failure.code + ' — ' + resolution.failure.text)` before returning false; single-kind branches publish exactly what they publish today (D-26: concise capsule via the existing publishStatus line, detail via the diagnostic port).

    5. applyForceSpacing scoped branch: keep every existing guard and the action-time snapshot comment verbatim. After the records snapshot, read `const selectedKeyIds = input.getSelectedKeyIds?.() ?? []` and compute `const scopeKeyIds = selectedKeyIds.length >= 2 ? Object.freeze([...selectedKeyIds]) : null` (D-10: exactly one key selected = full timeline; two or more = selected-only scope). Add `scopeKeyIds` to the intent object — the 37-01 resolver treats null/undefined as the full-timeline path, so the single-selection GFS-3 behavior is byte-identical. In the resolution-failure branch: when scopeKeyIds is non-null and the failure code is 'duplicate-destination-frame' or 'over-capacity', publish 'Spacing rejected — not enough room' via publishStatus (UI-SPEC copy) instead of the raw text; in all scoped failure cases also call publishDiagnostic with 'force-spacing rejected: ' + code + ' — ' + text; when scopeKeyIds is null the failure branch publishes exactly what it publishes today. The no-change branch (publishes proposal.status.text) is unchanged for both scopes — the control never becomes a silent no-op (D-10). The success branch (publishes proposal.status.text after acceptance) is unchanged — scoped success copy reuses the existing grammar per UI-SPEC.

    6. Do not touch duplicateKey, pasteKey, addEmptyKey, insertRotoFrame, or any availability computed in this task (D-16).

    Trace-check by hand: GDel-1 wiring — set {B,C} -> one delete-key-group intent -> one coordinator execution -> one accepted history entry, survivor D via the 37-01 proposal selectedKeyId. GFS-1 wiring — set {B,C}, N=2 -> scopeKeyIds {B,C} -> resolver anchors B. GFS-3 wiring — single selection -> scopeKeyIds null -> full-timeline intent identical to 36.14. Record all three in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "'delete-key-group'" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "scopeKeyIds" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "GROUP_DELETE_SUCCESS_MESSAGE" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "DELETE_SUCCESS_MESSAGE" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "rotoPhysicalActions.deleteRotoFrame" app/src/components/physic-paint/PhysicsPaintStudio.tsx && grep -c "publishDiagnostic" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'delete-key-group'"` on the hook file returns at least 3 (runner union, intent kind, operation kind); `grep -c "scopeKeyIds"` returns at least 2 (local + intent member).
    - deleteRotoFrame branches on the controller-supplied set size; the < 2 branch body is the pre-task single-key body verbatim; both Studio delete route references (lines 830 and 1034) are byte-untouched (`git diff` on PhysicsPaintStudio.tsx shows no change at those lines in this task).
    - runPhysicalAction's failure branch adds the publishDiagnostic call ONLY for the delete-key-group kind; every other kind's published output is unchanged.
    - applyForceSpacing: scoped failures publish the exact 'Spacing rejected — not enough room' copy for the wall/capacity codes plus diagnostic detail; null-scope behavior (guards, intent, messages, no-change and success publishes) is byte-identical to the pre-task body; the action-time snapshot comment survives verbatim.
    - `pnpm --dir app typecheck` exits 0.
    - GDel-1, GFS-1, and GFS-3 wiring derivations recorded in the SUMMARY match the locked mappings.
  </acceptance_criteria>
  <done>Every delete route (Backspace/Delete key, toolbar icon) executes one shared delete-key-group transaction when a multi-selection is active; Force Spacing scope follows selection size per D-10 with atomic reject copy and diagnostic detail; single-key behavior provably unchanged; typecheck green.</done>
  <reversibility rating="costly">Group delete and scoped spacing become user-visible behavior locked by native-UAT mappings GDel-1/GDel-2 and GFS-1..GFS-3 in 37-05; changing the semantics afterward means re-locking mappings and re-running native UAT.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Select All availability/guarded-reason derivation + 'All keys selected' status entry + plan-level D-18/D-19 gates (D-03, D-26)</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts — availability computed declarations (225-234), ActionAvailability interface (586-589), computeForceSpacingAvailability (666-676) whose guard chain shape the new computed mirrors, bundle interface availability member docs (96-104 as the JSDoc model), bundle useMemo (518-537).
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — selectAllRotoKeys callback created by 37-02 (tracer task; assigns selectedKeyIds/selectionAnchorKeyId from rotoKeyRecords), useRotoTimelineActions composition (430-451).
    - .planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md — Select All interaction contract (guarded presentation, verbatim controller reason, 'All keys selected' status entry; selection-only changes produce no status entry) and Pitfall 7 (icon placement is 37-04 scope, not this plan).
    - .planning/phases/37-multi-select-physical-roto-keys/37-02-PLAN.md — selectAllRotoKeys reducer contract (no-op when no real keys).
  </read_first>
  <action>
    1. Add a module-level `computeSelectAllKeysAvailability(input: RotoTimelineActionsInput): ActionAvailability` beside the other availability helpers, mirroring the computeForceSpacingAvailability guard chain shape: launch missing -> { eligible: false, reason: 'Select a Physics Paint Roto timeline before selecting keys.' }; pendingOperationId non-null -> { eligible: false, reason: 'A Roto physical edit is already in flight.' }; `(input.getRotoKeyRecords?.() ?? []).length === 0` -> { eligible: false, reason: 'No real Roto keys to select.' }; otherwise eligible. These strings are the verbatim controller reasons the 37-04 guarded icon renders through buildGuardedActionTooltipCopy (36.15 D-28 grammar: name + ' — unavailable: ' + reason). Availability deliberately stays eligible when every key is already selected — Select All is idempotent.

    2. Bundle interface + computed declarations: add `canSelectAllKeys: ReadonlySignal<boolean>` and `selectAllKeysDisabledReason: ReadonlySignal<string | null>` with JSDoc in the availability-member style, declared via computed(() => computeSelectAllKeysAvailability(input)) beside canApplyForceSpacing, and add both to the bundle useMemo with dependencies.

    3. Studio status entry: in the 37-02 selectAllRotoKeys callback, after the signals are assigned (and only on the path that actually selects — the no-real-keys no-op path publishes nothing), call `setApplyMessage('All keys selected')` (the same publisher the bundle's publishStatus uses, so capsule arbitration stays single-owner per 36.15 D-15). Both the Cmd/Ctrl+A route (37-02 dispatcher branch) and the future 37-04 icon route share this callback, so both produce exactly one feedback line per invocation. Do not add any status publication to the toggle/range/collapse paths — selection-only changes produce no status entry (UI-SPEC).

    4. Plan-level gates (run after tasks 1-3 edits are all in place):
       a. D-18: `git status --porcelain` shows no `.test.` path.
       b. D-19 added-line scan: `git diff -U0 -- app/src/components/physic-paint/hooks/useRotoTimelineActions.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx | grep -E '^\+' | grep -cE 'sourceFrame|displayFrame|inBetweenCount'` equals 0 (the pre-existing occurrences in this file's legacy interpolation adapter are untouched context lines, not additions).
       c. Transaction-authority scan: added lines contain no direct bridge import or send — `git diff -U0 -- app/src/components/physic-paint/hooks/useRotoTimelineActions.ts | grep -E '^\+' | grep -c 'physicPaintBridge'` equals 0.
       d. Full typecheck green with all edits in final state.
    5. Record in the SUMMARY: the four gate outputs verbatim, and confirmation that no strip/view file appears in `git status --porcelain` (view integration is 37-04).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "canSelectAllKeys" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "selectAllKeysDisabledReason" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "computeSelectAllKeysAvailability" app/src/components/physic-paint/hooks/useRotoTimelineActions.ts && grep -c "All keys selected" app/src/components/physic-paint/PhysicsPaintStudio.tsx && ! git status --porcelain | grep -q '\.test\.' && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `canSelectAllKeys` / `selectAllKeysDisabledReason` exist as computeds on the bundle, derive from launch/pending/record-count only, and carry the three exact verbatim reason strings; `grep -c "canSelectAllKeys"` on the hook file returns at least 3 (interface, declaration, memo).
    - The Studio selectAllRotoKeys callback publishes 'All keys selected' via setApplyMessage on the successful path only; no publish was added to toggle/range/collapse paths.
    - D-18 gate: `git status --porcelain` contains no `.test.` path.
    - D-19 gate: added-lines scan for legacy dual-frame identifiers on both touched files returns 0; transaction-authority scan returns 0.
    - No file under `view/` appears in `git status --porcelain`.
    - `pnpm --dir app typecheck` exits 0 with all plan edits final.
  </acceptance_criteria>
  <done>Select All availability and guarded reasons derive reactively on the bundle ready for the 37-04 icon; the Select All status entry routes through the shared callback; all plan-level D-18/D-19/authority gates pass; typecheck green.</done>
  <reversibility rating="reversible">Additive computeds plus one status-publish line in an existing callback; removing them restores prior behavior with no data or contract impact.</reversibility>
</task>

</tasks>

<!-- planner-discipline-allow: sourceFrame|displayFrame|inBetweenCount -->
<!-- planner-discipline-allow: physicPaintBridge -->
<!-- (D-19/authority literals appear only inside Task 3's gate commands and acceptance-criteria grep patterns, never as implementation guidance) -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View/gesture layer -> action bundle | Grabbed keyId, drag target, and the gesture-derived selection state are untrusted inputs entering the wiring layer. |
| Action bundle -> coordinator/bridge | The frozen publication crosses into the acknowledged transaction; integrity between prepare and commit is the safety invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03-01 | Tampering | Selection-set consumption in prepareRotoKeyGroupDrag / deleteRotoFrame / applyForceSpacing | medium | mitigate | Fail-closed set validation before resolution (size >= 2, bounded, unique, grabbed membership for drag) plus the 37-01 resolver's own membership authority ('unknown-operation-identity', 'duplicate-id'); the set is only ever read from the controller port, never derived from frames or DOM state. |
| T-37-03-02 | Tampering | commitRotoKeyGroupDrag publication integrity | high | mitigate | Task 1: four wrapper-coherence checks (operation kind, grabbed match, moved-set shallow equality, non-empty launch tuple) before executePhysicalEdit; the exact retained proposal object passes through with zero recomputation or cloning (D-09), so a stale or tampered publication returns false instead of committing an unseen mapping. |
| T-37-03-03 | Repudiation | Partial mutation or history skew on rejected group operations | high | mitigate | All group ops route through runPhysicalAction or the prepare/commit seam into the one generic coordinator — rejection happens before any coordinator execution (no proposal, no stage); acceptance flows through the 37-01-extended isOrdinaryOperationKind guard producing exactly one accepted-only history entry; Task 3 gate (c) statically proves no direct bridge send was added. |
| T-37-03-04 | Elevation of Privilege (UX integrity) | Select All fired while a physical edit is in flight | low | mitigate | computeSelectAllKeysAvailability mirrors the pending-operation guard of the other availability computeds; the 37-02 Cmd/Ctrl+A dispatcher branch additionally checks mutationLocked; the guarded icon (37-04) blocks activation before the controller call with the verbatim reason. |
| T-37-03-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (37-RESEARCH Package Legitimacy Audit: not applicable); this plan has no install tasks, so no slop-check checkpoint is required. |
</threat_model>

<verification>
Plan-level bounded static gates (D-18: no test creation, modification, deletion, renaming, or execution; no dev server, browser, or native process):

1. `pnpm --dir app typecheck` exits 0 with both files in final state.
2. Wiring presence: `prepareRotoKeyGroupDrag`, `commitRotoKeyGroupDrag`, `getSelectedKeyIds`, `publishDiagnostic`, `canSelectAllKeys`, `selectAllKeysDisabledReason`, `scopeKeyIds`, `'move-key-group'` (>= 3), `'delete-key-group'` (>= 3), `conflictingAppFrames` all present in useRotoTimelineActions.ts; `getSelectedKeyIds: () => selectedKeyIds.value`, `publishDiagnostic`, and `'All keys selected'` present in PhysicsPaintStudio.tsx.
3. Single-key preservation: `operationKind: 'move-key'` count unchanged from pre-plan; DELETE_SUCCESS_MESSAGE still referenced; Studio delete-route references (lines 830/1034) untouched.
4. D-18 gate: `git status --porcelain` shows no `.test.` path.
5. D-19 gate: added-lines scans show zero legacy dual-frame identifiers and zero bridge imports in the hook's added lines; no view-layer file modified.
6. Locked-mapping wiring evidence: GD-1, GDel-1, GFS-1, GFS-3 hand derivations recorded in the SUMMARY against the implemented wiring.
</verification>

<success_criteria>
- One move-key-group publication flows end-to-end from prepare through the frozen opaque publication into the acknowledged commit seam with zero recomputation (37-GROUP-DRAG, 37-ATOMIC-TRANSACTIONS).
- Backspace/Delete key and the toolbar Delete icon execute one shared delete-key-group transaction under multi-selection with 'Keys deleted' acceptance copy and exactly one history entry (37-GROUP-DELETE, D-13).
- Force Spacing applies selection-size-dependent scope with earliest-selected anchoring delegated to the resolver, atomic reject copy, and byte-identical single-selection behavior (37-GROUP-FORCE-SPACING, D-10..D-12, GFS-3).
- Select All availability and guarded reasons derive reactively; 'All keys selected' publishes once per successful invocation (37-SELECT-ALL, D-03).
- All rejections route concise copy to the capsule seam and full detail to the diagnostic port (D-26); typecheck green; zero test artifacts; D-19 prohibitions hold.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (for downstream plans 37-04..37-06)

New/modified symbols in app/src/components/physic-paint/hooks/useRotoTimelineActions.ts:
- `RotoTimelineActionsInput.getSelectedKeyIds?: () => readonly string[]` — controller-owned selection set read port (consumes 37-02's Studio signal).
- `RotoTimelineActionsInput.publishDiagnostic?: (message: string) => void` — D-26 detail leg (Studio wires the console diagnostic channel; retired-LOG-tab assumption flagged).
- `RotoDragPublication.movedKeyIds?: readonly string[]` — present + frozen on group publications; absent on single-key.
- `RotoDragPreparationResult` failure branch gains `conflictingAppFrames?: readonly number[]` and `detail?: string`.
- `prepareRotoKeyGroupDrag(grabbedKeyId: string, target: RotoDragTarget): RotoDragPreparationResult` — group drag preparation; failure results carry UI-SPEC concise copy ('Move rejected — key in the way' / 'Move rejected — not enough room'), structured conflicts, and full detail; publishes nothing during the gesture.
- `commitRotoKeyGroupDrag(publication: RotoDragPublication): Promise<boolean>` — coherence-checked opaque commit with operationKind 'move-key-group'.
- `deleteRotoFrame` — group-aware (>= 2 selected -> one delete-key-group transaction, 'Keys deleted'); single path unchanged.
- `applyForceSpacing` — scope-aware (>= 2 selected -> scopeKeyIds; else null = full timeline); scoped reject copy 'Spacing rejected — not enough room'.
- `GROUP_DELETE_SUCCESS_MESSAGE` constant; `PhysicalActionRunnerInput.operationKind` widened with 'delete-key-group'.
- `canSelectAllKeys: ReadonlySignal<boolean>` / `selectAllKeysDisabledReason: ReadonlySignal<string | null>` + `computeSelectAllKeysAvailability` (three verbatim guard reasons).

Modified in app/src/components/physic-paint/PhysicsPaintStudio.tsx:
- useRotoTimelineActions composition: `getSelectedKeyIds: () => selectedKeyIds.value` and `publishDiagnostic` wiring.
- `selectAllRotoKeys` callback publishes 'All keys selected' via setApplyMessage on success.

Consumed by:
- 37-04: strip group drag session calls prepare/commit pair; blocked-target preview consumes `conflictingAppFrames` + `detail`; release-reject publishes the returned reason (gesture-timing contract documented in Task 1); Select All guarded icon consumes `canSelectAllKeys`/`selectAllKeysDisabledReason` and calls the Studio `selectAllRotoKeys` callback.
- 37-05: native UAT anchors GD-1..GD-3 (drag wiring), GDel-1/GDel-2 (shared delete transaction), GFS-1..GFS-3 (scoped spacing), Select All availability + status entry.
- 37-06: post-UAT regression tests over preparation/commit wiring and availability derivation.
</artifacts_produced>

<output>
Create `.planning/phases/37-multi-select-physical-roto-keys/37-03-SUMMARY.md` when done, including: the GD-1 / GDel-1 / GFS-1 / GFS-3 wiring derivations, the four plan-level gate outputs (D-18, D-19 added-lines scan, transaction-authority scan, typecheck), confirmation that no view-layer or test file was touched, and the flagged retired-LOG-tab diagnostic-routing assumption for 37-05 UAT.
</output>
