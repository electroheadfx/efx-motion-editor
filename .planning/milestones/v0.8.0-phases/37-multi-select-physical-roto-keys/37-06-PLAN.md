---
phase: 37-multi-select-physical-roto-keys
plan: 06
type: tdd
wave: 5
depends_on: [37-05]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
autonomous: true
requirements:
  - 37-UAT-THEN-REGRESSION
  - 37-GROUP-DRAG
  - 37-GROUP-DELETE
  - 37-GROUP-FORCE-SPACING
  - 37-ATOMIC-TRANSACTIONS
  - 37-MULTI-SELECT-IDENTITY
tags:
  - physics-paint
  - roto
  - selection
  - group-operations
  - regression-tests
  - vitest

must_haves:
  truths:
    - "EXECUTION GATE (D-18, 37-UAT-THEN-REGRESSION): this plan runs ONLY after the user's explicit native-UAT approval recorded in `.planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md`; the executor asserts that file exists and records approval BEFORE any test file is created or any vitest command is run, and halts otherwise."
    - "GD-1/GD-2/GD-3 locked mappings are encoded as deterministic resolver assertions over baseline A@1,B@3,C@5,D@10 — `pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` exits 0 with the move-key-group describe block green (D-06..D-09; 37-GROUP-DRAG)."
    - "GDel-1/GDel-2 locked mappings are encoded as deterministic resolver assertions including delete-to-empty (empty mapping, selectedKeyId null) and the D-14 survivor rule — same vitest command exits 0 with the delete-key-group describe block green (D-13..D-15; 37-GROUP-DELETE)."
    - "GFS-1/GFS-2/GFS-3 locked mappings plus invalid-value rejections (negative, fractional) are encoded as deterministic resolver assertions — same vitest command exits 0 with the scoped force-spacing describe block green (D-10..D-12; 37-GROUP-FORCE-SPACING)."
    - "Atomicity is asserted at the level tests can prove: every rejected group intent (GD-2, GFS-2, unknown/duplicate/absent member) returns NO proposal, so zero mutation is possible (D-07, D-13; 37-ATOMIC-TRANSACTIONS); accepted group proposals carry operationKind 'move-key-group'/'delete-key-group' or 'force-spacing' matching their intent 1:1 (the same kind the 37-01-extended history ordinary guard records as exactly one accepted-only entry)."
    - "Selection-controller reducers are covered: select-all ordering + empty-set, toggle add/remove/no-duplicate/never-empty/current-transfer, range extension anchor semantics, collapse, unknown-keyId fail-closed, and the D-17 post-acceptance rules for 'move-key-group' / 'force-spacing' / 'delete-key-group' / all other kinds — `pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` exits 0 (D-01, D-02, D-05, D-17; 37-MULTI-SELECT-IDENTITY)."
    - "Group presentation view-model coverage: role 'moved' applies to EVERY member of drag.movedKeyIds (grabbed identified via movedKeyId), the single-key fallback path is unchanged, and getRotoCellSelectedTooltipCopy returns the exact UI-SPEC copy — `pnpm --dir app vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` exits 0 with the new describe blocks green (D-04, D-08, Pitfall 2; 37-GROUP-DRAG)."
    - "Final phase gate green in one pass: `pnpm --dir app vitest run` (full suite, 0 failed), `pnpm --dir app typecheck` (exit 0), and `pnpm --dir app build` (exit 0) — matching and not regressing the 36.14-30 green baseline of 717 passed / 0 failed."
  prohibitions:
    - "No test file is created, modified, deleted, renamed, or executed before the 37-05 explicit UAT approval is asserted (D-18); this plan is the first and only Phase-37 test activity."
    - "The resolver test file covers group intents and the locked GD/GDel/GFS mappings ONLY; 36.14's deferred single-key resolver coverage is a separate authorized follow-up and is NOT absorbed here (37-RESEARCH Wave 0 Gaps)."
    - "No production source file is modified by this plan; a failing test against UAT-approved production is a regression signal — the executor HALTS and reports to the user instead of patching production to make the test pass."
    - "No vitest watch mode, no --watch/--watchAll flags, no dev server, no browser, no native application launch; every test command is `vitest run` (CLAUDE.md, D-18)."
    - "No legacy dual-frame identifiers, compatibility fields, migration code, forwarding wrappers, aliases, or dual-write paths are introduced in any test file (D-19); test fixtures use stable keyId + direct appFrame identity only."
    - "No deferred ideas are encoded as tests: no group Duplicate, no group Copy/Paste, no Shift+Arrow keyboard range extension (CONTEXT.md Deferred Ideas)."
    - "No test-config change, no one-off vitest config, no test harness workaround (project memory: use the existing app/vitest.config.ts setup only)."
    - "Tests encode the UAT-APPROVED semantics of the two flagged probe assumptions (toggle-out-of-current transfer; shift-click current-transfer) exactly as recorded in 37-05-SUMMARY.md — if UAT changed them, the tests match the approved behavior, not the planner's original interpretation."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts (NEW)
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts (NEW)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts (EXTENDED — new describe blocks appended; existing blocks untouched)
  key_links:
    - "Test fixture builder -> resolvePhysicPaintRotoPhysicalEdit({ identities, intent, capacity, interpolationEnabled }) -> proposal.mapping / failure.code+conflictingAppFrames assertions (one shared baseline identity set A@1,B@3,C@5,D@10 across every locked-mapping test)."
    - "physicsPaintRotoMultiSelection.test.ts -> pure reducers exported from physicsPaintRotoMultiSelection.ts (37-02) -> RotoKeySelectionState assertions (keyId-only, never frame-derived)."
    - "Group presentation test -> real GD-1 proposal from the resolver -> getRotoDragPreviewViewModel moved-set roles (fixtures stay honest by construction; no hand-fabricated proposal literals for the moved-set test)."
---

<objective>
Lock the UAT-approved Phase 37 behavior with deterministic post-UAT regression coverage (D-18): a NEW resolver test file covering the group intents and locked GD/GDel/GFS mappings only, NEW selection-controller reducer tests (toggle/range/collapse/select-all, D-17 post-op rules), and EXTENDED group presentation view-model tests (moved-set roles, selected tooltip copy), then the full phase gate — full `vitest run` suite, typecheck, build.

Purpose: 37-01..37-04 shipped production code under bounded static checks only; 37-05 proved the behavior natively with the user. This plan converts the locked mappings GD-1..GD-3, GDel-1/GDel-2, GFS-1..GFS-3 (baseline A@1,B@3,C@5,D@10) and the selection/presentation contracts into executable regression anchors so future phases cannot silently drift from the approved semantics.

Output: two new test files + one extended test file, all green via `vitest run`; full suite + typecheck + build green in one final pass.

**D-18 gate (blocking):** this plan is POST-UAT ONLY. It executes strictly after the explicit user UAT approval recorded by plan 37-05. The executor asserts the approval before creating or running any test and halts if it is absent.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md
@.planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md
@.planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md
@.planning/phases/37-multi-select-physical-roto-keys/37-VALIDATION.md
@.planning/phases/37-multi-select-physical-roto-keys/37-01-PLAN.md
@.planning/phases/37-multi-select-physical-roto-keys/37-02-PLAN.md
@.planning/phases/37-multi-select-physical-roto-keys/37-03-PLAN.md
@.planning/phases/37-multi-select-physical-roto-keys/37-04-PLAN.md
</context>

<precondition>
Plan 37-05 closed with EXPLICIT user native-UAT approval: `.planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md` exists and records approval of the GD-1..GD-3, GDel-1/GDel-2, GFS-1..GFS-3 mappings, the 3 UI-SPEC backstops, and the two flagged probe assumptions (toggle-out-of-current transfer; shift-click current-transfer). Production symbols this plan tests exist: `grep -c "move-key-group" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` >= 4, `grep -cE "export (function|const) (selectAll|collapse|toggle|extend)" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` >= 4, `grep -c "resolvePostAcceptanceRotoSelection" app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts` >= 1, `grep -c "getRotoCellSelectedTooltipCopy" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` >= 1, `grep -c "movedKeyIds" app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` >= 1. Halt if unmet.
</precondition>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): NEW resolver test file — move-key-group section (GD-1/GD-2/GD-3) green end-to-end via vitest run (D-06..D-09; 37-GROUP-DRAG)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - .planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md — the UAT approval record (assert FIRST; halt if absent or not approved); final approved semantics of any flagged probe assumption.
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — the unit under test: exported `resolvePhysicPaintRotoPhysicalEdit` signature and input shape ({ identities, intent, capacity, interpolationEnabled }), the move-key-group intent variant ({ kind, movedKeyIds, grabbedKeyId, target }), PhysicPaintRotoPhysicalEditTarget ('physical-cell' / 'before-key' / 'after-key'), the proposal shape (mapping/assignments, selectedKeyId, selectedAppFrame, drag metadata movedKeyIds/grabbedKeyId, removedKeyIds, status.text), the failure shape (code, text, conflictingAppFrames), and the exact failure-code literals emitted by the group builders ('duplicate-destination-frame', 'over-capacity', 'out-of-range-frame', 'malformed-target', 'unknown-target-identity', 'moved-as-target', 'unknown-operation-identity', 'duplicate-id', 'malformed-identity', 'empty-key-set').
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — the identity-record shape the resolver's `identities` input expects (stable keyId + direct appFrame) and any exported record builder/parser usable for fixtures.
    - app/src/components/physic-paint/roto/rotoTimelineSelectors.test.ts — the same-directory style analog: import style (`import { describe, expect, it } from 'vitest'`), tiny fixture factory at the top, one `it` per behavior, `toEqual` on plain data.
    - .planning/phases/37-multi-select-physical-roto-keys/37-01-PLAN.md — artifacts_produced: exact group intent shapes, drag metadata, failure conflict fields, status copy ('Keys moved'), and the GD-1..GD-3 derivations these tests encode.
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — locked mappings GD-1..GD-3 (baseline A@1, B@3, C@5, D@10).
  </read_first>
  <behavior>
    - Test 1 (GD-1, accept empty whole-cell): baseline A@1,B@3,C@5,D@10; intent { kind: 'move-key-group', movedKeyIds: [B,C], grabbedKeyId: B, target: { kind: 'physical-cell', appFrame: 7 } } resolves ok; the complete final map is A@1, B@7, D@8, C@9 (source gaps close, D ripples 10 to 8); proposal.selectedKeyId is B; drag metadata movedKeyIds equals the frozen moved set and grabbedKeyId/movedKeyId is B; status text is 'Keys moved'.
    - Test 2 (GD-2, atomic reject): same selection dropped on frame 6 fails with code 'duplicate-destination-frame'; failure.conflictingAppFrames identifies the colliding frame (C landing on rippled D@8); resolution.ok is false and NO proposal exists — zero mutation possible (D-07).
    - Test 3 (GD-3, accept occupied before-caret): same selection, target { kind: 'before-key', keyId: D } resolves ok to A@1, B@10, D@11, C@12 (source gaps at 3 and 5 stay open; D ripples right at the destination boundary); proposal.selectedKeyId is B.
    - Test 4 (intent validation, fail-closed): a move-key-group intent whose movedKeyIds contains an absent/unknown keyId rejects with 'unknown-operation-identity'; a duplicate member rejects with 'duplicate-id'; a grabbedKeyId that is not a member of movedKeyIds rejects (the exact code is read from the production branch — assert that literal).
  </behavior>
  <action>
    D-18 assertion FIRST: read 37-05-SUMMARY.md and confirm explicit approval. Then create `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` following the rotoTimelineSelectors.test.ts style analog: vitest imports, a tiny identity-set fixture factory for the locked baseline (keyIds 'A','B','C','D' at appFrames 1, 3, 5, 10 — read the exact `identities` input shape from the production resolver/model before writing it), and a helper that builds the resolve input ({ identities, intent, capacity, interpolationEnabled }) with the production capacity constant.

    RED/GREEN note for this post-UAT plan: production is UAT-approved, so each test is expected to pass on first run. Write the full move-key-group describe block (Tests 1-4 above), then run the single-file vitest command. If ANY test fails, treat it as a regression signal against approved production: HALT and report to the user — do NOT edit production code to make a test pass (must_haves.prohibitions).

    Assertions compare plain data only: extract the final map from the proposal (keyId -> appFrame pairs) and `toEqual` it against the locked mapping; assert selectedKeyId, drag metadata, status text, failure code, and conflictingAppFrames explicitly. Scope is move-key-group ONLY in this task — GDel/GFS sections are Task 2, and 36.14's deferred single-key coverage stays out of scope entirely.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The vitest command exits 0; the output shows the new file's move-key-group tests passed with 0 failed (never watch mode).
    - The file contains exactly one describe block for move-key-group with one `it` per behavior (GD-1, GD-2, GD-3, intent validation) and the locked baseline fixture factory at the top.
    - GD-2 asserts `resolution.ok === false`, the exact failure code, and the conflicting frame in `conflictingAppFrames`; no assertion fabricates a proposal for the rejected case.
    - `git status --porcelain` shows ONLY the new resolver test file (no production file touched, no other test file touched).
  </acceptance_criteria>
  <done>The first resolver group-intent test section runs green end-to-end via `vitest run`; GD-1/GD-2/GD-3 and move-key-group intent validation are locked as executable regression anchors; zero production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolver test expansion — delete-key-group (GDel-1/GDel-2 + idempotency) and scoped force-spacing (GFS-1..GFS-3 + invalid values) (D-10..D-15; 37-GROUP-DELETE, 37-GROUP-FORCE-SPACING, 37-ATOMIC-TRANSACTIONS)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — as created in Task 1 (extend the same file; reuse its baseline fixture factory and resolve-input helper).
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — the delete-key-group intent variant ({ kind, keyIds }), the scoped force-spacing variant (scopeKeyIds?: readonly string[] | null), the proposal.removedKeyIds set, buildDeleteGroupCandidate's D-14 survivor rule, the scoped force-spacing branch (earliest-selected anchor, hard-wall rejection), the spacing-value validation order and its exact failure-code literal for negative/fractional emptyFrames, and the status copy ('Keys deleted').
    - .planning/phases/37-multi-select-physical-roto-keys/37-01-PLAN.md — Task 2 artifacts: removedKeyIds semantics (delete-key-group sets removedKeyId null and carries the full set), GDel/GFS hand derivations.
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — locked mappings GDel-1/GDel-2, GFS-1..GFS-3 and D-10..D-15.
  </read_first>
  <behavior>
    - Test 1 (GDel-1): intent { kind: 'delete-key-group', keyIds: [B,C] } over the baseline resolves ok to A@1, D@8 (D ripples 10 to 8); proposal.selectedKeyId is D (smallest-frame survivor after the group's last position, D-14); proposal.removedKeyIds contains exactly B and C; status text is 'Keys deleted'.
    - Test 2 (GDel-2, delete-to-empty): intent with all four keyIds resolves ok to an EMPTY final map with selectedKeyId null and selectedAppFrame null, passing finalizeProposal (D-15).
    - Test 3 (GDel idempotency, fail-closed): keyIds containing an absent/unknown member rejects with 'unknown-operation-identity' and no proposal; a duplicate member rejects with 'duplicate-id'.
    - Test 4 (GFS-1, scoped accept): intent { kind: 'force-spacing', emptyFrames: 2, selectedKeyId: B, scopeKeyIds: [B,C] } resolves ok to A@1, B@3, C@6, D@10 (B anchors at its current frame; D untouched).
    - Test 5 (GFS-2, scoped hard-wall reject): same scope with emptyFrames 6 fails with code 'duplicate-destination-frame' (C would land on D@10), conflictingAppFrames identifying frame 10, and no proposal (D-11).
    - Test 6 (GFS-3, null scope = full timeline): intent with emptyFrames 2 and scopeKeyIds null (and the undefined variant) resolves exactly like the 36.14 path to A@1, B@4, C@7, D@10; proposal.status.operationKind is 'force-spacing' (wire kind unchanged, research A2).
    - Test 7 (invalid values, fail-closed): emptyFrames -1 and emptyFrames 1.5 each reject with the exact spacing-validation failure code read from the production builder (assert the literal; no proposal) — for BOTH the null-scope and the scoped [B,C] variants.
  </behavior>
  <action>
    Append two describe blocks to the same resolver test file — `delete-key-group` (Tests 1-3) and `scoped force-spacing` (Tests 4-7) — reusing the Task-1 baseline fixture factory and resolve-input helper. Read the exact spacing-validation failure-code literal and the exact delete-key-group validation codes from the production resolver BEFORE writing the assertions; assert those literals verbatim (no string-parsing of failure text anywhere).

    For GDel-2, assert the proposal passes with an empty mapping: every keyId removed, selectedKeyId null — this is the executable anchor for research assumption A3's resolver half (the parent empty-records acceptance was UAT-proven in 37-05). For the atomicity truth (37-ATOMIC-TRANSACTIONS), every reject test asserts resolution.ok === false AND that no proposal field is reachable (zero mutation possible, D-07/D-13); every accept test asserts the proposal operationKind matches its intent kind 1:1.

    Run the single-file vitest command after each describe block lands. Any failure = HALT and report (approved production is not patched by this plan).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The vitest command exits 0 with the full file green: move-key-group, delete-key-group, and scoped force-spacing describe blocks, 0 failed.
    - GDel-2 asserts an empty final mapping with selectedKeyId null; GDel-1 asserts survivor D and removedKeyIds [B,C]; GFS-2 asserts the hard-wall failure code and conflictingAppFrames.
    - GFS-3 covers BOTH the null and the undefined scopeKeyIds variants against the 36.14 full-timeline result; invalid-value tests cover negative and fractional emptyFrames in both scopes with the exact production failure-code literal.
    - `git status --porcelain` shows only the resolver test file modified.
  </acceptance_criteria>
  <done>All eight locked mappings (GD-1..3, GDel-1/2, GFS-1..3) plus idempotency and invalid-value rejections are locked as executable anchors in one green resolver test file; atomic-reject (no proposal) is asserted for every failure case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Selection-reducer tests + group presentation view-model tests + final phase gate (vitest full, typecheck, build) (D-01, D-02, D-04, D-05, D-17; 37-MULTI-SELECT-IDENTITY, 37-UAT-THEN-REGRESSION)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</files>
  <read_first>
    - .planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md — the UAT-APPROVED semantics of the two flagged probe assumptions (toggle-out-of-the-current-key transfer; shift-click makes the clicked key current): the tests encode the approved behavior, whatever it is.
    - .planning/phases/37-multi-select-physical-roto-keys/37-02-SUMMARY.md — final exported symbol names from physicsPaintRotoMultiSelection.ts (discretion names: selectAllRotoKeyIds, collapseRotoKeySelection, toggleRotoKeySelection, extendRotoKeySelectionRange, resolvePostAcceptanceRotoSelection, RotoKeySelectionState).
    - app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.ts — the unit under test: exact exported reducer signatures, RotoKeySelectionState shape, the D-17 kind literals ('move-key-group', 'force-spacing', 'delete-key-group').
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts — getRotoDragPreviewViewModel (moved-set generalization over drag.movedKeyIds with the drag.movedKeyId fallback) and getRotoCellSelectedTooltipCopy (exact copy 'Selected key' / 'Selected key — {base}').
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts — the existing file being EXTENDED (235 lines; append new describe blocks after the existing ones; do not touch existing blocks).
    - .planning/phases/37-multi-select-physical-roto-keys/37-04-PLAN.md — artifacts_produced: presentation symbols and exact tooltip copy contract.
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — D-01, D-02, D-05, D-17.
  </read_first>
  <behavior>
    Selection reducers (NEW physicsPaintRotoMultiSelection.test.ts, baseline ordered ids [A,B,C,D] with current key B):
    - select-all returns every keyId in the given order with anchor = current key; empty input returns an empty selection and never fabricates an identity (D-03/D-05).
    - toggle adds an absent keyId keeping identity-order without duplicates; toggle removes a present keyId; removal that would empty the set keeps the current key (D-02 never-empty); removing the CURRENT key transfers current to the next selected key in identity order with previous-key fallback — assert the UAT-approved transfer semantics from 37-05.
    - range extension selects the contiguous run anchor-to-target inclusive over the ordered list, anchor unchanged across repeated extensions, and returns the target as the new current — assert the UAT-approved shift-click current-transfer semantics.
    - collapse returns exactly [currentKeyId] with anchor = currentKeyId; collapse with null current returns the empty state.
    - every reducer rejects an unknown keyId fail-closed (state unchanged).
    - D-17 aftermath: kind 'move-key-group' keeps the set and moves anchor/current to the accepted selectedKeyId (grabbed); kind 'force-spacing' keeps the set unchanged; kind 'delete-key-group' collapses to the accepted survivor; any other kind string collapses to the accepted selectedKeyId.
    Group presentation (EXTEND physicsPaintWorkflowPresentation.test.ts):
    - getRotoDragPreviewViewModel over a REAL GD-1 proposal (produced by calling resolvePhysicPaintRotoPhysicalEdit with the move-key-group intent — do not hand-fabricate the proposal literal) assigns role 'moved' to EVERY member of drag.movedKeyIds at its proposed appFrame and 'shifted' to the rippled unselected key (D), with grabbed identified via movedKeyId (Pitfall 2).
    - a single-key move proposal (movedKeyIds fallback path) assigns 'moved' exactly as before — existing single-key behavior unchanged.
    - getRotoCellSelectedTooltipCopy returns exactly 'Selected key' for base 'real-key' and the 'Selected key — {lowercased base copy}' composition for other bases (UI-SPEC copy contract, D-04).
  </behavior>
  <action>
    1. Create `app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` (same-directory style analog: rotoTimelineSelectors.test.ts): pure reducer assertions over plain keyId arrays — no Preact imports, no signals, no DOM. One describe per reducer plus one for the D-17 aftermath resolver; one `it` per behavior row above. Encode the UAT-APPROVED probe-assumption semantics exactly as recorded in 37-05-SUMMARY.md (if UAT upheld the planner interpretation, assert it as implemented; if UAT changed it, assert the approved behavior — the production reducers were already corrected pre-approval, so the tests pass either way; a failure here means the 37-05 record and production disagree — HALT and report).

    2. Append new describe blocks to `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` (existing blocks byte-untouched): the moved-set view-model tests build their proposal by calling the production resolver with the GD-1 move-key-group intent over the same baseline identity fixture (fixtures stay honest by construction); the tooltip-copy tests assert the exact strings. Import only exported production symbols.

    3. Final phase gate (one pass, from repo root, full output captured):
       a. `pnpm --dir app vitest run` — full suite, 0 failed. Compare against the 36.14-30 green baseline (717 passed / 0 failed): pass count must grow by exactly the number of tests this plan added; any failure in a file NOT created/touched by this plan is out-of-scope debt or a regression — HALT and report, do not fix.
       b. `pnpm --dir app typecheck` — exits 0.
       c. `pnpm --dir app build` — exits 0.
       All three must be green in the same final state; never run vitest without `run`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts && pnpm --dir app vitest run && pnpm --dir app typecheck && pnpm --dir app build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` exits 0 with 0 failed.
    - `pnpm --dir app vitest run` exits 0 with 0 failed across the full suite; the pass-count delta vs the 36.14-30 baseline equals exactly the tests added by this plan (recorded in the SUMMARY).
    - `pnpm --dir app typecheck` exits 0; `pnpm --dir app build` exits 0.
    - `git status --porcelain` shows exactly three test files (two new, one modified) and zero production files.
    - The moved-set view-model test's proposal comes from a real resolver call (grep the new describe block for `resolvePhysicPaintRotoPhysicalEdit` returns >= 1); the D-17 describe asserts all four kind rules including the collapse-default.
  </acceptance_criteria>
  <done>Selection reducers, D-17 aftermath rules, moved-set preview roles, and selected-tooltip copy are locked by green tests; the full suite, typecheck, and build are green in one final pass with zero production edits and zero out-of-scope test churn.</done>
</task>

</tasks>

<!-- planner-discipline-allow: watch -->
<!-- (the watch-mode literal appears only inside prohibition statements and gate wording, never as an instruction to run watch mode) -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> production resolver/reducers | Fixture identity sets and intents are inputs to the fail-closed production validators; tests must exercise (never bypass) those validators. |
| This plan -> UAT-approved production | The approved production source is a frozen authority; test code may observe it but never mutate it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-06-01 | Tampering | UAT-approved production source | high | mitigate | Hard prohibition + acceptance gate: `git status --porcelain` must show only the three test files; any failing test against approved production halts the plan for user decision instead of triggering a production edit (D-18 sequencing preserved). |
| T-37-06-02 | Repudiation | Locked-mapping drift | high | mitigate | Every GD/GDel/GFS mapping is asserted as exact keyId->appFrame equality plus failure-code literals read from production; vague or partial assertions are rejected at review (each `it` maps 1:1 to a locked mapping ID). |
| T-37-06-03 | Tampering | Test-fixture fabrication bypassing production validation | medium | mitigate | Presentation moved-set tests build proposals through a real `resolvePhysicPaintRotoPhysicalEdit` call rather than hand-written proposal literals; fixture factories use the production identity-record shape read from the model module. |
| T-37-06-04 | Denial of Service (developer workflow) | Watch-mode or dev-server launch during verification | low | mitigate | Every verify command is `vitest run`; watch-mode/dev-server launches are prohibited in must_haves.prohibitions and in CLAUDE.md. |
| T-37-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (test files import only vitest and existing production symbols); no install tasks, so no slop-check checkpoint is required. |
</threat_model>

<verification>
Plan-level gates (all post-UAT; the D-18 approval assertion precedes everything):

1. D-18 gate: 37-05-SUMMARY.md exists and records explicit user approval — asserted before any test file exists.
2. `pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` — exits 0; covers GD-1..3, GDel-1/2 (+ idempotency), GFS-1..3 (+ invalid values) only.
3. `pnpm --dir app vitest run src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` — exits 0; covers select-all/toggle/range/collapse/fail-closed/D-17.
4. `pnpm --dir app vitest run src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` — exits 0; existing blocks untouched, new group blocks green.
5. Full gate: `pnpm --dir app vitest run` (0 failed), `pnpm --dir app typecheck` (0), `pnpm --dir app build` (0) — one pass, output captured.
6. Scope gate: `git status --porcelain` shows exactly the three test files; zero production files; zero test-config files.
</verification>

<success_criteria>
- The locked mappings GD-1..GD-3, GDel-1/GDel-2, GFS-1..GFS-3 are executable regression anchors in a NEW resolver test file scoped to group intents only (37-GROUP-DRAG, 37-GROUP-DELETE, 37-GROUP-FORCE-SPACING).
- Atomic reject (no proposal on any failure) and 1:1 intent-kind/operation-kind correspondence are asserted for every group operation (37-ATOMIC-TRANSACTIONS at the provable level).
- Selection reducers and the D-17 post-acceptance rules are locked, encoding the UAT-approved probe-assumption semantics (37-MULTI-SELECT-IDENTITY).
- Group presentation moved-set roles and selected-tooltip copy are locked (37-GROUP-DRAG presentation half).
- Full suite + typecheck + build green in one pass (37-UAT-THEN-REGRESSION complete); zero production edits; zero watch-mode invocations.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

| Artifact | Kind | Location | Coverage |
|----------|------|----------|----------|
| Resolver group-intent regression tests | NEW test file | `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` | GD-1..GD-3, GDel-1/GDel-2, GFS-1..GFS-3, idempotency (unknown/duplicate member), invalid spacing values (negative/fractional, both scopes), atomic-reject no-proposal, drag metadata (movedKeyIds/grabbedKeyId), removedKeyIds, status copy |
| Selection-controller reducer tests | NEW test file | `app/src/components/physic-paint/roto/physicsPaintRotoMultiSelection.test.ts` | select-all ordering + empty, toggle add/remove/no-duplicate/never-empty/current-transfer, range anchor semantics, collapse, unknown-keyId fail-closed, D-17 four-rule aftermath |
| Group presentation view-model tests | EXTENDED test file | `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts` | moved-set roles over a real GD-1 resolver proposal, single-key fallback unchanged, exact selected-tooltip copy |

Consumed by: future phases touching the resolver, selection model, or presentation layer — these tests are the executable form of the 37-05 UAT approval.
</artifacts_produced>

<output>
Create `.planning/phases/37-multi-select-physical-roto-keys/37-06-SUMMARY.md` when done, recording: the D-18 approval assertion evidence (37-05 approval quote), the per-file test counts and the full-suite pass-count delta vs the 36.14-30 baseline (717 passed / 0 failed), the three final gate outputs verbatim (vitest full, typecheck, build), confirmation that zero production files and zero test-config files were touched, and any halted regression signal if one occurred (with its resolution).
</output>
