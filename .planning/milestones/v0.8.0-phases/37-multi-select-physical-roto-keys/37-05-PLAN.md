---
phase: 37-multi-select-physical-roto-keys
plan: 05
type: execute
wave: 4
depends_on: ["37-04"]
files_modified:
  - .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md
  - .planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md
autonomous: false
requirements:
  - 37-UAT-THEN-REGRESSION
  - 37-DOWNSTREAM-PARITY
  - 37-UI-INTEGRATION

must_haves:
  truths:
    - "Every locked deterministic mapping is exercised by the user natively with the exact expected map: GD-1 (A@1, B@7, D@8, C@9), GD-2 (atomic reject, zero mutation), GD-3 (A@1, B@10, D@11, C@12), GDel-1 (A@1, D@8 with survivor D current), GDel-2 (empty timeline), GFS-1 (A@1, B@3, C@6, D@10), GFS-2 (atomic reject with status reason), GFS-3 (A@1, B@4, C@7, D@10 — 36.14 single-key semantics unchanged) per D-01..D-15"
    - "Each group operation records exactly one Undo/Redo history entry and one Undo restores the exact baseline map A@1, B@3, C@5, D@10 (37-ATOMIC-TRANSACTIONS evidence gathered during UAT)"
    - "Downstream parity is user-verified after an accepted group map: save/close/reopen preserves the accepted physical map and stable keyId ownership; live pixels, caches, dirty state, playback, onion/reference, preview, export, missing/background rendering, and timeline extent all derive from the accepted map only (37-DOWNSTREAM-PARITY)"
    - "Single-key regression anchors hold: D-29 occupied before/after drag examples and the frame-6 source-closing drag, plus single-key Copy/Duplicate/Insert/Paste under multi-selection (D-16), all unchanged"
    - "Basic perfect-freehand layers and FX p5.brush layers behave exactly as before this phase (explicit user confirmation section)"
    - { statement: "Native visual UAT confirms blocked destination cells render the destructive-family dotted outline plus fade and the grabbed key shows the cannot-drop cursor before release, clearly distinct from all valid green target treatments.", verification: backstop }
    - { statement: "Native visual UAT confirms secondary selected keys are instantly distinguishable from the current key's orange ring and from drag-preview treatments at 18px cell size, across empty/cached/real semantic fills.", verification: backstop }
    - { statement: "Native visual UAT confirms the icon-only action row stays fit-content and non-wrapping in its 28px band with the Select All icon added, and the strip geometry (155px bands, 18px cells) is unchanged.", verification: backstop }
    - "The flagged planner assumptions receive an explicit user ruling during UAT: shift-click making the clicked key the current editing key (37-02/37-04), toggle-out-of-the-current-key transfer semantics (37-02), reject detail routing through the console diagnostic channel after the 36.15-11 LOG-tab retirement (37-03), and Select All icon placement at the end of the key-utilities pill after Delete (37-04 Pitfall 7)"
    - "Only the user's explicit approval marks the UAT passed; the agent records outcomes verbatim and never declares a pass on the user's behalf (37-UAT-THEN-REGRESSION, D-18)"
  artifacts:
    - ".planning/phases/37-multi-select-physical-roto-keys/37-UAT.md — the executable native UAT script (36.14-UAT.md format) with one numbered section per mapping/backstop/parity check and a result line recorded per section"
    - ".planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md — outcome record: per-section results, user rulings on the four flagged assumptions, approval or rejection verbatim, and the resulting 37-06 eligibility statement"
  key_links:
    - "37-UAT.md Section 1 (GD-1 happy path) is the tracer gate: it proves the end-to-end group-drag path (selection Signals -> group intent -> resolver complete mapping -> coordinator acknowledged transaction -> accepted-only history -> strip visuals) before any other section runs"
    - "Each 37-UI-SPEC backstop truth maps to exactly one UAT script section whose result the user records; a backstop cannot be waived by the agent"
    - "Explicit approval in 37-05 is the sole eligibility gate for plan 37-06 regression tests, typecheck, and build (D-18); rejection routes to gap-closure replanning, never to 37-06"
  prohibitions:
    - "The agent must never declare the UAT passed, self-approve, or paraphrase approval — only the user's explicit typed approval counts"
    - "Basic perfect-freehand and FX p5.brush behavior must remain unchanged; any observed change is a blocking UAT failure"
    - "No test file creation, modification, deletion, renaming, or execution in this plan (D-18); no vitest, typecheck, or build gates run here"
    - "No production source edits in this plan — this plan touches only .planning documentation artifacts"
    - "The agent never launches the dev server, browser, or native app; the user runs everything natively (CLAUDE.md)"
    - "No revival of historical Plans 36.14-13 through 36.14-18 and no sourceFrame/displayFrame compatibility, migration code, forwarding wrappers, aliases, dual-write paths, or additional timing/transaction authority (D-19)"
    - "Deferred ideas (group-aware Duplicate, group Copy/Paste, Shift+Arrow keyboard range extension) must not be tested as features; absence of them is correct behavior, not a defect"
---

<objective>
**As a** stop-motion animator, **I want to** select multiple real Physics Paint Roto keys — including Select All — and drag/drop, delete, and Force-Space them as one group, **so that** retiming and reorganizing many keys is fast and safe while the Phase 36.14 canonical physical-frame model and the Phase 36.15 final timeline UI remain the only authorities.

This plan is the **blocking native user-owned UAT checkpoint** for Phase 37 (D-18). Production shipped in plans 37-01..37-04 with bounded static checks only; this plan halts execution until the user natively verifies every locked deterministic mapping (GD-1..GD-3, GDel-1..GDel-2, GFS-1..GFS-3), the three 37-UI-SPEC backstops, the downstream parity checklist, and the four flagged planner assumptions — then explicitly approves or rejects.

Purpose: 37-UAT-THEN-REGRESSION requires native user approval BEFORE any regression test work (37-06); 37-DOWNSTREAM-PARITY and 37-UI-INTEGRATION are manual-only verifications (37-VALIDATION.md) anchored here.

Output: `37-UAT.md` (executable script + recorded results) and `37-05-SUMMARY.md` (approval/rejection record and 37-06 eligibility statement).
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md
@.planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md
@.planning/phases/37-multi-select-physical-roto-keys/37-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize the executable native UAT script (37-UAT.md) anchored on the locked mappings, backstops, parity checklist, and flagged assumptions</name>
  <files>.planning/phases/37-multi-select-physical-roto-keys/37-UAT.md</files>
  <read_first>
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — locked mappings GD-1..GD-3, GDel-1..GDel-2, GFS-1..GFS-3 (baseline A@1, B@3, C@5, D@10) and D-01..D-19 decisions; the script's expected maps are copied verbatim from here, never paraphrased
    - .planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md — the 3 backstop statements in `## UI Considerations` (blocked-target preview treatment, secondary-selected distinctness at 18px cells, fit-content non-wrapping action row in the 28px band), the Copywriting Contract capsule copy (`Move rejected — key in the way`, `Spacing rejected — not enough room`, `Keys moved`, `Keys deleted`, `All keys selected`, busy copy `Moving keys…` / `Deleting keys…`), and the empty-state copy (`No real Roto keys` / `Paint on a real frame or duplicate a key to begin timing.`)
    - .planning/phases/37-multi-select-physical-roto-keys/37-VALIDATION.md — Manual-Only Verifications table (downstream parity + UI integration are native-UAT-only)
    - .planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UAT.md — the format pattern: frontmatter (status, phase, source summaries, started/updated), `## Current Test`, `## Tests` with numbered `### N. Title` sections each carrying `expected:` and a recorded `result:`
    - .planning/phases/37-multi-select-physical-roto-keys/37-02-PLAN.md and 37-04-PLAN.md must_haves.assumptions — the flagged UNRESOLVED probe row (shift-click current-transfer; toggle-out-of-current transfer semantics) to convert into explicit UAT questions
    - .planning/phases/37-multi-select-physical-roto-keys/37-03-PLAN.md must_haves.assumptions — the retired-LOG-tab diagnostic-routing assumption to convert into an explicit UAT question
  </read_first>
  <action>
    Create 37-UAT.md in the 36.14-UAT.md format (frontmatter with status: in-progress, phase, source list 37-01..37-04 SUMMARYs, started timestamp; `## Current Test` set to section 1; `## Tests` with the numbered sections below). Every section gets an `expected:` block with the exact locked outcome and an empty `result:` line the user/executor fills during the run. Baseline setup section (Section 0) instructs the user: launch the native app themselves (agent never launches it), create a disposable test project, add one Physics Paint layer, open Roto mode, and paint four real keys with visually distinct payloads (e.g. four different colors) at physical frames 1, 3, 5, and 10 (identities A, B, C, D). Distinct payloads are required so identity/payload preservation is visible after every mapping.

    Script sections, in this exact order (Section 1 is the tracer — it runs first and alone before anything else):

    - S1 (TRACER) GD-1 happy path: Cmd/Ctrl-click B and C; grab B; drag onto empty frame 7; release. Expected final map exactly A@1, B@7, D@8, C@9 (source gaps closed: D rippled 10→8); payloads follow their keys; the moved group {B,C} stays selected with B as the current editing key (orange ring) and C secondary-selected (white-family outline); focus/scroll follow B; exactly one new Undo history entry; one Undo restores A@1, B@3, C@5, D@10 and one Redo re-applies A@1, B@7, D@8, C@9. Capsule shows `Keys moved`.
    - S2 GD-2 atomic reject with blocked preview: from baseline, select {B,C}, grab B, drag toward frame 6. During the gesture, BEFORE release, the conflicting destination cell shows the blocked-target treatment (destructive-family dotted outline plus fade) and the grabbed key shows the cannot-drop cursor — visually distinct from every valid green target treatment (BACKSTOP 1 statement recorded verbatim). On release: atomic reject, zero mutation (map still A@1, B@3, C@5, D@10), capsule shows `Move rejected — key in the way`, no new history entry.
    - S3 GD-3 occupied caret: from baseline, select {B,C}, grab B, release on D's before-caret. Expected exactly A@1, B@10, D@11, C@12; source gaps at frames 3 and 5 stay OPEN (empty cells remain, no left ripple); one Undo restores baseline.
    - S4 GDel-1: from baseline, select {B,C}, press the toolbar Delete icon → exactly A@1, D@8; survivor D becomes the single selected/current key; capsule `Keys deleted`; exactly one history entry; one Undo restores the full baseline. Repeat via the Backspace keyboard route from a fresh baseline — identical map, identical single history entry (shared transaction, D-13).
    - S5 GDel-2 delete-to-empty: from baseline, Select All via the action-row icon, then Delete → empty timeline; empty-state copy `No real Roto keys` with body `Paint on a real frame or duplicate a key to begin timing.`; editing context returns to the launch frame as a plain empty cell; one Undo restores A@1, B@3, C@5, D@10 completely.
    - S6 GFS-1 / GFS-2 / GFS-3 plus input validation: from baseline with {B,C} selected, N=2 Apply → exactly A@1, B@3, C@6, D@10 with the multi-selection preserved on B and C (D-17). From baseline with {B,C} selected, N=6 Apply → atomic reject, zero mutation, capsule `Spacing rejected — not enough room`, no history entry. With exactly one key selected, N=2 Apply → full-timeline result exactly A@1, B@4, C@7, D@10 (36.14 semantics unchanged, GFS-3). Negative, fractional, and nonnumeric N values are each rejected as no-ops (36.14 D-19/D-20 validation unchanged).
    - S7 Selection gestures and the four flagged-assumption questions: Cmd/Ctrl-click toggles keys in/out; toggling OUT the current editing key transfers the current highlight to the next selected key in frame order (fallback previous) and can never empty the selection — QUESTION Q1 asks the user to confirm or correct this transfer semantics (37-02 flagged assumption). Shift-click selects the contiguous real-key range from the anchor to the clicked key (generated/empty cells skipped) and makes the CLICKED key the current editing key — QUESTION Q2 asks the user to confirm or correct this (37-02/37-04 flagged assumption). Escape collapses a multi-selection to the current key (and still cancels an active drag first); plain click collapses to the clicked key; the selection is never empty. Select All fires from both the icon and Cmd/Ctrl+A with the timeline strip focused, showing `All keys selected`; Cmd/Ctrl+A inside a text input keeps native select-all-text behavior. QUESTION Q3 asks the user to confirm the Select All icon placement at the end of the key-utilities pill immediately after Delete (37-04 Pitfall 7). QUESTION Q4 asks the user to confirm reject detail routing through the console diagnostic channel is acceptable given the 36.15-11 LOG-tab retirement (37-03 flagged assumption). Also verified here: generated and empty cells ignore all selection gestures; grabbing an unselected real key collapses the selection and starts a normal single-key drag; Copy, Duplicate, Insert, and Paste keep exact single-key behavior targeting the current editing key under an active multi-selection (D-16); the `Key {n}` chip still identifies the current key.
    - S8 UI backstops 2 and 3: BACKSTOP 2 — with a multi-selection spanning cells over empty, cached, and real semantic fills, the secondary-selected treatment is instantly distinguishable from the current key's orange ring and from drag-preview treatments at 18px cell size (statement recorded verbatim from 37-UI-SPEC). BACKSTOP 3 — the icon-only action row with Select All added stays fit-content and non-wrapping in its 28px band and the strip geometry (155px bands, 18px cells) is unchanged (statement recorded verbatim from 37-UI-SPEC).
    - S9 Downstream parity (37-DOWNSTREAM-PARITY): after re-establishing an accepted group map (e.g. the GD-1 result A@1, B@7, D@8, C@9) — save the project, close, reopen: the accepted physical map and keyId ownership are preserved. Then verify each item derives from the accepted map only: live pixel caches sit at the new frames; project dirty state was set by the operation; cached playback sequences the real keys at their new positions with no empty trailing frames; onion/reference overlays show the correct neighboring frames; the parent EFX Motion preview matches at the new frames; an exported PNG sequence matches preview frame-for-frame; frames with no key still play transparent/background; the timeline extent reflects the new last key position. Also run one group drag with interpolation ENABLED and confirm generated cells re-derive strictly between adjacent real keys of the accepted map (36.14 UAT Test 4 regression class — must not recur).
    - S10 Non-regression sweep: single-key D-29 examples still produce exactly A@1,C@5,B@8,D@9 (before D) and A@1,C@5,D@8,B@9 (after D) from A@1,B@3,C@5,D@8, and the frame-6 whole-cell drag still produces A@1,C@4,B@6,D@8; single-key Copy/Duplicate/Paste/Insert unchanged; interpolation toggle still changes only enabled state; a Basic perfect-freehand paint layer and an FX p5.brush layer behave exactly as before (paint a few strokes in each and confirm no behavioral change). Any Basic/FX change is a BLOCKING failure.

    End the file with an approval block: a line for the user's verbatim approval or rejection, and a note that only the user's explicit typed approval passes the UAT and makes plan 37-06 eligible (D-18).
  </action>
  <verify>
    <automated>test -f .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md && grep -c "^### " .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 10' && grep -c "^result:" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 10' && grep -c "A@1, B@7, D@8, C@9" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "A@1, B@10, D@11, C@12" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "A@1, B@3, C@6, D@10" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "A@1, B@4, C@7, D@10" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "A@1, D@8" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "cannot-drop cursor before release" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "155px bands, 18px cells" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 1' && grep -c "QUESTION Q" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 == 4'</automated>
  </verify>
  <acceptance_criteria>
    - 37-UAT.md exists with at least 10 numbered test sections (S1..S10), each carrying an `expected:` block and an empty `result:` line
    - All eight locked mapping outcomes appear verbatim: GD-1 A@1, B@7, D@8, C@9; GD-3 A@1, B@10, D@11, C@12; GDel-1 A@1, D@8; GDel-2 empty timeline; GFS-1 A@1, B@3, C@6, D@10; GFS-3 A@1, B@4, C@7, D@10 (GD-2/GFS-2 are zero-mutation rejects against the baseline)
    - The three backstop statements appear verbatim from 37-UI-SPEC (blocked-target preview; secondary-selected distinctness at 18px; fit-content non-wrapping 28px action row with unchanged 155px/18px geometry)
    - Exactly four flagged-assumption questions (Q1 toggle-transfer, Q2 shift-click current-transfer, Q3 Select All placement, Q4 diagnostic routing) are present
    - The downstream parity section lists all of: save/reopen, live pixels, caches, dirty state, playback, onion/reference, preview, export, missing/background rendering, timeline extent
    - The Basic perfect-freehand and FX p5.brush non-regression check is present and marked blocking
  </acceptance_criteria>
  <done>37-UAT.md is a complete, executable native script in the 36.14-UAT.md format; every automated gate above passes; no production source file was touched.</done>
  <reversibility rating="reversible">Documentation artifact only; rewriting the script costs nothing and changes no production behavior.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (TRACER gate): User runs UAT Section 1 — the end-to-end group-drag happy path (GD-1) — before any other section</name>
  <files>.planning/phases/37-multi-select-physical-roto-keys/37-UAT.md</files>
  <read_first>
    - .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md (Task 1 output) — Section 0 setup and Section 1 steps
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — GD-1 locked mapping and D-06/D-17 selection aftermath (group stays selected, grabbed key current)
    - .planning/phases/37-multi-select-physical-roto-keys/37-04-SUMMARY.md (if present) — the implemented gesture wiring and any recorded deviations the user should know before running
  </read_first>
  <action>
    Hand the user the Section 0 setup and Section 1 script from 37-UAT.md and wait. This is the tracer: it is the thinnest path through every layer the phase shipped — selection Signals (37-02), group move intent and complete resolver mapping (37-01), action wiring and acknowledged transaction (37-03), and strip gestures/preview/visuals (37-04) — and it must pass before the remaining sections mean anything. The agent does not launch anything, does not observe on the user's behalf, and does not interpret results: the user runs natively and reports. On the user's report, record the verbatim outcome in the Section 1 `result:` line of 37-UAT.md (pass with observations, or issue with the user's verbatim description, severity, and evidence references, per the 36.14-UAT.md record pattern). If the user reports an issue, STOP: do not proceed to Task 3, do not attempt fixes in this plan — report the failure back for gap-closure replanning.
  </action>
  <what-built>Plans 37-01..37-04 shipped the complete group-drag path: multi-selection state and reducers at the Studio boundary, the `move-key-group` resolver intent with the GD-1 complete mapping, acknowledged coordinator transaction with one accepted history entry, and the strip modifier-click/group-drag/preview/secondary-selected visuals. Nothing in this plan builds code — this checkpoint verifies the shipped path end-to-end.</what-built>
  <how-to-verify>
    User, natively (you run the app; the agent never launches it):
    1. Section 0: create a disposable project, add one Physics Paint layer, open Roto mode, paint four visually distinct real keys at physical frames 1, 3, 5, 10 (A, B, C, D).
    2. Cmd/Ctrl-click B, then Cmd/Ctrl-click C — both show selected state; B keeps the strongest (orange) current highlight.
    3. Grab B and drag it onto empty frame 7; during the gesture the complete-mapping preview shows every moved/shifted/vacated/generated cell; release.
    4. Confirm the final map is EXACTLY A@1, B@7, D@8, C@9 — D rippled from 10 to 8 (source gaps closed), payloads traveled with their keys, the group {B,C} is still selected with B current (orange) and C secondary (white outline), focus/scroll followed B, and the capsule showed `Keys moved`.
    5. Press Undo once: the map returns to exactly A@1, B@3, C@5, D@10. Press Redo once: exactly A@1, B@7, D@8, C@9 again. Confirm the group drag consumed exactly ONE history entry.
    6. Report "gd1 pass" (with any observations) or describe the failure with a screenshot.
  </how-to-verify>
  <verify>
    <automated>grep -A3 "^### 1" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | grep -c "^result: pass" | awk '$1 == 1'</automated>
    <human-check>User executes steps 1-6 natively and explicitly reports the GD-1 outcome; the executor records it verbatim in 37-UAT.md.</human-check>
  </verify>
  <acceptance_criteria>
    - User explicitly confirms the final map A@1, B@7, D@8, C@9 with payloads preserved and source gaps closed
    - User explicitly confirms selection aftermath: group {B,C} still selected, B current with the orange ring, C secondary-selected, focus/scroll followed B
    - User explicitly confirms exactly one Undo/Redo entry and exact baseline restore on Undo
    - The Section 1 `result:` line in 37-UAT.md contains the user's outcome recorded verbatim; any issue stops the plan (no Task 3, no fixes here)
  </acceptance_criteria>
  <resume-signal>Type "gd1 pass" (optionally with observations) to continue to the full script, or describe the failure to halt for gap-closure replanning.</resume-signal>
  <done>The tracer gate is passed by the user's explicit report and the result is recorded; or the plan is halted with the user's failure report ready for gap-closure.</done>
  <reversibility rating="reversible">Recording a UAT section result is documentation-only; a failed tracer simply routes to replanning.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User runs the full remaining UAT script (S2..S10) and gives explicit approval or rejection; executor records everything verbatim</name>
  <files>.planning/phases/37-multi-select-physical-roto-keys/37-UAT.md, .planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md — Sections 2 through 10 and the approval block
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — D-07/D-08 (reject + blocked preview), D-09 (GD-3 source-gap policy), D-13..D-15 (group delete incl. delete-to-empty), D-10..D-12 (scoped Force Spacing), D-16 (single-key actions unchanged), D-18 (approval gates 37-06)
    - .planning/phases/37-multi-select-physical-roto-keys/37-UI-SPEC.md — the three backstop statements, capsule/tooltip copy contract, guarded Select All presentation
    - .planning/phases/37-multi-select-physical-roto-keys/37-VALIDATION.md — Manual-Only Verifications table (this task is its execution)
  </read_first>
  <action>
    Walk the user through Sections 2-10 of 37-UAT.md in order, one section at a time, waiting for the user's report after each. Record every section outcome verbatim in its `result:` line (pass / issue with severity and evidence, per the 36.14-UAT.md pattern). The four QUESTION items (Q1 toggle-out-of-current transfer semantics, Q2 shift-click current-transfer, Q3 Select All placement, Q4 diagnostic-channel routing) require an explicit confirm-or-correct ruling from the user — record each ruling verbatim next to the question. The three backstops (blocked-target preview in S2; secondary-selected distinctness in S8; action-row fit in S8) pass ONLY on the user's visual confirmation — the agent cannot waive or self-assess them. The downstream parity section (S9) requires the user to confirm each checklist item derives from the accepted map only, including save/close/reopen and one group drag with interpolation enabled. The non-regression sweep (S10) requires explicit user confirmation that single-key D-29 examples, single-key utilities, the interpolation toggle, and Basic perfect-freehand / FX p5.brush layers are all unchanged. If ANY section fails, stop at that section, record the failure, and halt — no fixes in this plan. If all sections pass, ask the user for the final explicit ruling. Only the user's typed approval passes the UAT; on approval, set 37-UAT.md frontmatter status to resolved and update `## Current Test` to `[testing complete]`, then write 37-05-SUMMARY.md containing: per-section results, the four question rulings verbatim, the user's approval/rejection verbatim, and the explicit statement of whether plan 37-06 (post-UAT regressions, typecheck, build) is now eligible per D-18.
  </action>
  <what-built>The complete Phase 37 production surface from plans 37-01..37-04: group drag with blocked-target preview, group delete (toolbar + keyboard, incl. Select All + Delete to empty), scoped Force Spacing with validation, the full selection gesture set, Select All icon + Cmd/Ctrl+A, secondary-selected and blocked-target visuals, and unchanged downstream/single-key/Basic/FX behavior.</what-built>
  <how-to-verify>
    User, natively, per the 37-UAT.md script (agent never launches the app):
    - S2 GD-2: blocked preview visible BEFORE release (dotted destructive outline + fade on the conflicting cell; cannot-drop cursor); release rejects atomically; map unchanged; capsule `Move rejected — key in the way`; no history entry.
    - S3 GD-3: release on D's before-caret yields exactly A@1, B@10, D@11, C@12 with source gaps at 3/5 left open; one Undo restores baseline.
    - S4 GDel-1: Delete via toolbar AND via Backspace each yield exactly A@1, D@8 with survivor D current and exactly one history entry per route.
    - S5 GDel-2: Select All + Delete empties the timeline with the documented empty-state copy; one Undo restores all four keys.
    - S6 GFS: {B,C} + N=2 → exactly A@1, B@3, C@6, D@10 with selection preserved; {B,C} + N=6 → atomic reject with `Spacing rejected — not enough room`; single key + N=2 → exactly A@1, B@4, C@7, D@10; negative/fractional/nonnumeric N are no-ops.
    - S7: gesture set works and the user rules on Q1-Q4 (confirm or correct each).
    - S8: user visually confirms backstop 2 (secondary-selected distinct at 18px across empty/cached/real fills) and backstop 3 (action row fit-content non-wrapping in 28px band; 155px/18px geometry unchanged).
    - S9: save/close/reopen preserves the accepted map and keyIds; live pixels, caches, dirty state, playback, onion/reference, preview, export, missing/background rendering, and timeline extent all derive from the accepted map only; one interpolation-ENABLED group drag re-derives generated cells strictly between adjacent real keys.
    - S10: single-key D-29 examples and frame-6 drag unchanged; single-key Copy/Duplicate/Paste/Insert unchanged; interpolation toggle enabled-state-only; Basic and FX layers unchanged (any change = blocking failure).
    Then the user types the final ruling: "approved" (UAT passed) or a rejection listing the failing sections.
  </how-to-verify>
  <verify>
    <automated>grep -c "^result:" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 >= 10' && grep -c "status: resolved" .planning/phases/37-multi-select-physical-roto-keys/37-UAT.md | awk '$1 == 1' && grep -c -i "approved" .planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md | awk '$1 >= 1' && test -z "$(git status --porcelain -- app packages)"</automated>
    <human-check>User executes S2..S10 natively, rules on Q1-Q4, visually confirms the three backstops, and types the final explicit approval or rejection.</human-check>
  </verify>
  <acceptance_criteria>
    - Every section S1..S10 in 37-UAT.md has a recorded `result:` line with the user's verbatim outcome
    - All four flagged-assumption questions carry an explicit user ruling (confirm or correct), recorded verbatim
    - All three 37-UI-SPEC backstops carry the user's explicit visual confirmation — none waived, none agent-assessed
    - The final ruling is the user's typed words, reproduced verbatim in both 37-UAT.md and 37-05-SUMMARY.md; the agent never paraphrases it into a pass
    - On approval: 37-05-SUMMARY.md explicitly states plan 37-06 is eligible per D-18; on rejection: the plan halts and the failure report is ready for gap-closure replanning — 37-06 is NOT eligible
    - No production source file (app/, packages/) was created or modified by this plan (git status gate)
  </acceptance_criteria>
  <resume-signal>Type "approved" only if every section passed and all four questions are ruled on; otherwise describe the failing section(s) to halt for gap-closure replanning.</resume-signal>
  <done>Native user-owned UAT is complete with every outcome recorded verbatim and an explicit user ruling on file; 37-06 eligibility is stated exactly per D-18.</done>
  <reversibility rating="costly">User approval locks GD-1..GD-3 / GDel-1..GDel-2 / GFS-1..GFS-3 and the four assumption rulings as the group-operation contract and unblocks 37-06 regression work; changing any semantic afterward means re-locking mappings and re-running native UAT.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed by this plan. It creates and updates planning documentation only; it runs no code, installs no packages, launches no processes, and touches no production surface (the UAT itself is executed natively by the user against the already-shipped 37-01..37-04 production build, whose threat registers live in those plans).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-05-01 | Tampering | UAT outcome record (37-UAT.md, 37-05-SUMMARY.md) | medium | mitigate | Only the user's explicit typed approval is transcribed verbatim into the record; the plan forbids agent self-approval, and the Task 3 automated gate cross-checks that a resolved status and approval text exist before 37-06 eligibility is stated |
| T-37-05-02 | Repudiation | 37-06 eligibility gate (D-18) | medium | mitigate | Eligibility is stated in the SUMMARY only alongside the verbatim user ruling; rejection leaves 37-06 ineligible and routes to gap-closure replanning |

No new attack surface: package legitimacy gate not applicable (zero installs); ASVS level 1 satisfied with the two documentation-integrity threats above; no high-severity items.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `.planning/phases/37-multi-select-physical-roto-keys/37-UAT.md` — the executable native UAT script (10 sections S1–S10, locked-mapping anchors GD-1..GD-3 / GDel-1..GDel-2 / GFS-1..GFS-3, 3 UI-SPEC backstops, downstream parity checklist, 4 flagged-assumption questions Q1–Q4). Consumer: the user's native UAT run (Tasks 2–3).
- `.planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md` — the UAT outcome record (per-section results, verbatim question rulings, verbatim final approval/rejection, 37-06 eligibility statement per D-18). Consumer: plan 37-06's eligibility gate (execution precondition).

No production symbols, no test artifacts, no new file paths outside `.planning/` — docs-only plan per D-18.
</artifacts_produced>

<verification>
- Task 1 automated gates: 37-UAT.md exists with >= 10 sections, >= 10 `result:` lines, all six non-baseline locked mapping outcomes verbatim, all three backstop statement fragments, and exactly four QUESTION items.
- Task 2 gate: Section 1 result recorded as pass only from the user's explicit report.
- Task 3 automated gates: >= 10 recorded results, `status: resolved` present, approval text present in 37-05-SUMMARY.md, and `git status --porcelain -- app packages` empty (docs-only plan).
- D-18 invariant: this plan runs zero vitest/typecheck/build gates and creates zero test files; those belong exclusively to 37-06 after approval.
</verification>

<success_criteria>
- The user natively executed the full UAT script: all eight locked mappings, both delete routes, all Force Spacing scopes and rejections, the complete selection gesture set, both Select All routes, all three UI backstops, the full downstream parity checklist, and the non-regression sweep.
- All four flagged planner assumptions (Q1-Q4) have explicit user rulings on record.
- The user's explicit approval or rejection is recorded verbatim; approval makes plan 37-06 eligible per D-18 and nothing else does.
- No production source changed; no test artifact created, modified, renamed, deleted, or executed.
</success_criteria>

<output>
Create `.planning/phases/37-multi-select-physical-roto-keys/37-05-SUMMARY.md` when done, recording: per-section UAT results, the four question rulings verbatim, the user's final approval/rejection verbatim, the 37-06 eligibility statement per D-18, and confirmation that no production file or test artifact was touched.
</output>
