---
phase: 37-multi-select-physical-roto-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/lib/physicPaintBridge.ts
autonomous: true
requirements:
  - 37-GROUP-DRAG
  - 37-GROUP-DELETE
  - 37-GROUP-FORCE-SPACING
  - 37-ATOMIC-TRANSACTIONS
must_haves:
  truths:
    - "GD-1 (D-06/D-09): move-key-group with selection {B,C}, grabbed B, dropped on empty physical frame 7 over baseline A@1,B@3,C@5,D@10 resolves through the existing finalizeProposal to the complete map A@1,B@7,D@8,C@9 with selectedKeyId = B; source gaps close (D ripples 10 to 8) and selected destinations are absolute original-plus-delta frames with no destination opening."
    - "GD-2 (D-07): the same group dropped on frame 6 fails closed with code 'duplicate-destination-frame', the failure carries conflictingAppFrames identifying the colliding frame, and no proposal exists (zero mutation possible)."
    - "GD-3 (D-09): the same group released on D's before-caret resolves to A@1,B@10,D@11,C@12; source gaps at 3 and 5 stay open, selected keys insert in ascending destination order, and destination openings ripple only unselected keys."
    - "GDel-1 (D-13/D-14): delete-key-group {B,C} resolves to A@1,D@8 with proposal selectedKeyId = D (smallest-frame survivor after the group's last position) and removedKeyIds carrying both removed identities."
    - "GDel-2 (D-15): delete-key-group over every real key resolves to an empty mapping with selectedKeyId null, passes finalizeProposal, and the parent bridge apply path accepts an empty records array through the same revision/launch/project/selection checks (research A3)."
    - "GFS-1 (D-12): force-spacing with scopeKeyIds {B,C} and emptyFrames 2 resolves to A@1,B@3,C@6,D@10; the earliest selected key anchors at its current frame and unselected keys keep frames."
    - "GFS-2 (D-11): force-spacing with scopeKeyIds {B,C} and emptyFrames 6 fails closed against hard-wall unselected D@10 with code 'duplicate-destination-frame' and no proposal."
    - "GFS-3 (D-10): force-spacing with scopeKeyIds null/undefined resolves exactly like the 36.14 full-timeline path (A@1,B@4,C@7,D@10 for N=2) and the wire operation kind stays 'force-spacing' (research A2)."
    - "The four kind allowlists admit 'move-key-group' and 'delete-key-group' in lockstep: resolver isResolverOperationKind, resolver operation-kind union, types/physicPaint.ts wire union plus isPhysicPaintRotoPhysicalEditOperationKind, and useRotoPhysicalEditHistory isOrdinaryOperationKind (37-ATOMIC-TRANSACTIONS: one accepted-only history entry per group op)."
    - "Every group candidate feeds the existing finalizeProposal; identity-set coverage, unique in-range final frames, deterministic ordering, strict-interior generated cells, selection, and status are proven there and not duplicated inside builders (D-19 single authority)."
    - "Group operations carry semanticDelta null and nextRecords null; the parent bridge treats them as ordinary mapping-only edits and registers them in acceptedPhysicalCommands so Undo/Redo replay works unchanged (D-13 exactly one Undo/Redo action)."
    - "Deleting a keyId set containing an already-absent or unknown keyId fails closed with 'unknown-operation-identity' and no proposal (idempotency probe for 37-GROUP-DELETE)."
    - "Resolver status text for the new kinds matches the UI-SPEC copy contract: 'Keys moved' for move-key-group and 'Keys deleted' for delete-key-group; scoped force-spacing reuses the existing 'Force Spacing applied' grammar."
  prohibitions:
    - "No parallel group resolver, second transaction or coordinator path, operation-specific preview resolver, or additional timing authority is introduced (D-19); group operations extend the one closed intent union only."
    - "No sourceFrame/displayFrame/inBetweenCount compatibility field, migration code, forwarding wrapper, alias, or dual-write path appears in any touched file's code (D-19)."
    - "The wire apply payload (PhysicPaintRotoPhysicalEditApplyPayload) and the persisted physical document gain no multi-selection fields; group intents exist only inside the resolver boundary and only the single selectedKeyId crosses the bridge (D-05, research Pattern 5)."
    - "No test file is created, modified, deleted, renamed, or executed in this plan (D-18); verification is bounded static checks plus scoped typecheck only."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
    - app/src/types/physicPaint.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/lib/physicPaintBridge.ts (verification-only expected; edit only if a gap is found)
  key_links:
    - "resolvePhysicPaintRotoPhysicalEdit intent branches -> buildMoveGroupCandidate/buildDeleteGroupCandidate/scoped buildForceSpacingCandidate -> finalizeProposal (the one finalizer proves every group proposal)."
    - "types/physicPaint.ts isPhysicPaintRotoPhysicalEditOperationKind -> physicPaintBridge.applyPhysicPaintRotoPhysicalMap generic ordinary path -> one store replacement + acceptedPhysicalCommands registration."
    - "useRotoPhysicalEditHistory.isOrdinaryOperationKind -> accepted-only effect -> exactly one history command per accepted group operation."
---

<objective>
Extend the Phase 36.14 canonical physical resolver with the three group operations locked in 37-CONTEXT: `move-key-group` (GD-1..GD-3 per D-06..D-09), `delete-key-group` (GDel-1/GDel-2 per D-13..D-15), and scoped `force-spacing` via `scopeKeyIds` (GFS-1..GFS-3 per D-10..D-12) — then admit the two new operation kinds through every allowlist seam (types wire validator, history ordinary-kind guard, parent bridge generic path) so group operations ride the existing single acknowledged `replace-roto-physical-map` transaction.

Purpose: The resolver is the sole behavior seam (36.14 D-09/D-22); preview and commit both consume its complete immutable proposals. Every group legality rule must live here so 37-03 (action wiring) and 37-04 (strip UI) never re-derive legality, and 37-ATOMIC-TRANSACTIONS holds by construction: one intent, one proposal, one coordinator execution, one parent acknowledgement, one accepted-only history entry.

Output: Extended resolver (intents, builders, failure conflicts, group drag metadata, removed-key set, status copy), extended wire/history allowlists, and bounded static evidence that the parent bridge accepts group kinds and the empty-records delete-to-empty map.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md
@.planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md
@.planning/phases/37-multi-select-physical-roto-keys/37-PATTERNS.md

@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/lib/physicPaintBridge.ts
</context>

<precondition>
Phase 36.14-30 closed with typecheck, vitest run, and build green; the working tree has no uncommitted edits in the four files this plan touches (verify with `git status --porcelain app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts app/src/types/physicPaint.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts app/src/lib/physicPaintBridge.ts` — empty output). Halt if unmet.
</precondition>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): `move-key-group` intent resolved end-to-end through finalizeProposal — GD-1/GD-2/GD-3 (D-06..D-09)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — the file being modified; anchor sections: intent union (lines 102-126), resolver operation-kind union (131-137), PhysicPaintRotoPhysicalEditTarget (88-91), PhysicPaintRotoPhysicalDragPresentation (194-199), proposal (221-248), failure type (278-282), isResolverOperationKind (554-561), fail() helper (563-572), ValidatedIdentities + validateIdentities (583-634), Candidate (640-657), buildMoveCandidate (859-933), removeMovedIdentityWithoutClosingSource (972-982), cutSource (989-1004), openAndInsert (1013-1053), computeChanged (1075-1083), finalizeProposal (1245-1387), buildStatusText (1389-1422), resolvePhysicPaintRotoPhysicalEdit move-key branch (1652-1670) and exhaustive fallthrough (1680-1682), module header prohibitions block (44-55).
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — locked mappings GD-1..GD-3 and decisions D-06..D-09 (baseline A@1,B@3,C@5,D@10).
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md — Architecture Pattern 1 (closed union extension) and Pattern 2 (group-move algorithm derivation); Pitfall 1 (structured conflict data) and Pitfall 2 (group drag metadata).
  </read_first>
  <action>
    All edits are inside physicsPaintRotoPhysicalResolver.ts; never fork the resolver (D-19).

    1. Header comment: in the "Prohibitions enforced" block replace the "- No multi-selection, group movement, occupied-key overwrite, or operation-specific preview resolver." line with a Phase-37 note stating group movement is now implemented via the move-key-group intent per Phase-37 D-06..D-09 / GD-1..GD-3, while occupied-key overwrite and operation-specific preview resolvers remain prohibited. Also update the stale "intentionally INACTIVE after Plan 02" paragraph framing only if it contradicts the new group scope; keep every other prohibition verbatim.

    2. Intent union: add variant `{ readonly kind: 'move-key-group'; readonly movedKeyIds: readonly string[]; readonly grabbedKeyId: string; readonly target: PhysicPaintRotoPhysicalEditTarget }` (D-06: grabbed key anchors the drop; every other selected key shifts by the same physical delta).

    3. Resolver operation-kind union (line 131) and isResolverOperationKind (line 554): add `'move-key-group'` to both.

    4. PhysicPaintRotoPhysicalDragPresentation: add `readonly movedKeyIds: readonly string[]` and `readonly grabbedKeyId: string`; keep `movedKeyId` (always equals the grabbed key) so single-key consumers are untouched (Pitfall 2). Update BOTH existing construction sites — buildMoveNoChangeCandidate (958-963) and openAndInsert (1046-1051) — to populate `movedKeyIds: [movedKeyId]` and `grabbedKeyId: movedKeyId` for the single-key path, freezing the array.

    5. PhysicPaintRotoPhysicalEditFailure: add optional `readonly conflictingAppFrames?: readonly number[]` (Pitfall 1: D-08 blocked-target preview needs cell-level data in 37-04). Extend the private `fail()` helper with an optional fourth parameter `conflictingAppFrames?: readonly number[]` that attaches a frozen sorted copy when present; every existing call site stays unchanged.

    6. New private builder `buildMoveGroupCandidate(identities: ValidatedIdentities, movedKeyIds: readonly string[], grabbedKeyId: string, target: PhysicPaintRotoPhysicalEditTarget, capacity: number): MoveBuilderResult` implementing the D-09 split by the grabbed key's target kind, derived exactly from GD-1..GD-3 (research Pattern 2):

       - `physical-cell` path (close source gaps): validate target frame is a nonnegative integer below capacity ('out-of-range-frame'); reject 'malformed-target' when the target cell is occupied in the ORIGINAL input by an UNSELECTED real key (mirror lines 882-889 but exclude every member of movedKeyIds — dropping onto another group member's cell is a legal group shift). Build postCut: each unselected survivor shifts left by the count of selected sources whose original frame is below its frame (GD-1: D@10 shifts left 2 to D@8; A@1 unchanged). `delta = target.appFrame - grabbedOriginalFrame`; each selected destination = original frame + delta (absolute; NO destination opening — this is what distinguishes the group path from single-key cutAndInsert). Reject 'over-capacity' when any selected destination is outside `0 .. capacity - 1`. Reject 'duplicate-destination-frame' with `conflictingAppFrames` = sorted colliding frames when any selected destination equals a postCut unselected frame (GD-2: C→8 collides with rippled D@8). Mapping = postCut + selected destinations; `changed` via computeChanged (no own-cell special-case branch: a delta-0 drop that closes source gaps is a real change; an exact identity map is a valid no-change proposal). Roles: 'moved' for selected keys whose frame changed, 'ripple-left' for shifted unselected survivors. Candidate: selectedKeyId = grabbedKeyId, operationKind 'move-key-group', drag metadata { targetKind, targetKeyId: null, resolvedInsertionAppFrame: target.appFrame, movedKeyId: grabbedKeyId, movedKeyIds (frozen), grabbedKeyId }.

       - `before-key`/`after-key` path (leave source gaps open): validate targetKeyId is bounded, exists ('unknown-target-identity'), and is NOT a member of movedKeyIds ('moved-as-target', generalizing line 907-912). postRemoval = selected identities removed, every survivor keeps its original frame (gaps stay open — generalize removeMovedIdentityWithoutClosingSource to a removal set). targetFrame = postRemoval.get(targetKeyId); insertionFrame = targetFrame (before) or targetFrame + 1 (after); reject 'over-capacity' when insertionFrame >= capacity. `delta = insertionFrame - grabbedOriginalFrame`; selected destinations = original + delta computed ONCE from original frames (GD-3: delta = +7 → B→10, C→12). Insert selected keys in ASCENDING destination order: for each destination, ripple every unselected key currently at/after that destination right by 1 (freeing the slot), then place the selected key; after each placement, if any unselected key's current frame equals a not-yet-placed selected destination, reject atomically 'duplicate-destination-frame' with conflictingAppFrames (D-09 ripple-conflict rule). Any ripple or destination reaching capacity rejects 'over-capacity'. Roles: 'moved' for selected, 'ripple-right' for shifted unselected. Candidate: selectedKeyId = grabbedKeyId; drag metadata carries targetKeyId and resolvedInsertionAppFrame = insertionFrame.

    7. resolvePhysicPaintRotoPhysicalEdit: add a `move-key-group` branch mirroring the move-key branch's guard order — intent.movedKeyIds must be a non-empty array of bounded unique keyIds all present in identities ('unknown-operation-identity'; duplicates reject 'duplicate-id'); intent.grabbedKeyId must be bounded, present, AND a member of movedKeyIds ('malformed-identity' otherwise); identities.ordered empty rejects 'empty-key-set'; target must be a discriminated record ('malformed-target'). Route to buildMoveGroupCandidate then finalizeProposal; failure branch returns without a proposal (D-07 zero partial mutation).

    8. buildStatusText: add a 'move-key-group' branch returning 'Keys moved' when changed (UI-SPEC copy contract) and 'No change' otherwise.

    Trace-check the implementation by hand against the locked mappings before verifying: GD-1 yields A@1,B@7,D@8,C@9 with B selected; GD-2 yields the conflict failure with no proposal; GD-3 yields A@1,B@10,D@11,C@12. Record the three derivations in the plan SUMMARY as static evidence.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "readonly kind: 'move-key-group'" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "buildMoveGroupCandidate" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "movedKeyIds: readonly string\[\]" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "grabbedKeyId: string" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "conflictingAppFrames" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "'Keys moved'" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `PhysicPaintRotoPhysicalEditIntent` contains the `move-key-group` variant with `movedKeyIds`, `grabbedKeyId`, and `target` members; the resolver operation-kind union and `isResolverOperationKind` both admit `'move-key-group'`.
    - `PhysicPaintRotoPhysicalDragPresentation` exposes `movedKeyIds` and `grabbedKeyId`; both single-key construction sites (buildMoveNoChangeCandidate, openAndInsert) populate them.
    - `PhysicPaintRotoPhysicalEditFailure` exposes optional `conflictingAppFrames`; `fail()` accepts it as an optional trailing parameter and no existing call site was edited to pass it.
    - `buildMoveGroupCandidate` implements the D-09 whole-cell/caret split: postCut left-shift by selected-source count, absolute original-plus-delta destinations with no destination opening on the whole-cell path, ascending-order per-insertion openings rippling only unselected keys on the caret path, atomic 'duplicate-destination-frame' rejects carrying conflictingAppFrames on both paths.
    - The `move-key-group` branch in resolvePhysicPaintRotoPhysicalEdit validates the moved set (non-empty, bounded, unique, existing) and grabbed membership before building.
    - `pnpm --dir app typecheck` exits 0.
    - GD-1/GD-2/GD-3 hand derivations recorded in the SUMMARY match the locked mappings exactly.
  </acceptance_criteria>
  <done>One `move-key-group` intent resolves end-to-end through the existing finalizeProposal producing the GD-1 complete map; GD-2/GD-3 semantics implemented with structured conflict data; typecheck green; no other file touched.</done>
</task>

<task type="auto">
  <name>Task 2: `delete-key-group` + scoped `force-spacing` (`scopeKeyIds`) — GDel-1/GDel-2, GFS-1..GFS-3 (D-10..D-15)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — anchor sections: intent union force-spacing variant (110-114), buildDeleteCandidate (707-748), buildForceSpacingCandidate (1094-1152), Candidate (640-657), proposal removedKeyId (239), finalizeProposal affectedKeyIds step 6 (1343-1355) and status step 7 (1357-1368), buildStatusText (1389-1422), resolver delete-key branch (1564-1578) and force-spacing branch (1672-1678); the Task-1 `move-key-group` additions just landed in this file.
    - .planning/phases/37-multi-select-physical-roto-keys/37-CONTEXT.md — D-10..D-15 and locked mappings GDel-1/GDel-2, GFS-1..GFS-3.
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md — Pattern 3 (group delete) and Pattern 4 (scoped force-spacing); Pitfall 3 (removed-key set and empty-mapping edge).
  </read_first>
  <action>
    All edits are inside physicsPaintRotoPhysicalResolver.ts.

    1. Removed-key set generalization (Pitfall 3): add `readonly removedKeyIds: readonly string[]` to the private Candidate interface AND to PhysicPaintRotoPhysicalEditProposal (frozen). Keep `removedKeyId: string | null` unchanged in meaning: the sole removed identity for `delete-key`, null for everything else (delete-key-group sets removedKeyId null and carries the full set in removedKeyIds). Update every Candidate construction site: buildInsertCandidate, buildDeleteCandidate (removedKeyIds: [selectedKeyId]), buildDuplicateCandidate, buildPasteCandidate, buildMoveNoChangeCandidate, openAndInsert, buildForceSpacingCandidate, buildMoveGroupCandidate — each adds the field (`[]` except single delete). finalizeProposal step 6: push EVERY member of candidate.removedKeyIds not already present into affectedList (replacing the single-ID push); step 7: pass candidate.removedKeyIds to buildStatusText; proposal carries the frozen set. GDel-1 anchor: affectedKeyIds must include both B and C plus rippled D.

    2. Intent union: add `{ readonly kind: 'delete-key-group'; readonly keyIds: readonly string[] }`. Add `readonly scopeKeyIds?: readonly string[] | null` to the existing force-spacing variant (null/undefined = full timeline per D-10).

    3. Resolver operation-kind union and isResolverOperationKind: add `'delete-key-group'`.

    4. New private builder `buildDeleteGroupCandidate(identities: ValidatedIdentities, keyIds: readonly string[]): Candidate` (research Pattern 3): removal set R; each unselected survivor shifts left by the count of removed keys whose original frame is below its frame (GDel-1: D@10 - 2 → D@8); roles 'ripple-left' for shifted survivors. Survivor selection per D-14: successor = smallest-frame unselected key with original frame strictly greater than max(removed frames); fallback previous = largest-frame unselected key with original frame strictly below min(removed frames); when nothing remains, selectedKeyId = null (GDel-2 delete-to-empty). Candidate: expectedKeyIds = identities minus R, removedKeyId: null, removedKeyIds = the frozen removal set, operationKind 'delete-key-group', changed: true.

    5. resolvePhysicPaintRotoPhysicalEdit `delete-key-group` branch: intent.keyIds must be a non-empty array of bounded unique keyIds all present in identities ('unknown-operation-identity' for absent/unknown — this is the idempotency guard; 'duplicate-id' for repeated members; 'malformed-identity' for unbounded); empty identities reject 'empty-key-set'. Route to the builder then finalizeProposal — the empty final mapping (GDel-2) must pass coverage/uniqueness and produce all-empty cells with selectedKeyId null and selectedAppFrame null.

    6. Scoped force-spacing inside buildForceSpacingCandidate (keep the existing full-timeline code path byte-identical when scopeKeyIds is null/undefined — GFS-3, research A2: wire kind stays 'force-spacing'). When scopeKeyIds is present: validate non-empty array of bounded unique keyIds all present in identities ('unknown-operation-identity'); order the selected subset by frame; anchor = earliest selected key's CURRENT frame; step = emptyFrames + 1; selected key at ordered index i maps to anchor + i * step; unselected keys keep their frames and are hard walls (D-11) — reject 'duplicate-destination-frame' with conflictingAppFrames when any computed selected destination equals an unselected key's frame (GFS-2: C→10 = D@10); over-capacity destinations are left for the common finalizer. selectedKeyId stays the intent's selectedKeyId (its identity is unchanged; its frame may move); roles 'reanchored' for selected keys that move; changed via computeChanged. Thread the intent's scopeKeyIds from the resolver's force-spacing branch (line 1672) into the builder signature.

    7. buildStatusText: add a 'delete-key-group' branch returning 'Keys deleted' (UI-SPEC copy contract); 'force-spacing' text unchanged.

    Trace-check by hand: GDel-1 → A@1,D@8 with D selected and removedKeyIds [B,C]; GDel-2 → empty mapping, selectedKeyId null; GFS-1 → A@1,B@3,C@6,D@10; GFS-2 → conflict failure, no proposal; GFS-3 → A@1,B@4,C@7,D@10. Record all five derivations in the plan SUMMARY as static evidence.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "readonly kind: 'delete-key-group'" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "scopeKeyIds" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "removedKeyIds: readonly string\[\]" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "buildDeleteGroupCandidate" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && grep -c "'Keys deleted'" app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `delete-key-group` intent variant exists with `keyIds`; force-spacing variant exposes optional `scopeKeyIds`; resolver operation-kind union and isResolverOperationKind admit `'delete-key-group'`.
    - Candidate and PhysicPaintRotoPhysicalEditProposal both expose `removedKeyIds`; every existing Candidate construction site compiles with the new field; `removedKeyId` semantics for single `delete-key` are unchanged.
    - finalizeProposal pushes every removed identity into affectedKeyIds and the proposal carries the frozen removed set.
    - buildDeleteGroupCandidate implements the count-based left ripple and the D-14 successor-then-previous survivor rule with a null survivor for delete-to-empty.
    - Scoped force-spacing anchors the earliest selected key, keeps unselected keys as hard walls with conflict rejection carrying conflictingAppFrames, and the null-scope path is the untouched 36.14 algorithm.
    - `pnpm --dir app typecheck` exits 0 (note: useRotoPhysicalEditHistory.ts buildReplayProposal will fail compilation here because the proposal gained a required member — Task 3 owns that fix; if the executor orders Task 3's history edit first, this task's typecheck is green directly; either sequencing is acceptable as long as the plan ends green).
    - GDel-1/GDel-2/GFS-1/GFS-2/GFS-3 hand derivations recorded in the SUMMARY match the locked mappings exactly.
  </acceptance_criteria>
  <done>Group delete and scoped force-spacing resolve through finalizeProposal per GDel/GFS locked mappings; removed-key set generalized across the proposal contract; status copy matches the UI-SPEC; plan-level typecheck is green after Task 3 completes the history stub.</done>
</task>

<task type="auto">
  <name>Task 3: Admit group kinds through the wire/history allowlists and prove the parent bridge generic path (37-ATOMIC-TRANSACTIONS, research A2/A3)</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts — the file being modified: PhysicPaintRotoPhysicalEditOperationKind wire union (50-61) with its doc comment (40-49), isPhysicPaintRotoPhysicalEditOperationKind allowlist (194-206), PhysicPaintRotoPhysicalEditApplyPayload (129-145), isPhysicPaintRotoPhysicalEditApplyPayload records check (295-306).
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts — the file being modified: header ordinary-kind comment (14-15), RotoPhysicalEditOrdinaryOperationKind (77-80), isOrdinaryOperationKind (124-133), buildReplayProposal proposal literal (201-232), accepted-only effect guard (290).
    - app/src/lib/physicPaintBridge.ts — verification target: apply-kind validation gate (118), applyPhysicPaintRotoPhysicalMap (553-700): kind special-case branches (555, 605-608, 652, 665-681), record parsing (586-594), selection null-consistency (595-603), acceptedPhysicalCommands registration condition (700).
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — parsePhysicPaintRotoRealKeyRecordCollection empty-array behavior (433-471).
    - .planning/phases/37-multi-select-physical-roto-keys/37-RESEARCH.md — Shared Pattern "Closed discriminated unions with 1:1 intent-kind/operation-kind growth" and assumption A3.
  </read_first>
  <action>
    1. types/physicPaint.ts: extend the PhysicPaintRotoPhysicalEditOperationKind wire union with `'move-key-group' | 'delete-key-group'` and add both literals to isPhysicPaintRotoPhysicalEditOperationKind — closed allowlist extension only, never loosen to a generic string (Security: forged-kind tampering, T-37-01). Update the union's doc comment with one sentence noting Phase 37 group kinds ride the same generic acknowledged envelope. Do NOT touch PhysicPaintRotoPhysicalEditApplyPayload, the semantic-delta union, or the document allowlist — group operations are mapping-only edits with semanticDelta null, and no multi-selection field may cross the bridge (D-05 prohibition).

    2. useRotoPhysicalEditHistory.ts: add `'move-key-group'` and `'delete-key-group'` to isOrdinaryOperationKind so accepted group operations record exactly one accepted-only snapshot command through the existing effect at line 290 (D-13; scoped force-spacing needs nothing — it keeps kind 'force-spacing'). Update the header comment at lines 14-15 to include the group kinds. Fix buildReplayProposal (line 210-231): add `removedKeyIds: []` (frozen empty array) to the proposal literal so the Task-2 proposal extension compiles. Do NOT hand-write history entries anywhere.

    3. physicPaintBridge.ts — verification-first, edit only if a gap is found (expected zero edits): confirm by source inspection that (a) the only kind-keyed branches are play-script (555), interpolation changes (605-606), duplicate/paste semantic validation (652), and replay (608, 665) — group kinds fall through to the generic ordinary path unchanged; (b) the ordinary-edit interpolation-preservation check (609-615) applies to group kinds exactly as it does to move-key; (c) an EMPTY payload.records array passes every gate: length vs capacity (583), parsePhysicPaintRotoRealKeyRecordCollection accepts an empty array (model 437-470: loop over zero entries returns a frozen empty array), selection null/null consistency (595), staged revision over empty records (651 — buildPhysicPaintRotoPhysicalRevision is already exercised with an empty array at bridge line 1052), staged document construction (684-693), and replaceRotoPhysicalDocument (694); (d) cursorAppFrame falls back to the clamped startFrame when selection is null (683) — the D-15 "editing context returns to the launch frame" behavior; (e) acceptedPhysicalCommands registration at line 700 includes group kinds (the !isReplay && !isInterpolationChange && !isPlayScript condition passes), preserving Undo/Redo replay. If any non-empty assumption IS found, make the minimal fail-closed fix inside applyPhysicPaintRotoPhysicalMap and document it prominently in the SUMMARY.

    4. Record in the SUMMARY: the exact bridge line evidence for (a)-(e), and confirmation that no `.test.` path appears in `git status --porcelain` output (D-18).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "'move-key-group'" app/src/types/physicPaint.ts && grep -c "'delete-key-group'" app/src/types/physicPaint.ts && grep -c "kind === 'move-key-group'" app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts && grep -c "kind === 'delete-key-group'" app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts && grep -c "removedKeyIds" app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts && ! git status --porcelain | grep -q '\.test\.' && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - types/physicPaint.ts wire union and isPhysicPaintRotoPhysicalEditOperationKind both contain exactly the two new literals; the apply payload, semantic-delta union, and document allowlists are untouched (grep confirms no `selectedKeyIds`/`selectionAnchor` member added to any wire or document type).
    - useRotoPhysicalEditHistory.ts isOrdinaryOperationKind admits both group kinds; buildReplayProposal carries `removedKeyIds: []`; no other history logic changed.
    - physicPaintBridge.ts group-kind flow evidence (a)-(e) is recorded in the SUMMARY with line references; any bridge edit, if required, is minimal, fail-closed, and documented.
    - `pnpm --dir app typecheck` exits 0 across the whole app with all four touched files in their final state.
    - `git status --porcelain` shows no `.test.` path (D-18 honored).
    - Comment-filtered scan of all four touched files shows zero code occurrences of forbidden compatibility identifiers: `grep -vE '^\s*(\*|//)' &lt;files&gt; | grep -c 'sourceFrame\|displayFrame\|inBetweenCount'` equals 0 (D-19).
  </acceptance_criteria>
  <done>All four kind allowlists admit the group kinds in lockstep; the parent bridge's generic ordinary path is statically proven to accept group operations and the empty delete-to-empty records array; typecheck green; D-18 and D-19 gates hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio child window → parent bridge | Untrusted apply payloads (operation kind, records, selection) cross the window boundary into the parent store; the parent revalidates everything before mutation. |
| View/action layer → pure resolver | Intent records (movedKeyIds, keyIds, scopeKeyIds, targets) are untrusted input to the fail-closed resolver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | `isPhysicPaintRotoPhysicalEditOperationKind` (types/physicPaint.ts:194) | high | mitigate | Task 3 extends the closed literal allowlist with exactly `'move-key-group'`/`'delete-key-group'` in lockstep with the wire union; the parent revalidates kind plus complete records inside `applyPhysicPaintRotoPhysicalMap` before one store replacement; never loosen to a generic string check. |
| T-37-02 | Tampering | Resolver intent validation for `movedKeyIds`/`keyIds`/`scopeKeyIds` | medium | mitigate | Tasks 1-2 fail closed before any candidate exists: non-empty arrays, bounded unique keyIds, all present in identities, grabbed-key membership; unknown members reject with 'unknown-operation-identity', duplicates with 'duplicate-id'. |
| T-37-03 | Repudiation | Partial mutation or history skew on rejected group operations | high | mitigate | Tasks 1-2: builders return no proposal on any collision (atomic reject, D-07); the existing finalizeProposal proves coverage/uniqueness; the unchanged coordinator snapshot/settlement seam rolls back on rejection/timeout; Task 3's history guard extension keeps exactly one accepted-only entry per group op. |
| T-37-04 | Tampering | Delete-to-empty (GDel-2) bypassing parent validation via an empty records array | medium | mitigate | Task 3 statically proves the empty array flows through the SAME revision, launch, project-context, capacity, and selection null-consistency gates as every ordinary edit; any discovered non-empty assumption is fixed fail-closed and documented. |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages (37-RESEARCH Package Legitimacy Audit: not applicable); this plan has no install tasks, so no slop-check checkpoint is required. |
</threat_model>

<verification>
Plan-level bounded static gates (D-18: no test creation, modification, deletion, renaming, or execution; no dev server, browser, or native process):

1. `pnpm --dir app typecheck` exits 0 with all four files in final state.
2. Lockstep allowlist proof: `'move-key-group'` and `'delete-key-group'` present in the resolver operation-kind union, `isResolverOperationKind`, the types wire union, `isPhysicPaintRotoPhysicalEditOperationKind`, and `isOrdinaryOperationKind`.
3. Locked-mapping evidence: GD-1, GD-2, GD-3, GDel-1, GDel-2, GFS-1, GFS-2, GFS-3 hand derivations recorded in the SUMMARY against the implemented builders.
4. D-19 gate: comment-filtered scan of touched files shows zero code occurrences of `sourceFrame`/`displayFrame`/`inBetweenCount`; no new resolver/transaction authority introduced.
5. D-18 gate: `git status --porcelain` shows no `.test.` path.
6. Bridge evidence: parent generic ordinary path accepts group kinds and the empty records array (research A2/A3), recorded with line references.
</verification>

<success_criteria>
- `move-key-group`, `delete-key-group`, and scoped `force-spacing` (`scopeKeyIds`) intents resolve through the existing finalizeProposal implementing GD-1..GD-3, GDel-1..GDel-2, GFS-1..GFS-3 exactly.
- `PhysicPaintRotoPhysicalEditFailure` carries optional structured `conflictingAppFrames`; drag metadata carries `movedKeyIds` + `grabbedKeyId`; the proposal carries the `removedKeyIds` set — all ready for 37-03 wiring and 37-04 presentation.
- The two new operation kinds pass through the wire validator, the parent bridge generic ordinary path, and the history ordinary-kind guard with no structural change to coordinator or bridge (37-ATOMIC-TRANSACTIONS).
- Scoped force-spacing keeps wire kind `force-spacing`; the full-timeline path is byte-identical (GFS-3).
- Typecheck green; zero test files touched; D-19 prohibitions hold.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (for downstream plans 37-02..37-06)

New resolver symbols (app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts):
- Intent variants: `{ kind: 'move-key-group'; movedKeyIds: readonly string[]; grabbedKeyId: string; target: PhysicPaintRotoPhysicalEditTarget }` and `{ kind: 'delete-key-group'; keyIds: readonly string[] }`; `force-spacing` gains `scopeKeyIds?: readonly string[] | null`.
- Operation kinds `'move-key-group'` and `'delete-key-group'` added to the resolver union + `isResolverOperationKind`.
- Private builders `buildMoveGroupCandidate` and `buildDeleteGroupCandidate`; scoped branch inside `buildForceSpacingCandidate`.
- `PhysicPaintRotoPhysicalEditFailure.conflictingAppFrames?: readonly number[]` (+ optional 4th `fail()` parameter).
- `PhysicPaintRotoPhysicalDragPresentation.movedKeyIds: readonly string[]` and `.grabbedKeyId: string` (single-key sites populate both; `movedKeyId` retained = grabbed).
- `PhysicPaintRotoPhysicalEditProposal.removedKeyIds: readonly string[]` (and the private Candidate equivalent).
- Status copy: `'Keys moved'` (move-key-group), `'Keys deleted'` (delete-key-group).

Modified allowlists / stubs:
- app/src/types/physicPaint.ts — wire union + `isPhysicPaintRotoPhysicalEditOperationKind` (+2 literals).
- app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts — `isOrdinaryOperationKind` (+2 kinds); `buildReplayProposal` gains `removedKeyIds: []`.
- app/src/lib/physicPaintBridge.ts — verification-only expected (zero edits unless a non-empty-records assumption is found and fixed fail-closed).

Consumed by: 37-03 (`prepareRotoKeyGroupDrag`/`commitRotoKeyGroupDrag` intents, group delete routing, scoped `applyForceSpacing`, blocked-preview conflict passthrough), 37-04 (blocked-target preview from `conflictingAppFrames`, group preview moved-set roles from `movedKeyIds`/`grabbedKeyId`), 37-06 (post-UAT resolver regression tests over the locked mappings).
</artifacts_produced>

<output>
Create `.planning/phases/37-multi-select-physical-roto-keys/37-01-SUMMARY.md` when done, including: the eight locked-mapping hand derivations (GD-1..3, GDel-1..2, GFS-1..3), the bridge line-referenced evidence for the generic ordinary path and empty-records acceptance, the D-18/D-19 gate outputs, and any bridge edit if one was required.
</output>
