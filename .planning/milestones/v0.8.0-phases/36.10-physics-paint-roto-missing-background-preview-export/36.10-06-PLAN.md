---
phase: 36.10-physics-paint-roto-missing-background-preview-export
plan: 06
type: execute
wave: 6
depends_on:
  - 36.10-05
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/lib/previewRenderer.ts
  - app/src/lib/previewRenderer.test.ts
  - app/src/lib/exportRenderer.test.ts
autonomous: true
gap_closure: true
requirements:
  - 36.10-MISSING-TRANSPARENT
  - 36.10-MISSING-BACKGROUND
  - 36.10-PREVIEW-EXPORT-PARITY
must_haves:
  truths:
    - "D-02/D-14/D-15: Reopened Roto launches preserve selected paper/background metadata while keeping real-key paint PNGs alpha-only."
    - "D-11/D-13: Preview and export composite the same Roto real-key visual result: saved paper/background behind transparent real-key strokes, without stale/live Physics Paint layer artifacts."
    - "D-14/D-15: Save current and save-on-leave reuse one active-frame transparent stroke capture instead of duplicating expensive engine background/load/export passes."
  artifacts:
    - app/src/types/physicPaint.ts exposes and validates optional PhysicPaintLaunchContext.rotoBackground.
    - app/src/lib/physicPaintBridge.ts includes stored Roto background metadata in standalone launch contexts.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx restores Roto background settings on launch and reuses one active-frame capture for save output/onion payloads.
    - app/src/lib/previewRenderer.ts composites real-key Roto paper/background behind transparent strokes for preview/export parity.
    - Focused Vitest coverage protects reopen metadata, real-key paper compositing, export delegation, and no duplicate Roto save render contract.
  key_links:
    - physicPaintStore.getRotoBackgroundMetadata(layerId) -> createPhysicPaintLaunchContext -> PhysicPaintLaunchContext.rotoBackground -> PhysicsPaintStudio restored settings/engine background.
    - PreviewRenderer Roto branch -> draw background for real-key paper metadata -> draw saved transparent stroke PNG -> exportRenderer delegation.
    - saveRotoFrame/requestRotoFrameNavigation -> snapshotCurrentRotoFrame -> flushRotoFrame -> single active-frame capture reused for renderedFrame and onionDataUrl.
---

<objective>
Close the three diagnosed Phase 36.10 UAT gaps only: reopened Roto launches must restore paper/background metadata, preview/export must composite real-key Roto paper backgrounds cleanly without an extra live Physics Paint layer artifact, and Roto save-current/save-on-leave must avoid duplicate expensive stroke renders.

Purpose: Phase 36.10 cannot be considered live-UAT ready while saved paper backgrounds disappear after reopen, exported videos show incorrect Physics Paint compositing, or save-on-leave visibly blocks the UI by re-rendering the same active frame multiple times.
Output: A tight gap-closure implementation with focused regression coverage and no new feature scope beyond the three diagnosed UAT gaps.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-CONTEXT.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-RESEARCH.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-PATTERNS.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-UAT.md
@.planning/debug/phase-3610-uat-roto-paper.md
@.planning/debug/phase-36-10-uat-gap.md
@.planning/debug/phase-3610-uat-save-slowdown.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-01-SUMMARY.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-02-SUMMARY.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-03-SUMMARY.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-04-SUMMARY.md
@.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-05-SUMMARY.md
@app/src/types/physicPaint.ts
@app/src/lib/physicPaintBridge.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/lib/previewRenderer.ts
@app/src/lib/exportRenderer.ts
@app/src/stores/physicPaintStore.ts
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/lib/previewRenderer.test.ts
@app/src/lib/exportRenderer.test.ts
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Phase 36.10 missing Roto frames preview/export consistently without accidental paint content | Task 2 preserves preview/export parity while fixing real-key paper compositing. |
| REQ 36.10-MISSING-TRANSPARENT | Transparent missing frames stay transparent and no stale paint appears | Task 2 keeps missing-frame resolver semantics unchanged and adds regression coverage around real-key compositing only. |
| REQ 36.10-MISSING-BACKGROUND | Background-only/paper metadata drives paper background rendering | Task 1 restores persisted metadata into standalone Roto; Task 2 uses the same metadata for real-key preview/export. |
| REQ 36.10-PREVIEW-EXPORT-PARITY | Preview and export share visual rules | Task 2 keeps export delegated through PreviewRenderer and verifies no duplicate export-specific Roto logic. |
| CONTEXT D-02 | Use saved Roto paper/background metadata first, layer fallback otherwise, transparent if none | Tasks 1 and 2 cite and implement saved metadata first. |
| CONTEXT D-11 | Existing real cached frames continue rendering normally; playback remains real-key-only | Task 2 composites behind real-key strokes without changing playback sequencing or missing support materialization. |
| CONTEXT D-13 | Preview/export must produce the same visual rule | Task 2 verifies PreviewRenderer-owned behavior and export delegation. |
| CONTEXT D-14 | Background rendering must not mutate/bake paper into real-key alpha caches | Tasks 2 and 3 keep paper separate from transparent stroke PNGs and reuse alpha-only captures. |
| CONTEXT D-15 | Paper/background stays separate from paint alpha cache for Phase 36.11 repaint | Tasks 1, 2, and 3 preserve metadata separate from cached stroke pixels. |
| RESEARCH | Store owns bounded support; renderer owns dynamic drawing | Task 2 keeps dynamic real-key background compositing in PreviewRenderer. |
| RESEARCH | Export should not import physics engine or duplicate Roto replay logic | Task 2 adds source-contract coverage against export-side replay/duplication. |
| RESEARCH | No new package dependencies | All tasks modify existing TypeScript/tests only. |
</source_audit>

<dependency_graph>
## Execution Waves

This is a single gap-closure plan in Wave 6 after Phase 36.10 plans 01-05. Tasks run sequentially within the plan because they share Roto launch/render/save files and build on each other:

1. Task 1 restores paper metadata into reopened standalone Roto launch contexts.
2. Task 2 consumes the restored/persisted metadata for real-key preview/export compositing.
3. Task 3 optimizes save-current/save-on-leave capture reuse without changing the Task 1/2 metadata and compositing contracts.
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore persisted Roto paper metadata on standalone reopen</name>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <behavior>
    - Test 1: a PhysicPaintLaunchContext containing rotoBackground passes validation when the value satisfies isPhysicPaintRotoBackgroundMetadata, and fails validation for malformed paper metadata.
    - Test 2: createPhysicPaintLaunchContext for a Roto layer with stored physicPaintStore.getRotoBackgroundMetadata(layerId) includes a copied rotoBackground in the launch context.
    - Test 3: PhysicsPaintStudio launch handling for workflowMode roto applies context.rotoBackground to settings and to the engine background controls after load, preserving background, paperGrain, grainStrength, and optional color.
  </behavior>
  <action>Implement UAT gap 1 per D-02, D-14, and D-15. Extend PhysicPaintLaunchContext with optional rotoBackground: PhysicPaintRotoBackgroundMetadata. Update isPhysicPaintLaunchContext to accept the field only through isPhysicPaintRotoBackgroundMetadata. In createPhysicPaintLaunchContext, read physicPaintStore.getRotoBackgroundMetadata(layerId) for Roto launch contexts and include a defensive copy as rotoBackground when present; do not synthesize new metadata there and do not mutate the store. In PhysicsPaintStudio, update launch-context application so Roto contexts restore background, paperGrain, grainStrength, and color into PhysicsPaintStudioSettings and call the corresponding engine setters when the engine is ready. Preserve Play render option behavior for Play mode; Roto restored paper metadata must not be overwritten by Play-only settings. Keep paper/background metadata separate from renderedFrame/onionDataUrl pixels, per D-14 and D-15.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>After project save/close/reopen, a Roto standalone launch receives and applies the saved paper/background settings, while malformed launch metadata is rejected and no paper pixels are baked into real-key cached PNGs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composite real-key Roto paper backgrounds in preview/export</name>
  <files>app/src/lib/previewRenderer.ts, app/src/lib/previewRenderer.test.ts, app/src/lib/exportRenderer.test.ts</files>
  <behavior>
    - Test 1: PreviewRenderer draws saved paper/background metadata behind a real-key Roto renderedFrame.dataUrl when that PNG is transparent stroke-only.
    - Test 2: missing transparent frames still draw no Roto pixels, and background-only missing frames still use the existing missing-frame resolver path.
    - Test 3: exportRenderer remains delegated to PreviewRenderer and does not add a separate Physics Paint engine replay, missing-frame resolver, or export-only Roto compositing branch.
  </behavior>
  <action>Implement UAT gap 2 per D-02, D-11, D-13, D-14, and D-15. In PreviewRenderer's physic-paint branch, keep resolving missing Roto frames through resolveMissingRotoFrameDrawForLayer. Add a real-key Roto paper-background draw path that uses physicPaintStore.getRotoBackgroundMetadata for the paint layer and composites that paper/color background before drawing the saved transparent renderedFrame source. Preserve existing background-only missing-frame behavior, transparent missing-frame behavior, blend mode handling, and layer opacity. Do not draw paper for non-Roto Physics Paint layers. Do not move this logic into exportRenderer; export must continue to inherit the same visual rule by delegating to PreviewRenderer. Avoid importing the Physics Paint engine, replaying strokes, or baking paper into renderedFrame.dataUrl.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/previewRenderer.test.ts src/lib/exportRenderer.test.ts</automated>
  </verify>
  <done>Preview and export show real-key Roto strokes over their selected paper/background, missing-frame rules remain unchanged, and export has no extra live/replayed Physics Paint layer path beyond PreviewRenderer output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reuse one active-frame Roto capture for Save current and save-on-leave</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <behavior>
    - Test 1: snapshotCurrentRotoFrame captures the active frame's transparent stroke canvas once and stores/reuses that payload for both saved output and onion preview for the same flush.
    - Test 2: saveRotoFrame and requestRotoFrameNavigation do not trigger two independent buildRotoOutputFrame/buildRotoOnionPreviewFrame export passes for the active dirty frame.
    - Test 3: the active-frame save path avoids an engine background flip plus load round-trip when a current transparent stroke capture is available, while non-active frame flushes still restore the correct editable state before saving.
  </behavior>
  <action>Implement UAT gap 3 per D-14 and D-15. Replace the duplicate active-frame Roto save rendering contract with a single captured transparent stroke output for the currently edited frame. Capture that image at snapshot/save time and store it with the frame state so flushRotoFrame can reuse it for renderedFrame and onionDataUrl instead of calling separate output and onion builders that each perform an engine background/load cycle. Keep renderedFrame.dataUrl alpha-only and do not include paper pixels. Keep support cleanup, dirty frame tracking, occupied/editable frame state, apply-canvas payload shape, and save-on-leave ordering intact. For non-current frame flushes where no active capture exists, keep the existing safe state-restore path but avoid duplicate output/onion renders within the same flush. Do not introduce broad new useEffect loops; follow existing PhysicsPaintStudio callback/ref patterns and Preact project guidance.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>Saving current Roto paint or navigating away from a dirty frame reuses one active-frame transparent capture, avoids duplicate expensive stroke exports for that frame, keeps cached outputs alpha-only, and preserves existing save-on-leave correctness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted project -> launch context | Saved .mce/project metadata is loaded and passed into the standalone Physics Paint window. |
| standalone Physics Paint -> parent store | Apply-canvas payloads and cached frame outputs cross from the editor window back into physicPaintStore/project state. |
| project/store -> preview/export canvas | Persisted Roto metadata and cached PNGs are composited into preview/export output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.10-06-01 | Tampering | PhysicPaintLaunchContext.rotoBackground | medium | mitigate | Validate optional launch metadata only with isPhysicPaintRotoBackgroundMetadata; reject malformed background, grain, and strength values. |
| T-36.10-06-02 | Information Disclosure | preview/export Roto compositing | low | accept | The composited paper/stroke output is user-authored project content already visible in preview/export; no new external sink is added. |
| T-36.10-06-03 | Denial of Service | Roto save-current/save-on-leave rendering | medium | mitigate | Reuse one active-frame transparent capture and add source-contract tests that prevent duplicate engine export/background/load passes in one save. |
| T-36.10-06-04 | Tampering | real-key alpha caches | medium | mitigate | Keep paper/background metadata separate from renderedFrame.dataUrl/onionDataUrl per D-14/D-15 and verify real-key caches remain alpha-only. |
| T-36.10-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager installs are planned; if an executor attempts one, stop and require Package Legitimacy Audit plus human verification. |
</threat_model>

<verification>
Run focused checks after each task, then run the full Phase 36.10 gap-closure gate without starting the dev server:

<automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/lib/previewRenderer.test.ts src/lib/exportRenderer.test.ts</automated>
<automated>pnpm --dir app typecheck</automated>
<automated>pnpm --dir packages/efx-physic-paint build</automated>
<automated>pnpm --dir app exec vitest run</automated>

<human-check>Do not start the dev server. Ask the user to rerun UAT Test 5 in their running app: paint Roto with paper background, save project, close/reopen, verify paper background remains; render/export and verify no extra live Physics Paint layer artifact; save current and navigate frames to confirm the UI no longer becomes blocky from duplicate save renders.</human-check>
</verification>

<success_criteria>
- Gap 1 closed: saved Roto paper/background metadata survives save/close/reopen into the standalone Roto editor and engine settings.
- Gap 2 closed: preview/export real-key Roto frames composite saved paper/background behind transparent strokes using the same PreviewRenderer-owned rule, while missing transparent/background-only semantics remain unchanged.
- Gap 3 closed: Save current and save-on-leave reuse a single active-frame transparent capture and no longer duplicate expensive Roto render/export passes for the same frame.
- D-02, D-11, D-13, D-14, and D-15 remain honored; no deferred Phase 36.11 repaint scope is implemented here.
- No new dependencies are added and no server is started.
</success_criteria>

<output>
Create `.planning/phases/36.10-physics-paint-roto-missing-background-preview-export/36.10-06-SUMMARY.md` when done. Include the three UAT gaps closed, tests added/updated, commands run, and whether the final live UAT check is automated-ready or user-approved.
</output>
