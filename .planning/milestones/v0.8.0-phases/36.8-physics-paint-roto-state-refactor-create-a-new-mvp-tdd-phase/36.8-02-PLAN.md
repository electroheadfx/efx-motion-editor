---
phase: 36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase
plan: 02
type: tdd
wave: 2
depends_on:
  - 36.8-01
files_modified:
  - app/src/components/physic-paint/physicsPaintRotoSession.ts
  - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
autonomous: true
requirements:
  - 36.8-REG-05
must_haves:
  truths:
    - "36.8-REG-05 / D-10: Dirty save-before-navigation returns a `saveFrame` effect before `navigate` and keeps the requested target pending until save success."
    - "36.8-REG-05 / D-10: Dirty save-before-action returns a `saveFrame` effect before the key transaction effect and preserves the source frame as the save truth."
    - "36.8-REG-05 / D-11: Failed saves keep the source frame dirty, clear queued action/destination, record failed-save feedback, and require a later explicit action/navigation to retry."
    - "D-13: The boundary exposes dirty current-frame state for close prompting, but does not own close-window prompt state or Tauri window-close handling."
  artifacts:
    - "`physicsPaintRotoSession.ts` supports save-before-action/navigation transitions and save result transitions."
    - "`physicsPaintRotoSession.test.ts` covers sticky save failure state."
  key_links:
    - "Studio remains responsible for executing actual save effects; the session only decides ordering and transitions."
---

<objective>
Add the save orchestration half of the Roto session boundary so dirty navigation/key actions are decisioned in one place while external save execution stays in `PhysicsPaintStudio` per D-02, D-10, D-11, and D-13.

Purpose: Preserve visible dirty edits and cancel safely on save failure while preventing scattered pending-navigation/action refs from remaining the source of truth.
Output: Tested save-before-action, save-on-leave, save-success, and save-failure transition behavior on the compact session object.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-CONTEXT.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-RESEARCH.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-PATTERNS.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-VALIDATION.md
@app/src/components/physic-paint/physicsPaintRotoSession.ts
@app/src/components/physic-paint/physicsPaintRotoSession.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<artifacts_this_phase_produces>
- New or completed session transition methods: `onSaveSucceeded`, `onSaveFailed`, and optional `completeEffect`/`consumePendingAction` helpers if needed by the executor's chosen design.
- New pending state fields on the session object: pending navigation target, pending key action, saving frame, failed-save feedback, and current-frame dirty exposure if the executor chooses these names.
- New `RotoSessionEffect` metadata fields such as `reason: 'beforeNavigate' | 'beforeAction'` and `after`/`resume` descriptors if needed to express ordered continuation.
- New tests in `physicsPaintRotoSession.test.ts` for dirty `requestFrame`, dirty key action, save success, and save failure transitions.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add save-before-action and sticky failure boundary tests</name>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - .planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-PATTERNS.md
  </read_first>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.test.ts</files>
  <behavior>
    - Test 36.8-REG-05 navigation: starting at dirty frame 5, `requestFrame(8)` returns first effect `saveFrame` with frame 5 and reason `beforeNavigate`; the session exposes saving frame 5 and pending navigation 8; after `onSaveSucceeded(5)`, dirty frame 5 is cleared and the next result includes `navigate` to frame 8.
    - Test 36.8-REG-05 key action: starting at dirty frame 5, `deleteKey()` or `pasteKey()` returns first effect `saveFrame` for frame 5 with reason `beforeAction`; it does not mutate real-key ownership until save success resumes the queued action.
    - Test 36.8-REG-05 failure: after a pending navigation/action save fails, `onSaveFailed(5, message)` keeps frame 5 dirty, clears pending navigation/action, clears saving frame, records feedback containing frame 5, and the next explicit `requestFrame(8)` returns a new `saveFrame` retry.
    - Test D-13: the session exposes whether the current frame is dirty but has no API for close-window prompts, close choices, or Tauri close handling.
  </behavior>
  <action>Add failing Vitest tests against the public session API created in Plan 01. Use existing Phase 36.6 behavior as the contract: the source frame is saved first, only the latest explicit retry proceeds, and failed save stays on the dirty source. These tests must prove the boundary decides the rules and returns descriptors; they must not mock bridge calls or canvas engine calls. Do not add close prompt ownership to the session; assert its absence by using only dirty exposure and by checking source text/type surface if needed.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The focused test command fails in RED because save transition methods or expected descriptors are missing/incomplete.
    - Tests name 36.8-REG-05 and cite D-10, D-11, and D-13.
    - Failure assertions verify source dirty retention and queued destination/action clearing in the session state.
  </acceptance_criteria>
  <done>Failing save-orchestration tests exist before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement save-before-action and save result transitions</name>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/physicsPaintRotoKeyController.ts
    - .planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-RESEARCH.md
  </read_first>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.ts</files>
  <action>Update `physicsPaintRotoSession.ts` so `requestFrame` and key actions consult the session dirty-frame state before returning navigation or transaction descriptors per D-10. When the current/source frame is dirty, return an ordered result whose first effect is `saveFrame` with the source frame and reason `beforeNavigate` or `beforeAction`; store the pending target/action inside the boundary. Add save result transitions such as `onSaveSucceeded(frame)` and `onSaveFailed(frame, message)`. On success, clear that source frame from dirty state, clear saving state, and return the pending navigation or queued key transaction effects. On failure, keep the source frame dirty, clear queued navigation/action, clear saving state, record failed-save feedback that includes the failed frame, and require the next explicit action/navigation to retry per D-11. Expose dirty current-frame status for existing close prompt checks without adding close-window prompt actions or Tauri/window APIs per D-13.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts && pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED tests from Task 1 pass.
    - Save failure never removes the source frame from `dirtyFrames`.
    - Pending navigation/action is empty after `onSaveFailed`.
    - No imports from bridge, Tauri, engine, or Studio appear in `physicsPaintRotoSession.ts`.
    - No close prompt/close choice/window close method is exported from the session object.
  </acceptance_criteria>
  <done>Roto session save-before-action/navigation transitions pass tests and typecheck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — align transition names for Phase 36.9 readiness</name>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - .planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-CONTEXT.md
  </read_first>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.ts, app/src/components/physic-paint/physicsPaintRotoSession.test.ts</files>
  <action>Refine names and type unions only if needed so save transitions are state-machine-shaped per D-08 and remain explicit named methods per D-06. Keep action result order deterministic. Preserve the absence of XState and any formal state-machine package. Avoid changing test intent or loosening failure assertions.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts && pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests and typecheck pass after any naming cleanup.
    - Effect descriptors remain serializable plain objects.
    - Save transition methods remain explicit named methods, not a generic UI dispatch API.
  </acceptance_criteria>
  <done>Save transition API is clear enough for Phase 36.9 without changing Phase 36.8 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI navigation/key action -> Roto session | User intent can cross while source frame contains unsaved edits. |
| Roto session -> Studio save adapter | Save descriptors will trigger bridge/canvas persistence in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.8-04 | Tampering | save-before-action transition | high | mitigate | Boundary tests assert save happens before navigation/key mutation and source frame remains the save truth. |
| T-36.8-05 | Repudiation | save failure feedback | medium | mitigate | Sticky failed-save state records frame-specific feedback and leaves dirty state observable for retry. |
| T-36.8-06 | Denial of Service | queued navigation/action | medium | mitigate | Failure clears queued continuation so stale actions cannot fire after an unsuccessful save. |
| T-36.8-SC | Tampering | npm/pip/cargo installs | high | mitigate | No dependency install or upgrade is planned. |
</threat_model>

<verification>
Run `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- physicsPaintRotoSession.test.ts && pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
- D-10, D-11, D-13, and 36.8-REG-05 are covered by failing-then-passing boundary tests.
- Dirty save-before-action/navigation preserves visible edits by keeping dirty source state on failure.
- No close-window prompt or bridge/canvas execution moves into the session boundary.
</success_criteria>

<output>
Create `.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-02-SUMMARY.md` when done.
</output>
