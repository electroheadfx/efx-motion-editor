---
phase: 36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - PH36.4-CLEAN-CLOSE
  - PH36.4-DIRTY-CHOICES
  - PH36.4-DISCARD
  - PH36.4-CANCEL
  - PH36.4-SAVE-CLOSE
must_haves:
  truths:
    - "User can close Physics Paint from a clean current Roto frame without seeing a prompt."
    - "User closing a dirty current Roto frame sees exactly three choices: Close without saving, Cancel, and Close saving."
    - "User can choose Close without saving and the dirty current frame is not written while any previously saved cached frame remains unchanged."
    - "User can choose Cancel and remain in Physics Paint with the unsaved canvas state still available."
    - "User can choose Close saving, see saving feedback, and the window closes only after the existing Save current apply path completes successfully."
    - "User can reopen Physics Paint and verify the chosen saved or discarded outcome."
    - "After clean close, discard, cancel, save success, save error, or timeout, the module is not stuck in a blocked close/saving state."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Behavior-first regression coverage for dirty close prompt, discard, cancel, save-close, clean close, reopen outcome, and stuck-state recovery"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Roto-only dirty close state machine, three-choice dialog, close bypass, and apply-result-driven close-after-save"
      contains: "Close without saving"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Minimal close dialog and saving/error feedback styling using existing confirmation modal vocabulary"
      contains: "physics-paint-confirmation"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "@tauri-apps/api/window getCurrentWindow().onCloseRequested"
      via: "native close request is prevented only for dirty current Roto frame"
      pattern: "onCloseRequested"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "existing saveRotoFrame(null) / flushRotoFrame(currentFrame, { force: true, advanceToFrame: null }) path"
      via: "Close saving reuses Save current rather than constructing a new persistence path"
      pattern: "saveRotoFrame\(null\)"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "handleApplyResult(detail)"
      via: "programmatic close happens only after a matching successful apply result"
      pattern: "detail\.operationId"
---

<objective>
Implement the smallest autonomous MVP vertical slice for Phase 36.4: explicit Roto dirty-close behavior in Physics Paint.

Purpose: The Phase 36.4 spec requires the animator to choose whether a dirty current Roto frame is discarded, kept open, or saved before close, without silently losing work or corrupting existing cached output.
Output: Behavior-first tests, a Roto-only dirty-close prompt with exact choices, discard/cancel/save-close handlers, apply-result-driven close-after-save, stuck-state recovery, and minimal visible state feedback.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** close Physics Paint with an unsaved Roto frame by choosing close without saving, cancel, or close saving, **so that** I can choose between speed and preserving the current frame.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPECS/36.x-phases/phase-36.4-explicit-close/spec-36.4-explicit-close.md
@.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-RESEARCH.md
@.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-PATTERNS.md
@.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-VALIDATION.md
@.planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-01-SUMMARY.md
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/lib/physicPaintBridge.ts
@app/src/stores/physicPaintStore.ts
</context>

## Artifacts this phase produces

- Updated tests in `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` for PH36.4-CLEAN-CLOSE, PH36.4-DIRTY-CHOICES, PH36.4-DISCARD, PH36.4-CANCEL, PH36.4-SAVE-CLOSE, reopen outcome, and stuck-state recovery.
- Updated `PhysicsPaintStudio.tsx` identifiers or equivalent local state: `closeGuardBypassRef`, `closeAfterApplyOperationIdRef`, a close prompt state with idle/prompt/saving/error outcomes, dirty current Roto detection based on `dirtyRotoFramesRef.current.has(currentFrame)`, discard/cancel/save-close handlers, and apply-result close completion.
- Visible dialog copy exactly: `Close without saving`, `Cancel`, `Close saving`, and saving feedback aligned with `Saving current frame…`.
- Minimal CSS in `physicsPaintStudio.css` that reuses or extends `.physics-paint-confirmation*` patterns without changing broad Physics Paint layout.
- No new packages, no cache redesign, no persistence refactor, no engine changes, no broader lifecycle cleanup, no Play close changes, and no final Pencil layout polish.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify explicit Roto close behavior</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - SPECS/36.x-phases/phase-36.4-explicit-close/spec-36.4-explicit-close.md acceptance checklist and behavior contract.
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-VALIDATION.md Wave 0 requirements.
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-PATTERNS.md `PhysicsPaintStudio.test.ts` section.
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts existing Phase 36.3 close/save assertions that currently expect close to snapshot only.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx existing `beforeunload`, `onCloseRequested`, `saveRotoFrame`, `flushRotoFrame`, `handleApplyResult`, `startApplyTimeout`, and dirty Roto state symbols.
  </read_first>
  <behavior>
    - Test clean current Roto close: the close block snapshots the current Roto frame but allows default close when `dirtyRotoFramesRef.current.has(currentFrame)` is false, and it does not show the close prompt.
    - Test dirty current Roto close: after snapshotting, the Tauri close handler calls `event.preventDefault()` only for the current dirty Roto frame and renders exactly `Close without saving`, `Cancel`, and `Close saving`.
    - Test discard: the discard handler bypasses the close guard and closes without calling `saveRotoFrame`, `flushRotoFrame`, `sendPhysicPaintApplyPayload`, `applyCanvas`, or any delete-roto-frame path.
    - Test cancel: the cancel handler dismisses the prompt, clears no dirty current-frame state, does not close the window, and leaves the same editing context reachable.
    - Test save-close: `Close saving` shows `Saving current frame…`, calls `saveRotoFrame(null)`, records the returned operation id, and closes only from `handleApplyResult` after a matching successful apply result.
    - Test stuck-state recovery: send errors, failed apply results, apply timeouts, and unmount cleanup leave the window open, clear stale close-after-save refs, and restore actionable prompt/error state rather than leaving permanent saving state.
    - Test reopen outcome at the source/assertion level: discard preserves previous cached reference semantics by sending no write/delete payload; save-close reuses the existing Save current apply-canvas path that updates the current frame before close.
  </behavior>
  <action>Update `PhysicsPaintStudio.test.ts` before production changes. Replace the stale 36.3 assertion that dirty Tauri close must never prevent default with Phase 36.4 assertions that prevention is Roto-current-dirty-only while `beforeunload` remains snapshot-only and non-saving. Prefer component-level tests where the existing harness supports rendering the prompt; otherwise use the existing source-inspection style in this file with scoped block extraction around named symbols and explicit assertions for exact labels, branch handlers, save path reuse, matching operation-id close completion, and error/timeout reset behavior. Use existing Vitest setup only; do not create one-off configs. Run the focused test and record the intended RED failure in the summary.</action>
  <verify>
    <automated>pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `PhysicsPaintStudio.test.ts` contains tests or scoped assertions for all seven Phase 36.4 spec checklist items.
    - The focused test run fails before production changes for missing explicit close behavior, not for syntax/import/test-environment errors.
    - The tests assert the exact labels `Close without saving`, `Cancel`, and `Close saving`.
    - The tests assert discard sends no save/apply/delete payload and save-close waits for a matching successful apply result.
    - No new test config or package is introduced.
  </acceptance_criteria>
  <done>Phase 36.4 close behavior is captured as failing behavior-first coverage in the existing studio test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the Roto dirty-close prompt and choices</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts updated by Task 1.
    - SPECS/36.x-phases/phase-36.4-explicit-close/spec-36.4-explicit-close.md behavior contract and acceptance checklist.
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-RESEARCH.md Recommended MVP Implementation, Common Pitfalls, and Security Domain.
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-PATTERNS.md `PhysicsPaintStudio.tsx` and `physicsPaintStudio.css` pattern assignments.
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx existing `dirtyRotoFramesRef`, `snapshotCurrentRotoFrame`, `saveRotoFrame`, `flushRotoFrame`, `handleApplyResult`, `startApplyTimeout`, `applyStatus`, `applyMessage`, and `onCloseRequested` code.
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx existing confirmation modal accessibility structure for `role="dialog"`, `aria-modal="true"`, and `aria-labelledby`.
  </read_first>
  <behavior>
    - Clean Roto close and non-Roto close do not prompt and do not block normal window close.
    - Dirty current Roto close snapshots first, detects only the current frame dirty flag, prevents the close request, and shows an accessible in-studio dialog.
    - `Close without saving` programmatically closes with the close guard bypassed and does not invoke any persistence or delete path.
    - `Cancel` dismisses the dialog and preserves dirty state/canvas context.
    - `Close saving` sets saving feedback, disables duplicate close-save action while in flight, calls `saveRotoFrame(null)`, tracks the returned operation id, and waits for `handleApplyResult` success before closing.
    - Failed send, failed apply result, apply timeout, and cleanup clear stale close-save refs and leave the prompt/window usable.
  </behavior>
  <action>Implement the smallest local state machine in `PhysicsPaintStudio.tsx`: add close prompt state near the existing apply/dirty refs, a close-bypass ref for programmatic close after discard/save, and a close-after-apply operation id ref for save-close completion. In the Tauri `onCloseRequested` handler, keep `beforeunload` as snapshot-only, call `snapshotCurrentRotoFrame()` for Roto close requests, compute dirty from `workflowMode === 'roto' && dirtyRotoFramesRef.current.has(currentFrame)`, allow default close for clean current frames, and call `event.preventDefault()` only for dirty current frames. Render one accessible dialog using the existing confirmation modal vocabulary with the exact choices `Close without saving`, `Cancel`, and `Close saving`. For discard, set bypass and close the current Tauri window or browser fallback without sending apply/delete payloads. For cancel, only dismiss prompt/saving state and keep the editor state untouched. For save-close, show `Saving current frame…`, call `saveRotoFrame(null)`, store its operation id when present, and close only in `handleApplyResult` when the successful result operation id matches the close-after-save ref. On apply error, send error, timeout, or unmount, clear close-after-save state and keep the window open with actionable feedback. In CSS, add only minimal classes needed for close prompt feedback while reusing `.physics-paint-confirmation`, `.physics-paint-confirmation-card`, `.physics-paint-confirmation-actions`, and existing button/status styles. Do not modify `physicPaintStore`, `physicPaintBridge`, persistence schema, engine internals, Play lifecycle, cache format, interpolation, generated frames, or broader UI layout.</action>
  <verify>
    <automated>pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <automated>pnpm --dir app test --run src/lib/physicPaintBridge.test.ts src/stores/physicPaintStore.test.ts</automated>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Clean current Roto close and non-Roto close allow normal close without prompt.
    - Dirty current Roto close presents exactly the three required choices and uses accessible dialog semantics.
    - `Close without saving` closes without sending save/apply/delete payloads, preserving previously saved cache.
    - `Cancel` leaves the window open and preserves the unsaved current canvas/dirty state.
    - `Close saving` uses `saveRotoFrame(null)`, shows `Saving current frame…`, and closes only after the matching successful apply result.
    - Apply error, timeout, send error, and cleanup do not leave stale saving/close refs that block later choices.
    - Focused studio tests, bridge/store regression tests, and typecheck pass.
  </acceptance_criteria>
  <done>The dirty close vertical slice is green and implements discard, cancel, save-close, clean close, reopen outcome preservation, and stuck-state recovery without broad scope changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify full phase gate and user-run UAT instructions</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - SPECS/36.x-phases/phase-36.4-explicit-close/spec-36.4-explicit-close.md acceptance checklist.
    - .planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-VALIDATION.md sampling rate and manual-only verification.
    - .planning/phases/36.3-physics-paint-roto-durable-core-recovery/36.3-01-SUMMARY.md and 36.3-02-SUMMARY.md for the validated Save current/reopen cache workflow and the close-behavior deferral being closed here.
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts final assertions from Tasks 1 and 2.
  </read_first>
  <behavior>
    - Automated phase gate covers dirty prompt, discard, cancel, save-close, clean close, and stuck-state recovery.
    - Manual UAT instructions cover reopening Physics Paint after discard and after save-close to confirm previous cache unchanged versus current frame saved.
    - The executor does not run the dev server or Tauri app; user owns live UAT per project instruction.
  </behavior>
  <action>Run the phase-focused automated verification commands and fix only failures that are directly caused by this plan's files. Confirm the tests encode every checklist item from the spec, especially `Reopening Physics Paint confirms the chosen save/discard outcome` through source/component assertions plus the manual UAT instructions in the plan summary. In the final summary, include exact user-run UAT steps: dirty a current Roto frame, close and choose `Close without saving`, reopen and confirm prior cache unchanged; dirty again, close and choose `Cancel`, confirm the window remains open and canvas state remains; dirty again, close and choose `Close saving`, confirm saving feedback appears, the window closes after save, and reopen shows the saved current frame; test clean close without prompt; confirm no stuck state after each choice. Do not start the server or app.</action>
  <verify>
    <automated>pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts src/lib/physicPaintBridge.test.ts src/stores/physicPaintStore.test.ts</automated>
    <automated>pnpm --dir app test --run</automated>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Focused and full app test runs pass using existing Vitest setup.
    - `pnpm --dir app typecheck` passes.
    - The summary lists user-run UAT steps for clean close, dirty discard, dirty cancel, dirty save-close, reopen outcomes, and stuck-state recovery.
    - The executor has not run the dev server or Tauri app.
    - No files outside the three planned files are modified unless a direct test failure requires a scoped fix and the summary explains why.
  </acceptance_criteria>
  <done>Automated phase gate is green and manual UAT has exact user-run instructions for the remaining live close/reopen proof.</done>
</task>

</tasks>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Animator can close Physics Paint with an unsaved Roto frame by choosing discard, cancel, or save-close | 36.4-01 | COVERED | Tasks 1-3 implement and verify the full user story. |
| REQ | PH36.4-CLEAN-CLOSE | Closing a clean Roto frame closes without prompting | 36.4-01 | COVERED | Tasks 1-2 test and implement clean-current-frame close pass-through. |
| REQ | PH36.4-DIRTY-CHOICES | Dirty current Roto close presents exactly three choices | 36.4-01 | COVERED | Tasks 1-2 require exact labels. |
| REQ | PH36.4-DISCARD | Close without saving leaves previous cache unchanged | 36.4-01 | COVERED | Tasks 1-2 forbid save/apply/delete payloads on discard. |
| REQ | PH36.4-CANCEL | Cancel keeps window open and dirty canvas intact | 36.4-01 | COVERED | Tasks 1-2 require prompt dismissal without close or dirty clearing. |
| REQ | PH36.4-SAVE-CLOSE | Close saving uses existing Save current path, shows feedback, and closes after completion | 36.4-01 | COVERED | Tasks 1-2 require `saveRotoFrame(null)` and matching apply result. |
| RESEARCH | — | No new packages; use Preact, existing Tauri window API, existing Vitest | 36.4-01 | COVERED | Tasks use existing files and commands only. |
| RESEARCH | — | Close-after-save must wait for matching apply result, not send resolution | 36.4-01 | COVERED | Task 2 handles via operation id ref and `handleApplyResult`. |
| RESEARCH | — | Dirty detection is current Roto frame only | 36.4-01 | COVERED | Task 2 computes from `dirtyRotoFramesRef.current.has(currentFrame)`. |
| RESEARCH | — | Discard must not send apply/delete payloads | 36.4-01 | COVERED | Tasks 1-2 explicitly assert/implement this. |
| RESEARCH | — | Error/timeout must leave module unstuck | 36.4-01 | COVERED | Tasks 1-2 cover failed send/result/timeout cleanup. |
| CONTEXT | — | No 36.4 CONTEXT.md exists | 36.4-01 | COVERED | Planning uses spec/research/patterns/validation; no locked D-XX decisions for 36.4. |
| SPEC | AC-01 | Closing a clean Roto frame closes without prompting | 36.4-01 | COVERED | Must-have truth and Tasks 1-2. |
| SPEC | AC-02 | Closing a dirty Roto frame presents exactly the three expected choices | 36.4-01 | COVERED | Must-have truth and Tasks 1-2. |
| SPEC | AC-03 | Close without saving closes and leaves previous cache unchanged | 36.4-01 | COVERED | Must-have truth and Tasks 1-2. |
| SPEC | AC-04 | Cancel keeps the window open and preserves dirty canvas state | 36.4-01 | COVERED | Must-have truth and Tasks 1-2. |
| SPEC | AC-05 | Close saving shows saving feedback, saves current frame, then closes | 36.4-01 | COVERED | Must-have truth and Tasks 1-2. |
| SPEC | AC-06 | Reopening Physics Paint confirms chosen save/discard outcome | 36.4-01 | COVERED | Tasks 1 and 3 include source/component proof plus user-run UAT steps. |
| SPEC | AC-07 | The module never gets stuck after any close choice | 36.4-01 | COVERED | Tasks 1-3 cover recovery and verification. |

Excluded as out of scope: cache redesign, persistence refactor, engine changes, broader lifecycle cleanup, all dirty Roto frames beyond the current frame, Play close behavior, interpolation, generated frames, key utilities, missing-background rules, and final Pencil layout polish.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User close gesture → Physics Paint window close handler | A potentially destructive lifecycle action crosses into application state. |
| Physics Paint window → EFX Motion parent store | Close saving sends an apply-canvas payload through the existing bridge/store path. |
| Async apply lifecycle → programmatic window close | The window may close only after the trusted matching apply result confirms save completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.4-01 | Tampering | Dirty close handler in `PhysicsPaintStudio.tsx` | mitigate | Prevent silent dirty close by showing the exact three-choice prompt for current dirty Roto frames. |
| T-36.4-02 | Tampering | `Close without saving` handler | mitigate | Bypass close guard and close without invoking save/apply/delete payloads so previously saved cache is unchanged. |
| T-36.4-03 | Repudiation | `Cancel` handler | mitigate | Cancel dismisses prompt without closing, clearing dirty state, or sending payloads; tests assert state-preserving branch behavior. |
| T-36.4-04 | Tampering / Denial of Service | `Close saving` and `handleApplyResult` | mitigate | Reuse existing `saveRotoFrame(null)` path and close only after matching successful operation id; reset state on send error/result error/timeout. |
| T-36.4-05 | Denial of Service | Async close-save timeout path | mitigate | Clear stale close-after-save refs and keep the window open with actionable feedback after timeout/failure. |
| T-36.4-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are planned; if an executor adds one, stop and require package legitimacy audit before continuing. |
</threat_model>

<verification>
- Focused test: `pnpm --dir app test --run src/components/physic-paint/PhysicsPaintStudio.test.ts`.
- Related regression tests: `pnpm --dir app test --run src/lib/physicPaintBridge.test.ts src/stores/physicPaintStore.test.ts`.
- Full app suite: `pnpm --dir app test --run`.
- Typecheck: `pnpm --dir app typecheck`.
- User-run UAT is required after execution because the agent must not run the server/app: clean close without prompt; dirty close discard and reopen previous cache; dirty close cancel and continue editing; dirty close save and reopen saved current frame; no stuck state after any choice.
</verification>

<success_criteria>
- All Phase 36.4 spec acceptance checklist items are represented in automated tests and summary UAT instructions.
- The implementation modifies only the existing Physics Paint Studio close workflow and minimal CSS needed for visible prompt/feedback.
- No discard path writes or deletes cached output.
- Save-close waits for existing apply result confirmation before closing.
- Clean close is not blocked.
- Failed close-save attempts leave the module usable.
</success_criteria>

<output>
Create `.planning/phases/36.4-physics-paint-roto-explicit-close-behavior-use-specs-36-x-ph/36.4-01-SUMMARY.md` when done.
</output>
