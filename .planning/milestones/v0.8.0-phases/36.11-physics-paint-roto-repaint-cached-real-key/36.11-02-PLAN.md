---
phase: 36.11-physics-paint-roto-repaint-cached-real-key
plan: 02
type: tdd
wave: 2
depends_on:
  - 36.11-01
files_modified:
  - app/src/components/physic-paint/physicsPaintRotoSession.ts
  - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements:
  - 36.11-REPAINT-CACHED-REAL-KEY
  - 36.11-NO-STROKE-RESTORE
must_haves:
  truths:
    - "D-04: Opening a cached real key without new live paint keeps it classified as a clean cached real key."
    - "D-09: Navigating away after reopening a cached real key without new live paint preserves the existing cached key with no save/rewrite."
    - "D-10: Explicit Save current without new live paint preserves the existing cache and reports compact no-new-paint feedback."
    - "D-11: Cached-key repaint becomes dirty only on the first live edit: paint, live erase, or clearing live repaint content."
    - "D-12: If live overlay returns to empty while cached base is unchanged, the frame returns to clean and save-on-leave no-ops preserve."
    - "D-14: Opening a cached base emits compact existing-surface feedback explaining the base is visible and non-editable."
    - "D-07: Undo/Redo affects only post-reopen live overlay actions; old cached base pixels/history are never loaded as editable undo history."
    - "The old cached content is not treated as editable stroke state."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintRotoSession.ts"
      provides: "Signals/session clean/dirty/no-change semantics for cached-base repaint sessions"
    - path: "app/src/components/physic-paint/physicsPaintRotoSession.test.ts"
      provides: "Behavior tests for cached-base clean reopen, first-live-edit dirty, and clean restoration after empty live overlay"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "No-change Save current/save-on-leave preservation and live-overlay-only clear behavior"
  key_links:
    - from: "physicsPaintRotoSession.ts"
      to: "PhysicsPaintStudio.tsx"
      via: "effect descriptors and dirty frame signals decide whether Studio saves, no-ops, or preserves cached base"
      pattern: "dirtyFrames.*saveFrame"
    - from: "PhysicsPaintStudio.tsx"
      to: "physicPaintStore"
      via: "no-change cached-base save avoids delete-roto-frame and avoids rewriting real-key cache"
      pattern: "delete-roto-frame"
  prohibitions:
    - statement: "Reopening cached base alone must not mark the frame dirty or create editable stroke restoration."
      status: "resolved"
      verification: "Session and Studio tests assert clean state on base load and dirty only after live edit intent."
    - statement: "Save current with no new paint must not delete or rewrite the cached real key."
      status: "resolved"
      verification: "Studio source-contract test asserts no-change cached-base save returns preserve feedback before delete/no-persist path."
---

<objective>
Deliver the no-change and live-overlay boundary slice: a user can reopen a cached real key, inspect it, leave or Save current without losing it, and only live overlay actions affect dirty state.

Purpose: Additive repaint is safe only if cached-base display is not mistaken for editable engine content. This plan hardens the clean/dirty/no-op semantics and prevents no-change saves or Clear from destroying prior cached work.
Output: Session and Studio tests plus implementation for cached-base clean reopen, first live edit dirty, clean restoration when live overlay empties, and compact no-new-paint feedback.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** repaint cached real Roto keys, **so that** I can add paint without losing prior work.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-RESEARCH.md
@.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-PATTERNS.md
@SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
@.planning/phases/36.8-physics-paint-roto-state-refactor-create-a-new-mvp-tdd-phase/36.8-05-SUMMARY.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-02-SUMMARY.md
@app/src/components/physic-paint/physicsPaintRotoSession.ts
@app/src/components/physic-paint/physicsPaintRotoSession.test.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
</context>

## Artifacts this phase produces

- New or extended cached-base repaint state fields in `RotoSession` if needed
- New or extended session action/effect names for cached-base no-change preserve if needed
- Additional `physicsPaintRotoSession.test.ts` cases for D-04, D-07, D-09, D-10, D-11, and D-12
- Additional `PhysicsPaintStudio.test.ts` source-contract cases for cached-base-open feedback, no-change Save current, Undo/Redo live-overlay-only behavior, and Clear live-overlay-only behavior
- Compact existing-status feedback string for cached-base open, e.g. `Cached key base loaded — add paint to update.` per D-14
- Compact existing-status feedback string for no-new-paint Save current, e.g. `No new paint to save for frame N.`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - specify cached-base clean/no-change session semantics</name>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
    - SPECS/36.x-phases/phase-36.11-repaint-cached-real-key/spec-36.11-repaint-cached-real-key.md
  </read_first>
  <behavior>
    - Test 1: A cached real-key base loaded for the current frame leaves `currentFrameIsDirty` false and does not queue a save when `requestFrame(nextFrame)` runs with no live edit.
    - Test 2: First live edit on a cached-base repaint frame marks only that frame dirty and queues normal save-before-navigation when leaving.
    - Test 3: If live overlay is cleared or undone back to empty while cached base is unchanged, dirty state is removed and save-on-leave preserves the cached key without sending apply/delete.
    - Test 4: Explicit Save current on a clean cached-base frame reports compact no-new-paint feedback through the existing status/apply message path and avoids deleting or rewriting the real key.
    - Test 5: Per D-14, opening/loading a cached base sets compact feedback in the existing Physics Paint status/workflow surface explaining that the cached base is visible and non-editable.
    - Test 6: Per D-07, Undo after a post-reopen live paint action removes only that live overlay change and returns to a clean cached-base state; Redo reapplies only the live overlay change, and no old cached base pixels/history are loaded into editable undo state.
  </behavior>
  <action>Add failing tests that encode D-04, D-07, and D-09 through D-14. Prefer behavior-level session tests for state transitions and source-contract Studio tests for apply/delete avoidance, cached-base-open feedback, and Undo/Redo live-overlay-only behavior. D-14 tests must assert compact feedback is emitted through existing status/workflow/apply-message surfaces when the cached base opens and that no modal, toast, new persistent control, or timeline redesign is introduced. D-07 tests must prove Undo/Redo only targets live overlay actions after reopen: undoing live paint returns to clean cached-base state, redo reapplies only the live overlay, and old cached base pixels/history are never represented as editable stroke history. Do not require a new UI component or modal. If session APIs need a new method, name it concretely in the test, such as `markCachedBaseLoaded(frame)` and `markLiveOverlayEmpty(frame)`, but keep the implementation for Task 2.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoSession.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cite D-04, D-07, D-09, D-10, D-11, D-12, D-13, and D-14 in names or comments.
    - Session tests assert no `saveFrame` effect for clean cached-base navigation.
    - Studio tests assert no-change Save current does not enter the delete-roto-frame branch and does not call the merge helper.
    - Studio tests assert cached-base-open feedback uses the existing status/workflow/apply-message surface and explains visible non-editable base per D-14.
    - Session or Studio tests assert Undo/Redo boundaries per D-07: old cached base is absent from editable history, Undo clears only live overlay changes, and Redo restores only live overlay changes.
    - Focused tests fail in RED for missing cached-base clean/no-change behavior, not syntax mistakes.
  </acceptance_criteria>
  <done>RED commit proves clean cached-base reopen and no-change preserve behavior is not yet fully implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - implement cached-base dirty/no-op preserve semantics</name>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintRotoSession.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/physicsPaintRotoAlphaMerge.ts
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-PATTERNS.md
  </read_first>
  <action>Extend the existing Preact Signals/session boundary instead of adding broad `useEffect` orchestration to Studio. Represent cached-base repaint state as a small normalized frame-number set or equivalent semantic state that can answer: base loaded, live overlay dirty, live overlay empty. Opening a cached base per D-04 must not call `markDirty`; it must emit compact existing-surface feedback such as `Cached key base loaded — add paint to update.` per D-14 and D-13, explaining that the visible base is non-editable. First live paint/erase/clear intent per D-11 marks dirty only when live overlay content changes. Undo/Redo per D-07 must stay inside the live engine overlay history created after reopen: undoing the last live paint/erase returns to clean cached-base state when the overlay is empty, redo reapplies only the live overlay, and no cached-base pixels or old stroke/script objects are added to editable history. When live overlay becomes empty per D-12, remove that frame from dirty state while preserving the cached base. In `PhysicsPaintStudio.tsx`, add an explicit clean cached-base Save current/no-change branch before any delete/no-persist path; set compact existing status feedback such as `No new paint to save for frame ${frame}.` per D-10 and D-13. Clear on a cached-base repaint frame must call the engine clear for live overlay only and keep cached base state, real-key lists, and saved cache metadata intact per D-06.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoSession.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `physicsPaintRotoSession.ts` continues using `signal`, `computed`, and explicit actions/effects; no XState or new external state library is introduced.
    - Opening/loading a cached base does not add the frame to `dirtyFrames`.
    - Opening/loading a cached base emits compact existing-surface feedback explaining that the base is visible and non-editable per D-14.
    - First live edit marks the cached-base frame dirty.
    - Undo/Redo affects only live overlay actions created after reopen; undoing back to empty removes the frame from `dirtyFrames`, redo restores only live overlay dirty state, and old cached base pixels/history remain outside editable stroke history per D-07.
    - Empty live overlay removes the frame from `dirtyFrames` while retaining cached-base identity.
    - `PhysicsPaintStudio.tsx` has an explicit no-new-paint Save current branch before any branch that removes a Roto frame.
    - Clear on cached-base repaint does not remove the current frame from real-key/cache lists or saved Roto frame lists.
    - Focused tests pass.
  </acceptance_criteria>
  <done>Cached-base no-change and dirty transition behavior preserves prior work and avoids old stroke restoration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - protect live-overlay-only clear and feedback contract</name>
  <files>app/src/components/physic-paint/physicsPaintRotoSession.ts, app/src/components/physic-paint/physicsPaintRotoSession.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/physicsPaintRotoSession.ts
    - app/src/components/physic-paint/physicsPaintRotoSession.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - .planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-CONTEXT.md
  </read_first>
  <action>Refactor only to clarify cached-base repaint state and source-contract coverage. Keep feedback in the existing workflow/status/apply message surfaces per D-13 and D-17, including D-14 cached-base-open copy. Strengthen tests so `clearActiveSource` for cached-base repaint cannot call `removeEditableRotoFrame`, remove the cached base frame from saved/occupied real-key state, or clear the base URL/state. Strengthen Undo/Redo source-contract tests so live undo/redo calls cannot traverse old cached-base pixels/history or reconstructed stroke state per D-07. Do not add modal, toast, timeline redesign, new control, destructive raster mask, or old-stroke undo history.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoSession.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source-contract tests fail if cached-base Clear removes saved real-key state.
    - Source-contract tests fail if no-change Save current reaches `delete-roto-frame` behavior.
    - Source-contract tests fail if cached-base-open feedback is missing, emitted through a new UI surface, or omits visible/non-editable meaning.
    - Source-contract tests fail if Undo/Redo loads old cached base into editable stroke history or applies to anything other than post-reopen live overlay actions.
    - No new UI component file or CSS surface is added for Phase 36.11 feedback.
    - `pnpm --dir app typecheck` passes.
  </acceptance_criteria>
  <done>No-change preserve, live-overlay-only clear, and compact feedback semantics are stable and typed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> Roto session state | Pointer/tool/Clear/Undo actions change dirty state and can trigger save-before-navigation. |
| Roto session effects -> Studio bridge payloads | Session save/no-op decisions determine whether durable cache is rewritten, preserved, or left dirty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.11-04 | Tampering | clean cached-base Save current | high | mitigate | Add explicit preserve branch before delete/no-persist logic; tests assert no delete/rewrite for no-change saves. |
| T-36.11-05 | Tampering | clearActiveSource | high | mitigate | Clear only live overlay for cached-base repaint; tests assert real-key/cache state remains intact. |
| T-36.11-06 | Repudiation | compact status feedback | low | mitigate | Existing status/apply message reports no-new-paint preserve and merge outcomes so UAT can distinguish branches. |
| T-36.11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are required; Package Legitimacy Audit says no external packages. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/physicsPaintRotoSession.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts`
- `pnpm --dir app typecheck`
</verification>

<success_criteria>
- Reopen-only cached real key stays clean and navigates away without save/rewrite.
- Save current with no new paint preserves the cache and emits compact existing-surface feedback.
- Cached-base-open feedback appears in the existing status/workflow surface and explains visible non-editable base.
- Clear/erase/undo/redo affect only live overlay state and never old cached pixels/history.
</success_criteria>

<output>
Create `.planning/phases/36.11-physics-paint-roto-repaint-cached-real-key/36.11-02-SUMMARY.md` when done.
</output>
