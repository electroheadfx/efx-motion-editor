# Phase 36.11: Physics Paint Roto Repaint Cached Real Key - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-29
**Phase:** 36.11-physics-paint-roto-repaint-cached-real-key
**Areas discussed:** Base display, Erase/clear rules, No-change saves, Status feedback

---

## Base Display

| Question | Options | Selected |
|----------|---------|----------|
| When reopening a cached real Roto key, how should the old cached alpha base appear while the animator adds new paint? | Normal paint; Reference styling; You decide | Normal paint |
| Should the cached base be part of the live physics-paint engine state, or only a visual paint underlay while editing? | Visual underlay; Engine background; You decide | Visual underlay |
| When new live paint is added over the cached base, what should the live preview communicate about the final save result? | Exact save preview; Approximate preview; You decide | Exact save preview |
| How should reopening a cached real key with no newly painted strokes be classified in the timeline/status? | Clean cached key; Editable current; You decide | Clean cached key |

**Notes:** The cached base should look like normal paint while remaining non-editable raster underlay state. Opening the cached base alone is clean and must not imply old stroke restoration.

---

## Erase/Clear Rules

| Question | Options | Selected |
|----------|---------|----------|
| In Phase 36.11, what should the erase tool do when a cached base is visible under new live strokes? | Live-only erase; Disable erase; You decide | Live-only erase |
| What should the existing Clear action do on a reopened cached real key with a non-editable base? | Clear live only; Clear whole key; Disable clear | Clear live only |
| What should Undo/Redo cover during a cached-key repaint session? | Live actions only; Include base merge; You decide | Live actions only |
| If a user wants to remove or replace old cached pixels, how should Phase 36.11 handle that desire? | Explicitly out; Delete then repaint; You decide | Explicitly out |

**Notes:** Old cached pixels are immutable during this phase. Delete key remains the explicit destructive real-key action; Clear/Erase/Undo operate only on live repaint edits.

---

## No-Change Saves

| Question | Options | Selected |
|----------|---------|----------|
| If a cached real key is reopened and the user navigates away without adding live paint, what should happen? | No-op preserve; Rewrite unchanged; You decide | No-op preserve |
| If the user explicitly presses Save current on a reopened cached key without adding paint, what should happen? | Preserve with feedback; Force rewrite; You decide | Preserve with feedback |
| When should a cached-key repaint session become dirty? | On first live edit; On open; You decide | On first live edit |
| If the user paints new strokes and then undoes/clears all new live changes, what should save-on-leave do? | Return clean; Stay dirty; You decide | Return clean |

**Notes:** Loading/displaying the cached base does not dirty the frame. If the live repaint layer returns to empty, the frame returns to clean and preserves the existing cached key without rewriting.

---

## Status Feedback

| Question | Options | Selected |
|----------|---------|----------|
| What minimal status should appear when a cached real key opens with a non-editable base? | Compact strip copy; Badge on cell; No new copy | Compact strip copy |
| What should the successful save feedback say after merging cached alpha plus new live paint? | Mention merge; Generic saved; You decide | Mention merge |
| If save/merge fails after live repaint, what must the UI preserve and say? | Keep live dirty; Discard live strokes; You decide | Keep live dirty |
| How much new UI is allowed for Phase 36.11 feedback? | No new surfaces; Tiny label only; You decide | No new surfaces |

**Notes:** All feedback stays in the existing workflow strip/status surfaces. Successful repaint save should explicitly mention merge; failed merge preserves cached base plus live dirty edits for retry.

---

## Claude's Discretion

None. The user selected concrete choices for all discussed decisions. Exact compact status wording may be refined during planning while preserving the decisions in CONTEXT.md.

## Deferred Ideas

- Destructive raster erase/masking over the cached base belongs in a future phase with explicit masking/compositing rules.
