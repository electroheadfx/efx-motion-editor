---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 04
type: execute
wave: 3
depends_on:
  - 36.1-02
  - 36.1-03
files_modified:
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements:
  - PH36.1-PLAY-SCRUB
must_haves:
  truths:
    - "A saved Play script opened at an in-between frame shows start, end, and local preview position in the standalone Play timeline per D-15."
    - "Local Play scrubbing previews only and does not move the main editor scrubber/playhead per D-13 and D-14."
    - "Saved scripts preview cached frames by default, but remade unsaved scripts preview live editable state until Save play replaces cached frames per D-16 through D-18."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Local Play preview scrub marker and max duration messaging"
      exports: ["PhysicsPaintWorkflowStrip"]
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Saved-script launch restore, cached/live preview selection, Save play cache replacement"
  key_links:
    - from: "PhysicsPaintWorkflowStrip"
      to: "PhysicsPaintStudio"
      via: "local Play preview callback distinct from synced frame navigation"
      pattern: "onPreviewPlayFrame|onInspectPlayFrame"
---

<objective>
Make the standalone Physics Paint Play canvas open and scrub saved scripts locally without moving the main editor playhead, while respecting max-duration constraints and cached/live preview truth.

Purpose: The user can preview in-between frames in the standalone Play tab, remake a script safely, and publish only when clicking Save play.
Output: Workflow strip/studio preview behavior and tests for PH36.1-PLAY-SCRUB.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-01-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-02-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.test.ts
</context>

## Artifacts this phase produces

- Prop field on `PhysicsPaintWorkflowStripProps`: `currentPreviewFrame`
- Prop field on `PhysicsPaintWorkflowStripProps`: `maxPlayFrameCount`
- Prop field on `PhysicsPaintWorkflowStripProps`: `maxPlayFrameCountReason`
- Prop callback on `PhysicsPaintWorkflowStripProps`: `onPreviewPlayFrame(frame: number)`
- State/ref in `PhysicsPaintStudio`: local Play preview frame state
- State/ref in `PhysicsPaintStudio`: saved Play cache dirty/remade flag
- Function/helper: local cached Play frame preview lookup

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add local Play preview scrub UI and max-duration message</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
  </read_first>
  <behavior>
    - Test source exports/accepts `currentPreviewFrame`, `maxPlayFrameCount`, `maxPlayFrameCountReason`, and `onPreviewPlayFrame` on `PhysicsPaintWorkflowStripProps`.
    - Test Play range click/drag invokes `onPreviewPlayFrame` and does not invoke `onNavigateToSyncedFrame` per D-13 and D-14.
    - Test input clamp uses `maxPlayFrameCount` when provided, and shows the provided max/limit message per D-11.
  </behavior>
  <action>Extend `PhysicsPaintWorkflowStripProps` with `currentPreviewFrame?: number`, `maxPlayFrameCount?: number`, `maxPlayFrameCountReason?: string`, and `onPreviewPlayFrame?: (frame: number) => void`. Render a current preview position marker within the Play range using the existing Play lane geometry; the marker should represent the relative in-between frame and coexist with start/end endpoints. Update Play lane pointer/click handling so Play mode scrubbing calls `onPreviewPlayFrame(frame)` for preview only, while Roto navigation continues using `onNavigateToSyncedFrame`. Clamp Play frame count input to `Math.min(PHYSIC_PAINT_MAX_APPLY_FRAMES, maxPlayFrameCount)` when a max is present and render the exact max message from `maxPlayFrameCountReason` or a concrete fallback such as `Play duration limited to {maxPlayFrameCount} frames before the next saved Play script.`</action>
  <acceptance_criteria>
    - Source assertion: `PhysicsPaintWorkflowStripProps` includes `currentPreviewFrame`, `maxPlayFrameCount`, `maxPlayFrameCountReason`, and `onPreviewPlayFrame`.
    - Source assertion: Play lane preview path calls `onPreviewPlayFrame` and not `onNavigateToSyncedFrame`.
    - UI behavior assertion: opened saved range `[0..5]` at preview frame `2` shows start endpoint, end endpoint, and a current preview marker between them.
    - Behavior assertion: if `maxPlayFrameCount` is `3`, numeric input cannot produce a frame count greater than `3`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Standalone Play strip supports local in-between preview and max duration limits without publishing or syncing editor playhead per D-11, D-13, D-14, and D-15.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire saved-script cached preview and Save play cache replacement in Studio</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/types/physicPaint.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
  </read_first>
  <behavior>
    - Test launch context with `workflowMode: 'play'`, `selectedPlayScriptId: 'play-b'`, `playStartFrame: 8`, `playFrameCount: 4`, and `previewFrame: 2` initializes Play mode and local preview frame 2 per D-08 and D-15.
    - Test local Play preview handling does not call `sendPhysicPaintFrameSyncMessage` or mutate main editor frame sync paths per D-14.
    - Test saved cached frames are used for an existing saved script until `clearActiveSource` or remake marks the Play cache dirty; then preview source switches to live editable/current engine state per D-16 and D-17.
    - Test `savePlay` sends `apply-play-canvas` and resets dirty/cache status so newly rendered frames replace the old range per D-18.
  </behavior>
  <action>In `PhysicsPaintStudio`, initialize `workflowMode`, `currentFrame`, `framesToApply`, and local preview frame from the extended launch context. Keep Roto frame navigation using `navigateToSyncedFrame`, but introduce a distinct local Play preview callback/state that updates preview frame and applies a cached rendered frame or live editable preview without calling `sendPhysicPaintFrameSyncMessage`. For saved scripts, prefer cached rendered frames matching `playStartFrame + previewFrame` while the saved range cache is clean. When the user clears/remakes Play content before Save play, mark the selected saved range cache dirty and switch preview source to live/current editable state. On `savePlay`, use the current script start frame and frame count, send `apply-play-canvas`, update latest frames, clear the dirty flag, and let the store replace old cached frames for the saved range. Pass `maxPlayFrameCount` and `maxPlayFrameCountReason` from launch context to the workflow strip.</action>
  <acceptance_criteria>
    - Behavior assertion: local Play scrub from frame `2` to frame `3` changes standalone preview state only and does not post a `physic-paint:seek-frame` message.
    - Behavior assertion: existing saved script preview uses cached frame at app frame `playStartFrame + previewFrame` while clean.
    - Behavior assertion: after Play clear/remake, old cached frame is not used for preview until `Save play` regenerates frames.
    - Behavior assertion: `Save play` payload uses the selected script start frame and frame count, not the editor scrubber's in-between preview frame.
    - Regression assertion: Phase 36 scrub range behavior and queued outline preview preservation remain intact while local Play preview is added; queued outlines continue to preview unsaved physics strokes and are not replaced by final rendered cached frames until `Save play`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>Standalone Play canvas preview is local, cache-aware, remake-aware, and Save-play-published per D-13 through D-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Launch context -> standalone studio state | Cross-window context determines selected saved script, preview frame, and max duration. |
| Standalone preview -> editor apply payload | Preview scrubbing must not publish or mutate editor timeline until explicit Save play. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-09 | Tampering | `PhysicsPaintWorkflowStrip` frame count input | mitigate | Clamp frame count to validated `maxPlayFrameCount` and existing min/max bounds before preview or save. |
| T-36.1-10 | Spoofing/Tampering | `PhysicsPaintStudio` launch context restore | mitigate | Use already-validated launch context fields and preserve `isPhysicPaintLaunchContext` parsing before applying state. |
| T-36.1-11 | Repudiation | local Play scrub | mitigate | Keep preview-only path distinct from Save play apply path and avoid frame-sync messages for Play scrubbing. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned or required for this phase. |
</threat_model>

<verification>
Run focused workflow strip and studio tests. Manual app/server validation is deferred to Plan 06 checkpoint.
</verification>

<success_criteria>
- Saved Play opens directly to Play tab at the relative preview frame.
- Local Play scrubbing previews without changing main editor playhead.
- Old cached frames stop being preview truth after remake and are replaced after Save play.
- Phase 36 scrub range behavior and queued outline preview behavior remain preserved while Play-local scrubbing is introduced.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-04-SUMMARY.md` when done.
</output>
