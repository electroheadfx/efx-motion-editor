---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 02
type: execute
wave: 2
depends_on:
  - 36.1-01
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
autonomous: true
requirements:
  - PH36.1-BRIDGE
must_haves:
  truths:
    - "Opening the Physic Paint canvas from an editor scrubber inside one saved Play script opens that exact script per D-07 and D-08."
    - "Opening from a gap never chooses the nearest script and sends a max Play frame count for safe new script creation per D-09 through D-12."
  artifacts:
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Scrubber-aware launch context resolution for saved Play ranges and gaps"
      exports: ["createPhysicPaintLaunchContext", "openPhysicPaintCanvas"]
  key_links:
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "range lookup and gap max-duration helpers"
      pattern: "findPlayScriptRangeAtFrame|getMaxPlayFrameCountFromGap|getPlayScriptRanges"
---

<objective>
Make the editor-to-standalone launch bridge choose the correct saved Play script only when the editor scrubber is inside that range, otherwise open Roto with no-overlap Play creation constraints.

Purpose: The user positions the main editor scrubber over the desired physics paint animation range and opens the right script, while gaps remain useful for Roto work or starting constrained new Play scripts.
Output: Bridge launch context selection logic and focused tests for PH36.1-BRIDGE.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-01-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/lib/physicPaintBridge.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/lib/physicPaintBridge.test.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
</context>

## Artifacts this phase produces

- Function/helper: `resolvePhysicPaintLaunchSelection(layerId, frame)`
- Launch context field behavior: `selectedPlayScriptId`
- Launch context field behavior: `previewFrame`
- Launch context field behavior: `maxPlayFrameCount`
- Launch context field behavior: `maxPlayFrameCountReason`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve containing-range launches and gap launches in the bridge</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/stores/physicPaintStore.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
  </read_first>
  <behavior>
    - Test frame `10` inside saved range `{ id: 'play-b', startFrame: 8, frameCount: 4 }` produces `workflowMode: 'play'`, `startFrame: 8`, `playStartFrame: 8`, `playFrameCount: 4`, `selectedPlayScriptId: 'play-b'`, and `previewFrame: 2` per D-07 and D-08.
    - Test frame `5` in a gap before `{ startFrame: 8, frameCount: 4 }` produces `workflowMode: 'roto'`, `startFrame: 5`, no `selectedPlayScriptId`, and `maxPlayFrameCount: 3` per D-09 through D-12.
    - Test frame outside all ranges but after the last range opens Roto at the current frame and never chooses the nearest saved range per D-12.
  </behavior>
  <action>Extend `createPhysicPaintLaunchContext` by resolving the layer's saved Play script ranges from `physicPaintStore`. If `findPlayScriptRangeAtFrame(layerId, Math.trunc(frame))` returns exactly one range, set the launch context to Play with `startFrame` and `playStartFrame` equal to range `startFrame`, `playFrameCount` equal to range `frameCount`, `selectedPlayScriptId` equal to range `id`, `previewFrame` equal to `frame - range.startFrame`, `editableSource: 'play'`, and range-specific `editableState` when present. If no range contains the frame, set `workflowMode: 'roto'`, `startFrame` equal to the current frame, omit `selectedPlayScriptId`, and include `maxPlayFrameCount` plus a clear `maxPlayFrameCountReason` from the store gap helper. Keep canvas dimensions, fps, operation id, Tauri/browser fallback, origin checks, and apply payload validation unchanged. Do not implement nearest-range selection or new IPC/window architecture.</action>
  <acceptance_criteria>
    - Behavior assertion: frame inside range `[8..11]` opens `play-b` at `previewFrame: 2` and not any other range.
    - Behavior assertion: frame `5` in gap before frame `8` opens Roto with `maxPlayFrameCount: 3`.
    - Behavior assertion: frame `6` with saved ranges `[0..4]` and `[8..11]` does not open `[0..4]` or `[8..11]`.
    - Source assertion: browser fallback message listener retains `event.origin !== window.location?.origin` rejection.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run app/src/lib/physicPaintBridge.test.ts app/src/stores/physicPaintStore.test.ts app/src/types/physicPaint.test.ts</automated>
  </verify>
  <done>Bridge launch contexts implement scrubber-contained Play selection and gap/Roto max-duration constraints per D-07 through D-12.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode validated launch contexts through openPhysicPaintCanvas</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test `openPhysicPaintCanvas` encodes the selected saved script launch context in the browser URL `context` parameter and `isPhysicPaintLaunchContext(context)` returns true.
    - Test encoded gap launch includes `maxPlayFrameCount` and `maxPlayFrameCountReason` so standalone can clamp Play duration per D-10 and D-11.
  </behavior>
  <action>Ensure `openPhysicPaintCanvas` uses the extended `createPhysicPaintLaunchContext` result, validates it with `isPhysicPaintLaunchContext`, and encodes all new fields in the browser fallback URL and Tauri launch event payload. Preserve existing `PHYSIC_PAINT_LAUNCH_EVENT`, `PHYSIC_PAINT_APPLY_EVENT`, duplicate apply operation id handling, and payload result behavior. Do not run the app/server.</action>
  <acceptance_criteria>
    - Behavior assertion: decoded browser `context` for a containing range includes `selectedPlayScriptId` and `previewFrame`.
    - Behavior assertion: decoded browser `context` for a gap includes `workflowMode: 'roto'`, current `startFrame`, `maxPlayFrameCount`, and `maxPlayFrameCountReason`.
    - Source assertion: `openPhysicPaintCanvas` still returns `{ ok: false, error: 'Invalid physics paint launch context' }` when validation fails.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run app/src/lib/physicPaintBridge.test.ts</automated>
  </verify>
  <done>All launch paths carry validated saved-script or gap constraint context to the standalone Physics Paint window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor state -> launch context | Store-derived saved ranges and current frame become cross-window launch payload fields. |
| Browser fallback window -> main editor | Apply messages remain cross-window messages and must preserve origin and payload validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-04 | Tampering | `createPhysicPaintLaunchContext` | mitigate | Only derive `selectedPlayScriptId`, `previewFrame`, and `maxPlayFrameCount` from validated store helpers and validate the full context before launch. |
| T-36.1-05 | Spoofing/Tampering | browser fallback message listener | mitigate | Preserve same-origin check and typed `isPhysicPaintApplyPayload` validation before applying output. |
| T-36.1-06 | Denial of Service | gap launch max duration | mitigate | Clamp max frame counts to existing `PHYSIC_PAINT_MAX_APPLY_FRAMES` and no-overlap gap size before the standalone can create a Play range. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned or required for this phase. |
</threat_model>

<verification>
Run focused bridge/store/type tests. Manual app launch is not required in this plan and the server must not be run by Claude.
</verification>

<success_criteria>
- `[open fx paint canvas]` launch context is deterministic from the editor scrubber frame.
- Gaps open Roto and carry max-duration constraints instead of choosing nearest saved scripts.
- Cross-window payload safety remains validated.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-02-SUMMARY.md` when done.
</output>
