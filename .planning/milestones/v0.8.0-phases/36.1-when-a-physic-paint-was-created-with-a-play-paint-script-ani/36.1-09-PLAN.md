---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 09
type: execute
wave: 7
depends_on:
  - 36.1-08
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/components/sidebar/PhysicPaintProperties.tsx
  - app/src/components/sidebar/PhysicPaintProperties.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: false
gap_closure: true
requirements:
  - PH36.1-STORE
  - PH36.1-BRIDGE
  - PH36.1-PLAY-SCRUB
must_haves:
  truths:
    - "Existing saved Play scripts reopen with cached frames active/green and scrub-previewable without re-render per UAT Test 4 and D-16."
    - "Clearing a Play script in EFX Physics removes the saved script line/cache from the EFX Motion Physics Paint layer until Render and Save play recreate it per UAT Tests 1 and 4."
    - "Saved Play scripts can be deleted from EFX Motion app with destructive confirmation, removing marker and cached frames per UI-SPEC section 5."
    - "Frame/cache mutations bump `physicPaintVersion` so EFX Motion layer and markers update without reload per project memory and UAT Test 1."
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Range delete/clear cache mutation helpers with version bumps"
    - path: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      provides: "Editor-side saved Play script delete controls and confirmation"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Clear script mutation that removes editor-visible cache/line until render/save"
  key_links:
    - from: "PhysicsPaintStudio.clearActiveSource"
      to: "physicPaintStore/remove or apply bridge"
      via: "selectedPlayScriptId clear payload/helper"
      pattern: "removePlayScriptRange|removeFrameRange|selectedPlayScriptId|physicPaintVersion"
    - from: "PhysicPaintProperties delete action"
      to: "physicPaintStore.removePlayScriptRange"
      via: "destructive confirmation and sourceLayerId"
      pattern: "Delete Play script|removePlayScriptRange|physicPaintVersion"
---

<objective>
Close the remaining cache lifecycle gaps: existing saved scripts must reopen with green cached frames, clear/remake must remove editor-visible cache immediately, and scripts must be deletable from EFX Motion.

Purpose: UAT Tests 1 and 4 failed because cached saved scripts were not active on reopen and destructive clear/delete did not update the EFX Motion Physics Paint layer.
Output: Store deletion/cache helpers, editor delete UI, standalone clear behavior, tests, and a focused human visual checkpoint.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UAT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-01-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-04-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-08-PLAN.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/sidebar/PhysicPaintProperties.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

## Artifacts this phase produces

- Store helper: `deletePlayScriptRange(layerId: string, scriptId: string)` or enhanced `removePlayScriptRange` that also removes cached frames for the range.
- Store helper: `clearPlayScriptCache(layerId: string, scriptId: string)` or equivalent, removing cached frames and removing the saved script line/marker from EFX Motion until `Render` and `Save play` recreate it.
- Editor UI: `Delete Play script` destructive confirmation with exact UI-SPEC copy.
- Standalone clear behavior: `Clear script` removes selected saved line/cache from EFX Motion until Render and Save play recreate it.
- Status copy: `Cached frames ready for preview.` and `Live preview active. Render and Save play to update the cached frames.`
- Test file if absent: `app/src/components/sidebar/PhysicPaintProperties.test.ts`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make cached saved ranges reopen green and removable with version bumps</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.ts
    - app/src/stores/physicPaintStore.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/types/physicPaint.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UAT.md
  </read_first>
  <behavior>
    - Test an existing saved Play range with all rendered frames launches with `playCacheStatus: 'cached'`, `cachedPlayFrames.length === frameCount`, and `PhysicsPaintWorkflowStrip` receives cached status so cells are green immediately.
    - Test missing any cached rendered frame changes launch/cache status to `missing` or `stale`, not green cached.
    - Test clearing selected saved script removes its cached frame range and bumps `physicPaintVersion`.
    - Test after clear the standalone status copy is exactly `Live preview active. Render and Save play to update the cached frames.`.
  </behavior>
  <action>Following `36.1-PATTERNS.md` Physics Paint store mutation contract, Save Play publishing boundary, and PhysicsPaintStudio standalone bridge-client analogs, strengthen the cache truth model so a saved Play range is considered cached only when every frame in `startFrame..startFrame+frameCount-1` exists in `physicPaintStore` with matching `appFrame`. If cache is complete on launch, `PhysicsPaintStudio` must show green cached cells immediately and next/previous/start/end scrub must use cached frames without requiring Render. Add or enhance a store deletion/cache-clear helper that removes the selected script's cached frame range, removes the saved range line/marker from EFX Motion, invalidates serialization, bumps `physicPaintVersion`, and marks project dirty. Wire standalone `clearActiveSource` for Play mode so the clear/trash action removes the selected saved script line/cache from EFX Motion immediately, then sets dirty/live preview state and exact UI-SPEC status copy. Do not rely on a later render to update the editor layer; the clear mutation must publish immediately through the existing store/bridge path.</action>
  <acceptance_criteria>
    - Behavior assertion: complete cached frame range reopens green and scrub-previewable without re-render.
    - Behavior assertion: clear/trash removes cached frames for the selected range and increments `physicPaintVersion`.
    - Source assertion: `Live preview active. Render and Save play to update the cached frames.` appears in the clear/remake path.
    - Source assertion: every mutation path that removes cached frames calls the store notification/version bump path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/stores/physicPaintStore.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>Saved scripts reopen with active cached frames, and standalone Clear immediately removes the saved script line/marker and cached frames from the editor-visible Physics Paint layer until Render and Save play recreate them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EFX Motion delete saved Play script action</name>
  <files>app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/components/sidebar/PhysicPaintProperties.test.ts, app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    - app/src/components/sidebar/PhysicPaintProperties.tsx
    - app/src/stores/physicPaintStore.ts
    - app/src/stores/physicPaintStore.test.ts
    - app/src/components/timeline/TimelineRenderer.ts
    - app/src/lib/frameMap.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
  </read_first>
  <behavior>
    - Test/source-contract the editor properties panel lists saved Play scripts for the selected physics paint layer with a text-labeled delete action, not an icon-only destructive action.
    - Test/source-contract destructive confirmation title/copy uses exact UI-SPEC text: `Delete Play script` and `Delete this saved Play script and remove its cached frames from the Physics Paint layer? This cannot be undone.`.
    - Test deleting script id `play-8-13` removes its range and cached frames, leaves non-overlapping other scripts intact, and bumps `physicPaintVersion`.
    - Test after delete, frameMap/timeline marker source sees no marker for the deleted script.
  </behavior>
  <action>Following `36.1-PATTERNS.md` sidebar live store subscription and store mutation contract analogs, add an editor-side saved Play script management section in `PhysicPaintProperties.tsx` that reads `physicPaintStore.getPlayScriptRanges(sourceLayerId)` under the existing `physicPaintVersion` subscription. Keep UI compact and non-noisy: show only saved range summary and a text-labeled delete affordance per range. When delete is requested, show destructive confirmation using exact UI-SPEC copy. On confirm, call the store helper from Task 1 to remove that saved script and cached frames; clear status/error state and show a concise success message. Do not add marker click editing or extra timeline rows. Do not delete the whole physics paint layer; delete only the selected saved Play script range.</action>
  <acceptance_criteria>
    - Source assertion: confirmation copy exactly matches UI-SPEC `Delete Play script` text.
    - Behavior assertion: deleting one script removes only that script's marker/cache and keeps other ranges.
    - Behavior assertion: deleted script's frames no longer return from `physicPaintStore.getFrame` for that range.
    - Source assertion: delete action is text-labeled and not icon-only.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/stores/physicPaintStore.test.ts src/components/sidebar/PhysicPaintProperties.test.ts</automated>
  </verify>
  <done>User can delete a saved Play script from EFX Motion and the marker/cache disappear without affecting other saved scripts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User visual verification of cache, clear, and delete lifecycle</name>
  <files>app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <read_first>
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UAT.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-UI-SPEC.md
  </read_first>
  <action>Following `36.1-PATTERNS.md` manual UAT/no-server pattern and cache lifecycle contracts, pause after automated tests pass. Do not run the dev server. Ask the user to verify the cache lifecycle in their running app and resume only when they type `approved` or report issues.</action>
  <what-built>Claude implemented cached saved-script reopen, clear/remake editor update, and EFX Motion delete saved script behavior.</what-built>
  <how-to-verify>
    1. User runs the app with their normal local workflow.
    2. Open an already-created Play script from EFX Motion using `Play paint`; confirm cached cells are green immediately and next/previous/start/end scrub previews cached frames without pressing Render.
    3. Press the standalone clear/trash action for that script; confirm the saved script line/marker and cached frames are removed from the EFX Motion Physics Paint layer immediately, with no stale/non-cached marker left visible.
    4. Confirm standalone shows `Live preview active. Render and Save play to update the cached frames.`.
    5. Render and Save play again; confirm green cached cells and the EFX Motion marker/cache return.
    6. In EFX Motion, delete one saved Play script with the destructive confirmation; confirm only that script's nested marker and cached frames are removed, while other scripts stay visible.
  </how-to-verify>
  <acceptance_criteria>
    - Human assertion: existing saved Play scripts reopen green and scrub-previewable without re-render.
    - Human assertion: clear removes the saved script line/marker and cached frames from EFX Motion immediately until Render and Save play recreate them.
    - Human assertion: delete from EFX Motion removes one saved script after destructive confirmation.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run src/stores/physicPaintStore.test.ts src/components/sidebar/PhysicPaintProperties.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <human-check>User types "approved" or reports issues with screenshots/notes.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues with screenshots/notes.</resume-signal>
  <done>User confirms cached reopen, clear/remake, and editor delete lifecycle works in the running app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User destructive action -> store mutation | Delete/clear removes persisted cached frames and saved range metadata. |
| Standalone clear -> editor layer state | Standalone action must update EFX Motion output state through bridge/store, not only local UI. |
| Store mutation -> timeline marker render | Version bump drives marker/cache redraw in EFX Motion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-23 | Tampering | `removePlayScriptRange` / delete helper | mitigate | Delete only matching script id, remove only its frame interval, leave other non-overlapping ranges intact, and cover with tests. |
| T-36.1-24 | Repudiation | destructive confirmations | mitigate | Require explicit confirmation using UI-SPEC copy before editor delete; status message confirms deleted range. |
| T-36.1-25 | Denial of Service | stale cache preview | mitigate | Treat cache as valid only when every expected rendered frame exists; otherwise show stale/missing, not green cached. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned or required. |
</threat_model>

<verification>
Focused store/sidebar/studio tests plus user-run visual verification. Claude must not run the server.
</verification>

<success_criteria>
- Existing saved Play scripts reopen with cached green frame cells and scrub preview works without re-render.
- Clearing a script removes its EFX Motion saved script line/marker and cached frames immediately until Render and Save play recreate them.
- EFX Motion can delete one saved Play script after confirmation without deleting other scripts.
- Every cache/delete mutation bumps `physicPaintVersion` and updates visible markers without reload.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-09-SUMMARY.md` when done.
</output>
