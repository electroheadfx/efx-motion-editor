---
phase: 36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/efx-physic-paint/src/animation/AnimationPlayer.ts
  - packages/efx-physic-paint/src/animation/types.ts
  - packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts
autonomous: true
requirements:
  - PH36.1-SEQUENTIAL
must_haves:
  truths:
    - "Play script rendering paints strokes sequentially in recorded order like one artist hand per D-19 and D-20."
    - "Longer strokes receive more animation frames and tight durations still finish the full drawing within the chosen range per D-21 and D-22."
    - "There is no user-facing Sequential/Parallel mode toggle per D-23."
  artifacts:
    - path: "packages/efx-physic-paint/src/animation/AnimationPlayer.ts"
      provides: "Sequential recorded-order stroke-to-frame allocation"
      exports: ["AnimationPlayer"]
    - path: "packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts"
      provides: "Sequential animation behavior coverage"
  key_links:
    - from: "PhysicsPaintStudio.savePlay"
      to: "AnimationPlayer.play"
      via: "existing AnimationPlayer usage"
      pattern: "new AnimationPlayer\(engine\)|playerRef\.current\?\.play"
---

<objective>
Replace the Play animation stroke distribution with recorded-order sequential allocation inside the physics paint package animation engine.

Purpose: Saved Play scripts should look like one pencil/hand drawing strokes in sequence, not multiple letters/strokes drawing in parallel.
Output: Sequential `AnimationPlayer` behavior and tests for PH36.1-SEQUENTIAL.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint/src/animation/AnimationPlayer.ts
@/Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint/src/animation/types.ts
</context>

## Artifacts this phase produces

- New file: `packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts`
- Function/internal method behavior: sequential `distributeStrokes(strokes, totalFrames)`
- Type/interface field if needed: internal `FrameStroke` metadata for allocated frame spans

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnimationPlayer tests for one-hand sequential playback</name>
  <files>packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts, packages/efx-physic-paint/src/animation/AnimationPlayer.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/animation/AnimationPlayer.ts
    - packages/efx-physic-paint/src/animation/types.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-PATTERNS.md
  </read_first>
  <behavior>
    - Test three strokes with point counts `12`, `30`, `6` render in recorded order: the second stroke does not start until the first stroke has completed under normal duration per D-19.
    - Test longer stroke receives more frame coverage than shorter stroke when `frameCount` allows weighted allocation per D-21.
    - Test tight duration where `frameCount < strokeCount` still renders all strokes complete on the final frame per D-22.
    - Test no public config field or source string adds `parallel`, `sequential`, `mode`, or a user-facing Sequential/Parallel toggle per D-23.
  </behavior>
  <action>Create `AnimationPlayer.test.ts` using Vitest. Mock an `EfxPaintEngine` with `getStrokes`, `setInputLocked`, `setAnimationMode`, `renderPartialStrokes`, `renderAllStrokes`, and `exportCompositeCanvas`. Mock `requestAnimationFrame` so `play()` advances deterministic frames. The tests should observe `renderPartialStrokes` calls rather than reaching into private fields. Include fixtures for strokes in recorded array order with distinct point counts; use timestamps that would previously overlap if timestamp-ratio mapping were used to prove timestamps no longer drive Play behavior.</action>
  <acceptance_criteria>
    - New file exists at `packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts`.
    - Behavior assertion: under normal duration no render call includes partial/new points from stroke 2 before stroke 1 is complete.
    - Behavior assertion: final frame contains every stroke with full point counts when frame count is smaller than stroke count.
    - Source assertion: no user-facing sequential/parallel toggle API is added to `AnimationConfig`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run ../packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts</automated>
  </verify>
  <done>Failing or newly meaningful tests describe the required sequential Play behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recorded-order weighted sequential distribution</name>
  <files>packages/efx-physic-paint/src/animation/AnimationPlayer.ts, packages/efx-physic-paint/src/animation/types.ts, packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts</files>
  <read_first>
    - packages/efx-physic-paint/src/animation/AnimationPlayer.ts
    - packages/efx-physic-paint/src/animation/types.ts
    - packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts
    - .planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-CONTEXT.md
  </read_first>
  <behavior>
    - Sequential allocation ignores stroke timestamps and uses recorded array order from `engine.getStrokes()` per D-19 and D-20.
    - Frame allocation weight uses `stroke.points.length` as the concrete first implementation formula per D-21.
    - The chosen `frameCount` remains authoritative and the last frame renders all strokes complete per D-22.
  </behavior>
  <action>Replace the timestamp-ratio `distributeStrokes` implementation in `AnimationPlayer.ts` with recorded-order sequential allocation. Use `Math.max(1, stroke.points.length)` as the weight for each stroke. For normal durations, allocate contiguous frame spans in stroke order; longer point-count strokes should receive proportionally more frames than shorter strokes, while total allocated spans stay within `0..totalFrames - 1`. For tight durations where there are fewer frames than strokes, compress strokes across the available frames and ensure `renderFrame(totalFrames - 1)` includes every stroke with all points. Preserve `play(config)`, `stop()`, `getState()`, input locking, animation mode, `onFrame`, and `onComplete` behavior. Extend internal `FrameStroke` metadata only if needed; do not add public `AnimationConfig` mode/toggle fields.</action>
  <acceptance_criteria>
    - Behavior assertion: all tests in `AnimationPlayer.test.ts` pass.
    - Source assertion: `AnimationPlayer.ts` no longer maps `stroke.timestamp` through min/max timestamp ratio to choose `startFrame`.
    - Source assertion: public `AnimationConfig` in `types.ts` does not add a sequential/parallel option.
    - Behavior assertion: `renderAllStrokes()` still runs at completion and input locking/animation mode are released.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test --run ../packages/efx-physic-paint/src/animation/AnimationPlayer.test.ts</automated>
  </verify>
  <done>AnimationPlayer produces one-hand sequential Play rendering with duration-authoritative compression and no mode toggle per D-19 through D-23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine strokes -> animation renderer | Recorded stroke data drives animation frame rendering and can be large or malformed. |
| Animation loop -> UI thread | Frame allocation can degrade preview responsiveness if it is unnecessarily heavy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-12 | Denial of Service | `AnimationPlayer.distributeStrokes` | mitigate | Use linear pass over strokes/points metadata and avoid nested replay/render precomputation; keep allocation based on point counts only. |
| T-36.1-13 | Tampering | `AnimationPlayer.renderFrame` | mitigate | Preserve engine rendering boundary through `renderPartialStrokes` and do not mutate stroke source arrays during allocation. |
| T-36.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned or required for this phase. |
</threat_model>

<verification>
Run the focused AnimationPlayer test through pnpm. Do not run a dev server.
</verification>

<success_criteria>
- Play animation is sequential by recorded stroke order.
- Stroke length affects frame allocation.
- Final frame completes the drawing even under tight duration.
- No Sequential/Parallel UI or public mode toggle is added.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36.1-when-a-physic-paint-was-created-with-a-play-paint-script-ani/36.1-05-SUMMARY.md` when done.
</output>
