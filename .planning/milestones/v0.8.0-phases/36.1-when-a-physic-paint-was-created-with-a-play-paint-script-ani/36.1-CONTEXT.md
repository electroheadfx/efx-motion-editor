# Phase 36.1: Physics Paint Play-Script Timeline Markers and Sequential Playback - Context

**Gathered:** 2026-06-13
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 36.1 makes saved Physics Paint Play scripts visible and reopenable from the EFX Motion editor timeline. A Physic Paint layer can contain several non-overlapping saved Play script ranges over time; the editor timeline must show those ranges as polished nested markers inside the existing Physic Paint layer bar, and opening the standalone Physics Paint canvas from a scrubber position inside a saved range must reopen the correct Play script at the matching relative frame.

The phase also improves standalone Play canvas scrubbing and rendering: saved-script scrubbing previews cached rendered frames for speed, newly remade scripts preview from live editable state until `Save play`, and Play animation renders strokes sequentially like one artist hand instead of multiple parallel hands.

This phase does not introduce a general multi-lane source model, arbitrary overlaps, editor-side live simulation, automatic publishing, or broader preview-play caching. Those remain future work.

</domain>

<decisions>
## Implementation Decisions

### Editor Timeline Play Script Markers
- **D-01:** Keep the existing main EFX Motion Physic Paint FX/layer bar as the container. Multiple saved Play scripts appear as nested mini-ranges inside that layer bar, not as separate timeline rows.
- **D-02:** The nested markers must be polished centered graphics inside the layer block height. Do not render literal ASCII/text examples like `|-----|`, `•----•`, or `[4]---[10]` as the UI.
- **D-03:** Markers should communicate range start, range end, and duration visually through endpoint/range graphics. The main timeline ruler already provides frame/time context, so markers should not show frame badges directly by default.
- **D-04:** Marker positions and widths must follow the existing main timeline frame scale, zoom, and horizontal scroll. If the user zooms the timeline, the marker positions and widths must zoom/move with the layer bar.
- **D-05:** The active saved Play script marker uses an accent highlight. Inactive saved Play script markers remain subdued inside the Physic Paint layer bar.
- **D-06:** Saved Play script ranges on the same Physic Paint layer must not overlap. Saving/creating a Play script must respect occupied saved-script ranges.

### Opening the Correct Script
- **D-07:** `[open fx paint canvas]` chooses the saved Play script from the editor scrubber/current frame. If the scrubber is inside exactly one saved Play script range, open that saved script segment.
- **D-08:** When opening a saved Play script from inside its range, the standalone Physics Paint window opens directly to the Play canvas tab for that script at the scrubber's relative frame inside the range.
- **D-09:** If the editor scrubber is not inside any saved Play script range, `[open fx paint canvas]` opens Roto at the current frame.
- **D-10:** From a gap/outside-range Roto open, the user may switch to Play canvas to create a new script, but the standalone must receive/evaluate the maximum allowed Play frame count from the editor based on the available gap before the next saved Play script range.
- **D-11:** If the available gap before the next saved Play script is smaller than the current/default Play duration, clamp the Play duration to the available no-overlap gap and show a clear max/limit message.
- **D-12:** Do not choose the nearest segment when the scrubber is outside a range. Gaps should remain useful for Roto work or starting a new constrained Play script.

### Play Canvas Preview Scrubbing
- **D-13:** Dragging/scrubbing the local Play canvas timeline previews only. It must not save, apply, or publish output until the user clicks `Save play`.
- **D-14:** Local Play canvas scrubbing must not move the main EFX Motion editor scrubber/playhead while the standalone Physics Paint window is open.
- **D-15:** A saved Play script opened at an in-between frame should show the script range with start, end, and current preview position in the standalone Play canvas timeline.
- **D-16:** For an existing saved Play script, local scrubbing previews cached rendered frames by default for faster feedback.
- **D-17:** If the user clears/remakes the animation, old cached frames stop being the preview truth. While remaking before `Save play`, preview comes from the live/current editable script state.
- **D-18:** When the user clicks `Save play`, newly rendered cached frames replace the old saved range frames.

### One-Hand Sequential Playback
- **D-19:** Play script rendering must distribute strokes sequentially in recorded order, like one artist hand painting the drawing, instead of rendering multiple strokes/letters in parallel.
- **D-20:** The current timestamp-ratio/parallel behavior in `AnimationPlayer` is not the desired user-facing Play behavior and should be replaced for Play script rendering.
- **D-21:** Frame allocation within the sequential playback should be based on stroke length: longer strokes receive more frames; short strokes may complete in one frame when the selected duration is tight.
- **D-22:** The chosen Play duration/range length is authoritative. If the duration is too short for smooth drawing, compress all strokes so the complete drawing still finishes within the chosen duration.
- **D-23:** Do not add a user-facing Sequential/Parallel mode toggle in this phase. Sequential playback is the Play animation behavior.

### Claude's Discretion
Planner/researcher may choose the exact storage shape for multiple saved Play script ranges, the exact marker geometry/styling, and the exact short-duration stroke-length allocation formula, but must preserve the no-overlap rule, scrubber-based script selection, cached saved-script preview behavior, and one-hand sequential playback outcome above.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase scope and milestone boundaries
- `.planning/ROADMAP.md` — Phase 36.1 user story and examples for timeline markers, scrubber-positioned script opening, in-range preview scrubbing, and sequential animation.
- `.planning/REQUIREMENTS.md` — v0.8.0 standalone/output requirements and explicit future integration boundaries.
- `.planning/PROJECT.md` — v0.8.0 context, non-replacement boundary, and prior failed adapter direction.
- `.planning/STATE.md` — Current workflow state and the inserted urgent Phase 36.1 scope note.

### Prior phase decisions
- `.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md` — Existing Roto/Play workflow strip decisions, manual `Save play`, preview-only Play behavior, dev export boundary, and previous single-range Play marker assumptions.
- `.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md` — Editor-to-standalone-to-editor workflow, rendered-output-only bridge, `[apply play canvas]` frame sequence behavior, and editable-state vs rendered-output distinction.
- `.planning/phases/34-standalone-demo-shell/34-CONTEXT.md` — Standalone package-local demo boundaries and public API preference.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/src/components/timeline/TimelineRenderer.ts` — Draws the main editor FX/content-overlay range bars and selected-layer keyframe markers. Phase 36.1 should add polished nested Play script markers for `physic-paint` layers here or in adjacent timeline rendering code.
- `app/src/components/timeline/TimelineInteraction.ts` — Handles main timeline FX track selection, hit-testing, seeking, and range-bar drag behavior. Use this for marker click/selection behavior only if needed; scrubber-inside opening is the primary decision.
- `app/src/stores/sequenceStore.ts` — Owns FX sequence range updates for the outer Physic Paint layer bar. Saved Play script ranges are nested inside a layer, not replacements for the outer FX sequence range.
- `app/src/components/sidebar/PhysicPaintProperties.tsx` — Existing `[open fx paint canvas]` entry point from the selected `physic-paint` layer and current editor frame.
- `app/src/lib/physicPaintBridge.ts` — Creates the `PhysicPaintLaunchContext`; currently derives a single `playStartFrame` from layer workflow metadata. Phase 36.1 must extend this path so the scrubber can select the correct saved script segment and send max-duration constraints for gaps.
- `app/src/types/physicPaint.ts` — Defines launch/apply payloads, workflow metadata, rendered frames, frame count clamp, and validation. Likely needs new typed metadata for multiple saved Play script ranges and max allowed Play duration.
- `app/src/stores/physicPaintStore.ts` — Stores rendered frames, one editable state, and single-range workflow metadata per layer. Phase 36.1 must evolve this to support several non-overlapping saved Play script ranges per layer while preserving explicit `physicPaintVersion` invalidation and dirty marking.
- `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx` — Existing standalone bottom strip with Roto/Play tabs, local Play range visualization, frame-count input, play preview, `Save play`, and range click inspection.
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` — Existing standalone behavior for play preview, save/apply play, conversion, cached/latest play frames, onion preview, and bridge send/apply logic.
- `packages/efx-physic-paint/src/animation/AnimationPlayer.ts` — Current frame-based replay engine. It currently distributes strokes by timestamp ratio and allows overlapping stroke playback; this is the target for one-hand sequential distribution.
- `packages/efx-physic-paint/src/animation/types.ts` — Animation config/state/frame-stroke types; may need adjustment if sequential frame allocation needs richer metadata.

### Established Patterns
- Physics paint remains additive and does not replace perfect-freehand basic paint or p5.brush FX paint.
- The rejected headless/batch adapter/editor-driven replay direction must not return. Preserve standalone interactive behavior and rendered-frame/cache output.
- Normal artist workflow remains manual: preview/scrub is not publish; `Save play` explicitly regenerates/applies cached frames.
- Non-reactive Map storage must bump explicit version signals (`physicPaintVersion`) and mark the project dirty on mutations.
- The main editor timeline uses Canvas 2D rendering and existing zoom/scroll frame math; nested Play markers should reuse that coordinate system.
- The standalone Physics Paint UI should stay visually polished and app-like, not demo-like.

### Integration Points
- Creating/saving Play scripts must update both rendered cached frames and saved script metadata for timeline markers/reopen behavior.
- Opening from `PhysicPaintProperties` must inspect the selected physics layer's saved script ranges against `timelineStore.currentFrame`.
- Main timeline rendering must subscribe to physics paint output/metadata changes so markers refresh after `Save play`, clear/remake, or project load.
- Project persistence currently serializes `physic_paint_outputs`; saved multi-script metadata must persist there or in the appropriate existing physics paint persistence path.
- Preview rendering/compositing should continue using cached rendered frames for the editor; local standalone scrub can use cached frames for saved scripts and live editable state for unsaved/remade scripts.

</code_context>

<specifics>
## Specific Ideas

- User examples for main timeline display were explanatory, not literal UI: `Physic paint [ [0]------[4]    [8]-----[23]    [30]----[34] ]`. Implement a polished visual equivalent centered within the layer bar.
- User explicitly wants marker graphics inside the layer block height, not an ASCII-looking demo render.
- The main timeline already shows frame/time; nested markers do not need text frame badges by default.
- Scrubber over a saved script range means "open the right script"; scrubber in a gap means open Roto at that current frame.
- When creating a new Play script from a gap, the maximum frame count should be evaluated and sent to EFX Physics Paint so the new script cannot collide with the next existing script animation.
- Local Play timeline preview should support in-between frames, for example start/end with a current marker between them.
- Cached frames are preferred for saved-script scrub responsiveness because physics paint drawing is slow.
- Sequential animation should feel like painting `C`, then `h`, then `l`, then `o`, then `e`, not painting multiple letters at the same time.

</specifics>

<deferred>
## Deferred Ideas

- Later performance/cache phase: think more broadly about caching for other physics paint preview-play paths because drawing physics paint is very slow.
- Future source-lane model: arbitrary mixing/stacking of Play scripts and Roto corrections remains outside Phase 36.1.
- Future advanced conflict handling: overlapping saved Play scripts, replace/merge workflows, or stacked script lanes are outside Phase 36.1.

</deferred>

---

*Phase: 36.1-Physics Paint Play-Script Timeline Markers and Sequential Playback*
*Context gathered: 2026-06-13*
