---
phase: 35-interactive-physics-paint-controls
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/components/sidebar/PhysicPaintProperties.tsx
  - app/src/components/timeline/AddFxMenu.tsx
  - app/src/components/layout/LeftPanel.tsx
autonomous: true
requirements: [PAINT-04, DIAG-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-13: User can create/select a physic-paint layer and click `[open fx paint canvas]` from its parameters."
    - "D-07/DIAG-01: App-side parameters show current frame/output status and bridge/open errors."
    - "PAINT-04: Existing Paint / Rotopaint and p5.brush FX paths remain available and unmodified."
  artifacts:
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Open/focus standalone workflow with typed launch context"
      exports: ["openPhysicPaintCanvas", "createPhysicPaintLaunchContext"]
    - path: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      provides: "Sidebar controls for physic-paint layer"
      contains: "[open fx paint canvas]"
    - path: "app/src/components/timeline/AddFxMenu.tsx"
      provides: "Add Physic Paint layer menu item"
      contains: "physic-paint"
    - path: "app/src/components/layout/LeftPanel.tsx"
      provides: "Selected-layer routing to PhysicPaintProperties"
      contains: "PhysicPaintProperties"
  key_links:
    - from: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "open button click"
      pattern: "openPhysicPaintCanvas"
    - from: "app/src/components/timeline/AddFxMenu.tsx"
      to: "app/src/types/layer.ts"
      via: "Layer source `{ type: 'physic-paint' }`"
      pattern: "type: 'physic-paint'"
---

<objective>
Add the editor entry point for the interactive physics paint workflow.

Purpose: The user must be able to create/select a separate `physic-paint` layer, see its parameters, and open the standalone physics paint canvas with current layer/frame context per D-01 and D-13.
Output: Bridge helper, sidebar properties panel, Add FX menu item, and properties routing for the new layer type.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
@.planning/phases/35-interactive-physics-paint-controls/35-RESEARCH.md
@.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
@.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
@.planning/phases/35-interactive-physics-paint-controls/35-01-SUMMARY.md
@app/src/components/timeline/AddFxMenu.tsx
@app/src/components/layout/LeftPanel.tsx
@app/src/components/sidebar/PaintProperties.tsx
@app/src/lib/ipc.ts
</context>

## Artifacts this phase produces

- New file: `app/src/lib/physicPaintBridge.ts`
- Function: `createPhysicPaintLaunchContext(layer, frame): PhysicPaintLaunchContext`
- Function: `openPhysicPaintCanvas(context): Promise<Result<PhysicPaintLaunchContext>>`
- Constant/event key: `PHYSIC_PAINT_LAUNCH_EVENT = 'physic-paint:launch'`
- New file: `app/src/components/sidebar/PhysicPaintProperties.tsx`
- Component: `PhysicPaintProperties`
- UI button copy: `[open fx paint canvas]`
- AddFxMenu handler: `handleAddPhysicPaintLayer`
- AddFxMenu menu item label: `Physic Paint`
- LeftPanel route: selected `physic-paint` layers render `PhysicPaintProperties`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the typed bridge for opening the standalone canvas</name>
  <files>app/src/lib/physicPaintBridge.ts</files>
  <read_first>
    @.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
    @.planning/phases/35-interactive-physics-paint-controls/35-RESEARCH.md
    @.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
    @.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
    @app/src/lib/ipc.ts
    @app/src/types/physicPaint.ts
    @app/src/types/layer.ts
  </read_first>
  <behavior>
    - `createPhysicPaintLaunchContext` includes the current `layerId`, layer name, `startFrame`, `operationId`, and project/canvas dimensions when available.
    - `openPhysicPaintCanvas` returns a `Result`-style success/error object and never throws uncaught UI errors.
    - Browser/dev fallback opens or focuses a standalone physics paint URL with serialized context when Tauri window APIs are unavailable; Tauri runtime path uses Tauri WebviewWindow/event APIs when available.
  </behavior>
  <action>Create `app/src/lib/physicPaintBridge.ts` using the `Result` style from `app/src/lib/ipc.ts`. Export `PHYSIC_PAINT_LAUNCH_EVENT`, `createPhysicPaintLaunchContext`, and `openPhysicPaintCanvas`. The bridge should accept only `Layer` objects whose `type` and `source.type` are `physic-paint`; return an error otherwise. Include the exact launch context fields from Plan 35-01. For opening, prefer dynamic imports of Tauri window/event APIs so the app still builds in browser/dev contexts, and provide a browser fallback using `window.open` with the context encoded in the URL/search/hash. Do not run the dev server. Do not implement apply-back listeners here; Plan 35-04 owns apply payload handling.</action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `openPhysicPaintCanvas` rejects non-`physic-paint` layers before opening any window.
    - Source assertion: bridge code contains no imports from `@efxlab/efx-physic-paint` and no editable stroke/engine transport fields.
    - Source assertion: Tauri imports are dynamic or otherwise safe for app build, and any event/window listener created by this file has cleanup or is a one-shot open action.
    - Command assertion: `pnpm --dir app typecheck` exits 0.
  </acceptance_criteria>
  <done>The app has a safe launch bridge that carries layer/frame context to the standalone workflow per D-01 without apply-back scope creep.</done>
</task>

<task type="auto">
  <name>Task 2: Add Physic Paint layer creation and sidebar properties entry point</name>
  <files>app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/components/timeline/AddFxMenu.tsx, app/src/components/layout/LeftPanel.tsx</files>
  <read_first>
    @.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
    @.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
    @.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
    @app/src/components/sidebar/PaintProperties.tsx
    @app/src/components/timeline/AddFxMenu.tsx
    @app/src/components/layout/LeftPanel.tsx
    @app/src/lib/physicPaintBridge.ts
    @app/src/stores/physicPaintStore.ts
  </read_first>
  <action>Create `PhysicPaintProperties` as a compact sidebar panel following `PaintProperties` spacing/style patterns. It must show current layer name/id, current frame, rendered-output status using `physicPaintStore.hasOutput`, empty-state copy `No physics paint output yet`, the required body copy from UI-SPEC, and a primary button labelled exactly `[open fx paint canvas]` per D-01. The button calls `openPhysicPaintCanvas` with current layer/frame context and displays success/error copy; when context is invalid, keep the button visible but disabled with `Select a physics paint layer and frame first.`. Update `AddFxMenu.tsx` with a sibling `handleAddPhysicPaintLayer` that creates a layer named `Physic Paint`, `type: 'physic-paint'`, `source: { type: 'physic-paint', layerId }`, normal blend/opacity/default transform, selects it, and does not call paint-mode APIs. Add a PAINT menu item labelled `Physic Paint` without changing the existing `handleAddPaintLayer` or `Paint / Rotopaint` item. Update `LeftPanel.tsx` to render `PhysicPaintProperties` for selected `physic-paint` layers before the generic sidebar properties branch.</action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `PhysicPaintProperties.tsx` contains exact visible copy `[open fx paint canvas]`, `No physics paint output yet`, and `Select a physics paint layer and frame first.`.
    - Source assertion: `AddFxMenu.tsx` contains `handleAddPhysicPaintLayer` and leaves `handleAddPaintLayer` behavior intact.
    - Source assertion: `LeftPanel.tsx` imports and routes `PhysicPaintProperties` only for `selectedLayer.type === 'physic-paint'`.
    - Behavior assertion: creating a physics layer does not toggle existing paint edit mode or modify p5.brush/perfect-freehand code paths.
    - Command assertion: `pnpm --dir app typecheck` exits 0.
  </acceptance_criteria>
  <done>User can add/select a distinct physics paint layer and open the standalone workflow from its parameter button per D-01/D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App UI -> standalone window | Layer/frame context crosses into a separate browser/Tauri window. |
| Add layer menu -> project state | New layer creation mutates editor project/layer state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering | `openPhysicPaintCanvas` | mitigate | Reject non-`physic-paint` layers and include operationId/startFrame in launch context. |
| T-35-05 | Spoofing | Browser fallback window | accept | Local desktop/dev-only workflow; no secrets or authenticated state. Diagnostics expose failed open errors. |
| T-35-06 | Tampering | `AddFxMenu.tsx` | mitigate | Add a sibling physics layer flow and do not modify existing `handleAddPaintLayer` semantics. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this plan; RESEARCH.md Package Legitimacy Audit says no external package installation is recommended. |
</threat_model>

<verification>
Run `pnpm --dir app typecheck`. User-run UAT later verifies the server/window behavior because project instructions say Claude must not run the server.
</verification>

<success_criteria>
- A distinct `Physic Paint` layer can be created without replacing Paint / Rotopaint.
- Selected physics paint layers show `[open fx paint canvas]` and current status in the properties panel.
- Opening passes current layer/frame context through a typed bridge and reports errors in UI.
</success_criteria>

<output>
Create `.planning/phases/35-interactive-physics-paint-controls/35-02-SUMMARY.md` when done.
</output>
