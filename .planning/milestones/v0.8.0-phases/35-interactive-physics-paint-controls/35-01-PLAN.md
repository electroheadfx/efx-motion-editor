---
phase: 35-interactive-physics-paint-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/layer.ts
  - app/src/types/physicPaint.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
autonomous: true
requirements: [PAINT-04, DIAG-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-13: A distinct physic-paint layer type exists without replacing existing paint/perfect-freehand/p5.brush paths."
    - "D-02/D-03: App-side contracts accept rendered output only, never editable strokes or engine internals."
    - "D-10/DIAG-01: Apply sequence frame counts are clamped and diagnosable."
  artifacts:
    - path: "app/src/types/layer.ts"
      provides: "Layer/source union entries for physic-paint"
      contains: "physic-paint"
    - path: "app/src/types/physicPaint.ts"
      provides: "Typed launch/apply payloads and validation helpers"
      exports: ["PHYSIC_PAINT_MAX_APPLY_FRAMES", "clampPhysicPaintFrameCount", "isPhysicPaintApplyPayload"]
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Rendered-output frame store and version invalidation"
      exports: ["physicPaintStore", "physicPaintVersion"]
  key_links:
    - from: "app/src/types/layer.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "layerId/source contract"
      pattern: "physic-paint"
    - from: "app/src/types/physicPaint.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "validated rendered-frame payloads"
      pattern: "RenderedPhysicPaintFrame"
---

<objective>
Create the app-side contracts and rendered-output store for Phase 35.

Purpose: Downstream app UI, bridge, preview, and standalone apply code need a strict rendered-output-only contract before they mutate editor state. This implements D-02, D-03, D-10, and D-13 without touching existing basic paint or p5.brush FX paint behavior.
Output: Distinct `physic-paint` layer/source type, typed launch/apply payloads, validation helpers, and a tested rendered-frame store.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
@.planning/phases/35-interactive-physics-paint-controls/35-RESEARCH.md
@.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
@.planning/phases/35-interactive-physics-paint-controls/35-VALIDATION.md
@.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
@app/src/types/layer.ts
@app/src/stores/paintStore.ts
</context>

## Artifacts this phase produces

- `LayerType` union member: `'physic-paint'`
- `LayerSourceData` union member: `{ type: 'physic-paint'; layerId: string }`
- New file: `app/src/types/physicPaint.ts`
- Constant: `PHYSIC_PAINT_MAX_APPLY_FRAMES = 600`
- Constant: `PHYSIC_PAINT_DEFAULT_APPLY_FRAMES = 120`
- Function: `clampPhysicPaintFrameCount(value: unknown): number`
- Function/type guard: `isPhysicPaintLaunchContext(value: unknown): value is PhysicPaintLaunchContext`
- Function/type guard: `isPhysicPaintApplyPayload(value: unknown): value is PhysicPaintApplyPayload`
- Types: `PhysicPaintLaunchContext`, `PhysicPaintRenderedFrame`, `PhysicPaintApplyCanvasPayload`, `PhysicPaintApplyPlayCanvasPayload`, `PhysicPaintApplyPayload`, `PhysicPaintApplyResult`
- New file: `app/src/stores/physicPaintStore.ts`
- Signal/export: `physicPaintVersion`
- Store/export: `physicPaintStore`
- Store methods: `getFrame`, `getFrames`, `setFrame`, `applyCanvas`, `applySequence`, `hasOutput`, `clearLayer`, `reset`
- New file: `app/src/stores/physicPaintStore.test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add distinct physic-paint layer and rendered-output payload contracts</name>
  <files>app/src/types/layer.ts, app/src/types/physicPaint.ts</files>
  <read_first>
    @.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
    @.planning/phases/35-interactive-physics-paint-controls/35-RESEARCH.md
    @.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
    @.planning/phases/35-interactive-physics-paint-controls/35-UI-SPEC.md
    @app/src/types/layer.ts
  </read_first>
  <behavior>
    - `LayerType` includes `'physic-paint'` as a sibling to `'paint'`, preserving PAINT-04 and D-13.
    - `LayerSourceData` includes `{ type: 'physic-paint'; layerId: string }` and does not change `{ type: 'paint'; layerId: string }`.
    - `PhysicPaintApplyPayload` models rendered PNG/data URL output only per D-02/D-03; no field names `strokes`, `engine`, `SerializedProject`, `project`, or `internals` are accepted for apply-back.
    - `clampPhysicPaintFrameCount` returns integers in UI-spec range 1..600, defaulting to 120 for invalid input per D-10.
  </behavior>
  <action>Create `app/src/types/physicPaint.ts` with launch context, rendered-frame, apply-canvas, apply-play-canvas, result, and readiness/error-state types. Include `operationId`, `layerId`, `startFrame`, `frameCount`, and `frames`/`renderedFrame` fields. Add runtime guards for launch/apply payloads that require `layerId`, non-negative integer `startFrame`, valid operation id, payload kind, and rendered PNG/data URL frame data; reject editable engine/session state in apply payloads per D-02, D-03, D-11, and D-12. Modify `app/src/types/layer.ts` only by adding the additive `physic-paint` layer/source union entries; do not alter `paint`, `PaintModeSelector`, perfect-freehand, or p5.brush FX types.</action>
  <verify>
    <automated>pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/src/types/layer.ts` contains exactly one LayerType member `'physic-paint'` and keeps existing `'paint'` intact.
    - Source assertion: `app/src/types/physicPaint.ts` exports `PHYSIC_PAINT_MAX_APPLY_FRAMES`, `PHYSIC_PAINT_DEFAULT_APPLY_FRAMES`, `clampPhysicPaintFrameCount`, `isPhysicPaintLaunchContext`, and `isPhysicPaintApplyPayload`.
    - Source assertion: apply payload contracts contain rendered frame/image fields and do not contain editable stroke/engine/internal apply-back fields.
    - Command assertion: `pnpm --dir app typecheck` exits 0.
  </acceptance_criteria>
  <done>Distinct physics paint contracts exist and enforce the rendered-output-only boundary required by D-02/D-03/D-13.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rendered physics paint store with version invalidation</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    @.planning/phases/35-interactive-physics-paint-controls/35-CONTEXT.md
    @.planning/phases/35-interactive-physics-paint-controls/35-RESEARCH.md
    @.planning/phases/35-interactive-physics-paint-controls/35-PATTERNS.md
    @.planning/phases/35-interactive-physics-paint-controls/35-VALIDATION.md
    @app/src/stores/paintStore.ts
    @app/src/types/physicPaint.ts
  </read_first>
  <behavior>
    - `setFrame(layerId, frame, renderedFrame)` stores rendered output for one app frame and increments `physicPaintVersion`.
    - `applyCanvas(payload)` writes the current rendered image to `payload.startFrame` per D-08.
    - `applySequence(payload)` writes generated frame `i` to app frame `payload.startFrame + i` per D-09/D-10.
    - `reset()` clears all rendered outputs and increments the version when data existed.
  </behavior>
  <action>Create `app/src/stores/physicPaintStore.ts` following the centralized invalidation style from `paintStore`: internal `Map<string, Map<number, PhysicPaintRenderedFrame>>`, exported `physicPaintVersion` signal, and store methods `getFrame`, `getFrames`, `setFrame`, `applyCanvas`, `applySequence`, `hasOutput`, `clearLayer`, and `reset`. Mark project dirty through the same project-store dirty mechanism used by paint mutations when available, and always bump `physicPaintVersion` on mutations per user memory. Create `app/src/stores/physicPaintStore.test.ts` before implementation; test still-frame placement, sequence placement from `startFrame`, invalid frame-count rejection/clamp behavior through the typed helpers, version bumps, and reset/clear behavior. Do not import `@efxlab/efx-physic-paint` or engine types into the app store per D-02.</action>
  <verify>
    <automated>pnpm --dir app test --run app/src/stores/physicPaintStore.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Test assertion: applying three rendered frames with `startFrame: 10` stores frames at 10, 11, and 12 only.
    - Test assertion: applying a still payload stores exactly one frame at `startFrame`.
    - Test assertion: every successful mutation increments `physicPaintVersion`.
    - Source assertion: `app/src/stores/physicPaintStore.ts` has no imports from `@efxlab/efx-physic-paint` and no fields named `strokes` or `engine`.
    - Command assertion: `pnpm --dir app test --run app/src/stores/physicPaintStore.test.ts && pnpm --dir app typecheck` exits 0.
  </acceptance_criteria>
  <done>App has a tested, rendered-output-only physics paint frame store suitable for app entry, apply-back, preview, and diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Standalone/apply payload -> app store | Untrusted rendered-output payloads mutate editor-visible frames. |
| Layer model -> existing paint systems | New layer union must not tamper with existing basic/FX paint semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | `isPhysicPaintApplyPayload` | mitigate | Validate payload kind, layerId, operationId, startFrame, frameCount, and rendered frame data before store mutation. |
| T-35-02 | Denial of Service | `clampPhysicPaintFrameCount` | mitigate | Clamp sequence length to 1..600 and default invalid values to 120 per UI spec. |
| T-35-03 | Tampering | `LayerType`/`LayerSourceData` | mitigate | Add `physic-paint` as a sibling only; do not alter `paint` type/source behavior. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this plan; RESEARCH.md Package Legitimacy Audit says no external package installation is recommended. |
</threat_model>

<verification>
Run `pnpm --dir app test --run app/src/stores/physicPaintStore.test.ts && pnpm --dir app typecheck`.
</verification>

<success_criteria>
- The app type system represents physics paint as a separate layer/source.
- Rendered-output apply payloads cannot carry editable engine/stroke internals.
- Store writes still and sequence rendered frames at the correct app frames and bumps `physicPaintVersion`.
- Existing `paint` layer typing remains unchanged except for additive union widening.
</success_criteria>

<output>
Create `.planning/phases/35-interactive-physics-paint-controls/35-01-SUMMARY.md` when done.
</output>
