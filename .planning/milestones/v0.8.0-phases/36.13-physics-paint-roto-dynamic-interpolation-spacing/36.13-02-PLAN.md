---
phase: 36.13-physics-paint-roto-dynamic-interpolation-spacing
plan: 02
type: tdd
wave: 2
depends_on: [36.13-01]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
autonomous: true
requirements:
  - 36.13-SOURCE-DISPLAY-MODEL
  - 36.13-SEGMENT-SPACING-OVERRIDES
  - 36.13-PERSISTENCE
must_haves:
  truths:
    - "D-01/D-02: hydration/regeneration infers custom overrides from existing distant source-key gaps and leaves compact segments global-controlled."
    - "D-04: project save/load persists source-endpoint segment overrides, not target-local flags."
    - "D-09: generated/custom spacing is clamped by existing safe frame/count limits."
    - "Toggle on/off, close/reopen Physics Paint, and project save/load reconstruct the same source/display result."
    - "Generated metadata is disposable cache; source real keys plus settings plus overrides can regenerate the display result."
  prohibitions:
    - statement: "Do not trust persisted generated frames as source truth after load."
      status: resolved
      verification: "store load tests delete/stale generated metadata and assert regeneration from real keys/settings/overrides."
    - statement: "Do not erase custom overrides when global in-between count changes."
      status: resolved
      verification: "store settings tests assert count changes affect only non-custom segments."
  artifacts:
    - path: app/src/stores/physicPaintStore.ts
      provides: "Store-owned override normalization, inferred hydration, regenerated cache, version invalidation, and MCE serialization."
      exports:
        - physicPaintStore.getRotoInterpolationSettings
        - physicPaintStore.setRotoInterpolationSettings
        - physicPaintStore.regenerateRotoInterpolationCache
        - physicPaintStore.toMceOutputs
        - physicPaintStore.loadFromMceOutputs
  key_links:
    - from: app/src/components/physic-paint/physicsPaintWorkflowState.ts
      to: app/src/stores/physicPaintStore.ts
      via: "store regeneration consumes override-aware `getExpandedRotoRealKeyFrames` from Plan 01"
    - from: app/src/stores/physicPaintStore.ts
      to: app/src/lib/previewRenderer.ts
      via: "preview/export continue consuming `physicPaintStore.getRotoFrame` derived cache"
---

<objective>
Persist and regenerate override-aware Roto interpolation through the store so source/display truth survives toggle, close/reopen, and project save/load.

Purpose: Users must be able to create custom timing once and trust that regenerated generated frames are derived from authoritative source keys plus durable segment overrides.
Output: Store tests and implementation for inferred overrides, settings serialization, generated-cache regeneration, clamping, and `physicPaintVersion` invalidation.
</objective>

## Phase Goal

**As a** stop-motion animator, **I want to** intentionally distant real Roto keys to create remembered per-segment interpolation spacing, **so that** custom timing survives global in-between count changes, interpolation toggles, save/load, preview, and export.

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-CONTEXT.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-RESEARCH.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-PATTERNS.md
@SPECS/36.x-phases/phase-36.13-dynamic-interpolation-spacing/spec-36.13-dynamic-interpolation-spacing.md
@.planning/phases/36.12-physics-paint-roto-generated-interpolation/36.12-08-SUMMARY.md
@.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-01-SUMMARY.md
@app/src/stores/physicPaintStore.ts
@app/src/stores/physicPaintStore.test.ts
</context>

## Artifacts this phase produces

- Store normalization support for `segmentSpacingOverrides`.
- Store hydration inference path for distant source-key gaps.
- Store regenerated generated-cache metadata using override-aware display frames.
- Persisted `.mce` `roto_interpolation_settings.segmentSpacingOverrides` field.
- Regression tests for version invalidation and save/load reconstruction.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED store tests for durable override regeneration</name>
  <files>app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    @app/src/stores/physicPaintStore.test.ts
    @app/src/stores/physicPaintStore.ts
    @.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-01-SUMMARY.md
    @SPECS/36.x-phases/phase-36.13-dynamic-interpolation-spacing/spec-36.13-dynamic-interpolation-spacing.md
  </read_first>
  <behavior>
    - D-01/D-02: loading or regenerating real keys `0/1/2/6` with global count 2 infers/preserves a `2 -> 6` override with four in-betweens while compact `0/1/2` stores no overrides.
    - D-04: `toMceOutputs()` serializes explicit source endpoints and count; `loadFromMceOutputs()` normalizes settings and regenerates generated cache from real keys plus overrides.
    - D-09: overrides above `PHYSIC_PAINT_MAX_APPLY_FRAMES` are clamped or rejected by existing store normalization behavior and do not create unbounded generated cache.
    - Persistence: global count changes after a custom override preserve the custom segment and bump `physicPaintVersion`.
  </behavior>
  <action>Add failing store tests using existing `makeFrame`, `physicPaintStore.reset`, MCE serialization, and generated-cache assertions. Tests must assert real-key metadata keeps `sourceFrame` stable while `displayFrame` changes according to override-aware mapping. Include D-01, D-02, D-04, and D-09 citations in test descriptions or comments only where they will not conflict with grep gates.</action>
  <acceptance_criteria>
    - RED tests fail against the current store because override fields are not persisted/regenerated yet.
    - Tests prove generated cache can be rebuilt without relying on stale generated metadata from saved output.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/stores/physicPaintStore.test.ts -x</automated>
  </verify>
  <done>Failing store tests describe durable custom spacing and bounded regeneration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN store normalization, serialization, and regeneration</name>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    @app/src/stores/physicPaintStore.ts
    @app/src/stores/physicPaintStore.test.ts
    @app/src/components/physic-paint/physicsPaintWorkflowState.ts
    @app/src/types/physicPaint.ts
  </read_first>
  <action>Implement store-owned persistence for Plan 01 overrides. Update `_normalizeRotoInterpolationSettings`, `_cloneRotoInterpolationSettings`, `setRotoInterpolationSettings`, `regenerateRotoInterpolationCache`, `toMceOutputs`, and `loadFromMceOutputs` so custom overrides survive serialization and generated cache is regenerated from source real keys plus normalized settings. During load/regeneration, infer missing overrides from distant adjacent source-key gaps per D-01/D-02 when no explicit override already exists, and never infer for compact adjacent/global segments. Preserve real-key cache entries as authoritative and call `_notifyVisualChange()` whenever settings/overrides/regenerated output changes.</action>
  <acceptance_criteria>
    - Store returns normalized settings including `segmentSpacingOverrides`.
    - MCE save/load round-trips explicit overrides and regenerates generated frames at display positions matching the spec examples.
    - Changing global count preserves custom overrides and bumps `physicPaintVersion`.
    - Malformed or excessive overrides cannot generate unbounded cache frames.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/stores/physicPaintStore.test.ts -x</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <done>Store persistence/regeneration tests and typecheck pass without starting the server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCE/project output -> store hydration | Saved settings/cache metadata may be stale or malformed. |
| store mutation -> preview/export subscribers | Missing invalidation can leave stale generated output visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.13-03 | Tampering | `loadFromMceOutputs` | high | mitigate | Normalize overrides, infer only from real source keys, regenerate generated cache, and avoid trusting persisted generated metadata as source truth. |
| T-36.13-04 | Denial of Service | `_regenerateGeneratedRotoCache` | medium | mitigate | Clamp override counts by `PHYSIC_PAINT_MAX_APPLY_FRAMES` and test bounded generated cache output. |
| T-36.13-05 | Repudiation | `physicPaintVersion`/dirty state | medium | mitigate | Route override/settings changes through `_notifyVisualChange()` and assert version bumps in tests. |
| T-36.13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install tasks exist; use existing workspace dependencies only. |
</threat_model>

<verification>
Focused store Vitest and app typecheck. If typecheck requires local package declarations, build the existing workspace package only; do not install or upgrade packages.
</verification>

<success_criteria>
Custom segment spacing is durable in store/MCE state and regenerated output is identical after close/reopen or project save/load.
</success_criteria>

<output>
Create `.planning/phases/36.13-physics-paint-roto-dynamic-interpolation-spacing/36.13-02-SUMMARY.md` when done.
</output>
