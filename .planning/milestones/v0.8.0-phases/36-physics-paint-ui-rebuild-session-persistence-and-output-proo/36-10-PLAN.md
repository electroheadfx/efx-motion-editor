---
phase: 36-physics-paint-ui-rebuild-session-persistence-and-output-proo
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/physicsPaintSessionFile.ts
  - app/src/components/physic-paint/physicsPaintSessionFile.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
gap_closure: true
requirements: [SAVE-01, SAVE-02]
must_haves:
  truths:
    - "UAT gap 6: Save state opens a native Tauri/macOS save dialog in the desktop app so the user can choose the JSON destination."
    - "UAT gap 6: browser fallback still downloads editable JSON when native dialog APIs are unavailable."
    - "UAT gap 6: editable state JSON excludes rendered PNG output and preserves invalid-load behavior."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintSessionFile.ts"
      provides: "Tauri-capable editable-state save adapter with browser fallback"
      exports: ["downloadPhysicsPaintState"]
    - path: "app/src/components/physic-paint/physicsPaintSessionFile.test.ts"
      provides: "Native save, cancel, fallback, and rendered-output exclusion coverage"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Active rebuilt Studio Save state path using native-capable helper"
  key_links:
    - from: "PhysicsPaintStudio.saveEditableState"
      to: "physicsPaintSessionFile native adapter"
      via: "downloadPhysicsPaintState(engine.save())"
      pattern: "downloadPhysicsPaintState"
    - from: "physicsPaintSessionFile.ts"
      to: "@tauri-apps/plugin-dialog and @tauri-apps/plugin-fs"
      via: "dynamic imports in Tauri runtime"
      pattern: "plugin-dialog|plugin-fs|writeTextFile"
---

<objective>
Close UAT gap 6 by making the active rebuilt Save state path use a Tauri/native save dialog in the desktop app, while preserving the browser fallback and editable-JSON-only contract.

Purpose: The user expects Save state to behave like a desktop save operation and choose a path, matching the native feel of Load state. The current active rebuilt path uses an anchor download helper.
Output: `Save state` in `PhysicsPaintStudio` calls a helper that uses `@tauri-apps/plugin-dialog.save` and `@tauri-apps/plugin-fs.writeTextFile` in Tauri, handles cancel cleanly, falls back to browser download outside Tauri, and never includes rendered PNG output in editable state JSON.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/ROADMAP.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UI-SPEC.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-UAT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/debug/phase-36-uat-gap-test-6.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-02-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintSessionFile.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudioToolbar.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add native save adapter tests for Save state</name>
  <files>app/src/components/physic-paint/physicsPaintSessionFile.test.ts</files>
  <behavior>
    - Test 1: When a native save adapter selects a path, serialized editable JSON is written to that path through `writeTextFile` and success copy is returned.
    - Test 2: When native save is cancelled, no write occurs and the helper returns clean cancel behavior without setting an error state.
    - Test 3: When native dialog/fs APIs are unavailable, the existing browser download adapter path still receives filename, JSON contents, and `application/json` MIME type.
    - Test 4: Serialized JSON still excludes rendered PNG/apply payload fields such as `data:image/png`, `renderedFrame`, `frames`, and `PhysicPaintRenderedFrame`.
  </behavior>
  <action>Extend `physicsPaintSessionFile.test.ts` to cover the UAT gap 6 native save behavior through injected adapters/import seams rather than real OS dialogs. Preserve existing invalid-load and rendered-output separation tests. Do not start the app server and do not require a real Tauri runtime in tests.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/physicsPaintSessionFile.test.ts</automated>
  </verify>
  <done>Tests fail before implementation because no native save adapter path exists in the active helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Tauri/native Save state path with browser fallback</name>
  <files>app/src/components/physic-paint/physicsPaintSessionFile.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <behavior>
    - In Tauri, `Save state` dynamically imports `@tauri-apps/plugin-dialog` and `@tauri-apps/plugin-fs`, calls `save` with a JSON filter/default filename, and writes serialized editable JSON with `writeTextFile`.
    - If the user cancels the native save dialog, no file write is attempted and the UI does not show a scary failure.
    - Outside Tauri, the existing Blob/anchor download behavior remains the fallback.
    - The active rebuilt Studio path uses this helper; the legacy toolbar is not the active source of truth.
  </behavior>
  <action>Update `physicsPaintSessionFile.ts` so `downloadPhysicsPaintState` defaults to a native-capable adapter: detect Tauri using the same injected-global/runtime pattern already used in `physicPaintBridge.ts` and the legacy toolbar, dynamically import `@tauri-apps/plugin-dialog` and `@tauri-apps/plugin-fs`, call `save({ defaultPath: filename, filters: [{ name: 'Physics paint state', extensions: ['json'] }] })`, and call `writeTextFile(selectedPath, contents)` only when a path is selected. Preserve the browser Blob/anchor fallback when native save is unavailable or not in Tauri. Ensure cancellation produces clean informational/no-error behavior while keeping the success copy `Saved editable JSON state.` for actual writes/downloads. Adjust `PhysicsPaintStudio.saveEditableState` only if needed so cancellation does not set `lastError`. Keep editable JSON serialization strictly `engine.save()` and do not include rendered PNG output.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/physicsPaintSessionFile.test.ts</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
    <automated>grep -n "plugin-dialog\|plugin-fs\|writeTextFile" /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintSessionFile.ts</automated>
    <automated>grep -n "data:image/png\|PhysicPaintRenderedFrame\|renderedFrame" /Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintSessionFile.ts; test $? -ne 0</automated>
  </verify>
  <done>Clicking Save state in Tauri opens a native save dialog and writes editable JSON to the selected path; browser fallback remains available; editable JSON contains no rendered PNG output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state -> local filesystem | Editable state JSON crosses from app memory to a user-selected path. |
| native dialog result -> fs write | User-selected/cancelled path controls whether a write occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10-01 | Tampering | native save path | mitigate | Only write to path returned by Tauri save dialog; do not fabricate paths or write on cancel. |
| T-36-10-02 | Information Disclosure | state JSON file | accept | User explicitly chooses to save editable session JSON locally; no rendered PNG output included. |
| T-36-10-03 | Denial of Service | failed dialog/fs import | mitigate | Fall back to browser download outside Tauri/unavailable plugins; report write errors clearly. |
| T-36-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
Run targeted session-file tests and typecheck. Manual desktop check: click Save state in Tauri, confirm macOS save dialog appears with JSON filter/default filename, cancel produces no error, selecting a path writes a valid editable JSON state. Browser fallback can be verified in non-Tauri dev context if needed.
</verification>

<success_criteria>
- UAT gap 6 missing item 1 covered: Tauri-capable editable-state save adapter exists.
- UAT gap 6 missing item 2 covered: rebuilt Studio Save state uses native adapter with browser fallback.
- UAT gap 6 missing item 3 covered: tests cover native save, cancel, fallback, and exclusion of rendered PNG output.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-10-SUMMARY.md` when done.
</output>
