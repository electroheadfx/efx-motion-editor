---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 11
type: execute
wave: 13
depends_on:
  - 36.14-19
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts
  - app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoSession.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.ts
autonomous: true
requirements:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION
must_haves:
  truths:
    - "D-01/D-03: The bounded Studio, workflow, session, editing, and Script straggler set contains no reachable source/display, live interpolation-count, frame-pair move, or operation-specific physical mutation route after owner plans 03 and 05-10 complete their deletions."
    - "D-05/D-06/D-07/D-08/D-09: Central UI and workflow callers reach the stable physical action/coordinator/history interfaces created by prior owner plans; Duplicate, Paste-to-empty, and Paste-to-existing semantics come from Plan 20, final key-utility composition comes from Plan 19, and no caller rebuilds mapping, settlement, rollback, or history semantics."
    - "D-04/D-10: Script Copy/Apply remains immutable and reusable, the durable Script library keeps explicit load-to-clipboard behavior, and Play Script keeps deterministic held-pose Motion plus complete physical targeting."
    - "D-06/D-10: Frame editing, persistence coordination, session, and workflow central stragglers resolve stable keyId plus current appFrame; Clear cleanup is owned here through useRotoFrameEditingController.ts and does not recover timing from removed adapters or generated cache provenance."
    - "D-11/D-12: Final production reachability is audited against explicit Plans 01-10 outputs, authoritative Plan 20 and Plan 19 summaries/interfaces, and current importers/callers; no regression artifact is touched or executed before native UAT."
    - "D-12: The final typecheck-then-build command chain was invoked exactly once. Typecheck stopped on obsolete pre-UAT Script source/display test constructions, so build did not run; by explicit user decision, Plan 11 closes on its bounded static production audits and Plan 13 owns the deferred full chain after approved stale-test transfer/update."
  artifacts:
    - "The 14 declared central files are physically wired to prior plans' stable physical ports with genuine stragglers removed."
    - "PhysicsPaintWorkflowStrip.tsx and PhysicsPaintScriptsPanel.tsx use their actual view paths and remain semantic presentation surfaces."
    - "Script clipboard, library, and Play modules preserve approved behavior under stable physical targeting and separate Motion settings."
    - "36.14-11-SUMMARY.md records the bounded reachability matrix, verified removed-module inventory, approved dormant-module non-reachability, Script-preservation evidence, changed central files, the single failed typecheck attempt, the skipped build, and the user-approved Plan 13 deferral."
  key_links:
    - "Plan 06 stable action bundle -> PhysicsPaintStudio -> Workflow Strip and keyboard semantic callbacks without another mapping owner."
    - "Plan 03 physical timeline/read ports -> frame editing/session/workflow central callers -> stable keyId and direct appFrame."
    - "Plan 05 final coordinator/result/history ports -> central orchestration -> one accepted physical transaction route."
    - "Script clipboard/library/Play controllers -> stable physical destination and separate Motion -> existing complete parent-authority path."
    - "Plan 20 semantic Duplicate/Paste ownership + Plan 19 final utility composition + current caller audit + Clear through useRotoFrameEditingController.ts + removed-module absence + approved dormant-module production non-reachability -> bounded static production readiness with transparent compile-gate deferral -> Plan 12 native UAT."
  prohibitions:
    - "No regression file creation, modification, deletion, renaming, or execution; no Vitest or other test runner."
    - "No application, Vite, preview, Tauri, browser, watch, or development server process."
    - "No package installation, removal, upgrade, manifest edit, lockfile edit, generated dependency file, or package-manager audit."
    - "No broad hypothetical cleanup or deletion of files outside the declared 14 central stragglers; completed owner-plan deletions are verified, while the three approval-gated dormant modules remain untouched and production-unreachable until Plan 13."
    - "No source/display adapter, live interpolation-count route, frame-pair move route, operation-specific settlement/history, compatibility wrapper, or direct bridge bypass is introduced or retained in the declared central graph."
    - "No useState/useEffect mirror or duplicate Signal for physical projection, pending edit, history, selection, or Script Motion."
    - "No change to Basic perfect-freehand, FX p5.brush, blend/background, non-Roto Physics Paint, immutable reusable Script clipboard, durable Script library, or deterministic Play Script behavior."
---

## Phase Goal

**As a** stop-motion animator, **I want to** have every real Roto key, generated in-between, timeline edit, persisted frame, and rendered output use one deterministic physical-frame authority, followed by a polished timeline UI that exposes the approved behavior without duplicating business logic, **so that** editing, reopening, previewing, and exporting the same Roto work cannot disagree about frame identity.

<objective>
Perform the bounded final production reachability audit after Plans 01-10, Plan 20, and Plan 19 per D-01 through D-12: treat Plan 20's five foundation owners and Plan 19's five composition owners as authoritative read-only audit surfaces, clean genuine remaining workflow/session/editing/Script stragglers inside the exact 14-file boundary, move Clear onto stable identity through `useRotoFrameEditingController.ts`, verify completed owner-plan deletions plus the production non-reachability of the three approval-gated dormant modules, preserve approved Script behavior, and prove the final central graph reaches stable physical interfaces without a second timing or transaction route.

Purpose: Close only the remaining central composition gaps before native validation. Plan 20 owns Duplicate/Paste operation kinds, complete proposals, shared semantic-delta proof, parent bridge validation before mutation/ledger insertion, parent-echoed discriminated exact acknowledgement, and accepted-only history; Plan 19 owns final Duplicate/Paste/Copy/navigation utility composition and preserves Clear for this plan. Persistence, cache, operation-specific, source/display, and interpolation-count cleanup stays with the owner plans that cut over those consumers; Plan 11 verifies those outputs and repairs only the declared central files. `useRotoPersistenceIntegration.ts`, `rotoSaveTransactions.ts`, and `rotoCacheTransactions.ts` remain dormant solely for stale pre-UAT test imports and are deleted with those tests by Plan 13 after byte-exact Plan 12 approval.

Output: A bounded 14-file production cleanup, stable-identity Clear through `useRotoFrameEditingController.ts`, Script-preservation evidence, an explicit Plan 20/Plan 19/current-importer reachability audit covering Duplicate, both Paste forms, Copy, navigation, Clear, removed modules, and approved dormant modules, plus transparent evidence that the single typecheck-then-build attempt stopped on obsolete pre-UAT tests before build and is deferred to Plan 13 by explicit user decision. Plan 11 then stops ready for user-owned native UAT in Plan 12 on bounded static production evidence only.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-RESEARCH.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PATTERNS.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-VALIDATION.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-UI-SPEC.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-PLAN-OUTLINE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-01-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-02-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-03-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-04-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-05-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-06-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-07-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-08-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-09-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-10-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-20-SUMMARY.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-19-SUMMARY.md

# Plan 20 foundation owners — authoritative read-only audit surfaces
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
@app/src/lib/physicPaintBridge.ts

# Plan 19 composition owners — authoritative read-only audit surfaces
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
@app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts

The executed Plan 01-10, Plan 20, and Plan 19 SUMMARY files, not the pre-phase source shape, are authoritative for actual exported names and final file ownership. Before editing, verify all twelve prerequisite SUMMARY files exist and that Plan 20 reports the closed semantic Duplicate/Paste foundation while Plan 19 reports the exact five-owner composition cutover with unchanged Clear. If a required physical model, resolver, coordinator, history, semantic Duplicate/Paste seam, utility composition, ownership, persistence, or downstream port is absent, stop and report the dependency delta; do not recreate source/display timing, add a compatibility layer, or widen Plan 11 to reimplement an earlier plan.
</context>

<assumptions>
- Executed Plans 01-10 own the physical model, existing resolver/coordinator/history, persistence, cache/live-pixel, rendering, and existing operation-specific cutovers. Plan 20 owns semantic Duplicate/Paste kinds, complete ordinary proposals, shared operation-specific semantic-delta validation, parent bridge enforcement before mutation/ledger insertion, discriminated parent-authoritative acknowledgement, coordinator exact received-field matching, and accepted-only history. Plan 19 owns final Duplicate/Paste/Copy/navigation composition while preserving Clear. All twelve prerequisite SUMMARY files are authoritative for final exports, completed deletions, and approved dormant-module sequencing.
- Plan 03 deletes the source/display model; Plan 05 deletes move-only history and obsolete apply lifecycle/transaction wrappers; Plans 06-08 remove their Insert/Delete/Drag/live-count legacy routes. Plans 09-10 remove all production importers of `useRotoPersistenceIntegration.ts`, `rotoSaveTransactions.ts`, and `rotoCacheTransactions.ts`, but the exact source files remain dormant solely for stale pre-UAT test imports. Plan 11 verifies completed deletions and zero production reachability for those three dormant modules; Plan 13 deletes each dormant module together with its stale test after byte-exact Plan 12 approval.
- Stable `keyId` owns payload and `appFrame` is current placement. Generated cells are runtime-only strict interiors. Script Motion remains separate.
- The declared 14 files are the exact permitted central cleanup boundary. Plan 20's five owners and Plan 19's five owners are audited read-only as authoritative contracts; do not reopen their semantic operation or utility-composition ownership. `PhysicsPaintStudio.tsx` remains listed only as part of the inherited 14-file boundary and current-importer audit, not as permission to revise Plan 19's utility composition. If a required fix belongs to an undeclared owner or to an authoritative Plan 20/19 region, stop and report the owner-plan dependency delta rather than widening or reopening this plan.
- Existing effects remain only for external lifecycle work such as timers, bridge subscriptions, pointer listeners, engine/reference lifecycle, and disposal.
- No dependency, test artifact, test execution, browser, application process, or server is required.
</assumptions>

<scope_boundary>
Included: read-only verification of Plan 20's five owners, Plan 19's five owners, and their current importers/callers; final cleanup in the declared Workflow Strip, Scripts Panel, frame editing/persistence controllers, session/workflow/key controller, and Script clipboard/library/Play modules; stable-identity Clear cleanup through `useRotoFrameEditingController.ts`; verification of completed owner-plan deletions and approved dormant-module production non-reachability; Script behavior preservation; no-test/package audit; chained typecheck/build.

Preserved: Basic perfect-freehand; FX p5.brush; non-Roto Physics Paint; immutable reusable Script clipboard and repeated Apply; explicit library load-to-clipboard; Script scan/save/load/rename/delete and WebP thumbnails; Play Script saved-project/capacity/cancellation/revision/progressive-render/alpha-merge/progress/status/deterministic held-pose behavior.

Excluded: edits to Plan 20's five foundation owners or Plan 19's five composition-owner contracts/regions; persistence/cache/operation module implementation already owned by Plans 03 and 05-10 and 20; Duplicate/Paste/Copy/navigation recomposition already owned by Plan 19; broad repository cleanup; native UAT (Plan 12); regression work (Plans 13-14); final UI work and visual UAT (Plans 15-18); deferred group movement; old-format migration.
</scope_boundary>

<dependency_graph>
- Task 1 verifies Plan 20/19 owner outputs read-only, cleans remaining workflow/frame-edit/persistence/key-controller stragglers, and moves Clear through stable identity in `useRotoFrameEditingController.ts` without reopening Duplicate/Paste/Copy/navigation composition.
- Task 2 depends on Task 1 and preserves Script Copy/Apply/library/Play while removing only central Script adapter stragglers.
- Task 3 depends on Tasks 1-2, audits the bounded production graph, records the already-attempted chained typecheck/build result and its approved Plan 13 deferral without rerunning either command, and stops before native UAT.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Audit Plan 20/19 authority and clean stable-identity Clear and central stragglers</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts, app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts</files>
  <read_first>
- Executed `36.14-03-SUMMARY.md` through `36.14-10-SUMMARY.md` for final physical timeline ports, action bundle, coordinator/history/result settlement, cache/render ownership, and owner-plan deleted-file inventory
- `36.14-20-SUMMARY.md` plus its five foundation owners for authoritative `duplicate-key`/`paste-key` kinds, complete proposals, shared semantic-delta validation, parent bridge enforcement before mutation/ledger insertion, discriminated exact acknowledgement, coordinator matching, accepted output, and ordinary-history classification; audit read-only
- `36.14-19-SUMMARY.md` plus its five composition owners for authoritative Duplicate/Paste/Copy/navigation composition and unchanged Clear handoff; audit read-only
- Current production importers/callers of Plan 20 and Plan 19 public interfaces, discovered from the current graph/source rather than assumed from pre-phase code
- `36.14-CONTEXT.md` D-01 through D-12 and `36.14-PATTERNS.md` for clean-break and Preact composition boundaries
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` for semantic physical actions and presentation-only state
- `app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts` and `useRotoFramePersistenceCoordinator.ts` for current stable-identity Clear/edit/reference/cache ports
- `app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts` only for genuine remaining central helpers outside Plan 20/19 ownership
  </read_first>
  <action>
Per D-01/D-03/D-05 through D-10, first verify that all twelve prerequisite summaries exist. Treat `36.14-20-SUMMARY.md` and `36.14-19-SUMMARY.md` as authoritative: audit Plan 20's exact five owners and Plan 19's exact five owners read-only, then enumerate their current production importers/callers. Verify that Duplicate reaches Plan 20's semantic ordinary proposal and shared parent validator proving destination `sourceAppFrame + 1`, unchanged source and earlier records, exact `+1` ripple for every existing record at or after the destination with stable identity/payload ownership and canonical payload placement retargeting, and exactly one fresh immutable retargeted source-paint clone. Verify Paste-to-empty reaches its fresh-identity creation proposal and parent validator proving exactly one addition at the requested physical target, and Paste-to-existing reaches its destination-identity-preserving proposal and parent validator proving exactly one payload replacement. Verify all three are validated by `physicPaintBridge.ts` before store mutation and ordinary-ledger insertion, return the parent-authoritative discriminated acknowledgement, pass unchanged through the result controller, and settle only after exact coordinator received-field matching before accepted output/history. Verify Plan 19 alone composes those seams while preserving immutable reusable Copy and direct physical navigation. Do not edit or reinterpret those ten owner surfaces in this plan; if their summaries, interfaces, or current reachability disagree, stop with the exact owner/interface delta.

Before central cleanup, verify that the completed owner-plan deletions are absent: `rotoSourceDisplayModel.ts`, `useRotoKeyMoveHistory.ts`, `rotoApplyTransactions.ts`, `useRotoApplyLifecycle.ts`, and `rotoKeyTransactions.ts`. Separately verify that the approval-gated dormant sources `useRotoPersistenceIntegration.ts`, `rotoSaveTransactions.ts`, and `rotoCacheTransactions.ts` still exist byte-for-byte as sequenced by the Plan 09-10 summaries and have zero non-test production importers. If a completed deletion remains, or a dormant module is production-reachable, stop with that plan/file dependency delta; do not delete, migrate, shim, alias, or configure around the dormant modules here.

Keep `PhysicsPaintWorkflowStrip.tsx` on the actual view path and presentation-only: semantic Insert/Delete/Drag/Force Spacing and Plan 19 utility callbacks, physical cells, pending/availability/status, and Script actions. It must not regain direct map, bridge, cache, history, interpolation-count, Duplicate, or Paste ownership. Preserve the approved pointer lifecycle and leave final layout/polish to Plans 15-18.

Own the deferred Clear cleanup explicitly in `useRotoFrameEditingController.ts`. Resolve the selected real key through accepted stable `keyId` and its current direct `appFrame`, preserve the approved toolbar/shortcut Clear behavior and live-pixel/cache invalidation contract, and remove any remaining frame-collision, source/display, generated-cell, or dormant-wrapper ownership inference. Route Clear through the existing accepted physical record/edit-buffer authority without adding a new physical operation kind, resolver proposal, coordinator path, history path, bridge bypass, or clipboard behavior. Keep persistence coordination, reference/save behavior, and any genuine retained key-controller helper on stable identity/current placement; remove only central dead imports/types/callbacks and keep unrelated approved behavior unchanged. Use existing Signals/direct derivation and external-lifecycle effects only.
  </action>
  <acceptance_criteria>
- All twelve prerequisite summaries exist; Plan 20's five owners and Plan 19's five owners are audited read-only and match their authoritative semantic-operation and composition contracts.
- Duplicate, Paste-to-empty, and Paste-to-existing each have a current caller path through parent operation-specific semantic-delta validation before mutation/ledger and a parent-echoed discriminated acknowledgement matched without child reconstruction; Copy and navigation have their Plan 19 paths; no alternate semantic operation, proposal, acknowledgement, settlement, or history authority exists.
- The five completed owner-plan legacy deletions are absent before central cleanup; the three approval-gated dormant modules exist with zero non-test production importers, and Plan 11 does not modify or become implementation owner of any of them.
- Workflow Strip remains a thin view and preserves pointer/keyboard semantics without timing reconstruction.
- `useRotoFrameEditingController.ts` owns stable-identity Clear cleanup while preserving approved Clear behavior and without creating a new physical operation/coordinator/history route.
- Frame editing, persistence coordination, and retained key-controller utilities resolve stable identity/current placement; no declared central straggler retains a reachable source/display, live-count, frame-pair move, direct bridge, or operation-specific settlement route.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'),path=require('path');const removed=['app/src/components/physic-paint/roto/rotoSourceDisplayModel.ts','app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts','app/src/components/physic-paint/roto/rotoApplyTransactions.ts','app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts','app/src/components/physic-paint/roto/rotoKeyTransactions.ts'];const dormant=['app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts','app/src/components/physic-paint/roto/rotoSaveTransactions.ts','app/src/components/physic-paint/roto/rotoCacheTransactions.ts'];for(const p of removed)if(fs.existsSync(p))throw new Error('completed deletion still exists: '+p);for(const p of dormant)if(!fs.existsSync(p))throw new Error('approval-gated dormant source missing: '+p);const names=dormant.map(p=>path.basename(p,'.ts'));const skip=new Set(dormant.map(p=>path.normalize(p)));const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);for(const p of walk('app/src')){if(!/\.(ts|tsx)$/.test(p)||/\.(test|spec)\.(ts|tsx)$/.test(p)||skip.has(path.normalize(p)))continue;const s=fs.readFileSync(p,'utf8');for(const n of names)if(s.includes(n))throw new Error('production reference to dormant module '+n+' in '+p);}"</automated>
  </verify>
  <done>Plan 20 semantic ownership and Plan 19 composition ownership are verified read-only through current callers; Clear resolves stable identity through `useRotoFrameEditingController.ts`; the bounded central editing graph contains no alternate timing, operation, persistence, cache, acknowledgement, or history route.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve Script Copy, library, and Play behavior under physical targeting</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts, app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts</files>
  <read_first>
- Task 1 final central physical composition and owner-deletion verification
- Executed `36.14-03-SUMMARY.md`, `36.14-05-SUMMARY.md`, `36.14-09-SUMMARY.md`, and `36.14-10-SUMMARY.md` for stable physical targeting, coordinator result, persistence/cache, and rendering contracts
- `36.14-CONTEXT.md` D-01, D-03, D-04, D-09, D-10, D-12, D-27, and D-28
- `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx` for current Script action order, guarded reasons, and semantic callbacks
- `app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts` and `useRotoPlayScriptController.ts` for immutable clipboard, launch lifecycle, cancellation, status, and bridge boundaries
- `app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts` and `physicsPaintRotoPlayScriptRenderer.ts` for physical targeting, capacity, held-pose Motion, progressive rendering, alpha merge, and complete commit
- Plan 20's five foundation owners and Plan 19's five composition owners for the bounded ten-owner semantic audit; `useRotoFrameEditingController.ts` is the separate Clear-ownership sentinel
  </read_first>
  <action>
Per D-04/D-09/D-10, preserve Script Copy/Apply and Play semantics while removing only genuine central adapter residue. The Scripts Panel remains on its actual view path, emits semantic actions in the approved order, and displays guarded reasons without physical mapping or transport logic.

Keep `useRotoScriptClipboardController` connected to the immutable reusable clipboard/library snapshot and stable current physical target. Copy does not consume source data; Apply does not consume clipboard data; repeated Apply and routine refresh remain valid. Destination resolution uses stable target identity plus current direct frame, with empty-target creation and replacement routed through the established complete parent-authority port. Remove any remaining source/display claim or old transaction-wrapper dependency without replacing it with a compatibility adapter.

Keep `useRotoPlayScriptController`, the Play controller, and renderer behavior intact: saved-project and capacity guards, immutable Script snapshot, deterministic held-pose deformation/position, progressive rendering, cancellation, revision recheck, existing-paint alpha merge, progress/status, and one complete parent commit. Local contracts use stable physical identity/direct placement and separate Motion settings. If the external engine API still names a destination argument with legacy wording, confine that spelling to the direct package call and derive its value from physical `appFrame`.

Use explicit controller actions and existing Signals; effects remain only for external Script/engine/bridge lifecycle and disposal. Do not alter final UI geometry, Script library storage policy, thumbnail format, or unrelated paint behavior.

Before completing this task, run the bounded static audit in `<verify>` across all ten authoritative Plan 20/19 owners. It must prove both ordinary kinds exist in the shared union and both runtime validators; the canonical resolver has explicit Duplicate/Paste branches and invokes `validatePhysicPaintRotoPhysicalEditSemanticDelta` before staging; the parent bridge routes both ordinary kinds, invokes that same validator before `replaceRotoPhysicalDocument` and before `acceptedPhysicalCommands.set`, and constructs the complete parent result tuple including the actual accepted revision; the coordinator matches received result fields directly and contains neither the old bridge-result reconstruction object nor an accepted-revision fallback; history classifies both kinds as ordinary; the five Plan 19 owners contain none of the three removed legacy utility symbols or two removed temporal-bridge symbols; and `useRotoFrameEditingController.ts` still exports the Clear owner. Scope source/display/count/spacing checks only to the three Plan 19 utility contract regions and Studio's bounded `keyUtilities` object ending before the stable Script controller boundary. Do not inspect later Script, external-engine, result, telemetry, cache-display, or performance fields with that negative concept check.
  </action>
  <acceptance_criteria>
- Scripts Panel remains semantic presentation with approved action order and guarded reasons.
- Copy/Apply clipboard content stays immutable, reusable, and non-consuming across repeated targets and refreshes.
- Script target selection uses stable physical identity/current placement and no source/display or selected-frame transaction adapter.
- Play Script preserves deterministic Motion, guards, cancellation, revision checks, progressive render, alpha merge, progress/status, and complete parent commit.
- No Script-local compatibility timing model or duplicate reactive authority is introduced.
- The automated ten-owner audit proves shared ordinary kinds/validators, resolver semantic branches and delta proof, parent validation-before-mutation-before-ledger ordering plus exact result tuple, coordinator received-result matching without reconstruction/fallback, ordinary history classification, Plan 19 legacy/temporal-symbol absence, bounded concept scans that preserve unrelated published fields, and Clear ownership through `useRotoFrameEditingController.ts`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p20=['app/src/types/physicPaint.ts','app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts','app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts','app/src/lib/physicPaintBridge.ts'];const p19=['app/src/components/physic-paint/PhysicsPaintStudio.tsx','app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts','app/src/components/physic-paint/hooks/useRotoNavigationCoordinator.ts','app/src/components/physic-paint/hooks/useRotoTimelineActions.ts','app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts'];const scripts=['app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx','app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts','app/src/components/physic-paint/hooks/useRotoScriptClipboardController.ts','app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts','app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts'];const clear='app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts';const all=[...p20,...p19,...scripts,clear];for(const p of all)if(!fs.existsSync(p))throw new Error('missing bounded audit owner '+p);const text=Object.fromEntries([...p20,...p19].map(p=>[p,fs.readFileSync(p,'utf8')]));const between=(value,start,end)=>{const a=value.indexOf(start);if(a&lt;0)throw new Error('missing scope '+start);const b=value.indexOf(end,a+start.length);if(b&lt;0)throw new Error('missing scope end '+end);return value.slice(a,b)};const requireTokens=(name,value,tokens)=>{for(const token of tokens)if(!value.includes(token))throw new Error(name+' missing '+token)};const kinds=['duplicate-key','paste-key'];const types=text[p20[0]];for(const [name,scope] of [['operation union',between(types,'export type PhysicPaintRotoPhysicalEditOperationKind =','export interface PhysicPaintRotoPhysicalEditReplayProvenance')],['payload validator',between(types,'export function isPhysicPaintRotoPhysicalEditApplyPayload','export function isPhysicPaintRotoPhysicalEditApplyResult')],['result validator',between(types,'export function isPhysicPaintRotoPhysicalEditApplyResult','export type PhysicPaintApplyKind')]])requireTokens(name,scope,kinds);const resolver=text[p20[1]];requireTokens('resolver',resolver,[...kinds,'validatePhysicPaintRotoPhysicalEditSemanticDelta']);if(!/intent\.kind\s*===\s*['\"]duplicate-key['\"]/.test(resolver)||!/intent\.kind\s*===\s*['\"]paste-key['\"]/.test(resolver))throw new Error('resolver missing explicit semantic branches');const bridge=between(text[p20[4]],'function applyPhysicPaintRotoPhysicalMap','export async function applyPhysicPaintScriptLibraryRequest');requireTokens('parent bridge',bridge,[...kinds,'validatePhysicPaintRotoPhysicalEditSemanticDelta','operationId','operationKind','launchOperationId','layerId','projectContextId','startFrame','expectedRevision','stagedRevision','acceptedRevision','selectedKeyId','selectedAppFrame','ok']);const validation=bridge.indexOf('validatePhysicPaintRotoPhysicalEditSemanticDelta');const mutation=bridge.indexOf('physicPaintStore.replaceRotoPhysicalDocument');const ledger=bridge.indexOf('acceptedPhysicalCommands.set');if(validation&lt;0||mutation&lt;0||ledger&lt;0||!(validation&lt;mutation&amp;&amp;mutation&lt;ledger))throw new Error('parent semantic validation/mutation/ordinary-ledger order is not exact');const coordinator=text[p20[2]];requireTokens('coordinator received-result matching',coordinator,[...kinds,'consumePhysicalEditResult','consumeBridgeApplyResult','detail.operationKind','detail.launchOperationId','detail.expectedRevision','detail.stagedRevision','detail.acceptedRevision','detail.selectedKeyId','detail.selectedAppFrame']);for(const token of [['const full','Result'].join(''),['acceptedRevision: detail.ok ? pending.','stagedRevision : null'].join(''),['detail.acceptedRevision ?? pending.','stagedRevision'].join('')])if(coordinator.includes(token))throw new Error('coordinator reconstructs authoritative result: '+token);const ordinary=between(text[p20[3]],'function isOrdinaryOperationKind','function snapshotRecordsEqual');requireTokens('ordinary history classification',ordinary,kinds);const removed=[['RotoKeyUtility','Transaction'].join(''),['RotoKeyPersistence','Port'].join(''),['saveRealKeyAt','DisplayFrame'].join(''),['rotoTimelineActions','Ref'].join(''),['resolvePasteTargetFor','DisplayFrame'].join('')];for(const file of p19)for(const token of removed)if(text[file].includes(token))throw new Error(file+' retains '+token);const scopes=[['key utilities',between(text[p19[1]],'export interface RotoKeyUtilitiesInput','export interface RotoKeyUtilities')],['navigation',between(text[p19[2]],'export interface UseRotoNavigationCoordinatorInput','export function useRotoNavigationCoordinator')],['timeline actions',between(text[p19[3]],'export interface RotoTimelineActionsInput','interface PhysicalActionRunnerInput')]];const studio=text[p19[0]];const stable=studio.indexOf('const rotoPlayScript = useRotoPlayScriptController');const start=studio.indexOf('keyUtilities: {');const end=studio.indexOf('\n    },\n    playback: {',start);if(stable&lt;0||start&lt;0||end&lt;0||start&gt;=stable||end&gt;=stable)throw new Error('Studio keyUtilities scope is not bounded before Script workflows');scopes.push(['Studio key utilities',studio.slice(start,end)]);const concepts=[['source','Frame'].join(''),['display','Frame'].join(''),['inBetween','Count'].join(''),['segmentSpacing','Overrides'].join('')];for(const [name,value] of scopes)for(const token of concepts)if(value.includes(token))throw new Error(name+' retains temporal concept '+token);const clearText=fs.readFileSync(clear,'utf8');requireTokens('Clear owner',clearText,['export function useRotoFrameEditingController','clearCurrentFrame']);"</automated>
  </verify>
  <done>Script presentation and controllers retain reusable Copy/Apply, durable-library integration, and deterministic Play behavior while targeting the accepted physical model directly; the bounded automated audit proves the complete ten-owner Duplicate/Paste authority, exact parent/coordinator/history wiring, Plan 19 cleanup, scoped temporal checks, and separate Clear ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Clean remaining session/workflow Script stragglers, audit reachability, and record the deferred compile gate</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts, app/src/components/physic-paint/roto/physicsPaintRotoSession.ts, app/src/components/physic-paint/roto/physicsPaintRotoWorkflow.ts</files>
  <read_first>
- Tasks 1-2 final bounded central graph and Script controller behavior
- Executed `36.14-01-SUMMARY.md` through `36.14-10-SUMMARY.md`, plus authoritative `36.14-20-SUMMARY.md` and `36.14-19-SUMMARY.md`, for explicit outputs, deletions, final semantic-operation owners, final utility-composition owners, and current importer expectations
- `36.14-PATTERNS.md`, `36.14-VALIDATION.md`, and `36.14-CONTEXT.md` D-01 through D-12
- `app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts` and `physicsPaintRotoScriptLibrary.ts` for immutable snapshots, durable library metadata, WebP thumbnails, and explicit load semantics
- `app/src/components/physic-paint/roto/physicsPaintRotoSession.ts` and `physicsPaintRotoWorkflow.ts` for remaining central session/workflow identity and cell semantics
  </read_first>
  <action>
Finish the bounded central cleanup per D-01/D-03/D-04/D-10. Keep Script clipboard entries immutable and reusable, keep the durable library's scan/save/load/rename/delete, selection, WebP thumbnail, and explicit load-to-clipboard behavior, and ensure any physical target metadata uses stable identity/direct placement rather than a deleted timing adapter. Keep session/workflow semantics on current physical records and real/generated/empty cells; remove only remaining central aliases or dead fallback branches.

Build the final audit set from the explicit changed-file inventories in Plans 01-10, Plan 20's five read-only foundation owners, Plan 19's five read-only composition owners, the exact 14-file Plan 11 boundary, and current production importers/callers of the final physical timeline, action bundle, semantic Duplicate/Paste intents and complete proposals, shared semantic-delta validator, parent bridge validation/result construction, coordinator exact result matching, history, result listener, utility composition, ownership/cache, persistence, renderer, Clear controller, and Script ports. Verify completed owner-plan deleted modules remain absent, and verify the three approval-gated dormant modules remain present but have zero non-test production importers. Scope negative checks to Roto authority and current importers; do not demand unrelated repository-wide renames or broad hypothetical deletion.

Record a reachability matrix in the Plan 11 SUMMARY for Insert/Delete, Drag, Force Spacing, Undo/Redo, Duplicate, Paste-to-empty, Paste-to-existing payload replacement, immutable reusable key Copy, direct physical navigation, Clear through `useRotoFrameEditingController.ts`, project save/reopen/launch, identity-owned cache/dirty/selection, generated output, playback/onion/reference, preview/export/missing background, timeline extent, Script Copy/Apply, durable Script library, Play Script, and visual invalidation. Each row names the final owner port and confirms no central alternate route. The Duplicate and both Paste rows must name Plan 20 as semantic operation/proposal/shared-delta/parent-bridge/coordinator/history owner, state that parent validation precedes mutation and ordinary-ledger insertion, state that the parent-echoed discriminated acknowledgement is matched without child reconstruction, and name Plan 19 as final composition owner; Copy and navigation must name Plan 19 composition; Clear must name `useRotoFrameEditingController.ts` and must not be attributed to Plan 19. Explicitly record Script behavior preservation, the actual completed owner-plan deletion list, and separate evidence that the three approval-gated dormant modules are production-unreachable and reserved for Plan 13 deletion with their stale tests.

Confirm the Plan 11 diff contains no test/spec/fixture/snapshot, package manifest, lockfile, generated dependency, or unrelated Basic/FX implementation path. The Task 3 chained command was already invoked exactly once and must not be rerun: `pnpm --dir app typecheck && pnpm --dir app build` stopped during typecheck because obsolete pre-UAT tests still construct the removed Script source/display contracts, so the `&&` chain never launched build. By explicit user decision, do not edit, discover, exclude, or execute those tests here and do not add compatibility or configuration bypasses. Record the failure transparently and defer the complete chain to Plan 13 after byte-exact Plan 12 approval unlocks the stale-test contract transfer/update.

After the bounded static production and reachability audits pass, stop and report automated production readiness for user-owned Plan 12 native UAT with the compile gate explicitly deferred. Do not claim typecheck success, build success, UAT approval, regression completion, final UI completion, or phase completion.
  </action>
  <acceptance_criteria>
- Session/workflow and durable Script modules use stable physical semantics without timing adapters while preserving clipboard/library behavior.
- Audit scope is explicit Plans 01-10 outputs, Plan 20's five read-only owners, Plan 19's five read-only owners, the exact 14-file Plan 11 boundary, and current production importers/callers; no broad hypothetical deletion is required.
- Completed owner-plan deleted modules are absent; the three approval-gated dormant modules are present but production-unreachable; every reachability row points to a final owner port, including Plan 20 semantic ownership, Plan 19 composition ownership, and Clear through `useRotoFrameEditingController.ts`.
- Script Copy/Apply, library, and Play behavior preservation is recorded explicitly.
- No regression/package/server boundary is crossed.
- The single Task 3 typecheck-then-build attempt is recorded exactly: typecheck failed only on obsolete pre-UAT Script source/display test constructions, build did not run, and the complete chain is assigned to Plan 13 after approved test transfer/update.
- Execution stops ready for Plan 12 on bounded static production audits without claiming compile success, native approval, or phase completion.
  </acceptance_criteria>
  <verify>
    <automated>Bounded static production audits from Tasks 1-2 plus the no-test/package diff audit. Do not rerun `pnpm --dir app typecheck &amp;&amp; pnpm --dir app build`; record its single failed typecheck attempt, skipped build, and Plan 13 deferral.</automated>
  </verify>
  <done>The bounded production graph and Script workflows are reachability-clean, completed owner-plan deletions and approved dormant-module production non-reachability are verified, the failed typecheck and skipped build are documented without compatibility workarounds, and execution stops ready for user-owned native UAT with the complete compile gate deferred to Plan 13.</done>
</task>

</tasks>

## Artifacts this phase produces

| Path | Artifact | Required interface or behavior |
|---|---|---|
| Plan 20's five owners and Plan 19's five owners | Authoritative read-only audit surfaces | Prove semantic Duplicate/Paste proposals, parent bridge delta validation/exact acknowledgement, coordinator matching, and accepted history remain in Plan 20, while final Duplicate/Paste/Copy/navigation composition remains in Plan 19; do not reopen these contracts. |
| `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` | Final central workflow presentation | Emits semantic physical actions and preserves pointer/keyboard behavior without timing or transaction ownership. |
| `app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx` | Script action presentation | Preserves approved action order and guarded reasons through semantic controller callbacks. |
| Frame-edit, persistence, key-controller, session, and workflow files | Stable central consumers | Resolve identity/current placement, route Clear through `useRotoFrameEditingController.ts`, and contain no owner-deleted operation or timing adapter. |
| Script clipboard/controller/library/Play files | Preserved physical Script workflows | Keep immutable reusable Apply, durable library lifecycle, explicit load-to-clipboard, deterministic Motion, progressive render, alpha merge, and complete commit. |
| Owner-output, caller-reachability, and dormant-module audit | Verified prior ownership | Confirms Plan 20/19 authority through Duplicate, both Paste forms, Copy, navigation, and Clear callers; confirms completed source/display and operation deletions are absent; and confirms the three approval-gated persistence/cache modules remain present with zero non-test production importers until Plan 13. |
| `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md` | Production-readiness evidence | Records bounded Plan 11 changed files, verified completed deletions, dormant-module production non-reachability, reachability matrix, Script preservation, no-test/package proof, the single failed typecheck attempt, skipped build, approved Plan 13 deferral, and Plan 12 readiness. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 20 semantic/parent-authority owners and Plan 19 composition owners -> current callers | Final ports can be bypassed if a caller reconstructs Duplicate/Paste proposals or acknowledgement fields, skips parent semantic-delta validation, or duplicates settlement, history, Copy, navigation, or Clear ownership. |
| Script snapshot/library -> physical target and renderer | Reusable Script data must preserve immutable behavior while resolving current stable physical targets. |
| Current import graph -> removed or approval-gated dormant modules | A stale production import or wrapper can silently restore a second operation, persistence, or cache route even when the source exists only for pre-UAT tests. |
| Production source -> deferred compile gate | Test/package changes, compatibility bypasses, or rerunning the already-failed chain before approved stale-test transfer can invalidate the UAT-first boundary. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-36.14-11-01 | Spoofing | Central selected/Script target ownership | high | mitigate | Resolve stable keyId through final physical ports and derive current appFrame; reject unknown identities without frame fallback. |
| T-36.14-11-02 | Tampering | Duplicate/Paste/Copy/navigation/Clear alternate routes | high | mitigate | Audit Plan 20's operation/proposal/shared-delta/parent-bridge/coordinator/history owners and Plan 19's final utility composition read-only; require parent delta validation before mutation/ledger and exact parent-echoed acknowledgement matching; move only Clear through `useRotoFrameEditingController.ts`; reject caller-local proposals, reconstructed results, settlement, history, navigation timing, or bridge writes. |
| T-36.14-11-03 | Repudiation | Owner-output and dormant-module audit | medium | mitigate | Verify the explicit completed-deletion list, the approved dormant list, and current production importers, then record final owners and reachability rows in the summary. |
| T-36.14-11-04 | Tampering | Script reuse and Play behavior | high | mitigate | Preserve immutable clipboard/library snapshots, separate Motion, revision/cancellation checks, deterministic held poses, and one complete parent commit. |
| T-36.14-11-05 | Denial of Service | Play Script lifecycle | medium | mitigate | Retain capacity bounds, progressive render, cancellation, revision recheck, and existing cleanup without compatibility scans. |
| T-36.14-11-06 | Elevation of Privilege | Removed and approval-gated dormant wrappers | high | mitigate | Require completed deletions to be absent and dormant modules to have zero non-test production importers; do not create forwarding aliases, declarations, or configuration bypasses. |
| T-36.14-11-07 | Tampering | Preact reactive state | medium | mitigate | Keep shared truth in existing Signals/coordinator/history and effects only for external lifecycle cleanup. |
| T-36.14-11-08 | Tampering | Production gate ordering | medium | mitigate | Preserve the single attempted `typecheck && build` result without rerun or bypass: typecheck failure stops build, Plan 12 establishes native truth, and Plan 13 owns the full chain after approved stale-test transfer/update. |
| T-36.14-11-SC | Tampering | pnpm package supply chain | low | accept | No package installation, audit, manifest change, or lockfile change occurs; pnpm invokes existing scripts only. |
</threat_model>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One deterministic physical authority reaches final central and Script consumers | Tasks 1-3 verify Plans 01-10 plus Plan 20 semantic ownership and Plan 19 composition ownership, clean bounded stragglers including stable-identity Clear, and audit final caller reachability | COVERED |
| REQ | `36.14-PHYSICAL-IDENTITY` | Central edit/session/Script targets use stable keyId and current appFrame | COVERED |
| REQ | `36.14-DERIVED-INTERPOLATION` | Workflow/session central files consume semantic cells and retain no live-count authority | COVERED |
| REQ | `36.14-ATOMIC-FRAME-MAPPING` | Duplicate and both Paste forms are audited through Plan 20's complete proposal, parent semantic-delta validation before mutation/ledger, discriminated exact acknowledgement, coordinator matching, and history authority; central callers do not publish directly | COVERED |
| REQ | `36.14-DOWNSTREAM-PARITY` | Reachability matrix verifies Plan 20 semantic owners, Plan 19 utility composition, Clear through frame editing, prior persistence/cache/render owners, and central/Script consumers | COVERED |
| REQ | `36.14-UAT-THEN-REGRESSION` | No regression work; chained production gate stops before Plan 12 | COVERED |
| RESEARCH | Stable extracted interfaces reduce Studio orchestration | Task 1 treats Plan 20/19 owners as authoritative read-only contracts and audits current callers rather than recreating operation or composition semantics | COVERED |
| RESEARCH | Preact Signals/direct derivation with lifecycle-only effects | Tasks 1-2 and prohibitions | COVERED |
| RESEARCH | Script behavior remains unchanged under physical targeting | Tasks 2-3 | COVERED |
| CONTEXT | D-01 through D-10 | Plans 01-10 owner-output verification, Plan 20 semantic ownership, Plan 19 composition ownership, stable-identity Clear cleanup, Script preservation, and caller reachability audit | COVERED |
| CONTEXT | D-11 bounded sequential plan | Wave 13 depends on Plan 19; exact 14-file boundary is preserved while Plan 20/19 owners are audited read-only | COVERED |
| CONTEXT | D-12 production before native UAT/regression | Task 3 chained gate and hard stop | COVERED |
| CONTEXT | Deferred migration and group movement | Not implemented | EXCLUDED — DEFERRED |
| UI-SPEC | Final visual geometry and polish | Remains Plans 15-18 | COVERED ELSEWHERE |

## Production Consumer Reachability Audit

The executor must verify and record each row in `36.14-11-SUMMARY.md`; no row may be marked through an adapter, alias, fallback, or duplicated local derivation.

| Consumer group | Required authority path |
|---|---|
| Insert / Delete | Semantic action -> pure physical resolver -> acknowledged coordinator -> accepted history |
| Drag preview / commit | One immutable resolver proposal -> preview -> same proposal through coordinator -> accepted history |
| Force Spacing | Raw session input -> resolver complete mapping -> coordinator -> accepted history; no persisted count/override |
| Undo / Redo | Generic history complete mapping -> same coordinator -> exact acknowledgement |
| Duplicate | Plan 19 supplies only the stable source identity -> Plan 20 `duplicate-key` intent derives destination exactly `sourceAppFrame + 1`, preserves source/earlier records, ripples every existing record at/after destination exactly `+1` with identity/payload ownership retained and canonical placement retargeted, and adds one fresh immutable source-paint clone -> complete next-record proposal -> parent bridge independently proves that full delta before mutation/ordinary-ledger insertion -> parent-echoed discriminated acknowledgement -> coordinator exact received-field match -> Plan 20 ordinary accepted history |
| Paste to empty | Plan 19 immutable clipboard composition -> Plan 20 `paste-key` creation intent and one fresh identity -> complete next-record proposal -> parent bridge proves exactly one declared addition before mutation/ordinary-ledger insertion -> parent-echoed discriminated acknowledgement -> coordinator exact received-field match -> Plan 20 ordinary accepted history |
| Paste to existing | Plan 19 immutable clipboard composition -> Plan 20 `paste-key` replacement intent -> preserved destination `keyId` and `appFrame` with exactly one payload replacement -> parent bridge proves unchanged identity/frame sets and all other payloads before mutation/ordinary-ledger insertion -> parent-echoed discriminated acknowledgement -> coordinator exact received-field match -> Plan 20 ordinary accepted history |
| Key Copy | Plan 19 immutable reusable copied-paint snapshot; no physical mutation, consumption, timing reconstruction, or history action |
| Navigation | Plan 19 direct physical `appFrame` route; no source/display projection or mutation authority |
| Clear | Toolbar/shortcut route -> `useRotoFrameEditingController.ts` -> accepted stable `keyId` and current direct `appFrame` -> existing edit-buffer/live-pixel/cache invalidation authority; no new physical operation/coordinator/history route |
| Project save / reopen / launch | Closed project/bridge validator -> complete physical records -> atomic store/ownership publication |
| Paint/editable/cache/dirty/selection | Stable `keyId` ownership -> current accepted `appFrame`; no frame collision ownership |
| Generated output | Enabled plus adjacent real keys -> runtime strict interior -> adjacent identity/revision-bound render cache |
| Playback / onion / reference | Accepted physical projection and exact identity-owned render lookup |
| Preview / export / missing background | Shared Plan 10 physical renderer and semantic cell; no local timing recovery |
| Timeline extent | Highest accepted real-key direct `appFrame` plus one; no generated-cache authority |
| Script Copy / Apply | Immutable reusable Script snapshot -> stable physical target -> canonical complete parent authority |
| Durable Script library | Closed physical Script metadata -> explicit immutable clipboard load; no coordinate alias |
| Play Script | Stable physical selection and capacity -> separate Motion -> progressive render -> complete physical batch through parent authority |
| Visual invalidation | Accepted visual publication -> one monotonic `physicPaintVersion`; never identity/equality/revision |

## Edge-Case Coverage

| Edge | Required deterministic result |
|---|---|
| Empty project or no real keys | Valid empty physical state; no generated cells, selected owner, cache-derived timing, or Roto extent. |
| One real key | Stable identity at one direct frame; no generated cells regardless of interpolation enabled state. |
| Adjacent real keys | No strict interior generated cell. |
| Wide gap with interpolation enabled | Every and only strict interior integer is generated at runtime; no stored count chooses a subset. |
| Interpolation disabled | Gap cells are empty; key placement and Motion remain unchanged. |
| Duplicate key ID, duplicate appFrame, out-of-capacity frame, partial mapping | Reject the complete payload/proposal; publish no partial state, history, or visual invalidation. |
| Duplicate with unknown source, reused identity, destination overflow/capacity failure, incomplete or incorrect `+1` ripple, payload-ownership transfer, stale revision, or mismatched acknowledgement | Plan 20 rejects atomically before acceptance or rolls back through the existing coordinator; an occupied adjacent destination is valid and must ripple, while Plan 19 and current callers provide no fallback mutation route. |
| Paste-to-empty with malformed payload, reused identity, occupied/out-of-capacity destination, stale revision, or mismatched acknowledgement | Plan 20 rejects before acceptance or rolls back through the existing coordinator; Plan 19 and current callers provide no fallback mutation route. |
| Paste-to-existing | Preserve destination `keyId` and `appFrame`, replace only immutable payload retargeted to that frame, and record one accepted ordinary command after exact acknowledgement. |
| Repeated Paste after Copy | Plan 19 clipboard remains immutable and reusable; each accepted Paste resolves independently through Plan 20 without consuming copied paint. |
| Clear on stale/generated/unknown selection | `useRotoFrameEditingController.ts` rejects or resolves through the accepted real-key identity contract; no frame-collision or generated-cache ownership fallback. |
| Move to the same complete mapping | Semantic no-change; no parent mutation, accepted history, or invalidation. |
| Stale/unknown/deleted key in UI or Script target | Reject or clear selection according to the established physical contract; never retarget by frame collision. |
| Acknowledgement mismatch, timeout, cancellation, launch replacement | Exact pending operation does not accept; complete rollback/cancellation path leaves no accepted history. |
| Undo/Redo after another accepted edit | Revalidate complete replay through the coordinator; reject stale replay rather than patching a pair. |
| Generated cache with stale anchors/revisions | Ignore as unavailable derived output; it cannot establish a cell or borrow neighboring paint. |
| Async live-pixel completion after move/replacement | Stable identity/revision ownership prevents stale pixels from publishing as current. |
| Reopen malformed Roto data | Fail the Roto payload closed; no source/display or partial-frame recovery. |
| Script applied repeatedly | Clipboard remains immutable and reusable; each target resolves independently to current physical identity. |
| Play Script cancellation or revision change | Preserve existing cancellation/recheck behavior and avoid partial parent publication. |
| External engine field uses published source-frame spelling | Allow only at direct package call with value derived from target physical `appFrame`; no local propagation. |
| Basic or FX paint present beside Roto | Existing perfect-freehand/p5.brush paths, blend modes, and backgrounds remain unchanged. |
| Test or package file appears in Plan 11 diff | Fail before final typecheck/build and report boundary violation. |

<verification>
1. Confirm all twelve prerequisite SUMMARY files exist: Plans 01-10 plus Plan 20 and Plan 19. Extract their actual final owners, semantic operation/composition interfaces, and deleted-module inventory.
2. Confirm the five completed owner-plan legacy deletions named in Task 1 are absent, and confirm the three approval-gated dormant modules are present but have zero non-test production importers; report any mismatch instead of deleting, migrating, or configuring around it here.
3. Audit Plan 20's five owners and Plan 19's five owners read-only, then inspect their current production importers/callers plus the exact 14-file Plan 11 boundary for reachable timing adapters, caller-local Duplicate/Paste proposals, missing parent operation-specific semantic-delta validation, mutation/ledger ordering violations, generic or child-reconstructed acknowledgements, accepted-revision fallback, alternate history authority, and duplicate reactive authority.
4. Complete the positive reachability matrix for Duplicate, Paste-to-empty, Paste-to-existing payload replacement, Copy, navigation, Clear, all other edits, persistence/cache/render, timeline, Script, and visual-invalidation consumers. Name Plan 20 as Duplicate/Paste semantic proposal, parent validation/exact acknowledgement, coordinator matching, and history owner; name Plan 19 as final utility composition owner; and name `useRotoFrameEditingController.ts` as Clear owner.
5. Confirm immutable reusable Script Copy/Apply, durable library lifecycle/WebP thumbnails/explicit load, and deterministic Play Script behavior remain present.
6. Confirm no test/spec/fixture/snapshot, package manifest, lockfile, generated dependency, server, or unrelated Basic/FX file is changed or executed.
7. Do not rerun Task 3's already-attempted compile chain. Record that typecheck failed on obsolete pre-UAT Script source/display test constructions and that build did not run because `&&` stopped the chain.
8. Stop after the bounded static audits and create the Plan 11 SUMMARY with transparent compile-gate deferral and Plan 12 native-UAT readiness wording only.
</verification>

<success_criteria>
- All completed owner-plan legacy deletions are verified absent; the three approval-gated dormant modules are verified present and production-unreachable; none is broadly reimplemented, deleted, shimmed, or configured around in Plan 11.
- Plan 20's five semantic/parent-authority owners and Plan 19's five composition owners match their authoritative summaries under read-only audit; Duplicate/Paste parent delta validation precedes mutation/ledger insertion; the discriminated parent acknowledgement is passed and matched without reconstruction/fallback; and current callers contain no alternate proposal, settlement, rollback, history, Copy, or navigation authority.
- The exact 14-file Plan 11 boundary consumes stable physical ports with no reachable source/display, live-count, frame-pair, direct-bridge, or operation-specific settlement route.
- Clear resolves accepted stable identity/current placement through `useRotoFrameEditingController.ts` without a new physical operation, coordinator, history, or bridge path.
- Script Motion remains separate; Copy/Apply remains immutable and reusable; the durable library and deterministic Play Script behavior remain intact under physical targeting.
- The bounded reachability matrix proves Duplicate, both Paste forms, Copy, navigation, Clear, every other edit, and every downstream consumer reaches its final owner without an alternate central path.
- Existing Signals/direct derivation and lifecycle-only effects are preserved; Basic, FX, and non-Roto Physics Paint remain unchanged.
- No regression artifact, package boundary, test execution, or server process occurs.
- The single Task 3 typecheck-then-build attempt is documented honestly: obsolete pre-UAT Script tests caused typecheck failure, build was not launched, no bypass was added, and Plan 13 owns the deferred full chain after approved stale-test transfer/update; execution stops ready for user-owned Plan 12 native UAT without claiming compile success, approval, or phase completion.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-11-SUMMARY.md` when execution is complete. Include the bounded Plan 11 changed-file inventory; read-only Plan 20 five-owner and Plan 19 five-owner audit; evidence that parent Duplicate/Paste semantic-delta validation precedes mutation/ledger insertion and that the parent-echoed discriminated result is matched without reconstruction/fallback; current importer/caller evidence; explicit Duplicate, Paste-to-empty, Paste-to-existing, Copy, navigation, and Clear reachability rows; stable-identity Clear ownership through `useRotoFrameEditingController.ts`; separately verified completed-deletion inventory; approval-gated dormant-module production non-reachability evidence; full consumer reachability matrix; external engine call allowlist if present; no-test/package diff proof; and exact compile-gate evidence. State that the sole `pnpm --dir app typecheck && pnpm --dir app build` attempt failed during typecheck because obsolete pre-UAT Script source/display tests still construct removed contracts, that build therefore did not run, that no compatibility/configuration bypass was added, and that the user explicitly deferred the complete chain to Plan 13 after approved stale-test transfer/update. Report only bounded static production readiness for Plan 12 user-owned native UAT; do not report typecheck/build success, native approval, UAT success, phase completion, regression completion, or final UI completion.
</output>
