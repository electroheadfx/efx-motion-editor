---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 21
subsystem: physics-paint-roto
tags: [preact, signals, immutable-snapshots, accepted-history, undo-redo, stable-identity]

requires:
  - phase: 36.14-11
    provides: One active acknowledged physical-edit coordinator, accepted-only history, stable keyId/direct appFrame authority, and bounded pre-UAT recovery sequencing
  - phase: 36.14-20
    provides: Ordinary Duplicate/Paste physical operations with exact parent acknowledgement and accepted-operation provenance
provides:
  - Complete immutable before/after child snapshots including confirmed frames, buffers, caches, reference, selection, current frame, and engine state
  - Exact target-snapshot Undo/Redo staging for Insert, Delete, Move, Force Spacing, Duplicate, and Paste
  - History cursor movement guarded by exact command ID, direction, source revision, target revision, launch, layer, project, capacity, selection, and current-frame authority
  - Stable hook-local Signals and complete rollback on rejection, timeout, mismatch, transport failure, launch replacement, and disposal
affects: [36.14-22, 36.14-28, 36.14-29, roto-history, roto-native-uat]

tech-stack:
  added: []
  patterns:
    - Preact useSignal values remain hook-stable while refs own serialized pending transaction state
    - Parent acknowledgement authorizes canonical records and interpolation; complete child snapshots remain local
    - Physical history stacks move only after one accepted output exactly matches the pending immutable replay command

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx

key-decisions:
  - "All six ordinary physical operation kinds share one narrowed immutable history command type; undo and redo can never be recorded as ordinary commands."
  - "Replay stages the stored before or after child snapshot directly, while the bridge continues to receive only canonical records, interpolation, selection, revision, and provenance."
  - "History availability changes only after the accepted replay output matches command identity, direction, source and target revisions, launch, layer, project, capacity, selection, and current frame."
  - "D-30 remains authoritative: this plan uses bounded static checks only and makes no native-UAT or phase-completion claim."

patterns-established:
  - "Exact replay target: Undo dispatches entry.before and Redo dispatches entry.after through replayTargetSnapshot."
  - "Accepted-only cursor: pending replay is consumed without stack movement unless every accepted authority field matches the stored command."
  - "Combined ledger: physical commands and established paint barriers continue to publish one availability count without exposing pending or rejected physical work."

requirements-completed:
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-ATOMIC-FRAME-MAPPING
  - 36.14-RIPPLE-INSERT-DELETE
  - 36.14-RIPPLE-DRAG
  - 36.14-FORCE-SPACING
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-UAT-THEN-REGRESSION

coverage:
  - id: D1
    description: "Undo and Redo stage the exact immutable child target for every accepted physical command, including identity-changing Delete, Insert, Duplicate, and Paste."
    requirement: 36.14-ATOMIC-FRAME-MAPPING
    verification:
      - kind: other
        ref: "Plan 36.14-21 bounded Task 2 static token and diff check"
        status: pass
    human_judgment: true
    rationale: "Static review proves the single replay path and complete snapshot wiring; integrated visible behavior remains assigned to the later user-owned native gap UAT."
  - id: D2
    description: "History stacks and combined availability move only after exact parent-authorized replay acceptance; rejected, mismatched, timed-out, or no-change work remains history-neutral."
    requirement: 36.14-DOWNSTREAM-PARITY
    verification:
      - kind: other
        ref: "Bounded single-coordinator, single-history, accepted-subscription, and pending-replay authority review"
        status: pass
    human_judgment: true
    rationale: "Native multi-level interleaving with engine paint barriers is intentionally deferred by D-30 to the later explicit UAT plan."
  - id: D3
    description: "Stable useSignal identities, direct keyId/appFrame ownership, complete rollback, first-paint target creation, payload retargeting, and accepted Paste refresh remain preserved."
    requirement: 36.14-PHYSICAL-IDENTITY
    verification:
      - kind: manual_procedural
        ref: "Mandatory Task 1 tracer checkpoint approved by the user before Task 2 continuation"
        status: pass
    human_judgment: true
    rationale: "The approved tracer establishes the working slice, but this summary does not substitute it for Plan 29's integrated native gap UAT."

duration: 5min
completed: 2026-07-24
status: complete
---

# Phase 36.14 Plan 21: Exact Accepted Physical Replay Summary

**Complete child-owned physical snapshots now replay through the sole acknowledged coordinator, with all six command kinds moving Undo/Redo history only after exact accepted provenance**

## Performance

- **Duration:** 5 min resumed execution
- **Started:** 2026-07-24T05:57:12Z
- **Completed:** 2026-07-24T06:01:55Z
- **Tasks:** 2
- **Production files modified:** 4

## Accomplishments

- Verified Task 1 commit `cb062bf0` exists at the continuation base, changes exactly the four Plan 21 production owners, preserves hook-stable `useSignal()` allocations, and carries complete `confirmedFrames` plus `replayTargetSnapshot` capture/stage/restore wiring.
- Generalized the immutable history command type across `insert-slot`, `delete-key`, `move-key`, `force-spacing`, `duplicate-key`, and `paste-key`, while excluding replay kinds from ordinary command insertion.
- Required accepted Undo/Redo output to match the pending command's direction, command ID, source revision, target revision, accepted revision, launch, layer, project context, capacity, selection, and current frame before moving one entry between applied and redo stacks.
- Preserved direct target-snapshot staging for identity-owned frame states, preview/captured/confirmed frames, dirty/live-overlay state, editable frames, cached reference/repaint base, selection, current frame, and engine state without expanding the parent bridge authority.
- Preserved the established interleaved paint-barrier ledger: pending or rejected physical work does not change availability, accepted ordinary work clears redo once, and engine history events remain the sole paint-barrier reconciliation source.

## Task Commits

1. **Task 1: Replay one Insert command through an exact immutable target snapshot** - `cb062bf0` (fix)
2. **Task 2: Generalize exact accepted-only replay across every physical command and availability transition** - `04437412` (fix)

## Files Created/Modified

- `app/src/components/physic-paint/roto/rotoCoordinatorPorts.ts` - Extends the complete snapshot/buffer contract with confirmed frames and one child-local immutable replay target.
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` - Keeps four hook-stable Signals, stages exact replay targets locally, sends only canonical parent authority, and restores the captured current snapshot on every failure path.
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` - Routes all six ordinary command kinds through immutable before/after targets and guards stack movement with exact accepted replay authority.
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - Wires confirmed-frame capture/restore, buffers, cached reference/repaint base, engine state, selection, and the existing single coordinator/history composition.

## Decisions Made

- Ordinary physical history is a closed six-kind union. `undo` and `redo` are replay directions only and cannot enter the ordinary command ledger.
- The parent remains authoritative only for canonical physical records, interpolation, revision, selection, and provenance. Child caches, buffers, confirmed frames, references, and serialized engine state never cross the bridge.
- A replay acknowledgement is insufficient by operation ID alone. The history cursor moves only when the accepted source and target authority and all four provenance members match the immutable pending command.
- D-30 continues to prohibit tests, typecheck, build, package, server, browser, Tauri, and native application execution during this recovery plan.

## Deviations from Plan

None - Plan 21 was completed within its declared four-file production boundary and bounded static verification contract.

## Issues Encountered

None. Task 1 already established the complete exact-snapshot tracer; Task 2 narrowed ordinary command typing and completed exact accepted-output cursor guards without introducing another coordinator, history hook, pending replay reference, subscription, or state authority.

## Deferred Issues

None introduced by Plan 21. Integrated visible validation remains intentionally assigned to the later D-30 native gap UAT sequence.

## Known Stubs

None. Null values, empty collections, and no-op defaults in the reviewed owners are explicit absence/initialization states or pre-existing port defaults, not Plan 21 placeholder UI data or an unwired replay path.

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required.

## Verification

Passed only the Plan 21-authorized bounded static checks:

- Confirmed commit `cb062bf0` exists, is preserved as the continuation base, and contains Task 1's four declared production owners.
- Ran Task 2's exact static token/authority command and `git diff --check` across the four Plan 21 production owners.
- Performed the bounded static authority review confirming one coordinator hook, one history hook, one accepted-output subscription, one pending replay reference, and one Studio history composition.
- The user approved the mandatory Task 1 tracer human-verify checkpoint before this continuation.

No tests were created, modified, discovered, or executed. No typecheck, build, package, server, browser, Tauri, native application, install, audit, manifest, lockfile, compatibility, migration, alias, wrapper, dual-write, or second-authority action occurred.

## Next Phase Readiness

- The complete accepted-only physical replay path is production-ready for the remaining bounded recovery plans and Plan 28's integrated static review.
- Integrated native behavior remains for the explicit user-owned Plan 29 gap UAT; this summary does not claim native success or Phase 36.14 completion.
- Plans 13-18 remain unexecuted, and no `36.14-12-APPROVAL.txt` or replacement approval token was created.

## Self-Check: PASSED

- All four Plan 21 production owners and this summary exist.
- Task commits `cb062bf0` and `04437412` exist in Git history.
- The bounded Task 2 verification passed, and unrelated dirty production/debug files remain unstaged and preserved.

---
*Phase: 36.14-physics-paint-roto-timeline-ui-from-pencil*
*Completed: 2026-07-24*
