---
phase: 36.14-physics-paint-roto-timeline-ui-from-pencil
plan: 30
type: execute
wave: 27
depends_on:
  - 36.14-29
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts
  - app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts
  - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/lib/previewRenderer.test.ts
  - app/src/lib/exportRenderer.test.ts
  - app/src/lib/frameMap.test.ts
  - app/src/lib/physicPaintPersistence.test.ts
  - app/src/lib/physicPaintPlayScriptBridge.test.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts
  - app/src/components/physic-paint/hooks/useRotoPlayScriptController.test.ts
  - app/src/components/physic-paint/hooks/useRotoReferenceController.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
autonomous: false
requirements:
  - 36.14-UAT-THEN-REGRESSION
  - 36.14-DOWNSTREAM-PARITY
  - 36.14-PHYSICAL-IDENTITY
  - 36.14-DERIVED-INTERPOLATION
  - 36.14-ATOMIC-FRAME-MAPPING

must_haves:
  truths:
    - "Final gate green in one pass from repo root: pnpm --filter efx-motion-editor typecheck exits 0, pnpm --filter efx-motion-editor exec vitest run reports 0 failed, and pnpm build exits 0, with full output captured for each command."
    - "Every one of the 18 baseline-failing test files is either repaired to green against the canonical physical contract or deleted with per-file justification recorded in this plan and the summary; no failing file is left unaddressed, newly skipped, or shimmed around."
    - "Production source changes are limited to the removal of the unused declaration in app/src/lib/physicPaintBridge.ts plus any additional minimal fixes explicitly authorized through this plan's blocking decision checkpoint; no new production symbols, adapters, wrappers, aliases, or config workarounds are created."
    - "No retired field or contract is reintroduced in any edited test: the removed snapshot coordinate pair, launch-level background field, cached-playback initial-fps input, and live in-between count timing authority stay removed."
    - "Any touched test that exercises the playback-readiness seam keeps the MutationObserver/ResizeObserver dry-canvas bounds contract, the cached-roto-playback-ready hide guard, and parent-authoritative settlement semantics intact."
    - "PHYSICAL-IDENTITY/empty: empty and single-real-key sets derive zero interpolation interiors; transferred physicPaintStore assertions prove generated output exists only between adjacent real keys."
    - "DERIVED-INTERPOLATION/boundary: adjacent real keys generate zero interior frames and a one-frame gap generates exactly one, per the max(0, right-left-1) derivation; re-proven by transferred physicPaintStore interpolation assertions."
    - "ATOMIC-FRAME-MAPPING/idempotency: an exact operation retry returns the original result and an altered operation-ID collision is rejected; re-proven by transferred physicPaintPlayScriptBridge assertions."
    - "ATOMIC-FRAME-MAPPING/concurrency: parent authority is operation-correlated through capacity, real keys, and revision, so interleaved or stale operations cannot settle; re-proven by transferred physicPaintPlayScriptBridge assertions."
    - "DOWNSTREAM-PARITY/empty: missing or empty Roto frames render as transparent or background-only without cache writes, and empty persistence inputs hydrate cleanly; re-proven by transferred previewRenderer and physicPaintPersistence assertions."
    - "UAT-THEN-REGRESSION/idempotency: the three-command final gate is deterministic and rerunnable; two consecutive invocations produce identical exit codes and identical failure counts."
    - "UI-SPEC guarded Copy Script and Apply Script controls: unavailable actions stay in order and focusable with controller-supplied reasons; the WorkflowStrip and ScriptsPanel contract tests assert against the current production source only."
    - "UI-SPEC Scripts toolbar: Discard Script remains only in the existing right-panel Scripts toolbar with its explicit label and unchanged durable library layout; no timeline row is added by any touched test."
    - "UI-SPEC context action row: the locked action sequence stays one fit-content non-wrapping pill; contract tests assert current production composition, not superseded projection-era structure."
    - { statement: "PHYSICAL-IDENTITY/adjacency: equal or touching key positions resolve through stable identity with unique appFrame ownership, proven by Plans 01-04 physical model summaries and the 2026-07-24 user-approved native UAT.", verification: backstop }
    - { statement: "PHYSICAL-IDENTITY/ordering: equal-comparing elements have a specified stable output order through the ordered physical projection seam, proven by Plan 02 projection summaries and native UAT.", verification: backstop }
    - { statement: "DERIVED-INTERPOLATION/precision: interior derivation uses exact integer frame arithmetic with no rounding or tie-breaking surface, proven by Plans 01-04 and native UAT Duplicate/Blend approval.", verification: backstop }
    - { statement: "RIPPLE-INSERT-DELETE/adjacency: insert/delete at touching boundaries shifts exactly the ordered later identities by one physical slot, proven by Plans 05-07 summaries and native UAT.", verification: backstop }
    - { statement: "RIPPLE-INSERT-DELETE/empty: insert/delete on empty or single-key layouts validates capacity and preserves identity without creating phantom keys, proven by Plans 05-07 and native UAT.", verification: backstop }
    - { statement: "RIPPLE-INSERT-DELETE/ordering: the complete mapping transaction validates one ordered unique keyId-to-appFrame map before publication, proven by Plans 04-07 summaries.", verification: backstop }
    - { statement: "RIPPLE-INSERT-DELETE/idempotency: an acknowledged insert/delete replayed with identical operation identity settles without double-shifting, proven by the accepted-only history design of Plans 21-22.", verification: backstop }
    - { statement: "RIPPLE-INSERT-DELETE/concurrency: pending settlement blocks a second mutation and rejection or timeout restores the complete pre-operation state, proven by Plans 04, 21-22 and native UAT rejection neutrality.", verification: backstop }
    - { statement: "RIPPLE-DRAG/idempotency: committing the same resolved drag mapping twice is a no-op because commit requires the retained publication target signature, proven by Plan 08 and the D-29 native UAT drag scenarios.", verification: backstop }
    - { statement: "RIPPLE-DRAG/concurrency: cancellation or settlement failure during drag publishes nothing and restores map and focus, proven by Plan 08 summaries and native UAT.", verification: backstop }
    - { statement: "FORCE-SPACING/boundary: N = 0 produces adjacent ordered keys and the capacity boundary accepts exactly at capacity and rejects one beyond, proven by Plan 09 summaries and native UAT.", verification: backstop }
    - { statement: "FORCE-SPACING/precision: Force Spacing accepts only exact nonnegative base-10 safe integers with identity i mapped to first + i*(N+1), proven by Plan 09 canonical parsing and resolver ownership.", verification: backstop }
    - { statement: "FORCE-SPACING/idempotency: applying the unchanged spacing proposal is detected as no-change and records no history entry, proven by Plan 09 no-change detection.", verification: backstop }
    - { statement: "FORCE-SPACING/concurrency: a pending Force Spacing application guards further mutations and validation failure leaves all state and history untouched, proven by Plan 09 and native UAT.", verification: backstop }
    - { statement: "DOWNSTREAM-PARITY/encoding: preview, export, and persistence compare frames by exact appFrame identity and PNG data identity rather than string-length heuristics, proven by Plans 10, 23, 26 and native export/reopen approval.", verification: backstop }
    - { statement: "UI-INTEGRATION/empty: zero-key timelines render the documented empty state without editable targets, proven by native UAT and Plan 27 summaries.", verification: backstop }
    - { statement: "UI-INTEGRATION/encoding: status capsule text truncates with full detail retained through accessible text and the LOG tab regardless of content length, proven by native UAT and the UI-SPEC covered rows.", verification: backstop }
    - { statement: "UI-INTEGRATION/idempotency: repeated control activation while a mutation lock is active is guarded and publishes nothing, proven by native UAT workflow retest.", verification: backstop }
    - { statement: "UI-INTEGRATION/concurrency: playback, navigation, and settlement interleavings remain parent-authoritative with provisional local state, proven by Plans 21-27 and native UAT.", verification: backstop }
    - { statement: "UAT-THEN-REGRESSION/concurrency: no parallel or interrupted gate execution is claimed; the final gate runs once to completion in a single pass after user-approved native UAT, per D-30 and the Plan 29 summary.", verification: backstop }
    - { statement: "UI-SPEC header overflow backstop: native visual UAT at the minimum supported host width confirms every fixed header control island remains visible and only the status capsule truncates; evidence is the 2026-07-24 user-approved native UAT, since Plan 30 changes no UI.", verification: backstop }
    - { statement: "UI-SPEC populated timeline backstop: native visual UAT confirms the populated fixed 155px strip, band geometry, semantic cell states, ruler alignment, and action-row proportions match the corrected reference; evidence is the Plan 29 approval.", verification: backstop }
    - { statement: "UI-SPEC long-text ruler backstop: native visual UAT with the largest supported frame labels confirms tabular numerals remain legible without changing fixed cell pitch; evidence is the Plan 29 approval.", verification: backstop }
  prohibitions:
    - "Deleting a test whose underlying contract is still live without transferring the still-valid assertion to the canonical physical contract."
    - "Weakening a transferred assertion so it no longer proves the original behavior (looser equality, dropped identity check, removed settlement await, removed error-contract detail)."
    - "Reintroducing a retired field or contract to make an old test compile, including the removed snapshot coordinate pair, launch-level background, cached-playback initial-fps input, or live in-between count timing authority."
    - "Editing production code beyond the authorized unused-declaration removal and any checkpoint-authorized minimal fixes in order to satisfy a stale test, instead of repairing or deleting the test."
    - "Claiming the final gate green from partial command output, cached results, a subset of the three commands, or a run with new skips or todos added to mask failures."
    - "Adding compatibility adapters, forwarding wrappers, aliases, TypeScript exclusions, tsconfig or test-config workarounds, or any new production symbol."
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts"
      provides: "Tracer cluster: 18 failing tests and 29 typecheck errors triaged to green against the physical snapshot contract."
    - path: "app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts"
      provides: "Deleted wholesale; the production hydrateRotoLaunchContext unconditionally throws as a retired contract."
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Unused declaration removed at the authorized single production edit site."
    - path: ".planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-30-SUMMARY.md"
      provides: "Per-file and per-cluster transfer/delete justification plus full final-gate output."
  key_links:
    - from: "18 failing test files"
      to: "canonical physical contract modules (physicsPaintRotoPhysicalModel.ts, physicsPaintRotoPhysicalResolver.ts, physicsPaintRotoScriptClipboard.ts, useRotoPlayScriptController.ts, useRotoCachedPlayback.ts, physicPaintStore.ts)"
      via: "per-assertion transfer to the live physical contract or deletion of retired-contract assertions"
    - from: "app/src/lib/physicPaintBridge.ts unused declaration"
      to: "pnpm --filter efx-motion-editor typecheck exit 0"
      via: "single authorized removal plus checkpoint-authorized minimal fixes"
    - from: "blocking decision checkpoint"
      to: "final three-command gate"
      via: "explicit user authorization for the three additional production-file typecheck errors discovered in the baseline"
---

<objective>
Triage all 18 baseline-failing test files after the user-approved Plan 29 native UAT: transfer every still-valid assertion to the canonical physical-frame contract, delete assertions whose contract was retired, remove the one authorized unused production declaration, and bring the deferred executable gates fully green in a single pass.

Purpose: D-30 made regression, typecheck, and build work eligible only after exact native approval, which the user gave on 2026-07-24. The user then authorized this bounded stale-test contract transfer scope. This plan is the regression half of 36.14-UAT-THEN-REGRESSION.

Output: 17 repaired test files, 1 wholesale-deleted test file, 1 authorized production removal (plus any checkpoint-authorized minimal fixes), a per-file justification record, and the three-command final gate green from repo root.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-CONTEXT.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-30-SCOPE-BRIEF.md
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/gate-baseline-typecheck.txt
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/gate-baseline-vitest.txt
@.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-29-SUMMARY.md
</context>

<scope_constraints>
- Historical Plans 12-18 remain locked and non-executable; do not consult them as executable scope and do not create 36.14-12-APPROVAL.txt or any replacement approval token.
- Tests are edited or deleted, never shimmed: no compatibility adapters, forwarding wrappers, aliases, TypeScript exclusions, or tsconfig/test-config workarounds.
- Do not reintroduce launch-level background, the removed snapshot coordinate pair, the cached-playback initial-fps input, live in-between count timing authority, or any source/display compatibility field.
- The only pre-authorized production change is the unused-declaration removal in app/src/lib/physicPaintBridge.ts. The three additional production-file typecheck errors found in the baseline route through the blocking decision checkpoint; any further production defect discovered during triage is a flagged finding for separate authorization, never fixed in place.
- No new production symbols are created by this plan.
- No native UAT is required unless production behavior changes beyond the authorized scope; if that happens, stop and surface the deviation.
- Use pnpm (never npm). Use vitest run only, never watch mode. Do not start the dev server or any native application.
</scope_constraints>

## Per-File Triage Justification (all 18 baseline-failing files)

| # | Test file | Baseline debt | Triage direction | Justification |
|---|-----------|---------------|------------------|---------------|
| 1 | components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts | 29 typecheck errors, 18 failing tests | TRANSFER + selective DELETE | Clipboard apply/reuse/cancellation/error contracts are live (quick 260715-kgf approved); only the snapshot coordinate shape, the renamed prepare port, and the retired copied-source accessor changed. Tracer cluster. |
| 2 | components/physic-paint/roto/rotoLaunchHydration.test.ts | 4 failing tests | DELETE wholesale | Production hydrateRotoLaunchContext unconditionally throws; the hydration contract was retired by the physical persistence cutover (D-03). Zero transferable assertions. |
| 3 | components/physic-paint/bridge/physicsPaintLaunchContext.test.ts | 2 typecheck errors, 3 failing tests | TRANSFER | Launch-context parsing is live; only the retired launch-level background literal must be removed from two Partial literals. |
| 4 | components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts | 4 typecheck errors, 2 failing tests | TRANSFER | Launch replacement drain and mounted-clipboard retention are live; snapshot literals and one mock return type drifted. |
| 5 | lib/physicPaintBridge.test.ts | 1 typecheck error, 8 failing tests | TRANSFER | Bridge hydration, fallback listener, close/apply publication, and generated-only redirect rejection are live parent-authority contracts; launch-level background usage is retired. |
| 6 | stores/physicPaintStore.test.ts | 14 typecheck errors, 12 failing tests | TRANSFER + selective DELETE | Gap-derived enabled-only interpolation and real-key authority are live (D-01/D-02); spacing-override, source-endpoint, and count-based assertions assert contracts retired by D-03 and are deleted. |
| 7 | lib/previewRenderer.test.ts | 7 failing tests | TRANSFER | Missing-frame source contract, paper-before-alpha ordering, and generated-cache draw order are live; display-position span expectations move to direct physical appFrame positions (D-10). |
| 8 | lib/exportRenderer.test.ts | file-level failure | TRANSFER or DELETE after diagnosis | Export shares the physical render-source resolver; diagnose the collection-time failure first, then transfer parity assertions or delete retired ones. |
| 9 | lib/frameMap.test.ts | 1 failing test | TRANSFER | Timeline/FX range extension is live; generated-range expectation moves from display frames to physical frames. |
| 10 | lib/physicPaintPersistence.test.ts | 8 failing tests | TRANSFER | Cache-path serialization, PNG-only rejection, unsafe-path skipping, and hydration are live; persisted shape is the canonical physical document. |
| 11 | lib/physicPaintPlayScriptBridge.test.ts | 6 failing tests | TRANSFER | Operation-correlated capacity/revision, complete-set replacement, exact-retry idempotency, and background persistence are live Plan 26 contracts. |
| 12 | components/physic-paint/hooks/useRotoCachedPlayback.test.ts | 5 typecheck errors, 4 failing tests | TRANSFER | Playback start/loop/stop, clamped-FPS restart, final-frame clearing, and missing-cache status are live; input shape moved to initialSettings plus start(fps?) and one call site now requires an argument. |
| 13 | components/physic-paint/hooks/useRotoPlayScriptController.test.ts | 8 typecheck errors, 1 failing test | TRANSFER | Dynamic port proxying is live; ports now require the coordinator port (executePhysicalEdit, pendingOperationId, acceptedOutput), the selection shape is kind/keyId/appFrame, and the mirrorAccepted/getBackground ports were retired. |
| 14 | components/physic-paint/hooks/useRotoReferenceController.test.ts | 2 failing tests | TRANSFER | Dirty-frame refusal and cached-frame resolution order are live; display-frame ordering expectations move to physical appFrame ordering. |
| 15 | components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts | 6 typecheck errors, 4 failing tests | TRANSFER | Play commit/mirror/authority contracts are live (quick 260717-m9k, Plan 26); authority result requires physicalCapacity, selection uses keyId/appFrame, and the commit mock must satisfy RotoPlayScriptCommitResult. |
| 16 | components/physic-paint/PhysicsPaintStudio.test.ts | 1 failing test | TRANSFER | Delete-shortcut wiring sharing the visible-delete reference is a live Plan 19 contract; the expected wiring drifted. |
| 17 | components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts | 3 failing tests | TRANSFER | Source-contract assertions (interpolation lock guards, generated non-editable cells, control presence) are live; expected source patterns must match current production source. |
| 18 | components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts | 1 failing test | TRANSFER | The accessible Play Script dialog contract is live (Plan 25); the expected surface drifted. |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — repair the script-clipboard cluster end-to-end to green</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts (current full state)
    - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts — canonical RotoScriptSourceSnapshot interface at lines 7-12 (selectionKind, layerId, keyId, appFrame), RotoScriptPhysicalTarget at 14-17, controller ports at 88-91 (prepareTarget is the only prepare port), and the controller's exposed copied-state surface
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — canonical physical key/interpolation contract the snapshot derives from
    - gate-baseline-typecheck.txt lines for this file (29 errors) and the FAIL lines for its 18 tests in gate-baseline-vitest.txt
    - 36.14-30-SCOPE-BRIEF.md cluster 2 and Hard Constraints
  </read_first>
  <action>
  <!-- planner-discipline-allow: sourceFrame -->
  Prove the transfer-vs-delete triage method on this single file before any other file is touched. Work assertion by assertion through all 29 typecheck errors and 18 failing tests:

  1. Rewrite every retired-coordinate snapshot literal to the canonical shape `{ selectionKind, layerId, keyId, appFrame }` declared at physicsPaintRotoScriptClipboard.ts lines 7-12, choosing keyId/appFrame values that preserve each test's original scenario intent (same identities, same target frames).
  2. Replace every `prepareEmptyTarget` port mock with the canonical `prepareTarget` port (production line 91), preserving each mock's resolve/reject behavior.
  3. For the two assertions reading the retired copied-source accessor (test lines 116 and 135): if the intent is "the reusable clipboard retains its immutable source identity", transfer to the equivalent canonical exposed state (the immutable snapshot's keyId/appFrame, or the controller's actual copied-state surface discovered in read_first); if no canonical equivalent exists, delete that assertion and record the deletion reason.
  4. For each of the 18 failing tests, keep the test only where its behavior — sequential apply with duplicate/unrelated completion ignore, immutable reusable clipboard, accepted-only destination binding, cancellation drain, empty-target claim on first acceptance, durable Load+Apply lock transfer, final-composite-only publication, detailed error contract, current-Motion sampling at Apply time — is still a live contract per the production controller. Re-point expectations to the physical snapshot/ports. Delete only assertions whose specific expectation was retired with the old coordinate contract.
  5. Record every transfer and every deletion with a one-line reason for the summary. Do not weaken kept assertions: identity checks, settlement awaits, and error-detail expectations stay at full strength.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep 'physicsPaintRotoScriptClipboard.test.ts')"</automated>
  </verify>
  <acceptance_criteria>
    - cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts exits 0 with 0 failed tests in that file.
    - pnpm --filter efx-motion-editor typecheck output contains no line referencing physicsPaintRotoScriptClipboard.test.ts.
    - Every kept test still asserts at least one behavioral identity, settlement, or error-contract expectation (no empty or expectation-free tests remain).
    - A per-assertion transfer/delete ledger exists for this file in working notes, ready for the summary.
  </acceptance_criteria>
  <done>The tracer cluster is green in isolation, typecheck-clean for its file, and the triage ledger demonstrates transfer, port-rename, and retired-assertion deletion outcomes.</done>
  <reversibility rating="reversible">Test-only edits in one file; fully revertible via git.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize the three additional production-file typecheck errors</name>
  <files>No repository files; decision checkpoint only</files>
  <read_first>
    - gate-baseline-typecheck.txt lines 16, 35-38 (the three production-file errors)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx line 1 and lines 340-460
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts lines 58-70
    - 36.14-30-SCOPE-BRIEF.md Hard Constraints ("Any other production defect ... flagged finding for separate authorization")
  </read_first>
  <decision>The scope brief authorizes only the single unused-declaration removal in app/src/lib/physicPaintBridge.ts, yet the measured baseline contains three more production-file errors that no test edit can resolve and that block the typecheck gate. Authorize or refuse minimal fixes for each.</decision>
  <context>The final gate requires pnpm --filter efx-motion-editor typecheck exit 0. These three errors live in production source, outside test reach:
    (a) PhysicsPaintStudio.tsx line 1: TS6133 — 'useEffect' is imported from 'preact/hooks' but never read. Minimal fix: remove 'useEffect' from that import list.
    (b) useRotoFramePersistenceCoordinator.ts line 64: TS6133 — 'buffer' is declared (const buffer = editBuffer.bufferRef.current;) but never read. Minimal fix: delete that single declaration line.
    (c) PhysicsPaintStudio.tsx line 448: TS2345 — the executePhysicalEdit port passes RotoPhysicalEditExecuteInput&lt;PhysicPaintRotoPhysicalEditProposal, unknown&gt; where RotoPhysicalEditCoordinatorExecuteInput&lt;SerializedProject&gt; is required (the coordinator at line 353 is instantiated as useRotoPhysicalEditCoordinator&lt;SerializedProject&gt;). Minimal fix: the smallest type-level correction at this port boundary so the proposal payload type is SerializedProject instead of unknown; no runtime behavior change.
  </context>
  <options>
    <option id="authorize-all">
      <name>Authorize all three minimal fixes</name>
      <pros>Final typecheck gate can go green inside this plan; each fix is deletion-level or type-level with no runtime behavior change, matching the approved native surface.</pros>
      <cons>Expands production edits beyond the single pre-authorized removal.</cons>
    </option>
    <option id="authorize-subset">
      <name>Authorize a named subset only</name>
      <pros>Keeps production edits minimal per user judgment.</pros>
      <cons>The typecheck gate cannot reach exit 0 for any refused error; the plan then stops at the final gate and reports the deviation.</cons>
    </option>
    <option id="stop-plan">
      <name>Refuse all; stop the plan</name>
      <pros>Strictest reading of the scope brief.</pros>
      <cons>The final gate remains red on production errors; this plan halts and files a flagged finding.</cons>
    </option>
  </options>
  <resume-signal>Select: authorize-all, authorize-subset (naming which of a/b/c), or stop-plan</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the authorized production removals and checkpoint-authorized fixes</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts lines 470-500 (the unused declaration and its neighbors)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx line 1 and lines 340-460
    - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts lines 58-70
    - Task 2 checkpoint resolution
  </read_first>
  <action>
  <!-- planner-discipline-allow: buildRotoRevision -->
  1. First confirm the authorized removal target is truly dead: run a repo-wide search for buildRotoRevision under app/src and verify the only occurrence is its declaration at physicPaintBridge.ts line 484. If any call site or reference exists, STOP, do not remove it, and surface a flagged finding that the declaration is load-bearing (per the scope brief).
  2. Remove the buildRotoRevision function declaration (lines 484-489) from app/src/lib/physicPaintBridge.ts. Nothing else in that file changes.
  3. If and only if the Task 2 checkpoint authorized them: remove 'useEffect' from the preact/hooks import at PhysicsPaintStudio.tsx line 1; delete the unused 'buffer' declaration at useRotoFramePersistenceCoordinator.ts line 64; apply the minimal type-level correction at PhysicsPaintStudio.tsx line 448 so the executePhysicalEdit port payload type is SerializedProject. Each fix touches only its named line(s); no runtime logic changes.
  4. Any refused item stays untouched and is carried as a flagged finding.
  </action>
  <verify>
    <automated>cd app &amp;&amp; test "$(grep -c 'buildRotoRevision' src/lib/physicPaintBridge.ts)" = "0" &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep -E 'physicPaintBridge\.ts|PhysicsPaintStudio\.tsx|useRotoFramePersistenceCoordinator\.ts' | grep 'error TS')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "buildRotoRevision" app/src/lib/physicPaintBridge.ts prints 0.
    - pnpm --filter efx-motion-editor typecheck reports no TS6133 in physicPaintBridge.ts, and no error at all in PhysicsPaintStudio.tsx or useRotoFramePersistenceCoordinator.ts for every checkpoint-authorized item.
    - git diff for the three production files shows only line deletions (plus, for item (c), the minimal type-level correction) — no added functions, wrappers, or aliases.
  </acceptance_criteria>
  <done>The authorized unused declaration is gone, checkpoint-authorized minimal fixes are applied, and no other production line changed.</done>
  <reversibility rating="costly">Production deletions are git-revertible, but they touch the approved native surface; bounded by the pre-authorized scope plus the explicit Task 2 checkpoint.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Retired launch contracts — delete the dead hydration suite and repair the two launch-context test files</name>
  <files>app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts, app/src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/rotoLaunchHydration.ts lines 100-120 (unconditional throw)
    - app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts (full)
    - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts (full) and app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts (parser contract)
    - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts (full) and app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts
    - 36.14-CONTEXT.md D-01, D-03, D-10
  </read_first>
  <action>
  1. Delete app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts wholesale (git rm). Justification: production hydrateRotoLaunchContext unconditionally throws "Legacy Roto launch hydration was retired by the physical persistence cutover."; all four tests exercise a dead contract with zero transferable assertions. Reversibility commentary: whole-file coverage deletion is acceptable here only because the covered function is itself retired; recovery is one git revert away, and the summary must record this reasoning verbatim.
  2. In physicsPaintLaunchContext.test.ts: remove the retired launch-level background property from the two Partial&lt;PhysicPaintLaunchContext&gt; literals (lines 15 and 46). Keep and repair the three failing parse tests (encoded/flat Roto contexts with rejection of incomplete input; workflow labels without layer-name replacement; parent-owned project and stable layer display metadata) against the current parser contract in physicsPaintLaunchContext.ts. Canonical runtime background lives on the physical record, not the launch context — do not add any replacement launch-level field.
  3. In usePhysicsPaintLaunchIntegration.test.ts: rewrite the snapshot literals at lines 53, 68, and 109 to the canonical { selectionKind, layerId, keyId, appFrame } shape; fix the line-141 mock to return void or Promise&lt;void&gt; (TS2322). Repair the two failing tests (drains a real accepted Apply before clear/load while retaining the captured old-destination publication; keeps an established mounted clipboard through coalesced navigation and delayed launch contexts) against the current hook behavior, preserving their drain/retention assertions at full strength.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/components/physic-paint/bridge/physicsPaintLaunchContext.test.ts src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts &amp;&amp; test ! -e src/components/physic-paint/roto/rotoLaunchHydration.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep -E 'rotoLaunchHydration|physicsPaintLaunchContext\.test|usePhysicsPaintLaunchIntegration\.test')"</automated>
  </verify>
  <acceptance_criteria>
    - rotoLaunchHydration.test.ts no longer exists and git status shows the deletion.
    - The two remaining files pass under vitest run with 0 failed.
    - Typecheck output references none of the three files.
    - Kept tests retain their original behavioral assertions (parse rejection, label preservation, drain/retention), not weakened restatements.
  </acceptance_criteria>
  <done>The dead hydration suite is deleted with recorded justification, and both launch-context test files are green and typecheck-clean.</done>
  <reversibility rating="reversible">Wholesale deletion of a suite covering a retired throwing function; recoverable via git, justification recorded per the scope rule on whole-file coverage deletion.</reversibility>
</task>

<task type="auto">
  <name>Task 5: Repair the bridge test file against the physical launch and apply contracts</name>
  <files>app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.test.ts (full, 818 lines)
    - app/src/lib/physicPaintBridge.ts — current PhysicPaintLaunchContext handling, hydration of cached frame summaries, browser fallback listener, and close/apply publication paths
    - app/src/types/physicPaint.ts — current PhysicPaintLaunchContext and physical record types
    - gate-baseline-vitest.txt FAIL lines for this file (8 tests) and gate-baseline-typecheck.txt line 90
  </read_first>
  <action>
  Triage all 8 failing tests plus the TS2339 at test line 188:
  1. Remove the retired launch-level background access at line 188; where a test's intent was background fidelity, re-point the assertion to the canonical physical record background (rotoPhysical background on the persisted document), matching how production bridge code reads it today.
  2. Transfer the live contracts: hydration of every cached Roto frame summary into launch context; no stale layer-level editable state on cached-only reopen; generated-only launch targets rejected as render-only (36.12 D-16); defensive copy of persisted Roto paper metadata; browser fallback listener dispatching exactly one apply-result event; postMessage apply replies through the child source; generated cache and settings publication through close/apply; invalid launch contexts rejected before window open. Each must assert against current production behavior read in read_first — never against the retired field shape.
  3. Delete only assertions whose specific expectation was the retired launch-level background field itself.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/lib/physicPaintBridge.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep 'physicPaintBridge.test.ts')"</automated>
  </verify>
  <acceptance_criteria>
    - vitest run for this file exits 0 with 0 failed.
    - Typecheck output contains no reference to physicPaintBridge.test.ts.
    - The exactly-one-apply-result-event and generated-only-redirect tests still assert their original strength (exact dispatch count; render-only rejection).
  </acceptance_criteria>
  <done>Bridge tests are green against the physical launch/apply contract with per-assertion triage recorded.</done>
</task>

<task type="auto">
  <name>Task 6: Repair the store test file against gap-derived physical interpolation</name>
  <files>app/src/stores/physicPaintStore.test.ts</files>
  <read_first>
    - app/src/stores/physicPaintStore.test.ts (full, 1391 lines)
    - app/src/stores/physicPaintStore.ts — physical record ownership (stable unique keyId, unique direct appFrame), enabled-only interpolation settings, gap-derived regeneration
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — canonical derivation max(0, rightFrame - leftFrame - 1)
    - 36.14-CONTEXT.md D-01, D-02, D-03 (retired concepts list)
    - gate-baseline-vitest.txt FAIL lines for this file (12 tests) and gate-baseline-typecheck.txt lines 92-105
  </read_first>
  <action>
  Triage the 12 failing tests and the 14 null-safety typecheck errors (lines 962-972 TS2531; 1089-1131 TS18047):
  1. DELETE assertions asserting retired D-03 concepts: persisted per-segment spacing restoration (the D-01/D-02 titled test), source-endpoint override serialization (the D-04 titled test), stale trailing rendered-frame pruning tied to source/display metadata (the D-05/D-06 titled test), and "does not move source real keys when interpolation count changes" insofar as it asserts count-based timing authority. Each deletion gets a one-line recorded reason naming the retired contract.
  2. TRANSFER the live regeneration contracts to gap-derived physical interpolation: alpha-only cache across integer spans with real-key authority; visible-hold (duplicate) and alpha-blend mode normalization; real keys preserved across interpolation toggles; regeneration after reopen from persisted real keys plus enabled state; regeneration after key upsert/removal/replacement and clean disable; blend PNG derivation from both neighboring alpha sources; source-neighbor provenance without moving real keys. Expected interior sets follow max(0, right-left-1): adjacent keys produce zero interiors.
  3. Fix null-safety errors with explicit narrowing guards consistent with neighboring passing tests in the same file (no new `as` casts and no non-null assertion operators unless that is already the file's local convention — confirm from surrounding code first).
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/stores/physicPaintStore.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep 'physicPaintStore.test.ts')"</automated>
  </verify>
  <acceptance_criteria>
    - vitest run for this file exits 0 with 0 failed.
    - Typecheck output contains no reference to physicPaintStore.test.ts (all 14 null-safety errors resolved).
    - Transferred interpolation tests assert exact interior frame sets derivable from max(0, right-left-1), including the adjacent-keys zero-interior case.
    - The deletion ledger names each removed assertion and its retired contract (D-03 concept).
  </acceptance_criteria>
  <done>Store tests are green against enabled-only gap-derived interpolation, retired spacing/count assertions are deleted with reasons, and null-safety errors are resolved.</done>
</task>

<task type="auto">
  <name>Task 7: Repair preview, export, and frame-map parity tests to direct physical positions</name>
  <files>app/src/lib/previewRenderer.test.ts, app/src/lib/exportRenderer.test.ts, app/src/lib/frameMap.test.ts</files>
  <read_first>
    - app/src/lib/previewRenderer.test.ts (full) and app/src/lib/previewRenderer.ts — missing-frame source order and paper/alpha composition
    - app/src/lib/exportRenderer.test.ts (full) and app/src/lib/exportRenderer.ts — shared physical render-source resolver path
    - app/src/lib/frameMap.test.ts (full) and app/src/lib/frameMap.ts
    - 36.14-CONTEXT.md D-10 (downstream consumers use direct physical positions)
    - gate-baseline-vitest.txt FAIL lines for these three files
  </read_first>
  <action>
  1. previewRenderer.test.ts (7 failing): transfer the missing-frame source contract to physical positions — renderer-owned paper before merged real-key alpha repaint; paper baseline before transparent real frames; real paper texture for interior missing frames; cached-real-first resolution order; generated interpolation alpha cache drawn after close/reopen load; the 36.13-era "custom 2 -&gt; 6 span at override-aware display positions" expectation becomes the equivalent physical assertion: regenerated output at direct appFrame positions after save/load with no override concept. Delete only the override/display-position phrasing, never the parity assertion itself.
  2. exportRenderer.test.ts (file-level failure): diagnose the collection-time failure first (likely an import or fixture referencing a retired export). Transfer preview/export parity assertions to the shared physical render-source resolver; if a whole test exists solely to assert a retired display-position export path, delete it with a recorded reason.
  3. frameMap.test.ts (1 failing): transfer "extends the parent timeline and FX range to generated Roto interpolation frames" from display frames to direct physical frames, keeping the extension expectation at full strength.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/lib/previewRenderer.test.ts src/lib/exportRenderer.test.ts src/lib/frameMap.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep -E 'previewRenderer\.test|exportRenderer\.test|frameMap\.test')"</automated>
  </verify>
  <acceptance_criteria>
    - All three files pass under vitest run with 0 failed.
    - Export/preview parity is asserted at identical physical appFrame positions on both sides of the comparison.
    - Any deleted test is named in the ledger with its retired-contract reason.
  </acceptance_criteria>
  <done>Downstream render parity tests assert direct physical positions and all three files are green (36.14-DOWNSTREAM-PARITY re-proof).</done>
</task>

<task type="auto">
  <name>Task 8: Repair persistence and Play Script bridge tests to the canonical physical document</name>
  <files>app/src/lib/physicPaintPersistence.test.ts, app/src/lib/physicPaintPlayScriptBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintPersistence.test.ts (full) and app/src/lib/physicPaintPersistence.ts — physical document serialization/hydration
    - app/src/lib/physicPaintPlayScriptBridge.test.ts (full) and app/src/lib/physicPaintPlayScriptBridge.ts — operation-correlated capacity/revision and complete-set replacement
    - 36.14-26-SUMMARY.md — accepted Play Script parent-authority contract
    - gate-baseline-vitest.txt FAIL lines for these two files (8 + 6 tests)
  </read_first>
  <action>
  1. physicPaintPersistence.test.ts (8 failing): transfer live contracts to the canonical physical document — hydration of cached frames back to runtime data URLs; hydration of persisted real keys and settings while ignoring generated metadata without cache files; non-PNG runtime frames rejected instead of inline serialization; project cache removed when no outputs remain; malformed outputs without frames skipped; unsafe persisted cache paths skipped; frames stored as project cache files with only cache paths serialized; transparent Roto onion frames stored as project cache files. Update fixtures from any retired persisted shape to the physical record shape (keyId + direct appFrame + enabled-only interpolation).
  2. physicPaintPlayScriptBridge.test.ts (6 failing): transfer live Plan 26 contracts — fail-closed capacity with capping of large valid ranges; transparent background preserved through batch persistence; rejection of omission, modification, and injection outside the affected destination range; whole real-key set replacement with untouched keys retained and single regeneration; operation-correlated capacity/real-keys/revision reporting; exact-retry returning the original result with altered operation-ID collisions rejected. These assertions stay at full strength; only fixture shapes move to the physical document.
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/lib/physicPaintPersistence.test.ts src/lib/physicPaintPlayScriptBridge.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep -E 'physicPaintPersistence\.test|physicPaintPlayScriptBridge\.test')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files pass under vitest run with 0 failed.
    - The exact-retry idempotency test and the operation-correlated capacity test still assert their original behavioral strength (36.14-ATOMIC-FRAME-MAPPING re-proof).
    - No fixture reintroduces a retired persisted field.
  </acceptance_criteria>
  <done>Persistence and Play bridge tests are green against the canonical physical document with idempotency and parent-authority assertions intact.</done>
</task>

<task type="auto">
  <name>Task 9: Repair the three hook contract test files</name>
  <files>app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts, app/src/components/physic-paint/hooks/useRotoPlayScriptController.test.ts, app/src/components/physic-paint/hooks/useRotoReferenceController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts lines 13-33 (UseRotoCachedPlaybackInput: initialSettings, start(fps?)) and its test file
    - app/src/components/physic-paint/hooks/useRotoPlayScriptController.ts lines 1-100 (ports: Omit&lt;RotoPlayScriptControllerPorts, 'requestAuthority' | 'commit'&gt; &amp; RotoPlayScriptCoordinatorPort; retired mirrorAccepted/getBackground) and its test file
    - app/src/components/physic-paint/hooks/useRotoReferenceController.test.ts and app/src/components/physic-paint/hooks/useRotoReferenceController.ts
    - 36.14-30-SCOPE-BRIEF.md Hard Constraints (playback-readiness seam preservation)
  </read_first>
  <action>
  1. useRotoCachedPlayback.test.ts (5 typecheck errors, 4 failing): replace the retired `initialFps` input property at lines 77, 102, 141, 173 with the canonical `initialSettings: PhysicPaintRotoPlaybackSettings` shape; fix the line-192 call to pass the now-required argument per the production signature. Transfer the 4 failing tests (final transient frame cleared before editable-state reveal with stale ticks ignored after Stop; unchanged missing-cache status without playback start; restart at clamped FPS with reset for a new launch; start/loop/stop lifecycle) to the current input/settings contract. Keep any MutationObserver/ResizeObserver dry-canvas bounds and cached-roto-playback-ready hide-guard expectations intact wherever the touched tests exercise them.
  2. useRotoPlayScriptController.test.ts (8 typecheck errors, 1 failing): update the ports fixtures — remove the retired mirrorAccepted and getBackground mocks; add the coordinator port surface executePhysicalEdit, pendingOperationId, and acceptedOutput; change getSelection literals to { kind, keyId, appFrame }. Keep the "proxies every dynamic port and refreshes availability after rerender" test asserting the current port set.
  3. useRotoReferenceController.test.ts (2 failing): transfer dirty-frame refusal without repaint-base clearing and the cached-frame resolution order test from display-frame ordering to physical appFrame ordering (generated, preview-real, launch/store-real, confirmed, then generic-store).
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts src/components/physic-paint/hooks/useRotoPlayScriptController.test.ts src/components/physic-paint/hooks/useRotoReferenceController.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep -E 'useRotoCachedPlayback\.test|useRotoPlayScriptController\.test|useRotoReferenceController\.test')"</automated>
  </verify>
  <acceptance_criteria>
    - All three files pass under vitest run with 0 failed.
    - Typecheck output references none of the three files.
    - No fixture reintroduces initialFps, mirrorAccepted, or getBackground; coordinator port fields are present in every ports fixture.
  </acceptance_criteria>
  <done>Hook contract tests are green against the current port/input surfaces with the playback-readiness seam preserved.</done>
</task>

<task type="auto">
  <name>Task 10: Repair the Roto Play Script controller and three UI contract test files</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (full) and app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts — PhysicPaintRotoAuthorityResult (physicalCapacity required number), RotoPlayScriptCommitResult, selection shape
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts and the delete-shortcut wiring in app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts and app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (current source the contract tests assert against)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts and app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  </read_first>
  <action>
  1. physicsPaintRotoPlayScriptController.test.ts (6 typecheck errors, 4 failing): make the authority-result fixture satisfy PhysicPaintRotoAuthorityResult with a required numeric physicalCapacity (line 11); rewrite selection literals at lines 20, 52, 54 to { kind, keyId, appFrame }; type the commit mocks to return Promise&lt;RotoPlayScriptCommitResult&gt; (lines 33, 112). Transfer the 4 failing tests (one complete additive real-key commit with captured background then accepted-state mirror and playback stop; no mirror on failed commit or cancelled render; authority/selection revalidation before commit without partial mirror publication; current Max with selected durable row reloaded into the operation snapshot) to these canonical types.
  2. PhysicsPaintStudio.test.ts (1 failing): update the delete-shortcut wiring test so keyboard and visible-button paths assert the exact current shared delete reference in production Studio source.
  3. PhysicsPaintWorkflowStrip.test.ts (3 failing): update the source-contract expectations to match the current production strip source — interpolation controls disabled and handler-guarded only while the mutation lock is active; generated frames non-editable with real cached frames selectable; interpolation, onion, and key utility controls present in the current composition. Never weaken a guard assertion to make it pass; match the real source text patterns.
  4. PhysicsPaintScriptsPanel.test.ts (1 failing): update the accessible Play Script dialog contract to the current production panel surface (dialog distinct from cached Roto playback, per Plan 25).
  </action>
  <verify>
    <automated>cd app &amp;&amp; pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts &amp;&amp; test -z "$(pnpm --filter efx-motion-editor typecheck 2>&amp;1 | grep -E 'physicsPaintRotoPlayScriptController\.test|PhysicsPaintStudio\.test|PhysicsPaintWorkflowStrip\.test|PhysicsPaintScriptsPanel\.test')"</automated>
  </verify>
  <acceptance_criteria>
    - All four files pass under vitest run with 0 failed.
    - Typecheck output references none of the four files.
    - Source-contract tests grep production source for patterns that exist in the current committed source (no assertion against retired composition).
  </acceptance_criteria>
  <done>Controller and UI contract tests are green against current production surfaces.</done>
</task>

<task type="auto">
  <name>Task 11: Final three-command gate from repo root, green in one pass</name>
  <files>.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-30-SUMMARY.md</files>
  <read_first>
    - 36.14-30-SCOPE-BRIEF.md Final Gate section (definition of done)
    - The per-assertion transfer/delete ledger accumulated across Tasks 1-10
    - git status / git diff summary for the whole plan (test edits, one wholesale test deletion, the authorized production edits)
  </read_first>
  <action>
  1. From the repo root, run the three gate commands sequentially in one pass and capture full output: pnpm --filter efx-motion-editor typecheck; pnpm --filter efx-motion-editor exec vitest run; pnpm build. All three must succeed in the same pass. If any command fails, do not claim partial success: diagnose whether the failure is test-side (fix within this plan's triage rules) or production-side (STOP and surface a flagged finding for separate authorization).
  2. Confirm the vitest summary shows 0 failed with no new skips or todos introduced by this plan (pre-existing skips/todos per the baseline remain acceptable).
  3. Confirm the production diff contains only the authorized unused-declaration removal and the checkpoint-authorized minimal fixes — no new production symbols, adapters, wrappers, aliases, or config changes.
  4. Write the summary with the complete per-file triage ledger (every transfer and deletion with its reason), the Task 2 checkpoint resolution, and the full gate output verdicts.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor typecheck &amp;&amp; pnpm --filter efx-motion-editor exec vitest run &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter efx-motion-editor typecheck exits 0.
    - pnpm --filter efx-motion-editor exec vitest run reports 0 failed (and no test files failed).
    - pnpm build exits 0 (package plus app).
    - The vitest summary's skipped/todo counts are not higher than the baseline's pre-existing 1 skipped / 82 todo.
    - git diff --stat shows production changes confined to app/src/lib/physicPaintBridge.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, and app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts.
  </acceptance_criteria>
  <done>All three gate commands are green in a single pass from repo root, the triage ledger is complete, and the summary records the evidence.</done>
</task>

</tasks>

## Artifacts this phase (Plan 30) produces

Deletions (wholesale):
- app/src/components/physic-paint/roto/rotoLaunchHydration.test.ts — dead contract; production hydrateRotoLaunchContext unconditionally throws.

Edited test files (17): physicsPaintRotoScriptClipboard.test.ts, physicsPaintLaunchContext.test.ts, usePhysicsPaintLaunchIntegration.test.ts, physicPaintBridge.test.ts, physicPaintStore.test.ts, previewRenderer.test.ts, exportRenderer.test.ts, frameMap.test.ts, physicPaintPersistence.test.ts, physicPaintPlayScriptBridge.test.ts, useRotoCachedPlayback.test.ts, useRotoPlayScriptController.test.ts, useRotoReferenceController.test.ts, physicsPaintRotoPlayScriptController.test.ts, PhysicsPaintStudio.test.ts, PhysicsPaintWorkflowStrip.test.ts, PhysicsPaintScriptsPanel.test.ts.

Production edits:
- app/src/lib/physicPaintBridge.ts — removal of the unused declaration at line 484 (pre-authorized by the scope brief).
- app/src/components/physic-paint/PhysicsPaintStudio.tsx and app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts — only the minimal fixes explicitly authorized through the Task 2 blocking checkpoint; refused items stay untouched and become flagged findings.

No new production symbols are created by this plan. No new test helpers, adapters, wrappers, aliases, tsconfig changes, or test-config changes are introduced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite ↔ production contract | Deleted or weakened assertions cross here silently; coverage loss is invisible to the final gate if the gate only checks exit codes. |
| agent ↔ gate output | A green claim could rest on partial, cached, or subset command output rather than one full pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36.14-30-01 | Tampering | Per-file test triage (all 18 files) | high | mitigate | Per-assertion transfer/delete ledger recorded in every task and the summary; tracer task proves the method on the largest cluster before expansion; acceptance criteria require kept tests to retain identity/settlement/error assertions. |
| T-36.14-30-02 | Tampering | Production files (physicPaintBridge.ts, PhysicsPaintStudio.tsx, useRotoFramePersistenceCoordinator.ts) | high | mitigate | Scope limited to one pre-authorized removal plus checkpoint-authorized minimal fixes; Task 2 blocking checkpoint gates any expansion; Task 11 acceptance criteria confine the production diff by path. |
| T-36.14-30-03 | Repudiation | Final gate claim | medium | mitigate | Task 11 requires one sequential pass of all three commands from repo root with full output captured; partial or subset claims are prohibited by must_haves.prohibitions. |
| T-36.14-30-04 | Information Disclosure | — | low | accept | Test-only scope touches no secrets, credentials, or user data; nothing to disclose. |
| T-36.14-30-SC | Tampering | package installs | low | accept | Not applicable: this plan installs no npm/pip/cargo packages; the dependency set is unchanged. |
</threat_model>

<verification>
- Every task carries its own file-scoped vitest run plus a typecheck filter proving its files are clean.
- Task 2 is a blocking decision checkpoint for the three production-file errors outside the pre-authorized scope.
- Task 11 is the definition of done: the three-command gate from the scope brief, green in one pass from repo root.
- No native UAT is run or required; any production behavior change beyond the authorized scope stops the plan instead.
</verification>

<success_criteria>
- 17 test files repaired to green against the canonical physical contract; 1 test file deleted wholesale with recorded justification; 0 failing test files remain.
- typecheck, full vitest run, and build are all green in one pass from repo root.
- Production diff confined to the authorized removal plus checkpoint-authorized minimal fixes; no new production symbols.
- Per-file and per-assertion triage ledger complete in the summary.
- Plans 12-18 remain locked; no approval token created; D-30 recovery order preserved.
</success_criteria>

<output>
Create `.planning/phases/36.14-physics-paint-roto-timeline-ui-from-pencil/36.14-30-SUMMARY.md` when done
</output>
