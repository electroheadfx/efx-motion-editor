---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 07
type: execute
wave: 1
depends_on: [36.2-03, 36.2-06]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements: [PH36.2-CACHE]
user_setup: []
must_haves:
  truths:
    - "Closing standalone after painting a Roto frame awaits one final dirty-frame flush before window close is allowed, per D-07 and UAT gap 2."
    - "Roto cells remain gray for empty/background-only visited frames, green for cached-only frames, and pink only for editable real-key session frames, per D-11 through D-16."
    - "Cached-only Roto references reopen at full visual strength as repaintable references and are not composited into replacement saves, per D-01 through D-04."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Awaited close flush, real editable frame classification, cached-reference lifecycle"
      contains: "flushCurrentRotoFrameBeforeClose"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Full-strength cached Roto reference visual styling"
      contains: "cached-roto"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Regression contracts for close flush and editable/cached frame classification"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      via: "editableRotoFrames prop must use editable real-key frames, not occupied/background-only frames"
      pattern: "editableRotoFrames"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "close flush sends normal apply-canvas payload before standalone closes"
      pattern: "sendPhysicPaintApplyPayload"
---

<objective>
Close UAT gap 2 by making Roto close-save blocking enough to preserve dirty cached output, restoring gray/green/pink cell classification after navigation, and removing the attenuated cached-reference visual treatment.

Purpose: The user reported that closing Physics Painter loses unsaved Roto cache, reopened cached paint looks semi-transparent, and moving through frames turns empty frames pink. This plan fixes those diagnosed failures without adding durable per-frame editable stroke persistence, honoring D-01 through D-16.
Output: Updated Studio lifecycle/classification code, cached-reference CSS, and focused regression tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-UAT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-03-SUMMARY.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-06-SUMMARY.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make close/unload Roto flush awaited and single-path</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <behavior>
    - Test 1: Source/runtime contract proves close/unload calls the same named dirty-frame flush helper used by Save pending/current and frame-leave navigation, per D-05, D-07, D-08, and UAT gap 2.
    - Test 2: Closing with a dirty current Roto frame awaits or blocks on the flush promise before invoking the standalone close path; it does not fire and immediately close.
    - Test 3: The close flush still sends the normal apply-canvas rendered-frame payload and does not introduce a separate persistence path or re-render all frames, per D-09 and D-10.
  </behavior>
  <action>Add failing coverage in PhysicsPaintStudio.test.ts for the diagnosed close-loss path, then update PhysicsPaintStudio.tsx so `flushCurrentRotoFrameBeforeClose` returns/awaits the `flushRotoFrame` promise before native/browser close completion. If a Tauri close event cannot be synchronously cancelled, prevent the first close, run the flush once, then resume close after success or visible failure state. Keep the close path best-effort but ordered: dirty frame render/apply must be attempted before the window is allowed to disappear. Reuse the existing operation guard and avoid another render-all pathway, per D-05 through D-10.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>Painting a Roto frame then closing standalone attempts exactly one ordered apply-canvas flush before close; tests fail if the close helper starts the flush without awaiting/sequencing it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate editable real keys from occupied and background-only frames</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</files>
  <behavior>
    - Test 1: Navigating through empty/background-only frames does not add those frames to the editable-session set passed as `editableRotoFrames`, per D-11 through D-16 and UAT gaps 2 and 6.
    - Test 2: Frames become pink only after real editable Roto content exists in the open session; cached-only frames remain green and empty frames remain gray.
    - Test 3: Background-only snapshot decisions do not create green cache cells or pink editable cells, per D-17 through D-20.
  </behavior>
  <action>Replace any `editableRotoFrames=occupiedRotoFrames` wiring with an explicitly derived real editable Roto key set. Do not let `snapshotCurrentRotoFrame` add visited/background-only frames to the editable set unless actual editable Roto state/content exists. Keep cached PNG presence in the cached/saved set and session editable state in a separate set, preserving D-01 through D-04 cache-first semantics and D-11 through D-16 cell colors. Add regression assertions that an empty navigated frame stays gray and a cached-only reopened frame stays green rather than pink.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Moving through empty Roto frames no longer turns every frame pink; only true editable real-key session frames receive pink classification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore cached Roto reference strength without export compositing</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <behavior>
    - Test 1: Cached-only reference CSS no longer attenuates the cached Roto PNG; reopened cached paint appears at full intended opacity.
    - Test 2: Repainting a cached-only reference still clears/excludes the reference before `buildRotoOutputFrame` so replacement saves do not composite old cache, per D-04.
  </behavior>
  <action>Update the cached Roto reference overlay styling so the cached image is not dimmed by a 0.78-style opacity treatment. Keep it visibly a reference through border/status/copy rather than transparency. Preserve the existing save-time clearing/reset path before exporting a replacement frame so the full-strength reference is never baked into the new cache, per D-02, D-03, and D-04.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>Reopened cached Roto paint is not visually attenuated, while replacement saves still produce a clean new cached frame rather than a double-composited one.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 36.2 | Cache-first Roto workflow with save-on-close and gray/green/pink cells | 36.2-07 | COVERED | Closes UAT gap 2 user-visible failures.
REQ | PH36.2-CACHE | Cached Roto cell semantics and cache lifecycle | 36.2-07 | COVERED | Focused on close flush, reference display, editable/cached classification.
RESEARCH | Pitfalls 1-5 | Save only at lifecycle boundaries, no fourth color, store/cache semantics | 36.2-07 | COVERED | Uses existing apply-canvas path and no render-all behavior.
CONTEXT | D-01..D-16 | Cache-first model, auto-save rules, square cell semantics | 36.2-07 | COVERED | Directly cited in tasks.
CONTEXT | Deferred durable editable persistence | Out of scope | 36.2-07 | EXCLUDED | Plan does not add durable per-frame editable stroke persistence.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| standalone window to editor store | Close/save sends rendered-frame apply payloads across the existing bridge. |
| cached PNG reference to replacement export | A visual reference must not be accidentally captured into a new rendered frame. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-07-01 | Tampering | `flushCurrentRotoFrameBeforeClose` | mitigate | Reuse existing validated apply-canvas payload path and operation guard; do not create a second close persistence protocol. |
| T-36.2-07-02 | Integrity | cached Roto reference export | mitigate | Tests must prove cached references are reset/excluded before `buildRotoOutputFrame` replacement export. |
| T-36.2-07-03 | Denial of Service | close flush | mitigate | Flush only the dirty current frame; do not render all cached frames on close, per D-09. |
| T-36.2-07-SC | Tampering | package installs | accept | No npm/pnpm/cargo package installs are planned. |
</threat_model>

<verification>
Run focused tests after each task, then run:
<automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts</automated>
<automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
Do not run the dev server.
</verification>

<success_criteria>
- Closing after dirty Roto paint preserves the cached output through the normal apply path.
- Empty/background-only visited frames remain gray, cached-only frames remain green, and editable real-key session frames are pink.
- Cached Roto references reopen at full visual strength and replacement saves do not include old cache pixels.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-07-SUMMARY.md` when done.
</output>
