---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 07
subsystem: ui
tags: [physics-paint, roto-cache, preact, tauri, vitest]

requires:
  - phase: 36.2
    provides: Plan 03 dirty Roto lifecycle flushing and cached reference semantics
  - phase: 36.2
    provides: Plan 06 Roto workflow strip cell classifications and cached playback context
provides:
  - Awaited dirty Roto current-frame flush before standalone Tauri close resumes
  - Separate editable real-key Roto frame classification from occupied/background-only frames
  - Full-strength cached Roto reference overlay with export reset ordering preserved
affects: [physics-paint-roto-cache, standalone-close, workflow-strip]

tech-stack:
  added: []
  patterns:
    - Tauri close-request prevention with one-shot resume after awaited flush
    - Separate occupied/cached frame state from editable-session frame markers
    - Cached reference visibility through outline styling instead of opacity attenuation

key-files:
  created:
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-07-SUMMARY.md
  modified:
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintStudio.css

key-decisions:
  - "Roto close requests are prevented once, flushed through the existing apply-canvas path, then resumed via the Tauri window close API."
  - "Editable-session pink cells are driven only by real editable stroke content in the open session; cached/background-only occupancy remains separate."
  - "Cached Roto references stay full opacity and use outline treatment for reference status so old cache pixels are not visually diminished."

patterns-established:
  - "Use editableRotoFrames for pink session cells and occupied/saved/cached markers for gray/green cache state."
  - "Reset cached Roto preview background immediately before buildRotoOutputFrame to prevent replacement export compositing."

requirements-completed: [PH36.2-CACHE]

duration: 5min
completed: 2026-06-17
status: complete
---

# Phase 36.2 Plan 07: Roto Close Flush and Cache Cell Semantics Summary

**Dirty Roto close now awaits the normal apply-canvas flush, cached/background-only frames keep gray/green semantics, and cached references reopen at full strength.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-17T19:15:21Z
- **Completed:** 2026-06-17T19:20:15Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added an ordered standalone close path that prevents the first Tauri close request, awaits `flushCurrentRotoFrameBeforeClose`, then resumes close after the existing single-frame apply path runs.
- Split editable Roto session frames from occupied/saved/cache frame lists so empty navigation and background-only persistence no longer turn cells pink.
- Removed cached Roto reference opacity attenuation while keeping reset-before-export ordering covered by tests.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make close/unload Roto flush awaited and single-path** - `205d3590` (feat)
2. **Task 2: Separate editable real keys from occupied and background-only frames** - `6f540b53` (fix)
3. **Task 3: Restore cached Roto reference strength without export compositing** - `7d33d491` (fix)

**Plan metadata:** pending final docs commit

## Files Created/Modified

- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - Orders close through awaited Roto flush, tracks editable Roto frames separately, and preserves cache reset before replacement export.
- `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` - Adds close flush, editable/cached/background-only classification, and cached reference opacity/export ordering contracts.
- `app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts` - Verifies Studio passes explicit editable Roto frames rather than occupied frames.
- `app/src/components/physic-paint/physicsPaintStudio.css` - Shows cached Roto references at full opacity with a dashed outline reference indicator.

## Verification

- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts` - passed
- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts` - passed
- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck` - passed

## Decisions Made

- Roto close requests should reuse the validated `apply-canvas` flush path rather than introducing a second persistence channel.
- Background-only Roto frames may be occupied/saved for cache durability but must not count as editable session frames.
- Cached reference visibility should be communicated by border/outline styling, not lowered opacity.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None found in files modified by this plan.

## Threat Flags

None. The plan touched the expected close/apply bridge and cached reference export boundary already covered by the threat model.

## Issues Encountered

- The Context7 CLI fallback was present with a different argument shape than expected, so current Tauri close API usage was implemented from the installed project API surface and verified by source/typecheck rather than fetched docs.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- UAT gap 2 close-loss, reference opacity, and cell color regressions have focused source-contract coverage.
- Later Roto cache work should continue to keep cached-only references, background-only persistence, and editable stroke state as distinct concepts.

## Self-Check: PASSED

- Found summary file and all modified source/test/style files.
- Found task commits `205d3590`, `6f540b53`, and `7d33d491` in git history.

---
*Phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-*
*Completed: 2026-06-17*
