---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 02
type: execute
wave: 2
depends_on: [36.2-01]
files_modified:
  - app/src/components/physic-paint/physicsPaintWorkflowState.ts
  - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: false
requirements:
  - PH36.2-CACHE
must_haves:
  truths:
    - "D-11/D-12/D-13/D-14/D-15/D-16: Roto cells use exactly gray, green, and pink semantic fills; the current frame is outline-only over the fill."
    - "D-10: The Roto primary action is Save pending or Save current, flushes only unsaved dirty frame state, and is disabled when nothing is dirty."
    - "D-03: Cached-only frames communicate repaintable-not-stroke-editable through subtle status text, not a modal."
  artifacts:
    - path: "app/src/components/physic-paint/physicsPaintWorkflowState.ts"
      provides: "Pure Roto cell fill/status helper"
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Gray/green/pink cells, Save pending/current control, subtle cached-only status"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Semantic Roto fill classes and outline-only current frame styling"
  key_links:
    - from: "PhysicsPaintWorkflowStrip.tsx"
      to: "physicsPaintWorkflowState.ts"
      via: "imports helper to classify Roto cells"
      pattern: "getRotoCellFill"
  prohibitions:
    - statement: "No fourth dirty fill color is added to Roto cells."
      status: "resolved"
      verification: "Tests assert only empty/cached-only/editable-session fill classes carry semantic color; pending state uses text or marker class only."
---

<objective>
Implement the standalone Roto spreadsheet's user-visible cache semantics from D-03 and D-10 through D-16.

Purpose: Users must immediately understand which Roto frames are empty, cached-only, or editable in the current session, without confusing dirty state or current-frame state with a fourth semantic color.
Output: Tested workflow helper, component props/rendering, CSS fills, and a human visual checkpoint for gray/green/pink/current outline fidelity.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
@SPECS/phase-36.2-physics-paint-roto-cache.md
@app/src/components/physic-paint/physicsPaintWorkflowState.ts
@app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@app/src/components/physic-paint/physicsPaintStudio.css
@.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure Roto cell fill and pending-state helpers</name>
  <files>app/src/components/physic-paint/physicsPaintWorkflowState.ts, app/src/components/physic-paint/physicsPaintWorkflowState.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - app/src/components/physic-paint/physicsPaintWorkflowState.test.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
  </read_first>
  <behavior>
    - Editable-session frames classify as pink even if a cached PNG also exists.
    - Cached-only frames classify as green when cached PNG exists and no editable session state exists.
    - Empty frames classify as gray when neither editable session state nor cached PNG exists.
    - Pending/dirty state is represented as a separate boolean/label and never changes semantic fill result.
  </behavior>
  <action>Add `RotoCellFill = 'empty' | 'cached-only' | 'editable-session'` and `getRotoCellFill(frame, cachedFrames, editableFrames)` to `physicsPaintWorkflowState.ts`. Add a small `getRotoPendingLabel(hasPending, isSaving)` helper returning concise strings such as `Unsaved Roto frame pending` and `Saving Roto frame...` for subtle status text. Tests must cite D-11 through D-16 and prove current-frame state is not an input to the fill helper.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/physicsPaintWorkflowState.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Test shows frame 5 with `editableFrames` and `cachedFrames` returns `editable-session`.
    - Test shows frame 6 with only `cachedFrames` returns `cached-only`.
    - Test shows frame 7 with neither returns `empty`.
    - No helper returns a value named dirty, yellow, orange, or current as a semantic fill.
  </acceptance_criteria>
  <done>Cell classification is deterministic and source-testable before component rendering changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render gray/green/pink Roto cells and Save pending/current</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintStudio.css
    - app/src/components/physic-paint/physicsPaintWorkflowState.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
  </read_first>
  <behavior>
    - The Roto primary action label is `Save pending` or `Save current`, not the old `Save roto frame` primary copy.
    - The action is disabled when there is no pending dirty Roto state or when `ready === false`.
    - Cell classes map empty/cached-only/editable-session to gray/green/pink fills.
    - `.physics-paint-roto-cell.current` changes border/outline/box-shadow only and does not set `background`.
  </behavior>
  <action>Extend `PhysicsPaintWorkflowStripProps` with concrete fields: `cachedRotoFrames?: PhysicPaintRotoCacheFrame[]`, `editableRotoFrames?: number[]`, `pendingRotoFrames?: number[]`, `rotoSaveInFlight?: boolean`, and `onSavePendingRotoFrames: () => void`. Before calling `getRotoCellFill`, derive the cached-cell set from `cachedRotoFrames.filter(frame => frame.source === 'real-key')` only; generated interpolation entries with `source === 'generated-interpolation'` must be excluded from green/pink square rendering and passed only to connector/cache UI introduced in Plan 06. Render Roto cells using `getRotoCellFill` and class names `roto-fill-empty`, `roto-fill-cached-only`, and `roto-fill-editable-session`. Replace the old Roto save button label with `Save pending` when any pending frame exists and `Save current` when only current-frame dirty state is pending, and disable it when no pending frames are supplied. Add subtle cached-only copy: `Cached reference: repaintable, not stroke-editable` when the current frame is cached-only per D-03. Update CSS colors: gray neutral dark, green cached fill, pink editable fill; current uses border/outline/box-shadow without setting background.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Source/component tests find `roto-fill-empty`, `roto-fill-cached-only`, and `roto-fill-editable-session` in `PhysicsPaintWorkflowStrip.tsx`.
    - Source/component tests assert `cachedRotoFrames` is typed or consumed with `PhysicPaintRotoCacheFrame` metadata and that generated interpolation entries are filtered before square cell fill calculation.
    - Source tests assert `Save roto frame` no longer appears in `PhysicsPaintWorkflowStrip.tsx` outside historical test text.
    - Source tests assert `Save pending` or `Save current` appears and the button uses pending/ready disabled logic.
    - CSS for `.physics-paint-roto-cell.current` contains border/outline/box-shadow styling and no `background:` declaration in that rule.
  </acceptance_criteria>
  <done>Roto spreadsheet and action copy match locked color/save semantics in source and tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification for Roto cell semantics</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/physicsPaintStudio.css
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
  </read_first>
  <action>Pause after automated implementation so the user can verify the exact visible Roto spreadsheet surface. Do not run the dev server; the project instruction says the user runs it.</action>
  <what-built>Gray/green/pink Roto spreadsheet cells, outline-only current frame, subtle cached-only status, and Save pending/current button state.</what-built>
  <how-to-verify>
    1. User runs the app/server locally.
    2. Open a Physics Paint layer in Roto mode.
    3. Verify empty cells are gray, cached-only cells are green after reopening cached output, editable session cells are pink, and the current frame is only a bright outline over the existing fill.
    4. Verify the primary Roto action reads Save pending or Save current, is disabled with no pending dirty frame, and does not offer default Save all behavior.
  </how-to-verify>
  <verify><automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/physicsPaintWorkflowState.test.ts --run</automated></verify>
  <acceptance_criteria>
    - Human confirms current-frame outline does not obscure gray/green/pink fills.
    - Human confirms Save pending/current is disabled with no pending dirty frame and no default Save all action is offered.
  </acceptance_criteria>
  <done>Human confirms the Roto spreadsheet visual semantics and Save pending/current state match D-10 through D-16.</done>
  <resume-signal>Type "approved" or describe the visible mismatch.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `RotoCellFill`
- `getRotoCellFill(frame, cachedFrames, editableFrames)`
- `getRotoPendingLabel(hasPending, isSaving)`
- `cachedRotoFrames` workflow strip prop typed as `PhysicPaintRotoCacheFrame[]`, with generated interpolation frames filtered out of square-cell fill rendering
- `editableRotoFrames` workflow strip prop
- `pendingRotoFrames` workflow strip prop
- `rotoSaveInFlight` workflow strip prop
- `onSavePendingRotoFrames` workflow strip prop
- CSS classes `roto-fill-empty`, `roto-fill-cached-only`, `roto-fill-editable-session`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store/launch cache summary -> UI | Cached frame data controls visual state and available actions. |
| User click -> standalone navigation/save callbacks | UI events may trigger frame navigation or cache mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-04 | Tampering | `PhysicsPaintWorkflowStrip` class derivation | mitigate | Derive fill classes from typed helper inputs only; generated/dirty/current state must not masquerade as real editable keys. |
| T-36.2-05 | Denial of Service | Save pending button | mitigate | The button calls only pending/current flush callback and never defaults to all-frame rerender per D-09/D-10. |
| T-36.2-06 | Information Disclosure | Cached-only status text | accept | Text reveals only local cache/editability state; no PII or external data. |
| T-36.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit states no install. |
</threat_model>

<verification>
Automated focused tests plus the blocking human visual check prove semantic fill fidelity. Full wave gate: `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- --run` and `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
User can distinguish empty, cached-only, and editable-session Roto frames without a dirty color, and can flush only pending/current dirty state without a default Save all action.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-02-SUMMARY.md` when done.
</output>
