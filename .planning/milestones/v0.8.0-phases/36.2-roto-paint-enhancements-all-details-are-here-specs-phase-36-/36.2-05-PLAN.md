---
phase: 36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-
plan: 05
type: execute
wave: 4
depends_on: [36.2-02, 36.2-03, 36.2-04]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: false
requirements:
  - PH36.2-CACHE
  - PH36.2-PLAYBACK
must_haves:
  truths:
    - "Spec: Roto Play previews cached Roto frames only and does not run editable stroke animation replay."
    - "D-18/D-19/D-20: Roto Play missing frames follow transparent/background virtual behavior without creating cached cells."
    - "D-09/D-10: Roto playback controls do not re-render all frames by default; Save pending/current remains the only flush action."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Roto cached Play button and playback status copy"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Cached Roto playback state using cached PNG frame lookup"
  key_links:
    - from: "PhysicsPaintStudio.tsx"
      to: "physicPaintStore.ts"
      via: "cached frame lookup during Roto playback"
      pattern: "getFrame\(.*roto"
  prohibitions:
    - statement: "Roto Play must not call AnimationPlayer over editable strokes or render all frames before playback."
      status: "resolved"
      verification: "Tests assert Roto playback uses cached frame data/status and no AnimationPlayer path for Roto mode."
---

<objective>
Add Roto cached playback UX after the cache cells, reference navigation, and playback/export foundations are in place.

Purpose: Users need to preview the cache-first Roto animation quickly from the standalone UI without triggering costly all-frame rendering or treating Roto like a Play script.
Output: Roto Play control, cached-frame playback state, missing-frame status, and human verification steps.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
@.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-PATTERNS.md
@SPECS/phase-36.2-physics-paint-roto-cache.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Roto cached Play control and status surface</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintStudio.css
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
    - SPECS/phase-36.2-physics-paint-roto-cache.md
  </read_first>
  <behavior>
    - Roto mode exposes a clear `Play` button separate from Save pending/current.
    - The Play button starts cached playback only; it does not imply rendering.
    - Missing cache status is shown as text such as `Missing frames play transparent/background` rather than a modal.
  </behavior>
  <action>Extend `PhysicsPaintWorkflowStripProps` with `rotoCachedPlaybackAvailable?: boolean`, `rotoCachedPlaybackStatus?: string | null`, `onToggleRotoPlayback?: () => void`, and `isRotoCachedPlaybackActive?: boolean`. In Roto mode, render a compact `Play` / `Stop` control beside navigation controls. Keep Save pending/current as the only render/apply action. Add CSS for a modest transport-style button using existing dark UI classes; do not add a new bright semantic cell color.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Source/component tests find `onToggleRotoPlayback`, `rotoCachedPlaybackAvailable`, and `isRotoCachedPlaybackActive` props.
    - Source/component tests find Roto `Play` and `Stop` copy but do not find any Roto control labeled `Render all` or `Save all`.
    - CSS changes do not add semantic Roto cell colors beyond empty/cached-only/editable-session from Plan 02.
  </acceptance_criteria>
  <done>Workflow strip exposes cached Roto playback without changing save/render semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cached Roto playback in Studio</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - app/src/stores/physicPaintStore.ts
    - app/src/lib/previewRenderer.ts
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-RESEARCH.md
  </read_first>
  <behavior>
    - Roto cached playback iterates cached PNG frames from launch/store/local preview refs.
    - Missing frames use transparent/background virtual behavior and do not force cache generation.
    - Playback stops cleanly when the user exits Roto mode, changes frame manually, or starts editing.
    - Roto playback code does not call `AnimationPlayer` for Roto mode.
  </behavior>
  <action>Add `isRotoCachedPlaybackActive` state and a timer/requestAnimationFrame loop that advances through the layer's Roto frame range using cached frame lookup only. Display frames via the same cached reference overlay path introduced in Plan 03. Stop playback on edit intent, workflow mode change, or explicit Stop. Pass availability/status/toggle props to `PhysicsPaintWorkflowStrip`. Reuse fps from `launchContext.fps` with safe fallback to existing project default; do not add a new replay/render engine path.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - Source contract finds cached Roto playback state and toggle passed to `PhysicsPaintWorkflowStrip`.
    - Source contract verifies Roto cached playback uses cached frame lookup helpers and not `AnimationPlayer` in the Roto branch.
    - Source contract finds playback stop conditions for edit intent or manual navigation.
    - Full focused tests pass without adding a one-off test config.
  </acceptance_criteria>
  <done>Standalone Roto can preview cached PNG animation without rendering or replaying strokes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification for cached Roto playback</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
    - .planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-CONTEXT.md
  </read_first>
  <what-built>Roto cached Play/Stop preview using saved PNG frames and missing-frame status.</what-built>
  <action>Ask the user to verify only the visual/interactive Roto playback behavior because the project instruction says the user runs the app/server.</action>
  <how-to-verify>
    1. User runs the app/server locally.
    2. Paint and navigate across at least two Roto frames so they are cached.
    3. Click Play in Roto mode and verify cached frames preview quickly without a render-all delay.
    4. Stop playback, navigate manually, and verify the preview stops and the current frame remains a cached reference or editable state as appropriate.
  </how-to-verify>
  <verify><automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts --run</automated></verify>
  <acceptance_criteria>
    - Human confirms Play/Stop previews cached Roto frames without triggering a Render all/Save all flow.
    - Human confirms missing frames do not create new green cached cells during playback.
  </acceptance_criteria>
  <done>Human confirms cached Roto playback previews saved PNG frames without all-frame rendering.</done>
  <resume-signal>Type "approved" or describe the playback mismatch.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `rotoCachedPlaybackAvailable` workflow strip prop
- `rotoCachedPlaybackStatus` workflow strip prop
- `onToggleRotoPlayback` workflow strip prop
- `isRotoCachedPlaybackActive` workflow strip prop/state
- Cached Roto playback loop in `PhysicsPaintStudio.tsx`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cached PNG map -> standalone preview | Cached data URLs are displayed in the standalone playback surface. |
| User playback controls -> animation loop | User starts/stops local cached playback loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-13 | Denial of Service | Roto playback loop | mitigate | Use existing fps/default clamping, stop loop on mode change/navigation/edit, and do not render/replay strokes. |
| T-36.2-14 | Tampering | Missing-frame playback | mitigate | Missing frames are displayed virtually and do not mutate store cache. |
| T-36.2-15 | Spoofing | UI status | accept | Local-only preview status; no remote trust boundary. |
| T-36.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; Package Legitimacy Audit states no install. |
</threat_model>

<verification>
Focused Studio/Strip tests plus human visual check. Full wave gate: `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- --run` and `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`.
</verification>

<success_criteria>
The standalone Roto Play button previews cached frames only, never forces all-frame render, and does not create cache for missing frames.
</success_criteria>

<output>
Create `.planning/phases/36.2-roto-paint-enhancements-all-details-are-here-specs-phase-36-/36.2-05-SUMMARY.md` when done.
</output>
