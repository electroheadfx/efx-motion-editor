# Phase 36.6: Physics Paint Roto Save On Leave - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-20
**Phase:** 36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase
**Areas discussed:** Navigation timing, Saving feedback, Failure behavior, Manual save role

---

## Navigation Timing

| Option | Description | Selected |
|--------|-------------|----------|
| Navigation timing | Whether dirty-frame navigation waits strictly for save completion and how rapid clicks are queued/collapsed. | ✓ |
| Saving feedback | What the animator sees while navigation waits. | |
| Failure behavior | What happens if save-on-leave fails. | |
| Manual save role | Whether Save current remains visible and how it behaves. | |

**User's choice:** Discuss all four areas, with a narrow-scope note.
**Notes:** User explicitly locked scope: existing Roto navigation already works; do not rebuild navigation. Painting must only mark the current frame dirty. Save/render cache must happen once at the frame-leave boundary, using the existing Save current/cache path, with minimal visible pending feedback.

### Questions

| Question | Options presented | User's choice / notes |
|----------|-------------------|-----------------------|
| When a dirty Roto frame is left, which navigation timing should downstream planning lock? | Wait then switch; Switch after snapshot; You decide | Wait then switch, but only at the frame-leave boundary. Painting marks dirty only and never renders cache per stroke. Keep source visible, save once through existing path, then open destination. |
| If the animator clicks multiple destination frames while the source frame is saving, what should happen? | Use latest click; Ignore extra clicks; Disable clicks | Use latest click. Rapid clicks must not trigger multiple saves. Save original dirty source once, then open only the latest requested destination. |
| Which existing frame-leave triggers should use save-on-leave in this MVP? | All Roto nav; Cell clicks only; Buttons only | Apply save-on-leave to all existing Roto frame navigation paths only. Do not rebuild navigation or add new navigation controls. |
| Should save-on-leave run when the current Roto frame is clean? | No save; Validate only; You decide | No save. |

---

## Saving Feedback

| Question | Options presented | User's choice / notes |
|----------|-------------------|-----------------------|
| What minimal feedback should the animator see while a dirty frame is saving before navigation switches? | Cell + status; Cell only; Blocking overlay | Cell + status. |
| During the save wait, which controls should remain interactive? | Queue nav only; Disable all; Keep all active | Queue nav only. |
| What status wording should downstream agents use for the MVP? | Saving frame N…; Saving before leaving…; You decide | Saving frame N… |
| After the save completes and navigation switches, should there be any extra success notification? | No toast; Brief status; Toast | No toast. |

**Notes:** Feedback should remain minimal: pending/saving cell state plus one compact status line, without modal or toast noise.

---

## Failure Behavior

| Question | Options presented | User's choice / notes |
|----------|-------------------|-----------------------|
| If the automatic save-on-leave fails, what should the editor do? | Stay source; Prompt choice; Switch anyway | Stay source. |
| What should happen to the pending/latest destination after a save-on-leave failure? | Clear it; Keep it; You decide | Clear it. |
| What retry path should be exposed after save-on-leave fails? | Next nav retries; Save current; Retry button | Next nav retries. |
| How should prior cached content be handled if save-on-leave fails? | Preserve cache; Mark missing; You decide | Preserve cache. |

**Notes:** Failure must be data-safe: no destination switch, previous cache untouched, dirty source remains visible, and the next navigation attempt retries.

---

## Manual Save Role

| Question | Options presented | User's choice / notes |
|----------|-------------------|-----------------------|
| Now that dirty frames save on leave, what should happen to the existing Save current action? | Keep visible; De-emphasize; Hide it | Keep visible. |
| When Save current is clicked manually on a dirty frame, should it also navigate anywhere if a queued destination exists? | No navigate; Use queue; You decide | No navigate. |
| Should the UI copy explain auto-save-on-leave explicitly, or keep the existing Save current wording only? | Subtle hint; No copy; Prominent copy | Subtle hint. |
| Should save-on-leave change the semantics of close behavior from Phase 36.4? | No change; Auto-close save; You decide | No change. |

**Notes:** Save current remains the explicit manual cache action. Save-on-leave affects frame-to-frame Roto navigation only and does not change dirty-close choices.

---

## Claude's Discretion

None.

## Deferred Ideas

None.
