---
phase: 36.6
slug: physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase
status: verified
threats_open: 0
asvs_level: 1
register_authored_at_plan_time: true
created: 2026-06-21T08:08:23Z
---

# Phase 36.6 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Editor frame state → Roto save coordinator | User navigation requests move from UI controls/keyboard into save-on-leave coordination. | Frame numbers, dirty-state flags, pending destination. |
| Save coordinator → existing Roto cache/apply path | Dirty source frame is sent through the existing Save current/cache path before opening a destination. | Apply payload, source frame, operation id, cached PNG/delete state. |
| Standalone/apply result → editor message listener | Apply results return asynchronously and can advance or retain editor frame state. | `operationId`, `kind`, `startFrame`, success/failure state. |
| Studio → workflow strip | Source-frame saving state is rendered as compact status and action-disabled UI. | `rotoSavingFrame`, `rotoSaveInFlight`, pending/dirty flags. |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-36.6-01 | Tampering | `requestRotoFrameNavigation` | mitigate | Target and source frames are validated as non-negative integers before snapshot, sync, or flush. `PhysicsPaintStudio.test.ts` covers invalid frame rejection and source-contract routing. | closed |
| T-36.6-02 | Tampering | `flushRotoFrame` save-on-leave source selection | mitigate | The coordinator captures `sourceFrame = currentFrame` before destination navigation and passes that source into `flushRotoFrame(sourceFrame, { force: true, advanceToFrame: targetFrame })`, preserving source-specific cache writes. | closed |
| T-36.6-03 | Tampering / Data loss | rapid navigation queue | mitigate | Active save-on-leave requests update `pendingRotoAdvanceRef.current` to the latest destination without starting duplicate source saves. Plan 03 regression coverage verifies the stale applying-state interval. | closed |
| T-36.6-04 | Tampering / Data loss | apply failure handling | mitigate | Failed result, send error, and timeout branches clear the queued destination, restore dirty source state, clear source-saving refs, and avoid post-save destination opening. | closed |
| T-36.6-05 | Spoofing | apply result message listener | mitigate | Existing same-origin and active-operation matching are preserved before consuming apply results, and `pendingApplyRef` records `operationId`, `kind`, and `startFrame` for matching source results. | closed |
| T-36.6-06 | Tampering | `getRotoPendingLabel` | mitigate | Source-frame status copy uses the provided frame only when it is a non-negative integer; otherwise it falls back to the existing current-frame saving copy. | closed |
| T-36.6-07 | Denial of Service | Workflow strip navigation during save | mitigate | Roto navigation remains available to update queued destination while Save current and destructive actions are disabled during `rotoSaveInFlight`. | closed |
| T-36.6-08 | Repudiation | ambiguous save feedback | mitigate | The compact status line identifies the source frame with `Saving frame N…`; UAT confirmed source-specific feedback. | closed |
| T-36.6-09 | Tampering | destructive/convert actions during save | mitigate | Workflow strip disabled conditions remain keyed to `rotoSaveInFlight` for Save current and destructive/convert actions. | closed |
| T-36.6-SC | Tampering | package installs | accept | No package-manager install task exists for this phase; no dependency changes were introduced. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-36.6-SC | T-36.6-SC | No package-manager install task exists for this phase; no dependency change requires supply-chain mitigation. | Claude security gate | 2026-06-21 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-06-21 | 10 | 10 | 0 | Claude security gate |

---

## Evidence

| Evidence | Result |
|----------|--------|
| `36.6-01-PLAN.md`, `36.6-02-PLAN.md`, and `36.6-03-PLAN.md` contain parseable plan-time threat models. | `register_authored_at_plan_time: true` |
| `PhysicsPaintStudio.tsx` contains frame validation, source-frame capture, pending queue, apply-result matching, and failure cleanup paths. | Mitigations present |
| `PhysicsPaintWorkflowStrip.tsx` and `physicsPaintWorkflowState.ts` contain source-frame feedback and disabled-action wiring. | Mitigations present |
| `PhysicsPaintStudio.test.ts`, `PhysicsPaintWorkflowStrip.test.ts`, and `physicsPaintWorkflowState.test.ts` cover the source-contract mitigations. | Automated verification present |
| `36.6-UAT.md` completed with 3/3 passing runtime UAT checks. | Human-visible behavior confirmed |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-06-21
