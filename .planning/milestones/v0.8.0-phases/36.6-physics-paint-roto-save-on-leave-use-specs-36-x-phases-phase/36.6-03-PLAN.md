---
phase: 36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase
plan: 03
type: execute
wave: 3
depends_on: [36.6-02]
files_modified:
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
gap_closure: true
requirements:
  - 36.6-AC-01
  - 36.6-AC-02
  - 36.6-AC-03
  - 36.6-AC-04
  - 36.6-AC-05
  - 36.6-AC-06
  - 36.6-FB-01
must_haves:
  truths:
    - "Dirty Roto source-frame navigation saves the source frame once, then opens the requested destination after the matching successful apply result, per D-02 and D-03."
    - "The post-save destination advance is not blocked by a stale applying-state closure while applyStatus was still applying, per D-02."
    - "Failed save-on-leave keeps the dirty source frame, clears the queued destination, and preserves previous cached output, per D-10, D-11, and D-12."
    - "Manual Save current saves the current frame and does not navigate after success, per D-13 and D-14."
    - "Rapid Roto navigation during the whole save-on-leave active interval, including the post-send/pre-result applying window, keeps only the latest requested destination and does not start a duplicate source save, per D-03 and D-08."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      provides: "Regression contract for successful save-on-leave destination advance across stale applyStatus boundary"
      contains: "stale applyStatus"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Internal post-save destination-opening path that bypasses the public navigation guard only for successful matching save-on-leave apply results"
      contains: "pendingRotoAdvanceRef"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts"
      via: "source-contract test asserts the apply-result success branch uses the internal post-save advance path instead of the guarded navigateToSyncedFrame path"
      pattern: "handleApplyResult"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "requestRotoFrameNavigation stores latest destination in pendingRotoAdvanceRef for the whole save-on-leave active interval, from pre-send through post-send/pre-result applying state; handleApplyResult consumes it only after successful matching source-frame apply result"
      pattern: "pendingRotoAdvanceRef\\.current"
---

<objective>
Create a focused Phase 36.6 gap-closure implementation prompt for the diagnosed dirty Roto save-on-leave navigation failure.

Purpose: close the UAT failure where the source frame saves successfully but the requested destination frame does not open because the post-save advance calls the public guarded navigation function while the handler closure still sees applyStatus as applying.
Output: one RED/GREEN/verification plan that modifies only PhysicsPaintStudio source and its existing test file during execution.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-CONTEXT.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-UAT.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-01-SUMMARY.md
@.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-02-SUMMARY.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — capture successful save-on-leave destination advance across stale applying state</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <behavior>
    - Dirty Roto source-frame click stores the requested destination, saves the source frame once, and after the successful matching apply result opens the requested destination per D-02.
    - The success advance path must not call the public navigation path that returns false while applyStatus is still applying in the stale handler closure.
    - Rapid navigation remains latest-destination-only for the whole save-on-leave active interval per D-03 and D-08, including after `flushRotoFrame` has returned its payload and `rotoFlushInFlightRef.current` has been cleared but before the matching apply result has arrived.
    - Rapid navigation in that post-send/pre-result applying window must update `pendingRotoAdvanceRef.current` to the newest target without starting a second `flushRotoFrame` for the same save-on-leave source frame.
    - Manual Save current has no queued destination and does not navigate after success per D-14.
    - Failed save-on-leave still clears the queued destination, keeps the dirty source frame, and preserves cached output per D-10, D-11, and D-12.
  </behavior>
  <action>Add one focused regression test in the existing `PhysicsPaintStudio Roto cache-first autosave contract` describe block. Keep the existing source-contract testing style that reads `PhysicsPaintStudio.tsx` as text. The test must fail before the production fix by asserting the handleApplyResult success branch no longer performs the queued-destination advance through the guarded public navigation callback and instead uses an internal post-save Roto frame opening path whose block does not contain the applying-state guard. Include assertions that the manual save path still passes `null` from `onSaveRotoFrame`, that failure handling still clears `pendingRotoAdvanceRef.current`, and that rapid navigation still updates `pendingRotoAdvanceRef.current = targetFrame` while save-on-leave is active. The rapid-navigation assertion must cover the post-send/pre-result applying window where `saveOnLeaveSourceFrameRef.current` still identifies the source frame but `rotoFlushInFlightRef.current` may already be null after the flush promise `finally`; in that window a new click must update the queued latest destination without starting another `flushRotoFrame`. Require the test to look for a coordinator guard based on the save-on-leave source/apply-result pending state, not only the conjunction of `saveOnLeaveSourceFrameRef.current` and `rotoFlushInFlightRef.current`. Cite D-02, D-03, D-08, D-10, D-11, D-12, D-14 in the test name or nearby assertion comments only if comments already match local test style; avoid broad rewrites.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts -t "stale applyStatus" --run</automated>
  </verify>
  <done>The targeted regression test fails before the fix because `handleApplyResult` still routes `nextFrame` through `navigateToSyncedFrame(nextFrame)` or equivalent guarded path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add an internal post-save Roto destination opener that bypasses only the stale applying guard</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <behavior>
    - Successful dirty-source save-on-leave opens the queued destination even if the apply-result closure still has applyStatus as applying.
    - The source frame is still saved through `flushRotoFrame` and the existing apply-canvas/delete-roto result path.
    - Rapid clicks during the whole save-on-leave active interval, including after `flushRotoFrame` settles and before the matching apply result, overwrite the queued destination and do not trigger duplicate saves.
    - Failed save-on-leave does not open the destination and leaves retry behavior intact.
    - Manual Save current still saves and stays on the current frame.
  </behavior>
  <action>Implement the narrow fix in `PhysicsPaintStudio.tsx`. Add or extract an internal Roto frame opening helper for already-authorized post-save advances, for example `openSyncedRotoFrameAfterSave(targetFrame)`, that performs the safe subset of `navigateToSyncedFrame`: validate the target frame, stop cached Roto playback, clear cached playback overlay, load the target frame's local editable state when present or clear/reset/load cached reference when absent, send `sendPhysicPaintFrameSyncMessage(targetFrame, bridgeMode)`, and update `launchContext.startFrame`. This internal helper must not check `rotoFlushInFlightRef.current` or `applyStatus === 'applying'`, because it is called only after the matching successful apply result consumes `pendingRotoAdvanceRef.current`. Keep the public `navigateToSyncedFrame` guard unchanged for user-initiated clean navigation and general navigation safety. Expand `requestRotoFrameNavigation` so the no-duplicate-save queueing branch covers the whole save-on-leave active interval: if `saveOnLeaveSourceFrameRef.current` still identifies the dirty source awaiting its matching apply result, update `pendingRotoAdvanceRef.current = targetFrame` and return without calling `flushRotoFrame`, even when `rotoFlushInFlightRef.current` has already been cleared after the flush promise settled. Use explicit pending apply/save-on-leave state such as `saveOnLeaveSourceFrameRef` plus the pending apply operation/result state already available in the component; do not rely only on `saveOnLeaveSourceFrameRef.current !== null && rotoFlushInFlightRef.current`. In `handleApplyResult`, after the success branch snapshots `nextFrame`, commits the saved source cache mutation, deletes the source from `dirtyRotoFramesRef`, clears `pendingRotoAdvanceRef`, clears save-on-leave refs/state, and syncs pending frames, call the internal helper for `nextFrame !== null` instead of the guarded public callback. Successful save-on-leave must not add an extra modal, toast, or success notification beyond destination opening plus existing saved/cached cell state per D-09; do not introduce or expand a success message for this path. Do not change the failure branch, timeout branch, send-error cleanup, dirty close flow, or manual `saveRotoFrame(null)` behavior except where needed to share the helper safely.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts -t "stale applyStatus" --run</automated>
  </verify>
  <done>The targeted stale-applying-state regression passes, successful save-on-leave can open the queued destination after source save, and no failed-save/manual-save/rapid-queue behavior is weakened.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full save-on-leave contract and summarize gap closure</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, .planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-03-SUMMARY.md</files>
  <action>Run the focused and full PhysicsPaintStudio test gates. Inspect failures before changing anything else. If Vitest continues watching despite `--run`, stop it only after PASS or FAIL output is visible. Create the normal plan summary at `.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-03-SUMMARY.md` with the diagnosis, files changed, verification commands, and note that app source behavior was intentionally limited to the stale applying-state boundary. Do not run the dev server; the project instruction says the user runs it.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts -t "save-on-leave" --run</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts --run</automated>
  </verify>
  <done>Both test commands pass, the summary exists, and the final implementation preserves: source frame saves, failed saves retain dirty source and previous cached output, rapid navigation keeps latest destination, and manual Save current does not navigate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physics Paint window -> EFX Motion parent bridge | Apply and frame-sync messages cross the standalone/editor window boundary. |
| User Roto navigation -> local state/cache refs | Untrusted rapid user clicks update queued destination state while a save is in flight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.6-03-01 | Tampering | `handleApplyResult` post-save navigation | mitigate | Only consume `pendingRotoAdvanceRef.current` after matching `detail.operationId === activeOperationIdRef.current` and `detail.ok`; keep existing origin/type guards for message results. |
| T-36.6-03-02 | Denial of Service | Rapid Roto navigation during save-on-leave | mitigate | Preserve single save-on-leave source save and latest-destination overwrite behavior across the whole active interval; do not start additional saves while `saveOnLeaveSourceFrameRef` still identifies a source awaiting its matching apply result, even if `rotoFlushInFlightRef` has already settled. |
| T-36.6-03-03 | Tampering | Failed save-on-leave cache state | mitigate | Preserve existing failure/timeout/send-error behavior that re-adds the source frame to dirty state, clears queued destination, and delays cached output mutation until matching success. |
| T-36.6-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Run, at minimum:

1. `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts -t "stale applyStatus" --run`
2. `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts -t "save-on-leave" --run`
3. `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app test -- PhysicsPaintStudio.test.ts --run`

Do not run the development server.
</verification>

<success_criteria>
- The dirty source frame still saves through the existing Roto apply/cache path.
- A successful matching save-on-leave result opens the latest requested destination even across the stale applying-state closure boundary.
- Failed save-on-leave leaves the user on the dirty source frame, clears queued navigation, and preserves previous cached output.
- Rapid navigation during the whole save-on-leave active interval, including the post-send/pre-result applying window, opens only the latest destination after success and does not start duplicate source saves.
- Manual Save current remains isolated and does not navigate.
- Phase 36.4 dirty close behavior is unchanged.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Phase 36.6 user can move between Roto frames without manually pressing Save current every time | Covered by Tasks 1-2 stale applying-state regression and fix. |
| REQ | 36.6-AC-01 through 36.6-AC-06, 36.6-FB-01 | Covered by preserving existing save-on-leave coordinator, feedback, failure, and manual save contracts while fixing successful destination advance. |
| CONTEXT | D-01 painting only marks dirty | Preserved; no brush/input changes. |
| CONTEXT | D-02 save source once then open requested destination | Covered by Tasks 1-2. |
| CONTEXT | D-03 rapid changes open latest destination | Covered by Tasks 1 and 3. |
| CONTEXT | D-04 all existing Roto navigation paths | Preserved by keeping requestRotoFrameNavigation as the existing coordinator. |
| CONTEXT | D-05 clean navigation immediate | Preserved by keeping public navigateToSyncedFrame guard/path unchanged for clean navigation. |
| CONTEXT | D-06 through D-09 saving feedback | Preserved; no workflow strip changes. |
| CONTEXT | D-10 through D-12 failure behavior | Covered by Tasks 1-3 preservation gates. |
| CONTEXT | D-13 through D-15 manual Save role | Covered by Tasks 1 and 3 manual save isolation gates. |
| CONTEXT | D-16 close behavior unchanged | Covered by Task 3 preservation criteria. |
| RESEARCH | Reuse existing Save current/Roto cache path; do not rebuild navigation | Covered by Task 2 using existing flush/apply success path and preserving public navigation. |
</source_audit>

<output>
Create `.planning/phases/36.6-physics-paint-roto-save-on-leave-use-specs-36-x-phases-phase/36.6-03-SUMMARY.md` when done.
</output>
