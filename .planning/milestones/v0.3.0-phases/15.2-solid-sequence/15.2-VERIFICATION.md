---
phase: 15.2-solid-sequence
verified: 2026-03-22T15:09:16Z
status: human_needed
score: 8/8 must-haves verified
re_verification: false
human_verification:
  - test: "Open app, create content sequence, verify split add button shows Camera icon (top half) and Square icon (bottom half) instead of old Plus button"
    expected: "Two-zone button: Camera on top navigates to imported images, Square on bottom adds black solid cell immediately"
    why_human: "Visual button layout cannot be verified programmatically; requires rendered DOM inspection"
  - test: "Click Square (add solid), hover over the solid cell, verify 4 corner controls appear: delete (top-right), frames badge (bottom-right), Pipette (bottom-left), toggle icon (top-left)"
    expected: "All 4 controls visible on hover; no controls visible when not hovering (opacity-0 to opacity-100 transition)"
    why_human: "Hover-triggered opacity transitions require real browser interaction"
  - test: "Click the Pipette (bottom-left), verify ColorPickerModal opens with HSV area, hue slider, HEX/RGBA/HSL tabs, and Cancel/Apply buttons"
    expected: "Full-featured modal with backdrop, visual saturation-value area; picking a color live-updates the cell without adding multiple undo entries; Apply commits one undo action; Escape reverts"
    why_human: "Live preview / undo stack behavior cannot be verified without running the app and exercising Ctrl+Z"
  - test: "Click the Square/Blend toggle (top-left) to switch between solid and transparent mode"
    expected: "Solid cell shows solid color background; transparent cell shows checkerboard pattern and Pipette disappears; toggle back restores color"
    why_human: "Visual checkerboard CSS pattern and conditional icon rendering require browser rendering"
  - test: "Check timeline canvas for a sequence with solid and transparent entries"
    expected: "Solid ranges show as filled colored rectangles; transparent ranges show Photoshop-style 4px checkerboard (#B0B0B0/#808080); photo ranges show image thumbnails"
    why_human: "Canvas 2D drawing cannot be verified without a rendered canvas element"
  - test: "Play the sequence through the preview canvas"
    expected: "Solid frames fill the entire canvas with the solid color; transparent frames show black (canvas cleared to background); photo frames show the image normally; cross-dissolve between photo and solid blends smoothly"
    why_human: "Canvas rendering and animation playback require live execution"
  - test: "In the sidebar, add a content sequence whose first key photo is a solid"
    expected: "Sidebar thumbnail shows the solid color as a color swatch; transparent-first sequence shows checkerboard; photo-first shows the image thumbnail (unchanged behavior)"
    why_human: "Sidebar thumbnail rendering requires visual confirmation"
---

# Phase 15.2: Solid Sequence Verification Report

**Phase Goal:** Add solid color and transparent key entries to sequences — data model, UI controls, and rendering across timeline/preview/export surfaces.
**Verified:** 2026-03-22T15:09:16Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All automated checks pass. The phase has no missing artifacts, no stub implementations, and no broken wiring. Human visual verification is required for rendering surfaces (timeline canvas, preview canvas, sidebar thumbnails) and UI behavior (color picker UX, hover controls, undo behavior).

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | KeyPhoto type supports photo, solid, and transparent variants via optional fields | VERIFIED | `sequence.ts:37-38` — `solidColor?: string`, `isTransparent?: boolean` with 3 helper discriminators exported |
| 2 | User can add a key solid, change its color, and toggle to transparent — all operations undoable | VERIFIED | `sequenceStore.ts:485,508,547` — `addKeySolid`, `updateKeySolidColor`, `toggleKeyEntryTransparent` with snapshot/restore/pushAction pattern; undo descriptions confirmed |
| 3 | FrameEntry and KeyPhotoRange carry solidColor and isTransparent to renderers | VERIFIED | `frameMap.ts:25-26,73-74` — spread-conditional propagation in both `frameMap` and `trackLayouts` computeds; 4 frameMap tests pass |
| 4 | Project format v10 serializes and deserializes solid/transparent fields with v9 backward compat | VERIFIED | `projectStore.ts:197` — `version: 10`; lines 98-99 serialize; lines 330-331 hydrate; v9 compat via conditional spread |
| 5 | buildSequenceFrames produces FrameEntry with solid info; preloadExportImages filters empty imageIds | VERIFIED | `exportRenderer.ts:25` — `solidColor: kp.solidColor`; line 240 — `.filter(id => id !== '')` |
| 6 | Asset usage scanner skips empty-string imageId entries | VERIFIED | `assetUsage.ts:29` — `&& kp.imageId !== ''` guard added to usage check |
| 7 | UI controls exist: SplitAddButton (Camera/Square), ColorPickerModal, 4-corner solid card controls, transparent toggle, checkerboard display | VERIFIED | `KeyPhotoStrip.tsx:864-887` — AddKeyPhotoButton uses Camera+Square split layout; `solidColor`/`isTransparent` passed to `KeyPhotoCard`; `ColorPickerModal` at line 238; `isSolidEntry` branch at 732; checkerboard CSS at 745; toggle at 791; Pipette at 800 |
| 8 | Timeline, preview, and export rendering surfaces handle solid/transparent frames | VERIFIED | `TimelineRenderer.ts:456,528,542` — `drawCheckerboard` method, solid/transparent branches in `drawLinearTrack`; `previewRenderer.ts:115,194,202,206` — `hasDrawable` solid check, `handledAsSolid` guard, `fillStyle = entry.solidColor`, empty `imageId` guard at line 334 |

**Score:** 8/8 truths verified (automated)

### Required Artifacts

| Artifact | Status | Evidence |
|----------|--------|---------|
| `Application/src/stores/sequenceStore.test.ts` | VERIFIED | Exists; 6 concrete tests for `addKeySolid`, `updateKeySolidColor`, `toggleKeyEntryTransparent`; all pass |
| `Application/src/lib/frameMap.test.ts` | VERIFIED | Exists; 4 concrete tests for solid/transparent `FrameEntry`; all pass |
| `Application/src/lib/exportRenderer.test.ts` | VERIFIED | Exists; 4 `.todo` tests for `buildSequenceFrames` solid behavior (appropriate — function is not exported) |
| `Application/src/types/sequence.ts` | VERIFIED | `solidColor?: string`, `isTransparent?: boolean` on `KeyPhoto`; `isKeySolid`, `isKeyTransparent`, `isKeyImage` exported |
| `Application/src/types/timeline.ts` | VERIFIED | `solidColor?` and `isTransparent?` on both `FrameEntry` and `KeyPhotoRange` |
| `Application/src/types/project.ts` | VERIFIED | `solid_color?: string`, `is_transparent?: boolean` on `MceKeyPhoto` |
| `Application/src/stores/sequenceStore.ts` | VERIFIED | `addKeySolid` (line 485), `updateKeySolidColor` (508), `updateKeySolidColorLive` (532), `toggleKeyEntryTransparent` (547) — all with proper undo descriptions |
| `Application/src/stores/projectStore.ts` | VERIFIED | `version: 10`; serialize `solid_color`/`is_transparent`; hydrate to `solidColor`/`isTransparent` with v9 compat |
| `Application/src/lib/frameMap.ts` | VERIFIED | Propagates `solidColor`/`isTransparent` in both `frameMap` and `trackLayouts` computeds |
| `Application/src/lib/assetUsage.ts` | VERIFIED | `kp.imageId !== ''` guard at line 29 |
| `Application/src/lib/exportRenderer.ts` | VERIFIED | `solidColor: kp.solidColor` in `buildSequenceFrames`; `.filter(id => id !== '')` in `preloadExportImages` |
| `Application/src/components/sequence/KeyPhotoStrip.tsx` | VERIFIED | `ColorPickerModal` (line 238), split `AddKeyPhotoButton` (line 865), `KeyPhotoCard` with 4-corner controls, `solidColor`/`isTransparent` props wired |
| `Application/src/components/sequence/SequenceList.tsx` | VERIFIED | `isKeySolid`/`isKeyTransparent` imported; `firstKpIsSolid`/`firstKpIsTransparent` checks; `firstKp.solidColor` for thumbnail background |
| `Application/src/components/timeline/TimelineRenderer.ts` | VERIFIED | `drawCheckerboard` private method (line 456); solid/transparent branches in `drawLinearTrack` (lines 528, 542) |
| `Application/src/lib/previewRenderer.ts` | VERIFIED | `hasDrawable` solid check (line 115); `handledAsSolid` pattern (lines 194-256); `fillStyle = entry.solidColor` (line 202); empty `imageId` guard (line 334) |

### Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|---------|
| `sequenceStore.test.ts` | `sequenceStore.ts` | calls `addKeySolid`, `updateKeySolidColor`, `toggleKeyEntryTransparent` | WIRED | Test imports store, uses `store.addKeySolid/updateKeySolidColor/toggleKeyEntryTransparent`; all 6 tests pass |
| `frameMap.test.ts` | `frameMap.ts` | asserts `solidColor`/`isTransparent` in `FrameEntry` | WIRED | Tests import `frameMap`; 4 tests pass green |
| `sequenceStore.ts` | `sequence.ts` | `KeyPhoto` with `solidColor`/`isTransparent` | WIRED | Store imports `KeyPhoto` type; methods use the optional fields |
| `frameMap.ts` | `timeline.ts` | `FrameEntry.solidColor` populated from `KeyPhoto.solidColor` | WIRED | `frameMap.ts:25` — `solidColor: kp.solidColor` |
| `projectStore.ts` | `project.ts` | `MceKeyPhoto` serialization with `solid_color`/`is_transparent` | WIRED | `projectStore.ts:98-99` serialize; lines 330-331 hydrate |
| `KeyPhotoStrip.tsx` | `sequenceStore.ts` | calls `addKeySolid`, `updateKeySolidColor`, `updateKeySolidColorLive`, `toggleKeyEntryTransparent` | WIRED | Lines 880, 817, 820, 791 in `KeyPhotoStrip.tsx` |
| `SequenceList.tsx` | `sequence.ts` | `isKeySolid`/`isKeyTransparent` discriminators for thumbnail | WIRED | `SequenceList.tsx:17` imports helpers; lines 97-99 use them |
| `TimelineRenderer.ts` | `timeline.ts` | `KeyPhotoRange.solidColor`/`isTransparent` | WIRED | `TimelineRenderer.ts:528` — `range.solidColor && !range.isTransparent`; line 542 — `range.isTransparent` |
| `previewRenderer.ts` | `timeline.ts` | `FrameEntry.solidColor`/`isTransparent` | WIRED | `previewRenderer.ts:115` — `entry.solidColor || entry.isTransparent`; line 202 — `entry.solidColor` |
| `exportRenderer.ts` | `previewRenderer.ts` | renderer receives solid frame data | WIRED | `exportRenderer.ts:25` — `solidColor: kp.solidColor` in `buildSequenceFrames`; filter at line 240 |

### Requirements Coverage

| Requirement | Plans | Description | Status | Evidence |
|-------------|-------|-------------|--------|---------|
| SOLID-01 | 00, 01, 02 | User can add key solid (black) and key transparent entries to any content sequence via split add button | SATISFIED | `addKeySolid` in store; `AddKeyPhotoButton` split Camera/Square in `KeyPhotoStrip.tsx:865` |
| SOLID-02 | 02 | User can change a key solid's color via inline color picker popover with live canvas preview | SATISFIED | `ColorPickerModal` with `onLiveChange`/`onCommit` pattern; `updateKeySolidColorLive` for no-undo live preview |
| SOLID-03 | 00, 02 | User can toggle a key solid between solid mode and transparent mode via top-left corner button | SATISFIED | `toggleKeyEntryTransparent` in store; toggle button in `KeyPhotoCard` at `KeyPhotoStrip.tsx:791` |
| SOLID-04 | 00, 01, 02 | Key solid and transparent entries have hold frames, reorder, and delete identical to key photos | SATISFIED | `KeyPhotoCard` renders frames badge and delete button for all entries; `FramesPopover` with +/- buttons added in Plan 03 UX fix |
| SOLID-05 | 03 | Timeline renders key solid ranges as colored rectangles and key transparent ranges as checkerboard | SATISFIED (visual) | `TimelineRenderer.ts:528-554` — solid fill + border; checkerboard via `drawCheckerboard`; requires human visual confirmation |
| SOLID-06 | 03 | Canvas preview renders key solids as full-resolution colored rectangles and key transparents as canvas background | SATISFIED (visual) | `previewRenderer.ts:194-256` — `handledAsSolid` path with `fillStyle = entry.solidColor`; requires human visual confirmation |
| SOLID-07 | 00, 01, 03 | Cross-dissolve between key photo and key solid blends normally via standard alpha interpolation | SATISFIED (visual) | `exportRenderer.ts:25` carries `solidColor` through `buildSequenceFrames`; cross-dissolve uses `renderFrame` which now handles solid via `handledAsSolid`; requires human visual confirmation |
| SOLID-08 | 01 | Key solid/transparent entries persist in project file (.mce v10) with backward-compatible reading of v9 files | SATISFIED | `projectStore.ts:197` — `version: 10`; serialize lines 98-99; hydrate lines 330-331 with conditional spread for v9 compat |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `KeyPhotoStrip.tsx` | 3 | `Plus` still imported though `AddKeyPhotoButton` now uses `Camera`/`Square` | Info | No functional impact — `Plus` is legitimately used in `FramesPopover` increment button (Plan 03 UX fix). Not a dead import. |

No blocker or warning anti-patterns found. All `solidColor` / `isTransparent` values flow through real data (not hardcoded). No TODO/FIXME/PLACEHOLDER comments in modified files. No stub implementations.

### Test Suite Results

- `sequenceStore.test.ts`: 6/6 pass (addKeySolid, updateKeySolidColor, toggleKeyEntryTransparent including round-trip)
- `frameMap.test.ts`: 4/4 pass (solid FrameEntry, transparent FrameEntry, photo FrameEntry, interleaved)
- `exportRenderer.test.ts`: 4 `.todo` tests (appropriate — `buildSequenceFrames` is not exported; behavior is tested indirectly via `frameMap.test.ts`)
- Full suite: 122 passed, 3 failed (pre-existing `audioWaveform.test.ts` failures from Phase 15-01 — unrelated to this phase)
- TypeScript: compiles clean (0 errors)

### Human Verification Required

#### 1. Split Add Button Visual

**Test:** Open app, create a content sequence, look at the key photo strip.
**Expected:** The old single Plus button is replaced by a vertically split 24x56px button: Camera icon on top (navigates to imported images panel), Square icon on bottom (immediately adds a black solid cell).
**Why human:** Button layout and icon rendering require visual confirmation.

#### 2. Key Solid Cell 4-Corner Controls

**Test:** Add a key solid (Square button), hover over the solid cell.
**Expected:** 4 corner controls appear on hover — delete X (top-right), frames badge (bottom-right), Pipette color picker trigger (bottom-left), solid/transparent toggle Square/Blend icon (top-left). Controls fade in/out with hover.
**Why human:** CSS opacity transitions and hover states require live browser interaction.

#### 3. Color Picker Modal UX and Undo Behavior

**Test:** Click the Pipette icon on a solid cell. Pick a color by dragging the saturation-value area. Close with Apply. Then Ctrl+Z.
**Expected:** Modal opens with HSV area, hue slider, HEX/RGBA/HSL tabs. Dragging the color area live-updates the cell without flooding the undo stack. Apply commits exactly one undo action. Escape reverts to the original color. Ctrl+Z undoes the single commit.
**Why human:** Live preview undo-safety and undo stack depth cannot be verified without running the app.

#### 4. Solid/Transparent Toggle

**Test:** Click the Square toggle (top-left of solid cell) to switch to transparent. Click the Blend toggle to switch back.
**Expected:** Switching to transparent shows CSS checkerboard background and hides the Pipette button. Switching back to solid restores the previous color. Both directions are undoable via Ctrl+Z.
**Why human:** Visual checkerboard CSS, conditional icon display, and undo behavior require live interaction.

#### 5. Timeline Canvas Rendering

**Test:** Add a content sequence with a mix of photo, solid, and transparent key entries. Look at the timeline track.
**Expected:** Photo ranges show image thumbnails (tiled). Solid ranges show filled colored rectangles in the solid's color with frame borders. Transparent ranges show a Photoshop-style 4px checkerboard pattern (#B0B0B0 light / #808080 dark) with frame borders.
**Why human:** Canvas 2D drawing output cannot be verified without a rendered canvas.

#### 6. Canvas Preview Rendering

**Test:** Navigate to solid/transparent frames in the preview canvas.
**Expected:** Solid frames fill the entire canvas with the exact solid color. Transparent frames show black (canvas background, no fill). Both are visually distinct from photo frames. Cross-dissolve between a photo and a solid frame blends smoothly without artifacts.
**Why human:** Canvas rendering and animation playback require live execution.

#### 7. Sidebar Thumbnail

**Test:** In the sidebar, create a content sequence with a solid as the first key entry.
**Expected:** Sidebar thumbnail shows the solid color as a solid-colored swatch. A transparent-first sequence shows the checkerboard pattern. A photo-first sequence shows the image thumbnail (unchanged behavior).
**Why human:** Sidebar DOM rendering with computed styles requires visual confirmation.

### Gaps Summary

No gaps found. All automated checks pass. The phase is complete pending user visual verification of the 7 rendering/UX behaviors listed above. These are not gaps — they are inherently human-verifiable behaviors (canvas drawing, CSS transitions, live UX) that cannot be confirmed programmatically.

---

_Verified: 2026-03-22T15:09:16Z_
_Verifier: Claude (gsd-verifier)_
