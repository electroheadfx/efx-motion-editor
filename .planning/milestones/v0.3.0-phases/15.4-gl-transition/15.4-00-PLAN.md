---
phase: 15.4-gl-transition
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Application/src/lib/glslRuntime.test.ts
  - Application/src/lib/exportRenderer.test.ts
  - Application/src/stores/sequenceStore.test.ts
  - Application/src/lib/transitionEngine.test.ts
  - Application/src/stores/projectStore.test.ts
autonomous: true
requirements:
  - GLT-02
  - GLT-04
  - GLT-05
  - GLT-07
  - GLT-08

must_haves:
  truths:
    - "Test scaffolds exist for all GL transition behaviors before implementation begins"
    - "Existing test suites pass unchanged after scaffold extension"
  artifacts:
    - path: "Application/src/lib/glslRuntime.test.ts"
      provides: "Test stubs for buildTransitionFragmentSource and renderGlslTransition"
      contains: "describe('renderGlslTransition'"
    - path: "Application/src/lib/exportRenderer.test.ts"
      provides: "Test stubs for GL transition overlap rendering"
      contains: "GL transition"
    - path: "Application/src/stores/sequenceStore.test.ts"
      provides: "Test stubs for setGlTransition, removeGlTransition, mutual exclusion"
      contains: "setGlTransition"
    - path: "Application/src/lib/transitionEngine.test.ts"
      provides: "Test stubs for computeTransitionProgress"
      contains: "computeTransitionProgress"
    - path: "Application/src/stores/projectStore.test.ts"
      provides: "Test stubs for GL transition serialization round-trip"
      contains: "gl_transition"
  key_links: []
---

<objective>
Create test scaffolds for all GL transition behaviors before implementation begins. This is a Wave 0 plan ensuring Nyquist compliance -- every subsequent task has automated verification via Vitest.

Purpose: Establish test expectations upfront so Wave 1-3 plans can verify against real automated tests, not just `tsc --noEmit`.
Output: 5 test files (1 new, 4 extended) with `.todo` stubs that become real tests as implementation proceeds.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.4-gl-transition/15.4-CONTEXT.md
@.planning/phases/15.4-gl-transition/15.4-VALIDATION.md

<interfaces>
<!-- Existing test patterns to follow -->

From Application/src/lib/transitionEngine.test.ts (existing pattern):
```typescript
import { describe, it, expect } from 'vitest';
import { computeFadeOpacity, computeSolidFadeAlpha } from './transitionEngine';
// Uses helper factory functions, direct unit testing of pure functions
```

From Application/src/stores/sequenceStore.test.ts (existing pattern):
```typescript
import {describe, it, expect, beforeEach} from 'vitest';
import {sequenceStore} from './sequenceStore';
const store = sequenceStore as any;  // for methods not yet added
function seedSequence(): string { ... }
// Uses beforeEach with sequenceStore.reset()
```

From Application/src/stores/projectStore.test.ts (existing pattern):
```typescript
import {describe, it, expect, beforeEach} from 'vitest';
import {projectStore} from './projectStore';
// Tests serialization/deserialization round-trips
```

From Application/src/lib/exportRenderer.test.ts (existing pattern):
```typescript
import { describe, it } from 'vitest';
// Uses it.todo for stubs requiring DOM/canvas mocking
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create glslRuntime.test.ts and extend transitionEngine.test.ts</name>
  <read_first>
    - Application/src/lib/transitionEngine.test.ts
    - Application/src/lib/glslRuntime.ts (to understand the function signatures for stubs)
    - Application/src/lib/transitionEngine.ts (to understand computeTransitionProgress interface)
  </read_first>
  <files>
    Application/src/lib/glslRuntime.test.ts
    Application/src/lib/transitionEngine.test.ts
  </files>
  <action>
**1. Create `Application/src/lib/glslRuntime.test.ts` (NEW file):**

This file tests the non-WebGL parts of glslRuntime -- specifically the fragment source builder which is pure string logic. WebGL rendering itself requires a GL context that jsdom cannot provide, so those tests use `.todo`.

```typescript
import { describe, it, expect } from 'vitest';

describe('glslRuntime', () => {
  describe('buildTransitionFragmentSource', () => {
    // NOTE: buildTransitionFragmentSource is not exported.
    // These tests validate behavior indirectly or require the function to be exported for testing.
    // If not exported, convert these to .todo stubs.

    it.todo('wraps shader source in WebGL2 #version 300 es header');
    it.todo('declares u_from and u_to sampler2D uniforms');
    it.todo('declares progress and ratio float uniforms');
    it.todo('provides getFromColor and getToColor helper functions');
    it.todo('includes shader fragmentSource containing transition(vec2 uv)');
    it.todo('generates uniform float u_{key} for each shader param');
    it.todo('auto-generates vec2 reconstruction for paired X/Y params');
  });

  describe('renderGlslTransition', () => {
    // WebGL2 context not available in jsdom -- these remain .todo
    // but document the expected contract
    it.todo('returns null when WebGL2 context unavailable');
    it.todo('clamps progress to [0.001, 0.999] range');
    it.todo('binds fromCanvas to TEXTURE0 and toCanvas to TEXTURE1');
    it.todo('sets iResolution uniform to (width, height, 1.0)');
    it.todo('returns the shared _glCanvas after rendering');
  });

  describe('disposeGlslRuntime', () => {
    it.todo('cleans up _fromTexture and _toTexture');
  });
});
```

**2. Extend `Application/src/lib/transitionEngine.test.ts` with computeTransitionProgress tests (GLT-07):**

Add a new describe block at the end of the file:

```typescript
describe('computeTransitionProgress', () => {
  // NOTE: Import will fail until Plan 01 adds the function.
  // Use dynamic import or `as any` pattern until then.

  it.todo('returns 0 when overlapDuration is 0');
  it.todo('returns 0 at overlapStart frame with linear curve');
  it.todo('returns 0.5 at midpoint of linear overlap');
  it.todo('returns 1.0 at last frame of overlap');
  it.todo('clamps to 0 when globalFrame is before overlapStart');
  it.todo('clamps to 1 when globalFrame is past overlap end');
  it.todo('applies ease-in-out easing to progress value');
});
```
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx vitest run src/lib/glslRuntime.test.ts src/lib/transitionEngine.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>glslRuntime.test.ts created with buildTransitionFragmentSource and renderGlslTransition stubs. transitionEngine.test.ts extended with computeTransitionProgress stubs. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend sequenceStore, projectStore, and exportRenderer test files</name>
  <read_first>
    - Application/src/stores/sequenceStore.test.ts
    - Application/src/stores/projectStore.test.ts
    - Application/src/lib/exportRenderer.test.ts
  </read_first>
  <files>
    Application/src/stores/sequenceStore.test.ts
    Application/src/lib/exportRenderer.test.ts
    Application/src/stores/projectStore.test.ts
  </files>
  <action>
**1. Extend `Application/src/stores/sequenceStore.test.ts` with GL transition CRUD stubs (GLT-05):**

Add a new describe block after the existing `sequenceStore solid/transparent` block:

```typescript
describe('sequenceStore GL transitions (GLT-05)', () => {
  beforeEach(() => {
    sequenceStore.reset();
  });

  describe('setGlTransition', () => {
    it.todo('sets glTransition on the specified sequence');
    it.todo('clears crossDissolve when setting glTransition (D-02 mutual exclusion)');
    it.todo('marks project dirty');
    it.todo('pushes undo action');
  });

  describe('removeGlTransition', () => {
    it.todo('removes glTransition from the specified sequence');
    it.todo('marks project dirty');
    it.todo('pushes undo action');
  });

  describe('updateGlTransitionParams', () => {
    it.todo('updates shader params on existing glTransition');
    it.todo('no-ops if sequence has no glTransition');
  });

  describe('updateGlTransition', () => {
    it.todo('merges partial updates into existing glTransition');
    it.todo('can update duration and curve independently');
  });

  describe('addTransition mutual exclusion', () => {
    it.todo('clears glTransition when adding cross-dissolve (D-02)');
  });
});
```

**2. Extend `Application/src/lib/exportRenderer.test.ts` with GL transition rendering stubs (GLT-04):**

Add a new describe block after the existing `preloadExportImages with solids` block:

```typescript
describe('GL transition rendering (GLT-04)', () => {
  it.todo('renders GL transition overlap via dual-capture when overlap has glTransition');
  it.todo('calls renderGlslTransition with correct shader, canvases, and progress');
  it.todo('preserves existing cross-dissolve rendering when overlap has no glTransition');
  it.todo('creates and reuses offscreen canvases for dual-capture');
  it.todo('computes eased progress via computeTransitionProgress');
});
```

**3. Extend `Application/src/stores/projectStore.test.ts` with GL transition persistence stubs (GLT-08):**

Add a new describe block at the end of the file:

```typescript
describe('GL transition persistence (GLT-08)', () => {
  describe('serialization (save)', () => {
    it.todo('serializes glTransition as gl_transition with snake_case fields');
    it.todo('serializes shaderId as shader_id');
    it.todo('serializes params HashMap');
    it.todo('omits gl_transition when sequence has none');
  });

  describe('deserialization (load)', () => {
    it.todo('deserializes gl_transition back to glTransition with camelCase fields');
    it.todo('maps shader_id back to shaderId');
    it.todo('defaults missing curve to ease-in-out');
    it.todo('handles v10 files without gl_transition field (backward compat)');
  });

  describe('version', () => {
    it.todo('saves with version 11');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx vitest run src/stores/sequenceStore.test.ts src/stores/projectStore.test.ts src/lib/exportRenderer.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>sequenceStore.test.ts extended with GL transition CRUD stubs. exportRenderer.test.ts extended with GL transition rendering stubs. projectStore.test.ts extended with serialization round-trip stubs. All existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `cd Application && npx vitest run --reporter=verbose` — all existing tests pass, new .todo stubs show as skipped
2. `grep -c "it.todo" Application/src/lib/glslRuntime.test.ts` — 12 stubs
3. `grep -c "computeTransitionProgress" Application/src/lib/transitionEngine.test.ts` — matches
4. `grep -c "setGlTransition" Application/src/stores/sequenceStore.test.ts` — matches
5. `grep -c "gl_transition" Application/src/stores/projectStore.test.ts` — matches
6. `grep -c "GL transition" Application/src/lib/exportRenderer.test.ts` — matches
</verification>

<success_criteria>
- glslRuntime.test.ts exists with stubs for buildTransitionFragmentSource and renderGlslTransition
- transitionEngine.test.ts extended with computeTransitionProgress stubs
- sequenceStore.test.ts extended with GL transition CRUD stubs
- exportRenderer.test.ts extended with GL transition rendering stubs
- projectStore.test.ts extended with GL transition persistence stubs
- All existing tests still pass (zero regressions)
- Full Vitest suite runs green with new stubs showing as skipped/todo
</success_criteria>

<output>
After completion, create `.planning/phases/15.4-gl-transition/15.4-00-SUMMARY.md`
</output>
