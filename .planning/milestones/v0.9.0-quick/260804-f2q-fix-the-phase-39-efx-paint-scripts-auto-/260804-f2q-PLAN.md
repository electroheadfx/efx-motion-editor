---
phase: 260804-f2q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: false
requirements:
  - HYDR-01
  - HYDR-02
  - HYDR-03
  - HYDR-04
  - HYDR-05
  - HYDR-06

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Opening a saved project with durable project scripts and launching EFX Paint on its Paint/Physics Paint layer populates script rows automatically, with no Refresh click (HYDR-01)"
    - "Save Script is enabled as soon as saved-project authority arrives and no library operation is busy (HYDR-02)"
    - "Exactly one automatic scan runs per authoritative context; in-flight results from a replaced project/layer context are rejected and never populate rows; no duplicate scan/listener after close/reopen (HYDR-03)"
    - "A genuinely unsaved project still shows 'Save the project first.', keeps Save Script disabled, and issues no persistence request (HYDR-04)"
    - "Manual Refresh and Scripts-tab entry still rescan on demand; Copy/Apply/Clear Buffer/Load+Apply/Play/rename/delete/selection/diagnostics/clipboard behavior and default-open accessibility are unchanged (HYDR-05)"
    - "Hydration consumes the exact authoritative context payload synchronously; the fix diff contains no timer, polling, frame-callback, or microtask deferral primitives (HYDR-06)"
  artifacts:
    - "app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts — updateProjectContext accepts and consumes the exact PhysicPaintLaunchContext payload with exactly-once auto-scan marker"
    - "app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts — owned regression coverage (fail-before-fix proof, exactly-once, stale-generation rejection, unsaved gating)"
    - "app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts — synchronous exact-payload project-context handoff; microtask deferral removed"
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx — onSettledLaunchContext passes the settled context through to the script library"
  key_links:
    - "usePhysicsPaintProjectContextBridge callback -> merged updated context -> input.onSettledLaunchContext?.(updated) invoked synchronously with the exact payload"
    - "PhysicsPaintStudio onSettledLaunchContext -> rotoScriptLibrary.updateProjectContext(context) (both launch-settle and project-context-event paths)"
    - "updateProjectContext(context) reads ONLY the payload argument for identity/saved state; ports.getLaunchContext() is used exclusively by the manual refresh() path"
---

<objective>
Fix the Phase 39 EFX Paint Scripts auto-hydration regression at its confirmed root cause: the project-context handoff in `usePhysicsPaintLaunchIntegration.ts` discards the exact updated launch context, and `rotoScriptLibrary.updateProjectContext()` rereads `ports.getLaunchContext()` — a getter whose freshness depends on render-commit timing — so the scan observes the stale `project.saved === false` state, clears rows, and never runs. Manual Refresh only works because it executes after the committed render.

Purpose: Satisfy REQUIREMENTS.md HYDR-01 through HYDR-06 so Phase 39 can close by verification per the ROADMAP Phase 39 execution note. The fix must be event/reactivity-driven with an explicit-payload handoff — any timing-hack in the diff is automatic rejection (HYDR-06).

Output: A controller that hydrates from the exact authoritative context exactly once per context, a synchronous bridge handoff that passes that payload through, owned regression tests that fail before the fix and pass after, and a user-performed native UAT checklist.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md

Read first (root cause and consumers, already identified):
- app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts:154-166 — the project-context bridge callback wraps the handoff in a state updater plus a microtask deferral; the exact `updated` payload exists in the closure but is thrown away. Line 128 shows the launch-settle path already calling `onSettledLaunchContext?.(hydration.context)` with the exact payload.
- app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts:53-130 — controller construction, `projectSaved` signal, availability computed, `contextGeneration`/`operationGeneration`/`contextKey` staleness machinery, and `refresh()` (lines 111-130) which rereads `ports.getLaunchContext()`.
- app/src/components/physic-paint/PhysicsPaintStudio.tsx:318-335 (controller hook wiring, `getLaunchContext: () => launchContext`) and :945 (`onSettledLaunchContext: () => { void rotoScriptLibrary.updateProjectContext(); }` — ignores the payload).
- app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts — ports adapter reads `portsRef.current` per render (keep unchanged).
- app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts — existing harness pattern to extend (`harness(saved)` + `setLaunch`).
- app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx:203-206 — `enterScripts` on Scripts-tab entry (existing behavior, must stay unchanged).

Project constraints that apply: Preact Signals patterns per CLAUDE.md; pnpm (never npm); `vitest run` only (never watch mode); use the existing Vitest config (no one-off test configs); no backward-compat shims; GSD artifacts in English.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Explicit-context controller hydration with fail-first regression tests (RED then GREEN)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts</files>
  <behavior>
    - Test 1 (HYDR-01/HYDR-02, the regression): harness created with `saved = false`; `setLaunch` is NEVER called, so `ports.getLaunchContext()` keeps returning the unsaved context — this simulates the uncommitted render that caused the shipping bug. `await controller.updateProjectContext(savedContext)` (exact payload, `saved: true`, same contextId/layerId) MUST issue exactly one `scan` request, populate rows, set `availability.value.canSave === true` with `saveDisabledReason === null`, and set status `Found 2 scripts`. This test fails before the fix (current signature takes no argument and rereads the stale getter, so zero scans and `canSave === false`).
    - Test 2 (HYDR-03, exactly once): after Test-1-style hydration, a second `updateProjectContext(sameContext)` issues NO additional scan (still 1 total); `projectSaved` gating remains applied. Then call `setLaunch(savedContext)` so `ports.getLaunchContext()` returns the saved context — mirroring the committed render that manual Refresh has always depended on — and assert a subsequent manual `controller.refresh()` DOES issue a second scan (explicit rescan preserved, HYDR-05).
    - Test 3 (HYDR-03, stale replacement): hydrate context-1; mock the next scan pending; call `updateProjectContext(context-2)` (different contextId); settle the context-1 scan result late — its rows MUST NOT publish; only the context-2 scan result populates rows.
    - Test 4 (HYDR-04): `updateProjectContext(unsavedContext)` issues no request, clears rows, and keeps `saveDisabledReason === 'Save the project first.'`; `saveActiveFrame()` still refuses without any persistence request.
    - No test uses timer mocking, timer advancement, or real-time waits around the hydration path — every assertion follows the awaited promise directly (Pitfall 2 warning sign).
  </behavior>
  <action>
    0. FIRST, before any commit in this plan: record the pre-plan base SHA durably with `git rev-parse HEAD > /Users/lmarques/Dev/efx-motion-editor/.planning/quick/260804-f2q-fix-the-phase-39-efx-paint-scripts-auto-/.base-sha`. The HYDR-06 diff gate in Task 2 reads this file (shell env does not persist across Bash invocations, so an env var is not acceptable).
    RED: add the four tests above to `physicsPaintRotoScriptLibrary.test.ts` using the existing harness/result helpers; run the suite and confirm the new hydration test fails deterministically for the root-cause reason (zero scans, canSave false) — do not proceed if it fails for an unrelated reason. Commit the failing tests per GSD TDD convention, then implement GREEN in `physicsPaintRotoScriptLibrary.ts`:
    1. Change the interface member to `updateProjectContext: (context: PhysicPaintLaunchContext) => Promise<void>;`.
    2. Extract the body of `refresh()` (lines 111-130) into an internal `refreshWithContext(context: PhysicPaintLaunchContext | null)` that is IDENTICAL to current logic except it takes the context as a parameter instead of reading `ports.getLaunchContext()`. `refresh()` becomes `refreshWithContext(ports.getLaunchContext())` — manual Refresh and `enterScripts` (which aliases `refresh`) keep byte-identical behavior (HYDR-05).
    3. Add module-scope-in-factory marker `let lastAutoHydratedKey: string | null = null`. In the context-identity reset branch (key changed), also reset `lastAutoHydratedKey = null` so each newly arriving authoritative context may auto-scan exactly once; also clear it in `dispose()`.
    4. Implement `updateProjectContext(context)`: compute identity from the PAYLOAD ONLY (never the getter); if the key changed, apply the same reset as `refreshWithContext` (bump `contextGeneration`/`operationGeneration`, clear busy/rows/selection/rename/deleteConfirmation, update `contextKey`); set `projectSaved.value` from the payload immediately so Save Script enables even when the scan below is skipped (HYDR-02); if the payload is not saved, apply the same clearing branch as `refreshWithContext` and issue NO request (HYDR-04); if saved and `lastAutoHydratedKey !== contextKey`, set `lastAutoHydratedKey = contextKey` and run the same scan + status publication as `refreshWithContext`; if saved and the marker already matches, return without scanning (exactly-once, HYDR-03).
    5. Keep the existing `execute()` generation/operationId staleness guards untouched — Test 3 relies on them.
    6. Do not add any timing, polling, frame-callback, or microtask deferral primitives anywhere in the file, including inside comments and test names (HYDR-06; the diff-level gate in Task 2 scans added lines). Refer to the removed mechanism only as "the deferred microtask handoff" in prose if a comment is needed at all.
    GREEN: run the suite; all new and pre-existing tests pass. The pre-existing suite (gating, sorting, rename revisions, stale-after-dispose, failure-preserves-rows, load/clipboard contract) must pass UNCHANGED — any edit to a pre-existing assertion is a red flag; stop and re-diagnose instead.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts</automated>
  </verify>
  <done>All four new regression tests pass; the hydration test demonstrably failed before the controller change (noted in the commit message or summary); every pre-existing test in the file passes unmodified; `updateProjectContext` consumes only its payload argument.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronous exact-payload handoff through the launch bridge and Studio wiring</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <action>
    1. In `usePhysicsPaintLaunchIntegration.ts`, add `peekLaunchContext: () => PhysicPaintLaunchContext | null` to the hook input (new member beside `state`/`lifecycle`, typed in the input interface). Rewrite the `usePhysicsPaintProjectContextBridge` callback (currently lines 159-166) so it: reads `const current = input.peekLaunchContext()`; returns early when null (same drop behavior as today); builds `const updated = { ...current, project };`; calls `input.state.setLaunchContext(updated)` directly (object form, not the updater-function form); then calls `input.onSettledLaunchContext?.(updated)` SYNCHRONOUSLY in the same callback. Remove the state-updater wrapper and its deferred microtask handoff entirely — the exact payload now flows in the argument, so no deferral of any kind is needed (HYDR-06; do not mention the removed primitive names in comments). The launch-settle path at line 128 already passes the exact `hydration.context` — leave it untouched.
    2. In `PhysicsPaintStudio.tsx`: pass `peekLaunchContext: () => launchContext` into the `usePhysicsPaintLaunchIntegration` input object (the closure is refreshed every render, so it always reads the latest committed context; the bridge callback fires at event time, before any pending state commit, so the merged `updated` is derived from committed truth — this is what makes the handoff race-free without deferral). Change line 945 to `onSettledLaunchContext: (context) => { void rotoScriptLibrary.updateProjectContext(context); },` so BOTH the launch-settle path (exact hydrated context) and the project-context-event path (merged updated context) deliver the payload to the controller (HYDR-01/HYDR-02).
    3. Nothing else in the Studio changes: no panel, right-panel, scripts-toolbar, or accessibility markup edits (HYDR-05). Do not touch `useRotoScriptLibraryController.ts` or `usePhysicsPaintParentBridge.ts` — the existing listener install/unlisten cleanup already covers the no-duplicate-listener clause of HYDR-03.
    4. Verify no new timing primitive entered the diff: the base SHA for the scan was recorded durably at the very start of this plan (see Task 1 action step 0) in `/Users/lmarques/Dev/efx-motion-editor/.planning/quick/260804-f2q-fix-the-phase-39-efx-paint-scripts-auto-/.base-sha`. Run the added-lines scan in the verify block — it diffs from that recorded SHA to HEAD and must count 0. The scan covers ALL added lines, including comment lines and test names, matching the HYDR-06 ban in Task 1 step 6 exactly (no comment exclusion). This is the Phase 39 automatic-rejection gate from the ROADMAP execution note.
    5. Run the related suites: the launch-integration test (coordinator behavior unchanged), the Studio source-contract suite, and the right-panel/scripts-panel contract suites — all must pass with no edits to their assertions.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts && git diff $(cat /Users/lmarques/Dev/efx-motion-editor/.planning/quick/260804-f2q-fix-the-phase-39-efx-paint-scripts-auto-/.base-sha)..HEAD -- app/src/components/physic-paint | grep -E '^\+' | grep -vE '^\+\+\+' | grep -cE 'setTimeout|setInterval|requestAnimationFrame|queueMicrotask|advanceTimersByTime' | grep -qx '0'</automated>
    <human-check>After Task 3's checklist passes in the packaged app, Phase 39's UAT gate is satisfied.</human-check>
  </verify>
  <done>Bridge callback hands the exact merged context synchronously; Studio passes the settled context into `updateProjectContext`; all four listed suites pass; the added-lines diff scan for timing primitives counts 0; no pre-existing test assertion was modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Native packaged-app UAT — Phase 39 acceptance (user-performed)</name>
  <what-built>Event-driven Scripts auto-hydration: saved-project script rows and Save Script availability now arrive automatically from the exact authoritative project context, with exactly-once scanning, stale-context rejection, preserved unsaved gating and manual Refresh, and owned regression tests. All automated checks pass.</what-built>
  <how-to-verify>
    MANUAL UAT CHECKLIST (performed by the user in the native app; per project rule, the executor does not run the server or drive the native UI):
    1. HYDR-01: Open a SAVED project that already contains durable project scripts; open its Paint/Physics Paint layer in EFX Paint; open the Scripts tab — existing script rows are ALREADY populated without clicking Refresh.
    2. HYDR-02: With the library idle, Save Script is enabled immediately (no contradictory state where Copy/Apply work but Save is disabled).
    3. HYDR-04: Create/open a genuinely UNSAVED project and open EFX Paint — the panel shows `Save the project first.`, Save Script stays disabled. Then save the project from the main editor WHILE EFX Paint is open — rows hydrate and Save Script enables automatically, with no Refresh click.
    4. HYDR-03: Close EFX Paint and reopen it three times — rows populate exactly once per open, no duplicate rows, no stale rows from a previously opened project or layer.
    5. HYDR-05: Click Refresh — an explicit rescan runs. Exercise Copy, Apply, Load+Apply, Play, rename, delete, row selection — all behave exactly as before; the Scripts tab still opens by default as before.
    6. HYDR-06 (review gate): confirm the plan diff's added-lines timing-primitive scan counted 0 (executor-reported), and note any "wait a moment" step would be an automatic failure — none of the steps above requires waiting.
  </how-to-verify>
  <resume-signal>Type "approved" with any per-step notes, or describe the failing step and observed behavior</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main window → EFX Paint window | `physic-paint:project-context` event payloads cross the Tauri event bus (plus browser-fallback postMessage/CustomEvent channels) |
| EFX Paint UI → main-window persistence | script-library scan/save/rename/delete requests mutate the on-disk project scripts folder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | project-context payload merge in `usePhysicsPaintLaunchIntegration` | medium | mitigate | Payload shape is validated in `usePhysicsPaintProjectContextBridge.accept` (name/saved/contextId type checks) before any merge; unvalidated payloads are dropped |
| T-39-02 | Tampering | stale/replaced context populating the library | high | mitigate | Exact-payload handoff + `contextGeneration`/`operationGeneration`/operationId guards reject stale results; regression Test 3 pins the behavior (HYDR-03) |
| T-39-03 | Elevation of Privilege | unsaved project triggering persistence writes | high | mitigate | `projectSaved` gate checked from the exact payload before any request; HYDR-04 regression test asserts zero requests for unsaved contexts; main-side bridge independently rejects with `Save the project first.` |
| T-39-04 | Denial of Service | duplicate scans/listeners after close/reopen | low | mitigate | Exactly-once auto-scan marker per context key; existing awaited unlisten + controller `dispose()` on unmount; UAT step 4 covers close/reopen ×3 |
</threat_model>

<verification>
- RED proof: Task 1's hydration regression test fails before the controller change for the root-cause reason (zero scans, `canSave === false`).
- GREEN: `pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts` — all tests pass, pre-existing assertions untouched.
- Wiring: launch-integration, Studio contract, and panel contract suites pass.
- HYDR-06 gate: added-lines diff scan for timing/polling/frame-callback/microtask primitives counts 0 across the plan diff.
- Broader safety net: `pnpm --dir app exec vitest run` (full app suite, run mode only) passes; the repo's existing typecheck gate passes.
- Native UAT checklist (Task 3) approved by the user — Phase 39 is not satisfied until this passes (ROADMAP execution note; automated-ready only until then).
</verification>

<success_criteria>
- All six HYDR requirements demonstrably met: automated regression coverage for HYDR-01/02/03/04 mechanics, preserved-behavior suites for HYDR-05, zero-timing-primitive diff gate for HYDR-06.
- No pre-existing test assertion modified anywhere in the diff.
- User-approved native UAT for all six checklist steps.
</success_criteria>

<output>
Create `.planning/quick/260804-f2q-fix-the-phase-39-efx-paint-scripts-auto-/260804-f2q-SUMMARY.md` when done, including: the RED-proof observation, the final test counts, the diff-gate result, and the UAT outcome per checklist step.
</output>
