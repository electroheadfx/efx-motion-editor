---
phase: quick-260717-9hw-polish-physics-paint-right-sidebar-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
autonomous: false
requirements:
  - QUICK-260717-9HW
must_haves:
  truths:
    - "D-01: A failed durable-preset activation leaves the last successfully loaded row selected, preserves its immutable clipboard, and reports the attempted failure through Scripts status and LOG."
    - "D-02: Pointer, Enter, and Space activation of any complete script row reloads that preset every time, including the selected row, without applying it."
    - "D-03: The Paintbrush toolbar action is enabled only when a preset is selected and the current destination can receive replacement Apply; it reloads the selected preset and invokes the approved Apply Script path exactly once only after load succeeds."
    - "D-03: The clipboard remains reusable after Apply, while a disabled Play control with no callback immediately follows Paintbrush."
    - "The right-panel navigation appears immediately after Brush color with exact labels Brush color, Tool, Onion, Motion, and Scripts, and remains usable without horizontal overflow at desktop, narrow desktop, and stacked mobile widths."
    - "D-04: Production implementation and production-only build checks complete before execution stops for native visible UAT; regression work begins only after explicit approval."
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts"
      provides: "Transactional ID-specific durable preset activation and load"
      exports: ["RotoScriptLibraryController", "createRotoScriptLibraryController"]
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts"
      provides: "Typed replacement-destination eligibility separate from clipboard presence"
      exports: ["RotoScriptClipboardController", "createRotoScriptClipboardController"]
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      provides: "Accessible full-row load-only activation and six-control Scripts toolbar"
      exports: ["PhysicsPaintScriptsPanel", "PhysicsPaintScriptsPanelProps"]
    - path: "app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx"
      provides: "Relocated mixed-case right-panel tab navigation"
      exports: ["PhysicsPaintRightPanel"]
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Explicit row-load and selected-preset reload-then-Apply event handlers"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Compact focus, tab, row, toolbar, and responsive overflow styling"
    - path: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts"
      provides: "Post-UAT semantic UI regression coverage"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts"
      provides: "Post-UAT transactional activation regressions"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts"
      provides: "Post-UAT replacement eligibility and reusable clipboard regressions"
  key_links:
    - from: "app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts"
      via: "row pointer/keyboard activation calls activateAndLoad(row.id) only"
      pattern: "activateAndLoad"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts"
      via: "one awaited event handler reloads the selected durable preset before calling the existing applyScript once"
      pattern: "activateAndLoad[\\s\\S]*applyScript"
    - from: "app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts"
      via: "validated persisted script replaces the immutable clipboard before successful selection commit"
      pattern: "replaceClipboard"
    - from: "app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx"
      to: "app/src/components/physic-paint/physicsPaintStudio.css"
      via: "moved tablist uses mixed-case-safe tab styling and responsive min-width rules"
      pattern: "physics-paint-options-tabs"
---

<objective>
Polish the Physics Paint right sidebar and durable Scripts interactions while retaining the approved persistence, immutable clipboard, and Apply engine boundaries.

Purpose: Make the final Scripts UI direct, accessible, transactional, and visually stable without introducing effect-driven synchronization, a second replay path, or authority changes.
Output: Production controller/view/CSS integration, a mandatory native visible UAT stop, then user-approved regression coverage and final gates.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260717-9hw-polish-physics-paint-right-sidebar-tabs-/260717-9hw-CONTEXT.md
@.planning/quick/260717-9hw-polish-physics-paint-right-sidebar-tabs-/260717-9hw-RESEARCH.md
@.planning/quick/260716-dby-implement-durable-project-scoped-physics/260716-dby-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts
@app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
@app/src/components/physic-paint/physicsPaintStudio.css

Use the existing Preact Signals controllers and explicit event handlers. Do not add effects for controller synchronization, do not add dependencies, and do not create another replay/apply implementation.
</context>

<tasks>

## Pre-UAT task group — production files only

<task type="auto">
  <name>Task 1: Implement transactional Scripts activation, combined Load + Apply, and right-panel polish</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <action>
Implement the research file's exact source seams as one production-only integration pass.

In `physicsPaintRotoScriptLibrary.ts`, replace selection-first loading with an ID-specific transactional `activateAndLoad(id): Promise&lt;boolean&gt;` controller action. Capture the previously successful selection, request the opaque preset ID without publishing the attempted ID as preferred selection, validate/convert the returned document through the existing schema path, synchronously call the existing clipboard replacement port, and commit `selectedId` only after replacement succeeds. On request, validation, stale-operation, or replacement failure, retain the previous selected ID and clipboard, set the established status, and use the existing error LOG route per D-01. Do not short-circuit when `id` is already selected: every activation must reload per D-02. Keep Save, Rename, Delete, Refresh, sorting, invalid-file diagnostics, context generations, opaque IDs, and native authority unchanged.

In `physicsPaintRotoScriptClipboard.ts`, extend the typed availability contract with replacement-Apply destination eligibility and its typed disabled reason. Reuse the existing busy/disposal, Roto-mode, and generated-frame guards while intentionally omitting only the current-clipboard requirement, because the combined toolbar action loads first. Keep `applyScript()` unchanged as the sole approved sequential replay engine, and do not compare display strings to infer eligibility.

In `PhysicsPaintStudio.tsx`, expose two explicit handlers through the existing view-model boundary: row activation awaits `rotoScriptLibrary.activateAndLoad(id)` and performs no Apply; the Paintbrush action reloads `rotoScriptLibrary.selectedId.peek()` through the same transactional action and, only on success, awaits the existing `rotoScript.applyScript()` exactly once per D-03. Disable the combined action unless a row is selected, neither controller is busy, and replacement destination eligibility is true. Route failures through the existing Scripts status/apply message/last-error surfaces. Preserve clipboard reuse after Apply. Do not add a synchronization effect or duplicate replay path.

In `PhysicsPaintScriptsPanel.tsx`, update props for row activation and combined action. Make each complete row the single focusable `role="option"` surface with `tabIndex={0}`, pointer activation, and Enter/Space activation that calls only the row's transactional load. Preserve `aria-selected`, thumbnail, name, provenance, count, selected styling, listbox semantics, rename/delete focus management, and status. Ensure rename input and explicit nested actions stop pointer and relevant keyboard propagation so they never activate the row. Replace the current durable Load toolbar control with the existing Lucide `Paintbrush` icon wired to selected-preset reload + Apply; immediately follow it with the existing Lucide `Play` icon as a disabled, callback-free control whose accessible title/reason says playback is unavailable. Keep exactly six ordered controls: Save, Paintbrush, Play, Rename, Delete, Refresh, preserving all non-load actions.

In `PhysicsPaintRightPanel.tsx`, move rather than duplicate the existing Tool/Onion/Motion/Scripts tablist so it appears immediately after the Brush color section and before the selected options panel. Keep Tool first. Render the exact labels `Brush color`, `Tool`, `Onion`, `Motion`, and `Scripts`; retain the conditional LOG route and existing tab behavior.

In `physicsPaintStudio.css`, remove uppercase transformation from this tab system, visually attach the moved navigation to Brush color, add pointer/hover/focus-visible treatment to the full script row, and preserve compact thumbnail/ellipsis rules. Keep four option tabs flexible with `min-width: 0`, retain the six-column toolbar, and make desktop, 286px narrow desktop, and stacked mobile layouts avoid horizontal scrolling or clipped toolbar content. Use existing styles and icons only per Claude's discretion; do not alter unrelated Physics Paint surfaces.

Do not create or modify any test file in this task per D-04. After production edits, run only the production build checks in `<verify>` and inspect `git diff --check` plus `git diff --` for the six production files. Do not run the development server or Vitest before the checkpoint.
  </action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vite build &amp;&amp; git -C /Users/lmarques/Dev/efx-motion-editor diff --check &amp;&amp; git -C /Users/lmarques/Dev/efx-motion-editor diff -- app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.ts app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts app/src/components/physic-paint/PhysicsPaintStudio.tsx app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx app/src/components/physic-paint/physicsPaintStudio.css</automated>
  </verify>
  <done>All six production seams implement D-01, D-02, and D-03; typecheck, production Vite build, and diff checks pass; no test file changed; execution is ready to stop for native visible UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Stop for native visible UAT approval</name>
  <files>None — hard checkpoint; do not modify production or test files while awaiting approval.</files>
  <action>Per D-04, stop execution after Task 1. Do not add or modify regression tests, do not run Vitest or broader gates, and do not proceed to Task 3 until the user explicitly approves the native visible UAT checklist below. If the user reports an issue, fix the production behavior first, rerun Task 1's production-only checks, and present the checklist again.</action>
  <what-built>Transactional durable-preset row loading, selected-preset Paintbrush Load + Apply, disabled Play control, relocated mixed-case tabs, and responsive/focus polish.</what-built>
  <how-to-verify>
Perform these 15 checks in the native Physics Paint window without starting a development server:

1. Confirm `Brush color` uses exact mixed case and the `Tool`, `Onion`, `Motion`, `Scripts` navigation sits immediately beneath it, in that exact order, with no duplicate tab row.
2. Open each of Tool, Onion, Motion, and Scripts and confirm the existing panel content and conditional LOG route still work.
3. At normal desktop width, confirm the right panel scrolls vertically as needed and tabs, script toolbar, rows, and text are not clipped horizontally.
4. At a narrow desktop/right-panel width near the existing 286px breakpoint, confirm all four tabs and all six toolbar controls remain visible without horizontal scrolling.
5. In the stacked mobile layout, confirm the right panel remains vertically scrollable and the toolbar and rows have no horizontal overflow.
6. In Scripts, click a complete row and confirm the entire row is clickable, becomes selected only after loading succeeds, loads the preset into the transient clipboard, and does not paint/apply anything.
7. Focus another row and press Enter; confirm the same load-only behavior and visible selected-row/focus treatment.
8. Focus a row and press Space; confirm the same load-only behavior, no page-scroll side effect, and no Apply.
9. Activate the already-selected row again and confirm it reloads instead of being ignored; the canvas remains unchanged until Paintbrush is used.
10. Exercise a durable load failure if a safe fixture/path is available; confirm the previously successful row stays selected, its clipboard remains usable, the failed row is not shown as loaded, and the failure appears in Scripts status and LOG. If no safe native failure fixture exists, report this check as unavailable rather than altering project files manually.
11. Use inline Rename and its Enter/Escape handling; confirm editing the nested control does not activate/reload the row.
12. Confirm Save, Rename, Delete with confirmation/focus return, Refresh, compact thumbnail, name, provenance, brush count, selected styling, clipboard Discard, and timeline Copy/Apply remain operational.
13. On an eligible real-key or true-empty Roto destination, select a durable preset, discard/clear the transient clipboard, and confirm Paintbrush remains enabled because it can reload before applying.
14. Trigger Paintbrush once and confirm it reloads the selected durable preset as authoritative content, applies through the existing Apply Script path exactly once, publishes the final composite, and leaves the clipboard reusable for another destination. Also confirm a load failure does not start Apply and an ineligible/generated destination disables the combined action entirely.
15. Confirm disabled Play immediately follows Paintbrush, has the existing Play icon, communicates that playback is unavailable, and performs no action; confirm no disabled Import control remains.
  </how-to-verify>
  <verify>
    <automated>BLOCKED UNTIL USER ACTION — explicit native visible UAT approval is required before any regression command or test-file modification.</automated>
    <human-check>Reply `approved` only if all available checks pass, or describe each failing check by number.</human-check>
  </verify>
  <resume-signal>Type "approved" to authorize post-UAT regression work, or report failing checklist numbers.</resume-signal>
  <done>The user explicitly approves the native visible UAT, authorizing Task 3; without approval the plan remains paused.</done>
</task>

## Post-UAT task group — blocked until explicit approval

<task type="auto" tdd="true">
  <name>Task 3: Add approved regressions and run final verification</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</files>
  <behavior>
    - "Transactional row activation commits selection and immutable clipboard only after a successful load/replacement; request or replacement failure preserves both previous values and reports status/LOG."
    - "Activating the selected row again issues another durable load, and row activation never invokes Apply."
    - "Replacement destination eligibility permits real-key and true-empty targets without a current clipboard, but rejects busy, wrong-mode, and generated targets with typed reasons."
    - "The combined Paintbrush handler reloads the selected preset before exactly one approved Apply, blocks Apply after failed load, and leaves the clipboard reusable."
    - "The Scripts panel exposes exact toolbar order Save/Paintbrush/Play/Rename/Delete/Refresh, callback-free disabled Play, full-row pointer and Enter/Space activation, propagation-safe rename controls, exact mixed-case tabs, and responsive no-overflow CSS contracts."
  </behavior>
  <action>Only after explicit approval, update the existing focused Vitest files to cover the accepted mounted behavior. Prefer behavioral controller tests over source-text assertions for transactions and eligibility. Extend the library harness to prove success, repeated activation, request failure, and clipboard replacement failure; assert selection commits after replacement and that LOG/status use established routes. Extend clipboard tests for the typed replacement-destination availability matrix without changing the approved `applyScript()` behavior. Update the Scripts panel contract tests for mixed-case labels, tab placement seam, six-control order, Paintbrush/Play semantics, focusable row activation, Enter/Space prevention, nested-action propagation, and deterministic responsive CSS. Add no alternate test configuration and use `vitest run`.

Run the three focused files first, then the existing related Studio/clipboard/library/UI group, the Physics Paint subtree, full app Vitest, typecheck, Vite production build, Physics Paint package check/build, and `git diff --check`. Review the final diff specifically for accidental persistence/schema/native-authority changes, effects used as control flow, duplicate Apply/replay logic, callback-bearing Play, and test changes predating the recorded UAT approval. Produce the normal quick SUMMARY/completion artifacts only after all gates pass; describe the result as automated-ready if any separate final live acceptance remains outstanding.
  </action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.test.ts src/components/physic-paint/view/PhysicsPaintRightPanel.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vite build &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint check &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint build &amp;&amp; git -C /Users/lmarques/Dev/efx-motion-editor diff --check</automated>
  </verify>
  <done>Post-UAT regressions cover the approved transaction, availability, interaction, toolbar, casing, and responsive contracts; focused and broad Vitest runs, typecheck, production builds, package gates, diff check, and final review all pass; completion artifacts record the native approval boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script row/UI → Signals library controller | Pointer and keyboard input supplies only an existing opaque row ID; no path or filesystem authority crosses into the UI. |
| Library controller → parent/native script service | Validated typed requests retain project/native authority, operation correlation, and schema checks. |
| Durable load → immutable clipboard → Apply engine | Persisted content must validate and replace the clipboard transactionally before the sole approved Apply path can consume it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260717-9HW-01 | Tampering | `activateAndLoad(id)` | medium | mitigate | Accept only IDs present in controller rows, retain typed opaque-ID requests, validate through the existing persisted schema, and commit selection only after clipboard replacement succeeds. |
| T-260717-9HW-02 | Repudiation | Scripts failure reporting | low | mitigate | Preserve operation-correlated controller status, diagnostics, and the existing error LOG route for failed activation. |
| T-260717-9HW-03 | Elevation of Privilege | Scripts UI/native boundary | high | mitigate | Add no path-bearing API, import flow, schema change, native command, bridge bypass, or filesystem access; keep parent/native authority unchanged. |
| T-260717-9HW-04 | Denial of Service | Repeated row activation and combined Apply | medium | mitigate | Reuse both controllers' busy/disposal guards, await transactional load, and invoke Apply at most once after success. |
| T-260717-9HW-05 | Tampering | Generated/ineligible destination | medium | mitigate | Use typed replacement-destination eligibility before enabling the combined action and retain generated-frame guards inside the existing Apply controller. |
| T-260717-9HW-SC | Tampering | Package supply chain | low | accept | No package install or dependency change is planned; existing Lucide icons are reused. |
</threat_model>

<verification>
1. Task 1 changes production files only and passes typecheck, Vite production build, and diff checks.
2. Task 2 is a hard blocking native UAT checkpoint with all 15 requested checks; execution cannot advance without explicit approval.
3. Task 3 begins only after approval, adds focused regressions with `vitest run`, then passes related, subtree, full-app, type, build, package, and diff gates.
4. Final diff remains within the six production seams and three approved test seams, with no persistence, schema, native authority, replay, Motion, history, cache, project-discovery, or timeline Copy/Apply redesign.
</verification>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | QUICK-260717-9HW | Polish Physics Paint right sidebar tabs and Scripts interactions | 01 | COVERED | Tasks 1-3 deliver production polish, UAT, then approved regressions. |
| REQ | QUICK-260717-9HW | Single focused plan with a mandatory production-before-UAT boundary | 01 | COVERED | D-04 is enforced by separate pre-UAT, checkpoint, and post-UAT groups. |
| CONTEXT | D-01 | Failed activation preserves prior successful selection and clipboard; status/LOG report failure | 01 | COVERED | Transactional library action plus post-UAT controller tests. |
| CONTEXT | D-02 | Every pointer/keyboard activation reloads; row activation is load-only | 01 | COVERED | Full-row pointer/Enter/Space action and repeated-load contract. |
| CONTEXT | D-03 | Paintbrush reloads selected preset then applies exactly once; ineligible/load failure blocks; clipboard reusable; disabled Play follows | 01 | COVERED | Typed replacement eligibility, awaited Studio handler, six-control toolbar. |
| CONTEXT | D-04 | Production checks, hard native UAT stop, then tests and broad verification | 01 | COVERED | Task ordering and blocking checkpoint make reordering impossible. |
| CONTEXT | D-05 | Handler composition, focus styling, responsive CSS, method naming at Claude's discretion within Preact/controller boundaries | 01 | COVERED | Explicit event handlers, Signals controllers, semantic row focus, existing CSS system. |
| RESEARCH | R-01 | Move existing tablist; exact mixed-case labels; preserve LOG | 01 | COVERED | `PhysicsPaintRightPanel.tsx` and CSS action. |
| RESEARCH | R-02 | Transactional `activateAndLoad(id)` commits selection after clipboard replacement | 01 | COVERED | Library controller action and failure regression behavior. |
| RESEARCH | R-03 | Typed replacement destination eligibility omits only missing-clipboard guard | 01 | COVERED | Clipboard availability extension and matrix tests. |
| RESEARCH | R-04 | Studio composes awaited reload then existing Apply exactly once | 01 | COVERED | Explicit handler; no effect or second replay engine. |
| RESEARCH | R-05 | Full-row accessible activation with nested-action propagation protection | 01 | COVERED | Semantic option rows, pointer/Enter/Space, focus and propagation handling. |
| RESEARCH | R-06 | Six toolbar columns: Save, Paintbrush, Play, Rename, Delete, Refresh | 01 | COVERED | Existing icon dependency only; disabled callback-free Play. |
| RESEARCH | R-07 | Desktop, narrow desktop, and stacked mobile overflow safety | 01 | COVERED | CSS implementation plus native checklist checks 3-5. |
| RESEARCH | R-08 | Preserve native/schema/bridge authority and existing Apply/cache/persistence behavior | 01 | COVERED | Scope guard in actions, threat model, and final diff review. |
| RESEARCH | R-09 | Minimum pre-UAT commands are typecheck, Vite build, and diff check | 01 | COVERED | Task 1 automated verification uses the exact recommended commands. |
| RESEARCH | R-10 | Tests are prohibited until native UAT approval | 01 | COVERED | Task 2 blocks Task 3 and forbids test-file changes/commands before approval. |
| CONTEXT | Deferred | Persistence, replay, Motion, history, cache, schema, discovery, native authority, timeline Copy/Apply redesign | NONE | EXCLUDED | Explicitly out of scope; actions preserve these systems. |

</source_audit>

<success_criteria>
- Exact right-panel label casing and tab placement are visible and responsive.
- Durable row activation is accessible, repeatable, transactional, and load-only.
- Failed activation cannot displace the last successful selection or clipboard and is visible in status/LOG.
- Paintbrush performs selected-preset reload then one existing Apply only when the destination is eligible; Play is disabled and inert.
- The immutable clipboard remains reusable and no duplicate replay/controller synchronization path exists.
- Production build checks pass before native UAT, execution stops, and regression work occurs only after explicit approval.
- All post-UAT focused and broad automated gates pass and completion artifacts record the approved boundary.
</success_criteria>

<output>
Create `.planning/quick/260717-9hw-polish-physics-paint-right-sidebar-tabs-/260717-9hw-SUMMARY.md` when post-UAT execution is complete.
</output>
