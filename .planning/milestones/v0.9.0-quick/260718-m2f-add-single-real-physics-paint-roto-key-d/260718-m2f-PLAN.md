---
phase: quick-260718-m2f-add-single-real-physics-paint-roto-key-d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts
  - app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts
  - app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts
  - app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: false
requirements:
  - QUICK-260718-M2F
must_haves:
  truths:
    - "D-01/D-07/D-09: Dragging one real Roto key to a valid empty destination performs one non-ripple atomic replacement whose `frameMappings` contains exactly one `{ fromFrame, toFrame, mode: 'move' }` entry; every unrelated real key retains its canonical source frame."
    - "D-02/D-03/D-04: Generated cells, occupied real-key cells, same-source targets, stale cells, invalid visible ranges, and newly locked contexts reject the drop without local mutation or parent publication."
    - "D-04/D-09: After the established live-pixel save barrier succeeds and latest state is re-read, destination ownership and spacing are resolved against a hypothetical canonical model with the dragged source removed; the final projection must map the moved key back to the requested visible destination or the move is rejected without mutation."
    - "D-08: Pointer capture, a deliberate threshold, compact source/target preview, Escape-first cancellation, idempotent cleanup, scroll-aware hit testing, and proportional rAF edge auto-scroll preserve ordinary click selection and leave no listener/capture/animation residue."
    - "D-04/D-09: A committed move preserves the complete cached real-key object plus editable, preview, dirty, spacing, background, and launch/cache state; generated interpolation is regenerated once by one local `replaceRotoKeyFrames` call and one parent replacement payload."
    - "D-05/D-10: Matching parent acceptance alone finalizes selection/focus, success status, and one move-specific history command; bridge-send rejection, parent rejection, and timeout rollback while the original launch identity remains authoritative, whereas launch replacement or disposal settles false and prevents stale rollback/finalization."
    - "D-05/D-10: Accepted move, Undo, and Redo each stop cached playback, display the correct destination/source pixels, remain one logical local history action in chronological order with paint mutations, and do not change the global application timeline drag behavior."
  artifacts:
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts"
      provides: "Canonical single-key move transaction with one move mapping and rebased spacing overrides"
      contains: "mode: 'move'"
    - path: "app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts"
      provides: "Move-specific local replacement/publication port with bridge-send failure settlement"
    - path: "app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts"
      provides: "Idempotent operation-bound settlement for acceptance, parent rejection, timeout, launch replacement, and disposal"
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts"
      provides: "Authoritative launch-replacement cancellation through the existing reset seam"
    - path: "app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts"
      provides: "Focused move-only snapshot, rollback, replay, and chronological paint-barrier coordinator"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Captured Pointer Events gesture, reliable hit testing, auto-scroll, compact preview, and focus handoff"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Latest-model drop validation, local history dispatch, playback stop, and workflow wiring"
    - path: "app/src/components/physic-paint/physicsPaintStudio.css"
      provides: "Grab, active-source, valid/invalid target, and committing states that preserve focus visibility"
  key_links:
    - from: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "`onMoveRotoKey(fromDisplayFrame, toDisplayFrame)` is invoked only after a threshold-crossed valid release"
      pattern: "onMoveRotoKey"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts"
      via: "Studio revalidates the latest projection and passes canonical source/destination data to the existing key transaction seam"
      pattern: "moveKey"
    - from: "app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts"
      via: "One move-specific optimistic local replacement and one acknowledgement-aware `replace-roto-key-frames` publication"
      pattern: "replace-roto-key-frames"
    - from: "app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts"
      to: "app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts"
      via: "Only matching acceptance records/finalizes; transport rejection, parent rejection, or timeout requests identity-guarded rollback; replacement/disposal settles false without stale restoration"
      pattern: "operationId"
    - from: "app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "Studio Undo/Redo dispatch chooses the chronologically latest paint barrier or acknowledged key command"
      pattern: "undo"
---

<objective>
Add Pointer Events drag movement for exactly one real Physics Paint Roto key through the existing canonical transaction and persistence seams, implementing D-01 through D-10 without introducing multi-selection, ripple behavior, replacement, swapping, or global timeline changes.

Purpose: Let the user reposition a complete real Roto key safely and visibly while preserving canonical interpolation timing, cached/editable payload, parent authority, rollback integrity, and one-action local Undo/Redo semantics.
Output: A canonical single-move transaction, acknowledgement-aware optimistic apply/rollback and history coordination, a compact captured-pointer timeline interaction with reliable auto-scroll, production typecheck/build proof, and a blocking native visible UAT checkpoint.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260718-m2f-add-single-real-physics-paint-roto-key-d/260718-m2f-CONTEXT.md
@.planning/quick/260718-m2f-add-single-real-physics-paint-roto-key-d/260718-m2f-RESEARCH.md
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts
@app/src/components/physic-paint/roto/rotoSourceDisplayModel.ts
@app/src/components/physic-paint/roto/rotoKeyTransactions.ts
@app/src/components/physic-paint/roto/rotoApplyTransactions.ts
@app/src/components/physic-paint/roto/rotoSaveTransactions.ts
@app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts
@app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts
@app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts
@app/src/components/physic-paint/hooks/useRotoEditBufferController.ts
@app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx
@app/src/stores/physicPaintStore.ts
@app/src/components/physic-paint/physicsPaintStudio.css
@app/package.json

This is the production-first pre-UAT stage. Do not create, modify, rename, delete, or run regression tests. Do not start a development or preview server, use MCP Chrome DevTools, install packages, alter global application timeline drag behavior, or add multi-selection/group/ripple/replacement/swap behavior. Use Preact-native refs and direct Pointer Events; effects are permitted only for external gesture cleanup/invalidation, not as state synchronization chains.
</context>

<decisions>
- D-01: Move exactly one real Roto key with non-ripple semantics; every other real key keeps its canonical source position and crossed keys do not shift.
- D-02: A different occupied real-key destination is invalid; release cancels without replace, swap, insert, shift, mutation, or publication.
- D-03: Generated interpolation cells are render-only and invalid as drag sources or destinations; never redirect to their owner or promote them.
- D-04: Resolve visible source/destination cells through the canonical source/display model, preserve interpolation OFF/ON absolute/projection behavior, and persist canonical source ownership plus spacing overrides rather than projected display ownership.
- D-05: After acknowledged success, select/display the moved key at its final visible destination, focus that cell where practical, stop cached playback, show cached pixels immediately, and report source and destination.
- D-06: Use gentle proportional horizontal edge auto-scroll through the native scroller; hit testing remains correct with `scrollLeft`, resize, scrolling, and Safari-style overscroll clamping, and the loop stops on every completion/cancellation path.
- D-07: Route the move through the existing atomic `frameMappings` array with exactly one `{ fromFrame, toFrame, mode: 'move' }` entry; do not add selection arrays, group state, or speculative selection architecture.
- D-08: Use Pointer Events and pointer capture with a deliberate threshold; below-threshold interaction remains ordinary selection, Escape cancels the active drag first, unrelated pointers are ignored, and cleanup is idempotent for release outside, invalid/no-op drop, cancellation, lost capture, unmount, model/range/context change, stale data, and newly active locks.
- D-09: Revalidate latest source, destination, model, context, and locks at drop; apply one complete resulting real-key set, relocate all associated local state, rebase spacing, regenerate interpolation once, and publish one parent replacement payload. Never model the move as Delete plus Paste.
- D-10: Parent rejection or timeout restores the complete before snapshot; only an acknowledged move enters local history as one logical Undo/Redo action, hover/rejected work never enters history, and global application history/timeline behavior remains untouched.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Build canonical post-removal move resolution and exhaustive acknowledgement settlement</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts, app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts, app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts</files>
  <read_first>app/src/components/physic-paint/roto/physicsPaintRotoKeyController.ts, app/src/components/physic-paint/roto/rotoSourceDisplayModel.ts, app/src/components/physic-paint/roto/rotoKeyTransactions.ts, app/src/components/physic-paint/roto/rotoApplyTransactions.ts, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPersistenceIntegration.ts, app/src/components/physic-paint/hooks/useRotoApplyLifecycle.ts, app/src/components/physic-paint/hooks/usePhysicsPaintApplyResultController.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts, app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts, app/src/stores/physicPaintStore.ts</read_first>
  <action>
Implement D-01/D-02/D-03/D-04/D-07/D-09 in `physicsPaintRotoKeyController.ts` as one focused `move` branch; do not alter Copy/Duplicate/Insert/Delete/Paste behavior. The move input carries the requested source/destination display frames, the latest canonical source frame, latest real-key payloads, and latest interpolation settings. Validate integer/nonnegative frames, require the latest source display cell to be a real key, reject a generated source or destination without owner redirection, reject a different occupied real-key display cell, and reject a same-source/no-op move.

Resolve destination ownership in the required order. First remove the dragged source from the latest canonical source list and create a hypothetical `RotoSourceDisplayModel` from those remaining sources plus settings whose segment overrides have been normalized for that source-removed model. Resolve the requested destination display frame against that hypothetical model with the existing real-key save-target logic; do not call `saveRealKeyAtDisplayFrame` against the pre-move model. Reject if the resolved canonical target collides with a remaining real key. Then build the final one-mapping transaction, rebase source-related overrides through `{ fromFrame, toFrame, mode: 'move' }`, merge the hypothetical target's `previousSegmentOverride`, create the final source/display model, and require its real-key projection for the moved canonical target to have `displayFrame === requestedToDisplayFrame`. If ownership, spacing, or this final-projection invariant cannot be established, return failure before any snapshot, local replacement, history change, or publication.

For a valid transaction, clone the complete source `PhysicPaintRotoCacheFrame` to the destination canonical source frame, preserve every unrelated real-key payload at its existing canonical source frame, preserve all non-identity payload fields, and return exactly one move mapping. Include destination `activeFrame`/`load-real-key` restore, generated/reference cleanup for one full regeneration, source/destination changed/removed sets, rebased valid overrides with unrelated custom spans intact, and a success label reporting both visible endpoints. Never compose Delete plus Paste.

Implement a move-specific persistence port in `useRotoPersistenceIntegration.ts`; do not widen `rotoCoordinatorPorts.ts`, `useRotoNavigationCoordinator.ts`, or the generic Roto session. The port must generate the operation ID, register optional settlement before optimistic apply/send, expose the existing one-call local `physicPaintStore.replaceRotoKeyFrames` replacement plus cache/list synchronization for forward/rollback/replay, and send exactly one complete `replace-roto-key-frames` payload with rebased interpolation settings. A synchronous or asynchronous rejection from `sendPhysicPaintApplyPayload` is a terminal transport failure: invoke the registered failure settlement immediately, do not start/retain the timeout, and rollback only if the snapshot's original layer/launch operation identity is still authoritative.

Extend `useRotoApplyLifecycle.ts` with one optional move settlement record bound to operation ID, kind, layer, and start frame. Use one idempotent terminal settlement function that first clears the timeout, pending apply, active operation, callback record, and committing ownership, then invokes exactly one outcome callback. Matching parent `ok` resolves true; matching parent rejection and matching timeout resolve false and request identity-guarded rollback; mismatched/ignored results do nothing and cannot finalize or rollback. Expose explicit cancellation for authoritative launch replacement and component disposal: both clear timers/callbacks once and settle the move Promise false, but they must not restore the obsolete snapshot or permit later transport/result/timeout callbacks to finalize or rollback it.

Use the concrete existing launch reset seam in `usePhysicsPaintLaunchIntegration.ts`: immediately before `resetRotoSessionForLaunch` clears pending apply state for a newly settled authoritative context, call the lifecycle's launch-replacement cancellation. Keep the hook's unmount cleanup routed through the same idempotent disposal settlement. Do not add another launch coordinator, save path, or generalized command infrastructure, and preserve existing status/result behavior for non-move applies.
  </action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <done>Destination ownership is resolved only against the latest source-removed hypothetical model and the final projection maps the moved key to the requested visible destination; one operation controls optimistic apply/publication; transport rejection, parent rejection, timeout, launch replacement, and disposal each settle exactly once with identity-safe rollback/finalization and no changes to generic key utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Flush live state, commit through focused move history, and wire the captured-pointer gesture</name>
  <files>app/src/components/physic-paint/hooks/useRotoKeyMoveHistory.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts, app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts, app/src/components/physic-paint/hooks/useRotoEditBufferController.ts, app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx, app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</read_first>
  <action>
Implement D-10 in a new `useRotoKeyMoveHistory.ts` focused exclusively on acknowledged single-key moves. Keep move commands and opaque paint-order barriers in refs, expose combined Undo/Redo availability through the existing Signal shape, and do not create generalized command, selection, or application-history infrastructure. Capture complete before/after move snapshots through narrow Studio-provided ports: canonical real-key/cache frames, interpolation settings and segment overrides, edit-buffer `frameStates`, `previewFrames`, `dirtyFrames`, editable-frame list, selected source/display frame, launch cached frames, cached reference/background state, and the identity needed to restore the visible canvas deterministically. Apply forward, rollback, Undo, and Redo through the same Task 1 one-call local replacement plus one parent publication seam; relocate editable/preview/dirty ownership through the single move mapping. Record only matching accepted forward moves, advance the history cursor only after matching accepted replay, keep the cursor unchanged after failed replay, truncate redo on newly accepted move or paint work, preserve the existing ten-level paint behavior, and clear move commands when launch identity changes.

Implement D-02/D-03/D-04/D-05/D-09/D-10 in `PhysicsPaintStudio.tsx` with one async `moveRotoKey(fromDisplayFrame, toDisplayFrame)` callback passed through the existing inferred workflow prop seam. At callback entry capture only the expected current launch identity for stale detection, stop cached playback, call the established current-frame save barrier in its existing order—`engineRef.current?.flushPendingStrokeFinalizations()` followed by `await rotoPersistence.flushLivePixels(currentFrameOwnerSourceFrame ?? currentFrame)`—and abort false with no snapshot, transaction, local mutation, history change, or move publication if either step fails. Do not introduce a second save/snapshot path.

Only after that barrier resolves, re-read from current refs/signals/store—not the pre-await closure—the launch layer/operation identity, latest canonical real keys/cache, timeline model and projection, interpolation settings, edit-buffer dirty state, current rendered timeline window, bridge mode, and every key/apply/script/navigation/mutation lock. Reject if the captured launch identity is no longer current, the source is no longer a real-key display cell, source or destination is generated, the destination is occupied by another real key, the target is same-source, the destination is outside the latest existing `frameCells` window/scroller model, or any lock/dirty/apply state became invalid. Pass this post-barrier latest state to Task 1, which alone resolves canonical destination ownership against the hypothetical source-removed model and verifies the final projection; never call `rotoTimelineActions.saveRealKeyAtDisplayFrame(toDisplayFrame)` against the pre-move model.

Immediately before optimistic local apply, let the move-history coordinator capture the complete before snapshot and bind it to the current launch identity. On matching acceptance only, capture/store the after snapshot, select and display the moved real key at the verified final visible destination, restore its cached pixels/reference, set `Moved key {source} to frame {destination}.`, notify the script source revision, and return true for focus handoff. On transport rejection, parent rejection, or timeout, rollback the full before snapshot only when that original identity is still authoritative and return false; on launch replacement or unmount, settle false, clear committing ownership, and allow no stale rollback/finalization. Wire Studio Undo/Redo through the focused coordinator while delegating paint barriers to the existing `rotoFrameEditing.undo/redo`; feed each completed paint mutation ID into the coordinator before the existing early returns so chronological local ordering remains correct.

Implement D-06/D-08 in `PhysicsPaintWorkflowStrip.tsx` with one imperative gesture-session ref and one compact rendered preview/commit state. Add `onMoveRotoKey?: (fromDisplayFrame: number, toDisplayFrame: number) => Promise<boolean>` plus explicit display-frame and semantic-kind data attributes. Eligible real-key cells use a grab affordance; generated and empty cells never start a drag. On primary-pointer down retain pointer ID, origin/latest viewport coordinates, source frame/element, candidate, capture state, rAF ID, and one idempotent cleanup closure. Use a 6 CSS-pixel Euclidean threshold; below threshold preserve ordinary click selection. Once crossed, consume only the eventual drag click, capture the pointer, show compact cell-level source/target state, install Escape in capture phase, and ignore unrelated pointers.

During drag use `document.elementFromPoint`, `closest('[data-roto-display-frame]')`, and containment in the current native timeline scroller; do not use captured `event.target` or custom-thumb arithmetic. Read fresh cell attributes on every pointer/rAF tick and invalidate the gesture on props/model/range/lock changes. Classify real occupied, generated, same-source, outside/range-stale, and lock-invalid candidates as invalid; only a currently empty rendered display cell is visually valid, while Studio remains authoritative after the save barrier. Keep hover free of local or parent data mutation.

Add the timestamp-scaled rAF edge-scroll loop with 32 CSS-pixel zones and 40–160 CSS pixels/second proportional speed. Clamp fractional `scrollLeft` to `0..scrollWidth-clientWidth`, call the existing scrollbar updater, and re-hit-test at unchanged viewport coordinates. Stop and clean up on pointerup, invalid/no-op release, Escape, `pointercancel`, lost capture, unmount, model/range/context invalidation, or new locks. End capture/listeners/rAF before awaiting the move Promise; retain only pending UI, clear it on every true/false/disposal settlement, and focus the destination data-attributed cell only after true when it still exists. Preserve ordinary below-threshold clicks and suppress exactly one post-drag click.

Style D-05/D-08 in `physicsPaintStudio.css` using the existing timeline vocabulary: grab cursor, active-source outline, valid target outline with a non-color cue, subdued invalid target, and pending destination. Preserve `.current`, `:focus-visible`, cell dimensions, semantic fills, scrollbar, and compact layout. Do not add a pixel ghost, selection rectangle, multi-selection UI, or global drag overlay.
  </action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <done>The local timeline offers one accessible captured-pointer drag; Studio completes the established live-pixel barrier before snapshot/transaction, re-reads all authoritative state, and commits only a move whose source-removed destination resolution projects back to the requested visible frame; accepted moves alone enter focused chronological Undo/Redo and every failure/disposal path clears pending UI safely.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pass production gates and approve native single-key drag behavior</name>
  <files>None — verification only; regression tests remain untouched</files>
  <action>Run the exact automated commands below after Tasks 1-2. If either fails, fix production code only and rerun the same commands. Do not run Vitest, modify tests, start a server, use MCP Chrome DevTools, install packages, or proceed to post-UAT regression work. After both commands pass, stop and present the native UAT flow. Await explicit approval; the executor must not claim the quick complete or begin tests while this checkpoint is unresolved.</action>
  <what-built>A single-real-key Pointer Events drag in the local Physics Paint Roto timeline, with canonical non-ripple movement, exact payload preservation, proportional edge auto-scroll, parent-acknowledged rollback, final destination focus/selection, and one-action local Undo/Redo.</what-built>
  <how-to-verify>
1. With interpolation OFF, click a real key without moving beyond the threshold. Confirm ordinary selection occurs, no drag preview appears, no parent save is triggered, and the canvas shows that key normally.
2. Drag one real key later to a visible empty frame. Before release, confirm the source remains unchanged, only compact source/target cues appear, and no canvas/cache/timeline data moves during hover. Release and confirm exactly one key relocates, crossed keys do not shift, playback stops, final cached pixels appear immediately, the destination becomes selected/focused, and status reports both frames.
3. Compare the moved key before/after: cached alpha pixels, background/paper metadata, editable paint, preview state, onion composition, and export/playback appearance remain exact. Save/close/reopen and confirm the moved source ownership and pixels persist.
4. Undo once using the tool rail and keyboard, confirming the complete key returns to the source as one action with spacing/pixels intact. Redo once, confirming the same complete move returns to the destination as one action. Paint before and after a move, then verify Undo/Redo follows the actual chronological local action order without affecting the main application timeline.
5. Drag a key earlier across one or more other real keys. Confirm non-ripple semantics: only the dragged key moves and every crossed/unrelated key retains its canonical source position.
6. Attempt release over a different occupied real key. Confirm the target shows an invalid cue and release causes no mutation, replacement, swap, shift, history entry, status success, or parent publication.
7. With interpolation ON, attempt to start from and release over generated cells. Confirm generated cells never start or accept movement and are not redirected to their owner. Move a real key to a valid empty visible destination and confirm projected display timing is correct while persisted ownership remains canonical.
8. Create or use distant custom spacing, move an endpoint earlier and later, and confirm the affected segment overrides rebase correctly, unrelated custom spans remain unchanged, generated interpolation refreshes once, and OFF/ON toggling preserves the approved absolute/projected positions.
9. Begin dragging, then test Escape, release outside the timeline, `pointercancel`-equivalent interruption if available, window/context change, and a lock becoming active. Confirm Escape cancels before other local Escape behavior, every path removes preview/capture/auto-scroll immediately, and none publishes or enters history.
10. Scroll the custom timeline horizontally and drag to targets at multiple scroll positions. Confirm hit testing lands on the cell under the pointer rather than a stale/unscrolled frame. Hold near each horizontal edge and confirm gentle proportional auto-scroll, continuous correct target updates, clamped ends, and immediate stop on release/cancel.
11. While Apply, key mutation, script Load/Apply, Play Script, or navigation lock is active, attempt a drag and confirm it cannot start or commit. Start a drag before a context/range/model change and confirm drop revalidation rejects stale source or destination without publication.
12. Acknowledged manual limitation for this pre-test native checkpoint: the inspected production UI/bridge exposes no safe reproducible rejection or timeout trigger for a valid move payload. Do not close windows, corrupt project/layer data, use DevTools, add fault-injection/debug controls, or manufacture malformed payloads. If a genuine native bridge rejection/timeout occurs during the other steps, confirm complete rollback and no history entry; otherwise record these terminal paths as not manually inducible and leave their deterministic regression coverage for the explicitly post-UAT `vitest run` stage. A normal successful move must still prove that only its matching acknowledgement finalizes selection/status/history.
13. Confirm keyboard Backspace/Delete still removes only the selected real key through its approved path after a move/Undo/Redo, ordinary frame navigation and cached playback still work, focus-visible styling remains legible, and there is no modifier/range/marquee/group/multi-selection behavior.
  </how-to-verify>
  <resume-signal>Type `approved` only after the full native matrix passes, or describe the first failing click, drag, mapping, payload, spacing, acknowledgement, rollback, history, scroll, focus, persistence, preview/playback/onion/export, keyboard-deletion, or scope-isolation case. The executor must stop here.</resume-signal>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app build</automated>
  </verify>
  <done>Typecheck and minimum production build pass without tests or a server, and the user explicitly approves the complete native single-key drag, cancellation, canonical mapping, payload, rollback, history, persistence, and parity matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer/DOM surface → workflow gesture | Untrusted pointer identity and arbitrary DOM hit targets are interpreted as a potential timeline move. |
| Workflow display frames → Studio canonical model | Visible cells must be re-resolved against latest source/display projection, locks, range, and launch identity before mutation. |
| Optimistic local store/edit buffer → parent bridge | Local complete replacement precedes parent authority and must be reversible if the matching operation fails. |
| Parent apply result → history/final selection | Only the operation-ID/kind/start-frame-matched acknowledgement may finalize status, selection, focus, and history. |
| Local key history → engine paint history | Key commands and paint mutations share local Undo/Redo ordering without entering global application history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260718-M2F-01 | Spoofing / Tampering | `PhysicsPaintWorkflowStrip` hit testing | high | mitigate | Accept only data-attributed cells contained by the current native scroller, then re-resolve the display frame in Studio's latest canonical model. |
| T-260718-M2F-02 | Tampering | Drop-time move validation | high | mitigate | Flush pending stroke finalization and `flushLivePixels` first; after it resolves, re-read launch identity, projection, visible range, dirty state, source/target kind, occupancy, bridge, and all locks before any snapshot or transaction. |
| T-260718-M2F-03 | Tampering | Roto key transaction | high | mitigate | Resolve ownership/spacing against the latest source-removed hypothetical model, require the final moved-key projection to equal the requested visible destination, and otherwise return before apply/publication. |
| T-260718-M2F-04 | Tampering | Apply settlement | high | mitigate | Bind one idempotent settlement to operation ID, kind, layer, and start frame; handle send rejection, parent rejection, timeout, launch replacement, and disposal explicitly while ignored/mismatched results do nothing. |
| T-260718-M2F-05 | Information / Integrity loss | Optimistic rollback | high | mitigate | Capture full cache, interpolation, edit-buffer, dirty, launch, selection, and reference state immediately before apply; rollback on transport/parent/timeout failure only while the original identity remains authoritative, and never after replacement/disposal. |
| T-260718-M2F-06 | Denial of Service | Pointer/apply timers and callbacks | medium | mitigate | Use idempotent gesture cleanup plus idempotent apply settlement so capture, listeners, rAF, timeouts, callbacks, and pending UI clear exactly once on every terminal path. |
| T-260718-M2F-07 | Repudiation / Tampering | Local Undo/Redo coordinator | medium | mitigate | Record only acknowledged moves, retain paint-mutation ordering barriers, move the cursor only after acknowledged replay, and clear commands when launch identity changes. |
| T-260718-M2F-SC | Tampering | Package supply chain | low | accept | No package installation or dependency change is planned; native Pointer Events, DOM hit testing, `scrollLeft`, rAF, Preact refs, Signals, and existing project modules are sufficient. |
</threat_model>

<verification>
1. Each production task typechecks independently with `pnpm --dir app typecheck`; no test command is authorized before native approval.
2. The final automated gate runs `pnpm --dir app typecheck` and `pnpm --dir app build`, proving TypeScript contracts and the minimum Vite production build without starting a server.
3. Source review confirms the established stroke-finalization plus `flushLivePixels` barrier precedes every move snapshot/transaction; destination resolution uses the latest source-removed hypothetical model; final projection equals the requested display frame; one mapping/replacement/publication occurs; and every settlement path is idempotent and identity-guarded.
4. Native UAT proves click preservation, valid earlier/later movement, invalid/generated/occupied/stale cancellation, custom spacing and interpolation parity, scrolled targeting/auto-scroll, payload persistence, chronological Undo/Redo, keyboard deletion, preview/playback/onion/export parity, focus/accessibility, and absence of multi-selection or global timeline changes. Rejection/timeout is explicitly recorded as not safely inducible unless it occurs naturally.
5. Execution halts at the blocking native checkpoint; regression tests and post-UAT closure remain outside this plan.
</verification>

<source_audit>
| SOURCE | ID | Feature/Requirement | Task/Checkpoint | Status | Notes |
|--------|----|---------------------|-----------------|--------|-------|
| GOAL | QUICK-260718-M2F | Add Pointer Events drag movement for exactly one real Physics Paint Roto key through the atomic frame-mapping seam | Tasks 1-2 + Task 3 | COVERED | Production transaction, UI, acknowledgement/history, automated gate, and native UAT are all planned. |
| REQ | QUICK-260718-M2F | Production functionality plus pre-UAT typecheck/minimum build; no regression tests before visible UAT | Task 3 | COVERED | Only typecheck/build commands are used and execution stops at UAT. |
| RESEARCH | Pointer gesture architecture | One imperative ref session, compact render state, pointer capture, threshold, idempotent cleanup | Task 2 | COVERED | Uses the existing right-panel cleanup pattern and Preact-native refs. |
| RESEARCH | Scroll-aware target mapping | `elementFromPoint`, scroller containment, native `scrollLeft`, proportional timestamp rAF | Task 2 | COVERED | Includes 6 px threshold, 32 px zones, 40-160 px/s, clamping, and re-hit-testing. |
| RESEARCH | Canonical destination truth table | Reject generated/occupied/same/stale; resolve ownership after removing the dragged source and verify the final projection | Tasks 1-2 | COVERED | Studio runs the save barrier/re-read; controller resolves the hypothetical model and enforces display-frame equality. |
| RESEARCH | Atomic replacement seam | `frameMappings` already supports move and store replacement owns interpolation regeneration | Task 1 | COVERED | Exactly one move mapping and one accepted-path replacement/regeneration. |
| RESEARCH | Current persistence gap | Existing key persistence is dispatch-only and lacks rollback/settlement across every terminal path | Tasks 1-2 | COVERED | Send rejection, parent rejection, timeout, launch replacement, and disposal have explicit idempotent outcomes with authority-guarded rollback. |
| RESEARCH | Current history gap | Engine Undo/Redo does not own Roto key relocation | Task 2 | COVERED | Resolved with a focused move-only coordinator and opaque paint barriers, not generalized command infrastructure. |
| RESEARCH | No dependencies | Native APIs and existing project seams are sufficient | Tasks 1-2 | COVERED | No package or lockfile change. |
| CONTEXT | D-01 | Single-key non-ripple movement; crossed keys do not shift | Task 1 + Task 3 | COVERED | Controller moves only source payload; UAT covers earlier/later crossing. |
| CONTEXT | D-02 | Occupied destination invalid with no replacement/swap/shift/publication | Tasks 1-2 + Task 3 | COVERED | Preview and authoritative drop validation both reject it. |
| CONTEXT | D-03 | Generated source/destination invalid without owner redirect or promotion | Tasks 1-2 + Task 3 | COVERED | Semantic cell attributes plus canonical revalidation enforce render-only behavior. |
| CONTEXT | D-04 | Canonical source/display mapping with OFF/ON and spacing preservation | Tasks 1-2 + Task 3 | COVERED | Resolver runs against the latest source-removed hypothetical model and final projection must equal the requested display target. |
| CONTEXT | D-05 | Destination selection/focus, playback stop, pixels, and status after success | Task 2 + Task 3 | COVERED | Finalization occurs only after acknowledgement. |
| CONTEXT | D-06 | Reliable edge auto-scroll with `scrollLeft`, resize, and immediate stop | Task 2 + Task 3 | COVERED | Native scroller rAF loop and scroll-position UAT included. |
| CONTEXT | D-07 | Exactly one move mapping; no speculative multi-selection state/UI | Task 1 + Task 2 | COVERED | Scalar source/candidate only and one frame mapping. |
| CONTEXT | D-08 | Pointer capture, threshold, click preservation, Escape precedence, exhaustive cleanup | Task 2 + Task 3 | COVERED | All cancellation/invalidation paths are explicit and UAT-mapped. |
| CONTEXT | D-09 | Latest drop revalidation, complete payload move, one regeneration, one publication, not Delete+Paste | Tasks 1-2 + Task 3 | COVERED | Existing stroke-finalization/`flushLivePixels` barrier completes before latest-state re-read and transaction construction. |
| CONTEXT | D-10 | Acknowledgement rollback and one accepted local Undo/Redo action | Tasks 1-2 + Task 3 | COVERED | Transport/parent/timeout failures rollback only under the original identity; replacement/disposal settles false without stale restoration; accepted commands alone enter history. |
| CONTEXT | Deferred | Multi-selection, group behavior, ripple, replacement, swap, dependencies, global timeline behavior | None | EXCLUDED | Explicitly out of scope and prohibited in task actions. |
</source_audit>

<success_criteria>
- A primary-pointer drag can start only from one eligible real Roto key, crosses a deliberate threshold, preserves normal clicks, previews compactly, and cleans up capture/listeners/rAF on every terminal path.
- Scroll-aware hit testing and gentle proportional edge auto-scroll remain accurate across custom scrollbar movement, native `scrollLeft`, resize, and timeline model/range changes.
- The existing pending-stroke finalization plus `flushLivePixels` barrier succeeds before any move snapshot/transaction; all authoritative model, dirty, range, lock, bridge, and launch state is then re-read.
- Valid drop revalidation creates exactly one non-ripple move mapping after source-removed hypothetical destination resolution, preserves the complete real-key and local editable/preview payload, rebases affected spacing, requires final projection to equal the requested display frame, regenerates interpolation once locally, and publishes one complete parent replacement.
- Generated, occupied, same-source, outside, out-of-range, stale, barrier-failed, projection-mismatched, and newly locked drops publish nothing and mutate nothing.
- Matching parent success alone finalizes destination selection/focus, cached pixels, status, script revision, and one local history command; transport rejection, parent rejection, and timeout restore only under the original authoritative identity, while launch replacement/disposal settles false and blocks stale rollback/finalization.
- Undo/Redo replays an accepted move as one logical local action in chronological order with paint mutations, while existing paint history, keyboard deletion, navigation, playback, onion, export, persistence, and the global application timeline remain intact.
- `pnpm --dir app typecheck` and `pnpm --dir app build` pass; no tests, server, browser automation, dependency changes, or completion claim occur before explicit native UAT approval.
</success_criteria>

<output>
Stop at Task 3 after the automated gate and report the absolute plan path plus `awaiting native UAT`. Do not create or modify regression tests, start a server, use MCP Chrome DevTools, or claim completion until the user explicitly approves the native matrix.
</output>
