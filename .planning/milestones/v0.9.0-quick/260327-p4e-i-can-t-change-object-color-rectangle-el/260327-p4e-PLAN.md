---
phase: quick
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/sidebar/PaintProperties.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can select a rectangle, ellipse, or line and change its color via the color picker in SELECTION mode"
    - "User can select a rectangle, ellipse, or line and change its stroke width"
    - "User can select a rectangle, ellipse, or line and convert it to a brush stroke"
  artifacts:
    - path: "Application/src/components/sidebar/PaintProperties.tsx"
      provides: "Color picker and width controls that work with shapes"
---

<objective>
Fix color change and width change for shape tools (rect, ellipse, line) in SELECTION mode, and add "Convert to Brush" functionality for shapes.
</objective>

<context>
@Application/src/types/paint.ts
@Application/src/components/sidebar/PaintProperties.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix color picker for shapes in SELECTION mode</name>
  <files>Application/src/components/sidebar/PaintProperties.tsx</files>
  <action>
Fix the SELECTION mode color reading (lines 191-197) and color picker callbacks (lines 937-966) to also handle shapes (line, rect, ellipse).

1. In the loop at lines 191-197, change:
   ```
   if (el.tool === 'brush' && sel.has(el.id)) {
   ```
   to also check for shape tools:
   ```
   if ((el.tool === 'brush' || el.tool === 'line' || el.tool === 'rect' || el.tool === 'ellipse') && sel.has(el.id)) {
   ```
   And read currentColor from shapes using `(el as any).color`.

2. In the onLiveChange callback (around line 948), change:
   ```
   if (el.tool === 'brush' && sel2.has(el.id)) (el as any).color = c;
   ```
   to also handle shapes:
   ```
   if ((el.tool === 'brush' || el.tool === 'line' || el.tool === 'rect' || el.tool === 'ellipse') && sel2.has(el.id)) (el as any).color = c;
   ```
</action>
  <verify>
    <automated>grep -n "el.tool === 'brush'" Application/src/components/sidebar/PaintProperties.tsx | head -5</automated>
  </verify>
  <done>Color picker updates shapes when selecting rect, ellipse, or line</done>
</task>

<task type="auto">
  <name>Fix stroke width for shapes in SELECTION mode</name>
  <files>Application/src/components/sidebar/PaintProperties.tsx</files>
  <action>
Fix the applyWidth function (lines 199-215) to also update shapes.

Change the loop at lines 201-204 from:
  ```
  for (const el of pf.elements) {
    if (el.tool !== 'brush') continue;
    if (!sel.has(el.id)) continue;
    (el as any).size = newSize;
  }
  ```

To handle shapes:
  ```
  for (const el of pf.elements) {
    if (!sel.has(el.id)) continue;
    if (el.tool === 'brush') {
      (el as any).size = newSize;
    } else if (el.tool === 'line' || el.tool === 'rect' || el.tool === 'ellipse') {
      (el as any).strokeWidth = newSize;
    }
  }
  ```
</action>
  <verify>
    <automated>grep -n "el.tool !== 'brush'" Application/src/components/sidebar/PaintProperties.tsx</automated>
  </verify>
  <done>Width slider updates strokeWidth for shapes when selecting rect, ellipse, or line</done>
</task>

<task type="auto">
  <name>Add Convert to Brush button for shapes</name>
  <files>Application/src/components/sidebar/PaintProperties.tsx</files>
  <action>
Add a "Convert to Brush" button in the SELECTION mode section (after the reorder buttons around line 299) that converts selected shapes (line, rect, ellipse) into PaintStroke brush elements.

Implementation:
1. Add a helper function to convert a PaintShape to a PaintStroke:
   - For 'line': Create a stroke with points along the line path
   - For 'rect': Create a stroke tracing the rectangle outline
   - For 'ellipse': Create a stroke tracing the ellipse outline
2. Add a "Convert to Brush" button after the reorder buttons
3. On click: remove the original shape, add new brush strokes at same position, select the new brush strokes

The function signature:
```
function shapeToBrushStrokes(shape: PaintShape, brushOptions: PaintStrokeOptions): PaintStroke[]
```

For line: 2 points along the line.
For rect: 4 sides as separate strokes or one continuous stroke.
For ellipse: Sample ~36 points around the ellipse.
</action>
  <verify>
    <automated>grep -n "Convert to Brush" Application/src/components/sidebar/PaintProperties.tsx</automated>
  </verify>
  <done>Convert to Brush button appears when shapes are selected and converts them to brush strokes</done>
</task>

</tasks>

<verification>
Manual: Enter paint mode, draw a rectangle, switch to select tool, select the rectangle, click color picker - rectangle color should change. Change width slider - stroke width should change. Click "Convert to Brush" - shape should become a brush stroke.
</verification>

<success_criteria>
- Rectangle, ellipse, and line elements can have their color changed via the color picker in SELECTION mode
- Rectangle, ellipse, and line elements can have their stroke width changed via the width slider in SELECTION mode
- Selected shapes can be converted to brush strokes via "Convert to Brush" button
</success_criteria>

<output>
After completion, create `.planning/quick/260327-p4e-i-can-t-change-object-color-rectangle-el/260327-p4e-SUMMARY.md`
</output>
