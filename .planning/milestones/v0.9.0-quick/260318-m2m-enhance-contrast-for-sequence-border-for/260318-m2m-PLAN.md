---
phase: quick-260318-m2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/index.css
  - Application/src/components/sequence/SequenceList.tsx
autonomous: false
requirements: [QUICK-260318-m2m]

must_haves:
  truths:
    - "Selected sequence card has a clearly visible, high-contrast border across all three themes"
    - "Non-selected sequence cards have a subtle but visible border that distinguishes them from the background"
    - "The contrast difference between selected and non-selected borders is immediately obvious at a glance"
  artifacts:
    - path: "Application/src/index.css"
      provides: "Updated --sidebar-border-selected and --sidebar-border-unselected values for all three themes"
      contains: "--sidebar-border-selected"
    - path: "Application/src/components/sequence/SequenceList.tsx"
      provides: "Sequence item border rendering using CSS variables"
      contains: "--sidebar-border-selected"
  key_links:
    - from: "Application/src/components/sequence/SequenceList.tsx"
      to: "Application/src/index.css"
      via: "CSS custom property references in inline styles"
      pattern: "sidebar-border-(selected|unselected)"
---

<objective>
Enhance the visual contrast of sequence card borders in the sidebar so selected and non-selected states are clearly distinguishable across all three themes (dark, medium, light).

Purpose: Currently the sequence border colors (`--sidebar-border-selected` and `--sidebar-border-unselected`) blend too much with the panel backgrounds, making it hard to tell at a glance which sequence is selected.

Output: Updated CSS variables for all three themes with stronger differentiation, and optionally a slightly thicker border for the selected state.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/index.css (CSS variables for all three themes -- lines 76-98 dark, 161-183 medium, 246-268 light)
@Application/src/components/sequence/SequenceList.tsx (SequenceItem component, border at line 210)

<interfaces>
<!-- Current border CSS variables across themes -->

Dark theme (line 83-84):
  --sidebar-border-selected: #636382;     /* vs panel bg #252539 */
  --sidebar-border-unselected: #3D3D5A;   /* vs panel bg #252539 */

Medium theme (line 168-169):
  --sidebar-border-selected: #737392;     /* vs panel bg #343449 */
  --sidebar-border-unselected: #4D4D6A;   /* vs panel bg #343449 */

Light theme (line 253-254):
  --sidebar-border-selected: #9393B2;     /* vs panel bg #D5D5E9 */
  --sidebar-border-unselected: #ADADCA;   /* vs panel bg #D5D5E9 */

SequenceItem border usage (SequenceList.tsx line 210):
  border: `1px solid var(${isActive ? '--sidebar-border-selected' : '--sidebar-border-unselected'})`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase border contrast for selected and non-selected sequences across all themes</name>
  <files>Application/src/index.css, Application/src/components/sequence/SequenceList.tsx</files>
  <action>
Update CSS variables in Application/src/index.css for all three themes to increase border contrast:

**Dark theme** (lines 83-84):
- `--sidebar-border-selected`: Change from `#636382` to `#8888AA` (brighter, more visible against #252539 bg)
- `--sidebar-border-unselected`: Change from `#3D3D5A` to `#2E2E48` (darker/more subtle, closer to bg -- creates bigger gap from selected)

**Medium theme** (lines 168-169):
- `--sidebar-border-selected`: Change from `#737392` to `#9898BB` (brighter against #343449 bg)
- `--sidebar-border-unselected`: Change from `#4D4D6A` to `#3E3E58` (more subtle, closer to bg)

**Light theme** (lines 253-254):
- `--sidebar-border-selected`: Change from `#9393B2` to `#7070A0` (darker/more contrast against #D5D5E9 bg)
- `--sidebar-border-unselected`: Change from `#ADADCA` to `#C2C2D8` (lighter/more subtle, closer to bg)

Then in Application/src/components/sequence/SequenceList.tsx, update the SequenceItem outer div border (around line 210) to use `2px` for the selected state and `1px` for non-selected, making the selected sequence card even more prominent:

```
border: `${isActive ? '2px' : '1px'} solid var(${isActive ? '--sidebar-border-selected' : '--sidebar-border-unselected'})`
```

This gives two distinct contrast signals: color difference AND thickness difference.

Note: The existing `opacity: isActive ? 1 : 0.7` on the same element already helps differentiate. The border thickness change will compound with that for a strong visual hierarchy.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -n "sidebar-border-selected\|sidebar-border-unselected" Application/src/index.css && grep -n "border.*sidebar-border" Application/src/components/sequence/SequenceList.tsx</automated>
  </verify>
  <done>All three themes have updated border values with clearly higher contrast between selected and non-selected states. Selected sequence uses 2px border, non-selected uses 1px. Visual hierarchy is immediately apparent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sequence border contrast across all themes</name>
  <files>Application/src/index.css, Application/src/components/sequence/SequenceList.tsx</files>
  <action>
Human verification of the visual border contrast changes.
  </action>
  <verify>User confirms borders look good across all three themes.</verify>
  <done>User approves the visual contrast of selected vs non-selected sequence borders in dark, medium, and light themes.</done>
  <what-built>Enhanced sequence border contrast across all three themes with both color and thickness differentiation</what-built>
  <how-to-verify>
    1. Run the app with `pnpm tauri dev` (if not already running)
    2. Create or open a project with at least 2 sequences in the sidebar
    3. Click between sequences -- the selected one should have a clearly visible, thicker, brighter border
    4. Non-selected sequences should have a subtle, thinner border
    5. Switch themes (dark, medium, light) via the theme toggle and verify the contrast is good in all three
    6. The difference between selected and non-selected should be immediately obvious at a glance
  </how-to-verify>
  <resume-signal>Type "approved" or describe any contrast issues per theme</resume-signal>
</task>

</tasks>

<verification>
- CSS variables for `--sidebar-border-selected` and `--sidebar-border-unselected` updated in all three theme blocks
- SequenceItem border uses 2px for selected, 1px for non-selected
- No other components broken by the CSS variable changes (grep for other usages of these variables)
</verification>

<success_criteria>
Selected sequence borders are visually prominent and non-selected borders are subtle but visible across dark, medium, and light themes. The contrast ratio between selected border and panel background is significantly higher than before.
</success_criteria>

<output>
After completion, create `.planning/quick/260318-m2m-enhance-contrast-for-sequence-border-for/260318-m2m-SUMMARY.md`
</output>
