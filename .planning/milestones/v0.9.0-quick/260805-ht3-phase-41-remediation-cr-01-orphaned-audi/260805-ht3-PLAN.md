---
phase: quick-260805-ht3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
  - app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts
  - app/src-tauri/src/lib.rs
autonomous: false
requirements:
  - CR-01
  - WR-07
  - WR-08

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Stop-during-prepare never dispatches playAtCursor and never claims audio ownership (CR-01)"
    - "start→stop→start rejects the stale prepare completion; only the newest session can play (CR-01)"
    - "Ordinary Play/Stop/close/auto-resume behavior is unchanged (CR-01)"
    - "A crafted or out-of-file Range header returns 416 with Content-Range bytes */{file_size}; no u64 underflow, no oversized allocation, no abort (WR-07)"
    - "Valid open-ended (bytes=N-) and suffix (bytes=-N) Range forms still work; oversized valid ends clamp safely; empty files cannot panic (WR-07)"
    - "Seek/read failures surface as 500, never as zero-filled 206 bodies (WR-07)"
    - "efxasset refuses paths outside the allowed roots, symlink escapes, directories, and non-media extensions with 403 (WR-08)"
    - "The supported extension set has a single source shared with the MIME-mapping code (WR-08)"
    - "Legitimate current uses still pass: image/video layer media and EFX Paint audio (.aif/.aiff/.wav/.mp3/.aac/.flac/.m4a) under user dirs (WR-08)"
  artifacts:
    - "app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts — session-generation guard around the deferred playAtCursor"
    - "app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts — RED→GREEN regression tests for the three CR-01 scenarios"
    - "app/src-tauri/src/lib.rs — pure byte-range resolution helper + 416/500 handling, with unit tests in the existing #[cfg(test)] module"
    - "app/src-tauri/src/lib.rs — canonicalizing path-scope helper + shared extension/MIME table, with temp-fixture unit tests"
    - ".planning/quick/260805-ht3-phase-41-remediation-cr-01-orphaned-audi/260805-ht3-01-SUMMARY.md — finalized ONLY after UAT approval"
  key_links:
    - "start() session counter ↔ deferred prepare().then(playAtCursor) gate ↔ finishPlayback() invalidation (CR-01)"
    - "efxasset video Range branch ↔ pure resolve helper ↔ 416/206/500 responses (WR-07)"
    - "efxasset handler entry ↔ canonicalize + component-boundary root check ↔ shared MIME table ↔ tauri.conf.json assetProtocol.scope roots (WR-08)"
---

<objective>
Close the three Phase 41 review findings locked in scope: CR-01 (orphaned audio after stop-during-prepare in `useRotoCachedPlayback`), WR-07 (efxasset Range u64 underflow / swallowed IO errors), WR-08 (efxasset unscoped arbitrary filesystem reads). One atomic RED→GREEN commit per finding, then full gates, an unsigned packaged build, and a blocking combined native re-UAT checkpoint.

Purpose: Phase 41 shipped audio monitoring with a race that can leave orphaned audio playing and a latched ownership claim; the same phase's connect-src grant made the pre-existing efxasset Range underflow and missing path scoping load-bearing. These are correctness/security fixes on the exact sites named in the review — nothing else moves.

Output: three fix commits (`fix(41): CR-01 …`, `fix(41): WR-07 …`, `fix(41): WR-08 …`), green full gates, a fresh unsigned packaged build, and a SUMMARY finalized only after user-approved native UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-REVIEW.md
@app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts
@app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts
@app/src-tauri/src/lib.rs
@app/src-tauri/tauri.conf.json

Project rules that bind this plan (from CLAUDE.md / MEMORY):
- Use `vitest run` (NEVER watch mode); use pnpm, not npm.
- Preact-native patterns: the CR-01 guard is a plain ref alongside the existing `timerRef` — no new signals, no useEffect, no React-style workarounds.
- No backward-compat shims; clean edits on the named sites only.
- Never open/read/search certificate or signing files; packaged build is UNSIGNED.
- No new test config files; use the existing test setup.
- Security tests use temp fixture directories and synthetic files ONLY — never read /etc/passwd, ~/.ssh, or any real sensitive file.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — session-generation guard on deferred prepare→playAtCursor</name>
  <files>app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts, app/src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts</files>
  <behavior>
    - Test 1 (stop-during-prepare, RED first): with a mocked audio section present, start() with a deferred (manually resolved) prepare promise; call stop() while prepare is pending; then resolve prepare. Expect `playAtCursor` NEVER called and `efxPaintAudioOwnership.claimAudio` (via the monitor mock surface) never reached — no ownership retained or claimed.
    - Test 2 (start→stop→start, RED first): first start with deferred prepare A; stop(); second start with its own prepare B resolving immediately; then resolve stale prepare A. Expect `playAtCursor` called exactly once, with the SECOND session's cursor/range arguments (first cached appFrame as cursor, last cached appFrame + 1 as range end).
    - Test 3 (happy path unchanged): start with audio section, prepare resolves while still playing → `playAtCursor` called once; stop() → `efxPaintAudioMonitor.stop()` called (existing funnel).
    - Test 4 (no-audio path unchanged): no audio section → monitor's prepare never called (existing tests keep passing).
  </behavior>
  <action>
    RED: extend the existing test file (same file, same preact/hooks hoisted-mock pattern already at its top). Add `vi.mock` for `../audio/efxPaintAudioMonitor`, `../audio/efxPaintAudioPreviewStore`, and `../audio/efxPaintAudioOwnership` using `vi.hoisted` fn handles so each test can install deferred promises and assert call counts. Default store mock returns a null section so the pre-existing tests in this file keep passing unchanged; the new tests opt into a two-track section fixture. Use `vi.useFakeTimers()` + the existing `installWindowTimers()` helper for the interval. Write tests 1–4 above, run them, and confirm 1 and 2 FAIL against the current implementation before touching source.

    GREEN: in `useRotoCachedPlayback.ts`, add a monotonic session guard next to `timerRef` (a `useRef<number>` counter, e.g. `audioSessionRef` — a plain mutable ref, NOT a signal and NOT useState, per project Preact conventions; this value never drives rendering). Increment it at the top of `start()` so every fresh start (including `updateFps` re-entry and `resetForLaunch`→`start`) gets a new generation, and invalidate the pending generation inside `finishPlayback()` (the single stop funnel — increment there too, so any in-flight prepare from the just-ended session goes stale). In `start()`, capture the generation into a local const immediately before calling `efxPaintAudioMonitor.prepare(audioPreview)`, and inside the `.then` continuation dispatch `playAtCursor(...)` ONLY when BOTH `timerRef.current !== null` AND `audioSessionRef.current` still equals the captured generation; otherwise return silently. Do not change `efxPaintAudioMonitor`, `efxPaintAudioOwnership`, the audioPreview payload, the loop-wrap/drift/note calls, or any status-queue behavior — the guard is strictly additive around the deferred dispatch.

    Run the focused file until all four tests pass plus every pre-existing test in the file.
  </action>
  <verify>
    <automated>cd app && pnpm vitest run src/components/physic-paint/hooks/useRotoCachedPlayback.test.ts</automated>
  </verify>
  <done>
    Tests 1–2 confirmed failing pre-fix and passing post-fix (RED→GREEN observed in output, not assumed); full file green. Commit exactly one atomic commit containing both the tests and the fix: `fix(41): CR-01 guard deferred audio play behind playback session generation`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-07 — pure Range resolution helper; 416 on invalid ranges; 500 on IO failure</name>
  <files>app/src-tauri/src/lib.rs</files>
  <behavior>
    - `Range: bytes=100-50` on any file → 416, `Content-Range: bytes */{file_size}`, empty body.
    - `Range: bytes=99999999-` on a smaller file → 416 (same shape); no allocation attempted.
    - `Range: bytes=N-` (open-ended, valid) → 206 spanning N..file_size-1.
    - `Range: bytes=-K` (suffix form) → 206 spanning the last K bytes (K clamped to file_size).
    - `Range: bytes=0-999999` on a smaller file → 206 with end clamped to file_size-1.
    - Empty file (file_size = 0) with any Range → 416; no subtraction overflow.
    - Seek or read failure → 500, never a zero-filled/partial 206 body.
  </behavior>
  <action>
    RED: in the existing `#[cfg(test)] mod tests` at the bottom of `app/src-tauri/src/lib.rs` (do NOT create a new test file or test config), add unit tests for a pure helper `resolve_byte_range(range_header: Option<&str>, file_size: u64)` covering every behavior case above. The helper returns a small enum (e.g. `Satisfied { start: u64, end: u64 }` / `Unsatisfiable`) — pure, no IO, no Response building. Run `cargo test` and confirm the new tests FAIL because the helper does not exist yet (compile error counts as RED only if the tests were first written against the intended signature; state the intended signature in the test module).

    GREEN: implement `resolve_byte_range` as a free function (module-private) in `lib.rs`: parse only the `bytes=` unit and a single range; treat malformed specs as unsatisfiable; reject `end < start` and `start >= file_size`; clamp `end` to `file_size - 1`; compute suffix length with saturating arithmetic (`min(K, file_size)`); never compute `end - start + 1` before validation. Then rewire the video Range branch (~:443-483) to call the helper: on `Unsatisfiable` return 416 with `Content-Range: bytes */{file_size}` and `Access-Control-Allow-Origin: *`; on `Satisfied` allocate exactly `end - start + 1`, and replace `file.seek(...).ok()` and `let _ = file.read_exact(&mut buf)` with error propagation that returns a 500 response (empty body, same CORS header) instead of a zero-filled buffer. Keep the 206 response headers (`Content-Range`, `Content-Length`, `Accept-Ranges`, CORS) byte-identical in shape to today. Do not touch the non-video full-read branch or anything outside the Range path; no new crates.

    Rebased-range note for tests: the 500-on-IO-failure path is covered indirectly (helper tests prove no underflow; the closure change is mechanical). Do not fake IO failures with real sensitive files; if a failure-path test is written, use a temp fixture file deleted between metadata and open.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test resolve_byte_range</automated>
  </verify>
  <done>
    All new range tests pass; existing `mod tests` tests still pass; no `unwrap_or(file_size - 1)`-style unguarded arithmetic remains in the Range branch; exactly one atomic commit: `fix(41): WR-07 resolve efxasset byte ranges without u64 underflow`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-08 — canonicalized path scoping + shared extension/MIME table; then full gates and unsigned packaged build</name>
  <files>app/src-tauri/src/lib.rs</files>
  <behavior>
    - File inside an allowed root (temp fixture) with a supported extension → served.
    - `../` traversal escaping an allowed root → 403.
    - Symlink inside an allowed root pointing OUTSIDE it → 403 (canonicalization resolves the link before comparison).
    - Path that is a directory or otherwise not a regular file → 403 (or 404 if it does not exist).
    - Unsupported extension (e.g. `.txt`, `.exe`, extensionless) → 403, even inside an allowed root.
    - Supported audio extensions `.aif/.aiff/.wav/.mp3/.aac/.flac/.m4a` inside an allowed root → served with an `audio/*` MIME.
    - All path comparisons use path-component boundaries (`Path::starts_with`), never string prefix matching.
    - SECURITY-TEST BOUNDARY: every test uses `std::env::temp_dir()`-based unique fixture dirs and synthetic files ONLY. No real sensitive path is ever opened by a test. No new crates.
  </behavior>
  <action>
    RED: extend the same `#[cfg(test)] mod tests` with tests for two new pure helpers against temp fixtures: (1) `mime_for_efxasset_path(path: &str) -> Option<&'static str>` — the single shared extension→MIME source; (2) `resolve_efxasset_path(decoded_path: &str, allowed_roots: &[PathBuf]) -> Result<PathBuf, EfxassetRejection>` — percent-decoded input, `std::fs::canonicalize` (resolves symlinks), regular-file requirement, component-boundary root membership. Cover every behavior case above; confirm they fail pre-implementation.

    GREEN: (a) Replace the ends_with MIME chain (~:398-417) with `mime_for_efxasset_path`, a single match over lowercased extensions returning `Option<&'static str>`: keep every existing image/video mapping, and ADD the audio set the app imports (`wav, mp3, aac, flac, m4a, aif, aiff` — per the audio import filter in `app/src/components/views/ImportedView.tsx:384`) mapped to `audio/wav`, `audio/mpeg`, `audio/aac`, `audio/flac`, `audio/mp4`, `audio/aiff` respectively. This is the one shared source for both MIME and the allowlist; unknown extensions return `None`. Note the behavioral change: these audio files previously fell through to `application/octet-stream` — the new explicit mappings preserve that legitimate use. (b) Implement `resolve_efxasset_path`: canonicalize the decoded path (canonicalize failure → `NotFound`); require `metadata.is_file()`; require `mime_for_efxasset_path` to be `Some` (`UnsupportedExtension` → 403); require the canonical path to `starts_with` at least one canonical allowed root (`OutOfScope` → 403). (c) In the protocol closure, rename `_app` to `app` and build the allowed-root list per request to mirror `assetProtocol.scope` in `tauri.conf.json` (:39-42): `$APPDATA` and `$RESOURCE` via Tauri's path resolver on the AppHandle, `$HOME` via the home directory, plus the literal `/Volumes`, `/tmp`, `/private`; canonicalize each root and silently drop roots that fail to canonicalize (e.g. absent dirs). Keep the root list and helper inside `lib.rs` — no config-file reads at runtime, no new modules. (d) Wire the handler entry: after percent-decoding, call `resolve_efxasset_path`; 403 with CORS header and empty body on scope/extension/not-regular-file rejections, 404 on missing; use the resolved canonical path for all subsequent IO and the shared table for `Content-Type`. Everything downstream (Range branch from Task 2, full-read branches, headers, status codes for accepted files) stays byte-identical.

    After the WR-08 tests pass, run the full locked gate sequence: `cd app && pnpm vitest run` (full suite), the project's existing TypeScript check (`pnpm typecheck` — the existing package script running `tsc --noEmit`; no new configs), `cd app/src-tauri && cargo test` (full suite) and `cargo check`, then a fresh UNSIGNED packaged build `cd app && pnpm tauri build`. Per standing user preference, run the long gates in the background — launch the full Vitest suite, the full cargo test, and especially the packaged build with `run_in_background`, and resume only on the completion notification (no polling). Never open/read/search certificate or signing files; if the environment attempts signing, use the project's established unsigned-build path. Do NOT launch or open the built app — the user runs the server/app themselves.
  </action>
  <verify>
    <automated>cd app/src-tauri && cargo test efxasset && cargo check && cargo test && cd ../../app && pnpm typecheck && pnpm vitest run && pnpm tauri build</automated>
  </verify>
  <done>
    All scoping tests green with temp fixtures only; full Vitest suite green; `pnpm typecheck` (`tsc --noEmit`) clean; full `cargo test` + `cargo check` clean; fresh unsigned packaged build succeeds; exactly one atomic commit: `fix(41): WR-08 scope efxasset protocol to canonical media roots`. Long gates (full cargo test, full Vitest, packaged build) run in the background with completion notification, not polling. Gates evidence (commands + outcomes) recorded for the SUMMARY; then STOP for the UAT checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Blocking combined native re-UAT (audio monitor, efxasset range + scoping)</name>
  <what-built>
    Three atomic fix commits on this branch: CR-01 (session-generation guard on the deferred audio play in useRotoCachedPlayback), WR-07 (pure range resolution, 416/500 semantics in the efxasset video branch), WR-08 (canonicalized path scoping + shared extension/MIME table in efxasset). Full gates green: focused RED/GREEN tests per finding, full Vitest suite, tsc --noEmit, cargo test, cargo check, and a fresh UNSIGNED packaged build.
  </what-built>
  <how-to-verify>
    Launch the freshly built UNSIGNED packaged app (user runs it — the executor never launches the app or opens MCP Chrome DevTools) and run the combined focused re-UAT:
    1. EFX Paint audio basics: open a Physics/EFX Paint window with audio tracks present; Play → audio monitors in sync; Stop → audio stops; close the child window → ownership released; press Play in the main editor after child audio → main audio is suppressed while child plays and auto-resumes after child stop (ownership auto-resume unchanged).
    2. Stop-during-prepare silence: with a large WAV in the audio tracks, press Play then IMMEDIATELY press Stop (during decode) — expect total silence afterward: no orphaned audio starts playing after the stop, and the main editor's audio is NOT suppressed afterward (no latched ownership claim). Repeat as Play → Stop → Play: the second Play must start audio correctly at the cursor.
    3. Video layer: a sequence with a video layer renders frames and SEEKS correctly (scrub to several positions — AVFoundation range requests must still succeed).
    4. Image layer: a sequence with an image layer (including a path with accented characters, e.g. under a "Téléchargements"-style folder if available) renders correctly.
    5. Legitimate audio fetch: EFX Paint audio files in .aif/.wav (and other imported formats) under user directories still load and play — no 403 regressions from the new scoping.
    Report any anomaly with the exact step and a screenshot where relevant.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues. The executor does NOT self-approve: on "approved" it finalizes .planning/quick/260805-ht3-phase-41-remediation-cr-01-orphaned-audi/260805-ht3-01-SUMMARY.md with the UAT evidence and closes the quick task; on issues it returns to the relevant finding's task.</resume-signal>
</task>

</tasks>

<verification>
- Per-finding focused tests observed RED then GREEN (vitest run for TS — never watch; cargo test for Rust).
- Full Vitest suite green; `tsc --noEmit` clean; `cargo test` and `cargo check` clean.
- Fresh UNSIGNED packaged build produced (no certificate/signing file access).
- Exactly three fix commits, one per finding, each containing its failing-first test plus the fix.
- Blocking native UAT checkpoint approved by the user before SUMMARY finalization.
</verification>

<success_criteria>
- CR-01: stop-during-prepare is silent and claims nothing; start→stop→start rejects the stale prepare; ordinary Play/Stop/close/auto-resume unchanged — proven by tests and native UAT steps 1–2.
- WR-07: invalid ranges return 416 with `Content-Range: bytes */{file_size}`; valid open-ended/suffix/clamped forms return correct 206 bodies; IO failures return 500; no underflow or oversized allocation is reachable — proven by unit tests and UAT step 3.
- WR-08: efxasset serves only canonical paths under roots mirroring `assetProtocol.scope`, only regular files, only extensions from the single shared MIME table; symlink escapes and traversal rejected; image/video/audio legitimate uses preserved — proven by temp-fixture unit tests and UAT steps 3–5.
- SUMMARY.md finalized only after explicit user approval at the checkpoint.
</success_criteria>

<output>
Create `.planning/quick/260805-ht3-phase-41-remediation-cr-01-orphaned-audi/260805-ht3-01-SUMMARY.md` only after the user approves the blocking UAT checkpoint.
</output>
