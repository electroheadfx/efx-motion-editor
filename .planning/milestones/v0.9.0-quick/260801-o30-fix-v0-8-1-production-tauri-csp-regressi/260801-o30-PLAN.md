---
phase: quick-260801-o30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src-tauri/tauri.conf.json
  - app/src/releaseContract.test.ts
autonomous: true
requirements:
  - QUICK-260801-O30-CSP-01

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The production Tauri CSP img-src directive allows the data: scheme, so cached Roto PNG and Physics Paint data-URL images decode in the packaged app
    - All pre-existing img-src sources ('self', asset:, http://asset.localhost, efxasset:, blob:, https://*) remain present
    - No other CSP directive (default-src, script-src, style-src, connect-src, media-src) gains the data: scheme
    - Every product version surface remains 0.8.1 (no v0.8.2)
    - The release contract test suite proves the CSP shape and the canonical data:image/png;base64 Roto format
  artifacts:
    - app/src-tauri/tauri.conf.json with data: added to img-src only
    - app/src/releaseContract.test.ts extended with CSP contract assertions
  key_links:
    - tauri.conf.json CSP img-src <-> WebKit image decode of data:image/png;base64 URLs used by rotoCanvasFrames.ts / physicPaintStore.ts caches
    - releaseContract.test.ts CSP assertions <-> tauri.conf.json app.security.csp string (regression gate against future CSP edits)
---

<objective>
Fix the v0.8.1 production Tauri CSP regression that blocks every cached PNG data URL
("Refused to load data:image/png;base64,... because it does not appear in the img-src
directive"), which breaks Roto PNG reopen, Physics Paint cached previews, and green
Paint/Roto cell feedback in the packaged macOS app.

Purpose: Native UAT of the v0.8.1 packaging hotfix proved build/sign/notarize works but
the production CSP is missing the data: scheme in img-src while the app pervasively uses
data:image/png;base64 URLs (rotoCanvasFrames.ts, physicsPaintRotoAlphaMerge.ts,
previewRenderer.ts, projectPaperRaster.ts, ThumbnailCache.ts, physicPaintStore.ts).
Output: A one-directive CSP fix plus regression coverage in the existing release contract
test file; all versions stay at 0.8.1.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@app/src-tauri/tauri.conf.json
@app/src/releaseContract.test.ts

Locked implementation (user decision — non-negotiable):
- Modify ONLY the img-src directive of app.security.csp in app/src-tauri/tauri.conf.json.
- Add the data: scheme to img-src; preserve every existing img-src source.
- Do NOT add the data: scheme to default-src, script-src, style-src, connect-src, or media-src.
- Keep versions at 0.8.1 everywhere; do not tag, push, sign, or create releases.

Locked regression coverage (user decision — non-negotiable):
- Extend app/src/releaseContract.test.ts (no new test config, no browser harness) to assert:
  the CSP has an img-src directive; img-src contains the data: scheme; the existing
  asset/blob/https img-src sources remain; script-src, default-src, style-src, and
  connect-src do NOT gain the data: scheme; the canonical Roto PNG header constant
  ROTO_PNG_DATA_URL_HEADER = 'data:image/png;base64' (which produces the runtime
  data:image/png;base64, format) remains in
  app/src/components/physic-paint/roto/rotoCanvasFrames.ts.

Out of scope (hard constraints): Roto PNG persistence format changes; blob-URL migration;
backward-compat migrations; filesystem/permission changes; cache transaction semantics;
Apple credential/certificate access; tagging/pushing; GitHub releases; dev server;
Vitest watch mode (vitest run only).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSP img-src fix with test-first release contract assertions</name>
  <files>app/src/releaseContract.test.ts, app/src-tauri/tauri.conf.json</files>
  <behavior>
    - Test A: tauri.conf.json app.security.csp contains an img-src directive, and its
      whitespace-separated source tokens include 'self', asset:, http://asset.localhost,
      efxasset:, blob:, data:, and https://*
    - Test B: the source tokens of default-src, script-src, style-src, and connect-src
      (and media-src) do NOT include the data: scheme token
    - Test C: app/src/components/physic-paint/roto/rotoCanvasFrames.ts still declares the
      canonical header constant ROTO_PNG_DATA_URL_HEADER = 'data:image/png;base64' (the
      source-side constant that produces the runtime data:image/png;base64, format — the
      comma is appended when constructing URLs and located dynamically when parsing)
    - RED first: with the current config (img-src lacks the scheme), Test A must fail;
      then the config edit makes all tests pass
  </behavior>
  <action>
    RED — extend app/src/releaseContract.test.ts first, following the existing file's
    style (fs/JSON/text contract assertions, vitest describe/it, no new harness):
    1. Widen the tauriConfig type cast to include app?: { security?: { csp?: string } }.
    2. Add a small helper that extracts one directive's value from the CSP string by
       matching `(?:^|;)\s*&lt;name&gt;\s+([^;]+)` and a token helper that splits the value
       on whitespace — token-level matching, never substring, so e.g. an https://* token
       can never be mistaken for a scheme grant.
    3. Add a new describe/it block 'Tauri CSP image data-url contract' implementing
       behaviors A, B, C above. For Test C, read rotoCanvasFrames.ts via readFileSync and
       assert it contains the header constant declaration
       ROTO_PNG_DATA_URL_HEADER = 'data:image/png;base64' (do NOT assert a trailing
       comma — the file stores the header without it and appends/locates the comma at
       runtime, so a comma-terminated literal can never match the source text).
    4. Run pnpm --dir app exec vitest run src/releaseContract.test.ts and confirm the new
       img-src assertion FAILS (the other new assertions may pass already) — capture the
       failure as proof the test guards the regression.
    GREEN — edit ONLY the img-src directive inside the csp string in
    app/src-tauri/tauri.conf.json: insert the data: scheme token into the img-src source
    list (resulting source order: 'self' asset: http://asset.localhost efxasset: blob:
    data: https://*). Touch no other directive, no other key, and keep the JSON otherwise
    byte-identical. Do not bump the version field (stays 0.8.1).
    5. Re-run the focused test file and confirm the whole file passes.
    Per project CLAUDE.md: vitest run only, never watch mode; do not start the dev server.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/releaseContract.test.ts</automated>
  </verify>
  <done>
    The focused release contract suite passes, including new assertions proving img-src
    grants the data: scheme alongside all pre-existing sources, no other directive gained
    it, and the canonical Roto base64 PNG prefix is intact; tauri.conf.json diff touches
    only the img-src directive and version remains 0.8.1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full verification gate — suite, typecheck, build, release preflight</name>
  <files>app/src-tauri/tauri.conf.json, app/src/releaseContract.test.ts</files>
  <action>
    Run the complete verification battery for this fix, in order, from the repo root:
    1. pnpm --dir app exec vitest run  (full app suite — must pass with zero failures)
    2. pnpm --dir app run typecheck    (must pass)
    3. pnpm build                      (production build; confirm app/dist/index.html and
       bundled assets are emitted)
    4. bash -n scripts/macos-release.sh  (shell syntax check)
    5. bash scripts/macos-release.sh preflight  (macOS release preflight must pass;
       this is read-only validation — it must NOT trigger signing, notarization,
       tagging, pushing, or any credentialed step; if preflight attempts a credentialed
       operation, stop and report instead of proceeding)
    6. Confirm every product version surface is still 0.8.1: app/package.json,
       app/src-tauri/tauri.conf.json, app/src-tauri/Cargo.toml, app/src-tauri/Cargo.lock,
       PRODUCT_VERSION in scripts/macos-release.sh (the release contract version test in
       Task 1 already enforces this; re-confirm via git diff showing no version edits).
    Do not modify any file in this task beyond what Task 1 already changed; if any gate
    fails, fix only within the two owned files or report the failure — never expand scope.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run &amp;&amp; pnpm --dir app run typecheck &amp;&amp; pnpm build &amp;&amp; bash -n scripts/macos-release.sh &amp;&amp; bash scripts/macos-release.sh preflight</automated>
  </verify>
  <done>
    Full app vitest suite passes, typecheck passes, production build emits
    app/dist/index.html plus all assets, macos-release.sh passes bash -n and preflight,
    and git diff shows version 0.8.1 unchanged on every surface with edits confined to
    the img-src directive and the release contract test file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → CSP | The packaged WebKit WebView enforces the CSP; cached PNG data URLs cross from app JS into image decode |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o30-01 | Elevation of Privilege | app.security.csp | medium | mitigate | Grant data: ONLY in img-src (images cannot execute script); Test B contract-locks script-src/default-src/style-src/connect-src/media-src against ever gaining the scheme |
| T-o30-02 | Tampering | tauri.conf.json | low | mitigate | Test A token-level assertions lock the full img-src source list so a future edit cannot silently drop asset:/efxasset:/blob: sources |
</threat_model>

<verification>
- pnpm --dir app exec vitest run src/releaseContract.test.ts — focused CSP contract passes
- pnpm --dir app exec vitest run — full app suite passes
- pnpm --dir app run typecheck — passes
- pnpm build — production build emits app/dist/index.html and all assets
- bash -n scripts/macos-release.sh && bash scripts/macos-release.sh preflight — release script healthy
- git diff — only the two owned files changed; all product versions remain 0.8.1
- Native UAT (user, post-execution): packaged app reopens cached Roto PNG frames, Physics
  Paint cached previews render, and Paint/Roto cells turn green after painting
</verification>

<success_criteria>
The production Tauri CSP img-src directive includes the data: scheme with all prior
sources preserved and no other directive loosened; regression coverage lives in
releaseContract.test.ts; all automated gates pass; versions stay 0.8.1; ready for the
user's native packaged-app UAT of Roto/Physics Paint cache flows.
</success_criteria>

<output>
Create `.planning/quick/260801-o30-fix-v0-8-1-production-tauri-csp-regressi/260801-o30-SUMMARY.md` when done
</output>
