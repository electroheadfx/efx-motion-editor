---
phase: quick-260801-sp2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/assets/header-efx-motion.png
  - README.md
autonomous: false
requirements:
  - QUICK-260801-SP2
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The header image renders at the top of README.md through a Git-tracked, repository-relative path (docs/assets/header-efx-motion.png)
    - README.md contains no reference to the gitignored SPECS/ directory or any absolute local filesystem path
    - The `# EFX Motion Editor` text heading is preserved immediately above the image for accessibility, navigation, and search indexing
    - README.md describes Physics Paint as a shipped standalone Tauri window with a typed editor bridge, not as future or planned work
    - Documented v0.8.1 features (deterministic Roto timeline, caching, Undo/Redo, group operations, Roto Scripts, Play Script, frame sync, packaging safeguards) match the implementation and the published release notes
    - All repository-relative links referenced from README.md resolve to existing paths
  artifacts:
    - docs/assets/header-efx-motion.png (byte-identical copy of the user-provided SPECS image, tracked by Git)
    - README.md (refreshed opening description, header image, Standalone Physics Paint section, corrected stale facts)
  key_links:
    - README.md image reference -> docs/assets/header-efx-motion.png (tracked file)
    - README.md inline links -> app/src/lib/shaders/SHADER-SPEC.md, LICENSE, and any other referenced repo paths must exist
---

<objective>
Refresh the root README.md for the published v0.8.1 Standalone Physics Paint release: add the user-provided header artwork as a tracked repo asset, and rewrite the stale paint/physics sections so the README reflects what actually shipped.

Purpose: The README currently describes Physical paint as a "placeholder for future efx-physic-paint engine", which is false since v0.8.0/v0.8.1 shipped. The README is the project's public face and must match the released product.
Output: Tracked header image at docs/assets/header-efx-motion.png and a refreshed README.md whose feature claims match the v0.8.1 implementation.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
@app/src/types/paint.ts
@app/src/lib/physicPaintBridge.ts
@docs/macos-signed-release.md

Sources of truth for feature wording (use these; do not invent feature names):
- Published v0.8.1 GitHub release notes (Standalone Physics Paint, deterministic Roto timeline, complete Roto workflow, performance, native integration, packaging hardening sections).
- Current code: PaintMode is 'flat' | 'fx-paint' (app/src/types/paint.ts); physics paint communicates via the typed bridge in app/src/lib/physicPaintBridge.ts and the Tauri physic-paint:seek-frame listener (quick 260801-azb).
- .gitignore line 36 ignores SPECS/ — the README must never link there.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Copy header image to tracked docs/assets path and reference it at the top of README.md</name>
  <files>docs/assets/header-efx-motion.png, README.md</files>
  <action>
    End-to-end slice of this quick: the visible header artwork, tracked and rendered from a repository-relative path.

    1. Create the docs/assets/ directory and copy the user-provided image byte-for-byte: cp SPECS/header-efx-motion.png docs/assets/header-efx-motion.png (source path is allowed as the copy origin; it is gitignored, which is exactly why the tracked copy is needed).
    2. Run git add docs/assets/header-efx-motion.png so the asset is staged/tracked in the same commit as the README change.
    3. In README.md, keep the `# EFX Motion Editor` heading as the first line (do not delete or demote it), then insert a blank line followed by a GitHub-compatible Markdown image on its own line: an image whose src is the relative path docs/assets/header-efx-motion.png and whose alt text meaningfully describes the artwork (e.g. alt text conveying it is the EFX Motion Editor header artwork/banner). No HTML img tag with absolute path, no reference to the SPECS source location anywhere in the Markdown body.
  </action>
  <verify>
    <automated>test -f docs/assets/header-efx-motion.png && cmp -s SPECS/header-efx-motion.png docs/assets/header-efx-motion.png && git ls-files --error-unmatch docs/assets/header-efx-motion.png && head -1 README.md | grep -qx '# EFX Motion Editor' && grep -c 'docs/assets/header-efx-motion.png' README.md | grep -qv '^0$' && test "$(grep -v '^#' README.md | grep -c '](SPECS')" = "0"</automated>
  </verify>
  <done>
    docs/assets/header-efx-motion.png exists, is byte-identical to the SPECS original, and is tracked by Git; README.md still starts with the text heading and references the image via the repository-relative docs/assets path with meaningful alt text; no Markdown image or link in README.md points into SPECS/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh README content for the shipped v0.8.1 Standalone Physics Paint release</name>
  <files>README.md</files>
  <action>
    Rewrite the stale portions of README.md using the live codebase and the published v0.8.1 release notes as sources of truth. Verify every product term against the code before writing it; do not invent feature names or retain stale mode names.

    1. Opening description paragraph: rework the "3-mode paint system (flat/FX/physical)" claim. The editor paint layer has two modes — Flat (perfect-freehand vector strokes) and FX (p5.brush spectral pigment rendering) per app/src/types/paint.ts (PaintMode = 'flat' | 'fx-paint'). Physics Paint is not a third mode of that layer; it is a shipped standalone application window. Mention the standalone Physics Paint window and the deterministic Roto timeline among the headline capabilities.
    2. Paint Layer / Rotopaint section: rewrite the "3-Mode Paint System" paragraph so it describes only the Flat and FX modes and their mode-conversion dialogs. Delete the sentence that casts the Physical mode as something not yet built. Keep all other accurate paragraphs in this section (Brush FX Styles, Inline Color Picker, Circle Cursor, Wireframe Overlay, Stroke Draw-Reveal, Select Tool, Bezier Path Editing, Paint Mode, Stroke List Panel, Tablet & Pen Support) unchanged unless a fact is clearly stale.
    3. Add a new top-level feature section (### heading, placed after the Paint Layer / Rotopaint section) titled "Standalone Physics Paint". Document, matching the v0.8.1 release notes and the code:
       - efx-physic-paint runs as a complete standalone interactive application in its own Tauri window, preserving native incremental physics behavior (the failed headless batch adapter approach was abandoned).
       - The parent editor and the Physics Paint window communicate through a typed Tauri/postMessage bridge with exact acknowledgements (app/src/lib/physicPaintBridge.ts), including native frame synchronization via the Tauri-native physic-paint:seek-frame path.
       - Deterministic Roto timeline: stable physical-frame keys with direct frame positioning, generated interpolation, dynamic interpolation spacing controls, atomic acknowledged edits.
       - Automatic live PNG pixel caching with durable alpha composites, cached playback, and real-key repaint.
       - Accepted-only Undo/Redo history (10-level per-brush).
       - Single-key drag movement; rigid multi-select group drag and group delete; Force Spacing.
       - Group Copy, Paste, Duplicate, and fail-closed Cut.
       - Roto Scripts: durable project-scoped script library with reusable JSON presets and WebP thumbnails, Copy Script / Apply Script clipboard, and the Play Script one-hand multi-frame replay workflow (the former separate Play workflow was fused into Roto Scripts).
    4. Add a short "Release" note (one short paragraph or a few bullets, wherever it fits naturally — e.g. near the top after the features or in a Download/Release subsection) stating that v0.8.1 ships as a Developer ID signed and Apple-notarized macOS (Apple Silicon) DMG, and briefly naming the production packaging safeguards: complete frontend bundle entry, generated EFX application icon set, system codesign resolution, and a production CSP that grants PNG data URLs in img-src for cached Roto/Physics Paint frames. Do not turn this into a full release-notes copy.
    5. Tech Stack table: keep it accurate — the Paint Engine row should reflect perfect-freehand (flat strokes), p5.brush spectral pigment FX, fit-curve + bezier-js path editing, and @efxlab/efx-physic-paint as the standalone Physics Paint engine with the typed editor bridge.
    6. Correct clearly stale facts only: the Fade/GL transitions section claims project persistence via ".mce v11 format" while the Tech Stack table says ".mce v15" — grep the code (app/src-tauri models / app/src project-format constants) for the current format version and make both mentions consistent with it. Do not touch any other version surfaces, Git tags, or releases.
    7. Preserve the existing accurate sections unchanged: Key Photo Workflow, Multi-Sequence Timeline, Layer System, Cinematic FX Effects, GLSL Shader Library, Gradient Fills, transitions tables, Audio, Motion Blur, Canvas Motion Path, Keyframe Animation, Media Management, Video Export, Canvas Preview, UI Theme System, Professional Controls, Canvas Motion Fork, Prerequisites, Getting Started, Building for Production, Project Structure, License — except for the single stale format-version fact above.
    8. Boundaries: keep this a single monolithic README.md (no splitting into multiple Markdown files, no documentation-architecture redesign); do not modify any application code; do not reference SPECS/ paths or absolute local paths from the Markdown body.
  </action>
  <verify>
    <automated>test "$(grep -c 'future efx-physic-paint' README.md)" = "0" && test "$(grep -v '^#' README.md | grep -c '](SPECS')" = "0" && grep -q 'Standalone Physics Paint' README.md && grep -q 'physic-paint:seek-frame' README.md && grep -q 'Roto Scripts' README.md && grep -q 'Force Spacing' README.md && grep -q 'WebP' README.md && grep -qi 'notariz' README.md && for p in app/src/lib/shaders/SHADER-SPEC.md LICENSE docs/assets/header-efx-motion.png; do test -e "$p" || exit 1; done && test "$(grep -oE '\]\(([^)#]+)\)' README.md | sed -E 's/\]\(([^)#]+)\)/\1/' | grep -v '^https\?://' | while read -r link; do test -e "$link" || echo "MISSING:$link"; done | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>
    README.md no longer describes Physics Paint as future/planned work; a "Standalone Physics Paint" section documents the standalone Tauri window, typed bridge, frame sync, deterministic Roto timeline, caching/playback/repaint, Undo/Redo, drag/group operations, Force Spacing, clipboard operations, Roto Scripts with JSON presets and WebP thumbnails, and Play Script; v0.8.1 signing/notarization and packaging safeguards are mentioned briefly; the .mce format-version mentions are consistent with the code; every relative link in the file resolves to an existing path; no SPECS/ or absolute-path references remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of the rendered README and v0.8.1 feature accuracy</name>
  <what-built>
    Tracked header image at docs/assets/header-efx-motion.png referenced directly under the README title, plus a refreshed README.md: reworked opening description, corrected Paint Layer / Rotopaint section, new Standalone Physics Paint feature section, brief v0.8.1 release/packaging note, and consistent .mce format-version mentions. Automated checks confirm the image is tracked and byte-identical, no SPECS/ links remain, and all relative links resolve.
  </what-built>
  <how-to-verify>
    1. Open README.md in a Markdown preview (VS Code preview, or push nothing — just view the rendered file locally, e.g. `open` the repo on GitHub only after commit, or use any local Markdown renderer).
    2. Confirm the header artwork renders immediately under the "EFX Motion Editor" title.
    3. Skim the new "Standalone Physics Paint" section against what you actually see in the running app (Roto timeline keys, interpolation/spacing, caching, Undo/Redo, group drag, Force Spacing, Copy/Paste/Duplicate/Cut, Roto Scripts with presets/thumbnails, Play Script) — flag any feature name or claim that does not match the product.
    4. Confirm nothing in the README still reads as if Physics Paint is upcoming work, and that no link is broken.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the inaccuracies to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-provided asset -> repo | A binary image is copied from the gitignored SPECS/ directory into the tracked tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-SP2-01 | Tampering | docs/assets/header-efx-motion.png | low | mitigate | Byte-identity check (cmp) against the user-provided original before tracking |
| T-SP2-02 | Information disclosure | README.md | low | mitigate | Gates forbid references to the gitignored SPECS/ directory and absolute local paths that would leak the developer's filesystem layout |
| T-SP2-03 | Repudiation | README.md | low | accept | Docs-only change; no executable surface; Git history provides attribution |
</threat_model>

<verification>
- git ls-files --error-unmatch docs/assets/header-efx-motion.png (image tracked)
- cmp -s SPECS/header-efx-motion.png docs/assets/header-efx-motion.png (byte-identical copy)
- README.md starts with `# EFX Motion Editor` and references docs/assets/header-efx-motion.png
- Zero `](SPECS` links and zero "future efx-physic-paint" phrasing in README.md
- All relative Markdown links in README.md resolve to existing paths
- Human visual review of the rendered README approved
</verification>

<success_criteria>
- Header image renders from a tracked, repository-relative path with the text heading preserved
- README accurately presents Physics Paint as a shipped standalone Tauri window with the documented v0.8.1 Roto/bridge/packaging capabilities
- No stale "future placeholder" language, no SPECS/ or absolute-path references, no broken relative links
- User approves the rendered result
</success_criteria>

<output>
Create `.planning/quick/260801-sp2-refresh-readme-md-for-v0-8-1-standalone-/260801-sp2-01-SUMMARY.md` when done
</output>
