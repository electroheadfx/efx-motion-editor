---
phase: quick-260718-fp9-rename-the-visible-physics-paint-standal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/timeline.ts
  - app/src/lib/frameMap.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/sidebar/PhysicPaintProperties.tsx
  - app/src/lib/physicPaintBridge.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts
  - app/src-tauri/src/lib.rs
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
autonomous: false
requirements:
  - QUICK-260718-FP9
must_haves:
  truths:
    - "D-01: Physics Paint FX tracks display `PPaint #1`, `PPaint #2`, `PPaint #3`, and so on from their current top-to-bottom timeline order."
    - "D-01: Reordering or deleting a Physics Paint FX track immediately renumbers the remaining visible Physics Paint headers without changing persisted names."
    - "D-04/D-06: The standalone EFX Paint workflow header matches the selected Physics Paint track's current derived presentation label."
    - "D-07: `layerName`, sequence/layer names, IDs, Roto terminology, source types, caches, scripts, project data, and provenance remain unchanged."
    - "D-08/D-10: No tests or server are touched before renewed native UAT; the correction lands as one new atomic code commit after `9c491f26` and remains UAT-pending."
  artifacts:
    - path: "app/src/types/timeline.ts"
      provides: "Presentation-only `FxTrackLayout.headerLabel` contract"
    - path: "app/src/lib/frameMap.ts"
      provides: "Order-derived Physics Paint ordinals and unchanged non-Physics-Paint sequence labels"
    - path: "app/src/components/timeline/TimelineRenderer.ts"
      provides: "Normal and reorder-ghost headers consuming the derived label"
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Validated `workflowLabel` launch request and context propagation"
    - path: "app/src-tauri/src/lib.rs"
      provides: "Native serde and URL preservation for the presentation label"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "Standalone header rendering the selected workflow label"
  key_links:
    - from: "app/src/lib/frameMap.ts"
      to: "app/src/components/timeline/TimelineRenderer.ts"
      via: "`FxTrackLayout.headerLabel` drives both the normal track header and reorder ghost"
      pattern: "headerLabel"
    - from: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "Selected layer parent FX layout supplies `workflowLabel` at launch time"
      pattern: "workflowLabel"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src-tauri/src/lib.rs"
      via: "The optional presentation label crosses the native launch command without replacing `layerName`"
      pattern: "workflowLabel|workflow_label"
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      via: "`launchContext.workflowLabel` is passed as a workflow-strip prop"
      pattern: "workflowLabel"
---

<objective>
Correct the failed first native UAT by deriving Physics Paint labels from current timeline order and carrying the selected track's presentation label into the standalone header, per D-01 through D-06.

Purpose: Make multiple Physics Paint tracks visibly distinguishable and dynamically renumbered while preserving every persisted/domain identity contract per D-07.
Output: Presentation-only timeline layout metadata, renderer and standalone launch wiring, one new atomic code commit after `9c491f26`, production-only automated gates, and a blocking renewed native-UAT handoff; regression tests remain explicitly deferred until approval per D-08/D-10.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@app/src/types/timeline.ts
@app/src/lib/frameMap.ts
@app/src/components/timeline/TimelineRenderer.ts
@app/src/components/sidebar/PhysicPaintProperties.tsx
@app/src/lib/physicPaintBridge.ts
@app/src/types/physicPaint.ts
@app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts
@app/src-tauri/src/lib.rs
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/timeline/AddFxMenu.tsx
@app/package.json

The first implementation is already committed as `9c491f26` (`Roto #1` changed to a fixed `PPaint #1` in the standalone). Native UAT failed because every Physics Paint timeline track and the standalone header showed `PPaint #1`. Root-cause inspection confirms `getTimelineFxHeaderLabel()` and `PhysicsPaintWorkflowStrip` hardcode that text, while `AddFxMenu.handleAddPhysicPaintLayer()` intentionally persists the same `Physic Paint` sequence/layer names for every track. `fxTrackLayouts` already follows current sequence/timeline order, and `PhysicPaintLaunchContext.layerName` is real layer/script provenance.

Execute Task 1 only, including the red-capable inline source/function check, production gates, and one new atomic code commit; then halt at Task 2. Do not amend `9c491f26`. Do not start a development server. Do not create, modify, rename, delete, or run tests before explicit renewed native-UAT approval. Do not create a completion SUMMARY or update STATE/ROADMAP before approval.
</context>

<uat_history>
- First native UAT: FAILED after commit `9c491f26`.
- Observed failure: all Physics Paint timeline tracks and the standalone header displayed the same `PPaint #1` label.
- Required correction: presentation labels derive from current timeline order, and the standalone receives the selected track's derived label without mutating persisted identity/provenance.
</uat_history>

<tasks>

<task type="auto">
  <name>Task 1: Derive ordered Physics Paint labels and propagate the selected label to the standalone</name>
  <files>app/src/types/timeline.ts, app/src/lib/frameMap.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/lib/physicPaintBridge.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts, app/src-tauri/src/lib.rs, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <read_first>app/src/types/timeline.ts, app/src/lib/frameMap.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/sidebar/PhysicPaintProperties.tsx, app/src/components/timeline/AddFxMenu.tsx, app/src/lib/physicPaintBridge.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts, app/src-tauri/src/lib.rs, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/package.json</read_first>
  <action>
Before editing, run the same inline Node source-contract command defined in this task's automated verification and confirm that it exits non-zero against the failed-UAT implementation. This is the required red-capable production check; it is not a Vitest invocation and must not create or modify a test file. If it unexpectedly passes before edits, stop and inspect the source rather than implementing against stale assumptions.

Implement D-01/D-02 in `timeline.ts` and `frameMap.ts`. Add required presentation-only `headerLabel: string` metadata to `FxTrackLayout`. In the existing `fxTrackLayouts` computed projection, declare `let physicPaintOrdinal = 0` before iterating `sequenceStore.sequences.value`; while preserving that current top-to-bottom iteration, assign `headerLabel` to ``PPaint #${++physicPaintOrdinal}`` only when the primary layer type is `physic-paint`, and assign `seq.name` for every other layout. Do not change `sequenceName`, sequence creation, stored layer names, or ordering data.

Implement D-03 in `TimelineRenderer.ts`. Change `getTimelineFxHeaderLabel()` to consume `headerLabel` rather than deriving a fixed Physics Paint number. Keep its narrow input compatible with incomplete/direct callers by allowing optional `headerLabel` and returning `headerLabel ?? sequenceName`; this fallback must not invent an ordinal. Use this same helper for both the normal FX header and `ghostTrack` reorder header so dragging cannot diverge from the visible track label.

Implement D-04 in `PhysicPaintProperties.tsx` with event-time Preact Signal reads, not new component state or effects. Import `sequenceStore` and `fxTrackLayouts`; inside `handleOpenCanvas`, find the current parent sequence whose layers contain the selected layer ID, find its current layout by `sequenceId`, and pass `workflowLabel: fxLayout?.headerLabel` to `openPhysicPaintCanvas`. Use `.peek()` for these click-time reads so reorder and deletion are reflected at launch without introducing subscriptions or hook dependency orchestration.

Implement D-05 across the launch contract. Add optional `workflowLabel` to `PhysicPaintOpenRequest` and `PhysicPaintLaunchContext`. In `validateOpenRequest`, reject a supplied label unless it is a string with non-whitespace content, return the trimmed value, and pass it to `createPhysicPaintLaunchContext`; include it in the context only when present. In `isPhysicPaintLaunchContext`, validate it with the existing optional non-empty-string helper. Parse the optional `workflowLabel` flat URL parameter in `physicsPaintLaunchContext.ts`. Add Rust `workflow_label: Option<String>` with `#[serde(rename = "workflowLabel", skip_serializing_if = "Option::is_none")]`, and append its percent-encoded `workflowLabel` query parameter in `physics_paint_url()`. Preserve the full context through the existing native stored-state, emitted-event, and command serialization paths. Keep `layerName: layer.name` and Rust `layer_name` unchanged because they remain real persisted/script provenance per D-07.

Implement D-06 in `PhysicsPaintStudio.tsx` and `PhysicsPaintWorkflowStrip.tsx`. Add optional `workflowLabel` to the workflow-strip props, pass `launchContext?.workflowLabel` in the Studio workflow view model, and render `workflowLabel ?? 'PPaint'` in `.physics-paint-mode-label`. Preserve `aria-label="Selected Physics Paint mode"`. The fallback is deliberately non-ordinal for incomplete/direct launch contexts; do not leave a fixed numbered label in the renderer or standalone source.

Per D-07/D-08, keep all layer IDs, persisted sequence/layer names, `layerName`, Roto terminology, source types, cache ownership, scripts, project data, interpolation/playback behavior, and provenance unchanged. Do not create, edit, rename, delete, or run tests or test configuration. Do not start a development server and do not install packages.

Run the inline Node source-contract check again and require it to pass, then run `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`, `cargo check --manifest-path /Users/lmarques/Dev/efx-motion-editor/app/src-tauri/Cargo.toml`, and `git -C /Users/lmarques/Dev/efx-motion-editor diff --check`. Inspect the scoped diff and status. Per D-09/D-10, stage exactly the ten production files listed by this task and create one new atomic commit after `9c491f26` with message `fix(quick-260718-fp9): derive Physics Paint track labels`; never amend the earlier commit and never include this PLAN or unrelated files. Stop immediately at Task 2 after the commit.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const root='/Users/lmarques/Dev/efx-motion-editor/'; const read=(path)=&gt;fs.readFileSync(root+path,'utf8'); const timeline=read('app/src/types/timeline.ts'); const frameMap=read('app/src/lib/frameMap.ts'); const renderer=read('app/src/components/timeline/TimelineRenderer.ts'); const properties=read('app/src/components/sidebar/PhysicPaintProperties.tsx'); const bridge=read('app/src/lib/physicPaintBridge.ts'); const contextType=read('app/src/types/physicPaint.ts'); const parser=read('app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts'); const rust=read('app/src-tauri/src/lib.rs'); const studio=read('app/src/components/physic-paint/PhysicsPaintStudio.tsx'); const strip=read('app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx'); const fixed=['PPaint','#1'].join(' '); const checks=[timeline.includes('headerLabel: string'), frameMap.includes('let physicPaintOrdinal = 0'), frameMap.includes('PPaint #${++physicPaintOrdinal}'), frameMap.includes('headerLabel'), renderer.includes('headerLabel ?? fxTrack.sequenceName') || renderer.includes('headerLabel ?? sequenceName'), renderer.includes('getTimelineFxHeaderLabel(ghostTrack)'), !renderer.includes(fixed), properties.includes('workflowLabel: fxLayout?.headerLabel'), bridge.includes('workflowLabel'), contextType.includes('workflowLabel?: string'), parser.includes("nonEmptyParam(params, 'workflowLabel')"), rust.includes('workflow_label: Option&lt;String&gt;'), rust.includes('workflowLabel'), studio.includes('workflowLabel: launchContext?.workflowLabel'), strip.includes("workflowLabel ?? 'PPaint'"), !strip.includes(fixed)]; if(checks.some((value)=&gt;!value)) throw new Error('dynamic Physics Paint label source contract failed');" &amp;&amp; pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck &amp;&amp; cargo check --manifest-path /Users/lmarques/Dev/efx-motion-editor/app/src-tauri/Cargo.toml &amp;&amp; git -C /Users/lmarques/Dev/efx-motion-editor diff --check</automated>
  </verify>
  <acceptance_criteria>
- `FxTrackLayout.headerLabel` is required on real layout projections and is derived from current sequence order.
- Mixed timelines number only Physics Paint tracks, so three such tracks present `PPaint #1`, `PPaint #2`, and `PPaint #3` regardless of intervening non-Physics-Paint tracks.
- Normal and reorder-ghost headers consume the same derived label.
- Launching from the selected layer resolves its current parent layout and transports that layout's label through validated TypeScript, URL, and Rust/native launch contexts.
- The standalone displays the supplied workflow label and uses only the non-ordinal `PPaint` fallback when optional launch metadata is absent.
- `sequenceName`, `layerName`, stored names, IDs, source types, caches, scripts, project data, and Roto behavior remain unchanged.
- The inline source contract, app typecheck, Rust cargo check, and diff check exit 0 without starting a server or executing tests.
- HEAD is one new atomic commit directly after `9c491f26`, containing exactly the ten listed production files.
  </acceptance_criteria>
  <done>The dynamic presentation-label correction is committed and automated-ready, with all regression tests and completion tracking still blocked on renewed native UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve dynamic timeline numbering and selected-track standalone identity</name>
  <files>None — native user verification only</files>
  <action>Stop after the new atomic production commit. Present the exact native verification flow below and wait for explicit approval. Per D-08/D-10, do not run or modify tests, perform post-UAT work, create completion artifacts, update planning state, or claim completion while this checkpoint is unresolved.</action>
  <what-built>Physics Paint track labels now derive from current timeline order, the reorder ghost uses the same label, and launching the standalone carries the selected track's presentation label without changing persisted names or `layerName` provenance.</what-built>
  <how-to-verify>
1. In the native application you run locally, open or create a project with at least three Physics Paint FX tracks; place another FX type between two Physics Paint tracks if convenient.
2. Confirm the Physics Paint headers, read top to bottom among Physics Paint tracks, show exactly `PPaint #1`, `PPaint #2`, and `PPaint #3`; confirm the other FX track keeps its ordinary sequence name.
3. Reorder the Physics Paint tracks. While dragging, confirm the reorder ghost matches the dragged track's visible label; after dropping, confirm all Physics Paint tracks immediately renumber from the new top-to-bottom order.
4. Delete the middle Physics Paint track and confirm the remaining tracks immediately renumber to `PPaint #1` and `PPaint #2` without manually renaming anything.
5. Select each remaining Physics Paint track in turn, launch the standalone EFX Paint window, and confirm its workflow-strip header exactly matches the selected timeline track's current label.
6. Confirm the surrounding Roto controls, key/interpolation/playback behavior, caches, scripts, and project behavior remain unchanged. If persisted names or script provenance are visible in the workflow, confirm they still use the established real layer identity rather than the presentation ordinal.
  </how-to-verify>
  <resume-signal>Type `approved` to authorize the separate post-UAT regression-test and closure stage, or describe the first mismatching timeline, drag, delete, launch, or Roto behavior. Do not claim task completion at this checkpoint.</resume-signal>
  <verify>
    <automated>test "$(git -C /Users/lmarques/Dev/efx-motion-editor rev-parse --short=8 HEAD^)" = "9c491f26" &amp;&amp; test "$(git -C /Users/lmarques/Dev/efx-motion-editor show --pretty=format: --name-only HEAD | grep -v '^$' | sort)" = "$(printf '%s\n' 'app/src-tauri/src/lib.rs' 'app/src/components/physic-paint/PhysicsPaintStudio.tsx' 'app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts' 'app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx' 'app/src/components/sidebar/PhysicPaintProperties.tsx' 'app/src/components/timeline/TimelineRenderer.ts' 'app/src/lib/frameMap.ts' 'app/src/lib/physicPaintBridge.ts' 'app/src/types/physicPaint.ts' 'app/src/types/timeline.ts' | sort)"</automated>
  </verify>
  <done>The user has explicitly approved ordered numbering, reorder/delete renumbering, selected-track standalone matching, and unchanged Roto behavior; only then is the separate post-UAT stage unblocked.</done>
</task>

</tasks>

<post_uat_pending_checkpoint status="blocked-until-explicit-native-uat-approval">
Per D-08/D-10, regression coverage is explicitly deferred and is not authorized by this initial plan execution. After, and only after, explicit renewed native UAT approval, resume in a separate stage: update focused existing Vitest coverage for mixed-order ordinal derivation, reorder/delete renumbering, normal/ghost header parity, launch-request validation and `workflowLabel` propagation, `layerName` provenance preservation, launch-context parsing, and workflow-strip supplied-label/fallback rendering; add or update focused Rust coverage for serde and URL preservation. Use existing test configuration only, run focused suites with `vitest run`, then run the full required app/Rust gates. Commit test/final-gate work separately. Only after that stage passes may the quick receive `260718-fp9-SUMMARY.md` and STATE/ROADMAP tracking updates.
</post_uat_pending_checkpoint>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current sequence order → timeline presentation | Mutable top-to-bottom sequence order is projected into non-persisted Physics Paint ordinals. |
| Selected layer → parent FX layout | A launch click resolves the selected layer to the current parent sequence and current derived label. |
| TypeScript launch context → Rust/native window | Optional presentation metadata crosses validation, JSON/URL encoding, Rust serde, stored launch state, and emitted events. |
| Presentation label → persisted provenance | `workflowLabel` must remain separate from `sequenceName`, `layerName`, IDs, caches, and scripts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260718-FP9-01 | Tampering | `fxTrackLayouts` ordinal derivation | medium | mitigate | Derive ordinals only while iterating current sequence order, increment only for `physic-paint`, and preserve `sequenceName` for persistence/non-Physics-Paint presentation. |
| T-260718-FP9-02 | Tampering | Normal header and reorder ghost | medium | mitigate | Route both drawing paths through `getTimelineFxHeaderLabel()` so drag feedback cannot display a different identity. |
| T-260718-FP9-03 | Spoofing | Selected layer to standalone launch | medium | mitigate | Resolve the selected layer's parent sequence and exact current `FxTrackLayout` at click time before supplying `workflowLabel`. |
| T-260718-FP9-04 | Tampering | TypeScript/Rust launch transport | medium | mitigate | Validate and trim optional input, model it independently in both languages, percent-encode the native URL parameter, and preserve existing full-context serialization. |
| T-260718-FP9-05 | Information Disclosure | `layerName` and script provenance | medium | mitigate | Never replace `layerName` or persisted identifiers with the presentation ordinal; transport `workflowLabel` as a separate optional field. |
| T-260718-FP9-06 | Repudiation | Renewed native visual behavior | low | mitigate | Require explicit UAT across multi-track numbering, reorder, deletion, selected launches, and unchanged Roto controls before tests or completion claims. |
| T-260718-FP9-07 | Denial of Service | Missing/direct launch metadata | low | accept | The standalone renders the safe non-ordinal `PPaint` fallback rather than failing or claiming an incorrect track number. |
| T-260718-FP9-SC | Tampering | Package supply chain | low | accept | No package or dependency installation is planned. |
</threat_model>

<verification>
1. Run the inline source-contract command before edits and observe the expected red result against the fixed-label implementation.
2. After edits, the same command proves the presentation field, order counter, renderer/ghost consumption, selected launch wiring, TypeScript/Rust transport, Studio prop, fallback, and absence of fixed numbered source labels.
3. `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`, Rust `cargo check`, and `git diff --check` all exit 0; no server or test command runs.
4. The new HEAD commit directly follows `9c491f26` and contains exactly the ten production files declared by Task 1.
5. Execution halts at renewed native UAT; tests, SUMMARY, STATE, ROADMAP, and completion claims remain blocked.
</verification>

<source_audit>
| SOURCE | ID | Feature/Requirement | Task/Checkpoint | Status | Notes |
|--------|----|---------------------|-----------------|--------|-------|
| GOAL | QUICK-260718-FP9 | Correct the failed visible labeling behavior for multiple Physics Paint tracks and the standalone | Task 1 + Task 2 | COVERED | Ordered presentation metadata is propagated end to end and verified natively. |
| REQ | QUICK-260718-FP9 | Revise the existing quick plan after failed UAT without creating another quick | Existing PLAN + Task 1 + Task 2 | COVERED | The same quick directory and plan are reused. |
| RESEARCH | Root cause | Timeline helper and standalone source fixed every Physics Paint label to the first ordinal | Task 1 | COVERED | Both fixed derivations are removed. |
| RESEARCH | Ordering seam | `fxTrackLayouts` already follows current `sequenceStore.sequences` order | Task 1 | COVERED | Ordinals are derived inside that projection. |
| RESEARCH | Identity seam | Physics Paint sequences intentionally persist the same name and `layerName` carries provenance | Task 1 | COVERED | Presentation is added separately; persisted identity is untouched. |
| CONTEXT | D-01 | Number Physics Paint tracks by current top-to-bottom order and renumber after reorder/delete | Task 1 + Task 2 | COVERED | Counter-based projection plus native reorder/delete UAT. |
| CONTEXT | D-02 | Add presentation-only `FxTrackLayout.headerLabel`; non-Physics-Paint labels remain `sequenceName` | Task 1 | COVERED | Required projection field with explicit fallback assignment. |
| CONTEXT | D-03 | Normal header and reorder ghost consume the same derived label | Task 1 + Task 2 | COVERED | Shared renderer helper and drag UAT. |
| CONTEXT | D-04 | Resolve selected layer's current parent FX label when launching | Task 1 + Task 2 | COVERED | Event-time parent/layout resolution and per-track launch UAT. |
| CONTEXT | D-05 | Add, validate, and preserve optional `workflowLabel` through TypeScript and Tauri | Task 1 | COVERED | Request, context, parser, Rust serde, URL, and existing full-context paths are covered. |
| CONTEXT | D-06 | Standalone renders the selected supplied label with a safe incomplete-context fallback | Task 1 + Task 2 | COVERED | Studio prop wiring and non-ordinal fallback. |
| CONTEXT | D-07 | Preserve names, IDs, Roto terminology, caches, scripts, project data, and provenance | Task 1 + Task 2 | COVERED | Explicit no-touch boundary and unchanged-behavior UAT. |
| CONTEXT | D-08 | No test changes or execution before renewed native UAT approval | Task 1 + Task 2 + pending checkpoint | COVERED | Production-only gates; tests are approval-blocked. |
| CONTEXT | D-09 | Create one new atomic code commit after `9c491f26` without amending | Task 1 + Task 2 automated gate | COVERED | Exact parent and committed-file set are checked. |
| CONTEXT | D-10 | Stop for renewed UAT; no SUMMARY or STATE/ROADMAP update before approval | Task 2 + pending checkpoint | COVERED | Closure artifacts remain blocked. |
</source_audit>

<success_criteria>
- Physics Paint tracks present sequential `PPaint #n` labels from their current top-to-bottom order, excluding other FX types from the counter.
- Reordering or deletion immediately renumbers the remaining presentation labels, and the reorder ghost matches the normal header.
- Launching the standalone from a selected Physics Paint layer displays that track's current derived label.
- Missing optional launch metadata uses the non-ordinal `PPaint` fallback and never fabricates the first ordinal.
- Persisted sequence/layer names, `layerName`, IDs, Roto terminology, cache/script/project contracts, and provenance remain unchanged.
- Production-only source, TypeScript, Rust, and diff gates pass; one new atomic ten-file code commit follows `9c491f26`.
- Execution stops as automated-ready and native-UAT pending, with tests and completion tracking still explicitly deferred.
</success_criteria>

<output>
After Task 1, stop at Task 2 and report the absolute plan path plus the new atomic code commit hash as renewed-native-UAT pending. Per D-08/D-10, do not create `260718-fp9-SUMMARY.md`, update STATE/ROADMAP, create or modify tests, run tests, start a server, or claim completion before explicit approval. The post-UAT pending checkpoint records the separately authorized regression and closure stage.
</output>
