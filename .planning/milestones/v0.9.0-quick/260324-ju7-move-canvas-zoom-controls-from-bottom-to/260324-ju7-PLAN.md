---
phase: quick
plan: 260324-ju7
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/layout/CanvasArea.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Zoom controls (minus, percentage, plus, fit, fullscreen) appear at the top of the canvas area"
    - "Zoom controls no longer appear at the bottom of the canvas area"
    - "All zoom interactions still work (zoom in, zoom out, fit toggle, fullscreen)"
  artifacts:
    - path: "Application/src/components/layout/CanvasArea.tsx"
      provides: "Canvas area with top-positioned zoom controls"
  key_links:
    - from: "CanvasArea zoom controls"
      to: "canvasStore"
      via: "zoomIn/zoomOut/toggleFitLock/fitLocked signals"
      pattern: "canvasStore\\.(zoomIn|zoomOut|toggleFitLock)"
---

<objective>
Move the canvas zoom controls bar from the bottom of the canvas area to the top.

Purpose: Improve UX by placing zoom controls at the top of the canvas where they are more accessible and consistent with the user's preferred layout.
Output: Updated CanvasArea.tsx with zoom controls positioned at top.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/layout/CanvasArea.tsx
@Application/src/stores/canvasStore.ts
</context>

<interfaces>
<!-- From canvasStore.ts — signals and methods used by zoom controls -->
```typescript
export const canvasStore = {
  zoom: Signal<number>;
  fitLocked: Signal<boolean>;
  zoomPercent: ReadonlySignal<number>;
  isAtMinZoom: ReadonlySignal<boolean>;
  isAtMaxZoom: ReadonlySignal<boolean>;
  zoomIn(): void;
  zoomOut(): void;
  toggleFitLock(): void;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move zoom controls from bottom to top of CanvasArea</name>
  <files>Application/src/components/layout/CanvasArea.tsx</files>
  <action>
In `CanvasArea.tsx`, the component returns a flex-col div containing:
1. Canvas container div (lines 257-284) — the preview with zoom/pan
2. SpeedBadge (line 286)
3. Zoom controls div (lines 288-337) — the bar with minus, percentage, plus, fit, fullscreen

Move the zoom controls div (the `div` with class `flex items-center justify-center gap-3 w-full h-[42px] px-5 shrink-0`) so it renders BEFORE the canvas container div, not after it. This means the JSX order inside the outer wrapper becomes:

1. Zoom controls div (moved to top)
2. Canvas container div (the preview area)
3. SpeedBadge
4. FullSpeedBadge (move outside the old zoom controls div since we're extracting it)

Specifically:
- Cut the entire zoom controls block (from `{/* Preview Controls */}` through the closing `</div>` of that section, lines 287-337).
- Paste it immediately after the opening outer div (after line 255, before the `{/* Preview Frame with zoom/pan */}` comment).
- Keep the `FullSpeedBadge` component: it was inside the zoom controls div at the bottom. Keep it inside the controls div at its new top position — that is fine since it only shows conditionally.
- Remove the `SpeedBadge` from between canvas and controls — move it to render just above or below the canvas container (it's an overlay badge, keep it after the canvas container).

The outer wrapper already uses `flex flex-col` so moving the div to the top of the children list will position it at the top of the canvas area.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Zoom controls bar renders at the top of the canvas area instead of the bottom. TypeScript compiles without errors. All zoom buttons, percentage display, fit toggle, and fullscreen button remain functional with their existing onClick handlers and store bindings intact.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- Visual: zoom controls bar (minus, percentage, plus, fit, fullscreen) appears at the top of the canvas area
- All buttons retain their existing functionality (no logic changes, only position change)
</verification>

<success_criteria>
- Zoom controls visually positioned at top of canvas area
- No TypeScript compilation errors
- All existing zoom/fit/fullscreen interactions work identically
</success_criteria>

<output>
After completion, create `.planning/quick/260324-ju7-move-canvas-zoom-controls-from-bottom-to/260324-ju7-SUMMARY.md`
</output>
