---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 07
subsystem: history
tags: [undo-redo, physical-history, rail-set, batch-operations, tdd]

requires:
  - phase: 43.6-multi-rail-selection-and-batch-operations (plans 02/04/05)
    provides: move-rails / spacing-on-set resolver intents, delete-rails shared pure proposer, D-06 rail-set snapshot side-channel
  - phase: 43.5-timeline-toolbox-and-directional-push-tools
    provides: push-rails history allowlist precedent (8751a903)
provides:
  - delete-rails / move-rails / spacing-on-set recorded as ordinary undoable/redoable history commands
  - regression proof that one batch acceptance appends exactly one applied command and undo/redo replay the exact before/after document snapshots
affects: [43.6-multi-rail-selection-and-batch-operations, physical-history, rail-set-selection]

actuals:
  tokens: 4000
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Batch rail-set kinds join the ordinary history allowlist by explicit whitelist extension only (8751a903 precedent) — no replay-branch or resolver changes"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts

key-decisions:
  - "43.6-07: The three batch kinds are recorded through the existing ordinary branch unchanged — allowlist membership only, following the 8751a903 push-rails precedent; the replay branch, snapshot equality, and the D-06 resolver are byte-untouched"
  - "43.6-07: Regression fixtures compute before/after through the REAL pure paths (proposePhysicPaintRotoDeleteRails parsed document, resolvePhysicPaintRotoPhysicalEdit mapping) so the history round-trips exactly what each coordinator publishes"

patterns-established:
  - "Batch history regression shape: one describe block, three focused its, shared push-style replay harness + round-trip assertion helper over real pure-path fixtures"

requirements-completed: [RSET-03, BDEL-02]

coverage:
  - id: D1
    description: "One accepted batch operation (Delete Rails / Move Rails / Key Spacing on a set) appends exactly one applied history command with availability {undo: 1, redo: 0}; undo replays the exact pre-operation snapshot and redo reapplies the exact post-operation snapshot"
    requirement: RSET-03
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts#useRotoPhysicalEditHistory batch operations on a rail set (43.6 gap closure)"
        status: pass
      - kind: integration
        ref: "pnpm --dir app exec vitest run src/components/physic-paint (85 files, 1881 tests)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The D-06 rail-set snapshot side-channel is consumed by the created history command: undo restores the recorded before set and redo restores the recorded after set"
    requirement: BDEL-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts#useRotoPhysicalEditHistory batch operations on a rail set (43.6 gap closure)"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 07: Batch Operation History Recording Summary

**Delete Rails, Move Rails, and Key Spacing on a rail set now record one undoable/redoable physical history command each — the three batch kinds join the ordinary allowlist, closing the silent-drop root cause behind G-43.6-3/4/6/8 (undo/redo part)**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-19T07:31:31Z
- **Completed:** 2026-08-19T07:36:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Regression tests prove one accepted batch operation (delete-rails on a mixed Key Rail + Group Rail set, move-rails on two Key Rails, spacing-on-set on two Key Rails) appends exactly one applied history command and that undo/redo replay the exact before/after document snapshots computed by the REAL pure paths (shared delete-rails proposer, resolver intents)
- `isOrdinaryOperationKind` extended with `move-rails`, `spacing-on-set`, and `delete-rails` following the 8751a903 push-rails precedent — allowlist membership only; the replay branch, snapshot equality, and D-06 resolver untouched
- Full physic-paint regression sweep green: 85 files, 1881 tests, including the unchanged coordinator transport suite

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — regression test that each batch kind records one command and replays both directions** - `1fc9dfb8` (test)
2. **Task 2: GREEN — add the three batch kinds to isOrdinaryOperationKind** - `e1259f92` (fix)

**Plan metadata:** recorded in the final docs commit

## TDD Gate Compliance

Gate sequence verified in git log: RED `test(43.6-07)` commit `1fc9dfb8` (three tests failing with availability stuck at {undo: 0, redo: 0}) followed by GREEN `fix(43.6-07)` commit `e1259f92` (all three tests passing). No refactor commit needed — the GREEN change is a three-line allowlist extension.

## Files Created/Modified
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts` — new describe block 'useRotoPhysicalEditHistory batch operations on a rail set (43.6 gap closure)' with shared replay harness, round-trip assertion helper, and three focused batch tests
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` — `isOrdinaryOperationKind` allowlist extended with the three batch kinds

## Decisions Made
- The fix is allowlist membership only, exactly per the diagnosed root cause and the 8751a903 precedent — no changes to the replay branch, identity/dedup guards, or the D-06 rail-set snapshot side-channel
- Delete-rails fixture builds a TRUE mixed set (Key Rail segment [F,G] + Group Rail group-sibling, with Group Rail group-main surviving) through the shared pure proposer `proposePhysicPaintRotoDeleteRails`; move-rails and spacing-on-set fixtures resolve the REAL resolver intents and derive after-state from `proposal.mapping`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Cmd+Z / Cmd+Shift+Z now have a recorded command to replay for all three batch operations; the D-06 rail-set snapshot side-channel is live (keyed by the original command operationId via history provenance)
- Closes G-43.6-3 (undo/redo part), G-43.6-4 (undo/redo part), G-43.6-6, G-43.6-8 (undo/redo part); the remaining "only the last rail is deleted" half of G-43.6-3 is the separate selection-collapse/classifier defect owned by another gap-closure plan
- Native UAT of the batch undo/redo path remains phase-level verification scope

## Self-Check: PASSED

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*
