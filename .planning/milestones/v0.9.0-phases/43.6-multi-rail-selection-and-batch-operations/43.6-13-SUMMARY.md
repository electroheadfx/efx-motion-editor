---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 13
subsystem: physic-paint
tags: [preact, signals, vitest, tdd, lease, concurrency, roto]

# Dependency graph
requires:
  - phase: 43.6-04
    provides: delete-rails execute input and deferred publication/lease settlement path
provides:
  - recovery-lease self-heal at the top of executePhysicalEdit (G-43.6-2 secondary defect closed)
  - coordinator regression test pinning the self-heal for an accepted-then-failed Group publication
affects: [43.6 UAT G-43.6-3 re-run, BDEL-01, BDEL-02]

# Actuals (#2632)
actuals:
  tokens: 733
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Best-effort recovery-lease release wired at the single production caller; store token validation remains the concurrency authority"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts

key-decisions:
  - "The self-heal calls releasePhysicalEditRecoveryLease() best-effort BEFORE the serialize guard; the release routes through ports.lease.release(recoveryToken) so a parent refusal (stale token) still leaves recoveryLeaseSignal set and the guard keeps blocking."
  - "recoveryLeaseSignal.peek() stays in the guard; in-flight/pending/settled branches are byte-identical — only a successfully-released stale token lets the edit proceed."

patterns-established:
  - "Orphaned-release wiring: when a lease/release primitive has no production caller, wire it at the guarded entry point as a best-effort self-heal rather than weakening the guard."

requirements-completed: [BDEL-01, BDEL-02]

coverage:
  - id: D1
    description: "A recovery lease left by an accepted-then-failed Group publication self-heals on the next executePhysicalEdit — the second edit proceeds, releaseLease fires with the recovery token, recoveryLease clears"
    requirement: BDEL-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts#self-heals a recovery lease left by a deferred Group publication failure so the next edit proceeds (G-43.6-2)"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts (full suite: 59 passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Native UAT re-run for G-43.6-3 (the blocked-delete report that motivated this closure) confirms Delete/Backspace routing is live after a failed publication"
    requirement: BDEL-02
    verification: []
    human_judgment: true
    rationale: "Per plan success criteria, the native UAT re-run for G-43.6-3 remains human-run; do not mark that UAT item resolved at code-green time."

# Metrics
duration: 4min
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 13: Recovery-Lease Self-Heal (G-43.6-2 Secondary Defect) Summary

**Wired the orphaned releasePhysicalEditRecoveryLease as a best-effort self-heal at the top of executePhysicalEdit, so a stale recovery lease from a failed deferred publication never blocks every later edit until app relaunch.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-08-19T13:39:50Z
- **Completed:** 2026-08-19T13:43:37Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- G-43.6-2 SECONDARY defect closed: executePhysicalEdit now best-effort releases any stale recovery lease before the serialize guard, restoring edit availability (mutationLocked clears) without a relaunch.
- TDD RED/GREEN: failing regression test first (second executeGroupPaint blocked with PHYSICAL_EDIT_SERIALIZE_MESSAGE), then the one-statement fix turned it green.
- Concurrency authority preserved: recoveryLeaseSignal.peek() stays in the guard; a parent refusal to release a stale token still blocks; in-flight/pending/settled branches byte-identical.

## Task Commits

Each task was committed atomically:

1. **Task 1 (tracer, RED): coordinator regression test** - `286c6e01` (test)
2. **Task 2 (GREEN): best-effort recovery-lease self-heal in executePhysicalEdit** - `4452d1e2` (fix)

_Note: TDD flow — test commit (RED) then implementation commit (GREEN). No refactor commit needed._

## Files Created/Modified
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` — one comment + one `releasePhysicalEditRecoveryLease();` statement inserted at the top of executePhysicalEdit (before the serialize guard), plus the callback added to the useCallback deps array.
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` — one new it block: 'self-heals a recovery lease left by a deferred Group publication failure so the next edit proceeds (G-43.6-2)'.

## Decisions Made
- Self-heal placement: top of executePhysicalEdit (the ONLY production caller of releasePhysicalEditRecoveryLease), before the guard — per plan. Release goes through ports.lease.release(recoveryToken) so parent authority (validateRotoPhysicalOperationLease / owner-agnostic delete-by-scope) is never bypassed.
- No changes to the store lease registry, shared lease acquisition, transfer-to-recovery path, bridge, Studio, or history — the defect was the orphaned caller, not the lease semantics.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. RED failed exactly as predicted (second executeGroupPaint returned false via the serialize guard); GREEN passed the full 59-test coordinator suite and tsc --noEmit exits 0.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 43.6 plan set complete (13/13 plans summarized). G-43.6-2 secondary defect closed at code level.
- Remaining: the native UAT re-run for G-43.6-3 (which reported the blocked delete) is human-run per 43.6-UAT.md — do not mark that UAT item resolved from this code-green.
- Plan 43.6-12 (primary G-43.6-2 defect, delete-rails before-snapshot) completed immediately before this plan; together they close both G-43.6-2 defects.

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*

## Self-Check: PASSED

- FOUND: .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-13-SUMMARY.md
- FOUND: 286c6e01 (RED test commit)
- FOUND: 4452d1e2 (GREEN fix commit)
