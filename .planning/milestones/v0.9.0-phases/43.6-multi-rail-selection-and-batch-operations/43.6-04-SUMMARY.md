---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 04
subsystem: ui
tags: [preact, signals, roto, rail-selection, batch-operations, delete-rails, classifier, parent-authority, undo-redo]

# Dependency graph
requires:
  - phase: 43.6-01
    provides: multi-rail selection SET session scope, canonical cross-type ordering, D-06 aftermath carrier (recordRailSetSnapshot + resolveRailSetPostAcceptance with the 'delete-rails' -> null branch)
  - phase: 43.6-02
    provides: the 'move-rails' resolver intent branch and transport union precedent for the new 'delete-rails' semantic-delta kind
  - phase: 43.6-03
    provides: the D-06 set aftermath wiring in the Studio (recordRailSetSnapshot + resolveRailSetPostAcceptance on acceptance/undo/redo) and the one-mapper copy discipline
provides:
  - proposePhysicPaintRotoDeleteRails — ONE shared pure proposer composing per-Group 43.2 delete semantics + Key Rail record removal + 43.4 break normalization, consumed by BOTH the child coordinator and the parent bridge recompute (T-43.6-01 exact-match link)
  - 'delete-rails' operation-kind + semantic-delta kind `{ kind: 'delete-rails'; members; cleanupKeyIds; previousRevision; nextRevision }` with strict parsers, coordinator RotoRailSetDeleteExecuteInput (no child intent), and bridge parent recompute + exact-match validation with distinct error strings
  - classifyRotoDeleteTarget 'rail-set' branch evaluated FIRST (before the loop/key-rail branches): active non-empty set -> { kind: 'rail-set', members, firstFrame, lastFrame } validated against current segments + loopClips; stale set fails closed on the existing stale mapping
  - deleteRotoFrame 'rail-set' dispatch through the new executeRailSetDelete port (no runPhysicalAction, no confirmation modal at any set size — D-23) with the locked accepted copy via the one mapper
  - D-06 delete aftermath: selection empties (set -> null, single-rail signals collapse to the accepted key), cursor stays on its current valid frame, snapshot records an EMPTY after set so Undo restores the exact pre-delete set and Redo clears it again
affects: [43.6-05, 43.6-06]

# Actuals — pairs with the plan's `estimate` (80000 tokens) to calibrate future estimates.
actuals:
  tokens: 24187    # chars/4 over the realized diff (96747 chars, 42aca0c4~1..64e8671f)
  tasks: 3         # tasks completed
  commits: 6       # commits made (3 RED + 3 GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "ONE shared pure proposer for child proposal AND parent recompute: proposePhysicPaintRotoDeleteRails composes per-Group 43.2 semantics + Key Rail record removal + break normalization; the bridge recomputes from its own document authority and exact-matches every complete-state field with distinct error strings (43.2 parent-authority pattern, T-43.6-01)"
    - "Specialized coordinator execute input with NO child intent: RotoRailSetDeleteExecuteInput { operationKind, expectedLaunch, members } — the parent recomputes; the child proposes complete state (Group-lifecycle discipline)"
    - "One activation-time Delete classifier shared by keyboard and visible control (D-22): the 'rail-set' branch runs FIRST, before the loop/key-rail branches it supersedes; a stale set never classifies as deletable (T-43.6-02)"
    - "Direct no-modal dispatch (D-23): deleteRotoFrame's 'rail-set' branch calls the executeRailSetDelete port — no runPhysicalAction, no confirmation/warning call at any set size; the accepted copy publishes through the one mapper"
    - "D-06 delete aftermath: recordRailSetSnapshot with an EMPTY after set for 'delete-rails' (before = pre-delete set, after = null) so Undo restores the exact pre-delete set and Redo clears it again; undo/redo lookups use the ORIGINAL command id from historyProvenance"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoGroupParity.test.ts
    - app/src/types/physicPaint.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/lib/physicPaintBridge.ts
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts

key-decisions:
  - "The plan's named test twin physicsPaintRotoGroupLifecycle.test.ts does not exist; the actual twin is physicsPaintRotoGroupParity.test.ts — Task 1 tests live there"
  - "Stale rail-set mapping: a set with ANY stale member (key-rail firstKeyId matching no derived segment, or loop member resolving to no valid Group) classifies as the existing 'stale-key-rail' — the plan's 'existing stale/no-target mapping', fail-closed, never deletable"
  - "The rail-set target carries firstFrame/lastFrame (inclusive product range from canonical half-open intervals) so the accepted-copy mapper stays pure and deterministic — same pattern as the key-rail target's firstKeyFrame/lastKeyFrame"
  - "Copy strings follow the plan's verbatim acceptance criteria with ASCII hyphens ('Deleted 3 Rails - frames 12-88. The intervals stay intentional gaps.'); the UI-SPEC M4 table renders em/en dashes — the plan's verbatim strings are the execution contract for this plan"
  - "D-06 redo-clears requires the snapshot's after set to be NULL for 'delete-rails': the Studio records recordRailSetSnapshot(operationId, beforeSet, operationKind === 'delete-rails' ? null : beforeSet) — the Plan 03 wiring recorded before == after == current (move-stable), which would have restored the pre-delete set on redo"
  - "The hook publishes the accepted Delete Rails copy on success (plan: 'publish the locked accepted copy on success through the one mapper'), unlike the Group delete path where the Studio publishes; a port rejection reclassifies and publishes the mapped reason"

patterns-established:
  - "delete-rails transport chain: isPhysicPaintRotoPhysicalEditOperationKind runtime guard (the missing 'delete-rails' entry was the Task 2 debug root cause) -> semantic-delta parser -> coordinator specialized input -> bridge GROUP_LIFECYCLE_OPERATION_KINDS gate -> recompute via the shared proposer -> exact-match with distinct error strings, ALL wrapped with the APPLY_ERROR prefix"
  - "Classifier set branch: session RailSetIdentity[] (Plan 01 authority) validated against freshly derived keyRailSegments + loopClips; key-rail members gain their keyIds from the matched segment; the whole set fails closed on any single stale member"
  - "Direct delete dispatch: executeRailSetDelete port mirrors executeGroupLifecycleDelete (Studio ref + dispatchAndWaitForAcceptedRotoPhysicalEdit with expectedLaunch from launchContextRef); the hook publishes the accepted copy and reclassifies on rejection"

requirements-completed: [BDEL-01, BDEL-02, RSET-03]

# Coverage metadata — per-deliverable traceability for deterministic UAT routing.
coverage:
  - id: D1
    description: "proposePhysicPaintRotoDeleteRails pure proposer: validation loop (unknown-member/stale-member/duplicate-member/malformed-member), deterministic order (placementStart asc, loopId asc), sequential composition over the accumulating document (loop -> proposePhysicPaintRotoDeleteGroup; key-rail -> applyKeyRailDeletion with mid-composition segment re-derivation and 'stale-member' fail-closed), impact { kind: 'delete-rails', members, cleanupKeyIds, previousRevision, nextRevision }"
    requirement: BDEL-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoGroupParity.test.ts (43.6-04 Task 1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "'delete-rails' transport: operation-kind union + runtime guard, semantic-delta union with strict parsers (bounded ids, discriminant, non-empty, unique rails, valid revision transition), coordinator RotoRailSetDeleteExecuteInput with no child intent, semanticDeltaEquals delete-rails branch, bridge recompute via the shared proposer + exact-match with distinct error strings"
    requirement: BDEL-02
    verification:
      - kind: unit
        ref: "app/src/lib/physicPaintBridge.test.ts + app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts (43.6-04 Task 2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "classifyRotoDeleteTarget 'rail-set' branch FIRST: active mixed set classifies as { kind: 'rail-set', members, firstFrame, lastFrame } before any single-rail branch (even with selectedLoopClipIds populated); stale key-rail member and stale loop member both fail closed as 'stale-key-rail' — never deletable"
    requirement: BDEL-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#classifies an active mixed rail set as Delete Rails before any single-rail branch (43.6-04 Task 3)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Locked copy verbatim: scope labels 'Delete 3 Rails' / 'Delete 1 Rail' (accessible name + guarded tooltip heading only; visible label stays 'Delete'), accepted 'Deleted 3 Rails - frames 12-88. The intervals stay intentional gaps.' and 'Deleted 1 Rail - frames 12-40. The interval stays an intentional gap.' via the one mapper"
    requirement: BDEL-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#dispatches Delete Rails directly through the executeRailSetDelete port with the locked acceptance copy (43.6-04 Task 3)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Direct no-modal dispatch: deleteRotoFrame 'rail-set' branch calls executeRailSetDelete with the classified members — no runPhysicalAction, no confirmation/warning call at any set size; rejection publishes the mapped reason and mutates nothing (no success status, no history entry)"
    requirement: BDEL-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#rejects a stale rail set before mutation or success publication (43.6-04 Task 3)"
        status: pass
    human_judgment: false
  - id: D6
    description: "D-06 delete aftermath: accepted 'delete-rails' empties the set (resolveRailSetPostAcceptance -> null) and single-rail signals collapse to the accepted key (cursor stays on its current valid frame); the snapshot records before = pre-delete set, after = null so Undo restores the exact pre-delete set and Redo clears it again"
    requirement: RSET-03
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts#records delete-rails with an empty after set so redo clears it again (D-06) (43.6-04 Task 3)"
        status: pass
    human_judgment: false

# Metrics
duration: 3h 0m
completed: 2026-08-18
status: complete
---

# Phase 43.6 Plan 04: Delete Rails Summary

**Delete Rails end-to-end: one atomic, direct (no-modal), mixed-set deletion with per-type semantics — ONE shared pure proposer composing 43.2 Group delete + Key Rail record removal + 43.4 break normalization, parent-side recompute-and-exact-match validation with distinct error strings, one activation-time classifier shared by keyboard and the visible control, and complete Undo/Redo restoration of the document AND the exact pre-delete selection set**

## Performance

- **Duration:** 3h 0m
- **Started:** 2026-08-18T17:30:00Z
- **Completed:** 2026-08-18T20:30:00Z
- **Tasks:** 3
- **Files created:** 0
- **Files modified:** 11

## Accomplishments

- `proposePhysicPaintRotoDeleteRails` (Task 1, tracer) — ONE shared pure proposer in physicsPaintRotoGroupLifecycle.ts: a validation loop (unknown-member / stale-member / duplicate-member / malformed-member), deterministic ordering (placementStart asc, then loopId asc), and sequential composition over the accumulating document state — loop members delegate to the 43.2 `proposePhysicPaintRotoDeleteGroup` authority (never forked), key-rail members run `applyKeyRailDeletion` which re-derives segments against the accumulating document and fails closed with 'stale-member' on mid-composition staleness. The impact carries `{ kind: 'delete-rails', members, cleanupKeyIds, previousRevision, nextRevision }`. The plan's named test twin `physicsPaintRotoGroupLifecycle.test.ts` does not exist; the actual twin is `physicsPaintRotoGroupParity.test.ts`.
- 'delete-rails' transport (Task 2, auto) — the operation-kind union + the missing runtime guard entry in `isPhysicPaintRotoPhysicalEditOperationKind` (the Task 2 debug root cause), the semantic-delta kind with strict parsers (bounded ids, discriminant, non-empty members, unique rails, valid revision transition), the coordinator's specialized `RotoRailSetDeleteExecuteInput` with NO child intent (parent recomputes; child proposes complete state), `semanticDeltaEquals` delete-rails branch, and the bridge's GROUP_LIFECYCLE_OPERATION_KINDS gate + recompute branch calling the shared proposer against the parent's own document authority, then exact-matching impact + records + groupOverrideRecords + loopClips + breaks + interpolation + selectedKeyId + cursorAppFrame + revision with distinct error strings — every failure wrapped with the APPLY_ERROR prefix, zero accepted-state settlement on divergence (43.2 carryover).
- Classifier rail-set branch (Task 3, auto) — `classifyRotoDeleteTarget` gains the 'rail-set' branch evaluated FIRST, before the loop/key-rail branches it supersedes: an active non-empty set (session `RailSetIdentity[]`, Plan 01 authority) validates every member against the current derivation — key-rail members must match exactly one derived segment (firstKeyId + ordered keyIds), loop members must resolve to a valid Group (phaseOrigin + visibleRanges) — and yields `{ kind: 'rail-set', members, firstFrame, lastFrame }`; ANY stale member fails the whole set closed on the existing 'stale-key-rail' mapping (never delete on stale authority, T-43.6-02).
- Direct dispatch (Task 3) — `deleteRotoFrame`'s 'rail-set' branch calls the new `executeRailSetDelete` input port with the classified members: no runPhysicalAction, no confirmation modal at any set size (D-23); the accepted copy publishes through the one mapper; a port rejection reclassifies and publishes the mapped reason (never a success status on a rejected path). The Studio wires the port through the coordinator (`RotoRailSetDeleteExecuteInput` with expectedLaunch from launchContextRef, mirroring `groupLifecycleDeleteExecuteRef`), and the strip's existing `deleteScopeLabel` routing carries 'Delete {N} Rails' / 'Delete 1 Rail' into the accessible name and guarded tooltip heading only — the visible label stays 'Delete'.
- D-06 delete aftermath (Task 3) — the Plan 01/03 aftermath carrier is reused: accepted 'delete-rails' empties the set (`resolveRailSetPostAcceptance` -> null) and the single-rail signals collapse to the accepted key (cursor stays on its current valid frame); the snapshot records before = the exact pre-delete set and after = NULL (the Plan 03 move-stable before == after recording would have restored the pre-delete set on redo — corrected so Redo clears it again); undo/redo lookups use the ORIGINAL command id from `historyProvenance.historyCommandId`.
- Full verification: 2214 tests green across src/components/physic-paint + src/lib (111 files), typecheck green, existing Delete Frame / single-rail / group delete tests unchanged and green (43.2/43.4 regression boundary).

## Task Commits

Each task was committed atomically (TDD RED/GREEN):

1. **Task 1 (tracer tdd): proposePhysicPaintRotoDeleteRails pure proposer**
   - `42aca0c4` (test: add failing delete-rails proposer tests)
   - `158e5c27` (feat: implement proposePhysicPaintRotoDeleteRails)
2. **Task 2 (auto tdd): 'delete-rails' transport, coordinator authorization, and bridge parent validation**
   - `826d78e5` (test: add failing delete-rails transport, coordinator, and bridge tests)
   - `5bf51ebb` (feat: wire delete-rails transport, coordinator, and bridge validation)
3. **Task 3 (auto tdd): Classifier rail-set branch + one-path Delete dispatch + locked copy + D-06 empty/restore aftermath**
   - `c6b03911` (test: rail-set Delete classifier, dispatch, and copy tests)
   - `64e8671f` (feat: rail-set Delete classifier branch, direct dispatch, and Studio port)

**Plan metadata:** `docs(43.6-04): complete plan` (final docs commit)

## Files Created/Modified

- `app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts` - `proposePhysicPaintRotoDeleteRails` (L877-967): validation loop, deterministic ordering, sequential composition (loop -> proposePhysicPaintRotoDeleteGroup; key-rail -> applyKeyRailDeletion with mid-composition segment re-derivation), impact with members/cleanupKeyIds/revisions
- `app/src/components/physic-paint/roto/physicsPaintRotoGroupParity.test.ts` - Task 1 proposer tests (the plan's named twin physicsPaintRotoGroupLifecycle.test.ts does not exist)
- `app/src/types/physicPaint.ts` - 'delete-rails' in the operation-kind union AND the runtime guard `isPhysicPaintRotoPhysicalEditOperationKind` (the missing guard entry was the Task 2 debug root cause), `RailSetDeleteMember` type, semantic-delta variant with strict parser, `operationSemanticDeltaIsValid` inclusion
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` - `RotoRailSetDeleteExecuteInput` (no child intent), `RotoRailSetDeleteImpact`, executePhysicalEdit isRailSetDelete branch with proposal staging via the shared proposer, `railSetDeleteMembersEqual`, staged-state branches, clearPendingOnce lease release, settlement parking
- `app/src/lib/physicPaintBridge.ts` - 'delete-rails' in GROUP_LIFECYCLE_OPERATION_KINDS, recompute branch via proposePhysicPaintRotoDeleteRails, exact-match with distinct error strings, operationLabel-based error copy
- `app/src/lib/physicPaintBridge.test.ts` - lifecycleHarness delete-rails cases: exact-match acceptance + divergent members/cleanupKeyIds/records/loopClips/breaks/selection/cursor/revision rejections with distinct error strings, empty-members parser rejection, stale-member rejection
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` - "accepts Delete Rails" coordinator case (groupLifecycleDocument sharedSourceOwner fixture, releaseLease 1)
- `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` - 'rail-set' RotoDeleteTarget variant, `deriveRailSetDeleteMembers` helper, classifier branch FIRST, `buildRotoDeleteScopeLabel` set forms, `buildDeleteRailSetSuccessMessage`, mapRotoDeleteProductReason 'rail-set' -> null, `getRailSetMembers` + `executeRailSetDelete` input ports, deleteRotoFrame 'rail-set' dispatch
- `app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts` - 5 new tests: mixed-set classification before single-rail branches, stale key-rail member fail-closed, stale loop member fail-closed, direct dispatch + locked accepted copy (3 Rails), singular copy (1 Rail), stale rejection before mutation
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - `railSetDeleteExecuteRef` + port construction (RotoRailSetDeleteExecuteInput with expectedLaunch), `getRailSetMembers` + `executeRailSetDelete` input wiring, delete-rails snapshot recording with empty after set (redo clears)
- `app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts` - delete-rails recording contract test (before = set, after = null; undo restores, redo clears)

## Decisions Made

- **ONE shared pure proposer (Research Open Question 1):** the child coordinator AND the parent bridge recompute call the SAME `proposePhysicPaintRotoDeleteRails` — the exact-match link whose breakage would let a forged/divergent delete commit (T-43.6-01). The 43.2 Group lifecycle authority is reused, never forked.
- **Stale set mapping:** a set with ANY stale member classifies as the existing 'stale-key-rail' (the plan's "existing stale/no-target mapping") — fail-closed, never deletable, no new copy invented.
- **Target carries the product range:** `{ kind: 'rail-set', members, firstFrame, lastFrame }` — the inclusive range derives from canonical half-open intervals at classification time so the accepted-copy mapper stays pure (same pattern as the key-rail target's frame facts).
- **Copy strings:** the plan's verbatim acceptance criteria (ASCII hyphens) is the execution contract; the UI-SPEC M4 table renders em/en dashes — noted for phase-level reconciliation.
- **Redo-clears requires after = null:** the Plan 03 wiring recorded before == after == current (move-stable); for 'delete-rails' the Studio records after = null so Redo clears the set again (D-06).
- **Hook publishes the accepted copy:** unlike the Group delete path (Studio publishes), the plan directs the hook to publish the locked Delete Rails copy on success through the one mapper; a port rejection reclassifies and publishes the mapped reason.

## Deviations from Plan

### Auto-fixed Issues

**1. [Test twin] The plan's named test file does not exist**
- **Found during:** Task 1 RED
- **Issue:** The plan names `physicsPaintRotoGroupLifecycle.test.ts` as the Task 1 test twin; the file does not exist — the actual twin is `physicsPaintRotoGroupParity.test.ts`.
- **Fix:** Task 1 tests live in `physicsPaintRotoGroupParity.test.ts`.
- **Files modified:** physicsPaintRotoGroupParity.test.ts
- **Verification:** full suite green.
- **Committed in:** 42aca0c4 (Task 1 RED commit)

**2. [Runtime guard] Missing 'delete-rails' in isPhysicPaintRotoPhysicalEditOperationKind**
- **Found during:** Task 2 GREEN (bridge test debugging)
- **Issue:** The type union included 'delete-rails' but the runtime guard did not — the bridge rejected every valid delete-rails payload with 'Invalid physics paint apply payload'.
- **Fix:** Added `|| value === 'delete-rails'` to the runtime guard (before the undo/redo lines).
- **Files modified:** app/src/types/physicPaint.ts
- **Verification:** bridge delete-rails acceptance tests green.
- **Committed in:** 5bf51ebb (Task 2 GREEN commit)

**3. [Test fixture] Coordinator cleanupKeyIds mismatch**
- **Found during:** Task 2 GREEN
- **Issue:** The default fixture has only group-1 (owns A,B); deleting it removes A,B as cleanup keys, so the test expected [] but the proposal produced ['A','B'].
- **Fix:** Switched the fixture to `groupLifecycleDocument({ sharedSourceOwner: true })` so group-shared still references A/B.
- **Files modified:** useRotoPhysicalEditCoordinator.test.ts
- **Verification:** coordinator delete-rails acceptance test green.
- **Committed in:** 5bf51ebb (Task 2 GREEN commit)

**4. [Test fixture] Divergent-variant no-ops in the bridge tests**
- **Found during:** Task 2 GREEN
- **Issue:** Three rejection variants were no-ops: 'divergent records' (proposal records = [] so mapping was a no-op and apply ACCEPTED), 'divergent loopClips' (proposal loopClips = [] so the synthetic clip was dropped), 'divergent breaks' (the removed key created an orphan-break fingerprint rejection instead of the impact mismatch).
- **Fix:** Added `makePhysicalRecord('extra', 29)` with kind stripped for records; a synthetic group-extra clip for loopClips; changed the harness members to only `[{ kind: 'key-rail', firstKeyId: 'ordinary', keyIds: ['ordinary'] }]` so the proposal keeps source-A/source-B for the breaks variant.
- **Files modified:** physicPaintBridge.test.ts
- **Verification:** all divergent variants reject with the distinct error strings.
- **Committed in:** 5bf51ebb (Task 2 GREEN commit)

**5. [Typecheck] railSetDeleteMembersEqual narrowing**
- **Found during:** Task 2 GREEN
- **Issue:** TypeScript does not narrow `other` from `member.kind !== other.kind`.
- **Fix:** Explicit `other.kind === 'loop'` / `other.kind === 'key-rail'` checks.
- **Files modified:** useRotoPhysicalEditCoordinator.ts
- **Verification:** typecheck green.
- **Committed in:** 5bf51ebb (Task 2 GREEN commit)

**6. [Typecheck] Test casts**
- **Found during:** Task 2 GREEN
- **Issue:** `mismatchDelta` unknown and the bridge test payload cast needed explicit types.
- **Fix:** Cast to `PhysicPaintRotoPhysicalEditSemanticDelta` + type import; `as unknown as PhysicPaintApplyPayload` in the bridge test.
- **Files modified:** useRotoPhysicalEditCoordinator.test.ts, physicPaintBridge.test.ts
- **Verification:** typecheck green.
- **Committed in:** 5bf51ebb (Task 2 GREEN commit)

**7. [Syntax] await in a non-async callback**
- **Found during:** Task 3 GREEN
- **Issue:** `deleteRotoFrame` is a non-async useCallback; the first rail-set branch draft used `await`.
- **Fix:** Restructured to `.then(...)` matching the existing non-async style.
- **Files modified:** useRotoTimelineActions.ts
- **Verification:** full suite green.
- **Committed in:** 64e8671f (Task 3 GREEN commit)

---

**Total deviations:** 7 auto-fixed (1 test-twin discovery, 1 runtime-guard bug, 3 test-fixture corrections, 2 typecheck corrections, 1 syntax correction)
**Impact on plan:** All corrections align the implementation with the plan's intent; no behavior change and no scope creep.

## TDD Gate Compliance

- **Task 1 (tracer tdd):** RED (`42aca0c4`) then GREEN (`158e5c27`) — gate sequence satisfied.
- **Task 2 (auto tdd):** RED (`826d78e5`) then GREEN (`5bf51ebb`) — gate sequence satisfied.
- **Task 3 (auto tdd):** RED (`c6b03911`) then GREEN (`64e8671f`) — gate sequence satisfied.

## Issues Encountered

- **Missing runtime guard (debug root cause):** the type union had 'delete-rails' but `isPhysicPaintRotoPhysicalEditOperationKind` did not — isolated via a temporary debug fixture, fixed with the guard entry.
- **Divergent-variant no-ops:** three bridge rejection variants silently accepted because the proposal's empty records/loopClips made the mutations no-ops; fixed with synthetic records/clips and a key-rail-only harness.
- **Redo-clears contract:** the Plan 03 move-stable snapshot recording (before == after == current) would have restored the pre-delete set on redo; the delete-rails recording now uses after = null.
- **Plan test-twin mismatch:** the plan names a non-existent test file; the actual twin is physicsPaintRotoGroupParity.test.ts.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 05/06 (Key Spacing, Solo) consume the same D-06 aftermath carrier, the one-mapper copy discipline, and the classifier-first pattern established here.
- The `executeRailSetDelete` port + `railSetDeleteExecuteRef` Studio pattern is the reference for any remaining batch-operation execute seam.
- The delete-rails transport (runtime guard + strict parser + parent recompute) is the reference for the remaining batch operation kinds.
- No blockers. Delete Rails is fully wired end-to-end: proposer, transport, coordinator, bridge validation, classifier, dispatch, locked copy, and D-06 aftermath are all proven by test.

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-18*

## Self-Check: PASSED

- Created files verified: 43.6-04-SUMMARY.md
- All 6 task commits verified in git log: 42aca0c4, 158e5c27, 826d78e5, 5bf51ebb, c6b03911, 64e8671f
