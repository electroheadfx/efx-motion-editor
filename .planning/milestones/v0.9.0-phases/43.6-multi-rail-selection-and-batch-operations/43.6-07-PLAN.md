---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
autonomous: true
requirements: [RSET-03, BDEL-02]
gap_closure: true
gap_ids: [G-43.6-3, G-43.6-4, G-43.6-6, G-43.6-8]

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One accepted batch operation (Delete Rails / Move Rails / Key Spacing on a set) appends exactly one applied history command; the status capsule availability reflects {undo: 1, redo: 0} immediately after acceptance."
    - "Cmd+Z on a batch operation replays the exact pre-operation document snapshot and Cmd+Shift+Z reapplies the exact post-operation snapshot; the batch operationKind rides the replay call."
    - "The D-06 rail-set snapshot side-channel is consumed by the created history command: Undo restores the recorded before set and Redo restores the recorded after set."
  artifacts:
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
  key_links:
    - "isOrdinaryOperationKind guard -> recordAcceptedEdit ordinary branch -> appliedRef push -> undo()/redo() replay (L490-532, L581-702)"
    - "history command operationId -> resolveRailSetPostAcceptance snapshot lookup (physicsPaintRotoRailSetSelection.ts L299-315)"
---

<objective>
Make the three Phase 43.6 batch operation kinds undoable/redoable history commands.

Purpose: Root cause RC-A — 'move-rails', 'spacing-on-set', and 'delete-rails' were never added to
isOrdinaryOperationKind in useRotoPhysicalEditHistory.ts. Every batch acceptance falls through to the
'replay acceptance' branch, finds no pendingReplayRef, and is silently dropped — the command never enters
the applied undo stack, so Cmd+Z / Cmd+Shift+Z have nothing to replay. Keyboard routing is already correct
(physicsPaintStudioKeyboard.ts L100-117). The D-06 rail-set snapshot side-channel is wired and correct but
dead until a history command exists to key it. Fix following the 8751a903 push-rails precedent.
Output: The three batch kinds join the ordinary allowlist and a regression test proves one batch
acceptance -> one applied command -> undo replays before / redo replays after.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md

# Diagnoses (root cause, exact line references, eliminated hypotheses)
@/Users/lmarques/Dev/efx-motion-editor/.planning/debug/43-6-undo-redo-batch-ops.md

# Precedent regression test shape (push-rails one-command undo/redo at L1597-1716)
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts

# Prior plan SUMMARYs only for the batch resolver/proposer fixture shapes
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-02-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-04-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-05-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — regression test that each batch kind records one command and replays both directions</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</files>
  <behavior>
    - Test A ('delete-rails'): a TRUE multi-member mixed set (one key-rail segment + one loop) proposes a complete next document via the shared pure proposer proposePhysicPaintRotoDeleteRails; one coordinator acceptance with operationKind 'delete-rails' must make availability {undo: 1, redo: 0}; history.undo() returns true and restores the exact pre-delete snapshot; history.redo() returns true and reapplies the exact post-delete snapshot; the replay call carries operationKind 'delete-rails'.
    - Test B ('move-rails'): a two-member set translates one unit; one acceptance with operationKind 'move-rails' must make availability {undo: 1, redo: 0}; undo/redo round-trip the before/after snapshots with operationKind 'move-rails' on the replay call.
    - Test C ('spacing-on-set'): a two-member set respaces; one acceptance with operationKind 'spacing-on-set' must make availability {undo: 1, redo: 0}; undo/redo round-trip the before/after snapshots with operationKind 'spacing-on-set' on the replay call.
    - Each test must fail at this commit (RED) because isOrdinaryOperationKind returns false for the three kinds and recordAcceptedEdit drops the acceptance (availability stays {undo: 0, redo: 0}).
  <action>
  Add a new describe block 'useRotoPhysicalEditHistory batch operations on a rail set (43.6 gap closure)' beside the push atomic command block. Model the harness on the push-rails test at L1597-1716 (same pushSnapshot-style before/after builder, same recordsPort/getLiveSourceSnapshot wiring, same replay executePhysicalEdit that swaps `current` and publishes acceptedOutput with historyProvenance). For each of the three kinds compute the before/after snapshots through the REAL pure path its coordinator uses, not synthetic state:
  - 'delete-rails': call proposePhysicPaintRotoDeleteRails (physicsPaintRotoGroupLifecycle.ts) on a document with a mixed 2-member set; reuse the fixture idioms from physicsPaintRotoGroupParity.test.ts / 43.6-04-SUMMARY.md.
  - 'move-rails': resolve the real resolver intent { kind: 'move-rails', ... } through resolvePhysicPaintRotoPhysicalEdit; reuse fixture idioms from 43.6-02-SUMMARY.md.
  - 'spacing-on-set': resolve the real spacing-on-set resolver intent; reuse fixture idioms from 43.6-05-SUMMARY.md.
  Drive each acceptance through the same hook instantiation the push test uses: set acceptedOutput.value = { before, after, acceptedRevision, operationId, operationKind: '<kind>', historyProvenance: null }, then assert availability, await history.undo() and history.redo() asserting `current` equals the exact snapshots and that executePhysicalEdit.mock.calls carry the batch operationKind in order ['undo', 'redo']. Keep one parameterized it.each or three focused it blocks. Do NOT modify isOrdinaryOperationKind yet.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts -t "batch"</automated>
  </verify>
  <done>
    The three new tests exist and FAIL (RED) with assertions proving the acceptance was dropped (availability stays {undo: 0, redo: 0}); commit `test(43.6-07): add failing history regression for batch operation kinds`.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — add the three batch kinds to isOrdinaryOperationKind</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts</files>
  <action>
    Extend isOrdinaryOperationKind (L196-224) with three more clauses: `|| kind === 'move-rails'` after the 'push-rails' line, `|| kind === 'spacing-on-set'`, and `|| kind === 'delete-rails'`. Follow the exact 8751a903 push-rails precedent — allowlist membership only, no other branch changes. Do NOT touch the replay branch (L490-513), snapshot equality, or the D-06 resolver. The ordinary-kind type is already `Exclude<PhysicPaintRotoPhysicalEditOperationKind, 'undo' | 'redo'>`, so the guard type-checks as-is; no type edits. Then run the Task 1 tests to GREEN, then the full history suite and the coordinator suite (the coordinator's delete-rails/move-rails/spacing-on-set transport tests must stay green — the allowlist widening must not alter transport validation).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</automated>
  </verify>
  <done>
    isOrdinaryOperationKind lists all three batch kinds; the new regression tests pass (one acceptance -> one applied command -> undo replays before / redo replays after with the batch operationKind); the coordinator transport suite stays green; commit `fix(43.6-07): record batch operations in the physical history allowlist`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator acceptedOutput -> history allowlist | untrusted operationKind crosses into the applied undo stack; the allowlist is the only gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-07-01 | Tampering | isOrdinaryOperationKind | low | mitigate | Explicit allowlist extension limited to the three closed-union batch kinds; the guard remains a whitelist and the replay branch is untouched |
| T-43.6-07-02 | Spoofing | recordAcceptedEdit identity check | low | mitigate | Existing launchOperationId/layerId/projectContextId/capacity tuple check and lastAcceptedOperationIdRef dedup guard stay unchanged (L483-488) |
| T-43.6-07-03 | Tampering | D-06 snapshot side-channel | low | accept | Snapshot lookups are keyed by the original command operationId and pruned on launch replacement (session-only); no new trust surface introduced |
</threat_model>

<verification>
- Task 1 fails RED; Task 2 turns it green.
- `pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts` fully green after Task 2.
- Regression sweep: `cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint` (full physic-paint suite) — no pre-existing history/undo test may flip (the allowlist only adds kinds; nothing previously asserted a batch acceptance is dropped).
</verification>

<success_criteria>
- Cmd+Z / Cmd+Shift+Z undo/redo Delete Rails, Move Rails, and Key Spacing on a set, restoring the exact pre-operation document and the recorded rail set.
- The three batch kinds are visible in isOrdinaryOperationKind and covered by regression tests.
- G-43.6-3(undo/redo part), G-43.6-4(undo/redo part), G-43.6-6, G-43.6-8(undo/redo part) closed by this plan.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-07-SUMMARY.md` when done
</output>
