---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 02
subsystem: ui
tags: [preact, signals, roto, rail-selection, batch-operations, move-rails, resolver, transport]

# Dependency graph
requires:
  - phase: 43.4
    provides: shared rail roving focus machinery, Key Rail derived segments, single-rail selection signals
  - phase: 43.5
    provides: push-rails directional-set/clamp authority template, straddle verdict pattern, D-17 preview-is-the-commit discipline
  - phase: 43.6-01
    provides: multi-rail selection SET session scope, canonical cross-type ordering, D-06 aftermath carrier
provides:
  - Exported pure set-derivation + clamp authority for the explicit rail set (derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta) consumed by the resolver branch and (Plan 03) the drag preview — one authority, preview-is-the-commit
  - The 'move-rails' intent branch in the physical resolver: rigid one-unit translation, set-level clamp/reject, generalized straddle guard (D-10), 43.3/43.4 break travel (D-11), fail-closed validation
  - 'move-rails' in the transport operation-kind union + intent payload + strict parser (types/physicPaint.ts) and the coordinator authorization case
affects: [43.6-03, 43.6-04, 43.6-05, 43.6-06]

# Actuals (#2632) — pairs with the plan's `estimate` (60000 tokens) to calibrate future estimates.
actuals:
  tokens: 17185    # chars/4 over the realized diff (68741 chars, 92ae2db1..HEAD)
  tasks: 2         # tasks completed
  commits: 4       # commits made (2 RED + 2 GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "One exported pure set-derivation + one exported pure clamp as the single authority consumed by both the resolver branch and the Plan 03 drag preview (preview-is-the-commit, D-17)"
    - "Explicit-set membership validated against freshly derived Key Rail segments + loop ranges at resolution time — fail-closed on stale or unknown members (T-43.6-02)"
    - "Straddle verdict derived from canonical attachment + sourceCycleId only, never caller flags; 'move-rails-source-straddle' fails closed with zero partial proposal (D-10, T-43.6-04)"
    - "D-11 break travel: internal breaks travel with moved key identity; vacated-successor rule lands a break on the first surviving key at/after the vacated end; landing-gap rule manufactures a break only when a gap actually opens; adjacent landing never merges"
    - "43.3 algebra carried: source-attached Groups move source keys with identity via nextLoopClips; duplicated placements move placement-only (placementStart += delta), source keys unmoved"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
    - app/src/types/physicPaint.ts
    - app/src/types/physicPaint.test.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts

key-decisions:
  - "Explicit-set membership is exact-match against freshly derived segments/ranges: firstKeyId + ordered keyIds for Key Rails, loopId for Groups; stale or unknown members fail closed with 'malformed-identity' (T-43.6-02)"
  - "The set clamps or rejects as ONE unit at the first collision with any unselected key, unselected Group occupancy (including linked occurrences), frame 0, or capacity; committed delta IS the clamped delta (D-07, D-17)"
  - "Straddle verdict generalizes 43.5 D-16 to the explicit set: a source-attached selected Group sharing sourceCycleId with an UNSELECTED Group rejects the whole move fail-closed with 'move-rails-source-straddle'; duplicated placements never straddle (D-10)"
  - "D-11 break travel at set level: internal breaks travel with moved key identity; the vacated-successor rule manufactures a NEW break only when the first surviving key at/after the vacated end follows group-owned keys; the landing-gap rule manufactures only when the set's first key does not already own a break (group source key case); landing adjacent to a fixed-side neighbor never merges"
  - "Delta 0 is a valid no-change input to the clamp (never a failure); the no-commit discipline lives in Plan 03 (D-13)"
  - "semanticDelta is null (mapping-only edit, same as move-key-rail); selectedKeyId null; status carries operationKind 'move-rails', changed, affectedKeyIds, and a concise internal text (product copy is the Plan 03 mapper's job)"

patterns-established:
  - "One authority for set movement: derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta are the ONLY movement authorities; the resolver branch and the Plan 03 preview consume the same exports so preflight and rejection can never disagree"
  - "Fail-closed intent branch: malformed members, stale membership, non-integer delta, straddle, or collision produce a stable failure code + concise text with zero partial proposal"
  - "nextLoopClips + nextIncomingInterpolationBreakKeyIds on the proposal carry the complete Group placement and break collections — the authority link whose breakage desyncs placements and interpolation from moved keys"

requirements-completed: [BMOV-01, BMOV-04, BMOV-05]

# Coverage metadata (#1602) — per-deliverable traceability for deterministic UAT routing.
coverage:
  - id: D1
    description: "Exported pure set-derivation + clamp authority for the explicit rail set (derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta) with exact-match membership, moved-key set, and the generalized straddle verdict"
    requirement: BMOV-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts#derivePhysicPaintRailSetMove/clampPhysicPaintRailSetMoveDelta contract cases (43.6-02 Task 1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "move-rails intent branch: rigid one-unit translation preserving relative offsets/internal gaps, set-level clamp/reject at unselected key / unselected Group occupancy / frame 0 / capacity, zero-delta valid no-change proposal"
    requirement: BMOV-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts#resolvePhysicPaintRotoPhysicalEdit — move-rails (explicit-set rigid translation) (43.6-02 Task 2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Shared-source algebra: source-attached Groups move source keys with identity and placementStart follows via nextLoopClips; duplicated placements move placement-only; unselected Group sharing a source cycle rejects fail-closed with 'move-rails-source-straddle'"
    requirement: BMOV-04
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts#move-rails source-attached placement follow + straddle rejection cases (43.6-02 Task 2)"
        status: pass
    human_judgment: false
  - id: D4
    description: "D-11 break/gap travel: internal breaks travel with moved key identity, vacated-successor break landing, landing-gap rule, no-merge on adjacent landing; nextIncomingInterpolationBreakKeyIds carries the complete next collection"
    requirement: BMOV-05
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts#move-rails break-travel cases (internal break, edge-gap landing, no-merge) (43.6-02 Task 2)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Transport + coordinator wiring: 'move-rails' in the operation-kind union and both strict parsers, intent payload round-trip, coordinator authorization pairing the exact intent"
    requirement: BMOV-01
    verification:
      - kind: unit
        ref: "app/src/types/physicPaint.test.ts#move-rails parser round-trip + rejection cases; app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts#authorizes a matched move-rails intent and rejects a mismatched one (43.6-02 Task 2)"
        status: pass
    human_judgment: false

# Metrics
duration: 1h 30m
completed: 2026-08-18
status: complete
---

# Phase 43.6 Plan 02: move-rails Batch Intent Summary

**The batch Move commit authority: exported pure set-derivation + clamp for the explicit rail set, the 'move-rails' intent branch with rigid one-unit translation, set-level clamp/reject, the generalized straddle guard (D-10), 43.3/43.4 break travel (D-11), and fail-closed validation — joined end-to-end through the transport types and the coordinator authorization switch**

## Performance

- **Duration:** 1h 30m
- **Started:** 2026-08-18T18:30:00Z
- **Completed:** 2026-08-18T19:10:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Exported pure set authorities `derivePhysicPaintRailSetMove` + `clampPhysicPaintRailSetMoveDelta` beside the push authorities: exact-match membership against freshly derived Key Rail segments + loop ranges, moved-key set (Key Rail keys + source keys of source-attached selected Groups; duplicated placements contribute NO moved keys), the generalized straddle verdict (source-attached selected Group sharing sourceCycleId with an UNSELECTED Group), and the signed-delta clamp scanning toward 0 with blocked-edge + colliding-member identity, ok:false only when zero valid movement exists.
- The `move-rails` intent branch in the resolver: validates members/delta fail-closed, consumes the set authorities, translates moved keys by delta, chains nextLoopClips (attached: placementStart follows the moved first source key; duplicated in-set: placementStart += delta), computes nextIncomingInterpolationBreakKeyIds per D-11, and finalizes through the common finalizer.
- D-11 break travel at set level: internal breaks travel with moved key identity; the vacated-successor rule lands a break on the first surviving key at/after the vacated end (skipping moved + group-owned keys); the landing-gap rule manufactures a break only when a gap actually opens at the set's first key; adjacent landing never merges.
- `'move-rails'` joined the operation-kind unions in the resolver and the transport (types/physicPaint.ts) with the intent payload type, the strict parser chain (round-trip + malformed rejection), and `'move-rails-source-straddle'` joined the failure-code union.
- `createAuthorizedPhysicalEditPayload` gained the `'move-rails'` case pairing the exact intent; a mismatched intent is rejected before staging.
- Full verification: 260 targeted tests (resolver 169, coordinator 55, transport), typecheck green, full physic-paint suite 1762 tests green (82 files), full app suite 2454 tests green (132 files).

## Task Commits

Each task was committed atomically (TDD RED/GREEN):

1. **Task 1: Exported pure set-derivation + clamp authority for the explicit rail set (with generalized straddle verdict)**
   - `75b16467` (test: add failing rail-set move derivation and clamp tests)
   - `58a0a2de` (feat: implement rail-set move derivation and clamp authorities)
2. **Task 2: move-rails intent branch + transport union + coordinator authorization**
   - `7593c621` (test: add failing move-rails intent branch tests)
   - `77fc98da` (feat: implement move-rails intent branch with set clamp and break travel)

**Plan metadata:** `docs(43.6-02): complete plan` (final docs commit)

## Files Created/Modified

- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` - `PhysicPaintRailSetMoveMember` type, `derivePhysicPaintRailSetMove` + `clampPhysicPaintRailSetMoveDelta` exports, `derivePhysicPaintRailSetMoveIncomingInterpolationBreakKeyIds`, the `move-rails` intent branch, `'move-rails'` in the operation-kind union, `'move-rails-source-straddle'` in the failure-code union, buildStatusText case
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts` - Task 1 set-derivation/clamp/straddle contract cases + Task 2 `move-rails` describe block (10 intent cases: mixed-set rigid translation, edge-gap break landing, internal-break travel, no-merge, duplicated placement-only, source-attached placement follow, straddle rejection, collision rejection, zero-delta no-change, malformed)
- `app/src/types/physicPaint.ts` - `PhysicPaintRotoRailSetMoveMember` transport type, move-rails intent union member + strict parser + serializer, `'move-rails'` in the transport operation-kind union and both strict parsers
- `app/src/types/physicPaint.test.ts` - move-rails parser round-trip + rejection cases (extra key, empty members, non-integer delta, firstKeyId not first, duplicate rails, unknown member kind, unbounded ids)
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` - `case 'move-rails'` in createAuthorizedPhysicalEditPayload
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts` - matched move-rails intent authorization + mismatched-intent rejection test

## Decisions Made

- **Exact-match membership (T-43.6-02):** the explicit member list is validated against freshly derived Key Rail segments + loop ranges at resolution time — firstKeyId + ordered keyIds for Key Rails, loopId for Groups; stale or unknown members fail closed with `'malformed-identity'`.
- **One-unit clamp/reject (D-07):** the set clamps or rejects as ONE unit at the first collision with any unselected key, unselected Group occupancy (including linked occurrences), frame 0, or capacity; the committed delta IS the clamped delta (D-17 preview-is-the-commit).
- **Generalized straddle verdict (D-10):** a source-attached selected Group sharing sourceCycleId with an UNSELECTED Group rejects the whole move fail-closed with the dedicated `'move-rails-source-straddle'` code; duplicated placements never straddle; the family is never silently pulled in.
- **D-11 break travel:** internal breaks travel with moved key identity; the vacated-successor rule manufactures a NEW break only when the first surviving key at/after the vacated end follows group-owned keys; the landing-gap rule manufactures only when the set's first key does not already own a break (group source key case); landing adjacent to a fixed-side neighbor never merges.
- **Delta 0 is valid:** a no-change input to the clamp is never a failure; the no-commit discipline lives in Plan 03 (D-13).
- **Mapping-only edit:** semanticDelta null, selectedKeyId null, status carries operationKind `'move-rails'`, changed, affectedKeyIds, and a concise internal text (product copy is the Plan 03 mapper's job).

## Deviations from Plan

### Auto-fixed Issues

**1. [Test-fixture correction] Segment merging across gaps in deriveKeyRailSegments**
- **Found during:** Task 2 RED (edge-gap and internal-break break-travel tests)
- **Issue:** `deriveKeyRailSegments` splits segments ONLY on group ownership or incoming breaks, NEVER on gaps. Two planned fixtures merged adjacent rails into one segment (B [20,30) + C flush at 30 with no break), producing `'malformed-target'` instead of the intended break-landing scenario.
- **Fix:** Replaced the flush-successor fixture with a group-gap successor (B [20,30), group-owned g0-g3 [31,35), C [40,50)) so the successor rail is a distinct segment; rewrote the internal-break case as a group-source-key case (P@0, g0@20, g1@21, loop-G attached, breaks ['g1']) which exercises both internal-break travel (g1 carried) and the landing-gap manufacturing rule (g0).
- **Files modified:** physicsPaintRotoPhysicalResolver.test.ts
- **Verification:** all 10 move-rails intent cases pass; full suite green.
- **Committed in:** 77fc98da (Task 2 GREEN commit)

**2. [Test-fixture correction] Source-attached fixture straddled an unselected Group**
- **Found during:** Task 2 RED (source-attached placement-follow test)
- **Issue:** The planned `buildPushGroupClips` fixture contains loop-D sharing the source cycle with loop-G, so the source-attached move correctly rejected with `'move-rails-source-straddle'` — the placement-follow scenario needs a single-attached fixture.
- **Fix:** Added `buildSingleAttachedGroupClips` (loop-G attached at 20, source g0/g1, no loop-D) and asserted `nextLoopClips` length 1 for the placement-follow case; the straddle rejection case keeps the two-loop fixture.
- **Files modified:** physicsPaintRotoPhysicalResolver.test.ts
- **Verification:** all 10 move-rails intent cases pass; full suite green.
- **Committed in:** 77fc98da (Task 2 GREEN commit)

---

**Total deviations:** 2 auto-fixed (2 test-fixture corrections)
**Impact on plan:** Both corrections align the fixtures with the documented deriveKeyRailSegments semantics; no production-code behavior change and no scope creep.

## TDD Gate Compliance

- **Task 1 (auto tdd):** RED (`75b16467`) then GREEN (`58a0a2de`) — gate sequence satisfied.
- **Task 2 (auto tdd):** RED (`7593c621`, 10 failing tests) then GREEN (`77fc98da`) — gate sequence satisfied.

## Issues Encountered

- **Segment merging across gaps:** `deriveKeyRailSegments` never splits on gaps, so two planned break-travel fixtures merged adjacent rails into one segment and failed with `'malformed-target'`. Resolved with the group-gap successor fixture and the group-source-key internal-break case (see Deviations).
- **Landing-gap rule no-op discovery:** for a Key Rail member, the landing-gap rule can never manufacture a NEW break (a segment split requires a break on the first key, which is the same key the rule would add); the manufacturing case requires a group source key — proven by the rewritten internal-break test.
- **Scratch test debugging:** a scratch test in the src tree (vitest include is `src/**/*.test.ts`) confirmed the `'malformed-target'` failure codes before the fixture corrections; removed after diagnosis.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 (drag preview) consumes the SAME exported authorities (`derivePhysicPaintRailSetMove` + `clampPhysicPaintRailSetMoveDelta`) — preflight and rejection can never disagree (D-17); the strip pre-clamps so the resolver treats collisions as hard walls.
- Plan 03 owns the product copy via the one mapper; the resolver status text stays concise and internal.
- Plans 03/04/05/06 consume the D-06 aftermath carrier and `deriveRailSetOrder` from Plan 01 for clamp/spacing/solo.
- No blockers. The move-rails intent is fully wired end-to-end: resolver branch, transport union + strict parser, and coordinator authorization are all proven by test.

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-18*

## Self-Check: PASSED

- Created files verified: 43.6-02-SUMMARY.md
- All 4 task commits verified in git log: 75b16467, 58a0a2de, 7593c621, 77fc98da
