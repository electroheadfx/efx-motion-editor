---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
autonomous: true
requirements: [BMOV-01, BMOV-04, BMOV-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An explicit rail set translates rigidly by one signed delta: relative offsets, internal gaps, and internal breaks are preserved exactly; unselected objects never move (D-07)."
    - "The set clamps or rejects as ONE unit at the first collision with any unselected key, unselected Group occupancy (including linked occurrences), frame 0, or capacity (D-07)."
    - "One exported pure set-derivation + one exported pure clamp are the single authority consumed by both the resolver branch and (in Plan 03) the drag preview — preview-is-the-commit, preflight and rejection can never disagree (43.5 D-17 pattern)."
    - "Source-attached Groups in the set move their source keys with identity; duplicated placements in the set move placement-only via nextLoopClips (43.3 algebra, D-10)."
    - "An unselected Group sharing a source cycle with a selected source-attached Group rejects the whole move fail-closed with a dedicated failure code; the family is never silently pulled in (D-10)."
    - "Internal breaks travel with moved key identity; movement-created gaps at the set edges land a break on the first surviving successor only when a gap actually opens; landing adjacent to a fixed-side neighbor never merges (D-11, 43.3 D-12 / 43.4 D-19 authority)."
    - "The move-rails intent joins the closed operation-kind union with strict parsing and canonical revision participation; failure branches carry only a stable code + concise text, never a partial proposal (43.3 D-14 precedent)."
  artifacts:
    - "physicsPaintRotoPhysicalResolver.ts — derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta pure exports and the 'move-rails' intent branch"
    - "physicsPaintRotoPhysicalResolver.test.ts — set derivation, clamp, straddle, break-travel, and intent cases"
    - "app/src/types/physicPaint.ts — 'move-rails' in the transport operation-kind union + intent payload + strict parser"
    - "useRotoPhysicalEditCoordinator.ts — createAuthorizedPhysicalEditPayload 'move-rails' case"
  key_links:
    - "Exported pure clamp/derivation consumed by resolver commit AND (Plan 03) strip preview — the one-authority link whose breakage forks preview from commit"
    - "nextLoopClips + nextIncomingInterpolationBreakKeyIds on the proposal — the Group/break authority link whose breakage desyncs placements and interpolation from moved keys"
---

<objective>
Add the `move-rails` batch intent to the physical resolver: an explicit-set rigid translation with set-level clamp, the generalized straddle guard (D-10), 43.3/43.4 break travel (D-11), and fail-closed validation — joining the closed intent union end-to-end through the transport types and the coordinator authorization switch.

Implements BMOV-01 (rigid translation, one-unit clamp/reject), BMOV-04 (shared-source algebra + straddle rejection), BMOV-05 (break/gap travel) per D-07, D-10, D-11.

Purpose: the batch Move commit authority lands before any drag UI exists, so Plan 03's drag session is pure wiring over a proven resolver path (tracer-first: the hardest correctness surface is proven in isolation first).
Output: a tested resolver that accepts `{ members, delta }` and returns one complete fail-closed proposal (records mapping + nextLoopClips + nextIncomingInterpolationBreakKeyIds + status) or a stable failure code.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-RESEARCH.md
@.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exported pure set-derivation + clamp authority for the explicit rail set (with generalized straddle verdict)</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts L2230-2440 (derivePhysicPaintPushSet + clampPhysicPaintPushDestination — the directional-set/clamp template including the PushRail shape, canonical attachment test L2329-2335, straddle scan L2399-2415, and the nearest-free-delta scan L2213-2228)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts L4055-4061 (move-key-rail exact segment-match validation idiom)
    - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts L37+ (deriveKeyRailSegments — Key Rail derivation input)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-PATTERNS.md §roto/physicsPaintRotoPhysicalResolver.ts (Pattern 2 + Set clamp authority shared pattern)
  </read_first>
  <behavior>
    - derivePhysicPaintRailSetMove validates the EXPLICIT member list against freshly derived Key Rail segments + loop ranges: exact-match membership (firstKeyId + ordered keyIds for Key Rails; loopId for Groups), fail-closed on stale or unknown members.
    - Moved key set = Key Rail members' keys plus source keys of source-attached selected Groups; duplicated placements contribute NO moved keys (43.3 algebra, Pitfall 6 carried: attachment derives from frameByKeyId.get(sourceKeyIds[0]) === placementStart, never caller flags).
    - Straddle verdict: a source-attached selected Group sharing sourceCycleId with an UNSELECTED Group yields a straddle verdict (generalizes 43.5 D-16 to the explicit set); duplicated placements never straddle.
    - clampPhysicPaintRailSetMoveDelta: signed-delta input; scans from the proposed delta toward 0 returning the first delta keeping every moved member clear of unselected key frames, unselected Group occupancy intervals (including linked occurrences from loop ranges), frame 0, and capacity; returns the clamped delta, the blocked edge ('left'|'right'|null) and the colliding member identity; ok:false only when zero valid movement exists.
    - Delta 0 is a valid no-change input to the clamp (never a failure); the no-commit discipline lives in Plan 03 (D-13).
    - Both functions are pure, frozen-output, and exported so the Plan 03 preview consumes the SAME authority (preflight and rejection can never disagree).
  </behavior>
  <action>
    RED first: extend physicsPaintRotoPhysicalResolver.test.ts with failing cases: explicit-set derivation (mixed Key Rail + Motion + Static members), stale member rejection, duplicated-placement moved-key exclusion, straddle verdict on shared sourceCycleId with an unselected Group, clamp at unselected key / unselected Group occupancy / frame 0 / capacity, blocked-edge direction (rightward intent pulled back → right edge of the rightmost blocked member; leftward pushed forward → left edge), zero-valid-movement ok:false, delta-0 ok.

    Implement in physicsPaintRotoPhysicalResolver.ts beside the push authorities (L2230+ section): export `PhysicPaintRailSetMoveMember` (discriminated: `{ kind: 'key-rail'; firstKeyId: string; keyIds: readonly string[] } | { kind: 'loop'; loopId: string }`), `derivePhysicPaintRailSetMove(input)` returning `{ ok: true; members; movedKeyIds: ReadonlySet<string>; movedSetBounds; straddle } | { ok: false; code; text }`, and `clampPhysicPaintRailSetMoveDelta(input)` returning `{ ok: true; delta; blockedEdge; collidingMemberId } | { ok: false }`. Reuse the existing boundary vocabulary (unselected keys, Group occupancy, frame 0, capacity) and the canonical attachment test verbatim from derivePhysicPaintPushSet — factor shared internals privately if duplication is exact, but keep both public authorities' signatures independent (the push set is directional; the rail set is explicit — D-20: Push never consumes the set).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - physicsPaintRotoPhysicalResolver.ts exports derivePhysicPaintRailSetMove and clampPhysicPaintRailSetMoveDelta (source assertion: both names appear as exported functions).
    - Tests cover every <behavior> bullet including straddle on unselected shared source and zero-valid-movement ok:false.
    - derivePhysicPaintPushSet and clampPhysicPaintPushDestination behavior is unchanged (existing push tests stay green — the whole resolver test file passes).
    - Typecheck green.
  </acceptance_criteria>
  <done>The explicit-set derivation, clamp, and straddle verdict exist as tested pure exports that both the resolver branch (Task 2) and the Plan 03 drag preview consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: move-rails intent branch + transport union + coordinator authorization</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts, app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts L172-187 (operation-kind union), L291-331 (proposal shape: nextRecords/nextLoopClips/nextIncomingInterpolationBreakKeyIds), L338-365 (failure-code union), L4015-4075 (move-key-rail branch — rigid member translation + exact segment match + clamp consumption idiom)
    - app/src/types/physicPaint.ts L720-760 (PhysicPaintRotoPhysicalEditOperationKind transport union) and L1015-1060 (isPhysicPaintRotoPhysicalEditOperationKind strict parser + isPhysicPaintRotoOrdinaryOperationKind)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts L330-379 (createAuthorizedPhysicalEditPayload switch)
    - .planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-CONTEXT.md D-07/D-10/D-11 (movement algebra), D-12/D-13 (status/no-op discipline — product copy lands in Plan 03 via the one mapper)
  </read_first>
  <behavior>
    - Intent `{ kind: 'move-rails'; members; delta }`: delta is a signed integer; members use the Task 1 discriminated shape; validation is fail-closed on malformed members, stale membership, non-integer delta, or any moved destination colliding with an unselected key frame or unselected Group occupancy ('no-free-space-in-direction' reuse) — the strip pre-clamps, so the resolver treats collisions as hard walls.
    - Source-attached selected Groups: their source keys move with identity; placementStart follows (nextLoopClips carries them with placementStart = old + delta). Duplicated placements in the set: placementStart += delta via nextLoopClips, source keys unmoved.
    - Breaks (D-11): internal breaks travel with moved key identity; when a gap opens at a set edge, a break lands on the first surviving successor (43.3 D-12); breaks on moved keys travel unchanged; landing adjacent to a fixed-side neighbor never merges. nextIncomingInterpolationBreakKeyIds carries the complete next collection.
    - New failure code 'move-rails-source-straddle' joins the union; the straddle branch fails closed with zero partial proposal (D-10).
    - semanticDelta is null (mapping-only edit, same as move-key-rail); status carries operationKind 'move-rails', changed, affectedKeyIds (every moved key), and a concise internal text (product copy is the Plan 03 mapper's job).
    - Transport: 'move-rails' joins the types/physicPaint.ts operation-kind union, the intent union, and both strict parsers; the coordinator switch gains the 'move-rails' case pairing the exact intent.
  </behavior>
  <action>
    RED first: resolver tests for the intent branch — mixed-set rigid translation (relative offsets/internal gaps preserved), per-edge gap break landing, internal break travel, no-merge on adjacent landing, duplicated-placement placement-only move, source-attached placement follow, straddle rejection with the new code, collision rejection, zero-delta valid no-change proposal (changed === false). Then GREEN.

    Resolver: extend PhysicPaintRotoPhysicalEditOperationKind (L172-187) with 'move-rails'; add the intent type to the intent union; add 'move-rails-source-straddle' to the failure-code union; implement the branch after the move-key-rail branch consuming Task 1's derive/clamp authorities — validate membership exactly, translate moved keys by delta, compute nextLoopClips (attached: placementStart follows the moved first source key; duplicated in-set: placementStart += delta), compute nextIncomingInterpolationBreakKeyIds per D-11, and finalize through the existing common finalizer (capacity/over-capacity discipline unchanged).

    types/physicPaint.ts: add 'move-rails' to the transport operation-kind union (L729-753), the intent union with its payload, the strict parser chain (L1015-1045), and isPhysicPaintRotoOrdinaryOperationKind. Keep literal strings identical across resolver and transport (43.3 D-14).

    useRotoPhysicalEditCoordinator.ts: add the 'move-rails' case to createAuthorizedPhysicalEditPayload (L330-379) pairing `intent?.kind === 'move-rails'`; no semanticDeltaEquals change (delta is null).
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts && pnpm run typecheck && pnpm exec vitest run src/components/physic-paint</automated>
  </verify>
  <acceptance_criteria>
    - Resolver test file contains passing cases for every <behavior> bullet; the 'move-rails-source-straddle' failure is asserted with zero proposal on the failure branch.
    - types/physicPaint.ts and the resolver union contain the identical 'move-rails' literal; the strict parser round-trips a valid move-rails intent and rejects malformed ones (test).
    - createAuthorizedPhysicalEditPayload returns a payload for a matched move-rails intent and null for a mismatched intent (coordinator test or existing switch test extension).
    - Full physic-paint suite + typecheck green; no existing move-key-rail/move-group/push-rails test changes (byte-preserved behavior).
  </acceptance_criteria>
  <done>The move-rails intent resolves one complete fail-closed proposal for an explicit set with correct Group algebra and break travel, wired through transport and coordinator authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child intent -> parent transport | The move-rails intent crosses the bridge as a typed payload; malformed or forged intents must never mutate |
| proposal -> accepted document | A stale or divergent proposal must never replace the canonical document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-01 | Tampering | move-rails intent payload | high | mitigate | Closed union + strict parser in types/physicPaint.ts; fail-closed resolver branch; failure branch carries no partial proposal (Task 2) |
| T-43.6-02 | Tampering/Repudiation | stale-authority commit | high | mitigate | Intent membership validated against freshly derived segments/ranges at resolution time; proposalVersion discipline unchanged (existing guard); Plan 03 commit revalidates before dispatch |
| T-43.6-04 | Tampering | silent set expansion via shared sources | high | mitigate | Straddle verdict derived from canonical attachment + sourceCycleId only, never caller flags (Task 1); 'move-rails-source-straddle' fails closed (D-10) |
| T-43.6-SC | Tampering | package installs | low | mitigate | No package installs in this plan; legitimacy gate not triggered |
</threat_model>

<verification>
- `pnpm exec vitest run src/components/physic-paint` green after every task; full suite + typecheck before plan completion.
- VALIDATION.md row 43.6-BMOV-01 (BMOV-01/04/05) is covered by the resolver test extensions.
</verification>

<success_criteria>
- The resolver proves the complete batch-Move algebra — rigid set translation, one-unit clamp, straddle rejection, placement follow, break travel — with fail-closed validation and zero behavior change to existing intents.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-02-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 02)

New symbols (for plan-review-convergence source grounding):
- `PhysicPaintRailSetMoveMember` (type) — physicsPaintRotoPhysicalResolver.ts
- `derivePhysicPaintRailSetMove` (function) — physicsPaintRotoPhysicalResolver.ts
- `clampPhysicPaintRailSetMoveDelta` (function) — physicsPaintRotoPhysicalResolver.ts
- `PhysicPaintRailSetMoveStraddleVerdict` (type or reuse of PhysicPaintPushStraddleVerdict) — physicsPaintRotoPhysicalResolver.ts
- Operation kind literal `'move-rails'` — physicsPaintRotoPhysicalResolver.ts + app/src/types/physicPaint.ts
- Failure code literal `'move-rails-source-straddle'` — physicsPaintRotoPhysicalResolver.ts
- move-rails intent payload type — app/src/types/physicPaint.ts
</output>
