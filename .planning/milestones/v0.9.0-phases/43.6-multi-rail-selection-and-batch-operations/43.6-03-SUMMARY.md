---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 03
subsystem: ui
tags: [preact, signals, roto, rail-selection, batch-operations, move-rails, drag-session, preview, ghost]

# Dependency graph
requires:
  - phase: 43.6-01
    provides: multi-rail selection SET session scope, canonical cross-type ordering, D-06 aftermath carrier (recordRailSetSnapshot + resolveRailSetPostAcceptance)
  - phase: 43.6-02
    provides: exported set authorities (derivePhysicPaintRailSetMove + clampPhysicPaintRailSetMoveDelta), the 'move-rails' resolver intent branch, transport union + coordinator authorization
provides:
  - The batch Move Rails pointer-session hook (usePhysicsPaintRailSetDrag): port-injected gesture lifecycle with set-level preview state { delta, blockedEdge, collidingMemberId, gapIntervals }, preview-is-the-commit, zero-delta no-publish, Escape/pointercancel/lostpointercapture cleanup
  - D-08 collapse-first routing in both rail families: set member -> batch session; non-member -> own 43.3/43.4 drag unchanged
  - Per-member ghost/gap/blocked-edge preview paint (55% kind colors, 2x12px #FF6B6B bar, roto-fill-empty gap cells) on a layer between 7 and 8
  - prepareRailSetMove / commitRailSetMove ports with stale-authority revalidation (T-43.6-02) and the locked live/accepted/rejection copy via the one mapper
  - D-06 set aftermath wiring: recordRailSetSnapshot at acceptance, resolveRailSetPostAcceptance on commit/undo/redo so the set survives and restores exactly
affects: [43.6-04, 43.6-05, 43.6-06]

# Actuals (#2632) — pairs with the plan's `estimate` (75000 tokens) to calibrate future estimates.
actuals:
  tokens: 29175    # chars/4 over the realized diff (116700 chars, 4b800238~1..89d6fdbf)
  tasks: 2         # tasks completed
  commits: 4       # commits made (2 RED + 2 GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Port-injected pointer-session drag hook (template: usePhysicsPaintKeyRailDrag): resolver/store concerns enter ONLY through projectDelta/clampDelta/prepareAtDelta/onDropCommit/onCancel/onRejected/onPreviewChange/clearClickSequence/windowLike ports; the hook owns gesture lifecycle and set-level preview state only"
    - "Preview-is-the-commit (D-17): the publication prepared during the drag is retained exactly and committed at pointer-up, never recomputed; commit revalidates the current proposal version and fails closed on mismatch (T-43.6-02, 43.5 commitRotoPush precedent)"
    - "D-08 collapse-first routing: pointer-down on a set member hands the event to the batch session; a non-member runs its own 43.3/43.4 drag unchanged — collapse is a click-time selection side-effect in the Studio"
    - "One deterministic copy mapper (D-27): mapRotoRailSetMoveProductReason with kinds disabled/rejected/live/accepted; straddle sentence verbatim; no-space delegates to mapRotoGroupDragProductReason"
    - "Live readout publishes through the capsule pendingOperation chain only (Pitfall 7): pushDragFeedback ?? railSetDragFeedback ?? rotoDragFeedback ?? busy status — hover alone never changes the capsule"

key-files:
  created:
    - app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts
    - app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts
  modified:
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx
    - app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
    - app/src/components/physic-paint/physicsPaintStudio.css
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx

key-decisions:
  - "The hook's preview state carries only the SET-level result { delta, blockedEdge, collidingMemberId, gapIntervals }; per-member ghost geometry and beforeRange/afterRange are derived by the strip from member intervals + delta — the hook owns NO member geometry"
  - "The strip's clampDelta port forwards the PROPOSED delta for the no-space case (clamp ok:false) so prepare fails with the no-space rejection; the hook's D-13 zero-delta skip is reserved for origin return; blockedEdge/collidingMemberId are normalized to null when unclamped"
  - "D-06 aftermath: recordRailSetSnapshot(operationId, current, current) at acceptance (identities are move-stable); undo/redo lookups use accepted.historyProvenance?.historyCommandId ?? accepted.operationId — the ORIGINAL command id, never the replay command's own id"
  - "RailSetIdentity (no keyIds) is NOT assignable to PhysicPaintRailSetMoveMember (requires keyIds): the Studio resolves members against freshly derived keyRailSegments in a memo, never passing the selection identity directly"
  - "Loop clip mode includes 'progressive': the ghost kind class treats non-static as purple (kind class 'key-rail' for key rails, 'mode-static' for static, default purple for motion/progressive)"
  - "Ghosts are aria-hidden paint-only at 55% kind colors on a layer between 7 and 8 (z-index 8); originals stay at 100% with orange selection lines for the whole drag; the 2x12px #FF6B6B bar is the sole blocked signal"

patterns-established:
  - "Batch drag session state machine: modifier guard (L140 verbatim), 4px horizontal threshold, capture-phase Escape cancel, pointercancel/lostpointercapture cleanup exactly once, trailing-click suppression armed only when a session actually started"
  - "One authority for set movement consumed by both preflight and commit: the retained publication revalidates against the current proposal version before dispatch (T-43.6-02)"
  - "Set survival across batch ops: recordRailSetSnapshot before dispatch + resolveRailSetPostAcceptance on acceptance/undo/redo — the D-06 side-channel keeps the set selected at the new positions and restores it exactly on Undo/Redo"

requirements-completed: [BMOV-02, BMOV-03, BMOV-06, RSET-03]

# Coverage metadata (#1602) — per-deliverable traceability for deterministic UAT routing.
coverage:
  - id: D1
    description: "usePhysicsPaintRailSetDrag pointer-session hook: L140 modifier guard verbatim, 4px threshold, preview-is-the-commit (prepared publication committed verbatim), zero-delta publishes nothing, prepare-failure rejection at drop, Escape/pointercancel/lostpointercapture cleanup exactly once, click suppression armed only after start"
    requirement: BMOV-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts (43.6-03 Task 1, 14 cases)"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-08 collapse-first routing in both rail families: set member pointer-down routes to the batch session (never the own drag), non-member runs its own 43.3/43.4 drag unchanged, set-member trailing click consumes the batch suppression first"
    requirement: BMOV-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx#routes set-member pointer-down to the batch session and keeps the own drag for non-members (D-08) (43.6-03 Task 2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Per-member ghost/gap/blocked-edge preview paint: 55% kind colors (Motion #8B5CF6 / Static #06B6D4 / Key Rail #8A939C), 2x12px #FF6B6B bar on the colliding member's blocked edge, roto-fill-empty gap cells, ghosts on a layer between 7 and 8, all aria-hidden + pointer-events none"
    requirement: BMOV-03
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx#paints batch Move ghosts at 55% kind colors and the 2x12px blocked-edge bar (UI-SPEC M3); physicsPaintStudio.css .physics-paint-rail-set-* rules (43.6-03 Task 2)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Locked copy via the one mapper: live readout 'Move Rails +{D} - set frames {A}-{B} -> {A'}-{B'}' (U+2212 leftward, ', gap {G1}-{G2}' only when a gap would open), accepted 'Moved {N} Rails by {D} frames - set now frames {A}-{B}.' with singular 'Moved 1 Rail', straddle sentence verbatim, 'No empty space in that direction.'; live readout flows through the capsule pendingOperation chain only"
    requirement: BMOV-06
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#mapRotoRailSetMoveProductReason locked-string cases (43.6-03 Task 2)"
        status: pass
    human_judgment: false
  - id: D5
    description: "prepareRailSetMove / commitRailSetMove ports: intent shape from set members + clamped delta, proposal version attached, commit revalidates the CURRENT proposal version before dispatch and fails closed with zero mutation on mismatch (T-43.6-02)"
    requirement: BMOV-06
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts#prepareRailSetMove/commitRailSetMove port cases (43.6-03 Task 2)"
        status: pass
    human_judgment: false
  - id: D6
    description: "D-06 set aftermath: recordRailSetSnapshot keyed by the accepted operationId before dispatch, resolveRailSetPostAcceptance on 'move-rails' acceptance (keep set) and on undo/redo via historyProvenance.historyCommandId — the set stays selected at the new positions and restores exactly"
    requirement: RSET-03
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts#move-rails aftermath cases (43.6-03 Task 2)"
        status: pass
    human_judgment: false

# Metrics
duration: 2h 30m
completed: 2026-08-18
status: complete
---

# Phase 43.6 Plan 03: Batch Move Rails Gesture Summary

**The batch Move Rails gesture end-to-end: a port-injected pointer-session hook (template: usePhysicsPaintKeyRailDrag) driving per-member ghost/gap/blocked-edge preview paint, committing the retained clamped publication through the Plan 02 `move-rails` resolver intent as one atomic history command, with the locked live readout, rejection, zero-delta, and Escape-cancel discipline, and the D-06 set aftermath (stay-selected after commit; exact set restore on Undo/Redo via the Plan 01 side-channel)**

## Performance

- **Duration:** 2h 30m
- **Started:** 2026-08-18T19:30:00Z
- **Completed:** 2026-08-18T22:00:00Z
- **Tasks:** 2
- **Files created:** 2
- **Files modified:** 8

## Accomplishments

- `usePhysicsPaintRailSetDrag` — the batch pointer-session hook mirroring `usePhysicsPaintKeyRailDrag` structure: same session ref lifecycle, capture, grabbing cursor, suppression, cleanup, unmount disposal; the L140 modifier guard verbatim (non-primary/non-left/modifier pointer-downs never start a batch drag — selection gestures keep working during an active set); the 4px horizontal threshold; preview-is-the-commit (the publication prepared during the drag is retained exactly and committed at pointer-up, never recomputed); zero-delta publishes nothing (no commit call, no rejection); Escape capture-phase cancel, pointercancel, and lostpointercapture clean up exactly once with byte-untouched originals; trailing-click suppression arms only when a session actually started. The preview state carries only the SET-level result `{ delta, blockedEdge, collidingMemberId, gapIntervals }` — the hook owns NO member geometry.
- D-08 collapse-first routing in both rail families (PhysicsPaintKeyRail + PhysicsPaintLoopClipRail): a set member's pointer-down hands the event to the batch session; a non-member runs its own 43.3/43.4 drag unchanged; a set member's trailing click consumes the batch suppression first, never the own-drag suppression.
- Per-member ghost/gap/blocked-edge preview paint in the strip: ghosts at the clamped destination in kind base colors at 55% opacity (Motion #8B5CF6 / Static #06B6D4 / Key Rail #8A939C) on a layer between 7 and 8 (z-index 8), the 2x12px #FF6B6B blocked-edge bar on the colliding member's ghost blocked edge per the direction rule, would-open gaps previewing as ordinary roto-fill-empty cells (byte-identical to the 43.2-43.5 gap treatment), all aria-hidden + pointer-events none and removed on commit/cancel/reject.
- `prepareRailSetMove` / `commitRailSetMove` ports in useRotoTimelineActions.ts mirroring prepareRotoPush/commitRotoPush: prepare builds the move-rails intent from the set members + clamped delta and attaches the proposal version; commit revalidates the CURRENT proposal version before dispatch (T-43.6-02) and fails closed with zero mutation on mismatch; acceptance publishes the locked D-12 copy via the one mapper.
- One deterministic copy mapper `mapRotoRailSetMoveProductReason` (D-27) with kinds disabled/rejected/live/accepted: the live readout 'Move Rails +{D} - set frames {A}-{B} -> {A'}-{B'}' (U+2212 MINUS SIGN for leftward, ', gap {G1}-{G2}' only when a gap would open), the accepted 'Moved {N} Rails by {D} frames - set now frames {A}-{B}.' with singular 'Moved 1 Rail by ...', the straddle sentence verbatim, and 'No empty space in that direction.' for zero-movement rejection (no-space delegates to mapRotoGroupDragProductReason).
- The live readout publishes through the capsule pendingOperation chain only (Pitfall 7): `pushDragFeedback ?? railSetDragFeedback ?? rotoDragFeedback ?? (keyUtilitiesDisabledByBusyState ? getRotoKeyBusyStatus : null)` — hover alone never changes the capsule.
- D-06 set aftermath in the Studio: `recordRailSetSnapshot(operationId, current, current)` at acceptance (identities are move-stable), `resolveRailSetPostAcceptance` routed on 'move-rails' acceptance and on undo/redo via `accepted.historyProvenance?.historyCommandId ?? accepted.operationId` (the ORIGINAL command id, never the replay command's own id) — the set stays selected at the new positions with the anchor following its rail, Undo restores the set at the original positions, Redo re-applies at the destination.
- Full verification: 144 targeted tests (useRotoTimelineActions 100, usePhysicsPaintRailSetDrag 14, physicsPaintRotoRailSetSelection 30), typecheck green, full physic-paint suite 1794 tests green (83 files).

## Task Commits

Each task was committed atomically (TDD RED/GREEN):

1. **Task 1 (tracer): usePhysicsPaintRailSetDrag pointer-session hook with per-member preview state**
   - `4b800238` (test: add failing rail-set drag session tests)
   - `944614f8` (feat: implement rail-set drag session hook)
2. **Task 2 (auto): Strip/rail wiring, ghost + gap + blocked-edge paint, prepare/commit ports, locked copy, and D-06 set aftermath**
   - `8b881635` (test: add failing batch Move prepare/commit port tests)
   - `89d6fdbf` (feat: wire batch Move Rails gesture end-to-end)

**Plan metadata:** `docs(43.6-03): complete plan` (final docs commit)

## Files Created/Modified

- `app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.ts` (created) - the batch pointer-session hook: `RailSetDragSessionApi`, `RailSetDragPreviewState` ({ delta, blockedEdge, collidingMemberId, gapIntervals }), the L140 modifier guard verbatim, 4px threshold, preview-is-the-commit, zero-delta no-publish, Escape/pointercancel/lostpointercapture cleanup, click suppression armed only after start, injectable WindowLike/SourceElement surfaces
- `app/src/components/physic-paint/hooks/usePhysicsPaintRailSetDrag.test.ts` (created) - hoisted-mock harness + WindowDouble/SourceDouble; 14 cases: modifier-guard no-start, sub-threshold release (click falls through), threshold start, preview-is-the-commit, zero-delta publishes nothing, prepare-failure rejection at drop, Escape/pointercancel/lostpointercapture cleanup exactly once, click suppression armed only after start
- `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` - `prepareRailSetMove` (L2481) / `commitRailSetMove` (L2593) ports, `RotoRailSetMovePublication` (L476), `mapRotoRailSetMoveProductReason` (L531), `buildRailSetMoveProposalVersion` (L2969), `deriveRotoRailSetMovePresentationFacts` (L2995), `buildRailSetTooltipSentence` (M1 set tooltip line)
- `app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts` - prepare/commit port cases (intent shape, version revalidation, copy strings via the mapper, rejection mapping) + tooltip sentence cases
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` - `railSetMoveMembers` memo + ref, `usePhysicsPaintRailSetDrag` wiring with clampDelta/prepareAtDelta/onDropCommit/onRejected/onPreviewChange ports, `railSetPaintTick` signal, per-member ghost rails memo, beforeRange/afterRange derivation, gap preview frame set, `railSetDragFeedback` live readout, capsule composition, ghost layer JSX, rail props `onRailSetDragPointerDown` + `onRailSetDragClickSuppressed`, `data-rail-set-paint-tick` section attribute
- `app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx` - `onRailSetDragPointerDown` / `onRailSetDragClickSuppressed` props on both the rail and target props; D-08 pointer-down routing; batch suppression first in handleClick
- `app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx` - 2 new tests: D-08 routing (set member -> batch session, non-member -> own drag, trailing click consumes batch suppression) and UI-SPEC M3 CSS assertions (55% kind colors, 2x12px #FF6B6B bar, z-index 8 layer)
- `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx` - identical D-08 routing pattern (props on both the rail and target props, batch suppression first in handleClick, pointer-down routing)
- `app/src/components/physic-paint/physicsPaintStudio.css` - `.physics-paint-rail-set-ghost-layer` (z-index 8), `.physics-paint-rail-set-ghost` (+ `.mode-static`, `.key-rail`), `.physics-paint-rail-set-blocked-edge` (2x12px #FF6B6B)
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - `railSetMoveMembers` memo resolving `effectiveRailSetSelection?.members` against keyRailSegments, `handleRotoRailSetMoveRejected`, D-06 aftermath in consumeBridgeApplyResult (recordRailSetSnapshot + resolveRailSetPostAcceptance), strip prop bundle wiring

## Decisions Made

- **Set-level preview state (D-17):** the hook's preview carries only `{ delta, blockedEdge, collidingMemberId, gapIntervals }`; per-member ghost geometry and beforeRange/afterRange are derived by the strip from member intervals + delta — the hook owns NO member geometry and imports nothing from the resolver, store, or Studio (ports only).
- **No-space clamp forwarding:** the strip's clampDelta port forwards the PROPOSED delta for the no-space case (clamp ok:false) so prepare fails with the no-space rejection; the hook's D-13 zero-delta skip is reserved for origin return; blockedEdge/collidingMemberId are normalized to null when unclamped.
- **D-08 collapse-first routing:** a set member's pointer-down hands the event to the batch session (never the own 43.3/43.4 drag); a non-member runs its own drag unchanged; collapse is a click-time selection side-effect already in the Studio (plain click clears railSetSelection).
- **D-06 aftermath keying:** recordRailSetSnapshot keyed by the accepted operationId before dispatch; undo/redo lookups use `accepted.historyProvenance?.historyCommandId ?? accepted.operationId` — the ORIGINAL command id, never the replay command's own id (verified via rotoCoordinatorPorts.ts L180-186 and the PhysicPaintRotoPhysicalEditReplayProvenance type).
- **Member resolution:** RailSetIdentity (no keyIds) is NOT assignable to PhysicPaintRailSetMoveMember (requires keyIds); the Studio resolves members against freshly derived keyRailSegments in a memo (key-rail -> { kind: 'key-rail', firstKeyId, keyIds }, loop -> { kind: 'loop', loopId }).
- **Progressive mode widening:** loop clip mode includes 'progressive'; the ghost kind class treats non-static as purple (kind class 'key-rail' for key rails, 'mode-static' for static, default purple for motion/progressive).
- **One mapper (D-27):** mapRotoRailSetMoveProductReason with kinds disabled/rejected/live/accepted; straddle sentence verbatim (Group-domain vocabulary); no-space delegates to mapRotoGroupDragProductReason; the resolver status text stays concise and internal.
- **Live readout gate (Pitfall 7):** the drag feedback publishes through the capsule pendingOperation chain only — no direct capsule/status write from the drag preview path.

## Deviations from Plan

### Auto-fixed Issues

**1. [Typecheck] Unused `RailSetDragPreviewState` import in the strip**
- **Found during:** Task 2 GREEN (strip wiring)
- **Issue:** The strip imported `RailSetDragPreviewState` but the preview is read through `railSetDragApiRef.current?.preview`, so the type import was unused (TS6133).
- **Fix:** Removed the unused type import from the strip.
- **Files modified:** PhysicsPaintWorkflowStrip.tsx
- **Verification:** typecheck green; full suite green.
- **Committed in:** 89d6fdbf (Task 2 GREEN commit)

**2. [Typecheck] `RailSetDragSessionApi` is not generic**
- **Found during:** Task 2 GREEN (strip wiring)
- **Issue:** The hook's `RailSetDragSessionApi` interface has no type parameter, but the strip declared `useRef<RailSetDragSessionApi<RotoRailSetMovePublication> | null>` (TS2315).
- **Fix:** Declared `useRef<RailSetDragSessionApi | null>` — the session API is publication-agnostic; the publication type flows through the ports.
- **Files modified:** PhysicsPaintWorkflowStrip.tsx
- **Verification:** typecheck green; full suite green.
- **Committed in:** 89d6fdbf (Task 2 GREEN commit)

**3. [Typecheck] Loop clip mode includes 'progressive'**
- **Found during:** Task 2 GREEN (strip wiring)
- **Issue:** The ghost rails memo typed the mode as `'static' | 'motion' | null`, but the loop clip mode union includes 'progressive' (TS2322).
- **Fix:** Widened the ghost rail mode type to `'motion' | 'static' | 'progressive' | null` in both the ghost rails memo and `railSetGhostRailKindClass`; the kind class treats non-static as purple.
- **Files modified:** PhysicsPaintWorkflowStrip.tsx
- **Verification:** typecheck green; full suite green.
- **Committed in:** 89d6fdbf (Task 2 GREEN commit)

---

**Total deviations:** 3 auto-fixed (3 typecheck corrections)
**Impact on plan:** All three corrections align the wiring with the hook's actual exported surface; no behavior change and no scope creep.

## TDD Gate Compliance

- **Task 1 (tracer tdd):** RED (`4b800238`) then GREEN (`944614f8`) — gate sequence satisfied; the Wave 0 obligation for usePhysicsPaintRailSetDrag.test.ts satisfied by Task 1 RED.
- **Task 2 (auto tdd):** RED (`8b881635`) then GREEN (`89d6fdbf`) — gate sequence satisfied.

## Issues Encountered

- **RailSetIdentity vs PhysicPaintRailSetMoveMember mismatch (design discovery):** the selection's key-rail member identity lacks `keyIds`, so `railSetSelection.value?.members` is NOT directly assignable to the move member type; resolved by the Studio's `railSetMoveMembers` memo resolving against freshly derived keyRailSegments.
- **Undo/redo snapshot keying (design discovery):** the accepted undo/redo output carries `historyProvenance.historyCommandId` (the ORIGINAL command id); the snapshot lookup must use that, never the replay command's own operationId — verified via rotoCoordinatorPorts.ts L180-186 and the PhysicPaintRotoPhysicalEditReplayProvenance type.
- **Three typecheck corrections** during the strip wiring (unused import, non-generic session API, 'progressive' mode widening) — see Deviations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 04/05/06 consume the D-06 aftermath carrier and `deriveRailSetOrder` from Plan 01 for clamp/spacing/solo; the batch Move gesture is the reference interaction machinery for the remaining batch operations.
- The D-08 routing seam (onRailSetDragPointerDown / onRailSetDragClickSuppressed) is the shared pattern for any future batch gesture on the rail families.
- The one mapper (D-27) is the copy authority for the remaining batch operation readouts/statuses.
- No blockers. The batch Move Rails gesture is fully wired end-to-end: hook, ports, strip/rail/CSS paint, Studio aftermath, and locked copy are all proven by test.

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-18*

## Self-Check: PASSED

- Created files verified: 43.6-03-SUMMARY.md
- All 4 task commits verified in git log: 4b800238, 944614f8, 8b881635, 89d6fdbf
