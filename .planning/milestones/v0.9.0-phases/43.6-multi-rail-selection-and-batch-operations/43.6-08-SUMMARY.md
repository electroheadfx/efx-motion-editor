---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 08
subsystem: ui
tags: [preact, signals, selection, rail-set, tdd, physics-paint]

requires:
  - phase: 43.6-multi-rail-selection-and-batch-operations (plans 01-04)
    provides: rail-set reducer, cross-type ordering authority, Delete Rails classifier, keyboard routing
provides:
  - seedRailSetSelection pure seed bridge carrying a plain-selected rail into the rail set on the first modifier gesture (UI-SPEC M1)
  - Studio handler wiring seeding from selectedRotoKeyRail.value / selectedLoopClipId.value
  - Synchronous Loop Clip modifier-click commit closing the 250ms Delete-swallow window (G-43.6-8)
affects: [43.6-09, roto-selection, batch-operations]

actuals:
  tokens: 4000
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Seed bridge: plain-selected single rail promotes into the rail-set anchor on the first modifier gesture; active set always wins; malformed identity fails closed"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx

key-decisions:
  - "43.6-08: seedRailSetSelection returns the active set unchanged, seeds a one-member anchored set from a valid single-rail identity, and returns null otherwise so the reducer keeps fail-closed range/union semantics (T-43.6-08-01/04)"
  - "43.6-08: modifier clicks on Loop Clip rails commit synchronously with no 250ms timer — a modifier click is never an open-editor double-click intent (T-43.6-08-03); plain-click timing and the WR-02 dead-zone close are byte-untouched"

patterns-established:
  - "Seed bridge pattern: selection tracks are bridged at the reducer input, never inside the reducer — updatePhysicsPaintRotoRailSetSelection and reconcileRailSetSelection stay byte-identical"

requirements-completed: [RSET-01, RSET-02, BDEL-01]

coverage:
  - id: D1
    description: "Plain-click rail A then Cmd+click rail B yields {A, B} anchored on A; plain-click then Shift+click yields the canonical anchor-to-clicked range (M1)"
    requirement: RSET-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts#seedRailSetSelection (43.6-08 seed bridge)"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#seeds the set from the plain-selected single-rail signals on the first modifier gesture (43.6-08 M1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "First-ever modifier gestures without any prior selection keep fail-closed reducer semantics (null anchor range/union unchanged)"
    requirement: RSET-02
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts#range and union leave state unchanged with no valid anchor"
        status: pass
    human_judgment: false
  - id: D3
    description: "A modifier-clicked Loop Clip rail commits set membership synchronously so Delete/Backspace pressed immediately after routes to Delete Rails (G-43.6-8)"
    requirement: BDEL-01
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx#commits modifier-click set membership synchronously with no single-click timer (43.6-08 G-43.6-8)"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts (keyboard routing suite)"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-08-19
status: complete
---

# Phase 43.6 Plan 08: Rail-Set Seed Bridge and Synchronous Loop Clip Modifier Commit Summary

**Pure seedRailSetSelection bridge carrying a plain-selected rail into the rail-set anchor on the first modifier gesture, plus synchronous Loop Clip modifier-click commit closing the 250ms Delete-swallow window**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-19T07:39:49Z
- **Completed:** 2026-08-19T07:47:32Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments
- seedRailSetSelection pure helper: active set wins, valid single-rail identity seeds a one-member anchored set, malformed/null input fails closed — closing root causes RC-B (G-43.6-3/4/8 member loss) and RC-D (G-43.6-1 anchor-less range/union) at the reducer input
- Both Studio modifier branches (handleSelectRotoKeyRail, handleSelectRotoLoopClip) seed from selectedRotoKeyRail.value / selectedLoopClipId.value so plain-click A then Cmd+click B yields {A, B} anchored on A and plain-click then Shift+click yields the canonical range (UI-SPEC M1)
- Loop Clip modifier clicks commit onSelectLoopClip synchronously (no 250ms timer), so the rail is .selected immediately and Delete/Backspace pressed right after routes through isPhysicsPaintRotoDeleteTarget to the Delete Rails classifier (G-43.6-8)
- Reducer fail-closed semantics for genuinely anchor-less/malformed input untouched: updatePhysicsPaintRotoRailSetSelection and reconcileRailSetSelection byte-identical (T-43.6-08-04)

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — seedRailSetSelection helper tests** - `3b3925af` (test)
2. **Task 2: GREEN — helper implementation + Studio seed wiring** - `791aa174` (fix)
3. **Task 3: Loop Clip modifier clicks commit synchronously** - `1eedd80c` (fix)

**Plan metadata:** recorded in the final docs commit for this plan

## Files Created/Modified
- `app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.ts` - seedRailSetSelection pure seed bridge export
- `app/src/components/physic-paint/roto/physicsPaintRotoRailSetSelection.test.ts` - seven seed-bridge and plain-to-modifier transition specs
- `app/src/components/physic-paint/PhysicsPaintStudio.tsx` - both modifier branches derive singleIdentity from the single-rail signals and pass the seeded set into the reducer
- `app/src/components/physic-paint/PhysicsPaintStudio.test.ts` - updated two reducer-routing source contracts plus a new M1 bridge contract test
- `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx` - synchronous modifier-commit branch before the plain-click timer
- `app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx` - fake-timers test pinning synchronous modifier commit and preserved plain-click delay

## Decisions Made
- Seeding lives at the reducer input (seedRailSetSelection) rather than inside updatePhysicsPaintRotoRailSetSelection so the reducer's fail-closed contract stays byte-identical and auditable (T-43.6-08-04)
- Modifier clicks reset lastClickTimestampRef to null after the synchronous commit so a following plain click can never be misread as a double-click against a stale timestamp

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Source-contract assertion required single-line reducer call**
- **Found during:** Task 2 (GREEN — Studio seed wiring)
- **Issue:** The plan-mandated source-contract assertion `updatePhysicsPaintRotoRailSetSelection(currentSet,` did not match the multi-line call formatting I first wrote, failing the two updated source-contract tests
- **Fix:** Reformatted both reducer calls so `currentSet` sits on the call's opening line, matching the asserted source text exactly
- **Files modified:** app/src/components/physic-paint/PhysicsPaintStudio.tsx
- **Verification:** Both Studio source-contract tests and the new M1 bridge test pass (112/112 in the two suites)
- **Committed in:** 791aa174 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Formatting-only alignment with the plan's own source-contract assertion; zero behavior change, no scope creep.

## Issues Encountered
None beyond the formatting deviation above. RED failed exactly as planned (import/undefined error, 7 failing specs); GREEN passed on the first implementation.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- G-43.6-1, G-43.6-3 (member-loss part), G-43.6-4, and G-43.6-8 (delete-window part) are closed by this plan; the seed bridge and synchronous commit are in place for plan 43.6-09
- Full physic-paint suite green (1890 tests, 85 files) and `tsc --noEmit` clean
- Native UAT confirmation of the plain-to-modifier gesture algebra and the immediate Delete-after-modifier-click routing remains the user's surface-level verification

## Self-Check: PASSED

- All 6 modified files verified present on disk
- All 3 task commits verified in git history: 3b3925af, 791aa174, 1eedd80c

---
*Phase: 43.6-multi-rail-selection-and-batch-operations*
*Completed: 2026-08-19*
