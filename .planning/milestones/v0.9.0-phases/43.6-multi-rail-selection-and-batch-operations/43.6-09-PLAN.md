---
phase: 43.6-multi-rail-selection-and-batch-operations
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
autonomous: false
requirements: [SOLO-02, SOLO-06]
gap_closure: true
gap_ids: [G-43.6-2, G-43.6-7]

estimate:
  tokens: 20000
  raw_tokens: 10000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "While Solo is armed, the strip Solo button shows the 43.5 armed-tool orange language (border #F59E0B, background rgba(245,158,11,0.2), icon/label #FBBF24) and keeps it until exit — the armed signal already persists through solo playback."
    - "Disarmed rendering of the Solo button is byte-identical to the pre-fix state (the added base class carries no visual when not armed)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  key_links:
    - "Solo button className template (L3330) -> compound CSS selector .physics-paint-push-tool-button.physics-paint-push-tool-armed (physicsPaintStudio.css L2343/2349-2350)"
    - "soloArmedClass conditional (L1329) -> armed class suffix appended to the base class"
---

<objective>
Make the Solo button render the armed orange tint while solo playback is active.

Purpose: Root cause RC-C (G-43.6-2 and G-43.6-7) — the Solo button className template
`physics-paint-roto-key-icon-button${soloArmedClass}` (PhysicsPaintWorkflowStrip.tsx L3330) reuses the
.physics-paint-push-tool-armed class NAME but omits the sibling .physics-paint-push-tool-button base class
that the compound CSS selector `.physics-paint-push-tool-button.physics-paint-push-tool-armed`
(physicsPaintStudio.css L2343/2349-2350) requires. With the class string
`physics-paint-roto-key-icon-button physics-paint-push-tool-armed` no rule matches, so the armed class is
inert and the orange tint never renders. The Push button (L3291) carries both classes and renders the tint
correctly — the Solo button is the only divergence. The armed signal itself is correct and persists through
solo playback (all disarm paths are intentional D-14 spec paths). Fix: mirror the Push button's class
template.
Output: the Solo button template carries the base class plus the conditional armed class, a source-contract
test pins it, and a blocking human-verify confirms the visible orange tint.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/PROJECT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md

# Diagnosis (root cause, eliminated hypotheses, exact class strings)
@/Users/lmarques/Dev/efx-motion-editor/.planning/debug/43-6-solo-armed-visual.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: add the physics-paint-push-tool-button base class to the Solo button template and pin it with a test</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <action>
    In PhysicsPaintWorkflowStrip.tsx change the Solo button className template at L3330 from
    `class={`physics-paint-roto-key-icon-button${soloArmedClass}`}` to
    `class={`physics-paint-roto-key-icon-button physics-paint-push-tool-button${soloArmedClass}`}` —
    mirroring the Push button at L3291 exactly. Zero other changes: no new CSS, no color literals, no
    aria/JSX changes, no soloArmedClass changes (L1329 stays). The base class carries no visual when
    disarmed, so the disarmed button renders byte-identically.
    Add a source-contract test in PhysicsPaintWorkflowStrip.test.ts that reads the Solo button template
    (the slice containing 'Solo selected Rails' / the physics-paint-solo-tool-group block) and asserts the
    className template string contains both `physics-paint-push-tool-button` and `${soloArmedClass}` in the
    same template literal — proving the compound selector can match when armed.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>
    The Solo button template carries the base class plus the conditional armed class; the new source-contract test passes and the full strip suite stays green; commit `fix(43.6-09): render the armed orange tint on the Solo button`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: approve the armed orange Solo tint visually in the running app</name>
  <what-built>One-line CSS-class fix: the Solo strip button now carries the .physics-paint-push-tool-button base class, so the existing .physics-paint-push-tool-armed compound selector renders the orange armed tint while solo is active.</what-built>
  <how-to-verify>
    Run the app locally (the dev server is user-run per project convention). In the EFX Paint/Roto Studio:
    1. Select any single rail (plain click on a Key Rail or Loop Clip rail).
    2. Click the Solo button in the bottom action row (Focus icon, immediately after the Push tool group).
    3. Confirm the button turns orange: #F59E0B border, translucent orange background, #FBBF24 icon/label, aria-pressed=true — and stays orange while solo playback runs.
    4. Re-click Solo (or press Escape) and confirm the orange tint clears.
    5. Confirm disarmed Solo looks identical to before this fix (the base class adds no disarmed visual).
  </how-to-verify>
  <resume-signal>Type "approved" if the orange armed tint renders and clears correctly, or describe what is still off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| class template -> CSS compound selector | the armed visual depends on the exact class pairing; no user-controlled input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.6-09-01 | Tampering | Solo button className template | low | mitigate | Change is limited to the base-class literal; the armed signal, aria-pressed, disarm lifecycle, and CSS rules are untouched |
| T-43.6-09-02 | Spoofing | armed visual vs armed state | low | accept | The tint now follows the existing armed signal 1:1 through the compound selector; no new state path introduced |
</threat_model>

<verification>
- `cd /Users/lmarques/Dev/efx-motion-editor && pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` passes.
- Human-verify checkpoint confirms the orange tint renders and clears (blocking).
</verification>

<success_criteria>
- The Solo button shows and keeps the orange armed tint while solo is active and clears on exit; disarmed rendering is unchanged.
- G-43.6-2 and G-43.6-7 closed by this plan.
</success_criteria>

<output>
Create `.planning/phases/43.6-multi-rail-selection-and-batch-operations/43.6-09-SUMMARY.md` when done
</output>
