---
phase: 44-integrated-uat-signed-release
plan: 03
type: execute
wave: 3
depends_on:
  - 44-02
files_modified:
  - .planning/phases/44-integrated-uat-signed-release/44-VERIFY-DOWNLOADED.md (NEW — downloaded-artifact evidence record)
  - .planning/phases/44-integrated-uat-signed-release/44-STOP-CONDITIONS.md (NEW — hard-gate checklist record)
autonomous: false
requirements:
  - REL-03
user_setup:
  - service: "GitHub Releases download"
    why: "REL-03 requires verifying a SEPARATELY DOWNLOADED artifact (icon caches lie; the dev-machine bundle cannot prove the distribution path)"
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A v0.9.0 GitHub draft release exists with the DMG uploaded before any download verification"
    - "The downloaded artifact passes `bash scripts/macos-release.sh verify-downloaded <dmg>` → DOWNLOADED ARTIFACT PASS (REL-03)"
    - "The user installs the downloaded app into Applications and launches it normally — no Control-click/Open, no Gatekeeper bypass"
    - "The 'do not publish if' stop-condition checklist (spec Phase 5) is recorded with ZERO active conditions before publish (D-07)"
    - "v0.9.0 is published as GitHub Latest only after verify-downloaded + install/launch + stop-condition checklist pass (D-06)"
  artifacts:
    - GitHub release draft `v0.9.0` with the DMG asset attached
    - .planning/phases/44-integrated-uat-signed-release/44-VERIFY-DOWNLOADED.md (DOWNLOADED ARTIFACT PASS + install/launch evidence)
    - .planning/phases/44-integrated-uat-signed-release/44-STOP-CONDITIONS.md (every stop condition recorded, zero active)
    - GitHub release `v0.9.0` marked Latest (final)
  key_links:
    - "44-02 signed UAT pass -> draft upload -> verify-downloaded (DOWNLOADED ARTIFACT PASS) -> install/launch -> stop-condition checklist -> publish Latest"
    - "gh release create --draft -> gh release edit --latest (draft state protects against premature publication)"
    - "stop-condition checklist (D-07) is SEPARATE from UAT pass — a passing UAT never substitutes for the explicit checklist"
---

<objective>
Final publication plan for v0.9.0: create the GitHub draft release and upload the DMG, verify the separately-downloaded artifact (`verify-downloaded`), install + launch normally, run the hard-gate stop-condition checklist, and only then publish as GitHub Latest.

Purpose: Publication is public and one-way; REL-03 demands the distribution path be proven on a downloaded artifact (not the dev machine) and that no release stop condition is active. D-06 locks publish as a separate plan sequenced after verify-downloaded + install/launch; D-07 locks the stop-condition checklist as a hard gate independent of the UAT pass.
Output: GitHub release v0.9.0 published as Latest, with downloaded-artifact verification evidence and a recorded zero-active stop-condition checklist.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/44-integrated-uat-signed-release/44-PATTERNS.md
@/Users/lmarques/Dev/efx-motion-editor/SPECS/milestone-v0.9.0-plan.md (Phase 5 Release stop conditions, lines 611-629)
@/Users/lmarques/Dev/efx-motion-editor/scripts/macos-release.sh (verify-downloaded mode)
@/Users/lmarques/Dev/efx-motion-editor/docs/macos-signed-release.md
</context>

# Artifacts this phase produces

| Artifact | Change | Evidence |
|----------|--------|----------|
| GitHub release draft `v0.9.0` | NEW — created with the DMG attached (Task 1) | `gh release view v0.9.0 --json assets,draft` |
| `.planning/phases/44-integrated-uat-signed-release/44-VERIFY-DOWNLOADED.md` | NEW — DOWNLOADED ARTIFACT PASS + install/launch evidence | `verify-downloaded` output + user launch confirmation |
| `.planning/phases/44-integrated-uat-signed-release/44-STOP-CONDITIONS.md` | NEW — every "do not publish if" item recorded, zero active | checklist record |
| GitHub release `v0.9.0` (Latest) | publish: `gh release edit v0.9.0 --latest` | `gh release view v0.9.0 --json isLatest` → true |

# Flagged assumptions (from the edge probe — surfaced, not auto-resolved)

1. **REL-03 unclassified (edge, manual review)** — the publish decision is a human decision recorded via the stop-condition checklist. The checklist is the audit trail; it is not an automated assert.
2. **REL-03 download idempotency (edge)** — downloading the uploaded DMG twice yields the same artifact; the clean-session download is what proves the distribution path (Gatekeeper quarantine assessment runs on the downloaded file). If the download fails, re-download and re-run `verify-downloaded` — no silent retry that masks a corrupted artifact.
3. **REL-03 ordering (edge)** — order is significant: draft first, then verify-downloaded, then install/launch, then stop-condition checklist, then publish (D-06). A draft is not public; publication is the one-way step.
4. **REL-03 A4 (assumption)** — the existing v0.8.1 GitHub "Latest" will be superseded by v0.9.0 via `gh release edit --latest`; verify `gh release view v0.9.0` state before editing.

<tasks>

<task type="tracer">
  <name>Task 1: Create GitHub draft release v0.9.0 and upload the signed DMG</name>
  <files>none (evidence in .planning/phases/44-integrated-uat-signed-release/44-VERIFY-DOWNLOADED.md)</files>
  <read_first>
    .planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md — Publish pattern (gh release draft → upload → verify → publish), A4 assumption, Evidence ledger
    scripts/macos-release.sh — read-only; verify-downloaded mode contract
  </read_first>
  <action>
    1. Confirm the signed DMG from plan 02 exists and is named exactly `app/src-tauri/target/release/bundle/dmg/EFX Motion Editor_0.9.0_aarch64.dmg`.
    2. Create the GitHub draft release with the DMG attached (do NOT publish yet — the draft state is the protection against premature publication):
       gh release create v0.9.0 --draft --title "EFX Motion Editor v0.9.0" app/src-tauri/target/release/bundle/dmg/*_0.9.0_*.dmg
    3. Verify the draft state and the asset attachment:
       gh release view v0.9.0 --json draft,assets --jq '{draft, assets: [.assets[].name]}'
       Expect draft=true and the assets list to contain the DMG filename.
    4. Record the release URL and asset name in 44-VERIFY-DOWNLOADED.md (the download path the user will verify in Task 2).
  </action>
  <verify>
    <automated>gh release view v0.9.0 --json draft --jq .draft 2>&1 | grep -q true && gh release view v0.9.0 --json assets --jq '.assets[].name' 2>&1 | grep -q 'EFX Motion Editor_0.9.0_aarch64.dmg' && echo "draft + DMG attached"</automated>
  </verify>
  <acceptance_criteria>
    - `gh release view v0.9.0 --json draft --jq .draft` → `true`
    - `gh release view v0.9.0 --json assets --jq '.assets[].name'` → contains `EFX Motion Editor_0.9.0_aarch64.dmg`
    - the release is still in DRAFT state (not published)
  </acceptance_criteria>
  <done>
    Draft release v0.9.0 exists with the DMG uploaded; still draft (not Latest); evidence recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify the downloaded artifact (verify-downloaded) + install into Applications + normal launch</name>
  <read_first>
    .planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md — D-05 (verify on the downloaded artifact, icon caches lie)
    docs/macos-signed-release.md — verify-downloaded flow + evidence checklist
    scripts/macos-release.sh — verify-downloaded mode contract (read-only)
  </read_first>
  <what-built>
    The GitHub draft release v0.9.0 DMG (uploaded in Task 1). REL-03 requires the signed/notarized artifact to be verified in a SEPARATE download path — the dev machine's local bundle cannot prove the distribution path.
  </what-built>
  <how-to-verify>
    USER side:
    1. Download `EFX Motion Editor_0.9.0_aarch64.dmg` from the draft release URL (a clean session / fresh download — not the local bundle path).
    2. Report the downloaded DMG absolute path to the agent.

    AGENT side:
    3. Run (credential-free, gate does not touch Apple secrets):
       bash scripts/macos-release.sh verify-downloaded /absolute/path/to/EFX\ Motion\ Editor_0.9.0_aarch64.dmg
       Expect `DOWNLOADED ARTIFACT PASS` + the "normal visible launch remains a required user-owned check" note.
    4. Record the output in 44-VERIFY-DOWNLOADED.md.

    USER side (continue):
    5. Open the downloaded DMG, drag the app into Applications.
    6. Launch the installed app NORMALLY (double-click; NO Control-click/Open, NO Gatekeeper bypass). The app must launch visibly and run.
    7. Confirm the icon on the launched/dowloaded app surface (not the dev machine — icon caches lie per D-05).
    8. Report launch success/failure to the agent, who records it.
  </how-to-verify>
  <acceptance_criteria>
    - `bash scripts/macos-release.sh verify-downloaded <dmg>` → `DOWNLOADED ARTIFACT PASS`
    - The app launched normally from Applications without any Gatekeeper bypass
    - Icon verified on the downloaded artifact, not the dev machine
  </acceptance_criteria>
  <resume-signal>Type "downloaded, installed, launched normally" (or describe any Gatekeeper/interference issue).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Stop-condition checklist (hard gate) + publish v0.9.0 as GitHub Latest</name>
  <read_first>
    SPECS/milestone-v0.9.0-plan.md — Phase 5 Release stop conditions, lines 611-629 (the authoritative "do not publish if" list)
    .planning/phases/44-integrated-uat-signed-release/44-CONTEXT.md — D-07 (hard gate, explicit checklist recorded), D-08 (stop and flag)
    .planning/phases/44-integrated-uat-signed-release/44-RESEARCH.md — Pattern/Pitfall 6 (chunk budget), Pitfall 5 (truncation label)
  </read_first>
  <decision>
    Publish EFX Motion Editor v0.9.0 as GitHub Latest?
  </decision>
  <context>
    Publication is public and one-way (REVERSIBILITY: one-way). The release is a hard gate: every "do not publish if" stop condition from spec Phase 5 must be individually recorded and confirmed NOT active before `gh release edit v0.9.0 --latest`. This checklist is SEPARATE from the UAT pass — a green UAT never substitutes for the explicit stop-condition record (D-07). Any active condition blocks publication and flags the user (D-08).
  </context>
  <options>
    <option id="publish">
      <name>Publish v0.9.0 as GitHub Latest</name>
      <pros>Completes the milestone on the 2026-08-31 target; matches the proven v0.8.1 flow</pros>
      <cons>Public and irreversible; any post-publish defect becomes a hotfix release</cons>
    </option>
    <option id="hold">
      <name>Hold publication (do not publish)</name>
      <pros>No public exposure while any stop condition remains active</pros>
      <cons>Misses the publish date; requires a later publish decision</cons>
    </option>
  </options>
  <action>
    AGENT: record the explicit "do not publish if" stop-condition checklist in 44-STOP-CONDITIONS.md BEFORE presenting the checkpoint. Each item is confirmed inactive with the evidence source (e.g., "UAT step 12 passed", "preflight PASS"):
    1. Existing project scripts or Save Script require manual Refresh after opening a saved project → NOT ACTIVE (UAT step 3)
    2. Automatic hydration duplicate scans / stale project-layer rows / unsaved-project gating broken → NOT ACTIVE (UAT step 4)
    3. Old unreadable icon / placeholder / incorrect opaque corners / missing icon on any required macOS surface → NOT ACTIVE (UAT step 1 + 44-02 icon evidence)
    4. Packaged icon metadata or ICNS validation fails → NOT ACTIVE (releaseContract.test.ts + preflight)
    5. Audio drifts from Paint playback → NOT ACTIVE (UAT step 6)
    6. EFX Paint mutates main-editor audio tracks → NOT ACTIVE (UAT step 7)
    7. Closing EFX Paint leaves audio playing → NOT ACTIVE (UAT steps 5-7)
    8. Import cleanup creates startup or bridge regressions → NOT ACTIVE (full gate suite + UAT)
    9. Progressive PlayScript output changes unexpectedly → NOT ACTIVE (UAT step 8)
    10. Hold Loop repetitions duplicate durable source assets or render a different source cycle on later repeats → NOT ACTIVE (UAT steps 9-11)
    11. Finite/infinite loops overlap the next clip, ignore a partial-cycle interruption, or change after reopen → NOT ACTIVE (UAT steps 10-13, 16)
    12. The timeline uses the ambiguous term `clip bloquant` instead of the next-clip explanation → NOT ACTIVE (UAT step 12 asserts the English label; the term is prohibited in every language)
    13. Static output changes after reopen/regeneration → NOT ACTIVE (UAT step 16)
    14. Cancellation leaves partial keys or loop metadata → NOT ACTIVE (gate suite + UAT)
    15. Preview/export or release validation fails → NOT ACTIVE (Phase 43 signed boundary UAT step 17 + verify-downloaded PASS)

    If every item is NOT ACTIVE: present the decision to the user. On the user's explicit decision to publish, run:
       gh release edit v0.9.0 --latest
    then verify:
       gh release view v0.9.0 --json isLatest --jq .isLatest → true
       gh release view v0.9.0 --json assets --jq '.assets[].name' → contains the DMG
    Record the publish confirmation in 44-STOP-CONDITIONS.md.
  </action>
  <verify>
    <automated>gh release view v0.9.0 --json isLatest --jq .isLatest 2>&1 | grep -q true && gh release view v0.9.0 --json assets --jq '.assets[].name' 2>&1 | grep -q 'EFX Motion Editor_0.9.0_aarch64.dmg' && echo "v0.9.0 published as Latest with DMG"</automated>
  </verify>
  <acceptance_criteria>
    - 44-STOP-CONDITIONS.md records every "do not publish if" item with zero active
    - `gh release view v0.9.0 --json isLatest --jq .isLatest` → `true`
    - the DMG asset is attached to the published release
  </acceptance_criteria>
  <resume-signal>Type "publish" to approve publication as Latest, or "hold" to defer.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API → release | The `gh` CLI (authenticated as electroheadfx) uploads and edits release assets; the draft state protects against premature publication |
| Downloaded artifact → user | The downloaded DMG crosses Gatekeeper; normal launch must succeed without any bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-09 | Tampering | uploaded release asset | high | mitigate | `verify-downloaded` on the SEPARATELY downloaded DMG → DOWNLOADED ARTIFACT PASS (REL-03); draft state prevents public distribution before verification |
| T-44-10 | Spoofing | gatekeeper bypass | high | mitigate | normal visible launch without Control-click/Open recorded; any bypass attempt is a failure (UAT 17 / install step) |
| T-44-11 | Repudiation | stop-condition checklist | medium | mitigate | D-07 hard gate: every "do not publish if" item recorded with evidence source in 44-STOP-CONDITIONS.md before publish |
| T-44-12 | Tampering | gh CLI state | medium | mitigate | verify draft→published transition via `gh release view --json isLatest` after every state change |
</threat_model>

<verification>
1. `gh release view v0.9.0 --json draft --jq .draft` → true (Task 1)
2. `bash scripts/macos-release.sh verify-downloaded <downloaded.dmg>` → DOWNLOADED ARTIFACT PASS (Task 2)
3. Normal launch in Applications without Gatekeeper bypass reported by user (Task 2)
4. 44-STOP-CONDITIONS.md zero active (Task 3)
5. `gh release view v0.9.0 --json isLatest --jq .isLatest` → true (Task 3)
</verification>

<success_criteria>
- Draft release created with the DMG uploaded; still draft before verification.
- Downloaded-artifact verification and normal visible launch complete before publication (REL-003).
- The stop-condition checklist records ZERO active conditions before publish (D-007).
- v0.9.0 published as GitHub Latest only after all three tasks (D-006).
</success_criteria>

<output>
Create `.planning/phases/44-integrated-uat-signed-release/44-03-SUMMARY.md` when done, including: the draft creation/upload evidence, the DOWNLOADED ARTIFACT PASS output, the install/launch confirmation, the stop-condition checklist record, and the final isLatest verification.
</output>
