---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 13
type: execute
wave: 10
depends_on:
  - 43-12
files_modified:
  - app/src/lib/frameMap.ts
  - app/src/lib/frameMap.test.ts
  - app/src/types/timeline.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/components/timeline/TimelineInteraction.test.ts
  - app/src/components/timeline/TimelineCanvas.tsx
autonomous: true
gap_closure: true
requirements:
  - HOLD-05
  - HOLD-06
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-53: the Motion Editor main timeline retains only a passive marker type/projection/renderer carrying `{startFrame, frameCount, mode}` and painting 3px mode-colored strips with white canonical endpoint cuts inside the existing PPaint FX bar; no `loopCapsules`, `loopClips`, IDs, source keys, repeat metadata, status, selection, callbacks, commands, hit region, tooltip, hover/focus state, keyboard route, context action, edit command, drag, navigation, mutation, or hidden shortcut remains."
    - "D-34R/D-36R: rich Motion Editor capsule ownership is removed here; the now-unmounted tooltip/geometry and rejected EFX lane/style residue are explicitly handed to dependent Plan 43-14 for deletion while the passive marker type/projection/renderer/tests are protected."
    - "D-42: generic main-timeline rendering/interaction and EFX physical-cell gesture, drag, toolbar geometry, and linked indicators remain regression-green; rail activation may clear physical selection only through the D-57 mutually exclusive selection boundary."
    - "D-46/D-58: canonical persistence, resolver, controller, complete physical-edit authority, accepted background, preview/playback, save/reopen, and export consumption remain intact."
    - "D-57: rail-owned selection, exact one-cycle physical authorization, cumulative ripple, source-attached placement follow, unchanged Interpolation, atomic records-plus-Loop-Clips history, and fail-closed provenance remain protected while obsolete rich capsule interaction is removed."
    - "D-57 does not broaden the Motion Editor boundary: selection and Key Spacing remain EFX-local; the main timeline receives only passive `{startFrame, frameCount, mode}` paint intervals."
    - "No invisible Loop Clip pointer or keyboard surface survives the visual cleanup."
  artifacts:
    - path: "app/src/lib/frameMap.ts"
      provides: "generic frame map with interval-only passive marker projection and no Loop Clip identity or metadata"
    - path: "app/src/types/timeline.ts"
      provides: "minimal passive marker type with stale rich TimelineLoopCapsule types removed"
    - path: "app/src/components/timeline/TimelineInteraction.ts"
      provides: "generic timeline input routing without Loop Clip ownership"
    - path: "app/src/components/timeline/TimelineCanvas.tsx"
      provides: "generic timeline canvas with no Loop Clip tooltip mount or state"
  key_links:
    - from: "canonical physical-frame document"
      to: "preview/playback/save/export"
      via: "retained store and resolver consumers, not a main-timeline authoring projection"
      pattern: "resolvePhysicPaintRoto"
    - from: "PhysicsPaintWorkflowStrip"
      to: "PhysicsPaintLoopClipRail"
      via: "one integrated host inside the physical row"
      pattern: "PhysicsPaintLoopClipRail"
  prohibitions:
    - statement: "The passive Motion Editor marker may not expand into a read-only summary, identity-bearing `loopCapsules`/`loopClips` projection, rich rail/capsule, badge, label, tooltip, status surface, navigation adapter, or interaction route."
      status: resolved
      verification: automated
    - statement: "The rejected EFX lane may not remain behind display:none, dead CSS, compatibility exports, or stale positive tests."
      status: resolved
      verification: automated
    - statement: "Canonical physical/resolver/controller/preview/export code must not be deleted as collateral cleanup."
      status: resolved
      verification: automated
    - statement: "The D-57 timed source-offset, rail/physical selection, ordered-ripple, placement-follow, interpolation, and atomic-history runtime may not be mistaken for obsolete rich capsule interaction, moved into the Motion Editor, or removed during cleanup."
      status: resolved
      verification: automated
---

<objective>
Replace the rejected Motion Editor rich capsule system with the minimal passive mode-marker type/projection/renderer and remove every Loop Clip-specific Motion Editor interaction after the approved EFX rail/sidebar and D-57/D-58 recovery are complete, while explicitly protecting EFX-local selection, source timing, cumulative ripple, placement follow, atomic settlement, and background authority.

Purpose: Make the corrected ownership boundary structural rather than cosmetic: interval visibility remains in the existing PPaint FX bar, while identity and all Loop Clip interaction remain exclusively in Physics Paint Studio.
Output: Retained interval-only marker type/projection/pure Canvas renderer; removed stale rich capsule types/drawing, input routing, and tooltip mount; dependent Plan 43-14 deletes obsolete helpers and rejected EFX residue without touching the passive marker path.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-12-SUMMARY.md
@app/src/lib/frameMap.ts
@app/src/types/timeline.ts
@app/src/components/timeline/TimelineRenderer.ts
@app/src/components/timeline/TimelineInteraction.ts
@app/src/components/timeline/TimelineCanvas.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve passive marker projection/renderer while removing rich capsule types and drawing</name>
  <files>app/src/lib/frameMap.ts, app/src/lib/frameMap.test.ts, app/src/types/timeline.ts, app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33R and D-46 ownership/consumer boundary)
    - app/src/lib/frameMap.ts (`loopCapsules`, range derivation, capsule builder)
    - app/src/types/timeline.ts (capsule source/capsule/layout types)
    - app/src/components/timeline/TimelineRenderer.ts (now-disabled draw definition/imports)
    - app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts (approved EFX replacement facts/geometry owner)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (protected D-57 source timing, cumulative ripple, and placement follow)
    - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts (protected D-11/D-57 authorization and atomic Force Spacing boundary)
  </read_first>
  <behavior>
    - Sentinel test: `projects only passive Loop Clip intervals to the Motion Editor frame map`.
    - The sentinel asserts the public frame-map result contains only minimal `{startFrame, frameCount, mode}` markers, the renderer paints 3px purple/cyan strips with white actual endpoint cuts inside the existing PPaint FX bar, and ordinary FX ordering/layout remains unchanged without rich capsule fields.
    - Protected focused regressions assert rail/physical selection authority, non-uniform cadence, complete-cycle deduplication, cumulative ripple, source-attached placement follow, unchanged Interpolation, atomic records-plus-Loop-Clips Undo/Redo, and Play Script background parity remain EFX/physical-document owned and green.
  </behavior>
  <action>RED: keep the sentinel `projects only passive Loop Clip intervals to the Motion Editor frame map`. GREEN: preserve one minimal marker type and projection carrying `{startFrame, frameCount, mode}` from canonical effective intervals; paint Progressive purple, Static/Hold cyan, and white cuts only at actual canonical endpoints; remove stale rich capsule types/drawing and every identity-bearing field. Retain canonical physical records, accepted background, resolver/store/controller, preview/playback/save/export consumers, and the full D-57 selection/ripple/placement/history path. Do not import rail selection, source IDs, repeat data, status, tooltip, hit testing, or commands into the Motion Editor. Run the sentinel unchanged, then frame-map/renderer plus the focused D-57/D-58 recovery matrix.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/frameMap.test.ts -t "projects only passive Loop Clip intervals to the Motion Editor frame map" && pnpm --dir app exec vitest run src/lib/frameMap.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Main timeline data exposes only `{startFrame, frameCount, mode}` passive marker intervals; no identity, source/repeat metadata, status, selection, callbacks, or commands survive.
    - The renderer paints exact 3px purple/cyan strips plus white actual endpoint cuts inside the existing PPaint FX bar with no new row/height, text, badge, tooltip, state surface, or hit target.
    - Generic frame-map, FX-track, and renderer behavior remains green.
    - Canonical physical Loop Clip records, allowed source-attached placement follow, accepted background, and resolution remain EFX/physical-document owned.
    - D-57/D-58 selection, cumulative ripple, unchanged Interpolation, atomic Undo/Redo, and background parity remain green.
  </acceptance_criteria>
  <done>The Motion Editor data and rendering pipeline retains only the passive interval marker and contains no rich Loop Clip presentation code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove main-timeline Loop Clip interaction, tooltip, and rich capsule geometry routes</name>
  <files>app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineInteraction.test.ts, app/src/components/timeline/TimelineCanvas.tsx</files>
  <read_first>
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-33R zero-interaction boundary)
    - app/src/components/timeline/TimelineInteraction.ts (capsule hit, hover/focus, selection, keyboard, operation callers)
    - app/src/components/timeline/TimelineCanvas.tsx (tooltip import/mount)
    - app/src/components/timeline/TimelineCapsuleTooltip.tsx (stale main-editor dependencies/actions)
    - app/src/components/timeline/loopCapsuleGeometry.ts (confirm required compact rail derivation is already owned by EFX presentation tests)
  </read_first>
  <behavior>
    - Sentinel test: `ignores former Loop Clip coordinates and keys in the Motion Editor`.
    - The sentinel sends pointer and Enter/Delete/Backspace input at former coordinates and expects no Loop Clip hover, focus, selection, navigation, edit, unlink, or delete intent.
  </behavior>
  <action>RED: add exactly one test named `ignores former Loop Clip coordinates and keys in the Motion Editor` to `TimelineInteraction.test.ts`. Run `pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts -t "ignores former Loop Clip coordinates and keys in the Motion Editor"`. The precise RED failure is that former coordinates or keys still emit a Loop Clip hover/selection/action intent. GREEN: make only the active interaction cleanup required by D-33R: remove every caller, import, mount, hit route, hover/focus publication, keyboard/action route, and other executable reference to the obsolete `TimelineCapsuleTooltip` and `loopCapsuleGeometry` modules from the files owned by this task. Preserve ordinary playhead, layer selection, drag, keyboard, unrelated tooltip/overlay, and playback behavior. The now-unreferenced tooltip/geometry files may remain on disk when 43-13 completes; physical deletion of those dead modules is exclusively Plan 43-14's responsibility. Run `pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts -t "ignores former Loop Clip coordinates and keys in the Motion Editor"` byte-for-byte unchanged; it must pass before broader verification. Only after GREEN run interaction, renderer, EFX rail/sidebar/Edit, timed resolver, selection/ripple guard, coordinator, and loop history regression files.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts -t "ignores former Loop Clip coordinates and keys in the Motion Editor" && pnpm --dir app exec vitest run src/components/timeline/TimelineInteraction.test.ts src/components/timeline/TimelineRenderer.test.ts src/components/physic-paint/view/physicsPaintLoopClipPresentation.test.ts src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.tsx src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/physicsPaintRotoLoopHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The passive marker remains visible, but no visible or invisible Motion Editor Loop Clip-specific input/floating-UI surface remains.
    - Obsolete tooltip and geometry modules have no caller, import, mount, hit route, or executable reference; their now-unreferenced files are explicitly allowed to remain on disk at 43-13 completion.
    - Physical deletion of the dead tooltip and geometry modules is exclusively Plan 43-14's responsibility.
    - Generic timeline interaction and EFX-owned rail/sidebar/Edit suites remain green, with no dedicated popover mount.
  </acceptance_criteria>
  <done>The Motion Editor has zero Loop Clip interaction ownership at pointer, keyboard, selection, tooltip, navigation, action, and mutation layers; only the passive marker projection/painter remains, and the unreferenced rich tooltip/geometry files may remain on disk until Plan 43-14 deletes them.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical physical output → Motion Editor | Main editor may consume resolved pixels and interval-only passive markers but must not acquire Loop Clip identity or authoring UI |
| timeline pointer/keyboard → commands | Removed coordinates and keys must not reach stale Loop Clip operations |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43-13-01 | Spoofing | passive marker expanding into duplicate authoring surface | high | mitigate | Retain only `{startFrame, frameCount, mode}` and pure mode-strip/endpoint paint; remove rich capsule types/drawing, tooltip mounts/imports/callers, identity, source/repeat metadata, and interaction ownership together |
| T-43-13-02 | Elevation of Privilege | invisible hit/keyboard routes | high | mitigate | Behavioral absence tests prove removed coordinates/keys emit no Loop Clip intent |
| T-43-13-03 | Tampering | generic timeline and physical cells | medium | mitigate | Retain focused non-Loop renderer/interaction/cell regressions |
| T-43-13-04 | Information Disclosure | stale tooltip state | low | mitigate | Remove tooltip mount/state and every executable reference; physical component-file deletion remains exclusive to Plan 43-14 |
| T-43-13-05 | Tampering | D-57/D-58 runtime removed as apparent capsule residue | high | mitigate | Run selection/action/resolver/coordinator/history/background regressions and keep all authoring identity EFX-local |
| T-43-13-SC | Tampering | package supply chain | high | mitigate | No package install or dependency change is permitted |
</threat_model>

<verification>
- Run both one-test RED/GREEN commands, then their broader focused regressions.
- Confirm the passive Motion Editor marker type/projection/painter remains exact while rich capsule projection/rendering and all pointer, keyboard, selection, navigation, action, mutation, and tooltip-mount routes are gone; EFX replacement tests remain green.
- Confirm canonical model/resolver/store/controller/preview/export files are unchanged; Plan 43-14 owns deletion of now-unmounted helper and rejected EFX residue.
</verification>

<success_criteria>
- D-33R is structural: only the minimal passive marker type/projection/painter remains; no rich capsule type/draw path, identity, metadata, caller, tooltip import/mount, hit route, hover/focus state, keyboard route, selection, navigation, action, callback, command, or mutation remains.
- D-34R/D-36R active rejected lane/capsule ownership is gone; now-unreferenced tooltip/geometry files are allowed to remain on disk at 43-13 completion because physical deletion is exclusively Plan 43-14's responsibility.
- D-42/D-46 generic and canonical behavior remains green.
- D-57/D-58 rail selection, timed loop ripple, placement/history atomicity, unchanged Interpolation, and Play Script background runtime remain intact, EFX-local or physical-document owned, and regression-green while obsolete rich main-timeline interaction is removed.
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-13-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Main timeline frame map, minimal marker type, pure passive renderer, interaction, and canvas with passive marker only and zero Loop Clip-specific interaction
- Explicit cleanup handoff for now-unmounted tooltip/geometry and rejected EFX lane residue in 43-14
- Absence contracts plus preserved generic timeline and EFX-owned regressions
