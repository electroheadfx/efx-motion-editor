---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 07
type: execute
wave: 4
depends_on:
  - 43-02
  - 43-03
files_modified:
  - app/src/components/timeline/loopCapsuleGeometry.ts
  - app/src/components/timeline/loopCapsuleGeometry.test.ts
  - app/src/types/timeline.ts
  - app/src/lib/frameMap.ts
  - app/src/components/timeline/TimelineRenderer.ts
  - app/src/components/timeline/TimelineRenderer.test.ts
autonomous: true
requirements:
  - HOLD-06

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The capsule is a pure view of the resolver's derived loop intervals — TimelineRenderer never recomputes placement starts, boundaries, or effective durations (D-24, roadmap boundary note)"
    - "First-cycle cells render real thumbnails (downscaled cached PNGs via ThumbnailCache + drawImage); cells backed by real source keys (original loop whose placement overlaps its source keys) keep real-key diamonds; duplicated-loop first-cycle cells show the shared source thumbnails with the dashed linked border and NO diamonds; ghost cells never show thumbnails or diamonds (D-15, D-23, placement/source correction)"
    - "The capsule feed carries ONE compact interval model per Loop Clip — never a per-frame list; ghost cells and band segments are computed only for the visible frame window at draw time (D-32, audit finding 2)"
    - "Badge text is compact math only: `Cycle 5f × 5 = 25f`, `Cycle 5f × ∞`, `Cycle 5f × 1 = 5f`; truncation never changes the badge; the badge never renders `Infinityf` (D-19)"
    - "Repetition rendering is zoom-adaptive: frameWidth ≥ 16px renders ghost linked cells (LOOP_GHOST_FILL, dashed 1px border); 8-15px renders the perforated/hatched band (LOOP_BAND_BASE + LOOP_BAND_HATCH 45°/4px period/1px lines); < 8px renders band + badge only with truncateText below the 18px label minimum (D-16)"
    - "The truncation diagonal draws at every zoom in #FFB020 1.5px: mid-ghost-cell for a partial cycle, exactly on a cycle boundary for complete cycles, on the band end at low zoom (D-21)"
    - "A zero-effective loop renders the greyed anchor flag (~6px-high pill, 24px wide, `0f` marker) pinned at the placement start — never invisible (D-22)"
    - "A timeline with no Loop Clips renders no capsule and no placeholder (S1 empty)"
    - "A loop with unresolvable source refs renders the capsule (or its zero/error marker) with a 2px #FF4444 outline — the capsule never silently disappears, and the unresolved record stays intact across save/reopen (D-23, D-31, S1 stale)"
    - "Capsule visual states follow the locked precedence: error red > keyboard focus ring > selected accent outline > truncation diagonal > hover raise > idle; states change paint only, never geometry (D-23)"
    - "All capsule drawing is Canvas 2D paint calls on the physic-paint FX row — zero DOM nodes per capsule or cell (D-32)"
  artifacts:
    - path: "app/src/components/timeline/loopCapsuleGeometry.ts"
      provides: "pure capsule geometry: band extents, ghost-cell grid, diagonal landing x, anchor-flag position, zoom-band selection, badge text forms"
      exports: ["badgeTextForLoop", "zoomBandForFrameWidth"]
    - path: "app/src/lib/frameMap.ts"
      provides: "loopCapsules projection field on FxTrackLayout fed from the store/resolver derivation"
      contains: "loopCapsules"
    - path: "app/src/components/timeline/TimelineRenderer.ts"
      provides: "capsule drawing: thumbnails, band, ghost cells, badge, diagonal, anchor flag, state outlines"
      contains: "LOOP_BAND_HATCH"
  key_links:
    - from: "app/src/lib/frameMap.ts"
      to: "app/src/stores/physicPaintStore.ts"
      via: "fxTrackLayouts reads the resolver-derived loop ranges from the store, same pattern as rotoKeyFrames"
      pattern: "loopCapsules"
    - from: "app/src/components/timeline/TimelineRenderer.ts"
      to: "app/src/components/timeline/loopCapsuleGeometry.ts"
      via: "capsule drawing consumes pure geometry functions; renderer holds no boundary math"
      pattern: "loopCapsuleGeometry"
  prohibitions:
    - statement: "MUST NOT reuse the dead FxTrackLayout.playScriptMarkers field for the capsule — a new projection field is added (RESEARCH State of the Art)"
      status: resolved
      verification: test
    - statement: "MUST NOT ship the superseded French truncation label or the D-20 prohibited French term anywhere in canvas copy — English `Loop shortened by next clip` only"
      status: resolved
      verification: test
    - statement: "MUST NOT add DOM nodes per capsule/cell or new image-loading pipelines — canvas paint calls and ThumbnailCache only (D-32, D-15)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT change ghost-cell geometry, interpolated-frame visuals, ordinary clips, or the real-key diamond convention — capsule visuals are strictly additive (D-23)"
      status: resolved
      verification: judgment
---

<objective>
Render the filmstrip capsule on the main editor timeline: pure geometry module (unit-testable), the frameMap projection feed, and the TimelineRenderer drawing for source thumbnails, zoom-adaptive repetitions, badge, truncation diagonal, zero-effective anchor flag, and all visual states.

Purpose: HOLD-06's presentation half. The capsule is a pure view of the 43-02 derivation (locked roadmap boundary: capsule ships WITH the resolver); UI-SPEC locks every color, metric, zoom threshold, and copy form.
Output: a physic-paint FX row with Loop Clips renders the complete capsule at all zoom bands and states.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-02-SUMMARY.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-03-SUMMARY.md
@app/src/components/timeline/TimelineRenderer.ts
@app/src/lib/frameMap.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pure capsule geometry module + frameMap projection feed</name>
  <files>app/src/components/timeline/loopCapsuleGeometry.ts, app/src/components/timeline/loopCapsuleGeometry.test.ts, app/src/types/timeline.ts, app/src/lib/frameMap.ts</files>
  <read_first>
    - app/src/components/timeline/TimelineRenderer.ts (getPhysicPaintRotoKeyMarkerGeometry 41-50; drawPhysicPaintPlayScriptMarkers 423-496; truncateText label minimum at 1585; MIN_FRAME_WIDTH_FOR_THUMB at 68)
    - app/src/lib/frameMap.ts (fxTrackLayouts computed 171-205; timeline length derivation 124-153)
    - app/src/types/timeline.ts (FxTrackLayout 45-60; dead playScriptMarkers at 56)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (Spacing zoom bands; Color S1 constants)
  </read_first>
  <behavior>
    - badgeTextForLoop: finite → `Cycle {N}f × {R} = {D}f`; single → `Cycle {N}f × 1 = {N}f`; infinity → `Cycle {N}f × ∞` (never a numeric or spelled-out infinity suffix)
    - zoomBandForFrameWidth: >= 16 → 'high'; 8-15 → 'default'; < 8 → 'low'
    - Ghost-cell grid: repetition region starts at placementStart + cycleLength (never at placementStart); cells are cycleLength frames wide at high zoom
    - Diagonal landing x: partial cycle → mid-ghost-cell containing effectiveEnd; complete cycles → exactly on the cycle boundary at effectiveEnd; low zoom → band end
    - Anchor flag geometry: pinned at placementStart, 24px wide hit basis, ~6px-high pill
    - First-cycle cell classification: a first-cycle cell is real-key-backed iff a real source key exists at that presentation frame (original loop whose placement overlaps its source keys); otherwise it is a linked/virtual first-cycle cell (duplicated loop) — the geometry model carries this flag per first-cycle cell so the renderer chooses diamond+solid border vs no-diamond+dashed border, and 43-08 chooses real-key click vs linked-occurrence click
    - frameMap exposes loopCapsules on FxTrackLayout for physic-paint layers — ONE compact interval model per loop derived from the 43-02 interval derivation through the store (same pattern as the rotoKeyFrames feed), including unresolved state and missing-ref lists; no per-frame structures for virtual occurrences
  </behavior>
  <action>
    RED: create app/src/components/timeline/loopCapsuleGeometry.test.ts covering every behavior case (all three badge forms, zoom-band boundaries at exactly 16px and exactly 8px, diagonal landing for partial vs complete truncation, anchor-flag position, repetition-region start offset); commit test(43-07). GREEN: (1) create app/src/components/timeline/loopCapsuleGeometry.ts with pure exported functions in the getPhysicPaintRotoKeyMarkerGeometry style — badge text forms (locked D-19 strings), zoom-band selection, capsule x-extents, ghost-cell grid, diagonal landing x, anchor-flag position — no store/signal imports, fully unit-testable. (2) app/src/types/timeline.ts: add a TimelineLoopCapsule layout type and an optional loopCapsules field on FxTrackLayout beside rotoKeyFrames; do NOT touch the dead playScriptMarkers field. (3) app/src/lib/frameMap.ts: populate loopCapsules in the fxTrackLayouts computed for physic-paint layers by reading the 43-02 derivation through the store (same pattern as the rotoKeyFrames feed); TimelineRenderer never recomputes anything. Commit feat(43-07).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run loopCapsuleGeometry frameMap</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run loopCapsuleGeometry exits 0
    - The spec asserts the three exact badge strings `Cycle 5f × 5 = 25f`, `Cycle 5f × ∞`, `Cycle 5f × 1 = 5f`
    - grep -c "loopCapsules" app/src/types/timeline.ts returns >= 1 and grep -c "loopCapsules" app/src/lib/frameMap.ts returns >= 1
    - grep -n "playScriptMarkers" app/src/lib/frameMap.ts returns no producer lines (dead field untouched)
  </acceptance_criteria>
  <done>Pure geometry module green; FxTrackLayout carries resolver-derived loop capsule models; no boundary math exists outside the resolver derivation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TimelineRenderer capsule drawing — thumbnails, band, ghosts, badge, diagonal, anchor flag, states</name>
  <files>app/src/components/timeline/TimelineRenderer.ts, app/src/components/timeline/TimelineRenderer.test.ts</files>
  <read_first>
    - app/src/components/timeline/TimelineRenderer.ts (drawPhysicPaintPlayScriptMarkers 423-496; drawRotoKeyMarkers 500-540; getThemeColors 74-100; PLACEHOLDER_BG_A/B 66-67; roundRect clip idiom 441-495; truncateText 1585)
    - app/src/components/timeline/ThumbnailCache.ts (get(imageId, thumbnailUrl) 7-42)
    - app/src/components/timeline/loopCapsuleGeometry.ts (Task 1)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (Color S1 table, Spacing capsule metrics, visual-state precedence)
  </read_first>
  <behavior>
    - Source-cycle cells draw downscaled real-key payload thumbnails via ThumbnailCache.get + drawImage, with `rgba(255,255,255,0.22)` borders and #F5A623 diamonds on real-key-backed cells only; duplicated-loop first-cycle cells draw the shared source thumbnails with LOOP_GHOST_BORDER dashed border and NO diamond
    - Default zoom draws LOOP_BAND_BASE `rgba(255,255,255,0.05)` over fxTrackBg plus LOOP_BAND_HATCH `rgba(255,255,255,0.14)` 45-degree 4px-period 1px lines; high zoom draws LOOP_GHOST_FILL `rgba(255,255,255,0.06)` cells with LOOP_GHOST_BORDER `rgba(255,255,255,0.24)` 1px dashed (4px dash/4px gap); low zoom draws solid band + badge only
    - Badge pill: 16px high, 8px horizontal padding, 8px radius, 4px inset from capsule start, fill `rgba(13,13,13,0.85)`, text `rgba(255,255,255,0.85)`, `600 10px system-ui`, tabular-nums, truncated via truncateText with the 18px minimum
    - Truncation diagonal: #FFB020 1.5px slicing the trailing top-right corner across the whole capsule outline including the band, landing per the geometry module at every zoom
    - Zero-effective loop: greyed anchor flag (fill #666666, `0f` text #E8E8E8) ~6px high, 24px wide, pinned at placementStart
    - State paint: idle outline `rgba(255,255,255,0.22)` 1px; hover `rgba(255,255,255,0.50)` 1.5px; selected `colors.accent` 2px around the whole capsule; focus ring accent 2px + 2px offset; disabled/stale 55% opacity; error #FF4444 2px outline; precedence error > focus > selected > diagonal > hover > idle; no state changes geometry
  </behavior>
  <action>
    Extend TimelineRenderer.test.ts RED-first with geometry-driven assertions (the renderer's capsule draw consumes loopCapsuleGeometry outputs; assert computed extents/landing positions through the exported geometry + a draw-call spy on the canvas context where the existing test harness permits); commit test(43-07). GREEN in TimelineRenderer.ts: add the capsule painter on the physic-paint FX row following the drawPhysicPaintPlayScriptMarkers structure (ctx.save, roundRect clip to bar, draw, ctx.restore; getThemeColors for theme-derived colors, hardcoded functional constants LOOP_BAND_BASE/LOOP_BAND_HATCH/LOOP_GHOST_FILL/LOOP_GHOST_BORDER/LOOP_TRUNCATION_COLOR #FFB020 per the UI-SPEC Color S1 table). Source thumbnails come from ThumbnailCache using real-key payload dataUrls, downscaled via drawImage at draw time — no new image pipeline. First-cycle cells branch on the geometry model's real-key-backed flag: diamond + solid border for real-key-backed cells (original loops), no diamond + LOOP_GHOST_BORDER dashed border for duplicated-loop linked first-cycle cells. Implement every behavior bullet with the exact locked constants and metrics. Capsule data comes only from fxTrackLayouts.loopCapsules (Task 1 — one compact interval model per loop). Draw only visible cells: ghost cells, band segments, and thumbnails are computed for the visible frame window at draw time (canvas paint calls, zero DOM nodes, no duration-sized structures even for Infinity loops). Commit feat(43-07).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run TimelineRenderer loopCapsuleGeometry</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --dir app exec vitest run TimelineRenderer exits 0 including the new capsule cases
    - TimelineRenderer.ts contains the constants LOOP_BAND_BASE, LOOP_BAND_HATCH, LOOP_GHOST_FILL, LOOP_GHOST_BORDER and the #FFB020 truncation color
    - grep -c "clip bloquant" app/src/components/timeline/TimelineRenderer.ts == 0 and the English string `Loop shortened by next clip` appears only in tooltip-copy assembly consumed by 43-08 (or is absent here if tooltips are fully owned by 43-08)
    - No new DOM element creation in the capsule path (canvas ctx calls only)
  </acceptance_criteria>
  <done>The capsule renders all locked visual forms at all zoom bands and states from the resolver-derived projection; geometry specs green; visual fidelity queued for 43-10 native UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted payload dataUrls → canvas | Source thumbnails come from parsed project data; rendering must not execute or fetch anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-07-01 | DoS | many loops on a long timeline | low | mitigate | Canvas paint calls only for visible cells; zero DOM nodes (D-32); geometry is O(1) per capsule |
| T-43-07-02 | Tampering | thumbnail dataUrls from project file | low | accept | drawImage of a data URL cannot execute script; existing ThumbnailCache discipline unchanged; no new fetch surface (V8 partial — loopClips carry keyIds, not paths) |
| T-43-07-03 | Tampering | capsule copy strings | low | mitigate | Badge/tooltip forms are locked constants with numeric slots only — no user-controlled text is interpolated into canvas copy |
| T-43-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan (UI-SPEC Registry Safety: zero new dependencies); any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run loopCapsuleGeometry frameMap TimelineRenderer — exit 0
- Full suite green
</verification>

<success_criteria>
- Every D-15..D-23 visual form renders from the resolver-derived projection
- Badge/diagonal/anchor-flag geometry is proven by pure-function specs
- No DOM nodes, no new image pipeline, no recomputed boundaries in the renderer
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- `app/src/components/timeline/loopCapsuleGeometry.ts` — pure geometry module (badgeTextForLoop, zoomBandForFrameWidth, capsule extents, ghost-cell grid, diagonal landing x, anchor-flag position)
- `loopCapsuleGeometry.test.ts` (new spec)
- `TimelineLoopCapsule` type + `FxTrackLayout.loopCapsules` optional field (types/timeline.ts)
- loopCapsules feed in `fxTrackLayouts` (frameMap.ts)
- Capsule painter + constants LOOP_BAND_BASE / LOOP_BAND_HATCH / LOOP_GHOST_FILL / LOOP_GHOST_BORDER / LOOP_TRUNCATION_COLOR (TimelineRenderer.ts)
