---
phase: 43-hold-loop-clips-filmstrip-capsule
plan: 08
type: execute
wave: 6
depends_on:
  - 43-06
  - 43-07
files_modified:
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/components/timeline/TimelineInteraction.test.ts
  - app/src/components/timeline/TimelineCapsuleTooltip.tsx
  - app/src/components/timeline/TimelineCapsuleTooltip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - HOLD-06

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Six capsule hit regions behave per the locked contract: real-key-backed source thumbnail cell click selects/seeks the real source key; duplicated-loop first-cycle cells and ghost cell/band clicks select the loop and pin the occurrence tooltip; badge click opens loop-edit mode; outline click selects the loop; truncation edge surfaces the truncation tooltip; the anchor flag has a ≥24×24px hit target that never overlaps the blocking key's hit target (UI-SPEC hit regions 1-6, placement/source correction)"
    - "The pinned occurrence tooltip shows `Repeat {n} · Source frame {i} of {N}` flat-multiline plus a dedicated action labeled exactly `Edit source frame` that seeks to the modulo-resolved real source key; the playhead does NOT move on click alone (D-17)"
    - "The truncated tooltip reads `{Badge} · Requested {R}f · Effective {E}f · Loop shortened by next clip ({partial cycle | complete cycles}) · {Progressive | Static / Hold}`; the zero-effective tooltip reads `Cycle {N}f × {R} = {D}f · Effective 0f — fully shortened by the next clip`; the error tooltip lists each missing source reference on its own line then `Repair, relink, unlink, or delete the loop.` (D-19, D-21, D-22, D-31)"
    - "The main-timeline tooltip host follows the Phase 38 flat-multiline discipline: 1000ms hover delay, immediate on keyboard focus, Escape hides, 8px viewport margin, pointerleave cancels (Pitfall 5 closure)"
    - "Keyboard model: the capsule is one unit in the timeline virtual focus order with a visible accent focus ring; Enter pins the tooltip/actions; Escape closes and returns focus; Delete on a focused capsule invokes unlink-only `Delete loop`; arrow keys unchanged; ghost cells are never focusable or selectable as keys (D-23, D-03)"
    - "The capsule tooltip exposes `Duplicate linked loop`, `Unlink loop`, and `Delete loop` actions wired to the 43-06 atomic ops; Duplicate prompts for a destination start frame and validates same-start collision and overlap per D-14/D-05; on an unresolved (error-state) loop the tooltip additionally exposes `Repair loop…` and `Relink loop…` wired to the 43-06 repairLoop/relinkLoop ops, so all four locked D-31 recovery affordances are offered and functional"
    - "Studio workflow strip linked repetition cells gain the additive link badge — inset 1px accent box-shadow border plus 4px accent corner dot at 2px top-right inset — with zero geometry change, zero palette change, and no new first-class cell state (D-18)"
    - "Every loop state — shrunk, zero-effective, unresolved — offers a visible recovery path (re-expand, repair/relink/unlink/delete) through the capsule or its tooltip (D-22, D-31)"
  artifacts:
    - path: "app/src/components/timeline/TimelineInteraction.ts"
      provides: "capsule hit regions, loop-object selection, keyboard focus unit"
      contains: "loopCapsule"
    - path: "app/src/components/timeline/TimelineCapsuleTooltip.tsx"
      provides: "main-timeline flat-multiline tooltip host with pinned actions"
    - path: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      provides: "additive link badge on linked repetition cells"
      contains: "link"
  key_links:
    - from: "app/src/components/timeline/TimelineInteraction.ts"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "badge hit region invokes the launch-or-focus open-loop-edit helper (43-06)"
      pattern: "OpenLoopEdit"
    - from: "app/src/components/timeline/TimelineCapsuleTooltip.tsx"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts"
      via: "tooltip actions invoke updateLoop/unlinkLoop/duplicateLinkedLoop controller ops; error-state actions invoke repairLoop/relinkLoop (D-31)"
      pattern: "duplicateLinkedLoop"
  prohibitions:
    - statement: "MUST NOT move the playhead on a ghost-cell/band click — seek happens only through the explicit `Edit source frame` action (D-17)"
      status: resolved
      verification: test
    - statement: "MUST NOT expose ghost cells to pointer selection, focus, or keyboard navigation as keys (D-23)"
      status: resolved
      verification: test
    - statement: "MUST NOT introduce a second tooltip system — the main-timeline host follows the PhysicsPaintStyledTooltip discipline (Pitfall 5)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT change cell geometry, the Phase 36.15 legend, or the status palette for the Studio link badge — strictly additive inset visuals (D-18)"
      status: resolved
      verification: test
    - statement: "MUST NOT trap the user in any loop state — shrunk, zero-effective, and unresolved loops all keep clickable/selectable recovery affordances (D-22, D-23, D-31)"
      status: resolved
      verification: judgment
---

<objective>
Make the capsule interactive: six hit regions, the new main-timeline tooltip host with pinned actions, the canvas keyboard model, badge-click → loop-edit bridge invocation, and the Studio strip's additive link badge.

Purpose: HOLD-06's interaction half; closes Pitfall 5 (no main-timeline tooltip surface existed) and wires the 43-06 bridge message and loop ops to the canvas.
Output: users can hover, select, keyboard-focus, and act on loops from the main timeline and see linked cells badged in the Studio strip.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-PATTERNS.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-06-SUMMARY.md
@.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-07-SUMMARY.md
@app/src/components/timeline/TimelineInteraction.ts
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capsule hit regions + loop-object selection + keyboard focus unit</name>
  <files>app/src/components/timeline/TimelineInteraction.ts, app/src/components/timeline/TimelineInteraction.test.ts</files>
  <read_first>
    - app/src/components/timeline/TimelineInteraction.ts (hit-test gating at line 353; existing pointer dispatch and keyboard handling)
    - app/src/components/timeline/loopCapsuleGeometry.ts (43-07 geometry for hit extents)
    - app/src/lib/shortcuts.ts (isPaintEditMode guards at 32, 422, 439, 455 — any new global shortcut must follow the same guard)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (Canvas hit regions, Canvas keyboard model)
  </read_first>
  <behavior>
    - Clicking a real-key-backed source thumbnail cell (original loop whose placement overlaps its source keys) selects/seeks the corresponding real source key (existing real-key behavior preserved)
    - Clicking a duplicated-loop first-cycle cell (linked/virtual at that placement, no real key there) selects the loop object and pins the occurrence tooltip showing repeat instance 0 and `Source frame {i} of {N}` — never real-key selection at the destination (placement/source correction)
    - Clicking a ghost cell or the band selects the loop object and pins the occurrence tooltip; the playhead does not move
    - Clicking the badge invokes the 43-06 launch-or-focus open-loop-edit flow with the loop's id
    - Clicking the capsule outline selects the loop object; clicking the truncation edge surfaces the truncation tooltip
    - The zero-effective anchor flag hit target is at least 24×24px and does not overlap the blocking real key's hit target
    - The capsule participates in the timeline virtual focus order as one unit; Delete on a focused capsule invokes unlink-only delete; ghost cells and duplicated-loop first-cycle cells are unreachable by pointer, focus, or keyboard as keys
  </behavior>
  <action>
    Extend TimelineInteraction.ts: add capsule hit-testing on the physic-paint FX row using the 43-07 geometry outputs (six regions per the UI-SPEC table, in precedence order: badge, anchor flag, source thumbnail cells, truncation edge, ghost cells/band, outline). Selection unit is the whole loop object. Source thumbnail cells branch on the geometry model's real-key-backed flag: real-key-backed cells dispatch the existing real-key select/seek; duplicated-loop first-cycle cells dispatch the linked-occurrence path (select loop + occurrence tooltip with repeat instance 0). Wire the badge region to the 43-06 bridge helper (launch-or-focus + open loop-edit). Wire the ghost/band region to select the loop and request the pinned occurrence tooltip (Task 2 host). Add the capsule to the timeline virtual focus order as one unit with Enter/Escape/Delete semantics per the locked keyboard model; Delete maps to the unlink-only loop deletion, never real-key deletion. Ghost cells and duplicated-loop first-cycle cells never produce key selection, drag start, or focus stops. Any new global shortcut follows the isPaintEditMode() guard convention in shortcuts.ts. Add spec coverage for hit-region dispatch (region → action mapping, real-key-backed vs duplicated first-cycle branching, anchor-flag 24×24 target, no-playhead-move on ghost click). Commit feat(43-08).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run TimelineInteraction</automated>
  </verify>
  <acceptance_criteria>
    - TimelineInteraction specs exit 0 with the six-region dispatch cases
    - A spec asserts ghost-cell click does not move the playhead and does not create a key selection
    - A spec asserts badge click calls the open-loop-edit helper with the correct loopId
  </acceptance_criteria>
  <done>All six hit regions dispatch correctly; the loop object is the selection unit; keyboard model reaches every pointer capability; no ghost-cell key leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main-timeline tooltip host with pinned capsule actions</name>
  <files>app/src/components/timeline/TimelineCapsuleTooltip.tsx, app/src/components/timeline/TimelineCapsuleTooltip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (useStyledTooltip controller 28-90: 1000ms delay, focus immediacy, Escape, TOOLTIP_VIEWPORT_MARGIN 8)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (Copywriting Contract tooltip rows; canvas keyboard model)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md (D-17, D-19, D-21, D-22, D-31)
  </read_first>
  <behavior>
    - Hover shows the tooltip after 1000ms; keyboard focus shows immediately; Escape hides; pointerleave cancels; 8px viewport margin
    - Occurrence tooltip content: `Repeat {n} · Source frame {i} of {N}` plus the action `Edit source frame` which seeks to the modulo-resolved real source key
    - Truncated tooltip, zero-effective tooltip, error tooltip (missing refs one per line + remedy line), and disabled/stale `{reason}` tooltip all render with the exact locked forms
    - Pinned tooltip actions `Duplicate linked loop` (prompts destination start frame, validates D-14 collision/overlap), `Unlink loop`, `Delete loop` invoke the 43-06 controller ops
    - On an unresolved (error-state) loop the pinned action row leads with `Repair loop…` (invokes the 43-06 repairLoop flow — prefilled source-edit, regeneration + sourceKeyIds retarget as one atomic commit) and `Relink loop…` (invokes the 43-06 relinkLoop flow — explicit guarded choice of an existing source cycle), followed by `Unlink loop` and `Delete loop`; when an op's guard rejects, the action surfaces the reason and the record stays verbatim (D-31)
    - Content is flat-multiline — one fact per line, no rich formatting; English only
  </behavior>
  <action>
    Create app/src/components/timeline/TimelineCapsuleTooltip.tsx — the main-timeline tooltip host (one host, not per-capsule DOM; positioned from the 43-07 geometry and the timeline focus host). Follow the PhysicsPaintStyledTooltip controller discipline exactly: delayed hover, immediate focus, Escape, viewport margin, idempotent cleanup with mountedRef — but implement show/hide against the timeline's pointer/focus events rather than DOM element handlers, since the capsule is canvas. Render the locked copy forms from behavior with flat-multiline layout; render the pinned action row (`Edit source frame`, `Duplicate linked loop`, `Unlink loop`, `Delete loop`) wired to the 43-06 controller ops and the seek action. On an unresolved loop, render the error-state action row (`Repair loop…`, `Relink loop…`, `Unlink loop`, `Delete loop`) wired to the 43-06 repairLoop/relinkLoop/unlinkLoop/delete ops so every remedy named in the locked error copy is a working action (D-31); a guard rejection from repairLoop/relinkLoop surfaces its reason inline and leaves the unresolved record untouched. The D-20 prohibited French term and the superseded French truncation label must not appear anywhere; English strings only. Add spec coverage: copy forms for each tooltip variant with substituted values, action dispatch to controller ops (including repairLoop and relinkLoop on the error state), delay/focus/Escape behavior. Commit feat(43-08).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run TimelineCapsuleTooltip</automated>
  </verify>
  <acceptance_criteria>
    - TimelineCapsuleTooltip specs exit 0
    - Specs assert the exact forms `Repeat 3 · Source frame 2 of 5`, `Cycle 5f × 5 = 25f · Effective 0f — fully shortened by the next clip`, and the error remedy line `Repair, relink, unlink, or delete the loop.`
    - grep -c "clip bloquant" app/src/components/timeline/TimelineCapsuleTooltip.tsx == 0
    - The action row wires all four actions (Edit source frame / Duplicate linked loop / Unlink loop / Delete loop)
    - The error-state action row wires `Repair loop…` and `Relink loop…` to the controller's repairLoop and relinkLoop (spec asserts dispatch with the correct loopId), alongside Unlink loop and Delete loop — all four D-31 remedies are functional, not copy-only
  </acceptance_criteria>
  <done>The main timeline has one tooltip host rendering every locked capsule tooltip form with working pinned actions — including the D-31 repair/relink recovery actions on the error state; hover/focus/Escape discipline matches the Studio convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Studio workflow strip additive link badge (D-18)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (cell derivation block 1297-1343; dragEligible 1315)
    - app/src/components/physic-paint/physicsPaintStudio.css (roto-cell palette: empty #4d535a, cached #2d6f48, generated #365ed6)
    - .planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UI-SPEC.md (S5 table)
  </read_first>
  <behavior>
    - A linked repetition cell (typed 'linked' or 'linked-unresolved' from the 43-02 visible-window query) renders its existing cell-state fill PLUS an inset 1px accent border (box-shadow: inset 0 0 0 1px rgba(45,91,227,0.9)) and a 4px filled accent dot at top-right with 2px inset
    - Cell geometry is unchanged (18px×24px cells, no layout impact); the Phase 36.15 palette and legend are unchanged
    - Source-cycle cells keep real-key diamonds; linked cells remain non-draggable and non-selectable as keys (43-02 audit)
  </behavior>
  <action>
    In PhysicsPaintWorkflowStrip.tsx, extend the per-cell derivation: when the visible-window resolution is linked or linked-unresolved, layer an additive link-badge class onto the existing cellClass string — no new first-class cell state, no fill change. In physicsPaintStudio.css add the link-badge class: inset-only accent box-shadow border and the 4px accent corner dot positioned 2px from the top-right inside the cell (drawn glyph or pseudo-element — no icon font). CSS additions are strictly additive. Extend the strip spec: linked cells carry the badge class with unchanged fill class and unchanged geometry; dragEligible stays false. Commit feat(43-08).
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run PhysicsPaintWorkflowStrip</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip specs exit 0 with the additive-badge case
    - The CSS contains the inset accent box-shadow and the 4px dot rule; no existing roto-fill rule is modified (git diff physicsPaintStudio.css shows additions only)
    - Strip cells keep their 18px×24px geometry (spec or existing layout assertions unchanged)
  </acceptance_criteria>
  <done>Studio strip shows linked repetitions with the additive accent badge; palette, legend, geometry, and cell-state semantics untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas pointer/keyboard → ops | Hit regions dispatch directly into atomic loop ops; mis-scoped regions would mutate the wrong object |
| tooltip actions → controller | Pinned actions invoke commits from a floating surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-08-01 | Tampering | hit-region dispatch | medium | mitigate | Region precedence and loop-object selection pinned by specs; Delete on capsule maps only to unlink-only loop deletion, never key deletion |
| T-43-08-02 | DoS | tooltip host lifecycle | low | mitigate | Single host with idempotent cleanup (mountedRef pattern); window listeners only while visible |
| T-43-08-03 | Tampering | tooltip copy interpolation | low | mitigate | Only numeric slots and locked constant strings are interpolated; no user-controlled free text reaches tooltip markup |
| T-43-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this plan; any addition triggers slopcheck + blocking human checkpoint |
</threat_model>

<verification>
- pnpm --dir app exec vitest run TimelineInteraction TimelineCapsuleTooltip PhysicsPaintWorkflowStrip — exit 0
- Full suite green
</verification>

<success_criteria>
- All six hit regions, the keyboard model, and the tooltip actions (including error-state repair/relink per D-31) work against the 43-06/43-07 machinery
- Studio strip shows the additive badge with zero geometry/palette change
- Every loop state keeps a visible recovery path
</success_criteria>

<output>
Create `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-08-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan)

- Capsule hit-region dispatch + loop-object selection + keyboard focus unit (TimelineInteraction.ts)
- `app/src/components/timeline/TimelineCapsuleTooltip.tsx` (new main-timeline tooltip host with pinned actions)
- Additive link-badge class on linked-loop cells (PhysicsPaintWorkflowStrip.tsx)
- Link-badge CSS: inset accent box-shadow + 4px corner dot (physicsPaintStudio.css)
