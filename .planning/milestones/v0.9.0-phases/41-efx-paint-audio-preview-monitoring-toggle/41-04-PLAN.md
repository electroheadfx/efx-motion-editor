---
phase: 41-efx-paint-audio-preview-monitoring-toggle
plan: 04
type: execute
wave: 4
depends_on: [41-03]
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/playbackEngine.ts
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
  - app/src/components/physic-paint/audio/efxPaintAudioOwnership.ts
  - app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts
  - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts
autonomous: true
requirements: [AUDIO-05, AUDIO-06]
tags: [audio, ownership-guard, toggle-ui]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "First-player-wins: whichever window starts audio first owns monitoring; pressing Play in the other window is a no-op for audio while visual playback proceeds — doubled audio is impossible (D-05, AUDIO-06)"
    - "Suppression is never silent: while the guard suppresses EFX Paint audio, a small non-blocking status note (e.g. 'Audio playing in main editor') shows via the existing publishStatus funnel (D-06)"
    - "When the main editor stops while EFX Paint is still visually playing with the toggle On, monitoring auto-resumes at the current Paint cursor and the suppressed note clears (D-07)"
    - "The Audio Preview toggle is a compact speaker icon button next to the loop toggle in the physics-paint-pill--playback group, guarded-icon-with-tooltip per the Phase 36.15 pattern, defaulting On per session and never persisted (D-12, D-13)"
    - "Toggling Off mid-playback silences audio immediately while visual playback continues; toggling On resumes audio at the current Paint cursor without restarting visual playback (D-14)"
    - "Idempotency: toggling On when already On (or Off when already Off) is a no-op — no double engine start, no doubled stopAll side effects (AUDIO-05 edge, pinned by test)"
    - "Concurrency: a toggle racing a revisioned update serializes through the monitor's single control funnel — Off always ends silent, On always ends positioned at the current cursor (AUDIO-05 edge, pinned by test)"
  artifacts:
    - app/src/components/physic-paint/audio/efxPaintAudioOwnership.ts
  key_links:
    - "Main broadcasts playback state from playbackEngine.start()/stop() (app/src/lib/playbackEngine.ts lines 35-58/60-71) via a bridge publisher shaped like publishPhysicPaintProjectContext"
    - "Toggle state lives in audio/efxPaintAudioPreviewStore.ts as a module-scope signal(true) — fresh bundle per window open gives the D-13 reset for free (soloStore precedent)"
    - "Toggle button mounts in PhysicsPaintWorkflowStrip.tsx physics-paint-pill--playback group (line ~365) beside the loop toggle (line ~366)"
  prohibitions:
    - "No persisting the toggle to project data, .mce files, or app config (D-13, AUDIO-05)"
    - "No silent suppression — the suppressed note is mandatory whenever the guard blocks EFX Paint audio (D-06)"
    - "No mutation of main-editor track mute/volume state or export from EFX Paint — the toggle only gates local monitoring (AUDIO-05)"
    - "No forcing ownership events through the revision counter — ownership is transient session state on separate lightweight events (RESEARCH Pattern 5 recommendation, A5)"
---

<objective>
Add the doubled-audio ownership guard (D-05..D-07) and the session-local Audio Preview toggle (D-12..D-14). Together these complete AUDIO-05 and the no-doubled-audio half of AUDIO-06, including both flagged AUDIO-05 edge rows.

Purpose: Two windows with two Web Audio contexts will double audio without deterministic arbitration; the guard and its status note make the arbitration visible and predictable, and the toggle gives the user session-local silence without touching main-editor state.
Output: Ownership state machine with cross-window playback-state events + toggle UI wired through the monitor funnel.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-PATTERNS.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-03-SUMMARY.md
@app/src/lib/physicPaintBridge.ts
@app/src/lib/playbackEngine.ts
@app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
@app/src/stores/soloStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: First-player-wins ownership guard with auto-resume</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/playbackEngine.ts, app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts, app/src/components/physic-paint/audio/efxPaintAudioOwnership.ts, app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts (lines 26-44 event constants; 823-834 publisher shape; 44 PHYSIC_PAINT_WINDOW_LABEL)
    - app/src/lib/playbackEngine.ts (lines 35-58 start(), 60-71 stop() — the single main-window funnel to broadcast from)
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts (lines 23-35 dual-transport sender targeting 'main')
    - app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts (publishStatus ~lines 202-208 — status note funnel)
    - app/src/stores/soloStore.ts (session-signal container shape)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md (D-05, D-06, D-07, D-08)
  </read_first>
  <behavior>
    - Main window: playbackEngine.start() and stop() broadcast { playing: boolean } on PHYSIC_PAINT_AUDIO_PLAYBACK_STATE_EVENT = 'physic-paint:audio-playback-state' to the child via a bridge publisher (emitTo label + CustomEvent + opener.postMessage fallbacks)
    - Child on Play: if the last received main playback state is playing, the monitor does NOT start audio (visual playback unaffected) and the suppressed note 'Audio playing in main editor' is published (D-05/D-06); otherwise audio starts and the child emits an ownership claim to 'main'
    - Main on Play (symmetric guard): if the child holds a claim, the main window's own audio start is suppressed for monitoring parity — visual playback proceeds; main-side suppression needs no status note in this phase (main editor is the authority; its existing UI shows playback)
    - Main stop broadcast while child is visually playing and toggle On: child auto-resumes audio at the current Paint cursor and clears the suppressed note (D-07)
    - Claim lifecycle: child emits claim on audio start, release on stop/close; a release for a non-held claim is a no-op (idempotent)
    - Race: simultaneous Play in both windows resolves deterministically — the window whose audio start processed first wins; the loser suppresses; no mixing ever (both windows check before starting)
  </behavior>
  <action>
    1. lib/physicPaintBridge.ts: add PHYSIC_PAINT_AUDIO_PLAYBACK_STATE_EVENT constant and publishPhysicPaintAudioPlaybackState(playing: boolean) following the publishPhysicPaintProjectContext shape. In lib/playbackEngine.ts call it from start() (true) and stop() (false) — the only two funnel points; guard the import so the child bundle never executes the main-side publisher (the child never imports playbackEngine per the authority boundary — this call lives only in playbackEngine, which is main-window code).
    2. bridge/physicsPaintBridgeTransport.ts: add sendPhysicPaintAudioOwnership(claim: boolean, bridgeMode) dual-transport sender targeting 'main' (same shape as sendPhysicPaintScriptLibraryRequest); in lib/physicPaintBridge.ts add the matching main-side listener that records the child's claim in a module signal and gates the main audio start in playbackEngine.start() (suppress audio when claim held — check before startAudioPlayback()).
    3. NEW audio/efxPaintAudioOwnership.ts: soloStore-shaped session module — signals otherWindowPlaying (from the playback-state listener) and ownership state; exposes canStartAudio(), noteMainPlaybackState(playing), claimAudio(), releaseAudio(). Wire the monitor's playAtCursor entry through canStartAudio(): suppressed → publish the D-06 note via the injectable status callback (useRotoCachedPlayback's publishStatus) and return without engine dispatch. On noteMainPlaybackState(false) with child visually playing + toggle On + previously suppressed → auto-resume at cursor via the monitor funnel and clear the note (D-07).
    4. Child-side listener for the playback-state event lives in usePhysicsPaintParentBridge.ts's sibling pattern — reuse the useEfxPaintAudioContextBridge listener shape from 41-03 (add a small listener in the ownership module's install function if cleaner; one listener per event).
    Extend tests: (a) main-playing state blocks child audio start and publishes the note, (b) main stop auto-resumes at cursor and clears the note, (c) claim/release round-trip, release-without-claim no-op, (d) child claim suppresses main startAudioPlayback, (e) toggle Off blocks auto-resume (D-07 condition). Mock the event layer; do not spin real Tauri events. Commit as feat(41-04): first-player-wins audio ownership guard with auto-resume.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts src/lib/playbackEngine.test.ts &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - physicPaintBridge.ts exports PHYSIC_PAINT_AUDIO_PLAYBACK_STATE_EVENT = 'physic-paint:audio-playback-state' and publishPhysicPaintAudioPlaybackState; playbackEngine.start()/stop() broadcast through it
    - Tests (a)-(e) from action pass with exact engine-dispatch counts (suppressed start dispatches zero play calls)
    - efxPaintAudioOwnership.ts contains no imports of audioStore/timelineStore/playbackEngine
    - Suppressed note string 'Audio playing in main editor' appears exactly once in the source and routes through the publishStatus funnel
    - pnpm --dir app exec vitest run exits 0 for touched files; typecheck exits 0
  </acceptance_criteria>
  <done>Doubled audio is structurally impossible: both windows check ownership before starting audio, suppression is visible, and auto-resume restores monitoring when the main editor stops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audio Preview toggle — session signal, speaker button, immediate effect</name>
  <files>app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (line ~365 physics-paint-pill--playback group; line ~366 loop-toggle button to mirror; lines 432-466 RotoTimelineCellButtonImpl tooltip mount shape; lines 1241-1242 playback prop wiring)
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (lines 28-90 useStyledTooltip contract: 1000ms hover, immediate on focus, Escape hides, hide() on activation; region="bottom" for playback controls)
    - app/src/stores/soloStore.ts (session-signal shape — toggle store mirrors it)
    - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts (control funnel from 41-03)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <behavior>
    - Toggle signal audioPreviewEnabled lives in efxPaintAudioPreviewStore.ts initialized signal(true) — session-local, default On, resets on each window open, never written to project data or app config (D-13)
    - Button: speaker icon (lucide Volume2 when On, VolumeX when Off, size 15) in the physics-paint-pill--playback group immediately beside the loop toggle; physics-paint-nav-button class plus conditional active class; aria-pressed reflects state; dynamic aria-label ('Disable audio preview' / 'Enable audio preview'); one useStyledTooltip instance on a wrapping anchor span with region="bottom" and tooltip copy ('Audio preview On — click to mute monitoring' / 'Audio preview Off — click to hear monitoring') (D-12)
    - Off mid-playback: audioEngine.stopAll() fires immediately through the monitor funnel; visual playback continues untouched (D-14)
    - On mid-playback: audio resumes at the current Paint cursor via playAtCursor; visual playback does not restart (D-14)
    - Off while ownership-suppressed: no-op beyond state change; the suppressed note lifecycle is unchanged
    - Idempotent: setAudioPreviewEnabled(true) when already true performs zero engine calls (AUDIO-05 edge)
  </behavior>
  <action>
    Add audioPreviewEnabled signal + setAudioPreviewEnabled(v) to efxPaintAudioPreviewStore.ts (soloStore shape). Gate the monitor funnel: playAtCursor and the D-07 auto-resume check the signal first; setAudioPreviewEnabled(false) while playing routes stop() through the funnel; setAudioPreviewEnabled(true) while visually playing routes playAtCursor(currentCursor). In PhysicsPaintWorkflowStrip.tsx add the toggle button in the playback pill mirroring the loop-toggle button markup (same class pattern, aria-pressed, guarded onClick via optional callback prop onAudioPreviewToggle), wrapped in the anchor-span + useStyledTooltip mount (hide() inside onClick before the action, per the contract). Wire new props audioPreviewEnabled and onAudioPreviewToggle through the strip's playback prop section alongside onPlaybackLoopChange (lines ~330/1241). Extend tests: (a) toggle Off mid-playback calls stopAll exactly once and visual state is untouched, (b) toggle On mid-playback dispatches playAtCursor at the current cursor without touching visual playback, (c) idempotent same-value set performs zero engine calls, (d) toggle racing a revisioned update serializes — Off always ends silent, On always ends positioned (single-funnel assertion), (e) store initializes true and persists nothing (assert no localStorage/project-store writes). Commit as feat(41-04): session-local Audio Preview toggle with immediate mid-playback effect.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - efxPaintAudioPreviewStore.ts initializes audioPreviewEnabled with signal(true); grep of the file shows no localStorage, no project-store import, no persistence call
    - PhysicsPaintWorkflowStrip.tsx renders the speaker button inside the physics-paint-pill--playback group with aria-pressed and the useStyledTooltip mount (region bottom)
    - Tests (a)-(e) from action pass with exact dispatch counts
    - Button uses lucide Volume2/VolumeX at size 15 mirroring the loop-toggle pattern
    - pnpm --dir app exec vitest run exits 0 for the touched file; typecheck exits 0
  </acceptance_criteria>
  <done>The toggle silences/resumes monitoring immediately mid-playback, defaults On per session, persists nothing, and never touches main-editor audio state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main ↔ child (ownership events) | Transient playback-state/claim events cross in both directions |
| child session state | Toggle and ownership state live in child-only signals; isolation from project data is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-10 | Tampering | Spoofed ownership/playback-state event suppresses or doubles audio | medium | mitigate | emitTo window-label targeting both directions; origin check on postMessage fallback; guard treats events as transient hints — worst case is a suppressed note, never main-state mutation |
| T-41-11 | DoS | Event storm from rapid Play/Stop toggling in both windows | low | mitigate | Events are two funnels only (playbackEngine start/stop, monitor start/stop); guard transitions are O(1) signal writes; idempotent claim/release |
| T-41-12 | Information disclosure | Toggle state leaking into project files/config | low | mitigate | Source assertion: store module has no persistence imports; test (e) asserts no storage writes; D-13 session-only by construction |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs no external packages (RESEARCH Package Legitimacy Audit); no install tasks exist |
</threat_model>

<verification>
- Ownership guard and toggle covered by unit tests including both AUDIO-05 edge rows (idempotency, concurrency)
- Native doubled-audio and toggle-isolation confirmation deferred to the 41-05 packaged-app UAT checkpoint
</verification>

<success_criteria>
AUDIO-05 and the doubled-audio half of AUDIO-06 are code-complete and test-pinned: deterministic first-player-wins arbitration with a visible suppressed note and auto-resume, plus a session-local toggle with immediate mid-playback effect.
</success_criteria>

<output>
Create `.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-04-SUMMARY.md` when done.

## Artifacts this phase produces (this plan)

- New bridge exports: `PHYSIC_PAINT_AUDIO_PLAYBACK_STATE_EVENT` ('physic-paint:audio-playback-state'), `publishPhysicPaintAudioPlaybackState` (app/src/lib/physicPaintBridge.ts)
- New transport sender: `sendPhysicPaintAudioOwnership` (app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts)
- New module: `app/src/components/physic-paint/audio/efxPaintAudioOwnership.ts` — `canStartAudio`, `noteMainPlaybackState`, `claimAudio`, `releaseAudio`
- New store API: `audioPreviewEnabled` signal, `setAudioPreviewEnabled` (app/src/components/physic-paint/audio/efxPaintAudioPreviewStore.ts)
- New UI: Audio Preview speaker toggle button + props `audioPreviewEnabled`, `onAudioPreviewToggle` (app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx)
</output>
