---
phase: 41-efx-paint-audio-preview-monitoring-toggle
plan: 05
type: execute
wave: 5
depends_on: [41-04]
files_modified:
  - app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
  - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
  - app/src-tauri/tauri.conf.json
  - app/src/releaseContract.test.ts
  - app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts
autonomous: false
requirements: [AUDIO-06]
tags: [audio, lifecycle, csp, uat]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Closing EFX Paint stops and releases all audio resources: stopAll fires and the AudioContext is closed on window close (both the close-requested path and the unmount path), and re-opening the window starts from a clean engine (D-08, AUDIO-06)"
    - "The connect-src CSP grant for efxasset: is proven necessary BEFORE the grant is added via the proof mode locked at the 41-01 Task 3 checkpoint (contract-test RED, or a pre-grant packaged build observing the efxasset fetch failure if the user selected the literal D-04 reading), is exactly one directive token, and is pinned by the extended releaseContract.test.ts in the same commit (D-04 discipline, v0.8.1 img-src precedent)"
    - "The image CSP contract is unchanged: the img-src data: grant tests in releaseContract.test.ts pass unmodified (D-04: image contract untouched)"
    - "Native packaged-app UAT confirms: audible frame-synced monitoring, seek/loop/stop without drift or doubling, toggle isolation from main-editor mute state, suppressed note when the main editor owns playback, and silence after window close (REL-02 audio steps)"
  artifacts:
    - app/src-tauri/tauri.conf.json (connect-src gains efxasset:)
    - app/src/releaseContract.test.ts (connect-src efxasset contract block)
  key_links:
    - "Engine release hooks into the usePhysicsPaintCloseFlush close-requested funnel (usePhysicsPaintParentBridge.ts lines 19-44) AND the component unmount path — the current flush early-returns when !hasPending, so release must not depend on it"
    - "Contract test reuses the file's existing cspDirectiveTokens helper and mirrors the 'Tauri CSP image data-url contract' describe block (lines 107-132)"
  prohibitions:
    - "No second audio engine instance per window and no engine reuse after ctx.close() — a closed context is discarded, never resurrected (D-08)"
    - "No CSP broadening beyond the single efxasset: token in connect-src; no data: or blob: added to connect-src; no change to img-src/media-src directives (D-04)"
    - "No audio MIME additions to the Rust efxasset handler — fetch + decodeAudioData ignores MIME; the Rust diff for Phase 41 stays limited to the launch-context struct field (RESEARCH Pitfall 8)"
---

<objective>
Close out AUDIO-06: guaranteed engine release on window close, the D-04-mandated CSP connect-src grant proven by a failing test before the grant lands, and the blocking native packaged-app UAT that is the phase's sync-quality oracle.

Purpose: Resource leaks across open/close cycles and unproven CSP grants are the two remaining failure modes; the native UAT is the only honest proof of drift-free, doubling-free audio.
Output: Release-safe monitor lifecycle + narrow pinned CSP grant + user-approved native UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-RESEARCH.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-PATTERNS.md
@.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-04-SUMMARY.md
@app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts
@app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
@app/src-tauri/tauri.conf.json
@app/src/releaseContract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine release on close — stopAll + AudioContext close on both close paths</name>
  <files>app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts, app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts, app/src/components/physic-paint/audio/efxPaintAudioPreview.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts (lines 19-44 usePhysicsPaintCloseFlush — note the hasPending early-return; release must run on unconditional close too)
    - app/src/lib/audioEngine.ts (ensureContext lines 17-26, stopAll, ctx lifecycle — add nothing here; the monitor owns release orchestration)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (component unmount path where cleanup effects return)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-RESEARCH.md (Pitfall 6: scheduled playDelayed sources survive close races — explicit release required)
  </read_first>
  <behavior>
    - monitor.release(): stopAll() (covers scheduled playDelayed sources), then close the AudioContext if one exists, then discard the reference so a later ensureContext creates a fresh context — a closed context is never reused (D-08)
    - release() is idempotent: calling it twice performs one stopAll and at most one ctx.close() (ctx.close() on a closed context rejects — guard by state check or try/catch)
    - Release fires on BOTH paths: the onCloseRequested close funnel and the Studio unmount cleanup; double-firing is absorbed by idempotency
    - Missing-asset tolerance stays intact after release wiring: warn-and-skip tests from 41-02 remain green
  </behavior>
  <action>
    Add release() to efxPaintAudioMonitor.ts per the behavior contract, including an engineHasContext() probe on the audioEngine singleton (add a minimal accessor if the engine does not expose one — smallest possible engine diff, no behavior change to play/stopAll). Wire release into usePhysicsPaintParentBridge.ts: call it unconditionally inside the onCloseRequested handler (before or independent of the hasPending gate — the flush early-return must not skip audio release) and from the Studio unmount cleanup effect. Extend tests: (a) release after play dispatches stopAll then ctx.close, (b) second release is a no-op, (c) release with never-created context is a safe no-op, (d) after release a subsequent playAtCursor creates a fresh context (ensureContext called again). Commit as feat(41-05): release audio engine on EFX Paint close.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/audio/efxPaintAudioPreview.test.ts &amp;&amp; pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - efxPaintAudioMonitor.ts exports release(); tests (a)-(d) pass
    - usePhysicsPaintParentBridge.ts invokes release on the close-requested path outside the hasPending gate, and the Studio unmount path also releases
    - Any audioEngine.ts diff is limited to a read-only context accessor (grep shows no change to play/playDelayed/stopAll/fade logic)
    - pnpm --dir app exec vitest run exits 0; typecheck exits 0
  </acceptance_criteria>
  <done>Window close always stops and releases audio; scheduled future sources cannot outlive the window; re-open starts clean.</done>
</task>

<task type="auto">
  <name>Task 2: CSP connect-src grant for efxasset: — failing contract test first, single token, pinned</name>
  <reversibility rating="costly">CSP is release security posture; the grant is removable but ships in the release contract. The D-04 one-way boundary interpretation itself was gated at the 41-01 decision checkpoint.</reversibility>
  <files>app/src/releaseContract.test.ts, app/src-tauri/tauri.conf.json</files>
  <read_first>
    - app/src/releaseContract.test.ts (lines 107-132 'Tauri CSP image data-url contract' block and the cspDirectiveTokens helper — mirror this discipline exactly)
    - app/src-tauri/tauri.conf.json (line ~38 csp string; current connect-src is 'self' ipc: http://ipc.localhost https://*)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-RESEARCH.md (Pitfall 1: fetch to a custom scheme is governed by connect-src; Tauri v2 CSP docs citation)
    - .planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-CONTEXT.md (D-04: single narrow directive proven by a failing packaged-app test)
  </read_first>
  <action>
    Read the proof mode locked at the 41-01 Task 3 checkpoint (recorded in the truth table's DECISIONS LOCKED section) and follow the matching branch:
    - If d04-proof-packaged-build was selected (literal D-04 reading): FIRST produce a packaged build against the unmodified tauri.conf.json and observe the efxasset fetch failure inside the packaged EFX Paint webview (open EFX Paint on a project with audio, press Play, capture the connect-src refusal in the console) — this is the failing packaged-app proof D-04 names verbatim. Record the observed failure in the plan SUMMARY, then proceed to the RED contract test below.
    - If d04-proof-contract-red was selected (recommended default): the contract-test RED below is the accepted failing proof, per the user's explicit acceptance at the 41-01 gate; the packaged-webview fetch physically exercises the directive at the Task 3 native UAT.
    RED first: extend releaseContract.test.ts with a new describe block 'Tauri CSP connect-src efxasset contract' reusing cspDirectiveTokens: (1) connect-src contains every pre-existing source ('self', ipc:, http://ipc.localhost, https://*) AND efxasset:; (2) no other directive beyond img-src/media-src/connect-src gains efxasset:; (3) connect-src does NOT contain data: or blob: (narrow-grant guard per D-04). Run it against the unmodified tauri.conf.json and confirm the new block FAILS. Then edit tauri.conf.json adding exactly one token efxasset: to the connect-src source list — no other directive touched, image contract untouched. Re-run: the new block and all pre-existing CSP tests pass. Do not add audio MIME entries to the Rust handler (RESEARCH Pitfall 8). Commit test + config in ONE commit per the v0.8.1 precedent: fix(41-05): grant efxasset in connect-src for EFX Paint audio fetch with contract-test guard.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/releaseContract.test.ts &amp;&amp; node -e "const c=require('./app/src-tauri/tauri.conf.json'); const csp=c.app.security.csp; const m=csp.match(/connect-src ([^;]+)/); if(!m||!m[1].split(/\s+/).includes('efxasset:')) process.exit(1); if(/connect-src[^;]*(data:|blob:)/.test(csp)) process.exit(2); console.log('connect-src grant pinned, no data:/blob:')"</automated>
  </verify>
  <acceptance_criteria>
    - The proof mode recorded in the truth table (41-01 Task 3) was followed: contract-test RED observed before the grant, plus a pre-grant packaged-build fetch failure captured in the SUMMARY if d04-proof-packaged-build was selected
    - Git history for this task is a single commit containing BOTH the tauri.conf.json connect-src change and the releaseContract.test.ts extension
    - pnpm --dir app exec vitest run src/releaseContract.test.ts exits 0, including the pre-existing 'Tauri CSP image data-url contract' block unmodified
    - tauri.conf.json csp string: connect-src contains efxasset: and no data:/blob:; img-src and media-src directives are byte-identical to before this task
    - No changes under app/src-tauri/src/ in this task
  </acceptance_criteria>
  <done>The single narrow connect-src grant is in place, proven necessary by the RED-first contract test, and pinned against future CSP creep in the same commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Native packaged-app UAT — audio sync, doubling guard, toggle, close release</name>
  <what-built>
    Complete read-only audio monitoring inside EFX Paint: revisioned launch + push context (D-01/D-02), fetch/decode via efxasset with the new connect-src grant (D-04), frame-mapped playback with silent scrub, loop-wrap re-seek, and 40ms drift correction (D-09/D-10/D-11), first-player-wins doubling guard with suppressed note and auto-resume (D-05..D-07), session-local speaker toggle beside the loop control (D-12..D-14), and engine release on close (D-08).
  </what-built>
  <how-to-verify>
    Run the packaged app (production build with the new CSP), open a project that has at least two audio tracks on the main timeline (one muted), then:
    1. Open the Paint/Physics Paint layer in EFX Paint — with the toggle at its default On, press Play: you hear the main-editor arrangement at the correct Paint cursor position; the muted main-editor track is inaudible (AUDIO-01/02/03).
    2. Scrub the cursor — silence while dragging; press Play again — audio starts at the new position (D-09).
    3. Enable loop over a range and let it wrap at least twice — audio re-seeks at each wrap, no drift accumulating (D-11, D-10).
    4. While EFX Paint plays, edit audio in the main editor (move a track, change volume) — monitoring restarts at the current cursor reflecting the change (D-02/D-03).
    5. Press Play in the MAIN editor while EFX Paint plays (or vice versa) — no doubled audio; EFX Paint shows the 'Audio playing in main editor' note when suppressed; stop the main editor — EFX Paint monitoring auto-resumes (D-05/D-06/D-07).
    6. Click the speaker toggle Off mid-playback — immediate silence, visuals continue; click On — audio resumes at the cursor (D-14). Confirm the main editor's track mute buttons were never touched (AUDIO-05).
    7. Delete/rename one audio file on disk, reopen EFX Paint — a non-blocking warning behavior (console) and the remaining track still plays; Paint editing is never blocked (AUDIO-06).
    8. Close EFX Paint while audio is playing — audio stops immediately with the window; reopen — monitoring starts clean (D-08).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step(s) with what you heard/saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child webview → efxasset protocol | Fetch of audio bytes now permitted by the new connect-src token |
| CSP configuration | Release security posture; single-token change under contract-test guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-13 | Security misconfiguration | CSP broadening creep beyond the proven grant | high | mitigate | RED-first contract test pins connect-src token set exactly (efxasset: plus pre-existing sources, no data:/blob:); image contract untouched; single commit pairs config + guard per v0.8.1 precedent |
| T-41-14 | DoS | Audio resources leaking across open/close cycles (scheduled playDelayed sources, unclosed contexts) | medium | mitigate | release() on both close paths stops all sources and closes the context; idempotency tests; D-08 single-instance discipline |
| T-41-15 | Tampering | fetch of unintended local files via efxasset now reachable from child | medium | mitigate | Child fetches only URLs received in validated revisioned sections (closed-key + type guard, 41-02); handler is read-only; capability file still grants the child no fs permissions |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | Phase installs no external packages (RESEARCH Package Legitimacy Audit); no install tasks exist |
</threat_model>

<verification>
- Full automated gate: pnpm --dir app exec vitest run (entire suite), pnpm --dir app typecheck, cargo check --manifest-path app/src-tauri/Cargo.toml — all green before the UAT checkpoint
- Native packaged-app UAT (Task 3) approved by the user — nothing is "done" until live UAT passes (project convention; REL-02 audio steps)
</verification>

<success_criteria>
AUDIO-06 fully closed: engine release on close is test-pinned, the CSP grant is proven and pinned, and the user has approved native packaged-app UAT covering sync, doubling guard, toggle isolation, missing assets, and close release.
</success_criteria>

<output>
Create `.planning/phases/41-efx-paint-audio-preview-monitoring-toggle/41-05-SUMMARY.md` when done.

## Artifacts this phase produces (this plan)

- New monitor API: `release` (app/src/components/physic-paint/audio/efxPaintAudioMonitor.ts)
- Possible minimal engine accessor: read-only context probe on the audioEngine singleton (app/src/lib/audioEngine.ts — only if no existing probe)
- CSP grant: `efxasset:` token in connect-src (app/src-tauri/tauri.conf.json)
- New contract test block: 'Tauri CSP connect-src efxasset contract' (app/src/releaseContract.test.ts)
</output>
