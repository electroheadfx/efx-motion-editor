# Phase 43.1: Intentional Gap Insert and Local Interpolation Breaks - Context

**Gathered:** 2026-08-09
**Status:** Ready for planning

<domain>
## Phase Boundary

Enable the existing Physics Paint Roto `Insert` action when the current cursor frame is genuinely empty. The accepted operation creates a new real empty Paint key at that exact frame plus a persistent interpolation break immediately before that key, as one atomic physical-document/history transaction. Interpolation never bridges from the preceding segment into the new key, while a later real key may interpolate locally from the inserted key to form a new segment.

The break remains durable while Interpolation is Off and becomes effective again when Interpolation is turned On; Insert never changes the global Interpolation toggle. The key and break survive save/reopen and accepted timing edits through stable key identity. Generated interiors remain derived and are never materialized. Existing occupied-key Insert/Open-and-Insert behavior remains unchanged.

This phase does not change Loop Clip persistence, Repeat/source/placement semantics, the Motion Editor timeline, internal Paint track count, the interpolation engine, generated-frame materialization, or release/signing work.

</domain>

<decisions>
## Implementation Decisions

### Empty-key and break contract

- **D-01:** Empty-frame Insert creates a genuinely empty real Paint key at the cursor frame and a persistent interpolation break owned by that key's stable identity. It must never duplicate, copy, or materialize the previous rendered drawing. Key plus break are accepted, rejected, undone, and redone as one atomic physical-document command. — **Reversibility:** one-way — once persisted and included in canonical revision/history snapshots, changing ownership away from stable key identity would require rewriting saved physical documents and replay semantics.
- **D-02:** The break suppresses only the incoming interpolation span from the preceding real-key segment. A later real key after the inserted key may interpolate normally from the inserted key, creating a new local segment.
- **D-03:** Interpolation Off does not delete, hide, or rewrite the break and Insert never toggles Interpolation. Turning Interpolation On again re-applies the stored boundary.
- **D-04:** When a break-bearing key temporarily has no preceding real key, the break remains attached but dormant. It is not drawn as a leading boundary while the key is first; it becomes visible and effective again if a predecessor later exists.

### Physical-strip marker

- **D-05:** Show the intentional break as a compact left-edge cut immediately before the owning real key inside the existing physical-row/cell geometry. Add no timeline row, track height, persistent badge, or separate marker lane.
- **D-06:** The cut stays visible when Interpolation is Off because the boundary remains durable. It follows normal key visibility and is drawn only when the owning key currently has a predecessor.
- **D-07:** Explain the cut by extending the existing real-key cell tooltip/accessibility copy with the exact fact `Starts a new interpolation segment`. The cut has no independent pointer target or tooltip lifecycle.

### Insert affordance and accepted feedback

- **D-08:** Keep the existing visible toolbar label `Insert` in both valid contexts. Do not add a second action, context-changing button name, persistent helper, or new toolbar surface.
- **D-09:** On a genuinely empty frame, the enabled action tooltip is `Insert an empty key and start a new interpolation segment.` Existing real-key Insert tooltip/status copy remains unchanged; the contextual tooltip alone distinguishes the two valid behaviors.
- **D-10:** After exact acceptance, publish `Inserted empty key at frame {N}. New interpolation segment started.` Keep the cursor at `{N}`, select the new real key, and show its empty Paint canvas ready for drawing.

### Break lifecycle across existing key operations

- **D-11:** Duplicate and Copy/Paste copy only Paint key payload/content. They never copy an intentional break to a fresh key identity; creating a new segment remains an explicit empty-frame Insert action.
- **D-12:** Deleting the real key that owns a break deletes that break in the same atomic delete transaction. The break is never transferred to the next key and never persists as an orphan frame-position boundary.
- **D-13:** Pasting/replacing Paint content onto an existing break-bearing key preserves the destination key identity and its break; only the Paint payload changes.
- **D-14:** Every accepted identity-preserving timing edit carries the break with the stable key identity, including direct drag, group drag, directional Push Left/Right, and partial Key Spacing. If ordering changes, the break applies to the owning key's new predecessor.

### Availability and rejection feedback

- **D-15:** Known-invalid empty-frame Insert targets are disabled before activation and expose the exact reason through the existing guarded tooltip. The physical resolver remains the fail-closed authority for stale/racing inputs and must reject before mutation.
- **D-16:** Product reasons are target-specific: generated render-only frame, resolved linked occurrence, unresolved linked frame, target occupied for the empty-frame operation, invalid destination, out-of-capacity destination, and edit already in flight. Do not expose raw resolver codes as user copy.
- **D-17:** A race rejected after activation publishes the same verbatim mapped product reason used by the disabled tooltip. One reason mapper owns both surfaces so preflight and rejection cannot disagree.
- **D-18:** Failure leaves records, breaks, Loop Clips, selection, cursor, and history unchanged. No modal, alert, automatic navigation, nearest-empty fallback, or partial optimistic marker is introduced.

### Locked regression boundaries

- **D-19:** Existing Insert/Open-and-Insert behavior on an occupied selected real key must not regress. The new empty-frame path is additive and context-dispatched.
- **D-20:** Generated/non-editable frames, resolved or unresolved linked frames, invalid/out-of-capacity destinations, and occupied targets for the new empty-frame intent cannot create a key or break.
- **D-21:** Existing physical-key selection, painting, deletion, Copy/Paste, drag, Key Spacing, Undo/Redo, Loop Clip, and interpolation workflows remain unchanged except where the decisions above explicitly describe break participation.

### Claude's Discretion

- Exact left-edge cut dimensions, color, and layering within the existing cell tokens, provided it stays compact, visible in all supported themes/states, adds zero geometry, and cannot be confused with the 3px Loop Rail's canonical white endpoint cuts.
- Exact target-specific failure sentences beyond the locked enabled tooltip and accepted success message, provided one mapper supplies both tooltip and status copy.
- Exact additive persistent field/type shape and resolver intent name, provided the break is stable-key-owned, fail-closed, revisioned, persisted, and included in complete accepted history snapshots.
- Exact test decomposition and UI-SPEC need assessment; native visual UAT remains required for the marker and contextual Insert behavior.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Locked product intent and phase boundaries

- `SPECS/milestone-v0.9.0-new-phases/phase-43.1-intentional-gap-insert-prompt.md` — primary Phase 43.1 product contract: empty-key insertion, persistent incoming interpolation break, failure invariants, scope boundaries, and native acceptance. The roadmap explicitly activates this SPECS file for Phase 43.1.
- `.planning/ROADMAP.md` §`Phase 43.1` — active phase placement after Phase 43 and before Phase 44; canonical source reference and dependency.
- `.planning/REQUIREMENTS.md` — milestone requirement authority; research/planning must derive Phase 43.1 requirement IDs and traceability without changing Phase 43 or release requirements.
- `.planning/PROJECT.md` — project constraints: macOS/Tauri/Preact Signals, Canvas timeline, smooth 15/24 fps preview, and established physical-frame architecture.

### Prior accepted physical-timeline and Loop Clip authority

- `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-CONTEXT.md` — latest accepted parent-authoritative physical document, Loop Rail, Loop Clip, interpolation, selection, and atomic history boundaries that Phase 43.1 must preserve.
- `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-UAT.md` — accepted native Phase 43 behavior that ordinary Loop Clip and interpolation flows must retain.
- `.planning/phases/43-hold-loop-clips-filmstrip-capsule/43-VALIDATION.md` — current automated/native coverage map for adjacent physical/Loop Clip behavior.

### Existing implementation seams to extend

- `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` — current computed Insert availability, contextual disabled reasons, `runPhysicalAction`, legacy occupied-key `insert-slot`, empty-key add/paste seam, and status publication.
- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` — existing visible `Insert` action, guarded tooltip copy, physical-cell presentation, selection/navigation, and zero-extra-row geometry.
- `app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts` — real/generated/empty cell view models and existing tooltip/accessibility-copy projection.
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` — canonical real-key/document types, strict parsers, Loop Clips, stable IDs, and revision input contract.
- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` — physical edit intents, mapping/finalization, strict-interior generated-cell derivation, validation, and fail-closed proposal authority.
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` — exact accepted publication, staging, settlement, rollback, and complete records/Loop Clips transaction path.
- `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` — accepted-only complete before/after snapshots and one-command Undo/Redo replay.
- `app/src/stores/physicPaintStore.ts` — physical document/interpolation installation, generated-cache regeneration, revisions, and persistence-facing store authority.
- `app/src/lib/physicPaintPersistence.ts` — physical document parse/hydrate/save/reopen path.
- `app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts` — existing reusable blank payload construction and Copy/Paste behavior whose payload-only semantics must remain.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `computeInsertAvailability` currently enables Insert only for a selected real key; it is already a Preact Signals `computed` source with a paired disabled reason, so the empty-cursor branch can extend the same action instead of adding UI state.
- `addEmptyKey` and `toEmptyKeyPayload` already create a real empty key through the acknowledged paste-to-empty machinery. The phase should reuse the proven blank payload and coordinator path while adding the break to the same atomic semantic delta.
- `runPhysicalAction` already centralizes launch readiness, pending-operation rejection, resolver invocation, exact coordinator publication, and status copy.
- `buildGuardedActionTooltipCopy` already renders either the enabled description or `unavailable: {verbatim reason}`; D-15/D-17 extend this established feedback pattern.
- `useRotoPhysicalEditHistory` already records one immutable complete accepted before/after command and replays it through the coordinator for Undo/Redo.

### Established Patterns

- The physical document is the canonical parent-authoritative truth. Child/UI state must not present a key, break, marker, or selection as accepted before exact settlement.
- Generated interpolation cells are strict interiors derived from ordered real keys. Phase 43.1 must add a local incoming-boundary exclusion to this derivation rather than create another interpolation engine or materialize frames.
- Persistent physical changes must participate in strict parsing, canonical revision fingerprints, project equality, bridge payloads, save/reopen, complete history snapshots, and replay validation.
- Shared Studio state uses Preact Signals/computed values; avoid hook/effect mirroring and prop-drilling sprawl.
- The fixed physical strip and Loop Rail have zero spare-row authority. The break marker belongs inside existing cell presentation and must not alter row height or Loop Rail interaction.
- MemPalace recall reinforced the accepted-only interpolation authority rule: interpolation document updates must travel through the existing physical transaction and must never split authority back into legacy settings.

### Integration Points

- Context-dispatch `Insert`: selected real key continues through current `insert-slot`; genuinely empty current cursor uses a new atomic empty-key-plus-break intent; every other current-cell kind receives a mapped reason.
- Physical model/revision/persistence: store an additive stable-key-owned break collection or equivalent canonical field and include it in strict parsing, revision, bridge equality, persistence, and complete snapshots.
- Projection/cache: suppress generated cells only for adjacent pairs whose right key owns a break; later spans from that key remain normal.
- Existing operations: deletion removes owned breaks, payload replacement preserves destination breaks, fresh-key Duplicate/Paste omits breaks, and identity-preserving mappings rebase breaks by identity without separate frame-position logic.
- Workflow strip/presentation: draw the left-edge cut and append tooltip/accessibility copy without creating a new interactive target.

</code_context>

<specifics>
## Specific Ideas

- Example truth: `[0]--[3]--[6]    [10]`, then adding frame 13 yields `[0]--[3]--[6]    [10]--[13]`.
- Empty-frame enabled tooltip: `Insert an empty key and start a new interpolation segment.`
- Accepted status: `Inserted empty key at frame {N}. New interpolation segment started.`
- Break tooltip/accessibility fact: `Starts a new interpolation segment`.
- The marker is a compact left-edge cut on the owning key, always visible when a predecessor exists even if Interpolation is Off.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 43.1-intentional-gap-insert-and-local-interpolation-breaks*
*Context gathered: 2026-08-09*
