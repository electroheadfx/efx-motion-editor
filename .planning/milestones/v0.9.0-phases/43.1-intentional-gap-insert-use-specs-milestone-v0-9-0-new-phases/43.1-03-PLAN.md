---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 03
type: tdd
wave: 3
depends_on:
  - 43.1-02
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts
  - app/src/types/project.ts
  - app/src/lib/physicPaintPersistence.ts
  - app/src/lib/physicPaintPersistence.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
autonomous: true
requirements:
  - GAP-01
  - GAP-03
  - GAP-06
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Break ownership round-trips byte-identically through project serialization and save/reopen, while absent persisted state hydrates to the frozen empty collection."
    - "The parent bridge independently rejects stale revision, malformed ownership, false semantic delta, or unrelated state changes before any accepted publication."
    - "Per D-01/D-18, records, breaks, Loop Clips, interpolation, selection, cursor, canvas/reference state, and history settle or roll back together."
    - "One accepted empty-segment command creates one history entry; one Undo removes key and break, and one Redo restores both."
  artifacts:
    - path: "app/src/types/project.ts"
      provides: "Optional persisted incomingInterpolationBreakKeyIds member"
      contains: "incomingInterpolationBreakKeyIds"
    - path: "app/src/lib/physicPaintPersistence.ts"
      provides: "Strict save/hydrate round trip for stable owner IDs"
      contains: "incomingInterpolationBreakKeyIds"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      provides: "Complete break capture, staging, payload, settlement, rollback, and replay ports"
    - path: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      provides: "Accepted-only break equality and ordinary-operation Undo/Redo replay"
  key_links:
    - from: "app/src/lib/physicPaintPersistence.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts"
      via: "hydrated project member is revalidated by the canonical document parser"
      pattern: "parsePhysicPaintRotoPhysicalDocument"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      to: "app/src/lib/physicPaintBridge.ts"
      via: "complete staged records/interpolation/breaks/Loop Clips payload with expected revision"
      pattern: "incomingInterpolationBreakKeyIds"
    - from: "app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts"
      to: "app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts"
      via: "Undo/Redo replays the complete immutable before/after snapshot"
      pattern: "insert-empty-segment"
  prohibitions:
    - "Do not add migration code for old projects; absent additive state means empty under the canonical parser."
    - "Do not stage or roll back only records while leaving breaks, Loop Clips, selection, cursor, or canvas state changed."
    - "Do not trust child semantic claims without parent recomputation, and do not expose internal validation text as product copy."
---

<objective>
Make break ownership durable and atomic across project persistence, parent validation, coordinator settlement, and accepted-only history.

Purpose: Close the authority gaps that could otherwise leave an accepted key without its break, a break without its key, or stale/partial state after rejection, per D-01, D-12..D-14, and D-18.
Output: Byte-identical save/reopen, strict parent semantic validation, complete rollback, and one-command Undo/Redo.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-02-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
@app/src/types/project.ts
@app/src/lib/physicPaintPersistence.ts
@app/src/lib/physicPaintBridge.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts

<interfaces>
All complete physical snapshots and replay targets gain `incomingInterpolationBreakKeyIds`. Replay target wins during Undo/Redo; otherwise `proposal.nextIncomingInterpolationBreakKeyIds` wins; otherwise current canonical state is retained. The persisted `McePhysicPaintRotoPhysicalDocument` member is optional on disk but required after runtime parse.
</interfaces>
</context>

<planning_note>spec-less probe fallback skipped: phase had no requirement IDs at plan-phase init; goal-backward must_haves were derived from the activated SPECS prompt, CONTEXT.md, RESEARCH.md, and canonicalized GAP-01..GAP-06 instead. The 58,000-token estimate is provisional and un-calibrated because no completed-phase calibration samples exist.</planning_note>

<scope_rationale>PATTERNS.md confirms exact Loop Clip analogs across all 11 files. They remain one plan because persistence/model/bridge acceptance, coordinator rollback, and history replay form one indivisible complete-document contract: splitting would allow an accepted durable break shape to exist without the matching rollback/replay invariant. The three tasks are strictly sequential within Wave 3—canonical persisted state first, parent semantic acceptance second, coordinator/history settlement third—and no task exceeds five files.</scope_rationale>

<assumptions>
- Project equality uses the same canonical physical revision source rather than a separate frame-based comparison.
- Existing coordinator record ports can be extended with `getIncomingInterpolationBreakKeyIds` and `replaceIncomingInterpolationBreakKeyIds` without a new authority object.
- Existing accepted reconciliation/rollback infrastructure is retained; this plan adds complete break state before Plan 04 proves immediate blank-canvas behavior.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and compare the complete break-bearing document</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts, app/src/types/project.ts, app/src/lib/physicPaintPersistence.ts, app/src/lib/physicPaintPersistence.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts — canonical encoder, revision builder, physical document equality/allowlist, and Loop Clip additive-state policy
    - app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts — parser/revision/round-trip gauntlet analog
    - app/src/types/project.ts — `McePhysicPaintRotoPhysicalDocument` persisted shape
    - app/src/lib/physicPaintPersistence.ts — strict persisted allowlist, serialize and hydrate paths
    - app/src/lib/physicPaintPersistence.test.ts — canonical physical-document save/reopen fixtures
  </read_first>
  <behavior>
    1. RED→GREEN: absent persisted member hydrates through the public document/persistence interface to frozen empty; a non-empty valid owner list round-trips byte-identically.
    2. RED→GREEN: malformed persisted ownership rejects hydration, then duplicate and orphan ownership each reject in their own subsequent observable case.
    3. RED→GREEN: non-empty ownership changes canonical revision and project equality, while empty ownership preserves additive compatibility.
    4. RED→GREEN: save/reopen retains stable owner IDs after accepted timing changes without predecessor/frame-derived persistence.
  </behavior>
  <action>Execute the numbered persistence behaviors as vertical public-interface micro-cycles. Add and commit one failing save/hydrate observation, implement only the optional persisted member plus frozen runtime reconstruction needed for GREEN, and rerun unchanged. Add malformed, duplicate, and orphan cases one at a time, extending the closed shape and canonical parser only after each RED. Then add revision/equality and timing-change round-trip observations in separate cycles with the minimum deterministic encoding/serialization changes needed. Serialize canonical owner IDs only and route hydration through `parsePhysicPaintRotoPhysicalDocument`. Preserve Plan 01 empty encoding; add no migration/version branch or frame-position metadata. Refactor only while all focused tests are green, then run the final command.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts src/lib/physicPaintPersistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `McePhysicPaintRotoPhysicalDocument.incomingInterpolationBreakKeyIds` is optional on disk and always reconstructed at runtime.
    - JSON save/reopen preserves the exact owner ID array for a valid document.
    - Malformed/duplicate/orphan persisted state throws before installation.
    - Empty ownership leaves existing empty-document revisions byte-stable; non-empty ownership changes revision and equality.
    - The focused command fails before production implementation and passes after GREEN.
  </acceptance_criteria>
  <done>Break ownership is a strict additive persisted fact with canonical revision/equality and byte-identical save/reopen behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Independently validate empty-segment semantic deltas in the parent bridge</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts — physical apply handler, operation-kind semantic checks, current/staged document reads, and rejection result construction
    - app/src/lib/physicPaintBridge.test.ts — ordinary operation fixtures, stale revision tests, Loop Clip atomicity tests, and no-mutation snapshots
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts — exact `insert-empty-segment` proposal and semantic-delta contract from Plan 02
    - app/src/types/physicPaint.ts — closed apply/result guards from Plan 01/02
  </read_first>
  <behavior>
    1. RED→GREEN: the public parent apply interface accepts only a delta that adds one blank real key at destination and exactly its ID to breaks while preserving interpolation/Loop Clips.
    2. RED→GREEN: false insertedKeyId or destination claims reject before mutation; subsequent cycles add non-blank payload and unrelated record-change rejection one observable case at a time.
    3. RED→GREEN: extra break additions/removals, Loop Clip changes, and interpolation changes each reject in separate before-state-equality cases.
    4. RED→GREEN: occupied/generated/linked/capacity races and stale revision each reject with complete before-state equality.
    5. RED→GREEN: accepted result echoes the complete break collection used for accepted revision.
  </behavior>
  <action>Execute the numbered parent-authority behaviors as vertical public-interface micro-cycles. Start with one accepted apply test, commit it failing, then minimally extend the existing operation-kind validation for GREEN. Add each false claim, unrelated-state mutation, race/stale rejection, and result-echo behavior one at a time; after every RED, implement only the corresponding independent current-versus-staged comparison and rerun the unchanged test. Require one new stable identity at the claimed in-capacity destination, the validated empty Paint payload, and one matching owner addition before the existing one-shot document replacement. Keep diagnostics internal for Plan 04's mapper. Refactor only after all cases are green, then run the final focused regression command.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts -t "validates insert-empty-segment semantics independently|rejects stale false or unrelated empty-segment deltas without mutation"</automated>
  </verify>
  <acceptance_criteria>
    - Parent validation recomputes the allowed before/after delta for records and breaks.
    - Interpolation and Loop Clips are byte-identical for this operation.
    - Rejected cases assert records, breaks, Loop Clips, interpolation, selection, cursor, and revision are unchanged.
    - Accepted result contains the canonical complete owner collection and revision.
    - The identical command is used for RED and GREEN.
  </acceptance_criteria>
  <done>The parent bridge is the fail-closed semantic authority for the empty-key-plus-break transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Settle, roll back, Undo, and Redo key plus break as one command</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts — snapshot type, clone helpers, capture/stage/payload/deferred-settlement/rollback/replay paths
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts — staged payload, rejection, timeout, settlement mismatch, and accepted-output fixtures
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts — complete snapshot equality, ordinary-operation allowlist, command push, Undo/Redo replay
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts — one-command before/after replay pattern
  </read_first>
  <behavior>
    1. RED→GREEN: coordinator payload, staged local state, and accepted before/after snapshots expose complete cloned breaks through the coordinator interface.
    2. RED→GREEN: transport rejection and timeout restore every physical/UI-reference surface; malformed result, operation mismatch, and revision mismatch follow as separate observable rollback cycles.
    3. RED→GREEN: one accepted `insert-empty-segment` creates one history command and one Undo removes key plus break.
    4. RED→GREEN: one Redo restores key plus break at the same stable identity/frame.
    5. RED→GREEN: rejected and no-change operations add no history entry and preserve existing redo state.
  </behavior>
  <action>Execute the numbered settlement/history behaviors as vertical public-interface micro-cycles. First add one failing coordinator snapshot/payload observation, then minimally extend ports and immutable cloning for GREEN. Add each rollback class one at a time, restoring complete breaks before selection/cursor/accepted output only after its RED proves the gap. Add accepted command plus Undo as one observable cycle, then Redo as the next, and rejected/no-change ledger behavior last. Stage replay-target breaks first, proposal next breaks second, current breaks otherwise; include them in staged revision/apply payload. Extend history equality and ordinary-kind recognition only when the corresponding history test is RED, and replay complete snapshots through the coordinator. Preserve accepted-only publication and Phase 43 Loop Clip/background behavior. Refactor only while green, then run the final combined regression command.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every coordinator snapshot and accepted output contains frozen/cloned incoming break IDs.
    - Every listed rejection path restores all complete physical and UI-reference state, with no accepted output.
    - One Undo and one Redo visibly change both record and owner collection in tests.
    - Rejected/no-change operations add no history command.
    - Git history preserves RED before GREEN for Plan 03 behavior.
  </acceptance_criteria>
  <done>Empty key and incoming break settle, reject, roll back, Undo, and Redo as one complete accepted physical command.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new source or test file paths. It introduces:
- Persisted field: optional `McePhysicPaintRotoPhysicalDocument.incomingInterpolationBreakKeyIds`.
- Coordinator snapshot/port fields for complete incoming break get/replace/clone/stage/replay behavior.
- History equality and ordinary-operation support for `insert-empty-segment`.
- Parent semantic validation for the `insert-empty-segment` delta.
- Persistence and history test fixtures embedded in existing test files; no new helper file.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| project JSON → persistence/model parser | Saved data may contain malformed, duplicate, orphan, or semantically inconsistent break owners. |
| child proposal → parent semantic validator | A stale or tampered proposal may claim an empty-segment delta while changing unrelated state. |
| coordinator staged state → accepted acknowledgement | Rejection, timeout, or mismatch must not leave partial child state. |
| accepted command → Undo/Redo replay | Replay must restore the exact complete document, not reconstruct ownership by frame. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.1-09 | Tampering | persistence hydration | high | mitigate | Closed member validation plus canonical referential parser; malformed/duplicate/orphan owner cases fail before install. |
| T-43.1-10 | Tampering / Repudiation | bridge semantic delta | high | mitigate | Parent independently compares complete before/after records, breaks, interpolation, and Loop Clips against claimed operation fields. |
| T-43.1-11 | Tampering | stale revision | high | mitigate | Reject expectedRevision mismatch before staging publication; focused total-no-mutation tests. |
| T-43.1-12 | Tampering / Denial of Service | coordinator rollback | high | mitigate | Complete immutable snapshots and exhaustive rejection/timeout/mismatch rollback tests cover every mutated surface. |
| T-43.1-13 | Repudiation | history replay | medium | mitigate | Accepted-only command ledger stores exact before/after break collections and replays through the same coordinator authority. |
</threat_model>

<verification>
- Run each task's focused one-shot Vitest command RED then GREEN.
- Run all Plan 03 test files together after Task 3.
- `pnpm --dir app run typecheck` exits 0.
- `git diff --check` exits 0.
</verification>

<success_criteria>
- GAP-03 is covered across strict persistence, canonical revision/equality, parent validation, coordinator rollback, and history replay.
- D-18 failure invariants include every listed physical/UI/history surface.
- Save/reopen and Undo/Redo use stable identity rather than frame reconstruction.
- No old-project migration branch or secondary authority is introduced.
</success_criteria>

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-03-SUMMARY.md` when done.
</output>
