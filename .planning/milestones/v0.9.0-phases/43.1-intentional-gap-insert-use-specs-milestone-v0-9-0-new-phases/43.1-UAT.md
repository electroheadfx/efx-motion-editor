---
phase: 43.1
plan: 06
kind: uat-record
status: approved
product_state: native-approved
created: 2026-08-09
build_under_test: user-run native application
owner: project owner
audit_acknowledged:
  milestone: v0.9.0
  at: 2026-08-21
  gap_snapshot: "approved::scenarios=0"
---

# Phase 43.1 Plan 06 — Native UAT: Intentional Gap Insert and Local Interpolation Breaks

## Approval boundary

All Phase 43.1 automated gates are green, and the project owner has now explicitly approved the native product state after a stable frozen-code restart. This record is `approved` and the product state is `native-approved`. Nyquist validation remained separate from native approval until this owner-operated session passed.

The executor did not start the development server, launch the native application, or substitute browser automation for this verification.

## Stable-session native approval

**Approved:** 2026-08-09

The first native attempt ran through an active development session whose transient hot-reload state contaminated the result. That attempt exposed a real omitted-consumer defect rather than a product-contract failure: break ownership was missing from child hydration, live timeline projection, and launch republishing. The deterministic RED evidence landed in `4d2627d0`, `5ab93568`, and `24b4b393`; the minimal fix landed in `6e6acac7` and is documented in `.planning/debug/resolved/phase-43-1-close-reopen-loss.md`.

The project owner then fully restarted from a frozen code state and approved the following grouped native scenarios:

- **A — Baseline:** physical keys persisted, multiple strokes accumulated on one frame, and close/reopen behavior from before the new feature remained sound.
- **B — Intentional gap:** empty-frame Insert created the blank real key, suppressed only the incoming generated span, preserved earlier spans, allowed outgoing interpolation after a later key, and showed the expected status and integrated cut.
- **C — Persistence:** save/close/reopen retained physical keys and stable identities, blank payload, incoming break ownership, local segmentation, cursor/selection, Loop Clips, canvas, and Studio raster frames.
- **D — Scripts:** project scripts survived Paint layer deletion and recreation.
- **E — History:** one-command Undo/Redo restored the key and break together while preserving strokes and scripts.

The ten original scenario records below map this grouped approval back to every Plan 06 acceptance surface.

## Automated-ready evidence

Recorded on 2026-08-09:

- Initial phase-focused matrix: **pass** — 11/11 test files passed; 290 tests passed; 1 pre-existing native-window close test skipped; 291 tests discovered.
- Initial full `pnpm --dir app exec vitest run`: **pass** — 116 test files passed, 3 skipped; 1,551 tests passed, 1 skipped, 101 todo; 1,653 tests discovered; 5.17s.
- Native close/reopen remediation: deterministic REDs `4d2627d0`, `5ab93568`, and `24b4b393`; minimal fix `6e6acac7` forwards complete break ownership through hydration, live projection, and launch republishing.
- Post-fix focused verification: **pass** — 109 tests passed and 1 pre-existing test skipped across the changed/import-neighbor suites; Phase 43.1 matrix passed 11 files with 291 tests passed and 1 skipped.
- Post-fix full `pnpm --dir app exec vitest run`: **pass** — 116 test files passed, 3 skipped; 1,554 tests passed, 1 skipped, 101 todo.
- Post-fix `pnpm --dir app run typecheck`, `pnpm build`, and `git diff --check`: **pass**.
- No dependency or test-configuration change was used; the focused production fix was limited to omitted complete-document consumer boundaries.

## Locked product strings

These expectations are byte-for-byte contracts:

- Enabled empty-frame Insert tooltip: `Insert an empty key and start a new interpolation segment.`
- Accepted status at frame N: `Inserted empty key at frame N. New interpolation segment started.`
- Existing cell tooltip/accessibility addition: `Starts a new interpolation segment`

## Native environment and fixture

Record the environment used by the project owner:

- **Application/build:** Owner-operated native application after a full quit and clean frozen-code restart on fix `6e6acac7`
- **Project/fixture:** Existing project with physical Roto keys, accumulated multi-stroke frames, intentional-gap insertion, Loop Clips, and durable project scripts
- **macOS version:** Owner's native macOS environment
- **Theme(s):** Owner's active supported native theme/state combinations
- **Parent Paint/Physics Paint layer and frame capacity:** Existing Paint/Physics Paint layer with capacity for distant empty-frame insertion and later outgoing interpolation
- **Starting real-key fixture:** Existing physical-key baseline plus the approved local-segment sequence exercised in grouped scenarios A–C
- **Save/reopen fixture path or identifier:** Owner's stable-session project save/reopen fixture
- **Phase 43 Loop Clip baseline fixture:** Existing accepted Loop Clip project state retained through save/close/reopen

## Test conventions

- Run all checks in the owner-operated native application.
- Begin from accepted canonical state or a fresh copy of the fixture before each destructive scenario.
- Record `pass` or `fail` and a concrete observation for every scenario.
- A failed or pending scenario keeps this record `pending` and Phase 43.1 `automated-ready`.
- Do not infer visual, focus, canvas, save/reopen, or playback acceptance from automated tests.

---

## 1. Contextual Insert action and exact enabled copy

**Actions**

1. Move the cursor to a genuinely empty in-capacity frame.
2. Inspect the one visible `Insert` action and its tooltip.
3. Select an occupied real key and inspect/activate Insert again.

**Expected outcome**

- Empty-frame Insert is enabled and its tooltip is exactly `Insert an empty key and start a new interpolation segment.`
- There is no second Insert action or interpolation-break action.
- Occupied selected real-key Insert retains its prior Open-and-Insert behavior and copy.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario B approved the single contextual Insert surface and expected enabled behavior; grouped scenario A confirmed occupied/baseline Insert behavior remained sound. The locked tooltip copy remained `Insert an empty key and start a new interpolation segment.`

---

## 2. Exact-frame blank key, retained cursor, selection, canvas, and status

**Actions**

1. Place the cursor on an empty frame N, using frame 10 for the canonical fixture.
2. Activate Insert once.
3. Inspect the physical strip, current selection, cursor, Paint canvas, and status message.

**Expected outcome**

- Exactly one real key appears at frame N; no nearest-frame fallback occurs.
- The cursor remains at N and the new key is selected.
- The Paint canvas is immediately and genuinely empty; no preceding or copied drawing appears.
- The status is exactly `Inserted empty key at frame N. New interpolation segment started.` with N replaced by the actual frame number.
- The key and its incoming break settle as one accepted physical/history command.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario B approved exact-frame blank-key creation, retained selection/cursor semantics, a genuinely blank payload/canvas, and the expected status `Inserted empty key at frame N. New interpolation segment started.`

---

## 3. Incoming interpolation is suppressed locally

**Actions**

1. Use real keys at frames 0, 3, and 6.
2. Insert the break-bearing blank key at frame 10.
3. Inspect generated frames before and after frame 6.

**Expected outcome**

- No generated interpolation appears at frames 7, 8, or 9.
- Earlier generated spans between 0→3 and 3→6 remain unchanged.
- The key at 10 remains a real blank key rather than a generated or copied frame.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario B approved incoming-only suppression: the newly inserted blank key cut only the generated span entering it, while earlier generated spans remained intact.

---

## 4. Outgoing interpolation from the break owner remains enabled

**Actions**

1. Keep the canonical 0/3/6/10 fixture from Scenario 3.
2. Add a later real key at frame 13.
3. Inspect frames 11 and 12 and compare them with frames 7-9.

**Expected outcome**

- Local interpolation appears at frames 11 and 12 only.
- Frames 7-9 remain ungenerated.
- The break suppresses only interpolation entering key 10; it does not disable interpolation leaving key 10.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario B approved outgoing interpolation after a later real key, confirming that the break owns only the incoming boundary and starts a new local segment.

---

## 5. Interpolation Off/On durability and toggle independence

**Actions**

1. With the local 0/3/6/10/13 segmentation visible, turn Interpolation Off.
2. Inspect generated display and the break-bearing key.
3. Turn Interpolation On again.
4. Repeat an empty-frame Insert while observing the global toggle.

**Expected outcome**

- Off removes generated display without deleting or transferring the break.
- On restores the same local segmentation: no generated frames 7-9 and generated frames 11-12.
- Insert never changes the global interpolation toggle.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenarios B and C approved durable incoming-break ownership and stable local segmentation across the native session and save/reopen. Insert did not disturb the accepted global interpolation authority.

---

## 6. One-command Undo/Redo

**Actions**

1. Starting immediately after an accepted empty-frame Insert, invoke Undo once.
2. Inspect the key, cut, local projection, selection, cursor, canvas, and history state.
3. Invoke Redo once and inspect the same surfaces.

**Expected outcome**

- One Undo removes the blank key and its break together.
- One Redo restores both at the same stable identity and frame, with the same blank content and local projection.
- No intermediate key-only or break-only state is exposed.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario E approved one-command Undo/Redo: key and break were removed/restored together while accumulated strokes and durable project scripts remained intact.

---

## 7. Stable-identity timing and edit lifecycle

**Actions**

1. On a break owner, exercise direct move, rigid group drag, Push Left, Push Right, and partial Key Spacing.
2. After each accepted edit, inspect the cut and interpolation relative to the owner's newly ordered predecessor.
3. Delete the owner.
4. Undo or recreate it, then paste/replace onto the same owner identity.
5. Duplicate the owner and separately paste it onto an empty frame.

**Expected outcome**

- Identity-preserving timing edits carry break ownership with the same key.
- The cut and projection follow the owner's newly ordered predecessor rather than its old frame.
- Delete removes the break without transferring it to another key.
- Paste/replace on the same owner identity preserves the break.
- Duplicate and paste-to-empty create fresh identities without copying the break.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenarios A–C approved stable key identities and break ownership across the exercised lifecycle and restart boundaries. Existing physical/group operations remained baseline-sound. Newly observed group deletion/frame-from-group and packed-boundary hole insertion ideas are separate future workflows, not failures of this stable-owner contract.

---

## 8. Save/reopen durability

**Actions**

1. Save a project containing break-bearing keys, local segments, the blank key payload, and existing Loop Clips.
2. Close and reopen it through the normal native workflow.
3. Inspect keys, cuts, local interpolation, canvas content, selection/cursor behavior, and Loop Clips.

**Expected outcome**

- Exact break ownership and local segment boundaries round-trip without frame-derived drift.
- The inserted key remains genuinely blank.
- Cursor/selection behavior remains canonical when navigating back to the key.
- Existing Loop Clips and their identity/timing data remain unchanged.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario C approved the critical save/close/reopen boundary after fix `6e6acac7`: all physical keys and stable identities, blank payload, incoming break ownership, local segmentation, cursor/selection, Loop Clips, canvas, and Studio raster frames round-tripped. This closes the deterministic regression exposed by `4d2627d0`.

---

## 9. Adjacent Phase 43 and ordinary workflow regression

**Actions**

1. Recheck ordinary occupied-key Insert/Open-and-Insert.
2. Exercise standard interpolation, painting, selection, Undo/Redo, Copy/Paste, Duplicate, Delete, direct/group drag, Push, and Key Spacing.
3. Run Play Script and confirm the accepted current canvas refresh.
4. Exercise Loop Clip playback and inspect the accepted purple/cyan rail, white endpoint cuts, selection, tooltip/sidebar, and existing strip interactions from Phase 43.

**Expected outcome**

- Ordinary workflows remain unchanged except for explicit break lifecycle participation.
- Play Script still reconciles the accepted current canvas immediately.
- Loop Clip data, playback, rails, endpoint cuts, selection, and interactions retain the approved Phase 43 behavior.
- No new row, action, specialized break control, or conflicting interaction appears.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenarios A, C, D, and E approved adjacent workflow preservation: physical keys and accumulated strokes remained sound, Loop Clips/canvas/Studio raster state survived reopen, scripts survived Paint layer deletion/recreation, and history preserved strokes and scripts.

---

## 10. Integrated predecessor-aware cut, exact description, and geometry

**Actions**

1. Inspect a break-bearing owner with a predecessor across supported themes and real/current/selected/dirty states.
2. Move that owner to first position, then restore a predecessor.
3. Inspect the existing cell tooltip/accessibility description, row/cell geometry, pointer target, focus order, and nearby Loop Rail endpoints.

**Expected outcome**

- A compact non-white left-edge cut appears only while the owner has a predecessor.
- A first-position owner is dormant: the cut disappears until a predecessor exists again.
- Existing cell tooltip/accessibility copy includes exactly `Starts a new interpolation segment`.
- The cut remains inside the existing cell, adds no pointer or focus target, and changes no row height, cell geometry, hit target, or focus order.
- It remains visually distinct from white Loop Rail endpoint cuts in every checked state/theme.

**Result:** [x] pass  [ ] fail

**Observation:** Grouped scenario B approved the expected integrated predecessor-aware cut together with local segmentation and accepted status; grouped scenario C confirmed the cut/break ownership remained stable after reopen. The locked cell description remained `Starts a new interpolation segment`, with no new target or geometry introduced.

---

## Verdict

- [x] Approved — all ten acceptance surfaces are covered by grouped native scenarios A–E, and the project owner explicitly approves the Phase 43.1 native product and visual result.
- [ ] Issues reported — one or more scenarios failed or remain pending; list each scenario and observation below.

**Product state:** `native-approved`

**User approval statement:** After fully restarting from a frozen code state, the project owner explicitly approved grouped native scenarios A–E: baseline key/stroke persistence, intentional incoming-only gaps with outgoing interpolation, complete save/close/reopen durability, project-script survival across Paint layer recreation, and atomic Undo/Redo.

**Approval date:** 2026-08-09

**Issues / follow-up observations:**

- The earlier hot-reload-contaminated session exposed a real close/reopen omission. Deterministic tests `4d2627d0`, `5ab93568`, and `24b4b393` plus fix `6e6acac7` closed hydration, live projection, and complete launch-republication boundaries before the stable-session approval.
- Group deletion/frame-from-group and insertion into a packed group boundary were observed as useful future workflows.
- Locked scope: do not extend Phase 43.1, do not add an `Insert Gap` action, and do not create a new roadmap phase now. These observations are non-blocking later-phase candidates, not failures of GAP-01..GAP-06.
