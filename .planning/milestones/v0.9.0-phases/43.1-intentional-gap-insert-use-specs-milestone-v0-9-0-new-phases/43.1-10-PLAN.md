---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 10
type: tdd
wave: 9
depends_on:
  - 43.1-09
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
autonomous: true
gap_closure: true
requirements:
  - GAP-03
  - GAP-06
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GAP-03 / D-01: the parent reruns the sole canonical resolver for every ordinary mapping intent and accepts only the exact complete resulting document tuple."
    - "GAP-03 / D-14: records, stable ordering, selection, Loop Clips, incoming-break ownership, interpolation state, and revision must all match parent recomputation before mutation."
    - "D-18: a structurally valid but semantically unrelated proposal rejects without document replacement, publication, delivered-operation success, or accepted-command ledger entry."
    - "GAP-03 / D-18: operation-ID deduplication fingerprints the canonical stable serialization of exact ordinary intent before cached success lookup can authorize a request."
    - "GAP-06 / D-21: canonical proposals for Insert, delete, move, group move/delete, Force Spacing, Duplicate/Paste, and intentional-gap insertion remain accepted with existing outcomes."
  artifacts:
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Intent-aware dedup fingerprint, parent-side ordinary intent recomputation, and complete semantic equality gate"
      contains: "fingerprintApplyPayload|applyPhysicPaintRotoPhysicalMap"
    - path: "app/src/lib/physicPaintBridge.test.ts"
      provides: "Canonical acceptance and adversarial no-publication matrix for every mapping operation"
  key_links:
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "resolvePhysicPaintRotoPhysicalEdit"
      via: "parent reconstructs authoritative current input and resolves the submitted typed intent"
      pattern: "resolvePhysicPaintRotoPhysicalEdit"
    - from: "parent resolver result"
      to: "replacePhysicPaintRotoPhysicalDocument"
      via: "complete records/selection/Loop Clips/break/interpolation/revision equality must succeed first"
      pattern: "replacePhysicPaintRotoPhysicalDocument"
---

<objective>
Close GAP-03-B at the parent trust boundary by independently rerunning the canonical physical resolver for every existing ordinary mapping operation and rejecting any complete-document claim that differs from the exact result.

Purpose: Make the physical document genuinely parent-authoritative per D-01, including stable break ownership and all related physical state, without changing approved user workflows or duplicating resolver semantics.
Output: One fail-closed bridge validator and adversarial public-interface tests for all ordinary mapping operations.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-VERIFICATION.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-REVIEW.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-08-SUMMARY.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-09-SUMMARY.md
@app/src/lib/physicPaintBridge.ts
@app/src/lib/physicPaintBridge.test.ts

<interfaces>
`applyPhysicPaintPayload` is the public parent test seam. `applyPhysicPaintRotoPhysicalMap` currently parses the complete proposed document, validates revision/launch/layer/context, and has specialized semantic validation for empty segment, Duplicate/Paste, Play Script, and interpolation. Plan 08 supplies the strict intent; `resolvePhysicPaintRotoPhysicalEdit` remains the sole canonical ordinary mapping resolver. Preserve specialized validators where they prove payload provenance in addition to mapping semantics.
</interfaces>
</context>

## Artifacts this phase produces

- Parent-side canonical recomputation for all ordinary physical mapping operations.
- A complete semantic comparison gate covering records, ordering, selection, Loop Clips, incoming breaks, interpolation, and resulting revision.
- Adversarial no-mutation/no-publication tests for unrelated but structurally valid document proposals.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Parent-recompute one Insert Slot command and reject an unrelated valid document</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts: `fingerprintApplyPayload`, pre-recomputation `deliveredOperations` lookup, `applyPhysicPaintRotoPhysicalMap`, current document reconstruction, semantic validators, document replacement, and acceptedPhysicalCommands write
    - app/src/lib/physicPaintBridge.test.ts: existing generic apply, empty-segment semantic authority, stale rejection, and no-publication assertions
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts: resolver input/result fields used for comparison
    - app/src/types/physicPaint.ts: Plan 08 ordinary intent parser contract
  </read_first>
  <behavior>
    - RED: a structurally valid `insert-slot` payload with a valid matching intent but unrelated records or selection rejects and leaves the store/document unchanged.
    - RED: after accepting operation ID X, reuse X with a different valid ordinary intent and otherwise identical payload; the request rejects as a different fingerprint rather than returning cached success.
    - GREEN: `fingerprintApplyPayload` includes Plan 08's canonical stable intent serialization before the `deliveredOperations` lookup, and the parent rebuilds resolver input from accepted records, capacity, interpolation, Loop Clips, breaks, and exact intent.
    - Rejected semantic or dedup claims create neither successful delivered-operation state nor accepted-command history authorization.
  </behavior>
  <action>First extend `fingerprintApplyPayload` for ordinary physical edits with the canonical stable serialization supplied by Plan 08, preserving ordered authorization arrays and closed discriminants. Because the fingerprint is computed before `deliveredOperations.get`, a reused operation ID with a different valid intent must produce a different fingerprint and reject before cached success can return. Add the public adversarial sequence accepting operation ID X and then resubmitting X with changed valid intent but otherwise identical complete payload; assert failure, no cached success, no replacement/publication, and no new replay ledger authority. Then, after strict payload, authority tuple, and expected-revision validation, invoke `resolvePhysicPaintRotoPhysicalEdit` inside the parent for the Insert Slot tracer. Construct its input only from parent-accepted current state plus the parsed intent and capacity. Compare the exact canonical result with the submitted complete document and selection before calling document replacement. Include incoming-break ownership and Loop Clips even when Insert Slot leaves them unchanged. Keep all mutation and ledger writes after both fingerprint and semantic comparison gates per D-18.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exact canonical Insert Slot payload is accepted once and publishes the parent result.
    - Any valid-shaped divergent records, order, selection, Loop Clips, breaks, interpolation, or revision reject without mutation.
    - Rejection does not create replay authorization.
    - Reusing an accepted operation ID with different valid intent rejects as a fingerprint mismatch and never returns cached success.
  </acceptance_criteria>
  <done>The parent independently proves one ordinary mapping command before accepting its complete document.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact parent recomputation across the complete mapping-operation matrix</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <read_first>
    - app/src/lib/physicPaintBridge.ts: Task 1 canonical comparison seam and existing specialized semantic validators
    - app/src/lib/physicPaintBridge.test.ts: Task 1 harness plus existing Duplicate/Paste, empty-segment, Play Script, interpolation, and break lifecycle cases
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts: canonical operation truth tables to mirror through the public bridge seam
  </read_first>
  <behavior>
    - Canonical delete one/group, move one/group, Force Spacing, Duplicate, Paste one/group, and Insert Empty Segment proposals are accepted.
    - For each operation, changing one otherwise-valid field among records, stable order, selection, Loop Clips, breaks, interpolation, or revision causes rejection before mutation.
    - Stale, duplicate, missing, reordered, or ambiguous authorization rejects consistently with the resolver.
    - Empty/single-key and idempotent duplicate-delivery cases retain deterministic existing results.
  </behavior>
  <action>Generalize the tracer gate to every ordinary typed intent and centralize complete-result comparison in one bridge helper that compares normalized canonical values, not object identity. Require the parent-resolved records and ordering, selected identity/frame, complete Loop Clips, complete incoming-break collection, unchanged or canonical interpolation, and canonical document revision to match the proposal. Ensure every ordinary intent kind uses the same stable fingerprint serializer before dedup lookup, including ordered group/spacing authorization. Continue running existing payload-provenance validators for Duplicate/Paste, empty-segment paint emptiness, and any other specialized semantic delta; parent recomputation complements rather than weakens them. Add an adversarial matrix through `applyPhysicPaintPayload`, asserting the accepted store document, publication callbacks, delivered result, and command ledger remain unchanged on semantic or intent-fingerprint rejection. Do not implement deferred group UI, packed-boundary insertion, or a separate action.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every existing ordinary physical mapping operation is independently recomputed by the parent.
    - A proposal cannot substitute unrelated structurally valid state under a valid launch and revision.
    - Existing specialized Play Script, interpolation, Duplicate/Paste, and intentional-gap validation remains active.
    - Canonical adjacent workflows continue to settle with their prior results.
  </acceptance_criteria>
  <done>GAP-03-B is closed: parent authority semantically proves every existing mapping edit before publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physics Paint child payload → parent bridge | Untrusted complete-document and operation claims enter the canonical authority process. |
| Parent validator → document replacement/history ledger | A validated command becomes accepted project state and future replay authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.1-10-01 | Tampering | `applyPhysicPaintRotoPhysicalMap` | high | mitigate | Parent reruns the canonical resolver and compares the complete resulting document before mutation. |
| T-43.1-10-02 | Elevation of Privilege | Group/move/spacing operation claims | high | mitigate | Exact intent authorization, stable ordering, capacity, and complete result equality are required. |
| T-43.1-10-03 | Repudiation | Delivered operation and command ledger | high | mitigate | Include canonical exact intent in the pre-lookup fingerprint; record or return cached success/replay authority only for byte-equivalent canonical payload identity after exact semantic acceptance. |
| T-43.1-10-04 | Denial of Service | Adversarial repeated proposals | medium | mitigate | Reuse bounded resolver inputs and existing operation deduplication without performing document mutation on failure. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts`
- `pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts`
- `pnpm --dir app exec tsc --noEmit`
- `git diff --check`
</verification>

<success_criteria>
- The parent semantically proves every ordinary mapping operation using the sole canonical resolver.
- Exact ordinary intent participates in canonical operation dedup identity before cached-success lookup.
- Exact complete-state divergence or intent-fingerprint divergence rejects before all mutation/publication/history seams.
- Canonical existing operations retain accepted behavior and intentional-gap break lifecycle.
- No unrelated security review finding or deferred UI behavior enters the implementation.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Key plus incoming break remain one parent-authoritative fact | 43.1-10 | COVERED | Parent computes and compares the complete transition. |
| REQ | GAP-03 | Parent bridge proves all lifecycle edits | 43.1-10 | COVERED | Direct closure of GAP-03-B. |
| REQ | GAP-06 | Existing physical workflows remain unchanged | 43.1-10 | COVERED | Canonical acceptance matrix protects adjacent operations. |
| RESEARCH | — | Resolver/coordinator/parent validate the same complete semantic delta | 43.1-10 | COVERED | Sole resolver is invoked at parent boundary. |
| CONTEXT | D-01 | Atomic parent-authoritative command | 43.1-10 | COVERED | Complete tuple comparison before replacement. |
| CONTEXT | D-14 | Stable identity lifecycle | 43.1-10 | COVERED | Exact records/order/break comparison. |
| CONTEXT | D-18 | Rejection leaves state unchanged | 43.1-10 | COVERED | Adversarial no-publication tests. |
| CONTEXT | D-21 | Preserve adjacent workflows | 43.1-10 | COVERED | Existing canonical proposals remain accepted. |

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-10-SUMMARY.md` when done.
</output>
