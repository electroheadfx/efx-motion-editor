---
phase: 43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases
plan: 08
type: tdd
wave: 7
depends_on:
  - 43.1-06
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
autonomous: true
gap_closure: true
requirements:
  - GAP-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "GAP-03 / D-01: every ordinary physical mapping operation has one strict transport-safe intent that names the exact canonical resolver request."
    - "GAP-03 / D-14: stable identities, ordered group membership, movement targets, spacing authorization, Loop Clips, and incoming-break ownership are sufficient for deterministic parent recomputation."
    - "GAP-03 / D-18: malformed, unknown, mismatched, duplicate, or ambiguous intent data rejects before any document mutation is possible."
  artifacts:
    - path: "app/src/types/physicPaint.ts"
      provides: "Standalone transport-safe ordinary physical-edit intent union and strict runtime parser, not yet activated on the apply payload"
      contains: "PhysicPaintRotoPhysicalEditIntent"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      provides: "Single canonical intent type shared by child resolution and parent recomputation"
      contains: "PhysicPaintRotoPhysicalEditIntent"
    - path: "app/src/types/physicPaint.test.ts"
      provides: "Adversarial strict-parser matrix"
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts"
      provides: "Deterministic intent-to-proposal reproduction tests"
  key_links:
    - from: "app/src/types/physicPaint.ts"
      to: "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts"
      via: "one exported ordinary physical-edit intent union used by both transport validation and canonical resolution"
      pattern: "PhysicPaintRotoPhysicalEditIntent"
    - from: "app/src/types/physicPaint.test.ts"
      to: "PhysicPaintRotoPhysicalEditIntent"
      via: "standalone closed intent parser and canonical stable serialization tests"
      pattern: "isPhysicPaintRotoPhysicalEditIntent"
---

<objective>
Create the strict typed-intent contract required for GAP-03-B so the parent can reproduce every existing physical mapping operation through the same canonical resolver instead of trusting a child-authored complete document.

Purpose: Carry D-01 parent authority and D-14 identity semantics in a dependency-safe shared contract while failing closed per D-18. This plan deliberately leaves `PhysicPaintRotoPhysicalEditApplyPayload` and its active guard unchanged so the repository remains buildable until Plan 09 atomically updates all producers and consumers.
Output: One standalone closed intent union/parser, canonical stable serialization, and deterministic resolver reproduction tests for all current ordinary mapping operations.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-CONTEXT.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-RESEARCH.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-PATTERNS.md
@.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-VERIFICATION.md
@app/src/types/physicPaint.ts
@app/src/types/physicPaint.test.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts

<interfaces>
The existing `PhysicPaintRotoPhysicalEditIntent` union already covers `insert-slot`, `insert-empty-segment`, `delete-key`, `delete-key-group`, `move-key`, `move-key-group`, `force-spacing`, `duplicate-key`, `paste-key`, and `paste-key-group`. `resolvePhysicPaintRotoPhysicalEdit(input)` is the sole canonical resolver. `PhysicPaintRotoPhysicalEditApplyPayload` and `isPhysicPaintRotoPhysicalEditApplyPayload` are the bridge wire contract and strict runtime guard.
</interfaces>
</context>

## Artifacts this phase produces

- A single shared, transport-safe typed-intent contract for all current ordinary physical mapping operations.
- A standalone strict parser and canonical stable serializer for intent values, independent of the active apply-payload guard.
- Resolver tests proving parsed serialized intent reproduces the same complete canonical proposal.
- An independently typecheckable Plan 08 boundary with no required apply-payload field until Plan 09.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one Insert Slot intent through strict parsing into canonical resolution</name>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts: `PhysicPaintRotoPhysicalEditIntent`, resolver input, and accepted/rejected result shape
    - app/src/types/physicPaint.ts: operation-kind union, apply payload, semantic delta, and strict payload allowlist
    - app/src/types/physicPaint.test.ts: existing strict semantic-delta parser tests
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts: current Insert Slot behavior examples
  </read_first>
  <reversibility rating="costly">The shared internal wire contract spans child actions, coordinator transport, and parent validation; undoing its shape requires coordinated call-site changes but no project migration.</reversibility>
  <behavior>
    - RED: a standalone serialized `insert-slot` intent with an unknown field, malformed identity/frame, or another discriminant fails the new intent parser.
    - GREEN: one valid serialized Insert Slot intent passes strict parsing, canonical stable serialization, and produces the same accepted proposal as the direct resolver request for the same authoritative input.
    - The active apply-payload interface and `isPhysicPaintRotoPhysicalEditApplyPayload` behavior remain unchanged in this plan.
    - The resolver remains the only implementation of Insert Slot mapping semantics.
  </behavior>
  <action>Promote or relocate the existing ordinary resolver intent union to a dependency-safe shared export that both transport code and the resolver can import without a cycle. Add a standalone `isPhysicPaintRotoPhysicalEditIntent` parser and canonical stable serializer for the Insert Slot tracer. Validate the closed field allowlist, discriminant, stable identity, finite integer frame, and nested value shape. Do not add any intent member—required or optional—to `PhysicPaintRotoPhysicalEditApplyPayload`, do not alter `isPhysicPaintRotoPhysicalEditApplyPayload`, and do not update producers in this plan; Plan 09 owns that atomic activation boundary. The ordinary intent is the authorization request, not a replacement resolver or a child result claim. Preserve D-01 and reject malformed standalone values per D-18.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - One Insert Slot intent has one strict serialized shape, one stable canonical serialization, and one canonical resolver interpretation.
    - Unknown members, malformed identity/frame data, and discriminant mismatch reject through the standalone parser.
    - The active apply payload and all current producers remain buildable and unchanged until Plan 09.
    - No new resolver, package, schema, optional compatibility field, or compatibility branch is introduced.
  </acceptance_criteria>
  <done>The parent-facing contract can represent and independently resolve one production ordinary mapping operation end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the closed intent matrix to every existing mapping operation</name>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts: Task 1 shared intent and strict parser
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts: every branch of `resolvePhysicPaintRotoPhysicalEdit`
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts: delete, move, group, spacing, duplicate, and paste truth tables
  </read_first>
  <behavior>
    - Valid typed intents parse for delete one/group, move one/group, Force Spacing, Duplicate, Paste one/group, and `insert-empty-segment`.
    - Duplicate identities, unstable/reordered authorization lists where canonical order matters, invalid frame targets, invalid spacing, and ambiguous group declarations reject.
    - Empty and single-key inputs resolve deterministically according to existing behavior.
    - Parsing then resolving the same intent repeatedly is stable and does not mutate input arrays.
  </behavior>
  <action>Complete the standalone strict discriminated-union parser and canonical stable serializer for every existing ordinary mapping operation. Reuse the exact resolver vocabulary and constraints rather than creating parallel transport-only semantics. Preserve ordered identity arrays where order is authoritative, validate uniqueness and finite integer/frame constraints, and retain D-14 stable-key ownership inputs needed for move/delete/spacing lifecycle behavior. Prove parse → serialize → parse stability and parse → canonical resolver reproduction with independent expected literals. Keep Play Script, interpolation, Undo, and Redo outside this ordinary intent union on their existing specialized paths. Leave the active apply-payload interface and guard untouched; do not add deferred group UI, an additional Insert action, or packed-boundary behavior.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts &amp;&amp; pnpm --dir app run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every current mapping operation has a matching strictly parsed intent.
    - Every mismatch or malformed authorization rejects before parent handling.
    - Existing canonical resolver outputs, identity lifecycle, Loop Clip placement behavior, and incoming-break behavior are unchanged.
  </acceptance_criteria>
  <done>The complete typed-intent contract required by GAP-03-B exists and is deterministically consumable by the sole canonical resolver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serialized ordinary intent → standalone shared parser | Untrusted transport-shaped intent data is validated independently before payload activation in Plan 09. |
| Parsed intent → canonical resolver | Validated intent becomes a request to compute an authoritative document transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.1-08-01 | Tampering | Standalone ordinary physical-edit intent | high | mitigate | Closed discriminated-union parsing, canonical stable serialization, identity uniqueness, and numeric-domain checks before Plan 09 payload activation. |
| T-43.1-08-02 | Elevation of Privilege | Group and spacing authorization | high | mitigate | Carry exact ordered identities and reject missing, duplicate, reordered, or ambiguous authorization before resolution. |
| T-43.1-08-03 | Denial of Service | Oversized or malformed intent collections | medium | mitigate | Reuse capacity-bounded resolver validation and reject invalid arrays before parent mutation. |
| T-43.1-08-04 | Repudiation | Operation-kind declaration | low | accept | Exact operation/intent discrimination is sufficient for local command attribution; no external audit system is in scope. |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/types/physicPaint.test.ts src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts`
- `pnpm --dir app exec tsc --noEmit`
- `git diff --check`
</verification>

<success_criteria>
- All ordinary physical mapping operations have one standalone strict intent contract, parser, and canonical stable serializer.
- The contract can be resolved by the existing canonical resolver without duplicating mapping semantics.
- Plan 08 typechecks without changing the active apply payload or any producer; Plan 09 receives one explicit atomic activation boundary.
- Invalid standalone intent fails closed, and deferred UI/group behavior plus approved Phase 43.1 visual surfaces remain untouched.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Empty key and stable-ID break remain one accepted document fact | 43.1-08 | COVERED | Intent contract preserves complete key/break resolver inputs. |
| REQ | GAP-03 | Bridge validation and timing edits preserve ownership atomically | 43.1-08 | COVERED | Supplies the missing strict intent contract. |
| RESEARCH | — | Parent reruns the existing canonical resolver | 43.1-08 | COVERED | Shared intent, no duplicate resolver. |
| RESEARCH | — | Strict allowlisted runtime parsing | 43.1-08 | COVERED | Closed parser and adversarial matrix. |
| CONTEXT | D-01 | Key plus break are one atomic physical command | 43.1-08 | COVERED | Intent describes one complete transition. |
| CONTEXT | D-14 | Identity-preserving timing edits retain ownership | 43.1-08 | COVERED | Stable identities and operation-specific targets remain explicit. |
| CONTEXT | D-18 | Rejection leaves state unchanged | 43.1-08 | COVERED | Malformed data is blocked at parsing. |

<output>
Create `.planning/phases/43.1-intentional-gap-insert-use-specs-milestone-v0-9-0-new-phases/43.1-08-SUMMARY.md` when done.
</output>
