---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 06
type: execute
wave: 4
depends_on:
  - 43.4-01
  - 43.4-02
  - 43.4-04
  - 43.4-05
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements:
  - KRAIL-01
  - KRAIL-02
  - KDRAG-01

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every continuous ordinary-key segment renders one mid-gray 3px rail (#8A939C) in the existing integrated rail band with zero added timeline height; a one-key rail is exactly one 18px cell wide; zero ordinary keys removes all Key Rail DOM (D-07, D-09, D-11; UI-SPEC K1 empty/populated/zero-one-many)"
    - "Key Rail boundaries show the 2px #F8FAFC white endpoint cuts plus matching white boundary-cell borders; two rails adjacent across a break show a double divider; adjacent Motion/Static/Key Rails never overlap or visually merge (D-11; UI-SPEC K1 partial)"
    - "Selection uses the shared orange #F59E0B treatment on the 3px line only; hover brightens to #A7B0B9 without approaching selection orange; focus shows the 2px #F2F5F7 ring with 1px offset; member key cells keep ordinary appearance (D-08; UI-SPEC state precedence)"
    - "The 12px transparent target stays inside the rail band (top: 0), never covers or steals pointer interaction from the physical cell/key body, and rails never cover key markers, gap cells, linked indicators, synchronization dots, ruler labels, drag previews, or toolbar controls (D-11)"
    - "Plain click selects exactly one derived Key Rail (tooltip/accessible name switch to the Selected D-10 form) without moving the physical cursor or playhead; Shift/Cmd-click resolve as plain selection; Key Rails never join Motion/Static range or toggle selections (KRAIL-02)"
    - "Key Rail selection, Motion/Static Rail selection, and physical real-key selection are mutually exclusive session modes cleared synchronously in both directions; stale authority (membership, breaks, or revision change) clears the invalid Key Rail selection instead of inventing a fallback (KRAIL-02, RESEARCH Pitfall 5)"
    - "During a drag the gray ghost renders at 55% opacity at the clamped destination with the red 2x12px blocked-edge bar on the clamped side and roto-fill-empty gap preview cells; Escape or rejection removes all preview paint with zero state change (D-16; UI-SPEC K4 rows)"
    - "Long segments clip at the shared horizontal viewport and reappear on scroll; the strip remains exactly 161px with the 38px row, 3px rail, 12px target, 18px pitch (D-11; UI-SPEC K1 overflow, responsive contract)"
    - "The Key Rail host mounts under its OWN condition (any ordinary real key exists) — never gated on Loop Clip infrastructure (RESEARCH Pitfall 3)"
    - "During controller commit the accepted rail stays visible at 55% opacity with aria-busy='true'; no optimistic split/move/delete ever presents (UI-SPEC K1 loading, busy contract)"
    - "The Key Rail visual/interaction surface implements the approved 43.4-UI-SPEC.md produced per D-22; all 21 resolved UI Considerations are lifted into plan must_haves across plans 01-08 with the Visual Acceptance Matrix rows driving the plan-08 checkpoint (D-22)"
  artifacts:
    - "app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx exists as the rail host with target/segment/ghost/blocked-edge structure, keyboard contract, and tooltip"
    - "physicsPaintStudio.css contains additive mode-keyrail paint rules (base #8A939C, hover #A7B0B9, selected #F59E0B shared, cuts, ghost, blocked edge) with zero modified or removed existing rules"
    - "PhysicsPaintWorkflowStrip.tsx mounts the Key Rail host under its own gate and paints the key-rail gap preview cells"
    - "PhysicsPaintStudio.tsx declares the Key Rail selection signal with bidirectional mutual-exclusion clearing and stale-authority revalidation"
  key_links:
    - "deriveKeyRailSegments (plan 01) -> rail host geometry: left = firstKeyFrame x 18px, width = max(1px, (lastKeyFrame + 1 - firstKeyFrame) x 18px), viewport-clipped like existing rails"
    - "rail target pointer-down -> plan-02 session hook -> plan-05 prepare/commit ports: the gesture reaches the canonical path with the clamp port wired to clampPhysicPaintKeyRailDragDestination (plan 03)"
    - "Studio Key Rail selection signal -> plan-04 classifier input: Delete scope sees the rail selection at activation time"
---

<objective>
Give Key Rails their visible surface: the rail host component forked from PhysicsPaintLoopClipRail, additive gray CSS, the strip mount with its own gate and gap preview, and the Studio session selection signal with mutual-exclusion and stale-authority clearing.

Purpose: This plan composes every prior wave — derivation (01), session hook (02), classifier input (04), prepare/commit ports (05), clamp port (03) — into the user-visible rail. Selection wiring is the phase's highest-leak-risk seam (Pitfall 5), so clearing discipline is pinned by tests.
Output: A fully interactive Key Rail band — render, select, drag with ghost/clamp/gap preview, keyboard support — integrated without touching the locked Motion/Static rail behavior.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration; no assumption-delta signal; no ORM schema files. Reversibility: new component + additive CSS + minimal Studio wiring — reversible; no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-01-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-02-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-04-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PhysicsPaintKeyRail host component plus additive mode-keyrail CSS</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx, app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (entire file — structural template: anchor/target/segment structure L191-237, tooltip idiom, consumeClickSuppression L127-145, band group L296; DO NOT copy the double-click/Edit timers L152-175, the lifecycle dot L211-213, truncated/unresolved arms L201, or Shift/Cmd mapping L147-151)
    - app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts (plan-01 exports: segments + D-10 copy builders)
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts (plan-02 session API)
    - app/src/components/physic-paint/physicsPaintStudio.css (lines 2738-2900 rail/ghost/boundary rules the new classes sit beside)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K1 geometry, Key Rail paint table, layer order, state precedence, keyboard and focus, accessibility)
  </read_first>
  <behavior>
    - Test 1 (render): one visible 3px segment per derived segment at left = firstKeyFrame x 18px (+ the shared 4px scroll-leading offset), width = max(1px, span x 18px), viewport-clipped; a one-key segment is 18px wide; zero segments renders nothing — no placeholder, label, or hidden focus target
    - Test 2 (selection): plain click calls the selection port with exactly that segment's identity; aria-pressed reflects selection; the tooltip/accessible name equals the Selected D-10 form when selected and the Base form otherwise; member key cells are untouched
    - Test 3 (keyboard): each target exposes role='button' tabIndex=0 in first-key frame order; Space plain-selects (preventDefault against page scroll); Enter performs no action; Escape hides the tooltip without changing selection or focus
    - Test 4 (boundaries): actual segment start/end render the 2px white endpoint cut plus boundary-cell border classes; two break-adjacent segments render a double divider; viewport-clipped edges render no false cut
    - Test 5 (ghost): during a session the ghost element renders at the clamped left/width with aria-hidden='true' and pointer-events none, the blocked-edge bar on the clamped side, while the accepted segment keeps full opacity and position
    - Test 6 (busy): with the controller busy the accepted rail carries aria-busy='true' at 55% opacity
  </behavior>
  <action>Fork PhysicsPaintLoopClipRail.tsx structure into the new host per PATTERNS.md differences: no double-click/Edit timers, no lifecycle/synchronization dot, no truncated/unresolved arms, selection mapping reduced to plain-only (Shift/Cmd resolve as plain), and the band group accessible name written directly as 'Rails' (role='group'; rendered only when any rail exists). Geometry: 12px transparent target at top: 0 spanning the segment width with cursor pointer (grabbing during a session), 3px visible segment at y: 0, layer order unchanged (targets 6, paint 7, ghost between 7 and 8, focus ring 8, tooltip 60). Tooltip: PhysicsPaintStyledTooltip region bottom fed by the plan-01 D-10 copy builders with guarded-reason substitution for unavailable drag/delete and never advertising Scissor (D-10). Drag wiring: pointer-down delegates to the plan-02 session hook with the projection port (18px pitch), the clamp port bound to clampPhysicPaintKeyRailDragDestination, and the prepare/commit ports from plan 05; below-threshold release keeps plain click selection; fork the consumeClickSuppression call. CSS: strictly additive new mode-keyrail classes — base #8A939C, hover #A7B0B9 (never approaching selection orange), selected #F59E0B (shared treatment), endpoint cuts 2px #F8FAFC plus white boundary-cell borders, ghost #8A939C at 55% opacity with no cuts and no selection paint, blocked edge 2x12px #FF6B6B, busy state 55% opacity, focus ring 2px #F2F5F7 with 1px offset, no transitions anywhere (reduced-motion contract). Zero modifications or removals of existing rules; zero geometry changes (161px strip, 38px row, 3px rail, 12px target, 18px pitch).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintKeyRail.tsx exists with the target/segment/ghost/blocked-edge structure and the keyboard contract; PhysicsPaintStudio.tsx is untouched by this task
    - All six behavior tests pass
    - physicsPaintStudio.css diff is additions only — node -e or git diff -U0 filtered to the css shows zero removed/modified lines
    - git diff shows zero changes to PhysicsPaintLoopClipRail.tsx
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Key Rail segments render, select, focus, and ghost correctly as a sibling host with additive gray paint and the locked D-10/D-08/D-11 presentation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip mount under its own gate plus key-rail gap preview cells</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 1552-1572 the Loop Clip rail mount gate to NOT reuse and the port wiring idiom at 1566-1570; 895-935 the group-drag gap preview useMemo idiom; the plan-01 Scissor block)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pitfall 3 mount gating; Don't Hand-Roll gap preview row)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (K4 gap preview: roto-fill-empty cells byte-identical to 43.2/43.3)
  </read_first>
  <behavior>
    - Test 1 (own gate): with ordinary real keys present and ZERO Motion/Static Groups (loopResolutionContext null), Key Rail segments render; with zero ordinary keys no Key Rail DOM exists (Pitfall 3)
    - Test 2 (coexistence): Key Rails and Motion/Static Rails tile the same band without overlap — Group-owned spans render Motion/Static Rails, ordinary segments render Key Rails, intentional gaps render no rail
    - Test 3 (gap preview): during a key-rail drag session the vacated interval plus any newly opened destination interval paint as roto-fill-empty cells (border #3D454D / background #4D535A — the existing rule, byte-identical treatment); adjacent landing previews zero gap cells (UI-SPEC K4 gap zero-one-many)
    - Test 4 (cleanup): commit, cancel, or rejection removes all gap preview paint with no residue
    - Test 5 (regression): existing strip suites (including viewport tests) pass unmodified
  </behavior>
  <action>Mount PhysicsPaintKeyRail next to the Loop Clip rail mount (~L1552-1572) under its OWN condition — any ordinary real key exists in the accepted document; never gate on loopResolutionContext, onSelectRotoLoopClip, or onOpenRotoLoopEdit (Pitfall 3). Wire the drag ports mirroring L1566-1570: prepare/commit from plan 05, the clamp input getter, the rejection status port (onKeyRailDragRejected publishing through the existing status channel), and the preview-change port. Add a sibling gap-preview collector (useMemo Set of app frames over the key-rail preview publication: vacated interval plus newly opened destination interval, modeled on collectRotoGroupDragGapPreviewAppFrames) and paint those cells with the existing roto-fill-empty class — no new gap visual (UI-SPEC K4). All preview paint is aria-hidden='true', pointer-events none, and removed on commit/cancel/rejection. Keep the plan-01 Scissor block and the locked toolbar order byte-identical.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Key Rail host mounts with zero Groups present and is absent with zero ordinary keys
    - Gap preview cells use the existing roto-fill-empty class only; no new gap CSS class is introduced
    - All five behavior tests pass; the strip suites exit 0
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>Key Rails render on any timeline with ordinary keys, tile the band alongside Motion/Static Rails without overlap, and preview vacated/opened intervals as ordinary empty cells during drags (KRAIL-01 render complete, KDRAG-01 paint half).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Studio Key Rail selection signal with mutual exclusion and stale-authority clearing</name>
  <files>app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (lines 111-120 session signal declarations, 141-144 launch-replacement clearing, 311-313 Select-All clearing — every existing rail-clearing path; the physical-key selection paths that must now also clear the Key Rail signal)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (the plan-04 classifier input this signal feeds)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pitfall 5 mutual-exclusion leaks; Open Question 1 pinned: identity = firstKeyId + full member equality)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md (Selection ownership contract)
  </read_first>
  <behavior>
    - Test 1 (mutual exclusion): selecting a Key Rail clears Motion/Static Rail selection AND physical selection synchronously; selecting a physical key or a Motion/Static Rail clears the Key Rail selection; orange paint can never land on two rails
    - Test 2 (clearing paths): launch replacement and Select-All clear the Key Rail signal alongside the existing rail signals (both directions covered)
    - Test 3 (stale authority): when the accepted document changes such that the selected firstKeyId no longer heads a derived segment with an identical member list (membership, breaks, or revision drift), the Key Rail selection clears — no fallback scope is invented
    - Test 4 (session-only): the signal is never persisted and never sent across the bridge (keyId-only session state, same discipline as selectedLoopClipIds)
    - Test 5 (regression): existing Studio tests pass unmodified
  </behavior>
  <action>Add the Key Rail selection signal as a sibling of selectedLoopClipIds (anchored on the segment's firstKeyId; store the member list snapshot needed for revalidation), declared with useSignal and documented session-only/never-persisted/never-bridged exactly like the L116-117 comment discipline. Route ALL clearing paths in both directions: Key Rail selection clears selectedLoopClipId/selectedLoopClipIds/loopSelectionAnchorId and physical selection; physical-key selection and Group-rail selection clear the Key Rail signal; extend the L141-144 launch-replacement block and the L311-313 Select-All block. Add stale-authority revalidation: on authoritative document changes, re-derive segments (plan-01 pure function) and clear the selection unless a segment exists whose firstKeyId matches AND whose member keyIds are exactly equal (Open Question 1 pinned). Feed the signal into the plan-04 classifier input through the existing input-interface idiom. Keep the wiring minimal — no new logic beyond selection bookkeeping in this file (project constraint); any helper richer than a few lines lives in a focused module. Do NOT touch the L838 deletion-status fallback line — plan 07 owns that migration site.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Key Rail selection signal exists with bidirectional clearing on every existing rail-clearing path
    - All five behavior tests pass; the Studio suite exits 0
    - pnpm --dir app typecheck exits 0
    - git diff on PhysicsPaintStudio.tsx is limited to the signal declaration, clearing-path extensions, revalidation wiring, and classifier input plumbing
  </acceptance_criteria>
  <done>Key Rail selection is a first-class mutually exclusive session mode with stale-authority clearing, feeding the Delete classifier and rail paint (KRAIL-02 complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer/keyboard -> session selection | Untrusted gestures drive mutually exclusive selection modes |
| preview paint -> accepted document | Presentation-only ghost/gap state must never read as accepted truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-06-01 | Tampering | Selection-mode state | high | mitigate | Synchronous bidirectional clearing on every path plus stale-authority revalidation against the accepted derivation (firstKeyId + member equality); ambiguous state fails closed to no rail selection (Task 3, Pitfall 5) |
| T-43.4-06-02 | Repudiation (UX-trust) | Ghost/gap preview paint | medium | mitigate | All preview paint is aria-hidden, pointer-events none, and removed on commit/cancel/rejection; the accepted rail never moves before parent acknowledgement (Tasks 1-2, D-16) |
| T-43.4-06-03 | Tampering (integrity) | Locked Motion/Static rail regression boundary | high | mitigate | Sibling host + additive-only CSS; acceptance criteria forbid diffs to PhysicsPaintLoopClipRail.tsx and non-additive css changes (Task 1) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintKeyRail.test.tsx — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/PhysicsPaintStudio.test.ts — green
- pnpm --dir app exec vitest run — full suite green (wave merge gate)
- pnpm --dir app typecheck — exits 0
- git diff confirms zero modifications to PhysicsPaintLoopClipRail.tsx and additive-only physicsPaintStudio.css changes
</verification>

<success_criteria>
Key Rails are visible, selectable, and draggable on any timeline with ordinary keys: gray 3px segments with white endpoint cuts in the shared band, shared orange selection with mutual exclusion, gray ghost + red clamp bar + roto-fill-empty gap preview during drags, and the full keyboard/accessibility contract. Terminology migration of the pre-existing Motion/Static rail copy is plan 07; history/persistence proofs are plan 08.
</success_criteria>

## Artifacts this phase produces (this plan)

- New component `app/src/components/physic-paint/view/PhysicsPaintKeyRail.tsx` (rail host: target/segment/ghost/blocked-edge/tooltip/keyboard contract)
- Additive `mode-keyrail` CSS classes in physicsPaintStudio.css
- Key Rail mount gate, drag port wiring, and key-rail gap-preview collector in PhysicsPaintWorkflowStrip.tsx
- Key Rail selection signal, bidirectional clearing, stale-authority revalidation in PhysicsPaintStudio.tsx
- New test file `PhysicsPaintKeyRail.test.tsx` plus strip/Studio test extensions

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-06-SUMMARY.md` when done
</output>
