---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 08
type: execute
wave: 6
depends_on:
  - 43.4-05
  - 43.4-06
  - 43.4-07
files_modified:
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UAT.md
autonomous: false
requirements:
  - HIST-01
  - REG-01

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Scissor, Key Rail drag, and Delete Key Rail each commit as exactly one atomic physical-timeline history command through the existing coordinator/history authority — one Undo fully reverts, one Redo fully reapplies, and a new accepted edit after Undo truncates the Redo branch (D-24)"
    - "Undo of a Delete Key Rail restores every key, every break, and the selection/cursor state; Undo of a drag restores keys and breaks to their exact pre-drag layout (D-13, D-24)"
    - "Save/reopen reconstructs identical keys, gaps, breaks, and derived Key Rails without history replay — derivation recomputes from canonical facts (D-24, HIST-01)"
    - "No phase-local history stack exists and no brush/timeline history merge occurs (D-24)"
    - "Interpolation Off/On preserves derived rails, splits, and deletion gaps exactly (D-15, UI-SPEC Derivation Contract)"
    - "Native visual UAT from a fully restarted frozen-code session passes the product prompt's 15-step acceptance list and the UI-SPEC Visual Acceptance Matrix (D-23)"
  artifacts:
    - "useRotoPhysicalEditHistory.test.ts and/or useRotoPhysicalEditCoordinator.test.ts contain the one-command Undo/Redo, Redo-truncation, and save/reopen parity proofs for all three operations"
    - ".planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UAT.md records the frozen-session native acceptance evidence"
  key_links:
    - "coordinator executePhysicalEdit -> history immutable before/after replay: each operation is one command (D-24)"
    - "persisted breaks + keys -> save/reopen -> deriveKeyRailSegments: derived rails reconstruct identically with zero new persisted state (D-25)"
---

<objective>
Prove the phase's atomicity and persistence contract with integration tests — one-command Undo/Redo per operation, Redo truncation, and save/reopen derivation parity — then run the blocking frozen-session native UAT against the 15-step product acceptance list and the UI-SPEC Visual Acceptance Matrix.

Purpose: HIST-01 is mostly free (breaks already participate in parsing/revision/persistence; the coordinator already replays immutable before/after documents) — this plan proves it rather than assumes it, then hands the phase to the final oracle (D-23).
Output: Green integration proofs and a user-approved native UAT record.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration; no assumption-delta signal; no ORM schema files. Reversibility: test additions plus a UAT record — reversible; no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UI-SPEC.md
@SPECS/milestone-v0.9.0-new-phases/phase-43.4-derived-key-groups-scissor-prompt.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-01-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-03-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-05-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-06-SUMMARY.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: History and persistence integration proofs for all three operations</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts (existing one-command replay harness shape)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts (existing accepted-only publication harness)
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts (immutable before/after replay authority)
    - app/src/lib/physicPaintPersistence.ts (strict parse/hydrate/save seam the save/reopen test flows through)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md (D-24 verbatim)
  </read_first>
  <behavior>
    - Test 1 (one command per op): an accepted scissor, an accepted key-rail drag, and an accepted delete-key-rail each push exactly ONE history entry; history depth grows by one per operation
    - Test 2 (Undo/Redo fidelity): Undo of each operation restores the exact prior keys, breaks, and selection/cursor facts (delete restores every member key and normalized break; drag restores pre-drag positions and break ownership); Redo reapplies each as one command
    - Test 3 (Redo truncation): a new accepted edit after Undo truncates the Redo branch (D-24)
    - Test 4 (save/reopen parity): serializing the accepted document through the persistence seam and re-parsing yields identical keys, breaks, and gaps, and deriveKeyRailSegments over the re-parsed document equals the pre-save derivation — with no history replay
    - Test 5 (interpolation toggle): Interpolation Off/On preserves derived rails, splits, and deletion gaps exactly (derivation is interpolation-independent)
    - Test 6 (regression): all pre-existing history/coordinator tests pass unmodified
  </behavior>
  <action>Extend the existing history and coordinator test files (do not create one-off configs — project constraint). Drive each operation through the same coordinator/executePhysicalEdit path the actions layer uses (scissor and delete via the runner, drag via the plan-05 prepare/commit pair) against fixture documents exercising: a middle Key Rail deletion with surviving neighbors, a first-content deletion (dormant successor break), a drag opening a gap, and a drag landing break-adjacent. Assert history depth deltas, before/after document equality on Undo/Redo including the break collection and selection/cursor facts, Redo truncation, and save/reopen derivation parity via deriveKeyRailSegments over the re-parsed document. Reuse the existing test fixtures and builders; add no new persistence fields anywhere (D-25).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six behavior tests pass in the existing harnesses
    - pnpm --dir app exec vitest run (full suite) exits 0
    - pnpm --dir app typecheck exits 0
    - pnpm --dir app build exits 0 (phase gate before UAT)
  </acceptance_criteria>
  <done>One-command Undo/Redo, Redo truncation, save/reopen derivation parity, and interpolation-toggle stability are proven for all three operations through the existing history authority (HIST-01 complete).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Frozen-session native UAT — 15-step product acceptance plus UI-SPEC Visual Acceptance Matrix</name>
  <what-built>Derived Key Rails (gray 3px segments with white endpoint cuts over ordinary-key segments), the Scissor toolbar action with guarded no-op semantics, selection-scoped Delete Key Rail with persistent-gap normalization, Key Rail clamped drag with ghost/blocked-edge/gap preview, the D-01 Rail-family terminology migration, and one-command history/persistence parity — all automated gates green (full vitest suite, typecheck, build).</what-built>
  <how-to-verify>
    Run from a FULLY RESTARTED frozen-code parent/child session (43.2 D-39 / D-23): freeze the implementation commit, restart both windows, then execute the activated product prompt's 15-step native acceptance list (SPECS/milestone-v0.9.0-new-phases/phase-43.4-derived-key-groups-scissor-prompt.md §Native acceptance) and the UI-SPEC Visual Acceptance Matrix rows 1-11: (1) contiguous ordinary keys show one mid-gray rail, an isolated key shows a one-cell rail, keys between Groups form their own rail; (2) a 43.1 break divides one rail into two with a double white cut and the owner keeps its blue left-edge marker; (3) clicking any rail paints exactly its 3px line orange and clears other selections without cursor movement; (4) Scissor sits after Cut with the distinct icon, splits before the targeted key, and an existing-break activation no-ops with unchanged history depth; (5) generated/empty/Group/linked targets disable Scissor with the exact mapped reasons; (6) the Delete control announces exactly the live scope with no modal; (7) Delete Key Rail removes every member key, leaves a neutral-gray intentional gap, and publishes the locked D-14 copy; (8) drag previews show the 55% gray ghost, red clamp bar, and roto-fill-empty gap cells with Escape restoring the exact pre-drag view; (9) accepted drags publish 'Moved Key Rail to frame {N}.' plus the gap clause when opened and zero-space drags publish 'No empty space in that direction.'; (10) one Undo/Redo exactly restores/reapplies each operation, a new edit after Undo truncates Redo, and save/reopen reconstructs identical keys, gaps, breaks, and derived rails; (11) touched rail surfaces present only Motion Rail / Static Rail / Key Rail vocabulary with dialog/Scripts/Actions surfaces unchanged. Also verify Interpolation Off/On preserves splits and deletion gaps exactly. Record step-by-step evidence in .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" with the frozen commit hash and UAT evidence recorded, or describe the failing step(s) with screenshots</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history replay -> store | Undo/Redo re-applies immutable before/after documents to the canonical store |
| persistence round-trip | Serialized documents cross save/reopen; strict parsing is the integrity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-08-01 | Tampering (integrity) | History replay | high | mitigate | One-command immutable before/after replay per operation proven by depth-delta and document-equality tests including breaks and selection (Task 1, D-24) |
| T-43.4-08-02 | Tampering (integrity) | Save/reopen round-trip | high | mitigate | Strict parse/hydrate parity proven: identical keys/gaps/breaks and equal derived segments without history replay; zero new persisted fields (Task 1, D-25) |
| T-43.4-08-03 | Repudiation (UX-trust) | Native acceptance | medium | mitigate | Blocking frozen-session UAT against the 15-step list and the UI-SPEC matrix with recorded evidence (Task 2, D-23) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run — full suite green
- pnpm --dir app typecheck — exits 0
- pnpm --dir app build — exits 0
- 43.4-UAT.md records user-approved frozen-session evidence for all 15 product steps and UI-SPEC matrix rows 1-11
</verification>

<success_criteria>
All three Key Rail operations are one atomic history command each with exact Undo/Redo and Redo truncation, save/reopen reconstructs identical derived rails from canonical facts alone, and the frozen-session native UAT passes the full product acceptance list — Phase 43.4 is complete and REG-01's final oracle is satisfied.
</success_criteria>

## Artifacts this phase produces (this plan)

- New integration test blocks in useRotoPhysicalEditHistory.test.ts and useRotoPhysicalEditCoordinator.test.ts (no new production symbols)
- `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-UAT.md` (frozen-session native acceptance record)

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-08-SUMMARY.md` when done
</output>
