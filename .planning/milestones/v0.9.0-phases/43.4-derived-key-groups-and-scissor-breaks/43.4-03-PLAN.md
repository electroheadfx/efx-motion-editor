---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 03
type: execute
wave: 2
depends_on:
  - 43.4-01
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
autonomous: true
requirements:
  - KDRAG-01
  - KDRAG-02
  - KDEL-02
  - GUARD-01

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "New move-key-rail and delete-key-rail intents round-trip the strict parser and canonical serializer and join the closed unions and runtime kind guard (43.3 D-14 precedent, GUARD-01)"
    - "clampPhysicPaintKeyRailDragDestination clamps at frame 0, capacity/parent end, every previous/next real key outside the dragged segment, and every Group interval (including linked occurrences); the dragged segment's own keys are pass-through; break-adjacent landing is legal; rejection only on zero valid movement in the dragged direction (D-16, D-20, RESEARCH Pitfall 4)"
    - "Resolving move-key-rail rigidly translates every member key by the same delta; the dragged rail's first key keeps its incoming break, a landing gap adds a break only when a gap actually opens, and the next real key at/after the vacated interval's end owns or reuses the incoming break — never when the interval ends content, never duplicated (D-17)"
    - "Resolving delete-key-rail removes every member key AND adds the vacated-interval successor break per 43.3 rule (a): the next surviving real key at/after the vacated end owns/reuses the break — including when the deleted rail was the first content (dormant break, RESEARCH Open Question 3 pinned YES); no break references a deleted key identity, no dangling or duplicate break survives (D-15, RESEARCH Pitfall 1)"
    - "Every break output is a complete ascending collection replacement, never a delta (43.3 reuse-never-duplicate contract)"
    - "Stale, ambiguous, rejected, or no-op move/delete resolutions produce no proposal (GUARD-01)"
    - "UI-SPEC K4 partial consideration (resolver half): the clamp result the ghost paints is exactly the destination the resolver commits — preview is the commit"
  artifacts:
    - "app/src/types/physicPaint.ts contains both new union members with parser branches and serializer cases"
    - "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts exports clampPhysicPaintKeyRailDragDestination and dispatches move-key-rail and delete-key-rail branches before the terminal fail-closed guard"
    - "Post-move and post-delete break derivations exist as pure complete-collection replacements mirroring deriveMoveGroupIncomingInterpolationBreakKeyIds rules (a)/(b)/(c)"
  key_links:
    - "clamp sibling -> ghost paint (plan 02 port) -> commit destination: one clamp authority feeds both preview and resolution so preview is the commit (43.3 D-05)"
    - "delete-key-rail branch -> break normalization -> generated-cell suppression: the successor break keeps the vacated interval an intentional gap under Interpolation On (D-15)"
    - "types union members -> resolver dispatch branches -> mapped coordinator ports: typecheck enumerates every switch (43.3 D-14)"
---

<objective>
Extend the resolver with the two remaining Key Rail operation semantics: the key-rail clamp authority and move branch with post-move break re-derivation, and the delete-key-rail branch with the vacated-interval successor-break normalization that the existing delete-key-group branch lacks — plus the transport vocabulary for both intents.

Purpose: This is the phase's only net-new semantics (RESEARCH: "the only net-new semantics is a composition of two verified analogs"). TDD-first pins the (a)/(b)/(c) break rules and the clamp algebra before any UI wires them.
Output: Pure, fully tested resolver semantics for Key Rail move and delete; transport intents parsing/serializing; the clamp export that plan 02's session port and plan 05's prepare/commit will consume.

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; planner-adopted IDs per 43.3 precedent). No external API integration; no assumption-delta signal; no ORM schema files. Reversibility: additive closed-union members and pure functions — reversible; no decision checkpoint required.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md
@.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transport vocabulary — move-key-rail and delete-key-rail closed-union members</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts (lines 382-436 intent union, 438-444 hasUniqueBoundedPhysicalKeyIds, 508-580 strict parser, 581+ serializer, plus the plan-01 scissor member as the in-phase model)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md (closed-union discipline: three sites move together; typecheck exhaustiveness lever)
  </read_first>
  <behavior>
    - Test 1 (round-trip): { kind: 'move-key-rail', memberKeyIds, destinationFirstKeyAppFrame } and { kind: 'delete-key-rail', keyIds } parse, reject excess/missing keys, reject unbounded or duplicate key IDs, and serialize canonically
    - Test 2 (validators): both kinds pass the operation-kind validators; the mapped coordinator ports compile with no rotoCoordinatorPorts.ts edits
    - Test 3 (regression): all pre-existing transport tests plus the plan-01 scissor round-trip pass unmodified
  </behavior>
  <action>RED first with the round-trip tests, then extend types/physicPaint.ts additively. Union members (names locked for this phase): { readonly kind: 'move-key-rail'; readonly memberKeyIds: readonly string[]; readonly destinationFirstKeyAppFrame: number } and { readonly kind: 'delete-key-rail'; readonly keyIds: readonly string[] }. Parser branches: hasOnlyKeys allowlists (['kind','memberKeyIds','destinationFirstKeyAppFrame'] and ['kind','keyIds']), hasUniqueBoundedPhysicalKeyIds for the key lists, bounded non-negative integer validation for destinationFirstKeyAppFrame. Serializer cases follow the neighboring-kind idiom. Add both literals to PhysicPaintRotoPhysicalEditOperationKind and both kind validators. Do not touch any existing member, branch, or case. Run typecheck and let the mapped coordinator ports enumerate every switch that will need a resolver branch in Tasks 2-3.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - types/physicPaint.ts contains both literal kinds in the union, parser branches, serializer cases, and validator lists
    - The three behavior tests pass
    - pnpm --dir app typecheck exits 0 (or lists ONLY the missing resolver dispatch arms, which Tasks 2-3 close)
    - git diff shows zero changes to pre-existing union members, parser cases, and serializer cases
  </acceptance_criteria>
  <done>Both Key Rail intents are first-class transport vocabulary with strict parsing and canonical serialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure key-rail clamp authority plus move-key-rail resolver branch with (a)/(b)/(c) break re-derivation</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 2010-2106 clampPhysicPaintGroupDragDestination — the algebra model including the own-keys pass-through at 2036-2046 and the directional nearest-free search at 2092-2106; 2110-2186 deriveMoveGroupIncomingInterpolationBreakKeyIds docstring and rules; 1645+ move-key-group rigid translation; 3162-3207 fail/branch idiom)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-PATTERNS.md (clamp simplifications for Key Rails; break derivation mirror rules)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md (D-16, D-17, D-20 verbatim)
  </read_first>
  <behavior>
    - Test 1 (clamp boundaries): proposals against frame 0, capacity/parent end, a previous real key, a next real key, and a Motion/Static Group interval (including a linked occurrence) each clamp to the nearest free position in the drag direction; the dragged segment's own keys never block; landing immediately before the next segment's first key (break-adjacent) is legal
    - Test 2 (zero-space rejection): when no valid movement exists in the dragged direction the clamp returns not-ok and the resolver fails with the existing 'no-free-space-in-direction' code (D-20 reuses 43.3 D-06 verbatim)
    - Test 3 (rigid translation): an accepted move translates every member key by delta = destinationFirstKeyAppFrame - firstKeyFrame with no reordering, stretching, splitting, or retiming (D-16)
    - Test 4 (rule a): the next real key at/after the vacated interval's end owns (or reuses) the incoming break; when the interval ends content no break is added
    - Test 5 (rule b): a source-attached move landing more than one frame after its new predecessor gives the first moved key a new incoming break; adjacent landing keeps the split via the successor's existing break without adding one
    - Test 6 (rule c): every unrelated existing break is carried unchanged; the output is the complete ascending collection, never a delta; a break owned by a moved key travels with its stable identity (43.1 D-14)
    - Test 7 (fail-closed): unknown/duplicate/malformed member IDs, Group-owned members, or a destination translating any key outside capacity return failure resolutions with no proposal (GUARD-01)
    - Test 8 (regression): all pre-existing resolver tests including move-key-group and move-group blocks pass unmodified
  </behavior>
  <action>RED first with the new describe blocks, then implement. Export clampPhysicPaintKeyRailDragDestination as a pure sibling of clampPhysicPaintGroupDragDestination with the simplifications PATTERNS.md locks: no attached/duplicated branch (always source-attached), no Infinity/zero-width cases (span always >= 1 frame of real keys); preserve verbatim the own-keys pass-through, the intervalFree/keysLandFree/valid composition, and the directional nearest-free search that never crosses the current position. Boundaries: frame 0, capacity/parent end, every real key outside the dragged segment, and every Group interval including linked occurrences (via the same derived ranges input shape the group clamp consumes). Add the move-key-rail dispatch branch before the terminal fail-closed guard: validate member IDs (bounded, unique, existing, ordinary — reject Group-owned members fail-closed), verify members form exactly one derived segment per the plan-01 derivation semantics (contiguous ordinary keys with no internal break; a stale/ambiguous membership fails closed), run the clamp, reject zero-movement with 'no-free-space-in-direction', build the mapping as rigid translation by delta, and compute nextIncomingInterpolationBreakKeyIds via a new pure derivation mirroring deriveMoveGroupIncomingInterpolationBreakKeyIds rules (a)/(b)/(c) verbatim — complete ascending collection replacement. Finish through the unchanged finalizeProposal seam. Do NOT copy move-key-group's rejection-on-collision semantic (43.3 Pitfall 3); do NOT modify clampPhysicPaintGroupDragDestination, deriveMoveGroupIncomingInterpolationBreakKeyIds, or the move-key-group/move-group branches (regression boundary).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - clampPhysicPaintKeyRailDragDestination is exported and covered against every D-16 boundary plus the pass-through and break-adjacency cases
    - The move-key-rail branch passes all eight behavior tests
    - pnpm --dir app typecheck exits 0
    - git diff shows zero changes to clampPhysicPaintGroupDragDestination, deriveMoveGroupIncomingInterpolationBreakKeyIds, and the move-key-group/move-group branches and their test blocks
  </acceptance_criteria>
  <done>Key Rail moves clamp-and-commit with the full D-17 break travel/landing/vacated-ownership contract as pure tested resolver semantics (KDRAG-01 clamp, KDRAG-02 semantics complete).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: delete-key-rail resolver branch with vacated-interval successor-break normalization</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 3162-3207 delete-key-group branch — member-removal machinery to reuse and the L3200-3202 filter-only break handling this branch must exceed; 2110-2186 rule (a) successor ownership; 2525-2530 break suppression the normalization protects)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-RESEARCH.md (Pitfall 1 — the exact gap this branch closes; Open Question 3 pinned YES: a successor owns the break even when the deleted rail was first content)
    - .planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-CONTEXT.md (D-15 verbatim)
  </read_first>
  <behavior>
    - Test 1 (member removal): deleting a middle Key Rail removes every member key; surviving neighbors keep exact positions
    - Test 2 (successor break): the next surviving real key at/after the vacated interval's end owns (or reuses) an incoming break — so Interpolation On cannot bridge the gap and the two surviving rails cannot merge (D-15)
    - Test 3 (first-content deletion): deleting the first rail leaves the successor owning a dormant break (legal per 43.1: dormant while the owner has no predecessor); deleting the LAST rail adds no break (no successor exists)
    - Test 4 (normalization invariants): no output break references a deleted key identity; no duplicate owners; the collection is complete ascending; unrelated breaks carry unchanged
    - Test 5 (segment proof): deriving Key Rail segments from the accepted delete proposal yields two non-merged surviving segments separated by the gap
    - Test 6 (fail-closed): empty/unknown/duplicate/Group-owned member lists return failure resolutions with no proposal; the loop-source-key rejection guard applies exactly as in delete-key-group (D-07 carried)
    - Test 7 (regression): the delete-key-group branch and its tests pass byte-unmodified
  </behavior>
  <action>RED first, then implement the delete-key-rail dispatch branch before the terminal fail-closed guard. Reuse the delete-key-group validation and member-removal machinery (bounded/unique/existing checks, loop-source-key rejection via countLoopsReferencingSourceKey, buildDeleteGroupCandidate) but REPLACE the filter-only break handling per RESEARCH Pitfall 1: start from the collection minus deleted owners, then apply 43.3 rule (a) — compute the vacated interval's end from the pre-delete accepted facts, select the next surviving real key at/after that end from the post-delete frames, and add it as a break owner unless already owned; skip only when no successor exists. First-content deletion DOES add the dormant successor break (Open Question 3 pinned YES). Output: frozen complete ascending collection replacement via the same deterministic ordering idiom as deriveMoveGroupIncomingInterpolationBreakKeyIds. Membership validation: the keyIds must form exactly one derived segment (plan-01 semantics); a stale/ambiguous list fails closed. Finish through the unchanged finalizeProposal seam. Do NOT modify the delete-key-group branch (its manual multi-key delete contract never promised gap persistence — regression boundary).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The delete-key-rail branch passes all seven behavior tests including the successor-break, first-content, and last-rail cases
    - Deriving segments from the delete proposal proves surviving rails never merge (Test 5)
    - pnpm --dir app typecheck exits 0
    - git diff shows zero changes to the delete-key-group branch and its test blocks
  </acceptance_criteria>
  <done>Delete Key Rail removes the segment and normalizes boundary breaks so the vacated interval persists as an intentional gap under Interpolation On/Off with zero dangling, duplicate, or deleted-key break references (KDEL-02 resolver half complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child view -> resolver | Untrusted member-key lists and destination frames cross into the pure canonical edit authority |
| resolver output -> parent store | Proposals carry complete break-collection replacements the parent applies verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.4-03-01 | Tampering | move/delete intent parsing | high | mitigate | hasOnlyKeys allowlists + hasUniqueBoundedPhysicalKeyIds + bounded frame validation; exhaustive serializer compile check (Task 1) |
| T-43.4-03-02 | Tampering | Clamp and membership validation | high | mitigate | Own-keys pass-through with all-external boundaries; stale/ambiguous membership fails closed; zero-movement rejection carries the locked failure code (Task 2, GUARD-01) |
| T-43.4-03-03 | Tampering (integrity) | Break-collection normalization | high | mitigate | Complete ascending replacement (never a delta), successor-break addition, deleted-owner filtering, and duplicate prevention proven by dedicated tests (Task 3, D-15/Pitfall 1) |
| T-43.4-03-04 | Tampering (integrity) | Locked move-key-group / delete-key-group / move-group regression boundary | high | mitigate | Purely additive branches and exports; acceptance criteria forbid diffs inside those blocks (Tasks 2-3) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — green (all new describe blocks plus the full pre-existing suite)
- pnpm --dir app typecheck — exits 0
- git diff confirms zero modifications to clampPhysicPaintGroupDragDestination, deriveMoveGroupIncomingInterpolationBreakKeyIds, and the move-key-group/move-group/delete-key-group branches
</verification>

<success_criteria>
All three Key Rail operations now have canonical resolver semantics: scissor (plan 01), clamped rigid move with (a)/(b)/(c) break re-derivation, and segment delete with successor-break gap preservation. No UI wires move/delete yet — plans 04/05 own routing and commit, plan 06 owns paint.
</success_criteria>

## Artifacts this phase produces (this plan)

- Intent union members `{ kind: 'move-key-rail', memberKeyIds, destinationFirstKeyAppFrame }` and `{ kind: 'delete-key-rail', keyIds }` plus operation-kind literals and validator entries (app/src/types/physicPaint.ts)
- `clampPhysicPaintKeyRailDragDestination` export (physicsPaintRotoPhysicalResolver.ts)
- `move-key-rail` and `delete-key-rail` resolver dispatch branches plus their pure post-move/post-delete break-derivation helpers (physicsPaintRotoPhysicalResolver.ts)
- New resolver test describe blocks: key-rail clamp, move-key-rail, delete-key-rail, transport round-trips

<output>
Create `.planning/phases/43.4-derived-key-groups-and-scissor-breaks/43.4-03-SUMMARY.md` when done
</output>
