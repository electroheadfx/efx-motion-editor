---
phase: 43.4-derived-key-groups-and-scissor-breaks
plan: 02
subsystem: physics-paint-ui
tags: [preact, signals, pointer-events, drag-session, vitest]

requires:
  - phase: 43.3-motion-and-static-group-drag-within-free-space
    provides: Proven 4px rail-drag threshold, pointer-capture, Escape, and click-suppression interaction contract
provides:
  - Node-testable Key Rail drag session hook with guarded pointer lifecycle
  - Presentation-only gray ghost and immutable preview publication state
  - Injected projection, clamp, prepare, and commit ports with no resolver/store/model dependency
  - Post-drop click suppression and rejected-commit focus recovery
affects: [43.4-05, 43.4-06, key-rail-drag, physics-paint-workflow-strip]

actuals:
  tokens: 5578
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - Preact Signal-backed presentation state with refs for imperative pointer-session identity
    - Generic retained-publication prepare/commit boundary
    - Injectable window and source-element surfaces for node-only gesture tests

key-files:
  created:
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts
  modified: []

key-decisions:
  - "Keep the Key Rail session as a sibling hook rather than generalizing the approved Group Rail hook."
  - "Inject destination projection, collision clamp, preparation, and commit ports so the session owns no resolver, store, or model logic."
  - "Use Preact Signals only for presentation state and refs for active pointer-session identity; no effect-mirrored state."

patterns-established:
  - "Preview-is-retained-publication: the commit port receives the exact object identity exposed to preview paint."
  - "Gesture lifecycle fails closed: guarded arming, one active session, synchronous paint cleanup, and focus restoration on cancellation or commit failure."

requirements-completed: [KDRAG-01]

coverage:
  - id: D1
    description: Key Rail drag starts only after more than 4px horizontal travel, captures the pointer, uses a grabbing cursor, and releases capture on completion.
    requirement: KDRAG-01
    verification:
      - kind: unit
        ref: app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts#threshold-capture-and-guards
        status: pass
    human_judgment: false
  - id: D2
    description: Escape and pointer cancellation remove all session paint without commit, while the post-drop browser click is consumed exactly once.
    requirement: KDRAG-01
    verification:
      - kind: unit
        ref: app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts#escape-and-click-suppression
        status: pass
    human_judgment: false
  - id: D3
    description: Live pointer projection and caller-supplied clamping publish exact ghost geometry and retain the immutable publication by identity through commit.
    requirement: KDRAG-01
    verification:
      - kind: unit
        ref: app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts#ghost-preview-and-publication-identity
        status: pass
    human_judgment: false

# Metrics
duration: 6min
completed: 2026-08-15
status: complete
---

# Phase 43.4 Plan 02: Key Rail Drag Session Summary

**A node-testable Preact Key Rail drag state machine with 4px activation, pointer capture, Escape cancellation, click suppression, and injected presentation-only ghost/publication ports.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-15T15:58:11Z
- **Completed:** 2026-08-15T16:04:23Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added guarded Key Rail pointer sessions with a horizontal-only 4px threshold, pointer capture, grabbing cursor, single-session enforcement, and source-focus restoration.
- Added consume-once suppression for the trailing browser click and synchronous cancellation cleanup for Escape, pointer cancel, and lost capture.
- Exposed presentation-only ghost and preview state driven by injected destination projection and clamp ports, retaining the exact prepared publication object for the commit callback.
- Kept the approved Group Rail hook and Loop Clip rail component byte-unchanged and introduced no resolver, store, or model imports.
- Added nine node-only Vitest cases covering threshold, capture, cursor, Escape, suppression, guards, clamping, retained identity, rejection cleanup, and rejected commit recovery.

## Task Commits

Each task followed the mandatory TDD RED/GREEN sequence:

1. **Task 1 RED: Key Rail drag session behavior tests** - `97c892f1` (test)
2. **Task 1 GREEN: Key Rail drag session lifecycle** - `5bbecdf8` (feat)
3. **Task 2 RED: Ghost and preview publication tests** - `e213b0e5` (test)
4. **Task 2 GREEN: Preview publication and commit-failure recovery** - `50759831` (feat)

## Files Created/Modified

- `app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts` - Generic Key Rail pointer-session hook with Signal-backed ghost/preview state and injected projection/clamp/prepare/commit boundaries.
- `app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts` - Node-only hook harness and nine behavioral tests using injected window/source-element doubles.

## Decisions Made

- Kept the new implementation as a focused sibling of `usePhysicsPaintGroupRailDrag` to preserve the Phase 43.3 regression boundary and avoid a generalized rail god-hook.
- Named the destination-specific preparation port `prepareAtDestination` so every live clamped destination retains a matching immutable publication; `onDropCommit` receives that exact publication identity.
- Used `useSignal` for render-visible ghost/preview state and refs for imperative session and suppression identity. The sole `useEffect` is lifecycle cleanup for external window/source listeners.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Handled rejected commit promises**
- **Found during:** Task 2 (Ghost/preview publication state exposure)
- **Issue:** A rejected asynchronous commit port would otherwise create an unhandled rejection and fail to restore keyboard focus after session paint had cleared.
- **Fix:** Added a terminal rejection handler that preserves accepted document authority, leaves no preview paint, and restores focus to the source rail target.
- **Files modified:** `app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts`
- **Verification:** The rejected-commit regression test fails before the fix and passes afterward; targeted Vitest and typecheck are green.
- **Committed in:** `50759831`

---

**Total deviations:** 1 auto-fixed (1 missing critical functionality)
**Impact on plan:** Required error handling only; no scope expansion or architectural change.

## Issues Encountered

- The first typecheck exposed the test double's overly narrow `Record<string, unknown>` event parameter. It was corrected to `unknown`, preserving the intentionally loose injectable listener contract.

## Verification

- `pnpm --dir app exec vitest run src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts` - 9/9 tests passed.
- `pnpm --dir app typecheck` - passed.
- `git diff` confirmed zero changes to `usePhysicsPaintGroupRailDrag.ts` and `PhysicsPaintLoopClipRail.tsx`.
- Import-boundary scan confirmed zero references to `physicsPaintRotoPhysicalResolver`, `physicPaintStore`, or `physicsPaintRotoPhysicalModel`.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 05 can supply the canonical `prepareKeyRailDrag` / `commitKeyRailDrag` publication pair through the generic callback boundary.
- Plan 06 can mount the hook in the Key Rail host and bind the 18px pointer projection, resolver clamp, gray ghost, and strip gap-preview paint.
- No blocker remains for downstream Key Rail drag integration.

## Self-Check: PASSED

- Created hook and test files exist.
- All four TDD task commits exist in git history.
- Targeted tests and application typecheck pass.
- Protected Phase 43.3 Group Rail files remain unchanged.

---
*Phase: 43.4-derived-key-groups-and-scissor-breaks*
*Completed: 2026-08-15*
