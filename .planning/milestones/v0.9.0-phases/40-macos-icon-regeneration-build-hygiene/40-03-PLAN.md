---
phase: 40-macos-icon-regeneration-build-hygiene
plan: 03
type: execute
wave: 2
depends_on: ["40-02"]
files_modified:
  - .planning/phases/40-macos-icon-regeneration-build-hygiene/baseline-build-warnings.txt
  - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-TRIAGE.md
  - app/src/viteBuild.test.ts
  - app/src/stores/uiStore.ts
  - app/src/main.tsx
  - app/src/stores/paintStore.ts
autonomous: false
requirements: [BUILD-02, BUILD-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "baseline-build-warnings.txt exists in the phase directory containing the full raw `pnpm --dir app build` output re-captured by the executor (not the researcher's /tmp file) — it is the triage reference and before/after evidence (D-07)"
    - "Every mixed-import warning in the baseline has a recorded classification in 40-TRIAGE.md — fix / preserve-with-reason / report-as-DI — with per-case evidence (eager import proven, no cycle created, no init-timing change), and the user approved the list BEFORE any import was edited (D-08)"
    - "Only user-approved corrections were applied; every preserved warning carries its written reason; Tauri/browser runtime guards, genuine lazy chunks, and cycle-breaking dynamic imports remain untouched; no global warning suppression was added anywhere (D-09, BUILD-02)"
    - "Store-to-store dynamic imports (paintStore.ts → timelineStore.ts / layerStore.ts) remain dynamic unless the user explicitly approved a proven-safe conversion — they are presumed cycle-breakers (D-09, Pitfall 3)"
    - "viteBuild.test.ts declares a CORRECTED_MIXED_IMPORT_PATHS string array populated from the approved D-08 list (empty array if preserve-all was approved) and asserts no captured warning string includes any of those module paths — module-path absence only, never exact message matching (D-13)"
    - "Dependency-inversion cases (if any) are written up with module pairs and rationale in the phase SUMMARY and noted for the project backlog as separately scoped architecture work — they are never fixed in this phase (D-10)"
    - "The full test suite `pnpm --dir app exec vitest run` is green after all approved corrections, and a post-triage `pnpm --dir app build` warning set contains exactly the preserved warnings and none of the corrected ones"
  artifacts:
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/baseline-build-warnings.txt
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-TRIAGE.md
    - app/src/viteBuild.test.ts (CORRECTED_MIXED_IMPORT_PATHS + non-return assertion)
    - Triage-approved source edits only (subset of: uiStore.ts, main.tsx, paintStore.ts — or none if preserve-all approved)
  key_links:
    - "The D-08 approval checkpoint sits between classification and editing — zero source edits exist before it (git diff of app/ and packages/ is empty at checkpoint time)"
    - "Non-return assertions consume the `warnings` capture array built in plan 40-02 — module-path absence via String.includes, robust to Vite/Rollup wording changes"
    - "Post-triage build output is compared against baseline-build-warnings.txt as before/after evidence"
---

<objective>
Execute the BUILD-02 mixed-import triage under strict approval discipline: re-capture the production warning baseline (D-07), classify every warning with per-case evidence (D-08/D-09), hold for user approval, then apply ONLY the approved corrections and pin their non-return via module-path-absence assertions in the build seam (D-13). Report dependency-inversion cases as separately scoped work (D-10).

Purpose: The 12 baseline mixed-import warnings are mostly deliberate (Tauri runtime guards, cycle-breakers, genuine lazy chunks). This plan converts only what is provably ineffective — with the user as the gate — and makes the corrections permanent through the BUILD-03 seam.
Output: Baseline evidence file, classified+approved triage document, approved import corrections, non-return assertions, full suite green.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-CONTEXT.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md
@.planning/phases/40-macos-icon-regeneration-build-hygiene/40-02-SUMMARY.md
@app/src/viteBuild.test.ts
</context>

<flagged_assumptions>
- **BUILD-02 (unresolved, unclassified probe row):** The final set of corrected mixed imports is unknowable at plan time by design — it is decided at the D-08 approval gate from per-case cycle/timing evidence. All 12 baseline warnings start as presumed preserves; the researcher's "candidate fix" reads (#5 uiStore→appConfig, #9 main→unsavedGuard, #10 main→themeManager, #12 paintStore→paintPreferences) are hypotheses the executor must re-prove, not pre-approvals. A preserve-all outcome is a fully valid phase result and still ships the D-13 assertion (over an empty path list).
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Re-capture the warning baseline and classify every warning with per-case evidence</name>
  <files>.planning/phases/40-macos-icon-regeneration-build-hygiene/baseline-build-warnings.txt, .planning/phases/40-macos-icon-regeneration-build-hygiene/40-TRIAGE.md</files>
  <read_first>
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-RESEARCH.md (Code Examples → "Mixed-import baseline inventory" — the 12-warning table with dynamic/static import sites and initial reads)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md (section "Triage-approved mixed-import corrections" — guardrails; "Evidence capture" — D-07 re-capture rule)
    - app/src/stores/uiStore.ts (around line 180 — dynamic import of ../lib/appConfig)
    - app/src/main.tsx (around lines 25, 84 — dynamic imports of themeManager and unsavedGuard)
    - app/src/stores/paintStore.ts (around lines 106, 478-479, 535, 541, 545-546 — dynamic imports of paintPreferences, timelineStore, layerStore)
  </read_first>
  <action>Produce the D-07 evidence and the D-08 classified candidate list. (1) Run `pnpm --dir app build` and save the COMPLETE raw output to `.planning/phases/40-macos-icon-regeneration-build-hygiene/baseline-build-warnings.txt` (executor re-capture — do not reuse the researcher's /tmp file). (2) Enumerate every mixed static/dynamic import warning from that output (research baseline: 12). (3) For EACH warning, inspect the actual import sites and classify into exactly one of: FIX (only with full per-case evidence: the module is provably already eager in the importing module's static graph, converting creates no import cycle, and no initialization-timing change results), PRESERVE (with a concrete reason — e.g. Tauri/browser runtime guard, genuine lazy chunk, store-to-store cycle-breaker), or REPORT-AS-DI (fixing would require dependency inversion — record module pair + why). Apply D-09 strictly: incomplete evidence means PRESERVE. Research guidance to re-verify, not trust: warnings #1-4 and #8 (Tauri APIs via bridge files) are expected runtime-guard preserves; #6/#7 (paintStore → timelineStore/layerStore) are presumed cycle-breakers; #11 (projectStore → physicPaintBridge) is the prime DI suspect; #5/#9/#10/#12 are the conversion candidates requiring proof — for #10 (themeManager via main.tsx) init-timing evidence is critical since the dynamic import may deliberately defer theme init. (4) Write `40-TRIAGE.md` in the phase directory: one row per warning with module path, dynamic import site (file:line), static importers sampled, classification, and the evidence or preserve reason. Make ZERO source edits — `git status` on app/ and packages/ must be clean when this task completes.</action>
  <verify>
    <automated>test -s .planning/phases/40-macos-icon-regeneration-build-hygiene/baseline-build-warnings.txt &amp;&amp; test -s .planning/phases/40-macos-icon-regeneration-build-hygiene/40-TRIAGE.md &amp;&amp; test -z "$(git status --porcelain -- app/ packages/)"</automated>
  </verify>
  <acceptance_criteria>
    - `baseline-build-warnings.txt` exists, is non-empty, and contains the literal `pnpm`/`vite` build output including the mixed-import warning lines
    - The number of classification rows in `40-TRIAGE.md` equals the number of distinct mixed-import warning subjects in `baseline-build-warnings.txt` (no warning left unclassified)
    - Every FIX row cites all three evidence points (eager import proven, no cycle, no init-timing change); every PRESERVE row names a concrete reason; every REPORT-AS-DI row names the module pair and the inversion rationale
    - `git status --porcelain -- app/ packages/` is empty (zero source edits before the approval gate)
  </acceptance_criteria>
  <done>A complete, evidence-backed fix/preserve/report-as-DI list covering every baseline warning exists and is ready for user approval, with the working tree untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-08 approval gate — user approves the classified mixed-import list before any edit</name>
  <decision>Approve which baseline mixed-import warnings get corrected (converted from dynamic to static import), which stay preserved-with-reason, and which are reported as dependency-inversion scope.</decision>
  <context>BUILD-02 permits correcting only provably ineffective mixed imports, and D-08/D-09 lock the discipline: the executor has classified all baseline warnings in `40-TRIAGE.md` with per-case evidence and has made zero source edits. Tauri/browser runtime guards, genuine lazy chunks, and cycle-breaking dynamic imports are expected preserves; a preserve-all outcome is valid.</context>
  <options>
    <option id="approve-all">
      <name>Approve the triage list as presented</name>
      <pros>Fastest path; executor applies exactly the FIX rows</pros>
      <cons>Trusts the executor's per-case evidence as written</cons>
    </option>
    <option id="approve-subset">
      <name>Approve a subset — name which FIX rows are authorized</name>
      <pros>Extra caution on specific modules (e.g. theme init timing)</pros>
      <cons>Unselected FIX rows become preserves for this phase</cons>
    </option>
    <option id="preserve-all">
      <name>Preserve all 12 — no import edits this phase</name>
      <pros>Zero regression risk; phase still ships baseline evidence, triage document, and the D-13 assertion over an empty corrected-path list</pros>
      <cons>Provably ineffective dynamics (if any) remain until a future phase</cons>
    </option>
  </options>
  <resume-signal>Select: approve-all, approve-subset (list the authorized modules), or preserve-all. You may also request deeper evidence on any row before deciding.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply approved corrections, pin non-return by module-path absence, report DI scope</name>
  <files>app/src/stores/uiStore.ts, app/src/main.tsx, app/src/stores/paintStore.ts, app/src/viteBuild.test.ts</files>
  <read_first>
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-TRIAGE.md (the approved classifications — the sole authority for what may be edited)
    - .planning/phases/40-macos-icon-regeneration-build-hygiene/40-PATTERNS.md ("Triage-approved mixed-import corrections" — conversion style follows existing static importers of the same module)
    - app/src/viteBuild.test.ts (the `warnings` capture array from plan 40-02 — consumption point for D-13)
    - Each approved target file at its dynamic-import site (per the triage rows) plus one existing static importer of the same module for style
  </read_first>
  <action>Execute ONLY what the user approved at the D-08 gate. (1) For each approved FIX row: convert the dynamic `await import('...')` at the triage-identified site into a top-level static import in the same form used by that module's existing static importers (plain named/namespace import, correct relative path); remove the now-dead dynamic call site. Apply one correction at a time and run `pnpm --dir app exec vitest run src/viteBuild.test.ts` after each — a red run means the conversion broke the build graph; revert that single correction and reclassify it as preserved-with-reason in 40-TRIAGE.md. Never touch any warning the user did not approve; never touch store-to-store dynamics, Tauri runtime guards, or lazy chunks absent explicit approval; add no global warning suppression. (2) In `app/src/viteBuild.test.ts`, declare a `CORRECTED_MIXED_IMPORT_PATHS: string[]` constant containing the module path substring of each actually-applied correction exactly as it appears in the original warning text (e.g. the module path the warning named) — an empty array if preserve-all was approved or every correction was reverted — and add an assertion that `warnings.some(w => CORRECTED_MIXED_IMPORT_PATHS.some(p => w.includes(p)))` is false, with a descriptive message per D-13 (module-path absence only; never exact message matching). (3) For each REPORT-AS-DI row: write the module pair and inversion rationale into the phase SUMMARY's deferred-items section and note it for the project backlog (D-10) — do not attempt any DI refactor. (4) Run the FULL suite `pnpm --dir app exec vitest run` (includes the ~60-90s build seam) and a final `pnpm --dir app build`; compare its warning output against baseline-build-warnings.txt and record the before/after delta (corrected paths gone, preserved paths present) in the SUMMARY.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir app exec vitest run` exits 0 (full suite including the build seam)
    - `git diff --stat -- app/src packages/` touches only files named in approved FIX rows (plus viteBuild.test.ts) — nothing else
    - `grep -n 'CORRECTED_MIXED_IMPORT_PATHS' app/src/viteBuild.test.ts` shows the constant and the non-return assertion
    - The post-triage `pnpm --dir app build` output contains none of the applied-correction module paths in mixed-import warnings, and still contains every preserved warning's module path (spot-check against baseline-build-warnings.txt; delta recorded in SUMMARY)
    - No source file gains a warning-filter, log-override, or global suppression construct
  </acceptance_criteria>
  <done>Every approved correction is applied and proven non-returning by module-path-absence assertion on the captured build warnings; preserved warnings carry reasons; DI cases are documented as separately scoped work; the full suite is green.</done>
  <reversibility rating="reversible">Import-form conversions are ordinary source edits under git; each was individually verified green. Revert restores the dynamic form with no data or contract impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triage classification → source edits | Only the user-approved subset may cross into the working tree (D-08 gate) |
| build warnings → regression evidence | Warnings are process-integrity evidence; suppressing them would mask real regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-07 | Tampering | import-graph conversions | medium | mitigate | D-08 user approval precedes every edit; D-09 conservative default; per-correction immediate build-seam run with single-correction revert rule; store-to-store dynamics presumed cycle-breakers (Pitfall 3) |
| T-40-08 | Repudiation | warning suppression | medium | mitigate | No global suppression permitted (BUILD-02/D-09); D-13 asserts corrected module paths absent from captured warnings rather than silencing output; acceptance criterion greps for absence of filter/override constructs |
| T-40-09 | Elevation of scope | dependency inversion | low | mitigate | DI cases are documented (module pairs + rationale) and routed to SUMMARY + backlog, never implemented here (D-10) |
| T-40-SC | Tampering | package installs | high | mitigate | No installs in this plan; Package Legitimacy Audit has nothing to check |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run` — full suite green after corrections
- Post-triage `pnpm --dir app build` warning delta vs baseline-build-warnings.txt recorded in SUMMARY
- D-08 checkpoint approval recorded
</verification>

<success_criteria>
1. baseline-build-warnings.txt and 40-TRIAGE.md exist with every baseline warning classified and evidenced (D-07, D-08)
2. Only user-approved corrections were applied; guards/lazy chunks/cycle-breakers untouched; no suppression added (BUILD-02, D-09)
3. CORRECTED_MIXED_IMPORT_PATHS non-return assertion is green on the captured build warnings (D-13, completing BUILD-03)
4. DI cases documented in SUMMARY + noted for backlog (D-10)
</success_criteria>

## Artifacts this phase (plan 40-03) produces

- `.planning/phases/40-macos-icon-regeneration-build-hygiene/baseline-build-warnings.txt` — NEW evidence file (raw build output)
- `.planning/phases/40-macos-icon-regeneration-build-hygiene/40-TRIAGE.md` — NEW triage document (classified warning list + approval record)
- `app/src/viteBuild.test.ts` — NEW test-scope constant `CORRECTED_MIXED_IMPORT_PATHS: string[]` + non-return assertion consuming the `warnings` array
- Conditional (only if approved at D-08): static-import conversions at the triage-identified sites in `app/src/stores/uiStore.ts`, `app/src/main.tsx`, and/or `app/src/stores/paintStore.ts` — no new exported symbols, import-form changes only

<output>
Create `.planning/phases/40-macos-icon-regeneration-build-hygiene/40-03-SUMMARY.md` when done. Record: the approval decision (approve-all / subset / preserve-all), applied corrections with per-correction green evidence, reverted corrections (if any) with reasons, the before/after warning delta, and DI cases written for the backlog (D-10).
</output>
