---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 11
type: execute
wave: 10
depends_on: [43.2-19]
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/lib/ipc.ts
  - app/src/lib/ipc.scriptLibraryTransaction.test.ts
autonomous: true
requirements: [GRP-06]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Per D-25–D-28/D-35, TypeScript exposes exact forward/Undo/Redo prepared/committed/recovery-required/acknowledged variants, retained artifact identity, cleanup-pending state, and protected history release results."
    - "No transport facade performs orchestration or publishes accepted physical state."
  artifacts:
    - app/src/types/physicPaint.ts
    - app/src/lib/ipc.ts
  key_links:
    - "closed Rust result -> strict TS validator -> thin invoke wrapper"
---
<objective>
Add the closed TypeScript contract and thin IPC wrappers for active recovery, retained Action history, direction-specific replay, and protected release.

Purpose: Connect GRP-06 Rust authority without weakening D-25 through D-28 or creating a frontend side channel.
Output: Exact discriminated unions, validators, invoke wrappers, and transport tests.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-19-SUMMARY.md</context>

## Artifacts this phase produces

A fail-closed browser/Rust transport for every durable transaction state.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define exact transaction request/result validators</name>
  <read_first>app/src/types/physicPaint.ts script library request/result unions and validators; app/src/types/physicPaint.test.ts script-library validation cases; app/src-tauri/src/commands/script_library.rs durable command request/result names</read_first>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts</files>
  <behavior>
    - requests cover direction, history command ID/generation, retained artifact identity, prepare, commit, status/recover, acknowledge, and release with closed modes/reasons;
    - results distinguish prepared, committed, cleanup-pending, recovery-required, acknowledged, retained, released, active-recovery-blocked, and typed failures;
    - unknown keys, directions, modes, release reasons, token states, missing authorities, revisions/hashes, or malformed physical/selection targets reject.
  </behavior>
  <action>Add exhaustive validators matching Rust Plans 10/19 exactly. Include direction, stable command ID/generation, mode, Action presence/revision and retained artifact metadata, project/layer/launch authority, physical before/after revisions/hashes/documents, selection/cursor before/after, active recovery payload, cleanup-pending state, and closed release reason. Keep the simple unreferenced direct-delete contract unchanged per D-25.</action>
  <verify><automated>pnpm --dir app exec vitest run src/types/physicPaint.test.ts</automated></verify>
  <acceptance_criteria>Every TypeScript validator file run is owned; exact valid variants pass and malformed/stale-shaped variants reject.</acceptance_criteria>
  <done>The browser cannot misinterpret a journal state.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add thin invoke wrappers and correlation tests</name>
  <read_first>app/src/lib/ipc.ts existing script library invoke wrappers; app/src-tauri/src/lib.rs durable command registrations; app/src/types/physicPaint.ts new transaction validators</read_first>
  <files>app/src/lib/ipc.ts, app/src/lib/ipc.scriptLibraryTransaction.test.ts, app/src/types/physicPaint.test.ts</files>
  <behavior>
    - each wrapper invokes one registered command and parses one exact result;
    - operation/token/direction/command/generation correlation rejects stale or mismatched replies;
    - wrappers contain no scan freezing, physical replacement, history, version, selection, or recovery orchestration.
  </behavior>
  <action>Add one thin wrapper per Rust active transaction, recovery, acknowledge, retained-history status, direction-specific replay, and history-release command plus dedicated transport tests. Keep authority and orchestration in service/controller layers; map malformed replies to typed failure and expose no publication side channel.</action>
  <verify><automated>pnpm --dir app exec vitest run src/lib/ipc.scriptLibraryTransaction.test.ts src/types/physicPaint.test.ts</automated></verify>
  <acceptance_criteria>Command names, payloads, result parsing, mismatch rejection, and facade non-orchestration pass.</acceptance_criteria>
  <done>The durable journal is reachable through a closed, thin IPC seam.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Rust JSON→TypeScript | Native responses are untrusted until parsed. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-03 | Tampering | transport | high | mitigate | ASVS L1 input-validation purpose: closed variants, required authorities, operation/token correlation tests. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>Owned type and IPC tests pass; then `pnpm --dir app run typecheck`.</verification>
<success_criteria>All durable transaction states cross IPC exactly and fail closed.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-11-SUMMARY.md` when done.</output>
