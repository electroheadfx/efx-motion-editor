---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 18
type: execute
wave: 3
depends_on: [43.2-05]
files_modified:
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements: [GRP-03, GRP-04, GRP-05, GRP-06, GRP-08]
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Per D-35, one project/layer-scoped physical-operation lease is enforced at the canonical store publication boundary and is not a second document authority."
    - "Paint, Delete, Regenerate, referenced Action forward/Undo/Redo, ordinary history replay, project replacement, and hydration cannot publish revision N+1 while an exclusive or recovery lease owns revision N."
    - "Committed recovery reconstructs the lease before Studio availability and never overwrites a newer accepted revision/hash."
  artifacts:
    - app/src/stores/physicPaintStore.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/lib/physicPaintBridge.ts
  key_links:
    - "store lease registry -> coordinator acquisition before final preflight -> bridge token/authority recheck -> store token validation -> settlement/cleanup-pending/recovery release"
---
<objective>
Expand the tracer lease into the canonical physical-operation concurrency gate for every accepted mutator and startup recovery path.

Purpose: Close the D-35 revision race without introducing UI-only locking or another document authority.
Output: Exact acquisition, token propagation, publication rejection, recovery gating, cleanup-pending transfer, and event-ledger evidence.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-05-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce the canonical store lease registry and publication token</name>
  <read_first>app/src/stores/physicPaintStore.ts symbols replaceRotoPhysicalDocument, updateRotoPhysicalRealKeyPayload, getRotoPhysicalDocument, physicPaintVersion, project replacement, and hydration paths; app/src/stores/physicPaintStore.test.ts physical replacement, direct real-key, project replacement, hydration, and unchanged-state tests; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-35</read_first>
  <files>app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <behavior>
    - one project/layer-scoped registry acquires unique exclusive or recovery lease tokens without becoming document authority;
    - `replaceRotoPhysicalDocument` validates the matching active token before publication;
    - direct real-key mutation plus relevant full-project replacement and hydration paths consult the same registry;
    - missing, stale, replayed, cross-layer, or mismatched tokens reject fail-closed with unchanged document, revision, `physicPaintVersion`, history, selection, cursor, canvas, and library state.
  </behavior>
  <action>Add the authoritative acquisition registry and closed token operations at the canonical store boundary per D-35. Require the matching token for replacement, `updateRotoPhysicalRealKeyPayload`, and every relevant project/document replacement or hydration publication while an exclusive/recovery lease owns the project/layer. Preserve token-free behavior only where no active lease exists and the existing authority contract permits it; never queue, silently bypass, or create a second document authority.</action>
  <verify><automated>pnpm --dir app exec vitest run src/stores/physicPaintStore.test.ts</automated></verify>
  <acceptance_criteria>The store suite proves acquisition registry semantics, token-required replacement/direct-real-key/project/hydration publication, cross-layer/replay rejection, and byte-for-byte unchanged accepted state with no version or selection/history event on rejection.</acceptance_criteria>
  <done>The store is the canonical token-enforced physical publication boundary.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Acquire before final preflight and propagate the lease through the bridge</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts symbols executePhysicalEdit and restoreSnapshot; app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts Wave 0 acquisition/concurrency ledger; app/src/lib/physicPaintBridge.ts symbol applyPhysicPaintRotoPhysicalMap and acceptedPhysicalCommands; app/src/lib/physicPaintBridge.test.ts authority, token, and accepted-event ledger; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-35</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <behavior>
    - coordinator acquires a unique project/layer token before its final live snapshot/preflight and records revision/hash N;
    - bridge receives the token and independently rechecks physical revision/hash plus project/layer/launch authority under that lease;
    - Paint, Delete, Regenerate, referenced Action forward/Undo/Redo, ordinary history replay, and direct publication conflicts reject rather than queue;
    - rejection changes no document, version, history, selection, cursor, canvas, or library state.
  </behavior>
  <action>Update every coordinator entry into the canonical physical mutation gate to acquire before final preflight, then propagate the matching token through `applyPhysicPaintRotoPhysicalMap`. Make the bridge verify token, operation allowlist, current revision/hash, and expected project/layer/launch identity immediately before calling the store. Cover concurrent Paint/Delete/Regenerate/Action/history paths, missing/stale/replayed/cross-layer tokens, and changed authority after acquisition per D-35.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/lib/physicPaintBridge.test.ts</automated></verify>
  <acceptance_criteria>The coordinator and bridge suites prove acquire-before-final-preflight ordering, exact token propagation, final authority recheck, token-checked store call, every named concurrency rejection, and unchanged accepted document/version/history/selection evidence.</acceptance_criteria>
  <done>All physical coordinators reach the canonical store gate through one leased bridge path.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Hold, transfer, reconstruct, and release the lease across settlement and recovery</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts accepted settlement, acknowledge handoff, rollback, and finalization states; app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts settlement/recovery ledger; app/src/components/physic-paint/PhysicsPaintStudio.tsx hydration, recovery availability, and complete physical action surface; app/src/components/physic-paint/PhysicsPaintStudio.test.ts startup/recovery gating; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-35</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts</files>
  <behavior>
    - lease remains held through exact physical/library/selection/history settlement and acknowledge handoff;
    - acknowledge cleanup failure transfers to durable cleanup-pending ownership before ordinary release;
    - startup committed recovery reconstructs a recovery lease before Studio/library availability, while prepared recovery restores Action authority and releases without physical replacement;
    - every Studio physical mutation entry remains gated until recovery resolves, without a permanent lock;
    - recovery invariant mismatch never overwrites a newer accepted revision/hash.
  </behavior>
  <action>Define coordinator settlement states that retain the token through accepted publication and acknowledgement handoff, transfer ownership to cleanup-pending when native cleanup cannot finish, and release only after another recoverable owner exists. During startup, discover recovery before exposing Studio/library mutation actions and reconstruct the recovery lease before committed settlement. Project lease/recovery availability through existing Signals/computed state to gate the complete Studio mutation surface while the store remains authoritative; resolve prepared, committed, cleanup-pending, stale, and newer-document cases deterministically per D-35.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated></verify>
  <acceptance_criteria>The same-task suites prove settlement hold, acknowledge handoff, cleanup-pending transfer, startup lease reconstruction, complete Studio gating, prepared/committed recovery, eventual release without permanent lock, and refusal to overwrite newer accepted physical state.</acceptance_criteria>
  <done>The lease survives settlement and recovery exactly as long as required, then releases safely.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| coordinator/bridge→store lease | Tokens and claimed revision/hash are untrusted. |
| recovery→Studio availability | Physical mutation must remain gated until exact recovery ownership exists. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-01 | Tampering | canonical publication | high | mitigate | ASVS L1 expected-authority purpose: token validation, revision/hash recheck, deterministic rejection, unchanged-state tests. |
| T-43.2-02 | Tampering | direct Paint/project replacement bypass | high | mitigate | ASVS L1 business-logic purpose: every accepted mutator consults the same gate. |
| T-43.2-03 | Tampering/DoS | recovery lease | high | mitigate | ASVS L1 transaction-integrity purpose: cleanup-pending transfer, startup reconstruction, newer-document protection. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>`pnpm --dir app exec vitest run src/stores/physicPaintStore.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts` exits 0 as the read-only aggregate after Tasks 1–3; then `pnpm --dir app run typecheck`.</verification>
<success_criteria>One authoritative lease excludes every competing physical publication and recovers without bypass, overwrite, or permanent lock.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-18-SUMMARY.md` when done.</output>
