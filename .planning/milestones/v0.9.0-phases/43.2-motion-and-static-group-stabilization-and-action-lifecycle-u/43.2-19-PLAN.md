---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 19
type: execute
wave: 9
depends_on: [43.2-10]
files_modified:
  - app/src-tauri/src/services/script_library.rs
  - app/src-tauri/src/commands/script_library.rs
  - app/src-tauri/src/lib.rs
  - app/src-tauri/tests/script_library_transaction_contract.rs
  - app/src-tauri/tests/script_library_command_contract.rs
autonomous: true
requirements: [GRP-06]
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Per D-35, initial acknowledge removes active recovery state but retains exact Action bytes while the referenced-deletion command remains in the ten-level history."
    - "Undo atomically restores Action authority from retained bytes and Redo atomically hides/removes it through direction-specific journals before committed-only physical settlement."
    - "Explicit release on eviction, redo truncation, or session clear never removes an artifact referenced by active recovery; orphan handling is deterministic."
  artifacts:
    - app/src-tauri/src/services/script_library.rs
    - app/src-tauri/src/commands/script_library.rs
    - app/src-tauri/tests/script_library_transaction_contract.rs
  key_links:
    - "stable history command ID/generation -> retained Action artifact -> direction-specific recovery journal -> protected release/GC"
---
<objective>
Add the retained Rust Action history artifact, bidirectional restore/re-delete transactions, and explicit protected release lifecycle.

Purpose: Make referenced Action deletion genuinely reversible as one durable two-authority history command.
Output: Exact retained bytes/integrity metadata, Undo/Redo service commands, release/GC, orphan recovery, and Cargo evidence.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-10-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Retain exact Action history bytes and run direction-specific Undo/Redo recovery</name>
  <read_first>app/src-tauri/src/services/script_library.rs Phase 43.2 prepare/commit/status/recover/acknowledge implementation plus managed_path, document_revision, atomic_write_json, scan_root, and authority lock; app/src-tauri/tests/script_library_transaction_contract.rs retained-history and direction fixtures from Plan 03</read_first>
  <files>app/src-tauri/src/services/script_library.rs, app/src-tauri/tests/script_library_transaction_contract.rs</files>
  <behavior>
    - first forward prepare creates one hidden artifact keyed by stable command ID/generation with exact managed bytes, script/path identity, original revision, and integrity metadata;
    - acknowledge removes only working tombstone/active journal and leaves retained artifact;
    - Undo prepare verifies deleted Action state and exact physical target, then commit atomically restores retained bytes; Redo verifies restored Action revision and atomically hides/removes it;
    - prepared interruption restores prior Action authority; committed restart exposes exact direction-specific recovery payload.
  </behavior>
  <action>Extend the service state machine under the existing authority lock. Keep the retained artifact path distinct from temporary active journal and working tombstone paths. Validate history direction, command ID, generation, mode, expected project/layer/launch authorities, Action presence/absence/revision, physical revision/hash, semantic target, and artifact integrity before mutation. Active journals carry the exact target physical document and selection/cursor payload required for recovery. Commit Action authority first, sync file/directory transitions, write a direction-specific committed marker, and preserve stale/replay rejection per D-35.</action>
  <reversibility rating="costly">The retained artifact and direction-specific journal formats govern crash recovery and history replay.</reversibility>
  <verify><automated>cargo test --manifest-path app/src-tauri/Cargo.toml --test script_library_transaction_contract history</automated></verify>
  <acceptance_criteria>Keep Groups and cascade forward/Undo/Redo restore/remove exact Action authority and recovery payloads; interruption in Undo/Redo, integrity mismatch, stale/replayed direction/generation, and acknowledge retry all pass.</acceptance_criteria>
  <done>Rust retains enough exact authority to reverse and replay referenced deletion after initial acknowledge.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Release retained artifacts only on owned history lifecycle events</name>
  <read_first>app/src-tauri/src/commands/script_library.rs Phase 43.2 transaction facades; app/src-tauri/src/lib.rs invoke registrations; app/src-tauri/tests/script_library_command_contract.rs history release fixtures; app/src-tauri/src/services/script_library.rs startup scan/recovery ordering</read_first>
  <files>app/src-tauri/src/services/script_library.rs, app/src-tauri/src/commands/script_library.rs, app/src-tauri/src/lib.rs, app/src-tauri/tests/script_library_command_contract.rs</files>
  <behavior>
    - explicit release accepts only eviction, redo-branch-truncation, or session-history-clear reasons with matching owner/command/generation;
    - active recovery reference blocks release and repeated release is idempotent;
    - startup orphan scan distinguishes active, retained-and-owned, releasable orphan, and corrupt state without exposing ordinary scans;
    - command facade and handler registration remain thin and closed.
  </behavior>
  <action>Add the service-level history release/GC operation and thin Tauri command registration. Require owning session/history identity, stable command ID/generation, allowed release reason, integrity verification, and no active recovery reference before deletion. Define deterministic orphan cleanup: complete/restore active recovery first; preserve valid retained artifacts still owned; remove only provably unowned artifacts; remain recovery-required on corrupt or ambiguous state. Repeated release of an already released artifact succeeds without affecting Action or physical authority.</action>
  <verify><automated>cargo test --manifest-path app/src-tauri/Cargo.toml --test script_library_command_contract history_release && cargo test --manifest-path app/src-tauri/Cargo.toml services::script_library</automated></verify>
  <acceptance_criteria>Ten-level eviction, redo truncation, intentional clear, active-recovery protection, duplicate release, orphan cleanup, corrupt-state gating, and exact command registration pass.</acceptance_criteria>
  <done>Retained Action bytes have one explicit, protected, idempotent lifecycle.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| history request→retained bytes | Command IDs, generations, directions, authorities, and release reasons are untrusted. |
| orphan scan→ordinary library | Ambiguous retained/recovery state must not be silently deleted or exposed. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-03 | Tampering | retained Action history | high | mitigate | ASVS L1 transaction-integrity purpose: exact bytes/revision/path/integrity metadata, direction validation, committed markers, protected release. |
| T-43.2-01 | Tampering | physical recovery payload | high | mitigate | Expected project/layer/launch revision/hash and exact target payload validation. |
| T-43.2-02 | Tampering | cascade restore/remove set | high | mitigate | Closed semantic target digest and exact retained provenance facts. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>Both exact Cargo integration commands exit 0.</verification>
<success_criteria>Forward, Undo, Redo, restart, acknowledge, release, and orphan handling preserve exact Action authority without premature history-byte deletion.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-19-SUMMARY.md` when done.</output>
