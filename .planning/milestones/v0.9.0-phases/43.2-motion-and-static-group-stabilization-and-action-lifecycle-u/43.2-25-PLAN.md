---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 25
type: execute
wave: 19
depends_on: [43.2-21, 43.2-22, 43.2-23, 43.2-24]
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
  - .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [GRP-02, GRP-04, GRP-07, GRP-08]
estimate: { tokens: 48000, raw_tokens: 48000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Per D-03/D-22/D-35, every Group mutation transports and validates one complete proposed physical document: operation target remains in startFrame while the accepted physical cursor travels independently as cursorAppFrame, including when selectedKeyId and selectedAppFrame are null."
    - "Per D-03/D-35, changing Repeat rebuilds the selected Group's lifecycle-complete finite extent from its accepted source cycle while preserving stable identity, placement, provenance, settings, synchronization authority, exact source-cycle phase, and one-command Undo/Redo."
    - "Per D-12/D-13/D-17/D-18, Delete and Backspace follow accepted selection scope directly: an individual Group-owned frame dispatches Delete Frame, a selected Group Rail dispatches Delete Group, and only the sole-occurrence Delete Frame path opens the focused warning."
    - "Per D-11/D-18/D-23/D-35, accepted forward, Undo, Redo, rejection, and immediate retry settle the same complete document, selection, cursor, lifecycle ranges, records, gaps, and history without a restart, reload, layer replacement, revision refresh, or weakened stale guard."
    - "Per D-36/D-37, focused and complete Vitest, Cargo, typecheck, and build gates pass without changing Motion Editor production, test configuration, or consumer authority."
    - "Per D-39, Plan 43.2-17 remains blocked until the frozen implementation commit passes the owned native deletion, Repeat, retry, history, save/reopen, playback, preview, and export retest from a fully restarted parent/child session."
  artifacts:
    - app/src/types/physicPaint.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
    - app/src/lib/physicPaintBridge.ts
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
  key_links:
    - "child complete proposal cursor -> mandatory apply payload cursorAppFrame -> strict runtime parser -> coordinator pending tuple -> parent lifecycle recomputation -> accepted result/history snapshot -> exact child settlement"
    - "Repeat draft -> accepted source-cycle length and placement -> lifecycle extent rebuild -> semantic delta -> parent acknowledgement -> exact forward/Undo/Redo documents"
    - "physical-frame versus Group-Rail selection -> activation-time delete classification -> direct operationKind dispatch -> sole-occurrence-only warning -> canonical physical edit coordinator"
    - "existing RED matrices -> focused GREEN gates -> full validation -> frozen commit hash in UAT/STATE -> restarted native checkpoint"
---

<objective>
Close the remaining Plan 17 native Group Delete and Repeat lifecycle failures by restoring complete-document transport and direct selection-scoped deletion.

Purpose: Remove the shared parent/child divergence in which structural revision stays equal while cursor or lifecycle extent differs, without bypassing rejection or resetting authority.
Output: Explicit cursor transport, lifecycle-complete Repeat rebuilding, direct Delete Frame/Delete Group routing, focused and full automated evidence, a frozen implementation commit, and a blocking native retest handoff.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UI-SPEC.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-17-PLAN.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-21-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-22-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-23-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-24-SUMMARY.md
</context>

## Diagnosed gap boundary

The working-tree Vitest additions are the proven RED matrix and must drive implementation rather than be replaced with weaker local tests. `physicPaintBridge.test.ts` proves that parent and child may share the same structural revision while holding different complete cursor authority; the apply payload omits `cursorAppFrame`, the coordinator overloads the lifecycle target into `startFrame`, and the parent reconstructs cursor from `selectedAppFrame ?? startFrame`. Group operations normally clear ordinary-key selection, so this reconstruction changes the proposed complete document and rejects valid Delete Frame/Delete Group operations, especially at nonzero placement, after Key Spacing, and when Group-Rail selection leaves the cursor elsewhere. `physicsPaintRotoPlayScriptController.test.ts` separately proves that Repeat changes publish a new scalar repeat without rebuilding lifecycle extents. `useRotoTimelineActions.test.ts` and `PhysicsPaintStudio.test.ts` define the superseding direct selection-scoped Delete contract and retire the routine scope-choice modal.

The repair must transport and compare the complete target document. Do not add a local lifecycle-comparison exception, retry bypass, stale acceptance path, revision-only equivalence, cursor clamp, silent revision refresh, forced selection reset, reload, layer recreation, or restart-based recovery. Preserve strict parent authority and immediate retry on the same accepted layer after rejection.

## Multi-Source Coverage Audit

| Source | ID | Gap requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable Motion/Static Groups support exact accepted mutation, lifecycle, deletion, history, persistence, and consumer parity | 25 | COVERED | Repairs the complete-document boundary blocking Plan 17 native UAT. |
| REQ | GRP-02 | Canonical Group model, lifecycle extents, persistence, and exact accepted reconstruction remain complete | 25 | COVERED | Explicit cursor transport and Repeat lifecycle rebuild preserve one authority. |
| REQ | GRP-04 | Exact-frame Paint/Delete, Delete Group, atomic cleanup, and exact Undo/Redo work on accepted Group state | 25 | COVERED | Existing fresh/post-spacing/retry/history RED matrices become GREEN. |
| REQ | GRP-07 | Selection scope, Delete UI, Group Rail behavior, and accessible focused warning match the product contract | 25 | COVERED | Removes the superseded routine choice modal and keeps only the sole-occurrence warning. |
| REQ | GRP-08 | Parent authority, strict stale guards, persistence, playback, preview, export, and native validation agree | 25 | COVERED | Full gates and frozen native handoff retain Plan 17 as the approval boundary. |
| RESEARCH | — | Complete immutable physical document crosses coordinator/bridge boundaries; derived revision cannot substitute for cursor/selection authority | 25 | COVERED | Mandatory `cursorAppFrame` is validated, staged, echoed, compared, and replayed. |
| RESEARCH | — | Lifecycle-mutating controls rebuild finite range authority rather than changing one scalar field | 25 | COVERED | Repeat uses the accepted source cycle and canonical lifecycle derivation. |
| CONTEXT | D-03/D-11/D-22/D-23/D-35 | Stable identity, complete atomic target, exact cursor/selection settlement, and strict parent acknowledgement | 25 | COVERED | Transport, Repeat, Delete, retry, and history share one complete-document invariant. |
| CONTEXT | D-12/D-13/D-17/D-18 | Selection-scoped direct deletion, sole-occurrence warning, complete Group cleanup, Action retention | 25 | COVERED | Timeline action and Studio routing implement the superseding contract. |
| CONTEXT | D-36/D-37/D-39 | Consumer parity, preserved architecture, and restarted frozen-session evidence | 25 | COVERED | Full automation precedes the blocking native checkpoint. |

Deferred Action editing, Relink, Push, Group drag, Key Groups, Scissor, multiple Paint tracks, Motion Editor changes, and release work remain excluded per D-05/D-38.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Transport one complete cursor-aware Group deletion target through child and parent</name>
  <read_first>app/src/lib/physicPaintBridge.test.ts selectionAuthorityHarness, fresh deletion matrix, post-Key-Spacing matrix, rejected-to-retry case, and Delete-Undo-Redo control; app/src/types/physicPaint.ts PhysicPaintRotoPhysicalEditApplyPayloadBase, result types, strict apply/result validators, and closed key allowlists; app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts execute input, Group lifecycle preflight, payload construction, pending tuple, result matching, and deferred settlement; app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts physical edit payload/result settlement cases; app/src/lib/physicPaintBridge.ts apply validation, lifecycle target recomputation, accepted document staging, command ledger, Undo, and Redo; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-03, D-11, D-18, D-22, D-23, D-35</read_first>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <behavior>
    - RED-to-GREEN tracer: one Group Frame delete with child cursor different from mutation target transports the child proposal's cursor explicitly and is accepted end to end without changing startFrame semantics;
    - every physical edit apply payload contains mandatory integer cursorAppFrame; the strict parser rejects an omitted, unknown, non-integer, negative, or capacity-exceeding cursor rather than substituting or clamping another field;
    - selectedKeyId remains paired only with selectedAppFrame, while cursorAppFrame remains present and authoritative when both selection fields are null;
    - coordinator pending/result correlation includes the explicit cursor, and accepted settlement publishes the complete proposal cursor even when no ordinary key is selected;
    - parent lifecycle recomputation, staged accepted document, semantic comparison, history before/after snapshots, Undo, Redo, and accepted result all use the transported cursor rather than selectedAppFrame or startFrame fallback;
    - the existing eight fresh deletion cells, eight post-Key-Spacing cells, rejected-to-retry case, and Delete-Undo-Redo control pass without weakening lease, revision, lifecycle, duplicate-destination, or stale-operation guards.
  </behavior>
  <action>Run the single differing-cursor bridge case first and preserve its failing evidence. Add mandatory `cursorAppFrame` to the physical edit apply contract and every closed runtime allowlist/validator that parses or correlates apply and accepted-result payloads. Populate it from the coordinator's complete proposed/accepted physical document, carry it in pending correlation, require exact acknowledgement, and publish it during settlement even when `selectedAppFrame` is null. In the parent, validate the transported cursor against the active physical capacity before mutation and use it directly for lifecycle recomputation, the staged accepted document, semantic comparison, command snapshots, Undo/Redo targets, and result echo. Keep `startFrame` as operation target/range metadata only. Preserve the D-35 strict stale and semantic guards; a malformed or stale command rejects atomically and a corrected command retries immediately on the same layer. Do not infer cursor from selection, clamp malformed input, refresh revision, reset the layer, or add operation-specific acceptance exceptions. Update coordinator coverage only where necessary to pin the mandatory payload/result contract; retain the bridge working-tree matrix as the public end-to-end oracle.</action>
  <verify><automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts -t "selection authority|delete-group-frame|delete-group|Key Spacing|retry|Undo.*Redo" && pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts -t "physical edit|cursor|settlement|retry"</automated></verify>
  <acceptance_criteria>The differing-cursor tracer fails before implementation and passes afterward; all fresh and post-spacing deletion matrix cells accept the exact child proposal, malformed cursor payloads reject atomically, rejected-to-retry stays usable without authority reset, and forward/Undo/Redo reproduce exact complete documents.</acceptance_criteria>
  <done>Group mutation target and physical cursor are independent, explicit, strictly validated authorities across child, parent, settlement, and history per D-03/D-35.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild Repeat lifecycle and replace the Delete scope-choice modal with direct selection routing</name>
  <read_first>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts four lifecycleLoopClip Repeat extent/Undo/Redo RED cases; app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts updateLoop and canonical range/readout helpers; app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts GroupDeleteActivation RED contract and ordinary-key controls; app/src/components/physic-paint/hooks/useRotoTimelineActions.ts RotoDeleteTarget, target classification, activation-time reclassification, and deleteRotoFrame; app/src/components/physic-paint/PhysicsPaintStudio.test.ts direct lifecycle-delete and sole-warning source contracts; app/src/components/physic-paint/PhysicsPaintStudio.tsx GroupDeleteTarget/GroupDeleteChoice state, lifecycle submission, focus handling, and modal markup; app/src/components/physic-paint/physicsPaintStudio.css Group delete choice and focused-warning selectors; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UI-SPEC.md Delete interaction contract; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-03, D-12, D-13, D-14, D-15, D-17, D-18, D-22, D-35, D-37</read_first>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/physicsPaintStudio.css, .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md, .planning/STATE.md</files>
  <behavior>
    - Repeat 1-to-3 and 3-to-1 at placement 0 and placement 10 publish lifecycle-complete Groups with exact originalEndExclusive and one finite contiguous visible range derived from source-cycle length times Repeat;
    - Repeat preserves loopId, placementStart, ordered sourceKeyIds, mode, scriptId, Motion, override color, synchronization and provenance state, frame overrides, phaseOrigin, source sharing, selection, and cursor; one Undo and one Redo restore exact complete collections;
    - a selected Group Rail classifies as Group deletion before frame ownership is considered and dispatches operationKind delete-group without moving or reclassifying the cursor;
    - an individually selected Group-owned physical frame dispatches operationKind delete-group-frame for that exact appFrame and phaseOrigin;
    - only a sole-occurrence Delete Frame invokes the focused warning; cancellation changes nothing, rejection leaves warning/focus and command authority retryable, and acceptance closes it after exact parent acknowledgement;
    - ordinary real-key deletion, key-group deletion, Action retention, fragments, source/cache sharing, stale rejection, and keyboard routes remain unchanged;
    - the routine Delete Group/Delete Frame choice type, state, copy, option cards, handlers, and solely owned CSS are absent.
  </behavior>
  <action>Use the four existing Repeat RED cases one at a time. Replace the scalar-only Repeat update with one pure lifecycle-complete rebuild from the accepted Group source cycle and canonical finite-range derivation. Preserve every stable identity, placement, provenance, setting, synchronization, override, and sharing field; update Repeat plus its dependent original extent/range authority in the same proposed document so semantic delta and history contain the complete transition per D-03/D-35. Do not re-render sources, replace loopId, move placement, recreate the layer, or hydrate lifecycle after acceptance.

Then use the existing timeline/Studio RED tests to implement the superseding D-12/D-13 selection contract. Add selected Group IDs to delete classification input; classify a complete Group Rail as `group`, an individual owned frame as `group-frame`, and reclassify at activation before dispatch. Route both directly through `executeGroupLifecycleDelete` with explicit operationKind/groupId/appFrame/phaseOrigin. Route only `group-frame` with `onlyOccurrence` through `requestSoleOccurrenceDeleteWarning`, preserving the exact UI-SPEC heading, warning copy, Cancel/Delete Frame controls, macOS-safe focus capture, Escape, acceptance-only close, and rejection retry. Remove the obsolete routine choice state, types, markup, copy, handlers, and CSS selectors without altering unrelated dialog styles or introducing mirrored Signal/effect state. Preserve ordinary deletion and the referenced Action per D-18.

After focused GREEN, run the complete gates below. Do not start a server and do not alter test configuration. If every gate passes, update `43.2-UAT.md` with the diagnosed root cause, exact focused/full command evidence, remaining blocked native rows, and the final implementation commit hash; update `.planning/STATE.md` to mark the phase automated-ready while Plan 43.2-17 remains native-blocked. Create the final frozen implementation/evidence commit using only the intended production, existing RED test, UAT, and state files; do not amend it or change code after recording its hash. If any gate fails, do not freeze or release the native checkpoint.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts -t "Repeat" && pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts -t "Delete Frame|Delete Group|sole|selection|retry|scope" && pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && cargo test --manifest-path app/src-tauri/Cargo.toml && pnpm --dir app exec vitest run && pnpm --dir app run typecheck && pnpm build</automated></verify>
  <acceptance_criteria>All four Repeat lifecycle cases and the direct Delete UI/controller RED tests pass; the full bridge deletion/retry/history matrix passes; Cargo, complete one-shot Vitest, typecheck, and production build exit 0; the obsolete routine scope-choice modal is gone; UAT/STATE record the frozen commit and keep native approval blocked.</acceptance_criteria>
  <done>Repeat and Delete now produce complete canonical lifecycle targets, direct selection-scoped commands, exact history, and an automated-ready frozen candidate without any rejection bypass or restart repair.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Retest Group Delete and Repeat lifecycle from the frozen parent/child build</name>
  <what-built>Mandatory cursor-aware complete-document transport; lifecycle-complete Repeat expansion/contraction; direct selection-scoped Delete Frame/Delete Group with only the sole-occurrence warning; exact rejection/retry and forward/Undo/Redo settlement on the frozen implementation commit.</what-built>
  <how-to-verify>Keep Plan 43.2-17 blocked. Fully quit both applications, build and launch the exact recorded frozen commit, and begin one clean parent/Physics Paint child session per D-39; do not use hot reload, reload, layer recreation, or restart as a repair after a failed command. Re-run the `43.2-UAT.md` rows `[UAT-05]`, `[UAT-06]`, `[UAT-13]`, `[DELETE-FRAME]`, `[DELETE-GROUP]`, and `[EDIT-GROUP-REPEAT]`: test Motion and Static Groups at placement 0 and a nonzero placement, Repeat 1 and 3, individual physical-frame selection and complete Group-Rail selection, then repeat deletion after Apply Key Spacing. Verify Delete/Backspace directly performs the selected scope with no routine choice dialog; the sole remaining occurrence shows only the locked focused warning; rejection changes nothing and the same layer accepts an immediate corrected retry; accepted Delete Frame keeps cursor on the deleted frame; accepted Delete Group keeps cursor fixed and clears removed Group selection; Repeat 1-to-3 and 3-to-1 rebuild exact rail/cell extents while preserving identity, placement, Action provenance, settings, and source phase; one Undo and one Redo reproduce every complete state. Save/reopen the fixture, then check Studio playback, preview, and export against the same accepted extents/gaps. Record evidence in UAT, but approve Plan 43.2-17 only after all remaining owned rows—not merely this targeted subset—pass.</how-to-verify>
  <resume-signal>Type "approved" only after the frozen-session targeted matrix and the remaining Plan 43.2-17 checklist pass, or report the exact failing row, selection scope, placement, Repeat, cursor, and forward/Undo/Redo step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| child proposal→physical edit payload | Complete cursor, selection, records, lifecycle, and operation target cross from child authority to strict transport. |
| transport parser→parent staging | Untrusted or stale payload data must be closed-key validated before lifecycle recomputation or mutation. |
| parent accepted document→child settlement/history | Acknowledgement, pending correlation, cursor publication, and Undo/Redo must reproduce the same complete immutable document. |
| timeline selection→destructive command | Frame versus Group-Rail scope determines Delete Frame versus Delete Group without a routine choice dialog. |
| automated evidence→native approval | Headless tests cannot substitute for one frozen restarted parent/child lifecycle session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-25-01 | Tampering | physical edit payload cursor | high | mitigate | Require explicit bounded cursorAppFrame in closed validators and exact pending/result correlation; reject omission or mismatch before mutation. |
| T-43.2-25-02 | Tampering | lifecycle target recomputation | high | mitigate | Use transported cursor and lifecycle-complete Repeat derivation in parent semantic comparison; retain all stale, lease, identity, range, sharing, and destination guards. |
| T-43.2-25-03 | Repudiation | accepted mutation history | high | mitigate | Store and replay exact complete before/after documents, with RED forward/Undo/Redo controls for Delete and Repeat. |
| T-43.2-25-04 | Elevation of Privilege | selection-scoped destructive routing | high | mitigate | Activation-time classification prioritizes selected Group Rail for Delete Group and exact owned frame for Delete Frame; only sole occurrence receives a warning. |
| T-43.2-25-05 | Denial of Service | rejected command retry | medium | mitigate | Rejection preserves document, version, lease, pending state, selection, cursor, and history so the same layer accepts an immediate valid retry. |
| T-43.2-25-06 | Tampering | frozen native evidence | medium | mitigate | Record the final commit hash before D-39 restart and reject mixed hot-reload or post-freeze code evidence. |
| T-43.2-SC | Tampering | packages | low | accept | No package installs, dependency changes, or lockfile changes are permitted. |
</threat_model>

<verification>
<automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts && cargo test --manifest-path app/src-tauri/Cargo.toml && pnpm --dir app exec vitest run && pnpm --dir app run typecheck && pnpm build</automated>
<human-check>Blocking: execute Task 3 from the exact frozen commit in one fully restarted parent/child session. No server startup, hot reload, reload, layer recreation, silent revision refresh, or restart-based retry is valid evidence. Plan 43.2-17 remains blocked until the full owned native checklist passes.</human-check>
</verification>

<success_criteria>The complete cursor-aware document crosses transport and history unchanged; Repeat rebuilds exact lifecycle extents; selection directly invokes Delete Frame or Delete Group with only the sole-occurrence warning; fresh, post-Key-Spacing, retry, and Undo/Redo matrices pass; all complete automated gates pass; UAT/STATE record a frozen commit; and Phase 43.2 remains native-blocked until the restarted frozen-session checkpoint is approved.</success_criteria>

<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-25-SUMMARY.md` when implementation and automated gates are complete. Do not mark Plan 43.2-17 or Phase 43.2 approved before Task 3 passes.</output>
