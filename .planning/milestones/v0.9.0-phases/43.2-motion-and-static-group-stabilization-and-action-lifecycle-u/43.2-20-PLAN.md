---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 20
type: execute
wave: 12
depends_on: [43.2-12]
files_modified:
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
  - app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts
  - app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
autonomous: true
requirements: [GRP-06]
estimate: { tokens: 38000, raw_tokens: 38000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Per D-35, referenced Action deletion occupies one history entry carrying stable Rust command ID/generation, exact physical and selection before/after facts, mode, authorities, revisions, and hashes."
    - "Undo and Redo each run mutation-free direction preflight, acquire the shared lease, wait for Rust committed, pass exact direction/command/generation/Action/physical/authority/token facts to the bridge, publish exact target once, and move the history pointer only after accepted local settlement."
    - "Eviction, redo-branch truncation, and session clear invoke protected Rust release; acknowledge retry or stale replay causes no duplicate event or pointer movement."
  artifacts:
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts
    - app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts
    - app/src/lib/physicPaintBridge.ts
  key_links:
    - "history entry artifact identity -> direction preflight/lease/Rust committed wait -> direction-specific bridge validation and exact-once settlement -> pointer move -> protected release hook"
---
<objective>
Implement frontend bidirectional referenced-Action replay and retained-artifact release as one exact history command.

Purpose: Ensure Undo restores both Action and physical provenance, Redo removes both again, and history lifecycle owns durable-byte release.
Output: Extended history entry, direction-specific orchestration, exact event ledger, pointer settlement, and release hooks.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-12-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-19-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Orchestrate direction-specific history replay before pointer movement</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts symbols RotoPhysicalEditHistoryEntry, replayAcceptanceMatchesPending, appliedRef, redoRef, undo, and redo; app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts referenced-deletion replay ordering; app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts direction preflight, lease acquisition, Rust prepare/commit/status, and committed settlement API; app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts Wave 0 direction event ledgers; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-12-SUMMARY.md committed-only settlement contract</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts, app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts, app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts</files>
  <behavior>
    - referenced-deletion entry carries stable command ID/generation, mode, exact physical before/after revisions/hashes/documents, selection/cursor before/after, and project/layer/launch authorities;
    - Undo and Redo each run mutation-free direction preflight, acquire the shared lease, and wait for the matching Rust committed state before requesting local settlement;
    - Keep Groups and cascade choose their exact direction-specific Action, physical, provenance, and selection target;
    - `appliedRef`/`redoRef` move only after accepted local settlement; preflight failure, interruption, stale/replay rejection, or acknowledge failure leaves pointers and accepted state unchanged.
  </behavior>
  <action>Extend `RotoPhysicalEditHistoryEntry` only for referenced Action commands while preserving ordinary physical snapshot replay. Route `undo` and `redo` through the library controller's direction-specific preflight/orchestration and canonical lease acquisition, never direct snapshot restoration. Wait for the exact Rust committed direction before invoking the bridge settlement port, and move history pointers only after that port reports accepted local settlement per D-35. Keep bridge production and bridge tests untouched in this task.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts</automated></verify>
  <acceptance_criteria>The history/controller suites pass Keep Groups and cascade direction preflight, acquisition, Rust committed wait, interruption/restart, stale/replayed generation rejection, and pointer-after-acceptance ordering without modifying or invoking bridge evidence.</acceptance_criteria>
  <done>Referenced deletion directions reach settlement only after durable committed authority and retain exact pointer ordering.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Settle direction-specific replay through the leased bridge exactly once</name>
  <read_first>app/src/lib/physicPaintBridge.ts replay provenance, applyPhysicPaintRotoPhysicalMap, acceptedPhysicalCommands, and token-checked publication; app/src/lib/physicPaintBridge.test.ts forward/Undo/Redo authority, replay, and event-ledger cases; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-12-SUMMARY.md lease handoff and committed-only settlement</read_first>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts</files>
  <behavior>
    - settlement validates direction, stable command ID, generation, exact current Action presence/absence/revision, and the direction-specific target;
    - settlement verifies current physical revision/hash, project/layer/launch authority, and the matching active lease token;
    - accepted forward/Undo/Redo each publish exact physical state once, emit one version notification, settle selection/cursor once, and report one acceptance;
    - stale, replayed, mismatched-direction, wrong-generation, wrong-Action, wrong-authority, wrong-hash, or wrong-token input leaves the complete accepted state unchanged.
  </behavior>
  <action>Implement the direction-specific leased bridge settlement used by Task 1. Close the accepted operation variants for referenced Action forward, Undo, and Redo; verify direction, stable command ID, generation, exact Action state, physical revision/hash, project/layer/launch authority, semantic target, and matching lease token immediately before the sole store publication. Settle physical/version/selection exactly once and return acceptance for later history-pointer movement; reject stale/replayed or mismatched input with no accepted-state event per D-35.</action>
  <verify><automated>pnpm --dir app exec vitest run src/lib/physicPaintBridge.test.ts</automated></verify>
  <acceptance_criteria>The bridge suite proves exact forward/Undo/Redo direction and authority validation, matching token enforcement, one physical/version/selection settlement per accepted direction, and unchanged accepted state for every stale/replayed/rejected input.</acceptance_criteria>
  <done>The leased bridge is the exact-once local settlement boundary for bidirectional referenced deletion.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Release retained Action artifacts on history eviction, truncation, and clear</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts accepted edit append, ten-level cap, redoRef clearing, launch identity reset, and session clear paths; app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts release ownership cases; app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts history-release IPC adapter and active-recovery response; app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts release correlation; app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts generation/recovery ledger</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts, app/src/components/physic-paint/hooks/useRotoScriptLibraryController.ts, app/src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts</files>
  <behavior>
    - trimming beyond ten releases only evicted referenced-deletion artifacts;
    - a new accepted edit releases referenced commands discarded from redo, while failed/stale/no-change edits release nothing;
    - intentional owning-session clear/reset releases both stacks only after active recovery ownership resolves;
    - active-recovery deferral and release retry are idempotent and cause no physical, library, history-pointer, selection, or visible event.
  </behavior>
  <action>Add one release callback/port from history to the library controller carrying owner, command ID, generation, and one closed reason: eviction, redo-branch-truncation, or session-history-clear. Invoke it after the corresponding local history ownership change, never during failed/stale/no-change operations. Correlate retries without moving pointers or changing visible state; when Rust reports active recovery, retain ownership and retry after recovery instead of deleting or abandoning the artifact.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts</automated></verify>
  <acceptance_criteria>Ten-level eviction, redo truncation, session clear, failed-edit non-release, active-recovery deferral, idempotent retry, and zero visible/event/pointer changes during release all pass.</acceptance_criteria>
  <done>Frontend history ownership and Rust retained-byte ownership remain synchronized.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| history entry→Rust direction transaction | Stored command provenance may be stale or replayed. |
| history lifecycle→artifact release | Premature or duplicate release could destroy Undo authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-03 | Tampering | bidirectional replay/release | high | mitigate | ASVS L1 transaction-integrity purpose: closed direction/generation, retained artifact identity, pointer-after-settlement, protected release reasons. |
| T-43.2-01 | Tampering | target physical replay | high | mitigate | Exact current revision/hash and lease token validation before bridge/store publication. |
| T-43.2-02 | Tampering | restored cascade ownership | high | mitigate | Exact before/after snapshots and parent semantic recomputation. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>`pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts src/components/physic-paint/hooks/useRotoScriptLibraryController.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/roto/physicsPaintRotoScriptLibrary.test.ts` exits 0 as the read-only aggregate after Tasks 1–3; then `pnpm --dir app run typecheck`.</verification>
<success_criteria>Undo/Redo and history release preserve exact two-authority state with one pointer/event settlement and no premature artifact deletion.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-20-SUMMARY.md` when done.</output>
