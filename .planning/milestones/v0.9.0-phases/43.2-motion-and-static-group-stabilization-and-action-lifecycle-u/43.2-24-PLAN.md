---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 24
type: execute
wave: 18
depends_on: [43.2-14, 43.2-15]
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
gap_closure: true
requirements: [GRP-07]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Per D-29/D-30, selecting an individual Group-owned physical frame may leave its Action selected, but the Group Rail is linked-only rather than selected: one subtle mode-colored 3px segment and no persistent visual treatment on the full 12px target."
    - "Per D-15/D-30, selecting the whole logical Group while its Action remains selected produces one orange 3px segment across every fragment; selected orange overrides passive Action linkage without adding a second rail."
    - "Per D-37, the fix preserves the existing 3px rail, 12px interaction target, white endpoint cuts, lifecycle dots, cursor, counts, accessibility, physical-cell behavior, Signals authority, and zero-added-height geometry."
    - "Plan 43.2-17 remains blocked until the targeted native individual-frame-versus-whole-Group visual retest passes from a fully restarted frozen-code parent/child session per D-39."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
    - app/src/components/physic-paint/physicsPaintStudio.css
  key_links:
    - "individual physical-frame click -> Group selection clears while Action selection remains -> rail target has action-linked without selected -> linked treatment stays on .physics-paint-loop-clip-rail-segment"
    - "whole Group selection -> rail target has action-linked and selected -> selected segment rule wins with orange and target box remains visually transparent"
---

<objective>
Close the diagnosed UAT-11 parasite-rail regression without changing Group/Action selection authority.

Purpose: Remove the bright partial rectangle caused by painting the full 12px linked target while preserving the legitimate single 3px Group Rail and the intentional distinction between individual-frame and whole-Group selection.
Output: RED-first component/CSS coverage for linked-only versus linked-and-selected states, a segment-local CSS correction unless RED identifies a real class-projection defect, focused rail/workflow gates, full typecheck/build, and a targeted native visual retest handoff while Plan 43.2-17 stays blocked.
</objective>

<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-14-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-15-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md</context>

## Diagnosed gap boundary

The initiating cause is confirmed and must not be re-diagnosed as a state-authority failure. Selecting a Group also selects its referenced Action. Clicking an individual physical frame correctly clears Group selection while retaining Action selection, so the rail target remains `action-linked` but loses `selected`. The current CSS applies a 1px spread and glow to the full 12px `.physics-paint-loop-clip-rail-target`, which paints the unauthorized bright partial rectangle and apparent vertical end-caps around the legitimate 3px segment. `PhysicsPaintLoopClipRail.test.tsx` explicitly locks in that target-level `box-shadow`; replace that assertion with the corrected visual contract. Preserve the selection lifecycle, Signals, Action linkage, cursor, linked counts, and accessibility rather than clearing the Action to hide the CSS defect.

## Multi-Source Coverage Audit

| Source | ID | Gap requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable Motion/Static Group and Action linkage remains visually coherent under physical-frame and whole-Group selection | 24 | COVERED | Corrects only the linked rail presentation boundary. |
| REQ | GRP-07 | Group/Action selection and linked highlighting remain distinct, passive, and scope-safe | 24 | COVERED | Pins linked-only versus selected operation scope in component and workflow tests. |
| RESEARCH | — | Rail presentation is a computed view over existing Signals/controller state; no mirrored state or effect-driven selection repair | 24 | COVERED | Production is CSS-only unless RED proves the existing class projection is wrong. |
| CONTEXT | D-15/D-29/D-30 | Whole-Group orange selection, retained Action linkage, passive mode-colored linked treatment | 24 | COVERED | Tests both class combinations and selected precedence. |
| CONTEXT | D-37/D-39 | Preserve geometry/selection boundaries and require frozen-session native acceptance | 24 | COVERED | Geometry guards and exact visual retest are included. |

Deferred ideas, Motion Editor production, Group-selection lifecycle changes, and Action deselection work remain excluded.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace the target-level linked halo with one segment-local rail treatment</name>
  <read_first>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx linked-halo and exact-geometry cases; app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx target class composition and segment child; app/src/components/physic-paint/physicsPaintStudio.css selectors for rail target, segment, action-linked, selected, boundaries, lifecycle dot, and focus-visible; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-15, D-29, D-30, D-37</read_first>
  <files>app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <behavior>
    - RED: render the same Motion and Static Group in an individual-frame state with `linkedLoopClipIds` containing the Group and `selectedLoopClipIds` empty; each target is `action-linked`, is not `selected`, reports `aria-pressed=false`, and owns exactly one 3px segment;
    - RED: the linked-only target rule has no persistent target-level border, outline, spread shadow, background, pseudo-element, or endpoint decoration; the existing keyboard `:focus-visible` outline remains separately intact for accessibility;
    - RED: passive linked Motion and Static treatment is constrained to `.physics-paint-loop-clip-rail-segment`, using the existing lighter mode tokens `#c4b5fd` and `#67e8f9` without an outer spread or a second line;
    - RED: render the same Group with both `action-linked` and `selected`; `aria-pressed=true`, exactly one segment remains, and selected orange `#f59e0b` wins over linked mode color;
    - RED: legitimate white start/end cuts remain children of the 3px segment, lifecycle dots remain unchanged, and the 12px target remains the interaction/focus box only.
  </behavior>
  <action>Write the failing component/CSS regression first and run it to prove the existing assertions still require the bad full-target shadow. Replace the two target-level `.action-linked:not(.selected)` shadow rules with segment-descendant rules: Motion linked-only uses `#c4b5fd`, Static linked-only uses `#67e8f9`, and neither rule applies box-shadow, border, outline, background, pseudo-elements, or geometry to the 12px target. Keep the selected segment selector authoritative so an Action-linked selected Group stays orange per D-15/D-30. Preserve the existing white segment endpoint cuts, unresolved precedence, lifecycle dots, hover/focus behavior, 3px segment, 12px target, anchor width, and focus-visible outline per D-37. Do not change Signals, selected/linked props, class composition, Action selection, cursor behavior, counts, callbacks, ARIA, or component markup unless the RED render proves one of those existing contracts is actually wrong; if that occurs, stop and document the contradictory RED evidence before expanding production beyond CSS.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx -t "linked|selected|rail, target, endpoint, focus"</automated></verify>
  <acceptance_criteria>The focused test fails against the old target shadow, then passes with linked-only and linked-plus-selected states showing exactly one 3px segment; no persistent visual box is painted on the 12px target, selected orange wins, and legitimate focus/endpoints/dots/geometry remain pinned.</acceptance_criteria>
  <done>The confirmed CSS root cause is removed without changing selection authority or introducing a second rail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard workflow state separation and run automated/native handoff gates</name>
  <read_first>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts corrected Loop Clip ownership and rail-selection-line-only contracts; app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx individual cell click handler and PhysicsPaintLoopClipRail prop wiring; app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx Task 1 state matrix; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md UAT-02, UAT-11, PRE-FROZEN-PARENT-CHILD, EVIDENCE-FIELDS, and APPROVAL-FIELDS</read_first>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <behavior>
    - workflow wiring keeps `selectedRotoLoopClipIds` and `linkedRotoLoopClipIds` independent and passes both unchanged to the rail;
    - an individual physical-cell click still calls `onSelectRotoLoopClip(null)` before its existing physical selection/navigation path, while retained Action linkage may continue to supply `linkedRotoLoopClipIds`;
    - the workflow/rail contract does not clear the selected Action, move the cursor unexpectedly, alter linked logical-Group counts, or turn passive linkage into `aria-pressed` operation scope;
    - CSS source guards reject target-level linked spread/end-cap regressions while retaining selected orange, white segment endpoints, lifecycle dots, 18px frame pitch, 38px lane, and 161px strip geometry;
    - focused suites, typecheck, and production build complete without starting a server or changing test configuration.
  </behavior>
  <action>Add a focused workflow source/component regression that explicitly locks the diagnosed transition: physical-cell activation clears only Group selection at the strip seam, while selected and linked Group ID props remain independent inputs to `PhysicsPaintLoopClipRail`. Extend the rail CSS assertions to prevent reintroduction of target-level linked decoration and to preserve selected precedence, endpoint cuts, dots, accessibility, and geometry. Do not alter `PhysicsPaintWorkflowStrip.tsx`, `PhysicsPaintLoopClipRail.tsx`, Studio Signals, Action selection, cursor publication, counts, callbacks, or ARIA unless Task 1 RED produced concrete contradictory evidence; the expected production diff is `physicsPaintStudio.css` only. Run one-shot focused Vitest, typecheck, and build with the existing configuration. Do not run or start any development server. Keep Plan 43.2-17 blocked after automation; native evidence is a targeted retest, not phase approval.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts && pnpm --dir app run typecheck && pnpm build</automated></verify>
  <acceptance_criteria>Both focused suites, typecheck, and build exit 0; the workflow regression preserves the confirmed frame-versus-Group selection lifecycle, and the production diff remains CSS-only unless recorded RED evidence proves otherwise.</acceptance_criteria>
  <done>Automated coverage prevents the parasite target rectangle from returning while preserving every accepted Group/Action interaction boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| selection Signals→rail classes | Group operation scope and passive Action linkage are separate accepted facts and must not be visually conflated. |
| 12px interaction target→3px visible segment | Accessibility/focus geometry may be larger than the rail, but persistent linked/selected paint must stay on the visible segment. |
| automated CSS contract→native rendering | Source assertions can pass while platform compositing still creates a visible second rail; frozen native retest remains required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-24-01 | Tampering | Group versus Action selection presentation | medium | mitigate | Component state matrix proves linked-only is not selected and linked-plus-selected keeps orange operation scope. |
| T-43.2-24-02 | Tampering | rail target CSS | medium | mitigate | Negative source guards prohibit persistent linked decoration on the 12px target; linked treatment is segment-local. |
| T-43.2-24-03 | Denial of Service | interaction/accessibility target | low | mitigate | Preserve 12px pointer target, focus-visible outline, ARIA, callbacks, and keyboard behavior while changing only persistent paint. |
| T-43.2-24-04 | Tampering | native visual evidence | medium | mitigate | Require a fully restarted frozen-code parent/child comparison of individual-frame and whole-Group states before unblocking Plan 17. |
| T-43.2-SC | Tampering | packages | low | accept | No installs, dependency changes, or lockfile changes. |
</threat_model>

<verification>
<automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts && pnpm --dir app run typecheck && pnpm build</automated>
<human-check>Do not run a server. Keep Plan 43.2-17 blocked. From one fully restarted frozen-code parent/child native session per D-39, use the retained UAT fixture and compare the same Action-linked Group in two states: (1) click one individual physical frame inside the Group; verify the Action remains selected, the Group is not selected, the cursor/frame behavior and linked count stay unchanged, and only one subtle purple Motion or cyan Static 3px segment is visible with no bright partial rectangle, parallel line, or target-level vertical end-caps; (2) select the whole Group Rail; verify every fragment becomes one orange 3px segment while the Action remains selected, with no second rail; (3) inspect a Group start and end plus a lifecycle dot to confirm the legitimate white endpoint cuts, passive dot, focus treatment, and geometry remain intact. Record targeted evidence for UAT-11/UAT-02, but do not resume or approve Plan 43.2-17 until this retest passes.</human-check>
</verification>

<success_criteria>The bad target-level linked shadow is replaced by one segment-local mode-colored treatment, individual-frame and whole-Group states remain semantically distinct, selected orange wins, focus/endpoints/dots/geometry/Signals/Action/cursor/counts/accessibility are preserved, focused Vitest plus typecheck/build pass without a server, and Plan 43.2-17 remains blocked pending the targeted frozen-session native visual pass.</success_criteria>

<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-24-SUMMARY.md` when done; do not update UAT or unblock Plan 43.2-17 until the targeted native retest passes.</output>
