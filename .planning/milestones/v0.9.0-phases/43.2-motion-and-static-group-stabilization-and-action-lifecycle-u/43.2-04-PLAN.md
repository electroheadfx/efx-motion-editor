---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 04
type: execute
wave: 1
depends_on: [43.2-01, 43.2-02, 43.2-03]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/lib/physicPaintBridge.test.ts
autonomous: true
requirements: [GRP-02, GRP-03]
estimate: { tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Per D-03/D-06/D-11/D-35, one production-quality exact-frame Paint path crosses model, pure proposal, coordinator lease acquisition, parent validation, token-checked sole store replacement, one version notification, and one history command."
    - "Per D-35, stale or malformed impact is rejected without changing accepted document, version, or history."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts
    - app/src/lib/physicPaintBridge.ts
  key_links:
    - "exact frame -> pure COW proposal -> coordinator final preflight/lease -> bridge recomputation -> token-checked replaceRotoPhysicalDocument -> one version/history settlement"
---
<objective>
Implement the first authority-changing production work as an end-to-end exact-occurrence Group Paint tracer.

Purpose: Prove GRP-02/03 architecture after all Wave 0 contracts, implementing D-03, D-06, D-07, D-09, D-11, D-35, and D-37.
Output: One retained accepted COW path with exact settlement and fail-closed rejection.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-01-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-02-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md</context>

## Artifacts this phase produces

A production COW walking path that remains the skeleton for every later Group lifecycle operation.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish the pure exact-occurrence proposal slice</name>
  <read_first>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts symbols PhysicPaintRotoLoopClip and parsePhysicPaintRotoPhysicalDocument; app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts Wave 0 exact-frame COW and field-participation fixtures; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-RESEARCH.md Open Questions (RESOLVED) item 2</read_first>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts, app/src/components/physic-paint/roto/physicsPaintRotoGroupLifecycle.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts</files>
  <behavior>
    - a repeated occurrence receives the minimal unique real-key override required before pixel mutation while other repetitions and Groups remain byte-equivalent;
    - the exact-occurrence helper returns a frozen proposal plus explicit semantic impact for the coordinator/bridge tracer;
    - proposal construction owns no store, Signal, version, history, cache, selection, rendering, filesystem, DOM, lease, or authority publication state;
    - malformed ranges, duplicate overrides, ambiguous source sharing, unresolved precedence, and cleanup/reference mismatch reject before proposal acceptance.
  </behavior>
  <action>Convert only the Wave 0 model/proposal cases needed by the tracer to accepted behavior. Preserve internal identifiers per D-02, isolate the exact frame per D-06/D-07/D-09, and implement `physicsPaintRotoGroupLifecycle.ts` as a pure copy-on-write proposal/validation helper. Return the complete frozen candidate and exact semantic impact needed for later authority recomputation, but do not acquire a lease, call the bridge/store, emit `physicPaintVersion`, or insert history in this task.</action>
  <reversibility rating="costly">Exact-frame rather than cycle-phase Paint is persisted behavior per D-06.</reversibility>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts</automated></verify>
  <acceptance_criteria>The focused model suite passes for one Modified override, byte-equivalent unaffected occurrences/Groups, a frozen proposal/impact, and every named malformed or ambiguous proposal rejection; no authority publication path is introduced.</acceptance_criteria>
  <done>The production-quality pure proposal required by the tracer is accepted and independently verified.</done>
</task>
<task type="tracer" tdd="true">
  <name>Task 2: Publish one leased exact-occurrence Paint and preserve rejection evidence</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts physical transaction settlement; app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts Wave 0 lease/rejection ledger; app/src/lib/physicPaintBridge.ts replace-roto-physical-map handler; app/src/lib/physicPaintBridge.test.ts parent semantic-recomputation and accepted-event ledger; app/src/stores/physicPaintStore.ts replaceRotoPhysicalDocument and physicPaintVersion; .planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-CONTEXT.md D-03/D-11/D-35</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts, app/src/stores/physicPaintStore.ts</files>
  <behavior>
    - coordinator acquires one project/layer physical-operation lease before final preflight, records revision/hash N, and propagates the matching token;
    - bridge independently recomputes the exact candidate/impact and rechecks project, layer, launch, revision, hash, ranges, identity, sharing, precedence, and cleanup authority;
    - `replaceRotoPhysicalDocument` is the sole token-checked store replacement and rejects missing, mismatched, stale, or replayed publication while the tracer lease is active;
    - accepted settlement replaces once, emits exactly one `physicPaintVersion` notification, inserts exactly one history command, and releases only after settlement;
    - every rejection leaves document, version, history, selection, cursor, and canvas unchanged.
  </behavior>
  <action>Wire Task 1's pure proposal through the first production-quality authority-changing tracer per D-03/D-11/D-35. Acquire before the final live snapshot, perform the parent authority and semantic recomputation, propagate the token into the sole store replacement, and settle exactly once. Preserve exact rejection evidence for stale revision/hash, malformed ranges, duplicate overrides, ambiguous sharing, unresolved precedence, cleanup/reference mismatch, changed operation payload, and missing/mismatched/replayed token. Do not broaden the operation allowlist to Delete or Regenerate.</action>
  <verify><automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/lib/physicPaintBridge.test.ts</automated></verify>
  <acceptance_criteria>The coordinator and bridge suites pass one pure-proposal-to-accepted-publication path with token-checked sole replacement, one version, one history command, and unchanged accepted state for every named rejection.</acceptance_criteria>
  <done>A real leased exact-occurrence Group Paint transaction works end to end and fails closed.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| child proposal→parent bridge | Complete candidate and claimed impact are untrusted. |
| parent candidate→store | Only validated exact replacement may publish. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-01 | Tampering | bridge/model | high | mitigate | ASVS L1 closed parser, expected authorities, semantic recomputation, exact rejection tests. |
| T-43.2-02 | Tampering | COW ownership | high | mitigate | Pre-mutation COW and byte-isolation evidence. |
| T-43.2-SC | Tampering | packages | low | accept | No installs. |
</threat_model>
<verification>`pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoLoopClips.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/lib/physicPaintBridge.test.ts` exits 0 as the read-only aggregate after Tasks 1–2; then `pnpm --dir app run typecheck`.</verification>
<success_criteria>Plan 04 contains the first authority-changing production tracer, with one accepted publication and complete fail-closed evidence after the independently passing pure proposal slice.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-04-SUMMARY.md` when done.</output>
