---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 22
type: execute
wave: 18
depends_on: [43.2-06, 43.2-14]
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
autonomous: true
gap_closure: true
requirements: [GRP-02, GRP-04, GRP-07, GRP-08]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Per D-03/D-07/D-37, Group-scoped Apply Key Spacing at values 2 and 3 preserves loopId, ordered sourceKeyIds, repeat 3, placement, mode, Action provenance, source sharing, and physical-cell/Group-Rail alignment."
    - "Per D-03/D-14/D-36/D-37, retiming a finite source cycle rebuilds phaseOrigin, originalEndExclusive, and one contiguous visible range from the post-spacing cycle length and repeat, so derivation exposes the complete three-cycle extent with no partial cycle."
    - "Per D-11/D-35, the accepted spacing mutation settles records and lifecycle together as one history command, and Undo/Redo restores both exact states without duplicate authority or settlement."
    - "Plan 43.2-17 remains blocked until the targeted fully restarted native retest confirms Repeat 3 preservation and rail/cell alignment per D-39."
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts
  key_links:
    - "Group Rail selection -> useRotoTimelineActions.applyForceSpacing -> resolvePhysicPaintRotoPhysicalEdit -> lifecycle-complete nextLoopClips -> derivePhysicPaintRotoLoopRanges -> projectPhysicsPaintLoopClipGeometry"
    - "resolver proposal records plus lifecycle -> physical coordinator accepted output -> one history entry -> exact Undo/Redo snapshots"
---
<objective>
Close the confirmed Group-scoped Key Spacing lifecycle-retiming gap without widening the production surface.

Purpose: Preserve Repeat 3 Groups and exact rail/cell geometry when source keys are retimed, including the anchored-first-key case where placement does not move, per D-03, D-07, D-11, D-14, D-35, D-36, D-37, and D-39.
Output: A lifecycle-aware resolver correction, end-to-end RED regression coverage for spacing 2 and 3, atomic history evidence, and a targeted native retest contract while Plan 43.2-17 remains blocked.
</objective>
<execution_context>@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-06-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-14-SUMMARY.md
@.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-UAT.md
@app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
@app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
@app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts</context>

## Gap diagnosis

`computeSourceAttachedLoopPlacementFollow` currently updates only `placementStart` and returns `null` when the first source key stays anchored. Group-scoped Apply Key Spacing retimes later source keys, changing the source-cycle length while leaving stale `phaseOrigin`, `originalEndExclusive`, and `visibleRanges` untouched. `derivePhysicPaintRotoLoopRanges` correctly treats those lifecycle fields as canonical authority, so Repeat 3 is clipped to the old extent and the Group Rail no longer matches physical cells.

## Multi-Source Coverage Audit

| Source | ID | Gap-relevant contract | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable Motion/Static Groups retain complete lifecycle and aligned accepted presentation after Group operations. | 43.2-22 | COVERED | Fixes the rejected Group-scoped spacing path while leaving Plan 17 blocked for native approval. |
| REQ | GRP-02 | Canonical Group lifecycle, phase, ranges, provenance, and sharing remain complete after retiming. | 43.2-22 | COVERED | Resolver rebuilds lifecycle authority from post-spacing source timing. |
| REQ | GRP-04 | Group mutation is atomic and exactly Undoable/Redoable. | 43.2-22 | COVERED | Coordinator/history regression proves one command with exact before/after snapshots. |
| REQ | GRP-07 | Group Rail geometry and physical cells stay aligned. | 43.2-22 | COVERED | End-to-end regression derives ranges and projects geometry from the dispatched spacing proposal. |
| REQ | GRP-08 | Native lifecycle UAT remains the final oracle. | 43.2-22 | COVERED | Targeted frozen-session retest is required before Plan 17 can resume. |
| RESEARCH | — | Canonical document fields remain authority; derived rail geometry consumes accepted ranges and no second authority is introduced. | 43.2-22 | COVERED | Production is bounded to the physical resolver; UI/controller/history production remains unchanged. |
| CONTEXT | D-01..D-39 | Existing phase plans retain complete decision coverage; this closure specifically reinforces D-03, D-07, D-11, D-14, D-35, D-36, D-37, and D-39. | 43.2-22 | COVERED | Deferred D-05/D-38 features and Motion Editor production remain excluded. |

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Drive Group-scoped spacing through lifecycle derivation and rail geometry</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts linked source-position and rail-owned Force Spacing harnesses; app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts authorized linked spacing and derive helpers; app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts computeSourceAttachedLoopPlacementFollow, buildLinkedForceSpacingCandidate, and derivePhysicPaintRotoLoopRanges; app/src/components/physic-paint/view/physicsPaintLoopClipPresentation.ts projectPhysicsPaintLoopClipGeometry</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts</files>
  <behavior>
    - RED case: for a lifecycle-rich Motion Group with ordered source keys A/B/C, repeat 3, stable loopId, Action provenance, and shared-source facts, Group-scoped Apply Key Spacing values 2 and 3 dispatch exactly one force-spacing proposal;
    - the first source key remains anchored and placement remains unchanged, yet nextLoopClips is lifecycle-complete rather than null because the post-spacing cycle length changed;
    - loopId, sourceKeyIds order, repeat, placement, mode, motion/color settings, synchronization/provenance state, script reference, frame overrides, and sharing metadata remain byte-equivalent unless they are the lifecycle extent fields being recomputed;
    - post-spacing cycle lengths derive from the mapped source positions; originalEndExclusive equals phaseOrigin plus cycleLength times repeat, visibleRanges contains the one complete contiguous range, and every derived range reports partialCycle false;
    - projectPhysicsPaintLoopClipGeometry starts at the preserved placement and has width equal to the complete derived extent times the existing frame pitch for both spacing values.
  </behavior>
  <action>First add the parameterized end-to-end regression to `useRotoTimelineActions.test.ts`: invoke the public `applyForceSpacing` action from a selected Group Rail, capture the exact dispatched proposal, apply its mapping to the source records, feed `proposal.nextLoopClips` into `derivePhysicPaintRotoLoopRanges`, and project the resulting rail geometry. Run the focused test and retain the expected RED failure caused by stale lifecycle authority. Add a pure resolver-level companion case in `physicsPaintRotoLoopGuards.test.ts` that covers the anchored-first-key branch and a second Group sharing the same ordered source cycle without changing either logical identity or placement. Then update only `physicsPaintRotoPhysicalResolver.ts`: separate rigid placement-follow behavior from source-cycle retiming as needed, compute the post-mapping source timing for eligible finite Groups, and rebuild `phaseOrigin`, `originalEndExclusive`, and contiguous `visibleRanges` from the preserved/followed placement plus post-spacing `cycleLength * repeat`. Return a complete `nextLoopClips` collection whenever placement or any lifecycle extent field changes, including when the first key did not move. Preserve every unrelated field by immutable record copy, keep duplicated/shared Groups at their own placements, keep sourceKeyIds order stable, and do not add UI state, cache authority, compatibility migration, or changes outside the resolver production file per D-03/D-37.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts</automated>
  </verify>
  <acceptance_criteria>The RED regression fails before production correction and passes afterward for spacing 2 and 3; each Repeat 3 Group derives its full post-spacing extent, one contiguous visible range, exact phase, aligned rail geometry, preserved identity/provenance/sharing, and no partial cycle.</acceptance_criteria>
  <done>Group-scoped Key Spacing now carries lifecycle-complete canonical Groups from the public action path through range and rail projection per D-03/D-07/D-14/D-36/D-37.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove one-command settlement and exact Undo/Redo</name>
  <read_first>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts force-spacing harness, staged Loop Clip collection, acceptedOutput, rejection, and rollback cases; app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts Group lifecycle participation, operation deduplication, and exact lifecycle Undo/Redo; app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts force-spacing proposal shape</read_first>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</files>
  <behavior>
    - the coordinator stages mapped records and rebuilt lifecycle fields in the same replace-roto-physical-map payload and emits one accepted output only after matching parent acceptance;
    - the accepted force-spacing output creates exactly one history entry despite signal re-publication or repeated observation;
    - one Undo restores the original key timing and original lifecycle extent, and one Redo restores the spaced key timing and rebuilt complete Repeat 3 extent;
    - Undo/Redo preserve loopId, sourceKeyIds order, repeat, placement, phase, Action provenance, sharing, synchronization state, selection, and cursor while moving the history pointer once per accepted replay;
    - rejection or transport failure restores records and lifecycle together and creates no history command.
  </behavior>
  <action>Extend the existing coordinator force-spacing fixture with one lifecycle-rich Repeat 3 Group and assert that the resolver-produced `nextLoopClips` travels with the mapped records through staging, payload publication, accepted before/after snapshots, and rollback. Extend the history lifecycle suite with an accepted `force-spacing` output whose before/after snapshots use the exact resolver result from Task 1; assert one entry, exact Undo, exact Redo, and deduplication. Keep `useRotoTimelineActions.ts`, `useRotoPhysicalEditCoordinator.ts`, `useRotoPhysicalEditHistory.ts`, rail production, store production, bridge production, and test configuration unchanged; the confirmed root cause is resolver lifecycle reconstruction, so these tests must consume the existing canonical settlement path rather than creating another path per D-11/D-35.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts</automated>
  </verify>
  <acceptance_criteria>Focused suites prove lifecycle-complete spacing is one accepted physical/history command, rejection is atomic, and Undo/Redo restore exact original and retimed Repeat 3 states without duplicate settlement.</acceptance_criteria>
  <done>The spacing correction uses the existing canonical coordinator/history authority exactly once per accepted forward, Undo, and Redo operation per D-11/D-35.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Group selection/action -> resolver | Selected Group and spacing provenance must resolve to the exact canonical source cycle before retiming. |
| resolver proposal -> canonical settlement/history | Mapped records and rebuilt lifecycle authority must remain one indivisible accepted document transition. |
| canonical ranges -> Group Rail | Presentation must consume complete accepted lifecycle ranges without reconstructing an independent extent. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-43.2-22-01 | Tampering | source-cycle lifecycle retiming | high | mitigate | Derive cycle length from exact post-mapping ordered source identities and rebuild finite lifecycle fields immutably in the resolver. |
| T-43.2-22-02 | Tampering | coordinator/history settlement | high | mitigate | Pin records plus lifecycle in one payload, one accepted output, one history entry, and exact bidirectional replay tests. |
| T-43.2-22-03 | Denial of Service | repeat/range extent | medium | mitigate | Reuse existing capacity validation and compact range derivation; do not materialize repeated destinations. |
| T-43.2-22-SC | Tampering | packages | low | accept | No installs, dependency changes, or lockfile changes. |
</threat_model>

<verification>
<automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/useRotoTimelineActions.test.ts src/components/physic-paint/roto/physicsPaintRotoLoopGuards.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditHistory.test.ts && pnpm --dir app run typecheck && pnpm build</automated>
<human-check>Do not start a development server. After the automated gates pass, keep Plan 43.2-17 blocked and request a targeted native retest from a fully quit/restarted frozen-code parent and Physics Paint child per D-39: create or open a Motion Group with Repeat 3; apply Group-scoped Key Spacing 2 and verify all three complete cycles remain aligned with physical cells and the single Group Rail; Undo and Redo once and verify exact original/retimed restoration; repeat with Key Spacing 3 and verify placement, phase, provenance, sharing, lifecycle dot, rail endpoints, and Repeat 3 remain intact. Plan 17 may resume only after this targeted retest passes.</human-check>
</verification>
<success_criteria>Spacing 2 and 3 preserve complete Repeat 3 lifecycle authority, range derivation, physical-cell/rail alignment, provenance/sharing, and one-command Undo/Redo; focused tests, typecheck, and build pass; no server runs; Plan 43.2-17 remains blocked pending the required native result.</success_criteria>
<output>Create `.planning/phases/43.2-motion-and-static-group-stabilization-and-action-lifecycle-u/43.2-22-SUMMARY.md` when done.</output>
