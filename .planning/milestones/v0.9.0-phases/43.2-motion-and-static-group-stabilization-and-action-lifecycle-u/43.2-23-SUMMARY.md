---
phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u
plan: 23
subsystem: physics-paint-persistence
tags: [typescript, vitest, physics-paint, group-lifecycle, persistence, canonical-revision]

requires:
  - phase: 43.2-05
    provides: Finite pre-lifecycle Group hydration and lifecycle-complete canonical persistence
provides:
  - Exact historical fingerprint validation for supported finite five-field pre-43.2 Groups before lifecycle hydration
  - Public persistence-seam evidence that the literal historical document reopens as current canonical lifecycle authority
  - Stale-content rejection when persisted Group content changes while retaining its historical revision
  - Targeted copied-project native retest contract while Plan 43.2-17 remains blocked
affects: [43.2-17, group-persistence, save-reopen, canonical-revision, native-uat]

actuals:
  tokens: 2304
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - Validate an exact supported historical canonical shape and fingerprint before additive lifecycle hydration
    - Return only lifecycle-complete current canonical authority after historical validation succeeds
    - Exercise compatibility and stale rejection through the public sidecar persistence load seam

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts
    - app/src/lib/physicPaintPersistence.test.ts

key-decisions:
  - "Legacy compatibility is limited to finite Loop Clip records carrying exactly the five pre-43.2 members; partial lifecycle, provenance additions, infinity, malformed references, and changed content remain rejected."
  - "The persisted historical revision is compared with a fingerprint recomputed from the exact raw legacy content before lifecycle defaults are added; accepted output receives the current lifecycle-complete canonical revision."
  - "The retained user project fixture was not accessed or modified; native save/reopen approval remains pending on a copy and Plan 43.2-17 remains blocked."

patterns-established:
  - "Historical-before-current validation: strict legacy bytes/content are authenticated first, then normalized into current canonical authority."
  - "Compatibility is shape- and content-exact, never a revision allowlist, missing-field bypass, or catch-and-retry acceptance path."

requirements-completed: [GRP-02, GRP-08]

coverage:
  - id: D1
    description: "A literal finite five-field pre-43.2 Group with historical revision physical-163-beed993b loads through sidecar persistence and becomes a lifecycle-complete document with current revision physical-225-6e46481e."
    requirement: GRP-02
    verification:
      - kind: integration
        ref: "app/src/lib/physicPaintPersistence.test.ts#loads a literal pre-43.2 finite Group through the persistence seam"
        status: pass
    human_judgment: false
  - id: D2
    description: "Changing only legacy Group repeat from 2 to 3 while retaining physical-163-beed993b rejects as a canonical revision mismatch through the public persistence load seam."
    requirement: GRP-08
    verification:
      - kind: integration
        ref: "app/src/lib/physicPaintPersistence.test.ts#rejects changed legacy Group content that retains the historical revision"
        status: pass
    human_judgment: false
  - id: D3
    description: "A copied supported project opens in one fully restarted frozen-code parent and Physics Paint child session without canonical revision mismatch while the retained original stays untouched."
    requirement: GRP-08
    verification: []
    human_judgment: true
    rationale: "Native fixture validation requires the user-owned frozen application session; execution intentionally did not access any project fixture or start a server. This targeted check does not unblock Plan 43.2-17."

duration: 5min
completed: 2026-08-11
status: complete
---

# Phase 43.2 Plan 23: Exact Legacy Group Persistence Summary

**Supported finite pre-43.2 Groups now authenticate against their exact historical fingerprint before lifecycle hydration, while changed content retaining the old revision fails closed.**

## Performance

- **Duration:** 5 min across the approved tracer continuation
- **Started:** 2026-08-11T17:46:57Z
- **Completed:** 2026-08-11T17:51:35Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Loaded a literal persisted five-field finite Group with historical revision `physical-163-beed993b` through `loadPhysicPaintData`, including its real-key PNG sidecar, and returned complete synchronized/attached lifecycle authority at current revision `physical-225-6e46481e`.
- Added an exact legacy fingerprint gate that preserves raw pre-hydration Group content long enough to validate it before lifecycle defaults are added.
- Proved that changing only `repeat` from 2 to 3 while retaining the historical revision rejects with `PhysicPaintRotoPhysicalDocument: canonical revision mismatch.`
- Kept current lifecycle-complete documents, malformed partial lifecycle rejection, typecheck, and production build green without changing persistence production code, project schemas, test configuration, or dependencies.
- Did not access or modify any user project fixture, did not start a server, and kept Plan 43.2-17 blocked pending its existing targeted native retests.

## Task Commits

1. **Task 1: Load one literal pre-43.2 finite Group through persistence**
   - `bb38fda3` — `test(43.2-23): capture pre-lifecycle persistence mismatch` (RED)
   - `c31456b8` — `fix(43.2-23): validate legacy Group revision before hydration` (GREEN)
2. **Task 2: Prove legacy validation remains content-exact and run closure gates**
   - `0ea7449d` — `test(43.2-23): reject stale legacy Group content`

Task 2 intentionally required no additional production change: the exact raw-content fingerprint gate added by Task 1 already rejected the changed-repeat fixture through the public persistence seam.

## Files Created/Modified

- `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts` — Recognizes only exact finite five-field legacy Group records, recomputes their historical canonical fingerprint before hydration, and returns the normal lifecycle-complete current document only after an exact match.
- `app/src/lib/physicPaintPersistence.test.ts` — Covers literal historical save/reopen success and adjacent changed-repeat stale-revision rejection through real sidecar hydration.

## Decisions Made

- The compatibility path accepts only non-empty collections of finite Group records carrying exactly `loopId`, `placementStart`, `sourceKeyIds`, `repeat`, and `mode`. It does not accept infinity records, partial lifecycle records, provenance additions, unknown keys, or malformed references.
- Historical validation reuses the existing strict record ordering, interpolation, incoming-break, length-prefix encoding, canonical Loop Clip encoding, and FNV fingerprint rules. No hardcoded revision allowlist or schema-wide stale bypass was introduced.
- Lifecycle defaults are added only after the exact historical revision matches; callers receive the current canonical revision and complete lifecycle fields.
- Native fixture approval remains separate. The retained project stays untouched, any future check must open only a copy after a full frozen parent/child restart, and Plan 43.2-17 remains blocked regardless of this isolated correction.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Tracking Bug] Corrected progress metadata after the state handler wrote the completed-plan count into the percentage field**
- **Found during:** Final tracking update
- **Issue:** `state.update-progress` correctly counted 63 completed plans and rendered the 97% progress bar, but wrote `percent: 63` in STATE frontmatter.
- **Fix:** Restored the frontmatter percentage to 97 and updated Plan 23 activity text while preserving Plan 17 as the blocked current position.
- **Files modified:** `.planning/STATE.md`
- **Verification:** STATE reports 63/65 completed plans, `percent: 97`, `Plan: 17 of 24`, and the existing blocked status.
- **Committed in:** Final plan metadata commit

---

**Total deviations:** 1 auto-fixed (1 tracking bug)
**Impact on plan:** Tracking-only correction; implementation scope and Plan 17's blocking boundary are unchanged. No persistence seam implementation, transport/project schema, store/publication path, user fixture, server, dependency, lockfile, test configuration, Motion Editor production, or other gap scope changed.

## Issues Encountered

- The Task 2 changed-repeat characterization passed on its first focused run because Task 1's exact historical fingerprint implementation already enforced the required stale-content rejection. No second production correction was necessary.
- The production build emitted existing Vite CJS API deprecation and dynamic/static import warnings. The build completed successfully, and Plan 23 did not change the warning-producing surfaces.

## Verification

- Orchestrator tracer rerun: `pnpm --dir app exec vitest run src/lib/physicPaintPersistence.test.ts -t "pre-43.2 finite Group"` — 1 focused test passed with the exact historical/current revision literals.
- Task 2 focused stale check: `pnpm --dir app exec vitest run src/lib/physicPaintPersistence.test.ts -t "rejects changed legacy Group content"` — 1 test passed; 19 skipped by filter.
- Complete persistence suite: `pnpm --dir app exec vitest run src/lib/physicPaintPersistence.test.ts` — 1 file passed; 20 tests passed.
- Typecheck: `pnpm --dir app run typecheck` — passed (`tsc --noEmit`).
- Production build: `pnpm build` — passed for `@efxlab/efx-physic-paint` and `efx-motion-editor`.
- `git diff --check` — passed before the Task 2 commit.
- No development server was started.

## Known Stubs

None. Existing `loop-placeholder` references describe the intentional unresolved-Group domain variant and are not implementation stubs. No TODO, FIXME, skipped test, or test todo was introduced.

## Threat Model Results

- T-43.2-01 is mitigated by recomputing the exact historical fingerprint from the validated raw finite five-field Group collection and requiring equality before compatibility acceptance.
- T-43.2-02 is mitigated by adding lifecycle defaults only after the historical match and returning the newly computed current canonical revision.
- T-43.2-03 remains respected: no user project fixture was accessed or modified. Native verification is explicitly restricted to a copy in a fully restarted frozen-code session.
- No new endpoint, authentication path, network surface, package, schema boundary, or filesystem access pattern was introduced.

## User Setup Required

None - no external service configuration is required. The targeted native copied-project check remains user-owned UAT rather than setup.

## Next Phase Readiness

- Automated Plan 23 closure gates are complete and the isolated save/reopen correction is ready for targeted native verification.
- For that targeted check, preserve the supported original project unchanged, duplicate it outside the application, fully quit and restart the frozen-code parent and Physics Paint child, and open only the copy. Confirm it opens without canonical revision mismatch and presents lifecycle-complete Groups.
- This targeted save/reopen result is not native fixture approval for the broader phase. Plan 43.2-17 and phase completion remain blocked pending the existing fully restarted Delete Frame/Delete Group and Repeat 3 Key Spacing retests.

## Self-Check: PASSED

- Both owned implementation/test files and this summary exist on disk.
- Commits `bb38fda3`, `c31456b8`, and `0ea7449d` exist in repository history.
- Focused and complete persistence tests, TypeScript typecheck, production build, and diff whitespace verification pass.
- The retained user fixture was not accessed or modified, no server was started, and Plan 43.2-17 remains blocked.

---
*Phase: 43.2-motion-and-static-group-stabilization-and-action-lifecycle-u*
*Completed: 2026-08-11*
