---
phase: 43.3-motion-and-static-group-drag-within-free-space
plan: 04
type: execute
wave: 4
depends_on:
  - 43.3-03
files_modified: []
autonomous: false
requirements:
  - GDRAG-09
  - GDRAG-10

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The full automated suite, typecheck, and production build are green with zero modifications to pre-existing test expectations"
    - "Existing key drag, rigid physical-key group drag, Key Spacing, Group Rail selection, inspector, Edit Group, Regenerate, synchronization status, linked-source sharing, and preview/export parity are byte-unchanged in behavior (D-15 regression boundary)"
    - "The phase diff does not touch the 43.2 frozen repair paths: project-local save staging, Paint into a deleted Group gap, or original-extent Regenerate (CONTEXT carryover caution, Pitfall 8)"
    - "Key Group rails (Phase 43.4), Push Right/Push Left (Phase 43.5), internal multi-track Paint, and Motion Editor timeline changes remain out of scope and untouched (D-20)"
    - "The user has natively approved the 11-step product acceptance list plus the 10-row UI-SPEC Visual Acceptance Matrix from a fully restarted frozen-code parent/child session (43.2 D-39)"
  artifacts:
    - ".planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UAT.md records the executed native acceptance steps and evidence"
  key_links:
    - "full-suite gate + native UAT approval: the phase is automated-ready only until the user's frozen-session native approval lands (project rule: do not claim done before UAT)"
---

<objective>
Prove the regression boundary and obtain native user acceptance: full automated gates, explicit non-modification proofs for the locked drag/lifecycle paths and the 43.2 frozen repair surface, then the blocking native visual UAT covering the 11-step product acceptance list and the UI-SPEC matrix.

Purpose: GDRAG-09 makes byte-unchanged behavior of the existing gestures a first-class deliverable; GDRAG-10 makes the user the final oracle from a fully restarted frozen-code session.
Output: Green gates, recorded UAT evidence, and an approved phase ready for verification.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UI-SPEC.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-01-SUMMARY.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-02-SUMMARY.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression gate plus locked-boundary and frozen-path non-modification proofs</name>
  <files>.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UAT.md</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (locked regions: move-key-group dispatch, buildMoveGroupCandidate, computeSourceAttachedLoopPlacementFollow)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (locked region: physical-key drag session, lines 1107-1322)
    - .planning/STATE.md (Blockers/Concerns: the exact 43.2 frozen repair paths that must stay untouched)
  </read_first>
  <action>Run the full automated gate set per VALIDATION.md: pnpm --dir app exec vitest run (full suite, never watch mode), pnpm --dir app typecheck, pnpm --dir app build. Then produce the non-modification proofs against the phase base commit (the commit before 43.3-01 landed): (a) git diff shows zero changes inside the move-key-group resolver dispatch branch, buildMoveGroupCandidate, computeSourceAttachedLoopPlacementFollow, the strip physical-key drag session (PhysicsPaintWorkflowStrip.tsx lines ~1107-1322, excluding additive Group-drag gap-preview wiring), buildProposalVersion and its call sites, runPhysicalAction and its kind Extract, and PhysicsPaintStudio.tsx; (b) git diff shows zero changes to the 43.2 frozen repair surface — project-local save staging (cache/.physic-paint-staging paths and Tauri allow-exists scope), Paint-into-a-deleted-Group-gap handling, and original-extent Regenerate logic; any incidental discovery in those areas routes to a new quick task, never this phase (Pitfall 8); (c) git diff on test files shows only ADDED describe/test blocks — zero modified or deleted pre-existing expectations. Record the gate outputs and the three proofs in 43.3-UAT.md as the automated section. Do not run the dev server (the user runs it); do not use MCP Chrome DevTools. The phase is automated-ready after this task, not done — native UAT is Task 2.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run && pnpm --dir app typecheck && pnpm --dir app build</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest suite exits 0; typecheck exits 0; production build exits 0
    - 43.3-UAT.md records the gate outputs and the three non-modification proofs (locked move-key-group/strip/buildProposalVersion regions, 43.2 frozen repair surface, test-file additions-only)
    - git diff proof commands and their outputs are quoted in 43.3-UAT.md
  </acceptance_criteria>
  <done>All automated gates green with recorded proof that locked regression boundaries and the 43.2 frozen repair paths are untouched; phase is automated-ready (GDRAG-09).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking native visual UAT — 11-step product acceptance plus UI-SPEC matrix from a restarted frozen-code session</name>
  <what-built>The complete Group Rail drag feature: threshold-disambiguated rail drag gesture, translucent mode-colored ghost, clamped blocked-edge bar, neutral-gray gap preview, clamp-and-commit free-space movement for source-attached and duplicated placements, 43.1-semantic persistent gap recording, mapped rejection/acceptance status copy, atomic one-Undo/one-Redo transactions, and post-commit selection/cursor stability — with all locked regression boundaries proven untouched.</what-built>
  <how-to-verify>
    Freeze the implementation commit, then fully restart the parent and child (frozen-code session per 43.2 D-39 — never an active hot-reload state). Execute the 11-step product acceptance list from SPECS/milestone-v0.9.0-new-phases/phase-43.3-group-drag-free-space-prompt.md plus the 10-row Visual Acceptance Matrix in 43.3-UI-SPEC.md:
    1. Drag a source-attached Motion Group into free space — complete source cycle, rail, linked repeat interval, placement, boundaries, and derived preview move together; neighbors never move
    2. Drag a source-attached Static Group — same whole-Group movement
    3. Threshold disambiguation — click, Shift-click, Cmd-click, double-click Edit Group behave exactly as before; drag starts only past ~4px
    4. Preview clamps at neighboring content, frame 0, and capacity/parent end with the red blocked-edge bar visible before drop; drop commits the clamped preview — never overlaps, crosses, reorders, stretches, splits, cuts, or changes Repeat/Infinity
    5. Drag a duplicated shared-source placement — only that placement and its linked interval move; shared source keys never move, clone, or retime
    6. Vacated and newly opened intervals persist as intentional gaps; Interpolation On never bridges them; gaps survive Interpolation Off/On
    7. Group-local deleted phases and fragments travel rigidly with the Group; synchronization status never flips spuriously
    8. Zero-free-space drag publishes 'No empty space in that direction.' with zero mutation — no modal, no sound, no silent snap-back
    9. Accepted move publishes 'Moved Group to frame {N}.' plus 'Gap left at frames {A}–{B}.' when a gap opened; Escape mid-drag restores the exact pre-drag view with no history entry
    10. One Undo restores the pre-drag document (placement and movement-created breaks together); one Redo reapplies it; save/reopen reproduces placement, gaps, and breaks
    11. Post-commit stability — moved Group stays selected, cursor unmoved, no automatic navigation; canvas reconciles when the current frame's content changed
    Include an Infinity-Group rightward drag row (derived occurrences re-derive; Repeat unchanged) so the RESEARCH Open Question 1 resolution receives explicit native judgment. Record pass/fail evidence per step in 43.3-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" with the completed 43.3-UAT.md evidence, or describe the failing rows for gap-closure replanning</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted feature -> locked prior behavior | New gesture must not alter the locked drag/lifecycle/regression surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.3-04-01 | Tampering (integrity) | Locked regression boundaries (move-key-group, rail click path, 43.2 frozen surface) | high | mitigate | Task 1 non-modification proofs recorded in 43.3-UAT.md; full-suite gate; additions-only test diff rule |
| T-43.3-04-02 | Repudiation | Acceptance evidence | medium | mitigate | Frozen-commit, fully restarted session, per-step recorded evidence; no "done" claim before native approval (project rule) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run && pnpm --dir app typecheck && pnpm --dir app build — all exit 0
- 43.3-UAT.md contains the automated gate record, the three non-modification proofs, and the user's per-step native evidence
- User approval signal received on the blocking checkpoint
</verification>

<success_criteria>
GDRAG-09 proven by green gates plus recorded non-modification proofs; GDRAG-10 satisfied by the user's native approval of the 11-step acceptance list and UI-SPEC matrix from a fully restarted frozen-code session. The phase then proceeds to /gsd-verify-work.
</success_criteria>

## Artifacts this phase produces (this plan)

- `.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UAT.md` (automated gate record, non-modification proofs, native acceptance evidence)
- No production symbols — this plan verifies only

<output>
Create `.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-04-SUMMARY.md` when done
</output>
