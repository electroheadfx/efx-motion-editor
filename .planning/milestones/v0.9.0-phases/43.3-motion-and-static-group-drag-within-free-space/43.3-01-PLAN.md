---
phase: 43.3-motion-and-static-group-drag-within-free-space
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - GDRAG-01
  - GDRAG-03
  - GDRAG-07

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new 'move-group' intent carrying loopId and destinationPlacementStart round-trips through the strict parser and canonical serializer and joins the closed operation-kind union (D-14)"
    - "Resolving 'move-group' for a source-attached Group with free destination space rigidly translates every source key and the Group lifecycle fields (phaseOrigin, originalEndExclusive, every visibleRanges entry, every frameOverrides appFrame) by the same signed delta (D-03/GDRAG-03, D-13)"
    - "prepareRotoGroupDrag/commitRotoGroupDrag retain an immutable publication whose proposalVersion fingerprint includes the incoming break collection, and commit forwards the retained objects to executePhysicalEdit with zero recomputation (GDRAG-07, RESEARCH Pitfall 1)"
    - "Pointer drag on the 12px rail target past a 4px horizontal threshold starts a drag session with pointer capture and a paint-only mode-colored ghost at 55% opacity at the proposed destination; the original rail stays fully visible in place (D-01, D-04)"
    - "UI-SPEC G2 populated: the ghost renders the dragged Group's exact effective interval geometry at 55% mode-color opacity, clipped at the shared viewport"
    - "UI-SPEC G2 overflow: the ghost clips at the shared horizontal viewport and reappears on scroll; it never wraps, stacks, or adds a scrollbar"
    - "UI-SPEC G1 empty: with zero Groups the rail stays absent from the DOM and no drag affordance exists (unchanged behavior, regression-verified)"
    - "Escape during a session cancels with zero state change and zero history entry; plain click, Shift/Cmd multi-select, and double-click Edit Group behave exactly as before (D-04)"
  artifacts:
    - "app/src/types/physicPaint.ts contains the 'move-group' union member, hasOnlyKeys parser branch, serializer case, and operation-kind validator entries"
    - "app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts contains the 'move-group' dispatch branch before the terminal fail-closed guard"
    - "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts contains prepareRotoGroupDrag and commitRotoGroupDrag"
    - "app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts exists as the focused gesture-session hook module"
    - "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx arms the drag session from the rail target pointer-down and hosts the ghost element"
  key_links:
    - "types union member -> resolver dispatch branch: the terminal exhaustive guard must not fire for 'move-group' (compile + runtime proof)"
    - "prepare -> resolver -> retained publication -> commit -> executePhysicalEdit: one atomic parent-acknowledged transaction path (GDRAG-07)"
    - "rail pointer-down -> session hook -> prepare/commit pair: the gesture reaches the canonical edit path without touching PhysicsPaintStudio.tsx"
---

<objective>
Establish the end-to-end Group-drag slice: a new dedicated `move-group` physical-edit intent, its resolver branch for the source-attached happy path, the prepare/commit publication pair, and the rail-side drag session with the unclamped ghost — one real path from rail pointer-down to accepted atomic commit.

Purpose: Tracer-first decomposition (TRACER_MODE=true) proves the closed-union extension, the publication idiom, and the rail gesture compose correctly before clamp/break/preview expansion builds on them. TDD mode is enabled: the tracer starts red with failing transport/resolver tests, and the hooks task carries explicit test expectations before implementation.
Output: Working source-attached Group drag on free space (no clamp, no gap preview, no status copy yet — those are plans 02/03), all automated gates green.

Planning notes: spec-less probe fallback was skipped visibly at plan-phase init (phase has no requirement IDs in REQUIREMENTS.md; GDRAG-01..GDRAG-10 are planner-adopted from RESEARCH.md). No external API integration (detector negative); no assumption-delta signal (detector negative); no ORM schema files in scope.

Scope note (advisory, per ADR-2629 smart-zone budget): this plan rides at ~80% of the smart-zone budget (3 tasks, 9 files, uncalibrated). No re-slice is required up front; however, if execution shows context pressure after Task 2 completes, Task 3 (rail session hook + ghost) MUST be split into a follow-on plan rather than degrading quality mid-plan.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-RESEARCH.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-PATTERNS.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end 'move-group' intent — transport union member plus resolver source-attached happy path (one path only)</name>
  <files>app/src/types/physicPaint.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts, app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</files>
  <read_first>
    - app/src/types/physicPaint.ts (lines 360-431 intent union, 503-617 parser/serializer, 655-676 operation-kind union, 934-966 validators — the exact seams to extend)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (lines 276-301 failure codes, 754-919 placement-follow helper, 1649-1771 buildMoveGroupCandidate, 2985-3021 dispatch branches)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (lines 256-310 Loop Clip lifecycle field shapes)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-PATTERNS.md (verbatim analog excerpts for every seam above)
  </read_first>
  <behavior>
    - Test 1 (transport round-trip): an intent { kind: 'move-group', loopId, destinationPlacementStart } parses via isPhysicPaintRotoPhysicalEditIntent, rejects payloads with excess or missing keys, and serializes canonically through serializePhysicPaintRotoPhysicalEditIntent; 'move-group' passes isPhysicPaintRotoPhysicalEditOperationKind and isPhysicPaintRotoOrdinaryOperationKind
    - Test 2 (resolver happy path, source-attached): given a Group whose placementStart equals its first source key frame, resolving 'move-group' to a free destination returns ok with a mapping translating every sourceKeyIds member by delta = destinationPlacementStart - placementStart, and nextLoopClips carrying placementStart = destinationPlacementStart plus phaseOrigin, originalEndExclusive, every visibleRanges entry, and every frameOverrides appFrame translated by the same delta with relative offsets preserved verbatim (D-13)
    - Test 3 (fail-closed): unknown loopId, excess intent keys, or destination translating any key outside capacity returns a failure resolution carrying no proposal
    - Test 4 (regression): all pre-existing resolver and transport tests pass unmodified
  </behavior>
  <action>RED first: add the failing tests from the behavior block (extend the existing "transport-safe physical edit intent tracer" block near resolver test line 129 and add a new move-group describe block). Then implement per D-14. In types/physicPaint.ts: add a union member with readonly kind 'move-group', readonly loopId string, readonly destinationPlacementStart number; add an explicit hasOnlyKeys(value, ['kind','loopId','destinationPlacementStart']) parser branch validating loopId with the same bounded-id discipline as neighboring kinds and destinationPlacementStart as a bounded non-negative integer frame; add the exhaustive serializer case; add 'move-group' to PhysicPaintRotoPhysicalEditOperationKind and both kind validators. In the resolver add a dispatch branch before the terminal fail-closed guard: look up the clip by loopId fail-closed (unknown or ambiguous loopId -> fail with an existing suitable code or one new closed-union code such as 'malformed-identity'); determine source attachment resolver-side as clip.placementStart === identities.framesByKeyId.get(clip.sourceKeyIds[0]) (the verified coincidence test at resolver lines 875-878; never a UI-supplied flag, Pitfall 4); compute delta = intent.destinationPlacementStart - clip.placementStart; build the mapping as a rigid translation of every sourceKeyIds member by delta reusing the buildMoveGroupCandidate capacity and duplicate-destination guard algebra (lines 1700-1734); build nextLoopClips by setting the moved clip placementStart and translating phaseOrigin, originalEndExclusive, EVERY visibleRanges entry, and EVERY frameOverrides appFrame by the same delta — never rebuild visibleRanges as a single full range (Pitfall 6; the single-range rebuild at lines 905-909 belongs to Key Spacing retime and must not be copied); finish through the unchanged finalizeProposal common finalizer. Do NOT implement clamping, collision handling, break derivation, or the duplicated placement-only branch in this task (plan 02 owns them); for this tracer the destination is assumed free and the branch may fail-closed on any collision until plan 02 replaces that with clamp-and-commit. Do NOT modify the move-key-group branch, its candidate builder, or any existing parser/serializer case (D-15 regression boundary).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The four behavior tests pass; the new move-group describe block and transport round-trip assertions exist in physicsPaintRotoPhysicalResolver.test.ts
    - types/physicPaint.ts contains the literal kind 'move-group' in the intent union, one hasOnlyKeys branch, one serializer case, and both validator lists
    - physicsPaintRotoPhysicalResolver.ts dispatches intent.kind === 'move-group' before the terminal malformed-target guard
    - pnpm --dir app typecheck exits 0 (the mapped RotoOrdinaryPhysicalEditExecuteInput type couples the new kind without edits to rotoCoordinatorPorts.ts)
    - git diff shows zero changes inside pre-existing move-key-group test blocks and zero changes to buildMoveGroupCandidate
  </acceptance_criteria>
  <done>The 'move-group' intent parses, serializes, and resolves a source-attached free-space move end-to-end through the pure resolver with lifecycle fields translated rigidly; all existing suites stay green unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prepareRotoGroupDrag / commitRotoGroupDrag publication pair with break-aware proposalVersion</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 129-149 mapper precedent, 519-522 bounded runner Extract, 1110-1236 prepareRotoKeyGroupDrag/commitRotoKeyGroupDrag — the byte-close model, 1398-1406 buildProposalVersion break-omission site)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalModel.ts (lines 947-986 revision builder whose 4th parameter defaults to empty breaks)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts (existing prepare/commit harness shape)
  </read_first>
  <behavior>
    - Test 1 (guard order): prepare rejects with a product reason when launch is absent, ports are absent, or an operation is in flight — same order as prepareRotoKeyGroupDrag
    - Test 2 (resolver input + fingerprint): prepare threads input.getIncomingInterpolationBreakKeyIds?.() into BOTH the resolver input and a break-aware revision fingerprint; two preparations identical except break authority produce different proposalVersions (Pitfall 1)
    - Test 3 (no-change rejection): a drag whose resolver result is a no-op is rejected at prepare with no publication
    - Test 4 (commit coherence): commit returns false on kind/intent mismatch or empty launch tuple, and on success forwards exactly the retained { proposal, expectedLaunch, operationKind: 'move-group', intent, selectedKeyId, selectedAppFrame } to executePhysicalEdit with no recomputation
    - Test 5 (regression): existing prepare/commit tests for move-key and move-key-group pass unmodified; buildProposalVersion's existing call sites are untouched
  </behavior>
  <action>Model the new pair byte-closely on prepareRotoKeyGroupDrag/commitRotoKeyGroupDrag (useRotoTimelineActions.ts lines 1110-1236) per the PATTERNS.md excerpts. prepareRotoGroupDrag(loopId, destinationPlacementStart): apply the identical guard order (launch present -> ports present -> no in-flight operation -> identity well-formed), read the coherent snapshot (records, interpolation, capacity, loopClips), build the 'move-group' intent, invoke resolvePhysicPaintRotoPhysicalEdit with the break collection included via input.getIncomingInterpolationBreakKeyIds?.(), reject no-change results, then compute the proposalVersion with a NEW break-aware revision call that passes records, interpolation, loopClips, AND the incoming break collection as the 4th argument of buildPhysicPaintRotoPhysicalRevision — introduce a separate helper (e.g. buildGroupDragProposalVersion) and do NOT change buildProposalVersion or its existing call sites (regression boundary, Pitfall 1). Freeze the publication { proposal, intent, proposalVersion, expectedLaunch: { operationId, layerId }, ... } exactly like the analog. commitRotoGroupDrag(publication): verify proposal.status.operationKind === 'move-group' and intent.kind === 'move-group', run the same shallow member-equality and launch-tuple checks as the analog, then forward the retained objects to input.executePhysicalEdit with zero recomputation. Do not route through runPhysicalAction — its PhysicalActionRunnerKind is a bounded Extract (lines 519-522) that intentionally excludes drag commits.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useRotoTimelineActions.ts exports prepareRotoGroupDrag and commitRotoGroupDrag
    - The new prepare passes the break collection into both the resolver input and the revision fingerprint; a dedicated break-aware version helper exists and buildProposalVersion is byte-unchanged
    - All five behavior tests pass; the full useRotoTimelineActions.test.ts suite exits 0
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The Group-drag prepare/commit pair retains immutable publications with break-aware staleness detection and commits through the existing coordinator path as one atomic transaction (GDRAG-07 substrate).</done>
</task>

<task type="auto">
  <name>Task 3: Rail drag session hook plus unclamped ghost presentation on the Group Rail</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx (entire file — click/double-click timer machinery lines 62-128, stopPointerEvent replacement point line 143, rail presentation classes line 140, geometry projection lines 181-195)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 1107-1322 session idiom: modifier guard, window listeners, pointer capture, capture-phase Escape, lostpointercapture, edge scroll, suppressNextRotoClickRef click suppression, focus restoration; line 296 ROTO_DRAG_THRESHOLD_PX — do NOT reuse)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UI-SPEC.md (G1/G2 geometry: 4px horizontal threshold, grabbing cursor during session, ghost 3px at y:0, 55% opacity, pointer-events none, aria-hidden, z-order between layers 7 and 8)
    - app/src/components/physic-paint/physicsPaintStudio.css (existing rail target and cell classes the ghost must sit beside without altering)
  </read_first>
  <action>Create the focused sibling hook module usePhysicsPaintGroupRailDrag.ts (RESEARCH Open Question 3 resolution; keeps the rail component presentational and new logic out of PhysicsPaintStudio.tsx) copying the strip session idiom structurally: arm on rail-target pointer-down only when event.isPrimary, button 0, and no meta/ctrl/shift modifiers; track with window pointermove/pointerup/pointercancel listeners plus capture-phase keydown Escape and lostpointercapture; start the drag only after more than 4px HORIZONTAL travel (new module constant, e.g. GROUP_RAIL_DRAG_THRESHOLD_PX = 4 — the 6px Euclidean ROTO_DRAG_THRESHOLD_PX belongs to the locked cell-drag path and stays untouched). On threshold crossing: cancel the pending single-click timer via the rail's clearClickSequence, set pointer capture on the rail target, switch cursor to grabbing, set click suppression so the post-drop click event cannot re-fire selection or Edit Group (the strip's suppressNextRotoClickRef + setTimeout(0) clear idiom, Pitfall 2), invoke prepareRotoGroupDrag once to retain the publication, and show the ghost. During the session update session-only Signal/ref preview state from pointermove (never useEffect-mirrored state; CLAUDE.md Preact directives) and recompute the proposed destination from the live pointer position using the same 18px frame pitch projection the rail already uses. Escape cancels with zero state change, zero history entry, immediate preview removal, and focus restoration (D-04). Drop calls commitRotoGroupDrag with the retained publication; on commit failure restore focus and leave every visible fact unchanged. In PhysicsPaintLoopClipRail.tsx replace onPointerDown={stopPointerEvent} (line 143) with a real handler that keeps event.stopPropagation() (43 D-42 isolation from physical-cell handlers) and delegates to the session hook; below-threshold release must leave the existing plain click / Shift-click / Cmd-click / 250ms-deferred double-click machinery byte-identical in behavior. Render the ghost as a separate paint-only element: same half-open interval geometry as the dragged rail segment (left = proposedPlacement x 18px, width = max(1px, (effectiveEnd - proposedPlacement) x 18px)), 3px high at y:0 of the physical-frame row, mode color (#8B5CF6 Motion / #06B6D4 Static) at 55% opacity, pointer-events: none, aria-hidden="true", no selection orange, no endpoint cuts, no lifecycle dots; effective 0f Groups ghost the existing 8x6px flag glyph at 55% opacity. The original accepted rail never translates, dims, or hides before parent acceptance (D-01). CSS additions are strictly additive new classes in physicsPaintStudio.css; the ghost clips at the shared timeline viewport exactly like the accepted rail. Clamped blocked edge and gap-preview cells are NOT in this task (plan 03).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts exists and owns the session state machine; PhysicsPaintStudio.tsx is untouched
    - New rail tests pass: sub-threshold release preserves click selection; threshold crossing cancels the pending single-click timer; Escape mid-session leaves selection/history untouched; post-drop click does not re-fire selection or open Edit Group; ghost element renders with aria-hidden="true" and pointer-events none at 55% opacity
    - The existing PhysicsPaintLoopClipRail.test.tsx expectations pass unmodified
    - physicsPaintStudio.css diff is additions only (no modified or removed existing rules)
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>A pointer drag on the rail past 4px shows a mode-colored ghost following the cursor while the original rail stays in place; drop commits the prepared publication; Escape cancels cleanly; all pre-existing rail gestures behave exactly as before (GDRAG-01 gesture substrate, D-01/D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child view -> resolver | Untrusted pointer-derived destination and loopId cross into the pure canonical edit authority |
| child -> parent bridge | Retained publication + intent cross the transport; parent revalidates before store replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.3-01-01 | Tampering | 'move-group' intent parsing (types/physicPaint.ts) | high | mitigate | hasOnlyKeys allowlist branch, bounded IDs, no normalization/coercion; exhaustive serializer switch fails compile if a case is missed (Task 1) |
| T-43.3-01-02 | Tampering / Repudiation | proposalVersion staleness check (useRotoTimelineActions.ts) | high | mitigate | Break-aware revision fingerprint threads getIncomingInterpolationBreakKeyIds into the 4th revision parameter so stale break authority rejects before mutation (Task 2, Pitfall 1) |
| T-43.3-01-03 | Elevation of Privilege (local) | Ghost/preview state | medium | mitigate | Session-only Signal/ref state consumed for paint only; commit consumes only the immutable retained publication; accepted rail never moves before parent acknowledgement (Task 3, D-01) |
| T-43.3-01-04 | Tampering (integrity) | Locked move-key-group regression boundary | high | mitigate | New union member, dispatch branch, prepare/commit pair, and session hook are purely additive; acceptance criteria forbid diffs inside move-key-group code and test blocks (Tasks 1-3) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx — green
- pnpm --dir app typecheck — exits 0
- git diff confirms zero modifications to move-key-group source paths, existing parser/serializer cases, buildProposalVersion, and PhysicsPaintStudio.tsx
</verification>

<success_criteria>
A source-attached Group can be dragged from its rail into free space and committed as one atomic parent-acknowledged transaction through the new 'move-group' intent, with the ghost presentation-only and all locked regression boundaries byte-unchanged in behavior. Clamping, break recording, gap preview, blocked edge, and status copy are explicitly plan 02/03 scope and absent here.
</success_criteria>

## Artifacts this phase produces (this plan)

- Intent union member `{ kind: 'move-group', loopId, destinationPlacementStart }` in `PhysicPaintRotoPhysicalEditIntent` (app/src/types/physicPaint.ts)
- Operation-kind literal `'move-group'` in `PhysicPaintRotoPhysicalEditOperationKind` plus validator entries
- New resolver dispatch branch for `intent.kind === 'move-group'` (physicsPaintRotoPhysicalResolver.ts)
- `prepareRotoGroupDrag` / `commitRotoGroupDrag` exports (useRotoTimelineActions.ts)
- Break-aware proposal-version helper, e.g. `buildGroupDragProposalVersion` (useRotoTimelineActions.ts)
- New module `app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts` (session hook; threshold constant, e.g. `GROUP_RAIL_DRAG_THRESHOLD_PX`)
- Ghost paint-only element + additive CSS classes (PhysicsPaintLoopClipRail.tsx, physicsPaintStudio.css)
- New test blocks: move-group resolver describe, transport round-trip assertions, prepare/commit specs, rail session specs

<output>
Create `.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-01-SUMMARY.md` when done
</output>
