---
phase: 43.3-motion-and-static-group-drag-within-free-space
plan: 03
type: execute
wave: 3
depends_on:
  - 43.3-01
  - 43.3-02
files_modified:
  - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts
  - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx
  - app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts
  - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - GDRAG-02
  - GDRAG-06
  - GDRAG-07
  - GDRAG-08

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The ghost shows exactly the clamped destination the resolver will commit — preview is the commit contract (D-05)"
    - "UI-SPEC G2 partial: a clamped ghost paints the 2x12px #FF6B6B blocked-edge bar on the bounded side, and drop commits exactly the clamped preview"
    - "UI-SPEC G3 populated: the vacated interval and any newly opened destination interval preview as ordinary roto-fill-empty cells, byte-identical to the 43.2 deleted-Group-gap treatment (D-02)"
    - "UI-SPEC G3 zero-one-many: an adjacent landing previews zero destination-gap cells; end-of-content and before-all-content moves preview no break-bearing gap (D-12); a multi-frame gap previews every covered cell uniformly"
    - "UI-SPEC G4 zero-one-many: an unclamped ghost shows no bar; a left clamp shows the bar on the left edge; a right clamp on the right edge; zero free space in the dragged direction rejects with the mapped status"
    - "UI-SPEC G1 loading: an in-flight Group mutation gates new drag sessions through the existing controller busy state; no optimistic movement occurs"
    - "UI-SPEC G1 error / G5 error: rejection removes all preview paint, preserves prior geometry/selection/focus, and publishes the mapped reason; failures leave every visible fact unchanged (D-18)"
    - "UI-SPEC G5 long-text: status copy is fixed-form with bounded frame numbers and the existing status line truncation behavior applies unchanged"
    - "An accepted move publishes 'Moved Group to frame {N}.' plus 'Gap left at frames {A}–{B}.' when a gap opened, through the single product-reason mapper (D-07)"
    - "After an accepted drag the moved Group stays selected, the physical cursor stays where it is, no automatic navigation occurs, and the canvas reconciles only when the current frame's content changed (D-17)"
  artifacts:
    - "app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx renders the clamped ghost plus blocked-edge bar from the shared clamp authority"
    - "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx applies roto-fill-empty preview classes for vacated and destination-gap cells"
    - "app/src/components/physic-paint/hooks/useRotoTimelineActions.ts owns the Group-drag product-reason mapper and accepted-status copy"
    - "physicsPaintStudio.css contains the additive blocked-edge and any ghost-clamp classes; no existing rule modified"
  key_links:
    - "shared clamp export (plan 02) -> ghost presentation: one authority computes what preview shows and what commit applies (D-05)"
    - "commit acceptance -> mapper -> aria-live status line: one copy owner for no-space rejection and accepted-move facts (D-06/D-07)"
    - "controller busy gate -> session arm: a second drag cannot start while a Group mutation is in flight"
---

<objective>
Complete the user-facing surface of the Group drag: clamped ghost with the red blocked-edge bar, neutral-gray gap preview cells, the mapped status copy for rejection and acceptance, the busy gate, and post-commit stability (selection, cursor, no navigation).

Purpose: Everything visual and verbal the user perceives lands here, built on the plan-01 gesture/commit slice and the plan-02 clamp/break authority. Preview remains presentation-only Signal state; the accepted document is the sole truth (D-01).
Output: The complete interactive drag experience matching the approved UI-SPEC, ready for the plan-04 regression gate and native UAT. The ghost rail, clamped red blocked edge, neutral-gray gap preview, gesture threshold, and status copy visuals are governed by the approved 43.3-UI-SPEC.md (D-16).

Planning notes: spec-less probe fallback skipped visibly (no REQUIREMENTS.md IDs; GDRAG set adopted). No external API / assumption-delta / schema-push signals.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UI-SPEC.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-PATTERNS.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-01-SUMMARY.md
@.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clamped ghost, blocked-edge bar, and roto-fill-empty gap preview</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.tsx, app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts, app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts (plan-01 session hook — the file being extended)
    - app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts (the plan-02 exported pure clamp function — single authority the preview must consume)
    - app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.ts (lines 720-741 collectRotoDragVacatedAppFrames — the diffing shape to mirror)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (line 820 vacated-frames consumption, cell class application sites)
    - app/src/components/physic-paint/physicsPaintStudio.css (line 2993 .physics-paint-roto-cell.roto-fill-empty — the locked 43.2 gap treatment)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UI-SPEC.md (G2/G3/G4 geometry and paint contract, drag-state paint table, state precedence)
  </read_first>
  <action>Wire the ghost to the plan-02 exported pure clamp function so the previewed destination IS the clamped value the resolver will commit (D-05 — never duplicate the clamp math in the view layer; RESEARCH Open Question 2). During the session, recompute proposed destination + clamp from the live pointer position on every pointermove, including during horizontal scroll (existing timeline scroll behavior; UI-SPEC responsive contract). Blocked edge: when the clamp binds, paint a 2px wide x 12px high vertical bar in #FF6B6B on the ghost's blocked edge — left edge for a left boundary, right edge for a right boundary — spanning the full 12px interaction band, pointer-events none, same z-layer as the ghost, absent when unclamped (UI-SPEC G4); the ghost itself stays mode-colored at 55% opacity while clamped. Gap preview: extend the strip's vacated-cell diffing (collectRotoDragVacatedAppFrames pattern, presentation lines 720-741) so the Group-drag session's vacated interval AND any newly opened destination interval between the moved Group's first source key and its new predecessor apply the existing roto-fill-empty class (border #3D454D, background #4D535A) — byte-identical to 43.2 deleted-gap cells, class application only, no new DOM nodes, no diamonds, no distinct drag-gap visual (D-02, UI-SPEC G3). Adjacent landing previews zero destination-gap cells; end-of-content/before-all-content moves preview no break-bearing gap (D-12 mirrored in preview). All preview state stays in session-only Signals/refs consumed for paint only — never canonical revision, history, or persistence (Pitfall 5). Escape/reject removes every preview paint immediately with zero state change. CSS: additive classes only for the blocked-edge bar and any clamp-state ghost variant; no existing rule modified; no transition added (reduced-motion contract). Keep the top-band propagation stop intact so neither the rail drag nor the physical-cell drag can start the other (D-15 coexistence).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The ghost destination derives from the resolver-exported clamp function (imported symbol, not reimplemented math)
    - Test suite homes are explicit: the new gap-preview diffing assertions (vacated and destination-gap frames receive the roto-fill-empty class; adjacent landing applies no destination-gap class) live in physicsPaintWorkflowPresentation.test.ts — the presentation helper's own suite; the clamped-ghost blocked-edge bar and Escape preview-removal assertions live in PhysicsPaintLoopClipRail.test.tsx; strip-level class consumption stays covered by PhysicsPaintWorkflowStrip.test.ts
    - New tests pass: clamped ghost shows the bar on the correct edge; unclamped shows none; vacated and destination-gap frames receive the roto-fill-empty class; adjacent landing applies no destination-gap class; Escape removes all preview classes/elements
    - physicsPaintStudio.css diff is additions only; .roto-fill-empty rule itself is unmodified
    - Existing rail and strip suites pass unmodified; pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The drag preview shows the exact clamped destination with a red blocked-edge bar at collision limits and 43.2-identical neutral-gray gap cells for vacated/opened intervals — all presentation-only, removed cleanly on cancel or reject (GDRAG-02, D-01..D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Product-reason mapper, locked status copy, busy gate, and post-commit stability</name>
  <files>app/src/components/physic-paint/hooks/useRotoTimelineActions.ts, app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.ts (lines 129-149 mapper precedent, 862-874 accepted-status precedent with successMessage, 519-522 bounded runner Extract, plan-01 prepare/commit pair)
    - app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts (existing mapper/status harness)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-CONTEXT.md (D-06, D-07, D-17 verbatim copy)
    - .planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-UI-SPEC.md (copywriting contract — inclusive {A}–{B} derived from canonical half-open ranges at presentation time only)
  </read_first>
  <behavior>
    - Test 1 (no-space rejection): a commit attempt whose resolver result failed with the plan-02 zero-space code publishes exactly 'No empty space in that direction.' and mutates nothing (D-06)
    - Test 2 (accepted, no gap): an accepted move publishes exactly 'Moved Group to frame {N}.' with {N} = accepted destination placementStart (D-07)
    - Test 3 (accepted, gap): when a vacated gap opened, the status additionally publishes 'Gap left at frames {A}–{B}.' with the inclusive product range derived from the canonical half-open vacated interval at presentation time only (43.2 presentation rule)
    - Test 4 (single mapper): disabled preflight, rejection, and acceptance copy all flow through the one Group-drag mapper; raw resolver failure text never reaches the status line
    - Test 5 (busy gate): prepareRotoGroupDrag rejects while a Group mutation is in flight (existing pendingOperationId gate), so a second drag session cannot start
    - Test 6 (post-commit, D-17): after acceptance the moved Group remains selected, selectedAppFrame/cursor are unchanged from the publication, no navigation occurs, and reconciliation runs only when the current frame's content changed
  </behavior>
  <action>Add one Group-drag product-reason mapper beside the existing mappers in useRotoTimelineActions.ts (single copy owner per 43.1/43.2 precedent; raw resolver diagnostics stay internal). Map the plan-02 zero-space failure code to the literal copy 'No empty space in that direction.' (D-06). On commit acceptance — via the same .then((accepted) => ...) continuation pattern the strip drag uses, NOT runPhysicalAction whose kind union is a bounded Extract (lines 519-522) — publish 'Moved Group to frame {N}.' and, when the accepted proposal opened a vacated gap, append 'Gap left at frames {A}–{B}.' deriving the inclusive bounds from the canonical half-open interval only at presentation time; frame numbers use tabular-nums per the 43 contract. Publish through the existing aria-live status line; rejections use role="alert" per 43-UI-SPEC. Confirm the existing pendingOperationId busy gate covers the new prepare (it must, since the plan-01 pair reuses the same in-flight guard) and add an explicit test. Post-commit stability (D-17): keep the moved Group selected, forward the publication's selectedKeyId/selectedAppFrame unchanged, trigger no navigation, and reconcile the Studio canvas through the existing current-frame reconciliation path only when the current frame's content changed (43.1-04 reconciliation precedent). A failed, rejected, or cancelled drag changes nothing and creates no history entry (D-18).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The six behavior tests pass, including the three literal-copy assertions and the busy-gate test
    - useRotoTimelineActions.ts contains exactly one new mapper owning Group-drag disabled/rejection/acceptance copy
    - Existing mapper and status tests pass unmodified; runPhysicalAction and its kind Extract are untouched
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>No-space drags publish the locked rejection copy with zero mutation; accepted moves publish destination-plus-gap facts through the single mapper; the busy gate serializes drags against in-flight mutations; post-commit selection/cursor/navigation behavior matches D-17 (GDRAG-07 status, GDRAG-08 busy substrate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview layer -> canonical document | Presentation-only state must never become accepted truth (confused-deputy risk) |
| resolver diagnostics -> status line | Internal failure text must never leak into product copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.3-03-01 | Elevation of Privilege (local) | Ghost/gap preview state | high | mitigate | Preview lives in session-only Signals consumed for paint; commit consumes only the immutable retained publication; acceptance criteria forbid canonical/revision writes from preview paths (Task 1, Pitfall 5) |
| T-43.3-03-02 | Tampering | Preview/commit divergence | high | mitigate | Ghost destination derives from the same resolver-exported clamp function the commit path uses — one authority, no duplicated math (Task 1, D-05) |
| T-43.3-03-03 | Information Disclosure (UX integrity) | Status copy | low | mitigate | Single product-reason mapper; raw resolver diagnostics never reach the status line; literal-copy tests lock the strings (Task 2) |
| T-43.3-03-04 | Denial of Service (local) | Concurrent drag during in-flight mutation | medium | mitigate | Existing pendingOperationId busy gate covers the new prepare; explicit regression test (Task 2) |
</threat_model>

<verification>
- pnpm --dir app exec vitest run app/src/components/physic-paint/view/PhysicsPaintLoopClipRail.test.tsx app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/view/physicsPaintWorkflowPresentation.test.ts — green
- pnpm --dir app exec vitest run app/src/components/physic-paint/hooks/useRotoTimelineActions.test.ts — green
- pnpm --dir app exec vitest run — full suite green (wave merge gate per VALIDATION.md)
- pnpm --dir app typecheck && pnpm --dir app build — exit 0
</verification>

<success_criteria>
The complete drag experience matches the approved UI-SPEC: clamped ghost with red blocked edge, 43.2-identical gap preview, locked rejection/acceptance status copy, busy gating, and D-17 post-commit stability — all preview presentation-only, all commits atomic through the retained publication.
</success_criteria>

## Artifacts this phase produces (this plan)

- Blocked-edge bar element + additive CSS classes (PhysicsPaintLoopClipRail.tsx, physicsPaintStudio.css)
- Group-drag gap-preview diffing helper (physicsPaintWorkflowPresentation.ts, e.g. a collect* function mirroring collectRotoDragVacatedAppFrames)
- Group-drag product-reason mapper + accepted-status continuation (useRotoTimelineActions.ts)
- New test blocks: clamp/blocked-edge/gap-preview rail+strip specs, mapper/status/busy/post-commit specs

<output>
Create `.planning/phases/43.3-motion-and-static-group-drag-within-free-space/43.3-03-SUMMARY.md` when done
</output>
