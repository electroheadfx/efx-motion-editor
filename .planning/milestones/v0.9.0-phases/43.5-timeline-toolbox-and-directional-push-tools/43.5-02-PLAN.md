---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: [TBX-01, TBX-02, TBX-03, TBX-04, TBX-05]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The ToolCase `Tools` button sits at the old Interpolation pill spot (after the key-utilities group, before Close) and toggles a non-modal popover whose body renders entirely above the strip, bottom edge 4px above the strip top, never covering rails/cells/spacing proxies (D-01, D-05)"
    - "The popover contains the relocated Interpolation group (Blend toggle + mode select) and Key Spacing group (Set Key Space input + Apply) rendered with their existing classes, props, labels, guards, pending states, and error messages verbatim — zero behavior change (D-03)"
    - "The popover edits timeline-owned state only through the existing callbacks (onRotoInterpolationEnabledChange, force-spacing handlers) — no local mirror, no second state authority, no persistence change (D-03, TBX-02)"
    - "Outside pointerdown and Escape dismiss the popover; no focus trap, no backdrop; Cmd+Z, Delete/Backspace, and timeline shortcuts keep normal routing while it is open (D-02, Pitfall 1: Delete guard keyed off aria-modal only)"
    - "The ToolCase button always shows live Interpolation on/off at a glance via the 6px badge dot — a read-only projection of the timeline-owned signal, never interactive, never a second authority (D-04)"
    - "The popover is always fully rendered with both sections (no empty/partial variant), width clamped 232-280px inside the viewport, max-height 240px, z-index 60 (UI Considerations: T2 populated/overflow/partial/long-text)"
    - "The ToolCase button is always present in the toolbar with the fixed short label `Tools` regardless of rail count (UI Considerations: T1 populated/empty/long-text/error)"
    - "The workflow strip remains exactly 161px; the toolbar row stays readable and unclipped at 1280x720 (TBX-05)"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — ToolCase button, popover host, relocated control groups
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts — popover-aware Delete guard, Escape layering
    - app/src/components/physic-paint/physicsPaintStudio.css — additive popover/badge classes
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts — popover-open shortcut routing regression coverage
  key_links:
    - "popover controls → existing interpolation/force-spacing callbacks → timeline-owned state (D-03 single authority)"
    - "physicsPaintStudioKeyboard.ts Delete guard keyed off aria-modal=true only — the popover is role=dialog aria-modal=false and must NOT suspend Delete/Backspace (Pitfall 1)"
    - "Escape precedence: active drag (capture) > popover dismiss > armed-tool disarm > collapseRotoSelection — one Escape handles at most one layer (Pitfall 2)"
---

<objective>
Relocate the top-level Interpolation pill and Key Spacing form into one Studio-local anchored popover opened by a compact ToolCase button, with zero behavior change and uninterrupted shortcut routing.

Purpose: Frees the toolbar space the Push pill occupies in plan 43.5-05 while preserving every accepted control behavior, guard, and message byte-identically (TBX-01..TBX-05, D-01..D-05).
Output: A working ToolCase + popover surface; both control groups live only inside the popover; keyboard dispatcher refined so the non-modal popover never captures Delete/Backspace/Cmd+Z.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end ToolCase button + popover with the Interpolation group relocated and shortcut-safe</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 337-339 buildGuardedActionTooltipCopy, 494-561 header chrome with the interpolation pill at L546-552 and Close at L553-558, 673 interpolationEnabled projection, 1848-1876 icon-button idiom)
    - app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx (full file — useStyledTooltip idiom L29-91, createPortal topmost idiom L353, computeTooltipPlacement L152-240)
    - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts (lines 31-42 shortcut target + Delete dialog guard, 139-155 Escape branch)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md (T1/T2 geometry, color, copy tables — locked)
  </read_first>
  <action>Add the ToolCase button at the interpolation pill's exact header spot (after the key-utilities group, before Close) reusing the physics-paint-roto-key-icon-button idiom (26px, 18px ToolCase icon from lucide-preact — export verified in the installed 0.577.0 package, 10px `Tools` label) with the guarded-tooltip idiom (buildGuardedActionTooltipCopy, aria-describedby, PhysicsPaintStyledTooltip). Build the popover as a Studio-local anchored surface mounted via the createPortal idiom (safe against the shared horizontal scroller — RESEARCH Open Question 3): role="dialog", aria-modal="false", aria-label="Timeline tools", body rendered entirely above the strip with its bottom edge exactly 4px above the strip top, left edge aligned to the button's left edge clamped 8px inside the timeline viewport, width 232-280px, max-height 240px, z-index 60, dark card idiom (#20262D background, 1px #58616b border, 8px radius, 16px padding). RELOCATE the interpolation pill markup (strip L546-552: div.physics-paint-pill.physics-paint-pill--interpolation.physics-paint-roto-interpolation-controls with the Blend toggle, mode select, and styled tooltip) into the popover's first section with every prop, callback, class, guard, aria attribute, and pending/disabled idiom byte-identical — state authority stays in the timeline; the popover edits through the existing callbacks only (D-03, TBX-02). Toggle: clicking the button toggles the popover; outside pointerdown (window capture-phase listener registered only while open) and Escape dismiss; no focus trap, no backdrop, no automatic focus move (D-01, D-02). In physicsPaintStudioKeyboard.ts refine isPhysicsPaintRotoDeleteTarget (L39-42): replace the blanket [role="dialog"], [aria-modal="true"] querySelector check with an aria-modal="true"-only check so the non-modal popover never suspends Delete/Backspace routing (Pitfall 1, D-02). Extend the Escape branch so popover dismiss is handled before collapseRotoSelection and one Escape handles at most one layer (Pitfall 2). Add regression tests to physicsPaintStudioKeyboard.test.ts: popover open (role=dialog aria-modal=false present in document) + Delete on a selected key still routes; modal (aria-modal=true) still suspends; Escape dismisses the popover without collapsing selection. CSS strictly additive (popover panel, sections, badge) — zero change to the 161px strip height or any existing class.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts && pnpm --dir app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip.tsx no longer renders the interpolation pill in the header chrome; it renders the ToolCase button at that spot and the popover host
    - The popover markup carries role="dialog" aria-modal="false" aria-label="Timeline tools" and contains the interpolation group with its original classes (physics-paint-roto-interpolation-controls)
    - physicsPaintStudioKeyboard.ts Delete guard matches aria-modal="true" only (grep: the guard no longer matches a bare [role="dialog"] selector)
    - New keyboard tests pass: popover-open Delete routes, modal Delete suspends, Escape layering
    - pnpm --dir app typecheck exits 0; full suite pnpm --dir app exec vitest run stays green
  </acceptance_criteria>
  <done>Clicking Tools opens the popover above the strip with the live Interpolation controls working identically; outside click/Escape dismiss; Delete/Backspace/Cmd+Z route normally while open; all tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Key Spacing relocation + live Blend badge + popover section layout and dismissal polish</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (lines 2102-2142 Key Spacing form — physics-paint-pill physics-paint-pill--apply-spacing physics-paint-roto-force-spacing-controls with guarded tooltip and aria-describedby reason span)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (spacing selection signals wiring — read to confirm no duplicated state is introduced; do not modify in this plan)
    - app/src/components/physic-paint/roto/physicsPaintRotoSpacingSelection.ts (spacing proxy model the relocated controls continue to drive)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md (T2 internal layout, badge paint, copy tables)
  </read_first>
  <action>RELOCATE the Key Spacing form (strip L2102-2142: the form.physics-paint-pill.physics-paint-pill--apply-spacing.physics-paint-roto-force-spacing-controls with the Set Key Space input, Apply pill button, guarded tooltip, and aria-describedby reason span) from the bottom action row into the popover's second section with every prop, callback, class, guard, label, pending state, and error message byte-identical (D-03, TBX-01). isPhysicsPaintShortcutTarget already excludes input/textarea/select so typing in the relocated input never triggers timeline shortcuts — assert this stays true. Popover internal layout per UI-SPEC T2: two stacked sections separated by an 8px gap and a 1px #3F4851 divider; section headings `Interpolation` and `Key Spacing` in the Micro role (10px/600, #9CA3AF, letter-spacing 0.04em, title case); the popover adds ONLY the headings and panel chrome. Add the Blend-state badge to the ToolCase button per D-04 and UI-SPEC: 6px filled dot pinned at the icon's top-right corner (2px insets), paint-only, aria-hidden, present only while interpolation is on — deriving directly from the existing interpolationEnabled projection (strip L673: props.rotoInterpolationEnabled === true), never from local state; badge OFF = absent, no button paint change. Accessible name carries live state: `Timeline tools, interpolation on` / `Timeline tools, interpolation off`; enabled tooltip `Open timeline tools — Interpolation and Key Spacing.`; aria-expanded/aria-controls reflect popover state. Badge paint: #DBE4FF dot with 1px #37393A ring. Remove the now-empty top-level pill wrappers; verify the freed header/bottom-row space and that the strip stays exactly 161px with nothing covering cells or rails (Pitfall 7). CSS remains strictly additive.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/ && pnpm --dir app typecheck && pnpm --dir app exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - PhysicsPaintWorkflowStrip.tsx no longer renders the Key Spacing form in the bottom action row; the form renders inside the popover with its original classes (physics-paint-roto-force-spacing-controls)
    - The ToolCase button renders a 6px badge dot iff the interpolation-enabled projection is true, aria-hidden, not interactive; accessible name reflects live state
    - Both popover section headings present: `Interpolation`, `Key Spacing`
    - grep confirms no new useState/useEffect mirrored interpolation or spacing state in the popover host (D-03: edits flow through existing callbacks only)
    - Full suite + typecheck green
  </acceptance_criteria>
  <done>Both control groups live only inside the popover with byte-identical behavior; the badge reflects live Blend state at a glance; the toolbar's freed space is ready for the Push pill; strip geometry unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard dispatcher → timeline actions | Shortcut routing must not be captured or suspended by presentation-only surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.5-04 | Tampering | isPhysicsPaintRotoDeleteTarget dialog guard | medium | mitigate | Guard keyed off aria-modal="true" only; regression tests prove popover-open Delete routing and modal Delete suspension both hold (Pitfall 1) |
| T-43.5-05 | Tampering | duplicated toolbox state | medium | mitigate | Popover edits through existing callbacks only; no local mirror of interpolation/spacing values (D-03); acceptance criterion greps for mirrored state |
| T-43.5-SC | Tampering | npm installs | high | mitigate | No package installs this phase (lucide-preact ToolCase export already verified in installed 0.577.0) |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/view/` green (keyboard regression tests included)
- `pnpm --dir app exec vitest run` + `pnpm --dir app typecheck` green before wave merge
- Native visual confirmation of popover geometry/behavior is deferred to plan 43.5-06 UAT (UI-SPEC acceptance matrix rows 1-4)
</verification>

<success_criteria>
The ToolCase popover fully replaces the top-level Interpolation/Key Spacing footprint with zero behavior change, zero state duplication, uninterrupted shortcuts, and unchanged 161px strip geometry.
</success_criteria>

## Artifacts this phase produces (this plan)

- `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx`: ToolCase `Tools` button, toolbox popover host (portal-mounted, role="dialog" aria-modal="false"), relocated Interpolation and Key Spacing sections, Blend badge
- `app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts`: aria-modal-scoped Delete guard, popover-layer Escape handling
- `app/src/components/physic-paint/physicsPaintStudio.css`: additive popover panel/section/divider/badge classes
- `app/src/components/physic-paint/view/physicsPaintStudioKeyboard.test.ts`: popover-open Delete routing + Escape layering regression tests

<output>
Create `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-02-SUMMARY.md` when done
</output>
