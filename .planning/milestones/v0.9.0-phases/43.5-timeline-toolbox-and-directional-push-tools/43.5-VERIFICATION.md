---
phase: 43.5-timeline-toolbox-and-directional-push-tools
verified: 2026-08-18T13:45:00Z
status: passed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
deferred: []
behavior_unverified_items: []
human_verification: []
---

# Phase 43.5: Timeline Toolbox and Directional Push Tools Verification Report

**Phase Goal:** Free space in the EFX Paint/Roto bottom toolbar by consolidating Interpolation and Key Spacing into one compact ToolCase toolbox button opening a Studio-local anchored popover with every accepted behavior preserved and no second state authority — and add ONE mode-toggle directional Push tool where the anchor is resolved from the Rail under the pointer on drag, the moved set is the anchor Rail plus every Rail starting at/after its start (the same set for both drag directions), moved as one rigid atomic transaction with one Undo/Redo, clamping at the nearest fixed boundary (left) or capacity (right), with a straddle guard, persistent 43.1 gap/break recording, and full drag preview (hover pre-highlight, per-rail ghosts, gap preview, blocked-edge bar, live readout).
**Verified:** 2026-08-18T13:45:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | ToolCase `Tools` button at the old Interpolation pill spot opens a non-modal anchored popover (role=dialog, aria-modal=false) containing the relocated Interpolation (Blend toggle + mode select) and Key Spacing (Set Key Space input + Apply) control groups byte-identically, with no second state authority | ✓ VERIFIED | `PhysicsPaintWorkflowStrip.tsx` L789-871: ToolCase button + `PhysicsPaintToolboxPopover` (portal-mounted, placement math `computeToolboxPopoverPlacement` L610-633, dismissal L716-740); relocated controls render with existing classes/props verbatim; `physicsPaintToolboxPopover.test.ts` placement/liquid-glass/dismiss tests pass |
| 2   | Live Blend-state badge on the ToolCase button — 6px dot, read-only projection of the timeline-owned interpolation signal, never interactive | ✓ VERIFIED | `PhysicsPaintWorkflowStrip.tsx` L805-808: badge renders iff `props.interpolationEnabled`; CSS `.physics-paint-toolbox-badge` L3937-3947; accessible name carries live state L797 |
| 3   | ONE mode-toggle Push tool — arms freely (no Rail selection required), anchor resolved from the Rail under the pointer on drag (which selects it and moves its set), never from a pre-selected Rail | ✓ VERIFIED | `PhysicsPaintWorkflowStrip.tsx` L2887-2917 (single Push button, `togglePushTool()` on click, `aria-pressed`); `handleLanePushPointerDownCapture` L1518-1563 resolves anchor from `.physics-paint-key-rail-target`/`.physics-paint-loop-clip-rail-target` under the pointer; `physicsPaintPushArmedTool.ts` session-only armed signal; A6 test asserts no selection-derived anchor |
| 4   | The moved set is THE SAME for both drag directions — the anchor Rail plus every Rail starting at/after its start (suffix set); the opposite side stays byte-position fixed | ✓ VERIFIED | `derivePhysicPaintPushSet` `physicsPaintRotoPhysicalResolver.ts` L2357-2365: `rails.filter((rail) => rail.intervalStart >= anchorRail.intervalStart)`; behavioral test "derives the directional suffix set with byte-position-fixed opposite side and no straddle" PASSES |
| 5   | The complete movement is one rigid atomic parent-acknowledged transaction with one Undo/Redo restoring records, Loop Clip placement, break collection, selection, and cursor | ✓ VERIFIED | `commitRotoPush` `useRotoTimelineActions.ts` L2217-2263 dispatches through `executePhysicalEdit`; `isOrdinaryOperationKind` allowlist includes `push-rails` (`useRotoPhysicalEditHistory.ts` L216); behavioral test "restores the complete pre-push state … through one Undo and one Redo (PUSH-06)" PASSES |
| 6   | Clamping at the nearest fixed boundary on the left (previous rail/key end, or frame 0) for Push Left and capacity/parent end for Push Right; zero valid movement fails closed with `No empty space in that direction.` | ✓ VERIFIED | `clampPhysicPaintPushDestination` `physicsPaintRotoPhysicalResolver.ts` L2455-2473 (directional nearest-free search, ok:false on zero valid movement); dispatch commits the clamped delta (preview-is-the-commit) L4383-4394; behavioral clamp tests PASS |
| 7   | Shared-source straddle guard — a source-attached Group in the moved set sharing its source cycle with a fixed-side Group fails closed with zero mutation | ✓ VERIFIED | `derivePhysicPaintPushSet` straddle verdict L2399-2415; dispatch fails closed `push-source-straddle` L4374-4377; mapper sentence `mapRotoPushProductReason` L411-412; behavioral straddle tests PASS |
| 8   | Opened intervals are recorded as persistent Phase 43.1 stable-key-owned interpolation-break gaps (complete-collection replacement, reuse-never-duplicate, moved-key identity travel, frame-0 reverse-close normalization) | ✓ VERIFIED | `derivePhysicPaintPushIncomingInterpolationBreakKeyIds` `physicsPaintRotoPhysicalResolver.ts` L2488-2541; behavioral break tests PASS (see WR-02 for a non-zero reverse-close edge case) |
| 9   | Full drag preview — armed-anchor pre-highlight, per-rail ghosts at 55% kind color, `roto-fill-empty` gap preview, 2x12px #FF6B6B blocked-edge bar, live status-line readout during the drag only | ✓ VERIFIED | `PhysicsPaintWorkflowStrip.tsx` L2554-2615 (hover layer, ghost layer, blocked edge, anchor capsule), L1490-1512 (gap preview + live readout via `mapRotoPushProductReason`), L2526-2528 (roto-fill-empty class application); CSS L2390-2464 matches spec values; native UAT rows M3-M8 approved |
| 10  | Armed lifecycle — disarm on Escape, re-click, any other toolbar action, Select All, external selection change, and lock transition; session-only armed state never persisted; stay-armed-after-commit with re-arm watchdog | ✓ VERIFIED | `physicsPaintStudioKeyboard.ts` L141-185 (Select All disarm, Escape layering); `PhysicsPaintStudio.tsx` L575-580 (lock-transition disarm + unmount cleanup); `PhysicsPaintWorkflowStrip.tsx` L1248-1262 (re-arm watchdog), L2620-2628 (any-other-toolbar-action disarm); keyboard disarm tests PASS (see WR-03 for a manual-disarm edge case) |

**Score:** 10/10 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `app/src/components/physic-paint/roto/physicsPaintRotoPhysicalResolver.ts` | push-rails dispatch, `derivePhysicPaintPushSet`, `clampPhysicPaintPushDestination`, break derivation, straddle guard | ✓ VERIFIED | 5145 lines; push branch L4328-4464; all three pure authorities exported and consumed by both resolver and strip preflight (D-17) |
| `app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts` | armed push gesture session hook with injected ports, 4px threshold, clamp-and-prepare pipeline, fail-safe matrix | ✓ VERIFIED | 376 lines; full session lifecycle L169-376; 16 tests pass |
| `app/src/components/physic-paint/hooks/useRotoTimelineActions.ts` | `prepareRotoPush`, `commitRotoPush`, `mapRotoPushProductReason`, break-aware proposal version, presentation facts | ✓ VERIFIED | L2109-2263 prepare/commit; L404-443 mapper; L2490-2583 version/facts; 78 tests pass |
| `app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx` | ToolCase button + popover, Push button, mode-toggle arming, pointer anchor resolution, re-arm watchdog, preview paint | ✓ VERIFIED | 2953 lines; all wiring present; 113 tests pass |
| `app/src/components/physic-paint/view/physicsPaintPushArmedTool.ts` | session-only armed-direction signal module | ✓ VERIFIED | 61 lines; `isPushToolArmed`/`togglePushTool`/`disarmPushTool`/`setPushCommitInFlight` |
| `app/src/components/physic-paint/view/physicsPaintKeyRailPresentation.ts` | `resolvePhysicPaintPushAnchor` — any non-empty cell to its containing Rail | ✓ VERIFIED | L117-130; 8 tests pass |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditHistory.ts` | `isOrdinaryOperationKind` allowlist accepts `push-rails` | ✓ VERIFIED | L216; PUSH-06 one-Undo/one-Redo test passes |
| `app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts` | push-rails execute-input arm | ✓ VERIFIED | L364-365 |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| Strip `handleLanePushPointerDownCapture` | `usePhysicsPaintPushDrag.onPointerDown` | `pushDragApiRef.current?.onPointerDown(event)` L1562 | WIRED | Anchor resolved from rail target under pointer, session started |
| Strip `clampDestination` port | `clampPhysicPaintPushDestination` (resolver) | L1378-1384 | WIRED | Single clamp authority, preview-is-the-commit |
| Strip `prepareAtDestination` port | `prepareRotoPush` (useRotoTimelineActions) | L1416 | WIRED | Frozen publication with break-aware version |
| Strip `onDropCommit` port | `commitRotoPush` | L1429 | WIRED | Dispatches through coordinator/history; re-arm watchdog L1248-1262 |
| Strip `onBlocked` port | `mapRotoPushProductReason` | L1454-1468 | WIRED | Pointer-anchored blocked-direction tooltip (Defect 2) |
| Strip live readout | `mapRotoPushProductReason` (kind: 'live') | L1503-1512 | WIRED | Status capsule L1695 |
| Strip gap preview | `pushGapPreviewAppFrames` → `roto-fill-empty` cell class | L1490-1498, L2526-2528 | WIRED | Byte-identical gap treatment |
| `commitRotoPush` | `executePhysicalEdit` → coordinator → history | L2244-2251 | WIRED | `isOrdinaryOperationKind` allowlist L216 |
| Studio lock transition | `disarmPushTool` | `PhysicsPaintStudio.tsx` L575-580 | WIRED | `isPushCommitInFlight` exemption for own commit |
| Keyboard dispatcher | `disarmPushTool` (Escape, Select All) | `physicsPaintStudioKeyboard.ts` L141-185 | WIRED | One Escape handles at most one layer |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| Push moved set | `movedRails` | `derivePhysicPaintPushSet` from canonical records/loopClips | Yes — real set derivation, not static | ✓ FLOWING |
| Push clamp delta | `clampedDeltaFrames` | `clampPhysicPaintPushDestination` + committed proposal delta | Yes — real clamp math | ✓ FLOWING |
| Push break collection | `nextIncomingInterpolationBreakKeyIds` | `derivePhysicPaintPushIncomingInterpolationBreakKeyIds` | Yes — real break derivation | ✓ FLOWING |
| Live readout | `pushDragFeedback` | `mapRotoPushProductReason` from retained publication facts | Yes — real publication facts | ✓ FLOWING |
| Gap preview | `pushGapPreviewAppFrames` | retained publication `gapInterval` | Yes — real opened-gap interval | ✓ FLOWING |
| Ghost paint | `pushDragGhost.deltaFrames` | hook's clamped signed delta | Yes — real clamped delta | ✓ FLOWING |
| Blend badge | `props.interpolationEnabled` | timeline-owned interpolation signal | Yes — read-only projection | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Directional suffix set derivation (same set both directions) | `vitest run physicsPaintRotoPhysicalResolver.test.ts -t "derives the directional suffix set with byte-position-fixed opposite side and no straddle"` | 1 passed | ✓ PASS |
| One-Undo/one-Redo atomic push history | `vitest run useRotoPhysicalEditHistory.test.ts -t "restores the complete pre-push state"` | 1 passed | ✓ PASS |
| Drag session drop commit exactly once | `vitest run usePhysicsPaintPushDrag.test.ts -t "drops: commits the retained publication exactly once"` | 1 passed | ✓ PASS |
| Stale-authority fail-closed commit | `vitest run useRotoTimelineActions.test.ts -t "rejects a stale break-aware proposal version with zero mutation"` | 1 passed | ✓ PASS |
| Push gate on busy/lock only (A1) | `vitest run PhysicsPaintWorkflowStrip.test.ts -t "A1"` | 8 passed | ✓ PASS |
| Full automated gate | `vitest run` | 2383 passed, 1 skipped, 101 todo | ✓ PASS |
| Typecheck | `pnpm typecheck` | clean | ✓ PASS |

### Probe Execution

No probes declared in the phase PLAN/SUMMARY; the phase's verification is the automated gate (vitest/typecheck/build) plus native visual UAT. Step 7c: SKIPPED (no probe scripts).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| TBX-01 | 43.5-02 | Key Spacing form relocated into popover byte-identically | ✓ SATISFIED | `PhysicsPaintWorkflowStrip.tsx` L826-869; strip test asserts guarded form pattern |
| TBX-02 | 43.5-02 | Interpolation group relocated byte-identically; non-modal keyboard routing | ✓ SATISFIED | L815-824; keyboard tests pass |
| TBX-03 | 43.5-02 | ToolCase button + popover geometry (above strip, 4px gap, 232-280px, z-60) | ✓ SATISFIED | L649-686, L610-633; placement tests pass |
| TBX-04 | 43.5-02 | Live Blend badge, read-only projection | ✓ SATISFIED | L805-808; badge CSS |
| TBX-05 | 43.5-02/05 | Strip stays 161px; toolbar readable at 1280x720 | ✓ SATISFIED | CSS L1972 `height: 161px`; band-sum test |
| PUSH-01 | 43.5-01 | push-rails closed-union intent + directional set derivation | ✓ SATISFIED | Resolver L4328-4464; types test passes |
| PUSH-02 | 43.5-01 | Fail-closed edges (duplicated-placement-only, zero-delta no-change, malformed) | ✓ SATISFIED | Resolver tests pass |
| PUSH-03 | 43.5-01 | Push gap break derivation (complete-collection replacement) | ✓ SATISFIED | L2488-2541; break tests pass |
| PUSH-04 | 43.5-04/05 | Armed push drag session + real commit through wired ports | ✓ SATISFIED | `usePhysicsPaintPushDrag.ts`; strip wiring; 16 session tests pass |
| PUSH-05 | 43.5-01/03 | Pure push clamp + prepare/commit publication pair + locked copy family | ✓ SATISFIED | L2455-2473; L2109-2263; mapper tests pass |
| PUSH-06 | 43.5-03 | One-Undo/one-Redo push history | ✓ SATISFIED | History allowlist L216; PUSH-06 test passes |
| PUSH-07 | 43.5-05 | Armed lifecycle, hover pre-highlight, straddle preflight, drag preview paint | ✓ SATISFIED | Strip L1217-1563, L2554-2615; native UAT rows M2-M10 approved |
| PUSH-08 | 43.5-04/05 | Direct Group/Key Rail drag stays local (byte-untouched) | ✓ SATISFIED | `git diff 4cfba215..HEAD` on both direct-drag hooks is empty |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `PhysicsPaintWorkflowStrip.tsx` | L1469-1475 | WR-01: `onPreviewChange` clears `pushDragBlocked` only when `preview === null` — a blocked drag that becomes valid leaves the blocked-direction tooltip/cursor lingering until drop | ⚠️ Warning | UI polish; does not block the push or commit |
| `physicsPaintRotoPhysicalResolver.ts` | L2520-2533 | WR-02: reverse-close normalization removes the opened-gap break only when the moved set returns to frame 0 — a Push Right then Push Left back to a non-zero original position leaves a persistent break, so interpolation state differs from pre-push | ⚠️ Warning | Edge-case deviation from D-15 "reverse push that closes the gap normalizes the break collection"; frame-0 case works |
| `PhysicsPaintWorkflowStrip.tsx` | L1248-1262, L2898-2904 | WR-03: re-arm watchdog can undo a manual disarm between commit settlement and mutation-lease release (Push button onClick does not clear `rearmAnchorRef`) | ⚠️ Warning | Edge-case UX; the watchdog re-arms after an explicit user disarm |
| `usePhysicsPaintPushDrag.ts` | L205-209 | IN-01: `clearSuppressionSoon` uses `setTimeout(0)` which can race the post-drop click dispatch | ℹ️ Info | Timing-dependent, not a confirmed defect |
| `PhysicsPaintWorkflowStrip.tsx` | L1545-1562 | IN-02: `armedAnchorRef`/`pushSessionRef` left set if the hook's `onPointerDown` returns early | ℹ️ Info | Edge case requiring a missed pointerup |

No `TBD`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` markers found in any phase-modified file. No stub patterns (no `return null`/empty-array/static-return implementations in the push path).

### Human Verification Required

None outstanding. The native visual UAT (43.5-UAT.md) was approved by the user on 2026-08-18 from a restarted frozen-code session (43.2 D-39), covering the 13 prompt acceptance steps and the UI-SPEC visual matrix rows M1-M10 (badge, armed tint + cursor, pre-highlight, mode-toggle grab-any-rail, 55% ghosts / 100% originals, gap preview, blocked-edge bar, live readout, anchor capsule persistence, stay-armed after commit). The 43.5-05-SUMMARY D2/D3 items marked `human_judgment: true` (hover pre-highlight and drag preview paint) were resolved by that approval.

### Gaps Summary

No blocking gaps. The phase goal is achieved: the ToolCase toolbox popover relocates both control groups with zero behavior change and no second state authority, and the mode-toggle directional Push tool delivers the suffix-set movement, rigid atomic one-Undo/one-Redo transaction, directional clamping, straddle guard, persistent gap/break recording, and the full drag preview surface. The automated gate is green (2383 tests, typecheck, build) and the native UAT was approved.

Three WARNING-level issues from the deep code review (43.5-REVIEW.md) remain unfixed in the current code and are documented above: WR-01 (stale blocked-direction tooltip when a blocked drag becomes valid), WR-02 (non-zero reverse-close push leaves a persistent interpolation break — a partial implementation of the D-15 normalization contract), and WR-03 (re-arm watchdog can undo a manual disarm in the async settlement window). None fails a must-have truth or blocks the phase goal; they are recommended follow-up items.

---

_Verified: 2026-08-18T13:45:00Z_
_Verifier: Claude (gsd-verifier)_
