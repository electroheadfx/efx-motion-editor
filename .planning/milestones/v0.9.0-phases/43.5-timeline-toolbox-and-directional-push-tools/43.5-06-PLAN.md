---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 06
type: execute
wave: 4
depends_on: [43.5-05]
files_modified:
  - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UAT.md
autonomous: false
requirements: [TBX-01, TBX-02, TBX-03, TBX-04, TBX-05, PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05, PUSH-06, PUSH-07, PUSH-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The full automated gate is green on the frozen candidate commit: vitest run, typecheck, build"
    - "Native visual UAT passes all 13 prompt acceptance steps plus the UI-SPEC Visual Acceptance Matrix from a fully restarted frozen-code parent/child session (43.2 D-39)"
    - "UAT evidence records the frozen commit hash and each step's pass/fail with screenshots or notes"
  artifacts: []
  key_links:
    - "frozen-code session: native approval comes from a fully restarted session, never an active hot-reload state (43.2 decision)"
---

<objective>
Freeze the implementation and run the native visual UAT — the phase's final oracle — covering the prompt's 13-step acceptance list and the UI-SPEC Visual Acceptance Matrix.

Purpose: Automated tests prove composition correctness; only the user can judge rendered popover/drag-preview fidelity and the integrated armed-tool experience (43.2 D-39).
Output: A frozen candidate commit and an approved (or gap-documented) native UAT record.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md
@SPECS/milestone-v0.9.0-new-phases/phase-43.5-timeline-toolbox-directional-push-prompt.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate + freeze candidate commit</name>
  <files>.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UAT.md</files>
  <read_first>
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-VALIDATION.md (sampling rate and phase gate)
    - SPECS/milestone-v0.9.0-new-phases/phase-43.5-timeline-toolbox-directional-push-prompt.md (13-step native acceptance list)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UI-SPEC.md (Visual Acceptance Matrix)
  </read_first>
  <action>Run the complete automated gate: pnpm --dir app exec vitest run (never watch mode), pnpm --dir app typecheck, pnpm --dir app build. Do NOT run the dev server (user runs it). If green, create the UAT checklist file 43.5-UAT.md transcribing the 13 prompt acceptance steps AND the 13 UI-SPEC matrix rows with a frozen-commit placeholder, then commit the frozen candidate and record its hash in the checklist. The checklist must include the restart instruction: the user tests from a fully restarted frozen-code parent/child session, not hot reload (43.2 D-39). If any gate fails, fix forward within the owning plan's scope and re-run before freezing.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run && pnpm --dir app typecheck && pnpm --dir app build</automated>
  </verify>
  <acceptance_criteria>
    - vitest run, typecheck, and build all exit 0 on the frozen commit
    - 43.5-UAT.md exists containing all 13 prompt steps and all 13 UI-SPEC matrix rows with the frozen commit hash recorded
    - The checklist states the fully-restarted frozen-code session requirement
  </acceptance_criteria>
  <done>Automated gates green; frozen candidate committed; UAT checklist ready for the user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Native visual UAT — 13 prompt steps + UI-SPEC matrix from a fully restarted frozen session</name>
  <what-built>The complete Phase 43.5 surface: ToolCase toolbox popover (relocated Interpolation + Key Spacing with live Blend badge and uninterrupted shortcuts), armed Push Right / Push Left tools (hover pre-highlight + pivot tick, straddle preflight, per-rail ghosts, gap preview, blocked-edge clamp bar, live status readout), and the atomic push transaction with one Undo/Redo and persistent 43.1 gap breaks.</what-built>
  <how-to-verify>
    From a FULLY RESTARTED frozen-code parent/child session (no hot reload), work through `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UAT.md` — the 13 prompt steps:
    1. Tools button opens relocated Interpolation and Key Spacing controls with unchanged values and behavior (popover above the strip; rails/cells/spacing proxies visible and clickable while open).
    2. Existing shortcuts, partial spacing, Group Rail-owned multi-Group spacing, guards, and Undo/Redo remain intact (incl. Cmd+Z and Delete/Backspace while the popover is open).
    3. Direct dragging of any Motion/Static/Key Group still moves only that Group inside free space (no neighbor push).
    4. Push Right from Group #2 moves #2 and everything after it; Group #1 stays byte-position fixed.
    5. Push Left from Group #2 moves Group #1 plus #2; everything after #2 stays fixed.
    6. Motion, Static, and Key Rails all participate in either directional set.
    7. A frame inside a Group targets the whole Group — never an implicit cut.
    8. Every push-opened gap stays empty with Interpolation On and survives Interpolation Off/On exactly; neither tool toggles Interpolation.
    9. Group synchronization status, Action provenance, source sharing, and internal timing unchanged after pushes.
    10. Frame-0/capacity clamp commits at the clamp (red bar shown); zero-space drag publishes `No empty space in that direction.` with zero mutation; straddled anchor hover shows not-allowed with the straddle tooltip and never starts a drag; zero-delta commit publishes nothing.
    11. One Undo/Redo restores and reapplies the complete push including movement-created breaks, selection, and cursor.
    12. Save/reopen (starts disarmed), Studio playback, preview, and export preserve moved sets and exact gaps.
    13. Toolbar readable, unclipped, and usable at 1280x720; strip stays exactly 161px.
    Plus the UI-SPEC matrix visual rows: badge on/off at a glance, armed tint + directional cursor, hover pre-highlight brightening + pivot tick with fixed side dark, ghosts at 55% with originals at 100%, roto-fill-empty gap preview, red blocked-edge bar on clamp, live readout only during drag.
    Record pass/fail per row in 43.5-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" with the completed 43.5-UAT.md, or describe any failing row (with screenshot) for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen build ↔ developer session | UAT evidence must come from the frozen commit, not a mutated hot-reload state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.5-07 | Repudiation | UAT evidence from a non-frozen session | medium | mitigate | Checklist requires a fully restarted frozen-code session and records the commit hash (43.2 D-39) |
| T-43.5-SC | Tampering | npm installs | high | mitigate | No package installs this phase |
</threat_model>

<verification>
- Automated gate green on the frozen commit (Task 1)
- Native UAT checklist fully passed and approved by the user (Task 2)
</verification>

<success_criteria>
All 13 prompt acceptance steps and the UI-SPEC Visual Acceptance Matrix pass on the frozen commit from a fully restarted session; Phase 43.5 is ready for `/gsd-verify-work` closure.
</success_criteria>

## Artifacts this phase produces (this plan)

- `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-UAT.md`: frozen-commit native UAT checklist and evidence record

<output>
Create `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-06-SUMMARY.md` when done
</output>
