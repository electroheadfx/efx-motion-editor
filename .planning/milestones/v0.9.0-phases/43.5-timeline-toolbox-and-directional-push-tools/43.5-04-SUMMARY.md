---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 04
subsystem: armed push drag gesture session hook
tags: [push-drag-session, injected-ports, click-passthrough, cancel-vectors, click-suppression, preview-is-the-commit, push-08-regression-boundary]

# Dependency graph
requires:
  - phase: 43.5-01-directional-push-tool-resolver-authority
    provides: clampPhysicPaintPushDestination (PUSH-05 pure clamp — the clamp authority wired by the caller), derivePhysicPaintPushSet (D-17 shared set authority)
  - phase: 43.5-03-directional-push-prepare-commit-publication-pair
    provides: prepareRotoPush (prepareAtDestination port), commitRotoPush (onDropCommit port), mapRotoPushProductReason (rejection copy)
provides:
  - usePhysicsPaintPushDrag — armed push gesture session hook owning gesture lifecycle + presentation state only; resolver/store/model enter exclusively through injected ports (T-43.5-02)
  - PUSH_DRAG_THRESHOLD_PX = 4 horizontal-only threshold (6px Euclidean ROTO_DRAG_THRESHOLD_PX path untouched)
  - Full session fail-safe matrix: sub-threshold click passthrough (D-09), Escape/pointercancel/lostpointercapture cancel vectors, no-publication rejection routing, commit-failure focus restore, once-only click suppression
  - PUSH-08 proof: usePhysicsPaintKeyRailDrag.ts and usePhysicsPaintGroupRailDrag.ts byte-untouched — direct drag stays local
affects: [43.5-05 strip paint preflight port + armed-tool wiring]

actuals:
  tokens: 7037    # chars/4 (28151 added chars) over the realized diff 0ce00b70..HEAD
  tasks: 2
  commits: 4

tech-stack:
  added: []       # no new packages
  patterns:
    - "Injected-port gesture session (43.4-02 precedent): the hook owns pointer lifecycle and presentation Signals only; projectDestination/clampDestination/prepareAtDestination/onDropCommit/onRejected/onPreviewChange/clearClickSequence/windowLike enter as ports, so the session is node-testable with a fake windowLike and imports no resolver/store/model"
    - "Click-suppression armed only past threshold (D-09 deviation): suppressNextClickRef arms in beginDrag (which runs only after >4px travel), so a sub-threshold plain click passes through unsuppressed and normal frame navigation proceeds with the tool still armed"

key-files:
  created:
    - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts — PUSH_DRAG_THRESHOLD_PX, PushDragGhostState/PushDragPreviewState/PushDragWindowLike/PushDragSourceElement/PushDragSessionInput/PushDragSessionApi, usePhysicsPaintPushDrag
    - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts — 15-test session suite (7 tracer + 8 edge-path)
  modified: []

key-decisions:
  - "The hook accepts the caller-resolved anchor and never derives canonical facts itself (D-07) — the caller binds the resolved anchor (keyId or loopId) and direction into the prepareAtDestination closure"
  - "clampDestination result is consumed for paint AND forwarded to prepare — preview-is-the-commit (D-14); the clamp authority is the resolver's exported pure clamp wired by the caller, never reimplemented in the hook"
  - "Click suppression arms only when a drag session actually started past the threshold (beginDrag) — the D-09 deviation from the key-rail hook; a sub-threshold release never arms it, so the click passes through unsuppressed"
  - "Every cancel vector (Escape, pointercancel, lostpointercapture) clears the preview signal and removes listeners exactly once via the active-flag-guarded cleanup; cancelSession restores focus and reports onCancel only when a session had started"

patterns-established:
  - "Pattern: fail-safe drop matrix (mirrored from usePhysicsPaintKeyRailDrag L244-294): no retained publication ⇒ onRejected(reason, detail) with zero mutation + focus restore; false-resolving or rejecting onDropCommit ⇒ focus restore, zero mutation; clearSuppressionSoon schedules the suppression ref clear so the post-drop click cannot re-fire selection"
  - "Pattern: presentation-only ghost/preview Signals (T-43.5-03): ghost/preview are session-only, consumed for paint only; commit consumes only the immutable retained publication"

requirements-completed: [PUSH-04, PUSH-08]

coverage:
  - id: D1
    description: "usePhysicsPaintPushDrag session hook — PUSH_DRAG_THRESHOLD_PX=4 horizontal-only threshold, injected port interface (projectDestination/clampDestination/prepareAtDestination/onDropCommit/onCancel/onRejected/onPreviewChange/clearClickSequence/windowLike), window pointermove/pointerup/pointercancel + capture-phase Escape + lostpointercapture session mechanics, pointer capture + grabbing cursor, clamp-and-prepare pipeline (preview-is-the-commit), presentation-only ghost/preview Signals, no resolver/store/model imports"
    requirement: PUSH-04
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#starts only after horizontal travel exceeds the 4px push threshold"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#captures the pointer, switches to grabbing, and releases capture on drop"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#projects the pointer delta, clamps it, and prepares the retained publication"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#retains the prepared publication by identity for preview paint and commit"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#drops: commits the retained publication exactly once and clears every preview paint"
        status: pass
    human_judgment: false
  - id: D2
    description: "Session fail-safe matrix — sub-threshold click passthrough unsuppressed (D-09), Escape cancel with stopImmediatePropagation + zero commit + zero preview residue, pointercancel/lostpointercapture cancel with listeners removed exactly once, drop with no retained publication routes to onRejected(reason, detail) with zero mutation, false-resolving/rejecting onDropCommit restores focus with zero mutation, post-drop click suppression consumed exactly once"
    requirement: PUSH-04
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#passes a sub-threshold click through unsuppressed so navigation proceeds (D-09)"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#cancels on Escape without committing, clears session paint, and stops propagation"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#cancels on pointercancel, clears preview, and removes every listener exactly once"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#cancels on lostpointercapture and clears session paint"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#routes a drop with no retained publication to onRejected with zero mutation"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#restores focus and leaves zero mutation when the commit port resolves false"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#restores focus when the commit port rejects"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts#suppresses the trailing post-drop click exactly once"
        status: pass
    human_judgment: false
  - id: D3
    description: "PUSH-08 regression boundary — usePhysicsPaintKeyRailDrag.ts and usePhysicsPaintGroupRailDrag.ts byte-untouched by this plan; push originates only from armed-tool state, direct Group/Key Rail drag stays local"
    requirement: PUSH-08
    verification:
      - kind: unit
        ref: "git diff 0ce00b70..HEAD --stat -- app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts (empty)"
        status: pass
    human_judgment: false

duration: 21min
completed: 2026-08-17
status: complete
---

# Phase 43.5 Plan 04: Armed Push Drag Session Hook Summary

**usePhysicsPaintPushDrag delivered: a node-testable armed push gesture session with injected ports, 4px horizontal threshold, clamp-and-prepare preview-is-the-commit pipeline, and a fully proven fail-safe matrix — sub-threshold click passthrough (D-09), all cancel vectors, rejection routing, commit-failure focus restore, and once-only click suppression — with direct-drag hooks byte-untouched (PUSH-08)**

## Performance

- **Duration:** 21 min (commit window 20:03:11Z Task 1 RED → 20:24:03Z Task 2 GREEN; Task 1 slice user-verified between sessions)
- **Started:** 2026-08-17T20:03:11Z (Task 1 RED)
- **Completed:** 2026-08-17T20:24:03Z (Task 2 GREEN, plus full-suite/typecheck/PUSH-08 verification)
- **Tasks:** 2
- **Files modified:** 2 (both created this plan)

## Accomplishments
- `usePhysicsPaintPushDrag` — armed push gesture session hook mirroring the UAT-approved `usePhysicsPaintKeyRailDrag` structure: arm on pointer-down with no modifiers, window pointermove/pointerup/pointercancel + capture-phase Escape (`preventDefault` + `stopImmediatePropagation`) + `lostpointercapture` cancel, pointer capture + grabbing cursor, cleanup removing every listener exactly once. Resolver/store/model enter exclusively through injected ports (T-43.5-02); the hook owns gesture lifecycle and presentation state only.
- `PUSH_DRAG_THRESHOLD_PX = 4` horizontal-only threshold (GROUP_RAIL_DRAG_THRESHOLD_PX precedent); the 6px Euclidean `ROTO_DRAG_THRESHOLD_PX` belongs to the locked physical-cell drag path and stays untouched.
- Clamp-and-prepare pipeline: `projectDestination` → `clampDestination` (the resolver's exported pure clamp wired by the caller — never reimplemented) → `prepareAtDestination` with the clamped signed delta (preview-is-the-commit, D-14). The caller binds the resolved anchor (keyId or loopId) and direction into the prepare closure (D-07); the hook never derives canonical facts itself.
- Presentation-only `useSignal` ghost/preview consumed for paint only (T-43.5-03); commit consumes only the immutable retained publication.
- Full fail-safe matrix (Task 2): sub-threshold click passes through unsuppressed so normal frame navigation proceeds with the tool still armed (D-09 deviation — suppression arms only in `beginDrag`, i.e. only after the threshold); drop with no retained publication routes to `onRejected(reason, detail)` with zero mutation and focus restore; false-resolving or rejecting `onDropCommit` restores focus with zero mutation; `consumeClickSuppression` returns true exactly once for the post-drop click; every cancel vector clears preview and removes listeners exactly once.
- PUSH-08 regression boundary proven: `usePhysicsPaintKeyRailDrag.ts` and `usePhysicsPaintGroupRailDrag.ts` byte-untouched (empty diff over the plan window).
- 15 targeted tests green (7 tracer + 8 edge-path); full suite 2341 passed (101 todo, 1 skipped); `pnpm --dir app typecheck` clean.

## Task Commits

Each task was committed atomically (TDD RED-then-GREEN):

1. **Task 1 (tracer): End-to-end push drag session — threshold, capture, preview, drop commit via injected ports** — `cd7a8ba5` (test, 7 failing), `7831f711` (feat). **User-verified** — the tracer slice was accepted before Task 2 resumed.
2. **Task 2: Session edge paths — click passthrough, cancel vectors, rejection, commit failure, click suppression** — `ffe8e8d3` (test, 5 failing), `77a6d4df` (feat).

**Plan metadata:** pending — final metadata commit follows this SUMMARY.

## Files Created/Modified
- `app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts` — `PUSH_DRAG_THRESHOLD_PX`, `PushDragGhostState` (active/deltaFrames/blockedEdge), `PushDragPreviewState<Publication>`, `PushDragWindowLike`, `PushDragSourceElement`, `PushDragProjectionInput`, `PushDragClampResult`, `PushDragPreparationResult`, `PushDragSessionInput<Publication>`, `PushDragSessionApi<Publication>` (onPointerDown/ghost/preview/consumeClickSuppression), `usePhysicsPaintPushDrag`
- `app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts` — fake `WindowDouble`/`SourceDouble` with `removeEventListener` spy counts; 15 tests: 4px threshold, capture/grabbing/release, clamp-then-prepare, identity retention, rejected-destination paint clear, drop commit exactly once, guarded pointer-down, sub-threshold passthrough (D-09), Escape cancel, pointercancel, lostpointercapture, no-publication rejection, commit-false focus restore, commit-throw focus restore, once-only click suppression

## Decisions Made
- The hook accepts the caller-resolved anchor and never derives canonical facts itself (D-07) — the caller binds the resolved anchor and direction into the `prepareAtDestination` closure.
- `clampDestination` result is consumed for paint AND forwarded to prepare — preview-is-the-commit (D-14); the clamp authority is the resolver's exported pure clamp wired by the caller, never reimplemented in the hook.
- Click suppression arms only when a drag session actually started past the threshold (`beginDrag`) — the D-09 deviation from the key-rail hook; a sub-threshold release never arms it, so the click passes through unsuppressed.
- Every cancel vector clears the preview signal and removes listeners exactly once via the active-flag-guarded cleanup; `cancelSession` restores focus and reports `onCancel` only when a session had started.

## Deviations from Plan
None - plan executed exactly as written. Task 1's tracer slice was user-verified (checkpoint resume) and Task 2 completed the fail-safe matrix per the plan's behavior bullets.

## Issues Encountered
- None. The RED phase for Task 2 failed exactly the 5 expected tests (Escape onCancel, no-publication onRejected, commit-false focus, commit-throw focus, suppression consume); the sub-threshold/pointercancel/lostpointercapture tests passed immediately as characterization of existing Task 1 behavior. GREEN made all 15 pass.

## User Setup Required
None - no external service configuration required. No package installs this plan.

## Next Phase Readiness
- Plan 43.5-05's strip wiring consumes the session ports: `projectDestination`, `clampDestination` (the resolver's exported pure clamp from 43.5-01), `prepareAtDestination` (`prepareRotoPush` from 43.5-03), `onDropCommit` (`commitRotoPush` from 43.5-03), `onRejected`, `onPreviewChange`, `clearClickSequence`, and `windowLike` — the hook is the gesture-layer owner; 43.5-05 wiring is pure presentation.
- Armed/disarm state itself lives in session-only Signals outside the gesture hook (D-19: never persisted, never in document/history) — 43.5-05 owns that wiring.
- The D-09 click-passthrough contract is proven: a sub-threshold plain click on a valid anchor while armed performs normal frame cursor navigation and keeps the tool armed.

## TDD Gate Compliance
- Task 1: `test(43.5-04)` commit (cd7a8ba5, 7 failing tests) then `feat(43.5-04)` commit (7831f711) — gates present
- Task 2: `test(43.5-04)` commit (ffe8e8d3, 5 failing tests) then `feat(43.5-04)` commit (77a6d4df) — gates present
- All gates verified in git log order: test before feat for every task

## Self-Check: PASSED
- Both created files exist and carry the expected content (verified during execution)
- All 4 plan commits present in git log (cd7a8ba5, 7831f711, ffe8e8d3, 77a6d4df)
- 15 targeted tests green; full suite 2341 passed (101 todo, 1 skipped); `pnpm --dir app typecheck` green; PUSH-08 regression grep empty

---
*Phase: 43.5-timeline-toolbox-and-directional-push-tools*
*Completed: 2026-08-17*
