---
phase: 43.5-timeline-toolbox-and-directional-push-tools
plan: 04
type: execute
wave: 2
depends_on: [43.5-01]
files_modified:
  - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts
autonomous: true
requirements: [PUSH-04, PUSH-08]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A pointer down on a valid anchor while armed, dragged past the 4px horizontal threshold, starts a push session with pointer capture, grabbing cursor, and clamped-destination preview recomputation on every move (D-06, D-08)"
    - "A sub-threshold plain click performs normal frame cursor navigation and keeps the tool armed — the session never swallows the click (D-09)"
    - "Capture-phase Escape cancels the session with stopImmediatePropagation, zero state change, and zero history; pointercancel and lostpointercapture also cancel"
    - "Drop with a retained publication commits via the injected onDropCommit port; commit failure restores focus with zero mutation; drop with no retained publication routes to onRejected"
    - "The hook owns gesture lifecycle only — resolver/store/model enter exclusively through injected ports; injectable windowLike keeps the session node-testable"
    - "Direct Group/Key Rail drag hooks remain byte-untouched — push originates only from armed-tool state (PUSH-08 regression boundary)"
  artifacts:
    - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts — new armed push gesture session hook
    - app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts — new session test suite
  key_links:
    - "ports consumed by plan 43.5-05 wiring: projectDestination, clampDestination (the resolver's exported pure clamp from 43.5-01 — preview-is-the-commit), prepareAtDestination (prepareRotoPush from 43.5-03), onDropCommit (commitRotoPush from 43.5-03), onRejected, onPreviewChange, clearClickSequence, windowLike"
    - "preview/ghost state is session-only Signals consumed for paint only; commit consumes only the immutable retained publication (43.3 D-01)"
---

<objective>
Create the armed push drag session hook — the gesture-layer owner for Push Right/Push Left — mirroring the UAT-approved Key Rail drag session structure with push-specific anchor and click-passthrough deviations.

Purpose: Isolates all pointer-session complexity in one focused, node-testable sibling hook (43.4-02 precedent) so plan 43.5-05 wiring is pure presentation (PUSH-04).
Output: `usePhysicsPaintPushDrag` with injected ports and a full session test suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-CONTEXT.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md
@.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end push drag session — threshold, capture, preview, drop commit via injected ports</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts (full file — port interface L76-89, Signals L126-129, cleanup L174-190, beginDrag L216-233, drop commit L244-270, Escape L289-294, threshold constant L5)
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts (full file — test structure to mirror)
    - app/src/components/physic-paint/hooks/usePhysicsPaintGroupRailDrag.ts (clamp-port shape)
    - .planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-PATTERNS.md (verbatim port/session excerpts and push deviations)
  </read_first>
  <behavior>
    - RED: session test — pointer down on a valid anchor (armed), move 5px horizontally: session starts, clampDestination called with the projected destination, prepareAtDestination produces a retained publication, onPreviewChange receives the preview state, pointer capture set, cursor grabbing
    - RED: drop: onDropCommit called once with the retained publication; preview cleared after commit
    - GREEN: minimal hook implementation passes both
  </behavior>
  <action>Create app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts copying the usePhysicsPaintKeyRailDrag port/session structure verbatim: export PUSH_DRAG_THRESHOLD_PX = 4 (horizontal-only; the 6px Euclidean ROTO_DRAG_THRESHOLD_PX belongs to the locked cell-drag path and stays untouched); a port interface carrying projectDestination, clampDestination (result consumed for paint AND forwarded to prepare — preview-is-the-commit, the clamp authority is the resolver's pure export from 43.5-01), prepareAtDestination, onDropCommit, onCancel, onRejected, onPreviewChange, clearClickSequence, and an injectable windowLike. Session mechanics copied from the key-rail hook: window pointermove/pointerup/pointercancel listeners, pointer capture on the source element, grabbing cursor during the session, capture-phase Escape with preventDefault + stopImmediatePropagation canceling the session, lostpointercapture cancel, cleanup removing every listener. Presentation-only state in useSignal (ghost/preview), consumed for paint only; commit consumes only the immutable retained publication. PUSH DEVIATIONS (CONTEXT D-07/D-09): the anchor descriptor passed to the ports is any non-empty cell frame OR rail band (not only a rail target button) — the hook accepts the resolved anchor from its caller and never derives canonical facts itself; and a sub-threshold release must NOT swallow the click — the session lets the strip's normal cell click run so plain-click navigation proceeds with the tool still armed. Create usePhysicsPaintPushDrag.test.ts mirroring usePhysicsPaintKeyRailDrag.test.ts with a fake windowLike (file named *.test.ts — Pitfall 8; vitest includes only src/**/*.test.ts).</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - usePhysicsPaintPushDrag.ts exports PUSH_DRAG_THRESHOLD_PX (= 4), the port interface, and the hook
    - RED-then-GREEN: the session tests failed before the hook existed and pass after
    - The hook contains no resolver/store/model imports (ports only) — grep the import block to confirm
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>A full push drag gesture — down, past-threshold move, clamped preview, drop, commit — runs end-to-end through injected ports with tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session edge paths — click passthrough, cancel vectors, rejection, commit failure, click suppression</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts, app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts (lines 244-294 — drop/no-publication rejection, commit failure focus restore, click suppression ref, cancel vectors)
    - app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.test.ts (edge-case test idioms)
  </read_first>
  <behavior>
    - Sub-threshold release: no session starts, no prepare call, the click is not suppressed — normal frame navigation proceeds and the tool stays armed (D-09)
    - Escape during an active session: cancel with zero commit call, zero preview residue, stopImmediatePropagation observed
    - pointercancel / lostpointercapture: session canceled, listeners removed, preview cleared
    - Drop with no retained publication: onRejected invoked with reason/detail; zero mutation
    - Commit failure (onDropCommit resolves false): focus restored, zero mutation, no accepted status
    - Post-drop click cannot re-fire selection (suppression ref consumed once)
  </behavior>
  <action>Complete the session's fail-safe matrix in usePhysicsPaintPushDrag.ts, copying the key-rail hook's proven vectors: drop with no retained publication routes to onRejected(reason, detail) with zero mutation; a false-resolving onDropCommit restores focus and mutates nothing; the click-suppression ref is consumed exactly once so the post-drop click cannot re-fire selection — EXCEPT the sub-threshold case, where the click must pass through unsuppressed (D-09 deviation from the key-rail hook; guard the suppression so it arms only when a drag session actually started past the threshold). Ensure every cancel vector (Escape, pointercancel, lostpointercapture) clears the preview signal and removes listeners exactly once. Extend the test suite with one test per behavior bullet, using the fake windowLike; assert listener removal via the fake's removeEventListener spy counts.</action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts && pnpm --dir app typecheck && pnpm --dir app exec vitest run</automated>
  </verify>
  <acceptance_criteria>
    - One passing test per behavior bullet above
    - grep confirms usePhysicsPaintKeyRailDrag.ts and usePhysicsPaintGroupRailDrag.ts are byte-untouched by this plan (PUSH-08 regression boundary — direct drag stays local)
    - Full suite + typecheck green
  </acceptance_criteria>
  <done>Every cancel/failure/passthrough path is proven with zero mutation and zero preview residue; existing drag hooks untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer events → session → prepare/commit ports | Gesture input is untrusted; the session must never mutate canonical state directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43.5-02 | Tampering | UI-derived authority (anchor/attachment) spoofing | high | mitigate | The hook accepts only the caller-resolved anchor and delta; all canonical derivation happens in the resolver via injected ports (Pitfall 6); the hook imports no store/model |
| T-43.5-03 | Tampering/Repudiation | preview presenting uncommitted state as accepted | medium | mitigate | Preview state is session-only Signals; originals stay at 100% until exact parent acknowledgement; every cancel vector clears preview residue — Task 2 tests |
| T-43.5-SC | Tampering | npm installs | high | mitigate | No package installs this phase |
</threat_model>

<verification>
- `pnpm --dir app exec vitest run src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts` green
- `pnpm --dir app exec vitest run` + `pnpm --dir app typecheck` green before wave merge
</verification>

<success_criteria>
The push gesture session is fully proven in isolation: happy path, click passthrough, all cancel vectors, rejection, and commit failure — with direct-drag hooks untouched.
</success_criteria>

## Artifacts this phase produces (this plan)

- `app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.ts`: `usePhysicsPaintPushDrag` hook, `PUSH_DRAG_THRESHOLD_PX`, push session port/preview/ghost types
- `app/src/components/physic-paint/hooks/usePhysicsPaintPushDrag.test.ts`: full session test suite

<output>
Create `.planning/phases/43.5-timeline-toolbox-and-directional-push-tools/43.5-04-SUMMARY.md` when done
</output>
