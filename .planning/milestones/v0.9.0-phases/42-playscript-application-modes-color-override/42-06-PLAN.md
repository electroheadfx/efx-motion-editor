---
phase: 42-playscript-application-modes-color-override
plan: 06
type: execute
wave: 6
depends_on:
  - 42-05
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
  - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - PLAY-03
  - PLAY-04

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Play Script dialog is a compact independent modal overlay ABOVE the Paint interface: canvas and Studio layout stay in place behind it, it never occupies the canvas grid cell, never resizes the Paint layout, and closes (Cancel/Escape/success) without changing the Paint layout (D-19; Image #153 target / Image #154 rejected)"
    - "The modal uses its own dialog-scoped DARK tokens verbatim from the approved proposal (--ps-surface/raised/inset/foot/fg/muted/faint/border/accent/accent-hi/ok/error/radius) — the old light Play Script palette is fully removed from the modal scope (D-19)"
    - "All selectors stay inside the .physics-paint-play-script-* modal scope — surrounding Paint UI is never restyled (D-19)"
    - "Compact header: 'Play Script' left, 'Max {N} · F{start}–F{end}' right, one header row (D-16)"
    - "Main-options grid: LEFT Timing card (Frames / Frames per cycle + helper/validation, Repeat + Infinity + helper/validation) occupies the full row height; RIGHT column stacks Color on top and Motion wiggle directly underneath; Color + gap + Motion wiggle combined height EQUALS the Timing card height; Motion wiggle is never a full-width row and never above Color (D-16)"
    - "Motion wiggle card: 'Motion wiggle' heading with compact 'Reset defaults' heading link; Deformation and Position compact slider rows with visible numeric values; no unused vertical space (D-16)"
    - "Row 4 full-width Requested/Effective summary bar (Requested left, Effective right, truncation clause when applicable); Row 5 compact footer INSIDE the modal (progress/status + Cancel + Generate) — never attached to the canvas/window bottom (D-16)"
    - "No-scroll compact fit: at the minimum supported application window size all modal rows are visible with NO vertical or horizontal scrollbar, no body scrolling region, no clipped card/helper/summary/footer (D-19)"
    - "Sole trigger preserved: the existing Scripts-panel Play Script toolbar action with its existing guards; no demo trigger copied, no auto-open, no second action (D-17)"
    - "Functional contracts unchanged: one-source-cycle generation, loop-intent Repeat/Infinity, live Custom color (read-only toward settings.color, snapshot at Generate), success-only summary, E5 atomicity, application-time Motion, source-script/thumbnail immutability, erase exclusion (D-02/D-08R/D-11/D-14/D-18)"
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (modal shell + revised grid markup)
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx (overlay mount — canvas untouched behind)
    - app/src/components/physic-paint/physicsPaintStudio.css (modal-scoped dark restyle, compact metrics; old light play-script rules removed within the scope)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts (structural cases for the revised layout)
  key_links:
    - "Same controller interface as 42-05 (mode, overrideEnabled, dialogMotion, repeatText/infinity via setInfinity, repeatError, loopReadout, resetDialogMotion, error, canCancel) — zero controller changes in this plan"
    - "Proposal dialog CSS block (~line 214) is the verbatim token/metric source; the demo shell and trigger are NOT product UI (D-17)"
  prohibitions:
    - "Never render the dialog as a full-page/full-height editor surface or inside the canvas grid cell (D-19)"
    - "Never retain the old light Play Script palette or large light cards inside the modal scope (D-19)"
    - "Never solve the height constraint with a scrolling body region (D-19)"
    - "Never multiply frameCount by Repeat or feed Effective duration into generation (D-02/D-14)"
    - "Never write, seed, normalize, or mutate settings.color from the dialog or controller (D-18)"
    - "Never add/copy a demo trigger, auto-open, or a second Play Script action (D-17)"
    - "Never modify the controller, renderer, panel summary block, or InlineColorPicker.tsx in this plan"
    - "Never display the term 'clip bloquant' or any French loop label in Phase 42 UI"
---

<objective>
Correct the Play Script dialog's visual architecture to the approved target: a compact, dark, independent modal overlay (Image #153) replacing the rejected light full-canvas implementation (Image #154) — with the final Timing-left / Color-over-Motion-right main grid and a no-scroll compact fit.

Purpose: UAT visual correction only. All functional contracts from 42-02/42-05 stay untouched; this plan re-mounts and restyles the dialog and restructures its middle section.
Output: modal overlay mount + dialog-scoped dark restyle + revised grid + updated structural tests.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md
@.planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md
@.planning/phases/42-playscript-application-modes-color-override/42-05-SUMMARY.md
@SPECS/playscript-proposal/playscript-panel.html
@SPECS/playscript-proposal/ui-play-script-specs.md
@app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modal overlay re-mount + compact dark restyle (shell, header, footer)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx (current dialog mount — how it occupies the canvas grid cell; what renders behind it)
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (current shell/header/footer markup, focus-trap Tab query, status line carrying 'Max N · F..–F..')
    - app/src/components/physic-paint/physicsPaintStudio.css (current .physics-paint-play-script-* rules — full inventory before rewrite)
    - SPECS/playscript-proposal/playscript-panel.html (dialog CSS block ~line 214: .dialog, .dialog-head, .dialog-foot, .summary, .btn, .progress-* — token/metric source; root tokens ~lines 9-22)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Modal Architecture, Color, Typography, Spacing tables — revised)
  </read_first>
  <behavior>
    - When confirmationOpen is true the dialog renders as a centered modal overlay above the Paint interface: the canvas and Studio layout remain mounted in their normal location behind it (no grid-cell occupation, no layout resize); a dimmed backdrop layer sits between the Paint UI and the modal (proposal modal semantics; backdrop click is NOT wired — close only via Cancel/Escape/success per D-17/D-19)
    - Compact header row: 'Play Script' (15px/650) left, `Max {N} · F{start}–F{end}` (11.5px muted, tabular-nums) right, 1px bottom border
    - Compact footer INSIDE the modal: progress track + status left, Cancel/Generate right, --ps-foot background, 1px top border; generation failure renders the reason in --ps-error above the footer actions (existing E5 contract, restyled)
    - The modal shell: width 700px, max-width 94%, max-height 96%, radius 14px, --ps-surface background, --ps-border border, proposal shadow, flex column, overflow hidden
    - All old light-palette play-script rules are removed from the modal scope; the full rule set is the proposal-derived dark token set declared in UI-SPEC (custom properties on the modal root scope class)
    - Focus trap and Escape/Enter keyboard contract keep working inside the overlay
  </behavior>
  <action>
    In PhysicsPaintStudioView.tsx, change the dialog mount so it no longer occupies the canvas grid cell: render the modal as an overlay (out-of-flow positioning; the canvas subtree stays mounted in place regardless of confirmationOpen). In PhysicsPaintPlayScriptDialog.tsx, restructure the shell: backdrop layer + centered modal; header row (title left, range right — reuse the existing status composition for `Max {N} · F{start}–F{end}`); keep the existing control bindings verbatim (mode radiogroup, fields, color toggle, sliders, summary data, actions) while moving the range text out of the old status placement. In physicsPaintStudio.css, REWRITE the .physics-paint-play-script-* rule set: declare the --ps-* custom properties on the modal root class; overlay/backdrop positioning (fixed inset covering the Studio area, dimmed backdrop, high z-index inside the Studio stacking context); modal shell, compact header/footer, summary bar, inputs, segmented controls, checkbox, sliders, buttons, progress — all per the UI-SPEC Spacing/Color/Typography tables; DELETE every old light-palette dialog rule inside the scope (surface #f7f5ef, white cards, 42px inputs, clamp paddings, large gaps). Update the dialog test for the new shell structure (header range placement, backdrop presence, footer inside modal) and run the dialog + panel suites; commit: feat(42-06): re-mount play script dialog as compact dark modal overlay.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0
    - Studio view keeps the canvas subtree mounted when the dialog opens; the dialog element is out of the canvas grid cell (structural assertion in the dialog test or a view test)
    - Dialog header contains the title left and the `Max` range right; footer renders inside the modal element
    - physicsPaintStudio.css diff removes the old light play-script rules and adds only .physics-paint-play-script-* selectors carrying the --ps-* dark tokens
    - No rule outside the .physics-paint-play-script-* scope is modified
    - pnpm --dir app typecheck exits 0
  </acceptance_criteria>
  <done>The dialog opens as a compact dark modal overlay above an untouched Paint layout, with the compact header/footer and the full proposal token set.</done>
</task>

<task type="auto">
  <name>Task 2: Final grid — Timing left full-height, Color over Motion wiggle right, no-scroll compact fit</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx, app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <read_first>
    - app/src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.tsx (post-Task-1 markup)
    - SPECS/playscript-proposal/playscript-panel.html (.dialog-body grid, .group, .field, .check, .color-pane, .slider-row, .summary — metric source)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Layout diagram D-16 — equal-height contract)
  </read_first>
  <behavior>
    - Main-options grid (Row 3): two equal columns; LEFT is the single Timing card spanning the full row height (Frames / Frames per cycle + help/error, Repeat + Infinity + help/error); RIGHT is a stacked column: Color card on top, Motion wiggle card directly underneath, internal gap from the body grid gap
    - Combined height of Color + gap + Motion wiggle EQUALS the Timing card height (same row track; right column is a flex/grid container filling the row, cards sized so nothing overflows and no dead area remains); individual card heights may differ
    - Motion wiggle card: heading with 'Reset defaults' link; Deformation and Position compact slider rows (grid 88px 1fr 26px) with visible outputs; no unused vertical space
    - Color card: segmented control + one compact pane (original dots row OR custom chip/hex row) — no large empty reserved area
    - Row 4 summary bar full width: Requested left, Effective right, truncation clause when applicable
    - No-scroll compact fit: NO overflow-y:auto body region anywhere in the modal; at the minimum supported application window size every row renders fully (structural CSS guarantee: compact metrics + max-height 96% with content sized to fit, no scroll containers)
  </behavior>
  <action>
    Restructure the dialog body markup into the D-16 final grid: Mode card (span-2), then a main-options row — left Timing card (move the frame field + Repeat/Infinity block in, with their help/error spans), right stacked column (Color card, then Motion wiggle card with the heading link and the two compact slider rows), then the summary bar (span-2). CSS: body grid `grid-template-columns: 1fr 1fr; gap: 14px 24px`; Mode and summary span both columns; the main row's columns are the Timing card and a right-column flex container (`display: flex; flex-direction: column; gap: 14px`) whose two cards share the row height exactly (Timing stretches to the row track; right column fills it; Color card content compact with the pane centered per the proposal's `.color-pane`; Motion wiggle card gets the remaining space without inner dead area). Remove any overflow-y:auto from the dialog body; ensure compact metrics make the whole modal fit at the minimum window size. Extend the dialog test with structural cases: (a) Timing card is the left sibling of the right-column stack; (b) Color card precedes Motion wiggle inside the right column; (c) the body has no scroll-region class/style; (d) the summary bar renders Requested left / Effective right after the main grid; (e) all prior behavioral cases (radiogroups, live chip, labels, E5) stay green. Run the dialog + panel + controller suites; commit: feat(42-06): finalize play script modal grid — timing left, color over motion right, no-scroll compact fit.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/view/PhysicsPaintPlayScriptDialog.test.ts src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with structural cases (a)-(e) green
    - Dialog markup matches the D-16 final grid (Mode → Timing | Color-over-Motion → summary → footer)
    - No overflow-y:auto / scroll container remains in the modal scope
    - physicsPaintStudio.css diff touches only .physics-paint-play-script-* selectors
    - Full app suite (`pnpm --dir app exec vitest run`) + package animation tests + pnpm --dir app typecheck all exit 0
  </acceptance_criteria>
  <done>The modal matches Image #153's structure: Timing left at full height, Color over Motion wiggle right with equal total height, summary bar beneath, footer inside — no scrolling anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal CSS scope → Studio | dark tokens must not leak outside the modal scope |
| user → dialog inputs | unchanged (controller validation remains the single gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-06-01 | Tampering | CSS scope leakage | low | mitigate | Acceptance gate: diff confined to .physics-paint-play-script-* selectors; no pre-existing rule modified |
| T-42-06-SC | Tampering | package installs | low | accept | No package installs (zero new dependencies) |
</threat_model>

## Artifacts this phase produces

This plan creates:
- Modal overlay mount in `PhysicsPaintStudioView` (canvas untouched behind the modal)
- Revised `PhysicsPaintPlayScriptDialog`: backdrop + compact dark shell, compact header (title + range), D-16 final grid, summary bar, in-modal footer
- Rewritten `.physics-paint-play-script-*` CSS (dark --ps-* token set, compact metrics; old light rules removed)
- Extended `PhysicsPaintPlayScriptDialog.test.ts` (structural layout cases)

<verification>
- Task gates: per-task `<automated>` commands exit 0
- Wave gate: `pnpm --dir app exec vitest run` + package animation tests (`NODE_PATH=app/node_modules app/node_modules/.bin/vitest --run --config /dev/null packages/efx-physic-paint/src/animation/staticStrokeSchedule.test.ts packages/efx-physic-paint/src/animation/progressiveStrokeSchedule.test.ts`) + `pnpm --dir app typecheck` all exit 0
- Renderer/controller byte-untouched: this plan modifies no controller/renderer file
</verification>

<success_criteria>
- Modal is recognizably the compact dark overlay from Image #153 — never the rejected light page from Image #154
- D-16 final grid with equal left/right main-row heights; footer inside the modal; no scrolling
- All 42-02/42-05 functional contracts preserved (controller/renderer untouched)
</success_criteria>

<output>
Create `.planning/phases/42-playscript-application-modes-color-override/42-06-SUMMARY.md` when done
</output>
