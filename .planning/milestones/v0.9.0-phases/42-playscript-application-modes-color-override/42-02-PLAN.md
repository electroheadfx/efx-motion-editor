---
phase: 42-playscript-application-modes-color-override
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
  - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "mode='static' routes rendering through buildStaticStrokeSchedule/getStaticFrameStrokes; mode='progressive' keeps the existing progressive path byte-identical in behavior (PLAY-01, D-01)"
    - "The color override recolors paint strokes only in BOTH modes; erase strokes (tool === 'erase') pass through untouched in Progressive and Static / Hold alike (PLAY-02, D-11)"
    - "The override is applied AFTER transformRecordedStrokeForHeldPose, so override renders have point-for-point identical geometry to original-color renders under nonzero Motion (Pitfall 2, PLAY-02)"
    - "The input script snapshot remains deeply equal to its pre-render value after generation, and a deep-frozen script input renders without any mutation attempt (PLAY-02 source-immutability invariant)"
    - "Confirm never invokes any script-library write port; the source script document, its metadata, and thumbnail remain byte-identical (PLAY-02 contract test — manually reviewed and covered by these explicit renderer/controller invariants)"
    - "Generated frame count equals the Frames/Cycle frames field only — repeat/infinity never multiply generation (D-02, D-03, D-14)"
    - "Repeat is a positive finite integer; the authoritative maximum is derived BEFORE multiplication as floor(Number.MAX_SAFE_INTEGER / cycleLength) against the current cycle value, so the accepted cycle × repeat product is always Number.isSafeInteger; unsafe or out-of-range values block Generate with the inline error 'Repeat is too large for this cycle length.' — distinct from the format error 'Enter a positive integer.' (edge: PLAY-04 boundary/precision, D-12, D-15)"
    - "Infinity is a separate boolean that never enters numeric multiplication; toggling on preserves the last valid finite Repeat (including a large valid value), toggling off restores it; requested duration renders the literal form `Cycle {N}f × ∞` (edge: PLAY-04 precision, D-12, Pitfall 7)"
    - "Re-running Generate with identical script/options produces identical staged output — deterministic, and a second run replaces via the existing atomic path (edge: PLAY-04 idempotency)"
    - "Cancellation or failure mid-generation leaves zero partial destination frames and zero timeline mutations; only one generation runs at a time via the existing generation token (edge: PLAY-04 concurrency; UI-SPEC E5)"
    - "Generation-error lifecycle is explicit: starting a generation clears the stale error; on failure progress is cleared (progress bar hides), phase becomes 'failed', the dialog stays open, inputs re-enable, and the failure reason is exposed on the controller error channel; retry starts from a clean error state; normal user cancellation returns phase 'cancelled' with the error channel null — never an error shown (UI-SPEC E5)"
    - "Field validation and generation failure are never ambiguous: validationError (frames) and repeatError (repeat) are field channels; error is the generation channel — separate signals, separate dialog placements (UI-SPEC E1/E5)"
    - "resetDialogMotion(): void is the explicit controller boundary for Reset to Motion defaults — openConfirmation initializes dialogMotion from ports.getMotion(), edits stay application-time only, reset re-reads the CURRENT defaults through the port, and reset never writes to Motion panel defaults, project data, app config, or per-stroke properties (D-06; UI-SPEC E3)"
    - "Effective duration = min(requested, layerEndExclusive − canonicalStart) from retained authority signals; the untruncated readout omits the truncation clause (edge: PLAY-03 partial / UI-SPEC E4, D-13, Pitfall 5)"
    - "appliedSummary composes atomically from the options and destination actually committed: line 1 '{Mode} · {Original colors | Override #rrggbb} · Motion {deformation}/{position}', line 2 'F{start}–F{end} · {count} frames generated' with end = start + count − 1 (exact at the layer-end boundary, no off-by-one); before the first successful Generate the locked first-time defaults show (line 2 'No frames generated yet'); unsaved edits, dialog Cancel, generation cancellation, and generation failure leave both previous lines byte-identical (D-07; UI-SPEC E5/E6 update contract)"
    - "All option state lives in controller signals for the session only — nothing is written to project data or app config (D-10)"
  artifacts:
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (extended: option signals, resetDialogMotion, generation-error lifecycle, loop readout with safe-product bound, applied-summary composer)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts (extended: mode + overrideColor input, schedule selection, post-Motion color substitution)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (extended)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts (extended)
  key_links:
    - "Controller confirm passes mode/overrideColor/dialog-Motion into renderRotoPlayScriptFrames — the single renderer entry point both modes share"
    - "layerEndExclusive signal is set in openConfirmation alongside capacity — the loop readout's only boundary source (Pitfall 5)"
    - "Color substitution happens inside the existing per-frame transform callback in the renderer — after the Motion transform, before engine render"
    - "resetDialogMotion, repeatError, setInfinity, error (generation channel), and appliedSummary are the exact controller-interface names 42-03 and 42-04 consume — no dialog/panel access to controller construction ports"
  prohibitions:
    - "Never use the engine AnimationStrokeStyleOverride/strokeStyleOverride API for the color override (flattens tool/params — D-11)"
    - "Never recolor erase strokes or alter erase behavior (D-11)"
    - "Never write to the source script document, its thumbnail, or any script-library write port during application (PLAY-02)"
    - "Never apply the color override before transformRecordedStrokeForHeldPose (color is hashed into the determinism seed — recordedStrokeMotion.ts:51)"
    - "Never write dialog Motion values back to Motion panel defaults, physicPaintStore, or per-stroke properties (D-06)"
    - "Never persist Play Script options to project data, LazyStore, or app config (D-10)"
    - "Never generate repeat/infinity frames in Phase 42 — cycle-only generation (D-02)"
    - "Never let loop truncation block, warn-gate, or alter generation (D-14)"
    - "Never feed Infinity into numeric multiplication (Pitfall 7)"
---

<objective>
Wire the Play Script controller and renderer for two-mode generation with an application-time color override and Hold Loop option state — the phase's end-to-end core. Leads with a tracer: one static/hold generation path with override, proven controller→renderer→staged frames by automated tests. Then locks the three controller boundaries the dialog and panel consume: the safe-product Repeat bound, the explicit reset/generation-error/applied-summary surface.

Purpose: Everything the dialog (42-03) and panel (42-04) render is projected from the controller state built here; the renderer's schedule-selection + color-substitution seams are the only engine-adjacent changes in the phase.
Output: Extended controller (mode/overrideColor/dialogMotion/repeat/infinity/layerEndExclusive signals, resetDialogMotion, repeatError, setInfinity, loopReadout, generation-error lifecycle, appliedSummary composer) + extended renderer (mode, overrideColor) with full test coverage.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md
@.planning/phases/42-playscript-application-modes-color-override/42-RESEARCH.md
@.planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md
@.planning/phases/42-playscript-application-modes-color-override/42-01-SUMMARY.md
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts
@app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts
@packages/efx-physic-paint/src/animation/recordedStrokeMotion.ts
@packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "static/hold generation with color override" — one path through controller and renderer, with the full PLAY-02 invariant set</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts (full file — schedule seam at the buildProgressiveStrokeSchedule/getProgressiveFrameStrokes call sites, RotoPlayScriptRenderInput, flattenScriptStrokes/cloneStroke)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (full file — signal group, openConfirmation authority retention, confirm renderer invocation, parseCount)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts (vi.mock harness for '@efxlab/efx-physic-paint' and '/animation')
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (harness/authority factories, renderer-input assertion pattern)
    - packages/efx-physic-paint/src/animation/staticStrokeSchedule.ts (42-01 export names/signatures)
    - packages/efx-physic-paint/src/animation/recordedStrokeMotion.ts (transform signature; color-in-seed line)
    - .planning/phases/42-playscript-application-modes-color-override/42-PATTERNS.md (renderer + controller sections)
  </read_first>
  <behavior>
    - RED first — new failing renderer cases: (a) mode 'static' invokes the static schedule builder/accessor, not the progressive pair; (b) overrideColor recolors paint strokes only in BOTH modes — a tool 'erase' stroke passes the transform callback unrecolored under mode 'static' AND under mode 'progressive'; (c) geometry parity — the stroke handed to transformRecordedStrokeForHeldPose carries the ORIGINAL color whether or not overrideColor is set, and the rendered point arrays of an override run are point-for-point identical to an original-color run under nonzero Motion (deformation/position > 0) in both modes; (d) default mode ('progressive' when mode omitted or 'progressive') keeps calling the progressive pair exactly as today; (e) source immutability — the input script snapshot is deeply equal to its pre-render structuredClone after generation in both modes (override and no-override); (f) a deep-frozen script input (Object.freeze applied recursively to brushes, strokes, points, params) renders successfully with zero mutation attempts in both modes
    - RED first — new failing controller cases: (g) confirm calls renderRotoPlayScriptFrames with expect.objectContaining({ mode, overrideColor, motion: dialog Motion values }) where motion comes from dialog signals initialized from ports.getMotion() at openConfirmation, not re-read from the port at confirm time; (h) confirm never invokes any script-library write port and the snapshot's metadata fields (name, thumbnail) are never reassigned — the harness library exposes read-only loadSnapshot and spied write-capable members that must see zero calls
  </behavior>
  <action>
    RED: add the failing cases (a)-(h) to both test files (extend the animation module mock with buildStaticStrokeSchedule/getStaticFrameStrokes vi.fn()s; build the erase-capable stroke fixtures from the 42-01 factory; structuredClone the script fixture pre-render for the deep-equal assertion; write a recursive deep-freeze helper for case (f)); run the quick command, confirm red. GREEN, renderer: extend RotoPlayScriptRenderInput with mode ('progressive' | 'static', defaulting to progressive when absent) and overrideColor (string | null, default null); at the existing schedule seam select buildStaticStrokeSchedule/getStaticFrameStrokes when mode is 'static', else the progressive pair — no other render-pipeline line changes (engine init, alpha merge, PNG encode, memory guards, abort all stay shared); inside the existing per-frame transform callback, compute transformed = transformRecordedStrokeForHeldPose(stroke, { destinationSourceFrame, strokeIndex, deformation, position }) first, then return a shallow copy of transformed with color replaced by input.overrideColor only when overrideColor is non-null AND transformed.tool !== 'erase', otherwise return transformed (per D-11; implements PLAY-02). The existing flattenScriptStrokes/cloneStroke deep-clone discipline already guarantees cases (e)-(f) — do not weaken it. GREEN, controller: add signals mode ('progressive' | 'static', default 'progressive'), overrideColor (string | null, default null), overrideEnabled (boolean, default false — owned separately from any picker per D-09/Pitfall 3), dialogMotion ({ deformation, position }); in openConfirmation initialize dialogMotion from ports.getMotion() (D-06 init-from-defaults); at confirm, build the renderer input motion from dialogMotion.value and pass mode/overrideColor (overrideColor only when overrideEnabled, else null); expose all new signals on the RotoPlayScriptController interface and returned object. Generation count semantics are UNCHANGED — count stays the Frames/Cycle frames value and buildPhysicalPublication's staged.length === count validation is untouched (D-02/D-03; progressive default-options output stays byte-identical). Run quick command until green; commit: feat(42-02): wire static/hold mode and color override through controller and renderer.
  </action>
  <reversibility rating="one-way">D-02 cycle-only generation is user-locked in 42-CONTEXT.md (one-way: generating repeat frames in 42 would force a Phase 43 data cleanup); recorded here — the lock itself is the decision record, no additional checkpoint required.</reversibility>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with the new static-mode, per-mode erase-pass-through, geometry-parity (point-identical under nonzero Motion, both modes), deep-equal-snapshot, deep-frozen-input, progressive-default, controller-input, and no-library-write cases green alongside all pre-existing cases
    - Renderer test asserts the transform callback receives the original stroke color when overrideColor is set (geometry parity, Pitfall 2)
    - Renderer test asserts erase strokes keep erase behavior (unrecolored, tool unchanged) under mode 'static' and under mode 'progressive'
    - Renderer test asserts the input script fixture equals its pre-render structuredClone after generation, and a recursively frozen fixture renders without throwing
    - Controller test asserts rendered called with objectContaining mode/overrideColor and motion equal to dialog-signal values, and asserts zero invocations on every harness library write port during confirm
    - physicsPaintRotoPlayScriptRenderer.ts contains the union type 'progressive' | 'static' on the input interface and references buildStaticStrokeSchedule
    - git diff on progressiveStrokeSchedule.ts remains empty (regression lock intact)
  </acceptance_criteria>
  <done>A static/hold generation with color override runs end-to-end through the real controller and renderer with the complete PLAY-02 invariant set proven (paint-only recolor both modes, erase preserved both modes, original color into Motion, point-identical geometry, deep-equal and freeze-safe source, zero library writes); progressive behavior with default options is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hold Loop option state with the safe-product Repeat bound, requested/effective readout, and Infinity preserve/restore</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (current state after Task 1 — signal group, openConfirmation, confirm/commit flow, parseCount strict-regex pattern)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (harness: authority() factory already returns layerEndExclusive: 8 / capacity: 4; table-driven validation pattern)
    - app/src/types/physicPaint.ts lines 596-615 (PhysicPaintRotoAuthorityResult fields verbatim)
    - .planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md (D-02, D-03, D-10, D-12, D-13, D-14, D-15 verbatim)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Copywriting Contract — readout forms, field error pattern; Interaction Contract — Infinity toggle)
  </read_first>
  <behavior>
    - RED first — new failing controller cases: (a) repeatText format validation mirrors parseCount discipline — '', '0', '-2', '1.5', 'abc' each produce exactly 'Enter a positive integer.' and block confirm; (b) safe-product bound — with cycle value C the accepted maximum is floor(Number.MAX_SAFE_INTEGER / C): for C=5 the value 1801439850948198 is accepted, 1801439850948199 is rejected with exactly 'Repeat is too large for this cycle length.'; for C=2 the individually-safe value 9007199254740991 is rejected with the same copy because 2 × 9007199254740991 is not a safe integer; repeat 1 always passes format and bound; (c) requested/effective/truncation derivation — cycle 5 × repeat 5 with layerEndExclusive − canonicalStart = 18 yields requested 25, effective 18, truncated true; untruncated case yields effective === requested and truncated false; (d) infinity on → readout uses the literal `Cycle 5f × ∞` form, repeat validation is skipped, and repeatText is preserved (including a preserved large valid value such as 1801439850948198); toggling infinity off via setInfinity(false) restores the last finite repeat value (D-12); (e) first-time defaults when switching to static: cycle 1, repeat 1, infinity off (D-15); (f) signals survive dialog close/reopen within the session (D-10); (g) generated frame count stays exactly the cycle value regardless of repeat — renderRotoPlayScriptFrames is called with frameCount equal to the Frames/Cycle frames value even when repeatText holds a large valid value or infinity is on (D-02)
  </behavior>
  <action>
    RED: add the failing cases (a)-(g) to the controller test using the existing harness/table-driven patterns; run quick command, confirm red. GREEN, controller: add signals repeatText (default '1'), infinity (boolean, default false), lastFiniteRepeat (string, default '1'), layerEndExclusive (number | null, default null); set layerEndExclusive.value = authority.layerEndExclusive in openConfirmation next to the existing capacity assignment (Pitfall 5). Add parseRepeat(text, cycleLength) mirroring parseCount's trim + /^\d+$/ discipline: empty or non-/^\d+$/ input yields { count: null, error: 'Enter a positive integer.' }; a parsed value that is not a safe positive integer yields the same; otherwise compute maxRepeat = Math.floor(Number.MAX_SAFE_INTEGER / cycleLength) BEFORE any multiplication and reject count > maxRepeat with { count: null, error: 'Repeat is too large for this cycle length.' } — the bound derives from the CURRENT cycle value (the parsed shared Frames/Cycle frames field, where 'Max' resolves to capacity); when the cycle value is temporarily unparseable, skip the product check because the frames validation error already blocks confirm. The accepted cycle × repeat product is therefore always Number.isSafeInteger. Expose parsedRepeat computed, repeatError computed (null while infinity is on — the disabled field is not validated), and block confirm when repeatError is non-null, alongside the existing count guard. Add computed loopReadout producing exactly: untruncated finite form `Requested: {R}f ({C}f × {n}) · Effective: {R}f`; truncated finite form `Requested: {R}f ({C}f × {n}) · Effective: {E}f — shortened by the next clip`; infinity form `Cycle {C}f × ∞` with requested unbounded and effective still derived from the retained boundary — all per D-12/D-13 verbatim copy; effective = min(requested, layerEndExclusive − canonicalStart) when both retained values exist, readout null otherwise; never feed the readout back into generation (D-14). Add setInfinity(enabled: boolean) on the controller interface: turning on stores repeatText.value into lastFiniteRepeat and sets infinity true; turning off restores repeatText from lastFiniteRepeat and sets infinity false (Pitfall 7). Infinity never enters numeric multiplication anywhere. Static-mode first-time defaults: when mode switches to 'static' and no session values exist, set cycle field to '1', repeatText '1', infinity false (D-15; progressive defaults untouched). Expose everything on the controller interface/return. Run quick command until green; commit: feat(42-02): add hold loop option state with safe-product repeat bound and duration readout.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with all (a)-(g) cases green plus the full pre-existing suite
    - Controller source contains parseRepeat computing maxRepeat as Math.floor(Number.MAX_SAFE_INTEGER / cycleLength) before any multiplication, and the two exact error strings 'Enter a positive integer.' and 'Repeat is too large for this cycle length.'
    - Test asserts acceptance of floor(Number.MAX_SAFE_INTEGER / 5) and rejection of that value + 1 for cycle 5; rejection of 9007199254740991 for cycle 2 (individually safe, product unsafe)
    - Controller source contains a layerEndExclusive signal assigned from authority.layerEndExclusive inside openConfirmation, and exposes setInfinity, repeatError, and loopReadout on the returned object
    - Test asserts truncated copy 'Effective: 18f — shortened by the next clip' for the 5×5-vs-18 fixture and the infinity literal 'Cycle 5f × ∞'
    - Test asserts the infinity toggle preserves and restores a large valid finite repeat (e.g. 1801439850948198) and that renderRotoPlayScriptFrames receives frameCount equal to the cycle value regardless of repeat/infinity
  </acceptance_criteria>
  <done>Controller exposes the Hold Loop surface with the locked safe-product Repeat bound: repeat/infinity with preserve-restore, validated readout with exact D-13 copy, repeatError field channel, and generation provably pinned to the cycle value — session-only, nothing persisted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Controller boundaries — resetDialogMotion, generation-error lifecycle, and exact applied-summary composition</name>
  <files>app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts, app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts</files>
  <read_first>
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.ts (current state after Task 2 — confirm try/catch/finally flow, fail(), cancel(), generation token, existing error/progress/phase signals)
    - app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts (harness: getMotion port factory returns { deformation: 25, position: 40 }; rendered mock reject/resolve control; commit spy)
    - .planning/phases/42-playscript-application-modes-color-override/42-CONTEXT.md (D-06, D-07 verbatim)
    - .planning/phases/42-playscript-application-modes-color-override/42-UI-SPEC.md (Copywriting Contract — panel summary line 1/2 forms, summary update contract; Interaction Contract — generation failure/cancellation rows)
  </read_first>
  <behavior>
    - RED first — reset boundary cases: (a) openConfirmation initializes dialogMotion from ports.getMotion() values; (b) editing dialogMotion signals never triggers any write toward Motion panel defaults — the harness records every port invocation and only getMotion reads may occur; (c) after editing dialogMotion, resetDialogMotion() re-reads the CURRENT ports.getMotion() values and assigns them to dialogMotion (a port value changed between open and reset is what reset returns); reset never writes anywhere
    - RED first — generation-error lifecycle cases: (d) starting confirm clears a stale error from a previous failure; (e) on renderer failure: phase becomes 'failed', error exposes the failure message, progress becomes null (progress bar hides), confirmationOpen stays true, canCancel becomes false (inputs re-enable), the commit port is never called, and staged output leaves zero partial destination frames or timeline mutations; (f) retry after failure starts from a clean error state and can succeed; (g) normal user cancellation mid-render: phase becomes 'cancelled', error stays null, confirmationOpen stays true, commit never called, no partial mutations; (h) failure and cancellation leave remembered options and appliedSummary byte-identical
    - RED first — applied-summary composition cases: (i) a successful Static / Hold Generate with override '#3366ff' and dialog Motion 25/40 over canonicalStart 4 count 4 yields exactly line1 'Static / Hold · Override #3366ff · Motion 25/40' and line2 'F4–F7 · 4 frames generated' (layer-end boundary: end = start + count − 1 = 7 = layerEndExclusive − 1, no off-by-one); (j) before the first successful Generate, line2 is exactly 'No frames generated yet' and line1 is the first-time default 'Progressive · Original colors · Motion {d}/{p}' with {d}/{p} from the Motion defaults port read at first openConfirmation; (k) editing dialog options after a success without confirming leaves both lines byte-identical; (l) dialog Cancel and generation cancellation leave both lines byte-identical; (m) generation failure leaves both lines byte-identical; (n) a second successful Generate replaces BOTH lines atomically from the same generation's committed options — neither line retains any first-generation value
  </behavior>
  <action>
    RED: add the failing cases (a)-(n) to the controller test; run quick command, confirm red. GREEN, controller — reset boundary: add resetDialogMotion(): void to the RotoPlayScriptController interface and implementation, assigning dialogMotion.value a fresh copy of ports.getMotion() at call time; this is the ONLY reset path the dialog will call (42-03 consumes this name; the dialog never touches controller construction ports). GREEN — generation-error lifecycle: reuse the existing error signal as the explicit generation-error channel (distinct from validationError/repeatError field channels); in fail() additionally set progress.value = null so the progress bar hides on failure; confirm and openConfirmation already clear error at start — keep and lock by test; the existing isAbort path (phase 'cancelled', error null) is the normal-cancellation contract — lock by test; no partial destination frames or timeline mutations are possible on either path because the commit port is the sole mutation and is never reached — assert via the commit spy. GREEN — applied summary: add a controller-internal composeAppliedSummary({ mode, overrideColor, motion, start, count }) producing exactly line1 `${Mode label} · ${'Original colors' or 'Override #rrggbb'} · Motion ${deformation}/${position}` with Mode label 'Progressive' or 'Static / Hold', and line2 `F${start}–F${start + count - 1} · ${count} frames generated`; add appliedSummary state as line1/line2 string signals initialized at construction to the first-time defaults (line1 'Progressive · Original colors · Motion 0/0' placeholder refreshed from ports.getMotion() on the first openConfirmation while no successful generation has occurred yet; line2 'No frames generated yet'); update appliedSummary in exactly ONE place — immediately after a successful commit, from the options snapshot and destination actually used for that generation (mode/overrideColor-when-enabled/dialogMotion captured for the renderer call, plus start/count) — never from live dialog draft values; cancel, cancellation, and failure paths must not touch it. Expose appliedSummary line1/line2 and resetDialogMotion on the interface/return. Run quick command until green; commit: feat(42-02): add reset/generation-error/applied-summary controller boundaries.
  </action>
  <verify>
    <automated>pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verify command exits 0 with all (a)-(n) cases green plus the full pre-existing suite
    - Controller interface and returned object expose resetDialogMotion, appliedSummary (line1/line2 signals), and the error generation channel; the dialog-facing surface requires no access to controller construction ports
    - Test asserts the exact strings line1 'Static / Hold · Override #3366ff · Motion 25/40' and line2 'F4–F7 · 4 frames generated' after the successful static+override Generate, with the layer-end boundary exact (end = layerEndExclusive − 1)
    - Test asserts fail() leaves progress null, phase 'failed', confirmationOpen true, error populated; and that cancellation leaves phase 'cancelled' with error null — each with zero commit-port invocations and both summary lines unchanged
    - Controller source updates appliedSummary at exactly one assignment site located after the successful commit, sourcing only the committed options snapshot
  </acceptance_criteria>
  <done>The controller boundary consumed by 42-03/42-04 is complete and exact: resetDialogMotion re-reads Motion defaults with zero writes, the generation-error lifecycle drives the E5 UI states, and appliedSummary composes both lines atomically from the committed generation with byte-stability across edits, cancel, cancellation, and failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → controller | repeat/cycle text fields are untrusted numeric input |
| controller → engine | color string assigned onto stroke.color before canvas render |
| child → parent bridge | authority/commit path (regression-locked, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-02-01 | Tampering | parseRepeat numeric input | medium | mitigate | Strict trim + /^\d+$/ + safe-integer + positive validation mirroring parseCount, plus the dynamic safe-product bound maxRepeat = floor(Number.MAX_SAFE_INTEGER / cycleLength) derived before multiplication; table-driven rejection tests for '', '0', '-2', '1.5', 'abc', max+1, and individually-safe-but-product-unsafe values (ASVS V5) |
| T-42-02-02 | Tampering | overrideColor into stroke.color | low | mitigate | Value only ever sourced from InlineColorPicker hex output; assigned solely to stroke.color ('#rrggbb' \| null contract); never interpolated into HTML/CSS |
| T-42-02-03 | DoS | generation volume | low | accept | Existing guards unchanged and sole bound: authority capacity gate, MAX_FRAME_COUNT=10_000, MAX_AGGREGATE_RGBA_BYTES=512MB (D-14 keeps capacity as the only gate); the Repeat bound is display-math safety only — it never raises generation volume (D-02) |
| T-42-02-04 | Tampering | mid-render authority change | low | accept | Existing triple authority re-check + revision/capacity equality before commit (controller.ts:158-164) is untouched and applies to both modes identically |
| T-42-02-SC | Tampering | package installs | low | accept | No package installs in this plan (RESEARCH: zero new dependencies) |
</threat_model>

## Artifacts this phase produces

This plan creates (no new files; new symbols on existing modules):
- `RotoPlayScriptRenderInput` new fields: `mode?: 'progressive' | 'static'`, `overrideColor?: string | null` (app/src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.ts)
- `RotoPlayScriptController` interface new signals/computeds: `mode`, `overrideColor`, `overrideEnabled`, `dialogMotion` ({ deformation, position }), `repeatText`, `infinity`, `lastFiniteRepeat`, `layerEndExclusive`, `parsedRepeat` (computed), `repeatError` (computed — null while infinity on), `loopReadout` (computed), `appliedSummary` ({ line1, line2 } signals) — all session-only signals (physicsPaintRotoPlayScriptController.ts)
- `RotoPlayScriptController` interface new methods: `resetDialogMotion(): void` (re-reads ports.getMotion() into dialogMotion; the ONLY reset path — consumed by name in 42-03), `setInfinity(enabled: boolean)` (preserve/restore lastFiniteRepeat — consumed by name in 42-03)
- `RotoPlayScriptController` generation-error channel: the existing `error: Signal<string | null>` is locked as the generation-failure surface (cleared at generation start; populated + progress nulled on failure; null on cancellation) — distinct from the `validationError`/`repeatError` field channels; rendered by 42-03 through the shared inline-error style
- New controller-internal functions `parseRepeat(text, cycleLength): { count: number | null; error: string | null }` (with the floor(Number.MAX_SAFE_INTEGER / cycleLength) bound) and `composeAppliedSummary({ mode, overrideColor, motion, start, count }): { line1: string; line2: string }` (exact forms: line1 '{Mode} · {Original colors | Override #rrggbb} · Motion {d}/{p}'; line2 'F{start}–F{end} · {count} frames generated'; first-time line2 'No frames generated yet')
- Extended test files: physicsPaintRotoPlayScriptController.test.ts, physicsPaintRotoPlayScriptRenderer.test.ts

<verification>
- Quick gate after each task: `pnpm --dir app exec vitest run src/components/physic-paint/roto/physicsPaintRotoPlayScriptController.test.ts src/components/physic-paint/roto/physicsPaintRotoPlayScriptRenderer.test.ts` exits 0
- Wave gate: `pnpm --dir app exec vitest run` + package animation tests + `pnpm --dir app typecheck` all exit 0
</verification>

<success_criteria>
- Both modes generate through one renderer entry point; progressive output with default options unchanged
- Override is provably color-only, paint-only, post-Motion, in both modes; the source script is deep-equal/freeze-safe and never written — the complete PLAY-02 invariant set
- Loop readout derives requested/effective/truncation from retained authority values with locked English copy; the Repeat maximum is the dynamic safe-product bound; Infinity never enters arithmetic
- resetDialogMotion is the explicit, test-locked reset boundary; the generation-error lifecycle drives E5 with failure/cancellation atomicity proven
- appliedSummary composes exactly and updates only on successful Generate
</success_criteria>

<output>
Create `.planning/phases/42-playscript-application-modes-color-override/42-02-SUMMARY.md` when done
</output>
