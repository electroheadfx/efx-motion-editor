---
phase: 20-paint-brush-fx
plan: 04
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - Application/src/stores/paintStore.ts
  - Application/src/lib/paintPersistence.ts
  - Application/src/lib/paintPersistence.test.ts
  - Application/src/components/sidebar/PaintProperties.tsx
autonomous: false
requirements:
  - PAINT-12
  - PAINT-13
must_haves:
  truths:
    - "User can flatten all FX strokes into the frame cache for fastest playback per D-17"
    - "Export renders FX strokes identically to preview per PAINT-12"
    - "Stroke fxState and brushStyle/brushParams persist in sidecar JSON per PAINT-13"
    - "Frame FX cache is regenerated on project load from persisted stroke data"
    - "Flattened frames use single drawImage() from frame cache for fastest playback per D-16"
  artifacts:
    - path: "Application/src/stores/paintStore.ts"
      provides: "flattenFrame() method that re-renders all FX strokes and marks frame as flattened"
      contains: "flattenFrame"
    - path: "Application/src/lib/paintPersistence.ts"
      provides: "Updated persistence for fxState, regenerates frame FX cache on load"
      contains: "fxState"
    - path: "Application/src/lib/paintPersistence.test.ts"
      provides: "Filled test stubs for persistence round-trip"
    - path: "Application/src/components/sidebar/PaintProperties.tsx"
      provides: "Flatten button in paint tools UI"
      contains: "Flatten"
  key_links:
    - from: "Application/src/stores/paintStore.ts"
      to: "Application/src/lib/brushP5Adapter.ts"
      via: "renderFrameFx called during flattenFrame to generate frame cache"
      pattern: "renderFrameFx"
    - from: "Application/src/lib/paintPersistence.ts"
      to: "Application/src/lib/brushP5Adapter.ts"
      via: "renderFrameFx called during loadPaintData to regenerate frame FX cache"
      pattern: "renderFrameFx"
---

<objective>
Implement flatten, export parity, persistence updates, and visual verification.

Purpose: Complete the post-process FX workflow with the flatten optimization (per D-17), ensure export renders identically to preview (per PAINT-12), update persistence to save/restore fxState and regenerate frame FX cache on load (per PAINT-13), and verify the complete system visually.

Output: Flatten functionality, export parity, updated persistence with tests, and human verification of the full FX workflow.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-paint-brush-fx/20-CONTEXT.md
@.planning/phases/20-paint-brush-fx/20-01-SUMMARY.md
@.planning/phases/20-paint-brush-fx/20-02-SUMMARY.md
@.planning/phases/20-paint-brush-fx/20-03-SUMMARY.md

<interfaces>
<!-- Key types from previous plans -->

From Application/src/types/paint.ts (after Plan 01):
```typescript
export type StrokeFxState = 'flat' | 'fx-applied' | 'flattened';
export interface PaintStroke {
  fxState?: StrokeFxState;
  brushStyle?: BrushStyle;
  brushParams?: BrushFxParams;
  // NOTE: No fxCachedCanvas -- caching is per-frame in paintStore
}
```

From Application/src/lib/brushP5Adapter.ts (after Plan 01):
```typescript
export function renderFrameFx(
  strokes: PaintStroke[], width: number, height: number
): HTMLCanvasElement | null;
// Renders ALL FX-applied strokes together on one p5.brush canvas for spectral mixing
```

From Application/src/lib/paintRenderer.ts (after Plan 02):
```typescript
export function renderPaintFrameWithBg(
  ctx: CanvasRenderingContext2D, frame: PaintFrame, width: number, height: number,
  layerId?: string, frameNum?: number
): void;
export function strokeToPath(
  points: [number, number, number][], size: number, options: PaintStrokeOptions
): Path2D | null;
```

From Application/src/stores/paintStore.ts (after Plan 01):
```typescript
paintBgColor: Signal<string>
setFrameFxCache(layerId: string, frame: number, canvas: HTMLCanvasElement): void
getFrameFxCache(layerId: string, frame: number): HTMLCanvasElement | null
invalidateFrameFxCache(layerId: string, frame: number): void
addElement(layerId, frame, element): void
markDirty(layerId, frame): void
```

From Application/src/lib/paintPersistence.ts:
```typescript
export async function savePaintData(projectDir: string): Promise<void>;
export async function loadPaintData(projectDir: string, layerIds: string[]): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement flatten and add Flatten button to PaintProperties</name>
  <files>
    Application/src/stores/paintStore.ts,
    Application/src/components/sidebar/PaintProperties.tsx
  </files>
  <read_first>
    Application/src/stores/paintStore.ts,
    Application/src/components/sidebar/PaintProperties.tsx,
    Application/src/lib/brushP5Adapter.ts,
    Application/src/types/paint.ts,
    Application/src/lib/paintRenderer.ts
  </read_first>
  <action>
**1. Add flattenFrame() and unflattenFrame() to paintStore.ts:**

Add imports at top of paintStore.ts:
```typescript
import {renderFrameFx} from '../lib/brushP5Adapter';
import {projectStore} from './projectStore';
```

Update the type import to include PaintStroke:
```typescript
import type {PaintElement, PaintFrame, PaintToolType, PaintStrokeOptions, BrushStyle, BrushFxParams, PaintStroke} from '../types/paint';
```

Add methods to the `paintStore` object:

```typescript
flattenFrame(layerId: string, frame: number): void {
  const paintFrame = this.getFrame(layerId, frame);
  if (!paintFrame || paintFrame.elements.length === 0) return;

  const projW = projectStore.width.peek();
  const projH = projectStore.height.peek();

  // Collect all brush strokes for renderFrameFx
  const brushStrokes = paintFrame.elements.filter(
    (el) => el.tool === 'brush'
  ) as PaintStroke[];

  // Ensure all styled strokes are marked fx-applied for renderFrameFx to include them
  for (const s of brushStrokes) {
    if (s.brushStyle && s.brushStyle !== 'flat' && s.fxState !== 'fx-applied') {
      s.fxState = 'fx-applied';
    }
  }

  // Render all FX strokes together on one p5.brush canvas (spectral mixing)
  const fxCanvas = renderFrameFx(brushStrokes, projW, projH);
  if (fxCanvas) {
    this.setFrameFxCache(layerId, frame, fxCanvas);
  }

  // Mark all FX strokes as flattened
  for (const s of brushStrokes) {
    if (s.brushStyle && s.brushStyle !== 'flat') {
      s.fxState = 'flattened';
    }
  }

  this.markDirty(layerId, frame);
  paintVersion.value++;
  _markProjectDirty?.();
},

unflattenFrame(layerId: string, frame: number): void {
  const paintFrame = this.getFrame(layerId, frame);
  if (!paintFrame) return;

  const projW = projectStore.width.peek();
  const projH = projectStore.height.peek();

  // Restore FX strokes to fx-applied state
  const brushStrokes = paintFrame.elements.filter(
    (el) => el.tool === 'brush'
  ) as PaintStroke[];

  for (const s of brushStrokes) {
    if (s.brushStyle && s.brushStyle !== 'flat') {
      s.fxState = 'fx-applied';
    } else {
      s.fxState = 'flat';
    }
  }

  // Re-render frame cache with all FX strokes
  const fxCanvas = renderFrameFx(brushStrokes, projW, projH);
  if (fxCanvas) {
    this.setFrameFxCache(layerId, frame, fxCanvas);
  } else {
    this.invalidateFrameFxCache(layerId, frame);
  }

  this.markDirty(layerId, frame);
  paintVersion.value++;
},
```

**2. Add Flatten button to PaintProperties.tsx:**

Add a "Flatten Frame" button in the paint tools section, visible when the select tool is active:

```tsx
{activeTool === 'select' && (
  <div class="px-1 pt-1">
    <button
      class="w-full text-[11px] py-1 px-2 rounded"
      style={{
        backgroundColor: 'var(--sidebar-bg-hover)',
        color: 'var(--sidebar-text-primary)',
        border: '1px solid var(--sidebar-border)',
      }}
      onClick={() => {
        const layerId = layer.id;
        const frame = timelineStore.currentFrame.peek();
        paintStore.flattenFrame(layerId, frame);
      }}
    >
      Flatten Frame
    </button>
    <div class="text-[9px] mt-1 opacity-50" style={{color: 'var(--sidebar-text-secondary)'}}>
      Merge all FX strokes into single image for fastest playback
    </div>
  </div>
)}
```
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit --project tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - paintStore.ts contains `flattenFrame` method
    - paintStore.ts contains `unflattenFrame` method
    - paintStore.ts imports `renderFrameFx` from brushP5Adapter (NOT renderSingleStroke)
    - paintStore.ts flattenFrame calls renderFrameFx (per-frame, not per-stroke)
    - paintStore.ts flattenFrame marks strokes as 'flattened'
    - PaintProperties.tsx contains "Flatten Frame" button text
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Flatten re-renders all FX strokes together via renderFrameFx and stores frame cache. Unflatten restores fx-applied state. Flatten button visible in select mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update persistence to save fxState and regenerate frame FX cache on load</name>
  <files>
    Application/src/lib/paintPersistence.ts,
    Application/src/lib/paintPersistence.test.ts
  </files>
  <read_first>
    Application/src/lib/paintPersistence.ts,
    Application/src/lib/paintPersistence.test.ts,
    Application/src/lib/brushP5Adapter.ts,
    Application/src/stores/paintStore.ts,
    Application/src/types/paint.ts
  </read_first>
  <action>
**1. Update paintPersistence.ts to regenerate frame FX cache on load:**

Add imports at top:
```typescript
import {renderFrameFx} from './brushP5Adapter';
import {projectStore} from '../stores/projectStore';
import type {PaintFrame, PaintStroke} from '../types/paint';
```

In the `loadPaintData` function, after the line `paintStore.loadFrame(layerId, frame, parsed)`, add frame-level cache regeneration:

```typescript
// Regenerate frame-level FX cache for frames with FX-applied strokes (per PAINT-13)
const projW = projectStore.width.peek();
const projH = projectStore.height.peek();
if (projW > 0 && projH > 0) {
  const hasFxStrokes = parsed.elements.some((el) => {
    if (el.tool !== 'brush') return false;
    const s = el as PaintStroke;
    return s.fxState === 'fx-applied' && s.brushStyle && s.brushStyle !== 'flat';
  });

  if (hasFxStrokes) {
    try {
      const brushStrokes = parsed.elements.filter(
        (el) => el.tool === 'brush'
      ) as PaintStroke[];
      const fxCanvas = renderFrameFx(brushStrokes, projW, projH);
      if (fxCanvas) {
        paintStore.setFrameFxCache(layerId, frame, fxCanvas);
      }
    } catch (e) {
      console.warn(`[paintPersistence] Failed to regenerate frame FX cache for ${layerId}:${frame}:`, e);
    }
  }
}
```

Note: The fxState field is already part of PaintStroke and will be serialized/deserialized automatically via JSON.stringify/parse. No changes needed for savePaintData since fxState is just a string field on the stroke object. The `fxCachedCanvas` does NOT exist on strokes (per-frame caching architecture), so there's nothing to strip during serialization.

**2. Fill paintPersistence.test.ts stubs:**

Replace ALL `it.todo(...)` calls with real test implementations:

```typescript
import {describe, it, expect} from 'vitest';
import type {PaintStroke, PaintFrame, BrushFxParams} from '../types/paint';

describe('paintPersistence brush FX round-trip', () => {
  const makeStroke = (overrides: Partial<PaintStroke> = {}): PaintStroke => ({
    id: 'test-stroke',
    tool: 'brush',
    points: [[10, 20, 0.5], [30, 40, 0.7]],
    color: '#FF0000',
    opacity: 1,
    size: 10,
    options: {
      thinning: 0.5, smoothing: 0.5, streamline: 0.5,
      simulatePressure: true, pressureEasing: 'linear',
      pressureCurve: 2, taperStart: 0, taperEnd: 0, tiltInfluence: 0.3,
    },
    ...overrides,
  });

  it('PaintStroke with brushStyle serializes to JSON correctly', () => {
    const stroke = makeStroke({brushStyle: 'watercolor'});
    const json = JSON.stringify(stroke);
    const parsed = JSON.parse(json);
    expect(parsed.brushStyle).toBe('watercolor');
  });

  it('PaintStroke with brushParams serializes all FX param values', () => {
    const params: BrushFxParams = {bleed: 0.6, grain: 0.4, fieldStrength: 0.3};
    const stroke = makeStroke({brushStyle: 'watercolor', brushParams: params});
    const json = JSON.stringify(stroke);
    const parsed = JSON.parse(json);
    expect(parsed.brushParams.bleed).toBe(0.6);
    expect(parsed.brushParams.grain).toBe(0.4);
    expect(parsed.brushParams.fieldStrength).toBe(0.3);
  });

  it('PaintStroke fxState serializes and deserializes correctly', () => {
    const stroke = makeStroke({fxState: 'fx-applied', brushStyle: 'ink'});
    const json = JSON.stringify(stroke);
    const parsed = JSON.parse(json);
    expect(parsed.fxState).toBe('fx-applied');
  });

  it('PaintStroke without brushStyle deserializes with undefined (backward compat)', () => {
    const stroke = makeStroke();
    delete (stroke as any).brushStyle;
    const json = JSON.stringify(stroke);
    const parsed = JSON.parse(json);
    expect(parsed.brushStyle).toBeUndefined();
  });

  it('Old sidecar JSON without brushStyle/brushParams/fxState loads without error', () => {
    const oldJson = JSON.stringify({
      elements: [{
        id: 'old-stroke', tool: 'brush',
        points: [[0, 0, 0.5]], color: '#000', opacity: 1, size: 5,
        options: {thinning: 0.5, smoothing: 0.5, streamline: 0.5,
          simulatePressure: true, pressureEasing: 'linear',
          pressureCurve: 2, taperStart: 0, taperEnd: 0, tiltInfluence: 0.3},
      }],
    });
    const parsed: PaintFrame = JSON.parse(oldJson);
    expect(parsed.elements).toHaveLength(1);
    const s = parsed.elements[0] as PaintStroke;
    expect(s.brushStyle).toBeUndefined();
    expect(s.brushParams).toBeUndefined();
    expect(s.fxState).toBeUndefined();
  });

  it('Round-trip: serialize then deserialize preserves brushStyle, brushParams, and fxState', () => {
    const frame: PaintFrame = {
      elements: [
        makeStroke({id: 's1', brushStyle: 'ink', brushParams: {edgeDarken: 0.7, fieldStrength: 0.15}, fxState: 'fx-applied'}),
        makeStroke({id: 's2', brushStyle: 'charcoal', brushParams: {grain: 0.6, scatter: 0.4}, fxState: 'fx-applied'}),
      ],
    };
    const json = JSON.stringify(frame);
    const parsed: PaintFrame = JSON.parse(json);
    const s1 = parsed.elements[0] as PaintStroke;
    const s2 = parsed.elements[1] as PaintStroke;
    expect(s1.brushStyle).toBe('ink');
    expect(s1.brushParams?.edgeDarken).toBe(0.7);
    expect(s1.fxState).toBe('fx-applied');
    expect(s2.brushStyle).toBe('charcoal');
    expect(s2.brushParams?.grain).toBe(0.6);
    expect(s2.fxState).toBe('fx-applied');
  });

  it('Serialized PaintStroke has no fxCachedCanvas field (per-frame caching, not per-stroke)', () => {
    const stroke = makeStroke({fxState: 'fx-applied', brushStyle: 'watercolor'});
    const json = JSON.stringify(stroke);
    const parsed = JSON.parse(json);
    expect(parsed.fxCachedCanvas).toBeUndefined();
    expect(Object.keys(parsed)).not.toContain('fxCachedCanvas');
  });
});
```

**3. Verify export parity (PAINT-12):**

Read `exportRenderer.ts` to confirm it delegates to PreviewRenderer for paint layers (does NOT have its own paint rendering). The export pipeline calls `PreviewRenderer.renderFrame()` which uses `renderPaintFrameWithBg()` -- same code path as preview. No changes needed to exportRenderer.ts unless it has separate paint handling.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx vitest run src/lib/paintPersistence.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - paintPersistence.ts imports `renderFrameFx` (NOT renderSingleStroke)
    - paintPersistence.ts regenerates frame-level FX cache on load via renderFrameFx
    - paintPersistence.ts uses `paintStore.setFrameFxCache` to store regenerated cache
    - paintPersistence.test.ts has zero `it.todo` calls
    - paintPersistence.test.ts includes test verifying no fxCachedCanvas field exists
    - `npx vitest run src/lib/paintPersistence.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Persistence saves fxState and regenerates frame FX cache on load via renderFrameFx. All paintPersistence test stubs filled and passing. No per-stroke cache fields in serialized data.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete FX workflow</name>
  <files>N/A</files>
  <action>
    Human visual verification of the complete post-process paint FX workflow.
    No code changes in this task -- this is a checkpoint for the human to verify
    all automated work from Plans 01-04.
  </action>
  <verify>Human approval of all 12 verification checks below.</verify>
  <done>Human types "approved" after verifying all checks pass, or describes issues found.</done>
  <what-built>
    Complete post-process paint FX workflow with per-frame caching:
    1. Types extended with fxState, select tool (no per-stroke fxCachedCanvas)
    2. Store with paintBgColor, selectedStrokeIds, frameFxCache Map, flattenFrame
    3. brushP5Adapter with renderFrameFx for per-frame batch rendering (spectral mixing)
    4. paintRenderer with solid background and frame-level cache compositing
    5. PaintOverlay with select tool, FX application via frame re-render, rollback, delete
    6. PaintProperties with background color picker, overlay toggle, flatten button
    7. previewRenderer using renderPaintFrameWithBg
    8. paintPersistence regenerating frame FX cache on load
  </what-built>
  <how-to-verify>
    **Full workflow test (start the app, open a project with a paint layer):**

    1. **Flat drawing (D-01):** Select brush tool, draw several strokes. Verify they render as flat strokes (solid color fill via perfect-freehand, no FX styling).

    2. **Select tool (D-05, D-06):** Switch to select tool (in paint tools). Click on a flat stroke -- verify dashed blue bounding box appears around it. Cmd/Ctrl+click to multi-select additional strokes.

    3. **Apply FX (D-08):** With strokes selected, change brush style to "Watercolor" in PaintProperties. Verify selected strokes re-render with watercolor appearance (soft edges, bleed, texture). The re-render may take a moment -- this is expected as it re-renders all FX strokes on the frame.

    4. **Style swap (D-09):** With the same strokes still selected, change style to "Charcoal". Verify the entire frame's FX strokes re-render immediately with the new style.

    5. **Rollback (D-10):** Change style back to "Flat". Verify strokes revert to original flat vector rendering (no FX). Remaining FX strokes on the frame should still show their FX appearance.

    6. **Solid background (D-11, D-12):** Verify paint layer renders on white background (not transparent). Open PAINT BACKGROUND section, click swatch to change color. Verify background changes. Click "Reset" to restore white.

    7. **Delete in select mode (D-07):** Select an FX-applied stroke, press Delete/Backspace. Verify stroke is removed and remaining FX strokes re-render correctly.

    8. **Flatten (D-17):** Apply FX to several strokes. Click "Flatten Frame" button. Verify visual appearance is unchanged but playback should be slightly faster (single drawImage from frame cache).

    9. **Export parity (PAINT-12):** Export a frame with FX strokes. Compare exported PNG with canvas preview -- should be identical.

    10. **Persistence (PAINT-13):** Save the project. Close and reopen. Verify FX-applied strokes still show their styled appearance (frame FX cache regenerated from persisted data).

    11. **Color mixing / Spectral mixing (PAINT-06):** Draw overlapping strokes with different colors (e.g., blue and yellow). Select both, apply watercolor or ink FX. Verify the overlap produces physically-correct spectral mixing (blue + yellow = green, not gray). This is the key test for per-frame caching architecture.

    12. **Sequence overlay (D-13, D-14):** Click the "Show sequence overlay" checkbox or press "O". Verify the sequence frame appears underneath the paint layer at reduced opacity. Toggle off -- verify it disappears. This is reference-only, not in export.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 checks pass, or describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
1. `cd Application && npx vitest run src/lib/paintPersistence.test.ts --reporter=verbose` -- all tests pass
2. `cd Application && npx tsc --noEmit --project tsconfig.json` -- no type errors
3. `cd Application && npx vitest run --reporter=verbose` -- full test suite passes
4. `grep 'flattenFrame' Application/src/stores/paintStore.ts` -- flatten method exists
5. `grep 'renderFrameFx' Application/src/lib/paintPersistence.ts` -- frame cache regeneration on load
6. `grep 'renderFrameFx' Application/src/stores/paintStore.ts` -- flatten uses renderFrameFx
7. Human verification of all 12 visual checks
</verification>

<success_criteria>
- Flatten creates frame FX cache via renderFrameFx for fast playback per D-17
- Export renders identically to preview per PAINT-12
- Persistence saves fxState and regenerates frame FX cache per PAINT-13
- All test stubs filled and green
- Human approves all 12 visual verification checks including spectral mixing
- Full vitest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-paint-brush-fx/20-04-SUMMARY.md`
</output>
