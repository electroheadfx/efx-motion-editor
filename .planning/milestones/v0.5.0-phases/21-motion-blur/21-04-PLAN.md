---
phase: 21-motion-blur
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - Application/src/lib/shortcuts.ts
  - Application/src/stores/motionBlurStore.test.ts
  - Application/src/lib/motionBlurEngine.test.ts
autonomous: false
requirements:
  - MBLR-01
  - MBLR-02
  - MBLR-03
  - MBLR-04
  - MBLR-05
  - MBLR-06
  - MBLR-07
  - MBLR-08
  - MBLR-09

must_haves:
  truths:
    - "User can toggle motion blur with keyboard shortcut M"
    - "All motion blur tests pass covering store, velocity computation, persistence"
    - "Visual verification confirms moving layers blur and stationary layers stay sharp"
    - "Export with motion blur produces correctly blurred frames"
    - "Save/load cycle preserves motion blur settings"
  artifacts:
    - path: "Application/src/lib/shortcuts.ts"
      provides: "Keyboard shortcut M for motion blur toggle"
      contains: "motionBlurStore.toggleEnabled"
    - path: "Application/src/stores/motionBlurStore.test.ts"
      provides: "Complete store test coverage"
    - path: "Application/src/lib/motionBlurEngine.test.ts"
      provides: "Complete engine test coverage"
  key_links:
    - from: "Application/src/lib/shortcuts.ts"
      to: "Application/src/stores/motionBlurStore.ts"
      via: "imports motionBlurStore for keyboard toggle"
      pattern: "motionBlurStore\\.toggleEnabled"
---

<objective>
Add keyboard shortcut, finalize tests, and visually verify the complete motion blur feature end-to-end.

Purpose: Polish the feature with keyboard shortcut access and verify all requirements are met via visual testing.
Output: Keyboard shortcut wired, complete test coverage, visual confirmation of all MBLR requirements.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-motion-blur/21-CONTEXT.md
@.planning/phases/21-motion-blur/21-01-SUMMARY.md
@.planning/phases/21-motion-blur/21-02-SUMMARY.md
@.planning/phases/21-motion-blur/21-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keyboard shortcut and final test coverage</name>
  <files>
    Application/src/lib/shortcuts.ts,
    Application/src/stores/motionBlurStore.test.ts,
    Application/src/lib/motionBlurEngine.test.ts
  </files>
  <read_first>
    Application/src/lib/shortcuts.ts,
    Application/src/stores/motionBlurStore.ts,
    Application/src/stores/motionBlurStore.test.ts,
    Application/src/lib/motionBlurEngine.ts,
    Application/src/lib/motionBlurEngine.test.ts
  </read_first>
  <action>
    **Part A: Add keyboard shortcut 'M' for motion blur toggle**

    1. Read `shortcuts.ts` to understand the existing pattern. Look for how 'Shift+B' (blur bypass) is registered.

    2. Add import:
       ```typescript
       import {motionBlurStore} from '../stores/motionBlurStore';
       ```

    3. Add a keyboard shortcut for 'M' (lowercase m, no modifiers) that calls `motionBlurStore.toggleEnabled()`. Follow the exact same pattern as the existing blur bypass shortcut.

    4. IMPORTANT: Per project memory note "Guard shortcuts in paint mode" — check if the shortcut handler has a guard for `isPaintEditMode()`. If so, add the 'M' shortcut to the guarded section so it does NOT fire when the user is in paint mode (where M might conflict with paint tools). Look for how other single-letter shortcuts are guarded.

    **Part B: Extend test files for complete coverage**

    Add these additional tests to `motionBlurStore.test.ts`:
    - Test: toggleEnabled twice returns to false
    - Test: setShutterAngle clamps to 0 minimum
    - Test: setShutterAngle clamps to 360 maximum

    Add these additional tests to `motionBlurEngine.test.ts`:
    - Test: VelocityCache returns null for first frame (no previous data)
    - Test: VelocityCache returns correct velocity for sequential frames
    - Test: VelocityCache invalidates on non-sequential frame (seek: frame 5 then frame 20)
    - Test: VelocityCache.clear() resets all state (returns null after clear)
    - Test: computeLayerVelocity with zero deltas returns all zeros
    - Test: isStationary with velocity {dx: 0.1, dy: 0.1, dRotation: 0.1, dScale: 0.1} returns true (sum=0.4 < 0.5)
    - Test: isStationary with velocity {dx: 0.3, dy: 0.3, dRotation: 0, dScale: 0} returns false (sum=0.6 >= 0.5)

    Use vitest `describe`/`it` blocks. For VelocityCache tests, create KeyframeValues objects with the required fields (opacity, x, y, scaleX, scaleY, rotation, blur).
  </action>
  <verify>
    <automated>cd Application && npx vitest run src/stores/motionBlurStore.test.ts src/lib/motionBlurEngine.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Application/src/lib/shortcuts.ts contains `motionBlurStore.toggleEnabled`
    - Application/src/lib/shortcuts.ts contains `import.*motionBlurStore`
    - Application/src/stores/motionBlurStore.test.ts contains `toggleEnabled twice`
    - Application/src/stores/motionBlurStore.test.ts contains `clamps to 0`
    - Application/src/stores/motionBlurStore.test.ts contains `clamps to 360`
    - Application/src/lib/motionBlurEngine.test.ts contains `VelocityCache`
    - Application/src/lib/motionBlurEngine.test.ts contains `returns null for first frame`
    - Application/src/lib/motionBlurEngine.test.ts contains `invalidates on non-sequential`
    - All tests exit 0
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Keyboard shortcut 'M' toggles motion blur (guarded in paint mode). Full test coverage for motionBlurStore and motionBlurEngine including VelocityCache seek invalidation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete motion blur feature</name>
  <files>N/A (visual verification only)</files>
  <action>
    This is a human verification checkpoint. The executor presents the verification steps below to the user and waits for approval.
    All code changes were completed in Plans 01-03 and Task 1 of this plan. This task verifies the complete feature visually.
  </action>
  <what-built>
    Complete per-layer motion blur system:
    - WebGL2 GLSL directional blur shader (glMotionBlur.ts)
    - Velocity computation from keyframe deltas (motionBlurEngine.ts)
    - Preview integration with per-layer blur pass (previewRenderer.ts)
    - Export with combined GLSL + sub-frame accumulation (exportRenderer.ts + exportEngine.ts)
    - Toolbar toggle button with shutter angle + quality popover (Toolbar.tsx)
    - Export dialog with Motion Blur section (FormatSelector.tsx)
    - Project persistence in .mce format v15 (projectStore.ts)
    - Keyboard shortcut 'M' for toggle (shortcuts.ts)
  </what-built>
  <how-to-verify>
    **MBLR-01 + MBLR-09: Toggle and smooth playback**
    1. Open a project with keyframe-animated content layers (moving position)
    2. Press 'M' or click the Zap icon in toolbar — motion blur should toggle on
    3. Press play — moving layers should show directional blur streaks
    4. Playback should maintain smooth fps (no major frame drops)

    **MBLR-02: Per-layer directional blur**
    5. Verify blur direction matches movement direction (horizontal motion = horizontal streak)
    6. Verify faster-moving layers have more visible blur than slower ones

    **MBLR-03 + MBLR-04: Shutter angle and quality**
    7. Click dropdown arrow next to Zap icon — popover should appear
    8. Drag shutter angle slider from 180 to 360 — blur should intensify
    9. Drag to 0 — blur should disappear
    10. Switch quality between Off/Low/Medium — blur sharpness should change

    **MBLR-08: Stationary layers**
    11. Verify layers that are NOT moving remain perfectly sharp (no blur)

    **MBLR-05 + MBLR-06: Export with motion blur**
    12. Open export dialog — verify "Motion Blur" section appears
    13. Enable motion blur in export, set sub-frames to 8
    14. Export a few frames — verify exported frames show smooth motion blur

    **MBLR-07: Persistence**
    15. With motion blur enabled and shutter angle set to a non-default value, save the project
    16. Close and reopen the project — verify motion blur is still enabled with correct settings

    **Backward compatibility**
    17. Open an older project (saved before motion blur) — verify no errors, motion blur defaults to off
  </how-to-verify>
  <verify>
    <automated>cd Application && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms MBLR-01: motion blur toggles on/off
    - User confirms MBLR-02: moving layers show directional blur
    - User confirms MBLR-03: shutter angle changes blur intensity
    - User confirms MBLR-04: quality tier changes visible
    - User confirms MBLR-05: export dialog shows motion blur section
    - User confirms MBLR-06: exported frames have motion blur
    - User confirms MBLR-07: settings persist after save/load
    - User confirms MBLR-08: stationary layers stay sharp
    - User confirms MBLR-09: playback maintains smooth fps
  </acceptance_criteria>
  <done>User approves visual verification of all 9 MBLR requirements</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd Application && npx vitest run --reporter=verbose` — all tests green
- `cd Application && npx tsc --noEmit` — clean compilation
- Visual verification confirms all MBLR requirements met
</verification>

<success_criteria>
1. Keyboard shortcut 'M' toggles motion blur on/off (guarded in paint mode)
2. All unit tests pass (store + engine + velocity cache)
3. Visual: moving layers blur directionally, stationary layers stay sharp
4. Visual: shutter angle and quality controls affect blur appearance
5. Visual: export produces correctly blurred frames with sub-frame accumulation
6. Visual: save/load preserves all motion blur settings
7. Visual: old projects load without errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-motion-blur/21-04-SUMMARY.md`
</output>
