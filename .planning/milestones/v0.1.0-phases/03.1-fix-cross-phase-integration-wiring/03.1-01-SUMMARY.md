---
phase: 03.1-fix-cross-phase-integration-wiring
plan: 01
subsystem: integration
tags: [rust, preact-signals, auto-save, thumbnail, uiStore, markDirty-callback]

# Dependency graph
requires:
  - phase: 02-ui-shell-image-pipeline
    provides: image_pool.rs process_image, imageStore signals
  - phase: 03-project-sequence-management
    provides: projectStore hydrate/close, autoSave effect, sequenceStore markDirty pattern
provides:
  - Thumbnail directory consistency between image_pool.rs and project_io.rs
  - Auto-save triggers on image import via effect subscription and isDirty flag
  - uiStore.selectedSequenceId syncs on project open and resets on project close
affects: [04-timeline-playback, 05-fx-layers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "markDirty callback pattern extended to imageStore (mirrors sequenceStore pattern)"
    - "Auto-save effect subscribes to all mutable store signals including images"

key-files:
  created: []
  modified:
    - Application/src-tauri/src/services/image_pool.rs
    - Application/src/stores/imageStore.ts
    - Application/src/lib/autoSave.ts
    - Application/src/stores/projectStore.ts

key-decisions:
  - "markDirty callback pattern reused for imageStore (same as sequenceStore) to avoid circular imports"
  - "Both effect subscription AND isDirty flag needed for auto-save: effect triggers scheduleSave, isDirty passes the guard"

patterns-established:
  - "markDirty callback pattern: any store that mutates data calls _markDirty?.() to propagate dirty state to projectStore"
  - "Auto-save effect must subscribe to all mutable store signals that represent saveable state"

requirements-completed: [IMPT-03, PROJ-02, PROJ-03, PROJ-04, SEQN-01]

# Metrics
duration: 2min
completed: 2026-03-03
---

# Phase 3.1 Plan 01: Gap Closure Summary

**Fixed three cross-phase integration wiring bugs: thumbnail directory mismatch (.thumbs), auto-save not triggering on image import, and uiStore.selectedSequenceId not syncing on project open/close**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-03T11:31:51Z
- **Completed:** 2026-03-03T11:33:33Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Reverted image_pool.rs thumbnail directory from "thumbs" to ".thumbs" to match project_io.rs convention
- Added _setImageMarkDirtyCallback to imageStore and wired it in projectStore module init, ensuring image imports set isDirty=true
- Added imageStore.images.value to auto-save effect subscription so image changes trigger debounced save
- Added uiStore.selectSequence() call in hydrateFromMce so opening a project syncs the selected sequence highlight
- Added uiStore.reset() call in closeProject so closing a project clears stale selectedSequenceId

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix thumbnail directory mismatch and add imageStore markDirty callback** - `1c14634` (fix)
2. **Task 2: Wire auto-save subscription, imageStore markDirty callback, and uiStore sync** - `b2a59d7` (fix)

## Files Created/Modified
- `Application/src-tauri/src/services/image_pool.rs` - Reverted thumbnail dir from "thumbs" to ".thumbs" (1-char fix)
- `Application/src/stores/imageStore.ts` - Added _setImageMarkDirtyCallback export and _markDirty?.() call after import batch
- `Application/src/lib/autoSave.ts` - Added imageStore import and imageStore.images.value to effect subscription
- `Application/src/stores/projectStore.ts` - Added uiStore import, selectSequence in hydrate, reset in close, imageMarkDirty wiring

## Decisions Made
- Reused the markDirty callback pattern (identical to sequenceStore) for imageStore to avoid circular imports between imageStore and projectStore
- Both effect subscription AND isDirty flag are needed: the effect triggers scheduleSave(), but scheduleSave()'s guard requires isDirty to be true

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All three integration gaps from the v1.0 milestone audit are now closed
- Thumbnails survive temp-to-real project migration (both files use .thumbs)
- Image imports trigger auto-save within 2 seconds (effect + isDirty)
- Project open/close correctly syncs uiStore.selectedSequenceId
- Application builds successfully with no TypeScript errors
- Ready to continue to Phase 4 (Timeline & Playback)

## Self-Check: PASSED

All 4 modified files verified on disk. Both task commits (1c14634, b2a59d7) verified in git log. SUMMARY.md exists at expected path.

---
*Phase: 03.1-fix-cross-phase-integration-wiring*
*Completed: 2026-03-03*
