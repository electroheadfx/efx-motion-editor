---
phase: quick-260925-iy6
plan: 260925-iy6
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/paperPass.test.ts
  - app/src/lib/paperPass.frameGuard.test.ts
  - app/src/lib/paperPass.ts
  - app/src/lib/projectPaperRaster.ts
  - app/src/stores/physicPaintStore.ts
  - packages/efx-physic-paint/src/index.ts
  - .planning/quick/260925-iy6-dynamic-post-bake-paper-pass-holes-relie/260925-iy6-SUMMARY.md
autonomous: true
requirements: [QUICK-260925-IY6]
estimate:
  tokens: 50000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A baked full-opacity stroke on a textured paper shows the REAL PAPER through the paint: the valley map darkens toward the paper's own tint (never toward black — tint-floored multiply), the peak map lifts fibers, at 100% paint opacity — encoded at tile BUILD time via s = (h - 0.5) * 2 * strength with conditioned h; unit-pinned by encodePassTiles formula rows (neutral 0.5 → white/black no-op, h=0.1 → paper-tint valley, h=0.9 → tint-scaled lift, strength 0 → fully neutral) and natively by UAT row 1; paint alpha untouched (destination-in alpha restore; depositAlpha/strokeOpacity/physics files diff-guarded empty — 260924-m7w PIN 0 law)"
    - "Changing paper, grain scale, or grain strength AFTER bake re-textures the already-baked paint immediately, no repaint and no re-bake, on canvas AND export — unit-pinned: the tile cache key contains exactly paper texture key + WxH + normalized grainScale + strength (any single variation yields a different tile), the fond signature already carries grainStrength/grainScale/paperGrain, and setRotoBackgroundMetadata clears both memos + bumps revision; native UAT rows 2-3"
    - "The display frame path contains NO per-pixel JS loop for the paper pass: physicPaintStore.ts has zero pixel-array canvas API occurrences (base-green control, must stay zero), the applyPaperPass function region has zero pixel-array canvas API occurrences (region-scoped source guard), and the pass is drawImage + globalCompositeOperation (≤5 GPU draws) against a precomputed tile — RED pin 3, unit"
    - "The export pixel path honours the SAME pass: exactly ONE applyPaperPass call site exists (in _resolveFlattenedFrame, between compositeFrame and the fond draw), exportRenderer.ts and previewRenderer.ts contain no pass implementation of their own, and export reaches the passed raster through PreviewRenderer → getFlattenedFrame — structural unit pins + native parity UAT row (exported file vs canvas capture of the same frame)"
    - "Grain-off and None strength fully skip the pass: paperGrain '' (CR-01 round-trip) or grainStrength ≤ 0 → applyPaperPass returns before touching the context (zero logged ops in unit); strength modulates tooth depth ONLY — never paint opacity"
    - "One paper sheet: the pass runs ONCE on the flattened mixed-paint raster after compositeFrame and before the fond draw — never inside compositeFrame, never per track/layer — so stacking layers cannot fill the holes; source-shape ordering pin + native UAT row 7"
    - "Guardrails held: physics sim/brush files, document format, the TopBar grain-strength segmented control, the 260924-ffd grain-scale stepper, physicsPaintStudioSettings.ts (CR-01 mapping) and package.json/pnpm-lock all untouched (diff guardrails empty); no installs, no push, no ROADMAP edit"
  artifacts:
    - app/src/lib/paperPass.test.ts (RED pins: tile encode formulas, cache keying, apply GCO contract, '' skip, package/raw-image exports)
    - app/src/lib/paperPass.frameGuard.test.ts (RED pins: single call site + ordering in the store; pixel-array API absence; export/preview single-implementation chain)
    - app/src/lib/paperPass.ts (tile builder + applyPaperPass shared routine; pixel work at build time only)
    - app/src/lib/projectPaperRaster.ts (raw decoded texture getter + grain-scale normalizer exports — decode-once cache reused)
    - packages/efx-physic-paint/src/index.ts (one-line conditionHeightMap export — conditioning shared, no drift)
    - app/src/stores/physicPaintStore.ts (single pass call in _resolveFlattenedFrame + texture-resolve subscription for the includeFond=false branch)
    - .planning/quick/260925-iy6-dynamic-post-bake-paper-pass-holes-relie/260925-iy6-SUMMARY.md (status automated-ready + native UAT rows)
  key_links:
    - "setRotoBackgroundMetadata (paper/scale/strength change) → memo clear + revision bump → _resolveFlattenedFrame memo MISS → getPaperPassTile(paper, WxH, scale, strength) → applyPaperPass(result raster) → record freeze/WebP encode → previewRenderer AND exportRenderer both consume that one record"
    - "_ensureFondTextureSubscription(textureKey) → texture resolve → flattened memo clear → next resolve rebuilds the tile; unresolved texture → null tile → pass skipped (deterministic, no blank draw)"
    - "package conditionHeightMap export → paperPass build → identical mean-centre/clamp conditioning as the engine (260925-dso law, single source of truth)"
---

<objective>
Add a dynamic post-bake paper pass: after the paint is baked and layers are mixed (v1 compositor law), modulate the mixed-paint raster with a precomputed per-paper signed-height tile so valleys/tooth show the real paper through the paint (paper-tinted modulation — not alpha-punch, not black multiply) and peaks/fibers lift, at 100% paint opacity. Changing paper / grain scale / grain strength re-textures already-baked paint immediately on canvas and export; playback stays real-time with zero per-frame pixel loops.

Purpose: 260925-dso (9a) deleted the bake-time grain/emboss passes; this quick (9b) re-adds the tooth where it belongs — the composite stage, driven by the chosen paper, invalidated live through the existing metadata chain. The pass lives at ONE seam (_resolveFlattenedFrame) so preview, program monitor, and export structurally share it — the dual-pipeline failure that 9a just cleaned up must not reappear.

Output: two RED pin files at base, GREEN implementation (paperPass lib + one call site + two one-line exports), guardrailed full battery, and a SUMMARY with status automated-ready and native UAT rows. No installs, no push, no format change, no UI redesign.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quick/260925-iy6-dynamic-post-bake-paper-pass-holes-relie/260925-iy6-CONTEXT.md
@.planning/quick/260925-iy6-dynamic-post-bake-paper-pass-holes-relie/260925-iy6-RESEARCH.md

Key code anchors (all line numbers verified live this session):
- app/src/stores/physicPaintStore.ts — `const result = compositeFrame(...)` at 2611; `const fondInstruction = includeFond ? ...` at 2626; fond block `_ensureFondTextureSubscription` call at 2634, `fondCtx.drawImage(result.raster, 0, 0)` at ~2647, record freeze after; insertion point = between 2611 and 2626 (applies in BOTH fond and includeFond=false branches). `_resolveFondSource` at 1540 (kind 'paper' carries metadata.background / paperGrain / grainStrength / grainScale); `_fondSourceSignature` at 1607-1611 already includes paperGrain, grainStrength, grainScale; `_ensureFondTextureSubscription` at 1632-1641 (resolve → clear `_flattenedMemo` + bump `physicPaintVersion`); `setRotoBackgroundMetadata` at 2850 (idempotence guard, clears `_flattenedMemo` + `_trackRasterMemo`, `bumpTrackRevision`). Store currently contains ZERO pixel-array canvas API occurrences — baseline control.
- app/src/lib/projectPaperRaster.ts — private `textureCache: Map<string, HTMLImageElement>` (line 7), `cacheKey = \`${paperTexture}:${width}x${height}:${normalizeGrainScale(scale)}\`` (line 93), pattern.setTransform idiom at 38-42 (scale ≠ 1 only), `subscribeProjectPaperTextureResolve` at 139.
- packages/efx-physic-paint/src/core/paper.ts — `conditionHeightMap` at 71 (mean-centre 0.5, clamp [0.10, 0.90], idempotent; 260925-dso locked). packages/efx-physic-paint/src/index.ts exports only engine/types/transform (lines 2-17) — conditionHeightMap NOT exported yet.
- app/src/lib/rotoFrameDraw.ts:130 — the fond grain gate precedent: `if (!instruction.paperGrain || grainStrength <= 0) return;` — the pass mirrors this gate exactly (CR-01 grain-off '' → skip; strength None/0 → skip).
- app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx:44-49 — GRAIN_STRENGTH_OPTIONS = segmented presets [{None,0},{Soft,0.35},{Med,0.65},{Hard,0.95}] — NOT a stepper (reconciliation note in planning_context). The 260924-ffd NumericStepper is the grain SCALE control (line 56-58). Both stay untouched.
- Consumption chain: previewRenderer.ts:216/272 calls `physicPaintStore.getFlattenedFrame(...)`; exportRenderer.ts:1 imports PreviewRenderer and calls `renderGlobalFrame(renderer, ...)` at 442 — export therefore renders through previewRenderer's getFlattenedFrame seam. PhysicsPaintProgramMonitor uses `getFlattenedFrame(..., false)` (includeFond=false).
- Test precedents: packages/efx-physic-paint/src/brush/paint.grainRemoval.test.ts (source-shape + canvasFactory stub + document stub pattern); app/src/lib/projectPaperRaster.test.ts (canvas/getContext stubbing); app/vitest.config.ts collects `src/**/*.test.ts` ONLY.
</context>

<tasks>

<!-- planner-discipline-allow: getImageData -->
<!-- planner-discipline-allow: putImageData -->

<task type="tdd">
  <name>Task 1: RED — paper-pass pins: tile encode, cache keying, apply contract, frame/export guards</name>
  <files>app/src/lib/paperPass.test.ts, app/src/lib/paperPass.frameGuard.test.ts</files>
  <behavior>
  - P1 encode (pure): `encodePassTiles(height, width, heightPx, strength, tint)` with tint {r:230,g:224,b:212}:
    (a) h=0.5 → valley pixel [255,255,255] and peak pixel [0,0,0] (neutral — multiply/lighten no-ops);
    (b) h=0.1, strength=0.65 (s=-0.52, v=0.52) → valley [242,239,233] = round(255 - v*(255-tint)) per channel — paper-tinted, every channel > 100 (never black multiply, never toward 0);
    (c) h=0.9, strength=0.65 (u=0.52) → peak [120,116,110] = round(u*tint) per channel (fiber lift);
    (d) strength=0 → every valley white and every peak black regardless of h (None = no tooth);
    (e) all output alpha bytes 255 (straight-alpha tiles).
  - P2 build + cache: `getPaperPassTile(paperTexture, width, height, grainScale, strength)` with a fake raw image {width:4,height:4} and a stubbed canvas (document.createElement stub, createPattern/fillRect/getImageData/putImageData log):
    (a) RED at base — module does not exist (import failure);
    (b) constant red-channel input (all bytes 200) → conditioned to 0.5 → tiles fully NEUTRAL (all valleys white, all peaks black) — proves the build pipes through conditionHeightMap (unconditioned h≈0.788 would produce non-neutral output);
    (c) cache identity: same params twice → same tile object (toBe); differing ONLY paperTexture / width / height / grainScale / strength → different objects (five separate rows — the full key is pinned);
    (d) `getPaperPassTile('' , ...)` (CR-01 grain-off) returns null with ZERO canvas ops logged;
    (e) unresolved texture (raw image getter returns null) returns null with zero canvas ops;
    (f) byte-budget eviction (per-entry bytes = width×height×8, budget 64MB): build a tile at width/height args 2560×2560 (≈52.4MB, fake source stays 4×4) under key A, then the same size under a DIFFERENT key B — total ≈105MB > 64MB → eviction runs oldest-first until under budget (the older entries, including A, drop out; the 12-entry cap is nowhere near) — rebuilding A returns a NEW object (not the cached one) and B (the just-inserted entry) remains cached (toBe); during rows (c)-(e) the tiny entries never come anywhere near either bound.
  - P3 apply: `applyPaperPass(ctx, width, height, tile)` asserted against a UNIFIED op log — the stub's 2D-context factory returns a logging context for EVERY getContext call (the passed ctx AND the tmp-canvas context the routine creates internally), all appending to ONE shared log; clear that log immediately before invoking applyPaperPass so the assertion covers only the routine's own ops. The pin spans the whole routine in exactly the order Task 2's sequence produces:
    (a) null/undefined tile → log EXACTLY empty (early return before creating the tmp canvas or touching any context — CR-01/None);
    (b) with tile → first logged op is save (the routine saves BEFORE its capture drawImage), last op is restore; the GCO assignment sequence contains 'multiply', 'lighten', 'destination-in', and 'copy' in that order; it NEVER assigns 'destination-out' (alpha-punch is a locked rejection);
    (c) drawImage count ≤ 5 (capture + 2 blend passes + alpha restore + writeback — all GPU, no pixel loop);
    (d) the unified log contains no pixel-array read/write API calls (the getImageData/putImageData family) — the routine is drawImage/composite-op only;
    (e) source-shape leg: within paperPass.ts, the region from `export function applyPaperPass` to the next top-level export contains no pixel-array API identifiers (region-scoped — build functions elsewhere in the file legitimately use them).
  - P4 frame/export guard (readFileSync source-shape, mirror paint.grainRemoval.test.ts):
    (a) physicPaintStore.ts source contains EXACTLY ONE occurrence of `applyPaperPass(` — RED at base (zero);
    (b) ordering: index of `const result = compositeFrame(` < index of `applyPaperPass(` < index of `fondCtx.drawImage(` — RED at base (middle missing);
    (c) CONTROL green at base: physicPaintStore.ts contains zero pixel-array canvas API occurrences anywhere (must remain zero);
    (d) CONTROL green at base + single-implementation pins: exportRenderer.ts and previewRenderer.ts contain zero `applyPaperPass` occurrences; previewRenderer.ts contains `getFlattenedFrame(`; exportRenderer.ts contains `renderGlobalFrame(` (the export→preview→seam chain);
    (e) RED at base: packages/efx-physic-paint/src/index.ts source matches an export statement for conditionHeightMap (read via `new URL('../../../packages/efx-physic-paint/src/index.ts', import.meta.url)`); RED at base: app/src/lib/projectPaperRaster.ts exports `getProjectPaperTextureImage` and `normalizeGrainScale`.
  - Expected RED evidence: paperPass.test.ts fails on the missing module; frameGuard fails on legs (a)(b)(e-export)(e-raw getter); controls (c)(d) green and prove the harnesses.
  </behavior>
  <action>TDD RED step — write ONLY the two test files, zero production edits. Read app/src/lib/projectPaperRaster.test.ts and packages/efx-physic-paint/src/brush/paint.grainRemoval.test.ts first and mirror their harness idioms: canvasFactory-style context stubs with an op log, `vi.stubGlobal('document', { createElement })`, and readFileSync + `new URL(..., import.meta.url)` for source-shape legs. Encode formulas are exact — P1 rows hardcode the expected byte values listed in the behavior block (the rounding rule is Math.round per channel); tint input to encodePassTiles is supplied by the caller (the build computes the mean tint from sampled pixels — not under test here). For P2 build rows, craft the stub getImageData to return a Uint8ClampedArray you control (constant 200 for the conditioning proof; any varied pattern for the cache rows — cache identity does not depend on pixel content). For P3, the 2D-context stub records: save/restore pushes, a `gco` setter capturing assigned values in order, drawImage pushes — and EVERY context the stub factory hands out (the passed ctx AND the routine's internal tmp context) appends to the SAME shared log, so the pin spans the whole routine; clear the log right before calling applyPaperPass, then assert the exact gate list from the behavior block (first op save, last op restore, GCO order multiply→lighten→destination-in→copy). For P4 use substring indexOf ordering checks exactly as specified — leg (b) must compare raw indexOf results so a missing middle term fails, not passes. Keep every negative assertion region-scoped or control-verified per the behavior block (leg P4c is a base-green CONTROL — it must pass before your edits, proving the guard is wired correctly). Name both files `.test.ts` — vitest collects only that extension. Commit: test(260925-iy6): RED — paper-pass tile encode, cache keying, apply contract, frame/export guards. Do NOT touch any production file in this task.
  </action>
  <verify>
  <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && ./node_modules/.bin/vitest run src/lib/paperPass.test.ts src/lib/paperPass.frameGuard.test.ts</automated>
  </verify>
  <done>Both files exist and RUN: paperPass.test.ts fails (module missing), frameGuard fails on the four RED legs listed above while every CONTROL leg passes; RED output recorded (failing test names + expected/actual) for the SUMMARY; `git diff --name-only` shows zero production files; commit exists.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — paperPass lib, two one-line exports, single call site in _resolveFlattenedFrame</name>
  <files>packages/efx-physic-paint/src/index.ts, app/src/lib/projectPaperRaster.ts, app/src/lib/paperPass.ts, app/src/stores/physicPaintStore.ts, app/src/lib/paperPass.test.ts, app/src/lib/paperPass.frameGuard.test.ts</files>
  <behavior>
  - All Task 1 pins GREEN: encode formulas exact, cache keying across all five terms, byte-budget eviction row, '' and unresolved texture return null with zero ops, apply contract (unified log: save→capture→multiply→lighten→destination-in→copy→restore, no destination-out, ≤5 drawImage, no pixel-array ops), single ordered call site, package export, raw-image getter export.
  - app typecheck clean; package `tsc --noEmit` clean; full app + package suites at base results.
  - Guardrail: the only touched production files are the four listed — physics sim, brush, types/format, TopBar, studio settings, package manifests byte-identical (diff gate in verify).
  </behavior>
  <action>GREEN step, in three commits.

  Commit 2a (shared inputs): in packages/efx-physic-paint/src/index.ts add the one-line re-export `export { conditionHeightMap } from './core/paper'` — behavior inside the package unchanged (260925-dso conditioning law untouched; this is the A4-recommended export so the app-side tile builder shares the ONE conditioner instead of re-implementing it — dual-pipeline drift is the forbidden pattern). In app/src/lib/projectPaperRaster.ts add two additive exports reading existing state: `getProjectPaperTextureImage(paperTexture: string): HTMLImageElement | null` (returns `textureCache.get(paperTexture)` — decode-once cache reused, never a new Image) and `export` the existing `normalizeGrainScale` (tile keys must normalize identically to the fond cacheKey, line 93 — no key drift).

  Commit 2b (the lib): create app/src/lib/paperPass.ts exporting exactly what Task 1 imports:
  - `encodePassTiles(height: Float32Array, width: number, heightPx: number, strength: number, tint: {r:number;g:number;b:number}): { valley: Uint8ClampedArray; peak: Uint8ClampedArray }` — per pixel: s = (h - 0.5) * 2 * strength; v = max(0, -s); u = max(0, s); valley RGB = Math.round(255 - v * (255 - tintChannel)) per channel (paper-tinted floor — the valley at full depth approaches the paper tone, NEVER black: this is the locked hole-color semantics); peak RGB = Math.round(u * tintChannel) per channel (fiber lift); alpha 255 everywhere; strength 0 → white/black neutral. Signed height is evaluated HERE, at build time — never at draw time.
  - `getPaperPassTile(paperTexture: string, width: number, height: number, grainScale: number, strength: number): PaperPassTile | null` — gate first: falsy paperTexture OR strength ≤ 0 OR `getProjectPaperTextureImage` returns null → return null with zero canvas ops (CR-01 grain-off + None + unresolved texture). Cache key `v1:${paperTexture}:${width}x${height}:${normalizeGrainScale(grainScale)}:${strength}` in a module-level Map; on miss: draw the raw image tiled at scale into a temp canvas using the proven pattern.setTransform idiom (scale !== 1 only, guard for fakes without setTransform — copy the lines 36-52 pattern but WITHOUT the white fill and WITHOUT the 0.18 alpha; this canvas is a height probe, not a fond draw), read pixels ONCE (build time only), red/255 into a Float32Array, pipe through the imported `conditionHeightMap`, compute the mean RGB tint of the sampled pixels as the tint input (assumption A3 — the paper's own tone), encode via `encodePassTiles`, put each channel map into its own canvas (putImageData at BUILD time is fine), store. Bound the cache on TWO axes, evicting oldest insertion (Map order) whenever either binds: (i) entry cap 12; (ii) byte budget 64MB — track approximate stored bytes per entry as width×height×8 (valley + peak RGBA maps) and evict oldest until total ≤ 64MB, EXCEPT never evict the just-inserted entry (an entry larger than the budget is kept as the sole occupant). The entry cap alone bounds cardinality but not footprint (12 full-HD entries ≈ 200MB — this WKWebView has a documented OOM history); sizes/scales/strengths are finite but unbounded in theory.
  - `applyPaperPass(ctx: CanvasRenderingContext2D, width: number, height: number, tile: PaperPassTile | null): void` — THE shared routine. Signature guardrail: it takes a target 2D context + width/height and never reaches for the live/global canvas (ctx.canvas is the caller's own). Sequence (ORDER IS PINNED — Task 1's P3 asserts the unified op log across the passed ctx and the tmp context in exactly this order): (0) `if (!tile) return` before creating the tmp canvas or touching ANY context op; (1) create the tmp canvas of width×height — no context ops yet; (2) tmpCtx save → drawImage(ctx.canvas) capture → globalCompositeOperation = 'multiply' → drawImage(tile.valley, 0, 0, width, height) → restore — the save comes BEFORE the capture, so the unified log's first op is save and the capture rides the default source-over; (3) tmpCtx save → 'lighten' → drawImage(tile.peak, 0, 0, width, height) → restore; (4) tmpCtx save → 'destination-in' → drawImage(ctx.canvas) → restore — THIS is the alpha restore required by the straight-alpha law D-02: a blend-mode draw also paints where the destination is transparent, and without this mask the tile color would leak into the unpainted regions (and, in the fond branch, cover the paper background — pitfall 1). It costs one GPU drawImage and keeps the paint alpha byte-exact — no destination-out, no alpha math, PIN 0 laws untouched; (5) ctx save → 'copy' → drawImage(tmp, 0, 0) → restore (writeback with original alpha). Unified log reads: save, drawImage, multiply, drawImage, restore, save, lighten, drawImage, restore, save, destination-in, drawImage, restore, save, copy, drawImage, restore — first op save, last op restore, GCO assignment order multiply→lighten→destination-in→copy, exactly 5 drawImage, no destination-out (this is the literal reading Task 1's P3 pins). Total ≤5 drawImage, all GPU-composited; 1-2 of them are the modulation blend passes per the budget — everything else is the D-02 alpha-preservation scaffolding. Zero pixel-array canvas calls in this function (the only pixel loops in the file live in the build path). IMPORTANT comment discipline: do not name the pixel-array API identifiers inside applyPaperPass or its comments — the frameGuard region leg greps that region.
  Commit 2b message: feat(260925-iy6): precomputed paper pass tiles + shared applyPaperPass routine.

  Commit 2c (the seam): in app/src/stores/physicPaintStore.ts — import `applyPaperPass, getPaperPassTile` and insert ONE call block between line 2611 (`const result = compositeFrame(...)`) and line 2626 (`const fondInstruction = ...`), so it runs in BOTH the fond and includeFond=false branches. The block: resolve `const passSource = _resolveFondSource(layerId, efxDocument)`; if it is kind 'paper', derive textureKey = metadata.background, gate on `metadata.paperGrain` truthy AND `metadata.grainStrength > 0` (mirror the fond grain gate at rotoFrameDraw.ts:130 — CR-01: '' means grain off, pass fully skipped); call `_ensureFondTextureSubscription(textureKey)` (this MUST also run when includeFond=false — today only the fond branch subscribes, so a Studio monitor frame would never pick up a late-arriving texture); then `const passTile = getPaperPassTile(textureKey, size.width, size.height, metadata.grainScale ?? 1, metadata.grainStrength)`; if passTile, get the 2d context from result.raster and call `applyPaperPass(ctx, size.width, size.height, passTile)` — the routine writes back in place, so `result.raster` itself carries the pass and the fond draw, record freeze, and WebP encode all consume it unchanged. BEFORE mutating in place, confirm (one read of compositeFrame's return path) that `result.raster` is a canvas freshly created for THIS flattened call and not a shared/memoized reference (A2 assumption); if it is shared, copy to a private canvas first and use that as the record raster — still ≤5 draws, still no pixel loop. Invalidation needs NO new code: setRotoBackgroundMetadata already clears both memos and _fondSourceSignature already rotates on paper/grainStrength/grainScale — a metadata change makes the next _resolveFlattenedFrame a memo MISS, which misses the tile cache (new key), and rebuilds. Strength UI activation: verify (grep the existing path) that TopBarSegmented → onGrainStrengthChange → setGrainStrength → buildRotoBackgroundMetadata → setRotoBackgroundMetadata already carries grainStrength end-to-end; it does — so NO UI file changes: the segmented preset control stays AS-IS (reconciliation note: it is not the 260924-ffd NumericStepper; the stepper is the grain SCALE control — spatial only, untouched). No new hooks, effects, or signals anywhere — store edits are plain module functions (efx-preact-reactivity: nothing subscribes, nothing writes signals; idempotence guard in setRotoBackgroundMetadata untouched). Commit 2c message: feat(260925-iy6): apply the paper pass in _resolveFlattenedFrame (single seam, preview+export).

  HARD GUARDRAILS during 2a-2c: if the only viable implementation were a per-pixel JS loop in the frame path, STOP and report (escalate to phase/GPU) — the design above is the non-loop path. Do NOT reintroduce any bake-time grain/emboss/fbm helper deleted in 260925-dso. Do NOT touch: wet-layer/fluids/drying/compositor/paint/erase (thickness field 260924-stb, Spread 260925-b7c, deposit laws), types (format), PhysicsPaintTopBar.tsx, physicsPaintStudioSettings.ts, any manifest, premultiplied-alpha buffers (D-02 straight alpha holds), layer blend modes, erase semantics, or the fond placement (D-09 — fond stays beneath; the pass modulates the mixed paint above it).
  </action>
  <verify>
  <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && ./node_modules/.bin/vitest run src/lib/paperPass.test.ts src/lib/paperPass.frameGuard.test.ts; p=$?; npm run typecheck; t=$?; cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && npm run check; c=$?; ../../app/node_modules/.bin/vitest run; d=$?; g="$(git -C /Users/lmarques/Dev/efx-motion-editor diff --name-only HEAD -- packages/efx-physic-paint/src/core packages/efx-physic-paint/src/brush packages/efx-physic-paint/src/render packages/efx-physic-paint/src/types.ts packages/efx-physic-paint/src/engine app/src/components/physic-paint/view/PhysicsPaintTopBar.tsx app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts app/src/types/physicPaint.ts package.json app/package.json packages/efx-physic-paint/package.json pnpm-lock.yaml)"; test $p -eq 0 && test $t -eq 0 && test $c -eq 0 && test $d -eq 0 && test -z "$g"</automated>
  </verify>
  <done>All pins GREEN; typecheck clean both packages; full app and package suites at base results; single ordered call site wired between compositeFrame and the fond draw; the pass gates on paperGrain/grainStrength and skips grain-off; raw-image getter + conditionHeightMap exports live; guardrail diff list EMPTY (sim/brush/types/UI/manifests untouched); RED evidence recorded before, GREEN after; no done claim before native UAT.</done>
</task>

<task type="auto">
  <name>Task 3: full battery, guardrails, SUMMARY with native UAT rows</name>
  <files>.planning/quick/260925-iy6-dynamic-post-bake-paper-pass-holes-relie/260925-iy6-SUMMARY.md</files>
  <action>Run the full battery and write the SUMMARY. Battery: both new pin files; the full app suite (`cd /Users/lmarques/Dev/efx-motion-editor/app && ./node_modules/.bin/vitest run`); the full package suite plus `npm run check` in packages/efx-physic-paint (the law gates — PIN 0 body ratio, thickness field, Spread calibration, conditioning law — must be at base results; if ANY law gate fails because of this quick's changes, STOP immediately, commit nothing further, and report the failing gate with measured numbers for a user decision — do not weaken a gate); app typecheck. Verify the strength end-to-end claim by reading (not editing) the chain TopBarSegmented → setRotoBackgroundMetadata — the segmented presets stay as-is, strength only feeds the tile key/tooth depth. Write 260925-iy6-SUMMARY.md (English, status automated-ready) with: what landed (tile builder, shared routine, single seam, two exports), the RED evidence from Task 1, the GCO recipe chosen (multiply valley toward paper tint + lighten peak + destination-in alpha restore + copy writeback — and WHY the restore exists: blend-mode draws paint into transparent regions, D-02 forbids that leak), the tile cache key and BOTH cache bounds (12-entry cap + 64MB byte budget, oldest-first eviction), and these native UAT rows for the user (live/disk evidence — vitest cannot judge rendering):
  (1) HOLES: with a paper selected and a FULL-OPACITY baked stroke, the paint shows the paper's tooth through it — valleys darken toward the paper tone (not gray/black), fibers lift; compare against grain-off;
  (2) LIVE RE-TEXTURE: swap the paper on an already-baked layer → texture changes immediately, no repaint/re-bake; same for grain scale (density) and strength presets (depth: None/Soft/Med/Hard visibly differ, None = flat);
  (3) EXPORT PARITY: export the same frame and compare against a canvas capture — tooth identical (shared routine, one seam);
  (4) REAL-TIME: playback stays smooth with the pass active (no stutter — the pass runs per rebuilt frame, GPU draws only);
  (5) ONE SHEET: stack 2+ layers over the same area → holes are NOT filled by the upper layer (grain modulates the mixed paint once);
  (6) GRAIN-OFF: paperGrain '' round-trip (CR-01) and strength None → paint completely untextured, fond unchanged;
  (7) FOND INTACT: paper background looks exactly as before (no double-texture over the fond region; transparent tracks still show through between each other — v1 law); check the Studio program monitor (includeFond=false branch) for the CSS-blend paper beneath — flag if it double-textures (open question 2);
  (8) LAWS: stroke body opacity/footprint/thickness unchanged vs before this quick (PIN 0 / 260924-stb / 260925-b7c — if any row regresses, report, do not tune).
  Also note in the SUMMARY: strength control intentionally kept as segmented presets (reconciliation — the NumericStepper contract belongs to grain scale), parity UAT = row 3, and status is automated-ready only — no done claim before the user's native UAT. Leave the SUMMARY uncommitted (orchestrator owns the docs commit — precedent: 260925-dso).
  </action>
  <verify>
  <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && ./node_modules/.bin/vitest run; a=$?; cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run; p=$?; npm run check; c=$?; cd /Users/lmarques/Dev/efx-motion-editor/app && npm run typecheck; t=$?; test $a -eq 0 && test $p -eq 0 && test $c -eq 0 && test $t -eq 0</automated>
  </verify>
  <done>Full app suite, full package suite, package tsc, and app typecheck all at base results (pre-existing failures documented); SUMMARY exists with status automated-ready, RED/GREEN evidence, the GCO-recipe rationale, and the 8 native UAT rows; guardrail diff still empty; SUMMARY uncommitted; no done claim before native UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local Canvas 2D rendering of local static paper textures — no new external input, no network, no auth, no parsing of untrusted data; the pass reads only existing app-owned texture assets and metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-iy6-01 | Denial of Service | per-frame pixel loop in the display frame path would stall playback | high | mitigate | Design forces build-time-only pixel work behind the tile cache; frameGuard pins assert zero pixel-array canvas APIs in physicPaintStore.ts (control) and in the applyPaperPass region; apply = ≤5 GPU drawImage/GCO ops per rebuilt frame; if a loop were ever the only viable path the plan STOPs and escalates to a phase/GPU solution (hard guardrail) |
| T-iy6-02 | Tampering | paint alpha / PIN 0 deposit laws (260924-m7w) and straight-alpha law D-02 | high | mitigate | The pass never touches depositAlpha/strokeOpacity/coverage — package sim/brush files are diff-guarded empty; destination-in alpha restore keeps the raster alpha byte-exact; 'destination-out' is a locked rejection (no alpha-punch) and is unit-pinned as absent from the GCO sequence |
| T-iy6-03 | Tampering | dual-pipeline drift (a second pass implementation in export) — the exact failure 9a deleted | medium | mitigate | Single applyPaperPass call site in _resolveFlattenedFrame, unit-pinned (exactly one occurrence, ordered between compositeFrame and the fond draw); exportRenderer/previewRenderer pinned to contain no pass implementation; export reaches the same record via PreviewRenderer → getFlattenedFrame |
| T-iy6-04 | Repudiation | silently weakening prior law gates (PIN 0, thickness, Spread, conditioning) to make the battery pass | medium | mitigate | Task 3 decision tree: any law-gate failure → STOP with a measured gate-by-gate report and no further commits; guardrail diff proves sim/brush files untouched; gates are never edited |
| T-iy6-05 | Elevation of Privilege | package installs during execution | high | mitigate | Zero installs in scope (no new dependencies — Canvas 2D + existing machinery only); Task 2/3 diff guardrails prove package.json / app/package.json / pnpm-lock.yaml unchanged |
| T-iy6-06 | Information Disclosure | none — local vitest runs, local textures, no network, no user data | low | accept | External I/O out of scope for this quick |
| T-iy6-07 | Denial of Service | tile cache footprint — an entry-count cap alone still allows ~200MB at HD in a WKWebView with a documented OOM history | medium | mitigate | Dual cache bounds: 12-entry cap AND 64MB byte budget (width×height×8 per entry) with oldest-first eviction that never evicts the just-inserted entry; unit-pinned by P2(f) byte-budget eviction row |
</threat_model>

<verification>
- RED gate: Task 1's two vitest files fail at base on the four RED legs (missing module, missing call site/ordering, missing package export, missing raw-image getter) while every CONTROL leg (store pixel-API absence, export/preview chain, base-green orderings) passes; zero production diffs.
- GREEN gate: same command exits 0 after Task 2; app typecheck clean; package tsc clean; full app + package suites at base results.
- Law gate: full package suite law gates (PIN 0, thickness, Spread, conditioning) at base results — or the plan STOPped with a measured report and no gate edits.
- Scope gate: `git diff --name-only` guardrail list empty for sim/brush/types/UI/manifests files; no installs, no push, no ROADMAP/PLAN docs commit (orchestrator owns it), no format change.
</verification>

<success_criteria>
Both pins RED at base then GREEN; tile encodes signed height at build time (paper-tinted valleys, tint-scaled peaks, strength-gated); cache keyed on paper+WxH+scale+strength with grain-off/unresolved early exits; ONE ordered call site in _resolveFlattenedFrame covering both fond branches; export structurally shares the record (no second implementation); frame path proven loop-free by source guards; app+package suites and typechecks at base; SUMMARY status automated-ready with the 8 native UAT rows pending the user's live check (no done claim before native UAT).
</success_criteria>

<output>
Create `.planning/quick/260925-iy6-dynamic-post-bake-paper-pass-holes-relie/260925-iy6-SUMMARY.md` when done (uncommitted per quick convention; orchestrator owns the docs commit)
</output>
