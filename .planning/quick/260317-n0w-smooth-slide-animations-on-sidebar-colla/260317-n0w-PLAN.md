---
phase: quick
plan: 260317-n0w
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/sidebar/CollapsibleSection.tsx
  - Application/src/components/sidebar/SidebarProperties.tsx
  - Application/src/components/sequence/SequenceList.tsx
autonomous: true
requirements: [quick-260317-n0w]
must_haves:
  truths:
    - "Collapsing/expanding sidebar sections (SEQUENCES, LAYERS, PROPERTIES) animates smoothly with a slide effect (~150ms)"
    - "A 'Key' label appears before the keyframe nav buttons in the Properties panel"
    - "Sequence titles have the same font weight as layer titles (500, not 600)"
  artifacts:
    - path: "Application/src/components/sidebar/CollapsibleSection.tsx"
      provides: "Animated collapse/expand via max-height transition"
    - path: "Application/src/components/sidebar/SidebarProperties.tsx"
      provides: "Key label before KeyframeNavBar"
    - path: "Application/src/components/sequence/SequenceList.tsx"
      provides: "Sequence title with fontWeight 500"
  key_links:
    - from: "CollapsibleSection.tsx"
      to: "LeftPanel.tsx"
      via: "children rendered inside overflow-hidden transition container"
      pattern: "transition-\\[max-height\\]"
---

<objective>
Three small UI polish items: (1) add smooth slide animation to sidebar CollapsibleSection collapse/expand, (2) add "Key" label before the keyframe controller in Properties, (3) unbold sequence titles to match layer titles.

Purpose: Visual polish and consistency across sidebar UI elements.
Output: Updated CollapsibleSection.tsx, SidebarProperties.tsx, SequenceList.tsx
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/sidebar/CollapsibleSection.tsx
@Application/src/components/sidebar/SidebarProperties.tsx
@Application/src/components/sequence/SequenceList.tsx
@Application/src/components/layer/LayerList.tsx (reference: layer title fontWeight is 500 on line 155)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add smooth slide animation to CollapsibleSection</name>
  <files>Application/src/components/sidebar/CollapsibleSection.tsx</files>
  <action>
    Replace the hard conditional render `{!collapsed.value && children}` with a CSS max-height transition approach, matching the pattern already used in SequenceList.tsx line 324:

    1. Always render children, but wrap them in a container div with:
       - `overflow-hidden`
       - `transition-[max-height] duration-150 ease-out`
       - When expanded: `max-height: 9999px` (large enough to never clip)
       - When collapsed: `max-height: 0px`

    The pattern is: `<div class="overflow-hidden transition-[max-height] duration-150 ease-out" style={{ maxHeight: collapsed.value ? '0px' : '9999px' }}>{children}</div>`

    This is the exact same approach used for the key photo strip in SequenceList.tsx. The 9999px value is fine because the content never exceeds that and CSS transitions from 0 to a large max-height work smoothly for slide-down effects.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && npx tsc --noEmit --project Application/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>CollapsibleSection children animate with a smooth 150ms slide when toggling collapsed state. The chevron rotation transition (already 150ms ease) stays synchronized.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Key" label and unbold sequence titles</name>
  <files>Application/src/components/sidebar/SidebarProperties.tsx, Application/src/components/sequence/SequenceList.tsx</files>
  <action>
    Two small changes:

    **SidebarProperties.tsx** -- Add a "Key" label before the KeyframeNavBar, matching the "Blur" label style on line 92.

    In the JSX block starting at line 89 (`<div class="flex items-center gap-3">`), add a span before KeyframeNavBar:
    ```
    <span class="shrink-0" style={{ fontSize: '12px', fontWeight: 500, color: 'var(--sidebar-text-secondary)' }}>Key</span>
    <KeyframeNavBar layer={layer} />
    ```
    This mirrors the exact style used for the "Blur" label on line 92.

    **SequenceList.tsx** -- Change sequence title fontWeight from 600 to 500 in two places:
    - Line 235 (edit input style): `fontWeight: 600` -> `fontWeight: 500`
    - Line 250 (display span style): `fontWeight: 600` -> `fontWeight: 500`

    This matches the layer title fontWeight of 500 in LayerList.tsx line 155.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && npx tsc --noEmit --project Application/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>"Key" label visible before keyframe nav buttons in Properties panel. Sequence titles render at fontWeight 500, visually matching layer title weight.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual: Open app, collapse/expand SEQUENCES, LAYERS, PROPERTIES sections -- each animates with a smooth 150ms slide
- Visual: Select a non-base layer, check Properties panel -- "Key" label appears before the keyframe nav buttons
- Visual: Check sequence titles in SEQUENCES panel -- same visual weight as layer names
</verification>

<success_criteria>
All three sidebar polish items implemented: animated collapse, Key label, unbold sequence titles. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260317-n0w-smooth-slide-animations-on-sidebar-colla/260317-n0w-SUMMARY.md`
</output>
