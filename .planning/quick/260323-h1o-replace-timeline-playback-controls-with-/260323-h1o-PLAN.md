---
phase: quick
plan: 260323-h1o
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/layout/TimelinePanel.tsx
autonomous: true
must_haves:
  truths:
    - "Timeline header shows 7 transport buttons in correct order"
    - "Skip Back seeks to frame 0"
    - "Step Back navigates to previous sequence boundary"
    - "Rewind goes to previous frame"
    - "Play toggles playback"
    - "Fast Forward goes to next frame"
    - "Step Forward navigates to next sequence boundary"
    - "Skip Forward seeks to last frame"
  artifacts:
    - path: "Application/src/components/layout/TimelinePanel.tsx"
      provides: "7-button transport bar replacing old 4-button controls"
  key_links:
    - from: "TimelinePanel.tsx"
      to: "sequenceNav.ts"
      via: "findPrevSequenceStart, findNextSequenceStart"
      pattern: "findPrevSequenceStart|findNextSequenceStart"
    - from: "TimelinePanel.tsx"
      to: "playbackEngine.ts"
      via: "seekToFrame, stepForward, stepBackward, toggle"
      pattern: "playbackEngine\\.(seekToFrame|stepForward|stepBackward|toggle)"
---

<objective>
Replace the existing 4-button playback controls (seek-to-start, play/pause, step-back, step-forward) in the timeline header with a full 7-button transport bar.

Purpose: Give the user professional NLE-style transport controls for precise timeline navigation including sequence boundary jumps.
Output: Updated TimelinePanel.tsx with 7 transport buttons.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/layout/TimelinePanel.tsx
@Application/src/lib/playbackEngine.ts
@Application/src/lib/sequenceNav.ts
@Application/src/lib/frameMap.ts

<interfaces>
<!-- Existing APIs the executor needs -->

From Application/src/lib/playbackEngine.ts:
```typescript
export class PlaybackEngine {
  seekToFrame(frame: number): void;
  stepForward(): void;
  stepBackward(): void;
  toggle(): void;
}
export const playbackEngine: PlaybackEngine;
```

From Application/src/lib/sequenceNav.ts:
```typescript
export function findPrevSequenceStart(layouts: TrackLayout[], currentFrame: number): number | null;
export function findNextSequenceStart(layouts: TrackLayout[], currentFrame: number): number | null;
```

From Application/src/lib/frameMap.ts:
```typescript
export const totalFrames: ReadonlySignal<number>;
export const trackLayouts: ReadonlySignal<TrackLayout[]>;
```

From Application/src/stores/timelineStore.ts:
```typescript
timelineStore.currentFrame: Signal<number>;
timelineStore.isPlaying: Signal<boolean>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace playback controls with 7-button transport bar</name>
  <files>Application/src/components/layout/TimelinePanel.tsx</files>
  <action>
Replace the current 4 playback buttons (lines 41-88 in TimelinePanel.tsx: seek-to-start via Rewind icon, Play/Pause, SkipBack for step backward, SkipForward for step forward, and the Loop toggle) with a 7-button transport bar. Keep the Loop toggle button after the transport bar (it is not part of the 7 buttons).

**New imports needed:**
- Add `ChevronFirst`, `ChevronLast`, `ChevronsLeft`, `ChevronsRight` from `lucide-preact` (keep existing `Play`, `Pause`, `SkipBack`, `SkipForward`; REMOVE `Rewind` import since it is repurposed)
- Add `findPrevSequenceStart`, `findNextSequenceStart` from `../../lib/sequenceNav`
- Add `totalFrames`, `trackLayouts` from `../../lib/frameMap`

**Button layout (left to right), all using the same button styling pattern already in use:**

1. **Skip Back** — icon: `SkipBack` (size 14) — action: `playbackEngine.seekToFrame(0)` — title: "Skip to start"
2. **Step Back** — icon: `ChevronFirst` (size 14) — action: call `findPrevSequenceStart(trackLayouts.value, timelineStore.currentFrame.value)`, if result is not null call `playbackEngine.seekToFrame(result)` — title: "Previous sequence"
3. **Rewind** — icon: `ChevronsLeft` (size 14) — action: `playbackEngine.stepBackward()` — title: "Previous frame"
4. **Play/Pause** — icon: `Play`/`Pause` (size 14, conditional on isPlaying) — action: `playbackEngine.toggle()` — title: dynamic "Pause (Space)" / "Play (Space)" — keep the accent-colored styling for this button
5. **Fast Forward** — icon: `ChevronsRight` (size 14) — action: `playbackEngine.stepForward()` — title: "Next frame"
6. **Step Forward** — icon: `ChevronLast` (size 14) — action: call `findNextSequenceStart(trackLayouts.value, timelineStore.currentFrame.value)`, if result is not null call `playbackEngine.seekToFrame(result)` — title: "Next sequence"
7. **Skip Forward** — icon: `SkipForward` (size 14) — action: `playbackEngine.seekToFrame(totalFrames.value - 1)` — title: "Skip to end"

**Styling:** All 7 buttons use the same non-accent button class already used by the existing step buttons:
`"rounded bg-[var(--color-bg-input)] px-2 py-[5px] text-[var(--color-text-secondary)] hover:bg-[var(--color-bg-hover-item)] hover:text-white cursor-pointer transition-colors"`

Exception: the Play/Pause button keeps the accent styling:
`"rounded bg-[var(--color-accent)] px-2 py-[5px] text-white hover:brightness-125 cursor-pointer transition-colors"`

After the 7 transport buttons, keep the existing Loop toggle button and the vertical divider before the timecode display. The rest of the header (timecode, zoom controls, add-layer menus) remains unchanged.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Timeline header displays 7 transport buttons in the specified order with correct icons, actions, and titles. TypeScript compiles without errors. Loop toggle remains after the transport bar.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Visual inspection: 7 buttons visible in timeline header in correct left-to-right order
- Skip Back seeks to frame 0
- Step Back jumps to previous sequence boundary
- Rewind goes to previous frame
- Play toggles playback
- Fast Forward goes to next frame
- Step Forward jumps to next sequence boundary
- Skip Forward seeks to last frame
- Loop toggle still works
</verification>

<success_criteria>
Timeline header has exactly 7 transport buttons plus the loop toggle, all functional with correct lucide icons. No TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-h1o-replace-timeline-playback-controls-with-/260323-h1o-SUMMARY.md`
</output>
