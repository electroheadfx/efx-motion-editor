---
phase: quick-260921-e21
plan: 260921-e21
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts
  - app/src/stores/efxPaintChildParentSync.scratch.test.ts
  - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintFlush.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts
  - .planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-RED-EVIDENCE.json
  - .planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-SUMMARY.md
autonomous: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A reference-image selection made in the Studio reaches the realm that owns the save path BEFORE the Studio closes, so the very next Studio launch carries it — the reopen shows the same active reference. (Quick 260921-e21, surface 1.)"
    - "A background image clip placed in the Studio (its keyframe/time position) reaches that same realm, so the reopen shows the clip at the same frame with the same source refs. (Surface 2.)"
    - "A track added with the Studio's (+), together with the content painted on it, reaches that same realm, so the reopen shows the track and its content. (Surface 3.)"
    - "All three survive app quit/relaunch through the .mce package: the layer sub-file `layers/<layerId>.json` carries them and a reopen reproduces them."
    - "The fix extends the EXISTING child→main document-sync pair (or the launch/hydration path it feeds). No parallel transport, no second channel for the same need (guardrail 3)."
    - "No manifest schema shape change beyond what the bindings require; the 52.2 reference-only law holds (guardrail 4)."
    - "The 260921-bjm gallery import path still persists (regression row)."
    - "Quick 260921-c7x is not re-opened: the canonical loop-clip revision authority and the terminal physical-edit mismatch release behave exactly as shipped (guardrail 5)."
    - "Native UAT is left explicitly pending in the SUMMARY — never claimed (guardrail 6)."
  artifacts:
    - .planning/quick/260921-e21-.../260921-e21-RED-EVIDENCE.json — the per-surface (a)/(b)/(c) verdicts with raw file:line evidence, the probe outputs, the halt decision, and the verbatim RED run for each pin
    - .planning/quick/260921-e21-.../260921-e21-SUMMARY.md — the verdicts, the before/after of the diagnosed seam, the raw RED→GREEN output, the scope + guardrail audit, the gates, and the five native UAT rows marked pending
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts — the three end-to-end reopen pins driven through the REAL functions on both sides of the event pair
    - app/src/stores/efxPaintChildParentSync.scratch.test.ts — the store-seam lock extended from tracks to the three surfaces (child push → parent register → parent save → package layer file)
    - app/src/lib/physicPaintBridge.ts — the parent-side apply of the child document (the diagnosed fix lives here when the verdict names the apply link)
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts — the child-side serialize/encode of the pushed document (the fix lives here when the verdict names the transport link)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — the child push wiring (dirty flag, quiet scheduler, close flush); touched only when the verdict names the wiring link
  key_links:
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx:3879-3967 — the CHILD PUSH. pushLiveProjection runs serializeRuntimeIntoDocument(layerId) through documentSyncPushGuard.evaluate, then sendEfxPaintDocumentSync(document, mode, backgroundSources) (:3953). The guard (app/src/components/physic-paint/bridge/documentSyncPushGuard.ts:38-50) latches lastPushedFingerprint BEFORE the send resolves, so a send that fails still latches the fingerprint for that content."
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx:3972-4047 — the DIRTY/QUIET SCHEDULER. documentSyncDirty is set true on every efxPaintVersion bump (:3974-3977). Both flush paths assign documentSyncDirty.value = false BEFORE the mode check (non-gesture :4004-4009, gesture :4035-4040), so when bridgeModeRef.current is neither 'Tauri' nor 'Browser fallback' the pending change is cleared and NEVER pushed — a silent drop of the exact shape this defect reports."
    - "app/src/components/physic-paint/PhysicsPaintStudio.tsx:4056-4090 + app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts:20-51 — the CLOSE FLUSH. usePhysicsPaintCloseFlush gate at PhysicsPaintStudio.tsx:1911 is `hasPending` → pendingDocumentSyncRef.current() (:3980 = documentSyncDirty.peek()); flushDocumentSyncRef (:3981-3989) pushes only while dirty. workflowMode is the constant 'roto' (:789), so the close gate is NOT mode-gated — H-close is testable by reading, and a close-time drop must come from the dirty flag already being false."
    - "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts:323-382 — the CHILD TRANSPORT. projectEfxPaintDocumentForSync (:271-307) rebuilds `{ ...document, tracks }` — the spread preserves background.clips / photoReference / activeTrackId. projectRecordsForSync (:239-263) returns NULL when a record cannot be projected; the caller then falls back to the RAW records (`realKeyRecords ?? roto.realKeyRecords`, :297) with in-memory bytes, which toTransportPayload must encode. A throw anywhere here fails the WHOLE document push (caught at PhysicsPaintStudio.tsx:3957)."
    - "app/src/lib/physicPaintBridge.ts:3253-3311 — the PARENT APPLY. installPhysicPaintEfxPaintDocumentListener → parseEfxPaintDocument(fromTransportPayload(incoming.document ?? payload)) (:3277) → applyDocumentSyncFrameMedia (:3281) → the idempotency guard (:3282-3283) → registerEfxPaintDocument (:3284) → the per-track silent runtime mirror (:3296-3307). ANY throw is caught at :3308 and logged as '[physicPaintBridge] Rejected EFX Paint document sync:', leaving ZERO mutation — the uniform whole-document drop shape that would lose all three surfaces at once."
    - "app/src/lib/physicPaintBridge.ts:3433-3543 — the LAUNCH CARRIER (the reopen half). createPhysicPaintLaunchContext builds the carrier as `{ ...baseDocument, tracks }` from the PARENT realm's getEfxPaintDocument(layerId) (:3498-3515), then parseCanonicalPhysicsPaintLaunchValue fails CLOSED — null, then the throw at :3541 — if parseEfxPaintDocument(value.document) rejects or if startFrame !== the active track's cursorAppFrame (app/src/components/physic-paint/bridge/physicsPaintLaunchContext.ts:57-97)."
    - "app/src/stores/efxPaintStore.ts:67-70 / :1708-1734 — _notifyChange bumps efxPaintVersion AND calls _markProjectDirty (the CHILD realm's inert projectStore); serializeRuntimeIntoDocument re-projects the runtime into the document and bumps documentRevision only on a content change."
    - "app/src/stores/efxPaintStore.ts:183-206 (addTrack), :692-723 (addBackgroundClip), :1129-1176 (setPhotoReferenceSource) — verified live at plan time: ALL THREE write _documents and call _notifyChange(), so each one DOES bump efxPaintVersion in the child. The child side is not silent; the loss is downstream of the dirty flag."
    - "app/src/efx-paint/document/efxPaintDocumentRevision.ts:119-151 — buildEfxPaintDocumentRevision covers tracks, background.clips, photoReference and activeTrackId. The parent's idempotency guard therefore CANNOT skip a real change to any of the three surfaces."
    - "app/src/stores/efxPaintChildParentSync.scratch.test.ts:229-309 — RAN LIVE AT PLAN TIME (pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintChildParentSync.scratch.test.ts, 2026-09-21: 3 passed). It proves the STORE-LEVEL seam is already correct: child push document → parent registers → parent save writes layers/<layerId>.json with BOTH tracks' keys. The store/save seam is therefore EXONERATED as the defect; the executor reproduces this before bisecting further."
    - "app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts:114-198 — the LAUNCH HYDRATION (verdict (c) territory): the sessionStorage checkpoint is consumed only when its operationId matches the NEW launch (:124-125), the library is loaded BEFORE registerDocument (:145-148), and both source hydrations run with the launch-library fallback (:160-170). The re-registered document is hydration.context.document — whatever the carrier delivered."
    - "The existing event pairs in app/src/lib/physicPaintBridge.ts (physic-paint:efx-paint-document at :97, plus :74, :102-111) are the ONLY sanctioned transport for this plan. Extending one of them is the fix shape; adding a new pair for the same need is the guardrail-3 violation."
---

<objective>
Make Studio-origin document state persist: the active reference image, the background image keyframes, and the tracks added with (+).

Diagnosis performed at PLAN TIME (the executor reproduces it before touching production code — Task 1):

- The three surfaces are all CHILD-REALM document mutations (app/src/components/physic-paint/PhysicsPaintStudio.tsx imports addTrack, addBackgroundClip, setBackgroundClipSource, setPhotoReferenceSource, serializeRuntimeIntoDocument). Each is verified to write the child document and bump efxPaintVersion (efxPaintStore.ts:183-206, :692-723, :1129-1176), so the child is not silent: the loss happens between the child's dirty flag and the realm that owns the save path.
- A child→main document sync ALREADY exists (physic-paint:efx-paint-document; sendEfxPaintDocumentSync → installPhysicPaintEfxPaintDocumentListener → registerEfxPaintDocument) and the launch carrier is built from the parent's document (createPhysicPaintLaunchContext). So the fix is NOT a new transport — it is a broken link in an existing chain, and the diagnosis must name which link.
- The store/save seam is already GREEN at plan base (efxPaintChildParentSync.scratch.test.ts, 3 passed, run live 2026-09-21): a child-pushed document registered by the parent and then saved does keep a child-added track in layers/<layerId>.json. That exonerates the save path and points the bisection at the transport/apply/launch chain and the Studio's push wiring.
- The 52.2 reference-only package law is expected to be exonerated again (the document already carries background.clips / photoReference / tracks; only the transfer is in question).

Purpose: find the one broken link with live evidence, then fix that link by extending the existing pair — never by adding a channel, never by touching the manifest shape, never by touching the c7x revision authority.

Output: the per-surface (a)/(b)/(c) verdicts with evidence, three RED reopen pins driven through the real functions on both sides of the bridge, the fix at the diagnosed link, GREEN locks at the store and transport seams, and the five native UAT rows left pending.

GUARDRAIL 1 IS A HARD GATE: if any surface's root is STRUCTURAL — (S1) it needs a manifest/package format change or breaks the 52.2 reference-only law, (S2) it needs a new independent transport rather than an extension of an existing pair, (S3) it needs the physical-edit revision/identity authority or the loop-clip lifecycle resolver changed (260921-c7x territory), or (S4) it needs a Rust command or capability change — the executor HALTS after Task 1 and reports. The halt is enforced by Task 1's automated verify, not by judgement.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts
@app/src/lib/physicPaintBridge.ts
@app/src/stores/efxPaintStore.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts

Idiom oracle (shipped, UAT-approved 2026-09-21) — read both before diagnosing:
@.planning/quick/260921-bjm-imported-gallery-background-images-are-l/260921-bjm-PLAN.md
@.planning/quick/260921-bjm-imported-gallery-background-images-are-l/260921-bjm-SUMMARY.md

Do NOT re-open (settled, guardrail 5):
@.planning/quick/260921-c7x-physical-edit-cluster-result-mismatch-ti/260921-c7x-SUMMARY.md

Plan base: `6ec09422` (record `git rev-parse HEAD` at start — it is the scope-gate base; the working tree is clean).

Verified code facts (live reads at plan time, 2026-09-21) are in `must_haves.key_links`. Every line number was read, not recalled.

Rules that bind this plan:
- Commands (project rule, inherited verbatim): `pnpm --filter efx-motion-editor exec vitest run <paths>` — vitest run only, NEVER watch; types via `pnpm --filter efx-motion-editor exec tsc --noEmit`; full suite `pnpm --filter efx-motion-editor exec vitest run`. The suite was 4059 passed / 1 skipped / 101 todo / 0 failed at the c7x base — attribute any new failure by name, never absorb it.
- Do NOT launch the app or the dev server (the user runs it); native UAT is owed by the user, never claimed here.
- efx-preact-reactivity applies: no useState, signals only, no signal write in a render body, idempotent setters, identity-stable effect deps. efx-async-orchestration applies to any changed request/result or timer path: bounded turns, every timeout must terminate, no unbounded retry.
- Guardrails (verbatim intent): extend the existing bridge idiom (no parallel transport); no manifest schema shape change beyond what the bindings require (52.2 reference-only law holds); do not re-open 260921-c7x; diagnose with live evidence on a real project before any code change; RED first with the three pins observed failing.
- The executor writes the two `.planning/quick/260921-e21-.../` artifacts but does NOT commit them — the orchestrator commits docs in its final step. Commits are code (+ the RED evidence artifact when the halt path fires) only.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Diagnose the three surfaces with live evidence, pin each one RED — HALT if a root is structural</name>
  <files>app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts, .planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-RED-EVIDENCE.json</files>
  <behavior>
    NO PRODUCTION FILE IS TOUCHED IN THIS TASK. Evidence first, then pins, then the gate.

    STEP 1 — bisect the chain in this order and record raw output for each probe. The chain is: child store mutation → efxPaintVersion → documentSyncDirty → pushLiveProjection → sendEfxPaintDocumentSync (projectEfxPaintDocumentForSync → toTransportPayload) → [event] → installPhysicPaintEfxPaintDocumentListener (fromTransportPayload → parseEfxPaintDocument → frame media → revision guard → registerEfxPaintDocument) → createPhysicPaintLaunchContext carrier → child hydration.

    * PROBE A (child side, read + run): confirm all three mutations bump the version and set the dirty flag (efxPaintStore.ts:183-206, :692-723, :1129-1176 each call _notifyChange) — record the read; then confirm the two flush paths clear documentSyncDirty BEFORE the mode check (PhysicsPaintStudio.tsx:4004-4009 and :4035-4040) and that documentSyncPushGuard latches lastPushedFingerprint before the send resolves (documentSyncPushGuard.ts:38-50). Verdict for this link: is a change ever cleared without a push?
    * PROBE B (child transport, run): drive projectEfxPaintDocumentForSync through sendEfxPaintDocumentSync with a document carrying a background clip, a photo reference and an added track, and record the emitted payload shape — does the emit resolve, and does the payload carry all three (the spread at :302-306 preserves them)? What does projectRecordsForSync (:239-263) do when a record cannot be projected — does the fallback to RAW records (:297) still encode?
    * PROBE C (parent apply, run): feed the captured payload into the REAL parent listener path (parseEfxPaintDocument(fromTransportPayload(...)) → the guard → registerEfxPaintDocument) and read back getEfxPaintDocument(layerId). Does it apply, or is it rejected (the console.warn at physicPaintBridge.ts:3308)? Record the outcome AND the guard comparison (buildEfxPaintDocumentRevision(current) vs …(incoming)).
    * PROBE D (reopen carrier, run): call createPhysicPaintLaunchContext on the parent state after the apply and record whether the carrier carries each surface, plus whether parseCanonicalPhysicsPaintLaunchValue accepts it (:3540).
    * PROBE E (store seam, control — reproduce the plan-time observation): run src/stores/efxPaintChildParentSync.scratch.test.ts and record the result. It was 3 passed at plan base; it proves the store/save seam is correct for a child-added track, which is why the defect must be upstream of it.

    Verdict per surface — reference selection, background keyframes, (+) tracks — in the bjm (a)/(b)/(c) form, each with its file:line or probe evidence:
    (a) the mutation stays child-realm only (never pushed);
    (b) a bridge exists but the parent realm ignores or rejects it (the push is emitted and dropped/rejected);
    (c) it persists but the Studio launch drops it (the carrier or the hydration loses it).
    Record for EVERY surface whether its root is STRUCTURAL per the four S1..S4 conditions in the objective. If any root IS structural: STOP — write the verdicts, the probes and the halt reason into the RED-EVIDENCE.json, write a SUMMARY skeleton at .planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-SUMMARY.md whose first section is the halt report with the structural condition, commit the evidence artifact, and return without writing pins or production code.

    STEP 2 — the three RED pins, written only after the verdicts. In physicsPaintBridgeTransport.test.ts add a sibling describe (the file already hoists the @tauri-apps/api/event mock; follow the 52.2-10 legs at :403 for the doc-sync idiom and add a scoped afterEach mockClear so the hoisted spy cannot cross describe boundaries — the bjm deviation-2 lesson). Each pin drives the REAL functions on both sides of the event pair, i.e. the in-vitest faithful form of close Studio -> reopen Studio:
    * PIN 1 (reference selection): parent registers a document for the layer; the child applies setPhotoReferenceSource(layerId, [<refId>]); the child serializes and sends; the captured payload is fed through the parent apply; then createPhysicPaintLaunchContext produces a carrier whose photoReference.sourceFrameRefs equals the selection.
    * PIN 2 (background keyframes): same chain with addBackgroundClip(layerId, { startFrame, sourceFrameRefs, repeat }) — the carrier's background.clips must carry the same startFrame and source refs.
    * PIN 3 ((+) track with content): same chain with addTrack(layerId) plus content on the new track — the carrier's tracks must contain the new track id with that content.
    Each pin asserts the end-to-end carrier, not an intermediate. If a pin is GREEN at base while its surface is reported lost, that surface's break is at the wiring link the verdict named (PROBE A / launch hydration) — replace that pin with a leg over the named link (a source-shape contract in PhysicsPaintStudio.test.ts next to the existing doc-sync assertions at :217-228 and the close-sequence contracts at :2080-2092, or a hydration leg in usePhysicsPaintLaunchIntegration.test.ts), so that all three pins are RED at base for a diagnosed reason. Pin the diagnosed mechanism, never a guess: every pin's failure message must name the link that broke.

    STEP 3 — the gate. Capture the verbatim RED run for all three pins into 260921-e21-RED-EVIDENCE.json (format: bjm's 260921-bjm-RED-EVIDENCE.json), alongside the verdicts, the five probe outputs, the structural booleans and the halt decision. Do not rewrite a pin to accommodate an unexpected pass; a pass is a verdict datum, not a failure.

    Keep the pins free of literals that a later acceptance gate negative-greps for (comment-text discipline): assert presence of the contract you want, not the absence of a string you also wrote.
  </behavior>
  <action>
    Read the key_links, then run the probes in order, capture every raw output, and write the verdicts. Do not touch a production file. Do not write a pin before the verdicts exist — a pin written from a guess cannot be RED for a diagnosed reason. After the verdicts: write the three RED pins, run them, and capture the verbatim failing output. Then run the gate command below.

    If the gate exits non-zero because a verdict is structural, that IS the halt: finish the halt report, commit only the RED-evidence artifact, and return without pins or production code. If it exits non-zero because a pin was not observed RED, the pin does not yet pin the diagnosed mechanism — go back to STEP 2 (a pin that passes at base is a verdict datum pointing at a different link, not a licence to proceed).

    Commit (normal path): `test(260921-e21): RED — the three Studio-origin surfaces pinned through the real child-to-parent document chain`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor &amp;&amp; pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts 2>&1 | tail -60 ; node -e "const fs=require('fs');const p='/Users/lmarques/Dev/efx-motion-editor/.planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-RED-EVIDENCE.json';const e=JSON.parse(fs.readFileSync(p,'utf8'));const v=e.verdicts||[];const pins=e.pins||[];if(v.length!==3){console.error('need 3 verdicts');process.exit(1)}if(v.some(x=>x.structural!==false)){console.error('HALT: structural root recorded — stop after Task 1 and report');process.exit(2)}if(v.some(x=>!['a','b','c'].includes(x.verdict))){console.error('each verdict must be a, b or c');process.exit(1)}if(pins.length!==3||pins.some(x=>x.redObserved!==true)){console.error('all three pins must be observed RED');process.exit(1)}console.log('verdicts and RED pins recorded');"</automated>
  </verify>
  <done>The five probes and the three (a)/(b)/(c) verdicts are recorded in the RED-evidence artifact with raw output; the halt decision is explicit; the three pins over the real child-to-parent chain are observed RED with their verbatim output captured; no production file is modified by this task; the gate exits 0 on the normal path and 2 on a structural root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the diagnosed link by extending the existing pair (never a parallel transport)</name>
  <files>app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintFlush.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts, app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts</files>
  <behavior>
    CONDITIONAL SCOPE. The verdicts from Task 1 name the link; touch ONLY the files that link requires, and only the ones it requires. This list is the complete authorized set — any file outside it is out of scope and must be reported as a deviation with its justification, not silently edited. Nothing in app/src/efx-paint/document/**, nothing in the package/manifest writers, nothing in src-tauri/**, nothing in the loop-clip model or the physical-edit coordinator (guardrails 4 and 5).

    The fix shapes, one per verdict link — pick the one the verdicts name, and if two links are broken, fix both in this task:
    * transport link (PROBE B): the child's serialize/encode must deliver a document the parent can parse. If projectRecordsForSync returns null and the raw-record fallback ships an unencodable shape, keep the failure LOCAL to the record that failed (the reference-shaped record must survive; the parent must still receive tracks, clips and photoReference) instead of failing the whole push — and make the failure observable rather than silent.
    * apply link (PROBE C): the parent apply must accept what the child legitimately sends. Any widening of the accepted shape must be paired with the matching guard/bound at the boundary (types/physicPaint.ts is authorized for that pairing alone, and only if the verdict proves it is required — a widened parse without a guard is a T-260921-e21-01 regression).
    * wiring link (PROBE A): the pending change must never be cleared without a push — clear documentSyncDirty only AFTER a push is actually attempted and the mode is confirmed, and release the push guard's fingerprint latch when a send fails so the same content can be retried. Keep the fix inside the existing signal/effect discipline (no new signal, no render-body write, no useState) and keep every timer terminating (efx-async-orchestration).
    * launch/hydration link (PROBE D/E): the carrier or the hydration must stop dropping the surface. Prefer fixing the carrier construction over patching the hydration, since the carrier is what "the launch is the document" means.

    In every case: the transfer must remain the SAME channel (the existing child→main document sync / launch carrier). Do not add an event pair, do not add an IPC command, do not change the manifest shape.

    Then convert the three pins to GREEN without weakening them: the assertion each pin makes must remain the end-to-end reopen contract (the carrier carries the surface), and the pin's comment must name the link fixed and the verdict it came from. If the fix requires a new leg on the transport contract (e.g. the local-failure-isolation behaviour), add it.
  </behavior>
  <action>
    Apply the fix at the diagnosed link, run the pins green, then run the touched files plus `tsc --noEmit`. Keep the picker and manifest paths untouched; keep the child document sync the only carrier. Do not touch the loop-clip model, the physical-edit coordinator, or any revision/identity comparison (260921-c7x stays closed). If the fix needs a file outside the authorized set, STOP and report the deviation before editing it.

    Commit: `fix(260921-e21): the Studio-origin document reaches the save realm — reference selection, background keyframes and (+) tracks persist across Studio close`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor &amp;&amp; pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts src/stores/efxPaintChildParentSync.scratch.test.ts &amp;&amp; pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
  </verify>
  <done>The three pins pass as real end-to-end carrier assertions (no pin weakened or deleted); the touched files' suites and tsc --noEmit are green; the transfer is still the pre-existing document-sync/launch path with no new pair, no new IPC and no manifest shape change.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the store/package half, run the gates, audit scope and guardrails, write the SUMMARY (native UAT pending)</name>
  <files>app/src/stores/efxPaintChildParentSync.scratch.test.ts, .planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-SUMMARY.md</files>
  <behavior>
    No production change in this task. Any red leg here means Task 2 is incomplete — fix Task 2, never patch the test.

    * STORE-SEAM LOCKS (extend the file that already owns this seam, efxPaintChildParentSync.scratch.test.ts): its first case proves a child-pushed, parent-registered, parent-saved document keeps a child-added TRACK in the package layer sub-file. Extend the same harness to the other two surfaces, one case each: a child-pushed background clip survives into layers/<layerId>.json with its startFrame and source refs, and a child-pushed photo reference survives with its sourceFrameRefs. Where the fix changed the parent APPLY (verdict b on the apply link), route the case through that apply function rather than through a bare registerDocument, so the lock covers the link that actually broke.
    * REGRESSION ROW for bjm (guardrail: gallery imports still persist): assert the bjm import path's outcome is unchanged — the image-import pair's legs in physicsPaintBridgeTransport.test.ts (describe 'image-import bridge pair (quick-260921-bjm)') and the picker wiring assertions in PhysicsPaintStudio.test.ts must stay green untouched. If the fix touched the parent apply shared by both paths, say so explicitly in the SUMMARY and show the bjm legs green in the same run.
    * GATES, in order: the four touched test files; then `pnpm --filter efx-motion-editor exec tsc --noEmit`; then the full suite `pnpm --filter efx-motion-editor exec vitest run` (background it if the runtime allows and resume only on completion). Attribute any failure by name against the c7x-base reference (4059 passed / 1 skipped / 101 todo / 0 failed) — never absorb one.
    * SCOPE GATE: `git diff --name-only 6ec09422..HEAD` must list only this plan's files — the two `.planning/quick/260921-e21-.../` artifacts, the four test files, and the production files the verdicts named (at most the authorized set in Task 2). Any production file outside that set is a deviation and must be reported as one.
    * GUARDRAIL AUDIT by reading the diff, each recorded HELD or NOT HELD: (1) exactly one transport carries the document — no added event pair and no added IPC/Rust command; (2) no change to the manifest/package shape or to app/src/efx-paint/document/** — the 52.2 reference-only law holds; (3) no change to the loop-clip lifecycle authority, the canonical encoder, the physical-edit coordinator or any revision/identity comparison (c7x untouched — show the c7x-touched files absent from the diff); (4) no useState and no new signal in the Studio edit (efx-preact-reactivity), every timer/timeout still terminates (efx-async-orchestration); (5) the picker controller/view and its pre-flight copy are untouched.
    * SUMMARY at .planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-SUMMARY.md: the per-surface (a)/(b)/(c) verdicts with their raw evidence; the probe outputs (A..E) including the store-seam control result; the explicit structural check with its verdict ("not structural — no format change, no new transport" when that is the truth); the before/after of the diagnosed link; the raw RED output and the green runs; the lock legs' outcomes; the bjm regression row; the scope + guardrail audit; the gate table; and the FIVE native UAT rows below marked PENDING, never claimed.
  </behavior>
  <action>
    Write the lock legs, run the gates, read the diff for the audit, write the SUMMARY. Commit the code (the lock legs): `test(260921-e21): lock the three Studio-origin surfaces through the store/package seam`. The SUMMARY and the RED-evidence artifact are left uncommitted for the orchestrator's docs commit.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor &amp;&amp; pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintChildParentSync.scratch.test.ts src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.test.ts &amp;&amp; pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
  </verify>
  <done>The store/package locks cover all three surfaces and are green; the bjm regression legs are green in the same run; the full suite is green with any failure attributed by name; the scope gate lists only this plan's files; the guardrail audit is recorded with each item HELD or NOT HELD; the SUMMARY carries the verdicts with evidence, the probe outputs, the structural check, the before/after, the RED/GREEN output, the audits and the five UAT rows marked pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio child realm → main realm (document sync) | A document built in the child webview crosses the event bridge into the realm that owns the save path and the package. |
| Child document → parent document store | The parent registers the incoming document after a fail-closed parse and a revision guard; this is the only path by which child-origin structure becomes persistable. |
| Parent document → .mce package layer sub-file | The package writes layers/<layerId>.json; the 52.2 reference-only law governs what may be written there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260921-e21-01 | Tampering | `parseEfxPaintDocument` / the parent apply path | high | mitigate | Any widening of the accepted incoming shape must be paired with the boundary guard and bound in the same change; a widened parse without a guard is refused at review. The existing fail-closed parse stays the gate. |
| T-260921-e21-02 | Tampering | the parent idempotency / revision guard | high | mitigate | The guard (`buildEfxPaintDocumentRevision(current) === …(incoming)`) is the anti-rollback lease: the fix must never bypass it to make a stale child document win. Asserted by the pins plus a leg where an older incoming document is refused. |
| T-260921-e21-03 | Denial of service | the child push guard latch | medium | mitigate | A fingerprint latched before the send resolves permanently suppresses a retry of the same content; the fixed wiring releases the latch on a failed send so a dropped push is recoverable. |
| T-260921-e21-04 | Denial of service | the push quiet scheduler | medium | mitigate | The dirty flag must not be cleared without an attempted push; the two flush paths are restructured so a cleared flag always implies a send was attempted (the drop shape recorded in PROBE A). |
| T-260921-e21-05 | Information disclosure | local desktop app, same project directory | low | accept | No new I/O surface: the same document, the same event bridge, no network, no new capability. |
| T-260921-e21-SC | Tampering | pnpm installs | low | accept | No dependency changes in this plan — no package-legitimacy gate required. |
</threat_model>

<verification>
- Diagnosis gate: every probe (A..E) has raw output and every surface has an explicit (a)/(b)/(c) verdict with file:line or probe evidence; the store-seam control reproduces the plan-time green observation; the structural check is explicit per surface.
- Halt gate: the Task 1 automated gate exits 2 when any verdict marks a structural root, and the halt report is written before any production edit.
- RED gate: three pins observed failing at base, each failure naming the link it pins; no pin rewritten to accommodate a pass.
- Transfer gate: the document reaches the save realm over the pre-existing pair — no added event pair, no added IPC command, no manifest shape change.
- Persistence gate: after the fix, each surface survives the full in-vitest close/reopen chain (carrier assertion) and the store/package seam writes it into layers/<layerId>.json.
- Regression gate: the bjm image-import legs and the picker wiring assertions stay green untouched.
- c7x gate: every file c7x touched is absent from this plan's diff.
- Guardrail audit recorded per item (HELD / NOT HELD) by reading the diff; scope gate clean against base 6ec09422.
- Gates: four touched test files + `tsc --noEmit` + the full suite green, with any failure attributed by name against the 4059-pass c7x base.
- Native UAT: the five rows are pending in the SUMMARY, never claimed (the executor does not launch the app).
</verification>

<human_verification>
Native UAT — OWED BY THE USER after execution; the executor leaves these pending and never claims them (the executor does not launch the app; the user runs the dev server).

1. Select a reference image in the Studio → close the Studio → reopen it → the same reference is still selected. Then quit the app entirely and relaunch → still selected.
2. Place background image keyframes in the Studio → close the Studio → reopen it → the keys are present at their frames and render. Then quit/relaunch → same.
3. Add a track with the Studio's (+) → close the Studio → reopen it → the track is present with its content. Then quit/relaunch → same.
4. Save/load roundtrip of the .mce package keeps all three.
5. Regression: the quick-260921-bjm gallery imports still persist (import a photo, close Studio, reopen, quit/relaunch).
</human_verification>

<success_criteria>
- The diagnosis is on record per surface with live evidence and a structural verdict; the store/save seam is exonorated or indicted by evidence, not assumption.
- The broken link is named and fixed by extending the existing child→main document path — no parallel transport, no manifest shape change, no c7x territory.
- Three RED-then-GREEN pins assert the reopen contract end to end through the real functions on both sides of the bridge, plus store/package locks for all three surfaces.
- Four touched test files, tsc --noEmit and the full suite green; scope gate clean; guardrail audit recorded; the five native UAT rows explicitly pending.
</success_criteria>

<output>
Create `.planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-SUMMARY.md` when done — carrying the per-surface (a)/(b)/(c) verdicts with raw evidence, the A..E probe outputs, the structural check, the before/after of the diagnosed link, the raw RED output and the green runs, the lock legs' outcomes, the bjm regression row, the scope + guardrail audit, the gate table, and the five native UAT rows marked pending.

Also write `.planning/quick/260921-e21-studio-origin-state-never-persists-refer/260921-e21-RED-EVIDENCE.json` (Task 1) in the format of the bjm evidence file: verdicts, structural booleans, probe outputs, and the verbatim RED run per pin. Neither `.planning` artifact is committed by the executor.
</output>
