---
phase: quick-260921-e21
plan: 260921-e21
subsystem: ui
tags: [tauri-webview-bridge, preact-signals, efx-paint, persistence, document-sync, push-guard, mce-package]

# Dependency graph
requires:
  - phase: 52.2
    provides: the .mce reference-only package manifest and the child→main document-sync pair (physic-paint:efx-paint-document) this plan repairs — the format itself is exonerated and unchanged
  - phase: 52.2-15
    provides: the Studio document-sync scheduler (dirty flag, gesture-quiet scheduler, close flush) and its source-shape contract suite in PhysicsPaintStudio.test.ts
  - phase: 260921-bjm
    provides: the validated request/result bridge-pair idiom and the RED-EVIDENCE.json shape reused by this plan's diagnosis artifact
provides:
  - "Verdict (a) on all three Studio-origin surfaces: the mutation is consumed by the child push WIRING without an attempted push, and a failed send is latched away — every downstream link (transport, parent apply, reopen carrier, child hydration, store/package save) is exonerated by live probe"
  - "The push wiring no longer clears documentSyncDirty before the push mode is confirmed: a cleared flag now always implies a send was attempted (close flush + both scheduler flush bodies)"
  - "A failed document push re-arms the push guard and keeps the change owed within a bounded budget (DOCUMENT_SYNC_MAX_AUTO_RETRIES = 3), instead of latching that content away for the session"
  - "Three RED-then-GREEN source-shape pins over the wired contracts, one end-to-end GREEN leg through the real child transport → real parent listener → real launch carrier, and two store-seam locks writing the surfaces into layers/<layerId>.json"
affects: [53, any-future-studio-document-sync-work, any-future-child-realm-persistence-surface]

# Actuals (#2632) — pairs with the plan's estimate to calibrate future estimates.
# Same estimateTokens scale (chars/4 over the realized diff), never a harness token count.
actuals:
  tokens: 6042        # chars/4: 24,168 app-diff chars / 4 (git diff 6ec09422..HEAD | wc -c)
  tasks: 3
  commits: 3          # MEASURED: git rev-list --count 6ec09422..HEAD (#3968)
  plan_head_before: 6ec09422ffa88a4e8158ece88eeb886d97a8a2b0

tech-stack:
  added: []
  patterns:
    - "clear-after-attempt: a pending-change flag is cleared only INSIDE the gate that proves the push was attempted and the transport mode is confirmed — never before the gate is read"
    - "bounded push retry: a failed document push re-arms its dedupe guard and re-marks the change owed under a fixed retry budget, so a dropped send is recoverable without turning the debounce into an unbounded retry loop"
    - "replaced pin, not weakened pin: a pin that is GREEN at base while its surface is reported lost is a verdict datum pointing at a different link — it is re-pointed at the named link and the original end-to-end contract is kept as a permanent green lock"

key-files:
  created: []
  modified:
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx
    - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
    - app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts
    - app/src/stores/efxPaintChildParentSync.scratch.test.ts

key-decisions:
  - "Verdict (a) on all three surfaces: the mutation never leaves the child realm because the child PUSH WIRING consumes it without an attempted push and latches a failed send away. The transport, the parent apply, the reopen carrier, the child hydration and the store/package save are each exonerated by live probe — not by assumption. The plan's plan-time hypothesis (transport/apply) is falsified."
  - "The plan's original end-to-end carrier pins are GREEN at base, so they are verdict data, not failures. Per the plan's own Task 1 STEP 2 each was replaced by a leg over the link the verdict named, and the end-to-end carrier contract was KEPT as a permanent green regression lock in physicsPaintBridgeTransport.test.ts rather than deleted."
  - "The fix stays inside the authorized file set by re-arming the push guard FROM the component (documentSyncPushGuardRef.current = createDocumentSyncPushGuard()) instead of changing documentSyncPushGuard.ts, which the plan did not authorize. The guard's pre-resolve fingerprint latch is untouched."
  - "The retry is explicitly bounded: DOCUMENT_SYNC_MAX_AUTO_RETRIES = 3 automatic re-flushes, then the change stays owed and the user-driven one-shot close flush is the last attempt. The 2s debounce must never become an unbounded retry loop (efx-async-orchestration)."
  - "flushStudioStateRef (the save/export flush) was deliberately LEFT UNCHANGED: its existing `if (layerId && (mode === 'Tauri' || mode === 'Browser fallback')) {` shape is pinned by an existing contract assertion (PhysicsPaintStudio.test.ts:214), and that path reads the mode before clearing already."
  - "No manifest/package shape change, no new event pair, no new IPC or Rust command, no c7x revision/identity or loop-clip-lifecycle territory. Structural check S1..S4 all false — the halt path did NOT fire."

patterns-established:
  - "A child-realm change that appears lost is bisected link by link with runnable probes before any production edit; a GREEN probe exonerates its link and re-points the bisection, and a GREEN pin is replaced rather than weakened."
  - "A dirty flag that gates a cross-realm push is cleared only after the push is attempted under a confirmed transport mode; the companion dedupe guard is re-armed on failure so the same content remains retryable."

requirements-completed: []

# Coverage metadata (#1602) — one entry per shipped deliverable.
coverage:
  - id: D1
    description: "A reference image selected in the Studio reaches the realm that owns the save path before the Studio closes, so the reopen carries the same active reference."
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts#carries the reference selection, the background keyframes and the (+) track into the reopen carrier"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#PIN 1 (reference selection): the close flush consumes the pending change only after a push with a confirmed mode"
        status: pass
      - kind: unit
        ref: "app/src/stores/efxPaintChildParentSync.scratch.test.ts#keeps a child-selected reference source, with its refs, when the child pushes and the parent saves"
        status: pass
    human_judgment: true
    rationale: "The cross-webview Tauri event path (a real emitTo between two live webviews) is mocked in every unit leg, and the Studio cannot be mounted in vitest (Tauri window/engine deps) — so the close/reopen and quit/relaunch behaviour is native UAT row 1."
  - id: D2
    description: "A background image clip placed in the Studio reaches that realm with its frame position and source refs, so the reopen shows the clip at the same frame."
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts#carries the reference selection, the background keyframes and the (+) track into the reopen carrier"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#PIN 2 (background keyframes): neither scheduler flush consumes the pending change before the push mode is confirmed"
        status: pass
      - kind: unit
        ref: "app/src/stores/efxPaintChildParentSync.scratch.test.ts#keeps a child-placed background clip, at its frame with its source refs, when the child pushes and the parent saves"
        status: pass
    human_judgment: true
    rationale: "Same mock boundary as D1; the live clip-at-its-frame rendering after a real close and a real quit/relaunch is native UAT row 2."
  - id: D3
    description: "A track added with the Studio's (+) reaches that realm with its content, so the reopen shows the track and its painted content."
    verification:
      - kind: unit
        ref: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts#carries the reference selection, the background keyframes and the (+) track into the reopen carrier"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/PhysicsPaintStudio.test.ts#PIN 3 ((+) track with its content): a failed push is retryable, never latched away"
        status: pass
      - kind: unit
        ref: "app/src/stores/efxPaintChildParentSync.scratch.test.ts#keeps Track 1 keys when the child pushes its document and the parent saves"
        status: pass
    human_judgment: true
    rationale: "Same mock boundary as D1/D2, plus the painted content's visual reproduction after a real reopen is native UAT row 3."
  - id: D4
    description: "The transfer stays the pre-existing child→main document-sync channel: no added event pair, no added IPC/Rust command, no manifest schema change — the 52.2 reference-only law holds."
    verification:
      - kind: other
        ref: "scope gate: git diff --name-only 6ec09422..HEAD lists 4 app files, none in src-tauri/, none in app/src/efx-paint/document/**, no capability file, no event-constant file"
        status: pass
      - kind: unit
        ref: "app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts#Studio-origin document surfaces across the real pair (quick-260921-e21) — the handler installs on the PRE-EXISTING physic-paint:efx-paint-document pair"
        status: pass
    human_judgment: false
  - id: D5
    description: "The three surfaces survive the store/package seam: after the child pushes and the parent registers, the parent save writes them into layers/<layerId>.json."
    verification:
      - kind: unit
        ref: "app/src/stores/efxPaintChildParentSync.scratch.test.ts (5 tests, pass — the pre-existing track case plus the two new surface cases)"
        status: pass
    human_judgment: true
    rationale: "Unit-proven at the store/package seam; the durability of a real .mce package through a real quit/relaunch is native UAT row 4."
  - id: D6
    description: "The quick-260921-bjm gallery/background import path is unchanged and still persists; its bridge legs and picker wiring assertions stay green untouched."
    verification:
      - kind: unit
        ref: "bjm regression row: vitest run on physicsPaintBridgeTransport + PhysicsPaintStudio + BackgroundAssetPickerView + projectStore + physicPaintStore → 5 files passed, 313 passed | 9 todo (322), 0 failed"
        status: pass
    human_judgment: true
    rationale: "The automated row proves the legs are green in the same run; the live end-to-end import-then-reopen is native UAT row 5."
  - id: D7
    description: "Quick 260921-c7x is not re-opened: the canonical loop-clip revision authority and the terminal physical-edit mismatch release behave exactly as shipped."
    verification:
      - kind: other
        ref: "c7x gate: every file touched by c7x (useRotoPhysicalEditCoordinator.ts + .test.ts, physicsPaintRotoPhysicalModel.ts, physicPaintBridge.ts + .test.ts) is ABSENT from git diff --name-only 6ec09422..HEAD (comm -12 empty)"
        status: pass
      - kind: unit
        ref: "vitest run src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/lib/physicPaintBridge.test.ts → 2 files passed, 218 passed | 1 skipped (219)"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-09-21
status: complete
---

# Quick 260921-e21: Studio-Origin State Never Persists (Reference / Keyframes / Tracks) Summary

**Verdict (a) on all three surfaces — the loss is in the child PUSH WIRING, which cleared `documentSyncDirty` before reading the bridge mode and latched a failed send away for the session; the fix clears only after the mode is confirmed and re-arms the push guard under a bounded retry budget, with the transport, parent apply, reopen carrier, child hydration and store/package save all exonerated by live probe and the 52.2 package format untouched**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-09-21T10:15:00+02:00 (plan mtime)
- **Completed:** 2026-09-21T10:35:08+02:00
- **Tasks:** 3
- **Files modified:** 4 app files (1 production, 3 test) — +355 / −7 lines
- **Plan base:** `6ec09422` (working tree clean at start; `headAtStart` = `6ec09422ffa88a4e8158ece88eeb886d97a8a2b0`)

## Diagnosis — the (a)/(b)/(c) verdicts

The bisection walked the whole chain: child store mutation → `efxPaintVersion` → `documentSyncDirty` → `pushLiveProjection` → `sendEfxPaintDocumentSync` (`projectEfxPaintDocumentForSync` → `toTransportPayload`) → [event] → `installPhysicPaintEfxPaintDocumentListener` (`fromTransportPayload` → `parseEfxPaintDocument` → frame media → revision guard → `registerEfxPaintDocument`) → `createPhysicPaintLaunchContext` carrier → child hydration. Every link was driven with a runnable probe; the raw outputs are in `260921-e21-RED-EVIDENCE.json` (uncommitted, `.planning/` artifact).

**Verdict (a) — "the mutation stays child-realm only (never pushed)" — TRUE for all three surfaces; the root is the child PUSH WIRING.** The plan-time hypothesis pointed at the transport/apply; the probes falsify it and re-point the bisection at `PhysicsPaintStudio.tsx`, which has two drop shapes on the same link:

**Drop shape 1 — clear-before-mode-check (all three surfaces).** All three mutations DO write the child document and call `_notifyChange()`, and the child DOES set the dirty flag. But the flag is cleared before the transport mode is read:

| Path | Base (falsified) | Meaning |
|---|---|---|
| `flushDocumentSyncRef.current` (the CLOSE flush, verdict surface 1) | clears `documentSyncDirty.value = false` at `:3983`, reads `bridgeModeRef.current` at `:3986` | a change pending while the mode is still unresolved is consumed with no push and is never re-marked (the version does not bump again) |
| Scheduler `tryFlush`, non-gesture body | clears at `:4004`, mode gate at `:4007` | same shape; the timer is also dropped without rescheduling when `readInteractionIdle()` is false, leaving the change to the gesture path — which only re-runs on an `interactionIdle` transition |
| Scheduler `tryFlush`, gesture body | clears at `:4035`, mode gate at `:4038` | same shape |

`usePhysicsPaintBridgeMode` initialises to `'Unavailable'` until `detectPhysicsPaintBridgeMode` resolves, so a change made (or flushes armed) in that window is silently dropped. This is the whole-document, all-three-surfaces-at-once loss shape the defect reports.

**Drop shape 2 — the push guard latches before the send resolves (surface 3, and any surface whose push fails).** `documentSyncPushGuard.evaluate` latches `lastPushedFingerprint` BEFORE the send resolves (`documentSyncPushGuard.ts:44-48`) and `pushLiveProjection`'s `.catch` at `:3957` swallows the failure, so one failed send suppresses that exact content for the rest of the session (T-260921-e21-03). Nothing re-arms the guard and nothing re-marks the change.

**Verdict (b) — "a bridge exists but the parent ignores/rejects it" — FALSE.** PROBE C drove the REAL parent listener with the captured payload: the handler installs on the pre-existing pair, the apply is NOT rejected (no `[physicPaintBridge] Rejected EFX Paint document sync` warning), and the parent document afterwards holds all three surfaces. **T-260921-e21-01 / -02 hold at base**: `buildEfxPaintDocumentRevision` encodes `docrev/active/tracks/clips/photo` (`efxPaintDocumentRevision.ts:119-151`), so the anti-rollback lease cannot skip a real change to any of the three — the fix does not touch it.

**Verdict (c) — "it persists but the Studio launch drops it" — FALSE.** PROBE D built the real carrier and `parseCanonicalPhysicsPaintLaunchValue` accepted it (no fail-closed throw at `physicPaintBridge.ts:3541`); PROBE D2 hydrated that carrier into a fresh child store and the hydrated document kept all three surfaces. The launch and hydration are byte-unchanged by this plan.

**52.2 exoneration — explicit:** the `.mce` reference-only package rework is EXONERATED. `app/src/efx-paint/document/**`, the package/manifest writers and the `src-tauri/` tree are all absent from the diff; the document already carried `background.clips` / `photoReference` / `tracks` and persisted them correctly once the parent actually had them (PROBE E).

## Structural check (guardrail 1) — the halt path did NOT fire

| Condition | Verdict |
|---|---|
| S1 — manifest/package format change or a 52.2 reference-only-law break | **false** — no format change; the document still carries references only |
| S2 — a new independent transport rather than extending an existing pair | **false** — `physic-paint:efx-paint-document` stays the only carrier; no event pair, no IPC/Rust command added |
| S3 — c7x revision/identity authority or loop-clip lifecycle territory | **false** — no revision/identity comparison and no lifecycle resolver touched; c7x-touched files absent from the diff |
| S4 — a Rust command or capability change | **false** — `src-tauri/` and capability files absent from the diff |

**Structural verdict: not structural — no format change, no new transport.** The diagnosed link lives inside an authorized Task 2 file (`PhysicsPaintStudio.tsx`) and the fix removes two drop shapes that already exist there. `halt.fired = false`, `proceedToTask2 = true`. Task 1's automated gate observed **exit 0** (`verdicts and RED pins recorded`); it would have exited **2** on a structural root.

## Probe outputs (A..E, including the store-seam control)

| Probe | Kind | Output | Link verdict |
|---|---|---|---|
| **A** — child side: do the three mutations bump the version / set the dirty flag, and is a change ever cleared without a push? | read + run | `[e21][PROBE A] {"refOk":true,"clipOk":true,"trackOk":true,"trackId":"56f3ad1b-e990-4fe3-a782-a50b842540d7"}` | **BROKEN — the diagnosed link.** CLEAR-BEFORE-MODE-CHECK confirmed by read at `:3983/:3986`, `:4004/:4007`, `:4035/:4038`; guard pre-resolve latch confirmed at `documentSyncPushGuard.ts:44-48` |
| **B** — child transport: drive the real `sendEfxPaintDocumentSync` with a document carrying a clip, a photo reference and an added track | run | `[e21][PROBE B] {"emitCount":1,"wireLength":1368,"wireHasPhotoRef":true,"wireHasBgRef":true,"wireHasAddedTrack":true}` | **GREEN — transport exonerated.** The `{ ...document, tracks }` spread preserves `background.clips` / `photoReference`; no record failed projection in this shape |
| **C** — parent apply: feed the captured payload into the REAL listener path and read the parent document back | run | `[e21][PROBE C-handler] {"installedNames":["physic-paint:efx-paint-document","message"],"hasHandler":true}` / `[e21][PROBE C] {"appliedRevision":4,"parentRevision":0,"trackIds":["56f3ad1b-…","e21-track-1"],"photoRef":["ref-photo-1"],"clips":[{"startFrame":4,"refs":["ref-bg-1"]}]}` | **GREEN — apply exonerated**, and the idempotency guard is confirmed non-skipping (**T-260921-e21-02 holds at base**) |
| **D** — reopen carrier: call the REAL `createPhysicPaintLaunchContext` on the parent state left by the apply | run | `[e21][PROBE D] {"refOk":true,"clipOk":true,"trackOk":true,"parentTrackCount":2,"carrierBuilt":true,"carrierError":null,"carriedTrackCount":2,"carriedHasAddedTrack":true,"carriedPhotoRef":["e21-ref-1"],"carriedClips":[{"startFrame":4,"refs":["e21-bg-1"]}]}` | **GREEN — carrier exonerated** (no fail-closed throw at `physicPaintBridge.ts:3541`) |
| **D2** — child hydration: hydrate that carrier into a FRESH child store through the REAL `hydrateRotoPhysicalLaunchContext` | run | `[e21][PROBE D2] {"ok":true,"error":null,"hydratedTrackCount":2,"hydratedPhotoRef":["e21-ref-1"],"hydratedClips":[{"startFrame":4,"refs":["e21-bg-1"]}]}` | **GREEN — hydration exonerated (no verdict (c))** |
| **E** — store seam control, reproduces the plan-time observation | run | `pnpm --filter efx-motion-editor exec vitest run src/stores/efxPaintChildParentSync.scratch.test.ts` → `Test Files 2 passed (2) / Tests 9 passed (9)` | **GREEN — store/package save seam exonerated**; a child-pushed document registered by the parent and then saved keeps a child-added track in `layers/<layerId>.json`, and the manifest carries no `efx_paint_documents` blob |

## The diagnosed link — before / after

All inside `app/src/components/physic-paint/PhysicsPaintStudio.tsx` (+49 / −6). The transfer channel is unchanged.

| | Before | After |
|---|---|---|
| Close flush (`flushDocumentSyncRef.current`) | `documentSyncDirty.value = false;` then `const mode = bridgeModeRef.current;` — clear, then gate | mode read first; clear INSIDE the gate: `if (layerId && (mode === 'Tauri' \|\| mode === 'Browser fallback')) { documentSyncDirty.value = false; await pushLiveProjection(layerId, mode); }` |
| Both scheduler `tryFlush` bodies | clear at `:4004` / `:4035`, mode gate at `:4007` / `:4038` | mode read first; clear INSIDE the gate, then `void pushLiveProjection(layerId, mode)` |
| Early-return guard | `if (!documentSyncDirty.peek()) return;` | `if (!documentSyncDirty.peek() && documentSyncPushFailuresRef.current === 0) return;` — an owed change stays owed across a failure streak |
| `pendingDocumentSyncRef.current` (the close gate's `hasPending`) | `() => documentSyncDirty.peek()` | `() => documentSyncDirty.peek() \|\| documentSyncPushFailuresRef.current > 0` |
| `pushLiveProjection` failure path | `.catch` logged and swallowed; the guard's fingerprint stayed latched → that content was suppressed for the session | `.catch` re-arms the guard (`documentSyncPushGuardRef.current = createDocumentSyncPushGuard();`) and, while `documentSyncPushFailuresRef.current < DOCUMENT_SYNC_MAX_AUTO_RETRIES` (3), increments the streak and re-marks `documentSyncDirty.value = true` |
| `pushLiveProjection` success path | nothing | `.then` resets the failure streak to 0 (the budget re-arms) and deliberately leaves the pending flag as the caller set it — clearing it there would swallow a mutation that landed DURING the send |
| `flushStudioStateRef` (save/export flush) | reads the mode before clearing already | **deliberately unchanged** — its shape is pinned by the existing contract assertion at `PhysicsPaintStudio.test.ts:214` |

New named constant with its rationale comment: `DOCUMENT_SYNC_MAX_AUTO_RETRIES = 3`. New ref: `documentSyncPushFailuresRef = useRef(0)`. **No new signal, no `useState`, no render-body write, no new timer** — the retry rides the pre-existing debounce/close paths and every timer still terminates (efx-async-orchestration).

## Task Commits

Each task was committed atomically on `main`:

1. **Task 1 (tracer, TDD) — diagnose the three surfaces with live evidence, pin each one RED** — `d84e6ab7` (test/RED) — `PhysicsPaintStudio.test.ts` (+66), `physicsPaintBridgeTransport.test.ts` (+154)
2. **Task 2 — fix the diagnosed link by extending the existing pair** — `d418dbce` (fix/GREEN) — `PhysicsPaintStudio.tsx` (+49/−6), `physicsPaintBridgeTransport.test.ts` (+6/−3)
3. **Task 3 — lock the store/package half across all three surfaces** — `2238e5d1` (test) — `efxPaintChildParentSync.scratch.test.ts` (+84/−1)
4. **Plan metadata (SUMMARY + RED evidence):** NOT committed by this executor — `.planning/` artifacts are left uncommitted for the orchestrator's docs commit per the dispatch constraint.

## RED → GREEN

**RED** (`d84e6ab7`; raw evidence preserved verbatim in `.planning/quick/260921-e21-…/260921-e21-RED-EVIDENCE.json`, deliberately left uncommitted as a docs artifact) — command `pnpm --filter efx-motion-editor exec vitest run <the three Task 1 files>`, exit **1**. Exactly three pre-fix failures, each naming the link it pins (never a missing symbol, never a compile error):

```
❯ src/components/physic-paint/PhysicsPaintStudio.test.ts (147 tests | 3 failed) 585ms
   × Studio-origin document state reaches the save realm (quick-260921-e21) > PIN 1 (reference selection): the close flush consumes the pending change only after a push with a confirmed mode 4ms
     → expected 92 to be greater than 229
   × … > PIN 2 (background keyframes): neither scheduler flush consumes the pending change before the push mode is confirmed 0ms
     → expected 330 to be greater than 473
   × … > PIN 3 ((+) track with its content): a failed push is retryable, never latched away 7ms
     → expected 'const pushLiveProjection = (layerId: …' to contain 'documentSyncPushGuardRef.cr…'
```

- **PIN 1** — in `flushDocumentSyncRef.current` the pending flag is cleared at offset **92**, BEFORE the push-mode gate at offset **229** (base `:3983` clears, `:3986` gates).
- **PIN 2** — the same clear-before-gate shape in both scheduler bodies (base `:4004` clears, `:4007` gates; `:4035` / `:4038`).
- **PIN 3** — `pushLiveProjection` contains no re-arm of `documentSyncPushGuardRef`: the guard latches its fingerprint before the send resolves and the `.catch` swallows the failure (T-260921-e21-03).

The same RED run is GREEN for everything else: 144 passed in `PhysicsPaintStudio.test.ts`, 29 passed in `physicsPaintBridgeTransport.test.ts` (including the new end-to-end leg), 6 passed in `usePhysicsPaintLaunchIntegration.test.ts`.

**GREEN** (`d418dbce`) — all three pins converted to real-assertion passes, **no assertion weakened and none deleted**. **No production file was touched in Task 1.**

**The pins that were replaced, and why.** The plan's original pins asserted the end-to-end reopen carrier. Run at base, that chain is GREEN for all three surfaces (probes B/C/D/D2) — a carrier pin is therefore a **verdict datum pointing at a different link, not a failure**. Per the plan's own Task 1 STEP 2, each green pin was replaced by a leg over the link the verdict named, and the end-to-end carrier contract was **kept** as a permanent green regression lock (`physicsPaintBridgeTransport.test.ts#carries the reference selection, the background keyframes and the (+) track into the reopen carrier`, driving the real child transport → the real parent listener → the real launch carrier). Nothing was weakened to force RED.

## Lock legs' outcomes

- **Store/package seam** (`efxPaintChildParentSync.scratch.test.ts`, +84/−1) — the pre-existing track case was joined by two new ones, each following the same harness: install the owning sequence → register the base document → mutate the child store → push → parent registers → `projectStore.saveProject()` → read the staged layer sub-file:
  - `keeps Track 1 keys when the child pushes its document and the parent saves` (pre-existing) — `layers/<layerId>.json` carries the child-added track's keys.
  - `keeps a child-placed background clip, at its frame with its source refs, when the child pushes and the parent saves` — `document.background.clips` length 1, `startFrame === 4`, `sourceFrameRefs === ['bg-ref-1','bg-ref-2']`.
  - `keeps a child-selected reference source, with its refs, when the child pushes and the parent saves` — `document.photoReference?.sourceFrameRefs === ['ref-photo-1','ref-photo-2']`.
  - Result: **5 passed**.
- **End-to-end pair leg** (`physicsPaintBridgeTransport.test.ts#carries the reference selection, the background keyframes and the (+) track into the reopen carrier`) — the real `sendEfxPaintDocumentSync` payload is captured from the `emitTo` stub, re-applied through the installed real listener, and the real `createPhysicPaintLaunchContext` carrier is asserted to carry all three surfaces. **Pass** (green at base, kept as a permanent lock).

## Regression rows

**bjm (gallery/background import) — GREEN, untouched.** `vitest run` on `physicsPaintBridgeTransport` + `PhysicsPaintStudio` + `BackgroundAssetPickerView` + `projectStore` + `physicPaintStore` → **5 files passed, 313 passed | 9 todo (322), 0 failed**. The bjm baseline in its own SUMMARY was 309 passed | 9 todo (318); this run is **+4 = exactly the 3 pins + the 1 end-to-end leg added by this plan** — every bjm leg is present and green. The fix did not touch the parent apply shared with the import path (the apply is byte-unchanged; PROBE C drove it as-is).

**c7x (physical-edit settlement) — not re-opened.** `comm -12` between the c7x commit's file list and this plan's diff is **empty**: `useRotoPhysicalEditCoordinator.ts` / `.test.ts`, `physicsPaintRotoPhysicalModel.ts`, `physicPaintBridge.ts` / `.test.ts` are all absent from `git diff --name-only 6ec09422..HEAD`. Their suites were run anyway and are green: **2 files passed, 218 passed | 1 skipped (219)**.

## Gate results

| Gate | Command | Result |
|---|---|---|
| Task 1 diagnosis gate | `vitest run` (3 Task 1 files) + the RED-EVIDENCE JSON contract check | **exit 0** — `verdicts and RED pins recorded` (would exit 2 on a structural root; it did not) |
| Task 2 verify | `vitest run` (4 touched files) + `tsc --noEmit` | **exit 0** |
| Touched test files (4) | `vitest run` on physicsPaintBridgeTransport, PhysicsPaintStudio, usePhysicsPaintLaunchIntegration, efxPaintChildParentSync.scratch | **exit 0** — 4 files passed, **187 passed (187)** |
| Types | `pnpm --filter efx-motion-editor exec tsc --noEmit` | **exit 0**, no output |
| Full suite | `pnpm --filter efx-motion-editor exec vitest run` | **exit 0** — 216 passed \| 2 skipped (218 files), **4065 passed \| 1 skipped \| 101 todo (4167)** |
| bjm regression | the 5-file row above | **exit 0** — 313 passed \| 9 todo (322) |
| c7x regression | 2-file row above | **exit 0** — 218 passed \| 1 skipped (219) |
| Scope gate | `git diff --name-only 6ec09422..HEAD` | **clean** — exactly 4 app files (below) |
| `commits` measured | `git rev-list --count 6ec09422ffa88a4e8158ece88eeb886d97a8a2b0..HEAD` | **3** (from the persisted plan ledger, not narrated) |

Full-suite attribution: the c7x base was **4059 passed / 0 failed**; this run is **4065 passed / 0 failed** — **+6, exactly the new legs** (3 pins + 1 end-to-end leg + 2 store-seam cases). **Zero failures observed in this plan's runs, zero absorbed.**

## Scope gate

`git diff --name-only 6ec09422..HEAD` lists exactly four paths:

```
app/src/components/physic-paint/PhysicsPaintStudio.test.ts        (+66 / −0)
app/src/components/physic-paint/PhysicsPaintStudio.tsx            (+49 / −6)
app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts  (+156 / −0)
app/src/stores/efxPaintChildParentSync.scratch.test.ts            (+84 / −1)
```

No `src-tauri/`, no capability file, no dependency manifest, no `app/src/efx-paint/document/**`, no manifest/package writer, no `usePhysicsPaintLaunchIntegration.ts` production change, no `physicPaintBridge.ts` change, no `physicPaintFlush.ts` change, no `types/physicPaint.ts` change. **All four are inside the plan's authorized Task 1/2/3 file sets — zero out-of-set files.**

## Guardrail audit (by reading the diff)

1. **Exactly one transport carries the document — no added event pair, no added IPC/Rust command — HELD.** No `emitTo(`, `listen(`, `invoke(` or `new CustomEvent` is added anywhere in the diff; the end-to-end leg asserts the handler installs on the PRE-EXISTING `physic-paint:efx-paint-document` pair.
2. **No manifest/package shape change; `app/src/efx-paint/document/**` untouched; the 52.2 reference-only law holds — HELD.** Both paths are absent from the diff; the document keeps carrying `sourceFrameRefs` references only.
3. **No change to the loop-clip lifecycle authority, the canonical encoder, the physical-edit coordinator or any revision/identity comparison (c7x untouched) — HELD.** `comm -12` empty (shown above); no `buildEfxPaintDocumentRevision` comparison is touched, and **T-260921-e21-02's anti-rollback lease is never bypassed** — PROBE C proved it does not need to be.
4. **No `useState` and no new signal in the Studio edit; every timer/timeout still terminates — HELD.** The diff adds no `useState`, no `signal(` call, and no new timer; the retry rides the pre-existing 2s debounce and close flush, and is bounded by `DOCUMENT_SYNC_MAX_AUTO_RETRIES = 3`.
5. **The picker controller/view and its pre-flight copy are untouched — HELD.** `BackgroundAssetPickerView.tsx` and its test are absent from the diff; its 14 tests pass in the bjm regression row.

**Threat-register dispositions applied:** **T-260921-e21-01** (mitigate — the fail-closed parse is untouched; no shape was widened, and `types/physicPaint.ts` was correctly NOT needed), **T-260921-e21-02** (mitigate — the revision guard is byte-unchanged and PROBE C proved a real change to each of the three surfaces passes it), **T-260921-e21-03** (mitigate — the failure path re-arms the guard, PIN 3), **T-260921-e21-04** (mitigate — no flush path clears the flag without a send being attempted, PIN 1 + PIN 2), **T-260921-e21-05** (accept — no new I/O surface: same document, same event bridge, no network, no new capability), **T-260921-e21-SC** (accept — no dependency change, no package-install gate required).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Re-pointed the three pins from the end-to-end carrier form to the wiring link the verdicts named**
- **Found during:** Task 1 (diagnosis + RED)
- **Issue:** the plan's original pins asserted the end-to-end reopen carrier. Driven at base, that chain is GREEN for all three surfaces (probes B/C/D/D2) — so a carrier pin cannot be RED, and per the plan's own rule "a pin that passes at base is a verdict datum pointing at a different link — replace it, never weaken the assertion to force RED."
- **Fix:** all three pins were re-pointed at the two drop shapes of the child push wiring (clear-before-mode-check on the close flush, clear-before-mode-check on both scheduler bodies, and the pre-resolve guard latch), and the original end-to-end carrier contract was KEPT as a permanent green lock in `physicsPaintBridgeTransport.test.ts` rather than dropped. No assertion was weakened.
- **Files modified:** `app/src/components/physic-paint/PhysicsPaintStudio.test.ts`, `app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts`
- **Verification:** the three re-pointed pins observed RED at base with their verbatim output captured, then GREEN after the fix; the kept carrier leg is green at base and after.
- **Committed in:** `d84e6ab7` (pins), GREEN and the kept leg in `d418dbce`

**2. [Rule 3 - Blocking] Narrowed `carrier.document` in the new end-to-end leg so Task 1 type-checks**
- **Found during:** Task 2 (the `tsc --noEmit` gate)
- **Issue:** Task 1's committed end-to-end leg used `carrier.document.photoReference…` on a possibly-undefined property — `tsc --noEmit` reported 3 errors (`TS18048: 'carrier.document' is possibly 'undefined'` at `physicsPaintBridgeTransport.test.ts:829,830,832`). The leg is a blocking gate for Task 2 and could not be left as committed.
- **Fix:** narrowed without a non-null assertion: `const carrierDocument = carrier.document; if (!carrierDocument) throw new Error('the reopen launch carrier carried no document');` and asserted on `carrierDocument`.
- **Files modified:** `app/src/components/physic-paint/bridge/physicsPaintBridgeTransport.test.ts`
- **Verification:** `tsc --noEmit` → exit 0; the leg still passes and its assertions are unchanged in substance.
- **Committed in:** `d418dbce` (part of the Task 2 commit — Task 1's own commit did not type-check)

**3. [Rule 3 - Blocking] Installed the owning sequence in the two new store-seam cases**
- **Found during:** Task 3 (the store-seam locks)
- **Issue:** both new cases failed `Error: no staged write for layers/layer-1.json` from `stagedLayerDocument`. Root cause: the package save only indexes a Physics Paint layer that belongs to a live sequence, and the file's `beforeEach` runs `sequenceStore.reset()`. The pre-existing track case passes because it installs a sequence.
- **Fix:** added the module-scope `installParentSequence(layerId)` helper (the same shape the pre-existing case uses inline) and called it in both new cases. No production change, no test weakened.
- **Files modified:** `app/src/stores/efxPaintChildParentSync.scratch.test.ts`
- **Verification:** the file goes 3 passed → **5 passed**.
- **Committed in:** `2238e5d1`

---

**Total deviations:** 3 auto-fixed (1 bug, 2 blocking) — no Rule 4 architectural decision was needed, and **no file outside the plan's authorized sets was touched**.

## Issues Encountered

**The plan-time hypothesis was falsified by live probe — and that was the intended outcome.** The plan predicted the transport or the apply as the likely root. PROBE B/C/D/D2/E instead came back GREEN for all three surfaces, which is a *verdict datum*, not a failure: it exonerated every downstream link and narrowed the root to the push wiring. The plan anticipated exactly this in Task 1 STEP 2 and prescribed the pin-replacement protocol, which was followed.

**RED-evidence classifier limitation (tooling, not a RED-quality trip).** `check tdd-red-evidence` parses node-`--test` TAP summary blocks (`# tests` / `# pass` / `# fail`), which vitest emits in no reporter — `--reporter=tap` yields indented per-test lines the line-anchored regex misses, and `--reporter=tap-flat` yields flat `not ok N - …` lines but still no summary block, so the parsed count stays 0 → `INVALID_RED` / `zero_tests_discovered` on parser grounds alone. Same precedent as 52.2-01, 260919-azh, 260920-ji7, 52.3-01/-02 and 260921-bjm. The gate did NOT trip on RED quality: the three pins failed on real mechanism assertions, each naming the link it pins. The verbatim failing run is recorded in the uncommitted RED-EVIDENCE artifact.

## Deferred Issues

**`WINDOWS.md` ledger refuses writes (pre-existing, out of scope).** `windows append` returned `Ledger counts disagree with entries: frontmatter open/waived/fixed/total=24/1/42/67 but entries yield 29/1/42/72.` — identical to the failure recorded in the bjm SUMMARY; the frontmatter was last reconciled 2026-08-12 while entries kept being appended. This plan's deviation entry therefore could not be recorded in the ledger and is recorded here instead (the ledger contract is best-effort and never blocks execution). It needs a one-time count reconciliation before `/gsd-ship` can use it.

## Known Stubs

None. No hardcoded empty values flowing to UI, no placeholder copy, no unwired data source. The only outstanding items are the native UAT rows below, which are owed by the user and are not stubs.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change at a trust boundary. The transfer still runs over the pre-existing `physic-paint:efx-paint-document` pair with the same fail-closed parent parse and the same revision guard; the only new behaviour is retry-within-a-budget on the child side.

## Native UAT — PENDING, NOT CLAIMED

The executor did not launch the app or the dev server (project rule: the user runs it). All five rows are **PENDING**:

| # | Row | Status |
|---|---|---|
| 1 | Select a reference image in the Studio → close the Studio → reopen it → the same reference is still selected. Then quit the app entirely and relaunch → still selected. | **PENDING** |
| 2 | Place background image keyframes in the Studio → close the Studio → reopen it → the keys are present at their frames and render. Then quit/relaunch → same. | **PENDING** |
| 3 | Add a track with the Studio's (+) → close the Studio → reopen it → the track is present with its content. Then quit/relaunch → same. | **PENDING** |
| 4 | Save/load roundtrip of the `.mce` package keeps all three. | **PENDING** |
| 5 | Regression: the quick-260921-bjm gallery imports still persist (import a photo, close Studio, reopen, quit/relaunch). | **PENDING** |

**Automated-ready only. Nothing in this plan is claimed as user-verified.**

## Next Phase Readiness

- The child push wiring no longer consumes a pending change without an attempted push, and a failed send is now recoverable within a bounded budget — so the three Studio-origin surfaces ride the pre-existing document-sync pair into the realm that owns the save path.
- The `.mce` package format, the parent apply, the launch carrier and the child hydration are all byte-unchanged and exonerated; no migration, no version bump.
- 260921-c7x stays closed: its files are absent from this diff and its suites are green.
- Open item for the user: run the five native UAT rows above; and reconcile the `WINDOWS.md` ledger counts (deferred, pre-existing).
- No blockers for Phase 53.

---

## Self-Check: PASSED

- All four modified app files exist and appear in `git diff --name-only 6ec09422..HEAD` — verified.
- All three commits exist on `main`: `d84e6ab7`, `d418dbce`, `2238e5d1` — verified via `git log --oneline 6ec09422..HEAD`.
- `commits: 3` measured by `git rev-list --count 6ec09422ffa88a4e8158ece88eeb886d97a8a2b0..HEAD` against the persisted plan ledger, not narrated.
- Every gate above was run in this session and its output read directly.
- `.planning/` artifacts (`260921-e21-SUMMARY.md`, `260921-e21-RED-EVIDENCE.json`) are deliberately left **uncommitted** for the orchestrator's docs commit.

---
*Quick task: 260921-e21*
*Completed: 2026-09-21 (execution; native UAT PENDING)*
