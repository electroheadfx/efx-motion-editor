---
phase: quick-260905-f3v
plan: 260905-f3v
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
autonomous: true
requirements: [ACC-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Every toolbar button (Save Action, Load + Apply to Frame, Create Rail…, Delete Action, Refresh Actions) shows the styled guarded tooltip on hover INCLUDING while unavailable (reason in the copy), stays focusable via aria-disabled, and no native title attribute remains on these controls."
    - "The delete-confirmation focus flow is preserved: the Delete button is re-focused after cancel, exactly as before."
    - "In BOTH the inspector and the list renderings, Linked Rails navigation shows Previous (ChevronLeft) and Next (ChevronRight) with 'Previous Rail' / 'Next Rail' labels and styled guarded tooltips; at the boundaries the tooltip explains 'Already on the first linked Rail' / 'Already on the last linked Rail' and the button stays focusable (aria-disabled, never native disabled)."
    - "In the list view, a contextual Edit Rail button (Pencil) appears when the playhead cursor is on the current linked rail and edits that rail; it is absent otherwise. The inspector keeps its existing primary Edit Rail button and does not gain a second one."
    - "The single-rail 'Go to Rail' / 'Go to Group' case is unchanged."
    - "Script rows are readable by default: near-white name AND provenance/count sub-line text on a transparent/panel-tone background; the selected state stays distinct (#7e9cff border); hover and focus-visible remain visible; no contrast regression in script-picker dialog rows."
    - "The whole app test suite stays green and tsc --noEmit is clean (ACC-01)."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx (guarded Previous/Next nav buttons in both renderings, contextual Edit Rail in the list view, IconButton className/wrapperClassName props, ChevronLeft/ChevronRight imports)
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx (cursorOnCurrentLinkedRail derivation, handleEditCurrentLinkedGroup useCallback, memo deps, linkedGroupNavigation extension)
    - app/src/components/physic-paint/physicsPaintStudio.css (nav guarded-button rules + script-row readability rules)
    - app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts (toolbar guarded-tooltip contract tests, updated 43.2-15 nav assertions, Edit Rail harness test, script-row CSS contract tests)
  key_links:
    - "IconButton helper ↔ nav buttons: the new className/wrapperClassName props must not alter the toolbar buttons' existing rendering or styling."
    - "Studio memo deps (:3062) ↔ linkedGroupNavigation: cursorOnCurrentRail and handleEditCurrentLinkedGroup must be added to the deps array or the memo goes stale."
    - "effectiveLinkedGroup range ↔ cursorOnCurrentRail: the derivation must reuse effectiveLinkedGroup's own range so Edit Rail appears exactly when the playhead is on the current linked rail."
    - "43.2-15 test assertions: the native-disabled checks must be updated to aria-disabled and the button text to 'Previous Rail'/'Next Rail' or the suite breaks."
---

<objective>
Polish the Actions tab of the Physics Paint right panel: every toolbar button uses the styled guarded-tooltip idiom, Linked Rails navigation gets explicit Previous/Next controls with icons and contextual Edit Rail, and script rows become readable by default.

Purpose: Unify the toolbar and Linked Rails navigation on the guarded styled-tooltip pattern already proven by the Copy button (260905-dso), surface the linked-rail context with explicit labeled controls, and fix the low-contrast script rows.

Output: Guarded toolbar + nav buttons, contextual Edit Rail in the list view, readable script rows, and contract/harness tests locking all three.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Part A is ALREADY IMPLEMENTED — do not re-implement it.
# Commit 2af560a2 (quick 260905-dso) converted the shared IconButton helper
# (PhysicsPaintScriptsPanel.tsx :413-447) to the full Copy idiom: aria-disabled
# (button stays focusable), sr-only reason span, "unavailable: {reason}" tooltip
# copy, Enter/Space guard, hide-on-click, PhysicsPaintStyledTooltip
# region="right-edge" avoidRowOverlap. The five toolbar buttons (:182-186) all
# route through IconButton with title/disabled/disabledReason/descriptionId props
# and render NO native title attribute. Task 1 only verifies this and locks it
# with the missing test coverage.

@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/physicsPaintStudio.css
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts
@app/src/components/physic-paint/view/PhysicsPaintStyledTooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the guarded-tooltip toolbar idiom (already shipped by 260905-dso) and lock it with contract tests</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</files>
  <action>
    Do NOT re-implement Part A. The shared IconButton helper (PhysicsPaintScriptsPanel.tsx :413-447) already implements the full Copy idiom and the five toolbar buttons (Save Action, Load + Apply to Frame, Create Rail…, Delete Action, Refresh Actions) already route through it with title/disabled/disabledReason/descriptionId props. The button element renders aria-disabled (never native disabled), an sr-only reason span, a PhysicsPaintStyledTooltip with "unavailable: {reason}" copy when disabled, an Enter/Space guard, and hide-on-click. No native title attribute is rendered on any of these controls.

    Add contract tests to PhysicsPaintScriptsPanel.test.ts using the existing source-code assertion helpers (getGuardedToolbarBlock, getScriptsToolbarBlock, expectInOrder):
    - For EACH of the five toolbar buttons, the IconButton block contains aria-disabled, aria-describedby (when a disabledReason is present), PhysicsPaintStyledTooltip, and the button element carries no native title= attribute.
    - The tooltip copy uses the de-prefixed grammar: "unavailable: ${reason}" when disabled, plain title otherwise; the block does not contain the " — unavailable: " pattern.
    - The delete-confirmation focus flow is intact: the Delete IconButton receives buttonRef={deleteButtonRef} and the existing effect still re-focuses deleteButtonRef.current after cancel (the existing assertions at :180 already cover this — keep them green).
    - The toolbar buttons keep their ordered accessible labels (Save Action before Load + Apply to Frame before Create Rail… before Delete Action before Refresh Actions) and the Copy button stays the last guarded control.
    Do not modify production code in this task.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</automated>
  </verify>
  <done>New tests assert every toolbar button uses the guarded idiom (aria-disabled + styled tooltip + no native title) and the full PhysicsPaintScriptsPanel test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Linked Rails nav — guarded Previous/Next with icons + contextual Edit Rail</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</files>
  <action>
    PhysicsPaintScriptsPanel.tsx:
    - Add ChevronLeft and ChevronRight to the lucide-preact import (Pencil is already imported).
    - Extend PhysicsPaintScriptsPanelProps.linkedGroupNavigation (:17-23) with cursorOnCurrentRail: boolean and onEditCurrent: () => void.
    - Extend the IconButton helper (:413-447) with two optional props: className?: string (default 'physics-paint-script-icon-button', applied to the button element) and wrapperClassName?: string (default 'physics-paint-roto-key-icon-action', applied to the outer span). No other behavior change — the toolbar buttons keep their default classes.
    - Add const previousRailReasonId = useId(); and const nextRailReasonId = useId(); next to the existing reason ids (:69-72).
    - Convert the Previous/Next buttons in BOTH renderings (inspector :132-147, list :224-225) to the guarded idiom via IconButton:
      - Previous: label="Previous Rail", title="Previous Rail", disabled={currentIndex === 0}, disabledReason={currentIndex === 0 ? 'Already on the first linked Rail' : undefined}, descriptionId={previousRailReasonId}, onClick={onPrevious}, className="physics-paint-loop-clip-inspector-action", children = ChevronLeft size={16} aria-hidden + a span class="physics-paint-roto-key-icon-label" with text "Previous Rail".
      - Next: label="Next Rail", title="Next Rail", disabled={currentIndex === total - 1}, disabledReason={currentIndex === total - 1 ? 'Already on the last linked Rail' : undefined}, descriptionId={nextRailReasonId}, onClick={onNext}, className="physics-paint-loop-clip-inspector-action", children = ChevronRight size={16} aria-hidden + a span class="physics-paint-roto-key-icon-label" with text "Next Rail".
      - The single-rail case ("Go to Rail" in the inspector :122-129, "Go to Group" in the list :220-221) stays as-is — plain button, no guard.
    - Add the contextual Edit Rail button in the LIST view nav section only (inside the .physics-paint-loop-clip-inspector-actions div at :219-228, after Previous/Next), rendered only when linkedGroupNavigation.cursorOnCurrentRail is truthy: IconButton label="Edit Rail", title="Edit Rail", onClick={linkedGroupNavigation.onEditCurrent}, className="physics-paint-loop-clip-inspector-action", wrapperClassName="physics-paint-roto-key-icon-action physics-paint-loop-clip-edit-current", children = Pencil size={16} aria-hidden + a span class="physics-paint-roto-key-icon-label" with text "Edit Rail". The inspector view does NOT get this contextual button (it already has its primary Edit Rail button at :153-173).

    PhysicsPaintStudio.tsx:
    - Derive cursorOnCurrentLinkedRail near effectiveLinkedGroup (:2059-2063): true iff effectiveLinkedGroup exists AND currentFrame is inside its range — currentFrame >= effectiveLinkedGroup.placementStart AND (effectiveLinkedGroup.repeat === 'infinity' ? true : currentFrame < effectiveLinkedGroup.placementStart + effectiveLinkedGroup.sourceKeyIds.length * effectiveLinkedGroup.repeat). Reuse effectiveLinkedGroup's own range; currentFrame is the launch constant at :676 (the same value effectiveLinkedGroup already consumes).
    - Add handleEditCurrentLinkedGroup as a useCallback: if effectiveLinkedGroup exists, void handleOpenRotoLoopEdit(effectiveLinkedGroup.loopId) (handleOpenRotoLoopEdit at :2021-2027).
    - Extend the linkedGroupNavigation object (:3121-3129) with cursorOnCurrentRail: cursorOnCurrentLinkedRail and onEditCurrent: handleEditCurrentLinkedGroup.
    - Add cursorOnCurrentLinkedRail and handleEditCurrentLinkedGroup to the rightPanelPropsMemo.resolve deps array (:3062).

    physicsPaintStudio.css (near :1227-1234):
    - .physics-paint-loop-clip-inspector-actions .physics-paint-roto-key-icon-action { display: flex; min-width: 0; }
    - .physics-paint-loop-clip-inspector-actions .physics-paint-loop-clip-inspector-action { flex: 1 1 auto; width: 100%; gap: 4px; padding: 0 6px; }
    - .physics-paint-loop-clip-inspector-actions .physics-paint-loop-clip-inspector-action svg { flex: 0 0 auto; }
    - .physics-paint-loop-clip-inspector-actions .physics-paint-loop-clip-inspector-action .physics-paint-roto-key-icon-label { white-space: nowrap; }
    - aria-disabled grey-out mirroring the existing .physics-paint-loop-clip-inspector-action:disabled colors (:1233): border-color #4d5156, background #34373a, color #777e86, cursor not-allowed, pinned on hover and focus-visible so an unavailable nav button never looks interactive.
    - .physics-paint-loop-clip-inspector-actions .physics-paint-roto-key-icon-action.physics-paint-loop-clip-edit-current { grid-column: 1 / -1; } so Edit Rail spans the full row.

    PhysicsPaintScriptsPanel.test.ts:
    - Update the 43.2-15 assertions (:722-735): Previous/Next are now guarded — assert aria-disabled ('true' at the boundaries) instead of native disabled, and the button text is 'Previous Rail' / 'Next Rail' (textOf includes the label span). The single-rail fixture (:709-720) stays valid as-is.
    - Add source-code assertions: both renderings' Previous/Next use IconButton with ChevronLeft/ChevronRight, labels 'Previous Rail'/'Next Rail', disabledReason 'Already on the first linked Rail'/'Already on the last linked Rail', and no native disabled= on the nav buttons.
    - Add a harness test: with linkedGroupNavigation { currentIndex: 1, total: 3, cursorOnCurrentRail: true, onEditCurrent } the list view renders an Edit Rail button wired to onEditCurrent; with cursorOnCurrentRail: false it does not render.
    - Add Studio source assertions: the linkedGroupNavigation object includes cursorOnCurrentRail and onEditCurrent, handleEditCurrentLinkedGroup is a useCallback calling handleOpenRotoLoopEdit, and the memo deps array includes cursorOnCurrentLinkedRail and handleEditCurrentLinkedGroup.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</automated>
  </verify>
  <done>Both renderings show guarded Previous/Next with ChevronLeft/ChevronRight icons, 'Previous Rail'/'Next Rail' labels, and boundary reasons in the tooltip; the list view shows the contextual Edit Rail only when cursorOnCurrentRail is true and it edits the current rail; the Studio wires cursorOnCurrentRail + onEditCurrent through the memo; the full suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Script rows readable by default</name>
  <files>app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</files>
  <action>
    physicsPaintStudio.css (.physics-paint-script-row at :1196-1219):
    - Change the default row background from #3b3d3f to transparent so rows blend with the panel; the 1px border keeps row separation.
    - Keep color: #eef1f4 on the row (the name inherits it via .physics-paint-script-name { color: inherit }).
    - Change .physics-paint-script-provenance and .physics-paint-script-count color from #aeb5be to #eef1f4 so the sub-lines are near-white like the name.
    - Keep .physics-paint-script-row.selected border-color #7e9cff (distinct selected state) and its background tint.
    - Keep hover (border-color #747b83, background #414447) and the focus-visible outline — both remain visible on transparent rows.
    - Do not regress script-picker dialog rows: the app is dark-themed, so near-white text on transparent rows over the dark panel keeps contrast; verify no dialog that renders these rows sits on a light background.

    PhysicsPaintScriptsPanel.test.ts (CSS contract style, matching the existing rule-slice assertions):
    - Assert the .physics-paint-script-row default rule has background: transparent and does not contain #3b3d3f.
    - Assert the .physics-paint-script-provenance, .physics-paint-script-count rule color is #eef1f4.
    - Assert the .physics-paint-script-row.selected rule keeps border-color: #7e9cff.
    - Assert the hover and focus-visible rules still exist.
  </action>
  <verify>
    <automated>pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts</automated>
  </verify>
  <done>Script rows render with near-white name AND provenance/count text on a transparent/panel-tone background; the selected state keeps the #7e9cff border; hover and focus-visible remain visible; the CSS contract tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This quick is UI-only: no new external input, no new network surface, no new package installs. All tooltip/sr-only copy is developer-authored constants in the panel source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260905-f3v-01 | Tampering | Tooltip / sr-only reason copy | low | accept | Reasons are hardcoded strings in PhysicsPaintScriptsPanel.tsx; no user-controlled input reaches tooltip copy. |
| T-260905-f3v-02 | DoS | aria-disabled nav/toolbar buttons | low | mitigate | Buttons stay focusable while unavailable so the styled tooltip explains the reason; Enter/Space are prevented and activation is guarded, so no unintended action fires. |
| T-260905-f3v-03 | Information Disclosure | sr-only reason spans | low | accept | Reasons are the same developer-authored strings already shown in tooltips; no new sensitive data. |
| T-260905-f3v-SC | Tampering | npm/pnpm installs | low | accept | No new packages; ChevronLeft/ChevronRight come from lucide-preact, already a dependency. |
</threat_model>

<verification>
- pnpm --dir app vitest run src/components/physic-paint/view/PhysicsPaintScriptsPanel.test.ts (all three tasks; the file holds the toolbar, nav, Edit Rail, and script-row CSS contract tests)
- pnpm --dir app typecheck (tsc --noEmit clean, ACC-01)
- Native UAT (user-driven, no server start by Claude): hover each toolbar button enabled and unavailable — styled tooltip with reason copy; Linked Rails Previous/Next show icons + labels + boundary reasons; Edit Rail appears in the list view when the playhead is on the current linked rail and edits it; script rows readable by default with a distinct selected state.
</verification>

<success_criteria>
- All three tasks complete; the PhysicsPaintScriptsPanel test suite and the full app suite stay green; tsc --noEmit is clean.
- Native UAT confirms: guarded tooltips on every toolbar button (including unavailable states), explicit Previous/Next Linked Rails nav with icons and boundary reasons, contextual Edit Rail in the list view, and readable script rows with a distinct selected state.
</success_criteria>

<output>
Create `.planning/quick/260905-f3v-actions-tab-polish-styled-guarded-toolti/260905-f3v-SUMMARY.md` when done
</output>
