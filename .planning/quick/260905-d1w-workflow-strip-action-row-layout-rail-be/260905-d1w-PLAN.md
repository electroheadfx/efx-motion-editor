---
phase: quick-260905-d1w
plan: 260905-d1w
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx
autonomous: true
requirements: [ACC-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - The action row renders + Key, + Rail, Push, Insert, Duplicate, Copy, Cut, Scissor, Paste, All, Trash in that order; the Solo group is gone from the row.
    - + Rail is gated by the SAME availability law as + Key (canAddEmptyKey reactive port + ready + busy-state guard): aria-disabled, aria-describedby, sr-only reason span (id roto-key-action-reason-rail-create), Enter/Space blocked when disabled, guarded tooltip "Create rail" + the unavailable reason, and the Motion/Static/Reveal menu never opens while disabled.
    - Solo renders icon-only inside the playback pill immediately after the Loop toggle, with aria-label "Solo selected Rails", aria-pressed, the guarded styled tooltip (same armed/disarmed copy), soloToolDisabled/Reason gating, and the pill .active armed treatment (no new color literals).
    - Clicking Solo still disarms an armed Push tool (the D-20 toolbar disarm is preserved explicitly in the relocated onClick).
    - The whole app test suite stays green (ACC-01).
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (reordered + gated + Rail, relocated Solo)
    - app/src/components/physic-paint/physicsPaintStudio.css (.physics-paint-roto-solo-toggle.active rule; no orphaned group rules)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts (updated Solo armed-tint test + new order/gating/Solo-in-pill tests)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx (rotoPhysicalActions harness + menu-not-open-when-disabled test)
  key_links:
    - + Rail gating reuses canAddRotoKey / addRotoKeyDisabledReason (no rail-creation-specific availability port exists — verified by grep over useRotoTimelineActions.ts).
    - Solo's relocated onClick calls disarmPushTool() first, unconditionally, preserving the D-20 toolbar disarm the action-row onClickCapture guard previously provided.
    - The action-row onClickCapture guard still covers every remaining in-row button (Push keeps the physics-paint-push-tool-button class).
---

<objective>
Post-closure UI quick on the v1.0.0/reveal-rail branch: rework the Physics Paint Studio workflow strip action row so the Create-rail ("+ Rail") button sits immediately after + Key and is gated by the SAME availability law as + Key, the Push group sits immediately after + Rail, and the Solo button relocates out of the action row into the playback pill as an icon-only nav-button beside the Loop toggle.

Purpose: The + Rail button currently opens its Motion/Static/Reveal menu from ANY frame state (no availability gating at all), while + Key is correctly greyed on owned/generated frames. This quick makes + Rail mirror + Key's gating (same canAddEmptyKey port + ready + busy-state guard, same aria-disabled/aria-describedby/sr-only idiom, guarded tooltip, menu never opens while disabled), groups the rail-creation and Push tools next to + Key, and moves Solo into the playback pill as an icon-only toggle with the pill's .active armed treatment — preserving the D-20 toolbar disarm (clicking Solo still leaves push mode) and keeping every rail-creation flow, push drag mechanic, solo playback semantic, keyboard shortcut, and the timeline itself untouched.

Output: Reordered + gated action row (+ Key, + Rail, Push, Insert, Copy, Cut, Scissor, Paste, All, Trash), Solo relocated icon-only into the playback pill, the .physics-paint-roto-solo-toggle.active CSS rule, updated + new strip tests, and a green full suite.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Code anchors (read before editing)
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - Playback pill: L1045-1071 (Loop toggle L1046, Audio preview toggle L1047-1069, fps L1070)
  - canAddRotoKey L1720 / addRotoKeyDisabledReason L1721-1725 (the + Key availability law)
  - pushToolDisabled L1748 / pushArmedClass L1751 / pushTooltip L1799
  - soloArmed L1804 / soloArmedClass L1805 / soloToolDisabled L1810 / soloToolDisabledReason L1811-1815 / soloTooltip L1816
  - railCreateMenuOpen L1296 / railCreateAnchorRef L1302 / railCreateMenuPanelRef L1303 / railCreateTooltip L1818
  - Action row onClickCapture guard L4107-4117 (D-06/D-20 disarm)
  - + Key button L4124-4151 / Insert L4152 / Push group L4376-4407 / Solo group L4415-4449 / Create-rail group L4455-4505
@app/src/components/physic-paint/physicsPaintStudio.css
  - .physics-paint-nav-button L2236 / :disabled L2256 / .physics-paint-roto-loop-toggle.active L2299
  - .physics-paint-roto-key-utilities L2325 / .physics-paint-roto-key-icon-action L2396 / .physics-paint-roto-key-icon-button L2402
  - .physics-paint-push-tool-button.physics-paint-push-tool-armed L2467
  - .physics-paint-rail-create-group L4253 (stays — the group still exists)
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - getActionRowBlock L39 / getButtonBlock L51 / getHeaderBlock L659 / getCssRuleBlock L666
  - Solo armed-tint source contract L2085-2110 (MUST be updated when Solo leaves the row)
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx
  - Live-render harness renderStrip L90-111 (currently passes NO rotoPhysicalActions → canAddRotoKey is false → the menu-open test would break once + Rail is gated)
@app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts
  - createPhysicalActions helper L395-425 (the complete-bundle pattern the rail-create harness should mirror)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder the action row — + Rail (gated) immediately after + Key, Push immediately after + Rail; keep the rail-create live-render harness green</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx</files>
  <action>
    In PhysicsPaintWorkflowStrip.tsx, inside the `physics-paint-roto-key-utilities` div (L4123-4375):
    1. Move the ENTIRE Create-rail group div (`physics-paint-rail-create-group`, currently L4455-4505) so it renders immediately after the + Key button span (L4151), before the Insert span (L4152). Keep the wrapper span's `ref={railCreateAnchorRef}`, the tooltip handlers, the `PhysicsPaintRailCreateMenu` portal, and the three Motion/Static/Reveal menu buttons byte-identical — the AM-3 script-picker interpose flow and the onCreatePlayScriptRail('progressive'|'static') / onCreateRevealRail() ports stay untouched.
    2. Gate the + Rail button with the SAME availability law as + Key (per the task law): reuse `canAddRotoKey` (L1720) and `addRotoKeyDisabledReason` (L1721-1725) directly — a rail-creation-specific availability port does NOT exist (verified by grep over useRotoTimelineActions.ts; only canAddEmptyKey/addEmptyKeyDisabledReason exist, so the addEmptyKeyDisabledReason port is the availability port). Add to the button: `aria-disabled={!canAddRotoKey ? 'true' : undefined}`, `aria-describedby={!canAddRotoKey && addRotoKeyDisabledReason ? 'roto-key-action-reason-rail-create' : undefined}` (new reason id), keep `aria-expanded={railCreateMenuOpen.value ? 'true' : 'false'}`, guard the onClick with `if (!canAddRotoKey) return;` BEFORE toggling `railCreateMenuOpen.value` (the menu must not open while disabled), and add onKeyDown `if ((event.key === 'Enter' || event.key === ' ') && !canAddRotoKey) event.preventDefault();`. Add the sr-only reason span `<span id="roto-key-action-reason-rail-create" class="physics-paint-sr-only">{addRotoKeyDisabledReason}</span>` rendered only when `!canAddRotoKey && addRotoKeyDisabledReason`. Replace the tooltip copy with the guarded idiom: `{buildGuardedActionTooltipCopy('Create rail', addRotoKeyDisabledReason)}` (the previous descriptive copy "Create a Motion, Static, or Reveal rail on the current track." is replaced by the guarded idiom per the task law).
    3. Move the ENTIRE Push group div (`physics-paint-push-tool-group`, currently L4376-4407) so it renders immediately after the Create-rail group, before the Insert span. Push behavior, arm paint, aria-pressed, pushToolDisabled/Reason gating, and the physics-paint-push-tool-button class stay byte-identical (the action-row onClickCapture guard at L4107-4117 still covers it — the class is unchanged).
    4. Leave the Solo group div (L4415-4449) in the row for this task (it relocates in Task 2) — the existing Solo armed-tint source contract test (L2085-2110) must stay green at the end of this task.
    5. In PhysicsPaintWorkflowStripRailCreate.test.tsx, update the `renderStrip` harness (L90-111) to pass a `rotoPhysicalActions` bundle so `canAddRotoKey` is true: mirror the `createPhysicalActions` pattern from physicsPaintTrackHeaderColumn.test.ts (L395-425) — a complete `RotoPhysicalTimelineActionBundle` with `canAddEmptyKey: signal(true)`, `addEmptyKeyDisabledReason: signal(null)`, and the other members the strip reads during render backed by real signals / inert spies. Without this, the existing menu-open test (L150-193) would fail because `canAddRotoKey` is false when `rotoPhysicalActions` is absent.
    Do NOT change the rail-creation dialog tabs, the reveal creation mutation, push drag mechanics, solo playback semantics, keyboard shortcuts, the timeline, or the capture-guard semantics. No new window-level listeners. No useState additions.
  </action>
  <verify>
    <automated>pnpm exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx.test.ts</automated>
  </verify>
  <done>The action row renders + Key, + Rail (gated), Push, Insert, Duplicate, Copy, Cut, Scissor, Paste, All, Trash in that order; + Rail carries the + Key availability law (aria-disabled, aria-describedby roto-key-action-reason-rail-create, guarded onClick, Enter/Space block, guarded tooltip); the existing rail-create menu-open test and the full strip test file pass.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate Solo into the playback pill as an icon-only nav-button + CSS</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <action>
    In PhysicsPaintWorkflowStrip.tsx:
    1. Remove the Solo group div (`physics-paint-solo-tool-group`, L4415-4449) from the action row entirely.
    2. In the playback pill (L1045-1071), insert the Solo button immediately after the Loop toggle button (L1046), before the Audio preview toggle anchor (L1047). ICON-ONLY: drop the visible `<span class="physics-paint-roto-key-icon-label">Solo</span>` label. Wrap the button in a span hosting the tooltip handlers (mirroring the audio-preview-toggle-anchor pattern at L1048-1052), class `physics-paint-roto-solo-toggle-anchor` (structural only — no CSS rule required). Button className: `physics-paint-nav-button physics-paint-roto-solo-toggle${soloArmedClass}` (the pill nav-button style, NOT the action-row icon-button style). Keep: `aria-label="Solo selected Rails"`, `aria-pressed={soloArmed ? 'true' : 'false'}`, `aria-disabled={soloToolDisabled ? 'true' : undefined}`, `aria-describedby={soloToolDisabled ? 'roto-key-action-reason-solo' : undefined}`, the sr-only reason span `id="roto-key-action-reason-solo"`, the guarded styled tooltip with the SAME armed/disarmed copy (armed: 'Exit solo playback.'; disarmed: `buildGuardedActionTooltipCopy('Solo the selected Rails - play only their content within their frame range. Click again or press Escape to exit.', soloToolDisabledReason)`), and the soloToolDisabled/Reason gating. Icon: `<Focus size={15} aria-hidden="true" />` (matches the pill siblings' 15px icons).
    3. onClick order (preserves the D-20 toolbar disarm the action-row capture guard previously provided — the 43.6-06 spec's D-20 "ANY other toolbar action disarms an armed Push tool" covers Solo, and the current behavior is that clicking Solo leaves push mode): call `disarmPushTool()` FIRST, unconditionally (before the disabled check — a click on a disabled Solo button in the row also disarmed push today via the capture guard), then `soloTooltip.hide()`, then `if (soloToolDisabled) return;`, then `toggleSolo()`. onKeyDown: `if ((event.key === 'Enter' || event.key === ' ') && soloToolDisabled) event.preventDefault();`.
    4. Change the `soloArmedClass` definition (L1805) to the pill .active convention: `const soloArmedClass = soloArmed ? ' active' : '';` (the physics-paint-push-tool-armed class is a compound selector requiring the physics-paint-push-tool-button base class, which a nav-button does not carry — so the pill .active treatment is the armed visual, one armed visual, no new color literals).
    5. In physicsPaintStudio.css: extend the `.physics-paint-roto-loop-toggle.active` rule (L2299) to also cover `.physics-paint-roto-solo-toggle.active` (same accent border/background/color — no new color literals). Add `.physics-paint-roto-solo-toggle[aria-disabled="true"] { opacity: 0.35; }` mirroring the nav-button :disabled treatment (L2256) so the aria-disabled guarded state dims like the pill siblings' native-disabled state. Verify NO orphaned CSS: `physics-paint-solo-tool-group` and `physics-paint-push-tool-group` have NO CSS rules (verified by grep over app/src — nothing to remove); `.physics-paint-rail-create-group` (L4253) stays because the group still exists.
    6. In PhysicsPaintWorkflowStrip.test.ts, update the Solo armed-tint source contract test (L2085-2110): it currently slices `physics-paint-solo-tool-group` and asserts the className template carries `physics-paint-roto-key-icon-button` + `physics-paint-push-tool-button` + `${soloArmedClass}`. Rewrite it to assert the relocated button block (sliced from `aria-label="Solo selected Rails"` in the header block) contains `physics-paint-nav-button`, `physics-paint-roto-solo-toggle`, `${soloArmedClass}`, does NOT contain `physics-paint-roto-key-icon-button` or `>Solo</span>`, and that the source contains `const soloArmedClass = soloArmed ? ' active' : '';`.
    Do NOT change solo playback semantics, the Escape chain in physicsPaintStudioKeyboard.ts, the physicsPaintSoloArm module, or any keyboard shortcut. No new window-level listeners. No useState additions.
  </action>
  <verify>
    <automated>pnpm exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</automated>
  </verify>
  <done>Solo renders icon-only inside the playback pill immediately after the Loop toggle with the pill .active armed treatment, aria-pressed, the guarded styled tooltip, soloToolDisabled/Reason gating, and an explicit disarmPushTool() in the onClick; the Solo group is gone from the action row; the updated Solo armed-tint test and the full strip test file pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add new tests — action-row order, + Rail gating across unavailable states, menu-not-open-when-disabled, Solo-in-pill</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx</files>
  <action>
    In PhysicsPaintWorkflowStrip.test.ts (source-contract tests following the existing getActionRowBlock/getButtonBlock/getHeaderBlock/getCssRuleBlock conventions):
    1. "orders the action row as Key, Create rail, Push, Insert, Duplicate, Copy, Cut, Scissor, Paste, All, Trash (260905-d1w)": slice the action-row block, assert `aria-label="Add key"` < `aria-label="Create rail"` < `aria-label="Push"` < `getActionAriaLabelToken('Insert key before')` < `aria-label="Duplicate key"` < `aria-label="Copy key"` < `aria-label="Cut key"` < `getActionAriaLabelToken('Split Key Rail')` < `aria-label="Paste key"` < `getActionAriaLabelToken('Delete Frame')` by index, and assert the row does NOT contain `physics-paint-solo-tool-group`.
    2. "gates + Rail with the same availability law as + Key (260905-d1w)": get the + Rail button block via getButtonBlock(row, 'Create rail') and assert it contains `aria-disabled={!canAddRotoKey ? 'true' : undefined}`, `aria-describedby={!canAddRotoKey && addRotoKeyDisabledReason ? 'roto-key-action-reason-rail-create' : undefined}`, `if (!canAddRotoKey) return;` BEFORE the `railCreateMenuOpen.value = !railCreateMenuOpen.value;` toggle, `(event.key === 'Enter' || event.key === ' ') && !canAddRotoKey`, the sr-only id `roto-key-action-reason-rail-create`, and `buildGuardedActionTooltipCopy('Create rail', addRotoKeyDisabledReason)`.
    3. "relocates Solo into the playback pill as an icon-only nav-button (260905-d1w)": slice the header block, assert `physics-paint-roto-loop-toggle` < `aria-label="Solo selected Rails"` < `physics-paint-audio-preview-toggle-anchor` by index; the Solo button block contains `physics-paint-nav-button`, `physics-paint-roto-solo-toggle`, `${soloArmedClass}`, `aria-pressed`, `aria-disabled={soloToolDisabled`, `toggleSolo()`, `disarmPushTool()`, and does NOT contain `physics-paint-roto-key-icon-button` or `>Solo</span>`; assert the CSS contains `.physics-paint-roto-solo-toggle.active` and the source contains `const soloArmedClass = soloArmed ? ' active' : '';`.
    In PhysicsPaintWorkflowStripRailCreate.test.tsx (live-render harness):
    4. "does not open the rail-kind menu while + Rail is aria-disabled (260905-d1w)": render the strip with a `rotoPhysicalActions` bundle where `canAddEmptyKey: signal(false)` and `addEmptyKeyDisabledReason: signal('The current frame already has a real Roto key.')`; find the 'Create rail' button, assert `aria-disabled` is 'true' and `aria-expanded` is 'false'; invoke its onClick; re-render (hooks.rewind) and assert `aria-expanded` stays 'false' and no 'Rail kind' menu node exists.
    Then run the full suite and the type check.
  </action>
  <verify>
    <automated>pnpm exec vitest run && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>The four new tests pass (action-row order, + Rail gating, Solo-in-pill, menu-not-open-when-disabled); the full vitest suite is green; tsc --noEmit is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action row → rail-creation flow | The + Rail button now gates its Motion/Static/Reveal menu behind the + Key availability law; a disabled button never opens the menu, so the onCreatePlayScriptRail/onCreateRevealRail ports are only reachable from an eligible frame state. |
| playback pill → solo arm | The relocated Solo button is a session-only arm toggle (physicsPaintSoloArm.ts); it never mutates a document, never starts/stops transport, and its disarm of the Push tool is the same D-20 toolbar disarm it provided from the action row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d1w-01 | Spoofing | + Rail availability gating | low | mitigate | + Rail reuses the verbatim canAddRotoKey/addRotoKeyDisabledReason ports (the same reactive authority as + Key); the view never re-derives or shortens the reason, and the menu is unreachable while aria-disabled (onClick guard + Enter/Space preventDefault). |
| T-d1w-02 | Tampering | Solo relocated disarm | low | mitigate | disarmPushTool() is called first, unconditionally, in the relocated Solo onClick — preserving the D-20 toolbar disarm the action-row capture guard previously provided; the capture guard still covers every remaining in-row button. |
| T-d1w-03 | Denial of Service | aria-disabled + styled tooltip on a nav-button | low | accept | The aria-disabled state dims via the new opacity rule mirroring the nav-button :disabled treatment; no new listeners, no focus trap, no behavior change. |
| T-d1w-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan — no package-legitimacy gate required. |
</threat_model>

<verification>
- `pnpm exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts app/src/components/physic-paint/view/PhysicsPaintWorkflowStripRailCreate.test.tsx.test.ts` — Task 1 (reorder + gating + harness).
- `pnpm exec vitest run app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts` — Task 2 (Solo relocation + updated armed-tint test).
- `pnpm exec vitest run && pnpm exec tsc --noEmit` — Task 3 (new tests + full suite + type check).
- Native UAT (user drives): + Rail sits immediately right of + Key and Push immediately right of + Rail; on a frame where + Key is greyed, + Rail is greyed too with an explanatory tooltip and the menu does not open; Solo appears icon-only beside Loop and arms/disarms exactly as before; Push drags and disarms exactly as before and clicking Solo still leaves push mode; Undo/redo, arrows, Space, and Delete shortcuts still work.
</verification>

<success_criteria>
- The action row renders + Key, + Rail, Push, Insert, Copy, Cut, Scissor, Paste, All, Trash in that order.
- + Rail mirrors + Key's availability law across the same unavailable states (busy, not ready, canAddEmptyKey false) with the reason in its tooltip copy; the menu never opens while aria-disabled.
- Solo renders icon-only inside the playback pill immediately after the Loop toggle with the pill .active armed treatment, aria-pressed, the guarded styled tooltip, and soloToolDisabled/Reason gating.
- Clicking Solo still disarms an armed Push tool (D-20 preserved explicitly).
- No changes to rail-creation flows, push drag mechanics, solo playback semantics, keyboard shortcuts, or the timeline; no new window-level listeners; no useState additions.
- The whole app test suite stays green and tsc --noEmit is clean (ACC-01).
</success_criteria>

<output>
Create `.planning/quick/260905-d1w-workflow-strip-action-row-layout-rail-be/260905-d1w-SUMMARY.md` when done
</output>
