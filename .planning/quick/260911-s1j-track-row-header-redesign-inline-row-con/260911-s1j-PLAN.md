---
phase: quick-260911-s1j
plan: 260911-s1j
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
  - app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
  - app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.test.ts
autonomous: true
requirements: [TML-01, TML-04]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Every Paint track row exposes its controls inline and always visible, in the mockup order: grab grip, eye, solo S, frame-blending, then the track name, with the trash pinned far right. The ⋯ expander, its tools panel, and the duplicate button no longer render."
    - "The inline S button IS the row-level solo indicator: its armed state reads the document `solo` flag (orange when armed) and the 260911-sli standing badge is gone — no duplicate indicators."
    - "Armed solo and enabled frame blending both paint orange (#f59e0b border + rgba(245,158,11,0.2) fill + #fbbf24 glyph — the established armed-control family), unmistakable at rest."
    - "The Tools popover's Interpolation section holds ONLY the mode dropdown; the Blend on/off toggle is gone (the row button owns on/off per track)."
    - "The mode dropdown is document-level: choosing a mode writes it to EVERY track (each track's own enabled flag preserved) via the same direct store write the row blend button uses — not an active-track-scoped coordinator write. Verified 2026-09-11: the old path committed through the coordinator's `getActiveTrackId` (useRotoPhysicalEditCoordinator.ts:2174), active-track only."
    - "Hide/solo truth table and solo semantics are untouched: S still toggles `track.solo` via setTrackSolo; the session playback arm stays exclusive to the playback pill."
  artifacts:
    - app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx — header renders grip/eye/S/blend + right-pushed label + pinned trash; no tools panel, no ⋯ toggle, no badge
    - app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx — drops toolsOpenTrackId/onToggleTools/onCloseTools; keeps passing solo/layerId/onToggleBlend/onDuplicateTrack
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx — toolsOpenTrackId state + handlers removed; Tools popover loses the Blend toggle + badge/aria interpolation mentions
    - app/src/components/physic-paint/PhysicsPaintStudio.tsx — handleRotoInterpolationModeChange loops every document track through setRotoPhysicalInterpolationState; handleRotoInterpolationEnabledChange + useRotoInterpolationController usage removed
    - app/src/components/physic-paint/physicsPaintStudio.css — inline-controls row layout, orange armed states, retired panel/badge/toggle rules
  key_links:
    - "S click → strip `handleToggleSolo` (PhysicsPaintWorkflowStrip.tsx:1635) → Studio `handleToggleSolo` → `setTrackSolo` (efxPaintStore.ts:366) — the document flag `participatingPaintTracks` filters the composite on. Unchanged."
    - "Blend click → `handleToggleBlend` (PhysicsPaintStudio.tsx:3715) direct store write `setRotoPhysicalInterpolationState(layerId, trackId, {enabled: !current.enabled, mode: current.mode})` — the pattern the new document-level mode write mirrors."
    - "Mode change → new loop: for each `document.tracks` track, `setRotoPhysicalInterpolationState(layerId, track.id, { enabled: current.enabled, mode })`. Generated-frame caches key on the mode (physicPaintStore.ts:3609/3670), so each track re-renders; track/revision bumps ride the existing push path."
    - "Trash stays on the delete flow: `onDeleteTrack` → strip `handleRequestDeleteTrack` → acknowledge dialog. The duplicate path (onDuplicateTrack → handleDuplicateTrack → duplicateTrack) stays wired but has no button (retained for later re-exposure, per the user)."
---

<objective>
Redesign the track-row header to the approved mockup (user-attached screenshot, 2026-09-11 — the layout authority) and finish the two control-surface corrections it implies.

Mockup, measured against the real 185×30px row (2.03× image scale): buttons 20×18 (S chip 18×18, unchanged sizes), 5px gaps, order [grip 12px][eye][S][blend] left-packed; name right-pushed with ~8px right padding + the 5px row gap before the trash; trash flush at the far right (~20×18, same button family); the active track keeps its blue left border. No ⋯, no duplicate, no badge.

Four intents (user's work order, 2026-09-11):
1. Remove the ⋯ expander from track rows entirely — controls always visible inline, packed tight, in the mockup order with the trash pinned far right.
2. Remove the DUPLICATE button from the row UI — UI removal only, the duplication code path stays intact (re-exposable later).
3. Remove the Blend on/off toggle from the Tools popover — the row button owns on/off per track. The mode dropdown STAYS; it is the document-level mode choice (user clarification).
4. Toggle states unmistakable: armed solo = orange; frame blending on = orange. The inline S IS the row-level solo indicator — the 260911-sli standing badge folds into it.

Amends quick 260911-sli (its badge surface): the sli chip/badge binding contract is rewritten to the inline surface; the sli UAT rows fold into this quick's combined native pass (the armed indicator is now the orange S).

Verification carried by this quick (user-requested): the mode dropdown must write the DOCUMENT-LEVEL mode consumed by every track — not an active-track-scoped write. Verified pre-plan: it IS active-track-scoped today (read at PhysicsPaintStudio.tsx:674 via `studioActiveTrackId()`; write through `useRotoInterpolationController` → coordinator payload `trackId: getActiveTrackId(...)` at useRotoPhysicalEditCoordinator.ts:2174; the store map and bridge payload are keyed `(layerId, trackId)`). This quick fixes it to every-track write.

Out of scope, do not touch: `physicsPaintSoloArm.ts`, the playback pill, `participatingPaintTracks` / `resolvePhysicPaintTrackVisibility` (hide/solo truth table), the compositor, `duplicateTrack`/`handleDuplicateTrack` (kept intact), the Key Spacing section, `physicsPaintToolboxPopover` dismissal machinery, the ruler/playback pills.

Native UAT (user-run, one combined pass) — see <human_verification>: the 4 pending 260911-sli rows (amended: the armed indicator is the orange S), the folded 260911-g1g 3-track hide/solo scenario, and the new checks (no ⋯, no duplicate, no Blend toggle in Tools, orange armed states, layout matches the mockup, mode applies to all tracks). The executor leaves it explicitly pending and must not claim native UAT passed.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx
@app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/physicsPaintStudio.css

Current code facts (verified 2026-09-11):
- `PhysicsPaintTrackRowHeader` (PhysicsPaintTrackRow.tsx:784-1055): standing controls grip/eye/blend + label + badge; the ⋯ button `.physics-paint-track-row-tools-toggle` (1038-1050) opens `.physics-paint-track-row-tools` (990-1037: S, duplicate, trash); `toolsOpen` drives `data-tools-open` (868) and the CSS panel takeover (physicsPaintStudio.css:4889-4913).
- The S chip (998-1010) carries `physics-paint-track-row-solo` + `-armed` (blue family, css:4783-4820); the badge (977-983) carries `physics-paint-track-row-solo-badge` (css:4822-4839); blend armed `physics-paint-track-row-blend-enabled` (css:4841-4852) — all three go orange per intent 4.
- Strip state: `toolsOpenTrackId` (PhysicsPaintWorkflowStrip.tsx:1556), `handleToggleTrackTools` (1626), `handleCloseTrackTools` (1629), passed at 4254-4256; the header column forwards them (physicsPaintTrackHeaderColumn.tsx:153-155, 204-206).
- Tools popover (PhysicsPaintWorkflowStrip.tsx:1218-1227): section "Interpolation" = Blend toggle (1223, guarded `if (props.mutationLocked || props.interpolationPending) return;`) + mode select (1224). Tools button badge + aria at 1200/1208-1211; tooltip copy at 1215.
- Studio: `handleRotoInterpolationEnabledChange` (2950) / `handleRotoInterpolationModeChange` (2953) via `useRotoInterpolationController` (2934); wired at 4314. `handleToggleBlend` (3715-3724) is the mirror write shape for the new mode loop. `getEfxPaintDocument` already imported.
- `.physics-paint-track-row-label` (css:4715) is `flex: 1 1 auto` (left-aligned) — the mockup wants it right-pushed; `.physics-paint-track-row-label-ellipsis` (css:4773) is paint-row-only, safe to extend.

Tests to rewrite (pins on the retired surface):
- physicsPaintTrackHeaderColumn.test.ts: harness props `toolsOpenTrackId`/`onToggleTools` (158-182); TML-04 case (630-677: chip/badge/aria `(solo armed)`); Bg locked-row case (727).
- PhysicsPaintWorkflowStrip.viewport.test.ts: 752-758 (`data-tools-open`, more-button present) and 769-777 (Bg row has none).
- PhysicsPaintWorkflowStrip.test.ts: 182-202 (interpolation controls + guard regex), 682-701 (Blend toggle block), ~715-745 (toggle CSS pins + guard-count test), 1044-1094 (top-bar order/toolbox section pins).
- PhysicsPaintStudio.test.ts: 1074-1094 (handler list + workflow wiring pins).

Guardrail — working tree state: ~10 files of uncommitted stall instrumentation (physicPaintStore.ts, PhysicsPaintStudio.tsx, physicsPaintPerformanceTrace.ts/.test.ts, frameLru.ts, projectStore.ts, physicPaintBridge.ts, physicPaintBridgeTransport.ts, PhysicsPaintCanvasMount.runtime.test.ts, src-tauri commands/mod.rs + lib.rs + debug_capture.rs) MUST NOT be staged into this quick's commits. NOTE: `PhysicsPaintStudio.tsx` carries BOTH instrumentation edits and this quick's edits — stage it ONLY via explicit `git add -p`-free selective staging is not possible for partial files, so use `git stash push -- <instrumentation files>`? NO — do not stash. Instead: stage the file and VERIFY with `git diff --cached` that only the quick's hunks are staged; if the instrumentation hunks in PhysicsPaintStudio.tsx are unstaged neighbors, use `git add` after checking `git diff` hunks, or commit with the instrumentation hunks left unstaged via `git add -p`-equivalent (interactive not allowed) — so: verify which hunks exist and, if entangled, split the commit so the quick's hunks are staged explicitly with `git apply --cached` of a filtered diff. Simplest safe route (proven in 260911-sli): the instrumentation in PhysicsPaintStudio.tsx was never staged; check `git diff --stat` first and prefer hunks that touch only this quick's lines.

Command conventions (proven in this repo):
- Targeted: `pnpm --filter efx-motion-editor exec vitest run <path>`
- Full suite: `pnpm --filter efx-motion-editor exec vitest run` (long — run in the background, resume only on completion)
- Types: `pnpm --filter efx-motion-editor exec tsc --noEmit`
- Never run vitest in watch mode (project law). Signals only, no useState (project law).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inline the row controls — retire the ⋯ panel, drop the duplicate button, fold the badge into the orange S (RED → GREEN)</name>
  <files>app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts, app/src/components/physic-paint/view/PhysicsPaintTrackRow.tsx, app/src/components/physic-paint/view/physicsPaintTrackHeaderColumn.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <behavior>
    Rewrite the pins to the new inline surface:
    - physicsPaintTrackHeaderColumn.test.ts: harness loses `toolsOpenTrackId`/`onToggleTools`; TML-04 case keeps doc-flag chip binding but drops the badge assertions and the `(solo armed)` aria-label suffix; add: no `.physics-paint-track-row-tools`, no `.physics-paint-track-row-tools-toggle`, no `.physics-paint-track-row-solo-badge` in any Paint header; the S click still routes `onToggleSolo(trackId)`; eye routing preserved; session-arm regression case preserved (arming `toggleSolo()` never touches the row).
    - PhysicsPaintWorkflowStrip.viewport.test.ts: replace the more-button/`data-tools-open` pins with: no `.physics-paint-track-row-tools-toggle` and no `[data-tools-open]` on any paint header; Bg row unchanged (none).
    Pre-fix these MUST fail (the ⋯ toggle still renders) — capture the failing run as the RED proof.
  </behavior>
  <action>
    RED first: rewrite the two test files as specified, run both targeted, record the failure. Then implement:
    - `PhysicsPaintTrackRow.tsx` — `PhysicsPaintTrackRowHeader`: remove the ⋯ button, the `.physics-paint-track-row-tools` panel, the duplicate button, the `toolsOpen`/`onToggleTools`/`onCloseTools` props + `data-tools-open` + `onPointerLeave` close, and the badge span. New render order inside the standing row: grip (reorderable) → eye → S chip (`physics-paint-track-row-solo` + armed class bound to `solo`) → blend (unchanged, `physics-paint-track-row-blend-enabled` when enabled) → label (unchanged classes/title/rename) → trash button (`physics-paint-track-row-tool-button`, `deletable` guard + disabled state kept, onClick `onDeleteTrack`), pinned far right by the label's push. Keep `onDuplicateTrack` in the props interface (retained, not destructured-for-render). Header `aria-label` becomes `Select track ${label}` (no solo suffix — the S carries aria-pressed).
    - `physicsPaintTrackHeaderColumn.tsx` — drop `toolsOpenTrackId`/`onToggleTools`/`onCloseTools` props + pass-through; keep `solo`, `layerId`, `onToggleBlend`, `onDuplicateTrack` (retained), `onRequestDeleteTrack`.
    - `physicsPaintStudio.css` — delete `.physics-paint-track-row-tools` (4866-4877), the `[data-tools-open]` rules (4879-4913), `.physics-paint-track-row-tools-toggle*` (4915-4949), `.physics-paint-track-row-solo-badge` (4822-4839). Recolor `.physics-paint-track-row-solo-armed` and `.physics-paint-track-row-blend-enabled` to the armed orange family (`border-color: #f59e0b; background: rgba(245, 158, 11, 0.2); color: #fbbf24;` with the matching hover). Push the label right: extend `.physics-paint-track-row-label-ellipsis` with `margin-left: auto` and bump its `padding-right` to 8px (mockup: name→trash gap ≈ 8px + the row's 5px gap).
    Commit: `test(260911-s1j): pin the inline row controls and the retired tools panel (RED)` then `fix(260911-s1j): inline row controls — grip/eye/S/blend, right-pushed name, pinned trash` then `style(260911-s1j): orange armed states; retire panel/badge/toggle styles`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/physicsPaintTrackHeaderColumn.test.ts src/components/physic-paint/view/PhysicsPaintWorkflowStrip.viewport.test.ts</automated>
  </verify>
  <done>Paint headers render exactly [grip][eye][S][blend] + right-pushed name + pinned trash; no ⋯, no tools panel, no duplicate button, no badge anywhere; armed S and enabled blend both paint orange; eye/solo/trash routing and rename preserved; both targeted files green; RED recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tools popover loses the Blend toggle; the mode dropdown becomes document-level (RED → GREEN)</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintStudio.tsx</files>
  <behavior>
    Rewrite the pins:
    - PhysicsPaintWorkflowStrip.test.ts: the Interpolation section holds no `physics-paint-roto-interpolation-toggle` button and no click-guard `if (props.mutationLocked || props.interpolationPending) return;`; the mode select (`aria-label="Interpolation mode"`, options Frame duplicate / Frame blending) stays; the Tools button carries no `physics-paint-toolbox-badge` and its aria-label is plain "Timeline tools"; the toggle CSS pins for `.physics-paint-roto-interpolation-toggle` are removed; the toolbox-section/top-bar order pins stay.
    - PhysicsPaintStudio.test.ts: the stable-handler list keeps `handleRotoInterpolationModeChange`, drops `handleRotoInterpolationEnabledChange`; the workflow wiring pin becomes `onRotoInterpolationModeChange: handleRotoInterpolationModeChange` and asserts NO `onRotoInterpolationEnabledChange` wiring remains.
    Pre-fix these MUST fail — capture the failing run as the RED proof.
  </behavior>
  <action>
    RED first: rewrite the two test files, run both targeted, record the failure. Then implement:
    - `PhysicsPaintWorkflowStrip.tsx` — remove the Blend toggle button (1223) and its click guard; drop `onInterpolationEnabledChange` from the static-chrome props + the strip pass-through (4194) + its interface entry; remove the Tools badge span (1208-1211) and its `interpolationEnabled` consumer in the button (aria-label → `'Timeline tools'`); update the tooltip copy (1215) to drop "Interpolation and" (→ 'Open timeline tools — Key Spacing.'); remove the now-unused `interpolationTooltip` usage tied to the removed toggle only if it has no other consumer (the pill keeps its tooltip — keep the hook). Keep the pill's mode select, `data-enabled`/`data-pending` attrs, and `interpolationControlsDisabled` on the select.
    - `PhysicsPaintStudio.tsx` — replace `handleRotoInterpolationModeChange` with the document-level loop: guard `physicalEditCoordinator.pendingOperationId.value !== null` (same as `handleToggleBlend`), read `getEfxPaintDocument(launchContext.layerId)`, and for each track apply `physicPaintStore.setRotoPhysicalInterpolationState(layerId, track.id, { enabled: current.enabled, mode })` when the mode differs. Remove `handleRotoInterpolationEnabledChange`, the `updateRotoInterpolationSettingsRef`, the `useRotoInterpolationController` call + import (now unused — the hook file stays on disk, unwired), and the `onRotoInterpolationEnabledChange` wiring (4314). Keep `onRotoInterpolationModeChange: handleRotoInterpolationModeChange`.
    Commit: `test(260911-s1j): pin the Tools popover without the Blend toggle and the document-level mode write (RED)` then `fix(260911-s1j): drop the Blend toggle from Tools; the mode dropdown writes every track`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
  </verify>
  <done>The Tools popover Interpolation section holds only the mode dropdown; the Tools button has no interpolation badge/aria; the mode change writes every document track's physical interpolation state (enabled preserved per track) in one handler; no `onRotoInterpolationEnabledChange` remains in the Studio or strip; both targeted files green; RED recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Full gates + SUMMARY + STATE</name>
  <files>.planning/quick/260911-s1j-track-row-header-redesign-inline-row-con/260911-s1j-SUMMARY.md, .planning/STATE.md</files>
  <action>
    Run the full app suite (`pnpm --filter efx-motion-editor exec vitest run`, background, resume only on completion — pipe-free, redirect to a log and echo the exit code) and `pnpm --filter efx-motion-editor exec tsc --noEmit`. Investigate any red rather than papering over (the DOC-04 clean-break contract scans source text — a retired token or a new literal must not trip it). Scope-gate every commit: `git status --short` must show only this quick's files staged and the ~10 instrumentation files still unstaged/untouched. Write the SUMMARY (status: complete; native UAT pending) with the RED proofs, gate results, the mockup-mapped layout table, and the document-level-mode verification record; update STATE.md (Last activity + quick table row). Commit: `docs(quick-260911-s1j): Track-row header redesign — inline controls, Tools popover Blend toggle retired, document-level mode`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
  </verify>
  <done>Full suite 0 failed, tsc clean, SUMMARY + STATE committed, native UAT left explicitly pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational change plus an in-memory document-mode write; no network, no I/O, no new runtime surface. The integrity surface is that the row's orange states reflect the same flags the compositor/interpolation consume, and that the mode write applies to every track. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260911-s1j-01 | Integrity | S chip vs session arm (re-conflation) | medium | mitigate | The rewritten TML-04 contract keeps the session-arm regression case: arming `toggleSolo()` never arms the row chip. `physicsPaintSoloArm.ts` is untouched. |
| T-260911-s1j-02 | Integrity | Mode write scope (active-track vs document) | high | mitigate | The mode handler loops `document.tracks` through the same store op the row blend button uses; the Studio test pins the absence of the old wiring, and native UAT checks a 2-track document (mode change visible on both tracks). |
| T-260911-s1j-03 | Tampering | Scope discipline vs uncommitted instrumentation | medium | mitigate | Stage only this quick's files; verify `git diff --cached` per commit; the PhysicsPaintStudio.tsx edit is entangled with instrumentation hunks — stage only this quick's hunks. Full-suite + tsc as backstop. |
| T-260911-s1j-SC | Tampering | npm/pnpm installs | low | accept | No dependency changes — no package-legitimacy gate required. |
</threat_model>

<verification>
- Targeted gates (Task 1/2): the four pinned test files green, with RED proofs recorded.
- Full suite: `pnpm --filter efx-motion-editor exec vitest run` — 0 failed (background; resume only on completion).
- Types: `pnpm --filter efx-motion-editor exec tsc --noEmit` — clean.
- Scope gate: `git status --short` after each commit — only this quick's files (plus GSD artifacts); the ~10 uncommitted instrumentation files remain unstaged and unmodified by this quick.
- Law check (by reading the diff): no `physicsPaintSoloArm` import in the view files; S still routes `onToggleSolo`; `participatingPaintTracks` / `resolvePhysicPaintTrackVisibility` untouched; no compositor change; no `useState` added; the mode handler touches every `document.tracks` track and preserves each track's `enabled`.
</verification>

<human_verification>
Native UAT — OWED BY THE USER after execution; the executor must leave it pending, never claim it passed. ONE combined pass, superseding the 260911-sli rows (the armed indicator is now the orange S).

Amended 260911-sli rows:
1. Two paint tracks, both holding paint: the composite shows both.
2. Arm solo on the top track via its inline S (always visible now — no panel to open). Expected: S turns ORANGE immediately on the row; the lower track leaves the preview AND playback (locked truth table); un-solo restores it everywhere (row + both surfaces).
3. Reopen the project with solo armed (persisted): the orange S is still visible on the row — report whether solo should keep persisting across reopen (current behavior) or not (separate decision).
4. Folded 260911-g1g 3-track scenario: Track A visible, Track B hidden, Track C hidden+soloed → A renders normally in Studio, canvas composite, and an exported frame; C never appears. Then solo A: only A renders; unhide B: still only A; C still never appears (hide wins). Studio vs composite/export match at every step (WYSIWYG).

New checks (this quick):
5. Layout matches the mockup: every row shows [grip][eye][S][blend] tight-packed, the name right-pushed, the trash pinned far right; the active row keeps its blue left border.
6. No ⋯ button anywhere; no duplicate (copy) button anywhere; row controls never hide.
7. Tools popover: the Interpolation section holds ONLY the Frame duplicate / Frame blending dropdown — no Blend on/off toggle; the Tools button carries no interpolation badge.
8. Orange states read at a glance: armed solo = orange S; frame blending on = orange blend button; both off = neutral gray.
9. Document-level mode: with two tracks, set the mode in Tools — BOTH tracks' rows/playback follow (toggle one track's blending off/on around the change to confirm the mode sticks per document, not per active track).
10. Trash click still opens the acknowledge-and-delete flow; the last remaining track's trash stays disabled.
</human_verification>

<success_criteria>
- Rows render the mockup layout inline; ⋯/panel/duplicate/badge all gone; S and blend show unmistakable orange armed states.
- S still toggles the document solo flag; the session arm stays pill-exclusive; hide/solo truth table untouched.
- The Tools popover holds only the mode dropdown in its Interpolation section; the mode write hits every track (document-level) and is pinned by the Studio test.
- Full suite green (never watch mode) + `tsc --noEmit` clean; only this quick's files committed; instrumentation untouched.
- Native UAT pending on the user: the combined rows above.
</success_criteria>

<output>
Create `.planning/quick/260911-s1j-track-row-header-redesign-inline-row-con/260911-s1j-SUMMARY.md` when done — include both RED proofs, post-fix targeted + full-suite results, the layout mapping table, the document-level-mode verification record, and mark native UAT as pending.
</output>
