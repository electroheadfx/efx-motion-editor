---
phase: quick-260923-i17
plan: 260923-i17
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src-tauri/src/lib.rs
autonomous: true
requirements: []
estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Studio open passes the main window's current screen geometry (outer origin + inner size), read at open time, to the native open command — nothing cached or persisted between opens."
    - "The native open command places and sizes the Studio window on exactly that geometry on both the fresh-create and window-reuse paths, with no centred placement anywhere in the command."
    - "A failed main-window geometry read refuses to open (no native invoke, explicit error naming the main window geometry) instead of opening on unrelated geometry."
    - "The main window's existing hide-on-open / show-on-close lifecycle and the main window's own geometry are untouched by this change."
    - "Multi-monitor origins (including negative x/y for displays left of or above the primary) pass through verbatim — the Studio takes the main window's screen; nothing re-centres; a second open mirrors again from a fresh read (no drift)."
  artifacts:
    - app/src/lib/physicPaintBridge.test.ts
    - app/src/lib/physicPaintBridge.ts
    - app/src-tauri/src/lib.rs
  key_links:
    - "invoke('open_physics_paint_window', { context, bounds }) payload ↔ Rust `bounds: StudioWindowBounds` command parameter"
    - "Window.getByLabel('main') outerPosition + innerSize read ↔ tryOpenTauriPhysicPaintWindow immediately before the native invoke"
    - "bounds application (builder .position/.inner_size on create + set_position/set_size after show) ↔ BOTH the fresh-create arm and the reuse arm of open_physics_paint_window"
---

<objective>
Open the Studio window on the main window's screen geometry: on every open, the Studio takes over the main window's current origin + size, as if it replaces it on screen; on close the main window is back with its own geometry unchanged.

Purpose: today the Studio open path builds an unrelated 1280×900 window and then re-centres it on every launch (fresh arm `.center()` at `app/src-tauri/src/lib.rs:203`, unconditional `window.center()` after show at `:258`) — size and position have nothing to do with the EFX Motion main window. The quick's guardrails: mirror the main window each open (NO persisted/restored Studio geometry), do NOT hide or move the main window beyond what the existing Studio open path already does, multi-monitor copies the main window's screen with NO re-centring, and repeated open/close must not drift (open twice = same mirror).

Output: a RED-first pin (failing vitest legs committed before any production edit) proving the native open command is handed bounds equal to the main window's bounds at open time, the GREEN wiring (bridge reads main-window geometry → passes `bounds` → Rust applies it as the Studio window's position and size on both arms, centred placement removed), and automated-ready status — native UAT (4 rows) remains the user's after GREEN.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Grounding notes (verified at planning time — trust these, do not re-derive):

- Open seam (renderer): `app/src/lib/physicPaintBridge.ts` — `openPhysicPaintCanvas` (:3762) → `tryOpenTauriPhysicPaintWindow` (:3845) → `core.invoke('open_physics_paint_window', { context: toTransportPayload(context) })` (:3853). Sole production caller: `app/src/components/sidebar/PhysicPaintProperties.tsx:78` (main realm). No other file invokes the command (only `physicPaintBridge.test.ts:1523` asserts its payload).
- Open seam (Rust): `app/src-tauri/src/lib.rs` `open_physics_paint_window` (:161). Fresh-create arm: `WebviewWindowBuilder` with `.inner_size(1280.0, 900.0)` (:198) + `.center()` (:203). Reuse arm (:176) only navigates the existing window. After show: unconditional `window.center()` (:258) — runs on EVERY open. Main window is hidden at :263-265 and restored (show + focus) on the Studio's Destroyed event at :225-228. The ONLY two `center()` occurrences in lib.rs are :203 and :258, both inside this command; `set_position`, `set_size`, `PhysicalPosition`, `PhysicalSize` appear NOWHERE in lib.rs at base — the structural pins are RED at base.
- Installed Tauri API (`app/node_modules/@tauri-apps/api/window.d.ts`): `Window.getByLabel(label): Promise<Window | null>` (:228), `outerPosition(): Promise<PhysicalPosition>` (:347), `innerSize(): Promise<PhysicalSize>` (:359). The local `TauriWindowApi` interface at `physicPaintBridge.ts:176` must gain `outerPosition`/`innerSize` on the getByLabel result (cast style already used at :3100).
- Bounds semantics (Claude's discretion): `bounds = { x, y, width, height }` where x/y come from `outerPosition()` (physical px, may be negative for displays left of/above the primary) and width/height come from `innerSize()` (physical px). Outer origin + inner size = EXACT outer overlay, because main and Studio are both standard decorated windows with identical frame insets; physical units make mixed-DPI monitors correct. Main window config (`tauri.conf.json`): label `main`, 1440×900, minWidth 1024, minHeight 680 — always ≥ the Studio's `min_inner_size(960, 640)`, so the mirror can never be clamped by the minimum.
- Existing exact-payload assertion `physicPaintBridge.test.ts:1523` (`toHaveBeenCalledWith('open_physics_paint_window', { context: ... })`) breaks once `bounds` joins the payload — update it in GREEN with mock geometry matching that test's own window mock.
- Exactly THREE tests in `physicPaintBridge.test.ts` take the Tauri branch (doMock `@tauri-apps/api/core` with `isTauri: () => true`): "does not fall back to browser open…" (:1478), "opens Tauri physics paint through the native command" (:1513), "reports native launch failure…" (:1547). Each needs a `vi.doMock('@tauri-apps/api/window', …)` added alongside its core doMock in GREEN. The doMock block at :3676-3685 belongs to an `it.skip` — leave it. `physicPaintBridge.launchPackBytes.test.ts` mocks core WITHOUT `isTauri` and carries no `__TAURI*` window marker → browser-fallback branch (detectTauriRuntime at :3916 checks `core.isTauri()` then window markers) → unaffected, do not touch. `physicPaintStore.rotoLoopClips.test.ts` has no Tauri mocks → browser fallback → unaffected. All bounds reading lives INSIDE `tryOpenTauriPhysicPaintWindow`, so the browser branch never touches it.
- Harness to crib (Tauri-branch pattern): test at `physicPaintBridge.test.ts:1507-1530` — `Object.defineProperty(globalThis, 'window', …)` + `vi.doMock('@tauri-apps/api/core', () => ({ isTauri: () => true, invoke }))` + `await import('./physicPaintBridge')` + `registerDocument(makeTrackDocument('phys-layer-1'))` + `nativeSequenceStore.sequences.value = sequenceStore.sequences.peek()` + `openCanvas({ layer: physicLayer(), frame: 4 })`. File-scope fixtures: `physicLayer` (:299), `makeTrackDocument`, `TEST_TRACK_ID`. The file already has several top-level `describe`s (e.g. :3869) — append a new one at file end.
- Rust structural legs read `src-tauri/src/lib.rs` from the test via `fileURLToPath(new URL('../src-tauri/src/lib.rs', import.meta.url))` (test lives in `app/src/lib/`), region = `source.slice(source.indexOf('async fn open_physics_paint_window'), source.indexOf('fn physics_paint_url'))` — both anchors exist (:161, :308).
- Tests: vitest collects only `src/**/*.test.ts` (never .tsx); node environment. Run `pnpm --filter efx-motion-editor exec vitest run <files> [-t <pattern>]` (never watch). Types: `pnpm --filter efx-motion-editor run typecheck` (script = `tsc --noEmit`). Rust: `cd app/src-tauri && cargo check` / `cargo test` (ovi + bcm precedent). Do NOT start the dev server (CLAUDE.md).
- No chunk-budget change: `@tauri-apps/api/window` is already dynamically imported at `physicPaintBridge.ts:3100`; the new dynamic import adds no new chunk content worth a budget raise.
- RED evidence precedent: `.planning/quick/260923-fhn-…/260923-fhn-RED-EVIDENCE.json` + `gsd_run check tdd-red-evidence <record.json>` → `RED_EVIDENCE_OK`. Commit scopes for quicks: `test(quick-260923-i17): …` then `feat(quick-260923-i17): …` (fhn: `test(quick-260923-fhn): …` f335be71 → `feat(…)` cfa928f2).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin that the Studio is opened with the main window's bounds at open time</name>
  <files>app/src/lib/physicPaintBridge.test.ts</files>
  <behavior>
    - Test 1 (control, passes at base AND after GREEN): the Tauri-branch open still calls `open_physics_paint_window` with a `context` carrying `layerId: 'phys-layer-1'`, `startFrame: 4` — assert by inspecting the matching call's args (do NOT use whole-object `toHaveBeenCalledWith`, which must be allowed to gain the `bounds` key at GREEN).
    - Test 2 (RED at base): with the window API reporting main-window geometry `{ x: 111, y: 222, width: 1440, height: 900 }` (outerPosition + innerSize), `openPhysicPaintCanvas` invokes `open_physics_paint_window` with `bounds` deep-equal to exactly `{ x: 111, y: 222, width: 1440, height: 900 }` — the bounds-passing seam pin (fails at base: payload has no `bounds`).
    - Test 3 (RED at base): a second open whose window API now reports `{ x: -1280, y: 64, width: 1024, height: 680 }` (a display left of the primary → negative x) carries those NEW values in `bounds` — fresh read per open, negative origins pass through (fails at base: no `bounds` key).
    - Test 4 (RED at base): when `Window.getByLabel('main')` resolves null, the open REFUSES — result `ok: false` with an error containing `main window geometry`, `invoke` never called, `window.open` never called (fails at base: invoke IS called and the open succeeds).
    - Test 5 (RED at base): when `innerSize()` reports a non-finite width (NaN), the open refuses with the same `main window geometry` error and no invoke (fails at base: same reason).
    - Test 6 (RED at base, Rust source-shape): the `open_physics_paint_window` region of `src-tauri/src/lib.rs` contains the `StudioWindowBounds` parameter type, `set_position`, and `set_size` (fails at base: none of the three tokens exist anywhere in lib.rs).
    - Test 7 (RED at base, Rust source-shape): the same region performs NO centred placement — `expect(region).not.toMatch(/center\s*\(/)` (fails at base: both the builder call and the post-show call match).
    - Expected at base: `6 failed | 1 passed (7)` — every failure an AssertionError on the planned behavior, never module-not-found, fixture crash, or zero-test discovery (INVALID_RED otherwise).
  </behavior>
  <action>Create a new top-level `describe('Studio window opens on the main window geometry (quick 260923-i17)', …)` appended at the end of `app/src/lib/physicPaintBridge.test.ts`, RED first — no production file may be edited in this task. Reuse the file-scope fixtures (`physicLayer`, `makeTrackDocument`, `TEST_TRACK_ID`) and the proven Tauri-branch harness from the test at :1507-1530 (window defineProperty + `vi.doMock('@tauri-apps/api/core', () => ({ isTauri: () => true, invoke }))` + a sibling `vi.doMock('@tauri-apps/api/window', …)` exposing `Window.getByLabel` that resolves a main-window double with `outerPosition()`/`innerSize()` for label `'main'` and null otherwise + dynamic re-import of the bridge + `registerFreshDocument(makeTrackDocument('phys-layer-1'))` + the sequences copy) for the seven legs described in <behavior>. A small closure helper that registers the window doMock from a mutable `reported` bounds object keeps legs 2/3 readable and lets leg 3 re-report geometry between calls. For the two Rust source-shape legs, read the file once per test with `readFileSync(fileURLToPath(new URL('../src-tauri/src/lib.rs', import.meta.url)), 'utf8')`, slice the region between `async fn open_physics_paint_window` and `fn physics_paint_url`, first asserting both anchors were found (a missing anchor must fail as an assertion, not a crash), then assert the tokens on that region — keep the no-centred-placement assertion INSIDE the test code (the plan's own `<verify>` blocks contain no negative greps, so comment-text discipline does not apply). Run the targeted command, capture the RED output verbatim (`6 failed | 1 passed` with the named assertion failures), persist `.planning/quick/260923-i17-quick-5-studio-window-opens-on-the-main-/260923-i17-RED-EVIDENCE.json` in the fhn record shape (task, planBase, command, exitCode, targetTest, expected, actual, output), verify it with `gsd_run check tdd-red-evidence` until the verdict is `RED_EVIDENCE_OK`, and commit ONLY the test file + evidence record as `test(quick-260923-i17): RED pin — Studio window opens with the main window's bounds`.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts -t "main window geometry"</automated>
  </verify>
  <done>Committed RED state: the targeted run reports 6 failed | 1 passed (7) — Tests 2-7 fail on the planned assertions (missing `bounds` payload key, missing Rust tokens, centred placement present, fail-open instead of refuse) while the control passes; `gsd_run check tdd-red-evidence` verdict is `RED_EVIDENCE_OK`; zero production files touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — read the main window geometry at open and apply it as the Studio window's position and size</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts, app/src-tauri/src/lib.rs</files>
  <behavior>
    - Behaviour: the invoke payload for `open_physics_paint_window` is `{ context, bounds }` where `bounds` deep-equals the geometry the window API reported at that open (legs 2/3 green; negative x passes through).
    - Behaviour: geometry read failure (null main window or non-finite/degenerate size) returns `ok: false` with an error containing `main window geometry`, without invoking the native command and without falling back to `window.open` (legs 4/5 green).
    - Behaviour (Rust): the command declares the `bounds` parameter and applies it with `set_position` + `set_size` after show (covering the reuse arm) and with builder placement on the fresh-create arm; the command region contains no centred placement (legs 6/7 green).
    - Behaviour: the three pre-existing Tauri-branch tests stay green with a window doMock added, and the :1523 payload assertion is updated to the exact `{ context, bounds }` shape using that test's own mock geometry; the Task 1 control leg stays green unchanged.
    - Non-regression: browser-fallback opens, launch URL/context transport, main-window hide-on-open / show-on-close, occlusion monitor, display-sleep guard, title, min sizes, and the close/apply paths are byte-untouched.
  </behavior>
  <action>Implement GREEN in this order, then prove each gate. (1) `app/src/lib/physicPaintBridge.ts`: extend the local `TauriWindowApi` interface (:176) so the getByLabel result also exposes `outerPosition?: () => Promise<{ x: number; y: number }>` and `innerSize?: () => Promise<{ width: number; height: number }>`; add a `StudioWindowBounds` type `{ x: number; y: number; width: number; height: number }`; add a `readMainWindowBounds()` helper that dynamically imports `@tauri-apps/api/window`, resolves `Window.getByLabel('main')`, reads `outerPosition()` (→ x, y) and `innerSize()` (→ width, height), returns null unless all four numbers are finite with width/height ≥ 1, catching any throw as null. Wire it inside `tryOpenTauriPhysicPaintWindow` AFTER the existing `if (!core.invoke)` guard and BEFORE the invoke: on null return `{ ok: false, error: 'Could not read the main window geometry for the Studio window mirror' }` (this exact phrase carries the `main window geometry` substring the pins assert); otherwise invoke `open_physics_paint_window` with `{ context: toTransportPayload(context), bounds }`. One short comment at the read site documenting the contract — outer origin + inner size, read fresh at every open, never persisted (the quick's mirror law). (2) `app/src-tauri/src/lib.rs`: add `#[derive(serde::Deserialize)] struct StudioWindowBounds { x: f64, y: f64, width: f64, height: f64 }` next to the launch result struct; add `bounds: StudioWindowBounds` to the `open_physics_paint_window` signature; in the fresh-create builder replace the fixed inner size and the builder's centring call with `.position(tauri::PhysicalPosition::new(bounds.x, bounds.y))` and `.inner_size(tauri::PhysicalSize::new(bounds.width, bounds.height))` (keep title, `min_inner_size(960.0, 640.0)`, resizable, visible, focused, background-throttling, build exactly as-is); after `window.show()` replace the unconditional centring call with `window.set_position(tauri::PhysicalPosition::new(bounds.x, bounds.y))` and `window.set_size(tauri::PhysicalSize::new(bounds.width, bounds.height))`, each with its own `map_err` in the file's existing message style — this post-show application is what mirrors the window on the REUSE arm (the builder only covers fresh creation). Apply every bounds operation to the STUDIO window handle only: the main window keeps exactly its existing lifecycle (hide at :263-265, show+focus on Destroyed at :225-228) — do not add, move, resize, or re-position anything on the main window, and do not persist or restore Studio geometry anywhere (no store signal, no localStorage, no window-state plugin, no new files: read-at-open, apply, forget). One comment citing quick-260923-i17 and the mirror law. (3) `app/src/lib/physicPaintBridge.test.ts`: add a `vi.doMock('@tauri-apps/api/window', …)` with known geometry to each of the three existing Tauri-branch tests (:1478, :1513, :1547) immediately beside its core doMock, and update the :1523 payload assertion to the full `{ context, bounds }` shape using that test's mock values; do not touch the skipped-test mocks at :3676-3685 or any browser-fallback test. (4) Run the targeted gate (Task 1 describe + full `physicPaintBridge.test.ts`), then `cargo check`, then typecheck. Commit as `feat(quick-260923-i17): mirror the main window geometry onto the Studio window on every open`. Do not modify any file outside the three listed.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts && cd app/src-tauri && cargo check && cd ../.. && pnpm --filter efx-motion-editor run typecheck</automated>
  </verify>
  <done>All seven legs of the new describe pass (GREEN), the whole `physicPaintBridge.test.ts` file is green including the three updated Tauri-branch tests, `cargo check` is clean, typecheck is clean, the commit exists after the RED commit, and the diff touches only the three planned files.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression sweep and automated-ready handoff</name>
  <files>app/src/lib/physicPaintBridge.test.ts</files>
  <action>Run the full vitest suite once, the full Rust test suite, and typecheck to confirm no other source-shape pin or behaviour test broke (other suites read the bridge or lib.rs). If an unexpected failure is a pin asserting the OLD unmirrored open shape, update that pin to the mirrored contract; never weaken the Task 1 legs. Do not expand scope, do not start the dev server, and do not claim UAT: label the result automated-ready only — the 4 native UAT rows in <verification> are the user's to run after GREEN. If fixes were needed, commit them; otherwise this task produces no commit.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run && cd app/src-tauri && cargo test && cd ../.. && pnpm --filter efx-motion-editor run typecheck</automated>
  </verify>
  <done>Full vitest suite green, `cargo test` green, typecheck clean, diff confined to the 3 planned files, status reported as automated-ready with native UAT explicitly pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → Rust command args | The `bounds` numbers cross IPC from the main webview into `open_physics_paint_window` — untrusted-shape input (validated, placement-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-i17-01 | Tampering | `bounds` payload on `open_physics_paint_window` | low | mitigate | TS validates all four values finite with width/height ≥ 1 before invoke; Rust deserializes into the fixed `StudioWindowBounds` struct (missing/invalid fields fail the command); values can only position/size the Studio window — never files, paths, or network |
| T-i17-02 | Denial of Service | hostile geometry (NaN, non-finite, degenerate sizes) reaching the window manager | low | mitigate | NaN/non-finite/degenerate bounds are refused TS-side with the `main window geometry` error before any invoke (pinned by tests 4/5); negative x/y remain deliberately allowed (multi-monitor); the OS window manager clamps extremes |
| T-i17-03 | Elevation of Privilege | npm/pip/cargo installs | high | mitigate | No package installs in this plan — package-legitimacy gate not applicable; zero new dependencies; the Tauri window API used is already an installed dependency |
</threat_model>

<verification>
1. `pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts -t "main window geometry"` — all 7 legs green (was 6 failed | 1 passed at RED).
2. `pnpm --filter efx-motion-editor exec vitest run` — full suite green (only the :1523 payload assertion and the three Tauri-branch window mocks updated).
3. `cd app/src-tauri && cargo check && cargo test` — Rust clean, existing suites green.
4. `pnpm --filter efx-motion-editor run typecheck` — clean.
5. Native UAT (USER, after GREEN — plan stops at automated-ready, claims no UAT):
   - Row 1: exact overlay — open Studio from the main window; the Studio window covers exactly the main window's former origin + size.
   - Row 2: second monitor — main window on a secondary display; Studio opens on that same display (same screen, not re-centred onto another).
   - Row 3: close returns the main window, visible, with its own geometry unchanged.
   - Row 4: double open/close — second open mirrors exactly like the first (no drift, no accumulation).
</verification>

<success_criteria>
- RED pin existed (6 failed | 1 passed, RED_EVIDENCE_OK) before any production edit; GREEN makes all 7 legs pass.
- Every open carries freshly read main-window bounds; the native command applies them on both fresh and reuse arms with no centred placement left in the command; failed reads refuse the open.
- Guardrails hold: no persisted Studio geometry, no added main-window hide/move beyond the existing path, multi-monitor copied verbatim, repeated opens do not drift.
- Full suite + cargo + typecheck green; automated-ready only — user's 4 native UAT rows passed afterwards.
</success_criteria>

<output>
Create `.planning/quick/260923-i17-quick-5-studio-window-opens-on-the-main-/260923-i17-SUMMARY.md` when done
</output>
