---
phase: quick-260718-j3h-add-backspace-and-delete-keyboard-shortc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
  - .planning/quick/260718-j3h-add-backspace-and-delete-keyboard-shortc/260718-j3h-SUMMARY.md
autonomous: false
requirements:
  - QUICK-260718-J3H
must_haves:
  truths:
    - "D-01/D-02: Unmodified Backspace and Delete call the same `rotoKeyUtilities.deleteKey` callback as the visible Roto Delete button, leaving its real-key and busy-state guards authoritative."
    - "D-03: Repeated keydown events and Command, Control, Alt, or Shift variants never request frame deletion; an eligible handled deletion prevents the browser default."
    - "D-04: Editing controls, unrelated buttons, script rename/count fields, and open dialogs are protected, while the selected `.physics-paint-roto-cell.current`, Studio root, canvas, and local timeline remain eligible targets."
    - "D-05/D-06: Every existing Physics Paint shortcut and the visible Delete-button/controller architecture remain unchanged, and shortcut help names the new keys."
    - "D-07/D-08/D-10: The first execution changes production code only, passes typecheck, creates one atomic production commit plus an awaiting-native-UAT summary, and stops before tests or completion tracking."
  artifacts:
    - path: "app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts"
      provides: "Delete-key dispatch and target-specific eligibility without changing existing shortcut dispatch"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Direct wiring from keyboard actions to `rotoKeyUtilities.deleteKey`"
    - path: "app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx"
      provides: "Concise visible shortcut-help copy"
    - path: ".planning/quick/260718-j3h-add-backspace-and-delete-keyboard-shortc/260718-j3h-SUMMARY.md"
      provides: "Resumable handoff explicitly marked awaiting native UAT"
  key_links:
    - from: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      to: "app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts"
      via: "Keyboard action receives the exact `rotoKeyUtilities.deleteKey` function already used by `onDeleteRotoFrame`"
      pattern: "deleteKey"
    - from: "app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts"
      to: "app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx"
      via: "Target policy makes only the current Roto timeline-cell button an allowed button target while preserving the visible button path"
      pattern: "physics-paint-roto-cell"
---

<objective>
Add keyboard-only Backspace/Delete deletion for the selected real Physics Paint Roto key per D-01 through D-06, using the existing guarded key-utility transaction rather than introducing another removal path.

Purpose: Make real-key deletion faster without allowing browser editing semantics, modal interactions, generated/empty frames, or busy script/apply states to bypass the established controller safeguards.
Output: Three focused production edits, production-only automated validation, one atomic production commit, an awaiting-native-UAT SUMMARY handoff, and a blocking native verification checkpoint; regression tests and final gates remain deferred per D-07 through D-10.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts
@app/src/components/physic-paint/hooks/usePhysicsPaintStudioKeyboard.ts
@app/src/components/physic-paint/PhysicsPaintStudio.tsx
@app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts
@app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx
@app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx
@app/package.json

The Studio root owns the bubbling `onKeyDown` handler. `PhysicsPaintWorkflowStrip` already routes its visible Delete button to `onDeleteRotoFrame`, which Studio supplies from `rotoKeyUtilities.deleteKey`. That utility owns the real-key requirement, generated/empty/background-only status messages, mutation/apply/key-action blocking, transaction execution, persistence, canvas/cache cleanup, and session synchronization. Reuse it exactly; do not duplicate any availability calculation or store removal.

This is the production-only pre-UAT stage. Execute Task 1, including its two commits and partial SUMMARY, then halt at Task 2. Do not create, modify, rename, delete, or run regression tests before explicit native approval. Do not start the development server, install packages, edit `app/src/lib/shortcuts.ts`, update ROADMAP/STATE, or claim the quick task is complete.
</context>

<decisions>
- D-01: Backspace and Delete invoke the exact existing `rotoKeyUtilities.deleteKey` action used by the visible Delete button; no direct store mutation or second removal transaction is permitted.
- D-02: Existing key-utility availability, guard, status, transaction, persistence, cache, and canvas paths remain authoritative for real, generated, empty, background-only, dirty, busy, mutation-locked, pending Apply, and Play Script states.
- D-03: Only unmodified, non-repeating Backspace/Delete events are eligible; reject Command, Control, Alt, and Shift variants, and call `preventDefault()` only when the frame-delete action is actually dispatched.
- D-04: Protect inputs, textareas, selects, contenteditable regions, script rename, Play Script frame count, open dialogs/modals, unrelated buttons, and controls with local deletion semantics. Keep the selected `.physics-paint-roto-cell.current`, Studio root, canvas, and local timeline eligible.
- D-05: Preserve every existing Physics Paint shortcut and add concise help copy: `Backspace / Delete remove selected real key`.
- D-06: Do not change the global shortcuts module, dependencies, visible Delete behavior, Roto controller architecture, or established Preact pattern.
- D-07: Initial execution is production code only and runs app type checking plus the scoped diff check; no Vitest command or test edit is authorized.
- D-08: Stop after production validation and commits for native visible UAT; post-UAT regression tests and final gates require explicit approval and a resumed stage.
- D-09: Native UAT covers both keys, Undo/Redo, hold suppression, generated/empty protection, input/dialog protection, timeline-cell focus, and existing-shortcut non-regression.
- D-10: Create one atomic production commit, then a separate SUMMARY-only documentation commit whose status is `awaiting_native_uat`; do not update ROADMAP.md or STATE.md.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch guarded real-key deletion and record the native-UAT handoff</name>
  <files>app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, .planning/quick/260718-j3h-add-backspace-and-delete-keyboard-shortc/260718-j3h-SUMMARY.md</files>
  <read_first>app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts, app/src/components/physic-paint/hooks/usePhysicsPaintStudioKeyboard.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/hooks/useRotoKeyUtilities.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx, app/src/components/physic-paint/view/PhysicsPaintScriptsPanel.tsx, app/package.json</read_first>
  <action>
Implement D-01/D-02 in `physicsPaintStudioKeyboard.ts` by extending `PhysicsPaintStudioKeyboardActions` with an optional delete callback so existing production/test callers that have not yet supplied the new action continue to typecheck during this no-test-edit stage. Add a delete-specific target predicate rather than tightening `isPhysicsPaintShortcutTarget`, because the latter governs all established shortcuts and changing it could regress their current focus behavior. The delete predicate must first honor the existing editable-control exclusion; reject a target inside any rendered `[role="dialog"]` or `[aria-modal="true"]`; reject unrelated interactive targets such as buttons, links, listbox options, and ARIA controls with local key semantics; and make one explicit exception for a target at or inside `.physics-paint-roto-cell.current`. Ordinary Studio/root, canvas, and timeline elements remain allowed. Also suppress frame deletion whenever an open modal dialog exists in the target document, even if focus has momentarily returned to the Studio root.

Add a Backspace/Delete branch without changing the order or semantics of Undo/Redo, help, Space playback, arrows, G, O, or bracket shortcuts. Per D-03, the branch handles only `Backspace` or `Delete` when `event.repeat` is false and Meta/Ctrl/Alt/Shift are all false. If the target is ineligible or the optional callback is absent, return without preventing the default. Otherwise call `preventDefault()` exactly once and invoke the callback exactly once. Do not inspect the Roto store, recompute `canDelete`, pre-delete cache entries, or reproduce key-utility guard messages in the keyboard module.

Implement D-01/D-02 in `PhysicsPaintStudio.tsx` by supplying `deleteRotoKey: rotoKeyUtilities.deleteKey` in the existing `usePhysicsPaintStudioKeyboard` actions object. This must be the same function reference already exposed as `deleteRotoFrame` and passed to workflow `onDeleteRotoFrame`; do not wrap it in a second transaction, call `physicPaintStore.removeRealRotoKeyFrame`, or alter visible Delete availability. The existing utility/controller remains responsible for real-key selection, generated/empty/background-only rejection, dirty handling, busy/apply/script locks, status copy, persistence, cache cleanup, and canvas/session restoration.

Implement D-05 in `PhysicsPaintStudioView.tsx` by adding the concise line `Backspace / Delete remove selected real key` to the existing shortcuts help. Preserve all existing shortcut help and behavior. Per D-06, do not add state, effects, Signals, dependencies, global shortcut registration, or changes to `usePhysicsPaintStudioKeyboard`, `useRotoKeyUtilities`, `PhysicsPaintWorkflowStrip`, `app/src/lib/shortcuts.ts`, or visible Delete-button markup.

Per D-07, run only `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck` and `git -C /Users/lmarques/Dev/efx-motion-editor diff --check`; do not run Vitest, package builds that execute tests, or a server. Inspect status and the scoped diff, stage exactly the three production files, and create one atomic commit named `feat(quick-260718-j3h): add Roto key delete shortcuts`. Do not amend an earlier commit.

After the production commit, create `260718-j3h-SUMMARY.md` per D-08/D-10 with frontmatter status `awaiting_native_uat`, the production commit hash, production files changed, passing typecheck/diff checks, and explicit statements that no tests were created/modified/run, native UAT is still required, post-UAT tests/final gates are blocked until approval, and the task is automated-ready rather than complete. Include the exact UAT matrix from Task 2 and the resume instruction. Stage only that SUMMARY and create a separate documentation commit named `docs(quick-260718-j3h): record native UAT handoff`. Do not update STATE.md or ROADMAP.md. Stop immediately at Task 2.
  </action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck &amp;&amp; git -C /Users/lmarques/Dev/efx-motion-editor diff --check</automated>
  </verify>
  <acceptance_criteria>
- Eligible Backspace/Delete dispatches exactly one call to the optional keyboard delete action and prevents the browser default exactly once.
- Repeat events and Meta/Ctrl/Alt/Shift variants do not dispatch deletion and do not prevent their default behavior.
- Editable fields, script rename, Play Script count, open dialogs/modals, unrelated buttons, links, options, and local ARIA controls are excluded.
- `.physics-paint-roto-cell.current` remains eligible despite being a button; root, canvas, and local timeline non-control targets remain eligible.
- Studio supplies the exact `rotoKeyUtilities.deleteKey` reference also used by the visible Delete callback; no store-removal or duplicate transaction path is added.
- Existing shortcut branches and visible Delete behavior are unchanged, and help includes `Backspace / Delete remove selected real key`.
- Typecheck and diff checks exit 0 without a server, test command, test-file change, dependency change, or global-shortcuts change.
- One production commit contains exactly the three production files; a following docs commit contains only the awaiting-native-UAT SUMMARY.
  </acceptance_criteria>
  <done>The production shortcut integration is committed and automated-ready, and the quick SUMMARY truthfully records that native UAT and post-UAT regression work remain pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve native Backspace/Delete behavior and shortcut non-regression</name>
  <files>None — native user verification only</files>
  <action>Stop after Task 1's production and SUMMARY commits. Present the native verification flow below and wait for explicit approval. Per D-08/D-10, do not create or modify tests, run Vitest or final gates, update the partial SUMMARY to complete, update STATE/ROADMAP, or claim completion while this checkpoint is unresolved.</action>
  <what-built>Unmodified Backspace/Delete now routes through the same guarded Roto real-key deletion action as the visible Delete button, with repeat/modifier suppression and target protection for editing controls and dialogs.</what-built>
  <how-to-verify>
1. In the native standalone Physics Paint Studio, select a deletable real Roto key, place focus on the Studio root or canvas, press Backspace once, and confirm exactly that selected real key is deleted through the same visible behavior/status path as the Delete button.
2. Undo the deletion and confirm the real key returns with its expected cached/canvas state; Redo and confirm it is deleted again. Undo once more to restore it for the next checks.
3. With the restored real key selected, press Delete once and confirm the same one-key deletion behavior; Undo it again.
4. Hold Backspace long enough to generate repeat events and confirm only the initial keydown can delete—the next real key is not removed. Repeat with Delete if practical.
5. Select a generated interpolation cell, an empty cell, and any visible background-only/non-deletable cell in turn; press both Backspace and Delete and confirm no key is removed and the existing guard/status semantics remain authoritative.
6. While a key mutation, Apply, Load/Apply Script, or Play Script operation is busy/locked, press Backspace/Delete and confirm no deletion occurs. Do not interrupt the operation beyond its normal controls.
7. Focus the script rename input and press Backspace/Delete while editing text; confirm only the text field responds. Open the Play Script dialog, focus its Frames input, and confirm Backspace/Delete edits that value without deleting a Roto key.
8. With either Play Script or Delete Script confirmation dialog open, focus dialog buttons or dialog body and press Backspace/Delete; confirm no Roto key deletion occurs and dialog controls retain their normal behavior.
9. Focus the selected `.physics-paint-roto-cell` button itself and press Backspace, then after Undo press Delete; confirm both shortcuts remain allowed from the selected timeline cell despite it being a button. Confirm root, canvas, and local timeline focus also work.
10. Focus unrelated buttons/controls and press Backspace/Delete; confirm they do not delete a Roto key.
11. Recheck all established Studio shortcuts: Cmd/Ctrl+Z Undo, Cmd+Shift+Z or Ctrl+Y Redo, `?` help, Space cached playback, arrows and Shift+arrows navigation, G current-frame navigation, O onion toggle, and `[ ]` onion count. Confirm none regressed.
  </how-to-verify>
  <resume-signal>Type `approved` to authorize the resumed post-UAT regression-test and final-gate stage, or describe the first failing key, target, guard, Undo/Redo, hold, dialog, or existing-shortcut case. Do not claim completion at this checkpoint.</resume-signal>
  <verify>
    <automated>test "$(git -C /Users/lmarques/Dev/efx-motion-editor show --pretty=format: --name-only HEAD | grep -v '^$')" = ".planning/quick/260718-j3h-add-backspace-and-delete-keyboard-shortc/260718-j3h-SUMMARY.md" &amp;&amp; git -C /Users/lmarques/Dev/efx-motion-editor show --pretty=format: --name-only HEAD^ | grep -v '^$' | sort | diff -u - &lt;(printf '%s\n' 'app/src/components/physic-paint/PhysicsPaintStudio.tsx' 'app/src/components/physic-paint/view/PhysicsPaintStudioView.tsx' 'app/src/components/physic-paint/view/physicsPaintStudioKeyboard.ts' | sort) &amp;&amp; grep -q 'awaiting_native_uat' /Users/lmarques/Dev/efx-motion-editor/.planning/quick/260718-j3h-add-backspace-and-delete-keyboard-shortc/260718-j3h-SUMMARY.md</automated>
  </verify>
  <done>The user has explicitly approved both delete keys, Undo/Redo, hold suppression, protected/non-deletable targets, selected-cell focus allowance, busy guards, and all existing shortcuts; only then is post-UAT regression work authorized.</done>
</task>

</tasks>

<post_uat_pending_checkpoint status="blocked-until-explicit-native-uat-approval">
Per D-08/D-09, tests and final closure are not authorized during this initial run. After explicit native approval, resume this quick task in a separate stage: update the existing focused keyboard shortcut coverage to prove one-shot Backspace/Delete dispatch, repeat and modifier rejection, `preventDefault` behavior, protected inputs/dialogs/unrelated controls, the selected `.physics-paint-roto-cell.current` exception, and preservation of existing shortcut dispatch; add or update the narrow Studio source/wiring assertion only if the existing suite requires it. Use existing test configuration and `vitest run`, then run the required final app gates. Commit regression/final-gate work separately, update `260718-j3h-SUMMARY.md` from `awaiting_native_uat` to complete with the approved UAT and final results, and only then perform normal quick-task tracking updates. Do not pre-author these tests before approval.
</post_uat_pending_checkpoint>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Focused DOM target → Studio shortcut dispatcher | A bubbling keyboard event may originate from canvas/timeline surfaces or from an editing/control surface with its own deletion meaning. |
| Shortcut dispatcher → Roto key utility | Keyboard intent crosses into an existing destructive controller transaction and must not bypass its availability/status guards. |
| Visible modal state → background Studio | Dialog interactions must not leak destructive Backspace/Delete behavior to the underlying Studio root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260718-J3H-01 | Tampering | `dispatchPhysicsPaintStudioKeyDown` | high | mitigate | Accept only unmodified non-repeat Backspace/Delete events and invoke exactly the existing delete callback once. |
| T-260718-J3H-02 | Elevation of Privilege | DOM target eligibility | high | mitigate | Block editable fields, dialogs, unrelated controls, and local-interaction surfaces; explicitly allow only the current Roto cell among button targets. |
| T-260718-J3H-03 | Tampering | Roto store/cache removal | high | mitigate | Do not access stores or construct transactions in keyboard code; route exclusively to `rotoKeyUtilities.deleteKey`. |
| T-260718-J3H-04 | Denial of Service | Repeated keydown | medium | mitigate | Ignore `event.repeat` so holding either key cannot cascade across adjacent real keys. |
| T-260718-J3H-05 | Repudiation | Native destructive shortcut behavior | medium | mitigate | Require explicit native UAT across deletion, Undo/Redo, protected targets, guards, hold behavior, and existing shortcuts before tests and completion claims. |
| T-260718-J3H-SC | Tampering | Package supply chain | low | accept | No package installation or dependency change is planned. |
</threat_model>

<verification>
1. Production typecheck proves the optional action contract and Studio wiring compile without editing tests.
2. `git diff --check` proves the scoped production changes are mechanically clean.
3. The production commit contains only keyboard dispatch, Studio wiring, and help copy; the following docs commit contains only the awaiting-native-UAT SUMMARY.
4. Native UAT covers both delete keys, Undo/Redo, repeat suppression, generated/empty/background/busy protection, editable/dialog/control protection, selected timeline-cell allowance, and every existing shortcut.
5. Execution halts as automated-ready and UAT-pending; no regression test, final gate, server, ROADMAP update, STATE update, or completion claim occurs before approval.
</verification>

<source_audit>
| SOURCE | ID | Feature/Requirement | Task/Checkpoint | Status | Notes |
|--------|----|---------------------|-----------------|--------|-------|
| GOAL | QUICK-260718-J3H | Add Backspace/Delete keyboard deletion for the selected real Physics Paint Roto key | Task 1 + Task 2 | COVERED | Direct existing-action wiring plus native verification. |
| REQ | QUICK-260718-J3H | Small standalone Studio keyboard-only integration | Task 1 | COVERED | Three production files; no global shortcut or architecture change. |
| RESEARCH | Existing action seam | Visible Delete uses `rotoKeyUtilities.deleteKey` through `onDeleteRotoFrame` | Task 1 | COVERED | Keyboard receives the identical function reference. |
| RESEARCH | Existing target seam | Studio root owns bubbling keyboard dispatch; generic target guard currently protects editable fields only | Task 1 | COVERED | A separate delete-specific predicate avoids changing existing shortcuts. |
| RESEARCH | Modal/control seam | Script rename and Play Script count are inputs; script confirmations render `role="dialog" aria-modal="true"` | Task 1 + Task 2 | COVERED | Editable and modal protection is explicit and native-tested. |
| CONTEXT | D-01 | Reuse exact existing delete action; no store mutation or second transaction | Task 1 | COVERED | Direct callback wiring only. |
| CONTEXT | D-02 | Keep existing availability/guard/status path authoritative | Task 1 + Task 2 | COVERED | No duplicated delete logic; native guard matrix required. |
| CONTEXT | D-03 | Ignore repeat/modifier variants and prevent default only when handled | Task 1 + Task 2 | COVERED | Dedicated branch and hold/modifier UAT. |
| CONTEXT | D-04 | Protect editors/dialogs/local controls while allowing current timeline cell/root/canvas/timeline | Task 1 + Task 2 | COVERED | Target predicate and focus-specific UAT. |
| CONTEXT | D-05 | Preserve shortcuts and update help | Task 1 + Task 2 | COVERED | Existing branch ordering remains intact and all shortcuts are rechecked. |
| CONTEXT | D-06 | No global shortcuts, dependency, visible Delete, controller, or Preact architecture changes | Task 1 | COVERED | Explicit no-touch boundary. |
| CONTEXT | D-07 | Production-only first run with minimum production check | Task 1 | COVERED | Typecheck and diff check only. |
| CONTEXT | D-08 | Stop for native UAT; tests/final gate deferred | Task 2 + pending checkpoint | COVERED | Blocking checkpoint precedes all regression work. |
| CONTEXT | D-09 | Native UAT matrix | Task 2 | COVERED | All named cases appear as concrete steps. |
| CONTEXT | D-10 | Atomic production commit, awaiting-UAT SUMMARY, no ROADMAP update | Task 1 + Task 2 | COVERED | Separate production/docs commits preserve resumability and truthful status. |
</source_audit>

<success_criteria>
- Backspace and Delete remove only the selected deletable real Roto key through `rotoKeyUtilities.deleteKey`.
- Generated, empty, background-only/non-deletable, dirty/busy, mutation-locked, pending Apply, and Play Script states continue to follow existing utility/controller guards and statuses.
- Repeat and modified variants never request deletion; eligible handled events prevent the browser default once.
- Editing fields, dialogs, unrelated buttons, and local controls are protected, while the current Roto timeline-cell button, Studio root, canvas, and local timeline remain eligible.
- All established Physics Paint shortcuts and the visible Delete button remain unchanged; help advertises the new keys concisely.
- Production-only typecheck/diff checks pass, production changes are one atomic commit, and a separate SUMMARY-only commit records `awaiting_native_uat`.
- The initial run stops at native UAT with tests, final gates, tracking updates, and completion claims deferred until explicit approval.
</success_criteria>

<output>
After Task 1, stop at Task 2 and report the absolute plan path, production commit hash, SUMMARY commit hash, and `awaiting_native_uat` status. Do not update ROADMAP.md or STATE.md. Do not create/modify/run tests, start a server, run final gates, or claim completion before explicit native approval.
</output>
