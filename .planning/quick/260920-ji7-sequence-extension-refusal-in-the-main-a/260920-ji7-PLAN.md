---
phase: quick-260920-ji7
plan: 260920-ji7
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/lib/frameMap.ts
  - app/src/lib/frameMap.test.ts
  - app/src/components/timeline/TimelineInteraction.ts
  - app/src/stores/sequenceStore.ts
autonomous: true
requirements: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The exact path that refuses sequence extension on an EXISTING project is named with file:line evidence and reproduced by a runnable probe — never inferred from reading alone."
    - "D-08 verdict first: the diagnosis says whether the refusal is the 260918-o0n clamp family at sequence level (a derived or parent bound fed back as an authority) or a structural out_frame-management problem. The fix is attempted ONLY for the clamp verdict."
    - "Clamp verdict → extending an existing project's sequence/span grows the addressable extent: the bound is authorized by the live parent end in both directions, capped only by PHYSIC_PAINT_MAX_APPLY_FRAMES, and never by its own previous value."
    - "Structural verdict → NO fix is attempted: Tasks 2 and 3 do not run, the facts and the raw probe output are on record in the SUMMARY, the working tree is left clean, and the item goes back to the user for non-blocking re-classification."
    - "The fix (when attempted) touches only the diagnosed module and its test — no out_frame management redesign, and every behavior pinned by existing tests stays green."
    - "No existing real key, loop clip, or group is silently dropped or made invalid by the fix."
  artifacts:
    - .planning/quick/260920-ji7-sequence-extension-refusal-in-the-main-a/260920-ji7-SUMMARY.md — candidate verdicts with file:line evidence, the probe source and raw output, the CLAMP-or-STRUCTURAL verdict, and the executed or skipped tasks
    - the diagnosed path's existing test harness — one RED test encoding the refusal, then the same test green after the minimal fix (leading candidate harness: app/src/lib/physicPaintBridge.test.ts)
    - the diagnosed module — the minimal edit that removes the one-way bound (leading candidate: the launch-context capacity resolution in app/src/lib/physicPaintBridge.ts)
  key_links:
    - "app/src/lib/physicPaintBridge.ts:3283-3292 — read capacity, then layerEndExclusive = Math.min(parentLocalEndExclusive, capacity), then write that minimum back as the new capacity. Because the value written is a MINIMUM of the stored value, the stored capacity can only ever shrink or stay: after the first launch it equals the parent end of that moment and every later parent-end growth is discarded."
    - "app/src/stores/physicPaintStore.ts:3793-3810 — capacity read/write primitive; the ONE writer fed by the parent end is the launch path above, and it is written back min-bounded."
    - "app/src/stores/physicPaintStore.ts:3453-3536 — the document install path restores capacity from the persisted document, and capacity is a canonical-revision term (physicsPaintRotoPhysicalModel.ts:1258), so the freeze survives save/reopen: this is why the refusal is reported on an EXISTING project rather than a fresh one."
    - "app/src/lib/physicPaintBridge.ts:652-700 — the authority read refuses with 'No remaining Physics Paint sequence capacity is available.' when physicalRemaining (capacity - canonicalStart) is not positive; the same capacity bounds every key placement (physicsPaintRotoPhysicalModel.ts:940-968 throws for appFrame >= capacity) and the child document's whole end (physicsPaintRotoPhysicalResolver.ts:2260-2262)."
    - "app/src/lib/frameMap.ts:253-285 — the main-editor derived timeline end reads the paint display end, and loop-derived ends are bounded by the same capacity inside the interval derivation, so a latched capacity can also cap the derived sequence length the user sees."
    - "app/src/components/timeline/TimelineInteraction.ts:127-145, 861-877 + timelineFxSpanDrag.ts — the FX span drag path that 260918-o0n already freed of the derived-total clamp; it must stay the reference for what 'the clamp family looks like' and must not be re-tightened."
---

<objective>
Diagnose, then minimally fix (or stop and report), the refusal the user has been blocked on since 2026-09-14: **an existing project's sequence length cannot be extended** (workaround: create within the current bounds). Phase 53 D-08 makes this BLOCKING and explicitly diagnosis-first with an escape hatch.

Diagnosis-first, not fix-first — the whole point of D-08 is that the exact broken path is unknown. Four candidate paths were located by live reads at planning time (2026-09-20); the executor confirms or rejects each with evidence, then probes the survivor:

- **Path A — span drag past the derived end (260918-o0n family).** Closed on 2026-09-18 by eec835a8: `TimelineInteraction.getSpanDragFrame` passes a null ceiling into `resolveTimelinePointerFrame`, `resolveFxSpanDragRange` takes no timeline bound, and `sequenceStore.updateFxSequenceRange` (`sequenceStore.ts:352-358`) is clamp-free. Re-confirm by running o0n's four test files — if they are green, this path is NOT the refusal.
- **Path B — scrub past the end.** `timelineStore.seek` (`timelineStore.ts:74-78`) clamps to `totalFrames - 1`, and the ruler-drag branch (`TimelineInteraction.ts:931-935`) resolves through the clamped `getFrame`. A seek is not an extension, so this is a bound BY DESIGN — record it, do not change it.
- **Path C — manual out_frame edit.** No numeric in/out editor exists in the main app: the only writers of `Sequence.outFrame` are creation seeding (`AddFxMenu.tsx:76,78,119,121,152,154`; `ImportedView.tsx:103,105,123,172,174,192,244,246,266`) and the span drag. Record as "no such surface" rather than assuming a hidden clamp.
- **Path D — creation seeding.** Every new span/overlay is seeded from `totalFrames.peek()` at creation (`AddFxMenu.tsx:49,76,119,152`; `AddLayerMenu.tsx:40`), so later content lands truncated at the then-current length. Post-o0n this is extensible by drag — record the verdict; this is a seeding default, not a refusal.
- **Path E — the paint capacity latch (LEADING CANDIDATE, hypothesis to confirm by probe).** `createPhysicPaintLaunchContext` (`physicPaintBridge.ts:3283-3292`) reads the stored capacity, computes `Math.min(parentLocalEndExclusive, capacity)`, and writes THAT back as the capacity. A minimum-of-stored-value write-back is monotone non-increasing: after the first launch the capacity equals the parent end of that moment, and every later parent-end growth is discarded (`min(80, 40) = 40`). The frozen value is then the single end authority for key placement, for the authority read that refuses with "No remaining Physics Paint sequence capacity is available." (`physicPaintBridge.ts:652-700`), for the child document end (`physicsPaintRotoPhysicalResolver.ts:2260-2262`), and — through the loop interval derivation read by `frameMap.ts:253-268` — potentially for the main-editor derived timeline end. It is persisted in the document (`physicsPaintRotoPhysicalModel.ts:1258`) and restored on install (`physicPaintStore.ts:3477,3536`), which is why it bites an EXISTING project and survives reopen.

Hypothesis is not a verdict: Task 1 must reproduce Path E (or whichever path survives) with a runnable probe before any test or fix is written.

**Escape hatch (D-08, verbatim intent): if the diagnosis proves this structural — an out_frame management redesign, not a clamp — STOP after Task 1, record the facts, and attempt no fix.** The item is then re-classified non-blocking by the user.

Fix criterion when the verdict IS a clamp (minimal, no redesign): the bound must be authorized by the live parent end in both directions and capped only by `PHYSIC_PAINT_MAX_APPLY_FRAMES`; it must never be a function of its own previous value; existing keys/loops must not be silently invalidated.

Purpose: unblock the user's real project (they have been working around this since 2026-09-14) and settle whether v1.0.0 ships with a known sequence-extension refusal.
Output: a documented verdict with file:line evidence and a raw probe, plus the minimal fix and its regression test — or the documented stop with the facts on record.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/53-integrated-v1-0-0-acceptance/53-CONTEXT.md
@app/src/lib/physicPaintBridge.ts
@app/src/stores/physicPaintStore.ts
@app/src/lib/frameMap.ts
@app/src/components/timeline/TimelineInteraction.ts
@app/src/stores/sequenceStore.ts

Verified code facts (live reads, 2026-09-20):
- `physicPaintBridge.ts:3283-3292` — `const capacity = getRotoPhysicalCapacity(layerId, trackId); const timelineRange = getLayerLocalTimelineRange(layer); const layerEndExclusive = Math.min(timelineRange.localEndExclusive, capacity); physicPaintStore.setRotoPhysicalCapacity(layerId, trackId, layerEndExclusive);` — the write-back is the latch.
- `getRotoPhysicalCapacity` (`physicPaintStore.ts:3798-3800`) falls back to `PHYSIC_PAINT_MAX_APPLY_FRAMES` (600) when unset; `setRotoPhysicalCapacity` (`:3804-3808`) takes any non-negative integer and caps at 600. `setRotoPhysicalCapacity` has exactly ONE caller in the tree: the launch path above (verified by grep at planning time).
- The 600 cap is `PHYSIC_PAINT_MAX_APPLY_FRAMES` (`app/src/types/physicPaint.ts:371`), also the validator ceiling (`:2578`, `:2590`) and the resolver's capacity validation (`physicsPaintRotoPhysicalResolver.ts:868-874`).
- `createPhysicPaintLaunchContext` throws when no authoritative parent range exists (`getLayerLocalTimelineRange` returns null → 'Physics Paint layer has no authoritative parent timeline range.'), so a project whose sequence range is unresolvable refuses at launch rather than silently.
- Existing harness for the leading candidate: `app/src/lib/physicPaintBridge.test.ts` already drives REAL stores and calls `createPhysicPaintLaunchContext` + `carriedRotoPhysical(context).capacity` (see its "Parent end authority" case near line 470-495, which pins the launch-time parent-end semantics this fix must preserve) and `getPhysicPaintRotoAuthority` (line 78). Use that harness; do not invent a new test config (project rule: never add one-off test configs).
- Test command convention (project rule): `pnpm --filter efx-motion-editor exec vitest run <paths>` — vitest run only, never watch. Full suite at the plan base: 0 failures.
- Project reactivity rules apply to any UI-adjacent edit (`efx-preact-reactivity` skill); the leading candidate is store/bridge code, not a component.
- Native app runs are the user's side only — do NOT launch the app; native UAT is owed, never claimed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the refusal — verdict per candidate path, then a runnable probe</name>
  <files>app/src/lib/physicPaintBridge.test.ts (probe, conditional on the diagnosed path)</files>
  <action>
    This task is DIAGNOSIS. It may add the probe (below) and must not change production behavior. D-08 asks one question and requires evidence, not a plausible story.

    Step 1 — Re-confirm Path A is closed: run `pnpm --filter efx-motion-editor exec vitest run src/components/timeline/timelineFxSpanDrag.test.ts src/components/timeline/TimelineInteraction.test.ts src/components/timeline/TimelineRenderer.test.ts src/lib/frameMap.test.ts`, and read `TimelineInteraction.ts:127-145` + `:861-877`, `timelineFxSpanDrag.ts`, `sequenceStore.ts:352-358`. Record: does any bound remain on the FX span drag path? A green o0n suite means the drag path is not the refusal.

    Step 2 — Record verdicts, each with the exact file:line, for Path B (seek/scrub clamp — by design, and not an extension), Path C (no numeric out_frame surface exists; list the only writers of `Sequence.outFrame` you can find by grep, so the "no such surface" claim is grounded, not assumed), and Path D (creation seeding from `totalFrames.peek()`; a default, and extensible by drag post-o0n).

    Step 3 — Probe the survivor. If Path E is still standing, reproduce it end-to-end in the EXISTING real-stores harness `app/src/lib/physicPaintBridge.test.ts` (its harness style: register the efx-paint document for a physic-paint layer, `sequenceStore.add({kind:'fx', ..., inFrame, outFrame, layers:[layer]})`, then `createPhysicPaintLaunchContext`): launch against a parent span, read the carried capacity and `physicPaintStore.getRotoPhysicalCapacity`; extend the parent span with `sequenceStore.updateFxSequenceRange` to roughly double the end; launch again and read both values; then read `getPhysicPaintRotoAuthority({... canonicalStart: <old end>})` and record the exact refusal. Also read (do not edit) `frameMap.ts:253-285` and confirm in the derivation whether the same capacity can cap the main-editor derived timeline end for a loop-bearing paint layer, since that is the surface the user calls "sequence length". Capture the probe's raw numbers and refusal strings VERBATIM. If a different candidate path proves to be the refusal, probe that one instead, in its own existing harness: the probe must exercise REAL modules and real stores, never a re-implementation of the logic.

    Step 4 — Verdict, written into the SUMMARY: CLAMP or STRUCTURAL, the exact file:line of the refusing bound, the probe source and raw output, and what the user-visible action was that the bound refused. The CLAMP test is: the refusing value is a derived or previously-written bound fed back as an authority, removable without redesigning how out_frame is managed. The STRUCTURAL test is: honoring the extension requires changing how sequence ends are owned/managed (a redesign), not removing a bound.

    Step 5 — ESCAPE HATCH. If the verdict is STRUCTURAL: write the SUMMARY with the full facts and the probe source, DELETE the probe so the tree is clean (no committed red test with no fix), run `git status --short` to prove cleanliness, commit only the `.planning/` SUMMARY, and STOP — Tasks 2 and 3 do not run. Do not attempt a fix, do not refactor, do not "prepare" anything. Do not use the words v1/v2/simplified/partial for what is left undone: the facts go on record whole.

    Commit (CLAMP verdict): `docs(260920-ji7): diagnose the sequence-extension refusal — <path> clamp confirmed`. Commit (STRUCTURAL verdict): `docs(260920-ji7): sequence-extension refusal is structural — diagnosis only, no fix`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run src/components/timeline/timelineFxSpanDrag.test.ts src/components/timeline/TimelineRenderer.test.ts src/lib/frameMap.test.ts src/lib/physicPaintBridge.test.ts</automated>
  </verify>
  <done>The SUMMARY names one path as the refusal with file:line evidence and carries the raw probe output (the values that cannot grow, and the exact refusal string); every other candidate has a recorded verdict with its evidence; the verdict is explicitly CLAMP or STRUCTURAL. If STRUCTURAL, the probe is deleted, the tree is clean, no production file was modified, and the run STOPS here with Tasks 2 and 3 unexecuted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — the diagnosed refusal, as a failing test on the diagnosed path</name>
  <files>app/src/lib/physicPaintBridge.test.ts (or the diagnosed path's own existing harness)</files>
  <behavior>
    Conditional on a CLAMP verdict in Task 1. The test encodes the diagnosed refusal and the law the fix must satisfy — for the leading candidate (launch-context capacity latch), in the existing real-stores harness:
    - "an extended parent span raises the layer's addressable capacity": launch against a parent span of N, extend the parent span to 2N via `updateFxSequenceRange`, launch again → the carried capacity and `getRotoPhysicalCapacity` are 2N, not N. Today this FAILS with the latched N.
    - "the capacity is capped only by the global maximum": a parent span beyond `PHYSIC_PAINT_MAX_APPLY_FRAMES` yields exactly that ceiling, not an unbounded value.
    - "the parent end stays the authority": the existing launch-time parent-end semantics pinned near `physicPaintBridge.test.ts:470-495` stay true (a fresh launch against an unauthored-capacity layer carries the parent end), and a parent end that SHRINKS is still honored without throwing on the surviving records — assert the surviving real keys are still readable, never dropped.
    - "the authority read follows the extension": after the extension, `getPhysicPaintRotoAuthority` at the old end is no longer refused for lack of remaining capacity.
    If Task 1 diagnosed a DIFFERENT path, write the equivalent three-to-four cases at that path's own seam (its own existing harness), on the same law: the bound follows the live parent end in both directions, is capped only by the global maximum, and never by its own previous value.
  </behavior>
  <action>
    Promote the Task 1 probe into the permanent test: same file, correct expectations, no production edit yet. Run it and capture the RED verbatim (command + failing assertions) — a green run here means the expectation does not actually encode the refusal and must be re-derived from the probe before proceeding. Do not weaken, skip, or delete a case to pass, and do not use `it.todo`/`skip` for a case this plan is supposed to fix. Do not add a new test config (project rule) — extend the harness that already drives the real stores. Commit: `test(260920-ji7): RED — <path> bound refuses the sequence extension`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts</automated>
  </verify>
  <done>The new cases fail at the plan base with the refusal reproduced (RED output captured verbatim in the SUMMARY); no production file was modified in this task; every pre-existing case in that harness still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — minimal fix on the diagnosed path, then the full gates</name>
  <files>app/src/lib/physicPaintBridge.ts (or the diagnosed module), app/src/lib/physicPaintBridge.test.ts</files>
  <behavior>
    - The extension is honored: after the parent span grows, the layer's capacity and every end derived from it follow the live parent end.
    - The bound is capped only by `PHYSIC_PAINT_MAX_APPLY_FRAMES`; it is never a function of its own previous value.
    - No silent loss: pre-existing real keys, loop clips and groups remain valid and readable across the change; nothing is dropped to make the bound fit.
    - Every pre-existing law stays green: the launch-time parent-end semantics in `physicPaintBridge.test.ts`, the capacity validators (`physicsPaintStore.ts:3267-3340`, `physicsPaintRotoPhysicalModel.ts:940-968`, `physicsPaintRotoPhysicalResolver.ts:868-874`), the authority refusals for genuinely out-of-capacity requests, and the o0n span-drag suite.
  </behavior>
  <action>
    Apply the MINIMAL edit at the exact line the diagnosis named — for the leading candidate, the capacity resolution in `createPhysicPaintLaunchContext` (`physicPaintBridge.ts:3283-3292`), so the value written back is authorized by the live parent end rather than being the minimum of the parent end and its own stored value. Do not redesign how sequence ends are owned, do not add a new module, do not change `PHYSIC_PAINT_MAX_APPLY_FRAMES`, and do not touch any file outside Task 1's diagnosed path (if the diagnosed path needs a file that is not in this plan's frontmatter list, STOP and report the proposed file set instead of silently widening scope — quick-task scope discipline). Leave the validators and the refusal messages intact: a genuinely impossible request must still be refused. Then run, in order and record the results: the RED file green; the o0n suite; `pnpm --filter efx-motion-editor exec tsc --noEmit`; the full suite `pnpm --filter efx-motion-editor exec vitest run` (background it if the runtime allows and resume only on completion; attribute any pre-existing failure by name from `.planning/STATE.md` rather than absorbing it silently); the scope gate `git diff --name-only` against this plan's frontmatter list. Commit: `fix(260920-ji7): <path> honors the live parent end — sequence extension no longer refused`. Then finish the SUMMARY: what was diagnosed, what was changed, the exact before/after values, the gate results, and the pending native UAT rows. No UAT claim.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts src/components/timeline src/lib/frameMap.test.ts && pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
  </verify>
  <done>The diagnosed case flips green with the extension honored; capacity follows the live parent end and is capped only by the global maximum; every pre-existing test in the touched harness and the o0n suite still passes; the full suite shows no new failure; `tsc --noEmit` is clean; the scope gate lists only this plan's files plus `.planning/` artifacts; the SUMMARY records the diagnosis, the fix, the before/after values, the gates, and leaves native UAT explicitly pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent Sequence record → paint layer capacity | The parent span end authorizes how many frames a paint layer may address. Today a stored value is fed back as its own ceiling; the plan's risk surface is loosening that bound, not a new input crossing a boundary. |
| persisted document → restored runtime capacity | The capacity is a canonical-revision field restored on open; a value that survives save/reopen must stay validator-bounded (no untrusted widening from a crafted `.mce`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260920-ji7-01 | Denial of service | capacity bound (`physicPaintBridge.ts:3283-3292`, validators in `physicPaintStore.ts:3267-3340` + `physicsPaintRotoPhysicalModel.ts:940-968`) | medium | mitigate | The loosened bound is capped by `PHYSIC_PAINT_MAX_APPLY_FRAMES` at the single write site; every existing capacity validator and collection parse stays untouched, so a crafted document cannot widen capacity beyond the ceiling at install time either. |
| T-260920-ji7-02 | Tampering | key placement / loop derivation under a changed capacity | medium | mitigate | A genuinely out-of-capacity placement must still be refused (existing refusal cases retained and re-run); the RED/GREEN cases assert surviving keys/loops are never dropped, so the change cannot silently invalidate stored content. |
| T-260920-ji7-03 | Repudiation | the diagnosis verdict itself | low | accept | The verdict rides the SUMMARY with the probe source and raw output, so a structural stop is auditable rather than an unverifiable claim; no mitigation code is warranted. |
| T-260920-ji7-SC | Tampering | pnpm installs | low | accept | No dependency changes in this plan — no package-legitimacy gate required. |
</threat_model>

<verification>
- Diagnosis gate: the SUMMARY names ONE refusing path with file:line evidence, carries the probe source and raw output (the values that cannot grow, the exact refusal string), and records a verdict per candidate path.
- Escape-hatch gate: if STRUCTURAL, no production file is modified, the probe is deleted, `git status --short` is clean, and Tasks 2-3 are unexecuted.
- RED proof: the failing run before the fix, captured verbatim.
- Targeted gate: `pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts src/components/timeline src/lib/frameMap.test.ts` — all pass after Task 3.
- Types: `pnpm --filter efx-motion-editor exec tsc --noEmit` — clean.
- Full suite: `pnpm --filter efx-motion-editor exec vitest run` — 0 new failures (0 at the plan base); any pre-existing failure attributed by name.
- Scope gate: `git diff --name-only` lists only this plan's frontmatter files plus `.planning/` artifacts.
- Law check (by reading the diff, not grep): the fix removes a bound; it does not redesign how sequence ends are managed, and it does not delete a refusal that a genuinely impossible request still needs.
</verification>

<human_verification>
Native UAT — OWED BY THE USER after execution; the executor must leave it pending and never claim it passed (the executor does not launch the app).
1. THE ORIGINAL REPRO: open an existing project that is currently stuck (the one used since 2026-09-14), extend the sequence (span drag / the action the diagnosis names), and confirm the length actually grows beyond the previous end and persists across save/reopen.
2. Paint layer extent: extend the parent paint span, reopen the Studio, and confirm frames beyond the previous end are addressable — painting or keying there is accepted, not refused.
3. No regression on the closed path: shrink a span then drag it back out (the 260918-o0n case) — still works.
4. No regression on the refusals: a genuinely impossible request (beyond the global maximum) is still refused with its message.
5. If the diagnosis stopped as STRUCTURAL: this section is empty for the fix rows; the user re-classifies the item with the recorded facts.
</human_verification>

<success_criteria>
- The user-blocking sequence-extension refusal is either fixed at a diagnosed, evidence-named path (clamp verdict) or settled with the facts on record (structural verdict) — never left as a plausible story.
- The fix removes a one-way bound without redesigning out_frame management, and grows the addressable extent with the live parent end.
- No existing key, loop, or group is silently dropped; no validator or genuine-capacity refusal is weakened.
- Targeted suite, o0n suite, full suite and `tsc --noEmit` green; scope gate clean; native UAT explicitly pending.
</success_criteria>

<output>
Create `.planning/quick/260920-ji7-sequence-extension-refusal-in-the-main-a/260920-ji7-SUMMARY.md` when done — including the per-candidate verdicts with file:line evidence, the probe source and raw output, the CLAMP-or-STRUCTURAL verdict, the executed or skipped tasks, the gate results, and the pending native UAT rows.
</output>
