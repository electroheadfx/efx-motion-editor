---
phase: quick-260317-l8o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/sequence/KeyPhotoStrip.tsx
  - Application/src/components/sequence/SequenceList.tsx
  - Application/src/lib/shortcuts.ts
autonomous: true
must_haves:
  truths:
    - "Each key photo thumbnail shows an X delete button on hover"
    - "Selected key photo has a thick visible border (ring-2, not ring-1)"
    - "Delete/Backspace key removes the selected key photo, not the whole sequence"
    - "Sequence header row has visible highlight when selected (while a key photo is also selectable separately)"
    - "When a key photo is selected, unselected key photos appear greyed out"
    - "When the sequence header is clicked (no key photo selected), all keys return to normal appearance"
  artifacts:
    - path: "Application/src/components/sequence/KeyPhotoStrip.tsx"
      provides: "Delete button overlay, thick selection ring, greyout styling"
    - path: "Application/src/components/sequence/SequenceList.tsx"
      provides: "Sequence header focus indicator, greyout coordination"
    - path: "Application/src/lib/shortcuts.ts"
      provides: "Delete key targets selected key photo before sequence"
  key_links:
    - from: "Application/src/lib/shortcuts.ts"
      to: "sequenceStore.removeKeyPhoto"
      via: "handleDelete checks selectedKeyPhotoId before selectedSequenceId"
      pattern: "selectedKeyPhotoId\\.peek\\(\\)"
    - from: "Application/src/components/sequence/KeyPhotoStrip.tsx"
      to: "sequenceStore.selectedKeyPhotoId"
      via: "signal read for greyout and selection ring"
      pattern: "selectedKeyPhotoId\\.value"
---

<objective>
Fix six UX issues with the sequence key photo strip: restore the delete button on thumbnails, thicken the selection border, make Delete key remove the selected key photo (not the whole sequence), add visible sequence header highlight, grey out unselected keys when one is selected, and restore normal appearance when the sequence header is selected.

Purpose: Key photo management is broken/degraded -- users cannot delete individual key photos from thumbnails, selection feedback is too subtle, and the Delete key has destructive unintended behavior (deletes entire sequence instead of selected key).
Output: Updated KeyPhotoStrip.tsx, SequenceList.tsx, shortcuts.ts
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/sequence/KeyPhotoStrip.tsx
@Application/src/components/sequence/SequenceList.tsx
@Application/src/lib/shortcuts.ts
@Application/src/stores/sequenceStore.ts
</context>

<interfaces>
<!-- Key store APIs the executor needs -->

From Application/src/stores/sequenceStore.ts:
```typescript
export const sequenceStore = {
  sequences: Signal<Sequence[]>;
  activeSequenceId: Signal<string | null>;
  selectedKeyPhotoId: Signal<string | null>;

  removeKeyPhoto(sequenceId: string, keyPhotoId: string): void;
  selectKeyPhoto(id: string | null): void;
  clearKeyPhotoSelection(): void;
  getActiveSequence(): Sequence | null;
  getById(id: string): Sequence | null;
};
```

From Application/src/stores/uiStore.ts:
```typescript
export const uiStore = {
  selectedSequenceId: Signal<string | null>;
  selectSequence(id: string | null): void;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restore delete button, thicker selection ring, and greyout on KeyPhotoStrip</name>
  <files>Application/src/components/sequence/KeyPhotoStrip.tsx</files>
  <action>
In the `KeyPhotoCard` component:

1. **Restore delete button on hover**: Add an X button in the top-right corner of each thumbnail. It should be hidden by default and appear on hover (use a CSS group pattern: add `group` class to the outer div, then `opacity-0 group-hover:opacity-100` on the button). Import `X` from `lucide-preact`. The button should call `sequenceStore.removeKeyPhoto(sequenceId, keyPhotoId)` with `e.stopPropagation()` to prevent selecting the card. Style: absolute top-0.5 right-0.5, 14x14px, bg-[#000000AA] rounded-full, flex items-center justify-center, X icon size 10.

2. **Thicker selection border**: Change the selection ring from `ring-1` to `ring-2` on the outer div when `isSelected` is true. This makes the accent border noticeably thicker.

3. **Greyout unselected keys**: Read `sequenceStore.selectedKeyPhotoId.value` at the component level. When a key photo IS selected (selectedKeyPhotoId is not null) AND this card is NOT the selected one, apply `opacity-40` to the outer div. When no key photo is selected (null), all cards render at full opacity. When this card IS selected, render at full opacity. Logic: `const anySelected = sequenceStore.selectedKeyPhotoId.value !== null;` then on the outer div: `opacity: (!isSelected && anySelected) ? 0.4 : 1`.
  </action>
  <verify>npm run build (in Application/) succeeds with no type errors. Visual: hover over a key photo thumbnail shows X button; clicking a key photo shows thick ring-2 border; unselected keys grey out.</verify>
  <done>Key photo thumbnails show delete X on hover, selected key has ring-2 accent border, unselected keys grey out when one is selected, all keys return to normal when no key photo is selected.</done>
</task>

<task type="auto">
  <name>Task 2: Sequence header highlight and clear-key-selection on header click</name>
  <files>Application/src/components/sequence/SequenceList.tsx</files>
  <action>
In the `SequenceItem` component:

1. **Sequence header focus/highlight indicator**: The header row already has a left accent bar when `isActive`. Enhance it: when the sequence is active AND no key photo is selected (`sequenceStore.selectedKeyPhotoId.value === null`), add a subtle left border or background highlight to the header row to indicate it has focus. Specifically, when `isActive && !hasSelectedKeyPhoto`, add a 2px left border with `var(--color-accent)` color to the header row div (the one with `class="flex items-center gap-2 h-10"`). When a key photo IS selected, the accent bar still shows but the border is removed (indicating focus has moved to the key photo strip).

2. **Clear key photo selection on header click**: In the `handleSelect` callback, add `sequenceStore.clearKeyPhotoSelection()` call. This ensures clicking the sequence header row deselects any selected key photo, which in turn removes the greyout on the key photo strip (since selectedKeyPhotoId becomes null). The existing code already calls `sequenceStore.setActive(seq.id)` which resets selectedKeyPhotoId to null, so verify this path works. If `setActive` already clears it (it does -- see line 611 of sequenceStore.ts), then the greyout clears automatically. Add an explicit `sequenceStore.clearKeyPhotoSelection()` call anyway for clarity.

3. **Read selectedKeyPhotoId signal**: Add `const hasSelectedKeyPhoto = sequenceStore.selectedKeyPhotoId.value !== null;` at the top of the SequenceItem component so the component re-renders when key photo selection changes.
  </action>
  <verify>npm run build (in Application/) succeeds. Visual: clicking the sequence header clears any key photo selection and shows header focus indicator; all key photos return to normal opacity.</verify>
  <done>Sequence header has visible focus indicator when active and no key photo is selected. Clicking header clears key photo selection and removes greyout from all key photo thumbnails.</done>
</task>

<task type="auto">
  <name>Task 3: Delete key removes selected key photo instead of entire sequence</name>
  <files>Application/src/lib/shortcuts.ts</files>
  <action>
In the `handleDelete()` function in shortcuts.ts, add a new check AFTER the keyframe diamond check but BEFORE the layer/sequence checks. The new block should:

1. Check if a key photo is currently selected: `const selectedKpId = sequenceStore.selectedKeyPhotoId.peek();`
2. If selectedKpId is not null, find which sequence it belongs to: iterate `sequenceStore.sequences.peek()` to find the sequence containing a keyPhoto with that id.
3. Call `sequenceStore.removeKeyPhoto(owningSequenceId, selectedKpId)` to remove just that key photo.
4. Return early so the delete does NOT fall through to delete the entire sequence.

The updated handleDelete flow should be:
```
1. Check keyframe diamonds (existing)
2. Check selected key photo (NEW) -- delete just the key photo
3. Check selected layer (existing)
4. Check selected sequence (existing) -- only reached if no key photo selected
```

This ensures Delete/Backspace removes the focused key photo when one is selected, and only deletes the whole sequence when the sequence header has focus (no key photo selected).
  </action>
  <verify>npm run build (in Application/) succeeds. Manual test: select a key photo, press Delete -- only that key photo is removed. Click sequence header (clears key photo selection), press Delete -- whole sequence is removed.</verify>
  <done>Delete/Backspace key removes the selected key photo when one is selected. When no key photo is selected (sequence header focused), Delete removes the whole sequence as before.</done>
</task>

</tasks>

<verification>
1. `cd Application && npm run build` -- compiles with no errors
2. Visual check: hover key photo thumbnail shows X delete button
3. Visual check: selected key photo has thick ring-2 accent border
4. Visual check: unselected keys greyed out when one is selected
5. Visual check: clicking sequence header clears greyout, shows header focus
6. Keyboard: Delete key removes selected key photo, not sequence
7. Keyboard: Delete key removes sequence when header is focused (no key photo selected)
</verification>

<success_criteria>
- Delete button visible on key photo thumbnail hover, clicking it removes that key photo
- Selected key photo border is noticeably thicker (ring-2)
- Delete/Backspace removes selected key photo, not the whole sequence
- Sequence header shows focus indicator when active and no key photo selected
- Unselected key photos greyed out (opacity 0.4) when one is selected
- All key photos return to normal opacity when sequence header is clicked
</success_criteria>

<output>
After completion, create `.planning/quick/260317-l8o-fix-sequence-key-photo-ux-restore-delete/260317-l8o-SUMMARY.md`
</output>
