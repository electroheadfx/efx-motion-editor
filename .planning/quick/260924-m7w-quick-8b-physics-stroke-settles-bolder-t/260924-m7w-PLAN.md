---
phase: quick-260924-m7w
plan: 260924-m7w
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts
  - packages/efx-physic-paint/src/core/wet-layer.ts        # CONDITIONAL — edit only if diagnosis verdict = seam (a) deposit radius
  - packages/efx-physic-paint/src/core/fluids.ts           # CONDITIONAL — edit only if diagnosis verdict = seam (b) fluid spread
  - packages/efx-physic-paint/src/render/compositor.ts     # CONDITIONAL — edit only if diagnosis verdict = seam (c) display composite
  - .planning/quick/260924-m7w-quick-8b-physics-stroke-settles-bolder-t/260924-m7w-SUMMARY.md
autonomous: true
requirements: []
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Physics mode at default water: the settled stroke silhouette lands on the drawn ribbon envelope within antialiasing tolerance; pressure taper still reads."
    - "Diffusion still does its texture job: soft/wet edges and granulation remain visible on the settled mark — not a hard-edged stamp."
    - "waterAmount is the visible spread control: raising it deliberately widens the settled mark, lowering tightens toward the ribbon; no hidden constant inflation at any water level."
    - "Deterministic stop-motion law: the same gesture with the same params settles to a byte-identical footprint, pose after pose — no boil."
    - "Normal mode (basic brush raster), brush cursor ring, and committed stroke geometry are unchanged; the 260924-koa preview ribbon (canvas.ts / strokePreviewRibbon) is untouched."
    - "The guilty seam verdict — wet-layer deposit radius vs diffusion/fluids spread vs compositor display — plus the stage-wise width measurements (default water, waterAmount/speed/paper sweeps) are recorded in 260924-m7w-SUMMARY.md."
  artifacts:
    - packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts — stage-wise width measurement harness + the three RED pins (MUST be .test.ts)
    - Exactly ONE of packages/efx-physic-paint/src/core/wet-layer.ts | core/fluids.ts | render/compositor.ts edited, chosen by the diagnosis verdict (never all three)
    - .planning/quick/260924-m7w-quick-8b-physics-stroke-settles-bolder-t/260924-m7w-SUMMARY.md — carries the verdict, measurements, RED evidence, gates
  key_links:
    - "packages/efx-physic-paint/src/core/wet-layer.ts depositToWetLayer — seam (a): footprint bounded by radius + ff falloff; D-08 paper-height adsorption modulates deposit alpha inside that radius"
    - "packages/efx-physic-paint/src/core/fluids.ts fluidPhysicsStep — seam (b): height equalization + darkenEdges + velStep/advect carry pigment outward between deposit and settle"
    - "packages/efx-physic-paint/src/render/compositor.ts wetDisplayAlpha — seam (c): Beer-Lambert curve can lift a faint low-density tail above DRY_ALPHA_THRESHOLD so the visible silhouette reads wider than the dense core"
    - "packages/efx-physic-paint/src/core/diffusion.ts physicsStep — the settle orchestrator the harness must drive the same way the engine does (fluidPhysicsStep → dryStep)"
    - "packages/efx-physic-paint/src/engine/EfxPaintEngine.ts:599 default waterAmount: 50 and :1130 physicsStep call — the reference defaults for the harness parameters"
    - "Preview law locked by 260924-koa: packages/efx-physic-paint/src/render/canvas.ts + canvas.strokePreviewRibbon.test.ts are read-only for this quick"
---

<objective>
Diagnose WHY the Physics-mode settled stroke settles bolder than the drawn preview ribbon, then bound the settled footprint back onto the ribbon envelope without killing diffusion's texture job.

Purpose: The drawn ribbon is the CONTRACT (user decision, 2026-09-24). Diffusion keeps wet edges, granulation, softening — but must NOT inflate the footprint. Which seam inflates (deposit radius / fluid spread / display composite) is an empirical question, so diagnosis comes before any production edit; if bounding the spread would require re-architecting the fluid sim, STOP and report instead of coding.

Output: a stage-wise width measurement harness with three RED pins committed before any production edit, a single-seam bounding fix chosen by the recorded verdict, and measurements + verdict in the SUMMARY.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Proven test invocation for this package (from quick 260924-koa SUMMARY):
#   cd packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run <paths>
# (pnpm --filter @efxlab/efx-physic-paint exec vitest run fails — vitest is only a devDependency of app/)

# Preview ribbon law (LOCKED, read-only here): .planning/quick/260924-koa-quick-8-stroke-preview-as-a-pressure-var/260924-koa-SUMMARY.md

# Seam files observed live at planning time (edit authority limited to these three, verdict-conditional):
#   packages/efx-physic-paint/src/core/wet-layer.ts      (depositToWetLayer, D-08 adsorption)
#   packages/efx-physic-paint/src/core/fluids.ts         (fluidPhysicsStep: equalization, darkenEdges, advect)
#   packages/efx-physic-paint/src/render/compositor.ts   (wetDisplayAlpha, compositeWetLayer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seam diagnosis — stage-wise width measurement, verdict, STOP gate</name>
  <files>packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts</files>
  <action>Build a headless measurement harness (test-only, zero production edits) that reproduces the inflation and names the guilty seam. Setup: small canvas (e.g. 128x64), one straight horizontal stroke at mid-height, uniform pressure (mouse, p=1), default brush radius, default water (waterAmount 50 per EfxPaintEngine defaults), paperHeight null and a synthetic height map variant. Drive the real pipeline stages: deposit via depositToWetLayer, settle via the same fluidPhysicsStep tick count the engine runs between finalize and settle (mirror EfxPaintEngine's physics drive — localFluidPhysicsStep continuation or fluidPhysicsStep directly), then visibility via wetDisplayAlpha (pure function — no canvas needed). Define a cross-section width at stroke midpoint at three stages: W_deposit (wet.alpha after deposit), W_settle (wet.alpha after K settle ticks), W_visible (pixels whose wetDisplayAlpha result is at/above a visible threshold you calibrate, e.g. ~5% of 255). The ribbon reference width is 2*radius (uniform pressure). Compute and print (console) the three stage widths plus deltas: W_deposit - 2r names seam (a) if positive at deposit; W_settle - W_deposit names seam (b); W_visible - W_settle names seam (c). Sweep and record: waterAmount ∈ {10, 50, 90}, two sample spacings (dense vs sparse points — speed proxy), paperHeight null vs synthetic — same three widths each cell. Verdict rule: the seam whose delta accounts for the majority of (W_visible - 2r) at default water is GUILTY. Write the verdict and the full measurement table into 260924-m7w-SUMMARY.md (create it now with a Diagnosis section; Tasks 2-3 append later). GUARDRAILS: do not edit any production file in this task; do not modify canvas.ts / strokePreviewRibbon (260924-koa locked); do not touch brush/paint.ts (Normal mode). STOP CLAUSE (mandatory): if the guilty seam is (b) and bounding W_settle back within tolerance of the ribbon would require re-architecting the Stam fluid solver (changing its algorithm/structure — not bounding magnitude with existing knobs: diffusion extent, tick count, wetness-gated spread, edge clamp, display threshold), then HALT: commit the harness + measurements + verdict, append a "STOP — re-architecture required" section to the SUMMARY laying out the user's choice (a visible "spread" parameter vs accepting a fatter mark), and return control without writing the RED pins or any fix. A bounding fix that stays inside the existing architecture (e.g. clamping outward transport, wetness/water-gated spread limit, deposit falloff aligned to the ribbon) does NOT trigger the STOP.</action>
  <verify>
    <automated>cd packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/physicsSettledFootprint.test.ts</automated>
  </verify>
  <done>Harness runs and prints stage widths for every sweep cell; SUMMARY.Diagnosis contains the guilty-seam verdict (a/b/c) and the measurement table; zero production files modified (git status shows only the test file + SUMMARY); STOP clause evaluated and either cleared (proceed to Task 2) or triggered (halt, report).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — three footprint pins committed before any production edit</name>
  <files>packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts, .planning/quick/260924-m7w-quick-8b-physics-stroke-settles-bolder-t/260924-m7w-SUMMARY.md</files>
  <behavior>
    - Pin 1 (expect FAIL at base): at default water, W_visible is within bounded tolerance of the drawn ribbon width 2r (tolerance calibrated in Task 1, e.g. <= 2px + antialiasing margin) — and the harness must demonstrate today it is measurably larger (assert current width > tolerance in a clearly-labeled base-state expectation log so the RED failure message shows the inflation, not an vacuous pass)
    - Pin 2 (expect PASS-or-FAIL at base, pin the contract): raising waterAmount increases settled W_visible monotonically — width(90) > width(50) > width(10)
    - Pin 3 (expect PASS at base, control): two runs of identical deposit+settle inputs produce byte-identical footprint buffers (digest equality over wet.alpha, following the fluids.continuation.test.ts digest pattern)
  </behavior>
  <action>Convert the Task 1 harness cells into three named contract tests encoding the pins above, using the calibrated tolerance from the measurements. Commit RED with ONLY this test file (and the SUMMARY diagnosis already staged) — production untouched. Record in SUMMARY.RED-evidence: the command, the pass/fail counts per pin, and the observed base widths proving pin 1 fails because the settled mark is wider than the ribbon (not because of a broken harness). If pin 2 unexpectedly fails at base (width non-monotonic in water), that is additional diagnosis evidence — log it, do not fix anything yet.</action>
  <verify>
    <automated>cd packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/physicsSettledFootprint.test.ts</automated>
  </verify>
  <done>RED commit exists containing only test/SUMMARY changes; pin 1 demonstrably fails at base with the measured inflation visible in the failure output; pins 2 and 3 state the contract; SUMMARY records the RED evidence; git diff of production files (wet-layer.ts, fluids.ts, compositor.ts, canvas.ts, paint.ts) is empty.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — bound the guilty seam onto the ribbon, full gates, SUMMARY close-out</name>
  <files>packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts, packages/efx-physic-paint/src/core/wet-layer.ts, packages/efx-physic-paint/src/core/fluids.ts, packages/efx-physic-paint/src/render/compositor.ts, .planning/quick/260924-m7w-quick-8b-physics-stroke-settles-bolder-t/260924-m7w-SUMMARY.md</files>
  <action>Apply the bounding fix at EXACTLY the seam named by the Task 1 verdict — edit at most one of wet-layer.ts (align deposit falloff/radius to the ribbon envelope), fluids.ts (bound outward transport: wetness/water-gated spread limit, extent clamp around the deposited footprint — keep equalization, darkenEdges, advection and their texture contributions alive), compositor.ts (keep the Beer-Lambert texture but stop the faint tail from reading as silhouette width above the ribbon). Water must remain the spread knob: at water 10 the mark tightens toward the ribbon, at water 90 it deliberately widens — no constant offset that inflates every level equally. Texture guard: wet edges and granulation must survive — the fix bounds EXTENT, it does not threshold away the soft edge character (do not turn diffusion off, do not clamp to a hard binary stamp). Determinism: no randomness, no time-based branching in the fix path. Never touch canvas.ts / strokePreviewRibbon, brush/paint.ts (Normal mode), or engine finalization. Then run pin 1-3 green, the package suite, and the app suite; append Task 2/3 results, final measurements (before/after widths at each water level), and the confirmed verdict to SUMMARY.</action>
  <verify>
    <automated>cd packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/physicsSettledFootprint.test.ts && ../../app/node_modules/.bin/vitest run && cd ../../app && npx vitest run</automated>
  </verify>
  <done>Pin 1 passes: default-water settled silhouette within tolerance of 2r; pin 2 monotonic water spread; pin 3 determinism; package suite green (14/14 files — the pre-existing liveAlphaCache failure from 260924-koa remains the only known deferred item if still failing at base); app suite green; exactly one seam file changed; canvas.ts, paint.ts, strokePreviewRibbon test unchanged (git diff empty for those); SUMMARY carries verdict + before/after measurements + RED evidence; no new UI, no new dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production pipeline | The harness drives real deposit/fluids/compositor code headlessly; inputs are synthetic buffers, no user content or network data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m7w-01 | Denial of Service | physicsSettledFootprint.test.ts sim ticks | low | mitigate | Cap canvas at 128x64 and settle tick count to the engine-equivalent K; suite must stay < 60s |
| T-m7w-02 | Tampering | bounding fix in fluids.ts | medium | mitigate | Fix gated by three pins (envelope, monotonic water, determinism) + full package/app suites; texture guard asserted so diffusion cannot be silently disabled |
| T-m7w-03 | Information Disclosure | none | low | accept | No network, auth, file IO, or schema surface in this quick |
| T-m7w-SC | Tampering | npm installs | high | mitigate | No package installs in this quick; any install attempt is out of scope and must be rejected |
</threat_model>

<verification>
1. Diagnosis first: SUMMARY.Diagnosis exists with verdict + measurement table before any production edit (Task 1 commit is test+SUMMARY only).
2. RED before GREEN: pin-1 base failure evidence recorded; production diff empty at the RED commit.
3. Gates: footprint pins green; package suite via app vitest binary; app suite via npx vitest run (never watch).
4. Guardrail sweep: git diff empty for packages/efx-physic-paint/src/render/canvas.ts, canvas.strokePreviewRibbon.test.ts, src/brush/paint.ts.
5. STOP clause honored: if triggered, no fix tasks ran and the SUMMARY carries the re-architecture report.
</verification>

<success_criteria>
Physics mode at default water settles onto the preview-ribbon envelope within antialiasing tolerance; water up/down visibly controls spread; identical gestures settle byte-identical (no boil); wet edges and granulation still present; Normal mode, cursor, and preview untouched; verdict + measurements visible in SUMMARY. Final status: automated-ready — native UAT (5 rows: default-water envelope match side-by-side, Normal unchanged, water up/down spread, two-stroke held-pose identity, wet edges/granulation + cursor unchanged) is run by the user after GREEN.
</success_criteria>

<output>
Create `.planning/quick/260924-m7w-quick-8b-physics-stroke-settles-bolder-t/260924-m7w-SUMMARY.md` when done (Task 1 seeds the Diagnosis section; Task 3 completes it).
</output>
