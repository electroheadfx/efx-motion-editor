---
phase: quick-260924-d6l
plan: 260924-d6l
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/shared/NumericStepper.test.tsx
  - app/src/components/shared/NumericStepper.tsx
autonomous: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A stepper given only a constant step moves by exactly that step on every press — including at values inside the grain band ]0.5, 2.0[ (a 0.01 field at 1.00 emits 1.01, not a band jump) — and typed commits snap to the field's grid."
    - "The paper grain scale field still resolves its band-dependent step from the live value on each emission (interior 0.5, edges 0.1) and keeps free, comma-tolerant typed entry inside the shared grain bounds — exactly as 260923-bcm shipped."
    - "A source sweep over every NumericStepper/NumericInput call site finds the variable-step resolver and free-entry options only at the paper grain scale call site."
    - "No field's declared min/max/step changed; fps keeps 0.5 (D-24); the clamp+round commit path, hold-to-repeat, and coalesced undo contracts are intact."
  artifacts:
    - app/src/components/shared/NumericStepper.test.tsx
    - app/src/components/shared/NumericStepper.tsx
    - .planning/quick/260924-d6l-quick-7-urgent-numeric-stepper-the-grain/260924-d6l-RED-EVIDENCE.json
  key_links:
    - "NumericStepper stepBy/commitStepperInput fall back to the field's constant step and snap-to-grid when the optional per-call-site policies are omitted"
    - "PhysicsPaintTopBar grain-scale element is the sole consumer of the variable-step resolver and free-entry options"
    - "numericStepperSweep covers every call-site file, not only the six swept surfaces"
---

<objective>
Lock the shared numeric stepper's constraint-injection contract with three RED-first pins — a non-paper constant-step field increments by exactly its declared step (no adaptive policy), the paper grain scale still resolves its band-dependent step from the live value, and a sweep finds the variable-step/free-entry options only at the paper-grain call site — then make the classic-default contract explicit in the shared component and re-scope any leak the pins actually surface.

Purpose: The 260923-bcm grain-scale exception (variable step + free typed entry) must live only on the paper field; every other numeric field keeps its own constant increment (Scale 0.01, Rot 1, crop 0.01, fps 0.5 per D-24, audio fields), and the shared control's contract — omitted policy = classic constant-step, snap-to-grid input — is explicit and guarded by tests.

Diagnosis verdict (static sweep, done at planning time — main @ d90fbc02): all four leak hypotheses named in the defect were checked against this tree and refuted: (a) shared default — the exception options are optional with no defaults; the press path uses the field's step when the resolver is absent, and the commit path takes the free-entry branch only when opted in; (b) swept call-site rewrite — full-repo grep shows the only JSX call site passing either option is the grain element in PhysicsPaintTopBar.tsx, and both bcm follow-up commits touched only TopBar + NumericStepper + TopBar tests; (c) formatter/snap change — clamp and display behave identically for opted-out fields; (d) drag-scrub — NumericInput predates the bcm work untouched and passes neither option. Declared steps across call sites are all literals. Planning-time run: 34/34 green (18 stepper + 16 topbar). The app-wide leak is therefore not locatable statically; per the defect's own "confirm against the running app before changing anything" and mutable-scope discipline, the three pins are written unconditionally, the production re-scope is conditional on a pin surfacing an offender, and the user's live observation is an explicit UAT row with a report-don't-amputate stop clause.

Output: three contract pins committed before any production edit, an honest RED-evidence record, an explicit classic-default contract in the shared component (plus conditional re-scope), and a full-suite green ready for native UAT.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quick/260923-bcm-paper-grain-scale-in-tools-canvas-export/260923-bcm-SUMMARY.md

Grounding notes (verified at planning time — trust these, do not re-derive):

- Harness: vitest collects only `src/**/*.test.ts`; the stepper suite lives in `NumericStepper.test.tsx` and runs through the existing launcher `NumericStepper.test.tsx.test.ts` (plain re-import — no new launcher needed). Node environment, no jsdom. The suite stubs preact hooks (useRef/useEffect) and drives vnodes by direct invocation — reuse its existing `renderStepper`, `button`, `input`, `pointerDown` helpers. Run: `pnpm --filter efx-motion-editor exec vitest run <files>` (never watch); types: `pnpm --filter efx-motion-editor run typecheck`.
- Float pin math confirmed empirically at planning time against the exact clamp body: (1.0+0.01, 0.01)→1.01, (1.0−0.01, 0.01)→0.99, (0.5+0.01)→0.51, (1.0+0.5, 0.5)→1.5, (2.0+0.1, 0.1)→2.1, (0.5−0.1, 0.1)→0.4, (1..5−0.5, 0.5)→1.0, typed 1.2345@0.01→1.23, typed 45.5@1→46 — all strict-equality safe after the existing toFixed(6) normalization.
- Grain rule as UAT-approved in 260923-bcm (freeze exactly): the resolver returns 0.5 strictly inside ]0.5, 2.0[ and 0.1 at/below 0.5 and at/above 2.0; free typed entry clamps to the shared grain bounds without grid snap; comma-tolerant parse. The defect text's "0.1 or 0.2 outside" is a loose recollection — the shipped/approved rule is 0.5 interior / 0.1 at the edges; pins assert the shipped rule (guardrail: keep the rule 260923-bcm shipped).
- Existing coverage to EXTEND, not duplicate: `PhysicsPaintTopBar.test.ts` pins the grain element's props (resolver identity, free-entry flag, bounds, step), walks the band rule as a pure function, and covers free/comma/clamped commits through the helpers — but no test drives a stepper's press path with a per-emission resolver. `NumericStepper.test.tsx` pins step-1 and step-0.5 presses (fps / D-24) and a six-file numericStepperSweep (raw native number inputs, shared-treatment reference, explicit step) — no step-0.01 press pin, no snap-vs-free component pin, no call-site scope sweep for the exception options.
- Call-site inventory (planning-time): 12 files contain NumericStepper/NumericInput JSX instantiations (TransitionProperties, SidebarFxProperties, InlineColorPicker, SidebarProperties, PaintProperties, AudioProperties, NumericInput, ColorPickerModal, NewProjectDialog, KeyPhotoStrip, PhysicsPaintTopBar, PhysicsPaintWorkflowStrip). Only PhysicsPaintTopBar passes the exception options; the grain resolver has no other importer; no prop-spread instantiations exist.
- Sweep scan set: recursive `src/**/*.tsx`, exclude `.test.` files, include files whose source matches a NumericStepper/NumericInput JSX tag; the shared component defines but never instantiates itself, so its definition tokens stay out of the scan naturally. Allowlist for the exception options: `PhysicsPaintTopBar.tsx` only.
- Commit style: `test(260924-d6l): … — RED` then `fix(260924-d6l): …` / `docs(260924-d6l): …` (see recent quicks). RED-evidence TAP leaf-flatten adapter pattern: same as documented in the 260923-bcm SUMMARY. No server starts — the user runs live UAT themselves.
- Preact + @preact/signals only; no new component, no backward-compat code, no package installs.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-first — pin constant-step, grain live-value bands, and call-site scope</name>
  <files>app/src/components/shared/NumericStepper.test.tsx</files>
  <behavior>
    - Pin 1 — classic field, no exception options: a stepper with only value/step 0.01/min/max, pressed at value 1.00 (deliberately inside the grain band, where an escaped adaptive policy would emit a band jump) emits exactly 1.01 on + and exactly 0.99 on −; pressed at 0.50 emits exactly 0.51. Snap-not-free on typed commit: blur-commit "45.5" on a step-1 field emits 46; blur-commit "1.2345" on the 0.01 field emits 1.23.
    - Pin 2 — grain option shape through the real press path: a stepper driven with the grain field's exact props (resolver imported from PhysicsPaintTopBar, free-entry flag, grain bounds) emits, per single press: + at 1.0 → 1.5, + at 1.5 → 2.0, + at 2.0 → 2.1, − at 1.5 → 1.0, − at 0.5 → 0.4 — each press resolving the step from that live value. Through the component's blur-commit wiring: "1.3" stays 1.3; "2,2" → 2.2; "0" → the lower grain bound.
    - Pin 3 — call-site scope sweep: every non-test `.tsx` under `src` containing a NumericStepper/NumericInput JSX instantiation may contain the exception-option identifiers only in `PhysicsPaintTopBar.tsx`; the offender list (file paths) must be empty. Extends the existing numericStepperSweep describe; the six-file list assertions stay untouched.
    - Expected-at-planning: all three pins pass at birth per the static verdict; any failure is the located leak, not scaffolding.
  </behavior>
  <action>Create the three pins as a new describe block in `NumericStepper.test.tsx` before touching any production file, reusing the suite's existing render/press/input helpers and its fake-timer setup. Pin 1: `renderStepper` overrides with step 0.01, min 0, max 2 and no exception options; drive the Increase/Decrease buttons with `pointerDown`; assert emissions with strict `toBe` (math verified at planning time). For the typed-commit pins use the existing blur idiom with a step-1 field (value 45, min 0, max 60) and the 0.01 field. Pin 2: import `grainScaleStep` from `../physic-paint/view/PhysicsPaintTopBar` (test-only cross-import; the TopBar test already imports the shared module the other way, no cycle) and render with resolver + free-entry flag + grain bounds exactly as the grain element passes them; press at each band value listed in the behavior; blur-commit the free/comma/bound cases through the component's input handlers. Pin 3: inside the numericStepperSweep describe, walk `src` recursively with `node:fs` (readdirSync withFileTypes), filter `.tsx` files excluding `.test.` whose source matches a NumericStepper or NumericInput JSX tag, flag exception-option identifier matches outside `src/components/physic-paint/view/PhysicsPaintTopBar.tsx`, and `expect(offenders).toEqual([])` with the offending paths in the failure message. Do not modify any production file in this task. Run the suite, record the actual verdict to `.planning/quick/260924-d6l-quick-7-urgent-numeric-stepper-the-grain/260924-d6l-RED-EVIDENCE.json` (verdict `RED_EVIDENCE_OK` when at least one pin fails and its output names the locus; verdict `PINS_GREEN_AT_BIRTH` when all three pass — planning-time analysis predicted green-at-birth; never manufacture a red by weakening a passing assertion), and commit the pins plus evidence.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/shared/NumericStepper.test.tsx.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts</automated>
  </verify>
  <done>All three pins exist and run through the launcher; RED-EVIDENCE.json records the real verdict with failing-pin output (or the green-at-birth note) preserved; no production file was touched; committed as the first change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — explicit classic-default contract, conditional re-scope of whatever the pins flagged</name>
  <files>app/src/components/shared/NumericStepper.tsx</files>
  <behavior>
    - Behaviour: the shared component's docs state the full injection contract — min/max/step describe the field; the variable-step resolver and free typed entry are opt-in per call site; omitting them yields the classic constant-step, snap-to-grid input; the paper grain scale is the only current consumer of the options.
    - Behaviour (conditional on Task 1): any call-site file the sweep flagged loses the exception options while keeping its declared step/min/max byte-for-byte; the grain element in PhysicsPaintTopBar is never edited.
    - Behaviour (conditional on Task 1): if a behavioural pin failed, the failing path returns to the classic default while the grain option shape still passes Pin 2 unchanged.
    - Invariant: grain band rule, free entry, comma parsing, clamp+round commit path, hold-to-repeat timing, coalescing brackets, and D-24 fps 0.5 all unchanged.
  </behavior>
  <action>Always: tighten the NumericStepper.tsx header and props documentation so the contract named in the objective is explicit in the file itself (constraints injected per call site; omitted policy = classic constant-step, snap-to-grid; grain scale = sole current exception). Then act strictly on Task 1's evidence: if the sweep named offending call-site file(s), remove the exception options from those sites only — never from the grain element, never touching any declared step/min/max — and add each additionally-edited file to the summary's files list. If a behavioural pin failed, restore the classic default at exactly the failing path among the four hypotheses (opt-out default on the press/commit path, formatter/snap behaviour, or the label drag-to-scrub wrapper), keeping the grain option shape and every invariant above intact. If all pins passed at birth, stop at the documentation edit — no behavioural code change (scope, don't amputate: the guardrail forbids altering working increment behaviour). No new component, no package installs, no changes to declared constraints anywhere.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run src/components/shared/NumericStepper.test.tsx.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts && pnpm --filter efx-motion-editor run typecheck</automated>
  </verify>
  <done>The contract is explicit in the shared component; every leak Task 1 surfaced is re-scoped with all three pins green; the grain element and its behaviour are untouched (Pin 2 still passes); no declared step/min/max differs from before; typecheck clean; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression sweep before native UAT</name>
  <files>app/src/components/shared/NumericStepper.test.tsx</files>
  <action>Run the whole vitest suite once to confirm no other source-shape pin or behavioural test objected to the doc edit or any conditional re-scope (other suites read the shared stepper and the swept call-site files). If an unexpected failure appears, fix it only when it asserted pre-contract behaviour that this quick intentionally changes; never weaken the three new pins. Do not expand scope. Do not start any server — leave the app ready for the user's live checks and surface the native UAT rows in the return message, including the stop-and-report row. Record any file edited beyond the two listed in frontmatter in the summary.
  </action>
  <verify>
    <automated>pnpm --filter efx-motion-editor exec vitest run && pnpm --filter efx-motion-editor run typecheck</automated>
  </verify>
  <done>Entire suite green; typecheck green; three pins passing; UAT rows listed for the user; automated-ready — not claimed done until live UAT passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call-site props → shared stepper | Each field injects its constraints (step/min/max, optional policies); the shared component must treat the options as opt-in, never as ambient defaults |
| typed field text → commit path | User-typed strings flow through parse → clamp/snap → onChange; the classic path must keep the shared clamp+round contract (T-52.2-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d6l-01 | Tampering | NumericStepper press/commit defaults | medium | mitigate | Task 1 pins the classic path and the grain option shape BEFORE any edit; Task 2 changes only what evidence flags; grain element excluded from edits by guardrail |
| T-d6l-02 | Repudiation | user's live observation vs source | medium | accept | Static sweep found no leak; pins may pass at birth — UAT row 5 is an explicit report-don't-amputate stop clause instead of speculative code changes |
| T-d6l-03 | Denial of Service | hold-to-repeat / coalesced undo timing | low | mitigate | Repeat-delay/interval constants and coalescing brackets named as invariants; untouched by both hypotheses; full-suite sweep in Task 3 |
| T-d6l-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan — package-legitimacy gate not applicable; zero new dependencies |
</threat_model>

<verification>
1. `pnpm --filter efx-motion-editor exec vitest run src/components/shared/NumericStepper.test.tsx.test.ts src/components/physic-paint/view/PhysicsPaintTopBar.test.ts` — three new pins plus the existing grain suite green.
2. `pnpm --filter efx-motion-editor exec vitest run` — full suite green.
3. `pnpm --filter efx-motion-editor run typecheck` — clean.
4. Native UAT (user, after GREEN; user runs the app — do not start a server) — 5 rows:
   - Row 1: sidebar Scale (0.01) press + near 1.00 → 1.01 and − → 0.99 (no 0.5-band jumps); Rot (1) and crop (0.01) each move by their own step.
   - Row 2: typed commit snaps on a classic field — Rot "45.5" commits 46; unparseable text reverts.
   - Row 3: grain scale regression (260923-bcm unchanged) — 1.0 + → 1.5, 1.5 − → 1.0, 0.5 − → 0.4, 2.0 + → 2.1; typed 1.3 sticks; "2,2" commits 2.2; out-of-range clamps to the shared bounds.
   - Row 4: fps steps 0.5 (D-24); audio fields keep 1 / 0.1; holding a button repeats at the same step and collapses to one undo entry.
   - Row 5 (stop clause): if any non-paper field still shows band-style (0.5 / 0.1) increments or non-snapping typed entry after these pins pass — change nothing further; report the exact field, starting value, and press sequence (observation not reproducible from this tree — stale bundle/worktree check before any new plan).
</verification>

<success_criteria>
- The three RED-first pins exist, pass, and were committed before any production edit; the evidence file records the honest verdict.
- The shared control's contract is explicit in code and docs: constant-step, snap-to-grid by default; options opt-in; grain scale the only consumer — proven by the call-site sweep.
- No declared min/max/step changed anywhere; grain behaviour matches 260923-bcm exactly; T-52.2-08, hold-to-repeat, coalescing, comma parsing, and D-24 fps intact.
- Full vitest suite + typecheck green; the user's 5 native UAT rows completed (row 5 exercised only if a mismatch persists).
</success_criteria>

<output>
Create `.planning/quick/260924-d6l-quick-7-urgent-numeric-stepper-the-grain/260924-d6l-SUMMARY.md` when done
</output>
