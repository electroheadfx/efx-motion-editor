---
status: passed
approved: 2026-07-16
scope: native-mounted A-M
production_head: d6731712
---

# Quick 260716-dby Native UAT

## Result

The user explicitly approved the complete mounted native UAT A–M on 2026-07-16 and authorized post-UAT regression creation, final verification, GSD quick-data updates, and commits.

## Approved scenarios

- A. Unsaved project gating and `Save the project first.` reason.
- B. First autonomous script save, managed JSON creation, and complete newest-first row.
- C. Multiple independent presets with deterministic `-2` and `-3` naming.
- D. Valid capture-time WebP thumbnail after the native encoder correction.
- E. Independent scripts from other real frames with distinct provenance and previews.
- F. Project close/reopen filesystem rediscovery without `.mce` registry or index.
- G. Explicit Load followed by the approved Apply path with current Motion, per-brush Undo/Redo, additive repaint, ownership, and final publication intact.
- H. Loaded clipboard independence from durable Rename and Delete.
- I. Inline Rename semantics, stable physical filename, order stability, and reopen persistence.
- J. Refresh discovery and nonfatal malformed-file isolation.
- K. Save As source preservation, destination migration/collision behavior, and destination Load/Apply.
- L. Four-tab/six-control responsive and accessible SCRIPTS UI at supported widths.
- M. Existing Copy/Apply/Discard/Undo/Redo, rapid paint, navigation, cached repaint, generated-frame rejection, interpolation, preview, and export smoke behavior.

## Measured failures corrected before approval

1. Packaged WKWebView could not produce actual WebP through `canvas.toBlob('image/webp', 0.8)`.
   - Fixed by `48427a15`: bounded script-thumbnail RGBA is encoded as lossy WebP through an operation-correlated parent/native boundary.
2. The persistent library controller retained its initial `Unavailable` bridge mode and reported `Project script library is unavailable` after thumbnail encoding.
   - Fixed by `d6731712`: each library request resolves the current bridge mode and redetects Tauri at dispatch time.

## Authorization boundary

This approval unlocks the deferred regression work mapped as requirements 1–47 in `260716-dby-PLAN.md`. It does not relax the approved production behavior; tests must encode the mounted contract rather than adapt it.
