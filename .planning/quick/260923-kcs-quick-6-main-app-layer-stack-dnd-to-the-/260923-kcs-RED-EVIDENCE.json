{
  "task": "quick-260923-kcs Task 1 - RED: layer-stack bottom drop, identity names, top insert, inline rename pins",
  "command": "pnpm --filter efx-motion-editor exec vitest run --reporter=tap src/lib/fxReorder.test.ts src/lib/frameMap.test.ts src/stores/sequenceStore.test.ts src/components/timeline/TimelineInteraction.test.ts",
  "exitCode": 1,
  "targetTest": "keeps each FX stack header label equal to its own sequence name through reverse and deletion",
  "targetFile": "src/lib/frameMap.test.ts",
  "expected": "Every fxTrackLayouts row's headerLabel equals that row's own sequenceName in any array order: seeding Physics A / Physics B (physic-paint) + Grain Stack then reversing the array must keep physics-a -> 'Physics A', physics-b -> 'Physics B', grain -> 'Grain Stack'; deleting physics-a must leave physics-b -> 'Physics B'. At base the positional PPaint ordinal renumbers the physic rows (PPaint #1/#2 recomputed from array order), so the identity assertion fails.",
  "actual": "AssertionError: expected 'PPaint #1' to be 'Physics A' \u2014 frameMap.ts:332 headerLabel: primaryLayer?.type === 'physic-paint' ? `PPaint #${++physicPaintOrdinal}` : seq.name renumbers positionally on every render. Companion pins in the same run fail as expected: fxReorder.test.ts fails to load (module ./fxReorder does not exist), 4x nextFreeLayerName is not a function, 2x top-insert append-end mismatches, rename accepts 'bad\\x07name', and 3 source-shape pins (missing dblclick listener / resolver / fxRenameEdit wiring). Control legs green: the pre-existing frameMap renumber test and the rename->reorder store control both pass at base. Summary: 11 failed | 47 passed | 12 todo (70) across the four files.",
  "note": "Vitest's TAP reporter nests leaf tests under file-level subtests and never emits the node-style '# tests/# pass/# fail' summary block the gate parses; this record's output is the leaf-level TAP flattened to column 0 with the summary block derived from those leaf lines (same reconstruction precedent as 260923-fhn-RED-EVIDENCE.json). Raw nested tap preserved at rawTapPath; default-reporter run (exit 1, 'Tests 11 failed | 47 passed | 12 todo') captured at defaultReporterLogPath before any production edit. Commit ordering: test commit precedes feat commits.",
  "rawTapPath": "/tmp/kcs-red.tap",
  "defaultReporterLogPath": "/tmp/kcs-red-default.log",
  "output": "TAP version 13\nnot ok 1 - FX stack header labels are identity, not position (260923-kcs) # time=3.27ms\nnot ok 2 - keeps each FX stack header label equal to its own sequence name through reverse and deletion # time=3.24ms\nnot ok 3 - layer stack naming and placement (260923-kcs) # time=4.93ms\nnot ok 4 - nextFreeLayerName \u2014 first free Layer N index # time=2.51ms\nnot ok 5 - returns Layer 1 for an empty stack # time=2.06ms\nnot ok 6 - returns the index after a dense Layer run (spec example) # time=0.17ms\nnot ok 7 - returns the first FREE index, not max+1 # time=0.14ms\nnot ok 8 - ignores names that do not match the Layer N pattern # time=0.12ms\nnot ok 9 - top insert (position: top / content overlay) # time=1.97ms\nnot ok 10 - createFxSequence with position top lands the new sequence at the head of the stack # time=1.53ms\nnot ok 11 - createContentOverlaySequence always lands at the head of the stack # time=0.42ms\nnot ok 12 - rename hardening # time=0.43ms\nnot ok 13 - rejects control characters fail-closed without changing the name or pushing undo # time=0.26ms\nnot ok 14 - FX stack one-gesture drop + inline rename wiring (260923-kcs) # time=7.69ms\nnot ok 15 - routes the FX reorder commit through the insertion-point resolver and drops the length-1 clamp # time=4.65ms\nnot ok 16 - registers a double-click listener and drives the inline rename edit signal # time=1.07ms\nnot ok 17 - renders the inline rename input commit path on the canvas overlay # time=1.91ms\n# tests 17\n# pass 0\n# fail 17\n"
}