---
phase: quick
plan: 260323-g4n
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/layout/CanvasArea.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Canvas area shows only the preview, zoom controls, fit button, and fullscreen button -- no playback controls or timecode"
    - "Timeline header still has play/pause, step forward/back, loop toggle, and timecode display"
    - "SpeedBadge overlay still appears on canvas during JKL speed playback"
  artifacts:
    - path: "Application/src/components/layout/CanvasArea.tsx"
      provides: "Canvas area without duplicate playback controls"
  key_links: []
---

<objective>
Remove duplicate playback controls (prev frame, play/pause, next frame buttons and timecode display) from the CanvasArea bottom bar. Keep only the zoom controls, fit-to-window button, fullscreen button, and FullSpeedBadge. The timeline header (TimelinePanel.tsx) already has these same playback controls and is the canonical location.

Purpose: Eliminate redundant UI clutter -- playback controls only need to exist in the timeline header.
Output: Cleaned-up CanvasArea.tsx with no playback buttons or timecode in the bottom bar.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/layout/CanvasArea.tsx
@Application/src/components/layout/TimelinePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove playback controls and timecode from CanvasArea bottom bar</name>
  <files>Application/src/components/layout/CanvasArea.tsx</files>
  <action>
In `CanvasArea.tsx`, modify the "Preview Controls" div (line 289, the `h-[42px]` bar at the bottom):

1. **Remove these elements from the JSX** (lines 291-325):
   - The "Step backward" button (SkipBack, `onClick={() => playbackEngine.stepBackward()}`)
   - The "Play / Pause" button (Play/Pause, `onClick={() => playbackEngine.toggle()}`)
   - The "Step forward" button (SkipForward, `onClick={() => playbackEngine.stepForward()}`)
   - The "Timecode display" div (`formatTime(timelineStore.displayTime.value)`)
   - The total duration span (`/ {formatTime(timelineStore.totalDuration.value)}`)

2. **Keep these elements** (they must remain in the bar):
   - Zoom out button (Minus, `canvasStore.zoomOut()`)
   - Zoom percentage display (`canvasStore.zoomPercent`)
   - Zoom in button (Plus, `canvasStore.zoomIn()`)
   - Fit button (Minimize2/Maximize2, `canvasStore.toggleFitLock()`)
   - Fullscreen button (Maximize, `enterFullscreen()`)
   - FullSpeedBadge component

3. **Keep SpeedBadge** positioned above the bar (line 287) -- it is NOT in the controls bar, it floats over the canvas.

4. **Clean up dead imports** -- after removing the playback buttons, these imports are no longer needed:
   - Remove `Play, Pause, SkipBack, SkipForward` from the lucide-preact import (keep `Plus, Minus, Maximize, Maximize2, Minimize2`)
   - Remove `playbackEngine, isFullSpeed` from `../../lib/playbackEngine` import -- BUT CHECK FIRST: `playbackEngine` is used in the Space key handler (useEffect, lines 143-192) and `isFullSpeed` is used there too. These are keyboard shortcuts, NOT the buttons. So `playbackEngine` and `isFullSpeed` must STAY.
   - Remove `timelineStore` from imports -- BUT CHECK FIRST: `timelineStore` is used for `isPlaying` (line 62, which drives the removed buttons) AND in the Space key handler AND in `getSourceDimensionsForLayer`. So `timelineStore` must STAY.

5. **Remove the `formatTime` helper function** (lines 379-384) -- it is no longer called anywhere in this file after removing the timecode display.

6. **Remove `const isPlaying`** (line 62: `const isPlaying = timelineStore.isPlaying.value`) -- this was only used by the removed play/pause button's title and icon toggle. Verify no other reference exists in the JSX before removing.

After cleanup, the bottom bar should contain only: zoom out | percentage | zoom in | fit | fullscreen | FullSpeedBadge.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/Application && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - CanvasArea bottom bar has NO play/pause, step-forward, step-backward buttons, and NO timecode display
    - Zoom controls, fit button, fullscreen button, and FullSpeedBadge remain
    - SpeedBadge overlay above the bar remains
    - Space key handler still works (imports for playbackEngine/isFullSpeed/timelineStore preserved)
    - No unused imports, no dead `formatTime` function, no dead `isPlaying` variable
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Grep for `SkipBack|SkipForward|formatTime` in CanvasArea.tsx returns no matches
- Grep for `Play.*Pause` in CanvasArea.tsx import line returns no matches (only Plus/Minus/Maximize/Maximize2/Minimize2 remain from lucide-preact)
- TimelinePanel.tsx is unchanged (still has all playback controls)
</verification>

<success_criteria>
Canvas area bottom bar shows only zoom controls, fit button, fullscreen button, and full-speed indicator. All playback controls and timecode removed. No dead code remains. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-g4n-remove-duplicate-playback-controls-from-/260323-g4n-SUMMARY.md`
</output>
