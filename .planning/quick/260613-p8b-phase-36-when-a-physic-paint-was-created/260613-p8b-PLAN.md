---
phase: quick-260613-p8b-phase-36-physics-paint-script-ranges
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/physicPaint.ts
  - app/src/types/physicPaint.test.ts
  - app/src/stores/physicPaintStore.ts
  - app/src/stores/physicPaintStore.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/components/sidebar/PhysicPaintProperties.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements:
  - QUICK-P8B
must_haves:
  truths:
    - "A Physics Paint layer shows saved Play animation ranges such as [4]------[10] and multiple ranges on the same layer."
    - "Opening Physics Paint from an editor frame inside a saved Play range opens that range's editable script in the Play canvas tab."
    - "The Play canvas timeline lets the user scrub to an in-between frame inside the Play range and preview that interpolated frame without starting playback."
  artifacts:
    - path: "app/src/stores/physicPaintStore.ts"
      provides: "Multiple saved Play script segment metadata and editable states per physics paint layer"
    - path: "app/src/lib/physicPaintBridge.ts"
      provides: "Current-frame-to-Play-script launch selection"
    - path: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      provides: "Editor-side range markers for saved Play scripts"
    - path: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      provides: "Clickable/scrubbable Play range with frame indicator"
  key_links:
    - from: "app/src/components/sidebar/PhysicPaintProperties.tsx"
      to: "app/src/stores/physicPaintStore.ts"
      via: "physicPaintVersion subscription plus play script segment query"
      pattern: "getPlayScriptSegments"
    - from: "app/src/lib/physicPaintBridge.ts"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "PhysicPaintLaunchContext with selected Play editableState, playStartFrame, and playFrameCount"
      pattern: "playStartFrame.*playFrameCount.*editableState"
    - from: "app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx"
      to: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      via: "onInspectPlayFrame calls navigate/scrub for preview"
      pattern: "onInspectPlayFrame"
---

<objective>
Add visible Play script range markers for Physics Paint outputs and make the saved Play range editable/previewable from the correct timeline frame.

Purpose: when several Physics Paint Play animations exist on one layer, the user must see where each animation starts/ends, place the editor scrubber on the desired range, reopen the correct Play canvas script, and scrub inside the Play canvas range to preview interpolation frames.
Output: persisted per-range Play script metadata, editor-side range markers, current-frame-based script reopening, and Play canvas scrub preview behavior.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/CLAUDE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-09-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/.planning/phases/36-physics-paint-ui-rebuild-session-persistence-and-output-proo/36-11-SUMMARY.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/types/physicPaint.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/lib/physicPaintBridge.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/sidebar/PhysicPaintProperties.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintStudio.css

Project constraints:
- Use pnpm, not npm.
- Do not run the dev server.
- Preserve Physics Paint play state on reopen; do not reintroduce latest Play frames as ordinary onion overlays.
- No backward compatibility code for old project formats.
- Bump and subscribe to physicPaintVersion for physics paint mutations/render consumers.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist multiple Play script segments per Physics Paint layer</name>
  <files>app/src/types/physicPaint.ts, app/src/types/physicPaint.test.ts, app/src/stores/physicPaintStore.ts, app/src/stores/physicPaintStore.test.ts</files>
  <behavior>
    - Applying a Play canvas sequence at frame 4 for 7 frames records a script segment with startFrame 4, endFrame 10, frameCount 7, editableSource play, and the Play editable state for that segment.
    - Applying another Play canvas sequence on the same layer at frame 30 for 5 frames keeps both segments queryable and serializable.
    - Querying by editor frame returns the segment containing that frame; querying outside all ranges returns null.
    - Mutating Play script segment data increments physicPaintVersion and marks the project dirty through the existing callback path.
  </behavior>
  <action>Add a typed `PhysicPaintPlayScriptSegment` contract with stable `id`, `startFrame`, `endFrame`, `frameCount`, and `editableState` fields. Extend `physicPaintStore` with non-reactive per-layer Play segment storage plus `getPlayScriptSegments(layerId)` and `getPlayScriptSegmentAtFrame(layerId, frame)`. Update `applySequence` and `convertRotoToPlay` to upsert a segment for the saved Play range instead of replacing the only editable script for the whole layer; keep existing `getWorkflowMetadata` behavior for the last active workflow metadata so Phase 36 launch compatibility remains intact. Serialize segments into `physic_paint_outputs` as narrow primitives plus editable state; since project policy says no backward compatibility for old projects, do not add legacy migration branches beyond accepting the current in-memory output shape used by tests. Preserve Plan 36-09: do not use saved Play frames as regular Roto onion overlay sources.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <done>Two or more Play script segments can coexist on one Physics Paint layer, round-trip through project serialization, and be selected by containing editor frame.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reopen the correct Play script from the editor scrubber frame and show range markers</name>
  <files>app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts, app/src/components/sidebar/PhysicPaintProperties.tsx</files>
  <behavior>
    - With saved Play segments [4-10], [8-23], and [30-34], opening from editor frame 9 chooses the containing segment with the closest/recent relevant start according to a deterministic rule documented in the test, and launches Play canvas with that segment's editable state, playStartFrame, and playFrameCount.
    - Opening from a frame outside saved Play segments keeps the current launch behavior.
    - The Physics Paint sidebar renders compact markers like `[4]------[10]` for each saved Play script and highlights the marker containing the current editor frame.
  </behavior>
  <action>Update `createPhysicPaintLaunchContext` / `openPhysicPaintCanvas` so the current editor frame first checks `physicPaintStore.getPlayScriptSegmentAtFrame(sourceLayerId, frame)`. When found, launch with `workflowMode: 'play'`, `playStartFrame` equal to the segment start, `playFrameCount` equal to the segment length, `editableSource: 'play'`, and that segment's `editableState`. Keep the current Roto/default path when no containing segment exists. In `PhysicPaintProperties`, subscribe to `physicPaintVersion.value`, read `getPlayScriptSegments(sourceLayerId)`, and render a small "Play scripts" row under Rendered Output with all ranges as textual markers; highlight the active one based on `timelineStore.currentFrame.value`. Do not add timeline-click destructive behavior; opening remains through the existing `[open fx paint canvas]` button after the scrubber is positioned on the target range.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/lib/physicPaintBridge.test.ts src/stores/physicPaintStore.test.ts</automated>
  </verify>
  <done>Editor users can see all saved Play animation ranges for the selected Physics Paint layer and reopen the right Play canvas script by placing the editor scrubber inside that range before opening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the Play canvas range scrub to preview in-between frames</name>
  <files>app/src/components/physic-paint/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/components/physic-paint/physicsPaintStudio.css</files>
  <behavior>
    - In Play canvas mode, the range displays explicit start/end labels such as `[4]` and `[10]`, plus a visible scrubber indicator for the inspected frame.
    - Clicking or dragging within the Play range calls `onInspectPlayFrame` for the nearest frame in the saved range, including in-between positions such as frame 2 inside a 0-5 range.
    - Inspecting an in-between frame updates the mounted canvas preview for that frame without starting playback and without clearing the saved Play script.
  </behavior>
  <action>Extend `PhysicsPaintWorkflowStrip` play-lane UI so the Play range is not just a click target: render start/end bracket labels using absolute app frames, render a current inspected-frame scrubber, and handle pointer down/move/up on the range to call `onInspectPlayFrame` with the nearest frame between `playRange.startFrame` and `playRange.endFrame`. Keep clicks inspection-only per Phase 36 decision; no conversion or clearing from lane clicks. In `PhysicsPaintStudio`, ensure `onInspectPlayFrame={navigateToSyncedFrame}` previews the selected Play frame while remaining in Play mode and without calling `playPreview`. Style the scrubber and labels in `physicsPaintStudio.css` to match the compact existing Physics Paint strip aesthetic, avoiding extra noisy controls.</action>
  <verify>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.tsx</automated>
    <automated>pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck</automated>
  </verify>
  <done>Play canvas mode shows `[from]---[to]` markers and supports scrubber inspection of in-between frames for preview without playback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project file -> physicPaintStore | Serialized Play script segment metadata and editable states are loaded into runtime state. |
| editor frame -> standalone launch context | Current timeline frame selects which saved Play script/editable state is launched. |
| pointer input -> Play frame inspection | Untrusted pointer coordinates choose a preview frame inside the Play canvas range. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P8B-01 | Tampering | `physicPaintStore.loadFromMceOutputs` | mitigate | Validate segment start/count/end as non-negative bounded integers and editable state with the existing serialized project validator before storing. |
| T-P8B-02 | Tampering | `PhysicsPaintWorkflowStrip` pointer scrub | mitigate | Clamp calculated frame to `playRange.startFrame..playRange.endFrame` before calling `onInspectPlayFrame`. |
| T-P8B-03 | Information Disclosure | sidebar markers | accept | Markers expose only frame numbers for the currently selected local project layer, no new external boundary. |
| T-P8B-SC | Tampering | package installs | mitigate | No package installs are planned; if executor adds one, stop for package legitimacy audit first. |
</threat_model>

<verification>
Run targeted tests first, then typecheck:
- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app exec vitest run src/types/physicPaint.test.ts src/stores/physicPaintStore.test.ts src/lib/physicPaintBridge.test.ts src/components/physic-paint/PhysicsPaintWorkflowStrip.test.ts src/components/physic-paint/PhysicsPaintStudio.test.tsx`
- `pnpm --dir /Users/lmarques/Dev/efx-motion-editor/app typecheck`
Do not run the dev server.
</verification>

<success_criteria>
- Physics Paint layers with multiple saved Play canvas animations expose compact `[start]------[end]` markers in the editor sidebar.
- Placing the editor scrubber inside a marker range and opening Physics Paint launches the Play canvas tab for that range's editable script.
- The Play canvas timeline supports clicking/dragging between endpoints to preview intermediate frames without playback.
- Existing Roto onion snapshot behavior and Play-frame exclusion from ordinary onion overlays remain intact.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/quick/260613-p8b-phase-36-when-a-physic-paint-was-created/260613-p8b-SUMMARY.md` when done.
</output>
