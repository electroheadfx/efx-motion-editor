---
phase: quick-43
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/lib/shortcuts.ts
  - Application/src/components/overlay/ShortcutsOverlay.tsx
autonomous: true
requirements: [NAV-CMD-UPDOWN]

must_haves:
  truths:
    - "Cmd+ArrowUp jumps to the previous sequence start (same as Cmd+ArrowLeft)"
    - "Cmd+ArrowDown jumps to the next sequence start (same as Cmd+ArrowRight)"
    - "Cmd+Shift+ArrowUp jumps to timeline start (same as Cmd+Shift+ArrowLeft)"
    - "Cmd+Shift+ArrowDown jumps to timeline end (same as Cmd+Shift+ArrowRight)"
    - "Shortcuts overlay shows Up/Down alternatives alongside Left/Right"
  artifacts:
    - path: "Application/src/lib/shortcuts.ts"
      provides: "4 new tinykeys bindings for $mod+ArrowUp/Down and $mod+Shift+ArrowUp/Down"
    - path: "Application/src/components/overlay/ShortcutsOverlay.tsx"
      provides: "Updated Navigation entries showing Up/Down alternatives"
  key_links:
    - from: "Application/src/lib/shortcuts.ts"
      to: "sequenceNav"
      via: "findPrevSequenceStart / findNextSequenceStart"
      pattern: "find(Prev|Next)SequenceStart"
---

<objective>
Add Cmd+ArrowUp/Down shortcuts as aliases for the existing Cmd+ArrowLeft/Right sequence navigation, and Cmd+Shift+ArrowUp/Down as aliases for Cmd+Shift+ArrowLeft/Right timeline bounds navigation. Update the shortcuts overlay to reflect these additions.

Purpose: Some users expect vertical arrow keys with Cmd for sequence-level navigation (up = previous, down = next). This mirrors the existing horizontal Cmd+Arrow bindings added in quick-41.
Output: 4 new keyboard shortcut bindings and updated help overlay.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/lib/shortcuts.ts
@Application/src/components/overlay/ShortcutsOverlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cmd+ArrowUp/Down shortcut bindings and update overlay</name>
  <files>Application/src/lib/shortcuts.ts, Application/src/components/overlay/ShortcutsOverlay.tsx</files>
  <action>
In `Application/src/lib/shortcuts.ts`, add 4 new tinykeys bindings inside the `mountShortcuts()` call, right after the existing `$mod+Shift+ArrowRight` binding (line 490). Follow the identical pattern used by the existing `$mod+ArrowLeft/Right` and `$mod+Shift+ArrowLeft/Right` handlers:

1. `'$mod+ArrowUp'` handler: identical body to `$mod+ArrowLeft` (lines 455-465) -- calls `findPrevSequenceStart`, falls back to frame 0.
2. `'$mod+ArrowDown'` handler: identical body to `$mod+ArrowRight` (lines 466-477) -- calls `findNextSequenceStart`, falls back to last frame.
3. `'$mod+Shift+ArrowUp'` handler: identical body to `$mod+Shift+ArrowLeft` (lines 478-483) -- seeks to frame 0.
4. `'$mod+Shift+ArrowDown'` handler: identical body to `$mod+Shift+ArrowRight` (lines 484-490) -- seeks to last frame.

Each handler must include the standard guards: `shouldSuppressShortcut(e)`, `isFullscreen.peek()`, `e.preventDefault()`. Add a comment `// Cmd+Arrow Up/Down — vertical aliases for sequence navigation (NAV-CMD-UPDOWN)`.

In `Application/src/components/overlay/ShortcutsOverlay.tsx`, update the Navigation group entries (lines 46-49) to include Up/Down alternatives:
- Line 46: change keys from `'Home / \u21E7\u2318\u2190'` to `'Home / \u21E7\u2318\u2190\u2191'` (append up arrow unicode \u2191)
- Line 47: change keys from `'End / \u21E7\u2318\u2192'` to `'End / \u21E7\u2318\u2192\u2193'` (append down arrow unicode \u2193)
- Line 48: change keys from `'PgUp / \u2318\u2190'` to `'PgUp / \u2318\u2190\u2191'` (append up arrow)
- Line 49: change keys from `'PgDn / \u2318\u2192'` to `'PgDn / \u2318\u2192\u2193'` (append down arrow)
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && npx tsc --noEmit --project Application/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>All 4 new Cmd+Arrow Up/Down bindings registered in tinykeys; ShortcutsOverlay shows vertical arrow alternatives; TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- grep confirms all 4 new bindings: `$mod+ArrowUp`, `$mod+ArrowDown`, `$mod+Shift+ArrowUp`, `$mod+Shift+ArrowDown`
- ShortcutsOverlay Navigation entries include up/down arrow symbols
</verification>

<success_criteria>
- Cmd+ArrowUp/Down navigate between sequences (same behavior as Cmd+ArrowLeft/Right)
- Cmd+Shift+ArrowUp/Down jump to timeline start/end (same behavior as Cmd+Shift+ArrowLeft/Right)
- Shortcuts overlay accurately reflects the new alternatives
</success_criteria>

<output>
After completion, create `.planning/quick/260319-u8t-add-cmd-arrowup-down-shortcuts-to-naviga/260319-u8t-SUMMARY.md`
</output>
