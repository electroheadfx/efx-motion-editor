---
phase: quick-260318-m9x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/overlay/ShortcutsOverlay.tsx
autonomous: true
requirements: [QUICK-m9x]
must_haves:
  truths:
    - "Canvas section shows Cmd/Ctrl+Scroll shortcut for zoom at cursor"
    - "Timeline section shows Shift+Scroll shortcut for horizontal scroll"
  artifacts:
    - path: "Application/src/components/overlay/ShortcutsOverlay.tsx"
      provides: "Updated SHORTCUT_GROUPS with missing scroll entries"
      contains: "Shift+Scroll"
  key_links: []
---

<objective>
Add missing scroll-based shortcuts to the help menu overlay.

Purpose: Two functional shortcuts are missing from the ShortcutsOverlay: (1) Cmd/Ctrl+Scroll for canvas zoom at cursor, and (2) Shift+Scroll for timeline horizontal scroll. Both are already implemented in code but not documented in the help overlay.

Output: Updated ShortcutsOverlay.tsx with complete shortcut entries.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/overlay/ShortcutsOverlay.tsx
@Application/src/components/layout/CanvasArea.tsx (lines 73-87: Cmd/Ctrl+Scroll zoom handler)
@Application/src/components/timeline/TimelineInteraction.ts (lines 693-733: wheel handler with Shift+Scroll)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing scroll shortcuts to ShortcutsOverlay</name>
  <files>Application/src/components/overlay/ShortcutsOverlay.tsx</files>
  <action>
In the SHORTCUT_GROUPS array in ShortcutsOverlay.tsx, add the two missing entries:

1. In the "Canvas" group (index 4), add BEFORE the arrow nudge entries (after the "F" / Toggle fit lock entry):
   - keys: '\u2318Scroll' (or '\u2318/\u2303Scroll' to show both Cmd and Ctrl), description: 'Zoom at cursor'

   Note: The canvas handleWheel in CanvasArea.tsx checks `e.metaKey || e.ctrlKey`, so both Cmd and Ctrl work. Use '\u2318Scroll' since macOS is the primary platform (matching the existing '\u2303Scroll' pattern in Timeline but using Cmd symbol).

2. In the "Timeline" group (index 5), add after the existing '\u2303Scroll' zoom entry:
   - keys: '\u21E7Scroll', description: 'Horizontal scroll'

   Note: The TimelineInteraction.ts onWheel handler (line 728) converts Shift+deltaY into horizontal scroll.

After adding entries, update TAB_COUNT if needed (currently 9 -- should stay 9 since no new groups are added, only new entries within existing groups).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && grep -c "Scroll" Application/src/components/overlay/ShortcutsOverlay.tsx</automated>
  </verify>
  <done>
    - Canvas group contains Cmd+Scroll "Zoom at cursor" entry
    - Timeline group contains Shift+Scroll "Horizontal scroll" entry
    - Both entries use consistent Unicode symbols matching the existing style
    - App compiles without errors
  </done>
</task>

</tasks>

<verification>
- Open the app, press "?" to show shortcuts overlay
- Navigate to Canvas tab: verify "Zoom at cursor" with Cmd+Scroll is listed
- Navigate to Timeline tab: verify "Horizontal scroll" with Shift+Scroll is listed
- Sections index tab shows updated shortcut counts for Canvas and Timeline groups
</verification>

<success_criteria>
- ShortcutsOverlay shows all scroll-based shortcuts that exist in the codebase
- No scroll shortcut that is implemented in code is missing from the help overlay
- Unicode key symbols are consistent with existing entries
</success_criteria>

<output>
After completion, create `.planning/quick/260318-m9x-help-menu-verify-shift-scroll-and-cmd-sc/260318-m9x-SUMMARY.md`
</output>
