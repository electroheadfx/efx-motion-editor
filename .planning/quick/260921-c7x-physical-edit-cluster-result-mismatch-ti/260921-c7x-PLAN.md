---
phase: quick-260921-c7x
plan: 260921-c7x
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
  - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
  - app/src/lib/physicPaintBridge.ts
  - app/src/lib/physicPaintBridge.test.ts
  - app/src/types/physicPaint.ts
autonomous: true
requirements: []
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A rail physical edit completes and commits exactly once on every layer: Apply key spacing, move a rail, move or stretch a rail never produces 'Ignored mismatched physics paint physical edit result. Try the action again.' followed by 'timed out. The previous state was restored.'"
    - "A key move and click-positioning onto an interpolated frame behave identically on the SECOND layer as on the first — layer/track identity never changes the settlement outcome."
    - "A mismatched result releases the in-flight slot immediately (terminal settlement-mismatch): the next edit is not refused with 'A Roto physical edit is already in flight.', pendingOperationId clears, and the 5 s timeout is no longer the only escape. The mismatch is still surfaced as 'mismatch' to the result controller (diagnostic route unchanged) and the staged deferred document is never published."
    - "The child-computed stagedRevision and the parent-returned stagedRevision agree on EVERY result path — success, validation rejection, and early rejection — including compacted-record transports and clips whose lifecycle is synthesized by payload normalization."
    - "Ownership stays operationId-first: a result for a different operationId is still ignored; the fix must not widen acceptance."
    - "WR-01 kov semantics intact: an identical-span reveal resize remains a no-op (`{ ok: true, descriptor: null }`, no write), and a stretch still writes the requested end to both `originalEndExclusive` and the single `visibleRanges` entry."
    - "Native UAT is left explicitly pending — never claimed."
  artifacts:
    - app/src/lib/physicPaintBridge.test.ts — the live descriptor capture (real stores, real apply door): success, early reject, compacted transport, layer-2 identity, reveal unchanged-span and stretch; raw output recorded
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts — the two mandated RED legs (layer-2 identity, latch release), the term bisection, and the rewritten pinned latch test at :1257-1282
    - app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts — mismatch becomes a terminal settlement (release pending/in-flight/lease/timer, restore, still return 'mismatch'); the revision producer made single-shape
    - app/src/lib/physicPaintBridge.ts — every result path echoes the child-owned stagedRevision (no silent recompute over ref-shaped records)
    - app/src/types/physicPaint.ts — only if the verdict names the payload-carried revision: the payload/result field and its validator term
    - .planning/quick/260921-c7x-physical-edit-cluster-result-mismatch-ti/260921-c7x-SUMMARY.md — the verdict with raw captured descriptors, RED/GREEN output, guardrail audit, gates, pending native UAT rows
  key_links:
    - "useRotoPhysicalEditCoordinator.ts:562-590 — `transitionPhysicalEditResult`, the settlement predicate. :567 owns the result by `operationId`; :576 pins `detail.stagedRevision !== pending.stagedRevision`; :588 pins `acceptedRevision !== pending.stagedRevision` on ok. Any single term a live descriptor violates returns `{ type: 'mismatch' }` for the WHOLE operation — there is no partial acceptance."
    - "useRotoPhysicalEditCoordinator.ts:1412-1474 — `consumePhysicalEditResult`. The mismatch branch :1421-1424 logs `Roto physical edit result mismatch: <message>` and RETURNS WITHOUT CLEARING anything: `pendingRef`, `beforeRef`, `inFlightRef`, the settlement lease and the 5 s timer all survive. That single `return` is the latch."
    - "useRotoPhysicalEditCoordinator.ts:1131-1144 — `clearPendingOnce`, the only place `inFlightRef.current = false`, `pendingOperationIdSignal.value = null`, the lease release and the timer clear happen. Every clean path funnels through it; the mismatch branch does not."
    - "useRotoPhysicalEditCoordinator.ts:1519-1531 — the `executePhysicalEdit` guard; :1529 publishes `PHYSICAL_EDIT_SERIALIZE_MESSAGE` ('A Roto physical edit is already in flight.') when `inFlightRef || pendingRef || settledLeaseRef || recoveryLease`. This is the visible surface of symptom 1 (`useRotoTimelineActions.ts:3176-3179` applyForceSpacing reads `pendingOperationId` the same way)."
    - "useRotoPhysicalEditCoordinator.ts:2247-2253 — the timeout arm: `finalizeFailed(pending, beforeRef.current, 'timeout', PHYSICAL_EDIT_TIMEOUT_MESSAGE)` ('Roto physical edit timed out. The previous state was restored.'). The only exit from the latched state today."
    - "useRotoPhysicalEditCoordinator.ts:2080-2088 + 2124-2130 + 2191 — H-A. `stagedLoopClips` (:2080) feeds BOTH `stagedRevision = buildPhysicPaintRotoPhysicalRevision(..., stagedLoopClips, ...)` (:2124, RAW clips) and the wire payload's `loopClips: cloneLoopClips(stagedLoopClips)` (:2191, NORMALIZED). The parent recomputes its revision over the parsed, normalized clips — if `normalizeLoopClipForPayload` (:640-653) changes any hashed term for a clip without a stored lifecycle, the two revisions differ and every clip-carrying rail edit mismatches."
    - "useRotoPhysicalEditCoordinator.ts:2186 + 993-1012 + 2238 — H-B transport half. `recordsToApplyPayloadRecords` (:2186) keeps full records in the in-memory payload; `compactRecordsForTransport` (:993) runs only at the wire boundary (:2238) and replaces unchanged records with `{ keyId, appFrame, refToken }`. The pending/recovery copy keeps full records — so the child can always hash its own revision; the parent cannot when its result is built without one."
    - "physicPaintBridge.ts:1239-1297 — `physicalEditResult`, the parent echo. The `stagedRevision` fallback (:1251-1267) recomputes over `payload.records` — REF-shaped on the compacted path — and `'invalid-physical-revision'` (:1266) is the catch value. :1287 pins `appliedFrameCount = payload.records.length` on ok. Every early reject that cannot compute a revision returns through here without one."
    - "physicPaintBridge.ts:1594-1598 + 1794-1800 + 1966-1973 — the parent's own revision authority: `reject(error, stagedRevision?)` (:1598) echoes when given one; the success path computes `stagedRevision` from the PARSED proposals (:1794) and returns it with `acceptedRevision: acceptedDocument.revision` (:1966). Early rejects before proposal parsing (stale revision, capacity, unknown track) call `reject(error)` with no revision (:1687-1695)."
    - "physicPaintBridge.ts:558-581 + 583-609 + 1301 — `expandRotoPhysicalEditRecordRefs` resolves `{ keyId, appFrame, refToken }` against the store and fails closed; the expansion-failure path returns `applyFailureResult(payload as PhysicPaintApplyPayload, expanded.error)` — the payload still carries refs — and lands on `physicalEditResult(payload, { ok: false, error })` (:1301) with the ref-shaped fallback hash."
    - "types/physicPaint.ts:1273-1276 (ref allowlist `keyId`/`appFrame`/`refToken`), :1285-1310 (`PhysicPaintRotoPhysicalEditApplyResult`), :1760-1794 (validator — `hasOnlyKeys` and a REQUIRED non-empty `stagedRevision`, :1771). Any new payload/result field must be added to both the interface and the allowlist or the wire validator rejects the result outright."
    - "useRotoPhysicalEditCoordinator.test.ts:600-780 — the harness: execute helpers hardcode `expectedLaunch: { operationId: 'launch-1', layerId: 'layer-1' }`; `makeResult` (:726) rebuilds `stagedRevision` from `payload.records` + `payload.loopClips ?? []` with FULL records — the harness is self-consistent by construction, which is exactly why it cannot reproduce H-A/H-B; the real-store asymmetry must be captured in physicPaintBridge.test.ts. `mismatch()` (:773) perturbs `selectedAppFrame` to 15."
    - "useRotoPhysicalEditCoordinator.test.ts:1257-1282 — the pinned test 'preserves every prior surface on parent rejection and leaves mismatches pending without success': its second half asserts `mismatch()` stays pending (failureOutput null) and then cancels. That half IS the latch and must be rewritten by the fix; its first half (parent rejection preserves records/breaks/selection) stays."
    - "physicPaintBridge.test.ts:105-205 — the real-store harness (`TEST_TRACK_ID = 'track-1'`; `registerTrackDocument(layerId)`; `makePhysicalRecord`; `seedPhysicalDocument`; the exported apply door) plus the `expandRotoPhysicalEditRecordRefs` describe at :367-433 — the layer-2 leg registers a second document and drives the same edit shape through it."
    - "usePhysicsPaintApplyResultController.ts:170-181 — routes `replace-roto-physical-map` to the coordinator and maps a `'mismatch'` return to `setLastError('Ignored mismatched physics paint physical edit result. Try the action again.')` — the user-visible 'Try again'. Keep returning 'mismatch'; do not silence the diagnostic."
    - "PhysicsPaintStudio.tsx:457-461 + 819/1440/1943 + 1323/2593 — the layer-2 identity suspect: the studio's `studioActiveTrackId()` (launch-track fallback) feeds the records port while the coordinator's payload commits to `launch.getActiveTrackId(layerId)` (document activeTrackId). If the two disagree on the second layer, the child hashes track A's records while the parent applies and hashes track B's."
---

<objective>
Close the physical-edit cluster: rail edits (Apply key spacing, move, stretch, reveal stretch) die with "Ignored mismatched physics paint physical edit result. Try the action again." and then "Roto physical edit timed out. The previous state was restored.", and on a SECOND layer key moves and interpolated-frame targeting are dead entirely. The regression-window suspects are the kov commit `ff645d38` (reveal extent law) and the ji7 commit `0ff76f43` (launch-capacity law); `d340c71b` is docs-only. Both suspects change clip/identity fields that feed the settlement revision.

Diagnose FIRST, no production change before the verdict. The mechanism already visible at the plan base, to be confirmed with real stores:

- **The latch (confirmed by reading).** A result owned by the pending `operationId` (`transitionPhysicalEditResult`, :567) whose descriptor violates ANY term (:576 stagedRevision, :588 acceptedRevision, and the rest of the tuple) returns `'mismatch'`; `consumePhysicalEditResult` (:1421-1424) logs it and returns WITHOUT releasing `pendingRef`/`inFlightRef`/the lease/the timer. Every subsequent edit is refused by the guard at :1529 with 'A Roto physical edit is already in flight.' until the 5 s arm (:2247-2253) fires the timeout message. That is exactly the user's three-message sequence, and it explains why a second layer is fully dead after its first edit.
- **H-A — one clip shape, two revisions (high confidence).** The child hashes RAW `stagedLoopClips` (:2124) while the wire ships `cloneLoopClips(...)` NORMALIZED clips (:2191); the parent hashes what it parsed. Any clip whose lifecycle normalization changes a hashed term makes the two revisions differ on every clip-carrying rail edit — the kov/ji7 window is exactly where reveal/loop lifecycle terms entered this surface.
- **H-B — a parent result with no echo (high confidence).** Early rejects before proposal parsing return through `physicalEditResult` (:1239) with no `stagedRevision`; the fallback recomputes over `payload.records` — ref-shaped on the compacted path (:993/:2238) — producing `'invalid-physical-revision'` (:1266). The child sees a revision it never staged: mismatch instead of the parent's real error, latch included. The ref-expansion failure path (:583-609 → :1301) lands the same way.
- **H-C — layer/track identity read asymmetry (probe).** The records port reads `studioActiveTrackId()` (launch-track fallback) while the payload commits to the document's `activeTrackId`; if those diverge on a second layer, the child hashes one track's records and the parent applies/hashes the other.

Purpose: the physical-edit protocol must settle every honest edit on every layer, and a genuinely bad settlement must release its lease immediately instead of stranding the session for 5 s. Fix at the coordinator/identity layer — never by retrying or ignoring mismatched results.
Output: a verdict with raw captured descriptors, the two mandated RED legs (layer-2 identity parity; latch release), the identity fix per verdict, a rewritten pinned test, gates green, and native UAT rows left pending.

**STOP clause (from the dispatch):** if the probe shows the root is STRUCTURAL — the settlement predicate or the lease model itself cannot be satisfied by any descriptor the parent can honestly produce — make NO production change, record the verdict with evidence in the SUMMARY, and return without executing Task 2.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts
@app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts
@app/src/lib/physicPaintBridge.ts
@app/src/lib/physicPaintBridge.test.ts
@app/src/types/physicPaint.ts

Verified code facts (live reads, 2026-09-21). Every anchor is in `key_links` above; the load-bearing ones:

- The settlement predicate (:562-590) and the mismatch return that skips the release (:1421-1424); the release point `clearPendingOnce` (:1131-1144); the guard that publishes the in-flight message (:1529); the timeout arm (:2247-2253).
- The two revision producers that can disagree: child `stagedRevision` over raw clips (:2124-2130) vs the payload's normalized clips (:2191); parent fallback recompute over possibly-ref-shaped records (physicPaintBridge.ts:1251-1267).
- Test conventions to reuse (never invent a harness or a test config — project rule): the coordinator harness at :600-780 (extend it with identity overrides if the leg needs a second layer there; its `makeResult` must start echoing the payload's `stagedRevision` if the verdict carries it in the payload); the bridge real-store harness at :105-205 and the ref-expansion describe at :367-433.
- Commands (project rule, inherited verbatim): `pnpm --filter efx-motion-editor exec vitest run <paths>` — vitest run only, NEVER watch; types via `pnpm --filter efx-motion-editor exec tsc --noEmit`; full suite `pnpm --filter efx-motion-editor exec vitest run`.
- Preact/signals discipline applies to any signal/component touch (efx-preact-reactivity): no signal writes in a render body, no useState, idempotent setters. This quick's production edits are hook/bridge logic only.
- Native app runs are the user's side ONLY — do not launch the app or the dev server; native UAT is owed, never claimed (project rule).
- Scope guardrails (verbatim intent): fix at the coordinator/identity layer; do NOT paper over by retrying mismatched results; do NOT regress the WR-01 kov semantics (identical-span resize = no write); touch only the physical-edit settlement/identity surfaces — no change to the settlement predicate's MEANING, no change to rail rendering, no change to the resolver or capacity laws, no format change beyond the identity field the verdict names.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verdict — capture the live descriptors on the real stores, bisect the failing identity term, pin the two mandated RED legs (test-only)</name>
  <files>app/src/lib/physicPaintBridge.test.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</files>
  <behavior>
    DIAGNOSE FIRST — this task changes NO production file. Its output is a verdict with raw evidence: which settlement term the live parent descriptor violates, and which producer makes it disagree. Run everything against the real stores through the bridge's exported apply door and the coordinator's own harness — never the app.

    A. Real-store descriptor capture in `physicPaintBridge.test.ts` (harness at :105-205):
    - Success path: build a settleable physical-edit payload for a rail edit (the same staging shape the coordinator sends: full records, loop clips, `expectedRevision`, semantic delta) and apply it through the real door. Record the ACTUAL returned descriptor verbatim. Compare `result.stagedRevision` against the revision the child would compute over the RAW staged clips — `buildPhysicPaintRotoPhysicalRevision(records, interpolation, rawClips, breaks, overrides)` — and against the same call over the NORMALIZED clips the payload ships (`cloneLoopClips`'s output). H-A is confirmed if the parent's echo equals neither child value, or if raw and normalized hashes differ; record both hashes.
    - Term bisection: re-run the same payload with ONE clip-lifecycle term changed at a time (`syncState`, `railKind`, `provenanceState`, `phaseOrigin`, `originalEndExclusive`, `visibleRanges`, `frameOverrides`) and record which term flips `ok` or moves the echoed revision. Include a clip WITHOUT a stored lifecycle (the v1.0-created/reveal-created shape) — that is the suspected live case.
    - Early reject: force a validation rejection before proposal parsing (stale `expectedRevision`, capacity, unknown track) and record the descriptor: `ok`, `error`, and whether `stagedRevision` is the child's value or a recomputed `'invalid-physical-revision'`. That is H-B's verdict. Repeat for the compacted-record transport (unchanged records as `{ keyId, appFrame, refToken }` — drive the wire shape the coordinator produces at :2238) and for a ref-expansion failure (a refToken the parent cannot resolve).
    - Layer-2 leg (mandated pin 1): register TWO layer documents with DIFFERENT layer ids (and a second track id where the harness allows) and drive the same edit shape through both. Record both descriptors verbatim. Layer-2 must settle identically; if its revision/rejection differs, record exactly which field differs — that field names H-C or the layer-scoped producer.
    - Regression-window probes: (a) a reveal-carried payload whose span is UNCHANGED (the kov WR-01 case) and one that genuinely stretches — record the parent descriptor for each; the identical span must not be what triggers a mismatch, and the stretch must settle; (b) record whether any reveal/bake identity reaches this payload at all (negative result is a valid verdict — state it).

    B. Coordinator-level RED legs in `useRotoPhysicalEditCoordinator.test.ts` (harness at :600-780; extend its execute helpers with layer/track overrides if a leg needs a second layer — extend the existing harness, do not invent one):
    - Leg 1 (mandated pin 1 — layer-2 identity RED): a pending staged for a SECOND layer identity must accept the settleable descriptor captured for that layer in part A. At base the second layer's edit is ignored/mismatched while the layer-1 shape matches → RED. Express the leg through the real captured descriptor (or the same builder the verdict names), not an invented one.
    - Leg 2 (mandated pin 2 — latch RED): after `mismatch()`, the coordinator must RELEASE the in-flight slot: a following `executePhysicalEdit` must NOT publish 'A Roto physical edit is already in flight.', `pendingOperationId.value` must be null, the failure output must carry the dedicated `'settlement-mismatch'` reason with the mismatch message, the pre-edit snapshot must be restored, and `acceptedOutput.value` must stay null; `consumePhysicalEditResult` must have returned `'mismatch'` (the diagnostic route is preserved). At base the pending tuple survives → RED.
    - Leg 3 (expected green — guardrail lock): a descriptor with a DIFFERENT `operationId` still returns `'ignore'`; the fix must not widen acceptance.
    - Rewrite the pinned test at :1257-1282: its first half (parent rejection preserves records/breaks/selection) stays verbatim; its second half ('leaves mismatches pending') becomes Leg 2's assertions.

    C. Verdict, recorded in the SUMMARY and as the task's closing message with the raw captured descriptors as evidence, choosing exactly one:
    - NON-STRUCTURAL (proceed to Task 2): the disagreeing term is produced by the child revision computation (H-A), the parent revision echo (H-B), the layer/track identity read (H-C), or another descriptor producer — fixable at the coordinator/identity layer without changing what the predicate means.
    - STRUCTURAL (STOP): no descriptor the parent can honestly produce satisfies the predicate, or the lease model itself is the root. Make NO production change; record the full verdict with evidence in the SUMMARY; return without executing Task 2.
  </behavior>
  <action>
    Write the captures and legs first, run them, keep the raw output verbatim (it is the SUMMARY's ground truth). Do not touch a production file in this task. Commit: `test(260921-c7x): RED — live descriptor capture, layer-2 identity and latch legs`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor &amp;&amp; pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts</automated>
  </verify>
  <done>The SUMMARY carries the actual parent descriptors captured verbatim (success, each bisected lifecycle term, early reject, compacted transport, ref-expansion failure, layer-2, reveal unchanged-span and stretch) plus the two child revision hashes for H-A; leg 1 is RED against the real second-layer descriptor; leg 2 is RED (the latch); leg 3 is green; the pinned test at :1257-1282 is rewritten; the verdict names the root and its producer — or the structural STOP is invoked with zero production change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix — one revision authority across the child/parent boundary, and a mismatched result settles terminally (RED then GREEN)</name>
  <files>app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.ts, app/src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts, app/src/lib/physicPaintBridge.ts, app/src/lib/physicPaintBridge.test.ts, app/src/types/physicPaint.ts</files>
  <behavior>
    RED legs from Task 1 must be red at entry and green at exit; every other leg in the two files stays green.

    The fix has two parts, in this order:

    1. Latch release (unconditional, independent of the verdict). In `consumePhysicalEditResult`'s mismatch branch (:1421-1424): keep the diagnostic log and keep returning `'mismatch'`, but make the settlement TERMINAL — call `finalizeFailed(pending, before, 'settlement-mismatch', PHYSICAL_EDIT_RESULT_MISMATCH_MESSAGE)` before returning so the snapshot is restored, `failureOutput` carries the dedicated reason and error, and `clearPendingOnce` releases `pendingRef`, `inFlightRef`, `pendingOperationIdSignal`, the settlement lease and the timer. The staged `pending.deferredDocument` is NEVER published on this path. Do not retry, do not re-send, do not ignore the mismatch, do not touch the timeout arm (:2247-2253) or the predicate's meaning (operationId-first ownership at :567 and every term's semantics stay). Leg 3 (different operationId → 'ignore') must stay green.

    2. Identity agreement (per the Task 1 verdict — apply exactly what the evidence names, nothing more):
    - **H-A (single revision authority):** the revision the child stages and the clips the wire ships must be the SAME shape. Compute `stagedRevision` over the same normalized collection the payload carries (normalize once, feed both `buildPhysicPaintRotoPhysicalRevision` at :2124 and `loopClips:` at :2191 from that one value), or make `buildPhysicPaintRotoPhysicalRevision` itself canonicalize clips so raw and normalized hash identically — pick per the captured evidence and state why in the SUMMARY. Do not change `normalizeLoopClipForPayload`'s output semantics; do not change what the payload's canonical re-verification accepts.
    - **H-B (echo on every path):** every parent result must carry the child's `stagedRevision`. The expected mechanism: the payload (child-owned) carries `stagedRevision` and `physicalEditResult` (:1239) prefers `options.stagedRevision ?? payload.stagedRevision` before the ref-shaped fallback recompute, so early rejects (:1687-1695), the expansion-failure path (:1301) and compacted transports all echo the staged value; the fallback recompute (:1251-1267) and the `'invalid-physical-revision'` catch stay only as a last resort. If the type/validator must change (`PhysicPaintRotoPhysicalEditApplyPayload` / the result interface / `hasOnlyKeys` allowlists at types:1273-1276 and the validator at :1760-1794), update BOTH sides and every result producer — the validator requires a non-empty `stagedRevision` (types:1771) and rejects unknown keys outright.
    - **H-C (layer/track identity):** make the identity the child hashes equal the identity the parent applies to — one authority for the records read and the payload's `trackId` (:2176), or make the payload carry the identity the records were read under and have the parent apply/hash THAT. If the verdict instead shows the studio ports feed a different track than the document's active track, fix the port wiring at the narrowest point; do not restructure the Studio.
    - If the verdict named a different non-structural producer, fix it at the same layer with the same law: the child's staged identity and the parent's echoed identity must be equal by construction on every path.

    Harness consequences (mechanical, no assertion lost): if the payload now carries `stagedRevision`, update `makeResult` (:726-776) to echo it instead of recomputing; keep at least ONE leg that recomputes independently (the real-store legs in the bridge file) so the echo cannot mask a disagreement. Any new field added to the wire payload must pass the real validator — assert that with a bridge-level result leg (the validator rejects unknown keys).

    Untouched, deliberately: the settlement predicate's meaning and its term list; `PHYSICAL_EDIT_TIMEOUT_MS` and the timeout message; the recovery-lease design; `applyPhysicPaintRotoPhysicalMap`'s validation order and error strings; the capacity/launch laws (ji7 `0ff76f43`) and the reveal extent law (kov `ff645d38`) — `resizeRevealRail`'s identical-span `{ ok: true, descriptor: null }` no-write stays exactly as-is; `commitRevealBake` and the WR-02 per-frame bake.
  </behavior>
  <action>
    Capture the RED output first (Task 1's runs already are the entry evidence — re-run them if the working tree changed). Apply part 1, re-run, then part 2 per the verdict, re-run. Record in the SUMMARY: the verdict, the exact fix per hypothesis, and the echo law ("every parent result carries the child's stagedRevision on every path"). Commit: `fix(260921-c7x): physical-edit settlement — terminal mismatch release and one revision authority across the bridge`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor &amp;&amp; pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts &amp;&amp; pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
  </verify>
  <done>Leg 1 (layer-2 identity parity) and leg 2 (latch release: pendingOperationId null, 'settlement-mismatch' reason, snapshot restored, no in-flight message on the next edit, 'mismatch' still returned) are green with raw RED output recorded; leg 3 and every pre-existing leg stay green; a success descriptor's `stagedRevision` equals the child's staged revision on the real stores, and an early reject echoes it too (no 'invalid-physical-revision' on honest rejects); an identical-span reveal resize is still a no-write no-op; `tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Gates, guardrail audit, SUMMARY, native UAT rows pending</name>
  <files>.planning/quick/260921-c7x-physical-edit-cluster-result-mismatch-ti/260921-c7x-SUMMARY.md</files>
  <behavior>
    - Regression gates in order: the two targeted files; then the physical-edit neighbours (`src/components/physic-paint/PhysicsPaintStudio.test.ts`, `src/stores/efxPaintStore.reveal.test.ts` — the kov WR-01 lock must stay green); `pnpm --filter efx-motion-editor exec tsc --noEmit`; then the full suite `pnpm --filter efx-motion-editor exec vitest run` (background it if the runtime allows; resume only on completion). Attribute every failure by NAME, including the pre-existing failures already recorded in `.planning/STATE.md` — never absorb one silently.
    - Guardrail audit by READING the diff (not grep alone), against the plan-base commit: (1) the settlement predicate's term list and meaning are unchanged — operationId-first ownership intact; (2) the timeout constant/message and the recovery-lease design are unchanged; (3) mismatched results are never retried or ignored — the branch is terminal and still returns 'mismatch'; (4) the kov WR-01 no-op and the ji7 capacity laws are not in the diff; (5) no rail rendering, resolver, or Studio layout file is in the diff; (6) the only wire-format change (if any) is the identity field the verdict named, validators updated on both sides.
    - SUMMARY at `.planning/quick/260921-c7x-physical-edit-cluster-result-mismatch-ti/260921-c7x-SUMMARY.md`: the verdict (root + producer + the exact descriptor terms captured) with the RAW captured descriptors and the raw RED output; the fix per part; the echo law; the guardrail audit checklist; gate results; and the native UAT rows below marked explicitly PENDING — never claimed. If the verdict was STRUCTURAL, the SUMMARY IS the deliverable: verdict, evidence, no production change.
  </behavior>
  <action>
    Run the gates, write the SUMMARY, leave the native rows pending. Commit: `docs(260921-c7x): physical-edit cluster — verdict, fix, gates, native UAT pending`.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor &amp;&amp; pnpm --filter efx-motion-editor exec vitest run src/lib/physicPaintBridge.test.ts src/components/physic-paint/hooks/useRotoPhysicalEditCoordinator.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts src/stores/efxPaintStore.reveal.test.ts &amp;&amp; pnpm --filter efx-motion-editor exec tsc --noEmit</automated>
  </verify>
  <done>Targeted files, the kov WR-01 lock, `tsc --noEmit` and the full suite are green with any failure attributed by name; the guardrail audit is recorded from the diff; `git diff --name-only <plan-base>..HEAD` lists only this plan's files plus `.planning/` artifacts; the SUMMARY carries the verdict with raw descriptors, the before/after, the gates, and the four native UAT rows marked pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child coordinator → parent bridge (physical-edit wire) | The staged payload and the echoed result cross here; the revision/identity fields are the settlement contract — an asymmetric producer silently voids every edit. |
| parent result → child settlement predicate | Untrusted echo: the child accepts only a descriptor that matches its staged tuple term-for-term; the `operationId` is the sole ownership term. |
| pending edit → in-flight lease | A latched pending strands the session's edit slot; the lease/timer is the only escape today. |
| transport compaction → parent store | Unchanged records cross as `{ keyId, appFrame, refToken }`; expansion resolves against the parent store and fails closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260921-c7x-01 | Denial of service | `consumePhysicalEditResult` mismatch branch (:1421-1424) | high | mitigate | A mismatch now settles terminally: `finalizeFailed(..., 'settlement-mismatch', ...)` restores the snapshot and `clearPendingOnce` releases pending, in-flight flag, operationId signal, lease and timer. The 5 s timeout stops being the only escape and the next edit is never refused with the in-flight message. |
| T-260921-c7x-02 | Tampering | settlement identity (`stagedRevision` producers) | high | mitigate | One revision authority: the child stages the revision over the exact collection the wire ships, and every parent result echoes the child-owned revision on every path (no recompute over ref-shaped records). Verified by real-store legs; a fallback recompute remains only as a last resort. |
| T-260921-c7x-03 | Spoofing | acceptance widening | medium | mitigate | The predicate's meaning and term list are unchanged; a result with a different `operationId` is still ignored (locked by a green leg); a mismatch still returns `'mismatch'` to the result controller so the diagnostic surface cannot be silenced. |
| T-260921-c7x-04 | Tampering | wire payload/result shape | low | mitigate | Any new field is added to both the interface and the `hasOnlyKeys` allowlist and to every producer; the real validator (non-empty `stagedRevision` required) runs in the bridge legs, so an unknown-key result fails loudly rather than silently. |
| T-260921-c7x-05 | Information disclosure | offline desktop app | low | accept | No network, no new I/O, no persisted-format change — the diff is settlement/identity logic plus tests. |
| T-260921-c7x-SC | Tampering | pnpm installs | low | accept | No dependency changes in this plan — no package-legitimacy gate required. |
</threat_model>

<verification>
- Verdict gate: the SUMMARY names the root and its producer, backed by the RAW captured parent descriptors (success, each bisected lifecycle term, early reject, compacted transport, layer-2, reveal unchanged-span and stretch) and the two child revision hashes. A STRUCTURAL verdict means zero production change and no Task 2/3 execution.
- Mandated RED legs: (1) a second-layer edit settles with layer-1 parity — red at base, green after; (2) after a forced mismatch the in-flight slot is released (no permanent latch) — red at base, green after.
- Echo law: a success descriptor's `stagedRevision` equals the child's staged revision on the real stores, and an honest early reject echoes it too (no 'invalid-physical-revision').
- Latch law: after a mismatch, `pendingOperationId` is null, no in-flight message on the next execute, failure reason 'settlement-mismatch', snapshot restored, deferred document never published, `'mismatch'` still returned.
- Guardrail audit (by reading the diff): predicate meaning unchanged, ownership still operationId-first, timeout/lease design unchanged, mismatches never retried, kov WR-01 no-op and ji7 capacity laws untouched, no rail rendering/resolver/Studio layout change.
- Regression: the two targeted files + PhysicsPaintStudio + the kov reveal lock + `tsc --noEmit` green; the full suite with 0 new failures (pre-existing failures attributed by name).
- Scope gate: `git diff --name-only <plan-base>..HEAD` lists only this plan's files plus `.planning/` artifacts.
- Native UAT: the four rows below are listed in the SUMMARY as pending, never claimed (the executor does not launch the app).
</verification>

<human_verification>
Native UAT — OWED BY THE USER after execution; the executor leaves these pending and never claims them (the executor does not launch the app; the user runs the dev server).
1. On the SECOND layer: move a key, then click-positioning onto an interpolated frame — both behave exactly as on the first layer.
2. Move a rail, then stretch a reveal rail — each commits, and the reveal stretch persists across save/reopen (this replays the kov §4/WR-01 rang ③ row, which fix B blocked and which must be replayed now).
3. Apply key spacing on a rail — the spacing applies and NO "A Roto physical edit is already in flight." tooltip follows.
4. Deliberately trigger one failure (an edit the parent rejects), then make the NEXT edit — it must not be blocked, and no "timed out. The previous state was restored." follows.
5. Replay the kov §4/WR-02 rang ② row (multi-image reference frame-aligned bake) using a SEQUENCE IMPORT in one go — one-image-at-a-time import makes rang ② untestable.
</human_verification>

<success_criteria>
- The cluster's three visible symptoms are dead at their root: rail edits settle exactly once, layer-2 key moves and interpolated-frame targeting match layer 1, and a failed edit releases the in-flight slot immediately (no 'already in flight', no 5 s timeout as the only escape).
- The settlement identity has ONE authority across the child/parent boundary: every honest parent result echoes the child's staged revision on every path, including compacted transports and synthesized clip lifecycles.
- The mandated RED legs (layer-2 identity parity; latch release) are red at base and green after; the ownership-first guard leg stays green; the pinned test at :1257-1282 is rewritten to the new law.
- Guardrails held: no retry of mismatched results, predicate meaning unchanged, kov WR-01 no-op and ji7 capacity laws untouched.
- Targeted suites, the kov reveal lock, `tsc --noEmit` and the full suite green; scope gate clean; the native UAT rows left explicitly pending.
</success_criteria>

<output>
Create `.planning/quick/260921-c7x-physical-edit-cluster-result-mismatch-ti/260921-c7x-SUMMARY.md` when done — carrying the verdict with the raw captured descriptors and the raw RED output, the fix per part and the echo law, the guardrail audit checklist, the gate results, and the native UAT rows marked pending. If the verdict was STRUCTURAL, the SUMMARY is the sole deliverable.
</output>
