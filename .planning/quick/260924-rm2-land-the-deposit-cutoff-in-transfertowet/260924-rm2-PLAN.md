---
phase: quick-260924-rm2
plan: 260924-rm2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/efx-physic-paint/src/core/productionAaSettleMeasurement.test.ts
  - packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts
  - packages/efx-physic-paint/src/core/wet-layer.ts
  - .planning/quick/260924-rm2-land-the-deposit-cutoff-in-transfertowet/260924-rm2-SUMMARY.md
autonomous: true
requirements: [QUICK-260924-RM2]
estimate:
  tokens: 39000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The production path (transferToWetLayerClipped's own deposit gate, measured on the production-raster pyp harness) holds envelope W_visible ≤ 8 at default water AND texture d(b) ≥ 1 at every water 10/50/90, both papers, with PIN 0 (ratio ∈ [0.99, 1.01]) and PIN 0b (≥ 0.95× floors) in bounds in both harnesses — asserted by the new contract pins, GREEN only after the cutoff lands"
    - "The cutoff is include/exclude at the deposit gate only: deposited pixels keep the exact base math (a/255)*3000, body pixels keep full alpha — no scaling of deposit alpha with water or tier anywhere (m7w 24f40261 failure mode absent)"
    - "Landed tier is 70 (preferred) or 130 (only if the integration-path measurement at 70 shows W_visible > 8); tier 200 is never used — chosen tier and measured reason recorded in SUMMARY"
    - "The legacy synthetic suite ends coherent: PIN 2 explicitly DEFERRED with visible in-code rationale (known gap, not reshaped, not faked green); PIN 1 and the texture gate either pass with the landed cutoff or are explicitly dispositioned with rationale (synthetic ramp is not the oracle); zero unexplained RED remains"
    - "PIN 0 + PIN 0b never moved by the landed cutoff: if either moves in the integration path the plan STOPs and no GREEN commit exists"
    - "Locked files untouched at finish: git diff empty for fluids.ts, compositor.ts, canvas.ts, canvas.strokePreviewRibbon.test.ts, brush/paint.ts, brush/stroke.ts; SUMMARY status is automated-ready with the 6 native UAT rows listed — no claim of done before live UAT"
  artifacts:
    - packages/efx-physic-paint/src/core/productionAaSettleMeasurement.test.ts (new production-path contract-pin describe)
    - packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts (PIN 2 deferral record + post-GREEN dispositions)
    - packages/efx-physic-paint/src/core/wet-layer.ts (deposit keep-gate constant at the landed tier)
    - .planning/quick/260924-rm2-land-the-deposit-cutoff-in-transfertowet/260924-rm2-SUMMARY.md (written, left uncommitted — orchestrator owns docs commit)
  key_links:
    - "wet-layer.ts deposit keep-gate → pyp production-path contract pins (the RED→GREEN wire: base gate 20 fails envelope on null paper; gate 70/130 must pass every asserted cell)"
    - "wet-layer.ts deposit keep-gate → physicsSettledFootprint.test.ts integration measurement (PIN 0/0b hard gates catch any body-pixel movement; PIN 1/texture dispositioned against the pyp oracle)"
    - "pyp 24-cell OUTCOME TABLE (tier 70/130 rows) → tier choice evidence cited in SUMMARY decision trail"
---

<objective>
Land the deposit cutoff in `transferToWetLayerClipped` (wet-layer.ts): raise the deposit keep-gate so the settled physics stroke holds the ribbon envelope (W_visible ≤ 8 at default water) while feather d(b) ≥ 1 survives at every water — include/exclude at the deposit gate only, never alpha modulation.

Purpose: 260924-pyp measured the full tier outcome table on production's real continuous AA raster: tier 70 (and 130) hold envelope ≤ 8 AND d(b) = 1–2 at all waters/papers with 0 STOP findings, while tier 200 is a proven hard stamp (d(b)=0) and base tier 20 fails the envelope on null paper. This quick converts that evidence into the shipped fix under TDD: RED contract pins first, GREEN cutoff second, verification third.

Output: a production-path contract-pin describe in the pyp harness (RED at base), the gate constant landed in wet-layer.ts (GREEN), PIN 2 deferral + any synthetic-suite dispositions recorded in code, and a SUMMARY with status automated-ready (6 native UAT rows for the user). fluids.ts/compositor/preview untouched; no installs; no push.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260924-pyp-measure-production-s-continuous-aa-raste/260924-pyp-SUMMARY.md
@.planning/quick/260924-ort-water-coupled-deposit-cutoff-in-transfer/260924-ort-SUMMARY.md
@.planning/quick/260924-nqe-quick-8c-redo-bound-the-physics-settled-/260924-nqe-SUMMARY.md
@.planning/quick/260924-m7w-quick-8b-physics-stroke-settles-bolder-t/260924-m7w-SUMMARY.md
@packages/efx-physic-paint/src/core/wet-layer.ts
@packages/efx-physic-paint/src/core/productionAaSettleMeasurement.test.ts
@packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — production-path contract pins + PIN 2 deferral record (tests-only)</name>
  <files>packages/efx-physic-paint/src/core/productionAaSettleMeasurement.test.ts, packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts</files>
  <behavior>
    - Production path at base (keep-gate 20): envelope pin FAILS — null paper @ water 50 gives W_visible 9 > 8 (pyp OUTCOME TABLE tier-20 rows); texture pin FAILS on synthetic paper at waters 10/50 (d(b) = 0)
    - Production path PIN 0: body deposit ratio ∈ [0.99, 1.01] at every water, both papers — PASSES at base and must keep passing after GREEN
    - Production path PIN 0b: post-settle body meanAlpha AND meanDisplay ≥ 0.95× the pyp base literals — PASSES at base, must keep passing after GREEN
    - The 4 pre-existing pyp tests (sanity, PIN 3, base-tier, OUTCOME TABLE) stay GREEN throughout
    - Legacy suite after this task: PIN 2 skipped-with-rationale, PIN 1 + texture still RED (contract evidence), PIN 0/0b/3/sweep pass
  </behavior>
  <action>TDD RED step — tests only, zero production edits. Two edits.

(1) productionAaSettleMeasurement.test.ts — add a new describe block (mark it `260924-rm2`) asserting the deposit-cutoff CONTRACT on the PRODUCTION path: cells run with the RAW profile (no in-harness tier pre-filter — `applyTier` is a no-op for tier ≤ 20, so `transferToWetLayerClipped`'s own deposit gate decides inclusion; that no-op IS the integration path at whatever gate production applies). Reuse the existing helpers (`runCell`, `bodyState`, `pin0RatioRange`, `bodyMeans`, `checkPinGates`, `WATERS`, `PAPERS`, `ENVELOPE_BOUND`) — do not duplicate the substrate, do not import from physicsSettledFootprint.test.ts. Assert exactly: (a) envelope — `W_visible ≤ 8` at DEFAULT water 50, both papers; (b) texture — `d(b) = W_settle − W_deposit ≥ 1` at EVERY water {10, 50, 90}, both papers (this exercises the D-08 synthetic-paper adsorption path alongside null); (c) PIN 0 + PIN 0b as HARD gates over every production-path cell via `checkPinGates(..., assertMode true)` — any movement fails the run (STOP, never loosen a bound). Log the production-path table rows for direct lift into SUMMARY. Do NOT modify the existing 4 tests — they must stay green.

(2) physicsSettledFootprint.test.ts — PIN 2 deferral bookkeeping (user decision): convert the PIN 2 test into an explicitly-marked DEFERRED test that keeps the full original law text and body visible but does not report RED (e.g. `it.skip` with a name that says DEFERRED, plus a comment block citing: 260924-rm2 decision — water-monotone width is a known gap deferred to a future fluids.ts water-coupling feature; nqe structural evidence (the local fluid path has no water→settle coupling; widths are identical across waters at base — this is already true today); NOT part of this fix; the strict-monotone law must not be loosened, reshaped, or faked green — future work must satisfy the text as written). Do not delete the test history. Leave PIN 1, texture, PIN 0, PIN 0b, PIN 3, sweep untouched: PIN 1 and texture stay RED-at-base until Task 2 measures them against the landed cutoff.

Commit tests-only: `test(260924-rm2): RED — production-path deposit-cutoff contract pins + PIN 2 deferral record`.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/productionAaSettleMeasurement.test.ts; ../../app/node_modules/.bin/vitest run src/core/physicsSettledFootprint.test.ts</automated>
  </verify>
  <done>pyp suite exits 1 with ONLY the new contract-pin describe RED (envelope fails on null @ default water and/or texture on synthetic @ low water); no `STOP finding` PIN 0/0b lines; the 4 pre-existing pyp tests still pass. Legacy suite: 2 failed (PIN 1, texture) | 4 passed | PIN 2 deferred-skip with visible rationale comment. Production untouched: `git diff --name-only -- packages/efx-physic-paint/src/core/wet-layer.ts` empty. Tests-only RED commit exists.</done>
  <precondition>Working tree clean at efac33ac; pyp suite 4 passed; legacy suite 3 failed (PIN 1, PIN 2, texture) | 4 passed.</precondition>
  <reversibility rating="reversible">Tests-only commit; zero production edits by design.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — deposit keep-gate cutoff in transferToWetLayerClipped (tier 70 preferred)</name>
  <files>packages/efx-physic-paint/src/core/wet-layer.ts, packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts</files>
  <behavior>
    - Production path post-fix at tier 70: envelope `W_visible ≤ 8` @ water 50 both papers; texture `d(b) ≥ 1` @ waters 10/50/90 both papers (pyp tier-70 rows predict 7–8 and 1–2) — the Task-1 contract pins turn GREEN
    - Fallback: if any production-path `W_visible > 8` at tier 70, switch to tier 130 and re-run (pyp tier-130 rows: 7–8 ≤ 8, d(b) 1–2); if neither holds → STOP, no GREEN; tier 200 is never tried (proven hard stamp d(b) = 0)
    - PIN 0 ratio ∈ [0.99, 1.01] and PIN 0b ≥ 0.95× floors hold in BOTH harnesses at every water — if either moves in the integration path → STOP, no GREEN
    - PIN 3 determinism stays GREEN throughout; legacy sweep/PIN 0/PIN 0b/PIN 3 keep passing
  </behavior>
  <action>GREEN step — land the cutoff, then measure and disposition.

(1) wet-layer.ts, inside `transferToWetLayerClipped` only: introduce a named module constant (e.g. `DEPOSIT_KEEP_TIER = 70`) and replace the literal keep-gate (`if (a < 20) continue`) with the constant. This is include/exclude at the deposit gate — excluded pixels never run; included pixels keep the exact base deposit math `(a / 255) * 3000` and the untouched D-08 paper adsorption, D-09 granulation, D-10 subtractive mixing, wetness write, and strokeOpacity. FORBIDDEN (m7w failure 24f40261 / revert 1648658b): any scaling of depositAlpha, wetness, or strokeOpacity with waterAmount, with tier, or with any other parameter — body pixels keep full alpha (body plateau ≈ 244 ≫ 70; VERIFY via PIN 0/0b, do not assume). Comment on the constant: cite the 260924-pyp outcome table (tier 70 holds envelope ≤ 8 with d(b) = 1–2 at all waters/papers; tier 200 forbidden = hard stamp d(b) = 0; base tier 20 fails envelope on null paper; include/exclude only — never alpha modulation).

(2) Run the pyp suite — the Task-1 contract pins ARE the integration-path measurement (no in-harness pre-filter). Tier 70 is the landed choice if every pin passes; if envelope shows `W_visible > 8` anywhere, change the constant to 130 and re-run (only fallback authorized); if 130 also fails → STOP and report (no GREEN commit). Record the chosen tier and the measured numbers.

(3) STOP clauses — halt before any GREEN commit and report if: PIN 0 ratio leaves [0.99, 1.01] OR PIN 0b drops below 0.95× floors in EITHER harness (the cutoff moved body pixels); texture `d(b) < 1` at any water on the production raster (hard stamp); envelope unholdable at both 70 and 130.

(4) Run the legacy synthetic suite. Expected (ort's threshold-70 measurement on the graduated 160/110/60 ramp: W_deposit 10, d(b) = 0): PIN 1 (envelope) and the texture gate fail while PIN 0/0b/3/sweep pass. For each of PIN 1 / texture: if it PASSES post-GREEN, keep it active as-is; if it FAILS, disposition it explicitly in-file — skip with a rationale comment stating the synthetic graduated ramp is NOT the oracle for this contract (nqe proved this coarse ramp cannot express the dual gate; the contract is asserted GREEN on the production raster by the 260924-pyp harness at the landed tier) — keep the assertion code visible, never delete it, never loosen or reshape the law or the raster to fake a pass. PIN 0b in this suite is a hard gate: if it fails (body means below floors) → STOP, no GREEN.

(5) Commit GREEN atomically: `fix(260924-rm2): deposit keep-gate cutoff at tier <N> in transferToWetLayerClipped` — wet-layer.ts + physicsSettledFootprint.test.ts (dispositions) only. No other production file may appear in the commit.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/productionAaSettleMeasurement.test.ts src/core/physicsSettledFootprint.test.ts</automated>
  </verify>
  <done>pyp suite GREEN including the new contract pins at the landed tier (70 or 130, recorded with measured reason); legacy suite exits 0 in its coherent post-deferral state (PIN 2 deferred; PIN 1 + texture pass or dispositioned with rationale; PIN 0/0b/3/sweep pass); `git diff` for wet-layer.ts shows only the gate constant/line + comment (deposit arithmetic untouched); GREEN commit contains exactly wet-layer.ts + the legacy test file.</done>
  <precondition>Task 1 RED contract pins committed; pyp suite RED at base via the new describe.</precondition>
  <reversibility rating="costly">Changes production stroke deposit behavior — user-visible rendering change gated by the 6-row native UAT; revertable by reverting the GREEN commit but only live UAT proves the wet look.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Verification battery + SUMMARY (automated-ready)</name>
  <files>.planning/quick/260924-rm2-land-the-deposit-cutoff-in-transfertowet/260924-rm2-SUMMARY.md</files>
  <action>Run the full verification battery, then write the SUMMARY (left uncommitted — orchestrator owns docs commit; no push).

Battery: (1) pyp suite — GREEN including contract pins; capture the production-path table rows (per paper/water: W_deposit, W_settle, W_visible, d(b), PIN 0 min/max, PIN 0b means, envelope check) for the SUMMARY. (2) Legacy suite — enumerate the final coherent state test-by-test (pass / deferred / dispositioned); zero unexplained RED. (3) Package-wide regression: `cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run` — the ONLY acceptable failure is the pre-existing `EfxPaintEngine.liveAlphaCache.test.ts` (confirmed failing at base, deferred from 260924-koa); any other failing file → investigate, and if it traces to the cutoff → STOP before writing the SUMMARY. (4) Guardrails: `git diff --name-only` empty for locked files — src/core/fluids.ts, src/render/compositor.ts, src/render/canvas.ts, src/render/canvas.strokePreviewRibbon.test.ts, src/brush/paint.ts, src/brush/stroke.ts; wet-layer.ts diff confined to the gate constant/line + comment; no package.json / pnpm-lock changes; no UI files.

Write SUMMARY.md (frontmatter: phase quick-260924-rm2, plan 260924-rm2, status: automated-ready, subsystem, metrics with tasks/commits counts) containing: carry-over citations (260924-pyp = evidence base / oracle with the 24-cell outcome table; ort STOP clause ii superseded as oracle by pyp; nqe structural proof = basis of the PIN 2 deferral; m7w alpha-carry failure 24f40261/revert 1648658b = the never-modulate-alpha law); the mechanism (include/exclude at the deposit gate in `transferToWetLayerClipped`; deposited pixels keep exact base math `(a/255)*3000`; body pixels full alpha); the tier decision trail (70 landed vs 130 fallback with the actual integration-path measurements; 200 forbidden — proven hard stamp d(b)=0; base 20 fails envelope on null paper); PIN 0/0b zero-movement evidence for both harnesses at every water; texture d(b) values per water/paper at the landed tier (granulation/D-08 path untouched); the PIN 2 deferral record (known gap: water does not widen the mark today, same as at base per nqe — future fluids.ts water-coupling feature, NOT part of this fix; visible in code as a deferred test with the law text preserved); the synthetic-suite PIN 1/texture dispositions with rationale (synthetic graduated ramp is not the oracle; contract asserted GREEN on the production raster in the pyp suite); verification battery results (incl. the pre-existing liveAlphaCache failure); evidence citation to 260924-pyp; and the 6 native UAT rows verbatim below — with an explicit note that status is automated-ready, GREEN shipped, native UAT pending, no live UAT claimed.

Native UAT rows (copy VERBATIM into SUMMARY):
1. Physics, default water → settled stroke matches preview ribbon side-by-side, taper visible
2. stroke body FULLY as opaque as before (side-by-side vs pre-fix build — no wash-out)
3. Normal mode unchanged
4. two identical strokes in a held pose → identical footprint (no boil)
5. regression: wet edges + granulation present (NOT a hard stamp), cursor ring unchanged
6. documented non-goal: water does not change width (same as today) — confirm no accidental change either way</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/productionAaSettleMeasurement.test.ts src/core/physicsSettledFootprint.test.ts; git diff --name-only -- packages/efx-physic-paint/src/core/fluids.ts packages/efx-physic-paint/src/render/compositor.ts packages/efx-physic-paint/src/render/canvas.ts packages/efx-physic-paint/src/render/canvas.strokePreviewRibbon.test.ts packages/efx-physic-paint/src/brush/paint.ts packages/efx-physic-paint/src/brush/stroke.ts</automated>
  </verify>
  <done>Both suites GREEN (pyp incl. contract pins; legacy coherent — deferred/dispositioned items enumerated, zero unexplained RED); package suite shows only the pre-existing liveAlphaCache failure; guardrail `git diff --name-only` output empty; SUMMARY exists with status automated-ready, tier decision trail, PIN 2 deferral, PIN 1/texture dispositions, pyp evidence citation, and the 6 UAT rows verbatim; SUMMARY left uncommitted; no push; no claim of done before live UAT.</done>
  <reversibility rating="reversible">Read-only verification plus a docs write.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → wet-layer production module | the pyp/legacy harnesses drive `transferToWetLayerClipped` with locally generated buffers — no network, no untrusted input |
| executor edit surface → locked production files | the only sanctioned production edit is the deposit gate line in wet-layer.ts; fluids/compositor/canvas/preview/paint/stroke must stay out of the diff |
| package manager → supply chain | any install attempt crosses the legitimacy boundary with no Package Legitimacy Audit table present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-rm2-01 | Tampering | wet-layer.ts deposit path (cutoff implemented as alpha scaling — m7w 24f40261 failure mode) | high | mitigate | cutoff is include/exclude at the keep-gate only; deposit `(a/255)*3000` arithmetic untouched; PIN 0 + PIN 0b are hard asserted gates in BOTH harnesses — any body-pixel movement fails GREEN (STOP clause, Task 2) |
| T-rm2-02 | Tampering | scope creep into locked files (fluids.ts, compositor.ts, canvas.ts, strokePreviewRibbon, paint.ts, brush/stroke.ts) | high | mitigate | GREEN commit restricted to wet-layer.ts + test files; Task 2 and Task 3 gate on `git diff --name-only` over the locked path list |
| T-rm2-03 | Repudiation | bounds faked: PIN 2 reshaped green, or PIN 1/texture silently deleted to clear RED | medium | mitigate | PIN 2 kept as an explicit deferred test with the law text preserved and rationale comment; PIN 1/texture dispositions keep assertions visible with pyp-oracle citation; SUMMARY enumerates the final suite state test-by-test |
| T-rm2-04 | Tampering | package installs without a legitimacy audit | high | mitigate | no installs in scope (T-rm2-SC); Task 3 verifies no package.json/pnpm-lock changes |
| T-rm2-05 | Information Disclosure | none — local vitest runs, no network, no auth, no user data | low | accept | no external I/O in scope |
</threat_model>

<verification>
- RED gate: `cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/productionAaSettleMeasurement.test.ts` exits 1 with only the new contract-pin describe failing (base gate 20 fails envelope on null paper); legacy suite 2 failed | 4 passed | PIN 2 deferred-skip.
- GREEN gate: same command plus the legacy suite exits 0 — contract pins GREEN at landed tier, legacy coherent (zero unexplained RED), PIN 3 green throughout.
- Package regression: `../../app/node_modules/.bin/vitest run` — only pre-existing `EfxPaintEngine.liveAlphaCache.test.ts` fails.
- Guardrails: `git diff --name-only` empty for fluids.ts, compositor.ts, canvas.ts, canvas.strokePreviewRibbon.test.ts, paint.ts, brush/stroke.ts; wet-layer.ts diff = gate constant/line + comment only; no installs; no push.
</verification>

<success_criteria>
- Contract pins (envelope ≤ 8 @ default water, d(b) ≥ 1 @ every water, PIN 0/0b in bounds) GREEN on the production raster with the landed tier recorded (70 preferred, 130 fallback, 200 never).
- Deposit cutoff is pure include/exclude at the gate — no alpha modulation anywhere; body opacity provably untouched in both harnesses.
- Legacy suite coherent post-deferral: PIN 2 explicitly deferred with visible rationale; PIN 1/texture pass or dispositioned with rationale; no unexplained RED.
- SUMMARY status automated-ready with decision trail, pyp evidence citation, and the 6 native UAT rows verbatim; locked-file diffs empty; no done claim before live UAT.
</success_criteria>

<output>
Create `.planning/quick/260924-rm2-land-the-deposit-cutoff-in-transfertowet/260924-rm2-SUMMARY.md` when done (uncommitted — orchestrator owns the docs commit)
</output>
