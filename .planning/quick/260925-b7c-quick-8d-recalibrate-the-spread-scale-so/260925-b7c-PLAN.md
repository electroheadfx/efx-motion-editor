---
phase: quick-260925-b7c
plan: 260925-b7c
type: tdd
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260925-b7c-quick-8d-recalibrate-the-spread-scale-so/260925-b7c-SUMMARY.md
  - packages/efx-physic-paint/src/core/spreadScale.ts
  - packages/efx-physic-paint/src/core/spreadScale.test.ts
  - packages/efx-physic-paint/src/engine/EfxPaintEngine.ts
  - packages/efx-physic-paint/src/core/physicsWidthScaling.test.ts
  - packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts
  - packages/efx-physic-paint/src/core/productionAaSettleMeasurement.test.ts
autonomous: true
requirements: [QUICK-260925-B7C]
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Slider 50 produces exactly the physical effect old slider 30 produced: spreadCurveFor(50) is bitwise-equal to legacy (30/100)² = 0.09, and the derived ticks = max(1, ceil(curve·10)) and margin = ceil(2 + waterCurve·brushR·0.6 + curve·brushR·0.4) match the legacy-30 values at every cell of a fixed brush/water grid — asserted by the new spreadScale.test.ts, whose midpoint pin is RED at base"
    - "Slider 100 produces exactly the legacy-100 effect (spreadCurveFor(100) bitwise 1.0, same ticks=10 and margin), slider 0 gives spreadCurve = 0, and the curve is non-decreasing across the whole 0–100 range (range, zero, and monotone pins)"
    - "There is ONE law for the spread curve: both EfxPaintEngine local-mode spread computations (finalize continuation site and local-step site) and all three engine-mirrored harnesses (physicsWidthScaling, physicsSettledFootprint, productionAaSettleMeasurement) read the shared spreadCurveFor helper — no divergent inline (s/100)² copies remain"
    - "The UI contract is unchanged: slider min 0 / max 100 / label Spread / default displayed at 50 (engine state default 50, physicsPaintStudioSettings spread: 50) — zero UI file diffs; stored Spread values in projects/roto scripts are reinterpreted on the new scale with no migration code"
    - "Opacity law holds: depositAlpha / strokeOpacity / body coverage are never edited (m7w law, 24f40261/revert 1648658b) and PIN 0 body ratio stays 1.0000; the 260924-stb thickness field in fluids.ts and DEPOSIT_KEEP_TIER = 70 show empty diffs"
    - "After harness parity (Task 3), the 260924-stb/rm2 law gates still pass at the new default — PIN 0/0b opacity, monotone thin≤thick, determinism, texture presence, hairline ceiling — with calibrated numeric expectations recalibrated only where the intentional default change moved them, documented before/after in the SUMMARY; if a law gate cannot hold without touching the thickness field, the opacity path, the keep-gate, the pinned curve, or the UI, the plan STOPs with a report instead of shipping"
  artifacts:
    - packages/efx-physic-paint/src/core/spreadScale.ts (single spreadCurveFor law, new)
    - packages/efx-physic-paint/src/core/spreadScale.test.ts (calibration/range/zero/monotone pins, RED at base)
    - .planning/quick/260925-b7c-quick-8d-recalibrate-the-spread-scale-so/260925-b7c-SUMMARY.md (status automated-ready, 7 native UAT rows)
  key_links:
    - "spreadCurveFor → EfxPaintEngine margin + ticks at BOTH local-mode call sites (the only consumption path of localSpreadStrength)"
    - "spreadCurveFor → the three harness K_TICKS/engineLocalBbox mirrors (Engine-mirrored defaults contract — parity is what keeps the stb pins proving production behavior)"
    - "piecewise curve endpoints (0 / 50→0.09 / 100→1.0) ↔ the user's calibration law of 2026-09-25 (the three pinned points, verified bitwise in float before planning)"
    - "new default K ticks = 1 (was 3) ↔ harness law gates (the reconciliation seam of Task 3, with an explicit STOP decision tree)"
---

<objective>
Remap the Spread slider scale so the preview-matched width sits at the slider midpoint: new 50 = today's 30 (spreadCurve 0.09), new 100 = today's 100 (spreadCurve 1.0), new 0 = no spread, monotone in between — while the slider keeps min 0 / max 100 / label "Spread" and the default stays 50 (user decision 2026-09-25; Spread control is D-11).

Purpose: after 260924-stb the gesture survives settling, but the out-of-the-box stroke (default 50 = old "loose" feel, spreadCurve 0.25) settles fatter than the preview ribbon, violating the 260924-koa preview contract. The user measured live that value 30 is preview-matched; this quick makes that point the midpoint by changing the parameter curve only — no UI change, no opacity change, no touch of the locked thickness field.

Output: a shared spreadCurveFor helper (one law for engine + harnesses), the two EfxPaintEngine call sites wired to it, calibration/range/zero/monotone pins RED at base then GREEN, harness parity with a documented reconciliation, and a SUMMARY with status automated-ready (7 native UAT rows for the user). No installs, no push, no UI.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quick/260924-stb-quick-8c-physics-intensity-must-scale-wi/260924-stb-SUMMARY.md
@.planning/quick/260924-rm2-land-the-deposit-cutoff-in-transfertowet/260924-rm2-SUMMARY.md

Key code anchors (read before editing):
- packages/efx-physic-paint/src/engine/EfxPaintEngine.ts — default `localSpreadStrength: 50` (~line 610), `setLocalSpreadStrength` clamp 0–100 (~line 741), spread computation site 1 in the finalize local-continuation path (~line 2259: spreadFrac → spreadCurve → margin + ticks into createLocalFluidPhysicsContinuation), spread computation site 2 in the local-step path (~line 2534: same arithmetic into localFluidPhysicsStep).
- packages/efx-physic-paint/src/core/physicsWidthScaling.test.ts, physicsSettledFootprint.test.ts, productionAaSettleMeasurement.test.ts — each carries an "Engine-mirrored defaults" block with `SPREAD_STRENGTH = 50`, `K_TICKS = Math.max(1, Math.ceil((SPREAD_STRENGTH / 100) ** 2 * 10))`, and an inline `(SPREAD_STRENGTH / 100) ** 2` inside engineLocalBbox's margin — these mirror production and must follow the remap.
- app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx line 665 (slider min/max/label) and app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts (`spread: 50`) — read-only references proving the UI needs no edit.
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — spreadCurveFor seam + calibration pins</name>
  <files>packages/efx-physic-paint/src/core/spreadScale.ts, packages/efx-physic-paint/src/core/spreadScale.test.ts, packages/efx-physic-paint/src/engine/EfxPaintEngine.ts</files>
  <behavior>
  - Test 1 (calibration pin, MUST FAIL at base): spreadCurveFor(50) toBe bitwise-equal to legacy (30/100)² (== 0.09), and at brushR ∈ {2, 8, 16, 32} × water ∈ {10, 50, 90} the derived ticks = max(1, ceil(curve·10)) and margin = ceil(2 + (water/100)²·brushR·0.6 + curve·brushR·0.4) equal the same quantities computed from legacy (30/100)² — every cell equal.
  - Test 2 (range pin): spreadCurveFor(100) toBe bitwise-equal to legacy (100/100)² (== 1.0), derived ticks (= 10) and margin equal legacy-100 values on the same grid.
  - Test 3 (zero pin): spreadCurveFor(0) toBe 0; out-of-range inputs clamp (−5 → 0, 150 → 1) since the engine setter already clamps 0–100 (D-11).
  - Test 4 (monotone pin): sweep s = 0…100 step 0.5, values non-decreasing.
  - Control expectation: at base, tests 2–4 pass (the legacy law already satisfies them); test 1 fails — that failure IS the RED evidence.
  </behavior>
  <action>TDD RED step. First create the seam with a behavior-preserving extraction: new module packages/efx-physic-paint/src/core/spreadScale.ts exporting the pure function spreadCurveFor(strength: number): number that clamps strength to [0, 100] and implements TODAY's law — (strength/100) squared — unchanged. Then replace BOTH inline spread computations in EfxPaintEngine.ts (the finalize local-continuation site ~2259 and the local-step site ~2534) with a call to spreadCurveFor(this.state.localSpreadStrength); the margin and ticks arithmetic stays at the call sites exactly as-is, and no other line of the engine changes. Prove the extraction is behavior-identical by running the full package suite and observing results identical to base (the engine setter already guarantees state ∈ [0,100], so the clamp is unreachable-in-production hardening only). Then write packages/efx-physic-paint/src/core/spreadScale.test.ts with the four pin groups from the behavior block, plus a short header comment stating the law source: user decision 2026-09-25 — new 50 = old 30 (0.09), new 100 = old 100 (1.0), 0 = 0, monotone; legacy reference formulas written inline in the test so the pins are self-contained. Run the test: the calibration pin must be RED (0.25 vs 0.09) with the range/zero/monotone controls green. Record the RED output (test name + expected/actual) for the SUMMARY. Commit as two commits: the extraction (refactor: extract spreadCurveFor seam, behavior-identical) then the RED suite (test: RED — spread scale calibration pins). Do NOT change the curve law in this task.
  </action>
  <verify>
  <automated>cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/spreadScale.test.ts; ../../app/node_modules/.bin/vitest run</automated>
  </verify>
  <done>spreadScale.test.ts exists and runs: the 50→0.09 calibration pin fails (actual 0.25), range/zero/monotone controls pass; the full package suite shows results identical to base (sole failure remains the pre-existing EfxPaintEngine.liveAlphaCache); EfxPaintEngine's two sites call the helper; git diff shows no other engine lines; RED evidence recorded.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — flip the curve to the calibration law</name>
  <files>packages/efx-physic-paint/src/core/spreadScale.ts</files>
  <behavior>
  - spreadCurveFor(50) === (30/100)² (bitwise 0.09) → calibration pin GREEN; derived ticks/margin equal legacy-30 on the grid.
  - spreadCurveFor(100) === 1.0, spreadCurveFor(0) === 0 → range/zero pins GREEN.
  - Monotone sweep GREEN.
  - Full package suite back to base results (only pre-existing liveAlphaCache failure).
  </behavior>
  <action>GREEN step — the ONLY production edit is the formula inside spreadCurveFor. After the clamp, implement the piecewise-linear remap in curve space: for s ≤ 50 the curve rises linearly from 0 to 0.09 (0.09·(s/50)), for s > 50 it rises linearly from 0.09 to 1 (0.09 + 0.91·((s−50)/50)). These expressions were verified during planning to hit the three pinned points bitwise (0.09 at 50 equals legacy (30/100)² exactly; 0.09+0.91 equals 1.0 exactly) and to be monotone on a 0.01-step sweep — keep them literally as specified; do not "improve" them. Per the user decision (2026-09-25) these three points are the spec: new 50 = today's 30, new 100 = today's max, 0 = no spread, default stays 50, slider keeps 0–100 and the "Spread" label. Touch nothing else: no engine changes (already wired), no fluids.ts, no wet-layer.ts, no opacity path, no DEPOSIT_KEEP_TIER, no UI, no migration code — saved Spread values are simply reinterpreted on the new scale (project clean-break law). Run the new suite (must be fully green), then the full package suite (must match base) and tsc. Commit: feat(260925-b7c): recalibrate spread scale — midpoint is preview-matched width.
  </action>
  <verify>
  <automated>cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/spreadScale.test.ts && ../../app/node_modules/.bin/vitest run && npm run check</automated>
  </verify>
  <done>spreadScale.test.ts all green (calibration/range/zero/monotone); full package suite identical to base (sole failure = pre-existing liveAlphaCache); tsc clean; git diff limited to spreadScale.ts formula for this task.</done>
</task>

<task type="auto">
  <name>Task 3: Harness parity, reconciliation, guardrails, SUMMARY</name>
  <files>packages/efx-physic-paint/src/core/physicsWidthScaling.test.ts, packages/efx-physic-paint/src/core/physicsSettledFootprint.test.ts, packages/efx-physic-paint/src/core/productionAaSettleMeasurement.test.ts, .planning/quick/260925-b7c-quick-8d-recalibrate-the-spread-scale-so/260925-b7c-SUMMARY.md</files>
  <action>Restore the "Engine-mirrored defaults" contract: in each of the three harnesses replace the inline (SPREAD_STRENGTH / 100) ** 2 expressions (the K_TICKS constant and the spreadCurve inside engineLocalBbox's margin) with spreadCurveFor imported from ./spreadScale, and update the stale comments that state "K_TICKS = 3" / "3 ticks at localSpreadStrength 50" to reflect the helper (at SPREAD_STRENGTH 50 the curve is 0.09 and ticks = 1). Do not change SPREAD_STRENGTH (stays 50 — the default) and do not change any law gate. Then run the three harnesses and apply this decision tree: (a) everything green → proceed; (b) a calibrated numeric expectation moved (base body means, width bands, hairline excess band, envelope constants) ONLY because the default's physical effect intentionally changed from old-50 to old-30 physics → recalibrate that expectation to the newly measured value, keeping its law intent intact, and record before/after numbers in the SUMMARY; (c) a LAW gate fails at the new default — PIN 0 body ratio 1.0000, PIN 0b opacity floors, determinism (byte-identical held poses), monotone thin≤thick, texture presence (settled mark still texturizes vs deposit), hairline ceiling (hairline excess ≤ 3.0) — or holding it would require editing the 260924-stb thickness field, depositAlpha/strokeOpacity/body coverage, DEPOSIT_KEEP_TIER, the pinned curve points, or any UI → STOP immediately, commit nothing further, and report the failing gate with measured numbers for a user decision (the quick brief's stop clause). Guardrails, verified by diff scope: `git diff --name-only` must list ONLY the expected files (the three harnesses, spreadScale.ts/test.ts, EfxPaintEngine.ts, SUMMARY) and must be EMPTY for packages/efx-physic-paint/src/core/fluids.ts, packages/efx-physic-paint/src/core/wet-layer.ts, packages/efx-physic-paint/src/brush/stroke.ts, packages/efx-physic-paint/src/render/canvas.ts, packages/efx-physic-paint/src/render/compositor.ts, packages/efx-physic-paint/src/render/canvas.strokePreviewRibbon.test.ts, app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx, app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts, any package.json, and pnpm-lock.yaml. Run the full battery: three harnesses + package-wide vitest (only the pre-existing EfxPaintEngine.liveAlphaCache may fail), the app suite (cd /Users/lmarques/Dev/efx-motion-editor/app && ./node_modules/.bin/vitest run — must match its base), and tsc. Finally write 260925-b7c-SUMMARY.md with status automated-ready and the 7 native UAT rows from the brief: (1) fresh stroke at default 50 settles at preview-ribbon width with pressure gesture intact; (2) slider 100 still reaches the previous maximum bleed (visibly wetter/wider than default); (3) slider 0 = tight mark; (4) pressure signature at default: hairlines clean, taper visible (stb base no regression); (5) stroke body as opaque as before (no m7w wash-out); (6) monotone: raise Spread → wider, lower → tighter; (7) Normal mode unchanged. Leave the SUMMARY uncommitted (orchestrator owns the docs commit) unless the project's quick convention commits it with the last code commit — follow the 260924-stb precedent in this directory.
  </action>
  <verify>
  <automated>cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/physicsWidthScaling.test.ts src/core/physicsSettledFootprint.test.ts src/core/productionAaSettleMeasurement.test.ts src/core/spreadScale.test.ts; ../../app/node_modules/.bin/vitest run; git diff --name-only -- packages/efx-physic-paint/src/core/fluids.ts packages/efx-physic-paint/src/core/wet-layer.ts packages/efx-physic-paint/src/brush/stroke.ts packages/efx-physic-paint/src/render/canvas.ts packages/efx-physic-paint/src/render/compositor.ts packages/efx-physic-paint/src/render/canvas.strokePreviewRibbon.test.ts app/src/components/physic-paint/view/PhysicsPaintRightPanel.tsx app/src/components/physic-paint/engine/physicsPaintStudioSettings.ts</automated>
  </verify>
  <done>All four suites green (law gates unweakened; any recalibrated expectation documented before/after in the SUMMARY); package-wide vitest shows only the pre-existing liveAlphaCache failure; locked-surface diff list empty; UI untouched (slider 0–100, label Spread, default 50); SUMMARY exists with status automated-ready and the 7 native UAT rows; no STOP clause triggered — or, if triggered, the plan halted with a gate-by-gate report and no further commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local pure-parameter remap inside the paint engine; no new external input, no parsing, no network, no auth, no file format change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-b7c-01 | Tampering | stored Spread values in projects/roto scripts | low | accept | Clean-break law: values are reinterpreted on the new scale by design; no migration path exists to corrupt (user decision, project law) |
| T-b7c-02 | Repudiation | silent physics weakening via test-gate edits | medium | mitigate | Task 3 decision tree: law gates (PIN 0/0b, determinism, monotone, texture, hairline ceiling) may never be weakened — failure under (c) STOPs with measured report instead of editing gates |
| T-b7c-03 | Elevation of Privilege | package installs during execution | high | mitigate | No installs in scope; Task 3 diff guardrail proves no package.json / pnpm-lock.yaml changes (T-b7c-SC) |
| T-b7c-04 | Information Disclosure | none — local vitest runs, no network, no auth, no user data | low | accept | External I/O out of scope |
</threat_model>

<verification>
- RED gate: `cd /Users/lmarques/Dev/efx-motion-editor/packages/efx-physic-paint && ../../app/node_modules/.bin/vitest run src/core/spreadScale.test.ts` exits 1 at Task 1 with the 50→0.09 calibration pin failing (actual 0.25) and range/zero/monotone controls passing; production diff at that point limited to the behavior-identical extraction.
- GREEN gate: same command exits 0 after Task 2; full package vitest matches base (sole failure = pre-existing EfxPaintEngine.liveAlphaCache); `npm run check` (tsc) clean.
- Parity gate: the three engine-mirrored harnesses import spreadCurveFor (no inline (s/100)² copies remain — `grep -rn "(SPREAD_STRENGTH / 100) \*\* 2" src/core/` returns nothing), all law gates green or the plan STOPped with a report.
- Scope gate: locked-surface `git diff --name-only` list empty (fluids.ts, wet-layer.ts, stroke.ts, canvas.ts, compositor.ts, preview-ribbon test, RightPanel.tsx, physicsPaintStudioSettings.ts, package.json, pnpm-lock.yaml); app suite at base results; no UI, no migration, no new knob.
</verification>

<success_criteria>
All spreadScale pins green (50 = old-30 bitwise, 100 = old-100 bitwise, 0 = 0, monotone); one shared law used by both engine sites and all three harnesses; full package + app suites at base results with tsc clean; locked surfaces untouched; SUMMARY status automated-ready with the 7 native UAT rows pending the user's live check (no done claim before native UAT).
</success_criteria>

<output>
Create `.planning/quick/260925-b7c-quick-8d-recalibrate-the-spread-scale-so/260925-b7c-SUMMARY.md` when done (uncommitted per quick convention; orchestrator owns the docs commit)
</output>
