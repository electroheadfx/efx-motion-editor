---
phase: quick
plan: 260324-k49
type: execute
wave: 1
depends_on: []
files_modified:
  - Application/src/components/layout/TimelinePanel.tsx
  - Application/src/components/timeline/AddFxMenu.tsx
autonomous: true
must_haves:
  truths:
    - "Shader button no longer appears as standalone button in timeline header bar"
    - "Layer dropdown menu contains a Shader option with the Sparkles icon"
    - "Clicking Shader in the Layer dropdown opens/toggles the shader-browser mode (same behavior as before)"
  artifacts:
    - path: "Application/src/components/timeline/AddFxMenu.tsx"
      provides: "Shader menu item in Layer dropdown"
      contains: "shader-browser"
    - path: "Application/src/components/layout/TimelinePanel.tsx"
      provides: "Timeline header without standalone Shader button"
  key_links:
    - from: "Application/src/components/timeline/AddFxMenu.tsx"
      to: "uiStore.setEditorMode('shader-browser')"
      via: "onClick handler in Shader menu item"
      pattern: "shader-browser"
---

<objective>
Move the Shader button from the timeline header toolbar into the Layer dropdown menu.

Purpose: Declutter the timeline header by consolidating the Shader action into the existing Layer dropdown menu, keeping the Sparkles icon for discoverability.
Output: Modified TimelinePanel.tsx (button removed) and AddFxMenu.tsx (Shader option added)
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Application/src/components/layout/TimelinePanel.tsx
@Application/src/components/timeline/AddFxMenu.tsx
</context>

<interfaces>
<!-- Key imports/APIs the executor needs -->

From TimelinePanel.tsx (lines 221-235) — the Shader button to REMOVE:
```tsx
{/* Shader Browser */}
<button
  class="rounded px-2 py-[5px] bg-(--color-bg-input) hover:bg-(--color-border-subtle) transition-colors"
  onClick={() => {
    if (uiStore.editorMode.peek() === 'shader-browser') {
      uiStore.setEditorMode('editor');
    } else {
      capturePreviewCanvas();
      uiStore.setEditorMode('shader-browser');
    }
  }}
  title="Shader Browser"
>
  <span class="text-[10px] text-(--color-text-secondary) flex items-center gap-1"><Sparkles size={11} /> Shader</span>
</button>
```

From AddFxMenu.tsx — the Layer dropdown (AddLayerMenu component):
- Uses `uiStore` already imported
- Has sections: CONTENT, GENERATORS, ADJUSTMENTS
- Menu items follow pattern: button with colored dot + label, onClick calls handler then setMenuOpen(false)
- Also imports from `../../lib/shaderPreviewCapture` will be needed for capturePreviewCanvas
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move Shader button from TimelinePanel into Layer dropdown menu</name>
  <files>Application/src/components/layout/TimelinePanel.tsx, Application/src/components/timeline/AddFxMenu.tsx</files>
  <action>
In TimelinePanel.tsx:
1. Remove the entire Shader Browser button block (lines 221-235), including the comment `{/* Shader Browser */}`
2. Remove the `Sparkles` import from the lucide-preact import line (keep all other icons)
3. Remove the `capturePreviewCanvas` import from `../../lib/shaderPreviewCapture` (line 3) since it will no longer be used here

In AddFxMenu.tsx:
1. Add imports at top:
   - `import {Sparkles} from 'lucide-preact';`
   - `import {capturePreviewCanvas} from '../../lib/shaderPreviewCapture';`
2. Add a `handleShaderBrowser` function alongside the existing handlers:
   ```tsx
   const handleShaderBrowser = () => {
     setMenuOpen(false);
     if (uiStore.editorMode.peek() === 'shader-browser') {
       uiStore.setEditorMode('editor');
     } else {
       capturePreviewCanvas();
       uiStore.setEditorMode('shader-browser');
     }
   };
   ```
3. Add a new "SHADER" section at the bottom of the dropdown menu (after the ADJUSTMENTS section), with a divider before it:
   ```tsx
   <div class="border-t border-(--color-border-subtle) my-1" />
   <button
     class="w-full text-left px-3 py-1.5 text-xs text-(--color-text-button) hover:bg-(--color-hover-overlay) flex items-center gap-2"
     onClick={handleShaderBrowser}
   >
     <Sparkles size={11} class="shrink-0 text-(--color-text-secondary)" />
     Shader Browser
   </button>
   ```
   Note: Use the Sparkles icon directly instead of a colored dot span, to keep the shader icon as requested. Size 11 matches the original.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Shader button is removed from the timeline header toolbar in TimelinePanel.tsx
    - Layer dropdown (AddFxMenu.tsx) contains a "Shader Browser" menu item with Sparkles icon
    - Clicking it toggles shader-browser mode (same toggle logic as before)
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors)
2. Visual check: timeline header no longer has standalone Shader button
3. Visual check: Layer dropdown has Shader Browser option at bottom with sparkles icon
4. Functional check: clicking Shader Browser in dropdown opens shader browser view
</verification>

<success_criteria>
- Shader button removed from timeline header bar
- Shader Browser option present in Layer dropdown with Sparkles icon
- Toggle behavior preserved (click toggles between editor/shader-browser modes)
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/quick/260324-k49-move-the-shader-button-from-timeline-hea/260324-k49-SUMMARY.md`
</output>
