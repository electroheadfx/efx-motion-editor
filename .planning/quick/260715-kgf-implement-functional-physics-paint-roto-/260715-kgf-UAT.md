---
status: diagnosed
phase: quick-260715-kgf
source:
  - 260715-kgf-SUMMARY.md
started: 2026-07-15T18:39:15Z
updated: 2026-07-15T18:39:15Z
---

# Quick 260715-kgf Native UAT

## Current Test

[testing paused — repair required before continuing]

## Tests

### 1. Apply Script on the current empty/free frame
expected: After copying a non-empty script, navigating to the currently selected empty/free Roto frame keeps Apply Script enabled and applies the copied brushes to that selected frame without requiring existing paint.
result: issue
reported: "copy script work, I can apply but only on the current frame without delete current paint, so it apply only on the same paint at top of it. When I Move on any empty/free frame the apply script button is disabled."
severity: major

### 2. Copied script remains reusable across navigation
expected: Once copied, the script remains available for repeated application across eligible frames until the bound source becomes empty or the Studio session is disposed; ordinary frame navigation must not reset it.
result: issue
reported: "the copied script seem not always memorized, sometimes it reseted or maybe it need a special frame read above."
severity: major

### 3. Apply targets the selected frame without Insert-style key creation
expected: Applying to the currently selected empty/free frame paints that frame directly and must not create a separate Insert-style timeline key or shift/create another frame position.
result: issue
reported: "apply script create a new frame key (like inserted) but it should just apply on the current frame top of empty or top of already painted without create new frame key."
severity: major

### 4. Existing painted real-key Apply remains additive
expected: Applying to an existing painted real key keeps the existing cached base and live overlay and adds the copied script on top without clearing prior paint.
result: pass

### 5. Remaining native UAT A-M
expected: Resume the complete Copy Script / Apply Script UAT matrix after the three blocking behaviors above are repaired.
result: blocked
blocked_by: prior-phase
reason: "Repair the reported Copy Script / Apply Script target and clipboard lifecycle failures first."

## Summary

total: 5
passed: 1
issues: 3
pending: 0
skipped: 0
blocked: 1

## Gaps

- gap_id: G-260715-kgf-1
  truth: "After copying a non-empty script, Apply Script remains enabled on the currently selected empty/free Roto frame and applies the copied brushes there without requiring existing paint."
  status: failed
  reason: "User reported: Apply Script is disabled after moving to an empty/free frame and currently works only on the painted source/current real key."
  severity: major
  test: 1
  root_cause: "Apply eligibility already allows empty selections, but ordinary frame navigation is followed by a same-window launch-context echo. usePhysicsPaintLaunchIntegration treats every incoming context as a destructive launch replacement, and completeLaunchReplacement() clears clipboard.value. Apply then reports the missing-clipboard disabled reason on the empty target."
  artifacts:
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts"
      issue: "Every frame-sync/same-session context calls completeScriptLaunchReplacement without classifying transient navigation from actual Studio disposal/session replacement."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts"
      issue: "completeLaunchReplacement clears the session clipboard and source binding even though the Studio remains mounted."
  missing:
    - "Preserve clipboard and bound-source provenance across ordinary navigation and same-mounted-session launch echoes."
    - "Keep Apply enabled on true empty targets when a copied script exists; generated targets remain disabled."
    - "Add mounted Copy -> navigate empty -> Apply availability coverage."
  debug_session: "diagnosis-2026-07-15-empty-apply"

- gap_id: G-260715-kgf-2
  truth: "The copied script remains reusable across ordinary frame navigation until the bound source is cleared to empty or the Studio session is disposed."
  status: failed
  reason: "User reported: the copied script sometimes appears reset or unavailable after navigation."
  severity: major
  test: 2
  root_cause: "The copied script is session-owned in the controller, but completeLaunchReplacement unconditionally clears it on every parent/Tauri launch context, including frame-sync echoes, same-window refocus, and the delayed stored-context fetch. launchGeneration also invalidates an in-flight Copy on those equivalent updates, which makes the reset appear intermittent."
  artifacts:
    - path: "app/src/components/physic-paint/bridge/usePhysicsPaintParentBridge.ts"
      issue: "Stored and emitted contexts share the same replacement callback, including equivalent contexts for the still-mounted Studio."
    - path: "app/src/components/physic-paint/hooks/usePhysicsPaintLaunchIntegration.ts"
      issue: "Incoming contexts are not classified by stable session/layer identity before destructive script replacement lifecycle runs."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts"
      issue: "Clipboard lifetime is coupled to launch replacement rather than actual Studio disposal or confirmed bound-source empty state."
  missing:
    - "Classify same-session navigation/refocus echoes separately from actual layer/session replacement."
    - "Preserve immutable clipboard across ordinary navigation; returning to the bound source resumes refresh."
    - "Clear only when the bound source becomes empty or Studio is disposed, matching the locked session lifetime."
    - "Add stable source provenance needed to prevent another layer with the same frame number from refreshing or clearing the clipboard."
  debug_session: "diagnosis-2026-07-15-clipboard-lifetime"

- gap_id: G-260715-kgf-3
  truth: "Applying to the selected empty/free frame paints that frame directly and does not create a separate Insert-style timeline key or shift/create another frame position."
  status: failed
  reason: "User reported: Apply Script creates a new frame key like Insert instead of applying to the currently selected frame."
  severity: major
  test: 3
  root_cause: "Empty Apply uses the old prepareEmptyTarget -> saveRealKeyAtDisplayFrame transaction, which upserts the selected display frame into durable real-key/interpolation metadata. It does not call the Insert command or shift existing source keys, but the resulting real-key/projection update looks Insert-like. The corrected product contract is direct selected-cell ownership: paint the current absolute frame, preserve all existing key positions, and publish that selected frame only when the first brush is accepted."
  artifacts:
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      issue: "prepareEmptyTarget is wired to the generic save-real-key transaction and completion publishes its target/interpolation model."
    - path: "app/src/components/physic-paint/roto/physicsPaintRotoScriptClipboard.ts"
      issue: "Empty Apply requires a prepared real-key transaction before enqueueing instead of a direct selected-frame claim contract."
    - path: "app/src/components/physic-paint/roto/rotoKeyTransactions.ts"
      issue: "saveRotoRealKeyTransaction upserts durable key/interpolation metadata; it is semantically broader than direct selected-cell paint ownership."
    - path: "app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts"
      issue: "beginFrameEdit already marks the selected non-generated absolute frame dirty and is the correct first-accepted live-overlay ownership seam."
  missing:
    - "Replace the empty Apply target with an explicit direct selected-frame claim carrying sourceFrame === displayFrame === selected absolute frame."
    - "Do not invoke Insert or move/duplicate existing real-key positions; assert no frameMappings and no neighboring key movement."
    - "Publish durable paint/key occupancy only after the first brush is accepted; zero acceptance leaves the cell empty."
    - "Preserve Phase 36.13 distant/custom spacing, generated ownership, cache, reopen, preview, and export identity at the selected frame."
  debug_session: "diagnosis-2026-07-15-selected-frame-claim"
