---
phase: quick-260714-9es
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/lib/physicPaintRotoDurableCore.test.ts
  - app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts
  - app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
  - app/src/components/physic-paint/PhysicsPaintStudio.tsx
autonomous: true
requirements:
  - QUICK-260714-9ES
must_haves:
  truths:
    - "Clicking the existing Clear current Roto frame tool-rail action immediately empties the current editable real key without confirmation while preserving that key at the same selected frame."
    - "The cleared key loses all stale live paint, repaint/reference pixels, edit-buffer/captured/preview state, launch-context cache, confirmed cache, and durable cached pixels; generated interpolation derived from the old pixels is refreshed without changing interpolation or segment-spacing settings."
    - "Navigation, save-on-leave, close/reopen, preview, and export cannot restore the cleared paint, and a later stroke saves as the fresh replacement for the same real key."
    - "Other real keys, onion settings, interpolation settings, spacing overrides, and frame positions remain unchanged; Clear never performs Delete semantics and generated-only selections remain render-only."
    - "The existing status surface reports `Cleared roto frame {frame}.` and the canvas retains only paper/background plus valid onion overlays."
  artifacts:
    - path: "app/src/lib/physicPaintRotoDurableCore.test.ts"
      provides: "Mounted/native regression covering the tool-rail Clear click through durable reopen and replacement save"
      contains: "Clear current Roto frame"
    - path: "app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts"
      provides: "Real-key-only clear transaction entry point within the existing Roto editing controller"
      contains: "clearCurrentFrame"
    - path: "app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts"
      provides: "Atomic launch-context, confirmed-cache, and durable-store replacement of stale paint with a canonical blank real key"
      contains: "confirmedFramesRef"
    - path: "app/src/components/physic-paint/PhysicsPaintStudio.tsx"
      provides: "Existing tool-rail-to-controller wiring for the complete clear transaction"
      contains: "onClearFrame: clearActiveSource"
  key_links:
    - from: "PhysicsPaintToolRail clear-frame click"
      to: "useRotoFrameEditingController.clearCurrentFrame"
      via: "PhysicsPaintStudio clearActiveSource"
      pattern: "onClearFrame: clearActiveSource"
    - from: "clearCurrentFrame"
      to: "Roto edit buffer, reference controller, launch context, confirmed cache, and physicPaintStore"
      via: "one controller-owned clear callback supplied by useRotoFramePersistenceCoordinator"
      pattern: "clearCurrentFrame"
    - from: "blank real-key cache replacement"
      to: "generated interpolation cache"
      via: "existing store real-key upsert/regeneration path while preserving interpolation settings and segmentSpacingOverrides"
      pattern: "upsertRealKey"
---

<objective>
Fix QUICK-260714-9ES so the existing Physics Paint tool-rail Clear action is a complete, immediate Roto real-key content reset rather than a live-canvas-only clear.

Purpose: Prevent cached Roto paint from returning after navigation or persistence boundaries while preserving key topology, dynamic spacing, and the distinction between clearing a key's content and deleting the key itself.
Output: A mounted/native red-green regression and a minimal controller-bound transaction that clears every current-frame paint/cache representation and supports fresh replacement painting.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/.planning/STATE.md
@/Users/lmarques/Dev/efx-motion-editor/CLAUDE.md
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/view/PhysicsPaintToolRail.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/PhysicsPaintStudio.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/hooks/useRotoEditBufferController.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/stores/physicPaintStore.ts
@/Users/lmarques/Dev/efx-motion-editor/app/src/lib/physicPaintRotoDurableCore.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — reproduce cached real-key Clear through the mounted tool rail</name>
  <files>app/src/lib/physicPaintRotoDurableCore.test.ts</files>
  <behavior>
    - Test 1: Mount PhysicsPaintStudio on a cached editable real key with at least one unrelated real key, enabled interpolation, a custom segmentSpacingOverrides entry, and stale live/edit-buffer/reference/cache state; click the accessible `Clear current Roto frame` tool-rail button and observe no confirmation flow.
    - Test 2: The click clears engine strokes and preview-base/reference pixels, current-frame editable state, captured/preview data, dirty/live-overlay residue, launch-context cached paint, confirmed cache, and the durable store's old rendered pixels while retaining a blank real key at the same source/display frame.
    - Test 3: Selection stays on the same real key, key count/order and unrelated key pixels remain unchanged, dynamic interpolation settings/spacing overrides remain byte-for-byte equivalent, and regenerated frames no longer contain paint derived from the cleared source.
    - Test 4: Navigate away and back, then persist/hydrate and reopen; the old data URL never becomes a preview base, playback/export/store frame, or save-on-leave payload.
    - Test 5: Simulate a new stroke on the still-selected key, save current, and prove the new data URL becomes that key's sole real-key cache while unrelated keys and spacing remain unchanged.
    - Test 6: The visible existing-surface feedback is exactly `Cleared roto frame {frame}.`; the Clear path emits no delete-roto-frame/replace-key-topology transaction, while generated-only frame selection remains render-only and cannot be promoted by Clear.
  </behavior>
  <action>Extend the existing mounted/native durable Roto harness rather than adding source-string-only coverage. Drive the real public UI path `PhysicsPaintToolRail -> onClearFrame -> clearActiveSource -> rotoFrameEditing.clearCurrentFrame`, seed distinct old/other/new PNG literals, and inspect the mounted engine plus public launch/store/persistence outputs. Keep Clear and Delete assertions side by side so a topology-removing implementation cannot satisfy the test. Use tracer-bullet sequencing: first add the cached-real-key click and immediate cache assertions, run it and record the expected failure; then add navigation/reopen, fresh replacement save, spacing preservation, and generated-only guard assertions one behavior at a time, rerunning after each addition. Do not change production code while this task is RED, and do not create a new Vitest configuration.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/lib/physicPaintRotoDurableCore.test.ts -t "Clear current Roto frame"</automated>
  </verify>
  <done>The mounted test fails for the current bug because stale cached paint remains reachable, while proving the failure originates from the existing tool-rail click path rather than a direct private-controller call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — clear the complete real-key content transaction without deleting the key</name>
  <files>app/src/components/physic-paint/hooks/useRotoFrameEditingController.ts, app/src/components/physic-paint/hooks/useRotoFramePersistenceCoordinator.ts, app/src/components/physic-paint/PhysicsPaintStudio.tsx, app/src/lib/physicPaintRotoDurableCore.test.ts</files>
  <behavior>
    - Test 1: Clear applies only when workflow mode is Roto and the selected timeline item is an editable real key; generated/empty selections retain their existing render-only ownership/navigation contract.
    - Test 2: One synchronous controller transaction clears engine live strokes, engine preview base, cached reference URL/repaint base, edit-buffer frame state/preview/capture/dirty overlay state, confirmed cache, launch-context old pixels, and durable old pixels.
    - Test 3: The transaction preserves the selected frame as a real key by replacing its content with the canonical blank/background-only representation through the existing cache/store path, never invoking key deletion, ripple, source/display remapping, or spacing mutation.
    - Test 4: Existing interpolation regeneration removes frames derived from old paint while preserving enabled/count/mode/deform/position/segmentSpacingOverrides and all unrelated real keys.
    - Test 5: Subsequent painting marks the same key dirty and Save current replaces its blank cache with only the new stroke output.
  </behavior>
  <action>Make the smallest change inside the existing Roto controller/transaction boundaries. Remove the cached-repaint special case that preserves the old base. Have `clearCurrentFrame` reject non-real-key selections before mutation, stop playback, clear the engine and its preview base/reference, clear the current edit-buffer frame (including frame state, preview/captured frame, overlay count, and dirty residue), mark the live overlay empty, and delegate cache ownership to a single persistence-coordinator operation. That operation must remove the current frame from confirmed cache and stale launch-context entries, then publish a canonical blank real-key frame through the existing `store.upsertRealKey` path so the key remains selected/editable and existing generated-cache regeneration runs without changing interpolation settings or `segmentSpacingOverrides`. Wire this operation into `PhysicsPaintStudio` through the existing `rotoPersistence`/`rotoFrameEditing` ports; do not move ownership into PhysicsPaintStudio, add effect-driven synchronization, create another cache model, add confirmation/UI, or reuse Delete/key-utility transactions. Preserve paper/background and onion composition, set the existing success status to exactly `Cleared roto frame ${currentFrame}.`, and ensure a later input intent/save treats new paint as a fresh replacement. Implement only enough to turn each Task 1 assertion green, then refactor duplicated cache cleanup behind the coordinator seam while tests remain green.</action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/lib/physicPaintRotoDurableCore.test.ts -t "Clear current Roto frame"</automated>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/lib/physicPaintRotoDurableCore.test.ts src/components/physic-paint/PhysicsPaintStudio.test.ts</automated>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm typecheck</automated>
  </verify>
  <done>The mounted tool-rail Clear click empties every current-key paint/cache representation immediately, survives navigation and durable reopen without stale resurrection, preserves key topology and dynamic spacing, and allows a new stroke to save as the replacement cache for that same frame.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool-rail user action -> Roto controller | A destructive content-reset request enters through the existing accessible button and must be restricted to the editable real-key target. |
| In-memory edit/reference caches -> launch context and durable store | Multiple representations of the same rendered Roto pixels must be invalidated atomically enough that an older representation cannot repopulate another. |
| Real-key cache -> generated interpolation/preview/export | Derived frames must refresh from the cleared canonical source without altering unrelated timing or key topology. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260714-9ES-01 | Tampering | `clearCurrentFrame` target selection | high | mitigate | Gate the transaction on Roto `real-key` selection and keep generated/empty frames render-only. |
| T-260714-9ES-02 | Tampering | edit buffer / launch context / confirmed cache / store | high | mitigate | Clear all current-frame representations through one controller-owned persistence operation and test navigation plus reopen against the old data URL. |
| T-260714-9ES-03 | Denial of Service | interpolation/key topology | medium | mitigate | Preserve real-key identity and settings, use existing upsert regeneration, and assert unrelated keys plus segment spacing are unchanged. |
| T-260714-9ES-04 | Repudiation | user-visible destructive action result | low | mitigate | Reuse the existing status surface with exact frame-specific clear feedback and no confirmation or hidden delete operation. |
| T-260714-9ES-SC | Tampering | package installs | low | accept | No dependency installation is planned. |
</threat_model>

<source_coverage_audit>
- GOAL: Fix the existing Physics Paint Roto Clear tool-rail action completely — covered by Tasks 1-2.
- REQ QUICK-260714-9ES: Immediate no-confirmation clear; all live/local/launch/durable/derived caches; same-key selection; no ripple; unrelated state preservation; stale-resurrection prevention; fresh replacement save; generated-only guard; exact feedback; no UI redesign — covered explicitly by Task 1 behaviors and Task 2 transaction instructions.
- RESEARCH: No research phase requested; implementation follows the traced existing controller, persistence coordinator, edit buffer, and store regeneration seams.
- CONTEXT: All DATA_START required behavior and TDD points 1-7 are covered; no deferred ideas are introduced.
</source_coverage_audit>

<verification>
Follow strict RED -> GREEN -> REFACTOR sequencing. The first focused mounted/native command must fail before production edits and pass after implementation. Run only `vitest run` invocations, then the focused companion Studio suite and TypeScript check. Do not run the development server. Automated completion is ready for user UAT, not final visible acceptance.
</verification>

<success_criteria>
- The existing tool-rail button applies Clear immediately on a cached editable real key and reports the exact existing-surface message.
- No old current-frame paint survives in engine, reference/base, edit buffer, captured/preview data, launch context, confirmed cache, durable store, generated interpolation, persistence, playback, preview, or export paths.
- The selected real key remains at the same frame and can receive/save fresh paint; Clear does not behave like Delete.
- Other keys, source/display positions, segment spacing overrides, interpolation controls, onion settings, and unrelated frames are unchanged.
- Generated-only and empty selections remain non-editable under the existing ownership/navigation contract.
- Focused mounted/native regressions, companion Studio tests, and typecheck pass.
</success_criteria>

<output>
Create `/Users/lmarques/Dev/efx-motion-editor/.planning/quick/260714-9es-fix-the-clear-current-roto-frame-button-/260714-9es-SUMMARY.md` when done.
</output>
