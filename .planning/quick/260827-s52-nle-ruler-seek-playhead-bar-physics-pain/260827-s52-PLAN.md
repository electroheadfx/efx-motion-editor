---
phase: quick-260827-s52
plan: 260827-s52
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.ts
  - app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.test.ts
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
  - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.rulerSeek.test.ts
  - app/src/components/physic-paint/physicsPaintStudio.css
autonomous: true
requirements: []

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Clicking the time ruler at frame N moves the cursor to frame N (canvas shows frame N) with zero change to frame/key/rail selection and zero active-track change.
    - Dragging the ruler scrubs the cursor continuously, coalesced to at most one navigation per animation frame (rAF), never one per pointer event.
    - A plain sub-4px click on the ruler still seeks — it is never swallowed by the drag threshold machinery.
    - Cell clicks keep the existing select+navigate behavior (PhysicsPaintTrackRow.handleRowClick and the active-lane handlers are untouched).
    - A full-height accent playhead line overlays ruler + all rows + Bg row at the current frame, follows seeks/cell navigation/playback ticks/undo-redo restores, and never intercepts pointer events.
    - cursorAppFrame (props.currentFrame, fed by launchContext.startFrame) remains the single position source — no second position state exists.
  artifacts:
    - app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.ts
    - app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.test.ts
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx (ruler onPointerDown + playhead bar element)
    - app/src/components/physic-paint/physicsPaintStudio.css (.physics-paint-playhead-bar + ruler affordance)
    - app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.rulerSeek.test.ts
  key_links:
    - Ruler pointerdown → usePhysicsPaintRulerScrub → props.onNavigateToSyncedFrame (the SAME cursor-only navigation entry the armed-Push click uses at PhysicsPaintWorkflowStrip.tsx:2229-2238) — never any selection prop.
    - Playhead bar reads the existing currentFrameSignal (PhysicsPaintWorkflowStrip.tsx:1170-1171) and props.rotoCachedPlaybackTick only while isPlaying, mirroring RotoPlaybackCurrentFrameOutput (lines 648-651) so an idle strip holds zero per-tick subscriptions.
    - Studio navigation path (requestRotoFrameNavigation → scheduleRotoStartFramePropagation, PhysicsPaintStudio.tsx:2081-2085) already rAF-batches the render; the hook's own rAF throttle bounds navigation CALLS so flushLivePixels (Studio.tsx:2045) never runs per pointer event.
---

<objective>
Add NLE-style ruler seek + scrub and a full-height playhead bar to the Physics Paint Studio timeline.

Purpose: Today the only way to move the playhead is a frame-cell click, which always selects. Every NLE has ruler-seek: click/drag the time ruler repositions the playhead WITHOUT touching selection. This quick adds exactly that, plus the playhead bar that makes the position visible across every row.

Output: A new `usePhysicsPaintRulerScrub` hook (4px threshold, rAF-throttled scrub) wired onto the `.physics-paint-ruler` div, and a `PhysicsPaintPlayheadBar` leaf overlay inside `.physics-paint-timeline-scroll`.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/quick.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/hooks/usePhysicsPaintKeyRailDrag.ts (session/threshold pattern to mirror)
@/Users/lmarques/Dev/efx-motion-editor/app/src/components/physic-paint/physicsPaintStudio.css (ruler block ~L2973-3006, timeline-scroll ~L2923-2948)
@/Users/lmarques/Dev/efx-motion-editor/.claude/skills/efx-preact-reactivity/SKILL.md

Relevant facts already established (investigated, not guessed):
- The ruler is `<div class="physics-paint-ruler" aria-hidden="true">` at PhysicsPaintWorkflowStrip.tsx:3642, a flex row of 54px tick spans (`RULER_STEP=3` × `ROTO_CELL_WIDTH_PX=18`, lines 404-410), spanning the full lane width inside `.physics-paint-timeline-scroll` (line 3641). It currently has NO pointer handlers. The push-drag code already documents that a pointerdown on the ruler must never start a push (line 2180-2181 comment) — the ruler sits OUTSIDE the rows-region, so the 47-05 crossTrackDrag capture handler (line 3662) never sees ruler gestures.
- Cursor-only navigation precedent: `handleLanePushClickCapture` (lines 2218-2239) converts clientX → frame with `Math.floor((event.clientX - laneElement.getBoundingClientRect().left) / ROTO_CELL_WIDTH_PX)`, clamps `0 <= frame < frameCells.length`, and calls `props.onNavigateToSyncedFrame?.(frame)` — explicitly cursor-only, never selection. The ruler seek reuses this exact math against the ruler element's own rect.
- The 4px drag-threshold session pattern lives in hooks/usePhysicsPaintKeyRailDrag.ts (`KEY_RAIL_DRAG_THRESHOLD_PX = 4`, session object `{originX, latestX, started}`, window-level pointermove/pointerup listeners, `Math.abs(session.latestX - session.originX) > 4` gate at line 237). The new hook mirrors this shape.
- Strip props already carry everything needed: `currentFrame: number` (line 202), `isPlaying: boolean` (line 203), `rotoCachedPlaybackTick?: Signal<RotoCachedPlaybackTick | null>` (line 243), `onNavigateToSyncedFrame: (frame: number) => void` (line 352). NO PhysicsPaintStudio.tsx changes are required.
- `currentFrameSignal` (lines 1170-1171) mirrors props.currentFrame into a signal; `RotoPlaybackCurrentFrameOutput` (lines 648-651) is the established leaf-subscription pattern: read the tick signal ONLY when playback is active, else the currentFrame signal.
- `.physics-paint-timeline-scroll` (CSS L2923) is `display:flex; flex-direction:column; padding-left:4px; overflow-x:auto` and has NO `position: relative` yet — the playhead's absolute positioning needs it, and the 4px left padding must be included in the playhead's left offset (pitfall).
- Undo/redo restores and all other navigation sources already converge on `launchContext.startFrame` → props.currentFrame, so a playhead driven by that prop follows them for free.
- The vitest environment is node + source-inspection contract tests (see PhysicsPaintWorkflowStrip.test.ts: reads the .tsx/.css as text, extracts regions, asserts). Hook logic is directly unit-testable headless (usePhysicsPaintKeyRailDrag.test.ts precedent).
- Tests: `vitest run` only, never watch mode (project rule).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Ruler seek end-to-end — usePhysicsPaintRulerScrub hook + ruler wiring (selection-free)</name>
  <files>app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.ts, app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx</files>
  <behavior>
    - pointerdown on the ruler at x → immediately calls onSeek(frame) exactly once with frame = floor((clientX - rulerRect.left) / 18) clamped to [0, frameCount-1]; takes pointer capture on the ruler element.
    - pointermove with |latestX - originX| > 4 (RULER_SCRUB_THRESHOLD_PX = 4) enters scrub mode; subsequent moves store the latest frame and schedule ONE rAF — the seek fires at most once per animation frame with the LATEST frame, never per pointer event.
    - pointermove under the 4px threshold after the initial down-seek fires nothing extra (a plain click is one seek total — never swallowed, never doubled).
    - pointerup/cancel releases capture, cancels any pending rAF, and removes the window listeners (idempotent cleanup — efx-preact-reactivity loop-termination rule).
    - frame is derived from the ruler element's OWN getBoundingClientRect().left (the ruler is inside the 4px-padded scroll container, same origin as the lane cells).
    - The hook's API takes { onSeek } and never receives, references, or invokes any selection or track-activation callback.
  </behavior>
  <action>
    Create app/src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.ts mirroring the session shape of usePhysicsPaintKeyRailDrag.ts: an exported `RULER_SCRUB_THRESHOLD_PX = 4` constant, a `usePhysicsPaintRulerScrub(options: { frameCount: () => number; onSeek: (frame: number) => void })` hook returning `{ onPointerDown }` where onPointerDown is bound to the ruler div. Down-seek fires synchronously on pointerdown (NLE law: seek on press). Scrub mode arms past the 4px threshold and rAF-throttles: keep `pendingFrame` + `rafId` in the session ref; on rAF fire onSeek(pendingFrame) only if it differs from the last emitted frame. All listeners on window, removed on release and on unmount. Write the failing tests FIRST in usePhysicsPaintRulerScrub.test.ts following usePhysicsPaintKeyRailDrag.test.ts conventions (fake pointer events, vi.stubGlobal for requestAnimationFrame/cancelAnimationFrame, assert call counts and coalescing), then implement to green.

    In PhysicsPaintWorkflowStrip.tsx, instantiate the hook near the other drag hooks (~line 2016 area) with `frameCount: () => frameCells.length` and `onSeek: (frame) => props.onNavigateToSyncedFrame?.(frame)` — the same cursor-only port handleLanePushClickCapture uses (line 2235). Attach `onPointerDown={rulerScrub.onPointerDown}` to the `.physics-paint-ruler` div (line 3642). The ruler becomes interactive: drop `aria-hidden="true"` from the ruler div (keep the tick spans' labels as-is) and add `title="Seek playhead"` plus a `cursor: pointer` rule on `.physics-paint-ruler` in the CSS (Task 2 owns the css edit — add the rule there to keep one-file ownership per task; the tsx handler lands here). Do NOT add role="slider" — without live aria-valuenow it is an ARIA violation, and wiring valuenow would re-render the strip per frame; the nav pill already announces the current frame. Do NOT touch PhysicsPaintTrackRow.tsx, handleRowClick, or any active-lane cell handlers — cell gestures keep select+navigate byte-identically.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/components/physic-paint/hooks/usePhysicsPaintRulerScrub.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    Hook unit tests pass: down-seek fires once with the correct clamped frame; scrub past 4px coalesces to one onSeek per rAF with the latest frame; sub-threshold wiggle emits nothing beyond the down-seek; cleanup removes listeners and cancels rAF. The ruler div in the strip carries the new pointerdown binding, and typecheck passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playhead bar overlay — leaf component + CSS, single position source</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.tsx, app/src/components/physic-paint/physicsPaintStudio.css, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.rulerSeek.test.ts</files>
  <behavior>
    - The playhead is a full-height vertical line (2px, accent color) absolutely positioned inside `.physics-paint-timeline-scroll`, spanning from the ruler top through the Bg row bottom.
    - Its left offset = 4px scroll-padding + (frame × 18px) + 8px (cell center, minus half the line width), where frame = playbackTick appFrame while isPlaying, else currentFrame.
    - During active playback only the playhead leaf re-renders per tick (RotoPlaybackCurrentFrameOutput pattern); idle = zero tick subscriptions.
    - pointer-events: none always — cells, rails, capsules, and the ruler receive every click/drag exactly as before.
    - No new position state: the bar derives from currentFrameSignal + props.rotoCachedPlaybackTick only.
  </behavior>
  <action>
    Add a `PhysicsPaintPlayheadBar` function component in PhysicsPaintWorkflowStrip.tsx (module scope, next to RotoPlaybackCurrentFrameOutput ~line 648) taking `{ currentFrame: Signal<number>; playbackActive: boolean; playbackTick: Signal<RotoCachedPlaybackTick | null> | null | undefined; frameCount: number }`. It resolves `frame = playbackActive ? (playbackTick?.value?.appFrame ?? currentFrame.value) : currentFrame.value`, clamps to [0, frameCount-1], and renders a single div with class `physics-paint-playhead-bar`, `aria-hidden="true"`, and inline style left computed as `4 + frame * 18 + 8` px (4 = timeline-scroll padding-left, 18 = ROTO_CELL_WIDTH_PX, 8 = cell center minus half the 2px line). Render it as the LAST child inside `.physics-paint-timeline-scroll` (after the rows-region div, ~line 3703) so it overlays ruler + all rows; pass currentFrameSignal, props.isPlaying, props.rotoCachedPlaybackTick, frameCells.length.

    In physicsPaintStudio.css: add `position: relative` to `.physics-paint-timeline-scroll` (L2923 block — verify the 4px padding-left and column flex stay unchanged), add `cursor: pointer` to the existing `.physics-paint-ruler` block (L2973 — the ruler-seek affordance owned here per Task 1's note), and add `.physics-paint-playhead-bar { position: absolute; top: 0; bottom: 0; width: 2px; background: #A6D334; pointer-events: none; z-index: 30; }` — #A6D334 is the locked accent (47 close-out synchronized palette); z-index 30 clears the cell/rail layers while staying under tooltips. Because the rows-region scrolls vertically INTERNALLY and timeline-scroll only scrolls horizontally, top:0/bottom:0 spans exactly the visible ruler-through-Bg band, and the bar rides horizontal scroll with the content.

    Add source-inspection contract tests to the new PhysicsPaintWorkflowStrip.rulerSeek.test.ts asserting: the playhead class exists in the tsx and the css with pointer-events none; the playhead block reads the tick signal ONLY inside a playbackActive guard (mirror the line 649 ternary); no `useSignal(`/`signal(` position state is created for the playhead (region-scoped check on the playhead component body).
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run src/components/physic-paint/view/PhysicsPaintWorkflowStrip.rulerSeek.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    The playhead bar renders inside the scroll container, positioned at the current frame with the 4px-padding-inclusive offset, follows playback ticks via the guarded leaf subscription, has pointer-events none in CSS, and the contract tests plus typecheck pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Hard-contract regression gates + full suite</name>
  <files>app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.rulerSeek.test.ts, app/src/components/physic-paint/view/PhysicsPaintWorkflowStrip.test.ts</files>
  <action>
    Extend PhysicsPaintWorkflowStrip.rulerSeek.test.ts with region-scoped negative gates proving the HARD CONTRACT. Extract the ruler block (from `class="physics-paint-ruler"` to the closing of that div, using the getMatchingDivEnd helper pattern from PhysicsPaintWorkflowStrip.test.ts:58) and assert it contains the ruler-scrub pointerdown binding while containing NONE of the strip's selection-prop identifiers (the selection/rail selection callback prop names) — the ruler gesture structurally cannot mutate selection or active track. Add a gate that `usePhysicsPaintRulerScrub.ts` imports no store module and references no selection signal (pure gesture hook). Add a gate that PhysicsPaintTrackRow.tsx's handleRowClick still invokes its select-then-navigate pair (the existing behavior is asserted unchanged — grep the row source for its current handler shape). In PhysicsPaintWorkflowStrip.test.ts add one assertion that `onNavigateToSyncedFrame` is the ONLY navigation port the ruler path uses. Region-scope every negative gate to the ruler/playhead blocks; never write a bare tree-wide `== 0` grep. Ensure no negative-grep literal from these tests appears verbatim in any plan action text.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor/app && pnpm exec vitest run && pnpm exec tsc --noEmit</automated>
    <human-check>
      Native UAT in the running Physics Paint Studio:
      1. Click the ruler at frame N → canvas shows frame N; frame/key/rail selection and the orange selection state are byte-identical to before the click.
      2. Drag the ruler left/right → playhead and canvas follow smoothly (no per-event jank), selection untouched.
      3. Click a frame cell → still selects + navigates exactly as before.
      4. Playhead bar spans ruler through all track rows and the Bg row; follows cell clicks, ruler seeks, playback, and undo/redo restores; clicks pass through it.
      5. Scrub performance: smooth on (a) a Hold Loop Clip, (b) an interpolated segment, (c) a multi-track document.
    </human-check>
  </verify>
  <done>
    Full vitest suite and typecheck pass; the selection-immunity gates are green; native UAT confirms seek/scrub/playhead behavior and smooth scrub on all three document shapes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer→frame math | Untrusted pointer coordinates converted to a frame index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-s52-01 | Tampering | usePhysicsPaintRulerScrub frame mapping | low | mitigate | Frame index clamped to [0, frameCount-1] at the hook boundary before onSeek fires |
| T-s52-02 | DoS | Scrub pointermove storm | medium | mitigate | rAF coalescing in the hook bounds navigation calls to 1/animation-frame; Studio render path already rAF-batched (38.1 D-04) |
| T-s52-03 | Tampering | Playhead overlay intercepting input | low | mitigate | pointer-events: none enforced in CSS and asserted by contract test |
</threat_model>

<verification>
- `pnpm exec vitest run` (full suite) green, `pnpm exec tsc --noEmit` green.
- Ruler gesture proven structurally selection-free (region-scoped contract gates).
- Playhead bar present, correctly offset (4px padding included), pointer-transparent, playback-tick-follows only via guarded leaf subscription.
- No PhysicsPaintStudio.tsx, PhysicsPaintTrackRow.tsx, or store changes; no new dependencies.
</verification>

<success_criteria>
- Ruler click seeks without selection change; ruler drag scrubs rAF-throttled; sub-4px click never swallowed.
- Playhead bar overlays all rows + Bg at the current frame and follows every navigation source.
- Locked Phase 43/47 surfaces (rails, header column, status capsule, cross-track drag) untouched and green under the full suite.
- Native UAT passes all five acceptance rows, including the three scrub-performance shapes.
</success_criteria>

<output>
Create `.planning/quick/260827-s52-nle-ruler-seek-playhead-bar-physics-pain/260827-s52-SUMMARY.md` when done.
</output>
