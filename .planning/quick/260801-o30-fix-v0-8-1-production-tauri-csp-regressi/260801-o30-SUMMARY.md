---
phase: quick-260801-o30
plan: 01
subsystem: release
tags: [tauri, csp, img-src, data-url, macos-packaging, vitest, release-contract]

# Dependency graph
requires:
  - phase: quick-260801-jun
    provides: v0.8.1 macOS packaging hotfix (frontend entry fix, EFX icons, version bump, release contract test file)
provides:
  - Production Tauri CSP img-src grants the data: scheme, unblocking cached Roto PNG and Physics Paint data-URL image decode in the packaged app
  - Token-level CSP contract assertions in app/src/releaseContract.test.ts guarding the img-src source list and locking other directives against data: creep
affects: [roto-png-cache, physics-paint-cache, v0.8.1-release, macos-packaging]

# Actuals
actuals:
  tokens: 800
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CSP directive token extraction: regex (?:^|;)\\s*<name>\\s+([^;]+) plus whitespace token split — token-level matching, never substring"
    - "TDD on release contract surfaces: RED proves the contract guards the regression before the config edit"

key-files:
  created: []
  modified:
    - app/src-tauri/tauri.conf.json
    - app/src/releaseContract.test.ts

key-decisions:
  - "data: scheme granted ONLY in img-src (images cannot execute script); default-src/script-src/style-src/connect-src/media-src contract-locked against ever gaining it"
  - "img-src token order after fix: 'self' asset: http://asset.localhost efxasset: blob: data: https://* — all pre-existing sources preserved"

patterns-established:
  - "CSP contract assertions use token-level containment so e.g. https://* can never be mistaken for a scheme grant"

requirements-completed: [QUICK-260801-O30-CSP-01]

coverage:
  - id: D1
    description: "Production Tauri CSP img-src directive allows the data: scheme with all pre-existing sources preserved"
    requirement: QUICK-260801-O30-CSP-01
    verification:
      - kind: unit
        ref: "app/src/releaseContract.test.ts#Tauri CSP image data-url contract > img-src grants the data: scheme alongside every pre-existing source"
        status: pass
    human_judgment: true
    rationale: "Automated contract proves the CSP shape; real image decode of cached Roto PNG / Physics Paint previews requires the user's native packaged-app UAT"
  - id: D2
    description: "No other CSP directive (default-src, script-src, style-src, connect-src, media-src) gains the data: scheme"
    requirement: QUICK-260801-O30-CSP-01
    verification:
      - kind: unit
        ref: "app/src/releaseContract.test.ts#Tauri CSP image data-url contract > no other CSP directive gains the data: scheme"
        status: pass
    human_judgment: false
  - id: D3
    description: "Canonical Roto PNG header constant ROTO_PNG_DATA_URL_HEADER = 'data:image/png;base64' remains in rotoCanvasFrames.ts"
    requirement: QUICK-260801-O30-CSP-01
    verification:
      - kind: unit
        ref: "app/src/releaseContract.test.ts#Tauri CSP image data-url contract > rotoCanvasFrames.ts declares the canonical ROTO_PNG_DATA_URL_HEADER constant"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every product version surface remains 0.8.1 (package.json, tauri.conf.json, Cargo.toml, Cargo.lock, PRODUCT_VERSION)"
    requirement: QUICK-260801-O30-CSP-01
    verification:
      - kind: unit
        ref: "app/src/releaseContract.test.ts#release contract > all product-owned version surfaces agree with app/package.json"
        status: pass
      - kind: other
        ref: "git diff f354866f..HEAD --stat (only the two owned files changed)"
        status: pass
    human_judgment: false

# Metrics
duration: 6min
completed: 2026-08-01
status: complete
---

# Phase quick-260801-o30 Plan 01: v0.8.1 Production Tauri CSP Regression Fix Summary

**Tauri CSP img-src now grants the data: scheme — unblocking cached Roto PNG and Physics Paint data-URL image decode in the packaged macOS app — with token-level release contract assertions locking the CSP shape against future edits.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-01T16:51:37Z
- **Completed:** 2026-08-01T16:57:29Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Fixed the v0.8.1 production CSP regression: `img-src 'self' asset: http://asset.localhost efxasset: blob: data: https://*` — the data: scheme is now granted so `data:image/png;base64,...` URLs (rotoCanvasFrames.ts, physicsPaintRotoAlphaMerge.ts, previewRenderer.ts, projectPaperRaster.ts, ThumbnailCache.ts, physicPaintStore.ts) decode in the packaged WebKit WebView
- Extended `app/src/releaseContract.test.ts` with a `Tauri CSP image data-url contract` suite: img-src token-level assertions for all 7 sources, a data:-exclusion lock on default-src/script-src/style-src/connect-src/media-src, and a source-text assertion pinning `ROTO_PNG_DATA_URL_HEADER = 'data:image/png;base64'` in rotoCanvasFrames.ts
- TDD executed cleanly: RED commit proves the img-src assertion failed against the broken config; GREEN commit is the single-directive config fix; full verification gate (suite, typecheck, build, release preflight) passes with versions pinned at 0.8.1

## Task Commits

Each task was committed atomically (Task 1 is TDD — RED test commit then GREEN fix commit):

1. **Task 1 (RED): CSP contract assertions** - `17fb555f` (test)
2. **Task 1 (GREEN): CSP img-src data: fix** - `65e86af2` (fix)
3. **Task 2: Full verification gate** - no commit (verification only, no file changes)

## Files Created/Modified

- `app/src-tauri/tauri.conf.json` - img-src directive gains the `data:` token; all other directives and keys byte-identical; version stays 0.8.1
- `app/src/releaseContract.test.ts` - widened tauriConfig type cast with `app.security.csp`, added `cspDirectiveTokens` helper, added `Tauri CSP image data-url contract` describe block (3 new tests, 8 total in file)

## Decisions Made

None beyond the plan's locked user decisions — data: granted only in img-src; token-level (never substring) CSP matching; version surfaces untouched at 0.8.1.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Built efx-physic-paint workspace dist in the fresh worktree**
- **Found during:** Task 2 (full vitest suite)
- **Issue:** 7 test files failed with `Failed to resolve entry for package "@efxlab/efx-physic-paint"` — the workspace package's `dist/` output does not exist in a fresh worktree (it is not tracked in git), so vite could not resolve its entry points
- **Fix:** Ran `pnpm --dir packages/efx-physic-paint run build` (tsup) to produce `dist/` locally; no tracked files changed, no scope expansion
- **Files modified:** none tracked (build output only)
- **Verification:** Full suite re-run passes 96 files / 1010 tests

---

**Total deviations:** 1 auto-fixed (Rule 3 blocking — environment setup only, zero tracked-file changes)
**Impact on plan:** No scope creep; the two owned files remain the only tracked modifications on this branch.

## Issues Encountered

None beyond the worktree dist bootstrap above.

## Verification Results

| Gate | Result |
|------|--------|
| `pnpm --dir app exec vitest run src/releaseContract.test.ts` (focused) | PASS — 8/8 tests (RED→GREEN proven: img-src data: assertion failed pre-fix) |
| `pnpm --dir app exec vitest run` (full suite) | PASS — 96 files, 1010 tests passed, 1 skipped, 101 todo |
| `pnpm --dir app run typecheck` | PASS |
| `pnpm build` | PASS — `app/dist/index.html` and all bundled assets emitted (pre-existing chunk-size warning only) |
| `bash -n scripts/macos-release.sh` | PASS |
| `bash scripts/macos-release.sh preflight` | PREFLIGHT PASS — read-only (executable probes, xcrun --find, codesign PATH resolution, config validation, private-asset name guards); no signing/notarization/credentialed operation attempted |
| Version surfaces 0.8.1 | CONFIRMED — app/package.json, tauri.conf.json, Cargo.toml, Cargo.lock, PRODUCT_VERSION all 0.8.1; `git diff f354866f..HEAD --stat` shows only the two owned files |

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Ready for the user's native packaged-app UAT: reopen cached Roto PNG frames, render Physics Paint cached previews, confirm Paint/Roto cells turn green after painting
- Branch `quick-260801-o30-csp-fix` holds the two atomic commits; SUMMARY.md left uncommitted for the orchestrator's docs commit
- No tag/push/sign/notarize performed, per constraints

## Self-Check: PASSED

- FOUND: app/src-tauri/tauri.conf.json (img-src contains `data:` token, verified via git diff and passing contract test)
- FOUND: app/src/releaseContract.test.ts (8 tests passing)
- FOUND commit: 17fb555f (test — RED)
- FOUND commit: 65e86af2 (fix — GREEN)

---
*Phase: quick-260801-o30*
*Completed: 2026-08-01*
