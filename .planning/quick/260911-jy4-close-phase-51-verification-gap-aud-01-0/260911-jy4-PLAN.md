---
phase: quick-260911-jy4
plan: 260911-jy4
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-read-only-audio-preview/51-SECURITY.md
  - .planning/phases/51-read-only-audio-preview/51-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUD-01, AUD-02, AUD-03, AUD-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "51-SECURITY.md exists with the 48-SECURITY.md schema (frontmatter phase/slug/status/threats_open/asvs_level/created; Trust Boundaries; Threat Register; Accepted Risks Log; Security Audit Trail; Sign-Off), >=10 T-51 threat rows covering the AUDIO-01 authority boundary, the seek funnel, the audible-scrub funnel, ownership/doubled audio, release-on-close and the efxasset CSP token; T-51-03 stays an accepted risk (AR-51-01); threats_open: 0 at asvs_level 1; every non-accepted mitigation cites a test file or contract that exists on disk."
    - "51-VERIFICATION.md exists with the 48/49/50 verification schema and an explicit per-requirement verdict for AUD-01..04 (SATISFIED or PARTIAL), each citing evidence on disk (51-VALIDATION.md rows, the AUD-01 authority contract test, quick SUMMARY coverage metadata, recorded native UAT records incl. 260905-ibd 2026-09-05 and Phase 41's 8-step packaged UAT, git-verified commit hashes) plus a machine-readable verdict summary line."
    - "The honesty rule holds: no requirement is marked SATISFIED on a claim that has no recorded evidence of any tier; every residual live/perceptual item (at minimum the packaged-build sync item from 51-VALIDATION.md) appears in human_verification or deferred, and every PARTIAL appears in gaps."
    - "Every commit hash cited in either record resolves via git at write time; no vitest/tsc/build/cargo command is executed; no source, test, or audit file is modified."
    - "REQUIREMENTS.md AUD-01..04 checkboxes and traceability rows mirror the 51-VERIFICATION verdicts exactly (SATISFIED incl. with-caveat → [x] + Complete; PARTIAL → [ ] + 'Partial — <note>' citing 51-VERIFICATION.md); the footer records both 2026-09-11 backfills; no other requirement row/section and no audit document is touched."
  artifacts:
    - .planning/phases/51-read-only-audio-preview/51-SECURITY.md
    - .planning/phases/51-read-only-audio-preview/51-VERIFICATION.md
    - .planning/REQUIREMENTS.md (AUD-01..04 checkbox + traceability rows + footer only)
  key_links:
    - "51-VERIFICATION.md 'Verdict summary: AUD-01=…; AUD-02=…; AUD-03=…; AUD-04=…' line ↔ REQUIREMENTS.md checkboxes/traceability rows: Task 3 derives wording mechanically from the on-disk verdicts, never from the pre-existing Pending rows."
    - "51-VERIFICATION.md security block ↔ 51-SECURITY.md written in Task 1: threat counts and accepted-risk IDs are read from that file, never re-invented."
    - "Audit (b2565065) verification_gaps + recommendation 2 ↔ the two artifacts on disk: the gap is closed by the artifacts existing and the tracker matching them; the audit document itself stays untouched."
---

<objective>
Close the Phase 51 verification-artifact gap flagged by the v1.0.0 milestone re-audit (b2565065): Phase 51 was quick-delivered (260902-cfa + 260902-cfa-amendments) with green unit coverage and a nyquist-compliant 51-VALIDATION.md, but 51-VERIFICATION.md and 51-SECURITY.md do not exist and REQUIREMENTS.md still lists AUD-01..04 as Pending.

- Produce `51-SECURITY.md` — a lightweight threat record for the read-only audio-monitoring scope, compiled from the Phase 41 register (41-SECURITY.md, T-41-01..T-41-15 closed), the 260902-cfa plan threat model (T-51-01..T-51-03), the AUDIO-01 authority contract test, and the recorded unit/native evidence.
- Produce `51-VERIFICATION.md` — schema-conformant to 48/49/50-VERIFICATION.md, with an explicit verdict for each AUD-01..04 backed by cited evidence and git-verified commit hashes.
- Reconcile the REQUIREMENTS.md AUD-01..04 checkboxes + traceability rows to the verdicts.

Purpose: the audit's `verification_gaps` block milestone closure while the evidence already exists — only the formal artifacts are missing. This quick turns recorded evidence into the two records and aligns the tracker without re-running anything.

Output: `51-SECURITY.md`, `51-VERIFICATION.md`, and an AUD-scoped reconciliation of `.planning/REQUIREMENTS.md`.

Guardrails: documentation only — no code changes, no test/typecheck/build/cargo re-runs, no server start. The milestone audit document (`.planning/v1.0.0-MILESTONE-AUDIT.md`) is read-only evidence and must not be modified. All artifacts in English. No ROADMAP.md edits.
</objective>

<execution_context>
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/workflows/execute-plan.md
@/Users/lmarques/Dev/efx-motion-editor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0.0-MILESTONE-AUDIT.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-VERIFICATION.md
@.planning/phases/48-internal-compositor-and-flattened-parent-result/48-SECURITY.md
@.planning/phases/51-read-only-audio-preview/51-CONTEXT.md
@.planning/phases/51-read-only-audio-preview/51-VALIDATION.md
@.planning/REQUIREMENTS.md

The 48-VERIFICATION.md / 48-SECURITY.md files above are the FORMAT REFERENCES (the 2026-09-11 backfill written by quick 260911-f2p) — mirror their frontmatter key set and section structure exactly; do not invent new sections. 49-VERIFICATION.md / 50-VERIFICATION.md share the same verification schema if a second reference is needed.

Read-only evidence (do NOT modify any of these):
- Quick records: `.planning/quick/260902-cfa-wire-the-ruler-seek-to-the-audio-monitor/` (PLAN.md threat_model + SUMMARY.md coverage metadata) and `.planning/quick/260902-cfa-amendments/260902-cfa-amendments-SUMMARY.md`.
- Post-delivery live UAT record: `.planning/quick/260905-ibd-perf-stop-per-frame-workflowstrip-action/260905-ibd-SUMMARY.md` (native UAT 2026-09-05: paint scrub audio audible without pressing Play — PASSED; main-studio scrub — PASSED; commits 269c2bd9, 1cf575e0, aea03d2a, c10af211).
- Inherited register + native oracle: `.planning/milestones/v0.9.0-phases/41-efx-paint-audio-preview-monitoring-toggle/41-SECURITY.md` (T-41-01..T-41-15 + T-41-SC, all closed) and `41-VERIFICATION.md` (8-step packaged native UAT APPROVED 2026-08-05: sync, scrub, loop, live edit, doubling guard, toggle, missing file, close release).
- Locked truth table: `.planning/milestones/v0.9.0-phases/41-efx-paint-audio-preview-monitoring-toggle/41-FRAME-AUDIO-TRUTH-TABLE.md`.
- Code/test anchors: `app/src/components/physic-paint/audio/` (efxPaintAudioMonitor.ts, efxPaintAudioPreviewContext.ts, efxPaintAudioPreviewStore.ts, efxPaintAudioOwnership.ts, efxPaintAudioPreview.test.ts, efxPaintAudioAuthorityContract.test.ts), `app/src/components/physic-paint/hooks/useRotoCachedPlayback.ts`, `useRotoNavigationCoordinator.ts`, `usePhysicsPaintRulerScrub.ts`, `app/src/components/physic-paint/PhysicsPaintStudio.tsx`, `app/src/releaseContract.test.ts` (connect-src efxasset pin). Read only what you need to ground a citation; do not run any of them.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write 51-SECURITY.md — lightweight threat record for the audio scope</name>
  <files>/Users/lmarques/Dev/efx-motion-editor/.planning/phases/51-read-only-audio-preview/51-SECURITY.md</files>
  <action>
Read (never execute): `.planning/phases/48-internal-compositor-and-flattened-parent-result/48-SECURITY.md` (FORMAT REFERENCE — frontmatter keys, Trust Boundaries / Threat Register / legend lines / Accepted Risks Log / Security Audit Trail / Sign-Off, Status column included), `.planning/milestones/v0.9.0-phases/41-efx-paint-audio-preview-monitoring-toggle/41-SECURITY.md` (inherited register), `.planning/phases/51-read-only-audio-preview/51-CONTEXT.md` + `51-VALIDATION.md`, `.planning/quick/260902-cfa-wire-the-ruler-seek-to-the-audio-monitor/260902-cfa-PLAN.md` (threat_model: T-51-01 Tampering seek-while-playing restart / T-51-02 Spoofing seek target / T-51-03 DoS playback-frame flicker accepted / T-51-SC installs), both 260902-cfa SUMMARYs, and the audio module + test files you cite.

Write `51-SECURITY.md` with the Write tool. Frontmatter: `phase: 51`, `slug: read-only-audio-preview`, `status: verified`, `threats_open: 0`, `asvs_level: 1`, `created: 2026-09-11` (keep the 48 comment convention: threats_open counts only OPEN threats at/above `workflow.security_block_on` = high).

Body, mirroring 48-SECURITY.md's structure:
1. `# Phase 51 — Security` + the one-line contract quote.
2. `## Trust Boundaries` — table with Boundary / Description / Data Crossing. Cover at minimum: main editor → child payload (validated closed-key, revisioned read-only copy), child → main editor ownership events (emitTo window-label targeting + origin check), child monitor → shared `audioEngine` singleton (no second context, D-08), audio bytes → `efxasset://` single-token CSP grant, seek/scrub input → monitor dispatch funnel, close/unmount → engine release.
3. `## Threat Register` — table with Threat ID / Category / Component / Severity / Disposition / Mitigation / Status, at least 10 rows, using the T-51-01..T-51-03 IDs established by the 260902-cfa plan (T-51-03 stays `accept`) and extending sequentially for the rest. Required coverage (one row each at minimum): child audio module importing main-editor audio state (`audioStore`/`timelineStore`/`playbackEngine`) — mitigate via `app/src/components/physic-paint/audio/efxPaintAudioAuthorityContract.test.ts` (production 4-module scan + detector control test, 2 tests); seek-while-playing double-dispatch/orphaned audio — mitigate via the single funnel (`playAtCursor` = stopAll + re-dispatch) + the seek tests in `useRotoCachedPlayback.test.ts`; out-of-range seek target — mitigate via the `getFrames()` enumeration check + silent `positionedAt` fallback; seek-while-playing flicker — accept (T-51-03); audible-scrub stopAll spam/crackle — mitigate via `EFX_PAINT_AUDIO_SCRUB_THROTTLE_MS` 120ms + 4-frame snippet + the audible-scrub tests in `efxPaintAudioPreview.test.ts`; muted scrub breaking the locked silent-scrub rule — mitigate via the toggle check inside `scrubAt` so a muted scrub never sets the D-14 `toggleSilenced` flag; scrub snippet outliving release / loop-wrap — mitigate via `scrubEnd` on the single stop funnel + `loopStartIndexRef`; Play re-anchoring at the range start instead of the cursor — mitigate via `getCurrentAppFrame` + range-start clamp (D-01 tests); read-only guarantee / no source-audio mutation — mitigate via the session-local toggle store (no persistence imports) + the 41-VERIFICATION AUDIO-05/toggle evidence; doubled audio across windows (first-player-wins) — mitigate via `efxPaintAudioOwnership.ts` claim/release + suppression note + Phase 41 UAT step 5; audio-resource leak across open/close — mitigate via `release()` on the close path + unmount (`efxPaintAudioOwnership.ts` unmount listener) + release tests (a)-(d); CSP broadening creep — mitigate via the unchanged `connect-src` `efxasset:` single-token pin in `app/src/releaseContract.test.ts`. Include `T-51-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in the phase — no package-legitimacy gate required | closed`.
4. The two legend lines (status/severity/disposition) copied in the 48 style.
5. `## Accepted Risks Log` — AR-51-01 → T-51-03 (playback-frame flicker during the seek await; accepted in the 260902-cfa threat model 2026-09-02; UX artifact, canvas re-anchors at the target).
6. `## Security Audit Trail` — one honest row: `| 2026-09-11 | N | N-1 closed | 1 accepted (low, T-51-03 → AR-51-01) | backfill compile from the phase's recorded evidence (documentation-only; no audit or test command executed) |`. State explicitly that this record compiles Phase 41's closed register + the 260902-cfa threat model + the delivered tests, and that no new audit run occurred.
7. `## Sign-Off` — the 48 checklist (`disposition` on every threat, accepted risks logged, `threats_open: 0`, `status: verified`) with `**Approval:** compiled 2026-09-11 from recorded evidence`.

Rules: every non-accepted mitigation must name a test file/contract that exists on disk (verify the path with `ls` before citing — no invented test names; describe assertions generically unless you grep the exact `it(...)` title). A threat whose only support is an unrecorded manual scenario must be listed in the Accepted Risks Log or marked `open — below high threshold (non-blocking)` with its reason — never silently closed. Do not modify any other file.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && f=.planning/phases/51-read-only-audio-preview/51-SECURITY.md && test -f "$f" && for k in phase slug status threats_open asvs_level created; do grep -q "^$k:" "$f" || { echo "MISSING $k"; exit 1; }; done && grep -q "^threats_open: 0" "$f" && grep -q "^asvs_level: 1" "$f" && [ "$(grep -cE '^\| T-51-[0-9]{2} ' "$f")" -ge 10 ] && grep -q "^| T-51-03 " "$f" && grep -q "AR-51-01" "$f" && grep -q "efxPaintAudioAuthorityContract.test.ts" "$f" && grep -q "releaseContract.test.ts" "$f" && grep -q "efxPaintAudioPreview.test.ts" "$f" && grep -q "Trust Boundaries" "$f" && grep -q "Threat Register" "$f" && grep -q "Accepted Risks Log" "$f" && grep -q "Security Audit Trail" "$f" && grep -q "Sign-Off" "$f" && echo "51-SECURITY OK"</automated>
  </verify>
  <done>51-SECURITY.md exists with the 48 schema; >=10 T-51 rows cover the authority boundary, seek/scrub funnels, ownership, release, CSP and installs; T-51-03 is the recorded accepted risk (AR-51-01); every cited mitigation names an existing file; the audit trail states the compiled (no-audit-run) basis; threats_open: 0; no other file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Write 51-VERIFICATION.md — per-AUD verdicts from the recorded evidence, hashes git-verified</name>
  <files>/Users/lmarques/Dev/efx-motion-editor/.planning/phases/51-read-only-audio-preview/51-VERIFICATION.md</files>
  <action>
Read (never execute): all of `.planning/phases/51-read-only-audio-preview/` (51-CONTEXT.md decisions D-01..D-04, 51-VALIDATION.md per-task map + manual-only table + validation audit note), both quick records (`.planning/quick/260902-cfa-*/260902-cfa-PLAN.md` + `-SUMMARY.md`, `.planning/quick/260902-cfa-amendments/260902-cfa-amendments-SUMMARY.md` — incl. their `coverage:` metadata and `requirements-completed: [AUD-01, AUD-02, AUD-03, AUD-04]`), the 260905-ibd SUMMARY (native UAT 2026-09-05), `41-VERIFICATION.md` (8-step packaged UAT approved 2026-08-05) + `41-SECURITY.md`, the 51-SECURITY.md written in Task 1, the AUD-01 contract test `app/src/components/physic-paint/audio/efxPaintAudioAuthorityContract.test.ts`, and the audit's Phase 51 blocks (`.planning/v1.0.0-MILESTONE-AUDIT.md` lines with the AUD-01..04 row, the `verification_gaps` entry, and Cross-Phase Integration item 6 "Audio ↔ cursor — WIRED" with its file:line refs).

Before citing any hash, verify it with `git log -1 --format="%h %ad %s" --date=short <hash>`. Pre-verified candidate set (2026-09-11): d3427e97, be9c3ec3, 8f2340dc, 7af5382f (260902-cfa seek wiring); 746803b4 (cfa docs); 13a98b89, d326b1f5, 9fed9875, 982a343e (amendments); 95523517, 2ee87505 (amendments docs); 83a1d280 (phase-51 delivered marker); 22094f02, e57a0a04 (validation strategy + authority contract); c10af211, 1cf575e0, aea03d2a, 269c2bd9 (260905-ibd: launch-hydration prepare fix, scrub drag-gate, diagnostics, revert). Re-verify each one you cite and match the subject to the claim; drop or re-derive anything that does not resolve (summaries can carry typos — 260911-f2p recorded one).

Write `51-VERIFICATION.md` with the Write tool, mirroring the 48-VERIFICATION.md schema exactly:
- Frontmatter keys in order: `phase: 51-read-only-audio-preview`, `verified: <ISO timestamp now>`, `status`, `score`, `behavior_unverified: 0`, `overrides_applied: 0`, `gaps`, `deferred`, `behavior_unverified_items`, `coincidental_reliance_items`, `human_verification`.
- Body: title; Phase Goal (ROADMAP.md Phase 51 goal line) / Verified / Status / `**Re-verification:** No — initial verification (backfill 2026-09-11 from the phase's recorded evidence; no test re-run, no source modification)`; `## Goal Achievement` → `### Observable Truths (ROADMAP Success Criteria)` (the 4 criteria, each mapped 1:1 to AUD-01..04, with ✓ VERIFIED / PARTIAL + evidence); `### Required Artifacts`; `### Key Link Verification`; `### Data-Flow Trace (Level 4)`; `### Behavioral Spot-Checks` (RECORDED results only — 51-VALIDATION.md 8/8 rows green; suite snapshots 3306 (cfa) → 3320 (amendments) → 3471 (260905-ibd) → 3540 passed / 1 skipped / 101 todo + tsc clean (validation audit 2026-09-11); Phase 41 8-step packaged UAT 2026-08-05; 260905-ibd native UAT 2026-09-05 — never re-run anything); `### Requirements Coverage` (AUD-01..04, each with Status + Evidence); `### Anti-Patterns Found`; `### Human Verification Required`; `### Gaps Summary`.

Verdict rules — a requirement may only be SATISFIED on evidence of these tiers:
- T1 automated: the 51-VALIDATION.md per-task rows (all green) + the quick summaries' coverage metadata.
- T2 recorded native UAT exercising the same code path: the Phase 41 8-step packaged UAT (41-VERIFICATION.md truth rows 2/3/4/6/13: sync+scrub, seek/loop/stop, toggle no-mutation, missing-asset non-blocking, close release) and the 260905-ibd native UAT 2026-09-05 (paint scrub audio audible without Play — the launch-hydration prepare gap it exposed was fixed in c10af211). Note honestly that both predate the v1.0 multi-track document; the audio module is unchanged since Phase 41 except the seek/scrub funnels delivered by the quicks.
- T3 structural/contract: `efxPaintAudioAuthorityContract.test.ts` (2 tests) + the audit's Cross-Phase Integration item 6 (WIRED).

Per-requirement:
1. `AUD-01` (main-editor audio authoritative + read-only): SATISFIED if T3 proves the boundary (contract test's 4-production-module scan + detector control) and the read-only structure holds (closed-key validated revisioned payload, session-local toggle store with no persistence imports, no source writes). Cite the contract test path, 51-VALIDATION row 51-VAL-01, and the audit item 6 refs. Record the 51-VALIDATION audit caveat (the contract pins the three named modules; other main-side imports — the ownership event constant, the shared `audioEngine` — remain permitted by the documented boundary) in `coincidental_reliance_items`, and the live cross-window first-player-wins re-check (Phase 41 UAT step 5 only) in `human_verification` if unrecorded on v1.0.
2. `AUD-02` (one shared application-frame cursor; monitoring follows it): SATISFIED on T1 + T3 — frame identity is the locked truth-table law; `getCurrentAppFrame` feeds start/seek/scrub; cite the audit item 6 line refs, the D-01 re-anchor tests (51-VALIDATION rows am-01/am-04) and the loop-wrap-at-scrub row.
3. `AUD-03` (toggle On/Off does not mutate source audio; closing releases resources): SATISFIED only if the toggle store's no-persistence/no-mutation structure (T1 + 41-VERIFICATION AUDIO-05/toggle truth) AND the release path (release() on close + unmount, release tests (a)-(d), 41-VERIFICATION truth 6) are cited. The live v1.0 toggle/close re-check, if unrecorded, goes to `human_verification` (Phase 53 native UAT step 6 adjacency).
4. `AUD-04` (sync across seek, loop, pause, resume, stop): SATISFIED on T1 (seek-restart / silent re-anchor / loop-wrap / stop-funnel dispatch math, pause→stop per D-01 CONTEXT, resume = Play re-anchor) + T2 (Phase 41 UAT steps 1-3; 260905-ibd 2026-09-05 audible-scrub PASSED). The one clause with no recorded live result is the perceptual multi-track re-run on the v1.0 document — if you judge the requirement's transport matrix proven by the recorded unit + native evidence on the unchanged monitor path, mark SATISFIED and list that re-run in `human_verification` (it is already the Phase 53 step-6 packaged item); otherwise mark PARTIAL with that note.

Tie-breaker: when a clause of the requirement text itself lacks recorded proof of any tier, mark the requirement PARTIAL — do not inflate. `status: passed` only if all four are SATISFIED (with-caveat counts); `human_needed` if any is PARTIAL. Populate `gaps` with every PARTIAL and `human_verification` with every residual live item — including at minimum the 51-VALIDATION.md manual-only packaged-build sync row (explicitly assigned to Phase 53 native UAT step 6).

Also required in the record: a trailing machine-readable line in Requirements Coverage — `Verdict summary: AUD-01=<SATISFIED|PARTIAL>; AUD-02=…; AUD-03=…; AUD-04=…` — that Task 3 reads. Anti-Patterns must record, with references: the post-delivery launch-hydration prepare gap on the Phase 51 path (monitor `prepare()` ran only on Play/push, so idle scrub was silent in Play-less sessions — fixed in c10af211, quick 260905-ibd) and the accepted T-51-03 flicker; the 260905-ibd revert history (269c2bd9) is context, not a phase-51 defect. State in the Requirements Coverage section that the phase maps to AUD-01..04 and was delivered via quicks, with no phase-local PLAN/SUMMARY set.

Constraints: cite file paths / line refs / git-verified hashes for every claim; no vitest/tsc/build/cargo; do not edit any source, test, or audit file; write only this one file.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && f=.planning/phases/51-read-only-audio-preview/51-VERIFICATION.md && test -f "$f" && for k in phase verified status score behavior_unverified overrides_applied gaps deferred behavior_unverified_items coincidental_reliance_items human_verification; do grep -q "^$k:" "$f" || { echo "MISSING $k"; exit 1; }; done && grep -q "^overrides_applied: 0" "$f" && grep -qE "^status: (passed|human_needed)" "$f" && for a in 01 02 03 04; do grep -q "AUD-$a=" "$f" || { echo "MISSING verdict AUD-$a"; exit 1; }; done && grep -q "efxPaintAudioAuthorityContract.test.ts" "$f" && grep -q "51-VALIDATION" "$f" && grep -q "51-SECURITY" "$f" && grep -q "260905-ibd" "$f" && grep -q "41-VERIFICATION" "$f" && grep -q "c10af211" "$f" && grep -qE "be9c3ec3|13a98b89|d326b1f5" "$f" && echo "51-VERIFICATION OK"</automated>
  </verify>
  <done>51-VERIFICATION.md exists with the full schema; each AUD-01..04 has a verdict grounded in cited, on-disk evidence with git-verified hashes; the authority contract test, the validation strategy, the security record, both native UAT records and the post-delivery launch-hydration fix are cited; residual live items and any PARTIAL are explicit (human_verification / gaps); the verdict-summary line is present; no test was run and no other file changed.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md AUD-01..04 checkboxes + traceability rows to the verdicts</name>
  <files>/Users/lmarques/Dev/efx-motion-editor/.planning/REQUIREMENTS.md</files>
  <action>
Read the `Verdict summary:` line and the AUD-01..04 rows in `.planning/phases/51-read-only-audio-preview/51-VERIFICATION.md` (written in Task 2). Then use Edit (scoped replacements — never Write the whole file) on `.planning/REQUIREMENTS.md`:

1. `### AUD — Audio Preview (Phase 7)` checkboxes (currently `- [ ]` on the four AUD lines): SATISFIED (including satisfied-with-recorded-caveat) becomes `- [x]`; PARTIAL stays `- [ ]`. Do not alter the requirement text after the checkbox.
2. Traceability rows `| AUD-0N | Phase 51 | Pending |`: SATISFIED becomes `| AUD-0N | Phase 51 | Complete |`; PARTIAL becomes `| AUD-0N | Phase 51 | Partial — <one-line reason> |` citing 51-VERIFICATION.md (e.g. an unrecorded perceptual multi-track re-run). Derive every wording from the on-disk verdicts — never from the pre-existing Pending rows.
3. Footer (the `*Last updated: …*` line at the bottom): replace with a single line recording both 2026-09-11 backfills, preserving the existing formatting, e.g. `*Last updated: 2026-09-11 (CMP-01..06 + AUD-01..04 verification records backfilled — see 48-VERIFICATION.md, 52.1-VERIFICATION.md, 51-VERIFICATION.md)*`.

Hard scope limits: do NOT touch any other requirement row or section (DOC/TRK/TML/CMP/BKG/REF/RVL/ACC/FUT), do NOT edit `.planning/v1.0.0-MILESTONE-AUDIT.md`, do NOT edit ROADMAP.md, do NOT add traceability rows for other phases. If all four verdicts are SATISFIED the checkbox/traceability edits are the four `[x]` flips + four `Complete` rows and the footer bump — nothing else.
  </action>
  <verify>
    <automated>cd /Users/lmarques/Dev/efx-motion-editor && V=.planning/phases/51-read-only-audio-preview/51-VERIFICATION.md && R=.planning/REQUIREMENTS.md && SAT=$(grep -oE 'AUD-0[1-4]=(SATISFIED|PARTIAL)' "$V" | grep -c 'SATISFIED') && CHK=$(grep -c '^- \[x\] \*\*AUD-0' "$R") && [ "$SAT" -eq "$CHK" ] && COMP=$(grep -cE '^\| AUD-0[1-4] \| Phase 51 \| Complete \|' "$R") && [ "$COMP" -eq "$SAT" ] && grep -q "51-VERIFICATION.md" "$R" && D=$(git diff HEAD -- "$R") && if printf '%s\n' "$D" | grep -E '^[-+]' | grep -vE '^[-+][-+]' | grep -vE 'AUD-0|Last updated' | grep -q .; then echo "UNEXPECTED DIFF — non-AUD lines changed"; exit 1; fi && git diff HEAD --stat -- "$R" && echo "REQUIREMENTS RECONCILED"</automated>
  </verify>
  <done>REQUIREMENTS.md AUD checkboxes and traceability rows match the 51-VERIFICATION verdicts exactly (checked+Complete count equals the SATISFIED count); footer records both backfills; the diff touches only the four AUD checkbox lines, the four AUD traceability rows and the footer; the audit document and ROADMAP.md are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only backfill: no code, no network, no installs, no runtime surface. The only integrity surface is the truthfulness of the compiled records and the tracker's fidelity to them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260911-jy4-01 | Repudiation | Cited evidence in 51-SECURITY.md / 51-VERIFICATION.md | medium | mitigate | Every commit hash is re-verified with `git log -1` before citation; every claim cites a file path / line reference / test file present in the repo; the security record's audit trail states the compiled (no-audit-run) basis. |
| T-260911-jy4-02 | Integrity | AUD verdict inflation / silent SATISFIED | high | mitigate | Explicit evidence-tier rules + the tie-breaker "PARTIAL when a requirement clause lacks recorded proof of any tier"; residual live items must land in `human_verification`/`gaps`; Task 2's verify gates the verdict line and the cited evidence anchors. |
| T-260911-jy4-03 | Tampering | REQUIREMENTS.md reconciliation scope | low | mitigate | Scoped Edit only; Task 3's verify asserts the diff contains no non-AUD line changes and counts checkboxes against the on-disk verdicts. |
| T-260911-jy4-04 | Tampering | Milestone audit document | low | mitigate | Read-only by instruction; the final scope gate asserts `git diff --name-only -- .planning/v1.0.0-MILESTONE-AUDIT.md` is empty. |
| T-260911-jy4-SC | Tampering | npm/pnpm/cargo installs | low | accept | No package installs in this plan — no package-legitimacy gate required. |
</threat_model>

<verification>
- Task 1 grep gate: 51-SECURITY.md exists, full frontmatter key set, `threats_open: 0`, `asvs_level: 1`, >=10 T-51 rows, T-51-03 + AR-51-01, the authority/CSP/preview test paths cited, all five section headers present.
- Task 2 grep gate: 51-VERIFICATION.md exists, all 11 frontmatter keys, `overrides_applied: 0`, `status: passed|human_needed`, the AUD-01..04 verdict lines, the authority contract test + 51-VALIDATION + 51-SECURITY + 260905-ibd + 41-VERIFICATION citations, c10af211 and at least one 260902-cfa feature hash cited.
- Task 3 grep gate: checked-AUD count == SATISFIED count; Complete-row count == SATISFIED count; footer cites 51-VERIFICATION.md; `git diff HEAD` on REQUIREMENTS.md contains no non-AUD/non-footer line.
- Citation gate: every hash cited in either record resolves (`git cat-file -e <hash>` spot-check per cited hash).
- Scope gate: `git status --porcelain` shows only the three planned files (plus the quick SUMMARY created at close); `git diff --name-only -- '*.ts' '*.tsx' '*.rs' '*.css'` is empty; the audit document diff is empty.
- No test, typecheck, build, cargo, or server command is executed anywhere in this quick (documentation-only guardrail).
</verification>

<success_criteria>
- Both Phase 51 artifacts exist, schema-conformant, with per-requirement verdicts (AUD-01..04) and a threat register each backed by cited, on-disk evidence; residual live items and any PARTIAL are explicitly recorded, never silently waved through.
- Every cited commit hash resolves via git; no test was re-run; no source/test/audit file changed.
- REQUIREMENTS.md AUD-01..04 checkboxes + traceability wording mirror the 51-VERIFICATION verdicts; footer records both backfills; no other requirement touched.
- The audit's `verification_gaps` entry for phase 51 and recommendation 2 have their corresponding artifacts on disk, with the audit document itself untouched.
</success_criteria>

<output>
Create `.planning/quick/260911-jy4-close-phase-51-verification-gap-aud-01-0/260911-jy4-SUMMARY.md` when done
</output>
